## Shared Layer Rules (shared/**)

- **Strict Isolation**: `shared/` must NEVER import from `@/frontend/**`, `@/backend/**`, or `@/app/**`.
- **Translations**: Translation schemas and locale dictionaries belong in `shared/locale/`.
