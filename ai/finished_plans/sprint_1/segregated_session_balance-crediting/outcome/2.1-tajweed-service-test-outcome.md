# Task 2.1 — Tajweed-Lane Service-Level Credit Test (close REQ-017)

**Date:** 2026-09-12 · **Status:** PASS · **Requirements:** REQ-017, REQ-030

## Summary

Added one executable Tier-2 proof that a confirmed webhook on a Tajweed-laned plan credits `balance_tajweed` by exactly the plan's `sessionCount` while hifz/reviews/trial stay byte-identical. This retires the file-header assumption that previously claimed "hifz/tajweed byte-identical" on faith: the header note now names the REVIEWS- and TAJWEED-lane activations as proven members of the credit path (every `subscription_credit_lane` pgEnum member is routable by an executing test).

## Files Modified

1. `backend/services/billing/subscription-activation.service.test.ts`
   - New test in the existing Tier 2 describe (`settlement quarantine + exact credit`), placed after the reviews-lane sibling: `tajweed-lane activation: confirmed event credits balance_tajweed by sessionCount — hifz/reviews/trial untouched`. It uses `provisionPendingPair` with `{ balanceLane: SubscriptionCreditLane.Tajweed, sessionCount: 6 }`, drives `SubscriptionActivationService.processWebhookEvent` with a `confirmed` event inside `runInRollback` (tx passed through as `outerTx`), spies the notification insert + post-commit publish seams (mocked — no real notification rows, no real transport), and asserts: `balance_tajweed` grew by exactly 6 (before=0 → after=6), hifz/reviews/trial byte-identical, subscription active, payment paid, one notification insert, one publish.
   - Header coverage-map note (~:39) rewritten minimally: "(hifz/tajweed byte-identical — every pgEnum member is routable)" → names both REVIEWS- and TAJWEED-lane activations as credited proofs. One-paragraph change, no plan-artifact references in code comments.

2. `tasks.md` — flipped `[ ]` → `[x]` on lines 50, 53, 54, 55, 56 (2.1, 2.1.QL, 2.1.TE, 2.1.SEC, 2.1.SR/IV). No other lines touched.

## Files NOT Modified (and why)

- `backend/services/billing/subscription-activation.service.ts` — the lane mapper (`:224-242`) already routes Tajweed correctly; no production change needed or permitted.
- `backend/db/test/entity-setup.ts`, repo files, schema files — fixtures reuse existing helpers verbatim; no new helpers required.
- Existing tests in the target file — untouched; Tier 1/3/4 cases and the other Tier 2 cases remain byte-identical (no coverage weakened).

## Verification Results

| Check | Command | Result |
|---|---|---|
| Sub-loop (tsgo → oxlint → biome → lint:type-aware → duplicates) | `~/.bun/bin/bun --env-file=.env.test run scripts/health/sub-loop.ts backend/services/billing/subscription-activation.service.test.ts --lifecycle duplicates` | **exit 0** — all stages passed (duplicates stage reported "outside jscpd scan scope") |
| Suite | `~/.bun/bin/bun --env-file=.env.test run test/scripts/run-test.ts backend/services/billing/subscription-activation.service.test.ts` | **18 pass / 0 fail** (was 17 pass / 0 fail at baseline), 169 expect() calls, incl. the new tajweed case at ~14ms |

Rule files read (sub-loop printed + task list): `backend/services/AGENTS.md`, `backend/AGENTS.md`, `.agents/instructions/backend.instructions.md`, `.agents/instructions/tests.instructions.md`, plus root `AGENTS.md` and `backend/db/test/AGENTS.md` for the DB-test rules the file's header defers to.

## Tier Check (2.1.TE)

- The test joins the existing Tier-2 boundary block (exact-delta / lane-routing boundaries), matching the reviews-lane sibling's structure 1:1 (describe grouping, `runInRollback` wrapper, before/after balance reads, lane-agnostic unit assertions).
- External seams mocked exactly as the siblings: `NotificationRepository.createReturning` and `NotificationEngine.publishReceipts` spied via the file's `spyNotificationSeams` helper — no real notification rows, no transport, no network.
- Tier 1/3/4 patterns untouched.

## Security (2.1.SEC)

No caller-controlled lane/amount path is introduced. The credit amount derives server-side from the plan row (`plan.sessionCount`); the lane derives from the plan's stored `balance_lane` column mapped fail-closed by the service. The test adds no input surface — it constructs the plan fixture via the test helper and feeds only the stored payment reference + amount through the webhook event.

## Self-Review (2.1.SR) + Instruction Verification (2.1.IV)

- Race conditions: none added (single serialized `runInRollback` delivery; the concurrent-race case remains gated to real PostgreSQL via `testOnRealPostgres`).
- Clean comments: no REQ/task/phase ids in code comments; header note updated minimally.
- Scope boundary: only the test file + tasks.md checkboxes + this outcome file touched.
- Schema/type alignment: `SubscriptionCreditLane.Tajweed` enum member (runtime value import, no string literals); assertions use `$inferSelect` field names `balanceHifz/balanceTajweed/balanceReviews/balanceTrial` matching `backend/db/schema/students/students.ts:24-27`.
- House rules honored: fixtures only from `entity-setup.ts`; no seed queries; no `expect(...).rejects.toThrow()`; no `console.*`; `@/` alias imports; outcome-based assertions (no pinned throws).

## Carry-Forward Knowledge

- All three credit lanes (hifz/tajweed/reviews) now each have an executable service-level credit proof in this file; Tasks 2.2/2.3 (journey Tajweed leg, GQL INSUFFICIENT_BALANCE pin) remain the open gap closures.
- jscpd's cross-file duplicates scan does not cover this path (reported "outside jscpd scan scope"), but the new test deliberately mirrors sibling structure rather than introducing a novel pattern.
- Test runs require `--env-file=.env.test` (unchanged from Phase 0).

## Cross-File Dependencies / Blockers

None. No fixes required in files outside the assigned scope.
