# Task 3.1 — DBML Parity Sync (closes REQ-028 / gap G4)

**Date:** 2026-09-12 · **Scope:** `db/schema.dbml` only (docs artifact). No Drizzle schema, migration, or other source file was touched — and none needed to be: the drift was purely on the DBML side.

## Summary

Synced `db/schema.dbml` with the Drizzle ground truth so the ER diagram no longer drifts from `backend/db/schema/`. Added the `subscription_credit_lane` enum block, the trial columns + four CHECKs on `students`, `balance_lane` on `plans`, and the partial unique index on `subscriptions`. Every line was re-verified against the Drizzle source (and, for constraint/index names and SQL predicates, against the migration SQL in `backend/drizzle/`).

## Files Modified

- `db/schema.dbml` — the only code-artifact edit (10 lines added/touched: 1 enum block, 5 student column lines, 1 plans column line, 1 subscriptions index entry).
- `ai/plans/sprint_1/segregated_session_balance-crediting/tasks.md` — flipped the 3.1 / 3.1.QL / 3.1.TE checkboxes.
- `ai/plans/sprint_1/segregated_session_balance-crediting/outcome/3.1-dbml-sync-outcome.md` — this file (created).

## Files NOT Modified (and why)

- `backend/db/schema/students/students.ts`, `backend/db/schema/billing/plans.ts`, `backend/db/schema/billing/subscriptions.ts`, `backend/db/schema/enums.ts` — ground truth, already correct; task scope explicitly forbids touching them.
- `backend/drizzle/**` — migration SQL already contains exactly the constraints/columns mirrored here; no schema change was required, so no new migration.
- `scripts/health/sub-loop.ts` / `sub-loop-checks.ts` — see Quality Gate section: the oxlint stage exits 1 on `.dbml` files ("No files found to lint") and there is no extension-aware skip in the script. Fixing that would be a cross-file change outside this task's scope; recorded below as a carried-forward note, not a blocker (the task's own carve-out treats "nothing to lint for .dbml" as pass).

## Verification — Quality Gate (`3.1.QL`)

Command: `bun --env-file=.env.test run scripts/health/sub-loop.ts db/schema.dbml --lifecycle duplicates`

Result: **exit 1 at the oxlint stage — but solely because oxlint reports "No files found to lint" for a `.dbml` path** (oxlint only lints JS/TS). Per the task contract ("if the script reports nothing to lint for .dbml, that counts as pass — note it"), this counts as pass. Evidence that every stage is a genuine no-op on `.dbml`:

- **tsgo:** ✅ passed ("no errors for db/schema.dbml").
- **oxlint:** "No files found to lint. … Finished in 110ms on 0 files" (oxlint exits 1 on an empty file set; not a diagnostic about the file).
- **biome:** `bunx biome check db/schema.dbml` → exit 0, "These paths were provided but were ignored: db/schema.dbml".
- **duplicates:** `bunx jscpd db/schema.dbml` → 0 files analyzed, "Found 0 clones".
- **Instruction files (IV):** the sub-loop printed "No applicable instruction files found for this file"; the only AGENTS.md listed was the repo-root one (read).

There is no dedicated DBML validator in the repo (verified: `scripts/validate-mermaid.ts` absent, no `dbml` script in package.json), so parity was enforced by re-reading the Drizzle source per edited line, quoted below.

## Per-Line Parity Audit (every added/changed DBML line ↔ Drizzle/migration anchor)

### Enum block (`db/schema.dbml:79-83`)

```dbml
Enum subscription_credit_lane {
  hifz
  tajweed
  reviews
}
```

Anchor: `backend/db/schema/enums.ts:65` — `export const subscriptionCreditLane = pgEnum("subscription_credit_lane", ["hifz", "tajweed", "reviews"]);`
Migration: `backend/drizzle/20260907182455_ancient_scarecrow/migration.sql:3` — `CREATE TYPE "subscription_credit_lane" AS ENUM('hifz', 'tajweed', 'reviews');`

### `students` table (`db/schema.dbml:224-239`)

| DBML line | Drizzle / migration anchor |
|---|---|
| `balance_hifz integer [default: 0, check: `balance_hifz >= 0`, note: 'الحفظ والتسميع (students_balance_hifz_check)']` | `backend/db/schema/students/students.ts:24` (`integer("balance_hifz").default(0)` — nullable, so no `not null`) + `:42` `check("students_balance_hifz_check", sql\`${t.balanceHifz} >= 0\`)`; migration `20260904084151_omniscient_karen_page/migration.sql:274` |
| `balance_reviews integer [default: 0, check: ... (students_balance_reviews_check)]` | `students.ts:25` + `check` at `:43`; migration `:275` |
| `balance_tajweed integer [default: 0, check: ... (students_balance_tajweed_check)]` | `students.ts:26` + `check` at `:44`; migration `:276` |
| `balance_trial integer [not null, default: 0, check: `balance_trial >= 0`, note: 'Trial credits (students_balance_trial_check)']` | `students.ts:27` — `integer("balance_trial").notNull().default(0)`; `check` at `:45`; migration `:266` (`"balance_trial" integer DEFAULT 0 NOT NULL`) + `:277` (`CONSTRAINT "students_balance_trial_check" CHECK ("balance_trial" >= 0)`) |
| `trial_granted_at timestamp [note: 'When the trial credits were granted']` | `students.ts:28` — `timestamp("trial_granted_at")` (nullable, no default); migration `:267` (`"trial_granted_at" timestamp`) |

Balance lanes were already present in the DBML block; they gained the `check:` predicates + real constraint names (the file's existing CHECK idiom is the column-level `check:` attribute, e.g. `teacher.average_rating` at `db/schema.dbml:208`, `wallet.balance` at ~`:303`). `check:` attributes naming is done via the adjacent `note:` so both the exact constraint name and the `>= 0` predicate appear on each line.

### `plans` table (`db/schema.dbml:293`)

`balance_lane subscription_credit_lane [note: 'Lane credited on activation; NULL lanes fail closed (PLAN_LANE_UNCONFIGURED quarantine)']`

Anchor: `backend/db/schema/billing/plans.ts:29` — `balanceLane: subscriptionCreditLane("balance_lane")` (no `.notNull()`, nullable); column-level behavior note mirrors `plans.ts:14-16` JSDoc ("purchase flows treat a NULL lane as unconfigured and fail closed instead of guessing") and the `PLAN_LANE_UNCONFIGURED` fail-closed purchase path in `backend/services/billing/subscription-purchase.service.ts:272-278`. Migration: `20260907182455_ancient_scarecrow/migration.sql:20` — `ALTER TABLE "plans" ADD COLUMN IF NOT EXISTS "balance_lane" "subscription_credit_lane";` (nullable).

### `subscriptions` table (`db/schema.dbml:267-271`)

```dbml
indexes {
  user_id
  plan_id
  payment_reference [unique, note: 'subscriptions_payment_reference_unique: partial unique index WHERE payment_reference IS NOT NULL']
}
```

Anchor: `backend/db/schema/billing/subscriptions.ts:53-55` —
```ts
uniqueIndex("subscriptions_payment_reference_unique")
  .on(t.paymentReference)
  .where(sql`${t.paymentReference} IS NOT NULL`),
```
Migration: `20260907182455_ancient_scarecrow/migration.sql:22` — `CREATE UNIQUE INDEX IF NOT EXISTS "subscriptions_payment_reference_unique" ON "subscriptions" ("payment_reference") WHERE "payment_reference" IS NOT NULL;`. DBML has no WHERE-predicate syntax on index entries, so the predicate and the exact index name are expressed in the entry note (the file has no partial-index precedent; the `[unique, note: ...]` idiom matches the file's existing plain `indexes {}` block style).

## Self-Review (SR)

- ✅ Scope: only `db/schema.dbml` (+ plan checkboxes + this outcome file). `git status` confirms no other source files modified by this task.
- ✅ No invented schema facts: every column type/default/nullability, constraint name, and index predicate above quotes a live Drizzle line or migration SQL line.
- ✅ No plan-artifact ids in the DBML itself (no REQ-/task references; `PLAN_LANE_UNCONFIGURED` is a runtime error-code constant, not a plan artifact — it names observable purchase flow behavior). Existing `A.x/B.x/C.x/INV-Pxx` design-doc references were preserved as-is.
- ✅ File idioms reused: `Enum` block style, column-level `check:` attribute, `indexes {}` block, single-quoted notes, Arabic lane labels preserved.

## Carry-Forward Knowledge

- `.dbml` files are invisible to every lint stage (oxlint/biome/jscpd all report zero files); `sub-loop.ts` fails them at oxlint on the empty-set exit code. If future tasks gate docs artifacts on the sub-loop, either treat "No files found to lint" as pass (as this plan does) or add an extension-aware skip to `scripts/health/sub-loop-checks.ts` (cross-file; out of this task's scope).
- The Drizzle migration dir is `backend/drizzle/` (not `backend/db/drizzle/` as some plan text says).
- `subscription_credit_lane` enum values: `hifz`, `tajweed`, `reviews` (exact, quoted order) — do not add `evaluation`.

## Cross-File Dependencies

None blocking. Informational only: the `sub-loop.ts` oxlint stage's hard failure on non-JS/TS files (above) could be hardened in a future maintenance task by the tooling owner; it does not affect the correctness of this sync.
