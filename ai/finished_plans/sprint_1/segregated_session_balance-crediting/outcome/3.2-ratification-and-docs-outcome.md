# Task 3.2 — Ratification Record + Readiness Check-off + Canonical Doc Outcome

**Date:** 2026-09-12 · **Status:** PASS · **Requirements:** REQ-024, REQ-025, REQ-026, REQ-033, REQ-034
**Nature:** DOCS — ratification record, readiness check-off, and canonical domain doc; no product code touched.

## Summary

Ratified divergences D1–D4 (plan.md §1.3) by recording them in the new canonical doc with invariant bindings; ticked readiness §5.3.1/5.3.2/5.3.4/5.3.5 against executed evidence only (§5.3.3 left unchecked with a WHY note — DEV1-008 scope); created `docs/billing/segregated-session-balance.md` as the canonical reference for the segregated session-balance domain.

## Ratifications (D1–D4)

Each anchor was re-read and matched before citing:

1. **D1 — debit timing (REQ-024):** hold-as-debit at request, net −1 per attended session. Anchored to `docs/sessions/session-lifecycle.md` §4 (Hold-as-Debit Ruling) and §5 (Trial-First Ladder & Same-Lane Refund) — both read; the ruling explicitly reconciles with decision B.4.
2. **D2 — reviews-credit-only (REQ-025):** `(intent lane > 0) OR (trial > 0)` never reaches `balance_reviews` from a booking. Anchored to `backend/enum/scheduling/held-balance-lane.enum.ts` (docstring + enum members `trial|hifz|tajweed`; "reviews lane is deliberately NOT part of this vocabulary") and `docs/specs/state-machine-invariants.md` §4.2 INV-B5.
3. **D3 — "422" = VALIDATION family (REQ-020/021):** GraphQL envelope is HTTP 200 + `errors[].extensions.code`; canonical map `VALIDATION → 422` at `backend/lib/errors/error-code-taxonomy.ts` (ERROR_CODE_HTTP_STATUS, read). Executable transport pin = `backend/graphql/test/session-booking-balance.test.ts` (Task 2.3, green).
4. **D4 — trial-first eligibility (REQ-026):** `(intent lane > 0) OR (trial > 0)`, trial consumed first. Anchored to `backend/services/classes/session-lifecycle.booking.ts` (`debitBookingLadder`, trial lane attempted first, all-miss → `ValidationError("INSUFFICIENT_BALANCE", t.insufficientBalance)`) and `docs/specs/state-machine-invariants.md` §4.2 INV-B4/B8 (both read).

## Files Created / Modified

1. **Created** `docs/billing/segregated-session-balance.md` — canonical doc with sections in the mandated order: Why → Lane model (4 lanes + two deliberately separate vocabularies) → Credit path (activation) → Hold/refund path (booking escrow) → Guarded-mutation rules → Ratified semantics D1–D4 with invariant bindings → Anti-patterns → Test map → Related docs. Cross-links `docs/billing/subscription-purchase.md`, `docs/sessions/session-lifecycle.md`, `docs/specs/state-machine-invariants.md` §4.2 (all three read first; tone/structure matched — canonical-reference format with bold "Source of truth for" header, `##` sections, tables, consumer warnings).
2. **Modified** `docs/planning/PRODUCTION_READINESS.md` — §5.3.1–5.3.5 rows only (lines 243–247). Each tick carries an evidence ref (outcome evidence + test file):
   - 5.3.1 (INV-B1) → ✅ REQ-010, Task 1.1 evidence (`student.repository.test.ts` CHECK-floor tiers; `students.ts:42-45`).
   - 5.3.2 (INV-B2) → ✅ REQ-013/017/018, Tasks 1.1/2.1/2.2 evidence (exact-delta lane credits in `subscription-activation.service.test.ts`; journey legs in `subscription-purchase.journey.test.ts`).
   - 5.3.3 (INV-B3 expiry) → left ☐ with WHY: "expiry invariant is DEV1-008 scope (deferred item D5) — no expiry surface exists this sprint".
   - 5.3.4 (INV-B4) → ✅ REQ-020/021, Tasks 1.1/2.3 evidence (denial + chaos `session-lifecycle.service.test.ts`; GraphQL pin `session-booking-balance.test.ts` — Task 2.3's outcome file appeared complete with Status: PASS before this task started, so no polling was needed).
   - 5.3.5 (INV-B5) → ✅ REQ-015/018, Tasks 1.1/2.3 evidence (segregated-lane tiers in `student.repository.test.ts`; webhook idempotency/replay tiers in `subscription-activation.service.test.ts`).
3. **Modified** `ai/plans/sprint_1/segregated_session_balance-crediting/tasks.md` — flipped exactly the three `3.2` checkbox lines (3.2, 3.2.QL, 3.2.TE/SEC/SR/IV). No other lines touched.
4. **Created** this outcome file.

## Files NOT Modified (and why)

- `backend/graphql/test/session-booking-balance.test.ts` — Task 2.3's in-flight file (parallel agent); never opened for edit, only its completed outcome file read for the 5.3.4 evidence check.
- `docs/sessions/session-lifecycle.md`, `docs/billing/subscription-purchase.md`, `docs/specs/state-machine-invariants.md` — read-only cross-link targets; their content already covers the ratified semantics, the canonical doc references them.
- All plan/product/test files outside the four-file scope — Task 2.3's verification already pins the code; ratification required no code change (plan.md: "No code change — ratification only").

## Verification Results

| Gate | Command | Result |
|---|---|---|
| Sub-loop (canonical doc) | `~/.bun/bin/bun --env-file=.env.test run scripts/health/sub-loop.ts docs/billing/segregated-session-balance.md --lifecycle duplicates` | exit 1 — **solely** because oxlint reports "No files found to lint" for a `.md` target; tsgo stage green, no applicable instruction files. Per the task's gating rule, nothing-to-lint counts as PASS. |
| Sub-loop (readiness doc) | same command on `docs/planning/PRODUCTION_READINESS.md` | Same result — tsgo green, oxlint no-files for `.md`; nothing-to-lint = PASS per gating rule. |
| Semantic review (SR) | `grep -n "REQ-\|DEV1-\|Phase 2\|Task 1\.1\|Task 2\.3"` on the canonical doc | REQ ids appear ONLY inside the Ratified Semantics table (documentation file — permitted); no REQ/Task/Phase ids in any non-docs file (no product code touched at all). No oxlint-disable comments added anywhere. |

**IV:** the sub-loop printed only root `AGENTS.md` as applicable (no instruction files for `.md` targets); it was honored — no `console.*`, no code, no lint suppressions introduced.

## Tier / SR notes (3.2.TE / 3.2.SEC / 3.2.SR)

- 3.2.TE: N/A (docs) — no test artifacts created.
- 3.2.SEC: N/A — docs only; the canonical doc itself carries the balance-leak rules (no lane values on the wire, amounts server-owned).
- 3.2.SR: scope boundary verified — exactly the four files above were written; production-grade language throughout (no plan-specific one-off migration steps in the canonical doc — those live in outcome files); REQ ids only in docs.

## Cross-File Dependencies / Blockers

None. No fixes required in files outside the assigned scope. Note for the orchestrator: Task 2.3's evidence appeared complete in time, so readiness 5.3.4 IS ticked — no in-flight carry-forward for 5.3.x.

## Carry-Forward

- `docs/billing/segregated-session-balance.md` is the canonical reference for the balance-lane domain; downstream tickets (review-session flow, expiry sweep, admin balance surfaces) MUST read it plus the two vocabulary warnings in §2.2/§7 before touching `students.balance_*` or `sessions.held_balance_lane`.
- 5.3.3 (expiry) remains open — owned by DEV1-008 (deferred item D5). When that lands, tick it against its own executed evidence.
- REQ-034's second wave (4.2) is a later phase's scope; the canonical doc side of REQ-034 is done here.
- Sub-loop on `.md` targets always exits 1 at the oxlint stage ("No files found to lint"); the task's gating rule (nothing-to-lint = PASS) applies to all doc-only tasks in this plan.
