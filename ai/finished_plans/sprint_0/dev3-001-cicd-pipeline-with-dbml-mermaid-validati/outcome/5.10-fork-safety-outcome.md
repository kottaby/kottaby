# 5.10 — Fork-Safety Static Proof (REQ-031)

## Fresh static walk outputs @ tip
- `grep -n "secrets\." .github/workflows/ci.yml` → **zero matches**: workflow references no repository/org secrets anywhere ⇒ forks receive full pipeline function without secret grants.
- `grep -n "pull_request_target" .github/workflows/ci.yml` → single hit = the header BAN comment; no event usage.
- Top-level `permissions:` = `contents: read` only; no job elevates.
- All checkouts `persist-credentials: false` ⇒ no GITHUB_TOKEN leakage into fork-context artifacts.

## Behavioral statement
Live fork test not performed (no fork created; sandbox scope). By construction: a fork PR triggers the same `pull_request` flow — every job depends solely on committed files + fixture values (`.env.test.ci`, `kottaby` service creds marked ephemeral) — so pipelines execute fully inside fork sandboxes. Cache native isolation additionally prevents cross-fork restore (see 5.7).
