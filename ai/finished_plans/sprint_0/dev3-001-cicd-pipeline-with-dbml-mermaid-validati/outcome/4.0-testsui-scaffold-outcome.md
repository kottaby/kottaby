# Task 4.0 Outcome — `tests-ui` Scaffold (CI gap-closure / BLT-05 early closure)

**META:** Not a planned task checkbox — executed under **orchestrator authorization Task ID 11**
(pipeline prerequisite for the `tests-ui` job authored in Tasks 3.2/3.3; branch
`dev3-001/ci-cd-pipeline`, worktree-pinned execution per Task 8's HEAD-flipper protocol).

---

## 1. Classification & Coordination

| Dimension | Finding |
|---|---|
| **REQ-052 alignment (Missing Tooling Contract)** | The `test:ui:components` package.json entry referenced four preload paths and a test directory that did not exist on this branch (`test/ui/**` entirely absent) ⇒ per REQ-052 semantics ("any packaging gaps discovered become in-scope fixes"), this task creates the missing wiring that CI's required `tests-ui` check invokes. |
| **REQ-057 alignment (Green-Signal Semantics)** | A green `tests-ui` MUST mean an **honest full run** of the suite it references. Delivered here: real (non-placeholder) component tests exercising translation-system copy both EN and AR, snapshot-level Arabic assertion, full switch-flow behavioral test. Nothing skipped, no environment-gated partial green; no port binds (serverless in-memory render). |
| **DEV3-002 BLT-05 coordination** | Closes the dev3-002 deferred item **BLT-05** early for the COMPONENT tier ("no components/, e2e/, test-env.ts, translation-preload.ts, TestWrapper … scripts reference nonexistent paths" — their `deferred-items.md` line 23, plan-review-R1 §(c) row 16). Cross-plan pointer added as COORDINATION-NOTE in OUR ledger (`deferred-items.md`); ownership of flipping THEIR row stays with dev3-002 owners. |
| **Briefing risk #5 announce-note honored** | Per orchestrator briefing risk #5: before creating anything, `git ls-tree -r origin/main --name-only \| grep "^test/ui/"` was re-run post-fetch → exactly ONE upstream piece matches (`test/ui/AGENTS.md`). Blob-verified `git diff --stat origin/main HEAD -- test/ui/AGENTS.md` ⇒ IDENTICAL since repo-init on both lines (the file predates both tickets; no mid-flight upstream addition existed) — adoption requirement satisfied with ZERO content delta and zero staging churn; every other scaffold file was CREATED fresh on our branch. |

## 2. Adopted vs Created

| File | Verdict | Provenance |
|---|---|---|
| `test/ui/AGENTS.md` | **PRE-EXISTING / byte-identical on origin/main** | No-op adoption: origin/main tree contains the same file since `init` (blob diff empty vs HEAD @a360c34); risk #5 scan therefore required zero restores. Its TARGET-STATE conventions guided (and partially diverge from) this scaffold — see §3. |
| `test/ui/test-env.ts` | CREATED | Tiny env-posture loader reusing `backend/lib/test-ci-env.ts` `isTestCi()` semantics; normalizes `TEST_CI=1→true`; `TEST_SERVER_MODE` defaults `dev` per AGENTS.md; NODE_ENV=test cast mirrors ensure-env precedent. |
| `test/ui/components/happydom-preload.ts` | CREATED | `@happy-dom/global-registrator@^20.11.6` present in devDeps ✓ (grep node_modules) → named-export `GlobalRegistrator.register()` guarded by static `isRegistered` (register ONCE even across files/process reuse). |
| `test/ui/components/translation-preload.ts` | CREATED | Documents divergence from upstream AGENTS.md description (§3): compile-time sync translation system needs no Suspense prewarm; warms ONLY Landing+Auth namespaces ×{ar,en} eagerly (missing-key drift surfaces at PRELOAD time); registers ONE `next/navigation` mock whose `useParams()` locale feeds `frontend/hooks/useAppLocale` (the real app source of active locale) + refresh counter, exported as `testNavigationState`. |
| `test/ui/components/next-dynamic-mock.ts` | CREATED | Sync-first require resolution (loader() non-thenable → immediate true component), async-import() fallback cached per loader w/ loading placeholder; type-guard based (zero type assertions per oxlint). |
| `test/ui/components/TestWrapper.tsx` | CREATED | Composition mirrors `.storybook/StoryWrapper.tsx`: LocaleProvider → Emotion CacheProvider (rtl cache WITH stylis-plugin-rtl, prod parity) → ThemeProvider `createAppTheme()` — which theme.ts explicitly documents as "retained for Storybook and component tests (`TestWrapper`)". Plus `renderWithWrapper()` helper so suites share wrapper JSX (jscpd-safe). Name/path chosen to match upstream AGENTS.md convention to minimize future merge friction. |
| `test/ui/components/site-footer.test.tsx` | CREATED (REAL test #1) | Pattern-2 component (`useAppTranslation(Landing)` internally): EN landmark role + translated tagline/legal-link hrefs + 9 links; AR locale asserts Arabic tagline/copyright/columns + **toMatchSnapshot of `<footer>` markup (Arabic-locale snapshot-level assertion)**. All strings via `Landing.getLabels(getTranslations(locale))` — zero hardcoded UI text. |
| `test/ui/components/locale-switcher.test.tsx` | CREATED (REAL test #2) | Target-locale semantics verified both directions; POST `/api/set-locale` payload asserted `locale:"en"` under ar-active; router.refresh() counted =1 through mocked navigation; fetch double installed via Reflect.set (silent-network, restores afterAll). Labels via Auth namespace (incl Arabic `'العربية'` label + `'ع'` glyph assertions). |
| `test/ui/components/__snapshots__/site-footer.test.tsx.snap` | CREATED (artifact) | Generated by first honest run; committed intentionally to pin reviewed RTL markup. |

## 3. Divergence Notes vs Adopted AGENTS.md (honesty box)

Upstream main's AGENTS.md describes TARGET-STATE infra that does not exist on this branch and
was NOT invented divergently here:

- `@/shared/locale/client/translation-cache-store` + `readTranslation(handle, locale)` — module absent;
  branch ships the synchronous compile-time system (`getTranslations` pure, never suspends), so
  "preloading" degenerates to eager namespace warming (documented inside translation-preload.ts).
- `TestWrapper` Apollo `MockedProvider` requirement — deferred: current two components are pure
  presentational with no GraphQL surface; wrapper kept provider-minimal (comment cross-references
  AGENTS.md Apollo section for future suites).
- E2E tier (`test/ui/e2e/*`, lifecycle helpers) is OUT OF SCOPE for BLT-05-component-closure and for
  the CI `tests-ui` job (which runs only `test:ui:components`).
- Snapshot serializes `outerHTML` string rather than live Element (happy-dom Elements are live proxies;
  bun's deep serializer is unstable there — root-caused during bring-up: element-object snapshot crashed
  the worker silently).

## 4. Local Evidence (worktree @wt-ui branch off tip a360c34)

.env.test materialized IN-WORKTREE ONLY via prescribed flow:
`DATABASE_URL='postgresql://kottaby:ci-local-only-fixture@localhost:5432/kottaby_test' bun run scripts/ci/materialize-env-test.ts`
→ `.env.test written: 5 key(s), 1 from CI overrides` (gitignored by `.gitignore:35 .env*`; never staged).

Primary suite (`bun run test:ui:components`, exact four-preload contract, TEST_SERVER_MODE=production inline):

```
(pass) LocaleSwitcher > ar active: announces ENGLISH target and posts locale=en on click [177ms]
(pass) LocaleSwitcher > en active: offers Arabic — translated 'العربية' label with 'ع' glyph [16ms]
(pass) SiteFooter > en: renders landmark with translated tagline and legal links [91ms]
(pass) SiteFooter > ar: switches copy to Arabic and snapshot-pins the footer markup [29ms]
 4 pass, 0 fail, snapshots: 1, 20 expect() calls — Ran 4 tests across 2 files.
```

Stability re-run (second invocation): identical `4 pass 0 fail`, `1 snapshots` (no add/drop churn).

Sanity regression (`KOTTABY_TEST_RUNNER_OK=1 bun --env-file=.env.test test --parallel=1 scripts/ci/`):

```
70 pass, 0 fail, 157 expect() calls — Ran 70 tests across 3 files
(changed-docs 25 · materialize-env-test 21 · validate-docs-ci 24)
```

Quality-gate parity (all exit 0 after fixes):

| Gate | Result |
|---|---|
| `bun run tsgo` | exit 0, 0 errors (type errors in next-dynamic-mock draft caught+fixed mid-loop) |
| `bun run lint` (eslint service) | exit 0 — caught relative-import rule + nested ternary + display-name; all fixed |
| `bun run oxlint` | 0 warnings, 0 errors (400 files) — eliminated every unsafe-type-assertion/floating-promise finding without suppressions |
| `biome check test/ui/` | No diagnostics |
| `bun run check:duplicates` | **Found 0 clones** across ALL new ts/tsx files (218 files analyzed, 0.00% duplicated) |

## 5. Conflicts-Risk Assessment vs origin/main freshness

- Pre-create scan (protocol step) found ONLY `test/ui/AGENTS.md` on main ⇒ nothing else to conflict.
- Residual merge risk sits OUTSIDE test/ui: dev3-002 phase-2 commits touch `shared/locale/{ar,en}/messages.ts`
  (+errors keys) — my preloads import those message barrels generically, so extra namespaces merge cleanly;
  Landing/Auth handles untouched upstream ⇒ stable.
- If upstream later materializes its own `readTranslation` cache-store/TestWrapper variants, our
  TestWrapper/preload naming already matches their documented paths; reconciliation is additive
  (swap internals behind same exports), not structural.

## 6. Scope discipline

Files touched: exactly the nine listed in §2 + bookkeeping (`deferred-items.md` COORDINATION-NOTE append,
this outcome). `package.json` untouched (script contract pre-exists verbatim). Zero deletions. `.env.test`
excluded by gitignore policy (D3 precedent).
