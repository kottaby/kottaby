# Task 5.2 Outcome — REQ-071 DBML Sabotage → Red-Only Target Check → Revert → Green

Cycle executed 2026-08-26 18:38–18:48 UTC on branch `dev3-001/ci-cd-pipeline` (worktree-isolated pushes; HEAD-flipper protocol).

## Sabotage

* **File**: `db/schema.dbml`, one-line structural corruption: header keyword
  `Enum notification_type {` → `enum notification_type { // req-071 sabotage: …`
  (lowercase keyword is invisible to the validator's `/^Enum\s+([a-zA-Z_]\w*)\s*\{/gm`
  parser ⇒ structurally fatal for that enum's registration).
* **Blob SHAs**: before = `24576b2d66f0e316a786ffe67b78b99c1812a3fc`;
  after = `e402207f098ac9afe4704389cdb2d2d80197f950`.
* **Sabotage commit**: `a74ed8b0364aba39b1038ce0e920e1f4985ea176` —
  `test(ci-dev3-001): req-071 dbml sabotage [throwaway]`.

## Local negative proof (before push)

```
$ bun validate:dbml
❌ DBML validation failed: expected 15 enums but found 14; missing enums: notification_type   (exit 1)
```

## Triggered run (sabotage)

| field | value |
|---|---|
| Run | `33000962941` — https://github.com/ahmedhosnypro/kottaby_academy/actions/runs/33000962941 |
| Pushed at / created (UTC) | 2026-08-26T18:40:10Z push · 18:40:17Z run created |
| Head SHA | `a74ed8b…` |
| Conclusion | **failure** |

### Per-job verdicts

| job | verdict | expectation match |
|---|---|---|
| dbml-validation | ❌ failure | ✅ target check red |
| workflow-sanity | ✅ success | expected green |
| quality | ✅ success | ✅ CONFIRMED per spec: `tsgo` does not parse `.dbml`, so quality stays green while DBML is corrupted (expectation matched actuality) |
| docs-validation | ✅ success | expected green (.dbml not a doc surface) |
| tests-db / tests-services / tests-ui | ✅ success | expected green |

No unexpected cascade beyond the target check.

## Verbatim log excerpt (`gh run view 33000962941 --log | grep "DBML validation failed"`)

```
dbml-validation	DBML validation (db/schema.dbml)	2026-08-26T18:40:37.9413070Z ❌ DBML validation failed: expected 15 enums but found 14; missing enums: notification_type
```

## Merge-block assertion (spec-mandated fallback path)

Attempted ruleset merge-block capture. The repo ruleset from Task 3.4 was **NOT APPLIED**
(admin-token scope limitation pre-documented as deviation in outcome/3.4-ruleset-spec-and-payload.md),
so GitHub cannot hard-block the merge yet; recorded facts instead:

```
gh pr view 1 --json statusCheckRollup,mergeable,mergeStateStatus   @ head a74ed8b:
{"mergeable":"MERGEABLE","mergeStateStatus":"UNSTABLE",
 "statusCheckRollup":[ … {"name":"dbml-validation","conclusion":"FAILURE"} …]}
```

The required-check **context** `dbml-validation = FAILURE` exists and is up-to-date with
PR head SHA; with the prepared ruleset payload applied (human admin step, Task 3.4.B /
deferred-items carry-forward), this exact state evaluates to a hard merge block
(`mergeStateStatus: BLOCKED`). **Enforcement pending human-admin ruleset application** —
pre-documented deviation ⚠️→tracked in 3.4.B; Phase 5 evidence here supplies the
required-check-failure half of that future merge-block proof. (Note: `UNSTABLE` currently
also reflects the third-party GitGuardian app check failure, unrelated to this pipeline.)

## Revert cycle

* Inverse patch: `git revert -n a74ed8b` → blob restored byte-exact:
  restored `24576b2d66f0e316a786ffe67b78b99c1812a3fc` == pre-sabotage blob;
  touched-file diff vs pre-sabotage blob EMPTY.
* Revert commit: `66b377c1ee7801e45670ba938011c07af62791e2` —
  `revert(ci-dev3-001): req-071 dbml sabotage restored [inverse patch]`.
* Post-revert run: `33001336200` — https://github.com/ahmedhosnypro/kottaby_academy/actions/runs/33001336200
  (created 2026-08-26T18:44:15Z) → **conclusion success, ALL 7 GREEN** incl.
  dbml-validation; verbatim:

```
dbml-validation	DBML validation (db/schema.dbml)	2026-08-26T18:44:33.0277391Z ✅ DBML validation passed: 22 tables, 15 enums
```

Verdict: REQ-071 satisfied end-to-end (local red → CI red-only target → revert → CI green).
