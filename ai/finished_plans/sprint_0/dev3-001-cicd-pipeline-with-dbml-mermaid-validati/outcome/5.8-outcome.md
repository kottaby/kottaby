# 5.8 — Missing-Env Fail-Fast Verification (REQ-022)

## Cycle
| Step | SHA | Run | Result |
|---|---|---|---|
| Sabotage: remove ONLY the `DATABASE_URL` line from `tests-db` job env (`test(ci-dev3-001): req-022 missing-env failfast sabotage [throwaway]`) | `e24519d` | [`33006282399`](https://github.com/ahmedhosnypro/kottaby_academy/actions/runs/33006282399) | run FAILURE driven solely by job `tests-db` |
| Revert (inverse patch, byte-restored env line) | `5297d1b` | subsequent PR run | GREEN (see closure table in 5.10 file) |

## Failure anatomy on the sabotage run (jobs API + failed-log tail)
Step verdicts for tests-db:
```
Set up job / Initialize containers / Check out / Set up Bun / Install deps  → success
Materialize CI env template                                                → FAILURE
(db push and DB-test steps never appear ⇒ skipped by early exit)
Post steps                                                                  → success
```
Verbatim output of the failing step:
```
tests-db  Materialize CI env template  …  missing required CI env variable: DATABASE_URL
tests-db  Materialize CI env template  …  ##[error]Process completed with exit code 1.
```

## Requirement mapping
- **REQ-022** satisfied: the NAMED, human-attributable contract error surfaces at the earliest correct step — no downstream cryptic Postgres/connection noise possible because materializer gates every later DB step.
- Confirms the bun `--env-file` silent-ignore gap documented at Phase 0 (§11) is defense-covered: even if env loading were silently skipped elsewhere, this script still enforces presence deterministically.
