# 5.5 — Test Sabotage + Idempotent Rerun (REQ-074, REQ-041)

## Cycle ledger
| Step | SHA | Run | Result |
|---|---|---|---|
| Sabotage commit (`test(ci-dev3-001): req-074 tests-ui sabotage [throwaway]`) | `961a684` | [`33003316939`](https://github.com/ahmedhosnypro/kottaby_academy/actions/runs/33003316939) | **tests-ui FAILURE**, all six sibling checks success (surgical blast radius) |
| Revert (`revert(ci-dev3-001): req-074 tests-ui sabotage restored [inverse patch]`) | `629afea` | fresh-push green | ✅ |
| Full-workflow re-run on already-green SHA | `629afea` (same run id) | [`33003793967`](https://github.com/ahmedhosnypro/kottaby_academy/actions/runs/33003793967) | `run_attempt=2`, conclusion **success** |

## Job verdicts on the sabotage run
```
docs-validation: success   dbml-validation: success   workflow-sanity: success
quality: success           tests-ui: FAILURE          tests-services: success
tests-db: success
```
Per jobs API of run 33003316939 @ head_sha `961a684`.

## Idempotence-by-construction proof (both required legs)
1. **Fresh-push rerun** — post-revert head triggered a new pull_request event run: GREEN.
2. **Re-run all jobs on unchanged commit** — `gh run rerun` executed against the green run at `629afea`: attempt counter advanced to 2 with identical source context and concluded `success`. Zero state carry-over between attempts observed (cold bun/eslint restore paths re-executed per cache step logs).

## Notes
- The sabotaged assertion lived in `test/ui/components/site-footer.test.tsx` (non-DB suite ⇒ fastest signal path per plan text).
- Outcome drafted by continuation agent 14b from live API queries; predecessor session captured the sabotage leg before deadline.
