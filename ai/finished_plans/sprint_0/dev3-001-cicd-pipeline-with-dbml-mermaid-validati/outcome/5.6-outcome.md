# 5.6 — Concurrency Verification (REQ-077, REQ-024, REQ-044, REQ-047)

## Probe chain (rapid successive pushes < 30 s window)
| Order | Commit | Message | Run | Final status |
|---|---|---|---|---|
| c1 | `5db3ec2` | req-077 concurrency probe c1 | (superseded sequence start) | superseded by later pushes |
| c2 | `8e4df4a` | req-077 concurrency probe c2 | [`33004420232`](https://github.com/ahmedhosnypro/kottaby_academy/actions/runs/33004420232) | completed/**success** att=1 |
| c3 | `f538b95` | req-077 concurrency probe c3 | [`33005340494`](https://github.com/ahmedhosnypro/kottaby_academy/actions/runs/33005340494) | completed/**CANCELLED** att=1 |
| c4 | `b009106` | req-077 concurrency probe c4 | [`33005368089`](https://github.com/ahmedhosnypro/kottaby_academy/actions/runs/33005368089) | completed/**success** att=1 |
| restore | `609553e` | cleanup [restore] | subsequent run | success |

## Semantics actually observed
- While c3's run was in-flight, c4 was pushed → GitHub concurrency group
  `ci-CI-${{ pr-number }}` canceled the OLDER in-flight run (c3 → CANCELLED) and let only the newest (c4) complete: exactly the `cancel-in-progress = ${{ github.event_name == 'pull_request' }}` contract for PR events.
- Older run that had ALREADY completed (c2) was never retroactively cancelled — matching expected semantics.
- events were all `pull_request` (PR #1 synchronize); `att=1` columns confirm each was a fresh run, not an attempt.

## Branch-push non-cancellation on develop (task-note alternative path)
Not live-testable pre-merge (no direct pushes to `develop` performed); recorded as theory-checked against the workflow expression + concurrency group definition per tasks.md sanctioned fallback.

## End-state
Cleanup commit `609553e chore(ci-dev3-001): req-077 concurrency probes cleanup [restore]` returned the probe comment line to its steady-state content; trailing run green.
