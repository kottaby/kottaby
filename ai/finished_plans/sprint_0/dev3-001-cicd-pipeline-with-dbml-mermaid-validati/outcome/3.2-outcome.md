# Outcome 3.2 — `.github/workflows/ci.yml`: Workflow-Sanity · Quality · DBML · Docs Jobs

> **Task ID**: 8 (Task 3.2 of plan DEV3-001 Phase 3) · **Agent**: ci-workflow-author · **Branch**: `dev3-001/ci-cd-pipeline` @ `a6f14c1`
> **Plan dir note**: plan text references `ai/plans/dev3-001-ci-cd-pipeline/`; the ACTUAL directory is `ai/plans/dev3-001-cicd-pipeline-with-dbml-mermaid-validati/`.
> **Date**: 2026-08-26 · **Requirements**: REQ-010–019, REQ-023–026, REQ-029, REQ-030, REQ-032–035, REQ-044, REQ-045, REQ-047, REQ-050, REQ-051, REQ-053, REQ-063, REQ-085
> **Environment note**: all verification ran in an isolated linked worktree pinned at branch tip (sandbox HEAD-flipper mitigation, same protocol as outcome 2.M §10). Gates are bit-attributable to this tree.

---

## 1. What Was Built

**`.github/workflows/ci.yml`** (first workflow file in the repo; `.github/workflows/` did not exist before this task). Four jobs per plan §3.1 contract; tests-db/services/ui + cache-finalization split + codegen-drift enablement remain owned by Task 3.3:

| Job | needs | timeout | Steps (contract-exact order) |
|---|---|---|---|
| `workflow-sanity` | — | 5 | checkout (`persist-credentials: false`) → `Actionlint (static check of all GitHub Actions workflows)` — version+checksum-pinned binary over ALL repo workflows, exit code propagates natively |
| `quality` | `workflow-sanity` | 15 | checkout → `Assert Bun version pin present` (grep `"packageManager": "bun@"`, fail-fast w/ stderr) → setup-bun `bun-version-file: package.json` → restore Bun install cache → restore ESLint caches (`.eslintcache`, `.eslintcache-type-aware`) → `bun install --frozen-lockfile` → **tsgo → oxlint → biome:check → lint → duplicates** (canonical 1→5) → *(codegen drift gate PLACEHOLDER comment; wired in 3.3)* → `if: always()` cache SAVE → `if: always()` job summary (markdown digest: checks + outcomes + wall-clock) |
| `dbml-validation` | *(none — parallel)* | 5 | bootstrap (checkout → setup-bun → frozen install) → step name EXACTLY `DBML validation (db/schema.dbml)` running `bun validate:dbml`. NO path filter — unconditional every run |
| `docs-validation` | *(none — parallel)* | 5 | checkout w/ `fetch-depth: 0` + `persist-credentials: false` → setup-bun → frozen install → step name EXACTLY `Mermaid validation (changed docs; full set on push)` running `bun run scripts/ci/validate-docs-ci.ts` with `env:`-mapped `EVENT_NAME=${{ github.event_name }}`, `BASE_REF=${{ github.event.pull_request.base.ref }}` |

Workflow-level: `name: CI`; header comment linking `docs/quality/ci-pipeline.md` (path-only reference per brief — doc lands Phase 7 SAME PR) that also (a) forbids `pull_request_target` in policy language, (b) lists the required-check surface, (c) states the SHA/persist-credentials/env-mapping security posture, (d) records REQ-027 local↔CI parity intent. Triggers `pull_request[opened,synchronize,reopened,ready_for_review]` on `[develop, main]` + `push` on `[develop, main]`; top-level `permissions: contents: read` ONLY; concurrency group `ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}` with `cancel-in-progress: "${{ github.event_name == 'pull_request' }}"`.

Shared-bootstrap parity (GitHub Actions supports NO YAML anchors ⇒ parity-by-construction, then machine-verified): the setup-bun and frozen-install steps are **object-identical** across all three non-sanity jobs; dbml/docs checkouts differ ONLY by the semantically-required `fetch-depth: 0` (docs). Verified programmatically (§3.4).

## 2. Files Created / Modified

| File | Change |
|---|---|
| `.github/workflows/ci.yml` | NEW (231 lines) |
| `ai/plans/…/outcome/3.1-graphql-na-outcome.md` | NEW (Task 3.1 record) |
| `ai/plans/…/outcome/3.2-outcome.md` | This file |
| `ai/plans/…/tasks.md` | checkbox flips 3.1/3.1.A/3.2 + sub-items |

NOT modified: `package.json` (all invoked entries already exist — parity §3.3), `scripts/**` (validators/wrapper consumed as-is), anything else. Zero TS/MD product files touched ⇒ `sub-loop.ts`/test-suite obligations have no new subjects this task.

## 3. Verification Evidence

### 3.1 QL — actionlint IS the YAML analog of sub-loop (tasks.md 3.2.QL substitution)

Binary supply-chain verification THEN static validation (exact commands, real outputs):

```
$ gh api repos/rhysd/actionlint/releases/latest --jq .tag_name      → v1.7.12
$ gh release download v1.7.12 -R rhysd/actionlint \
    -p 'actionlint_1.7.12_checksums.txt' -O /tmp/actionlint-checksums.txt
$ rg "linux_amd64" /tmp/actionlint-checksums.txt
8aca8db96f1b94770f1b0d72b6dddcb1ebb8123cb3712530b08cc387b349a3d8  actionlint_1.7.12_linux_amd64.tar.gz
$ curl -fsSLo /tmp/actionlint_1.7.12_linux_amd64.tar.gz https://github.com/rhysd/actionlint/releases/download/v1.7.12/actionlint_1.7.12_linux_amd64.tar.gz
$ echo "8aca…a3d8  /tmp/actionlint_1.7.12_linux_amd64.tar.gz" | sha256sum --check -
/tmp/actionlint_1.7.12_linux_amd64.tar.gz: OK
$ /tmp/actionlint -version
1.7.12 / installed by downloading from release page / built with go1.26.1 compiler for linux/amd64
```

```
$ cd <worktree> && /tmp/actionlint -no-color
(no output)
ACTIONLINT_EXIT=0
```

Authoring-iteration honesty: ONE finding existed mid-authoring and drove a real fix — my initial draft interpolated `${{ github.run_started_at }}` into the job-summary env and actionlint rejected it (`property "run_started_at" is not defined … [expression]`). Replaced with a `$RUNNER_TEMP` epoch stamped by the FIRST mandated step (assert-bun-pin) keeping the brief-mandated step ordering intact; final run is clean above. Cross-check: embedded `run:` blocks additionally shellchecked locally with ShellCheck **v0.11.0** → 0 findings across all 9 run blocks (CI ubuntu runners ship shellcheck pre-installed, so the integration is active there as well).

### 3.2 TE-c — Boundary behavior of docs-validation command (real processes, worktree tree)

| Case | Command/env | Result |
|---|---|---|
| Broken/unknown base ref (requested literal probe `BASE_REF=HEAD^` ⇒ wrapper computes `origin/HEAD^`, not a resolvable rev here) | `EVENT_NAME=pull_request BASE_REF='HEAD^' bun run scripts/ci/validate-docs-ci.ts` | **exit 128** — stderr: `docs-validation: git diff against origin/HEAD^ failed — child stderr follows, un-truncated:` + git's own `fatal: ambiguous argument 'origin/HEAD^...HEAD'…`. A broken base can NEVER masquerade as an empty green set (REQ-026/REQ-051 fidelity live-proven) |
| TRUE empty changed-set (hermetic self-remote bench: HEAD ≡ origin/main ≡ empty seed commit) | `EVENT_NAME=pull_request BASE_REF=main …` in `/tmp/dev3-v32-noopbench/bench` | **exit 0**, stdout EXACTLY `No documentation changes — passing no-op (docs-validation)` |
| Real PR-mode run against real repo state | `EVENT_NAME=pull_request BASE_REF=main bun run scripts/ci/validate-docs-ci.ts` (worktree root) | **exit 0** — validator ran the genuine merge-base changed-set of this ticket (5 plan-dir markdown artifacts), `✅ Mermaid validation passed: 5 file(s), 0 diagram(s)` |

### 3.3 TE-b — YAML parity check (every workflow command exists verbatim repo-side)

| Workflow `run:` | Canonical entry proof |
|---|---|
| `bun tsgo` | package.json:16 `"tsgo": … tsgo -b --noEmit"` |
| `bun run oxlint` | package.json:46 `"oxlint": … oxlint --deny-warnings --ignore-path .gitignore` |
| `bun biome:check` | package.json:13 `"biome:check": … bunx @biomejs/biome check --write --unsafe .` |
| `bun run lint` | package.json:43 `"lint": "bun run scripts/lint-service.ts"` |
| `bun run check:duplicates` | package.json:47 `"check:duplicates": … jscpd` |
| `bun validate:dbml` | package.json:68 `"validate:dbml": "bun run scripts/validate-dbml.ts"` (target exists, db/schema.dbml present) |
| `bun run scripts/ci/validate-docs-ci.ts` | direct-file composition script per REQ-027; exists (13769 bytes); its JSDoc documents this exact invocation |

(Also asserted in-workflow: one combined save call covers both cache families until Task 3.3 finalizes the save-split wiring; TODO comments mark the seam.)

### 3.4 Structural verification battery (all machine-checked PASS)

```
YAML double-parse            python3 yaml.safe_load → OK (name/permissions/env/on/jobs as contracted)
triggers                     on-keys == [pull_request, push]; types/branches exactly per contract
permissions                  single top-level block, contents: read ONLY; zero write-grant words repo-wide in file
uses: pinning regex          'uses: [^@]+@[0-9a-f]{40} # vX.Y.Z$' — ALL 10 uses-lines conform (incl. subdir path forms)
pull_request_target          ZERO occurrences except the header BAN comment; parsed trigger keys prove it
upload-artifact              none
github.event.* in run:       NONE (only concurrency expressions + two env: mappings — REQ-035 honored)
canonical order              tsgo[2]→oxlint[3]→biome[4]→lint[5]→duplicates[6] contiguous ascending after install
if: always()                 exactly 2 — cache-save + job-summary
forbidden patterns           zero `|| true` / `continue-on-error` / `retry`
timeouts                     4/4 jobs declare timeout-minutes (5/15/5/5)
persist-credentials          4/4 checkout steps : false
REQ-053 names                step names byte-verbatim incl. parentheticals
bootstrap parity             setup-bun/install steps object-identical across quality/dbml/docs; dbml-vs-docs
                             checkouts differ only in fetch-depth: 0
```

### 3.5 Action-SHA resolution evidence (resolved LIVE this session via authenticated `gh api`)

```
gh api repos/actions/checkout/git/matching-refs/tags/ --jq '.[].ref' | tail   → latest stable v5.1.0
gh api repos/<repo>/commits/<tag> --jq .sha   (peels annotated tags to commit objects;
                                               404 on git/tags/<sha> confirms commit-object status)

actions/checkout   v5.1.0 → fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09   # 4× (every checkout step)
oven-sh/setup-bun  v2.2.0 → 0c5077e51419868618aeaa5fe8019c62421857d6   # 3× (quality/dbml/docs)
actions/cache/restore v4.3.0 → 0057852bfaa89a56745cba8c7296529d2fc39830 # 2× (bun cache, eslint caches)
actions/cache/save    v4.3.0 → 0057852bfaa89a56745cba8c7296529d2fc39830 # 1× (combined epoch save)
rhysd/actionlint   v1.7.12 → 914e7df21a07ef503a81201c76d2b11c789d3fca   # NOT USABLE AS AN ACTION — see deviations
```

Supporting manifest checks: `actions/cache@v4.3.0` tree contains `restore/` + `save/` subdirectories (subdir-action SHA valid for both entrypoints); `oven-sh/setup-bun@v2.2.0` action.yml declares the `bun-version-file` input ("e.g. \"package.json\"").

## 4. Deviations (recorded explicitly; none silent)

| # | Deviation | Justification |
|---|---|---|
| 1 | **`rhysd/actionlint` action-pin impossible** — preferred option abandoned. The repo ships NO `action.yml` at its root (verified via contents API @ v1.7.12: Dockerfile/README only); upstream `docs/usage.md` prescribes either a download-script or `uses: docker://rhysd/actionlint:latest` (an UNTAGGABLE mutable tag — violates REQ-032 harder). The plan-sketch input `with: { fail_level: error }` exists in NO upstream artifact. | Brief-sanctioned alternative chosen: run-step download pinned BY VERSION with the official release-asset checksum EMBEDDED IN THE STEP (`ACTIONLINT_SHA256` env) and enforced via `sha256sum --check --status` BEFORE execution; mismatch prints an explicit refusal line and exits 1. Binary executes only after checksum proof; exit codes propagate natively. Same checksum string independently proven locally (§3.1) — identical value lives in the workflow env and in this outcome. |
| 2 | **No YAML anchors anywhere** — GitHub Actions does not support anchors/merge-keys in workflow files, so the tasks.md "define via YAML anchors IF ALLOWED" clause resolves to NOT allowed. | Byte-parity maintained by construction AND machine-verified (§3.4 last row). Anchor-free grep also trivially passes (zero `&anchor`/`<<:` tokens). |
| 3 | Duration source in job summary: `$RUNNER_TEMP` epoch stamped by the assert-bun-pin step instead of a nonexistent `github.run_started_at` context. | Discovered BY actionlint during authoring (finding quoted §3.1). A separate leading timing step would violate the brief's EXACT step-order mandate; stamping inside the first mandated step keeps order + gives true stage wall-clock. Summary falls back to `~? min` if the marker is missing (e.g., assert-step failure — that fact is itself visible via the missing/outcome column). |
| 4 | Job summary reports ONE stage wall-clock figure, not per-check durations. | Per-check timing would force wrapping each canonical `run:` command (breaking REQ-027 exactness of `run: bun tsgo` etc.). REQ-028 durations clause is SHOULD-level; executed-checks+outcomes+duration ARE reported. Task 3.3 may extend. |
| 5 | Cache actions pinned to v4.3.0 (plan sketch said just `v4`). | "pick latest stable v4/v5 releases similarly" per brief — v4.3.0 IS latest v4.x at authoring time (2026-08-26). Save key retains the plan-literal `ci-epoch-<os>-<lockhash>-<run_id>` shape; TODO comments hand the split-key finalization to Task 3.3. |

## 5. Checklists concluded (pointers)

- **QL** ✅ → §3.1 (actionlint exit 0 post-fix; checksum-proven binary; shellcheck sweep 0 findings).
- **TE** ✅ → §3.2 boundaries (128-attributed failure / verbatim noop / real pr changed-set green) + §3.3 parity table + §3.4 parses.
- **SEC** ✅ → §3.4 rows: permissions, SHA-pinning regex, pull_request_target absence (ban-comment only), upload-artifact absence, github.event.* never in run blocks. Plus REQ-030 extra-words grep NONE.
- **SR** ✅ → §3.4 rows: canonical order/fail-fast adjacency, timeouts 4/4, persist-credentials 4/4, if-always() pair, forbidden-pattern greps, REQ-053 verbatim names, anchor-free bootstrap parity. Scope boundary: `git status` limited to `.github/workflows/ci.yml` + plan-dir bookkeeping (§2).
- **IV** ✅ → §6 compliance table below signed off row-by-row against specs.md REQ text re-read at authoring time.

## 6. IV — Requirement Compliance Table

| REQ | Subject | Where satisfied (line refs into `.github/workflows/ci.yml`) |
|---|---|---|
| REQ-010 | Trigger coverage | L29–34: pr types+branches; push branches — exactly the four PR types on develop/main + pushes |
| REQ-011 | Fork safety / no `pull_request_target` | L11–13 header ban (policy language); parsed `on:` = {pull_request, push} only; `contents: read` token L36–37; zero secrets referenced (no `secrets.` context anywhere — grep) |
| REQ-012 | Bun runtime pinning + fail-fast | L93–101 assert-bun-pin (named stderr message, exit 1); L102–105 setup-bun `bun-version-file: package.json` SHA-pinned v2.2.0 |
| REQ-013 | Deterministic install | L123–124 (`Install dependencies (deterministic frozen lockfile)` = `bun install --frozen-lockfile`), reached after caches, before any check |
| REQ-014 | Mechanical quality gate, canonical entry points | L125–139: tsgo/oxlint/biome/lint/duplicates steps invoke exactly the repo commands (parity §3.3); failures surface raw tool output natively |
| REQ-015 | Fail-fast canonical order | L125–139 sequence tsgo→oxlint→biome:check→lint→check:duplicates verified contiguous-ascending (§3.4) |
| REQ-016 | DBML always-full, blocking | Job L190–209 has NO `paths:`/path filter and NO conditions; `bun validate:dbml` runs every triggered run |
| REQ-017 | Mermaid changed-set w/ full-set fallback + explicit noop | docs-validation L211–231: wrapper step env-maps EVENT_NAME/BASE_REF; noop semantics proven §3.2 (wrapper owns them per outcome 2.3) |
| REQ-018 | Merge-blocking named checks | Job keys ARE the check names (workflow-sanity, quality, dbml-validation, docs-validation + future tests-* listed in header L20–22) |
| REQ-019 | Stage independence/parallelism; tests gated later | dbml/docs declare no `needs`; quality `needs: workflow-sanity` L85; tests-jobs gating is Task 3.3 scope |
| REQ-023 | Cache strategy, never clear | Restore L106–122 (bun + eslint families), save L143–153; zero delete/invalidate statements (greps) |
| REQ-024 | Superseded-run cancellation, push audit trail | L39–41 group + `cancel-in-progress` expression true ONLY on pull_request |
| REQ-025 | Timeout bounding | L56 sanity 5, L87 quality 15, L192 dbml 5, L213 docs 5 — declared per job |
| REQ-026 | Failure signal quality | Tools print native diagnostics (no wrappers/truncation); broken-base case surfaces git's own error verbatim (§3.2 row 1) |
| REQ-027 | Local↔CI parity | Every `run:` = repo-local documented command (table §3.3); composition lives in committed `scripts/ci/validate-docs-ci.ts` |
| REQ-028 | Job summary | L150–181 L154–184 summary step writing markdown digest (checks + outcome per check + stage wall-clock), `if: always()` |
| REQ-029 | Workflow syntax correctness gate | The `workflow-sanity` job ITSELF (L54–82) + local actionlint exit 0 evidence §3.1 |
| REQ-030 | Minimal token permissions | L36–37 `contents: read` ONLY; extra-permission-word grep NONE |
| REQ-032 | Action pinning by full SHA + version comment | All 10 `uses:` lines full-length SHA + `# vX.Y.Z` (regex-verified §3.4); SHAs resolved via GitHub API §3.5 |
| REQ-033 | Cache poisoning guard | Save key embeds `github.run_id` ⇒ unique-per-run, no overwrite of branch saves; GH-native branch scoping noted L145–147 |
| REQ-034 | No persisted checkout credentials | 4/4 checkouts `persist-credentials: false` |
| REQ-035 | Script-injection defense | github.event.* appears ONLY in concurrency exprs + step-level `env:` mappings; run-block scan NONE (§3.4); wrapper consumes env, never shell-interpolated |
| REQ-044 | Concurrency correctness across events | Group = PR number on PRs, ref otherwise (L40) — independent groups, no cross-group cancellation shape |
| REQ-045 | Cache atomicity | Keys content-addressed via `hashFiles('bun.lock', 'bun.lockb')` (restore key L113, save key L153); miss degrades to cold-correct |
| REQ-047 | Fork/concurrency interaction | Keyed groups cancel only their own superseded runs; `cancel-in-progress` expression restricts auto-cancel to PR context (L41) |
| REQ-050 | Blocking taxonomy | Each listed validator/test class maps 1:1 to a hard-failing step/job; nothing suppresses non-zero exits (greps §3.4); warnings-only tolerance stays owned by tool configs |
| REQ-051 | Exit-code contract | Zero `\|\| true` / `continue-on-error` / retry logic (§3.4); only cache-save + summary are `always()` |
| REQ-053 | Error attribution strings | Step names verbatim: `DBML validation (db/schema.dbml)` L204, `Mermaid validation (changed docs; full set on push)` L227, `ESLint (full-repo…)` L130(+name), tool-named steps otherwise |
| REQ-063 | Docs watch-set for Mermaid gate | Enforced INSIDE the wrapper (`WATCH_PATTERNS` = `/\.mmd$/` + `/^docs\/.+\.md$/` + fence-content fallback — frozen Task 2.1 exports, pinned by its unit suite); workflow passes through unchanged; pattern list recorded at outcome 2.1 §1/phase 0 §6 rather than duplicated into YAML |
| REQ-085 | Header links canonical doc | L4 header comment: `Canonical reference: docs/quality/ci-pipeline.md — READ THAT DOCUMENT BEFORE EDITING THIS FILE` (+ Phase-7 same-PR note) |

## 7. Carry-Forward Notes For Downstream Tasks

1. **Task 3.3**: (a) add tests-db/services/ui WITHOUT renaming existing jobs or the four reserved check names (ruleset surface); (b) finalize the cache save-split using run-id-suffixed keys — seams marked with TODO comments around L106/L143; (c) codegen-drift placeholder comment marks the insertion point AFTER `check:duplicates` and BEFORE cache-save — evaluate REQ-061 determinism 3× locally before enabling; (d) `.env.test.ci` + materializer + postgres service wiring must keep `DATABASE_URL` confined to tests-db.
2. **Branch protection (3.4)**: required-check names must be copied FROM the job keys verbatim.
3. **actionlint maintenance**: when bumping `ACTIONLINT_VERSION`, refresh BOTH the env version string and `ACTIONLINT_SHA256` from the official `actionlint_<ver>_checksums.txt` asset (mismatch = refuse-and-fail loudly by design).
4. **Local verification tools**: actionlint/shellcheck binaries fetched under /tmp are session-scoped, never staged; identical fetch is reproducible from §3.1 commands.
