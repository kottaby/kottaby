# Outcome 1.2 — `.env.test.ci` Committed Template

> **Task ID**: 2-a (Task 1.2 of plan DEV3-001 Phase 1) · **Agent**: phase1-schema-na · **Branch**: `dev3-001/ci-cd-pipeline`
> **Plan dir note**: plan text references `ai/plans/dev3-001-ci-cd-pipeline/`; ACTUAL dir `ai/plans/dev3-001-cicd-pipeline-with-dbml-mermaid-validati/`.
> **Date**: 2026-08-26 · **Requirements**: REQ-022, REQ-031, REQ-038 (+ cross-cited REQ-060/REQ-076 from Task 1.1 record)

---

## 1. File Authored: `.env.test.ci` (repo root)

Exact key set (mandated 5 keys, MINIMAL — no extra keys; brief comments only):

```dotenv
TEST_SERVER=1
TEST_CI=1
DATABASE_URL=overridden-by-ci
DATABASE_ENCRYPTION_KEY=cafef00dcafe1234cafef00dcafe1234cafef00dcafe1234cafef00dcafe1234
AUTH_COOKIE_SECURE=false
```

Header comment states verbatim: *"no secrets; CI fixture values only; values marked `overridden-by-ci` are replaced by workflow env."*

### Format research (per task instruction: check expected DATABASE_ENCRYPTION_KEY shape first)

| Source | What it says |
|---|---|
| `.env.example:18-21` | "Database Encryption Key used for Column-Level Encryption (AES-256-GCM)"; generate via `openssl rand -base64 32` |
| `backend/lib/env.ts:59,165-166` | optional outside production: throw only when NODE_ENV=production and key unset ("AES-256-GCM column-level encryption") — presence/shape only, no charset validation |
| `backend/lib/auth/jwt.ts:18-21,64-70` | dev fallback treats it as the "64-char hex AES key"; consumed as an OPAQUE string hashed `<key>:<domain>` SHA-256 (`deriveDevSecret`) |

Conclusion: no consumer strictly parses hex/base64 at load time; a **64-hex-char literal** satisfies both documented shapes and dotenv-safe charset rules. Fixture value is a deterministic patterned literal (`cafef00d…1234` × 4) — *obviously non-production*: never rotated, protects nothing, recorded publicly here (publishing it is by-design proof of fixture status).

### Loadability smoke (bun 1.3.14)

`bun --env-file=.env.test.ci -e …` → all 5 keys resolve set; regex `/^[0-9a-f]{64}$/` matches DATABASE_ENCRYPTION_KEY (len=64).

## 2. 1.2.QL — Quality Loop equivalent (gitignore proofs)

Per-file `sub-loop.ts` N/A for non-TS file (per Non-Negotiable Execution Protocol #2 and task rules). Required evidence instead:

| Command | Result | Meaning |
|---|---|---|
| `git check-ignore -v .env.test.ci` **before fix** | exit 0 → ``.gitignore:35:.env*	.env.test.ci`` | template WAS ignored ⇒ negation required |
| `git check-ignore .env.test.ci` **after fix** (plain) | **exit 1, zero output** | NOT ignored ⇒ committable ✓ (required proof) |
| `git add -n .env.test.ci` after fix | `add '.env.test.ci'`, exit 0 | git accepts it for commit ✓ |
| `git check-ignore -v .env.test.ci` after fix | prints ``.gitignore:38:!.env.test.ci`` (last-matching-rule display includes negations) | names the exact exempting rule (evidence aid; plain-form run above is the authoritative exit-code proof) |
| `git check-ignore -v .env.test` | exit 0 → ``.gitignore:35:.env*	.env.test`` | runtime `.env.test` IS gitignored ✓ |
| `ls .env.test` / `git ls-files --error-unmatch .env.test` | nonexistent / "did not match any file(s)" | untracked + absent ✓ |
| `git check-ignore .env.example` after fix | exit 1 | pre-existing opt-in `!.env.example` unaffected ✓ |

### ⚠️ Baseline correction carried forward

Phase 0 outcome §11 carry-forward note 1 claimed *"`.gitignore:35` `.env*` ⇒ commit name must dodge the pattern (`.env.test.ci` complies)"*. That is **factually wrong**: glob `.env*` DOES match `.env.test.ci` (proven above). Fix applied in scope: added `!.env.test.ci` opt-in to `.gitignore` (lines 37–38, immediately after the existing `!.env.example` opt-in — same established pattern). Phase 0 file intentionally NOT rewritten (outside this task's file scope); this section supersedes its note.

Diff to `.gitignore`:

```diff
 # env files (can opt-in for committing if needed)
 .env*
 !.env.example
+# committed CI test-env template (Task 1.2, plan dev3-001) — no secrets inside
+!.env.test.ci
```

## 3. 1.2.SEC — Security Audit

- Key-name diff vs committed secret-looking values: committed values are `1`, `overridden-by-ci`, fixed patterned hex fixture, `false`. **Zero real credentials**, zero provider tokens/API keys/hostnames/secrets.
- `.env.test`: absent from disk, untracked, covered by `.gitignore:35` (proofs §2). Task 2.2 materializer will create it at CI/local runtime only.
- Template cannot reach real infrastructure even if never overridden: DATABASE_URL placeholder `overridden-by-ci` is not a routable host; materializer contract makes absence of the override a fail-fast named error.
- No `console`-echo risk exists in this task (static file), and per REQ-038 nothing derived from env is printed anywhere by Phase 1 artifacts.
- GitHub secret-scanning safe: patterned hex fixture is not a valid credential prefix for any known provider.

## 4. 1.2.SR — Semantic Review

- Values consistent with `backend/services/AGENTS.md` test posture — line 18: seed writes are "allowed in local dev and CI test servers via `TEST_SERVER` / `TEST_CI`" ⇒ template sets exactly those flags. No app-layer imports involved (pure data file).
- `TEST_SERVER=1` keeps `logger.logDomainError` compact in test logs (REQ-022 posture; cf. `.env.example:223-226`, `test/scripts/run-server-tests.ts:178` already injects TEST_SERVER="1" for spawned servers).
- `AUTH_COOKIE_SECURE=false` matches `.env.example:185-189` ("Required in .env.test for Playwright" over http://localhost) and `test/ui/AGENTS.md:69`.
- `DATABASE_URL=overridden-by-ci` is the contract token Tasks 2.2 (materializer requires override) and 3.3 (workflow supplies ephemeral service-container URL, REQ-031) already expect; ledger D3's resolution path unchanged.
- Scope boundary: `git status --porcelain` shows only `.gitignore` (M), `.env.test.ci` (??), two outcome files (??), tasks.md (M). Schema/types/backend trees untouched (couples with outcome 1.1).
- Semantic Review checklist deltas applicable here: no dead branches/N/A code task; no module-level mutable state introduced; deferred-work rule satisfied — no NEW ledger row needed because consumption of this template is already owned by Tasks 2.2 + 3.3 via their mandated "Files to inspect: .env.test.ci" contracts (D3 tracks the materialization gap end-to-end).

## 5. 1.2.IV — Instruction Verification

Read `test/integration/AGENTS.md` IN FULL. Policy: "Secrets and provider URLs live in **`.env.test`** (gitignored)" (§ Environment); "`test:integration` is **not** part of default CI — suites skip when required env keys are absent" (§ Commands); gating via `describeLiveWhen` ⇒ `describe.skip` when keys missing (§ Gating).

**Template compliance**: `.env.test.ci` contains ZERO live-provider keys (no RESEND_*, TWILIO_*, FCM_*, ZOOM_*, GOOGLE_MEET_*, MS_TEAMS_*, WHATSAPP_*, OPENEXCHANGERATES/FIXER/UPSTASH/REDIS_*). The materialized `.env.test` therefore drives every live-key suite into its designed skip state — the template honors both the gitignore policy and the "live-key suites excluded from CI" policy by construction. The `!.env.test.ci` opt-in exempts ONLY the static, secret-free template; `.env.test` itself stays ignored (proof §2).

Also read `backend/services/AGENTS.md` (present) — see SR §4 posture alignment.

## 6. Carry-forward notes for downstream tasks

1. **Task 2.2 (materializer)**: parse must treat comment lines/blank lines as ignorable (template contains header comments); the only sentinel value requiring override is `overridden-by-ci` on DATABASE_URL (verify against your spec'd key list — spec also phrases print-contract `missing required CI env variable: <KEY>`).
2. **Tasks 2.2/3.3 — flag-value nuance (informational, no action owed)**: `backend/lib/test-ci-env.ts:5` recognizes `TEST_CI === "true"` or `CI === "true"` — NOT `"1"`. The template ships the plan-mandated literal `TEST_CI=1`; on GitHub Actions `CI=true` is always present so `isTestCi()` resolves true regardless. Do not add code paths that depend solely on `TEST_CI=1`.
3. **Task 2.2 tests (Tier 2)**: template deliberately has NO CRLF issues and NO trailing spaces (written LF) — do not assume those Tier-2 cases exercise this committed artifact; they still belong in the unit suite.
4. **Review waves**: any future env-key addition to `.env.test.ci` must keep zero-secret invariant (add keys only if referenced by CI-run bootstrap; document in `.env.example` per repo convention).

## 7. Files Written / Modified By This Task

| File | Change |
|---|---|
| `.env.test.ci` | NEW — committed CI fixture template (5 mandated keys + comments only) |
| `.gitignore` | +2 lines — `!.env.test.ci` opt-in negation (required; proven §2) |
| `ai/plans/dev3-001-cicd-pipeline-with-dbml-mermaid-validati/outcome/1.2-outcome.md` | This file |
| `ai/plans/dev3-001-cicd-pipeline-with-dbml-mermaid-validati/tasks.md` | Checkboxes [x]: 1.1, 1.1.A, 1.1.B, 1.1.SR, 1.2, 1.2.QL, 1.2.SEC, 1.2.SR, 1.2.IV (9 total) |
