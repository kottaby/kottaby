# Deferred Items Ledger

**Feature:** `dev3-002-shared-error-handling-response-contracts`  
**Plan Directory:** `ai/plans/dev3-002-shared-error-handling-response-contracts/`  
**Created:** `2026-08-26`

---

## Purpose

This ledger tracks all work deferred from one task to another to ensure no deferred item is forgotten. Every deferred item must be explicitly logged here and resolved before the plan is marked complete.

---

## Ledger Table

| ID | Deferred Item | Source Task | Target Task | Status | Verified By | Notes |
|---|---|---|---|---|---|---|
| BLT-01 | [HIGH][i18n] 7 of plan's 10 "new" errors keys already exist in `ErrorsLabels` (internalServerError, unauthorized, forbidden, notFound, conflict, badRequest, serviceUnavailable); `validationFailed`↔`validation` and `rateLimited`↔`rateLimitExceeded` near-duplicate semantics (REQ-055) | 0.1 | 1.2 (+ naming decision recorded in its outcome) | ✅ Done | Task 1.2 → `outcome/1.2-outcome.md` (reuse table + naming decisions; QL×3 exit 0; tsgo 0 errors; /tmp parity script PASS) | RESOLVED by Task 1.2: 7 keys REUSED unchanged; naming decisions recorded — keep canonical `validation` + `rateLimitExceeded`; only `duplicateRequest` added (en/ar/types). Final 18-key list in outcome for Phase 4. |
| BLT-02 | Stale `preloadSession` reference in `backend/graphql/AGENTS.md:135` (also dev2-001 plan artifacts) — symbol does not exist anywhere in backend source | 0.1 | Phase 6 knowledge propagation / deferred doc cleanup | ✅ Done | Task ID 12 → Phase-7 edit of `backend/graphql/AGENTS.md` §Serverless Cold-Start Optimization: bullet replaced with the real anchor `createGraphQLContext` (`gqlContextFactory.ts`) + single requestId resolution note (`resolveRequestId(request.headers)` composed once → `ctx.requestId`, correlation-only); sentence added retiring the legacy name. Sub-loop tsgo-stage exit 0. dev2-001 archived artifacts intentionally untouched (historical records). | RESOLVED Phase 7. Implementation anchors requestId resolution inside `createGraphQLContext`; AGENTS.md corrected explicitly rather than silently. |
| BLT-03 | Missing canonical docs cited by plan + root/backend AGENTS.md: `docs/backend/login-cold-start-resilience.md`, `docs/services/meeting-providers.md`, `docs/services/whatsapp-cloud-api.md` (pre-existing repo-wide dead refs) | 0.1 | Phase 7 canonical doc + deferred | ✅ Done | Task ID 12 → Phase-7 sweep of every live referencing spot (11 edits / 8 files): `backend/graphql/AGENTS.md` ×2 (cold-start See-link redirected to `docs/graphql/error-handling-contract.md`; WhatsApp canonical-ref annotated) · `backend/AGENTS.md` ×2 · `backend/services/AGENTS.md` ×2 · `backend/db/repo/AGENTS.md` ×1 · root `AGENTS.md` ×3 (meeting/WhatsApp bullets annotated; login-cold-start bullet redirected w/ new contract-doc entry added alongside) · `docs/auth/jwt-authentication-service.md` ×1 + `docs/auth/user-registration.md` ×1 (dead path replaced by the canonical contract-doc ref) | CHOICE OF RECORD = update-references-in-place over creating placeholder stubs (avoids bloat, single-canonical-doc rule). Error-contract-adjacent refs now resolve to the new doc; adapter/integration pointer rows carry a uniform `(doc file absent from this tree — pending the owning ticket; see this ledger row)` annotation so no future agent chases ghosts. Archived mentions under `ai/finished_plans/**` and sibling active-plan files left untouched as historical/planning records (their subjects remain future-ticket-owned). No dead path is cited as resolvable anywhere in living docs. |
| BLT-04 | `expectMutationError(expectedCode)` helper absent from tree; stale doc path `frontend/graphql/test/helpers.ts` cited by docs/graphql/domain-error-extensions-code.md:120 | 0.1 | Phase 3 (create helper in `test/helpers/`) | ✅ Done | Task ID 6 (Phase-3 prerequisite leg) → recorded in `outcome/3.1-outcome.md`: created `test/helpers/expect-mutation-error.ts` exporting `expectMutationError(error, expectedCode?): CombinedGraphQLErrors` atop `CombinedGraphQLErrors.is()` + existing `extractErrorCode`; re-exported via `@/test/helpers` barrel; stale doc link + typo fixed opportunistically in `docs/graphql/domain-error-extensions-code.md` §Testing Error Codes; QL sub-loop duplicates exit 0 on helper + barrel | Build atop `CombinedGraphQLErrors.is()` + `extractErrorCode`; real harness exists (testClient, setupTestServerLifecycle). Doc's stale link fixed opportunistically with helper creation. |
| BLT-05 | `test/ui/` scaffold absent (only AGENTS.md): no components/, e2e/, test-env.ts, translation-preload.ts, TestWrapper, `readTranslation`, or Translation registry; package.json scripts reference the nonexistent paths | 0.1 | Tasks 4.2.TE / 5.5 tiers — else deferred post-plan | ⚠️ Partial | plan-review-R1 §(c) row 16; Task ID 7-b re-probe → `outcome/4.2-outcome.md` §TE (`ls test/ui/` = AGENTS.md only; `test/ui/components` ENOENT; preloads absent) | E2E tier uses existing `getDefaultTranslations()` (shared/locale/server.ts:19). Component tier requires scaffold build-out per test/ui/AGENTS.md intent; if blocked at that point, defer tier here rather than faking helpers. **Task 4.2 applied exactly this rule**: shipped the colocated PURE-function tier (`frontend/components/ui/fieldError.test.ts`, 9 pass / 0 fail via sanctioned `run-test.ts` runner) and DEFERRED the Happy-DOM render tier for `PermissionDeniedFallback`/`RetryableNotice` to this row — no scaffold was invented (R1 correction #7). Render-tier unblock path: build test/ui scaffold (preloads + TestWrapper + client-path translation access), then add render tests asserting MUI-v9 classes (`MuiAlert-colorError` etc.), icon presence, retry-disabled-while-pending, `aria-invalid`. **Task ID 7-c re-probe + deferral added**: `test/ui/components` still ENOENT (nothing built between legs); Task 4.3 shipped its render-tier scope as pure-tier equivalents instead — `frontend/lib/mutationFieldErrors.test.ts` (14/0/49) drives a mocked `VALIDATION` CombinedGraphQLErrors through the real wiring asserting exact setError field:message pairs + clear-on-fix logic, and `documents.contract.test.ts` (11/0/53) locks REQ-063 document shape. Render recipe for the unblocked scaffold (4.3 §TE): drive RHF `handleSubmit(onSubmit)` against the mocked VALIDATION container → assert per-field helperText/`aria-invalid`, corrected resubmission clears them. **Post-plan re-audit (R8 @HEAD):** the COMPONENT-tier scaffold HAS since landed upstream — `test/ui/components/` now contains `TestWrapper.tsx` + `happydom-preload.ts` + `translation-preload.ts` + `next-dynamic-mock.ts` and render suites incl. `graph-ql-error-surface-host.test.tsx`; package.json's component-tier script paths now resolve and run via the mandated `bun run test:ui:components` (green 14/14 across review iterations R6–R7, incl. GraphQLErrorSurfaceHost 10/10 — see outcome/review-iteration-R7-outcome.md). Status stays ⚠️ Partial because the remaining scaffold gaps persist: `test/ui/e2e/` directory is still absent (`bun run test:ui:e2e` target) and `test/ui/mobile-desktop-isolation.test.ts` referenced by `test:ui:static` still does not exist. |
| BLT-06 | No Postgres server in sandbox (`DB_PROVIDER=sqlite`) ⇒ REQ-073 DB-provider-dependent test tiers cannot run: live-PG 23505 repository paths, PG portions of `runInRollback` matrix runs | 0.1 | Deferred — re-run on Postgres-capable env / CI | ⚠️ Partial | worklog (0-setup), plan-review-R1 §(c) adaptations | SQLite-parity unit coverage of `translateDbError` stands in for logic-level proof; DB-bound integration tiers must be executed before plan is declared fully verified outside this sandbox. **CI unblock recipe (6.5 gate):** on a Postgres-capable runner set `DB_PROVIDER=postgres` + valid `DATABASE_URL` in the CI test env, then `bun --env-file=.env.test.ci run test/scripts/run-test.ts <repo-tier>` for the REQ-073 live-PG 23505 repository legs, `bun run test:db` / `bun run test:services` (PG `runInRollback` matrix runs), plus BLT-13 row (c) and BLT-14 rows (b)/(c) when their fixtures land. NON-BLOCKING per completion-gate classification: environment-gated, precisely documented; all sandbox-runnable equivalents green at 6.5 (see outcome/6.5-outcome.md). |
| BLT-07 | ESLint full-repo lint tier unavailable in-sandbox: `bun run scripts/lint-service.ts --json --id baseline` exits 1 with empty output (exit ≈38.9s); direct `bun x eslint` child killed twice (suspected OOM with `-r ts6-eslint-patch.cjs --max-old-space-size=8192`). Per-rule totals NOT capturable as baseline | 0.1 | Deferred — re-baseline when env can sustain eslint | ⚠️ Partial | /tmp/baseline-lint.json, /tmp/baseline-lint-plain.txt | Pre-existing environment limitation — do NOT debug during implementation. Lint deltas judged via biome (391 files, 0 diags) + oxlint (371 files, 301 rules, 0 warn/err). Any eslint result obtained later is delta vs. "unavailable" baseline and must be triaged separately. |
| BLT-08 | Legacy taxonomy alias gap: production already emits `RATE_LIMIT_EXCEEDED` (RateLimitExceededError via GraphQL route 429 transport path) while plan canonicalizes only `RATE_LIMITED` | 0.1 | 2.1 taxonomy → carried into finalizeGraphqlErrors + frontend map; documented under "extending the taxonomy" | ✅ Done | Task 2.1 → `outcome/2.1-outcome.md` (alias encoded as data `LEGACY_ERROR_CODE_ALIASES`; accepted by `isErrorCode`, normalized via `normalizeErrorCode("RATE_LIMIT_EXCEEDED")→"RATE_LIMITED"`; QL×3 exit 0; suite 15/15; tsgo 0 errors) | CLOSED by Task 2.1. Consumption contract: pass-through producers keep emitting the legacy code; status/category derivation layers (2.4 envelope, finalize consumers, frontend map) normalize ONLY through `@/backend/lib/errors` → `normalizeErrorCode` + `ERROR_CODE_HTTP_STATUS`. Alias doc goes into the Phase-7 canonical contract doc §"extending the taxonomy". |
| BLT-09 | Layer-doc absences: `backend/lib/AGENTS.md`, `frontend/views/AGENTS.md`, `frontend/components/ui/AGENTS.md` do not exist though tasks cite them unconditionally | 0.1 | Phase 7 knowledge propagation | ✅ Done | Task ID 12 → AMEND-CITATIONS option chosen (ledger-sanctioned alternative to creating minimal layer docs): parent-layer docs extended to cover the missing layers — error-contract/envelope rules now in `backend/AGENTS.md` §Error Handling (governs `backend/lib/**`), component seams guidance in `frontend/AGENTS.md` §Error surfaces & Apollo error mapping (governs `frontend/views/**` + `frontend/components/ui/**`), canonical detail in `docs/graphql/error-handling-contract.md`. Task citations in `tasks.md` read "if present" where they name these paths. | CREATE-MINIMAL option rejected to avoid near-empty layer docs (no independent rule inventory for those layers beyond what parents + the canonical doc already state); nothing silences the absence — parent coverage is explicit. |
| BLT-10 | [MEDIUM][out-of-scope] DEV2-003 contract files are unstable under `bun biome:check` (auto-fix): commit `6160ff2` (post-baseline) landed `backend/types/contracts/**` + an unsorted `export * from "./contracts"` line in `backend/types/index.ts`; at gate re-run biome auto-fixed 9 files and a read-only pass reports 12 diagnostics (6× lint/style/useImportType errors in contracts/*.ts + organizeImports/format assists incl. types/index.ts). Discovered by 2.M gate; per protocol all 9 auto-writes were REVERTED via `git checkout -- backend/types/` (not Phase-2 scope; dev3-002 tree kept clean of foreign fixes). Read-only final diagnostic count = 12, ALL inside DEV2-003-owned paths; zero diagnostics in any dev3-002 file | 2.M | DEV2-003 ticket owner (lint cleanup commit on those files only) | ✅ Done | CLOSED via DEV2-003 commit `314629d` ("fix(types): DEV2-003 review iterations R6-R10 final fixes") landing the requested lint cleanup on those files; verified at 6.5 completion gate: `bun biome:check` → "Checked **447** files, **No fixes applied**", exit 0 AND read-only `bunx @biomejs/biome check .` → exit 0, **0 diagnostics repo-wide** (incl. all 9 formerly-unstable paths); zero files rewritten ⇒ no checkout-revert needed (captures: `/tmp/gate65-biome.txt`, `/tmp/gate65-biome-readonly.txt`). Original evidence when OPEN: /tmp/gate-biome-readonly.txt (full diagnostic dump); /tmp/gate-biome.txt (auto-fix run log); baseline ref phase0-baseline-outcome.md row #2 ("Checked 391 files, No fixes applied" @76ea7fa — contracts dir did not exist then) | NOT caused by dev3-002 Phase 0–2 work (files absent at baseline commit 76ea7fa; introduced between baseline capture and mid-gate by another ticket's commit). Does NOT block Phase 3: tsgo exit 0 project-wide; oxlint 0/0; all five Phase-2 suites green with these files restored to HEAD state. Resolution belongs to the owning ticket so the repo returns to "biome:check = no fixes applied" invariant. Do NOT let Phase 3+ agents' `biome:check` runs keep silently rewriting these 9 files + reverting — triage before scoped commits. |
| BLT-11 | Warning-capable surfaces documented as precedents (`releaseQuotaIfDeducted → {success, warning}`, `deleteClassInstance → DeleteClassInstanceResult{success, warnings}` per docs/graphql/domain-error-extensions-code.md Rules #6/#7) are NOT materialized in-tree: live schema exposes only {login, logout, refreshToken, registerUser}, so REQ-027's "warnings in payload" assertion cannot target a real warning-capable resolver yet | 0.2 inventory → 3.3 | Whichever plan wires the quota / class-instance domains (extend task 3.3's suite then) | ⚠️ Partial | Task ID 6-x3 → outcome/3.3-outcome.md §1-§2; grep across backend/{graphql,services} = zero warning-bearing resolvers; suite A2 pin frontend/graphql/test/warnings/warning-surfacing.test.ts:294 fails loudly if a surface lands without adopting Rule #6/#7 shapes | Convention locked via Section B (test-local verbatim reproduction of the two documented shapes, real graphql-js execute, DomainError-mirror codes, `expectMutationError(container, code)`); wire legs anchor the live surface (A1/A2 inventory, A3 wrapper-in-data, A4 failure-channel). When deleteClassInstance/releaseQuota land: extend Section B to execute THEM over the wire and re-point A2. |
| BLT-12 | frontend/graphql test-runner wrappers cannot complete under this sandbox for ANY file under frontend/graphql/test (out of scope to fix from Task 3.3): (a) direct `bun run test/scripts/run-server-tests.ts <file>` crashes at ENTRY-module load — `TypeError: require() async module …/graphql/index.mjs is unsupported` from graphql-tag UMD via @apollo/client transitively imported by `@/test/helpers`; bunfig `[test] preload` (incl. test/preload/graphql-interop.ts) does not apply to plain `bun run` entrypoints (reproduced 2×, exit 1). (b) delegated `bun run test/scripts/run-test.ts <file>` died silent SIGKILL exit 137 on its 3rd attempt (matches 3.1-outcome §4 "runner prints nothing before a SIGKILL" cascade). (c) Next 16 holds a PER-DIRECTORY dev lock: any leaked/orphaned `next dev` makes lifecycle boot fail with "Another next dev server is already running" → beforeAll timeout ⇒ pkill orphans first. Green path (recorded in suite header): `KOTTABY_TEST_RUNNER_OK=1 bun --env-file=.env.test test --timeout=150000 <file>` after port/orphan hygiene. Infra hint for owners: de-couple runner-entry import chain from @apollo/client (or apply interop preload inside runner entries); harness orphan-cleanup on bail paths (also noted by 3.1 carry-forward #1). One cross-task observation logged during probing: service-thrown `UnauthorizedError` surfaced as masked non-domain boundary item over HTTP once (`[ERROR] Unhandled non-domain error masked at GraphQL boundary [operationName:"Login", errorName:"GraphQLError"]` in logs/next-dev-server-3099.log, attempt-A at 18:53) — candidate Phase-6 triage, NOT investigated further by this ticket | 3.3 | CI enablement owner / test-infra ticket | ⚠️ Partial | Task ID 6-x3 probes recorded in outcome/3.3-outcome.md §3 (exact commands+exits); authored suite itself green via documented invocation: **9 pass / 0 fail / 25 expects, exit 0** | Not a task-code defect; zero production/harness files modified from this leg. CI may keep using wrappers once infra debt lands. |
| BLT-13 | Consolidated CI-only remainder of the Phase-5 integration matrices (Tasks 5.1–5.3, Task IDs 8-a + 8-c): everything sandbox-runnable ran GREEN in-sandbox (5.1: 36/0/222 incl. live-boot wire tier ×2 clean cycles; 5.2: 27/0/118; **8-c addition:** 5.3: 58/0/1063 — `security-abuse.contract.test.ts`). The ONLY cells still gated out are the surface/provider/artifact-tier items that no sandbox or current schema can satisfy: (a) 5.1 FORBIDDEN-over-wire via authenticated low-privilege seeded session on a gated mutation (needs auth-flow fixture; contract itself already locked non-interchangeably at boundary tier + anonymous `me` UNAUTHORIZED wire row); (b) 5.1 `{ENTITY}_NOT_FOUND`, (c) CONFLICT-via-unique-constraint (also BLT-06 PG-provider-bound), (d) VALIDATION-with-fields over the wire, (e) raw-throwing-resolver INTERNAL_SERVER_ERROR over the wire — none of these resolvers exist on today's live root surface (`login/logout/refreshToken/registerUser/_health/me/recitationReadings`); extend `backend/graphql/test/error-contract-matrix.test.ts` when owners land them; (f) 5.2 artifact-tier variant of the masked-500 row: body requestId ⇄ captured dev-server LOG FILE entry during a booted-server run (module-seam parity already proven on BOTH methods); **8-c additions:** (g) 5.3 cross-tenant `{ENTITY}_NOT_FOUND` oracle-resistance probe — foreign-resource body identical to missing-resource body (REQ-031) — no ownership-checking resolver exists on today's live root surface (`login/logout/refreshToken/registerUser/_health/me/recitationReadings`); (h) 5.3 schema-level BFLA probe — public-role input rejected by GraphQL validation layer BEFORE any resolver executes, instrumented call-count = 0 (REQ-032) — needs live instrumented GraphQL boot plus a gated public mutation; (i) 5.3 governance-state login rejection-parity bodies across `is_deleted`/`is_blocked`/`suspended`/wrong-password (REQ-021) — DB-backed seeded login states (PG-bound, also BLT-06); the producer-tier parity MECHANISM is already pinned green (security-abuse Tier 5 byte-identical repeat serialization + digit-free copy, 8 masked rounds identical). Producer-tier abuse proof NOT deferred: forced-failure zero-leak scan across all four surfaces, redaction abuse battery, injection/LIKE round-trip via whitelist fields channel, taxonomy fuzz, rejection-repeat parity all ran GREEN this leg | 5.1–5.3 | CI enablement owner + each owning-surface ticket | ⚠️ Partial | Task ID 8-a → outcome/5.1-outcome.md §🔒 + outcome/5.2-outcome.md §dispositions/§🔒 (full cell-by-cell tables); Task ID 8-c → outcome/5.3-outcome.md (coverage-vs-requirement table, 🔒 rows g/i, log-channel design note for wave 6.4); suites green via mandated runner: `bun run test/scripts/run-test.ts backend/graphql/test/error-contract-matrix.test.ts` · `bun run test/scripts/run-test.ts app/api/set-locale/test/set-locale-route.test.ts` | Sandboxed HTTP boot itself proved HEALTHY this session (probe + 2 teardown-clean cycles), so unlike BLT-12 the blocker is fixtures/surfaces, not runner infra |
| BLT-14 | [HIGH][integration] Storage/contract-bound remainder of Phase-5 chaos tier (Task 5.4, Task ID 8-c): REQ-076's error-path-purity HALF is proven in-sandbox (`backend/lib/errors/test/concurrency-chaos.contract.test.ts` — 12 pass / 0 fail / 702 expects: these producer modules import NO DB surface; allSettled storms byte-equal parallel-vs-sequential across 4 rounds × 36 legs of a six-class adversarial mix with zero shared-state drift post-barrage, exactly-once declared logging under full interleaving, caller inputs unmutated, module constants pristine). Remaining gated cells: (a) shared-idempotency-key replay burst through a LIVE guard proving exactly-one-success + `DUPLICATE_REQUEST` 409 over-wire (REQ-022/REQ-076) — no idempotency service exists in-tree yet (grep: `DUPLICATE_REQUEST` producers absent outside taxonomy/errors lib, api-response tests, docs, migration triggers); (b) replay-after-marked-5xx same-key retry with the 24h-expiry semantic (REQ-043 per `docs/IDEMPOTENCY.md` B-rules) — guard-dependent likewise, doc wording itself asserted at plan-review record; (c) forced child-insert failure inside a transaction ⇒ complete rollback preserved, typed error propagates, boundary masking does NOT swallow the rollback failure (REQ-041 DEV1-002 precedent test reuse/extension) — needs DB-backed transaction-wrapped services (PG-bound environment gate = BLT-06); classification-level guarantee already pinned green (Tier 2 UNIQUE-tail ring classifies CONFLICT through masked peers without signature loss) | 5.4 | CI enablement owner + idempotency-service ticket (rollback cells ride BLT-06 env gate); when a guard lands, extend THIS suite's Tier-1 allSettled pattern against the real guard rather than inventing a new convention | ⚠️ Partial | Task ID 8-c → outcome/5.4-outcome.md (coverage-vs-requirement table ✅/🔒 dispositions); suite green via mandated runner: `bun run test/scripts/run-test.ts backend/lib/errors/test/concurrency-chaos.contract.test.ts` | Zero production modules touched; zero production-bug observations — every hostile input degraded inside documented bounds (cyclic graphs terminate <2.5s, proxies/proxies-with-traps never read sensitive keys, 60k-segment paths cross verbatim while redactor caps boundedly, non-Error zoo yields well-formed masks both surfaces, hostile header battery collision-free across a 400-leg burst) |

---

## Status Values

- ✅ **Done** — Item completed and verified
- ⚠️ **Partial** — Partially completed, needs follow-up work
- ❌ **Blocked** — Not resolved, plan cannot complete until addressed
- 🔄 **In Progress** — Currently being worked on
