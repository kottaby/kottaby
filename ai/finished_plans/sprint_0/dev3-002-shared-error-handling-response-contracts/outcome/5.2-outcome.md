# Task 5.2 Outcome — API-Route Envelope Matrix Completion (dev3-002)

- **Task ID (execution leg):** 8-a
- **Scope:** complete the REQ-072 envelope-matrix rows still open after Task 3.2 for the in-scope route `app/api/set-locale` (the only ADOPTED route per 3.2 §1 disposition table), as far as the sandbox permits, and formally record every remaining cell's disposition.
- **Deliverable:** `app/api/set-locale/test/set-locale-route.test.ts` EXTENDED (same file — reuses its narrowed helpers; avoids a sibling-file clone-detection surface). 11 pre-existing pinned rows untouched; +16 completion rows added.

---

## Pre-execution knowledge read (cited)

`tasks.md` §5.2; `outcome/3.2-outcome.md` (§1 inventory dispositions, §2 adoption decisions incl. GET redirect exemption + catch-parity clause, §3 N/A matrix adaptation notes); `backend/lib/api/api-response.ts` FULL READ (acceptance rules of `resolveRequestId`: trim → non-empty → ≤128 → no comma → control-char-free, else fresh v4 never echoed/truncated; envelope key assemblies; Hop A/B/C classification); `app/api/set-locale/route.ts` (GET/POST branch order — note requestId resolution happens BEFORE the try block, so only in-try faults mask); specs REQ-013/016/017/019/072/077.

## Suite run records

| Gate | Command | Result |
|---|---|---|
| Mandated runner (pre-extension baseline preserved) + post-extension | `bun run test/scripts/run-test.ts app/api/set-locale/test/set-locale-route.test.ts` | **27 pass / 0 fail / 118 expects, exit 0** (re-verified after QL fixes) |
| Regression anchor | 3.2's original 11 rows all still pass inside the same run | ✅ |

## REQ-072 matrix — final disposition after this leg

| Cell | Disposition | Evidence |
|---|---|---|
| 200 read success envelope `{data,requestId}` | ✅ (pre-existing) kept green | exact key-order pin, inbound echo, v4 fallback, cookie flags |
| 201 create success / webhook-ack exemption | **N/A contract-absence** (route creates no resource; webhook route does not exist) | doc-only policy wording carried from 3.2 §1 row 3 |
| 400 malformed body | ✅ (pre-existing truncated-JSON row) + **breadth completed**: array/null/string/no-key/numeric-type JSON bodies all ⇒ exact `["code","message","requestId"]` error keys, localized invalidLocale | test.each ×5 NEW |
| 400 GET-method parity (no locale param) | ✅ NEW method-parity transport-class row with precise id echo | Task-5.2 describe |
| localized en/ar error parity | ✅ FORBIDDEN en+ar (pre-existing) now upgraded: **precise per-request echo isolation** (`corr-forbidden-en` vs `corr-forbidden-ar`, messages genuinely differ) NEW; GET ar `invalidLocale` NEW (completes both-locales on BOTH methods) | Task-5.2 describes |
| 401 missing credentials | **N/A** — route intentionally public (Security Note) | 3.2 decision stands |
| 403 insufficient permission | ✅ adapted origin-CSRF gate (pre-existing + NEW isolation row above) | — |
| 404 missing resource | **N/A** — no resource addressing on this route | 3.2 table stands |
| 409 conflict + DUPLICATE_REQUEST | **N/A** — no idempotency producer or unique entity here (lands with its service ticket) | 3.2 table stands |
| 422 field validation details/fields | **N/A route surface** — single-boolean-preference carries no field list; lib-level fields semantics locked by 2.3/2.4 suites | 3.2 table stands |
| header-fuzz × correlation (REQ-013/D4 stress) | ✅ NEW: oversized>128, comma-collapsed multi-value, C0-control payload surviving a lax transport, empty string → ALL fall back to generated v4 with ZERO fragments echoed; whitespace-padded valid id trims+echoes; hostile-id fallbacks provably UNIQUE across retries | CORRELATION_PROBES loop |
| masked 500 + requestId parity body↔log | POST ✅ (pre-existing stream-fault row). **GET twin ✅ NEW**: `nextUrl` getter fault inside the GET try/catch masks via the catch-parity clause (3.2 §2.2) → 500 INTERNAL_SERVER_ERROR, generic localized text, zero-leak scan negative, EXACTLY ONE correlated log with the SAME `corr-get-masked-500` id both sides | module-seam logger spy |
| masked-500 parity against captured SERVER LOG ARTIFACT | 🔒 CI-only cell (see consolidated note) — requires the booted dev-server's on-disk log capture while asserting correlation ids end-to-end; unit-tier seam parity is now proven on BOTH methods | command below |
| DB-bound runInRollback tiers (REQ-073) | **N/A** — zero DB access on the route (pure envelope helpers per REQ-040) | unchanged |

### 🔒 Consolidated CI-only remainder (ledger row BLT-13)

Single item for this task: the artifact-tier variant of the masked-500 row (body id ⇄ dev-server LOG FILE entry captured from `logs/*.log` during a live boot) — CI executes the suite via `bun run test/scripts/run-test.ts app/api/set-locale/test/set-locale-route.test.ts` after adding a booted-server fixture that reads the harness-spawned server's log file; everything else in the REQ-072 matrix is either proven HERE or recorded contract-absent above.

## Gates

| Gate | Command | Result |
|---|---|---|
| QL sub-loop | `bun run scripts/health/sub-loop.ts app/api/set-locale/test/set-locale-route.test.ts --lifecycle duplicates` | **exit 0** (fixes en route: two template-literal simplifications; one prefer-specific-assertions matcher) |
| Types | sub-loop tsgo stage (project-wide filtered to file) | 0 errors attributable to this file |

Project-wide tsgo currently red ONLY from foreign concurrent-leg files (`security-abuse.contract.test.ts` = Task-5.3 leg mid-write; `concurrency-chaos.contract.test.ts` = Task-5.4 leg) — attribution dump archived at `/tmp/tsgo2.log`; owning legs re-stamp (multi-leg discipline precedent 6-x2/7-b).

## Carry-forward

1. When the WhatsApp webhook route materializes: register its ack-exemption row in the inventory successor (3.2 §6.2 wording already ships in the route docblock).
2. When an idempotency-guarded or field-carrying API route lands: extend THIS matrix pattern rather than inventing a new envelope-suite convention.
3. `failedToSetLocale` stays dormant-by-design (masked path composes the canonical primitive) — pre-Phase-1 frozen key set, not this leg's deletion scope.
