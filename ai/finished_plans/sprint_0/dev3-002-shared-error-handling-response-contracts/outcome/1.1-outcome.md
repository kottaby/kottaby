# 1.1 Outcome — Canonical Error Contract Types (backend/types/errors/)

- **Date:** 2026-08-26
- **Task:** dev3-002 Phase 1, Task 1.1 (+ QL / TE / SEC / SR / IV gates)
- **Status:** DONE — all quality gates exit 0; `bun tsgo` full-project = **0 errors** (baseline held)
- Executed concurrently with Task 1.2 (different subagent, `shared/locale/**`) — zero file overlap; coordination verified via final `git status`.

---

## Pre-execution knowledge read (cited)

1. All existing outcome files: `outcome/plan-review-R1.md` (§(b) findings, §(c) inventory rows #1–#17, §(d) corrections #1–#10), `outcome/phase0-baseline-outcome.md` (baseline discipline: tsgo=0, tooling state, BLT seed), `outcome/0.2-outcome.md` (ground truth incorporated-by-reference).
2. `deferred-items.md` rows BLT-01…BLT-09 (notably BLT-08 legacy `RATE_LIMIT_EXCEEDED` alias → informed the doc comment on `ErrorCode`; BLT-07 eslint full-repo unavailability).
3. Task definition 1.1 in `tasks.md` (lines 64–78 incl. QL/TE/SEC/SR/IV subtasks) and plan `plan.md` §2.2 (exact prescribed shapes/naming — used verbatim as the shape-of-record).
4. Rule files: `backend/types/AGENTS.md` (`{X}Type`/`{X}ReturnType` convention, `.types.ts` location CRITICAL rule, barrel discipline `./` paths + `export *`), `backend/AGENTS.md` (type-definition pattern, DomainError reference), root `AGENTS.md` (barrel conventions, lint workflow, verification loop), `.agents/instructions/backend.instructions.md` (barrel conventions, i18n/logging rules — n/a here since pure types).
5. Existing related code: `backend/lib/errors.ts` read in full first (DomainError hierarchy, codes emitted today: VALIDATION/UNAUTHORIZED/FORBIDDEN/CONFLICT/RATE_LIMIT_EXCEEDED/{ENTITY}_NOT_FOUND) to align contract semantics.
6. During execution, `outcome/1.2-outcome.md` appeared (concurrent Task 1.2 completion) — its FINAL KEY LIST + naming decisions incorporated into carry-forward below.

---

## Files created / modified

### Created
1. **`backend/types/errors/api-error.types.ts`** — canonical transport-contract types, exactly per plan §2.2:
   - `export type ErrorCode` — string union with EXACTLY the nine REQ-010 category codes: `"BAD_REQUEST" | "UNAUTHORIZED" | "FORBIDDEN" | "CONFLICT" | "DUPLICATE_REQUEST" | "VALIDATION" | "RATE_LIMITED" | "SERVICE_UNAVAILABLE" | "INTERNAL_SERVER_ERROR`. Doc-commented as transport metadata, NOT DB/GraphQL enum (Decision D3), with pointer to the Task 2.1 legacy-alias rule (`RATE_LIMIT_EXCEEDED → RATE_LIMITED`, BLT-08).
   - `export interface ApiFieldErrorType` — `readonly field: string` (RHF path), `readonly code: string` (SCREAMING_SNAKE), `readonly message: string` (localized). Deliberately id-less value object.
   - `export interface ApiErrorEnvelopeReturnType` — nested `readonly error` object: `code: string` (category OR custom domain code), `message: string`, `details?: unknown` (REQ-033 whitelist-at-producer), `requestId: string` (REQ-013), `fields?: readonly ApiFieldErrorType[]` (REQ-015).
   - `export interface ApiSuccessEnvelopeReturnType<TData>` — `readonly data: TData; readonly requestId: string`.
   - `export interface GraphQLErrorExtensionsType` — `readonly code: string`, `readonly requestId?: string` (optional per §2.2 prescribed shape; producer decision documented as always-include/correlation-safe), `readonly fields?: readonly ApiFieldErrorType[]`.
   - Zero imports (self-contained types layer — provably no app/frontend/service imports). Every property `readonly`. No runtime values, no GraphQL object shapes, no enum values anywhere.
2. **`backend/types/errors/index.ts`** — single-line barrel: `export * from "./api-error.types";` (matches sibling barrels e.g. `auth/index.ts`).

### Modified
3. **`backend/types/index.ts`** — added `export * from "./errors";` in the existing alphabetical ordering (after `db.types`, before `notifications`). Header doc-comment updated truthfully: notes the `errors` domain holds transport-contract (non-entity) types and adjusts the DEV1-001 inventory sentence ("8 entity domain sub-directories … plus the dev3-002 transport-contract sub-directory `errors`").

### Explicitly NOT changed (and why)
- `backend/lib/errors.ts` — read-only alignment target; taxonomy/re-export work belongs to Task 2.1, not this task.
- `backend/db/schema/enums.ts`, `backend/enum/**`, any Drizzle schema/migration — Decision D3 prohibition + Phase-1 zero-schema-drift invariant (error codes are transport strings, never persisted).
- No GraphQL/Pothos files touched — these are transport-runtime contracts, not SDL shapes (SDL must not change; plan §3.1).
- `shared/locale/**`, `frontend/**` — out of scope (Task 1.2 subagent's territory / frontend phases).
- Plan-artifact edits kept minimal: tasks.md parent checkbox only; deferred-items.md left untouched (no new deferrals discovered — see below).

## Quality gate results (mandatory loop — all exit 0 on FIRST iteration)

| # | Command | Exit | Notes |
|---|---|---|---|
| 1 | `bun run scripts/health/sub-loop.ts backend/types/errors/api-error.types.ts --lifecycle duplicates` | **0** | tsgo ✅ · oxlint ✅ · biome ✅ · lint:type-aware ✅ · check:duplicates ✅ |
| 2 | `bun run scripts/health/sub-loop.ts backend/types/errors/index.ts --lifecycle duplicates` | **0** | all five stages ✅ |
| 3 | `bun run scripts/health/sub-loop.ts backend/types/index.ts --lifecycle duplicates` | **0** | all five stages ✅ |
| 4 | `bun tsgo` (full project, TE fixture present) | **0** | doubles as the TE proof — see below |
| 5 | `bun tsgo` (full project, fixture deleted) | **0** | baseline confirmed clean post-verification |

Sandbox note relevant to BLT-07: **file-scoped** `lint:type-aware` via the sub-loop's in-process lint service SUCCEEDED all three times (only the FULL-REPO eslint tier remains unavailable in this environment). Later tasks can rely on sub-loop lint stage, just not `bun run lint` full-repo.

## TE — type-level verification (tsgo proof)

Temporary fixture `_te-dev3-002-1-1-fixture.ts` (created at repo root → checked by tsconfig `**/*.ts` → `bun tsgo` → **exit 0** → DELETED; absent from final tree):

1. **Barrel resolution**: imported all five names from the ROOT barrel `@/backend/types` — compiles ⇒ root→`./errors`→`./api-error.types` chain resolves, no TS2308 collisions with any of the other 9 domain barrels.
2. **Exact union membership**: `Expect<Equal<ErrorCode, <the nine literals>>>` = true ⇒ no missing member, no extra member.
3. **Exhaustiveness pattern** (Tier-1 precedent for Task 2.1): `{ BAD_REQUEST:400 … INTERNAL_SERVER_ERROR:500 } satisfies Readonly<Record<ErrorCode, number>>` compiles; adding an 11th union member later breaks this map at compile time.
4. **Construction boundary (Tier 2)**: `ApiSuccessEnvelopeReturnType<null>`, error envelope with `details`+`fields`, and extensions with a custom domain code (`"USER_NOT_FOUND"`) all construct cleanly.
5. **Readonly enforcement**: 5 × `@ts-expect-error` mutation attempts (top-level envelope, nested `error.code`, success envelope `data`, `extensions.requestId`, field `field`) each REQUIRED a real compile error ⇒ readonly holds at every level. (`@ts-expect-error` fails the build if its error disappears, so the proof is sound.)
6. **`details` is exactly `unknown`** — `Equal<ApiErrorEnvelopeReturnType["error"]["details"], unknown>` proved; `fields` typed as `readonly ApiFieldErrorType[] | undefined` proved.

## SEC audit

- Types encode NO secret-bearing shapes: no token/password/hash/credential/internal fields exist anywhere in the five exported contracts; `requestId` is an opaque correlation string.
- `details?: unknown` BY CONSTRUCTION: TypeScript cannot assign-or-spread into it without producer-side narrowing — mechanically enforces the REQ-033 whitelist-mapping obligation at every producer (masking/envelope/finalize modules in Phase 2).
- No field can transport raw input echoes/SQL/stack traces by construction: envelopes contain only primitive strings + `ApiFieldErrorType[]` (value objects, themselves primitives) + `unknown`-gated `details`.
- Localized-message rule (REQ-050): `message` fields are producer-populated strings; consumers never format raw codes into user text (enforced downstream, types place no interpolation burden).

## SR semantic review (SKILL.md checklist, scope-filtered)

- Race conditions/concurrency: N/A — pure type module, zero runtime state. ✔️
- Env-config hygiene: no `resolveEnvConfig` anywhere. ✔️
- Dead code: zero dead exports — all 5 names are contractually mandated by tasks.md 1.1 and consumed from Task 2.1 onward; no unreferenced locals. ✔️
- Cross-layer imports: none — types layer imports NOTHING (`backend/types/errors/**` self-contained; grep-clean for `@/frontend`, `@/app`). ✔️
- Scope boundary: `git diff --name-only` final state = my three source files + shared plan artifacts + `shared/locale/**` deltas authored by the CONCURRENT Task 1.2 agent (verified theirs via BLT-01 closure note; I made zero `shared/` edits). New untracked `backend/types/errors/` matches scope exactly. ✔️
- Code quality: no branches/throws possible (types only); no console.*; no manual ReturnType helper needed. ✔️

## IV instruction verification

- Naming per `backend/types/AGENTS.md` conventions: envelope/export names use the `{X}ReturnType` pattern where return-shaped (`ApiErrorEnvelopeReturnType`, `ApiSuccessEnvelopeReturnType`) and the `{X}Type` suffix per plan §2.2's explicit prescription for contract/value-object shapes (`ApiFieldErrorType`, `GraphQLErrorExtensionsType`) — no renames beyond the plan.
- Barrel rules honored: sub-barrel `./` relative path, single-segment, `export *`-only, no imports; root barrel line follows the established ordering/pattern. Consumers import from `@/backend/types` only (no new deep import path created).
- Decision D3 compliance proven structurally: no `pgEnum`, no `backend/db/schema/enums.ts` entry, no Pothos enum registration, no value-level enum object — `ErrorCode` exists solely as a compile-time string union. Schema-drift proof formally owned by Task 1.3.

## Exported surface (canonical names — import from `@/backend/types`)

```
ErrorCode                       // "BAD_REQUEST" | "UNAUTHORIZED" | "FORBIDDEN" | "CONFLICT"
                                // | "DUPLICATE_REQUEST" | "VALIDATION" | "RATE_LIMITED"
                                // | "SERVICE_UNAVAILABLE" | "INTERNAL_SERVER_ERROR"
ApiFieldErrorType               // { field, code, message } — all readonly strings
ApiErrorEnvelopeReturnType      // { error: { code, message, details?, requestId, fields? } }
ApiSuccessEnvelopeReturnType<TData> // { data: TData, requestId }
GraphQLErrorExtensionsType      // { code, requestId?, fields? }
```

## Carry-forward knowledge for later tasks

1. **Task 2.1 (taxonomy)** builds directly on `ErrorCode`: `ERROR_CODE_HTTP_STATUS: Readonly<Record<ErrorCode, number>>` with statuses 400/401/403/409/409/422/429/503/500 (verbatim order above); use the `satisfies` exhaustiveness pattern validated here. Include the `RATE_LIMIT_EXCEEDED → RATE_LIMITED` alias (BLT-08) in `isErrorCode` normalization so legacy `RateLimitExceededError` output passes the guard, then re-declare-free: import `ErrorCode` from `@/backend/types`, never redefine the union locally (tasks.md "no inline shape re-declarations" rule).
2. **`code` fields are deliberately `string`, NOT `ErrorCode`** — custom SCREAMING_SNAKE domain codes (e.g. `${ENTITY}_NOT_FOUND` from `NotFoundError`, errored today in `backend/lib/errors.ts`) are legal transport values; ONLY the taxonomy/status-map layer narrows to categories. Do not "tighten" these two fields to `ErrorCode` later.
3. **Task 2.x i18n mapping** (aligned with the completed Task 1.2 outcome FINAL KEY LIST): INTERNAL_SERVER_ERROR→`errors.internalServerError`, UNAUTHORIZED→`unauthorized`, FORBIDDEN→`forbidden`, CONFLICT→`conflict`, DUPLICATE_REQUEST→`duplicateRequest`, VALIDATION→`validation`, RATE_LIMITED→`rateLimitExceeded`, SERVICE_UNAVAILABLE→`serviceUnavailable`, BAD_REQUEST→`badRequest`. There are NO `validationFailed`/`rateLimited` locale keys.
4. **Task 2.5 / finalizeGraphqlErrors**: `GraphQLErrorExtensionsType.requestId` is optional-in-type but ALWAYS-set-at-producer (correlation decision recorded in §2.2) — implement producers accordingly; don't let Apollo's extension merge be the excuse to drop it.
5. **Task 3.2 (set-locale/graphql routes) + envelope adoption**: type REST responses as `ApiErrorEnvelopeReturnType | ApiSuccessEnvelopeReturnType<TData>`; `details` population must be an explicit property-by-property whitelist build (REQ-033) — `unknown` will refuse naive spreads/satisfies-less dumps, which is intended friction.
6. **readOnly discipline**: fixtures/producer code must treat envelopes as immutable — mutation tests already proven to fail compilation at every nesting level; reuse the `@ts-expect-error`-mutation pattern in Phase 3 contract suites instead of hand-rolled assertions.
7. Deferred-items ledger: NO new rows required from this task. Bonus observation recorded above (file-scoped eslint works in-sandbox) refines BLT-07's applicability but does not resolve it — full-repo tier still unavailable.

## Cross-file dependencies discovered

- `backend/lib/errors.ts` is the upstream value-producer of everything this contract types — Phase 2 must keep its subclass codes (incl. legacy `RATE_LIMIT_EXCEEDED`) compatible with taxonomy normalization; no change needed in Phase 1.
- Root barrel now exports the five contract names globally under `@/backend/types` — future domain barrels MUST NOT introduce colliding symbols named `ErrorCode`, `ApiFieldErrorType`, `ApiErrorEnvelopeReturnType`, `ApiSuccessEnvelopeReturnType<TData>`, `GraphQLErrorExtensionsType` (would break `export *` with TS2308).
