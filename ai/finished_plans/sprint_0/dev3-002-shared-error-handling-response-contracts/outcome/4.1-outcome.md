# 4.1 Outcome — Apollo Error-Link Mapping Module (dev3-002)

- **Task ID (execution leg):** 7-a
- **Date:** 2026-08-26
- **Scope honored:** `frontend/providers/apollo/**` + colocated test ONLY. `frontend/components/ui/**` (concurrent Task 4.2 leg, confirmed active: its untracked files appeared mid-flight) was never read for edit nor touched; no other layer modified.
- **Files changed (complete delta):**
  - `frontend/providers/apollo/error-link.map.ts` — NEW, pure REQ-061 mapping module (no React, no DOM, no network, zero side effects)
  - `frontend/providers/apollo/utils.ts` — MODIFIED (integration into the existing errorLink; see §3)
  - `frontend/providers/apollo/error-link.map.test.ts` — NEW colocated paired suite
- **Mandatory pre-reads (cited):** tasks.md lines 256–278 (4.1 def + REQ-061 row wording); outcome/{plan-review-R1 (#7 scaffold-absent correction, #3 alias carry, [INFO] handle-object i18n mechanism), 0.2 (utils.ts = existing error-handling logic home), 1.2 (FINAL 18-key `errors.errors` list — `validation` & `rateLimitExceeded` are canonical; NO `validationFailed`/`rateLimited`), 2M (Phase-3 reminders, BLT-10 biome discipline), 3.1 (wire `extensions.code` values emitted by finalizer incl. UNAUTHORIZED↔UNAUTHENTICATED reality + legacy alias normalization)}; frontend/AGENTS.md; frontend/graphql/AGENTS.md; .agents/instructions/frontend.instructions.md; docs/IDEMPOTENCY.md §2–3 (DUPLICATE_REQUEST = success-equivalent); specs REQ-016/061/075/034.

---

## 1. Mapping table implemented ↔ REQ-061 rows

`mapGraphQLErrorByCode(code, context)` — context `{ contextKind: "query"|"mutation", hasForm, fields?, requestId? }`, returns typed `GraphQLErrorAction | null`. Branching on normalized `extensions.code` ONLY — zero HTTP-status logic anywhere in the client path (REQ-016).

| Normalized code (server emits via finalizer) | Context | Action produced (`kind`, message handle) | REQ-061 row |
|---|---|---|---|
| `UNAUTHORIZED` (+legacy `UNAUTHENTICATED` at TRIGGER level only) | any | `auth-recovery` → `Errors.unauthorized`; behavior executed by the preserved deduped-refresh path (refresh once → stay+refetch; fail → `buildLoginHref` redirect) | refresh-once then logout/login |
| `FORBIDDEN` | query | `permission-fallback` → `Errors.forbidden` (renders where sections listen; Task 4.2 component) | PermissionDeniedFallback |
| `FORBIDDEN` | mutation | `toast` → `Errors.forbidden` | localized toast |
| `VALIDATION` | hasForm ∧ fields[] present | `form-fields` → pairs verbatim from `extensions.fields[]` for RHF `setError(field,{message})` (type code carried on pair.code) | field-level form errors |
| `VALIDATION` | otherwise | `toast` → `Errors.validation`, pairs STILL attached so a form-bound consumer can adopt them locally | else-branch toast |
| `NOT_FOUND` / `{ENTITY}_NOT_FOUND` (`isNotFoundErrorFamily`) | any | `notice/not-found` → `Errors.notFound` | not-found notice |
| `CONFLICT` | any | `notice/conflict` → `Errors.conflict` | conflict notice |
| `DUPLICATE_REQUEST` | any | `notice/duplicate-request` → `Errors.duplicateRequest`, tone `info`, `duplicateSuccessEquivalent:true` (IDEMPOTENCY §3 success-equivalent UX, NOT an alarm) | "already submitted" notice |
| `RATE_LIMITED` (+local alias mirror `RATE_LIMIT_EXCEEDED`, R1 correction #3) | any | `notice/retry-later` → `Errors.rateLimitExceeded`, retryable; action shape PROVEN counter/threshold/window-free (test pins `"attempts" in action === false`) | retry-later inline |
| `SERVICE_UNAVAILABLE` | any | `notice/retryable-service-unavailable` → `Errors.serviceUnavailable`, retryable manual-retry | retryable notice |
| masked `INTERNAL_SERVER_ERROR` | any | `toast` → `Errors.internalServerError`, `requestIdCorrelationGuidance:true`, masked wire message NEVER propagated | generic toast + correlation guidance |
| anything else (`BAD_REQUEST`, transport presets `GRAPHQL_PARSE_FAILED`/`PAYLOAD_TOO_LARGE`, headerless items) | any | `null` — dispatcher leaves behavior untouched (REQ-061 defines no row; avoids double-reporting transport diagnostics) | — |

Shared mechanics: `correlationRequestId` copied from `extensions.requestId` onto EVERY surfaced action when present (REQ-013). String resolution contract: consumers render `useAppTranslation(Errors)[action.messageKey]` — handles/keyof-typed, never string-literal namespaces, never server text (REQ-002/REQ-075).

## 2. Ground-truth corrections discovered & applied

1. **Legacy trigger literal gap:** pre-existing `handleAuthError` triggered ONLY on `"UNAUTHENTICATED"`, but the DomainError hierarchy + pothos scopeAuth emit canonical `"UNAUTHORIZED"` (backend/lib/errors.ts docblock; 3.1 outcome §authScopes mapping). REQ-061 row 1 was therefore unreachable-by-code before this ticket.
2. **Layer rule:** `.dependency-cruiser.js` `frontend-no-backend-deps` (severity ERROR) forbids even TYPE-only backend imports ⇒ `ApiFieldErrorType` is mirrored locally as `WireFieldError` ({field,code,message}, keep-in-sync note) instead of `import type from "@/backend/types"` — same structural-mirror decision the concurrent 4.2 leg made for `fieldError.ts`. Scoped depcruise over `frontend/providers` proves clean (exit 0).
3. oxlint `no-unsafe-type-assertion` rejects ALL narrowing casts here (even widening to `Record<string, unknown>`) ⇒ guards use assertion-free `Object.entries` property reads; suite narrows mapping results via expect-first helper `mapped()` with explicit throw (zero casts).

## 3. Integration — preservation evidence (integrate, don't replace)

Single edit site: `createErrorLinkHandler`'s callback body extracted VERBATIM into exported `routeApolloLinkError(deps, error, operation)` (same statements/order), and `createErrorLinkHandler` now delegates (`new ErrorLink(cb => routeApolloLinkError(...))`). Wiring order inside the route:

1. `void handleAuthError(error, operation.operationName)` — UNCHANGED machinery byte-for-byte: module singleton authRecovery API (registerAuthRecovery/unregisterAuthRecovery untouched), `inFlightRefresh` dedupe, `getNewAccessToken`, FORBIDDEN-no-redirect branch, login/demoLogin/refreshToken op-name exemption, `/login` pathname guard, `isRedirectingToLogin` suppression, forced-diagnostic logging. The ONLY predicate change: trigger set became `new Set(["UNAUTHENTICATED","UNAUTHORIZED"])` (+ string-narrowed code extraction demanded by Apollo's extension typing) — fixes ground-truth gap #2 above while keeping every dedupe/redirect behavior identical; both literals remain honored for backwards compatibility.
2. `dispatchMappedGraphQLErrorActions(error, operation)` — NEW seam mirroring the registerAuthRecovery pattern: `registerGraphQLErrorActionListener` / `unregister…` (single slot). Behavior today-neutral: publishes mapped actions ONLY (a) after CombinedGraphQLErrors.is, (b) outside SSR, (c) off /login, (d) excluding self-surfaced operations (`login`,`demoLogin`,`refreshToken` — prevents double-reporting inside their forms), (e) skipping auth rows entirely (recovery owns that display), (f) skipping codes without a table row, (g) silently no-op with zero registered listeners (= exactly pre-4.1 behavior until 4.2's toast host registers). Link-level context passes `hasForm:false` — React form state is invisible at link scope; documented adoption protocol: VALIDATION actions ALWAYS carry `fieldErrors` pairs so a form-bound subscriber converts them to `setError(field,…)` instead of toasting fallback copy.
3. `if (error && isNetworkError(error)) { … }` — transport-layer LinkError/connectivity branch PRESERVED VERBATIM (this was the only transport handling that existed; nothing HTTP-status-based existed client-side, matching REQ-016's prohibition).

Evidence: pin tests execute the REAL wiring through `routeApolloLinkError` — connectivity `TypeError("Failed to fetch")` still fires setConnected(false)+notifyDisconnected; both UNAUTHORIZED double-paths proven live (see §4).

## 4. Tests — results & required adaptation

Suite: `bun run test/scripts/run-test.ts frontend/providers/apollo/error-link.map.test.ts` → **29 pass / 0 fail / 98 expects, exit 0** (mandated wrapper works here: pure unit tier, and bunfig `[test]` preloads resolve the @apollo/client entry as normal).

Coverage map: per-code branches ×context ×form×fields for ALL table rows incl. legacy alias folding (`RATE_LIMIT_EXCEEDED ≡ RATE_LIMITED`) and the documented pure-map retirement of `UNAUTHENTICATED`→null; null-row set (`BAD_REQUEST`, `GRAPHQL_PARSE_FAILED`, `PAYLOAD_TOO_LARGE`, `""`, junk); wire guards (`isWireFieldErrorEntry`, malformed-fields filtering); fields[]→pair fidelity (field paths incl. `password.confirm`, order pinned); duplicate-success-equivalence flag; counter-freeness pin; requestId passthrough (masked + non-masked); surface-seam exclusions (auth rows, self-surfaced ops, transport presets, headless, throwing-listener resilience, query-vs-mutation contextKind inference); **UNAUTHORIZED double-path**: two concurrent unauthorized containers share ONE deferred refreshToken mutation (refreshCalls===1) → release token → stay-on-page (href unchanged) + reFetch≥1; failure path → `window.location.href === buildLoginHref("/dashboard?tab=sessions")` (expected computed via imported builder, not hardcoded) and trailing-duplicate suppression (second route mints no additional refresh); fixtures built as genuine v4 `CombinedGraphQLErrors({errors:[…]})` containers.

⚠️ **Adaptation note (REQ-075 / R1 finding #7):** the component-tier `readTranslation(handle, locale)` / `TestWrapper` / `translation-preload.ts` scaffold named by the TE box is ABSENT (`test/ui/` = AGENTS.md only). Per R1 correction #7 all expected user-facing strings resolve via `getDefaultTranslations().errorsTranslations[messageKey]` + direct AR namespace access (`loadAllTranslations("ar").errorsTranslations.<key>` parity probe) — no render tier exists to drive MockedProvider/HappyDOM against (that tier belongs to 4.2's scaffolded components and remains recorded under BLT-05 by leg 7-b). Nothing faked; noted here as the task-required disclosure.

## 5. Gate ledger

| Gate | Command | Result |
|---|---|---|
| QL sub-loop — new map module | `bun run scripts/health/sub-loop.ts frontend/providers/apollo/error-link.map.ts --lifecycle duplicates` | exit **0** (tsgo → oxlint → biome → lint:type-aware → jscpd duplicates) |
| QL sub-loop — modified link wiring | `… frontend/providers/apollo/utils.ts --lifecycle duplicates` | exit **0** |
| QL sub-loop — colocated test | `… frontend/providers/apollo/error-link.map.test.ts --lifecycle duplicates` | exit **0** |
| TE unit tier | `bun run test/scripts/run-test.ts frontend/providers/apollo/error-link.map.test.ts` | **29 pass / 0 fail / 98 expects**, exit 0 |
| Layer isolation (bonus) | `bun x depcruise --config .dependency-cruiser.js frontend/providers` | exit **0**, zero violations (`frontend-no-backend-deps` incl. type-only imports clean) |
| Types project-wide | `bun run tsgo` | exit **0**, `error TS` count **0** — also re-stamps the project-wide stamp that leg 7-b found red solely from this file's mid-edit state |

Tree hygiene: `git status` scoped check shows ONLY my three files plus concurrent-leg artifacts I did not create or touch (`frontend/components/ui/*`, `outcome/4.2-outcome.md`, shared plan-file edits by 7-b). No biome auto-writes were swept (BLT-10 discipline held). No `console.*`; no hardcoded user-facing strings in source; TEST-data strings confined to the fixture allowlist of test/ui/AGENTS.md.

## 6. SR / SEC / IV spot-checks

- **SR:** pure function (no React/hooks/DOM/network/side-effects); zero direct style props, zero icons/components introduced (UI is 4.2's); `Translation`-registry naming resolved to its real mechanism — namespace HANDLE `Errors` + keyof-property access (R1 §(b)[INFO]); no switch-on-TS-enum patterns (`Record` lookup for aliases, `??` resolver chain); no duplicated formatting logic vs backend (map consumes wire codes only).
- **SEC:** branching exclusively on `extensions.code`; masked-500 actions strip server messages by construction (only handles flow out); RATE_LIMITED/DUPLICATE_REQUEST rows leak no thresholds/counters/idempotency metadata; requestId used correlation-only; FORBIDDEN/UNAUTHORIZED never interchanged (query-fallback vs mutation-toast vs recovery ownership pinned by tests).
- **IV:** validated against frontend.instructions.md (alias imports, bun commands, no-console/logger-only, React19/nested-ternary bans), frontend/AGENTS.md (enum-safety record/guard patterns), frontend/graphql/AGENTS.md (Apollo conventions — client-owned errorLink domain), docs/IDEMPOTENCY.md (duplicate semantics), docs/graphql/domain-error-extensions-code.md conventions cited in-module.

## 7. Carry-forward for the browser loops (.BF/.BS — orchestrator-scheduled) & Task 4.2 consumers

1. **Seam contract:** call `registerGraphQLErrorActionListener((action, meta) => …)` once at app scope (mirror of registerAuthRecovery lifecycle) — meta gives `{operationName, contextKind}` for section targeting; unregister on teardown. Note the single-slot semantics.
2. **Render keys:** permission-fallback → `PermissionDeniedFallback` (+ `forbiddenRole` page variant stays available per 1.2 carry-forward); notice/toast surfaces key off `{kind, noticeKind, tone}`; `retryable===true` drives RetryableNotice retry affordance; `requestIdCorrelationGuidance===true` renders "include the id below when reporting" using `action.correlationRequestId`.
3. **Form-binding protocol:** subscribers inside mutation forms should convert `fieldErrors` (even on kind:"toast") into RHF `setError(pair.field, { message: pair.message })`; link always sees hasForm=false by design.
4. **UX semantics already encoded:** DUPLICATE_REQUEST must render neutrally/info (success-equivalent), RATE_LIMITED copy must stay threshold-free, SERVICE_UNAVAILABLE offers MANUAL retry (no auto-retry timers added by this ticket).
5. Operational caveats for E2E: unauthorized-suppression flag (`isRedirectingToLogin`) is module-lifetime by design — scenario tests forcing repeated redirect failures need page reloads between attempts; login/demoLogin/refreshToken failures intentionally produce NO global toast (their forms self-surface).
6. Keep-sync debt: `WireFieldError` mirrors backend `ApiFieldErrorType` (documented inline; 4.2's `fieldError.ts` holds an identical mirror) — if the backend contract gains fields, both mirrors move together.
