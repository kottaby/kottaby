# Task 5.3 Outcome — Security & Abuse Tier (dev3-002)

- **Task ID (execution leg):** 8-c (re-stamp; prior leg died mid-write leaving the suite file present but unverified — this leg verified it, adjusted nothing, and formalized dispositions)
- **Scope:** REQ-074 security/abuse verification battery over the shared error-handling producers, as far as a zero-DB / zero-server-boot sandbox permits; every server-boot cell formally dispositioned into the consolidated ledger.
- **Deliverable:** `backend/lib/errors/test/security-abuse.contract.test.ts` (sole source file of this task; NOT modified by this leg — arrived complete from the interrupted leg and ran green as-is).

---

## Pre-execution knowledge read (cited)

`tasks.md` §5.3 definition + execution protocol; specs REQ-021/030/031/032/033/034/074 rows + REQ-070..077 tier definitions; `deferred-items.md` full ledger (BLT-06 PG absence, BLT-13 consolidation precedent from Task 8-a); outcomes 5.1/5.2 carry-forwards (owning-leg tsgo re-stamp obligation for `security-abuse.contract.test.ts` explicitly assigned to THIS leg); FULL READ of the suite itself (837 lines: LEAK_CORPUS/scan harness, Tier 1–5 describes); producer ground truth (`backend/lib/errors/error-masking.ts` whitelist log bag `{requestId, operationName, errorName, errorMessage(capped), errorKind}` → `redactLogContext`; `OPAQUE_RENDER_BUDGET` cap); root/backend AGENTS.md + tests.instructions.md via sub-loop rule surfacing.

## Suite run records

| Gate | Command | Result |
|---|---|---|
| Mandated runner | `bun run test/scripts/run-test.ts backend/lib/errors/test/security-abuse.contract.test.ts` | **58 pass / 0 fail / 1063 expects, exit 0** |
| Determinism re-run | same command, second invocation | **58 pass / 0 fail / 1063 expects, exit 0** (byte-stable) |
| QL sub-loop | `bun run scripts/health/sub-loop.ts backend/lib/errors/test/security-abuse.contract.test.ts --lifecycle duplicates` | **exit 0** (tsgo ✓ oxlint ✓ biome ✓ lint:type-aware ✓ duplicates-scope skip per jscpd scan bounds) |
| Types project-wide | `bun tsgo --noEmit` | **exit 0, zero errors** — THIS leg performs the owning-leg re-stamp that 5.1/5.2 outcomes deferred ("project-wide red SOLELY from security-abuse…concurrency-chaos mid-write files"); file was already type-clean on arrival |

Zero fixes were required in either iteration loop: the suite is green under the mandated runner and all QL stages on first contact.

## Coverage vs requirement (REQ-074 task rows)

**Legend:** ✅ proven green here · 🔒 CI-only remainder (consolidated, see below) · 🟡 partial shadow-proof at producer tier.

| REQ-074 cell (tasks.md §5.3 wording) | Status | Evidence (suite tiers/tests) |
|---|---|---|
| Forced raw driver failure under PROD config → client payload contains ZERO stack/SQL/env/PII (regex scan: stack frames `\bat \S+:\d+:\d+`, SQL keywords with `$n` placeholders + RETURNING, driver fingerprints `42P01`, driver detail text, PII parameter values, hex64 hash shapes, env names `DATABASE_URL`/`PGPASSWORD`, file paths `.ts:`) | ✅ | Tier 1 · 5 tests scan ALL producer surfaces: rebuilt GraphQL `errors[]`, REST envelope body, masked extensions payload, correlated boundary log line. Extensions pinned to exactly `["code","requestId"]` with no `debug` key under PROD; REST error keys exactly `["code","message","requestId"]`; generic localized message both surfaces |
| …eager diagnostic subject stays lean; DEV contrast control proves leanness is configuration not luck | ✅ | Tier 1: PROD mask emits no `"debug"` even with hostile subject; DEV+`includeDiagnostics:false` suppresses; DEV default keeps whitelisted debug snapshot |
| …credential-shaped OWN properties on the throwable cannot ride either surface | ✅ | Tier 1: `password_hash` key + hash sentinel absent from BOTH wire texts |
| …single correlated PROD log without leaking through stdout/stderr | ✅ | Tier 1 stream-capture: exactly ONE `[ERROR]` line carrying `sec-log-scan`; cause-chain/detail/hint/PG-code/file-path/PII/hash fragments asserted absent from logs too (log channel whitelist = name/message(capped)/kind + correlation ids, then redacted — matches error-masking.ts `buildUnexpectedErrorLogContext`) |
| Cross-tenant probe: caller A targets B's resource → `{ENTITY}_NOT_FOUND` identical to missing-resource body (REQ-031, no existence oracle) | 🔒 | Needs live process + ownership-checking resolvers; NO such resolver exists on today's surface (`login/logout/refreshToken/registerUser/_health/me/recitationReadings` — same root-cause as BLT-13 cells b/e). Consolidated in **BLT-13 extension** below |
| Schema-level BFLA probe: public-role input rejected by GraphQL validation layer BEFORE any resolver executes (instrumented call-count = 0) (REQ-032) | 🔒 | Requires live instrumented GraphQL boot + a gated public mutation; today's live surface has no role-gated public input to probe. Consolidated in **BLT-13 extension** below |
| LIKE/wildcard strings (`%`,`_`,`\`), quotes, SQL fragments, unicode/RTL fuzz round-tripped WITHOUT error-shape corruption (no partial JSON, no leaked driver text) | ✅ | Tier 3 · 26 tests: 12-payload corpus (LIKE metacharacters incl. `%`,`_%`,`\_%_\%`, `'; DROP TABLE`, quote-tautology, `<script>`, POSIX/Windows traversal, RTL override `‮gnp.exe‭`, env-var template, 2048-char wildcard tail) thrown-as-error masks fully on BOTH surfaces with reparsable JSON every time (×12); same corpus round-tripped through the ONLY sanctioned echo channel — `ValidationError.extensions.fields[]` whitelist projection byte-verbatim incl. quotes/backslashes, `field`/`code` intact, message slot localized (×12); mixed 12-element fuzz batch through finalizer keeps envelope shape end-to-end (ar locale); wildcard-bearing UNIQUE failure still classifies CONFLICT — classification immune to payload content |
| Public-endpoint abuse: repeated failures produce GENERIC localized messages, no threshold/counter/existence disclosure (REQ-034) | 🟡→✅ producer-tier | Tier 5 · 2 tests: N=12 rate-limit rejections serialize BYTE-IDENTICALLY (`Set.size===1`) with digit-free copy (`/\d/` negative on rendered message); 8 masked failures repeat identically regardless of distinct underlying throwables — mask cannot disclose attempts-count/existence |
| Governance-state rejection parity — identical bodies across `is_deleted`/`is_blocked`/`suspended`/wrong-password (REQ-021, login service path) | 🔒 | DB-backed login service states + seeded users needed (PG-bound per BLT-06 environment). Producer half pinned above (identical bytes across repeats IS the parity mechanism once wired). Consolidated in **BLT-13 extension** below |

### Bonus hardening shipped by the suite beyond the five task rows

| Extra tier | What it pins |
|---|---|
| Tier 2 redactLogContext abuse battery (REQ-035/042 shadow) | Nested token forests across meeting-provider (zoom/meet) + WhatsApp credential key shapes collapse to `[REDACTED]` ×5 exact census while benign siblings survive; bearer-header VALUES die at ANY nesting (case-insensitive), prose containing "bearers" survives; depth bound >6 enforced with bombsite never read; array bound 64 + explicit overflow marker ordered; counter-shaped keys (`tokenCount`/`secretCount`) share the fate while word-boundary discipline spares prose; caller-owned tree unmutated post-redaction |
| Tier 4 taxonomy enum/case-abuse fuzz | Canonical codes accept+self-normalize with numeric status present per code; alias folds RATE_LIMIT_EXCEEDED→RATE_LIMITED (BLT-08 consumption contract); 14 casing/shape variants (incl. Cyrillic lookalike `СОNFLICT`, trailing-NUL, whitespace padding) rejected byte-exact; prototype-adjacent names (`__proto__`, `toString`, …) never smuggle acceptance; non-string coercers (Symbol, BigInt, toString-liar, arrays, bags) rejected WITHOUT coercion; seeded xorshift 600-string fuzz accepts nothing outside vocabulary; taxonomy table `Object.isFrozen` + frozen-cell write fails + normalization stable/idempotent ×50 |

## Production-bug observations

None red. One design-consistency note for wave 6.4 (pentester): the masked-path SERVER log line intentionally carries whitelisted scalars `{requestId, operationName, errorName, errorMessage(≤budget, "…[TRUNCATED]"), errorKind}` — so raw driver MESSAGE TEXT (e.g. `INSERT INTO …` query shape) may appear in SERVER logs, capped, by contract (PROD_READINESS §4.3 diagnostic split: client payload clean ↔ server single-diagnostic-channel). The suite deliberately asserts everything STRICTLY below the whitelist (cause chain, detail/hint, PII parameter values, stack frames, file paths, PG codes) out of the LOG TOO. No ledger row warranted — behavior documented, pinned, and within spec.

## Gates summary

Runner green ×2 (58/0/1063) · QL duplicates exit 0 · project-wide tsgo exit 0 (zero errors — re-stamp performed) · no production modules touched · deliverable file untouched from the interrupted leg (verified complete-and-green rather than rewritten).

## Carry-forward

1. BLT-13 extension (below) carries the three server-boot abuse cells with their activation trigger; extend THIS suite (or its wire-tier successor) when ownership-checking resolvers or governance-state login fixtures land.
2. REQ-034 note: full threshold-copy nondisclosure also depends on the rate-limit backend ticket (DEV2-002) honoring digit-free copy — taxonomy locale key `rateLimitExceeded` currently has none, and Tier 5 will fail loudly if a counter ever leaks into copy.
