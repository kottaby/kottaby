# Task 5.1 Outcome — GraphQL Error-Contract Integration Matrix (dev3-002)

- **Task ID (execution leg):** 8-a
- **Scope:** REQ-071 full error-contract grid structured per the dispatch brief as `{DomainError subclass × wire shape}` rows, plus the REQ-063-mandated assertion conventions (`CombinedGraphQLErrors` containers + `expectMutationError(container, expectedCode)`), with every sandbox-runnable assertion executing GREEN HERE via the mandated runner.
- **Deliverable:** `backend/graphql/test/error-contract-matrix.test.ts` (new; sole source file created by this leg).

---

## Pre-execution knowledge read (cited)

`tasks.md` §Phase 5 (defs 5.1–5.4); outcome carry-forwards: `3.1-outcome.md` (single-registration evidence + formatError⇄finalizer RAW_ERROR_HOP contract + boot-cascade verdict §4/§6), `2.1-outcome.md` (taxonomy ownership + alias normalization recipe + `@/backend/lib/errors` file-as-barrel resolution), `2.2/2.4/2.5-outcome.md` (masking/envelope/requestId signatures), `3.2-outcome.md`, BLT-04/08/11/12 rows, `worklog.md` (6-x1…7-d); helpers: `test/helpers/{graphql-test-helpers,test-lifecycle,expect-mutation-error,index}.ts`; ground truth FULL READ: `backend/graphql/graphqlErrorsFinalizer.ts`, `backend/lib/errors/error-masking.ts` (classification hops A/B/B2/C), `backend/lib/errors.ts` (DomainError hierarchy), `error-code-taxonomy.ts`, `shared/locale/en/errors/index.ts`; `backend/graphql/AGENTS.md`; specs REQ-063/071/073/077; schema.graphql root surfaces (`_health`, `me`) + `auth.query.ts` / `pothos/builder.ts` scopeAuth mapping source.

## HTTP-boot probe verdict (dispatch step 1 — recorded honestly)

One throwaway `setupTestServerLifecycle` + `testClient` probe was executed first (file authored, run, then DELETED — stray hygiene held):

- After orphan/port hygiene (`pkill -f "next dev"; pkill -f next-server`; ports confirmed free), the harness **booted cleanly**: `POST /api/graphql 200 in 3.0s (application-code: 60ms)` in the Next log — the REQ-071 boot lifecycle IS healthy in this session.
- The probe test itself failed only for a client-side reason (raw query string handed where Apollo v4 demands a parsed document — fixed permanently by authoring the matrix with `gql` documents per the warning-surfacing precedent).
- Consequence: the matrix ships a LIVE wire tier inside the same suite instead of deferring everything — and it proved stable across TWO clean boot+teardown cycles this leg (orphan check clean after each).

## Suite run records

| Gate | Command | Result |
|---|---|---|
| Mandated runner | `bun run test/scripts/run-test.ts backend/graphql/test/error-contract-matrix.test.ts` | **36 pass / 0 fail / 222 expects, exit 0** — reproduced TWICE (second run = fresh boot cycle) |
| Boot-probe probe (throwaway, deleted) | direct `KOTTABY_TEST_RUNNER_OK=1 bun --env-file=.env.test test …tmp-boot-probe.test.ts` | verdict above |

## The matrix (all cells below RAN GREEN HERE unless marked 🔒)

**Legend:** ✅ ran green in-sandbox this leg · 🔒 CI-only (see consolidated ledger note) · shapes: LOC = graphql-js located carrier `{message,path,locations,originalError}` · HOP = plain formatted carrier + non-enumerable `attachRawErrorHop` (exact Apollo ≥5 route output).

### Section 1 — pass-through grid `{subclass × shape}` (code/message/path/locations/requestId attachment)

| DomainError subclass → code | LOC | HOP |
|---|---|---|
| `NotFoundError("USER")` → `USER_NOT_FOUND` | ✅ | ✅ |
| `UnauthorizedError` → `UNAUTHORIZED` | ✅ | ✅ |
| `ForbiddenError` → `FORBIDDEN` | ✅ | ✅ |
| `ConflictError` → `CONFLICT` | ✅ | ✅ |
| `ValidationError(+fields[2])` → `VALIDATION` (+ verbatim fields mirror, order+values) | ✅ | ✅ |
| custom `DomainError("BAD_REQUEST")` (taxonomy-native producer code) | ✅ | ✅ |
| `RateLimitExceededError` → `RATE_LIMIT_EXCEEDED` (legacy literal, BLT-08) | ✅ | ✅ |

Cross-cutting pins: localized en message preserved verbatim per row; `extensions.requestId` attached (`matrix-corr-1`); path `["mutation","registerUser",0]` (mixed string/number segments) + locations preserved through BOTH classification shapes; DEV-debug surface absent from serialized body; omitted `ctx.requestId` ⇒ key ABSENT (no empty-string correlation); deliberate EMPTY `fields[]` survives presence-semantics verbatim; ar-context row keeps identical transport CODE with its own `tAr.unauthorized` message (codes are locale-invariant constants).

### Sections 2–6 — behavior tiers (each ✅ = executed green here)

| Tier | Row | Result |
|---|---|---|
| authScopes pairing (REQ-020) | UNAUTHORIZED vs FORBIDDEN finalized side-by-side — codes never interchanged/masked; status derivation apart via taxonomy map (401≠403) | ✅ |
| authScopes pairing | neither scope failure emits the masked `logger.error` channel (zero calls pinned) | ✅ |
| masked tier | raw SQL/path-leaking Error behind HOP → generic localized `INTERNAL_SERVER_ERROR`, requestId present, leak-regex scan negative, EXACTLY ONE redacted correlated log whose bag carries the SAME id | ✅ |
| masked tier | ar locale masks through server translations w/ own correlation id (TypeError original) | ✅ |
| masked tier | opaque probe-less item still masks; kind metadata rides LOG ONLY, never wire | ✅ |
| masked tier | two masked elements in one result ⇒ one correlated log EACH, identical ctx requestId | ✅ |
| protocol presets | all six preset codes pass AS-IS: message untouched, code NOT rewritten, protocolExtra kept, DEV `stacktrace` key stripped, requestId added, ZERO logger.error | ✅ ×6 |
| legacy alias | alias literal passes VERBATIM everywhere while `normalizeErrorCode→RATE_LIMITED→429` folds only statuses; casing variants rejected; `DUPLICATE_REQUEST→409` contrast; alias-classified producers are DOMAIN pass-through (silent, unmasked) | ✅ |
| plugin surface | factory exposes exactly `{requestDidStart}` (the once-per-server registration artifact — single-registration site itself re-pinned by 3.1 suite/gates) | ✅ |
| wire tier (LIVE HTTP) | `_health` zero-op identity: intact data, NO error channel over full stack | ✅ |
| wire tier (LIVE HTTP) | malformed GraphQL document → `GRAPHQL_PARSE_FAILED` preset over the wire WITH `requestId:"wire-parse-failed"` echoed (protocol-preset contract proven END-TO-END, not just at composition level) | ✅ |
| wire tier (LIVE HTTP) | unknown-field failure → `GRAPHQL_VALIDATION_FAILED` preset via real client | ✅ |
| wire tier (LIVE HTTP) | anonymous gated field `me { id }` → **UNAUTHORIZED** with `path:["me"]` — schema-level scopeAuth contract over live HTTP, never FORBIDDEN (REQ-020) | ✅ |

### 🔒 Consolidated CI-only remainder (ledger row BLT-13)

Live-HTTP rows that remain gated OUT of any sandbox run are exactly: (a) **FORBIDDEN over the wire** (needs an authenticated low-privilege seeded session driving a permission-gated mutation — auth-flow + deterministic provider territory; boundary pair + anonymous `me` wire row already lock the non-interchange contract itself); (b) `{ENTITY}_NOT_FOUND`, (c) CONFLICT-via-unique-constraint (also PG-provider-bound per BLT-06), (d) VALIDATION-with-fields over the wire, (e) masked INTERNAL_SERVER_ERROR originating in a raw-throwing RESOLVER — none of these resolvers exist on the current live surface (`login/logout/refreshToken/registerUser/_health/me/recitationReadings`), so they activate when their owning tickets land surfaces (extend THIS suite then). Command basis for CI: `bun run test/scripts/run-test.ts backend/graphql/test/error-contract-matrix.test.ts`.

## Gates

| Gate | Command | Result |
|---|---|---|
| QL sub-loop | `bun run scripts/health/sub-loop.ts backend/graphql/test/error-contract-matrix.test.ts --lifecycle duplicates` | **exit 0** (fixes en route: value-import CombinedGraphQLErrors runtime binding; location-rebuild narrowing via explicit guard; sonar prefer-specific-assertions matcher) |
| Types — attributable delta | filtered tsgo from both sub-loop runs + final project-wide sweep attribution | **0 errors attributable to 8-a files** |
| Project-wide `bun run tsgo` | see /tmp/tsgo*.log attribution | currently exit 1 caused SOLELY by foreign concurrent-leg files `security-abuse.contract.test.ts` (Task-5.3 leg, mid-write @20:42) and `concurrency-chaos.contract.test.ts` (Task-5.4 leg) — untouched per multi-leg discipline (6-x2 §4 / 7-b precedent); owning legs re-stamp |

## Carry-forward

1. Extend THIS suite (Sections 2/7 + new wire rows) when FORBIDDEN-capable or entity-CRUD surfaces land; keep REQ-063 helper discipline.
2. Wire-tier stability note: clean-port hygiene before runs remains prudent in this environment even though both cycles here were clean (pkill orphans first).
3. REQ-073-flavored CONFLICT repository-path rows stay owned by BLT-06 (Postgres-capable env) regardless of surface availability.
