# 7.2 Outcome — Layer AGENTS.md & Root Reference Updates

- **Task:** `tasks.md` §7.2 (Task ID 12, Phase 7 knowledge propagation)
- **Edits (additive, terse, pattern-matched):**
  | File | Change |
  |---|---|
  | `backend/graphql/AGENTS.md` | **BLT-02 fix**: stale `preloadSession` bullet replaced by real anchor `createGraphQLContext` (single requestId resolution point, `resolveRequestId(request.headers)` composed once, legacy name retired). Added boundary rules: masking belongs to the boundary only; **exactly one** `createGraphqlErrorsFinalizerPlugin()` registration site (`app/api/graphql/route.ts`). |
  | `backend/AGENTS.md` | §Error Handling extended: canonical contract pointer, taxonomy-only-status rule (`ERROR_CODE_HTTP_STATUS` via `normalizeErrorCode`; numeric literals prohibited elsewhere), envelope-helpers location (`@/backend/lib/api` barrel). |
  | `shared/AGENTS.md` | New subsection under Translation System: `errors` namespace = canonical transport-message surface (18-key list verbatim; no `validationFailed`/`rateLimited`; no near-duplicates per REQ-055; historical auth overlaps documented, errors side owns shared copy). |
  | `frontend/AGENTS.md` | New §Error surfaces & Apollo error mapping: single-map rule (`mapGraphQLErrorByCode`, extensions.code-ONLY branching), component seams (`PermissionDeniedFallback` / `RetryableNotice` / `fieldError.ts` + `mutationFieldErrors.ts`); sx/palette rules referenced, not duplicated. |
  | root `AGENTS.md` | Important References entry for `docs/graphql/error-handling-contract.md` + BLT-03 annotations/redirects on the three dead-path bullets. |
- **Deliberately skipped (recorded deviation):** `app/AGENTS.md` + `backend/services/AGENTS.md` rule lines from the original task sketch — orchestrator scope for this pass covered the five files above (services file received only its BLT-03 annotations); the canonical doc carries the route-envelope guidance and exemptions register. `.agents/skills/security-review/SKILL.md` does not exist (SKILL domain-to-artifacts row n/a).
- **Gates:** sub-loop `--lifecycle tsgo` exit 0 on all five edited AGENTS.md files (md → documented tsgo-stage semantics). Full detail: `plan-completion-outcome.md`.
