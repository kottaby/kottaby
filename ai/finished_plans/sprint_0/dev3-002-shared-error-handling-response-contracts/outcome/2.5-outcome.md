# 2.5 Outcome — RequestId Context Plumbing (`ctx.requestId`)

- **Date:** 2026-08-26
- **Task:** `tasks.md` §2.5 (lines ~177–188) incl. .QL/.TE/.SEC/.SR/.IV sub-gates — Task ID **4-c**, EXECUTE leg
- **Status:** DONE — factory QL exit 0; test-file QL exit 0; paired suite **12 pass / 0 fail** (33 expects) via `bun run test/scripts/run-test.ts backend/graphql/test/request-id.test.ts`; project-wide `bun tsgo` **exit 0** (proves the additive `Context` change compiles across every consumer incl. Pothos builder slot)

---

## Pre-execution knowledge read (cited)

1. **Ground truth:** `backend/graphql/gqlContextFactory.ts` FULL READ — canonical `Context` interface is declared IN this module and consumed type-only by `backend/graphql/pothos/builder.ts` (`Context: Context` slot); rg sweeps for `interface.*GqlContext|type GqlContext` return ZERO other declarations → single additive extension point confirmed.
2. **2.4 export contract** (`outcome/2.4-outcome.md`): import via barrel `@/backend/lib/api`; signature `resolveRequestId(headers: RequestHeaderReader): string`; `Headers.get` satisfies `(name) => unknown` covariantly so `request.headers` passes directly.
3. **Harness precedent:** `setupTestServerLifecycle`/`testClient` (test/helpers) boots a live Next server — heavy tier reserved for Phase-3+ integration rows. A LIGHTER path exists (used instead of deferring): `createGraphQLContext` is directly callable on plain fetch `Request`s; ANONYMOUS constructions short-circuit before `UserRepository.findById`, so zero DB I/O while exercising the real production factory (mirrors how `getServerTranslations` was proven at unit tier in 2.4).

## Files changed

| File | Change |
|---|---|
| `backend/graphql/gqlContextFactory.ts` | MODIFIED — ADDITIVE ONLY: ① runtime import `import { resolveRequestId } from "@/backend/lib/api";` (barrel convention, alphabetical position kept); ② new `readonly requestId: string` property on `Context` with correlation-only contract docblock; ③ `createGraphQLContext` resolves ONCE immediately after locale extraction (`const requestId = resolveRequestId(request.headers);`) and returns it in the context literal. Zero edits to auth/locale/cookie logic; no new mints in module |
| `backend/graphql/test/request-id.test.ts` | CREATED — Tier 1–4 paired suite (12 tests / 33 expects), DB-free by anonymous-construction design |

## ctx.requestId wiring description

Single resolution point honored (Decision D4): the ONLY call site on the GraphQL path is inside `createGraphQLContext`; the only UUID mint in the entire request path remains `resolveRequestId` itself in `backend/lib/api/api-response.ts`.

## randomUUID sweep (STEP B.3 evidence)

| Site | Verdict |
|---|---|
| `backend/lib/api/api-response.ts` | ✅ THE sanctioned request-id mint (`resolveRequestId`); hygiene-pinned to exactly 1 occurrence by BOTH suites |
| `backend/graphql/gqlContextFactory.ts` | ✅ ZERO occurrences after this task (executable pin: `expect(factorySource.includes("randomUUID")).toBe(false)`) |
| `backend/lib/auth/jwt.ts:222` | Pre-existing UNRELATED — `session_id` JWT claim (session-correlation jti), not a request id; untouched |
| `backend/services/auth/registration.service.ts` | Pre-existing UNRELATED — hex entropy for credential/handshake fields; untouched |
| `backend/db/test/entity-setup.ts`, db tests, api-response.test.ts pin | Test fixtures — excluded per task definition |

**No competing request-id generation exists anywhere in the request path.**

## Gate results

| Gate | Command | Result |
|---|---|---|
| QL | sub-loop duplicates on `gqlContextFactory.ts` | ✅ exit 0 (tsgo → oxlint --deny-warnings → biome → lint:type-aware → jscpd) |
| QL | sub-loop duplicates on `request-id.test.ts` | ✅ exit 0 (one repair en route: sonarjs/prefer-specific-assertions → `toHaveLength` form) |
| Tests | `bun run test/scripts/run-test.ts backend/graphql/test/request-id.test.ts` | ✅ **12 pass / 0 fail**, 33 expects |
| Project compile | `bun tsgo` (full project, additive-type-change proof) | ✅ **0 errors**, exit 0 |

## TE evidence map

- **Tier 1:** bounded header verbatim onto `ctx.requestId`; case-insensitive lookup; inclusive 128-char bound pinned against imported `REQUEST_ID_MAX_LENGTH`; absent header → RFC-4122 v4 regex (version nibble `4` + variant `[89ab]`).
- **Tier 2:** two sequential constructions always distinct; format regex validated.
- **Tier 3:** two `Promise.all` PARALLEL constructions independent + 6-way concurrent batch collision-free while a pinned header row stays stable (no shared counter/state).
- **Tier 4:** oversized hostile header disqualified WHOLE — falls back to generated v4 AND zero fragments reflected anywhere in serialized ctx; comma-collapsed multi-value shape equally rejected; correlation id surfaces EXACTLY ONCE as one structured own-key.
- **SR pins (executable):** exactly ONE `resolveRequestId(` composition site in the factory; ZERO `randomUUID` occurrences; consumption strictly through the `@/backend/lib/api` barrel.

## SEC audit

- requestId is CORRELATION-ONLY: no resolver/route consumes it as an authorization input (route handler untouched — grep shows no requestId usage added under `app/api/**`).
- Inbound value length-bounded BEFORE acceptance (128 budget imported from one shared constant); rejection yields a locally minted id — never truncation, never echo (proved by serialized-context scan).
- Control-character smuggling impossible through real `Headers` (transport refuses) and additionally guarded inside the resolver contract (covered at 2.4 unit tier).

## SR semantic review

Single resolution site enforced both by construction and by executable pins; the additive `readonly requestId: string` compiles across all `Context` consumers including the Pothos builder binding and route wiring (`tsgo` full-project proof). Anonymous-construction harness keeps the suite inside REQ-040/076 purity rules (zero DB writes/reads).

## IV instruction verification

Mirrors the existing context-factory pattern (locale extraction precedes dependent wiring; plain object return; readonly fields per `backend/graphql/AGENTS.md` type conventions). `preloadSession`-style single-hop efficiency respected: resolution happens once before translations, reusing pure header reads. Import ordering follows the file's existing alias-grouping style.

## Carry-forward knowledge for Phase 3+

1. REQ-071(i) end-to-end "X-Request-Id honored" integration assertion lands in dev3-003's gateway ticket (per its specs matrix); plumbing here makes that a pure transport-level assert.
2. Logging correlation may now use structured `ctx.requestId` in resolvers; masked-path envelopes already correlate via the same helper family (2.4).
3. When the finalizeGraphqlErrors boundary finalizer ships (Phase 3), do NOT add a second resolution point — read `ctx.requestId`.
