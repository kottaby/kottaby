# 2.4 Outcome — API Route Envelope Helpers (`backend/lib/api`) Verification & Repair

- **Date:** 2026-08-26
- **Task:** `tasks.md` §2.4 (lines ~155–175) incl. .QL/.TE/.SEC/.SR/.IV sub-gates — Task ID **4-c**, VERIFY leg (artifacts already existed from prior agent; this leg validated them, repaired the paired test suite to full-gate green, and recorded the export contract consumed by Task 2.5)
- **Status:** DONE — three sub-loop runs exit 0; paired suite **39 pass / 0 fail** (333 expects) via `bun run test/scripts/run-test.ts backend/lib/api/test/api-response.test.ts`; `bun tsgo` filtered scopes clean (full-project run re-proven in Phase-wide final checks)

---

## Pre-execution knowledge read (cited)

1. **Outcome files:** `plan-review-R1.md`, `phase0-baseline-outcome.md`, `0.2-outcome.md` (context-factory ground truth), `1.1/1.2/1.3-outcome.md` (types barrel, i18n keys), `2.1-outcome.md` (taxonomy map ownership of every error status), `2.2-outcome.md` (masking primitives), `2.3-outcome.md` (`fields` presence semantics carried into envelopes).
2. **Task definition 2.4** (lines ~155–175): every rule and sub-gate re-checked against the shipped files.
3. **Ground truth FULL READ:** `backend/lib/api/api-response.ts` (337 lines), `backend/lib/api/index.ts` (barrel), `backend/lib/api/test/api-response.test.ts`.

## Files modified during VERIFY (repairs only)

| File | Change |
|---|---|
| `backend/lib/api/test/api-response.test.ts` | REPAIRED to gate-green (no production-file changes needed): ① JSON-record narrowing moved into an assertion-free structural guard `isPlainJsonObject(value): value is Record<string, unknown>` (kills TS2322 ×2 and two unsafe-type-assertion violations); ② dead `{ created: true } && …` always-truthy expression removed (TS2872); ③ `test.each(envelopeRows)` → `test.each([...envelopeRows])` with a fresh mutable table matching bun-test's variadic overload (TS2769/TS2345); ④ `.sort()` on readonly arrays replaced by comparator-bearing `.toSorted((a,b)=>a.localeCompare(b))` (sonarjs/no-alphabetical-sort); ⑤ unused `fixedRequestId` dropped (TS6133); ⑥ awaited loop refactored to build-then-drain with `Promise.all` (eslint/no-await-in-loop); ⑦ unreachable undefined-index guards deleted (sonarjs/different-types-comparison); ⑧ `/tmp/x.js` leak-marker path renamed `/srv/app/x.js` (sonarjs/publicly-writable-directories); ⑨ semantic corrections: JSON-shaped-but-bounded header value asserted OPAQUELY ACCEPTED per REQ-013 (was miscategorized hostile), unicode acceptance exercised through the structural reader contract because real `Headers` refuse non-latin1 values at construction |

## Gate results

| Gate | Command | Result |
|---|---|---|
| QL ×3 | sub-loop duplicates on `api-response.ts`, `index.ts`, test file | ✅✅✅ exit 0 (tsgo → oxlint --deny-warnings → biome → lint:type-aware → jscpd) |
| Tests | `bun run test/scripts/run-test.ts backend/lib/api/test/api-response.test.ts` | ✅ **39 pass / 0 fail**, 333 expects |

## Rule audit evidence (STEP A.4 greps)

- **No `console.*`** — zero matches under `backend/lib/api` outside the test's own hygiene pin (`expect(moduleSource.includes("console.")).toBe(false)`).
- **Taxonomy-only HTTP statuses** — production file has ZERO numeric error-status literals; sole derivation path is `normalizeErrorCode(code)` → `ERROR_CODE_HTTP_STATUS[canonical]` (+ declared-base `BAD_REQUEST` fallback for custom SCREAMING_SNAKE codes; masked path reads `ERROR_CODE_HTTP_STATUS.INTERNAL_SERVER_ERROR`). Pinned executably by test regex `\b(400|401|403|409|422|429|500|503)\b === false` over module source. Success 200/201 live in named constants `API_STATUS_OK` / `API_STATUS_CREATED` (outside REQ-010 error-taxonomy domain).
- **`details` whitelist** — `details` is NEVER synthesized: envelope ships only `code | message | requestId | fields?`, where `fields` is the verbatim mirror of `ValidationError.fields` (the 2.3-whitelisted producer payload). Absence asserted on every masked row, duplicate-style rows, and conflict translations.
- **No DB imports** — no `@/backend/db`, no driver/drizzle imports anywhere in the module; pinned by test assertion, composed only of fetch `Response`s.

## Export contract of record for Task 2.5

```ts
// backend/lib/api/api-response.ts
export type RequestHeaderReader = { readonly get: (name: string) => unknown };

/**
 * Resolves the per-request correlation id ONCE (Decision D4).
 * Honors inbound X-Request-Id as an opaque bounded string (trimmed ≤128 chars,
 * single-valued, control-char-free); otherwise generates UUID v4.
 */
export function resolveRequestId(headers: RequestHeaderReader): string;

/** Barrel: import { resolveRequestId } from "@/backend/lib/api"; */
```

`Headers#get` returns `string | null`, which satisfies `(name: string) => unknown` covariantly — so the GraphQL context factory can pass `request.headers` directly (NextRequest | Request both expose fetch `Headers`).

## SEC / SR / IV notes

- SEC: duplicated-idempotency detail channel intentionally ABSENT here (belongs to the idempotency producer landing with its service — REQ-022/043 wording ownership lives there); helpers make no auth decisions.
- SR: classification primitives are consumed from `@/backend/lib/errors` (hierarchy guards, cycle-safe walker, masking, redaction, taxonomy) — zero duplicated logic vs the GraphQL boundary; exactly ONE `translateDbError(` call site and ONE `randomUUID(` minting site per file-pins; localization solely through `getServerTranslations(locale)`; zero dead exports.
- IV: plain fetch `Response`s compose with existing `app/api/**` route-handler style without coupling `backend/lib` to `next/server` (asserted absent).

## Carry-forward knowledge for 2.5

1. Import point MUST be the barrel `@/backend/lib/api` (repo import convention; direct file path unnecessary).
2. Call site must be INSIDE `createGraphQLContext` (single resolution point D4); store on `ctx.requestId`; extend the `Context` interface additively (`readonly requestId: string`).
3. Module-hygiene pin expects exactly one `randomUUID(` occurrence in api-response.ts itself; the GraphQL factory adds ZERO new mints (composes `resolveRequestId` instead).
