# 0.2 Outcome — Verify Prerequisites & Existing-State Ground Truth (dev3-002)

- **Date:** 2026-08-26
- **Method:** Ground truth was authoritatively established by `outcome/plan-review-R1.md` during the Phase-1.5 gate (REQ-082). Per orchestration decision this task **incorporates R1 §(c) inventory rows #1–#17 and §(d) corrections #1–#10 by reference** instead of re-deriving them. Only items NOT covered there were briefly cross-checked (below). Tooling/baseline execution evidence lives in `outcome/phase0-baseline-outcome.md`.

## Incorporated ground truth (by reference — do not re-verify)

| Prerequisite | Status | Authoritative record |
|---|---|---|
| DomainError hierarchy (6 classes) | FOUND | R1 §(c) row 1 — backend/lib/errors.ts:18..:75 |
| Cycle-safe cause traversal ("isUniqueViolation") | PARTIAL/RENAMED | R1 §(c) row 2 — private `hasPgCode`:112, `hasSqliteUnique`:129; public `translateDbError`:97 (+ discovered `RateLimitExceededError`:82) |
| logger.error + logDomainError | FOUND | R1 §(c) row 3 — backend/lib/logger.ts:53/:72 |
| errors i18n namespace + MessageSchema registration | FOUND | R1 §(c) rows 4–5 — shared/locale/types/errors/index.ts (17 keys), message.ts:11, handle `Errors` |
| ratelimit contract surface (fail-open stub) | FOUND | R1 §(c) row 10 — backend/lib/ratelimit.ts |
| gqlContextFactory / preloadSession | FOUND / MISSING(stale doc) | R1 §(c) rows 6–7 — createGraphQLContext at gqlContextFactory.ts:122; AGENTS.md:135 stale |
| GraphQL bootstrap + app/api inventory (2 routes only) | FOUND | R1 §(c) rows 8–9 |
| errorLink / PermissionDeniedFallback(absent-as-planned) / token-refresh dedupe | FOUND/ABSENT | R1 §(c) rows 11–12 — frontend/providers/apollo/utils.ts |
| Test harness helpers | FOUND except expectMutationError | R1 §(c) rows 13–16 |
| Baseline tooling present | FOUND | R1 §(c) row 17 — now executed & recorded in phase0-baseline-outcome.md |

## New cross-checks performed by this task

- ✅ Reusable `auth` i18n keys (REQ-055 reuse set for field messages): `emailInvalid` — shared/locale/types/auth/index.ts:35; `passwordTooShort` — :38; `emailAlreadyExists` — :43.
- ✅ `backend/lib/AGENTS.md` confirmed ABSENT → root/backend layer docs govern `backend/lib/**` work (matches R1 [INFO]).
- ✅ REQ-082 gate satisfied: `outcome/plan-review-R1.md` exists before Phase 1.
- ✅ Sandbox env readiness: `.env` + `.env.test` both define `LINT_QUEUE_PORT=4777`; dev server on :3000 untouched.

## Carry-forward knowledge for later tasks

- Phase 2.5 requestId anchor = `createGraphQLContext` (NOT a `preloadSession` pattern); stale AGENTS.md line tracked as ledger row BLT-02.
- Phase 1.2 additive-keys decision set is constrained to `duplicateRequest` (+ one naming decision for validation/rate-limit pairs); the other 7 keys are REUSE-only (ledger BLT-01).
- Phase 3 must create `expectMutationError(expectedCode)` in `test/helpers/` BEFORE any suite referencing it (ledger BLT-04).
- No Postgres in sandbox ⇒ DB-provider test tiers deferred up-front (ledger BLT-06); eslint full-repo tier unavailable in-sandbox (ledger BLT-07) — lint deltas judged via biome+oxlint.
