# 2.1 Outcome — Error Code Taxonomy Module (dev3-002 Phase 2)

- **Date:** 2026-08-26
- **Task:** `tasks.md` §2.1 (lines ~105–117) incl. .QL/.TE/.SEC/.SR/.IV sub-gates — executed as the first leg of chained Task ID **4-a** (Phase-2 contract core: 2.1 → 2.3 → 2.2, sequential by design since all three share the `errors*.ts` surface)
- **Status:** DONE — all gates exit 0; paired suite 15/15 pass; `bun tsgo` full-project 0 errors

---

## Pre-execution knowledge read (cited)

1. **All existing outcome files:** `plan-review-R1.md` (§(b)[MEDIUM][taxonomy] legacy-code finding + §(d) correction #3 "include the legacy-code alias rule RATE_LIMIT_EXCEEDED → row-8 category"; §(c) rows #1–#17), `phase0-baseline-outcome.md` (baseline discipline: biome 391 files/0 diags, oxlint 371/0/0; BLT seed), `0.2-outcome.md`, `1.1-outcome.md` (**exported names consumed here:** `ErrorCode` union of exactly nine literals + `ApiFieldErrorType` from `@/backend/types`; its carry-forward #1 prescribed this task verbatim: map values 400/401/403/409/409/422/429/503/500 in that order + `satisfies` exhaustiveness pattern validated there + alias normalization in `isErrorCode` + "import ErrorCode from @/backend/types, never redefine"), `1.2-outcome.md` (final i18n key list incl. **rateLimitExceeded** canonical naming decision → taxonomy doc-comment cross-link; `validation` not needed by this pure-data task but noted for 2.2 mapping tables), `1.3-outcome.md` (zero schema drift invariant — no DB enums touched).
2. **deferred-items.md** rows BLT-01…BLT-09; **BLT-08 is closed by THIS task** (alias gap): `RATE_LIMIT_EXCEEDED → RATE_LIMITED` now encoded as data + guard acceptance + normalization function; ledger row updated to ✅ with pointers.
3. **Task definition 2.1** (`tasks.md` lines ~105–117) — every sub-item addressed below.
4. **Rules/docs:** `backend/AGENTS.md` (barrel conventions, DomainError CRITICAL rule, type-import-from-@/backend/types), root `AGENTS.md` (§Essential Commands, barrel/import-export rules lines 126–135, logger rules, run-test protocol line 58), `.agents/instructions/backend.instructions.md` (full read — layer separation, barrels, DomainError CRITICAL, i18n/logging rules), `docs/graphql/domain-error-extensions-code.md` (rules 1–4 incl. NotFoundError entity convention → informs 2.3; SCREAMING_SNAKE_CASE rule), `docs/auth/user-registration.md` §6/7.2/8 (23505→ConflictError precedent + cause-chain traversal gotcha — verified untouched), `docs/IDEMPOTENCY.md` (409/DUPLICATE_REQUEST semantics → CONFLICT+DUPLICATE_REQUEST both row-mapped to 409 in the table).
5. **Ground-truth sources:** `backend/lib/errors.ts` FULL READ (DomainError hierarchy; ValidationError overloaded ctors; private cycle-guarded `hasPgCode`/`hasSqliteUnique`; public `translateDbError`; confirmed **NO function literally named `isUniqueViolation` exists** — R1 renaming finding honored, no second walker introduced); `backend/lib/logger.ts` (logger.error/logDomainError TEST_SERVER=1 semantics — not used in this pure-data module); `shared/locale/server-graphql.ts` + `shared/locale/server.ts` (`getServerTranslations(locale).errorsTranslations` — ground-truth signature differs from tasks.md's two-arg sketch; relevant to 2.2, recorded in carry-forward); `backend/types/errors/api-error.types.ts` FULL READ (the `ErrorCode` source imported here).

## Files created / modified

| File | Change |
|---|---|
| `backend/lib/errors/error-code-taxonomy.ts` | **CREATED** — REQ-010 table as frozen data + guards/helpers (exports below) |
| `backend/lib/errors/test/error-code-taxonomy.test.ts` | **CREATED** — paired Tier1–4 suite (15 tests) |
| `backend/lib/errors.ts` | **MODIFIED, additive-only tail**: re-export per established pattern at end-of-file (`export * from "./errors/error-code-taxonomy";`) + routing comment. ZERO changes to class/hierarchy logic |
| `ai/plans/dev3-002.../tasks.md` | checkbox flips only |
| `ai/plans/dev3-002.../deferred-items.md` | BLT-08 → ✅ Done |

### Exported surface (canonical names)

```
ERROR_CODE_HTTP_STATUS    : Readonly<Record<ErrorCode, number>> — Object.freeze'd
                            BAD_REQUEST:400 · UNAUTHORIZED:401 · FORBIDDEN:403 · CONFLICT:409
                            DUPLICATE_REQUEST:409 · VALIDATION:422 · RATE_LIMITED:429
                            SERVICE_UNAVAILABLE:503 · INTERNAL_SERVER_ERROR:500
LEGACY_ERROR_CODE_ALIASES : Readonly<Record<string, ErrorCode>> — { RATE_LIMIT_EXCEEDED: "RATE_LIMITED" }
isErrorCode(value)        : value is ErrorCode — canonical ∪ aliases; case-sensitive; own-prop safe
normalizeErrorCode(code)  : string→ErrorCode|null — THE shared alias-normalization primitive
```

Consumption path stays flat: `import { ERROR_CODE_HTTP_STATUS, isErrorCode, normalizeErrorCode } from "@/backend/lib/errors"`.

## File↔directory duality decision (recorded per orchestration question)

`@/backend/lib/errors` continues to resolve to the FILE `backend/lib/errors.ts` — exact-file beats same-named directory under `moduleResolution: "bundler"` AND at bun runtime. Verified empirically (`bun -e 'await import("@/backend/lib/errors")'` → all symbols present, incl. re-exported taxonomy). Consequences:
- **No `backend/lib/errors/index.ts` created** — it would be an orphan barrel nothing imports (dead file); `errors.ts` itself IS the de-facto barrel via the appended `export *`. The tasks.md wording ("if the lib barrel pattern requires one") resolves to NOT required.
- Task 2.4/2.5 and Phase 3 must keep importing from `@/backend/lib/errors` (never deep paths).

## Design notes / deviations of record

1. **Two exported helpers beyond the plan's literal two-symbol list** (`LEGACY_ERROR_CODE_ALIASES`, `normalizeErrorCode`). Justification: R1 correction #3 requires carrying the alias rule *into* finalizeGraphqlErrors (2.2) and the frontend/status mapping (2.4+) WITHOUT split-brain duplication; a boolean-only guard cannot express "which category". `normalizeErrorCode` is the single place the alias literal maps to a family; consumers compose `normalizeErrorCode(code)` → `ERROR_CODE_HTTP_STATUS[canonical] ?? ownFallback`. Backed by the derivation recipe test so the composition contract is executable.
2. **Assertion-free implementation** (zero `as` casts — oxlint `no-unsafe-type-assertion` clean): internal `CANONICAL_SELF_MAP satisfies Record<ErrorCode, ErrorCode>` (second compile-time exhaustiveness lock) spread with `LEGACY_ERROR_CODE_ALIASES` into a `Readonly<Record<string, ErrorCode>>` normalization table guarded by `Object.hasOwn` (prototype-name immunity).
3. `getServerTranslations` signature reality (two-file chain returning `.errorsTranslations`, NOT `(locale, "errors")`) noted early for Task 2.2 — no locale/i18n code exists in this module (pure data).

## Quality gate results

| Gate | Command | Result |
|---|---|---|
| QL ×3 | sub-loop --lifecycle duplicates on {taxonomy, errors.ts, test} | ✅✅✅ exit 0 each (tsgo → oxlint → biome → lint:type-aware(in-process) → jscpd all green; oxlint runs `--deny-warnings`) |
| Tests | `bun run test/scripts/run-test.ts backend/lib/errors/test/error-code-taxonomy.test.ts` | ✅ **15 pass / 0 fail**, 151 expects |
| Compile | `bun tsgo` (project-wide inside sub-loop runs) | ✅ 0 errors |

Environment note (not a repo change): sandbox lacked `~/.bun/bin/bun` which `test/scripts/run-test.ts` hardcodes; a symlink `/home/z/.bun/bin/bun → /usr/local/bin/bun` was created so the mandated runner executes. Filesystem-level env accommodation only — zero repo files affected.

## TE — tier-by-tier evidence

- **Tier 1 (100% mapping + exhaustiveness):** every one of the 9 mappings asserted individually; runtime `{...map}` deep-equals a `satisfies Readonly<Record<ErrorCode, number>>` fixture (deleting/adding any `ErrorCode` member breaks `bun tsgo` AND the fixture equality simultaneously).
- **Tier 2 (guard boundaries):** 9 canonical accepts + self-normalization; `RATE_LIMIT_EXCEEDED` accept → normalizes to `RATE_LIMITED` (BLT-08 executable proof); 8 casing variants rejected (incl. trailing/leading whitespace & newline — data lookups are exact); empty/whitespace rejected; non-string primitives (undefined/null/42/NaN/true/false/Symbol/bigint) rejected throw-free; inherited names `toString|constructor|valueOf|hasOwnProperty` rejected (hasOwn immunity); custom domain codes `USER_NOT_FOUND|RECURRING_CLASS_DAYS_REQUIRED|QUOTA_NOT_FOUND` fall through to null (no masquerade — SEC); predicate-narrowing proven by typed assignment post-guard.
- **Tier 3 (fuzz):** 16 hostile strings (`\u0000` payloads, homoglyph Ó, SQL-injection-shaped, `__proto__`, `prototype`, numeric strings, JSON-shaped, truncated variants) → always boolean/false + null, never throws; structural impostors (arrays, plain object with `.code`, object whose `toString()` returns a valid code, real `Error`, `Symbol.for("RATE_LIMITED")`) rejected — coercion/widening impossible.
- **Tier 4 (sole-status-source + purity):** self-grep proves the module contains NO other `key: NNN` status literals beyond the 9-row canon map (comment-stripped regex count = exactly 9); freeze enforced (mutation attempts leave 409/RATE_LIMITED intact); consumption recipe preview pinned for 2.4 (`DUPLICATE_REQUEST→409`, alias→429, custom/prototype→null so caller-owned masking fallback applies).

## SEC audit

- Map is **frozen + Readonly** — no function can mutate taxonomy data (runtime probes included).
- No masquerade path: normalization returns the DECLARED base category only; custom codes never resolve through the table (explicit Tier-2 case); alias set is a closed, documented singleton.
- Module is pure data (REQ-040): zero I/O, zero logging, zero env reads, side-effect-free except init-time freeze.
- No secret-bearing content anywhere in file or logs.

## SR semantic review

Single source of truth for statuses AND for the alias rule (both literals appear exactly once across the repo surface this task owns); no dead exports (all four consumed by tests + queued for 2.2/2.4 consumers documented above); no console.*; no duplicated near-map (grep-proved); sibling placement beside `errors.ts` matches the plan's parallel-module organization ("taxonomy/mask modules in errors/ directory").

## IV instruction verification

Module placement under `backend/lib/` ✓; re-export style follows the established single-surface pattern (`export *`, relative `./`, one path segment — index-barrel rules applied analogously though this is a module-tail export, permitted because `errors.ts` is not an `index.ts`); Decision D3 honored — `ErrorCode` remains a string union imported TYPE-ONLY from `@/backend/types`; **no pgEnum / backend/db/schema/enums.ts entry / Pothos enum / GraphQL enum value** anywhere (schema-drift ownership stays with Task 1.3's green baseline).

## Carry-forward knowledge for Tasks 2.2–2.5 (and 3.x)

1. **Status derivation recipe (2.4 MUST reuse — do not reinvent):**
   ```ts
   const canonical = normalizeErrorCode(code);
   const status = canonical === null ? 500 /* masked fallback */ : ERROR_CODE_HTTP_STATUS[canonical];
   ```
   Never write new `code → status` literals.
2. **finalizeGraphqlErrors (2.2):** DomainError instances PASS THROUGH unmutated even when emitting legacy `RATE_LIMIT_EXCEEDED` — normalization happens exclusively in status/category derivation layers via `normalizeErrorCode`. Do NOT inject `extensions.originalCode`.
3. **i18n key mapping aligned with completed 1.2 outcome:** RATE_LIMITED(+alias)→`errorsTranslations.rateLimitExceeded`, VALIDATION→`validation`, DUPLICATE_REQUEST→`duplicateRequest`, masked-generic→`internalServerError` (via `getServerTranslations(locale).errorsTranslations.*` — NOTE the actual ground-truth accessor shape, not `(locale,"errors")`).
4. **Frontend mapping phase:** same alias closure — reuse `normalizeErrorCode` semantics server-side; mirror behavior client-side when the frontend map lands.
5. Custom domain codes (`${ENTITY}_NOT_FOUND` etc.) remain legal transport values outside the taxonomy — envelope layers must apply their own INTERNAL_SERVER_ERROR/BAD_REQUEST-classification fallbacks, never force-fit into categories.

BLT-08 ledger closure recorded alongside this outcome.
