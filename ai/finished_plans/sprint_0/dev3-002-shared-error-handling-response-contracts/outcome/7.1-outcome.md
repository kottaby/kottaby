# 7.1 Outcome — Canonical Reference Doc

- **Task:** `tasks.md` §7.1 (Task ID 12, Phase 7 knowledge propagation)
- **Deliverable:** [`docs/graphql/error-handling-contract.md`](../../../../../docs/graphql/error-handling-contract.md) — single canonical transport contract consolidating: REQ-010 taxonomy table (code↔HTTP semantics) + legacy alias normalization (`RATE_LIMIT_EXCEEDED`→`RATE_LIMITED` family), masking pipeline (pass-through vs mask rules, redaction pattern families, requestId/operationName bounds, severity semantics), API envelope shapes + exemptions inventory (set-locale GET redirect; future webhook provider-ack policy), REQ-061 client mapping table incl. duplicate-as-success UX + component seams guidance, and the per-guarantee test-suite matrix.
- **Naming decision of record:** the task text suggested `docs/graphql/error-response-contract.md`; orchestration chose **`error-handling-contract.md`** after reviewing `domain-error-extensions-code.md` (240-line producer-conventions doc). Extension-as-"Part 2" was REJECTED (mixing two lifecycle generations in one rollout-summary doc bloats both); instead the existing doc received a **superseded-by-reference** blockquote atop its code-table content while remaining authoritative for throw conventions (REQ-080).
- **BLT-03 disposition (dead canonical-doc paths):** choice = *update referencing spots*, not placeholder-stub docs (bloat avoided). Error-contract-adjacent refs redirected to the new doc; non-error-domain adapter/integration pointer rows annotated `(doc file absent from this tree — pending <owning ticket>; see deferred-items BLT-03)` in place. Edits: `backend/graphql/AGENTS.md` ×2 · `backend/AGENTS.md` ×2 · `backend/services/AGENTS.md` ×2 · `backend/db/repo/AGENTS.md` ×1 · root `AGENTS.md` ×3 · `docs/auth/jwt-authentication-service.md` ×1 · `docs/auth/user-registration.md` ×1. Archived mentions under `ai/finished_plans/**` and sibling plan artifacts left untouched as historical records. Ledger flipped ✅ with notes.
- **Gates:** sub-loop `--lifecycle tsgo` exit 0 on the new doc + `docs/graphql/domain-error-extensions-code.md` + all seven edited AGENTS/docs files (markdown → documented tsgo-stage semantics per 5.5 outcome). Full detail: `plan-completion-outcome.md`.
