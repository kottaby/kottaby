# 5.5 Outcome — i18n Parity & UI Test-String Discipline Gate (dev3-002)

- **Date:** 2026-08-26
- **Task:** `tasks.md` §5.5 — "Prove ar/en parity and translation-driven assertions" (_Requirements: REQ-054, REQ-075_)
- **Agent:** Task ID 9 (Phase 5 Task 5.5 subagent)
- **Files read before execution:** tasks.md §5.5 + Phase-6 §6.3 cross-refs; outcome/1.2-outcome.md (final 18-key list + REUSE decisions); `.agents/instructions/frontend.instructions.md` §i18n ("never hardcode… `useAppTranslation` / `getTranslations`"); `eslint.config.mjs` lines 322–333 (registers `local/no-hardcoded-strings` = **error** from `scripts/eslint-rules/no-hardcoded-strings.mjs`); current state of all touched locale/contract files; specs.md REQ-054/055/075 (+REQ-021/030..034 oracle rules).

---

## 1. Mechanical en/ar parity (REQ-054) — throwaway bun script

**Method:** live-module import (zero mocks) of ALL six registered namespaces (`shared/locale/{en,ar}/*` exactly as wired in `messages.ts`) into `/home/z/my-project/tool-results/dev3-002-task55-parity-check.ts` (throwaway; gitignored scratch). Machine assertions: identical sorted key sets en↔ar · string-only values · no duplicate literal VALUES within a namespace (per locale) · errors key set === Task 1.2 final 18-key list · REQ-055 exact + case/punctuation-insensitive value cross-check vs `auth`.

### Parity table

| Namespace | Ticket-touched? | Keys (en=ar) | Set equality | String-only | Duplicate literals | Verdict |
|---|---|---|---|---|---|---|
| **errors** | ✅ YES (only one: Task 1.2) | 18 | ✅ | ✅ (36/36 strings) | ✅ 0 | **PASS** |
| common | no | 19 | ✅ | 1 by-design interpolated fn (`documentTitleTemplate`, declared `(title:string)=>string` in types) | ✅ | PASS (informational) |
| auth | no (this ticket) | 64 | ✅ | 1 fn none; 8 pre-existing pair-dups (e.g. `email`/`loginEmail`) from interleaved landing commits | ℹ️ recorded | PASS (out-of-scope notes) |
| recitation | no | 25 | ✅ | ✅ | ✅ | PASS |
| dashboard | no | 58 | ✅ | 3 by-design interpolated fns | ✅ | PASS |
| landing | no | 189 | ✅ | — | ℹ️ 28 pre-existing pair-dups (interleaved landing plans) | PASS |

### Ticket-scope deep check (errors)

- Key set matches **1.2-outcome FINAL KEY LIST byte-exactly**: `accountBlocked accountDeleted accountSuspended badRequest conflict duplicateRequest failedToSetLocale forbidden forbiddenRole internalServerError invalidLocale invalidOrigin notFound rateLimitExceeded serviceUnavailable tokenExpired unauthorized validation` — still NO `validationFailed`, NO `rateLimited`.
- **Script verdict: `PARITY CHECK: PASS (failures: 0)`** (full machine output: `tool-results/dev3-002-task55-parity-output.txt`).
- Compile-gate corroboration: `bun run tsgo -b --noEmit` project-wide exit 0 — a missing/extra key in either locale cannot exist under `MessageSchema` typing (REQ-054 double-proof: compile + runtime sets).

## 2. REQ-055 near-duplicate gate (new errors keys vs auth)

Compared every `errors` VALUE against every `auth` VALUE, exact AND normalized-case-insensitive (en/ar separately):

| Locale | New-this-ticket overlaps (gate-failing) | Pre-existing overlaps recorded |
|---|---|---|
| en | **0** | 1 — `errors.rateLimitExceeded ≡ auth.rateLimitExceeded` (intentional same-copy reuse across namespaces, predates ticket, not a new near-dupe) |
| ar | **0** | 1 — same pair, Arabic copy |

Net-new key `duplicateRequest`: en "This value is already in use." / ar "هذه القيمة مستخدمة بالفعل." — value-level phrasing, NO match (even case/punct-insensitive) against any auth key → **REQ-055 PASS**.

## 3. Hardcoded-string discipline sweep (touched-code files)

**Scope swept (22 files):** `backend/lib/errors{.ts,/,/test/**}`, `backend/lib/api/**`, `frontend/components/ui/**`, `frontend/providers/apollo/error-link.map.ts(+test)+utils.ts(wiring hunks)`, `frontend/lib/mutationFieldErrors.{ts,test.ts}`, `app/api/set-locale/route.ts(+test)`.

**(a) Repo's own ESLint rule:** `local/no-hardcoded-strings` is config-enabled at `error` level, so both scoped runs through `scripts/lint-service.ts` exercised it:

| Run | Files | Exit |
|---|---|---|
| `lint-service --json --id t55-source` (12 source files) | errors.ts, taxonomy, masking, api×2, UI×3, apollo×2, mutationFieldErrors, set-locale route | **0 (clean)** |
| `lint-service --json --id t55-tests` (10 test files) | all ticket suites incl. 3 new Phase-5 contract tests | **0 (clean)** |

**(b) Manual grep corroboration** (sentence-case literals + template-literal-with-spaces + JSX text nodes): raw hits BEFORE fixes = **5**, classified:

| Hit | Classification |
|---|---|
| `api-response.ts:288` "Unhandled non-domain error masked at API route boundary" | log string (allowed) |
| `error-masking.ts:846` "Unhandled non-domain error masked at GraphQL boundary" | log string (allowed) |
| `error-masking.ts:690` `` `Domain rejection observed at boundary (${error.name})` `` | log string (allowed) |
| `utils.ts` log/message clusters (`[AuthRedirect]`, `[ErrorSurface]`, connectivity) | log/caller-tag strings only; wire `message:` sources `getServerNotAvailableMessage()` provider (localized upstream) |
| UI components (`PermissionDeniedFallback`, `RetryableNotice`) | 100% translation handles (`t.forbiddenRole/forbidden`, `errorsT.rateLimitExceeded/serviceUnavailable`, `commonT.retry`); ZERO JSX literal text |

True user-facing violations found in ticket-scope code: **0 → nothing to fix** (after-fixes count stays 0). Copy travelling to users enters exclusively via typed handles or server-localized `extensions.fields[].message` echoes (REQ-015 whitelist posture preserved in `mutationFieldErrors.ts` / `error-link.map.ts` — handles-as-keys, never server `message` text for masked classes).

**(c) Forbidden-import scan:** `next-intl` / `getBackendTranslations` / `shared/messages` greps across all 22 touched files → **zero hits** (exit-1 grep = PASS). Repo-wide hits are docs/AGENTS references only.

## 4. UI-test-string discipline (REQ-075)

Pattern scan `'expect(...).toBe|toContain|toEqual("[A-Z][a-z]+ …'` over the ticket's 10 test files → **5 raw hits, 0 actionable violations**:

| Site | Disposition |
|---|---|
| `error-masking.test.ts:705` `"Hafs Circle 3"` | redaction FIXTURE data round-trip assertion (not i18n surface) — compliant |
| `mutationFieldErrors.test.ts:199,201` email/password messages | verbatim ECHO of THIS TEST'S OWN wire fixture (`REGISTRATION_FIELD_PAIRS` simulates already-localized `extensions.fields[]`); module under test contractually passes producer-localized strings through untouched; same file separately resolves real labels via `getDefaultTranslations()`/`loadAllTranslations("ar")` (:252–253) — compliant |
| `errors-fields-contract.test.ts:86` `"Invalid input."` | constructor-passthrough pin (same literal supplied to `new ValidationError(...)` microseconds earlier) — compliant |
| `api-response.test.ts:391` `"User not found."` | echo of the explicit `NotFoundError("USER", "User not found.")` constructor arg; every LOCALIZED-resolution assertion in this file uses `getServerTranslations(locale)` (≥5 sites :404–702) — compliant |

Translation-object discipline inventory: `error-link.map.test.ts` resolves ALL expectations from `getDefaultTranslations().errorsTranslations` + `loadAllTranslations("ar")` (documented R1-correction-#7 strategy — handle-key equality, so ar/en drift is impossible-by-construction); `set-locale-route.test.ts` + `error-contract-matrix.test.ts` assert through `getServerTranslations("en"|"ar").errorsTranslations` (matrix even pins `tAr !== tEn` non-emptiness); `fieldError.test.ts` contains zero string-literal expectations. **No components/tests authored at component-render tier in ticket scope** (PermissionDeniedFallback/RetryableNotice ship logic-tier pure modules + map tests instead), so the `readTranslation(handle, locale)` arm has no applicable surface — deviation recorded, consistent with plan-review-R1 correction #7.

## 5. Arabic copy review — ORACLE VERDICT: **PASS**

Reviewed `shared/locale/ar/errors/index.ts` entries consumed by contract surfaces (`unauthorized, forbidden, validation, conflict, duplicateRequest, rateLimitExceeded, serviceUnavailable, internalServerError` via REQ-061 table+retry surfaces; `invalidLocale, invalidOrigin, badRequest` via set-locale route; defaults `forbiddenRole`):

- **No lockout thresholds / windows** — `rateLimitExceeded` ar = "طلبات كثيرة جداً. يرجى المحاولة لاحقاً." (no digits, window sizes, retry-after counts). ✅
- **No attempt counters** — zero counting copy anywhere in the 18 ar values. ✅
- **No account-existence disclosure in ticket surfaces** — `duplicateRequest` is phrased at value level ("هذه القيمة مستخدمة بالفعل."), matching en; governance trio (`accountDeleted/Blocked/Suspended`) is **NOT referenced by any ticket source/test consumer** (grep-proven; the only repo mention is a finished-plan doc) — login-path REQ-021 generic-copy parity remains owned by 5.3 evidence. ✅
- Punctuation/style conventions (trailing `.`, natural RTL phrasing) uniform across all 18 keys.

## 6. Gates

| Gate | Command | Result |
|---|---|---|
| QL | `lint-service --json --id t55-source` / `--id t55-tests` (scoped to 22 touched files; includes `local/no-hardcoded-strings`=error) | ✅ exit 0 / exit 0 |
| Compile | `bun run tsgo` (project-wide, `-b --noEmit`) | ✅ exit 0, **0 errors** |
| Parity | `bun tool-results/dev3-002-task55-parity-check.ts` | ✅ PASS, failures 0 |
| Spec sub-loop (markdown artifacts) | `sub-loop.ts <md> --lifecycle tsgo` on 5.5-outcome.md + tasks.md | ✅ exit 0 / exit 0 — full `--lifecycle duplicates` chain on `.md` stops at oxlint stage with "No files found to lint" (harness lints code globs only; no code file modified by this task, so no full-chain QL owed); tsgo-stage green for both artifacts |
| Code fixes required | — | 0 (sweep clean; no file modified ⇒ no additional QL loop owed) |

**Coverage vs §5.5 rows:** automated ar/en parity ✔ (compile + runtime sets) · E2E/component assertions via translation objects ✔ (inventory §4) · forbidden-import scan ✔ zero · outcome written ✔.

**Production-bug observations:** none red. Out-of-ticket informational notes (auth/landing pre-existing literal dups, interpolated-fn namespace members) are stable repo patterns unrelated to this contract work; no ledger row warranted.
