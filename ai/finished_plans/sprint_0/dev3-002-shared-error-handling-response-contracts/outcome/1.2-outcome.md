# 1.2 Outcome — Errors i18n Namespace Extension (dev3-002)

- **Date:** 2026-08-26
- **Task:** `tasks.md` §1.2 — "Add REQ-051 keys to the existing `errors` namespace in both locales"
- **Files modified (exactly 3, scope-enforced):** `shared/locale/types/errors/index.ts`, `shared/locale/en/errors/index.ts`, `shared/locale/ar/errors/index.ts`
- **Knowledge read before execution (cited):** all of `outcome/plan-review-R1.md` (**Finding #1 [HIGH][shared/locale]** + §(c) rows 4–5 + correction #1), `outcome/0.2-outcome.md`, `outcome/phase0-baseline-outcome.md`, `deferred-items.md` (BLT-01), `tasks.md` Task 1.2 block (lines 80–93), `plan.md` §2.3, `specs.md` REQ-050/051/054/055 (+REQ-021/034 oracle rules), current state of the 3 target files, `shared/locale/types/auth/index.ts` (reusable-key audit), `shared/locale/types/message.ts` (MessageSchema registration), `shared/locale/namespaces/errors/errors.namespace.ts` (handle), root `AGENTS.md` ("i18n / Localized Errors"), `shared/AGENTS.md` (Translation System + Namespace Registration), `.agents/instructions/frontend.instructions.md` §i18n.

---

## ⚠ CRITICAL CORRECTION APPLIED (plan-review-R1 Finding #1)

The plan's 10 requested keys were audited against ground truth BEFORE editing: **7 already exist verbatim** in `ErrorsLabels` and **2 more duplicate existing semantics under other names** (tsgo would also have failed outright on re-declaration). Per R1 correction #1, this task treats collisions as REUSE-only and adds genuinely missing keys.

## REUSE DECISION TABLE (REQ-051/plan §2.3 key list → action)

| Requested key | Action taken | Rationale |
|---|---|---|
| `internalServerError` | **REUSED-EXISTING** | Already declared in `ErrorsLabels`; en "An internal server error occurred." / ar "حدث خطأ داخلي في الخادم." exist |
| `unauthorized` | **REUSED-EXISTING** | Exists (`UNAUTHORIZED` category) |
| `forbidden` | **REUSED-EXISTING** | Exists (`FORBIDDEN`) |
| `notFound` | **REUSED-EXISTING** | Exists (`NOT_FOUND`/`{ENTITY}_NOT_FOUND`) |
| `conflict` | **REUSED-EXISTING** | Exists (`CONFLICT`) |
| `serviceUnavailable` | **REUSED-EXISTING** | Exists |
| `badRequest` | **REUSED-EXISTING** | Exists |
| `validationFailed` | **REUSED-EXISTING via `validation`** | Near-duplicate semantics vs existing `validation` ("Invalid input."/"إدخال غير صحيح.") → REQ-055 prohibits. **Naming decision (recorded): keep canonical `validation`.** Later tasks use `t.errors.validation` for the VALIDATION category message |
| `rateLimited` | **REUSED-EXISTING via `rateLimitExceeded`** | Duplicates existing `rateLimitExceeded`. **Naming decision (recorded): keep canonical `rateLimitExceeded`** — additionally aligns with production legacy code `RATE_LIMIT_EXCEEDED` emitted by `RateLimitExceededError` (R1 §(b) taxonomy finding; alias handled by Task 2.1). Later tasks use `t.errors.rateLimitExceeded` for categories 8/RATE_LIMITED |
| `duplicateRequest` | **ADDED-NEW** | Genuinely missing — no equivalent anywhere (audited errors namespace: absent; auth namespace: only email-specific `emailAlreadyExists` at `types/auth/index.ts:43`; common/dashboard/landing/ui/session/profile types: zero "already/duplicate" hits). Serves unique-constraint duplicate rejects (23505→ConflictError translation path) |

### Keys considered and NOT added beyond the above

None. Full audit found no other gaps: every remaining REQ-010 error category already has a localized message key, field-level validations reusing auth keys was out-of-scope (REQ-055), and governance/session keys (`accountDeleted/Blocked/Suspended`, `tokenExpired`, `forbiddenRole`) already exist from DEV1-002.

## Final changes made

- **Types:** added one property to `ErrorsLabels`: `duplicateRequest: string;` placed after `conflict` with doc comment (unique-constraint duplicate reject, REQ-051).
- **en:** `duplicateRequest: "This value is already in use."`
- **ar:** `duplicateRequest: "هذه القيمة مستخدمة بالفعل."` (natural RTL phrasing, same sentence style/punctuation convention as sibling keys; Arabic line-height tokens untouched — no styling files modified).
- Namespace stays registered exactly once: `ErrorsLabels` → `message.ts` line 11 `errorsTranslations: ErrorsLabels` → client handle `Errors = defineNamespace("errors.errors", …)` (`shared/locale/namespaces/errors/errors.namespace.ts`). No new namespace ⇒ registration steps 4–5 of shared/AGENTS.md n/a; `shared/locale/{types,en,ar}/index.*` barrels untouched.

## FINAL KEY LIST — carry-forward for Phase 4 UI work (canonical `errors.errors` surface, 18 keys)

`accountBlocked`, `accountDeleted`, `accountSuspended`, `badRequest`, `conflict`, `duplicateRequest`, `failedToSetLocale`, `forbidden`, `forbiddenRole`, `internalServerError`, `invalidLocale`, `invalidOrigin`, `notFound`, `rateLimitExceeded`, `serviceUnavailable`, `tokenExpired`, `unauthorized`, `validation`

⚠ Use these exact names in Task 4.x/finalizeGraphqlErrors mapping tables: there is NO `validationFailed` and NO `rateLimited` key (naming decisions above).

## Verification results

| Gate | Command | Result |
|---|---|---|
| QL ×3 | `bun run scripts/health/sub-loop.ts {shared/locale/types/en/ar-errors files} --lifecycle duplicates` | ✅✅✅ all exit 0 (each: tsgo-scoped + oxlint + biome:check + lint:type-aware via lint service + jscpd duplicates) |
| Compile gate | `bun run tsgo` (full project) | ✅ exit 0, **0 errors** → MessageSchema parity proof REQ-054 (missing ar/en key or unimplemented type property would fail compile) |
| TE Tier 1 | Throwaway `/tmp/dev3-002-task12-parity-check.ts` (imported live modules): en key set === ar key set === types-interface regex-extracted set, 18 keys, all values strings, zero duplicate literal keys | ✅ **PASS — PARITY CHECK: PASS** (no test file added to repo per task instructions) |
| TE Tier 2 | Interpolated-function calling convention | N/A — final keys are all static strings; no `(… ) => string` keys introduced (plan §2.3 prefers static absent throw-site need) |

## SEC copy review (REQ-021/034)

Added copy discloses none of: lockout thresholds, attempt counters, account existence (phrased at value level — "This value…"/"هذه القيمة…" — deliberately distinct from `auth.emailAlreadyExists` account-hint which is intentional per that flow's spec), governance flag identities. Existing 17 keys' copy unchanged (already compliant).

## SR semantic review

No near-duplicate introduced: single net-new key, verified absent across ALL `shared/locale/types/**` interfaces before adding. Types/en/ar triple kept mechanically in sync (single edit set, one insertion point, then machine-verified identical ordered key lists). No hardcoded user-facing strings introduced elsewhere. Forbidden symbols untouched (no next-intl / getBackendTranslations / shared/messages imports; grep-clean of the 3 files).

## IV instruction verification

Namespace-registration pattern validated against `shared/AGENTS.md`: extending an EXISTING registered namespace requires only steps 1–2 (types interface + locale implementations); step 3 (`message.ts`) export pre-existing and unchanged by design; steps 4–5 apply to new namespaces only. Property-access pattern (`t.<key>` compile-time chain) preserved — interface remains a flat readonly string map. Layer isolation intact: shared files import only type from `@/shared/locale/types/errors` (alias import convention).

## Scope boundary evidence

At execution end, `git diff --name-only` over my work contains ONLY the 3 in-scope shared/locale files (plus plan artifacts I authored). Concurrent `backend/types/*` entries in git status originate from the parallel Task 1.1 subagent (simultaneous execution confirmed at checkout start: those paths were untouched when I began).

## Cross-file dependencies (carry-forward)

- **Phase 2.x (masking/taxonomy/finalizeGraphqlErrors):** map INTERNAL_SERVER_ERROR→`t.errors.internalServerError`, RATE_LIMITED(+legacy alias RATE_LIMIT_EXCEEDED per R1/BLT-08)→`t.errors.rateLimitExceeded`, VALIDATION→`t.errors.validation`, 23505-duplicate domain rejects→`t.errors.duplicateRequest` (new).
- **Phase 3 tests:** assert `duplicateRequest` message equality for en+ar in translation-path coverage if suites cover it.
- **Phase 4 UI:** final key list above; permission-denied fallback should continue using `forbiddenRole`.
