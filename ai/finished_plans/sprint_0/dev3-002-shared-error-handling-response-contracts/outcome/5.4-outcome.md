# Task 5.4 Outcome — Concurrency & Chaos Tier (dev3-002)

- **Task ID (execution leg):** 8-c (re-stamp; prior leg died mid-write leaving the suite file present but unverified — this leg verified it, adjusted nothing, and formalized dispositions)
- **Scope:** REQ-076 chaos probes over the shared error-handling producers: the error-path-purity half executed green here, storage-bound cells formally dispositioned into a dedicated ledger row.
- **Deliverable:** `backend/lib/errors/test/concurrency-chaos.contract.test.ts` (sole source file of this task; NOT modified by this leg — arrived complete from the interrupted leg and ran green as-is).

---

## Pre-execution knowledge read (cited)

`tasks.md` §5.4 definition + execution protocol; specs REQ-022/040/041/043/076 rows (+ REQ-042/077 adjacent); `docs/IDEMPOTENCY.md` B-rules (24h expiry wording — verified no in-tree idempotency guard/service exists yet: `DUPLICATE_REQUEST` producers exist only in taxonomy/tests/docs/migration triggers); `deferred-items.md` full ledger (BLT-06 PG absence, BLT-13 Task-8-a consolidation precedent); outcome 5.2 carry-forward naming THIS file as a foreign mid-write type-red → owning-leg re-stamp obligation; FULL READ of the suite itself (817 lines: storm fixtures, cyclic graphs, proxies, zoo, header battery); ground truth `error-masking.ts` guarded channels + `resolveRequestId` acceptance rules.

## Suite run records

| Gate | Command | Result |
|---|---|---|
| Mandated runner | `bun run test/scripts/run-test.ts backend/lib/errors/test/concurrency-chaos.contract.test.ts` | **12 pass / 0 fail / 702 expects, exit 0** |
| QL sub-loop | `bun run scripts/health/sub-loop.ts backend/lib/errors/test/concurrency-chaos.contract.test.ts --lifecycle duplicates` | **exit 0** (tsgo ✓ oxlint ✓ biome ✓ lint:type-aware ✓ duplicates-scope skip per jscpd scan bounds) |
| Types project-wide | `bun tsgo --noEmit` | **exit 0, zero errors** — owning-leg re-stamp performed jointly with 5.3 (both files were the foreign mid-write reds blamed by the 5.1/5.2 outcomes) |

Zero fixes were required: the suite is green under the mandated runner and all QL stages on first contact.

## Coverage vs requirement (REQ-076 + cited REQs)

**Legend:** ✅ proven green here · 🔒 CI-only remainder (consolidated in BLT-14 below).

| REQ row (tasks.md §5.4 coverage wording) | Status | Evidence (suite tiers/tests) |
|---|---|---|
| `Promise.allSettled` replay burst on shared idempotency key → exactly one success + rest `DUPLICATE_REQUEST` (409 on API routes) (REQ-022/076) | 🔒 storage-bound | No idempotency guard/service exists in-tree (grep across backend: producer code absent outside taxonomy/errors lib). The CONCURRENCY HALF is proven at producer tier below — allSettled storms through the real finalizer with zero drift; what needs DB+guard is the one-success/DUPLICATE_REQUEST arbiter itself. Consolidated in **BLT-14** |
| After-marked-5xx first attempt → same-key retry allowed (24h expiry semantic wording, REQ-043) | 🔒 contract-bound | `docs/IDEMPOTENCY.md` B-rules pin the semantics; guard doesn't exist to test against. Doc-wording assertion owned by plan-review record; behavioral replay-after-5xx test lands with the owning service ticket via BLT-14 |
| Error-path purity: prove zero DB writes emitted from translation/masking/envelope/taxonomy utilities (REQ-040/076) | ✅ | Structural proof: these modules import NO DB surface whatsoever (suite exercises them directly; zero repo/pg/drizzle imports in their module graphs). Behavioral proof under duress: Tier 1 storms |
| — parallel purity WITHOUT shared-state drift | ✅ | Tier 1: 4 rounds × 36 legs (six-class adversarial mix ×6 reps: ConflictError w/ extensions, 23505-cause wrapper, opaque mask-record, ValidationError+fields, RateLimitExceededError, located Forbidden carrier) via `Promise.allSettled` — every parallel payload BYTE-EQUAL to the sequential twin AND to every other round AND to pre-storm pristine baseline; post-storm replay identical; module constants pristine (`REDACTION_MAX_DEPTH===6`, `REDACTION_MAX_ITEMS===64`, taxonomy verdicts unchanged after barrage); caller-owned inputs survive every invocation class UNMUTATED (snapshot before/after double-finalize per class) |
| — declared logging channel exactly-once per masked element under full interleaving | ✅ | Tier 1: exactly 6 `[ERROR]` markers per storm round (= masked elements only; domain-classified variants ride their silent observation channel — pinned by the "Unhandled non-domain" census = 6 on sequential twin too) |
| Forced child-insert failure inside transaction → complete rollback preserved; typed error propagates; boundary masking does NOT swallow the rollback error (REQ-041, DEV1-002 precedent) | 🔒 DB-bound | Requires transaction-wrapped service + live provider (PG-bound per BLT-06 even if SQLite stood in). Producer-side guarantee already pinned elsewhere: masking CLASSIFIES without consuming throwables (Tier 2 here proves classification survives hostile carriers incl. UNIQUE-tail rings — the rollback error's signature information survives to the right typed code). Whole test consolidated in **BLT-14** |

### Behavior tiers shipped beyond the minimum cells

| Tier | What it pins |
|---|---|
| Tier 2 cyclic cause graphs (REQ-042 fuzz) | Self-loop / tangled pair / triple ring / four-ring whose tail hides `UNIQUE constraint failed: chaos.handshake_code` ALL terminate deterministically: byte-equal passes, <2.5s budget, exactly 3 masked-path logs (UNIQUE-tail correctly domain-classified CONFLICT with localized message, ring members behind it masked ISE) |
| Tier 2 hostile carriers & huge paths | Throwing-getter traps on carrier `message`/`extensions` mask cleanly (guarded property reads; extensions collapse to exactly `["code","requestId"]`); redactor twin turns inaccessible bag slots into `[INACCESSIBLE]` keeping siblings alive; 60 000-segment path crosses finalizer VERBATIM end-to-end while the redactor enforces its 64+marker cap, <4s budget, single correlated log |
| Tier 2 proxies & exotic structures | Fresh-manufacturing proxy (infinite depth to naive walkers): depth cap collapses it <2s, sensitive key NEVER reaches the trap (`zoomAccessToken` get-count === 0, value short-circuited to `[REDACTED]`); self-referential structure terminates at documented bound; virtual billion-cell array served from its bounded window only (64+marker, first cell verbatim) |
| Tier 2 non-Error rejection zoo | Functions/arrows/classes/generator objects/Symbol/bigint/function-proxy/boxed-Symbol rejections all yield well-formed INTERNAL_SERVER_ERROR envelopes on BOTH surfaces (GraphQL item keys + REST envelope keys exact), localized message, ONE log call per masked element per surface (×16 census exact) |
| Tier 3 resolveRequestId hostile-header battery (Decision D4) | 10 smuggle shapes (comma multi-value, 8× oversized, boundary+1, trim-to-empty, NUL, newline, tab, object return, symbol return, empty) each lose to a FRESH UUIDv4 that changes across retries (never echoes/truncates); 3 honored shapes (plain, whitespace-padded-trimmed, exact-boundary echo) are deterministic across repeats; 400-leg generation burst: legit ids echo faithfully interleaved with hostile ones, every hostile mint UNIQUE (zero collisions) |

## Production-bug observations

None. All hostile inputs degraded inside documented bounds; no hang, no leak fragment, no cross-element contamination, no shared-state mutation detected anywhere in the battery.

## Gates summary

Runner green (12/0/702) · QL duplicates exit 0 · project-wide tsgo exit 0 (re-stamp performed) · no production modules touched · deliverable file untouched from the interrupted leg.

## Carry-forward

1. **BLT-14 (new ledger row)** carries the three storage/contract-bound cells (shared-key DUPLICATE_REQUEST arbitration over-wire, replay-after-5xx key release with 24h expiry wording, forced child-insert rollback preservation) until an idempotency-guarded mutation service exists; when it lands, extend THIS suite's Tier 1 pattern (`Promise.allSettled` + byte-compare discipline) against the real guard rather than inventing a new convention.
2. Harness exclusions of record (suite header §Harness exclusions): only channels the modules themselves GUARD receive hostile payloads; unguarded-channel traps sit outside both the modules' promises and this corpus.
