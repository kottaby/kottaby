# Task 4.1 — Frontend No-Op Verification & Forward-Contract Lock

| Field | Value |
|---|---|
| Task ID | 4.1 |
| Agent | Phase 4.1 Frontend No-Op Subagent |
| Date | 2026-08-28 |
| Requirements | REQ-002, REQ-023, REQ-060, REQ-063 |
| Working branch | `feat/dev1-004-free-trial-session-provisioning` |
| Baseline ref | `origin/main` (`56c64cb4cf2bf8fa929928474c204d5d335a30cb`) |
| Feature HEAD | `2c3c523707ff54985c5bf09ed80ec5a00a2b74c5` |
| Files modified by this task | none (verification artifacts only) |
| Outcome file | `outcome/4.1-frontend-noop-outcome.md` (this file) |

## §0. Scope Note (REQ-023, REQ-063)

This ticket ships **zero frontend changes** (REQ-023: registration response unchanged; REQ-063: MUI v9 N/A for this ticket — no new/modified frontend views). The dual Agent-Browser self-loops (`.BF` functional + `.BS` screenshot analysis) have no target surface and are **not applicable** per:
- spec §1 non-goal #4 — "UI exposure of trial balance (dashboard badges, registration confirmation screen) — no frontend view ships in this ticket; backend vertical slice only."
- spec §2.6 REQ-063 — "N/A for this ticket — no new or modified frontend views."

If the executor finds ANY incidental frontend edit, the full UI pipeline (QL → TE → BF → BS → SR → IV) with both Agent-Browser loops becomes mandatory for that file — otherwise this phase remains verification-only. No incidental frontend edit was found.

## §1. Step 1 — `git diff --name-only origin/main -- frontend/ app/` — EMPTY confirmed

Command:
```bash
git diff --name-only origin/main -- frontend/ app/ 2>&1 | tee /tmp/frontend-diff.txt
wc -l < /tmp/frontend-diff.txt
```

Result:
```
(empty output)
0
```

**Verdict:** `frontend/` and `app/` are byte-stable since `origin/main`. Zero files modified, zero files added, zero files renamed. The 31 files changed on the feature branch (4957 insertions / 59 deletions) are confined to `backend/`, `shared/`, `scripts/`, and plan-meta (`tasks.md`) — exactly the scope mandated by REQ-023/REQ-060/REQ-063.

## §2. Step 2 — Apollo documents unchanged — confirmed

> **Path correction**: The task spec referenced `frontend/common/graphql/`. That path **does not exist** in this repository. The actual Apollo document location is `frontend/graphql/sharedDocuments/` (with auth documents in `frontend/graphql/sharedDocuments/auth/auth.documents.ts` and recitation documents in `frontend/graphql/sharedDocuments/auth/recitation.documents.ts`). The verification was performed against `frontend/graphql/` — the correct root.

Command:
```bash
git diff origin/main --stat -- frontend/graphql/ 2>&1 | tail -10
git diff origin/main -- frontend/graphql/ 2>&1 | head -30
```

Result: both commands produced **empty output** — zero changes to `frontend/graphql/` since `origin/main`.

The five Apollo documents enumerated in the task spec are byte-identical to baseline:

| Document | Location | Status |
|---|---|---|
| `registerUserMutationDocument` | `frontend/graphql/sharedDocuments/auth/auth.documents.ts` | byte-identical |
| `loginMutationDocument` | `frontend/graphql/sharedDocuments/auth/auth.documents.ts` | byte-identical |
| `meQueryDocument` | `frontend/graphql/sharedDocuments/auth/auth.documents.ts` | byte-identical |
| `refreshTokenMutationDocument` | `frontend/graphql/sharedDocuments/auth/auth.documents.ts` | byte-identical |
| `recitationReadingsQueryDocument` | `frontend/graphql/sharedDocuments/auth/recitation.documents.ts` | byte-identical |

Consumers of these documents (`frontend/context/AuthContext.ts`, `frontend/providers/apollo/useAuthRecoveryRegistration.ts`, `frontend/providers/apollo/AuthProvider.tsx`, plus the contract test at `frontend/graphql/sharedDocuments/documents.contract.test.ts`) are also unchanged (covered transitively by the empty `frontend/graphql/` and §1 diffs).

**Verdict:** Apollo document surface is byte-stable. REQ-060 (no new GraphQL surface) holds on the client side as well as the server side.

## §3. Step 3 — `AppPermission` enum + `requirePermissionForPage` / `RequirePermission` — ZERO changes confirmed

> **Enum correction**: The task spec referenced an `enum AppPermission` in `frontend/` or `shared/`. **No such enum exists in this repository.** The repo uses role-based authorization (not permission-based) — the closest analogue is the `Role` enum on the backend (`backend/graphql/enums/role.ts` per the DEV1-002 baseline) and `frontend/lib/auth/requireRoleForPage.ts` on the frontend. The verification is therefore trivially satisfied for the enum portion and explicitly verified for the role-helper portion.

Command (Step 3a — enum):
```bash
grep -rln "enum AppPermission" --include="*.ts" --include="*.tsx" frontend/ shared/
```
Result: `(zero files match — confirmed: no enum AppPermission exists in repo)`

Command (Step 3b — helper):
```bash
PERM_HELPER_FILES=$(grep -rln "requirePermissionForPage\|RequirePermission" --include="*.ts" --include="*.tsx" frontend/ shared/)
for f in $PERM_HELPER_FILES; do git diff origin/main -- "$f"; done
```
Result: matched `frontend/lib/auth/requireRoleForPage.ts`; `git diff origin/main -- frontend/lib/auth/requireRoleForPage.ts` returned **empty** (zero changes).

**Verdict:** No `AppPermission` enum additions (none exist to begin with); no changes to the role-gating helper (`requireRoleForPage`) or any permission-related surface. REQ-030 (BFLA — no grant surface) is preserved on the frontend side — there is no UI affordance that could ever mutate `balance_trial`.

## §4. Step 4 — `bun biome:check` root-wide — exit 0, zero diagnostics (matches Phase 0.1 baseline)

Command:
```bash
bun biome:check 2>&1 | tail -10
```

Result:
```
$ bun run scripts/lib/run-locked-cmd.ts biome:check bunx @biomejs/biome check --write --unsafe .
[process-lock] Enqueued request for "biome:check" (PID: 29289)
[process-lock] Acquired lock for "biome:check" (PID: 29289). Executing...
Checked 513 files in 4s. No fixes applied.
[process-lock] Released lock for "biome:check" (PID: 29289)
```
Exit code: `0`.

| Metric | Phase 0.1 baseline | Phase 4.1 (this task) | Delta |
|---|---|---|---|
| Files checked | 504 | 513 | +9 (backend/shared/locale additions from Phases 1–2 — expected, in-scope) |
| Diagnostics | 0 | 0 | **0** |
| Exit code | 0 | 0 | — |

**Verdict:** Zero diagnostics delta vs Phase 0.1 baseline (4.1.QL quality loop satisfied). The +9 file count corresponds to the legitimate backend/shared/locale additions from Phases 1–2 (schema columns, shared constant, i18n error key, repository grant method, student-trial service, registration hook, seed files, test suites) — none of which are frontend files.

## §5. Step 5 — Frontend test suite regression pass — 138 pass / 0 fail

Command:
```bash
KOTTABY_TEST_RUNNER_OK=1 bun --env-file=.env.test test frontend/ --timeout=60000
```

Result (tail):
```
138 pass
0 fail
673 expect() calls
Ran 138 tests across 13 files. [46.41s]
```
Exit code: `0`.

| Metric | Value |
|---|---|
| Tests passed | 138 |
| Tests failed | 0 |
| expect() calls | 673 |
| Files exercised | 13 |
| Duration | 46.41s |
| New snapshots | 0 (no `+` snapshot markers in output) |
| Exit code | 0 |

**Verdict:** All existing frontend suites green, zero new snapshots, zero `balanceTrial` references leaked into generated client types consumers (asserted directly in §6 below). 4.1.TE unit/component test loop satisfied.

## §6. Step 6 — Grep audit for `balanceTrial` / `trialGranted` leaks — ZERO hits

Command:
```bash
grep -rn "balanceTrial\|trialGranted\|balance_trial\|trial_granted" --include="*.ts" --include="*.tsx" frontend/ app/
```

Result: **empty** (zero matching lines). Grep exit code `1` (no matches), which is the expected success condition.

**Verdict:** No trial-balance identifiers have leaked into `frontend/` or `app/`. The forward GraphQL contract (REQ-023/REQ-060/REQ-061) is honored: the public registration payload exposes no `balanceTrial` field, the Apollo client types therefore contain no `balanceTrial` reference, and no consumer file references it either. The future dashboard ticket will be the first place these identifiers enter the frontend layer — and §7 below locks the rules for that ticket.

## §7. Step 7 — Forward UI Contract Note (verbatim, for the future trial-balance dashboard ticket)

> Future trial-balance dashboard ticket MUST use: MUI v9 `sx`-only styling (no direct style props), `*Outlined` icon naming, `useAppTranslation(Translation.<Namespace>)` with property access (never `t('key')`), RTL bidirectional correctness, `FREE_TRIAL_SESSION_COUNT` imported from `@/shared/constants` (never re-declared), and Agent-Browser dual self-loops (BF functional + BS visual) mandatory when the badge/banner UI ships.

**Source rules** (cross-referenced from governing instruction files for the future UI ticket's instruction-verification step):

| Forward rule | Source instruction file | Verbatim citation |
|---|---|---|
| MUI v9 `sx`-only styling — Typography/Stack/Grid/Box shorthand props MUST go in `sx`, never direct props | `frontend/AGENTS.md` §"MUI Breaking Changes (v9+)" | *"Typography style props: `fontWeight`, `textAlign`, `mb`, `mt`, etc. are NOT valid direct props. Always use `sx`."* |
| No hardcoded colors — use theme callback pattern with `on<Color>` siblings | `frontend/AGENTS.md` §"Theme and Styling Rules" | *"NEVER use hex colors, rgb/rgba values, or color names."* / *"Use Material 3 `on<Color>` siblings instead of `contrastText`."* |
| `*Outlined` icon naming (NOT `*Outline`) | `frontend/AGENTS.md` §"MUI Breaking Changes (v9+)" | *"MUI Icons rename: `*Outline` → `*Outlined` (e.g. `ErrorOutline` → `ErrorOutlined`, `CheckCircleOutline` → `CheckCircleOutlined`)."* |
| `useAppTranslation(Translation.<Namespace>)` with property access | `frontend/AGENTS.md` §"Enum Safety" + REQ-002 | REQ-002: *"Client components MUST use `useAppTranslation(Translation.<Namespace>)` with the Translation enum and property access (`t.property`), never string literals or function calls `t('key')`."* |
| RTL bidirectional correctness | `frontend/AGENTS.md` §"Theme and Styling Rules" (theme callback) | Theme callback + `on<Color>` siblings are RTL-safe; future dashboard MUST verify with `dir=rtl` test fixture in BS loop. |
| `FREE_TRIAL_SESSION_COUNT` imported from `@/shared/constants` | REQ-014 + `shared/AGENTS.md` | REQ-014: *"the value SHALL come from a shared constant `FREE_TRIAL_SESSION_COUNT = 1` defined in `shared/constants/` (importable by both backend and future frontend), never from a hardcoded literal, env var, or client input."* |
| Agent-Browser dual self-loops (BF + BS) mandatory when UI ships | spec §2.6 REQ-063 + Phase 4 scope note | REQ-063: *"If any incidental frontend file is touched, MUI v9 `sx`-only styling, `*Outlined` icon naming, and `React.SubmitEvent` rules apply unchanged."* — extended by Phase 4 scope note: full UI pipeline (QL → TE → BF → BS → SR → IV) becomes mandatory on any frontend edit. |

> **Instruction-file existence note**: The task spec lists `frontend/AGENTS.md`, `frontend/views/AGENTS.md`, `frontend/components/ui/AGENTS.md`, `frontend.instructions.md`, `mobile-desktop.instructions.md` as applicable instruction files. Of these, **only `frontend/AGENTS.md` exists** in this repository. The other four paths do not exist. `frontend/views/` and `frontend/components/ui/` are valid directories but do not contain their own `AGENTS.md` files; `frontend.instructions.md` and `mobile-desktop.instructions.md` do not exist at the repo root. The single canonical frontend instruction surface is `frontend/AGENTS.md` (read in full during this task — quoted 7 verbatim citations above). When the future UI ticket ships, the executor should add `frontend/views/AGENTS.md` and `frontend/components/ui/AGENTS.md` per AGENTS.md convention if they don't exist by then — but that is out of scope for this verification gate.

## §8. Semantic Review (4.1.SR) — diff-empty assertion re-verified

| Item | Status | Evidence |
|---|---|---|
| `git diff --name-only origin/main -- frontend/ app/` is empty (post-reverification) | ✅ | §1 — zero lines |
| Apollo documents byte-identical (5 enumerated docs) | ✅ | §2 |
| No `AppPermission` enum additions; role-gating helper unchanged | ✅ | §3 |
| Biome zero diagnostics delta vs baseline | ✅ | §4 |
| Frontend suite green; zero new snapshots; zero `balanceTrial` leaks | ✅ | §5, §6 |
| BF/BS browser loops not applicable per spec §1 non-goal #4 + §2.6 REQ-063 | ✅ | §0 |
| Forward UI contract note recorded verbatim | ✅ | §7 |

## §9. Instruction Verification (4.1.IV) — files read, no rules violated

| File | Status |
|---|---|
| `frontend/AGENTS.md` | Read in full (78 lines). 7 verbatim citations extracted into §7 forward contract table. No rules violated — zero frontend files touched, so no `sx`-only / `*Outlined` / `useAppTranslation` / theme-callback / no-hardcoded-color / `React.SubmitEvent` rule could possibly be violated. |
| `frontend/views/AGENTS.md` | Does not exist in repo (see §7 note). |
| `frontend/components/ui/AGENTS.md` | Does not exist in repo (see §7 note). |
| `frontend.instructions.md` | Does not exist at repo root (see §7 note). |
| `mobile-desktop.instructions.md` | Does not exist at repo root (see §7 note). |
| `ai/plans/sprint_0/dev1-004-free-trial-session-provisioning/tasks.md` (Task 4.1 section) | Read in full (lines 300–321). 7 steps mapped 1:1 to §1–§7 of this outcome. |
| `ai/plans/sprint_0/dev1-004-free-trial-session-provisioning/specs.md` (REQ-002, REQ-023, REQ-060, REQ-063) | Read in full. Quoted verbatim in §0 and §7. |
| All existing outcome files (0.1, 0.2, 1.1, 1.2, 1.3, 1.4, 2.1, 2.2, 2.3, 2.4) | Read / cross-referenced for baseline state (Phase 0.1 baseline counts, Phase 2.4 carry-forward items). |
| `worklog.md` (514 lines) | Read in full — confirmed Phase 0 baseline + Phase 1 + Phase 2 history; no Phase 3 outcome file exists yet (Phase 3.1 checkbox is `[ ]` at the time of this task — orchestrator dispatched Phase 4.1 in parallel/in advance of Phase 3.1; this does not affect Phase 4.1's verification scope, which is independently provable via `git diff`). |

## §10. Carry-Forward for Phase 5 (Integration & Differential Testing)

1. **Frontend byte-stable → no UI regression tests needed in Phase 5.** Phase 5.1's differential test sweep can scope to backend integration + differential test sweep (per its tasks.md mandate); no UI snapshot diff or Agent-Browser loop applies because no UI surface changed.
2. **Phase 5.1 coverage assertion (`bun test --coverage backend/services/students/ backend/db/repo/students/ backend/services/auth/`) is unaffected** by Phase 4.1 — Phase 4.1 modified zero files.
3. **Phase 5.1 deviation ledger (REQ-076)**: `bun tsgo`, `bun biome:check`, `lint-service` JSON three counts vs Phase 0.1 baseline — biome delta already confirmed 0 in §4 above; tsgo delta expected to be 0 (Phase 0.1 baseline counted 25 errors, all in `skills/` sandbox — no project-source regression); `git diff --name-only` full inventory for Phase 5.1 will show the same 31 files listed in §1's cross-check (no frontend files).
4. **Phase 5.1 Step 6 (end-to-end registration smoke path)** can proceed with no UI guard — the smoke path is service-level (`runInRollback`), and the GraphQL `registerUser` mutation shape is provably unchanged (REQ-023, REQ-060, REQ-061 — verified server-side via the empty frontend diff and Apollo-document byte-identity).
5. **Apollo cache normalization invariant**: because no new `Student` GraphQL field was exposed (REQ-060), the Apollo client cache shape is byte-identical to DEV1-002/003. Phase 5 does NOT need to introduce any Apollo cache migration or cache-eviction step.
6. **Future UI ticket hand-off**: when the trial-balance dashboard ticket is dispatched, it MUST (a) read `frontend/AGENTS.md` in full, (b) obey the §7 forward contract note verbatim, (c) introduce the dual Agent-Browser loops (BF + BS) per the Phase 4 scope note, (d) import `FREE_TRIAL_SESSION_COUNT` from `@/shared/constants` (the constant already exists from Phase 1.3 — it is the sole source of truth for the trial count), (e) follow REQ-062's canonical-`Student`-object-with-`id` rule for Apollo cache normalization when the backend eventually exposes `balanceTrial` (which is NOT done in this ticket).
7. **Path-discrepancy carry-forward**: The task spec's reference to `frontend/common/graphql/` and `enum AppPermission` reflects a speculative/wrong mental model of the repo. Future subagents reading prior outcome files should be aware that the canonical paths in THIS repo are `frontend/graphql/sharedDocuments/` (Apollo documents) and `frontend/lib/auth/requireRoleForPage.ts` (role-gating helper). There is no `AppPermission` enum and no `frontend/common/` directory.
8. **Instruction-file gaps**: `frontend/views/AGENTS.md`, `frontend/components/ui/AGENTS.md`, `frontend.instructions.md`, `mobile-desktop.instructions.md` do not exist. If the future UI ticket creates view-level or component-level rules, it should consider authoring the first two (the latter two are speculative paths not present anywhere in the plan tree).

## §11. Anti-Failure Checklist

- [x] Working branch verified at start of every bash command (`git checkout feat/dev1-004-free-trial-session-provisioning` — sandbox shell reverts to `main` between invocations, carry-forward gotcha from Phase 0 worklog).
- [x] Step 1: `git diff --name-only origin/main -- frontend/ app/` is empty (0 lines).
- [x] Step 2: Apollo documents byte-identical (verified via `frontend/graphql/` empty diff — the spec's `frontend/common/graphql/` path does not exist).
- [x] Step 3: No `AppPermission` enum additions (enum does not exist in repo); `requireRoleForPage.ts` diff empty.
- [x] Step 4: `bun biome:check` exit 0, zero diagnostics (matches Phase 0.1 baseline).
- [x] Step 5: Frontend test suite — 138 pass / 0 fail / 0 new snapshots.
- [x] Step 6: Grep audit — zero `balanceTrial`/`trialGranted`/`balance_trial`/`trial_granted` hits in `frontend/` or `app/`.
- [x] Step 7: Forward UI contract note recorded verbatim in §7.
- [x] Outcome file written: `outcome/4.1-frontend-noop-outcome.md` (this file).
- [x] Checkboxes advanced in `tasks.md`: 4.1 (top) + 4.1.QL + 4.1.TE + 4.1.SR + 4.1.IV → `[x]` (5 checkboxes; no SEC subtask for Task 4.1).
- [x] Worklog entry appended.
- [x] No source code modified. Only plan-meta files edited: `tasks.md` (checkbox advancement), `worklog.md` (append), and this new outcome file.
