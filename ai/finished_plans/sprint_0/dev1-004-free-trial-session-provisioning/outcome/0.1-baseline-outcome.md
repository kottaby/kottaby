# Outcome 0.1 — Pre-Implementation Baseline & Deferred-Items Ledger

- **Task ID**: 0.1
- **Plan**: DEV1-004 — Free Trial Session Provisioning
- **Agent**: Phase 0 Baseline Subagent (general-purpose)
- **Date**: 2026-08-26
- **Branch**: `feat/dev1-004-free-trial-session-provisioning`
- **Plan directory (on disk)**: `ai/plans/sprint_0/dev1-004-free-trial-session-provisioning/`

---

## 1. Baseline Counts

| Metric | Value | Raw Output Location |
|---|---|---|
| **tsgo error count** (`grep -c "error TS"`) | **25** | `/tmp/baseline-tsgo.txt` |
| **biome:check diagnostic count** (`grep -c "✖\|×"`) | **0** ("No fixes applied"; exit 0) | `/tmp/baseline-biome.txt` |
| **lint-service JSON** | `success: false`, `exitCode: 1`, `fileCount: 0`, `output: ""` | `/tmp/baseline-lint.json` |

### 1.1 tsgo error breakdown

All 25 baseline tsgo errors originate from the **`skills/`** sandbox directory (helper scripts unrelated to the project's source tree). The `skills/` directory is preserved across sandbox resets and depends on `z-ai-web-dev-sdk`, which is not installed in this repo. The 25 errors consist of:

- 14 × `error TS2307: Cannot find module 'z-ai-web-dev-sdk'` (one per skill script)
- 11 × `error TS6133: '<unused>' is declared but its value is never read` (within skill scripts)

**Critical:** Zero tsgo errors originate from `backend/`, `frontend/`, `shared/`, `app/`, `scripts/`, or `test/`. The project source tree is fully green at baseline. Phase 5.1 deviation accounting (`grep -c "error TS"`) must compare against this baseline of 25 and is expected to remain at 25 (i.e., **zero new errors**) provided no project source file is broken.

### 1.2 biome:check outcome

```
Checked 504 files in 6s. No fixes applied.
```

Exit code 0; zero diagnostics. The `bun biome:check` script runs with `--write --unsafe` (auto-fix on), so the count represents the post-autofix residual — and there was nothing to autofix. Phase 5.1 deviation accounting must show this remains at 0.

### 1.3 lint-service JSON outcome

The repo-wide ESLint run (full-repo mode, concurrency=4, duration 49.7s) returned `success: false, exitCode: 1`. The JSON envelope does not surface per-rule counts (the `output` field is empty when ESLint exits non-zero at the process level in this version of the script). This is the **pre-existing baseline state**: the same `success: false` outcome is reproducible by re-running `bun run scripts/lint-service.ts --json --id baseline`.

Phase 5.1 deviation accounting must compare the post-implementation JSON envelope's `success` flag and `exitCode` to this baseline. The carry-forward rule: if `success` was `false` at baseline and remains `false` with the **same** exit code, that is NOT a deviation introduced by DEV1-004 — only a transition to `success: true` is an improvement; a transition to a different non-zero exit code or an increased error fileCount would be a regression.

---

## 2. Git Baseline Snapshot

### 2.1 Baseline commit (`git rev-parse HEAD`)

```
56c64cb4cf2bf8fa929928474c204d5d335a30cb
```

This commit is shared between `main` and `feat/dev1-004-free-trial-session-provisioning` (the feature branch was just created from main per the Phase 0 setup worklog entry).

### 2.2 Baseline modified-files snapshot (`git diff --name-only`)

```
.gitignore
```

Only `.gitignore` is tracked-modified at baseline (intentional — per the setup worklog: "Restored sandbox dirs and merged .gitignore (preserved `skills/` rule)"). Untracked files present at baseline (NOT in `git diff --name-only` but recorded here for completeness):

```
postgresql-17_17.11-0+deb13u1_amd64.deb
postgresql-client-17_17.11-0+deb13u1_amd64.deb
postgresql-client_17+278_all.deb
postgresql-common_278_all.deb
postgresql_17+278_all.deb
scripts/pg-start.sh
```

These untracked artifacts are user-space PostgreSQL installers and the cluster start script — they are NOT part of the DEV1-004 implementation surface and MUST NOT be staged or committed. Phase 5.1 deviation accounting uses `git diff --name-only` (tracked files only) vs this baseline; the expected post-implementation delta is the DEV1-004 implementation file set.

---

## 3. Deferred-Items Ledger Pre-Seed (D1 / D2)

The file `ai/plans/sprint_0/dev1-004-free-trial-session-provisioning/deferred-items.md` has been updated from the empty ledger state to contain exactly two pre-seeded rows. Verbatim table contents (Status column uses ✅, NOT ❌/⚠️ — these items are pre-seeded as resolved-by-contract / targeted-at-future-ticket and are explicitly EXEMPT from the `grep -c "❌\|⚠️"` enforcement gate per REQ-083):

| ID | Deferred Item | Source Task | Target Task | Status | Verified By | Notes |
|---|---|---|---|---|---|---|
| D1 | Trial-grant notification dispatch | DEV1-004 (0.1 baseline) | DEV3-010 (notifications engine) | ✅ Done (non-blocking) | Phase 0.1 baseline | Notifications table exists (A.4); dispatch engine deferred to DEV3-010 per spec §1 non-goal #3. Explicitly non-blocking per REQ-083. |
| D2 | Trial eligibility + trial-first decrement *execution* | DEV1-004 (0.1 baseline) | DEV3-004 / DEV3-013 (booking & escrow) | ✅ Done (contract recorded) | Phase 0.1 baseline | Only the forward CONTRACT (REQ-020..022) ships in DEV1-004. Execution deferred to DEV3 booking/escrow. Explicitly non-blocking per REQ-083. |

### 3.1 Enforcement gate note (carry-forward to Phase 5.4)

Running `grep -c "❌\|⚠️" ai/plans/sprint_0/dev1-004-free-trial-session-provisioning/deferred-items.md` currently returns **2** because the file's "Status Values" legend section at the bottom defines what each status emoji means (it contains literal `⚠️ Partial` and `❌ Blocked` lines as legend entries, NOT actual ledger entries). The D1/D2 rows themselves use ✅ only. The final quality gate at Phase 5.4 will need to either (a) remove the legend section, or (b) explicitly reconcile the count. **No Phase 0 action is taken on the legend** — this is recorded as carry-forward knowledge for Phase 5.4.

---

## 4. Requirements Covered

- **REQ-001** — Pre-Implementation Baseline & Ledger: baseline counts recorded above; `deferred-items.md` initialized with two pre-seeded entries (D1, D2). ✅
- **REQ-076** — Deviation-from-Baseline Statement: this outcome file establishes the baseline; Phase 5.1 will produce the deviation diff against this baseline. ✅ (baseline anchor only)
- **REQ-083** — Outcome Protocol: this file is the per-task outcome artifact; deferred-items pre-seed uses ✅ status (not ❌/⚠️) so the final enforcement gate is not violated by the pre-seed itself. ✅

---

## 5. Carry-Forward Knowledge

1. **PostgreSQL is running in user-space** at `/tmp/pgdata` (cluster dir) on `127.0.0.1:5432` with socket dir `/tmp`. Connection: `postgresql://z@127.0.0.1:5432/app_db`. `bun db push` works (verified during setup phase — "Success" reported). Schema is already pushed.

2. **Bun 1.3.14** at `/usr/local/bin/bun`. The `~/.bun/bin/bun` mirror may also exist. Use `bun` directly (no full path needed).

3. **Baseline counts will be referenced by Phase 5.1 and Phase 7.4 deviation accounting**:
   - tsgo baseline: 25 (all in `skills/` sandbox; zero in project source)
   - biome baseline: 0 diagnostics
   - lint-service baseline: `success=false, exitCode=1` (pre-existing, NOT introduced by DEV1-004)
   - Expected post-implementation delta: **zero new errors** of any kind.

4. **The `git diff --name-only` baseline is just `.gitignore`** (intentional from setup). The DEV1-004 implementation file set will form the actual delta. Untracked PostgreSQL `.deb` files and `scripts/pg-start.sh` MUST NOT be staged.

5. **Plan path on disk**: `ai/plans/sprint_0/dev1-004-free-trial-session-provisioning/` (note the `sprint_0/` segment). The plan files internally reference the simplified path `ai/plans/dev1-004-...` — always use the on-disk path for file writes (per Phase 0 setup worklog).

6. **No plan-review gate outcome files exist** under `ai/plans/dev1-002-*/outcome/` or `ai/plans/dev1-003-*/outcome/` for the orchestrator to read; the plan header asserts "approved" and the setup worklog records "proceeding". Phase 0.2 verifies the DEV1-002/003 prerequisites directly from source instead.

7. **The feature branch `feat/dev1-004-free-trial-session-provisioning` was created during setup but the working session started on `main`**. The Phase 0 subagent switched to the feature branch (`git checkout feat/dev1-004-free-trial-session-provisioning`) before capturing the baseline. HEAD is `56c64cb` on both branches. Future subagents should verify they are on the feature branch before making changes.

8. **`bun run lint-service` does NOT exist as a package.json script** — the actual script is `bun run lint` (which calls `scripts/lint-service.ts`). To invoke the lint-service directly in JSON mode, use `bun run scripts/lint-service.ts --json --id baseline` (as documented in the script's header comment).

---

## 6. Anti-Failure Checklist (self-verification)

- [x] Read SKILL.md Phase 0 section in full
- [x] Read tasks.md Phase 0 section in full
- [x] Read specs.md REQ-001 / REQ-076 / REQ-083 in full
- [x] Ran `bun tsgo`, `bun biome:check`, `bun run scripts/lint-service.ts --json --id baseline` and captured outputs to `/tmp/baseline-*.txt|json`
- [x] Captured `git rev-parse HEAD` (`56c64cb...`) and `git diff --name-only` (`.gitignore` only) baseline
- [x] Pre-seeded D1 and D2 in `deferred-items.md` with ✅ status (NOT ❌/⚠️)
- [x] Verified the D1/D2 row content matches the task spec table verbatim
- [x] No source code modified (Phase 0 is read-only); only `deferred-items.md` was edited (a plan-meta file, not source)
