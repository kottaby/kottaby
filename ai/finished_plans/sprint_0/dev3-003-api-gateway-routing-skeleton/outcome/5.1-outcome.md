# Task 5.1 Outcome — GraphQL Gateway Integration Matrix (REQ-071)

- **Task ID:** C4-livewire · date 2026-08-27 · suite by owner commit `0478a6f`, execution via isolated-mirror procedure (below)
- **Result: `frontend/graphql/test/gateway/gateway.integration.test.ts` — 14 pass / 0 fail, exit 0 (44 expects, ~17s)**

## Per-matrix-row evidence (over real HTTP against a booted `next dev -p 3066`)

| Row | Disposition | Evidence |
|---|---|---|
| (a) `_health` anon → 200 + full payload | ✅ PASS | live HTTP 200, `{status,service,version,timestamp}` |
| (b) unknown field → BAD_REQUEST-family | ✅ PASS | never 404, never unmasked 500 |
| (c) malformed JSON → 400 + requestId | ✅ PASS | envelope requestId present |
| (d) PUT/DELETE/PATCH/GET → 405 + `Allow: POST` | ✅ PASS | all four methods |
| (e) anon `me` → `extensions.code=UNAUTHORIZED` (transport-200) | ✅ PASS | masked envelope |
| (h) `X-Request-Id` inbound echo | ✅ PASS | echoed in error requestId |
| `/api/health` GET → envelope | ✅ PASS | 200 + `{data,requestId}` |
| unknown path 404, no engine activity | ✅ PASS | 404 asserted |
| CORS preflight — no wildcard ACAO | ✅ PASS | REQ-053 |
| introspection posture | ✅ PASS (suite rows) | prod-deny per D6/env.ts:69 |
| (f) FORBIDDEN role-gated op | ⏭️ `test.failing` deferred | no role-gated op in schema (Sprint-1 surfaces) — documented in suite header |
| (g) synthetic raw-throw masking | ⏭️ `test.failing` deferred | no test-only forced-failure fixture field |
| (i) login happy-path 3×Set-Cookie | ⏭️ `test.failing` deferred | SQLite sandbox: no seeded users (db/app.sqlite empty; registration is PG-bound) |
| (j) X-Idempotency-Key ctx propagation | ⏭️ `test.failing` deferred | needs echo fixture field; unit-tier proof exists (context-keys 14/0 + transport hostile-header battery) |
| (m) logout forced-failure cookies | ⏭️ `test.failing` deferred | resolver always succeeds; needs forced-failure fixture |

All five deferred rows failed-as-expected (<1ms, unconditional placeholders) — bun inverted each to pass; suite exit 0. Every deferral is schema/fixture-gated (not harness-gated) and documented in the suite header + here.

## Execution environment (BLT-07 resolution record)
- Owner `0478a6f` fixed the liveness poll document (`{ _health }` → `{ _health { status } }`) — the one-line class fix.
- Sandbox coexistence with the never-kill `:3000` server: **isolated-mirror procedure** — `rsync` project (excl. `.git/node_modules/.next/logs`) → `/tmp/kottaby-livewire`, real `bun install --frozen-lockfile` there (Turbopack rejects cross-root node_modules symlinks — PANIC recorded), `db/app.sqlite` copied, then `KOTTABY_TEST_RUNNER_OK=1 bun --env-file=.env test --timeout=180000 <file>` inside the mirror (own per-directory dev-lock scope; mirror server boots on 3066 and its afterAll teardown reaps it — port verified empty post-run; zero kills required; `:3000` PID untouched throughout).
- This is the ledger's sanctioned "dist-dir/port isolation honored by the helper" alternative — reproducible; logs at `/tmp/kottaby-livewire-logs/integration.log`.
- QL: sub-loop on the test file — `.tsx/.ts` under frontend/graphql/test run through the runner's in-process tier; file is `.ts` (no DOM preload needed); biome/tsgo clean at HEAD. SEC: leakage-scan rows are real regex assertions in-suite; 401-vs-403 exclusivity held by (e) live + (f) documented deferral. SR/IV: harness API per `frontend/graphql/test/AGENTS.md` rule 10 (no backend imports — raw fetch + shared testClient only); zero test-only surface ships in production (all deferrals are absence-of-fixture, no fixture code added).
