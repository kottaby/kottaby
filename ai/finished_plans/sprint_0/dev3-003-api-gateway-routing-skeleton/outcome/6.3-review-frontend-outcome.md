# Task C3-6.3 — Post-Implementation Review Wave · FRONTEND LENS (dev3-003)

- **Date:** 2026-08-27 (review cycle 3, wave DEV3-003 Phase 6)
- **Reviewer:** read-only review subagent (general-purpose), skill §Post-Implementation Review Wave › review-frontend
- **Diff range:** `12120dd..HEAD` (baseline frozen per `phase0-baseline-outcome.md`; HEAD = `6d50a47`)
- **Source-tree mutations:** NONE (read-only honored). Only writes: this outcome file + worklog is owned by orchestrator (not touched here). Dev server `:3000` never killed/restarted; `bun run build` NOT executed.

---

## Scope reviewed

Per checklist scope, diff-limited to `12120dd..HEAD`:

| File | Delta | Nature |
|---|---|---|
| `frontend/providers/apollo/apolloCache.ts` (+`.test.ts`) | +22 / NEW | plan-owned Task 4.1 (typePolicy) |
| `frontend/components/ApiStatusIndicator.tsx` | NEW 265 lines | parallel product stream |
| `frontend/components/SiteFooter.tsx` | +36 −~9 | parallel product stream (chip mount + polish) |
| `frontend/components/ui/GraphQLErrorSurfaceHost.tsx` | +22 | toast/banner entrance animation + hairline |
| `frontend/graphql/test/gateway/allowlist-coverage.test.ts` | NEW 233 lines | Task 5.2 REQ-072 gate |
| `frontend/graphql/test/warnings/warning-surfacing.test.ts` | 2 lines | doc-comment env-var rename only |
| `shared/locale/{ar,en,types}/landing/index.ts` | +6 ×3 | 4 chip-status i18n keys |
| `app/api/health/route.ts` | NEW 42 lines | REST endpoint the chip polls |
| `test/ui/components/api-status-indicator.test.tsx` (NEW), `site-footer.test.tsx` (+snap) | +163/+16 | component suites |

Pre-reads honored: `worklog.md` (C1-gate → C3-audit, esp. C2-feature-status + C2-phase4a entries), `outcome/phase0-baseline-outcome.md` (env-walls + i18n exemption + .BF/.BS N/A determination), `tasks.md` §Non-Negotiable Protocol + **UI-pipeline note (~line 21)**, `frontend/AGENTS.md`, `frontend/graphql/AGENTS.md` (embedded-type normalization policy). `frontend/components/AGENTS.md` and `frontend/providers/AGENTS.md` DO NOT EXIST on disk (verified; the governing rules live in `frontend/AGENTS.md`).

**UI-pipeline N/A re-confirmation (tasks.md:21):** plan §5 ships zero user-facing UI; `.BF`/`.BS` tiers remain N/A. The live footer chip + SiteFooter polish + GraphQLErrorSurfaceHost motion come from the *parallel product stream* explicitly acknowledged in worklog C2-feature-status ("parallel cron-feature stream WIP left byte-intact" during phase 4a), not from a dev3-003 task; the sole plan-owned frontend touch (Task 4.1 cache policy) is non-visual and unit-verified. Frontend-lens review was therefore conducted as static + suite-based evidence over the touched surfaces (this document).

The pre-existing intermittent `/en` hydration mismatch (emotion mui-rtl-global style-tag position race) is a REGISTERED residual risk (worklog C2-QA + C3-audit) — **not re-reported** as new per instructions; none of the reviewed diffs introduces any hydration-sensitive rendering (see §6 below).

---

## Per-checklist findings

### 1. MUI v9 compliance — PASS
- Zero hardcoded hex/rgb/named colors in NEW code. All color derivation rides theme tokens: `ApiStatusIndicator.tsx:72-81` (`--mui-palette-onPrimary/success-main/warning-main`), `:172-212` (theme-callback sx with `theme.palette.primary.light` / `common.white` inside `color-mix()`), `:200` (secondary-main focus ring). `"transparent"` appears only as a `color-mix` stop, mirroring the sanctioned SiteFooter social-icon precedent.
- Layout/style props correctly inside `sx` (MUI v9 direct-prop removals): `ApiStatusIndicator.tsx:236-247` (`component="span"` + all styles in sx), `SiteFooter.tsx:153-157` Stack layout props in sx, `SiteFooter.tsx:236` Typography caption styled via sx. No `fontWeight`/`alignItems` direct props anywhere in the delta.
- `@keyframes` embedded directly in sx objects (`ApiStatusIndicator.tsx:208-212`; `GraphQLErrorSurfaceHost.tsx` diff hunks) — matches AGENTS.md "New MUI v9 Patterns".
- `<Box component="output">` used instead of `role="status"` per AGENTS.md oxlint `prefer-tag-over-role` pattern.
- Note (PRE-EXISTING, untouched): `SiteFooter.tsx:118` `boxShadow: rgba(184,115,51,0.15)` is unchanged context from before `12120dd`; not attributable to this range.
- RTL-safety: chip builds layout from flex/gap and a symmetric `inset:-8` pseudo-element (`ApiStatusIndicator.tsx:194-198`) — direction-safe; version badge pins `direction:"ltr"` (`:256`) which is CORRECT for Arabic-first (semver glyphs stay LTR inside RTL text); site-footer hairline added this range uses logical `insetInlineStart/End` (`SiteFooter.tsx:128-129`).

### 2. Apollo hook patterns / typePolicy correctness — PASS
- Schema ground truth: `frontend/graphql/generated/schema.graphql:7-12` — `HealthCheck { service, status, timestamp, version }` is scalar-only with NO `id`, exposed solely via `Query._health: HealthCheck!` (`:53`). `keyFields:false` (`apolloCache.ts:36-38`) is exactly the sanctioned opt-out of `frontend/graphql/AGENTS.md` §Embedded type normalization policy, slotted alphabetically between `AdminNoteInfo`/`OnlineMeetingInfo` precedents with REQ-061/D4 provenance comment.
- Behavioral pairing is locked by `apolloCache.test.ts`: inline embedding (no `HealthCheck:` entity id after `writeQuery`, `:171-185`) and loss-safe rewrite (`:187-208`); config-exposure gate reads `cache.config.typePolicies` through cast-free guarded reflection, with the Apollo-v4 lazy-normalization rationale documented in-file (`:45-52`) so future agents don't chase `policies.typePolicies`.
- No GraphQL document consumer exists yet (REST probe is fetched via plain relative `fetch`, not Apollo) ⇒ BLT-03 deferral decision-of-record stands (worklog C2-phase4a); the policy was still landed per D4-MANDATORY. Consistent.

### 3. Zustand patterns — N/A
No store usage in any scope file (grep `zustand` over `ApiStatusIndicator.tsx`, `SiteFooter.tsx`, `apolloCache.ts` = 0 hits; locale resolves via LocaleProvider context + compile-time translation). Nothing to assess.

### 4. Theme compliance — PASS
- Dark/light safety: every token is scheme-aware (`success.main`, `warning.main`, `secondary-main`, `primary.light`) except the white hairlines over the permanently midnight-blue footer, which intentionally mirror the pre-existing social-icon treatment. Footer is a fixed-dark surface in both schemes, so the translucent `color-mix` pill holds contrast in either scheme.
- Focus rings: chip `&:focus-visible` outline 2px secondary-main + offset 2 (`ApiStatusIndicator.tsx:199-202`); parallel-range additions to SocialIcon/FooterLink add identical keyboard-parity rings (`SiteFooter.tsx:36-39, 292-296` diff).
- Reduced motion: pulse animation ONLY fires under `@media (prefers-reduced-motion: no-preference)` (`ApiStatusIndicator.tsx:232-233`); static glow/opacity transitions remain but are non-positional. `GraphQLErrorSurfaceHost.tsx` entrances (toast `ghToastIn`, banner `ghBannerIn`) gated identically behind `no-preference`, and use vertical `translateY` + scale so they are RTL-mirror-safe.

### 5. Component patterns — PASS
- Unmount cleanup: full teardown kit `disposed` flag + `controller.abort()` + listener removal + `clearTimeout` (`ApiStatusIndicator.tsx:146-151`); post-teardown fetch responses resolve into guarded no-op branches (`:103`, `:118`). Dedicated regression tests pin no-poll-after-unmount (`api-status-indicator.test.tsx:149-162`) and hidden-tab freeze/resume (`:130-147`).
- Visibility pause/resume logic verified sound by reading: in-flight poll finishing while hidden does not re-arm (`:123` returns before scheduling); becoming visible schedules an immediate `setTimeout(…, 0)` poll when no timer is pending (`:140`); double-toggle races cannot stack timers (`timer !== null` guards both sites).
- aria-live politeness: `<output>` carries implicit role "status" AND explicit `aria-live="polite"` (`ApiStatusIndicator.tsx:167-169`); animated dot `aria-hidden="true"` (`:216`); ≥44px invisible symmetric touch-target via `::after` inset (`:192-198`). A11y assertions present in the suite (`:84-89`).
- i18n: exactly 4 new keys ×3 sites (`footerStatusLabel/Checking/Operational/Offline`) — ar (`ar/landing/index.ts:257-261`), en (`en/landing/index.ts:270-274`), types (`types/landing/index.ts:251-255`); both locale consts typed `LandingLabels`, and `bun tsgo` green ⇒ no key used-but-missing and no untyped hole. Consumers reference keys only through `useAppTranslation(Landing)`; all strings asserted through `Landing.getLabels(getTranslations(locale))` in tests (zero hardcoded copy).
- Endpoint contract match: chip parses `{ data: { status, version }, requestId }` assertion-free (`readStringSlot/readObjectSlot`, `:57-68`, `:100-112`); producer `app/api/health/route.ts:38-42` emits exactly that envelope via `apiSuccessResponse` (confirmed shape at `backend/lib/api/api-response.ts:164-170`: body literally `{ data, requestId }`). Non-200 ⇒ degraded regardless of body (`route.ts` GET-only + `response.ok &&` conjunction at `:102`) — matches REQ-061 posture.
- `/api/health` itself: single-expression composition, no secrets/tenancy in payload (four machine fields), honors inbound `X-Request-Id` once via `resolveRequestId(request.headers)` — consistent with D2/D3/D4 and REQ-002's i18n exemption recorded at baseline §"REQ-002 exemption".

### 6. React correctness — PASS
- Hydration safety: initial render state is the constant `INITIAL_STATE` (`checking`, null extras) on BOTH server and client; all network/payload-driven output (`version` span, tooltip requestId, dot color) originates inside effects after mount — no `Date.now()`, no `Math.random()`, no environment-dependent render output in any scope file (grep sweep = 0 hits). The snapshot proves the SSR-safe pinned markup.
- Dep arrays: effect deps `[pollIntervalMs]` are exhaustive — every value captured (`setStatus`, refs, helpers) is stable/effect-local; cadence threading as a prop makes the interval visible to exhaustive-deps linting (worklog-noted; verified in source).
- Stale closures: none — `disposed`/`timer`/`consecutiveFailures` live in the per-effect closure; handlers reference only those + prop constants. Test #5 proves timers die at teardown even after successful polls.

---

## Findings register

**Verdict: ZERO blocking findings. ZERO CRITICAL/HIGH/MEDIUM findings. Feature-specific findings: ZERO.**
Only observational records below, all NEW unless tagged otherwise, all NON-BLOCKING (no ❌ rows proposed):

| # | Severity | Blocking? | Finding |
|---|---|---|---|
| R-1 | INFO | No | **Review-runner wiring for `.tsx` component suites.** Invoking the two UI suites through the generic protocol runner `bun run test/scripts/run-test.ts test/ui/components/{api-status-indicator.test.tsx,site-footer.test.tsx}` fails 0/5 and 0/2 with `ReferenceError: document is not defined`, because happy-dom/translation/next-dynamic preloads are wired ONLY in `package.json:33` `test:ui:components` (rationale comment `bunfig.toml:22-26` — Bun honors a single preload arg, so the general `[test] preload` can't include them without breaking DB tiers). The dedicated script passes fully (below). Harmless today since `test:ui:components` exists, but the divergence vs tasks.md protocol item #3 ("tests run ONLY via run-test.ts") should be worded deliberately at Task 7.x close-out so CI/dev-docs name the sanctioned command for tsx tiers. Evidence: capture in §Verification below. Owner: 7.1 docs propagation (cosmetic). |
| R-2 | LOW | No | **REQ-072 gate not independently re-runnable in-sandbox right now.** `run-test.ts frontend/graphql/test/gateway/allowlist-coverage.test.ts` neither passes nor crashes fast anymore: it HANGS until timeout (>120 s, no test output) through its delegated `run-server-tests.ts` branch while `:3000` lives — same family as the 5.2-recorded UMD/env wall (outcome/5.2-outcome.md §TE rows 2–3, which PROVED the shipped bytes green 8 pass / 0 fail / 42 expects via verbatim-copy at the unit tier). Static review of the 233-line suite found no defects (guarded reflection throughout, negative fixtures prove detector teeth, no server boot in-file). Owner: test-harness stream (BLT-07-class carry-forward already ledgered in 5.2). Not counted against dev3-003 code. |
| R-3 | INFO | No | Copy/bidi micro-nits, quality bar only: (a) `tooltipTitle` concatenates localized Arabic label + LTR uuid around a neutral "·" separator (`ApiStatusIndicator.tsx:161-162`) — first-strong bidi renders acceptably today; if RTL QA ever flags the tooltip, wrap the uuid in directional isolates (`\u2066…\u2069`). (b) EN uses typographic ellipsis "Checking…" while AR uses three dots "جارٍ التحقق..." — purely stylistic asymmetry; parity gates enforce keys, not glyphs. |

Explicitly **NOT re-reported** (registered residuals, pre-existing): `/en` intermittent hydration mismatch (emotion mui-rtl global position race); `translateX(3px)` hover shift in RTL on pre-existing `FooterLink` (`SiteFooter.tsx:289` — untouched by this diff); `rgba(184,115,51,…)` inset shadow (`SiteFooter.tsx:118` — unchanged context); pre-existing copyright year literal.

---

## Verification record (commands + literals)

| Command | Result |
|---|---|
| `bun run test/scripts/run-test.ts frontend/providers/apollo/apolloCache.test.ts` | ✅ **7 pass / 0 fail / 14 expect() calls** (215 ms) |
| `bun run test/scripts/run-test.ts shared/locale/errors-namespace.parity.test.ts` | ✅ **8 pass / 0 fail / 108 expect() calls** (112 ms) |
| `bun run test:ui:components` (sanctioned harness incl. `api-status-indicator.test.tsx` + `site-footer.test.tsx` + snap) | ✅ **19 pass / 0 fail / 1 snapshot / 71 expect() calls** across 4 files (6.31 s) |
| `bun run test/scripts/run-test.ts test/ui/components/{api-status-indicator.test.tsx,site-footer.test.tsx}` | ⚠️ 0/5 + 0/2 `ReferenceError: document is not defined` — preload wiring, see R-1 (superseded by the dedicated-script result above) |
| `bun tsgo` then `grep -c "error TS"` | ✅ **0** |
| `bun biome:check` | ✅ exit 0 — literal "**Checked 477 files in 7s. No fixes applied.**" |
| `git status --porcelain` (post-review) | clean of unintended edits (review touched no source files) |
| Forbidden-pattern scan over scope components (`console.` / `Date.now` / `Math.random` / `as any` / `: any`) | ✅ 0 hits |
| Live wire probe on held `:3000` | NOT repeated — backend/pentest waves own HTTP-tier proof; frontend claims rest on envelope-contract reading + suite evidence |
| allowlist-coverage via generic runner | ⚠️ hang >120 s → see R-2 (content previously proven green, 5.2 §TE) |

Worklog/diff cross-check: git history contains exactly the two expected feature commits — `8dfa4fa` (Phase 4 + 5.2: cache policy, parity gate, allowlist coverage) and `0a85bcd` (live API status indicator + detail polish) — matching worklog entries C2-phase4a and C2-feature-status; nothing else touches the frontend scope set.

---

## Carry-forward / next actions

1. Task 7.x owner: settle runner-command wording for `.tsx` component tiers in canonical docs (R-1) and keep BLT-07-family ownership for the delegated-chain hang (R-2).
2. First real GraphQL consumer of `_health` must land `healthCheckQueryDocument` per the preserved creation checklist (BLT-03 decision-of-record) — cache side already normalized (keyFields:false frozen by test).
3. No code changes requested from this wave; resumption of plan close-out (7.1–7.3) may proceed.
