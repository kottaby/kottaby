# Task 6.1 — Review Wave: review-types Outcome (dev3-003)

- **Task ID:** C3-6.1
- **Agent:** review-types subagent (Post-Implementation Review Wave, skill §review-types lens)
- **Date:** 2026-08-28 (UTC session)
- **Plan:** `ai/plans/sprint_0/dev3-003-api-gateway-routing-skeleton` · branch `main`
- **Diff basis:** `12120dd..HEAD` (baseline frozen per `outcome/phase0-baseline-outcome.md`: tsgo=0 errors, biome=0 diagnostics @ `12120dd`)
- **Mode:** READ-ONLY. Source tree untouched; this outcome file is the sole artifact written. Dev server `:3000` never probed/killed/restarted; `bun run build` NOT run.
- **Pre-reads honored:** `worklog.md` entries C1-gate → C3-audit (build context incl. Phase 2/3 constant-unification facts), `phase0-baseline-outcome.md`, `.agents/skills/spec-implementation/SKILL.md` §Post-Implementation Review Wave, `backend/types/AGENTS.md`, plan `tasks.md` §6.1 scope row, sibling precedent `backend/types/errors/api-error.types.ts`.

---

## Scope reviewed (type-lens, diff `12120dd..HEAD`)

| File | Status | Note |
|---|---|---|
| `backend/types/gateway/gateway-context.types.ts` | NEW | `GatewayRequestMetadata`, `TransportErrorKind`, `TransportGuardResult` |
| `backend/types/gateway/health-check.types.ts` | NEW | `HealthCheckReturnType` |
| `backend/types/gateway/index.ts` | NEW | domain barrel |
| `backend/types/index.ts` | MODIFIED | root barrel wiring (+`./gateway`) |
| `shared/locale/types/landing/index.ts` | MODIFIED | `LandingLabels` += 4 footer-status keys |
| `backend/graphql/gqlContextFactory.ts` (type surface) | MODIFIED | `GatewayRequestMetadata` extension: `ctx.idempotencyKey?: string \| null` |
| `backend/lib/gateway/version.ts` | NEW | `resolveAppVersion()` |
| `backend/lib/gateway/route-inventory.ts` | NEW | `RouteClassification`, `RouteInventoryEntry`, `ROUTE_INVENTORY` |
| `backend/lib/gateway/transport-guard.ts` | NEW | guards + `MAX_GRAPHQL_BODY_BYTES` |
| `backend/lib/gateway/public-operations.ts` | NEW | allowlist tuple/set/type-predicate |
| `backend/lib/gateway/index.ts` | NEW | lib barrel |
| Direct consumers examined for cross-checks | — | `app/api/graphql/route.ts`, `backend/services/gateway/health-check.service.ts`, `backend/graphql/pothos/shared/health.pothos.ts`, `backend/graphql/query/health.query.ts`, `backend/graphql/test/{context-keys,schema-surface}.test.ts`, `frontend/graphql/test/gateway/allowlist-coverage.test.ts`, `shared/locale/{ar,en}/landing/index.ts` |

---

## Checklist findings (skill §review-types)

### 1. Canonical type naming — PASS

- **`{Symbol}ReturnType` convention honored.** `health-check.types.ts:31` declares `HealthCheckReturnType`; docblock (:11–12) explicitly cites the dev3-002 `{Symbol}ReturnType` precedent in `backend/types/errors/` — verified against sibling `api-error.types.ts` (`ApiErrorEnvelopeReturnType`/`ApiSuccessEnvelopeReturnType`/`ApiFieldErrorType`). Deliberately NO `Schema` suffix (not a DB table type) — conforms.
- **`.types.ts` location rule honored** (`backend/types/AGENTS.md` "All .types.ts files MUST live in `backend/types/`"): both new contract modules live in `backend/types/gateway/`; no service-layer or Pothos-side `.types.ts` was created (Pothos object backs onto the canonical ref at `health.pothos.ts:26-28`).
- **Barrel hygiene per AGENTS.md:** `backend/types/gateway/index.ts` and root `backend/types/index.ts` use ONLY `./`-relative `export *` statements (root barrel delta = exactly +`export * from "./gateway"` at :30, alphabetically slotted, plus an updated inventory docblock listing the new non-entity `gateway` subdirectory). `backend/lib/gateway/index.ts` matches the lib-barrel style (`./`-relative `export *` exclusively).
- **Docblock density/quality** on every new shape states layer rules (types-only, readonly, union-vs-enum rationale, i18n exemption REQ-002, Apollo `keyFields:false` pairing D4/REQ-061) — consistent with the `errors/` precedent house style.

### 2. No duplicate type definitions — PASS

Repo-wide grep (`backend/**, app/**, shared/**, frontend/**` excluding generated/plan docs) for each exported name found EXACTLY ONE declaring site each:

| Symbol | Sole definition |
|---|---|
| `GatewayRequestMetadata` | `gateway-context.types.ts:30` |
| `TransportErrorKind` | `gateway-context.types.ts:51` |
| `TransportGuardResult` | `gateway-context.types.ts:69` |
| `HealthCheckReturnType` | `health-check.types.ts:31` |
| `RouteClassification` / `RouteInventoryEntry` | `route-inventory.ts:40/:43` |
| `PublicOperationName` / `PUBLIC_OPERATION_NAMES` / `PUBLIC_OPERATIONS` | `public-operations.ts:37/:52/:60` |

- Pothos/query tier IMPORTS shapes instead of re-declaring: `health.pothos.ts:26` `import type { HealthCheckReturnType } from "@/backend/types"` → `objectRef<HealthCheckReturnType>` (:28), no local type literal; `health.query.ts` consumes the pothos object ref only. NO ad-hoc duplicated payload interface exists anywhere in GraphQL land.
- Consumers agree via single-source derivation: `route-inventory.test.ts:22` imports `type RouteClassification`; `public-operations.test.ts:24` imports `type PublicOperationName`; nobody re-states the unions. The route-side wire table is keyed exhaustively off the canonical union (`route.ts:55 Record<TransportErrorKind, TransportWireSpec>`) so union drift would fail compile.
- Lib-layer runtime types (`RouteClassification`, `RouteInventoryEntry`, `PublicOperationName`) live beside their runtime registries in `backend/lib/gateway/*.ts` — assessed acceptable: they are derivations OF runtime values (`as const` tuples / literal registries), not entity contracts; plan/tasks explicitly own this placement (tasks.md §2.2), and nothing in `AGENTS.md` forbids value-derived types inside the lib module itself.

### 3. Import path consistency — PASS

- All type-bearing consumers import through the mandated barrel form `@/backend/types` / `@/backend/lib/gateway`: `transport-guard.ts:34`, `route.ts:12/:15`, `health-check.service.ts:23-24`, `health.pothos.ts:25-26`, `static-assertions.test.ts:46`, `schema-surface.test.ts:50`, `allowlist-coverage.test.ts:58`.
- Barrels themselves use `./`-relative paths exactly as `backend/types/AGENTS.md` mandates (no `@/` inside barrels, no `../` traversal, no explicit per-export `export type {}` blocks).
- Machine check: over ALL 31 changed `.ts` files in `12120dd..HEAD` (incl. tests), a parent-relative-import scan returned ZERO hits — no relative import crosses a directory boundary anywhere in the feature delta.

### 4. Enum usage — PASS

- Zero enums introduced, matching plan §2.3 "Enums: None": grep for `\benum\b` across `backend/types/gateway` + `backend/lib/gateway` hits ONLY docblock prose explaining WHY `TransportErrorKind` stays a TS string union (D3 precedent — transport metadata never persisted, never a pgEnum/Pothos enum). `TransportErrorKind` shipped as a 4-member union (`gateway-context.types.ts:51-55`); no `values: [` literal-array registration exists (A2 negative scanner stands guard).
- Value-vs-type import discipline correct under `isolatedModules: true` (tsconfig.json:13):
  - Purely-type sites use `import type` / inline `type`: `transport-guard.ts:34`, `route.ts:15` (`TransportErrorKind`), `route.ts:6` (`type Context` inline), `gqlContextFactory.ts:24/:30/:32`, `health-check.service.ts:24`, `health.pothos.ts:26`, tests (`type RouteClassification`, `type PublicOperationName`).
  - Value imports are genuinely value-used: `resolveAppVersion` (called at `health-check.service.ts:39`), `guardTransport` (`route.ts:329`), `UserRole`/`toUserRole` (`gqlContextFactory.ts:26` compared as value at :223 / called at :210 — plain import CORRECT because the enum doubles as a type positionally at :83), `ROUTE_INVENTORY`, `PUBLIC_OPERATIONS`.

### 5. Type quality — PASS (2 LOW notes)

- **No `any` leaks:** machine scan over all scope files for `as any` / bare `\bany\b` / `@ts-ignore|@ts-expect-error` / `!.`-style assertions → ZERO hits outside docblock prose. Success-arm body is deliberately `unknown` forcing downstream narrowing (`TransportGuardResult.ok:true.body`, `gateway-context.types.ts:74`) — strictness-positive design; consumed safely (`route.ts:342` re-stringifies without casting).
- **No unsafe non-null assertions** in any scope file.
- **Optional-in-type-only pattern DOCUMENTED** (checklist exemplar): `Context.idempotencyKey?: string | null` (`gqlContextFactory.ts:73`) carries a full docblock (:56–72) stating the rationale (pre-existing Context-shaped fixtures stay compile-clean) and guaranteeing the factory ALWAYS materializes it at runtime (capture :170, return :218, null-when-absent, never empty-string-coalesced). Compliant pattern-as-documented.
- **Readonly everywhere:** every property of `GatewayRequestMetadata`, `HealthCheckReturnType`, `RouteInventoryEntry`, `LandingLabels` additions, and both arms of `TransportGuardResult` is `readonly`; registry objects are frozen/tuple/`readonly`-typed (`ROUTE_INVENTORY: readonly RouteInventoryEntry[]` + `as const`, `PUBLIC_OPERATION_NAMES ... as const`, `PUBLIC_OPERATIONS: ReadonlySet<string>`).
- **Zero-runtime-export discipline structurally intact:** both `.types.ts` files contain ZERO import statements and only interfaces/type aliases (stronger than the A5 requirement); the enforcing scanner (`scanRuntimeExports` + A5 import-origin scanner, `static-assertions.test.ts:110–120`) is present in-tree and its docblock pins the constraint.
- **Dead exports:** full consumption matrix compiled (§2 table): every exported symbol except one has ≥1 code consumer beyond its own module/tests. Exception noted below as F2.

---

## Verdict table

| ID | Severity | Blocking? | NEW vs PRE-EXISTING | Finding |
|---|---|---|---|---|
| F1 | LOW | **Non-blocking** (doc-drift only) | NEW (this plan) | Stale forward-looking docblock on `MAX_GRAPHQL_BODY_BYTES`: `transport-guard.ts:18-19` ("the LIVE route block KEEPS its inline `GRAPHQL_MAX_BODY_BYTES` until Task 3.2 moves the block") and `:45-47` ("byte-identical twin of the still-live inline …"). Task 3.2 HAS completed and removed the inline constant (verified: zero `GRAPHQL_MAX_BODY_BYTES` hits in backend/app sources; `route.ts:12` imports `guardTransport`; exactly ONE canonical constant ships). The frozen invariant held; only the two prose paragraphs describe a superseded transitional state. Suggest a fix-stream touch-up: reword both paragraphs to past tense ("removed by Task 3.2; route consumes this module via `guardTransport`"). No code/type change implied. |
| F2 | LOW | **Non-blocking** (by-design, plan-mandated) | NEW (this plan) | `GatewayRequestMetadata` (`gateway-context.types.ts:30`) is exported but has zero importers in code — referenced only in docblock prose (`gqlContextFactory.ts:63`, `context-keys.test.ts:22`). This is EXPLICITLY the intended role: tasks.md §1.1 defines it as a "documentary contract over context, no runtime construction", and the file docblock says no construction site may exist. Recorded here so future dead-export sweeps don't misfire; optionally the A5-style assertion could pin its documentary status executably (owner: none needed). |

**No CRITICAL / HIGH / MEDIUM findings. Zero blocking findings.**

Pre-existing conditions checked and filtered out (per baseline discipline): none observed in scope — both new `.types.ts` files, all five lib modules, and both barrels are entirely NEW files, so every characteristic above is attributable to dev3-003; `gqlContextFactory.ts` deltas were reviewed hunk-by-hunk against R1 ground-truth #8 (pre-existing `ctx.requestId` landed pre-plan) and Correction #7 (idempotencyKey-only extension) — the extension matches its sanctioned minimal-diff shape.

---

## Verification record

| # | Command | Result |
|---|---|---|
| 1 | `bun tsgo 2>&1 \| grep -c "error TS"` | **0** (baseline parity: 0) |
| 2 | `bun run test/scripts/run-test.ts backend/graphql/test/context-keys.test.ts` | **14 pass / 0 fail / 61 expects** |
| 3 | `bun run test/scripts/run-test.ts backend/graphql/test/schema-surface.test.ts` | **12 pass / 0 fail / 41 expects** (includes codegen byte-sync pin + `_health`↔allowlist agreement consuming the reviewed types) |
| 4 | Parent-relative import scan across all 31 changed `.ts` files | 0 hits |
| 5 | Duplicate-definition grep (7 symbol names, repo-wide `*.{ts,tsx}`) | 1 declaring site each |
| 6 | `any`/`@ts-`/non-null-assertion scan over scope files | 0 hits (outside docblock prose) |
| 7 | `\benum\b` scan over new gateway dirs | prose-only (union-not-enum explanations) |

Hard-rule compliance: no source file modified, dev server untouched, `bun run build` skipped — read-only greps/reads + the three sanctioned verification runs above.

---

## Wave routing

- Both findings are doc-level and routed ⚠️→fix-now-optional: F1 belongs naturally to Task 7.1 canonical-doc pass or any follow-up commit touching `transport-guard.ts` prose (one-comment change, zero behavior risk). F2 requires no action unless a future automated dead-export gate is introduced, where its documentary designation must be whitelisted.
- Handoff to sibling waves: locale landing-type symmetry (interface ≡ ar ≡ en, compile-enforced) confirmed — frontend reviewer may rely on `footerStatus*` keys being typed and present in BOTH locales; backend reviewer can assume single-sourced transport taxonomy (compile-exhaustive `Record<TransportErrorKind, …>` map).
