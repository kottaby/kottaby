# 0.1 Outcome — Record Error Baseline & Initialize Deferred-Items Ledger (dev3-003)

- **Date:** 2026-08-27 · **Commit:** `12120dd`
- **Method:** Full machine-literal capture executed this task; authoritative record lives in **`outcome/phase0-baseline-outcome.md`** — see its sections: "Baseline command results" (counts table @ `12120dd`), "Deferred-items ledger initial state" (seed + gate corrections), "Sandbox adaptations", and "REQ-002 health-payload i18n exemption + .BF/.BS N/A determination".

## Summary
| Baseline file | Exit | Literal result |
|---|---|---|
| `baseline/tsgo.txt` | 0 | `error TS` count = 0 |
| `baseline/biome.txt` | 0 | Checked 450 files — No fixes applied, 0 diagnostics |
| `baseline/lint.json` | 1 | env-limited (BLT-07-class OOM) → fallback `baseline/oxlint.txt` (also env-limited via tsgolint SIGKILL; verdict recorded in-file; biome-only lint signal) |
| `baseline/preexisting-modified-files.txt` | 0 | empty set (0 tracked files dirty) |
| `baseline/dbml.txt` | 0 | 22 tables, 15 enums PASS (+ renderer 1.0.31 sanity) |

## Ledger
`deferred-items.md` initialized from existing file (structure preserved). Rows BLT-01…BLT-06 seeded w/ owners, **zero ❌**. Gate corrections applied: phantom pre-seeds #4 (`/api/logs`) / #5 (`/api/cron/*`) DROPPED per R1 §F6 (renumber-free note in ledger); #6 set-locale → **✅ Done already-adopted reference row** (BLT-04); NEW BLT-06 pins the HIGH-F1 `_health` placeholder delete-before-register precondition owned by Task 3.1 itself.

## Required records made here by reference
1. REQ-002 health-payload i18n exemption (operator-facing machine constants; exempt from ar/en parity).
2. Phase-4 `.BF`/`.BS` N/A determination (plan §5 defines zero UI surfaces).

_Requirements satisfied: REQ-001, REQ-002, REQ-035, REQ-062, REQ-082, REQ-083 (pre-seed discipline)._ Sub-gates: SR ✅ (machine literals captured verbatim; template-conformant rows; no pre-seed row ❌) · OC ✅ (this stub + phase0 doc; boxes checked immediately after).
