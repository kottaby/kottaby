# Task 3.3 Outcome — `gqlContextFactory` Extended In Place (idempotencyKey)

- **Task ID:** C2-phase3b · dev3-003 Phase 3 · REQ-010 (step 4) / REQ-030 / REQ-031 / REQ-043; D10
- **Date:** 2026-08-27 · Base: HEAD `8e5ebb8` (same uncommitted-cycle WIP resume as 3.2 — the two tasks form one pipeline change-set)
- **Pre-reads honored:** worklog C1-gate→C2-phase3a (esp. R1 ground-truth row #8: `ctx.requestId` ALREADY landed ⇒ this task reduced to idempotencyKey ONLY); tasks.md §3.3 sub-gates + protocol §7; plan.md §4.1 factory row / §6.1–6.2 BOLA/BOPLA; plan-review-R1 Correction #7; backend/types/gateway/gateway-context.types.ts (`GatewayRequestMetadata` documentary contract — null-not-empty-string note); backend/graphql/AGENTS.md :135 context-anchor rule; docs/auth/jwt-authentication-service.md §"What NOT to Do" (no parallel auth helpers, no client-trusted role claims); docs/graphql/error-handling-contract.md unaffected.

---

## Files changed / NOT changed

| File | Action | Why |
|---|---|---|
| `backend/graphql/gqlContextFactory.ts` | MODIFIED (minimal) | +`idempotencyKey?` on the canonical `Context` interface; ONE capture line in `createGraphQLContext` (`request.headers.get("x-idempotency-key")` → `null` when absent, fetch-spec lookup is case-insensitive); +one returned-field slot. NO other behavior edits. |
| `backend/graphql/test/context-keys.test.ts` | CREATED | 3.3.TE paired suite — Tiers 1–4 + semantic-review pins (14 tests). |
| `ai/plans/sprint_0/dev3-003-api-gateway-routing-skeleton/outcome/3.3-outcome.md` | CREATED | This file. |

**NOT touched:** cookie matrix (`AUTH_COOKIE_NAMES`, `createAuthCookieOut`, `parseCookies` consumption verbatim), governance fail-closed path (verifyAccessToken → UserRepository.findById → anonymous fallthrough UNTOUCHED), refresh-rotation substrate (out of scope by construction), route.ts (consumes `ctx.idempotencyKey`; owns NO second capture), `backend/lib/**`, pothos tree, frontend.

## The minimal diff (contract summary)

- Type: `readonly idempotencyKey?: string | null` — optional IN THE TYPE only so pre-existing Context-shaped fixtures stay compile-clean; runtime ALWAYS materializes the field (present, with `null` for absent header — never `undefined`).
- Capture: exactly one site — inside `createGraphQLContext`, adjacent to the existing single `resolveRequestId` composition. Raw passthrough: absent → `null` (never empty-string-coalesced); present → verbatim (no trim/sanitize/classification policy here; duplicate-blocking/expiry semantics stay in the owning mutation's idempotency transaction per `docs/IDEMPOTENCY.md`).
- NON-AUTHORIZATION contract documented on the field itself (mirrors `GatewayRequestMetadata`): a client key can never grant/influence/substitute identity (REQ-030); context-whitelist member (REQ-031); D10/REQ-004 extend-in-place satisfied — no parallel helper, no fork.
- `requestId` NOT duplicated — pre-existing DEV3-002 lines consumed as-is.

## Verification results (commands + exit codes)

| Gate | Command | Exit | Result |
|---|---|---|---|
| QL × gqlContextFactory.ts | `bun run scripts/health/sub-loop.ts backend/graphql/gqlContextFactory.ts --lifecycle duplicates` | **0** | full five-stage lifecycle (real exit captured unpiped) |
| QL × context-keys.test.ts | same form | **0** | full five-stage lifecycle |
| TE | `bun run test/scripts/run-test.ts backend/graphql/test/context-keys.test.ts` | **0** | **14 pass / 0 fail**, 61 expects |
| Project TS | `bun tsgo` + `grep -c "error TS"` | **0 / 0** | zero errors project-wide (= baseline +0) |
| Project lint | `bun biome:check` | **0** | literal `Checked 470 files in 4s. No fixes applied.` |

### TE tier map vs tasks.md 3.3.TE (all green, 14 tests / 61 expects)

- **Tier 1 presence/absence:** present header crosses VERBATIM alongside its sibling requestId; absent header STILL materializes the field (`"idempotencyKey" in ctx === true`, value `null`, never `undefined`); case-insensitive transport lookup; requestId UUID-v4 contract untouched (structural RFC-4122 pin shared with request-id.test.ts).
- **Tier 2 null-not-"":** absent ⇒ exactly `null`, `.not.toBe("")`; interior-whitespace values propagate byte-exact; empty-string VALUE propagates verbatim (policy-free); edge-padding loss documented as fetch-spec OWS normalization ABOVE the factory layer.
- **Tier 3 concurrency isolation:** four parallel constructions with distinct pinned keys → keys map 1:1 to their own request, correlation ids pairwise unique + structurally valid, and every context's `authCookieOut` accumulator + cookies object are pairwise DISTINCT references (REQ-040 independence / REQ-074 support).
- **Tier 4 identity immunity:** across plain/oversized(1280 chars)/comma-collapsed/control-char key variants AND under a garbage Bearer credential, the identity snapshot (`user`,`safeUser`,`role`,`isSuperAdmin`,`permissions`) stays `.toEqual` the headerless baseline; elevated-looking key (`role=admin&grant=superuser`) moves nothing; verify-token failure falls through to anonymous — never a usable spoofed ctx (REQ-030 behavioral proof).

### DEV2-001 regression evidence (3.3.SEC requirement — pass counts via mandated runner)

| Suite (DEV2-001 surface exercised) | Result | Notes |
|---|---|---|
| `backend/graphql/test/request-id.test.ts` | **12 pass / 0 fail** | factory correlation-id contract (bounded inbound honor / UUID v4 mint) intact after edit. |
| `backend/graphql/test/context-keys.test.ts` Tier 4 rows | (within 14/0 above) | garbage-Bearer → anonymous fallthrough = DEV2-001 verify-failure semantics preserved. |
| `frontend/graphql/test/auth/auth.test.ts` | **ENV-BROKEN (pre-existing)** | dies at MODULE LOAD: `graphql-tag` UMD bundle issues `require()` of `graphql/index.mjs` → `TypeError: require() async module … unsupported` under bun 1.3.14 — crashes before ANY route/factory/auth code executes. Nothing in this change-set touches node_modules or frontend import graphs; failure class matches other pre-existing env-limits (repo-wide oxlint SIGKILL). Recorded honestly as an environment limitation blocking that integration file THIS CYCLE, not a code regression attributable to the diff (its beforeAll server boot would be env-locked anyway — see BLT-07). |
| Non-integration DEV2/DEV1 surfaces reachable without lifecycle boot | **green** | schema-surface 12/0 (real-schema scope stack builds; anonymous `_health` executes), error-finalizer 14/0 + error-masking 32/0 + errors-fields-contract 23/0 (masking/oracle-passthrough layers), api-response 39/0 (resolveRequestId/envelope helpers), set-locale-route 27/0 (envelope consumer routes). |

Cookie matrix & governance-fail-closed preservation evidence: the factory diff adds ONE pure variable + ONE returned field between the requestId resolution and translations preload — zero statements removed or reordered around `verifyAccessToken` / `UserRepository.findById` / `authCookieOut`; regression table above covers every runnable suite touching those paths.

**No DEV2-001 substrate defect discovered** in the auth/context path itself ⇒ NO ❌ row against the auth stream per protocol §7. The one defect DISCOVERED this cycle lives in the TEST HARNESS liveness probe, not the auth substrate; it is recorded as **BLT-07** in deferred-items.md with owning-stream assignment (test-harness / Task 5.x precondition).

### Semantic-review grep evidence (REQ-004/D10 single-source)

Executable pins live in context-keys.test.ts ("factory semantic-review pins"):
- `'headers.get("x-idempotency-key")'` occurs EXACTLY once — in gqlContextFactory.ts (comment-stripped code view);
- ZERO occurrences anywhere in app/api/graphql/route.ts (no parallel capture/helper);
- `resolveRequestId(` exactly once in the factory; zero `randomUUID` in the factory (mint delegated to the lib helper);
- authCookieOut/cookies/verifyAccessToken/UserRepository.findById substrate imports unchanged.

## Cross-file dependencies discovered

- Route step-order coupling: 3.2's POST docblock pins "capture realized at the factory (ONE site)" — future movers of that capture must update both docblocks plus the executable pins.
- `GatewayRequestMetadata` remains DOCUMENTARY-only (A5 zero-runtime-export discipline held); the live construction site is this factory alone.

## Carry-forward knowledge for downstream tasks

1. Consumers wanting the key today: read `ctx.idempotencyKey ?? null` — treat "" as a legal client value (propagation-only; classification policy belongs to consuming mutation services per docs/IDEMPOTENCY.md).
2. Harness blockers (BLT-07 + dev-singleton lock) make ALL `setupTestServerLifecycle` suites env-inoperable while :3000 runs — Task 5.1's REQ-071 matrix must land with BLT-07 resolved (probe shape `{ _health { status } }`) or an out-of-band lifecycle start point.
3. Sub-loop exit codes must be captured unpiped (pipeline `$?` masks failures) — cost a fix loop this cycle; pattern recorded in 3.2-outcome carry-forward #3.

## Sub-gate rollup

- **3.3.QL** ✅ — lifecycle duplicates exit 0 on gqlContextFactory.ts + the new test file.
- **3.3.TE** ✅ — Tier 1–4 suite 14/0 as mapped above.
- **3.3.SEC** ✅ — additive-context-fields-only deviation risk accepted: governance fail-closed, 401-vs-403 semantics, cookie sizing matrix untouched; DEV2-001 regression table above (pass counts); identity-immunity proof executable.
- **3.3.SR** ✅ — single source of truth preserved; capture-site-once grep/executable-pin evidence; no fork, no helper module.
- **3.3.IV** ✅ — backend/graphql/AGENTS.md context-anchor rule honored (single resolution anchor extended, not duplicated); jwt-authentication-service.md "What NOT to Do": no parallel auth helpers (extended the sanctioned factory in place), no client-supplied role trust (Tier 4 proves headers can't move role), tokens/credentials never logged (nothing new logs anything).
- **3.3.OC** ✅ — this file with regression evidence; checkbox flipped below.
