# Task 4.2 Outcome — i18n Transport-Message Key Verification (ar + en parity)

- **Task ID:** C2-phase4a · dev3-003 Phase 4 · REQ-002, REQ-051
- **Date:** 2026-08-27 · Base: HEAD `4d41c8e` (Phase 3 landed + pushed)
- **Pre-reads honored:** worklog C1-gate…C2-phase3c; tasks.md §Phase 4 (all 4.2 sub-gates) + Non-Negotiable Protocol; plan.md REQ-051 mapping row + D-decisions touching transport; outcome/3.2-outcome notes via C2-phase3b worklog entry (message keys `badRequest` chosen as canonical for the whole transport matrix); shared/locale/AGENTS.md (compile-time system, leaf-module rules, path alias discipline); shared/Messages_README.md parity law ("Both locales must have identical key structures — enforced by MessageSchema typing"); app/api/graphql/route.ts ground truth.

---

## 1. ENUMERATION — every message key the transport layer emits

Grep of `app/api/graphql/route.ts` (the seven-step pipeline from Task 3.2/3.3) for every message-key consumption site. The route accesses the compile-time `errors` namespace through **`getServerTranslations(locale).errorsTranslations.<key>`** in two realization forms — direct chain and held-variable (`const t = …errorsTranslations;`):

| # | Site | Emitter | Key | Status `ar` | Status `en` |
|---|---|---|---|---|---|
| 1 | `transportRejectionResponse()` :77 | step-1 guard rejections → 400/405/413 envelopes (`METHOD_NOT_ALLOWED`, `UNSUPPORTED_CONTENT_TYPE`, `PAYLOAD_TOO_LARGE`, `MALFORMED_JSON`) — locale = `extractLocale(request)` | `badRequest` | ✅ present | ✅ present |
| 2 | `withRateLimit()` :233–238 | 429 limiter branch (fail-closed envelope, RATE_LIMIT_EXCEEDED family) — hardcoded `"en"` fallback per pre-existing design | `rateLimitExceeded` | ✅ present | ✅ present |
| 3 | `POST()` catch :353–358 | post-engine infrastructure fault → masked INTERNAL_SERVER_ERROR envelope — hardcoded `"en"` fallback | `internalServerError` | ✅ present | ✅ present |

**Files listing (all three keys exist in ALL of these):**
- Type source: `shared/locale/types/errors/index.ts` (`ErrorsLabels` interface — the compile-time schema both leaves must satisfy)
- ar source: `shared/locale/ar/errors/index.ts` (`errorsAr: ErrorsLabels`)
- en source: `shared/locale/en/errors/index.ts` (`errorsEn: ErrorsLabels`)
- Composition: `shared/locale/ar/messages.ts` / `en/messages.ts` typed against `Translations` (MessageSchema) — the PARITY GATE itself is TypeScript: a key added to one interface but missing from either leaf fails `bun tsgo` immediately.

**Result: ZERO keys missing ⇒ NO edits to any locale file were needed.** The canonical single-key posture (`badRequest` reused across all four transport kinds) was Phase 3's deliberate choice ("no new keys needed; dedicated 405/413 copy may land with Task 4.2's key verification") — decision CONFIRMED here: reusable generic copy is correct for gateway-local validation failures (REQ-051's own wording lists them under one code family); no dead near-duplicate keys minted.

Design note on form-2 sites: the `"en"` hardcoding at the limiter/catch sites predates this ticket (DEV1-002 era), has no request-bound locale available at those points, and is out of this task's sanctioned surface (route.ts read-only). Recorded as an observation for the eventual error-stream polish ticket; not a defect against REQ-051 (both ar+en implementations EXIST; degradation is graceful).

## 2. REQ-002 exemption confirmed (documented; zero health keys)

- `_health` payload constants (`status:"ok"`, `"kottaby"`, version, ISO timestamp) are operator machine constants and i18n-EXEMPT by spec (REQ-012's "operator-facing … i18n-exempt per REQ-002", re-affirmed by specs REQ-012 row).
- Documentation-of-record lives where the surfaces are registered: `backend/lib/gateway/public-operations.ts:27–29` — "`_health` → LB/CI probe object payload — operator-facing machine constants only (**REQ-002 exemption**)".
- Executably pinned by the NEW suite below: NEITHER locale's errors map may EVER grow a health-flavored key (currently zero; the test fails if someone "translates" the payload).

## 3. Import hygiene (touched surface)

Grep evidence over `app/api/graphql/route.ts` (+ neighboring touched-surface modules): `next-intl` — 0 hits · `getBackendTranslations` — 0 hits anywhere (symbol does not exist in tree) · `shared/messages/` — directory absent at repo root, 0 references. Sole consumer stays `getServerTranslations` from `@/shared/locale/server-graphql`. All four facts are ALSO executable pins inside the new suite (see #4) so they cannot silently regress.

## Files changed / NOT changed

| File | Action | Why |
|---|---|---|
| `shared/locale/errors-namespace.parity.test.ts` | CREATED | The 4.2.TE ar+en key-parity suite (8 tests / 108 expects, details below). Co-located with the namespace surface it guards, matching the backend colocated-test convention; zero shared/** test files existed before, so no convention conflict possible. |
| `ai/plans/sprint_0/dev3-003-api-gateway-routing-skeleton/outcome/4.2-outcome.md` | CREATED | This file. |
| tasks.md · main-line checkbox `- [ ] 4.2` → `[x]` | MODIFIED | Per protocol rule 6. |

**NOT changed:** `shared/locale/{types,ar,en}/errors/index.ts` and every other locale file (nothing was missing — enumeration above); `app/api/graphql/route.ts` (read-only evidence substrate for 4.x); backend/** read-only; zero generated artifacts affected (git clean there).

## Verification results (commands + exit codes)

| Gate | Command | Exit | Result |
|---|---|---|---|
| QL × parity suite | `bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates` | **0** | five-stage lifecycle green after fix loops: `sonarjs/concise-regex` (`[A-Za-z0-9_]`→`\w`) + `sonarjs/assertions-in-tests` (throwing-helper loops → explicit expect-wrapped calls) |
| Locale validation script | (none exists — see note) | n/a | package.json carries NO dedicated i18n script (`validate:*` = dbml/mermaid/docs-ci/seed-assets only). Per shared/locale conventions the COMPILE-TIME gate IS the validator: satisfied executably — **`bun tsgo` exit 0 with grep -c "error TS" = 0** proves both `errorsAr`/`errorsEn` still satisfy `ErrorsLabels` while route consumers type-check. Runtime belt = the new suite. |
| TE parity suite | `bun run test/scripts/run-test.ts shared/locale/errors-namespace.parity.test.ts` | **0** | **8 pass / 0 fail**, 108 expect() calls |
| TE envelope-rendering citation | `bun run test/scripts/run-test.ts app/api/graphql/test/graphql-route.transport.test.ts` | **0** | **19 pass / 0 fail** — its rows assert `item.message === getServerTranslations("en").errorsTranslations.badRequest` (:147) AND `=== …(“ar”)…badRequest` (:161) plus ct/size variants (:181/:205): the envelopes carry LOCALIZED values, never literals. Companion `graphql-route.pipeline-order.test.ts` re-run green too (9 pass / 0 fail). |
| Project TS | `bun tsgo` then grep -c "error TS" | **0** / **0** | zero errors project-wide |
| Project lint | `bun biome:check` | **0** | literal `Checked 477 files in 4s. No fixes applied.` |
| Post-gate tree | `git status --porcelain` | — | sanctioned set only (+ parallel cron-feature stream's pre-existing dirty files, untouched) |

## What the new suite locks (4.2.TE)

1. **Runtime parity belt** — `Object.keys(errorsAr)` ≡ `Object.keys(errorsEn)` sorted-equal, and EVERY value on BOTH maps is a non-empty localized string (dead-key zero-tolerance; symmetric sweeps in both directions).
2. **Route-emitter coverage, self-maintaining** — reads `app/api/graphql/route.ts` FROM DISK, scans BOTH realization forms (`errorsTranslations\.(key)` regex catches direct chains AND held-variable access), asserts the three known emitters still appear (rot-guard), then for EACH discovered key: resolves non-empty on BOTH maps and `Object.hasOwn` against the compiled leaf (untyped holes impossible).
   *Property:* a future transport variant that emits a NEW key auto-enters the gate with zero suite edits — red until ar+en copy lands.
3. **REQ-002 negative space** — zero `*health*` keys permitted in either map, forever.
4. **Import hygiene pins** — route.ts contains neither `next-intl` nor `getBackendTranslations` nor `shared/messages`, WITH a positive control (`getServerTranslations` must stay present).

Envelope-localization behavioral proof deliberately NOT duplicated here — ownership stays with graphql-route.transport.test.ts (citation table above), preserving that suite's role as the wire-shape authority.

## Sub-gate rollup

- **4.2.QL** ✅ — sub-loop exit 0 on the only modified .ts; locale validation discharged via the compile-time gate (documented above; no dedicated script exists to run — recorded as env/tooling fact, NOT a workaround).
- **4.2.TE** ✅ — 8/0 new parity tier + 19/0 & 9/0 behavioral envelope citations re-run live.
- **4.2.SEC** ✅ — localized strings audited: every value is generic user-facing prose (verified during the non-empty sweep — no stack/path/env/internals vocabulary present; masking behavior itself remains DEV3-002-finalizer-owned and regression-green via error-finalizer suites untouched this cycle).
- **4.2.SR** ✅ — property-access consumption everywhere in route.ts (`errorsTranslations.badRequest` style; never string-concatenated key names); zero dead keys added (nothing added at all).
- **4.2.IV** ✅ — shared/locale/AGENTS.md respected (compile-time system; leaf purity untouched since no additions needed); server-context translation helper usage pattern matches AGENTS (`getServerTranslations(locale)` in API-route context).
- **4.2.OC** ✅ — this file; main-line checkbox flipped.

**Carry-forward:** if the future D7 GET-opt-in or HTTP-throttling tickets add transport-specific copy (e.g. dedicated methodNotAllowed/payLoadTooLarge phrasing), the parity suite ALREADY covers the addition mechanically — just add both locales' entries and the rot-guard list at the marked spot.
