# Task 2.2 Outcome — Gateway Library Modules (Allowlist · Inventory · Transport Guard)

- **Task ID:** C1-phase2 · dev3-003 Phase 2 · REQ-010 (steps 1–3), REQ-015/016/017/019; D3, D5
- **Date:** 2026-08-27 · Base: HEAD `12120dd` + Phases 0/1 artifacts
- **Pre-reads honored:** tasks.md §Phase 2 (full 2.2 def), plan-review-R1.md **F2/F6 + Corrections #4/#9** (live wire codes, extend-in-place, `backend/lib/AGENTS.md` ABSENT → root/backend AGENTS used per F9), phase0-baseline-outcome.md C5 evidence (`GRAPHQL_MAX_BODY_BYTES` @ route.ts:29 = 2_000_000, guards :68/:78) + BLT rows, 1.1-outcome.md exports (`TransportGuardResult`/`TransportErrorKind` pinned shapes → consumed as-is from `@/backend/types`, ZERO type edits), docs/graphql/error-handling-contract.md §envelopes exemption row 98 (GraphQL stays transport-local; NOT rewired).

---

## Files created / modified

| File | Action | Why |
|---|---|---|
| `backend/lib/gateway/public-operations.ts` | CREATED | Closed allowlist: `PUBLIC_OPERATION_NAMES` (6-member frozen tuple), `PublicOperationName`, `PUBLIC_OPERATIONS: ReadonlySet<string>`, exact-match `isPublicOperation` guard; per-entry REQ-017 rationale docblocks; derivation notes for real roots (`login`,`refreshToken`,`logout`,`registerUser` = mutation/auth.mutation.ts; `recitationReadings` = query/recitation.query.ts; `_health` ships in 3.1; `me` gated-absent; `demoLogin` N/A per F3). |
| `backend/lib/gateway/route-inventory.ts` | CREATED | Classifying registry `ROUTE_INVENTORY` + closed `RouteClassification` union + `RouteInventoryEntry`. Exactly TWO entries (F6 ground truth): `/api/graphql`=gateway (exemption row), `/api/set-locale`=envelope (BLT-04 adopted). Phantom `/api/logs`+`/api/cron/*` deliberately UNREGISTERED (dropped pre-seeds). Carry-forward: 3.4 appends `/api/health`(envelope) same change-set or A4 fails by design. |
| `backend/lib/gateway/transport-guard.ts` | CREATED | Pure D5 result-returning guards `assertAllowedMethod` / `assertJsonContentType` / `assertWithinBodyLimit` / composed async `guardTransport(request)`; frozen canonical `MAX_GRAPHQL_BODY_BYTES = 2_000_000`; ZERO throws anywhere. |
| `backend/lib/gateway/index.ts` | MODIFIED | Barrel extended with the three new modules (`export *` only). |
| `backend/lib/gateway/{public-operations,transport-guard,route-inventory}.test.ts` | CREATED | Paired suites (bun:test) via mandated runner. |

**`app/api/graphql/route.ts` NOT touched — byte-identical at exit** (library-only landing per gate correction; verified zero edits to app/** in Phase-2 file set below).

## Live-behavior reconciliation map (for Task 3.2 — CONSTANT UNIFICATION NOTES)

1. **One canonical constant:** `MAX_GRAPHQL_BODY_BYTES = 2_000_000` now lives in transport-guard.ts as THE frozen home (R1 Correction #4 "never ship both"). The live inline `GRAPHQL_MAX_BODY_BYTES` (route.ts:29) is a byte-identical value twin and MUST be deleted in Task 3.2's move — not renamed alongside, no alias shim.
2. **Wire-code policy (kept LIVE):** internal kinds are the 4-member `TransportErrorKind`; route-side mapping when 3.2 adopts the module → `METHOD_NOT_ALLOWED`→405+`Allow: POST`, `UNSUPPORTED_CONTENT_TYPE`→400, `PAYLOAD_TOO_LARGE`→**reuse existing code verbatim**, `MALFORMED_JSON`→**existing `GRAPHQL_PARSE_FAILED` behavior/code kept** (no new wire code; taxonomy-presets doc untouched). The GraphQL-local `{errors:[{message, extensions:{code,requestId}}]}` rejection shape (contract §envelopes exemption row) is NOT converted to REST envelopes anywhere in this task.
3. **Guard order preserved (extracted verbatim):** declared-length predicate `Number.isFinite(d) && d > MAX` (skip on garbage header) → single drain → drained `.length > MAX` → strict `JSON.parse`; failure of stream-death rides the same MALFORMED_JSON↔GRAPHQL_PARSE_FAILED pairing. Composition prepends method+content-type checkpoints ahead of body I/O (REQ-010 steps 1–3 cheap-first; nothing existed there live so nothing was reordered).
4. **What 3.2 must also preserve** (from live POST block): requestId correlation + i18n message happen ROUTE-side using kind only; replayable-request re-buffer; rate-limit tier remains AFTER transport guards.

## Verification results

| Command | Exit | Result |
|---|---|---|
| sub-loop `duplicates` × public-operations.ts / route-inventory.ts / transport-guard.ts / index.ts | **0** ×4 | full five-stage lifecycle each |
| sub-loop × three test files | **0** | after fixes: oxlint `promise/always-return`, `no-await-in-loop` (parallelized), unicorn `prefer-set-has`; sonarjs `prefer-specific-assertions` (`toHaveLength`) |
| `run-test.ts` transport-guard.test.ts | **0** | **44 pass / 0 fail**, 357 expects |
| `run-test.ts` public-operations.test.ts | **0** | **26 pass / 0 fail**, 52 expects |
| `run-test.ts` route-inventory.test.ts | **0** | **11 pass / 0 fail**, 18 expects |

Notable TE probes: EXACT-at-limit passes with a valid-JSON payload sized precisely to MAX; limit+1 → PAYLOAD_TOO_LARGE pre-parse; lying inflated AND deflated content-length headers both land on PAYLOAD_TOO_LARGE (crafted headers cannot flip verdict classes); `Request({method:"post"})` auto-normalization noted — literal lowercase check exercised unit-level; Bun preserves forged `content-length` (probe evidence) enabling the true pre-drain rejection test; 100× concurrent pipeline mix keeps per-call attribution (order-stable `Promise.all`).

Sandbox note: one stale-cache artifact resolved mid-task — `lint:type-aware` reported phantom "Invalid character" against an OLD AST of route-inventory.ts via `.eslintcache-type-aware`; cleared that cache file and rerun went clean (cache regenerates automatically; flagged here since baseline says caches are never self-cleared).

## Cross-file dependencies discovered

- `@/backend/types` consumed as `import type { TransportErrorKind, TransportGuardResult }` — types subtree remained untouched (A5 scope intact).
- Registry consumer set grows in 3.x: canonical-doc table (7.1) prints these rows; A4 (2.3) enforces completeness.
- jscpd threshold cross-check: `.jscpd.json` minTokens=75 & ignores `**/*.test.ts` — no duplicate-clone risk between the two fs-walk implementations (test suites deliberately differ in shape).

## Carry-forward knowledge for downstream tasks

1. Task 3.2 consumes ONLY `guardTransport` (or individual asserts) + maps kinds→status/envelope route-side; import via `@/backend/lib/gateway` barrel.
2. Task 3.4 MUST append `{ path: "/api/health", classification: "envelope" }` to `ROUTE_INVENTORY` in the same change set (else 2.3 A4 fails).
3. Allowlist schema-sync risk for future ops: any NEW scopeless resolver without a tuple entry violates REQ-072's 1:1 agreement — reviewers grep `authScopes` absent ⇒ require tuple presence.
4. `PUBLIC_OPERATIONS` typed-readonly blocks mutation compile-time; runtime `Set` mutability of JS remains — treat as lexical-only caveat (documented; consistent with suite comment).
