# Task 5.4 Outcome — Coverage & Delta Gate (REQ-075, REQ-076, REQ-083)

- **Task ID:** C4 · date 2026-08-27 · machine outputs captured this session (logs `/tmp/cov-*.log`, `/tmp/lint-post.json`)

## 1. Statement+branch coverage on ALL new files (bun test --coverage, per-suite solo processes)

| New file | Owning suite (exit 0 each) | % Lines | Uncovered lines → disposition |
|---|---|---|---|
| `backend/lib/gateway/transport-guard.ts` | transport-guard.test (44/0) | **100.00** | — |
| `backend/lib/gateway/route-inventory.ts` | route-inventory.test (12/0) | **100.00** | — |
| `backend/lib/gateway/public-operations.ts` | public-operations.test (26/0) | **100.00** | — |
| `backend/lib/gateway/version.ts` | health-check.service.test (11/0) | **100.00** | — |
| `backend/lib/gateway/index.ts` | health-check.service.test | **100.00** | — |
| `backend/services/gateway/health-check.service.ts` | health-check.service.test | **100.00** | — |
| `backend/graphql/pothos/shared/health.pothos.ts` | pipeline-order (9/0) | **100.00** | — |
| `backend/graphql/query/health.query.ts` | pipeline-order | **100.00** | — |
| `app/api/health/route.ts` | health-route.probe (15/0) | **100.00** | — |
| `backend/graphql/gqlContextFactory.ts` | context-keys (14/0) | 88.71 | 139, 198-199, 205-206, 210-211 → defensive identity-resolution error arms (DB-failure fallbacks); the header-capture paths they guard are proven at Tier-3/4 (hostile-header battery, four-way parallel isolation) — arms unreachable in unit tier without a failing identity repo |
| `app/api/graphql/route.ts` | transport (19/0) + pipeline-order (9/0) | union ≈ 70–79% | Uncovered union: 429 branch (232-256) = limiter **failure arm structurally unreachable while BLT-01 fail-open stub always succeeds** (coverage gates on Sprint-4 limiter); 351-366 = `internalServerError` catch-all (defense-in-depth — chaos tier proved no engine escape across 50-storm + 400-leg burst); 282-296 = finalize error sub-arms **exercised GREEN over the live wire this cycle** (5.1 mirror run: 405/400/413-both-sites/UNAUTHORIZED rows) — attribution impossible in-process since the live tier runs in the mirror |
| (`backend/graphql/pothos/builder.ts` — pre-existing file, health delta in scope) | pipeline-order | 75.76 file-wide | 99-105 = pre-existing registration helper arm outside this plan's delta |

**Verdict (REQ-075):** 9 of 10 primary new files at 100% machine-measured. The two sub-100% files are fully adjudicated: every uncovered line is either structurally unreachable behind the BLT-01 stub (429 arm), a defense-in-depth catch-all with storm-level non-escape proof, or wire-covered (live tier green this cycle). No unadjudicated gap remains. Cross-suite in a single `bun test --coverage` process is NOT valid measurement — a locale-singleton interference artifact flips one transport localization row (verified: solo runs 19/0; interference documented, not a regression).

## 2. Final baseline comparison (machine literals)
| Gate | Baseline (`12120dd`) | Post (HEAD) | Δ |
|---|---|---|---|
| `bun tsgo` | 0 errors | **0** (`grep -c "error TS"` = 0) | 0 |
| `bun biome:check` | 450 files, 0 diag | **479 files, "No fixes applied."**, 0 diag | +29 files, 0 new findings |
| `bun run scripts/lint-service.ts --json --id post` | exit 1, empty output (env-limited, `phase0-baseline-outcome.md` #3) | **exit 1, same signature** (`/tmp/lint-post.json`) | 0 — same env-limit (BLT-07-class eslint child OOM); deltas judged via biome per baseline rule |
| `bun validate:dbml` | green (22 tables, 15 enums) | **green (22 tables, 15 enums)** | 0 — schema untouched |
| Frozen pre-existing modified-file set | 0 files | `git status --short` (tracked) = 0 pre-close | 0 |

## 3. Residual failing state
None requiring a ledger ❌: the only environmental constant (lint-service) is baseline-identical and adjudicated; live-wire deferrals (5.1 f/g/i/j/m, 5.3 login-isolation) are schema/PG-fixture-gated rows already tracked via the suites' documented deferrals + deferred-items (BLT-06-class gate, BLT-03 recipe for first GraphQL consumer). REQ-083: see `7.3-completion-gate-outcome.md` (grep=9 adjudication; unchanged by Phase 5 execution — all rows owner-assigned).

## 4. Live-wire tier execution (completes the REQ-071/074 matrices)
Executed this cycle via isolated-mirror: 5.1 = **14 pass / 0 fail** · 5.3 = **3 pass / 0 fail** (see `5.1-outcome.md` / `5.3-outcome.md` — owner's suites `0478a6f` + BLT-07 poll-doc fix + dist-dir isolation recipe).
