# Task C3-6.2 — Review-Backend Wave Outcome (dev3-003 · API Gateway & Routing Skeleton)

- **Task ID:** C3-6.2 (review-backend wave, plan Task 6.2)
- **Date:** review executed against HEAD `6d50a47` (diff base `12120dd`)
- **Mode:** READ-ONLY on source files. Zero source edits made; dev server :3000 untouched (no kill/restart/probe required for this lens); no `bun run build`; error-contract-matrix NOT run (known env-locked live-boot tier per phase0 sandbox adaptation #3).
- **Pre-reads:** worklog.md (C1-gate → C3-audit tail ~250 lines), `outcome/phase0-baseline-outcome.md` (pre-existing filter + REQ-002 exemption), `backend/AGENTS.md`, `backend/services/AGENTS.md`, `backend/graphql/AGENTS.md` (`backend/lib/AGENTS.md` does not exist), docs/graphql/error-handling-contract.md references consumed via AGENTS citations.
- **Scope reviewed (file-by-file):** `backend/lib/gateway/{transport-guard,route-inventory,version,public-operations,index}.ts` · `backend/services/gateway/health-check.service.ts` · `backend/graphql/{gqlContextFactory.ts, pothos/builder.ts, pothos/shared/health.pothos.ts, query/health.query.ts, query/index.ts}` · `app/api/graphql/route.ts` · `app/api/health/route.ts`. Test files read as claim-evidence only.

---

## Checklist findings

### 1. Architecture compliance — layer boundaries lib→services→graphql/route

**PASS — zero violations.**

- `backend/lib/gateway/*` imports ONLY `@/backend/types` (transport-guard.ts:34 type-only) — zero upward imports into services/graphql/app. Sole grep hit for a services import is a NEGATIVE-FIXTURE string literal inside static-assertions.test.ts:312 (in-memory scanner input, not an import).
- Dependency direction correct throughout: services/gateway → lib/gateway (`health-check.service.ts:23`) → types; graphql/query → services (`health.query.ts:24`) + pothos; app routes → lib/api + graphql factory (`route.ts:6-16`, `health/route.ts:29-30`). Route-side composition purity is executably pinned (`graphql-route.transport.test.ts:317-323` forbids `@/backend/services/`, `@/backend/db/`, dynamic imports, console).
- AGENTS rule conformance spot-checks:
  - Single Canonical Object Type (backend/AGENTS.md CRITICAL rule): `health.pothos.ts:28` uses `objectRef<HealthCheckReturnType>` from `@/backend/types` barrel — no local literal types; id-less embedded value per D4 precedent pairing (documented in-file :12-15, cache policy enforces `keyFields:false`).
  - Resolver delegation-only (backend/graphql/AGENTS.md): `health.query.ts:30` returns `HealthCheckService.getHealthStatus()` verbatim — no business logic, no ctx reads.
  - Side-effect barrel convention (query/AGENTS.md): `query/index.ts:18` alphabetical slot, no named exports.
  - Services domain-of-concern namespace (services/AGENTS.md DDD rule): `HealthCheckService` gateway namespace mirrors RecitationCatalogService style; NO `.types.ts` under services (types live in backend/types/gateway).
  - builder.ts:132-144: Query declared EMPTY, fields via side-effect imports; `_health` placeholder deletion (BLT-06) verified — grep `_health` in builder.ts = 0 (comment refers to removal history only).
  - i18n: localized rejection messages flow through compile-time `errorsTranslations` (`badRequest` present ar+en, grep-verified; runtime parity suites green). Health payload stays machine-constant English under the Phase-0 REQ-002 exemption, negative-space-pinned by errors-namespace.parity suite.

### 2. TOCTOU races

**PASS with one recorded non-blocking hazard (LOW-1 below).**

- **transport-guard body read/limit:** declared checkpoint (`Number.isFinite` predicate verbatim from live, transport-guard.ts:149-155) then FULL `request.text()` drain (:157-163) then drained-length check (:165). A DEFLATED/absent content-length forces the server to buffer the entire incoming body before the drained checkpoint rejects — the memory window between drain-start and drained-reject is client-controlled (declared cap bypasses when absent/non-numeric BY DESIGN: live predicate preserved). Mirrors live-block behavior byte-for-byte (mandated extend-in-place, R1 F2/Correction #4); lying-header coverage is test-locked at BOTH distinct sites (transport-guard.test.ts boundary tier; graphql-route.transport.test.ts:252-260 drained-site lie → 413). Classified: inherent-to-design, PRE-EXISTING-equivalent, hardening belongs with BLT-01 (real Redis limiter, Sprint-4). Non-blocking.
- **rate-limiter check-then-act:** `withRateLimit` (route.ts:219-277) is check (`checkRateLimit`:229) → act (`eventHandler`:259) with no reservation step; with today's fail-open stub (ratelimit.ts:83-92 always `success:true`) there is no shared mutable counter, hence no race. When the real limiter lands, permit-reservation semantics will be its owner's concern (BLT-01). Ordering invariant "junk traffic consumes zero limiter state" holds (guards precede limiter, executably proven by pipeline-order suite's zero-context-construction canaries). Non-blocking.
- **health service caching:** NONE by contract — fresh payload per call (health-check.service.ts:35-42), `resolveAppVersion()` deliberately uncached env-chain (version.ts:24-27 docblock + probe-suite "fresh-timestamp-no-module-state" assertion). No stale-read race surface.

### 3. Dead code

**PASS — no dead/unreachable code introduced.**

- Prior fix class verified closed: the removed `handler === undefined` guard is gone; kind→wire mapping is a typed exhaustive `Record<TransportErrorKind, TransportWireSpec>` (route.ts:55-60) — every union member indexed at compile time, zero dead guards. All four kinds reachable (GET/PUT/DELETE/PATCH exports exercise METHOD_NOT_ALLOWED; wrong-ct/missing-ct rows, forged/lying size rows, unparsable/whitespace/empty body rows exercise the rest — all asserted in the transport suite).
- Unused-param scan: transport-guard guards use every param; ratelimit stub's `_identifier` is intentionally underscore-marked (PRE-EXISTING lib, untouched by this diff).
- Exports-with-production-consumers audit (barrel index.ts exposes 4 modules):
  - `guardTransport`, `MAX_GRAPHQL_BODY_BYTES` — consumed by app/api/graphql/route.ts:12+329 (constant consumed via guard; route-source pins assert the inline twin is deleted).
  - `PUBLIC_OPERATIONS`/`isPublicOperation`/`PUBLIC_OPERATION_NAMES`/`PublicOperationName` — consumed by schema-surface SEC-twin, allowlist-coverage gate (stretch 5.2), public-operations suite; registry-of-record role per D3 (consumed as security instrumentation even without a current runtime call site). Informational only (LOW-2).
  - `ROUTE_INVENTORY`/types — consumed by static-assertions A4 bidirectional disk↔registry walk (a CI-tier production gate) + canonical-doc printing owed by Task 7.1. Not dead.
  - `assertAllowedMethod`/`assertJsonContentType`/`assertWithinBodyLimit` — composed internally by guardTransport; individual exports exist solely as the unit-test seam (D5 tiering). LOW/informational (NEW), non-blocking.
  - `resolveAppVersion` — consumed by health-check.service.ts:39.

### 4. Cross-layer imports

**PASS — zero violations.**

- lib importing services/graphql: **NONE** (grep; single hit = test negative-fixture string, see §1).
- backend importing `@/app/`: only TEST files importing their module-under-test (`app/api/*/test/*.test.ts`) — matches pre-existing precedent (set-locale-route.test.ts:43) and does not create a production layer edge.
- shared→frontend: **NONE** (no real import statements; shared/AGENTS.md negative examples only).
- Module-state scan of all touched sources: exactly ONE module-level mutable structure — `requestContextMap = new WeakMap<NextRequest, Context>()` (route.ts:156), the sanctioned pre-existing hand-off channel, byte-preserved F8 machinery and executably pinned as the sole allowed mutable member (graphql-route.transport.test.ts:325-331: `new Map<` const-form banned, `let x = []` banned, WeakMap presence required). Test-forbidden patterns verified absent everywhere else in scope.
- shared/locale untouched by backend delta; types/gateway pure (zero import statements — re-scanned this session).

### 5. Module state & concurrency

**PASS.**

- Route module singletons (`server` :139, `handler` :184, finalizer plugin registration exactly-once per AGENTS "one registration site") are frozen after load; Apollo execution isolates per operation; per-request state rides request-scoped objects only:
  - context built per request in the handler hook and stored under the SAME engineRequest object later used for the WeakMap lookup (:338-344 rebuild → :187 set → :350/:369 flush with identical key) — concurrent requests cannot cross-contaminate (fresh key object per POST).
  - authCookieOut accumulators are per-context instances (factory :183); flush loop reads only its own entry list.
- Gateway lib modules hold zero mutable module state (`PUBLIC_OPERATIONS` constructed once, typed ReadonlySet, never written after load — 2.2.SR clause upheld). Guards are pure over arguments (D5 docblock) — concurrent-call independence proven by transport-guard matrix running parallel cases.
- Async error containment: every `await` in POST lives inside try (:325-350) whose catch (:351-370) synchronously builds a defined 500 envelope and re-flushes cookies; nothing escapes to Next's framework default. pipeline-order + context-keys suites demonstrate interleaved-request isolation (parallel isolation keys/ids assertions).

### 6. Error-path correctness

**PASS — every throw maps to a defined envelope; no double-send; cookie merge unconditional + append-only.**

- Defined-envelope mapping: transport kinds → frozen `TRANSPORT_WIRE_MAP` (405+Allow:POST+BAD_REQUEST / 400 BAD_REQUEST / 413 PAYLOAD_TOO_LARGE reused verbatim / 400 GRAPHQL_PARSE_FAILED live pairing) → `transportRejectionResponse` (:75-86) localized ar/en with mirrored requestId; limiter reject → 429 envelope :231-256; any thrown fault (incl. ctx-build DB failure) → catch → 500 internalServerError envelope :351-369. Locale KEYS for all three emitters exist in BOTH ar+en sources (errors/index.ts:9/:11/:12 each) and the self-maintaining parity suite locks them.
- Double header-send: impossible — each path constructs ONE response and returns it immediately; no response-object reuse after return; OPTIONS/HEAD build standalone responses.
- Set-Cookie: exactly ONE `headers.append("Set-Cookie", …)` code site (:179), zero `headers.set("Set-Cookie")` (executable pins, transport.test.ts:312-315); merge runs UNCONDITIONALLY on the engine-reaching success path (:350) AND the catch fallback (:369) per REQ-042 — an improvement over baseline where the catch path dropped accumulated cookies (baseline route-old.ts:315-330 had no flush). Pre-engine rejections correctly skip (engineRequest===null, no ctx exists by construction).
- Re-buffer note (verified-safe, no finding): POST replays `JSON.stringify(transport.body)` instead of baseline's raw string. Downstream semantics unchanged — the engine's own `JSON.parse` materializes identical JS values (double precision fixed at first parse), duplicate keys collapse identically, and any user-supplied content-length header is stripped/recomputed per fetch-spec Request construction, so no stale-length mismatch can reach the engine. End-to-end engine path exercised green by pipeline-order suite (real engine + limiter + finalizer over the re-buffered request).
- Recorded observations (all LOW/PRE-EXISTING, non-blocking):
  - The 429 envelope extensions `{code,limit,reset}` and the 500 fallback extensions `{code}` carry NO requestId, unlike transport/engine envelopes — byte-preserved pre-existing blocks (baseline :200-222 / :318-328), retained deliberately to keep limiter/catch tiers untouched; correlation arrives via headers/logs instead. Candidate unification whenever the envelope tiers are next touched (dev3-002 stream owns the shape).
  - Catch/429 branches render hardcoded `"en"` translations (:233, :353) even for ar requests — pre-existing verbatim; REQ-051 applies to the transport tier, which DOES localize per request locale. Key-level ar/en parity intact.

---

## Findings register

| # | Severity | Blocking? | NEW/PRE-EXISTING | Finding |
|---|----------|-----------|------------------|---------|
| 1 | LOW | no | PRE-EXISTING-equivalent (live predicate preserved by mandate R1 F2/Cor#4) | Declared-length bypass ⇒ unbounded server-side body buffering before drained-length check rejects (transport-guard.ts:149-168). Documented lie-header defense is position-2 only; stream-cap hardening should ride BLT-01/Sprint-4. |
| 2 | LOW (informational) | no | NEW | Individual guard exports (`assertAllowedMethod`/`assertJsonContentType`/`assertWithinBodyLimit`, transport-guard.ts:71/88/112) have no production consumer beyond internal composition — deliberate unit-tier seam, test-consumed; no action needed, do not "prune" as dead code. |
| 3 | LOW (informational) | no | PRE-EXISTING | Correlation asymmetry: 429 + 500-fallback envelopes omit requestId; runtime "en"-hardcoded in those two branches (route.ts:233/:353) though ar+en keys exist. Preserved verbatim by design; unify only when dev3-002-owned envelope shapes are next revised. |

**Blocking count: 0. CRITICAL/HIGH/MEDIUM count: 0.** All other checklist items: PASS with executable evidence. The single feature-critical fix identified by the plan-review waveform (`handler === undefined` dead guard) is confirmed removed; no sibling unreachable branches remain (exhaustive typed mapping).

---

## Verification record (this session)

| Command | Result |
|---|---|
| `bun tsgo` → `grep -c "error TS"` | **0** (expected 0 ✓) |
| `bun biome:check` | exit 0 — literal "**Checked 477 files in 8s. No fixes applied.**" (= last published gate) |
| `run-test.ts backend/lib/gateway/transport-guard.test.ts` | **44 pass / 0 fail**, exit 0 |
| `run-test.ts backend/lib/gateway/route-inventory.test.ts` | **12 pass / 0 fail**, exit 0 (12 = post-3.4 health-row lock) |
| `run-test.ts backend/lib/gateway/static-assertions.test.ts` | **17 pass / 0 fail**, exit 0 |
| `run-test.ts backend/lib/gateway/public-operations.test.ts` | **26 pass / 0 fail**, exit 0 |
| `run-test.ts backend/services/gateway/health-check.service.test.ts` | **11 pass / 0 fail**, exit 0 |
| `run-test.ts backend/graphql/test/context-keys.test.ts` | **14 pass / 0 fail**, exit 0 |
| `run-test.ts app/api/graphql/test/graphql-route.transport.test.ts` | **19 pass / 0 fail**, exit 0 |
| `run-test.ts app/api/graphql/test/graphql-route.pipeline-order.test.ts` | **9 pass / 0 fail**, exit 0 |
| `run-test.ts app/api/health/test/health-route.probe.test.ts` | **15 pass / 0 fail**, exit 0 |
| **Total (9 mandated suites)** | **167 pass / 0 fail** |
| error-contract-matrix | NOT RUN per dispatch order (env-locked live-boot tier, known) |
| Tree state post-review | tracked-clean; this outcome file is the only write |

Greps captured: lib→services/graphql/app imports NONE · shared→frontend NONE · backend→`@/app` test-files-only · module mutable state = exactly the sanctioned WeakMap (+ executable pin) · badRequest/rateLimitExceeded/internalServerError present ar+en · `_health` absent from builder.ts.

## Verdict

**APPROVE — backend wave passes with ZERO blocking findings.** Three LOW/informational records above are logged for the deferred-hardening owners (BLT-01 / dev3-002 envelope stream) and require no change-set within dev3-003. Layer discipline, transport-tier result-unions, seven-step pipeline ordering, idempotency-key single-capture, locale parity, allowlist/inventory coverage, module-state hygiene, and error-path containment all conform to the established AGENTS rules and the plan's pinned decisions.
