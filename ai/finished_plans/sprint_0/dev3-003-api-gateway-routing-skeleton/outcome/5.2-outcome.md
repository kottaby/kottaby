# Task 5.2 Outcome — Allowlist Coverage Gate (REQ-072 · BLOCKING) — STRETCH DELIVERED

- **Task ID:** C2-phase4a (stretch, executed only after 4.1 + 4.2 gated green) · dev3-003 Phase 5 · REQ-017, REQ-032, REQ-072; D3
- **Date:** 2026-08-27 · Base: HEAD `4d41c8e`
- **Pre-reads honored:** worklog C1…C2-phase3c (esp. BLT-07 env-lock + phase3b "exit codes MUST be captured unpiped" gotcha); tasks.md §5.2 all sub-gates; plan.md D3 row + §4 file-map (`frontend/graphql/test/gateway/**` = REQ-072 coverage home) + §6 default-deny/BFLA rows; specs REQ-017/032/072/074; outcome/3.1-outcome.md (schema-build tier substrate + allowlist SEC-twin precedent); Phase-0/plan-review-R1 Correction #9 (gateway subdir placement + its AGENTS layout-note duty); backend/graphql/test/schema-surface.test.ts; backend/lib/gateway/{public-operations.ts,public-operations.test.ts}; DEV2-002 finished-plan notes on the original structural-only D3 verification.

---

## DECISION OF RECORD: new file vs extend DEV2-002's D3 test in place

**CREATE `frontend/graphql/test/gateway/allowlist-coverage.test.ts` (plan primary option). Nothing to extend exists.** DEV2-002's D3 verification shipped STRUCTURAL ONLY — source-text grep + a manually enumerated set listing ("verified structurally — public set … is exactly the unscoped set"; the runner-driven introspection file was explicitly deferred there as deferred-item D3, owner "test-runner env unblock"). There is no extant introspection test to extend IN PLACE. The nearest real artifacts are complements, not targets, and stay untouched: `backend/lib/gateway/public-operations.test.ts` (registry closure/exact-match/write-capability tiers — a DIFFERENT layer of the same defense) and `schema-surface.test.ts`'s `_health`-only agreement block.

## What the gate enforces (REQ-072 rows → tests)

Introspects the BUILT production schema in-process (`graphQLSchema` from `@/backend/graphql/gqlSchema` — identical import path to schema-surface.test.ts; NO server boot, NO setupTestServerLifecycle per BLT-07). Scope reading walks each root field's Pothos extension snapshot: `field.extensions.pothosOptions.authScopes` (ground-truth verified live this cycle: `me` → `{authenticated:true}`; every one of the six public ops → undefined).

1. **Default deny (both roots)** — every Query/Mutation field declares authScopes OR is an exact `PUBLIC_OPERATIONS` member. Non-vacuous pins: Query unscoped set EXACTLY frozen `["_health","recitationReadings"]`; mutation walk ≥4 ops (the frozen DEV2-001 quartet).
2. **1:1 exact agreement, BOTH directions** — sorted schema-unscoped-set ≡ sorted allowlist (stray scopeless resolver FAILS forward); every allowlist entry resolves to a REAL scopeless field, incl. must-not-be-scoped check (stale registry row FAILS reverse); allowlist members never hide behind a scope block either.
3. **Role-escalation preservation (DEV2-002 REQ-074)** — any `grantRole|assignRole|elevate` mutator requires ADMIN-GRADE scope defined STRICTLY as `{superAdmin:true}` or `role:[…]` containing `UserRole.Admin` (enum VALUE import; bare `permission` gates deliberately do NOT qualify — proxy scopes can't secure role materialization). Also barred from scopelessness AND from ever entering the allowlist. (Vacuously green today — zero such mutations exist — but every arm is live code against future drift.)
4. **Negative-fixture proof (5.2.SR, in-process)** — synthetic drifted schema built via graphql-js `buildSchema` INSIDE the suite (production modules untouched): fed through the SAME collector+comparators it shows (a) stray scopeless field detected & pinpointed; (b) simulated deleted-field-with-stale-row breaks the reverse direction exactly as a real regression would. Both fire ⇒ the gate has documented teeth.

Execution evidence (mandated runner chain note below): **8 pass / 0 fail, 42 expect() calls**.

## Wiring into `bun run test:graphql` / CI

`bun run test:graphql` → `test/scripts/run-server-tests.ts`, whose `discoverTestFiles()` GLOBS `frontend/graphql/test/**/*.test.{ts,tsx}` — so the plan-pinned path is auto-composed with ZERO package.json/script changes. In the DEV3-001 CI `tests` stage (no interactive :3000 server competing for the Next 16 singleton) this resolves to the plain glob composition and the gate runs alongside the auth/warning suites.

### ENV-NOTES (documented, NOT blocking — two independent local walls)

1. **UMD wall (NEW discovery this task).** Locally, `run-server-tests.ts` dies at MODULE LOAD — long before server spawn — because its own import chain pulls `@/test/helpers` → `graphql-test-helpers`/`expect-mutation-error` → `@apollo/client` root → `graphql-tag` UMD bundle → Bun loader TypeError (`require() async module node_modules/graphql/index.mjs is unsupported`). Reproduced twice at head of run-server-tests attempts today; fatal instantly (exit 1 before discovery/spawn logic even logs). This is a LOADER/INTEROP defect of sandbox Bun v1.3.14 with that CJS/UMD interop shape — recorded here first; ownership suggestion: same stream that already owns BLT-07's harness fixes.
2. **BLT-07 wall (would be hit right after).** Even past the UMD crash, the spawned dev server hits the Next.js 16 global singleton ("Another next server is already running", PID of the mandatory-to-live :3000 process) — C2-phase3b's triple-evidenced finding stands unmodified.
   **Consequence:** `local ↔ CI parity RUN cannot be executed end-to-end in-sandbox while :3000 lives`. Per task instructions this is explicitly non-blocking; CI-parity confirmation is handed to the owning test-harness stream alongside the BLT-07 fix.
3. **Verification honesty preserved.** Because the sanctioned delegated chain was impassable, the IDENTICAL BYTES were verified through the runner's OTHER (non-delegating) branch: verbatim copy at `backend/graphql/test/tmp-allowlist-probe.test.ts` → `cmp` byte-equality proof → `bun run test/scripts/run-test.ts <copy>` = **8 pass / 0 fail / 42 expects** (log captured) → copy deleted immediately (git-clean verified). The shipped canonical file therefore carries PROVEN-green content; only its PATH's runtime wiring awaits an environment where the harness can boot.

## Files changed / NOT changed

| File | Action | Why |
|---|---|---|
| `frontend/graphql/test/gateway/allowlist-coverage.test.ts` | CREATED (+ new subdir) | The gate itself (8 tests, structure above), at the plan-pinned path (R1 Correction #9). |
| `ai/plans/sprint_0/dev3-003-api-gateway-routing-skeleton/outcome/5.2-outcome.md` | CREATED | This file. |
| tasks.md · main-line checkbox `- [ ] 5.2` → `[x]` | MODIFIED | Per protocol rule 6. |

**NOT changed:** package.json (glob already composes — checked, no edit needed); `backend/lib/gateway/*` (decision-of-record keeps them untouched); `frontend/graphql/test/AGENTS.md` Layout section NOT edited this cycle — the add-a-new-subdir doc step lands with Task 7.2's AGENTS propagation wave (kept out of my sanctioned file list deliberately; flagged there as pending duty together with the embedded-types list addition from 4.1).

## Verification results (commands + exit codes)

| Gate | Command | Exit | Result |
|---|---|---|---|
| QL × gate file | `bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates` | **0** | full lifecycle green after fix loops: TS2769 (bun expect overload vs readonly matcher arg → mutable allowlist view + spread conversions), `unicorn/prefer-set-has` (filter-includes probe → Set.has semantics), plus the pre-runner catch: block-comment terminated early by a literal `*/` inside prose (`assignRole*/`) — reworded (found ONLY because the verbatim-copy sanity run compiled it standalone; the canonical path would have hidden the failure behind the UMD wall) |
| TE (verbatim-byte route) | cmp(copy, canonical) == 0 && `bun run test/scripts/run-test.ts <tmp copy>` | **0** | **8 pass / 0 fail**, 42 expect() calls; tmp removed post-run |
| TE (canonical-path delegated chain) | `bun run test/scripts/run-test.ts frontend/graphql/test/gateway/allowlist-coverage.test.ts` | 1 | Fails BEFORE test execution at the run-server-tests module-load UMD crash (env-note #1) — evidence captured; not a content failure |
| Project TS | `bun tsgo` then grep -c "error TS" | **0** / **0** | zero errors project-wide |
| Project lint | `bun biome:check` | **0** | literal `Checked 477 files in 4s. No fixes applied.` |
| Post-gate tree | `git status --porcelain` | — | sanctioned set only (+ parallel cron-feature stream pre-existing dirty files, untouched; tmp probe absent) |

## Sub-gate rollup

- **5.2.QL** ✅ — sub-loop exit 0 (true unpiped exit).
- **5.2.SR/IV** ✅ — negative-fixture proof is LIVE CODE inside the suite (in-process synthetic schema through the same detector), not a narrative claim; layering deviation for schema/registry imports justified in the file header (unit-tier inspector, not API-driving integration test; zero repos/services/db imports).
- **5.2.OC** ✅ — this file; main-line checkbox flipped with the wiring env-notes on record.

**Carry-forward:** (a) test-harness stream: fix run-server-tests UMD exposure (e.g. narrow `killListenersOnPort` import path to avoid the apollo-pulling barrel) AND BLT-07's poll document — THEN rerun this gate through the full chain for true local↔CI parity evidence; (b) Task 7.2: gateway/ subdir Layout note; (c) future role-gated mutations should reuse isAdminGradeScope expectations — extend, don't fork.
