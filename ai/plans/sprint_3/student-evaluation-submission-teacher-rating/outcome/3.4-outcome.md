# Task 3.4 Outcome — Registration artifacts (codegen), SDL pins, wire matrix

> **Plan:** `ai/plans/sprint_3/student-evaluation-submission-teacher-rating/` · **Task:** 3.4 (Phase 3 — GraphQL surface) · **Branch:** `feat/student-evaluation-submission-teacher-rating` · **Date:** 2026-09-12
> Requirements: REQ-008, REQ-011, REQ-013.4 · **State:** VERIFIED — all gates green.

## Summary

Task 3.4 closes Phase 3: the Pothos registrations from 3.1–3.3 are materialized into the committed artifacts (`bun run generate:gqlSchema && bun codegen`), the artifact-side SDL pins are extended for the `Evaluation` surface, and the REQ-013.4 wire matrix proves the REQ-011 role/error contract over the real HTTP wire (`setupTestServerLifecycle` + `testClient` + `expectMutationError`, session-report wire precedent).

## Files (6)

| File | Status | Content |
|---|---|---|
| `frontend/graphql/generated/schema.graphql` | REGENERATED | + `Evaluation` object, `SubmitTeacherEvaluationInput` input, both root fields |
| `frontend/graphql/generated/gql/graphql.ts` | REGENERATED (byte-identical) | document-reachable types only — no change until 4.1 documents land |
| `backend/graphql/test/sdl-static-assertions.test.ts` | EXTEND | frozen roots re-anchored (additive) + student-evaluation artifact pins |
| `backend/graphql/test/student-evaluation.wire.test.ts` | NEW | REQ-013.4 wire matrix (role × operation × error code) |
| `backend/graphql/test/schema-surface.test.ts` | EXTEND | baseline-freeze pins re-anchored additively (mutation/query additions + named-type delta + `Evaluation`/`SubmitTeacherEvaluationInput`) |
| `frontend/graphql/test/warnings/warning-surfacing.test.ts` | EXTEND | `KNOWN_LIVE_MUTATION_FIELDS` + `submitTeacherEvaluation` (warning-incapable canonical payload, refresh note) |

## Regenerated artifacts (record)

- `bun run generate:gqlSchema` → `frontend/graphql/generated/schema.graphql` (lexicographically sorted Pothos SDL) — re-run clean: output byte-stable, the diff is exactly the +19 new-surface lines.
- `bun codegen` → `frontend/graphql/generated/gql/graphql.ts` (typescript-operations + typed-document-node over the regenerated SDL; documents globs unchanged — frontend documents are 4.1's scope). Regenerated deterministically: output is BYTE-IDENTICAL to the committed file (no frontend document references the new surface yet, so the document-reachable-type plugins emit nothing new).
- Verified: `type Evaluation` + `submitTeacherEvaluation` + `myTeacherEvaluations` + `SubmitTeacherEvaluationInput` all present in `schema.graphql` (the committed artifact of record for 3.4). They are (correctly) ABSENT from `graphql.ts` at this stage — see "Generated names observed".

## Verification results

- **Two-file suite** (`bun run test:graphql backend/graphql/test/student-evaluation.wire.test.ts backend/graphql/test/sdl-static-assertions.test.ts`): **ALL PASSED — 2/2 files, 65 tests, 0 failed, 359 expect() calls**.
- Per-file: wire matrix **21 tests / 108 assertions**; SDL static assertions **44 tests / 251 assertions** (21+44 = 65 ✓, 108+251 = 359 ✓).
- Full `bun run test:graphql` suite: see "Full-suite run" below (run last; port-hygiene protocol honored between runs).
- **Role matrix proven over the wire (SEC proof)** — every matrix cell asserted one-to-one via `expectMutationError` (error `code` only, never HTTP status): anonymous × both roots → `UNAUTHORIZED` (+ constant localized copy on both envelopes); teacher/parent/admin × `submitTeacherEvaluation` → `FORBIDDEN` (pre-resolver scope, admin NO bypass); teacher/parent/admin × `myTeacherEvaluations` → `FORBIDDEN`; student-participant happy paths return server-derived rows (`evaluatorId` = caller, `evaluatedId` = the session's teacher — identities never accepted from arguments); student-foreign → `SESSION_NOT_FOUND` byte-identical to unknown-id. Both roots are declared `$all { authenticated, role: [UserRole.Student] }` (3.1/3.2) and the wire denials match that conjunction exactly.
- Service error codes at the wire: `EVALUATION_SESSION_NOT_COMPLETED` (scheduled σ′ AND teacher-stamp-only σ″), `EVALUATION_ALREADY_SUBMITTED` (re-submit; stored rating untouched), `SESSION_NOT_FOUND` (foreign ≡ nonexistent), `VALIDATION` (rating 6 → `extensions.fields` = exactly `[{field:"rating", code:"TEACHER_RATING_INVALID", …}]`; malformed session id → bare message channel, NO fields).
- QL sub-loop (`bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates`): **exit 0 on BOTH mandated files** (tsgo · oxlint · biome:check · lint:type-aware · check:duplicates) and on the two re-anchored pin files (schema-surface, warning-surfacing) — all four exit 0 (log `/tmp/task34-ql2.log`).
- **Full-suite run**: `bun run test:graphql` → **ALL PASSED — 10/10 files, 154 tests, 0 failed, 928 expect() calls, 49.55s** (log `/tmp/task34-full.log`; first attempt failed ONLY in the two stale root-inventory pins — `warning-surfacing.test.ts` A1 and `schema-surface.test.ts` additions/named-type delta — which were then re-anchored additively as above; zero service changes).
- Port hygiene: the known EADDRINUSE hook-timeout cause (an orphaned `next dev -p 3066`) was killed before every run; a solo wire run against a leftover server reproduced the 240s hook timeout, confirming the diagnosis (environment, not code).

## Attestations

- **SEC** — the REQ-011 role matrix is proven over the REAL wire (HTTP → gateway → scope-auth → resolver → service → PostgreSQL): the six-caller × two-operation table in the wire test's header is executed test-by-test (anonymous/UNAUTHORIZED tier, student-participant tier with gates, student-foreign oracle, teacher/parent/admin FORBIDDEN tier incl. admin no-bypass and constant-copy envelope parity). No allowlist edit was needed or made: both operations are authenticated, so the anonymous public-operation allowlist is untouched.
- **SR** — no edit to `backend/lib/gateway/public-operations.ts` (git-tracked diff scope: the two generated artifacts + the two test files only); SDL pins are ADDITIVE — `FROZEN_MUTATION_FIELDS` 34→35 (`+ submitTeacherEvaluation`), `FROZEN_QUERY_FIELDS` 33→34 (`+ myTeacherEvaluations`), no existing pin mutated; comment hygiene fixed in the wire test (four plan-artifact references rephrased domain-only: header task ref, plan-section ref, negative-sweep ref, section-divider ref).
- **IV** — rule files read before verification/fixing: `.agents/instructions/tests.instructions.md`, `.agents/instructions/backend.instructions.md` (both listed by the sub-loop as applicable), and `backend/graphql/AGENTS.md` (scope composition, error-contract finalizer, codegen mandate).

## Carry-Forward Knowledge (Phase 4 — frontend documents task 4.1)

1. Generated document-type names (codegen `typed-document-node` naming, from the regenerated schema): the mutation document type is `TypedDocumentNode<SubmitTeacherEvaluationMutation, SubmitTeacherEvaluationMutationVariables>`; the query document type is `TypedDocumentNode<MyTeacherEvaluationsQuery, MyTeacherEvaluationsQueryVariables>`. Exact generated names to import from `@/frontend/graphql/generated/gql/graphql`: `SubmitTeacherEvaluationMutation`, `SubmitTeacherEvaluationMutationVariables`, `MyTeacherEvaluationsQuery`, `MyTeacherEvaluationsQueryVariables`, plus the shared `Evaluation` object type — verify verbatim in `frontend/graphql/generated/gql/graphql.ts` after codegen (the generated names are recorded below under "Generated names observed").
2. SDL on the artifact (sorted print order): `submitTeacherEvaluation(input: SubmitTeacherEvaluationInput!, sessionId: ID!): Evaluation!` (args alphabetical — `input` before `sessionId`) and `myTeacherEvaluations: [Evaluation!]!` (zero args). The frontend documents must use the same argument names (`sessionId`, `input`).
3. `Evaluation` wire shape (for selections): `id` (`ID!`), `evaluatedId`/`evaluatorId` (`Int!`), `sessionId` (`Int`, nullable), `score` (`Int`, nullable), `createdAt` (`DateTime!` → generated `string`). Selections must start with `id`.
4. Error surface over the wire (for the 4.2 error-link map): `UNAUTHORIZED`, `FORBIDDEN`, `SESSION_NOT_FOUND`, `EVALUATION_SESSION_NOT_COMPLETED`, `EVALUATION_ALREADY_SUBMITTED`, `VALIDATION` (+`extensions.fields[]` = `[{field:"rating", code:"TEACHER_RATING_INVALID", message}]` for the star-range denial; the malformed-id VALIDATION carries NO `fields`).
5. Non-paginated `myTeacherEvaluations` (empty = `[]`) — no page args to model on the frontend.

## Generated names observed

- **None emitted for the evaluation surface at this stage — by design.** `codegen.ts` runs only `typescript-operations` + `typed-document-node` (no base `typescript` plugin), so `graphql.ts` emits exclusively DOCUMENT-REACHABLE types; no frontend document references the new surface yet (documents are task 4.1's scope), hence the byte-identical regeneration.
- What 4.1 will see after adding the documents and re-running codegen: `SubmitTeacherEvaluationMutation` + `SubmitTeacherEvaluationMutationVariables`, `MyTeacherEvaluationsQuery` + `MyTeacherEvaluationsQueryVariables`, plus document-reachable `Evaluation` / `SubmitTeacherEvaluationInput` types — documents typed as `TypedDocumentNode` via the file's `import type { TypedDocumentNode as DocumentNode } from '@graphql-typed-document-node/core'` alias.
- SDL names 4.1 must use verbatim in documents (from `schema.graphql`): mutation `submitTeacherEvaluation(input: SubmitTeacherEvaluationInput!, sessionId: ID!)` (args alphabetical: `input` first), query `myTeacherEvaluations` (zero args).

## Test counts (raw)

| Suite | Files | Tests | Failed | Assertions |
|---|---|---|---|---|
| wire matrix (solo) | 1/1 | 21 | 0 | 108 |
| SDL static assertions (solo) | 1/1 | 44 | 0 | 251 |
| two-file run | 2/2 | 65 | 0 | 359 |
| re-anchored pins (solo pair) | 2/2 | 51 | 0 | 303 |
| full `test:graphql` suite | 10/10 | 154 | 0 | 928 |

## Full-suite run

**ALL PASSED — 10/10 files, 154 tests (0 failed), 928 assertions** on a clear port (log `/tmp/task34-full.log`). First full attempt failed exactly once (`warning-surfacing.test.ts` A1 inventory pin — `Received +1: submitTeacherEvaluation`), exposing the same drift class in `schema-surface.test.ts` (mutation/query additions + whole-schema named-type delta); both were re-anchored additively (see Files table), re-verified solo (51 tests), QL exit 0, and the full suite re-run green.
