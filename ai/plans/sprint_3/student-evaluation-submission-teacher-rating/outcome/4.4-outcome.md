# Task 4.4 — Component Tests (Rate CTA + RateTeacherDialog)

> **Plan:** `ai/plans/sprint_3/student-evaluation-submission-teacher-rating/` · **Status:** COMPLETE

## Summary

- Authored the REQ-009.8 component matrix as `test/ui/components/student/rate-teacher-dialog.test.tsx` (runner entry, two-phase bootstrap for bun's react-dom eval-order root cause) + `rate-teacher-dialog.suite.tsx` (suite body, the same entry/suite split as `StudentSessionsContainer.suite.tsx`). Happy DOM + Apollo `MockedProvider` tier per `test/ui/AGENTS.md`; zero hardcoded locale copy — every assertion resolves through the preloaded `Sessions`/`Errors` handles (`sessionSuiteLabels`), both locales.
- **Load-bearing dependency shipped:** `test/ui/components/translation-preload.ts` now warms the `Sessions` namespace handle (both `ar`/`en` catalogs) in its warming loop; `Errors` was already warmed. Missing-key drift for the whole rate surface now surfaces at preload time (`test/ui/AGENTS.md:163-165`).
- The 4.2 teacher-view regression (rate dialog mounted open on every teacher render through the role-shared seam → 28 teacher-suite failures) was fixed inside 4.2's own file set: the rate slot left the role-shared `StudentSessionsDialogs.tsx` seam entirely (`StudentRateDialogSlot` export) and mounts from the student container only. Full evidence, root cause, and verification live in `outcome/4.2-outcome.md` (supersession note + post-fix section); teacher suite green again at **35 pass / 12 skip / 0 fail** with zero rate markup in the teacher DOM tree.
- **Site-footer snapshot drift verdict: PRE-EXISTING** (determination experiment below) — documented and left untouched; no snapshot regeneration was performed.

### REQ-009.8 matrix → test-case mapping

| REQ-009.8 / REQ-009.2 requirement | Suite test (runs per locale: ar + en) | Wire-hygiene proof |
|---|---|---|
| CTA hidden pre-confirmation (REQ-009.2: dual-confirmed ∧ unrated is the only visible shape) | `rate CTA hidden on every pre-confirmation shape — scheduled, student stamp pending, teacher stamp missing` — asserts no Rate CTA on all three shapes, the stamp-pending row still shows its live Confirm affordance (a real gate decision, not an empty matrix), no dialog slot opened | **NO mutation mock in the render's mock list** — a leaked submit (or any extra operation) fails the branch as an unmatched MockLink operation |
| CTA hidden when already rated (REQ-009.5 end-state) | `rate CTA hidden once the session sits in the rated set — read-only rated chip renders instead` — write-once end-state is the non-interactive chip, never a button | Queries-only branch (list + history mocks); no mutation mock exists to be hit |
| CTA visible when dual-confirmed | `rate CTA visible on the dual-confirmed shape whose id is absent from the rated set` — enabled button, correct descriptor testid | Render carries only read mocks; the CTA is inert until activated |
| Dialog submit dispatches the mutation with the selected rating (REQ-009.4/5) | `dialog dispatch — submit gated until a star is chosen, then the mutation fires with the selected rating` (rating 4; shell pinned: localized title/cancel, exactly six radios = 1–5 + MUI clear radio; submit DISABLED before star choice — an empty submit can never reach the wire) + success arm: snackbar severity pinned `MuiAlert-colorSuccess`, dialog closed, cache prepend flips the row to the rated chip WITHOUT a refetch | Exactly one mutation mock (variables `{ input: { rating: 4 }, sessionId }`) — mock-list exhaustion proves a single wire call |
| Boundary ratings (Tier 2: rating at 1 and 5) | `boundary dispatch — the 1-star and 5-star choices both reach the wire` | One success mock per iteration, exhausted |
| VALIDATION field error render (REQ-009.6) | `server VALIDATION addressed at the rating field renders inline under the stars and keeps the dialog open` — server-localized pair echoed verbatim inline (`MuiFormHelperText-root` with `Mui-error`), dialog stays open, submit re-enabled; NO generic toast, NO failure snackbar, row stays un-rated | Deny path: mock list exhausted (list + history + the one failing mutation) — no second wire call can hide behind the deny |
| ALREADY_SUBMITTED → notice + rated state (REQ-009.6) | `write-once rejection — dialog closes, the row flips to its rated state, the app-scope notice stays mapped-only` — the mapped app-scope notice renders through the error-link surface; the local arm does NOT duplicate it (nor fire success/error snackbars); CTA gone, chip present, no refetch | Deny path: mock list exhausted after the single rejected call |

Bootstrap self-check `bootstrap — Happy-DOM document registered before the suite import resolves` pins the entry's preload contract. **15 tests total** (7 matrix cases × 2 locales + 1 bootstrap), **109 expect() calls, 15 pass / 0 fail**.

## Files

| File | Change |
|---|---|
| `test/ui/components/student/rate-teacher-dialog.test.tsx` | NEW — runner entry: sequential top-level-await preload chain, then dynamic suite import; bootstrap self-check |
| `test/ui/components/student/rate-teacher-dialog.suite.tsx` | NEW — suite body: REQ-009.8 matrix over `StudentSessionsContainer` + `RateTeacherDialog`, both locales, shared helpers/fixtures |
| `test/ui/components/translation-preload.ts` | EXTEND — `Sessions` handle warmed (ar+en) in the preload loop |
| `frontend/views/student/sessions/StudentSessionsDialogs.tsx` | FIX (4.2 regression) — rate slot removed from the role-shared seam; `StudentRateDialogSlot` student-only export added (detailed in `outcome/4.2-outcome.md`) |
| `frontend/views/student/sessions/StudentSessionsContainer.tsx` | FIX (4.2 regression) — mounts the rate dialog via `StudentRateDialogSlot` next to the seam |
| `ai/plans/sprint_3/.../outcome/4.4-outcome.md` | NEW — this file |
| `tasks.md`, `worklog.md` | 4.4 + sub-checkboxes `[x]`; 4.4 worklog entry |

## Verification (raw)

- **Rate suite** (official single-file stack, `KOTTABY_TEST_RUNNER_OK=1`, `.env.test.ci`, preloads `test-env → happydom-preload → translation-preload → next-dynamic-mock`, final re-run after ALL changes): **15 pass / 0 fail / 109 expect()**, exit 0 — log `/tmp/task44z-rate-final.log`.
- **Teacher suite** (`TeacherSessionsContainer.test.tsx`, exact 4.2 verify command): **35 pass / 12 skip / 0 fail / 401 expect()**, exit 0 (the 12 pre-existing deliberate environment deferrals, unchanged; zero rate-dialog markup in the teacher DOM — see `/tmp/task44x-teacher-after.log` and sweep log `/tmp/task44z-disp-b2.log`).
- **Student suite** (`StudentSessionsContainer.test.tsx`): **29 pass / 10 skip / 0 fail / 295 expect()**, exit 0 (the 10 pre-existing deliberate environment deferrals, unchanged; `/tmp/task44z-disp-b2.log`).
- **QL** ✅ — `bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates` **exit 0 on all 5 files** (tsgo · oxlint · biome:check · lint:type-aware · check:duplicates): `translation-preload.ts` (`/tmp/task44z-ql-translation-preload.log`), `rate-teacher-dialog.test.tsx` + `.suite.tsx` (`/tmp/task44z-ql-rate-teacher-dialog-*.log`), `StudentSessionsDialogs.tsx` + `StudentSessionsContainer.tsx` (`/tmp/task44z-ql-StudentSessions{Dialogs,Container}.log`; the first container attempt failed oxlint `max-lines-per-function` and was resolved by the `StudentRateDialogSlot` extraction — see 4.2 outcome).
- **Full-suite disposition** — the official single-shot `bun run test:ui:components` **dies mid-run with ZERO test failures and no summary** (bun runner process crash in the admin region, 44s in; log `/tmp/task44z-full-suite.log`), reproducing all three prior full attempts (`/tmp/task44b-full-suite.log`, `/tmp/task44s-full.log`, `/tmp/task44s-full-attempt1.log`). The sanctioned disaggregation — per-file runs of **all 48 component test files** with the identical official preload chain/`--parallel=1` (logs `/tmp/task44z-disp-b1.log`, `/tmp/task44z-disp-b2.log`) — yields **46/48 files exit 0, 620 pass / 30 skip / 1 fail / 3738 expect()** with exactly three environment/OOM notes:
  1. `admin-session-governance/AdminSessionGovernanceContainer.test.tsx` — **exit 137 (SIGKILL/OOM)** after 25 pass / 0 fail (deterministic; same exit already recorded pre-4.4 in `/tmp/task44s-chunks.log` + `/tmp/task44s-individual.log` — pre-existing, same class as the phase0 4 GB-RAM caveat).
  2. `admin/BroadcastComposeContainer.test.tsx` — **exit 134 (SIGABRT)** after 18 pass / 0 fail: every authored test in the file had already passed when the process aborted.
  3. The single-shot full-suite runner itself — process death mid-run in the admin region with no test failures attributable (no `(fail)` lines, no summary printed; the two crash files above are the same failure mode under the aggregate process).
  The only **test failure** repo-wide is the site-footer item below. Skips are all pre-existing deliberate deferrals (teacher 12, student 10, wallet 8).
- **Site-footer verdict (PRE-EXISTING) with evidence** —
  - Current tree (`/tmp/task44z-sitefooter-current.log`): 1 pass / 1 fail — `ar: … snapshot-pins the footer markup` fails on emotion class-hash drift: stored snapshot pins `test-rtl-1b0r344` (social-icon anchors) + `test-rtl-zliyy0` (links-column Stack); runtime renders `test-rtl-1nxin0` + `test-rtl-1jx0gqv` (all other hashes identical).
  - `git stash push -u -m task44z` (ALL 5 modified + 2 untracked 4.4 files stashed; tree verified clean) → re-run on the clean 4.1-era tree (`/tmp/task44z-sitefooter-clean.log`): **same 1 pass / 1 fail with the byte-identical drift signature** (grep count over both logs: 5× `1b0r344` vs 5× `1nxin0`, 1× `zliyy0` vs 1× `1jx0gqv` — identical on both trees). `git stash pop` restored all 7 files (post-pop `git status` identical to pre-stash; canonical copies held in `/tmp/task44z-backup/` throughout).
  - Corroborating: the committed snapshot last changed in #56/#49, long predating this branch — the hashes drifted before any 4.x work.
  - **Disposition: PRE-EXISTING → documented and left as-is.** No `--update-snapshots` regeneration (4.4 must not mask unrelated drift with a test-file-only change).

## Attestations

- **SEC ✅** — the unrated-render and deny paths carry a structural no-network-call proof, asserted in-test: the CTA-matrix renders (pre-confirmation × 3 shapes; already-rated) supply **no mutation mock at all**, so any leaked operation fails the branch as an unmatched MockLink operation; the deny paths (VALIDATION rating-field pair; EVALUATION_ALREADY_SUBMITTED) render against **fully exhausted mock lists** (list + history + exactly one failing mutation), so no second wire call can hide behind them; no generic toast/snackbar may fire on the field-error path (asserted null). The component tier itself is serverless (Happy DOM + `MockedProvider`, no browser, no port, no real network — `test/ui/AGENTS.md`), and the fixtures carry only ids/scores — no credentials, no raw payloads; deny-path assertions assert resolved copy, never error internals.
- **SR ✅** — no server dependency: the suite runs entirely on the Happy-DOM + mocked-Apollo stack (no `run-test.ts` DB wrapper, no server lifecycle, no fixtures/cleanup); behavior is asserted semantically (roles, accessible names, translated copy, cache-converged chips) — **zero `toMatchSnapshot()` in the rate suite** (grep-proven); no snapshots-as-truth (the only markup snapshot in the components tree is site-footer's pre-existing pin, deliberately left unregenerated). Lookup-table enum handling only; zero plan-artifact references in code comments (the suite docblocks document behavior rationale, not plan bookkeeping).
- **IV ✅** — `test/ui/AGENTS.md` read in full and observed: component preloads chain incl. `translation-preload.ts` (warmed `Sessions` accordingly, `test/ui/AGENTS.md:163-165`); label resolution via `<Handle>.getLabels(getTranslations(locale))` with one `locale` per file and the same locale handed to the render wrapper (`sessionSuiteLabels` / `componentSuiteLocales` scaffold helpers); no hardcoded Arabic/English strings (fixture DATA exception documented in the suite header); MUI v9 class-name gotchas applied (`MuiAlert-colorSuccess`, `Mui-error` via className includes); never `oxlint-disable` (root causes fixed, e.g. the `max-lines-per-function` extraction). Tasks.md execution protocol read (per-file QL gate, outcome write-back, checkbox discipline).

## Carry-Forward

- **Site-footer snapshot drift (pre-existing)** — owned outside 4.4; whoever next touches `SiteFooter`/emotion config should re-run the file and regenerate the snapshot in the SAME change that causes a new drift (the verdict evidence above makes the current drift safely attributable to history, not to a future editor).
- **Sandbox OOM envelope** — `AdminSessionGovernanceContainer` (exit 137) and `BroadcastComposeContainer` (exit 134) crash only as processes, never as test failures; the rate suite's dialog-absence-polling deferral (suite header: same-commit poll-safe signals instead of post-settlement `waitFor` absence polls) is the pattern that keeps the new suite OOM-safe under Happy DOM. `componentSuiteLocales` (`STUI_LOCALE`) remains the sanctioned split-run relief.
- **Full-suite single-shot crash** — no test failures attributable; if the infra team wants a green single-shot signal on 4 GB sandboxes, splitting the runner's admin region (or raising the memory envelope) is infra work, not feature work.
- None blocking 5.1/5.2: all four REQ-009.8 surface behaviors are green in-repo via the official stack.
