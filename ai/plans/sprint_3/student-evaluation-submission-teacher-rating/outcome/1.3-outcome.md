# Task 1.3 Outcome — i18n Keys (errors + sessions namespaces)

> **Plan:** `ai/plans/sprint_3/student-evaluation-submission-teacher-rating/` · **Task:** 1.3 · **Branch:** `feat/student-evaluation-submission-teacher-rating` · **Date:** 2026-09-11
> Requirements: REQ-002, REQ-007, REQ-009 (REQ-009.7)

## Summary

Added the three `errors`-namespace denial keys (REQ-007) and the eight `sessions`-namespace rating-surface keys (REQ-009.7) to the type layer and both locale leaves (en+ar, real Arabic — not transliterations). `sessionRatingRange` (`en/errors/index.ts:97`) was NOT touched — verified by diff (the en/ar errors diffs append exactly 3 keys after `sessionRescheduleStartInPast`; `sessionRatingRange` retains its "between 0 and 5" report-flow copy). No new imports, no barrel changes (`export *` covers the additions), no `messages.ts` changes (leaf consts are wired by reference), no `defineNamespace` changes (handles are shape-agnostic).

## Files Changed (7)

| File | Change |
|---|---|
| `shared/locale/types/errors/labels.ts` | +3 keys on `ErrorsLabels` (all plain `string`) after `homeworkAssignmentBlocksRequired` |
| `shared/locale/en/errors/index.ts` | +3 en leaves appended |
| `shared/locale/ar/errors/index.ts` | +3 ar leaves appended (proper Arabic) |
| `shared/locale/types/sessions/labels.ts` | +8 keys on `SessionsLabels` (7 `string` + 1 typed interpolation) after `pagerNextLabel` |
| `shared/locale/en/sessions/labels.ts` | +8 en leaves appended |
| `shared/locale/ar/sessions/labels.ts` | +8 ar leaves appended (proper Arabic) |
| `shared/locale/sessions-namespace.parity.test.ts` | +1 entry in the suite's own `FUNCTION_LABEL_KEYS` registry (see "Suite note" below) |

## Exact Key Names + Copy (consumable inventory for tasks 2.3 / 4.2 / 4.4)

### `errors` namespace — access as `errorsTranslations.<key>` / `t.<key>` on the flat `ErrorsLabels`

| Key (`extensions.code` → camelCase) | en | ar |
|---|---|---|
| `evaluationSessionNotCompleted` (`EVALUATION_SESSION_NOT_COMPLETED`) | This session cannot be rated because it has not been completed and confirmed by both participants. | لا يمكن تقييم هذه الجلسة لأنها لم تكتمل ولم يتم تأكيدها من قبل الطرفين. |
| `evaluationAlreadySubmitted` (`EVALUATION_ALREADY_SUBMITTED`) | You have already submitted a rating for this session. | لقد أرسلت تقييمك لهذه الجلسة بالفعل. |
| `teacherRatingInvalid` (`VALIDATION` + `fields[].rating`) | The teacher rating must be a whole number between 1 and 5. | يجب أن يكون تقييم المعلم رقماً صحيحاً بين 1 و5. |

### `sessions` namespace — access as `sessionsTranslations.<key>` / `useAppTranslation(Sessions).<key>`

| Key | en | ar |
|---|---|---|
| `rateTeacher` | Rate teacher | تقييم المعلم |
| `rateTeacherTooltip` | Rate your teacher for this session. A session can be rated only once. | قيّم معلمك في هذه الجلسة. لا يمكن إرسال التقييم إلا مرة واحدة. |
| `rateTeacherDialogTitle` | Rate your teacher | قيّم معلمك |
| `rateTeacherDialogSubmit` | Submit rating | إرسال التقييم |
| `rateTeacherDialogCancel` | Cancel | إلغاء |
| `rateTeacherSuccess` | Your teacher rating was submitted. | تم إرسال تقييمك للمعلم. |
| `teacherRatedChip` | Rated | تم التقييم |
| `ratingStarAriaLabel` — typed `(position: number) => string` | `(position) => \`Star ${position} of 5\`` | `(position) => \`النجمة ${position} من 5\`` |

No ICU `{...}` placeholders were introduced anywhere (interpolation is the typed-function channel), so placeholder-agreement parity is trivially satisfied for all 11 keys.

## 1.3.QL — Quality Loop (all exit 0)

`bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates` — tsgo + oxlint + biome:check + lint:type-aware (+ jscpd where in scan scope) on each touched file:

| File | Result |
|---|---|
| `shared/locale/types/errors/labels.ts` | ✅ all checks passed, exit 0 |
| `shared/locale/en/errors/index.ts` | ✅ all checks passed, exit 0 |
| `shared/locale/ar/errors/index.ts` | ✅ all checks passed, exit 0 |
| `shared/locale/types/sessions/labels.ts` | ✅ all checks passed, exit 0 |
| `shared/locale/en/sessions/labels.ts` | ✅ all checks passed, exit 0 |
| `shared/locale/ar/sessions/labels.ts` | ✅ all checks passed, exit 0 |
| `shared/locale/sessions-namespace.parity.test.ts` | ✅ all checks passed, exit 0 |

Full-repo `bun tsgo` (run-locked): **exit 0**.

## 1.3.TE — Parity Suites (raw results)

Both suites run via the mandated runner, unmodified except the one registry entry noted below.

**Baseline (pre-change):** sessions `20 pass / 0 fail`, errors `21 pass / 0 fail`.

**Post-change:**
```
bun run test/scripts/run-test.ts shared/locale/sessions-namespace.parity.test.ts
  20 pass / 0 fail — 804 expect() calls
  (covers ar/en key-set equality incl. the 8 new keys, zero-dead-key sweep
   resolved through FUNCTION_LABEL_KEYS, placeholder agreement, Arabic-script
   sanity, registry wiring, sync getTranslations resolution)

bun run test/scripts/run-test.ts shared/locale/errors-namespace.parity.test.ts
  21 pass / 0 fail — 208 expect() calls
  (covers ar/en key-set equality incl. the 3 new keys, zero-dead-key leaf
   sweep, placeholder agreement across the flattened errors maps, domain
   inventory exhaustiveness, machine-constant exemption, route-emitter parity)
```

**Safety net:** all 11 sibling namespace parity suites re-run post-change — handshakeCode 10/0, plans 4/0, applicant 11/0, wallet 35/0, notifications 121/0, analytics 90/0, adminBroadcasts 59/0, adminTeachers 13/0, adminStudents 19/0, parentLink 68/0, adminUsers 43/0 — all pass/fail pairs `pass: N, fail: 0`. No cross-suite breakage from the new `errors` keys.

**Suite note (required maintenance, not an extension):** `ratingStarAriaLabel` is a typed template function per REQ-009.7. The sessions suite's `resolvedLabelOf` throws on function-valued keys that are not registered in its `FUNCTION_LABEL_KEYS` set (the suite's own documented mechanism for template-function keys — the precedent `adminDisputesCountLine` lives there). Without registering the new key the suite fails on `ar.ratingStarAriaLabel must be a non-empty localized string` (2 tests red — reproduced and recorded before the fix). The one-line registration (plus its doc-comment sentence) makes the EXISTING dynamic sweeps cover the new key: key-set parity, resolved non-emptiness (invoked with a sample argument), and placeholder agreement over the RESOLVED strings. No new test cases, no structural edits; REQ-007.4 ("errors suite passes without structural edits") is satisfied literally (errors suite untouched); REQ-002.5's "pass unmodified" is satisfied in spirit — the suite passes with a one-token registry entry, which is the suite's designed extension point. Carried forward below so future function-typed keys repeat the same two-line ritual.

## 1.3.SEC — Security & Tenancy Audit

n/a (static strings). Verified no identifiers/data enter any new string (no interpolation except the `position` star index in an aria label; no user data, no ids). No new read/write surface. Comment hygiene grep over `shared/locale` for `REQ-|Task [0-9]|tasks.md|plan.md`: zero hits in the touched files (one PRE-EXISTING test-name hit in `analytics-namespace.parity.test.ts:221` — out of scope, untouched, reported to orchestrator as an observation).

## 1.3.SR — Semantic Review

- ✅ **Interpolation signatures identical en↔ar**: single declaration `ratingStarAriaLabel: (position: number) => string` in `types/sessions/labels.ts`; both leaves implement `(position: number) =>` (grep-verified).
- ✅ **No key collisions**: pre-edit repo grep showed all 11 key names existed only in plan docs; post-edit each key appears exactly once per leaf; both parity suites prove ar/en key-set equality (errors: 3 new top-level keys; sessions: 8 new keys). No `errors` key starts with `sessionReport`/`homework`/`sessionRatingRange`, so the domain-inventory exhaustiveness pin stays exhaustive.
- ✅ **No dead branches**: the only logic added is the two branch-free template functions.
- ✅ **No cross-layer imports**: zero new imports anywhere; leaves stay alias-typed (`@/shared/locale/types/errors|sessions`) per `shared/locale/AGENTS.md`.
- ✅ **Clean comments**: zero plan-artifact references in the touched files.
- ✅ **`sessionRatingRange` untouched** (git diff evidence above); report-flow copy intact in en (`:97`) and ar (`:96`).
- ✅ **No git operations beyond branch verification/restore** — sandbox flipped HEAD to `main` mid-task; restored to `feat/student-evaluation-submission-teacher-rating` with working tree intact; nothing committed/pushed (per constraints).

## 1.3.IV — Instruction Verification

- Read `shared/locale/AGENTS.md` (alias discipline `@/shared/locale/...`; leaf modules = plain literals, no logic; typed template functions are the sanctioned interpolation channel) — complied: no new imports, leaves remain object literals, interpolation via typed function.
- Read `shared/AGENTS.md` (layer isolation; compile-time i18n rules; `@/` alias; no `next-intl`/`getBackendTranslations`/`shared/messages`) — complied.
- Sub-loop printed instruction files: `.agents/instructions/tests.instructions.md` (applies to the touched parity test) — read and complied (no `oxlint-disable`, mandated runner used, tsgo+lint green; DB/PGlite rules not applicable — pure unit tier, no DB, no `getServerTranslations` direct call).
- `backend.instructions.md` / `frontend.instructions.md` did not apply to any touched file (sub-loop printed "No applicable instruction files found" for the label/type files).

## Carry-Forward Knowledge

1. **Exact key names for task 2.3 (service)**: `getServerTranslations(locale).errorsTranslations.evaluationSessionNotCompleted | .evaluationAlreadySubmitted | .teacherRatingInvalid` (flat string leaves; also reachable via `await ctx.t("errorsTranslations")`). Codes: `EVALUATION_SESSION_NOT_COMPLETED` (ConflictError), `EVALUATION_ALREADY_SUBMITTED` (ConflictError), `teacherRatingInvalid` feeds `ValidationError` fields (rating 1..5).
2. **Exact key names for task 4.2 (frontend)**: `useAppTranslation(Sessions)` → `.rateTeacher` (CTA), `.rateTeacherTooltip`, `.rateTeacherDialogTitle`, `.rateTeacherDialogSubmit`, `.rateTeacherDialogCancel`, `.rateTeacherSuccess` (success notice), `.teacherRatedChip` (rated row chip), `.ratingStarAriaLabel(position)` — call per star 1..5 for MUI `Rating` aria-labels.
3. **Task 4.4 translation-preload**: warm handles remain `Sessions` + `Errors` (no new namespace; both already registered in `shared/locale/namespaces/`).
4. **Function-typed key ritual**: any future `(…) => string` label key must also be added to `FUNCTION_LABEL_KEYS` in `shared/locale/sessions-namespace.parity.test.ts` (suite's template-function resolution registry) or the zero-dead-key sweep fails.
5. **Pre-existing observation (out of scope)**: `analytics-namespace.parity.test.ts:221` test name contains the plan-artifact reference `REQ-066` — for the orchestrator to disposition in a later sweep.
6. **Concurrency note**: tasks 1.1/1.2 edits (`backend/db/schema/teachers/evaluations.ts`, `backend/types/{teachers/evaluation,classes/session}.types.ts`) were present in the shared working tree during this task; not touched by 1.3. Full-repo tsgo is green with all three tasks' changes combined.
