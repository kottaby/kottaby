# Task 1.2 — Canonical Types Outcome

> **Plan:** `ai/plans/sprint_3/student-evaluation-submission-teacher-rating/` · **Task:** 1.2 Canonical Types · **Date:** 2026-09-11
> **Files (both modified, additive only):** `backend/types/teachers/evaluation.types.ts` (+20) · `backend/types/classes/session.types.ts` (+13)

## Summary

Extended the canonical types layer exactly per `plan.md` §2.3 / REQ-004:

1. `backend/types/teachers/evaluation.types.ts` — existing `EvaluationSelectType` (:3) preserved verbatim; added:
   - `EvaluationInsertType = typeof evaluations.$inferInsert` (line 5)
   - `EvaluationReturnType = Omit<EvaluationSelectType, "isDeleted" | "deletedAt" | "notes" | "updatedAt">` (lines 14–17; biome normalizes to a single line at the repo's line width — the four-member omit list is the invariant, not the wrapping)
   - `EvaluationSubmitInput` interface `{ readonly rating: number }` (lines 24–26)
2. `backend/types/classes/session.types.ts` — appended `SessionRatingEligibilityProbeType = Pick<SessionSelectType, "id" | "studentId" | "teacherId" | "status" | "confirmedByTeacherAt" | "confirmedByStudentAt">` (lines 85–88) immediately **after** `SessionTransitionProbeRowType` (:72–75), which is byte-for-byte unmodified (its exact text is pinned by `session.types.static-assertions.test.ts:147-149` — verified intact).

No barrel edits: `backend/types/teachers/index.ts:2` and `backend/types/classes/index.ts:5` already `export *` these modules, and the top-level barrel re-exports both domains.

## Verification Results

| Subtask | Check | Result |
|---|---|---|
| 1.2.QL | `bun run scripts/health/sub-loop.ts backend/types/teachers/evaluation.types.ts --lifecycle duplicates` | **exit 0** (tsgo · oxlint · biome:check · lint:type-aware · check:duplicates all passed; re-run on final content after biome's safe-fix normalization — still exit 0) |
| 1.2.QL | `bun run scripts/health/sub-loop.ts backend/types/classes/session.types.ts --lifecycle duplicates` | **exit 0** (same five checks; re-run on final content — exit 0) |
| 1.2.TE | Type-level correctness | tsgo-enforced: `bun run tsgo` = **0 errors** with the new types consumed (see barrel check); no runtime tests exist/needed for type aliases — nothing added |
| 1.2.SEC | Omit-list audit | Omit list is exactly `"isDeleted" \| "deletedAt" \| "notes" \| "updatedAt"` per REQ-004.1 — soft-delete internals + free-text notes + server-managed update stamp are structurally unreachable by GraphQL consumers (Pothos task 3.1 must ref `EvaluationReturnType`, so it cannot leak). Type-level negative proof: `["isDeleted"|"deletedAt"|"notes"|"updatedAt"] extends [keyof EvaluationReturnType] ? false : true` evaluated to `true` in the scratch check below; positive proof that `id`/`evaluatedId`/`evaluatorId`/`sessionId`/`score`/`createdAt` remain |
| 1.2.SR | Semantic review | See checklist below — all clean |
| 1.2.IV | Instructions | See attestation below |

**Barrel-import verification (type-level consumer):** a scratch file (`barrel-consumer-scratch.check.ts`, deleted after the run) imported all four new symbols **through `@/backend/types` only** and asserted with `Equals`/conditional-type probes: insert payload keys = the exact four repo-pick columns; ReturnType omit-list negative + core-fields positive; `EvaluationSubmitInput` mutual-assignability with `{ readonly rating: number }`; probe exactly equal to the six-column Pick of `SessionSelectType` and closed against `feeHeld`/`startedAt`/`updatedAt`. `bun run tsgo` = **0 errors** with the scratch present → every symbol resolves via the barrel with no barrel edit. Scratch removed; `bun run tsgo` re-run = **0 errors** clean.

### 1.2.SR Semantic Review — checklist (all verified)

- **No duplicate probe types:** pre-edit grep across the repo for `SessionRatingEligibilityProbeType|EvaluationReturnType|EvaluationInsertType|EvaluationSubmitInput` matched only plan artifacts (specs/plan/tasks/review) — zero source occurrences; no shadow definitions created. Within `session.types.ts` the only pre-existing probe is `SessionTransitionProbeRowType` (kept untouched; distinct member set). The admin-domain `GovernanceProbeRowType` (`backend/types/admin/admin-user.types.ts:289`) is a different entity's local shape — no conflict.
- **No new local types elsewhere:** only the two assigned files were edited (diff-stat: +20/+13, additive); no other file was created or modified for this task.
- **No dead branches:** no runtime code, conditionals, or unused exports added — all four symbols have downstream consumers named in the plan (REQ-005.2/3 → 2.1; REQ-006 → 2.3; REQ-008 → 3.1).
- **No cross-layer imports:** both files' import blocks unchanged (schema-only + the pre-existing enum type imports); no `@/frontend/**`, `@/shared/**`, or relative escapes introduced.
- **No manual ReturnType construction:** `EvaluationReturnType` derives from `EvaluationSelectType` (`typeof evaluations.$inferSelect`) via `Omit` — the repo's established inference pattern (per `backend/types/AGENTS.md` example); probe is a `Pick` projection of `SessionSelectType`, mirroring the adjacent transition-probe idiom.
- **Clean comments:** JSDoc domain descriptions only (GraphQL-facing visibility rationale; star→score conversion ownership; probe read-only role + unique-constraint arbitration). Zero plan-artifact references (no REQ-x / Task N / plan paths) — consistent with `session.types.static-assertions.test.ts` test 9's grep, which scans raw file content.
- **Existing type suites unbroken:** `SessionTransitionProbeRowType` exact-text pin (:147-149), `Omit<` prohibition for `session.types.ts` (test 1), and forbidden-pattern scans (test 8) all still hold — no `Omit`, `any`, spread, or `as unknown` introduced in that file.

### 1.2.IV Instruction Verification — attestation

Read and complied with: `backend/types/AGENTS.md` (single canonical type per entity, `$inferSelect`/`$inferInsert` foundations, barrel mechanics, schema ground truth), the sub-loop-printed rule files — `.agents/instructions/backend.instructions.md` (type-definition pattern, no local Pothos types, `@/backend/types` import rule), root `AGENTS.md` (quality workflow, barrel conventions, no plan-artifact/dead-code rules), and `backend/AGENTS.md` (layer flow, type-location rules). No violations found; no oxlint-disable comments anywhere.

## Carry-Forward Knowledge (for tasks 2.1 / 2.3 / 3.1)

Import all of these from **`@/backend/types`** (barrel) — never redefine locally:

| Type | Shape (post-Task-1.2) | Consumer |
|---|---|---|
| `EvaluationSelectType` | `typeof evaluations.$inferSelect` (pre-existing) | 2.1 repo return type; 2.3 mapper input |
| `EvaluationInsertType` | `typeof evaluations.$inferInsert` | 2.1 `insertOnce(values: Pick<EvaluationInsertType, "evaluatedId" \| "evaluatorId" \| "sessionId" \| "score">, tx)` |
| `EvaluationReturnType` | `Omit<EvaluationSelectType, "isDeleted" \| "deletedAt" \| "notes" \| "updatedAt">` | 2.3 service returns; 3.1 `objectRef<EvaluationReturnType>("Evaluation")` |
| `EvaluationSubmitInput` | `interface { readonly rating: number }` | 2.3 `submitTeacherEvaluation(..., input: EvaluationSubmitInput)`; 3.1 Pothos input (rating Int!) |
| `SessionRatingEligibilityProbeType` | `Pick<SessionSelectType, "id" \| "studentId" \| "teacherId" \| "status" \| "confirmedByTeacherAt" \| "confirmedByStudentAt">` | 2.1 `findRatingEligibilityProbe(sessionId, tx): Promise<SessionRatingEligibilityProbeType \| null>`; 2.3 gate reads |

Field-type notes for 2.1/2.3: `evaluations.score` is `number | null` (column nullable, CHECK 0–100); `sessionId` is `number | null`; `isDeleted` is `boolean | null` (tri-state — the repo's soft-delete exclusion must accept `false` OR `null`); probe `status` is the `sessionStatus` pgEnum string-literal union (drizzle `$inferSelect` shape), stamps are `Date | null`.

## Cross-File Dependencies / Environment Notes

- **Sandbox branch-flip quirk recurred:** HEAD flipped to `main` mid-task. `git checkout feat/...` was attempted per protocol but git **refused** ("local changes … would be overwritten by checkout") because parallel Task 1.1/1.3 agents have uncommitted edits to `tasks.md`/`worklog.md` in the shared tree. No force-checkout was performed (it would have destroyed their work); no commit/push was made. All deliverable edits are present in the working tree (verified post-flip); tsgo/sub-loop results are content-based and unaffected. A subsequent concurrent flip reverted the working-tree `tasks.md` to the feat-committed version (losing uncommitted 1.1/1.2/1.3 flips); 1.2's header + five sub-checkboxes were re-flipped after re-verifying file state — orchestrator should reconcile branch state at the next commit point.
- Parallel-agent modifications observed in the shared tree during this task (NOT touched by 1.2): `backend/db/schema/teachers/evaluations.ts` (1.1), `shared/locale/**` (1.3), `tasks.md` (1.1 flips + these 1.2 flips), `worklog.md`, `outcome/1.1-outcome.md`.
- `backend/types/teachers/evaluation.types.ts` had no static-assertions suite; none needed. If one is added later, note biome renders the `Omit` single-line at repo line width.
