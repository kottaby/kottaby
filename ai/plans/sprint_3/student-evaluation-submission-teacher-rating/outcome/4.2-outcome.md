# Task 4.2 Outcome — Hook + Dialog + CTA wiring (student teacher-rating)

> **Plan:** `ai/plans/sprint_3/student-evaluation-submission-teacher-rating/` · **Task:** 4.2 (Phase 4 — frontend hook/dialog/CTA) · **Branch:** `feat/student-evaluation-submission-teacher-rating` · **Date:** 2026-09-13
> Requirements: REQ-009 (REQ-009.2 gate, REQ-009.5/6 paths), REQ-011, REQ-002 · **State:** VERIFIED — all gates green.

## Summary

The student's teacher-rating flow is wired end-to-end with zero refetches: the rated-set read (`useMyTeacherEvaluations`) collapses `myTeacherEvaluations` rows into the session-id set gating the Rate CTA; the Rate CTA descriptor (`rate` row action) renders on dual-confirmed rows and yields to a read-only rated chip once the session is in the rated set; the row's intent opens a single rate-dialog slot (same slot system as cancel/dispute) hosting `RateTeacherDialog` — MUI `Rating` 1..5 whole stars, submit disabled until a star is chosen, dismissal gated while the mutation is pending; the dialog owns the `submitTeacherEvaluation` mutation with a cache-`update` prepend writer (`updateCacheOnSubmitted`) so the rated set converges in place; and the submit-error classification was EXTRACTED into `rateTeacherMutationError.ts` (module-scope arms function) so the dialog stays the seam only. The error-link mapping table gained the two REQ-009.5 rows — `EVALUATION_SESSION_NOT_COMPLETED` (gate reject, error-tone notice with its OWN denial copy) and `EVALUATION_ALREADY_SUBMITTED` (write-once reject, info-tone notice, success-equivalent-adjacent but NOT `duplicateSuccessEquivalent`) — with their branch-table unit tests; the pure suite stays green (31 pass).

## Files created/modified

Working-tree `git status` (this session's surface — the extraction fix + QL-fix re-sorts):

```
 M frontend/views/student/sessions/RateTeacherDialog.tsx
 M frontend/views/student/sessions/SessionRowActions.tsx
 M frontend/views/student/sessions/StudentSessionsContainer.tsx
 M frontend/views/student/sessions/useMyTeacherEvaluations.ts
 M frontend/views/student/sessions/useStudentSessionConfirm.ts
?? frontend/views/student/sessions/rateTeacherMutationError.ts
```

Full task-4.2 file set (implementation landed in the survival commit `8e7dff2` "wip(4.2): rate dialog, hook, CTA wiring"; this session extracted the error arms + fixed their import):

| File | Status | Content |
|---|---|---|
| `frontend/views/student/sessions/RateTeacherDialog.tsx` | NEW | The rating dialog: MUI `Rating` 1..5 whole stars (`*Outlined` icons), per-star `sessions.ratingStarAriaLabel(position)` aria labels, `React.SubmitEvent` form submit, `aria-busy` in-flight submit, dismissal gated while pending, full-screen under `sm`, reduced-motion `timeout: 0`, ≥44px touch targets; owns `useMutation(submitTeacherEvaluationMutationDocument)` with `update: updateCacheOnSubmitted`; delegates error classification to `handleRateTeacherMutationError` |
| `frontend/views/student/sessions/useMyTeacherEvaluations.ts` | NEW | Zero-variable `useQuery(myTeacherEvaluationsQueryDocument)` → `deriveRatedSessionIds` (`Set<number>`, `sessionId: null` rows skipped) + monotonic local `markSessionRated` markers ∪ cache rows; fail-open on a settled error (empty set — the write-once reject arm is the honest recovery); exports `updateCacheOnSubmitted` (cache `modify` PREPEND of the normalized `Evaluation` ref to `ROOT_QUERY.myTeacherEvaluations` — no refetch, no typePolicies change) |
| `frontend/views/student/sessions/useStudentSessionRateArms.ts` | NEW | Container wiring table for the five dialog outcome arms (success/missing/not-completed/already-submitted/failure): terminal arms drop the stale row alert + close the dialog slot; `handleAlreadySubmitted` marks the row rated locally; `handleFailure` leaves the dialog open for retry; not-completed/already-submitted notices ride the mapped app-scope surface, so their local arms carry only bookkeeping |
| `frontend/views/student/sessions/rateTeacherMutationError.ts` | NEW (extraction) | Module-scope `handleRateTeacherMutationError(error, arms)` — branch order preserved: not-found family → `evictSessionFromListFields` + `onSessionMissing`; `EVALUATION_ALREADY_SUBMITTED` → `onAlreadySubmitted`; `EVALUATION_SESSION_NOT_COMPLETED` → `onSessionNotCompleted`; `VALIDATION` with `extensions.fields[]` addressed at `rating` → server-localized inline field error; `FORBIDDEN` → `onFailure(errors.forbidden)`; else → `onFailure(sessions.genericError)`. Codes normalized via `normalizeGraphQLErrorCode`/`isNotFoundErrorFamily` (error-link.map) |
| `frontend/views/student/sessions/StudentSessionsContainer.tsx` | EXTEND | `useMyTeacherEvaluations` read; `rateArms = useStudentSessionRateArms(...)`; rated set + `onRate` into `StudentSessionsBody`; rate-dialog slot + arms into `StudentSessionsDialogs` |
| `frontend/views/student/sessions/StudentSessionsDialogs.tsx` | EXTEND | Third single-slot dialog: `RateTeacherDialog` mounted UNMOUNTED-KEYED per session (`key={sessionId}`) so every open starts from draft state |
| `frontend/views/student/sessions/StudentSessionsBody.tsx` | EXTEND | Rows branch passes `ratedSessionIds` + `onRate` into `studentActionsForSession` |
| `frontend/views/student/sessions/useStudentSessionConfirm.ts` | EXTEND | `studentActionsForSession` rate gate (REQ-009.2): rate descriptor ONLY on `Completed` ∧ BOTH stamps present ∧ not in the rated set; rated-set hit → read-only chip; confirm descriptor unchanged on the exactly-once pending shape |
| `frontend/views/student/sessions/sessionRowAction.ts` | EXTEND | `id` union + `"rate"`; `readOnly` affordance (non-interactive chip) + optional `onIntent` |
| `frontend/views/student/sessions/sessionRowPresentation.ts` | EXTEND | `RATE_ELIGIBLE_STATUSES` Record lookup (`Completed`) — enum-safety table, never `===` on the enum |
| `frontend/views/student/sessions/SessionRowActions.tsx` | EXTEND | `readOnly` actions render a Chip (tone-palette `sx`) instead of a Button; render key fix |
| `frontend/views/student/sessions/useStudentSessionDialogSlots.ts` | EXTEND | Third single-dialog slot: `rateDialogSessionId` + `openRateDialog`/`closeRateDialog` |
| `frontend/providers/apollo/error-link.map.ts` | EXTEND | Rows 9–10 of the inline-notice table: `EVALUATION_SESSION_NOT_COMPLETED` → `noticeKind: "conflict"`, `messageKey: "evaluationSessionNotCompleted"`, tone `error`, non-retryable; `EVALUATION_ALREADY_SUBMITTED` → tone `info`, non-retryable, no `duplicateSuccessEquivalent`; summary table rows documented |
| `frontend/providers/apollo/error-link.map.test.ts` | EXTEND | 2 branch-table rows pinning both mappings (own denial copy; info-tone; exclusivity of `duplicateSuccessEquivalent` to `DUPLICATE_REQUEST`) |
| `test/ui/components/student/StudentSessionsContainer.suite.tsx` | EXTEND | `emptyRatedSetMock()` appended to EVERY render's mock list — the container's new parallel `myTeacherEvaluations` read gets a settled empty shape; rate-history shapes are 4.4's dedicated cases |

Locale keys (pre-existing from the 1.x/3.x phase — consumed, not added, here): `sessions.rateTeacherDialogTitle/Submit/Cancel`, `sessions.rateTeacherSuccess`, `sessions.rateTeacher`, `sessions.rateTeacherTooltip`, `sessions.teacherRatedChip`, `sessions.ratingStarAriaLabel(position)`, `errors.evaluationSessionNotCompleted`, `errors.evaluationAlreadySubmitted` (both locales + typed label interfaces).

## Prototype fidelity (from `prototype/screens.json` + code; PNGs NOT opened)

The prototype defines 6 surfaces (`screens.json` screenId + file + state):

| Surface | State | Code realization (followed) |
|---|---|---|
| `sessions-list-rate-cta-desktop.png` / `-mobile.png` | `rate-cta-visible` | The row-level Rate CTA: `SessionRowAction { id: "rate" }` renders as an outlined `Button` with the one-shot consequence tooltip (`rateTeacherTooltip`), testid `session-action-<sessionId>-rate`, appearing exactly on dual-confirmed `Completed` rows not yet rated (REQ-009.2 gate) |
| `rate-teacher-dialog-desktop.png` / `-mobile.png` | `rating-dialog-open` | The Rate Teacher modal: `DialogTitle` = `rateTeacherDialogTitle` ("Rate your teacher" / "قيّم معلمك"), MUI `Rating` 1..5 whole stars with per-star aria template, Cancel + Submit actions, submit disabled until a star is chosen; `maxWidth="xs"`, `fullScreen` under the `sm` breakpoint = the mobile dialog surface |
| `sessions-list-rated-success-desktop.png` | `rated-success` | Success path: cache prepend (`updateCacheOnSubmitted`) → `sessions.rateTeacherSuccess` success snackbar → dialog closes → the CTA yields to the read-only rated chip (`teacherRatedChip`) on the next render |
| `sessions-list-error-not-completed-mobile.png` | `error-not-completed` | `EVALUATION_SESSION_NOT_COMPLETED` mapped row: localized error-tone notice (the code's OWN denial copy, not the generic conflict copy), non-retryable, rendered app-scope; the dialog slot closes |

Divergences inferable from the code/docs (pixel-level comparison not performed — PNGs not opened per instruction):

- The rated end-state is a read-only Chip in the row's action slot (`readOnly` affordance added to the generic `SessionRowAction` contract) rather than an entirely removed affordance — the chip makes the write-once end-state explicit.
- The write-once rejection (`EVALUATION_ALREADY_SUBMITTED`) surfaces as a NEUTRAL INFO notice (never an error treatment) with `duplicateSuccessEquivalent` deliberately NOT set — the map row + its unit test pin that the success-equivalent flag stays exclusive to `DUPLICATE_REQUEST`.
- VALIDATION denials addressed at `rating` render INLINE under the stars (server-localized field pair) instead of app-scope; every other failure keeps the dialog open for retry.

## Verification results

- **QL sub-loop** (`bun run scripts/health/sub-loop.ts frontend/views/student/sessions/rateTeacherMutationError.ts --lifecycle duplicates`): **exit 0** — tsgo · oxlint · biome:check · lint:type-aware · check:duplicates all passed (log `/tmp/task42t-ql.log`). First run FAILED with a REAL extraction bug: `TS2305: Module 'sessionListCacheEviction' has no exported member 'isNotFoundErrorFamily'` — the symbol is exported by `@/frontend/providers/apollo/error-link.map` (line 72, alongside `normalizeGraphQLErrorCode`); the import in `rateTeacherMutationError.ts` was corrected in-file (in-scope) and the sub-loop re-run green. The other 13 task files were QL-gated exit 0 in the prior session (handoff: 14/14 files).
- **Test A** — `bun run test/scripts/run-test.ts frontend/providers/apollo/error-link.map.test.ts`: **31 pass / 0 fail / 109 expect() calls** across 1 file, exit 0. Includes the two new mapping rows (`EVALUATION_SESSION_NOT_COMPLETED`, `EVALUATION_ALREADY_SUBMITTED`).
- **Test B** — single-file component suite with the official preload chain (`TEST_SERVER_MODE=production bun --env-file=.env.test.ci test --parallel=1 ./test/ui/components/student/StudentSessionsContainer.suite.tsx --preload test-env → happydom → translation-preload → next-dynamic-mock`): **28 pass / 10 skip / 0 fail / 292 expect() calls** across 1 file, exit 0. The 10 skips are the PRE-EXISTING deliberate environment deferrals (dispute typed 6c/6d, confirm-`SESSION_NOT_FOUND` 6h, cancel typed 7, list `SESSION_NOT_FOUND` 8 — × both locales), unchanged and unrelated to rating. The suite passed WITHOUT the `Sessions`/`Errors` translation-preload warming — see the 4.4 carry-forward for why (the 4.4 dependency is therefore a NOTE, not a live failure).
- Environment notes (worktree, no effect on repo state): the gitignored `.env.test` was absent in the fresh worktree (test A's `run-test.ts` hard-requires it) — materialized locally from `.env.test.ci` with a localhost `DATABASE_URL` (gitignored, uncommitted); bun requires the `./` path prefix for non-`.test.*` filter paths (the bare path matches zero files); the repo's direct-`bun test` guard is bypassed with `KOTTABY_TEST_RUNNER_OK=1`, exactly as the guard message prescribes for the sanctioned single-file invocation.

## Attestations

- **SEC** — `console\.` grep across ALL touched view/provider files (`RateTeacherDialog.tsx`, `useMyTeacherEvaluations.ts`, `useStudentSessionRateArms.ts`, `rateTeacherMutationError.ts`, `StudentSessionsContainer.tsx`, `StudentSessionsDialogs.tsx`, `StudentSessionsBody.tsx`, `useStudentSessionConfirm.ts`, `useStudentSessionDialogSlots.ts`, `sessionRowAction.ts`, `sessionRowPresentation.ts`, `SessionRowActions.tsx`, `error-link.map.ts`): **zero hits**; deny-paths never log sensitive payloads — the mapped surface resolves to action objects, the local arms only touch UI state; the mutation carries ONLY `input: { rating }` + `sessionId` (identities stay server-derived per 3.4's wire matrix); the error arms carry resolved COPY STRINGS (`validationCopy`/`forbiddenCopy`/`genericErrorCopy`), never raw wire payloads.
- **SR** — enum safety: rate eligibility is a Record lookup (`RATE_ELIGIBLE_STATUSES`, `CONFIRM_PENDING_STATUSES`, `DISPUTED_STATUS`) — no `===` on enum members anywhere in the touched code (the wire error-code constants in `rateTeacherMutationError.ts` are string-literal lookup keys against `normalizeGraphQLErrorCode` output, the file's own documented pattern); no hardcoded colors/fake data: hex-code grep over the touched files → **zero hits** (all color comes from theme palette / MUI color tokens via `sx`); no direct style props on MUI components (`sx`-only); no `console.*`; zero plan-artifact references in code comments (grep `REQ-|tasks.md|plan.md|outcome/` over the touched files → zero) — docblocks are domain-only; generated-type imports only (`MyTeacherEvaluationsQuery_myTeacherEvaluations`, `SubmitTeacherEvaluationMutation` from `graphql.ts`), no inline type literals beyond the documented arms interfaces.
- **IV** — rule files read via the sub-loop's applicable list: `AGENTS.md`, `frontend/AGENTS.md`, `frontend/views/AGENTS.md`, `.agents/instructions/frontend.instructions.md`; AGENTS.md files NOT modified (hand-curated rule, REQ-014.3).

## Carry-Forward Knowledge (for 4.4 — component coverage)

1. **Export surface to drive in tests**
   - `RateTeacherDialog` props: `{ sessionId: string; open: boolean; onClose(): void; onRated(sessionId: string); onSessionMissing(sessionId: string); onSessionNotCompleted(sessionId: string); onAlreadySubmitted(sessionId: string); onFailure(message: string) }` — mounted through `StudentSessionsDialogs` with `key={sessionId}` (fresh draft state per open).
   - `useMyTeacherEvaluations()` → `{ ratedSessionIds: ReadonlySet<number>; markSessionRated(sessionId: number); loading: boolean; error: unknown }`; pure helpers exported for the suite tier: `deriveRatedSessionIds(rows)`, `updateCacheOnSubmitted(cache, result)`; row type alias `TeacherEvaluationRow`.
   - `useStudentSessionRateArms(deps)` → `{ handleRated, handleSessionMissing, handleSessionNotCompleted, handleAlreadySubmitted, handleFailure }`; deps: `StudentSessionNoticeWiring` + `sessionsCopy` (`SessionsLabels`) + `errorsCopy` (`ErrorsLabels`) + `closeRateDialog` + `markSessionRated`.
   - `handleRateTeacherMutationError(error, arms)` + `RateTeacherErrorArms` (`rateTeacherMutationError.ts`) — the pure classification seam; a hook-logic/unit tier can pin the branch table directly (not-found → eviction + `onSessionMissing`; already-submitted; not-completed; VALIDATION `fields[]` → `rating` inline; FORBIDDEN; generic).
   - Suite scaffold: `sessionSuiteLabels(locale)` (in `test/ui/components/helpers/containerSuiteScaffold.tsx`) resolves `t/te/tc` for rate-dialog assertions; `emptyRatedSetMock()` (in `test/ui/components/student/StudentSessionsContainer.suite.tsx`) answers the parallel read — rate-history shapes (rated set non-empty) are 4.4's dedicated cases.
2. **i18n handles used** — `Sessions` namespace: `rateTeacherDialogTitle`, `rateTeacherDialogSubmit`, `rateTeacherDialogCancel`, `rateTeacherSuccess`, `rateTeacher`, `rateTeacherTooltip`, `teacherRatedChip`, `ratingStarAriaLabel(position)` (function-label key), `genericError`; `Errors` namespace: `validation`, `forbidden`, `sessionNotFound`, plus `evaluationSessionNotCompleted` / `evaluationAlreadySubmitted` resolved through the error-link map's `messageKey`s (app-scope notices).
3. **translation-preload warming need** — `test/ui/components/translation-preload.ts` warms Landing/Auth/Applicant/Errors/Notifications/Common/Dashboard/HandshakeCode/ParentLink/Admin* but **NOT the `Sessions` handle**. The current suite passes because its copy assertions resolve via the scaffold's `sessionSuiteLabels` (direct `getLabels` on the preloaded full-bundle translations, both locales). If 4.4's rate-dialog cases assert through the `Sessions` namespace handle at preload time (missing-key-drift posture), add `Sessions.getLabels(translations)` to the warming loop (both locale bundles). `Errors` is already warm. Recorded as a KNOWN 4.4 dependency — NOT a 4.2 failure (the suite ran 0-fail without it).
4. **Coverage split per the plan** — 4.2.TE scopes component coverage of the rate flow to 4.4; 4.2 verified the hook through the dialog component path (suite green with the parallel read mocked settled) and the two new mapping rows through the error-link unit suite.
