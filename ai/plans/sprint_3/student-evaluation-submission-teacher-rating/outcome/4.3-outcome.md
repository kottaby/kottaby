# Task 4.3 — Notification Deep-Link (SessionCompletion → student sessions route)

> **Plan:** `ai/plans/sprint_3/student-evaluation-submission-teacher-rating/` · **Status:** COMPLETE

## Summary

- Created the single-sourced `STUDENT_SESSIONS_ROUTE` leaf-module constant in `frontend/lib/notification-route-resolution.ts` (the `STUDENT_LINK_REQUESTS_ROUTE` precedent: leaf module, directive-free, framework-free) and mapped `NotificationType.SessionCompletion → STUDENT_SESSIONS_ROUTE` in `NOTIFICATION_ROUTE_BY_ENTITY_TYPE` — enum-member key in the existing lookup table, no bare literals, no `===` on enums.
- Migrated the student nav item (`frontend/views/dashboard/nav/navItems.ts`) from the `"/student/sessions"` literal to the imported `STUDENT_SESSIONS_ROUTE`; the docblock canonical-retargets note now records the shared constant. The route is genuinely single-sourced: the nav item and the session-completion notification deep link are the two cross-surface consumers and both import the ONE constant.
- Extended `test/ui/components/notifications/notification-deep-link.test.tsx`: a drawer-level `session_completion` row test (runs across both locales) + two table-driven pure-resolver cells — a full mapped-route table (both mapped entity types → their constants) and an exhaustive fallback-preservation table (every unmapped backend enum member + the issue-#99 free-string refinements + an out-of-vocabulary value + the absent pointer → `/notifications`).

## Files

| File | Change |
|---|---|
| `frontend/lib/notification-route-resolution.ts` | EXTEND — `STUDENT_SESSIONS_ROUTE` constant + `SessionCompletion` map entry |
| `frontend/views/dashboard/nav/navItems.ts` | EXTEND — nav literal → `STUDENT_SESSIONS_ROUTE` import |
| `test/ui/components/notifications/notification-deep-link.test.tsx` | EXTEND — table-driven mapping + fallback cells, drawer `session_completion` row |
| `ai/plans/sprint_3/.../outcome/4.3-outcome.md` | NEW — this file |
| `tasks.md`, `worklog.md` | 4.3 + sub-checkboxes `[x]`; 4.3 worklog entry |

## Verification

- **QL** ✅ — sub-loop `--lifecycle duplicates` exit 0 on all three touched files (tsgo · oxlint · biome:check · lint:type-aware · check:duplicates; test file is outside the jscpd scan scope and passes the rest of the stack). Logs: `/tmp/task43-subloop-*.log`.
- **TE** ✅ — deep-link suite: **13 pass / 0 fail / 38 expect()** (8 drawer cells — 4 per locale, including the new `session_completion` row anchoring exactly to `STUDENT_SESSIONS_ROUTE`; 5 pure resolver cells, including the 2 new table-driven ones). Log: `/tmp/task43-test-run-dom2.log` (sanctioned preload stack: `test-env` + `happydom-preload` + `translation-preload` + `next-dynamic-mock`, `.env.test.ci`). Note: the bare `bun run test/scripts/run-test.ts <path>` wrapper lacks the DOM preload stack, so its run proves the 5 pure resolver cells green while the DOM-tier drawer cells need the sanctioned `test:ui:components` preload chain (recorded, not a failure). Adjacent nav suite re-run: **39 pass / 0 fail / 175 expect()** (`/tmp/task43-nav-test.log`).
- **SEC** ✅ — the navigation target `app/(dashboard)/student/sessions/page.tsx` is already role-guarded: `withPageAuth({ roles: [UserRole.Student], redirectTo: "/student/sessions" })` (the server guard is the ONLY authorization boundary; anonymous → `/login?redirect=…`, role mismatch → own role dashboard). The deep link adds no new surface and no new guard — it merely routes completion notifications to an existing student-only page (REQ-011 posture untouched).
- **SR** ✅ —
  - *Literal single-sourcing (repo grep `"/student/sessions"`)*: app-code consumers are down to ONE definition — the leaf constant. Remaining hits, all accounted for: the constant itself; the page's own `withPageAuth redirectTo` self-reference (definition-site precedent — `app/(dashboard)/student/link-requests/page.tsx` keeps its identical literal after its single-sourcing task); `scripts-e2e/ui-capture-all.sh:48` (a bash screenshot tool that cannot import a TS constant — reported straggler, not force-migrated); frozen-value pins inside the test files (the established `navItems.test.ts:288` convention); historical `ai/finished_plans/` artifacts.
  - *Lookup tables, no `===` on enums*: map keys are enum members; grep proves zero enum-equality comparisons in the touched files.
  - *No dead branches*: `NotificationDrawerBody.tsx:111` resolves every drawer row's `relatedEntityType` through the map — the new entry is reachable by construction (backend writes `session_completion` for `SessionCompletion` rows).
  - *No cross-layer imports*: no new import edges (nav → leaf lib and leaf lib → backend enum are the pre-existing, by-design edges).
  - *Clean comments*: zero plan-artifact references in the touched files (grep-proven); *no hardcoded colors*: no styling touched (grep-proven).
- **IV** ✅ — `frontend/AGENTS.md` read and observed: cross-surface navigation targets MUST single-source route strings in a leaf lib module (the exact rule this task implements); enum safety via `Record` lookup tables, never `===`/switch; no hardcoded colors; never `oxlint-disable`. Frontend instructions from the task brief followed (leaf-module constant, table-driven tests, sub-loop gate).

## Carry-Forward

- `scripts-e2e/ui-capture-all.sh:48` holds the literal for screenshot capture — unfixable via constant import (bash); if the repo ever wants it parameterized, it would need an emitted-routes codegen step (deferred, not a `deferred-items.md` entry: zero behavior risk).
- Task 4.2/4.4 (Rate CTA/dialog): the deep link lands on the sessions list; once the Rate action ships, the completion-notification journey is end-to-end. No dependency of 4.3 on 4.2 code.
- Sandbox git-restore warfare recurred (HEAD + differing tracked files repeatedly reset to `main` between invocations). Countermeasure that held: `/tmp/task43-backup` canonical copies + `/tmp/task43-restore.sh` (rebuilds `tasks.md`/`worklog.md` from the feat blobs, re-applies code edits, verifies). If a later task finds the working tree reverted, re-run `bash /tmp/task43-restore.sh`.
