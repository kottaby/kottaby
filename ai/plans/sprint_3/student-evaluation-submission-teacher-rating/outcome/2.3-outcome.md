# Task 2.3 Outcome — Service: StudentEvaluationService (+ barrel + service tests) — journey GREEN

> **Plan:** `ai/plans/sprint_3/student-evaluation-submission-teacher-rating/` · **Task:** 2.3 (Phase 2) · **Branch:** `feat/student-evaluation-submission-teacher-rating` · **Date:** 2026-09-12
> Requirements: REQ-006, REQ-007, REQ-011, REQ-012, REQ-013.2
> **State:** COMPLETE — all gates green; the task-2.2 journey is GREEN (the plan's stop condition).

## Summary

Implemented `StudentEvaluationService` in NEW `backend/services/teachers/student-evaluation.service.ts` per the plan's service-flow contract: pre-DB guard order (shared `assertPositiveSafeSessionId` id guard — reused, not re-implemented — then the whole-star 1..5 rating guard projecting exactly the `rating` field), then ONE `withTransaction(outerTx, …)` (SAVEPOINT on a caller's transaction; own top-level transaction in production) resolving the non-locking `SessionRepository.findRatingEligibilityProbe`, the participant oracle collapse (`probe?.studentId !== studentUserId` → `NotFoundError("SESSION", t.sessionNotFound)` — foreign ≡ nonexistent, byte-identical), the dual-confirmation completion gate (status + both stamps → `ConflictError("EVALUATION_SESSION_NOT_COMPLETED", …)`), and `EvaluationRepository.insertOnce` with every stored column server-derived (rated subject from the probe row, rater = caller id — BOLA-safe, score = `rating × 20`); a `23505` on the cause chain maps to `ConflictError("EVALUATION_ALREADY_SUBMITTED", …)`, every other failure rethrows untouched. `listMyTeacherEvaluations` delegates to `EvaluationRepository.listByEvaluator` (caller-scoped, tx/cold dual path) and maps member-by-member to `EvaluationReturnType` (soft-delete/notes/updatedAt stripped, no spread). Every denial logs exactly ONE bounded `logger.logDomainError` (code, entity, entityId, locale — never the payload); success paths and the read surface log nothing. No lifecycle writes, no notifications/audit, no module-level state. Barrel export added to `backend/services/teachers/index.ts`. Service tests in NEW `backend/services/teachers/student-evaluation.service.test.ts` (template: `recitation.service.test.ts`). The journey (`test/workflows/teachers/student-teacher-rating.journey.test.ts`) went GREEN through the unmodified barrel seam.

Lint-driven, semantics-preserving notes: the oracle condition is the optional-chain form of the plan's `probe === null ||` comparison (type-aware lint mandate; identical truth table, recitation-service precedent); small file-local helpers (`logDenial`, `ratingFieldError`, `submitWithinTransaction`) were extracted to satisfy `max-lines-per-function` — the public namespace still exports EXACTLY the two plan functions, and the pipeline order is unchanged.

## Files (3)

| File | Status | Content |
|---|---|---|
| `backend/services/teachers/student-evaluation.service.ts` | NEW | `StudentEvaluationService.submitTeacherEvaluation` + `listMyTeacherEvaluations` |
| `backend/services/teachers/index.ts` | EXTEND | + `export * from "./student-evaluation.service"` |
| `backend/services/teachers/student-evaluation.service.test.ts` | NEW | Tier 1–4 service-test matrix (19 tests) |

## Verification results

**Environment:** `.env` `DB_PROVIDER=postgres` verified; PostgreSQL 17 up (`pg_ctl status`, port 5432); branch verified before every command (the sandbox reverts HEAD to `main` between invocations — every step ran against a re-restored `feat/student-evaluation-submission-teacher-rating`, untracked deliverables re-copied from `/tmp/task23-backup`).

### 2.3.QL — Quality Loop (mandated sub-loop, `--lifecycle duplicates`)

Exit 0 on all three files (tsgo project-wide filtered, oxlint, biome:check, lint:type-aware; jscpd on the source files, skipped as outside scan scope for the test file). Fix-and-rerun history: attempt 1 on the service failed `max-lines-per-function` (83 > 75 → extracted helpers); attempt 2 failed `prefer-optional-chain` (→ `probe?.studentId`); attempt 3 **exit 0**. Test file: attempt 1 failed `no-await-in-loop` ×6 + `no-unsafe-type-assertion` (→ recursive sequential driver for shared-transaction iterations per `docs/quality/linting-rules.md`, JSON-channel hostile payload per the documented `any`-typing recipe) + `sonarjs` sort/regex nits; attempt 3 **exit 0**. Final state: **service ✅ 0, barrel ✅ 0, test ✅ 0** (logs `/tmp/task23-ql-student-evaluation.service.log`, `/tmp/task23-ql-index.log`, `/tmp/task23-ql-student-evaluation.service.test.log`).

### 2.3.TE — Test Engineering (mandated runner; raw results)

| Suite | Command | Result |
|---|---|---|
| Service tests | `bun run test/scripts/run-test.ts backend/services/teachers/student-evaluation.service.test.ts` | **19 pass / 0 fail — 391 expect() calls, exit 0**, two CONSECUTIVE runs (logs `/tmp/task23-tests-service.log`, `/tmp/task23-tests-service-rerun.log`) |
| Journey (stop condition) | `bun run test/scripts/run-test.ts test/workflows/teachers/student-teacher-rating.journey.test.ts` | **10 pass / 0 fail — 127 expect() calls, exit 0**, two consecutive runs (logs `/tmp/task23-journey.log`, `/tmp/task23-journey-rerun.log`) — all ten steps green incl. the concurrent duplicate leg and the byte-identical oracle leg |

Service-suite tier map (19 tests): Tier 1 — happy write + read-back on a dual-confirmed session (row contents, GraphQL-facing key set, zero logs) and the full denial matrix (`VALIDATION` ×2, `SESSION_NOT_FOUND`, `EVALUATION_SESSION_NOT_COMPLETED`, `EVALUATION_ALREADY_SUBMITTED`) with byte-exact classes, codes, and translated messages; Tier 2 — the score matrix 1..5 → 20..100, the rating rejections at 0/6/2.5/NaN/string-coerced each projecting exactly `[{"field":"rating","code":"TEACHER_RATING_INVALID",…}]`, session-id fuzz (0, −5, 1.5, NaN, beyond safe-integer) denied BEFORE any database read (repository spies record zero probe/insert calls); Tier 3 — the completion-gate matrix over six constructible pre-handshake states (scheduled/started/cancelled, teacher-stamp-only, student-stamp-only (impossible via the real lifecycle, constructed at row level), both stamps missing), the sequential duplicate re-submit (stored row byte-identical), and the committed-fixture production path including the `Promise.allSettled` concurrent duplicate storm (exactly one winner at score 100, one typed-conflict loser, one stored row, exactly one bounded denial log); Tier 4 — foreign-student ≡ nonexistent-id byte-identical denial fingerprints (incl. unknown id 2,000,000,000), outer-transaction SAVEPOINT propagation (a denial rolls back only its savepoint; the caller's transaction still accepts further work), read scoping (two students each see exactly their own rows newest-first; a third sees none — both the tx and cold read paths), write purity (zero deltas on session row, users, notifications, audit), plus six text-level source pins (import allowlist, no notification/audit imports, no dynamic imports/console/process.env/type assertions, no module-level mutable state, no spread, no service-layer types file, zero plan-artifact references in the service source).

### 2.3.SEC — Security & Tenancy Audit

- ✅ **BOLA/IDOR**: the rater is ALWAYS the caller-supplied `studentUserId` argument (the GraphQL layer will pass `ctx.user.id`); the rated subject is the probe row's teacher, never an argument; the participant predicate compares the probe's student column against the caller — a cross-student attempt collapses to the byte-identical `SESSION_NOT_FOUND` (service test pins `denialShape` equality with the unknown-id denial; journey step 8 pins `code|message` signature equality).
- ✅ **BOPLA**: the insert payload and the return mapping are member-by-member object literals; the service source contains NO spread at all (pinned by a source-pin test); the rating denial projects the offending field by NAME, never the submitted value (log contexts pinned to the bounded key set {code, entity, entityId, locale}).
- ✅ **Oracle byte-identity**: unknown id (2,000,000,000) and foreign participant produce the same class (`NotFoundError`), same code (`SESSION_NOT_FOUND`), same translated message — asserted via `denialShape` equality at service level and `code|message` equality in the journey.
- ✅ **Denial logging exactly once**: every denial path emits exactly ONE bounded `logDomainError` (per-path single-log and batched-count assertions); the duplicate storm produced exactly one loser log; happy paths, the read surface, and collapse-to-null reads log NOTHING (`expectZeroLogCalls` on every success path).
- ✅ No i18n strings in logs beyond the bounded context; no user data enters any log line (payload-content probes).

### 2.3.SR — Semantic Review

- ✅ **No lifecycle writes**: the service imports no session-write path; the only write is `EvaluationRepository.insertOnce`. The write-purity test pins the session row (full-row snapshot), both user rows, notifications, and audit logs at zero deltas across a submission AND a read-back (read-only consumption of the session state; no lock taken — the probe is non-locking).
- ✅ **No notifications/audit emitted**: no notification/audit imports (source pin, incl. no transitive specifier); row-count oracles stay flat.
- ✅ **No module-level mutable state**: service source contains no `let`/`var` at all (pinned); all module-level members are consts or pure functions.
- ✅ **No dead branches**: both `withTransaction` arms exercised (SAVEPOINT arm via `runInRollback` tests, top-level arm via the committed-fixture block); both `listByEvaluator` executor arms exercised (tx path + cold path); the 23505-only mapping and the rethrow-untouched arm both exercised (conflict test + every gate denial flows through the rethrow).
- ✅ **No cross-layer imports**: import allowlist pinned (repo barrel, enum, tx helper, errors, logger, shared guards, shared unique-violation helper, types, locale server helper).
- ✅ **Enum value imports**: `SessionStatus.Completed` imported from the enum module and widened once (`SESSION_COMPLETED_STATUS`) — no bare status literals; no string literals where enums are expected.
- ✅ **Clean comments**: zero plan-artifact references in code comments (`rg` over all three files: the only match is the test's own source-pin REGEX literal, which asserts the service source is clean — the pin passes).

### 2.3.IV — Instruction Verification

- `backend/services/AGENTS.md` read in full: domain-scoped services, business logic hub, `getServerTranslations(locale)` i18n discipline, types from `@/backend/types`, no service-layer `.types.ts`, single-writer/write-once UNIQUE-arbiter rule, composition seams take `outerTx` as the FINAL parameter, no `oxlint-disable` — all complied.
- `backend/AGENTS.md` read in full: canonical type pattern, layer responsibilities, `DBTransaction`/`DBQueryExecutor` from `@/backend/types`, taxonomy-only statuses, no local Pothos types — complied.
- Sub-loop printed instructions: `.agents/instructions/backend.instructions.md` + `.agents/instructions/tests.instructions.md` read in full. Compliance notes: `runInRollback` + `tx` everywhere in the transactional block (committed-fixture block mirrors the sibling production-path convention); `expectRepoError` try/catch everywhere, zero `expect(...).rejects`; `bun:test`; types from `@/backend/types`; entity-setup helpers only. One documented deviation-in-kind: the tests call `getServerTranslations` directly (the instructions warn of an i18n-loader hang) — this matches the mandated template `recitation.service.test.ts` and the journey verbatim, and both suites ran green twice with no hang, so the template pattern prevails.
- AGENTS chain read: root `AGENTS.md` barrel conventions (relative `./` paths, `export *`, parent barrels never reach into nested files) for the barrel edit.

## Carry-Forward Knowledge (Phase 3 — GraphQL surface)

1. **Exact service signatures (import from the barrel):**
   ```ts
   import { StudentEvaluationService } from "@/backend/services/teachers";

   StudentEvaluationService.submitTeacherEvaluation(
     studentUserId: number,          // = ctx.user.id (server-derived; NEVER client input)
     sessionId: number,              // shape-coerced at the boundary (requirePositiveIntId(Number(args.sessionId)))
     input: EvaluationSubmitInput,   // { rating: number } — map member-by-member from args.input (BOPLA)
     locale: string,                 // ctx.locale
     outerTx?: DBTransaction,        // omit on the production path
   ): Promise<EvaluationReturnType>

   StudentEvaluationService.listMyTeacherEvaluations(
     studentUserId: number,          // = ctx.user.id
     tx?: DBQueryExecutor,           // omit on the production path (cold read is queryDb raw SQL)
   ): Promise<readonly EvaluationReturnType[]>
   ```
2. **Error codes the resolver must NOT catch or mask** (they propagate as `extensions.code`): `SESSION_NOT_FOUND` (NotFoundError, entity "SESSION"), `EVALUATION_SESSION_NOT_COMPLETED` (ConflictError), `EVALUATION_ALREADY_SUBMITTED` (ConflictError), `VALIDATION` (ValidationError; `fields[]` = `[{field:"rating", code:"TEACHER_RATING_INVALID", message: t.teacherRatingInvalid}]` for star inputs, bare `t.validation` message without fields for a malformed session id). All messages arrive already localized for the passed locale.
3. **Authorization lives in the builder** (`authScopes: { $all: { authenticated: true, role: [UserRole.Student] } }` + `if (!ctx.user)` narrowing) — the service does NOT re-check roles; it only enforces participant + eligibility + write-once.
4. **Idempotent UX hook**: `EVALUATION_ALREADY_SUBMITTED` means the rating EXISTS — the frontend may mark the session rated on this code (plan §3.3); the service guarantees the stored row is untouched.
5. **Wire-test seam**: the barrel resolution used by the journey (`import * as teachersServices from "@/backend/services/teachers"`) now resolves the real namespace — no test-side projection changes needed for Phase 3.
6. Sandbox note: the workspace restores HEAD to `main` between invocations and reverts TRACKED file modifications (untracked files survive) — canonical copies of all task files live in `/tmp/task23-backup/` with restore script `/tmp/task23-restore.sh`; re-run it before any verification if `git rev-parse --abbrev-ref HEAD` is not the feature branch.
