# Task 2.1 Outcome — Repository: EvaluationRepository + SessionRepository.findRatingEligibilityProbe

> **Plan:** `ai/plans/sprint_3/student-evaluation-submission-teacher-rating/` · **Task:** 2.1 · **Branch:** `feat/student-evaluation-submission-teacher-rating` · **Date:** 2026-09-12
> Requirements: REQ-005, REQ-012, REQ-013.1

## Summary

Implementation authored by prior agents (survival commits b697e2d → fd94f4a + working-tree refinements): `EvaluationRepository` namespace with `insertOnce` (typed payload, tx-required, unique-index 23505 surfaced raw) and `listByEvaluator` (caller-scoped, tx/`queryDb` raw-SQL dual path, soft-delete exclusion, `createdAt DESC, id DESC` order) in a NEW `backend/db/repo/teachers/evaluation.repository.ts`, exported via the NEW `backend/db/repo/teachers/index.ts` barrel; `SessionRepository` extended with `findRatingEligibilityProbe` (non-locking six-column projection of `SessionRatingEligibilityProbeType`, tx REQUIRED), with the verbatim gate-read extraction into NEW `backend/db/repo/classes/session.repository.gate.helpers.ts` (same pattern as `session.repository.helpers.ts` / `wave.helpers.ts`); tests in NEW `backend/db/test/repo/teachers/evaluation.repository.test.ts` plus probe coverage in `backend/db/test/repo/classes/session.repository.test.ts`. Finalizer agent verified tests + quality loop and completed attestations below.

## Files (6)

| File | Status | Content |
|---|---|---|
| `backend/db/repo/teachers/evaluation.repository.ts` | NEW | `EvaluationRepository.insertOnce(values, tx)` + `listByEvaluator(evaluatorId, tx?)` |
| `backend/db/repo/teachers/index.ts` | NEW | barrel export of `EvaluationRepository` |
| `backend/db/repo/classes/session.repository.ts` | EXTEND | + `findRatingEligibilityProbe(sessionId, tx)` |
| `backend/db/repo/classes/session.repository.gate.helpers.ts` | NEW | verbatim gate-reads extraction (helpers-file pattern) |
| `backend/db/test/repo/teachers/evaluation.repository.test.ts` | NEW | Tier 1–4 evaluation repo tests |
| `backend/db/test/repo/classes/session.repository.test.ts` | EXTEND | probe coverage (six-column projection, unknown id → null) |

## Verification results (finalizer)

**Environment:** `.env` `DB_PROVIDER=postgres` verified; PostgreSQL 17 up (`pg_ctl status`, port 5432, socket dir `/tmp/pgdata`); branch verified `feat/student-evaluation-submission-teacher-rating` before every git command.

### 2.1.TE — Test Engineering (mandated runner; both suites green on attempt 1, no retries)

| Suite | Command | Result |
|---|---|---|
| Evaluation repo | `bun run test/scripts/run-test.ts backend/db/test/repo/teachers/evaluation.repository.test.ts` | **18 pass / 0 fail** — 80 expect() calls, exit 0 (log `/tmp/task21-tests-repo.log`) |
| Session repo (probe) | `bun run test/scripts/run-test.ts backend/db/test/repo/classes/session.repository.test.ts` | **71 pass / 0 fail** — 462 expect() calls, exit 0 (log `/tmp/task21-tests-probe.log`) |

Tier map in the repo suite (18 tests): Tier 1 branch/statement — insert RETURNING shape with all four FK columns + score, list hit / empty-list-never-null miss, tx branch AND both standalone-executor branches (queryDb read + raw-pool fall-through); Tier 2 boundaries — nullable `session_id` (NULL stored, two NULL-session rows from one evaluator coexist), `is_deleted` tri-state false/null/true (only explicit flag excludes; another evaluator's row never leaks), score 0/100 accepted raw (table CHECK is the backstop), same-instant ordering with `id DESC` tiebreak; Tier 3 chaos — in-tx duplicate raises RAW `23505` via `expectRepoError` + `constraintNameOf(err) === "evaluations_session_evaluator_unique"` (never `expect().rejects`), two independent committed transactions racing `insertOnce` under `Promise.allSettled` → exactly one winner, loser's tx inserts zero rows; Tier 4 — enormous/negative evaluator id → empty list, nonexistent rated subject → raw `23503`; plus tx-propagation (rollback) and static source pins (REQUIRED tx on the write, bound parameters only, no wildcard select / prepared statements / SQL line comments, NULL-safe soft-delete on BOTH read paths, caller-scoped read, no i18n/logger/console, one namespace, no plan-artifact refs).

Probe coverage in the session suite (2 tests): `findRatingEligibilityProbe` returns exactly the six-column projection (`id, studentId, teacherId, status, confirmedByTeacherAt, confirmedByStudentAt`), surfaces null stamps verbatim, and returns `null` for unknown ids.

### 2.1.QL — Quality Loop (log `/tmp/task21-qloop.log`)

`bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates` — **exit 0 on attempt 1 for all six files** (tsgo project-wide filtered per file, oxlint, biome:check, lint:type-aware; jscpd run for source files, skipped as outside scan scope for test files):

| File | Result |
|---|---|
| `backend/db/repo/teachers/evaluation.repository.ts` | ✅ exit 0 |
| `backend/db/repo/teachers/index.ts` | ✅ exit 0 |
| `backend/db/repo/classes/session.repository.ts` | ✅ exit 0 |
| `backend/db/repo/classes/session.repository.gate.helpers.ts` | ✅ exit 0 |
| `backend/db/test/repo/teachers/evaluation.repository.test.ts` | ✅ exit 0 |
| `backend/db/test/repo/classes/session.repository.test.ts` | ✅ exit 0 |

Printed rule files: `.agents/instructions/backend.instructions.md` + `tests.instructions.md`; AGENTS chain `AGENTS.md`, `backend/db/test/AGENTS.md`, `backend/AGENTS.md`.

### Gate-reads extraction proof

`git diff HEAD -- backend/db/repo/classes/session.repository.ts` shows both gate reads (`lockForReportGate`, `findRatingEligibilityProbe`) replaced by one-line delegation to `sessionRepositoryGateImpl`; the extracted bodies in `session.repository.gate.helpers.ts` are statement-identical to the removed ones (same six/five-column projections, same `.for("update")`, same `.limit(1)`, same `rows[0] ?? null`) — the same verbatim-extraction pattern as `session.repository.helpers.ts` / `wave.helpers.ts`. The public `SessionRepository` surface, signatures, and doc-comments are unchanged in contract (`tx` still REQUIRED on both).

### 2.1.SEC — Security & Tenancy Audit

- ✅ `listByEvaluator` is caller-scoped: signature `(evaluatorId, tx?)` — no teacherId/evaluatedId/status filter parameter exists through which a caller could widen the read; both read branches bind only `evaluator_id` + the NULL-safe live-row flag. A test pin statically asserts the absence of widening parameters.
- ✅ Raw `23505` surfaces untranslated: `insertOnce` catches nothing and imports no error/i18n machinery — mapping to `EVALUATION_ALREADY_SUBMITTED` is the service's job (REQ-012.2, task 2.3).
- ✅ No i18n / logger / console imports in any touched file; no user data enters SQL strings (bound parameters only).
- ✅ Tenancy: evaluation rows are reachable only by the caller-supplied evaluator id; no admin/cross-tenant surface added.

### 2.1.SR — Semantic Review

- ✅ **No SELECT-then-INSERT**: `insertOnce` is a single `INSERT … RETURNING`; the unique index is the write-once arbiter (REQ-012.4) — no pre-check read exists anywhere in the flow.
- ✅ **`tx` unoptional on writes**: `insertOnce(values, tx: DBTransaction)` — required, last parameter; statically pinned by a test.
- ✅ **Probe classification-only**: `findRatingEligibilityProbe` is a plain non-locking read (no `.for("update")`), returns a projection + `null` — nothing in the repo layer feeds it into a guarded update; duplicate submissions are arbitrated downstream by `evaluations_session_evaluator_unique`.
- ✅ **No dead branches**: both `listByEvaluator` read branches are reachable and covered (tx branch via `runInRollback` tests; `queryDb` branch via standalone pool/executor tests).
- ✅ **No cross-layer imports**: source files import only `drizzle-orm`, `@/backend/db`, `@/backend/db/schema/…`, `@/backend/types`; tests import the repo through the top-level barrel `@/backend/db/repo` (proves the `teachers/index.ts` + top-level wiring).
- ✅ **Enum value imports**: session-status vocabulary flows through `SessionStatus` imported from `@/backend/enum/scheduling/session-status.enum` — never string literals.
- ✅ **Clean comments**: `rg "REQ-[0-9]|Task [0-9]|tasks\.md|plan\.md|sprint_3|plan-review"` over all touched files → zero hits; doc-comments describe domain behavior only.
- ✅ No git operations beyond branch verification; nothing committed/pushed.

### 2.1.IV — Instruction Verification

- `backend/db/repo/AGENTS.md`: one `namespace` per repository file, `@/` aliases, types from `@/backend/types`, no hardcoded user-facing error strings, guarded-write/query conventions, barrel rules — complied (namespace `EvaluationRepository`; `teachers/index.ts` gained `export * from "./evaluation.repository"`; `gate.helpers.ts` uses the sanctioned co-located sibling-helper pattern).
- `backend/AGENTS.md`: bare (non-transactional) reads MUST use `queryDb` — complied (tx-less `listByEvaluator` branch is raw parameterized SQL through `queryDb`, precedent `report.repository.ts:63-72`); `DBTransaction`/`DBQueryExecutor` imported from `@/backend/types`; zero `oxlint-disable` comments; test layer uses `runInRollback` + `tx` everywhere, with committed-fixture groups (hard-deleted in `afterAll`) only where standalone executors/concurrency require them.
- Sub-loop printed instruction files (`backend.instructions.md`, `tests.instructions.md`) read; every check green with no fix-ups needed.

## Carry-Forward Knowledge

1. **Task 2.3 (service)**: `insertOnce` throws the raw PG 23505 whose constraint is `evaluations_session_evaluator_unique` — catch and map to `ConflictError("EVALUATION_ALREADY_SUBMITTED", t.evaluationAlreadySubmitted)`; `listByEvaluator` returns `EvaluationSelectType` rows (service maps to `EvaluationReturnType`).
2. **Probe shape**: `SessionRatingEligibilityProbeType` — six fields; `status` is the `SessionStatus` literal union; `confirmedByTeacherAt`/`confirmedByStudentAt` are `Date | null` and arrive verbatim.
3. **Test fixtures**: standalone/concurrency groups commit real rows and hard-delete them in `afterAll`; new repo tests should reuse `entity-setup.ts` helpers (`createTestUser/Student/TeacherRow/Session/Evaluation`), never seed data.
4. **Nothing committed/pushed** (constraint) — the working tree carries the full task-2.1 implementation; orchestrator reconciles with the survival commits.
