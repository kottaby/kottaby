# Task 1.1 Outcome — Schema: Write-Once Arbiter + Doc-Comment

> **Plan:** `ai/plans/sprint_3/student-evaluation-submission-teacher-rating/` · **Task:** 1.1 · **Date:** 2026-09-11
> **File:** `backend/db/schema/teachers/evaluations.ts` · **Branch:** `feat/student-evaluation-submission-teacher-rating`

## Summary

Added the write-once arbiter `unique("evaluations_session_evaluator_unique").on(t.sessionId, t.evaluatorId)` as the last element of the table's third-arg constraint block (after the three per-FK indexes), added `unique` to the `drizzle-orm/pg-core` import (alphabetical, after `timestamp`), and rewrote the file header doc-comment to document both consumers of the table (applicant evaluation pipeline; student→teacher session rating with `score = rating × 20`). Applied to the live PostgreSQL 17 database via `bun run db push` (exit 0, no data-loss prompt); constraint verified present by introspection.

## Files Modified

- `backend/db/schema/teachers/evaluations.ts` — import (`unique`), doc-comment rewrite (:6-33), constraint appended at :60 (last element of the `t => [...]` block).

## Files NOT Modified (and why)

- **Drizzle meta/snapshot files (`backend/drizzle/**`)** — `bun run db push` generates none (git status shows only `evaluations.ts` modified). This repo's meta snapshots under `backend/drizzle/` are produced by the generate/migrate flow, which is explicitly out of scope for this task (push-only mandate; `db migrate` reserved for custom SQL). Precedent note for the orchestrator: commit `c8df3b0` (reports/home_work session-unique) DID ship a generated `migration.sql` + `snapshot.json`; if this team later wants a snapshot artifact for `evaluations_session_evaluator_unique`, that is a separate generate-flow decision — not done here to honor the "do NOT touch any other source file" constraint.
- **All readers/writers of `evaluations`** — unaffected (see reader-impact analysis below); modifying them is outside task 1.1's file scope.

## DDL Captured (verified via psql introspection)

From `\d evaluations`:

```
Indexes:
    "evaluations_pkey" PRIMARY KEY, btree (id)
    "evaluations_evaluated_id_idx" btree (evaluated_id)
    "evaluations_evaluator_id_idx" btree (evaluator_id)
    "evaluations_session_evaluator_unique" UNIQUE CONSTRAINT, btree (session_id, evaluator_id)
    "evaluations_session_id_idx" btree (session_id)
```

Raw index DDL (`pg_indexes`):

```sql
CREATE UNIQUE INDEX evaluations_session_evaluator_unique ON public.evaluations USING btree (session_id, evaluator_id)
```

Constraint registry (`pg_constraint`): `evaluations_session_evaluator_unique | u` (contype `u` = UNIQUE) alongside the pre-existing PK, CHECK, and three FKs. Drizzle push registered it as an inlined UNIQUE table constraint backed by a unique btree index.

## Verification Results

| Gate | Result |
|---|---|
| 1.1.QL — `bun run scripts/health/sub-loop.ts backend/db/schema/teachers/evaluations.ts --lifecycle duplicates` | **exit 0** — tsgo (project-wide, file-filtered) · oxlint · biome:check · lint:type-aware · check:duplicates (intra-file) all passed, pre- AND post-edit |
| Push — `bun run db push` | **exit 0** — `[✓] Changes applied` with **no data-loss / truncation prompt** (table had 0 rows: `SELECT count(*) FROM evaluations` → `0`, re-confirmed post-push) |
| DB state | `.env` line 1 = `DB_PROVIDER=postgres` (verified pre-push); postgres 17 at `127.0.0.1:5432/app_db` running (PID 1855) |
| 1.1.TE | **Transitive** — covered by task 2.1's repo tests (`expectRepoError` + `constraintNameOf(err) === "evaluations_session_evaluator_unique"`, 23505 assertion). No tests written in this task per plan. |
| 1.1.SEC | Index adds **no read surface** (pure write-arbiter). Reader-impact analysis below. |

**Sandbox/git note:** HEAD flipped back to `main` mid-task twice (known sandbox quirk; a parallel Phase-0 plan commit `2757c6d` landed on the feature branch in between). Reconciled by re-applying the `tasks.md` 1.1 checkbox flips and this task's `worklog.md` record on top of the feature branch; final state verified with HEAD on `feat/student-evaluation-submission-teacher-rating` (2757c6d). **The orchestrator MUST re-verify `git branch --show-current` before committing** — parallel task agents (1.2/1.3 edits visible in the shared working tree) and the flip quirk are both active.

## Reader-Impact Analysis (1.1.SEC)

Grep of every `evaluations` table consumer (excluding plan/docs files):

| Consumer | Kind | Impact |
|---|---|---|
| `backend/db/repo/admin/platform-analytics.repository.ts:78,359-402` | production READ (avg/count of non-soft-deleted `score`) | **None** — SELECT semantics unchanged; unique constraints constrain writes only |
| `backend/types/teachers/evaluation.types.ts:1-3` | type-only (`$inferSelect`) | **None** — column set unchanged |
| `backend/db/test/entity-setup.ts:412-430` (`createTestEvaluation`) | test writer | **None** — inserts fresh users per fixture, so (session_id, evaluator_id) pairs are naturally distinct; `sessionId: null` rows are unaffected (PG treats NULLs as distinct) |
| `backend/db/test/logic/admin/platform-analytics.repository.test.ts:863-864` | test read | **None** |
| `backend/services/admin/platform-analytics.service.test.ts`, `user-governance/user-management.chaos.test.ts` | cleanup-order comments | **None** |

Expected readers match the plan (production reader: `platform-analytics.repository.ts` only). No existing query breaks.

## Semantic Review (1.1.SR) — all verified

- ✅ Constraint name spelled **exactly** `evaluations_session_evaluator_unique` in schema (:60) and in the live DB — task 2.3 will map 23505 by this name.
- ✅ Snapshot/drizzle metadata: push flow produced **none** (git status = only `evaluations.ts`); documented above with the generate-flow precedent for the orchestrator.
- ✅ No dead branches — table definition is declarative; no conditional logic added.
- ✅ No cross-layer imports — imports are `drizzle-orm`, `drizzle-orm/pg-core`, and schema-domain siblings (`classes/session`, `users/users`) only.
- ✅ Clean comments — doc-comment describes domain behavior only; grep for `REQ-`, `Task `, `plan.md`, `DEV2`, `sprint`, `outcome`, `specs` (case-insensitive) → **0 matches**. No trivial comments.

## Instruction Verification (1.1.IV)

Read and complied with: `backend/db/schema/AGENTS.md` (Drizzle-only, db-push-after-schema-edit, enum verification, barrel conventions — no new table/enum, so additive rules apply), root `AGENTS.md` (quality workflow, import conventions), `backend/AGENTS.md` (printed by sub-loop), and `.agents/instructions/backend.instructions.md` (schema layer rules). Sub-loop's printed AGENTS list (root + backend) consumed in full.

## Carry-Forward Knowledge (for future subtasks)

1. **Exact constraint name: `evaluations_session_evaluator_unique`** — task 2.3's 23505 mapping and task 2.1's `constraintNameOf` assertions must use this exact string.
2. **DB state: `evaluations` still has 0 rows** post-push; the unique constraint is live. Unique-violation behavior is testable immediately.
3. **NULL semantics live**: rows with `session_id = NULL` never collide (PG unique treats NULLs as distinct) — repo test for "sessionId: null rows unaffected" can insert multiple NULL-session rows safely.
4. **No drizzle snapshot artifact was generated** — if a later task/commit needs migration files (generate flow), `evaluations_session_evaluator_unique` will appear as a new `DO $$ ALTER TABLE "evaluations" ADD CONSTRAINT ... UNIQUE("session_id","evaluator_id") $$` statement; precedent shape in `c8df3b0` (`backend/drizzle/20260907095925_remarkable_professor_monster/migration.sql`).
5. **Doc-comment is now dual-consumer** — task 2.x service comments should not re-describe the flows inline; reference the table doc instead.

## Cross-File Dependencies Discovered

- **Task 2.1 (repo tests)** must assert `constraintNameOf(err) === "evaluations_session_evaluator_unique"` (name locked by this task).
- **Task 2.3 (service)** maps 23505 → `EVALUATION_ALREADY_SUBMITTED` by the same name.
- **No blocking cross-file violations** found by the sub-loop fix-or-report rule (single-file change sufficed).
