# Task 5.2 Outcome — Knowledge Propagation (canonical doc + consumer-line pointer)

> **Plan:** `ai/plans/sprint_3/student-evaluation-submission-teacher-rating/` · **Task:** 5.2 · **Branch:** `feat/student-evaluation-submission-teacher-rating` · **Date:** 2026-09-13
> **Requirement:** REQ-014 · **State:** COMPLETE — doc published, pointer appended, verification green, zero plan-artifact references in the doc.

## Summary

Authored the canonical doc `docs/teachers/student-evaluation-submission.md` (NEW, 7 sections) consolidating the rating feature's durable knowledge extracted from ALL outcome files (1.1–4.4, midpoint, post-implementation, review rounds 1–10) per the propagation policy (docs-only; AGENTS.md / `.agents/instructions/` untouched — hand-curated). Appended a link-only pointer to the ratings consumer line in `docs/sessions/session-lifecycle.md:163`. The doc describes domain behavior only — grep for `REQ-|DEV2|DEV3|tasks\.md|plan\.md|sprint|outcome|5\.2` over the doc: **0 hits** — while citing real code paths (file:line verified current at write time, see verification).

## Deliverable Files (2)

| File | Status | Content |
|---|---|---|
| `docs/teachers/student-evaluation-submission.md` | NEW | Canonical reference: §1 Scope & Surfaces (two `evaluations` consumers, GraphQL/UI surfaces, completion deep-link); §2 Rating Write Contract (write-once per (session, evaluator), `evaluations_session_evaluator_unique` arbiter, insert pipeline, 23505 → typed conflict, non-locking-probe TOCTOU rationale); §3 Score Conversion & Average-Rating Forward Contract (`score = rating × 20`, `avg(score)/20` → `teacher.average_rating`, clean-row guarantees, NULL `session_id` = applicant evaluations MUST be excluded); §4 Error Contract (code → HTTP-free wire semantics → client surface → i18n key table + log discipline); §5 Security Posture (server-derived identity, `$all` role gate, oracle byte-identity, read scoping, BOPLA, wire fidelity, bounded logs, cross-surface purity); §6 What NOT to Do (8 anti-patterns); §7 References |
| `docs/sessions/session-lifecycle.md` | EXTEND | :163 ratings consumer row gains one link sentence: "The rating write contract's canonical reference: [`docs/teachers/student-evaluation-submission.md`](../teachers/student-evaluation-submission.md)." — link only, no semantic change |

## Propagation Table (recurring pattern → where documented)

Patterns with 2+ occurrences across the outcome/review files, and the doc section that now carries each:

| Recurring pattern / gotcha (seen in) | Documented in |
|---|---|
| Write-once via UNIQUE arbiter, NEVER a pre-check SELECT (TOCTOU) — plan D2; 1.1 SR; 2.1 SR; 2.3; midpoint review-backend; R6 security re-trace | §2 (write contract) + §6 (bullet 1) |
| Oracle collapse: foreign ≡ nonexistent → byte-identical `SESSION_NOT_FOUND` — 2.2 journey step 8; 2.3 SEC; 3.4 wire matrix; R6 | §2.1 (step 4) + §4 (row 3) + §5 (oracle byte-identity) |
| Server-derived evaluator/subject; the only client-owned value is `{ rating }` — 2.3 SEC; 3.2 BOPLA hand-off; 3.4 wire proof; 4.2 SEC | §5 (server-derived identity, mass assignment) + §6 (bullet 4) |
| `score = rating × 20` canonical 0–100 scale; never raw 1–5 — plan D5; 1.1 doc-comment; 2.2 step 3; 2.3 score matrix | §3 (conversion) + §6 (bullet 7) |
| 23505 on the cause chain → typed `EVALUATION_ALREADY_SUBMITTED`; repo surfaces raw, service maps — 1.1; 2.1; 2.3; 3.4; error-link map row | §2 (insert pipeline + 2.1 step 7) + §4 (row 5) |
| `sessionRatingRange` is report-flow-owned and MUST NOT be reused — 1.3 (verified untouched); plan D10 | §6 (bullet 3) |
| Exactly ONE bounded denial log per denial; `entity` label follows the id ("session") — 2.3 SEC; R3 taxonomy relabel finding | §4 (log discipline paragraph) |
| Read-only consumption of session state; non-locking probe safe (monotonic post-confirmation state) — 2.1 SR; 2.3 write-purity pin; midpoint review-backend | §2.1 (probe rationale) + §6 (bullet 2) |
| `$all { authenticated, role: [...] }` explicit conjunction (ANY-semantics + 401/403 split of a plain map) — 3.2; 3.3; midpoint; `applicant-lifecycle.md` precedent | §5 (role gate) |
| NULL `session_id` rows are applicant evaluations (PG NULLs-distinct); aggregation must exclude them — 1.1; schema doc-comment; 4.1 rated-set null-filter | §1 (consumer table) + §3 (row selection) |
| Zero plan-artifact references in shipped files (per-task SR greps) — 1.1–4.4 outcomes; R1/R2 comment fixes; R8/R10 sweeps | The doc itself (verified 0 hits) + §7 cites only code/docs paths |

Deferred topics (per SKILL.md: plan-specific constraints stay in outcome files, not permanent docs): notes/free-text and notifications are marked deferred **behaviorally** in §6 (no input field / no emission) without naming any plan ledger.

## Verification

| Subtask | Check | Result |
|---|---|---|
| 5.2.QL | Code-quality gates vs docs — **n/a with evidence**: `docs/**` is in the ignore lists of `eslint.config.mjs:46` and `oxlint.config.mts:193`; `tsconfig.json` includes only `**/*.ts`/`**/*.tsx` (tsgo never sees .md); `biome.json` has no Markdown parser/formatter; `scripts/health/sub-loop.ts` runs tsgo→oxlint→biome→lint:type-aware→check:duplicates, all source-file tools. No markdownlint/cspell step is wired into any `package.json` script or the sub-loop. |
| 5.2.QL (md surface) | Markdown lint-equivalent self-checks run instead: single H1, 7 H2 sections (sibling-docs numbering style), all tables column-consistent (the one raw pipe-count delta is the escaped `code\|message` cell — the same house convention as `docs/teachers/applicant-lifecycle.md:19` `passed \| failed`), `---` section separators per house style | ✅ consistent with sibling docs |
| 5.2.TE | n/a (documentation) — recorded as such | n/a |
| 5.2.SEC | Doc discloses the error contract accurately: all six codes cross-checked against the shipped sources — `error-link.map.ts:290/:302` tones + message keys, `en/errors/index.ts:105–108` keys, oracle byte-identity per service :131 + wire pins, `VALIDATION` fields[] shape per service :239 — and the security-posture table against the service/mutation/query sources | ✅ accurate |
| 5.2.SR | (a) Links resolve: `../teachers/student-evaluation-submission.md` from `docs/sessions/` exists; all 15 path-only and 38 line-pinned references existence-checked. (b) Line refs current at write time: 34/34 pinned file:line refs re-verified by `sed -n` pattern match (log `/tmp/task52-verify-refs.log`; the analytics citation was widened to `:359–403` to include its soft-delete filter line). (c) Markdown consistency vs siblings: see 5.2.QL row. | ✅ all green |
| 5.2.IV | n/a beyond root doc conventions — plan-artifact grep over the doc = 0 hits; hand-curated `AGENTS.md`/`.agents/instructions/` NOT touched (git status confirms neither modified) | ✅ |

## Files NOT Touched (policy)

- `AGENTS.md` (root and all nested) and `.agents/instructions/**` — hand-curated rule files; propagation is DOCS-ONLY.
- `docs/README.md` index — not in task scope (the task specifies exactly the new doc + the session-lifecycle pointer).
- No source/test files, no commit/push/build.

## Carry-Forward

None. (The aggregation consumer's obligations are the doc's §3 forward contract; nothing is owed by this task.)
