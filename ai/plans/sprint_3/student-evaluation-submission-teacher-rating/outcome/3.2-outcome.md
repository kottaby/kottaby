# Task 3.2 Outcome — Mutation: `submitTeacherEvaluation` (+ classes barrel)

> **Plan:** `ai/plans/sprint_3/student-evaluation-submission-teacher-rating/` · **Task:** 3.2 (Phase 3 — GraphQL surface) · **Branch:** `feat/student-evaluation-submission-teacher-rating` · **Date:** 2026-09-12
> Requirements: REQ-008.2, REQ-011, REQ-002 · **State:** VERIFIED — all gates green (verification evidence captured downstream in `3.4-outcome.md`; stale draft state remediated post-R10 — see Verification results).

## Summary

NEW `backend/graphql/mutation/classes/student-evaluation.mutation.ts` registers `submitTeacherEvaluation(sessionId: ID!, input: SubmitTeacherEvaluationInput!): Evaluation!` as a THIN side-effect module (no named exports). The resolver is logic-free delegation: the explicit `$all { authenticated: true, role: [UserRole.Student] }` conjunction (never a plain key-map — Pothos combines one scope map's keys with ANY semantics; `$all` yields the canonical anonymous→UNAUTHORIZED / non-student→FORBIDDEN split), an `if (!ctx.user)` narrowing throw whose message resolves through `await ctx.t("errorsTranslations")` (bound to `ctx.locale`), the house `requirePositiveIntId(Number(args.sessionId), "sessionId")` boundary coercion for the wire `ID`, a member-by-member BOPLA hand-off of the only client-owned field (`{ rating: args.input.rating }` — no spread, no client-supplied evaluator/score/session identity beyond the target arg), and a SINGLE delegation to `StudentEvaluationService.submitTeacherEvaluation(ctx.user.id, sessionId, input, ctx.locale)` (imported from the `@/backend/services/teachers` barrel). NO try/catch — the service's DomainErrors (`SESSION_NOT_FOUND`, `EVALUATION_SESSION_NOT_COMPLETED`, `EVALUATION_ALREADY_SUBMITTED`, `VALIDATION` + `fields[]`) propagate uncaught to the masking boundary. The `Evaluation` object + `SubmitTeacherEvaluationInput` register transitively through this module's import of the Pothos consts. The classes mutation barrel gains the side-effect import (listed right after `session-report.mutation`) plus its doc-comment registration line.

## Files (2)

| File | Status | Content |
|---|---|---|
| `backend/graphql/mutation/classes/student-evaluation.mutation.ts` | NEW | `submitTeacherEvaluation` root field (thin resolver) |
| `backend/graphql/mutation/classes/index.ts` | EXTEND | + side-effect import + doc-comment line |

## Verification results

> **Remediation note (2026-09-15, post-R10):** this file's verification sub-loop results were never written back at execution time — the same sandbox tracked-file-revert drift class documented in `round-9-10-review-outcome.md` (stale ledger states captured in earlier commits while code + outcome files were complete). The task's verification is proven by the downstream phase-closing and plan-closing artifacts:

- **3.4 phase-closing verification:** the mutation is materialized and pinned on the artifact — `schema.graphql` carries `submitTeacherEvaluation(input: SubmitTeacherEvaluationInput!, sessionId: ID!): Evaluation!` (args alphabetical, as specified in Carry-Forward Knowledge §1) and `sdl-static-assertions.test.ts` re-anchored `FROZEN_MUTATION_FIELDS` 34→35 (`+ submitTeacherEvaluation`, additive).
- **authScopes contract proven over the real wire** (exactly the contract specified in Carry-Forward Knowledge §2): anonymous × mutation → `UNAUTHORIZED` with constant localized copy; teacher/parent/admin → `FORBIDDEN` (pre-rescope, admin NO bypass); student-participant happy path returns server-derived rows (`evaluatorId` = caller, `evaluatedId` = the session's teacher — identities never accepted from arguments).
- **Service error surface at the wire** (exactly the codes enumerated in Carry-Forward Knowledge §3): `SESSION_NOT_FOUND` (foreign ≡ nonexistent), `EVALUATION_SESSION_NOT_COMPLETED` (scheduled σ′ AND teacher-stamp-only σ″), `EVALUATION_ALREADY_SUBMITTED` (re-submit; stored rating untouched), `VALIDATION` (rating 6 → `extensions.fields` = exactly `[{field:"rating", code:"TEACHER_RATING_INVALID", …}]`; malformed session id → bare message, NO fields).
- **Test counts:** wire matrix 21 tests / 108 assertions; two-file run 65 tests / 0 failed / 359 assertions; full `bun run test:graphql` 10 files / 154 tests / 0 failed / 928 assertions; QL sub-loop `--lifecycle duplicates` exit 0 on all four touched/mandated files.
- **Post-implementation review wave (5.1):** review-backend (backend scope incl. this resolver) FINDINGS 0; F4 (id coercion hardening in this file) FIXED and re-verified green.
- **5.3 Final Gate:** Definition of Done MET — outcome set complete incl. 3.1–3.4; traceability PASS; R1–R10 13 findings found / 13 fixed / 0 remaining.

## Carry-Forward Knowledge (Phase 3)

1. Registered operation name: `submitTeacherEvaluation` (Mutation root). Exact SDL shape for the 3.4 pins: `submitTeacherEvaluation(sessionId: ID!, input: SubmitTeacherEvaluationInput!): Evaluation!`.
2. authScopes contract: `$all { authenticated: true, role: [UserRole.Student] }` — anonymous → `UNAUTHORIZED`; teacher/parent/admin → `FORBIDDEN`; the in-resolver narrowing throw is TypeScript-only (unreachable past the scope wall) and localized via `ctx.t("errorsTranslations").unauthorized`.
3. Error surface for the wire tests (3.4): `UNAUTHORIZED`, `FORBIDDEN`, `SESSION_NOT_FOUND`, `EVALUATION_SESSION_NOT_COMPLETED`, `EVALUATION_ALREADY_SUBMITTED`, `VALIDATION` (with `extensions.fields[]` = `[{field:"rating", code:"TEACHER_RATING_INVALID", …}]` for star-range denials; bare message for a malformed session id).
4. Happy path returns the freshly inserted row in the `Evaluation` shape (`score` = `rating × 20`); no pagination/idempotency-key surface on this mutation.
