# 4.2 — Student Handshake Code Card — Outcome

**Task ID:** 4.2 (completion pass 4.2b — prior agent timed out at regression testing)
**Status:** ✅ Code + component-suite + QL + tsgo + SR complete. **4.2.BF / 4.2.BS (browser self-loops) = PENDING** for the browser agent.

## Files (all plan-owned; created/modified by this task)

| File | Kind |
|---|---|
| `frontend/views/students/dashboard/HandshakeCodeCard.tsx` | NEW — the card component (zero props, server-side identity via `myHandshakeCode`) |
| `frontend/views/students/dashboard/index.ts` | NEW — barrel (`export * from "./HandshakeCodeCard"`) |
| `frontend/views/dashboard/RoleDashboardPage.tsx` | MODIFIED — card mounted additively in the student `statusSlot` |
| `test/ui/components/translation-preload.ts` | MODIFIED — `HandshakeCode` namespace warmed for the component-suite process |
| `test/ui/components/students/HandshakeCodeCard.test.tsx` | NEW — component suite (both locales × all render branches + clipboard doubles + transient confirmation) |

## Evidence — commands, exit codes, pass counts

### 1. Component test (the new suite) + 2. regression — ONE sanctioned command

> **Runner note (important for the next agent):** `bun run test/scripts/run-test.ts <component-test>` is the WRONG
> runner for `test/ui/components/**` — it spawns `bun test <file>` WITHOUT the component preloads
> (`happydom-preload.ts` etc. are supplied only via the package.json script), so every component suite fails
> under it with `ReferenceError: document is not defined`. This is an invocation artifact, NOT a defect —
> the pre-existing `teachers/ApplicantStatusCard.test.tsx` fails identically under that runner. The sanctioned
> component runner is `bun run test:ui:components`.

| Command | Exit | Result |
|---|---|---|
| `bun run test:ui:components` | **0** | **52 pass / 0 fail / 1 snapshot — 6 files, 17.04s** |

Per-file breakdown:
- `students/HandshakeCodeCard.test.tsx` — **15/15 pass** (RTL/arabic ×7: skeleton, resolved code + LTR-isolated chip, copy success, copy failure, FORBIDDEN, STUDENT_NOT_FOUND, generic error; LTR/english ×7 same matrix; transient-confirmation self-clear ×1 en).
- Regression (5 pre-existing component files, all green — no regression from the RoleDashboardPage mount / preload edit): `api-status-indicator`, `graph-ql-error-surface-host`, `locale-switcher`, `site-footer`, `teachers/ApplicantStatusCard` (37 tests).

No production defects surfaced; no test-side fixes were needed once the correct runner was used.

### 3. QL sub-loops (`bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates`)

| File | Exit |
|---|---|
| `frontend/views/students/dashboard/HandshakeCodeCard.tsx` | **0** |
| `frontend/views/students/dashboard/index.ts` | **0** |
| `frontend/views/dashboard/RoleDashboardPage.tsx` | **0** |
| `test/ui/components/translation-preload.ts` | **0** |
| `test/ui/components/students/HandshakeCodeCard.test.tsx` | **0** |

### 4. Typecheck

| Command | Exit |
|---|---|
| `bun run tsgo` (→ `tsgo -b --noEmit`) | **0** (0 errors) |

### 5. SR greps (on all 5 task files)

| Check | Result |
|---|---|
| Direct style props (`fontWeight=` \| `mb=` \| `mt=` \| `p=` \| `display=` as JSX props) | **NONE** |
| Hardcoded colors (`#hex` / `rgb(`/`rgba(`) | **NONE** |
| Non-`*Outlined` `@mui/icons-material` imports | **NONE** (only `ContentCopyOutlined`, `TagOutlined`) |
| `FormEvent` | **NONE** |
| `useLazyQuery` | **NONE** (sole match is a doc comment in the test asserting its absence) |
| Student-id props on the card (`studentId` / `student_id` in `HandshakeCodeCard.tsx`) | **NONE** (card takes zero props) |

### 6. IV — frontend/AGENTS.md spot-check

- **MUI `sx`-only styling** ✅ — every visual rule lives in `sx` objects; zero direct style props.
- **Theme-callback colors** ✅ — every color resolves through `sx={theme => ({ ... theme.palette.* })}` callbacks (`primary`, `onPrimaryContainer`, `outlineVariant`, `text.secondary`, `success.main`, `error.main`, `surfaceContainerLow`, `shadow.card`).
- **Translation enum-property access** ✅ — `useAppTranslation(HandshakeCode)` / `useAppTranslation(Errors)` with property access (`t.yourCodeTitle`, `te.forbidden`, …); zero `t('key')` calls; zero hardcoded user-facing strings in the production files.
- Bonus discipline held: RTL-safe composition (LTR pin via HTML `dir="ltr"` + `unicode-bidi: isolate` — not a flippable `direction` declaration), `aria-busy` skeleton, `<output>` polite live region kept mounted.

## PENDING — carry-forward for the browser agent (4.2.BF / 4.2.BS)

- **Component location:** `frontend/views/students/dashboard/HandshakeCodeCard.tsx`, re-exported from
  `frontend/views/students/dashboard/index.ts`.
- **Where it renders:** `frontend/views/dashboard/RoleDashboardPage.tsx` — the student branch of
  `resolveStatusSlot(role)` returns `<HandshakeCodeCard />` into `DashboardView`'s `statusSlot`, i.e. the
  student dashboard page **`/student/dashboard`**, above the fold.
- **Login as a student:** `/login` (`app/(auth)/login/page.tsx`) with the seeded demo student:
  - Email: **`student@draftacademy.local`**
  - Password: the seeded default credential — `ADMIN_PASSWORD` env var read at seed time
    (`backend/db/seeds/lib/seed-config.ts`): **`adminpassword123`** per the current `.env`
    (fallback literal: `Seed_Pass1!`). Seed source: `backend/db/seeds/users/seed-users.ts`
    ("Demo Student", role `student`).
- **Browser checks to run:** card renders on `/student/dashboard` (title, description, `KSB-XXXXXXXX` chip,
  copy button); copy click → clipboard + transient confirmation that self-clears after ~2s; skeleton flash on
  load; `data-testid="handshake-code-card"`; both locales (ar RTL / en LTR) — code chip must read LTR in Arabic.
