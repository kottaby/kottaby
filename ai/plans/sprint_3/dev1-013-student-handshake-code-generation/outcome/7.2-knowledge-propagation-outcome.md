# Task 7.2 Outcome — Cross-Links & AGENTS.md Propagation (+ 7.2.SR)

**Task:** 7.2 Cross-Links & AGENTS.md Propagation (rule-only one-liners)
**Scope:** documentation-only — 7 files touched, zero code files, zero git state operations (read-only tree inspection only).
**Pre-execution knowledge read:** `outcome/7.1-canonical-doc-outcome.md` (canonical doc exists; docs-README registration not required per applicant-lifecycle precedent; cross-links into user-registration/workflow-04/invariants explicitly deferred to this task), `outcome/4.1-shared-documents-outcome.md` §1.4 (the interim embedded-types entry to verify/polish), plus full reads of every target file's surrounding formatting.

---

## 1. Edits (file → line added → rationale)

### 1.1 Cross-link 1 — `docs/auth/user-registration.md` (line 77, appended at end of §2 Handshake Generation)

```
Parent-side discovery by handshake code — the consumption half of this contract — is canonically documented in `docs/parents/handshake-code-discovery.md`.
```

- **Rationale:** §2 owns the *generation* half and its intro explicitly scopes out consumption ("The parent-link *consumption* workflow is out of scope here") — the appended line completes that hand-off by pointing at the consumption-half canonical doc. Backtick path (this file uses zero markdown links — grep `](` → 0 matches). No renumbering, no content edits: sections §2.1/§2.2 and every bullet untouched; the line sits after the §2.2 bullet list, before the `---` separator.

### 1.2 Cross-link 2 — `docs/workflows/04-parent-supervision-handshake.md` (line 5, appended to the header related-notes blockquote)

```
> **Implementation:** the §4.2 parent search read-side contract is canonically documented in `docs/parents/handshake-code-discovery.md`.
```

- **Rationale:** the header blockquote (`> **Source of truth:**` / `> **Related:**`) is this file's related/implementation-notes area; the new third line matches its `> **Label:**` convention without modifying the existing Related line (which stays byte-identical to the shared 5-workflow-doc template). §4.2 is the parent-search step the canonical doc implements server-side (the doc's Why section quotes Workflow 04 §4.2 verbatim).

### 1.3 Pointer check — `docs/specs/state-machine-invariants.md` (line 222, appended after the §7.2 INV-P1..P4 table)

```
The handshake-code discovery surface (parent search by code, preceding this lifecycle) is bound by INV-P1..P4 and is canonically documented in `docs/parents/handshake-code-discovery.md`.
```

- **Pointer-check verdict: pointer did NOT exist → one line added.** Full-file read (275 lines pre-edit): the only handshake mentions were the §7.1 `Code_Generated` state row and INV-P4's row text; zero references to `docs/parents/handshake-code-discovery.md` (the doc was created in Task 7.1, after this spec file was last touched).
- **Zero renumbering / zero content edits:** INV-P1..P4 rows byte-identical (verified post-edit — table rows untouched at lines 217–220); no section IDs introduced or shifted; the plain-paragraph line sits between the table and the existing `---` separator, matching the file's minimal-prose table style.

### 1.4 AGENTS one-liner — `backend/services/AGENTS.md` (line 4, inserted directly under the Registration pointer line)

```
- **Handshake-code discovery: `StudentHandshakeService` (`backend/services/students/student-handshake.service.ts`) answers a parent code lookup with ONLY the minimal masked payload — `{ maskedName, linkable }`, never an id or any other field; see `docs/parents/handshake-code-discovery.md` for the full contract.**
```

- **Rationale:** placed as the consumption-side sibling of the Registration entry (line 3); fully bold-wrapped to match the doc-pointer entry style of the file's first lines (Registration / Auth service / Teacher applicant lifecycle). Rule essence only: minimal payload, never an id — the doc's R1 carries the full payload-closure narrative (forbidden-field list, capability-by-code rationale).

### 1.5 AGENTS one-liner — `backend/db/repo/AGENTS.md` (line 40, inserted after the `inArray` + Prepared Statements PROHIBITED rule)

```
- **Handshake-code lookups**: `StudentRepository.findDiscoveryByHandshakeCode` (`backend/db/repo/students/student.repository.ts`) uses a single parameterized equality predicate on `students.handshake_code` (never LIKE/ILIKE or `sql` interpolation) and returns a fixed column list; the non-tx branch runs through `queryDb(tx)` (Neon HTTP), so module-level prepared statements do not apply. See `docs/parents/handshake-code-discovery.md`.
```

- **Rationale:** grouped with the file's other SQL-read-pattern rules (Prepared Statements CRITICAL / inArray PROHIBITED / Neon HTTP Client); bold-lead bullet + `See \`doc\`.` tail matches neighbors. Equality-lookup + fixed-column-list facts verified against the shipped repo code (`student.repository.ts` :118–155: `WHERE s.handshake_code = $1` `LIMIT 1`, fixed alias list, `queryDb` non-tx branch — which is why module-level prepared statements don't apply, consistent with the existing rule that prepared statements are removed when replaced by `queryDb(tx)` on non-tx read branches).

### 1.6 AGENTS entry polish — `frontend/graphql/AGENTS.md` (embedded-types list, lines 100–102)

**Verification of the Task 4.1 interim entry (accuracy check):**

| Claim in the 4.1 entry | Verified against | Verdict |
|---|---|---|
| fields: `linkable`, `maskedName` | GraphQL type `HandshakeCodeLookup { maskedName: String!, linkable: Boolean! }` (canonical doc §6; Pothos passthrough `backend/graphql/pothos/students/handshake-code.pothos.ts`) | ✅ accurate |
| "no `id`" | embedded value type — no id by design; registered in `frontend/providers/apollo/apolloCache.ts` :45–47 | ✅ accurate |
| `keyFields: false` registration | `apolloCache.ts` :45–47 `HandshakeCodeLookup: { keyFields: false, … }` | ✅ accurate |
| "cached inline under `Query.findStudentByHandshakeCode`" | `apolloCache.test.ts` behavioral proof (4.1 outcome §4: inline normalization, loss-safe rewrite, zero invariant-118) | ✅ accurate |

**Polish applied (one continuation line appended to the verified entry — the 4.1 entry predated the canonical doc, so it carried no doc pointer):**

```
  see `docs/parents/handshake-code-discovery.md` for the payload contract.
```

- The entry now reads: `- \`HandshakeCodeLookup\` (fields: \`linkable\`, \`maskedName\`) — no \`id\`; masked parent-discovery payload, cached inline under \`Query.findStudentByHandshakeCode\`; see \`docs/parents/handshake-code-discovery.md\` for the payload contract.`
- No other change: field list, no-id rule, and inline-caching note retained verbatim (all verified accurate); `keyFields: false` remains stated once at section level (the policy binds every listed type — no per-entry repetition, consistent with the other four entries). Entry stays one list item.

### 1.7 Root `AGENTS.md` — Important References pointer (line 463, appended at the end of the list)

```
- `docs/parents/handshake-code-discovery.md` — Parent handshake-code discovery canonical reference (code format + generation contract by reference, minimal masked payload with no `id`, governance-exclusion collapse, null-not-error not-found, advisory `linkable` semantics, binding link-request forward contract, brute-force posture)
```

- **Rationale:** appended after the applicant-lifecycle entry (the list's established append-at-end order; no reordering); `- \`path\` — description` style with topic-summary parenthetical matches every neighbor.

---

## 2. Edit summary

| # | File | Line(s) | Kind |
|---|---|---|---|
| 1 | `docs/auth/user-registration.md` | 77 | one-line cross-link (§2, appended) |
| 2 | `docs/workflows/04-parent-supervision-handshake.md` | 5 | one-line cross-link (header related-notes blockquote) |
| 3 | `docs/specs/state-machine-invariants.md` | 222 | one-line pointer (§7.2, after INV-P1..P4 table) — pointer-check positive |
| 4 | `backend/services/AGENTS.md` | 4 | one-line rule + doc pointer |
| 5 | `backend/db/repo/AGENTS.md` | 40 | one-line rule + doc pointer |
| 6 | `frontend/graphql/AGENTS.md` | 100–102 | polish of verified-accurate 4.1 entry: one continuation line (doc pointer) |
| 7 | `AGENTS.md` (root) | 463 | one-line Important References pointer |

Propagation check (grep `docs/parents/handshake-code-discovery.md` across `*.md`): exactly one pointer in each of the 7 target files + the canonical doc's own plan artifacts — no strays, no duplicates.

## 3. 7.2.SR Semantic Review

- **Single rule line + doc pointer (every AGENTS addition):** PASS — services (:4), repo (:40), frontend (:100–102), root (:463) are each exactly one bullet/list entry carrying the rule essence plus the `docs/parents/handshake-code-discovery.md` pointer.
- **No duplicated guidance between docs and AGENTS entries:** PASS — the AGENTS lines carry only compressed rules (minimal-payload shape; equality-predicate read pattern + prepared-statement N/A; embedded-type no-id listing); the narrative (Why, mask algorithm contract, governance table with fail-closed arms, null-precedence rationale, brute-force layer table, link-flow forward contract steps) lives solely in the canonical doc. No AGENTS line restates a doc section verbatim or adds new guidance.
- **No plan-artifact references in added lines:** PASS — zero REQ ids, task ids, ticket ids, or `ai/plans` paths in any added line (the neighboring pre-existing applicant-lifecycle/dev3-003 entries were not touched).
- **Formatting conventions matched:** PASS — backtick-path (user-registration uses no markdown links); blockquote `> **Label:**` (workflow 04 header); plain paragraph after table (invariants); fully-bold bullet (services top doc-pointer block); bold-lead bullet + `See \`doc\`.` tail (repo Rules); wrapped list-entry continuation (frontend embedded-types); `- \`path\` — description` (root Important References).
- **One-line discipline:** PASS — one added line per file (6 new lines + 1 pointer line + 1 polish continuation line extending an existing entry, the polish explicitly sanctioned by the task); zero renumbering, zero content edits to existing sections/rows/bullets (invariant rows verified byte-identical post-edit).

## 4. Gates

- `bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates` run for **all 7** edited files:
  - `tsgo` ✅ passed per-file ("no errors for …") in every run.
  - oxlint: "No files found to lint … 0 files" for every run — **structurally N/A for markdown** (identical to the 7.1 / 4.1 / 3.2 precedents; the script exits 1 on the 0-files oxlint stage, not on a finding).
- Compensating controls (7.1 precedent): `bun run tsgo` project-wide → exit 0; `bun run biome:check` → 546 files checked, no fixes applied, exit 0 (baseline unchanged, same 546-file count as the R10 final review).
- Zero code files touched; zero tests required (no behavior change); zero git state operations.

## 5. Forward notes

- Next: Task 7.3 Outcome Synthesis & Final Gate (task ledger, baseline comparison, coverage table, deferred-ledger final state, DEV1-014/015 planning seeds). The Phase 7 checkbox flips belong to 7.3.
- The three doc cross-links + four AGENTS pointers complete the knowledge-propagation surface asserted missing by the R10 review ([HIGH] process finding) — code was already READY; with 7.1 + 7.2 shipped, only 7.3 remains before the plan is closeable.
