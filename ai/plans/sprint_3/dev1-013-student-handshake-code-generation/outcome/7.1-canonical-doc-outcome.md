# Task 7.1 Outcome — Canonical Doc: `docs/parents/handshake-code-discovery.md`

**Ticket:** DEV1-013 (Student Handshake Code Generation) — Phase 7: Knowledge Propagation & Documentation
**Task:** 7.1 Canonical Doc + 7.1.SR Semantic Review
**Requirements:** REQ-080 (canonical doc), REQ-024 (code-space & collision posture recorded), with the discovery/mask/governance/forward-contract content sourced from REQ-015..021, REQ-033, REQ-034
**Git discipline:** ZERO git state operations (read-only only). No code files touched — this task is additive documentation only.

**Pre-execution knowledge read (protocol #1):** tasks.md Task 7.1 + 7.1.SR in full; specs.md REQ-015..021/024/030/033/034/080/081 + B.12/B.13/B.14/INV-P1 traceability rows; `docs/auth/user-registration.md` §2 (generation contract — full doc); `docs/teachers/applicant-lifecycle.md` in full (structure-convention exemplar + §3 null-precedence precedent); `docs/README.md` (index-registration check); `docs/graphql/error-handling-contract.md` + `docs/graphql/domain-error-extensions-code.md` (Why→Pattern→Rules→What-NOT-to-Do→Rollout→Related structure exemplars); live implementation verification of every cited file (see §4); worklog.md prior entries incl. round-R10 process finding (Phase 7 not yet executed) and 3.2/4.1 sub-loop precedents for non-code paths.

---

## 1. Deliverable

**Created:** `docs/parents/handshake-code-discovery.md` (232 lines, new `docs/parents/` directory).

Section map (grep-verified order, `rg -n "^## "`):

| # | Section | Content |
|---|---|---|
| — | Header block | Domain / Specs / Status + "single canonical reference" intro mirroring `applicant-lifecycle.md` + illustrative-non-authoritative code-block disclaimer + generation-vs-consumption split statement |
| 1 | **Why** | Discovery as the supervision feature's top PII-leak risk; structural minimalism ruling; INV-P1 verbatim quote + discovery-≠-monitoring invariant; Workflow 04 §4.2 quote; Workflow 04 soft-delete governance ruling |
| 2 | **Pattern** | §1 two read surfaces (role gates, args, results, `$all` 401/403); §2 code format (`KSB-[0-9A-F]{8}`, unique constraint, immutability, normalize-then-validate); §3 generation contract by reference (UUID-hex derivation, 16⁸ = 4,294,967,296 ≈ 4.3B keyspace, 23505 cause-chain detection, `HANDSHAKE_RETRY_LIMIT = 5` in-transaction retry, per-attempt savepoint, `ConflictError` on exhaustion, lock suite); §4 discovery pipeline (5 strictly ordered steps + repo/service ownership split); §5 name-mask contract (total / deterministic / grapheme-aware, `Intl.Segmenter` + code-point fallback, `***` cluster + empty placeholder, examples table); §6 payload (`HandshakeCodeLookup`, no `id` by design, `keyFields: false`) |
| 3 | **Rules** | R1 payload closure; R2 null-not-error (+ applicant §3 precedent); R3 governance-exclusion collapse (full row-state table incl. fail-closed arms, window math, one captured `now`); R4 `linkable` semantics (advisory, per-child never per-parent, B.12/B.13, `ON DELETE SET NULL` consequence, link requests out of scope / B.14); R5 binding forward contract for the link-request flow (re-resolve by code in own tx, never trust stored/transmitted id, re-check `parent_id IS NULL` server-side, re-evaluate governance); R6 brute-force posture (role gate, negligible payload, 32-bit keyspace, fail-open stub, link-time re-validation as today's real limiter); R7 oracle hygiene + side-effect absence (no caching); R8 logging discipline (two log sites, code never logged) |
| 4 | **What NOT to Do** | 12 prohibitions (payload widening, error-on-miss, governed-vs-nonexistent distinction, `linkable`-as-authorization, id-based link resolution, fail-open governance, code logging, LIKE/interpolation, caching, in-service rate limiting, generator/mask re-implementation, admin override) |
| 5 | **Rollout Summary** | 13-row shipped-surface table (path → test lock) + zero-schema-drift / zero-writes note |
| 6 | **Related Documents** | Generation contract (user-registration §2), null-precedence precedent (applicant-lifecycle §3), specs/invariants, recorded decisions (A.2/A.3/B.12/B.13/B.14), Workflow 04, error-contract docs, authoritative implementation paths |

## 2. 7.1.SR — Structure checklist

| Checklist item | Result | Evidence |
|---|---|---|
| Standard structure Why → Pattern → Rules → What NOT to Do → Rollout Summary → Related Documents | ✅ exact order | `rg -n "^## "` → lines 13 / 32 / 115 / 182 / 199 / 223 |
| English-only domain doc | ✅ | no non-English prose (Arabic strings appear only as mask *examples* — input/output data, matching the mask suite's own fixtures) |
| Docs-convention header (Domain/Specs/Status + single-canonical-reference intro) | ✅ | mirrors `docs/teachers/applicant-lifecycle.md` + `docs/auth/user-registration.md` |
| Illustrative-non-authoritative code blocks | ✅ | stated in intro; every section cites authoritative paths |
| No plan-artifact references (no REQ-xxx, no ticket ids, no `.ai/plans`, no deferred-item ids) | ✅ | `rg -n "REQ-\|DEV1\|DEV2\|DEV3\|ai/plans\|\.ai/\|deferred\|D[1-5] "` → **0 matches** (exit 1). Spec references follow the `applicant-lifecycle.md` convention: specs/invariants cited by doc path, recorded decisions cited by their canonical A.x/B.x letters (the same convention `docs/workflows/04-*.md` and `docs/specs/functional-requirements.md` themselves use), INV-P1 quoted verbatim from `docs/specs/state-machine-invariants.md:217`. Forward consumers are named domain-side ("the future link-request flow", "the future rate-limiting hardening stream") — no ticket ids |
| No emoji | ✅ | emoji-codepoint grep → 0 matches |
| Aspirational-content ban (every numeric/behavioral claim matches shipped code) | ✅ | §4 table below |

## 3. Docs-index registration decision

`docs/README.md` is the **master index of the original 5-stage refinement pipeline artifacts only** (domain/, architecture/, scenarios/, workflows/, specs/) — it does not register any later canonical doc (`docs/auth/*`, `docs/teachers/applicant-lifecycle.md`, `docs/graphql/*`, `docs/backend/*`, `docs/quality/*` are all absent from its tree). Following the `applicant-lifecycle.md` precedent, **no README registration is required or made** for `docs/parents/handshake-code-discovery.md`. Cross-links *into* `user-registration.md` §2, `workflows/04-*.md`, and `state-machine-invariants.md` are explicitly Task 7.2's scope (REQ-081) and were NOT performed here.

## 4. 7.1.SR — Claims-vs-code verification evidence (every claim → live source)

| Doc claim | Verified against (file:line) |
|---|---|
| `KSB-` prefix + exactly 8 uppercase hex `[0-9A-F]`; pattern `/^KSB-[0-9A-F]{8}$/`; ReDoS-free shape; dependency-free module | `shared/constants/handshake-code.constants.ts:24,33` (+ docblock :26-32 "linear-time" wording) |
| Normalize = `trim()` then `toUpperCase()`; validation always on the normalized value | same file :48-55 |
| `varchar(50)` NOT NULL + unique constraint `students_handshake_code_unique`; `parent_id` FK `ON DELETE SET NULL` (+ index) | `backend/db/schema/students/students.ts:29-30,38-39` |
| Generator: `crypto.randomUUID()` hex → uppercase → first 8 → `KSB-XXXXXXXX`; embedded `KSB-` literal with lock-pinned parity | `backend/services/auth/registration.service.ts:92-95` (+ constants docblock :16-23 parity statement) |
| 16⁸ = 4,294,967,296 ≈ 4.3 billion (32-bit keyspace); negligible collision at registration scale | arithmetic + `docs/auth/user-registration.md` §2.1 |
| Bounded in-transaction retry, `HANDSHAKE_RETRY_LIMIT = 5` | `registration.service.ts:56` (`const HANDSHAKE_RETRY_LIMIT = 5`), loop :347-387 |
| 23505 via `DrizzleQueryError.cause`-chain traversal (never top-level) + SQLite message parity | `registration.service.ts:103-118` |
| Per-attempt savepoint: insert wrapped in Drizzle nested transaction; rejected insert rolls back only the attempt; same tx reusable | `backend/db/repo/students/student.repository.ts:56` (`tx.transaction(async sp => …)`), docblock :38-47 (the D4 savepoint restructure — referenced domain-side without the plan id) |
| `ConflictError` on exhaustion; `HANDSHAKE_COLLISION` per collision / `HANDSHAKE_EXHAUSTED` on exhaustion; non-collision surfaces immediately | `registration.service.ts:352-363, 370-381` |
| Pipeline order: normalize→validate (`VALIDATION` before any DB read) → equality read → miss `null` → governance collapse `null` → two-key payload | `backend/services/students/student-handshake.service.ts:125-153` |
| Read: `students ⋈ users` on shared PK, `WHERE handshake_code = $1 LIMIT 1`, fixed column list (parentId, fullName + 5 governance cols), optional `tx` propagated, no LIKE/ILIKE/sql-templates/inArray; repo returns row faithfully, service owns governance filtering | `backend/db/repo/students/student.repository.ts:105-155` (both branches) |
| Governance table: isDeleted/isBlocked always excluded; !suspended included; suspended + missing `suspendedAt` OR null/≤0 `suspendedPeriodDays` → excluded (fail-closed, plain nullable int, no CHECK); window end strictly after `now` → excluded; at/before `now` → lapsed/included; window end = start + days×86,400,000 ms; one captured `now` per invocation | `backend/services/students/student-handshake.helpers.ts:17,39-58` (`MS_PER_DAY = 86_400_000`; strict `>` at :58) + `student-handshake.service.ts:145` (`new Date()` single capture) |
| Mask: total / deterministic / grapheme-aware (`Intl.Segmenter` grapheme, locale-free; `Array.from` code-point fallback, never splits surrogates); first grapheme per whitespace-separated part + `***`; whitespace runs collapse; empty-after-trim → `***`; examples `Yusuf`→`Y***`, `أحمد محمد`→`أ*** م***`, `ع`→`ع***`; no length-of-remainder signal | `shared/lib/mask-full-name.ts:23-62` (MASK_CLUSTER `***` :24, MASK_EMPTY_PLACEHOLDER `***` :27, `NAME_PART_SEPARATOR /\s+/u` :30, `firstGrapheme` :39-46, `maskFullName` :53-62) |
| Payload closed: `HandshakeCodeLookupReturnType` = readonly `{ maskedName, linkable }`; internal `HandshakeDiscoveryRowType` never surfaces | `backend/types/students/student.types.ts:12-24` |
| Pothos object: exactly two structural passthrough fields, NO `id` BY DESIGN | `backend/graphql/pothos/students/handshake-code.pothos.ts:32-39` (+ docblock :16-20) |
| Apollo `keyFields: false` embedded registration | `frontend/providers/apollo/apolloCache.ts:45-47` |
| SDL: `findStudentByHandshakeCode(code: String!): HandshakeCodeLookup` (nullable), two-field type | `frontend/graphql/generated/schema.graphql:24-27,76`; `backend/graphql/query/students/handshake-code.query.ts:82-97` (`nullable: true`) |
| `linkable = parentId === null` computed server-side; raw `parentId` never leaves the service | `student-handshake.service.ts:150-153` |
| Role gates: `$all { authenticated, role:[Parent|Student] }`; anonymous → `UNAUTHORIZED` (401), authenticated wrong role (sibling role, teacher, admin) → `FORBIDDEN` (403) pre-resolver; no admin override | `handshake-code.query.ts:57-97` + pinned live by `backend/graphql/test/handshake-code-surface.test.ts:202-263` |
| Two log sites only (`VALIDATION` malformed; `STUDENT_NOT_FOUND` self-read miss); submitted code never logged (not even post-validation); misses/collapses silent; no try/catch — bubble to masking boundary | `student-handshake.service.ts:26-33 (discipline docblock), 78-85, 127-134` |
| i18n keys `handshakeCodeInvalid` / `studentHandshakeNotFound`; `handshakeCode` UI namespace (en/ar parity) | service :133, :85; `app/(dashboard)/parent/handshake/page.tsx:26,37`; `shared/locale/handshakeCode-namespace.parity.test.ts` exists |
| Rate limiting: fail-open stub — `checkRateLimit` always succeeds; real throttling owned by the future hardening stream | `backend/lib/ratelimit.ts:83-92` (+ docblock "minimal fail-open stub", "future hardening work") |
| INV-P1 wording | `docs/specs/state-machine-invariants.md:217` (quoted verbatim) |
| B.12 / B.13 / B.14 wording (one parent per student via single FK; parent links multiple children, separate confirmation each; 7-day pending-link expiry) | `docs/specs/open-decisions-and-gaps.md:141-157` |
| Workflow 04 §4.2 "limited matching information" quote; soft-deleted ⇒ parent loses access immediately ruling | `docs/workflows/04-parent-supervision-handshake.md:90-92, 161` |
| Null-precedence precedent (applicant profile's one-null answer) | `docs/teachers/applicant-lifecycle.md:97` (§3) |
| Write-immutability: only registration insert writes `handshakeCode` | `student.repository.ts:51-73` is the only `.values({ … handshakeCode … })` site (grep-verified); scan-locked by `backend/db/test/logic/students/handshake-code-immutability-scan.test.ts` |
| Every Rollout-Summary path + test-lock path exists in-tree | verified via LS/Glob/Grep: all 13 table rows (constants/mask/types/repo/helpers/service/query/pothos/surface-test/apolloCache/documents/Card/DiscoveryContainer/page/guards/journey/parity tests) |
| Zero schema drift / zero writes for this surface | `students` schema pre-existing (registration-era); both service flows are pure reads (service docblock :34-35) |

**Deliberate wording decision:** the doc says "exactly 8 uppercase hexadecimal characters `[0-9A-F]`" (matching `HANDSHAKE_CODE_PATTERN` and the UUID-hex generator) and does NOT repeat `user-registration.md` §2.1's "alphanumeric" prose — that wording is stale (recorded as forward note D5 in the deferred ledger) and would have been an aspirational/inaccurate claim.

## 5. Post-edit verification (protocol #2)

- `bun run scripts/health/sub-loop.ts docs/parents/handshake-code-discovery.md --lifecycle duplicates`:
  - `tsgo` ✅ passed ("no errors for docs/parents/handshake-code-discovery.md").
  - oxlint structurally **N.A. for markdown** — "No files found to lint … on 0 files" (same tool behavior as the 3.2 generated-artifacts probe and the 4.1 AGENTS.md N.A.-by-design precedent; documented, not a violation).
  - Compensating controls (3.2 precedent): project-wide `bun run tsgo` → **exit 0**; `bun run biome:check` → **"Checked 546 files … No fixes applied"**, exit 0 (baseline unchanged, tree idempotent — the new doc introduces no findings).

## 6. SR fix log (self-review corrections applied before completion)

1. Why-section wording: "first letter of each name part" → "first grapheme of each name part" (the mask is grapheme-based; "letter" was imprecise for combining marks/emoji).
2. Code-format garbage list: removed the garbled "lowercase hex surviving normalization is impossible" fragment → "non-hex characters such as unicode letters or symbols" (lowercase input is *accepted* via normalization; only non-hex structure fails closed).

## 7. Task 7.1 bullet coverage map

| Task 7.1 bullet | Doc location |
|---|---|
| Code format + generation contract, link user-registration §2, collision model, describe-never-re-implement | Pattern §2 + §3, Related Documents, What NOT to Do (last two items) |
| Discovery payload minimalism ruling + GraphQL object NO id by design | Why, Pattern §6, Rules R1 |
| Mask algorithm contract (deterministic, grapheme-aware, total, empty placeholder) | Pattern §5 |
| Governance-exclusion collapse rule + fail-closed suspension-window handling | Why (ruling), Pattern §4 step 4, Rules R3, What NOT to Do |
| Null-not-error precedent (DEV2-004 applicant) | Rules R2 (cites applicant-lifecycle §3 — domain-side citation, no ticket id) |
| Binding forward contract for the parent-link ticket | Rules R5 + R4 advisory clause + What NOT to Do items 4-5 |
| Brute-force posture (role gate, minimal payload, 32-bit keyspace, future rate-limit stream) | Rules R6 + What NOT to Do |
| `linkable` semantics (advisory, per-child never per-parent, link requests out of scope) + INV-P1 anchoring | Why (INV-P1 verbatim + discovery-≠-monitoring), Rules R4 |

## 8. Next actions

- **7.2** Cross-links & AGENTS.md propagation (user-registration §2 one-liner, workflow-04 pointer, invariants pointer line if absent, four AGENTS one-liners) — the doc is now ready to be pointed at.
- **7.3** Outcome synthesis & final gate — this outcome is a ledger row (7.1 → completed).
