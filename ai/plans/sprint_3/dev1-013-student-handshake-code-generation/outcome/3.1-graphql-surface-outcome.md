# Task 3.1 Outcome — Pothos Object Type + Query Module (GraphQL Surface)

**Ticket:** DEV1-013 (Student Handshake Code Generation)
**Plan directory (actual):** `ai/plans/sprint_3/dev1-013-student-handshake-code-generation/`
**_Requirements:_ REQ-030, REQ-031, REQ-050, REQ-060, REQ-061**
**Git discipline:** ZERO git state operations (no checkout/switch/stash/restore/branch/commit/add). Read-only verification only — tree observed on `main` @ `78c0836` (== `feat/dev1-013-student-handshake-code-generation` per the orchestrator protocol; flips harmless; working-tree delta is the 4 new files + 1 barrel line listed in §1).

**Pre-execution knowledge read (protocol #1):** worklog.md in full (setup, 0.1–0.3, 1.1–1.4, 2.1×2, 2.2, 2.3, orchestrator-git-protocol, D4-fix, 2.4 §11 carry-forward, 2.M §9 carry-forward), tasks.md protocol + Task 3.1 in full (incl. 3.1.QL/TE/SEC/SR/IV), specs.md REQ-030/031/050/060/061 + permission-matrix traceability rows, plan.md §3.1–§3.4 + D7/D8 rows, ALL outcome/*.md (2.4 §11 Phase-3 carry-forward — binding; 2.M §9 — binding; 0.2 §4 scope-system verification incl. builder.ts:69-128 + applicant.query.ts:56-61 citations), `backend/graphql/AGENTS.md` + `backend/graphql/pothos/AGENTS.md` + `backend/graphql/query/AGENTS.md`, `docs/graphql/api-gateway-and-routing.md` §8, `docs/teachers/applicant-lifecycle.md` §3, the live precedents (`teachers/applicant.query.ts` + `teachers/applicant.pothos.ts` + `query/index.ts` + `query/teachers/index.ts` + `builder.ts` + `gqlSchema.ts` + `schema-surface.test.ts` + `allowlist-coverage.test.ts` + `applicant-profile.test.ts`), and the auto-discovered `.agents/instructions/{backend,tests}.instructions.md` in full.

---

## 1. Deliverables (exact surface)

### 1.1 `backend/graphql/pothos/students/handshake-code.pothos.ts` (NEW — 39 lines)

```ts
export const HandshakeCodeLookupPothosObject = gqlSchemaBuilder
  .objectRef<HandshakeCodeLookupReturnType>("HandshakeCodeLookup")
  .implement({
    fields: t => ({
      maskedName: t.exposeString("maskedName"),
      linkable: t.exposeBoolean("linkable"),
    }),
  });
```

- Backed EXCLUSIVELY by the canonical `HandshakeCodeLookupReturnType` from `@/backend/types` (1.3's closed two-key type; `readonly maskedName: string; readonly linkable: boolean`) — zero local type definitions in the file.
- **NO `id` field BY DESIGN** (REQ-019/D7): deliberate embedded value type following the `HealthCheck` precedent (docs §8.2's embedded-type exception to the general expose-`id` rule). The frontend `keyFields: false` Apollo registration is Task 4.1's deliverable (D7's second half).
- **NO Pothos sub-directory barrel created** — decision documented: NO `backend/graphql/pothos/*/index.ts` exists anywhere (teachers/, auth/, shared/, users/ all barrel-less); Pothos object registration rides the importing query module's static import transitively through the `gqlSchema.ts` side-effect chain (verbatim precedent: `applicant.pothos.ts`'s own docblock, "whose import of this module transitively registers the type"). A barrel nothing imports would be dead code. The task text's "+ sub-directory barrel if the convention requires one" resolves to: the convention requires NONE.
- No types-registry file exists to update (verified — object refs register via `.implement()` at import time; proven live: the built schema contains `HandshakeCodeLookup`).

### 1.2 `backend/graphql/query/students/handshake-code.query.ts` (NEW — 97 lines)

Two root query fields, both registered at import time via `gqlSchemaBuilder.queryField(...)` (no named exports, per `query/AGENTS.md`):

```ts
myHandshakeCode: String!  — authScopes: { $all: { authenticated: true, role: [UserRole.Student] } } }
findStudentByHandshakeCode(code: String!): HandshakeCodeLookup (nullable)
                           — authScopes: { $all: { authenticated: true, role: [UserRole.Parent] } } }
```

- Resolver bodies are THIN: `StudentHandshakeService.getMyHandshakeCode(ctx.user.id, ctx.locale)` and `StudentHandshakeService.findStudentByHandshakeCode(args.code, ctx.locale)` — delegate + locale propagation, NO tx (queries own no transaction), NO try/catch (DomainErrors `VALIDATION`/`STUDENT_NOT_FOUND` propagate pre-localized to the DEV3-002 boundary finalizer), NO `ctx.t` (service owns all messages via `getServerTranslations`).
- `myHandshakeCode` carries the `if (!ctx.user) throw new UnauthorizedError("Authentication required.")` TypeScript-narrowing branch — mirrors `applicant.query.ts:69-71` verbatim (unreachable in practice: the `$all { authenticated }` scope denies anonymous callers first; the repo-wide no-non-null-assertion rule forbids dereferencing `ctx.user` directly). NOT inline auth logic.
- `UserRole` is a VALUE import from `@/backend/enum/users/user-role.enum`; `StudentHandshakeService` imported from the highest barrel `@/backend/services` (applicant.query.ts precedent; the students line ships in commit `78c0836`). ALL imports top-level STATIC (Bun ESM rule).
- **D8 exact:** both scope maps use the explicit `$all` conjunction — byte-identical shape to `applicant.query.ts:56-61`, per `docs/teachers/applicant-lifecycle.md` §3 and REQ-031.

### 1.3 Barrels (wiring per docs §8 rule 1)

- `backend/graphql/query/students/index.ts` (NEW — 14 lines): `import "./handshake-code.query";` — side-effect-only sub-directory barrel (the sanctioned exception to the no-imports-in-barrels rule: "GraphQL mutation/query layers where imports register types in the GraphQL schema").
- `backend/graphql/query/index.ts` (MODIFIED, +2/−1): added `import "./students";` (alphabetical, after `./recitation.query`, before `./teachers`) + doc-comment enumeration line updated. This is the ONLY edit to an existing file — registration wiring, zero operation modifications.

### 1.4 `backend/graphql/test/handshake-code-surface.test.ts` (NEW — 291 lines, 22 tests / 44 expect())

Unit-tier schema gate, co-located with the existing graphql unit suites (`backend/graphql/test/` — the schema-surface/allowlist-coverage introspection tier; full live-wire matrix is 3.3's integration tier by the task text). Five sections:
1. **Field surface** — `myHandshakeCode` is `String!` with ZERO arguments (BOLA: no caller-supplied identity surface); `findStudentByHandshakeCode` is NULLABLE `HandshakeCodeLookup` (miss/governed → null, never an error) with EXACTLY one argument `code: String!` (the only client-controllable input).
2. **Declared scopes** — both fields' `extensions.pothosOptions.authScopes` snapshots deep-equal the documented maps verbatim (same introspection substrate as `allowlist-coverage.test.ts`); `$all` conjunction shape pinned with EXACTLY the `["authenticated", "role"]` inner keys (D8 — a plain ANY-semantics map is provably absent); sibling/teacher/admin roles provably ABSENT from each role set (no admin/supervisor read override).
3. **Object shape closure** — `HandshakeCodeLookup` is a `GraphQLObjectType` exposing EXACTLY `maskedName: String!` + `linkable: Boolean!`, carries NO `id` field (`Object.hasOwn` proof), selecting `id` FAILS `validate()` behaviorally, and selecting `{ maskedName linkable }` validates clean.
4. **Scope evaluation over the real engine** — anonymous (empty context) → exactly one error with `extensions.code = "UNAUTHORIZED"` on BOTH queries; authenticated parent/teacher/admin on `myHandshakeCode` and student/teacher/admin on `findStudentByHandshakeCode` → exactly one error with `extensions.code = "FORBIDDEN"` — executed through the built schema + scope-auth plugin via `graphql()`, NO server boot, NO DB (denials fire pre-resolver; happy paths are 3.3's tier). This is the unit-tier 401/403 non-interchange pin of the D8 contract.
5. **Allowlist posture** — both names ABSENT from `PUBLIC_OPERATIONS`; the registry itself stays EXACTLY the frozen six (allowlist untouched — no anonymous surface added).

### 1.5 NOT touched

`tasks.md` (orchestrator-owned), any existing GraphQL operation, `backend/lib/gateway/public-operations.ts` (verified frozen-six by test), any schema/migration/service/repository file, any DEV1-001/002/DEV2-002-owned surface, sibling test suites, `backend/graphql/test/schema-surface.test.ts` (pre-existing red — see §6; baselines NOT absorbed silently).

## 2. Requirements traceability

| Requirement | How satisfied |
|---|---|
| **REQ-030** | `myHandshakeCode` has ZERO arguments (unit-pinned: `args` length 0); identity is solely `ctx.user.id` (resolver passes it; no other identity surface exists). Discovery treats `code` as the out-of-band capability; parent-role gate fires BEFORE any read (scope evaluation precedes resolver execution — pinned by §1.4 section 4: denial codes surface with no service outcome). |
| **REQ-031** | Both fields carry the exact `$all` conjunction maps (deep-equal pinned on the built schema's snapshot). Anonymous → `UNAUTHORIZED` and wrong-role (incl. sibling role, teacher, admin) → `FORBIDDEN`, BOTH proven live over the real scope-auth engine (unit tier) — the 401/403 split is not inverted/mixed. NO admin/supervisor read override: no `superAdmin`/`permission` scope keys (scope-key closure pinned); admin is in the proven-denied role table. Full live-wire matrix over real tokens = 3.3. |
| **REQ-050** | Resolver files contain ZERO `new Error(`, zero catch blocks (grep §5); the only direct throw is the `UnauthorizedError` DomainError narrowing mirror of `applicant.query.ts`. `ValidationError`/`NotFoundError` propagate from the service pre-localized to the boundary finalizer unchanged. |
| **REQ-060** | Schema gains EXACTLY the two queries + `HandshakeCodeLookup` — proven by fresh-print SDL diff vs the committed artifact (§4): only `myHandshakeCode: String!`, `findStudentByHandshakeCode(code: String!): HandshakeCodeLookup`, `type HandshakeCodeLookup { linkable: Boolean! maskedName: String! }`. Zero mutations, zero enums, zero modified operations, allowlist untouched. Thin resolvers + static imports only. |
| **REQ-061** | Object backed by `HandshakeCodeLookupReturnType` from `@/backend/types` (no local types — grep-zero); NO `id` field BY DESIGN (type-level + behavioral proof); the Apollo `keyFields:false` registration + embedded-types list entry are Task 4.1's half of D7 (carry-forward §9). |

## 3. 3.1.QL — Quality Loop evidence (sub-loop `--lifecycle duplicates`, true exit codes)

| # | File | Exit |
|---|---|---|
| 1 | `backend/graphql/pothos/students/handshake-code.pothos.ts` | **0** |
| 2 | `backend/graphql/query/students/handshake-code.query.ts` | **0** (re-run after the §7.1 comment fix — still 0) |
| 3 | `backend/graphql/query/students/index.ts` | **0** |
| 4 | `backend/graphql/query/index.ts` | **0** |
| 5 | `backend/graphql/test/handshake-code-surface.test.ts` | **0** (`check:duplicates` skipped-by-design for `*.test.ts` — same behavior 2.4 recorded) |

The QL loop CAUGHT one real defect mid-flight (twice): the test file initially failed tsgo (TS2769 `toEqual` literal-union overload; TS18048 optional `errors`/`queryType` narrowing) and the query file's doc comment contained the literal text `await import(` which trips the gateway's A1 text scanner (§7.1). Both fixed within the loop; final passes all green.

## 4. 3.1.TE — Test Engineering evidence

### 4.1 New suite run

`bun run test/scripts/run-test.ts backend/graphql/test/handshake-code-surface.test.ts` → **exit 0 — 22 pass / 0 fail / 44 expect() calls** (re-run on the final state after all edits: exit 0).

### 4.2 Regression battery (every suite exit 0)

| Suite | Result |
|---|---|
| `backend/graphql/test/context-keys.test.ts` | exit 0 |
| `backend/graphql/test/request-id.test.ts` | exit 0 |
| `backend/graphql/test/error-finalizer.test.ts` | exit 0 |
| `backend/lib/gateway/transport-guard.test.ts` | exit 0 |
| `backend/lib/gateway/public-operations.test.ts` | exit 0 |
| `backend/lib/gateway/route-inventory.test.ts` | exit 0 |
| `backend/lib/gateway/static-assertions.test.ts` (A1–A5) | exit 0 — 17/17 (after §7.1 fix) |
| `frontend/graphql/test/gateway/allowlist-coverage.test.ts` | exit 0 — 8/8 (default-deny gate: both new fields are SCOPED, so the unscoped-set equality still holds) |
| `app/api/graphql/test/graphql-route.transport.test.ts` | exit 0 |
| `app/api/graphql/test/graphql-route.pipeline-order.test.ts` | exit 0 |
| `app/api/health/test/health-route.probe.test.ts` | exit 0 |
| `frontend/graphql/test/teachers/applicant-profile.test.ts` (live wire) | exit 0 — 8/8, 30 expect (the pre-existing `$all` scoped query still behaves end-to-end) |
| `test/workflows/parents/handshake-discovery.test.ts` (journey) | exit 0 — 8/8, 53 expect |
| `backend/services/students/student-handshake.service.test.ts` | exit 0 — 20/20, 176 expect |
| `bun run tsgo` (project-wide) | exit 0 — 0 errors |
| `bun run biome:check` | exit 0 — "Checked 530 files in 5s. No fixes applied." (2.M baseline 526 + exactly my 4 new files) |

### 4.3 SDL delta proof (the exact surface 3.2 will commit)

Fresh `printSchema(lexicographicSortSchema(graphQLSchema))` vs the committed `frontend/graphql/generated/schema.graphql` (written to /tmp — the committed artifact is 3.2's to regenerate):

```diff
+ type HandshakeCodeLookup {
+   linkable: Boolean!
+   maskedName: String!
+ }
+   findStudentByHandshakeCode(code: String!): HandshakeCodeLookup
+   myHandshakeCode: String!
```

EXACTLY the three sanctioned additions (REQ-060) — nothing else. Because the committed SDL already contains the upstream applicant surface (it was codegen-synced at HEAD), 3.2's committed diff will contain ONLY this ticket's additions.

### 4.4 Pre-existing reds (NOT this task's regressions — evidence below)

1. **`backend/graphql/test/schema-surface.test.ts`: 9 pass / 3 fail AT HEAD `78c0836` BEFORE any of my edits** (run captured pre-change): the upstream teacher-applicant ticket (`06f898c`, merged in `56c64cb`) added `myApplicantProfile`/`ApplicantProfile`/`ApplicantStatus` without refreshing the freeze baselines. After my change the same 3 tests fail with my additions ALSO listed, plus the **expected-transient** codegen-sync failure (committed SDL lacks my types until 3.2 runs `generate:gqlSchema && codegen` — docs §8.5 "Codegen in the same commit"; 3.2 re-greens it). The freeze-baseline refresh would absorb documented foreign drift — deliberately NOT done silently here (§7.2 decision); orchestrator/plan-owner call.
2. **`backend/graphql/test/error-contract-matrix.test.ts`: 35 pass / 1 fail** — the wire-tier `_health` test still expects the LEGACY `{ _health: "ok" }` String! shape while `_health` was retyped to `HealthCheck!` upstream (schema-surface's own docblock documents the sanctioned retyping; docs §10 known-limits table pre-dates it). My change cannot influence `_health`; the failing expectation is foreign, pre-existing upstream debt.
3. **Live-boot hazard discovered (operational, pre-existing):** a stale `next-server` left listening on :3066 by a prior suite's runner blocks the next live-boot suite with a 63s timeout; `bun run test:ui:kill` targets port **3099** in this tree (docs say 3066 — tooling drift). I killed my own leftover server PID and the suite booted fine. Carry-forward for 3.3 (§9).

## 5. 3.1.SEC — Security & Tenancy Audit (independent re-derivation of plan §3.4 from the code)

| Caller (ctx) | `myHandshakeCode` | `findStudentByHandshakeCode` | Code derivation |
|---|---|---|---|
| Anonymous (`ctx.user` null) | `UNAUTHORIZED` (401) | `UNAUTHORIZED` (401) | `$all` evaluates BOTH scopes; `authenticated` THROWS `UnauthorizedError` when `!ctx.user` (builder.ts:113-118); explicit scope throws pass through `scopeAuthOptions.unauthorizedError` VERBATIM (builder.ts:98-104) → `extensions.code UNAUTHORIZED`. Live-pinned (unit §1.4.4). |
| Student | ✅ own code only (`ctx.user.id`; zero args) | `FORBIDDEN` (403) | role scope `[Parent]` excludes Student → returns false → localized `ForbiddenError` (403). Live-pinned both directions. |
| Parent | `FORBIDDEN` (403) | ✅ discovery payload | Sibling-role denial live-pinned. |
| Teacher | `FORBIDDEN` (403) | `FORBIDDEN` (403) | Live-pinned. |
| Supervisor (permission-group identity; underlying `users.role` ≠ student/parent) | `FORBIDDEN` (403) | `FORBIDDEN` (403) | No `permission` scope declared on either field (scope-key closure = exactly `["authenticated","role"]`); a supervisor's underlying role (teacher/admin per the 4-member `UserRole`) fails the role scope identically. |
| Super Admin | `FORBIDDEN` (403) | `FORBIDDEN` (403) | No `superAdmin` scope (grep-zero in both files); `[Student]`/`[Parent]` exclude `Admin`; admin cell live-pinned → `FORBIDDEN`. |

- **Not inverted/mixed:** anonymous cells assert `UNAUTHORIZED` (never FORBIDDEN); wrong-role cells assert `FORBIDDEN` (never UNAUTHORIZED) — both over the real engine, on both queries.
- **No bypass surface:** zero `superAdmin`/`permission` keys; zero `notImpersonating`; no scopeless posture (allowlist gate green, names absent from `PUBLIC_OPERATIONS`).
- **Client-controllable input surface:** `myHandshakeCode` → 0 args (asserted); `findStudentByHandshakeCode` → exactly `code: String!` (asserted). Payload closure: object exposes exactly `maskedName`/`linkable` (asserted; no `id`); no input types defined; zero mutations (SDL diff proves the frozen mutation set unchanged — schema-surface's "ZERO new mutations" assertion still passes).
- **Tenancy:** identity exclusively context-derived; no ids traverse the discovery payload (type-level + behavioral).

## 6. 3.1.SR — Semantic Review

- Thin resolvers ONLY: delegate + `ctx.locale` propagation on both fields; the single `if (!ctx.user)` branch is the sanctioned applicant-precedent TS-narrowing guard (documented in-file), not business logic.
- Zero hardcoded i18n keys: no `getServerTranslations`, no `ctx.t(` in the resolver files (grep-zero) — the service owns every user-facing message.
- Zero types defined in resolver files (grep-zero `interface`/`type X =` in pothos/query files); backing type from `@/backend/types`.
- Barrel registrations complete: `students/index.ts` (new) + `query/index.ts` (+1 side-effect import) — the full chain `gqlSchema.ts → query/index.ts → students/index.ts → handshake-code.query.ts → handshake-code.pothos.ts` proven live by the built schema containing the type + both fields.
- Zero dead code: every import used; no named exports in the query module (by convention); biome/tsgo/oxlint/lint:type-aware all green (sub-loop ×5).
- Zero `console.*`, zero module-level mutable state, zero `await import(` text (A1 gate), zero plan-artifact references in code comments (grep: REQ-/DEV1-013/plan.md/tasks.md → zero hits across all 4 new files).

## 7. 3.1.IV — Instruction Verification

- `backend/graphql/AGENTS.md` — COMPLIANT: Pothos code-first; scopes per the canonical authScopes contract; nullability aligned with TS types (`String!` for the non-null self-read; explicit `nullable: true` for the discovery payload); resolvers delegate to services; locale propagated; canonical-type pattern (`objectRef<HandshakeCodeLookupReturnType>`); DomainError discipline (boundary-only masking; no plain `Error`). **Documented deviation:** the general "expose `id` on every object for Apollo cache normalization" rule is superseded for this object by the embedded-value-type exception (docs §8.2, `HealthCheck` precedent; REQ-019/D7; `keyFields:false` lands in 4.1).
- `backend/graphql/query/AGENTS.md` — COMPLIANT: side-effect imports only; new sub-directory `students/` with its own barrel + top-level barrel line (steps 1–4 of "Adding New Queries"; step 5 — codegen — is Task 3.2 by the plan's explicit split); no Pothos definitions inside the query tree; `@/` alias imports.
- `backend/graphql/pothos/AGENTS.md` — COMPLIANT: no local types; no enum registrations (nothing to register in `shared/enum.pothos.ts`); DataLoader rule N/A (no per-parent field resolvers — the object exposes only passthroughs and the root fields are single root calls).
- `docs/graphql/api-gateway-and-routing.md` §8 — COMPLIANT: resolver placement + side-effect barrel wiring (rule 1); object in `pothos/<domain>/` backed by `backend/types` (rule 2); no enums (rule 3 N/A); authorization declared on EVERY new operation via authScopes — no allowlist entry needed (rule 4); codegen in the same change set = 3.2 (rule 5 — plan-sequenced).
- `docs/teachers/applicant-lifecycle.md` §3 — the `$all` conjunction contract mirrored byte-for-byte (shape, semantics, and rationale documented in the resolver's docblock).
- `.agents/instructions/backend.instructions.md` — COMPLIANT: shortest-barrel service import; `./`-relative barrels; static imports only (A1 gate green); DomainError subclasses only; no hardcoded error strings (the one literal mirrors builder.ts's own scope message — the applicant.query.ts precedent).
- `.agents/instructions/tests.instructions.md` — COMPLIANT: `bun:test` imports; no `any` (guarded `isRecord` reflection helpers per the allowlist-coverage precedent); null guards before assertions; no `console.*`; no DB rules triggered (pure schema tier); mandated runner used.
- No `oxlint-disable` comments anywhere (linting-rules.md).

### 7.1 Mid-flight fixes the gates caught (documented for the record)

1. **A1 text-scanner trip:** the query file's original docblock contained the literal string `await import(...)` while DESCRIBING the prohibition — `static-assertions.test.ts` A1 scans raw file text (comments included) for `await import(`. Reworded to "dynamic import expressions". The gate re-ran green (17/17). Lesson carried forward: never write the forbidden literal, even in comments, inside `backend/graphql/{query,mutation,pothos}`.
2. tsgo strictness in the test file (toEqual literal-union overload → typed `string[]` views, the allowlist-coverage convention; optional `errors`/`queryType` narrowing → explicit null guards) — all resolved inside the QL loop.

### 7.2 Deliberate NON-changes (with rationale)

- **`schema-surface.test.ts` freeze baselines NOT refreshed** — refreshing would silently absorb the upstream applicant drift (`myApplicantProfile`/`ApplicantProfile`/`ApplicantStatus`) alongside this ticket's sanctioned additions. Per the plan's no-silent-absorption discipline the pre-existing red is documented (§4.4) and the refresh decision is escalated to the orchestrator/plan owner: a refresh must add the THREE foreign names (documented drift, failing at HEAD before this task) PLUS this ticket's three (`myHandshakeCode`, `findStudentByHandshakeCode`, `HandshakeCodeLookup`).
- **Codegen NOT run here** — 3.2's explicit deliverable ("Same Change Set"); the transient codegen-sync red in schema-surface.test.ts is the designed pipeline state between 3.1 and 3.2 (docs §8.5).

## 8. Deviations & decisions versus the task text

1. **No Pothos sub-directory barrel** — task said "+ sub-directory barrel if the convention requires one"; verified the convention requires NONE (all four existing pothos sub-directories are barrel-less; registration rides the query module import). §1.1.
2. **Query sub-directory barrel + top-level barrel line** — required by `query/AGENTS.md` steps 1–4 ("If a new sub-directory was created, add `import "./<subdir>";` to the top-level index").
3. **Unit test location** — `backend/graphql/test/` (the existing graphql unit-suite home, per the task's "find where existing graphql unit tests live"); scope introspection follows the `allowlist-coverage.test.ts` substrate; the live-engine 401/403 cells use `graphql()` over the built schema (no boot/DB) — a unit-tier addition ABOVE the task's minimum ("schema-shape unit test") that directly serves 3.1.SEC's independent re-derivation mandate.
4. **`myHandshakeCode` resolver async + narrowing branch** — mirrors `applicant.query.ts` (the repo's exemplar for a scoped zero-arg self-read) rather than a terser non-async form.

## 9. Carry-forward

**For 3.2 (codegen sync):**
- Run `bun run generate:gqlSchema && bun codegen`; expected committed diff = EXACTLY the three additions in §4.3 (the applicant surface is already committed upstream, so no foreign drift will appear in the diff — verified by the pre-change codegen-sync test being green at HEAD).
- After codegen, `schema-surface.test.ts`'s codegen-sync assertion re-greens automatically. The remaining 3 failures there are pre-existing/foreign (§4.4) — the freeze-baseline refresh is an orchestrator decision (§7.2), NOT to be absorbed silently by 3.2 either.
- Sub-loop on generated artifacts: likely skipped-by-design if the health tooling excludes generated paths (record the decision per 3.2.QL).

**For 3.3 (integration matrix):**
- The unit tier pins 401/403 + scope snapshots; 3.3 owns the FULL live-wire matrix over REAL tokens: reuse `applicant-profile.test.ts`'s `registerAndLogin` (public registerUser + login for student/parent) + direct-DB fixture pattern for admin (registerUser structurally rejects admin), per-request `Authorization: Bearer` header isolation.
- Add super-admin + supervisor-underlying-role cells (the plan matrix's supervisor row derives through users.role — build the fixture with a teacher/admin role + permission group if a supervisor identity is needed, or document the derivation).
- Failure cells: `VALIDATION` (malformed), `null` data with NO error (miss/governed — assert response-shape equality between the two channels, REQ-J3's network twin), `STUDENT_NOT_FOUND` (defect fixture), locale propagation en+ar on both error messages.
- Forbidden-key scan on the payload (id/studentId/userId/email/phone/parentId/isDeleted/isBlocked/suspended*) + self-identity two-student fixture.
- Service-spy zero-calls assertion on denied cells (Tier 4): my unit tier proves denial-by-code-shape; 3.3 should spy the service method to prove zero executions on denied cells over the wire.
- **Live-boot hazard:** stale `next-server` on :3066 from a prior suite blocks the next boot (63s timeout; `test:ui:kill` targets 3099 in this tree, not 3066). Before 3.3's live tier, check `ss -tlnp | rg 3066` and kill leftovers.
- `error-contract-matrix.test.ts` wire-tier `_health` legacy expectation is stale upstream debt (§4.4.2) — 3.3 reads `docs/graphql/error-handling-contract.md` + `domain-error-extensions-code.md`; if that tier is touched, the `_health` row is a known foreign red to NOT attribute to DEV1-013.

**For 4.1 (frontend):** D7's second half — `HandshakeCodeLookup: { keyFields: false }` in `apolloCache.ts` typePolicies + the embedded-types list entry; neither document selects `id`.

## 10. Files created/modified by this task

- CREATED: `backend/graphql/pothos/students/handshake-code.pothos.ts` (39 lines)
- CREATED: `backend/graphql/query/students/handshake-code.query.ts` (97 lines)
- CREATED: `backend/graphql/query/students/index.ts` (14 lines — sub-directory barrel)
- CREATED: `backend/graphql/test/handshake-code-surface.test.ts` (291 lines, 22 tests)
- MODIFIED: `backend/graphql/query/index.ts` (+2/−1 — `import "./students";` + doc comment)
- CREATED: this outcome file + the Task 3.1 worklog entry
- NOT touched: `tasks.md`, any existing GraphQL operation, `public-operations.ts`, any schema/migration/service/repo file, `schema-surface.test.ts`, any sibling suite.

**Verdict: ✅ Task 3.1 COMPLETE** — QL exit 0 ×5 files · TE 22/22 new suite + 14 regression suites exit 0 + tsgo 0 errors + biome 0 findings (530 files) · SEC matrix re-derived live (401 anon / 403 wrong-role incl. sibling + admin; no bypass; `code` the only input) · SR/IV verified with grep + gate evidence · SDL delta EXACTLY the three sanctioned additions · 2 pre-existing foreign reds documented (not absorbed) · codegen handoff staged for 3.2.
