# Task 2.4 Outcome — Service: `backend/services/students/student-handshake.service.ts` + Governance Helper

**Ticket:** DEV1-013 (Student Handshake Code Generation)
**Plan directory (actual):** `ai/plans/sprint_3/dev1-013-student-handshake-code-generation/`
**_Requirements:_ REQ-014, REQ-015, REQ-016, REQ-017, REQ-018, REQ-019, REQ-020, REQ-021, REQ-022, REQ-023, REQ-030, REQ-043, REQ-050, REQ-052, REQ-053, REQ-054, REQ-070**
**Git discipline:** ZERO git state operations (no checkout/switch/stash/restore/branch/commit/add). Read-only verification only — tree observed on `main` @ `5dd3b7e` (== `feat/dev1-013-student-handshake-code-generation` per the orchestrator protocol; flips harmless).

**Pre-execution knowledge read (protocol #1):** worklog.md in full (setup, 0.1–0.3, 1.1–1.4, 2.1 both dispatches, 2.2, 2.3, orchestrator-git-protocol, D4-fix), tasks.md protocol + Task 2.4 in full (incl. 2.4.QL/TE/SEC/SR/IV), specs.md REQ-014..023/030/043/050/052/053/054/070, plan.md §3.3 + §4.1 + §4.2/§4.3/§4.4, ALL outcome/*.md (2.2 §7 carry-forward signatures — binding; 2.3 §9 repo integration notes — binding; 1.4 §6 i18n API spellings — binding; 1.1/1.2/1.3/2.1/D4/0.2/0.3), `backend/services/AGENTS.md` + `backend/AGENTS.md` + `backend/db/test/AGENTS.md`, the journey test `test/workflows/parents/handshake-discovery.test.ts` + `test/workflows/helpers/journey-fixtures.ts` in full, and the in-tree service precedents (`registration.service.ts`, `applicant-lifecycle.service.ts` + its `.service.test.ts`).

---

## 1. Deliverables (exact surface)

### 1.1 `backend/services/students/student-handshake.helpers.ts` (NEW — 45 lines)

Pure runtime predicate (NOT a `.types.ts` file — runtime helper per the service-layer split rule):

```ts
export function isGovernanceExcludedFromDiscovery(
  governance: Pick<
    UserSelectType,
    "isDeleted" | "isBlocked" | "suspended" | "suspendedAt" | "suspendedPeriodDays"
  >,
  now: Date
): boolean
```

Implemented verbatim per plan §4.1's binding code: `isDeleted || isBlocked` → excluded; `!suspended` → included; suspended with missing `suspendedAt`/`suspendedPeriodDays` → **excluded (fail-closed)**; active-window math `suspendedAt + periodDays·86_400_000 > now` → excluded. The only deviation from the plan's literal text: `suspendedPeriodDays == null` written as `=== null` (the column type is `number | null` — identical semantics, eqeqeq-clean). `UserSelectType` imported from `@/backend/types` (barrel — verified live). Zero imports otherwise; zero module-level mutable state; no clock reads (`now` is a parameter).

### 1.2 `backend/services/students/student-handshake.service.ts` (NEW — 155 lines)

`export namespace StudentHandshakeService` (the repo's service convention — matches `RegistrationService`/`ApplicantLifecycleService` and the journey test's named import):

```ts
export async function getMyHandshakeCode(studentUserId: number, locale: string): Promise<string>
// null row → NotFoundError("STUDENT", <studentHandshakeNotFound msg>) → extensions.code = STUDENT_NOT_FOUND

export async function findStudentByHandshakeCode(
  code: string,
  locale: string,
  tx?: DBTransaction
): Promise<HandshakeCodeLookupReturnType | null>
```

`findStudentByHandshakeCode` follows the mandated STRICT order: (1) `normalizeHandshakeCode` → `isHandshakeCode` guard — failure → `ValidationError` (single-arg form → `extensions.code = "VALIDATION"`) with the `handshakeCodeInvalid` message, thrown **BEFORE any DB read**; (2) `StudentRepository.findDiscoveryByHandshakeCode(normalized, tx)` — `tx` propagated when present (REQ-043); (3) null row → return `null` (never an error); (4) `isGovernanceExcludedFromDiscovery(row, new Date())` → return `null` (byte-identical to the miss channel); (5) return `{ maskedName: maskFullName(row.fullName), linkable: row.parentId === null }` — the closed two-key `HandshakeCodeLookupReturnType` from `@/backend/types`; `parentId` never leaves the service.

Logging: `logger.logDomainError` fires ONLY on the two expected rejections — malformed code (ctx `{ code: "VALIDATION", entity: "students", locale }` — no entityId exists at this layer) and missing own row (ctx `{ code: "STUDENT_NOT_FOUND", entity: "students", entityId, locale }`). **The submitted code string is NEVER logged — not even post-validation** (stricter than REQ-052's allowance). Happy paths, misses, and governance collapses are silent (REQ-053). Zero `console.*`; zero writes/audit/notification rows (REQ-023); **zero catch blocks** — unexpected internals bubble unswallowed to the DEV3-002 GraphQL masking boundary, which owns the single correlated `logger.error` line (`backend/lib/errors/error-masking.ts` — the applicant-lifecycle service precedent; a service-level catch-log-rethrow would double-log against that contract).

i18n: `getServerTranslations(locale).errorsTranslations` — SINGLE-arg bundle + flat-key property access (`studentHandshakeNotFound`, `handshakeCodeInvalid`), exactly per Task 1.4 §6 carry-forward / 0.3 F3/F4 binding amendments. No `Translation` enum, no namespace-string second arg, no `getBackendTranslations`.

### 1.3 `backend/services/students/student-handshake.service.test.ts` (NEW — 638 lines, 20 tests / 176 expect())

4-tier service spec, fixtures-only (entities exclusively via `entity-setup.ts` helpers). Structure + conventions mirror `backend/services/teachers/applicant-lifecycle.service.test.ts` (service tests live next to the code, per `backend/services/AGENTS.md` §Testing).

### 1.4 Barrels (convention completion)

- `backend/services/students/index.ts` (NEW): `export * from "./student-handshake.service";` — every services domain subdir has its own barrel (auth/shared/teachers precedent).
- `backend/services/index.ts` (MODIFIED, +1 line): `export * from "./students";` (alphabetical between `./shared` and `./teachers`) — "Parent barrels re-export from nested barrels" per `.agents/instructions/backend.instructions.md` barrel conventions. Additive only; `StudentHandshakeService` is a unique symbol (verified: project tsgo exit 0).

### 1.5 NOT touched

`tasks.md` (orchestrator-owned), any schema/migration file, `backend/db/repo/**` (2.3-owned, verified byte-identical), any DEV1-001/002/DEV2-002-owned surface, the journey harness helpers, sibling test suites.

## 2. Requirements traceability

| Requirement | How satisfied |
|---|---|
| **REQ-014** | `getMyHandshakeCode(studentUserId, locale)` — identity is ONLY the argument (no tx/ctx surface); repo read `findHandshakeCodeByStudentId`; null → localized `NotFoundError("STUDENT", …)` → `STUDENT_NOT_FOUND`. Pinned by tests: verbatim happy path (committed row, default read path) + parent-id edge + absent-id edge. |
| **REQ-015/019** | Discovery returns EXACTLY `{ maskedName, linkable }` — `Object.keys` set-equality asserted on every payload shape test; `parentId`/ids/contact fields compile-blocked by `HandshakeCodeLookupReturnType` (1.3's keyof anchor) and runtime-pinned (serialized scan contains no parent id, no "parentId" substring). |
| **REQ-016** | Valid-format-missing → `null`, never an error (miss test + the governance deep-equal test: `null === null` channel identity). |
| **REQ-017/054** | `maskedName = maskFullName(row.fullName)` — the shared pure helper (1.2), deep-imported per its carry-forward; asserted `toBe(maskFullName(fullName))` AND `not.toBe(fullName)`. |
| **REQ-018** | `linkable = parentId === null` computed service-side; already-linked fixture → `linkable: false` + two-key shape; incumbent parent identity never disclosed. |
| **REQ-020** | `normalizeHandshakeCode` THEN `isHandshakeCode` — strictly pre-DB (Tier-3 spy: `StudentRepository.findDiscoveryByHandshakeCode` received ZERO calls across 21 malformed probes); lowercase + surrounding-whitespace variants of a REAL code resolve deep-equal to the exact-code lookup. |
| **REQ-021** | `isGovernanceExcludedFromDiscovery(row, new Date())` → `null`, deep-equal to the nonexistent-code result; three governance fixtures (isDeleted / isBlocked / active suspension) + lapsed-suspension VISIBLE + incomplete-window fail-closed. |
| **REQ-022/035** | Pre-DB regex gate + the repo's parameterized equality only; zero LIKE/ILIKE/`sql` templates in the service/helpers (grep §5). **`escapeLikeWildcards` is NOT APPLICABLE — affirmed: no LIKE/ILIKE query exists anywhere on this surface** (the only predicate is `handshake_code = $1` / `eq()`); recorded here per REQ-022 so pentester waves don't flag its absence as a gap. |
| **REQ-023** | Zero writes: Tier-4 count probes — `audit_logs`/`notifications` totals unchanged across a full read battery (happy + miss + governance + both rejection arms), per-tracked-actor attributable rows = 0. |
| **REQ-030** | `getMyHandshakeCode` has zero caller-supplied identity surface (the GraphQL query is zero-argument; the resolver passes `ctx.user.id` — 3.1's contract). Discovery treats the code as the out-of-band capability with governance collapse — oracle-hygiene rationale in §5. |
| **REQ-043** | `tx?: DBTransaction` optional-LAST on `findStudentByHandshakeCode`, propagated verbatim into the repo read; proven behaviorally (in-rollback fixture discoverable ONLY through the supplied tx). `getMyHandshakeCode` has no tx parameter by the binding 2.2 §7 signature (single read — no logical operation spans multiple calls, so no tx exists to propagate). |
| **REQ-050** | DomainError subclasses only — grep: zero `new Error(` in service+helpers (§5); `ValidationError` → `VALIDATION`; `NotFoundError("STUDENT", …)` → `STUDENT_NOT_FOUND` (entity-name form, never the full code). |
| **REQ-052/D11** | `logger.logDomainError` with bounded context on expected rejections; submitted code NEVER logged (Tier-4: `JSON.stringify(mock.calls)` contains none of the distinctive probes, raw or normalized); bounded key-set pinned (`["code","entity","locale"]` for VALIDATION; `+entityId` for STUDENT_NOT_FOUND). |
| **REQ-053** | Silent happy path — log spy counts 0 calls across self-read hit, discovery hit, miss, and governance collapse. |
| **REQ-070** | Coverage: **100.00% functions + 100.00% lines on BOTH new modules** (§4.2); branch coverage proven structurally + behaviorally (bun emits no branch metric — §4.3). |

## 3. 2.4.QL — Quality Loop evidence

| # | Command | Exit |
|---|---|---|
| 1 | `bun run scripts/health/sub-loop.ts backend/services/students/student-handshake.helpers.ts --lifecycle duplicates` | **0** (tsgo ✓ oxlint ✓ biome:check ✓ lint:type-aware ✓ check:duplicates ✓) |
| 2 | `bun run scripts/health/sub-loop.ts backend/services/students/student-handshake.service.ts --lifecycle duplicates` | **0** |
| 3 | `bun run scripts/health/sub-loop.ts backend/services/students/student-handshake.service.test.ts --lifecycle duplicates` | **0** (check:duplicates skipped-by-design for `*.test.ts`) |
| 4 | `bun run scripts/health/sub-loop.ts backend/services/students/index.ts --lifecycle duplicates` | **0** |
| 5 | `bun run scripts/health/sub-loop.ts backend/services/index.ts --lifecycle duplicates` | **0** |
| 6 | **DEFERRED 2.2 F6 debt —** `bun run scripts/health/sub-loop.ts test/workflows/parents/handshake-discovery.test.ts --lifecycle duplicates` | **0** — the journey file's FIRST-EVER sub-loop pass (impossible at 2.2: the intentional missing-service TS2307 short-circuited tsgo). The service now resolves; all five checks pass. |

**Journey-file formatting note (the only tree change outside my new files):** the sub-loop's biome step normalized the journey file's import ordering + line wrapping (`git diff --stat`: +12/−5, ZERO semantic change — verified hunk-by-hunk: import sort, two `catchJourneyError` call wrappings, one `map` callback wrapping). This is the sanctioned tool applying canonical formatting during the file's first QL pass; the journey suite re-ran GREEN on the formatted state (§6). Re-runs: journey 8/8 exit 0.

## 4. 2.4.TE — Test Engineering evidence

### 4.1 Test run

`bun run test/scripts/run-test.ts backend/services/students/student-handshake.service.test.ts` → **exit 0 — 20 pass / 0 fail / 176 expect() calls** (332ms; log under `logs/2026-08-29T05-52-30/…`).

| Tier | Tests | What is pinned |
|---|---|---|
| 1 | self-read verbatim; missing-row NotFoundError (parent edge + absent id, ctx + logging); localized ar/en; exact-code hit (two keys, mask, linkable true); already-linked (linkable false, no parentId key, serialized scan); valid-miss null; **in-rollback tx-propagation hit**; helper clean/deleted/blocked arms | Every statement/branch arm of both new modules + tx propagation + payload closure |
| 2 | lowercase resolves identically; whitespace variants (spaces/tab/CRLF) resolve identically; lapsed suspension VISIBLE; incomplete-window fail-closed; helper boundary (endsAt == now exactly → lapsed; +1 day → excluded; −1 day → visible) | Normalization algebra + the strict active-window boundary |
| 3 | 21 malformed probes (LIKE wildcards `%`/`_` prefix-suffix-inside, backslash, `KSB-`, 7/9-hex under/over-length, wrong prefix, no dash, non-hex `g`, empty, whitespace-only, Arabic, RTL `\u202E`, bidi `\u2066…\u2069`, emoji, NUL suffix, 5000-char tail) — ALL reject `ValidationError`/`VALIDATION` with the byte-equal localized message, **repo-method spy received ZERO calls**; three governance fixtures → null, deep-equal to nonexistent | Pre-DB fail-closed validation + governance/null channel identity |
| 4 | extensions-code mapping; ar/en distinct localized denials (both flows); domain-log bounded context + NEVER the submitted code (raw or normalized); silent happy/miss/collapse contract (0 log calls); zero writes (audit/notification totals unchanged + per-actor attributable = 0) | Security/i18n/logging contracts |

**DB discipline:** the tx-plumbed case runs inside `runInRollback` with `tx` passed to every call. The no-tx service reads (both flows go through the repository's DEFAULT executor — `queryDb` on a separate pooled connection) **cannot see uncommitted rollback fixtures**, so they run against ONE committed `beforeAll` cast (9 students incl. governance variants + 2 parents, one committing `db.transaction`) hard-deleted in `afterAll` (FK-safe: students → users, tracked ids) with post-teardown residue probes — the committed-fixture hygiene pattern (rule 9) proven by 2.3's Tier-2 default-executor tests and the journey layer. The absent probe code is DERIVED (last-hex flip) and repo-grounded against committed state before use; the absent user id is derived from `max(users.id) + 1_000_000`.

### 4.2 Coverage (REQ-070) — machine evidence

`bun run scripts/lib/run-locked-cmd.ts handshake-service-coverage bun --env-file=.env.test test backend/services/students/student-handshake.service.test.ts --coverage --timeout=60000` → exit 0. Text reporter:

```
 backend/services/students/student-handshake.helpers.ts | 100.00 | 100.00 |
 backend/services/students/student-handshake.service.ts | 100.00 | 100.00 |
```

lcov (same suite, `--coverage-reporter=lcov`):
- `student-handshake.helpers.ts`: **FNF:1 FNH:1 · LF:16 LH:16** (100% functions, 100% statements/lines)
- `student-handshake.service.ts`: **FNF:5 FNH:5 · LF:48 LH:48** (100% functions, 100% statements/lines)
- **Zero BRDA records — bun emits no branch metric** (documented 1.2/2.3 precedent).

### 4.3 Branch coverage — structural enumeration + behavioral distinction

**helpers.ts — conditional universe = 4 constructs, every arm executed AND behaviorally distinguished:**
1. `isDeleted || isBlocked`: isDeleted-only → true · isBlocked-only → true (second OR arm reached) · both-false → falls through (clean fixture).
2. `!suspended`: true (clean) · false (every suspended fixture).
3. `!suspendedAt || suspendedPeriodDays === null`: arm1 (suspendedAt null, period set) · arm2 (suspendedAt set, period null) · both-false (active + lapsed fixtures) — distinct observable outcomes (excluded vs evaluated window).
4. `endsAt > now` (strict): true (active: committed fixture, journey 6c, `governanceFixture(now,1)`) · false (lapsed: committed fixture + BOTH boundary constructions `endsAt == now` and `endsAt == now − 1d`) — the boundary test distinguishes the arms with a FIXED `now` (deterministic, no clock race).

**service.ts — conditional universe = 5 constructs, every arm executed AND behaviorally distinguished:**
1. `handshakeCode === null`: null (parent/absent id → NotFoundError) · non-null (happy path).
2. `!isHandshakeCode(normalized)`: true (all 21 malformed probes → ValidationError) · false (every valid lookup).
3. `row === null`: true (absent probe → null) · false (all hits).
4. `isGovernanceExcludedFromDiscovery(row, new Date())`: true (deleted/blocked/active/incomplete → null) · false (unlinked/linked/lapsed → payload).
5. `row.parentId === null`: true (unlinked) · false (linked) — surfaces as `linkable` true/false.
Plus the call-site tx propagation branch: with tx (in-rollback hit — fixture invisible to the default executor) vs without tx (committed-fixture hits).

→ **100% statement + branch coverage on both new modules** (statements machine-proven; branches by enumeration + behavioral distinction per the repo's sanctioned compensating methodology).

## 5. 2.4.SEC — Security & Tenancy Audit

- **BOLA/IDOR:** `getMyHandshakeCode` has ZERO caller-supplied identity surface — the only input is the resolver-derived id (GraphQL query is zero-argument; a parent id simply finds no students row → STUDENT_NOT_FOUND, tested). `findStudentByHandshakeCode` treats the code as the deliberate out-of-band capability: parent-role gating lives at the GraphQL scope layer (3.1), payload minimality is compile-time closed (1.3's keyof anchor), and governance collapse makes governed children unfindable.
- **Oracle-hygiene rationale (documented per the task):** the discovery miss, the governance exclusion, and the nonexistent code all share ONE indistinguishable `null` channel — deep-equal asserted (Tier 3) and journey-pinned (REQ-J3, byte-identical). No log, error, or timing distinction exists: governance collapses are SILENT (no log call — Tier 4), malformed inputs never reach the DB (µs regex reject), and the payload is identical for every hit regardless of linkage state beyond the sanctioned `linkable` boolean. The only information channels are exactly the two sanctioned ones: valid-format code → (`null` | masked payload), and role failures → canonical FORBIDDEN at the GraphQL tier.
- **BOPLA:** return type is the closed readonly `HandshakeCodeLookupReturnType` (`readonly maskedName` / `readonly linkable` — TS2540-pinned by 1.3's proof file). **Grep-verified ZERO `...` spread operators in the entire diff's production files** (service, helpers, both barrels — `rg '\.\.\.'` → NONE); the test file also contains zero spreads (governance fixtures built field-by-field via `governanceFixture(...)`, cast assembled with explicit keys). The payload is constructed as a fresh literal — never a projection/echo of the DB row.
- **BFLA:** no permission logic in the service (scopes gate at GraphQL per plan §3.4 — 3.1's matrix); governance reads never mutate (zero writes proven by Tier-4 probes); no privileged entry points exist in the module.
- **Injection:** pre-DB regex gate (ReDoS-safe anchored pattern, 1.1) + the repo's parameterized equality only (`= $1` / `eq()`); zero `sql` templates, zero LIKE/ILIKE, zero string concatenation of caller input in the service/helpers (grep: the ONLY `console`/`catch`/`sql` hits are doc-comment prose). Tier-3 hostile sweep (wildcards, backslash, bidi, NUL, unicode, multi-KB) all rejected BEFORE the DB — the spy proves the DB never sees them. **`escapeLikeWildcards`: NOT APPLICABLE — no LIKE/ILIKE exists on this surface (affirmed in §2 per REQ-022).**

## 6. CRITICAL FINAL STEP — the 2.2 journey test is GREEN

`bun run test/scripts/run-test.ts test/workflows/parents/handshake-discovery.test.ts` → **exit 0 — 8 pass / 0 fail / 53 expect() calls** (1.96s):

```
(pass) Step 1 — System: registration generated canonical, unique, non-null handshake codes
(pass) Step 2 — Student (Yusuf): self-read returns own code verbatim; foreign ids never leak another student's code
(pass) Step 3 — Parent (Fatima): exact-code discovery returns the minimal two-key payload
(pass) Step 4 — Parent (Fatima): lowercase resolves identically; garbage rejects pre-DB; valid-missing is null
(pass) Step 5 — Second Parent (Karim): already-linked child resolves linkable:false with zero parent identity (REQ-J2)
(pass) Step 6a — governance isDeleted collapses discovery to null, byte-identical to a nonexistent code (REQ-J3)
(pass) Step 6b — governance isBlocked collapses discovery to null, byte-identical to a nonexistent code (REQ-J3)
(pass) Step 6c — active suspension collapses discovery to null, byte-identical to a nonexistent code (REQ-J3)
```

The Task-2.2 test-first RED (`Cannot find module '@/backend/services/students/student-handshake.service'`) is fully resolved — the journey drove the service through all 8 specs-§2.9 steps against REAL committed actors (real registrations, real governance flips, real link emulation) and afterAll teardown left zero residue. Re-run on the post-formatting file state: identical 8/8 exit 0.

## 7. Regression + environment evidence

| # | Command | Result |
|---|---|---|
| 1 | `bun run tsgo` (project-wide) | **exit 0, ZERO `error TS` lines** — Task 2.2's known intentional TS2307 is RESOLVED by this task (the last red diagnostic in the project is gone) |
| 2 | `bun run test/scripts/run-test.ts backend/db/test/repo/students/student.repository.test.ts` | exit 0 — 10 pass / 0 fail (2.3 repo suite unaffected) |
| 3 | `bun run test/scripts/run-test.ts backend/db/test/logic/auth/registration.service.test.ts` | exit 0 — 18 pass / 0 fail (DEV1-002 suite unaffected) |
| 4 | `bun run test/scripts/run-test.ts backend/db/test/logic/students/handshake-code-generation-locks.test.ts` | exit 0 — **8 pass / 0 fail** (D4 absorption lock stays GREEN) |
| 5 | `bun run test/scripts/run-test.ts backend/db/test/logic/students/handshake-code-immutability-scan.test.ts` | exit 0 — 14 pass / 0 fail (new modules trip no write scanner) |
| 6 | `bun run test/scripts/run-test.ts test/workflows/helpers/journey-fixtures.smoke.test.ts` | exit 0 — 6 pass / 0 fail |
| 7 | `bun run test/scripts/run-test.ts backend/services/teachers/applicant-lifecycle.service.test.ts` | exit 0 — 25 pass / 0 fail (service-test sibling unaffected) |
| 8 | DB residue probe (bun script on `app_db`, the pre-existing env-override target): users=4 (the four pre-existing seed demo accounts — admin/teacher/parent/student, verified by email), students=1 (pre-existing seed), notifications=0, audit_logs=0, fixture users (`%@test.local`/`%@journey.test`)=0 | ✅ rollback + tracked-teardown clean |

## 8. 2.4.SR — Semantic Review

- **Validation strictly precedes persistence reads** — asserted by the Tier-3 spy test (repo method: ZERO calls across the whole malformed corpus); the service's statement order makes it structurally impossible to read first.
- **Pure predicate imported from helpers** — the service contains zero governance logic of its own; the helper imports nothing but the canonical `UserSelectType` (type-only).
- **No module-level mutable state** — grep: zero `let`/`var` at any scope in service+helpers (consts only); the test file's `let cast` is test-run state (journey-precedent pattern).
- **No catch-and-swallow** — grep: zero `catch` in service+helpers; unexpected failures propagate to the DEV3-002 boundary (single correlated `logger.error` there — no double logging).
- **DomainError subclasses only** — grep: zero `new Error(` in service+helpers (test files use `new Error` solely as fail-loud guards, the sanctioned test pattern).
- **Zero plan-artifact references in code comments** — grep over all 5 files: zero hits for `ai/plans|dev1-013|DEV1-|REQ-0|tasks.md|plan.md|specs.md|outcome/`.
- **Import discipline** — biome organize-imports clean (sub-loop ×5); types from `@/backend/types`; value imports where values are used; the helpers deep-import matches the "runtime file, not .types.ts" rule.

## 9. 2.4.IV — Instruction Verification

- `backend/services/AGENTS.md` — COMPLIANT: domain-driven per-domain service (no monolith); business-logic hub owns validation/governance/error mapping (repo stays pure); i18n via `getServerTranslations` (the AGENTS.md two-arg spelling is superseded by 0.3 F3's binding amendment — single-arg is the LIVE signature, `server-graphql.ts:3`); types from `@/backend/types`; NO service-layer `.types.ts` created (the runtime helpers file is the sanctioned split target); service test lives next to the code; zero external providers touched (no mocking needed — pure DB reads).
- `backend/AGENTS.md` — COMPLIANT: layer flow Schema→Types→Repo→Service; `DBTransaction` from `@/backend/types`; error handling per the DomainError taxonomy (`NotFoundError` entity-name form; no numeric status literals).
- **Auto-discovered instruction files (2.4.IV):** `.github/instructions/` does NOT exist (0.3 F9), BUT this task discovered **`.agents/instructions/{backend,tests,frontend}.instructions.md`** (sub-loop's documented mapping target — prior outcomes searched only `.github/`; recorded here as a correction). Both applicable files were read in FULL:
  - `.agents/instructions/backend.instructions.md` — COMPLIANT: barrel conventions (nested `students/index.ts` with `export *`, `./` paths, ≤1 `/`, no imports; parent barrel re-exports the nested barrel); types from `@/backend/types`; i18n/logging/DomainError/no-nested-ternary rules all satisfied. (The "Prepared Statements CRITICAL" bullet is repo-layer guidance — this task adds no repository methods; 2.3's queryDb pattern was the documented choice for these reads.)
  - `.agents/instructions/tests.instructions.md` — COMPLIANT with ONE documented tension: rule "NEVER call `getServerTranslations` directly in tests" (rationale: legacy loader hang). The LIVE system is synchronous/compile-time; the committed in-tree service-test precedent (`applicant-lifecycle.service.test.ts:85,281-282`) and the 2.2 journey test (line 60) call it directly, and this suite passes 20/20 in 332ms with no loader involvement. Followed the service-test precedent (byte-equality against the translated literals — the strongest assertion form); `expectRepoError` used for every rejection; `runInRollback` for the tx-plumbed case; committed `beforeAll` fixtures + `afterAll` hard-delete per the file's own "Non-rollback data … needs afterAll cleanup" rule; bun:test imports; zero `any`; entity-setup signatures verified against source before use; tx position checked (service: optional-LAST, 3rd).
- `backend/db/test/AGENTS.md` (imported conventions) — rules 1/2/3/6/9/12/15/16/17/20 satisfied as detailed in §4.1.

## 10. Deviations & decisions versus the task text (all documented, none blocking)

1. **Suspension "ends-exactly-now" boundary (Tier 2).** The task text says "ends-exactly-now boundary → excluded per spec", but BOTH binding sources — plan §4.1's exact helper code (`suspendedAt + periodDays > now` → excluded) and the task's own Part A restatement — mandate the STRICT comparison, under which a window ending exactly at the evaluation instant has LAPSED (not excluded). The repo's own active-window precedent is identical (`applicant-lifecycle.service.ts`: `cooldownUntil > now`, "a cooldown that expires exactly NOW no longer blocks"). Implemented the plan §4.1 math verbatim and pinned BOTH boundary arms deterministically on the pure helper with a FIXED `now` (`endsAt == now` → included; `endsAt = now + 1d` → excluded; `endsAt = now − 1d` → included) plus the service-level ends-in-past VISIBLE fixture. A service-level exact-equality fixture is not constructible from outside (the service captures its own `new Date()` after the fixture write) — the helper-level test is the only deterministic way to pin the boundary.
2. **`getMyHandshakeCode` has no `tx` parameter** — per the binding 2.2 §7 carry-forward signature (the journey test calls it with 2 args); single read, no caller-owned tx exists at the GraphQL query layer. Its tests therefore use committed fixtures (default-executor reads cannot see rollback fixtures — 2.3's documented finding).
3. **Service adds no `logger.error` catch path** — the DEV3-002 boundary owns the single correlated `logger.error` for unexpected failures (error-masking.ts); a service-level catch-log-rethrow would double-log. Matches the ApplicantLifecycleService precedent ("unexpected internals bubble up unswallowed").
4. **Barrels added** (`students/index.ts` + top-barrel line) — not in the task's deliverables list, but required by the repo's barrel conventions (every services domain has one); additive, sub-loop-gated, tsgo-clean.
5. **Journey-file formatting-only diff** — produced by the deferred QL pass's biome step (§3); zero semantic change; journey re-verified GREEN.
6. **Test placement** — `backend/services/students/student-handshake.service.test.ts` (next to the code) per `backend/services/AGENTS.md` §Testing, NOT under `backend/db/test/` (that tree hosts repo/logic specs; the service-test precedent is applicant-lifecycle's co-located suite).

## 11. Carry-forward for Phase 3 (GraphQL resolvers — tasks 3.1/3.3)

1. **Import surface:** `import { StudentHandshakeService } from "@/backend/services/students/student-handshake.service";` (deep path — what the journey test uses) OR `@/backend/services` / `@/backend/services/students` barrels (both now re-export it). `isGovernanceExcludedFromDiscovery` is exported from the helpers module path only (service-internal concern — resolvers never need it).
2. **Resolver shapes:** `myHandshakeCode` → `StudentHandshakeService.getMyHandshakeCode(ctx.user.id, ctx.locale)`; `findStudentByHandshakeCode` → `StudentHandshakeService.findStudentByHandshakeCode(args.code, ctx.locale)` — NO tx from resolvers (queries own no transaction). `UserRole` as a VALUE import for authScopes.
3. **Error contract:** resolvers add NO try/catch — `ValidationError`/`NotFoundError` propagate to the boundary finalizer with `extensions.code` `VALIDATION`/`STUDENT_NOT_FOUND` already localized (service owns messages; `ctx.t` NOT needed for these flows). Locale propagation to BOTH services is mandatory (ar/en messages verified distinct).
4. **Payload contract for 3.1's Pothos object:** back `HandshakeCodeLookup` with `objectRef<HandshakeCodeLookupReturnType>` from `@/backend/types` — `maskedName: t.exposeString`, `linkable: t.exposeBoolean`, NO `id` field (D7); the service already returns exactly the two keys (Object.keys pinned).
5. **3.3 integration matrix can reuse this suite's fixture recipes** — governance variants via `createTestUser` overrides; the already-linked fixture needs only a parent user id in `students.parentId` (FK targets `users.id`; a `parents` row is NOT required for FK integrity).
6. **Null semantics over the wire:** discovery miss/governed → `null` DATA with NO error field (the GraphQL field must be nullable); only malformed input errors.

## 12. Residual risks

- **`app_db` env-override quirk** (pre-existing, 2.3 §8.2 finding, NOT touched): all DB tests run against `app_db`; this task's committed-fixture + tracked-teardown discipline keeps it residue-free (probes: §7 row 8 — only the 4 pre-existing seed users remain).
- **pg deprecation warning** (`client.query() when the client is already executing a query`) surfaces from `Promise.all` batches sharing one transaction client (afterAll teardown + repo suite patterns) — pre-existing in-tree pattern (journey-fixtures teardown identical), warning-only, non-blocking.
- **Boundary-semantics deviation** (§10.1) is flagged for the plan owner: if a review wave prefers inclusive `>=` at the exact boundary, it is a one-character change in the helper + one flipped assertion — both boundary arms are already test-pinned, so the flip is a two-line PR.
- **`getServerTranslations`-in-tests rule tension** (§9) is a stale-doc item for Phase 7's doc refresh alongside the other documented AGENTS.md drift (0.3 F12); behavior is green and precedent-backed.

## 13. Files created/modified by this task

- CREATED: `backend/services/students/student-handshake.helpers.ts` (45 lines)
- CREATED: `backend/services/students/student-handshake.service.ts` (155 lines)
- CREATED: `backend/services/students/student-handshake.service.test.ts` (638 lines, 20 tests)
- CREATED: `backend/services/students/index.ts` (domain barrel, 1 line)
- MODIFIED: `backend/services/index.ts` (+1 line: `export * from "./students";`)
- MODIFIED (formatting-only, by the deferred QL pass): `test/workflows/parents/handshake-discovery.test.ts` (+12/−5, zero semantic change, re-verified GREEN)
- CREATED: this outcome file + the Task 2.4 worklog entry
- NOT touched: `tasks.md`, any schema/migration/repository file, `backend/lib/**`, DEV1-001/002/DEV2-002-owned surfaces, sibling suites.

**Verdict: ✅ Task 2.4 COMPLETE** — all gates green: QL exit 0 ×6 files (incl. the deferred journey-file pass) · TE 20/20 + 100% funcs/lines on both new modules + branch-universe proof · journey 8/8 GREEN (the 2.2 RED is resolved) · all 7 regression suites green · project tsgo 0 errors · SEC/SR/IV verified with grep + runtime evidence · DB residue-free.
