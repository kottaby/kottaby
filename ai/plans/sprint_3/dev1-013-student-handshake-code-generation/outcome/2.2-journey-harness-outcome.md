# Task 2.2 Outcome — Scaffold `test/workflows/` + Handshake-Discovery Journey Test (TEST-FIRST, REQ-077)

**Ticket:** DEV1-013 (Student Handshake Code Generation)
**Plan directory (actual):** `ai/plans/sprint_3/dev1-013-student-handshake-code-generation/`
**_Requirements:_ REQ-077, REQ-J1, REQ-J2, REQ-J3, REQ-J4, REQ-J5, REQ-010, REQ-016, REQ-018, REQ-019, REQ-020, REQ-021, REQ-023**
**Git discipline:** ZERO git state operations (no checkout/switch/branch/stash/restore/commit/add). Read-only verification only: tree observed on `main` @ `1442168` — per the orchestrator git protocol `main` == `feat/dev1-013-student-handshake-code-generation` == `1442168`, so watchdog branch flips are content-neutral no-ops.

**Provenance:** a prior agent created the four implementation files (partial work, timed out before QL/TE/SEC/SR/IV/outcome/worklog). This task reviewed the partial work in full, fixed one lint defect, and completed the remaining gates.

---

## 1. Files (final state)

| File | Status | Role |
|---|---|---|
| `test/workflows/AGENTS.md` | **PRESERVED byte-identical** (pre-existing, tracked) | Journey layer rules — NOT scaffolded by this task per plan-review F1 (see §2) |
| `test/workflows/helpers/journey-fixtures.ts` | NEW (prior agent) + 1 lint fix (this task, §3) | Cast builders, tracked-ID registry, committed fixture writers, teardown + residue probes |
| `test/workflows/helpers/journey-fixtures.smoke.test.ts` | NEW (prior agent), verified GREEN | Harness smoke proof through EXISTING services only |
| `test/workflows/helpers/index.ts` | NEW (prior agent) | Pure `export *` barrel (rule 10) |
| `test/workflows/parents/handshake-discovery.test.ts` | NEW (prior agent), intentionally RED | specs §2.9 journey steps 1→8 (missing-service test-first RED) |
| `ai/plans/sprint_3/dev1-013-student-handshake-code-generation/outcome/2.2-journey-harness-outcome.md` | NEW (this file) | |
| `worklog.md` | APPENDED (Task 2.2 entry) | |

NOT touched: `tasks.md` (orchestrator-owned), `backend/lib/env.ts`, any production/schema/config file, `test/workflows/AGENTS.md`, any Task 2.1 lock-test surface (`backend/db/test/**` — concurrent sibling; its in-flight root artifact `tmp-collision-probe.test.ts` observed but not touched).

## 2. Review findings on the partial work

The plan assumed `test/workflows/` did not exist; plan-review **F1 (binding)** established that `test/workflows/AGENTS.md` already exists (committed, 10 hard rules) and that Task 2.2 must **preserve and extend — never overwrite** it. Verified preserved: `git diff HEAD -- test/workflows/AGENTS.md` → **empty** (exit 0). Nothing was missing from it (rule 5 already carries the notification-spy guidance; rule 3 the UUID prefixes; rule 9 the never-seeds rule), so no extension was required — the prior agent correctly left it untouched.

Full review of the four files against the live codebase (every import/signature verified against source):

| Check | Result |
|---|---|
| `RegistrationService.registerUser(input, locale, outerTx?)` from `@/backend/services/auth/registration.service` (namespace export, line 157) | ✓ matches fixtures' call `registerUser(input, locale)` — no `outerTx` (journey layer never wraps service calls in transactions) |
| `RegistrationSubmitInput` (fullName/email/phone/password/country/role; `@/backend/types` barrel → users barrel → registration.types:42) | ✓ all supplied fields valid; `validateInput` passes (password ≥ MIN_PASSWORD_LENGTH; role ∈ {student, parent}) |
| `StudentRepository.findHandshakeCodeByStudentId` / `findDiscoveryByHandshakeCode` from `@/backend/db/repo` (Task 2.3, committed @ 1442168) | ✓ both exist (student.repository.ts:73,105); barrel re-exports `./students` |
| `NotFoundError`/`ValidationError` from `@/backend/lib/errors` with `.code` (`STUDENT_NOT_FOUND` via `NotFoundError("STUDENT", …)`; `VALIDATION` default) | ✓ errors.ts:37,65 |
| `getServerTranslations(locale)` single-arg from `@/shared/locale/server-graphql`; flat keys `errorsTranslations.studentHandshakeNotFound` / `handshakeCodeInvalid` | ✓ server-graphql.ts:3; en/errors/index.ts:25-26 (Task 1.4 flat-key contract, plan-review F3/F4) |
| `HANDSHAKE_CODE_PATTERN` from `@/shared/constants/handshake-code.constants` (Task 1.1) | ✓ |
| `HandshakeCodeLookupReturnType` from `@/backend/types` (Task 1.3) | ✓ student.types.ts:12 (readonly, exactly `maskedName` + `linkable`) |
| Schema columns used by fixtures: `users.role` (userRole enum: student/parent lowercase), `parents.id` (shared-PK FK→users.id cascade), `notifications.userId`, `auditLogs.actorId`/`entityId`, `users.phone` (NO unique constraint — shared phone safe) | ✓ all verified in schema files |
| Journey steps 1→8 vs specs §2.9 + tasks.md 2.2 spec | ✓ complete: step 1 (pattern/unique/non-null), step 2 (+2b documented denial cross-reference), step 3 (two-key payload, maskedName ≠ fullName, linkable true), step 4 (lowercase resolves identically; 5 invalid probes → ValidationError/VALIDATION + localized message; valid-miss → null with repo grounding first), step 5 (Second Parent linkable:false, exactly two keys, no parent identity — JSON serialization scan), step 6a/b/c (isDeleted/isBlocked/active suspension → null byte-identical to absent code), step 7 (record-only comment), step 8 (teardown + residue probes + REQ-023 zero side effects) |
| Actor attribution comments on every step | ✓ (System / Student Yusuf / Parent Fatima / Second Parent Karim / Admin-domain fixture) |
| Service signatures documented in journey header match tasks.md 2.4 verbatim | ✓ (§7 carry-forward) |

**Defect found and fixed (the only one):** `journey-fixtures.ts` line 211 used `const row = rows[0]; if (row === undefined) …` — `sonarjs/different-types-comparison` lint error (sub-loop `lint:type-aware` exit 1): with `noUncheckedIndexedAccess` off, `rows[0]` types as non-undefined so the explicit `=== undefined` comparison is type-impossible, although the guard is real at runtime (a zero-row UPDATE returns an empty `.returning()` array). **Root-cause fix** (no lint-disable comments): switched to `rows.at(0)`, whose `T | undefined` return type makes the defensive comparison type-legitimate. The analogous guard in `linkStudentToParentFixture` (`const [row] = …; if (!row)`) is a truthiness check and lints clean — left as-is.

## 3. 2.2.QL — Quality Loop evidence

| # | Command | Exit |
|---|---|---|
| 1 | `bun run scripts/health/sub-loop.ts test/workflows/helpers/journey-fixtures.ts --lifecycle duplicates` | **1 → 0** (first run failed on the §2 lint defect; exit 0 after the `.at(0)` root-cause fix; tsgo ✓ oxlint ✓ biome ✓ lint:type-aware ✓ check:duplicates ✓) |
| 2 | `bun run scripts/health/sub-loop.ts test/workflows/helpers/journey-fixtures.smoke.test.ts --lifecycle duplicates` | **0** |
| 3 | `bun run scripts/health/sub-loop.ts test/workflows/helpers/index.ts --lifecycle duplicates` | **0** |
| 4 | `bun run scripts/health/sub-loop.ts test/workflows/parents/handshake-discovery.test.ts --lifecycle duplicates` | **DEFERRED to Task 2.4** — plan-review **F6** binding amendment (see §6) |

`test/workflows/AGENTS.md` is a markdown rules file: sub-loop is code-only (oxlint reports "No files found to lint" for `.md`, exit 1) — no QL pass is applicable, and none is needed: the file is untouched by this task (§2), so Protocol #2 (post-edit verification) does not apply.

## 4. 2.2.TE — Test Engineering evidence

| # | Command | Result |
|---|---|---|
| 1 | `bun run test/scripts/run-test.ts test/workflows/helpers/journey-fixtures.smoke.test.ts` | **exit 0 — 6 pass / 0 fail / 20 expect()** (harness GREEN through existing services: real registration of student+parent, governance flips, DEV1-014 link-emulation write, side-effect zero probes, tracked teardown with residue = 0 on all five tables). Run BEFORE and RE-RUN AFTER the §2 `.at(0)` fix (the smoke's governance-flip test exercises the fixed path) — identical green result both times |
| 2 | `bun run test/scripts/run-test.ts test/workflows/parents/handshake-discovery.test.ts` | **exit 1 — 0 pass / 1 fail / 1 error** — `error: Cannot find module '@/backend/services/students/student-handshake.service' from '…/handshake-discovery.test.ts'` — the EXPECTED test-first RED: the sole failure is the not-yet-existing service module; zero fixture/harness defects surfaced |
| 3 | `bun run test/scripts/run-test.ts test/workflows` (whole layer; the approved-runner equivalent of tasks.md's `bun test test/workflows` — a direct `bun test` invocation is blocked by the project's runner guard without `KOTTABY_TEST_RUNNER_OK=1`, which the runner injects) | **exit 1 — 6 pass / 1 fail / 1 error** — smoke file fully GREEN + journey file RED exactly and only on the missing service. Re-run post-fix: identical result (6 pass / 1 fail / 1 error, exit 1) |
| 4 | Post-run residue probe (independent, outside the suites): `psql app_db` → `users where email like '%@journey.test'` = **0**; `notifications` total = **0**; `audit_logs` total = **0** | clean |

Project type state (`bun run tsgo`, exit 1): the ONLY error attributable to Task 2.2 files is `test/workflows/parents/handshake-discovery.test.ts(43,41): error TS2307: Cannot find module '@/backend/services/students/student-handshake.service'` — precisely the F6-expected RED vehicle. (One additional error exists in root `tmp-collision-probe.test.ts` — that is the CONCURRENT Task 2.1 agent's in-flight WIP, not Task 2.2's surface; observed, untouched.)

**RED discipline note:** because the missing-module resolution fails at import time, the journey file's `beforeAll` never executes on RED runs — no fixture rows are created and no teardown is owed; the residue probe (row 4) confirms the DB stays clean.

## 5. 2.2.SEC / 2.2.SR / 2.2.IV audits

**SEC — Security & Tenancy**
- **No elevated permissions (real roles only):** every actor is created through the REAL `RegistrationService.registerUser` with real `role` values (`student`/`parent`); the smoke test asserts the committed `users.role` values read back from the DB. Zero role/permission monkey-patching: grep `spyOn|mock.module|jest|vitest` over `test/workflows/` code files → 0 hits (rule-text prose in AGENTS.md only).
- **Teardown provably removes all rows:** `teardown()` hard-deletes every tracked id in FK-safe order (students → parents → users, one committing transaction); `residueCounts()` re-looks-up EVERY tracked id across `users`/`students`/`parents`/`notifications`/`audit_logs` and the suites assert every count is 0 (tracked-id list asserted empty on lookup — smoke `afterAll` GREEN); independently corroborated by the external psql probe (§4 row 4).
- **No real notification channels reachable:** no notification-dispatch module exists in the tree today; registration emits zero notification/audit rows (code-grep of `registration.service.ts` shows only `logger.logDomainError` on collision/exhaustion paths, which the happy-path cast never hits) and the suites assert `countSideEffectRows() === {0, 0}` — REQ-023's asserted-absent posture. The journey header documents that any future step that emits notifications MUST spy the dispatch boundary (never real email/SMS/push) per AGENTS.md rule 5.
- **Fixture-write hygiene:** `linkStudentToParentFixture` / `setGovernanceFixture` are explicit field-mapped updates (partial patch objects built key-by-key) — never spreads of caller input; ids are function parameters from the tracked registry.

**SR — Semantic Review**
- `rg -n "runInRollback" test/workflows/` → hits ONLY in prose (AGENTS.md rule 1, fixtures header comment) — **zero usages** (grep gate clean).
- Fixture ids tracked in the `trackedActors` registry (registration order), never hardcoded; the nonexistent-code probe is DERIVED from the live code (`deriveAbsentHandshakeCode`) and grounded by a repo-miss assertion before use — no magic constants.
- Every journey step carries an actor-attribution comment (System / Student (Yusuf) / Parent (Fatima) / Second Parent (Karim) / Admin-domain fixture); steps 2b and 7 are documented cross-references to the GraphQL tier / DEV2-002 context boundary as mandated.
- Zero `console.*` (grep clean); zero `any` casts (grep clean); no hardcoded English expectation strings (all error-substring assertions come from `getServerTranslations("en").errorsTranslations`).
- No plan-artifact references (`ai/plans`) in any code file (grep clean).

**IV — Instruction Verification**
- `test/workflows/AGENTS.md` — all 10 rules validated: (1) no `runInRollback` ✓; (2) committed fixtures in `beforeAll` + tracked hard-delete in `afterAll` ✓; (3) per-run `jrn_<domain>_<8 hex>` UUID prefixes embedded in actor emails (smoke-asserted) ✓; (4) honest authorization — real users, real roles, no monkey-patching ✓; (5) external effects — none exist to intercept; observable side-effect contract asserted absent (zero notification/audit rows) ✓; (6) no `expect(...).rejects.toThrow()` — `catchJourneyError` try/catch helper + translated substrings ✓; (7) `bun:test` imports only, no console, no any ✓; (8) `@/` aliases only, zero relative-parent imports ✓; (9) own entities, never demo/seed rows — provisioning through the real registration service satisfies the rule's intent and is what tasks.md 2.2 itself mandates (`registerStudentActor()` via real `RegistrationService.registerUser`); justified per plan-review F1's binding resolution ✓; (10) one journey per workflow under `parents/`, helpers barrel is a pure `export *` with a single `./` segment ✓.
- **Architectural Invariant 10 (cross-actor journeys):** satisfied — sequential actor-attributed steps calling REAL services (`RegistrationService`, `StudentRepository`, the future `StudentHandshakeService`) against the REAL test DB (app_db — see §8), committed `beforeAll` fixtures, tracked-ID hard-delete `afterAll`, `runInRollback` forbidden, permissions resolving honestly through real role membership, side effects asserted absent.
- Canonical doc `docs/testing/workflow-journey-tests.md` read in FULL — the suite matches its layer contract (service layer only, no HTTP/GraphQL/Apollo machinery; the journey lives between service-unit and GraphQL-integration tiers).
- **Auto-discovered instruction files:** no `test/AGENTS.md` at the test root, no `test/helpers/AGENTS.md`, no `*.instructions.md` anywhere in the repo, no `.github/instructions/` directory (confirms 0.3 finding F9) — the only applicable instruction file for this layer is `test/workflows/AGENTS.md` itself.

## 6. Deviation from tasks.md (documented per plan-review F6 — binding)

**`2.2.QL` for `test/workflows/parents/handshake-discovery.test.ts` is DEFERRED to Task 2.4.** The journey file's static import of the not-yet-existing `StudentHandshakeService` is the intentional test-first RED vehicle; `tsgo` (sub-loop's first, short-circuiting check) fails on `TS2307` for exactly that import, so a per-file sub-loop exit 0 is impossible until the service lands. Per the plan-review gate's binding resolution: at 2.2, write the journey, evidence the RED, and run sub-loop only on harness files that reference existing modules; Task 2.4 (which creates the service and already re-runs the journey green) performs the journey file's sub-loop pass. **No placeholder production module was created to force green.**

Secondary artifact-level notes (not deviations, recorded for traceability): tasks.md's `2.2.QL` mention of `test/workflows/AGENTS.md` predates F1 (the file already existed — preserved byte-identical, nothing to gate; sub-loop is code-only anyway), and its `bun test test/workflows` spelling is executed via the approved runner because the project's runner guard blocks direct `bun test` invocations (the runner injects `KOTTABY_TEST_RUNNER_OK=1`).

## 7. Carry-forward for Task 2.4 — exact service surface the journey test expects

```ts
// backend/services/students/student-handshake.service.ts  (module does not exist yet — the RED surface)
// Export shape: `StudentHandshakeService` as a namespace (repo convention), imported from
// "@/backend/services/students/student-handshake.service".
export namespace StudentHandshakeService {
  // Step 2 — identity is ONLY the argument (resolver will pass ctx.user.id).
  // A user id with NO students row (e.g. a parent id) MUST throw
  // NotFoundError("STUDENT", <msg>) — assertable as `.code === "STUDENT_NOT_FOUND"` and
  // message containing getServerTranslations("en").errorsTranslations.studentHandshakeNotFound.
  export async function getMyHandshakeCode(studentUserId: number, locale: string): Promise<string>;

  // Steps 3-6 — called WITHOUT tx by the journey (tx optional-last, per Task 2.3's contract).
  export async function findStudentByHandshakeCode(
    code: string,
    locale: string,
    tx?: DBTransaction
  ): Promise<HandshakeCodeLookupReturnType | null>;
}
```

Behavioral contract the journey pins (all derived from the assertions in `handshake-discovery.test.ts` + 2.3's carry-forward):

1. `getMyHandshakeCode` returns the row's code VERBATIM; cross-fixture isolation (different student ids → different codes); non-student id → `NotFoundError` (see above).
2. `findStudentByHandshakeCode` normalizes (trim → uppercase) then validates (`HANDSHAKE_CODE_PATTERN`) BEFORE any DB read: `KSB-`, `KSB-TOOLLONG99`, `%KSB-ABCD1234`, `"   "`, `ksb-abcd123g` → `ValidationError` with `.code === "VALIDATION"` and message containing `errorsTranslations.handshakeCodeInvalid`.
3. Lowercase variant of a real code resolves IDENTICALLY (deep-equal) to the exact-code lookup.
4. Valid-format-but-missing code → `null` (no throw) — REQ-016.
5. Payload has EXACTLY the keys `{ maskedName, linkable }`; `linkable = parentId === null` (REQ-018); `parentId` never leaks (REQ-019); linking flips ONLY `linkable` (maskedName unchanged) — REQ-J2.
6. `isDeleted` / `isBlocked` / active suspension (`suspendedAt` 1h ago + `suspendedPeriodDays` 30) each collapse to `null`, deep-equal to the nonexistent-code result — REQ-J3/REQ-021.

Supporting integration facts (verified live by this task): repo methods `StudentRepository.findHandshakeCodeByStudentId(studentId, tx?)` / `findDiscoveryByHandshakeCode(code, tx?)` exist and return governance columns faithfully (service MUST collapse); `HandshakeCodeLookupReturnType` importable from `@/backend/types`; `maskFullName` from `@/shared/lib/mask-full-name`; error classes from `@/backend/lib/errors`.

## 8. Residual risks & environment notes

1. **Journey sub-loop debt:** `test/workflows/parents/handshake-discovery.test.ts` carries no sub-loop pass until 2.4 (§6) — tracked as the F6 deviation; 2.4 MUST run it.
2. **DB target (pre-existing, NOT touched):** `applyDbEnvOverride()` in `backend/lib/env.ts` force-loads `.env`'s `DATABASE_URL` over `.env.test` at module-eval, so journey/smoke tests actually connect to **`app_db`** (dev), not `app_db_test` (Task 2.3 §8.2 finding). The journey layer's committed-fixtures + tracked-teardown discipline keeps it residue-free (proven: §4 row 4 = 0 rows on every touched table). Out of ticket scope.
3. **Concurrent sibling noise:** Task 2.1 (lock tests) ran in parallel; its transient root artifact `tmp-collision-probe.test.ts` briefly adds a foreign tsgo error to project-wide runs. Task 2.2's own files contribute exactly ONE project tsgo error (the intentional missing-service TS2307).
4. **`linkable` mask stability:** the journey asserts `maskedName` is unchanged by the link fixture — guaranteed because `maskFullName(fullName)` depends only on `fullName`, which no fixture writes. If DEV1-014 ever renames during linking, re-check step 5.
5. **Suite-level failure mode:** if a journey step fails once the service lands, `afterAll` still tears down tracked rows (bun runs `afterAll` even on step failures), and the per-actor registry makes the suite re-runnable without residue (fresh UUID prefix per run).

## 9. Reuse record (pre-execution outcome knowledge read)

Read in FULL before any edit: worklog.md (setup, 0.1–0.3, 1.1–1.4, 2.3, 2.1-halt, orchestrator-git-protocol), tasks.md protocol + Task 2.2 (+ 2.1/2.3/2.4 context), specs.md §2.9 + REQ-010/015/016/017/018/019/020/021/023/077 + REQ-J1..J5 + traceability, plan.md §4.4 (journey design, visibility matrix, harness scaffold), and ALL outcome/*.md (phase0, 0.2, 0.3 — F1/F6 binding amendments reused in §2/§6, 1.1–1.4, 2.3 — repo signatures + env finding reused in §7/§8). Reused: RegistrationService/StudentRepository/error-classes/locale-keys verification from 0.2/0.3/2.3; the flat-errors + single-arg-getServerTranslations amendments from 1.4/0.3-F3/F4; the lint precedents (toSorted+localeCompare, no lint-disables) from 2.3. Added: the journey layer itself, its smoke proof, and this outcome.
