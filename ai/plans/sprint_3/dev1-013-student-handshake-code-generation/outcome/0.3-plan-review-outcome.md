# Phase 0 Outcome — Task 0.3: Phase-1.5 Plan-Review Gate

**Ticket:** DEV1-013 (Student Handshake Code Generation)
**Plan directory (actual):** `ai/plans/sprint_3/dev1-013-student-handshake-code-generation/`
**Reviewed:** `specs.md` (REQ-001..REQ-083 + REQ-J1..J5, 257 lines) + `plan.md` (D1–D13, 496 lines) + `tasks.md` (cross-checked for traceability), read IN FULL
**Method:** `.agents/skills/plan-review/SKILL.md` workflow (layer mapping → layer AGENTS.md reading → cross-cutting rule check) + internal-consistency, coverage, testability, scope-boundary, and codebase-reality checks
**Captured at:** 2026-08-29 02:48–02:50 UTC, HEAD `e39096f`, clean tracked tree (baseline per `outcome/phase0-baseline-outcome.md`)
**_Requirements:_ REQ-083** (this outcome PREDATES all implementation outcomes — no Phase 1+ task has run; tasks 0.1/0.2 are zero-source-modification Phase-0 baseline/guard tasks, and 0.2 completed concurrently during this review with `git status` re-verified unchanged in kind)

---

## 1. Verdict

# ✅ APPROVED-WITH-NOTES

**Rationale:** The plan is architecturally sound, internally coherent on all load-bearing design decisions (D1–D13 mutually reinforce; no contradictions between the GraphQL surface, permission matrix, error contract, and test tiers), and fully traceable (every REQ cluster maps to ≥1 task; every task carries a `_Requirements_` line; no orphaned requirement, no orphaned task). The verify-only posture for DEV1-002 is consistently enforced (D1, REQ-004/010, STOP RULE in 0.2/2.1). Zero scope creep was found against the stated non-goals.

**However**, 12 findings (1 HIGH, 5 MEDIUM, 6 LOW) must be treated as **binding amendment notes** by implementing subagents — mostly factual mismatches between the plan's literal API/citation spellings and the current codebase surface, plus one wrong factual premise (a referenced "non-existent" layer that already exists) and one unsatisfiable gate command. None require architectural replanning; all have unambiguous resolutions recorded in §3. Implementation may proceed to Phase 1 **only with these notes attached** (per task 0.3: review blockers resolve into plan amendments — this file IS that amendment record; no ledger entries required since all items are non-blocking corrections).

---

## 2. Findings (numbered; severity · location · description + resolution)

### F1 — HIGH · `tasks.md` 0.2 item 7, task 2.2 scaffold preamble, `plan.md` §4.4 — `test/workflows/` ALREADY EXISTS
- **Plan says:** "Confirm `test/workflows/` DOES NOT exist → task 2.2 must scaffold it" (0.2.7); "Scaffold (mandatory — the layer does not exist per 0.2): `test/workflows/AGENTS.md` — journey rules…" (2.2); "Journey harness (NEW, scaffolded per REQ-077 — `test/workflows/` does not exist)" (plan §4.4).
- **Reality:** `test/workflows/AGENTS.md` **already exists** (committed; canonical ref `docs/testing/workflow-journey-tests.md`; 10 hard rules incl. unique per-run UUID prefixes, honest authorization, no `expect().rejects.toThrow()`, `@/` alias imports, `bun:test`-only). What genuinely does NOT exist: `test/workflows/helpers/` and `test/workflows/parents/` — those still need scaffolding.
- **Risk:** 0.2's check fails as literally written (STOP-RULE ambiguity), or a 2.2 agent **overwrites binding journey rules** with the plan's own paraphrase, losing rules the plan never restated (UUID-prefix rule 3, entity-setup rule 9's helper list, `bun:test` rule 7, alias rule 8).
- **Resolution (binding):** 0.2 records "`test/workflows/AGENTS.md` EXISTS (rules layer); `helpers/` + `parents/` subdirectories ABSENT — scaffolding still required." **[Independently confirmed:** task 0.2 (ran concurrently) verified the same delta in `outcome/0.2-dependency-guard-outcome.md` §7 — dir exists with ONLY the AGENTS.md rules file; non-blocking; no ❌ entry; 2.2 must scaffold `helpers/` + domain subdir inside the existing structure. **]** Task 2.2 **preserves and extends** the existing AGENTS.md (add only what is missing, e.g. notification-spy guidance already present in rule 5) — NEVER overwrites it. Fixture-creation tension (plan wants `RegistrationService.registerUser`; existing rule 9 says entity-setup.ts helpers): real-service registration satisfies rule 9's intent ("create your own entities — never seed/demo rows"); 2.2 outcome must explicitly justify this reading. Rules 3 (UUID prefixes), 6, 7, 8 apply verbatim to the new journey suite.

### F2 — MEDIUM · `specs.md` REQ-083, `tasks.md` 6.4/7.3, `plan.md` §7 — deferred-items grep gate can NEVER return 0
- **Plan says:** `grep -c "❌\|⚠️" ai/plans/dev1-013-student-handshake-code-generation/deferred-items.md` MUST equal 0 (excluding D1–D3).
- **Reality (executed):** the count is **2 TODAY** on the perfectly clean ledger — the template's own "Status Values" section defines `⚠️ **Partial**` and `❌ **Blocked**` (lines 28–29 of `deferred-items.md`). The literal gate is unsatisfiable while the template header is intact. Additionally the command uses the INTERNAL plan path (see F7).
- **Resolution (binding):** scope the count to the Ledger Table section: `sed -n '/## Ledger Table/,/## Status Values/p' <actual-path>/deferred-items.md | grep -c "❌\|⚠️"` (= 0 today; expected 0 at 6.4/7.3). Intent (no Blocked/Partial **entries** beyond the D1–D3 forward notes) is clear and preserved.

### F3 — MEDIUM · `specs.md` REQ-002; `plan.md` §4.1 contract rules; `tasks.md` 2.4/4.2/4.3 — i18n API spellings do not match the shipped API (intent correct, literals wrong)
- **(a)** `getServerTranslations(locale, "errors")` (two-arg) does not exist. Actual: **single-arg** `getServerTranslations(locale)` returning the full `Translations` object; namespace access via suffixed property — `const t = getServerTranslations(locale).errorsTranslations;` (ground truth: `backend/services/teachers/applicant-lifecycle.service.ts:201,211,256`; `shared/locale/server-graphql.ts:3`).
- **(b)** `useAppTranslation(Translation.<Namespace>)` with "the `Translation` enum" — **no `Translation` enum exists anywhere**. Actual pattern: named namespace handles — `import { Auth, useAppTranslation } from "@/shared/locale"; const t = useAppTranslation(Auth);` (ground truth: `frontend/components/AuthFormShared.tsx:7,89`; handles defined via `defineNamespace` in `shared/locale/namespaces/**`). The new UI namespace needs its own handle (e.g. `HandshakeCode`) registered per `shared/AGENTS.md` "Namespace Registration" steps 1–5 (types interface; en/ar leaves; `message.ts` MessageSchema entry; `serverLegacy.ts` namespacePaths; `defineNamespace` with stable string id).
- **(c)** "Server components MUST use `await getTranslations(locale)`" — the API is **synchronous** (`app/AGENTS.md:110`: "synchronous — returns a `<namespace>` property-chain, NOT a promise"); zero `await getTranslations` occurrences in `app/`. Use `const t = getTranslations(locale).<ns>;` (plan §5.4's component tree already shows it without `await` — specs REQ-002 is the outlier).
- **Resolution (binding):** implement the INTENT (compile-time typed labels, enum/handle + property access `t.key`, never `t('key')`, never string namespaces, never `next-intl`/`getBackendTranslations`/`shared/messages`) using the actual API spellings above. REQ-002's literals are non-normative spellings.

### F4 — MEDIUM · `plan.md` §2.5(a); `tasks.md` 1.4 vs `specs.md` REQ-051 — errors-namespace shape: nested group vs FLAT convention
- **Plan says:** add `handshakeCode: { handshakeCodeInvalid: …; studentHandshakeNotFound: … }` as a NESTED group to the errors schema; task 2.4 itself hedges ("…`.errors.handshakeCode?` — per negotiated errors-shape").
- **Reality:** `ErrorsLabels` (`shared/locale/types/errors/index.ts`) is a **FLAT** interface; its header rule: "Keys are lowercase camelCase of the SCREAMING_SNAKE_CASE codes"; `shared/AGENTS.md` ("The `errors` namespace — canonical transport-message surface") documents the flat enumerated key list; the DEV2-004 precedent added **flat** keys (`applicantNotFound`, `applicantCooldownActive`). specs REQ-051 itself only names the two keys (flat-compatible).
- **Resolution (binding):** add the two keys FLAT to the errors triple (types + en + ar): `handshakeCodeInvalid`, `studentHandshakeNotFound`. Service usage: `getServerTranslations(locale).errorsTranslations.studentHandshakeNotFound` (exact `NotFoundError("STUDENT", t.studentHandshakeNotFound)` precedent at `applicant-lifecycle.service.ts:211`). Do NOT introduce the nested group. (Phase 7 may optionally update the "18-key list" sentence in `shared/AGENTS.md` errors section → 20 keys.)

### F5 — MEDIUM · `specs.md` REQ-031 (literal scope snippet) vs `plan.md` D8/§3.2 + `tasks.md` 3.1 — authScopes shape contradiction
- **Specs say:** `authScopes: { authenticated: true, role: [UserRole.Student] }` — the **un-wrapped** shape.
- **Plan says (correctly):** D8 mandates `{ $all: { authenticated: true, role: […] } }` because sibling scope keys combine with **ANY** semantics in `@pothos/plugin-scope-auth` (wrong-role callers could pass; anonymous maps to FORBIDDEN instead of UNAUTHORIZED). Verified against reality: the `$all` shape is live in `backend/graphql/query/teachers/applicant.query.ts:56-63` and the lesson is documented in `docs/teachers/applicant-lifecycle.md` §3 (lines 88–95).
- **Resolution (binding):** implement the **D8 `$all` shape** on both queries. Interpret REQ-031's snippet as shorthand for its semantic requirement (anonymous → `UNAUTHORIZED` 401; any wrong role incl. sibling roles → `FORBIDDEN` 403), which only the `$all` shape delivers.

### F6 — MEDIUM · `tasks.md` 2.2 (2.2.QL vs 2.2.TE) + Non-Negotiable Protocol #2 — test-first RED conflicts with the per-file sub-loop gate
- **Plan says:** 2.2.QL requires `sub-loop … --lifecycle duplicates` exit 0 on `test/workflows/parents/handshake-discovery.test.ts` AT task 2.2, while 2.2.TE expects the journey file RED because `StudentHandshakeService` does not exist yet ("the test file MUST compile"). A static import of a not-yet-existing module fails `tsgo` — which is sub-loop's FIRST check (`scripts/health/sub-loop.ts`: tsgo → oxlint → biome → lint → duplicates, short-circuits on failure).
- **Resolution (binding):** at 2.2 write the journey test (static import is fine), run it via `bun run test/scripts/run-test.ts` to evidence the RED state, and run sub-loop on the harness files that reference only EXISTING modules (`AGENTS.md`, `journey-fixtures.ts` — `RegistrationService`/repos exist). Defer the journey **test file's** sub-loop pass to task 2.4 (where the service lands and 2.4 already re-runs the journey green). Record this sequencing deviation explicitly in the 2.2 outcome. Do NOT create a placeholder production module to force green.

### F7 — LOW · all plan files — internal path vs ACTUAL path
- Every internal reference (headers, outcome paths, grep gates) says `ai/plans/dev1-013-student-handshake-code-generation/`; the ACTUAL path is **`ai/plans/sprint_3/dev1-013-student-handshake-code-generation/`**. All outcome writes, greps, and cross-references MUST use the ACTUAL path (a grep against the internal path errors out with "No such file"). Known since setup; recorded here as the standing amendment for every implementing subagent.

### F8 — LOW · `specs.md` REQ-071 (also REQ-001/076 command spellings) — wrong test-runner path / script invocations
- REQ-071 says `bun run scripts/run-test/run-test.ts <path>` — that file does **not** exist. The sanctioned runner (per tasks.md protocol item 3, `test/workflows/AGENTS.md`, and the repo) is **`bun run test/scripts/run-test.ts <path>`**. Likewise `bun tsgo` / `bun biome:check` should be run as the package scripts: `bun run tsgo`, `bun run biome:check` (as the 0.1 baseline already did). tasks.md's own spellings are correct; specs.md's are not.

### F9 — LOW · `tasks.md` 4.2.IV / 4.3.IV (and 1.1.IV/1.2.IV's "auto-discovered `*.instructions.md`") — referenced instruction files do not exist
- `frontend/views/AGENTS.md`, `frontend/components/ui/AGENTS.md`, `frontend.instructions.md`, `mobile-desktop.instructions.md`, and `.github/instructions/*.instructions.md` (sub-loop's discovery source) are all **absent** from the repo. (Note: `shared/AGENTS.md` itself contains a dangling reference to `frontend/views/AGENTS.md` — pre-existing repo doc issue, out of scope here.) IV steps degrade gracefully: read the layer parents that DO exist (`frontend/AGENTS.md`, `app/AGENTS.md`, `shared/AGENTS.md`, `shared/locale/AGENTS.md`, `frontend/graphql/AGENTS.md`, `frontend/graphql/sharedDocuments/AGENTS.md`) and record "no auto-discovered instruction files" in the outcome.

### F10 — LOW · citation nits (content verified; citations imprecise)
- (a) `specs.md` §3 Architectural Standards: "`docs/graphl api-gateway-and-routing.md`" — typo (`graphl`, missing `/`); correct: `docs/graphql/api-gateway-and-routing.md`.
- (b) `plan.md` §3.1 + `tasks.md` 3.1 cite "§8" of that doc — the doc has **unnumbered** sections (Why/Pattern/Rules/What NOT to Do/Related); the invoked contract (resolver module placement, side-effect barrel import through `<subdir>/index.ts`, no dynamic imports) is **Rules item 1** (~line 142). Content exists and matches the plan.
- (c) `docs/auth/user-registration.md` §2 describes the code as "8 uppercase **alphanumeric**" while the generator emits **hex** (`randomUUID().replace(/-/g,"").toUpperCase().slice(0,8)`); the plan/specs correctly say hex and the canonical regex `^KSB-[0-9A-F]{8}$` matches the CODE. No plan change needed; optionally note the wording during 7.2's cross-link pass.

### F11 — LOW · plan-review skill generic rule vs plan D7 — "documents must include `id`" superseded by the repo's embedded-types policy
- The skill's generic rule presumes normalizable entities. The repo's own `frontend/graphql/AGENTS.md` (lines 93–106) explicitly sanctions **id-less value types** that opt out of normalization via `keyFields: false` (existing precedents `AdminNoteInfo`, `OnlineMeetingInfo` in `frontend/providers/apollo/apolloCache.ts`). `HandshakeCodeLookup` is exactly such a value type (REQ-019/061/D7: no DB identity BY DESIGN). **Resolved by repo-specific precedent — NOT a violation.** Recorded so review wave 6.3 doesn't re-litigate it.

### F12 — LOW · pre-existing repo nits surfaced by the review (report-only; NOT plan defects)
- `shared/AGENTS.md` documents `useAppTranslation("auth").login` (string-arg form, `clientLegacy.ts`) while live code uses namespace handles — the AGENTS example is stale; the plan's handle-based intent matches the live pattern.
- `registration.service.ts`'s doc comment says "uppercase alphanumeric" (hex in reality) — cosmetic, DEV1-002-owned file, verify-only per D1 (do NOT touch).

---

## 3. Requirement-Coverage Spot-Check (sample)

| REQ ID | Covered by task(s) | Coverage assessment |
|---|---|---|
| REQ-001 (baseline + ledger) | 0.1 ✅ (done; outcome exists; D1–D3 seeded `📝 Forward`) | Complete |
| REQ-004 (dependency guard) | 0.2 | Complete — all 7 guard artifacts verified to EXIST (see §4); note F1 on item 7's inverted premise |
| REQ-010 (generation lock) | 2.1 | Complete — format lock N=50 vs `HANDSHAKE_CODE_PATTERN`; path verified real (`registration.service.ts:92-95`) |
| REQ-014 (student self-read) | 2.3, 2.4, 3.1, 3.3, 4.2 | Complete across repo→service→GraphQL→UI |
| REQ-016 (null not error) | 2.4, 3.3 | Complete; consistent with D4 and DEV2-004 null-precedence precedent (`myApplicantProfile`) |
| REQ-017 (mask) | 1.2, 2.4 | Complete — 4-tier test matrix incl. RTL/emoji/combining marks |
| REQ-020 (normalize→validate) | 1.1, 2.4 | Complete — ordering enforced by spy test (pre-DB rejection) |
| REQ-021 (governance collapse) | 2.4 (+ 2.2 step 6) | Complete — fail-closed predicate; three fixtures byte-identical to nonexistent |
| REQ-031 (role gates) | 3.1, 3.3 | Complete — subject to F5 ($all shape mandatory) |
| REQ-045 (zero schema drift) | 2.M, 5.2 | Complete — gate command verified meaningful (`git diff backend/db/schema/**` currently empty) |
| REQ-061 (embedded type, no id) | 3.1, 4.1 | Complete — `keyFields:false` precedent verified in `apolloCache.ts`; AGENTS embedded-list rule verified |
| REQ-077 + REQ-J1..J5 (journey) | 2.2 (green at 2.4/2.M) | Complete — subject to F1 (existing AGENTS.md) + F6 (sub-loop sequencing) |
| REQ-083 (gates) | 0.3 (this) + 6.4 + 7.3 | Complete — subject to F2 (grep scoping) + F7 (actual path) |

**Full-coverage assertion:** the tasks.md coverage table (lines 432–448) clusters every requirement family (REQ-001..083 + J1..J5); each cluster maps to ≥1 concrete task; no requirement is orphaned and no task lacks a `_Requirements_` line. Phase ordering (0→7) is coherent: tests precede service (2.2 RED → 2.4 GREEN), GraphQL precedes frontend, reviews (6.1–6.4) precede propagation (7.1–7.3), final gate (7.3) synthesizes.

---

## 4. Referenced-Artifact Existence Check (codebase reality)

**Docs (14/14 exist; content spot-verified):**
`docs/auth/user-registration.md` §2 "Handshake Generation" ✓ (KSB- generator, `createForRegistration`, in-transaction 23505 retry — matches plan claims); `docs/teachers/applicant-lifecycle.md` §3 "$all" lesson ✓ (lines 88–95); `docs/graphql/api-gateway-and-routing.md` ✓ (resolver placement rule present; "§8" citation imprecise — F10b); `docs/graphql/error-handling-contract.md` ✓; `docs/graphql/domain-error-extensions-code.md` ✓ (`VALIDATION`/`FORBIDDEN`/`UNAUTHORIZED`; `NotFoundError(entity)` → `` `${entity.toUpperCase()}_NOT_FOUND` `` confirms `STUDENT_NOT_FOUND` mapping); `docs/workflows/04-parent-supervision-handshake.md` §4.2 ✓ ("limited matching information … without exposing full student data" — quoted accurately by REQ-015); `docs/specs/state-machine-invariants.md` ✓ (INV-P1..P4, INV-U1/U4/U5 all present); `docs/specs/open-decisions-and-gaps.md` ✓ (A.2, A.3, A.7, B.6, B.12, B.13, B.14 all present); `docs/auth/jwt-authentication-service.md` §5 "RBAC Consumption Guide" ✓; `docs/testing/workflow-journey-tests.md` ✓; `docs/IDEMPOTENCY.md` ✓; `docs/DATABASE_MIGRATIONS.md` ✓; `docs/drizzle/prepared-statements.md` ✓; `docs/graphql/dataloader-batching.md` ✓.

**Code contracts (all verified):**
`students.handshake_code` varchar(50) NOT NULL + `unique("students_handshake_code_unique")` ✓; `students.parent_id` FK → users.id ON DELETE SET NULL ✓; shared-PK `students.id` → users.id ON DELETE CASCADE ✓; users governance (`is_deleted`, `is_blocked`, `suspended`, `suspended_at`, `suspended_period_days`) ✓; `generateHandshakeCode()` + `HANDSHAKE_RETRY_LIMIT = 5` + `isUniqueViolation` (23505) + SAVEPOINT-aware `withTransaction` ✓ (`backend/services/auth/registration.service.ts`); `StudentRepository.createForRegistration(userId, code, tx)` ✓; `UserRole` value enum (Admin/Teacher/Student/Parent) ✓; `NotFoundError`/`ValidationError` DomainError classes ✓; `logger.logDomainError` ✓ (`backend/lib/logger.ts:72`); `withPageAuth` ✓ (`frontend/lib/auth/withPageAuth.ts`); `apolloCache.ts` `typePolicies` with `AdminNoteInfo`/`OnlineMeetingInfo` `keyFields:false` precedent ✓; parent nav grouping ✓ (`frontend/views/dashboard/navItems.ts` `[UserRole.Parent]`); parent dashboard route ✓ (`app/(dashboard)/parent/dashboard/page.tsx` — `/parent/handshake` will be an additive sibling); student surface for the card ✓ (`app/(dashboard)/student/dashboard/page.tsx`); scripts `tsgo`/`biome:check`/`lint`/`generate:gqlSchema`/`codegen`/`test:ui:components` ✓; `shared/constants/index.ts` + `shared/lib/` ✓ (new files land in existing dirs).

**Test helpers (all exist):**
`backend/db/test/entity-setup.ts` (`createTestUser/Student/Parent/Applicant/Admin`) ✓; `runInRollback` + `expectRepoError` ✓ (`backend/db/test/test-utils.ts:34,77`); `setupTestServerLifecycle` ✓ (`test/helpers/test-lifecycle.ts:41`); `testClient` ✓ (`test/helpers/graphql-test-helpers.ts:8`); `CombinedGraphQLErrors` + `expectMutationError` ✓ (`test/helpers/graphql-test-helpers.ts`, `test/helpers/expect-mutation-error.ts:33`); `readTranslation`/`TestWrapper`/`translation-preload.ts` ✓ (`test/ui/components/`); `PermissionDeniedFallback` ✓ (`frontend/components/ui/PermissionDeniedFallback.tsx`); `runInRollback`-forbidden journey layer rules ✓ (`test/workflows/AGENTS.md` — EXISTS, see F1); `backend/db/test/AGENTS.md` rule 15 "Always Create Test Data — Never Query Seed Data" ✓.

**i18n surface (exists; spellings corrected per F3/F4):** `shared/locale/server-graphql.ts` (`getServerTranslations`, single-arg) ✓; `shared/locale/client/use-app-translation.ts` (`useAppTranslation(handle)`) ✓; namespace handles via `defineNamespace` ✓; errors triple (types/en/ar) ✓; `shared/AGENTS.md` Namespace Registration steps 1–5 ✓; `MessageSchema` term ✓ (= `types/message.ts` `Translations` map).

**Prototype:** 5 PNGs confirmed — `student-handshake-code-card.png`, `parent-link-child-success.png`, `parent-link-child-not-found.png`, `parent-link-child-invalid-format.png`, `parent-link-child-already-linked.png`. Filenames map 1:1 onto the outcome-state matrices of tasks 4.2/4.3 (card; found-linkable; null; invalid; already-linked). Per the plan-review skill's prototype-aware implementation rule they serve as **visual imagination aids** for the UI tasks (4.2/4.3) — visual-state assertions in 4.2.BS/4.3.BS must follow the PLAN's state matrix (§5.5) as the binding contract, with the screenshots as styling references.

---

## 5. Notes for Implementing Subagents (binding amendments summary)

1. **ALWAYS use the ACTUAL path** `ai/plans/sprint_3/dev1-013-student-handshake-code-generation/` for outcomes, greps, and cross-references (F7).
2. **0.2:** item 7's expected outcome is INVERTED — `test/workflows/AGENTS.md` exists; record that + the absence of `helpers/`/`parents/`; do NOT trigger the STOP RULE for this (the artifact exists; the plan's expectation of absence was wrong) (F1). **[Status: 0.2 already completed with this exact finding — see `outcome/0.2-dependency-guard-outcome.md` §7; no further action needed.]**
3. **2.2:** preserve/extend the existing `test/workflows/AGENTS.md`; apply its 10 rules (esp. UUID prefixes, honest roles, no `rejects.toThrow`, `@/` aliases, `bun:test`); defer the journey test file's sub-loop pass to 2.4; justify real-service fixture creation vs rule 9 in the outcome (F1, F6).
4. **i18n (1.4, 2.4, 4.2, 4.3):** single-arg `getServerTranslations(locale).errorsTranslations`; FLAT error keys `handshakeCodeInvalid`/`studentHandshakeNotFound`; namespace handles (`useAppTranslation(HandshakeCode)` style) — no `Translation` enum exists; sync `const t = getTranslations(locale).<ns>;` in server components (F3, F4).
5. **3.1:** `$all`-wrapped scopes ONLY (D8) — REQ-031's un-wrapped snippet is non-normative (F5).
6. **6.4/7.3:** scope the deferred-items emoji grep to the Ledger Table section and use the ACTUAL path (F2).
7. **All IV steps:** several referenced instruction/AGENTS files don't exist — fall back to the parent-layer AGENTS.md files that do exist and record the absence (F9).
8. **Test runner:** `bun run test/scripts/run-test.ts <path>` (NOT `scripts/run-test/run-test.ts`); quality scripts as `bun run tsgo` / `bun run biome:check` (F8).
9. **Do NOT touch** `registration.service.ts`, schema files, or DEV1-001/002/2-002-owned surfaces (verify-only; D1/REQ-004) — the cosmetic doc-comment nits in F10c/F12 are explicitly NOT a license to edit.
10. Baseline discipline: this gate predates all implementation outcomes; baseline is 0 errors / 0 findings / 0 lint problems (see `phase0-baseline-outcome.md`, incl. the `LINT_QUEUE_CONCURRENCY=1` cold-cache caveat).

---

**Outcome of the gate:** Phase 1 may open. No ledger entries required (no ❌/⚠️ items — all findings are non-blocking amendment notes owned by this document). REQ-083's plan-review-predates-implementation clause is satisfied by this file's timestamp.
