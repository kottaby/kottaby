# Task 3.2 Outcome — Codegen Sync (Same Change Set)

**Ticket:** DEV1-013 (Student Handshake Code Generation)
**Plan directory (actual):** `ai/plans/sprint_3/dev1-013-student-handshake-code-generation/`
**_Requirements:_ REQ-062** (with REQ-076 codegen-diff-discipline evidence and REQ-060's surface closure re-proven through the artifact)
**Git discipline:** ZERO git state operations (no checkout/switch/stash/restore/branch/commit/add). Read-only verification only — tree observed on `main` @ `78c0836` (== `feat/dev1-013-student-handshake-code-generation` per the orchestrator protocol; flips harmless). Per REQ-062's "SAME change set" clause the regenerated artifacts are staged in the working tree for the ORCHESTRATOR's commit (no subagent commits — binding instruction).

**Pre-execution knowledge read (protocol #1):** worklog.md in full (setup, 0.1–0.3, 1.1–1.4, 2.1×2, 2.2, 2.3, D4-fix, 2.4, 2.M, 3.1 — esp. 3.1 §4.3 SDL delta proof + §4.4 pre-existing reds + §7.2 non-changes + §9 carry-forward for 3.2), tasks.md protocol + Task 3.2 in full (incl. 3.2.QL/SR/IV), specs.md REQ-062/063/076, `frontend/graphql/AGENTS.md` (Code Generation + Embedded type normalization policy sections), `backend/graphql/AGENTS.md` (codegen-after-schema-change rule), ALL outcome/*.md (3.1's is the binding predecessor), `codegen.ts` (plugin/config shape), `scripts/generate-gql-schema.ts`, `scripts/health/sub-loop.ts`, `backend/graphql/test/schema-surface.test.ts` (all 12 cells), the five tool configs that govern generated-path exclusion (`tsconfig.json`, `oxlint.config.mts`, `biome.json`, `eslint.config.mjs`, `.jscpd.json`), and `package.json` script names.

---

## 1. Commands run (exact names + exit codes)

| # | Command (as prescribed by `frontend/graphql/AGENTS.md` §Code Generation + REQ-062) | Exit code | Notes |
|---|---|---|---|
| 1 | `bun run generate:gqlSchema` | **0** | `[generate:gqlSchema] wrote /home/z/my-project/frontend/graphql/generated/schema.graphql (3606 bytes)` — deterministic `printSchema(lexicographicSortSchema(graphQLSchema))` emission. |
| 2 | `bun codegen` | **0** | `graphql-codegen --config codegen.ts` → "Generate to ./frontend/graphql/generated/gql/graphql.ts"; 5 ✔ stages, no errors (`noSilentErrors: true` honored). Schema input = the SDL written by step 1. |
| 3 | `bun run tsgo` | **0** | Project-wide `tsgo -b --noEmit` — 0 errors. |
| 4 | `bun run biome:check` | **0** | "Checked 530 files in 4s. No fixes applied." — same 530-file baseline as 3.1; tree byte-stable across the run (git status unchanged before/after). |
| 5 | `bun run scripts/health/sub-loop.ts frontend/graphql/generated/gql/graphql.ts --lifecycle duplicates` | **1** (expected — see §4) | Deliberate probe run to EMPIRICALLY prove the generated path is outside the health tooling's scope: fails at step 2 (oxlint) with "No files found to lint… on 0 files" — the exclusion, not a defect. |
| 6 | `bun run test/scripts/run-test.ts backend/graphql/test/schema-surface.test.ts` | pre-codegen **1** → post-codegen **1** | 8/4 → 9/3 (the codegen-sync cell re-greens; the 3 remaining failures are the documented pre-existing freeze-baseline cells — §6). |

Codegen command-name verification (task asked to confirm the exact name): `package.json` scripts are `"generate:gqlSchema": "bun run scripts/generate-gql-schema.ts"` and `"codegen": "graphql-codegen --config codegen.ts"` — i.e. `bun run generate:gqlSchema` then `bun codegen` (no `run` on the second), exactly matching the docs' prescribed pair.

## 2. Generated-artifact diff audit (the REQ-062 closure)

### 2.1 `git status --porcelain` + `git diff --stat` after codegen (read-only)

```
 M backend/graphql/query/index.ts          ← 3.1's wiring (pre-existing working-tree delta)
 M frontend/graphql/generated/schema.graphql   ← THIS task's artifact delta
?? ai/plans/.../outcome/3.1-graphql-surface-outcome.md   (3.1's, uncommitted)
?? backend/graphql/pothos/students/  ?? backend/graphql/query/students/
?? backend/graphql/test/handshake-code.test... (3.1's new files)

 frontend/graphql/generated/schema.graphql | 7 +++++++
 1 file changed, 7 insertions(+)
```

**`frontend/graphql/generated/gql/graphql.ts`: ZERO diff** (empty `git diff`, byte-identical to HEAD). graphql-codegen v7 skip-writes identical content — the file's mtime (02:17, pre-session) is untouched, `git diff` empty, and `rg Handshake` in it finds nothing.

### 2.2 The full schema.graphql hunk (the ONLY artifact delta — 7 insertions, 0 deletions)

```diff
@@ -21,6 +21,11 @@ enum Gender {
   Other
 }

+type HandshakeCodeLookup {
+  linkable: Boolean!
+  maskedName: String!
+}
+
 type HealthCheck {
   service: String!
   status: String!
@@ -68,12 +73,14 @@ type Mutation {

 type Query {
   _health: HealthCheck!
+  findStudentByHandshakeCode(code: String!): HandshakeCodeLookup

   """
   Returns the authenticated user (or null for anonymous). Anonymous callers receive null — the AuthProvider uses this to detect expired access tokens and silently restore the session via `refreshToken`.
   """
   me: User
   myApplicantProfile: ApplicantProfile
+  myHandshakeCode: String!

   """
   Returns the canonical list of recitation readings (Qira'at) for the registration form selector. Public — no authentication required. ...
```

**REQ-062 verdict: the schema diff contains EXACTLY this ticket's additions** — the two root queries (`myHandshakeCode: String!`, `findStudentByHandshakeCode(code: String!): HandshakeCodeLookup`) + the `HandshakeCodeLookup` object (`linkable: Boolean!`, `maskedName: String!`, NO `id`). Byte-for-byte the 3.1 §4.3 fresh-print delta. Placement is `lexicographicSortSchema`-deterministic (`findStudentByHandshakeCode` < `me`; `myApplicantProfile` < `myHandshakeCode` < `recitationReadings`) — zero churn. Zero mutations, zero enums, zero modified operations, zero deletions.

### 2.3 Why `graphql.ts` is unchanged (expected, not a gap)

`codegen.ts` plugins are `typescript-operations` + `typed-document-node` — **only operation-derived types** are emitted, and NO document references the new queries yet (verified: `rg Handshake` in `frontend/graphql/sharedDocuments/` + `frontend/views/**.documents.ts` → zero hits; the documents are REQ-063 / Task 4.1's deliverable). The `HandshakeCodeLookup` OBJECT type therefore lands only in the SDL artifact now; the `MyHandshakeCodeQuery`/`FindStudentByHandshakeCodeQuery` typed documents + their extracted types will be emitted by 4.1's codegen pass once the documents exist. Schema-level object types are deliberately never generated ("Object types should never be used" — `frontend/graphql/AGENTS.md` §Type Safety), and generated types will be referenced only from `graphql.ts`/generated roots by 4.1's documents (`TypedDocumentNode<…Query, …QueryVariables>` imports from `@/frontend/graphql/generated/gql/graphql`).

## 3. Drift investigation (task item 2)

**Result: ZERO unrelated drift in the generated diff.**

- The upstream teacher-applicant surface (`myApplicantProfile`, `ApplicantProfile`, `ApplicantStatus`) is ALREADY COMMITTED in the HEAD SDL — proven read-only: `git show HEAD:frontend/graphql/generated/schema.graphql` contains `type ApplicantProfile`, `enum ApplicantStatus`, `myApplicantProfile: ApplicantProfile` (upstream `06f898c` codegen-synced its own artifacts; last artifact commit per `git log -- frontend/graphql/generated/`). Hence my diff carries ONLY this ticket's names — exactly as 3.1 §4.3 predicted ("the committed SDL already contains the upstream applicant surface… 3.2's committed diff will contain ONLY this ticket's additions").
- `graphql.ts` byte-identical (§2.1) — no foreign typed-document drift either.
- No other generated artifact exists (`frontend/graphql/generated/` holds exactly `schema.graphql` + `gql/graphql.ts`; `codegen.ts` `generates` maps only the latter; `generate-gql-schema.ts` writes only the former — both verified in source).

## 4. 3.2.QL — Quality Loop: SKIPPED-by-design on generated artifacts (justification + empirical proof)

**Config-level justification — ALL FIVE sub-loop tools exclude `frontend/graphql/generated` by configuration:**

| Tool (sub-loop step) | Exclusion citation |
|---|---|
| tsgo (step 1) | `tsconfig.json` `exclude` → `"frontend/graphql/generated"` (line 67) — the generated file is not a program root (it IS still transitively type-checked through importer files, which are roots). |
| oxlint (step 2) | `oxlint.config.mts` `ignorePatterns` → `"frontend/graphql/generated/**"` (line 134). |
| biome:check (step 3) | `biome.json` `files.includes` → `"!frontend/graphql/generated"` (line 13). |
| lint:type-aware / eslint (step 4) | `eslint.config.mjs` `globalIgnores` → `"frontend/graphql/generated/**"` (line 59; + the local-rules block's `ignores`, line 327). |
| check:duplicates / jscpd (step 5) | `.jscpd.json` `ignore` → `"frontend/graphql/generated/**"` (line 37) — and `sub-loop.ts:shouldSkipJscpd` auto-skips any path matched there. |

**Empirical proof (the probe run, command #5):** `sub-loop --lifecycle duplicates` on `frontend/graphql/generated/gql/graphql.ts` → step 1 tsgo passed trivially (no diagnostics attributable — the file is excluded from program roots), then step 2 **oxlint FAILED with `No files found to lint. Please check your paths and ignore patterns. Finished … on 0 files`** → sub-loop exit 1 at the first file-level tool. The health tooling structurally CANNOT process generated paths; the skip is recorded per the task instruction ("If the health tooling excludes generated paths, skip WITH justification and record the decision").

**Compensating controls actually run instead (the meaningful quality surface for a generated artifact):**
1. `bun run tsgo` project-wide → **exit 0, 0 errors** — the generated `graphql.ts` remains transitively type-checked through every importing document/view (unchanged content anyway), and the whole change set (3.1's four files + SDL) type-checks clean.
2. **Codegen determinism is enforced by a test, not lint**: the schema-surface `Codegen sync` cell proves the committed SDL is BYTE-IDENTICAL to a fresh `printSchema(lexicographicSortSchema(...))` emission — re-greened by this task (§5/§6).
3. `bun run biome:check` project-wide → exit 0, 530 files, no fixes (generated dir excluded by design; everything else stays at the 2.M/3.1 baseline).

## 5. 3.2.TE — Test evidence (the codegen-sync cell + battery)

### 5.1 Target suite: `backend/graphql/test/schema-surface.test.ts` (12 cells)

| Run | Result | Failing cells |
|---|---|---|
| **Pre-codegen** (captured BEFORE running codegen — the designed 3.1→3.2 transient state) | 8 pass / **4 fail** (exit 1) | ① "root query retains EXACTLY the baseline fields plus the probe" ② "ZERO new enums" ③ "whole-schema named-type delta is EXACTLY one new type: HealthCheck" ④ **"Codegen sync — committed SDL is byte-identical" (the expected-transient red from 3.1 — committed SDL lacked the handshake additions; the runner's diff showed the 3 additions missing)** |
| **Post-codegen** (after commands #1+#2) | **9 pass / 3 fail** (exit 1) | ①②③ only — see §6 |

**RE-GREENED BY THIS TASK: the "Codegen sync — committed SDL is byte-identical to the built schema" cell** (schema-surface.test.ts:224-233 — `expect(committedSdl).toBe(freshlyPrinted)` + the `_health: HealthCheck!` / `type HealthCheck {` containment belt-and-braces). The 3.1→3.2 pipeline state documented in 3.1 §4.4 is now closed: generated artifacts are in lockstep with the code-first builder.

### 5.2 Regression battery (all via the mandated runner; exit codes)

| Suite | Result |
|---|---|
| `backend/graphql/test/handshake-code-surface.test.ts` (3.1's 22-test gate over the built schema) | exit 0 — 22/22 |
| `frontend/graphql/test/gateway/allowlist-coverage.test.ts` (default-deny gate over schema introspection) | exit 0 — 8/8, 42 expect |
| `frontend/graphql/test/teachers/applicant-profile.test.ts` (LIVE WIRE over the regenerated SDL — boots next dev :3066, real HTTP, typed documents) | exit 0 — 8/8, 30 expect, 7.67s (after clearing a PRE-EXISTING stale server — §8) |
| `backend/graphql/test/error-contract-matrix.test.ts` (wire tier) | exit 1 — **35 pass / 1 fail = 3.1's documented pre-existing state exactly** (the `_health` legacy String! cell; §6.2). One intermediate run flaked 32/4 with three 60s wire timeouts — wedged-dev-server environment, not code: clean-port re-run = 3.37s, 35/1 (§8). |
| `backend/graphql/test/context-keys.test.ts` | exit 0 — 14/14 |
| `backend/graphql/test/request-id.test.ts` | exit 0 — 12/12 |
| `backend/graphql/test/error-finalizer.test.ts` | exit 0 — 14/14 |
| `backend/lib/gateway/public-operations.test.ts` | exit 0 — 26/26 |
| `backend/lib/gateway/static-assertions.test.ts` (A1–A5) | exit 0 — 17/17 |
| `bun run tsgo` | exit 0 — 0 errors |
| `bun run biome:check` | exit 0 — 530 files, no fixes |

## 6. Remaining reds — pre-existing, NOT this task's regressions (do NOT absorb)

### 6.1 `schema-surface.test.ts` — the ONLY remaining reds are the 3 pre-existing freeze-baseline cells (foreign upstream applicant drift)

All three failed at HEAD `78c0836` BEFORE 3.1's edits (3.1 §4.4 evidence) because upstream `06f898c` (DEV2-004 teacher-applicant) added its surface without refreshing the frozen `PRE_3_1_*` baselines captured at `8e5ebb8`. Exact post-codegen failure payloads (runner transcripts):

```text
# Cell ① (schema-surface.test.ts:101) — expected ["_health"], received:
  [ "_health", + "findStudentByHandshakeCode", + "myApplicantProfile", + "myHandshakeCode" ]
# Cell ② (schema-surface.test.ts:175) — frozen enum set, received extras:
  [ + "ApplicantStatus", "Gender", "RecitationReading", "RegisterPublicRole", "UserRole" ]
# Cell ③ (schema-surface.test.ts:185) — expected ["HealthCheck"], received:
  [ + "ApplicantProfile", + "ApplicantStatus", + "HandshakeCodeLookup", "HealthCheck" ]
```

Attribution: `myApplicantProfile` / `ApplicantProfile` / `ApplicantStatus` = foreign upstream drift (DEV2-004); `myHandshakeCode` / `findStudentByHandshakeCode` / `HandshakeCodeLookup` = this ticket's REQ-060/062-sanctioned additions. **This ticket contributes ZERO of the enum drift** (cell ② is 100% foreign). Per 3.1 §7.2 the freeze-baseline refresh is an ORCHESTRATOR/plan-owner decision — a refresh must add the THREE foreign names PLUS this ticket's three, deliberately and documented, never silently. NOT refreshed here.

### 6.2 `error-contract-matrix.test.ts` — 35/36, the `_health` wire cell (upstream retyping debt)

Identical to 3.1 §4.4.2: the wire-tier cell expects the LEGACY `{ _health: "ok" }` String! shape while `_health` was retyped to `HealthCheck!` upstream. My change cannot influence `_health`; the failing expectation is foreign, pre-existing. My codegen cannot fix it and did not worsen it (35/1 both before-at-3.1 and now).

## 7. 3.2.SR — Semantic Review + 3.2.IV — Instruction Verification

**SR (diff audit):** the complete committed-diff surface for this task is §2.2's two hunks (+7/−0) — additions only, no deletions, no reordering churn (sorted-schema determinism), no foreign names, no mutation/enum movement (frozen sets re-proven by cells ② and "ZERO new mutations" staying green apart from the foreign enum). The typed-document artifact is byte-stable (§2.1/§2.3). Generated types are referenced from nowhere yet — by design, 4.1's documents will import them from `@/frontend/graphql/generated/gql/graphql` (generated roots only); no hand-authored code was touched by this task (zero non-generated file edits — `git diff --stat` §2.1 shows only 3.1's pre-existing wiring line + the SDL).

**IV (artifact placement):**
- `frontend/graphql/AGENTS.md` §Code Generation — COMPLIANT: ran EXACTLY the prescribed pair (`bun run generate:gqlSchema` then `bun codegen`) after a backend schema change; generated types live in `frontend/graphql/generated/gql/` (single `graphql.ts`); nothing moved.
- `frontend/graphql/AGENTS.md` §Embedded type normalization policy — NOT yet applicable to this task: `HandshakeCodeLookup` has no `id`, and the policy's `apolloCache.ts` `keyFields: false` registration + AGENTS.md list entry are **Task 4.1's deliverable** (D7's second half, per 3.1 §1.1/§9 — frontend provider code is out of 3.2's scope). Carry-forward §10.
- `backend/graphql/AGENTS.md` — COMPLIANT: the codegen-after-schema-change rule (stated in the Pothos enum-registration section and docs §8 rule 5) satisfied — schema artifacts regenerated in the SAME change set as 3.1's Pothos surface (REQ-062), staged for the orchestrator's single commit.
- `scripts/generate-gql-schema.ts` + `codegen.ts` — the two sanctioned artifacts are exactly the two paths written; no stray outputs (`git status` §2.1 proves it).

## 8. Operational finding (environmental, pre-existing — recurred)

The stale/wedged `next dev` (:3066) hazard 3.1 documented RECURRED, in two forms:
1. A LEFTOVER dev server (PIDs 29880/29893, started BEFORE this session — a prior agent's leak) blocked the applicant-profile live-wire suite: first attempt timed out (300s), re-attempt hit `⨯ Another next dev server is already running` (Next's PID-file detection) → "Dev server failed to respond within 180s". `kill <pid>` (SIGTERM) was insufficient — the next-server ignored it while burning 45% CPU; required `kill -9` on BOTH the `next dev` wrapper AND `next-server`. Suite then passed 8/8 in 7.67s.
2. The FIRST error-contract-matrix run booted its own server that WEDGED at 99% CPU (never responded) → three wire cells timed out at exactly 60s each (32/4). After `kill -9` + free port, the clean re-run completed in **3.37s at 35/1** — proving the timeout cells were environmental, not regressions. That run also leaked its server (torn down only after manual kill); final state verified clean (`ss -tlnp` no 3066/3099 listeners, no next processes).
3. `bun run test:ui:kill` still targets port 3099 in this tree (not 3066) — tooling drift already on 3.1's record.

**Carry-forward for 3.3 (binding): before ANY live-wire suite, check BOTH `ss -tlnp | rg 3066` AND `ps aux | rg next-server`** — a WEDGED non-responding server is as blocking as a listening one — and `kill -9` both PIDs of any leftover pair.

## 9. Deviations & decisions versus the task text

1. **No commit** — REQ-062 says "commit generated artifacts in the SAME change set"; the binding instruction + repo protocol reserve commits for the orchestrator (the batch commit after 3.x). The regenerated `schema.graphql` sits staged in the working tree next to 3.1's files = the same change set, committed once by the orchestrator.
2. **3.2.QL sub-loop skipped on generated artifacts** — justified + empirically proven (§4), per the task's own skip-with-justification provision. `bun run tsgo` (0 errors) + the codegen-sync test cell + project biome:check serve as the compensating controls.
3. **Probe sub-loop run recorded as exit 1** — deliberate (the exclusion proof); NOT counted as a quality failure of the artifacts.
4. **`graphql.ts` zero-diff documented as expected** — operation-derived-only codegen; the task text's "typed-document diff" clause resolves to "no typed-document delta until REQ-063 documents exist" (§2.3).
5. **Freeze-baseline cells NOT refreshed** — orchestrator decision per 3.1 §7.2; this task re-greens ONLY the codegen-sync cell it owns (§5.1/§6.1).

## 10. Carry-forward

**For the orchestrator:** (a) commit batch must include `frontend/graphql/generated/schema.graphql` (+7) TOGETHER with 3.1's five files — REQ-062 same-change-set; (b) the schema-surface freeze-baseline refresh decision (3 foreign + 3 sanctioned names, §6.1) and the error-contract `_health` legacy cell (§6.2) remain open upstream-debt escalations.

**For 4.1 (frontend):** after authoring `frontend/graphql/sharedDocuments/students/handshake-code.documents.ts` (REQ-063), re-run `bun run generate:gqlSchema && bun codegen` — THEN `graphql.ts` will gain `MyHandshakeCodeQuery`/`FindStudentByHandshakeCodeQuery` (+Variables) and the extracted field types; documents import them from `@/frontend/graphql/generated/gql/graphql` only. Also D7's second half: `HandshakeCodeLookup: { keyFields: false }` in `frontend/providers/apollo/apolloCache.ts` typePolicies + the embedded-types list entry in `frontend/graphql/AGENTS.md` (§7 IV note).

**For 3.3:** the live-wire matrix must re-check the schema-surface codegen-sync posture implicitly (its fixtures run over the built schema, not the SDL); the stale/wedged-server pre-flight protocol (§8) is binding before any boot.

## 11. Files created/modified by this task

- MODIFIED: `frontend/graphql/generated/schema.graphql` (+7/−0 — EXACTLY the three sanctioned additions; `generate:gqlSchema` output)
- REGENERATED-BYTE-IDENTICAL: `frontend/graphql/generated/gql/graphql.ts` (codegen ran, skip-wrote identical content — zero diff)
- CREATED: `ai/plans/sprint_3/dev1-013-student-handshake-code-generation/outcome/3.2-codegen-sync-outcome.md` (this file)
- APPENDED: Task 3.2 entry to `worklog.md`
- NOT touched: `tasks.md`, any hand-authored source file, `codegen.ts`, any test file, any schema/migration/service/repository file, 3.1's deliverables (left as-is for the orchestrator's batch)

**Verdict: ✅ Task 3.2 COMPLETE** — `generate:gqlSchema` exit 0 + `codegen` exit 0 · artifact delta = EXACTLY the two queries + `HandshakeCodeLookup` (+7/−0 in SDL, typed-docs byte-identical — zero foreign drift, applicant surface proven pre-committed at HEAD) · codegen-sync cell RE-GREENED (8/4 → 9/3; remaining 3 = documented pre-existing freeze-baseline foreign reds, not absorbed) · error-contract-matrix confirmed at its documented 35/1 pre-existing state (flake run attributed to the wedged-server environment, clean re-run 3.37s) · tsgo 0 errors · biome 0 findings (530 files) · QL skip on generated paths justified with 5 config citations + empirical sub-loop probe · 9-suite regression battery green · stale-:3066-server hazard recurred + documented with a stronger pre-flight protocol for 3.3.
