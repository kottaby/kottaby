# Task 1.1 Outcome — Shared Constants: `shared/constants/handshake-code.constants.ts`

**Ticket:** DEV1-013 (Student Handshake Code Generation)
**Plan directory (actual):** `ai/plans/sprint_3/dev1-013-student-handshake-code-generation/`
**_Requirements:_ REQ-011, REQ-020, REQ-002** · **Mode:** additive (new shared module + barrel line + co-located unit spec)
**HEAD context:** working tree on `main` @ `e39096f` (identical commit to `feat/dev1-013-student-handshake-code-generation`); this task made ZERO git ref/branch changes — orchestrator switches to the feature branch before committing (standing note from 0.1/0.2/0.3)

**Pre-execution knowledge read (per protocol #1):** worklog.md (setup + 0.1/0.2/0.3), tasks.md §Phase 1 + Task 1.1 in full (incl. 1.1.QL/TE/SEC/SR/IV), specs.md REQ-002/REQ-011/REQ-020 (+ REQ-010/035/073 context), plan.md §2.3 (D2), outcome/phase0-baseline-outcome.md, outcome/0.2-dependency-guard-outcome.md (§3 hex-vs-alphanumeric nuance, verify-only contract), outcome/0.3-plan-review-outcome.md (F7 path amendment, F9 instruction-file absence, F8 runner path), shared/AGENTS.md in full.

---

## 1. Files created/modified

| File | Action | Content |
|---|---|---|
| `shared/constants/handshake-code.constants.ts` | CREATED | Exactly 4 exports per plan §2.3: `HANDSHAKE_CODE_PREFIX = "KSB-"`, `HANDSHAKE_CODE_PATTERN = /^KSB-[0-9A-F]{8}$/`, `isHandshakeCode(value: unknown): value is string` (typeof + pattern type guard), `normalizeHandshakeCode(value: string): string` (`value.trim().toUpperCase()`). **Zero imports** (verified: the only "import" token in the file is prose inside the header doc comment). Domain-only JSDoc; zero plan-artifact references in comments. |
| `shared/constants/index.ts` | MODIFIED (+1 line) | `export * from "./handshake-code.constants";` added as line 1 (alphabetical order ahead of `./recitation-reading.enum`; barrel-relative `./` re-export is the sanctioned ESLint exception per shared/AGENTS.md "Import Convention"). |
| `shared/constants/handshake-code.constants.test.ts` | CREATED | 4-tier unit spec (59 tests) following the committed shared-layer test precedent (`shared/locale/*.parity.test.ts`): spec co-located beside source, `bun:test` imports, `@/shared/...` deep imports, "WHAT THIS LOCKS" header, section dividers, `test.each` tables. |

Convention discovery (1.1.TE placement): the only committed shared-layer unit tests live **beside their source** as `shared/locale/*.parity.test.ts`; no `test/`-side shared pure-function precedent exists; `bunfig.toml` has a single `[test]` section with **no root/path restriction** (only preloads), so `bun test` picks up `shared/constants/*.test.ts` → co-located placement confirmed. `backend/db/test/AGENTS.md` is DB-test-layer rules (rollback/fixtures) — not applicable to a pure unit spec.

## 2. Verification evidence (commands + exit codes)

| Gate | Command | Result |
|---|---|---|
| 1.1.TE test run | `bun run test/scripts/run-test.ts shared/constants/handshake-code.constants.test.ts` | **exit 0** — **59 pass / 0 fail**, 358 expect() calls, 61–68 ms (ran twice; log at `logs/2026-08-29T02-58-12/shared/constants/handshake-code.constants.test.ts.log`) |
| 1.1.QL quality loop (constants) | `bun run scripts/health/sub-loop.ts shared/constants/handshake-code.constants.ts --lifecycle duplicates` | **exit 0** — tsgo ✅, oxlint ✅, biome:check ✅ (no fixes applied), lint:type-aware ✅, check:duplicates ✅ |
| 1.1.QL quality loop (barrel) | `bun run scripts/health/sub-loop.ts shared/constants/index.ts --lifecycle duplicates` | **exit 0** — all five checks ✅ |
| Protocol #2 (test file) | `bun run scripts/health/sub-loop.ts shared/constants/handshake-code.constants.test.ts --lifecycle duplicates` | **exit 0** — tsgo/oxlint/biome/lint ✅; check:duplicates skipped-by-design for `*.test.ts` (sub-loop `shouldSkipJscpd`) |
| Project-wide typecheck vs baseline | `bun run tsgo` | **exit 0**, `grep -c "error TS"` = **0** (baseline 0.1 = 0 → zero new errors) |
| Worktree integrity | `git status --porcelain` | Only: `M shared/constants/index.ts`, `?? shared/constants/handshake-code.constants.ts`, `?? shared/constants/handshake-code.constants.test.ts` (+ pre-existing Phase-0 plan-artifact changes: `M deferred-items.md`, `M tasks.md`, `?? outcome/`). Zero DEV1-001/002-owned files touched. |

## 3. Test matrix (1.1.TE) — 59 tests across 6 describes

- **Canonical constants contract (3):** prefix pinned to exactly `"KSB-"`; pattern pinned to anchored/flagless shape `^KSB-[0-9A-F]{8}$` (drift-proof: any loosening fails here first); prefix-constant + 8-hex composition accepted by the pattern (ties the two constants together).
- **Tier 1 — branch coverage (21):** accept branch (6 valid codes spanning `0-F`); typeof branch via 13 non-string kinds (null, undefined, number, NaN, bigint, boolean, object-carrying-valid-code, empty object, array-wrapping-valid-code, empty array, function-returning-valid-code, symbol, Date); string-miss branch (empty string + near-miss).
- **Tier 2 — boundaries (17):** hex-tail lengths 7/9/0 rejected, exactly-8 accepted across alphabet span; prefix variants (no-dash `KSBABCD1234`, bare `KSB-`, missing prefix, wrong prefix letter `XSB-`, trailing dash, lowercase hex raw-rejected); leading/trailing whitespace (space/tab/CRLF/NBSP) fails RAW but passes POST-normalization; INTERNAL whitespace stays rejected post-normalization.
- **Tier 3 — hostile fuzz, fail closed raw AND post-normalization (23):** LIKE wildcards `%` (prefix/suffix/inside) and `_` (replacing dash / inside hex / leading); backslash (prefix/inside/suffix); SQL comment marker `--`; RTL bidi injections (`\u202E` override, `\u2066…\u2069` embedding); Arabic unicode/RTL payloads; emoji (single + ZWJ family); NUL bytes (prefix/suffix/inside — written `\u0000` to avoid legacy-octal escapes in strict mode); zero-width joiner/space inside the hex tail; multi-KB payloads (5000-char tail, 10 000 `%`, 1000× code repeat).
- **Tier 4 — normalization algebra (5):** idempotence `normalize(normalize(x)) === normalize(x)` across the full ~36-entry corpus AND a seeded (deterministic LCG, reproducible) 200-case fuzz sweep over a hostile alphabet; purity/determinism (same input → same output, no hidden state); **lowercase-of-valid normalizes INTO acceptance** (`ksb-abcd1234`, `Ksb-AbCd1234`, padded variants → raw reject, normalized accept, result === `input.trim().toUpperCase()`); near-miss variants stay rejected post-normalization (no smuggling).
- **Barrel pinning (1):** `@/shared/constants` re-exports all four symbols **by identity** (`===` against the deep imports) — guards deliverable #2.

## 4. 1.1.SEC — Security & Tenancy Audit

- **ReDoS-safe by construction (and pinned by test):** the pattern is `^KSB-[0-9A-F]{8}$` — both-ends anchored, single character class, one **fixed bounded** quantifier `{8}`, no alternation, no nesting, no unbounded repetition → linear-time `test()` on any input; multi-KB payloads assert fail-closed in the suite. The canonical-shape pin test (`source === "^KSB-[0-9A-F]{8}$"`, `flags === ""`) makes any future loosening (e.g. `+`, `*`, `?`, groups) a compile-visible test failure, so the ReDoS posture cannot silently regress.
- **No normalization smuggling:** `normalizeHandshakeCode` is total and pure (`String.prototype.trim()` + `toUpperCase()` only — no locale-sensitive `toLocaleUpperCase`, no substrings, no replacements, no concatenation), so exactly one string enters and one string exits; Tier 3 proves every hostile payload is rejected on BOTH the raw and the normalized value (i.e. normalization cannot launder `%`/`_`/`\`/bidi/NUL/zero-width content into acceptance), and Tier 4 proves idempotence (no second distinct string can emerge on re-normalization). The intended consumption order — validate strictly AFTER normalization — is documented on both functions and exercised by the whitespace/case-fold tests.

## 5. 1.1.SR — Semantic Review

- **Single source of the pattern:** repo-wide grep (excluding `ai/**` plan docs) for `^KSB-` regexes → the ONLY canonical-pattern occurrence is `shared/constants/handshake-code.constants.ts:29` (plus its own spec's drift-pin, which is the lock itself). Remaining live occurrences are pre-existing, DEV1-002/001-owned, and NOT validation-pattern duplication: `backend/db/test/logic/auth/registration.service.test.ts:204,353,454` pins the BROADER superset `/^KSB-[A-Z0-9]{8}$/` (test assertion locks; hex ⊂ alnum ⇒ no false rejections; verify-only per D1 — untouched), `backend/services/auth/registration.service.ts:92-95` generates via template literal `KSB-${hex.slice(0,8)}` (**verified: the generator has NO validation regex at all** — not modified, per the verify-only contract; note: the task brief cited `backend/services/users/registration.service.ts`, actual path is `backend/services/auth/registration.service.ts`, consistent with 0.2 §3), `backend/db/test/entity-setup.ts:84` builds fixture codes (template literal), `docs/auth/user-registration.md:37,45` documents the format. **Zero hits in new code outside this file.**
- **Zero consumers yet:** `HANDSHAKE_CODE_*` / `isHandshakeCode` / `normalizeHandshakeCode` referenced only by the new module, its spec, and the barrel — Phase 2 (service) / Phase 3 (GraphQL) / Phase 4 (frontend skip-gate) will consume them; no premature coupling introduced.
- **Purity:** module imports NOTHING (no `@/backend|frontend|app` — the ESLint `no-restricted-imports` shared ban at `eslint.config.mjs:262-276` holds trivially), no I/O, no clock/env, no module-level mutable state, no side effects; both functions are pure/deterministic; no unused exports (all four are the sanctioned contract surface; the barrel's `export *` re-export is consumed by the barrel-pinning test).

## 6. 1.1.IV — Instruction Verification

- **`shared/AGENTS.md` compliance:** `shared/constants/` is the sanctioned home for "stable domain constants" (File Organization table); type-guard pattern matches the committed precedent (`isRecitationReading` in `recitation-reading.enum.ts`); deep-import preference respected by consumers (test uses `@/shared/constants/handshake-code.constants`); barrel-relative `./` re-export allowed by the explicit index.ts exception; no `@/backend|frontend|app` imports; no `oxlint-disable` comments; no i18n/locale surface touched (out of 1.4 scope).
- **Auto-discovered `*.instructions.md` for `shared/constants/`:** NONE exist — sub-loop's discovery itself reported "No applicable instruction files found for this file" for both the constants file and the barrel (and for the test file: `.github/instructions/tests.instructions.md` is flagged ⚠ absent, matching plan-review F9). Repo-wide glob for `**/*.instructions.md` → zero files. Only root `AGENTS.md` + `shared/AGENTS.md` apply; both satisfied.

## 7. Decisions & mismatches vs plan

1. **Barrel line order:** plan §2.3 says "gains exactly one line"; placed FIRST (alphabetical before `./recitation-reading.enum`) — biome organize-imports clean. No semantic delta.
2. **Test placement:** plan/tasks did not prescribe a location; resolved via convention discovery (§1) → `shared/constants/handshake-code.constants.test.ts` co-located beside source, exactly as the task brief's fallback prescribed. No mismatch.
3. **Brief path nit:** task brief referenced the DEV1-002 generator at `backend/services/users/registration.service.ts`; the real file is `backend/services/auth/registration.service.ts` (verified unmodified — §5). Recorded so later tasks grep the right path.
4. **NUL-byte literals in tests** written as `\u0000` (not `\0`-before-digit) to avoid legacy-octal strict-mode syntax errors — test-authoring detail, no plan delta.

## 8. Residual risks

- **None blocking for this task.** Carry-forward (owned by later phases): the DEV1-002 test locks pin the broader `[A-Z0-9]{8}` superset while the canonical constant is `[0-9A-F]{8}` — intentionally compatible (hex ⊂ alnum), documented in 0.2 §3 and here; if Phase 2.1 wants the permanent format lock to use `HANDSHAKE_CODE_PATTERN`, it must ADD assertions (not edit) the DEV1-002-owned file, or keep the stricter pin in the new 2.1 lock file.
- `normalizeHandshakeCode` trims Unicode whitespace including NBSP/U+3000 (verified accepted→pass post-normalization); generation emits only ASCII so no collision surface is affected.

## 9. Environment notes / carry-forward knowledge

- **`run-test.ts` bun-path fix (affects ALL later test tasks):** the mandated runner spawns `~/.bun/bin/bun` (`homedir()/.bun/bin/bun`) but this sandbox only had `/usr/local/bin/bun`; first run failed `ENOENT … posix_spawn '/home/z/.bun/bin/bun'`. Fixed at ENVIRONMENT level (no repo change): `mkdir -p /home/z/.bun/bin && cp /usr/local/bin/bun /home/z/.bun/bin/bun` (symlinks are blocked in this sandbox; the 88 MB copy works, `bun 1.3.14` both paths). Later tasks running tests via `bun run test/scripts/run-test.ts` will now work out of the box.
- Sub-loop per-file lint runs fine at default concurrency in this sandbox (only cold FULL-REPO lint needs `LINT_QUEUE_CONCURRENCY=1`, per 0.1 §4.2).
- jscpd `check:duplicates` skips `*.test.ts` by design (sub-loop `shouldSkipJscpd`) — "duplicates passed" for the spec is a skip-pass, consistent with the tool's scope (intra-file clones in production files).
- Branch discipline held: no `git checkout`/commit performed by this task; working tree changes are branch-independent (both refs at `e39096f`) and will ride the orchestrator's switch to `feat/dev1-013-student-handshake-code-generation`.

**Task 1.1 verdict: ✅ COMPLETE** — all gates green (QL exit 0 ×3 files, TE 59/59 pass, SEC verified, SR verified, IV verified); zero DEV1-001/002-owned file modifications; zero schema/config/dependency changes.
