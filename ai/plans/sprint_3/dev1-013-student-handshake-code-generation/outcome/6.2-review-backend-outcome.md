# Task 6.2 Outcome — `review-backend` Wave (Post-Implementation Review)

**Ticket:** DEV1-013 (Student Handshake Code Generation)
**Plan directory:** `ai/plans/sprint_3/dev1-013-student-handshake-code-generation/`
**_Requirements:_ REQ-020, REQ-021, REQ-033, REQ-043, REQ-050, REQ-052, REQ-053, REQ-070, REQ-077/J1–J5**
**Git discipline:** ZERO git state operations. Read-only (`git diff origin/main HEAD --name-only`) only.
**Date:** 2026-08-30

**VERDICT: ✅ PASS — zero blocking findings. All 6.2 verification points hold against the shipped code with direct file/line evidence. 2 informational notes (NEW) + 2 pre-existing observations (out-of-scope surface), none blocking.**

---

## 1. Verification matrix (all from `tasks.md` 6.2)

### 1.1 Normalize-then-validate ordering — REQ-020 ✅
- `backend/services/students/student-handshake.service.ts:126-134` — `normalizeHandshakeCode(code)` (trim → uppercase) runs FIRST, then `isHandshakeCode(normalized)` rejects with `ValidationError` BEFORE the repository call at `:137`. No DB read precedes validation.
- Test lock: `student-handshake.service.test.ts:464-486` — Tier-3 fuzz (21 malformed probes: `%`/`_`/`\`, RTL/bidi, emoji, NUL, 5000-char overflow, whitespace-only) with a `spyOn(StudentRepository, "findDiscoveryByHandshakeCode")` asserting **`toHaveBeenCalledTimes(0)`** — validation strictly precedes persistence reads, proven not asserted-by-faith.
- Case-variant acceptance (`ksb-abcd1234` → valid) locked at `:420-440` (lowercase + whitespace padding resolve identically).

### 1.2 Governance-collapse equivalence — REQ-021/033 ✅
- Service `:139-147`: miss (step 3) and governed child (step 4, `isGovernanceExcludedFromDiscovery`) BOTH return the SAME `null` — one channel, no error, no distinguishing log (see 1.6).
- Fail-closed predicate: `student-handshake.helpers.ts:29-45` — deleted/blocked always excluded; suspended with missing window data fails closed (`:39-41`); strict `>` boundary (window ending exactly at `now` = lapsed → visible, `:42-44`). Caller-supplied `now` (no clock read in the pure module) — one captured instant, no TOCTOU between checks.
- Equivalence locks: service test `:488-501` (three governance fixtures each `toEqual` the nonexistent-code null); journey steps 6a/6b/6c (`test/workflows/parents/handshake-discovery.test.ts:256-315`) assert byte-identical null vs the absent-probe code on the real DB.

### 1.3 `tx` propagation — REQ-043 ✅
- `findStudentByHandshakeCode(code, locale, tx?)` — optional-last, propagated verbatim into `StudentRepository.findDiscoveryByHandshakeCode(normalized, tx)` (`:137`).
- Repo: tx branch executes the Drizzle select on the SUPPLIED executor; no-tx branch uses `queryDb` (`student.repository.ts:134-154`) — no `tx`/global-`db` mixing inside one logical operation.
- Proof-by-isolation: service test `:402-416` — an in-rollback fixture is discoverable ONLY through the propagated `tx`.
- `getMyHandshakeCode` carries no `tx` param: the GraphQL read path owns no transaction, and REQ-043 binds propagation only "where a tx exists" — satisfied. (Forward-compat note in §3.)
- D4 savepoint restructure reviewed: `createForRegistration` wraps the insert in `tx.transaction(async sp => …)` (`:56-73`); the registration retry loop (`registration.service.ts:347-387`) is strictly sequential recursion on the SAME `tx`, so sibling savepoint-name reuse (`spN`) is well-defined PostgreSQL semantics — no concurrent-sibling hazard. Non-collision errors rethrow unchanged (cause-chain intact for `isUniqueViolation`); on success `RELEASE` is transparent. Reviewed for correctness: **correct**.

### 1.4 DomainError taxonomy + `extensions.code` matrix — REQ-050 ✅
- `ValidationError(t.handshakeCodeInvalid)` → code `VALIDATION` (`backend/lib/errors.ts:113` default-form constructor).
- `NotFoundError("STUDENT", …)` → auto-generated `STUDENT_NOT_FOUND` (`errors.ts:39`) — entity-name form, never the full code.
- Zero `new Error(...)` on the new surfaces (the two `throw new Error` guards in test fixture helpers are test-tier, permitted). GraphQL layer adds only the scope-layer `UNAUTHORIZED`/`FORBIDDEN` and an unreachable `UnauthorizedError` narrowing guard.
- `extensions.code` asserted end-to-end: service tier (`assertErrorCode`, `:116-122` + `:321`, `:475`); GraphQL tier over the real schema+scope engine (`handshake-code-surface.test.ts:206-265` — UNAUTHORIZED for anonymous, FORBIDDEN for sibling/teacher/admin roles); journey tier (`:163`, `:213`); HTTP live-wire 12-cell matrix (3.3, re-verified green in 5.1 §2 row 9).

### 1.5 Log hygiene incl. D11 code-elision — REQ-052 ✅
- The SUBMITTED code is NEVER logged — not raw, not normalized, not as a fragment. VALIDATION bag = `{ code, entity, locale }` (`:128-132`); STUDENT_NOT_FOUND bag = `{ code, entity, entityId, locale }` (`:79-84`); both bounded key sets.
- Test lock `:523-547`: distinctive probes (RTL, emoji, over-length, wildcards) asserted absent from `JSON.stringify` of every captured log call; key set asserted exactly `["code","entity","locale"]`. Message strings never interpolate input.
- `logDomainError` (debug in test / warn in prod) is the only channel — zero `console.*` in the new backend files (grep: only the prose mention in the service doc header).

### 1.6 Silent happy path — REQ-053 ✅
- `:549-561` — happy self-read, discovery hit, miss, and governance collapse all emit **0** domain-log calls. No swallowed catches anywhere on the new read paths (unexpected errors bubble to the masking boundary by contract).

### 1.7 100% coverage evidence present — REQ-070 ✅ (with documented caveat)
- Cited from `outcome/5.1-full-test-surface-outcome.md` §5: the four REQ-070 modules — `student-handshake.helpers.ts`, `student-handshake.service.ts`, `handshake-code.constants.ts`, `mask-full-name.ts` — all **100.00% functions + 100.00% lines/statements, zero uncovered lines** (`bun test --coverage`, 101 pass / 0 fail). Grand total across surfaces: 309 pass / 0 fail.
- Caveat (documented, accepted): bun emits **no branch metric** (zero BRDA records); compensating structural proof (both arms of every branch behaviorally distinguished) stands from 1.1/1.2/2.4 — verified by reading the suites: the predicate's 6 arms, all ValidationError/NotFound arms, and both repo executor branches are each exercised (`student.repository.test.ts` Tier 2 covers tx + default `queryDb` paths).

### 1.8 Journey suite realism — REQ-077/J1–J5 ✅
- **Real services**: cast provisioned through the REAL `RegistrationService.registerUser` (commits its own transactions) — `journey-fixtures.ts:271`; reads through the real `StudentRepository` (`:280`). Zero mocks/monkey-patching.
- **Committed fixtures**: `beforeAll` registrations commit (`handshake-discovery.test.ts:108-126`).
- **Tracked teardown**: `afterAll` hard-deletes every tracked id in FK-safe order inside one committing transaction + residue probes on users/students/parents/notifications/auditLogs all asserted 0 (`:324-340`, `journey-fixtures.ts:317-344`).
- **No `runInRollback` under `test/workflows/`**: grep confirms the only occurrences are the prohibition statements themselves (`journey-fixtures.ts:7`, `test/workflows/AGENTS.md:13`) — zero usages.
- Side-effect absence asserted directly (notifications + audit rows = 0, `:327-329`); J1–J5 step mapping present (steps 1–8 + documented cross-references for scope-tier denials).

### 1.9 Architecture ✅
- **Thin resolvers**: `handshake-code.query.ts:66-77, 95` — delegate + `ctx.locale` only; zero business logic inline; no try/catch (masking-boundary contract). The `ctx.user` guard (`:73-75`) is a documented TS-narrowing guard mirroring builder.ts's message verbatim.
- **Repos = zero business rules**: `student.repository.ts` returns rows faithfully (governance filtering lives in the service helper); no hardcoded user-facing strings; no i18n imports.
- **No cross-layer imports**: repo → schema/types/`@/backend/db` only; service → repo barrel/types/shared; GraphQL → `@/backend/services` barrel. Barrel wiring verified: `services/index.ts:11 → students/index.ts → service`; `query/index.ts:19 → students/index.ts → handshake-code.query.ts` (side-effect registration, no named exports).
- **No `id` on the embedded object**; payload closed by the canonical `HandshakeCodeLookupReturnType` (Pick-composed — 6.1's domain).

### 1.10 Dead code ✅
- All new exports consumed: `isGovernanceExcludedFromDiscovery` (service + tests), `normalizeHandshakeCode`/`isHandshakeCode` (service + frontend container), `maskFullName` (service + tests + UI), `HandshakeCodeLookupReturnType`/`HandshakeDiscoveryRowType` (service/repo/pothos + `.test-d.ts` type-proof suite). `HANDSHAKE_CODE_PREFIX` is a plan-mandated 4-export contract member (1.1, plan §2.3), lock-test asserted — not dead, though it has no production consumer yet (see §3 P-2).
- No unreachable branches introduced beyond the documented defensive narrowing guard (repo-wide no-non-null-assertion rule); the D4 `.returning()` empty-row guard is the pre-existing defensive pattern.

### 1.11 Race conditions ✅
- New read paths are single-statement parameterized equality reads (`WHERE handshake_code = $1 LIMIT 1` / `eq(students.id, …)`), no locks, no read-modify-write — nothing to race. D4 savepoint retry: sequential-only, reviewed correct (§1.3).

### 1.12 Plan-artifact references in code comments ✅
- Grep across every backend/shared/locale/test/workflows file in the ticket diff for `REQ-|Task [0-9]|Phase [0-9]|specs.md|tasks.md|plan.md|sprint_3`: **zero matches**. All comments are domain-terms only (incl. the D4 savepoint rationale).

---

## 2. Findings

### NEW (this ticket's diff)
- **[INFO] backend/db/repo/students/student.repository.ts:123-154** — the discovery read exists in two representations (Drizzle builder on the tx branch; raw SQL string `readSql` on the `queryDb` branch) with the column list duplicated in both forms. Drift between branches is the residual risk; mitigated by the identical-alias comment and 2.3's tests exercising BOTH branches per method. Required by the repo AGENTS.md `queryDb` pattern — accepted, no action.
- **[INFO] backend/services/students/student-handshake.service.ts:74** — `getMyHandshakeCode` exposes no optional `tx` passthrough (discovery does). Not a REQ-043 violation (no caller owns a tx today), but a DEV1-014 consumer calling the self-read inside a link transaction would need the parameter added. Forward-compat note only.
- **[INFO] backend/graphql/query/students/handshake-code.query.ts:73-75** — the `ctx.user` null guard is unreachable in practice (guaranteed by `$all { authenticated: true }`); deliberately retained as TS narrowing under the repo-wide no-non-null-assertion rule and documented as such. Acceptable — not counted as dead code.

### PRE-EXISTING (outside this ticket's diff — verify-only DEV1-002 surface)
- **[LOW] backend/services/auth/registration.service.ts:94** — `generateHandshakeCode()` hardcodes the `KSB-` literal instead of consuming `HANDSHAKE_CODE_PREFIX` from the (new) canonical constants module, so the "single source of truth" prefix is currently enforced only by the 1.1 lock tests, not by the generator's import graph. Byte-original file (D4 outcome §3) — correctly untouched under the verify-only mandate. Recommendation (report-only): add a non-blocking forward note to `deferred-items.md` so a future registration-path ticket switches the generator to the shared constant.
- **[LOW] backend/services/auth/registration.service.ts:86** — doc comment says "8 uppercase alphanumeric" but the generator emits hex (`[0-9A-F]`); stale prose on the same pre-existing surface. Same forward-note candidate.

No NEW [BLOCKER]/[HIGH]/[MEDIUM] findings. Nothing to cycle back open.

---

## 3. Verification summary

| 6.2 check | Result | Primary evidence |
|---|---|---|
| Normalize-then-validate (REQ-020) | ✅ | service `:126-137`; repo-spy zero-calls test `:464-486` |
| Governance-collapse equivalence (REQ-021/033) | ✅ | service `:139-147`; helper fail-closed `:29-45`; null-equality tests (service `:488-501`, journey 6a/6b/6c) |
| `tx` propagation (REQ-043) | ✅ | optional-last `tx?` → repo both-branch executors; in-rollback isolation test `:402-416` |
| DomainError + `extensions.code` (REQ-050) | ✅ | VALIDATION / STUDENT_NOT_FOUND mapping asserted at service, GraphQL-engine, journey, and HTTP tiers |
| Log hygiene + code elision (REQ-052) | ✅ | bounded key-set + probe-absence assertions `:523-547`; zero `console.*` |
| Silent happy path (REQ-053) | ✅ | 0-log assertion `:549-561` |
| 100% coverage evidence (REQ-070) | ✅ | 5.1 §5 table: 4/4 modules 100% funcs+lines (branch metric absent from bun — compensating both-arms proof documented) |
| Journey realism (REQ-077) | ✅ | real RegistrationService, committed beforeAll, tracked afterAll teardown + residue probes, zero `runInRollback` |
| Thin resolvers / repo purity / barrels | ✅ | query file delegates + locale only; repo zero business rules; barrels wired once |
| Dead code | ✅ | all exports consumed; guard branches documented |
| Races | ✅ | single equality reads; D4 sequential savepoint retry reviewed correct |
| Plan-artifact refs in comments | ✅ | grep over the full backend diff: zero matches |

## 4. Files created/modified by this task
- CREATED: `ai/plans/sprint_3/dev1-013-student-handshake-code-generation/outcome/6.2-review-backend-outcome.md` (this file)
- APPENDED: `/home/z/my-project/worklog.md` (Task 6.2 entry)
- NOT touched: any production code, any test file, any schema/migration/config file (report-only wave).
