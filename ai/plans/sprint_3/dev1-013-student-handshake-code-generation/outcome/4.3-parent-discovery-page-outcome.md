# 4.3 — Parent Discovery Page — Outcome

**Task ID:** 4.3 (completion pass 4.3b — QL sub-loops + regression; browser self-loops 4.3.BF + 4.3.BS remain for the browser agent)
**Status:** ✅ **Code + tests + QL complete.** 4.3.BF / 4.3.BS (browser loops) **PENDING** — carry-forward in §6.

## 1. Files (all plan-owned; created/modified by this task)

| File | Kind |
|---|---|
| `app/(dashboard)/parent/handshake/page.tsx` | NEW — server-component route shell; `withPageAuth` guard + `generateMetadata`; passes server-translated shell labels (`pageTitle` / `pageDescription`) to the container |
| `frontend/views/parent/handshake/HandshakeDiscoveryContainer.tsx` | NEW — client container: skipToken-gated stateful `useQuery(findStudentByHandshakeCodeQueryDocument)` + derived outcome state machine (idle / validation / generic-error / searching / not-found / found) |
| `frontend/views/parent/handshake/HandshakeCodeSearchForm.tsx` | NEW — code input + search submit (44px target, inline format teacher) |
| `frontend/views/parent/handshake/HandshakeCodeResultCard.tsx` | NEW — masked-name result card, `linkable`-driven copy, **NO CTA (D1)** |
| `frontend/views/parent/handshake/index.ts` | NEW — barrel |
| `frontend/views/dashboard/DashboardSidebar.tsx` | MODIFIED — resolves feature-owned nav labels through `HandshakeCodeLabels` (`resolveNavItemLabel`) |
| `frontend/views/dashboard/navItems.ts` | MODIFIED — `/parent/handshake` nav item (`navLinkMyChild`) added **to the Parent role group only** |
| `shared/locale/types/handshakeCode/index.ts` | MODIFIED — `navLinkMyChild: string` label key |
| `shared/locale/en/handshakeCode/index.ts` | MODIFIED — `navLinkMyChild: "Link my child"` |
| `shared/locale/ar/handshakeCode/index.ts` | MODIFIED — `navLinkMyChild: "ربط طفلي"` |
| `test/ui/components/parent/HandshakeDiscoveryContainer.test.tsx` | NEW — 28 component tests, both locales × all outcome branches + zero-network proofs + D1 no-CTA |
| `test/ui/page-guards/parent-handshake-page.test.ts` | NEW — 6 SSR guard tests (anonymous → login, student/teacher/admin → own role dashboard, parent renders container en/ar + metadata) |

## 2. QL sub-loops (`bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates`)

All 12 files → **exit 0**. Three failed on first pass and were fixed in this completion pass:

| File | First | Fix | Final |
|---|---|---|---|
| `app/(dashboard)/parent/handshake/page.tsx` | 0 | — | **0** |
| `frontend/views/parent/handshake/HandshakeDiscoveryContainer.tsx` | 1 | type-aware lint: relative sibling imports (`./HandshakeCodeResultCard`, `./HandshakeCodeSearchForm`) → `@/frontend/views/parent/handshake/…` alias per AGENTS.md | **0** |
| `frontend/views/parent/handshake/HandshakeCodeSearchForm.tsx` | 0 | — | **0** |
| `frontend/views/parent/handshake/HandshakeCodeResultCard.tsx` | 0 | — | **0** |
| `frontend/views/parent/handshake/index.ts` | 0 | — | **0** |
| `frontend/views/dashboard/DashboardSidebar.tsx` | 0 | — | **0** |
| `frontend/views/dashboard/navItems.ts` | 0 | — | **0** |
| `shared/locale/types/handshakeCode/index.ts` | 0 | — | **0** |
| `shared/locale/en/handshakeCode/index.ts` | 0 | — | **0** |
| `shared/locale/ar/handshakeCode/index.ts` | 0 | — | **0** |
| `test/ui/components/parent/HandshakeDiscoveryContainer.test.tsx` | 1 | `sonarjs/no-trivial-assertions` (statically-true `expect(A).not.toBe(B)` between two const literals) → replaced with the meaningful runtime normalizer precondition | **0** |
| `test/ui/page-guards/parent-handshake-page.test.ts` | 1 | oxlint `no-unsafe-type-assertion` ×3 + lint `sonarjs/void-use` → replaced `as {…}` JSX casts with an `in`-guard narrowing helper (`readShellElement`, no casts) and the unused catch binding with optional `catch {}` | **0** |

## 3. Tests + typecheck

| Command | Exit | Result |
|---|---|---|
| `bun run test:ui:components` | **0** | **80 pass / 0 fail / 1 snapshot — 7 files, 20.14s** (28 new parent tests + 52 prior across 6 files; zero regression) |
| `bun run test/scripts/run-test.ts test/ui/page-guards/parent-handshake-page.test.ts` | **0** | **6 pass / 0 fail**, 21 expect() calls |
| `bun run test/scripts/run-test.ts shared/locale/handshakeCode-namespace.parity.test.ts` | **0** | **10 pass / 0 fail**, 187 expect() calls — the new `navLinkMyChild` key is parity-covered |
| `bun run tsgo` (→ `tsgo -b --noEmit`) | **0** | 0 errors |

Runner notes (unchanged from 4.2): `run-test.ts` is the sanctioned runner for `test/ui/page-guards/**` and `shared/locale/*.parity.test.ts`; `test/ui/components/**` MUST go through `bun run test:ui:components` (preloads).

## 4. SR greps (on all new/modified files)

| Check | Result |
|---|---|
| Direct style props (`fontWeight=` \| `mb=` \| `mt=` \| `p=` \| `display=` as JSX props) | **NONE** — `sx` only |
| Hardcoded colors (`#hex` / `rgb(` / `rgba(`) | **NONE** — theme-palette callbacks only |
| Non-`*Outlined` `@mui/icons-material` imports | **NONE** — `SearchOutlined`, `SearchOffOutlined`, `PersonSearchOutlined`, `LinkOffOutlined`, `LinkOutlined` (nav) |
| `FormEvent` / `useLazyQuery` in code | **NONE** — skipToken-gated stateful `useQuery`; doc comments mention the bans only |
| CTA / "Send link request" element (D1) | **NONE** — `Button` exists only in the search form (the submit action); the result card renders zero buttons (test-asserted on both found branches) |
| Nav item parent-group-only | **VERIFIED** — `/parent/handshake` (`labelKey: "navLinkMyChild"`) appears exactly once, inside `[UserRole.Parent]` (`navItems.ts:85`); not in Student/Teacher/Admin groups |
| i18n parity of new key | en `"Link my child"` / ar `"ربط طفلي"` / types `readonly navLinkMyChild: string` — parity suite green |

## 5. IV — instruction verification

- `app/AGENTS.md` (line 14): dashboard pages use the shared `withPageAuth()` wrapper instead of inline auth — **compliant**: the page's only authorization boundary is `await withPageAuth({ roles: [UserRole.Parent], redirectTo: "/parent/handshake" })`, behavior pinned by the 6 SSR guard tests (anonymous → `/login?redirect=/parent/handshake`; student/teacher/admin → their own role dashboard; parent → container).
- `frontend/AGENTS.md`: "NO hardcoded colors allowed — all colors from theme palette" — **compliant** (§4 greps); translations resolve exclusively through the compile-time `HandshakeCode` / `Errors` namespace handles (`useAppTranslation`) client-side and `getTranslations(locale)` server-side — no user-facing string literals in the components.

## 6. Carry-forward for the browser agent (4.3.BF + 4.3.BS)

- **Login:** parent account `parent@draftacademy.local` / `adminpassword123` (all seeded users share `ADMIN_PASSWORD` from `.env`; `users.id=3`, role `parent`).
- **Route:** `/parent/handshake` — also reachable via the sidebar "Link my child" nav item (parent group only).
- **Real linkable code (found + can-link branch):** `KSB-D002246D` — belongs to `student@draftacademy.local` (`students.id=4`), `parent_id IS NULL` → NOT linked → expect masked-name card + can-link copy, **no CTA** (D1).
- **Already-linked branch (already-linked copy, still no CTA):** `KSB-FE1BE23B` is currently linked to `test-5d979eef-d329-4bcd-82d1-6875bd4fdeb1@test.local` → expect `linkable: false` copy. Alternative unlinked codes if a fresh miss/rotate is needed: `KSB-13982C3B`, `KSB-8DDE5E66`, `KSB-D8EC4D73`, `KSB-126F0817`, `KSB-EE2FF5D4`, `KSB-3385CA2D`, `KSB-8604B792`, `KSB-DB0D2721`, `KSB-42388566`, `KSB-765A91DC`.
- **Not-found branch:** any well-formed but unknown code (e.g. `KSB-00000000`).
- **Read-only fixture checks** (NO DB writes — psql client is not installed; use bun + `postgres` one-liners): DB `postgresql://postgres@127.0.0.1:5432/app_db`; columns are snake_case — `SELECT s.handshake_code, s.parent_id, u.email FROM students s JOIN users u ON u.id = s.id WHERE s.handshake_code = '<CODE>'`; `parent_id IS NOT NULL` → already-linked. (Note: `students.id` is the shared PK = `users.id`; there is no `user_id` column.)
- **Creating a stable already-linked fixture:** prefer using an already-linked row (above). If a deterministic fixture is truly required, coordinate with the orchestrator for a sanctioned seed change — do NOT `UPDATE` app_db from the browser agent.
- **4.3.BF checklist:** en+ar flows (RTL), idle→searching skeleton (`aria-busy`)→found (linkable + already-linked)→not-found (neutral card, not Alert)→VALIDATION inline field error→generic error Alert; lowercase input must send the UPPERCASE variable on the wire (HAR); malformed input must produce ZERO network operations; sidebar nav label localized in both locales; parent-gated (student/teacher/admin accounts bounce to their own dashboards).
- **4.3.BS checklist:** pixel-sampling vs theme palette (surfaceContainerLow cards, outlineVariant borders, text.secondary), RTL mirroring, 375px no-overflow, 44px submit target, en/ar × light/dark × viewports; archive captures to `outcome/screens/4.3/`.

## 7. Residual risks / notes

- The `frontend/providers/theme/typography.ts:146` unmounted `--font-jetbrains-mono` var (pre-existing, noted in 4.2) remains out of scope; the parent discovery surface uses no mono font.
- MUI Emotion hydration-mismatch console noise on `/login` is pre-existing app-wide (4.2 finding) — ignore unless reproducible only on `/parent/handshake`.
