# Task 2.3 Outcome — Repository: Additive Read Methods on `backend/db/repo/students/student.repository.ts`

**Ticket:** DEV1-013 (Student Handshake Code Generation)
**Plan directory (actual):** `ai/plans/sprint_3/dev1-013-student-handshake-code-generation/`
**_Requirements:_ REQ-014 (student self-read data source), REQ-035 (injection surface — none), REQ-042 (read-only query discipline), REQ-043 (tx propagation)**
**Git discipline:** NO git state operations performed (no checkout/switch/branch/stash/restore; no commit). Working tree observed on `main` @ `e39096f` for the whole task (see §8).

---

## 1. What was added

### 1.1 `backend/db/repo/students/student.repository.ts` — exactly two new methods (additive-only)

```ts
export async function findHandshakeCodeByStudentId(studentId: number, tx?: DBTransaction): Promise<string | null>

export async function findDiscoveryByHandshakeCode(
  code: string,
  tx?: DBTransaction
): Promise<HandshakeDiscoveryRowType | null>
```

Both follow the repo's sanctioned read pattern (`UserRepository.findByEmail`/`findById` and `ApplicantRepository.findByUserId` precedents):

- **`tx` branch (transactional read):** Drizzle query-builder select on the supplied executor —
  - `findHandshakeCodeByStudentId`: `tx.select({ handshakeCode: students.handshakeCode })…where(eq(students.id, studentId)).limit(1)`;
  - `findDiscoveryByHandshakeCode`: `tx.select({ 7 fixed columns })…from(students).innerJoin(users, eq(users.id, students.id)).where(eq(students.handshakeCode, code)).limit(1)` — students⋈users JOIN on the shared PK.
- **default branch (non-transactional read):** raw parameterized SQL through `queryDb` from `@/backend/db` (Neon-HTTP-eligible fast path), `$1`-bound values array, snake_case columns aliased to the exact camelCase keys of `HandshakeDiscoveryRowType`. Both branches return identical column sets (same aliases).
- Misses return `null`; **no thrown errors, no business rules, no log strings, no i18n imports** — validation, governance filtering and error mapping are service-layer concerns (verified by the governed-fixture test: the repo returns `isBlocked: true` rows faithfully).
- **Existing `createForRegistration` is byte-identical** (verified: extracted-method `diff` of HEAD vs working tree → identical). The only "deleted" diff line is the expanded `import type` line (adds `HandshakeDiscoveryRowType`); header doc-comment gains read-convention prose only. Diff stat: `+98 / −1`.

### 1.2 `backend/types/students/student.types.ts` — Task 1.3 dependency RESTORED (see §8)

The working tree (on `main`) did NOT contain Task 1.3's artifacts (they live on `feat/dev1-013-student-handshake-code-generation` @ `a5e6e56`). Since 2.3's imports REQUIRE `HandshakeDiscoveryRowType` from `@/backend/types`, the file was restored **byte-identically to the feat branch version** (verified: `git show feat/...:backend/types/students/student.types.ts | diff - backend/types/students/student.types.ts` → identical). Content = Task 1.3's canonical deliverable: `HandshakeCodeLookupReturnType` (interface, readonly) + `HandshakeDiscoveryRowType` (`Pick<StudentSelectType, "parentId"> & Pick<UserSelectType, "fullName" | "isDeleted" | "isBlocked" | "suspended" | "suspendedAt" | "suspendedPeriodDays">`) with `UserSelectType` deep-imported from `@/backend/types/users/user.types` (1.3's §3 import-location decision, preserved). Byte-identity makes the eventual merge with the feat branch conflict-free. (The 1.3 `.test-d.ts` proof file was NOT recreated — it exists on feat and is 1.3-owned.)

### 1.3 `backend/db/test/repo/students/student.repository.test.ts` — NEW 4-tier repo spec

First file in `backend/db/test/repo/students/` — the location is dictated by `backend/db/test/AGENTS.md` rule 11 (`repo/` = per-repository-method coverage tests) + `backend/db/test/logic/AGENTS.md` ("Files are grouped by domain, mirroring the `backend/db/test/repo/`, `backend/db/repo/`, … layouts" → students domain subdir) + `backend/AGENTS.md` §6 (Test Layer = `backend/db/test/repo/*.test.ts`). No existing student repo test existed to copy; `logic/auth` + `logic/teachers` were the in-tree style references. No barrels needed (test files are glob-discovered; `logic/AGENTS.md` "Adding New Tests" rule 3).

## 2. Requirements traceability

| Requirement | How satisfied |
|---|---|
| **REQ-014** | `findHandshakeCodeByStudentId(studentId, tx?)` is the single-column read behind `myHandshakeCode`; the resolver/service passes `ctx.user.id` (shared PK ≡ `students.id`). Repo adds no identity surface of its own. |
| **REQ-035** | The ONLY predicate in both methods is parameterized equality: `eq(students.handshakeCode, code)` (tx branch) / `WHERE … = $1` + values array (default branch). Grep proof in §5. |
| **REQ-042** | `queryDb` raw-parameterized-SQL non-tx branch (Neon-HTTP-eligible pattern per `backend/db/repo/AGENTS.md`); `tx?: DBTransaction` optional-LAST; NO `inArray`, NO prepared statements (no `.prepare()` anywhere in the diff), NO locks (plain SELECTs — no `FOR UPDATE`, no advisory locks; TOCTOU advisory window on `linkable` is owned forward by DEV1-014 per plan §4.3). |
| **REQ-043** | `tx` optional-last on both methods and propagated into EVERY Drizzle call on the tx branch; the raw-SQL branch runs the read on `queryDb` ONLY when no tx exists (never mixes `tx` + global pool inside one logical operation). Tests exercise both paths per method. |

## 3. 2.3.TE — Test Engineering (evidence)

**Runner:** `bun run test/scripts/run-test.ts backend/db/test/repo/students/student.repository.test.ts` → **exit 0, 10 pass / 0 fail, 43 expect() calls**.

Tier map (every in-tx fixture via `entity-setup.ts` helpers inside `runInRollback`, `tx` passed in the correct last-param position):

| Tier | Test | Asserts |
|---|---|---|
| 1 | `createForRegistration` (pre-existing write, coverage completion) | shared PK, verbatim code, zeroed balances, `parentId: null` |
| 1 | `findHandshakeCodeByStudentId` in-tx | returns the fixture row's exact `handshakeCode` |
| 1 | `findDiscoveryByHandshakeCode` unlinked fixture | **`Object.keys(row)` set-equals EXACTLY the 7 picked columns** (sorted comparator) + full value equality (`parentId: null`, governance defaults, `fullName`) |
| 1 | `findDiscoveryByHandshakeCode` linked + governed fixture | `parentId` maps to the parent user's id; `isBlocked`/`suspended`/`suspendedAt`/`suspendedPeriodDays` map faithfully (repo does NOT filter governed rows — service owns exclusion) |
| 2 | default-executor (no-tx) `findHandshakeCodeByStudentId` | resolves the committed `beforeAll` fixture's code (committed via `db.transaction`, since `queryDb` reads cannot see uncommitted rollback fixtures) |
| 2 | default-executor (no-tx) `findDiscoveryByHandshakeCode` | returns the committed fixture row with **exactly the picked keys** on the raw-SQL branch too |
| 3 | nonexistent id (both executor paths) | `null` |
| 3 | nonexistent code (both executor paths) | `null` |
| 3 | unicode/garbage sweep | 12 hostile inputs (`""`, whitespace, `%`, `_`, `\`, SQL-injection strings, Arabic, emoji, RTL override `\u202E`, 104-char over-length, embedded spaces) → every probe `null`, nothing throws — validation is the service's pre-DB contract |
| 4 | rollback purity | fixtures created inside `runInRollback` are visible INSIDE the tx (both methods) and invisible on a fresh session AFTER the forced rollback (both methods → `null`) |

**Committed-fixture hygiene (AGENTS.md rule 9):** the `beforeAll` fixture is hard-deleted in `afterAll` (`db.delete(users)` cascades to `students` via the shared-PK FK) with a post-teardown invisibility probe on BOTH methods inside `afterAll`. Externally re-verified after the run: `select count(*) from users where full_name like '%Fixture%'` → **0 rows** (and `app_db_test.users` count 0).

**Coverage (rule 14):** `bun test --coverage` (lcov) on the suite → `backend/db/repo/students/student.repository.ts` **100% functions (7/7), 98.41% lines (62/63)**. The lcov section shows exactly ONE uncovered line: `DA:57,0` = the pre-existing, unreachable defensive `throw` in DEV1-002-owned `createForRegistration` (`.returning()` always yields the inserted row; covering it would require an unsafe `as unknown as DBTransaction` mock that the repo's no-unsafe-type-assertion lint forbids). **Both NEW methods: 100% lines, both executor branches exercised per method.**

**Regression:** `bun run test/scripts/run-test.ts backend/db/test/logic/auth/registration.service.test.ts` → exit 0, **18 pass / 0 fail** (pre-existing suite unaffected by the types-file restoration).

## 4. 2.3.QL — Quality Loop evidence

| # | Command | Exit |
|---|---|---|
| 1 | `bun run scripts/health/sub-loop.ts backend/db/repo/students/student.repository.ts --lifecycle duplicates` | **0** (tsgo ✓ oxlint ✓ biome ✓ lint:type-aware ✓ check:duplicates ✓) |
| 2 | `bun run scripts/health/sub-loop.ts backend/db/test/repo/students/student.repository.test.ts --lifecycle duplicates` | **0** (after 3 root-cause lint fixes — see §6; check:duplicates skipped-by-design for test files, sub-loop reports pass) |
| 3 | `bun run scripts/health/sub-loop.ts backend/types/students/student.types.ts --lifecycle duplicates` | **0** (restored dependency file, protocol #2 for every edited file) |

## 5. 2.3.SEC — Security & Tenancy Audit

- **BOLA — no unscoped caller-controlled owner id:** `findHandshakeCodeByStudentId` takes a `studentId` that the SERVICE derives exclusively from the authenticated identity (`ctx.user.id` — REQ-014; the resolver accepts zero arguments by GraphQL construction). `findDiscoveryByHandshakeCode` takes the out-of-band capability key (the handshake code), not any owner/tenant id; access is scoped by the service/GraphQL role gates above (parent role, task 3.1) and governance collapse (task 2.4). The repo layer adds no authorization of its own BY DESIGN (repo AGENTS.md separation of concerns) — documented here as required.
- **Injection surface — parameterized equality only:** `git diff -- <repo file> <types file> | rg 'sql`'` → 2 hits, BOTH negative-assertion prose inside doc-comments ("no `sql` templates" at lines 17 and 98); **zero executable `sql` tagged-template usages**. No string concatenation of caller input into SQL (raw-SQL branch is a fixed string + `$1` + values array), no LIKE/ILIKE (grep: only prose), no `inArray` (grep: only prose), no `.prepare()` (grep: none). Tier-3 hostile sweep proves the parameterized path is harmless for unicode/garbage payloads.
- **BOPLA — fixed column list, never spread-driven:** the tx branch selects an explicit 7-key object; the raw-SQL branch selects 7 explicitly aliased columns. Grep for `...` spreads in the repo file → **none**. The column set is compile-pinned to `HandshakeDiscoveryRowType` and test-pinned by the `Object.keys` set-equality assertion (a schema or selection drift fails the test first).

## 6. 2.3.SR — Semantic Review

- **Additive-only diff:** existing `createForRegistration` byte-identical (§1.1); only the import line "modified" (one line replaced by the expanded type import). No method signatures, barrels, re-exports, or schema files touched (`git status`: exactly `M student.repository.ts`, `M student.types.ts`, `?? backend/db/test/repo/`).
- **Import ordering per biome:** `drizzle-orm` → `@/backend/db` → `@/backend/db/schema/students/students` → `@/backend/db/schema/users/users` → `type { … } from "@/backend/types"` — mirrors `user.repository.ts`; biome:check (organizeImports on) passes.
- **Barrels:** none touched — `backend/db/repo/students/index.ts` already re-exports `./student.repository` (verified, unchanged); the repo namespace gains both methods through the existing barrel automatically; `backend/types` barrel path for `HandshakeDiscoveryRowType` verified live by the repo's own import.
- **No plan-artifact references in code comments** (grep: zero `ai/plans` mentions in the three files); zero `console.*`; zero unused imports/vars (tsgo + oxlint clean).
- **Test-file lint root-cause fixes during QL** (no `*-disable` comments used): `unicorn/no-array-sort` → `toSorted(comparator)`; `sonarjs/no-alphabetical-sort` → shared `localeCompare` comparator; `eslint/no-await-in-loop` → hostile probes collected via `Promise.all` over `map` (independent reads); TS2769 on `toEqual(readonly string[])` → mutable `string[]` constant.

## 7. 2.3.IV — Instruction Verification

- `backend/db/repo/AGENTS.md` — COMPLIANT: non-transactional reads use `queryDb` (the Neon-HTTP-eligible pattern as actually implemented by the `UserRepository`/`ApplicantRepository` in-tree precedents; note the `docs/drizzle/neon-http-client.md` `queryDb(tx)`-executor prose does not match the shipped `queryDb(text, params)` helper — the codebase precedent was followed, matching how the AGENTS.md rule is satisfied by every existing read repo in this tree); `tx` optional-LAST; **no prepared statements** for these single-scalar equality reads (prepared statements are optional "MAY", and both existing read repos use plain parameterized SQL — consistency chosen); no `inArray`+placeholder hazard (no `inArray` at all); namespace-per-file convention preserved; types from `@/backend/types`; no hardcoded user-facing strings (the only `throw` is the pre-existing DEV1-002 technical guard).
- `backend/AGENTS.md` — COMPLIANT: types imported from `@/backend/types`; repository = pure data access; `DBTransaction` from `@/backend/types` only.
- `backend/db/test/AGENTS.md` — COMPLIANT: rules 1 (runInRollback + tx to every call), 2 (entity-setup helpers), 3 (no `rejects.toThrow`; no error-assertions needed at all — reads return null), 4 (tsgo/lint clean), 6 (bun:test), 9 (committed fixture with afterAll hard-delete), 11 (`repo/` placement), 14 (coverage measured; see §3 for the one pre-existing uncovered guard line), 15 (own data, never seeds; nonexistent probes use constants far outside the id space), 16 (`tx` param-position checked per signature), 17 (helper signatures read from `entity-setup.ts` source before use), 18 (schema columns verified against `students.ts`/`users.ts`), 19 (no `getServerTranslations` usage).
- Root `AGENTS.md` — read (commands, testing conventions, layer rules).
- **Auto-discovered instruction files:** `.github/instructions/` does NOT exist in this repo (confirmed 0.3 finding F9) — no auto-discovered instruction files apply; parent-layer AGENTS.md files above were read instead.
- `docs/drizzle/prepared-statements.md` read in full — the "What NOT to Prepare" section (single-scalar equality MAY prepare; writes never) plus the repo AGENTS.md no-prepared-statement note for this ticket's reads: **zero prepared statements shipped** (simplest compliant posture, matching both in-tree read-repo precedents).

## 8. Environment & workspace-state observations (non-blocking, for the orchestrator)

1. **Branch state:** working tree on `main` @ `e39096f` for this entire task; the feature branch `feat/dev1-013-student-handshake-code-generation` @ `a5e6e56` holds the committed Phase 0–1 work (incl. `outcome/` files and Task 1.3's types). Per the standing git-discipline instruction I performed NO branch operations. **Consequence:** Task 1.3's `student.types.ts` artifacts were absent from the working tree; I restored them byte-identically from the feat branch (§1.2) so 2.3 compiles and the future merge is trivial. ORCHESTRATOR: `student.types.ts` in the working tree now equals the feat-branch version exactly.
2. **Which DB do db tests hit?** Pre-existing behavior (committed at `e39096f`, NOT touched by this task): `applyDbEnvOverride()` in `backend/lib/env.ts` force-loads `.env`'s DB keys at module-eval time, overriding `.env.test`'s `DATABASE_URL` — so `bun run test/scripts/run-test.ts` db tests actually connect to **`app_db`** (dev), not `app_db_test`. Empirically confirmed (`current_database()` probe under the exact run-test child env: `app_db`). All pre-existing suites run this way today; `runInRollback` + the tracked beforeAll/afterAll hard-delete keep the DB residue-free (verified: 0 leftover fixture rows post-run; `app_db_test.users` = 0 rows). Recorded for awareness; fixing `env.ts` is out of scope (verify-only surface).
3. **Parallel siblings:** Tasks 2.1/2.2 ran concurrently; their files never collided with mine (no `backend/db/test/repo/` or `student.repository.*` writes observed from them). No `test/workflows/` or lock-test files were touched by this task.
4. `bun` coverage emits no branch metric (documented 1.2 precedent); branch coverage of both new methods is instead proven behaviorally — each method's exactly-two executor branches are each exercised by dedicated tests (tx path in Tier 1/3, default path in Tier 2/3), and lcov DA records show every new-method line hit.

## 9. Carry-forward for Task 2.4 (service) — binding integration notes

1. **Signatures (tx optional-LAST, propagate per REQ-043):** `findHandshakeCodeByStudentId(studentUserId, tx?)` → `string | null`; `findDiscoveryByHandshakeCode(code, tx?)` → `HandshakeDiscoveryRowType | null`. Import via `@/backend/db/repo` (barrel) or the deep repo path — both valid; the test file uses the barrel.
2. **`findDiscoveryByHandshakeCode` returns governance columns FAITHFULLY** (deleted/blocked/actively-suspended rows included). The service MUST apply `isGovernanceExcludedFromDiscovery(row, now)` and collapse excluded rows to `null` — byte-identical to the nonexistent-code channel (REQ-021). The Tier-1 governed-fixture test pins this repo contract.
3. **No normalization in the repo** — the service MUST `normalizeHandshakeCode` (trim → toUpperCase) + `isHandshakeCode` guard BEFORE any DB read (D9/REQ-020); the repo is exact-match (verified: lowercase garbage returns null at the repo tier).
4. **Payload mapping:** the discovery row's `parentId` is the raw FK — the service computes `linkable: parentId === null` and MUST NOT leak `parentId` (REQ-019); `maskedName: maskFullName(row.fullName)` from `@/shared/lib/mask-full-name`. Return type: `HandshakeCodeLookupReturnType` (already in `student.types.ts`, barrel-importable from `@/backend/types`).
5. **Null semantics:** both repo methods return `null` on miss — `getMyHandshakeCode` maps null → `NotFoundError("STUDENT", …)` (extensions.code `STUDENT_NOT_FOUND`); `findStudentByHandshakeCode` maps null → plain `null` payload (never an error — D4).
6. **Type import:** `HandshakeDiscoveryRowType` and `HandshakeCodeLookupReturnType` from `@/backend/types` (barrel verified live by this task's repo import).

## 10. Residual risks

- None material for this task's surface. Two environment notes: (a) the `student.types.ts` restoration must be reconciled at merge time (byte-identical to feat — zero-conflict by construction); (b) the `.env`-override DB-target quirk (§8.2) is a pre-existing repo-wide behavior worth an orchestrator decision someday — NOT a DEV1-013 defect.

## 11. Files created/modified by this task

- MODIFIED: `backend/db/repo/students/student.repository.ts` (+98/−1; two methods added; existing method byte-identical)
- MODIFIED: `backend/types/students/student.types.ts` (+20; Task 1.3 dependency restored byte-identically to the feat branch)
- CREATED: `backend/db/test/repo/students/student.repository.test.ts` (4-tier spec, 10 tests)
- CREATED: `ai/plans/sprint_3/dev1-013-student-handshake-code-generation/outcome/2.3-student-repository-lookups-outcome.md` (this file; recreated the `outcome/` dir in the working tree — it exists committed only on the feat branch)
- APPENDED: `/home/z/my-project/worklog.md` (Task 2.3 section)
- NOT touched: `tasks.md` (orchestrator-owned), any barrel, any schema/migration file, any sibling-task file (2.1 lock tests, 2.2 journey harness), `backend/lib/env.ts`, DEV1-001/002/DEV2-002-owned surfaces.
