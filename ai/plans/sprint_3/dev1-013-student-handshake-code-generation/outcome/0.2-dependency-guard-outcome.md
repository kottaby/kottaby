# Task 0.2 Outcome — Dependency Guard: Verify (Do NOT Rebuild) Existing Artifacts

**Ticket:** DEV1-013 (Student Handshake Code Generation)
**Plan directory (actual):** `ai/plans/sprint_3/dev1-013-student-handshake-code-generation/`
**Mode:** READ-AND-ASSERT ONLY — zero source/schema/config modifications by this task
**_Requirements:_ REQ-004** · **_Blocked-by tickets verified:_ DEV1-001, DEV1-002, DEV2-002**
**HEAD:** `e39096f` (working tree on `main`; feature branch `feat/dev1-013-student-handshake-code-generation` at the SAME commit — see §9)

**Verdict: ✅ PASS** — all dependency artifacts exist with the expected contracts; zero broken dependencies; no ❌ ledger entry required. One plan-assumption delta (item 7, `test/workflows/`) and one doc-prose nuance (item 3, hex vs "alphanumeric" label) are recorded below — neither blocks, neither warrants an ❌ entry per the STOP RULE (nothing missing, nothing broken, nothing to patch).

---

## 1. Artifact 1 — `backend/db/schema/students/students.ts` (DEV1-001-owned) — ✅ VERIFIED

All four contract points present in one read:

| Contract | Evidence (file:line) |
|---|---|
| `handshake_code varchar(50) NOT NULL` | `backend/db/schema/students/students.ts:29` — `handshakeCode: varchar("handshake_code", { length: 50 }).notNull()` |
| `unique("students_handshake_code_unique")` | `backend/db/schema/students/students.ts:38` — `unique("students_handshake_code_unique").on(t.handshakeCode)` (also `students_parent_id_idx` at :39) |
| `parent_id` nullable FK → `users.id` ON DELETE SET NULL | `backend/db/schema/students/students.ts:30` — `parentId: integer("parent_id").references(() => users.id, { onDelete: "set null" })` — nullable (no `.notNull()`); import of `users` at :3 |
| Shared-PK `students.id = users.id` | `backend/db/schema/students/students.ts:21-23` — `id: integer("id").primaryKey().references(() => users.id, { onDelete: "cascade" })`; doc comment :8-9 states "Shared PK = FK to users.id with cascade delete (no auto-increment …)" |

## 2. Artifact 2 — `backend/db/schema/users/users.ts` (DEV1-001-owned) — ✅ VERIFIED

All five governance columns present:

| Column | Evidence (file:line) |
|---|---|
| `is_deleted` | `backend/db/schema/users/users.ts:23` — `isDeleted: boolean("is_deleted").default(false)` (plus `deletedAt` :24) |
| `suspended` | `backend/db/schema/users/users.ts:25` — `suspended: boolean("suspended").default(false)` |
| `suspended_at` | `backend/db/schema/users/users.ts:26` — `suspendedAt: timestamp("suspended_at")` |
| `suspended_period_days` | `backend/db/schema/users/users.ts:27` — `suspendedPeriodDays: integer("suspended_period_days")` |
| `is_blocked` | `backend/db/schema/users/users.ts:28` — `isBlocked: boolean("is_blocked").default(false)` (plus `blockedAt` :29) |

All nullable/optional as governance flags; registration service sets them server-side (`registration.service.ts:292-299`).

## 3. Artifact 3 — `RegistrationService` / `StudentRepository.createForRegistration` bounded 23505-retry generation (DEV1-002-owned) — ✅ VERIFIED

**Retry bound (≤5):** `backend/services/auth/registration.service.ts:56` — `const HANDSHAKE_RETRY_LIMIT = 5;` (doc comment :55 "Max attempts to generate a non-colliding `handshake_code` per registration"). The retry loop `createStudentWithHandshakeRetry` (:347-387) is a recursive `attemptInsert` that starts at `attempt: 1` and terminates with `ConflictError("Handshake code generation failed after retries")` + `logger.logDomainError` (code `HANDSHAKE_EXHAUSTED`, :353-364) when `attempt > HANDSHAKE_RETRY_LIMIT` → **max 5 insert attempts**.

**In-transaction:** the retry runs inside the single registration transaction — `registerUser` (:157-186) opens `withTransaction(outerTx, …)` and `createRoleChild` → `createStudentWithHandshakeRetry(userId, tx)` passes the SAME `tx` down to `StudentRepository.createForRegistration(userId, handshakeCode, tx)` (:367); collision retries reuse the same `tx` (:382).

**23505 detection:** `isUniqueViolation` (:103-118) traverses the `DrizzleQueryError.cause` chain for `current.code === "23505"` (:108) plus SQLite-parity message checks (:112); non-collision errors are surfaced immediately (:370-374); each collision is logged with `HANDSHAKE_COLLISION` (:376-381).

**Generation format `KSB-<8 uppercase hex>`:** `generateHandshakeCode()` (:92-95):
```ts
const hex = randomUUID().replace(/-/g, "").toUpperCase();
return `KSB-${hex.slice(0, 8)}`;
```
`crypto.randomUUID()` yields hex chars `0-9a-f` → uppercased `0-9A-F` → emitted codes are exactly `KSB-` + 8 chars from `[0-9A-F]` (12 chars total, comfortable headroom under `varchar(50)`).

**Repository contract:** `backend/db/repo/students/student.repository.ts:26-46` — `createForRegistration(userId: number, handshakeCode: string, tx: DBTransaction)` inserts `{ id: userId, handshakeCode, balanceHifz: 0, balanceTajweed: 0, balanceReviews: 0, parentId: null }` (:31-41) with required-`tx` atomicity (:8-9 doc comment).

**Doc conformance (`docs/auth/user-registration.md` §2, lines 30-75):** §2.1 (:34-50) documents the format with a code sample (:43-46) byte-equivalent to the implementation; §2.2 (:52-75) documents the bounded in-tx retry with `HANDSHAKE_RETRY_LIMIT = 5` (:73) and the `23505` unique-violation trigger (:54). Section §3.1/:117 lists the exact `createForRegistration(userId, handshakeCode, tx)` signature confirmed above.

**Live-test pinning:** `backend/db/test/logic/auth/registration.service.test.ts:204, :353, :454` — `expect(studentRow.handshakeCode).toMatch(/^KSB-[A-Z0-9]{8}$/)` (format test "handshakeCode format: KSB- prefix + 8 uppercase alphanumeric" at :443-456); handshake collision retry + budget exhaustion covered per §9.2 of the doc.

**⚠️ Nuance (doc-prose label, NOT a defect):** `docs/auth/user-registration.md` §2.1 heading says "KSB-<8 uppercase alphanumeric chars>" (and the test name at registration.service.test.ts:443 says "alphanumeric"), but the ACTUAL emitted charset is uppercase HEX (`[0-9A-F]`, UUID-derived) — a strict subset of `[A-Z0-9]`. The doc's own code sample and the implementation agree; only the prose label is broader than reality. Consequence for Phase 1: the planned constant `HANDSHAKE_CODE_PATTERN = /^KSB-[0-9A-F]{8}$/` (task 1.1) is **compatible with every code the DEV1-002 generator can emit** (no false rejections); the broader existing test regex `[A-Z0-9]{8}` accepts a superset. No action required — do NOT edit the DEV1-002-owned doc/test (verify-only contract).

## 4. Artifact 4 — DEV2-002 scope system: `authenticated` + `role` scopes resolvable; `$all` conjunction doc current — ✅ VERIFIED

- **Scope type surface:** `backend/graphql/pothos/builder.ts:69-80` — `AuthScopes` type parameter enumerates `authenticated: boolean` (:71), `role: UserRole[]` (:73), `permission: string[]` (:75), `superAdmin: boolean` (:77), `notImpersonating: boolean` (:79) — typo'd scope keys are compile errors (:62-64).
- **Scope resolvers:** `builder.ts:108-128` — `authScopes: ctx => ({ … })` initializer: `authenticated` throws `UnauthorizedError("Authentication required.")` when `!ctx.user` (:113-118 → 401 semantics), `role: (roles: UserRole[]) => (ctx.role ? roles.includes(ctx.role) : false)` (:120, OR semantics), plus `permission`/`superAdmin`/`notImpersonating` (:123-127). 401-vs-403 mapping locked at source via `scopeAuthOptions.unauthorizedError` (:97-106).
- **`authenticated` scope in live use:** `backend/graphql/query/auth.query.ts:41-43` — `me` query carries `authScopes: { authenticated: true }` (doc comment :10-16 explains the 401 semantics + AuthProvider refresh path).
- **`role:` scope in live use:** `backend/graphql/query/teachers/applicant.query.ts:56-61` — `authScopes: { $all: { authenticated: true, role: [UserRole.Teacher] } }` (import of `UserRole` at :44).
- **`$all` conjunction documentation still current:** `docs/teachers/applicant-lifecycle.md` §3 "Query Contract & Precedence" (:79-97) documents that `{ role: […] }` alone mis-codes anonymous as 403 (:93), a plain map has ANY semantics (:94), and the explicit `$all` conjunction is load-bearing for the 401/403 split (:95). Cross-checked against LIVE code: `applicant.query.ts:55-61` uses exactly `$all: { authenticated: true, role: [UserRole.Teacher] }`, and its header comment (:17-33) plus `builder.ts:84-96` repeat the identical rationale (ANY default strategy, explicit throws pass through verbatim, role misses → localized ForbiddenError). **Doc and code agree — §3 is current.**
- **Supporting docs:** `backend/graphql/AGENTS.md:4` points to `docs/auth/jwt-authentication-service.md` for the canonical authScopes contract (`authenticated`/`role`/`permission`/`superAdmin`/`notImpersonating`), 401-vs-403 state chart, fail-closed rule.

## 5. Artifact 5 — `withPageAuth({ roles, redirectTo })` server wrapper — ✅ VERIFIED

- **Location:** `frontend/lib/auth/withPageAuth.ts` — exported at :67 `export async function withPageAuth(options?: WithPageAuthOptions): Promise<WithPageAuthResult>`; re-exported via barrel `frontend/lib/auth/index.ts:3` (`export * from "./withPageAuth"`).
- **Options contract:** `WithPageAuthOptions` (:34-47): `readonly roles?: readonly UserRole[]` (OR-semantics role whitelist, :41) and `readonly redirectTo?: string` (login return-path, `""` omits `?redirect=`, :46).
- **Guard behavior suitable for `/parent/handshake`:** anonymous callers → `redirect("/login?redirect=<path>")` (:70-78); role mismatch → redirect to caller's role-specific dashboard via `roleDashboardPath(ctx.role)` (:81-87, redirect-loop-safe per `docs/auth/REDIRECT_LOOP_FIX.md` referenced :25); success → `{ userId, user, role }` (:91-95). Auth verified server-side via `getServerUserContext` (:29, :68) reading the httpOnly cookie.
- **Usage precedent for a Server Component page:** doc comment :10-19 shows `const { user, role } = await withPageAuth({ roles: [UserRole.Student] });` — the `/parent/handshake` page can call `withPageAuth({ roles: [UserRole.Parent], redirectTo: "/parent/handshake" })` unchanged. No modification needed.

## 6. Artifact 6 — `frontend/providers/apollo/apolloCache.ts` typePolicies + embedded-value precedent — ✅ VERIFIED

- **`typePolicies` block:** `frontend/providers/apollo/apolloCache.ts:22` — inside `new InMemoryCache({ typePolicies: { … } })` of `createApolloCache()` (:20-44).
- **Embedded-value precedent `AdminNoteInfo`:** :30-32 — `AdminNoteInfo: { keyFields: false }`.
- **Embedded-value precedent `OnlineMeetingInfo`:** :39-41 — `OnlineMeetingInfo: { keyFields: false }`.
- **Additional precedents:** `HealthCheck: { keyFields: false }` (:36-38) and `AdminDashboardScheduleResult.fields.rows.merge = false` (:23-29, replace-not-merge for paginated/filtered lists).
- **Rationale documented** (:10-19): embedded value types with no `id` field are opted out of normalization (`keyFields: false`) to avoid "Cache data may be lost" warnings; always read back through the enclosing parent. This is the exact pattern task 3.x will follow if the handshake lookup returns an embedded info object (e.g. a `HandshakeCodeLookup`-shaped embedded type).

## 7. Artifact 7 — `test/workflows/` existence check — ⚠️ VERIFIED WITH DELTA (directory EXISTS, containing ONLY a rules file; NOT a broken dependency)

- **Plan assumption:** tasks.md item 7 says "Confirm `test/workflows/` DOES NOT exist → task 2.2 must scaffold it".
- **Actual state:** `test/workflows/` **DOES exist** and contains exactly one file: `test/workflows/AGENTS.md` (verified via directory listing; no `helpers/`, no `<domain>/` subdirectories, no test files).
- **The AGENTS.md is a rules file, not scaffolding:** its own status note (`test/workflows/AGENTS.md:7-9`) says "This directory currently contains only this rules file. The first journey implementation scaffolds `test/workflows/helpers/` (cast provisioning + cleanup, pure `export *` barrel) and the first `test/workflows/<domain>/` subdirectory." It defines 10 hard rules for the layer: no `runInRollback` (:13-16), committed fixtures + tracked cleanup (:17-20), unique UUID prefixes (:21-23), honest authorization (:24-26), external-effects interception (:27-30), no `expect(...).rejects.toThrow()` (:31-32), `bun:test` only (:33), `@/` aliases (:34), never demo/seeded rows as fixtures (:35-38), one journey file per workflow under `test/workflows/<domain>/` with shared scaffolding in `test/workflows/helpers/` (:39-42); canonical reference `docs/testing/workflow-journey-tests.md` (:4-5); run commands (:44-56).
- **Effect on task 2.2 (informational, non-blocking):** task 2.2 does NOT need to create the directory — it already exists WITH binding rules. Task 2.2 must **read and follow `test/workflows/AGENTS.md`** and scaffold only the missing pieces (`helpers/` + the handshake domain subdirectory) inside the existing structure. The guard outcome is recorded here so the 2.2 executor sees the delta; per the STOP RULE this is NOT a missing/broken artifact (nothing owned by DEV1-001/002/DEV2-002 is absent or defective), so no ❌ ledger entry was made and the plan does NOT halt.

## 8. 0.2.SR — Semantic Review: zero-modification assertion — ✅ PASSED

`git status --porcelain` immediately after all 7 verifications (before writing this outcome):

```
 M ai/plans/sprint_3/dev1-013-student-handshake-code-generation/deferred-items.md
 M ai/plans/sprint_3/dev1-013-student-handshake-code-generation/tasks.md
?? ai/plans/sprint_3/dev1-013-student-handshake-code-generation/outcome/
```

This is **identical tracked-file state to the 0.1 snapshot** (0.1 modified `deferred-items.md` — seeded D1-D3 — and `tasks.md` — flipped 0.1 checkbox — and created `outcome/phase0-baseline-outcome.md`). `git diff --name-only` shows the same two tracked plan files. **Task 0.2 made ZERO modifications** to any source, schema, config, dependency, or doc file before this outcome was written; the only writes performed by this task are this outcome file, the 0.2 checkbox flip in `tasks.md`, and the worklog append. No DEV1-001/DEV1-002/DEV2-002-owned file was touched.

## 9. Environment observations (non-blocking, for the orchestrator)

1. **Working tree is on `main`** (`git branch --show-current` → `main`) at `e39096f`, NOT on `feat/dev1-013-student-handshake-code-generation` as the task brief stated. Both refs point at the **identical commit** `e39096f8472c456d2214fdb06ce67fddc7945f74` (verified via `git rev-parse`), so verification results are branch-independent and valid. Per the 0.1 env-freeze discipline this task left the checkout untouched; the orchestrator should switch to the feature branch before the first implementation commit ("never on main").
2. **Deferred ledger intact:** `deferred-items.md` still carries exactly the three pre-seeded `📝 Forward` entries D1 (DEV1-014), D2 (DEV2-002), D3 (DEV3-019) seeded by 0.1 — no additions needed (no broken dependencies found).

## 10. Conclusion — PASS

| # | Artifact | Verdict | Primary citation |
|---|---|---|---|
| 1 | `students.ts` handshake_code/unique/parent_id/shared-PK | ✅ VERIFIED | `backend/db/schema/students/students.ts:29,38,30,21-23` |
| 2 | `users.ts` governance columns | ✅ VERIFIED | `backend/db/schema/users/users.ts:23,25,26,27,28` |
| 3 | Bounded ≤5 in-tx 23505-retry `KSB-<8 uppercase hex>` generation | ✅ VERIFIED | `registration.service.ts:56,92-95,103-118,347-387` + `student.repository.ts:26-46` + `docs/auth/user-registration.md` §2 (:30-75) |
| 4 | DEV2-002 `authenticated`/`role` scopes + `$all` doc currency | ✅ VERIFIED | `builder.ts:69-80,108-128`; `auth.query.ts:41-43`; `applicant.query.ts:56-61`; `docs/teachers/applicant-lifecycle.md:79-97` |
| 5 | `withPageAuth({ roles, redirectTo })` | ✅ VERIFIED | `frontend/lib/auth/withPageAuth.ts:34-47,67-96` (+ barrel `index.ts:3`) |
| 6 | `apolloCache.ts` typePolicies + embedded-value precedent | ✅ VERIFIED | `frontend/providers/apollo/apolloCache.ts:22,30-32,39-41` |
| 7 | `test/workflows/` non-existence | ⚠️ DELTA — dir exists with ONLY `AGENTS.md` rules file; nothing broken; 2.2 must scaffold `helpers/` + domain subdir per those rules | `test/workflows/AGENTS.md` (sole file; :7-9 status note) |

**STOP RULE not triggered** — no artifact is missing or broken; no ❌ entry recorded in `deferred-items.md`; the plan may proceed to task 0.3 (Phase-1.5 plan-review gate).

**Files written/modified by this task:**
- CREATED: `ai/plans/sprint_3/dev1-013-student-handshake-code-generation/outcome/0.2-dependency-guard-outcome.md` (this file)
- MODIFIED: `ai/plans/sprint_3/dev1-013-student-handshake-code-generation/tasks.md` (checkbox 0.2 `[ ]` → `[x]` only)
- APPENDED: `/home/z/my-project/worklog.md` (Task 0.2 section)
- Zero source/schema/config/doc modifications.
