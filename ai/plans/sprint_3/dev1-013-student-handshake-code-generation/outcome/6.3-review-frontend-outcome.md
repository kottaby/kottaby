# 6.3 — `review-frontend` Wave Outcome

**Task ID:** 6.3 · **Ticket:** DEV1-013 · **Wave:** post-implementation frontend review (independent fresh context)
**Scope:** `git diff origin/main HEAD` — `frontend/` + `app/` files: HandshakeCodeCard, HandshakeDiscoveryContainer + form/result components, `app/(dashboard)/parent/handshake/page.tsx`, navItems/DashboardSidebar/RoleDashboardPage, shared documents, apolloCache, tests.
**Status:** ✅ **PASS — zero blocking findings.** 2 LOW (new, non-blocking UX/discipline notes) + 1 INFO.

---

## 1. Verification checklist (tasks.md 6.3)

| # | Check | Verdict | Evidence |
|---|---|---|---|
| 1 | MUI v9 `sx`-only discipline (zero direct style props) | ✅ | All layout/styling values (`alignItems`, `justifyContent`, `fontWeight`, `p`, `gap`, `flexDirection`, `mx`…) live in `sx`; only sanctioned direct props remain (`Stack direction/spacing`, `Card elevation`, `Button variant/startIcon`, `TextField label/error/helperText/fullWidth/slotProps`, `Skeleton variant`, `fontSize`). |
| 2 | `*Outlined` icons only | ✅ | `ContentCopyOutlined`, `TagOutlined` (card); `SearchOffOutlined` (container); `SearchOutlined` (form); `LinkOffOutlined`, `PersonSearchOutlined` (result); `LinkOutlined` (navItems:83). Zero non-Outlined icon imports in the diff. |
| 3 | Theme-palette-only colors (no hardcoded hex/rgb/names) | ✅ | Every color resolves via `sx={theme => ({ …theme.palette.* })}` (incl. `success.main`/`error.main` through the `resolveCopyNotice` palette callback). Diff-wide grep for hex/rgb/rgba: zero color hits (only test-fixture credentials). |
| 4 | `React.SubmitEvent` (no `FormEvent` anywhere) | ✅ | `HandshakeCodeSearchForm.tsx:43` — `React.SubmitEvent<HTMLFormElement>`. Grep across diff: `FormEvent` appears only in doc-comments ("never `FormEvent`"). |
| 5 | Skip-gate: no network on invalid input | ✅ | `HandshakeDiscoveryContainer.tsx:52-55` — stateful `useQuery` gated by `validatedCode === null ? skipToken : { variables }`; `handleSubmit` only promotes `normalizeAndValidate`-passed codes. Component-test evidence: `test/ui/components/parent/HandshakeDiscoveryContainer.test.tsx` — idle test (L202) and parameterized invalid-submit test (L224) assert ZERO network operations via a real `ApolloLink` traffic recorder + `settleNetwork()`; wire variable is the normalized UPPERCASE code. |
| 6 | No `useLazyQuery` anywhere in the diff | ✅ | Grep: `useLazyQuery` appears only inside doc-comments asserting its absence. |
| 7 | Embedded `keyFields:false` registered + documents omit `id` | ✅ | `apolloCache.ts` — `HandshakeCodeLookup: { keyFields: false }` (alphabetical slot, comment explains identity-keying prohibition); `handshake-code.documents.ts` selects exactly `maskedName` + `linkable` (no `id`); `apolloCache.test.ts` pins both the policy declarations and inline (non-entity) write behavior; `frontend/graphql/AGENTS.md` embedded-types list updated. |
| 8 | Translation-driven everything | ✅ | All components resolve copy via `useAppTranslation(HandshakeCode)` / `useAppTranslation(Errors)` property access (`t.yourCodeTitle`, `te.internalServerError`, …). Zero `t('key')` calls; zero hardcoded user-facing strings (grep-verified; `KSB-` literals exist only in test fixtures, and the format-helper placeholder is masked). Nav label resolves through the namespace-owning bundle via the `isDashboardLabelKey` type guard (`navItems.ts`), with the new `navLinkMyChild` key covered by the locale parity suite (10/10). |
| 9 | ERR rendering per `extensions.code` | ✅ | Card: `UNAUTHORIZED`/`FORBIDDEN` → `PermissionDeniedFallback`; `STUDENT_NOT_FOUND` → `errors.studentHandshakeNotFound`; other → `errors.internalServerError`. Container: `UNAUTHORIZED`/`FORBIDDEN` → fallback; `VALIDATION` → inline field error (same format-teaching copy as client gate); other → generic `Alert`. Tests author raw `errors[0].extensions.code` through Apollo's real error-wrapping path (card tests L257-291; container tests) — not string shortcuts. |
| 10 | RTL mirroring + LTR code atoms | ✅ | Chip: `dir="ltr"` HTML attribute + `unicodeBidi: "isolate"` with documented cssjanus rationale (`HandshakeCodeCard.tsx:169-190`); input: `slotProps.htmlInput.dir="ltr"` (`HandshakeCodeSearchForm.tsx:83`); masked Arabic names intentionally ride ambient direction. Logical properties (`px/py/mx`, `flexDirection`) throughout. 4.3.BS found + fixed one RTL defect (input LTR isolation) before final captures. |
| 11 | Both Agent-Browser self-loops evidenced + final screenshot matrices | ✅ | 4.2 outcome §4.2.BF (L71) + §4.2.BS (L94) — 15 captures archived under `outcome/screens/4.2/` (en/ar × light/dark × desktop/tablet/mobile + card-only + skeleton), including one browser-verified defect (chip mono font) found + fixed + recaptured. 4.3 outcome §6.1/§6.2 — 25 captures under `outcome/screens/4.3/` (every state × locale × theme × viewport, incl. searching + invalid), in-DOM verified before each shot. All 40 PNGs present in the diff. |
| 12 | Zero fake data surviving from prototypes | ✅ | Every rendered value traces to the query payload (`myHandshakeCode`, `maskedName`, `linkable`) or translated labels. `KSB-` literals exist only in test fixtures; the `invalidFormat` helper copy carries a masked placeholder, never a working code. |
| 13 | Server component never calls GraphQL; `withPageAuth` correct | ✅ | `page.tsx` is a pure server shell: `withPageAuth({ roles: [UserRole.Parent], redirectTo: "/parent/handshake" })` (matching the `RoleDashboardPage` convention) + server-side label resolution via `getTranslations(locale)`; GraphQL lives only in the client container. SSR guard tests pin anonymous → `/login?redirect=/parent/handshake` and non-parent bounces. |

## 2. Prototype layout-parity verdict

**PARITY MAINTAINED — no regressions found.** The five prototype screens (`student-handshake-code-card`, `parent-link-child-{success,not-found,invalid-format,already-linked}`) each have a corresponding implemented state, and each state is covered by archived browser captures in both locales/themes/viewport tiers. Flow parity matches the prototype narrative (submit → skeleton → one of found/not-found/invalid). Two deliberate, functionally-justified deviations, both improvements: (a) the chip uses a literal fixed-pitch mono stack instead of the unmounted `--font-jetbrains-mono` variable (browser-verified fix — the prototype's `var()` would have silently degraded to proportional Inter); (b) not-found renders as a neutral card (no `Alert`/error palette) per REQ-067, visually distinct from the generic-error `Alert`. Caveat (method, not finding): PNG-vs-PNG pixel diff was not re-executed in this wave — parity is attested by the archived 4.2/4.3 capture matrices plus code-level layout review.

## 3. Findings

- **[LOW] (NEW)** `frontend/views/parent/handshake/HandshakeDiscoveryContainer.tsx:78-86` — retry requires an edit round-trip: after a generic error (e.g. `SERVICE_UNAVAILABLE`), re-clicking submit with an UNCHANGED field leaves `validatedCode` at the same value, so the stateful `useQuery` does not refetch and the stale error persists until the user edits (which nulls the gate) and resubmits. Not a network-discipline violation (skip-gate intact); a `refetch`/nonce escape hatch would polish the retry path. Non-blocking — surface host/tosting semantics and the outcome's "form stays retryable" claim slightly overstate the current affordance.
- **[LOW] (NEW)** `frontend/views/students/dashboard/HandshakeCodeCard.tsx:125`, `HandshakeDiscoveryContainer.tsx:91,254`, `HandshakeCodeResultCard.tsx:53` — `<Typography component="h1|h2|h3">` conflicts with the `frontend/AGENTS.md` MUI v9 note ("Typography `component` prop: Not a valid direct prop"). Consistent with the pre-existing codebase convention (`ApplicantStatusCard`, `DashboardView`, `ComingSoonView`) and type-clean under the 5.2 zero-new-findings baseline, so accepted here — but the AGENTS rule and the code disagree; recommend a rule clarification in a docs pass (do NOT touch shipped code for this).
- **[INFO] (NEW)** `HandshakeCodeCard.tsx:239` — the `import("@mui/material/styles").Palette` inline type keeps `resolveCopyNotice` self-contained; works, but a top-level `import type` would read cleaner. Cosmetic only.

**Pre-existing findings surfaced by the wave:** none — the diff's touched shared surfaces (`navItems.ts` label resolution, `DashboardSidebar` prop threading, `RoleDashboardPage` slot extraction, `apolloCache` policy map) follow the existing conventions and add no regressions.

## 4. Verification summary

Code review of all in-scope files (full reads) + diff-wide pattern greps + test-evidence inspection + outcome-evidence cross-check (4.2/4.3 §BF/BS) + screenshot-matrix existence check (40/40 PNGs). Skip-gate, ERR-branch matrix, embedded-cache policy, LTR isolation, i18n property access, and SSR-guard discipline all verified with concrete evidence. **Wave verdict: PASS** — the two LOW notes are forward-polish items, not blockers; no task needs to be cycled back open by this wave.
