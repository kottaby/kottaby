# Task 5.2 Outcome — Differential & Discipline Verification Gates

**Ticket:** DEV1-013 (Student Handshake Code Generation)
**Plan directory:** `ai/plans/sprint_3/dev1-013-student-handshake-code-generation/`
**_Requirements:_ REQ-013, REQ-022, REQ-023, REQ-032, REQ-035, REQ-044, REQ-045, REQ-062, REQ-076**
**Git discipline:** ZERO git state operations. Read-only only (`diff`/`status`/`rev-parse`/`log`/`merge-base`).
**Date:** 2026-08-30 · HEAD `cc1ba1d` (= `feat/dev1-013-student-handshake-code-generation`, 11 commits ahead of `origin/main` `e39096f`, 0 behind)

**VERDICT: ✅ ALL 8 GATES PASS — one real lint finding (REQ-076 initial run) found, root-caused, and FIXED in plan-owned code (3.3's test file); gate re-run green; affected test re-run green (35/0).**

> **Command-note:** pipelines below use `rg` (ripgrep) — the sandbox's sanctioned `grep` equivalent — with the gate's regex verbatim (`rg` and `grep -E` are syntax-compatible for every pattern used here). Raw scan corpora persisted at `/tmp/52-diff-u0.txt` (the `-U0` diff, 11,283 lines) and `/tmp/52-diff-full.txt`.

---

## 0. Summary table

| # | Gate | REQ | Command (literal) | Result | Verdict |
|---|---|---|---|---|---|
| 1 | Zero schema drift | REQ-045 | `git diff origin/main HEAD -- backend/db/schema backend/db/migration` + `git status --porcelain -- backend/db/schema backend/db/migration` | BOTH outputs EMPTY | ✅ PASS |
| 2 | Codegen diff discipline | REQ-062 | `git diff origin/main HEAD --stat -- frontend/graphql/generated/` | +7 / +16 lines, 0 deletions — ONLY this ticket's additions | ✅ PASS |
| 3 | Immutability scan | REQ-013 | `git diff origin/main HEAD -U0 \| rg "^\+" \| rg -i "handshake_?code"` (write-verb filter) | Only production write = `createForRegistration` insert (pre-existing DEV1-002 path, D4-restructured); rest = reads/types/tests/docs; ZERO updates | ✅ PASS |
| 4 | BOPLA scan | REQ-032 | `git diff origin/main HEAD \| rg "^\+.*\.\.\.(input\|args\|row)"` | ZERO matches (rg exit 1) | ✅ PASS |
| 5 | No-LIKE scan | REQ-022/035 | `git diff origin/main HEAD \| rg "^\+.*(LIKE\|ILIKE\|ilike\|like\()"` | Zero in backend code (only doc-comment negatives + test fixture labels + outcome prose); `escapeLikeWildcards` NOT-APPLICABLE affirmed | ✅ PASS |
| 6 | No-caching scan | REQ-044 | cache/memo/redis/key terms over the ticket diff | ZERO backend cache layers/keys; only Apollo CLIENT typePolicy (REQ-063 requirement) + its tests | ✅ PASS |
| 7 | Baseline delta | REQ-076 | `bun run tsgo` / `bun run biome:check` / `bun run scripts/lint-service.ts --json --id final` | 0 / 0 / 0 — equals baseline (after 1 real finding fixed, §7.3) | ✅ PASS (post-fix) |
| 8 | Side-effect absence | REQ-023 | write-verb scan over diff for audit/notification + 2.4 count probes | ZERO audit/notification writes in lookups; Tier-4 probes green (2.4 + 5.1 re-run) | ✅ PASS |

**Diff scope (context):** `git diff origin/main HEAD --name-only` → 113 files: backend source/tests, frontend source/tests, shared modules, `bunfig.toml`, 2 generated artifacts, plan/outcome docs + screenshots. ZERO files under `backend/db/schema/` or `backend/db/migration/`.

---

## 1. Gate 1 — Zero schema drift (REQ-045)

Literal commands + outputs (both EMPTY):

```
$ git diff origin/main HEAD -- backend/db/schema backend/db/migration
[exit=0 — no output]

$ git status --porcelain -- backend/db/schema backend/db/migration
[exit=0 — no output]
```

Corroboration: `git diff origin/main HEAD --name-only` (full 113-file list, §0) contains ZERO paths under `backend/db/schema/**` or `backend/db/migration/**` — no schema file is touched in any form (tracked change, untracked addition, or working-tree modification). The only `backend/db/` files in the diff are `repo/students/student.repository.ts` (M) and test files.

**Verdict: ✅ PASS — zero schema drift, exactly as required by REQ-045 and the ticket's "zero schema drift" scope note.**

## 2. Gate 2 — Codegen diff discipline (REQ-062)

Literal command + output:

```
$ git diff origin/main HEAD --stat -- frontend/graphql/generated/
 frontend/graphql/generated/gql/graphql.ts | 16 ++++++++++++++++
 frontend/graphql/generated/schema.graphql |  7 +++++++
 2 files changed, 23 insertions(+)
[exit=0]
```

What is in the diff (full content reviewed):

1. **`schema.graphql` — +7 lines, 0 deletions.** Exactly this ticket's GraphQL surface from 3.1:
   - `type HandshakeCodeLookup { linkable: Boolean!, maskedName: String! }` (5 lines incl. blank separator)
   - `findStudentByHandshakeCode(code: String!): HandshakeCodeLookup` added under `type Query`
   - `myHandshakeCode: String!` added under `type Query`
   - Alphabetical ordering of both blocks preserved (inserted between `HealthCheck`/`findStudent…` and around `me`/`myApplicantProfile`/`myHandshakeCode` per the generator's sort) — no reordering of any pre-existing line.
2. **`gql/graphql.ts` — +16 lines, 0 deletions.** Exactly 4.1's two shared documents (codegen-synced by 3.2):
   - 5 type exports: `MyHandshakeCodeQuery`, `MyHandshakeCodeQueryVariables`, `FindStudentByHandshakeCodeQuery_findStudentByHandshakeCode` (selection `maskedName`+`linkable`, **NO `id`** — the embedded-value exception), `FindStudentByHandshakeCodeQuery`, `FindStudentByHandshakeCodeQueryVariables` (`code: string`)
   - 2 document constants: `MyHandshakeCodeDocument`, `FindStudentByHandshakeCodeDocument`
   - Inserted in the generator's deterministic position (after the `RecitationReadings*` entries) — untouched context lines confirm the trailing "No newline at end of file" state is pre-existing (it sits on a CONTEXT line, not a changed one).

**No unrelated drift:** every added line names `MyHandshakeCode` / `FindStudentByHandshakeCode` / `HandshakeCodeLookup` only; no other type, document, comment, or formatting hunk appears. Both artifacts are the exact codegen output verified by 3.2's sync gate.

**Verdict: ✅ PASS — the generated-artifacts diff contains ONLY this ticket's additions.**

## 3. Gate 3 — Immutability scan: `handshakeCode` writes only in the registration insert path (REQ-013)

Literal command (gate form) + analysis:

```
$ git diff origin/main HEAD -U0 | rg "^\+" | rg -i "handshake_?code"
→ 700+ added lines across 63 files (full output persisted; per-file counts below)
```

Per-file match counts (top code files): `handshake-code.test.ts` 81 · `handshake-code.documents.test.ts` 56 · `student-handshake.service.test.ts` 55 · `handshake-code.constants.test.ts` 43 · namespace parity 40 · journey 38 · generation-locks 30 · repo test 27 · graphql surface test 27 · … plus production files (repo 14, service 14, types, query, pothos, constants, views, generated, i18n).

**Write-semantics filter over every matched line** (`insert|values({|.set(|update(|onConflict|returning(`) — the complete result set, attributed:

```
ai/plans/.../outcome/2.1-generation-lock-tests-outcome.md :: | **Immutability scan (REQ-013)** — ... zero `.set({handshakeCode:…})`, zero raw SET ... (documentation row)
backend/db/test/logic/students/handshake-code-generation-locks.test.ts :: tx.update(students).set({ handshakeCode: COLLISION_CODE })...   (test fixture — collision setup)
backend/db/test/logic/students/handshake-code-generation-locks.test.ts :: tx.insert(students).values({ id: winner.id, handshakeCode: COLLISION_CODE }),   (test fixture)
backend/db/test/logic/students/handshake-code-generation-locks.test.ts :: tx.insert(students).values({ id: loser.id, handshakeCode: COLLISION_CODE }),    (test fixture)
backend/db/test/logic/students/handshake-code-generation-locks.test.ts :: await tx.insert(students).values({ id: winner.id, handshakeCode: COLLISION_CODE });  (test fixture)
backend/db/test/logic/students/handshake-code-generation-locks.test.ts :: await tx.insert(students).values({ id: loser.id, handshakeCode: freshCode });          (test fixture)
backend/db/test/logic/students/handshake-code-immutability-scan.test.ts :: comment lines + `content:` negative-fixture strings (the VIOLATION samples the scanner must detect — non-vacuity proof)
```

Analysis of every added line referencing handshake code:

- **Production write — exactly ONE site:** `backend/db/repo/students/student.repository.ts` `createForRegistration` (`:51`) — `.insert(students).values({ id: userId, handshakeCode, balanceHifz: 0, … })`. This is the pre-existing DEV1-002 registration insert path (file status `M` — the ticket only relocated the same insert inside a nested `tx.transaction(async sp => …)` savepoint per the D4 fix). `backend/services/auth/registration.service.ts` is NOT in the ticket diff at all (verified: `git diff origin/main HEAD --name-only | rg registration` → no match) — the DEV1-002 generation path is untouched, verify-only honored.
- **Production reads only elsewhere:** the same repo file's additions are `findHandshakeCodeByStudentId` (`:86`, SELECT) and `findDiscoveryByHandshakeCode` (`:118`, parameterized equality SELECT). All other production matches are types/schema/documents/UI/i18n/constants — no storage verbs.
- **NO UPDATE targeting `handshakeCode` anywhere in production code:** every `.set({ handshakeCode: … })` / `update` occurrence in the diff lives in test fixtures (the collision-setup writes in `handshake-code-generation-locks.test.ts` and the scanner's negative fixtures, which are string content the scanner must FLAG, not executed production writes).
- **Standing enforcement:** `backend/db/test/logic/students/handshake-code-immutability-scan.test.ts` (14/14 green in 5.1) statically asserts zero `.set({handshakeCode…})`, zero raw `SET handshake_code =`, zero `onConflictDoUpdate` set payloads across `backend/db/repo`, `backend/services`, `backend/graphql`, `backend/db/seeds`.

**Verdict: ✅ PASS — handshakeCode writes exist ONLY in the registration insert path (repo `createForRegistration`) + test fixtures; zero UPDATEs.**

## 4. Gate 4 — BOPLA scan: zero object-spread-to-persistence (REQ-032)

Literal command + output:

```
$ git diff origin/main HEAD | rg "^\+.*\.\.\.(input|args|row)"
[rg exit 1 — ZERO matches]
```

Supplementary sweep (beyond the gate regex) for spreads of other identifiers reaching a persistence API — `...(body|payload|dto|data|vars|params|filter|criteria|patch|entity|record)` co-occurring with `insert|values(|.set(|update(|queryDb|execute` → **zero matches**. Full inventory of `...` tokens in added PRODUCTION backend lines: a single doc-comment ellipsis (`gqlSchemaBuilder.queryField(...)`) — not an object spread.

**Verdict: ✅ PASS — zero `{ ...input }` / `...args` / `...row` (or any other spread) feeding persistence anywhere in the ticket diff; closed inputs only, per REQ-032.**

## 5. Gate 5 — No-LIKE scan (REQ-022/035) + `escapeLikeWildcards` NOT-APPLICABLE affirmation

Literal command + output (full diff):

```
$ git diff origin/main HEAD | rg "^\+.*(LIKE|ILIKE|ilike|like\()"
→ 15 lines — ALL attributed below; ZERO in executable backend code
```

Attribution of every match:

| File | Lines | Nature |
|---|---|---|
| `shared/constants/handshake-code.constants.test.ts` | 6 | hostile-fuzz tier test-case LABELS ("LIKE wildcard % prefix" etc.) — inputs the validator must REJECT |
| `backend/db/repo/students/student.repository.ts` | 2 | doc-comment NEGATIVE assertions: "no `inArray`, no LIKE/ILIKE, no `sql` templates" / "(`WHERE handshake_code = $1`); no LIKE/ILIKE, no `sql` templates" |
| outcome markdown (2.4, D4, 2.3, 2.1, 1.1) | 8 | documentation of the fuzz tiers + the N/A affirmation itself |

**Zero LIKE/ILIKE/`ilike(`/`like(` in any executable statement.** The lookup predicate inventory (live file): raw-SQL path `WHERE s.handshake_code = $1` (`student.repository.ts:132`) and Drizzle path `.where(eq(students.handshakeCode, code))` (`:148`) — parameterized equality only, both executor paths.

**`escapeLikeWildcards` NOT-APPLICABLE affirmation (REQ-022):** no LIKE/ILIKE query exists anywhere on this surface — the only predicates are `handshake_code = $1` / `eq(students.handshakeCode, code)`; therefore wildcard-escaping is not applicable (nothing to escape in an equality parameter). The repo-wide helper `escapeLikeWildcards` exists for other surfaces; this ticket neither adds nor needs it (its sole in-repo mention outside docs is a pre-existing comment in `backend/types/contracts/contract-guards.test.ts`, untouched by this diff). First affirmed in 2.3/2.4 outcomes; re-affirmed here for the pentester wave (6.4).

**Verdict: ✅ PASS — zero LIKE/ILIKE/`sql`-template usage in new backend code; parameterized equality only.**

## 6. Gate 6 — No-caching scan (REQ-044)

Scan: every added line (full ticket diff) matching `cache|redis|memoiz|lru|evict|invalidate` (+`ttl`), attributed. **Backend result:**

```
backend/graphql/pothos/students/handshake-code.pothos.ts ::  *    cache registers it with `keyFields: false`, mirroring the `HealthCheck`
```

— a doc-comment referencing the CLIENT-side Apollo registration; the only other backend hits are `Promise.allSettled` lines in the locks test (regex artifact: "se-TTL-ed") — not cache code.

All remaining hits are frontend and are the Apollo **client** InMemoryCache surface (required by REQ-063 / plan §4.1): `apolloCache.ts` `HandshakeCodeLookup: { keyFields: false }` typePolicy + its docblock; the documents-tier cache round-trip tests (`apolloCache.test.ts` 10/10, `handshake-code.documents.test.ts` 9/9 — cache-data-loss warning assertions); `bunfig.toml`/`test/preload/apollo-dev-flag.ts` comments enabling Apollo's dev-mode cache warnings under `bun test`; a UI comment about Emotion's stylis RTL cache; and `HandshakeDiscoveryContainer.tsx`'s "A fresh edit invalidates the previous search" — local React state reset, not a cache layer.

**Zero server-side cache layers, zero pinged cache keys, zero memoization (positive or negative caching) for lookup results.** Every lookup goes to the DB; the Apollo typePolicy is client normalization policy (embedded-value, `keyFields: false`, no identity-derived keys), explicitly required by the plan and locked by tests.

**Verdict: ✅ PASS — no cache layer introduced for lookup results (REQ-044).**

## 7. Gate 7 — Baseline delta (REQ-076)

### 7.1 tsgo — `bun run tsgo`

```
$ bun run tsgo
[exit=0]
$ bun run scripts/restore-next-env-dts.ts && bun run scripts/lib/run-locked-cmd.ts tsgo tsgo -b --noEmit
Restored next-env.d.ts to canonical dev dist dir (.next-dev).
[process-lock] Enqueued request for "tsgo" (PID: 31544)
[process-lock] Acquired lock for "tsgo" (PID: 31544). Executing...
[process-lock] Released lock for "tsgo" (PID: 31544)
```

`rg -c "error TS"` on the full log → **0**. (Re-run after the §7.3 fix: identical, exit 0, 0 errors.)

### 7.2 biome:check — `bun run biome:check` (has `--write`; tree must be unchanged after)

```
$ bun run biome:check
[exit=0]
$ bun run scripts/lib/run-locked-cmd.ts biome:check bunx @biomejs/biome check --write --unsafe .
[process-lock] Enqueued/Acquired/Released for "biome:check" (PID: 31582)
Checked 545 files in 5s. No fixes applied.
```

Finding count (`warn|error` lines in output) → **0**. `git status --porcelain` immediately after the run is IDENTICAL to before the run (only the two pre-existing plan-doc entries + the §7.3 fix file) — **biome applied no fixes; nothing to revert.** File-count delta vs baseline (504 → 545 = +41) is exactly this ticket's new files; per-file verdicts are 0/0.

### 7.3 lint-service — `bun run scripts/lint-service.ts --json --id final` — **1 real finding found → FIXED → 0**

**Initial run (literal command):**

```
$ bun run scripts/lint-service.ts --json --id final
[exit=1]
{
  "success": false,
  "output": "\n/home/z/my-project/frontend/graphql/test/students/handshake-code.test.ts\n  756:3  error  Add at least one assertion to this test case  sonarjs/assertions-in-tests\n\n✖ 1 problem (1 error, 0 warnings)\n\n(node:31627) ESLintPoorConcurrencyWarning: ...\n",
  "exitCode": 1,
  ...
}
```

**Root cause (real violation in plan-owned code — 3.3's live-wire integration matrix):** the test at `handshake-code.test.ts:756` ("re-signed token with a NON-CANONICAL role claim degrades to FORBIDDEN") asserted only through the imported helper `expectMutationError` (from `@/test/helpers`, cross-module). `sonarjs/assertions-in-tests` behaves differently per lint mode: in **type-aware** mode (what per-file `sub-loop` uses) the rule follows the call through the TypeScript type checker into the helper's implementation, sees its internal `expect(...)` calls, and counts it as an assertion — so every sub-loop since 3.3 legitimately passed. In **syntactic mode** (what the full-repo `lint-service` command uses — the 0.1 baseline's mode) the rule resolves same-file references only, cannot follow the cross-module helper, and flags the test as assertion-less. This surfaced only now because 5.2 is the first full-repo lint re-run since 0.1. Sibling tests (lines 730, 744) pass in both modes because they additionally call bare `expect(...)`.

**Fix (minimal, style-consistent with the sibling tests):** added two direct assertions pinning the empty data channel on the FORBIDDEN path:

```ts
expectMutationError(selfRead.error, "FORBIDDEN");
expect(selfRead.data?.myHandshakeCode).toBeFalsy();            // + new
...
expectMutationError(discovery.error, "FORBIDDEN");
expect(discovery.data?.findStudentByHandshakeCode).toBeFalsy(); // + new
```

(with a comment explaining the both-modes requirement). **This strengthens the test** — it now also proves no data leaks alongside the 403 error, mirroring lines 741/753.

**Post-fix verification (all literal outputs):**

- Per-file lint, syntactic mode: `bunx eslint --no-cache <file>` → **exit 0**
- Per-file lint, type-aware mode: `ESLINT_TYPE_AWARE="true" bunx eslint --no-cache <file>` → **exit 0**
- Protocol #2 sub-loop: `bun run scripts/health/sub-loop.ts frontend/graphql/test/students/handshake-code.test.ts --lifecycle duplicates` → **exit 0** (tsgo ✅ oxlint ✅ biome:check ✅ lint:type-aware ✅ check:duplicates ✅)
- Affected test re-run (binding 5.1 port protocol observed — pre-flight clean; the known per-run server leak killed -9 post-run, final state verified clean):

```
$ bun run test/scripts/run-test.ts frontend/graphql/test/students/handshake-code.test.ts
→ exit 0 — 35 pass / 0 fail / 169 expect() calls
```

(35/0 unchanged from 5.1; expect count 167 → 169 = the two added no-data-leak assertions.)

- Full-repo re-runs: tsgo exit 0 / 0 errors (§7.1) · biome exit 0 / 0 findings / no fixes applied / tree unchanged (§7.2) · lint-service below.

**lint-service final runs:**

```
$ bun run scripts/lint-service.ts --json --id final          # cold cache, default concurrency
→ exit 1, { "success": false, "output": "", "exitCode": 1, fileCount: 0, 47888ms }   ← documented pre-existing sandbox OOM (baseline §4.2), NOT a lint finding

$ LINT_QUEUE_CONCURRENCY=1 bun run scripts/lint-service.ts --json --id final-c1      # cold-safe per baseline §4.2
→ exit 0, { "success": true, "output": "", "exitCode": 0, 36959ms }                   ← full cold lint: ZERO problems

$ bun run scripts/lint-service.ts --json --id final          # warm cache — THE literal gate command
→ exit 0, {
  "success": true,
  "output": "(node:32410) ESLintPoorConcurrencyWarning: You may reduce or disable concurrency to improve performance.\n(Use `node --trace-warnings ...` to show where the warning was created)\n",
  "exitCode": 0,
  "metrics": { "id": "final", "scope": "full-repo", "fileCount": 0, "durationMs": 15435, ... }
}
```

(The concurrency-1 detour + default-command warm re-run is exactly the baseline §4.2 documented procedure; the final recorded run is the default command shape, matching how the 0.1 headline number was itself captured.)

### 7.4 Side-by-side comparison vs 0.1 baseline

| Check | 0.1 baseline (`phase0-baseline-outcome.md`) | 5.2 final (post-fix) | Delta |
|---|---|---|---|
| `bun run tsgo` | exit 0 · **0 errors** | exit 0 · **0 errors** | **0 new** |
| `bun run biome:check` | exit 0 · **0 findings** · "Checked 504 files… No fixes applied." · tree clean after | exit 0 · **0 findings** · "Checked 545 files… No fixes applied." · tree unchanged after | **0 new findings** (file count 504→545 = this ticket's +41 files; auto-fixes: none in both) |
| `bun run scripts/lint-service.ts --json --id final` | exit 0 · `success: true` · `exitCode: 0` · **0 lint problems** (only benign `ESLintPoorConcurrencyWarning`) | exit 0 · `success: true` · `exitCode: 0` · **0 lint problems** (same benign warning only) | **0 new** — 1 finding surfaced by the initial run was fixed in plan-owned code (§7.3) and the gate re-run is clean |

**Verdict: ✅ PASS (post-fix) — baseline parity restored: tsgo 0 / biome 0 / lint 0, zero NEW findings attributable to the ticket.**

## 8. Gate 8 — Side-effect absence: lookups emit zero audit/notification rows (REQ-023)

**Grep evidence (ticket diff):** added lines with a storage verb (`insert|values({|.set(|update(|create(`) referencing `audit_logs`/`auditLogs`/`notifications` → **ZERO matches anywhere in the diff.** Broader scan — added PRODUCTION lines mentioning those tables at all → **ZERO** (production lookup code never references the audit/notification tables). Every mention in the diff is either outcome-documentation or READ-ONLY test probing:

- `backend/services/students/student-handshake.service.test.ts` — 2.4's Tier-4 count probes (`:563` "read flows write ZERO rows — audit/notification totals unchanged, no attributable rows"): `db.$count(auditLogs)` / `db.$count(notifications)` before vs after a full read battery (happy + miss + governance + both rejection arms) asserted equal (`:567-579`), plus per-tracked-actor attributable rows asserted 0 (`:584-591`). Green in 2.4 (20/0) and re-run green in 5.1 (20/0/176).
- `test/workflows/helpers/journey-fixtures.ts` (`:300-313`) + journey tests — read-only SELECT side-effect counters (fixture users' notification/audit rows counted, asserted zero) behind the 8/8 journey (2.2/2.4, re-run green in 5.1).

**Verdict: ✅ PASS — zero audit/notification INSERTs in any lookup path (REQ-023); service-test count probes + journey counters prove zero side-effect rows empirically.**

---

## 9. Fixes applied by this task

1. **`frontend/graphql/test/students/handshake-code.test.ts`** (plan-owned, 3.3): +2 direct `expect(...).toBeFalsy()` assertions (empty data channel on the non-canonical-role FORBIDDEN path) to satisfy `sonarjs/assertions-in-tests` in BOTH eslint modes (the syntactic matcher cannot follow the cross-module `expectMutationError` helper). File was otherwise untouched; 35/0 pass preserved (expect 167→169). All verification re-run green: per-file lint ×2 modes, sub-loop, the file's own suite, tsgo, biome, full-repo lint.
   - **Working-tree note for the orchestrator:** this fix + 5.1's `tasks.md` flip + 5.1's outcome file are uncommitted — include in the next commit batch (same handling as 2.M's note).

## 10. Operational notes / carry-forward

1. **Lint-mode asymmetry (NEW knowledge, forward-relevant):** `sonarjs/assertions-in-tests` recognizes imported assertion helpers only in type-aware mode. Any test asserting solely via cross-module helpers will pass `sub-loop` but fail the full-repo `lint-service` (syntactic mode). Rule of thumb for future tasks: every `test()` needs at least one direct `expect(...)`/`assert(...)` call.
2. The cold-cache default-concurrency full-repo lint OOM (baseline §4.2) reproduced once (empty output, fileCount 0, ~48 s) after the earlier run's cache file was lost; the documented `LINT_QUEUE_CONCURRENCY=1` warm-up then the default command both ran clean (36.9 s / 15.4 s). Procedure unchanged.
3. Pre-existing/foreign items NOT absorbed by this task: schema-surface freeze baselines + error-contract `_health` legacy cell (open orchestrator/upstream escalations per 3.1 §4.4 / 3.2); `test:ui:kill` port-3099 tooling drift (3.2); MUI Emotion hydration-mismatch page errors (4.2.BF, pre-existing).
4. Branch observation (no action taken, per discipline): working tree is on local `main` at `cc1ba1d`, which currently EQUALS `feat/dev1-013-student-handshake-code-generation` (verified `git rev-parse` — same commit; `origin/main` `e39096f` is 0-ahead/11-behind HEAD). Reported to the orchestrator as before ("flips harmless" per 3.1) — commits continue to be made by the orchestrator only.
5. Sandbox server hygiene: pre-flight found NO stale servers this time; the live-wire re-run leaked its usual `next dev`/`next-server` pair (PIDs 32153/32166 on 3066) — killed -9, final state verified clean (zero listeners/processes).

## 11. Files created/modified by this task

- MODIFIED: `frontend/graphql/test/students/handshake-code.test.ts` (+2 assertions — the §7.3/§9 fix)
- CREATED: `ai/plans/sprint_3/dev1-013-student-handshake-code-generation/outcome/5.2-differential-gates-outcome.md` (this file)
- MODIFIED: `ai/plans/sprint_3/dev1-013-student-handshake-code-generation/tasks.md` (checkbox 5.2 `[ ]` → `[x]` only)
- APPENDED: `/home/z/my-project/worklog.md` (Task 5.2 entry)
- NOT touched: any production code, any schema/migration file, any generated artifact, any config.
