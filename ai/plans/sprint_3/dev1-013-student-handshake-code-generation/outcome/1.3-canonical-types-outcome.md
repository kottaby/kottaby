# Task 1.3 Outcome — Canonical Types: `backend/types/students/student.types.ts` (additive-only)

**Ticket:** DEV1-013 (Student Handshake Code Generation)
**Plan directory (actual):** `ai/plans/sprint_3/dev1-013-student-handshake-code-generation/`
**_Requirements:_ REQ-003 (canonical types discipline), REQ-015 (limited confirmation payload), REQ-019 (no database identity in payload)**
**HEAD:** `e39096f` (working tree on `main`; feature branch `feat/dev1-013-student-handshake-code-generation` at the SAME commit — see §10; this task did not touch git)

---

## 1. What was added (exact definitions)

`git diff backend/types/students/student.types.ts` → **+20 lines, 0 modified, 0 removed** (additive-only; existing `StudentSelectType`/`StudentInsertType` byte-identical):

```ts
import type { UserSelectType } from "@/backend/types/users/user.types";   // NEW import (decision in §3)

/** The ONLY payload a parent-facing handshake-code lookup may return.
 *  Minimal by construction: no database identity, no contact fields, no
 *  governance state — masked confirmation plus the linkable signal only. */
export interface HandshakeCodeLookupReturnType {
  readonly maskedName: string;
  readonly linkable: boolean;
}

/** Internal discovery row shape for the handshake-code lookup — composed
 *  exclusively from the canonical select types via indexed access (no
 *  re-derived column shapes). Service-internal only: it never surfaces
 *  through GraphQL; the parent-facing payload is HandshakeCodeLookupReturnType. */
export type HandshakeDiscoveryRowType = Pick<StudentSelectType, "parentId"> &
  Pick<UserSelectType, "fullName" | "isDeleted" | "isBlocked" | "suspended" | "suspendedAt" | "suspendedPeriodDays">;
```

No local Pothos types, no service-layer `.types.ts`, no new entity types file (REQ-003 negative patterns all avoided).

## 2. Key-name verification against Drizzle `$inferSelect` (ground truth)

| Plan key | Actual `$inferSelect` key | Schema evidence | Inferred member type | Match |
|---|---|---|---|---|
| `parentId` (students) | `parentId` | `backend/db/schema/students/students.ts:30` — `parentId: integer("parent_id").references(() => users.id, { onDelete: "set null" })` (nullable) | `number \| null` | ✅ byte-identical |
| `fullName` (users) | `fullName` | `backend/db/schema/users/users.ts:15` — `varchar("full_name", …).notNull()` | `string` | ✅ |
| `isDeleted` | `isDeleted` | `users.ts:23` — `boolean("is_deleted").default(false)` | `boolean \| null` | ✅ |
| `isBlocked` | `isBlocked` | `users.ts:28` — `boolean("is_blocked").default(false)` | `boolean \| null` | ✅ |
| `suspended` | `suspended` | `users.ts:25` — `boolean("suspended").default(false)` | `boolean \| null` | ✅ |
| `suspendedAt` | `suspendedAt` | `users.ts:26` — `timestamp("suspended_at")` | `Date \| null` | ✅ |
| `suspendedPeriodDays` | `suspendedPeriodDays` | `users.ts:27` — `integer("suspended_period_days")` | `number \| null` | ✅ |

**Verdict: ALL SEVEN key names in the plan are byte-identical to the actual `$inferSelect` names — no mapping/rename decision was needed.** `StudentSelectType` (`student.types.ts:4`) and `UserSelectType` (`backend/types/users/user.types.ts:3`, canonical file; re-exported via `backend/types/users/index.ts:4` and root barrel `backend/types/index.ts:35`) were confirmed by reading the schema-derived definitions directly.

## 3. Import-location decision (documented deviation from plan literal)

- **Plan/tasks literal:** "`UserSelectType` imported from `@/backend/types`".
- **Implemented:** `import type { UserSelectType } from "@/backend/types/users/user.types";` — the canonical users-domain file.
- **Rationale:**
  1. **Repo convention inside the `backend/types/` layer is direct-file imports**, with 3 existing precedents: `backend/types/auth/auth.types.ts:29`, `backend/types/users/registration.types.ts:22`, `backend/types/contracts/teacher-availability.contract.types.ts:19` (all `import type { UserSelectType } from "@/backend/types/users/user.types"`).
  2. The barrel spelling would introduce the layer's **first barrel cycle** (`@/backend/types` → `./students` → `./student.types` → `@/backend/types`). `import type` is erased so it compiles, but no in-layer precedent exists and the cycle is avoidable for free.
  3. **Type identity is identical either way**; downstream consumers (tasks 2.3/2.4/3.1) still import `HandshakeCodeLookupReturnType`/`HandshakeDiscoveryRowType` from `@/backend/types` — verified available: `backend/types/students/index.ts:1` re-exports `./student.types`, root barrel re-exports `./students` (`backend/types/index.ts:33`).
  4. Follows the 0.3 plan-review amendment principle (F3/F4/F8): where plan literals mismatch repo reality, implement the intent with the repo's actual spellings. Intent here = "compose from the users-domain canonical select type" — preserved exactly.
- **No barrel edit needed** (as task 1.3 anticipated): `backend/types/students/index.ts` already `export * from "./student.types";` — the two new types flow through `@/backend/types` with zero barrel changes.

## 4. Type-level proof (1.3.TE) — `backend/types/students/student.types.test-d.ts` (NEW)

**Convention followed:** the repo's only existing compile-assertion suite is `backend/types/contracts/contracts.conformance.test-d.ts` (header: "Validated by `bun tsgo` (the compiler is the test runner)… `.test-d.ts` suffix = outside bun test runner glob"; positives use `satisfies`, negatives use `@ts-expect-error` immediately before the offending line). The new proof file mirrors that header, helper (`const v = (x: unknown): boolean => Boolean(x)`), and structure. It is picked up by `bun run tsgo` via tsconfig `**/*.ts` and is invisible to the bun test glob.

**Assertions (146 lines):**
- **`keyof` equality anchors (the tripwire):**
  - `Equal<keyof HandshakeCodeLookupReturnType, "maskedName" | "linkable">` — EXACTLY the two keys, nothing else;
  - `Equal<keyof HandshakeDiscoveryRowType, "parentId" | "fullName" | "isDeleted" | "isBlocked" | "suspended" | "suspendedAt" | "suspendedPeriodDays">` — EXACTLY the seven joined columns.
- **Member-type anchors:** `parentId: number | null` (nullability drives `linkable`), `fullName: string` (mask input).
- **Positives:** full-payload `satisfies` for both types (incl. an Arabic masked name and a linked/governed discovery row).
- **Negatives (`@ts-expect-error`):** on the payload — `id`, `email`, `parentId`, `isDeleted` keys rejected; `maskedName: number` and `linkable: string` rejected; missing-key rejected; readonly reassignment rejected (TS2540). On the discovery row — `id`, `handshakeCode`, `email`, `passwordHash` rejected; `suspendedAt: string` rejected; missing-key rejected.
- **`Equal` definition:** `type Equal<X, Y> = [X] extends [Y] ? ([Y] extends [X] ? true : false) : false;` — mutual assignability of tuple-wrapped operands (exact for literal-union `keyof` comparisons). The classic conditional-identity `Equal` (`(<T>() => T extends X ? 1 : 2) extends …`) was rejected by oxlint's `no-unnecessary-type-parameters` (single-use function type param, deny-warnings) — root cause fixed by reformulation, **no lint-disable comments** (per `backend/types/AGENTS.md` linting rule).

**Mutation proof (executed with BOTH Equal formulations — identical tripwire behavior):** temporarily adding `readonly id: number;` to `HandshakeCodeLookupReturnType` and running `bun run tsgo`:

```
backend/types/students/student.types.test-d.ts(21,7): error TS2322: Type 'true' is not assignable to type 'false'.   ← keyof anchor fires FIRST
backend/types/students/student.types.test-d.ts(25,42): error TS2741: Property 'id' is missing … satisfies …
backend/types/students/student.types.test-d.ts(28,7): error TS2741: Property 'id' is missing …
backend/types/students/student.types.test-d.ts(32,1): error TS2578: Unused '@ts-expect-error' directive.
```

→ **A future additive field fails compile at the proof file first** (three independent tripwires: keyof anchor, satisfies positives, negative-directive flip). File restored; tsgo re-verified (§5).

## 5. Verification evidence (commands + exit codes)

| # | Command | Exit | Result |
|---|---|---|---|
| 1 | `bun run tsgo` (full project, both new files present) | **0** | zero errors — run BEFORE the concurrent task-1.2 WIP appeared (see §10); repeated after mutation-restore: exit 0 again |
| 2 | Mutation demo (`readonly id: number` injected) | **1** | errors ONLY at `student.types.test-d.ts` (lines 21/25/28/32) — proof file fails first; no errors anywhere else |
| 3 | `bun run scripts/health/sub-loop.ts backend/types/students/student.types.ts --lifecycle duplicates` (1.3.QL) | **0** | tsgo ✓ oxlint ✓ biome:check ✓ lint:type-aware ✓ check:duplicates ✓ |
| 4 | `bun run scripts/health/sub-loop.ts backend/types/students/student.types.test-d.ts --lifecycle duplicates` (Protocol #2 for every edited file) | **0** | all five checks ✓ (first attempt exit 1 — oxlint `no-unnecessary-type-parameters` ×2 on the classic `Equal`; fixed via tuple-mutual-assignability reformulation, re-run exit 0) |
| 5 | `rg -l "HandshakeDiscoveryRowType\|HandshakeCodeLookupReturnType"` (outside plan artifacts) | — | only `student.types.ts` + `student.types.test-d.ts` — zero GraphQL/service/frontend references today (consumers are tasks 2.3/2.4/3.1) |

## 6. Security & tenancy audit (1.3.SEC) — BOPLA

- **Return type is BOPLA-minimal BY CONSTRUCTION (compile-proven, not prose):** `keyof HandshakeCodeLookupReturnType` is EXACTLY `"maskedName" | "linkable"` (anchor assertion) — zero database identifiers (`students.id`/`users.id`), zero contact fields (`email`/`phone`), zero governance state (`isDeleted`/`isBlocked`/`suspended`/`suspendedAt`/`suspendedPeriodDays`), zero balances/sessions/handshake code. REQ-015 "nothing else" and REQ-019 no-identity are now compiler-enforced invariants; the forbidden-key negatives (`id`, `email`, `parentId`, `isDeleted`) double-lock them.
- **Discovery row type is service-internal only:** `HandshakeDiscoveryRowType` carries governance columns + `fullName` + `parentId` BY DESIGN (inputs to the governance-exclusion predicate, the mask, and the `linkable` computation) — the security property is that it never leaves the service layer. Grep (§5 row 5) proves zero references in `backend/graphql/**`, `backend/services/**`, `frontend/**`, `app/**` today; the GraphQL object (task 3.1) is contractually bound to `gqlSchemaBuilder.objectRef<HandshakeCodeLookupReturnType>` per tasks.md 3.1. The type's doc-comment states "Service-internal only: … never surfaces through GraphQL".

## 7. Semantic review self-check (1.3.SR)

- **Composition via `Pick` only** — two `Pick` intersections, zero re-derived column shapes; complies with the composition-only rule (`docs/backend/cross-stream-contracts.md:24`: fields sourced via indexed-access/`Pick`/`NonNullable<>`/literal refs — "Contracts must NEVER redefine DB-column shapes from scratch").
- **readonly immutability on the return type** — `readonly maskedName`/`readonly linkable`; compile-proven by the TS2540 negative in the proof file. (The discovery row keeps the plan's exact non-readonly `Pick` shape — it is an internal read row, and readonly was only mandated for the return type.)
- **No enums anywhere in the diff** — imports are `students` (schema type) and `UserSelectType` (type-only); the "enums are value imports, never `import type`" rule is trivially satisfied.
- **Dead code:** the two new exports have no production consumers yet — they are the canonical deliverable consumed by tasks 2.3/2.4/3.1 (same pattern as `registration.types.ts` landing before consumers); the proof file consumes both. No unused imports/variables introduced (all proof consts consumed via `v()`; `Equal` used in three anchors).
- **Layer purity:** `backend/types` imports only from `@/backend/db/schema/**` and `@/backend/types/**` — nothing from `shared/`, `@/frontend/**`, `@/app/**`.
- **Atomicity/config:** N/A — pure type declarations, zero runtime code, no env, no mutable module state.

## 8. Instruction verification (1.3.IV)

- Read in full: `backend/types/AGENTS.md`, `backend/AGENTS.md`, root `AGENTS.md` (sub-loop's always-applicable file), plus `backend/types/students/index.ts` barrel and the schema/types files cited in §2.
- **Compliance:** single canonical type file extended (no new entity file); naming follows the `{Entity}…Type`/`…ReturnType` family; barrels untouched and compliant (`./` relative + `export *`); "Never create local type definitions in Pothos files" (root AGENTS.md:161) and "Service-layer `.types.ts` files are prohibited" (root AGENTS.md:162) — none created; no `oxlint-disable` comments anywhere in the diff.
- **Auto-discovered instruction files:** sub-loop maps `backend/**` files to `.github/instructions/backend.instructions.md` — `.github/instructions/` does NOT exist in this repo (matches plan-review finding F9); recorded as "no auto-discovered instruction files"; the parent-layer AGENTS.md files above were read instead.

## 9. Decisions/mismatches versus plan (summary)

| Item | Plan literal | Implemented | Why |
|---|---|---|---|
| `UserSelectType` import source | `@/backend/types` (barrel) | `@/backend/types/users/user.types` (canonical file) | §3 — 3 repo precedents; avoids first-in-layer barrel cycle; identical type identity; consumers still use `@/backend/types` |
| `Equal` helper for keyof proof | (unspecified — `Equal<keyof …, …>` assertion required) | tuple mutual-assignability `Equal` | oxlint `no-unnecessary-type-parameters` (deny-warnings) rejects the classic single-use-type-param formulation; semantics identical for literal-union keyof equality; mutation-proven both ways |
| Proof-file harness | "compile-only spec file (or tsgo assertion harness per repo convention)" | `student.types.test-d.ts`, tsgo-is-the-runner | mirrors `contracts.conformance.test-d.ts` — the repo's sole existing compile-assertion convention |

All `$inferSelect` key names matched the plan exactly (§2) — no key-mapping deviations.

## 10. Environment & concurrency observations (non-blocking, for the orchestrator)

1. **Branch state:** working tree on `main` @ `e39096f`; `feat/dev1-013-student-handshake-code-generation` exists at the identical commit (known since 0.1). This task performed NO git operations (per its instruction to never switch branches); commits must be landed on the feature branch by the orchestrator.
2. **Concurrent sibling tasks:** tasks 1.1 (`shared/constants/handshake-code.constants.ts` + test + barrel line) and 1.2 (`shared/lib/mask-full-name.ts` + test) appeared in the working tree DURING this task (timestamps 03:00–03:01). At final verification, project-wide `bun run tsgo` is red ONLY at `shared/lib/mask-full-name.test.ts:74` (TS2540 — `Intl.Segmenter` readonly assignment in the 1.2 agent's in-flight mock). That file is NOT this task's and was not touched (cross-file rule). **This task's cleanliness evidence:** (a) two full-project `bun run tsgo` exit-0 runs with this task's files present, before the 1.2 WIP landed; (b) after the final mutation-restore, the ONLY project diagnostic is the foreign one — zero diagnostics mention this task's files; (c) sub-loop's project-wide tsgo (filtered per-file) exits 0 on both this task's files.
3. **tasks.md checkbox 1.3 intentionally NOT flipped** — the orchestrator's instruction for this subagent explicitly forbids editing tasks.md; per protocol #6 the flip belongs to whoever owns that file (gates pass + this outcome exists, so the flip is unblocked).

## 11. Residual risks

- None material. Forward contract notes: (a) tasks 2.3/2.4/3.1 must import the new types from `@/backend/types` (barrel path verified); (b) task 3.1's Pothos object must back with `HandshakeCodeLookupReturnType` — never the discovery row (§6); (c) any future additive field on either type will intentionally fail compile at `student.types.test-d.ts` first — the anchor comment documents this at the failing site; (d) if a review wave prefers the literal barrel import for §3, it is a one-line, zero-semantic-delta change.

## 12. Files created/modified by this task

- MODIFIED: `backend/types/students/student.types.ts` (+20 lines, additive-only)
- CREATED: `backend/types/students/student.types.test-d.ts` (type-level proof suite)
- CREATED: `ai/plans/sprint_3/dev1-013-student-handshake-code-generation/outcome/1.3-canonical-types-outcome.md` (this file)
- APPENDED: `/home/z/my-project/worklog.md` (Task 1.3 section)
- NOT touched: `tasks.md` (per orchestrator instruction), any barrel, any schema/migration file, any DEV1-001/DEV1-002/DEV2-002-owned file, any sibling-task file (1.1/1.2 WIP).
