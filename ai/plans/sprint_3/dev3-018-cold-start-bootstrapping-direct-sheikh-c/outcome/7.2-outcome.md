# Task 7.2 Outcome — Inbound/Outbound Doc Reconciliation (One-Line Pointers)

**Date:** 2026-09-02 · **Requirements:** REQ-081

## Edits (one-line pointers ONLY)

| File | Change |
|---|---|
| `docs/admin/user-management.md` §6 | Single SHIPPED pointer line inserted after the §6 scope-split table, immediately above the `### Consumer obligations` heading: `> **DEV3-018 SHIPPED:** canonical reference — docs/admin/cold-start-certification.md.` — NO renumbering, NO invariant re-litigation, no other edits |
| `docs/teachers/applicant-lifecycle.md` §6 | "Direct admin onboarding" consumer-table row gains a one-line link appended in-cell: `Canonical reference: docs/admin/cold-start-certification.md.` — sentence appended, row structure/columns unchanged |

Note: `docs/admin/cold-start-certification.md` is created by sibling task 7.1 — linked by path, not awaited (per task mandate).

## Verification

- `git diff -- docs/specs/` — **EMPTY before and after** both edits (zero diff on `docs/specs/open-decisions-and-gaps.md` and `docs/specs/state-machine-invariants.md`; this ticket mints no decisions/invariants).
- `git diff --stat` on the two edited docs: `user-management.md` +2 lines (blank + pointer), `applicant-lifecycle.md` 1 line changed — minimal, as mandated.
- 6.5-reconciliation-outcome.md and 5.3-endgates-outcome.md read for final state (no conflicting pointer conventions found; ledger/glyph gates unaffected — no deferred-items edit by this task).

## 7.2.QL — Sub-loop (expected .md no-op, recorded)

| File | Lifecycle | Result |
|---|---|---|
| `docs/admin/user-management.md` | `lint` | tsgo PASS (0 errors for file); oxlint stops with `No files found to lint ... 0 files` — expected no-op: markdown carries no lint coverage; exit 1 is tooling no-op, not a content diagnostic |
| `docs/teachers/applicant-lifecycle.md` | `lint` | Identical — tsgo PASS; oxlint no-op no-op on 0 files |

No ❌/⚠️ glyphs introduced anywhere by this task.

## Files changed by this task

- EDITED `docs/admin/user-management.md` (+1 pointer line at §6, after scope-split table)
- EDITED `docs/teachers/applicant-lifecycle.md` (§6 Direct-admin-onboarding row: +1 in-cell link)
- EDITED `ai/plans/sprint_3/dev3-018-cold-start-bootstrapping-direct-sheikh-c/tasks.md` (flipped 7.2 and 7.2.QL to `[x]` only)
- CREATED this outcome file

## Deviations

None.
