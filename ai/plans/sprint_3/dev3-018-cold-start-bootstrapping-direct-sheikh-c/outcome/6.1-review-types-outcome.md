# Wave A Outcome — review-types (TASK 6.1)

**Date:** 2026-09-02
**Reviewer role:** REVIEW subagent (read-only on source; this file is the only write)
**Scope files:** `backend/types/teachers/teacher.types.ts`, `shared/locale/types/errors/index.ts`,
`shared/locale/types/applicant/index.ts`, `shared/locale/en/errors/index.ts`,
`shared/locale/ar/errors/index.ts`, `shared/locale/en/applicant/index.ts`,
`shared/locale/ar/applicant/index.ts`
**Requirements:** REQ-002, REQ-003, REQ-051

---

## Verdict: CLEAN — 0 findings

Every checklist item verified against the live working tree, not just the prior outcomes.
`git status --short -- shared/locale backend/types/teachers` shows exactly the 7 scope files
modified (diff is additions-only: +26 lines across the six locale files + the type interface),
matching outcomes 1.1 / 1.2 with no scope creep.

### Checklist evidence

1. **Canonical placement (REQ-003)** — `TeacherColdStartCertificationInput` lives at
   `backend/types/teachers/teacher.types.ts:6-9`, in the types layer. Repo-wide symbol grep
   returns exactly ONE definition (the other two hits are the service's import at
   `backend/services/admin/cold-start-certification.service.ts:71` and its parameter annotation
   at `:164`). No service-layer `.types.ts` was created. Existing
   `TeacherSelectType`/`TeacherInsertType` (`teacher.types.ts:3-4`) byte-identical, as required.
   Barrel `backend/types/teachers/index.ts` already carries `export * from "./teacher.types";`
   (line 3) — zero barrel edit needed, as 1.1 predicted.

2. **Readonly contracts** — both fields declared `readonly`
   (`teacher.types.ts:7-8`), matching the sibling `*Input` idiom. Interface itself is not
   `readonly`-shaped beyond the fields, consistent with house style.

3. **Flat `ErrorsLabels` convention / no userNotFound duplication (REQ-051)** — the three new
   keys are FLAT top-level members of `ErrorsLabels`
   (`shared/locale/types/errors/index.ts:100-105`: `teacherAlreadyCertified`,
   `teacherRoleRequired`, `teacherAccountGoverned`), positioned after the pre-existing flat
   domain keys (`trialAlreadyGranted` at :99), mirroring the established
   `applicantNotFound`/`handshakeCodeInvalid` pattern. `userNotFound` exists exactly ONCE —
   inside the `adminUsers` group (`types/errors/index.ts:69`, `en/errors/index.ts:38`,
   `ar/errors/index.ts:38`); there is NO top-level flat `userNotFound` duplicate.

4. **No namespace procreation (REQ-051)** — the notification copy landed on the EXISTING
   `applicant` namespace (`types/applicant/index.ts:52-55`: `coldStartCertifiedTitle`,
   `coldStartCertifiedBody`). `shared/locale/types/message.ts` grep shows NO cold-start
   namespace entries — registration steps 3–5 inapplicable, as 1.2 recorded. The 2.5 outcome's
   consumer wiring confirms `getServerTranslations(...).applicantTranslations.coldStartCertified*`
   resolves.

5. **en/ar parity, genuine Arabic** — key sets verified identical by both parity suites (see
   below) AND by direct read: en ↔ ar mirrors at `en/errors/index.ts:52-54` ↔
   `ar/errors/index.ts:52-54` and `en/applicant/index.ts:18-20` ↔ `ar/applicant/index.ts:17-18`.
   The Arabic strings are genuine translations reusing the existing governance vocabulary
   (حذف/حظر/موقوف matching `accountDeleted`/`accountBlocked`/`accountSuspended` at
   `ar/errors/index.ts:17-19`); no transliterations, no untranslated English strings. No new ICU
   placeholders introduced — the parity suite's `{cooldownUntil}` pins stay intact.

6. **No next-intl / Translation-enum residue (REQ-002)** — all six locale files import ONLY
   `@/shared/locale/types/{errors,applicant}` (alias form per `shared/locale/AGENTS.md`); leaf
   modules are plain literals with zero logic. `teacher.types.ts` imports only the Drizzle schema
   type. No `next-intl`, `getBackendTranslations`, `shared/messages`, or `Translation` enum
   references in any scope file.

7. **`ErrorMessageKey` union auto-derivation intact** — the union is the mapped
   `keyof`-derivation at `shared/locale/types/errors/index.ts:108-110`; the three new flat keys
   join automatically with no manual edit (1.2's claim verified against the live source). Its
   consumer `frontend/providers/apollo/error-link.map.ts:27,174` imports the type unchanged.

8. **Consumers compiled in outcome/2.5 cross-checked (read-only)** —
   `backend/services/admin/cold-start-certification.service.ts` consumes exactly:
   `tErrors.adminUsers.userNotFound` (line 186 — REUSED group, per plan), flat
   `tErrors.teacherRoleRequired` / `tErrors.teacherAccountGoverned` / `tErrors.teacherAlreadyCertified`
   (lines 190, 194, 197), and `t.applicantTranslations.coldStartCertifiedTitle/Body`
   (lines 220-221). Codes ride `ConflictError(code, message)`; `NotFoundError("USER", …)` reuses
   `adminUsers.userNotFound`. The GraphQL surface (`backend/graphql/mutation/admin/admin-teachers.mutation.ts:62`)
   delegates to the service — no independent message sources.

### Test verification (commands + results)

| Suite | Command | Result |
|---|---|---|
| errors parity | `bun run test/scripts/run-test.ts shared/locale/errors-namespace.parity.test.ts` | **8 pass / 0 fail**, 84 expect() — incl. "identical sorted key sets across BOTH locale sources" |
| applicant parity | `bun run test/scripts/run-test.ts shared/locale/applicant-namespace.parity.test.ts` | **11 pass / 0 fail**, 112 expect() — incl. key-set equality, registry wiring, `applicantTranslations` bundle slice |

### Baseline discussion

No deviation from the recorded baseline vector (tsgo 0 / biome 0 / lint 0, outcome
0-baseline) was observed in-scope: the 1.1/1.2 outcomes each recorded `--lifecycle duplicates`
sub-loop exit 0 on every scope file, the parity suites re-verified green above, and the wave
introduced no new failures. No C1-C4 foreign-session issue intersects these files.

## Findings

None. No NEW or pre-existing issues in scope.

## Files written by this wave

- CREATED `ai/plans/sprint_3/dev3-018-cold-start-bootstrapping-direct-sheikh-c/outcome/6.1-review-types-outcome.md` (this file)

No checkbox flips performed (owned by the 6.5 reconciliation agent).
