# Task 3.2 Outcome — Codegen Regeneration + Frozen-Baseline Re-Pin (REQ-061)

**Date:** 2026-09-01
**Requirements:** REQ-061

---

## Summary

Ran `bun run generate:gqlSchema && bun codegen` against the tree carrying the task-3.1
mutation registration, then re-pinned BOTH frozen-baseline suites to the CURRENT live
inventory per plan §3.5's verification-first reconciliation rule. The live Mutation root
(14 fields) and Query root (14 fields) were extracted from the regenerated artifact
(`frontend/graphql/generated/schema.graphql`) and cross-checked against both test suites;
every baseline entry beyond the one new field is a justified delta for an ALREADY-SHIPPED
field (see "Justified baseline deltas"). Re-pin is additive-only, sorted under the suites'
`localeCompare` collation, and nothing was dropped silently.

Per protocol, regenerated artifacts stay in the working tree — NO git commit was run by
this task (the orchestrator owns commits).

## Commands & exit codes

| Command | Exit | Result |
|---|---|---|
| `bun run generate:gqlSchema` | 0 | `schema.graphql` written (11767 bytes) |
| `bun codegen` | 0 | `frontend/graphql/generated/gql/graphql.ts` regenerated — NO diff (the new operation has no frontend document yet; task 4.1 owns it) |
| `bun run test/scripts/run-test.ts backend/graphql/test/handshake-code-surface.test.ts` | 0 | 22 pass / 0 fail — frozen-six public allowlist UNCHANGED (`login`, `logout`, `refreshToken`, `registerUser`, `recitationReadings`, `_health`… untouched) |
| `bun run test/scripts/run-test.ts backend/graphql/test/plan-catalog.schema.test.ts` | 0 | 5 pass / 0 fail — committed-vs-live SDL BYTE-PARITY green |
| `bun run test/scripts/run-test.ts backend/graphql/test/schema-surface.test.ts` | 0 | 33 pass / 0 fail |
| `bun run test/scripts/run-test.ts backend/graphql/test/sdl-static-assertions.test.ts` | 0 | 18 pass / 0 fail |
| `bun run scripts/health/sub-loop.ts backend/graphql/test/sdl-static-assertions.test.ts --lifecycle duplicates` | 0 | tsgo → oxlint → biome → lint → jscpd all pass (jscpd: outside scan scope) |
| `bun run scripts/health/sub-loop.ts backend/graphql/test/schema-surface.test.ts --lifecycle duplicates` | 0 | all stages pass (jscpd: outside scan scope) |
| `bun run scripts/health/sub-loop.ts frontend/graphql/generated/gql/graphql.ts --lifecycle duplicates` | 1 | **sub-loop short-circuits at oxlint with "No files found to lint"** — tsgo passes (file is in tsconfig scope), but oxlint's ignore patterns exclude the generated dir entirely (0 files scanned). This is the "generated dirs ignored by linters" behavior the task anticipated — recorded, not an error in the artifact (see Deviation 2) |

## Regenerated-artifact diff (verbatim)

`git diff --stat -- frontend/graphql/generated`:

```
 frontend/graphql/generated/schema.graphql | 4 ++++
 1 file changed, 4 insertions(+)
```

`git diff -- frontend/graphql/generated/schema.graphql`:

```diff
@@ -224,6 +224,10 @@ type LogoutPayload {
 }
 
 type Mutation {
+  """
+  Admin-only cold-start certification (INV-TV1(b) / FR-3.9): promotes an existing teacher-role user to a certified founding Sheikh — inserts or elevates the teacher row (is_approved=true, is_evaluator per flag), finalizes any applicants row (passed, cooldown cleared), appends one override audit row, and notifies the teacher. Repeat calls answer TEACHER_ALREADY_CERTIFIED.
+  """
+  adminCertifyTeacherColdStart(makeEvaluator: Boolean = true, userId: Int!): AdminUserDetail!
   adminCreateUser(input: AdminCreateUserInput!): AdminUserDetail!
```

Exactly the +4-line block task 3.1's transient run predicted; ZERO other artifact churn
(`frontend/graphql/generated/gql/*` byte-stable).

## Baseline re-pins (before → after)

### `backend/graphql/test/sdl-static-assertions.test.ts`

- `FROZEN_MUTATION_FIELDS`: **7 → 14**. Added: `adminCertifyTeacherColdStart` (this
  change set), plus `adminCreateUser`, `adminSetUserDeleted`, `adminUpdateUser`,
  `createPlan`, `setPlanActiveStatus`, `updatePlan` (already-shipped drift reconciliation).
- `FROZEN_QUERY_FIELDS`: **6 → 14**. Added: `adminPlans`, `adminUserActivity`,
  `adminUserDetail`, `adminUserStats`, `adminUsers`, `findStudentByHandshakeCode`,
  `myHandshakeCode`, `planCatalog` (all already-shipped drift reconciliation).
- Test-name/comment text updated to describe the re-pinned 14-op baseline (no behavioral
  assertion changes beyond the arrays and the Deviation-1 lexical narrowing).

### `backend/graphql/test/schema-surface.test.ts`

- `PRE_3_1_MUTATION_FIELDS`: **10 → 14**. Added: `adminCertifyTeacherColdStart`,
  `adminCreateUser`, `adminSetUserDeleted`, `adminUpdateUser`.
- `PRE_3_1_QUERY_FIELDS`: **7 → 11**. Added: `adminUserActivity`, `adminUserDetail`,
  `adminUserStats`, `adminUsers`. The probe-additions expectation stays EXACTLY
  `["_health", "findStudentByHandshakeCode", "myHandshakeCode"]` — the admin directory
  reads are baseline-enrolled, not additions.
- `PRE_3_1_ENUMS`: **7 → 9**. Added: `AdminUserGovernanceFilter`, `AuditActionType`
  (shipped with the admin user-management surface).
- `PRE_3_1_TYPE_NAMES`: **21 → 34**. Added: `AdminCreateUserInput`, `AdminUpdateUserInput`,
  `AdminUserFiltersInput`, `AdminParentSnapshot`, `AdminStudentSnapshot`,
  `AdminTeacherSnapshot`, `AdminUserActivityEntry`, `AdminUserDetail`,
  `AdminUserListItem`, `AdminUserPage`, `AdminUserStats`, `AdminUserGovernanceFilter`,
  `AuditActionType`. The whole-schema additions delta stays EXACTLY
  `["DateTime", "HandshakeCodeLookup", "HealthCheck"]` — the new mutation returns the
  pre-existing `AdminUserDetail` type, so it introduces ZERO new named types.

## Justified baseline deltas (each entry an already-shipped field, enrolled now)

| Baseline entry | Shipped by | Evidence |
|---|---|---|
| `adminCreateUser` / `adminUpdateUser` / `adminSetUserDeleted` | Admin user-management surface (DEV3-016 lineage) | present in HEAD's committed `schema.graphql` Mutation root |
| `createPlan` / `updatePlan` / `setPlanActiveStatus` | Plan-catalog surface (DEV1-005 lineage) | present in HEAD SDL; pinned by `plan-catalog.schema.test.ts` |
| `adminPlans`, `planCatalog` (query) | Plan-catalog surface | present in HEAD SDL |
| `adminUsers`, `adminUserDetail`, `adminUserStats`, `adminUserActivity` (+ their types/enums: `AdminUser*`, `AdminUserGovernanceFilter`, `AuditActionType`) | Admin user-management directory/audit surface | present in HEAD SDL Query root |
| `findStudentByHandshakeCode`, `myHandshakeCode` (into the artifact-side `FROZEN_QUERY_FIELDS`) | Handshake-code query surface | present in HEAD SDL; locked by `handshake-code-surface.test.ts` |
| `adminCertifyTeacherColdStart` | THIS change set (task 3.1 registration) | +4-line artifact diff above |

HEAD verification: `git show HEAD:frontend/graphql/generated/schema.graphql` already
carried 13 Mutation / 14 Query fields — the frozen arrays were measurably stale at HEAD
(the 0.2 drift register's flags), so the re-pin reconciles tests to shipped reality rather
than shrinking anything.

## Deviation 1 — lexical `Subscription` guard narrowed to the root-type token

`sdl-static-assertions.test.ts` had a belt-and-braces lexical check
`expect(sdlText).not.toContain("Subscription")`. The LIVE artifact legitimately contains
`hasActiveSubscription: Boolean!` (`AdminStudentSnapshot`) and
`studentHasActiveSubscription: Boolean` (`AdminUserListItem`) — shipped snapshot fields —
so the broad substring check false-positives (it was already red against the committed
HEAD artifact for this reason, independent of this task). The structural assertion (NO
`Subscription` object type among AST definitions) is unchanged; the lexical check is
narrowed to the definition token: `expect(sdlText).not.toContain("type Subscription")`,
with an inline comment explaining the narrowing. The invariant (no Subscription root —
realtime is the WebSocket sidecar's contract) is preserved in full.

## Deviation 2 — sub-loop on the regenerated artifact

Sub-loop on `frontend/graphql/generated/gql/graphql.ts`: tsgo stage passes, but oxlint
exits 1 with "No files found to lint … 0 files" — the project's oxlint ignore patterns
exclude the generated directory, so the sub-loop short-circuits before biome/lint. This is
the anticipated "linters are configured to ignore generated dirs" behavior; recorded as
environmental, not a defect in the artifact. Generated artifacts require no lint hygiene
by project convention.

## 3.2.SR — Semantic Review

- Re-pin is ADDITIVE and SORTED: every array grew monotonically; sort order matches the
  suites' `localeCompare` collation (verified by the suites themselves — both compare
  against `toSorted(localeCompare)` output; e.g. `adminUsers` < `adminUserStats`, caught
  and fixed by the first red run).
- NOTHING dropped: every pre-existing baseline entry survives; probe field `_health` is
  still asserted NOT to be a mutation; the handshake-code surface and public-allowlist
  suites are green UNTOUCHED (frozen-six allowlist intact — this op is not public).
- Generated artifacts left in the working tree per protocol; NO `git commit` run by this
  task (orchestrator owns the commit).
- Comments/headers updated where they described the old counts (no ❌/⚠️ glyphs, no
  REQ/task refs in CODE comments).

Live inventories, for the record (extracted from the regenerated SDL):

- Mutation (14): `adminCertifyTeacherColdStart`, `adminCreateUser`, `adminSetUserDeleted`,
  `adminUpdateUser`, `createPlan`, `login`, `logout`, `markAllNotificationsRead`,
  `markNotificationRead`, `refreshToken`, `registerUser`, `setPlanActiveStatus`,
  `updateMyLocale`, `updatePlan`
- Query (14): `_health`, `adminPlans`, `adminUserActivity`, `adminUserDetail`,
  `adminUserStats`, `adminUsers`, `findStudentByHandshakeCode`, `me`,
  `myApplicantProfile`, `myHandshakeCode`, `myNotifications`,
  `myUnreadNotificationCount`, `planCatalog`, `recitationReadings`

## Files changed

- `frontend/graphql/generated/schema.graphql` — regenerated (+4 lines, verbatim above).
- `frontend/graphql/generated/gql/graphql.ts` — re-emitted by codegen, byte-identical (no diff).
- `backend/graphql/test/sdl-static-assertions.test.ts` — baselines re-pinned
  (`FROZEN_MUTATION_FIELDS` 7→14, `FROZEN_QUERY_FIELDS` 6→14), header/test-name text
  refreshed, lexical Subscription guard narrowed (Deviation 1).
- `backend/graphql/test/schema-surface.test.ts` — baselines re-pinned
  (`PRE_3_1_MUTATION_FIELDS` 10→14, `PRE_3_1_QUERY_FIELDS` 7→11, `PRE_3_1_ENUMS` 7→9,
  `PRE_3_1_TYPE_NAMES` 21→34), baseline-history comment refreshed.

No UI/E2E runners executed (session policy). `backend/db/schema/` and
`backend/db/migration/` untouched.
