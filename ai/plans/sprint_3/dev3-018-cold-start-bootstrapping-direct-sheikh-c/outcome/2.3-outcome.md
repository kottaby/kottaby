# Task 2.3 Outcome — ApplicantRepository.finalizeOnCertification (ADDITIVE)

**Date:** 2026-09-01
**Requirements:** REQ-012, REQ-044, REQ-070

---

## Summary

Added `ApplicantRepository.finalizeOnCertification(userId, tx?): Promise<boolean>` to
`backend/db/repo/teachers/applicant.repository.ts` as a pure append (one import line + one new
namespace method). It issues exactly ONE parameterized `UPDATE applicants SET status =
ApplicantStatus.Passed, cooldown_until = NULL, updated_at = now() WHERE id = $1 RETURNING id`,
returning `true` iff RETURNING produced a row. The write is unconditional on prior
status/cooldown (supersede-on-certification); `verification_attempts` / `last_attempt_at` are
never in the SET clause; the row is never deleted. A new 7-test suite
`backend/db/repo/teachers/applicant.finalize.test.ts` covers the mandated matrix; all existing
applicant suites re-ran unchanged-green.

## Files Changed

- MODIFIED `backend/db/repo/teachers/applicant.repository.ts`
  - +1 import: `import { ApplicantStatus } from "@/backend/enum";` (highest available barrel; VALUE
    import used in the runtime SET clause).
  - +1 method appended inside the `ApplicantRepository` namespace: `finalizeOnCertification`
    (tx branch then standalone `db` branch, mirroring `recordVerificationAttempt`'s executor
    discipline; no prepared statement — writes excluded per `docs/drizzle/prepared-statements.md`).
- CREATED `backend/db/repo/teachers/applicant.finalize.test.ts` (7 tests, all inside
  `runInRollback`, `tx` passed to every call, fixtures only via `createTestUser` /
  `createTestApplicant`, independent read-back oracle via direct Drizzle select).
- CREATED `ai/plans/sprint_3/dev3-018-cold-start-bootstrapping-direct-sheikh-c/outcome/2.3-outcome.md` (this file)
- MODIFIED `tasks.md` — flipped ONLY the 2.3 / 2.3.QL / 2.3.TE / 2.3.SEC / 2.3.SR / 2.3.IV lines.

## Files Deliberately NOT Changed

- Nothing under `backend/db/schema/` or `backend/db/migration/` — zero-drift gate; `git diff
  --stat` on those paths is EMPTY (exit 0).
- `backend/db/test/logic/teachers/applicant-lifecycle.test.ts` — the existing suite; untouched,
  re-run green.
- `backend/db/test/entity-setup.ts` — `createTestApplicant` already accepts the needed overrides;
  no fixture changes required.
- `backend/db/repo/teachers/index.ts`, `teacher.repository.ts`, `backend/services/admin/*`,
  locale/type files — in-flight files of concurrent Wave-2 subagents; never edited.
- `deferred-items.md` — no new rows needed (no blockers encountered; zero ❌/⚠️ glyph obligation).

## Verification

| Command | Result |
|---|---|
| `bun run scripts/health/sub-loop.ts backend/db/repo/teachers/applicant.repository.ts --lifecycle duplicates` | **exit 0** — tsgo ✅ oxlint ✅ biome ✅ lint:type-aware ✅ duplicates ✅ (2.3.QL) |
| `bun run scripts/health/sub-loop.ts backend/db/repo/teachers/applicant.finalize.test.ts --lifecycle duplicates` | **exit 0** — all stages ✅ (duplicates skipped: outside jscpd scope) |
| `bun run test/scripts/run-test.ts backend/db/repo/teachers/applicant.finalize.test.ts` | **7 pass / 0 fail**, 34 expects |
| `bun run test/scripts/run-test.ts backend/db/repo/teachers` (SR suite) | **7 pass / 0 fail** (same file; only test file in that dir) |
| `bun run test/scripts/run-test.ts backend/db/test/logic/teachers/applicant-lifecycle.test.ts` (pre-existing suite, unchanged) | **10 pass / 0 fail**, 38 expects |
| `bun run test/scripts/run-test.ts backend/enum/teachers/applicant-status.enum.test.ts` | **21 pass / 0 fail** |
| `git diff --stat -- backend/db/schema/ backend/db/migration/` | empty (exit 0) |
| `git diff backend/db/repo/teachers/applicant.repository.ts` | purely additive (+50 lines: 1 import, 1 method; zero hunks touching existing code) |

First sub-loop pass surfaced 2 `sonarjs/different-types-comparison` errors (`row !== undefined`
both branches); fixed by `rows.length > 0` on the RETURNING array; re-run exit 0.

## Test Matrix Mapping (REQ-070 / REQ-012 / REQ-044)

- pending → passed + cooldown null, `updated_at` stamped (Tier 1) ✅
- absent row → `false`, no row created, no side effects (Tier 1) ✅
- failed + ACTIVE future cooldown → passed + cooldown cleared in the same statement (supersede,
  Tier 2) ✅
- in_evaluation → passed (Tier 2) ✅
- double finalize → `true` both times, terminal state unchanged (chaos, Tier 3) ✅
- `verificationAttempts`/`lastAttemptAt` byte-identical before/after; row never deleted (Tier 4) ✅
- tenancy: finalize on user A leaves user B byte-identical incl. `updated_at` (Tier 4) ✅

## 2.3.SEC Audit

- SET clause writes exactly `status`, `cooldownUntil`, `updatedAt` — nothing else.
- `id` bound via `eq(applicants.id, userId)` — Drizzle-parameterized, never concatenated.
- No business logic in the repo (supersede semantics are the data contract; gating is the
  service's job per layer separation).

## 2.3.SR Self-Review

- No read-then-write (single atomic UPDATE); no `R.reject`/`rejects.toThrow` in tests; no
  module-level mutable state; enum as VALUE import; no `console.*`; no cross-layer or plan
  meta-artifact comments; existing methods untouched (additive-only diff proves zero drift);
  existing suites green post-change.

## 2.3.IV

`.agents/instructions/backend.instructions.md` validated and followed: barrel import
(`@/backend/enum` highest barrel), types from `@/backend/types`, `tx` last param, write without
prepared statement, no `inArray`+placeholder, no hardcoded user-facing strings, no `console.*`.

## Cross-File Dependencies

- Task 2.5 (`ColdStartCertificationService`) consumes `ApplicantRepository.finalizeOnCertification`;
  its row-absent branch (audit `applicantRow: "absent"`) keys on the `false` return — contract delivered.
- None blocking for other wave files.

## Deviations

- Task text suggested `create applicant.finalize.test.ts "if needed"` under "extend the existing
  applicant repo test file" — the existing applicant suite lives at
  `backend/db/test/logic/teachers/applicant-lifecycle.test.ts`; per the assignment's explicit
  directive a SEPARATE file `backend/db/repo/teachers/applicant.finalize.test.ts` was created and
  the existing file was not touched (it was run as the regression lock and is unchanged-green).
- Both branches of `finalizeOnCertification` return via `rows.length > 0` instead of a boolean
  cast on the destructured row — required by `sonarjs/different-types-comparison` (see
  Verification).
