# Task 7.3 Outcome — AGENTS.md Propagation (Rule Lines Only)

**Date:** 2026-09-02 · **Requirements:** REQ-082

Rule-line-only propagation of the DEV3-018 cold-start certification single-writer contract into the three existing AGENTS.md files. No code, no schema, no new files outside this outcome.

## Edits (one rule line per file)

| File | Line added (anchor) | Content summary |
|---|---|---|
| `backend/services/AGENTS.md` | inserted immediately before the `**Audit-emission rule: ...**` line (directly after the Admin user-management rule) | `ColdStartCertificationService.certifyTeacherColdStart` (`backend/services/admin/cold-start-certification.service.ts`) is the single writer of the certified-state (`is_approved=true`) path outside the evaluation loop; governed by `docs/admin/cold-start-certification.md` |
| `backend/db/repo/AGENTS.md` | inserted immediately before the `**Neon HTTP Client for Bare Reads (CRITICAL)**` line (top of the Rules list) | `TeacherRepository` (`backend/db/repo/teachers/teacher.repository.ts`) is the teachers-domain governed-write repo — `insertColdStartCertified` + guarded `elevateToCertified` ONLY; no other teacher-table writers |
| `AGENTS.md` (root) | appended at the end of the "Important References" list, after `docs/students/free-trial-provisioning.md` | one-liner pointing to `docs/admin/cold-start-certification.md` (DEV3-018 cold-start bootstrapping canonical reference: `certifyTeacherColdStart` single-writer contract, guarded `TeacherRepository` insert/elevate writes, admin-gate + audit-emission contract) |

Placement matched existing conventions: the services line sits exactly where the Admin user-management / single-writer / audit-emission rule lines live; the repo line opens the Rules section alongside the other domain-repo rules; the root line follows the canonical-reference one-liner style (`— Canonical reference (...)`). The machine-managed `nextjs-agent-rules` block at the end of root `AGENTS.md` was NOT touched.

Exact method names (`certifyTeacherColdStart` at `cold-start-certification.service.ts:155/162`; `insertColdStartCertified` / `elevateToCertified` in `teacher.repository.ts:85/118`) verified against the source tree before writing.

## 7.3.QL — Quality Loop

`bun run scripts/health/sub-loop.ts <file> --lifecycle lint` on each of the three edited files:

- tsgo: **pass** for all three (no errors).
- oxlint: **no-op** — `No files found to lint` (0 files matched; markdown is outside every lint glob). This is the expected no-op result for `.md` targets; no diagnostics were produced and none were fixable. Biome/lint/duplicates stages not reached by design (progressive short-circuit), and equally no-ops for `.md`.

## 7.3.IV — Instruction Verification

- All three edited files pre-exist in-tree (verified via post-edit grep anchors; each new line present exactly once).
- No new instruction or AGENTS.md files were created; only existing files listed in the task were modified.

## Files changed by this task

- EDITED `backend/services/AGENTS.md` (+1 rule line)
- EDITED `backend/db/repo/AGENTS.md` (+1 rule line)
- EDITED root `AGENTS.md` (+1 Important References line; nextjs-agent-rules block untouched)
- EDITED `ai/plans/sprint_3/dev3-018-cold-start-bootstrapping-direct-sheikh-c/tasks.md` (checkbox flips: 7.3, 7.3.QL, 7.3.IV → `[x]`)
- CREATED `ai/plans/sprint_3/dev3-018-cold-start-bootstrapping-direct-sheikh-c/outcome/7.3-outcome.md` (this file)

## Deviations

None. No tests required (docs-only); no git mutations performed.
