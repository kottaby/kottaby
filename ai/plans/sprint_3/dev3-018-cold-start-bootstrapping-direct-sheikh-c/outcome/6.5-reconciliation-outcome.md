# Task 6.5 Outcome — Reconciliation + Deferred-Items Final Check

**Date:** 2026-09-02 · **Requirements:** REQ-076
**Inputs consumed:** `outcome/6.1-review-types-outcome.md` (CLEAN), `outcome/6.2-review-backend-outcome.md` (B-1/B-2/B-3), `outcome/6.3-review-frontend-outcome.md` (F-1/F-2), `outcome/6.4-pentester-outcome.md` (F-D1..F-D4).

---

## 1. Findings disposition

### FIXED (with re-verification evidence)

| Finding | Fix | Evidence |
|---|---|---|
| **B-2** — `logCertificationDenial` entity vocabulary drifted from the gate convention (`"users"` / `"teacher"` vs the documented `"user"`) | `backend/services/admin/cold-start-certification.service.ts:82` — single-token normalization to `entity: "user"` (matches `admin-gate.helpers.ts:65,75,85,113,122,131` and the DEV3-016 service convention); the one pinning assertion in `cold-start-certification.service.test.ts:534` updated to `"user"` (value-only change; assertion shape/count/logic untouched) | Service suite `28 pass / 0 fail` (168 expects); chaos `9 / 0` (160); gate suite `10 / 0` (33); sub-loop `--lifecycle duplicates` exit 0 on service + test |
| **F-D3** — `TEACHER_ROLE_REQUIRED` denied against a non-teacher target logged `entity: "teacher"` | Same edit as B-2 (both findings collapse onto the single `entity` expression; every denial now logs `entity: "user"` — the target IS a `users` row regardless of role mismatch) | Same as B-2 |
| **F-1** — contract test pinned variable NAMES only; types and the `makeEvaluator = true` default unpinned (regression-detection gap, REQ-062) | `frontend/graphql/sharedDocuments/admin/teacher-certification.documents.test.ts` — the variables test now also pins, from the AST, `userId: NonNullType(NamedType Int)`, `makeEvaluator: NamedType Boolean`, and `defaultValue: { kind: "BooleanValue", value: true }`. All 7 pre-existing tests and all prior assertions kept (names assertion retained verbatim; +4 expects) | Contract suite `7 pass / 0 fail` (21 expects, up from 17); sub-loop `--lifecycle duplicates` exit 0 |

### CARRIED (RESOLVED-REFERENCE rows added to `deferred-items.md`)

| Finding | Ledger row | Owner | Rationale (one line) |
|---|---|---|---|
| B-1 — plain `new Error` guards at `service.ts:99/:152` | `D-B1-PLAIN-ERROR` | Error-handling contract stream | Unreachable-by-design internal guards; masking to `INTERNAL_SERVER_ERROR` is desired; REQ-050's closed-set ruling forbids a new subclass (higher-priority instruction) |
| B-3 — governance re-read null-tolerant micro-window | `D-GATE-NULL-READ` | Governance-mutation follow-up | Non-exploitable in production (no users hard-delete surface; INV-U1 preserves rows); test-harness-only reachability |
| F-2 — `sharedDocuments/AGENTS.md` Layout omits `admin/` subdir | `D-DOCS-SHAREDDOCS-LAYOUT` | Frontend GraphQL doc-hygiene pass | Pre-existing DEV3-016 lineage; this ticket adds a file to an existing directory, so the AGENTS "new sub-directory" step does not apply |
| F-D1 — gate double-read micro TOCTOU window | `D-GATE-DOUBLE-READ` | Gate-consolidation / governance-mutation follow-up (D-GATE-LANDED lineage) | Forced by byte-parity-locked `assertActorAdmin` (2.4 Deviation 2); `audit_logs.actor_id` FK forces rollback on mid-window hard-delete |
| F-D2 — VALIDATION denials carry no domain log | `D-VALIDATION-LOG` | Rate-limiting hardening stream (D-RATE-LIMIT owner) | Deliberate DEV3-016 parity (2.5 Deviation 1); pre-DB denial, zero DB contact; observability decision belongs with the limiter decision |
| F-D4 — finalizer doc anchor drift (`route.ts` vs actual `apollo-server.ts:44`) | `D-GQL-DOC-ANCHOR` | GraphQL docs stream | Pre-existing/environmental; the guarded invariant (exactly ONE registration) holds — only the anchor text is stale |

**Ledger gate:** `grep -cE "❌|⚠️" ai/plans/sprint_3/dev3-018-cold-start-bootstrapping-direct-sheikh-c/deferred-items.md` → **0** (re-verified after adding the six rows).

## 2. Full re-sweep (Phase D, after fixes)

| Suite | Command | Result |
|---|---|---|
| Admin services (incl. cold-start service 28 + chaos 9 + gate 10) | `bun run test/scripts/run-test.ts backend/services/admin` | 115 pass / 1 fail — the 1 fail is `listDirectory` happy path = ledgered foreign **D-C1** (shared-DB data-volume), NOT DEV3-018 |
| Teacher repos | `bun run test/scripts/run-test.ts backend/db/repo/teachers` | **17 pass / 0 fail** (88 expects) |
| GraphQL wire | `bun run test/scripts/run-test.ts backend/graphql/test` | 210 pass / 6 fail — 4 = plan-catalog role-matrix (ledgered **D-C2**), 2 = notification wire `beforeAll` seeded-admin login (ledgered **D-C3**); all DEV3-018 wire probes (incl. `admin-teachers.mutation.test.ts` 19) green |
| Locale parity | `bun run test/scripts/run-test.ts shared/locale` | 95 pass / 2 fail — both = `handshakeCode-namespace.parity` on the nested `errors.planCatalog` group (ledgered **D-C4**); DEV3-018's errors/applicant suites green |
| Frontend GraphQL | `bun run test/scripts/run-test.ts frontend/graphql` | **177 pass / 0 fail** (1052 expects) |
| Journey workflow | `bun run test/scripts/run-test.ts test/workflows/admin/cold-start-certification.journey.test.ts` | **12 pass / 0 fail** (193 expects) |
| Type check | `bun tsgo` | exit **0** |
| Schema drift | `git diff --name-only -- backend/db/schema/ backend/db/migration/` | **EMPTY** (zero drift) |

Every failure in the sweep maps to an existing ledgered D-C* row; no new failures introduced by the fix-forward edits. No `test:ui*` runners touched; no raw `bun test` used (session policy honored).

## 3. Hygiene confirmations

- **No new `console.*`**: grep over the three edited files → zero matches.
- **Zero schema/migration drift**: diff empty (above).
- **Glyph gate 0**: ledger re-checked after row insertion.

## Files changed by this task

- EDITED `backend/services/admin/cold-start-certification.service.ts` (B-2/F-D3: `entity: "user"` normalization at `:82`)
- EDITED `backend/services/admin/cold-start-certification.service.test.ts` (`:534` assertion value `"teacher"` → `"user"`)
- EDITED `frontend/graphql/sharedDocuments/admin/teacher-certification.documents.test.ts` (F-1: variables test now pins types + `Boolean = true` default; header comment extended)
- EDITED `ai/plans/sprint_3/dev3-018-cold-start-bootstrapping-direct-sheikh-c/deferred-items.md` (+6 RESOLVED-REFERENCE rows: D-B1-PLAIN-ERROR, D-GATE-NULL-READ, D-GATE-DOUBLE-READ, D-VALIDATION-LOG, D-DOCS-SHAREDDOCS-LAYOUT, D-GQL-DOC-ANCHOR)
- EDITED `ai/plans/sprint_3/dev3-018-cold-start-bootstrapping-direct-sheikh-c/tasks.md` (checkbox flips: 6.1, 6.2, 6.3, 6.4, 6.5 → `[x]`)
- CREATED `ai/plans/sprint_3/dev3-018-cold-start-bootstrapping-direct-sheikh-c/outcome/6.5-reconciliation-outcome.md` (this file)
