# Task 4.1 Outcome — `adminCertifyTeacherColdStartMutationDocument` (Document + Barrel + Contract Test)

**Date:** 2026-09-01
**Requirements:** REQ-062, REQ-063, REQ-064

---

## Summary

Created the frontend Apollo document `adminCertifyTeacherColdStartMutationDocument` exactly per
plan.md §5.4 — a single `TypedDocumentNode<AdminCertifyTeacherColdStartMutation,
AdminCertifyTeacherColdStartMutationVariables>` constant emitting the named mutation
`AdminCertifyTeacherColdStart($userId: Int!, $makeEvaluator: Boolean = true)` against the backend
field `adminCertifyTeacherColdStart`, with selection order `id` FIRST then `role`, `isDeleted`,
`suspended`, `isBlocked`, `applicant { id status }`, `teacher { isApproved isEvaluator isOnline
averageRating }`. Imports are `gql` + `type TypedDocumentNode` from `@apollo/client` and the two
generated operation types from `@/frontend/graphql/generated/gql/graphql` — mirroring the sibling
`admin-users.documents.ts` import pattern verbatim.

### Codegen sequencing (document-first → regenerate)

1. Created the document file (types did not yet exist).
2. Ran `bun codegen` — exit 0. The regen diff on `frontend/graphql/generated/gql/graphql.ts` is
   exactly **+15 lines**: the extracted payload/snapshot types
   (`AdminCertifyTeacherColdStartMutation_adminCertifyTeacherColdStart(_applicant|_teacher)`),
   `AdminCertifyTeacherColdStartMutation`, `AdminCertifyTeacherColdStartMutationVariables`, and the
   compiled `AdminCertifyTeacherColdStartDocument` AST. No other operation was touched;
   `schema.graphql` shows +4 lines already attributed to task 3.2's regen.
3. Verified the exports exist (`graphql.ts:228-241`, document at `:407`).
4. Authored the contract test against the now-present types.

The admin barrel gained exactly ONE line: `export * from "./teacher-certification.documents";`
(after `./admin-users.documents` — the sub-directory barrel convention from
`frontend/graphql/sharedDocuments/AGENTS.md`).

## Files Changed

- CREATED `frontend/graphql/sharedDocuments/admin/teacher-certification.documents.ts` (~50 lines:
  header JSDoc + the two imports + one typed `gql` document).
- MODIFIED `frontend/graphql/sharedDocuments/admin/index.ts` — +1 line
  (`export * from "./teacher-certification.documents";`).
- CREATED `frontend/graphql/sharedDocuments/admin/teacher-certification.documents.test.ts`
  (7 tests) — pins (a) single named mutation `AdminCertifyTeacherColdStart`, (b) exact declared
  variable set `{userId, makeEvaluator}` in source order, (c) payload root field
  `adminCertifyTeacherColdStart` (operation-name↔backend-field contract), (d) `id`-first payload
  selection order with full field-order pin, (e) `applicant` selects `["id", "status"]` and
  `teacher` selects `["isApproved", "isEvaluator", "isOnline", "averageRating"]`, (f) barrel
  identity — domain barrel AND top-level barrel resolve to the SAME instance (cache-key safety,
  precedent `sharedDocuments/documents.contract.test.ts`), (g) compile-time
  `TypedDocumentNode` assignment proof with `.loc` runtime binding. All kind-narrowing via
  string-literal `kind === "…"` guards (oxlint `no-unsafe-type-assertion` clean).
- REGENERATED `frontend/graphql/generated/gql/graphql.ts` (+15 lines, recorded above).
- MODIFIED `ai/plans/sprint_3/dev3-018-cold-start-bootstrapping-direct-sheikh-c/tasks.md` — flipped
  ONLY the 4.1 / 4.1.QL / 4.1.TE / 4.1.BF / 4.1.BS / 4.1.SR / 4.1.IV checkbox lines.
- CREATED this outcome file.

## Files Deliberately NOT Changed

- `frontend/graphql/sharedDocuments/index.ts` — already re-exports `./admin` at line 1; the
  barrel-identity test proves the top-level path resolves to the same instance.
- `frontend/providers/apollo/apolloCache.ts` — `AdminUserDetail` is an identity type (`id` leads
  the payload); no `keyFields: false` entry is required (plan §5.4 note). Untouched.
- `frontend/views/dashboard/navItems.ts`, every page/view/component — no UI whatsoever.
- `backend/lib/gateway/public-operations.ts` — operation stays default-deny (not public).
- Nothing under `backend/db/schema/` or `backend/db/migration/` (zero-drift gate).

## Verification

| Check | Command | Exit | Result |
|---|---|---|---|
| Codegen regen | `bun codegen` | **0** | +15-line generated diff; new op types exported |
| Contract test | `bun run test/scripts/run-test.ts frontend/graphql/sharedDocuments/admin/teacher-certification.documents.test.ts` | **0** | **7 pass / 0 fail**, 17 expect() calls |
| Sub-loop (document) | `bun run scripts/health/sub-loop.ts frontend/graphql/sharedDocuments/admin/teacher-certification.documents.ts --lifecycle duplicates` | **0** | tsgo → oxlint → biome → lint:type-aware → jscpd all pass |
| Sub-loop (barrel) | `bun run scripts/health/sub-loop.ts frontend/graphql/sharedDocuments/admin/index.ts --lifecycle duplicates` | **0** | all stages pass |
| Sub-loop (test) | `bun run scripts/health/sub-loop.ts frontend/graphql/sharedDocuments/admin/teacher-certification.documents.test.ts --lifecycle duplicates` | **0** | all stages pass |
| Duplicate doc probe (before) | `grep adminCertifyTeacherColdStart frontend/graphql` | — | only `generated/schema.graphql` (SDL field) — no pre-existing document |
| Duplicate doc probe (after) | `grep adminCertifyTeacherColdStart frontend/graphql` | — | matches confined to the new document file, its test, and `generated/**` — exactly one authored document |

No UI/E2E runners executed (session policy).

## 4.1.BF — Agent-Browser Functional Self-Loop (plan-declared N/A, verbatim rationale)

"N/A (explicit, recorded): REQ-063 ships ZERO markup; no page exists to drive. Rationale recorded
in `outcome/4.1-outcome.md`. The D-UI follow-up owns both browser loops." — recorded here per
tasks.md; flipped as plan-declared N/A-complete.

## 4.1.BS — Agent-Browser Visual Self-Loop (plan-declared N/A, verbatim rationale)

"N/A (explicit, recorded): no UI renders; D-UI forward constraint REQ-064 carries the
MUI v9/RTL/touch-target rules to the owning ticket." — recorded here per tasks.md; flipped as
plan-declared N/A-complete.

## 4.1.SR — Semantic Review

- Generated-type imports ONLY (`AdminCertifyTeacherColdStartMutation`,
  `AdminCertifyTeacherColdStartMutationVariables` from `@/frontend/graphql/generated/gql/graphql`);
  no inline type literals, no mapping layers, no schema-level types.
- Operation name `AdminCertifyTeacherColdStart` ↔ backend field `adminCertifyTeacherColdStart`
  matches the 3.1 registration (SDL block pinned by `schema-surface.test.ts`, baseline-repinned by
  3.2).
- `id` selected FIRST in the payload and in `applicant` — Apollo normalization contract
  (`sharedDocuments/AGENTS.md` §`id` Field Requirement).
- Registered through the domain barrel (`export * from "./teacher-certification.documents";`) and
  transitively through the top-level barrel; the identity test pins all three import paths to one
  instance.
- No duplicate document of this operation anywhere (grep evidence above).
- No `console.*`, no user-facing strings, no REQ/task refs in the document file's comments/JSDoc.
- `apolloCache.ts` untouched — `AdminUserDetail` is an identity type.

## 4.1.IV — Instruction Verification

`.agents/instructions/frontend.instructions.md` + `frontend/graphql/AGENTS.md` +
`frontend/graphql/sharedDocuments/AGENTS.md` validated against the final files:

| Rule | Verdict |
|---|---|
| Documents in `sharedDocuments/<domain>.documents.ts`, barrel-wired (file → domain `index.ts` → top-level) | PASS — new domain `admin` file + one barrel line; top barrel untouched (already re-exports `./admin`) |
| `gql` + `TypedDocumentNode` from `@apollo/client` (never `…/core`); hooks from `@apollo/client/react` | PASS — document imports match verbatim; no hooks used (document-only task) |
| TypedDocumentNode naming convention `<operation>MutationDocument` ↔ `TypedDocumentNode<{Op}Mutation, {Op}MutationVariables>` | PASS — `adminCertifyTeacherColdStartMutationDocument` ↔ `AdminCertifyTeacherColdStartMutation(/Variables)` |
| `id` on every object selection (cache normalization) | PASS — `id` first in payload; `applicant { id status }` |
| NO `useLazyQuery`; NO inline type literals; NO schema-types; codegen types only | PASS — none present |
| Codegen after document changes (`bun run generate:gqlSchema` + `bun codegen`) | PARTIAL BY DESIGN — task 3.2 already regenerated the SDL (`bun run generate:gqlSchema`, schema byte-stable since); this task ran `bun codegen` only, per its own document-first sequencing (backend schema unchanged in this task) |
| No `console.*`; no hardcoded user-facing strings | PASS |

## Cross-File Dependencies

- None blocking. The D-UI follow-up consumes `adminCertifyTeacherColdStartMutationDocument` via
  `@/frontend/graphql/sharedDocuments` and `useMutation` from `@apollo/client/react` — documented
  in the plan §5.4 hook note.
- The generated artifacts remain uncommitted in the working tree alongside task 3.1/3.2's changes;
  the orchestrator owns the commit.

## Deviations

- None. The document matches plan.md §5.4 byte-for-byte (modulo the file header, which follows the
  `admin-users.documents.ts` header convention). The contract test uses the
  `documents.contract.test.ts` assertion-free kind-guard helpers shape (string-literal
  discriminated narrowing) rather than `graphql`'s `Kind` enums — both precedents exist in-tree;
  this avoids `no-unsafe-type-assertion` without any cast.
