# Task 5.2 Outcome — Cross-Entity Purity Oracle + Devil's-Advocate Differential Sweep

**Date:** 2026-09-02 (started 2026-09-01 ~23:53)
**Requirements:** REQ-020, REQ-070..076
**Status:** COMPLETE — purity oracle green; DEV3-018-attributable failure FIXED; three foreign-stream
CRITICAL findings recorded below (nothing weakened, nothing silenced).

---

## 1. Purity oracle (the changed test)

**Placement decision:** the oracle lives in
`backend/services/admin/cold-start-certification.service.test.ts` (new `Tier 4` describe), NOT the chaos
file. Rationale: the per-file fixture conventions decide — the purity oracle is a single-threaded
before/after snapshot, which belongs to the `runInRollback` harness (zero committed residue, no
teardown bookkeeping); the chaos file is reserved for committed-fixture cross-transaction concurrency.

**What the new test asserts** (`Tier 4 — cross-entity purity oracle`, 1 test, 15 expect() calls):

- Whole-table COUNT snapshot taken AFTER fixtures, BEFORE `certifyTeacherColdStart`, and again AFTER,
  inside the same transaction, for every untouched-by-contract surface.
- **Untouched set (verified against `backend/db/schema/**` before writing):** `users`,
  `wallet`, `subscriptions`, `plans`, `session`, `teacher_transaction`, `student_payments`,
  `student_subscriptions`. Exact `toEqual` record comparison ⇒ zero movement anywhere.
- **Contracted movers, exact deltas:** `teacher` +1 (created row), `audit_logs` +1, `notifications` +1;
  `applicants` COUNT unchanged with the row proven moved BY UPDATE (fixture seeded `InEvaluation`,
  post-call re-read asserts `Passed`) — "+1 where applicable" honored: applicants moves in place,
  not by insert.
- Fixture shape: pending-application target (`createTestApplicant(..., { status: InEvaluation })`) so
  the finalize stage has real work; success precondition pinned via `detail.teacher?.isApproved`.

### Table-name verification against the drizzle schema (step-1 mandate)

| Task wording | Real schema identifier | Source |
|---|---|---|
| `users` | `users` (`"users"`) | `backend/db/schema/users/users.ts` |
| `wallet` | `wallet` (`"wallet"`) | `backend/db/schema/billing/wallet.ts` |
| `subscriptions` | `subscriptions` (`"subscriptions"`) | `backend/db/schema/billing/subscriptions.ts` |
| `plans` | `plans` (`"plans"`) | `backend/db/schema/billing/plans.ts` |
| `session` | `session` (`"session"`) | `backend/db/schema/classes/session.ts` |
| `teacher_transaction` | `teacherTransaction` (`"teacher_transaction"`) | `backend/db/schema/billing/teacher-transaction.ts` |
| *(adjacent billing)* | `student_payments` (`studentPayments`) | `backend/db/schema/billing/student-payments.ts` |
| *(adjacent billing)* | `student_subscriptions` (`studentSubscriptions`) | `backend/db/schema/billing/student-subscriptions.ts` |

Every named table EXISTS as named — no name-drift remapping was required. The two adjacent billing
tables (`student_payments`, `student_subscriptions`) were added to the untouched set per the task's
"any teacher_transaction-adjacent billing tables you find" clause.

**Harness idioms followed:** `countRows(tx, table: PgTable)` mirrors the precedent in
`backend/db/test/logic/auth/registration.service.test.ts:103` and
`backend/db/test/logic/students/handshake-code-generation-locks.test.ts:129` verbatim; snapshot loops
use `Promise.all` (oxlint `no-await-in-loop` safe); new table imports ride the top-level
`@/backend/db/schema` barrel (per `backend/db/schema/AGENTS.md`).

**Result:** `backend/services/admin/cold-start-certification.service.test.ts` — **28 pass / 0 fail**,
168 expect() calls. Tier 4 green on the first seeded run.

## 2. Full sweep ledger (all commands verbatim)

All runs via `bun run test/scripts/run-test.ts <path>` only. No `test:ui*` runner ever executed
(session policy honored).

| Sweep | Result | Exit | Notes |
|---|---|---|---|
| `backend/services/admin` | **115 pass / 1 fail** (116 tests, 5 files) | 1 | The 1 fail is a DEV3-016 latent data-threshold flake — Finding C1 below. DEV3-018 suites: 28/28 service + 9/9 chaos. |
| `backend/db/repo/teachers` | **17 pass / 0 fail** (2 files) | 0 | teacher.repository (10) + applicant.finalize (7) |
| `backend/graphql/test` | **210 pass / 6 fail** (216 tests, 14 files) | 1 | 4 plan-catalog + 2 notification seeded-admin — Findings C2/C3. Zero DEV3-018 attribution (proofs in §3). |
| `shared/locale` | **95 pass / 2 fail** (97 tests, 5 files) | 1 | handshakeCode parity vs nested `errors.planCatalog` — Finding C4. |
| `frontend/graphql` | **177 pass / 0 fail** (13 files) | 0 | GREEN after Fixing F1 (mutation inventory), verified solo (9/9 warning-surfacing) and full sweep. |
| `test/workflows/admin/cold-start-certification.journey.test.ts` | **12 pass / 0 fail** | 0 | unchanged green |

DEV3-016 regression locks (inside the services/admin sweep), explicit shard counts:
- `user-management.service.test.ts`: **60 pass / 1 fail** (expected 61 green — see Finding C1; the
  failing test is `listDirectory > happy path — admin lists directory; new student row observable with
  role-child headline`).
- `user-management.chaos.test.ts`: **8 pass / 0 fail** (meets the "chaos 8" lock verbatim).

### Purity-oracle deltas (the requirement's headline number)

Under the oracle run: untouched surfaces delta = **0 rows** (exact record equality,
8 tables); movers = `teacher` **+1**, `audit_logs` **+1**, `notifications` **+1**,
`applicants` **+0 rows / status flipped InEvaluation→Passed** (in-place UPDATE).

## 3. Fixes and findings

### F) FIXED — frontend/graphql warning-surfacing inventory (DEV3-018-attributable, the ONLY one)

`frontend/graphql/test/warnings/warning-surfacing.test.ts` A1 — the drift-guard contract lock
`KNOWN_LIVE_MUTATION_FIELDS` did not yet enumerate the task-3.x mutation
`adminCertifyTeacherColdStart`, which the live Mutation root now exposes. Tasks 3.1/3.2 re-pinned
`backend/graphql/test/schema-surface.test.ts` and `sdl-static-assertions.test.ts` but missed this
lock. This failure was CAUSED by DEV3-018's own surface addition and fixed here, in the way the lock
itself prescribes ("they still belong on this drift-guard list" / the array header documents exactly
this refresh flow for every prior stream — DEV3-016, DEV3-010, D2, upstream #28):

- ADD `"adminCertifyTeacherColdStart"` to `KNOWN_LIVE_MUTATION_FIELDS` (sorted position) + one comment
  line classifying the mutation: returns the canonical `AdminUserDetail` payload with every denial in
  `errors[]` — warning-incapable, identical class to the DEV3-016 trio.

Evidence the fix (not a weakening) is contract-faithful: the lock's purpose is "every deployed
Mutation root field is enumerated" — an omitted new field is precisely what it exists to catch;
adding the enumeration is the documented maintenance, not an assertion change. Verified green:
`warning-surfacing.test.ts` solo **9 pass / 0 fail**, then the entire `frontend/graphql` sweep
**177 / 0**.

### C1) CRITICAL FINDING — DEV3-016 `listDirectory` happy path fails at current shared-DB state (foreign)

- Symptom: `page.items.find(id === student.id)` is `undefined`; pageSize 25, repo orders
  `asc(createdAt), asc(id)` — a brand-new student's row sits past page 1 whenever ≥25
  directory-eligible students are committed.
- Evidence: the SAME file/code was green in this task's own sweep at 23:54 (116/116) and is
  deterministic-red since 00:39; the test database currently holds 28 active directory-eligible
  students (users joined to `students`), ALL created by OTHER streams' committed wire fixtures
  (email prefixes `gateway-f-*`, `handshake-*`, `matrix-*`, `wire-*`, `test-*`; none newer than
  2026-09-01 21:31 — DEV3-018 suites committed ZERO rows: service tier runs under `runInRollback`,
  chaos/journey tiers track-delete with zero-residue oracles). The flip corresponds to a
  concurrently active agent mutating shared state (a second live session held/respawned the 3066
  test server during this task).
- Attribution: NOT DEV3-018 — zero ownership overlap; the implementation (ascending order, paging)
  is correct; the TEST carries a latent data-volume assumption.
- Action taken: NONE on the file (foreign test; fixing the assertion = weakening a DEV3-016 lock —
  prohibited; deleting foreign committed fixture rows = destructive shared-state action outside my
  mandate).
- Recommended owner fix (for the orchestrator): make the happy-path lookup volume-independent —
  e.g., probe with a unique-search/marker-scoped directory call or page-walk until found — owned by
  DEV3-016's surface.

### C2) CRITICAL FINDING — plan-catalog role-matrix: anonymous receives FORBIDDEN, tests expect UNAUTHORIZED (foreign, committed)

- 4 deterministic failures in `backend/graphql/test/plan-catalog.roles.test.ts`
  (`adminPlans` query + `createPlan` / `updatePlan` / `setPlanActiveStatus` mutations, all at the
  anonymous tier): expected extensions.code `UNAUTHORIZED`, received `FORBIDDEN`.
- Evidence: the resolvers gate via Pothos `authScopes: { role: [UserRole.Admin] }` — the
  anonymous → FORBIDDEN mapping lives in the shared auth-scope machinery; DEV3-018's
  `admin-gate.helpers.ts` delta is PURELY ADDITIVE (new `assertActorAdminActive`; the existing
  `assertActorAdmin` is byte-unchanged — `git diff` inspected), and no plan-catalog resolver consumes
  it. Both the tests and the gate code are committed at HEAD (streams #28/#35); the last commit
  `a259524` touched plan docs only.
- Fast failures (sub-ms) and byte-identical across three independent runs ⇒ deterministic, not flaky.
- No file touched: foreign stream (billing plan catalog) owns the contract reconciliation.

### C3) CRITICAL FINDING — notification wire suites fail in beforeAll: seeded demo admin absent (environment)

- `notification-query.test.ts` + `notification-integration.matrix.test.ts` fail in `beforeAll` at
  `loginActor(ADMIN_EMAIL, ADMIN_CREDENTIAL)` — server log verbatim:
  `[DOMAIN] Failed login: user not found … entityId: "ad***@kottaby.local"` followed by
  `UnauthorizedError`. The suite rides the SEEDED admin (`ADMIN_EMAIL`/`ADMIN_PASSWORD` env
  fallback); that user does not exist in the current shared test database (verified: same
  deterministic failure in the 23:54 full sweep AND in isolation at 00:31; sibling wire suites that
  provision their own actors are green).
- DEV3-018 touches nothing in registration/login/seed surfaces; both files are committed by stream
  #35. Attribution: environment/seed-state, not code.
- Not "fixed" by seeding: writing demo rows into a shared database while another agent session is
  live is an out-of-mandate shared-state mutation; recorded for the orchestrator
  (likely `bun run db seed` in a quiescent window restores both suites).

### C4) CRITICAL FINDING — handshakeCode parity tests vs nested `errors.planCatalog` group (foreign, committed)

- 2 deterministic failures in `shared/locale/handshakeCode-namespace.parity.test.ts`: its flat
  per-key sweeps (`Object.keys(errorsAr)`) call `nonEmptyLabelOf` expecting strings, but the errors
  namespace now contains the NESTED group `planCatalog: PlanCatalogErrorsLabels`
  (`shared/locale/{en,ar}/errors/index.ts:22`, `shared/locale/types/errors/index.ts:46`) — an object,
  hence `must be a non-empty localized string`.
- Both sides are committed: the parity test (stream #34/#32) predates/interleaves the billing
  plan-catalog locale group. DEV3-018's own errors additions are flat strings in BOTH locales and the
  parity sweep accepts them — failure references ONLY `planCatalog`.
- No file touched: choosing between flattening the billing keys and hardening the parity sweep's
  flat assumption is the billing stream's call.

## 4. 5.2.SR record

- NO test was weakened: the only assertion-side change anywhere is the sanctioned drift-guard
  enumeration (Fix F1) whose own comments mandate exactly that maintenance on every new mutation.
- The one DEV3-018-attributable failure was caught AND fixed (F1) — the oracle + sweep did the job
  they exist for.
- Every remaining red shard is reported above as a CRITICAL finding with attribution evidence
  (file ownership, commit provenance, determinism, and zero-contamination proof for DEV3-018).
  Nothing was suppressed, retried-until-green, or hidden.
- Flake hygiene observed during the work: a stale port-3066 dev server (leftover of my own
  solo verification run of warning-surfacing) transiently contaminated one backend/graphql/test
  re-run ("Server on port 3066 did not start", cascaded timeouts) — that run was DISCARDED from the
  ledger and re-executed on a free port; the discarded run is not counted anywhere above.
  The residue process itself was cleaned up (my own orphan only; a second live agent's server was
  actively respawning on the same port during the 00:2x–00:4x window and was left alone).

## 5. Verification (this task's own commands)

| Check | Command | Result |
|---|---|---|
| Sub-loop (service test, lint) | `bun run scripts/health/sub-loop.ts backend/services/admin/cold-start-certification.service.test.ts --lifecycle lint` | exit 0 |
| Sub-loop (service test, duplicates) | same with `--lifecycle duplicates` | exit 0 |
| Service suite after oracle | `bun run test/scripts/run-test.ts backend/services/admin/cold-start-certification.service.test.ts` | 28/0, 168 expects |
| Sub-loop (warning lock, lint) | `bun run scripts/health/sub-loop.ts frontend/graphql/test/warnings/warning-surfacing.test.ts --lifecycle lint` | exit 0 |
| Sub-loop (warning lock, duplicates) | same with `--lifecycle duplicates` | exit 0 |
| Warning lock solo | `bun run test/scripts/run-test.ts frontend/graphql/test/warnings/warning-surfacing.test.ts` | 9/9 |
| Schema drift | `git diff --stat -- backend/db/schema/ backend/db/migration/` | empty (exit 0) |

## Files Changed

- MODIFIED `backend/services/admin/cold-start-certification.service.test.ts` — header coverage map
  line for Tier 4; one `PgTable` type import; one top-level schema-barrel import (8 untouched-surface
  tables + `applicants`); `countRows`/`snapshotUntouchedCounts` helpers; Tier 4 describe (1 test,
  15 expects). No existing assertion touched.
- MODIFIED `frontend/graphql/test/warnings/warning-surfacing.test.ts` — exactly +1 inventory entry
  (`adminCertifyTeacherColdStart`) and +3 comment lines of classification rationale.
- MODIFIED `tasks.md` — ONLY the 5.2 and 5.2.SR checkbox lines.
- CREATED this outcome file.

## Files Deliberately NOT Changed

- Nothing under `backend/db/schema/` or `backend/db/migration/` (verified empty diff).
- All C1–C4 implicated files (user-management suite, plan-catalog tests/resolvers, handshakeCode
  parity + plans/errors locale surfaces, notification suites) — foreign ownership; report-only.
- No seed/DB writes performed (C3 remediation deliberately left to the orchestrator).

## Deviations

1. The DEV3-016 lock mandate ("unchanged-green: service 61 + chaos 3 suites") is partially NOT MET in
   the current shared-DB state (service 60+1F; chaos 8/8 met verbatim). Recorded as Finding C1 with
   full attribution — DEV3-018 cannot restore it without either weakening a foreign assertion or
   deleting foreign committed data; both are outside this task's mandate.
2. `frontend/graphql` reached full green only after Fix F1; the intermediate 176/1 ledger line is
   superseded by the post-fix 177/0 run.
