# Task 6.2 Outcome — Wave B (review-backend)

**Date:** 2026-09-02 · **Requirements:** REQ-004, REQ-010..REQ-020, REQ-031..REQ-034, REQ-040..REQ-045, REQ-050..REQ-053
**Scope reviewed (read-only):** `backend/db/repo/teachers/teacher.repository.ts`, `backend/db/repo/teachers/applicant.repository.ts` (finalizeOnCertification delta :154-197), `backend/services/admin/admin-gate.helpers.ts` (assertActorAdminActive delta :92-137 + byte-parity of :59-90), `backend/services/admin/cold-start-certification.service.ts`, `backend/graphql/mutation/admin/admin-teachers.mutation.ts`, barrels `backend/db/repo/teachers/index.ts`, `backend/services/admin/index.ts`, `backend/graphql/mutation/admin/index.ts`.

**VERDICT: 3 findings — all LOW, none blocking.** Every plan-decision and layering checklist item below verified HONORED with fresh anchors. No CRITICAL/HIGH/MEDIUM issues; the C1–C4 concurrent-session failures were re-checked against scope and are NOT attributable to this wave's files (already registered in `deferred-items.md` D-C1..D-C4).

---

## 1. D-decision conformance (plan.md §4), re-verified from code

| Decision | Verdict | Evidence |
|---|---|---|
| D1 — actor gate WITH governance, pre-tx | HONORED | `cold-start-certification.service.ts:170` `assertActorAdminActive(actorId, locale, outerTx)` runs BEFORE `withTransaction` at :182; gate governance order deleted→blocked→suspended at `admin-gate.helpers.ts:110-136` |
| D2 — two guarded write shapes | HONORED | Plain INSERT `teacher.repository.ts:85-103` (raw 23505 surfaces); guarded `UPDATE … WHERE id AND is_approved=false … RETURNING` at `:123-131` — predicate+mutation one statement, zero-row → null |
| D4 — publish strictly post-commit | HONORED | In-tx emit `:216-228`; `publishReceipts` at `:237` AFTER `withTransaction` resolves (:232); engine in-tx branch returns receipt WITHOUT publishing (`notification-engine.service.ts:65-68`); publish failure degrades via catch → `NOTIFICATION_DELIVERY_DEGRADED` log + resolve (`notification-engine.publish.ts:32-34`) |
| D6 — consume-and-extend, no private copy | HONORED | **Independent byte-parity proof run by THIS wave:** `git show a259524^:backend/services/admin/user-management.service.ts` lines 240-271 vs `admin-gate.helpers.ts:59-90` — diff empty except the `export` keyword (`BYTE-PARITY-OK`). `user-management.service.ts:65` imports the shared gate, 7 call sites (:118,:157,:193,:231,:271,:342,:395), zero private definition |
| D7 — `makeEvaluator` default true at BOTH layers | HONORED | SDL `defaultValue: true` (`admin-teachers.mutation.ts:41`) + resolver coalesce `?? true` (`:64`) + service coalesce `:180` |
| D8 — audit = fixed 3-field JSON | HONORED | `:207-211` `JSON.stringify({ makeEvaluator, applicantRow, elevation })`; `actionType: AuditActionType.Override`, `entityType: "teacher"` (lowercase label), `entityId: userId` (:201-214); ≤2000 guaranteed by construction + `truncateDetailsSafely` belt (`audit.service.ts:37-53`) |
| D10 — repeat-call = conflict, no idempotency keys | HONORED | `existing.isApproved === true` → `TEACHER_ALREADY_CERTIFIED` at `:134-137` BEFORE any write/notify |
| D11 — custom codes via `ConflictError(code, message)` overload | HONORED | Overload verified at `backend/lib/errors.ts:170-182`; three custom codes thrown at `service.ts:128,136,150,190,194`; zero new error subclasses |
| D12 — copy in the ADMIN's (emitter) locale | HONORED | `getServerTranslations(locale)` bound to call locale at `:172`; `coldStartCertifiedTitle/Body` read from it at `:220-221` |
| D5 — refreshed detail via existing assembler, same tx | HONORED | `AdminUserManagementService.getUserDetail(userId, locale, actorId, tx)` at `:230`; signature re-verified at `user-management.service.ts:225-230` |

## 2. Layering checklist

- **tx propagation to EVERY call:** the single `withTransaction(outerTx, tx => …)` (`:182`) threads the same `tx` to `UserRepository.findById` (:183), `certifyTeacherRow` internals (:115,119,139,147), `ApplicantRepository.finalizeOnCertification` (:199), `AuditService.createAuditLog` (:213), `NotificationEngine.emitForUser` (:226), `getUserDetail` (:230). `outerTx` only seeds the pre-tx gate (:170) and boundary; `publishReceipts` (:237) correctly carries NO tx post-commit. **No mixed tx/db anywhere.**
- **JR-C-1 (denials write ZERO audit rows):** every denial path throws before `:201` (gate pre-tx; shape validation :175-177; target checks :184-195; conflict paths inside `certifyTeacherRow` :128,:136,:150). 27+1-test service suite pins zero-audit oracles per denial (green today).
- **Silent happy path:** `grep`-level review confirms every `logCertificationDenial` call sits in a denial branch only (:127,:135,:149,:185,:189,:193); zero domain logs on the success path (asserted by Tier-1 test, per 2.5 outcome).
- **Closed error taxonomy:** UNAUTHORIZED/FORBIDDEN (gate), VALIDATION (:176), USER_NOT_FOUND (`NotFoundError("USER", …)` :186 — code auto-generated per `errors.ts:37-43`), TEACHER_ROLE_REQUIRED/TEACHER_ACCOUNT_GOVERNED/TEACHER_ALREADY_CERTIFIED (ConflictError overload). The ONLY try/catch is the 23505 translation (:121-124).
- **23505 cause-checked translation, non-unique rethrow:** `isUniqueViolation` traverses the Drizzle `cause` chain (`user-provisioning.helpers.ts:54-69`); non-unique errors rethrow untouched at `:122-124` (test-pinned per 2.5 outcome).
- **Repo discipline:** writes take REQUIRED `tx: DBTransaction` as last param (`teacher.repository.ts:88,121`); no prepared statements on writes; ids bound via `eq()`/parameterized `$1` (:67-68); NO `--` inline comments in the raw SQL (:57-68 checked); payloads built field-by-field, zero spreads. `findById` bare-read branch uses `queryDb(tx)` per the M3 ruling, byte-shaped like `UserRepository.findById` (`user.repository.ts:75-95`; no PoolClient branch in EITHER — precedent parity confirmed by grep).
- **`finalizeOnCertification` delta:** unconditional supersede write (`status=Passed`, `cooldownUntil=null`, `updatedAt=now()`) in ONE statement (`applicant.repository.ts:174-197`); `verificationAttempts`/`lastAttemptAt` never in the SET clause; row never deleted; `false` on absent row. Mirrors `recordVerificationAttempt`'s executor idiom (`:132-152`) exactly — pre-existing pattern, tx last, optional with `db` fallback identical to sibling (service always passes tx).
- **Audit single-writer + notification single-writer (backend/services/AGENTS.md):** sole `AuditService.createAuditLog` call inside the tx; notifications written ONLY via `NotificationEngine.emitForUser`; no other `notifications`/`audit_logs` writer touched.
- **Mutation layer (REQ-030/033/050/060):** `$all: { authenticated: true, role: [UserRole.Admin] }` verbatim at `admin-teachers.mutation.ts:45-50`; description string byte-identical to plan.md:180; thin resolver (guard + field-by-field delegation, no spread, no try/catch); `AdminUserDetailPothosObject` reused, zero new Pothos types; side-effect barrel import added alphabetically (`mutation/admin/index.ts:11`).
- **Barrels:** `teachers/index.ts` two `export *` lines; `services/admin/index.ts` four `export *` lines alphabetical; GraphQL barrel side-effect-only per the layer exception. All conform.
- **Schema drift (informational):** `git diff --name-only -- backend/db/schema/ backend/db/migration/` re-verified EMPTY during review.
- **Canonical doc:** `docs/admin/cold-start-certification.md` does not yet exist — EXPECTED at this stage: task 7.1 owns it and is still `[ ]` (unflipped). Not a defect; D1's "divergence recorded in canonical doc" obligation lands there.

## 3. Findings

| ID | Severity | Finding | Anchor | Disposition |
|----|----------|---------|--------|-------------|
| B-1 | [LOW] | **Two plain `new Error` throws in the service** conflict formally with `docs/graphql/error-handling-contract.md:165` ("never `new Error` in services — throw DomainError subclasses"). Both are unreachable-by-design internal-inconsistency guards (in-tx emit returning a bare row; elevate zero-row + approved re-read false), and masking to `INTERNAL_SERVER_ERROR` is the DESIRED semantics; the shape matches the DEV3-016 corrupt-data precedent (`user-management.mappers.ts:64,193,238`) and was pre-registered as 2.5 Deviation 4. | `cold-start-certification.service.ts:99` and `:152` | Record-only (this outcome) OR ledger row; no fix recommended — introducing a new subclass contradicts REQ-050's closed-set "no new subclasses" ruling, which is the higher-priority instruction. |
| B-2 | [LOW] | **Log-context entity vocabulary drift**: `logCertificationDenial` emits `entity: "users"` (plural) for USER_NOT_FOUND and `"teacher"` for target conflicts, while the documented DEV3-016 convention is `entity: "user"` (`docs/admin/user-management.md` §REQ-052 line; the gate itself uses `"user"` at `admin-gate.helpers.ts:66,75,84`). Log-context only; the bounded `{ code, entity, entityId, locale }` bag shape and PII hygiene are intact. | `cold-start-certification.service.ts:79-86` (`:82`) | Optional cosmetic normalization; safe to defer to the 6.5 reconciliation (no behavioral contract depends on the `entity` string). |
| B-3 | [LOW] | **Governance re-read is null-tolerant**: `assertActorAdminActive`'s second `findById` uses `actor?.isDeleted / isBlocked / suspended` (`admin-gate.helpers.ts:110,119,128`) — if the actor row vanished between the role-gate read and the governance re-read, the gate passes silently. Non-exploitable in production (no `users` hard-delete surface exists; INV-U1 soft-delete preserves rows); reachable only via test-harness hard-delete inside a concurrent session window. Registered implicitly by 2.4 Deviation 2 (re-read design). | `admin-gate.helpers.ts:109-110` | Optional hardening: `if (!actor) throw new ForbiddenError(tErrors.forbidden)` after :109. Not required for this ticket's threat model (D1 targets a suspended/blocked/deleted actor with a LIVE row). |

No NEW CRITICAL/HIGH/MEDIUM findings. C1–C4 (concurrent-session failures) are already ledgered (deferred-items.md D-C1..D-C4) and were NOT re-counted.

## 4. Evidence commands run (this wave, fresh)

| Command | Result |
|---|---|
| Byte-parity: `git show a259524^:…/user-management.service.ts` lines 240-271 vs `admin-gate.helpers.ts:59-90` (diff after adding `export`) | **BYTE-PARITY-OK** (empty diff) |
| `bun run test/scripts/run-test.ts backend/services/admin/cold-start-certification.service.test.ts` | exit 0 — **28 pass / 0 fail**, 168 expect() |
| `bun run test/scripts/run-test.ts backend/db/repo/teachers` | exit 0 — **17 pass / 0 fail** (teacher.repository 10 + applicant.finalize 7) |
| `bun run test/scripts/run-test.ts backend/services/admin/admin-gate.helpers.test.ts` | exit 0 — **10 pass / 0 fail** |
| Substrate reads: `notification-engine.service.ts:43-78/117-123`, `notification-engine.publish.ts:32-34`, `audit.service.ts:36-90`, `errors.ts:37-43/170-182`, `user-provisioning.helpers.ts:54-69`, `with-transaction.ts:27-35`, `user-management.service.ts:65/225-230`, `user.repository.ts:75-95` | every consumed signature/contract verified verbatim against the service call sites |
| `grep -n "PoolClient" user.repository.ts teacher.repository.ts` | identical precedent shape (no PoolClient branch in either) |
| `git diff --name-only -- backend/db/schema/ backend/db/migration/` | EMPTY (zero drift preserved) |
| task-state grep on tasks.md | 5.1/5.2/5.3 `[x]`; 6.x and 7.1 `[ ]` — canonical-doc absence is scheduled, not a defect |

## Files Changed

- CREATED `ai/plans/sprint_3/dev3-018-cold-start-bootstrapping-direct-sheikh-c/outcome/6.2-review-backend-outcome.md` (this file only — read-only discipline honored; NO source edits, NO checkbox flips)

## Deviations

None. Review was read-only on all source; temporary extraction artifacts were written to `/tmp` only (`/tmp/ums-before.ts`, `/tmp/old-gate.ts`, `/tmp/new-gate.ts`).
