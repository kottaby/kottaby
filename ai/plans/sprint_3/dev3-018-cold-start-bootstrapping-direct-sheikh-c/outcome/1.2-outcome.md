# Task 1.2 Outcome — i18n Keys: 3 Errors Keys + 2 Applicant Notification Keys (BOTH Locales)

**Date:** 2026-09-01
**Requirements:** REQ-002, REQ-051, REQ-074

---

## Summary

Added the five new compile-time i18n keys to the two EXISTING namespaces (no new namespaces
minted):

- `errors` namespace — three NEW FLAT `ErrorsLabels` keys: `teacherAlreadyCertified`,
  `teacherRoleRequired`, `teacherAccountGoverned` (flat domain-prefixed convention, matching the
  existing `applicantNotFound`/`handshakeCodeInvalid`/`trialAlreadyGranted` flat leaves).
  `adminUsers.userNotFound` is REUSED as-is — not duplicated.
- `applicant` namespace — two NEW `ApplicantLabels` keys: `coldStartCertifiedTitle`,
  `coldStartCertifiedBody` (the certification-notification copy; the emit subject IS the (former)
  applicant, so per REQ-051 the `applicant` namespace owns it).

Each key landed in the type interface plus BOTH locale implementations (en English, ar genuine
Arabic translations, checked against the existing tone of the governance keys
`accountDeleted/accountBlocked/accountSuspended` at `shared/locale/en/errors/index.ts:17-19` and
the applicant certified copy `certifiedSummary`).

---

## Files Changed

- MODIFIED `shared/locale/types/errors/index.ts` — appended the three flat keys to
  `ErrorsLabels` (after `trialAlreadyGranted`, before the closing brace), each with the house-style
  JSDoc (sentence + failure-context, no plan/REQ references).
- MODIFIED `shared/locale/en/errors/index.ts` — three English values:
  - `teacherAlreadyCertified: "This teacher is already certified."`
  - `teacherRoleRequired: "This user is not a teacher."`
  - `teacherAccountGoverned: "This account has been deleted, blocked, or suspended."`
- MODIFIED `shared/locale/ar/errors/index.ts` — three Arabic values:
  - `teacherAlreadyCertified: "هذا المعلم معتمد بالفعل."`
  - `teacherRoleRequired: "هذا المستخدم ليس معلماً."`
  - `teacherAccountGoverned: "تم حذف هذا الحساب أو حظره أو إيقافه."`
    (reuses the existing ar governance vocabulary حذف/حظر/موقوف from
    `accountDeleted`/`accountBlocked`/`accountSuspended`).
- MODIFIED `shared/locale/types/applicant/index.ts` — appended `coldStartCertifiedTitle` and
  `coldStartCertifiedBody` to `ApplicantLabels` with JSDoc (pure copy, no identifiers).
- MODIFIED `shared/locale/en/applicant/index.ts` —
  `coldStartCertifiedTitle: "Your Teacher Certification Is Complete"`;
  `coldStartCertifiedBody: "Congratulations — you are now a certified Qur'an teacher. All teaching
  surfaces are now open for you in the dashboard."`
- MODIFIED `shared/locale/ar/applicant/index.ts` —
  `coldStartCertifiedTitle: "اكتمل اعتمادك كمعلم قرآن"`;
  `coldStartCertifiedBody: "تهانينا! أنت الآن معلم قرآن معتمد، وقد فُتحت لك جميع أقسام التعليم في
  لوحة التحكم."`

The derived `ErrorMessageKey` union (`shared/locale/types/errors/index.ts:103-105`) picks the new
keys up automatically — no manual union edit needed.

## Files Deliberately NOT Changed

- `shared/locale/types/message.ts`, `shared/locale/namespaces/**`,
  `shared/locale/serverLegacy.ts`, `app/[locale]/layout.tsx` — the registration checklist's steps
  3–5 apply only to NEW namespaces; both namespaces here are pre-existing and fully registered
  (proven live by the applicant parity test's registry-wiring assertions, still green).
- Both parity suites (`shared/locale/errors-namespace.parity.test.ts`,
  `shared/locale/applicant-namespace.parity.test.ts`) — untouched; the new keys are covered
  MECHANICALLY (top-level key-set equality + non-empty-leaf walks), and the applicant key-set pins
  were NOT loosened.
- Nothing under `backend/db/schema/` or `backend/db/migration/` (zero-drift mandate).
- `deferred-items.md` — untouched by this task (gate `grep -c "❌|⚠️"` unaffected).

## Verification

### 1.2.QL — per-file sub-loop (`--lifecycle duplicates`), all six files

```
bun run scripts/health/sub-loop.ts shared/locale/types/errors/index.ts    --lifecycle duplicates → exit 0
bun run scripts/health/sub-loop.ts shared/locale/en/errors/index.ts       --lifecycle duplicates → exit 0
bun run scripts/health/sub-loop.ts shared/locale/ar/errors/index.ts       --lifecycle duplicates → exit 0
bun run scripts/health/sub-loop.ts shared/locale/types/applicant/index.ts --lifecycle duplicates → exit 0
bun run scripts/health/sub-loop.ts shared/locale/en/applicant/index.ts    --lifecycle duplicates → exit 0
bun run scripts/health/sub-loop.ts shared/locale/ar/applicant/index.ts    --lifecycle duplicates → exit 0
```

Each reported `All checks for lifecycle "duplicates" passed` (tsgo → oxlint → biome → lint →
duplicates stages all green per file).

### 1.2.TE — parity suites (both paths exist as named; verified via Glob before running)

```
bun run test/scripts/run-test.ts shared/locale/errors-namespace.parity.test.ts    → exit 0 — 8 pass, 0 fail, 84 expect() calls
bun run test/scripts/run-test.ts shared/locale/applicant-namespace.parity.test.ts → exit 0 — 11 pass, 0 fail, 112 expect() calls
```

The applicant suite's key-set pins (`identical sorted key sets across BOTH locale sources`,
registry id `applicant.applicant`, `getLabels` bundle-slice identity) remain green unmodified.

### 1.2.SR — semantic self-review

- ONE-argument consumer contract intact: no edits to `server.ts`/`server-graphql.ts`/`client/`;
  future consumers resolve the new keys through the existing
  `getServerTranslations(locale).errorsTranslations.<key>` /
  `getServerTranslations(locale).applicantTranslations.<key>` / `ctx.t("errors")` property-access
  paths — nothing here introduces a namespace theater, positional drift, or string handles.
- No `next-intl`, no `Translation` enum, no string-handle `useAppTranslation` — none added.
- JSDoc comments contain no plan/REQ/task meta-references (code-comment hygiene).
- Copy is generic — no user identifiers, matching the `adminUsers` group convention and the
  errors-file JSDoc rule that admin-authored identifiers must not appear in messages.
- No ICU placeholders introduced in any of the five keys, so the applicant suite's
  `{cooldownUntil}`-placeholder pins are unaffected (suite green confirms).

### 1.2.IV — instruction verification

- `shared/AGENTS.md` namespace-registration checklist: steps 1–2 executed (interface + both
  locale implementations); steps 3–5 inapplicable to key-additions on existing namespaces
  (no new `MessageSchema` member / `namespaces` handle / `namespacePaths` entry — verified by the
  already-registered state proven green in the parity suites).
- `shared/locale/AGENTS.md`: imports in the touched implementation files remain `@/shared/locale/...`
  alias form (untouched existing import lines); leaf modules remain plain string/object literals
  with no logic; no relative traversal added.
- `.agents/instructions/backend.instructions.md` i18n section: no hardcoded error strings
  introduced in any layer; the localized keys are now available for the future service/resolver
  consumers.

## Cross-File Dependencies

- Task 2.4/2.5 (service + gate): `ConflictError("TEACHER_ROLE_REQUIRED", t.teacherRoleRequired)`,
  `ConflictError("TEACHER_ACCOUNT_GOVERNED", t.teacherAccountGoverned)`,
  `ConflictError("TEACHER_ALREADY_CERTIFIED", t.teacherAlreadyCertified)` are now resolved at
  compile time; `NotFoundError("USER", t.adminUsers.userNotFound)` continues to reuse the existing
  `adminUsers` group.
- Task 2.5 (notification emit): copy resolved via
  `getServerTranslations(locale).applicantTranslations.coldStartCertifiedTitle/…Body` — BOTH keys
  now exist in en + ar.
- Future frontend code→behavior map additions (REQ-061 family) for the three new
  `extensions.code`s are out of scope here; the keys merely exist for them.

## Deviations

None. All six mandated file edits landed exactly as specified; both parity test paths were valid
as written (no path fallback needed).
