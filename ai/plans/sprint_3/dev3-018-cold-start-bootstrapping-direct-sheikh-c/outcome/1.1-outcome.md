# Task 1.1 Outcome — Canonical Type `TeacherColdStartCertificationInput`

**Date:** 2026-09-01
**Requirements:** REQ-003

---

## Summary

Added the canonical service-input type `TeacherColdStartCertificationInput` to
`backend/types/teachers/teacher.types.ts`, exactly as specified (readonly `userId: number`,
readonly `makeEvaluator: boolean`). The existing `TeacherSelectType`/`TeacherInsertType`
definitions were left byte-identical. The barrel `backend/types/teachers/index.ts:3` already
re-exports `./teacher.types` — **no barrel edit was required** (the task's expected default).

Pre-add uniqueness check: `git grep TeacherColdStartCertificationInput` matched only plan-dir
documents (`tasks.md`, `specs.md`, `plan.md`) — no in-tree type definition existed, so no
duplicate/fork risk.

---

## Files Changed

- MODIFIED `backend/types/teachers/teacher.types.ts` — appended:

```typescript
export interface TeacherColdStartCertificationInput {
  readonly userId: number;
  readonly makeEvaluator: boolean;
}
```

- CREATED `ai/plans/sprint_3/dev3-018-cold-start-bootstrapping-direct-sheikh-c/outcome/1.1-outcome.md` (this file)
- `tasks.md` — flipped only the `1.1`, `1.1.QL`, `1.1.SR`, `1.1.IV` checkbox lines

## Files Deliberately NOT Changed

- `backend/types/teachers/index.ts` — barrel already re-exports `./teacher.types` (line 3);
  the task anticipated no edit and none was needed. Zero delta.
- Nothing under `backend/db/schema/` or `backend/db/migration/` (zero-drift gate)
- No service, repository, GraphQL, or test files — this task is a single-type add only
- `deferred-items.md` — no blockers or deferrals arose, so no ledger rows added

---

## Verification (commands + results)

### 1.1.QL — Quality Loop

```
$ bun run scripts/health/sub-loop.ts backend/types/teachers/teacher.types.ts --lifecycle duplicates
```

- **Exit code: 0**
- tsgo: ✅ passed (no errors for the file)
- oxlint: ✅ passed
- biome:check: ✅ passed
- lint:type-aware (in-process lint service): ✅ passed
- check:duplicates (intra-file jscpd): ✅ passed

### 1.1.SR — Semantic Review

- **Canonical placement**: type lives in `backend/types/teachers/teacher.types.ts` per the
  Types layer; NO service-layer `.types.ts` file was created or touched.
- **Readonly fields**: both `userId` and `makeEvaluator` are declared `readonly`, matching the
  immutable-input idiom used by sibling contract types (e.g. `AuditLogWriteContract`).
- **No duplicate elsewhere**: repo-wide symbol grep pre-add returned plan-doc matches only;
  post-add the single definition is at `backend/types/teachers/teacher.types.ts:6-9`.
- **No plan meta-artifacts in code**: no comments or JSDoc were added, so no REQ-/task
  references exist in the source file.

### 1.1.IV — Instruction Verification (`.agents/instructions/backend.instructions.md`)

- ✅ "ALL types from `@/backend/types`" — added in the canonical types layer, re-exported
  through `backend/types/teachers/index.ts` → `backend/types/index.ts` chain (verified barrel
  present; no edit needed).
- ✅ Barrel conventions: no barrel edit made; existing barrel already conforms
  (`export * from "./teacher.types"`).
- ✅ Naming: `TeacherColdStartCertificationInput` follows the `{Entity}...Input` specialization
  convention (task-prescribed name per plan.md:126 and specs.md REQ-003).
- ✅ No `console.*`, no hardcoded strings, no Pothos-local types — none introduced.
- Note: the sub-loop banner references `.github/instructions/backend.instructions.md`, which
  does NOT exist in-tree (Glob: no matches). The live instruction file is
  `.agents/instructions/backend.instructions.md` (confirmed by outcome 0.2's instrumentation
  audit and this task's own assignment text) — that file was read and applied. Recorded as an
  observation, not a violation; the sub-loop's mapping table is stale but advisory only.

---

## Cross-File Dependencies

- Downstream consumers (task 2.3 service `certifyTeacherColdStart` signature, task 3.x Pothos
  mutation input) can now `import type { TeacherColdStartCertificationInput } from
  "@/backend/types"` — the export chain was re-verified live (barrel line 3) at completion time.
- Outcome 0.2's anchor-drift register: no interaction; this task touched only the types layer.

## Deviations

- None. The type was added exactly as specified, the barrel was verified and left untouched,
  and the full `duplicates` lifecycle passed on the first run.
