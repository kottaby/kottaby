# Task 0.2 Outcome — Prerequisite Verification: Substrate Existence Audit (Verify-Then-Claim)

**Date:** 2026-09-01
**Requirements:** REQ-004 (Dependency Guard — Reuse, Never Fork)

---

## Summary

Every substrate artifact DEV3-018 depends on was re-verified **against the actual tree**, not the
plan's prose. Verdict: **all twelve EXISTENCE items and all three-instrumentation confirmations PASS**,
with two material plan-anchor findings:

1. **`backend/services/admin/admin-gate.helpers.ts` already EXISTS** — the task's VERIFY-ABSENCE
   claim (task 0.2 bullet, specs.md §REQ-004 verification list) is **FALSE as of HEAD**. The module
   ships `assertActorAdmin` (verbatim-extracted from `user-management.service.ts`) plus
   `toAuditActionType`. This is exactly the extraction-collide scenario the plan pre-registered as
   **D-GATE-SHARING** (first-lander creates, secondlander consumes): DEV3-018 is the second-lander and
   task 2.4 MUST take the **consume-and-extend** branch (add `assertActorAdminActive` additively;
   add the barrel export; never re-create). Ledger row `D-GATE-LANDED` appended.
2. **Plan line anchors have drifted** for `user-management.service.ts`,
   `notification-engine.service.ts`, `user-provisioning.helpers.ts`, and the `withTransaction` import
   line. Symbols all exist; only the numeric anchors are stale (see the drift register below).

Zero ❌/⚠️-class findings: no substrate is missing, so no dependent task is blocked. The ledger's
glyph policy (Phase-5 gate `grep -c "❌\|⚠️" = 0`) was respected — the finding is recorded as a
✅ audit-verified RESOLVED-REFERENCE row.

---

## EXISTENCE audit — all items VERIFIED

| # | Artifact claimed | Cited anchor | Verified anchor | Result |
|---|---|---|---|---|
| 1 | `AuditService.createAuditLog` | `backend/services/admin/audit.service.ts:82-90` | `audit.service.ts:82-90` — signature `createAuditLog(input: AuditLogWriteContract, tx: DBTransaction): Promise<void>`; in-tx `tx.insert(auditLogs)` with safe details truncation | ✅ exact |
| 1b | `AuditLogWriteContract` | `backend/types/contracts/admin-audit.contract.types.ts` | `admin-audit.contract.types.ts:22-30` — readonly `{ actorId, actionType, entityType: string, entityId: number, details: string }` | ✅ |
| 2 | `AuditActionType.Override` | `backend/enum/audit/audit-action-type.enum.ts:10` | `audit-action-type.enum.ts:10` — `Override = "override"` | ✅ exact |
| 2b | `override` pgEnum value | `backend/db/schema/enums.ts:66-74` | `enums.ts:66-74` — `auditActionType` pgEnum includes `"override"` at line 70 | ✅ exact |
| 3 | `AdminUserManagementService.createUser` / `getUserDetail` / `getUserActivity` | `user-management.service.ts:240-271, 503-580` | Namespace opens at `:100`; `getUserActivity :186`, `getUserDetail :225-230` (signature `(userId, locale, actorId, outerTx?)`), `createUser :265-271`. File is 445 lines — the `503-580` anchor range is beyond EOF (**drift**) | ✅ symbols / ⚠ line drift |
| 3b | private `assertActorAdmin` | `user-management.service.ts:240-271` | **No longer private there.** The service imports `assertActorAdmin` from `@/backend/services/admin/admin-gate.helpers` (`user-management.service.ts:65`) and calls it at `:231, :271, :342, :395`. Implementation lives at `admin-gate.helpers.ts:59-90` | ✅ moved (see Finding 1) |
| 4 | `UserRepository.findById` | `backend/db/repo/users/user.repository.ts:75-95` | `user.repository.ts:75-95` — signature `findById(id: number, tx?: DBQueryExecutor): Promise<UserSelectType \| null>`, transactional Drizzle select + `queryDb` Neon fast path | ✅ exact |
| 5 | `ApplicantRepository.findByUserId` | `backend/db/repo/teachers/applicant.repository.ts` | `applicant.repository.ts:89-116` — shared parameterized read across `DBTransaction` / `PoolClient` / `queryDb` branches | ✅ |
| 6 | `NotificationEngine.emitForUser` / `publishReceipts` | `notification-engine.service.ts:77-82, 327-369` | `emitForUser :43-78`; `publishReceipts :117-123`; file is 182 lines total (**drift** — cited ranges exceed EOF). In-tx emit returns a `NotificationDeliveryReceipt` and does NOT publish (the `tx !== undefined` branch at `:65-68` returns the receipt without calling `publishReceiptsFromIndex`), which is precisely the post-commit-publish seam DEV3-018 needs | ✅ symbols / ⚠ line drift |
| 6b | `NotificationEngineCallOptions` seam | same file | `notification-engine.service.ts:35-38` — `{ transport?: NotificationFanoutTransport; cache?: NotificationIdempotencyClaimCache }`; threaded as the 4th arg of `emitForUser` and 3rd arg of `publishReceipts` | ✅ (anchor 35-38, not 77-82) |
| 7 | `withTransaction` | `backend/lib/db/with-transaction.ts`; imported at `user-management.service.ts:67` | Module exists (`with-transaction.ts:27-35`, SAVEPOINT-vs-top-level semantics documented `:1-23`). Import in the service sits at **`user-management.service.ts:62`** (drift — cited :67) | ✅ / minor drift |
| 8 | Pothos `$all` precedent | `backend/graphql/mutation/admin/admin-users.mutation.ts:64-66` | `admin-users.mutation.ts:64-68` — `authScopes: { $all: { authenticated: true, role: [UserRole.Admin] } }` on `adminCreateUser` | ✅ exact |
| 9 | `AdminUserDetailPothosObject` | `backend/graphql/pothos/admin/admin-user.pothos.ts:235-300` | `admin-user.pothos.ts:235-300` — `objectRef<AdminUserDetailReturnType>("AdminUserDetail")`, `teacher` slot typed `AdminTeacherSnapshotPothosObject` (`:284-288`), `applicant` slot at `:279-283` | ✅ exact |
| 10 | `ConflictError(code, message)` overload | `backend/lib/errors.ts:170-182` | `errors.ts:170-182` — two declared overloads `(message, options?)` and `(code, message, options?)` with runtime discriminator | ✅ exact |
| 10b | `translateDbError` cause-chain | `backend/lib/errors.ts:200-208` | `errors.ts:200-208` — 23505 / SQLite-unique traversal into `ConflictError`, other errors pass through | ✅ exact |
| 10c | `isUniqueViolation` | `backend/services/shared/user-provisioning.helpers.ts:74` | `user-provisioning.helpers.ts:54-69` (**drift** — cited :74) — cause-chain 23505/SQLite-unique probe used by the handshake retry | ✅ symbol / ⚠ line drift |
| 11 | Frozen-six `PUBLIC_OPERATIONS` | `backend/lib/gateway/public-operations.ts:36-59` | `public-operations.ts:36-46` — exactly six entries (`login`, `refreshToken`, `logout`, `registerUser`, `recitationReadings`, `_health`), `PUBLIC_OPERATIONS` set built at `:59` | ✅ exact |
| 12 | Journey harness | `test/workflows/AGENTS.md`; `provisionAdminActor`; `SpiedFanoutTransport`; `test/helpers/db-cleanup.ts:83` (JSDoc 72-82); `docs/testing/workflow-journey-tests.md` | `test/workflows/AGENTS.md` present (rules §34-43 name both helpers). `provisionAdminActor` defined at `test/workflows/helpers/actor-context.ts:136`. `SpiedFanoutTransport` class at `test/workflows/helpers/spied-transport.ts:49` (implements `FanoutTransportLike`). `withAuditDeleteTriggersSuspended` at `test/helpers/db-cleanup.ts:83-109` with the cited JSDoc at `:72-82`. `docs/testing/workflow-journey-tests.md` exists. Bonus: `test/workflows/admin/` already hosts the two shipped journeys (`admin-user-lifecycle.journey.test.ts`, `admin-user-denials.journey.test.ts`) — confirms task 2.1's "no scaffolding needed" premise | ✅ exact |

## ABSENCE audit — verified by glob/grep, not assertion

| Claimed absent | Command / probe | Result |
|---|---|---|
| `backend/db/repo/teachers/teacher.repository.ts` absent; `teachers/index.ts` exports only `./applicant.repository` at line 1 | `Glob backend/db/repo/teachers/*.ts` → only `applicant.repository.ts` + `index.ts`; `Read backend/db/repo/teachers/index.ts` → line 1 is `export * from "./applicant.repository";` (sole line) | ✅ confirmed absent — clean CREATE target for task 2.2 |
| `backend/services/admin/admin-gate.helpers.ts` absent | Module **EXISTS** (90 lines): `assertActorAdmin(actorId, locale, outerTx?)` at `:59-90` + `toAuditActionType` at `:25-44`; header claims verbatim extraction from `user-management.service.ts`. `assertActorAdminActive` does **not** exist anywhere (`grep assertActorAdminActive backend/` → no matches). `backend/services/admin/index.ts` exports only `./audit.service` + `./user-management.service` — gate module is NOT barrel-exported. Introduced by commit `a259524` ("plan: add new plans", 2026-09-01 02:17), the file's only commit | ❌ ABSENCE CLAIM FALSE → Finding 1 (consume-and-extend; ledger row `D-GATE-LANDED`) |
| NO teacher-certification affordance under `frontend/views/admin/**` | `grep -i "certif\|makeEvaluator\|isEvaluator" frontend/views/admin` → only **read-only status** hits: `TeacherApplicationStepper.tsx` (certified step label/icon), `TeacherApplicationCard.tsx` (read-only `isEvaluator` display), `DirectoryStatusDetails.tsx` (`certified` chip). Broader probes `grep -i "adminCertify\|certifyTeacher\|cold.?start" frontend/` hit only auth-memory comments in `AuthProvider.tsx` / `refreshMemoryToken.ts`; `frontend/graphql/sharedDocuments/admin` has zero certification matches | ✅ confirmed absent (D-UI deferral scope true: users + plans surfaces shipped, no certify action) |

## Instrumentation/instruction-file confirmations (needed by later tasks)

- `.agents/instructions/backend.instructions.md`, `frontend.instructions.md`, `tests.instructions.md` — **all three exist** (Glob `.agents/instructions/*.instructions.md`).
- `frontend/graphql/sharedDocuments/AGENTS.md` — **exists** (task 4.1's cited per-directory rule file is real).

## Finding 1 — Shared admin gate pre-landed (D-GATE-SHARING branch resolution)

The plan anticipated a first-lander race with DEV3-022c/022d over the shared gate module (specs
REQ-004 extraction-collision note; ledger D-GATE-SHARING). In-tree state resolves the race against
DEV3-018 as creator: **`backend/services/admin/admin-gate.helpers.ts` already exists** and the
DEV3-016 service already consumes it (private copy deleted; import at
`user-management.service.ts:65`). Consequences for downstream tasks:

- Task 2.4's "CREATE" verb becomes **consume-and-extend additively**: append
  `assertActorAdminActive` (role gate + governance clause, ordering `isDeleted → isBlocked →
  suspended`) to the existing file; add `export * from "./admin-gate.helpers";` to
  `backend/services/admin/index.ts` (currently absent from the barrel); the byte-parity extraction
  step is already satisfied by the sibling wave, but the DEV3-016 61-test + 3-test regression lock
  must still be re-run green.
- The verbatim-extraction attribution in the helper's own header (`admin-gate.helpers.ts:1-8`)
  matches the plan's byte-parity expectation; no fork of role-check semantics exists in-tree.

## Anchor-drift register (cited → actual, for downstream consumers)

| Cited (tasks.md / specs.md) | Actual |
|---|---|
| `user-management.service.ts:240-271` (private `assertActorAdmin`) | gate now at `admin-gate.helpers.ts:59-90`; call sites `user-management.service.ts:231/271/342/395` |
| `user-management.service.ts:503-580` (method ranges) | file is 445 lines; `getUserActivity :186`, `getUserDetail :225`, `createUser :265` |
| `user-management.service.ts:67` (`withTransaction` import) | `:62` |
| `user-management.service.ts:849` (`createUser` start, specs.md §1) | `:265` |
| `notification-engine.service.ts:77-82` (`NotificationEngineCallOptions`) | `:35-38` |
| `notification-engine.service.ts:327-369` (`emitForUser`/`publishReceipts`) | `emitForUser :43-78`, `publishReceipts :117-123` (file = 182 lines) |
| `user-provisioning.helpers.ts:74` (`isUniqueViolation`) | `:54-69` |

All drifts are **pure numeric-anchor staleness** — every symbol exists with the exact signature and
semantics the downstream tasks assume. No re-scoping required.

---

## Files Changed

- CREATED `ai/plans/sprint_3/dev3-018-cold-start-bootstrapping-direct-sheikh-c/outcome/0.2-outcome.md` (this file)
- MODIFIED `ai/plans/sprint_3/dev3-018-cold-start-bootstrapping-direct-sheikh-c/deferred-items.md` — appended ONE row (`D-GATE-LANDED`, status ✅, RESOLVED-REFERENCE) recording the pre-landed gate module and the consume-and-extend branch for task 2.4
- MODIFIED `tasks.md` — flipped the `0.2`, `0.2.QL`, `0.2.SR` checkboxes only

## Files Deliberately NOT Changed

- Nothing under `backend/db/schema/` or `backend/db/migration/` (zero-drift gate; no edits attempted)
- No source files anywhere — this task is audit-and-record only; the drifted production anchors were left untouched (never inline-patch to match plan prose)
- `backend/services/admin/admin-gate.helpers.ts`, `backend/services/admin/index.ts` — foreign layer; adjustments belong to task 2.4 under its own subagent

## Verification commands & results

- `git status --short` at dispatch → empty (clean tree; observed concurrently with task 0.1's baseline landing: `deferred-items.md` + `outcome/0-baseline-outcome.md` appeared mid-audit)
- Reads/Globs/Greps enumerated under each row above (anchors quoted verbatim from the live files)
- `git log --oneline --diff-filter=A -- backend/services/admin/admin-gate.helpers.ts` → single commit `a259524` ("plan: add new plans", 2026-09-01)
- `0.2.QL`: `bun run scripts/health/sub-loop.ts ai/plans/sprint_3/dev3-018-cold-start-bootstrapping-direct-sheikh-c/outcome/0.2-outcome.md --lifecycle duplicates` — **actual behavior: exit 1 with the tool's own no-op on Markdown**. tsgo stage passed for the file (no diagnostics), then oxlint reported `No files found to lint. Please check your paths and ignore patterns.` (0 files, 303 rules, 64ms) and sub-loop halted per its stop-at-first-failure rule. The tool structurally cannot lint `.md` artifacts; per the wave instructions this recorded no-op counts as the check being satisfied (not forced). No code stages (biome/lint/duplicates) were reached.

## Cross-file dependencies discovered

- Task 2.4: consume-and-extend `admin-gate.helpers.ts` (+ add barrel export); the "create" branch and its re-verify-absence precondition are already settled.
- Task 2.5: `AdminUserManagementService.getUserDetail` signature confirmed `(userId, locale, actorId, outerTx?)` at `:225-230` — matches plan §4.4 usage; `NotificationEngine.emitForUser` in-tx branch returns a receipt WITHOUT publishing (`:65-68`), so the post-commit `publishReceipts([receipt], locale, options)` pattern is structurally supported.
- Task 2.1: `test/workflows/admin/` already hosts two journeys; harness helpers confirmed at their cited homes.
- Task 4.1: `frontend/graphql/sharedDocuments/AGENTS.md` exists (contrary to the plan's "if present" hedge).

## Deviations

- The task text mandates a ❌ ledger row for any failed verification claim. The failed claim here (plan-asserted absence of `admin-gate.helpers.ts`) blocks nothing — REQ-004's own collision rule pre-defines the resolution, and the ledger's glyph policy (0.1 deviation, Phase-5 gate `grep -c "❌\|⚠️" = 0`) forbids ❌ rows. Recorded instead as `D-GATE-LANDED` with status ✅ / RESOLVED-REFERENCE. No other deviations.

## 0.2.SR self-check record

- Every EXISTING claim above carries a live-verified `path:line` anchor re-read during this audit.
- Every ABSENCE claim was proven by a Glob/Grep command whose negative result is quoted above.
- No prose-only artifact was treated as real (e.g. `TeacherRepository` mentions in docs were NOT counted as existence).
