# Task 7.1 Outcome — Canonical Doc: `docs/admin/cold-start-certification.md`

**Date:** 2026-09-02 · **Requirements:** REQ-080

---

## What was done

CREATED `docs/admin/cold-start-certification.md` (~24 KB) as the canonical reference for the
admin cold-start certification surface, following the house style of
`docs/admin/user-management.md` (header block with Domain/Specs/Status; `1. Why` → `2. Pattern`
→ `3. Rules` → `4. What NOT to Do` → `5. Rollout Summary` → `6. Related Documents`).

Mandatory sections (per tasks.md 7.1) all present:

1. **§2.1 Mutation contract + `makeEvaluator` committee semantics (FR-3.9/REQ-019, D7)** — exact
   SDL, `$all` conjunction, three-layer `default true` (SDL arg / resolver coalesce / service
   coalesce), `false` = teaches-but-cannot-evaluate.
2. **§2.2 Create-vs-elevate decision rule (D2)** — row-absent INSERT into certified state vs
   guarded `UPDATE … WHERE is_approved = false … RETURNING`; zero-row ⇒ cold-path re-read ⇒
   conflict or unexpected-internal (never silent no-op); rejected upsert/read-then-write
   alternatives recorded.
3. **§2.3 Applicants-finalize rule** — unconditional `status='passed'` + `cooldown_until=NULL`
   supersession (INV-TV1(b) supreme-authority reading; pass ⇒ cooldown cleared per the lifecycle's
   own rule); attempts-untouched, row-never-deleted, absence recorded.
4. **§2.4 Override-audit shape (D8 JSON + JR-C-1)** — fixed 3-field
   `{ makeEvaluator, applicantRow, elevation }` table; single-writer in-tx emission; denials emit
   ZERO audit rows; B-2/F-D3 reconciliation noted (`entity: "user"`).
5. **§2.5 Cross-entity purity envelope (REQ-020)** — write set is exactly
   `teacher` / `applicants` / `audit_logs` / `notifications`; A.7 governance columns read-only;
   D4 receipt/publish split documented inline.
6. **§2.6 Actor-governance blast-radius divergence (REQ-031/D1)** — why the role-only gate
   leaves the governance window open (no filter in `createGraphQLContext` /
   `UserRepository.findById`); `assertActorAdminActive` deleted→blocked→suspended deterministic
   order; double-read TOCTOU + ledger rows `D-GATE-DOUBLE-READ` / `D-GATE-NULL-READ` recorded as
   the documented divergence boundary.
7. **§2.7 Concurrency rulings (REQ-042)** — PK unique constraint (insert path) + guarded
   predicate under the PG row lock (elevate path); explicit NON-usage statement: no
   `SELECT FOR UPDATE`, no advisory locks, no Redis; accepted residual windows (mid-tx governance,
   snapshot eligibility) documented.
8. **§2.8 Idempotency ruling (REQ-043)** — conflict-not-keys; outside the
   `docs/IDEMPOTENCY.md` mandated set; replay precedes any second write/audit/notification.
9. **§2.9 Error-code table** — copied VERBATIM from plan.md §3.3 (all 10 rows incl. emission
   points); REQ-050 closed-taxonomy note appended.
10. **§4 What-NOT-To-Do list** — all five mandated bullets (no second certification writer; no
    `cooldown_until` semantic drift; no purchase/evaluation-lifecycle routing; no widening beyond
    admin callers; no inline emitter-locale re-litigation) plus house-style additions (no locking,
    no out-of-tx audit, no PII in details, no teacher rows for non-teachers, specs read-only).

## Grounding

Every claim cites the shipped code: `cold-start-certification.service.ts` (pipeline, gate order,
audit shape, D12 locale), `admin-gate.helpers.ts:105-137` (`assertActorAdminActive`),
`teacher.repository.ts:85-133` (two write shapes), `applicant.repository.ts:174-197`
(`finalizeOnCertification`), `admin-teachers.mutation.ts:36-69` (SDL/scopes/resolver), plus
`user-management.md`, `applicant-lifecycle.md`, and `realtime-engine.md` contracts. Outcome files
consumed: 6.5 (reconciliation — B-2/F-D3/F-1 fixes), 5.3 (end-gates), 6.2 (backend review), 6.4
(pentester), 2.5 (service outcome), 2M (midpoint). Spec anchors quoted from `specs.md`
(REQ-010..020, REQ-031, REQ-040..043).

## 7.1.QL — Quality Loop (recorded behavior)

`bun run scripts/health/sub-loop.ts docs/admin/cold-start-certification.md --lifecycle duplicates`
→ tsgo PASS (no errors for the file) → oxlint reports `No files found to lint` (markdown is not
in oxlint's file set) → sub-loop stops with exit 1. This is the **nominally-expected no-op for
`.md` targets** (the loop is a code-file verifier; there is nothing code-checkable in a doc).
Per the 7.1.QL task brief ("sub-loop no-ops on .md; record behavior, treat as satisfied"), the
loop is recorded as satisfied. Exit code 1 is an artifact of the short-circuit-on-first-fail
design, not a defect of the document.

## Files changed

- CREATED `docs/admin/cold-start-certification.md`
- CREATED this outcome file
- EDITED `ai/plans/sprint_3/dev3-018-cold-start-bootstrapping-direct-sheikh-c/tasks.md` —
  checkbox flips: 7.1 and 7.1.QL lines only.

## Deviations

- 7.1.QL exit code is 1 rather than 0 — expected no-op-on-markdown behavior per the task brief;
  treated as satisfied (see above).
