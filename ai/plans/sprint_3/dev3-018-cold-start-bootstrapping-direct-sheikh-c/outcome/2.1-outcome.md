# Task 2.1 Outcome — Journey J-1 Cold-Start Certification (TEST-FIRST, now GREEN)

**Date:** 2026-09-01 · **Requirements:** REQ-075 (J-1), REQ-012..017, REQ-030/031/032, REQ-041/042/043

## Summary

Authored `test/workflows/admin/cold-start-certification.journey.test.ts` covering all 13 steps of journey J-1 against the REAL service layer: one committed cast in a single `beforeAll` `db.transaction` (Admin A certifier, Admin B observer, student probe, governed admin, governed teacher target, cooldown applicant, unapproved elevation teacher) via `provisionAdminActor`/`provisionStudentActor` + `entity-setup` helpers, all registered in `TrackedFixtures`; `SpiedFanoutTransport` injected through the service's `NotificationEngineCallOptions` seam; denials via the try/catch helper + translated substrings; teardown in `afterAll` via `withAuditDeleteTriggersSuspended(() => tracked.cleanup())` plus a post-teardown zero-residue oracle sweep. The parallel-landed service (`backend/services/admin/cold-start-certification.service.ts`, barrel-exported) matched every encoded plan-§4.4 contract (locked import surface `import { ColdStartCertificationService } from "@/backend/services/admin"` verified against reality).

## Files Changed

- CREATED `test/workflows/admin/cold-start-certification.journey.test.ts` (~870 lines, 12 tests, 193 expect() calls)
- CREATED this outcome file; `tasks.md` — flipped the six 2.1* checkboxes only

## Verification (exact commands + results)

- `bun run test/scripts/run-test.ts test/workflows/admin/cold-start-certification.journey.test.ts` — **exit 0, 12 pass / 0 fail, 193 expect() calls** (run 1: green immediately, run 2 after two lint-driven no-behavior fixes: green again).
- `bun run scripts/health/sub-loop.ts ... --lifecycle duplicates` — run 1 exit 1 (tsgo TS2769 `toBe(string|undefined)` — fixed by null-guarding `adminARow`); run 2 exit 1 (sonarjs/different-types-comparison on `id === undefined` — fixed via truthiness guard, the 2.3 recipe); run 3 **exit 0** (tsgo, oxlint, biome, lint:type-aware all pass; duplicates skipped — file outside jscpd scope).
- RED→GREEN history: service landed mid-authoring; suite was authored test-first against the contract, then verified green against reality on first execution (no assertion weakening).

## J-1 step → assertion mapping

1. **Cast committed** — all 7 users + role-child rows exist; cooldown fixture failed+future; elevation fixture unapproved.
2. **Create** — `createUser(role=teacher)` commits users + applicants(pending) + ONE audit(create, Admin A); ZERO teacher rows.
3. **Certify** — teacher{approved, evaluator} + applicants{passed, cooldown null} + ONE override audit (`{makeEvaluator:true, applicantRow:"finalized", elevation:"created"}`) + ONE `evaluation_result` row + EXACTLY ONE envelope to `[target]`.
4. **Cross-actor visibility** — target inbox shows the localized copy; Admin B + student inboxes EMPTY; trio oracle coherent.
5. **Admin B observer** — `getUserDetail` snapshots; `getUserActivity` Create entry attributed to Admin A; full trail oracle newest-first Override→Create, both Admin A.
6. **Student denial** — ForbiddenError; zero teacher/applicant/audit/notification movement; zero envelopes.
7. **Governed admin** (suspended post-provisioning) — ForbiddenError `accountSuspended`; cooldown target byte-identical.
8. **Non-teacher target** — ConflictError `TEACHER_ROLE_REQUIRED`; zero writes; student rows byte-identical.
9. **Governed target** — `TEACHER_ACCOUNT_GOVERNED` + zero writes; after reactivation the same call SUCCEEDS (composition proof).
10. **Repeat** — `TEACHER_ALREADY_CERTIFIED`; audit count pinned at 2; no second notification/envelope; evaluator flag unchanged.
11. **Cooldown supersession** — failed + future-cooldown applicant certified → passed + cooldown null.
12. **Elevation** — `{isApproved:false}` row + `makeEvaluator:false` → approved non-evaluator; audit `elevation:"elevated"`, `applicantRow:"absent"`.
13. **Teardown** — tracked reverse-order hard-delete (audit rows under trigger suspension) + zero-residue oracle sweep across audit/notifications/teacher for all targets.

## Deviations

- Step 5: `getUserActivity` reads `entity_type = 'user'` rows only (verified `admin-user.repository.ts:253`), so the Override row (entity type `teacher`) is asserted via the audit oracle alongside it instead of through the timeline — the spec sentence listing Override on the activity surface is satisfiable only via that oracle; recorded here, not weakened.
- Governed-actor/target `suspended` flips use committed `db.update` calls (the admin service has no suspension surface; `setUserDeleted` covers `isDeleted` only) — honest state transitions, no permission patching.
