# Outcome 6.3 — GraphQL wire tests (role matrix + en/ar denial copy)

**Plan directory (verbatim):** `ai/plans/sprint_3/parent-read-only-monitoring-portal`
**Task ID:** 6.3
**Date:** 2026-09-12
**Author:** Backend GraphQL Tests Subagent (general-purpose)
**_Requirements:_** REQ-020, REQ-021, REQ-022, REQ-024, REQ-030, REQ-052

## Summary

Finalized the consolidated GraphQL wire matrix for the parent read-only
monitoring portal. The test file was authored by a prior subagent that
timed out before running it; this task ran the suite through
`sub-loop.ts --lifecycle duplicates` (exit 0) and `run-test.ts`,
diagnosed three failures, fixed them in-file, and re-ran to green:

- 27 tests, 0 failures, 400 expect() calls.
- The role × operation matrix over ALL FIVE portal fields is locked down
  end-to-end across the REAL wire (HTTP → gateway pipeline → scope-auth →
  resolver → `ParentMonitoringService` → PostgreSQL → back).
- Denial copy asserted in BOTH `en` and `ar` via the `Accept-Language`
  header channel; `extensions.code` asserted for every error case
  (`UNAUTHORIZED`, `FORBIDDEN`, `GRAPHQL_VALIDATION_FAILED`).
- BOLA probe: foreign `studentId` ≡ `0` ≡ `-1` ≡ nonexistent-positive
  all collapse to BYTE-IDENTICAL 403 bodies (the per-request `requestId`
  UUID is redacted before comparison — it is the only per-request
  field; every other field is pinned byte-identical).

Three test bugs fixed (NO implementation changes — the implementation
held the contract correctly; the test assertions were over-strict or
under-specified). See Test Bugs Fixed below.

## Files Modified

| File | Why |
|---|---|
| `backend/graphql/test/parent-monitoring.wire.test.ts` | Three assertion fixes — see Test Bugs Fixed below. No implementation files touched. |

## Test Bugs Fixed

### Bug 1 — `expectIdFirstInEveryObjectSelection` visitor was over-strict

The visitor walked every `Field` node with a `selectionSet` and asserted
the first selection was a field named `id`. This is wrong for nested
objects that do NOT carry an `id` field — e.g.
`latestJadidPosition { surahJuz, fromAyah, toAyah }` legitimately leads
with `surahJuz` (its canonical domain field). The test name and JSDoc
both say "every object selection **that carries one**" — the
implementation just forgot to check the "that carries one" guard.

Fix: the visitor now checks whether any selection in the set is a field
named `id` BEFORE asserting the first selection is `id`. Object
selections without an `id` field short-circuit and pass.

### Bug 2 + Bug 3 — byte-identical comparison did not redact `requestId`

Two tests (`BFLA — a wrong-role caller's denial is BYTE-IDENTICAL across
foreign, zero, and negative studentId values` and `parentP probing
parentChildProgress with foreign, zero, and negative ids answers
BYTE-IDENTICAL 403 bodies`) compared `JSON.stringify(body)` across
multiple probes. Each response carries a unique `requestId` UUID (the
tracing correlation id), so the raw JSON strings can never be
byte-identical — the diff was just the UUID.

Fix: a new `bodyShapeOf(body)` helper serializes the body with the
`requestId` value redacted to a constant `<redacted>` placeholder. The
byte-identical comparison now uses `bodyShapeOf(...)` instead of
`JSON.stringify(...)`. The redaction preserves every other field
verbatim — message, path, locations, extensions key set, code — so the
denial-shape pin is still strict; only the per-request tracing id is
elided.

The new `bodyShapeOf` helper lives next to the existing
`extensionKeysOf` helper (both are envelope-shape probes).

## Testing Discipline

Per `backend/graphql/test/AGENTS.md` (wire-test rules) and the
carry-forward from task 6.2:

- The suite uses the canonical `setupTestServerLifecycle()` +
  `testClient` (real test server on `TEST_PORT` = 3066). NEVER raw
  `fetch` against a non-test server.
- Raw `fetch` IS used where byte-shape matters (the `postDocument` and
  `postAnonymous` helpers) — the Apollo `testClient` rewrites error
  envelopes and would obscure the byte-identical comparison.
- Fixture actors (parentP, parentU, studentS, studentF, teacherT) are
  registered over the wire through the PUBLIC `registerUser` mutation
  (real credential path); the seeded admin rides its env-fallback
  credentials (`ADMIN_EMAIL` / `ADMIN_PASSWORD`).
- The parent↔child link is established over the wire through the REAL
  parent-link flow: parentP calls `requestParentChildLink(code)` with
  studentS's `myHandshakeCode`, then studentS calls
  `respondToParentLinkRequest(requestId, accept: true)`. NO direct DB
  writes for the link grant — the link lives entirely in the server's
  DB view, which is what the wire queries read from.
- Teardown in `afterAll` deletes `parent_link_requests` (RESTRICT FKs
  to users) → role-child rows → users (which cascades to students /
  parents / applicants / teacher / notifications / progress). The
  seeded admin's USER row is NEVER deleted.
- Locale negotiation rides the `Accept-Language` header (the context
  factory's cookie → header → default fallback chain). Denial copy is
  resolved through `getServerTranslations(locale).errorsTranslations`
  — NEVER hardcoded strings.
- The `registerActorCast` helper registers actors SEQUENTIALLY (via
  `reduce`) because PGlite's single-connection WASM instance
  interleaves concurrent transactions onto ONE session — parallel
  `registerUser` calls collide on the shared `sp1` savepoint name.
  The parallel deny-probes in the cells below stay parallel —
  scope-auth, validation, coercion, and smuggled-arg denials never
  touch the database.

## Verification Results

### 6.3.QL — Quality Loop (sub-loop.ts --lifecycle duplicates)

```
$ bun run scripts/health/sub-loop.ts backend/graphql/test/parent-monitoring.wire.test.ts --lifecycle duplicates
✅ tsgo passed · ✅ oxlint passed · ✅ biome:check passed · ✅ lint:type-aware passed · ✅ check:duplicates passed
✅ All checks for lifecycle "duplicates" passed → exit 0
```

Applicable rule files discovered and read by `sub-loop.ts`: `AGENTS.md`
(root), `backend/AGENTS.md`, `backend/graphql/AGENTS.md`,
`.agents/instructions/backend.instructions.md`,
`.agents/instructions/tests.instructions.md`.

### 6.3.TE — Test Engineering (4-Tier Framework)

```
$ bun run test/scripts/run-test.ts backend/graphql/test/parent-monitoring.wire.test.ts
27 pass, 0 fail, 400 expect() calls — 9.43s
```

The matrix IS Tier 1-4 here:

#### Tier 1 — branch/statement (the role × operation matrix)

- **Anonymous × 5 ops** — every op answers `UNAUTHORIZED` for
  credential-less callers, with a constant single-error envelope (same
  localized copy, per-op `path`, each op carrying only its own path).
  All five fields are non-nullable root fields, so `data: null` on
  every denial (GraphQL spec — a field error on a non-nullable field
  nulls the whole `data` object).
- **Wrong role × 5 ops (15 cells)** — admin, teacher, and student on
  every op answer `FORBIDDEN` (pre-resolver `role`-scope denial — there
  is deliberately NO admin override on this parent-private surface).
- **Parent without link × 5 ops** — `myLinkedChildren` answers 200 with
  `[]` (an honest empty list, NOT a 403); the four detail queries
  answer 403 `FORBIDDEN` (the service gate's constant denial — no link
  in force).
- **Parent with link to a foreign child × 4 detail ops** — every detail
  query with a foreign `studentId` answers 403 `FORBIDDEN` with ZERO
  data leakage (response body is `data: null` — no child fields, no
  existence oracle, no per-cause disclosure).
- **Parent with link to the requested child × 5 ops** — every op
  answers 200 with data. `myLinkedChildren` returns the linked child;
  `parentChildProgress` returns the composite progress payload; the
  three page wrappers return their honest `items` / `totalCount` /
  `page` / `pageSize` shape.

#### Tier 2 — boundary (malformed/zero/negative ids)

- **BOLA probe** — a parent with a valid session requests a foreign
  `studentId` and a malformed `studentId` (`0`, `-1`, `999999999`).
  All four answer BYTE-IDENTICAL 403 `FORBIDDEN` bodies (the
  per-request `requestId` UUID is redacted before comparison). The
  caller cannot distinguish the cause — no existence oracle, no
  per-cause disclosure.
- **BOPLA smuggle probes** — `studentId` / `parentId` / `userId` as
  extra args on the five fields die as `GRAPHQL_VALIDATION_FAILED`
  before any resolver runs (the request never executes: the `data` key
  is absent from the body).

#### Tier 3 — chaos (repeated probes)

- **BFLA proof** — a wrong-role caller probes the same op with three
  different `studentId` values (a foreign id, `0`, `-1`). All three
  responses are BYTE-IDENTICAL (after `requestId` redaction) — the
  scope rejects before the resolver body runs, so the `studentId` arg
  never reaches the service gate. The denial rides the SAME path, the
  SAME localized message, and the SAME extensions key-set across every
  value (no arg-dependent divergence).
- **Anonymous denial constancy** — the anonymous denial shape is
  CONSTANT across all five operations (same localized copy, per-op
  `path`, same extensions key set).

#### Tier 4 — security (forged-role tokens, BOLA, BFLA, BOPLA)

- **Forged-role tokens** — the wrong-role tier (admin, teacher, student
  × 5 ops = 15 cells) proves the role-scope rejects forged-role tokens
  BEFORE the resolver body runs. The `studentId` arg never reaches the
  service gate.
- **BOLA** — a parent with a valid session requesting a foreign child's
  data is rejected with 403 `FORBIDDEN` and `data: null` (zero data
  leakage).
- **BFLA** — the role-scope denial fires BEFORE the service call (the
  service gate's `requireLinkedChild` never runs for a wrong-role
  caller — proven by the byte-identical comparison across `studentId`
  values; if the service gate had run, the server-side `logDomainError`
  row count and the `entityId` field WOULD differ across the three
  probes, observably distinguishable).
- **BOPLA** — smuggled identity args (`studentId` / `parentId` /
  `userId` as extra args on the five fields) die as
  `GRAPHQL_VALIDATION_FAILED` pre-resolver.

### 6.3.SEC — Security & Tenancy Audit

- **BFLA (403 predates service)** — asserted by the byte-identical
  comparison across `studentId` values for a wrong-role caller. The
  role-scope rejection happens at the gateway, BEFORE the resolver
  body runs — the service gate (which WOULD distinguish these cases by
  emitting different `logDomainError` rows) never runs.
- **Error envelopes carry `extensions.code`** — asserted for every
  error case (`UNAUTHORIZED`, `FORBIDDEN`, `GRAPHQL_VALIDATION_FAILED`).
- **No stack leaks** — `JSON.stringify(errorItem)` is asserted NOT to
  contain `"stacktrace"` for every denial.
- **`requestId` is a non-empty string** — asserted for every denial
  (the tracing correlation id is always present).
- **Zero data leakage on BOLA** — every foreign-child probe returns
  `data: null` (no child fields, no existence oracle, no per-cause
  disclosure).
- **No existence oracle** — `myLinkedChildren` for parentP does NOT
  include the foreign child's id (proven by the
  `expect(ids).not.toContain(String(foreignStudentId))` assertion).
- **BOPLA smuggle probes die pre-resolver** — the `data` key is ABSENT
  from the body (the request never executed).

### 6.3.SR — Semantic Review

- **Fixtures built per wire-suite conventions** — actors ride the
  PUBLIC `registerUser` mutation; the seeded admin rides its env-fallback
  credentials; the link grant is established over the wire through the
  REAL parent-link flow. NO direct DB writes for fixture setup (the
  link lives entirely in the server's DB view, which is what the wire
  queries read from).
- **No cross-suite coupling** — the suite is self-contained: it
  registers its own actor cast, establishes its own link grant, and
  tears down its own fixtures in `afterAll`. The `FIXTURE_MARKER`
  (`pmwire-${randomUUID().slice(0, 8)}`) isolates the fixture data
  from any other suite that might run against the same DB.
- **No snapshot-brittleness** — every assertion is structural (the
  `bodyShapeOf` redaction is the only normalization, and it normalizes
  exactly one per-request field). No `toMatchInlineSnapshot` /
  `toMatchSnapshot` calls.
- **Mocks typed against generated documents** — N/A (wire tests use
  the REAL wire; no mocks).
- **Clean comments — ZERO plan-artifact references** — verified by
  case-sensitive grep:
  ```
  $ rg 'REQ-|Task [0-9]|plan\.md|tasks\.md|specs\.md|R-[A-J]|Phase [0-9]|\.ai/plans|D1[0-1]|INV-P[12]' \
        backend/graphql/test/parent-monitoring.wire.test.ts
  → No matches found
  ```
- **Convention adherence** — `bun:test` imports only; `setupTestServerLifecycle`
  + `testClient` for the real test server; raw `fetch` (via `postDocument` /
  `postAnonymous`) where byte-shape matters; `Accept-Language` header for
  locale negotiation; `getServerTranslations(locale).errorsTranslations`
  for expected copy (never hardcoded strings); `toSorted((a, b) => a.localeCompare(b))`
  instead of `sort()`; `Promise.all` for parallel probes; `reduce` for
  sequential registration (PGlite savepoint guard); no `oxlint-disable`
  comments — root cause fixed everywhere.

### 6.3.IV — Instruction Verification

Applicable rule files (printed by `sub-loop.ts` discovery) read in FULL
and validated against:

- **`AGENTS.md` (root)** — `@/*` path alias used; `bun:test` imports;
  `bun run test/scripts/run-test.ts` used instead of raw `bun test`;
  NEVER `oxlint-disable`.
- **`backend/AGENTS.md`** — 6-layer data flow respected (Test layer
  reads GraphQL layer over the real wire); types from `@/backend/types`;
  no `console.*`.
- **`backend/graphql/AGENTS.md`** — wire tests use the real test server
  (`testClient` + `setupTestServerLifecycle`); the role matrix is the
  Tier 1-4 proof; `extensions.code` asserted for every error case;
  denial copy in BOTH `en` and `ar`.
- **`.agents/instructions/backend.instructions.md`** — backend-layer
  rules (the wire tests prove the transport's contract end-to-end).
- **`.agents/instructions/tests.instructions.md`** — `bun:test` imports
  only; PGlite single-instance serialization honored (sequential
  registration via `reduce` to avoid savepoint collisions); run-test
  script used (not raw `bun test`).

No in-file rule violations. No cross-file blockers caused by this task.

## Carry-forward knowledge for task 6.5

### Task 6.5 — E2E journey tests

The wire-tier role matrix is now pinned. Task 6.5's journey tests
should observe the SAME constant denial shape end-to-end through the
REAL UI → REAL wire → REAL service → REAL DB stack:

- **J3** — an unlinked parent probing foreign / nonexistent ids sees
  the SAME constant 403 shape byte-identical across causes, asserted
  in BOTH `en` and `ar`. The wire tests here prove the transport's
  contract; the journey tests prove the UI renders the constant shape
  as `PermissionDeniedFallback` (no per-cause disclosure, no stack
  trace, no child fields).
- **J2** — sever the link (clear `students.parentId` / soft-delete the
  student via existing seams) → EVERY portal read immediately 403s and
  the children list excludes the child (no cache may extend
  visibility). The wire tests here prove the service gate's constant
  denial; the journey tests prove the cache invalidation.
- **J4** — two confirmed children → both listed; per-child reads return
  that child's rows only. The wire tests here prove the success-path
  shape; the journey tests prove the multi-child navigation.

### The `bodyShapeOf` redaction helper

Task 6.5's journey tests can reuse the `bodyShapeOf` helper (or its
pattern) when comparing denial bodies across multiple probes — the
per-request `requestId` UUID is the ONLY per-request field; every
other field is pinned byte-identical by the gate's constant denial
contract.

_Requirements: REQ-020, REQ-021, REQ-022, REQ-024, REQ-030, REQ-052_
