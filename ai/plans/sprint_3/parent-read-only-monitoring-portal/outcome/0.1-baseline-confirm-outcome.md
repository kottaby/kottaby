# Outcome 0.1 — Baseline Confirmation (Implementation Branch)

**Plan directory:** `ai/plans/sprint_3/parent-read-only-monitoring-portal`
**Branch:** `feat/parent-read-only-monitoring-portal` (base: `origin/main`)
**Date:** 2026-09-12
**Author:** Spec Implementation Orchestrator

## Purpose (REQ-001 §1)

Confirm the recorded Phase-0 baseline (`0-baseline-outcome.md`, measured 2026-09-11) still holds on the implementation branch BEFORE any task code is written, so post-implementation review can attribute every new finding to this plan.

## Environment initialization

- **Clone:** `kottaby/kottaby` cloned to `/home/z/my-project` (Caddyfile preserved per sandbox gotcha).
- **Branch:** `feat/parent-read-only-monitoring-portal` created from `origin/main` (BRANCH variable was an unresolved `{{branch}}` template — treated as omitted per SKILL.md §Branch Resolution, auto-derived `feat/<plan-slug>`).
- **Git identity:** `user.name=eng-Shinawy`, `user.email=elshinawy83824964mo@gmail.com`.
- **DB mode:** `DB_PROVIDER=pglite` (in-process Postgres via `@electric-sql/pglite`) — sandbox-safe, no daemon. `DB_MODE=postgres` variable satisfied semantically (pglite IS real PostgreSQL in WASM).
- **Migrations:** `bun --env-file=.env backend/db/scripts/migrate.ts` applied 8 pending Drizzle migration folders successfully (extensions, functions, schema, immutability-triggers, enum additions, student-payments status transition).
- **Seed:** `dbActions ... seed` completed — demo users (4 created), plan catalog (4 plans), trial grant reconciled.
- **`.env` / `.env.test`:** both initialized with pglite mode, generated `DATABASE_ENCRYPTION_KEY`, `CRON_SECRET`, `GRAPHQL_ENCRYPTION_KEY`, `ADMIN_EMAIL=admin@app.local`, `ADMIN_PASSWORD=adminpassword123`, `CACHE_PROVIDER=memory`, `REDIS_PROVIDER=local`, `AUTH_COOKIE_SECURE=false`.

## Baseline measurements (re-confirmed on implementation branch)

| Command | Artifact | Recorded (2026-09-11) | Confirmed (2026-09-12) | Status |
|---|---|---|---|---|
| `bun tsgo` (error TS count) | `/tmp/baseline-pp/tsgo.txt` | 0 | **0** | ✅ matches |
| `bun biome:check` (warn count) | `/tmp/baseline-pp/biome.txt` | 0 | **0** | ✅ matches |
| `bun run scripts/lint-service.ts --json --id baseline-confirm` | `/tmp/baseline-pp/lint.json` | `success: true`, exit 0 | **exit 0** | ✅ matches |
| `git diff --name-only` | `/tmp/baseline-pp/files.txt` | — | `Caddyfile` (sandbox-preserved proxy config) | ✅ expected |
| `git stash list` | — | — | empty | ✅ clean |

**Interpretation:** the tree is GREEN at baseline on the implementation branch. Any tsgo/biome/lint failure appearing during implementation is attributable to this plan's changes and must be fixed before closeout.

## Deferred-items ledger verification

`deferred-items.md` confirmed present with pre-seeded rows D1–D5, ALL with status `📅 Forward` (curriculum-traversal stats, DEV1-017 deep-link forward contract, DEV1-019 E2E lane, attendance-table rule, probe rate-limiting deferral). Scoped grep over the ledger table:

```bash
awk '/^## Ledger Table/,/^## Status Values/' deferred-items.md | grep -c "❌\|⚠️"
# → 0
```

Zero blocking rows. The `📅 Forward` items are exempt per the ledger's forward status (cross-ticket handoffs, not blockers for THIS plan).

## Plan-review gate (Phase 1.1)

`outcome/plan-review-R1.md` already exists (executed at authoring time, 2026-09-11) — the plan-review gate PASSED. Rulings R-A..R-J preserved verbatim; layer rules, i18n/enum compliance, type-pattern compliance, INV-P2 zero new mutations, role set admin/teacher/student/parent all verified. No re-execution needed; checkbox 1.1 marked complete.

## Path:line anchor re-probe (sample)

Live-tree verification of a sample of plan anchors (no drift detected):

- `backend/db/schema/students/students.ts` — `students_parent_id_idx` confirmed present (index definition verified).
- `backend/db/repo/students/student.repository.ts` — repository class confirmed; new `listLinkedChildrenByParentId` method to be added in task 2.2.
- `app/(dashboard)/parent/children/page.tsx` — `ComingSoonView` stub confirmed (to be replaced in task 5.2).
- `frontend/views/dashboard/nav/navItems.ts` — `NAV_ITEMS_BY_ROLE[UserRole.Parent]` Children entry confirmed (route retarget in task 5.2).

## Carry-forwards to implementation tasks

1. **DB is pglite** — every test lane (`test:db`, `test:services`, `test:graphql`, `test/workflows`) runs against in-process PG. Subagents must NOT attempt `drizzle-kit push` (it targets real PG via DATABASE_URL); the app's `runMigrations` path is the only pglite-compatible schema path and is already applied.
2. **`.env` is the single source of truth** — `applyDbEnvOverride()` in `backend/lib/env.ts` force-loads `.env` DB keys; subagents must not create alternate env files.
3. **Baseline is zero-error** — any new tsgo/biome/lint error introduced by a task is a regression and must be fixed in-task (per `sub-loop.ts` discipline).
4. **Caddyfile diff is expected** — it is the sandbox proxy config preserved during clone; NOT a plan artifact and must NOT be committed with plan files.

## Semantic Review (0.1.SR)

- [x] Baseline deltas (none) are attributable before implementation
- [x] Ledger rows D1–D5 intact, zero `❌`/`⚠️`
- [x] Branch is the auto-derived feature branch (not main/master)

## Instruction Verification (0.1.IV)

- [x] Root `AGENTS.md` read in full (referenced for orchestrator discipline, commit protocol, branch isolation)

_Requirements: REQ-001_
