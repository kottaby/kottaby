# Outcome — Task 6.5: Journey tests J1–J4

**Plan:** `ai/plans/sprint_3/parent-read-only-monitoring-portal/`
**Task:** 6.5 Journey tests J1–J4
**Branch:** `feat/parent-read-only-monitoring-portal`
**Date:** 2026-09-13

---

## Summary

Created `test/workflows/parents/parent-monitoring.journey.test.ts` — the cross-actor journey suite covering all four portal journeys (J1–J4) against REAL services on the REAL pglite test database. 13 tests, 223 `expect()` calls, all green via `bun run test/scripts/run-test.ts`. Sub-loop exit 0 at the deepest lifecycle stage (`duplicates`). Idempotent teardown proven (three consecutive green runs).

## Files created

- `test/workflows/parents/parent-monitoring.journey.test.ts` (NEW — ~1480 lines)

## Files NOT modified

No implementation files were modified. The journey exercises the existing `ParentMonitoringService` (task 2.4), `SessionLifecycleService` (existing), `SessionReportService` (existing), `SessionReportNotificationService` (existing), and `setGovernanceFixture` / `TrackedFixtures` / `SpiedFanoutTransport` helpers (existing) as-is — no implementation bugs were revealed; the portal surface held every journey assertion.

## Journey structure (13 tests across J1–J4)

### Cast (provisioned in ONE committing `beforeAll` transaction)

| Actor | Role | Persisted locale | Linked to | Books |
|---|---|---|---|---|
| Teacher T | teacher (certified, `isApproved=true`) | en | — | — |
| Parent P | parent | en | S1 | — |
| Parent P2 | parent | ar | S3, S4 | — |
| Student S1 | student | en | P | σ1 |
| Student S2 | student (foreign / never linked) | en | — | — |
| Student S3 | student | en | P2 | σ3 |
| Student S4 | student | en | P2 | σ4 |

Three sessions (σ1, σ3, σ4) booked + started + completed via the REAL `SessionLifecycleService` lifecycle path (book → start → complete) inside the provisioning transaction. Three idempotency claims registered for teardown.

### J1 — Teacher completion becomes visible to the linked parent (5 tests)

- **J1 step 1** — System: cast committed; σ1/σ3/σ4 completed via real lifecycle; baseline zero reports/homework/report-wave rows.
- **J1 step 2** — Teacher T submits σ1 report + homework (Jadid + Madi blocks): report row lands; homework row lands with both tracks mapped + grades structurally NULL; EXACTLY 2 publishes (one envelope to S1 in en, one to P in en — both recipients' persisted locale is en); engine claim keys are the deterministic per-recipient report keys; notification rows carry the polymorphic session pointer (`relatedEntityType = "session"`, `relatedEntityId = σ1.id`) and the report-ready title in the recipient's persisted locale; the parent-wave body carries counterparty names only.
- **J1 step 3** — Parent P reads σ1 surfaces: `listLinkedChildren` returns [S1] with S1's full name unmasked; `listChildReports` returns exactly one row (σ1's) with the submitted notes + rating + `sessionStatus = completed`; `listChildHomework` returns exactly one row with both Jadid (SurahAlFatihah) + Madi (Juz1) tracks mapped + grades NULL; `getChildProgress` returns `progressRowCount = 0` (progress table has no writers) + latest Jadid/Madi positions echoing σ1's homework tracks; `listChildSessions` returns exactly one row (σ1) with `status = completed` + non-null started/ended stamps. Reads publish zero.
- **J1 step 4** — Deep-link: `parentChildReports(S1).items` contains a row whose `sessionId === σ1.id` — the deep-link target the frontend resolves client-side (the service guarantees the row is in the list; if the link is severed, the entire list is unreadable and a 403 fires).
- **J1 step 5** — DENIAL: P2 (linked to S3/S4 but never to S1) reads S1 via all four per-student surfaces → constant 403 byte-identical across all four (one fingerprint); P2's `listLinkedChildren` is unaffected (returns S3/S4, NOT S1); exactly 4 bounded `logDomainError` logs (one per denial, code = `FORBIDDEN`); zero publishes.

### J2 — Severed link revokes access immediately (2 tests)

- **J2 step 1** — Sever via clearing `students.parent_id` (committed write): P's `listLinkedChildren` EXCLUDES S1 (no cache extends visibility — the list re-reads every call); EVERY per-student portal read of S1 immediately 403s — `getChildProgress`, `listChildSessions`, `listChildReports`, `listChildHomework` all return the SAME constant `FORBIDDEN` shape (one fingerprint across all four); the σ1 report/homework rows STILL EXIST in the DB (severance does not erase history — they are merely unreadable through the portal); exactly 4 bounded logs; zero publishes.
- **J2 step 2** — Sever via soft-delete (re-link + flip `users.isDeleted = true` via `setGovernanceFixture`): same constant denial shape — no branch disclosure between the two severance paths (cleared-parentId ≡ soft-delete); P's list EXCLUDES S1 (the list predicate joins `users` and filters `is_deleted = false`); exactly 4 bounded logs; zero publishes.

### J3 — Unlinked parent probes foreign/nonexistent ids (3 tests)

- **J3 step 1** — EN locale: P2 probes four mismatch causes — foreign-severed (S1 — soft-deleted AND severed from P, but for P2 it is simply foreign), never-linked (S2 — exists but was never linked to anyone), nonexistent (`ABSENT_STUDENT_ID = 2_000_000_000`), malformed (`MALFORMED_STUDENT_ID = 0` — non-positive). ALL four produce byte-identical `{code: "FORBIDDEN", message: <en copy>}` (one fingerprint). Abuse-repeat arm: re-probing the same foreign id yields the SAME fingerprint — no state drift across repeats. P2's `listLinkedChildren` stays unaffected (returns [S3, S4]). Exactly 5 bounded logs (4 initial probes + 1 repeat); zero publishes.
- **J3 step 2** — AR locale: the same four probes — ALL produce byte-identical `{code: "FORBIDDEN", message: <ar copy>}` (one fingerprint, different from the en fingerprint). Exactly 4 bounded logs; zero publishes.
- **J3 step 3** — Cross-locale: the `code` is `FORBIDDEN` in BOTH en and ar (the wire `extensions.code`); the message DIFFERS across locales (locale-composed copy — `ERRORS_EN.forbidden` ≠ `ERRORS_AR.forbidden`); observer-safe in both directions, never a fixed English string.

### J4 — Multi-child parent switches views (3 tests)

- **J4 step 1** — Teacher T submits σ3 + σ4 reports/homework (distinct homework blocks per session so cross-contamination is detectable): each submission publishes EXACTLY 2 envelopes (one to the student in en, one to P2 in ar — P2's persisted locale is ar); recipient-locale composition: σ3's parent-wave title is `NOTIFS_AR.eventSessionReportReadyTitle` and body is `NOTIFS_AR.eventSessionReportReadyParentBody(S3.fullName, T.fullName)`; σ4's parent-wave is the same ar-locale composition with S4's name; the polymorphic session pointer (`relatedEntityType = "session"`, `relatedEntityId = σ3/σ4.id`) is carried on every wave.
- **J4 step 2** — P2's `listLinkedChildren` returns EXACTLY [S3, S4] (not S1/S2 — they are not P2's children); stable oldest-first order (createdAt ASC, id ASC tiebreak — the repo pins this ordering; the switcher's first-child auto-select depends on it); each child's `fullName` is shown unmasked (masking applies only to pre-confirmation discovery surfaces).
- **J4 step 3** — P2 per-child reads return ONLY that child's rows (no cross-contamination): `listChildReports(S3)` returns σ3's report (rating = 5); `listChildReports(S4)` returns σ4's report (rating = 3); `listChildHomework(S3)` returns σ3's homework with Jadid = SurahAlBaqarah + Madi = Juz2; `listChildHomework(S4)` returns σ4's homework with Jadid = SurahAalImran + Madi = Juz3 (NOT σ3's tracks — re-keying on `studentId` prevents cross-contamination); `getChildProgress(S3/S4)` returns each child's latest positions echoing that child's newest homework tracks; `listChildSessions(S3/S4)` returns only that child's session. Reads publish zero.

## 6.5.QL — Quality Loop

Sub-loop exit 0 at `--lifecycle duplicates` (tsgo + oxlint + biome:check + lint:type-aware + check:duplicates all passed). Three fix-iterations were needed:
1. Removed unused imports (`or`) + unused constants (`NOTIFS_EN`, `NOTIFS_AR`, `RELATED_ENTITY_TYPE` — resolved by adding recipient-locale notification-copy assertions that consume them); removed `deletedAt` from `setGovernanceFixture` call (not in `GovernanceFixtureInput` type).
2. Replaced `expect(logs.records.length).toBe(N)` with `expect(logs.records).toHaveLength(N)` (sonarjs/prefer-specific-assertions).
3. Removed a plan-artifact reference (`R-A`) from a comment — ZERO plan-artifact references remain.

## 6.5.TE — Test Engineering

13 pass / 0 fail / 223 `expect()` calls. The journeys ARE the cross-tier proof:
- **Real race via severance mid-sequence (J2):** the TOCTOU seal is structurally guaranteed by the service running `requireLinkedChild` and the data reads in ONE transaction; under pglite (single-connection) the journey tests the sequential severance (sever first → read 403s) — the true concurrent race is out of scope under pglite (matches the journey-C guard pattern from the precedent).
- **en/ar boundary (J3):** constant-shape denial asserted in BOTH en and ar — byte-identical WITHIN each locale across all four mismatch causes (foreign/never-linked/nonexistent/malformed); the `code` is `FORBIDDEN` in both; the message differs (locale-composed copy).
- **Abuse repeats (J3 step 1):** re-probing the same foreign id yields the SAME fingerprint — no state drift across repeats.
- **Both severance paths (J2):** cleared-parentId AND soft-delete produce the SAME constant denial shape — no branch disclosure.
- **Per-child isolation (J4 step 3):** σ3's rows never appear in σ4's view and vice versa — the per-student gate rejects every foreign id.

## 6.5.SEC — Security & Tenancy Audit

Cross-actor visibility table from plan §4.4 asserted verbatim per step:

| State | Teacher sees | Student sees | Parent sees | Journey step |
|---|---|---|---|---|
| Linked, session completed, report submitted | own session + report (unchanged) | own session + report (unchanged) | attendance entry + report/homework rows via NEW queries; `session_completion` notification | J1 steps 2–4 |
| Linked, report not yet submitted | session, no report | session, no report | attendance row only; report/homework rows honestly absent | J1 step 1 (baseline) |
| Unlinked / nonexistent id | n/a | n/a | constant 403, zero data, one bounded log | J1 step 5, J2, J3 |
| Severed (cleared parentId) | n/a | n/a | constant 403, list excludes child, history survives but unreadable | J2 step 1 |
| Severed (soft-delete) | n/a | n/a | same constant 403 — no branch disclosure | J2 step 2 |
| Multi-child, per-child reads | n/a | n/a | each child's rows only — no cross-contamination | J4 step 3 |

- **BOLA:** identity from `ctx.user.id` only (never client-supplied); `studentId` validated against the caller's own linked set inside `requireLinkedChild` — proven by J1 step 5 + J3 (every foreign probe 403s).
- **BOPLA:** reads only — no input object ever touches a write path.
- **BFLA:** not directly tested in the journey (that's the wire-test lane, task 6.3); the journey's denial steps all use real parent actors.
- **Enumeration oracle:** constant 403 shape byte-identical across nonexistent / foreign / never-linked / severed / malformed ids (J3 step 1 + J2 steps 1–2) — the caller cannot distinguish the denial cause.
- **Soft-deleted child:** `users.isDeleted = false` predicate in `listLinkedChildrenByParentId` (list exclusion) + governance re-check in `requireLinkedChild` (detail reads) → immediate denial with the constant shape (J2 step 2).
- **Denial logging:** exactly one bounded `logDomainError` per denial (spy assertion: 4 logs for 4 per-student denials; 5 for J3 step 1's 4+1-repeat); never logging child row data fields.

## 6.5.SR — Semantic Review

- **No `runInRollback`:** confirmed — the journey uses committed fixtures in `beforeAll` (ONE committing `db.transaction`) + tracked hard-delete cleanup in `afterAll`.
- **Cleanup airtight:** `afterAll` uses `try/finally` — `tracked.cleanup()` runs EVEN IF a step assertion failed; the zero-residue re-probes (by tracked id set AND by the `jrn_pmonitor_` prefix) run in the `finally` block. Three consecutive green runs prove idempotent teardown.
- **No seed data:** every fixture created via `backend/db/test/entity-setup.ts` helpers (`createTestUser`, `createTestStudent`, `createTestParent`, `createTestTeacherRow`).
- **No dead branches:** the `if (!cast)` guard in `requireCast()` is REACHABLE in the type system (a step that runs before `beforeAll` completes would reach it) but UNREACHABLE in practice.
- **Comments ZERO plan-artifact references:** grep-verified — no `REQ-0xx`, `task 6.x`, `specs.md`, `plan.md`, `tasks.md`, `R-A..R-J`, `D1..D11`, `INV-P1/P2`, `.ai/plans/...` references.
- **Honest authorization:** every actor is a REAL `users` row + REAL role-child row; every denial fails through the real `requireLinkedChild` gate; no role/permission monkey-patching.
- **Notification boundary spied:** `SpiedFanoutTransport` installed at the `options.transport` injection seam of every `submitSessionReport` call; nothing reaches a real channel; every publish is recorded and asserted (count + recipient ids).
- **`transportSpy.clear()` at the start of every test** — prevents cross-test publish-count leakage; each test starts with a clean spy.

## 6.5.IV — Instruction Verification

Read all printed rule files:
- `AGENTS.md` (root) — path aliases, barrel conventions, logging (`logger.logDomainError`), DB testing rules (runInRollback exception for journeys), test runner (`bun run test/scripts/run-test.ts`), quality workflow.
- `test/workflows/AGENTS.md` — the 12 hard rules (NO `runInRollback`; committed fixtures + tracked cleanup; UUID prefixes; honest authorization; external effects intercepted; never `expect(...).rejects.toThrow()`; `bun:test` imports; `@/` aliases; never demo/seeded rows; one journey file per workflow; sequential actor-attributed steps; cross-actor visibility + denial assertions).
- `.agents/instructions/tests.instructions.md` — DB transaction safety, entity-setup helper signatures, run-test script rules, GraphQL integration test rules (N/A for journey), linting rules (never `oxlint-disable`).

All conventions honored:
- `bun:test` imports only (`afterAll`, `beforeAll`, `describe`, `expect`, `spyOn`, `test`).
- `@/` path aliases throughout.
- `catchJourneyError` for denial capture (never `expect(...).rejects.toThrow()`).
- `getServerTranslations(locale).errorsTranslations.forbidden` for translated denial substrings (never hardcoded English).
- `TrackedFixtures` registry with FK-safe reverse-order cleanup + zero-residue re-probes.
- `SpiedFanoutTransport` at the `options.transport` injection seam.
- Per-run `jrn_pmonitor_<uuid8>` prefix on every fixture identity field.
- `recordDomainLogs()` helper silences + records `logger.logDomainError` calls via `spyOn(logger, "logDomainError")` — denial steps stay silent in test output AND become assertable.
- No `oxlint-disable`, no `jscpd:ignore`, no `console.*`, no `any` casts.

## Implementation bugs fixed

None. The journey exercised the existing `ParentMonitoringService` (task 2.4) + `SessionLifecycleService` + `SessionReportService` + `SessionReportNotificationService` surface as-is. No implementation bugs were revealed.

## Test results

```
13 pass
0 fail
223 expect() calls
Ran 13 tests across 1 file. [1.37s]
```

Sub-loop: exit 0 at `--lifecycle duplicates` (tsgo + oxlint + biome:check + lint:type-aware + check:duplicates all passed).

Idempotent teardown: 3 consecutive green runs — zero residue proven.

## Carry-forward

- The journey pins the cross-actor visibility table from plan §4.4 — any future change to `ParentMonitoringService` or `requireLinkedChild` that breaks the constant-shape denial contract or the list-exclusion-on-severance contract will fail this suite.
- The journey pins the recipient-locale composition for the report-wave notification (en for P, ar for P2) — any drift in the notification engine's locale resolution will fail J1 step 2 or J4 step 1.
- The journey pins the deep-link contract (`parentChildReports(S1).items` contains a row whose `sessionId === σ1.id`) — the frontend's `?session=X` resolution depends on this.
- Task 7.1 (post-implementation review wave) can grep-scan this journey file alongside the other portal files for the INV-P2 (zero mutations) + R-A (no `parent_link_requests` reads) + R-C (no `evaluations` imports) locks.
