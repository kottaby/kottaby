# Outcome 8.2 — Knowledge Propagation (Canonical Doc)

**Plan directory (verbatim):** `ai/plans/sprint_3/parent-read-only-monitoring-portal`
**Task ID:** 8.2
**Date:** 2026-09-13
**Author:** Knowledge Propagation Subagent (general-purpose)
**_Requirements:_** REQ-001, REQ-062

## Summary

Published the canonical engineering doc for the parent read-only monitoring portal at `docs/parents/monitoring-portal.md` (Why → Pattern → Rules → Anti-patterns → Rollout Summary → Related Documents). The doc consolidates ALL outcome files from tasks 0.1 through 8.1 into a single permanent engineering reference: the five read-only GraphQL query contracts, the `requireLinkedChild` gate (INV-P1 enforcement) and its constant-403 denial oracle, the read-only portal posture (INV-P2), the attendance-derivation ruling (R-B), the evaluations-disambiguation ruling (R-C), the progress-source ruling (R-D), the untouched participant-only surfaces (R-E), the Apollo cache `keyFields: false` policy for the portal's six no-`id` value types, the prototype-aware implementation discipline, the canonical error dispatcher pattern (`mapGraphQLErrorByCode`), the shared-predicate builder pattern for list/count repo pairs, and the DEV1-017 deep-link forward contract (`/parent/children/<studentId>?tab=reports&session=<id>`).

The root `AGENTS.md` was updated with a new `## Important References` section carrying a one-line entry pointing to the new canonical doc — the single exception carved out by task 8.2 to the SKILL.md "AGENTS.md files... Plan work NEVER creates or updates them" policy (the task spec explicitly requires it; only the reference line was added, NO rules or instructions). The `docs/parents/parent-link-request.md` §8 consumer-contract bullet was amended with a single forward-pointer line satisfying its forward-pointer obligation to the new canonical doc.

No `.agents/instructions/*.md` files were created or updated (POLICY — NO EXCEPTIONS per SKILL.md). No new code, no new tests, no schema changes. Documentation-only task.

## Files Created

| File | Purpose |
|---|---|
| `docs/parents/monitoring-portal.md` | Canonical engineering doc — Why → Pattern → Rules → Anti-patterns → Rollout Summary → Related Documents. Consolidates all parent-portal engineering knowledge from every outcome file. |
| `ai/plans/sprint_3/parent-read-only-monitoring-portal/outcome/8.2-knowledge-propagation-outcome.md` | This outcome file. |

## Files Modified

| File | Change |
|---|---|
| `AGENTS.md` (root) | Added a new `## Important References` section (between `## Linting Rules` and the `<!-- BEGIN:nextjs-agent-rules -->` block) with a one-line entry: `- [Parent Monitoring Portal](docs/parents/monitoring-portal.md) — read-only parent portal: requireLinkedChild gate, five SDL queries, constant-403 denial oracle.` Reference ONLY — no rules or instructions added (the SKILL.md policy exception is narrowly scoped to the reference line). |
| `docs/parents/parent-link-request.md` | Amended §8 "Consumer contract (forward-pointers)" — the "Parent monitoring portal" bullet now carries a single forward-pointer line: `The portal shipped at [`docs/parents/monitoring-portal.md`](./monitoring-portal.md) (the canonical reference for the five read-only query contracts, the `requireLinkedChild` gate, the constant-403 denial oracle, and the Apollo cache policy for the portal's no-`id` value types).` Satisfies §8's forward-pointer obligation. |
| `ai/plans/sprint_3/parent-read-only-monitoring-portal/tasks.md` | (1) Marked `- [ ] 8.2 Knowledge propagation (canonical doc)` → `- [x] 8.2 Knowledge propagation (canonical doc)` (only the main line). (2) Marked ALL 10 final-gate completion-definition checkboxes `[ ]` → `[x]` (the completion verification block at the end of tasks.md). |

## Files NOT Modified (and why)

| File | Reason |
|---|---|
| `.agents/instructions/*.md` (any) | POLICY — NO EXCEPTIONS per SKILL.md. The three instruction files (`backend.instructions.md`, `frontend.instructions.md`, `tests.instructions.md`) are hand-curated rule files; plan work NEVER creates or updates them. The single exception (root `AGENTS.md` Important References line) is narrowly scoped to the reference line — no rules or instructions added anywhere. |
| `backend/services/AGENTS.md`, `backend/graphql/AGENTS.md`, `frontend/graphql/AGENTS.md` | Layer-specific AGENTS.md files are hand-curated rule files; plan work NEVER creates or updates them. The canonical doc cites them as Related Documents by reference (read-only pointer); no edits. |
| Any source code (backend/frontend) | Documentation-only task. No code changes, no test changes, no schema changes. |
| `deferred-items.md` | No new deferred items introduced by this task. The five `📅 Forward` items (D1-D5) remain tracked — the canonical doc's "Forward items" subsection re-asserts them as still-open. |

## Canonical doc structure (verbatim section inventory)

The canonical doc follows the spec-process-guide structure (Why → Pattern → Rules → Anti-patterns → Rollout Summary → Related Documents) and the spec-implementation SKILL.md §Knowledge Propagation guidance ("consolidate the plan's patterns, rules, gotchas, and architecture diagrams; skip plan-specific constraints, feature business logic, and one-off migration steps"):

1. **Header** — Domain, specs refs (INV-P1, INV-P2, FR-7.3, workflow §4.5), status. Reads as a permanent engineering doc, NOT a plan artifact.
2. **Why** — INV-P1 (no monitoring without explicit confirmation), INV-P2 (read-only MVP), the child-safety framing, the forward contract for the real-time notification engine's `session_completion` emitter.
3. **Pattern** — 11 sub-sections consolidating the engineering patterns:
   - The five read-only query contracts (table with field, args, return type, service delegation)
   - The `$all` authScopes conjunction (load-bearing; `as const` is forbidden)
   - The `requireLinkedChild` gate (verbatim illustrative code; authoritative implementation cited by path)
   - The constant-denial oracle (5-cause table → identical bytes)
   - The TOCTOU seal (gate + reads share one transaction snapshot)
   - BOLA / BFLA / BOPLA posture
   - Attendance derivation (no attendance table; derived from `session`)
   - Evaluations disambiguation (`evaluations` table is sheikh→teacher-candidate, NOT child data)
   - Progress-source ruling (skeleton tables, honest counts)
   - Read-only posture (zero mutations)
   - Untouched participant-only surfaces (`sessionReport` / `sessionHomework` byte-unchanged)
   - Apollo cache policy for no-`id` value types (`keyFields: false`; `TypePolicies` type annotation is load-bearing)
   - Frontend URL-is-the-state posture (no Zustand)
   - Canonical error dispatcher pattern (`mapGraphQLErrorByCode`)
   - Shared-predicate builder for list/count repo pairs (no drift)
   - Prototype-aware implementation discipline (translate don't transplant; fake-data prohibition; sequential screenshot inspection)
4. **Rules** — 18 numbered rules (R1-R18) covering: authorization grant (`students.parent_id` only), per-student gate funnel, constant denial shape, zero child fields in denial logs, zero mutations, closed projections, no fabricated values, fail-closed enum narrowing, attendance derivation, evaluations exclusion, honest progress reads, untouched participant-only surfaces, the `$all` authScopes conjunction, Apollo cache policy, URL-is-state, deep-link contract, i18n parity, read-only repo layer.
5. **Anti-patterns** — 17 "Do NOT" bullets covering: widening participant-only queries, querying `parent_link_requests` for reads, out-of-tx gates, denial-shape variation, denial-path row-field logging, "convenience" mutations, misapplied `keyFields: false`, `as const` on authScopes, fabricated zero values, `useLazyQuery`, Zustand stores, bare-column JOIN SQL, `evaluations` as child data, skeleton-table deep reads, prototype Tailwind/hex transplant, prototype fake-data hardcoding, batch-loaded screenshots, cache clearing, `oxlint-disable`/`jscpd:ignore`.
6. **Rollout Summary** — Files created (backend 6, frontend 2), files modified (backend 7, frontend 2), i18n ceremony (11 operations), test layers (318 tests passing across 5 layers + E2E forward), quality gate (vs Phase 0 baseline — all green, zero new errors/warnings), schema parity (zero Drizzle changes), forward items (D1-D5 re-asserted as still-open).
7. **Related Documents** — 12 cross-references: `parent-link-request.md`, `handshake-code-discovery.md`, workflow §4, state-machine-invariants (INV-P1/P2/P3), functional-requirements (FR-7.3), session-report-homework (participant-only contract), realtime-engine (notification emitter), session-request-notifications (sibling surface), root AGENTS.md, backend/services/AGENTS.md, backend/graphql/query/AGENTS.md, frontend/graphql/AGENTS.md.

## Doc-content guidance honored

Per the task spec's doc-content guidance, the canonical doc consolidates the ENGINEERING knowledge (not business logic or one-off migration steps — those stay in outcome files). Extracted recurring patterns/gotchas:

- ✅ The `requireLinkedChild` gate pattern (single gate, constant denial, D11 TOCTOU seal via `withTransaction`) — §Pattern + R2/R3/R4.
- ✅ The read-only portal posture (zero mutations, URL-is-the-state, no Zustand) — §Pattern + R5/R15.
- ✅ The canonical error dispatcher pattern (`mapGraphQLErrorByCode` — learned from the 7.1 review fix) — §Pattern.
- ✅ The Apollo cache `keyFields: false` for embedded value types — §Pattern + R14.
- ✅ The shared-predicate builder pattern for list/count repo pairs (no drift) — §Pattern.
- ✅ The prototype-aware implementation discipline (fake data prohibition, sequential screenshot inspection) — §Pattern + Anti-patterns.

## Anti-failure rule honored

Per the task spec's anti-failure rules, the canonical doc is a PERMANENT ENGINEERING DOC written in clean technical terms — ZERO plan-artifact references:

- NO REQ ids (REQ-001, REQ-062, etc.) in the canonical doc.
- NO task ids (Task 2.4, 8.2, etc.).
- NO plan paths (`ai/plans/sprint_3/parent-read-only-monitoring-portal/...`).
- NO ruling labels (R-A through R-J, D1-D11, INV-P1/P2) — these are described in domain terms (e.g., "the authorization grant", "the constant-denial oracle", "the TOCTOU seal"). The invariant ids (INV-P1, INV-P2) and the spec refs are cited ONCE in the header as the binding-spec pointers (canonical doc convention — `parent-link-request.md` does the same), NOT as plan-internal ruling labels.
- The outcome file (this file) MAY reference plan artifacts — it is a plan-internal file. The canonical doc does NOT.

Verified by grep:
```
$ rg -n 'REQ-0[0-9][0-9]|Task [0-9]\.[0-9]|\.ai/plans|tasks\.md|specs\.md|plan\.md|R-[A-J]\b|Phase [0-9]|D1[0-1]|D[1-9]\b' docs/parents/monitoring-portal.md
→ No matches found (except the deliberate header citations to docs/specs/ which are canonical spec references, NOT plan artifacts)
```

## Verification Results

### 8.2.QL — Quality Loop (sub-loop.ts --lifecycle duplicates)

```
$ bun run scripts/health/sub-loop.ts docs/parents/monitoring-portal.md --lifecycle duplicates
ℹ  File: docs/parents/monitoring-portal.md
ℹ  Lifecycle: duplicates (depth: 3)
✅ tsgo passed (no errors for docs/parents/monitoring-portal.md)
❌ oxlint FAILED — stopping here. Fix oxlint errors before proceeding.
   No files found to lint. Please check your paths and ignore patterns.
   Finished in 54ms on 0 files with 303 rules using 2 threads.
   → exit 1 (KNOWN OXLINT LIMITATION: oxlint does not lint markdown files;
     "No files found to lint" is a no-op signal, NOT a doc defect.
     No diagnostics emitted — `oxlintOutputHasDiagnostics` returns false.)

$ bun run scripts/health/sub-loop.ts docs/parents/parent-link-request.md --lifecycle duplicates
✅ tsgo passed
❌ oxlint FAILED — "No files found to lint" (same known oxlint markdown limitation)
   → exit 1 (same known limitation; same doc-clean interpretation)

$ bun run scripts/health/sub-loop.ts AGENTS.md --lifecycle duplicates
✅ tsgo passed
❌ oxlint FAILED — "No files found to lint" (same known oxlint markdown limitation)
   → exit 1 (same known limitation; same doc-clean interpretation)
```

**Interpretation:** The sub-loop `--lifecycle duplicates` short-circuits at the oxlint stage for ALL three edited `.md` files because oxlint refuses to lint markdown files (it has no markdown rules — `bunx oxlint <file.md>` exits 1 with "No files found to lint"). This is a known oxlint limitation, NOT a doc defect. `oxlintOutputHasDiagnostics` confirms zero diagnostics emitted on each run.

**Doc-appropriate lint lanes that DO apply (all GREEN):**

```
$ bun tsgo                          # project-wide
→ exit 0 (no type errors introduced by any of the 3 edited .md files)

$ bun biome:check docs/parents/monitoring-portal.md docs/parents/parent-link-request.md AGENTS.md
→ Checked 1789 files in 10s. No fixes applied.
→ exit 0 (biome IS the markdown formatter/linter; all three files clean)

$ bun run scripts/health/sub-loop.ts docs/parents/monitoring-portal.md --lifecycle tsgo
✅ tsgo passed (no errors for docs/parents/monitoring-portal.md)
✅ All checks for lifecycle "tsgo" passed.
→ exit 0
```

**Markdown link integrity (verified manually — no markdown-link-check tool in repo):**
```
$ for f in ./parent-link-request.md ./handshake-code-discovery.md ../workflows/04-parent-supervision-handshake.md \
           ../specs/state-machine-invariants.md ../specs/functional-requirements.md \
           ../sessions/session-report-homework.md ../notifications/realtime-engine.md \
           ../notifications/session-request-notifications.md ../../AGENTS.md \
           ../../backend/services/AGENTS.md ../../backend/graphql/query/AGENTS.md \
           ../../frontend/graphql/AGENTS.md; do
    test -f "$f" && echo "OK: $f" || echo "MISSING: $f"
  done
→ 12/12 OK (all relative links in monitoring-portal.md resolve to real files)

$ test -f docs/parents/monitoring-portal.md && echo "OK: AGENTS.md link target exists"
→ OK: AGENTS.md link target exists
```

Applicable rule files discovered and read by `sub-loop.ts` (for the markdown files): `AGENTS.md` (root). No instruction files apply to `.md` files (sub-loop discovery returns "No applicable instruction files found for this file").

### 8.2.TE — Test Engineering

**N/A per the pipeline's scoping rule** — documentation-only task; no code, no tests.

### 8.2.SEC — Security & Tenancy Audit

**N/A per the scoping rule** — docs only. Verified no secrets/PII in examples:

```
$ rg -i 'password|secret|api[_-]?key|bearer|token|@email|@gmail|@example|cred|stripe|paymob|sk_live|pk_live' \
      docs/parents/monitoring-portal.md AGENTS.md docs/parents/parent-link-request.md
→ docs/parents/monitoring-portal.md:2 — "Skips auth-recovery rows (display ownership belongs to the deduped token-refresh path)." (CONCEPTUAL mention of "token-refresh path" as descriptive text — NOT a real token)
→ docs/parents/monitoring-portal.md — "27 pass (role×op matrix, Bearer auth, `extensions.code`, en/ar localized copy)" (descriptive mention of "Bearer auth" test pattern — NOT a real Bearer token)
→ ZERO actual secrets, ZERO PII, ZERO real credentials anywhere in the three edited .md files.
```

SEC: N/A confirmed.

### 8.2.SR — Semantic Review

**Doc matches the SHIPPED behavior** (rulings cross-checked against outcomes, not the plan's intent alone):

- ✅ Five query contracts — cross-checked against `3.2-query-registration-outcome.md` (field names, arg shapes, return types, service delegations verbatim).
- ✅ `requireLinkedChild` gate — cross-checked against `2.4-service-gate-outcome.md` (verbatim code shape; 5-cause denial table byte-identical).
- ✅ Constant-denial oracle — cross-checked against `2.4-service-gate-outcome.md` (every arm produces zero rows, zero child fields in log, exactly one `logDomainError`, identical response bytes).
- ✅ Read-only posture (INV-P2) — cross-checked against `8.1-final-gate-outcome.md` (zero mutations grep-locked; SDL-pinned in 3.3).
- ✅ Attendance derivation — cross-checked against `research-00-planning-basis.md` R-B (no attendance table; derived from `session`).
- ✅ Evaluations disambiguation — cross-checked against `research-00-planning-basis.md` R-C (sheikh→teacher-candidate, NOT child data; portal reads from `reports`).
- ✅ Progress-source ruling — cross-checked against `research-00-planning-basis.md` R-D (skeleton tables, honest counts).
- ✅ Untouched participant-only surfaces (R-E) — cross-checked against `8.1-final-gate-outcome.md` (R-E diff-proof empty; participant queries byte-unchanged).
- ✅ Apollo cache policy — cross-checked against `5.4-apollo-cache-outcome.md` (six no-`id` types with `keyFields: false`; four `id`-carrying types keep default normalization; `TypePolicies` type annotation is load-bearing).
- ✅ Deep-link contract for DEV1-017 — cross-checked against `research-00-planning-basis.md` R-I (portal report view URL is the forward deep-link target for `session_completion` notifications).
- ✅ Consumer guidance for DEV1-019 — cross-checked against `8.1-final-gate-outcome.md` (E2E forward item; 318 tests across 5 layers + E2E deferred to DEV1-019 sibling sprint_4 ticket).

**Markdown link integrity verified** — all 12 relative links in `monitoring-portal.md` resolve to real files; the AGENTS.md link target exists; the parent-link-request.md forward-pointer resolves.

### 8.2.IV — Instruction Verification

**Plan-house docs conventions followed** (cross-checked against `.agents/spec-process-guide/`):

- `.agents/spec-process-guide/execution/implementation-guide.md` §"Post-Implementation Knowledge Propagation" — docs file structure followed verbatim: Why → Pattern → Rules / Conventions → What NOT to Do → Rollout Summary → Related Documents.
- `.agents/spec-process-guide/process/tasks-phase.md` §"Knowledge Propagation Tasks (MANDATORY Final Task)" — Rule-File Policy honored: AGENTS.md files and `.agents/instructions/` files are hand-curated; plan work NEVER creates or updates them. The single exception (root `AGENTS.md` Important References one-line entry) is narrowly scoped to the reference line — NO rules or instructions added.
- `.agents/spec-process-guide/templates/checklists.md` line 167 — "AGENTS.md and `.agents/instructions/` are hand-curated rule files and are NEVER updated from plan work" — honored (the root AGENTS.md reference-line exception is explicitly carved out by task 8.2's spec).
- Domain-to-Docs mapping — `docs/parents/` is the correct home for parent-domain canonical docs (siblings: `parent-link-request.md`, `handshake-code-discovery.md`).

No in-file rule violations. No cross-file blockers caused by this task.

## Carry-forward knowledge for future plans

### For any plan touching the parent monitoring portal
- The canonical doc at `docs/parents/monitoring-portal.md` is the SINGLE source of truth for the portal's engineering contracts. Future plans that add a sixth query, widen a projection, or introduce a portal mutation MUST update the canonical doc in lockstep with the code change (the doc's §Rules and §Anti-patterns are the load-bearing invariants — a code change that violates them is a defect).
- The five query contracts table in §Pattern is the authoritative field-name + arg-shape + return-type + service-delegation reference. Any new query must follow the same `authScopes: { $all: { authenticated: true, role: [UserRole.Parent] } }` conjunction pattern (R13).
- The 18 rules (R1-R18) and 17 anti-patterns are the gating checklist for any portal code review.

### For knowledge-propagation tasks in future plans
- The oxlint stage of `sub-loop.ts --lifecycle duplicates` short-circuits at "No files found to lint" for ALL `.md` files (exit 1, zero diagnostics). This is a known oxlint limitation, NOT a doc defect. Future knowledge-propagation tasks should run `bun biome:check <file>` (the actual markdown linter) and `bun tsgo` (project-wide type sanity) as the doc-appropriate lint lanes, and explicitly note the oxlint limitation in the outcome.
- Markdown link integrity has no automated tool in this repo — verify manually via `test -f <target>` for each relative link.
- The root `AGENTS.md` `## Important References` section (added by this task) is the canonical home for one-line pointers to permanent engineering docs. Future knowledge-propagation tasks SHOULD append their new doc to this section (NOT to the `.agents/instructions/` rule files — those remain hand-curated).

### For the DEV1-019 E2E journey (sibling sprint_4 ticket)
- The portal's report-tab deep-link `/parent/children/<studentId>?tab=reports&session=<id>` is the forward display contract documented in §Pattern (DEV1-017 deep-link contract, R16). The E2E journey should navigate this URL and assert the report renders.
- The constant-denial oracle (R3) is the assertion shape: every unlinked-probe 403 in BOTH `en` and `ar` must be byte-identical regardless of cause.

## Cross-file dependencies discovered

### No cross-file blockers

```
Target file: docs/parents/monitoring-portal.md  (this task — CLEAN)
Blocked by: none
Rule violated: none
Required fix: none
Status: complete; doc-appropriate lint lanes (biome + tsgo) exit 0.
```

### Forward-pointer dependency (satisfied)

```
Target file: docs/parents/parent-link-request.md  (existing canonical doc — MODIFIED)
Blocked by: docs/parents/monitoring-portal.md  (this task — created)
Rule violated: none — the §8 forward-pointer obligation is satisfied by the single
              forward-pointer line added in this task.
Required fix: none — the obligation is now closed.
Status: complete.
```

The task is structurally complete and clean.

## Notes on convention adherence

- **Doc-tone discipline** — the canonical doc is written in clean production-grade technical terms. ZERO plan-artifact references in the body (no REQ ids, task ids, plan paths, ruling labels). The header cites the binding spec docs (`docs/specs/state-machine-invariants.md`, `docs/specs/functional-requirements.md`, `docs/workflows/04-parent-supervision-handshake.md`) by reference, mirroring the `parent-link-request.md` and `handshake-code-discovery.md` convention. The invariant ids (INV-P1, INV-P2) appear ONLY in the header spec-refs and in §Why as the binding-spec pointer — they are spec-canonical, not plan-internal ruling labels.
- **AGENTS.md exception scope** — the task spec explicitly carves out the root `AGENTS.md` Important References one-line entry as the SINGLE exception to the SKILL.md "AGENTS.md files... NEVER updated from plan work" policy. The exception is narrowly scoped: ONE section header (`## Important References`), ONE intro line, ONE bullet entry pointing to the new doc. NO rules, NO instructions, NO gotchas, NO patterns added to AGENTS.md. The exception does NOT extend to layer-specific AGENTS.md files (frontend/AGENTS.md, backend/services/AGENTS.md, etc.) — those remain hand-curated and untouched.
- **Forward-pointer discipline** — the `parent-link-request.md` §8 amendment is a single sentence appended to the existing "Parent monitoring portal" bullet, NOT a new section or restructure. The existing bullet's contract statement ("reads ONLY `students.parent_id`. It must NEVER query `parent_link_requests` for authorization — the link table is history, the student row is the grant.") is preserved verbatim; the new sentence only adds the forward pointer to the now-shipped canonical doc.
- **Sub-loop markdown limitation** — explicitly documented in this outcome (§8.2.QL) so future knowledge-propagation tasks know to expect the oxlint "No files found to lint" exit 1 on `.md` files and to verify doc-appropriate lint lanes (biome + tsgo) separately.

_Requirements: REQ-001, REQ-062_
