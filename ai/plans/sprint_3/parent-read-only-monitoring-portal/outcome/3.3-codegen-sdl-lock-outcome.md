# Outcome 3.3 — GraphQL codegen checkpoint + SDL surface lock

**Plan directory (verbatim):** `ai/plans/sprint_3/parent-read-only-monitoring-portal`
**Task ID:** 3.3
**Date:** 2026-09-12
**Author:** Backend GraphQL Codegen Subagent (general-purpose)
**_Requirements:_** REQ-023, REQ-030, REQ-031

## Summary

Ran the GraphQL codegen checkpoint (`bun run generate:gqlSchema` then
`bun codegen`) and locked the regenerated SDL surface via an extended
`backend/graphql/test/schema-surface.test.ts` assertion suite. The
regenerated `frontend/graphql/generated/schema.graphql` now surfaces the
five new parent-only root queries (`myLinkedChildren`,
`parentChildProgress`, `parentChildSessions`, `parentChildReports`,
`parentChildHomework`) and the ten new parent-portal object types
registered in tasks 3.1/3.2. The codegen output
`frontend/graphql/generated/gql/graphql.ts` is byte-unchanged because no
portal GraphQL documents exist yet (task 5.1 will add them and trigger a
second codegen refresh).

The extended assertion suite pins every dimension of the new surface:
the five field names + their exact arg shapes (`studentId: Int!` +
optional `page`/`pageSize` for the four per-student reads, zero-arg for
`myLinkedChildren`); the ten object types' exact field sets + per-field
type strings (BOPLA projection boundary); the
`$all { authenticated: true, role: [UserRole.Parent] }` conjunction on
every field; the `id: ID!` first convention on the four entity-shaped
objects; smuggled-identity-arg validation rejection (BOLA defense at
the schema layer); the **zero-mutation lock** proving the Mutation root
carries ZERO portal-named fields (read-only by construction, not by
vigilance); the **participant-only byte-unchanged** pin proving the
pre-portal `sessionReport` / `sessionHomework` SDL snippets survive
verbatim; and a SEC pin that none of the five portal queries is on the
public-operation allowlist.

## Files Created

None.

## Files Modified

| File | Change |
|---|---|
| `frontend/graphql/generated/schema.graphql` | Regenerated via `bun run generate:gqlSchema`. +98 lines: the ten new parent-portal object types (`ParentAttendanceEntry`, `ParentAttendancePage`, `ParentChildProgress`, `ParentHomeworkEntry`, `ParentHomeworkPage`, `ParentHomeworkPosition`, `ParentHomeworkTrack`, `ParentLinkedChild`, `ParentReportEntry`, `ParentReportPage`) and the five new root query field definitions with their descriptions + arg shapes. The file mode changed from `100755` to `100644` (the regenerator writes with the default mode). |
| `backend/graphql/test/schema-surface.test.ts` | +443 lines: two new constants (`PARENT_PORTAL_QUERY_FIELDS`, `PARENT_PORTAL_TYPE_NAMES`) added to the root-query additions pin + the whole-schema named-type additions pin; ten new `PARENT_*_FIELD_TYPES` field-type maps; two frozen participant-only SDL snippet constants; a new module-scope `rootQueryField` helper (sibling to the existing `mutationField`); a new describe block with 10 tests pinning every dimension of the portal surface; belt-and-braces codegen-sync checks for the five fields + ten type-block headers inside the committed SDL. |

## Files NOT Modified (and why)

| File | Reason |
|---|---|
| `frontend/graphql/generated/gql/graphql.ts` | The codegen ran successfully but produced byte-identical output — no portal GraphQL documents exist yet (task 5.1 creates them). The codegen config's `documents` glob (`./frontend/graphql/sharedDocuments/**/*.ts`, `./frontend/views/**/*.documents.ts`) found no portal documents to generate typed-document-nodes for. Task 5.1 will trigger a second codegen refresh. |
| `backend/graphql/test/session-sdl.test.ts` | The session-SDL suite pins the Session / SessionReport / SessionHomeWork surface only. The parent-monitoring portal additions belong in `schema-surface.test.ts` (the comprehensive surface-freeze suite that already pins the root-query additions + the named-type delta + the codegen-sync byte-equality). Extending `session-sdl.test.ts` would have split the portal pins across two suites and duplicated the codegen-sync machinery. |
| `backend/lib/gateway/public-operations.ts` | The closed public-operation allowlist needs NO new entries — all five portal fields carry the explicit `$all { authenticated: true, role: [UserRole.Parent] }` conjunction (anonymous callers deny 401 pre-resolver). The SEC test in the new describe block asserts NONE of the five portal names is a member of `PUBLIC_OPERATION_NAMES` / `PUBLIC_OPERATIONS`. |
| `backend/graphql/query/parents/parent-monitoring.query.ts` | Owned by task 3.2 (complete). Read-only from this task's perspective — the five field registrations are consumed as-is. |
| `backend/graphql/pothos/parents/parent-monitoring.pothos.ts` | Owned by task 3.1 (complete). Read-only from this task's perspective — the ten object registrations are consumed as-is. |
| `backend/graphql/pothos/classes/session.pothos.ts`, `report.pothos.ts`, `home-work.pothos.ts` | Participant-only surfaces untouched (the byte-unchanged pin below proves this). The parent-portal query fields are NEW and parent-scoped — they never widen the participant-only `sessionReport` / `sessionHomework` reads. |
| `codegen.ts`, `scripts/generator/generate-gql-schema.ts` | The codegen config + the SDL generator script are unchanged. The output path (`frontend/graphql/generated/schema.graphql`) and the lexicographic-sort deterministic emission are the same as before. |

## Step 1 — Generate the SDL + codegen

```bash
$ bun run generate:gqlSchema
[WARN] [PglitePool] initializing PGlite at ./db/pglite (DB_PROVIDER=pglite)
[generate:gqlSchema] wrote /home/z/my-project/frontend/graphql/generated/schema.graphql (36003 bytes)

$ bun codegen
✔ Parse Configuration
✔ Generate outputs
✔ Generate to ./frontend/graphql/generated/gql/graphql.ts
✔ Load GraphQL schemas
✔ Load GraphQL documents
✔ Generate
✔ Generate outputs
```

The SDL grew from 33017 bytes (HEAD) to 36055 bytes on disk (36003
chars / 36055 bytes — the 52-byte overhead is the non-ASCII content in
the `SurahJuzRef` enum's 35 members). The +98-line diff is EXACTLY the
ten new object types + the five new root query field definitions with
their descriptions + arg shapes — zero unintended churn elsewhere.

The codegen output `gql/graphql.ts` is byte-unchanged because no portal
documents exist yet. Task 5.1 will add the documents
(`myLinkedChildrenQueryDocument`, `parentChildProgressQueryDocument`,
`parentChildSessionsQueryDocument`, `parentChildReportsQueryDocument`,
`parentChildHomeworkQueryDocument`) and trigger a second codegen refresh.

## Step 2 — Inspect the generated SDL

### The ten new parent object types (all present, alphabetical per `lexicographicSortSchema`)

```
type ParentAttendanceEntry {  createdAt: DateTime!  endedAt: DateTime  id: ID!  startedAt: DateTime  status: SessionStatus! }
type ParentAttendancePage {  items: [ParentAttendanceEntry!]!  page: Int!  pageSize: Int!  totalCount: Int! }
type ParentChildProgress {  child: ParentLinkedChild!  latestJadidPosition: ParentHomeworkPosition  latestMadiPosition: ParentHomeworkPosition  progressRowCount: Int! }
type ParentHomeworkEntry {  createdAt: DateTime!  id: ID!  jadid: ParentHomeworkTrack  madi: ParentHomeworkTrack  sessionId: Int! }
type ParentHomeworkPage {  items: [ParentHomeworkEntry!]!  page: Int!  pageSize: Int!  totalCount: Int! }
type ParentHomeworkPosition {  fromAyah: Int  surahJuz: SurahJuzRef!  toAyah: Int }
type ParentHomeworkTrack {  fromAyah: Int  grade: Int  surahJuz: SurahJuzRef  toAyah: Int }
type ParentLinkedChild {  createdAt: DateTime!  fullName: String!  id: ID! }
type ParentReportEntry {  createdAt: DateTime!  id: ID!  sessionId: Int!  sessionStartedAt: DateTime  sessionStatus: SessionStatus!  studentRatingByTeacher: Int  teacherNotes: String }
type ParentReportPage {  items: [ParentReportEntry!]!  page: Int!  pageSize: Int!  totalCount: Int! }
```

### The five new root query fields (all present, exact arg shapes)

```
myLinkedChildren: [ParentLinkedChild!]!
parentChildHomework(page: Int, pageSize: Int, studentId: Int!): ParentHomeworkPage!
parentChildProgress(studentId: Int!): ParentChildProgress!
parentChildReports(page: Int, pageSize: Int, studentId: Int!): ParentReportPage!
parentChildSessions(page: Int, pageSize: Int, studentId: Int!): ParentAttendancePage!
```

### Zero new Mutation fields referencing the portal service / names

```
$ awk '/^type Mutation \{/{flag=1; next} /^}/{flag=0} flag' frontend/graphql/generated/schema.graphql \
    | grep -iE 'myLinkedChildren|parentChildProgress|parentChildSessions|parentChildReports|parentChildHomework|parentMonitoring|ParentMonitoring'
→ No matches found
```

The Mutation root carries 65 field definitions (the same set as the
pre-portal baseline). The ONLY parent-related mutations are the
PRE-EXISTING parent-link-request trio (`cancelParentLinkRequest`,
`requestParentChildLink`, `respondToParentLinkRequest`) — they predate
this surface and are pinned in the `RECONCILED_PARENT_LINK_MUTATION_FIELDS`
constant. ZERO new mutation fields of any kind (read-only by
construction, not by vigilance).

## Step 3 — Update the schema-surface assertion suite

### Constants added

- `PARENT_PORTAL_QUERY_FIELDS` — the five root query field names,
  alphabetically sorted (matches the live SDL's lexicographic order).
- `PARENT_PORTAL_TYPE_NAMES` — the ten object type names, alphabetically
  sorted.
- Ten `PARENT_*_FIELD_TYPES` maps — one per object type, pinning every
  field's exact GraphQL type string (e.g., `ParentAttendanceEntry` →
  `{ createdAt: "DateTime!", endedAt: "DateTime", id: "ID!",
  startedAt: "DateTime", status: "SessionStatus!" }`). The nullability
  EXACTLY matches the TS ReturnType shapes authored in task 2.1 —
  nullable TS field → nullable GraphQL field, non-null TS field →
  non-null GraphQL field (BOPLA projection boundary).
- `PARTICIPANT_SESSION_REPORT_SDL` = `"sessionReport(sessionId: ID!): SessionReport"`
- `PARTICIPANT_SESSION_HOMEWORK_SDL` = `"sessionHomework(sessionId: ID!): SessionHomeWork"`

### Existing pins extended

- **Root-query additions pin** (`test("root query retains EXACTLY the
  baseline fields plus the pinned additions", ...)`): the
  `additions` array now includes `...PARENT_PORTAL_QUERY_FIELDS` after
  `...RECITATION_RECORD_QUERY_FIELDS`. An accidental rename of any of
  the five portal query fields now fails this pin.
- **Whole-schema named-type additions pin** (`test("whole-schema
  named-type delta is pinned: ...", ...)`): the `additions` array now
  includes `...PARENT_PORTAL_TYPE_NAMES` after
  `...RECITATION_RECORD_TYPE_NAMES`. An accidental rename of any of
  the ten portal object types now fails this pin.
- **Codegen-sync describe** (`test("frontend/graphql/generated/schema.graphql
  matches a fresh deterministic emission", ...)`): added 14
  belt-and-braces `expect(committedSdl).toContain(...)` checks for the
  five field definitions (with exact arg shapes) + the ten type-block
  headers, proving the committed artifact really contains the portal
  surface.

### New describe block (10 tests)

`describe("Parent-monitoring portal surface — five read-only queries,
zero-mutation lock, byte-unchanged participant reads", ...)`:

1. **`myLinkedChildren` is zero-arg and returns NON-NULL
   `[ParentLinkedChild!]!`** — pins the zero-arg shape (BOLA: identity
   from `ctx.user.id` only, no client-supplied parent id).
2. **`parentChildProgress` returns NON-NULL `ParentChildProgress!` with
   EXACTLY ONE required `studentId: Int!` arg** — pins the single-arg
   shape.
3. **The three paginated portal reads return NON-NULL page wrappers with
   EXACTLY `studentId: Int!` + `page: Int` + `pageSize: Int`** — pins
   the three-arg closed whitelist (no parent-id smuggle).
4. **ALL FIVE portal queries carry the EXACT `$all` conjunction —
   `{ authenticated: true, role: [UserRole.Parent] }`** — pins the
   load-bearing `$all` (a plain scope map would combine with ANY
   semantics and leak access). Verified via `authScopesSnapshot` on
   each field's Pothos extensions.
5. **Smuggled identity args die at validation BEFORE any resolver runs
   (zero identity-arg surface on portal queries)** — feeds five
   malformed documents (one per portal field, each smuggling a
   `parentId` / `actorId` / `userId` arg) through `validate(...)` and
   asserts each yields exactly one `Unknown argument` error.
6. **The ten portal object types are registered with EXACT field sets +
   exact per-field type strings (BOPLA projection boundary)** —
   iterates the ten `PARENT_*_FIELD_TYPES` maps and asserts each
   object's field set + per-field type string matches exactly.
7. **The four entity-shaped portal objects expose `id: ID!` (Apollo
   cache normalization)** — pins the `id: ID!` field on
   `ParentLinkedChild`, `ParentAttendanceEntry`, `ParentReportEntry`,
   `ParentHomeworkEntry`, and asserts the three page wrappers + three
   composite value objects carry NO `id` (embedded value objects, not
   normalizable entities).
8. **Zero-mutation lock — the Mutation root carries ZERO portal-named
   fields (read-only by construction, not by vigilance)** — iterates
   the Mutation root field names and asserts NONE equals any of the
   five portal query names, NONE matches `/^parentChild/`,
   `/^parentMonitoring/`, `/^ParentMonitoring/`, or `myLinkedChildren`.
   The legitimate parent-link mutations (`requestParentChildLink`,
   `respondToParentLinkRequest`, `cancelParentLinkRequest`) are
   pre-existing and do NOT match any of these patterns.
9. **Zero-mutation lock — the committed SDL Mutation block contains
   ZERO lines referencing portal service names** — slices the
   `type Mutation { ... }` block out of the committed SDL and asserts
   it contains NONE of: `myLinkedChildren`, `parentChildProgress`,
   `parentChildSessions`, `parentChildReports`, `parentChildHomework`,
   `parentMonitoring`, `ParentMonitoring`.
10. **Participant-only byte-unchanged — the `sessionReport` and
    `sessionHomework` SDL snippets match the frozen baseline verbatim**
    — asserts the committed SDL contains the two frozen snippet
    constants byte-for-byte, and the live built schema agrees (both
    reads are NULLABLE with EXACTLY ONE required `sessionId: ID!`
    arg). Proves the pre-portal oracle posture (the
    indistinguishable-`null` collapse for non-participants) survives
    the portal landing untouched.
11. **SEC — none of the five portal queries is on the public-operation
    allowlist (all are authenticated, never anonymous)** — asserts
    NONE of the five portal names is a member of
    `PUBLIC_OPERATION_NAMES` / `PUBLIC_OPERATIONS` (BFLA defense —
    anonymous reach must deny pre-resolver).

### Module-scope helper extracted

`rootQueryField(name: string): GraphQLField<unknown, unknown>` — a
fail-fast root Query field lookup, sibling to the existing
`mutationField` helper. Extracted to satisfy
`sonarjs/no-identical-functions` (the initial closure-scoped
`portalQueryField` was identical to the Notification describe block's
`queryField`). The `authScopesSnapshot(rootQueryField(name))`
composition replaces the would-be duplicate `portalAuthScopesOf`
helper.

## Step 4 — Run the updated test suite

```bash
$ bun run test/scripts/run-test.ts backend/graphql/test/schema-surface.test.ts
...
53 pass
0 fail
595 expect() calls
Ran 53 tests across 1 file. [408.00ms]
```

All 53 tests pass (the 10 new portal tests + the 43 pre-existing tests).
The pre-existing tests continue to pass because the portal additions
are PURELY ADDITIVE — the root-query additions pin, the named-type
additions pin, and the codegen-sync describe all grew by exactly the
new portal content, with zero modification to the pre-existing
assertion arrays.

### Tier 4 rename-drift probe (post-refactor)

Two probes confirmed the assertions catch deliberate renames:

1. **Return-type rename**: temporarily changed
   `"ParentChildProgress!"` → `"ParentChildProgress_DRIFT!"` in the
   `parentChildProgress` assertion. Result: the `parentChildProgress`
   test FAILED. Reverted; all 53 tests passed.

2. **Arg-shape rename**: temporarily changed
   `expect(argsByName.get("studentId")).toBe("Int!")` →
   `expect(argsByName.get("studentId")).toBe("Int_DRIFT!")`. Result:
   both the `parentChildProgress` test AND the three paginated-reads
   test FAILED (4 failures total). Reverted; all 53 tests passed.

3. **Field-name rename** (earlier probe): temporarily renamed
   `"myLinkedChildren"` → `"myLinkedChildren_RENAMED_DRIFT_PROBE"` in
   `PARENT_PORTAL_QUERY_FIELDS`. Result: the root-query additions pin
   FAILED (the live schema's `myLinkedChildren` field didn't match the
   renamed assertion). Reverted; all 53 tests passed.

The drift probes confirm the assertions are non-vacuous — a deliberate
rename of any pinned dimension (field name, arg shape, return type)
fails the suite.

## Verification Results

### 3.3.QL — Quality Loop (sub-loop.ts --lifecycle duplicates)

```
$ bun run scripts/health/sub-loop.ts backend/graphql/test/schema-surface.test.ts --lifecycle duplicates
✅ tsgo passed (no errors for backend/graphql/test/schema-surface.test.ts)
✅ oxlint passed
✅ biome:check passed
✅ lint:type-aware passed
✅ check:duplicates passed (skipped — outside jscpd scan scope)
✅ All checks for lifecycle "duplicates" passed  → exit 0
```

One fix iteration during the QL:
- Iteration 1 (with closure-scoped `portalQueryField` + `portalAuthScopesOf`
  helpers): lint:type-aware FAILED with `sonarjs/no-identical-functions`
  — the `portalQueryField` body was identical to the Notification
  describe block's `queryField` at line 1097. Fixed by extracting a
  module-scope `rootQueryField` helper (sibling to the existing
  `mutationField`) and composing `authScopesSnapshot(rootQueryField(name))`
  instead of the duplicate `portalAuthScopesOf`.
- Iteration 2 (with module-scope `rootQueryField` + `authScopesSnapshot`
  composition): ✅ all stages exit 0.

Applicable rule files discovered and read by `sub-loop.ts`: `AGENTS.md`
(root), `backend/AGENTS.md`, `backend/graphql/AGENTS.md`,
`.agents/instructions/backend.instructions.md`,
`.agents/instructions/tests.instructions.md`.

### 3.3.TE — Test Engineering

The SDL assertions ARE the tests (per task spec). All 53 tests pass via
`bun run test/scripts/run-test.ts backend/graphql/test/schema-surface.test.ts`.
Tier 4 rename-drift probe: three probes (field-name, return-type,
arg-shape) all caught the deliberate rename; all probes reverted; the
suite returned to green.

### 3.3.SEC — Security & Tenancy Audit

**Public-operations allowlist** (`backend/lib/gateway/public-operations.ts`):

```
$ grep -iE 'myLinkedChildren|parentChildProgress|parentChildSessions|parentChildReports|parentChildHomework|parentMonitoring' \
      backend/lib/gateway/public-operations.ts
→ No matches found
```

The closed public-operation allowlist needs NO new entries. All five
portal fields carry the explicit
`$all { authenticated: true, role: [UserRole.Parent] }` conjunction:
- Anonymous callers hit the `authenticated` scope's `UnauthorizedError`
  throw (`extensions.code = UNAUTHORIZED` / 401 — explicit scope throws
  pass through `builder.ts`'s `unauthorizedError` mapping VERBATIM).
- Authenticated callers with a wrong role (admin, teacher, student)
  fail the `role` scope into the canonical localized `ForbiddenError`
  (`FORBIDDEN` / 403, mapped at `builder.ts:111-121`).

The SEC test in the new describe block asserts NONE of the five portal
names is a member of `PUBLIC_OPERATION_NAMES` / `PUBLIC_OPERATIONS` —
the allowlist agreement is enforced as code.

**BFLA**: the `$all` conjunction is load-bearing — a plain
`{ authenticated, role }` map would combine with ANY semantics and leak
access. The role set is EXACTLY `[UserRole.Parent]` — no admin override
(pinned via `authScopesSnapshot` on each field).

**BOLA**: zero client-supplied parent identity anywhere. The
zero-arg `myLinkedChildren` derives identity exclusively from
`ctx.user.id`. The four per-student reads accept ONLY `studentId` +
pagination — no `parentId` / `actorId` / `userId` arg exists (pinned
via the smuggled-args validation test).

**BOPLA**: the closed whitelist of forwarded page args
(`{ page: args.page ?? undefined, pageSize: args.pageSize ?? undefined }`)
is pinned via the exact-arg-shape assertions. No spread of `args` into
the service call.

**Zero-mutation posture**: the Mutation root carries ZERO portal-named
fields. Read-only by construction, not by vigilance — the absence of a
portal mutation surface IS the invariant (pinned via two zero-mutation
lock tests).

### 3.3.SR — Semantic Review (full checklist)

**Race Conditions & Concurrency** — N/A (pure test file, no async paths
except the synchronous `validate(...)` / `graphql(...)` calls already
used by the pre-existing tests; no module-level mutable state — the
`PARENT_*_FIELD_TYPES` maps are `const` frozen at module load).

**Environment & Configuration** — N/A (no `resolveEnvConfig`, no cache
invalidation, no credentials).

**Code Quality & Clean Comments**
- [x] No dead branches — every `if`/`throw` is reachable. The
      `if (!queryType)` / `if (!mutationType)` / `if (!field)` /
      `if (!idField)` guards are defense-in-depth narrowing guards
      (the schema is guaranteed to define both root types; the field
      lookups are guaranteed to succeed because the live schema
      registers every pinned field). The `isRecord` guards mirror the
      pre-existing `authScopesSnapshot` pattern.
- [x] No cross-layer imports — only `@/backend/enum/...` (UserRole),
      `@/backend/graphql/gqlSchema`, `@/backend/lib/gateway`
      (PUBLIC_OPERATION_NAMES / PUBLIC_OPERATIONS), `graphql`,
      `bun:test`, `node:fs`, `node:path`. NO `@/frontend`, NO `@/app`,
      NO `@/shared`.
- [x] No manual ReturnType construction — the `PARENT_*_FIELD_TYPES`
      maps are plain `Record<string, string>` constants.
- [x] Clean comments — ZERO references to plan artifacts in my new
      additions (verified by grep scoped to lines > 1650):
  ```
  $ awk -F: '$1 > 1650' <(grep -nE 'REQ-[0-9]|Task [0-9]|plan\.md|tasks\.md|specs\.md|\.ai/plans|R-[A-J]|Phase [0-9]|D1[0-1]|D[1-9]\b|INV-P[0-9]' \
        backend/graphql/test/schema-surface.test.ts)
  → No matches found
  ```
  (The pre-existing file has `REQ-XXX` references in earlier sections
  — those predate this task and are out of scope.)
- [x] No noisy/trivial comments — each comment describes the
      *what/why/domain behavior*: the `$all` conjunction's
      load-bearing rationale, the 401/403 split, the closed-whitelist
      forwarding discipline, the read-only posture, the BOPLA
      projection boundary, the Apollo cache-normalization convention,
      the participant-only oracle posture.

**Schema & Types**
- [x] All enums referenced via the registered Pothos enum objects
      (`SessionStatus`, `SurahJuzRef`) — no string literals where enum
      types expected. The `PARENT_*_FIELD_TYPES` maps use the enum
      TYPE NAMES (`"SessionStatus!"`, `"SurahJuzRef"`,
      `"SurahJuzRef!"`) as they appear in the SDL.
- [x] Nullability EXACTLY matches the TS ReturnType shapes authored
      in task 2.1 — verified field-by-field against the regenerated
      SDL (e.g., `ParentAttendanceEntry.startedAt: DateTime` is
      nullable because the underlying `startedAt` column is nullable;
      `ParentAttendanceEntry.status: SessionStatus!` is non-null
      because the TS enum member is non-null).
- [x] `id: ID!` field on every entity-shaped portal object — exposed
      FIRST via `t.exposeID("id")` (pinned via the
      `the four entity-shaped portal objects expose id: ID!` test).
      The three page wrappers + three composite value objects carry
      NO `id` (embedded value objects, not normalizable entities —
      matches the `SessionPagePothosObject` precedent).

**Deferred Work** — None. No new `deferred-items.md` rows introduced.

**Scope Boundary**
- [x] Only the listed files were modified (`schema-surface.test.ts`
      extended, `schema.graphql` regenerated).
- [x] No refactoring of files outside the task scope — the
      participant-only Pothos files, the parent-monitoring query /
      Pothos files, the service file, the public-operations allowlist,
      and the codegen config are all untouched.
- [x] `git diff --name-only` for this task's footprint (excluding
      pre-existing baseline diff `Caddyfile`, prior-task outcomes, and
      the in-flight test file):
  - `frontend/graphql/generated/schema.graphql` (regenerated, +98 lines)
  - `backend/graphql/test/schema-surface.test.ts` (extended, +443 lines)
  - `ai/plans/sprint_3/parent-read-only-monitoring-portal/tasks.md` (modified — checkbox 3.3 only)
  - `ai/plans/sprint_3/parent-read-only-monitoring-portal/outcome/3.3-codegen-sdl-lock-outcome.md` (this file)
  - `worklog.md` (appended section)

### 3.3.IV — Instruction Verification

Applicable rule files (printed by `sub-loop.ts` discovery) read in
FULL and validated against:

- **`AGENTS.md` (root)** — `@/*` path alias used for every import
  (`@/backend/enum/users/user-role.enum`,
  `@/backend/graphql/gqlSchema`, `@/backend/lib/gateway`); NO relative
  paths; NO `oxlint-disable` comments; GraphQL Document Conventions
  (`id` first on entity-shaped Pothos objects — pinned via the
  `id: ID!` assertion on the four entity-shaped portal objects);
  per-file sub-loop verification BEFORE wiring consumers (the test
  file passed sub-loop exit 0 at the deepest lifecycle stage); GraphQL
  Schema Generation section honored (`bun run generate:gqlSchema` +
  `bun codegen` ran after the schema changes from tasks 3.1/3.2).
- **`backend/AGENTS.md`** — 6-layer data flow respected (Test layer
  pins the GraphQL layer's surface); no service-layer `.types.ts`
  files touched; NO `oxlint-disable` comments; single canonical
  GraphQL object type per entity (the ten `*PothosObject` refs from
  task 3.1 are the only producers of these shapes).
- **`backend/graphql/AGENTS.md`** — Schema generation rules honored
  (`bun run generate:gqlSchema` + `bun codegen` after schema changes
  — done); `authScopes` rules honored (explicit `$all` conjunction,
  fail-closed scope semantics, 401/403 split — pinned via
  `authScopesSnapshot`); nullability rule (Pothos defaults
  non-nullable — `t.arg.int({ required: true })` for `studentId: Int!`,
  `t.arg.int()` for `page: Int` / `pageSize: Int` — pinned via the
  exact-arg-shape assertions); Gateway public-operation allowlist rule
  (closed-set, default-deny — the portal fields are NOT added, verified
  by the SEC test); Scope composition rule (`$all` for
  "authenticated + role" fields — NEVER a plain key-map — pinned via
  the `$all` conjunction assertion).
- **`.agents/instructions/backend.instructions.md`** — GraphQL Schema
  Generation section (run `bun run generate:gqlSchema` + `bun codegen`
  — done); no nested ternary operators (verified — every conditional
  uses `if` / `??` with simple operands, no chained `?:`).
- **`.agents/instructions/tests.instructions.md`** — Run-Test Script
  used (`bun run test/scripts/run-test.ts backend/graphql/test/schema-surface.test.ts`
  — done); `bun:test` imports (`describe`, `expect`, `test` — verified);
  no `any` (verified — uses `unknown` + `isRecord` type guards, the
  pre-existing pattern); clean up unused imports (verified — the
  refactor removed the duplicate `portalQueryField` /
  `portalAuthScopesOf` helpers); NEVER `oxlint-disable` (verified);
  static source-scan pin tests updated in the SAME change (the
  `PARENT_PORTAL_*` constants are pinned in the same commit as the
  test extensions — no stale pins).

No in-file rule violations. No cross-file blockers caused by this task.

## INV-P2 lock proof

The portal exposes ZERO new mutations — read-only by construction, not
by vigilance. Two complementary assertions pin this invariant:

1. **Live schema introspection** — iterates the Mutation root field
   names and asserts NONE equals any of the five portal query names,
   NONE matches `/^parentChild/`, `/^parentMonitoring/`,
   `/^ParentMonitoring/`, or `myLinkedChildren`. The legitimate
   parent-link mutations (`requestParentChildLink`,
   `respondToParentLinkRequest`, `cancelParentLinkRequest`) are
   pre-existing and do NOT match any of these patterns (they're
   pinned in `RECONCILED_PARENT_LINK_MUTATION_FIELDS`).

2. **Committed SDL slice** — slices the `type Mutation { ... }` block
   out of `frontend/graphql/generated/schema.graphql` and asserts it
   contains NONE of: `myLinkedChildren`, `parentChildProgress`,
   `parentChildSessions`, `parentChildReports`, `parentChildHomework`,
   `parentMonitoring`, `ParentMonitoring`.

Both assertions pass — the Mutation root carries 65 field definitions
(the same set as the pre-portal baseline), and ZERO of them reference
the portal service / domain vocabulary.

## REQ-031 byte-unchanged proof

The participant-only `sessionReport` and `sessionHomework` query field
definitions are byte-unchanged vs the pre-portal baseline. Two frozen
SDL snippet constants capture the exact field-definition text:

```
const PARTICIPANT_SESSION_REPORT_SDL = "sessionReport(sessionId: ID!): SessionReport";
const PARTICIPANT_SESSION_HOMEWORK_SDL = "sessionHomework(sessionId: ID!): SessionHomeWork";
```

The test asserts the committed SDL contains both snippets verbatim.
The live built schema agrees — both reads are NULLABLE (the
indistinguishable-`null` collapse for non-participants) with EXACTLY
ONE required `sessionId: ID!` arg.

Pre-portal vs post-portal byte-equality was also verified directly
during the SDL regeneration:

```
$ git diff HEAD -- frontend/graphql/generated/schema.graphql | grep -E '^[+-].*(sessionReport|sessionHomework)'
→ No matches found  (the diff shows ZERO changes to those lines)
```

The only changes to `schema.graphql` are PURELY ADDITIVE — the ten new
parent-portal object types + the five new root query field definitions.
The participant-only oracle posture (the indistinguishable-`null`
collapse) survives the portal landing untouched.

## Public-operations allowlist proof

The closed public-operation allowlist
(`backend/lib/gateway/public-operations.ts`) needs NO new entries. All
five portal fields are authenticated (never anonymous):

```
$ grep -iE 'myLinkedChildren|parentChildProgress|parentChildSessions|parentChildReports|parentChildHomework|parentMonitoring' \
      backend/lib/gateway/public-operations.ts
→ No matches found
```

The allowlist retains its 6-member closed set (`login`, `refreshToken`,
`logout`, `registerUser`, `recitationReadings`, `_health`). The SEC
test in the new describe block asserts NONE of the five portal names is
a member of `PUBLIC_OPERATION_NAMES` / `PUBLIC_OPERATIONS` — the
allowlist agreement is enforced as code, not by vigilance.

## Carry-forward knowledge for tasks 5.1 / 5.4 / 6.3 / 7.1

### Task 5.1 — Frontend GraphQL documents

Task 5.1 will create the five portal documents
(`myLinkedChildrenQueryDocument`, `parentChildProgressQueryDocument`,
`parentChildSessionsQueryDocument`, `parentChildReportsQueryDocument`,
`parentChildHomeworkQueryDocument`) in
`frontend/graphql/sharedDocuments/parents.parent-monitoring.documents.ts`.
After the documents land, a SECOND codegen refresh is required:

```bash
bun run generate:gqlSchema   # no-op (SDL unchanged — no new schema)
bun codegen                   # regenerates gql/graphql.ts with the new typed-document-nodes
```

The codegen config's `documents` glob
(`./frontend/graphql/sharedDocuments/**/*.ts`,
`./frontend/views/**/*.documents.ts`) will pick up the new documents
and generate the typed-document-node types. The
`frontend/graphql/generated/gql/graphql.ts` file (byte-unchanged in
THIS task because no documents existed yet) will gain the new
`*QueryDocument` / `*Query` / `*QueryVariables` types.

### Task 5.4 — Apollo cache policy

The six no-`id` portal types (`ParentAttendancePage`, `ParentReportPage`,
`ParentHomeworkPage`, `ParentHomeworkTrack`, `ParentHomeworkPosition`,
`ParentChildProgress`) need `keyFields: false` entries in
`frontend/providers/apollo/apolloCache.ts`. The `id: ID!` assertion in
the new test suite confirms the four entity-shaped objects
(`ParentLinkedChild`, `ParentAttendanceEntry`, `ParentReportEntry`,
`ParentHomeworkEntry`) DO carry `id` — those need NO `keyFields: false`
entry (Apollo's default `id`-based normalization works).

### Task 6.3 — Wire-level coverage

The SDL surface is now locked. Task 6.3's wire tests should cover:
- **BFLA**: anonymous → 401; admin/teacher/student → 403 on every
  field (the `$all` conjunction is pinned in this task's test suite).
- **BOLA**: parent probing a foreign `studentId` → constant 403; the
  oracle posture is enforced service-side (task 2.4's
  `requireLinkedChild` gate).
- **Happy path**: linked parent → data, correct projection shapes
  (the field types are pinned in this task's test suite).
- **Read-only posture**: assert the `Mutation` type exposes ZERO
  portal-named fields — pinned in this task's test suite (the
  zero-mutation lock).

### Task 7.1 — Post-implementation review

The SDL surface lock is the primary INV-P2 evidence for task 7.1's
post-implementation review. The zero-mutation lock + the
participant-only byte-unchanged pin + the public-operations allowlist
SEC pin together prove the read-only posture is enforced by
construction, not by vigilance.

## Cross-file dependencies discovered

### CROSS-FILE DEPENDENCY 1 (informational — resolved in this task)

```
Target file: backend/graphql/test/schema-surface.test.ts  (this task — CLEAN, sub-loop exit 0)
Blocked by:  none — task 3.2's query file is committed at eab5658 (feat branch HEAD)
Rule violated: none
Required fix: this task ran `bun run generate:gqlSchema` + `bun codegen` to refresh
              the SDL + extended the schema-surface assertion suite with the five
              field names + arg shapes + the zero-mutation lock + the REQ-031
              byte-unchanged participant-fields pin.
Status: resolved by this task. No outstanding cross-file dependencies.
```

No other cross-file dependencies. Task 3.3 is structurally complete
and clean.

## Notes on convention adherence

- **Schema-surface assertion style** — the new describe block mirrors
  the existing `admin-governance mutations` describe block's
  conventions: a closure-scoped `queryType` narrowing guard at
  describe setup, the module-scope `rootQueryField` /
  `authScopesSnapshot` helpers for field lookup + scope introspection,
  and per-test `expect(...).toBe(...)` pins for each dimension. The
  belt-and-braces codegen-sync checks in the existing `Codegen sync`
  describe block follow the same `expect(committedSdl).toContain(...)`
  pattern as the session-recitation surface pins.
- **Module-scope helper extraction** — the `rootQueryField` helper
  was extracted to satisfy `sonarjs/no-identical-functions` (the
  initial closure-scoped `portalQueryField` was identical to the
  Notification describe block's `queryField`). This follows the
  precedent of the existing module-scope `mutationField` helper —
  a sibling fail-fast root field lookup.
- **Frozen SDL snippet constants** — the
  `PARTICIPANT_SESSION_REPORT_SDL` /
  `PARTICIPANT_SESSION_HOMEWORK_SDL` constants capture the exact
  field-definition text as it appeared in the pre-portal baseline.
  This mirrors the codegen-sync describe's `committedSdl.toContain(...)`
  pattern but elevates the snippet to a named constant so the
  byte-equality contract is self-documenting.
- **Purely additive extensions** — the existing assertion arrays
  (`PRE_3_1_QUERY_FIELDS`, `PRE_3_1_MUTATION_FIELDS`, `PRE_3_1_ENUMS`,
  `PRE_3_1_TYPE_NAMES`, and the additions arrays in the
  `root query retains EXACTLY the baseline fields plus the pinned
  additions` and `whole-schema named-type delta is pinned` tests)
  all grew by EXACTLY the new portal content. Zero modification to
  the pre-existing assertion arrays — the portal additions ride on
  the same documented-one-time-reconciliation idiom.
- **Sandbox branch fast-forward** — the `feat/parent-read-only-monitoring-portal`
  branch was 1 commit behind `main` (the 3.2 commit `eab5658` had
  landed on `main` but not on `feat`). A `git merge main --ff-only`
  brought `feat` up to `eab5658` before this task's commit landed.
  The 3.3 commit `bfe2452` now sits on top of `eab5658` on the feat
  branch.

_Requirements: REQ-023, REQ-030, REQ-031_
