# Task 6.2 Outcome — review-backend (READ-ONLY)

**Task ID:** 6.2 · **Agent:** review-backend · **Date:** 2026-09-07 · **Verdict: CONDITIONAL PASS — 1 HIGH + 1 LOW, 0 CRITICAL/MEDIUM. Zero production-code diff CONFIRMED; journey and wire deltas are high-quality and green. The HIGH is a process/coverage loss: the two service-tier cells task 2.2 claims to have added are NOT in the tree (see F1).** No fixes applied (read-only mission).

Delta files reviewed: `test/workflows/parents/student-confirmation-of-link.journey.test.ts` (NEW, 1278 lines), `backend/graphql/test/parent-link.wire.test.ts` (+200/−11), `backend/services/parents/parent-link-request.service.test.ts` (**ZERO diff in the delta range** — this zero-diff is itself finding F1).

---

## Check 1 — Zero production-code diff (git evidence) — PASS (with F1)

- `git diff ffce457 feat/dev1-015-student-confirmation-of-parent-link --stat -- backend/` → **exactly ONE file**: `backend/graphql/test/parent-link.wire.test.ts | 211 ++++++…` (+200/−11).
- `git diff ffce457 feat/… --stat -- backend/ test/workflows/` → exactly TWO files: the wire test + `student-confirmation-of-link.journey.test.ts | 1278 +++` (new).
- Non-test exclusion probe — `git diff ffce457 feat/… --stat -- backend/ ':(exclude)*.test.ts' ':(exclude)test/**'` and the explicit scoped command `-- 'backend/services/**' 'backend/db/repo/**' 'backend/graphql/mutation|query|pothos/**' ':!*.test.ts'` → **EMPTY output: zero production-code diff** in services, repos, and all GraphQL resolver/pothos layers. Guarded-claim SQL provably unchanged (nothing in `backend/db/**` differs; no `SELECT FOR UPDATE`/advisory-lock text added anywhere in the delta diff — `rg '^\+'` over the full range diff = zero matches).
- ⚠️ The check's expectation "ONLY the three test files appear" does NOT hold: the third in-scope file (`parent-link-request.service.test.ts`) shows a **ZERO diff**, although commit `40634a3` is titled "test(services): governance pre-tx ordering + committed double-respond idempotency cells" and 2.2-outcome.md §4 documents a `+198/−2` change to it. That commit contains ONLY `tasks.md` (verified: `git show 40634a3 --stat` = tasks.md 6+/6−). → **F1**.
- Working-tree parity: `git diff feat/… --stat -- <three delta files>` = empty (reviewed content == branch HEAD).

## Check 2 — Journey audit — CLEAN

- **Honest permission resolution:** zero `mock.module`/`mock(`/`vi.`/`jest.` in the file; every denial (role arm, governed arm, BOLA arm) resolves through the REAL `requireActor` re-check against real committed rows (L57–59, L336–348, L573–600, L679–711). Governance is an honest fixture (`setGovernanceFixture` committed write, re-verified at L685–689); L is genuinely pre-linked via `linkStudentToParentFixture` (L466). No monkey-patching anywhere.
- **`runInRollback` ABSENT:** `rg 'runInRollback\('` over the journey → zero code hits (only the docblock's "NEVER runInRollback" statement, L70). All fixtures committed; services own their transactions.
- **Committed fixtures + FK-safe teardown + zero-residue probes:** cast provisioned in ONE committing transaction (L405–452, commit-or-nothing); `TrackedFixtures` deletes in reverse registration order (notifications → requests → role-child rows → users — helper contract verified in `test/workflows/helpers/tracked-fixtures.ts`) with a **load-bearing** re-probe + `jrn_sconfirm_` prefix residue probe (L1255–1276). Race-cast ids tracked (L1019); inbox rows of ALL journey users registered LAST in `afterAll` (L1239–1246).
- **Spied (never real) fanout/email/SMS:** `SpiedFanoutTransport` installed ONLY at the `options.transport` seam (L196, L236–238, L1041–1049); per-racer transports isolate winner/loser publish proofs (L1039–1049, L1097–1098). No Redis/WS/email anywhere.
- **`isPgliteProvider` wholesale-skip on the true-race cell ONLY:** L985 `describeOnRealPostgres` wraps Step 9b (L987–1104) exclusively; Steps 1–8, 9a, 10, 11 run everywhere. Verified live: on this environment's real-PostgreSQL provider 9b RAN and passed (1.03s).

## Check 3 — Service-test cells — VIOLATED BY ABSENCE (F1)

- The file still honors its own conventions (pre-existing): 28 `runInRollback` cells with explicit `tx` propagation, 20 `expectRepoError` try/catch uses, zero `rejects.toThrow` (only a doc-comment mention), notification engine reached ONLY via the `options.transport` adapter seam (`RecordingFanoutTransport`, L210/234/246).
- **BUT the two cells 2.2 says it added — Cell A "governance PRE-TX ordering" (claimed at S L2048) and Cell B "sequential committed double-respond idempotency" (claimed at S L1079) — DO NOT EXIST.** Distinctive-phrase search (`governance PRE-TX ordering|sequential double-respond idempotency|student-decide-twice|zero calls past the gate`) across the repo excluding `ai/` → zero hits. Executed suite counts **35 tests** = the documented pre-2.2 inventory (2.2-outcome: "Before this task: 35 tests"), not the post-2.2 37 (33 pass/4 skip quoted by 2.2-outcome §1 and 2.M-outcome L24). → **F1**.

## Check 4 — SQL guards unchanged; log hygiene; `console.*` — CLEAN

- No `FOR UPDATE` / `pg_advisory` text added in the entire backend+workflows range diff; zero production diff at all (check 1). Static-locks suite (the guarded-claim/BOPLA pin corpus) re-run: **36 pass / 0 fail**.
- Added logging: NONE in the delta (no `logDomainError`, no `console.*` — `rg 'console\.'` over all three files → zero matches; the `[DOMAIN]` lines in suite output are the services' own bounded emissions, not test logging). No names/codes leak risk introduced.

## Check 5 — Race-condition semantics of the added cells — MOSTLY CLEAN (2 of the named cells are missing, F1)

- **Journey 9a double-respond loser collapse (deterministic emulation):** committed winner first, then the loser respond → typed `PARENT_LINK_REQUEST_ALREADY_RESOLVED` + zero loser notifications/publishes (L907–976). No timing dependency; pins the replay-silence invariant deterministically. (Journey-tier sibling of the missing service Cell B — partial mitigation, not a substitute for the exactly-one-notification byte-identity replay pin.)
- **Journey 9b TRUE race:** `Promise.allSettled`, zero sleeps; exactly-one-winner/one-loser shape pinned; loser accepted as typed conflict (`ALREADY_RESOLVED`/`TARGET_ALREADY_LINKED`) OR `40P01` deadlock abort via cause-chain walk (L1071–1077) — both prove "loser commits nothing"; per-racer transport spies; base-cast isolation snapshot before/after (L992, L1101–1102). Correctly wholesale-skip-gated on PGlite. Non-flaky design.
- **Journey 10 boundary + wire expired cell:** deterministic boundary injection — journey writes `expiresAt = new Date()` directly (L1113–1127) and the wire fixture seeds `expiresAt = now − 60min` at `beforeAll` (diff hunk @ L646+); strict-`>` liveness then denies deterministically, and the fold-first semantic (`Expired` + `respondedAt` NULL, the denial's only write) is pinned. Wire foreign≡absent byte-identity is stabilized by a pinned `x-request-id` correlation header (no per-request `extensions.requestId` drift). No arbitrary sleeps anywhere (`rg 'setTimeout|sleep'` → zero).
- **Service governance pre-tx ordering cell:** MISSING (F1) — the repo-spy zero-invocation ordering proof REQ-022/D9a has NO tier anywhere (journey Step 5 pins the denial + zero side effects, which cannot distinguish pre-tx from post-harmless-read ordering).
- **Service double-respond idempotency cell:** MISSING (F1) — the committed sequential replay with "parent keeps EXACTLY ONE notification + ONE publish total" has NO service-tier pin.

## Check 6 — Dead code in the delta test files — CLEAN

- Journey: every import consumed (verified one-by-one, incl. `like`/`or` teardown probes, `hasPgCode`, `settleRace`, `errorFingerprint`, all 11 helper imports); no unused locals; closed key-set constants (`OUTGOING_KEYS`/`INCOMING_KEYS`) all used.
- Wire delta: new `registerActorCast` helper (sequential reduce — documented PGlite single-connection SAVEPOINT `sp1` rationale), `expiredRequestId`, the expired seeded row (pair-collision-safe: rides studentG's Rejected-pair outside the partial-unique pending predicate), and the new `tEn` message pins all used; pre-existing helpers reused verbatim (`expectDenialCode(body, code, "absent")` matches the existing 3-arg signature).

## Run evidence (this environment, real-PostgreSQL provider; read-only runs)

| Suite | Result |
|---|---|
| `backend/services/parents/parent-link-request.service.test.ts` | **35 pass / 0 fail** (328 expect) — 35 = pre-2.2 inventory ⇒ 2.2 cells absent (F1 evidence) |
| `test/workflows/parents/student-confirmation-of-link.journey.test.ts` | **12 pass / 0 fail** (257 expect); Step 9b RAN (skip guard inert on real PG) |
| `backend/graphql/test/parent-link.wire.test.ts` | **25 pass / 0 fail** (474 expect) |
| `backend/services/parents/parent-link.static-locks.test.ts` | **36 pass / 0 fail** |

---

## Findings

- **[HIGH]** `backend/services/parents/parent-link-request.service.test.ts` (whole file; commit `40634a3`; docs `2.2-outcome.md` §4/L99–117, `2.M-outcome.md` L24/L55, `tasks.md:121`) — the two service-tier regression cells task 2.2 documents as added ("Tier 4 — governance PRE-TX ordering on the decision mutation… repo spies: zero calls past the gate" at claimed S L2048, and "Tier 1/3 — sequential double-respond idempotency… EXACTLY ONE notification total" at claimed S L1079, +198/−2) are **absent from every branch, commit, stash, and the working tree**: the 2.2 commit contains only tasks.md, the file has a ZERO diff across ffce457..feature-HEAD, and the executed suite runs 35 tests (the documented pre-2.2 count) instead of 37. Consequences: (a) REQ-022's pre-tx ordering (repo-spy zero-invocation proof) is unpinned at ANY tier; (b) REQ-013's committed sequential replay idempotency (exactly-one-parent-notification + single `respondedAt` stamp byte-identity) is unpinned at service tier (only the journey-9a loser-collapse sibling and the concurrent real-PG chaos cell exist); (c) 2.2/2.M outcome run-evidence (33/4/0, "2.2-modified +198/−2") and the tasks.md 2.2 checkbox now describe a tree state that never landed — documentation integrity drift that wave 6.2 was mandated to catch. Suggested fix: re-apply the two cells from 2.2-outcome.md §4's complete specifications (Cell A: `runInRollback` + `outerTx`, call-through spies over the 8 post-gate collaborators, assertions BEFORE `mockRestore()`; Cell B: committed `student-decide-twice` fixture, `expectConflict` replay, inbox-delta convention) as one test-only commit, then correct/annotate the 2.2 + 2.M outcome counts and re-run 5.1's services-dir differential — no production files involved.
- **[LOW]** `test/workflows/parents/student-confirmation-of-link.journey.test.ts:1119` — the Step-10 boundary fixture injects `expiresAt` from the JS clock (`new Date()`) while the service claim predicate compares against the DB's `now()`; determinism therefore relies on app-host and DB-host clocks not drifting apart by more than the insert→claim elapsed time. Safe on this sandbox and on PGlite (same process/host) and it is the sanctioned DEV1-014 journey-C idiom, but on a split-host real-PostgreSQL CI the boundary respond could theoretically win the claim and flip the expected `PARENT_LINK_REQUEST_EXPIRED` denial into a confirm. Suggested fix: source the injected instant from the database (e.g., `SELECT now()` / `db.execute(sql`now()`)`) so fixture and predicate share one clock — one-line hardening; acceptable to defer with a comment.

**Pre-existing (outside delta — filtered, not counted):** wire-tier `afterAll` deletes in FK-safe order but carries no explicit zero-residue re-probe (the journey's `TrackedFixtures` standard); sequential `registerActorCast` costs ~6 sequential HTTP registrations on real-Postgres runs (documented, correct); 5.1's known foreign `schema-surface` pin drifts (DI-3.1-01-at-HEAD).

---

## Fix round 1 (FIX-A)

**Task ID:** FIX-A · **Agent:** Backend Fix Subagent · **Date:** 2026-09-07 · **Scope:** test-only; resolves F1 (HIGH) + F2 (LOW). File edits only — zero git state changes, zero production files touched.

### F1 (HIGH) — the two 2.2 service cells RE-APPLIED

Both cells rebuilt exactly per `2.2-outcome.md` §4's specifications (the clobbered working-tree diff re-created from the documented specs; no other cell touched). Placements mirror the documented post-2.2 inventory: Cell B directly after the repo-level ALREADY_RESOLVED cell inside `respondToLinkRequest`; Cell A directly after the pre-issued-token governed-denial cell inside the security tier.

- **Cell A** — `Tier 4 — governance PRE-TX ordering on the decision mutation: the governed actor's respond denies BEFORE any transactional work (repo spies: zero calls past the gate)` (S L2063): `runInRollback` + `tx` as the service's `outerTx`; `expectRecheckDenial` try/catch idiom (no `rejects.toThrow`); governed student's respond vs parent role arm → byte-equal `ForbiddenError` fingerprints; call-through spies over ALL EIGHT post-gate collaborators (`respondToPendingForStudent`, `findById` [the classifier oracle], `linkParentIfUnlinked`, `expireSiblingPendingsForStudent`, `findIncomingRowById`, `markExpiredIfPending`, `findLocalesByIds`, `emitForUser`) → ZERO invocations; the gate's own `UserRepository.findById` records EXACTLY its sanctioned reads `[studentGovId, parentAId]` in call order (identity from `actorUserId` alone — BOLA); exactly ONE bounded `{code:"FORBIDDEN", entity:"users", entityId, locale}` log per denial; zero publishes; every spy assertion placed BEFORE the `finally` restores (bun's `mockRestore` clears mock state).
- **Cell B** — `Tier 1/3 — sequential double-respond idempotency (own-commit): the replay answers the constant ALREADY_RESOLVED conflict and the parent holds EXACTLY ONE notification total` (S L1084): dedicated committed `student-decide-twice` fixture in its own committing `db.transaction`, tracked in `trackedRequestIds` (the file's own-commit convention — the service owns the commit boundary on BOTH decisions, so `runInRollback` cannot host it); decision #1 accept → in-tx emit + EXACTLY ONE post-commit publish; sequential replay → `expectConflict` with `PARENT_LINK_REQUEST_ALREADY_RESOLVED` + the frozen `{code, entity:"parent_link_requests", entityId, locale}` log EXACTLY ONCE; parent inbox filtered by `relatedEntityId` stays at EXACTLY ONE row with the acceptance title/body in the parent's persisted locale; `publishCount` stays 1; the row keeps its single terminal state with the FIRST `respondedAt` byte-identical (`toISOString` equality); the link write is untouched; the deciding student's inbox gains nothing.
- **Header coverage map** re-extended in place (−2/+14, documentation hygiene) to name both pins.

**Run evidence** (this environment: `DB_PROVIDER=postgres` — the 4 boundary-tier cells RUN here; on the 2.2-documented `pglite` provider they are the pre-existing `describeBoundary` skips, so a pglite run shows the 2.2-quoted 33 pass / 4 skip):

| # | Command | Result |
|---|---|---|
| 1 | `run-test.ts backend/services/parents/parent-link-request.service.test.ts` (BEFORE fix) | **35 pass / 0 fail** (328 expect) — F1's evidence count |
| 2 | same (after, run 1) | **37 pass / 0 fail** (372 expect) — +2 tests, +44 expects, 0 regressions |
| 3 | same (after, run 2 — teardown idempotency) | **37 pass / 0 fail** (372 expect) |
| 4 | `run-test.ts backend/services/parents/parent-link.static-locks.test.ts` (corpus guard) | **36 pass / 0 fail** — the modified file is in its corpus; `console.*` lock + corpus guards hold |
| 5 | `sub-loop.ts backend/services/parents/parent-link-request.service.test.ts --lifecycle duplicates` | **exit 0** (tsgo, oxlint, biome:check, lint:type-aware, check:duplicates) |

Diff check: `git diff --stat -- backend/ test/workflows/` after the fix shows exactly the two intended test files (service test +219/−2 vs HEAD; journey +13/−2 for F2) — zero production files.

### F2 (LOW) — journey Step-10 boundary clock hardened

`test/workflows/parents/student-confirmation-of-link.journey.test.ts` (~L1119): the boundary fixture's `expiresAt` is now sourced from the DATABASE clock — `tx.execute<{ now: Date }>(sql`select now() as now`)` inside the fixture's own committing transaction (the `db.execute<T>(…).rows` idiom the analytics journey already uses) — instead of the JS `new Date()`. The injected instant and the strict-`>` liveness predicate now share one clock; the split-host drift exposure named by the finding is closed. Run evidence: journey **12 pass / 0 fail** (257 expect); `sub-loop.ts …journey --lifecycle duplicates` → **exit 0**. (One intermediate sonarjs `different-types-comparison` hit on `rows[0]?.now === undefined`, fixed by the file's own `.at(0)` idiom; final runs clean.)

**Follow-ups NOT in FIX-A's mission (left for the wave lead):** annotate the 2.2/2.M outcome counts (their 33/4/0 was a pglite-provider count of a tree that had not landed; the tree now matches it) and re-run 5.1's services-dir differential.

---

## Worklog

- **Task ID:** 6.2 · **Agent:** review-backend · **Date:** 2026-09-07
- Grep'd `tasks.md` for "6.2" −A7 (wave definition); read all three in-scope files in full (journey 1278 L, wire 1456 L, service 2275 L) plus the full `git diff ffce457 feat/…` hunks for the wire file.
- Check 1: ran the scoped `--stat` diffs (backend/, backend+test/workflows/, non-test exclusions) — zero production diff; discovered the service test's ZERO diff and traced commit `40634a3` (tasks.md-only), the dangling stash (`de32963`, locale-only), and `git log --all -- <service test>` (last touch `b01d21d`, pre-delta) to prove the 2.2 cells never landed.
- Checks 2–6: full-read audits (honest permission resolution, `runInRollback` absence, committed fixtures/FK-safe teardown/zero-residue probes, `SpiedFanoutTransport` seam, `isPgliteProvider` single-cell guard, `console.*`/sleep/FOR-UPDATE/mock.module greps, import-by-import dead-code walk) + helper-contract verification (`tracked-fixtures.ts`, `spied-transport.ts`, wire `expectDenialCode` signature).
- Ran four suites read-only for evidence (service 35/0, journey 12/0, wire 25/0, static-locks 36/0); no file modified except this outcome; `tasks.md` untouched; no git state changes (status/show/diff/fsck read-only only).
- Verdict per check: 1 PASS+F1 · 2 CLEAN · 3 F1 · 4 CLEAN · 5 CLEAN-modulo-F1 + F2 · 6 CLEAN. Findings: 1 HIGH, 1 LOW.

- **Task ID:** FIX-A · **Agent:** Backend Fix Subagent · **Date:** 2026-09-07
- Read `2.2-outcome.md` §1–§11 in full (§4 cell specs, §2 coverage table, §5–§7 conventions) and the service test file end-to-end (2275 L) to recover the file's idioms before writing (governed-denial cell, notification-count/inbox-delta idiom, committed-fixture + tracked-cleanup, `expectRecheckDenial`/`expectConflict` try-catch, assert-before-`mockRestore`).
- F1: re-created Cell B (own-commit `student-decide-twice` fixture, replay → constant ALREADY_RESOLVED + one bounded log + exactly-one parent notification/publish + FIRST `respondedAt` byte-identity + link untouched) and Cell A (governance PRE-TX ordering: call-through spies over the 8 post-gate collaborators → zero calls; gate's `UserRepository.findById` = exactly `[studentGovId, parentAId]` in call order; byte-fingerprint parity governed ≡ role arm; one `entity:"users"` log per denial); re-extended the header coverage map (−2/+14). NO git commands beyond read-only status/diff.
- F2: journey Step-10 `expiresAt` now read from the DB clock inside the fixture transaction (`tx.execute(sql`select now() as now`)` + `.at(0)` guard) instead of `new Date()`.
- Verified: service suite 35/0 → **37/0 (372 expect), twice**; static-locks corpus guard 36/0; services sub-loop duplicates **exit 0**; journey **12/0 (257 expect)**; journey sub-loop duplicates **exit 0**. Outcome doc updated (Fix round 1 section above); 2.2/2.M counts and 5.1 differential left to the wave lead.
