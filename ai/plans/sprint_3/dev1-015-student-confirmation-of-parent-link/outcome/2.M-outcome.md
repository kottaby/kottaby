# 2.M — Mid-Point Review GATE — Outcome

**Date:** 2026-09-07 · **Task ID:** 2.M (DEV1-015 — Student Confirmation of Parent Link)
**Nature:** Verification-only gate (BLOCKS Phase 3+). **Zero source files modified by this task** (only: this outcome file, the `2.M` checkbox flip in `tasks.md`, and the DI-2.1-01 status reclassification in `deferred-items.md`).

---

## VERDICT: ✅ GATE PASS

All seven checklist items pass with evidence below. Zero NEW backend findings. Phase 3 is unblocked.

---

## 1. Gate checklist evidence

### Item 1 — `bun run test/scripts/run-test.ts backend/services/parents` — ✅ ALL GREEN (exit 0)

**Totals: 96 pass / 15 skip / 0 fail · 483 `expect()` calls · 111 tests across 4 files.**

| File | Pass | Fail | Skip | Note |
|---|---|---|---|---|
| `parent-link-request.helpers.test.ts` | 27 | 0 | 0 | |
| `parent-link.static-locks.test.ts` (DI-0.2-01 CORRECT filename) | 36 | 0 | 0 | corpus guards hold over the modified service-test + new journey files (incl. the console-lock cell (d)) |
| `parent-link-request.service.test.ts` (2.2-modified) | 33 | 0 | 4 | 4 skips = PRE-EXISTING PGlite boundary tier (`describeBoundary`), untouched |
| `parent-link-request.chaos.test.ts` | 0 | 0 | 11 | all real-PostgreSQL cells behind the preserved `describeOnRealPostgres` guard (PGlite environment) |

Log: `/tmp/gate-svc-parents.log` → `logs/2026-09-07T09-16-57/backend/services/parents.log`. Runner totals quoted verbatim (the runner exits 0 when the suite is green; 0 fail).

### Item 2 — `bun run test/scripts/run-test.ts test/workflows/parents` — ✅ ALL GREEN (exit 0)

**Totals: 38 pass / 1 skip / 0 fail · 598 `expect()` calls · 39 tests across 3 files (both journeys + sibling).**

| File | Pass | Fail | Skip |
|---|---|---|---|
| `parent-link-request.journey.test.ts` (DEV1-014 sibling) | 19 | 0 | 0 |
| `handshake-discovery.test.ts` | 8 | 0 | 0 |
| `student-confirmation-of-link.journey.test.ts` (2.1) | 11 | 0 | 1 (Step 9b TRUE-race cell behind the `isPgliteProvider` guard) |

Log: `/tmp/gate-wf-parents.log`. Layer-wide `test/workflows` deliberately NOT re-run — per the 2.M gate scope (tasks.md 2.M: `backend/services/parents` + `test/workflows` parents domain); the 13 foreign-domain failures are pre-existing on base and are ledgered (DI-2.1-01, reclassified below).

### Item 3 — `bun run tsgo` — ✅ 0 errors (baseline = 0, ≤ baseline)

- Exit code **0**; `rg -c "error TS"` over the full output = **0 matches**; no "Found N errors" summary line emitted. Output log: `/tmp/gate-tsgo.log`.

### Item 4 — `git diff` vs Phase-0 baseline (empty baseline) — ✅ ZERO unexpected production edits

Baseline was captured at 0.1 as a clean tree; every diff below is therefore attributable to this plan. Full inventory (`git diff --name-only` = 7 modified; untracked = 6):

**Modified (7) — classification:**

| File | Class | Owner |
|---|---|---|
| `ai/plans/sprint_3/dev1-015-student-confirmation-of-parent-link/deferred-items.md` | EXPECTED — plan-dir ledger | 0.2→2.M |
| `ai/plans/sprint_3/dev1-015-student-confirmation-of-parent-link/tasks.md` | EXPECTED — plan-dir checkbox flips | 1.1/2.1/2.2 (2.M this task) |
| `backend/services/parents/parent-link-request.service.test.ts` | EXPECTED — the ONE allowed backend diff: the 2.2 TEST file (+198/−2, the −2 = header coverage-map comment lines) | 2.2 |
| `shared/locale/types/parentLink/index.ts` | EXPECTED | 1.1 |
| `shared/locale/en/parentLink/index.ts` | EXPECTED | 1.1 |
| `shared/locale/ar/parentLink/index.ts` | EXPECTED | 1.1 |
| `shared/locale/parentLink-namespace.parity.test.ts` | EXPECTED — DI-0.2-03 mandated-keys extension | 1.1 |

**Untracked (6) — classification:**

| File | Class | Owner |
|---|---|---|
| `outcome/0.1-outcome.md`, `0.2-outcome.md`, `1.1-outcome.md`, `2.1-outcome.md`, `2.2-outcome.md` | EXPECTED — plan-dir outcome files | 0.1/0.2/1.1/2.1/2.2 |
| `test/workflows/parents/student-confirmation-of-link.journey.test.ts` | EXPECTED — 2.1 journey (new TEST file) | 2.1 |

**Production-path explicit check (all EMPTY):** `git diff --name-only -- backend/db/repo backend/graphql backend/types app frontend backend/db/schema backend/db/migration` → **zero files**; `git diff --name-only -- backend/services` → ONLY the test file above. Nothing staged (`git diff --cached` empty). **Unexpected production edits: NONE → gate condition met.**

### Item 5 — Ledger review — ✅ CURRENT, zero ❌/⚠️ remaining

Reclassification executed this gate:

- **DI-2.1-01: ❌ Blocked → 📅 Forward** — note added verbatim-per-brief: *"pre-existing on origin/base, outside plan scope, surfaced to user in Execution Summary; not attributable to DEV1-015 delta (zero production diff in those domains)"*, plus the isolation proof pointer (2.1-outcome.md §5: each of the 13 failures reproduces with DEV1-015 files absent) and the Item-4 diff proof (zero edits in those domains). Target updated to "2.M (reclassified at 2.M), 5.1 final gate, + owning session/governance/notification tickets". Layer-wide greenness remains owned by 5.1 + the foreign-domain tickets (which must themselves respect the scope gate).
- Remaining ledger statuses: DI-0.2-01 📅 Forward · DI-0.2-02 📅 Forward · DI-0.2-03 ✅ Done · DI-2.1-01 📅 Forward. **No other ❌/⚠️ entries — verified.**

### Item 6 — Cross-task consistency — ✅ NO unresolved dependencies

- **2.1 carry-forward → 2.2**: satisfied (2.2 executed and landed both remaining cells); → **2.M**: DI-2.1-01 handled by the Item-5 reclassification (the only 2.M-directed item); → 3.1/4.1 items are forward-phase work items, not cross-file blocks.
- **2.2 carry-forward → 2.M**: "quote 33/4/0 for the service suite" — confirmed (Item 1 table; the 4 skips are the pre-existing PGlite boundary tier); DI-2.1-01 "still blocks the layer-wide gate" → resolved per the gate's reclassification ruling.
- **1.1 §12 cross-file dependencies**: the 4-file locale atomic unit is green (parity 68/0 recorded at 1.1; no drift since — Item 1/3 runs green on the current tree); `Common.retry` consumption is a 4.2 design note, not a block.
- No `CROSS-FILE DEPENDENCY` block markers anywhere in the plan dir (grep verified). **Nothing unhandled.**

### Item 7 — Backend-scoped mini-review of the two test files — ✅ ZERO NEW FINDINGS

Evidence per check (journey = `test/workflows/parents/student-confirmation-of-link.journey.test.ts`; service = `backend/services/parents/parent-link-request.service.test.ts`):

| Check | Evidence |
|---|---|
| Honest permission resolution | Journey cast = real `users` + role-child rows via `provision*Actor` factories (L406–413) in ONE committing `db.transaction` (L405, commit-or-nothing); G genuinely suspended via `setGovernanceFixture` (L459–464), L genuinely pre-linked via `linkStudentToParentFixture` (L466–467); every denial rides the REAL `requireActor` / `classifyUnclaimableRequest`; denial copy via `getServerTranslations` (zero hardcoded English). Service Cell A (L2048) re-derives identity from `actorUserId` alone — spy call-order pin. |
| `runInRollback` absence in the journey | `rg runInRollback` over the journey: **zero code matches** (only the header doc-comment "NEVER `runInRollback` — the services spawn their own top-level transactions"). Service test uses it correctly per REQ-061 with `tx` as `outerTx` (28 sites) and committed fixtures where the commit boundary is the subject (Cell B L1085). |
| Committed fixtures + FK-safe afterAll | Journey `afterAll` (L1232–1277): side-effect inbox rows registered LAST (⇒ deleted FIRST), `tracked.cleanup()` (L1253, reverse-registration order), then mandatory zero-residue re-probes ×5 by tracked ids AND the `jrn_sconfirm_%` email prefix (L1260–1274). Two consecutive green runs (2.1 §4 + Item 2 rerun) prove teardown idempotency. Service Cell B fixture registered in `trackedRequestIds` (L1099). |
| Spied fanout | `SpiedFanoutTransport` at the `options.transport` seam (journey L196; per-raced-call distinct transports L1041–1042); service `RecordingFanoutTransport` (L216) + `expectSinglePublish` closed-shape helper (L240). Nothing reaches a real channel (static-locks corpus guard re-verified green). |
| PGlite race guard | Journey L985–987: `const describeOnRealPostgres = isPgliteProvider() ? describe.skip : describe` wraps Step 9b — the ONLY skip in the suite. Service boundary tier behind the same guard idiom (L1556). |
| No cross-layer imports | Import audit: journey imports = `bun:test`, `node:crypto`, `drizzle-orm`, `@/backend/**`, `@/shared/**`, `@/test/workflows/helpers` only; service test = `bun:test`, drizzle, `@/backend/**`, `@/shared/**`. Zero `@/frontend` / `@/app` / relative-parent imports (grep verified both files). |
| Enums value-imported | `NotificationType` (journey L83) and `LinkStatus` (journey L84; service L1112/L1160 as values `LinkStatus.Confirmed`) are VALUE imports used as members; no raw status string literals; the notification-type constant is typed as the DB column union (no unsafe enum comparison). |
| No `console.*` | Zero matches in both files (and static-locks lock (d) — "zero console calls in the new parent-link test files" — passes in Item 1's run). `[DOMAIN]` lines in wrapper output are the services' own bounded `logDomainError` emissions, not test `console.*`. |
| No dead code | Fresh quality loop run THIS gate: `scripts/health/sub-loop.ts <file> --lifecycle duplicates` → **exit 0 for BOTH files** (tsgo + oxlint + biome:check + lint:type-aware + check:duplicates). Logs: `/tmp/gate-subloop-journey.log`, `/tmp/gate-subloop-service.log`. |

**Findings list: (none)** — zero `[SEVERITY]` entries. Both files match their outcome-file claims line-for-line (Cell A L2048–2116 ordering/spy discipline incl. asserts-BEFORE-`mockRestore`; Cell B L1079–1166 committed-replay idempotency).

---

## 2. Run outputs (counts, verbatim)

```
backend/services/parents  →  96 pass / 15 skip / 0 fail   483 expect   111 tests / 4 files   EXIT 0
test/workflows/parents    →  38 pass /  1 skip / 0 fail   598 expect    39 tests / 3 files   EXIT 0
bun run tsgo              →  exit 0 · "error TS" matches: 0  (baseline 0 → Δ 0)
sub-loop duplicates ×2    →  exit 0 (journey) / exit 0 (service)
```

## 3. Diff classification summary

7 modified + 6 untracked; **all 13 EXPECTED** (plan-dir docs ×2 modified + 5 outcome untracked; 1.1 locale ×4; 2.1 journey untracked; 2.2 service TEST file). Unexpected: **0**. Production dirs `backend/db/repo`, `backend/graphql`, `backend/types`, `app`, `frontend`, `backend/db/schema`, `backend/db/migration`: **0 diffs**. Scope gate intact (no schema/migration drift).

## 4. Ledger status after gate

| ID | Status |
|---|---|
| DI-0.2-01 | 📅 Forward |
| DI-0.2-02 | 📅 Forward |
| DI-0.2-03 | ✅ Done |
| DI-2.1-01 | 📅 Forward (reclassified from ❌ Blocked at this gate — see Item 5) |

Open ❌/⚠️: **0**.

## 5. Verdict

**GATE PASS** — Phase 3 (GraphQL resolvers & API surface verification) may start. Phase 3 must keep the same diff discipline: `backend/graphql/**` edits limited to the 3.1 wire-test file (test layer); any Pothos edit forces the schema-surface same-change rule.
