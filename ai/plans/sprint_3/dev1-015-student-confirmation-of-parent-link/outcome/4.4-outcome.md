# Task 4.4 Outcome — Nav pin verification (verify-first; NO-OP branch + sanctioned constant-import refactor, REQ-053)

## 1. Summary

**Verdict: NO-OP branch (route was already the REAL student route) — with the sanctioned single-source-of-truth refactor applied.**

Per 0.2, `frontend/views/dashboard/nav/navItems.ts` L114 already targeted `/student/link-requests` (the REAL decision page, not the `app/(dashboard)/[feature]/page.tsx` catch-all ComingSoon view) — **no RETARGET was needed and the rendered route is behaviorally byte-identical**. The entry's `route` was however a raw string literal; tasks.md 4.4.SR mandates the route constant be single-sourced across nav/card/drawer, so the minimal sanctioned change was applied: the literal was swapped for the shared `STUDENT_LINK_REQUESTS_ROUTE` constant (ONE definition site: `frontend/components/ui/useNotificationDrawerActions.ts:18` — the 4.1 export whose docblock already names the student nav entry as its third consumer). The previously-absent nav pin test was ADDED (0.2 explicitly logged this gap: "⚠️ `navItems.test.ts` has NO pin for this entry today").

## 2. Files modified / not modified

| File | Delta |
|---|---|
| `frontend/views/dashboard/nav/navItems.ts` | **Modified (3 lines)** — (a) +1 import `STUDENT_LINK_REQUESTS_ROUTE` from `@/frontend/components/ui/useNotificationDrawerActions` (L27, before the codegen graphql import per the file's import-order discipline); (b) L118 `{ route: STUDENT_LINK_REQUESTS_ROUTE, labelKey: "linkRequests", Icon: LinkChildIcon }` (was the literal `"/student/link-requests"`); (c) one docblock bullet in the canonical-retargets list. No route value change, no icon/label change, no new entries, no reordering. |
| `frontend/views/dashboard/nav/navItems.test.ts` | **Extended (+1 describe block, +1 import, +1 docblock bullet)** — the REQ-053 pin suite (§4). All pre-existing describes untouched. |
| `frontend/components/ui/useNotificationDrawerActions.ts` | **NOT modified** — the constant site (L18) is consumed as-is. |
| `app/(dashboard)/student/link-requests/page.tsx` | **NOT modified** — page guard verified read-only, conforms (§3). |
| Mobile bottom-nav | **NOT touched** — mobile nav is the temporary MUI `Drawer`; explicitly out of scope. |

## 3. Page-guard conformance (4.4 read-only check — NO violation, zero edits)

- **Page anchor:** `app/(dashboard)/student/link-requests/page.tsx:36` — `await withPageAuth({ roles: [UserRole.Student], redirectTo: "/student/link-requests" });` is the ONLY authorization boundary; the Server Component body renders `StudentLinkRequestsContainer` after it.
- **Guard anchor (anonymous):** `frontend/lib/auth/withPageAuth.ts:70-87` — anonymous callers `redirect("/login?redirect=" + encodeURIComponent(redirectTo))` (RELATIVE path, PNA-safe; never an absolute `http://localhost:3000` URL).
- **Guard anchor (wrong role):** `frontend/lib/auth/withPageAuth.ts:90-96` — role mismatch `redirect(roleDashboardPath(ctx.role))` — the caller's OWN role dashboard. **Bare `/dashboard` is never a browser redirect target** (preview-gateway 301/308 loop documented in-file and in `roleDashboardRoute.ts`). Conforms exactly to the 4.4 invariant.
- **Test-harness precedent (recorded, not extended):** `test/ui/page-guards/parent-handshake-page.test.ts` (L1-60) drives the REAL page module against a `getServerUserContext` double + recording throwing `redirect` double (`mock.module` before dynamic import; serverless). No `student-link-requests` guard suite exists in `test/ui/page-guards/` (only parent-handshake + admin-broadcasts) — the four-cell role matrix is 4.4.BF's browser scope (§8); per mission, this file was NOT created (read-only branch: no violation found).

## 4. Test evidence (4.4.TE)

`frontend/views/dashboard/nav/navItems.test.ts` — **32 pass / 0 fail / 156 expect() calls** across 1 file (`[1.26s]`), exit 0.

New describe block `"Student link-requests navigation (REQ-053, DEV1-015)"` (7 cells incl. test.each):

1. **exactly-ONE pin** — student nav filtered by `STUDENT_LINK_REQUESTS_ROUTE` → `toHaveLength(1)`, `labelKey === "linkRequests"`; frozen-value pin `STUDENT_LINK_REQUESTS_ROUTE === "/student/link-requests"` (belt-and-braces, mirroring the 4.1/4.2/4.3 suites — a future retarget onto the `[feature]` catch-all breaks this first).
2. **label-ownership pin** — `"linkRequests" in dashboardEn === true`, `"linkRequests" in handshakeCodeEn === false` (runtime mirror of the ownership matrix, audit/broadcasts pattern).
3. **both-locale resolution** — through the REAL runtime path (`resolveNavItemLabel` + `DashboardNs.getLabels(getTranslations(locale))`) the label resolves to `dashboardLabels.linkRequests`, non-empty, in ar AND en.
4. **role gating (test.each Teacher/Parent/Admin)** — NO non-student role carries the link-requests entry.

All legacy assertions stay green (label-ownership sweep, resolution blocks, role subsets + student fallback, REQ-054/064 plans, REQ-065 audit, DEV3-022d broadcasts).

**Runner note (carry-forward):** the mission's literal `KOTTABY_TEST_RUNNER_OK=1 bun --env-file=.env.test test …` invocation fails fast in `test/ui/test-env.ts:34` — the materialized `.env.test` (493 B) carries DB/AUTH vars only, NOT the `TEST_CI=1` posture flag that lives in the committed `.env.test.ci` template (materialization overrides `DATABASE_URL` but the posture flags are not propagated to the runtime file). The sanctioned 4.1-outcome invocation was used verbatim instead:

```
bun run scripts/lib/run-locked-cmd.ts test:ui:components TEST_SERVER_MODE=production bun --env-file=.env.test.ci test --parallel=1 \
  frontend/views/dashboard/nav/navItems.test.ts \
  --preload ./test/ui/test-env.ts --preload ./test/ui/components/happydom-preload.ts \
  --preload ./test/ui/components/translation-preload.ts --preload ./test/ui/components/next-dynamic-mock.ts
→ 32 pass / 0 fail (156 expect())
```

## 5. Quality Loop (4.4.QL)

- `bun run scripts/health/sub-loop.ts frontend/views/dashboard/nav/navItems.ts --lifecycle duplicates` — **exit 0** (tsgo ✅, oxlint ✅, biome:check ✅, lint:type-aware ✅, check:duplicates ✅).
- `bun run scripts/health/sub-loop.ts frontend/views/dashboard/nav/navItems.test.ts --lifecycle duplicates` — **exit 0** (same chain; check:duplicates skipped as outside jscpd scan scope — pass).
- `bun run tsgo` (full project, `tsgo -b --noEmit` via run-locked-cmd) — **exit 0, zero errors**.

## 6. Semantic review (4.4.SR)

- **Route constant single-sourced:** `rg -l "student/link-requests" frontend/ app/` → 6 files; runtime-literal audit: `useNotificationDrawerActions.ts:18` (the ONE definition site), `app/(dashboard)/student/link-requests/page.tsx:36` (the page's own `withPageAuth redirectTo` — the route's server-side identity, not cross-consumer drift; L13 is the same string in a doc comment), `navItems.test.ts:233` (sanctioned frozen-literal test pin). `frontend/views/students/link-requests/{LinkRequestCard,StudentLinkRequestsContainer}.tsx:18` + `frontend/components/ui/LinkRequestTimestampLines.tsx:10` are DOC-COMMENT mentions only (DEV1-014 view logic, zero runtime literals — task 4.5 scope, untouched). **`navItems.ts` itself now carries ZERO `student/link-requests` literals** — the entry consumes the constant; `PendingParentLinkRequestsCard.tsx:8,130` (4.2) and the drawer (4.1) already consume the SAME export → three consumers, one constant, zero drift, as specced.
- **Nav item diff minimal:** constant-import + retarget-to-constant + doc bullet only; route VALUE byte-identical (`/student/link-requests`), no new items, no reorder, no mobile-nav work.
- **No duplicate entries:** exactly ONE link-requests entry in the student nav (pinned by test); the entry is absent from Teacher/Parent/Admin navs (pinned by test.each).
- **No string duplication across nav/card/drawer:** all three consumers resolve the route from `@/frontend/components/ui/useNotificationDrawerActions`.

## 7. Instruction Verification (4.4.IV)

- `.agents/instructions/frontend.instructions.md` (PRESENT; the `.github/instructions/…` path in the sub-loop banner is the known prose-phantom — verified ABSENT on disk, same finding as 4.3.IV): complied — import-order discipline honored (app-module import inserted ahead of the codegen graphql import), no UI copy in the delta, no hardcoded strings/colors, no `console.*`; enforced mechanically by the green oxlint/biome/lint:type-aware chain.
- Dashboard-layer AGENTS.md: `frontend/views/dashboard/AGENTS.md` and `frontend/views/dashboard/nav/AGENTS.md` verified **ABSENT** (`ls` exit 2; `frontend/views/AGENTS.md` remains the known prose-phantom per 0.2) — nothing to comply with.

## 8. BROWSER-EVIDENCE

### 4.4.BF — Agent-Browser Functional Self-Loop
**PENDING (separate browser loop; not executed in this code+verification pass).** Four cells to capture: student clicks sidebar link-requests entry → real decision route (NOT ComingSoon); parent + teacher direct-nav to `/student/link-requests` → redirect to their own `roleDashboardPath(role)`; anonymous → `/login?redirect=/student/link-requests`. Server-side guard conformance is code-verified in §3; the static side of the four-cell matrix is pinned by the new nav gating tests (§4) + the existing guard harness precedent (`test/ui/page-guards/parent-handshake-page.test.ts`).

### 4.4.BS — Agent-Browser Visual & Styling Self-Loop (Screenshot Analysis)
**PENDING (separate browser loop).** Six-cell sidebar capture set (desktop drawer expanded + mobile temporary Drawer × en/ar × 1440×900 / 768×1024 / 375×812): `linkRequests` label localized, icon `*Outlined` (`LinkOutlined as LinkChildIcon`, navItems.ts L14), active-state highlight on the decision route, RTL drawer mirroring in ar. Zero styling deltas landed in this task (route-only change), so no defect-driven iteration is expected.
