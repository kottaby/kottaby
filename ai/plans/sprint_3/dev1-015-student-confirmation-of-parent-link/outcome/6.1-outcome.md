# Task 6.1 Outcome — review-types (READ-ONLY)

**Task ID:** 6.1 · **Agent:** review-types · **Date:** 2026-09-07 · **Verdict: PASS with 2 LOW findings (no CRITICAL/HIGH/MEDIUM), zero code fixes applied (read-only mission).**

Scope reviewed (git diff `ffce457..feat/dev1-015-student-confirmation-of-parent-link`, code files only — 17 files): `shared/locale/{types,en,ar}/parentLink/index.ts`, `shared/locale/parentLink-namespace.parity.test.ts`, `frontend/components/ui/{useNotificationDrawerActions.ts,NotificationDrawerBody.tsx}`, `frontend/views/students/dashboard/{PendingParentLinkRequestsCard.tsx,pending-parent-link-requests.ts,index.ts}`, `frontend/views/dashboard/home/RoleDashboardPage.tsx`, `frontend/views/dashboard/nav/{navItems.ts,navItems.test.ts}`, `test/ui/components/dashboard/RoleDashboardPage.slot.test.tsx`, `test/ui/components/notifications/notification-deep-link.test.tsx`, `test/ui/components/students/PendingParentLinkRequestsCard.test.tsx`, `test/workflows/parents/student-confirmation-of-link.journey.test.ts`, `backend/graphql/test/parent-link.wire.test.ts`.

**Scope note:** `backend/services/parents/parent-link-request.service.test.ts` is listed in the wave-6.1 scope but has a ZERO diff in the branch (`git diff ffce457..feat/… -- <file>` is empty) — nothing to review; baseline = clean.

---

## Review checks

### 1. Zero local types in resolvers/components — CLEAN
- **Resolvers:** NO resolver file is in the delta (backend production is zero-diff by charter; only the wire TEST changed). Nothing to audit beyond the test tier.
- **Row/prop contracts are 100% canonical/codegen:**
  - Card + derivation module: rows typed `MyIncomingParentLinkRequestsQuery_myIncomingParentLinkRequests` from `@/frontend/graphql/generated/gql/graphql` (graphql.ts:559) — the codegen projection of `IncomingParentLinkRequestReturnType`, field-for-field identical (`id, status, parentFullName, createdAt, expiresAt, respondedAt`).
  - **`IncomingParentLinkRequestReturnType` sole-row-contract check: PASS** — no view-layer type re-declares the row anywhere in the delta. The only new exported type is `ActionableIncomingSummary` (`pending-parent-link-requests.ts:20`) — a DERIVED aggregate `{ count, latestParentFullName }`, not a row re-declaration.
  - Drawer hook/body: codegen `MyNotificationsFilterInput` / `MyNotificationsQuery_myNotifications_items`; the hook's `UseNotificationDrawerActionsParams`/`NotificationDrawerActions` interfaces are PRE-EXISTING (delta added only the route-resolution block, verified against the base blob).
  - Journey test imports canonical `IncomingParentLinkRequestReturnType`, `OutgoingParentLinkRequestReturnType`, `UserSelectType`, `DBTransaction` from `@/backend/types` (L92-97); its local types are drizzle `$inferSelect` aliases + fixture-state interfaces (`JourneyCastState`, `RequestKey`). Wire test local `WireActor`/`GovernancePatch`/`StudentFixture` are DB fixture shapes (pre-existing pattern; delta adds none).

### 2. Import path consistency — CLEAN
- Every intra-repo import in all 17 delta files is `@/`-aliased; the only relative imports are the same-directory barrel entries (`frontend/views/students/dashboard/index.ts:1-2` — pre-existing `./HandshakeCodeCard` pattern). Zero relative imports across layers.
- Frontend value-importing `@/backend/enum/**` follows the ESTABLISHED pre-delta precedent (`withPageAuth.ts`, `roleDashboardRoute.ts`, base `RoleDashboardPage.tsx` all did it at ffce457).

### 3. TypedDocumentNode / new documents — CLEAN
- `frontend/graphql/**` is untouched by the delta. The one document the delta surfaces consume — `myIncomingParentLinkRequestsQueryDocument` (`parent-link.documents.ts:70`) — is pre-existing `TypedDocumentNode<MyIncomingParentLinkRequestsQuery>`, reused verbatim by the card, the slot test, and the card suite (one query, no new documents, no count endpoint).
- The wire-test delta reuses the pre-existing `RESPOND_DOCUMENT`; raw `gql` strings are that file's wire-tier charter, and the delta adds no new document. **No new documents added by the delta — confirmed.**

### 4. i18n types complete — CLEAN, one LOW (F1)
- All 6 new `dashboardCard*` members exist bijectively: types (`types/parentLink/index.ts:84-104`), en (`en/parentLink/index.ts:28-33`), ar (`ar/parentLink/index.ts:26-37`) — 39 members on all three surfaces (33 + 6).
- Parity suite pins them: `MANDATED_KEYS` 33→39 (L56-96), `FUNCTION_KEYS` 4→6 (L107-114), exhaustive-inventory pin updated to "39 slots" (L163-168), exact template pins for `dashboardCardCount`/`dashboardCardLatestRequester` (L234-246), Arabic-script probes extended to all six function slots (L195-203). No missing key members.
- **LOW (F1):** two of the six members (`dashboardCardLoading`, `dashboardCardLoadError`) have ZERO runtime consumers — see Findings. (Independently recorded by wave 6.3 as its F1/INFO.)

### 5. Enum value imports (runtime expressions) — CLEAN
- `LinkStatus`: real `export enum` in codegen (graphql.ts:119) — VALUE-imported and used at runtime in `pending-parent-link-requests.ts:2,50` (`=== LinkStatus.Pending`), the card suite, and the slot test; journey + wire tests value-import it from `@/backend/enum/shared/link-status.enum` (`Object.entries(LinkStatus)` wire-name derivation at wire L355-365).
- `NotificationType`: VALUE-imported in `useNotificationDrawerActions.ts:4` as a computed Record key (`[NotificationType.ParentLinkRequest]`, L32); deep-link test imports BOTH spellings deliberately (codegen `NotificationType` for the wire `type` field, backend enum aliased `BackendNotificationType` for the free-varchar `relatedEntityType`, L33/44); journey test values it at L83/132/248.
- `UserRole`: VALUE-imported in `RoleDashboardPage.tsx:3` (runtime `switch`), slot test carries both backend (`BackendUserRole`) and wire (`WireUserRole`) aliases with correct per-field usage. Zero `import type` of any enum that is used in a runtime expression anywhere in the delta.

### 6. No `any`, no unsafe casts — CLEAN
- `rg ': any|<any>|as any|as unknown|@ts-ignore|@ts-expect-error|@ts-nocheck'` over all 17 delta files → **zero matches**. `unknown` is used only with runtime type guards (wire test `isRecord`/`recordOf`/`soleErrorItemOf`, L106-120); test fixture doubles use precise intersections (`MeQuery_me & { __typename: "User" }`), never casts.

### 7. Type naming canonical (no near-duplicates) — CLEAN
- Repo-wide grep: `ActionableIncomingSummary` exists exactly once (declaration + its two consumers); no near-duplicate summary/row type anywhere in `frontend/`.
- Single-definition-site discipline for the shared route constant: `STUDENT_LINK_REQUESTS_ROUTE` is defined ONCE (`useNotificationDrawerActions.ts:18`); the only production literal `/student/link-requests` IS that constant — every other occurrence is a test frozen-value pin (navItems.test L233, deep-link L214/243, slot L268, card L255). Nav, card CTA, and drawer deep-link all import the constant — no drift surface.

**Extra evidence:** `bun run tsgo -b --noEmit` (project typecheck) passes clean over the delta working tree.

---

## Findings

- **[LOW]** `shared/locale/types/parentLink/index.ts:98-104` · `shared/locale/en/parentLink/index.ts:32-33` · `shared/locale/ar/parentLink/index.ts:36-37` (+ `parentLink-namespace.parity.test.ts:94-95`) — `dashboardCardLoading` and `dashboardCardLoadError` are declared in the type surface, translated in BOTH locales, and parity-pinned, but have ZERO runtime consumers: the card's skeleton branch renders `<CardShell busy>` (`CardShell.tsx` has no accessible-label prop) and the error branch resolves copy exclusively via `resolveParentLinkDenialCopy(extractErrorCode(error), te)` — dead type members and contract drift vs `1.1-outcome.md` ("Task 4.2 consumes EXACTLY these keys"), which named both as 4.2 consumers — suggested fix: either consume them (add an accessible loading label through `CardShell`; fold `dashboardCardLoadError` in as the Alert fallback for unmapped denial codes) or delete both members from type/en/ar + `MANDATED_KEYS` in one change-set and amend the 1.1 docblock (delete is lower-risk: the denial-copy path is the security-sanctioned one). Same issue independently recorded by wave 6.3 (its F1) — track once.
- **[LOW]** `shared/locale/ar/parentLink/index.ts:27-33` — ar `dashboardCardCount` carries an unreachable `count === 0` branch ("لا توجد طلبات ربط قيد الانتظار"): the card renders `null` at zero actionable rows (REQ-015/052 branch 3) so the argument can never be 0, and en has no matching 0-branch — a locale-asymmetric dead branch in a delta file; harmless today — suggested fix: drop the 0-branch or mark it explicitly defensive with a comment. (Cross-ref: wave 6.3 INFO, same observation.)

No CRITICAL/HIGH/MEDIUM findings. No blocking issue for Phase 7.

---

## Worklog

- **Task ID:** 6.1 · **Agent:** review-types · **Date:** 2026-09-07
- Grep'd tasks.md for "6.1" (wave definition) and 0.2-outcome.md "types" sections (canonical-type anchors, i18n key list, enum inventory).
- Read all 17 delta code files (targeted reads); confirmed via `git diff --name-only` that `backend/services/parents/parent-link-request.service.test.ts` (also in the stated scope) has a ZERO diff.
- Verified: codegen row type ≡ `IncomingParentLinkRequestReturnType` (graphql.ts:559); codegen `export enum` value surfaces (LinkStatus/NotificationType/UserRole); pre-delta precedent for frontend `@/backend/enum` imports (`git show ffce457` + `git grep`); pre-existing hook interfaces and pre-existing `RESPOND_DOCUMENT`/`TypedDocumentNode` document; locale 39-key bijectivity by manual key-set walk of types/en/ar; repo-wide uniqueness of `ActionableIncomingSummary`; single definition site + frozen-value pins for `STUDENT_LINK_REQUESTS_ROUTE`.
- Scans: `any`/`as unknown`/ts-directive sweep over all 17 files (zero-match); enum-usage inventory across delta tests; non-`@/` import audit (external packages only).
- Ran `bun run tsgo -b --noEmit` — clean (read-only; working tree verified unmodified afterward).
- READ-ONLY: zero file edits except this outcome; tasks.md untouched; no git state operations (status/show/diff only); no fixes applied.
- Verdict per check: 1 CLEAN · 2 CLEAN · 3 CLEAN · 4 CLEAN + F1 · 5 CLEAN · 6 CLEAN · 7 CLEAN (+F2 informational). Findings: 2 LOW, 0 CRITICAL/HIGH/MEDIUM.

> **Fix round 1 (FIX-B) cross-ref:** this file's F1 (dead `dashboardCardLoading`/`dashboardCardLoadError`) and F2 (ar `dashboardCardCount` 0-branch) are RESOLVED — both keys are now consumed (skeleton `role="status"` + `aria-label` via a new `CardShell.busyLabel`; Alert fallback via new `resolveParentLinkDenialCopyOrNull`) and the ar 0-branch is removed (plural classes 1/2/3–10/11+ kept). Full fix record + evidence: `outcome/6.3-outcome.md` → "Fix round 1 (FIX-B)".
