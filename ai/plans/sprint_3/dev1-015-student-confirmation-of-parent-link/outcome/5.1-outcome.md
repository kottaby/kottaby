# Task 5.1 Outcome — Full integration battery & differential verification

**Task ID:** 5.1 · **Plan:** DEV1-015 · **Date:** 2026-09-07 · **Verdict: PASS — 0 new failures.** Every suite green at (or byte-identical to) its recorded baseline; the only reds are the ledgered pre-existing items (4 schema-surface pin drifts, DI-3.1-01-at-HEAD; foreign journeys not in scope paths). All differentials (codegen / schema / db push / typecheck / biome / lint) clean. One sanctioned fix-forward inside the battery (see §5).

Environment: real PostgreSQL 17 @ 127.0.0.1:5432/app_db (`DB_PROVIDER=postgres` in `.env`/`.env.test`, seeded); dev server on :3000; branch worktree at HEAD with the DEV1-015 delta staged/unstaged (feature tip content-identical for the differential paths).

---

## 1. Command matrix (each battery command run ONCE; counts as recorded)

| # | Command | Counts | Verdict |
|---|---|---|---|
| 1 | `bun run test/scripts/run-test.ts test/workflows/parents` | **39 pass / 0 fail / 614 expect()** across 3 files `[3.47s]` | GREEN — DEV1-015 journey (steps 1–11 incl. boundary + deep-link contract cells) + DEV1-014 journey |
| 2 | `bun run test/scripts/run-test.ts backend/services/parents` | **109 pass / 0 fail / 593 expect()** across 4 files `[2.02s]` | GREEN — service + helpers + chaos (real PG) + **static-locks** (see §4) |
| 3 | `bun run test/scripts/run-test.ts backend/graphql/test/parent-link.wire.test.ts` | **25 pass / 0 fail / 474 expect()** `[13.25s]` | GREEN — matches 3.1 baseline |
| 4 | `bun run test/scripts/run-test.ts backend/graphql/test/schema-surface.test.ts` | **37 pass / 4 fail / 262 expect()** / 41 tests `[388ms]` | PRE-EXISTING 4 pin drifts ONLY (see §2.1) — counts byte-identical to the 3.1 baseline (37/4/262) |
| 5a | scoped UI: `test/ui/components/students/PendingParentLinkRequestsCard.test.tsx` | **21 / 0 / 81 expect()** `[6.34s]` | GREEN — matches 4.2 baseline |
| 5b | scoped UI: `test/ui/components/dashboard/RoleDashboardPage.slot.test.tsx` | **7 / 0 / 51 expect()** `[2.40s]` | GREEN — matches 4.3 baseline (7/51) |
| 5c | scoped UI: `test/ui/components/notifications/notification-deep-link.test.tsx` | **6 / 0 / 14 expect()** `[1.09s]` | GREEN — matches 4.1 baseline |
| 5d | scoped UI: `frontend/views/dashboard/nav/navItems.test.ts` | **32 / 0 / 156 expect()** `[1.34s]` | GREEN — matches 4.4 baseline (32/156) |
| 5e | scoped UI: `test/ui/components/students/StudentLinkRequestsContainer.test.tsx` | **22 / 0 / 122 expect()** `[3.83s]` | GREEN — matches 1.1/4.5 baseline (22/122) |
| 6 | `bun run test/scripts/run-test.ts shared/locale/parentLink-namespace.parity.test.ts` | **68 pass / 0 fail / 434 expect()** `[81ms]` | GREEN — matches DI-0.2-03 resolution baseline (68/68) |

UI scoped invocation (the sanctioned 4.1/4.3/4.4 carry-forward pattern, verbatim per file):

```
bun run scripts/lib/run-locked-cmd.ts test:ui:components TEST_SERVER_MODE=production bun --env-file=.env.test.ci test --parallel=1 <file> \
  --preload ./test/ui/test-env.ts --preload ./test/ui/components/happydom-preload.ts \
  --preload ./test/ui/components/translation-preload.ts --preload ./test/ui/components/next-dynamic-mock.ts
```

UI sub-total: **88 pass / 0 fail / 480 expect() across 5 files.**

### 2.1 Schema-surface failure analysis (step 4) — the 4 fails are EXACTLY the known foreign pins, nothing new

Failing cells (unique): ① `Query._health — retyped probe surface > root query retains EXACTLY the baseline fields…` ② `mutation set grows ONLY by the sanctioned additions…` ③ `enum set is pinned…` ④ `whole-schema named-type delta is pinned…` — i.e. **root-query freeze, mutation-set freeze, enum-set pin, whole-schema type-delta pin**, the identical 4 cells recorded in 3.1-outcome §8. Underlying drift names confirmed there: `adminBroadcastNotification`, `adminCertifyTeacherColdStart`, `cancelParentLinkRequest`, enums `BroadcastAudienceType`/`LinkStatus` — all surfaces landed by OTHER features (pre-existing at HEAD; `schema-surface.test.ts` itself is unmodified in this tree — `git status` shows no entry for it). Counts 37/4/262 byte-identical to 3.1. Ledgered as DI-3.1-01-at-HEAD (surface-pin refresh owner); **not caused by, and not touched by, DEV1-015**.

## 2. Differentials

| Differential | Command (once) | Evidence | Verdict |
|---|---|---|---|
| Codegen | `bun run generate:gqlSchema` → `bun codegen` | `[generate:gqlSchema] wrote …/frontend/graphql/generated/schema.graphql (21756 bytes)`; `✔ Generate outputs` → `git status --short -- frontend/graphql/generated/` **empty**, `git diff HEAD --stat -- frontend/graphql/generated/` **empty** → regenerated `schema.graphql` + `gql/graphql.ts` are **byte-identical**; filtered `git status` contains only expected plan-doc + DEV1-015 delta entries (no generated artifacts, no unrelated drift) | **ZERO drift** |
| Schema | `git diff feat/dev1-015-student-confirmation-of-parent-link -- backend/db/schema backend/db/migration` | **empty** (exit 0); equivalently `git diff HEAD -- backend/db/schema backend/db/migration` **empty** and `git status --short --` those paths **empty** (feature tip content-identical to HEAD for the differential paths) | **MUST-be-empty: empty** |
| db push | `bun run db push` | `[i] No changes detected` → `✓ Success` | **No-op confirmed** |
| Typecheck | `bun run tsgo` (exit code checked explicitly) | exit **0**, zero diagnostics | **0 errors = baseline 0** |
| Biome | `bun run biome:check` | exit **0**; final state: `Checked 1425 files in 9s. No fixes applied.` | **0 = baseline 0** (see §5 for the mid-battery auto-fix incident) |
| Lint | `bun run lint` (lint-service cli) | exit **0**, silent success | **0 = baseline 0** |

## 3. Coverage gate evidence (REQ-060 — NEW code at 100% statement/branch)

Measured with the repo's scoped bun coverage runner (`bun --env-file=.env.test.ci test --coverage --parallel=1 <card suite + slot suite> --preload <4 preloads>`; bun's table has no separate branch column — branch completeness evidenced by the cell enumeration below, per the sanctioned traceability-table form):

| New delta file | bun `--coverage` (scoped union: card + slot suites) | Branch evidence (cell enumeration) |
|---|---|---|
| `frontend/views/students/dashboard/pending-parent-link-requests.ts` (helpers) | **100.00% funcs / 100.00% lines — no uncovered lines** | Every `deriveActionableIncoming` arm pinned by dedicated pure-helper cells in `PendingParentLinkRequestsCard.test.tsx` ("deriveActionableIncoming (pure helper)" describe: mixed rows / expired-but-newest exclusion / latest selection) |
| `frontend/views/students/dashboard/PendingParentLinkRequestsCard.tsx` (card) | **100.00% lines, zero uncovered line numbers** (funcs 5/6 — bun func-slot attribution quirk; no unexecuted line exists) | All 4 render branches × en+ar + error-code map + retry-refetch + post-decision flip pinned by the 21 cells of `PendingParentLinkRequestsCard.test.tsx` (incl. "query error → ONE localized Alert … + retry refetches", L318/331) |
| Drawer seam `frontend/components/ui/useNotificationDrawerActions.ts` + `NotificationDrawerBody.tsx` | consumed via full `NotificationDrawer` render | `notification-deep-link.test.tsx` (6 cells): persisted (type, relatedEntityType, relatedEntityId) triple → drawer row href = `STUDENT_LINK_REQUESTS_ROUTE`; journey Step 11 pins the data contract server-side |
| Nav diff `frontend/views/dashboard/nav/navItems.ts` (constant consumption) | n/a (static nav module) | `navItems.test.ts` REQ-053 pin block inside the 32-cell suite: route = shared constant, no new entries, no reorder, label/icon unchanged |
| Slot `frontend/views/dashboard/home/RoleDashboardPage.tsx` (`resolveStatusSlot` Student branch) | rendered by the slot suite | `RoleDashboardPage.slot.test.tsx` (7 cells / 51 expect): student renders BOTH cards (HandshakeCode + PendingParentLinkRequests), other roles unchanged, slot ordering |
| Locale keys `shared/locale/{en,ar}/parentLink/*` | — | `parentLink-namespace.parity.test.ts` 68/68: MANDATED_KEYS inventory incl. 1.1's dashboard-card additions, en≡ar parity, no silent minting |

## 4. Static locks (explicit step-10 record)

`backend/services/parents/parent-link.static-locks.test.ts` (the correct filename per DI-0.2-01; the tasks.md name `parent-link-request.static-locks.test.ts` remains a typo) is one of the 4 files in the step-2 directory run → **GREEN** (within 109 pass / 0 fail). No backend file landed under its scanned roots in this plan → corpus NOT extended (as anticipated).

## 5. Failures found & fix-forward (ONE sanctioned incident, resolved in-battery)

1. **biome:check auto-fix broke the card suite (mid-battery, fixed-forward).** The repo's `biome:check` runs with write-back: during step 9 it auto-applied `lint/complexity/noUselessFragments` to OUR delta test file `test/ui/components/students/PendingParentLinkRequestsCard.test.tsx`, collapsing the load-bearing `<>…</>` wrapper in `renderCard` (whose own comment documents that MockedProvider + array-children renders an empty tree in this environment). Immediate re-run: **7 pass / 14 fail** — a tool-induced test break in a delta file. Sanctioned fix-forward applied: fragment restored + `/* biome-ignore lint/complexity/noUselessFragments: … */` with the rationale inline (rule category verified via a file-scoped `bunx biome check`). Re-verification: file-scoped biome check **clean ("No fixes applied")**, card suite restored to the exact baseline **21 / 0 / 81**, and the repo-wide `biome:check` re-run now reports `Checked 1425 files … No fixes applied.` with exit 0. No production file was touched by the fix-forward; `RoleDashboardPage.tsx`'s mtime pre-dates the battery (its unstaged `gap: 2` form is the pre-existing 4.3 delta, not a biome artifact).
2. **No new failures otherwise.** The 4 schema-surface reds = ledgered foreign-surface pin drift (§2.1). The DI-2.1-01 foreign-domain journey failures (admin/account-governance, sessions, notifications) did not surface because the battery's journey scope is `test/workflows/parents` (parents domain only); they remain 📅 Forward with their owning tickets.
3. **Consciously accepted deviations:** none new. Already ledgered and unchanged: DI-0.2-01 (static-locks filename typo in tasks.md — correct file ran green), DI-0.2-02 (slot suite CREATED at 4.3, now green 7/51), DI-2.1-01 (foreign journeys), DI-3.1-01-at-HEAD (4 surface pins), the OOM ceiling / scoped-runner carry-forward (mission's literal `KOTTABY_TEST_RUNNER_OK=1 bun --env-file=.env.test` fails fast in `test/ui/test-env.ts:34`; `.env.test.ci` + 4 preloads used throughout, per 4.1–4.5).

## 6. Verdict

**GATE PASS.** 260 tests green across the battery (39+109+25+88+68 UI/wire/service/journey/parity cells = 329 pass / 0 fail when counting step-2's 4 files and UI sub-total; aggregate `pass` count 329, `fail` 0 excluding the 4 ledgered surface pins). All five differentials clean; generated artifacts byte-identical; schema diff empty; `db push` no-op; typecheck/biome/lint at baseline 0. REQ-050/060/061/063/064/065 verification evidence recorded above. Phase 6 review waves may proceed.

---

## Worklog

- **Task ID:** 5.1 · **Agent:** Integration Battery Subagent · **Date:** 2026-09-07
- Ran the battery sequentially, each command ONCE: journeys 39/0 (614 expect, 3 files); services dir 109/0 (593, 4 files incl. static-locks GREEN); wire 25/0 (474); schema-surface 37/4 (262) = exactly the 4 known foreign pin cells, counts byte-identical to 3.1; scoped UI ×5 = 88/0 (480) all at baseline; locale parity 68/0 (434).
- Differentials: codegen regenerated `frontend/graphql/generated/{schema.graphql,gql/graphql.ts}` with zero git status/diff entries (byte-identical); `git diff` on `backend/db/schema`+`backend/db/migration` empty vs feat tip AND vs HEAD; `bun run db push` → "No changes detected"; `tsgo`/`biome:check`/`lint` all exit 0 (baseline 0).
- Coverage: scoped `bun --coverage` union (card+slot suites) → `pending-parent-link-requests.ts` 100% funcs/100% lines; card 100% lines, zero uncovered line numbers; full cell-enumeration traceability table recorded (card branches × locales, drawer seam, nav pin, slot, locale parity).
- Incident + fix-forward: biome:check's write-back collapsed the card test's load-bearing fragment → 14 fails; restored fragment + `biome-ignore lint/complexity/noUselessFragments` (delta test file only), re-verified file-scoped biome clean + card suite back to 21/0/81 + repo biome:check "No fixes applied".
- Outcome written; tasks.md `- [ ] 5.1` flipped to `[x]`. No other code, test, or doc edits.
