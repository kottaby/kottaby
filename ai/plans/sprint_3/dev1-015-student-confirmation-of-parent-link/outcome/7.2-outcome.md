# Task 7.2 Outcome — Layer knowledge propagation (minimal, conditional)

**Task ID:** 7.2 · **Plan:** DEV1-015 · **Date:** 2026-09-07 · **Verdict: PASS** — one durable rule propagated to `frontend/AGENTS.md`; root AGENTS.md reference gap closed; SKILL/instructions: none needed.

## 1. Rule evaluation: "one shared route constant for all parent-link deep-links"

**Durable — YES.** It is a cross-cutting navigation invariant, not a one-off: three independent consumers (nav entry `navItems.ts`, card CTA `PendingParentLinkRequestsCard.tsx`, drawer row anchors `NotificationDrawerBody.tsx`) resolve the same route through ONE definition site (`STUDENT_LINK_REQUESTS_ROUTE` in the leaf module `frontend/lib/notification-route-resolution.ts`), and drift would silently strand users on the notifications feed. The leaf-module constraint (directive-free, framework-free) is part of the rule — consumers must not transitively import the drawer hook graph. This rule is genuinely NEW (0.2 found the drawer hard-anchoring `/notifications`; no prior navigation rule existed in any AGENTS.md), so per the conditional mandate the ONE-line addition fires.

## 2. Layer AGENTS.md selection (existence-verified, not assumed)

| Candidate | Exists? | Evidence |
|---|---|---|
| `frontend/AGENTS.md` | **PRESENT** | file read; themed rule sections (Theme, React & Next.js Patterns, MUI, Performance, Linting) |
| `frontend/views/AGENTS.md` | ABSENT | filesystem check (matches 0.2's code-verified ABSENT list) |
| `frontend/components/ui/AGENTS.md` | ABSENT | filesystem check (matches 0.2) |

Most specific PRESENT candidate = `frontend/AGENTS.md` → the line was added there, under **React & Next.js Patterns** (the section that already carries cross-surface contract rules like the Apollo error-mapping map):

> **Cross-surface navigation targets** (nav entries, card CTAs, notification deep-links) MUST single-source their route string in a leaf lib module — never duplicate route literals across consumers. Reference: `docs/parents/parent-link-request.md` (§DEV1-015 Closure).

One line + one-line pointer, phrased generically (no plan artifacts).

## 3. Root AGENTS.md "Important References" — verify-then-add

- **Verified:** `docs/parents/parent-link-request.md` was **genuinely absent** — repo grep for `parent-link-request` in root `AGENTS.md` → zero matches; only the sibling `docs/parents/handshake-code-discovery.md` was recorded (L459). The tasks.md expectation "parents docs already recorded" held for the discovery doc but NOT for the binding-workflow canonical doc (created by DEV1-014 and since extended).
- The section format accommodates it (one-line bullet per canonical doc with an em-dash summary) → **ONE line added** immediately after the handshake-code-discovery entry, same style: state machine with 7-day expiry, guarded single-writer student link, expiry/reminder semantics, notification choreography, error/oracle matrix, confirmation discoverability closure; INV-P1 binding.
- No other section of root AGENTS.md touched.

## 4. SKILL / instructions updates — evaluation (none expected, none needed)

- `.agents/instructions/frontend.instructions.md`: reviewed — it is generic agent-usage guidance (NextAuth cast, lint queue); it carries no domain navigation rules, so no update fires. The new rule lives in `frontend/AGENTS.md`, which the instructions file defers to.
- No SKILL files exist for this surface; single-writer/expiry/notification rules were already documented in `backend/services/AGENTS.md` and the canonical docs (the plan's "expected: none" held for every candidate EXCEPT the route-constant rule, which §2 disposes).

## 5. Verification

- Markdown validator on all touched files: `bun run scripts/validation/validate-mermaid.ts docs/parents/parent-link-request.md AGENTS.md frontend/AGENTS.md` → **exit 0** (sub-loop `--lifecycle duplicates` is N/A on `.md` — TS-only toolchain, recorded in 7.1-outcome §4).

## 6. Files modified

| File | Change |
|---|---|
| `frontend/AGENTS.md` | +1 bullet (cross-surface navigation single-source rule + pointer) |
| `AGENTS.md` | +1 Important References bullet (`docs/parents/parent-link-request.md`) |

## 7. Checkboxes

- tasks.md `- [ ] 7.2` → `- [x] 7.2` (7.2's sub-items are plain bullets, not checkboxes — nothing else to flip).
