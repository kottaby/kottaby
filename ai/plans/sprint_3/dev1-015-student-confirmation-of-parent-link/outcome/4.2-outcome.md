# Task 4.2 Outcome — PendingParentLinkRequestsCard dashboard discoverability card (REQ-015/052)

**Task ID:** 4.2 · **Plan:** DEV1-015 · **Date:** 2026-09-07 · **Verdict: VERIFY + FIX + DOCUMENT — all render-contract items verified, ONE test-harness defect fixed, suite green 21/0.**

---

## 1. Summary

The card files existed from the prior build mission. This mission **verified** every render-contract item against the implementation, found the component contract **fully compliant**, fixed **one test-file defect** (MockedProvider children must be a SINGLE fragment child — the array-children form renders an empty tree in this environment, failing all 14 render cells with `getByTestId` misses; the suite's own doc comment documented this requirement but the fragment wrapper was missing from `renderCard`), ran the full quality loop, and documented. No component or helper code changes were needed.

**Data path (document → hook → derivation):**
`myIncomingParentLinkRequestsQueryDocument` (`frontend/graphql/sharedDocuments.ts` — the SAME id-first list document as the decision page, REQ-051) → `useQuery(...)` in `PendingParentLinkRequestsCard` (zero-argument query, identity resolved server-side; NO count endpoint, NO bespoke invalidation bus — the Apollo normalized cache is the single truth, so the decision page's respond write-back re-renders the card to zero, REQ-016) → `deriveActionableIncoming(data.myIncomingParentLinkRequests, nowMs)` (pure helper reusing `displayLinkRequestStatus` + `isLinkRequestActionable` verbatim from `frontend/lib/parent-link-request-status.ts` — never forked) → `ActionableIncomingSummary | null` → render branch.

**Render states (REQ-015/052):**

| # | Condition | Surface |
|---|-----------|---------|
| 1 | query in flight | `CardShell` skeleton card (`aria-busy="true"`, zero settled copy, mirrored geometry) |
| 2 | query error | ONE localized inline `Alert` (codes folded via `extractErrorCode` + `resolveParentLinkDenialCopy` — raw server messages never reach the DOM) + retry `Button` calling `refetch` |
| 3 | zero actionable rows | `null` — renders NOTHING (no empty-state chrome) |
| 4 | N ≥ 1 actionable | title + count chip (`dashboardCardCount(N)`) + MOST RECENT requester line (bidi-isolated) + CTA to `STUDENT_LINK_REQUESTS_ROUTE`; no per-request list |

---

## 2. Files created / modified / not modified

| File | Status | Notes |
|---|---|---|
| `frontend/views/students/dashboard/PendingParentLinkRequestsCard.tsx` | created (prior agent) · **verified, unmodified** | 135 lines; all 4 branches per §1 |
| `frontend/views/students/dashboard/pending-parent-link-requests.ts` | created (prior agent) · **verified, unmodified** | PURE module — no React/Apollo imports, no clock reads (`nowMs` caller-owned); single pass, no `reduce`, no indexed access; latest = max `createdAt` (array order never trusted) |
| `frontend/views/students/dashboard/index.ts` | modified (prior agent) · verified via `git diff` | + `export * from "./PendingParentLinkRequestsCard";` — the barrel is the 4.3 import surface |
| `test/ui/components/students/PendingParentLinkRequestsCard.test.tsx` | created (prior agent) · **FIXED here** | `renderCard`: wrapped `<ApolloClientProbe /> + <PendingParentLinkRequestsCard />` in a single `<>…</>` fragment inside `MockedProvider` (array children → empty tree → 14 render cells failed). No assertion or fixture changes. |
| `CardShell.tsx`, `useNotificationDrawerActions.ts`, `sharedDocuments.ts`, `parent-link-request-status.ts`, `parent-link-denials.ts`, `isolate-bidi.ts`, locale `parentLink`/`common`/`errors` namespaces | **NOT modified** | consumed as-is (labels pre-existed from 1.1; route constant from 4.1) |

**Carry-forward for 4.3 (exact import):**

```ts
import { PendingParentLinkRequestsCard } from "@/frontend/views/students/dashboard";
```

(Mount it in the `RoleDashboardPage` student status slot next to `HandshakeCodeCard`; zero props.)

---

## 3. Test evidence (4.2.TE)

- Suite: `test/ui/components/students/PendingParentLinkRequestsCard.test.tsx` — Happy DOM + Apollo `MockedProvider` via `renderWithWrapper`, locale loop `["ar", "en"]` + locale-independent pure-helper block.
- Run (single-file, mirroring the `test:ui:components` runner verbatim — that script hardcodes the `test/ui/components/` directory so it does NOT accept a path filter; direct `bun test` is blocked by the runner-guard banner, so the identical invocation was used for the isolated run):

```
21 pass / 0 fail / 81 expect() — Ran 21 tests across 1 file
```

Cells (7 per locale × 2 locales = 14 render + 7 pure-helper = 21):
cold load → skeleton `aria-busy` · zero actionable → empty container (REQ-015/052) · present-1 → count-1 chip + FULL name (`dir="auto"` + `isolateBidi`) + CTA href pinned to constant AND frozen `"/student/link-requests"` · present-N → count-2 + MOST RECENT requester (max `createdAt`), confirmed row drops out, no name list · expired-row exclusion → stored-pending past `expiresAt` NOT counted even when newest · query error → ONE localized Alert, masked wire message asserted by ABSENCE, retry drives a REAL refetch (traffic `[Q, Q]`) · post-decision disappearance → `client.writeQuery` flips row to Confirmed → card unmounts (REQ-016) · helper edges: empty, all-terminal, boundary `expiresAt === now` NOT actionable (strict `>`), +1 ms actionable, ordering by max, mixed counting.

**Full-suite note (pre-existing, NOT this task):** `bun run test:ui:components` (full directory) exits 1 because the bun process is **OOM-killed by the kernel** (`dmesg: global_oom task=bun`) after ~10 files / 115 passes — deterministically, in BOTH runs, BEFORE ever reaching the `students/` files (the run boundary is identical pre- and post-fix). This is an environment memory limit of the sandbox, not a test failure and not a 4.2 regression. The task's own suite is fully green standalone; carry the OOM ceiling forward to 6.0/5.1 missions.

---

## 4. Quality Loop (4.2.QL)

`bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates` — exit **0** for all three:

- `frontend/views/students/dashboard/PendingParentLinkRequestsCard.tsx` ✅ (tsgo → oxlint → biome → lint:type-aware → duplicates)
- `frontend/views/students/dashboard/pending-parent-link-requests.ts` ✅
- `test/ui/components/students/PendingParentLinkRequestsCard.test.tsx` ✅ (re-verified after the fragment fix)

`tsgo` whole-project at task end: exit **0**, no errors.

---

## 5. Semantic checklist (4.2.SR) — verified item by item

- ✅ `useQuery(myIncomingParentLinkRequestsQueryDocument)` — SAME document as `StudentLinkRequestsContainer`; no new query, no count endpoint.
- ✅ `deriveActionableIncoming` PURE — no React/Apollo imports, no clock reads; `nowMs` caller-owned; actionability reused verbatim (computed status + strict-`>` liveness), never forked.
- ✅ Zero actionable → `return null` (card renders nothing, REQ-015/052/016).
- ✅ Loading → `CardShell busy` → `aria-busy="true"` + `Skeleton` placeholders mirroring settled geometry.
- ✅ Error → ONE localized `Alert` (`resolveParentLinkDenialCopy(extractErrorCode(error), te)`; raw messages never in DOM) + retry via `refetch` (`.catch(() => undefined)` guard).
- ✅ N=1/N>1 contract — count chip + MOST RECENT requester only; no per-request list on the dashboard.
- ✅ CTA ≥44px (`minHeight: 44`) + `focusVisibleRingSx` + `href={STUDENT_LINK_REQUESTS_ROUTE}` (4.1 constant import, no re-declared string).
- ✅ `useAppTranslation(ParentLink/Errors/Common)` property access (`t.dashboardCardTitle`, `tc.retry`, …) — never `t('key')`; zero hardcoded user-facing strings.
- ✅ `dir="auto"` + `isolateBidi(summary.latestParentFullName)` on the requester line; name assembled BEFORE interpolation.
- ✅ sx-only styling (no direct style props); colors exclusively `theme.palette.*` (warning.main, text.secondary via sx callbacks); no hex/rgb.
- ✅ `*Outlined` icons only (`PendingActionsOutlined`, `RefreshOutlined`).
- ✅ Logical properties (no physical left/right — `width`/`maxWidth` are direction-neutral; RTL via the emotion-cache stylis-plugin-rtl pipeline).
- ✅ Read purity — ONE `now` captured at mount via lazy `useState` initializer; no impure calls during render.
- ✅ No `console.*`; no Apollo-dispatch spaghetti (single `useQuery`, no client-side cache eviction, no invalidation bus).

---

## 6. Security (4.2.SR adjunct)

- **No data leaks in copy:** every user-facing string resolves through compile-time i18n handles; the error path folds ALL wire codes (mapped or unmapped) onto `errors`-namespace copy — raw server messages (including masked-class transport text) never reach the DOM (test asserts the masked fixture message by absence).
- **Name display sanctioned:** the parent FULL name on the dashboard card is the sanctioned disclosure per REQ-042/plan §1.1 — it is the minimum viable discoverability datum, rendered once (most recent only), bidi-isolated, and never duplicated into a list; `PARENT_NAME_*` fixtures in tests are the documented sanctioned exception.
- Zero-argument query keeps authorization server-side; the component carries NO client-side role logic — the page-level server guards remain the only boundary.

---

## 7. Instruction Verification (4.2.IV)

- `.github/instructions/frontend.instructions.md` (path surfaced by the sub-loop banner): verified **ABSENT** on disk (`[ -f ]` check) — prose-phantom, cited only for absence; nothing to comply with.
- `frontend/views/AGENTS.md`: verified **ABSENT** — per 0.2 prose-phantom ruling, confirmed again this mission; nothing to comply with.
- Present and honored: root `AGENTS.md` + `frontend/AGENTS.md` (client-component, sx/theme-token, logical-property discipline observed throughout).

---

## 8. BROWSER-EVIDENCE

**BF/BS: pending separate mission.** The agent-browser functional self-loop (student login → dashboard card renders with count/name/CTA → CTA lands on `/student/link-requests` → decide → card disappears; N=2 seeding; forced network error → localized Alert + retry) and the visual/styling self-loop (six-cell screenshot set 1440×900 / 768×1024 / 375×812 × en LTR/ar RTL, count-chip wrap + full-width CTA at 375px, long-Arabic-name truncation, RTL mirroring, dark/light contrast) are intentionally NOT executed in this code mission. The component-level contracts are proven at the Happy-DOM tier (§3).

---

## Worklog

- **Task ID:** 4.2 · **Agent:** Dashboard Card Finish Subagent · **Date:** 2026-09-07
- Read card, helper, barrel diff, and test; verified every render-contract item against the implementation (all compliant — see §5 checklist).
- Fixed the test harness defect: `renderCard` now passes MockedProvider a SINGLE fragment child (array children render an empty tree in this environment; suite's own comment documented the requirement). Component/helper/barrel untouched.
- Health: sub-loop `--lifecycle duplicates` exit 0 ×3; `tsgo` exit 0 (0 errors).
- Tests: card suite 21 pass / 0 fail / 81 expect() (14 render cells en+ar + 7 pure-helper units). Full `test:ui:components` OOM-killed by kernel after 10 files/115 passes in both runs — pre-existing sandbox memory ceiling, unrelated (dies before reaching `students/`); carry forward.
- Wrote this outcome; flipped tasks.md 4.2 / 4.2.QL / 4.2.TE / 4.2.SR / 4.2.IV to `[x]` (4.2.BF / 4.2.BS left unchecked for the browser mission).

## BF functional evidence (4.x-BF)
- Loop A (N=1): dashboard card renders with count chip "1 pending request" + "Latest request from BF Parent Alpha" (browser-evidence/4.2-dashboard-student1-count1.png); CTA click → /student/link-requests; CONFIRM from decision page (browser-evidence/4.2-confirm-dialog.png, 4.2-after-confirm.png); return to dashboard → card GONE (post-decision convergence, REQ-016) (browser-evidence/4.2-dashboard-post-decision-card-gone.png).
- Loop B (N=2, fresh student bf-student2): card shows "2 pending requests" + MOST RECENT requester "BF Parent Beta" (browser-evidence/4.2n-dashboard-student2-count2.png).
- Loop C (error): GraphQL route aborted → localized inline Alert rendered (browser-evidence/4.2err-graphql-aborted-alert.png); route unblocked → retry via refetch recovers to the normal card (browser-evidence/4.2err-recovered-dashboard.png).
- Verdict: 4.2.BF PASS (all REQ-064 functional cells exercised live).

## BS visual evidence (4.x-BS)
- **Task ID:** 4.x-BS · **Agent:** Browser Visual Subagent · **Date:** 2026-09-07
- Cells captured: present-N (count=2, most-recent requester = فاطمة الزهراء بنت الحسن العلوي): bs-dash-1440x900-{en,ar}.png, bs-dash-768x1024-{en,ar}.png, bs-dash-375x812-{en,ar}.png; present-1: bs-dash1-1440-en.png, bs-dash1-375-ar.png; bonus dark-mode: bs-dash-dark-1440-en.png.
- Inspection findings per checklist:
  - MUI theme-palette compliance: code audit of PendingParentLinkRequestsCard — only `theme.palette.warning.main` / `text.secondary` tokens + Chip `color="warning"`; zero hex/rgb literals; CardShell uses `outlineVariant`/`surfaceContainerLow`/`shadow.card`.
  - Typography hierarchy vs HandshakeCodeCard: identical — both titles `variant="h5" component="h2" fontWeight 700` + small icon + `body2` secondary line + contained Button. Consistent.
  - Count-chip wrapping on mobile (375px): chip row `flexWrap: "wrap"`; chip measured ABOVE the requester line (top 552 vs 573) — wraps above, not beside.
  - Full-width CTA at 375px: CTA `width: { xs: "100%", sm: "auto" }` — measured 293px = exact content-box width of its padded container; ≥44px minHeight.
  - Long-name truncation/overflow: long Latin name in ar present-1 card — `scrollWidth == clientWidth` (no overflow), wraps to 2 lines, no ellipsis.
  - RTL mirroring in ar: dir=rtl at all 3 viewports; icon/CTA alignment logical; count chip uses Arabic dual form "طلبا ربط قيد الانتظار" (N=2) and singular "طلب ربط واحد" (N=1) — i18n verified live.
  - dir="auto" name isolation: Arabic name inside en chrome and Latin name inside ar chrome both carry `unicodeBidi: isolate` on the requester line — testable in both directions.
  - Dark/light contrast: dark body rgb(10,20,34) vs title rgb(241,245,251) / secondary rgb(168,181,204) / CTA primary rgb(61,107,160) — token-driven, high contrast (bs-dash-dark-1440-en.png).
  - Horizontal overflow: scrollWidth == clientWidth at ALL six cells (no overflow anywhere).
- Defects found/patched: none in the card's own styling (see 4.3 for the slot-level defect that initially blanked this card).
- Final screenshots: bs-dash-1440x900-en.png, bs-dash-1440x900-ar.png, bs-dash-768x1024-en.png, bs-dash-768x1024-ar.png, bs-dash-375x812-en.png, bs-dash-375x812-ar.png, bs-dash1-1440-en.png, bs-dash1-375-ar.png, bs-dash-dark-1440-en.png.
- Verdict: 4.2.BS PASS.
