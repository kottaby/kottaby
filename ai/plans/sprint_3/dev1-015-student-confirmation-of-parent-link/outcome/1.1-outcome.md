# 1.1 — Pin Canonical Types (zero additions) + Extend `parentLink` i18n Namespace for Dashboard-Card Copy

**Date:** 2026-09-07 · **Task ID:** 1.1 (DEV1-015 — Student Confirmation of Parent Link)
**Requirements:** REQ-003, REQ-004, REQ-042, REQ-015 (copy contract) · **Sub-pipelines:** QL ✅ · TE ✅ · SEC ✅ · SR ✅ · IV ✅

---

## 1. Summary

Phase 1 executed exactly within its scope gate (NO schema/enum/migration work, NO new namespace):

1. **Canonical types CONFIRMED present — zero edits, zero type gaps, zero STOPs.**
2. **`parentLink` namespace extended** with the 6 dashboard-card keys frozen by 0.2 §4.5, in the type surface + `en` + `ar` sources. Retry copy was **NOT** added (0.2 §4.5 ruling verified live: `CommonLabels.retry` exists in both locales — `shared/locale/types/common/index.ts` L7, `shared/locale/en/common/index.ts` L9 `"Retry"`, `shared/locale/ar/common/index.ts` L9 `"إعادة المحاولة"`).
3. **Parity suite `MANDATED_KEYS` pin extended** per DI-0.2-03 (33→39 mandated keys; function-slot inventory 4→6) in the SAME change-set; suite green.
4. **Namespace registration checklist** (`shared/AGENTS.md` L234–239): only steps 1–2 fired (types interface + en/ar implementations). Steps 3–5 verified ALREADY wired — `message.ts` L33 (`parentLinkTranslations: ParentLinkLabels`), `en/messages.ts` L34 / `ar/messages.ts` L34 bundle mapping, `ParentLink` handle const (`shared/locale/namespaces/parentLink/parentLink.namespace.ts`, id `parentLink.parentLink`). No wiring file touched.

## 2. Canonical-Types Verification (verification-first anchors)

| Required symbol | Anchor (path + line, read in full) | Verdict |
|---|---|---|
| `ParentLinkRequestSelectType` | `backend/types/parents/parent-link-request.types.ts` L10 (`export type ParentLinkRequestSelectType = typeof parentLinkRequests.$inferSelect;`) | ✅ FOUND — REUSE, zero additions |
| `OutgoingParentLinkRequestReturnType` | same file L32–39 (readonly closed shape; `status: LinkStatus`; `studentMaskedName`) | ✅ FOUND |
| `IncomingParentLinkRequestReturnType` | same file L55–62 (readonly `{ id, status, parentFullName, createdAt, expiresAt, respondedAt }` — exactly the card's row contract) | ✅ FOUND |
| `DBTransaction` | `backend/types/db.types.ts` L23 | ✅ FOUND |
| `DBQueryExecutor` | `backend/types/db.types.ts` L30 | ✅ FOUND |
| Barrel export | `backend/types/index.ts` (L12 documents `DBTransaction`/`DBQueryExecutor` live in `@/backend/types`; parents barrel re-exported) | ✅ FOUND |

**Result: zero type gaps → no STOP, no ledger entry, no local type created anywhere in this task.**

## 3. Files Created / Modified (exact paths)

| File | Change |
|---|---|
| `shared/locale/types/parentLink/index.ts` | `ParentLinkLabels` +6 readonly members under a new "Student dashboard discoverability card" section; header doc gains surface #3 (card names the requesting PARENT only — REQ-015 copy contract note) |
| `shared/locale/en/parentLink/index.ts` | +6 en values (see §5 table) |
| `shared/locale/ar/parentLink/index.ts` | +6 ar values (proper RTL Arabic; Arabic-Indic digit pluralization following the `studentDirectory`/`summaryCountChip` convention) |
| `shared/locale/parentLink-namespace.parity.test.ts` | DI-0.2-03: `MANDATED_KEYS` 33→39; `FUNCTION_KEYS` 4→6; exhaustive-inventory test + comments updated ("39 slots"); 2 new exact template pins (`dashboardCardCount`, `dashboardCardLatestRequester`); Arabic-script probes extended to all six function slots; "callable/non-empty" sweep extended to all six |

`git diff --name-only` (after completion) = exactly these 4 files + the plan-dir docs (`tasks.md` checkbox flip, `deferred-items.md` DI-0.2-03 resolution, this outcome file). **Zero** files under `backend/db/schema/**`, `backend/db/migration/**`, `backend/services/**`, `backend/graphql/**`, `frontend/**`, `app/**` touched. Nothing committed; no `git add`.

## 4. Files NOT Modified (and why)

- `backend/types/parents/parent-link-request.types.ts`, `backend/types/db.types.ts`, `backend/types/index.ts` — canonical types verified complete (§2); 0.2 frozen as REUSE/zero-additions.
- `shared/locale/types/common/index.ts`, `shared/locale/{en,ar}/common/index.ts` — `retry` already exists in Common (0.2 §4.5 ruling + live verification); adding a parentLink retry key would create a near-duplicate semantic (forbidden by the `errors`-namespace near-duplicate rule in `shared/AGENTS.md`).
- `shared/locale/types/message.ts`, `shared/locale/{en,ar}/messages.ts`, `shared/locale/namespaces/parentLink/parentLink.namespace.ts`, server/LocaleProvider wiring — registration-checklist steps 3–5 pre-wired; only label members were added (handle const and getter unchanged).
- Every other repo file — out of 1.1 scope (scope gate + SR file-list discipline).

## 5. Keys Added (en + ar values, verbatim)

| Key | Shape | en value | ar value |
|---|---|---|---|
| `dashboardCardTitle` | `string` | `Pending link requests` | `طلبات ربط قيد الانتظار` |
| `dashboardCardCount` | `(count: number) => string` | `count === 1 ? "1 pending request" : \`${count} pending requests\`` | `0 → "لا توجد طلبات ربط قيد الانتظار"`, `1 → "طلب ربط واحد قيد الانتظار"`, `2 → "طلبا ربط قيد الانتظار"`, `3–10 → \`${count.toLocaleString("ar")\} طلبات ربط قيد الانتظار\``, `11+ → \`${count.toLocaleString("ar")\} طلب ربط قيد الانتظار\`` |
| `dashboardCardLatestRequester` | `(parentName: string) => string` | `` `Latest request from ${parentName}` `` | `` `أحدث طلب من ${parentName}` `` |
| `dashboardCardCta` | `string` | `Review requests` | `مراجعة الطلبات` |
| `dashboardCardLoading` | `string` | `Loading link requests…` | `جارٍ تحميل طلبات الربط…` |
| `dashboardCardLoadError` | `string` | `We couldn't load your link requests right now. Please try again.` | `تعذر تحميل طلبات الربط الآن. يرجى المحاولة مرة أخرى.` |

**Retry copy:** intentionally NOT added — consumers use `CommonLabels.retry` (`t.retry` via the `Common` handle), the established pattern (`NotificationDrawerBody.tsx` L64). Namespace total: 33 → **39** keys, en/ar bijective.

Arabic copy conventions honored: tone matches the existing namespace (`طلبات الربط` vocabulary, `تعذر… يرجى المحاولة` pattern from `sendUnavailableNotice`), Arabic-Indic digits via `toLocaleString("ar")`, Arabic plural classes (singular/dual/3–10 plural/11+ singular) mirroring the `studentDirectory` example in `shared/AGENTS.md`.

## 6. Verification Results (evidence)

### 6.1 Quality Loop (1.1.QL) — sub-loop `--lifecycle duplicates`, exit 0 per file

| File | tsgo | oxlint | biome:check | lint:type-aware | check:duplicates | EXIT |
|---|---|---|---|---|---|---|
| `shared/locale/types/parentLink/index.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `shared/locale/en/parentLink/index.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `shared/locale/ar/parentLink/index.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `shared/locale/parentLink-namespace.parity.test.ts` | ✅ | ✅ | ✅ | ✅ | ✅ (jscpd n/a — outside scan scope) | **0** |

### 6.2 Test Engineering (1.1.TE)

- **Parity suite (mandated runner):** `bun run test/scripts/run-test.ts shared/locale/parentLink-namespace.parity.test.ts` → **68 pass / 0 fail, 434 expect() calls** (was 57 tests before the 6 new mandated-key cells + 2 new template pins). Includes: en/ar identical key sets; mandated key × 39 on BOTH maps; exhaustive inventory (no silent key minting); status vocabulary untouched (exactly 4 status keys); Arabic-script probe on every ar string slot + all six function slots; exact template pins for both new function slots; registry wiring unchanged.
- **Key-access compile check (no orphan keys):** `bun run tsgo` → **exit 0, zero `error TS` findings** project-wide. Both leaf consts are typed `ParentLinkLabels` and the interface sits in the `Translations` tree via `message.ts` — a key present in a map but missing from the interface, in one locale only, or unreachable through the tree would fail compile. The runtime exhaustive-inventory test is the second belt (no extra keys beyond the mandated 39).
- **Locale server loader regression:** `bun run test/scripts/run-test.ts shared/locale/server.test.ts` → **21 pass / 0 fail** (`catalog.parentLinkTranslations` resolves).
- **Consumer regression (namespace consumers unchanged):** `test/ui/components/students/StudentLinkRequestsContainer.test.tsx` (en LTR + ar RTL, 22 tests) → **22 pass / 0 fail**. Run with the sanctioned `test:ui:components` preload chain (`test/ui/test-env.ts`, `happydom-preload.ts`, `translation-preload.ts`, `next-dynamic-mock.ts`) + `.env.test.ci`; the repo's test-runner guard bypass (`KOTTABY_TEST_RUNNER_OK=1`, the guard's own documented quick-debug escape) was used to scope one file instead of the full 6-minute battery. Attribution note: an initial wrapper invocation without the happy-dom preload failed all 22 with `ReferenceError: document is not defined` — infra, not code (identical tree; green with preloads).

### 6.3 Baseline parity

0.1 baseline was fully clean (tsgo 0 / biome 0 / lint 0). Post-1.1: tsgo 0 errors; all four files pass the full sub-loop chain → **zero new tool findings** (≤ baseline).

## 7. Security & Tenancy Audit (1.1.SEC)

- ✅ **No user data embedded in labels** — the two parameterized slots take only `(count: number)` and the already-assembled `parentName` display string; JSDoc on `dashboardCardLatestRequester` pins the `isolateBidi`-before-interpolation obligation for the 4.2 component.
- ✅ **No disclosure-bearing copy / no student identifiers** — all six strings name only the requesting parent (the sanctioned incoming disclosure, REQ-015) and generic chrome; nothing student-identifying exists anywhere in the card copy (the card is student-facing, so the parent-side masking contract R9 is not implicated; still, no student data of any kind enters the namespace).
- ✅ **No existence-oracle or error-detail leakage** — `dashboardCardLoadError` is a generic localized failure line; per-denial-class copy keeps flowing through `resolveParentLinkDenialCopy` (4.2's obligation, recorded as carry-forward).
- ✅ No secrets, URLs, env values, or injection surfaces (pure data files; one format spec `toLocaleString("ar")`).

## 8. Semantic Review (1.1.SR) — item-by-item

| Item | Result |
|---|---|
| No duplicate keys anywhere in the tree | ✅ en/ar `Object.keys` sets identical (parity test 1); `MANDATED_KEYS` = 39 entries / 39 unique, `FUNCTION_KEYS` = 6/6 (scripted count check); duplicate object-literal keys would also fail tsgo (0 errors) |
| No orphan keys (reachable through the `Translations` tree) | ✅ tsgo 0 errors (compile gate) + exhaustive-inventory runtime belt (no keys beyond the mandated 39) + `server.test.ts` bundle-resolution pass |
| No `next-intl` imports | ✅ `rg "next-intl"` over all 4 files: zero matches (system fully migrated; verified live) |
| Flat `ErrorsLabels`-style conventions | ✅ new members are flat camelCase interface members (no nesting), domain-prefixed (`dashboardCard*`), typed function slots for pluralization/interpolation per the compile-time system's pluralization/interpolation patterns |
| No dead branches / dead keys | ✅ every new key has both en+ar implementations consumed by pins; ar `dashboardCardCount`'s `count === 0` branch is defensive plural-safety (card unmounts at 0) — covered by the function-contract shape, not dead code |
| No cross-layer imports | ✅ locale files import only `@/shared/locale/types/parentLink` (type-only); parity test imports only `@/shared/locale/**` + `bun:test`; zero `@/frontend|@/backend|@/app` refs in touched files |
| No `console.*` | ✅ zero matches in touched files |
| Scope boundary | ✅ `git diff --name-only` = exactly the 4 code files (+ plan-dir docs: tasks.md checkbox flip, deferred-items.md DI-0.2-03 → ✅, this outcome file); no untracked strays; nothing staged/committed |
| Enum discipline | ✅ N/A-by-shape — 1.1 adds pure label data + test pins; no runtime enum usage was introduced. The parity test's existing deliberate string-literal status fixtures (`STATUS_LABEL_KEYS`) are per that file's own no-backend-enum-import rule (unchanged). Backend value-import enums (`LinkStatus`, `UserRole`, `NotificationType`) remain untouched in this task's scope |

## 9. Instruction Verification (1.1.IV)

- **`shared/AGENTS.md` (read in FULL)** — namespace checklist steps 1–2 executed in order (types → en → ar); steps 3–5 verified pre-wired, untouched; layer isolation respected (no `@/frontend|backend|app` imports); no new namespace registered; `errors`-namespace near-duplicate rule respected (retry NOT duplicated into parentLink); pluralization pattern followed.
- **Root `AGENTS.md` (printed by sub-loop for every touched file; read in FULL)** — deep-import + barrel conventions, i18n rules (no next-intl, property access), no-console, no summary-markdown rule (outcome files are plan-process artifacts, not repo summaries), never-clear-caches, run-test wrapper for tests.
- **Sub-loop instruction discovery for the 3 locale source files:** "No applicable instruction files found" → confirms `.agents/instructions/frontend.instructions.md` does **NOT** scope to `shared/**` (its `applyTo` glob covers `frontend/**` + `app/**` only). Its rules were still cross-checked as design context for 4.2 consumption; nothing in it governs shared/locale edits.
- **Sub-loop instruction discovery for the parity test:** `.github/instructions/tests.instructions.md` (the corpus file actually lives at `.agents/instructions/tests.instructions.md`; read in FULL) — compliance: `bun:test` imports only; pure unit tier (no DB, no `runInRollback` needed, no `getServerTranslations`); no `console.*`; no `any`; static source-scan pins updated in the SAME change as the scanned surface (this file IS the pin); runner guard respected.

## 10. Environment Deviations (recorded, not ledgered)

1. **Branch tooling quirk:** the task brief places this work on `feat/dev1-015-student-confirmation-of-parent-link`, but the sandbox repeatedly auto-reverts `git checkout` of that branch back to `main` (observed twice via reflog; branch exists at `ffce457`, one commit behind local `main` = `df09054`, which carries the Phase 0 docs). Since committing is forbidden for this task and working-tree diffs are branch-independent, all work was performed as **uncommitted changes on the `main` working tree** (HEAD `df09054`, containing 0.1/0.2 outcomes + ledger). The orchestrator should ensure these diffs land on the feature branch when staging Phase 1. No code impact.
2. **Parity-test sub-loop prints `.github/instructions/tests.instructions.md`** but the corpus file lives at `.agents/instructions/tests.instructions.md` — read the real file; no action needed.

## 11. Carry-Forward for Future Subtasks

- **Task 4.2 (card) consumes EXACTLY these keys** via `useAppTranslation(ParentLink)` property access: `t.dashboardCardTitle`, `t.dashboardCardCount(count)`, `t.dashboardCardLatestRequester(isolatedParentName)`, `t.dashboardCardCta`, `t.dashboardCardLoading` (skeleton `aria-busy` copy), `t.dashboardCardLoadError` (inline Alert body), and `t.retry` **from the `Common` handle** for the retry affordance. The requester name must be `isolateBidi`-assembled BEFORE being passed (see `shared/lib/isolate-bidi.ts`).
- **Parity-suite maintenance rule for 4.2+/5.1:** any future key under `parentLink` must extend `MANDATED_KEYS` (and `FUNCTION_KEYS` if function-valued) + the inventory count comments in the same change-set — the exhaustive-inventory test fails otherwise (DI-0.2-03 resolved; rule now embedded in the suite).
- **Phase 5.1 parity battery** should run `bun run test/scripts/run-test.ts shared/locale/parentLink-namespace.parity.test.ts` (68 cells) + `shared/locale/server.test.ts` (21 cells).
- Arabic count copy uses Arabic-Indic digits (`toLocaleString("ar")`) — component tests asserting count chips in ar must expect `٣`-style digits.

## 12. Cross-File Dependencies Discovered

- `shared/locale/parentLink-namespace.parity.test.ts` is load-bearing on BOTH leaf maps AND on itself (mandated-list ↔ leaf-map bijection) — types/en/ar/test form a 4-file atomic unit; none can drift alone (compile + suite enforce).
- `CommonLabels.retry` is now a hard dependency of the 4.2 card's error state (retry affordance copy lives in the `Common` namespace, not `parentLink`) — if `Common.retry` were ever removed, 4.2's retry breaks independently of this namespace.
- `shared/locale/server.test.ts` (L124) and `test/ui/components/students/StudentLinkRequestsContainer.test.tsx` are the nearest consumer suites — both green post-change.
- Ledger: DI-0.2-03 **✅ Done** (resolved by this task; evidence §6.2). DI-0.2-01 / DI-0.2-02 untouched (owned by 2.2/4.3). No new ledger rows — zero unexpected substrate/code discoveries this task.

## 13. Checkbox State

`tasks.md` 1.1 + sub-checkboxes 1.1.QL, 1.1.TE, 1.1.SEC, 1.1.SR, 1.1.IV all flipped to `[x]` **after** this outcome file's evidence existed.
