# Task 4.5 Outcome — Decision-page regression pin (read-only verification, no redesign)

**Task ID:** 4.5 · **Plan:** DEV1-015 · **Date:** 2026-09-07 · **Verdict: PASS — all 3 relevant suites GREEN, `frontend/views/students/link-requests/**` byte-UNMODIFIED, zero reconciliation needed against 4.1–4.4.**

---

## 1. Suites located & run (each ONCE)

Located per 0.2 §item 10 (view = 8 files under `frontend/views/students/link-requests/`) and a repo grep (`test/**` referencing `students/link-requests` → exactly ONE suite hits the view tier; the two documents-adjacent suites pin the shared wire documents the view consumes):

| # | Suite | Tier / runner | Result | Baseline cross-check |
|---|---|---|---|---|
| 1 | `test/ui/components/students/StudentLinkRequestsContainer.test.tsx` | Happy DOM + Apollo `MockedProvider` — scoped `test:ui:components` invocation (4 preloads + `.env.test.ci`, `KOTTABY_TEST_RUNNER_OK=1`) | **22 pass / 0 fail / 122 expect()** `[4.32s]` | identical to 1.1-outcome §baseline (22/0, en LTR + ar RTL) — zero drift |
| 2 | `frontend/graphql/sharedDocuments/parents/parent-link.documents.test.ts` | PURE bun (header: "Zero server boot, zero DB, zero network" — no DOM import) → `bun run test/scripts/run-test.ts` | **15 pass / 0 fail / 112 expect()** `[188ms]` | identical to 3.2-outcome baseline (15/112) — zero drift |
| 3 | `frontend/graphql/sharedDocuments/documents.contract.test.ts` (generic structural lock at `sharedDocuments/`, NOT in `parents/`) | PURE bun (same header discipline) → `bun run test/scripts/run-test.ts` | **20 pass / 0 fail / 124 expect()** `[159ms]` | identical to 3.2-outcome baseline (20/124) — zero drift |

**Totals: 57 pass / 0 fail / 358 expect() across 3 files.**

Exact invocations:

```
# DOM tier (the sanctioned 4.1/4.3/4.4 scoped pattern, verbatim):
bun run scripts/lib/run-locked-cmd.ts test:ui:components TEST_SERVER_MODE=production \
  bun --env-file=.env.test.ci test --parallel=1 \
  test/ui/components/students/StudentLinkRequestsContainer.test.tsx \
  --preload ./test/ui/test-env.ts --preload ./test/ui/components/happydom-preload.ts \
  --preload ./test/ui/components/translation-preload.ts --preload ./test/ui/components/next-dynamic-mock.ts

# Pure tier (no DOM, no DB → run-test.ts suffices, per task note):
bun run test/scripts/run-test.ts frontend/graphql/sharedDocuments/parents/parent-link.documents.test.ts
bun run test/scripts/run-test.ts frontend/graphql/sharedDocuments/documents.contract.test.ts
```

Environment note: real PostgreSQL 17 per mission posture; neither documents suite touches it (pure AST tier) and the container suite is Apollo-mocked (no DB). All green on first run — no pre-existing failures, no ledger rows required.

## 2. Unmodified proof (the pin)

```
$ git status --short -- frontend/views/students/link-requests/
(empty — exit 0)

$ git diff HEAD --stat -- frontend/views/students/link-requests/
(empty)

$ sha1sum of the 8 view files (index.ts, StudentLinkRequestsContainer.tsx, LinkRequestCard.tsx,
  LinkRequestDecisionDialog.tsx, IncomingBody.tsx, IncomingStates.tsx, IncomingStates.parts.tsx,
  useLinkRequestDecision.ts) | sha1sum
→ 9a8fe79a1a36847e0d91828bfe179eb37e1109fa
```

The full working-tree `git status --short` shows only the expected 4.1–4.4 deltas (drawer seam, nav constant+pin, dashboard card/slot, locale keys, backend service/wire test extensions) — **no entry under `frontend/views/students/link-requests/**`**. DEV1-014 view logic was never touched, per the hard boundary.

## 3. Overlap audit vs 4.1–4.4 (no reconciliation required)

4.1–4.4 changed: `useNotificationDrawerActions.ts` (route-constant export + resolver), `NotificationDrawerBody.tsx` (row href), `navItems.ts` (constant consumption), `RoleDashboardPage.tsx` (slot composition), the new dashboard card — none is inside the decision-page view; the container consumes only `myIncomingParentLinkRequestsQueryDocument` + `respondToParentLinkRequestMutationDocument` (shared documents unchanged — proven by suites #2/#3 at identical counts). The suites pin `"/student/link-requests"`-behaviour only through data/CTA wiring that 4.1–4.4 deliberately left behaviorally byte-identical; every count and cell (dialog in-flight disable, refetch flip, expired-COMPUTED, FORBIDDEN/RATE_LIMITED arms) reproduced the 1.1/3.2 baselines exactly. **No suite referenced changed behavior; zero edits made anywhere in this task.**

## 4. Verdict

**REGRESSION PIN GREEN.** The decision page's existing behavior is pinned by green suites at pre-4.x baseline counts with the view byte-identical to HEAD. Requirements touched: REQ-010 (decision page renders per link status), REQ-014 (read purity), REQ-016 (cache write-back → UI flip), REQ-060 (documents contract) — all verified at the component + document tiers.

---

## Worklog

- **Task ID:** 4.5 · **Agent:** Decision Page Pin Subagent · **Date:** 2026-09-07
- Located suites: container suite (only `test/**` file referencing the view), parent-link documents lock, generic documents contract lock; classified documents suites as PURE from their headers → `run-test.ts`; container suite via the sanctioned 4-preload scoped runner.
- Ran each suite ONCE: 22/0 (122 expect), 15/0 (112 expect), 20/0 (124 expect) — 57/0 total; all match recorded baselines (1.1: 22/0; 3.2: 15/112 + 20/124).
- Captured unmodified proof: empty `git status --short` + empty `git diff HEAD --stat` for `frontend/views/students/link-requests/`; aggregate SHA1 `9a8fe79a…1109fa`.
- No code, test, or view edits made (read-only mission); outcome written; tasks.md `- [ ] 4.5` flipped to `[x]`.
