# Task 4.3 Outcome — Dashboard status-slot composition (HandshakeCodeCard + PendingParentLinkRequestsCard, REQ-015/053)

## 1. Summary

The student branch of `resolveStatusSlot` in `frontend/views/dashboard/home/RoleDashboardPage.tsx` now composes `HandshakeCodeCard` + `PendingParentLinkRequestsCard` as SIBLINGS inside a single `Stack` (`sx`-only spacing, `theme.spacing(2)` — plan D6). No `DashboardView` contract change (its `statusSlot?: ReactNode` prop is consumed as-is), no new props, no other-role impact: the `Teacher` case and the `default` (parent/admin) case are byte-identical to the pre-task state (verified via `git diff`, §5). Both cards remain zero-prop client components whose hooks live INSIDE the card components — the server composition is plain JSX, so no conditional-hook surface exists. The pending-requests card renders `null` on an empty actionable queue, so the slot degrades gracefully to the handshake card alone.

## 2. Files created / modified / not modified

| File | Delta |
|---|---|
| `frontend/views/dashboard/home/RoleDashboardPage.tsx` | **Modified** — student branch of `resolveStatusSlot` wrapped in `<Stack sx={theme => ({ display: "flex", flexDirection: "column", gap: theme.spacing(2) })}>`; `PendingParentLinkRequestsCard` added to the existing `@/frontend/views/students/dashboard` import; doc comment updated. Teacher/default branches untouched. |
| `test/ui/components/dashboard/RoleDashboardPage.slot.test.tsx` | **Created** — the slot suite (DI-0.2-02: none existed; this is the FIRST `RoleDashboardPage`/slot suite in `test/ui/components/dashboard/`, joining `profile-view.test.tsx`). |
| `frontend/views/dashboard/home/DashboardView.tsx` | **NOT modified** — no contract change. |
| Any other role page / nav / layout | **NOT modified** — scope boundary held. |

## 3. Test evidence (4.3.TE)

`test/ui/components/dashboard/RoleDashboardPage.slot.test.tsx` — **7 pass / 0 fail / 51 expect() calls** across 1 file (`[2.3s]`):

- **Student (en + ar, the CHANGED branch — both locales per suite convention):**
  - student → BOTH cards compose in the status slot beside the untouched dashboard chrome: `handshake-code-card` settled (translated `yourCodeTitle`) AND `pending-parent-link-requests-card` settled (`dashboardCardTitle` + `dashboardCardCount(1)` + the ONE CTA whose `href` is pinned to `STUDENT_LINK_REQUESTS_ROUTE` AND the frozen `"/student/link-requests"`); welcome header + stat grid (`sessionsCompleted`) intact.
  - slot composition → `handshakeCard.compareDocumentPosition(pendingCard) & DOCUMENT_POSITION_FOLLOWING !== 0` (handshake strictly PRECEDES pending) AND both cards share ONE parent element (the slot Stack — sibling composition, not nesting).
- **Other roles (en matrix — unchanged surfaces pinned by absence + chrome presence):**
  - teacher → `applicant-status-card` renders (`ApplicantStatus.Pending` → `statusPending` copy); ZERO student cards mount — absence pinned at settled AND skeleton AND error testids.
  - parent / admin → empty status slot (no card of any kind, no teacher card); dashboard renders unchanged (welcome + stats). No Apollo mocks supplied — an unmatched-operation error would fire if the slot ever mounted a query-bearing card, making the empty-slot pin load-bearing.
- **Harness:** the REAL `createRoleDashboardPage` factory runs per test against a controlled `getServerUserContext` double (`mock.module("@/backend/lib/auth/server-auth")` registered BEFORE the dynamic import of the SERVER-ONLY `home/server` barrel — the `test/ui/page-guards/parent-handshake-page.test.ts` precedent); the produced `DashboardView` element renders under `MockedProvider` + `renderWithWrapper` + `AuthContext.Provider` (the story-harness composition). No DB, no network, no real `next/navigation` (translation-preload's inert double covers the never-taken redirect paths).

**Runner note (carry-forward of the 4.2 OOM ceiling):** the positional path filter in `bun run test:ui:components <file>` is NOT supported (the arg is ignored and the full directory runs, then the kernel OOM-kills bun after ~10 files — pre-existing sandbox ceiling). The sanctioned bypass was used instead: `KOTTABY_TEST_RUNNER_OK=1 TEST_SERVER_MODE=production bun --env-file=.env.test.ci test --parallel=1 <paths> --preload <the four script preloads>`. The scoped `dashboard/ + students/` pairing ran **73 pass / 0 fail / 363 expect() across 5 files** (profile-view + slot suite + the three students/ suites) — no regression in neighboring suites.

## 4. Quality Loop (4.3.QL)

- `bun run scripts/health/sub-loop.ts frontend/views/dashboard/home/RoleDashboardPage.tsx --lifecycle duplicates` — **exit 0** (tsgo ✅, oxlint ✅, biome:check ✅, lint:type-aware ✅, check:duplicates ✅).
- `bun run scripts/health/sub-loop.ts test/ui/components/dashboard/RoleDashboardPage.slot.test.tsx --lifecycle duplicates` — first run **exit 1** (4 × `no-floating-promises` on the async `renderRoleDashboard` call sites; 1 × `jsx-no-constructed-context-values` warning → fixed by `await`ing the helper and hoisting the AuthContext value out of the JSX prop), second run **exit 1** (1 × `sonarjs/prefer-specific-assertions` → fixed by switching the DOM-order assertion to `.not.toBe(0)`), final run **exit 0** (plus one `vitest(no-commented-out-tests)` false-positive defused by rewording a comment containing `it (`).
- `bun run tsgo` (full project) — **exit 0, zero errors**.

## 5. Semantic checklist (4.3.SR) — verified item by item

- **Single-role diff surface:** `git diff -- frontend/views/dashboard/home/RoleDashboardPage.tsx` shows ONLY (a) the `Stack` import, (b) `PendingParentLinkRequestsCard` merged into the existing students-barrel import, (c) the doc-comment student bullet, (d) the student `case` body. The `case UserRole.Teacher:` arm and `default: return undefined;` arm appear as UNCHANGED context lines — no other role's branch was touched. No other file in `frontend/views/dashboard/**` was modified.
- **`sx`-only wrapper:** the Stack carries NO `spacing`/`direction` MUI props — spacing is `gap: theme.spacing(2)` inside the `sx` callback (theme token, not a magic px); `flexDirection: "column"` is explicit in the same `sx`. `gap` is a logical property (axis-relative, RTL-safe — no `marginLeft`-class physical property anywhere in the delta).
- **No hardcoded colors:** the delta contains zero color literals (no hex/rgb/named colors, no string palette access). All existing card styling stays inside the card components.
- **No conditional-hook violations:** `resolveStatusSlot` is a plain function returning JSX; `useQuery`/`useState` live inside `HandshakeCodeCard` / `PendingParentLinkRequestsCard` ("use client") — hook ORDER is per-component and unaffected by which branch composes them. The server factory remains a Server Component (`withPageAuth` + element return, no hooks).
- **Scope boundary:** `RoleDashboardPage.tsx` + 1 new test file + plan docs (tasks.md checkboxes, this outcome) — nothing else.

## 6. Instruction Verification (4.3.IV)

- `.agents/instructions/frontend.instructions.md` (PRESENT — the `.github/instructions/frontend.instructions.md` path surfaced by the sub-loop banner is a prose-phantom, verified ABSENT on disk): complied — MUI v9 (`sx` callback only, no style props, no hardcoded colors/theme tokens respected), i18n (no UI copy in the delta; test asserts only through `<Namespace>.getLabels(getTranslations(locale))` with `renderWithWrapper` receiving the SAME locale), no `console.*`.
- Dashboard-layer AGENTS.md: `frontend/views/AGENTS.md`, `frontend/views/dashboard/AGENTS.md`, `frontend/views/dashboard/home/AGENTS.md` all verified **ABSENT** (root AGENTS.md's `frontend/views/AGENTS.md` table row remains the known prose-phantom per 0.2) — nothing to comply with.
- `test/ui/AGENTS.md` (PRESENT): component-test conventions followed — the four script preloads, `renderWithWrapper` + Apollo `MockedProvider` (never a real server), same-locale discipline, namespace-handle assertions, no `.skip`/`.only`. (Its pointer to `frontend/components/ui/AGENTS.md` is another prose-phantom — ABSENT.)

## 7. BROWSER-EVIDENCE

**BF/BS: pending separate mission.** The agent-browser functional self-loop (student login → dashboard renders both cards; navigate away/back → no duplicate fetches via Apollo cache; teacher dashboard shows zero student cards, zero console errors) and the visual/styling self-loop (six-cell screenshot set 1440×900 / 768×1024 / 375×812 × en LTR/ar RTL with BOTH cards present — vertical rhythm, stack spacing vs design tokens, 375px overflow, RTL order/alignment in ar) are intentionally NOT executed in this code mission. The composition contracts are proven at the Happy-DOM tier (§3); spacing-token tuning feedback, if any, lands as an `sx` edit in this same file.

---

## Worklog

- **Task ID:** 4.3 · **Agent:** Dashboard Slot Subagent · **Date:** 2026-09-07
- Read tasks.md 4.3, 0.2 anchors (`resolveStatusSlot` student branch), 4.2 carry-forward (exact barrel import), full `RoleDashboardPage.tsx`; confirmed DI-0.2-02 (no existing dashboard slot suite → created one).
- Edited `RoleDashboardPage.tsx` only in the student branch (Stack + two zero-prop cards, `sx` gap via `theme.spacing(2)`); teacher/default arms untouched — verified via `git diff`.
- Created `test/ui/components/dashboard/RoleDashboardPage.slot.test.tsx`: real `createRoleDashboardPage` over a `server-auth` double (page-guard-suite precedent), MockedProvider + renderWithWrapper + AuthContext (story-harness composition); 7 tests = student matrix en+ar (both cards + chrome) ×2, slot-ordering/sibling cell ×2, teacher absence cell, parent/admin `test.each` empty-slot cells.
- Health: sub-loop `--lifecycle duplicates` exit 0 on BOTH files (test file needed two lint fix-ups: awaited async render helper + hoisted context value + `.not.toBe(0)` matcher); `tsgo` exit 0 (0 errors).
- Tests: new suite 7 pass / 0 fail / 51 expect(); scoped `dashboard/ + students/` run 73 pass / 0 fail / 363 expect() across 5 files. Full-directory `test:ui:components` positional filter unsupported + kernel OOM ceiling (pre-existing, per 4.2) — bypassed with the sanctioned `KOTTABY_TEST_RUNNER_OK=1` scoped invocation; carry the ceiling forward to 6.0/5.1.
- Wrote this outcome; flipped tasks.md 4.3 / 4.3.QL / 4.3.TE / 4.3.SR / 4.3.IV to `[x]` (4.3.BF / 4.3.BS left unchecked for the browser mission).
