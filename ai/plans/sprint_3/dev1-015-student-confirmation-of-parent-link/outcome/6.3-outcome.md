# Task 6.3 Outcome — review-frontend (READ-ONLY)

**Task ID:** 6.3 · **Agent:** review-frontend · **Date:** 2026-09-07 · **Verdict: PASS with 2 LOW + 2 INFO findings — no CRITICAL/HIGH/MED, zero code fixes applied (read-only mission).**

Scope reviewed: `frontend/components/ui/useNotificationDrawerActions.ts`, `frontend/components/ui/NotificationDrawerBody.tsx`, `frontend/views/students/dashboard/PendingParentLinkRequestsCard.tsx`, `frontend/views/students/dashboard/pending-parent-link-requests.ts`, `frontend/views/students/dashboard/index.ts`, `frontend/views/dashboard/home/RoleDashboardPage.tsx`, `frontend/views/dashboard/nav/navItems.ts`, `shared/locale/{types,en,ar}/parentLink/index.ts` — plus 4.1–4.4 outcome BF/BS sections and `outcome/browser-evidence/` contents.

---

## Review checklist

### 1. MUI v9 discipline — CLEAN
- `sx`-only everywhere in the delta (no direct `style` props); `RoleDashboardPage.tsx:56` Stack carries the serializable-object form `sx={{ display: "flex", flexDirection: "column", gap: 2 }}` — the 4.3.BS SSR fix (function-sx across the server→client boundary) is CONFIRMED PRESENT; `gap: 2` maps through MUI System spacing (no `theme.spacing()` call in serialized JSX).
- Colors: `rg '#hex|rgb(|rgba(|hsl('` over all ten delta files → **zero matches**; only `theme.palette.warning.main`, `text.primary/secondary`, `primary.main` via sx callbacks + Chip `color="warning"` token.
- Icons: `PendingActionsOutlined`, `RefreshOutlined` (card), `LinkOutlined` (nav) — all `*Outlined`; every icon in `navItems.ts` is the Outlined variant.
- ≥44px targets: CTA/retry `minHeight: 44` (card L18/L21), drawer error retry `minHeight: 44` (NotificationDrawerBody L63), rows are MUI `ListItemButton` (default `minHeight: 48`, `py: 1.5` overrides padding only).
- Logical properties: no physical `left`/`right` anywhere; `gap`/`flexDirection` axis-relative; the lone `paddingTop: "7px"` (NotificationDrawerBody L113) is vertical → direction-neutral, permitted by the plan's "no physical left/right" rule.
- Person names: `dir="auto"` + `isolateBidi(...)` assembled BEFORE interpolation (card L126–127); drawer emitter copy `dir="auto"` on title+body.

### 2. i18n — CLEAN, one dead-key finding (F1)
- Handle-form property access throughout: `t.dashboardCardTitle/Count/LatestRequester/Cta`, `tc.retry`, `te` (Errors), drawer `t.loadErrorTitle/emptyTitle/...`, `commonT.retry`; `aria-label={t.title}` also handle-form. **Zero hardcoded user-facing strings** in the delta (only `aria-busy="true"`, `dateTime`, testids).
- Both locales: component suites loop `["ar","en"]` per 4.1/4.2/4.3 outcomes; `shared/locale/parentLink-namespace.parity.test.ts` runtime-pins en/ar key parity.

### 3. Apollo — CLEAN
- ONE list document: `myIncomingParentLinkRequestsQueryDocument` (`frontend/graphql/sharedDocuments/parents/parent-link.documents.ts:70`) — id-first selection, zero-argument (identity server-side) — consumed verbatim by BOTH the card (`PendingParentLinkRequestsCard.tsx:63`) and the decision page (`StudentLinkRequestsContainer.tsx:75`). No new query, no count endpoint.
- Single `useQuery` in the card; NO bespoke invalidation bus / `cache.evict` / `writeQuery` in any delta file — normalized-cache write-back is the sole convergence mechanism. Drawer hook consumed by `NotificationDrawer.tsx:101` (not orphaned).

### 4. Component patterns — CLEAN
- `"use client"` correct on all three client files (`useNotificationDrawerActions.ts`, `NotificationDrawerBody.tsx`, `PendingParentLinkRequestsCard.tsx`); `navItems.ts` client-module directive consistent with its client consumers; `pending-parent-link-requests.ts` is PURE (no React/Apollo imports, no clock reads — `nowMs` caller-owned; reuses `displayLinkRequestStatus`/`isLinkRequestActionable` verbatim, never forked).
- `RoleDashboardPage.tsx` stays a Server Component (no hooks; `withPageAuth` + element return); cards compose as plain-JSX siblings → no conditional-hook surface; card's own `useState`/`useQuery` all precede its early returns.

### 5. Browser evidence review — 2 LOW gaps (F2)
- All BF verdicts PASS with every referenced screenshot VERIFIED PRESENT on disk (4.1: drawer-before-click, linkrequests-after; 4.2: count1/confirm-dialog/after-confirm/post-decision-gone/count2/err-aborted/err-recovered; 4.3: both-cards-student, teacher-no-student-cards; 4.4: sidebar-landing + teacher/parent/anonymous redirects).
- Six-cell sets: **4.2 complete** (bs-dash-1440x900/768x1024/375x812 × en/ar + dash1 + dark extras); **4.3 complete** (documents reuse of the same post-patch bs-dash six). **4.1.BS and 4.4.BS captured only 2 cells each** — see F2.

### 6. Leftover fake data scan — CLEAN
`rg 'BF Parent|Parent Alpha|Parent Beta|bf-student|…'` over `frontend/` → **zero matches**; the BF-seeded names exist only in outcome prose as browser-seeding evidence. Delta components/stores carry no hardcoded names, rows, or metric literals — only skeleton/empty/error scaffolding with settled copy from i18n handles.

### 7. Dead code / unused exports — 1 finding (F1)
- All delta exports consumed: `STUDENT_LINK_REQUESTS_ROUTE` (drawer body + card + nav + tests), `resolveNotificationRoute` (drawer body), `useNotificationDrawerActions` (NotificationDrawer), `deriveActionableIncoming`/`ActionableIncomingSummary` (card), barrel `index.ts` (4.3 import surface), `getNavItemsForRole`/`resolveNavItemLabel`/`NotificationsIcon` (pre-existing consumers). Private helpers `withoutPendingId`/`NO_PENDING_IDS` used. `respondedAt` in the shared document is decision-page scope (single shared doc — correct).
- No `console.*`, no TODO/FIXME markers in the delta (`logger.debug` in the retry path is the sanctioned frontend logger).

---

## Findings

- **[LOW]** `shared/locale/types/parentLink/index.ts:99,104` · `shared/locale/en/parentLink/index.ts:32-33` · `shared/locale/ar/parentLink/index.ts:36-37` (+ `parentLink-namespace.parity.test.ts:94-95`) — `dashboardCardLoading` and `dashboardCardLoadError` are declared, translated in BOTH locales, and parity-pinned, but have ZERO runtime consumers: the card's skeleton branch renders `<CardShell busy>` (aria-busy with no accessible loading label) and the error Alert resolves through `resolveParentLinkDenialCopy(extractErrorCode(error), te)` — contract drift vs `1.1-outcome.md:120`, which specified 4.2 would consume both keys — suggested fix: either consume them (pass `aria-label={t.dashboardCardLoading}` through `CardShell`'s skeleton; fold `dashboardCardLoadError` in as the Alert fallback for unmapped denial codes) or delete both keys from type/en/ar + parity list and amend the 1.1 docblock (delete is the lower-risk direction: the denial-copy path is the security-sanctioned one).
- **[LOW]** `outcome/4.1-outcome.md` (BS §, L115-126) · `outcome/4.4-outcome.md` (BS §, L80-90) — six-cell screenshot sets are INCOMPLETE: `bs-drawer-*` has only 1440-en + 375-ar (missing 1440-ar, 768×1024 en/ar, 375-en) and `bs-nav-*` has only 1440-en + 375-ar (missing 4 cells incl. all 768×1024), while tasks.md:315 mandates six-cell sets and both BS verdicts are recorded PASS — suggested fix: capture the missing cells in the next browser mission, or append an explicit descope rationale (drawer/nav are pre-existing surfaces with route-only deltas) to both outcomes.
- **[INFO]** `outcome/4.3-outcome.md` (BS §, L79) — `4.3-both-cards-student.png` predates the final post-SSR-patch composition (documented in-file); the post-patch state IS evidenced by the six post-patch bs-dash BS cells — evidence chain acceptable, no action required.
- **[INFO]** `shared/locale/ar/parentLink/index.ts:28` — ar `dashboardCardCount` carries an unreachable `count === 0` branch ("لا توجد طلبات ربط قيد الانتظار"): the card renders `null` at zero actionable rows (REQ-015/052), and en has no matching 0 branch — harmless today, but the asymmetry would surface if the render contract ever changes — suggested fix: drop the 0-branch or mark it explicitly defensive in a comment.

---

## Worklog

- **Task ID:** 6.3 · **Agent:** review-frontend · **Date:** 2026-09-07
- Grep'd tasks.md for 6.3; read 4.1/4.2/4.3/4.4 outcomes (BF/BS sections) + all ten delta files + CardShell, NotificationDrawer wiring, parent-link.documents.ts, use-notification-mark-actions (confirmed `markAllNotificationsRead` resolves non-throwing → drawer `handleMarkAll` pending state can never stick).
- Ran verification scans: hex/rgb + console + TODO over the delta (all zero-match); fake-name sweep over `frontend/` (clean); route-constant single-source grep (3 consumers, 1 definition, zero literals in navItems.ts); locale-key consumer grep (surfaced F1); browser-evidence directory cross-check against every artifact named in the four outcomes (F2).
- READ-ONLY: zero file edits except this outcome; tasks.md untouched; no git operations.
- Verdict per check: 1 CLEAN · 2 CLEAN (F1 dead keys) · 3 CLEAN · 4 CLEAN · 5 two LOW gaps (F2) · 6 CLEAN · 7 F1 only. No blocking findings for 6.4/5.x; F1/F2 are cheap follow-ups.

---

## Fix round 1 (FIX-B)

Resolves this file's F1 ([LOW] dead i18n keys) and the ar `dashboardCardCount` 0-branch INFO. Cross-ref: wave 6.1 F1/F2 (same two issues, tracked once — see `6.1-outcome.md`).

**Fixes applied (consume, don't delete — the 1.1 copy contract REQ-015 mandates loading/error copy):**

1. `dashboardCardLoading` now consumed: `CardShell` gained an optional `busyLabel` prop (`frontend/views/students/dashboard/CardShell.tsx`) that renders `role="status"` + `aria-label={busyLabel}` on the busy frame; `PendingParentLinkRequestsCard`'s skeleton branch passes `busyLabel={t.dashboardCardLoading}` (`frontend/views/students/dashboard/PendingParentLinkRequestsCard.tsx` branch 1). `LoadingSkeleton`/`HandshakeCodeCard` unchanged (optional prop, zero behavior change).
2. `dashboardCardLoadError` now consumed: new `resolveParentLinkDenialCopyOrNull` (`frontend/lib/parent-link-denials.ts`) returns `null` for absent/unmapped codes; `resolveParentLinkDenialCopy` delegates to it (shared `internalServerError` fold preserved for `StudentLinkRequestsContainer` / `OutgoingSectionStates` / `HandshakeSendRequestAffordance`). The card's Alert renders `resolveParentLinkDenialCopyOrNull(extractErrorCode(error), te) ?? t.dashboardCardLoadError` — mapped denial classes keep their `errors`-namespace copy; unmapped/internal classes fold onto the card's own generic line. Zero hardcoded strings; raw wire messages still never reach the DOM.
3. ar 0-branch removed: `shared/locale/ar/parentLink/index.ts` `dashboardCardCount` dropped the unreachable `count === 0` branch (card renders `null` at zero actionable rows, REQ-015/052 branch 3) — en/ar plural structures now align (1 / 2 / 3–10 / 11+ preserved), with an in-file comment marking the contract.
4. `parentLink-namespace.parity.test.ts`: NO changes required — no template pin referenced the removed ar 0-branch (pins cover ar 1/2/3/12, en 1/3 only); `MANDATED_KEYS` stays 39, `FUNCTION_KEYS` stays 6, zero keys removed.
5. Card suite updated (`test/ui/components/students/PendingParentLinkRequestsCard.test.tsx`): cold-load cell now asserts the skeleton carries the localized `role="status"` accessible name (`getByRole("status", { name: t.dashboardCardLoading })` identity-pinned to the loading testid, both locales); the unmapped-error cell (RATE_LIMITED) now pins the fold onto `t.dashboardCardLoadError` with `te.internalServerError` asserted absent; NEW mapped-denial cell (`PARENT_LINK_REQUEST_NOT_FOUND`) pins that `errors`-namespace copy keeps flowing with no fallback fold — both locales.

**Evidence:**

- `scripts/health/sub-loop.ts --lifecycle duplicates` → exit 0 for all touched files: card component, card test, `ar/parentLink`, `en/parentLink`, `types/parentLink`, parity test, `parent-link-denials.ts`, `CardShell.tsx`.
- `bun run test/scripts/run-test.ts shared/locale/parentLink-namespace.parity.test.ts` → 68 pass / 0 fail (434 expect calls).
- Card suite → **23 pass / 0 fail** (21 baseline cells kept green + 2 new mapped-denial cells, 95 expect calls). Regression: `RoleDashboardPage.slot.test.tsx` → 7 pass / 0 fail.
- `bun run tsgo` → 0 errors.

## Worklog

- **Task ID:** FIX-B · **Agent:** Frontend Fix Subagent · **Date:** 2026-09-07
- Read the 6.1/6.3 findings + CardShell/LoadingSkeleton conventions + `parent-link-denials.ts` before editing; verified no test pinned the ar 0-branch copy (`لا توجد طلبات ربط قيد الانتظار` appears only in the ar leaf map + outcome docs) and no test pinned `dashboardCardCount(0)` anywhere.
- Edits: `CardShell.tsx` (+`busyLabel` prop), `PendingParentLinkRequestsCard.tsx` (busyLabel pass-through, Alert fallback, docblock branch table), `parent-link-denials.ts` (+`resolveParentLinkDenialCopyOrNull`, delegate), `ar/parentLink/index.ts` (0-branch removal), card test (aria-label assertion, unmapped-cell re-pin, mapped-denial cell). Zero git operations; no key deletions; no hardcoded copy.
- Verification runs recorded above (all green). Findings F1 (this file) + wave-6.1 F1/F2 closed; the incomplete-screenshot LOW (F2 in this file) is NOT in FIX-B scope and remains open.
