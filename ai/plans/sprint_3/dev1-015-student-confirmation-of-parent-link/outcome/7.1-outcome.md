# Task 7.1 Outcome — Canonical parent-link doc amendment (NO fork)

**Task ID:** 7.1 · **Plan:** DEV1-015 · **Date:** 2026-09-07 · **Verdict: PASS** — one section appended to the canonical doc; no fork, no spec-file edits.

## 1. What was done

Amended `docs/parents/parent-link-request.md` by inserting ONE new section, `## DEV1-015 Closure — Student Confirmation Discoverability (2026-09)`, between **Rollout** and **Related** (the doc's closing nav-block stays last). The doc's existing header/status/Rules/What-NOT-to-Do sections are untouched; nothing was forked and no new canonical doc was created.

## 2. Anchors recorded in the section

- **Dashboard discoverability card** — files (`frontend/views/students/dashboard/PendingParentLinkRequestsCard.tsx` + pure helper `pending-parent-link-requests.ts` + barrel `index.ts`; mounted beside `HandshakeCodeCard` in the dashboard student status slot), data path (`myIncomingParentLinkRequestsQueryDocument` → `useQuery` → `deriveActionableIncoming`), render states (skeleton in-flight / localized alert+retry on error / `null` at zero actionable / N ≥ 1 title+count+most-recent-requester+CTA), the six `parentLink` dashboard-card i18n keys (+ `Common` retry), and the CTA destination `/student/link-requests`.
- **Notification deep-link (CLOSE-GAP result)** — entity-type-keyed route resolution in the leaf module `frontend/lib/notification-route-resolution.ts`; `STUDENT_LINK_REQUESTS_ROUTE` has ONE definition site there; map keys are backend `NotificationType` enum members; `resolveNotificationRoute` falls unknown/absent pointers through to `/notifications` unchanged, never interpolates `relatedEntityId`; drawer body consumes the resolver, mark-read-then-close unchanged.
- **Nav retargeting: NO-OP** — the student nav entry already targeted the real `/student/link-requests` page (not the `[feature]` catch-all); the route literal was single-sourced onto the shared constant and the previously-absent nav pin tests added.
- **Verification stack added** — journey `test/workflows/parents/student-confirmation-of-link.journey.test.ts` (11 actor-attributed steps + true concurrent race cell gated to real PostgreSQL, deterministic twin pinned everywhere); service regression cells (governance denial ordered BEFORE the transaction opens with zero post-gate repository calls; committed double-respond idempotency with exactly ONE parent notification+publish); wire cells (expired-claim fold-first denial, foreign ≡ absent byte-identity, input-coercion tier).
- **INV-P1 closure statement (verified)** — no link without the student's explicit confirmation: guarded single writer remains the only production writer of `students.parent_id`; negative probes prove wrong-role/foreign/governed actors can never decide; race cells prove one winner per pair, loser commits nothing; 7-day window bound by the open-decisions record.
- **Bind-by-reference** — `docs/specs/state-machine-invariants.md` (INV-P1) and `docs/specs/open-decisions-and-gaps.md` (B.14) are linked, NOT edited or renumbered (verified: neither file touched).

## 3. Style compliance

Matches the doc's tone/format (bold lead-ins, path anchors, en-dash asides); no plan-artifact references (no `tasks.md`, no `REQ-xxx` — plain domain descriptions); section is 12 rendered lines, within the ≤ ~60-line budget.

## 4. Verification

- `bun run scripts/health/sub-loop.ts docs/parents/parent-link-request.md --lifecycle duplicates` → **N/A (markdown)**: the script is a TS toolchain (tsgo/oxlint/biome) — "No applicable instruction files found" and oxlint's "No files found to lint" exit 1 is the tool's no-lintable-file artifact, not a doc defect.
- Repo markdown validation (the only one present): `bun run scripts/validation/validate-mermaid.ts docs/parents/parent-link-request.md AGENTS.md frontend/AGENTS.md` → **exit 0**, 3 files, 0 diagrams.
- Spec files untouched: no edits to `docs/specs/state-machine-invariants.md` / `docs/specs/open-decisions-and-gaps.md`.

## 5. Files modified

| File | Change |
|---|---|
| `docs/parents/parent-link-request.md` | +1 section (14 lines incl. heading + separators), inserted after Rollout, before Related |

## 6. Checkboxes

- tasks.md `- [ ] 7.1` → `- [x] 7.1`.
