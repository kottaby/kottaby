# 3.2 Outcome — Frontend Documents Parity (verification-only, no edits)

**Verdict: PASS** — documents unchanged, both contract suites green, zero codegen drift. REQ-050, REQ-051.

## A. Document anchors (no new document)
- `frontend/graphql/sharedDocuments/parents/parent-link.documents.ts` (165 L, untouched):
  - `myIncomingParentLinkRequestsQueryDocument` L70–81 — `TypedDocumentNode<MyIncomingParentLinkRequestsQuery>`, named op `MyIncomingParentLinkRequests`, ZERO-argument, `id` selected FIRST, row fields exactly `{ id, status, parentFullName, createdAt, expiresAt, respondedAt }`.
  - `respondToParentLinkRequestMutationDocument` L124–138 — `TypedDocumentNode<RespondToParentLinkRequestMutation, RespondToParentLinkRequestMutationVariables>`, variables exactly `{ requestId: ID!, accept: Boolean! }`, returned row `id` FIRST (same canonical shape → writes back into the same normalized cache entry).
  - Outgoing row carries `studentMaskedName` (never the real name); incoming row carries `parentFullName` — mutually exclusive on the wire.
- No other incoming-list document exists: `rg "myIncomingParentLinkRequests" frontend/ --files-with-matches` → exactly 7 files, all accounted for: this definition, its test, generated `schema.graphql`/`gql/graphql.ts` (field artifacts only), and the 3 decision-view files (`StudentLinkRequestsContainer.tsx`, `IncomingBody.tsx`, `LinkRequestCard.tsx`). Zero duplicate query definitions.
- Task 4.2 (later) consumes this SAME list document — no new document required (container already imports it via the barrel, L7/L75).

## B. Suite runs (once each, real-PG env irrelevant — pure in-memory document tests)
| Suite | Result |
|---|---|
| `frontend/graphql/sharedDocuments/parents/parent-link.documents.test.ts` | **15 pass / 0 fail / 112 expect** — named ops, sanctioned variable set (zero identity args), id-first canonical rows, masked/full-name exclusivity, zero-argument lists, `respondedAt` selected, codegen `TypedDocumentNode` binding, barrel parity, null collapse, LinkStatus wire names |
| `frontend/graphql/sharedDocuments/documents.contract.test.ts` (located via Glob; NOT in parents/ — it is the generic structural lock at `sharedDocuments/`) | **20 pass / 0 fail / 124 expect** — id-where-normalized, barrel ≡ deep-import identity, TypedDocumentNode typing |

## C. Codegen zero drift (cited, not re-run)
3.1 outcome (§ codegen) + 3.1-PG: `generate:gqlSchema` + `bun codegen` **DIFFERENTIAL CLEAN** — `frontend/graphql/generated/schema.graphql` (21756 B) and `gql/graphql.ts` byte-identical to HEAD; the documents' generated operation types resolve with zero drift. No document file touched in 3.2, so the differential remains valid.

## Checklists
- **SEC:** incoming list selects exactly `{ id, status, parentFullName, createdAt, expiresAt, respondedAt }` — matches the 0.2 record (row 8) field-for-field; `parentFullName` is the sole sanctioned disclosure, zero-argument (identity server-side), and no student-private field (real name, ids) leaks parent-side (outgoing rows are masked-name only; exclusivity test green).
- **SR:** single normalized-cache truth — the one `myIncomingParentLinkRequestsQueryDocument` is consumed by the decision page (`StudentLinkRequestsContainer.tsx` L7 imports from `@/frontend/graphql/sharedDocuments` barrel, L75 `useQuery`; barrel-parity test proves same instance) and is the SAME document task 4.2's card will consume; no duplicate query definitions anywhere.
- **IV:** `.agents/instructions/frontend.instructions.md` present; `frontend/graphql/AGENTS.md` present (TypedDocumentNode typing, `id` fields for cache normalization, barrel export, no codegen re-run needed since documents unmodified) — no-edit state is compliant.
- **QL:** no-op — zero files touched (git status shows no `frontend/graphql/**` change; only prior tasks' expected files).

Diff: none (verification-only task).
