# Requirements & Specification: DEV1-014 — Parent-Child Link Request Workflow (7-Day Expiry)

> **Sprint:** 3 · **Owner Stream:** Dev 1 · **Effort:** 5 SP
> **Target ticket:** DEV1-014 — Parent-Child Link Request Workflow (7-Day Expiry) (`docs/planning/TICKETS.md` §Sprint 3)
> **Plan directory (verbatim — every header, ledger path, and self-reference in this document uses this exact string):** `ai/plans/sprint_3/dev1-014-parent-child-link-request-workflow-7-day`
> **Blocking dependencies (SHIPPED & verified in-tree):** DEV1-002 (registration: `registerUser` mints `students` row with unique `handshake_code` — `backend/services/auth/registration.service.ts:22-48`), DEV1-013 (handshake-code substrate: `StudentHandshakeService` + `findStudentByHandshakeCode` query — `backend/services/students/student-handshake.service.ts`, `backend/graphql/query/students/handshake-code.query.ts`), DEV3-010 (single-writer notification engine + inbox — `backend/services/notifications/notification-engine.service.ts:158-288`).
> **Verify-then-claim notes (HARD GATE — ruled against the bundled tree BEFORE authoring):**
> - **NO `parent_link_requests` table exists** — `backend/db/schema/parents/` contains only `parents.ts` (verified tree). This ticket legitimately ships the FIRST new table since the DEV1-001 baseline; the `linkStatus` pgEnum (`backend/db/schema/enums.ts:28` — `["pending","confirmed","rejected","expired"]`) and its TS mirror (`backend/enum/shared/link-status.enum.ts:1-6`) ALREADY EXIST unused — this ticket is their first consumer (zero enum drift; enum-object Pothos registration is NEW work).
> - **NO `isLinkStatus` guard exists** (`backend/enum/shared/link-status.enum.ts` carries the bare enum) — net-new additive guard, mirroring the `isApplicantStatus` precedent (`backend/enum/teachers/applicant-status.enum.ts:7-9`).
> - **NO parent-link service/repository/resolvers exist** — `backend/services/` has no `parents/` subdir in the bundle; `backend/db/repo/parents/` = only `parent.repository.ts`; GraphQL query/mutation trees contain no parent-link surface (verified `backend/graphql/{query,mutation}/**`). Everything here is CREATE — never framed as "extend existing".
> - **The journey harness IS scaffolded** — `test/workflows/AGENTS.md` (rules live) + `TrackedFixtures`/`provision*Actor`/`SpiedFanoutTransport` helpers exist (existence anchored by imports at `backend/services/notifications/realtime/fanout-transport.test.ts:12` and `backend/services/notifications/notification-engine.emit.test.ts:31`). Do NOT re-scaffold.
> - **Frozen schema-surface baselines are STALE** — `backend/graphql/test/schema-surface.test.ts:19-71` (`PRE_3_1_*`) and `backend/graphql/test/sdl-static-assertions.test.ts:12-28` (`FROZEN_*`) predate the shipped DEV3-016 admin surface (live `backend/graphql/mutation/admin/index.ts:1` = only `admin-users.mutation`). This ticket performs the documented **reconcile-then-extend** (precedent: DEV3-018 plan §3.3) — never a silent baseline edit.
> - **Frontend parent/student dashboards** (`frontend/views/parent/handshake/*`, `frontend/views/students/dashboard/*`, `app/(dashboard)/parent/handshake/page.tsx`) are PROSE-VERIFIED only (rollout table `docs/parents/handshake-code-discovery.md:213-216`) — NOT in the bundle. Every UI touchpoint on them is UPDATE-with-verify-at-implementation.
> - **`students.parentId` FK exists** (`backend/db/schema/students/students.ts:18` — `integer("parent_id").references(() => users.id, { onDelete: "set null" })`); the ticket Gherkin line "students.parent_id is set to the parent's user_id (pending)" **conflicts with INV-P1 + Workflow 04 §4.4 and is reconciled in REQ-022** (the FK is written ONLY on student confirmation).

---

## 1. Executive Summary & Problem Statement

- **Feature:** The sanctioned, transactional parent↔child linking flow that converts an out-of-band handshake code into a monitored relationship: a parent re-submits the child's code (`requestParentChildLink`), the student is notified and must explicitly confirm within 7 days (B.14), and ONLY that confirmation writes `students.parent_id` (INV-P1). The flow introduces the platform's first persistent link-request record (`parent_link_requests` — statuses from the pre-existing `link_status` enum: `pending → confirmed | rejected | expired`), with lazy expiry materialization, one-parent-per-student enforcement under concurrency (B.12), and per-recipient localized notifications (request → student; accepted/rejected → parent) through the single-writer `NotificationEngine` (publish-after-commit).
- **Problem from user perspective:**
  - **Parent (Fatima):** found her child by code on the handshake page (`docs/parents/handshake-code-discovery.md`); today the "you can send a link request" affordance is a dead end — no request state, no record, no notification, no way to track or withdraw it.
  - **Student (Yusuf):** must SEE who wants to link and explicitly accept or reject from his account (Workflow 04 §4.4) — before this ticket there is no surface and no event delivering that decision.
  - **Super Admin / Platform integrity:** a parent gaining monitoring WITHOUT student confirmation is the worst-case supervision breach (INV-P1); a second parent attaching to an already-linked student (B.12) is a data-integrity breach; both must be structurally impossible, not code-reviewed.
  - **Downstream consumers:** DEV1-016 (parent monitoring portal) reads `students.parent_id` — it needs THIS ticket's guarded write as its only provenance; DEV1-017 (parent session-completion notifications, INV-P3) resolves parents through the same column; the session-completion notification contract (`notifications.relatedEntityType`) gains its first `parent_link_request` emitter.
- **Business value:** Parent supervision is the M3 release gate's first deliverable (ROADMAP §M3; SPRINT_PLAN Sprint-3 DoD line "Link requests expire after 7 days"; FR-7.2). The link request is the trust gate that makes every later parent feature (portal, notifications) legitimate.
- **Actors involved:** **Parent** (requester/canceller), **Student** (confirmer/rejecter), **System** (expiry materialization, sibling expiry), **NotificationEngine** (single-writer emit substrate), downstream observers (DEV1-016/017). No admin actor — link requests are NOT admin actions (zero `audit_logs` rows by design).
- **Non-goals (explicitly OUT of scope):**
  - NO parent monitoring reads (sessions/reports/homework/progress) — DEV1-016 owns them; this ticket ships the link, not the portal.
  - NO parent session-completion notifications — DEV1-017 (INV-P3 substrate emits once the link exists).
  - NO link revocation/unlink surface (the `Unlinked` state of Workflow 04 §2 belongs to a future revoke ticket; the FK's `ON DELETE SET NULL` semantics stay untouched).
  - NO admin-side parent-link override — Workflow 04's resolved "Admin can override … set `students.parent_id` directly during direct student onboarding" is DEV3-019 territory (direct onboarding), NOT this ticket.
  - NO scheduler/cron expiry sweep — expiry is materialized lazily at interaction points and rendered computed-on-read (a periodic sweep is a recorded forward-reference, not open debt).
  - NO expiry or sibling-expiry NOTIFICATIONS (silent expiry by decision; documented — see REQ-024).
  - NO changes to discovery (`findStudentByHandshakeCode`/`myHandshakeCode`), registration, handshake generation, the `students` table shape, the `link_status` pgEnum inventory, or the notifications engine.
  - NO `audit_logs` writes anywhere in this flow (A.5's audit trail covers ADMIN actions; this is a user-to-user handshake — denial-no-audit and success-no-audit alike).
  - NO hard-delete of any request row in production code paths (rows are append-and-transition only).

---

## 2. Requirements & Acceptance Criteria (EARS Format)

### 2.1 Baseline & Foundational Preparation (MANDATORY)

- **REQ-001 (Pre-Implementation Baseline & Ledger):** WHEN implementation begins THEN the agent SHALL record baseline error counts (`bun tsgo`, `bun biome:check`, `bun run scripts/lint-service.ts --json --id baseline`) AND initialize `ai/plans/sprint_3/dev1-014-parent-child-link-request-workflow-7-day/deferred-items.md` from `.agents/spec-process-guide/templates/deferred-items-template.md` AND write `ai/plans/sprint_3/dev1-014-parent-child-link-request-workflow-7-day/outcome/0-baseline-outcome.md` capturing counts and the pre-existing modified-file set (`git diff --name-only`).
- **REQ-002 (Type-Safe i18n & Enum Value Imports Compliance):**
  - Client components MUST use `useAppTranslation(<NamespaceHandle>)` with `defineNamespace` handle consts (e.g. `ParentLink`, `HandshakeCode`, `Errors`) and property access (`t.confirmAction`) — never string literals, never a `Translation` enum (it does not exist in this tree; `shared/locale/client/use-app-translation.ts:1-12` is the verified contract), never `t('key')`.
  - Server components MUST use `getTranslations(locale)` (ONE argument, full `Translations` tree — `shared/locale/server.ts:12-14`) with property access.
  - Services MUST use `getServerTranslations(locale)` (ONE argument — `shared/locale/server-graphql.ts:2-4`); resolvers MUST use `ctx.t("<namespace>")`.
  - All enums used at runtime (`LinkStatus`, `NotificationType`, `UserRole`) MUST be VALUE imports and enum members — never raw string literals. FORBIDDEN: `next-intl`, `getBackendTranslations`, `shared/messages/`, hardcoded user-facing strings.
- **REQ-003 (Canonical Types Discipline):** All link-request types SHALL live in the NEW `backend/types/parents/parent-link-request.types.ts` (`ParentLinkRequestSelectType`, `ParentLinkRequestInsertType`, `OutgoingParentLinkRequestReturnType`, `IncomingParentLinkRequestReturnType`) and export through `backend/types/parents/index.ts` (bundle-verified pattern at `backend/types/parents/index.ts:1`). NO local types in Pothos/services; NO service-layer `.types.ts` files. Shared constants live in `shared/constants/` with ZERO `@/backend/**` imports.
- **REQ-004 (Reuse-Never-Fork Substrate Guard):** WHEN domain work starts THEN the agent SHALL verify (and NOT reimplement): `NotificationEngine.emitForUser`/`publishReceipts` caller-tx + publish-after-commit composition (`backend/services/notifications/notification-engine.service.ts:159-235`), the governance-collapse predicate `isGovernanceExcludedFromDiscovery` (`backend/services/students/student-handshake.helpers.ts:3-18`), the discovery read `StudentRepository.findDiscoveryByHandshakeCode` (`backend/db/repo/students/student.repository.ts:48-81`), the guarded-UPDATE precedent `StudentRepository.grantFreeTrialOnce` (`student.repository.ts:100-115`), recipient-locale resolution `UserRepository.findLocalesByIds` (`backend/db/repo/users/user.repository.ts:63-92`) + `defaultLocale = "ar"` (`shared/locale/AppLocale.ts:3`), the `withTransaction(outerTx, fn)` helper (`backend/lib/db/with-transaction` — import anchored at `backend/services/admin/user-management.service.ts:13`), the `ConflictError(code, message)` overload (`backend/lib/errors.ts:90-99`), and the journey harness (`test/workflows/AGENTS.md` + helpers). IF any artifact is missing THEN a ❌ ledger entry SHALL be recorded and dependent tasks SHALL block — never inline-patch a foreign layer.

### 2.2 Core Feature Logic / Happy Paths

- **REQ-010 (New Table — `parent_link_requests`, the only sanctioned schema addition):** WHEN the schema ships THEN `backend/db/schema/parents/parent-link-requests.ts` SHALL define `parentLinkRequests = pgTable("parent_link_requests", …)` with EXACTLY: `id` (identity PK), `parentId integer NOT NULL REFERENCES users(id) ON DELETE RESTRICT`, `studentId integer NOT NULL REFERENCES students(id) ON DELETE RESTRICT`, `status link_status NOT NULL DEFAULT 'pending'` (REUSING the existing `linkStatus` pgEnum from `backend/db/schema/enums.ts:28` — zero enum edits), `createdAt timestamp NOT NULL DEFAULT now()`, `expiresAt timestamp NOT NULL` (application-written `createdAt + 7 days`, NEVER default-computed), `respondedAt timestamp NULL`; indexes: `(parent_id)`, `(student_id)`, and a PARTIAL UNIQUE index `(parent_id, student_id) WHERE status = 'pending'` enforcing one live pending link per pair. Delivery SHALL be `bun run db push` per `docs/DATABASE_MIGRATIONS.md` (Drizzle-expressible; IF the partial-unique `.where(...)` proves unexpressible in the bundled Drizzle version, the fallback is ONE additive custom SQL file under `backend/db/migration/` + new drizzle folder — recorded in the ledger, never a silent workaround). NO other file under `backend/db/schema/**` may change except the one-line barrel addition in `backend/db/schema/parents/index.ts`.
- **REQ-011 (Request Creation — the R5 handshake re-submission):** WHEN an authenticated parent submits `requestParentChildLink(code)` THEN the service SHALL, in ONE `withTransaction`: (1) normalize (`normalizeHandshakeCode`) and validate (`isHandshakeCode`) the code PRE-DB (malformed → `ValidationError(handshakeCodeInvalid)`, key exists verified); (2) re-verify the ACTOR from a fresh read (`UserRepository.findById(parentActorId, tx)`) — missing row / non-parent role / governed actor (`isDeleted || isBlocked || suspended`) → denial BEFORE any write (service-layer re-check, closing the documented context-boundary governance window for THIS surface ONLY — `backend/graphql/gqlContextFactory.ts:91-104` applies no such filter); (3) re-resolve the target via `StudentRepository.findDiscoveryByHandshakeCode(normalized, tx)` (capability-by-code — the student id is NEVER accepted from the client, per R5.1/R5.2 of `docs/parents/handshake-code-discovery.md`); (4) apply `isGovernanceExcludedFromDiscovery(row, now)` to the target (R5.4); (5) reject target-linked (`parentId !== null` → conflict) and same-pair-duplicate-pending (pre-check AND the partial-unique race → `translateDbError`-class 23505 traversal → SAME conflict); (6) insert the row with `expiresAt = now + LINK_REQUEST_TTL_MS`; (7) emit ONE in-tx notification to the STUDENT (`NotificationType.ParentLinkRequest`, `relatedEntityType: "parent_link_request"`, `relatedEntityId: request.id`, copy composed in the RECIPIENT's persisted locale via `findLocalesByIds` with `defaultLocale` fallback, verbatim-stored per the engine's §3.3 emitter-localization rule). WHEN the tx commits THEN `publishReceipts` SHALL run strictly post-commit; a push failure SHALL degrade (`NOTIFICATION_DELIVERY_DEGRADED`) and NEVER fail the request.
- **REQ-012 (Miss/Governed Collapse — Nullable Return):** WHEN the code is well-formed but resolves to no row OR a governance-excluded row THEN `requestParentChildLink` SHALL return `null` — byte-equivalent to the discovery miss channel (`docs/parents/handshake-code-discovery.md` R2/R3), NEVER an error, with ZERO rows written and ZERO notifications emitted. The field SHALL be nullable by design; observer-perspective indistinguishability is test-locked.
- **REQ-013 (Already-Linked Target):** WHEN the resolved target's `parentId IS NOT NULL` THEN the service SHALL reject with `ConflictError("PARENT_LINK_TARGET_ALREADY_LINKED", <localized>)` — an honest, code-bearing denial (the discovery surface already discloses `linkable: false`, so no new oracle exists), with ZERO writes.
- **REQ-014 (Duplicate Pending Per Pair):** WHEN a pending request already exists for (parent, student) THEN creation SHALL reject with `ConflictError("PARENT_LINK_ALREADY_PENDING", <localized>)`; the SAME conflict SHALL result from a concurrent-creation race surfacing as the partial-unique 23505 (cause-chain traversal, never top-level error reading).
- **REQ-015 (7-Day Expiry Semantics — B.14, Lazy Materialization):** `expiresAt` SHALL be stamped `createdAt + 7 * 86_400_000` ms at creation (shared constant — REQ-003). A request is actionable IFF `status = 'pending' AND expiresAt > <one captured now per invocation>` (strict-`>` parity with the applicant-cooldown predicate, `backend/services/teachers/applicant-lifecycle.service.ts` precedent). List/read paths SHALL render an expired-pending row as expired WITHOUT writing (read purity); WRITE paths (respond/cancel) SHALL materialize expiry via a guarded `UPDATE … SET status='expired' WHERE id=? AND status='pending'` inside the same tx BEFORE answering the denial (`PARENT_LINK_REQUEST_EXPIRED` on respond; cancel-of-expired answers per REQ-018). A periodic sweep is NOT shipped (ledger forward-reference to the cron stream, resolved-pointer).
- **REQ-016 (Student Confirmation — the INV-P1 write):** WHEN the owning student confirms (`respondToParentLinkRequest(requestId, true)`) THEN inside ONE tx the service SHALL: (1) re-verify the ACTOR (fresh read; student role; ungoverned — same fail-closed service re-check); (2) atomically claim the REQUEST via ONE guarded update `… SET status='confirmed', responded_at=now WHERE id=? AND student_id=<ctx.user.id> AND status='pending' AND expires_at > now RETURNING` — the ownership, liveness, and expiry predicates ride the UPDATE itself (NO read-then-write); (3) on zero rows, run the classifier: expired-pending → materialize `expired` + `ConflictError("PARENT_LINK_REQUEST_EXPIRED", …)`; resolved → `ConflictError("PARENT_LINK_REQUEST_ALREADY_RESOLVED", …)`; nonexistent OR foreign-owned → `NotFoundError("PARENT_LINK_REQUEST", …)` (constant shape — foreign ≡ nonexistent, mirroring the `NOTIFICATION_NOT_FOUND` oracle rule at `notification-engine.service.ts:254-276`); (4) only after a successful claim, write the LINK via ONE guarded `UPDATE students SET parent_id=<parentId from the claimed row> WHERE id=<studentId> AND parent_id IS NULL RETURNING` (NEW `StudentRepository.linkParentIfUnlinked`) — zero rows → `ConflictError("PARENT_LINK_TARGET_ALREADY_LINKED", …)` and the WHOLE tx rolls back (the request claim is NOT left committed — ghost confirmations are impossible); (5) expire ALL sibling pending requests for that student (`SET status='expired' … WHERE student_id=? AND status='pending' AND id <> <winner>`) in the same tx; (6) emit ONE in-tx `parent_link_request` notification to the PARENT (accepted copy, student's name, recipient locale); publish post-commit.
- **REQ-017 (Student Rejection):** WHEN the owning student rejects (`accept: false`) THEN the SAME guarded-claim pattern SHALL flip the request to `rejected` (+ `responded_at`), write NOTHING to `students.parent_id`, emit ONE in-tx rejection notification to the parent (rejected copy), and NEVER expire siblings (a rejection of one parent leaves other pending requests live until their own expiry — children choose parents, not first-come).
- **REQ-018 (Parent Cancellation):** WHEN a parent cancels (`cancelParentLinkRequest(requestId)`) THEN ONE guarded update `… WHERE id=? AND parent_id=<ctx.user.id> AND status='pending'` SHALL apply; zero rows → classifier produce `..._ALREADY_RESOLVED` (already answered), `..._EXPIRED` (stale — materialized), or `NotFoundError` for foreign/nonexistent — with NO write to `students`, NO notification (a withdrawn pending request is silent), and the cancelled row remaining permanently as request history. CANCELLATION MAPS TO `status='rejected'`? NO — `link_status` has no `cancelled` member and the enum is frozen; a cancelled request SHALL be recorded as **`rejected`** with a documented semantic note (act of withdrawal), and the audit-free history row persists. IF product later wants a distinct vocabulary, that is a schema-enum change owned by a future ticket, never patched here.
- **REQ-019 (Sibling Expiry on Confirmation):** WHEN a confirmation commits THEN every OTHER pending request targeting the same student SHALL transition to `expired` inside the same transaction (their opportunity window closed); NO notification SHALL be emitted to those sibling parents (silent — REQ-024).
- **REQ-020 (Self-Scoped Reads):** WHEN a parent calls `myOutgoingParentLinkRequests` (role-gated Parent) THEN the system SHALL return their requests newest-first (capped at 50, no pagination surface) with per-row computed display-status (expired-pending renders expired); WHEN a student calls `myIncomingParentLinkRequests` (role-gated Student) THEN the incoming rows SHALL carry the parent's FULL name (sanctioned disclosure — the confirmation decision requires knowing who asks, Workflow 04 §4.4) while the outgoing rows SHALL carry only the student's MASKED name via the existing `maskFullName` helper (`shared/lib/mask-full-name.ts` — referenced by `student-handshake.service.ts:7`; verification at implementation) — never raw student PII expansion on the parent surface.
- **REQ-021 (INV-P1 Structural Lock — `students.parent_id` Has Exactly One Writer):** WHEN this ticket ships THEN `linkParentIfUnlinked` (the guarded confirm-time writer) SHALL be the ONLY write path that sets `students.parent_id` in the entire codebase outside `entity-setup`/seed layers, proven by a static scan test (grep-lock over `backend/`, allowing only the new repo method + test-janitorial paths); a soft-deleted parent's link reopening via `ON DELETE SET NULL` remains untouched.
- **REQ-022 (Ticket-Prose Reconciliation — Recorded, Not Silent):** The ticket's Gherkin line "students.parent_id is set to the parent's user_id (pending)" conflicts with INV-P1 ("A parent cannot monitor a student without the student's explicit confirmation" — `docs/specs/state-machine-invariants.md` §7.2) and Workflow 04 §4.4. The BINDING resolution shipped here: `parent_id` is written ONLY on confirmation (REQ-016); the pending phase lives exclusively in `parent_link_requests`. This reconciliation SHALL be recorded in the canonical doc (REQ-080) and needs NO edits to the invariant file numbering.
- **REQ-023 (Notification Semantics — Engine Choreography):** ALL `notifications` rows SHALL be written ONLY inside `NotificationEngine.emitForUser` (single-writer rule, `docs/notifications/realtime-engine.md` §4) inside the caller's tx; copy SHALL pass verbatim (emitter-localized in the RECIPIENT's locale — engine doc §3.3 + DEV3-018's D6 pattern); `relatedEntityType: "parent_link_request"` + the request id SHALL be both present (engine co-presence validation, `emit-validation.ts:32-49`); publish SHALL occur only via `publishReceipts` after commit on own-commit paths; idempotency keys (when the forward-looking emitter chooses one) SHALL follow the engine's claim contract — this mutation family is OUT of the mandated `docs/IDEMPOTENCY.md` key set (natural guards: partial unique index + guarded state UPDATEs; the DEV3-016 idempotency ruling pattern applies verbatim).
- **REQ-024 (Silent Expiry & Silent Sibling Expiry — Documented):** WHEN a request expires OR a sibling is expired by another request's confirmation THEN ZERO notifications SHALL be emitted for it. The parent learns via the outgoing list's computed-expired rendering. (Forward item: a cron-stream reminder/expire notification — ledger pointer only.)

### 2.3 Security, Authorization & Tenancy

- **REQ-030 (BFLA — Role-Gated `$all` Conjunction On Every Operation):** EVERY new operation SHALL carry `authScopes: { $all: { authenticated: true, role: [<exactly one role>] } }` — `Parent` for request/cancel/outgoing-list; `Student` for respond/incoming-list (proven `$all` conjunction pattern: `backend/graphql/query/students/handshake-code.query.ts:9-15`; plain-map ANY-semantics hazard documented at `docs/teachers/applicant-lifecycle.md` §3). Anonymous → pre-resolver `UNAUTHORIZED`; wrong role → pre-resolver localized `FORBIDDEN` (`backend/graphql/pothos/builder.ts:40-51`). `PUBLIC_OPERATIONS` (`backend/lib/gateway/public-operations.ts:1-13`) SHALL remain the byte-identical frozen six.
- **REQ-031 (Service-Layer Actor Re-Check — Governance Fail-Closed Per Surface):** WHEN any mutation service method runs THEN the FIRST action SHALL be a fresh-read actor re-check: missing row → `UnauthorizedError`; wrong role → `ForbiddenError`; governed (`isDeleted || isBlocked || suspended`) → `ForbiddenError` with the existing constant governance denial copy — with ZERO writes and ZERO notifications (JR-C-1 parity: denials leave no side-effect rows). This closes the documented context-factory governance window (`gqlContextFactory.ts:91-104`) for THIS surface only and records the divergence honestly (same precedent as DEV3-018's strict re-check).
- **REQ-032 (BOLA / Capability-By-Code + Constant-Shape Id Denials):** The link-request flow SHALL address students EXCLUSIVELY by re-submitted handshake code (R5); `respondToParentLinkRequest`/`cancelParentLinkRequest` SHALL fold ownership into the guarded UPDATE predicate (foreign/nonexistent ≡ zero rows ≡ `NotFoundError` with a constant localized message — the `markReadOnce` precedent at `backend/db/repo/notifications/notification.repository.ts:128-139`); NO resolver argument may carry `parentId`/`studentId`/`userId` — smuggled identity fields die as `GRAPHQL_VALIDATION_FAILED` before any resolver runs.
- **REQ-033 (BOPLA — Field-By-Field Assembly Only):** All DB payloads SHALL be built field-by-field (the insert sets exactly `{ parentId, studentId, status: LinkStatus.Pending, expiresAt }`); NO `{ ...input }` spread SHALL appear anywhere in the new write paths; input closure is enforced at the GraphQL input types (request input = exactly `{ code: String! }`); the notification emit input is assembled explicitly (engine BOPLA pin at `notification-engine.emit.test.ts` "smuggled extra input fields are ignored" precedent).
- **REQ-034 (Oracle Hygiene Matrix — The Four Channels):** (1) code miss ≡ governed target → SAME `null` payload (REQ-012); (2) already-linked → honest `PARENT_LINK_TARGET_ALREADY_LINKED` (the `linkable` bit already disclosed it); (3) foreign vs nonexistent request id → identical NOT_FOUND shape; (4) malformed code → `VALIDATION` pre-DB with ZERO reads. Timing parity is asserted where materially meaningful (miss vs governed-run the same query path).
- **REQ-035 (PII & Log Hygiene):** `logger.logDomainError` contexts carry `{ code, entity: "parent_link_requests" | "students" | "users", entityId?, locale }` ONLY — NEVER codes, NEVER names, NEVER emails; the submitted handshake code is NEVER logged at any layer (carries the handshake surface's R8 discipline forward); `console.*` is forbidden; the happy path emits NOTHING to the domain log.
- **REQ-036 (Rate Limiting — Unchanged Stub Posture):** The fail-open platform limiter posture (`docs/parents/handshake-code-discovery.md` R6) SHALL remain untouched — NO bespoke limiter ships in this ticket; brute-force posture rests on the 32-bit code space + role gating + the null-miss channel.
- **REQ-037 (No LIKE/ILIKE Anywhere):** All reads are parameterized equality (`handshake_code = $1`, PK/owner predicates). `escapeLikeWildcards` EXISTS in-tree (anchored at `backend/services/admin/user-management.service.ts:12`) but is deliberately NOT needed — zero text-search surface; the absence of any `ilike(` in the new modules SHALL be scan-pinned.

### 2.4 Atomicity, Concurrency & Data Integrity

- **REQ-040 (Transaction Boundaries — Exact Membership):** request-create tx = { fresh actor read, code resolution, insert, notification insert } + post-commit publish; confirm tx = { actor re-check, guarded request claim, guarded students link write, sibling expiry, notification insert } + post-commit publish; reject/cancel tx = { actor re-check, guarded flip, notification insert (reject only) } + post-commit publish (reject only). EVERY repository/service call inside a tx SHALL receive THAT tx (no `tx`/`db` mixing).
- **REQ-041 (Guarded-Update-Only Rule — No TOCTOU):** EVERY state transition (`pending → confirmed | rejected`, claim-with-ownership-and-expiry, `students.parent_id` link write, sibling expiry, expiry materialization) SHALL be a SINGLE guarded `UPDATE … WHERE <predicates> RETURNING` statement; SELECT-then-UPDATE is PROHIBITED; the zero-row class is resolved by the documented classifier probes (existence vs status vs ownership).
- **REQ-042 (Concurrent Confirmation Race — Exactly One Winner):** WHEN two parents' pending requests for the SAME student are confirmed concurrently (two distinct student-actor confirmations are impossible — the student confirms twice OR two raced claims interleave), THEN at most ONE confirmation SHALL commit a `students.parent_id` write; the loser SHALL surface `PARENT_LINK_TARGET_ALREADY_LINKED` (from the zero-row guarded link write) with its ENTIRE tx rolled back (no claim, no notification row, no sibling expiry); final state: exactly ONE confirmed request, exactly ONE linked parent, all other pendings expired, exactly ONE notification per direction.
- **REQ-043 (Concurrent Duplicate-Create Race):** WHEN the same parent submits the same code twice concurrently THEN the partial unique index SHALL admit exactly ONE row; the loser SHALL translate the 23505 cause-chain into `PARENT_LINK_ALREADY_PENDING`; proven via `Promise.allSettled` in the chaos tier (skip-gated under pglite via `isPgliteProvider`, `test/helpers/skip-when-pglite.ts:1-5`).
- **REQ-044 (Deterministic Expiry Boundary):** WHEN an interaction lands exactly ON `expiresAt` THEN the request is EXPIRED (strict `expiresAt > now` liveness predicate), materialization is idempotent (double-expire is a zero-row no-op), and all evaluations in one call share ONE captured `now`.
- **REQ-045 (Additive-Only Schema Drift Gate):** WHEN the ticket completes THEN `git diff backend/db/schema/**` SHALL contain EXACTLY the new table file + the one-line parents barrel edit — NO edits to `enums.ts`, `students.ts`, `users.ts`, or any existing table (the `linkStatus` pgEnum is reused untouched). NO custom migration file is shipped unless REQ-010's fallback fires (ledger-recorded).
- **REQ-046 (FK-Safe Teardown Order):** Journey/teardown deletion SHALL order `parent_link_requests` BEFORE `students`/`users` (the requests' RESTRICT FKs would otherwise block cascades); the journey's `TrackedFixtures` registration order SHALL encode this (reverse-registration deletion per `test/workflows/AGENTS.md` rule 2).

### 2.5 Validation & Error Contracts

- **REQ-050 (DomainError Taxonomy — Closed Code Set):** New codes: `PARENT_LINK_TARGET_ALREADY_LINKED`, `PARENT_LINK_ALREADY_PENDING`, `PARENT_LINK_REQUEST_EXPIRED`, `PARENT_LINK_REQUEST_ALREADY_RESOLVED` (via the verified `ConflictError(code, message)` overload — `backend/lib/errors.ts:90-99`), `PARENT_LINK_REQUEST_NOT_FOUND` (via `NotFoundError("PARENT_LINK_REQUEST", …)` entity-form — `backend/lib/errors.ts:14-17`), plus reused `VALIDATION`/`UNAUTHORIZED`/`FORBIDDEN`. `SCREAMING_SNAKE_CASE` per `docs/graphql/domain-error-extensions-code.md`; plain `new Error(...)` in the flow is PROHIBITED.
- **REQ-051 (i18n Key Increments — Errors Namespace):** NEW FLAT keys on `ErrorsLabels` (`shared/locale/types/errors/index.ts` — FLAT per the `handshakeCodeInvalid` precedent at line 48; NOT a new nested group) implemented in BOTH locales: `parentLinkTargetAlreadyLinked`, `parentLinkAlreadyPending`, `parentLinkRequestExpired`, `parentLinkRequestAlreadyResolved`, `parentLinkRequestNotFound`. Machine code ↔ key naming SHALL hold (camelCase mirror). The errors parity suite (`shared/locale/errors-namespace.parity.test.ts`) SHALL stay green unchanged in shape.
- **REQ-052 (Notification Copy Slots — `notifications` Namespace Extension):** SIX new flat non-`type*` slots SHALL be added to `NotificationsLabels` (types/en/ar triple): `eventParentLinkRequestTitle/Body`, `eventParentLinkAcceptedTitle/Body`, `eventParentLinkRejectedTitle/Body` — the three bodies FUNCTIONS of the counterpart name (e.g. `(parentName: string) => string`), mirroring the plural/function-slot precedent (`unreadCount`, `markAllResult`). The parity suite's MANDATED_KEYS list (`shared/locale/notifications-namespace.parity.test.ts:8-35`) SHALL be extended IN THE SAME change set (current 26 baseline → 32); its "exactly seven `type*` keys" pin SHALL remain green UNCHANGED.
- **REQ-053 (Deterministic Denial Envelope):** EVERY denial SHALL answer with exactly one DomainError, a stable `extensions.code`, ZERO writes across ALL tables (`parent_link_requests`, `students`, `notifications`), and exactly ONE `logDomainError` line per the REQ-035 context bounds.
- **REQ-054 (Happy-Path Silence):** WHEN any happy path runs (create, confirm, reject, cancel, lists) THEN ZERO domain-log emissions SHALL occur; only engine-internal degradation warns are permitted on real push failures.

### 2.6 GraphQL & Frontend Contracts

- **REQ-060 (Exact GraphQL Surface):** The schema SHALL gain EXACTLY:
  ```graphql
  extend type Query {
    myOutgoingParentLinkRequests: [OutgoingParentLinkRequest!]!   # $all { authenticated, role: [Parent] }
    myIncomingParentLinkRequests: [IncomingParentLinkRequest!]!   # $all { authenticated, role: [Student] }
  }
  extend type Mutation {
    requestParentChildLink(code: String!): OutgoingParentLinkRequest      # NULLABLE — REQ-012 collapse
    respondToParentLinkRequest(requestId: ID!, accept: Boolean!): IncomingParentLinkRequest!
    cancelParentLinkRequest(requestId: ID!): OutgoingParentLinkRequest!
  }
  enum LinkStatus { Pending Confirmed Rejected Expired }
  type OutgoingParentLinkRequest { id: ID!, status: LinkStatus!, studentMaskedName: String!, createdAt: DateTime!, expiresAt: DateTime!, respondedAt: DateTime }
  type IncomingParentLinkRequest { id: ID!, status: LinkStatus!, parentFullName: String!, createdAt: DateTime!, expiresAt: DateTime!, respondedAt: DateTime }
  ```
  `id` SHALL be the FIRST exposed field on both object types (Apollo normalization); timestamp fields SHALL use the registered `DateTime` scalar (`backend/graphql/pothos/shared/scalar.pothos.ts:1-4`; builder Scalars slot `backend/graphql/pothos/builder.ts:16-18`) — `toISOString()`-into-`String` hand-serialization is PROHIBITED for all NEW fields (the pre-scalar legacy pattern in older modules is out of scope).
- **REQ-061 (Registration & Codegen Discipline):** `LinkStatusPothosEnum` SHALL be registered ONCE, enum-object form, in `backend/graphql/pothos/shared/enum.pothos.ts` (bundle-verified registry); resolvers SHALL live in `backend/graphql/{query,mutation}/parents/parent-link.{query,mutation}.ts` (NEW subtree + barrels wiring into `query/index.ts` / `mutation/index.ts`); Pothos objects in `backend/graphql/pothos/parents/parent-link-request.pothos.ts` (NEW); `bun run generate:gqlSchema && bun codegen` SHALL run and commit generated artifacts in the SAME change set; the STALE frozen baselines (`schema-surface.test.ts`, `sdl-static-assertions.test.ts`) SHALL first be RE-ANCHORED to the current live surface (reconciliation step, recorded in the outcome) and THEN extended with this ticket's surface — the two-step, documented sequence (DEV3-018 precedent), never a silent edit.
- **REQ-062 (Resolver Discipline):** Resolvers SHALL be thin: guard `ctx.user` (localized `UnauthorizedError` via `ctx.t("errorsTranslations")` — pattern anchored at `backend/graphql/mutation/notifications/notification.mutation.ts:31-34`), parse `requestId` via the canonical positive-safe-int ID parser pattern (`notification.mutation.ts:7-18` — `ID!` wire string → strict `/^[1-9]\d*$/` + `isPositiveSafeInt` from `emit-validation.ts:7-9`, invalid → `VALIDATION` pre-DB), delegate, NO try/catch, NO local types.
- **REQ-063 (Frontend Documents & Cache):** NEW `frontend/graphql/sharedDocuments/parents/parent-link.documents.ts` (+ `parents/index.ts` barrel + one line on the top barrel `frontend/graphql/sharedDocuments/index.ts:1-6`): five documents, named operations, `TypedDocumentNode<…>` per operation, `id` FIRST in every object selection, `useQuery`-only rule intact (NO `useLazyQuery`), a sibling documents contract test mirroring `frontend/graphql/sharedDocuments/notifications/notification.documents.test.ts`. Apollo cache: BOTH new object types carry real `id`s → normalized — the frozen cache-policy inventory (`frontend/providers/apollo/apolloCache.test.ts:90-99`) SHALL stay GREEN UNTOUCHED (no `keyFields:false` addition needed).
- **REQ-064 (Routes, Guards & Navigation):** NEW page `app/(dashboard)/student/link-requests/page.tsx` — Server Component guarded by `withPageAuth({ roles: [UserRole.Student], redirectTo: "/student/link-requests" })` (signature verified at `frontend/lib/auth/withPageAuth.ts:15-30`); wrong-role fallback SHALL route via `roleDashboardPath(ctx.role)` (`frontend/lib/auth/roleDashboardRoute.ts:9-22`) — bare `/dashboard` is FORBIDDEN as a target. The parent's outgoing-requests section SHALL land on the EXISTING handshake page container (`frontend/views/parent/handshake/HandshakeDiscoveryContainer.tsx` — PROSE-verified, UPDATE-with-verify). `frontend/views/dashboard/navItems.ts` SHALL gain ONE student item `{ route: "/student/link-requests", labelKey: "linkRequests", Icon: LinkChildIcon }` (student array at `navItems.ts:35-42`) with ONE new `linkRequests` label on `DashboardLabels` (en+ar) — the nav ownership matrix test (`navItems.test.ts:19-29`) SHALL stay green (the key is owned by Dashboard ONLY).
- **REQ-065 (MUI v9 / Accessibility / RTL):** ALL view code SHALL use `sx`-only styling with `theme.palette.*` tokens (no direct style props, no hardcoded colors), `*Outlined` icons, `focusVisibleRingSx` on interactive elements, ≥44px touch targets on confirm/reject/cancel/send CTAs, `Box component="output" aria-busy` for pending regions, skeleton/empty/error/permission-denied states (reusing `PermissionDeniedFallback`, `RetryableNotice`), full `ar` RTL rendering, `dir="auto"` on free-text names, and `React.SubmitEvent`/`React.SyntheticEvent` discipline.
- **REQ-066 (New UI Namespace — Full Registration):** A NEW `parentLink` namespace SHALL register end-to-end per the `shared/AGENTS.md` checklist: `shared/locale/types/parentLink/index.ts` (`ParentLinkLabels`), `shared/locale/{en,ar}/parentLink/index.ts`, `shared/locale/namespaces/parentLink/parentLink.namespace.ts` (`export const ParentLink = defineNamespace<ParentLinkLabels>("parentLink.parentLink", t => t.parentLinkTranslations)`), registry entry in `shared/locale/namespaces/index.ts`, `parentLinkTranslations` on `Translations` (`shared/locale/types/message.ts:12-24`) + both `messages.ts` bundles, and the parity suite `shared/locale/parentLink-namespace.parity.test.ts` (key-set identity, non-empty values, Arabic-script presence in `ar`, function-slot parity, registry/bundle pins — mirroring `notifications-namespace.parity.test.ts`). Copy covers: student inbox (confirm/reject CTAs, expiry line via function slot, identity line), parent outgoing list (status chips incl. computed-expired, cancel CTA, empty state), send flow CTA + success/denied feedback, page titles.

### 2.7 Test Coverage

- **REQ-070 (Repository Tier — 100% Branch Target):** NEW repo tests (`runInRollback`, `tx` propagated everywhere, `expectRepoError` try/catch — NEVER `rejects.toThrow`; fixtures only via `entity-setup.ts` helpers, signatures verified first) SHALL cover every branch of every new repo method: create/guarded claims (all zero-row classifier arms), sibling expiry, scoped cancel, `linkParentIfUnlinked` winner/loser arms, partial-unique index behavior, deterministic ordering, and the computed-expiry display path.
- **REQ-071 (Service 4-Tier Suite):** `backend/services/parents/parent-link-request.service.test.ts` SHALL run the full matrix: request (happy, collapse-null paths, already-linked, duplicate-pending, governed actor/target, malformed code pre-DB), confirm (ownership, expiry-boundary strict-`>`, double-claim, link-write race loser, sibling expiry content, notification rows + recipient-locale copy + zero publish on caller-tx), reject, cancel (all three classifier arms), reads (computed-expired rendering, newest-first, self-scope containment, no foreign-row bleed), zero-write/zero-audit/zero-notification count probes on EVERY denial, and repo-failure unmasked propagation — all inside `runInRollback`.
- **REQ-072 (Chaos & Concurrency):** `Promise.allSettled` proofs (skip under pglite): duplicate-create race → ONE row + ONE conflict; two-parent confirm race → ONE winner (single linked parent, single confirmed request, all others expired, single parent-facing notification); confirm-during-expiry-instant determinism; forced post-claim failure → ZERO residual rows across `parent_link_requests`/`students`/`notifications` (full rollback proof).
- **REQ-073 (GraphQL Wire Matrix):** `setupTestServerLifecycle` + `testClient` (+ raw `fetch` where byte-shape matters) SHALL assert: anonymous → `UNAUTHORIZED` on all five ops; wrong roles → `FORBIDDEN` (parent↔student cross-probes BOTH directions); wire≡oracle payload equality; governed-caller denial at the SERVICE layer (REQ-031); `id`-first selections; `requestId` fuzz (`"0"`, `"-1"`, `"1.5"`, `"abc"`, oversized) → `VALIDATION`; smuggled identity args (`studentId`, `parentId`, `userId`) → `GRAPHQL_VALIDATION_FAILED` pre-resolver; nullable collapse documented for `requestParentChildLink`.
- **REQ-074 (Static Locks):** Scan tests SHALL pin: (a) `students.parent_id` single-writer rule (REQ-021) across `backend/**`; (b) zero `ilike(`/LIKE construction in the new modules (REQ-037); (c) zero `auditLogs` writes in the new modules; (d) zero `console.*`; (e) `NotificationEngine.emitForUser` is the only `notifications` writer reached by the new code (single-writer rule).
- **REQ-075 (i18n Parity):** `parentLink-namespace.parity.test.ts` GREEN; `notifications-namespace.parity.test.ts` extended MANDATED_KEYS (26→32) GREEN; errors parity green; Arabic-script pins; function-slot pins for the three body functions; dashboard `linkRequests` key parity (the nav-ownership suite stays green).
- **REQ-076 (Cross-Actor Journey — MANDATORY, TEST-FIRST):** `test/workflows/parents/parent-link-request.journey.test.ts` SHALL be authored BEFORE the service surface exists, per `test/workflows/AGENTS.md` + `docs/testing/workflow-journey-tests.md`: committed fixtures in ONE `beforeAll` transaction, `TrackedFixtures` teardown honoring REQ-046's FK order with zero-residue re-probes, REAL actors via the `provision*Actor` factories, NO `runInRollback`, `SpiedFanoutTransport` at the injection seam, denials through REAL role/governance resolution (never monkey-patched), unique `jrn_plink_<uuid8>` prefixing. The §2.9 journeys map 1:1 onto it.
- **REQ-077 (Component Tier):** Happy DOM component tests (sanctioned runner `bun run test:ui:components`) SHALL cover, for the student incoming view AND the parent outgoing section, EVERY status branch (pending live / pending expired-rendered / confirmed / rejected / cancelled-as-rejected), CTA in-flight disable, deny-surface wiring through the existing error-link rows, empty states, and RTL rendering — assertions ONLY through translation handles (never hardcoded en/ar strings); the harness files (`TestWrapper`, translation preload) are PROSE-referenced (`test/ui/AGENTS.md`) and SHALL be verified at implementation before use.
- **REQ-078 (Baseline & Sub-Loop Gates):** `bun tsgo` / `bun biome:check` / lint-service counts SHALL equal the REQ-001 baseline with ZERO new errors; EVERY created/modified file SHALL pass `bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates` exit 0; the final gate `grep -c "❌\|⚠️" ai/plans/sprint_3/dev1-014-parent-child-link-request-workflow-7-day/deferred-items.md` SHALL equal 0 (forward items — cron expiry sweep, expiry notifications — recorded as resolved-pointer entries with owners, never open debt).

### 2.8 Documentation & Knowledge Gates

- **REQ-080 (Canonical Doc):** WHEN implementation completes THEN `docs/parents/parent-link-request.md` SHALL exist (house structure: Why / Pattern / Rules / What NOT to Do / Rollout / Related) covering: the `parent_link_requests` model + exact state machine, the R5 re-submission choreography, INV-P1 single-writer proof, the REQ-022 ticket-prose reconciliation, expiry semantics (strict-`>`, lazy materialization, silent expiry decision), sibling-expiry semantics, notification choreography (recipients, locale-at-emitter, related-entity, publish-after-commit), the error/oracle matrix, and the consumer contract for DEV1-016 (`parent_id` is monitoring-access truth) and DEV1-017.
- **REQ-081 (Surgical Doc Crosslinks — No Renumbering):** `docs/parents/handshake-code-discovery.md` R5 SHALL gain a one-line "shipped in DEV1-014 → see `docs/parents/parent-link-request.md`" pointer (its rules stay); `docs/workflows/04-parent-supervision-handshake.md` SHALL NOT be edited except OPTIONALLY one pointer line; `docs/specs/state-machine-invariants.md` and `docs/specs/open-decisions-and-gaps.md` SHALL NOT be renumbered or edited (this ticket implements B.12/B.14/INV-P1 — it mints no new invariant).
- **REQ-082 (AGENTS Propagation):** `backend/services/AGENTS.md` SHALL gain the one-line parent-link service rule (single-writer `parent_id` under student confirmation; single-writer notifications via the engine); `backend/db/repo/AGENTS.md` SHALL gain the `ParentLinkRequestRepository` entry; root `AGENTS.md` Important References SHALL gain the canonical doc line; NO `shared/AGENTS.md` edits (the checklist is the reference, not a changelog).
- **REQ-083 (Outcome & Gate Protocol):** EVERY task SHALL read all prior `ai/plans/sprint_3/dev1-014-parent-child-link-request-workflow-7-day/outcome/` files first, write `outcome/<task-id>-outcome.md` after, tick checkboxes, and the Phase-1.5 `@plan-review` gate (`outcome/plan-review-R1.md`) SHALL pass with zero violations BEFORE implementation begins.

### 2.9 Cross-Actor Workflow Scenarios (Journeys)

**Journey file (1:1):** `test/workflows/parents/parent-link-request.journey.test.ts` — TEST-FIRST.

#### Actor Table

| Actor | Role / Provisioning | Can Do | Cannot Do |
|---|---|---|---|
| Parent A | `role=parent` + `parents` row (`provisionParentActor`) | request/cancel own links; watch own outgoing list | confirm (student role); touch others' requests |
| Parent B | same | SAME as A (the second-parent contention probe) | link to an already-linked student (conflict) |
| Student S | `role=student` + `students` row (`provisionStudentActor`) | confirm/reject incoming; watch own incoming list | request links (parent-only); self-link is structurally unreachable |
| Governed student G | student + `suspended=true` | — | be targeted (collapses to null at request time) |
| Already-linked student L | student + `parentId = Parent A` | — | receive new requests (conflict) |
| Anonymous / cross-role callers | — | — | reach ANY of the five ops (401/403 pre-resolver) |

#### Ordered Step List

- **Journey A — Request → Notify → Confirm → Linked:**
  1. **System** → commit cast (Parents A/B, Students S/G/L with L pre-linked to A) + baseline row-count oracles.
  2. **Parent A** → `requestParentChildLink(S.code)` → ONE `parent_link_requests(S, A, pending, expiresAt=+7d)` row; ONE `notifications` row for S (`parent_link_request`, `relatedEntityId = request.id`, copy in S's locale carrying A's name); spied transport = EXACTLY ONE post-commit publish addressed to S only.
  3. **Student S (observer)** → `myIncomingParentLinkRequests` → sees the request with A's FULL name; **Parent B (observer)** → own outgoing list EMPTY (foreign-inbox invariance); the govened/oracle probes run here (code miss ≡ governed target → `null`, byte-shaped identical).
  4. **Student S** → `respondToParentLinkRequest(id, true)` → request `confirmed` (+`respondedAt`); `students[S].parentId = A.id`; ONE accepted-notification to A (A's locale, S's name); ONE publish to A.
  5. **Parent A (observer)** → outgoing list shows `confirmed`; **Admin-independent check** → ZERO `audit_logs` rows for the whole flow; ZERO side effects on G/L/B rows (byte-identical oracles).
- **Journey B — Reject & Cancel:**
  6. **Parent A** → request to (fresh) student → pending; **Student** → `respond(false)` → `rejected`; ONE rejected-notification to A; `students.parentId` stays NULL; A re-request CAN create a fresh pending row (re-application after rejection is legitimate).
  7. **Parent A** → cancel a live pending (fresh target) → row flips (`rejected` per REQ-018), ZERO notifications, target untouched; cancel of THAT row again → `..._ALREADY_RESOLVED`; cancel by Parent B (foreign id) → `PARENT_LINK_REQUEST_NOT_FOUND` (constant shape); cancel of a NONEXISTENT id → identical shape.
- **Journey C — Expiry & Contention (B.12/B.14):**
  8. **System** → Parent B's pending request to a fresh target with `expiresAt` forced into the past (committed fixture backdated) → **Student** confirm attempt → `PARENT_LINK_REQUEST_EXPIRED` AND the row materially flips `pending→expired` AND `students.parentId` stays NULL AND ZERO notifications.
  9. **Race (guarded, pglite-skipped):** two parents' live pendings to ONE student; the sequential second-confirm AFTER a committed first-confirm → `PARENT_LINK_TARGET_ALREADY_LINKED`; sibling pendings of the winner's student are ALL `expired`; exactly ONE linked parent; ZERO duplicate rows anywhere.
  10. **System teardown** → tracked deletes in REQ-046 order; re-probes = 0 residue.

#### Cross-Actor EARS Criteria (observer-perspective)

- **REQ-090:** WHEN Parent A sends a link request THEN the system SHALL persist ONE pending request row with a +7-day expiry AND Student S SHALL observe the request in his incoming surface AND exactly one localized notification addressed to S — and NO other cast member SHALL observe any state change.
- **REQ-091:** WHEN Student S confirms THEN `students[S].parent_id` SHALL become A's id AND Parent A SHALL observe `confirmed` + one accepted-notification AND every other pending request for S SHALL render/expire terminal — observed by BOTH parents' lists.
- **REQ-092:** IF a parent targets an already-linked student THEN the system SHALL reject with `PARENT_LINK_TARGET_ALREADY_LINKED` AND the student SHALL observe ZERO new incoming rows AND ZERO notifications SHALL exist about the attempt.
- **REQ-093:** IF any non-owner acts on a request (student confirming another student's request; parent cancelling another parent's request) OR references a nonexistent id THEN the denial SHALL be the constant `PARENT_LINK_REQUEST_NOT_FOUND` shape with ZERO writes observed by every actor.
- **REQ-094:** WHEN a pending request outlives its `expiresAt` THEN NO notification SHALL fire AND the next interaction SHALL deny with `PARENT_LINK_REQUEST_EXPIRED` AND the row SHALL persist as `expired` (never deleted) AND the target student's link field SHALL remain NULL.
- **REQ-095:** WHEN the same parent re-requests the same student while a pending row exists THEN the system SHALL answer `PARENT_LINK_ALREADY_PENDING` AND the student's incoming list SHALL still show exactly ONE pending row.
- **REQ-096:** IF the target student is governed (suspended/blocked/deleted) OR the code has no match THEN the parent's request answer SHALL be the SAME `null` (no oracle) AND zero rows SHALL exist afterwards.

---

## 3. System Decisions & State Machine Invariants Alignment

### Decision References (`docs/specs/open-decisions-and-gaps.md`)

| Decision | Alignment |
|---|---|
| **A.2 / B.12 (`students.parent_id` single FK — one parent per student)** | REQ-016's guarded `parent_id IS NULL` write enforces it under concurrency at the DB row level — not by pre-check. |
| **A.3 (unique `handshake_code`)** | The capability key: requests re-submit the code (REQ-011), never a stored/transmitted student id (R5). |
| **A.4 (notifications table)** | Request/accepted/rejected events ride the engine as `parent_link_request` rows with `relatedEntityId` = request id. |
| **A.7 (governance on `users`)** | Actor/target governance re-checks (REQ-011 step 2/4, REQ-031) read only — fail-closed per surface; the context-window divergence is documented, never claimed closed globally. |
| **B.13 (parent links multiple children)** | Pair-scoped partial-unique index + per-student gating (NEVER per-parent) — one parent freely links N children sequentially. |
| **B.14 (7-day expiry)** | REQ-015 implements it: stamped `expiresAt`, strict-`>` liveness, lazy + honest materialization, silent expiry (REQ-024). |
| **C.1 (parent role)** | Parent-gated operations ride the first-class role through the `$all` scope. |
| **`docs/IDEMPOTENCY.md`** | Out-of-key-set ruling (DEV3-016 pattern): natural guards (partial unique index + guarded transitions) ARE the replay protection; NO mandatory `X-Idempotency-Key` contract here. |

### State Machine & Lifecycle Invariants (`docs/specs/state-machine-invariants.md`)

| Invariant | How this ticket serves it |
|---|---|
| **INV-P1** (no monitoring without explicit student confirmation) | THE structural deliverable: `students.parent_id` has exactly ONE writer — the guarded confirm-time update (REQ-016/021, scan-locked). |
| **INV-P2** (MVP parent = read-only) | Unbroken: parents gain NO write surface over student data — their two mutations touch only their OWN request rows. |
| **INV-P3** (parent notified on child session completion) | Referenced only: binding requires `parent_id` — which this ticket now writes; the emitter itself is DEV1-017. |
| **INV-P4** (link model = `parent_id` FK + handshake code) | Both halves now have their flows: generation/discovery (shipped) + request/confirm (this ticket). |
| **INV-U1/U4/U5** | Zero deletes (request rows transition, never vanish); soft-delete semantics of parent/student accounts unchanged; `ON DELETE SET NULL` on `students.parent_id` untouched. |

### Canonical Workflows & Standards

- **Workflow 04 (`docs/workflows/04-parent-supervision-handshake.md`)** — THE owner: this ticket implements §4.3 (Link Request) + §4.4 (Student Confirmation) against its §2 state machine (`Link_Requested → Linked | Rejected | Expired`; the `Cancelled-by-parent` fold into `rejected` is the REQ-018 recorded rule).
- **Discovery forward contract (R1–R8, `docs/parents/handshake-code-discovery.md`)** — R5's four obligations are implemented verbatim by REQ-011.
- **Engine contract (`docs/notifications/realtime-engine.md`)** — single-writer, caller-tx receipts, publish-after-commit, emitter-localized copy (§3.1–§3.3) consumed unchanged.
- **Standards:** `docs/DATABASE_MIGRATIONS.md` (db push; `reset`/`cleanGenerate` disabled), `docs/graphql/error-handling-contract.md` + `docs/graphql/domain-error-extensions-code.md` (taxonomy + masking), `docs/IDEMPOTENCY.md` (out-of-set ruling), `docs/graphql/api-gateway-and-routing.md` (registration + default-deny allowlist untouched), `docs/testing/workflow-journey-tests.md` (journey discipline), `docs/graphql/dataloader-batching.md` (N/A — root-list reads only, no per-parent resolvers).

---

## 4. Cross-Layer Traceability Matrix

| Requirement ID | Decision Ref / Invariant | Backend Service | GraphQL Mutation/Query | Frontend View | Test Coverage |
|---|---|---|---|---|---|
| REQ-001..004 | Process canon; reuse guard | verify-only pass (engine, handshake helpers, repos, harness) | — | — | `outcome/0-baseline-outcome.md`; Phase-1.5 gate |
| REQ-010 / REQ-045 / REQ-046 | A.2; B.14; migration standards | `parent_link_requests` table (NEW) + partial unique index | — | — | repo tier; drift-gate diff pin; teardown-order journey proof |
| REQ-011..014 / REQ-032..034 | R5 contract; A.3; B.12; oracle matrix | `ParentLinkRequestService.requestLink` (NEW) | `requestParentChildLink` (nullable) | parent handshake page "send" affordance (verify-at-impl) | service 4-tier + wire matrix (REQ-073) + REQ-090/092/095/096 |
| REQ-012 / REQ-034 | R2/R3 collapse (discovery doc) | collapse branch | nullable payload contract | parent "not found" state | journey probe + wire-shape equality pin |
| REQ-015 / REQ-044 | B.14 | expiry predicate + lazy materialization | `expiresAt`/`status` fields | computed-expired chips | boundary tests (past/now/future) + determinism |
| REQ-016 / REQ-019 / REQ-021 | INV-P1; B.12; A.2 | confirm pipeline (guard claim → guarded link write → sibling expiry → notify parent) | `respondToParentLinkRequest` | student incoming page confirm CTA | chaos race (REQ-042/072); single-writer scan (REQ-074); REQ-091 |
| REQ-017 / REQ-018 | Workflow 04 §2; recorded cancel-as-rejected | reject/cancel flows | `respond…(accept:false)`; `cancelParentLinkRequest` | reject + withdraw CTAs | classifier-arm tests (REQ-071); foreign/nonexistent constant shape (REQ-093) |
| REQ-020 | R1 payload closure; Workflow 04 §4.4 sanctioned parent-name disclosure | list assembly (masked vs full counterpart naming) | both list queries | both lists | ordering/scope/containment tests; REQ-090/091 observer assertions |
| REQ-022 | INV-P1 vs ticket prose | — (reconciliation record) | — | — | canonical doc section (REQ-080) |
| REQ-023 / REQ-024 | A.4; engine §3.1–3.3 | in-tx `emitForUser` + post-commit `publishReceipts` | — | inbox/drawer (existing substrate) | spy-count + recipient-set assertions (REQ-076); silent-expiry pin |
| REQ-030 / REQ-031 | BFLA; documented governance-window divergence | `$all` scopes + fresh-read actor re-check | all five ops | server guard `withPageAuth` | 401/403 matrix + governed-denial cases (wire + service) |
| REQ-033 / REQ-037 | BOPLA; no-LIKE posture | field-by-field builders | input closure | — | smuggle probes + scans (REQ-073/074) |
| REQ-035 / REQ-053 / REQ-054 | logging discipline | `logDomainError` bounds | — | — | log-spy tests; happy-path silence |
| REQ-040..043 | integrity & concurrency | `withTransaction` + guarded updates only | — | — | chaos tier + forced-rollback proof |
| REQ-050..052 | error taxonomy docs | Conflict/NotFound overloads; new error keys; notification copy slots | `extensions.code` pins | localized denial rendering | code↔key parity suites (REQ-075); matrix code assertions |
| REQ-060..062 | surface contract; DateTime scalar rule | Pothos objects/enum/registration | the five ops + `LinkStatus` enum | documents | schema-surface + sdl-static reconcile-then-extend (REQ-061); codegen same-commit |
| REQ-063..066 | frontend platform rules | — | documents (5) | student page + parent section + nav item + `parentLink` namespace | documents contract test; cache-inventory pin stays green; component tier; parity suites |
| REQ-070..078 | testing canon | full suite matrix | wire tier + static locks | component tier | runners: `test:db`, `test:services`, `test:graphql`, `test:ui:components`, `bun run test/scripts/run-test.ts test/workflows/parents/parent-link-request.journey.test.ts` |
| REQ-080..083 | knowledge protocol | — | — | — | `docs/parents/parent-link-request.md`; handshake-doc R5 pointer; AGENTS lines; ledger gate = 0 ❌/⚠️ |
| REQ-090..096 (journeys) | INV-P1; B.12; B.13; B.14; R5 | real services, committed fixtures, honest roles | — | — | `test/workflows/parents/parent-link-request.journey.test.ts` (TEST-FIRST, REQ-076) |

---

**End of Specification — DEV1-014.** Governing next step: Phase 1.5 — invoke `@plan-review` on the complete plan (`specs.md` + `plan.md` + `tasks.md`) before any implementation begins.
