# Task 5.2 Outcome — Chaos & Concurrency Suite

## Artifact
- `backend/services/parents/parent-link-request.chaos.test.ts` (794 ln, 10 cells) — authored by general-purpose dispatch (died pre-gates on infra timeout), gated + bookkept by orchestrator-direct pass.
- Location: siblings the 2.3 service suite in backend/services/parents/ (repo convention).
- Skip-gate: `isPgliteProvider() ? describe.skip : describe` via `@/test/helpers/skip-when-pglite` (real-postgres-only races).

## Cells (10 pass / 0 fail vs real postgres; run via approved runner)
| # | Cell | Verdict |
|---|---|---|
| 1 | duplicate-create race, same pair → ONE committed row, loser PARENT_LINK_ALREADY_PENDING (raw 23505 never leaks) | PASS |
| 2 | concurrent double-respond → one claim wins, loser ALREADY_RESOLVED, one terminal state + one notification | PASS |
| 3 | cross-actor racer → FOREIGN student loses with constant NOT_FOUND shape, emits nothing | PASS |
| 4 | cancel-vs-respond race → deterministic single outcome, no ghosts, no orphan notifications | PASS |
| 5 | two-parent confirm race → ONE students.parent_id winner, loser commits NOTHING, sibling pendings expired | PASS |
| 6 | respond at expiresAt === now → deterministic EXPIRED (strict >) | PASS |
| 7 | expiry race +1ms (live side) → exactly ONE terminal state | PASS |
| 8 | expiry race −1ms (dead side) → claim collapses, sweep materializes EXPIRED | PASS |
| 9 | linkParentIfUnlinked single-writer → ONE row, other arm null | PASS |
| 10 | forced post-claim failure → own-commit unit rolls back, zero residual rows | PASS |

## Gates
- bun tsgo full: exit 0. QL sub-loop duplicates: exit 0. Regression: parent-link.repository.test.ts 31/0 (no drift). Wire suite unaffected (backend-only additive file; 22/0 standing from 5.1).
- SEC: every cell asserts zero cross-actor leakage (winner-takes-one-row; loser state byte-untouched; foreign actors emit nothing).
- SR: assertions probe post-race DB state exhaustively — row counts + content + zombie/ghost sweeps, not just error codes.
- IV: tests.instructions.md conventions (entity-setup fixtures, rollback discipline where required, expectRepoError pattern) — file follows the 2.3 suite's established patterns.

## Drift
- tasks.md §5.2 prose matched with zero path drift (file placed where service tests actually live, per 2.3 precedent).

## Carry-forwards
- 5.3 (static locks): chaos suite adds no new writers/queries patterns outside scanned conventions — expect clean.
- 5.4 (journey re-run): unchanged 19/19 expected; test:db 226 + service 271 baselines hold (chaos +10).
- D8 (browser QA) remains the only open ledger item in scope for Phase 4.
