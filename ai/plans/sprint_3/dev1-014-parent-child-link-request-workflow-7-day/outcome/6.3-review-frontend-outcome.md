# Outcome — Task 6.3: review-frontend wave (pages, components, documents, i18n)

- **Task ID:** 6.3 (Phase 6 post-implementation review, DEV1-014)
- **Status:** ✅ **PASS with minor findings** — 0 blocker / 0 major / **4 minor / 3 nit**; every REQ-relevant frontend invariant verified LIVE at HEAD `687bea2`
- **Agent:** general-purpose subagent (read-only review — no source edits, no commits)
- **Branch:** `feat/dev1-014-parent-child-link-request-workflow-7-day` @ `687bea2` (re-asserted before every batch — sandbox watchdog reset to `main` between batches repeatedly; untracked outcome files survive, tracked edits do not)
- **Baseline inputs:** worklog entries 4.1+4.4 / 4.2+4.3 / 5.4; `outcome/4.1-4.4-frontend-wiring-outcome.md` §Carry-forwards; `outcome/4.2-4.3-frontend-views-outcome.md` §Carry-forwards + §SR/IV verdicts

## Method

Every listed file read end to end; each invariant in tasks.md §6.3 + dispatch brief checked against the live tree (not the audit prose). en AND ar locale trees grepped for every key the components reference. Adversarial angle: audit claims from 4.1/4.2/4.3 were treated as claims, not facts — two of them did not survive verification (F1, F3).

## Findings

| # | Severity | File:Line | Finding | Evidence | Recommended fix |
|---|----------|-----------|---------|----------|-----------------|
| F1 | **minor** | `frontend/views/parent/handshake/index.ts:1-4` | Handshake barrel omits the two exports the 4.2+4.3 audits claim were added: `./OutgoingSectionStates` + `./OutgoingSectionStates.parts`. Both the outcome artifacts table ("Barrel + OutgoingSectionStates + OutgoingSectionStates.parts — IncomingStates-in-barrel precedent") and the worklog ("both added to the handshake barrel") are contradicted by the commit: `git show d836fc4 -- frontend/views/parent/handshake/index.ts` adds only 3 lines (CancelDialog, Card, Section). The student domain barrel DOES export its states/card/dialog files (`frontend/views/students/link-requests/index.ts:1-5`) → cross-domain precedent inconsistency + claim/artifact mismatch. Zero functional impact (the section imports both files via deep paths — `OutgoingLinkRequestsSection.tsx:17-23`). | barrel diff above; students barrel 5 exports | Add the two alphabetical export lines (precedent parity), or correct the 4.2/4.3 outcome + worklog prose in a bookkeeping pass |
| F2 | **minor** | `frontend/views/students/link-requests/StudentLinkRequestsContainer.tsx:134-140`; `frontend/views/parent/handshake/OutgoingLinkRequestsSection.tsx:126-132` | Post-success `await refetch()` sits INSIDE the mutation try-block: a refetch failure after a COMMITTED respond/cancel is caught by the mutation-denial handler → dialog closes, success toast suppressed, `denialCode` set → `resolveParentLinkDenialCopy` folds the unmapped refetch error to `internalServerError` → the user sees "An internal server error occurred." while the row's chip already shows Confirmed/Rejected (mutation payload normalized write-back). First response is misleading; self-healing on retry (lands on `PARENT_LINK_REQUEST_ALREADY_RESOLVED` copy). Neither 4.2's 22 tests nor 4.3's 20 tests cover the refetch-throws-after-success interleave — the class the §4.3 chaos wave missed at component tier. | `try { await respond(…); await refetch(); setSuccessToast(…) } catch { setDenialCode(extractErrorCode(…)) }` (both call sites, same shape) | Split the refetch into its own catch (success toast + cache state stand; surface a soft "refresh failed" affordance), or fire-and-forget with a non-denial failure surface |
| F3 | **minor (doc)** | `ai/plans/…/tasks.md:440`, `:443-444`, `:535` | navItems path drift is STILL LIVE in tasks.md — §4.4 prose cites `frontend/views/dashboard/navItems.ts` (`:35-42`, import `:8`); the 4.4.QL/TE commands (:443, :444) and §6.3 scope (:535) repeat the stale path. Real file: `frontend/views/dashboard/nav/navItems.ts` (student array `:103-110`, `LinkChildIcon` import `:11`). Adversarial catch: the 4.1 outcome (drift note 1, line 81) asserts "the live tasks.md line was already corrected to `:103-109`/`:11` by the 0.3 plan-review fixes" — **FALSE at HEAD**; no such correction exists in the live file. | `rg "navItems.*:35-42" tasks.md` → :440; live navItems.ts layout | Docs-only correction (4.4 prose + commands + 6.3 scope) in the next bookkeeping/6.5 pass; note the 4.1 outcome's "already corrected" sentence is itself inaccurate |
| F4 | **minor (doc/hygiene)** | `frontend/graphql/sharedDocuments/AGENTS.md:22-23` | The `parents/` Layout entry lists SEVEN stale artifact names (`parent-directory, parent-documents, parent-dashboard, parent-profile, parent-billing-history, parent-onboarding, parent-portal`) and omits the shipped `parent-link.documents.ts` + contract test; the actual directory is exactly `index.ts`, `parent-link.documents.ts`, `parent-link.documents.test.ts`. The doc-refresh candidate recorded by 4.1 → carried through 4.2/4.3 → 5.4 is confirmed **STILL OPEN** at `687bea2`. | `ls frontend/graphql/sharedDocuments/parents/` vs AGENTS.md §Layout | Docs-only dispatch: rewrite the `parents/` entry to the parent-link documents pair (+ co-located test) |
| F5 | nit | `frontend/lib/parent-link-denials.ts:21-40` | Mutation-level `UNAUTHORIZED`/`FORBIDDEN` (session dies between opening the dialog and submitting) fold to `internalServerError` — an auth problem is reported as an internal-error claim, while query-level auth errors get the dedicated `PermissionDeniedFallback`. Transient, self-healing (the next list query's 401 renders the fallback), constant-shape discipline intact. | `DENIAL_LABEL_ACCESSORS` has no UNAUTHORIZED/FORBIDDEN rows; both containers' query branch handles them separately | Optional: map the two auth codes to `errors.forbidden` copy (or render the fallback) for mutation denials |
| F6 | nit | `frontend/views/parent/handshake/OutgoingSectionStates.parts.tsx:37` | `OutgoingSkeletonList` is exported but has ZERO external consumers (only sibling `OutgoingUnsettledBody` renders it) — inconsistent with the module's own discipline where `OutgoingLoadErrorAlert` is module-private for exactly this reason (4.2 worklog: "isRetryableOutgoingCode made module-private… grep-proven: zero external consumers"). Harmless public-surface noise. | `rg "OutgoingSkeletonList"` → definition + 1 internal use only | Make module-private |
| F7 | nit | `frontend/views/parent/handshake/HandshakeDiscoveryContainer.tsx:222-231, 328-345` | The send success Snackbar lives INSIDE `SendRequestAffordance`, which unmounts on any code edit / discovery retry / outcome reset → success copy vanishes mid-toast. The student container hosts its toast at container level (survives branch churn) — parent send flow is the odd one out. Cosmetic (the invalidations posture arguably covers it). | render gate `resultState.kind === "found" && resultState.linkable` wraps the affordance incl. its Snackbar | Optional: hoist the Snackbar to the container like the student/cancel toasts |

**No blocker or major findings.** Wave 6.3 does not gate 6.5 on any code change; F1–F4 are one-line-or-prose follow-ups.

## Verified invariants (coverage statement — all checked against the live tree)

1. **Student page guard:** `withPageAuth({ roles: [UserRole.Student], redirectTo: "/student/link-requests" })` is the ONLY authorization boundary (`page.tsx:36`); wrong-role redirect goes through `roleDashboardPath(ctx.role)` (`withPageAuth.ts:95` → `roleDashboardRoute.ts:52`) — bare `/dashboard` is never a browser redirect target; anonymous → `/login?redirect=…`. `getTranslations(locale)` is single-arg synchronous (`shared/locale/server.ts:15`).
2. **Data flow discipline:** containers use `useQuery`/`useMutation` from `@apollo/client/react` ONLY; zero real `useLazyQuery` (all mentions are comment-only bans, incl. `HandshakeDiscoveryContainer` self-description); mutation→list refresh = plain `refetch()` / sanctioned `refetchQueries` (send CTA), no cache surgery anywhere.
3. **Per-row cards + dialogs (student):** Confirm/Reject CTAs only on `isLinkRequestActionable` rows, `minHeight: 44` + `focusVisibleRingSx` + in-flight disable (row-level AND global); decision dialog is form-bearing (`Box component="form"` + `React.SubmitEvent` — zero `FormEvent` usage in scope), submit button `loading`+`disabled` (LoadingButton posture), `aria-labelledby`/`aria-describedby`, `autoFocus`, body `dir="auto"` for the interpolated name.
4. **Expired-state READ-PURE (REQ-015):** computed chip via the SHARED `frontend/lib/parent-link-request-status.ts`; client liveness `expiresAt.getTime() > nowMs` is exact parity with the backend fold `status===Pending && expiresAt <= now → Expired` (`parent-link-request.helpers.ts:415`, same for `mapOutgoing`); `nowMs` captured once at mount (lazy initializer); ZERO client writes.
5. **Masked/full-name display rules (REQ-020):** student rows render `parentFullName` (`dir="auto"`, the sanctioned disclosure); parent rows render `studentMaskedName` ONLY; the five documents field-separate the two names and the contract test pins each side's exact row — the student's real name never crosses the wire to a parent.
6. **Handshake UPDATE-with-verify integrity:** discovery flow intact — client canonical gate (`normalizeHandshakeCode`+`isHandshakeCode`) → `skipToken` until validated; `network-only` for point-in-time lookups; unchanged-code resubmit forces `refetch`; edit clears `validatedCode`+`sendOutcome`; `deriveHandshakeResultState` untouched (errors outrank data). Send affordance gated on `found && linkable` ONLY. REQ-012 null-collapse: `payload === null` → INFO `sendUnavailableNotice` (code miss ≡ governed target — no oracle). `PARENT_LINK_ALREADY_PENDING`/`_TARGET_ALREADY_LINKED` (+ expired/resolved/not-found on respond/cancel) map through the constant-shape `parent-link-denials.ts` to localized inline Alerts; unmapped codes fold to `internalServerError` (raw code never reaches the DOM).
7. **Outgoing section:** Cancel CTA only on live-pending (`actionable`) rows; status chip computed from the SAME `parentLinkStatusChipSpec`/`displayLinkRequestStatus` pair as the student side (one status can never render two palettes); expiry line via `expiresLine` function slot; withdrawal fold → `rejected` chip after refetch; cancel dialog is form-bearing with ≥44px targets and in-flight disable.
8. **Documents (`frontend/graphql/sharedDocuments/parents/`):** `id` FIRST in all five object selections; zero identity variables — only `code` / `requestId`+`accept` / `requestId`; `TypedDocumentNode` typed from the single generated `graphql.ts`; `requestParentChildLink` is the ONLY nullable payload (REQ-012) with respond/cancel non-null; root barrel `index.ts` alphabetical (post-5.4 biome normalize) and barrel ≡ deep-import identity test-locked (15/0 suite re-run green in 5.4).
9. **i18n (en AND ar verified):** every referenced key exists in BOTH trees — `parentLink` ×30 (en/ar leaf consts typed `ParentLinkLabels`), `errors` ×6 (`internalServerError` + 5 `parentLink*` denials, `errors/index.ts:52-56` both locales), `common` ×2 (`retry`, `cancel`), `dashboard.linkRequests` (`en/ar/dashboard/index.ts:19`), `handshakeCode` ×4 (pageTitle/pageDescription/notFound pair). Compile-time type + `parentLink-namespace.parity.test.ts` runtime belt + no-English-fallthrough ar sweep. Zero hardcoded strings in scoped files (scan clean: no `console.*`, no `next-intl`/`useTranslations`, no literal copy in JSX). `dir` is server-side on `<html>` (`app/layout.tsx:42`) + `dir="auto"` on interpolated names.
10. **Styling discipline:** `sx`-only (zero `style={…}` props in scope); zero hardcoded colors (no hex/rgb hits — all colors via `theme.palette` callbacks or Chip `color` roles); `*Outlined` icons only (`LinkOutlined`, `SendOutlined` + pre-existing aliased set `SearchOutlined`/`PersonSearchOutlined`/`LinkOffOutlined`/`SearchOffOutlined`); `focusVisibleRingSx` on every interactive element; `minHeight:{xs:44}` touch-floor on compact-size retry buttons matches the `RetryableNotice` audit-R3 precedent; `component="output"`+`aria-busy` sanctioned by `frontend/AGENTS.md:44`.
11. **Hygiene candidates:** `apolloCache` frozen-list — **RESOLVED** (test now pins "six documented entries" incl. `NotificationListPage`; the 4.1-era 9/1 is closed upstream, suite 10/10 in 5.4); `sharedDocuments/AGENTS.md` doc-refresh — **STILL OPEN** (F4); navItems drift — **STILL OPEN** (F3).
12. **Accessibility:** semantic `h1`/`h2` headings; `output` regions with `aria-busy` for skeleton + settled lists; `aria-hidden` decorative empty-state icon; MUI Alert/Snackbar announce defaults relied on (no literal `role` props, per AGENTS rule); dialog focus trap + labelled + described + submit-focused; `aria-label` on settled list regions.

## Verdict

**PASS.** The shipped frontend at `687bea2` honors every DEV1-014 frontend invariant (REQ-012, REQ-015, REQ-020, REQ-035/037 hygiene, REQ-063/064/065) with no blocker and no major gap. 4 minor + 3 nit findings recorded; F1 and F3 are audit-claim corrections the 4.x waves missed, F4 confirms the standing docs-only carry-forward, F2 is the one code-quality follow-up worth scheduling (non-gating). No tasks.md changes made; no source files touched.
