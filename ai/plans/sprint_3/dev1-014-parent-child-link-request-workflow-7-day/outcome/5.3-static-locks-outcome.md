# Outcome — Task 5.3: Static locks (single-writer, no-LIKE, no-audit, no-console, single-notifications-writer)

- **Task ID:** 5.3
- **Status:** ✅ COMPLETE — static-locks suite 36/36 GREEN (78 expects); QL sub-loop exit 0; bun tsgo full exit 0; regressions green
- **Agent:** general-purpose subagent
- **Date:** 2026-09-02

## Artifact

| File | Kind | Notes |
|---|---|---|
| `backend/services/parents/parent-link.static-locks.test.ts` | CREATE (~820 ln) | Pure-filesystem scan tier — ZERO server, ZERO DB; `handshake-code-immutability-scan` precedent (SourceFile corpus, deterministic `localeCompare` traversal, corpus guards, per-scanner negative fixtures) |

## Lock × verdict table

| Lock | Scanner(s) | Verdict | Evidence |
|---|---|---|---|
| (a) students.parent_id single-writer | 4 scanners (Drizzle `.set` non-null incl. shorthand; `.insert(students)` 400-char values window; raw `SET parent_id =`; raw `INSERT INTO students`) + guarded-writer pin + sanctioned-caller scan | **PASS** | union collapses to EXACTLY `backend/db/repo/students/student.repository.ts` (linkParentIfUnlinked `.set({ parentId, … })` + its docblock-quoted SQL — both allowlisted); allowed-filtered set = `[]`; `linkParentIfUnlinked` referenced in production ONLY by the repo (definition) + the service (caller); guarded predicate `isNull(students.parentId)` + `.returning()` pinned |
| (b) zero-LIKE | `ilike(`/`like(` call-form over backend parents modules + parent-link frontend trees + lib helpers | **PASS** | 0 matches across all three corpora; negative fixtures prove `ilike(`/`LIKE(` fire while `alike(`/`ilikex(`/`eq(` spare |
| (c) zero auditLogs writes | `insert(auditLogs)` call-form; raw `insert into audit_logs`; audit-module imports / `AuditService`/`AuditLogRepository` identifiers | **PASS** | 0 matches in the new backend modules; docblock MENTIONS of `audit_logs` (the modules' own zero-audit documentation) do not fire — write-shape scanning only |
| (d) zero console.* | call-form scanner (16 members) over backend new/modified files, frontend new/modified files, AND the 7 new DEV1-014 test files | **PASS** | 0 matches; the suite itself is inside the scanned test-file set and stays clean (negative-fixture firing shapes are constructed via `join` so the file never contains literal call-form) |
| (e) single-notifications-writer | notifications schema/repo import ban; direct insert-shape ban; repo-wide insert-shape equality; engine-member surface pin; positive engine-path pins | **PASS** | parents modules import ONLY the engine (never the notifications data layer); every `NotificationEngine.<member>` ⊆ {emitForUser, publishReceipts}; repo-wide `insert(notifications)` == EXACTLY `[backend/db/repo/notifications/notification.repository.ts]`; `emitForUser` (service + helpers, in-tx) and `publishReceipts` (service, own-commit) positively pinned |

## Pipeline counts

| Gate | Command | Result |
|---|---|---|
| 5.3.TE | `bun run test/scripts/run-test.ts backend/services/parents/parent-link.static-locks.test.ts` | **36 pass / 0 fail** (78 expects; green at every iteration) |
| 5.3.QL | `bun run scripts/health/sub-loop.ts backend/services/parents/parent-link.static-locks.test.ts --lifecycle duplicates` | **exit 0** (tsgo → oxlint → biome → eslint type-aware → duplicates) |
| Type-check | `bun tsgo` (full project) | **exit 0 / 0 errors** |
| Regression | chaos suite | **10 / 0** (112 expects) |
| Regression | `parent-link-request.repository.test.ts` | **31 / 0** (131 expects) |
| Regression | `student.repository.test.ts` | **16 / 0** (70 expects) |

## QL fix log (same-changeset, zero disable comments)

1. tsgo TS6133 — unused `root` param in `listSingleFile` (removed).
2. oxlint `unicorn/prefer-set-has` — both allowlists → `ReadonlySet<string>` + `.has()`.
3. eslint type-aware sonarjs: `super-linear-regex` ×3 + `regex-complexity` ×2 — bounded lazy windows (`[\s\S]{0,400}?`) replaced by `anchoredWindows()` code-slicing + capture-token null checks (`isNonNullValueToken`, case-sensitive for TS tokens / case-insensitive for SQL `NULL`); console alternation trimmed to the 16 realistic members. `prefer-specific-assertions` ×4 → `toHaveLength`.

## SEC / SR / IV

- **5.3.SEC PASS** — this suite IS the audit: every green lock is load-bearing static proof for INV-P1 (`docs/specs/state-machine-invariants.md` §7.2), REQ-037 (audit admin-only), REQ-023 (no-LIKE discovery), REQ-021/REQ-074 (engine-only notifications).
- **5.3.SR PASS** — allowlists EXHAUSTIVE: `{student.repository.ts, entity-setup.ts}`; any future writer requires an explicit test edit (visible in the `toEqual` assertions) with a PR note. Scan paths are relative-pinned: REPO_ROOT is derived from the suite's OWN location (`backend/services/parents/` → 3 up), never `process.cwd()`, with a corpus guard proving the derivation.
- **5.3.IV PASS** — `.agents/instructions/tests.instructions.md` honored (bun:test only; zero DB ops; zero `console.*`; the `testLogger` rule respected in the scanned test files). "Architectural Invariant 7" verified as `docs/specs/state-machine-invariants.md` §7 Parent-Child Link Lifecycle — its §7.2 INV-P1 is exactly what lock (a) freezes.

## Drift

- tasks.md 5.3(b) names `frontend/views/parents/` — that directory does NOT exist; the real shipped view trees are `frontend/views/parent/handshake/` + `frontend/views/students/link-requests/` (tasks 4.2/4.3). Both are scanned, alongside the contracted `frontend/graphql/sharedDocuments/parents/`, the new lib helpers and the modified `navItems.ts`. Recorded in-suite (header docblock + corpus comment).
- `frontend/graphql/sharedDocuments/parents/` (contracted) exists and is scanned.
- Test-janitorial allowances honored: `entity-setup.ts` + `*.test.ts` excluded from production-tree scans (immutability-scan precedent); seeds write no students rows at all.

## Carry-forwards for 5.4

1. Journey re-run + full suites (`test:db`, `test:services`, `test:graphql`, `test:ui:components`) — 5.3 touched NO production code; the suite is additive-only, so any regression would be upstream.
2. REQ-080 canonical doc (`docs/parents/parent-link-request.md`) can cite this suite as the permanent INV-P1 static-proof artifact.
3. The `frontend/views/parents/` plan-path drift is worth a one-line correction if the plan text is ever refreshed (doc-refresh candidate alongside sharedDocuments/AGENTS.md from 4.1).
