# Task 0.2 Outcome — Prerequisite & Substrate Verification (reuse-never-fork guard)

**Task:** 0.2 (Phase 0) · **REQ:** REQ-004
**Plan:** `ai/plans/sprint_3/dev1-014-parent-child-link-request-workflow-7-day`
**Branch:** `feat/dev1-014-parent-child-link-request-workflow-7-day` @ `a259524` (verified up to date with `origin/main`; workspace had drifted back to `main` at task start — re-checked out the feature branch, same commit, working tree content identical, so all anchors are valid for the branch tip)
**Date:** 2026-09-01
**Method:** every claimed artifact was opened in the live tree (Read/Grep); verdicts below cite LIVE `path:line` anchors read this session. NO source code modified. Verdicts: CONFIRMED (anchor accurate) / DRIFTED (artifact exists, anchor moved — new anchor recorded) / MISSING (would downgrade dependent tasks to CREATE).

---

## 1. Verification Table

### 1.1 Backend service & repo substrates

| Artifact | Claimed anchor | Verdict | Live anchor (path:line) | Notes for dependent tasks |
|---|---|---|---|---|
| `emitForUser` (caller-tx, returns receipt, no publish) + `publishReceipts` composition | `backend/services/notifications/notification-engine.service.ts:288-340` | **DRIFTED** | `emitForUser` = `notification-engine.service.ts:43-78`; caller-tx receipt-WITHOUT-publish branch = `:65-68`; no-tx branch publishes = `:76`; `publishReceipts` = `:117-123` (delegates to `publishReceiptsFromIndex`, `notification-engine.publish.ts:43`) | File is 183 lines (anchor 288-340 is impossible). Contract matches the claim: `NotificationEngine.emitForUser(input, locale, tx?, options?)` returns `NotificationReturnType \| NotificationDeliveryReceipt`; publish happens ONLY when the caller supplies no `tx`. Claims flow through `claimOrPriorReceipt` (idempotency, `notification-engine.helpers.ts:34-54`). ⚠️ `emitForUser`'s internal `withTransaction` is the NOTIFICATION-ENGINE LOCAL copy (`notification-engine.helpers.ts:18-26`), byte-equivalent to the canonical `backend/lib/db/with-transaction.ts` (§1.2) — task 2.x/3.x emit calls work through either seam |
| `isGovernanceExcludedFromDiscovery(row, now)` | `backend/services/students/student-handshake.helpers.ts:33-60` | **CONFIRMED** | `:39-59` (pure module, 60 lines total) | Signature: `(governance: Omit<HandshakeDiscoveryRowType, "parentId">, now: Date): boolean` — first param is NAMED `governance` (not `row`). Fail-closed semantics documented at `:19-38`. Reuse AS-IS in 2.x link-request discovery |
| `findDiscoveryByHandshakeCode` (join + payload shape) | `backend/db/repo/students/student.repository.ts:78-120` | **DRIFTED** | `:128-165` (doc `:114-127`) | Join: `students s JOIN users u ON u.id = s.id`; payload = `parentId, fullName, isDeleted, isBlocked, suspended, suspendedAt, suspendedPeriodDays` → `HandshakeDiscoveryRowType`; sole predicate `handshake_code = $1`; tx branch Drizzle select, non-tx branch raw SQL via `queryDb`. Existing tests: `backend/db/test/repo/students/student.repository.test.ts:150+`; service consumer `student-handshake.service.ts:137` |
| `findLocalesByIds` | `backend/db/repo/users/user.repository.ts:200-250` | **DRIFTED** | `:152-186` (doc `:139-151`) | Returns `Map<number, AppLocale \| null>` pre-initialized with null for EVERY requested id (missing user ≡ no-locale ≡ null); empty input → empty map, no statement; tx branch `inArray(users.id, [...userIds])` (plain array), non-tx `= ANY($1)` via `queryDb`. Batch-lookup convention per backend.instructions.md |
| `defaultLocale = "ar"` | `shared/locale/AppLocale.ts:10` | **DRIFTED** | `:3` (`export const defaultLocale: AppLocale = "ar"`) | File is 8 lines: `locales = ["ar","en"]` `:1`, `AppLocale` type `:2`, `isAppLocale` `:5-7`. Claimed line 10 does not exist; artifact real |

### 1.2 Transaction helper

| Artifact | Claimed anchor | Verdict | Live anchor (path:line) | Notes for dependent tasks |
|---|---|---|---|---|
| `withTransaction(outerTx, fn)` | `backend/lib/db/with-transaction` (import anchored at `backend/services/admin/user-management.service.ts:13`) | **CONFIRMED** (artifact); import anchor **DRIFTED** | `backend/lib/db/with-transaction.ts:27-35`; import at `user-management.service.ts:62` (line 13 is prose) | Exact signature: `withTransaction<T>(outerTx: DBTransaction \| undefined, fn: (tx: DBTransaction) => Promise<T>): Promise<T>` — outerTx ⇒ SAVEPOINT; undefined ⇒ top-level `db.transaction`. ⚠️ TWO byte-equivalent implementations exist: the canonical lib one AND the notification-engine local copy (`notification-engine.helpers.ts:18-26`). Task 2.x must import the canonical `@/backend/lib/db/with-transaction` (shortest-path barrel rule) and must NOT add a third copy |

### 1.3 Error hierarchy

| Artifact | Claimed anchor | Verdict | Live anchor (path:line) | Notes for dependent tasks |
|---|---|---|---|---|
| `NotFoundError` entity form | `backend/lib/errors.ts:24-30` | **DRIFTED** | `:37-41` (`:24-29` is the DomainError tail) | `constructor(entity: string, message: string, options?)` → code `${entity.toUpperCase()}_NOT_FOUND` — e.g. `new NotFoundError("PARENT_LINK_REQUEST", …)` → `PARENT_LINK_REQUEST_NOT_FOUND` |
| `ConflictError` class overloads | `backend/lib/errors.ts:90-130` | **DRIFTED** | `:170-182` (doc `:158-169`) | Overloads CONFIRMED: `(message, options?)` → code `CONFLICT`; `(code, message, options?)` → custom typed code. Custom-code precedent strings: `USER_SELF_DEACTIVATION_FORBIDDEN` etc. — the 2.x/3.x codes (`PARENT_LINK_REQUEST_ALREADY_RESOLVED`, `PARENT_LINK_REQUEST_EXPIRED`, `PARENT_LINK_TARGET_ALREADY_LINKED`, `PARENT_LINK_ALREADY_PENDING`) follow this exact pattern |

### 1.4 Enums

| Artifact | Claimed anchor | Verdict | Live anchor (path:line) | Notes for dependent tasks |
|---|---|---|---|---|
| `LinkStatus` exists, NO `isLinkStatus` guard | `backend/enum/shared/link-status.enum.ts:1-6` | **CONFIRMED** | enum `:6-11` (`Pending/Confirmed/Rejected/Expired` = `"pending"/"confirmed"/"rejected"/"expired"`) | Repo-wide grep: **zero** `isLinkStatus` occurrences in live code (plan-doc references only); **zero** live consumers of `LinkStatus`/`linkStatus` — "first consumer is this ticket" TRUE. Doc comment `:4` self-describes as unused-parity. Task 1.x adds the additive guard mirroring `isApplicantStatus` |
| `isApplicantStatus` precedent + test file | `backend/enum/teachers/applicant-status.enum.ts:7-9` | **DRIFTED** (anchor is doc-comment) | enum `:12-17`; guard `:25-27` | Guard shape to mirror: `typeof value === "string" && (Object.values(ApplicantStatus) as string[]).includes(value)`. Test file **EXISTS**: `backend/enum/teachers/applicant-status.enum.test.ts` (Glob-verified) |
| `linkStatus` pgEnum `["pending","confirmed","rejected","expired"]` — FROZEN, first consumer this ticket | `backend/db/schema/enums.ts:54` | **CONFIRMED (exact)** | `backend/db/schema/enums.ts:54` | Line-for-line match; grep over `backend/**` shows the only `linkStatus` occurrence IS this definition → no prior consumer, frozen inventory holds |

### 1.5 Journey harness — plan claim is STALE (artifact EXISTS)

| Artifact | Claimed anchor | Verdict | Live anchor (path:line) | Notes for dependent tasks |
|---|---|---|---|---|
| Plan §0.2 bullet: "Journey harness DOES NOT EXIST … Task 2.1 must CREATE the harness" | — | **STALE CLAIM — HARNESS EXISTS** (recorded as ledger D5; 2.1 step downgraded CREATE → REUSE) | see rows below | `test/workflows/AGENTS.md:7-10` "Status: The shared harness is scaffolded" is NOT aspirational — every named file exists and the harness is already used by `test/workflows/parents/handshake-discovery.test.ts:47-49` (imports `createJourneyCast`) |
| Helpers barrel | — | **EXISTS** | `test/workflows/helpers/index.ts:24-29` — pure `export *` of 6 modules: `actor-context`, `journey-actor-fixtures`, `journey-cleanup`, `journey-fixtures`, `spied-transport`, `tracked-fixtures` | Names documented disjoint → collision-free barrel |
| `TrackedFixtures` | claimed missing | **EXISTS** | class `tracked-fixtures.ts:173` (interfaces `TrackedFixtureRegisterOptions :27`, `TrackedFixtureRecord :42`, `TrackedFixtureCleanupReport :54`) | Registration-order registry + zero-residue post-teardown re-probe (AGENTS.md hard rule 2) |
| `provisionStudentActor` | claimed missing | **EXISTS** | `actor-context.ts:88-96` — `(tx: DBTransaction, options?: ActorProvisionOptions) => Promise<JourneyActor>` | Real `users` row (role="student") + real `students` row; optional `tracked` registration |
| `provisionParentActor` | claimed missing | **EXISTS** | `actor-context.ts:122-130` (same shape) | Real `users` + `parents` rows. Siblings: `provisionCertifiedTeacherActor :105`, `provisionAdminActor :136` |
| `SpiedFanoutTransport` | claimed missing | **EXISTS** | class `spied-transport.ts:49` (implements `FanoutTransportLike :24`; `FanoutPublishCall :29`) | Injection-seam spy for the notification fan-out boundary (AGENTS.md rule 5) |
| Extra helpers NOT named in the plan | — | **EXISTS** | `createJourneyCast(domain)` `journey-fixtures.ts:251` → `JourneyCastType :113-128` (`registerStudentActor` returns the generated handshake code, `registerParentActor`, `trackedUserIds`, `countSideEffectRows`, `teardown`, `residueCounts`); `linkStudentToParentFixture(studentId, parentUserId)` `journey-fixtures.ts:157-169` (committed `students.parent_id` write — self-described emulation of the future link-request mutation); `setGovernanceFixture(userId, governance)` `journey-fixtures.ts:179-219`; `catchJourneyError` `journey-fixtures.ts:226`; `journeyCleanup(registry)` `journey-cleanup.ts:62`; `createJourneyFixtures(prefix)` `journey-actor-fixtures.ts:141` (+ `ANONYMOUS_ACTOR_ID :62`, `JourneyActorFixture :79`, `JourneyCast :86`, `JourneyFixtureBundle :114`) | `linkStudentToParentFixture` is the DIRECT-WRITE stand-in that 2.x's real `requestParentChildLink`/link flow supersedes for link journeys — keep it for governance/discovery journeys |
| Harness self-tests | — | **EXISTS** | `test/workflows/helpers/helpers.self-test.test.ts`, `test/workflows/helpers/journey-fixtures.smoke.test.ts` | Runnable via canonical `bun run test/scripts/run-test.ts <path>` |
| `test/workflows/parents/handshake-discovery.test.ts` | — | **EXISTS** | parent-discovery journey using the harness | Reuse as the structural template for 2.x link-request journeys |
| Referenced test files "do not exist" (`fanout-transport.test.ts`, `notification-engine.emit.test.ts`) | — | **STALE — BOTH EXIST** (plan also cited them under `test/workflows/notifications/`, wrong dir) | `backend/services/notifications/realtime/fanout-transport.test.ts`; `backend/services/notifications/notification-engine.emit.test.ts` (BOPLA "smuggled extra input fields are ignored" precedent at `:421`; `emitForUser` caller-tx receipt-no-publish matrix at `:341+`) | `test/workflows/notifications/` contains the journey tests `j1-targeted-single-recipient.test.ts` + `j2-cohort-broadcast-offline-persistence.test.ts` — the engine unit tests live under `backend/services/notifications/` |

### 1.6 Shared locale, masking, frontend auth

| Artifact | Claimed anchor | Verdict | Live anchor (path:line) | Notes for dependent tasks |
|---|---|---|---|---|
| `isPgliteProvider` | `test/helpers/skip-when-pglite.ts:1-5` | **DRIFTED** | `:48-50` (+ `describeGraphqlSuite` `:56`) | `DB_PROVIDER === "pglite"` check; sandbox/CI runs `DB_PROVIDER=postgres` so suites run normally |
| `maskFullName` | `shared/lib/mask-full-name.ts` (import ref `student-handshake.service.ts:7`) | **CONFIRMED** (import anchor **DRIFTED**) | `maskFullName(fullName: string): string` at `:53-62`; import at `student-handshake.service.ts:49`, usage `:151` | Total/deterministic/grapheme-aware (`Intl.Segmenter`); empty-after-trim → `"***"`. Front 4.x types on exactly this signature |
| `useAppTranslation` OVERLOAD contract | `shared/locale/client/use-app-translation.ts:1-12` | **CONFIRMED** | overloads `:8-9`, impl `:10-17` | EXACT contract as claimed: `useAppTranslation(): Translations` + `useAppTranslation<TLabels>(handle: NamespaceHandle<TLabels>): TLabels` |
| `getTranslations` 1-arg | `shared/locale/server.ts:12-14` | **DRIFTED** (minor) | `:15-17` (`resolveLocale` `:11-13`; also `getDefaultTranslations :19`, `loadAllTranslations :23`) | 1-arg `(locale: string): Translations`, unknown locale → `defaultLocale` fallback |
| `getServerTranslations` 1-arg | `shared/locale/server-graphql.ts:2-4` | **CONFIRMED** | `:3-5` | `(locale: string) => Translations` — thin pass-through to `getTranslations`; backend.instructions.md i18n mandate |
| `withPageAuth` `{ roles, redirectTo }` | `frontend/lib/auth/withPageAuth.ts:15-30` | **DRIFTED** (anchor is doc-comment) | options `WithPageAuthOptions :34-47`; result `WithPageAuthResult :50-57`; fn `:67-105` | Returns `{ userId, user, role }`; anonymous → `/login?redirect=…`; role mismatch → `roleDashboardPath(ctx.role)` (`:95`) |
| `roleDashboardPath(ctx.role)` | `frontend/lib/auth/roleDashboardRoute.ts:9-22` | **CONFIRMED** (anchor is doc-comment; symbol real) | fn `:52-65`; `ROLE_DASHBOARD_ROUTE` map `:35-40` — **Parent → `/parent/dashboard` at `:39`** | Accepts backend lowercase or codegen capitalized `UserRole`; unknown → student dashboard fallback. 4.x parent pages use exactly this pair |

### 1.7 Prose-only UI surfaces — PROVISIONAL verdicts (final verify inside tasks 4.2/4.3)

| Artifact | Provisional verdict | Live anchor | Provisional notes |
|---|---|---|---|
| `frontend/views/parent/handshake/HandshakeDiscoveryContainer.tsx` | **EXISTS** | 152 lines; client container; state-gated `useQuery(skipToken)`; doc `:40` pins "NO link-request CTA (deferred to the link-request feature)" — the exact hook where 4.2 adds the link-request action | Full structure (states idle/searching/not-found/found + `PermissionDeniedFallback`) read this session |
| `app/(dashboard)/parent/handshake/page.tsx` | **EXISTS** | 41 lines; `withPageAuth({ roles: [UserRole.Parent], redirectTo: "/parent/handshake" })` at `:34`; server shell passes translated static copy to the container (`:39`) | 4.3 extends this page/section per plan |
| `test/ui/AGENTS.md` harness (`TestWrapper`, translation preload) | **EXISTS** | `test/ui/AGENTS.md` (E2E + component-test rules; translation-first assertions `:74-170`); `TestWrapper` = `test/ui/components/TestWrapper.tsx:44`; `translation-preload.ts` = `test/ui/components/translation-preload.ts` | Component tests use client path `readTranslation` (never `getServerTranslations`); new namespaces in 1.x must be added to the preload array |

**Net MISSING count: 0.** Net downgrades of dependent-task steps: 1 (D5 — 2.1 CREATE → REUSE). No prose-only artifact required a downgrade; the three UI surfaces and the entire journey harness exist.

---

## 2. Files Created / Modified by Task 0.2

- **Created:** this outcome file (`outcome/0.2-prereq-outcome.md`).
- **Modified:** `deferred-items.md` — ONE ledger row added (D5, 🔄 In Progress, owned by Task 2.1). No other ledger changes (CONFIRMED/DRIFTED artifacts do not take rows).
- **Modified:** `tasks.md` — checkbox flips ONLY: task 0.2, its 0.2.QL / 0.2.SR / 0.2.IV sub-lines, and the 14 verification bullets inside 0.2 (see §5).
- **Not touched:** ANY source/test/GraphQL file; no commits; no stashes. (Workspace branch state only: re-checked out `feat/dev1-014-parent-child-link-request-workflow-7-day` after finding the repo on `main` — no file content changed by this.)

## 3. Quality Loop (0.2.QL)

Known repo limitation (recorded by 0.1, `scripts/health/sub-loop-checks.ts` `checkOxlint` has no `.md` skip): `sub-loop.ts --lifecycle duplicates` exits 1 on ANY `.md` target with "No files found to lint" — structural inapplicability, not a content violation. Per dispatch, compensated with direct `bunx jscpd -c .jscpd.json <file>` (the duplicates-stage check) on every `.md` written/edited — results in §6.

## 4. Cross-file dependencies discovered (carry-forward)

1. **DUAL `withTransaction`** — canonical `backend/lib/db/with-transaction.ts:27-35` vs notification-engine local `notification-engine.helpers.ts:18-26` (identical semantics: SAVEPOINT on outerTx / top-level otherwise). Task 2.x services import the canonical one; do not fork a third.
2. **Emit seam for 2.x notifications** — `NotificationEngine.emitForUser(input, locale, tx?, options?)` (`notification-engine.service.ts:43-78`): caller-tx ⇒ receipt WITHOUT publish; engine publishes only when it owns the transaction. Batch twin `emitForUsers :80-115`; post-commit publish composition `publishReceipts :117-123` → `publishReceiptsFromIndex` (`notification-engine.publish.ts:43`). `options = { transport?, cache? }` (`:35-38`) is the injection seam for `SpiedFanoutTransport` in tests.
3. **2.1 harness reuse (ledger D5)** — import everything from `@/test/workflows/helpers` barrel; do NOT recreate. Journey rules live in `test/workflows/AGENTS.md` (no `runInRollback`, committed fixtures + `TrackedFixtures` cleanup, unique `jrn_<domain>_<hex>` prefixes, real actors via actor-context, spied external effects, no `expect(...).rejects.toThrow()`, `bun:test`, `@/` aliases, one journey file per workflow).
4. **Handshake-code discovery chain is DONE substrate** — repo `findDiscoveryByHandshakeCode` (`student.repository.ts:128-165`) → service `findStudentByHandshakeCode` (`student-handshake.service.ts:137`) → governance predicate `isGovernanceExcludedFromDiscovery` (`student-handshake.helpers.ts:39-59`) → GraphQL/client container. 2.x link-request discovery REUSES this chain; `maskFullName` already masks at `student-handshake.service.ts:151`.
5. **Frozen pgEnum** — `link_status` (`schema/enums.ts:54`) has zero live consumers; 1.2's table becomes the first. TS mirror `LinkStatus` (`link-status.enum.ts:6-11`) equally unconsumed; the new `isLinkStatus` guard in 1.x mirrors `isApplicantStatus` (`applicant-status.enum.ts:25-27`), which already has a test file precedent (`applicant-status.enum.test.ts`).
6. **Error-code wiring** — `NotFoundError(entity, message)` uppercases entity into `_NOT_FOUND`; `ConflictError` custom-code overload (`errors.ts:170-182`) is the vehicle for the ticket's typed conflict codes. Both flow through `DomainError` → `extensions.code` (backend.instructions.md mandate).
7. **Locale plumbing** — `defaultLocale="ar"` (`AppLocale.ts:3`); server 1-arg `getTranslations` (`server.ts:15-17`) and `getServerTranslations` (`server-graphql.ts:3-5`); client overload contract `use-app-translation.ts:8-17`. Notification batch localization uses `UserRepository.findLocalesByIds` (`user.repository.ts:152-186`, null-for-missing semantics → emitter fallback locale).
8. **UI reuse pair for 4.x** — `withPageAuth({ roles: [UserRole.Parent], redirectTo })` (`withPageAuth.ts:34-47,67-105`) + `roleDashboardPath` (`roleDashboardRoute.ts:52-65`, Parent route `/parent/dashboard` at `:39`).

## 5. Checkboxes & Instruction Verification (0.2.IV)

- Ticked in `tasks.md`: `0.2`, `0.2.QL`, `0.2.SR`, `0.2.IV`, and the 14 un-numbered verification bullets (lines 50-63) — each was actually verified against live code this session. Nothing else touched.
- 0.2.SR self-check: every EXISTING artifact above carries a live `path:line` anchor captured this session; the single prose-only downgraded step (2.1 CREATE-harness) is DOWNGRADED-to-REUSE and recorded in ledger D5 + this outcome. No silent ignores.
- 0.2.IV: validated against `.agents/instructions/backend.instructions.md` — verification only, no code written (layer/barrel/type rules not triggered); repo accesses and greps executed with bun/bunx per the Bun rules; error-hierarchy and i18n substrate findings match the instructions' DomainError + `getServerTranslations` mandates; repo batch-lookup convention (`findLocalesByIds`) matches the documented DataLoader batch pattern.
- Deviation note (same pattern as 0.1 §7.1): tasks.md line 64 names the outcome `0-prereq-outcome.md`; executed per dispatch/skill convention as **`0.2-prereq-outcome.md`** (this file).

## 6. Raw QL evidence (verbatim)

```
$ bunx jscpd -c .jscpd.json ai/plans/sprint_3/dev1-014-parent-child-link-request-workflow-7-day/deferred-items.md
No duplicates found.
┌────────┬────────────────┬─────────────┬──────────────┬──────────────┬──────────────────┬───────────────────┐
│ Format │ Files analyzed │ Total lines │ Total tokens │ Clones found │ Duplicated lines │ Duplicated tokens │
├────────┼────────────────┼─────────────┼──────────────┼──────────────┼──────────────────┼───────────────────┤
│ Total: │ 0              │ 0           │ 0            │ 0            │ 0 (0.00%)        │ 0 (0.00%)         │
└────────┴────────────────┴─────────────┴──────────────┴──────────────┴──────────────────┴───────────────────┘
Found 0 clones.
time: 3.012ms
jscpd-ledger-exit=0
$ bunx jscpd -c .jscpd.json ai/plans/sprint_3/dev1-014-parent-child-link-request-workflow-7-day/outcome/0.2-prereq-outcome.md
No duplicates found.
… (identical table shape) …
Found 0 clones.
time: 2.831ms
jscpd-outcome-exit=0
```
Both runs exit 0 (0 clones). Note (same as 0.1 §6.3): `.jscpd.json` ignores `**/ai/**`, so "Files analyzed: 0" reflects the config's path exclusion of plan docs — the check still exits 0, which is the gate's requirement. The sub-loop oxlint-on-md limitation was NOT re-triggered (direct jscpd per dispatch instruction).
