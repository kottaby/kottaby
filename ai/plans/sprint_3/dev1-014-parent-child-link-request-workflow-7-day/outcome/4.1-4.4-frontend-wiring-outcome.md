# Outcome — Tasks 4.1 + 4.4: Frontend documents + barrel wiring + document-contract test; student nav entry

- **Task IDs:** 4.1 (REQ-063 · plan §5.4) + 4.4 (REQ-064 · plan §5.2)
- **Status:** ✅ COMPLETE — QL exit 0 ×5, TE green (documents 15/0, nav 15/0; apolloCache 9/1 = **pre-existing baseline failure**, see §Cache), `bun tsgo` 0 errors, SR/IV PASS
- **Agent:** general-purpose subagent
- **Date:** 2026-09-01
- **Branch:** `feat/dev1-014-parent-child-link-request-workflow-7-day` (HEAD `b8c1ee2` at dispatch; re-asserted repeatedly — see §Sandbox)

## Artifacts

| File | Action | Notes |
|---|---|---|
| `frontend/graphql/sharedDocuments/parents/parent-link.documents.ts` | CREATE | The five named TypedDocumentNode operations, EXACTLY per plan §5.4 / 3.3 pins |
| `frontend/graphql/sharedDocuments/parents/index.ts` | CREATE | Domain barrel (`export * from "./parent-link.documents";`) |
| `frontend/graphql/sharedDocuments/index.ts` | UPDATE | ONE line added: `export * from "./parents";` (alphabetical: admin, auth, billing, **parents**, notifications, students, teachers) |
| `frontend/graphql/sharedDocuments/parents/parent-link.documents.test.ts` | CREATE | Contract suite mirroring `notifications/notification.documents.test.ts` — 15 tests |
| `frontend/graphql/generated/gql/graphql.ts` | REGEN (`bun codegen`) | The expected 4.1 codegen diff — see below |
| `frontend/views/dashboard/nav/navItems.ts` | UPDATE | ONE student-array entry (4.4) — see §4.4 |
| `tasks.md`, `outcome/4.1-4.4-frontend-wiring-outcome.md`, `worklog.md` | BOOKKEEPING | mandated |

Untouched by design: `frontend/providers/apollo/apolloCache.ts` (zero new policies needed — both parent-link objects carry real `id`s and normalize into standalone cache entries), `public-operations.ts`, all locale files (`linkRequests` was registered on Dashboard by 1.1).

## 4.1 — the five documents (export names per `{camelCase}{Query|Mutation}Document` convention)

| Export | Operation | Channel / variables | Payload nullability |
|---|---|---|---|
| `myOutgoingParentLinkRequestsQueryDocument` | `MyOutgoingParentLinkRequests` | query, ZERO-arg (`TypedDocumentNode<T>`) | `[OutgoingParentLinkRequest!]!` rows |
| `myIncomingParentLinkRequestsQueryDocument` | `MyIncomingParentLinkRequests` | query, ZERO-arg (`TypedDocumentNode<T>`) | `[IncomingParentLinkRequest!]!` rows |
| `requestParentChildLinkMutationDocument` | `RequestParentChildLink` | mutation, `($code: String!)` | **NULLABLE** — the ONLY nullable new mutation (REQ-012 collapse) |
| `respondToParentLinkRequestMutationDocument` | `RespondToParentLinkRequest` | mutation, `($requestId: ID!, $accept: Boolean!)` | non-null |
| `cancelParentLinkRequestMutationDocument` | `CancelParentLinkRequest` | mutation, `($requestId: ID!)` | non-null |

Row selections (id FIRST, exact order): outgoing rows `id, status, studentMaskedName, createdAt, expiresAt, respondedAt`; incoming rows `id, status, parentFullName, createdAt, expiresAt, respondedAt`. `respondedAt` (sole nullable field) selected so resolved rows restyle without a refetch. Typed from the single generated `graphql.ts` (`useTypeImports`, no inline literals, no schema-level types). Zero identity variables — self-scoped by the server-verified caller.

### Codegen diff (the 4.1 growth 3.3 predicted)

`bun codegen` (exit 0) grew `frontend/graphql/generated/gql/graphql.ts` with EXACTLY: `enum LinkStatus {Confirmed='Confirmed', Expired='Expired', Pending='Pending', Rejected='Rejected'}` (wire names), the five `…Query/Mutation(+Variables)` operation types with compact extracted row types (`MyOutgoingParentLinkRequestsQuery_myOutgoingParentLinkRequests`, etc.; `DateTime→string`, `respondedAt: string | null`), and the five `…Document` TypedDocumentNode constants. `schema.graphql` NOT regenerated (3.3 already current; `generate:gqlSchema` not needed — no backend change in this task). Variables: `requestId: string | number` (ID scalar), `code: string`, `accept: boolean`.

## Document-contract test (15 tests / 112 expects, ZERO boot/DB/network)

Mirrors `notification.documents.test.ts`: assertion-free AST helpers (kind-guard narrowing, no unsafe assertions) + contract table.

1. **Named ops** — 5 table-driven: name ↔ export convention, channel, exact variable sets (`[]`, `[]`, `[code]`, `[requestId, accept]`, `[requestId]`) in declaration order.
2. **Argument wiring** — every declared variable threads into its root-field argument (no dead vars, no literal args).
3. **Variable surface** — exactly `["accept","code","requestId","requestId"]` sorted; belt-and-braces: no variable name contains `user`/`parent`/`student` (identity never crosses the wire).
4. **id-first + canonical rows** — each object selection `toEqual` the exact six-field row (closes the selection set) AND `[0] === "id"`; outgoing rows carry `studentMaskedName` never `parentFullName` (and vice versa) — masked-name contract REQ-020; both lists zero-arg with zero root-field arguments; `respondedAt` selected everywhere.
5. **Codegen binding + barrel parity** — root-barrel instances `toBe`-identical to deep imports; `TypedDocumentNode` compile-time assignments ×5 (`.loc` runtime uses); `requestParentChildLink` payload admits `null` (compile-time assignment + `isCollapsedPayload` guard = the consumer-side REQ-012 predicate; the NON-null side of respond/cancel is enforced at the tsgo gate — not expressible as a passing runtime assertion); generated `LinkStatus` members are the GraphQL WIRE names `{Confirmed, Expired, Pending, Rejected}`.

## 4.4 — student nav entry

- Added EXACTLY ONE entry to the `[UserRole.Student]` array (placed after `homework`, before `profile`): `{ route: "/student/link-requests", labelKey: "linkRequests", Icon: LinkChildIcon }`.
- `LinkChildIcon` import REUSED (pre-existing `LinkOutlined as LinkChildIcon` at `navItems.ts:11` — `*Outlined` discipline intact); ZERO import changes, ZERO other arrays touched.
- NO new translation labels: `linkRequests` pre-registered on Dashboard by 1.1 (verified at `shared/locale/types/dashboard/index.ts:47`, `en/dashboard/index.ts:19`, `ar/dashboard/index.ts:19`); zero changes under `shared/locale/**` in this task.
- Ownership matrix GREEN UNTOUCHED (test file not modified): `linkRequests` is owned ONLY by Dashboard (the `handshakeCode` leaf lacks it) so the `key in dashboardEn` discrimination stays unambiguous; all 15 nav tests pass (113 expects).

## Pipeline evidence

| Gate | Command | Result |
|---|---|---|
| 4.1.QL ×4 | `sub-loop.ts <file> --lifecycle duplicates` on documents.ts / parents barrel / documents.test.ts / root barrel | **exit 0 ×4** (final sweep; test file needed 2 fix iterations — see §QL fixes) |
| 4.4.QL | same on `navItems.ts` | **exit 0** |
| 4.1.TE (1/3) | `run-test.ts frontend/graphql/sharedDocuments/parents/parent-link.documents.test.ts` | **15 pass / 0 fail / 112 expects** |
| 4.1.TE (2/3) | `run-test.ts frontend/providers/apollo/apolloCache.test.ts` | **9 pass / 1 fail** — PRE-EXISTING at HEAD (proven via `git stash -u` pristine run: identical 9/1); see §Cache |
| 4.4.TE | `run-test.ts frontend/views/dashboard/nav/navItems.test.ts` | **15 pass / 0 fail / 113 expects** |
| Type-check | `bun tsgo` (full project) | **exit 0, 0 errors** |

### QL fix log (documents.test.ts)

1. TS6196 unused `…QueryVariables` type imports (zero-arg queries) → removed from the import list.
2. `import-x/no-duplicates` (two `graphql.ts` imports) → merged into one `import { LinkStatus, type … }`.
3. `sonarjs/no-trivial-assertions` + `prefer-specific-assertions` catch-22 on `expect(payload).toBeNull()` (type-aware linter knows the const is `null`) → reworked to the module-scope `isCollapsedPayload()` guard (the honest consumer-side predicate for REQ-012), keeping the compile-time null-assignment proof.

## Cache policy — UNTOUCHED, and a pre-existing drift recorded honestly

- **4.1 invariant HOLDS:** no `keyFields: false` needed for either parent-link object (both carry real `id`s); `apolloCache.ts` byte-untouched (`git status` empty under `frontend/providers/apollo/`); zero policies added/changed.
- **Pre-existing baseline failure (NOT introduced by this task):** `apolloCache.test.ts:178` "policy surface is FROZEN to the five documented entries" expects `{AdminDashboardScheduleResult, AdminNoteInfo, HandshakeCodeLookup, HealthCheck, OnlineMeetingInfo}` but the live cache registers SIX — upstream's notifications feature (commit `b3b9aac`) added `NotificationListPage: { keyFields: false }` to `apolloCache.ts` without extending the frozen list. Proof of pre-existence: `git diff --stat a259524 b8c1ee2 -- frontend/providers/apollo/` is EMPTY and a pristine-HEAD run (`git stash push -u` → run → pop) reproduces exactly 9 pass / 1 fail / 23 expects. All other 9 tests pass, including both `keyFields:false` pins and all behavioral probes. The 9 other tests covering the 4.1-relevant pins are GREEN.
- **Carry-forward (one-line fix, NOT applied — outside this task's touch list):** add `"NotificationListPage"` to the frozen array at `apolloCache.test.ts:178-184` (or let the orchestrator dispatch it) — until then this suite reports 9/1 regardless of DEV1-014 work.

## Drift notes

1. **Nav path drift (recorded per dispatch brief):** tasks.md 4.4 prose cites `frontend/views/dashboard/navItems.ts` (`:35-42`, import at `:8`) — STALE. The REAL file is `frontend/views/dashboard/nav/navItems.ts` (nav is a directory module with `nav/index.ts` barrel); the student array is `:103-110` and the `LinkChildIcon` import is at `:11`. (The live tasks.md line was already corrected to `:103-109`/`:11` by the 0.3 plan-review fixes; the dispatch brief's citation reflects the pre-review prose.)
2. **`.agents/instructions/frontend.instructions.md` doc drift (pre-existing, untouched):** its Apollo section cites `frontend/common/graphql/sharedDocuments/…` — the live tree uses `frontend/graphql/sharedDocuments/…`. My artifacts follow the LIVE layout mandated by `frontend/graphql/AGENTS.md` + `frontend/graphql/sharedDocuments/AGENTS.md`. No instruction file was edited.
3. **`useLazyQuery` repo scan:** 6 files mention the token — ALL comment-only ban statements (documents headers, contract-test headers, `HandshakeDiscoveryContainer.tsx:23,100` self-descriptions). ZERO real `useLazyQuery` usage repo-wide.

## SR verdicts — PASS

**4.1.SR PASS:** `useQuery`-only discipline holds repo-wide (scan above; this task introduces no hooks at all — documents tier); `id` FIRST in all five object selections (structurally test-locked: exact-row `toEqual` + `[0]==="id"`); `TypedDocumentNode` on 5/5 documents typed from the single generated `graphql.ts` (compile-time assignments pinned in-test); no inline `gql` strings in components (0 hits under `views/`, `components/`, `app/` — documents own all `gql` usage); export names follow the `{camelCase}{Query|Mutation}Document` convention; self-scoped surface (no identity variables — test-locked); both objects carry real `id`s so cache policy needed no change.

**4.4.SR PASS:** exactly one entry added; route count for `/student/link-requests` in `navItems.ts` = 1 (no duplicates, no retarget of an existing route); zero new label keys in this task (Dashboard-owned `linkRequests` from 1.1; zero `shared/locale/**` changes); icon import reused (no import-list diff); no mobile bottom-nav work (no such component exists — the temporary MUI Drawer picks the item up automatically).

## IV verdicts — PASS (zero doc edits)

- `frontend/graphql/AGENTS.md` + `frontend/graphql/sharedDocuments/AGENTS.md`: domain-file layout ✓ (`parents/` sub-directory added to the Layout plan — recorded as carry-forward note below), barrel pattern ✓, TypedDocumentNode convention ✓, id-field requirement ✓, `@apollo/client` import (never `/core`) ✓, codegen re-run after document change ✓, NO-useLazyQuery ✓.
- `frontend/AGENTS.md`: styling/MUI/React rules not exercised by this task (no UI surfaces touched; the reused icon is `*Outlined`-compliant).
- `.agents/instructions/frontend.instructions.md`: rules honored (generated-types-only typing, no inline type literals, documents exported via barrels); its stale `frontend/common/graphql/` path citation is pre-existing doc drift (see above) — NOT edited.
- No instruction/AGENTS file required any change; `git status` shows zero edits under `.agents/`, `frontend/graphql/AGENTS.md`, `frontend/AGENTS.md`.

## Sandbox watchdog notes (environment)

- The sandbox repeatedly force-reset the worktree to `main` (branch AND tracked-file working changes discarded; untracked files survived). Defense used throughout: `git checkout -f feat/dev1-014-…` + state re-application (root-barrel line, nav entry, tasks.md marks) + `bun codegen` re-run + verification, all inside single commands. `checkout -f` on the feature branch ALSO discards tracked-file edits, so the codegen diff must be re-created after any such checkout. Final tree state re-verified at end of task; if the orchestrator finds the branch on `main`, re-run the checkout and `git status` before committing.

## Carry-forwards for 4.2 / 4.3 (document export names + types)

- **Consumer imports (barrel, preferred):** `import { myIncomingParentLinkRequestsQueryDocument, respondToParentLinkRequestMutationDocument, … } from "@/frontend/graphql/sharedDocuments";` (deep path `@/frontend/graphql/sharedDocuments/parents/parent-link.documents` equally valid — test pins instance identity).
- **Hook typing for 4.2:** `useQuery(myIncomingParentLinkRequestsQueryDocument)` (zero variables); rows at `data.myIncomingParentLinkRequests` with `parentFullName: string`, `status: LinkStatus`, `respondedAt: string | null`; mutation `useMutation(respondToParentLinkRequestMutationDocument)` with variables `{ requestId: string | number; accept: boolean }` (wire the row `id: string` straight in); `PARENT_LINK_REQUEST_EXPIRED / _ALREADY_RESOLVED / _NOT_FOUND` arrive as GraphQL errors → map `extensions.code` to the Errors-tree keys.
- **Hook typing for 4.3:** `useQuery(myOutgoingParentLinkRequestsQueryDocument)` (zero variables; `studentMaskedName` for the REQ-020 masked-name contract); send affordance `useMutation(requestParentChildLinkMutationDocument)` — payload `… | null` ⇒ branch on `null` for the `sendUnavailableNotice` state (REQ-012); cancel `useMutation(cancelParentLinkRequestMutationDocument)` — NOTE: exact const is `cancelParentLinkRequestMutationDocument`; refetch after every mutation (no cache surgery).
- **`LinkStatus` chip mapping:** use the generated `LinkStatus` enum (wire names) — expired chip is COMPUTED from `expiresAt` client-side, never a stale write.
- **apolloCache frozen-list drift** (above): one-line follow-up for the orchestrator.
- **`sharedDocuments/AGENTS.md` Layout section** lists `parents/` with stale content descriptions (it predates this task's parents domain, described under parent-* documents) — a doc-refresh candidate for a future docs-only dispatch; not edited here (IV = zero doc edits).
