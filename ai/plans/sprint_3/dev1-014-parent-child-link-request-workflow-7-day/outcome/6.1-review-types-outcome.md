# Task 6.1 Outcome — review-types wave (canonical types, enums, scalars)

- **Task ID:** 6.1 (Phase 6 post-implementation review — READ-ONLY)
- **Verdict:** **PASS-with-notes** — 0 blocker / 0 major / **1 minor** / **2 nit**. Zero functional or type-level defects; all three findings are doc-comment drift.
- **Agent:** general-purpose subagent (review-types)
- **Date:** 2026-09-02
- **Branch:** `feat/dev1-014-parent-child-link-request-workflow-7-day` @ `687bea2` (re-asserted via `.task61-guard.sh` — the sandbox watchdog reset HEAD to `main` twice mid-review; see §Process note)
- **Mode:** No source edits, no commits, no tasks.md changes. Reading snapshots taken under guard into `.task61-snapshot/` (untracked, survives resets).

---

## 1. Findings

| # | Severity | Location | Evidence | Recommended fix (fix-forward rounds) |
|---|---|---|---|---|
| F1 | **minor** | `backend/enum/shared/link-status.enum.ts:4-5` | Header JSDoc still claims "**Currently unused by any table** but kept for parity with the schema." False since task 1.2: `parent_link_requests.status` is `linkStatus("status")` (`backend/db/schema/parents/parent-link-requests.ts:42`), `ParentLinkRequestSelectType` derives from that table (`backend/types/parents/parent-link-request.types.ts:10`), and the Pothos registry comment (`enum.pothos.ts:22,134-137`) correctly describes the enum as backing the link-request lifecycle. The stale sentence predates the ticket (present at branch base `a259524`); task 1.3 deliberately kept the header untouched under its append-only diff gate — so the contradiction shipped. Zero runtime impact; risk is maintainer confusion (enum reads as "spare"/re-purposable). | One-line doc-comment edit: replace the "unused by any table" sentence with "used by `parent_link_requests.status` (reused frozen pgEnum — zero enum edits)". No test/behavior change; sub-loop the file. |
| F2 | **nit** | `backend/types/index.ts:16` | Inventory comment says "Canonical Select/Insert type inventory: **22** entity type pairs across 8 entity domain sub-directories". Already stale at the branch base (26 `$inferSelect` types lived at `a259524`) and DEV1-014 added one more pair (`ParentLinkRequestSelectType`/`InsertType` in `backend/types/parents/parent-link-request.types.ts:10-11`) → 27 now. Same drift class as `backend/db/schema/index.ts`'s "22 tables … 15 pgEnums" header, already recorded as cosmetic in outcome 1.2 §11(a). | Fold into the same doc-refresh round as the schema-header drift (6.3's sharedDocuments/AGENTS.md refresh candidate is adjacent): "23+ entity type pairs" or drop the count. |
| F3 | **nit** | `frontend/graphql/sharedDocuments/parents/parent-link.documents.test.ts:24-25` | Header comment: "`respondedAt` (the sole nullable field) is selected **so resolved rows restyle without a refetch**". Overstates the field's role: the no-refetch restyle is driven by the mutation response's **`status`** write-back into the normalized cache entry (as `parent-link.documents.ts:119-122` correctly says); `respondedAt` itself is never rendered by any view (`rg respondedAt frontend/views/` → 0 non-test hits; UI usage is limited to test fixtures, e.g. `StudentLinkRequestsContainer.test.tsx:370`). Selecting it is still correct (cache-row completeness, sole-nullability pin) — only the causal phrasing is imprecise. | Reword the test-header line to "…is selected so the cached row is complete (the restyle itself is `status`-driven via the mutation write-back)". |

**Explicitly checked and NOT found** (the hazards this wave exists to catch): no service-layer `.types.ts`; no `StudentLinkTargetRowType` anywhere in GraphQL/Pothos/generated/frontend (repo + repo-test only); no second `enum LinkStatus` declaration repo-wide (outside generated); no second `enumType(LinkStatus` registration; no string-typed timestamp on the wire (SDL has `DateTime` on all six; zero `String!` timestamps on either object); no frontend-local duplicate of the row shapes or the enum (single source: `@/frontend/graphql/generated/gql/graphql`); no `@/backend|@/frontend|@/app` import in `shared/constants/**` (the only matches are the isolation doc-comments themselves); no `toISOString()` hand-rolling in the new GraphQL modules (doc-comment mention only).

---

## 2. Coverage matrix — exactly what was compared (zero-defect proof surface)

**Oracle documents:** plan.md §4.2 (pinned five service signatures); outcome/1.2 (schema + push delivery + column/nullability evidence), 1.3 (constants + guard), 2.2 (repo §6 signatures), 2.3 (service contract), 3.1-3.2 (Pothos surface + "Exact surface for 3.3 pinning" SDL block), 3.3 (reconcile-then-extend pins, codegen parity), worklog 4.2+4.3/5.1-5.4.

### Surface A — canonical types + barrels (type→gql direction)

| Invariant | Evidence |
|---|---|
| `OutgoingParentLinkRequestReturnType` = exactly {id, status, studentMaskedName, createdAt, expiresAt, respondedAt}, readonly, closed | `backend/types/parents/parent-link-request.types.ts:33-40` |
| `IncomingParentLinkRequestReturnType` = exactly {id, status, parentFullName, createdAt, expiresAt, respondedAt} | `:56-63` |
| `ParentLinkRequestSelectType`/`InsertType` derived from the live table (`$inferSelect`/`$inferInsert`) | `:10-11` |
| Barrels: `parents/index.ts` exports both files; `backend/types/index.ts` re-exports `./parents`; `backend/enum/shared/index.ts` re-exports the enum; `shared/constants/index.ts` re-exports the constants | all read |
| `StudentLinkTargetRowType` (8 fields) server-internal only; consumers = `student.repository.ts:72,97,293` + repo test via `@/backend/types` | grep: zero GraphQL/frontend hits |
| Service mapping preserves the shapes 1:1 (`mapOutgoing`/`mapIncoming`: all six fields, masked vs full counterpart name per side) | `parent-link-request.helpers.ts:391-432` |

### Surface B — LinkStatus five-layer chain (both directions)

| Layer | Value set | Evidence |
|---|---|---|
| TS canonical enum (keys PascalCase) | `Pending="pending"`, `Confirmed="confirmed"`, `Rejected="rejected"`, `Expired="expired"` | `backend/enum/shared/link-status.enum.ts:6-11` (pre-existing; 1.3 appended `isLinkStatus` only) |
| Enum guard | exact-member, fail-closed, never throws; 4-tier suite (branch/boundary/fuzz/security) pins `"pending\|confirmed\|rejected\|expired"` frozen | `:19-21` + `link-status.enum.test.ts` (Tier-1 value-set freeze test) |
| DB pgEnum | `link_status`: `["pending","confirmed","rejected","expired"]` — CREATED in base migration `backend/drizzle/20260825220000_schema/migration.sql:12-13`, REUSED by the table (task 1.2: zero enum edits; table delivered via `drizzle-kit push` per `docs/DATABASE_MIGRATIONS.md`, hence no new journal migration — psql introspection evidence in outcome 1.2 §5) | `backend/db/schema/enums.ts:54` |
| Pothos registration | `gqlSchemaBuilder.enumType(LinkStatus, { name: "LinkStatus" })` — ENUM-OBJECT form, registered exactly ONCE repo-wide (`enum.pothos.ts:146-148`); registry doc-header updated (`:22`); sole consumer = the two parent-link objects (`status: t.expose("status", { type: LinkStatusPothosEnum })`) | grep `enumType(LinkStatus` → 1 hit |
| SDL artifact | `enum LinkStatus { Confirmed Expired Pending Rejected }` | `frontend/graphql/generated/schema.graphql:215-220` |
| Generated codegen enum | `enum LinkStatus { Confirmed='Confirmed', … Rejected='Rejected' }` — wire names are the TS KEYS | `frontend/graphql/generated/gql/graphql.ts:80-85` |
| Wire names in tests | documents test pins `Confirmed/Expired/Pending/Rejected` (`parent-link.documents.test.ts:332-345`); wire matrix proves both layers: DB insert uses the lowercase VALUE (`parent-link.wire.test.ts:622`) while the wire payload asserts `"Pending"` (`:1004`); UI tier uses ONLY the generated enum | read |
| Reverse direction (gql→type) | every generated enum member traces to a TS key; no lowercase value leaks to any wire shape; no PascalCase leaks into DB-facing code | read |

### Surface C — DateTime scalar + the six timestamps + respondedAt sole-nullability (five-layer agreement)

| Layer | createdAt/expiresAt | respondedAt |
|---|---|---|
| DB (`outcome 1.2 §5` psql) | `not null` (created `now()`; expires app-written, NO default) | nullable |
| Canonical types | `Date` | `Date \| null` |
| Repo raw rows | `Date` | `Date \| null` (`.repository.ts:70,86`) |
| Pothos | `t.expose(…, { type: "DateTime" })` ×4 | `nullable: true` — the ONLY nullable field (`parent-link-request.pothos.ts:49,63`) |
| SDL | `DateTime!` ×4 | `DateTime` (bare) ×2 (`schema.graphql:211,356`) |
| Generated | `string` ×4 | `string \| null` ×5/5 selection shapes (`graphql.ts:370-404`) |
| Documents | selected on every row; test pins `respondedAt` present (`:278-284`) | selected; sole-nullability claim pinned |
| Behavior | journey: pending rows → `respondedAt` null (`journey.test.ts:522,564`), confirmed/rejected/withdrawn → non-null (`:719,832,956`); wire test: `payload.respondedAt` null on create (`:1008`) | — |
| Scalar registration | `addScalarType("DateTime", DateTimeResolver)` exactly once (`scalar.pothos.ts:28`); fields reference BY NAME; codegen maps `DateTime → string` (3.3.IV) | — |

### Surface D — five operations' variable surfaces ↔ service signatures (both directions)

| Operation (document) | Document variables | Generated `Variables` | Resolver args | Service signature (plan §4.2 verbatim) |
|---|---|---|---|---|
| `query MyOutgoingParentLinkRequests` | none | `Exact<{[key:string]: never}>` | zero-arg field | `listMyOutgoing(parentActorId, locale, tx?)` → `Outgoing…ReturnType[]` (`service.ts:384`) |
| `query MyIncomingParentLinkRequests` | none | `Exact<{[key:string]: never}>` | zero-arg field | `listMyIncoming(studentActorId, locale, tx?)` (`:402`) |
| `mutation RequestParentChildLink` | `$code: String!` | `{ code: string }` | `code: t.arg("String", required)` | `requestLink(code, parentActorId, locale, outerTx?, options?)` → `Outgoing… \| null` (`:148`) — resolver passes `args.code, ctx.user.id, ctx.locale` positionally |
| `mutation RespondToParentLinkRequest` | `$requestId: ID!, $accept: Boolean!` | `{ requestId: string\|number; accept: boolean }` | `ID`+`Boolean` required, threaded as variables | `respondToLinkRequest(requestId, accept, studentActorId, locale, outerTx?, options?)` (`:230`) |
| `mutation CancelParentLinkRequest` | `$requestId: ID!` | `{ requestId: string\|number }` | `ID` required | `cancelLinkRequest(requestId, parentActorId, locale, outerTx?)` (`:340`) |
| Payload nullability | `requestParentChildLink` is the ONLY nullable payload (generated `… \| null`; documents-test compile-pin `:320-330`); respond/cancel non-null; lists `[T!]!` non-paginated | matches SDL `:250,287,292,420,430` | `nullable: true` only on request (`mutation.ts:153`) | service return types match |
| Root-field names | documents' root fields + operation names == resolver registrations == generated Document ASTs (`graphql.ts:458-462`); `id` FIRST on all five selections | documents test contract table `:164-205` | — | — |

### Surface E — i18n error keys ↔ constant-shape service codes

| Wire code (service emits) | Locale key | en | ar | Frontend mapping |
|---|---|---|---|---|
| `PARENT_LINK_TARGET_ALREADY_LINKED` (ConflictError) | `parentLinkTargetAlreadyLinked` | `en/errors/index.ts:52` | `ar/errors/index.ts:52` | `parent-link-denials.ts` accessors table |
| `PARENT_LINK_ALREADY_PENDING` (ConflictError) | `parentLinkAlreadyPending` | `:53` | `:53` | ✓ |
| `PARENT_LINK_REQUEST_NOT_FOUND` (NotFoundError, entity `PARENT_LINK_REQUEST`) | `parentLinkRequestNotFound` | `:56` | `:56` | ✓ |
| `PARENT_LINK_REQUEST_ALREADY_RESOLVED` (ConflictError) | `parentLinkRequestAlreadyResolved` | `:55` | `:55` | ✓ |
| `PARENT_LINK_REQUEST_EXPIRED` (ConflictError) | `parentLinkRequestExpired` | `:54` | `:54` | ✓ |
| `PARENT_LINK_REQUEST_STATUS_CORRUPT` | — none — | log-only invariant code (`helpers.ts:372-384`), throws plain internal Error, never user-facing | | ✓ (internal-guard precedent) |
| Resolver-layer `unauthorized`/`validation`/`forbidden` | pre-existing keys, consumed via `ctx.t("errorsTranslations")` / `getServerTranslations` | — | — | ✓ |
| `handshakeCodeInvalid` (pre-DB validation) | pre-existing key at `:49` in BOTH locales | ✓ |

Unmapped codes fold to `internalServerError` in `resolveParentLinkDenialCopy` (membership-guarded). en/ar blocks are line-aligned (52-56) — parity test `parentLink-namespace.parity.test.ts` exists in the locale tree.

### tasks.md §6.1 verify items — results

| Item | Result |
|---|---|
| no service-layer `.types.ts` | ✅ (`backend/services/parents/`: service + helpers + tests + barrel only) |
| `StudentLinkTargetRowType` never leaks into GraphQL | ✅ (grep over backend/graphql, frontend/** → zero) |
| `LinkStatus` registered ONCE, ENUM-OBJECT form | ✅ (grep `enumType(LinkStatus` → exactly 1, `enum.pothos.ts:146`; string-array form absent) |
| timestamps all use `DateTime` | ✅ (SDL: 6/6 `DateTime`; zero `String` timestamps on both objects — schema-surface pin `:973` live-green at HEAD) |
| shared/constants has zero `@/backend/**` imports | ✅ (zero import statements at all; the only matches are the isolation doc-comments) |
| findings written to `outcome/6.1-review-types-outcome.md` | ✅ (this file) |

---

## 3. Verdict

**PASS-with-notes.** The type/enum/scalar surface is coherent across all five layers (canonical TS → DB → Pothos → SDL/codegen → documents/tests/UI): value sets, wire names, nullability, and variable surfaces agree bidirectionally with zero functional drift. The three findings are doc-comment-only (1 minor stale JSDoc contradicting the shipped schema usage, 2 nits of stale/imprecise comments); none block 6.2-6.5, and all are safe to batch into a single doc-refresh round (natural home: 6.3's sharedDocuments/AGENTS.md doc-refresh carry-forward).

## 4. Process note (for the orchestrator)

- The sandbox watchdog force-reset HEAD to `main` (`a259524`) **twice during this review** between command batches. Mitigated with `.task61-guard.sh` (untracked; re-asserts `feat/dev1-014-…` == `687bea2`, no `-f`) prefixed to every batch, plus an untracked read-snapshot (`.task61-snapshot/`) so reads were immune to the resets. Same failure class as worklog 5.3/5.4; both helper artifacts are disposable.
- No source file was edited; no commit was made; tasks.md was not touched (§6.1 checkboxes remain for the orchestrator to flip).
