# Task 2.1 Outcome — Journey test `parent-link-request.journey.test.ts` (TEST-FIRST, RED-by-construction verified)

**Task:** 2.1 (Phase 2) · **REQ:** REQ-076, REQ-090..REQ-096, REQ-046 · specs §2.9 · plan §4.2/§4.4
**Plan:** `ai/plans/sprint_3/dev1-014-parent-child-link-request-workflow-7-day`
**Branch:** `feat/dev1-014-parent-child-link-request-workflow-7-day` (base `b8c2ba3`, Phases 0+1)
**Date:** 2026-09-01
**Status:** ✅ COMPLETE — RED-by-construction verified. The journey file binds to the not-yet-existing service surface and fails with EXACTLY the sanctioned absent-service failure mode (see §3); it turns GREEN at 2.3/3.x with ZERO test edits.

---

## 1. Artifacts

| File | Action | Purpose |
|---|---|---|
| `test/workflows/parents/parent-link-request.journey.test.ts` | CREATED (prior interrupted session) + lint-hardened THIS session (~1122 lines) | The §2.9 journey file — journeys A (request→notify→confirm→linked, Steps 1–9), B (reject & cancel & re-application & constant NOT_FOUND both directions & already-linked zero-notify, Steps 10–16), C (backdated-expiresAt silent expiry + sequential race-loser emulation, Steps 17–19) |
| `ai/plans/.../tasks.md` | UPDATE | `### 2.1 [x]` + all five sub-checkboxes (QL/TE/SEC/SR/IV) ticked |
| `ai/plans/.../deferred-items.md` | UPDATE | D5 flipped 🔄 → ✅ (harness REUSED, not created) |
| `worklog.md` | APPEND | Task 2.1 entry |

**NOT created/modified:** any file under `test/workflows/helpers/` (D5 honored — every import comes from the `@/test/workflows/helpers` barrel: `TrackedFixtures`, `provisionParentActor`, `provisionStudentActor`, `SpiedFanoutTransport`, `catchJourneyError`, `setGovernanceFixture`, `linkStudentToParentFixture`, `JourneyActor`, `ANONYMOUS_ACTOR_ID`); the service file or any stub (forbidden — TEST-FIRST); docs (`docs/testing/workflow-journey-tests.md` etc. — zero doc edits needed, see §5).

## 2. Journey-file contract compliance (how the harness is REUSED)

- **RED-posture dynamic import as FIRST `beforeAll` statement** (`:374`): `const mod = await import(SERVICE_PATH)` with non-analyzable variable specifier `SERVICE_PATH = "../../../backend/services/parents/parent-link-request.service"` (`:114`), narrowed by an assertion-free runtime type guard `isParentLinkRequestService` (`:168-173`, all five §4.2 ops must be functions). `tsgo` stays 0 errors; a static import of the missing module would break the type-check.
- **Committed fixtures in ONE `beforeAll` `db.transaction`** (commit-or-nothing cast: Parent A, Parent B, Students S/G/L/F) + governance pre-state via REAL `setGovernanceFixture(suspended)` + already-linked pre-state via REAL `linkStudentToParentFixture(L→A)` — honest fixtures, never monkey-patched.
- **`jrn_plink_<uuid8>` prefix** stamped on every fixture identity field (`fullName`/`email`); handshake codes stay canonical `KSB-<8 hex>` (run-unique by construction); `absentCode` derived (Step A4 grounding).
- **NO `runInRollback`** anywhere (grep-proof §5). Teardown = `TrackedFixtures.cleanup()` (reverse-registration order ⇒ notifications → `parent_link_requests` → role-children → users; REQ-046 order) + FIVE mandatory zero-residue re-probes in `afterAll` (users/students/requests/notifications by cast-id sets + email-prefix probe) on top of TrackedFixtures' own verifyAllAbsent.
- **`SpiedFanoutTransport` at the `options.transport` injection seam** (`callOptions()`), re-armed per step (`transportSpy.clear()`); `expectSinglePublish(targetUserId, relatedEntityId)` pins EXACTLY ONE post-commit publish + its addressee + `NotificationType.ParentLinkRequest` + `relatedEntityType: "parent_link_request"`; `expectZeroPublishes()` on every silent path.
- **REQ-090..096 all wired:** +7d window (±1s, `PARENT_LINK_REQUEST_MS`, TTL=7); masked-name non-disclosure on outgoing; FULL parent name on incoming; foreign-outgoing-empty invariance; null-collapse byte-equality (miss ≡ governed, `JSON.stringify`); REAL re-check denials (anonymous → UNAUTHORIZED ×5 ops, cross-role student→parent-op → FORBIDDEN); `PARENT_LINK_ALREADY_PENDING` (REQ-095); sibling expiry seen by BOTH parents + materialized `expired` row (REQ-091); already-linked zero-notify (REQ-092); byte-equal constant `PARENT_LINK_REQUEST_NOT_FOUND` ×4 arms (foreign/nonexistent × student/parent directions, REQ-093); silent withdrawal → `rejected` fold with inbox parity across the whole cast (REQ-018); genuinely-stale `expiresAt` fixture → `PARENT_LINK_REQUEST_EXPIRED` + materialization + link stays NULL + zero notify (REQ-094); sequential second-confirm → `PARENT_LINK_TARGET_ALREADY_LINKED` with loser claim ROLLED BACK (B12 emulation).
- Zero `audit_logs` rows pinned for the whole flow (Step 9) + byte-stable users snapshots (governance/identity fields unchanged).

## 3. Pipeline evidence

### 2.1.QL — Quality Loop

```
$ bun run scripts/health/sub-loop.ts test/workflows/parents/parent-link-request.journey.test.ts --lifecycle duplicates
✅ tsgo passed → ✅ oxlint passed → ✅ biome:check passed → ✅ lint:type-aware passed → ✅ check:duplicates passed
✅ All checks for lifecycle "duplicates" passed for test/workflows/parents/parent-link-request.journey.test.ts
QL_EXIT=0
```

Defect-fixing round-trips (all root-cause fixed in THIS file, zero suppressions): oxlint — 5×`no-await-in-loop` (Promise.all + module-scope `expectEmptyLinkInboxes` helper, which also de-duplicated three repeated oracle blocks), 2×`no-unsafe-type-assertion` (`errorFingerprint` now takes the already-narrowed `DomainError`; dynamic-import surface narrowed via the `isParentLinkRequestService` type guard instead of `as`), 1×`no-unsafe-enum-comparison` (`PARENT_LINK_NOTIFICATION_TYPE: string` constant widened from the enum — DB column infers a string union), 2×`no-unnecessary-type-assertion` (removed), 2×`no-unnecessary-type-conversion` (String() recipe replaced by the typed constant). ESLint type-aware — 1×`sonarjs/async-test-assertions` + 6×`sonarjs/different-types-comparison` (index/`?.` guards replaced by `.not.toHaveLength(0)` pre-assertions or `.at(0)` narrowing; `expect(p).resolves` replaced by await-then-assert because bun:test's `.resolves` is sync-typed and oxlint `await-thenable` conflicts with ESLint's demand — await-then-assert satisfies both).

### 2.1.TE — Test Engineering (RED verified)

```
$ bun run test/scripts/run-test.ts test/workflows/parents/parent-link-request.journey.test.ts
error: Cannot find module '../../../backend/services/parents/parent-link-request.service' from '/home/z/my-project/test/workflows/parents/parent-link-request.journey.test.ts'
(fail) Journey — parent→child link request workflow (DEV1-014, journeys A→C) > (unnamed) [1.74ms]
 0 pass / 1 fail, 184.00ms · TE_EXIT=1
```

The failure is the EXACT sanctioned mode: `Cannot find module` for `parent-link-request.service`, thrown by the dynamic import as the FIRST `beforeAll` statement (the `(unnamed)` hook — before any test body, before ANY fixture write; 1.74ms ≈ zero DB work). Log: `logs/2026-09-01T15-25-02/test/workflows/parents/parent-link-request.journey.test.ts.log`.

**Zero-residue probe after the RED run** (psql 17, `app_db_test` — rows matching `jrn_plink%` identity through users joins):

| users | students | notifications | parent_link_requests (by parent) | parent_link_requests (by student) | parent_link_requests (total) |
|---|---|---|---|---|---|
| 0 | 0 | 0 | 0 | 0 | 0 |

### Type-check

```
$ bun run tsgo   # scripts/restore-next-env-dts.ts && run-locked-cmd tsgo tsgo -b --noEmit
TSGO_EXIT=0      # 0 errors project-wide
```

### 2.1.SEC — Security & Tenancy Audit: PASS

Denials resolve through the REAL role + governance code path — zero mocking/monkey-patching anywhere in the file (grep `mock|spyOn|mock.module` = 0 hits): governed student G is a REAL `users` row flipped via REAL `setGovernanceFixture` (suspended) and must collapse to the same `null` as a missing code; already-linked L is a REAL `students.parent_id` write via REAL `linkStudentToParentFixture`; anonymous calls pass `ANONYMOUS_ACTOR_ID = 0` into the service's REAL actor re-check (real lookup of a nonexistent user → `UnauthorizedError` on all five ops); cross-role denial passes Student F's REAL student id into the parent-only op (→ `ForbiddenError`). Zero notification side effects on every denial/silent path: `expectZeroPublishes()` + cast-wide inbox-count parity probes after Steps 4, 5, 11 (ALREADY_RESOLVED), 13, 14, 16, 18, 19. No logging fingerprint carries codes/names/emails: the test performs ZERO logging (no `console.*`, no logger calls); its only "fingerprint" is the assertion oracle `errorFingerprint()` = `{code, message}` of the DomainError contract itself — deliberately excluding names/emails — used solely for REQ-093 byte-equality, never emitted.

### 2.1.SR — Semantic Review: PASS

Every service call carries an honest `actorUserId` (the provisioned actor's real `users.id`; the single synthetic id is `ANONYMOUS_ACTOR_ID=0`, used only to represent no-session in the anonymous arm). Per-step assertions exist for BOTH actor visibility AND cross-actor invariance (Steps 2, 3, 7, 8, 11, 13, 15 pair owner-visible oracles with foreign-inbox/foreign-list emptiness; Step 9 pins byte-stability of all six users rows). No `runInRollback` (grep = 0). Teardown order proven by registration mechanics: actors register `users` then role-child; request rows + notifications register later ⇒ reverse-order cleanup deletes notifications → `parent_link_requests` → role-children (`students`/`parents`) → users — the REQ-046 order — with zero-residue re-probes (TrackedFixtures verifyAllAbsent + the five explicit `afterAll` probes).

### 2.1.IV — Instruction Verification: PASS (ZERO doc edits)

`test/workflows/AGENTS.md` rules 1–12: all satisfied (committed single-tx fixtures + tracked cleanup; `jrn_plink_<uuid8>` prefix per the `jrn_<domain>_<uuid8>` pattern; honest authorization via `actor-context` factories; transport spy at the injection seam; no `rejects.toThrow` — `catchJourneyError` + `getServerTranslations("en").errorsTranslations` substrings; `bun:test` only; no console/`any`; `@/` aliases for all static imports; one journey file in the domain subdir; sequential actor-attributed steps; both-direction visibility + denial probes). `docs/testing/workflow-journey-tests.md`: layer placement, fixture lifecycle, honest authorization, notification spying, file org, and the canonical run command all match. `.agents/instructions/tests.instructions.md`: the `runInRollback` mandate is superseded HERE by the journey layer's documented exception (`test/workflows/AGENTS.md` hard rule 1 + the doc's own "NO runInRollback in journeys" section); module-scope `getServerTranslations` matches the convention mandated by AGENTS.md rule 6 and every sibling journey (`handshake-discovery`, `j1`, `j2`, admin lifecycle) — the i18n-hang caution is scoped to rollback DB tests; run-test script used for execution. No genuinely new harness rule surfaced ⇒ no doc edits (the relative-path dynamic import is the task-mandated non-analyzable RED specifier, documented in-file; rule 8 governs static imports, and runtime resolution of a relative specifier is specifier-form-independent at GREEN time).

## 4. Service-surface contract this journey binds to (VERBATIM from plan §4.2 — the 2.2/2.3 implementation target)

```
backend/services/parents/parent-link-request.service.ts
export namespace ParentLinkRequestService {
  export async function requestLink(code, parentActorId, locale, outerTx?, options?: NotificationEngineCallOptions): Promise<OutgoingParentLinkRequestReturnType | null>;
  export async function respondToLinkRequest(requestId, accept, studentActorId, locale, outerTx?, options?): Promise<IncomingParentLinkRequestReturnType>;
  export async function cancelLinkRequest(requestId, parentActorId, locale, outerTx?): Promise<OutgoingParentLinkRequestReturnType>;
  export async function listMyOutgoing(parentActorId, locale, tx?): Promise<OutgoingParentLinkRequestReturnType[]>;
  export async function listMyIncoming(studentActorId, locale, tx?): Promise<IncomingParentLinkRequestReturnType[]>;
}
```

Exported via NEW `backend/services/parents/index.ts` + one root `backend/services/index.ts` line. Call-order pins the journey depends on (from §4.2/§4.4 — verbatim semantics): requestLink returns `null` for BOTH missing-code and governed-target (byte-identical, zero rows/notifications/publishes); `PARENT_LINK_TARGET_ALREADY_LINKED` conflicts; 23505 traverses to `PARENT_LINK_ALREADY_PENDING`; respond classify — foreign ≡ nonexistent → `PARENT_LINK_REQUEST_NOT_FOUND` (constant shape), non-pending → `PARENT_LINK_REQUEST_ALREADY_RESOLVED`, pending-but-stale → materialize `expired` + `PARENT_LINK_REQUEST_EXPIRED`; accept-path claim THEN guarded `linkParentIfUnlinked` (loser throw rolls back the whole claim tx) THEN sibling expiry THEN one accepted notify to the parent; reject-path = notify only, NO sibling expiry; cancel = silent fold to `rejected`, ZERO notifications; outgoing rows expose `studentMaskedName` only; incoming rows expose `parentFullName` only; reads render `expiresAt <= now` pendings as `LinkStatus.Expired` WITHOUT writing. The in-file mirror interface `ParentLinkRequestServiceContract` (test `:120-152`) is cast-checked at load by `isParentLinkRequestService`.

## 5. Carry-forwards

1. **TRUE concurrent race proofs → 5.2 chaos tier (REQ-072):** the journey covers ONLY the SEQUENTIAL race-loser emulation (Step 19). 5.2 owns: `Promise.allSettled` duplicate-create race (ONE row + ONE conflict — skip under pglite), two-parent confirm race (ONE winner, single linked parent, single parent-facing notification), confirm-on-`expiresAt` boundary determinism, forced post-claim failure → ZERO residual rows across `parent_link_requests`/`students`/`notifications`.
2. **RED→GREEN transition (2.3):** the test needs ZERO edits to turn GREEN — it only requires the §4.2 namespace surface at `backend/services/parents/parent-link-request.service.ts` (see §4). Every later task touching the service MUST re-run the TE command and keep it runnable (tasks.md 2.1.TE).
3. **Environment note (not a defect):** this sandbox's `test/scripts/run-test.ts` resolves `BUN_BIN = ~/.bun/bin/bun`; the rebuilt environment installs bun at `/usr/local/bin/bun`. Fixed OUTSIDE the repo with a symlink (`/home/z/.bun/bin/bun` → `/usr/local/bin/bun`) — no tracked file touched; if the environment is wiped again, recreate the shim or run-test fails with ENOENT before running any test.
4. **Expired-row cleanup semantics (informational for 2.2):** the journey registers fixture/service-created request rows for tracked teardown; `expired`/`rejected` rows are deleted by id in teardown — no repository bulk-delete dependency.
