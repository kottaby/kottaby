# Task 1.4 Outcome — Canonical types: link-request types + students link-target row + barrels

**Task:** 1.4 (Phase 1) · **REQ:** REQ-003 · plan §2.3
**Plan:** `ai/plans/sprint_3/dev1-014-parent-child-link-request-workflow-7-day`
**Branch state at task start/end:** repo on `main` @ `4eb3fbc` (same state 1.1/1.2/1.3 recorded — the Phase-0 commit; the `feat/dev1-014-…` branch tip is stale at `a259524`). Stayed on `main` with the uncommitted parallel Phase-1 work in the tree — checking out the stale branch would drop the Phase-0/1.x outcome files. No git operations, no commits, no stashes.
**Date:** 2026-09-01

---

## 1. Summary

Centralized the canonical link-request type surface in the types layer, strictly additively:

1. **`backend/types/parents/parent-link-request.types.ts` (CREATE)** — EXACTLY the plan §2.3 declarations:
   - `ParentLinkRequestSelectType = typeof parentLinkRequests.$inferSelect`
   - `ParentLinkRequestInsertType = typeof parentLinkRequests.$inferInsert`
   - `OutgoingParentLinkRequestReturnType` — closed readonly interface `{ id, status: LinkStatus, studentMaskedName, createdAt, expiresAt, respondedAt }`
   - `IncomingParentLinkRequestReturnType` — closed readonly interface `{ id, status: LinkStatus, parentFullName, createdAt, expiresAt, respondedAt }`
   Both return shapes carry ONLY the fields the GraphQL objects promise (field-for-field identical to the SDL surface — BOPLA at the return-type boundary). Foundation types derive from the 1.2 schema export via the canonical `$inferSelect`/`$inferInsert` pattern (deep import `@/backend/db/schema/parents/parent-link-requests`, mirroring the `parent.types.ts`/`student.types.ts` exemplars). `LinkStatus` is imported from the deep enum path `@/backend/enum/shared/link-status.enum` at TYPE positions only (`import type`, per 1.3's carry-forward), with the same NOTE comment the `applicant.types.ts`/`notification.types.ts` precedents carry (Biome `lint/style/useImportType` normalization; runtime consumers keep their own value imports).
2. **`backend/types/parents/index.ts` (UPDATE — ONE line)** — added `export * from "./parent-link-request.types";`. Biome's assist normalizes the barrel to `./parent.types` first, then `./parent-link-request.types` (verified stable across `biome check --write` runs; both orders were probed — the final on-disk order is biome-canonical and QL-clean). The root types barrel `backend/types/index.ts` ALREADY re-exports `./parents` (`:33`) and `./students` (`:34`), so the new types flow to every `@/backend/types` consumer with **zero root-barrel edits** (same wiring situation as 1.2's top schema barrel).
3. **`backend/types/students/student.types.ts` (UPDATE — ADDITIVE, +20 lines)** — appended `StudentLinkTargetRowType` (readonly interface: `studentId: number`, `parentId: number | null`, `fullName: string`, `isDeleted / isBlocked / suspended: boolean | null`, `suspendedAt: Date | null`, `suspendedPeriodDays: number | null`) — the server-internal link-target resolution row for the write path. Nullabilities match the live columns exactly (`users.is_deleted/suspended/is_blocked/suspended_at/suspended_period_days` are nullable-with-default; `students.parent_id` is `SET NULL`; `students.id` non-null; `users.full_name` non-null). JSDoc pins: server-internal, NEVER a GraphQL payload, parent-facing payload remains `HandshakeCodeLookupReturnType`; carries the raw `studentId` (unlike `HandshakeDiscoveryRowType`) so the 2.2 repo read + 2.3 service can address the target row directly. The structurally-satisfies relation holds: `StudentLinkTargetRowType` ⊇ `Omit<HandshakeDiscoveryRowType, "parentId">`, so the existing `isGovernanceExcludedFromDiscovery(governance, now)` predicate accepts it unchanged.

**Behavior-change audit:** zero. Pure type declarations — no runtime code, no enum/schema/repo/service/frontend edits, no new dependencies between layers beyond the sanctioned Schema→Types direction. Repo-wide grep proves ZERO consumers of the five new export names outside their defining files (2.2/2.3/3.x/4.1 are the designated consumers) — the modules are import-inert until then. **NO service-layer `.types.ts` file was created anywhere** (hard rule honored).

## 2. Files created / modified (anchors)

| File | Action | Anchor |
|---|---|---|
| `backend/types/parents/parent-link-request.types.ts` | CREATE | imports `:1` + `:8`; `ParentLinkRequestSelectType` `:10`; `ParentLinkRequestInsertType` `:11`; `OutgoingParentLinkRequestReturnType` `:31-38`; `IncomingParentLinkRequestReturnType` `:57-64` |
| `backend/types/parents/index.ts` | UPDATE (+1 line) | `:1` = `export * from "./parent.types";` (pre-existing, biome-canonical first slot), `:2` = `export * from "./parent-link-request.types";` |
| `backend/types/students/student.types.ts` | UPDATE (+20 lines, append-only) | `HandshakeDiscoveryRowType` `:23-24` untouched; `StudentLinkTargetRowType` interface `:35-44` with JSDoc `:26-34` |

**NOT modified (and why):** `backend/types/index.ts` (root barrel already re-exports `./parents` + `./students` — verified at `:33`/`:34`; no edit needed); `backend/db/schema/**` (types layer reads it via `import type` — frozen per REQ-045); `backend/enum/shared/link-status.enum.ts` (1.3's file consumed via type-only import — no edit); `backend/types/parents/parent.types.ts` (exemplar, read-only); `backend/types/students/student.types.test-d.ts` (pre-existing shape-proof suite — task text prescribes NO new test file for 1.4, per the 1.3 precedent of adding only mandated tests); NO repo/service/GraphQL/frontend files (later tasks); NO service-layer `.types.ts` anywhere (hard rule).

## 3. Quality Loop (1.4.QL) — exit 0 on ALL THREE files

```
$ bun run scripts/health/sub-loop.ts backend/types/parents/parent-link-request.types.ts --lifecycle duplicates
✅ tsgo passed → ✅ oxlint → ✅ biome:check → ✅ lint:type-aware → ✅ check:duplicates
✅ All checks for lifecycle "duplicates" passed for backend/types/parents/parent-link-request.types.ts
        (ql-types-exit=0; raw: /tmp/ql-14-types.log)

$ bun run scripts/health/sub-loop.ts backend/types/parents/index.ts --lifecycle duplicates
✅ All checks for lifecycle "duplicates" passed for backend/types/parents/index.ts
        (ql-barrel-exit=0; raw: /tmp/ql-14-parents-barrel.log)

$ bun run scripts/health/sub-loop.ts backend/types/students/student.types.ts --lifecycle duplicates
✅ All checks for lifecycle "duplicates" passed for backend/types/students/student.types.ts
        (ql-students-exit=0; raw: /tmp/ql-14-students-types.log)
```

The parents barrel was re-run a FINAL time on the exact final on-disk content after a barrel-ordering probe (below) — exit 0 again. Zero lint iterations were needed on any file (no oxlint/ESLint/sonarjs findings); the only normalization was Biome's own assist re-ordering the two `export *` lines in the barrel (cosmetic, deterministic, now stable).

## 4. Test Engineering (1.4.TE) — full-tree `bun tsgo`

Task text prescribes tsgo-only for 1.4 (no test file mandated — none added, matching the 1.3 disposition rule). Verbatim tail:

```
$ bun tsgo 2>&1 | tee /tmp/tsgo-14.log
$ bun run scripts/restore-next-env-dts.ts && bun run scripts/lib/run-locked-cmd.ts tsgo tsgo -b --noEmit
[process-lock] Enqueued request for "tsgo" (PID: 14521)
[process-lock] Acquired lock for "tsgo" (PID: 14521). Executing...
[process-lock] Released lock for "tsgo" (PID: 14521)
tsgo-exit=0
--- error-TS count: 0
```

**Error count == 0.1 baseline (0).** This covers the whole tree including the parallel 1.1/1.2/1.3 uncommitted work and the three new/edited type files — the `$inferSelect`/`$inferInsert` derivation against the live 1.2 schema export and the `LinkStatus` type import both compile clean.

## 5. Security & Tenancy Audit (1.4.SEC)

- **Return-type boundary = field-for-field BOPLA compliance.** `OutgoingParentLinkRequestReturnType` (6 fields) and `IncomingParentLinkRequestReturnType` (6 fields) are exactly the fields the GraphQL `OutgoingParentLinkRequest` / `IncomingParentLinkRequest` objects promise. Both are CLOSED readonly interfaces — no parent FK, no student id, no raw `fullName`, no governance columns, no audit/timestamps beyond the promised three. There is no field a client could read that the surface does not declare.
- **Masking discipline:** the outgoing shape exposes the student ONLY as `studentMaskedName` (the deterministic `maskFullName` output — same masking ownership as the handshake discovery chain); the raw student identity stays server-side inside `StudentLinkTargetRowType`/`ParentLinkRequestSelectType`. The incoming shape exposes the parent as `parentFullName` — the deciding student must see WHO asks; the parent user id is NOT exposed.
- **`StudentLinkTargetRowType` is NEVER serialized over the wire — proven, not just declared:** (a) repo-wide grep shows ZERO consumers outside its defining file — no Pothos object ref, no resolver, no GraphQL document references it; (b) it lives only in the backend types layer, which the frontend cannot import (layer isolation); (c) its JSDoc pins "NEVER surfaces through GraphQL (the parent-facing payload remains HandshakeCodeLookupReturnType)". Its sole purpose is to feed the 2.2 repo read (`findLinkTargetByHandshakeCode`) and the 2.3 service governance re-check — the governance columns (`isDeleted/isBlocked/suspended/suspendedAt/suspendedPeriodDays`) exist so the REQ-031-style exclusion predicate runs server-side BEFORE anything is mapped to a return type.
- **`SelectType`/`InsertType` are not wire types.** The Pothos layer binds object refs to the `*ReturnType` names (backend.instructions.md mandate); the repo consumes `SelectType`/`InsertType` internally. `InsertType`'s required trio `{parentId, studentId, expiresAt}` is constructed server-side only — the client supplies ONLY the handshake `code`, never ids, so there is no mass-assignment/BOPLA-write surface on this entity.
- **No secrets/PII on any type:** no `email`, `phone`, `passwordHash`, locale, or credential fields anywhere in the three files.
- **Tenancy:** all return shapes are self-scoped by construction (actor-scoped lists/respond/cancel are service-enforced in 2.x); the type layer adds no cross-tenant read path.

## 6. Semantic Review (1.4.SR) — FULL SKILL.md checklist

- [x] **Race conditions & concurrency:** N/A — pure type declarations; no runtime code, no read-then-write, no module-level mutable state, no async, no Redis.
- [x] **Environment & configuration:** no `resolveEnvConfig` calls, no cache invalidation, no credential setters (nothing to invalidate — declarations only).
- [x] **No dead branches / dead code:** zero branches; every declared type has a designated consumer in 2.2/2.3/3.x/4.1 per the plan's repo/service/GraphQL signatures (grep-verified zero accidental consumers today — inert-by-design, not dead).
- [x] **No cross-layer imports:** grep `@/frontend|@/app|console\.` over all three files = **0 hits**. Imports are exactly two, both DOWNWARD in the sanctioned 6-layer flow (Schema → Types): the schema table (`import type`) and the backend enum (`import type`).
- [x] **No manual ReturnType construction:** foundation derives from `typeof parentLinkRequests.$inferSelect` / `$inferInsert` — the canonical AGENTS.md pattern, no hand-rolled column shapes (same discipline as `HandshakeDiscoveryRowType`'s "no re-derived column shapes"). The return types are explicit closed interfaces — the sanctioned form for wire shapes whose field set is deliberately NOT the row shape (precedents: `HandshakeCodeLookupReturnType`, `ApplicantProfileReturnType`). The `toReturnType` runtime helper (`registration.service.ts`) is a runtime object-mapping utility — nothing here maps objects at runtime, so it is not applicable.
- [x] **Clean comments & JSDocs — ZERO plan-artifact references:** grep `REQ-[0-9]|Task [0-9]|Phase [0-9]|plan.md|tasks.md|specs.md` over all three files = **0 hits**. Comments describe domain behavior (wire-shape closure, masking ownership, guard-validated status, server-internal row purpose). The enum-import NOTE mirrors the live `applicant.types.ts`/`notification.types.ts` precedents verbatim in structure. No noisy trivial comments.
- [x] **Schema & types parity:** no migration touched; `StudentLinkTargetRowType`'s nullabilities were checked field-by-field against the LIVE Drizzle columns (`users.ts:15,30-35`, `students.ts:21,32`) and match exactly; `$inferSelect` names ↔ column names verified at 1.2 and unchanged.
- [x] **Enums as VALUE imports — type-position nuance handled per precedent:** `LinkStatus` appears ONLY in type positions in this file, so `import type` is the correct (and Biome-enforced) form; the file's NOTE comment explicitly warns that runtime consumers (2.x `isLinkStatus` read-mapping, status transitions, 3.x `gqlSchemaBuilder.enumType(LinkStatus, …)`) MUST keep their OWN value imports. No string literals stand in for enum members anywhere.
- [x] **DB column names match `$inferSelect` names:** N/A for edits (no schema change); the two foundation types re-derive from the table expression, so they cannot drift.
- [x] **No deferred work without ledger entry:** none discovered (§8).
- [x] **Scope boundary:** `git status` for this task = `M backend/types/parents/index.ts` (+1), `M backend/types/students/student.types.ts` (+20, append-only), `?? backend/types/parents/parent-link-request.types.ts`. Zero refactors outside scope; schema/enum/service/graphql trees untouched by 1.4.
- [x] **No local resolver types:** none created anywhere; the plan's 3.x Pothos objects bind `objectRef<OutgoingParentLinkRequestReturnType>` / `objectRef<IncomingParentLinkRequestReturnType>` from `@/backend/types` — all types centralized in this layer.

## 7. Instruction Verification (1.4.IV)

- **`.agents/instructions/backend.instructions.md`** (the sub-loop prints `.github/instructions/backend.instructions.md` as a ⚠ missing path — that mirror does not exist in the tree; `.agents/instructions/` is the only live copy and was verified directly):
  - 6-layer flow respected — types layer only; Schema→Types direction is the only import direction added.
  - Type Definition Pattern (CRITICAL) — `SelectType`/`InsertType` foundation exactly as prescribed; `*ReturnType` naming; "ALL types from `@/backend/types` — NEVER local definitions in Pothos files" — this file IS the central definition the 3.x resolvers will import; "Update `backend/types/index.ts` when adding new entity types" — satisfied transitively: the root barrel already re-exports `./parents`, so no edit is needed (verified `:33`; same precedent as 1.2's top schema barrel).
  - Barrel conventions (CRITICAL) — `./` relative path, `export *` exclusively, no imports, ≤1 `/` per path, no `../`, no aliasing ✓.
  - Pothos section — `nullable: true` mapping note and object-ref-uses-ReturnType both honored by the shapes handed forward (`respondedAt: Date | null` will map to `respondedAt: DateTime` nullable).
  - No `console.*`, no hardcoded error strings (no runtime paths at all), no nested ternaries, bun-only commands, never `oxlint-disable` ✓.
- **`backend/types/AGENTS.md`:** Single Canonical Object Type Pattern ✓ (`{Entity}SelectType`/`{Entity}InsertType`/`{Entity}ReturnType` naming; no duplicate/similar types for the same entity — `StudentLinkTargetRowType` is a distinct server-internal ROW type declared in the sanctioned sibling manner, not a competing canonical type for `students`, exactly like the pre-existing `HandshakeDiscoveryRowType` beside it); Types Location Rules (CRITICAL) ✓ (`.types.ts` only under `backend/types/`; barrels use `./` relative `export *`, no `@/` aliases, no `../`, no explicit per-export `export type {}`); GraphQL Integration ✓ (readonly closed shapes, enums properly typed via the shared TS enum).
- **`backend/AGENTS.md`:** Standard Type Definition Pattern (§2) ✓; "Use types from `backend/types/` as the foundation for GraphQL object types" ✓; single canonical object type rule ✓.

## 8. Deferred-items check — new out-of-scope discoveries

- **None.** No new ledger rows; `deferred-items.md` NOT modified. Open ❌/⚠️ rows in the Ledger Table remain **0** (D4 ✅; D5 🔄 owned by 2.1; D1–D3, D6, D7 📅 Forward).
- Cosmetic observation (documented here only, no action): Biome's assist sorts `export *` lines in barrels by its own comparator (it moved `./parent-link-request.types` AFTER `./parent.types`, while leaving 1.2's schema barrel `./parent-link-requests` BEFORE `./parents` — both states are biome-stable). Barrel ordering is therefore delegated to `biome check --write` and should not be hand-fought in future barrel edits; both existing orders pass QL.

## 9. Carry-forward knowledge (exact names for consumers)

**For 2.2 (repository):**
- `import type { ParentLinkRequestSelectType, ParentLinkRequestInsertType, StudentLinkTargetRowType } from "@/backend/types";` (root barrel — shortest path; deep paths `@/backend/types/parents/parent-link-request.types` / `@/backend/types/students/student.types` also resolve).
- `ParentLinkRequestInsertType` required trio = `{ parentId, studentId, expiresAt }` (status/createdAt from DB defaults; id omitted — identity PK), exactly as 1.2 pinned.
- `findLinkTargetByHandshakeCode(...): Promise<StudentLinkTargetRowType | null>` — the row shape is live; its `studentId`/`parentId`/governance columns are the 2.3 write-path inputs; the shape structurally satisfies `Omit<HandshakeDiscoveryRowType, "parentId">`, so `isGovernanceExcludedFromDiscovery(target, now)` accepts it unchanged.

**For 2.3 (service):**
- Map reads to `OutgoingParentLinkRequestReturnType` / `IncomingParentLinkRequestReturnType` (`import type ... from "@/backend/types"`); `status` MUST be a `LinkStatus` VALUE usage at runtime (`LinkStatus.Pending`/`Confirmed`/`Rejected`/`Expired` + `isLinkStatus` fail-closed read-mapping) — the types file deliberately imports the enum as `import type` only, so the service owns the runtime import.
- Outgoing rows carry `studentMaskedName` = `maskFullName(target.fullName)` (never raw `fullName`); incoming rows carry the parent's `fullName` verbatim as `parentFullName`.
- `ParentLinkRequestSelectType` is the classifier/`respondToPendingForStudent`/`cancelPendingForParent`/`markExpiredIfPending` row type (`.status`, `.studentId`, `.parentId`, `.expiresAt` reads).

**For 3.x (GraphQL):**
- `objectRef<OutgoingParentLinkRequestReturnType>("OutgoingParentLinkRequest")` / `objectRef<IncomingParentLinkRequestReturnType>("IncomingParentLinkRequest")` — field names already match the SDL 1:1 (`id` first, `status: LinkStatus!`, `studentMaskedName`/`parentFullName: String!`, `createdAt`/`expiresAt: DateTime!`, `respondedAt: DateTime` nullable). NO local types in `.pothos.ts` (hard rule already satisfied by construction).
- Register the `LinkStatus` Pothos enum-object ONCE from a VALUE import of `@/backend/enum/shared/link-status.enum` (the types file's NOTE points runtime consumers here).

**For 4.1 (frontend documents) / 4.x:** the codegen'd fragments for the two objects will carry exactly these six fields each; nothing else exists to select.

## 10. Cross-file dependencies established

1. `backend/types/parents/parent-link-request.types.ts` → `import type { parentLinkRequests } from "@/backend/db/schema/parents/parent-link-requests"` (1.2's export; `$inferSelect`/`$inferInsert` pinned by 1.2's smoke test) + `import type { LinkStatus } from "@/backend/enum/shared/link-status.enum"` (1.3's enum file; type-only here).
2. `backend/types/parents/index.ts` → 2-line barrel; flows through `backend/types/index.ts:33` to every `@/backend/types` consumer with zero further edits.
3. `backend/types/students/student.types.ts` → `StudentLinkTargetRowType` sits beside `HandshakeDiscoveryRowType` (which is untouched, append-only diff) and `HandshakeCodeLookupReturnType` (still the ONLY parent-facing student payload).
4. 2.2/2.3/3.x are the designated first consumers (§9) — until they land, the five new exports have zero references (inert modules, zero behavior change).

## 11. Outcome-file QL (ledger D6 disposition)

Per D6, `sub-loop.ts` is NOT run on `.md` targets (would exit 1 on oxlint's empty file set); compensating direct jscpd:

```
$ bunx jscpd -c .jscpd.json ai/plans/sprint_3/dev1-014-parent-child-link-request-workflow-7-day/outcome/1.4-types-outcome.md
Found 0 clones.        (jscpd-outcome-exit=0)
```
