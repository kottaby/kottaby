# Outcome — Task 3.3: Codegen + schema-surface baseline reconcile-then-extend (TWO documented steps in ONE changeset)

- **Task ID:** 3.3 (REQ-061 · plan §3.3 · tasks.md :322-339)
- **Status:** ✅ COMPLETE — QL exit 0 ×2, TE 74/74 across the three suites, `bun tsgo` 0 errors, SEC/SR/IV all PASS, codegen artifacts regenerated in the same changeset
- **Agent:** general-purpose subagent
- **Date:** 2026-09-01
- **Branch:** `feat/dev1-014-parent-child-link-request-workflow-7-day` (HEAD `db69f38` at dispatch)

## Pre-task state (why reconcile was mandatory)

The three committed baselines had gone stale against the already-shipped surfaces while the DEV1-014 feature branch carried them forward un-refreshed:

- `backend/graphql/test/schema-surface.test.ts` (`PRE_3_1_*` @ :91-146 pre-task): predated the **DEV3-016 admin surface** (and the whole-schema delta literal predated nothing else).
- `backend/graphql/test/sdl-static-assertions.test.ts` (`FROZEN_*` @ :67-100 pre-task): predated the **DEV1-005 plan-catalog CRUD**, the **DEV1-013 student-handshake queries**, and the **DEV3-016 admin surface**.
- Committed `frontend/graphql/generated/schema.graphql` predated the 3.1/3.2 registrations (codegen deliberately deferred to 3.3 per the 3.1+3.2 outcome).

RED evidence captured BEFORE any edit (both suites ran against the live builder + regenerated SDL):

| Suite | Pre-edit |
|---|---|
| schema-surface.test.ts | 29 pass / **4 fail** (query root freeze, mutation freeze, enum freeze, named-type delta) |
| sdl-static-assertions.test.ts | 15 pass / **3 fail** (Mutation root freeze, Query root freeze, NO-Subscription lexical scan) |
| plan-catalog.schema.test.ts | 5 pass / 0 fail (byte-parity restored the moment the SDL was regenerated) |

## STEP 1 — Re-anchor (reconcile; NEVER silent)

`bun run generate:gqlSchema` rebuilt `frontend/graphql/generated/schema.graphql` from the live built schema (13,186 chars / 13,210 bytes written). The rebuild's SDL diff vs the committed artifact is EXACTLY the DEV1-014 parent-link surface (see STEP 2) — i.e. the committed artifact had been frozen at the pre-3.1 state and the reconcile brought the test arrays (not the schema) up to the live surface.

### Baseline anchor changes — every entry (old → new)

`schema-surface.test.ts` `PRE_3_1_*`:

| Array | Old (10 entries max shown count) | Added (reconcile — DEV3-016) | Added (extend — DEV1-014) | New count |
|---|---|---|---|---|
| `PRE_3_1_QUERY_FIELDS` | 7 | `adminUserActivity`, `adminUserDetail`, `adminUsers`, `adminUserStats` | `myIncomingParentLinkRequests`, `myOutgoingParentLinkRequests` | 13 |
| `PRE_3_1_MUTATION_FIELDS` | 10 | `adminCreateUser`, `adminSetUserDeleted`, `adminUpdateUser` | `cancelParentLinkRequest`, `requestParentChildLink`, `respondToParentLinkRequest` | 16 |
| `PRE_3_1_ENUMS` | 7 | `AdminUserGovernanceFilter`, `AuditActionType` | `LinkStatus` | 10 |
| `PRE_3_1_TYPE_NAMES` | 21 | `AdminCreateUserInput`, `AdminParentSnapshot`, `AdminStudentSnapshot`, `AdminTeacherSnapshot`, `AdminUpdateUserInput`, `AdminUserActivityEntry`, `AdminUserDetail`, `AdminUserFiltersInput`, `AdminUserListItem`, `AdminUserPage`, `AdminUserStats`, `AdminUserGovernanceFilter`, `AuditActionType` (the named-type inventory includes enum names, mirroring the original array) | `LinkStatus` | 35 |

`sdl-static-assertions.test.ts` `FROZEN_*`:

| Array | Old | Added (reconcile) | Added (extend) | New count |
|---|---|---|---|---|
| `FROZEN_MUTATION_FIELDS` | 7 (`login`, `logout`, `markAllNotificationsRead`, `markNotificationRead`, `refreshToken`, `registerUser`, `updateMyLocale`) | `createPlan`, `setPlanActiveStatus`, `updatePlan` (DEV1-005), `adminCreateUser`, `adminSetUserDeleted`, `adminUpdateUser` (DEV3-016) | `cancelParentLinkRequest`, `requestParentChildLink`, `respondToParentLinkRequest` | 16 |
| `FROZEN_QUERY_FIELDS` | 6 (`_health`, `me`, `myApplicantProfile`, `myNotifications`, `myUnreadNotificationCount`, `recitationReadings`) | `adminPlans`, `planCatalog` (DEV1-005), `findStudentByHandshakeCode`, `myHandshakeCode` (DEV1-013), `adminUserActivity`, `adminUserDetail`, `adminUsers`, `adminUserStats` (DEV3-016) | `myIncomingParentLinkRequests`, `myOutgoingParentLinkRequests` | 16 |

Delta literal (schema-surface `whole-schema named-type delta`): `["DateTime", "HandshakeCodeLookup", "HealthCheck"]` → `["DateTime", "HandshakeCodeLookup", "HealthCheck", "IncomingParentLinkRequest", "OutgoingParentLinkRequest"]` (the two parent-link OBJECTS are the extend step's pinned delta; `LinkStatus` itself joined the baseline).

Re-worded stale entries (listed one by one — the ONLY places where existing baseline text changed meaning):

1. **`sdl-static-assertions.test.ts` "NO Subscription root" lexical scan** — `expect(sdlText).not.toContain("Subscription")` → `expect(sdlText).not.toContain("type Subscription")`. Root cause: the raw token scan false-positived on the DEV3-016 field names `AdminStudentSnapshot.hasActiveSubscription` / `studentHasActiveSubscription`. The invariant is about the ROOT operation type, so the scan is narrowed to the type-declaration token (the AST-level "no object type named Subscription" check is unchanged and still load-bearing). In-file comment records the reword.
2. **Test title** "Mutation root is EXACTLY the refreshed frozen **7-op** baseline" → "…refreshed frozen baseline" (the count is no longer 7).
3. **Test title** "whole-schema named-type delta … DEV1-013 handshake-code surface" → "…+ the DEV1-014 parent-link objects (extend step)".
4. **Header doc-comments + array doc-comments + baseline-section banners** in both files rewritten to carry the reconcile-then-extend growth history (traceability-in-code).

Monotonic-growth proof: NO baseline entry was removed or renamed — every old entry survives verbatim in the new arrays (verified entry-by-entry against the pre-edit file; also proven by the freeze tests themselves, which compare the FULL live root sets to the arrays).

## STEP 2 — Extend (pins added to the now-current baselines)

Pins derived from `outcome/3.1-3.2-graphql-surface-outcome.md` §"Exact surface for 3.3 pinning" and confirmed 1:1 against the rebuilt SDL (zero wire-name drift — the static member check held):

| Pin | Where pinned (built schema = schema-surface.test.ts · artifact = sdl-static-assertions.test.ts) |
|---|---|
| `myOutgoingParentLinkRequests: [OutgoingParentLinkRequest!]!` — NON-paginated, ZERO args | both |
| `myIncomingParentLinkRequests: [IncomingParentLinkRequest!]!` — NON-paginated, ZERO args | both |
| `requestParentChildLink(code: String!): OutgoingParentLinkRequest` — the ONLY nullable new mutation | both |
| `respondToParentLinkRequest(requestId: ID!, accept: Boolean!): IncomingParentLinkRequest!` | both |
| `cancelParentLinkRequest(requestId: ID!): OutgoingParentLinkRequest!` | both |
| `LinkStatus` enum presence + member set {Pending, Confirmed, Rejected, Expired} (+ lowercase runtime values + single-source agreement with the TS enum) | both |
| Both objects expose EXACTLY the six canonical fields (`id` `ID!`, `status` `LinkStatus!`, counterparty `String!`, `createdAt`/`expiresAt` `DateTime!`, `respondedAt` nullable `DateTime` — the ONLY nullable field) | both |
| `id`-FIRST invariant via source scan (both `fields: t => ({ … })` blocks in `parent-link-request.pothos.ts`, mirrored from the Notification precedent) | schema-surface |
| Surface probe — full pinned selections validate against the live schema with zero errors (2 list queries + 3 mutations, six-field selections) | schema-surface |
| Anonymous (context-free) in-process execution of ALL FIVE root fields → UNAUTHORIZED (scope-gated, never public) | schema-surface |
| Smuggled identity args (`studentId`/`parentId` on the new fields) die at validation BEFORE any resolver runs | schema-surface |
| NO parent-link page/connection wrapper exists (`ParentLinkRequestListPage`/`ParentLinkRequestConnection` absent; the only `*ParentLinkRequest*`-named SDL types are the two objects) | artifact |
| NO `String` leakage on the six timestamps | both |

New describe blocks: `"Parent-link surface (DEV1-014 extend) — five pinned root fields + LinkStatus + canonical objects"` (9 tests, schema-surface.test.ts) and `"Parent-link surface (DEV1-014 extend) — artifact-side pins (REQ-061)"` (8 tests, sdl-static-assertions.test.ts).

## Codegen artifacts (same changeset)

- `frontend/graphql/generated/schema.graphql` — REGENERATED (+50 diff lines: `enum LinkStatus {Confirmed Expired Pending Rejected}`, `type IncomingParentLinkRequest`, `type OutgoingParentLinkRequest`, the 3 mutation fields with their descriptions, the 2 query fields with their descriptions). Byte-identical to `printSchema(lexicographicSortSchema(graphQLSchema))` (proven by two suites).
- `frontend/graphql/generated/gql/graphql.ts` — re-ran `bun codegen` (exit 0, "Generate outputs" ✔); output is byte-IDENTICAL to the committed artifact because no GraphQL document changed yet (parent-link documents are task 4.1; `ignoreNoDocuments: true`). No spurious churn committed.
- No other tracked file touched by codegen.

## Pipeline evidence

| Gate | Command | Result |
|---|---|---|
| 3.3.QL (1/2) | `bun run scripts/health/sub-loop.ts backend/graphql/test/schema-surface.test.ts --lifecycle duplicates` | **exit 0** (lint:type-aware ✅; check:duplicates skipped — outside jscpd scan scope) |
| 3.3.QL (2/2) | `bun run scripts/health/sub-loop.ts backend/graphql/test/sdl-static-assertions.test.ts --lifecycle duplicates` | **exit 0** (same scope notes) |
| 3.3.TE (1/3) | `bun run test/scripts/run-test.ts backend/graphql/test/schema-surface.test.ts` | **43 pass / 0 fail / 282 expects** |
| 3.3.TE (2/3) | `bun run test/scripts/run-test.ts backend/graphql/test/sdl-static-assertions.test.ts` | **26 pass / 0 fail / 89 expects** |
| 3.3.TE (3/3) | `bun run test/scripts/run-test.ts backend/graphql/test/plan-catalog.schema.test.ts` | **5 pass / 0 fail / 22 expects** — committed-vs-live SDL byte-parity GREEN |
| Type-check | `bun run tsgo` (full project) | **exit 0, 0 errors** |

All TE gates ran in the two-step order (reconciled-then-extended baselines, regenerated artifact) — totals: **74 pass / 0 fail / 393 expects** across the three suites.

## 3.3.SEC verdict — PASS

- **Frozen `PUBLIC_OPERATIONS` six NOT de-registered:** `backend/lib/gateway/public-operations.ts` is byte-untouched (`git diff` empty — still `login`, `refreshToken`, `logout`, `registerUser`, `recitationReadings`, `_health`); the "Allowlist agreement" + "anonymous reachability" tests stay GREEN (schema↔allowlist 1:1 unchanged).
- **No new root fields beyond the five pinned:** both freeze tests compare the FULL live root sets to the refreshed baselines (any 6th root field would fail), the named-type delta literal is pinned EXACTLY, and the regenerated-SDL diff contains ONLY the five root fields + `LinkStatus` + the two objects. Additionally, all five new fields reject anonymous in-process execution with UNAUTHORIZED — none are public, none joined the allowlist.
- No identity-arg surface on the five fields (smuggled `studentId`/`parentId` probes die as validation errors, pinned in-suite).

## 3.3.SR verdict — PASS

- Reconcile is documented and traceable: growth-history banners in both test files, per-entry `// DEV3-016 reconcile:` / `// DEV1-014 extend:` inline comments in every baseline array, the reworded Subscription scan carries its own in-file explanation, and this outcome file lists every anchor change old → new.
- Baselines grew monotonically: zero deletions/renames of stale entries (all survive verbatim); the only semantic rewordings are the four items listed above, each recorded.
- The extend pins mirror the canonical contract from the 3.1+3.2 outcome with ZERO drift (the "if wire-name drift appears, fix the enum doc-header" carry-forward resolved as: no drift found, doc-header untouched).

## 3.3.IV verdict — PASS (zero doc edits)

- `.agents/instructions/backend.instructions.md` untouched (git status: only the two baseline test files + the SDL artifact + tasks.md/outcome/worklog changed).
- Architectural Invariant 11 (scalar registration) CONFIRMED, NOT ADDED: `DateTime` is registered exactly once in-tree (`backend/graphql/pothos/shared/scalar.pothos.ts:28`, `addScalarType("DateTime", DateTimeResolver)` — unchanged in this changeset); the six new timestamp fields reference it BY NAME (`type: "DateTime"`) exactly as the module contract requires; the SDL declares `scalar DateTime` once; `codegen.ts` already maps `DateTime → string` so no codegen config change was needed.

## Bookkeeping

- tasks.md: `### 3.3 [x]` + `3.3.QL/TE/SEC/SR/IV` all `[x]`.
- This outcome file written before the orchestrator's commit; worklog `---` section appended.
- NO commit/push performed (orchestrator owns git).

## Carry-forwards / risks for Phase 4

1. **4.1 documents:** the frontend documents must match the pinned signatures above EXACTLY (especially `requestParentChildLink`'s NULLABLE payload and the zero-arg lists); `frontend/graphql/generated/gql/graphql.ts` will GROW in 4.1 when the five documents land (codegen re-run expected to diff then — today's byte-identical re-run is the baseline for that diff).
2. **5.1 wire matrix:** the 401/403/validation split for the five fields is deferred there per tasks.md 3.2.TE; this task's anonymous-UNAUTHORIZED pins (in-process) are complementary, not a substitute.
3. **Environment quirk:** the sandbox auto-checks-out `main` whenever the work tree is CLEAN; keep the tree dirty (tasks.md `[-]`/`[x]` edit first) or re-run `git checkout -f feat/dev1-014-…` before operations that require the branch. Commits are unaffected (observed across tasks 2.1-3.2 and here).
4. **Byte-parity gates:** any Phase 4+ change to Pothos registrations MUST re-run `bun run generate:gqlSchema && bun codegen` in the same changeset — two suites (schema-surface "Codegen sync", plan-catalog "committed-vs-live") fail closed on drift.
