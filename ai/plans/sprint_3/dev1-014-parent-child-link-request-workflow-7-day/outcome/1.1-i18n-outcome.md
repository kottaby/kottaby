# Task 1.1 Outcome — i18n increments: errors flat keys, notification copy slots, dashboard nav label, NEW `parentLink` namespace (full registration)

**Task:** 1.1 (Phase 1) · **REQ:** REQ-002, REQ-051, REQ-052, REQ-066, REQ-075
**Plan:** `ai/plans/sprint_3/dev1-014-parent-child-link-request-workflow-7-day`
**Branch:** `feat/dev1-014-parent-child-link-request-workflow-7-day`
**Date:** 2026-09-01
**Verdict:** COMPLETE — all 20 file paths implemented per the task text; 1.1.QL exit 0 on every path; all four 1.1.TE suites GREEN; parity arithmetic (26→32 keys, 4→7 fn-slots, seven `type*` keys unchanged) holds; SEC/SR/IV documented below.

---

## 0. Session-start environment notes

- Repo was on `main` @ `4eb3fbc` (the Phase-0 plan-doc commit) while the feature branch sat stale at `a259524`. Aligned by `git checkout feat/dev1-014-parent-child-link-request-workflow-7-day && git merge --ff-only main` — a pure branch-pointer fast-forward (a259524 is an ancestor of 4eb3fbc; no commit created, no content lost). All 1.1 work then happened on the feature branch @ 4eb3fbc.
- **Parallel-tree reality:** task 1.2 ran concurrently in the same worktree (its `backend/db/schema/parents/parent-link-requests.ts`, `backend/db/schema/parents/index.ts` edit, and `backend/db/test/logic/parents/` were present alongside 1.1's files). Attribution guard: a full `bun tsgo` during the session showed errors ONLY inside 1.2's in-flight `backend/db/test/logic/parents/parent-link-requests.schema.test.ts` (`CatalogIndexRow`/`CatalogFkRow` constraint complaint — the file changed between two runs: 2 errors → 1 error, live editing); **zero tsgo errors reference `shared/`**. All 20 of 1.1's sub-loop runs passed their project-wide tsgo step filtered for 1.1's files.

## 1. What shipped (spec-traceable)

### 1.1 Errors namespace — FIVE new FLAT keys (REQ-051; camelCase mirror of the §2.5 machine codes)

Extended `ErrorsLabels` — flat top-level keys, same pattern as the existing `handshakeCodeInvalid` (which was NOT duplicated or moved):

| Key | Machine code (mirror) | en anchor | ar anchor |
|---|---|---|---|
| `parentLinkTargetAlreadyLinked` | `PARENT_LINK_TARGET_ALREADY_LINKED` | `shared/locale/en/errors/index.ts:52` | `shared/locale/ar/errors/index.ts:52` |
| `parentLinkAlreadyPending` | `PARENT_LINK_ALREADY_PENDING` | `:53` | `:53` |
| `parentLinkRequestExpired` | `PARENT_LINK_REQUEST_EXPIRED` | `:54` | `:54` |
| `parentLinkRequestAlreadyResolved` | `PARENT_LINK_REQUEST_ALREADY_RESOLVED` | `:55` | `:55` |
| `parentLinkRequestNotFound` | `PARENT_LINK_REQUEST_NOT_FOUND` (NotFoundError("PARENT_LINK_REQUEST") entity form) | `:56` | `:56` |

Type anchors: `shared/locale/types/errors/index.ts:100-109` (with per-key JSDoc naming the deny condition). The derived `ErrorMessageKey` union picks the new keys up automatically (string slots). The dynamic errors parity suite consumes emitters from route.ts only, so it stays green UNCHANGED in shape (proven by 1.1.TE run 3).

### 1.2 Notifications namespace — SIX new non-`type*` copy slots (REQ-052)

| Slot | Kind | en anchor | ar anchor |
|---|---|---|---|
| `eventParentLinkRequestTitle` | string | `shared/locale/en/notifications/index.ts:39` | `shared/locale/ar/notifications/index.ts:42` |
| `eventParentLinkRequestBody: (parentName: string) => string` | function | `:40` | `:43` |
| `eventParentLinkAcceptedTitle` | string | `:41` | `:44` |
| `eventParentLinkAcceptedBody: (studentName: string) => string` | function | `:42` | `:45` |
| `eventParentLinkRejectedTitle` | string | `:43` | `:46` |
| `eventParentLinkRejectedBody: (studentName: string) => string` | function | `:44` | `:47` |

Type anchors: `shared/locale/types/notifications/index.ts:95-116` (grouped under a "Parent-link lifecycle event copy" section comment; JSDoc pins that the bodies interpolate ONLY the already-assembled counterpart display name). Arabic-script pins hold inside the function outputs too (parity-test-locked).

Parity suite SAME-change-set edits (`shared/locale/notifications-namespace.parity.test.ts`):
- `MANDATED_KEYS` **26 → 32** (`:50-84`; doc "(26 slots)" → "(32 slots)").
- `FUNCTION_KEYS` **4 → 7** (`:105-114`): + `eventParentLinkRequestBody`, `eventParentLinkAcceptedBody`, `eventParentLinkRejectedBody`.
- Exhaustive-inventory test message updated to "beyond the 32 slots" (`:163`).
- ar function-slot Arabic-script test extended four → seven slots (`:195-204`).
- Callable/non-empty both-locales test extended (`:267-273`) + NEW template-pin test "parent-link event bodies embed the counterpart name in BOTH locales" (`:258-265`).
- **"exactly SEVEN `type*` keys" pin GREEN UNCHANGED** — the new keys start with `event`, not `type` (the `typeKeysOnMap` filter at the type-coverage describe still returns exactly the seven TYPE_LABEL_KEYS).

### 1.3 Dashboard nav label (REQ-064 label half; nav ROUTE item belongs to 4.4)

- `shared/locale/types/dashboard/index.ts:46-47` — `linkRequests: string` (grouped with the sidebar nav labels, right after `children`).
- `shared/locale/en/dashboard/index.ts:19` — `"Link Requests"`; `shared/locale/ar/dashboard/index.ts:19` — `"طلبات الربط"`.
- Owner: Dashboard ONLY. `linkRequests` exists in NO other label bundle, and no nav item references it yet → the nav ownership matrix (`frontend/views/dashboard/nav/navItems.test.ts`) stays green (proven by 1.1.TE run 4; the compile-time `NavLabelKey` collision carve-out in `navItems.ts:50-56` is unaffected).

### 1.4 NEW `parentLink` namespace — full end-to-end registration (REQ-066, `shared/AGENTS.md` checklist)

| Artifact | Status | Live anchor |
|---|---|---|
| `shared/locale/types/parentLink/index.ts` — `ParentLinkLabels` | CREATED | interface at `:27`; EXACTLY the plan §5.6 inventory: 30 keys, 3 function slots (`expiresLine :42`, `confirmDialogBody :58`, `rejectDialogBody :62`) |
| `shared/locale/en/parentLink/index.ts` — `parentLinkEn` | CREATED | `:3` (30 leaves; English copy) |
| `shared/locale/ar/parentLink/index.ts` — `parentLinkAr` | CREATED | `:3` (Arabic script in EVERY slot incl. all three function outputs) |
| `shared/locale/namespaces/parentLink/parentLink.namespace.ts` | CREATED | `:4-7` — `export const ParentLink = defineNamespace<ParentLinkLabels>("parentLink.parentLink", translations => translations.parentLinkTranslations)` |
| `shared/locale/namespaces/parentLink/index.ts` | CREATED | `:1` — `export * from "./parentLink.namespace";` (barrel `./` exception) |
| `shared/locale/namespaces/index.ts` | UPDATED | import `:10`, `export * from "./parentLink"` `:24`, registry entry `:39` (alphabetical position between Notifications and Plans — same insertion pattern as every prior namespace) |
| `shared/locale/types/message.ts` | UPDATED | import `:10`; `parentLinkTranslations: ParentLinkLabels` appended to `Translations` `:26` |
| `shared/locale/en/messages.ts` / `shared/locale/ar/messages.ts` | UPDATED | import `:10` / `:10`; bundle slot `:27` / `:27` (slice IS the leaf const — no re-wrapper) |
| `shared/locale/parentLink-namespace.parity.test.ts` | CREATED | `MANDATED_KEYS :53` (30 slots), `STATUS_LABEL_KEYS :92` (4 state chips), `FUNCTION_KEYS :95` (3), describes: key-set identity `:110`, status vocabulary `:153`, Arabic-script sweep `:166`, template pins `:185`, function-slot inventory `:216`, registry/bundle wiring `:231` (pins `ParentLink.id === "parentLink.parentLink"` + slice identity on both bundles) |

`shared/locale/types/index.ts` (public barrel) NOT extended — it is a curated surface that never exported errors/notifications/handshakeCode either; `Translations` (re-exported via `./message`) carries the namespace to product code exactly as the precedent namespaces do.

## 2. Files created / modified (complete list — 20 paths)

**Created (8):**
1. `shared/locale/types/parentLink/index.ts`
2. `shared/locale/en/parentLink/index.ts`
3. `shared/locale/ar/parentLink/index.ts`
4. `shared/locale/namespaces/parentLink/parentLink.namespace.ts`
5. `shared/locale/namespaces/parentLink/index.ts`
6. `shared/locale/parentLink-namespace.parity.test.ts`
(plus this outcome file)

**Modified (12):**
1. `shared/locale/types/errors/index.ts`
2. `shared/locale/en/errors/index.ts`
3. `shared/locale/ar/errors/index.ts`
4. `shared/locale/types/notifications/index.ts`
5. `shared/locale/en/notifications/index.ts`
6. `shared/locale/ar/notifications/index.ts`
7. `shared/locale/notifications-namespace.parity.test.ts`
8. `shared/locale/types/dashboard/index.ts`
9. `shared/locale/en/dashboard/index.ts`
10. `shared/locale/ar/dashboard/index.ts`
11. `shared/locale/namespaces/index.ts`
12. `shared/locale/types/message.ts`, `shared/locale/en/messages.ts`, `shared/locale/ar/messages.ts` (13th/14th — counted in the 20 total paths)

## 3. Files NOT modified (and why)

- `shared/locale/errors-namespace.parity.test.ts` — dynamic route-emitter design; no edit needed (GREEN unchanged).
- `frontend/views/dashboard/nav/navItems.ts` + `navItems.test.ts` — the ROUTE item is task 4.4's; 1.1 adds the LABEL only (matrix stays green).
- `shared/locale/types/index.ts` — curated public barrel; precedent namespaces aren't listed there either.
- `shared/AGENTS.md` — reference, not changelog (explicit REQ-082 rule).
- `shared/locale/serverLegacy.ts` / `app/[locale]/layout.tsx` — the former does not exist in the live tree (stale AGENTS.md checklist step 4; live pattern = `defineNamespace` handle + `namespaces` registry, matched exactly); the latter is conditional ("if used in layout SSR") and no 1.1 consumer renders in layout.
- Anything under `backend/**` / `frontend/**` / `app/**` — layer isolation; the 1.2 agent's in-flight files were left strictly untouched.

## 4. 1.1.QL — per-file quality verification (VERBATIM method + results)

`bun run scripts/health/sub-loop.ts <path> --lifecycle duplicates` executed for EACH of the 20 code paths (sequential, distinct runs; per-file stdout archived under `/tmp/subloop-shared_locale_*`). **exit=0 on all 20** (tsgo ✅ → oxlint ✅ → biome:check ✅ → lint:type-aware ✅ → check:duplicates ✅; for `*.test.ts` files the duplicates stage is skipped by the script as "outside jscpd scan scope", matching `.jscpd.json`'s `**/*.test.ts` ignore — same skip the pre-existing suites live under).

Representative verbatim tail (`parentLink-namespace.parity.test.ts`; the other 19 are the same shape):

```
ℹ  Running tsgo (project-wide, filtering for shared/locale/parentLink-namespace.parity.test.ts)...
✅ tsgo passed (no errors for shared/locale/parentLink-namespace.parity.test.ts)
ℹ  Running oxlint on shared/locale/parentLink-namespace.parity.test.ts...
✅ oxlint passed
ℹ  Running biome:check on shared/locale/parentLink-namespace.parity.test.ts...
✅ biome:check passed
✅ lint:type-aware passed
ℹ  Skipping check:duplicates for shared/locale/parentLink-namespace.parity.test.ts (outside jscpd scan scope)...
✅ check:duplicates passed
✅ All checks for lifecycle "duplicates" passed for shared/locale/parentLink-namespace.parity.test.ts
```

Explicit full-repo `bun tsgo` (barrel-change rule): the only "error TS" lines in the tree are inside the PARALLEL 1.2 agent's in-flight test file (§0) — zero reference `shared/`.

Markdown quality loops (ledger D6 disposition — direct duplicates check, true exit codes):
```
bunx jscpd -c .jscpd.json …/deferred-items.md      → jscpd-true-exit=0 (Found 0 clones.)
bunx jscpd -c .jscpd.json …/outcome/1.1-i18n-outcome.md → jscpd-true-exit=0 (Found 0 clones.)
bunx jscpd -c .jscpd.json …/tasks.md               → jscpd-true-exit=0 (Found 0 clones.)
```

## 5. 1.1.TE — Test Engineering results (VERBATIM tails)

All four suites run via the mandated runner `bun run test/scripts/run-test.ts <path>`:

```
=== shared/locale/notifications-namespace.parity.test.ts ===
  74 pass
  0 fail
  389 expect() calls
Ran 74 tests across 1 file. [76.00ms]

=== shared/locale/parentLink-namespace.parity.test.ts ===
  52 pass
  0 fail
  328 expect() calls
Ran 52 tests across 1 file. [80.00ms]

=== shared/locale/errors-namespace.parity.test.ts ===
  8 pass
  0 fail
  88 expect() calls
Ran 8 tests across 1 file. [93.00ms]

=== frontend/views/dashboard/nav/navItems.test.ts ===
  15 pass
  0 fail
  109 expect() calls
Ran 15 tests across 1 file. [3.32s]
```

No-regression sweep (not prescribed, run for safety): `applicant-namespace.parity.test.ts` 11 pass / 0 fail · `plans-namespace.parity.test.ts` 4 pass / 0 fail.

**Pre-existing failure discovered (proven NOT ours, ledger D7):** `shared/locale/handshakeCode-namespace.parity.test.ts` fails 2 tests ("format-copy security pin", "placeholder-name sets agree") on the PRISTINE tree — proven via `git stash push` → rerun (same "2 fail / 120 expect() calls") → `git stash pop` (stash list back to 0). Root cause: those two sweeps iterate `Object.keys(errorsAr/errorsEn)` with a flat-string helper and throw on the pre-existing nested `planCatalog`/`adminUsers` grouped blocks (first throw names `handshakeCode.ar.planCatalog`). 1.1's five errors keys are flat strings appended after those blocks. Fix belongs to a repo-hygiene ticket (walk nested blocks like `assertEveryLeafNonEmpty`, `errors-namespace.parity.test.ts:87-103`) — out of 1.1's file boundary, reported in ledger row **D7 (📅 Forward)**.

## 6. 1.1.SEC — Security & Tenancy Audit

- **No user input reaches these slots unsanitized:** the only parameters anywhere in the new copy are already-assembled display-name strings (`parentName`/`studentName`) and a pre-formatted date (`expiresLine`) — the i18n layer interpolates them into static sentences and does nothing else (no HTML/URL/attribute context; React escaping applies at render). JSDoc on each function slot pins "already-assembled display name only". The STUDENT side receives the parent's FULL name and the PARENT side the student's MASKED name — the i18n layer introduces no new disclosure surface (it only embeds what the sanctioned caller passes; §5.3 ownership unchanged).
- **Zero secrets/PII in labels:** programmatic probes over all new/changed locale content returned 0 hits for handshake-code-shaped literals (`KSB-XXXXXXXX` / 16+ hex runs), email shapes, and `password|token|secret|api_key` tokens. All new strings are generic product sentences; the errors copy follows the namespace's no-identifier discipline (no user ids/emails/handshake codes — only generic copy), consistent with `handshakeCode-namespace.parity.test.ts`'s format-copy security philosophy.
- **No behavior, no IO, no tenancy surface:** locale files are pure data modules — nothing here can leak across tenants or bypass a guard.

## 7. 1.1.SR — Semantic Review Checklist (SKILL.md, all items explicitly checked)

**Race Conditions & Concurrency**
- [x] No read-then-write sequences — pure const data modules, zero IO/DB/Redis.
- [x] No module-level mutable state — every export is a `const` object/interface; no Maps/Sets/arrays mutated.
- [x] No async credit/balance/quota deductions (N/A).
- [x] No Redis operations (N/A).

**Environment & Configuration**
- [x] No `resolveEnvConfig` calls (N/A).
- [x] No `resetX`/cache-invalidation functions (N/A).
- [x] No credential/secret setters (N/A).

**Code Quality & Clean Comments**
- [x] No dead branches — the only branches in touched content are the pre-existing notification pluralizers (untouched); new function slots are single templates.
- [x] No cross-layer imports — grep over every new/changed file: zero `@/frontend`, `@/backend`, `@/app`, zero `next-intl` (probe output `0`, §6 method).
- [x] No manual ReturnType construction (N/A — labels interfaces only).
- [x] Clean comments / JSDocs — grep for `REQ-\d|Task \d|Phase \d|DEV1-\d+|plans/sprint|.ai/plans` over all changed/new files: **0 hits**. Every new JSDoc describes domain behavior (deny condition, surface, interpolation discipline).

**Schema & Types**
- [x] No migrations/schema changes in this task (1.2's territory, untouched by 1.1).
- [x] No enum imports needed; no string literals standing in for enum types (status labels are display copy keyed by name, pinned 1:1 to the four states by `STATUS_LABEL_KEYS`).
- [x] Pothos input nullability (N/A).
- [x] `$inferSelect` naming (N/A).

**Deferred Work**
- [x] One discovered out-of-scope item → ledger row **D7** (📅 Forward) — no silent ignores; ledger open ❌/⚠️ count remains **0**.

**Scope Boundary**
- [x] `git status --porcelain` matches EXACTLY the 1.1 file list + the parallel 1.2 agent's three paths (`backend/db/schema/parents/*`, `backend/db/test/logic/parents/`) — no overlap, no refactoring outside scope ("while I'm here" count: 0).
- [x] No commits, no stashes left behind (stash probe popped; `git stash list` = 0).

## 8. 1.1.IV — Instruction Verification

- **`shared/AGENTS.md` checklist:** layer isolation ✅ (§7 probe); `@/` alias everywhere in `shared/locale/**`, barrels use the sanctioned `./` sibling exception ✅; namespace registration executed per the LIVE pattern (interface → ar/en leaves → `Translations` → bundles → `defineNamespace` handle with `<ns>.<ns>` id → `namespaces` registry), matching the `handshakeCode` precedent step-for-step ✅. Deviation NOTE: the AGENTS.md checklist's step 4 cites `serverLegacy.ts`/`namespacePaths`, which do not exist in the live tree (stale doc; live registration surface is `namespaces/index.ts`) — followed the live pattern, doc-drift reported here rather than edited (REQ-082 forbids `shared/AGENTS.md` edits in this plan); conditional step 5 (layout SSR `LocaleProvider`) not applicable — no layout consumer exists yet.
- **`.agents/instructions/backend.instructions.md` (locale discipline home):** i18n section honored — compile-time system only, zero `next-intl`/`getBackendTranslations`, zero hardcoded error strings outside locale files (this task IS the locale layer), no `console.*`, no nested ternaries in the new test, bun-only commands throughout ✅. Shared-layer rule "leaf modules contain no logic" ✅ (literals + typed template functions only, matching existing leaves).
- **Compile-time i18n discipline (both files):** FLAT error keys (no new nested groupings), property-access typing, key-set parity compile-pinned via both leaves typed against the interface + runtime belt ✅.

## 9. Acceptance criteria for 1.1 (tasks.md lines 81-108) — all satisfied

- Errors: 5 FLAT keys on `ErrorsLabels` + both leaves, Arabic-script in ar, `handshakeCodeInvalid` extended-not-duplicated ✅ (§1.1)
- Notifications: 6 non-`type*` slots (3 string + 3 functions) + both leaves + parity suite extended 26→32 / 4→7 with the `type*` pin unchanged ✅ (§1.2)
- Dashboard: `linkRequests` label in types/en/ar, Dashboard-only ownership, nav matrix green ✅ (§1.3)
- parentLink namespace: full end-to-end registration incl. registry, Translations, both bundles, parity suite ✅ (§1.4)
- Layer isolation rule ✅ (§6/§7 probes)
- 1.1.QL / 1.1.TE / 1.1.SEC / 1.1.SR / 1.1.IV ✅ (§4/§5/§6/§7/§8)

## 10. Carry-forward knowledge

**For 2.3 (service + notification fanout) — exact keys to consume (via `ctx.t("notifications")` / `getServerTranslations(locale, "notifications")` → `notificationsTranslations`):**
- Request received (student recipient, locale at emitter): title `eventParentLinkRequestTitle`; body `eventParentLinkRequestBody(parentFullName)` — full parent name is the sanctioned student-side identity.
- Confirmed (parent recipient): title `eventParentLinkAcceptedTitle`; body `eventParentLinkAcceptedBody(studentName)` — pass the student's display name the service assembles (masked is ALSO acceptable here — the slot takes an already-assembled string; the plan §4.2 emission matrix decides which).
- Rejected (parent recipient): title `eventParentLinkRejectedTitle`; body `eventParentLinkRejectedBody(studentName)`.
- Denial envelopes map 1:1 to the errors keys: `PARENT_LINK_TARGET_ALREADY_LINKED → errorsTranslations.parentLinkTargetAlreadyLinked`, `PARENT_LINK_ALREADY_PENDING → parentLinkAlreadyPending`, `PARENT_LINK_REQUEST_EXPIRED → parentLinkRequestExpired`, `PARENT_LINK_REQUEST_ALREADY_RESOLVED → parentLinkRequestAlreadyResolved`, `PARENT_LINK_REQUEST_NOT_FOUND → parentLinkRequestNotFound`. Zero-write denials localize via these keys only.
- Parity-suite discipline: any FUTURE notifications key must enter `MANDATED_KEYS`/`FUNCTION_KEYS` in the same change set or the exhaustive-inventory test fails by design.

**For 4.2/4.3 (UI containers) — `ParentLink` handle (`useAppTranslation(ParentLink)`), 30 keys:**
- Student container: `studentPageTitle/Subtitle`, `incomingEmptyTitle/Body`, per-row `fromLabel`, `sentAtLabel`, `expiresLine(formattedExpiry)`, `statusPending|Confirmed|Rejected|Expired` (expired = computed chip), `confirmAction`/`rejectAction`, dialogs `confirmDialogTitle/Body(parentName)` + `rejectDialogTitle/Body(parentName)`, toasts `confirmSuccessToast`/`rejectSuccessToast`.
- Parent outgoing section: `outgoingTitle`, `outgoingEmptyTitle/Body`, `cancelAction`, `cancelDialogTitle/Body`, `cancelSuccessToast`; send affordance: `sendRequestAction`, `sendRequestSuccessToast`, `requestPendingNotice` (already-pending state), `sendUnavailableNotice` (null-collapse/unavailable state).
- 4.4: the ONE student nav item consumes `linkRequests` from `DashboardLabels` — Dashboard-only ownership means `navItems.ts`'s `isDashboardLabelKey` discriminator resolves it from the dashboard bundle with zero changes there.
- 6.x UI-component tests: assertions must go through `ParentLink.getLabels(...)` handles (zero hardcoded copy), mirroring `navItems.test.ts` discipline.
- `test/ui` translation-preload: parentLink namespace now resolves through the standard registry — if the preload array enumerates handles, 4.x/6.x must verify it picks `ParentLink` up (not a 1.1 file).

**For everyone:** markdown QL keeps hitting ledger D6 (use direct jscpd); the handshakeCode parity suite is RED pre-existing (ledger D7 — do not attribute it to any Phase-1+ task); `ParentLinkLabels` is the canonical type — view-layer label slices must `Pick`/`Omit` it, never re-declare keys.
