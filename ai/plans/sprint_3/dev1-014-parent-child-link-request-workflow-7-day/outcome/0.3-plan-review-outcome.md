# Task 0.3 Outcome — Phase-1.5 `@plan-review` Gate

**Task:** 0.3 (Phase-1.5 gate) · **REQ:** REQ-083
**Plan:** `ai/plans/sprint_3/dev1-014-parent-child-link-request-workflow-7-day`
**Branch:** `feat/dev1-014-parent-child-link-request-workflow-7-day` @ `a259524` (verified on the feature branch at task start; no git operations performed)
**Date:** 2026-09-01
**Gate verdict: PASS-with-fixes** — zero unresolvable spec-level contradictions; 33 findings, ALL either fixed in place or ledger-recorded. NO silent ignores. NO requirement/scope changes.

---

## 1. What the gate executed

1. Read `worklog.md` (0-setup/0.1/0.2 carry-forward), `.agents/skills/plan-review/SKILL.md` IN FULL, plan protocol + task 0.3 (tasks.md lines 1–77), `outcome/0.1-baseline-outcome.md`, `outcome/0.2-prereq-outcome.md`, `specs.md` (249 lines, full), `plan.md` (576 lines, full), `tasks.md` (611 lines, full), `deferred-items.md`, and `.agents/spec-process-guide/templates/plan-review-template.md` (report structure + sub-loop expectation).
2. **Step 2–3 of the skill:** mapped every touched path to its layer and read the layer AGENTS.md set: root, `shared/`, `shared/locale/`, `backend/`, `backend/types/`, `backend/db/schema/`, `backend/db/repo/`, `backend/services/`, `backend/enum/`, `backend/graphql/`, `backend/graphql/pothos/`, `backend/graphql/query/`, `backend/graphql/mutation/`, `frontend/`, `frontend/graphql/`, `frontend/graphql/sharedDocuments/`, `app/`, `test/workflows/` (19 files).
3. **Step 4:** checked the plan against the skill's cross-cutting rules AND re-verified every load-bearing anchor the plan cites against the LIVE tree (Read/Grep/LS — ~60 symbols). Live probes this session included: `linkStatus` pgEnum (`enums.ts:54`), `isPositiveSafeInt` (`emit-validation.ts:50`), `isUniqueViolation` (`user-provisioning.helpers.ts:54`), `markReadOnce` (`notification.repository.ts:269-281`), `escapeLikeWildcards` (`backend/lib/db/escape-like-wildcards.ts:37`), `grantFreeTrialOnce` (`student.repository.ts:206`), `setDeletedOnce` (`admin-user.repository.ts:355`), `AdminUserDirectoryRow` (`admin-user-row-types.ts:72`), `parseNotificationIdArg` (`notification.mutation.ts:81`), ctx.user guard (`:114-116`), `$all` pattern (`handshake-code.query.ts:60-66`), governance window (`gqlContextFactory.ts:206-216`), `NOTIFICATION_NOT_FOUND` arm (`notification-engine.service.ts:161`), `PRE_3_1_*` (`schema-surface.test.ts:91-146`), `FROZEN_*` (`sdl-static-assertions.test.ts:67-92`), apolloCache `keyFields:false` asserts (`:167-192`), `nav/navItems.ts` (`LinkChildIcon :11`, student array `:103-109`), `nav/navItems.test.ts` (ownership doc `:7-17`), parity `MANDATED_KEYS` (`:50`; **26 keys / 4 fn-slots / 7 `type*` keys — plan arithmetic CONFIRMED**), `handshakeCodeInvalid` (`en/errors/index.ts:49`), `Translations` (`message.ts:13-25`), `getTranslations` (`server.ts:15-17`), DateTime scalar (`scalar.pothos.ts:28`), Scalars slot (`builder.ts:76`), `ForbiddenError` (`builder.ts:119`), harness imports (`fanout-transport.test.ts:25`, `emit.test.ts:70`), students/users schema cols, plus existence probes (all 14 cited docs; `browser-login.ts --inject`; `emotion-cache.tsx`; `focusVisibleRingSx`; `PermissionDeniedFallback`/`RetryableNotice`/`TestWrapper`; `entity-setup.ts`; `plan-catalog.schema.test.ts`; `schema.graphql`; wire suites with `setupTestServerLifecycle`; `errors-namespace.parity.test.ts`; `frontend/views/students/dashboard/*`).
4. **Directory-existence audit of every CREATE claim** — all CORRECT in the live tree (no duplicate-creating stale claims beyond the already-ledgered D5): no `parents/` under `backend/services`, `backend/graphql/{query,mutation,pothos}`, `frontend/graphql/sharedDocuments`; `backend/db/repo/parents/` and `backend/db/schema/parents/` contents exactly as the plan's "currently…" claims state; `shared/constants/index.ts` = 3 lines; root `backend/services/index.ts` = exactly admin/auth/billing/shared/students/teachers; `frontend/graphql/sharedDocuments/index.ts` = exactly 6 lines.
5. Wrote the full review to `outcome/plan-review-R1.md` (per `plan-review-template.md` structure) and this summary. Ledger + plan files fixed (§3/§4 below). Checkboxes flipped (§7). Worklog appended.

## 2. Checklist results (plan-review SKILL Step 4 — all PASS post-fix)

Type imports ✅ · Service boundaries ✅ · MUI v9 sx-only ✅ · i18n compile-time system ✅ · Logging (no console.*) ✅ · Test conventions (`runInRollback`/tx/no-`rejects.toThrow`, with the sanctioned journey-layer exception correctly honored) ✅ · GraphQL documents (named ops, `id`-first, `TypedDocumentNode`, `@apollo/client/react`) ✅ after the naming fix · Pothos enum-object once ✅ · DomainError + `ctx.t` ✅ · DataLoader N/A (root lists) ✅.
Final skill verdict line: **"Plan passes all AGENTS.md rules for affected layers."**

## 3. Findings by severity (33 total — full detail in `plan-review-R1.md`)

| Severity | Count | Disposition |
|---|---|---|
| HIGH | 5 | 4 fixed in place (2.1 harness CREATE→REUSE per ledger D5; specs "dashboards NOT in bundle" → EXISTS per 0.2 §1.7; `navItems` wrong path in 5 places; `escapeLikeWildcards` wrong file) + 1 high-class covered by the same doc-comment anchor fixes (parser/guard cites). |
| MEDIUM | 26 | ALL fixed in place — stale line anchors corrected to live ones (engine `:43-78/:65-68/:117-123`, governance predicate `:39-59`, discovery `:128-165`, `grantFreeTrialOnce :206`, `findLocalesByIds :152-186`, `with-transaction import :62`, `ConflictError :170-182`, `NotFoundError :37-41`, ctx window `:206-216`, `markReadOnce :269-281`, `skip-when-pglite :48-50`, parity `:50`, scalar `:28`/builder `:76`/`:119`, `setDeletedOnce :355`, `isUniqueViolation :54`, `AdminUserDirectoryRow` sibling file `:72`, schema cols, `server.ts :15-17`, `server-graphql :3-5`, `message.ts :13-25`, `errors :49`, `emit-validation :79`, apolloCache `:167-192`, `registerUser :79`, `PRE_3_1`/`FROZEN` arrays n/a to plan text but verified, `fanout-transport :25`/`emit :70`). |
| LOW | 2 | Fixed (document-const naming convention pinned in 4.1 + normalized in 4.2/4.3; `message.ts` span). |
| Ledger | 1 | **D6 added** (see §4). |

Plan-file fix counts: **specs.md 17 · plan.md 30 · tasks.md 21** (anchor corrections, path corrections, REUSE wording, naming convention).

## 4. Ledger rows added / updated

- **D6 (NEW, 📅 Forward, non-blocking):** `scripts/health/sub-loop-checks.ts` `checkOxlint` exits 1 on any `.md` target ("No files found to lint" — 0 files/0 diagnostics; structural inapplicability). Source 0.1/0.3 → target future repo-tooling ticket. Compensating check `bunx jscpd -c .jscpd.json <file>` documented. NOT fixable inside this plan's doc-only boundary; recorded so it is not a silent ignore.
- **D5 (updated note only):** recorded that 0.3 applied the CREATE→REUSE wording fix in tasks 2.1 (D5 stays 🔄 owned by 2.1).
- **Seed Verification section:** re-confirmed — **open ❌/⚠️ in the Ledger Table: 0** (D4/D5 🔄 owned by 1.2/2.1; D1–D3, D6 📅 Forward). Both ledger invariants hold.

## 5. Quality Loop (0.3.QL)

Known limitation per ledger D6: `sub-loop.ts <file>.md --lifecycle duplicates` short-circuits at oxlint (exit 1, 0 files, structural inapplicability — NOT re-triggered per 0.1/0.2 disposition). Compensated with the duplicates-stage check run DIRECTLY on every `.md` written/edited this task — results §6.

## 6. Raw QL evidence (verbatim)

```
$ bunx jscpd -c .jscpd.json ai/plans/sprint_3/dev1-014-parent-child-link-request-workflow-7-day/specs.md
Found 0 clones.
jscpd-specs-exit=0
$ bunx jscpd -c .jscpd.json ai/plans/sprint_3/dev1-014-parent-child-link-request-workflow-7-day/plan.md
Found 0 clones.
jscpd-plan-exit=0
$ bunx jscpd -c .jscpd.json ai/plans/sprint_3/dev1-014-parent-child-link-request-workflow-7-day/tasks.md
Found 0 clones.
jscpd-tasks-exit=0
$ bunx jscpd -c .jscpd.json ai/plans/sprint_3/dev1-014-parent-child-link-request-workflow-7-day/deferred-items.md
Found 0 clones.
jscpd-ledger-exit=0
$ bunx jscpd -c .jscpd.json ai/plans/sprint_3/dev1-014-parent-child-link-request-workflow-7-day/outcome/plan-review-R1.md
Found 0 clones.
jscpd-R1-exit=0
$ bunx jscpd -c .jscpd.json ai/plans/sprint_3/dev1-014-parent-child-link-request-workflow-7-day/outcome/0.3-plan-review-outcome.md
Found 0 clones.
jscpd-03-exit=0
```
(Note, same as 0.2 §6: `.jscpd.json` ignores `**/ai/**`, so "Files analyzed: 0" in the table body reflects config path exclusion — the check still exits 0, which is the gate's requirement.)

Post-fix stale-anchor sweeps over all three plan files (pattern grep for every corrected anchor): **0 hits** outside the intentionally preserved historical 0.2 bullets (tasks.md lines 50–63 — the executed verification record; authoritative live anchors live in `outcome/0.2-prereq-outcome.md` §1; disposition documented in `plan-review-R1.md`, not a silent ignore).

## 7. Instruction Verification (0.3.IV) + checkboxes

- The review checklist was sourced from `.agents/skills/plan-review/SKILL.md` (layer mapping + Step-4 cross-cutting rules) executed against the layer AGENTS.md set, cross-validated with `.agents/instructions/{backend,frontend}.instructions.md` (MUI v9 sx rules, React 19 event types, Apollo `useQuery`-only + `@apollo/client/react` imports + document naming convention, i18n mandates, `logger` not `console.*`, bun-only commands — all satisfied by the corrected plan) and `tests.instructions.md` discipline via 0.1/0.2 precedent. Evidence: `plan-review-R1.md` checklist table.
- Checkboxes flipped in `tasks.md`: `0.3`, `0.3.QL`, `0.3.SR`, `0.3.IV` → `[x]`. NOTHING else touched (whole-file audit: only 0.1/0.2/0.3 sections carry `[x]`).
- No source code, tests, GraphQL, or schema files modified. No commits, no stashes. All edits confined to the plan directory + worklog.

## 8. Files created/modified by Task 0.3

- **Modified:** `specs.md`, `plan.md`, `tasks.md` (surgical anchor/wording fixes only), `deferred-items.md` (D5 note + D6 row + seed-verification re-confirm line).
- **Created:** `outcome/plan-review-R1.md` (full gate report), `outcome/0.3-plan-review-outcome.md` (this file).
- **Checkbox flips:** 0.3 / 0.3.QL / 0.3.SR / 0.3.IV in `tasks.md`.

## 9. Notes for later tasks (carry-forward)

1. **The plan files changed THIS gate — Phase-1 subagents must read the CURRENT specs/plan/tasks**, not cached copies. Anchor corrections are extensive but semantic-neutral; nothing to re-litigate.
2. **Task 2.1:** journey harness is REUSED from `@/test/workflows/helpers` (ledger D5; exact export names in 0.2 §1.5) — the "RED at 2.1" posture is unchanged; the harness self-tests (`helpers.self-test.test.ts`, `journey-fixtures.smoke.test.ts`) must stay green.
3. **Task 4.1:** document consts are convention-pinned now — `myOutgoingParentLinkRequestsQueryDocument`, `myIncomingParentLinkRequestsQueryDocument`, `requestParentChildLinkMutationDocument`, `respondToParentLinkRequestMutationDocument`, `cancelParentLinkRequestMutationDocument` (4.2/4.3 hook references match).
4. **Task 1.1:** notifications parity baseline is verified live at **26 MANDATED_KEYS (`:50`) / 4 function slots / 7 `type*` keys** — the 26→32 and 4→7 arithmetic stands; `handshakeCodeInvalid` precedent line is `:49`.
5. **Task 4.4 + 6.3:** nav file is `frontend/views/dashboard/nav/navItems.ts` (+ `/nav/` test) — student array `:103-109`, `LinkChildIcon` import `:11`, ownership matrix doc `:7-17`.
6. **All tasks:** markdown quality loops keep hitting the D6 oxlint limitation — use the compensating direct-jscpd command; do not "fix" the exit-1 as if it were a content violation.
7. **1.2's D4 and 2.1's D5 remain the only 🔄 rows** — flip to ✅ with outcome references when those tasks complete; 6.5's ledger gate counts table rows (0 open ❌/⚠️ today).
