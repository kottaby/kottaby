# Task 5.1 Outcome — GraphQL Wire Matrix

## Authoring
- `backend/graphql/test/parent-link.wire.test.ts` (1267 ln) authored by general-purpose dispatch #1 (died on infra timeout post-QL ×2 green), completed + gated by orchestrator-direct pass after dispatch #2 also died. Location follows repo convention (siblings: schema-surface.test.ts, plan-catalog.roles.test.ts).

## Matrix (22 tests / 423 expects — run-test.ts → run-server-tests.ts harness, file GREEN)
| Tier | Coverage | Result |
|---|---|---|
| Anonymous (401) | credential-less caller × 5 ops → UNAUTHORIZED, zero rows | PASS |
| Wrong-role | FULL 15-cell matrix (parent↔student both directions, teacher, admin) → FORBIDDEN constant localized shape; entitled self-role probes ride same envelope (no self-service hole) | PASS |
| Governed caller (REQ-031) | PRE-ISSUED token denied at SERVICE layer on request + respond — FORBIDDEN (never UNAUTHORIZED), own rows untouched; relaxed READ path keeps self-scoped history | PASS |
| Payload≡oracle | wire myOutgoing/Incoming ≡ service oracle field-by-field (full-name disclosure on incoming) | PASS |
| Nullable collapse (REQ-012/034) | code miss + governed target → data null, byte-identical bodies, zero errors entries | PASS |
| Success shape | live code → exactly one pending row, payload ≡ oracle, emit lands | PASS |
| Validation pre-DB | respond/cancel fuzz ("0","-1","1.5","abc",oversized,whitespace,lazy-parse `<liveId>abc`) → VALIDATION, never 403/service error, live row byte-identical | PASS |
| BOPLA smuggle | studentId/parentId/userId extra args on all 3 mutations → GRAPHQL_VALIDATION_FAILED pre-resolver | PASS |
| id-first | printed-selections order pin across all five wire documents | PASS |
| Locale | en + ar denial copies via Accept-Language negotiation (VALIDATION + wrong-role) | PASS |

## Required cross-file reconcile (cross-file dependency → fixed in-same-changeset)
- `frontend/graphql/test/warnings/warning-surfacing.test.ts`: `KNOWN_LIVE_MUTATION_FIELDS` inventory contract ("every deployed Mutation root field is enumerated") broke on the 3 new mutations → added `requestParentChildLink`/`respondToParentLinkRequest`/`cancelParentLinkRequest` with `// DEV1-014 reconcile:` comments + docstring, mirroring the DEV3-016 precedent. Suite 9/0. (Edit was lost once to a same-branch `git checkout -f` — Lesson: checkout -f on the CURRENT branch discards tracked edits; never use it with dirty tracked work.)

## Gates
- bun tsgo full project: exit 0. QL sub-loop duplicates: wire ×2 exit 0 + warning-surfacing exit 0.
- schema-surface 43/0 · sdl-static 26/0 (byte-parity intact) · warning-surfacing 9/0 · wire 22/0.
- `bun run test:graphql`: 140/140 tests, 875 expects; 8/9 files file-green.

## Pre-existing environmental note (NOT a DEV1-014 regression)
- `frontend/graphql/test/auth/auth.test.ts`: 10/0 tests green (junit failures=0; raw bun test exit 0) but run-server-tests flags the FILE failed. Proven pre-existing: identical signature at baseline a259524 in a throwaway worktree (11 pass/1 file-flag). Cause class: harness file-level accounting vs sandbox-injected env; out of plan scope, surfaced here for Phase 6 honesty.
- Infra: killed dispatches left orphan `next-server` on :3066 (EADDRINUSE) — kill before reruns; cold turbopack compile can exceed the 120s per-test timeout — warm-server reuse (harness pollOnce path) makes runs deterministic. run-test.ts:157 strips DATABASE_URL by design; .env.test governs both server and test process.

## Drift
- tasks.md §5.1 "Run" prose names `bun run test:graphql` — both invocation paths exercised (single-file via run-test.ts delegation + full test:graphql). No other drift.

## Carry-forwards
- 5.2 (chaos/concurrency, pglite-skip-gated): reuse the wire fixtures; partial-unique 23505 race goes here.
- 5.3 (static locks): wire suite adds no writers; single-writer/no-LIKE/no-audit/no-console scans unaffected.
- 5.4 (journey re-run): unchanged RED-turned-GREEN 19/19 expected; test:db 226 baseline holds.
