# Task 0.1 Outcome — Record Baseline & Initialize Deferred-Items Ledger

**Task:** 0.1 (Phase 0: Pre-Implementation Baseline) · **REQ:** REQ-001
**Plan:** `ai/plans/sprint_3/dev1-014-parent-child-link-request-workflow-7-day`
**Branch:** `feat/dev1-014-parent-child-link-request-workflow-7-day` (was checked out from `main` at session start; both point at `a259524`, feature branch verified up to date with `origin/main`)
**Date:** 2026-09-01
**All numbers below are REAL captured stdout from this session. Raw evidence files listed in §8.**

---

## 1. Baseline Counts (captured 2026-09-01, repo root `/home/z/my-project`)

| Check | Command | Exit | Result |
|---|---|---|---|
| tsgo typecheck | `bun tsgo 2>&1` (runs `scripts/restore-next-env-dts.ts` + `tsgo -b --noEmit` behind `scripts/lib/run-locked-cmd.ts` process lock) | 0 | **"error TS" occurrences: 0** (full output = 5 lines: wrapper banner + lock acquire/release only; zero lines containing "error", case-insensitive) |
| Biome | `bun biome:check 2>&1` (`bunx @biomejs/biome check --write --unsafe .`) | 0 | **"warn" occurrences: 0**; "error" occurrences: 0; `Checked 1074 files in 9s. No fixes applied.` — NO files rewritten (verified: `git status --porcelain` still empty after the run) |
| ESLint (lint-service) | `bun run scripts/lint-service.ts --json --id baseline` | 0 | `{"success": true, "output": "", "exitCode": 0, "metrics": {"id": "baseline", "scope": "full-repo", "fileCount": 0, "durationMs": 56097, ...}}` — empty `output` = **0 ESLint findings** across the full repo. Note: `fileCount: 0` means the *full-repo scope* (empty file list → eslint config drives scope, per `scripts/lint-service.ts` header), not "zero files linted". Duration ≈ 56 s. |

### Pre-existing issues to IGNORE during later review (explicit catalog)
- tsgo errors attributable to pre-existing code: **0**
- Biome warnings: **0** · Biome errors: **0** · Files auto-fixed by biome at baseline: **0**
- ESLint findings (full-repo): **0**
- Pre-existing modified files (`git diff --name-only`): **0 files, list empty**
- Pre-existing stashes: **0**
- **Net: the baseline is perfectly clean — ANY error/warning appearing after task work starts is attributable to that task's changes. There are no pre-existing issues to filter out.**

---

## 2. Git Baseline

- `git stash list` → **0 entries** (saved: `/tmp/baseline-stash.txt`)
- `git diff --name-only` → **0 files, empty list** (saved: `/tmp/baseline-files.txt`)
- `git status --porcelain | head -40` → empty (working tree clean)
- HEAD at capture: `a259524` ("plan: add new plans")

---

## 3. Environment Verification

### 3.1 `.env` FILE content (source of truth) — CORRECT
```
DB_PROVIDER=postgres
DATABASE_URL=postgresql://postgres@127.0.0.1:5432/app_db
DATABASE_ENCRYPTION_KEY=*** (present, not reproduced)
CRON_SECRET=*** (present)
GRAPHQL_ENCRYPTION_KEY=*** (present)
ADMIN_EMAIL=admin@app.local
ADMIN_PASSWORD=*** (present)
CACHE_PROVIDER=postgres
REDIS_PROVIDER=local
```
Verdict: `DB_PROVIDER=postgres` ✅ and `DATABASE_URL=postgresql://postgres@127.0.0.1:5432/app_db` ✅ — exactly as required.

### 3.2 ⚠️ SANDBOX GOTCHA — injected process-env `DATABASE_URL` (CONFIRMED LIVE)
- The sandbox injects `DATABASE_URL=file:/home/z/my-project/db/custom.db` into the **process environment** of every shell.
- Proof: `bun --env-file=.env -e "console.log(process.env.DATABASE_URL)"` printed **`file:/home/z/my-project/db/custom.db`** — i.e. the already-set process env var WINS over the `.env` file value (Bun does not override pre-set vars). `process.env.DB_PROVIDER` = `postgres` (picked up from `.env`, since not pre-set).
- **Consequence (standing rule for all later tasks): any DB-touching command/script run in this sandbox MUST override inline, e.g. `DATABASE_URL=postgresql://postgres@127.0.0.1:5432/app_db <cmd>` (or the `app_db_test` twin), otherwise it silently hits a SQLite file at `db/custom.db`.** Already recorded by 0-setup in `worklog.md`; re-confirmed here with a live probe.
- The `.env` FILE itself is correct; only the process-env override is affected.

### 3.3 Postgres connectivity — PROOF
```
$ psql "postgresql://postgres@127.0.0.1:5432/app_db" -c "\dt"   (with LD_LIBRARY_PATH=/tmp/pgpkg/pgroot/usr/lib/postgresql/17/lib)
 public | admin | table | postgres
 public | applicants | table | postgres
 public | audit_logs | table | postgres
 public | evaluations | table | postgres
 public | home_work | table | postgres
 public | lessons | table | postgres
 public | notifications | table | postgres
 public | parents | table | postgres
 ... (17 tables listed; truncated at 20 lines)
```
`select version();` → `PostgreSQL 17.11 (Debian 17.11-0+deb13u1) on x86_64-pc-linux-gnu, compiled by gcc (Debian 14.2.0-19) 14.2.0, 64-bit`. User-space cluster at `/tmp/pgdata` is UP and serving `app_db`. ✅

### 3.4 bun binary path discrepancy (environment fix applied)
- `~/.bun/bin/bun` did **NOT exist** in this sandbox (only `~/.bun/install/`); real bun is `/usr/local/bin/bun` v1.3.14.
- Fix applied (outside the repo, no repo files touched): `~/.bun/bin/bun` → symlink to `/usr/local/bin/bun` (and `bunx` likewise) so that prompts/scripts hardcoding `~/.bun/bin/bun` work.
- Repo scripts (`package.json` bins) invoke `bun` from PATH — unaffected either way.

---

## 4. Deferred-Items Ledger Status

- `deferred-items.md` **already existed** (seeded at plan intake, 2026-08-31) and already carried the **standard template structure** from `.agents/spec-process-guide/templates/deferred-items-template.md`: `ID | Deferred Item | Source Task | Target Task | Status | Verified By | Notes` — i.e. description / source task / target task / status-emoji / resolution note are ALL present → **no structural restructure was required** (minimal-touch honored).
- Seeded with the FOUR plan-provided resolved-pointers (per tasks.md lines 36–39), all present: D1 (cron expiry sweep → future cron-stream ticket, 📅 Forward), D2 (distinct `cancelled` vocabulary → future product ticket, 📅 Forward), D3 (revocation/`Unlinked` → future revoke ticket, 📅 Forward), D4 (partial-unique index Drizzle expressibility → Task 1.2, 🔄 In Progress).
- Edits made (minimal):
  1. Added `**Seeded / baseline-verified:** 2026-09-01 (Task 0.1 …)` line under the header.
  2. Sharpened D4's note: explicitly "NOT an open ❌/⚠️ at seed time — owned by Task 1.2; must flip to ✅ with outcome reference when 1.2 completes".
  3. Added a **"Seed Verification (Task 0.1)"** section: open ❌/⚠️ count in the table = **0**, plus a note that ❌/⚠️ glyphs in the *legend* are definitions (final-gate enforcement counts must be taken over ledger table rows, not raw whole-file `grep -c`), plus the baseline summary.
- **Open ❌/⚠️ items at seed time: 0.** D1–D3 = 📅 Forward (informational, non-blocking for this plan); D4 = 🔄 In Progress (owned by Task 1.2, not blocked/partial).

---

## 5. Files Created / Modified by Task 0.1

- **Modified:** `ai/plans/sprint_3/dev1-014-parent-child-link-request-workflow-7-day/deferred-items.md` (minimal seed-verification edits, §4)
- **Created:** `ai/plans/sprint_3/dev1-014-parent-child-link-request-workflow-7-day/outcome/0.1-baseline-outcome.md` (this file); `outcome/` directory created (did not exist)
- **Not touched:** any source code, tests, specs, plan.md, tasks.md *content* (only the 0.1 checkboxes are flipped), or any file outside the plan dir. No git commits, no stashes created.

---

## 6. Quality Loop (0.1.QL) — VERBATIM RESULTS + known .md limitation

### 6.1 What the run showed (both files, real output)

Both `sub-loop.ts --lifecycle duplicates` runs behaved **identically**:
- ✅ **tsgo passed** (project-wide run, filtered for the target file — 0 errors; consistent with the §1 tsgo baseline of 0 errors repo-wide).
- ❌ **oxlint step failed with `No files found to lint. Please check your paths and ignore patterns.`** (oxlint exits 1 on a zero-file set — it does not lint `.md` at all).

### 6.2 Root cause (verified in `scripts/health/sub-loop-checks.ts`, this session)

`checkOxlint()` (line 124–130) invokes `bunx oxlint --deny-warnings --ignore-path .gitignore <file>` for **any** file type with **no extension skip**, while the duplicates stage's own `checkDuplicates()` *does* skip non-`.ts/.tsx` files (`shouldSkipJscpd()`, lines 187–205) — and `.jscpd.json` ignores `**/ai/**` anyway. Oxlint only supports JS/TS-family inputs, so for a markdown target the "No files found" exit-1 is a **structural inapplicability, not a content violation** (0 files scanned, 0 diagnostics). Reproduced directly: `bunx oxlint --deny-warnings --ignore-path .gitignore <file.md>` → `oxlint-direct-exit=1` with the same message.

### 6.3 Applicable-checks verdict & compensating evidence

| Check | Applies to `.md`? | Result |
|---|---|---|
| tsgo (project-wide, file-filtered) | yes (vacuously — no TS in md) | ✅ PASSED, both files |
| oxlint | **no** (JS/TS-only linter) | 0 files, 0 diagnostics; step exits 1 on empty file set — script limitation, reported verbatim per dispatch instruction |
| biome:check / lint:type-aware | no (never reached — short-circuit at oxlint) | n/a |
| check:duplicates (jscpd) | skipped by script for non-`.ts`; run DIRECTLY as compensation | ✅ `bunx jscpd -c .jscpd.json <file>` → **exit 0, "Found 0 clones."** for BOTH `deferred-items.md` and `0.1-baseline-outcome.md` |

**0.1.QL verdict: PASS** — every check that can apply to a markdown file passes (tsgo ✅, jscpd 0 clones ✅); the single failing step is oxlint on a file type it cannot lint (0 files, 0 diagnostics). Deviation recorded verbatim here per the dispatch instruction ("if the script errors on .md files, report that verbatim and continue"). ⚠️ Heads-up for task 0.3: the plan-review gate runs `sub-loop.ts specs.md --lifecycle duplicates` (per `.agents/spec-process-guide/templates/plan-review-template.md:77-78`) and will hit the SAME oxlint-on-md limitation — same disposition applies.

## 7. Deviations & Notes for Later Tasks

1. **Outcome filename:** tasks.md line 40 says `outcome/0-baseline-outcome.md`; the dispatch protocol (`<task-id>-outcome.md`, SKILL.md Task Execution Protocol rule 2) and the orchestrator instruction say `0.1-baseline-outcome.md`. Executed as **`0.1-baseline-outcome.md`** (per-task-ID convention; what later tasks read under `outcome/` is this file). If a reviewer looks for `0-baseline-outcome.md`, it is this document.
2. **Bun path:** `~/.bun/bin/bun` was absent (see §3.4); symlinked to `/usr/local/bin/bun` v1.3.14.
3. **biome:check mutability:** command embeds `--write --unsafe`; at baseline it applied **no fixes** ("No fixes applied") — later tasks must still expect reformatting side effects when they touch files.
4. **lint-service `fileCount: 0` semantics:** means full-repo scope (documented in `scripts/lint-service.ts` header), NOT zero files linted. Later tasks comparing per-file runs will see `scope: "files"` and a real fileCount.
5. **Next-task warnings (for 0.2):** (a) repo was on `main` at session start; feature branch `feat/dev1-014-parent-child-link-request-workflow-7-day` is NOW checked out (was left on `main` after 0-setup). (b) plan §0.2 claims about `test/workflows/helpers/` NOT existing are STALE per 0-setup intake (harness EXISTS — `TrackedFixtures`, `provisionParentActor`, etc.); 0.2 must verify anchors live and outcome the correction. (c) process-env `DATABASE_URL` gotcha (§3.2) applies to every DB command. (d) `sub-loop.ts` has no `.md` skip for oxlint (§6.2) — every future markdown-file quality loop (including 0.3's plan-review gate per `plan-review-template.md:77-78`) will short-circuit at oxlint with "No files found to lint"; disposition: report verbatim + compensate with direct jscpd, or fix the script in a dedicated task (NOT in 0.1's boundary).

---

## 8. Raw Evidence (verbatim, captured this session)

### 8.1 tsgo (`/tmp/tsgo-baseline-full.txt`, exit 0, full 5-line output)
```
$ bun run scripts/restore-next-env-dts.ts && bun run scripts/lib/run-locked-cmd.ts tsgo tsgo -b --noEmit
Restored next-env.d.ts to canonical dev dist dir (.next-dev).
[process-lock] Enqueued request for "tsgo" (PID: 2245)
[process-lock] Acquired lock for "tsgo" (PID: 2245). Executing...
[process-lock] Released lock for "tsgo" (PID: 2245)
```
`grep -c "error TS"` → **0**

### 8.2 biome:check (`/tmp/biome-baseline-full.txt`, exit 0, full 5-line output)
```
$ bun run scripts/lib/run-locked-cmd.ts biome:check bunx @biomejs/biome check --write --unsafe .
[process-lock] Enqueued request for "biome:check" (PID: 2294)
[process-lock] Acquired lock for "biome:check" (PID: 2294). Executing...
Checked 1074 files in 9s. No fixes applied.
[process-lock] Released lock for "biome:check" (PID: 2294)
```
`grep -c "warn"` → **0**

### 8.3 lint-service (`/tmp/baseline-lint.json`, CLI exit 0, stderr empty)
```json
{
  "success": true,
  "output": "",
  "exitCode": 0,
  "metrics": {
    "id": "baseline",
    "scope": "full-repo",
    "fileCount": 0,
    "durationMs": 56097,
    "enqueuedAt": 1788251591251,
    "startedAt": 1788251591251,
    "finishedAt": 1788251647348,
    "queueDepthAtEnqueue": 0
  }
}
```
(Preceded in the file by the three `[process-lock]` lines for `lint-service: baseline`, PID 2329.)

### 8.4 git baseline
- `/tmp/baseline-stash.txt`: empty (0 entries)
- `/tmp/baseline-files.txt`: empty (0 files)
- `git status --porcelain`: empty

### 8.5 Quality loop (0.1.QL) outputs — VERBATIM

`bun run scripts/health/sub-loop.ts ai/plans/sprint_3/dev1-014-parent-child-link-request-workflow-7-day/deferred-items.md --lifecycle duplicates` → **exit 1** (oxlint-on-md limitation, §6):
```
ℹ  File: ai/plans/sprint_3/dev1-014-parent-child-link-request-workflow-7-day/deferred-items.md
ℹ  Lifecycle: duplicates (depth: 3)

━━━ Applicable Rule Files ━━━

⚠  No applicable instruction files found for this file.

AGENTS.md files (read before fixing):
  ✓  AGENTS.md

[process-lock] Enqueued request for "sub-loop: ai/plans/sprint_3/.../deferred-items.md" (PID: 2597)
[process-lock] Acquired lock for "sub-loop: ai/plans/sprint_3/.../deferred-items.md" (PID: 2597). Executing...
ℹ  Running tsgo (project-wide, filtering for ai/plans/sprint_3/.../deferred-items.md)...
✅ tsgo passed (no errors for ai/plans/sprint_3/.../deferred-items.md)
ℹ  Running oxlint on ai/plans/sprint_3/.../deferred-items.md...
❌ oxlint FAILED — stopping here. Fix oxlint errors before proceeding.

No files found to lint. Please check your paths and ignore patterns.
Finished in 90ms on 0 files with 303 rules using 2 threads.

[process-lock] Released lock for "sub-loop: ai/plans/sprint_3/.../deferred-items.md" (PID: 2597)
```
(`Fix-Or-Report` + `Oxlint Fix Patterns` help-banner lines elided for brevity only; nothing else omitted. Full copy: `/tmp/subloop-ledger.txt`.)

`bun run scripts/health/sub-loop.ts ai/plans/sprint_3/dev1-014-parent-child-link-request-workflow-7-day/outcome/0.1-baseline-outcome.md --lifecycle duplicates` → **exit 1** (identical shape):
```
[process-lock] Enqueued request for "sub-loop: ai/plans/sprint_3/.../outcome/0.1-baseline-outcome.md" (PID: 2749)
[process-lock] Acquired lock for "sub-loop: ai/plans/sprint_3/.../outcome/0.1-baseline-outcome.md" (PID: 2749). Executing...
ℹ  Running tsgo (project-wide, filtering for ai/plans/sprint_3/.../outcome/0.1-baseline-outcome.md)...
✅ tsgo passed (no errors for ai/plans/sprint_3/.../outcome/0.1-baseline-outcome.md)
ℹ  Running oxlint on ai/plans/sprint_3/.../outcome/0.1-baseline-outcome.md...
❌ oxlint FAILED — stopping here. Fix oxlint errors before proceeding.

No files found to lint. Please check your paths and ignore patterns.
Finished in 62ms on 0 files with 303 rules using 2 threads.

[process-lock] Released lock for "sub-loop: ai/plans/sprint_3/.../outcome/0.1-baseline-outcome.md" (PID: 2749)
```
(Full copy: `/tmp/subloop-outcome.txt`. Header/banner sections identical in shape to the ledger run.)

Compensating direct jscpd (duplicates-stage check, since the script skips non-`.ts` and `.jscpd.json` ignores `**/ai/**`):
```
$ bunx jscpd -c .jscpd.json ai/plans/sprint_3/dev1-014-parent-child-link-request-workflow-7-day/deferred-items.md
Found 0 clones.
time: 9.066ms
jscpd-ledger-exit=0
$ bunx jscpd -c .jscpd.json ai/plans/sprint_3/dev1-014-parent-child-link-request-workflow-7-day/outcome/0.1-baseline-outcome.md
Found 0 clones.
time: 3.848ms
jscpd-outcome-exit=0
```

Reproduction of the oxlint limitation directly:
```
$ bunx oxlint --deny-warnings --ignore-path .gitignore ai/plans/sprint_3/.../deferred-items.md
No files found to lint. Please check your paths and ignore patterns.
Finished in 58ms on 0 files with 303 rules using 2 threads.
oxlint-direct-exit=1
```

### 8.6 Instruction-verification (0.1.IV) evidence

Validated task 0.1 against `.agents/instructions/tests.instructions.md` (baseline capture discipline) and `.agents/instructions/backend.instructions.md`:
- Baseline command set matches SKILL.md Phase 0 exactly: tsgo error count (`grep "error TS"` → 0), biome warn count (`grep -c warn` → 0), `lint-service --json --id baseline` (exit 0), `git stash list` + `git diff --name-only` (both empty). ✅
- `Always use bun — never npm/yarn/pnpm` (tests.instructions.md Quality; backend Bun rules): complied — all commands via bun/bunx v1.3.14. ✅
- `Run bun tsgo and bun run lint after creating or modifying tests`: no test or source files were touched this task (markdown plan docs only, outside the instructions' `applyTo` globs `backend/**/*.ts` / test globs); the tsgo + full-repo lint evidence above still covers the tree state. ✅
- i18n/logging/seeds/cron sections of backend.instructions.md: not applicable (no code written). ✅
