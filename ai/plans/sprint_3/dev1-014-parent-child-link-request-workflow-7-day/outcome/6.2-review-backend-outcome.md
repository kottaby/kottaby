# Outcome — Task 6.2: review-backend wave (repos + services + resolvers)

- **Task ID:** 6.2 (Phase 6 post-implementation review, READ-ONLY — no source edits, no commits, no tasks.md changes)
- **Branch:** `feat/dev1-014-parent-child-link-request-workflow-7-day` @ `687bea2` (guard rail re-asserted at every batch; a live watchdog reset HEAD to `main`/`a259524` between batches, so the review read from a verified snapshot of the branch tree)
- **Date:** 2026-09-02
- **Method:** worklog last-3 + outcome/2.2 §6 contract + outcome/2.3 service contract + outcome/3.1-3.2 root-field signatures read first; then every listed file END TO END; every invariant traced to its enforcing line; chaos (10 cells) + static-locks (36 tests) spot-read deeply (4+ assertions each evaluated for regression-catching power).

---

## 1. Verdict

**PASS — backend is ship-ready.** 0 blockers, 0 majors, **2 minors, 3 nits.** Every plan decision D1–D12 has a live artifact; every in-scope REQ (011/012/013/014/015/016/017/018/019/020/021/023/024/030..035/040..044/050/053/054) is honored in code, not just comments. The two minors are spec-letter/documentary drift with no runtime consequence; neither blocks 6.3/6.4/6.5.

## 2. Findings

| # | Severity | File:line | Finding | Evidence | Recommended fix |
|---|---|---|---|---|---|
| F1 | **minor** | `backend/services/parents/parent-link-request.service.ts:169→172, 241→243, 347→349` | **REQ-040 tx-membership letter drift:** all three mutations run `requireActor` (the fresh actor re-check) BEFORE `withTransaction` opens the unit. REQ-040 enumerates the fresh actor read INSIDE the tx membership ("request-create tx = { fresh actor read, code resolution, insert, notification insert }"; "confirm tx = { actor re-check, guarded claim, … }"). On the own-commit path the actor read is therefore a standalone `queryDb` read outside the tx. | `requestLink`: `const actor = await requireActor(...)` at :169, `withTransaction(outerTx, …)` at :172; same shape in `respondToLinkRequest` (:241/:243) and `cancelLinkRequest` (:347/:349). On caller-tx paths the read DOES join the caller's executor (`requireActor(..., outerTx, …)` → `UserRepository.findById(actorId, tx)`). | No atomicity/governance consequence (single read-only statement; the re-check still precedes every write, which is REQ-031's load-bearing property). Record the divergence in the REQ-080 canonical doc, or move `requireActor` inside the unit in a future touch. |
| F2 | **minor** (spec-conformant; documentary) | `backend/services/parents/parent-link-request.helpers.ts:116-125` + `:398` | **Re-request vs lapsed-but-unmaterialized pending row:** the pre-check `findPendingByPair` matches on `status='pending'` with NO liveness predicate (repo :191-219 — faithful read by contract), so a parent re-requesting after expiry-but-before-materialization gets `PARENT_LINK_ALREADY_PENDING` while the same row renders `Expired` in their outgoing list (`mapOutgoing` :398, strict `<=`). The unblock path is cancel-then-re-request (cancel's liveness failure triggers the REQ-094 fold). This is the LETTER of REQ-014/REQ-095 ("while a pending row exists") and REQ-015 scopes materialization to respond/cancel only — but the user-visible pair (chip says Expired, create says already-pending) is a D1 lazy-materialization quirk product/QA will ask about. | Specs REQ-014 (`specs.md:60`), REQ-095 (`:183`), REQ-015 (`:61` — "WRITE paths (respond/cancel) SHALL materialize"); D4 promises "reject→re-request" only. No test pins an expiry→re-request arm (none is required by spec). | Add one paragraph to the REQ-080 canonical doc (task 7.1) recording this consequence of lazy materialization; optionally a deferred-item row for a future "materialize-on-collide" refinement. No code change required for this ticket. |
| F3 | nit | `backend/db/repo/parents/parent-link-request.repository.ts:147-151` | REQ-033's literal insert payload is `{ parentId, studentId, status: LinkStatus.Pending, expiresAt }`; the shipped `create` omits `status` (schema default `'pending'` at `backend/db/schema/parents/parent-link-requests.ts:42` supplies it). Same effective value; field-by-field assembly (no spread) fully honored. | 2.2 outcome §1/§7 already recorded "status/createdAt come from the schema defaults". | Align REQ-033 prose in the canonical doc, or add the explicit field in a future touch for byte-exact parity. |
| F4 | nit | `backend/db/repo/parents/parent-link-request.repository.ts:154`, `parent-link-request.service.ts:305/311/363`, `helpers.ts:385` | REQ-050 says "plain `new Error(...)` in the flow is PROHIBITED", yet five `new Error(...)` invariant guards exist on the flow ("insert returned no rows", "in-tx emit returned a row…", "claimed row vanished…", corrupt-status). These are should-never-happen internal guards matching the pre-existing repo pattern (`StudentRepository.createForRegistration:142`), not domain denials — they surface as non-domain errors at the masking boundary. | 2.2.SR explicitly sanctioned the repo pair as "internal guards, matching the existing repo pattern". | One-line clarification in REQ-080 doc: the prohibition scopes to DOMAIN denials; invariant guards are exempt by precedent. |
| F5 | nit | `backend/services/parents/parent-link.static-locks.test.ts:288-297, 622-625` | Static-lock lexical blind spot (accepted-by-design caveat, restated here for the record): a spread-shaped students write (`tx.update(students).set({ ...payload })`) or an aliased notifications insert carries no literal `parentId`/`notifications` token and would evade lock (a)/(e) scanners. Compensating controls: REQ-033 field-by-field discipline (review-enforced) + the exhaustive-allowlist `toEqual` exact-match assertions, which DO catch any literally-written new writer. | Header "LEXICAL CAVEAT" at :58-65. | None required; if a future ticket introduces spread writes anywhere, add a spread-shape scanner. |

**Explicitly considered and dismissed (no finding):**

- `markExpiredIfPending` has no liveness predicate (repo :304-309) — by contract (2.2 §6): invoked only on the classified-expired arm; idempotent by the `status='pending'` predicate; a concurrent live claim wins by row-lock ordering (chaos cells 7/8 pin exactly-one-terminal-state).
- `respondToLinkRequest`/`cancelLinkRequest` success paths render with a second `new Date()` (service :325/:373) — inert: the claimed/folded row is non-`pending`, and the render-time remap only fires on pending rows. REQ-044's ONE-captured-`now` governs the liveness-bearing evaluations (claim + classifier + fold), which do share one instant (:244, helpers :97).
- Chaos cell 6's log-context assertion uses subset `toMatchObject` — compensated by the Tier-4 closed-key-set + secrets sweep (`parent-link-request.service.test.ts:1786-1796`: every logged context's keys ⊆ `{code, entity, entityId, locale}` across 4 denial calls, and the serialized calls contain no names/emails/codes).
- The two-parent race cell accepts a raw `40P01` deadlock as a loser shape — narrow escape hatch (only that pg code passes; any other raw failure fails the test), honestly documented in-test.
- `isUniqueViolation` checks `message` substrings per chain node — the plan pins this exact shared helper (`plan.md` D4 → `user-provisioning.helpers.ts:54`); the 23505 mapping is additionally proven end-to-end by chaos cell 1 (raw code asserted ABSENT from the loser's surfaced error).
- Resolver docblocks contain the strings "try/catch" and the repo docblock mentions `console.*` — grep hits are comments only; resolver bodies have zero try/catch and zero console calls (verified by reading + lock (d) corpus).
- Actor governance uses the three-flag check (`isDeleted || isBlocked || suspended`, helpers :262) without the suspension-window arithmetic used for target students — matches the codebase's own actor precedent (`backend/services/auth/auth.service.ts:95`) and REQ-031's exact wording; fail-closed direction for lapsed windows.

## 3. Invariant → enforcing line (coverage matrix)

**Repository — `backend/db/repo/parents/parent-link-request.repository.ts` (11 methods, read end-to-end):**

| Invariant | Enforcing line |
|---|---|
| D2 guarded single-UPDATE, no SELECT-then-UPDATE | respond :245-257 (id+student+pending+`gt(expiresAt, now)`+RETURNING), cancel :278-290 (id+parent+pending+strict liveness+RETURNING), markExpired :305-309 (id+pending), sibling sweep :326-337 (student+pending+`ne(id, winner)`+RETURNING count) |
| Strict `expires_at > now` (REQ-015/044) | `gt(...)` :253 (respond), :286 (cancel); classifiers re-derive honestly (helpers :301-306) |
| tx-LAST discipline | writes: create :141-144, respond :238-244, cancel :272-277, markExpired :304, expireSiblings :321-325 — REQUIRED `DBTransaction` last; reads take optional executor last |
| `(tx ?? db)` / dual executor | `isDBTransaction` guard :91-93; Drizzle branch vs `queryDb` raw branch on ALL 7 reads (:168-182, :191-219, :351-376, :385-410, :418-442, :450-474) |
| Parameterized equality, zero LIKE (REQ-037) | every predicate is `eq/and/gt/ne`; raw SQL binds `$1/$2/$3`; `LIMIT ${LIST_LIMIT}` is a module constant; grep: zero `ilike(`/`like(` |
| RETURNING clauses | respond/cancel RETURNING * ; sibling sweep RETURNING id → count :336-337; markExpired void per contract |
| 23505 propagation | `create` :141-157 — no catch; raw pg error surfaces (service owns mapping) |
| LIMIT 50 + deterministic order | LIST_LIMIT :51, LIST_ORDER :120 (created_at DESC, id DESC), both lists capped |
| Read purity of lists | lists return stored rows; expiry render is service-side only |

**Student repo — `backend/db/repo/students/student.repository.ts` (additive pair):**

| Invariant | Enforcing line |
|---|---|
| D6/REQ-032 server-internal id read | `findLinkTargetByHandshakeCode` :290-295 → shared `readHandshakeCodeJoinRow` :72-99 (single `eq(students.handshakeCode, code)`, dual executor, LIMIT 1); discovery drop-the-id mapping :195-214 |
| REQ-021/D3 INV-P1 single writer | `linkParentIfUnlinked` :314-325 — ONE `UPDATE students SET parentId, updatedAt=now() WHERE id AND parent_id IS NULL RETURNING *`, REQUIRED tx; repo-wide grep: only file updating `students` |

**Service + helpers:**

| Invariant | Enforcing line |
|---|---|
| REQ-031 actor re-check on EVERY op | helpers `requireActor` :222-274 (id guard :231, missing→401 :241-250, role→403 :252-260, governance fail-closed same-copy :262-271); called at service :169/:241/:347 (mutations, governance ON) and :389/:407 (relaxed reads) |
| REQ-012/D7 null-collapse byte-shape | helpers :100-104 (missing ≡ governed → same `null`, zero writes/notifications); service :192-194 returns before any publish |
| REQ-013 already-linked honest conflict | helpers :106-114 |
| REQ-014/043 pre-check + 23505 arbiter | helpers :116-125 (pre-check) and :137-152 (`isUniqueViolation` cause-chain → SAME conflict; non-23505 rethrown unmasked) |
| D3/REQ-042 whole-tx rollback on link race | service :266-275 (`linkParentIfUnlinked` null → ConflictError → unit rolls back; ghost confirmation impossible) |
| REQ-091 sibling expiry | service :277 (accept branch only, after link) |
| REQ-017 reject = claim only | service :279-281 (no students write, no sibling expiry) |
| D8 recipient-locale copy at emitter | request: helpers :175-191 (student locale via `findLocalesByIds` + `defaultLocale`); respond: service :284-302 (parent locale); verbatim-stored through `getServerTranslations(recipientLocale).notificationsTranslations` |
| REQ-023 in-tx emit + own-commit-only publish | `emitForUser(..., tx, options)` helpers :179-191 / service :288-302; `publishReceipts` gated `if (outerTx === undefined)` service :197-199, :321-323; cancel silent :372; receipt-vs-row narrowing `isDeliveryReceipt` helpers :363-367 (engine in-tx branch returns `{notifications, recipientUserIds, emitClaimKey}` — `notification-engine.service.ts:65-68`) |
| REQ-034 constant-shape classifier | helpers :289-307 (foreign ≡ nonexistent → "not-found"; fail-closed `toCanonicalLinkStatus` :375-386 before the pending comparison) |
| REQ-094 fold survives the denial | helpers :328-337 (`markExpiredIfPending` in its own surviving unit BEFORE the throw) |
| REQ-015 read purity | `mapOutgoing`/`mapIncoming` :394-421 (strict `expiresAt <= now` render, zero writes) |
| REQ-020 self-scope + masking | lists self-scoped on verified id (service :390/:408); `maskFullName` on outgoing (helpers :399); full parent name incoming (:416) |
| REQ-035/054 log hygiene | every `logDomainError` bag = {code, entity, entityId?, locale}; zero happy-path logs; handshake code never logged; Tier-4 closed-key-set + secrets sweep pins it in-test |
| REQ-050 closed code set | 5 new codes + 3 reused, all via ConflictError(code, message)/NotFoundError(entity, message) overloads |

**Resolvers (5 root fields, read end-to-end):**

| Invariant | Enforcing line |
|---|---|
| `$all` role conjunctions (REQ-030) | mutation :159-164 (Parent), :187-192 (Student), :210-215 (Parent); query :60-65 (Parent), :89-94 (Student) — 5/5 |
| Thinness + ctx identity only | bodies ≤14 lines, delegate with `ctx.user.id` + `ctx.locale`; zero identity args |
| Pre-DB ID parser | `parseLinkRequestIdArg` :99-108 (`/^[1-9]\d*$/` + `isPositiveSafeInt`, NaN sentinel) via `requireVerifiedRequestId` :130-144 (throws localized ValidationError pre-service) |
| ctx.t localized denials | :135-137, :140-142, :169-171; query :75-77, :99-101 |
| No try/catch swallowing | zero catch blocks in both files (docblock mentions only); DomainErrors propagate to the masking boundary |
| public-operations untouched | `git diff a259524..HEAD -- backend/lib/gateway/public-operations.ts` is EMPTY |
| Pothos objects | 6/6 fields each, `t.exposeID` id first, `LinkStatusPothosEnum` (once-registered), `type: "DateTime"` ×6, `respondedAt` only nullable — `parent-link-request.pothos.ts:40-65` |

**Schema / gates re-verified:**

- REQ-045: `git diff a259524..687bea2 -- backend/db/schema/` = EXACTLY `parents/index.ts` (+1 line) + `parent-link-requests.ts` (new, 54 lines: RESTRICT FKs, app-written `expiresAt`, partial unique `WHERE status='pending'` as a `uniqueIndex(...).where(...)` :50-52).
- Notification-receipt dedup across request/respond/cancel: exactly ONE inbox row + ONE publish per own-commit mutating op, bound to the winner's request id; cancel and every denial arm emit/publish ZERO — pinned by chaos cells 1/2/3/4/5 (publishCount + inbox-length assertions on both race branches) and service Tier-1 arms. No idempotency keys on this family (REQ-023 rules it out; natural guards).

## 4. Test honesty (deep spot-reads)

- **Chaos suite (10 cells — count verified):** cell 1 (duplicate-create) would catch: two committed rows (`pendingCountForStudent === 1`), raw 23505 leaking (`hasPgCode(reason,"23505") === false`), loser publishing (`secondTransport.publishCount === 0`). Cell 5 (two-parent confirm) pins the loser-committed-nothing property (`loserRow.status === Expired`, `respondedAt` null, loser inbox empty, winner's sibling sweep converges) with the narrow 40P01 escape hatch. Cell 10 (forced post-claim failure) pins whole-unit rollback across all three tables plus unmasked propagation and log silence. All fixtures are COMMITTED-plane (two real connections) — the races prove something.
- **Static locks (36 tests — 5 corpus guards + 7 + 3 + 4 + 3 + 5 + 9 negative fixtures):** lock (a)'s `toEqual([student.repository.ts])` exact-match assertions make any NEW literal writer fail loudly; the caller pin (`references === [repo, service]`) catches new production callers; negative fixtures genuinely fire (non-vacuity), including the self-scan-safe console fixture built via `join`. Caveats F5 (spread-shaped evasion) accepted by the suite's own header.
- **Byte-shape oracle (service Tier 1, :946-1002):** foreign ≡ nonexistent pinned via `expect(fingerprint).toBe(foreignStudent)` across BOTH directions — a genuine byte-identity regression trap.

## 5. Process notes

- The worklog's "watchdog reset-on-clean-tree" hazard re-manifested during this review (HEAD force-reset `687bea2` → `main`/`a259524` between command batches). The guard-rail checkout was re-asserted per batch and the review corpus was snapshotted after a verified HEAD check; all file:line references above were re-validated against the branch tree.
- outcomes 2.2 §6, 2.3 (contract) and 3.1-3.2 (five root-field signatures) were read BEFORE the code; the shipped surface matches all three contracts with the deltas this file records (F1, F3).

**Reviewer:** 6.2 review-backend wave (general-purpose subagent) · HEAD reviewed: `687bea2`
