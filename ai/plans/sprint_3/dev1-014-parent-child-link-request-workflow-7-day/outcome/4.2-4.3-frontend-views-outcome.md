# Outcome — Tasks 4.2 + 4.3: Student link-requests page + Parent handshake outgoing section (frontend views)

- **Status:** ✅ COMPLETE (all non-browser gates GREEN) — QL exit 0 on every touched file, `bun tsgo` 0 errors, `test:ui:components` 221 pass / 0 fail (16 files), regression suites green + byte-untouched. Browser self-loops (4.2.BF/BS, 4.3.BF/BS) deferred → ledger **D8** (dev server down all dispatch window).
- **Branch:** `feat/dev1-014-parent-child-link-request-workflow-7-day` @ `23d1b9f` (guard-script protected every batch; third pass over inherited artifacts — two prior agents wrote all code, this pass fixed the one known defect, ran gates, bookkept).

## Artifacts (final tree)

### 4.2 — Student surface
| File | Kind | Notes |
|---|---|---|
| `app/(dashboard)/student/link-requests/page.tsx` | CREATE | Server Component: `withPageAuth({ roles: [UserRole.Student] })`, `getTranslations(locale)` one-arg, renders `<StudentLinkRequestsContainer />` (39 ln) |
| `frontend/views/students/link-requests/StudentLinkRequestsContainer.tsx` | CREATE | `useQuery(MyIncomingParentLinkRequests)` + `useMutation(RespondToParentLinkRequest)`; branch table (permission/empty/skeleton/denial), confirm/reject dialogs via `confirmDialogBody`/`rejectDialogBody`, refetch on success (188 ln) |
| `frontend/views/students/link-requests/IncomingStates.tsx` | CREATE | Header/empty/toast/skeleton shells (172 ln) |
| `frontend/views/students/link-requests/IncomingBody.tsx` | CREATE | Settled list region (131 ln) |
| `frontend/views/students/link-requests/LinkRequestCard.tsx` | CREATE | Per-row card, computed expired chip via shared status lib (129 ln) |
| `frontend/views/students/link-requests/LinkRequestDecisionDialog.tsx` | CREATE | Confirm/reject decision dialog (93 ln) |
| `frontend/views/students/link-requests/index.ts` | CREATE | Barrel (5 exports) |
| `frontend/lib/parent-link-request-status.ts` | CREATE | Shared read-pure computed status (`isLinkRequestActionable` strict-`>` parity with the service classifier; `parentLinkStatusChipSpec` Record<LinkStatus,…>) (67 ln) |
| `test/ui/components/students/StudentLinkRequestsContainer.test.tsx` | CREATE | 22 tests — pending-live CTAs, expired-computed chip, confirmed/rejected CTAs absent, dialog flow + in-flight disable + refetch, `PARENT_LINK_REQUEST_EXPIRED` denial wave → Errors-tree inline Alert (462 ln) |

### 4.3 — Parent surface
| File | Kind | Notes |
|---|---|---|
| `frontend/views/parent/handshake/HandshakeDiscoveryContainer.tsx` | UPDATE | Send Request CTA on `linkable: true` wired to `useMutation(RequestParentChildLink)`; **null-collapse REQ-012** (`payload === null` → `sendUnavailableNotice` info state); `PARENT_LINK_ALREADY_PENDING`/`_TARGET_ALREADY_LINKED` → localized inline Alerts; `refetchQueries` on the outgoing list (343 ln) |
| `frontend/views/parent/handshake/OutgoingLinkRequestsSection.tsx` | CREATE | `useQuery(MyOutgoingParentLinkRequests)` + `useMutation(CancelParentLinkRequest)`; masked-name rows (REQ-020), Cancel CTA only on live-pending, post-cancel refetch = withdrawal fold to `rejected` chip (196 ln) |
| `frontend/views/parent/handshake/OutgoingSectionStates.tsx` | CREATE (split) | Settled/result surfaces: empty state, denial alert, settled list, success toast + `CANCEL_TOAST_AUTOHIDE_MS` (100 ln total / 70 code lines) |
| `frontend/views/parent/handshake/OutgoingSectionStates.parts.tsx` | CREATE (split) | Unsettled branch chain: retryable-code seam + skeleton list + generic-failure alert + `OutgoingUnsettledBody` flat early-returns (124 ln total / 91 code lines) |
| `frontend/views/parent/handshake/OutgoingLinkRequestCard.tsx` | CREATE | Masked-name row card, expiry line, Cancel affordance (106 ln) |
| `frontend/views/parent/handshake/OutgoingLinkRequestCancelDialog.tsx` | CREATE | `cancelDialogTitle`/`cancelDialogBody` dialog (85 ln) |
| `frontend/views/parent/handshake/index.ts` | UPDATE | Barrel + `OutgoingSectionStates` + `OutgoingSectionStates.parts` (IncomingStates-in-barrel precedent) |
| `frontend/lib/parent-link-denials.ts` | CREATE | Constant-shape denial table: 5 wire codes → `errors`-tree accessors; unmapped/null folds to `internalServerError` (raw code never reaches DOM) (41 ln) |
| `test/ui/components/parent/OutgoingLinkRequestsSection.test.tsx` | CREATE | 20 tests — Cancel CTA on live-pending only, expired chip NO CTA, cancel mutation + refetch + rejected chip, denial wave, empty/skeleton branches (449 ln) |
| `test/ui/components/parent/HandshakeDiscoveryContainer.test.tsx` | UPDATE | +send-affordance tier: CTA on linkable, exact `{ code }` variables, success toast + refetch, `null` payload → `sendUnavailableNotice`, conflict-code Alerts (38 tests total, 736 ln) |
| `test/ui/components/translation-preload.ts` | UPDATE | Preload extended for the new surfaces (5 lines) |

## Max-lines fix (KNOWN DEFECT #1 — the only code change of this pass)

`OutgoingSectionStates.tsx` was 157 code lines (207 total) vs the 150 frontend-view budget (`oxlint.config.mts:72`, `skipBlankLines+skipComments`). A prior agent died mid-refactor. **Fix:** cohesive decomposition into a sibling `OutgoingSectionStates.parts.tsx` — moved the *unsettled branch chain* (`OUTGOING_RETRYABLE_CODES`, skeleton list, private `OutgoingLoadErrorAlert`, `OutgoingUnsettledBody` flat early-returns); kept *result surfaces* (empty/denial/list/toast) in the parent file. Zero logic changes; the section now imports `OutgoingUnsettledBody` from the parts file directly; barrel exports both. Root-cause lint note: `isRetryableOutgoingCode` had to become module-private in the parts file — `react-refresh/only-export-components` forbids non-component exports from component files (the original never reached this lint stage; the QL sub-loop stops at first failing stage). Grep-proven: no external consumer existed. **70 + 91 code lines, both green.**

## Pipeline evidence

| Gate | Result |
|---|---|
| `bun tsgo` (FULL project) | **exit 0, 0 errors** |
| QL `sub-loop --lifecycle duplicates` — `OutgoingSectionStates.tsx`, `OutgoingSectionStates.parts.tsx`, `OutgoingLinkRequestsSection.tsx`, `handshake/index.ts`, `StudentLinkRequestsContainer.tsx` (re-confirm) | **exit 0 ×5** (earlier agent's log `/tmp/ql-out.txt` had only the max-lines failure outstanding; parts-file first run failed `react-refresh/only-export-components` → root-caused above → exit 0) |
| Targeted UI: `StudentLinkRequestsContainer.test.tsx` + `OutgoingLinkRequestsSection.test.tsx` + `HandshakeDiscoveryContainer.test.tsx` (locked runner, `.env.test.ci`, 4 preloads) | **80 pass / 0 fail** (22 + 20 + 38) |
| Full `bun run test:ui:components` (16 files) | **221 pass / 0 fail** |
| Regression: `navItems.test.ts` (run-test.ts) | **15 pass / 0 fail**, file byte-untouched |
| Regression: `parent-link.documents.test.ts` | **15 pass / 0 fail**, file byte-untouched |
| Regression: `apolloCache.test.ts` | **10/10 pass** (the 4.1-era 9/1 frozen-list drift is resolved upstream), file byte-untouched |
| DB-backed tests | N/A — no touched suite touches the DB (Apollo MockedProvider only); `DATABASE_URL` override + Postgres env prepared but not needed |

## SEC / SR / IV

- **SEC PASS:** identity args never cross the wire (zero-arg list queries, server-derived parent id); denial mapping is constant-shape via `parent-link-denials.ts`; no `console.*`, no raw code / PII in DOM; null-collapse branch is the ONLY consumer of the only-nullable `RequestParentChildLink` payload.
- **SR PASS:** `useQuery`-only (repo scan: zero real `useLazyQuery` usage — comment-only ban mentions intact in `HandshakeDiscoveryContainer`); `sx`-only styling, `theme.palette` roles, `focusVisibleRingSx`, `dir="auto"` names, `aria-busy` output regions; `useAppTranslation(ParentLink/Errors/Common)` property access; expired-state rendering read-pure (shared strict-`>` classifier parity, no writes); barrels exported in both dirs; zero local duplicates of generated `graphql.ts` types (enum imported from the single generated source).
- **IV PASS with ZERO instruction-doc edits:** `frontend.instructions.md`, `frontend/AGENTS.md`, `app/AGENTS.md` honored. All used i18n keys verified present in **both** `shared/locale/en/parentLink` + `shared/locale/ar/parentLink` (parity test green) — **zero locale edits needed, zero new keys**.

## Drift notes

- None new. (4.1-recorded drifts stand: tasks.md 4.4 nav path already corrected; `sharedDocuments/AGENTS.md` Layout staleness remains a docs-only candidate; `frontend.instructions.md` stale `frontend/common/graphql/` citation stands.)
- Sandbox: clean-tree watchdog risk mitigated by `.task423-backup/guard.sh` at the start of every batch (branch + HEAD `23d1b9f` verified each run).

## Carry-forwards

1. **D8 (ledger):** 4.2.BF/BS + 4.3.BF/BS agent-browser functional + visual/screenshot self-loops deferred — dev server on port 3000 down all window (mandate: leave it). Needs a dev-server-enabled orchestrator dispatch: login-inject sessions, seeded fixtures, 1440/768/375 × en/ar screenshots, RTL + palette polish loop, role cross-probe redirect assertions.
2. **Phase 5 wire matrix (5.1):** 401/403/validation split for the five root fields — component tier pins UI branches via MockedProvider; the network-tier matrix stays owned by 5.1.
3. `sharedDocuments/AGENTS.md` doc-refresh candidate (stale parents-domain description) — docs-only dispatch.
