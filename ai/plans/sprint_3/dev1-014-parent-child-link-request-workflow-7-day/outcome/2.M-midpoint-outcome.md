# Outcome — Task 2.4: Phase 2.M Mid-Point Review Gate

- **Task ID:** 2.4 · **Status:** ✅ GATE PASSED — Phase 3 unlocked
- **Agent:** orchestrator (mechanical sanity + semantic diff review), per REQ-083
- **Date:** 2026-09-01 · HEAD at gate: `6fb5088`

## Tier liveness (all GREEN at gate)
| Tier | Evidence |
|---|---|
| Journey (2.1) | 19 pass / 0 fail — turned GREEN by 2.3, re-verified twice; zero jrn_plink_ residue |
| Repo tier (2.2.TE) | test:db 15 files / 226 tests / 0 failed (31 parent-link + 16 student-repo tests included) |
| Service tier (2.3.TE) | test:services 271 passed / 0 failed (incl. 29 parent-link service tests / 287 expects) |
| Type-check | bun tsgo 0 errors (baseline 0.1 held) |
| Format/lint | bun biome:check 1074 files, no fixes (baseline held) |

## Sanity assertions (tasks.md 2.4, executed mid-flight)
1. Schema surface (REQ-045 pre-check): git diff a259524..6fb5088 -- backend/db/schema = EXACTLY parent-link-requests.ts (+54: table, 2 FKs RESTRICT, 3 indexes, partial-unique pending_pair_unique with native where — D4) + parents/index.ts (+1 barrel line). No other schema drift.
2. Single-writer scan (5.3 precursor): the only non-null students.parent_id writer is linkParentIfUnlinked (guarded parent_id IS NULL UPDATE; winner/second-null/rival-null proven by 16 student-repo tests). Other grep hits are READ mappings only (user-management.mappers.ts:155 pre-existing admin DTO; student.repository.ts row→type reads). 5.3 static pin ready.
3. Ledger: open ❌/⚠️ = 0 (D1–D3, D6, D7 📅 Forward; D4/D5 ✅). HARD GATE satisfied.

## 2.4.SR — journey diff-review (16d7d09 → 6fb5088, +58/−9)
Verdict: GREEN because the SERVICE surface is correct — no test weakening, no scope creep. Every edit same-or-stronger:
- beforeAll snapshots re-captured AFTER governance/link fixtures (byte-stability vs TRUE pre-state).
- Step 8: rows selected BY ID (repo pins created_at DESC); asserts BOTH rows (A confirmed, B expired) — full §4.4 sibling-visibility matrix.
- Steps 11/17/19: absolute inbox pins → exact-delta pins + per-request binding.
- Step 12: history-list semantics (2 rows by id: fresh pending + rejected) — property strengthened.
- Step 15: pair-scoped (A,F) count — same folded-history property, precisely scoped.
- Steps 13–16, 18 bodies UNCHANGED (failures were cascade from upstream uncleared spies).
- Zero-publish arms, byte-equality NOT_FOUND fingerprints, guarded-link, computed-expiry, REQ-053/054 probes, teardown residue: intact and passing.

## Deviations & ledger deltas since Phase 1
- Test-path drift (2.2 outcome §10): repo/journey tests live under backend/db/test/repo/** and test/workflows/** — stale literal paths in older task bullet text; commands executed on real locations.
- parent-link-request.helpers.ts (421 ln) extracted from service for max-lines budgets — public surface unchanged.
- Journey-test authoring fixes (2.3 outcome §"Journey-test edits") — all same-or-stronger.
- No new deferred items; no forward-status changes.

## Phase-3 readiness
- Repo + service + journey green; barrels wired. Phase 3 needs: Pothos LinkStatus enum (VALUE import, ONCE), Outgoing/IncomingParentLinkRequest objectRefs, viewer-scoped queries + mutations with canonical error mapping, t.parentLink* keys (landed in Phase 1).
- Environment note: a sandbox watchdog intermittently resets the working branch; recovery = git branch -f feat/dev1-014-... <origin-tip> && git checkout -f. Origin is the source of truth (pushed through 6fb5088).
