# Task 4.2 — Admin `/disputes` Extensions

**Task ID:** 4.2 · **Plan:** `ai/plans/sprint_3/dispute-resolution-with-admin-arbitration/`
**Agent:** general-purpose subagent (implementation) + orchestrator (completion verification after interrupted runs)

## Summary
The admin arbitration console now serves both dispute generations. `ResolveDisputeOptionGroup` became prop-driven (options derived from the row's escrow class via `resolveDisputeOutcomeOptions`: held → Cancel/Complete, consumed → Refund/PartialRefund/Uphold; the shipped unknown-value-collapses-to-Cancel handler is gone — every picked value passes through as-is). `ResolveDisputeDialog` threads `fee`/`feeHeld` from the container's query data, renders the classified option group, shows the partial-amount field only for PartialRefund (client gate `isPartialAmountValid`: `^\d+(\.\d{1,2})?$`, 0<amt<fee, fee-less fails closed), and surfaces localized errors (`partialRefundAmountInvalid`, `disputeResolutionMismatch`). A stale off-class selection renders the mismatch alert (never coerced). New `AdminDisputeCaseDialog` (+ `AdminDisputeCaseArtifacts`/`SessionFacts`/`AuditTrail` siblings) consumes `adminDisputeCaseQueryDocument` with honest empty states. `AdminDisputeEscrowChip` renders the escrow class on rows. Mobile uses the shipped responsive dialog geometry.

## Files created/modified
- NEW: `ResolveDisputeAmountField.tsx`, `ResolveDisputeIntroBanner.tsx`, `ResolveDisputeNoteField.tsx`, `ResolveDisputeActionsRow.tsx`, `ResolveDisputeFormFields.tsx`, `resolvePartialAmount.ts`, `resolveDisputeOutcomeOptions.ts`, `AdminDisputeCaseDialog.tsx`, `AdminDisputeCaseArtifacts.tsx`, `AdminDisputeCaseAuditTrail.tsx`, `AdminDisputeCaseSessionFacts.tsx`, `AdminDisputeEscrowChip.tsx` (all under `frontend/views/admin/disputes/`)
- MODIFIED: `ResolveDisputeDialog.tsx` (prop-driven, composition of extracted siblings), `ResolveDisputeOptionGroup.tsx` (component-only after rule-prescribed extraction), `AdminDisputeRow.tsx`, `AdminDisputesBody.tsx`, `AdminDisputesContainer.tsx`
- TESTS: `test/ui/components/admin/ResolveDisputeDialog.suite.tsx`+`.test.tsx`, `AdminDisputeCaseDialog.suite.tsx`+`.test.tsx`, `AdminDisputeEscrowChip.suite.tsx`+`.test.tsx`, `AdminDisputesContainer.suite.tsx` (extended)

## Files NOT modified (and why)
- `shared/locale/**` — consumed existing keys only (Task 4.4 owns namespace additions); `frontend/views/student/**` — Task 4.3's area.

## Verification results
- Sub-loop `--lifecycle duplicates`: exit 0 for ALL scope files (per-file loop; final pass 36/36 after fixes).
- UI tests (sanctioned runner, Happy DOM + mocked Apollo): admin suites 41 pass/0 fail; container suite 10 pass/4 skip (pre-existing env-deferral skips)/0 fail; combined slice 51 pass/0 fail/4 skip.
- `bun tsgo`: 0 errors. Knip: 0 findings.
- Fix cycles (documented): react-refresh/only-export-components ×2 (moved builders into sibling modules — rule's own prescription); jscpd intra-file clones in the dialog suite (extracted `disputeDialog(...)` builder); max-lines-per-function (extracted `ResolveDisputeFormFields`/`ResolveDisputeActionsRow`); oxlint `unicorn/no-array-sort` → `toSorted` in student suite (4.3 file, fixed jointly).

## Semantic review checklist
- [x] Race conditions: N/A (pure UI; mutation wire is the service's guarded path)
- [x] No module-level mutable state
- [x] No dead branches — old whitelist fully removed; all arms reachable
- [x] No cross-layer imports (views import sharedDocuments barrel + shared/locale only)
- [x] Enum value imports where runtime-used (`DisputeResolution` value import in options builder)
- [x] No string literals where enum types expected (wire comparisons via documented string idiom)
- [x] No fabricated data — honest empty states only
- [x] i18n via namespace handles + property access (no t('key'))
- [x] No deferred work without ledger entry
- [x] Scope boundary: admin disputes area only
- [x] Clean comments: zero plan-artifact references (grep-verified); domain language; no trivial comments

## Instruction verification
frontend.instructions.md + layer AGENTS.md read via sub-loop auto-discovery; react-refresh rule prescription followed; test-hygiene rules (no console.*, no any, no disables) hold.

## Carry-forward knowledge
- `resolveDisputeOutcomeOptions(feeHeld, t)` is the single source of the classification vocabulary — reuse for any future surface; never hardcode outcome lists.
- `deriveResolveDisputeDraftState` owns the dialog's live draft semantics (picked-value string idiom, stale-selection surfacing, live amount-policy error).
- Partial-amount UI gate is a convenience seam only — the server re-validates before any write.
- 4 pre-existing skips in `AdminDisputesContainer.suite.tsx` are env deferrals, untouched.

## Cross-file dependencies discovered
- `ResolveDisputeFormFields` imports the option type from `resolveDisputeOutcomeOptions` (moved module) — stable.
- Escrow chip + case dialog consume `adminDisputedSessions` row fields already selected by 4.1 documents.
