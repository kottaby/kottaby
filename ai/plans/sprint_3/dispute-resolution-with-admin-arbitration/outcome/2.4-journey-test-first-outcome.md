# Task 2.4 — Journey Test Authored TEST-FIRST (post-confirmation dispute arbitration)

**Task ID:** 2.4 · **Plan:** `ai/plans/sprint_3/dispute-resolution-with-admin-arbitration/`
**Date:** 2026-09-12 · **Agent:** general-purpose subagent
**Working tree at start:** HEAD with the uncommitted 2.1 pin update (`schema-surface.test.ts`), the 2.3 checkbox flip in `tasks.md`, and — discovered mid-run — Task 2.5's in-flight implementation (`session-arbitration.service.ts` + helpers + its test + the three new error-locale keys), authored by the parallel 2.5 execution. Untouched by this task.

---

## Summary

Authored the acceptance-harness journey
`test/workflows/sessions/post-confirmation-dispute.journey.test.ts` — 13 tests
covering J1 (Refund happy path with queue + case review), J2 (PartialRefund
`"15.00"` on the `"25.00"` fee), J3 (Uphold, zero financial writes), the
cross-actor denial matrix (non-admin 403 byte-identity, teacher
post-confirmation open oracle collapse, classification mismatch in both
directions, insufficient teacher wallet), and the real-PG double-arbitration
race. All layer rules hold: NO `runInRollback`, committed fixtures with
tracked FK-safe teardown + zero-residue re-probes (two consecutive runs leave
zero residue), honest actor provisioning through the shared factories with a
per-run `jrn_sessions_<8hex>` prefix, notification dispatch spied at the
`NotificationEngine` boundary, and try/catch denial assertions.

**Expected-red outcome (measured, not assumed):** the suite is red exactly on
the dispute-notification surface — **8 pass / 5 fail**, and every failure is
the same shape: a dispute-wave inbox-row count `Expected: 1, Received: 0`
(the `session_dispute_opened` admin wave on open; the
`session_dispute_resolved` participant waves on all four arbitration
outcomes). That is precisely Task 2.6's unlanded surface: the in-flight
arbitration service's own docblock marks the seam ("Until that service lands,
this module writes no notification rows and publishes nothing"). Everything
else — the entire 2.5 surface (open, queue, case read, all three outcomes,
financial exactly-once, audit rows) and every denial and the race — runs
GREEN already.

**Deviation from the brief's premise, documented:** the brief expected the
suite red on *missing-module imports* (2.5/2.6 not landed). While this task
ran, Task 2.5's implementation appeared in the working tree (parallel
execution — its files timestamped during this session), so the import surface
resolves and the red shifted to where 2.6 is still absent. The suite was
authored against the plan's Component-1 signatures, which the landed 2.5
matches exactly — no journey edits were needed to bind it. Nothing was
stubbed, forced green, or otherwise falsified: the five reds are the honest
acceptance state relative to 2.6.

## Files Created

1. **Created** `test/workflows/sessions/post-confirmation-dispute.journey.test.ts`
   (~1045 lines) — the only file this task touched. NOT touched: `worklog.md`
   (per protocol), the service/repo/locale files (2.5's in-flight authorship),
   anything else; nothing committed.

## Journey Coverage (what each leg pins)

| Leg | Assertions (through REAL services, no outer tx) |
|---|---|
| **J1 step 1** | Committed cast posture: 7 real users + role children in the session registry (1 admin), the race's second arbiter a REAL admin pair in his own tracked registry; the teacher starts with NO wallet (the credit slice ensures it). |
| **J1 step 2** | Booking → start → teacher-complete → student-confirm through `SessionLifecycleService`: dual-confirmed, `feeHeld=false`, fee `"25.00"`, provenance lane `trial` recorded (never rewritten), trial lane consumed 5→4, wallet credited exactly the fee (balance + total). |
| **J1 step 3** | The teacher submits the case artifacts through `SessionReportService` (report + Jadid/Madi homework) on the completed row — real cross-actor data for the case read. |
| **J1 step 4** | Teacher's post-confirmation open attempt → `SESSION_NOT_FOUND` (`NotFoundError`, exact `sessionNotFound` copy), row byte-identical — a participant teacher collapses into the nonexistent-caller oracle. |
| **J1 step 5** | Student opens via `SessionArbitrationService.openPostConfirmationDispute`: `disputed`, reason/disputedAt set, escrow stays consumed; the row appears in `listAdminDisputedSessions` (+1 total, `feeHeld=false`, reason, stamp); the opened wave asserted per-admin as inbox rows AND as ONE publish dispatch targeting both admins at the spied engine boundary, with zero fan-out to participants/parent. **RED — 2.6.** |
| **J1 step 6** | `getAdminDisputeCase`: session detail (`disputed`), the submitted report + homework, honest-null recitation, empty session-scoped audit trail. |
| **J1 step 7** | `arbitrateDispute(Refund)`: back to `completed` with note + stamp; wallet debited exactly the fee (`total_earning` untouched); trial lane +1 exactly once; exactly ONE `Override` audit row with `resolution`/`refundAmount`/`notePresent`; ledger = earning + ONE compensating `withdrawal`/`completed` row (amount verbatim, session-keyed, reversal description); resolved wave to exactly student+teacher (rows + ONE publish). **Wave half RED — 2.6.** |
| **J2** | PartialRefund `"15.00"` on the `"25.00"` fee: wallet −15.00 (not −25.00), lane +1, compensating row amount `"15.00"`, ONE audit row whose details carry `partialAmount` as the exact STRING (quoted-JSON match proves the decimal-string discipline survived persistence) + `notePresent`; resolved wave. **Wave half RED — 2.6.** |
| **J3** | Uphold: `completed` with note + stamp; ZERO financial writes (wallet balance + total, single earning ledger row, lane all byte-stable); ONE audit row with `resolution=Uphold`; both participants notified. **Wave half RED — 2.6.** |
| **Denial 1** | Non-admin resolve matrix (teacher / student / parent × `resolveSessionDispute` reference vs `arbitrateDispute`) — every denial `ForbiddenError`/`FORBIDDEN`/`forbidden` copy, byte-identical between the shared admin gate and the new surface; zero writes (row byte-identical, zero audit rows by any denied actor). |
| **Denial 2** | Classification mismatch both directions: `Refund` on a held disputed row and `Complete` on a consumed disputed row each → `ValidationError`/`VALIDATION` carrying the exact `disputeResolutionMismatch` copy; the two denials byte-identical; both rows byte-identical after. |
| **Denial 3** | Emptied teacher wallet (real `requestWithdrawal` of the full credited balance after confirmation): Refund → `ConflictError`/`WALLET_INSUFFICIENT_FUNDS` with the exact `insufficientBalance` copy; the session REMAINS `disputed` (full-row equality incl. `updatedAt`); zero financial/audit/notification/publish deltas. |
| **Race** | Two admins arbitrating one disputed row via `Promise.allSettled` (real PG; `test.skip` under PGlite like the sibling chaos tiers): exactly one fulfillment (`completed`), the loser a `ConflictError` `SESSION_INVALID_TRANSITION`; exactly one fee debit, one lane credit, one compensating row, one audit row, one resolved wave. **Wave half RED — 2.6.** |

## Expected-Red Analysis (exact, from the mandated runner)

```
bun run test/scripts/run-test.ts test/workflows/sessions/post-confirmation-dispute.journey.test.ts
→ 8 pass / 5 fail, 150 expect() calls, exit 1
```

| # | Failing test | Failure site | Exact shape |
|---|---|---|---|
| 1 | J1 step 5 | `:576` | `countDisputeNotifications(cast.admin, session_dispute_opened, refundSession)` → Expected 1, Received 0 |
| 2 | J1 step 7 | `:683` | `countDisputeNotifications(cast.primaryStudent, session_dispute_resolved, refundSession)` → 0 |
| 3 | J2 | `:778` | same counter, `partialSession` → 0 |
| 4 | J3 | `:835` | same counter, `upholdSession` → 0 |
| 5 | Race | `:1092` | same counter, `raceSession` → 0 |

**The missing symbol is exactly one:** the dispute-notification service
(`backend/services/classes/session-dispute-notification.service.ts` — Task 2.6,
not yet created). The arbitration service's in-flight docblock pins the seam
(`openPostConfirmationDispute`/`arbitrateDispute` "dispute-wave seam" marks).
The once-feared missing-module red for `SessionArbitrationService` did NOT
materialize because Task 2.5 landed mid-run (see Summary); the suite binds the
plan's exact Component-1 signatures:

```ts
openPostConfirmationDispute(callerUserId: number, sessionId: number, reason: string,
  locale: string, outerTx?: DBTransaction): Promise<SessionReturnType>
arbitrateDispute(adminId: number, sessionId: number, resolution: DisputeResolution,
  note: string | null, partialAmount: string | null, locale: string,
  outerTx?: DBTransaction): Promise<SessionReturnType>
getAdminDisputeCase(adminId: number, sessionId: number, locale: string,
  tx?: DBTransaction): Promise<AdminDisputeCaseReturnType>
```

Teardown integrity under red: both consecutive runs executed `afterAll` with
zero cleanup/residue errors — a second red run over a clean database proves
the committed-fixture lifecycle does not leak (AGENTS.md idempotent-teardown
proof).

## 2.4.QL — sub-loop `--lifecycle duplicates` (final run, exit 0)

| Check | Result |
|---|---|
| tsgo (project-wide, file-filtered) | ✅ 0 errors |
| oxlint | ✅ (after one fix cycle: two `no-await-in-loop` in `afterAll` → `Promise.all` probes) |
| biome:check | ✅ |
| lint:type-aware | ✅ |
| check:duplicates | ✅ (skipped — test file outside jscpd scope, as for all journey files) |
| **Exit** | **0** |

## 2.4.TE — 4-Tier mapping

- **Tier 1 (branch coverage via journey steps):** every happy-path branch of
  the new surface executes end-to-end (open, queue surfacing, case read, all
  three outcomes) and every denial branch fires through the real guards
  (not-found oracle, state conflicts, role gate, classification mismatch,
  funds guard).
- **Tier 2 (boundary):** the money boundary is the brief's exact pair —
  PartialRefund `"15.00"` strictly inside the `"25.00"` fee (debit −15.00,
  audit amount string verbatim); plus the escrow-class boundary (held vs
  consumed rows discriminated by `fee_held` in both mismatch directions) and
  the zero-debit boundary (Uphold = no financial writes at all).
- **Tier 3 (chaos/concurrency):** the real-PG double-arbitration race —
  exactly one commit wins, the loser classified, every financial artifact
  exactly-once (skipped under PGlite, matching the shipped race precedent's
  posture).
- **Tier 4 (security/denials):** byte-identical 403 split across three
  non-admin roles against both the shipped gate and the new surface;
  oracle-collapse on the teacher's open attempt; mismatch typed as
  `VALIDATION` + dedicated copy; insufficient funds leaves the row disputed
  with zero residue of any kind.

## 2.4.SEC — report per item

- **Honest role/authorization resolution:** the cast is real `users` rows +
  real role children via the shared factories (`buildSessionJourneyCast`,
  `provisionAdminActor`); no role/permission resolution is monkey-patched; the
  second arbiter is a second REAL admin, not a re-used identity. Denials fail
  through the production gates (`assertAdminGovernanceClean`, the student
  predicate, the classification probes).
- **Actor isolation via prefixed ids:** per-run `jrn_sessions_<8hex>` prefix
  on every idempotency key; identity fields are factory-generated UUIDs —
  repeated/parallel runs cannot collide; no seed/demo rows touched.
- **Oracle-collapse assertions:** the teacher-open denial asserts
  `SESSION_NOT_FOUND` (foreign ≡ nonexistent) and row byte-identity; the
  non-admin matrix asserts zero audit rows by any denied actor.
- **Notification spy boundary:** `NotificationEngine.publishReceipts` is
  spied (recording, never delivering) for the whole suite; every expected wave
  is asserted BOTH at the boundary (dispatch happened, which userIds, one
  dispatch per wave, negative recipients pinned) AND as persisted inbox rows —
  the dual-confirmation journey's established pattern.
- **Zero-write denials:** every denial leg pins full-row equality
  (incl. `updatedAt`) plus wallet/ledger/audit/notification/publish deltas.

## 2.4.SR — semantic checklist (each item)

1. **Scope boundary:** `git status` shows exactly ONE new file authored by
   this task (`test/workflows/sessions/post-confirmation-dispute.journey.test.ts`);
   the other working-tree entries belong to the 2.1/2.3/2.5 executions and
   were not touched. Nothing committed; `worklog.md` untouched.
2. **No fixtures left behind:** tracked registries (session registry +
   `TrackedFixtures` for the second arbiter) hard-delete cast, sessions,
   claims; case artifacts and both append-only ledgers are swept under the
   sanctioned trigger suspensions and re-probed to zero BEFORE the tracked
   teardown; after it, zero-residue re-probes cover sessions, wallets, and
   inboxes. Two consecutive runs are clean (no residue, no cross-run state).
3. **Clean comments, zero plan-artifact references:** grep over the file for
   `REQ-\d | DEV3- | Task \d | plan.md | tasks.md | specs.md | D-\d` = zero
   hits (the word "outcome" appears only as the domain noun for arbitration
   outcomes). The `runInRollback`/`rejects.toThrow` mentions are the layer-
   contract wording the shipped journey headers use, quoting prohibitions.
4. **No trivial comments:** every comment states a domain rationale (escrow
   generations, oracle collapse, provenance lane, publish-after-commit,
   exactly-once) — no narration of the code next to it.
5. **AGENTS.md naming/rules honored:** file name
   `post-confirmation-dispute.journey.test.ts` in the sessions domain dir;
   `bun:test` only; no `console.*`, no `any`, no `oxlint-disable`, no
   dynamic-import escape hatches; helpers under the length guidance;
   `@/` imports; assertions pin `DomainError.code` + exact translated copy
   (never raw keys, never hardcoded English).

## 2.4.IV — instruction verification

Sub-loop printed and I read in full: `.agents/instructions/tests.instructions.md`
and `AGENTS.md`; plus the layer rule file `test/workflows/AGENTS.md`
(rules 1–12 each verified above) and the pre-execution plan reads (specs
journeys section, plan design, tasks scope, all five outcome files).
Findings: journey-layer exception to the rollback rule documented in the
header; entity-setup signatures verified before use (second admin via
`provisionAdminActor`, cast via the session factory); run only through the
mandated runner; the `Promise.all` probe style follows the shipped
`no-await-in-loop` remediation; no GraphQL-server harness started in-process
(PGlite single-instance rule respected — the suite is DB-backed only).

## Carry-Forward for Task 2.6 (and 2.5 completion)

The journey pins the notification contract its five reds expect — land these
and the suite goes green without journey edits:

1. **Wave types + recipients:** `session_dispute_opened` → every admin
   (audience-resolved role broadcast; both cast admins asserted individually,
   non-recipients asserted at zero); `session_dispute_resolved` → exactly the
   two participants (student + teacher; admins and parent asserted at zero).
2. **Emission timing:** inbox rows commit INSIDE the flow's transaction (they
   must survive with the arbitration commit and roll back with a failed leg —
   the insufficient-funds leg asserts zero wave rows after the rollback).
3. **Publish contract at the engine boundary:** `publishReceipts` fired
   exactly ONCE per wave, after commit, with the wave's recipients —
   `publicationCallCount()` deltas of +1 per open and per resolution are
   pinned. Recipient ORDER is not pinned; membership is.
4. **Composition points:** the open wave inside `openPostConfirmationDispute`'s
   transaction; the resolved wave inside `arbitrateDispute`'s transaction
   (the service's marked seam comments). The journey never calls the
   notification service directly — the import surface stays
   `SessionArbitrationService` only.
5. **For 2.5 completion:** the race's loser classification
   (`SESSION_INVALID_TRANSITION` via the completion-leg-first order) and the
   audit-details keys (`resolution`, `refundAmount`/`partialAmount`,
   `notePresent`) are now journey-pinned and green against the in-flight
   implementation — keep them stable while landing 2.6.

## Cross-File Dependencies

- `test/workflows/sessions/post-confirmation-dispute.journey.test.ts` →
  `backend/services/classes/session-arbitration.service.ts` (2.5, in flight —
  bound and green), `session-lifecycle.service.ts` (shipped: booking,
  handshake, queue, held-family dispute, 403 reference),
  `session-report.service.ts` + `wallet.service.ts` (shipped fixture flows),
  `NotificationEngine` (spied boundary; 2.6 publishes through it),
  `test/workflows/helpers` (cast factory, actor-context factory, fixture
  registry, counters).
- Downstream: Task 2.6 turns the five reds green; Task 3.1 reuses the same
  denial bytes at the GraphQL surface; Task 5.1 inherits this suite as the
  journey gate for the hardening wave.
