# Task 4.3 — Student Dispute Action (post-confirmation)

**Task ID:** 4.3 · **Plan:** `ai/plans/sprint_3/dispute-resolution-with-admin-arbitration/`
**Agent:** general-purpose subagent (implementation) + orchestrator (completion verification after interrupted runs)

## Summary
Student rows on the sessions surface now offer the post-confirmation dispute action. Eligibility is the role-scoped predicate `isPostConfirmationDisputable(session)` (in `sessionRowPresentation.ts`): `status=completed ∧ confirmedByStudentAt≠null ∧ !feeHeld`, applied ONLY on the student-side arm — the shipped shared `DISPUTABLE_STATUSES` set was NOT widened (teacher rows keep exactly the pre-completion dispute path). `SessionDisputeConfirmDialog` was parameterized: the mutation-binding vocabulary moved to `sessionDisputeMutations.ts` (document + accessor arms for the shipped pre-completion document and the new `openPostConfirmationDisputeMutationDocument`); all pre-existing call sites keep byte-stable behavior; the student arm passes the new document. Success refreshes via the container's existing refetch pattern.

## Files created/modified
- NEW: `frontend/views/student/sessions/sessionDisputeMutations.ts`, `test/ui/components/student/StudentPostConfirmationDispute.suite.tsx` + `.test.tsx`
- MODIFIED: `frontend/views/student/sessions/sessionRowPresentation.ts` (predicate), `SessionDisputeConfirmDialog.tsx` (parameterized), `SessionRow.tsx` (CTA wiring), `StudentSessionsContainer.tsx` (import path), `frontend/views/teacher/sessions/TeacherSessionsContainer.tsx` (import path only — no behavior change)

## Files NOT modified (and why)
- `shared/locale/**` — consumed existing keys (Task 4.4 owns additions); admin area untouched (4.2).

## Verification results
- Sub-loop `--lifecycle duplicates`: exit 0 for all scope files.
- UI tests: `StudentPostConfirmationDispute.test.tsx` (both suites) — eligibility matrix (student×completed+confirmed+consumed → CTA; teacher rows → never; held rows → not via this path; other statuses → not), submit flow (id+reason), success refetch, denial surfacing — all green in the sanctioned-runner slice (51 pass/0 fail combined with admin suites).
- `DISPUTABLE_STATUSES` byte-stability asserted in tests.
- `bun tsgo`: 0 errors. Journey: 13 pass/0 fail (no regression).

## Semantic review checklist
- [x] Shared `DISPUTABLE_STATUSES` NOT widened (asserted)
- [x] Role trust boundary: container supplies the role token from server-guarded page auth; the server re-validates student ownership in the service predicate (client CTA is convenience only)
- [x] Prop-driven dialog (no hardwired document at the component seam)
- [x] No dead code (hardwired mutation binding removed)
- [x] i18n property-access handles only
- [x] No cross-layer imports
- [x] Scope boundary honored (import-path-only touch on teacher container)
- [x] Clean comments: zero plan-artifact references; domain language

## Instruction verification
frontend.instructions.md + layer AGENTS.md validated via sub-loop auto-discovery; react-refresh only-export-components prescription followed (mutation vocabulary extracted to sibling module).

## Carry-forward knowledge
- `isPostConfirmationDisputable` is the single student-arm eligibility source — any future surface must reuse it, never widen shared sets.
- `sessionDisputeMutations.ts` owns both generation arms of the dispute dialog binding — adding a third generation means a new arm there, not a new dialog.
- `feeHeld` is non-nullable `Boolean!` at the GraphQL layer (`?? false` at the Pothos resolve) — client predicates may use `!feeHeld` safely.

## Cross-file dependencies discovered
- Teacher container imports only the moved symbol path — zero behavior change (test-covered via existing teacher suites staying green in the full slice).
