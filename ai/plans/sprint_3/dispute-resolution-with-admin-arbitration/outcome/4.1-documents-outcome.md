# Task 4.1 — GraphQL documents (+ codegen outputs) — verified & completed

**Task ID:** 4.1 · **Plan:** `ai/plans/sprint_3/dispute-resolution-with-admin-arbitration/`
**Date:** 2026-09-12 · **Agent:** general-purpose subagent
**Base state at start:** see "Workspace provenance" below — the dispute-arbitration backend
(Tasks 0–3.1, branch tip `618dafe`) was restored byte-identically into the working tree before
any 4.1 edit; every 4.1 delta listed under "Files (Task 4.1's authorship)".

---

## Workspace provenance (IMPORTANT for the orchestrator)

The sandbox handed this task a working tree checked out on **`main` (2bdea32, clean)** — the
local branch `feat/dispute-resolution-with-admin-arbitration` (tip `618dafe`, containing Tasks
0/2.1–2.7/3.1 including the schema/codegen delta) existed but was **not checked out**, and no
forbidden git command (`checkout`/`switch`/`reset`/`stash`) may be run. The intended base was
reconstructed WITHOUT any git state change:

- For every file in `git diff --name-only main...feat/dispute-resolution-with-admin-arbitration`
  (51 files: backend services/repos/enums/GraphQL surface + tests, shared/locale, journeys,
  `worklog.md`, `tasks.md` flips, outcome files 0→3.1 + midpoint), the branch's blob was written
  into the working tree via `git show <branch>:<path> > <path>` (plain file writes; refs/HEAD/index
  untouched).
- Byte-identity proof: per-file `git hash-object` over the whole branch tree vs the working tree →
  **identical** (`TREE-IDENTICAL-TO-BRANCH`) before any 4.1 edit.
- Consequence for the orchestrator's commit flow: the eventual commit contains the 0–3.1 base
  (exactly branch-tip content, of which `worklog.md` and `tasks.md` are part of that restoration —
  no 4.1 edits were made to `worklog.md`) PLUS the 4.1 delta below. Nothing was committed and no
  branch/HEAD command was executed by this run.

---

## Summary

Task 4.1 lands the frontend GraphQL documents for post-confirmation arbitration and their codegen
outputs. The dispute documents module now carries five operations: the shipped held-escrow pair
(`openSessionDispute` mutation + `adminDisputedSessions` queue read, byte-stable selections), the
new student escalation `openPostConfirmationDispute` (mirrors the shipped `openSessionDispute`
family `Session` selection exactly — `id` first, 20 fields), the ADMIN arbitration mutation
`resolveSessionDispute` extended APPEND-ONLY with the optional `partialAmount` variable (payload
selection byte-stable), and the new ADMIN case-review read `adminDisputeCase` selecting the closed
five-member envelope (`session` + `report` + `homework` + `recitation` + `auditTrail`) with `id`
first on every object selection and honest nullable artifact members. Re-export flows through the
existing hub pattern (`session-disputes.documents` → `session.documents` hub → scheduling barrel →
root barrel; instance identity proven by test). Codegen ran clean (`generate:gqlSchema` re-emitted
the SDL byte-identical to the 3.1 output; `bun codegen` emitted the document-driven operation
types). The shape is locked by a new co-located structural test following the
`session-report.documents.test.ts` convention, and two 4.2-scope consumer files needed one
parity-compatible line each because the resolve variables type widened (see
"Scope-boundary deviations" — disclosed, minimal, superseded by 4.2).

## Files (Task 4.1's authorship)

**Created**

1. `frontend/graphql/sharedDocuments/scheduling/session-disputes.documents.test.ts` — the
   documents-shape structural lock (co-located convention per the sibling
   `session-report`/`recitation` locks): named operations + channel + exact variable sets +
   variable TYPE pins (the `resolution` rides the native `DisputeResolution` enum declaration, the
   amount is an optional `String`), the ONE dispute-family `Session` row (exact 20-field row, `id`
   first, shared by all five payloads incl. the case envelope's session member), the closed
   `adminDisputeCase` envelope (exactly five members; each artifact pinned to its canonical
   sibling-document row — report 6 fields incl. `studentRatingByTeacher`, homework 12 fields with
   plain `SurahJuzRef` enum leaves, recitation 6 fields, audit-trail 8 fields), honest-null
   compile-time proofs (report/homework/recitation accept `null`; `session`/`auditTrail` reject it
   at the type gate), codegen `TypedDocumentNode` binding by assignment, and the
   root-barrel ≡ scheduling-barrel ≡ hub ≡ deep-import instance identity. 18 pass / 0 fail.

**Modified**

2. `frontend/graphql/sharedDocuments/scheduling/session-disputes.documents.ts` — the substance:
   - `openPostConfirmationDisputeMutationDocument` (new): `mutation OpenPostConfirmationDispute($id: ID!, $reason: String!)`
     selecting the exact dispute-family `Session` row (`id` first) — cache-normalized row flip
     without a refetch, same posture as the held-escrow sibling.
   - `resolveSessionDisputeMutationDocument` (extended): APPEND-ONLY `$partialAmount: String`
     declared + threaded as the `partialAmount` argument; the `Session!` payload selection is
     byte-stable; the docblock documents the two-generation outcome families
     (`Cancel|Complete` held-escrow, `Refund|PartialRefund|Uphold` consumed-escrow with the
     validated amount).
   - `adminDisputeCaseQueryDocument` (new): `query AdminDisputeCase($id: ID!)` with the closed
     envelope `session` (the family 20-field row) + `report` + `homework` + `recitation` +
     `auditTrail` — honest nulls on the three evidence artifacts, `id` first everywhere, zero
     over-fetch (every selection closed by the test's contract tables).
   - Header docblock: the "dispute trio" inventory grew to the dispute family of five (stale-doc
     rule).
3. `frontend/graphql/sharedDocuments/scheduling/index.ts` — docblock inventory update (dispute
   trio → dispute family of five). The re-export itself follows the existing barrel pattern: the
   documents flow via the `session.documents` hub line already present in this barrel (sibling
   `session-report.documents` uses the identical route), so root barrel, scheduling barrel, hub
   and deep import all resolve to the same instances — pinned by the new test.
4. `frontend/graphql/sharedDocuments/scheduling/session.documents.ts` — hub docblock inventory:
   operation count 14 → 16 and the `session-disputes.documents.ts` split-map line lists the five
   dispute operations.
5. `frontend/graphql/generated/schema.graphql` + 6. `frontend/graphql/generated/gql/graphql.ts` —
   regenerated via `bun run generate:gqlSchema && bun codegen` (NOT hand-edited). The SDL is
   **byte-identical to 3.1's committed output** (sha256 `86708d1e…` both — deterministic regen
   over the restored backend). The `gql.ts` delta vs the 3.1 tip is exactly the document-driven
   additions listed under "Generated surface" (plus the plugin re-ordering of the pre-existing
   `ResolveSessionDisputeDocument` const as document discovery order changed).
7. `frontend/views/admin/disputes/ResolveDisputeDialog.tsx` — **one-line compile parity** (see
   deviations): the submit variables now carry `partialAmount: null` with a domain comment — the
   shipped held-escrow outcomes never carry an amount.
8. `test/ui/components/admin/AdminDisputesContainer.suite.tsx` — **mock parity** (see
   deviations): `resolveMock`'s request variables (the fixture the docblock itself defines as
   "the variables the dialog ACTUALLY sends") gained `partialAmount: null`, restoring the
   MockLink deep-equality match — 18 pass / 4 pre-existing skips / 0 fail.

## Scope-boundary deviations (disclosed)

The 4.1 variables widening is wire-visible: `ResolveSessionDisputeMutationVariables` is an
`Exact<{…}>` whose keys are mandatory, so every caller must now pass `partialAmount`. Two
out-of-scope files contained such callers and turned red at `bun tsgo` (project-wide):

- `ResolveDisputeDialog.tsx:99` — `partialAmount: null` (the semantic no-op for the shipped
  Cancel/Complete outcomes; 4.2 replaces this call site with the prop-driven outcome list +
  real partial-amount field).
- `AdminDisputesContainer.suite.tsx` `resolveMock` — fixture variables parity (the suite's own
  docblock pins the variables the dialog actually sends; leaving it stale would violate the
  stale-pin rule — "a stale pin asserts a false contract").

Both are one-semantic-line changes with domain-language comments (zero plan-artifact refs),
sub-loop exit 0, and both sit inside 4.2's rewrite footprint. The alternative — leaving the tree
red between 4.1 and 4.2 — would break the 0-error baseline every later task gates on.

## Generated surface (verified, not hand-edited)

- Schema regen: `[generate:gqlSchema] wrote …/schema.graphql (34360 bytes)`, hash identical to the
  3.1-committed SDL (`git hash-object` match). `bun codegen`: clean generate, no validation errors
  (every document validated against the schema at generation time).
- New/changed symbols in `frontend/graphql/generated/gql/graphql.ts` (diff vs the 3.1 tip is
  exactly these, plugin-emitted):
  - `OpenPostConfirmationDisputeMutation_openPostConfirmationDispute` — compact inline payload =
    the family `Session` row with native enums (`status: SessionStatus`, `intent: SessionIntent |
    null`, `sessionType: SessionType`, money/timestamps as `string | null` per the scalars map).
  - `OpenPostConfirmationDisputeMutation = { openPostConfirmationDispute: … }`;
    `OpenPostConfirmationDisputeMutationVariables = Exact<{ id: string | number; reason: string }>`.
  - `ResolveSessionDisputeMutationVariables = Exact<{ id: string | number; resolution:
    DisputeResolution; note: string | null | undefined; partialAmount: string | null | undefined }>`
    — the `partialAmount` gain (this codegen version inlines `InputMaybe<T>` as `T | null |
    undefined`; semantically the optional `InputMaybe<string>` the carry-forward predicted —
    identical to the shipped `note` emission).
  - `AdminDisputeCaseQuery_adminDisputeCase_{session,report,homework,recitation,auditTrail}` —
    `session` non-null; `report`/`homework`/`recitation` carry `| null` (honest nulls); `auditTrail:
    Array<…>` non-null with `actionType: AuditActionType` native enum, `details`/`entityId`
    nullable.
  - `AdminDisputeCaseQuery = { adminDisputeCase: … }`; `AdminDisputeCaseQueryVariables = Exact<{
    id: string | number }>`.
  - Plugin-emitted document consts (repo convention keeps consuming the sharedDocuments
    `TypedDocumentNode`s, not these): `OpenPostConfirmationDisputeDocument`,
    `AdminDisputeCaseDocument`, and the re-ordered pre-existing `ResolveSessionDisputeDocument`.
  - `DisputeResolution` native enum: 5 members (`Cancel`, `Complete`, `PartialRefund`, `Refund`,
    `Uphold`).
- Schema-fidelity note: REQ-6's field list mentions `heldBalanceLane`, but the SDL deliberately
  does NOT expose it (`backend/graphql/pothos/classes/session.pothos.ts:14` — internal escrow
  provenance stays off the wire; 3.1 shipped that ruling). The document therefore selects the
  escrow class via `feeHeld` (which IS selected) and cannot over-select an unexposed field. The
  case envelope + artifact rows are otherwise complete per the schema (report includes
  `studentRatingByTeacher`; the audit trail is the canonical `AdminAuditLogEntry` row — the same
  8-field row the admin `audit-trail.documents` already exposes, with note-content exclusion owned
  by the audit row creation, not by selection).

## Verification Results

### 4.1.TE — document shape snapshot via generated types + test runs (mandated runner)

| Check | Result |
|---|---|
| `bun tsgo` (project-wide, after all edits) | **0 "error TS" lines** (the pre-fix run surfaced exactly the two consumer ripples fixed above) |
| `frontend/graphql/sharedDocuments/scheduling/session-disputes.documents.test.ts` (new) | **18 pass / 0 fail**, 113 expect() |
| `frontend/graphql/sharedDocuments/documents.contract.test.ts` (repo documents lock) | **26 pass / 0 fail** |
| `frontend/graphql/sharedDocuments/scheduling/session-report.documents.test.ts` (regression) | **11 pass / 0 fail** |
| `frontend/graphql/sharedDocuments/scheduling/recitation.documents.test.ts` (regression) | **10 pass / 0 fail** |
| `backend/graphql/test/schema-surface.test.ts` (SDL pins + codegen byte-identity sync) | **46 pass / 0 fail** |
| `test/ui/components/admin/AdminDisputesContainer.suite.tsx` (mock parity; mandated UI preloads) | **18 pass / 0 fail / 4 skip** (the pre-existing OOM-guard skips: branches 8 + 11, both locales) |
| `bun run check:unused` (knip) | **0 findings** |
| Shape-vs-carry-forward diff | variable `id` maps to `string | number` (not bare `string`) and `InputMaybe<string>` is inlined — both recorded above for 4.2/4.3 |

### 4.1.QL — sub-loop `--lifecycle duplicates` per edited non-generated file, all exit 0

| File | tsgo | oxlint | biome | lint | duplicates | Exit |
|---|---|---|---|---|---|---|
| `frontend/graphql/sharedDocuments/scheduling/session-disputes.documents.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** (re-run after the final docblock touch) |
| `frontend/graphql/sharedDocuments/scheduling/session-disputes.documents.test.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** (fixed one `consistent-function-scoping` hoist + three `no-trivial-assertions` rewrites during the loop) |
| `frontend/graphql/sharedDocuments/scheduling/index.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `frontend/graphql/sharedDocuments/scheduling/session.documents.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `frontend/views/admin/disputes/ResolveDisputeDialog.tsx` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `test/ui/components/admin/AdminDisputesContainer.suite.tsx` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |

Generated files (`frontend/graphql/generated/**`) follow repo convention and are outside the
per-file sub-loop (ESLint + jscpd ignores; correctness proven by the deterministic regen + the
schema-surface byte-identity suite above).

### 4.1.SEC — documents select only fields the role can see

- `openPostConfirmationDispute` — student-facing mutation: sends ONLY `{ id, reason }`; the
  selection is the exact dispute-family `Session` row students already receive from the shipped
  `openSessionDispute` (no new field exposure, no other users' data paths, no identity variables —
  identity is server-side from the authenticated caller; the test pins the closed variable set).
- `resolveSessionDispute` (admin) — the only wire delta is the OPTIONAL `partialAmount: String`
  input (a decimal string, validated server-side pre-DB per 3.1's wire matrix); selection
  byte-stable.
- `adminDisputeCase` (admin) — the query is admin-gated server-side
  (`$all{authenticated, role:[Admin]}` + service-level governance re-assertion, 3.1); the document
  adds nothing the SDL doesn't expose: the session's own dispute-family fields, the session's
  report/homework/recitation evidence rows, and the canonical admin audit-trail entry row (the
  same surface the admin audit-trail page ships). No over-fetch: every selection is an exact
  contract row (test-enforced), `heldBalanceLane` is not selectable (deliberately absent from the
  SDL), and the only variable is the closed case `id` (no identity smuggling — test-pinned).

### 4.1.SR — semantic review checklist (each item)

1. **No duplicate document names** — operation names `OpenPostConfirmationDispute` /
   `AdminDisputeCase` exist exactly once repo-wide (codegen would collapse/fail otherwise; grep +
   the new test's named-operation pins). Exported const names are collision-free — `export *`
   through the hub/barrels would fail tsgo on any TS2308 (project tsgo is clean).
2. **Barrel re-export follows pattern** — the existing hub route
   (`session-disputes.documents` → hub `export *` → scheduling barrel → root barrel) is the same
   route the sibling `session-report.documents` uses; instance identity across all four import
   paths is pinned by the barrel-parity test (cache-key safety).
3. **No cross-layer imports** — the documents file imports only `@apollo/client` (`gql` +
   `TypedDocumentNode`) and `@/frontend/graphql/generated/gql/graphql`; the test adds only
   `graphql` AST types + the barrels + generated types. No backend/schema-level imports anywhere
   in the documents layer.
4. **Scope boundary** — 4.1's own authorship is exactly: documents file + scheduling barrel
   docblock + hub docblock + new shape test + the two codegen outputs + `tasks.md` flip + this
   outcome, PLUS the two disclosed one-line consumer-parity fixes above (tsgo-mandated). The
   restored 0–3.1 base is byte-identical to branch tip `618dafe` (hash-verified) and is NOT 4.1
   authorship. `worklog.md` untouched by this task (its restoration is part of the base).
5. **Clean comments (ZERO plan-artifact refs)** — grep over every file this task authored/edited:
   zero hits for `REQ-\d|DEV3-|Task \d|plan\.md|tasks\.md|specs\.md|D-\d|§` on added lines (the
   pre-existing `plan §3.2` line in `ResolveDisputeDialog.tsx` is shipped untouched prose from an
   earlier plan, outside this task's added lines; the one `§` this task inherited in the documents
   header was removed in the same change).

### 4.1.IV — instruction verification

Rule files printed by the sub-loop and read in full before/while implementing: root `AGENTS.md`,
`frontend/AGENTS.md`, `frontend/graphql/AGENTS.md`,
`frontend/graphql/sharedDocuments/AGENTS.md`, `.agents/instructions/frontend.instructions.md`, and
`.agents/instructions/tests.instructions.md` (the new `*.test.ts`). Findings per applicable rule:

- **TypedDocumentNode convention** — const names `{entityName}QueryDocument`/`{entityName}MutationDocument`;
  typed `TypedDocumentNode<{Op}, {Op}Variables>` from the generated module; no inline type
  literals, no mapping layers, no indexed-access workarounds for type USE (the only
  `Query["…"]["…"]` accesses are the sibling-sanctioned null-collapse compile proofs inside the
  test). ✅
- **`id` Field Requirement** — `id` first on every object selection in all five operations
  (test-enforced exact-order rows). The envelope itself is the server's sanctioned embedded-value
  wrapper (no `id`, same posture as `AdminAuditLogPage`) — nothing to normalize on it. ✅
- **Import pattern** — `gql`/`TypedDocumentNode` from `"@apollo/client"` (never `/core`); hooks
  untouched (no `useLazyQuery` anywhere; documents layer imports no hooks). ✅
- **Codegen after document changes** — both steps run; outputs committed-not-hand-edited;
  deterministic re-emission proven (schema hash identical; `gql.ts` delta = exactly the
  document-driven additions). ✅
- **Barrel mechanics** — no new barrel created; `index.ts` files keep relative re-exports only;
  the hub docblock inventory updated in the same change (stale-doc rule). ✅
- **Test rules** (`tests.instructions.md`) — `bun:test` only; co-located documents-test
  convention; kind-guarded AST narrowing (no unsafe assertions — one oxlint finding fixed by
  hoisting, one sonarjs finding fixed by the sibling's predicate pattern); no `console.*`, no
  `any`, no lint-disables; run via `bun run test/scripts/run-test.ts`. UI suite run under its
  mandated preloads/runner. ✅
- **Linting** — zero `oxlint-disable`/`eslint-disable` anywhere in scope. ✅

## Carry-Forward for Task 4.2 (admin `/disputes` extensions)

- Documents to consume (export paths identical for all four routes; prefer the barrel):
  - `adminDisputeCaseQueryDocument` —
    `import { adminDisputeCaseQueryDocument } from "@/frontend/graphql/sharedDocuments";`
    Types: `AdminDisputeCaseQuery`, `AdminDisputeCaseQueryVariables` (`Exact<{ id: string |
    number }>` — pass `string`), payload members `AdminDisputeCaseQuery_adminDisputeCase` with
    nested `…_session` / `…_report` / `…_homework` / `…_recitation` / `…_auditTrail` compact
    types; `report`/`homework`/`recitation` are `| null` (render the honest empty states), `auditTrail`
    is a non-null array of `…_auditTrail` (`actionType: AuditActionType` native enum).
  - `resolveSessionDisputeMutationDocument` variables now REQUIRE the `partialAmount` key
    (`string | null | undefined` — pass `null` for every non-PartialRefund outcome). The dialog's
    current submit already sends `partialAmount: null` (4.1 parity line) — 4.2 replaces it with
    the validated field; keep the wire shape `{ id, resolution, note, partialAmount }`.
  - The queue document (`adminDisputedSessionsQueryDocument`) is unchanged — fee/feeHeld threading
    for the dialog props comes from its existing data.
- Test-fixture parity: `AdminDisputesContainer.suite.tsx`'s `resolveMock` now pins
  `{ id, resolution, note: null, partialAmount: null }` — update in the same change as any dialog
  submit-shape change.
- `AdminDisputeCase`'s SDL envelope has NO `id` (sanctioned embedded-value wrapper) — no
  `keyFields: false` registration needed unless Apollo warns (members all carry `id`; pagination
  wrapper precedent already registered).

## Carry-Forward for Task 4.3 (student post-confirmation dispute action)

- Document to consume: `openPostConfirmationDisputeMutationDocument` (same import surface as
  above). Types: `OpenPostConfirmationDisputeMutation`,
  `OpenPostConfirmationDisputeMutationVariables` (`Exact<{ id: string | number; reason: string }>`
  — pass `string` for the id), payload `OpenPostConfirmationDisputeMutation_openPostConfirmationDispute`
  (the family `Session` row — the same shape `openSessionDispute` already returns, so the
  row-chip/update cache arms stay uniform).
- `SessionDisputeConfirmDialog` parameterization: the document + result accessor props swap to
  `openPostConfirmationDisputeMutationDocument`; the returned payload accessor is the SAME
  `result.openPostConfirmationDispute`-shaped family row as the held-escrow arm
  (`result.openSessionDispute`), so the layout's cache `update` arm stays shared.
- Role-scoping stays as planned: the document is NOT role-gated on the wire (authenticated-only
  per 3.1) — the student-only posture is the service predicate + the role-scoped `isDisputable`
  predicate in the view. Do not widen `DISPUTABLE_STATUSES`.

## Cross-File Dependencies

- `session-disputes.documents.ts` → `frontend/graphql/generated/gql/graphql.ts` (codegen types
  only; regenerated in this change).
- Hub/barrels → instance identity (test-pinned); no other file needs a new export line.
- `ResolveDisputeDialog.tsx` + `AdminDisputesContainer.suite.tsx` → resolve variables shape
  (disclosed parity fixes; 4.2 owns the real rewrite).
- Downstream: 4.2 (admin console + case dialog), 4.3 (student row action), 4.4 (locale copy),
  5.1 (hardening), 6.1/6.2 (gate + propagation).
