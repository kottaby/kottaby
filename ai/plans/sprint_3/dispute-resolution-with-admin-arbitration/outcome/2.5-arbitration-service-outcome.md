# Task 2.5 — SessionArbitrationService (open, arbitrate, case review) — verified & completed

**Task ID:** 2.5 · **Plan:** `ai/plans/sprint_3/dispute-resolution-with-admin-arbitration/`
**Date:** 2026-09-12 · **Agent:** general-purpose subagent (verification-and-completion run)
**Working tree at start:** HEAD + the uncommitted 2.1 pin update (`schema-surface.test.ts`), 2.3's repo/service-support files, 2.4's journey file, and — the subject of this run — Task 2.5's own in-flight implementation (`session-arbitration.service.ts` + `.helpers.ts` + its test + the barrel wiring + the three error-locale keys), left unverified by an interrupted prior execution of this task.

---

## Summary

Verified the in-flight `SessionArbitrationService` implementation line-by-line against plan
Component 1, D-4/D-5/D-10, the error/permission matrix, and the Task 2.5 scope — found it
**complete and conformant** (zero implementation gaps; no code changes were required), ran the
full mandatory pipeline to green, and produced the bookkeeping. The service lands the
post-confirmation dispute surface: the student-only dispute entry (oracle-collapsed, exactly-once,
zero audit rows), the admin's binding three-outcome arbitration (classification dispatch, strict
partial-amount policy BEFORE any write, compensating wallet reversal + quantized lane credit on
ONE transaction, exactly ONE `override` audit row that never carries the note's content), and the
admin case-review read (governance-gated, concurrent artifact reads, honest nulls). The
notification receipts stay a marked SEAM: this module writes no notification rows and publishes
nothing — Task 2.6 wires its wave service into the two marked composition points on the flows'
own transaction.

**Provenance note:** this task's working tree contained a substantial uncommitted in-flight
implementation of Task 2.5 (an interrupted earlier execution of this same task). This run
re-verified every scope element against the plan (verdict table below), proved the suite and all
regressions green, re-ran the per-file sub-loop on every scope file, re-confirmed the project-wide
typecheck, and re-measured the journey's expected-red state (unchanged: 8 pass / 5 fail, every
failure a Task 2.6 notification wave). Nothing was stubbed or forced green; nothing was committed;
`worklog.md` and the journey file were not touched.

## Files Created / Modified (by Task 2.5's authorship, verified this run)

1. **Created** `backend/services/classes/session-arbitration.service.ts` — the three service
   methods (`openPostConfirmationDispute`, `arbitrateDispute`, `getAdminDisputeCase`) with the
   module docblock pinning the composition order and the notification seam.
2. **Created** `backend/services/classes/session-arbitration.service.helpers.ts` — flow internals
   (pre-DB resolution policy, strict decimal pattern, denial classifiers, financial legs, audit
   contract builder) per the session-services' sibling-helpers extraction layout.
3. **Created** `backend/services/classes/session-arbitration.service.test.ts` — 21 DB-backed
   tests / 288 expect() calls covering the 4 tiers (below).
4. **Modified** `backend/services/classes/index.ts` — exactly one added barrel line:
   `export * from "./session-arbitration.service";`.
5. **Modified** `shared/locale/{en,ar}/errors/index.ts` + `shared/locale/types/errors/labels.ts` —
   the two new error keys `disputeResolutionMismatch` / `partialRefundAmountInvalid` (en/ar parity
   + typed labels; see the deviation note below).

**Not touched this run:** `worklog.md` (protocol), `test/workflows/sessions/post-confirmation-dispute.journey.test.ts`
(2.4's authorship — re-run only, for the expected-red measurement), the 2.1/2.3 artifacts, the
notification service (2.6's file does not exist and was not created), nothing committed.

## Component-1 signature conformance (verified exact)

```ts
openPostConfirmationDispute(callerUserId: number, sessionId: number, reason: string,
  locale: string, outerTx?: DBTransaction): Promise<SessionReturnType>
arbitrateDispute(adminId: number, sessionId: number, resolution: DisputeResolution,
  note: string | null, partialAmount: string | null, locale: string,
  outerTx?: DBTransaction): Promise<SessionReturnType>
getAdminDisputeCase(adminId: number, sessionId: number, locale: string,
  tx?: DBTransaction): Promise<AdminDisputeCaseReturnType>
```

All three match plan Component 1 byte-for-byte (tx is the FINAL parameter per the single-writer
composition rule; `outerTx` runs the flow inside a SAVEPOINT — the documented test path).

## Scope-element verification verdict (per brief bullet)

| # | Scope element | Verdict |
|---|---|---|
| 1 | `openPostConfirmationDispute` student-only, `assertPositiveSafeSessionId` + `normalizeRequiredReasonText` pre-DB | **met** |
| 2 | open: probe read INSIDE tx; non-participant (incl. the row's own teacher) → oracle-collapsed `SESSION_NOT_FOUND`; wrong shape (not dual-confirmed consumed `completed`) → `SESSION_INVALID_TRANSITION` | **met** |
| 3 | open: exactly-once guarded write (`openPostConfirmationDisputeOnce` re-asserts `id ∧ student_id=caller ∧ status='completed' ∧ confirmed_by_student_at IS NOT NULL ∧ fee_held=false`); double-submit is the state-conflict loser; reason never rewritten | **met** |
| 4 | open: ZERO audit rows; escrow/wallets untouched; single transaction | **met** |
| 5 | `arbitrateDispute`: `assertAdminGovernanceClean(adminId, t, outerTx)` re-assertion (pre-tx, per the guard-before-transaction rule); DB admin role is the authority (student/teacher/blocked-admin ids all `FORBIDDEN`) | **met** |
| 6 | arbitrate: classification probe read INSIDE the tx; unknown id → not-found; non-disputed → state conflict; held-family row → `disputeResolutionMismatch` | **met** |
| 7 | arbitrate: `Cancel`/`Complete` rejected pre-DB with `disputeResolutionMismatch` (held-family vocabulary never applies here) | **met** |
| 8 | arbitrate: partial-amount policy resolved BEFORE any write — strict `^\d+(\.\d{1,2})?$` shape, `0 < amt < fee`, exact string carried verbatim into ledger + audit; amount supplied with any non-`PartialRefund` outcome rejected (`partialRefundAmountInvalid`); Refund debits the row's own fee verbatim | **met** |
| 9 | arbitrate: Refund/PartialRefund → `debitForArbitrationOnce` (compensating `withdrawal`/`completed` ledger row keyed to the session + guarded balance decrement) + `incrementLane` +1 on the provenance lane; `heldBalanceLane` null → lane leg no-ops while the teacher debit still applies; Uphold → ZERO financial writes | **met** |
| 10 | arbitrate: `resolveConsumedDisputeOnce` null → state conflict (fires BEFORE money moves, so the race loser classifies as `SESSION_INVALID_TRANSITION`); wallet shortfall → typed `ConflictError("WALLET_INSUFFICIENT_FUNDS", t.insufficientBalance)` thrown on the flow's tx — full rollback (row stays `disputed`, zero financial/audit writes; the ensured wallet itself rolls back on the absent-wallet path) | **met** |
| 11 | arbitrate: exactly ONE audit row via `AuditService.createAuditLog` on the SAME tx — `action_type='override'`, `entity_type='session'`, `entity_id=sessionId`, `actor_id=adminId`, details `{resolution, refundAmount|partialAmount (absent for Uphold), notePresent}`, NEVER the note's content (the amount key is omitted — not nulled — on Uphold; both the service pins and the journey accept this shape) | **met** |
| 12 | arbitrate: note via `normalizeOptionalReasonText` (trimmed ≤500, whitespace → NULL, persisted `resolution_note`); receipt SEAM docblocks mark the in-tx composition points, no publish in-service | **met** |
| 13 | `getAdminDisputeCase`: admin re-assertion FIRST, session detail via `SessionAdminGovernanceService.getDetail` (its own gate re-run is the same belt), concurrent independent reads (report / homework / recitation / `AuditTrailService.listAuditTrail` session page), honest nulls + empty trail, unknown/malformed id → localized `sessionNotFound`, zero writes | **met** |
| 14 | Tests: probe-chain matrix, all three outcomes, classification mismatches both directions, amount matrix, audit-row assertions, insufficient-funds rollback, note-content exclusion, closed notification seam, committed double-arbitration race | **met** |

**Missing scope elements: none** — no fixes to the implementation were required.

## Verification Results

### 2.5.TE — test runs (mandated runner `bun run test/scripts/run-test.ts`)

| Suite | Result |
|---|---|
| `backend/services/classes/session-arbitration.service.test.ts` (new) | **21 pass / 0 fail**, 288 expect() calls |
| `backend/services/classes/session-lifecycle.service.test.ts` (regression) | **72 pass / 0 fail**, 859 expect() calls |
| `backend/services/classes/session-lifecycle.guards.test.ts` (regression) | **50 pass / 0 fail**, 162 expect() calls |
| `backend/services/classes/session-lifecycle.enforcement.test.ts` (regression) | **17 pass / 0 fail**, 56 expect() calls |
| `backend/services/classes/session-lifecycle.booking.test.ts` (regression) | **19 pass / 0 fail**, 2167 expect() calls |
| `backend/services/classes/session-admin-governance.service.test.ts` (regression over the shared classifiers/gate) | **49 pass / 0 fail**, 594 expect() calls |
| `backend/db/test/repo/classes/session.repository.test.ts` (repo behavior untouched — confirmed) | **83 pass / 0 fail**, 587 expect() calls |
| `backend/db/test/repo/billing/wallet.repository.test.ts` (repo behavior untouched — confirmed) | **10 pass / 0 fail**, 56 expect() calls |
| `shared/locale/errors-namespace.parity.test.ts` (en/ar parity with the new keys) | **21 pass / 0 fail**, 206 expect() calls |
| `bun tsgo` (project-wide) | **0 errors** (`grep -c "error TS"` = 0) |

Test-file note: the brief's premise said "28 tests"; the landed suite is **21 tests** (5 open +
10 outcomes/denials + 5 case-review + 1 race) with 288 expect() calls — the count in the
interrupted run's description was inaccurate, the coverage itself is complete per the 4 tiers.

#### 4-Tier coverage map (as pinned by the suite)

- **Tier 1 (branch/statement):** the open flow's full probe chain (unknown id, foreign student,
  the row's own teacher, scheduled/started/held/unstamped shapes, double-submit) and the
  arbitration flow's probe chain (unknown id, non-disputed row, held-family row both directions);
  the three outcomes' exact financial legs; every amount-policy denial arm; the audit contract's
  per-outcome details shape; the case bundle's composition and honest-null arms.
- **Tier 2 (boundary):** reason/note 500-char caps + trim normalization; partial amount's strict
  decimal shape (≤2 fractions), open (0, fee) range (zero, the fee, above the fee, over-precision,
  sign, NaN text, whitespace), exact-2dp acceptance; funds guard at the exact balance ("0.00")
  and one cent short; malformed/zero session ids.
- **Tier 3 (rollback + committed chaos):** insufficient-funds rollback proof (zero financial
  writes, zero audit rows, row still disputed, no resolution stamps; the ensured wallet itself
  vanishes on the absent-wallet path) and the committed-fixture double-arbitration race — two
  production-path arbitrations, exactly one commit, the loser `SESSION_INVALID_TRANSITION` with
  zero net effect on row/wallet/lane/trail (skipped under PGlite via `testOnRealPostgres`).
- **Tier 4 (typed denials + identity discipline):** every denial asserted through
  `DomainError.code` + the exact translated copy (never raw keys); the student identity is only
  ever the caller's (non-participants collapse to not-found); arbitration accepts only the
  governance-clean ADMIN role; the notification seam is asserted closed (zero notification rows).

### Journey expected-red re-measurement (untouched file, re-run for evidence)

`test/workflows/sessions/post-confirmation-dispute.journey.test.ts` → **8 pass / 5 fail**, 150
expect() calls — the five failures are byte-identical to 2.4's documented red set (each a
`countDisputeNotifications(...)` → Expected 1, Received 0): the `session_dispute_opened` admin
wave and the four `session_dispute_resolved` participant waves. That is Task 2.6's unlanded
surface, NOT a 2.5 regression; every non-notification leg (open, queue, case read, all three
outcomes, financial exactly-once, audit rows, denials, race) runs GREEN against this service.

### 2.5.QL — sub-loop `--lifecycle duplicates` per scope file, all exit 0

| File | tsgo | oxlint | biome | lint:type-aware | duplicates | Exit |
|---|---|---|---|---|---|---|
| `backend/services/classes/session-arbitration.service.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `backend/services/classes/session-arbitration.service.helpers.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `backend/services/classes/session-arbitration.service.test.ts` | ✅ | ✅ | ✅ | ✅ | skipped (test file outside jscpd scope) | **0** |
| `backend/services/classes/index.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `shared/locale/en/errors/index.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `shared/locale/ar/errors/index.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `shared/locale/types/errors/labels.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |

Stale-pin check: no source-scan pin test covers the new service files or the services barrel
(the lifecycle suite's `SERVICE_UNIT_FILES` pins scan only its own `session-lifecycle.*` unit
files — all green unchanged); the errors-namespace parity suite is green with the two new keys.

## 2.5.SEC — report per item

- **BOLA/IDOR:** the student caller id is folded INTO the guarded write predicate
  (`eq(session.studentId, studentId)`); a non-participant learns nothing (oracle collapse pinned
  by tests, including the row's own teacher). Arbitration/case identity is `adminId`, re-asserted
  against the DB role row (`assertAdminGovernanceClean`) — a client-claimed admin id without the
  DB role fails closed. The service takes identity from PARAMETERS; the resolver (3.1) binds
  `ctx.user.id`.
- **BFLA:** admin-only surfaces re-run the governance belt in-service (defense in depth over the
  3.1 scope gate); denials carry the shared `forbidden` copy.
- **BOPLA:** arbitration inputs are the strict positional whitelist (`resolution`, `note`,
  `partialAmount`); the service performs no client-shaped writes at all — every SET clause is a
  repo-primitive closed object literal (2.3's mass-assignment proof); the audit contract is a
  closed literal. No `{ ...input }` reaches any write.
- **Wildcard hygiene:** N/A — no LIKE/ILIKE, no free-text filters on the case read.
- **Money hygiene:** amounts are decimal STRINGS bound verbatim (never re-parsed for storage,
  never re-rounded); the strict shape check runs before the value can reach a ledger; the fee
  ceiling comparison parses to Number only for the range decision, the stored/audited value stays
  the exact supplied string.

## 2.5.SR — semantic review checklist (each item)

1. **Single-tx mutations** — both flows wrap every write in `withTransaction(outerTx, …)` with
   `tx` propagated to every repo/wallet/audit call; the case read is side-effect-free with
   optional `tx` propagation. ✅
2. **Guarded writes re-assert classification (no TOCTOU)** — the probe is read INSIDE the flow's
   transaction, and both write predicates re-assert the same classification (`status` +
   `fee_held` + caller in the WHERE) — verified in the repo helpers source this run; a drifted
   probe cannot pass a write. The arbitration's guarded completion leg fires FIRST so a race
   loser never moves money. ✅
3. **Receipts not published in-service** — the module docblock + both flow-level seam docblocks
   pin it; the service writes zero notification rows (test-asserted) and imports nothing from the
   notification layer. ✅
4. **Enums as value imports where runtime-used** — `DisputeResolution` (comparisons),
   `AuditActionType.Override`, `isDisputeResolution`, `isHeldBalanceLane` are value imports;
   `HeldBalanceLane` is a type import where only typed. `SessionStatus` in the repo helpers is a
   value import (runtime predicates). No string literals for enum members in runtime predicates. ✅
5. **Error classes reused, no reinvented shapes** — `NotFoundError` / `ValidationError` /
   `ConflictError` / `DomainError` from `@/backend/lib/errors`; the not-found / state-conflict
   pair is the SHARED classifiers (`rejectSessionNotFound` / `rejectStateConflict` from
   `session-admin-governance.helpers`), so all session surfaces emit the same two denial shapes;
   the funds conflict reuses the shipped `WALLET_INSUFFICIENT_FUNDS` code + `insufficientBalance`
   copy. ✅
6. **Audit contract never carries note content** — `buildArbitrationAuditContract` emits only
   `{resolution, refundAmount|partialAmount, notePresent}`; test asserts
   `details).not.toContain(noteContent)` while the trimmed note persists on the row. ✅
7. **No dead branches** — every branch has a live test on both sides (three resolution arms, lane
   null vs credited, debit hit vs null, probe miss arms, getDetail null vs row). The one
   test-unexercised arm is `creditLaneForArbitration`'s `!isHeldBalanceLane(lane)` fail-closed
   throw: the column is a pgEnum, so Postgres cannot store an off-vocabulary value — the arm is a
   TS↔DB vocabulary-DRIFT guard (the same class as `assertArbitrationResolution`'s re-guard of a
   payload that skipped the boundary parse), not dead code. Documented here as deliberate
   defense-in-depth.
8. **No module-level mutable state** — two `const` docblock-documented constants on the service
   module, pattern/description constants on the helpers; all functions pure; no `let` at module
   scope, no caches. ✅
9. **No cross-layer imports** — service imports only `@/backend/*` + `@/shared/locale/server-graphql`
   (the mandated service i18n seam); no `@/frontend` / `@/app` / GraphQL-context reach-ins
   (SSR-safe). ✅
10. **No `{...input}` spread into Drizzle writes (BOPLA)** — the service layer performs zero
    direct Drizzle writes (all through the 2.3 primitives' closed SET literals, re-verified in
    source this run). The test fixture's `...overrides` spread is test-oracle code building a
    precondition row, not a production write path. ✅
11. **Caller identity from parameters** — `callerUserId` / `adminId` are parameters with
    docblocks stating server-side context resolution; nothing reads request state in-service. ✅
12. **Scope boundary** — Task 2.5's authorship = the two service modules + the service test + the
    one-line barrel edit + the three locale files (+ this outcome + the tasks.md checkbox flip).
    The other working-tree entries belong to 2.1/2.3/2.4 and were not touched. `worklog.md`
    untouched; nothing committed. Clean-comments grep (`REQ-\d|DEV3-|Task \d|plan\.md|tasks\.md|
    specs\.md|D-\d`) over all three new files: zero hits; every comment states domain rationale.

## 2.5.IV — instruction verification

Sub-loop printed and read in full: `AGENTS.md` (every file), `backend/AGENTS.md` +
`backend/services/AGENTS.md` + `.agents/instructions/backend.instructions.md` (service files and
barrel), `.agents/instructions/tests.instructions.md` (the test file), root `AGENTS.md` +
`shared/AGENTS.md` (the locale trio, per the tasks.md layer mapping — the sub-loop's discovery
covers only the root file for `shared/locale/**`). Findings per applicable rule:

- **Service i18n rule** (services/AGENTS.md): all user-facing denials resolve through
  `getServerTranslations(locale)` → `errorsTranslations`; zero hardcoded strings. ✅
- **Single-writer discipline / composition seams** (services/AGENTS.md): `outerTx`/`tx` is the
  FINAL parameter on all three methods; governance pre-checks run BEFORE the transaction opens;
  all state changes flow through the designated repos. ✅
- **Type pattern** (backend.instructions.md): types from `@/backend/types`
  (`SessionReturnType`, `AdminDisputeCaseReturnType`, `DBTransaction`,
  `SessionArbitrationProbeType`, `AuditLogWriteContract`); no service-layer `.types.ts` file
  (runtime split lives in `.helpers.ts`, whose exported translations alias matches the
  `session-admin-governance.helpers` precedent); `SessionReturnType` here is
  `typeof session.$inferSelect` (no enum re-typing), so the raw guarded-write rows need no
  ReturnType mapping — the conversion rule is not triggered. ✅
- **Test rules** (tests.instructions.md): `runInRollback` + `tx`/`outerTx` propagated to every
  service/repo call; denials via the `expectRepoError` try/catch helper (no
  `expect(...).rejects`); entities only via `entity-setup` + file-local shared-PK fixtures (no
  seed data); `bun:test` only; no `console.*`, no `any`, no `oxlint-disable`; committed-fixture
  race group cleans up under `withImmutabilityTriggersSuspended` in FK order with tracked
  `afterAll`. Note: the suite imports `getServerTranslations` to obtain the expected translated
  copy — the rule's prohibition targets repo-layer assertion patterns that hang the loader; every
  shipped sibling service suite (`session-lifecycle.service.test.ts:140`,
  `session-admin-governance.service.test.ts:109`, `recitation.service.test.ts:79`, +4 more) uses
  the same import, and this suite runs green with no hang — recorded as the established
  service-test convention. ✅
- **Locale rules** (shared/AGENTS.md + AGENTS.md i18n): keys added INSIDE the existing `errors`
  namespace trio (no new namespace registration needed); typed labels + en/ar implementations;
  parity suite green; no cross-layer imports; `@/`-alias conventions hold. ✅
- **Linting**: zero `oxlint-disable` anywhere in scope. ✅

## Locale-keys deviation note (early-landed keys formally owned by Task 4.4)

The two errors-namespace keys (`disputeResolutionMismatch`, `partialRefundAmountInvalid`) were
landed EARLY by the interrupted 2.5 run because the service's localized denials require them.
**Kept** — the service cannot compile its denial copy without them, and the parity suite is green
(en + ar + typed labels). **Deviation to record:** the landed en copy for `partialRefundAmountInvalid`
is `"Enter a valid partial refund amount (greater than zero, less than the session fee, with up to
2 decimal places)."` vs the plan's illustrative example `"Partial refund amount must be greater
than 0 and less than the session fee."` — the landed string is a deliberate superset that also
names the two-decimal precision criterion of REQ-3.2 (the plan's string was marked "e.g.").
`disputeResolutionMismatch` has no plan example and follows the classification-matrix semantics.
**Task 4.4 remains the formal owner**: the remaining `sessions`-namespace labels (open-confirm
copy, outcome labels, case-dialog titles), the notifications copy for the two wave types, and the
full parity-test sweep.

## Carry-Forward for Task 2.6 (exact notification seam)

The service has two marked seam comments ("Dispute-wave seam: … emitted HERE on this transaction
by the dispute-notification service once it lands"):

1. **Open wave — `session_dispute_opened` → every admin:**
   `backend/services/classes/session-arbitration.service.ts` inside
   `openPostConfirmationDispute`'s `withTransaction` body, AFTER
   `SessionRepository.openPostConfirmationDisputeOnce` succeeds and BEFORE the `return disputed`
   (the marked block at the flow's tail). Inputs available at the seam: `sessionId`,
   `callerUserId` (the opener = the session's student), `locale`, `tx`.
2. **Resolved wave — `session_dispute_resolved` → exactly the two participants:**
   inside `arbitrateDispute`'s `withTransaction` body, AFTER the audit row and the response
   re-read, BEFORE `return current` (the marked block at the flow's tail). Inputs available:
   `sessionId`, `resolved.studentId`, `resolved.teacherId` (from the `resolveConsumedDisputeOnce`
   probe), `resolution`, `locale`, `tx`.

Expected signatures (plan Component 3): `notifyAdminsOfDisputeOpened(sessionId, openerUserId,
locale, tx): Promise<readonly NotificationDeliveryReceipt[]>` and
`notifyParticipantsOfDisputeResolved(sessionId, studentId, teacherId, resolution, locale, tx):
Promise<readonly NotificationDeliveryReceipt[]>` — receipts persisted in-tx (they must survive
the arbitration commit and vanish on the insufficient-funds rollback), claim keys
`session:<id>:dispute-opened|dispute-resolved`, publish-after-commit owned by the CALLER (3.1
resolver / the journey's spied `NotificationEngine` boundary). These two waves are the ONLY
notification behavior for the consumed generation; the held-family service stays
notification-silent. Landing 2.6 turns the journey's five reds green with zero journey edits.

## Carry-Forward for Task 3.1 (resolver dispatch)

- Bind the three service methods exactly as declared above (identity from `ctx.user.id`;
  `ctx.t("errorsTranslations")`-equivalent locale propagation — the service takes the raw
  `locale` string and resolves translations itself).
- Dispatch: `Cancel|Complete` → the shipped `SessionLifecycleService.resolveSessionDispute`;
  `Refund|PartialRefund|Uphold` → `arbitrateDispute`. The family mismatches are already rejected
  in-service (`assertArbitrationResolution` pre-DB + `classifyArbitrationProbe` in-tx), so a
  naive dispatch by escrow class is safe; keep the GraphQL input arg names (`id`, `resolution`,
  `note`) and append `partialAmount: String`.
- `adminDisputeCase(id: ID!)` → `getAdminDisputeCase(adminId, id, ctx.locale)`; bind the Pothos
  object to `AdminDisputeCaseReturnType` with `nullable: true` on `report`/`homework`/`recitation`
  and the auditTrail items from `AuditTrailService.listAuditTrail(...).items` (no mapping).
- Publish receipt batches post-commit per the realtime-engine contract (the service returns the
  session row only; 2.6's receipts are collected in-tx by the wave service it wires into the
  seams above — 3.1 owns the publish call ordering).
- The 2.1 carry-forward stands: the schema regen MUST ship together with this dispatch (the
  widened `DisputeResolution` reaching the shipped held-family service would otherwise execute
  Complete semantics on held escrow).

## Cross-File Dependencies

- `backend/services/classes/session-arbitration.service.ts` →
  `session-arbitration.service.helpers.ts` (flow internals), `session-admin-governance(.helpers)`
  (detail read + shared classifiers), `session-lifecycle.governance` (admin belt),
  `session-lifecycle.guards` (id/reason guards + status constants), the 2.3 repo primitives
  (`openPostConfirmationDisputeOnce`, `resolveConsumedDisputeOnce`, `findArbitrationProbe`,
  `debitForArbitrationOnce`, `ensureWalletOnce`, `incrementLane`, `findById`), `AuditService`
  + `AuditTrailService`, the 2.2 canonical types, and the two early-landed locale keys.
- `backend/services/classes/index.ts` → one-line barrel export of the service.
- `shared/locale/{types,en,ar}/errors/` → the two keys (parity-pinned by
  `errors-namespace.parity.test.ts`).
- Downstream: Task 2.6 (wave service wiring into the two seams), Task 3.1 (GraphQL dispatch +
  case object), Task 5.1 (journey green + pen probes over this surface).
