# Task 3.1 — Pothos mutations + query + schema/codegen — verified & completed

**Task ID:** 3.1 · **Plan:** `ai/plans/sprint_3/dispute-resolution-with-admin-arbitration/`
**Date:** 2026-09-12 · **Agent:** general-purpose subagent (verification-and-completion run)
**Working tree at start:** branch `feat/dispute-resolution-with-admin-arbitration`, HEAD + the 18
uncommitted, staged Task 3.1 files listed below — an interrupted prior execution of this task had
completed the implementation but stopped before verification/bookkeeping. Preconditions held:
`bun tsgo` 0 errors, `session-arbitration.wire.test.ts` 21/0.

---

## Summary

Verified the in-flight GraphQL surface line-by-line against Task 3.1's scope, plan.md
"API Design (GraphQL)" (SDL delta + field-name fidelity note + error/permission matrix), and the
2.1/2.5/2.6/2.7 carry-forward contracts — found it **complete and conformant** (zero
implementation gaps; no code changes were required), ran the full mandatory pipeline to green,
and produced the bookkeeping. The surface lands: the student-only `openPostConfirmationDispute`
entry (authenticated-only scope, service-side predicate, thin resolver, no outer tx), the
`resolveSessionDispute` extension APPENDING `partialAmount: String` with the outcome-family
dispatch (`Cancel|Complete` → the byte-stable shipped lifecycle service; `Refund|PartialRefund|
Uphold` → `SessionArbitrationService.arbitrateDispute`; the resolver publishes nothing — both
flows publish their own waves strictly after their own commits, per the 2.6 contract), the
admin-gated `adminDisputeCase` case-read query (`adminOnlyAuthScopes` `$all` conjunction +
service-side governance re-assertion) with the single canonical `AdminDisputeCase` envelope
object, and the regenerated schema/codegen outputs shipping TOGETHER with the dispatch (the
2.1/2.7 blocking pairing is closed).

**Provenance note:** this task's working tree contained a complete uncommitted in-flight
implementation of Task 3.1 (an interrupted earlier execution of this same task). This run
re-verified every scope element against the plan (verdict table below), proved the full GraphQL
slice, the journey, codegen determinism, the per-file sub-loop, and the project typecheck green,
re-ran `bun run check:unused` (knip) clean, and produced the outcome + checkbox flips. Nothing
was stubbed or forced green; nothing was committed; `worklog.md` untouched; no Phase 4 work
started.

## Files (Task 3.1's authorship, verified this run — all staged, all sub-loop exit 0)

**Created**

1. `backend/graphql/mutation/classes/session-arbitration.mutation.ts` — registers
   `openPostConfirmationDispute(id: ID!, reason: String!): Session!`;
   `authScopes: { authenticated: true }` ONLY (the student predicate is service-side, mirroring
   `openSessionDispute`/`cancelSession`); THIN resolver: `ctx.user` narrowing throw →
   `SessionArbitrationService.openPostConfirmationDispute(ctx.user.id, Number(args.id),
   args.reason, ctx.locale)` — no outer tx, no try/catch, no publish (the flow owns commit +
   post-commit wave publish).
2. `backend/graphql/pothos/classes/session-arbitration.pothos.ts` —
   `AdminDisputeCasePothosObject` = `objectRef<AdminDisputeCaseReturnType>("AdminDisputeCase")`
   from `@/backend/types` (no local types): `session: Session!` (canonical `SessionPothosObject`),
   `report`/`homework`/`recitation` nullable through the canonical `SessionReport` /
   `SessionHomeWork` / `SessionRecitation` objects, `auditTrail: [AdminAuditLogEntry!]!` through
   the canonical `AdminAuditLogEntryPothosObject` — every member entity keeps exactly one GraphQL
   object type; the envelope itself is the sanctioned embedded-value wrapper with NO `id`.
3. `backend/graphql/query/classes/session-arbitration.query.ts` — registers
   `adminDisputeCase(id: ID!): AdminDisputeCase!`; `authScopes: adminOnlyAuthScopes` (the shared
   `$all { authenticated, role:[Admin] }` prelude); `requireAdminUser(ctx)` TS-narrowing belt →
   `SessionArbitrationService.getAdminDisputeCase(user.id, Number(args.id), ctx.locale)`.
4. `backend/graphql/test/session-arbitration.wire.test.ts` — 21-test live-server wire matrix
   (denial byte-identity, oracle collapse, exactly-once, honest nulls, family dispatch both
   directions, financial proofs, trail-after-arbitration), committed `jrn_*`-prefixed cast via
   the shared `buildSessionJourneyCast` factory + FK-safe tracked `afterAll` teardown with
   zero-residue re-probes under the sanctioned trigger suspensions.

**Modified**

5. `backend/graphql/mutation/classes/session-lifecycle.mutation.ts` — `resolveSessionDispute`
   gains ONLY `partialAmount: t.arg({ type: "String", required: false })` (APPEND-ONLY; the
   shipped `id`/`resolution`/`note` args, `Session!` return, and the admin `$all` scope are
   byte-stable) + the family dispatch in the resolver (`Cancel|Complete` →
   `SessionLifecycleService.resolveSessionDispute`; everything else →
   `SessionArbitrationService.arbitrateDispute(adminId, sessionId, resolution, note ?? null,
   partialAmount ?? null, locale)`) + the header/docblock contract.
6. `backend/graphql/pothos/admin/audit-trail.pothos.ts` — `AdminAuditLogEntryPothosObject`
   de-`const`ed to `export` (one word) so the case envelope can embed the canonical entry type —
   no shape change, no second declaration.
7. `backend/graphql/pothos/shared/enum.pothos.ts` — docblock-only deltas: `DisputeResolution`
   now (Cancel|Complete|Refund|PartialRefund|Uphold) with the two-generation dispatch rationale;
   `NotificationType` seven → nine kinds. The enum-object registrations themselves are untouched
   (the canonical TS enum is derived dynamically — 2.1).
8. `backend/graphql/mutation/classes/index.ts` + 9. `backend/graphql/query/classes/index.ts` —
   one side-effect import each (`./session-arbitration.mutation` / `./session-arbitration.query`)
   + docblock inventory lines.
10. `backend/graphql/test/schema-surface.test.ts` — the new SDL pins: the three root fields'
    exact arg shapes + scope snapshots, `AdminDisputeCase`'s exact five-field disclosure, the
    widened `DisputeResolution` (5 members) + `NotificationType` (9 members) pins, the codegen
    byte-identity pins for the three new surface lines (closes 2.1's anticipated red), and the
    baseline inventory + `_health` reflect additions.
11. `backend/graphql/test/sdl-static-assertions.test.ts` — the frozen mutation/query inventories
    gain `openPostConfirmationDispute` (35-op) / `adminDisputeCase` (34-op) + baseline prose.
12. `frontend/graphql/generated/schema.graphql` + 13. `frontend/graphql/generated/gql/graphql.ts`
    — regenerated by `bun run generate:gqlSchema && bun codegen` (see the no-diff proof below;
    NOT hand-edited).
14. `frontend/views/notifications/utils/notification-type-presentation.ts` — the two new
    `NotificationType` members added to the compiler-enforced `Record` maps (label accessors →
    `typeSessionDisputeOpened/Resolved`, icons → `GavelOutlined`, chip order) — exhaustive,
    tsgo-proven.
15. `shared/locale/types/notifications/index.ts` + 16. `shared/locale/en/notifications/index.ts`
    + 17. `shared/locale/ar/notifications/index.ts` — the two type-label keys
    (`typeSessionDisputeOpened`, `typeSessionDisputeResolved`) typed labels + en/ar copy inside
    the existing `notifications` namespace trio (alongside 2.6's six event keys).
18. `shared/locale/notifications-namespace.parity.test.ts` — MANDATED_KEYS + TYPE_LABEL_KEYS
    inventory pins seven → nine (parity suite green).

## Scope-element verification verdict (per brief bullet)

| # | Scope element | Verdict |
|---|---|---|
| 1 | `openPostConfirmationDispute`: args exactly `id: ID!` + `reason: String!` → `Session!`; authenticated (NOT admin-gated — the plain single-key map, no role leg); binds `SessionArbitrationService.openPostConfirmationDispute(ctx.user.id, Number(id), reason, ctx.locale)`; NO outerTx; returns `Session!` | **met** |
| 2 | `resolveSessionDispute` APPEND-ONLY `partialAmount: String`; shipped args/return/scope untouched; dispatch `Cancel/Complete` → shipped `SessionLifecycleService.resolveSessionDispute`, `Refund/PartialRefund/Uphold` → `arbitrateDispute`; resolver does NOT publish (flows publish internally post-commit when called without outerTx — the 2.6 contract); no try/catch | **met** |
| 3 | `adminDisputeCase`: `adminOnlyAuthScopes` (`$all` conjunction); `AdminDisputeCase` registered ONCE (envelope `session: Session!`, `report/homework/recitation` nullable, `auditTrail: [AdminAuditLogEntry!]!`); binds `getAdminDisputeCase(user.id, id, ctx.locale)` | **met** |
| 4 | Codegen outputs regenerated, not hand-edited; deterministic re-emission byte-identical (proof below); `schema-surface` codegen-sync pin closed green | **met** |
| 5 | Notification presentation util + locale keys coherent: both new enum members present in ALL THREE compiler-enforced `Record` maps; `typeSessionDisputeOpened/Resolved` typed labels + en/ar copy; parity 131/0 with the 2.6 event keys | **met** |
| 6 | Schema regen ships WITH the dispatch (2.1/2.7 blocking pairing) — both are in this same uncommitted change | **met** |

## Verification Results

### 3.1.TE — test runs (mandated runner `bun run test/scripts/run-test.ts`)

| Suite | Result |
|---|---|
| `backend/graphql/test/session-arbitration.wire.test.ts` (new) | **21 pass / 0 fail**, 94 expect() calls, 10.2s |
| `backend/graphql/test/schema-surface.test.ts` (regen pins) | **46 pass / 0 fail**, 305 expect() |
| `backend/graphql/test/sdl-static-assertions.test.ts` | **39 pass / 0 fail**, 225 expect() |
| `backend/graphql/test/session-lifecycle-mutations.test.ts` (regression) | **36 pass / 0 fail** |
| `backend/graphql/test/session-sdl.test.ts` (regression) | **20 pass / 0 fail** |
| `backend/graphql/test/admin-session-governance.query.test.ts` (regression) | **21 pass / 0 fail** |
| `backend/graphql/test/admin-session-governance.mutation.test.ts` (regression) | **12 pass / 0 fail** |
| `backend/graphql/test/admin-session-filter-input.sdl.test.ts` (regression) | **6 pass / 0 fail** |
| `backend/graphql/test/session-report.wire.test.ts` (regression) | **46 pass / 0 fail** |
| `backend/graphql/test/recitation-record.wire.test.ts` (regression) | **26 pass / 0 fail** |
| `backend/graphql/test/audit-trail.query.test.ts` (the de-`const`ed export's suite) | **10 pass / 0 fail** |
| `backend/graphql/test/notification-query.test.ts` (widened NotificationType regression) | **12 pass / 0 fail** |
| `backend/graphql/test/notification-mutation.test.ts` (regression) | **13 pass / 0 fail** |
| `shared/locale/notifications-namespace.parity.test.ts` (7 → 9 pins) | **131 pass / 0 fail**, 825 expect() |
| `test/workflows/sessions/post-confirmation-dispute.journey.test.ts` (acceptance harness) | **13 pass / 0 fail**, 168 expect() — the 2.4/2.6 expected-red state is now fully green through the real service surface |
| `bun run check:unused` (knip, strict flags) | **0 findings** |
| `bun tsgo` (project-wide) | **exit 0, 0 "error TS" lines** |

### Codegen no-diff proof (3.1 scope bullet 4)

```
sha256 before regen:
433263c5…  frontend/graphql/generated/schema.graphql
7c78d3af…  frontend/graphql/generated/gql/graphql.ts
bun run generate:gqlSchema   → wrote schema.graphql (34360 bytes)
bun codegen                  → ✔ Generate to ./frontend/graphql/generated/gql/graphql.ts
sha256 after regen: IDENTICAL — diff of the hash files: empty
```

A second full regen over the working-tree outputs re-emitted both files byte-identically, so the
staged outputs are exactly what the current Pothos schema produces (the `M` vs HEAD is 3.1's
intended schema delta; the committed outputs at HEAD were stale by design until this task). The
`gql/graphql.ts` delta is exactly the two widened native enums — the operation types are
documents-driven (`codegen.ts` `documents: frontend/graphql/sharedDocuments/**`), so
`AdminDisputeCase`/`openPostConfirmationDispute` operation types appear when Task 4.1 adds the
documents (carry-forward below). The `schema-surface` "Codegen sync — committed SDL is
byte-identical to the built schema" test passes, including the new
`openPostConfirmationDispute(id: ID!, reason: String!): Session!`,
`adminDisputeCase(id: ID!): AdminDisputeCase!`, `type AdminDisputeCase {`, and
`resolveSessionDispute(id: ID!, note: String, partialAmount: String, resolution:
DisputeResolution!): Session!` artifact pins.

### 3.1.QL — sub-loop `--lifecycle duplicates` per scope file, all exit 0

| File | tsgo | oxlint | biome | lint | duplicates | Exit |
|---|---|---|---|---|---|---|
| `backend/graphql/mutation/classes/session-arbitration.mutation.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `backend/graphql/mutation/classes/session-lifecycle.mutation.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `backend/graphql/mutation/classes/index.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `backend/graphql/pothos/classes/session-arbitration.pothos.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `backend/graphql/pothos/admin/audit-trail.pothos.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `backend/graphql/pothos/shared/enum.pothos.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `backend/graphql/query/classes/session-arbitration.query.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `backend/graphql/query/classes/index.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `backend/graphql/test/session-arbitration.wire.test.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `backend/graphql/test/schema-surface.test.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `backend/graphql/test/sdl-static-assertions.test.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `frontend/views/notifications/utils/notification-type-presentation.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `shared/locale/types/notifications/index.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `shared/locale/en/notifications/index.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `shared/locale/ar/notifications/index.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `shared/locale/notifications-namespace.parity.test.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |

Generated-file treatment (verified, then followed): `frontend/graphql/generated/**` is excluded
from ESLint (`!frontend/graphql/generated` in eslint config ignores) and from jscpd
(`frontend/graphql/generated/**` in `.jscpd.json` ignore) — root AGENTS.md "Auto-generated types
(do not edit)". The two generated outputs are therefore outside the per-file sub-loop by repo
convention; their correctness is proven by the deterministic regen + the codegen-sync suite
instead.

## 3.1.SEC — report per item

- **Scope gates:** `openPostConfirmationDispute` declares ONLY `{ authenticated: true }` — the
  "authenticated, then the service checks the predicate" posture mandated for participation-gated
  operations (no role leg to force `$all`; an admin caller gets the same oracle-safe
  `SESSION_NOT_FOUND`). Both admin surfaces declare the explicit `adminOnlyAuthScopes` `$all`
  conjunction (the ANY-semantics key-map trap avoided) — anonymous → 401, non-admin → 403, both
  BEFORE the resolver body; the wire test proves both denials byte-identical to the shipped
  arbitration reference (message, `extensions.code`, extension key set; only `requestId`/`path`
  differ by design).
- **BOLA/IDOR:** identity comes exclusively from the verified context (`ctx.user.id` /
  `requireAdminUser(ctx).id`) — never from an arg; the student caller is folded into the
  service's guarded write predicate; non-participant ≡ nonexistent id (oracle collapse, tested
  for the row's own teacher, a foreign student, and id 999999999).
- **BFLA:** admin surfaces are double-belted (scope gate + service
  `assertAdminGovernanceClean`); the wire matrix runs student/teacher/parent against the case
  read and pins the 403 byte-identity.
- **BOPLA:** strict field-by-field mapping — `Number(args.id)` (shape-only; the service owns the
  id guard), `args.reason`, `args.note ?? null`, `args.partialAmount ?? null`; no spread, no
  local input re-shaping; the amount's range/precision policy stays service-side pre-DB
  (`partialAmount: "0"` and stray-amount-with-`Uphold` denials proven on the wire with the exact
  localized copy, row untouched).
- **Depth/complexity no-op:** the envelope adds NO new scalar fan-out depth — all members are
  existing bounded entity objects; the trail is the trail service's own bounded session page.

## 3.1.SR — semantic review checklist (each item)

1. **Single canonical object types** — `AdminDisputeCase` is registered exactly once (the only
   `objectRef<AdminDisputeCaseReturnType>` in the repo); it embeds the existing canonical
   `Session`/`SessionReport`/`SessionHomeWork`/`SessionRecitation`/`AdminAuditLogEntry` objects
   by reference and registers no duplicate of any of them; the audit-trail module's one-word
   `export` change reuses the canonical ref instead of a second declaration. Runtime duplicates
   would fail the schema build; the surface suites prove the built schema.
2. **Enum value imports** — `DisputeResolution` is a VALUE import in
   `session-lifecycle.mutation.ts` (runtime dispatch comparisons) and in `enum.pothos.ts`
   (runtime `enumType` registration); `UserRole`/`SessionIntent` likewise; no string literals
   stand in for enum members anywhere in scope.
3. **Resolver thinness** — three resolvers, zero business logic: one TS-narrowing throw each
   (unreachable; mirrors `builder.ts`'s own scope throw), the family `if` dispatch on the already
   -parsed enum, and the service calls; no try/catch (DomainErrors propagate to the masking
   boundary), no repository calls, no notification handling, no publish.
4. **Identity from ctx (BOLA)** — verified above; no arg carries identity on any of the three
   operations.
5. **Strict input mapping (BOPLA)** — verified above; closed arg whitelists are schema-enforced
   (unknown args die at validation).
6. **No dead branches** — the dispatch `if` is exhaustive over the enum with live tests on both
   sides (held-family values reach the shipped service — proven by the generic-`validation` copy
   on the never-started `Complete` leg AND by the `Cancel` happy path; consumed-family values
   reach the arbitration service — proven by `Uphold`/`Refund` happy paths and both amount
   denials). No other branching exists in scope.
7. **No cross-layer imports** — GraphQL modules import only `@/backend/*`; the frontend util
   imports `@mui/icons-material` + the codegen enum + `@/shared/locale/types` (sanctioned);
   the wire test's `@/backend/db` / `signAccessToken` / `@/test/*` imports follow the shipped
   `backend/graphql/test/*-wire.test.ts` convention (fixture provisioning + assertion reads —
   all operation flows still go through the live GraphQL boundary only).
8. **Scope boundary** — `git status` shows exactly the 18 in-scope files (+ this outcome +
   the tasks.md flip); no other file touched; `worklog.md` untouched; nothing committed.
9. **Clean comments (ZERO plan-artifact refs; domain language only)** — grep
   (`REQ-\d|DEV3-|Task \d|plan\.md|tasks\.md|specs\.md|D-\d`) over every scope file: the only
   hits are the shipped test files' PRE-EXISTING `REQ-0xx` pins from earlier plans (untouched
   lines); a grep restricted to ADDED lines returns zero hits. Comments state domain rationale
   (scope semantics, oracle collapse, dispatch safety, transaction ownership) — no narration.

## 3.1.IV — instruction verification

Sub-loop printed and I read in full: root `AGENTS.md`, `backend/AGENTS.md`,
`backend/graphql/AGENTS.md` + `backend/graphql/{mutation,query,pothos}/AGENTS.md`,
`.agents/instructions/backend.instructions.md`, `.agents/instructions/tests.instructions.md`,
plus `frontend/views/AGENTS.md`/`frontend/AGENTS.md` and `shared/AGENTS.md` via the sub-loop's
discovery for the frontend/locale files. Findings per applicable rule:

- **Side-effect barrel wiring** (`mutation/AGENTS.md` + `query/AGENTS.md`): both new files have
  NO named exports, register root fields via `mutationField`/`queryField` at import time, and are
  wired exclusively through their sub-directory barrels; no direct out-of-directory imports of
  the new modules. ✅
- **Pothos layer rules** (`pothos/AGENTS.md` + `backend/graphql/AGENTS.md`): no local type
  definitions (the object binds `AdminDisputeCaseReturnType` from `@/backend/types`); no enum
  re-registration (`DisputeResolutionPothosEnum` imported from `shared/enum.pothos.ts`); no
  dynamic imports; no DataLoader requirement triggered (no per-parent service calls — the
  envelope is a passthrough of one composed bundle); nullability explicit
  (`nullable: true` on the three honest-null artifacts). ✅
- **`id` exposure** — the envelope's members all expose `id` through the canonical objects; the
  envelope itself is the sanctioned embedded-value exception (same posture as
  `AdminAuditLogPage`). ✅
- **Schema regen** — `bun run generate:gqlSchema && bun codegen` ran clean after the Pothos
  edits (the codegen-sync suite pins the byte identity). ✅
- **Locale rules** (`shared/AGENTS.md`): keys added INSIDE the existing `notifications` namespace
  trio; typed labels + en/ar implementations; parity inventory pins updated in the SAME change
  (the stale-pin rule); property-access labels on the frontend (no call-by-key). ✅
- **Test rules** (`tests.instructions.md`): `bun:test` only; live-server harness via
  `setupTestServerLifecycle`; denials asserted on `result.error` (`errorPolicy: "all"`) with
  null-guards before payload assertions; committed-fixture teardown in FK order under the
  sanctioned trigger suspensions with tracked `afterAll` + zero-residue re-probes; unique per-run
  `jrn_` prefixes; no `console.*`, no `any`, no lint-disables; optional mutation args exercised
  both valued and absent (omission ≡ null on the wire, the shipped sibling convention). ✅
- **Linting**: zero `oxlint-disable`/`eslint-disable` anywhere in scope. ✅

## Carry-Forward for Task 4.1 (GraphQL documents)

- Codegen is DOCUMENTS-driven (`codegen.ts`: `documents: ["./frontend/graphql/sharedDocuments/**/*.ts", …]`
  with `typed-document-node` + `typescript-operations`, `enumType: "native"`,
  `avoidOptionals: true`, `extractAllFieldsToTypesCompact: true`). The current `gql/graphql.ts`
  regen delta is therefore ONLY the two widened enums; adding the documents generates the
  operation types.
- Expected generated names (convention: `{OperationName}`, `{OperationName}Variables` as
  `Exact<{…}>`, inline compact payload shapes per the document's selections):
  - `openPostConfirmationDisputeMutationDocument` →
    `OpenPostConfirmationDisputeMutation` / `OpenPostConfirmationDisputeMutationVariables`
    (`Exact<{ id: string; reason: string }>` — `ID` maps to `string`); payload
    `OpenPostConfirmationDisputeMutation_openPostConfirmationDispute` shaped by the selected
    fields (include `id` on every object per the repo document convention).
  - `adminDisputeCaseQueryDocument` → `AdminDisputeCaseQuery` /
    `AdminDisputeCaseQueryVariables` (`Exact<{ id: string }>`); payload
    `AdminDisputeCaseQuery_adminDisputeCase` with nested inline shapes for
    `session`/`report`/`homework`/`recitation`/`auditTrail` per selection (absent artifacts are
    `| null` members; the audit trail items carry `actionType: AuditActionType` native enum).
  - The resolve document's `ResolveSessionDisputeMutationVariables` gains
    `partialAmount: InputMaybe<string>` and its `resolution` widens to the five-member native
    `DisputeResolution` enum (already emitted); `AdminDisputeCase` type vocabulary is in
    `schema.graphql` verbatim (SDL shown in the codegen section above).
- The `schema-surface`/`sdl-static-assertions` inventories already include the new operations —
  documents do not touch them.

## Carry-Forward for Task 5.1 (hardening — denial behaviors already pinned, probes to add)

Pinned on the wire by this task's suite (byte-level, against the shipped
`resolveSessionDispute` reference): anonymous → `UNAUTHORIZED` byte-identity on all three
operations; student/teacher/parent → `FORBIDDEN` byte-identity on both admin surfaces;
`SESSION_NOT_FOUND` oracle collapse (teacher / foreign student / nonexistent id) on the student
entry and the case read; `SESSION_INVALID_TRANSITION` on wrong-state open and immediate
double-submit; pre-DB `VALIDATION` on whitespace reason; classification mismatch
(`Refund` on a held row) → `VALIDATION` with the exact `disputeResolutionMismatch` copy, row
untouched; never-started `Complete` → `VALIDATION` with the GENERIC copy (proves the shipped
service owns that leg); amount policy (`"0"`, stray amount beside `Uphold`) → `VALIDATION` with
the exact `partialRefundAmountInvalid` copy. NOT yet wire-pinned (5.1 pen-probe candidates, all
service-proven in 2.5/journey): `WALLET_INSUFFICIENT_FUNDS` over the wire, `partialAmount` fuzz
over-precision/NaN/negative/fee-equal strings, cross-family resolutions in the reverse pairing
(`Cancel/Complete` on a consumed row), the admin cohort fan-out bounds (D4, closes in 6.1).

## Cross-File Dependencies

- `session-arbitration.mutation.ts` / `session-arbitration.query.ts` →
  `SessionArbitrationService` (the 2.5 positional Component-1 signatures, bound exactly),
  `SessionLifecycleService` (the shipped dispatch leg), the Pothos objects, the shared
  admin prelude (`adminOnlyAuthScopes`/`requireAdminUser`), `enum.pothos.ts`'s
  `DisputeResolutionPothosEnum`.
- `session-arbitration.pothos.ts` → the five canonical member objects + `@/backend/types`
  (`AdminDisputeCaseReturnType`; the readonly trail array is copied at the field boundary).
- `audit-trail.pothos.ts` → one-word `export` consumed by the envelope (knip-clean).
- Barrels → `mutation/classes/index.ts`, `query/classes/index.ts` side-effect lines.
- Generated outputs ↔ Pothos schema (deterministic; pinned by the codegen-sync suite).
- Notifications trio + parity pin + presentation util ↔ the 2.1 enum members.
- Downstream: Task 4.1 (documents), Task 4.2/4.3 (UI consumes the surface), Task 4.4 (remaining
  `sessions`-namespace UI copy), Task 5.1 (journey green; hardening wave), Tasks 6.1/6.2 (gate +
  propagation).
