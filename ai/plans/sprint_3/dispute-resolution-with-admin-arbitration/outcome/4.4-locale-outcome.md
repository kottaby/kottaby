# Task 4.4 — Locale keys (en + ar) + parity — verified & completed

**Task ID:** 4.4 · **Plan:** `ai/plans/sprint_3/dispute-resolution-with-admin-arbitration/`
**Date:** 2026-09-12 · **Agent:** general-purpose subagent (verification-and-formalization run)
**Working tree at start:** HEAD (`34e4374`, Tasks 0–4.3 committed) + one pre-existing uncommitted
formatting normalization on `shared/locale/ar/sessions/labels.ts` (biome line-collapse of two
helper strings, left by an interrupted prior run of this task — retained, content identical).

---

## Summary

Verified the **complete locale surface** of the dispute-arbitration feature line-by-line: every
`t.*` / `te.*` key referenced by the Task 4.2/4.3 components and dialogs was inventoried (92 raw
tokens across `frontend/views/admin/disputes/`, `frontend/views/student/sessions/`,
`frontend/views/teacher/sessions/`), classified per namespace, and checked against **all three
layers** (`shared/locale/types/**`, `shared/locale/en/**`, `shared/locale/ar/**`). **Zero missing
keys** — the `sessions`-namespace additions (23 keys) landed with the interrupted 4.2/4.3 run in
commit `34e4374`, and the two errors-namespace keys landed early with Task 2.5; this run
**formalized** them as the task owner: extended the `sessions` parity suite's mandated registry
(+23 keys), sync-resolution and Arabic-script samples, and added the two arbitration denial keys
to the `errors` parity suite's domain-service pins (the belt its own docblock prescribes for
service-tier consumers the route-source discovery cannot see). No locale content was authored or
altered this run — all 25 keys' en/ar copy was verified present, non-empty, register-consistent,
and already pinned green by the compile-time `SessionsLabels`/`ErrorsLabels` typing.

## Files created/modified (this run)

- MODIFIED: `shared/locale/sessions-namespace.parity.test.ts` — mandated `MANDATED_SESSIONS_KEYS`
  registry extended with the 23 post-confirmation keys; `SYNC_SAMPLE_SESSIONS_KEYS` +
  `escrowConsumedChip`/`resolutionPartialRefundLabel`/`caseReviewTitle`; `ARABIC_SCRIPT_SAMPLE_KEYS`
  + `resolutionRefundHelper`/`caseReviewEmptyReport`; docblock bullet 2 documents the
  arbitration-extended inventory.
- MODIFIED: `shared/locale/errors-namespace.parity.test.ts` — new `session-arbitration denial keys`
  describe block pinning `disputeResolutionMismatch` + `partialRefundAmountInvalid` in BOTH locales
  + compile-time schema membership (the session-report/homework/recitation pin precedent: the
  route-emitter scan cannot see the arbitration service's or the resolve dialog's consumption
  sites); docblock bullet 5 updated.
- NOT modified: all six locale content files (`shared/locale/{types,en,ar}/{sessions,errors}/…`) —
  every referenced key already exists in every layer (see inventory); `worklog.md` (protocol);
  nothing committed.

## Key inventory (the 4.4-owned vocabulary: 0 added this run, 25 formalized as present)

Layer files per key: `shared/locale/types/sessions/labels.ts` + `shared/locale/en/sessions/labels.ts` +
`shared/locale/ar/sessions/labels.ts` (rows 1–23); `shared/locale/types/errors/labels.ts` +
`shared/locale/en/errors/index.ts` + `shared/locale/ar/errors/index.ts` (rows 24–25).

| # | Key | Status | en copy | ar copy (truncated) |
|---|-----|--------|---------|---------------------|
| 1 | `escrowHeldChip` | present (4.2/4.3 run) | Fee held | الرسوم محجوزة |
| 2 | `escrowConsumedChip` | present (4.2/4.3 run) | Fee consumed | الرسوم مستهلكة |
| 3 | `resolutionRefundLabel` | present (4.2/4.3 run) | Refund the student | رد الرسوم للطالب |
| 4 | `resolutionRefundHelper` | present (4.2/4.3 run) | The completed session is refunded in full: the fee returns to the student and the teacher's wallet is debited. | يُعاد كامل رسوم الجلسة المكتملة إلى الطالب… |
| 5 | `resolutionPartialRefundLabel` | present (4.2/4.3 run) | Partial refund | رد جزئي |
| 6 | `resolutionPartialRefundHelper` | present (4.2/4.3 run) | A chosen slice of the fee returns to the student and the rest stays with the teacher. The amount is decided below. | يُعاد جزء محدد من الرسوم إلى الطالب… |
| 7 | `resolutionUpholdLabel` | present (4.2/4.3 run) | Uphold the completion | إقرار الإنجاز |
| 8 | `resolutionUpholdHelper` | present (4.2/4.3 run) | The completed session stands and no money moves. | تبقى الجلسة المكتملة كما هي… |
| 9 | `partialAmountLabel` | present (4.2/4.3 run) | Refund amount | مبلغ الرد |
| 10 | `partialAmountPlaceholder` | present (4.2/4.3 run) | e.g. 12.50 | مثال: 12.50 |
| 11 | `reviewCase` | present (4.2/4.3 run) | Review case | مراجعة الملف |
| 12 | `caseReviewTitle` | present (4.2/4.3 run) | Dispute case review | مراجعة ملف النزاع |
| 13 | `caseReviewReportTitle` | present (4.2/4.3 run) | Session report | تقرير الجلسة |
| 14 | `caseReviewHomeworkTitle` | present (4.2/4.3 run) | Session homework | واجب الجلسة |
| 15 | `caseReviewRecitationTitle` | present (4.2/4.3 run) | Session recitation | سجل التلاوة |
| 16 | `caseReviewAuditTitle` | present (4.2/4.3 run) | Audit trail | سجل التدقيق |
| 17 | `caseReviewRatingLabel` | present (4.2/4.3 run) | Student rating by teacher | تقييم المعلم للطالب |
| 18 | `caseReviewHomeworkCurrentLabel` | present (4.2/4.3 run) | Current homework | الواجب الحالي |
| 19 | `caseReviewHomeworkRevisionLabel` | present (4.2/4.3 run) | Revision homework | واجب المراجعة |
| 20 | `caseReviewEmptyReport` | present (4.2/4.3 run) | No report has been submitted for this session. | لم يتم إرسال تقرير لهذه الجلسة. |
| 21 | `caseReviewEmptyHomework` | present (4.2/4.3 run) | No homework was recorded for this session. | لم يُسجل أي واجب لهذه الجلسة. |
| 22 | `caseReviewEmptyRecitation` | present (4.2/4.3 run) | No recitation record exists for this session. | لا يوجد سجل تلاوة لهذه الجلسة. |
| 23 | `caseReviewEmptyAudit` | present (4.2/4.3 run) | No audit entries have been recorded for this session. | لم تُسجل أي إدخالات تدقيق لهذه الجلسة. |
| 24 | `disputeResolutionMismatch` | present (landed early by 2.5, formalized) | This resolution is not allowed for the dispute in its current escrow state. | لا يُسمح بهذا القرار للنزاع… |
| 25 | `partialRefundAmountInvalid` | present (landed early by 2.5, formalized) | Enter a valid partial refund amount (greater than zero, less than the session fee, with up to 2 decimal places). | أدخل مبلغ استرداد جزئي صالحاً… |

**Deviation formalized (per the 2.5 outcome note):** the landed `partialRefundAmountInvalid` en
copy is the deliberate superset of the plan's "e.g." example — it additionally names the two-decimal
precision criterion of REQ-3.2; `disputeResolutionMismatch` had no plan example. Both strings are
KEPT byte-identical (the 2.5 service suite asserts the exact translated copy).

**Pre-existing keys REUSED by the 4.2/4.3 components (not 4.4-owned, verified present in all three
layers):** sessions — `disputeConfirmTitle`, `disputeConfirmBody`, `disputeReasonLabel`,
`disputeReasonPlaceholder`, `disputeReasonRequired`, `openDispute`, `disputeOpenedNotice`,
`genericError`, `resolveDispute*`, `resolutionCancel*/Complete*`, `resolutionNote*`,
`disputeReasonMeta/Expand/Collapse`, `disputedAtLabel`, `participantsLabel`, `fee`, `intent`,
`createdAt`, `pager*`, `adminDisputes*`; errors — `sessionNotFound`, `sessionInvalidTransition`,
`validation`, `forbidden`, `teacherNotCertified`; common — `cancel`. Notifications wave copy
(`eventSessionDisputeOpenedTitle/Body`, `eventSessionDisputeResolvedTitle/Body` + type labels) is
2.6-owned and present (verified in the notifications trio).

**Component → key reference map (grep-verified, `t.`/`te.` accesses):**
`ResolveDisputeDialog` → resolveDisputeTitle/Body/Submit + `te.partialRefundAmountInvalid` +
`te.disputeResolutionMismatch` + options via `resolveDisputeOutcomeOptions(t)` +
`Common.cancel`; `ResolveDisputeAmountField` → partialAmountLabel/Placeholder;
`ResolveDisputeNoteField` → resolutionNoteLabel/Placeholder; `AdminDisputeEscrowChip` →
escrowHeld/ConsumedChip; `AdminDisputeRow` → intent/fee/createdAt/disputedAtLabel/
participantsLabel/reviewCase/resolveDispute (+ caseReviewTitle/resolveDisputeTitle tooltips);
`AdminDisputeCaseDialog` → caseReviewTitle + genericError; `AdminDisputeCaseArtifacts` →
caseReviewReport/Homework/Recitation titles + rating/homework meta labels + empty states;
`AdminDisputeCaseAuditTrail` → caseReviewAuditTitle/EmptyAudit; `AdminDisputeCaseSessionFacts` →
disputeReasonMeta/disputedAtLabel/fee/participantsLabel; `AdminDisputesContainer/Body` →
adminDisputes*; `SessionDisputeConfirmDialog` (both generations) → disputeConfirmTitle/Body +
disputeReasonLabel/Placeholder/Required + openDispute + genericError + `te.validation`/`te.forbidden`.
Post-confirmation open-confirm copy is COMPLETE through the shipped shared dialog vocabulary
(title/body/submit/cancel/reason label+placeholder/required message; the char counter is a raw
`${reason.length}/${maxLength}` figure — deliberately not locale copy).

## Verification results

### 4.4.TE — parity suites (mandated runner `bun run test/scripts/run-test.ts`)

| Suite | Result |
|---|---|
| `shared/locale/sessions-namespace.parity.test.ts` (extended registry) | **20 pass / 0 fail**, 921 expect() calls |
| `shared/locale/errors-namespace.parity.test.ts` (+arbitration pins) | **23 pass / 0 fail**, 214 expect() calls |
| `shared/locale/notifications-namespace.parity.test.ts` (2.6 waves, regression) | **131 pass / 0 fail**, 825 expect() calls |
| `shared/locale/server.test.ts` (namespace-catalog sweep, regression) | **21 pass / 0 fail**, 42 expect() calls |
| `bun tsgo` (compile-time parity gate, via sub-loop first stage) | **0 errors** — `sessionsEn`/`sessionsAr` typed `SessionsLabels`, `errorsEn`/`errorsAr` typed `ErrorsLabels` |

The three parity belts that make a missing key impossible: (1) compile-time typing on both leaf
consts, (2) identical sorted ar/en key sets + zero-dead-key non-empty sweep, (3) the mandated
inventories extended this run.

### UI suites that consume the copy (package.json `test:ui:components` preload list mirrored exactly:
`test-env.ts` → `happydom-preload.ts` → `translation-preload.ts` → `next-dynamic-mock.ts`)

`KOTTABY_TEST_RUNNER_OK=1 TEST_SERVER_MODE=production bun --env-file=.env.test.ci test
--parallel=1 test/ui/components/admin/ResolveDisputeDialog.test.tsx
test/ui/components/admin/AdminDisputeCaseDialog.test.tsx
test/ui/components/student/StudentPostConfirmationDispute.test.tsx …`
→ **42 pass / 4 skip / 0 fail**, 294 expect() calls. The 4 skips are the pre-existing
env-deferral variants documented by the 4.2/4.3 outcomes (RTL + typed-submit arms), untouched.

### 4.4.QL — sub-loop `--lifecycle duplicates`, per edited file

| File | tsgo | oxlint | biome | lint:type-aware | duplicates | Exit |
|---|---|---|---|---|---|---|
| `shared/locale/sessions-namespace.parity.test.ts` | ✅ | ✅ | ✅ | ✅ | skipped (test file outside jscpd scope) | **0** |
| `shared/locale/errors-namespace.parity.test.ts` | ✅ | ✅ | ✅ | ✅ | skipped (test file outside jscpd scope) | **0** |

## 4.4.SEC — no user data enters locale files

Both edited files are test-only pins over static copy constants; all six locale content files were
read-verified, none authored. The `sessions`/`errors`/`notifications` content contains only UI
copy — no user identifiers, no session/reason/note content, no machine payload keys (the errors
suite's `_health` machine-constant exemption test re-affirms the boundary). The arbitration audit
row's no-note-content convention (REQ-8.3) is untouched by this task.

## 4.4.SR — semantic review checklist (each item)

1. **Property-access handles only** — every consumer verified (`useAppTranslation(Sessions|Errors|Common)`
   + property access; service tier via `getServerTranslations(locale).errorsTranslations.*`); zero
   `t('key')` calls, zero string-literal namespaces, no `Translation` enum, no `next-intl`/
   `getBackendTranslations` re-introduction (errors suite's import-hygiene pin re-run green). ✅
2. **No new namespace invented** — all 25 keys live in the EXISTING `sessions` and `errors`
   namespaces; the `Sessions`/`Errors` handles and the registry wiring are untouched and re-pinned
   by the parity suite (`sessions.sessions` id convention). ✅
3. **en/ar parity** — compile-typed leaf consts + identical key sets + placeholder-agreement +
   Arabic-script samples (extended to the new copy); both new samples pass. ✅
4. **No dead keys** — zero keys added this run; every pre-existing 4.4-scope key is referenced by
   at least one component in the reference map above (grep-verified), and the 23 sessions keys were
   already exercised by the 4.2/4.3 UI suites. Nothing removed. ✅
5. **Scope boundary** — only the two parity test files edited (+ outcome + checkbox flips); locale
   content files, components, and `worklog.md` untouched; nothing committed. ✅
6. **Clean comments** — the two test-file edits carry domain-language comments only (no
   plan-artifact references); docblock edits describe what the gates lock. ✅

## 4.4.IV — instruction verification

Sub-loop auto-discovery printed + read for both files: root `AGENTS.md`, `shared/AGENTS.md` (the
locale trio rules — keys inside existing namespaces, typed labels, parity suite, no cross-layer
imports, `@/` alias discipline), and `.agents/instructions/tests.instructions.md` (test-file
mapping). Findings: the mandated-runner convention holds; no `console.*`, no `any`, no
`oxlint-disable`; `bun:test` imports only; the extended registries follow the sibling
notifications-suite mandated-inventory precedent (64 → 70 slots) that 2.6 established. The
tasks.md layer mapping (`shared/locale/**` → `shared/AGENTS.md`) is honored.

## Carry-forward knowledge

- The 25-key inventory above is now REGISTRY-PINNED: removing any key from BOTH locale maps
  simultaneously still fails the suites (the mandated inventories survive even a typing loosen).
- The copy register for future arbitration-adjacent keys: en imperative-neutral factual sentences;
  ar MSA with the dispute register (نزاع/تحكيم/الرسوم المحجوزة-المستهلكة/رد جزئي/إقرار الإنجاز).
- `resolveDisputeOutcomeOptions(feeHeld, t)` remains the single source of the classification
  vocabulary — any new outcome label must extend `SessionsLabels` + both locales + the parity
  registry in one change.
- Task 5.1 (journey green + pen probes) and 6.1 (full quality gate) inherit a fully-pinned locale
  surface; no locale work is deferred.

## Cross-file dependencies discovered

- `frontend/views/admin/disputes/*` and `frontend/views/student/sessions/*` → the pinned keys (via
  `@/shared/locale` handles only — compile-typed, so a future key removal fails `bun tsgo` before
  any runtime undefined).
- `backend/services/classes/session-arbitration.service.ts` → the two errors keys (service-tier
  consumers; now double-pinned by the errors parity suite's domain-service block).
