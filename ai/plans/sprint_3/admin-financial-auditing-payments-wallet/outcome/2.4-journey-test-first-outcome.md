# Task 2.4 — Journey Test Authored TEST-FIRST Outcome

**Date:** 2026-09-12
**Executor:** coder subagent (agent-3), orchestrated by spec-implementation

## Summary

Created `test/workflows/billing/admin-financial-auditing.journey.test.ts` authored TEST-FIRST: the
service module (`backend/services/billing/admin-financial-auditing.service.ts`, Task 2.5) does not
exist yet, so the file is the EXPECTED RED state until 2.5 lands.

## Files Created

- `test/workflows/billing/admin-financial-auditing.journey.test.ts` — the acceptance harness for
  J-W1 (approve), J-W2 (reject + balance restore), J-ADJ (credit/debit adjustments + insufficient
  funds), payment-audit filter visibility, the denial matrix (teacher/student → ForbiddenError,
  anonymous → UnauthorizedError through the REAL admin gate; zero audit rows written by denials),
  and the concurrent double-settle race (`Promise.allSettled`, exactly one winner, one audit row).

## Files NOT Modified (and why)

- The service itself — Task 2.5's deliverable; creating it would break the test-first protocol.
- `backend/db/repo/billing/*` — Task 2.3's parallel agent owns them.
- `shared/locale/**` — Task 4.2's parallel wave owns them.

## Verification Results

- **RED state confirmed**: run-test on the journey → `0 pass / 1 fail / 1 error` with
  `Cannot find module '@/backend/services/billing/admin-financial-auditing.service'` — the expected
  red. No fixtures written (module load fails before `beforeAll`) → zero DB residue.
- Sibling journey untouched: `subscription-purchase.journey.test.ts` → 11 pass / 0 fail.
- Per the test-first protocol the per-file quality sub-loop is deferred until the service lands
  (it would fail on the missing module — expected); the green run happens in Phase 5.1.

## Journey Contract (what 2.5 must satisfy — carry-forward for Task 2.5)

- Service methods (static imports from `@/backend/services/billing/admin-financial-auditing.service`):
  `listStudentPaymentsForAdmin`, `getTeacherWalletForAdmin`, `listPendingWithdrawalsForAdmin`,
  `approveWithdrawal`, `rejectWithdrawal`, `adjustTeacherWallet` — signatures per plan.md Component 2.
- Audit vocabulary the journey asserts: `entityType="teacher_transaction"`, `entityId=<ledger id>`,
  approve/reject → `AuditActionType.Override`, adjust → `AuditActionType.Adjust`.
- The manual-debit adjustment description must differ from the shipped payout-request wording
  (`"Withdrawal request (pending payout)"`).
- Loss of a settle race → `ConflictError` carrying the `withdrawalNotPending` translated copy;
  over-balance debit → `ConflictError` carrying `insufficientBalance`.
- Teardown regime honored: `teacher_transaction` rows hard-deleted FIRST under
  `withImmutabilityTriggersSuspended(["teacher_transaction"])`; audit rows under
  `withAuditDeleteTriggersSuspended`; never registered in the tracked-fixture registry;
  `journeyPrefix("billing")` (`jrn_billing_<8hex>`); no `runInRollback`; committed fixtures in one
  `db.transaction`; all denial copy from `getServerTranslations("en").errorsTranslations`.

## Semantic Review (checklist)

- [x] No cross-layer imports (test imports only `@/backend/**`, `@/shared/**`, `@/test/**` — no `app/`).
- [x] No plan-artifact references in code comments.
- [x] Enum VALUE import (`WalletAdjustmentDirection`) — runtime use.
- [x] No hardcoded English assertion strings (translated substrings only).
- [x] No `any`, no `console.*`, bun:test only.
- [x] Deferred work recorded: journey green + quality loop after 2.5 (Phase 5.1 drives hardening).

## Carry-Forward Knowledge

- `WalletService.requestWithdrawal(callerUserId, rawAmount, locale, outerTx?)` is the shipped
  request seam the journey uses for fixtures.
- The journey is the acceptance harness for REQ-1…REQ-9; Phase 5.1 adds the adversarial probes.
