# Task 3.1 — GraphQL Surface Outcome

**Date:** 2026-09-12 (surface) / 2026-09-13 (hardening fixes)
**Executor:** coder subagents across the GraphQL wave; codegen + census edits verified green

## Summary

- Pothos objects/inputs: `AdminStudentPayment`, `AdminStudentPaymentPage`, `AdminTeacherWallet`,
  `AdminWithdrawalQueueRow`, `AdminWithdrawalQueuePage`, filter inputs, `AdjustTeacherWalletInput`;
  `WalletAdjustmentDirectionPothosEnum` registered in `pothos/shared/enum.pothos.ts`;
  `TeacherTransactionPothosObject` exported from `pothos/billing/wallet.pothos.ts` (module-private → one-line export).
- `backend/graphql/query/admin/admin-finance.query.ts` (3 queries) +
  `backend/graphql/mutation/admin/admin-finance.mutation.ts` (3 mutations): admin-only auth scopes
  via inline literal `$all: { authenticated: true, role: [UserRole.Admin] }` (the census-drift
  classifier recognizes literal role arrays), `requireAdminUser`, closed field-by-field input
  whitelists (no spreads), `ctx.locale` propagated, strict decimal-only ID coercion for all
  financial ID args.
- `bun run generate:gqlSchema && bun codegen` run; generated outputs committed.
- CI-pinned registries: schema-surface frozen inventory updated (+3 queries, +3 mutations, +1 enum,
  new objects/inputs); audit completeness catalog D-002 replaced by three `wired` rows
  (approve/reject → `override`, adjust → `adjust`); `ACTION_TYPE_COVERAGE.Adjust` → `wired`;
  producer lanes added to the audit-completeness journey; census-drift expectations updated.

## Verification

- `frontend/graphql/test/admin/admin-finance.integration.test.ts` → **27 pass / 0 fail**
  (anon/non-admin denial bytes per field, SDL pinning, settle/adjust happy paths, explicit-id cleanup).
- `backend/graphql/test/schema-surface.test.ts` → **53 pass / 0 fail**.
- `backend/db/test/logic/audit/audit-census-drift.test.ts` → **19 pass / 0 fail**.
- `test/workflows/admin/audit-completeness.journey.test.ts` → **15 pass / 0 fail** (twice consecutively).
- tsgo 0 errors; lint/biome clean on all touched files.

## Gotcha recorded

Full-repo lint of the documents file can replay STALE graphql-eslint results cached before
codegen regenerated `frontend/graphql/generated/schema.graphql`; a re-run after the schema write
(flushing the entry) reports clean. The canonical file-scoped service run is the source of truth.
