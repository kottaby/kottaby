# Task 2.1 — Trigger Amendment Migration Outcome

**Date:** 2026-09-12
**Executor:** coder subagent (agent-0), orchestrated by spec-implementation

## Summary

Authored the withdrawal-settlement trigger amendment mirroring the `4-student-payments-status-transition`
precedent, registered the SQLite parity variant in the custom-migration bundler's EXCLUDED_FILES,
fixed the stale schema docblocks, and formally verified the duplicate `custom_4-…` drizzle dirs
(**resolves ledger D1**).

## Files Created

| File | Content |
|---|---|
| `backend/db/migration/5-teacher-transaction-settlement.sql` | `CREATE OR REPLACE FUNCTION prevent_teacher_transaction_update()` — permits ONLY `OLD.status='pending' AND NEW.type='withdrawal' AND NEW.status IN ('completed','failed')` with all columns frozen via `IS NOT DISTINCT FROM` except `updated_at` (legitimately moves via `$onUpdate`; the student_payments precedent has no such column, hence the single deviation). Full Purpose/Mechanics/Idempotency/Dialect header, no plan-artifact references. |
| `backend/db/migration/5-teacher-transaction-settlement-sqlite.sql` | SQLite parity: `DROP TRIGGER IF EXISTS prevent_teacher_transaction_update_trigger` + `CREATE TRIGGER BEFORE UPDATE ON teacher_transaction` with NULL-safe `IS` comparisons and `RAISE(ABORT, …)`. Pure SQLite (no plpgsql / CREATE FUNCTION). |

## Files Modified

| File | Change |
|---|---|
| `backend/db/scripts/applyCustomMigrations.ts` | Added `"5-teacher-transaction-settlement-sqlite.sql"` to `EXCLUDED_FILES` (after the 4-student-payments sqlite entry) — prevents the bundler from shipping SQLite syntax into the PG pipeline (which would abort `bun db migrate`). |
| `backend/db/schema/billing/teacher-transaction.ts` | Docblock-only: IMMUTABLE paragraph now describes the settlement exception; stale "balance updated atomically by the same trigger" sentence replaced with the guarded-repo-update reality (verified against `backend/db/repo/billing/wallet.repository.ts`). |
| `backend/db/schema/billing/wallet.ts` | Docblock-only: same stale-trigger-claim fix; now describes `WalletRepository` guarded UPDATEs (credit/debit in the same transaction as the ledger insert, insufficient-funds guard + `>= 0` CHECK as backstop). |

## D1 Resolution (duplicate drizzle dirs)

Formal comparison of `backend/drizzle/20260907182426_custom_4-student-payments-status-transition/`
vs `backend/drizzle/20260908103411_custom_4-student-payments-status-transition/`:

- `migration.sql`: comment-only drift (lines 51-54 prose differ); with comments stripped,
  `diff` confirms the SQL payloads are **IDENTICAL**.
- Both dirs contain only `migration.sql` (no snapshot divergence).
- Both recorded in `backend/drizzle/.custom-migrations.json` `appliedFolders`.
- Fresh-DB proof: both applied cleanly to `kottaby_test` during environment bootstrap
  (`0-baseline-outcome.md`), and again in the Task 2.1 migration run — the pair is
  idempotency-safe (the PG function uses `CREATE OR REPLACE`).
- **D1 → ✅ Done** (ledger updated).

## Migration Application

- Applied via the custom-migration path: `bun --env-file=.env.test run backend/db/scripts/migrate.ts`.
- New drizzle folder created: `backend/drizzle/20260912091422_custom_5-teacher-transaction-settlement/`
  (naming convention confirmed). The registered sqlite variant was correctly EXCLUDED from the PG
  pipeline (bundler reported it in "Updated 1 migration file(s)" idempotency pass only).
- **Not** `db push` — correct path per plan (trigger amendments are custom SQL).

## Verification Results

### Static

- Lint service (with `.env.test`): `success: true, exitCode: 0` on all three edited `.ts` files.
- tsgo: exit 0, zero errors (baseline 0).

### Functional trigger tests (psql on `kottaby_test`, inside `BEGIN … ROLLBACK` — zero rows left)

| Case | Result |
|---|---|
| Settle pending withdrawal → `completed`, all else unchanged | ✅ SUCCEEDS (the only permitted update) |
| Settle second pending withdrawal → `failed` | ✅ SUCCEEDS (both target states) |
| Re-touch the settled row | ✅ RAISES |
| Settle with changed `amount` | ✅ RAISES |
| Update an earning row toward withdrawal/completed | ✅ RAISES |
| Update an earning row, same type/status (no-op style) | ✅ RAISES |
| Settle to `status='paid'` | ✅ RAISES (note: `'paid'` is not a `transaction_status` member — enum-level error fires before the guard; the guard's whitelist independently blocks it) |
| DELETE the row | ✅ RAISES (delete guard untouched) |

### Idempotency

- PG file applied twice to `kottaby_test` (direct psql + migration runner) — `CREATE OR REPLACE`
  both times, safe on already-migrated DBs.

## Trigger wiring (verified)

- Base trigger `prevent_teacher_transaction_update_trigger` (from `3-immutability-triggers.sql`)
  executes exactly `prevent_teacher_transaction_update()` — replacement-function-only approach
  re-arms the guard in place, matching the student_payments precedent. No trigger DDL needed in the
  amendment.

## Cross-File Dependencies Discovered

- None. The docblock fixes were same-file. The `EXCLUDED_FILES` registration is self-contained.

## Carry-Forward Knowledge

- `updated_at` on `teacher_transaction` moves via Drizzle `$onUpdate` on every guarded settlement —
  any future column freeze must keep excluding it.
- `transaction_status` enum members are exactly `pending | completed | failed`; `paid` belongs to
  `student_payments` only.
- Sandbox quirk: run tsgo as `bun --env-file=.env.test run tsgo` (see `0-baseline-outcome.md`).
