# Task 4.2 — Locale: `adminFinance` Namespace Outcome

**Date:** 2026-09-12
**Executor:** coder subagent (agent-4), orchestrated by spec-implementation

## Summary

Added the `adminFinance` namespace across all 5 touchpoints (+ both `messages.ts` trees), the 4 new
error keys (en + ar + type labels), the dashboard `finances` nav label, and the parity test — all
under `shared/` only.

## Files Created

| File | Content |
|---|---|
| `shared/locale/types/adminFinance/index.ts` | `AdminFinanceLabels` — meta/title/subtitle, 3 tab labels, payments filter labels + table headers + `paymentsResultCount(count)`, withdrawal queue headers + `pendingWithdrawalsCount(count)` + approve/reject + reject-dialog copy, wallet inspector (teacher picker, balance, totalEarnings, type/description headers), adjustment dialog (credit/debit, amount, reason, submit), loading/error/forbidden/empty states; pluralization as `(count: number) => string`; mirrors the analytics labels docblock style |
| `shared/locale/en/adminFinance/index.ts` | `adminFinanceEn` typed against `AdminFinanceLabels` |
| `shared/locale/ar/adminFinance/index.ts` | `adminFinanceAr` — real Arabic script, exact key parity |
| `shared/locale/namespaces/adminFinance/adminFinance.namespace.ts` | `defineNamespace<AdminFinanceLabels>("adminFinance.adminFinance", t => t.adminFinanceTranslations)` mirroring the wallet namespace |
| `shared/locale/namespaces/adminFinance/index.ts` | barrel re-export |
| `shared/locale/adminFinance-namespace.parity.test.ts` | mirrors the wallet parity test: errors-registry pin (4 new keys + `insufficientBalance` reuse, both locales, Arabic-script probe), parity belt (identical key sets, non-empty, 1-arg arity), registry wiring, sync resolution via `getTranslations`, fallback behavior; pure unit tier |

## Files Modified

| File | Change |
|---|---|
| `shared/locale/namespaces/registry.ts` + `shared/locale/namespaces/index.ts` | `AdminFinance` import + entry (alphabetical) |
| `shared/locale/types/message.ts` | `adminFinanceTranslations: AdminFinanceLabels` (next to `adminSessionGovernanceTranslations`) |
| `shared/locale/en/messages.ts` + `shared/locale/ar/messages.ts` | import + `adminFinanceTranslations` entries |
| `shared/locale/types/errors/labels.ts`, `shared/locale/en/errors/index.ts`, `shared/locale/ar/errors/index.ts` | the 4 new error keys (`withdrawalRequestNotFound`, `withdrawalNotPending`, `invalidAdjustmentAmount`, `adjustmentReasonRequired`) with sibling-style docblocks; `insufficientBalance` reused unchanged |
| `shared/locale/types/dashboard/index.ts`, `shared/locale/en/dashboard/index.ts`, `shared/locale/ar/dashboard/index.ts` | `finances` nav label (`"Finances"` / `"المالية"`) between `audit` and `analytics` |

## Verification Results

- Lint (17 touched files, `--id task42`): 0 problems, exit 0 (first run flagged 4
  `sonarjs/no-nested-conditional` on the count pluralizers — fixed by converting ternary chains to
  early returns, matching the dashboard `students.ts` idiom).
- tsgo: 0 errors (one transient TS6133 reported mid-run was the parallel Task 2.3 agent's
  in-flight edit, not this task's; forced full re-check: 0 errors; detection sanity-probed by
  temporarily renaming a type key → 5 errors, then restored byte-identical).
- `adminFinance-namespace.parity.test.ts`: **69 pass / 0 fail** (252 expect calls).
- `errors-namespace.parity.test.ts`: **21 pass / 0 fail** (stays green).
- `wallet-namespace.parity.test.ts` regression: 35 pass / 0 fail.

## Files NOT Modified (and why)

- `app/` / `frontend/` — Task 4.3 (console UI + nav wiring) consumes these labels.
- `backend/**` — resolver error keys are consumed via `getServerTranslations(locale)`; no backend
  changes belong to this task.
- `ai/plans/**` — orchestrator-owned (checkboxes/outcomes).

## Semantic Review (checklist)

- [x] Zero hardcoded strings touched (all copy through the typed namespace).
- [x] en/ar key parity exact (compile-typed both leaves + runtime belt).
- [x] Alias discipline: all imports inside `shared/locale/**` are `@/shared/locale/...`.
- [x] No plan-artifact references.
- [x] No cross-layer imports (shared never imports frontend/backend/app).

## Carry-Forward Knowledge

- Consumers import the handle exactly like existing admin namespaces:
  `import { AdminFinance } from "@/shared/locale/namespaces/adminFinance"` →
  `useAppTranslation(AdminFinance)` (same pattern as `AdminUsers`/`Analytics`, grep-verified).
- Server-side consumers: `getServerTranslations(locale)` → `t.adminFinanceTranslations.*`;
  resolvers: `ctx.t("errorsTranslations")` for the new error keys.
