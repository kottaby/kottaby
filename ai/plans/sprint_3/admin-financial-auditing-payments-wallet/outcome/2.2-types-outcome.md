# Task 2.2 — Canonical Types Outcome

**Date:** 2026-09-12
**Executor:** coder subagent (agent-1), orchestrated by spec-implementation

## Summary

Created `backend/types/billing/admin-finance.types.ts` (all 10 interfaces from plan.md), the
`WalletAdjustmentDirection` TS enum, and wired both through their domain barrels.

## Files Created

| File | Content |
|---|---|
| `backend/types/billing/admin-finance.types.ts` | `NormalizedAdminPaymentFilters` (with `studentNameSearch` — the escaped + `%..%`-wrapped pattern the repo binds to `ilike`), `AdminStudentPaymentRow` (extends `StudentPaymentSelectType` + resolved student name/email), `AdminStudentPaymentPageReturnType`, `AdminWalletTransactionFilters`, `AdminTeacherWalletReturnType` (null-pair `balance`/`totalEarning` = honest no-wallet state; flat fields, NOT the capped `Wallet` view), `AdminWithdrawalQueueRow`, `AdminWithdrawalQueuePageReturnType`, `WithdrawalSettlementProbe`, `AdminTeacherWalletProbe`, `AdminWalletAdjustmentSubmitInput` |
| `backend/enum/billing/wallet-adjustment-direction.enum.ts` | `WalletAdjustmentDirection { Credit = "credit", Debit = "debit" }` — service-layer vocabulary only (NOT a pgEnum, NOT persisted as a column) |

## Files Modified

| File | Change |
|---|---|
| `backend/types/billing/index.ts` | `export * from "./admin-finance.types";` |
| `backend/enum/billing/index.ts` | `export * from "./wallet-adjustment-direction.enum";` |

## Import Sources

All base types reused from the existing billing types (no `$inferSelect` re-derivation):
`StudentPaymentSelectType`, `TeacherTransactionSelectType`, `WalletSelectType`;
`PaymentStatus`, `TransactionType`, `TransactionStatus`, `PaymentGateway` from their enum files.

## Verification Results

- Lint service (`--id task22`, 4 files): success, exit 0, zero findings.
- tsgo: exit 0, zero errors (baseline 0 → zero new).

## Files NOT Modified (and why)

- `backend/graphql/pothos/shared/enum.pothos.ts` — the Pothos enum registration belongs to the
  GraphQL task (3.1).
- Schema/repo/service files — types are pure; the repo primitives are Task 2.3.

## Type-Name Collisions

None — all 11 new names existed only in plan artifacts before this change.

## Semantic Review (checklist)

- [x] No cross-layer imports (types file imports only from `backend/types/billing/*` + `backend/enum/billing/*`).
- [x] No duplicate/derivative type definitions of existing shapes (verified: no Pothos/service/repo usage yet).
- [x] No client data widened (all server-side admin shapes).
- [x] Enum value imports: `WalletAdjustmentDirection` used in type position only (import type);
  the enum file itself defines values.
- [x] Clean comments: no plan-artifact references (REQ-n / Task IDs absent).
- [x] No deferred items created.

## Carry-Forward Knowledge

- `AdminTeacherWalletReturnType` deliberately does NOT embed the `Wallet` object — that object is
  bound to the teacher-capped `WalletViewType` (plan D-5 forbids reusing it for admin inspection).
- `NormalizedAdminPaymentFilters.studentNameSearch` carries the service-escaped, `%..%`-wrapped
  pattern; the repo binds it directly to `ilike` without re-escaping.
- `WithdrawalSettlementProbe` / `AdminTeacherWalletProbe` are read-probe shapes for human-readable
  error disambiguation — never trusted for the write decision.
