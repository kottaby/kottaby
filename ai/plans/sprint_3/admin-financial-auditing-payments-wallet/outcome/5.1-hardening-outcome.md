# Task 5.1 — Hardening & Adversarial Wave Outcome

**Date:** 2026-09-13
**Executor:** coder subagent (agent-22), orchestrated by spec-implementation

## Journey green + adversarial coverage matrix

The billing journey and service suites cover the full penetration matrix; two genuinely-missing
probes were added in this wave:

| Probe | Tier | Status |
|---|---|---|
| teacher→approve 403, student→adjust 403, anonymous 401 | journey + integration | present |
| settle non-pending / unknown ids | service + integration | present |
| amount fuzz (zero/negative/3dp/overflow/non-numeric) | service validation matrix | present |
| reason fuzz (empty/whitespace/501-char) | service validation matrix | present |
| wildcard `%`/`_`/`\` search injection | repo tests | present |
| double-settle race (real parallel `Promise.allSettled`) | journey | present |
| insufficient-funds debit | service + journey | present |
| **unicode (Arabic/CJK) reason fuzz** — stored only on the ledger description; audit details carry `reasonPresent` only, no raw code points | service (new) | **added** |
| **concurrent settle + new-request race on the same wallet** — exactly one settle, new request pends with its own reserved debit, exact balance arithmetic | journey (new, committed-tx tier) | **added** |

## Verification

- `admin-financial-auditing.service.test.ts` → **19 pass / 0 fail** (was 18), two consecutive runs.
- `admin-financial-auditing.journey.test.ts` → **7 pass / 0 fail** (was 6), two consecutive runs — idempotent-teardown proof.
- tsgo 0 errors; lint + biome clean on both files.
- Full workflows slice (`bun run test/scripts/run-test.ts test/workflows`) — all workflows journeys
  green EXCEPT `session-report-homework.journey.test.ts` (5 pass / 9 fail), proven **pre-existing**:
  identical failure signature reproduced on a detached worktree at the base commit `7afd194`
  (its expectations predate the #88 completion-prompt wave; no plan file touches the classes or
  notifications domains). Fix belongs to the classes-domain journey owner, not this plan.

## Files changed

- `backend/services/billing/admin-financial-auditing.service.test.ts` — unicode fuzz block.
- `test/workflows/billing/admin-financial-auditing.journey.test.ts` — `step 7` concurrency probe.
