# Task 6.2 — Knowledge Propagation Outcome

**Date:** 2026-09-13
**Executor:** coder subagent (agent-23), orchestrated by spec-implementation

## Deliverables

- **Created** `docs/billing/admin-financial-auditing.md` (290 lines) covering: settlement model
  D-1 (debit-on-request; approve = settle-only; reject = settle-to-failed + restore), trigger
  amendment pattern D-2 (`custom_5-teacher-transaction-settlement`, frozen columns, `updated_at`
  via `$onUpdate`), adjustment vocabulary D-3 (directions, ledger row shapes, description markers),
  audit mapping D-4 (Override/Adjust, exact details vocabulary, `reasonPresent` boolean, parseable
  2000-char cap via `serializeAuditDetails`), admin pagination contract D-5 (page bounds, snapshot
  reads, flat `AdminTeacherWallet`, `keyFields: false` policies), trigger-freeze + concurrency
  guarantees, and the durable sandbox/testing lessons (Bun `import { type X }` instanceof pitfall,
  happy-dom dialog testing patterns, `cache-and-network` mock duplication, port-3066 kill ritual).
- **Updated** `docs/specs/state-machine-invariants.md` — the pre-existing INV-W5 item now carries
  an "implemented" note referencing the guarded `settleWithdrawalOnce` writer and the amended
  trigger (single-line edit, styled after the sibling INV-PAY2 note).

Every claim was cross-checked against the shipped sources (service, helpers, both repos, migration
SQL, GraphQL resolvers, apollo cache config). No AGENTS.md or instruction files touched; no
plan-artifact references in the doc.
