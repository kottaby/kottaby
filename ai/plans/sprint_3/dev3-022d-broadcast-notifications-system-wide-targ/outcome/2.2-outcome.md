# Task 2.2 — Shared Admin Gate (`assertActorAdmin`) — RE-SCOPED EXECUTION

**Plan:** `ai/plans/sprint_3/dev3-022d-broadcast-notifications-system-wide-targ`
**Task:** 2.2 (Phase 2) — original scope: "Extract shared `assertActorAdmin` + refactor `AdminUserManagementService`".
**Agent:** Implementation Executor (Phase 2 — shared admin gate, re-scoped per plan-review F-1).
**Result:** **COMPLETE — re-scoped to verification + focused-gap test. The shared gate already existed; NO fork was created; one focused test file ADDED; all regression suites green (118 pass / 0 fail); zero production files touched.**

---

## 1. Re-Scope Decision (cite: plan-review-R1.md finding F-1, BLOCKING)

The plan's task 2.2 (tasks.md:114-117) premised a VERBATIM extraction of `assertActorAdmin` from
`backend/services/admin/user-management.service.ts:240-271`, followed by "delete the private copy" and a barrel
export. **That premise is stale**:

- `outcome/plan-review-R1.md` §F-1 (verdict PASS-with-notes, blocking list = [F-1]): *"the extraction is ALREADY
  DONE, in a differently-named file"* — `backend/services/admin/admin-gate.helpers.ts:59` exports the gate, it is
  barrel-exported, and `user-management.service.ts` imports it (`:65`) with 7 call sites; the `:240-271` span is
  `createUser`, which CALLS the gate at `:271`. Executing 2.2 verbatim would **fork the admin gate — forbidden**
  (plan's own D8 "ONE canonical admin-actor gate"; `backend/services/AGENTS.md` duplication rules;
  `backend/services/AGENTS.md:10` already documents `admin-gate.helpers.ts` as the shared gate).
- `outcome/0.2-prerequisites-outcome.md` row 9 + Location-Drift Advisory #2 independently verified the same
  corrected anchor.

**Re-scoped execution (verbatim intent → current reality):**
| Original step | Re-scoped execution | Outcome |
|---|---|---|
| CREATE `backend/services/admin/assert-actor-admin.ts` (extract logic) | **NOT PERFORMED** — would fork the gate. Helper verified in place at `admin-gate.helpers.ts:59` | ✅ verified, untouched |
| UPDATE `user-management.service.ts` — import helper, delete private copy | **NOT PERFORMED** — no private copy exists; import already present at `:65` | ✅ verified, untouched |
| UPDATE `backend/services/admin/index.ts` — export the helper | **NOT PERFORMED** — barrel export already present at `index.ts:9` (`export * from "./admin-gate.helpers";`) | ✅ verified, untouched |
| Run ALL existing user-management suites — behavior-identical | **PERFORMED** (4 suites + journey, 118 pass / 0 fail — §3) | ✅ zero expectation diffs |
| Add focused unit test IF the three branches aren't all pinned | **PERFORMED** — the "actor row missing" branch was NOT pinned anywhere (§2); `backend/services/admin/admin-gate.helpers.test.ts` CREATED (6 tests) | ✅ gap closed |

Files touched: **exactly one** — `backend/services/admin/admin-gate.helpers.test.ts` (NEW, test-only).
Zero production files modified; zero schema files touched (`git diff -- backend/db/schema/ backend/db/migration/` empty).

---

## 2. Contract Verification Table (plan §4.3 / REQ-030 D8 / REQ-002)

| # | Contract element (REQ-030 D8 + plan §4.3 step 1) | Evidence (path:line) | Verdict |
|---|---|---|---|
| 1 | Single canonical gate — no forked copies | `rg "function assertActorAdmin"` → exactly ONE definition: `backend/services/admin/admin-gate.helpers.ts:59`; `ANONYMOUS_ACTOR_ID` sentinel in exactly ONE non-test backend file (`admin-gate.helpers.ts:18`) | ✅ |
| 2 | Signature `assertActorAdmin(actorId: number, locale: string, outerTx?: DBTransaction): Promise<void>` | `admin-gate.helpers.ts:59` | ✅ |
| 3 | Anonymous `actorId = 0` → `UnauthorizedError` | `admin-gate.helpers.ts:18` (sentinel) + `:62-69` (throw `UnauthorizedError(tErrors.unauthorized)`) | ✅ |
| 4 | Missing actor row → `ForbiddenError` | `admin-gate.helpers.ts:71-79` (`UserRepository.findById` → null → throw `ForbiddenError(tErrors.forbidden)`) | ✅ |
| 5 | Non-admin row → `ForbiddenError` | `admin-gate.helpers.ts:81-89` (`toUserRole(actor.role) !== UserRole.Admin` → throw `ForbiddenError(tErrors.forbidden)`); fail-closed role coercion `toUserRole` | ✅ |
| 6 | Localized denials (`getServerTranslations(locale).errorsTranslations.unauthorized / forbidden`) | `admin-gate.helpers.ts:15,60` (`@/shared/locale/server-graphql`); messages pinned by suites | ✅ |
| 7 | Identical logging shape — one bounded `logger.logDomainError` per denial, metadata only (`{ code, entity, entityId }`) | `admin-gate.helpers.ts:63-67` (UNAUTHORIZED/anonymous), `:73-77` (FORBIDDEN/row missing), `:83-87` (FORBIDDEN/not admin); exact ctx pinned by `audit-trail.service.test.ts:182,209` and by the NEW suite | ✅ |
| 8 | Pre-transaction (actor check BEFORE any tx opens; no tx created by the gate) | `admin-gate.helpers.ts:46-58` JSDoc + code: the gate only READS via `UserRepository.findById(actorId, outerTx)`; it never opens a transaction; anonymous branch returns before any DB contact (read-spy pin: NEW suite test 2) | ✅ |
| 9 | Zero writes / zero audit rows on denial (JR-C-1) | `admin-gate.helpers.ts:50-52` JSDoc; no `AuditService` import in the module; zero-write oracles in chaos suite (`user-management.chaos.test.ts:432,465`), service suite, audit-trail suite, NEW suite (`snapshotCounts` on `users` + `audit_logs`) | ✅ |
| 10 | Re-check against the LIVE `users` row — not a cached claim | `admin-gate.helpers.ts:71` → `UserRepository.findById` (`backend/db/repo/users/user.repository.ts:75`, tx-scoped SELECT on every call); promote/demote flip test in NEW suite (test 6) proves read-through semantics | ✅ |
| 11 | Denials carry `extensions.code` UNAUTHORIZED / FORBIDDEN only | `backend/lib/errors.ts:25` (`DomainError` sets `extensions.code`), `:44` (`UnauthorizedError`→`UNAUTHORIZED`), `:51` (`ForbiddenError`→`FORBIDDEN`); `.code` asserted in NEW suite (tests 2-4) | ✅ |
| 12 | Barrel export — `backend/services/admin/index.ts` re-exports the helper | `backend/services/admin/index.ts:9` (`export * from "./admin-gate.helpers";`); root `backend/services/index.ts:4` re-exports `./admin`; same-function-identity pinned by NEW suite test 1 | ✅ |
| 13 | Import direction correct (services consume helper; helper imports only repo/enum/errors/logger/types/shared) | `admin-gate.helpers.ts:9-15` (no service imports → no cycle); consumers: `user-management.service.ts:65` + 7 calls (`:118,157,193,231,271,342,395`), `audit-trail.service.ts:48` + call `:264` — all deep imports of the helper | ✅ |
| 14 | REQ-002 reuse (no reimplementation) | `assertActorAdmin` reused as-is; REQ-002's stale anchor (`user-management.service.ts:240-271`) superseded by `admin-gate.helpers.ts:59` per 0.2 advisory + plan-review N-1 | ✅ |

### Adjudicated nuance (carried from plan-review F-1 — NOT a gap)

Plan §4.3 (plan.md:232) describes the gate's log as `{ code, entity: "notifications", entityId: actorId, locale }`.
The SHIPPED gate logs `entity: "user"` (no `locale` key) — `admin-gate.helpers.ts:65,75,85`. Per plan-review F-1
("Contract nuance to carry into task 2.4"): **the gate keeps its behavior-identical shape (`entity: "user"`);
REQ-034's `entity: "notifications" | "plans"` context applies to the broadcast service's OWN `logDomainError`
calls (validation/cohort guards), never to the reused gate.** Task 2.4 must not "fix" the gate's log context.

### Oracle-hygiene note (2.2.SEC)

The gate discloses nothing beyond the two documented codes: anonymous → `UNAUTHORIZED`, missing-or-non-admin →
`FORBIDDEN`. The missing-row and non-admin branches are deliberately INDISTINGUISHABLE at the error channel
(same code, same localized message, same FORBIDDEN log code) — only the internal log MESSAGE differs
("actor row missing" vs "actor is not admin"), which is server-side only. No oracle about which `users` rows
exist is exposed beyond admin-gated knowledge.

---

## 3. 2.2.TE — Test Engineering (mandated runner; NEVER raw `bun test`)

| Suite | Path | Result | Note |
|---|---|---|---|
| **NEW** gate unit suite | `backend/services/admin/admin-gate.helpers.test.ts` | **6 pass / 0 fail / 34 expects** | Pins: barrel identity; anonymous (pre-DB, read-spy = 0 calls); **actor row missing** (previously unpinned — exactly ONE tx-scoped `findById` read, caller-tx identity asserted); non-admin; admin pass (zero logs/zero writes); LIVE-row promote/demote flip |
| user-management.service | `backend/services/admin/user-management.service.test.ts` | **61 pass / 0 fail / 236 expects** | unchanged — behavior-identical |
| user-management.chaos (the "8 chaos tests") | `backend/services/admin/user-management.chaos.test.ts` | **8 pass / 0 fail / 46 expects** | unchanged — BFLA probes (f) green |
| audit-trail.service (gate consumer) | `backend/services/admin/audit-trail.service.test.ts` | **35 pass / 0 fail / 190 expects** | unchanged — gate denials with exact log-ctx pins green |
| admin-user-denials journey (gate consumer) | `test/workflows/admin/admin-user-denials.journey.test.ts` | **8 pass / 0 fail / 97 expects** | unchanged — Journey C1/C2 real-auth denials green |
| **Total** | | **118 pass / 0 fail** | zero expectation diffs vs pre-task behavior |
| `bun tsgo` full-repo | — | **0 errors** | delta vs 0.1 baseline = 0 |

**Why the NEW suite was warranted (check-first result):** the anonymous and non-admin branches were already pinned
through calling services — `user-management.service.test.ts:167,214,429,589` (anonymous) and `:184,200,443,606`
(non-admin); `user-management.chaos.test.ts:432` (anonymous) / `:465` (non-admin);
`audit-trail.service.test.ts:165,192` (with exact log-ctx pins); journey C1 `:382` / C2 `:439`. The **"actor row
missing" branch was pinned NOWHERE** — every `absentUserId()` usage in the service suite (`:566,702,1000,1207`)
passes the absent id as the TARGET with an admin actor (→ `USER_NOT_FOUND`), and the journey fixtures
deliberately avoid the branch (file-scope cast comment, `admin-user-denials.journey.test.ts:220-227`). Per task
2.2.TE's conditional ("Add a focused unit test … if the existing suites don't pin all three branches"), the
focused suite was added **directly against the shared helper** — which is also the exact object task 2.4 will
consume.

**Conventions followed:** `runInRollback` everywhere; caller `tx` passed to the gate; `expectRepoError`
try/catch (NEVER `.rejects.toThrow()`); tracked `spyOn` registry + file-level `afterEach` restore (bun reuses one
mock per object+method pair); `silenceDomainLog`; messages pinned via `getServerTranslations("en").errorsTranslations`;
zero-write oracles (`users` + `audit_logs` counts); call-through repo spy (no behavior substitution).

---

## 4. Exact Import Surface for Task 2.4

`AdminBroadcastService.broadcast` step 1 (plan §4.1) MUST consume THIS helper. Canonical line:

```ts
import { assertActorAdmin } from "@/backend/services/admin/admin-gate.helpers";
```

- This is the plan-review F-1 mandated surface, and the exact precedent set by both existing consumers
  (`user-management.service.ts:65`, `audit-trail.service.ts:48`) — deep import keeps the graph minimal
  (`admin-gate.helpers.ts:9-15` imports only repo/enum/errors/logger/types/shared → zero service-to-service cycle)
  and matches the cross-domain import convention (no production service imports a services barrel — verified by grep).
- The admin barrel ALSO works (`import { assertActorAdmin } from "@/backend/services/admin";`) — same function
  identity, pinned by the NEW suite's test 1. Prefer the deep import for 2.4.
- Do NOT import via the root `@/backend/services` barrel from inside `backend/services/notifications/` — it
  re-exports every domain service and would create a self-cycle through `./notifications` once the broadcast
  service joins it.
- Call shape for 2.4 step 1 (pre-tx): `await assertActorAdmin(actorId, locale, outerTx);` — anonymous →
  `UnauthorizedError` (UNAUTHORIZED); missing/non-admin → `ForbiddenError` (FORBIDDEN); both pre-transaction with
  zero writes/audit (JR-C-1); gate logs `entity: "user"` (do not "fix" it — see §2 nuance).

---

## 5. Pipeline Record

| Subtask | Result | Evidence |
|---|---|---|
| 2.2.QL Quality Loop | ✅ exit 0 | `sub-loop.ts backend/services/admin/admin-gate.helpers.test.ts --lifecycle duplicates` → tsgo/oxlint/biome:check/lint:type-aware/duplicates ALL ✅ (first pass, zero fix iterations). Verification evidence run on the untouched `admin-gate.helpers.ts` also exit 0 (all stages ✅) |
| 2.2.TE Test Engineering | ✅ | §3 — 118 pass / 0 fail + NEW suite 6/0; tsgo 0 errors |
| 2.2.SEC Security & Tenancy Audit | ✅ | Re-check reads the LIVE `users` row via `UserRepository.findById` on EVERY call (`admin-gate.helpers.ts:71`; promote/demote flip test). Denials carry `extensions.code` UNAUTHORIZED/FORBIDDEN only (`errors.ts:25,44,51`). No oracle about which rows exist beyond admin-gated knowledge (missing vs non-admin indistinguishable at the error channel). Zero writes/audit on denial (JR-C-1). `passwordHash` never read/logged/returned (`admin-gate.helpers.ts:54-57` discipline) |
| 2.2.SR Semantic Review | ✅ | Single canonical admin gate — `rg "function assertActorAdmin"` = 1 definition; `ANONYMOUS_ACTOR_ID` in 1 non-test file; no `assert-actor-admin.ts` fork file exists; import direction correct (helper imports no services; both consumers deep-import the helper); barrel re-export verified at runtime (same-function identity test) |
| 2.2.IV Instruction Verification | ✅ | `.agents/instructions/backend.instructions.md`: barrel conventions (`export *`, relative `./`, existing barrels unmodified), DomainError discipline, `logger` (no `console.*`), i18n via `getServerTranslations`, `runInRollback` DB tests. `backend/services/AGENTS.md`: `:10` documents `admin-gate.helpers.ts` as the shared gate (consumed, not duplicated); `:9` JR-C-1 denial-no-audit rule honored; service tests live next to code + runInRollback/expectRepoError conventions (`:28-30,12-13`) |

## 6. Environment / Handoff Notes

- **Branch drift (orchestrator attention):** the tree was found checked out on `main` @ `b020816` ("broadcast
  foundations" — the Phase 1 commit), while `feat/dev3-022d-broadcast-notifications` still sits at `bbcb363`.
  NO checkout was performed: the working tree carries the committed Phase 1 work and switching would rewind it
  out from under any parallel Phase 2 task. This task is verification + one new test file and is branch-agnostic
  (the gate exists identically on both commits). Orchestrator should reconcile branch topology before committing.
- **No commits made** (per protocol). `git status` after task: `?? backend/services/admin/admin-gate.helpers.test.ts`
  (NEW) + pre-existing untracked `.issue42.json` (not mine).
- Carry-forward for 2.4: the gate's `entity: "user"` log shape is behavior-locked by `audit-trail.service.test.ts`
  and the NEW suite — broadcast-service logging (REQ-034) uses `entity: "notifications" | "plans"` on its OWN calls.
