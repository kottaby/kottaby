# 6.1 Review — R9: DB Layer Deep-Dive (repository × schema interplay)

**Task ID:** 6.1-R9 · **Mode:** READ-ONLY · **Plan:** DEV3-022d broadcast notifications
**Method:** full read of `broadcast-audience.repository.ts` (tx + raw branches), `notifications/index.ts` barrel, `schema/{audit,users,billing,notifications}/*.ts`, `migration/3-immutability-triggers.sql` + `2-functions.sql`, `audit.service.ts`, plus the two interplay counterparts (`admin-user-query-helpers.ts:134-143`, `admin-broadcast.service.ts`) and the repo test file. Every check answered from traced code, not docs. Prior-wave findings (R1b MEDIUM-1 cohort drift, R2 LOW body/receipt amplification, R3 MEDIUM batch amplification, R5 LOW enum-member pin) treated as known and not re-reported.

**Schema-drift gate (REQ-044) re-verified:** `git log -- backend/db/schema backend/db/migration` shows the last touches are pre-plan commits (`7449297` and earlier); no DEV3-022d commit modified schema or migrations. The plan added NO schema, as mandated.

---

## Check 1 — Governance predicate: tx branch vs raw branch — AGREE (semantic identity)

Side by side (broadcast-audience.repository.ts):

| Raw branch (`GOVERNANCE_PREDICATE_SQL`, :81, unaliased; `u.`-prefixed inline at :211-212) | Tx branch (`governanceConditions()`, :89-94) |
|---|---|
| `coalesce(is_deleted, false) = false` | `or(eq(users.isDeleted, false), isNull(users.isDeleted))` |
| `coalesce(is_blocked, false) = false` | `or(eq(users.isBlocked, false), isNull(users.isBlocked))` |

Three-valued logic walk: column `true` → raw `coalesce(true,false)=false` ⇔ false; Drizzle `true=false`⇔false ∧ `isNull`⇔false → excluded both ways. Column `false` → raw true; Drizzle `false=false`⇔true → included. Column `NULL` → raw `coalesce(NULL,false)=false` ⇔ true; Drizzle `NULL=false`⇔NULL, `OR isNull`⇔true → included (legacy NULL reads eligible). Identical outcome for all three states — the doc comment's claim (:84-87) is accurate.

**`suspended`:** NEITHER branch constrains it. The check text's "AND NOT suspended?" hypothesis is FALSE in both branches — deliberately, per REQ-015 and the file header (:30-32): suspension gates session requests, never inbox reads. Both branches agree with each other, with plan §4.4, and with REQ-015. Note the admin directory's `Active` governance filter (`admin-user-query-helpers.ts:77-81`) DOES exclude suspended — the broadcast cohort intentionally differs from the directory's "active" definition, and the divergence is documented in the repo header. Consistent, not a defect.

## Check 2 — Ordering + dedup — PASS

- `ORDER BY id ASC` (:110, :121, :132, :152, and every raw shape :175/:184/:194/:213) in BOTH branches. Why ASC: (a) the engine's claim key is `sha256(sortedIds:type:key)` (plan §4.6) — deterministic resolution ⇒ stable digest across retries/branches; (b) deterministic chunking for the ledgered D1 mega-broadcast future; (c) tests pin exact sequences (`toSorted` equality, repo test :190, :230, :264, :482).
- **DISTINCT is applied exactly where the fan-out exists and nowhere else, consistently across branches:** plan shape only (`selectDistinct` :140 / `SELECT DISTINCT u.id` :204) because the `subscriptions` join fans out per subscription row (Tier-3 test :339-358 pins the 2-subscription user resolving once). `all`/`role`/`country` project from `users` alone — `users.id` is the PK (`users.ts:14`), single row per id, so DISTINCT would be a no-op and is correctly omitted in both branches. No inconsistency found.
- Raw plan shape's `ORDER BY u.id ASC` orders the DISTINCT-projected column — valid under PG's `SELECT DISTINCT` + `ORDER BY` rule.

## Check 3 — Active-window plan predicate vs canonical — AGREE (byte-equivalent modulo table alias)

Canonical (`studentHasActiveSubscriptionSubquery`, admin-user-query-helpers.ts:139-141) vs broadcast repo:

| Canonical | Broadcast raw (:208-210) | Broadcast tx (:146-148) |
|---|---|---|
| `"subscriptions"."status" = 'active'` | `s.status = 'active'` | `eq(subscriptions.status, SubscriptionStatus.Active)` → `"subscriptions"."status" = $1` with `'active'` bound |
| `now() >= coalesce("subscriptions"."start_date", now())` | `now() >= coalesce(s.start_date, now())` | `sql` template, identical text |
| `("subscriptions"."end_date" IS NULL OR now() < "subscriptions"."end_date")` | `(s.end_date IS NULL OR now() < s.end_date)` | `or(isNull(endDate), sql`now() < endDate`)`, identical text |

The only textual deltas are the `s.` alias (sanctioned by plan §4.4's own canonical shape) and the tx branch binding `'active'` as a parameter instead of an inline literal (same value, same semantics — `SubscriptionStatus.Active = "active"` verified against the pgEnum `subscription_status`, enums.ts:46-52). Boundary conditions: `now() = start_date` → `>=` non-strict → **included**; `now() = end_date` → `<` strict → **excluded**; `start_date IS NULL` → coalesces to now() → included; `end_date IS NULL` → included — identical in all three forms. The Tier-2 edge test (repo test :293-317) pins both boundary edges deterministically on the transaction clock. The broadcast adds `plan_id = $1` and JOIN+DISTINCT where the canonical uses EXISTS+correlation — per-subscription semantics identical, cohort outcome identical.

## Check 4 — Owner-FK (generic `subscriptions.user_id`) — PASS

Raw: `JOIN subscriptions s ON s.user_id = u.id` (:206); tx: `innerJoin(subscriptions, eq(subscriptions.userId, users.id))` (:142). Neither constrains `users.role`, neither routes through `students`/`student_subscriptions` — a teacher's verification-plan subscription counts, exactly per decisions B.8/C.2 (plan.md:283, :90). Pinned by Tier-1: teacher subscriber resolves on both branches (repo test :235-269 tx, :514-523 raw).

## Check 5 — Audit write vs immutability triggers — CLEAN

`3-immutability-triggers.sql` arms ONLY `BEFORE UPDATE` (:43-47) and `BEFORE DELETE` (:49-53) row triggers on `audit_logs` — no INSERT trigger, no statement-level trigger, and `2-functions.sql` is unrelated (`current_user_id()` RLS helper). `AuditService.createAuditLog` (:83-89) does a plain `tx.insert(auditLogs)` → triggers never fire for INSERTs; an in-tx audit write is unaffected. The broadcast's audit row uses `entityId: null` — legal: `audit_logs.entity_id` is nullable (`audit-logs.ts:39`, no `.notNull()`; this is exactly the schema fact task 1.x's contract widening rode). `actionType: "create"` ∈ pgEnum `audit_action_type` (enums.ts:66-74); `entityType: "notification_broadcast"` fits varchar(100); `details` truncated to the varchar(2000) ceiling by the writer before insert. Append-only interplay holds end to end.

## Check 6 — Injection posture on ALL raw SQL — CLEAN

All four raw shapes (:175, :184, :194, :204-214) are template literals whose ONLY interpolation is `${GOVERNANCE_PREDICATE_SQL}` — a file-local compile-time constant (:81), never user input. Selector values ride numbered binds: role→`$1` (:185), country→`$1` (:195), planId→`$1` (:214). No `sql.placeholder`, no prepared statements, no inline `--` inside `sql` templates (AGENTS.md conventions held). Companion null-guards fail closed to `[]` before any SQL is built (:114-116, :125-127, :136-138 / :180-182, :190-192, :200-202). Tier-4 tests pin tautology and `DROP TABLE`-shaped payloads as inert bound values (:396-419). The service-side companions feeding these binds are normalized (trimmed country, coerced role, `isPositiveSafeInt` planId) and the wire layer's `t.int()` for planId (`admin-broadcast.pothos.ts:51`) bounds it to int32 before the resolver even runs.

## Check 7 — Cap-5000 on the RESOLVED array; branch equivalence — PASS

`AdminBroadcastService.broadcast` caps on the post-query resolved array: `resolveBroadcastRecipients` → `assertCohortSize(recipientIds, …)` (:357-360) → `recipientIds.length > BROADCAST_MAX_RECIPIENTS` (:293) rejects BEFORE any write; the engine's batch re-validation additionally rejects duplicates/non-positive ids pre-DB. Branch equivalence for the same committed data: both branches emit the same predicate set, both order `id ASC`, both dedup identically (DISTINCT on plan; PK-unique elsewhere), both project the single `number` id column → identical order AND content. The 5000 cap's stated rationale is arithmetically sound: engine insert ≈ 7 columns × 5000 = 35,000 parameters < PostgreSQL's 65,535 per-statement ceiling. Residual: the cap is post-QUERY, so an oversized cohort is fully materialized before rejection — sanctioned design (plan §4.5 mitigation row; D1 chunking ledgered), bounded memory (8 B/id), not re-litigated as a finding.

## Schema (READ-ONLY) interplay verification

- `audit_logs` (audit-logs.ts:30-47): `entity_id` nullable ✓ (broadcast writes null); `actor_id` restrict-FK to the verified admin; `(entity_type, entity_id)` composite index exists; no `updated_at` — writer never updates.
- `notifications` (notifications.ts:27-46): every column the broadcast path touches exists with matching bounds — `type` pgEnum includes `system_broadcast` (enums.ts:56-64) ✓; `title` varchar(255) notNull ↔ service+engine 255 gates ✓; `body` text nullable ↔ verbatim optional ✓; `related_entity_type/Id` nullable ↔ broadcast writes null/null ✓; `user_id` cascade-FK; `(user_id, is_read)` index serves the inbox reads.
- `subscriptions` (subscriptions.ts:19-42): `status` notNull pgEnum ('active' ∈ set), `start_date`/`end_date` nullable timestamps ↔ predicate's coalesce / IS NULL handling ✓; `subscriptions_plan_id_idx` + `subscriptions_user_id_idx` serve the plan filter and the canonical EXISTS respectively.
- `users` (users.ts): governance columns `is_deleted`/`is_blocked`/`suspended` boolean-with-default (nullable at rest — the coalesce/null-safe read discipline is therefore load-bearing, and the repo handles it); `country` varchar(100) nullable — `country = $1` never matches NULL-country rows in either branch (NULL≠value), and the null companion fails closed pre-query ✓.

---

## Findings

No production-code defect found on this layer. Three INFO-grade observations, all honestly attributed:

### [INFO] backend/db/repo/notifications/broadcast-audience.repository.ts:81, :89-94, :211-212 — the governance predicate exists in three textual forms within one file (constant, aliased inline copy, Drizzle pair)

`GOVERNANCE_PREDICATE_SQL` cannot be reused under the `u` alias, so the plan shape re-states it inline (:211-212) while the tx branch expresses it a third way via `governanceConditions()` — plus a fourth (semantic) copy in the admin directory filter (`admin-user-query-helpers.ts:78-80`). All agree today. A future governance-column change (e.g. a third exclusion flag) must be applied in every copy; drift would be caught by the Tier-1 governed-fixture tests on both branches, but only if those fixtures are extended to the new column. Disposition: none required now; if a fourth exclusion flag ever lands, hoist the predicate into a shared builder pair first.

### [INFO] backend/db/repo/notifications/broadcast-audience.repository.ts:67-69 — fifth file-local copy of `isDBTransaction` (pre-existing substrate pattern, faithfully mirrored)

The type guard is duplicated across `user.repository.ts:29`, `applicant.repository.ts:36`, `plan.repository.ts:29`, `student.repository.ts:34`, and now this repo. The new file follows its siblings' convention (and the `--lifecycle duplicates` gate passes), so this is not a plan defect; it is a hoist candidate (`backend/db/repo/utils/`) for a future cross-cutting cleanup ticket, not this one.

### [INFO] [SUBSTRATE] backend/types/db.types.ts:30 + broadcast-audience.repository.ts:244-247 — a `PoolClient` passed as `tx` silently routes to the global pool

`DBQueryExecutor` unions `PoolClient`, but the `"select" in tx` guard treats a checked-out `PoolClient` as non-transactional and falls to `queryDb` (global pool). For committed reads the outcome is identical; a caller holding uncommitted state on a manually-managed client would be silently bypassed. Inherited verbatim from the four sibling repos (same guard, same fallback); no caller in the tree passes a `PoolClient` today, and the broadcast service passes either a real Drizzle tx or nothing. Recorded for the substrate ledger owner only — no action in this ticket.

---

## Counts

- CRITICAL: 0
- HIGH: 0
- MEDIUM: 0
- LOW: 0
- INFO: 3 (in-file predicate triplication; `isDBTransaction` 5th substrate copy; PoolClient-routing substrate note)

## Verdict

**PASS — zero new production-code findings on the DB layer.** All seven checks verified clean against traced code: governance predicates are semantically identical across tx/raw branches with `suspended` deliberately included in both (REQ-015); DISTINCT+id-ASC applied exactly where the join fans out and consistently in both branches; the active-window predicate is byte-equivalent to the canonical `studentHasActiveSubscriptionSubquery` including the `now()=end_date` exclusive / `now()=start_date` inclusive boundaries; the owner FK is the unconstrained generic `subscriptions.user_id`; the immutability triggers (BEFORE UPDATE/DELETE only) cannot interfere with the in-tx audit INSERT; all raw SQL is parameterized with the only interpolation being a compile-time constant; and the 5000 cap is enforced post-resolution pre-write with branch-identical cohort arrays. The plan verifiably added NO schema (git log clean on `backend/db/schema` + `backend/db/migration`; `system_broadcast`/`active`/`create` all pre-exist in the pgEnums). Three INFO-grade observations recorded — none blocks, none requires a fix inside DEV3-022d. Per the ≥10-iteration protocol, the zero-new-findings streak continues (R9 contributes none at LOW-or-above); R10 should target the canonical doc (7.1) interplay and any R6-R8 residuals.
