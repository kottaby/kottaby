# Task 3.2 — Mutation `adminBroadcastNotification` + Codegen + Baseline Freeze Updates

**Plan:** `ai/plans/sprint_3/dev3-022d-broadcast-notifications-system-wide-targ`
**Task:** 3.2 (Phase 3 — GraphQL) — side-effect mutation module delegating to `AdminBroadcastService.broadcast`; mutation barrel wiring; `generate:gqlSchema && bun codegen` with artifacts in the change set; the two baseline-freeze suites updated exactly as REQ-062 enumerates.
**Agent:** Implementation Executor
**Result:** ✅ COMPLETE — 6 production/test files touched, QL exit 0 on all four checked files, the three enumerated baseline suites 21/0 + 33/0 + 22/0, codegen clean (mutation present in the committed SDL with the exact REQ-060 signature; `gql/graphql.ts` byte-identical — no frontend document consumes the field until Phase 4).

---

## 1. What changed (complete change set — nothing beyond the enumerated updates)

| File | Op | Change |
|---|---|---|
| `backend/graphql/mutation/notifications/admin-broadcast.mutation.ts` | CREATE | Side-effect module registering `adminBroadcastNotification(input: AdminBroadcastNotificationInput!): Int!` with `authScopes: { $all: { authenticated: true, role: [UserRole.Admin] } }` (the `$all` conjunction verbatim from the `admin-users.mutation.ts` precedent — ANY-semantics plain maps are the documented wrong answer for an admin surface). Anonymous guard via `ctx.t("errorsTranslations")` → `UnauthorizedError(tErrors.unauthorized)` (TypeScript-narrowing branch only — unreachable in practice, per the sibling `notification.mutation.ts` pattern). Resolver delegates EXCLUSIVELY to `AdminBroadcastService.broadcast({ title, body, audience }, ctx.user.id, ctx.locale, ctx.idempotencyKey ?? undefined)` with FIELD-BY-FIELD input mapping into the canonical `BroadcastNotificationSubmitInput` (never a spread; absent/null wire slots coalesce to `null`). NO try/catch (boundary-only masking); NO engine imports/calls; NO identity args; no named exports. |
| `backend/graphql/mutation/notifications/index.ts` | UPDATE | + `import "./admin-broadcast.mutation";` (alphabetically first) + header bullet naming what the new module registers. |
| `frontend/graphql/generated/schema.graphql` | REGEN | `bun run generate:gqlSchema` — exactly +24 lines: `input AdminBroadcastNotificationInput` + `input BroadcastAudienceInput` + `enum BroadcastAudienceType {All, Country, Plan, Role}` + `adminBroadcastNotification(input: AdminBroadcastNotificationInput!): Int!` on `Mutation` (with its description block). |
| `frontend/graphql/generated/gql/graphql.ts` | REGEN (byte-identical) | `bun codegen` exit 0. The typed-document-node output is document-driven and NO frontend operation references the mutation yet — the artifact is provably in lockstep (unchanged) until Phase 4's `adminBroadcastNotificationMutationDocument` lands. |
| `backend/graphql/test/sdl-static-assertions.test.ts` | UPDATE | `FROZEN_MUTATION_FIELDS` gains `"adminBroadcastNotification"` (alphabetical first, hard `toEqual` freeze); header bullets + const doc-comment extended; NEW describe `Admin broadcast surface — exact SDL signatures on the artifact` with three tests pinning the mutation signature + `BroadcastAudienceInput {type, role, country, planId}` types + `AdminBroadcastNotificationInput {title, body, audience}` types. |
| `backend/graphql/test/schema-surface.test.ts` | UPDATE | `PRE_3_1_MUTATION_FIELDS` gains `"adminBroadcastNotification"` (the freeze is a HARD `toEqual` on the baseline list — per plan-review F-5 there is NO additions array for mutations, so the frozen list absorbed the name; header bullet + doc comments extended); `PRE_3_1_ENUMS` += `"BroadcastAudienceType"`; `PRE_3_1_TYPE_NAMES` += the three new names (`AdminBroadcastNotificationInput`, `BroadcastAudienceInput`, `BroadcastAudienceType` — the enum name IS tracked there, absorbed additively per the "entries are NEVER dropped" convention, which also keeps the whole-schema delta array untouched). |

`backend/lib/gateway/public-operations.ts`: **UNTOUCHED** (diff-proven empty — the admin op is never public; REQ-018 Rule 8.4 satisfied via the declared `authScopes`, not the allowlist).

## 2. Verification evidence

### 2.1 Quality Loop (3.2.QL) — `bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates`

| File | tsgo (project-wide) | oxlint | biome:check | lint:type-aware | check:duplicates | Exit |
|---|---|---|---|---|---|---|
| `backend/graphql/mutation/notifications/admin-broadcast.mutation.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `backend/graphql/mutation/notifications/index.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `backend/graphql/test/sdl-static-assertions.test.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `backend/graphql/test/schema-surface.test.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |

The sub-loop tsgo stage is a full-repo build ⇒ tsgo delta vs the 2.M/3.1 baseline (0) remains 0.

### 2.2 Codegen (REQ-061) — `bun run generate:gqlSchema && bun codegen`

- generate: exit 0, wrote `schema.graphql` (12 349 bytes); committed-artifact diff is EXACTLY the four sanctioned blocks (+24 lines, no other drift).
- SDL proof (lexicographic artifact order): `adminBroadcastNotification(input: AdminBroadcastNotificationInput!): Int!` — the exact REQ-060 surface; input/enum shapes byte-match the 3.1-verified forms.
- codegen: exit 0, `gql/graphql.ts` regenerated **byte-identical** (0 diff) — document-driven output with zero referencing documents yet (Phase 4 adds the mutation document; the artifact still ships in this change set per Rule 8.5).
- `schema-surface.test.ts`'s "Codegen sync" test (committed SDL ≡ fresh `printSchema(lexicographicSortSchema(...))`) asserts green — the committed artifact is provably in lockstep with the built schema.

### 2.3 Baseline suites (3.2.TE)

| Suite | Result |
|---|---|
| `backend/graphql/test/sdl-static-assertions.test.ts` | **21 pass / 0 fail / 71 expects, exit 0** (was 18 tests; +3 new broadcast pins) |
| `backend/graphql/test/schema-surface.test.ts` | **33 pass / 0 fail / 175 expects, exit 0** (freeze + enum-pin + named-type delta all re-pinned) |
| `backend/graphql/test/handshake-code-surface.test.ts` | **22 pass / 0 fail / 44 expects, exit 0** (the `$all` extension-introspection precedent suite unaffected) |

Full-directory run (`bun run test/scripts/run-test.ts backend/graphql/test`, tasks.md 3.2.TE verbatim): **239 pass / 4 fail / 1529 expects across 14 files** — the 4 fails are ALL `Plan Catalog GraphQL Role-Matrix > Anonymous receives UNAUTHORIZED` (receives `FORBIDDEN`), and they are **PRE-EXISTING, not caused by 3.2**: isolation-proven by temporarily removing the new mutation file + barrel import and re-running `plan-catalog.roles.test.ts` → the identical 4 failures (21 pass / 4 fail). Root cause lives outside this ticket's scope: `plan-catalog.mutation.ts`/`plan-catalog.query.ts` declare maps without the `authenticated` key (`authScopes: { role: [UserRole.Admin] }` — ANY semantics), so an anonymous context is denied by the `role` scope with `FORBIDDEN`, while the test (written for the `$all` conjunction world) expects `UNAUTHORIZED`. **Flagged for the orchestrator / deferred-items triage — 3.2 did NOT touch plan-catalog files (scope discipline).**

### 2.4 3.2.SEC — Security & Tenancy Audit (PASS)

- **authScopes exactly the `$all` map:** the field's scope block is literally `{ $all: { authenticated: true, role: [UserRole.Admin] } }` — no extra keys, no plain-map fallback.
- **Zero identity args:** the field's ONLY argument is `input: AdminBroadcastNotificationInput!`; grep over the file for `userId|actorId|recipientId|userIds|sessionId|studentId|teacherId|parentId` in code → 0 (the single textual hit is the security post comment itself).
- **Only ctx-derived material crosses the boundary:** `ctx.user.id` (actorId), `ctx.locale`, and the gateway-captured `ctx.idempotencyKey ?? undefined` (propagation-only per the context factory: `null`-absent coalesced to the service's optional `undefined` slot — never re-derived/trimmed). The anonymous guard throws BEFORE any service call, so the narrow path can never reach the service with a null user.
- **No engine surface:** 0 references to `NotificationEngine`/`emitForUsers`/`publishReceipts` in the resolver (engine discipline — the service consumes the engine, by reference).
- **No try/catch** (0 matches): expected rejections keep their localized messages + `extensions.code`; unexpected internals bubble to the boundary finalizer — masking stays boundary-only.

### 2.5 3.2.SR — Semantic Review (PASS)

- **Delegation-only resolver:** the body is guard → field-by-field mapping → single `AdminBroadcastService.broadcast(...)` return. No business logic, no repository imports, no local cap re-declaration (`BROADCAST_MAX_RECIPIENTS` stays service-exported).
- **No duplicate registration:** `mutationField("adminBroadcastNotification"` appears EXACTLY once repo-wide (grep-proven); barrel wiring is side-effect-only (`import "./admin-broadcast.mutation";`), consistent with `mutation/AGENTS.md` (no named exports).
- **No dead exports:** the Pothos inputs consumed by the mutation args were already registered + barrel-wired in 3.1; the mutation file itself has no named exports; the resolver's narrowing guard mirrors the established sibling pattern (no unused helpers introduced).

### 2.6 3.2.IV — Instruction Verification (PASS)

| Rule source | Check |
|---|---|
| `backend/graphql/AGENTS.md` — resolvers delegate to services; locale propagation; `ctx.t` for direct throws; nullability defaults; no `console.*`; no `oxlint-disable` | all satisfied (guard message via `ctx.t("errorsTranslations")`; `ctx.locale` propagated; `type: "Int"` non-null by default; 0 `console.*`; 0 disable comments) |
| `docs/graphql/api-gateway-and-routing.md` Rule 8 (the REQ-018 registration contract) | (1) resolver module at `mutation/notifications/<name>.mutation.ts` via SIDE-EFFECT barrel ✓ (no dynamic import, no out-of-subtree import) · (2) inputs live in `pothos/notifications/` (3.1; no new objects — `Int!` is a scalar payload, no cache-`id` question) ✓ · (3) enum registered ONCE in `shared/enum.pothos.ts` via enum-object form (3.1; nothing re-declared) ✓ · (4) authorization declared on the operation — `authScopes` `$all` map; public allowlist untouched (empty diff) ✓ · (5) `generate:gqlSchema && bun codegen` run with artifacts in the SAME change set ✓ |
| `ctx.idempotencyKey` policy (AGENTS + gateway doc §6) | consumed propagation-only, coalesced `null`→`undefined`, never logged, never auth-relevant ✓ |
| Comment hygiene | grep `REQ-[0-9]+|tasks\.md|plan\.md|specs\.md|ai/plans|Task 3` over the new mutation file → **0 hits** |

## 3. Deviations / operational notes

1. **Sandbox branch drift (persistent, same as 2.M/3.1):** HEAD was force-reset to `main` between tool calls, and one drift window silently discarded the first `FROZEN_MUTATION_FIELDS` edit AFTER the in-editor change but BEFORE the /tmp master snapshot (caught by the failing freeze test + grep). Mitigation: `/tmp/t32/apply.sh` self-healing prelude (branch force-checkout → 3.1 artifact restore → 3.2 master restore → per-artifact grep verification incl. the frozen-list entry specifically), hardened mid-session after the lost-edit incident. All evidence above produced under verified branch state with edits live. Masters preserved at `/tmp/t32/` — re-applying is a 6-file copy if drift strikes before the orchestrator commits.
2. **Pre-existing plan-catalog failures:** see §2.3 — 4 anonymous-scope expectation mismatches (`FORBIDDEN` vs `UNAUTHORIZED`) proven pre-existing via isolation; NOT fixed here (out of the enumerated file scope). Recommend a deferred-items row / 3.3 decision: either the plan-catalog fields adopt the `$all` conjunction (code change) or the test expectation is corrected (test change) — a one-line decision for the owner, flagged rather than smuggled into this change set.
3. `gql/graphql.ts` ships byte-identical (no referencing document yet) — this satisfies "artifacts committed in the same change set" trivially while REQ-063 (Phase 4) will produce its first real delta.

## 4. Carry-forward for 3.3 (integration matrix) / Phase 4

1. The `$all` denial semantics to probe over the wire: anonymous → `UNAUTHORIZED`, authenticated non-admin → `FORBIDDEN` (mirror `handshake-code-surface.test.ts` extension-introspection pin for the authScopes snapshot; note the plan-catalog discrepancy in §3.2 — pin broadcast with `$all`, not ANY).
2. Wire-value nuance (3.1): audience `type` arrives as enum KEYS (`All`/`Role`/`Country`/`Plan`); the resolver maps the enum's runtime value (`"all"`…) into the selector — integration documents must send key-case values.
3. Resolver replay path: repeated `X-Idempotency-Key` must yield the same count with zero new rows/audit/publishes (service REQ-023 already proven at 2.4; 3.3 proves the header→ctx→service hop).
4. Phase 4: the first `gql/graphql.ts` delta arrives with `adminBroadcastNotificationMutationDocument` (REQ-063).
