# Task 4.2 — `/admin/broadcasts` Route (Server Component Guard) Outcome

**Task:** 4.2 CREATE `app/(dashboard)/admin/broadcasts/page.tsx` — Server Component with `withPageAuth({ roles: [UserRole.Admin], redirectTo: "/admin/broadcasts" })` rendering the client `BroadcastComposeContainer`
**Branch:** `feat/dev3-022d-broadcast-notifications` (re-verified before EVERY command batch; byte mirrors kept at `/tmp/t42/`)
**Method:** pre-read worklog (4.4 + 4.1 sections), plan bundle (tasks.md 4.2 verbatim, plan.md §5.1/§5.3, specs.md REQ-063 + REQ-030), all committed outcome files, then FULL reads of the three closest guarded-page precedents — `app/(dashboard)/audit/page.tsx` (DEV3-020), `app/(dashboard)/admin/users/page.tsx`, `app/(dashboard)/parent/handshake/page.tsx` — and mirrored their exact conventions (imports, `UserRole` source, direct view-module import, `generateMetadata` shape).

---

## 1. Change set

| File | Op | Contents |
|---|---|---|
| `app/(dashboard)/admin/broadcasts/page.tsx` | CREATE (~37 lines) | Server Component page: `generateMetadata()` (locale-cookie → single-arg `getTranslations(locale).adminBroadcastsTranslations` → `pageTitle`/`pageSubtitle`) + default export that awaits `withPageAuth({ roles: [UserRole.Admin], redirectTo: "/admin/broadcasts" })` as its ONLY statement before rendering `<BroadcastComposeContainer />` with no props |
| `test/ui/page-guards/admin-broadcasts-page.test.ts` | CREATE (~200 lines) | Per-route SSR guard suite (mirrors `parent-handshake-page.test.ts`, the established page-guard pattern): drives the REAL page module against controlled `getServerUserContext` / `next/navigation` / `next/headers` doubles + a component double for the 4.3-owned container specifier |

NOT touched: `frontend/views/admin/broadcasts/BroadcastComposeContainer.tsx` (owned by the parallel task 4.3 — never created, stubbed, or edited here), nav model (4.4 done), locale bundles (1.4 done), all other working-tree deltas.

## 2. Route anatomy (`page.tsx`)

1. **Guard (line 35, sole statement of the default export):** `await withPageAuth({ roles: [UserRole.Admin], redirectTo: "/admin/broadcasts" })`. `UserRole` is imported as a runtime VALUE from `@/backend/enum/users/user-role.enum` (audit/users/handshake sibling import path). The route never names bare `/dashboard` and never calls `getServerUserContext` itself — the wrapper owns it.
2. **Metadata (lines 24–30):** `const locale = await getLocaleFromCookie()` (`@/shared/locale/server-cookies`) then ONE-argument synchronous `getTranslations(locale).adminBroadcastsTranslations` → `title: t.pageTitle`, `description: t.pageSubtitle`. Verified the `adminBroadcastsTranslations` namespace was already shipped by the 1.4 locale-prep commit (types/message.ts:24 + en/ar bundles), so no namespace was invented here and the metadata is real localized copy in both locales.
3. **Composition (line 37):** `return <BroadcastComposeContainer />` — imported DIRECTLY from `@/frontend/views/admin/broadcasts/BroadcastComposeContainer` (no barrel hop — the `frontend/views/admin/` barrel still exports only plans + users; audit-page precedent "imported directly from the view module"). No props: the container resolves every label client-side through the `AdminBroadcasts` namespace handle (per the 4.3/5.4 contract), pinned by `propKeys == []` in the suite.
4. **Server purity:** zero `"use client"`, zero React hooks, zero browser APIs (grep-verified), zero plan-artifact tokens in code or comments (grep-verified for `REQ-`, `Task 4`, `dev3-022d`).

## 3. Gate evidence (all commands re-verified on `feat/dev3-022d-broadcast-notifications`)

### 4.2.QL — Quality Loop — ✅ exit 0 (attempt 5)

`bun run scripts/health/sub-loop.ts "app/(dashboard)/admin/broadcasts/page.tsx" --lifecycle duplicates` → **exit 0**, all five stages green:

```
✅ tsgo passed (no errors for app/(dashboard)/admin/broadcasts/page.tsx)
✅ oxlint passed
✅ biome:check passed
✅ lint:type-aware passed
✅ check:duplicates passed
```

Drift-tainted history (documented honestly): attempts 1–4 could NOT reach exit 0 through no fault of the page — the shared worktree was repeatedly flipped to `main` between/inside tool calls (attempt 1 ran on `main`, failing oxlint with `TS2339: Property 'adminBroadcastsTranslations' does not exist on type 'Translations'` because main's `message.ts` predates the namespace), and 4.3's sibling `BroadcastComposeContainer.tsx` kept appearing/disappearing in the shared tree (attempts 2/4 failed tsgo with exactly ONE diagnostic each: `TS2307: Cannot find module '@/frontend/views/admin/broadcasts/BroadcastComposeContainer'`; attempt 3 was re-polluted to main, same TS2339 signature). Per protocol: waited, re-verified, retried; while the sibling was absent the type-independent stages were run standalone and were GREEN — biome exit 0 ("Checked 1 file… No fixes applied"), jscpd exit 0 ("No duplicates found. / Found 0 clones"), oxlint reporting ONLY the single TS2307 (zero findings in my code). Attempt 5 ran with the sibling present on the verified feature branch → clean exit 0. Stale check: `page.tsx` byte-identical to the `/tmp/t42/` mirror after every drift event.

### 4.2.TE — Unit / Component Tests — ✅ green

The established guard pattern DOES include per-route pins (`test/ui/page-guards/parent-handshake-page.test.ts` from DEV1-013), so a mirrored per-route suite was created (this is the "role-redirect test additions" path, not a new pattern).

| Suite | Command | Result |
|---|---|---|
| NEW `test/ui/page-guards/admin-broadcasts-page.test.ts` | `bun run test/scripts/run-test.ts test/ui/page-guards/admin-broadcasts-page.test.ts` | **6 pass / 0 fail / 24 expect()** (final run on verified branch) |
| Existing `frontend/lib/auth/roleDashboardRoute.test.ts` (the `roleDashboardPath` contract the guard redirects through) | `bun run test/scripts/run-test.ts frontend/lib/auth/roleDashboardRoute.test.ts` | **11 pass / 0 fail / 44 expect()** |

Test-side module double (documented design decision, NOT a stub of the sibling file): the suite registers `mock.module("@/frontend/views/admin/broadcasts/BroadcastComposeContainer", …)` BEFORE importing the page, so the guard suite (a) runs while 4.3's file is transiently absent from the shared tree, and (b) stays decoupled from the container's own client-hook module graph; the rendered-element identity assertion (`shell.type` = the container) proves the page renders exactly that component. The container's real render tree is 4.3's own suite's subject.

**Pre-existing red, NOT touched (carry-forward):** `test/ui/page-guards/parent-handshake-page.test.ts` fails 1/6 on this branch — its anonymous test dies with `ERR_INVALID_URL` at `new URL(redirectTo ?? "")` (base-less parse of the RELATIVE `/login?redirect=…` location that `withPageAuth` has emitted since the Private-Network-Access fix in commit 7449297; the suite last changed in 6da55fb and predates it). Reproduced BEFORE any of my changes; file belongs to the finished DEV1-013 plan — left untouched, flagged for the orchestrator (fix = one-line base-supplied parse, as my suite uses).

### 4.2.SEC — Security & Tenancy — ✅ fail-closed verified

- Anonymous: `withPageAuth` calls `getServerUserContext()` (frontend/lib/auth/withPageAuth.ts:68 — governance-aware SSR context), and when `ctx.user/role/userId` are absent redirects to `/login?redirect=/admin/broadcasts` (build :84–86, `redirect(loginPath)` :86; relative location per the PNA fix). Suite pins it: `admin-broadcasts-page.test.ts:146–156` asserts pathname `/login` + `searchParams.get("redirect") === "/admin/broadcasts"` and that the page NEVER renders.
- Governed non-admin: role mismatch (OR-semantics whitelist :90) → `redirect(roleDashboardPath(ctx.role))` (:95) — student/teacher/parent land on `/student|/teacher|/parent/dashboard`, never bare `/dashboard` (suite :159–172 asserts exact target + `not.toBe("/dashboard")`); `roleDashboardPath` itself is pinned by the 11-test `roleDashboardRoute.test.ts`.
- Governed admin: renders the compose container; the page leaks nothing (no props) — all compose data flows through the Apollo mutation from 4.1/4.3.
- SSR-level BFLA echo: REQ-030's `$all` wall is backend-side (committed, e0831a2); this page is the SSR half — admin-only, fail-closed, and anonymous users get zero page markup.

### 4.2.SR — Semantic Review — ✅

- Zero client hooks / `"use client"` / browser APIs in the page (grep-verified). The only client module — the container — is rendered, not implemented, here.
- Metadata: `getTranslations(locale)` single-argument synchronous property chain (verified pattern-identical to audit/users/plans/handshake pages); no static `metadata` export (locale-derived, per REQ-062 precedent comment).
- Zero plan-artifact tokens in code/comments; comments read as natural engineering docs (guard contract + view-import rationale + metadata note).
- Formatting/lint: biome check --write --unsafe applied zero fixes; oxlint zero findings (post-sibling).

### 4.2.IV — Instruction Verification — ✅ (existence checked before citing)

- `.agents/instructions/frontend.instructions.md` EXISTS — honored: server translations via single-arg `getTranslations(locale)` property chain; no `console.*`; no `useLazyQuery` concerns (page has no data hooks); MUI/theme rules N/A (no styling in a shell page).
- `app/AGENTS.md` EXISTS (the task said verify) — followed where current: Server Components have no hooks/browser APIs; locale via cookies (no `[locale]` segment). NOTE: its guard citation is STALE — `app/(dashboard)/shared/withPageAuth.ts` does NOT exist (verified); the real module is `frontend/lib/auth/withPageAuth.ts` (`export * from "./withPageAuth"` barrel at frontend/lib/auth/index.ts:3), which is what all three sibling precedents and this page import.
- Guard conventions: `frontend/lib/auth/` modules read in full; redirect discipline ("never bare `/dashboard`" — `roleDashboardRoute.ts` docblock + `docs/auth/REDIRECT_LOOP_FIX.md` context) honored.

## 4. Carry-forward for Phase 5 / later gates

1. **4.3 sibling dependency:** the page's tsgo/oxlint/type-aware results are only green while `frontend/views/admin/broadcasts/BroadcastComposeContainer.tsx` exists on disk (it did at final QL). If Phase 5 full-suite runs hit `TS2307` here, check 4.3's file presence first — the page side is done.
2. **Metadata key:** page metadata intentionally uses the `adminBroadcastsTranslations.pageTitle/pageSubtitle` namespace (already shipped by 1.4). If 5.5's browser loop expects a different title string, it is one property away.
3. **Pre-existing parent-handshake suite failure** (base-less `new URL()` on a relative redirect) — one-line fix candidate, owner: orchestrator / DEV1-013 follow-up. My suite uses the base-supplied parse and is green.
4. **Branch-drift posture:** every sensitive step here ran as atomic checkout→verify→act sequences; mirrors at `/tmp/t42/{page.tsx,admin-broadcasts-page.test.ts}`; final tree verified byte-identical.

## 5. Scope

- CREATED: the two files above. BOOKKEEPING: this outcome file, tasks.md 4.2 + 4.2.QL/TE/SEC/SR/IV flips, worklog section. NOT committed (orchestrator owns commits). `Caddyfile` never touched; no `git add`/`git checkout .`/`git commit` run.
