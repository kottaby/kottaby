# Task 0.2 — Prerequisite Verification: Reuse Substrate (REQ-002)

**Plan:** `ai/plans/sprint_3/dev3-022d-broadcast-notifications-system-wide-targ`
**Task:** 0.2 (Phase 0) — verify the reuse substrate exists before Phase 1+ tasks begin.
**Agent:** Prerequisites Verifier (READ-ONLY — no source files modified)
**Result:** **ALL 16 ARTIFACTS PRESENT ✅ — no BLOCKED rows; Phase 1 may proceed.**
**Note on line drift:** the tasks.md expected-line hints for 3 artifacts point at stale offsets from another revision. Actual locations are cited below; later tasks should cite THESE, not the tasks.md hints.

---

## Verification Table

| # | Artifact | Verified path:line | Status | Signature / behavior note |
|---|----------|--------------------|--------|---------------------------|
| 1 | `NotificationEngine.emitForUsers` | `backend/services/notifications/notification-engine.service.ts:80` (tasks.md hinted :393 — file is 183 lines total) | ✅ | `emitForUsers(input: NotificationEmitBatchInput, locale: string, tx?: DBTransaction, options?: NotificationEngineCallOptions): Promise<NotificationDeliveryReceipt>` — validates input, claims idempotency key via `claimOrPriorReceipt` (returns prior receipt on hit), batch-inserts via `NotificationRepository.createManyReturning` inside `withTransaction`, publishes receipts. |
| 2 | `NotificationEngine.publishReceipts` | `backend/services/notifications/notification-engine.service.ts:117` (hinted :475) | ✅ | `publishReceipts(receipts: readonly NotificationDeliveryReceipt[], locale: string, options?): Promise<void>` — delegates to `publishReceiptsFromIndex` (`backend/services/notifications/notification-engine.publish`, imported at `notification-engine.service.ts:20`). |
| 3 | `validateEmitBatchInput` | `backend/services/notifications/emit-validation.ts:135` | ✅ | `export function validateEmitBatchInput(input: NotificationEmitBatchInput, validationMessage: string): void` — throws localized `ValidationError` on invalid batch. |
| 4 | `buildEmitClaimKey` | `backend/services/notifications/emit-idempotency.ts:67` | ✅ | `export function buildEmitClaimKey(userIds: readonly number[], type: NotificationType, idempotencyKey: string): string` — deterministic claim key builder. |
| 5 | `attemptEmitClaim` | `backend/services/notifications/emit-idempotency.ts:84` | ✅ | `export async function attemptEmitClaim(...)` — insert-or-return-prior idempotency claim. |
| 6 | `storeEmitReceiptQuietly` | `backend/services/notifications/emit-idempotency.ts:112` | ✅ | `export async function storeEmitReceiptQuietly(...)` — best-effort receipt persistence (swallows secondary failure). |
| 7 | `NotificationRepository.createManyReturning` | `backend/db/repo/notifications/notification.repository.ts:148` | ✅ | `export async function createManyReturning(...)` — batch insert with `.returning()`, accepts optional `DBQueryExecutor`. |
| 8 | `AuditService.createAuditLog` | `backend/services/admin/audit.service.ts:82` | ✅ | `export async function createAuditLog(input: AuditLogWriteContract, tx: DBTransaction): Promise<void>` — composition-only audit writer (rule pinned in `backend/services/AGENTS.md:9`). |
| 9 | `assertActorAdmin` | **Definition:** `backend/services/admin/admin-gate.helpers.ts:59` — `export async function assertActorAdmin(actorId: number, locale: string, outerTx?: DBTransaction): Promise<void>`. *Location drift:* tasks.md hinted `user-management.service.ts:240-271` — that range is `createUser`, which CALLS it at `user-management.service.ts:271` (import at `:65`); the service re-exports nothing. | ✅ | Admin gate: anonymous → `UNAUTHORIZED`, non-admin → `FORBIDDEN`; emits zero audit rows on denial. |
| 10 | `PlanRepository.existsById` | `backend/db/repo/billing/plan.repository.ts:109` | ✅ | `export async function existsById(id: number, tx?: DBQueryExecutor): Promise<boolean>`. |
| 11 | Active-subscription predicate | **Definition:** `backend/db/repo/admin/admin-user-query-helpers.ts:134-143` — `export function studentHasActiveSubscriptionSubquery(): SQL.Aliased<boolean>` (SQL `EXISTS` on `subscriptions` with `status='active'` AND `now()` inside `start_date`/`end_date` window, open-ended-safe via `coalesce`/`IS NULL`). *Location drift:* tasks.md hinted `admin-user.repository.ts:337-346` — that range is soft-delete predicate docs; the predicate is imported at `admin-user.repository.ts:45` and consumed at `:124` and `:290`. | ✅ | Scalar subselect, never a JOIN, never a balance touch. Reusable as the plan/subscription audience filter basis. |
| 12 | `withTransaction` (canonical) | `backend/lib/db/with-transaction.ts:27` — `export async function withTransaction<T>(outerTx: DBTransaction \| undefined, fn: (tx: DBTransaction) => Promise<T>): Promise<T>` — SAVEPOINT-nesting when `outerTx` given, else new `db.transaction`. Engine-local variant also exists: `backend/services/notifications/notification-engine.helpers.ts:18` (imported by `notification-engine.service.ts:11`). | ✅ | Both variants present; broadcast work composes either via existing imports. |
| 13 | `SpiedFanoutTransport` | `test/workflows/helpers/spied-transport.ts:49` | ✅ | `export class SpiedFanoutTransport implements FanoutTransportLike` — records frozen `FanoutPublishCall` entries; exposes `calls`, `publishCount`, `lastCall` (:57-84). |
| 14 | `projectMutationFieldErrors` | `frontend/lib/mutationFieldErrors.ts:125` | ✅ | `export function projectMutationFieldErrors(error: unknown): readonly ProjectedFieldError[]` — pure Apollo-error → RHF field-pair projection; companion `applyProjectedFieldErrors` at `:147`. |
| 15 | `withPageAuth` | `frontend/lib/auth/withPageAuth.ts:67` | ✅ | `export async function withPageAuth(options?: WithPageAuthOptions): Promise<WithPageAuthResult>` — SSR guard: cookie auth check + optional role whitelist, role-aware redirects. |
| 16 | `adminPlansQueryDocument` | `frontend/graphql/sharedDocuments/billing/plan-catalog.documents.ts:38` | ✅ | `export const adminPlansQueryDocument: TypedDocumentNode<AdminPlansQuery, AdminPlansQueryVariables> = gql\`...\`` — consumed by `frontend/views/admin/plans/catalog/PlanCatalogContainer.tsx:19,37`; pinned by `plan-catalog.documents.test.ts:34`. |

---

## test/workflows Layer Confirmation (task 2.0 reuses — scaffolds nothing)

| Artifact | path:line | Status | Note |
|----------|-----------|--------|------|
| `test/workflows/AGENTS.md` | `test/workflows/AGENTS.md:1-100` | ✅ | Journey-layer rules: NO `runInRollback`, committed fixtures + `TrackedFixtures` cleanup with post-teardown existence probes. Harness declared scaffolded (`:7-10`). |
| `provisionStudentActor` | `test/workflows/helpers/actor-context.ts:88` | ✅ | Real `users` row (`role="student"`) + `students` role-child; registers rows for tracked cleanup. |
| `provisionCertifiedTeacherActor` | `test/workflows/helpers/actor-context.ts:105` | ✅ | `role="teacher"` + approved `teacher` row (`isApproved=true`) — certified permission path. |
| `provisionParentActor` | `test/workflows/helpers/actor-context.ts:122` | ✅ | `role="parent"` + `parents` role-child. |
| `provisionAdminActor` | `test/workflows/helpers/actor-context.ts:136` | ✅ | `role="admin"` + `admin` role-child. |
| `SpiedFanoutTransport` | `test/workflows/helpers/spied-transport.ts:49` | ✅ | (same as #13 — shared via `test/workflows/helpers/index.ts` barrel). |
| `withAuditDeleteTriggersSuspended` | `test/helpers/db-cleanup.ts:83` | ✅ | `export async function withAuditDeleteTriggersSuspended<T>(fn: () => Promise<T>): Promise<T>` — suspends non-internal audit triggers around bulk fixture writes. |

---

## Missing Artifacts / BLOCKED Rows

**None.** All 16 substrate artifacts exist. Per task 0.2 protocol ("IF any artifact is missing → record ❌ in deferred-items.md and BLOCK"), `deferred-items.md` requires **no** new rows — the ledger stays as seeded (D1/D2 seeding is task 0.1's ownership).

## Location-Drift Advisory (informational — NOT blockers)

The tasks.md line hints below come from a different revision. Downstream tasks must cite the verified locations in this file:

1. `emitForUsers` / `publishReceipts` → actual `notification-engine.service.ts:80` / `:117` (file is 183 lines; hinted :393/:475 do not exist in this revision).
2. `assertActorAdmin` → defined in `admin-gate.helpers.ts:59`, not inside `user-management.service.ts` (which only imports/calls it at `:65`/`:271`).
3. Active-subscription predicate → `studentHasActiveSubscriptionSubquery()` in `admin-user-query-helpers.ts:134-143`, not `admin-user.repository.ts:337-346`.

## Quality Loop

Post-edit verification (`bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates`) is not applicable — this task writes only plan/outcome markdown, no production code, no source files.
