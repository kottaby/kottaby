# Task 3.1 — Pothos: Register `BroadcastAudienceType` Enum + Admin-Broadcast Input Types

**Plan:** `ai/plans/sprint_3/dev3-022d-broadcast-notifications-system-wide-targ`
**Task:** 3.1 (Phase 3 — GraphQL) — enum-object registration in `shared/enum.pothos.ts`; `BroadcastAudienceInput` + `AdminBroadcastNotificationInput` in `backend/graphql/pothos/notifications/admin-broadcast.pothos.ts`; domain barrel update. Pre-task fix (2.M review wave, LOW): `BroadcastAudienceRepository` tx-branch status literal → canonical enum value.
**Agent:** Implementation Executor (GraphQL registration only — NO mutation files touched; task 3.2 owns the mutation + codegen commit)
**Result:** ✅ COMPLETE — 4 production files touched, QL exit 0 on all four, schema builds clean (exit 0, exact REQ-060 surface in SDL), gate-A2 suite 17/0 green, repo suite 19/0 after the enum-value fix.

---

## 0. Pre-task fix (from 2.M review wave — LOW finding, tight scope)

`backend/db/repo/notifications/broadcast-audience.repository.ts`:

| Branch | Before | After |
|---|---|---|
| tx (Drizzle builders) | `eq(subscriptions.status, "active")` string literal | `eq(subscriptions.status, SubscriptionStatus.Active)` — canonical value import from `@/backend/enum/billing/subscription-status.enum` (import inserted in module-specifier sort order) |
| raw SQL (committed-fixture path) | `AND s.status = 'active'` | **UNCHANGED** (byte-equivalence with `admin-user-query-helpers.ts:139` is plan-mandated; the raw branch's doc comment remains accurate) |

Behavior-identical (`SubscriptionStatus.Active === "active"`): repo suite re-run → **19 pass / 0 fail / 61 expects, exit 0**.

## 1. What changed (complete change set)

| File | Op | Change |
|---|---|---|
| `backend/graphql/pothos/shared/enum.pothos.ts` | UPDATE | + `import { BroadcastAudienceType } from "@/backend/enum/notifications/broadcast-audience-type.enum";` (sort-position before the `notification-type.enum` import) · + header "Registered enums" list line · + `export const BroadcastAudienceTypePothosEnum = gqlSchemaBuilder.enumType(BroadcastAudienceType, { name: "BroadcastAudienceType" });` — enum-OBJECT form ONLY, appended after `NotificationTypePothosEnum` (notifications-domain grouping), JSDoc in the file's established registry style |
| `backend/graphql/pothos/notifications/admin-broadcast.pothos.ts` | CREATE | Two string-named `inputType` definitions (the sanctioned input-type exception) — `BroadcastAudienceInput { type: BroadcastAudienceType!, role: UserRole, country: String, planId: Int }` and `AdminBroadcastNotificationInput { title: String!, body: String, audience: BroadcastAudienceInput! }`; NO local type shapes, NO identity fields |
| `backend/graphql/pothos/notifications/index.ts` | UPDATE | + `export * from "./admin-broadcast.pothos";` (alphabetically first); header comment now mentions the compose inputs |
| `backend/db/repo/notifications/broadcast-audience.repository.ts` | UPDATE | the pre-task fix above |

Nothing else: NO mutation file (3.2's scope), NO `public-operations.ts`, NO baseline-freeze test edits (3.2), NO engine/service/repo logic beyond the one-line enum-value fix, NO codegen artifact left behind (the regenerated `schema.graphql` was restored after verification — task 3.2 runs `generate:gqlSchema && bun codegen` and commits artifacts in its change set per REQ-061).

## 2. Verification evidence

### 2.1 Quality Loop (3.1.QL) — `bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates`

| File | tsgo | oxlint | biome:check | lint:type-aware | check:duplicates | Exit |
|---|---|---|---|---|---|---|
| `backend/graphql/pothos/notifications/admin-broadcast.pothos.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `backend/graphql/pothos/shared/enum.pothos.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `backend/graphql/pothos/notifications/index.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `backend/db/repo/notifications/broadcast-audience.repository.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |

Logs: `/tmp/t31/ql-admin-broadcast.log`, `/tmp/t31/ql-enum.log`, `/tmp/t31/ql-index.log`, `/tmp/t31/ql-repo.log`. The sub-loop's tsgo stage is a project-wide build (filtered per file) — 4 clean builds ⇒ full-repo tsgo delta vs the 2.M baseline (0) remains 0.

### 2.2 Schema build (3.1.TE) — `bun run generate:gqlSchema`

```
[generate:gqlSchema] wrote /home/z/my-project/frontend/graphql/generated/schema.graphql (12119 bytes)
EXIT_GENERATE=0
```

Zero registration errors — grep over the full generator output for `error|already been declared|duplicate` → **0 hits** (no enum re-registration, no duplicate type names). The built schema surface proves the exact REQ-060 shapes (lexicographically sorted SDL):

```graphql
enum BroadcastAudienceType { All, Country, Plan, Role }   # wire names = TS member keys (UserRole precedent)

input BroadcastAudienceInput {
  country: String
  planId: Int
  role: UserRole
  type: BroadcastAudienceType!
}

input AdminBroadcastNotificationInput {
  audience: BroadcastAudienceInput!
  body: String
  title: String!
}
```

`mutation.adminBroadcastNotification` is NOT in this build (correct — the mutation field is task 3.2). Full codegen (`bun codegen` + regenerated artifacts) is 3.2's deliverable; the verification artifact was restored post-run to keep this change set byte-limited.

### 2.3 Gate A2 — `bun run test/scripts/run-test.ts backend/lib/gateway/static-assertions.test.ts`

**17 pass / 0 fail / 20 expects, exit 0** — the literal-array-enum scanner (`values: [`) stays green with the new registration (grep over both pothos files → 0 `values:` occurrences).

### 2.4 Repo suite after the pre-task fix (2.M carry-over)

`bun run test/scripts/run-test.ts backend/db/test/logic/notifications/broadcast-audience.repository.test.ts` → **19 pass / 0 fail / 61 expects, exit 0**.

## 3. 3.1.SEC — Security & Tenancy Audit (PASS)

- **Closed input (BOPLA):** both inputs are Pothos whitelists — the SDL above IS the whole surface. Any smuggled field (governance flags, `isDeleted`, `userId`, timestamps, extra companions) dies as a GraphQL validation failure (`GRAPHQL_VALIDATION_FAILED` / "field not defined") BEFORE a resolver runs; the resolver-side probe suite for this is task 3.3's integration matrix.
- **ZERO identity args:** grep over `admin-broadcast.pothos.ts` for `userId|actorId|recipient|userIds|sessionId|studentId|teacherId|parentId` → 0 field hits (only JSDoc prose stating their absence). Recipients are structurally derivable ONLY server-side from the audience selector. The mutation's sole actor source remains `ctx.user.id` (task 3.2).
- **No cross-layer leakage:** the file imports only the builder + the two registered Pothos enums; no service, repo, or schema imports.

## 4. 3.1.SR — Semantic Review (PASS)

- **Enum-object registration form:** `gqlSchemaBuilder.enumType(BroadcastAudienceType, { name: "BroadcastAudienceType" })` — real TS enum object passed, never `values:[...]`; registered ONCE in `shared/enum.pothos.ts` per the CRITICAL rule (domain file imports the registered `BroadcastAudienceTypePothosEnum`, never re-registers).
- **Canonical enums as values:** `BroadcastAudienceType` value-imported from `backend/enum/notifications/`; `UserRole` arrives through the existing `UserRolePothosEnum` registration — no string literals, no local unions, no re-declared shapes.
- **No local type shapes:** 0 `type`/`interface`/`inputRef`/`objectRef` declarations in the new file; inputs are the sanctioned string-named `inputType` exception, and their JSDoc points at the canonical `BroadcastAudienceSelector` / `BroadcastNotificationSubmitInput` in `backend/types/notifications/broadcast.types.ts` (task 1.2) that the resolver must map field-by-field (REQ-032 posture documented in-file).
- **No dead code:** both exported inputs are barrel-wired (3.1) and will be consumed by the mutation args (3.2); the enum registration is consumed by `BroadcastAudienceInput.type`.
- **Comment hygiene:** grep `REQ-[0-9]+|tasks\.md|plan\.md|specs\.md|ai/plans|Task 3|3\.1\.` over all four touched files → **0 hits**.

## 5. 3.1.IV — Instruction Verification (PASS)

| Rule source | Check |
|---|---|
| `backend/graphql/pothos/AGENTS.md` — Pothos Enum Registration Pattern (CRITICAL) | enum defined in `backend/enum/` (task 1.1) ✓; registered ONCE in `shared/enum.pothos.ts` via enum-object form ✓; domain file imports the registered Pothos enum from `shared/enum.pothos` ✓; no inline enum, no `values:[...]`, no string-union re-declaration ✓; regeneration command run (SDL stage) ✓ |
| `backend/graphql/AGENTS.md` — Type Definition Pattern / input exception | inputs use `inputType` (string-named; sanctioned input-type exception), no local type definitions, canonical shapes referenced in JSDoc ✓; locale/`ctx.t` rules untouched (no resolver in this task) ✓ |
| `backend/graphql/AGENTS.md` — gateway registration contract (REQ-018) | `public-operations.ts` UNTOUCHED (admin op is never public) ✓; idempotency-key propagation rules untouched ✓ |
| `.agents/instructions/backend.instructions.md` | no `console.*` ✓; no `oxlint-disable` ✓; barrels `export *` with `./` paths ✓; no nested ternaries ✓; enums as value imports ✓ |

## 6. Deviations / operational notes

1. **Sandbox branch drift (persistent, ≥6 resets this session):** HEAD was repeatedly force-reset to `main` BETWEEN tool calls, silently discarding tracked-file edits (the 2.M outcome documented the same behavior). Mitigation: every workspace command ran through a self-healing prelude (`checkout -f feat/dev3-022d-broadcast-notifications` → re-apply the four edits from `/tmp/t31` masters → grep-verify → then execute). All evidence above was produced under verified HEAD = the branch tip (924f656) with the edits live. **Orchestrator should re-verify `git status` shows these 4 files before committing** — if drift struck after this outcome was written, re-applying is a 4-file copy (masters preserved at `/tmp/t31/`).
2. `biome:check --write` (sub-loop stage) reformatted the long `AdminBroadcastNotificationInput` `inputType` call to its canonical multiline form — repo-canonical formatting retained.
3. The generated `schema.graphql` byte-diff during verification matched exactly the three new type blocks; restored post-verification (see §1) so the 3.1 commit stays clean.

## 7. Carry-forward for 3.2 / 3.3

1. **3.2 (mutation + freeze):** run `bun run generate:gqlSchema && bun codegen` and commit `frontend/graphql/generated/**` in the same change set; insert `adminBroadcastNotification` into `FROZEN_MUTATION_FIELDS` (sdl-static-assertions, alphabetical) and `PRE_3_1_MUTATION_FIELDS` (schema-surface — HARD `toEqual`, per plan-review F-5 there is NO additions array for mutations), `"BroadcastAudienceType"` into `PRE_3_1_ENUMS`, and the three new names into `PRE_3_1_TYPE_NAMES`. Import the input types from `@/backend/graphql/pothos/notifications/admin-broadcast.pothos` (or the domain barrel) — the `authScopes: { $all: { authenticated: true, role: [UserRole.Admin] } }` precedent at `backend/graphql/mutation/admin/admin-users.mutation.ts:64-69` applies unchanged.
2. **3.3 (integration matrix):** the BOPLA probes should post unknown input fields against `AdminBroadcastNotificationInput` / `BroadcastAudienceInput` (e.g. `{ title: "x", audience: { type: All, userId: 1 } }`) and expect `GRAPHQL_VALIDATION_FAILED` pre-resolver; the authScopes extension-introspection pin mirrors `handshake-code-surface.test.ts:9-27`.
3. **Wire-value nuance for tests/frontend:** GraphQL wire values are the enum KEYS (`All`, `Role`, `Country`, `Plan`); the service/repository still receive the runtime strings (`"all"`…) via Pothos's enum mapping — tests must send key-case values, not lowercase.
4. **`BROADCAST_MAX_RECIPIENTS`** stays service-exported — the GraphQL layer must not re-declare it (carried from 2.4).
