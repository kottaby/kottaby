# Task 1.4 — New i18n Namespace `AdminBroadcasts` + `broadcasts` Dashboard Label

**Plan:** `ai/plans/sprint_3/dev3-022d-broadcast-notifications-system-wide-targ`
**Task:** 1.4 (Phase 1 — Types, Enums & i18n Foundations)
**Agent:** Implementation Executor (shared/locale only; zero backend/frontend/app files touched)
**Result:** ✅ COMPLETE — namespace fully registered per the `shared/AGENTS.md` checklist; 27-slot key set per plan §5.5; all parity suites green; `bun tsgo` stays at 0 errors.
**Requirements:** REQ-051 (Localized Messages Only), REQ-063 (compose page i18n substrate), REQ-064 (nav label substrate), REQ-074 (namespace parity test).

---

## 1. Files created (5)

| File | Content |
|---|---|
| `shared/locale/types/adminBroadcasts/index.ts` | `AdminBroadcastsLabels` interface — 27 slots (26 strings + 1 plural function `successToast(count: number)`), `readonly`, JSDoc per key; header documents the oracle-hygiene posture (NO recipient-count preview key — REQ-063/DB-6; `successToast(count)` is the ONLY carrier of the count, resolved AFTER the write; the count is the sole interpolated value — SEC audit anchor). |
| `shared/locale/en/adminBroadcasts/index.ts` | `adminBroadcastsEn: AdminBroadcastsLabels` — full English copy (§3 below). |
| `shared/locale/ar/adminBroadcasts/index.ts` | `adminBroadcastsAr: AdminBroadcastsLabels` — full Arabic copy; `successToast` branches exactly on the `notificationsAr.markAllResult` precedent (`shared/locale/ar/notifications/index.ts:23-29`): 0 zero-form / 1 singular / 2 dual / 3–10 few-plural / 11+ counted-singular. |
| `shared/locale/namespaces/adminBroadcasts/adminBroadcasts.namespace.ts` | `export const AdminBroadcasts = defineNamespace<AdminBroadcastsLabels>("adminBroadcasts.adminBroadcasts", t => t.adminBroadcastsTranslations);` — handle is a CONST (never a string at call sites); stable string ID per the browser-translation-cache rule. |
| `shared/locale/namespaces/adminBroadcasts/index.ts` | Barrel `export * from "./adminBroadcasts.namespace";` (matches `namespaces/notifications/index.ts` shape — the registry imports the folder, so the barrel is required wiring). |
| `shared/locale/adminBroadcasts-namespace.parity.test.ts` | 54 tests / 311 expect() calls — see §5. |

## 2. Files updated (8)

| File | Change |
|---|---|
| `shared/locale/namespaces/index.ts` | Registry: `import { AdminBroadcasts }` + `export * from "./adminBroadcasts";` + `AdminBroadcasts` in the `namespaces` const (alphabetical, before `AdminUsers`). |
| `shared/locale/types/message.ts` | `import type { AdminBroadcastsLabels }` + `adminBroadcastsTranslations: AdminBroadcastsLabels;` on the `Translations` interface (placed after `adminUsersTranslations`, before `notificationsTranslations`). |
| `shared/locale/en/messages.ts` | `import { adminBroadcastsEn }` + `adminBroadcastsTranslations: adminBroadcastsEn,`. |
| `shared/locale/ar/messages.ts` | `import { adminBroadcastsAr }` + `adminBroadcastsTranslations: adminBroadcastsAr,`. |
| `shared/locale/types/dashboard/index.ts` | `broadcasts: string` added to `DashboardLabels` (after `audit`, mirroring the nav insertion point; JSDoc "Sidebar nav: Broadcasts link (admin — the broadcast compose surface)"). |
| `shared/locale/en/dashboard/index.ts` | `broadcasts: "Broadcasts",` after `audit`. |
| `shared/locale/ar/dashboard/index.ts` | `broadcasts: "الإعلانات",` after `audit`. |
| Plan-bundle protocol files | `tasks.md` (1.4 + five subtask checkboxes → `[x]`), this outcome file, `worklog.md`. |

**Deliberately NOT touched:** `shared/locale/{types,en,ar}/errors/index.ts` (concurrent task 1.3's ownership — verified only additive keys landed there from the sibling agent; my runs confirm their parity walker stays green). No backend/frontend/app files. No `git commit`.

## 3. Full key set (Phase 4 briefing — en + ar copy)

27 slots, exact plan §5.5 key order. `successToast(count: number)` is the only function; every other slot is a static string.

| # | Key | en | ar |
|---|---|---|---|
| 1 | `pageTitle` | Send a Broadcast | إرسال إعلان |
| 2 | `pageSubtitle` | Compose a system-wide announcement and deliver it to every recipient in your chosen audience. | أنشئ إعلاناً على مستوى النظام وسلّمه إلى كل مستلم ضمن الجمهور الذي تختاره. |
| 3 | `titleLabel` | Title | العنوان |
| 4 | `titlePlaceholder` | e.g. Scheduled maintenance this Friday | مثال: صيانة مجدولة يوم الجمعة |
| 5 | `titleRequired` | Title is required. | العنوان مطلوب. |
| 6 | `bodyLabel` | Message | نص الإعلان |
| 7 | `bodyPlaceholder` | Write the announcement your recipients will see. | اكتب الإعلان الذي سيظهر للمستلمين. |
| 8 | `audienceLabel` | Audience | الجمهور المستهدف |
| 9 | `audienceAll` | All users | جميع المستخدمين |
| 10 | `audienceRole` | By role | حسب الدور |
| 11 | `audienceCountry` | By country | حسب الدولة |
| 12 | `audiencePlan` | By subscription plan | حسب خطة الاشتراك |
| 13 | `roleLabel` | Role | الدور |
| 14 | `countryLabel` | Country | الدولة |
| 15 | `countryPlaceholder` | e.g. Egypt | مثال: مصر |
| 16 | `countryHelperText` | Matched exactly against each recipient's registered country, up to 100 characters. | تتم المطابقة التامة مع الدولة المسجلة لكل مستلم، بحد أقصى 100 حرف. |
| 17 | `planLabel` | Subscription plan | خطة الاشتراك |
| 18 | `planLoading` | Loading plans… | جارٍ تحميل الخطط… |
| 19 | `previewDisclaimer` | Recipients are resolved at send time — no recipient list is shown before sending. | يُحدَّد المستلمون وقت الإرسال — لا تُعرض قائمة المستلمين قبل الإرسال. |
| 20 | `confirmTitle` | Send this broadcast? | إرسال هذا الإعلان؟ |
| 21 | `confirmBody` | The announcement will be delivered as a notification to every recipient in the selected audience. | سيتم تسليم الإعلان كإشعار لكل مستلم ضمن الجمهور المحدد. |
| 22 | `confirmAction` | Send now | إرسال الآن |
| 23 | `cancelAction` | Cancel | إلغاء |
| 24 | `sendAction` | Send broadcast | إرسال الإعلان |
| 25 | `sendingAction` | Sending… | جارٍ الإرسال… |
| 26 | `successToast(count: number)` | 0 → "No recipients were reached" · 1 → "Broadcast sent to 1 recipient" · n → "Broadcast sent to N recipients" | 0 → "لم يتم إشعار أي مستلم" · 1 → "تم إرسال الإعلان إلى مستلم واحد" · 2 → "تم إرسال الإعلان إلى مستلمين" · 3–10 → "تم إرسال الإعلان إلى N مستلمين" · 11+ → "تم إرسال الإعلان إلى N مستلماً" |
| 27 | `errorTitle` | Couldn't send the broadcast | تعذّر إرسال الإعلان |

Dashboard bundle (NOT the AdminBroadcasts namespace — one-owner rule so the future `navItems.test.ts` ownership check passes):

| Key | en | ar |
|---|---|---|
| `broadcasts` | Broadcasts | الإعلانات |

## 4. Subtask pipeline evidence

- **1.4.QL Quality Loop** — `bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates` executed on ALL 13 touched/created ts files (`types/adminBroadcasts/index.ts`, `en|ar/adminBroadcasts/index.ts`, `namespaces/adminBroadcasts/{adminBroadcasts.namespace,index}.ts`, `namespaces/index.ts`, `types/message.ts`, `en|ar/messages.ts`, `types/dashboard/index.ts`, `en|ar/dashboard/index.ts`, parity test): **exit 0 for every file** ("All checks for lifecycle \"duplicates\" passed").
- **1.4.TE Test Engineering** — via the mandated runner (`bun run test/scripts/run-test.ts`, `.env.test` loaded; NEVER raw `bun test`):
  - `shared/locale/adminBroadcasts-namespace.parity.test.ts` → **54 pass / 0 fail**, 311 expect(), exit 0.
  - `shared/locale/notifications-namespace.parity.test.ts` (regression) → **64 pass / 0 fail**, exit 0.
  - `shared/locale/errors-namespace.parity.test.ts` (regression, incl. task 1.3's four new flat keys) → **8 pass / 0 fail**, exit 0.
  - Full directory per tasks.md: `bun run test/scripts/run-test.ts shared/locale` → **194 pass / 0 fail across 7 files** (adminUsers, applicant, adminBroadcasts, errors, handshakeCode, notifications, plans), exit 0.
  - `bun tsgo` → **exit 0, 0 "error TS" lines** (baseline 0 preserved — the compile-time parity gate holds: both leaf consts typed `AdminBroadcastsLabels`).
  - `bunx @biomejs/biome check shared/locale` (read-only) → 83 files, no errors/warnings.
- **1.4.SEC Security & Tenancy Audit** — PASSED. Placeholder inventory: the namespace's ONLY interpolated value is the numeric `count` of `successToast(count: number)` (typed `number` — no string channel). No label receives copy bodies, ids, recipient lists, country/plan values, or any other server data; `previewDisclaimer` statically pins the oracle-hygiene posture (no pre-send recipient disclosure, REQ-063/DB-6); `confirmBody` is deliberately non-interpolated (static audience summary per plan §5.6); `countryHelperText` documents the exact-match/≤100-char contract instead of echoing user input. Documented in the type-file header + JSDoc. Grep evidence: no `console.*`, no `@/backend|@/frontend|@/app` imports in any touched file.
- **1.4.SR Semantic Review** — PASSED. The handle is exported as a CONST (`AdminBroadcasts`) and consumed as a const; the id string `"adminBroadcasts.adminBroadcasts"` appears only at the definition site (and once as an id pin assertion in the parity test, mirroring the notifications gate). Zero `Translation` enum references anywhere in the touched surface (grep: none). The top-level interface is the existing `Translations` in `shared/locale/types/message.ts` (extended, not renamed); the new leaf interface follows the `*Labels` convention. All imports use `@/shared/locale/...` alias (only exception: the sanctioned sibling `export * from "./adminBroadcasts.namespace"` barrel).
- **1.4.IV Instruction Verification** — `shared/AGENTS.md` "Namespace Registration" checklist: (1) interface in `types/<ns>/index.ts` ✅; (2) `ar/<ns>/index.ts` + `en/<ns>/index.ts` ✅; (3) exported in `types/message.ts` (`Translations` — the current MessageSchema surface) ✅; (4) "namespacePaths in serverLegacy.ts" — VERIFIED STALE: no `serverLegacy.ts`/`namespacePaths` exists in the tree (grep hits are only old plan docs + AGENTS.md prose); actual server resolution is `shared/locale/server.ts` `messagesByLocale` over the full `Translations` object, which picks up the new key with zero extra registration ✅; (5) LocaleProvider SSR wiring — N/A: `frontend/providers/LocaleProvider.tsx` threads only the locale (no per-namespace map), and AdminBroadcasts is a client-surface namespace (`useAppTranslation(AdminBroadcasts)` → `handle.getLabels`) ✅. Plus the browser-cache rule: stable string ID passed to `defineNamespace` ✅. Public barrels: `shared/locale/index.ts` re-exports `./namespaces` (new handle flows through automatically); `shared/locale/types/index.ts` does not list notifications/adminUsers either — deep imports are the established pattern (matches AGENTS.md "prefer deep imports"). `bun tsgo` run after barrel changes per the AGENTS.md rule ✅.

## 5. Parity test coverage (`shared/locale/adminBroadcasts-namespace.parity.test.ts`)

Modeled on `notifications-namespace.parity.test.ts`; 54 tests:

1. **Key-set mirror** — identical sorted key sets ar/en; every slot non-empty string or function on BOTH maps (symmetric sweep); each of the 27 mandated keys present on both; inventory exhaustive (no silent key minting).
2. **Audience-option coverage** — exactly the four `audience*` option keys + `audienceLabel`; each option non-empty in both locales.
3. **No-English-fallthrough pins** — every ar STRING slot matches `/[\u0600-\u06FF]/`; the ar FUNCTION slot returns Arabic-script output.
4. **Pluralization pin** — `successToast` exact outputs at 0/1/2/3/10/11/42/5000 (5000 = the BROADCAST_MAX_RECIPIENTS cap boundary) in BOTH locales; interpolated-count placeholder parity (`toContain("5")`/`toContain("27")` both locales).
5. **Registry + bundle wiring** — `AdminBroadcasts` in the `namespaces` const; `id === "adminBroadcasts.adminBroadcasts"`; `getLabels(enMessages|arMessages)` resolves the bundle slices; `adminBroadcastsTranslations` present on both bundles; slice IS the leaf map (no re-wrapper).
6. **Function-slot inventory** — `successToast` is a function on BOTH maps and the ONLY function-valued slot (string/function split stable).
7. **Dashboard nav label** — `broadcasts` resolves non-empty in both locales from the dashboard bundle slices; ar value carries Arabic script.

## 6. Carry-forward knowledge for Phase 4 (4.2–4.4)

1. **Handle usage:** `import { AdminBroadcasts } from "@/shared/locale"` (or `@/shared/locale/namespaces/adminBroadcasts`) then `const t = useAppTranslation(AdminBroadcasts);` — all 27 keys via property access (`t.successToast(count)` for the toast). For the Server Component page metadata (task 4.2), `getTranslations(locale).adminBroadcastsTranslations` (synchronous, single-arg).
2. **Dashboard nav label (4.4):** use `broadcasts` from the DASHBOARD bundle (`labelKey: "broadcasts"` → `dashboardTranslations.broadcasts`) — NOT the AdminBroadcasts namespace; the one-owner test asserts nav labels resolve from `dashboardTranslations`.
3. **Plural contract:** `successToast(count)` consumes the mutation's returned `Int!` recipient count. Arabic branches 0/1/2/3–10/11+ are exact-pinned in the parity suite — any copy change must update `shared/locale/adminBroadcasts-namespace.parity.test.ts` rows in the same edit (both locales' expected strings live in one `test.each` table).
4. **No count-preview label exists by design** — Phase 4 must not add a recipient-count preview (REQ-063/DB-6); the parity suite's exhaustive 27-key inventory will FAIL if a key like `recipientPreview` is minted, which is the intended tripwire.
5. **`titleRequired`** is the client-side inline validation copy; server-side `BROADCAST_TITLE_INVALID` uses the errors-namespace flat key `broadcastTitleInvalid` (task 1.3) — do not cross-wire them.
6. **Concurrent-work note:** tasks 1.1–1.3 landed in parallel (enum, canonical types, 4 errors keys). The errors namespace files were being edited by the sibling agent while this task ran; zero conflicts occurred (disjoint files; my `messages.ts` edits are additive imports/keys unrelated to their errors key additions).

## 7. Verification snapshot (2026-09-02)

```
sub-loop --lifecycle duplicates → exit 0 ×13 files
run-test shared/locale/adminBroadcasts-namespace.parity.test.ts → 54 pass / 0 fail (exit 0)
run-test shared/locale/notifications-namespace.parity.test.ts   → 64 pass / 0 fail (exit 0)
run-test shared/locale/errors-namespace.parity.test.ts          → 8 pass / 0 fail (exit 0)
run-test shared/locale                                          → 194 pass / 0 fail across 7 files (exit 0)
bun tsgo                                                        → exit 0, 0 error lines
biome check shared/locale (read-only)                           → 83 files, clean
```
