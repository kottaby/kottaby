# 6.1-R1a — Review Wave 1a: canonical-type discipline (review-types)

Reviewer: 6.1-R1a (read-only wave, 1 of 4 parallel)
Scope: canonical-type discipline over the DEV3-022d plan's type-surface files (branch `feat/dev3-022d-broadcast-notifications` content @ `9af3e6a`, baseline tsgo 0 / biome 0 / lint 0).
Baseline for "changed": plan's first commit `b020816` (broadcast foundations) through HEAD; contract file diffed against pre-plan `7449297`.

Files reviewed in full:
- `backend/types/notifications/broadcast.types.ts` (new, 51 lines)
- `backend/types/notifications/index.ts`
- `backend/types/contracts/admin-audit.contract.types.ts` (widened)
- `backend/enum/notifications/broadcast-audience-type.enum.ts` + `.test.ts` + `index.ts`
- `backend/graphql/pothos/notifications/admin-broadcast.pothos.ts`
- `backend/graphql/mutation/notifications/admin-broadcast.mutation.ts`
Plus consumers touched for verification: `backend/graphql/pothos/shared/enum.pothos.ts`, `backend/services/notifications/admin-broadcast.service.ts`, `backend/db/repo/notifications/broadcast-audience.repository.ts`, `backend/services/admin/audit.service.ts`, `backend/services/admin/user-management.helpers.ts`, `backend/types/contracts/contracts.conformance.test-d.ts`, `backend/types/audit/audit-log.types.ts`, `backend/db/schema/audit/audit-logs.ts`, `frontend/views/admin/broadcasts/broadcast-compose.helpers.ts`, `frontend/graphql/generated/{gql/graphql.ts,schema.graphql}`.

No files were modified; no builds/tests/git state changes run (read-only git log/diff/show queries only).

---

## Findings

[LOW] backend/types/contracts/contracts.conformance.test-d.ts:173-180 — The designated type-level conformance suite was NOT extended to pin the plan's `AuditLogWriteContract.entityId` widening: all five AuditLogWriteContract probes (positives :174-180, :249-258; negatives :305-314, :316-325, :507-515) use `entityId: 1` only — there is no positive `entityId: null` `satisfies AuditLogWriteContract` assertion. A future revert of the widening (back to `readonly entityId: number`) would therefore not fail the conformance suite itself; it is currently guarded only transitively, by the real consumer `backend/services/notifications/admin-broadcast.service.ts:395` (`entityId: null` in the broadcast audit write) failing tsgo. Recommended: add one positive `entityId: null` probe to the POSITIVES block. (Also recommended for symmetry: a negative probe asserting a non-numeric `entityId` still errors — currently covered only incidentally by the actionType negative.)

---

## Checklist evidence (tasks.md 6.1)

1. **All type shapes live in backend/types/ — no duplicate/parallel definitions.** ✅
   - `BroadcastAudienceSelector` / `BroadcastNotificationSubmitInput` are declared ONLY in `backend/types/notifications/broadcast.types.ts:25,47` (repo-wide grep: every other hit is plan/spec docs or generated artifacts).
   - `admin-broadcast.pothos.ts` registers wire inputs via `gqlSchemaBuilder.inputType` with NO local type shapes (its header documents the canonical home); the mutation resolver maps field-by-field (:88-98) — never a spread.
   - Frontend consumes the codegen `BroadcastAudienceInput` / `BroadcastAudienceType` from `frontend/graphql/generated/gql/graphql.ts:75-87` (`broadcast-compose.helpers.ts:21,125`); `ComposeState` (:33-40) is the controlled form draft, a distinct-purpose shape, not a parallel wire definition.
   - `ValidatedAudienceSelector` (`admin-broadcast.service.ts:89-93`) is a discriminated narrowing union, structurally assignable to the canonical selector, explicitly ratified in `outcome/2.4-outcome.md` SR ("the one local union … a narrowing aid … not an entity shape") — not a violation.
   - The engine (`notification-engine.service.ts`) carries no audience/selector concepts (grep: 0 hits) — clean layering; the broadcast service maps into the engine's per-user `emitForUsers` contract field-by-field (:371-384).

2. **Enum VALUE import at runtime, type-only where type-only — every importer checked.** ✅
   - `broadcast.types.ts:1-2` — `import type { BroadcastAudienceType }` / `import type { UserRole }` (type-only use in interfaces) ✓
   - `enum.pothos.ts:28` — value import, consumed at runtime by `gqlSchemaBuilder.enumType(BroadcastAudienceType, …)` (:145-147, enum-OBJECT form, registered ONCE) ✓
   - `admin-broadcast.pothos.ts:28` — value import of the registered Pothos enum consts (runtime schema construction) ✓
   - `admin-broadcast.service.ts:44-47` — value import; runtime use in the coherence switch + `isBroadcastAudienceType` guard (:190-208) ✓
   - `broadcast-audience.repository.ts:56` — value import; runtime switch cases (:105-199) ✓
   - `admin-broadcast.mutation.ts:49` — `UserRole` value import for the runtime `authScopes` gate (:70); correctly does NOT import the audience enum (passes the Pothos-resolved value through) ✓
   - All five test files — value imports (runtime assertions) ✓; barrels re-export only ✓

3. **entityId widening scoped EXACTLY — no other contract drift.** ✅
   - `git diff 7449297..HEAD -- backend/types/contracts/admin-audit.contract.types.ts`: the ONLY semantic change is `readonly entityId: number` → `readonly entityId: AuditLogSelectType["entityId"]` (+ its JSDoc). `actorId`/`actionType`/`entityType`/`details` and `ActorContextRef` untouched.
   - Semantics verified end-to-end: `AuditLogSelectType` = `typeof auditLogs.$inferSelect` (`audit-log.types.ts:3`); the physical column is nullable `integer("entity_id")` (`audit-logs.ts:39`) → the widened type is exactly `number | null`, matching the comment's "rides the schema-derived column type" claim.
   - Consumers verified: `audit.service.ts:83-89` inserts field-by-field (null accepted); `user-management.helpers.ts:334-348` still builds `entityId: number` (assignable — backwards compatible); conformance test-d compiles (LOW gap above).

4. **Selector/input shapes CLOSED.** ✅
   - Both canonical interfaces: every field `readonly`, no index signatures, no `extends`, no extensibility hatch; the only optional members are the three type-discriminated companion slots (`role?`/`country?`/`planId?`), each explicitly `| null` — a BOPLA probe has no ride (excess properties on fresh literals error; unknown GraphQL input fields die at validation before a resolver runs).
   - Wire inputs mirror this: `BroadcastAudienceInput` carries ONLY type/role/country/planId; `AdminBroadcastNotificationInput` ONLY title/body/audience — no identity field of any kind.

5. **Import path consistency.** ✅
   - All scoped leaf files use `@/` aliases exclusively; the three barrel files use relative `./` within their own directory per the repo barrel convention. No relative cross-layer imports. Shared layer imports only `@/shared/...` (`shared/locale/namespaces/adminBroadcasts/adminBroadcasts.namespace.ts:1-2`) — no shared→frontend/backend edges. Frontend reaches broadcast shapes only via codegen + `@/shared/locale/types/adminBroadcasts` — never `@/backend/...`.

6. **backend/types runtime-code discipline.** ✅
   - `broadcast.types.ts` is interfaces + JSDoc only, with `import type` only; `backend/types/notifications/index.ts` is two `export *` lines. Zero runtime code. (The `isBroadcastAudienceType` guard correctly lives in the enum module, mirroring the `isNotificationType` precedent — enum modules are runtime modules; types modules are not.)

Additional verifications: registration-once confirmed (enum in `enum.pothos.ts`, inputs in the domain pothos file, mutation side-effect-imported through `mutation/notifications/index.ts:15` with no second registration; pothos barrel re-exports inputs while the resolver imports the leaf module per convention); regenerated artifacts committed in the same change set (`schema.graphql:198-210`, `gql/graphql.ts:75-87` — codegen enum uses wire names `All/Country/Plan/Role` over runtime strings, the documented UserRole precedent, mapped by the GraphQL enum layer).

## Verdict

- CRITICAL: 0
- HIGH: 0
- MEDIUM: 0
- LOW: 1

**PASS** (no CRITICAL/HIGH findings; 1 LOW — conformance-suite pin of the widened `entityId: null` branch, currently guarded transitively by tsgo via the broadcast service's null audit write).
