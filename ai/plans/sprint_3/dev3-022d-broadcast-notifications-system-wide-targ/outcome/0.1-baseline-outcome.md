# Task 0.1 — Pre-Implementation Baseline (Error Ledger + Deferred-Items Seeding)

**Plan:** `ai/plans/sprint_3/dev3-022d-broadcast-notifications-system-wide-targ`
**Task:** 0.1 (Phase 0) — record error baseline + initialize deferred-items ledger
**Agent:** Baseline Executor (MEASUREMENT + LEDGER only — zero production code, zero source files modified)
**Captured:** 2026-09-02T04:08–04:12Z on branch `feat/dev3-022d-broadcast-notifications` (bun 1.3.14, PostgreSQL 17.11 via /tmp/pgsock:5432, DB `kottaby_db`)

---

## 1. Baseline Counts (reference for ALL later review waves)

> **Standing rule for Phases 2.M / 5.2 / 6.1 / 7.3:** deltas vs these numbers MUST be ZERO.
> **Pre-existing issues below are NOT new findings and MUST NOT be "fixed" by later tasks.**

| Metric | Count | Command | Exit code | Verdict |
|---|---|---|---|---|
| `tsgo` errors (`grep "error TS" \| wc -l`) | **0** | `bun tsgo 2>&1 \| grep "error TS" \| wc -l` | 0 | ✅ clean |
| `biome` warnings (`grep -c "warn"`) | **0** | `bunx @biomejs/biome check .` (READ-ONLY) | 1 (format error, pre-existing — see §2) | ✅ zero warnings |
| `lint` (ESLint via lint-service) problems | **0** (empty output, success:true) | `bun run scripts/lint-service.ts --json --id baseline` | 0 | ✅ clean |
| `git diff --name-only` | **EMPTY (0 files)** | `git diff --name-only \| wc -l` → `0` | — | ✅ clean tree |
| `git stash list` | **EMPTY (0 entries)** | `git stash list` | — | ✅ no stashes |

Raw evidence files: `/tmp/baseline-tsgo.txt`, `/tmp/baseline-biome.txt`, `/tmp/baseline-lint.json`, `/tmp/tsgo-full.log`, `/tmp/biome-full.log`.

## 2. tsgo — exact output evidence

Command: `bun tsgo` (= `bun run scripts/restore-next-env-dts.ts && bun run scripts/lib/run-locked-cmd.ts tsgo tsgo -b --noEmit`), ~14s wall.

```
$ bun run scripts/restore-next-env-dts.ts && bun run scripts/lib/run-locked-cmd.ts tsgo tsgo -b --noEmit
Restored next-env.d.ts to canonical dev dist dir (.next-dev).
[process-lock] Enqueued request for "tsgo" (PID: 2455)
[process-lock] Acquired lock for "tsgo" (PID: 2455). Executing...
[process-lock] Released lock for "tsgo" (PID: 2455)
```

`grep "error TS" /tmp/tsgo-full.log | wc -l` → `0`. **Baseline: 0 type errors.**

## 3. biome — READ-ONLY variant (deliberate substitution)

The npm script `biome:check` is `biome check --write --unsafe .` (MUTATES files). Per task 0.1 instruction, the read-only variant was used instead and no `git checkout` restore was needed:

```
bunx @biomejs/biome check .
→ Checked 1099 files in 6s. No fixes applied.
→ Found 1 error.
grep -c "warn" → 0
```

**Warning count: 0.** The single pre-existing **error** is a formatter diff confined to the **untracked plan artifact `.issue42.json`** (root, `??` in git status — the issue-#42 fetch JSON dropped by the Task-0 orchestrator; `issue_field_values: [\n]` vs `[]`). It is NOT source code, NOT a lint warning, and is ledgered here as pre-existing baseline state — later waves must not "fix" it or count it as a regression.

## 4. lint — `lint-service.ts` CLI (report-only; no `--fix`)

`scripts/lint-service.ts` exists and supports `--json` + `--id` (verified `scripts/lint-service-cli.ts:34`); without `--fix` it is report-only (read-only). Full-repo scope, ~53s.

`bun run scripts/lint-service.ts --json --id baseline` → `/tmp/baseline-lint.json`:

```json
{
  "success": true,
  "output": "",
  "exitCode": 0,
  "metrics": {
    "id": "baseline",
    "scope": "full-repo",
    "fileCount": 0,
    "durationMs": 53207,
    ...
  }
}
```

`exitCode: 0` + `output: ""` ⇒ **0 errors, 0 warnings. Lint baseline: PASS.**

## 5. git baseline snapshot

```
$ git diff --name-only            → (empty; wc -l = 0)
$ git stash list                  → (empty)
$ git status --porcelain          → ?? .issue42.json
                                    ?? .../outcome/   (plan-outcome dir, untracked)
```

Working tree is clean against `feat/dev3-022d-broadcast-notifications` HEAD; the only untracked entries are plan artifacts (issue JSON + this outcome dir). No tracked file modified by this task.

## 6. Deferred-items ledger rows added (D1, D2 — both 📅 Forward, non-blocking)

`ai/plans/sprint_3/dev3-022d-broadcast-notifications-system-wide-targ/deferred-items.md` seeded with:

| ID | Deferred Item | Source Task | Target Task | Status | Verified By | Notes |
|---|---|---|---|---|---|---|
| D1 | Chunked mega-broadcast (>5000 recipients) | 0.1 | future scale ticket | 📅 Forward | phase 0 | per DB-4: recipient cap 5000 fail-closed; chunking deferred |
| D2 | Crash-between-commit-and-publishReceipts double-insert residual | 0.1 | engine hardening stream | 📅 Forward | phase 0 | engine §3.6 document-locked posture; owned by engine team |

Post-5.2-gate invariant — precision note: the **ledger rows** contribute **zero** ❌/⚠️ (both are 📅 Forward with owners/target tickets). A literal `grep -c "❌\|⚠️" deferred-items.md` returns **2**, but both hits are the template's own *Status Values legend* lines (⚠️ Partial / ❌ Blocked — committed with the template at a259524, pre-existing). The 5.2 gate should therefore be interpreted as "no ❌/⚠️ **rows** in the Ledger Table" (or the grep scoped to the table block). Task 0.2 (run in parallel) confirmed the ledger needs no additional rows. Ledger file is git-tracked (plan bundle committed in a259524); post-task `git diff --name-only` = exactly `deferred-items.md` — the mandated deliverable edit (baseline `git diff` was captured empty BEFORE seeding, per task step order).

## 7. Quality-loop note (reported, not fixed — per sub-loop's own Fix-Or-Report rule)

`bun run scripts/health/sub-loop.ts <file.md> --lifecycle duplicates` structurally **exits 1 for every markdown file** because its oxlint stage reports `No files found to lint. Please check your paths and ignore patterns.` on `.md` inputs. Control test: the **untouched** `worklog.md` produces the identical failure (exit 1, tsgo stage ✅, oxlint "No files found"). This is a pre-existing tool limitation independent of my edits; wiring oxlint to accept `.md` would require modifying lint scripts/ignore patterns — outside this task's no-source-changes mandate, so it is REPORTED per the sub-loop's printed rule. The tsgo stage of the sub-loop passed ✅ for both edited ledger files.

## 8. Files touched by task 0.1 (none are production source)

1. `ai/plans/.../deferred-items.md` — +2 ledger rows (D1, D2)
2. `ai/plans/.../outcome/0.1-baseline-outcome.md` — this file
3. `worklog.md` — Task 0.1 section appended
