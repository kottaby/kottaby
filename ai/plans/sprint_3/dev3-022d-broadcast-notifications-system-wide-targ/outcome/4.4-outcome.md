# Task 4.4 Outcome — Navigation Integration (admin nav item)

- **Plan:** DEV3-022d Broadcast Notifications (System-Wide & Targeted)
- **Task:** 4.4 [Navigation integration — admin nav item] — REQ-064
- **Branch:** `feat/dev3-022d-broadcast-notifications` @ `c7f7b35` (verified before every sensitive op; NO commit made)
- **Status:** COMPLETE — all subtasks green

## Scope actually touched (F-2 path correction applied)

Plan-review F-2 confirmed the real paths carry a `nav/` segment (the plan's
`frontend/views/dashboard/navItems.ts` citation is stale). Real files edited:

1. `frontend/views/dashboard/nav/navItems.ts`
2. `frontend/views/dashboard/nav/navItems.test.ts`

NO other files touched. NO mobile bottom-nav work (none exists — shared Drawer
list renders `NAV_ITEMS_BY_ROLE` directly via `DashboardSidebar.tsx` /
`SidebarListItem.tsx`; both untouched). Task 4.3's
`frontend/views/admin/broadcasts/BroadcastComposeContainer.tsx` NOT touched.

## Change 1 — navItems.ts (single ADD, 4 inserted lines total)

- Import: `CampaignOutlined,` added to the existing `@mui/icons-material`
  block (verified available — tsgo project-wide pass resolves the export; no
  alias used so the item is byte-identical to the mandated shape
  `{ route: "/admin/broadcasts", labelKey: "broadcasts", Icon: CampaignOutlined }`).
- Admin array: exactly ONE new entry inserted directly AFTER the `audit`
  entry, BEFORE `/profile`:
  `{ route: "/admin/broadcasts", labelKey: "broadcasts", Icon: CampaignOutlined },`
- Doc-comment: one bullet added to the canonical-routes list marking this a
  pure ADD (not a retarget) per DEV3-022d / REQ-064.
- Label key `broadcasts` resolves from the dashboard bundle ONLY (pre-existing
  from task 1.4: `shared/locale/types/dashboard/index.ts:43`,
  `en/dashboard/index.ts:17` "Broadcasts", `ar/dashboard/index.ts:17`
  "الإعلانات") — verified present on this branch before edit; no locale files
  touched by 4.4.

## Change 2 — navItems.test.ts (extended gates, 49 inserted lines)

Header "Verifies" list gains the REQ-064 (DEV3-022d) bullet. New describe
block `Admin broadcasts navigation (REQ-064, DEV3-022d)` mirroring the audit
precedent (DEV3-020 bbcb363):

- Admin list carries EXACTLY ONE `/admin/broadcasts` entry with
  `labelKey: "broadcasts"`, placed at `auditIndex + 1` (verbatim "after the
  audit entry" mandate pinned).
- `broadcasts` key stays owned by the dashboard bundle
  (`"broadcasts" in dashboardEn === true`, `in handshakeCodeEn === false`) —
  one-owner runtime pin.
- Label resolves non-empty in BOTH locales (ar + en) through the sidebar's
  real runtime path `resolveNavItemLabel`, and equals
  `dashboardLabels.broadcasts` (owner-bundle equality, zero hardcoded copy —
  translation discipline kept).
- `test.each` over Student/Teacher/Parent: NO non-admin role sees
  `/admin/broadcasts` (extended role-gating).
- ALL pre-existing tests byte-identical (role-gating + one-owner ownership
  suites untouched and still green).

## Subtask pipeline results

- **4.4.QL** — `bun run scripts/health/sub-loop.ts frontend/views/dashboard/nav/navItems.ts --lifecycle duplicates`
  → **exit 0**; tsgo pass, oxlint pass, biome:check pass, lint:type-aware pass,
  check:duplicates (jscpd intra-file) pass.
- **4.4.TE** — `bun run test/scripts/run-test.ts frontend/views/dashboard/nav/navItems.test.ts`
  → **26 pass / 0 fail, exit 0** (132 expect calls). Also re-run in the
  tasks.md 4.4.TE directory form `bun run test/scripts/run-test.ts frontend/views/dashboard`
  → **26 pass / 0 fail, exit 0**. Extended role-gating + one-owner ownership
  tests green.
- **4.4.SEC** — non-admin nav arrays provably unchanged (diff-pinned):
  sha256 of each extracted role block, pristine `c7f7b35` vs modified —
  Student `97fe87306fd32258`, Teacher `1ca5c5effe2e93dc`,
  Parent `9aa7bc403d213dda` — all **BYTE-IDENTICAL**. Full unified diff saved
  at `/tmp/t44/final-navItems.patch` + `/tmp/t44/final-navItems.test.patch`:
  navItems.ts = 3 hunks (import / doc-comment / admin-array line);
  test file = 2 hunks (header comment / appended describe). Zero hunks inside
  any non-admin array or any pre-existing test body.
- **4.4.SR** — single ADD verified: exactly one `/admin/broadcasts` entry
  app-wide (test-enforced `toHaveLength(1)`), no duplicate/retargeted entries
  (no pre-existing entry claimed — grep of pristine file shows zero
  `broadcast` occurrences), `broadcasts` key lives in the dashboard bundle
  only (both-locale bundle grep + ownership test).
- **4.4.IV** — `.agents/instructions/frontend.instructions.md` verified:
  `*Outlined` icon naming (`CampaignOutlined` OK), no style props touched
  (data-only module), no hardcoded user-facing copy (labels via
  `DashboardLabels`), i18n via `@/shared/locale` handles, no `console.*`,
  no `FormEvent`. Existing nav-test conventions followed (describe/test
  structure, `navItemFor` helper, translation-discipline comment, audit
  precedent shape).

## Operational note — shared-worktree race

A parallel agent (task 4.3) repeatedly flips the shared working tree between
`main` and this branch with `git checkout -f`, which resets tracked-file
edits. 4.4 mitigated with staged canonical copies (`/home/z/.t44-staging/`,
`/tmp/t44/`) and atomic verify-apply-test sequences; every green result above
was captured on verified `feat/dev3-022d-broadcast-notifications` with the
`broadcasts` locale keys present. Final tree state re-applied and re-verified
after the runs (see worklog). A first TE attempt failed 3 locale-dependent
tests when a flip landed the tree on `main` (dashboard bundle there lacks
`broadcasts`) — the re-run on the correct tree is the recorded green result.

**Files modified (working tree, NOT committed):** the two files above only.

## Re-implementation note (2026-09-02, task 5.2 recovery)

The original edits documented above were left uncommitted ("Files modified (working tree, NOT committed)") and were destroyed by the sandbox keeper's working-tree resets before the outcome-only commit `6d39288` landed. Task 5.2's full-repo drift checks surfaced the loss (zero `broadcast` occurrences in either file) and re-implemented BOTH files exactly from this outcome's recorded spec: import `CampaignOutlined` (no alias), single admin-array ADD after `/audit`, canonical-routes doc bullet, header "Verifies" bullet + the six-gate `Admin broadcasts navigation (REQ-064, DEV3-022d)` describe. Verification reproduces the recorded evidence exactly — 26 pass / 0 fail, 132 expect() calls, non-admin role blocks byte-identical to pristine `c7f7b35`, sub-loop `--lifecycle duplicates` exit 0. The re-implemented files are committed with the 5.2 gate commit.
