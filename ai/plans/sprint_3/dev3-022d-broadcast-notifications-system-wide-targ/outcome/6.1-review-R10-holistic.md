# 6.1-R10 — Fresh-eyes holistic review (final mandated iteration, 10 of ≥10)

**Reviewer:** 6.1-R10 (holistic pass — the cross-layer/seam sweep no single-layer wave performed)
**Method:** `git diff --stat 70601c9..HEAD` (32 files, every diff read; `BroadcastComposeContainer.tsx` + the journey read IN FULL at HEAD), plus whole-plan-range verification (`a259524..HEAD`, 15 commits): commit-message hygiene, tasks.md checkbox ↔ outcome-file reconciliation, the three standing gate reminders re-proven at HEAD, junk/secret/TODO sweeps, and the R1–R5 fix-now trail re-traced against its neighbors. READ-ONLY — this file is the only artifact written. All prior round files (R1a–R5) read in full first; their findings are treated as known and NOT re-reported unless a seam problem surfaced.

---

## A. Fix-now trail (R1–R5) — every fix landed coherently, with one pin gap

| Fix (source) | Landed as | Neighbor coherence |
|---|---|---|
| Snackbar 44px floor (R1c MEDIUM) | `SNACKBAR_DISMISS_SX = { ...focusVisibleRingSx, minHeight: 44, minWidth: 44 }` (broadcast-compose-skin.ts:24-25), consumed by the snackbar dismiss IconButton (BroadcastComposeSuccessSnackbar.tsx:35) | ✅ adjacent `ACTION_BUTTON_SX` comment fixed to "Confirm/cancel" (skin.ts:12 — R4 F6 closed); consumers of ACTION_BUTTON_SX are exactly submit/confirm/cancel |
| Redis timeouts (R1b LOW) | `commandTimeout: 2_000, connectTimeout: 2_000` + explanatory comment (redis-claim-cache.ts:109-118) | ✅ R2 re-verified fail-open ladder + Tier-3 honest-rejection pin unaffected |
| entityId conformance pin (R1a LOW) | positive `entityId: null` / `notification_broadcast` probe (contracts.conformance.test-d.ts:182-190) | ✅ no orphaned `@ts-expect-error`; R5 INFO residual (no negative upper-bound probe) recorded, no action |
| Copy-preserving kind switch (R4 F1 MEDIUM) | `changeAudienceKind` preserves title/body, resets companions only (BroadcastComposeContainer.tsx:84-96) with rationale comment; `initialComposeState` now used only by the after-success reset | ⚠️ comment present, but NO test pins it — see Finding 1 |
| Per-toast Snackbar remount (R4 F2 LOW) | `successEpoch` state + `key={successEpoch}` (Container:70-72, 131, 181) | ✅ rotation/reset sequence coherent; onClose still nulls count |
| aria-describedby on audience error (R4 F3 LOW) | `AUDIENCE_ERROR_ID` + conditional `aria-describedby` on RadioGroup + `id` on FormHelperText (BroadcastComposeFields.tsx:23, 62, 82) | ✅ |
| Wire member-set pin (R5 LOW) | new schema-surface test: 4 wire names + lowercase runtime values (schema-surface.test.ts:727-747) | ✅ mirrors the sibling AppLocale block exactly |
| Comment drift ×3 (R4 F6 / R1c LOW / R4 carried) | skin.ts:12; helpers `applyBroadcastFieldErrors` doc (broadcast-compose.helpers.ts:134-141); container catch comment (Container:142-145) | ✅ all three now describe reachable behavior; container header (:17-19) consistent |

No half-applied fix and no orphaned comment/test found anywhere on the trail — except Finding 1 below.

---

## B. Findings (NEW, holistic-only)

**[LOW] test/ui/components/admin/BroadcastComposeContainer.test.tsx (whole suite, 12 tests) — the R4 data-loss fix (copy-preserving kind switch) is implemented and doc-commented but pinned by NO test; a revert to the full-wipe form would pass every gate.** None of the 12 test names or bodies exercises a kind switch with pre-authored copy (grep: zero hits for kind-switch/preserve across the file; the e2e never switches kind while copy is present either — its country branch runs after a success reset). R4's own remediation bar for the OLD behavior was "pin it with a test + doc comment"; the fix adopted the preserve branch and wrote the comment but skipped the pin. Regression exposure: restoring `setCompose({ ...initialComposeState, audienceType: kind })` (the exact pre-fix code) passes tsgo/biome/lint, all 12 unit tests, journeys, and the e2e. Recommended: one unit test — fill title/body → click the country radio → expect title input value retained + companion cleared.

**[LOW] ai/plans/sprint_3/dev3-022d-broadcast-notifications-system-wide-targ/tasks.md:48-52, 108-112 — sub-track checkbox drift: all five 1.1.* and all five 2.1.* sub-checkboxes (QL/TE/SEC/SR/IV) are `[ ]` while their main tasks are `[x]` and the outcome files claim the sub-work complete** (1.1-outcome.md: "QL exit 0 on all three, tests 25/25 green"; 2.1-outcome.md: full 4-tier repo suite created and green). Sibling tasks 1.2–1.4/2.2–2.4 have all sub-boxes `[x]`, so this is not a convention — it is stale tracker state from b020816 that contradicts the outcome claims. tasks.md:297 (7.3) demands "Confirm ALL task checkboxes [x]"; these ten boxes will trip that reconciliation — fix them in 7.3 (or now) rather than mass-checking blindly.

**[LOW] tasks.md:215 — main task 4.3 remains `[ ]` (with 4.3.TE/.BF/.BS unchecked), so the review-gate expectation "remaining unchecked are 6.1/7.x only" does NOT literally hold at HEAD.** The partial state IS documented and coherent (4.3-outcome.md + DF-1/DF-2 rows retargeted 📅 Forward with owners + worklog), but the checkbox carries no partial-done annotation, and 7.3's blanket "Confirm ALL task checkboxes [x]" cannot be satisfied for 4.3 without either (a) a partial-completion annotation pointing at DF-1/DF-2, or (b) actually closing DF-1/DF-2. Disposition needed before closure.

**[LOW] deferred-items.md:23-27 — the ledger-ID namespace collision that R3 flagged ("naming conflict between decision-D4 and deferral-D4/D5 invites drift") was NOT resolved; the fix wave EXTENDED it.** plan.md:68-77 defines design decisions D1–D10 (e.g., D6 = the idempotency decision at plan.md:73, D5 = caller-tx emission, D4 = recipient cap, D3 = governance exclusion); plan.md:420 seeds the ledger with D1/D2 (overloading those two IDs from day one); the new rows D3–D7 overload five MORE IDs that exist as plan decisions with different meanings; and `docs/notifications/realtime-engine.md:182` uses D5 for a third thing (governance-context gate). Concretely confusing today: R1b/R2 cite "plan decision D6" for idempotency while deferred-items.md:26's D6 is the batched-amplification deferral. Doc-only, but this is exactly the drift R3 predicted; recommend prefixing the 6.1-sourced rows (e.g., `R-D3`) or renaming before 7.1/7.3 canonize the references.

**[LOW] deferred-items.md (ledger coverage) — several still-open LOW/INFO findings from the waves have neither a fix nor a ledger row, leaving tasks.md:274's "Record every finding as fix-now or ledgered" only partially satisfied.** Unledgered still-open items: R1c LOW lazy-init `useRef(randomUUID())` (BroadcastComposeContainer.tsx:64 — mints/discards a UUID per render), R1c/R4 LOW stale inline field errors (no clear-on-change), R4 F5 LOW outer submit-guard unpinned at unit tier, R3 LOW limiter identity forgeable via `x-forwarded-for` + 429 log-forging (backend/lib/ratelimit.ts:58-70), R2 INFO receipt cross-field consistency gap (emit-idempotency.ts:161-184), R5 INFO negative entityId probe. D3–D7 cover the MEDIUMs and two LOWs (body ceiling, plans-failure) but not these. The 7.3 synthesis must sweep the round files and disposition each (fix, ledger, or explicit accepted-risk) or the gate is unmet on paper.

**[LOW] commit a259524 ("plan: add new plans") — the only one of the 15 plan commits lacking the mandated `Plan:/Phase:/Tasks:` trailers** (it is the bulk multi-plan authoring commit; its body just repeats the subject). All 14 execution commits (b020816…d360f63) carry all three trailers with correct task/phase attribution. Historical, not re-writable without rewriting history — record as a known exception.

**[INFO] test/workflows/notifications/admin-broadcast.journey.test.ts:115 — naming residue of the 5.1 sentinel migration: `EG_TITLE` still embeds "Egypt cohort announcement"** while step 4 now fires the run-unique `COHORT_COUNTRY` cohort and the test/comment text was renamed to "targeted country cohort". The title string is arbitrary inert copy, so nothing breaks; cosmetic only (the constant name will outlive the story it tells).

**[INFO] working-tree hygiene: `.pgbin/`, `.pgdata/`, `.pgsocket/` (ENV-1's user-space PostgreSQL cluster) are untracked AND unignored** (`git check-ignore` exit 1; `git status` shows them as `??`). Nothing was ever committed from them, but the sandbox's own recovery doctrine (5.2 §7: "ANY completed file edit must be committed") makes a future `git add -A` plausible — a one-line .gitignore entry would defuse it. Not a plan-file defect; flag for the environment owner.

---

## C. Holistic verifications that PASSED (evidence)

1. **Layer coherence — one story, no seam disagreement.** Traced input DTO → pothos input → resolver field-by-field mapping → canonical submit type → service validation (title ≤255, country ≤100, planId positive safe int) → repo four-shape resolution → engine emit (`SystemBroadcast`, `relatedEntityType/Id=null`) → audit (`notification_broadcast`, `entityId: null`, metadata-only `{scope, companion, recipientCount}`) → post-commit publish → committed SDL/codegen (`AdminBroadcastNotificationInput!` → `Int!`) → container (field-by-field variables, header-borne key) → pluralized toast of the persisted count. Field names, nullability (`role?/country?/planId? | null` ↔ optional wire slots ↔ `string | null` codegen), and enum spelling (TS values `all/role/country/plan` ↔ wire keys `All/Role/Country/Plan` ↔ codegen key-string values ↔ pothos enum-object registration) agree at every seam; the new schema-surface member pin now locks the last unpinned seam. Journey audit-metadata assertions (`{ country: COHORT_COUNTRY }` etc.) match the service's `buildBroadcastAuditDetails` output key-for-key.
2. **Gates at HEAD (whole plan range `a259524..HEAD`):** `git diff -- backend/db/schema backend/db/migration backend/lib/gateway/public-operations.ts` → EMPTY (schema-drift gate + public-operations six-op freeze intact; `adminBroadcastNotification` correctly NOT allowlisted, `$all` scope pin unchanged); non-admin nav blocks byte-identical (full navItems.ts delta = import + doc bullet + single admin-array ADD after `/audit`, exactly per 4.4 recovery spec); `grep -c "❌\|⚠️" deferred-items.md` → 0.
3. **Checkbox ↔ outcome mapping:** all 20 `[x]` main tasks (0.1–5.2 incl. 4.4, 2.M) have committed outcome files; 4.3's outcome exists for its documented partial (see Finding 3).
4. **No junk, no secrets, no leftovers:** full-range filename scan (`.issue42*`, `*.log`, `/tmp`, `.env`, `*.bak`, secrets) → zero hits; tracked env files are only `.env.example` + `.env.test.ci`; the 5.1-created `.env.test` is untracked; zero TODO/FIXME/HACK/`@ts-ignore` in backend notifications, test trees, and broadcast views; working tree clean apart from the INFO-2 pg dirs.
5. **Round-claims vs code state:** every fix-now claim in the R1–R5 files and the d360f63 message matches actual HEAD content (table in §A); the five ledger rows D3–D7 accurately summarize R1b-MEDIUM/R1d-MEDIUM+LOW/R1d+R2-LOW/R3-MEDIUM/R4-LOW with owners; no outcome file claims a state the code contradicts (only the tasks.md sub-box drift of Finding 2 runs the other way).

---

## Verdict

- **CRITICAL: 0 · HIGH: 0 · MEDIUM: 0 · LOW: 6 · INFO: 2 — PASS.**
- The full-stack story is coherent end-to-end: every fix-now item from R1–R5 landed (one missing regression pin, Finding 1); all three standing gate reminders are provably intact at HEAD; no secrets/junk/TODO residue; no outcome-file claim contradicts the code.
- All six LOWs are bookkeeping/test-pin class, not runtime defects: 1 test pin + 3 plan-artifact reconciliations + 1 ledger-coverage sweep + 1 historical commit-message exception. None blocks the plan; Findings 1–5 should be folded into 7.1–7.3 (7.3's checkbox reconciliation and the canonical doc are the natural landing points).
- Stop-rule reading for the ≥10 protocol: R10 surfaces zero new code defects; the remaining items are tracker hygiene, so waves may close with this file as the holistic record and the dispositions carried into 7.3.

## Commit-log integrity

- Range audited: `a259524..HEAD` = exactly the 15 plan commits (b020816…d360f63 plus the bulk plan-authoring a259524); linear (single-parent) history; no merges.
- `git log --format=full 70601c9..HEAD | grep -i co-authored` → **EMPTY** (exit 1) — zero Co-authored-by trailers anywhere in the plan delta; author = committer = MohammedRamadan11 on all 14 execution commits (no identity churn).
- Trailers: 14/15 execution commits carry correct `Plan:` / `Phase:` / `Tasks:` lines (spot-verified task↔content correspondence for d360f63, 9af3e6a, e61ec46 — the d360f63 fix-now list matches the actual diff item-for-item). The sole exception is a259524 ("plan: add new plans") with no trailers — Finding 6, historical bulk-authoring commit, recorded as a known exception.
- **Commit-log integrity: PASS (14/15 fully conformant, 1 documented exception, 0 Co-authored-by, 0 hygiene violations).**
