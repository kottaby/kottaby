# Task 5.1 — Full-Suite Execution + Security Tier + Race Assertions — Outcome

**Date:** 2026-09-02 · **Branch:** `feat/dev3-022d-broadcast-notifications` (HEAD content; sandbox keeper mirrors it to local `main`) · **Executed by:** orchestrator + agent `51a` (backend sweep) after two gateway timeouts forced small-step local execution.

## 1. Environment setup record

- `~/.bun/bin/bun` → `/usr/local/bin/bun` symlink recreated (run-test.ts hardcodes `BUN_BIN`).
- `/home/z/my-project/.env.test` CREATED (gitignored): `DB_PROVIDER=postgres`, `DATABASE_URL=postgresql://postgres@127.0.0.1:5432/kottaby_db`, `DATABASE_ENCRYPTION_KEY=cafef00d…`, `CACHE_PROVIDER=postgres`, explicit `JWT_ACCESS_SECRET`/`JWT_REFRESH_SECRET`, `SEED_PROFILE=standard`, `ADMIN_EMAIL=admin@app.local`, `AUTH_COOKIE_SECURE=false`, **deliberately NO `REDIS_URL`** (engine + claim cache take the documented fail-open posture; suites inject scripted in-memory caches). Base: `.env.test.ci` + dev `.env`; `backend/lib/env.ts applyDbEnvOverride()` keeps DB keys consistent.
- PostgreSQL 16.2 user-space instance (`.pgbin`, data `.pgdata`, socket `.pgsocket`, port 5432) running; `kottaby_db` migrated (6 migration folders incl. custom 2-functions + 3-immutability-triggers) and standard-seeded (admin@app.local verified, 4 plans).

## 2. Per-suite execution matrix (all through run-test.ts / canonical runners)

| Suite | Command | Result |
|---|---|---|
| backend/enum/notifications | run-test.ts | **50 pass / 0 fail** |
| backend/db (full parallel: test:db) | run-db-tests-parallel.ts | **240 pass / 0 fail** (17 files, 29.8s) |
| backend/db/test/logic/notifications | run-test.ts | **47 pass / 0 fail** |
| backend/types/contracts | run-test.ts | **52 pass / 0 fail** (AuditLogWriteContract widening safe) |
| backend/services/admin | run-test.ts | **110 pass / 0 fail** |
| backend/services/notifications | run-test.ts | 134 pass / 2 fail → **2 isolation bugs fixed** → file re-run **16 pass / 0 fail**; dir re-run green |
| backend/graphql/test (dir) | run-test.ts | 145 pass / 11 fail → triaged: **4 pre-existing** plan-catalog (report-only) + **7 port-3066 contention** → killed leftover `next-server` pid 5512 → all re-runs green |
| backend/graphql/test/admin-broadcast.integration | run-test.ts | **8 pass / 0 fail** (BFLA/BOLA wire probes, `$all` pin, 1-arg surface) |
| backend/graphql (server suite: test:graphql) | run-server-tests.ts | **140 pass / 0 fail** (39s) |
| test/workflows/notifications (journeys) | run-test.ts | 27 pass / 2 fail → **journey country-cohort isolation fixed** → **29 pass / 0 fail** (admin-broadcast steps 1–10 + J1 + J2) |
| frontend/graphql/sharedDocuments/notifications | run-test.ts | **20 pass / 0 fail** |
| frontend/graphql/test/warnings | run-test.ts | **9 pass / 0 fail** (`adminBroadcastNotification` registered in live-mutation inventory) |
| frontend/providers/apollo/utils (authLink) | run-test.ts | **4 pass / 0 fail** |
| shared/locale | run-test.ts | **194 pass / 0 fail** (parity incl. AdminBroadcasts + errors keys) |
| test/ui/components (test:ui:components gate) | canonical script | green until `BroadcastComposeContainer` flow tier → **process SIGABRT (bun 1.3.14 + Happy-DOM mutation render — pre-existing, DF-1)**; container Tier-1: **5 pass / 0 fail**, 7 flow tests crash-blocked |
| test/ui/components remainder (6 dirs) | documented bypass `KOTTABY_TEST_RUNNER_OK=1` + full preloads | **141 pass / 0 fail** (13 files, incl. nav/dashboard) |
| test/ui/page-guards/admin-broadcasts-page | run-test.ts | **6 pass / 0 fail** |
| test/ui/e2e/admin-broadcasts.e2e.test.ts | not executed (browser tier blocked — DF-2, pre-existing 48 `next build` errors) | report-only |

## 3. Security-tier closure (REQ-075)

| Mandated case | Evidence | Verdict |
|---|---|---|
| Hostile audience discriminants + hostile copy reject-or-store-inert | `admin-broadcast.service.test.ts` validation matrix + verbatim-copy test; journey step 6; `admin-broadcast.integration.test.ts` BOPLA probes | ✅ PASSING |
| BFLA — all non-admin roles 403 + second admin cannot cross-alter audit | service suite `non-admin and missing actor → FORBIDDEN`; **NEW** `a seeded second admin may broadcast — audit rows stay append-only and actor-attributed`; journey step 7; integration UNAUTHORIZED/FORBIDDEN wall | ✅ PASSING |
| Replay cannot double-insert / double-publish | service `replay — same key + live cache returns the PRIOR count, zero new rows/audit/publishes`; journey step 2; j2 step 7 | ✅ PASSING |
| Sequential same-key ⇒ ONE row-set; parallel loser fail-open (engine §3.6) | **NEW** `concurrent same-key race` service test (scripted held-claim loser) | ✅ PASSING |
| Corrupt/absent cached receipt ⇒ fail-open insert + warn (by test) | service `cache-claim outage fails OPEN — exactly ONE engine warn`; `forced insert failure rolls the WHOLE unit back` | ✅ PASSING |

## 4. Coverage evidence (scoped `--coverage` runs)

| Module | Statements | Branch | Uncovered |
|---|---|---|---|
| `backend/enum/notifications/broadcast-audience-type.enum.ts` | 100% | 100% | — |
| `backend/db/repo/notifications/broadcast-audience.repository.ts` | 100% | 91.6% | per-case null-companion fail-closed guards (115,126,137,160,181,191,201,217-219) — unreachable through the validated-selector contract (defense-in-depth for direct repo calls) |
| `backend/services/notifications/admin-broadcast.service.ts` | 100% | 94%+ | mirrored defensive guards + hostile-input catch edges |
| `backend/services/notifications/redis-claim-cache.ts` | 87.5% | 86.1% | 116-120 = `redis.on("error")` handler — fires only with a REAL Redis connection failure (env-gated; outage semantics covered at `ioredis-fanout-client` tier) |
| `backend/graphql/pothos/notifications/admin-broadcast.pothos.ts` | 100% | 100% | — |
| `backend/graphql/mutation/notifications/admin-broadcast.mutation.ts` | 50% in-process | 42% | resolver body executes in the spawned test-server process — exercised over the REAL HTTP wire by `admin-broadcast.integration.test.ts` (8/0) but invisible to in-process coverage instrumentation |

**Disposition:** statement coverage is 100% on every plan-new module reachable in-process; residual branch gaps are (a) type-unreachable defensive guards, (b) the environment-gated Redis error handler, (c) wire-tier code proven by the passing HTTP integration suite instead. No uncovered branch corresponds to a reachable, untested behavior.

## 5. 5.1.SR semantic review checklist

- [x] Every test executed through `run-test.ts` / canonical runners (direct `bun test` blocked by repo guard; single documented bypass used for UI dirs orphaned by the DF-1 crash, with full preload parity).
- [x] No `.rejects.toThrow()` in any plan test.
- [x] No `runInRollback` in journey files (journeys commit fixtures + tracked afterAll cleanup).
- [x] No real Redis / real transport anywhere in tests (`REDIS_URL` deliberately absent; `SpiedFanoutTransport` + scripted caches only).

**Verdict: 5.1.SR PASS.**

## 6. Fixes applied during this task (all in plan-scope test files)

1. `backend/services/notifications/admin-broadcast.service.test.ts` — (a) country-cohort test now uses a run-unique country sentinel (committed seed/fixture rows holding the old literal polluted the exact-match cohort); (b) second-admin audit assertion now diffs against a pre-tx baseline (shared DB holds committed broadcast audits from other suites). Sub-loop `--lifecycle duplicates` exit 0.
2. `test/workflows/notifications/admin-broadcast.journey.test.ts` — step 4 uses the same run-unique sentinel for the targeted-country cohort (comments/header updated); steps 5–10 unaffected (step 5's earlier failure was cascade: step 4's audit row never entered the known-id baseline). Sub-loop exit 0; journeys 29/0.
3. `frontend/graphql/test/warnings/warning-surfacing.test.ts` + `admin-broadcast.service.test.ts` additions were authored by agent 51a as mandated missing security-tier cases / live-mutation inventory registration (verified legitimate, in-scope).

## 7. Pre-existing failures encountered (REPORT-ONLY, REQ-044)

- 4 × plan-catalog anonymous FORBIDDEN-vs-UNAUTHORIZED (`plan-catalog.roles.test.ts`).
- bun 1.3.14 + Happy-DOM mutation-render SIGABRT (7 container flow tests; kills the `test:ui:components` runner mid-file → DF-1).
- Browser tier + `next build` upstream breakage (48 errors) → DF-2.
- Port-3066 EADDRINUSE infrastructure contention (leftover `next-server` from a prior suite) — environmental, resolved by killing pid 5512; not a code defect.

## 8. Carry-forward knowledge

- The shared dev DB accumulates committed rows across suites/journeys: any exact-match cohort test MUST use run-unique sentinel values, and audit/inbox assertions MUST diff against an in-tx baseline. (Applied in both fixed files.)
- `run-test.ts` starts servers on fixed port 3066; after any aborted run, kill leftover `next-server` listeners before re-running server suites.
- The `test:ui:components` runner dies with its file under the Happy-DOM SIGABRT defect; later directories can be executed via the documented `KOTTABY_TEST_RUNNER_OK=1` bypass with identical preloads.

## 9. Cross-file dependencies

None new. `deferred-items.md` rows DF-1/DF-2 remain the owners of the two environment blockers; orchestrator dispositions them at 5.2/6.1 per the ledger gate.
