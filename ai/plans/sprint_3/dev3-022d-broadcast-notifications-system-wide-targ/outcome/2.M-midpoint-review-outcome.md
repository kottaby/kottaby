# Task 2.M — Mid-Point Review Gate (mandated checks + test battery)

**Plan:** `ai/plans/sprint_3/dev3-022d-broadcast-notifications-system-wide-targ`
**Task:** 2.M — self-review the whole backend half (single-writer, schema-drift, REQ-042, REQ-050, journey green, baseline delta) + full test battery
**Agent:** Midpoint Gate Executor (READ-ONLY + this outcome file + tasks.md/worklog protocol edits — zero production code touched, NO commits made)
**Executed:** on branch `feat/dev3-022d-broadcast-notifications` @ `67524ca` (verified via `git branch --show-current` + `git rev-parse --short HEAD`)
**VERDICT:** **GATE-PASSED** — all six checks green, zero gaps. Phase 3 may proceed.

---

## 0. Environment verification (pre-condition)

| Item | Expected | Observed |
|---|---|---|
| Branch | `feat/dev3-022d-broadcast-notifications` | ✅ (re-verified before every command — see §7 drift note) |
| HEAD | `67524ca` | ✅ |
| Working tree | clean (only `?? .issue42.json`, the ledgered 0.1 baseline artifact) | ✅ |
| PostgreSQL | up (journey/repo suites need it) | ✅ PID on `/tmp/pgsock:5432` (17.11, `kottaby_db`) |
| Mandated runner | `bun run test/scripts/run-test.ts` (never raw `bun test`) | ✅ all runs below |
| `~/.bun/bin/bun` symlink (0.2/1.3 fix) | present | ✅ → `/usr/local/bin/bun` (1.3.14) |

⚠️ The workspace was initially found checked out on `main` @ `7818918` (which contains only tasks 0.x–2.3: no `admin-broadcast.service.ts`, no `outcome/2.4-outcome.md`, tasks.md 2.4 unchecked). Restored to the briefed branch with a ref-only `git checkout feat/dev3-022d-broadcast-notifications` — no commit, no content change. Drift recurred repeatedly during the session; every check below was executed under a per-command re-checkout guard (§7).

---

## 1. Check 1 — Single-writer rule: ONLY the notification engine (via its repository) inserts into `notifications`

Commands: `rg "insert\(notifications" backend/`, `rg -i "insert\s+into\s+\"?notifications\"?" backend/` (zero raw-SQL hits), plus a `notifications\)` sweep to catch any other write form.

| Site | Kind | Verdict |
|---|---|---|
| `backend/db/repo/notifications/notification.repository.ts:131` (`createReturning` → `(tx ?? db).insert(notifications)…`) | production | ✅ sanctioned writer (engine's single-row path) |
| `backend/db/repo/notifications/notification.repository.ts:158` (`createManyReturning` → `.insert(notifications)…`) | production | ✅ sanctioned writer (engine's batch path) |
| `backend/services/notifications/notification-engine.service.ts:66,70,103,107` | production | ✅ the engine itself performs NO direct insert — import-by-reference composition of the two repository methods above (exactly the mandated shape) |
| `admin-broadcast.service.ts` | production | ✅ **zero** insert statements (its only textual "insert" hits are header prose). Verified directly by grep over the file |
| `broadcast-audience.repository.ts` / `redis-claim-cache.ts` | production | ✅ read-only / no DB writes (only a header comment mentions the engine's insert) |
| `backend/**` rest (`services/students`, `services/admin`, `graphql`, `db`) | production | ✅ zero insert sites |
| 6 test files (`notification-engine.chaos/inbox.test.ts`, `notification.repository.test.ts`, `notification-mutation/query/integration-matrix.test.ts`) | test fixtures only | ✅ allowed — tests are not production writers; single-writer rule governs the runtime write path |

**PASS** — the sole production write path into `notifications` is `NotificationRepository.createReturning/createManyReturning`, reachable only through the engine; the broadcast service composes the engine and never writes rows itself.

## 2. Check 2 — Schema-drift gate (REQ-044): `backend/db/schema/**` + `backend/db/migration/**` byte-identical to origin/main

| Command | Result |
|---|---|
| `git diff -- backend/db/schema/ backend/db/migration/` (worktree vs HEAD) | **empty (0 lines)** |
| `git diff --name-only origin/main HEAD -- backend/db/schema/ backend/db/migration/` | **0 files** |
| `git diff --name-status origin/main...HEAD -- backend/db/schema/ backend/db/migration/` | **empty** |

**PASS** — all three feature commits (b020816, 7818918, 67524ca) touch zero schema/migration files; the worktree is clean.

## 3. Check 3 — REQ-042 tx propagation (code reading of `admin-broadcast.service.ts` + 2.4 outcome evidence)

| Requirement element | Code evidence (line cites @ 67524ca) | Verdict |
|---|---|---|
| Cohort resolution receives the caller tx | `resolveBroadcastRecipients(…, outerTx)` passes `outerTx` to BOTH `PlanRepository.existsById(selector.planId, outerTx)` (`:261`) and `BroadcastAudienceRepository.resolveAudienceIds(selector, outerTx)` (`:272`) | ✅ |
| `emitForUsers` receives the unit's tx | `withTransaction(outerTx, async tx => { … emitForUsers({userIds, type: SystemBroadcast, title, body: input.body, relatedEntityType/Id: null, idempotencyKey}, locale, tx, engineOptions) …})` (`:370-383`) | ✅ |
| Audit write receives the SAME tx | `AuditService.createAuditLog({…entityId: null…}, tx)` inside the SAME `withTransaction` closure (`:390-398`) — object-identity `auditTx === emitTx` is spy-pinned in the service suite (the engine savepoints its insert one level deeper inside the handed-off unit, per 2.4 outcome §3) | ✅ |
| No mixed global-`db` writes inside the unit | the closure's only statements are the two calls above + replay detection; publish (`:408`) is strictly post-commit | ✅ |
| Behavioral atomicity | service suite `forced insert failure rolls the WHOLE unit back — zero notifications, zero audit, no publish` (14-test matrix, outcome/2.4 §3) | ✅ |
| Gate + reads pre-tx discipline | `assertActorAdmin(actorId, locale, outerTx)` is the first statement (`:338`); gate is a read-only pre-tx re-verification (2.2 outcome contract) | ✅ |

**PASS** — one transaction unit: plan-exists read → cohort read → engine emit → audit write all share `outerTx`/`tx`; REQ-042's prohibition on mixing tx writes with global-`db` reads holds.

## 4. Check 4 — REQ-050 taxonomy coverage: all four audience kinds in repo + service + journey tests

**Repo layer** — `backend/db/test/logic/notifications/broadcast-audience.repository.test.ts` (both tx branch and raw-SQL branch):

| Kind | Test names |
|---|---|
| `all` | "Tier 1 — 'all' resolves governed users in id ASC order (suspended included, deleted/blocked excluded)" (tx, `:170`); "Tier 1 — 'all' resolves the governed committed cohort, id ASC, duplicate-free" (raw, `:475`) |
| `role` | "Tier 1 — 'role' resolves exactly that role's governed users" (`:195`); "Tier 1 — 'role' resolves the committed role cohort and still excludes governed rows" (`:486`) |
| `country` | "Tier 1 — 'country' resolves exactly the exact-match cohort, id ASC" (`:217`); "Tier 1 — 'country' resolves exactly the committed exact-match cohort" (`:505`); + Tier 3 "country matches EXACT bytes only…" (`:360`, `:539`) |
| `plan` | "Tier 1 — 'plan' resolves exactly the active-window subscribers of that plan, any role" (`:235`); "Tier 1 — 'plan' resolves exactly the committed active-window subscribers" (`:514`) |

**Service layer** — `backend/services/notifications/admin-broadcast.service.test.ts` (each kind = full happy-path: rows + ONE metadata-only audit + ONE full-list envelope):

| Kind | Test name |
|---|---|
| `all` | "all cohort — one row per governed user, ONE metadata-only audit row, ONE full-list envelope, zero logs" (`:263`) |
| `role` | "role cohort — only the teacher role gains rows; audit metadata carries the role" (`:327`) |
| `country` | "country cohort — exact match on the trimmed country; the trimmed value is what resolves" (`:369`) |
| `plan` | "plan cohort — the active-window subscriber receives; the expired-window subscriber does not" (`:423`) |

**Journey layer** — `test/workflows/notifications/admin-broadcast.journey.test.ts` (specs §2.9 steps 1:1):

| Kind | Test name |
|---|---|
| `all` | "step 1 — Admin A fires \`all\`: every governed cast member gains a row, G excluded, ONE audit row, ONE envelope" (`:461`) |
| `role` | "step 3 — Admin A fires \`role:teacher\`: only T gains a row; S/Pa/S2/SX stay byte-identical" (`:558`) |
| `country` | "step 4 — Admin A fires \`country:\"EG\"\`: only S gains a row (exact match; seeded \"Egypt\" users unaffected)" (`:602`) |
| `plan` | "step 5 — Admin A fires \`plan:P\`: only the active-window subscriber S; the EXPIRED-P student excluded" (`:641`) |

**PASS** — 4 kinds × 3 layers fully covered (12 kind-specific happy-path tests + shared governance/denial matrices).

## 5. Check 5 — Test battery (ALL via `bun run test/scripts/run-test.ts`; never raw `bun test`)

| # | Target | Result | Files | Notes |
|---|---|---|---|---|
| 1 | `backend/services/notifications` (directory) | **134 pass / 0 fail / 1505 expects** | 11 | incl. `admin-broadcast.service.test.ts` (14), `redis-claim-cache.test.ts` (24), engine emit/inbox/chaos suites, transport suites |
| 2 | `backend/db/repo/notifications` | **0 test files** (3 source files searched) | — | repo tests live in `backend/db/test/logic/notifications/` per task 2.1's placement note (runner no-op, graceful) |
| 3 | `backend/db/test/logic/notifications` | **47 pass / 0 fail / 165 expects** | 2 | `broadcast-audience.repository.test.ts` (19) + `notification.repository.test.ts` (28) |
| 4 | `test/workflows` (journey — MUST stay green) | **80 pass / 0 fail / 911 expects** | 9 | incl. `admin-broadcast.journey.test.ts` 11/0 — the 2.0 test-first journey is GREEN |
| 5 | `backend/services/admin` (admin-gate + user-management) | **110 pass / 0 fail / 506 expects** | 4 | `admin-gate.helpers.test.ts` (6) + `user-management.service.test.ts` (61) + `user-management.chaos.test.ts` (8) + `audit-trail.service.test.ts` (35) |
| | **Battery total** | **371 pass / 0 fail** | 26 | zero failures, zero flakes (every run exit 0, single pass) |

## 6. Check 6 — tsgo delta vs 0.1 baseline

| Command | Result |
|---|---|
| `bun tsgo 2>&1 \| grep "error TS" \| wc -l` | **0** |
| 0.1 baseline (outcome/0.1 §1-2) | 0 |
| **Delta** | **0 ✅ (MUST-be-0 satisfied)** |

Raw log: `/tmp/tsgo-2M.log` (build completes clean; only the process-lock transcript lines).

## 7. Operational note — recurring branch drift (for the orchestrator)

The sandbox repeatedly reset the worktree to `main` @ `7818918` BETWEEN tool calls during this gate (observed ≥4 times: `admin-broadcast.service.ts` and `outcome/2.4-outcome.md` vanished from disk twice mid-session, `git rev-parse --abbrev-ref HEAD` returned `main` twice). Every mandated check above was therefore executed under a per-command guard: `git checkout -q feat/dev3-022d-broadcast-notifications && git rev-parse --short HEAD` (idempotent ref-only restore; `main` @ `7818918` is an ancestor of `67524ca`, so the restore can never lose committed work). Same phenomenon was logged by tasks 1.1, 2.0, 2.1, 2.2, 2.3, 2.4 — this file makes it explicit for the Phase 3+ agents: **re-verify `git branch --show-current` + HEAD before every sensitive operation; never trust a prior check's branch state.**

## 8. Verdict

| Check | Verdict |
|---|---|
| 1. Single-writer rule | ✅ PASS |
| 2. Schema-drift gate | ✅ PASS (empty vs origin/main AND vs worktree) |
| 3. REQ-042 tx propagation | ✅ PASS (code + spy/rollback test evidence) |
| 4. REQ-050 taxonomy coverage (4 kinds × repo/service/journey) | ✅ PASS |
| 5. Test battery (5 targets) | ✅ PASS — 371/371 |
| 6. tsgo delta vs 0.1 baseline | ✅ PASS — 0 errors |

### **GATE-PASSED** — no gaps to ledger; `deferred-items.md` needs no new rows. Phase 3 (GraphQL resolvers) may start.
