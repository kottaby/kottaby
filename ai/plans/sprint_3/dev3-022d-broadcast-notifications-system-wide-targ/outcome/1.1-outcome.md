# Task 1.1 — `BroadcastAudienceType` TS-only Enum + Fail-Closed Guard (Phase 1)

**Plan:** `ai/plans/sprint_3/dev3-022d-broadcast-notifications-system-wide-targ`
**Task:** 1.1 — CREATE `backend/enum/notifications/broadcast-audience-type.enum.ts` + barrel export + 4-tier test suite; TS-only, NO `pgEnum`.
**Agent:** Implementation executor (Phase 1 — Types, Enums & i18n Foundations)
**Result:** ✅ COMPLETE — 3 files (2 created, 1 modified), QL exit 0 on all three, tests 25/25 green, schema-drift gate intact (zero diffs under `backend/db/schema/**`, `backend/db/migration/**`).

---

## 1. What changed

### Created — `backend/enum/notifications/broadcast-audience-type.enum.ts` (29 lines)

- `enum BroadcastAudienceType { All = "all", Role = "role", Country = "country", Plan = "plan" }` — members/values/order exactly per plan §2.3.
- `isBroadcastAudienceType(value: unknown): value is BroadcastAudienceType` — byte-pattern mirror of `isNotificationType` (`notification-type.enum.ts:21-23`): `typeof value === "string" && (Object.values(BroadcastAudienceType) as string[]).includes(value)`. Membership derives from `Object.values(...)` (no parallel literal list to drift).
- Header JSDoc states domain behavior only: closed four-kind cohort vocabulary (`all` / `role` / `country` / `plan`); request-scoped selector type, never persisted as a column type (individual notification rows record the resolved recipient, not the cohort), hence no `pgEnum` counterpart. **Zero references to REQ-N / task IDs / plan paths anywhere in code comments** (grep-verified).
- File imports nothing — zero dependencies, zero throw paths, zero I/O.

### Modified — `backend/enum/notifications/index.ts`

- Added `export * from "./broadcast-audience-type.enum";` (alphabetically first, before the existing `notification-type.enum` line). Barrel conventions per `.agents/instructions/backend.instructions.md`: `export *` only, relative `./` paths, no imports, ≤1 `/` per path.

### Created — `backend/enum/notifications/broadcast-audience-type.enum.test.ts` (293 lines)

Mirrors `notification-type.enum.test.ts`'s tier discipline, minus the pgEnum parity tier (this enum is TS-only — nothing to mirror against; member-value tests take that tier's place). 25 tests / 295 assertions:

| Tier | Coverage |
|---|---|
| Member values | `Object.values` = exactly `["all","role","country","plan"]` in declared order (length 4 pinned); every member maps to its exact wire string (widened via `const x: string = …` — `String()` wrappers are sonarjs-flagged); module-value + wire-string forms both accepted |
| Tier 1 — member values | every enum member passes guard; exact member strings pass; non-member string fails (false branch); non-string short-circuits typeof (left-false branch); `true` narrows `unknown` → `BroadcastAudienceType` |
| Tier 2 — boundary | wrong case (`"All"`, `"ALL"`, `"PLAN"`…), whitespace variants (`"all "`, `" all"`, tab/newline), `""`, near-misses (`"alls"`, `"roles"`, `"countrys"`, `"planned"`, `"audience_all"`), `null`/`undefined`/`0`/`1`/`NaN`/`true`, plain + member-bearing objects + arrays |
| Tier 3 — chaos & fuzz | 200 deterministic LCG fuzz strings; 10k-char strings (incl. member-prefixed/suffixed); unicode/Arabic/RTL/combining-mark/emoji inputs; LIKE wildcards, quotes, backslashes, SQL-comment payloads; control chars (NUL, CR, BOM); **null-prototype objects** and **`__proto__`-bearing payloads** (own-enumerable-key form + nested) rejected WITHOUT throwing |
| Tier 4 — security & abuse | accepted string set is EXACTLY the four members — 500+ fuzz strings + systematic mutations (pluralized, padded, underscored, hyphenated, uppercased, NUL-suffixed) filtered through the guard yield only the canonical set (guards against accidental string-literal acceptance); `toString` override returning a member rejected without coercion; explosive `toString`/`valueOf` never invoked; `Symbol.for("all")` rejected; arrays and functions rejected — all `.not.toThrow()` |

Test-only `throw`s exist solely inside the explosive-fixture bodies (proving the guard never calls them) — the enum module itself throws nothing.

## 2. Verification evidence

### 2.1 Quality Loop (1.1.QL) — `bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates`

| File | tsgo | oxlint | biome:check | lint:type-aware | check:duplicates | Exit |
|---|---|---|---|---|---|---|
| `broadcast-audience-type.enum.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `index.ts` | ✅ | ✅ | ✅ | ✅ | ✅ | **0** |
| `broadcast-audience-type.enum.test.ts` | ✅ | ✅ | ✅ | ✅ | ✅ (skipped — outside jscpd scope) | **0** |

Iterations logged for the record: initial test draft produced 5 tsgo errors (`expect(<enum member>).toBe("…")` — bun:test overload types reject the raw literal against the narrowed enum parameter; fixed by widening through `const x: string`), 7 oxlint warnings (`no-array-sort` → `toSorted`, `consistent-function-scoping` → module-scope fixture, `no-unnecessary-type-conversion` → widened consts), then 4 sonarjs type-aware errors (`no-alphabetical-sort` → explicit comparator, `no-primitive-wrappers` → wrapper-object test dropped: the guard's `typeof` check makes boxed primitives structurally unreachable and object rejection is already tier-covered). All fixed in-file; no cross-file fixes were required.

### 2.2 Tests (1.1.TE) — `bun run test/scripts/run-test.ts backend/enum/notifications/broadcast-audience-type.enum.test.ts`

```
25 pass
 0 fail
 295 expect() calls
Ran 25 tests across 1 file. [141.00ms]
EXIT_TEST=0
```
Log: `logs/2026-09-02T04-30-56/backend/enum/notifications/broadcast-audience-type.enum.test.ts.log`. Never invoked via raw `bun test`.

### 2.3 Security & tenancy audit (1.1.SEC) — PASS

- **Fail-closed:** the only `true` path is exact membership in `Object.values(BroadcastAudienceType)`; every other input (non-string, wrong case, whitespace, foreign values, boxed primitives, exotic objects) → `false`. Test-proven across 295 assertions including prototype-pollution payloads.
- **No coercion:** guard never calls `String()`/`toString()`/`valueOf()` — explosive-fixture test proves hostile `toString` is never invoked (no `TypeError`-coercion side channel).
- **No leakage through error paths:** the module has no throw path, no logger, no I/O, no PII; it returns booleans only. Nothing can surface valid/invalid distinctions beyond the boolean.
- **No tenancy surface:** pure function; no DB, no context, no locale involvement at this layer.

### 2.4 Semantic review (1.1.SR) — PASS

- Zero dead code: both exports consumed (barrel + test suite); all test helpers (`CANONICAL_VALUES`, `compareStrings`, `fuzzStrings`, `memberReturningFunction`) referenced.
- No cross-layer imports: enum file imports nothing; test imports only `bun:test` + the deep enum module (`@/backend/enum/notifications/…`, per enum-layer deep-import preference).
- Value-import discipline: enum exported as a real (non-`const`) enum so downstream runtime consumers value-import members; type-only positions may `import type` (plan §2.2 pattern for task 1.2) — REQ-003's rule binds runtime usage, which stays value-imported.
- Comments/JSDoc are domain-only — grep for `REQ-`, `tasks.md`, `plan.md`, `specs.md`, `ai/plans`, `1.1` over all three files: zero code references (only English words like "throwing" match `throw` scans; no plan artifacts).

### 2.5 Instruction verification (1.1.IV) — PASS

| Rule source | Check |
|---|---|
| `backend/enum/AGENTS.md` (exists — verified) | File in correct sub-directory (`notifications/`) with `<entity>.enum.ts` naming; sub-directory `index.ts` barrel updated (its "Adding New Enums" step 3); deep-import preference honored in the test; backend-only (no frontend/shared import); GraphQL registration deliberately NOT done here (its step 5 belongs to the later Pothos task) |
| `.agents/instructions/backend.instructions.md` | Barrel: `export *` only, `./` paths, no imports, ≤1 `/` ✅; no `console.*` ✅; no `oxlint-disable` ✅; no nested ternaries ✅; types from canonical layers (none needed at enum layer) ✅ |
| `backend/AGENTS.md` | No local type definitions, no Pothos/schema touching ✅ |
| `.github/instructions/backend.instructions.md` | Does NOT exist (sub-loop's ⚠ confirms) — stale pointer, already ledgered in plan-review-R1; the real file is `.agents/instructions/backend.instructions.md` (read in full) |

## 3. Cross-file dependencies discovered / carry-forward

1. **bun:test `expect(enumMember).toBe(literal)` is a type error** — the strict overload types `.toBe` against the narrowed enum type. Downstream tasks writing enum-value assertions (tasks 1.2–2.4, schema-surface freeze edits) should widen via `const x: string = Enum.Member` or compare `Object.values(...)` arrays instead of `String()` (sonarjs `no-unnecessary-type-conversion` + `no-primitive-wrappers` both fire on `String()`/`new String()`).
2. **Lint interplay:** oxlint bans argument-less `.sort()`/`.toSorted()` (`no-array-sort`/sonarjs `no-alphabetical-sort`) — supply an explicit comparator; `consistent-function-scoping` forces test fixtures to module scope (mirror file's `memberReturningFunction` precedent confirmed again).
3. **Sub-loop biome stage is mutating** (`biome:check` script = `check --write --unsafe`): it reformatted the test import to multiline and arrow params to as-needed parens. Files are left in repo-canonical format; no action needed.
4. **Consumer contract for downstream tasks (2.1/2.3/2.4/3.x):** `isBroadcastAudienceType` narrows `unknown → BroadcastAudienceType`; treat `false` as pre-DB rejection (`BROADCAST_AUDIENCE_INVALID` path per plan §4.2) — never `as` casts. The Pothos enum-object registration (`BroadcastAudienceTypePothosEnum`) must consume the TS enum, NOT re-declare values.
5. **Branch state note:** the working tree was found on `main` (orchestrator environment drift) — both branches point at the same commit `bbcb363`, so a ref-only `git checkout feat/dev3-022d-broadcast-notifications` restored the briefed state with zero content change; prior tasks' uncommitted plan edits carried over untouched.
6. **Parallel-agent observation (not touched):** an untracked probe file `backend/types/contracts/tmp-entityid-probe.ts` appeared in the tree while task 1.2 was executing concurrently — flagged here for orchestrator awareness only.

## 4. Files touched by task 1.1

| File | Action |
|---|---|
| `backend/enum/notifications/broadcast-audience-type.enum.ts` | CREATE |
| `backend/enum/notifications/broadcast-audience-type.enum.test.ts` | CREATE |
| `backend/enum/notifications/index.ts` | UPDATE (+1 barrel line) |

Schema-drift gate: `git diff --name-only -- backend/db/schema/ backend/db/migration/` → **0 files**. No commits made (orchestrator owns commits).
