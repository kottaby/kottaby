# Task 1.3 — Register four new `ErrorsLabels` keys (broadcast domain, flat)

**Plan:** `ai/plans/sprint_3/dev3-022d-broadcast-notifications-system-wide-targ`
**Task:** 1.3 (Phase 1 — Types, Enums & i18n Foundations) · Requirements: REQ-050, REQ-051
**Agent:** implementation executor · Branch `feat/dev3-022d-broadcast-notifications` (no commits — orchestrator commits)
**Result:** ✅ COMPLETE — 3 files edited, sub-loop exit 0 ×3, `tsgo` 0 errors, errors ar/en parity suite 8 pass / 0 fail.

---

## 1. What changed (exactly 3 files — nothing else touched)

| File | Edit |
|---|---|
| `shared/locale/types/errors/index.ts` | Added 4 **flat top-level** `readonly string` keys to `ErrorsLabels` (after `trialAlreadyGranted`, before the closing brace), each with a doc comment pinning its `extensions.code`. NO new nested group; `ErrorMessageKey` derived type picks the new string keys automatically (no change needed). |
| `shared/locale/en/errors/index.ts` | 4 new entries on `errorsEn` (English copy below). |
| `shared/locale/ar/errors/index.ts` | 4 new entries on `errorsAr` (Arabic copy below). |

No changes to `shared/locale/types/message.ts`, `messages.ts` bundles, or the namespaces registry — the `errors` namespace is already fully wired (`errorsTranslations: ErrorsLabels` at `shared/locale/types/message.ts:16`; leaf consts imported by both bundles), so the shared/AGENTS.md *namespace-registration* checklist reduces to types+en+ar for a key addition. Zero imports added; leaf modules remain plain string literals.

## 2. Exact key names + copy (carry-forward for Phase 2 service / Phase 4 UI)

Keys are the lowercase-camelCase of the SCREAMING_SNAKE `extensions.code`s per the file-header convention (REQ-050 taxonomy). Services consume them via the errors bundle, e.g. `ValidationError("BROADCAST_TITLE_INVALID", t.broadcastTitleInvalid)` (plan §4.1 step 2).

| Key | `extensions.code` | en | ar |
|---|---|---|---|
| `broadcastTitleInvalid` | `BROADCAST_TITLE_INVALID` | `Broadcast title must be between 1 and 255 characters.` | `يجب أن يتكون عنوان البث من حرف واحد على الأقل وألا يتجاوز 255 حرفاً.` |
| `broadcastAudienceInvalid` | `BROADCAST_AUDIENCE_INVALID` | `Invalid broadcast audience. Select a valid audience type and provide exactly the field it requires.` | `جمهور البث غير صالح. اختر نوع جمهور صالحاً وأدخل الحقل المطلوب له فقط.` |
| `broadcastAudienceEmpty` | `BROADCAST_AUDIENCE_EMPTY` | `No recipients match the selected broadcast audience. Choose a different audience.` | `لا يوجد مستلمون مطابقون لجمهور البث المحدد. اختر جمهوراً آخر.` |
| `broadcastAudienceTooLarge` | `BROADCAST_AUDIENCE_TOO_LARGE` | `The selected broadcast audience is too large. Narrow the audience and try again.` | `جمهور البث المحدد كبير جداً. قم بتقييد الجمهور وحاول مرة أخرى.` |

Copy-bound derivation: the task prompt's "1-200 characters" was an example style only — the exact bound is **1–255** (REQ-020 "trimmed, non-empty, ≤255 chars"; plan §4.1 "≤255 (engine ceiling)"; verified engine constant `TITLE_MAX_LENGTH = 255` at `backend/services/notifications/emit-validation.ts:34` — matches `notifications.title` varchar(255)).

Deliberate copy decisions (SEC/SR rationale):
- **No recipient data, no counts, no internal identifiers** anywhere (REQ-033/REQ-034 oracle hygiene). The only number is the title's **character** bound (admin's own input, mirrors the existing `planCatalog.planTitleTooLong` "255 characters" style). The **5000 recipient cap is deliberately NOT quoted** — quoting a recipient-count constant would violate the 1.3.SEC "no counts" mandate and is unnecessary for actionability ("too large → narrow the audience").
- All strings are static (zero ICU placeholders) → no placeholder-parity surface, unlike `applicantCooldownActive`/`{cooldownUntil}`.
- Empty-cohort copy states the honest condition ("no recipients match") without enumerating or counting users.

## 3. Mandatory subtask pipeline — evidence

- **1.3.QL Quality Loop** — `bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates` for ALL three files (types, en, ar): each ran tsgo + oxlint + biome:check + lint:type-aware + jscpd intra-file duplicates → **exit 0 ×3**, `✅ All checks … passed`.
- **1.3.TE Test Engineering** — `bun run test/scripts/run-test.ts shared/locale/errors-namespace.parity.test.ts` (mandated runner, `.env.test` loaded) → **8 pass / 0 fail / 86 expect()** including: identical sorted ar/en key sets (now 31 top-level keys incl. the 4 new), every leaf non-empty on BOTH maps, route-emitter coverage unchanged. Sibling suites that import the errors leaves also green: `applicant-namespace.parity.test.ts` 11 pass / 0 fail; `plans-namespace.parity.test.ts` 4 pass / 0 fail. Primary parity gate (compile-time `ErrorsLabels` typing on both leaf consts) holds: `bun tsgo` → **0 "error TS"** (baseline 0, delta 0).
- **1.3.SEC Security & Tenancy Audit** — PASS. Copy reviewed line-by-line: no recipient identifiers/lists, no recipient counts (cap 5000 deliberately omitted), no internal ids/codes/emails; static strings only (no interpolation of server data).
- **1.3.SR Semantic Review** — PASS. Keys are domain-prefixed (`broadcast*`), camelCase-of-code per the namespace convention, collision-free (no pre-existing `broadcast*` key in the errors namespace). Programmatic uniqueness check (flatten both locale maps, 48 leaves each): **48 unique bodies / 0 duplicates per locale, 0 collisions with pre-existing bodies**. No new nested group; no near-duplicate keys minted in other namespaces (REQ-055).
- **1.3.IV Instruction Verification** — PASS. `shared/AGENTS.md`: layer isolation intact (no imports added at all); `shared/locale/AGENTS.md`: `@/`-alias discipline preserved, leaf modules still logic-free literals; registration checklist satisfied for an EXISTING-namespace key addition (interface + both leaf maps; `message.ts`/bundles/registry already wired — nothing to add); errors-namespace rules respected (no `validationFailed`/`rateLimited`-style near-duplicates; errors side owns the transport copy).

## 4. Deviations & environment note (carry-forward, IMPORTANT for parallel tasks)

- **Test-runner blocker fixed at environment level (not a repo change):** `test/scripts/run-test.ts:6` hardcodes `BUN_BIN = ~/.bun/bin/bun`, but this sandbox installs bun at `/usr/local/bin/bun` → every `run-test.ts` invocation died with `ENOENT … '/home/z/.bun/bin/bun'` before any test ran. Fix: `mkdir -p /home/z/.bun/bin && ln -sfn /usr/local/bin/bun /home/z/.bun/bin/bun` (same binary, v1.3.14). Zero repo files touched. Tasks 1.2 / 2.x / 3.x that run tests via the mandated runner need this symlink.
- No deviations from task scope. Working tree also contains parallel-task edits (1.1 enum files, 1.2 types files, 1.4 adminBroadcasts namespace + dashboard + messages.ts/message.ts/namespaces-registry wiring) — untouched by this task; my diff is exactly the 3 files in §1.

## 5. Dependencies & downstream notes

- **Consumed by:** task 2.4 (`AdminBroadcastService` validation/cohort guards → the four `ValidationError(code, message)` throws) and task 3.x/4.x (frontend field-error projection of `VALIDATION` errors via `mutationFieldErrors.ts` — the four messages surface verbatim on the compose form).
- **Parallel-safe with 1.4:** these keys live in the `errors` namespace; task 1.4's `AdminBroadcasts` namespace is disjoint (no key-name or message-body collision; verified uniqueness above).
- **Documentation drift (informational, no action taken — outside task scope):** `shared/AGENTS.md` §"The `errors` namespace" enumerates the original 18 transport-emitted keys; the namespace now additionally carries domain keys (incl. these 4). The statement remains accurate for the gateway-emitter subset (these 4 keys are service-thrown, not in `app/api/graphql/route.ts`, so the dynamic route-emitter scan correctly does not require them). If the orchestrator wants the doc refreshed, fold it into REQ-081 propagation (task 7.x).
- **Baseline deltas:** none — `bun tsgo` still 0 errors; only additive string-key changes (no new lint/biome findings; sub-loop green on all three files).
