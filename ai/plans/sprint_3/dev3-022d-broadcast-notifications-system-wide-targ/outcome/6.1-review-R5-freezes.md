# 6.1 Review — Round 5 (types/contracts/codegen freeze consistency)

Task ID: 6.1-R5 · DEV3-022d · review iteration 5 of ≥10 · READ-ONLY round (this file is the only artifact written).
Scope: the generated + locked surfaces of the admin broadcast feature and the DEV3-020 audit-widening freeze; Wave-1 fix re-verification.

Evidence base (all files read in full or at cited lines, zero builds/tests/git state changes):

- `frontend/graphql/generated/schema.graphql` (full, 530 lines)
- `frontend/graphql/generated/gql/graphql.ts` (full)
- `backend/graphql/gqlSchema.ts` + `gqlSchema.definitions.ts` (full)
- `backend/graphql/mutation/notifications/admin-broadcast.mutation.ts` + `backend/graphql/pothos/notifications/admin-broadcast.pothos.ts` + `backend/graphql/pothos/shared/enum.pothos.ts` + `backend/graphql/mutation/notifications/index.ts` (full)
- `backend/graphql/test/schema-surface.test.ts` (full, 829 lines) + `backend/graphql/test/sdl-static-assertions.test.ts` (full, 421 lines)
- `backend/enum/notifications/broadcast-audience-type.enum.ts` + its unit test (full)
- `backend/types/contracts/admin-audit.contract.types.ts`, `contracts.conformance.test-d.ts`, `contracts.static-assertions.test.ts`, `backend/types/audit/audit-log.types.ts`, `backend/types/audit/audit-trail.types.ts`, `backend/db/schema/audit/audit-logs.ts`, `backend/services/admin/audit.service.ts` (full)
- `shared/locale/types/dashboard/index.ts`, `shared/locale/types/adminUsers/index.ts`, `shared/locale/types/adminBroadcasts/index.ts`, `shared/locale/en/dashboard/index.ts`, `shared/locale/ar/dashboard/index.ts` (full)
- Inventory sweep: `frontend/graphql/test/warnings/warning-surfacing.test.ts`, `frontend/graphql/test/gateway/allowlist-coverage.test.ts`, `frontend/graphql/test/admin/admin-users.integration.test.ts`, `frontend/graphql/sharedDocuments/notifications/broadcast.documents.ts` + `.test.ts`, `documents.contract.test.ts`, `backend/graphql/test/admin-broadcast.integration.test.ts` (targeted); repo-wide `adminBroadcastNotification` / `entityId` / freeze-list greps.

---

## Findings

### New findings this round

[LOW] backend/graphql/test/schema-surface.test.ts:127-138 (+ backend/graphql/test/sdl-static-assertions.test.ts:321) — The `BroadcastAudienceType` WIRE MEMBER SET is not pinned at the GraphQL freeze tier; only its NAME is frozen (`PRE_3_1_ENUMS` / `PRE_3_1_TYPE_NAMES` in schema-surface.test.ts, and the type REFERENCE `BroadcastAudienceType!` pin at sdl-static-assertions.test.ts:321). Contrast the sibling enums, which both have explicit GraphQL-tier member pins: `NotificationType` (7 wire names + snake_case runtime values — schema-surface.test.ts:347-389) and `AppLocale` (2 values — schema-surface.test.ts:727-740 and sdl-static-assertions.test.ts:404-414). Today all four surfaces agree byte-consistently (TS enum keys All/Role/Country/Plan → SDL `enum BroadcastAudienceType { All Country Plan Role }` schema.graphql:205-210 → codegen `All='All'…Role='Role'` graphql.ts:82-87 → enum-object registration enum.pothos.ts:145-147), BUT a future TS-enum KEY rename regenerated via `generate:gqlSchema && bun codegen` would move the wire vocabulary (a client-breaking change to the committed codegen enum) while passing EVERY freeze suite: the member-level unit test (broadcast-audience-type.enum.test.ts:53-68) pins only the runtime lowercase VALUES, and the integration test derives wire names dynamically from the TS enum (`wireNameOf`, admin-broadcast.integration.test.ts:85-86) so it follows the rename silently. The byte-identity codegen-sync test (schema-surface.test.ts:818-829) only proves committed-artifact ≡ fresh emission, not member identity. Recommended (next round, additive): one member-set pin for `BroadcastAudienceType` in the sdl-static-assertions artifact tier (mirroring the AppLocale block) — no production change.

[INFO] backend/types/contracts/contracts.conformance.test-d.ts:182-190 — Residual half of the R1a-types symmetry recommendation (6.1-review-R1a-types.md:22): the Wave-1 fix added the POSITIVE `entityId: null` probe (lower bound of the widening now pinned) but no NEGATIVE probe pins the UPPER bound (e.g. `entityId: "1"` with `@ts-expect-error`), so a hypothetical over-widening of `AuditLogWriteContract.entityId` would still compile. Marginal residual risk only: the contract rides the schema-derived column type (`admin-audit.contract.types.ts:33` → `AuditLogSelectType["entityId"]` → nullable `integer("entity_id")` audit-logs.ts:39), so over-widening requires a physical column change first. Noted for ledger tracking; no action required this round.

### Checks performed — evidence per check

**Check 1 — mutation signature agreement: PASS (byte-for-byte across all four surfaces).**
- Pothos registration: `gqlSchemaBuilder.mutationField("adminBroadcastNotification", …)` with `type: "Int"` + `input: t.arg({ type: AdminBroadcastNotificationInput, required: true })` (admin-broadcast.mutation.ts:56-61); Pothos v3 strict nullability → `Int!` / `AdminBroadcastNotificationInput!`. Wired via side-effect barrel `mutation/notifications/index.ts:15` (documented at :11).
- SDL: `adminBroadcastNotification(input: AdminBroadcastNotificationInput!): Int!` verbatim (schema.graphql:277, description at :274-276).
- Codegen: `AdminBroadcastNotificationMutation = { adminBroadcastNotification: number }` (non-null scalar) + `AdminBroadcastNotificationMutationVariables = Exact<{ input: AdminBroadcastNotificationInput }>` (graphql.ts:373-378); document AST carries `NonNullType(AdminBroadcastNotificationInput)` variable and a bare-`Int` selection (graphql.ts:455).
- Freeze tier: exact-signature pin `type === "Int!"` + `args === [{ name: "input", type: "AdminBroadcastNotificationInput!" }]` (sdl-static-assertions.test.ts:306-310); mutation-name set is a HARD `toEqual` on `PRE_3_1_MUTATION_FIELDS` (schema-surface.test.ts:110-125, 286-292); committed-SDL ≡ fresh `printSchema(lexicographicSortSchema(...))` byte-identity (schema-surface.test.ts:818-829).

**Check 2 — BroadcastAudienceType enum agreement: PASS on current state (see the LOW finding above for the freeze-depth gap).**
- TS enum: keys All/Role/Country/Plan = wire names, values "all"/"role"/"country"/"plan" = runtime (broadcast-audience-type.enum.ts:13-18); registered ONCE in enum-object form (enum.pothos.ts:145-147, header registry updated :21).
- SDL members `{ All, Country, Plan, Role }` (schema.graphql:205-210, lexicographic emission) ≡ codegen members with key-string values (graphql.ts:82-87) ≡ TS enum KEYS — the 3.1 wire=KEYS convention holds exactly as documented for NotificationType (enum.pothos.ts:118-131).
- Consumer side: the compose container imports the CODEGEN enum (BroadcastComposeContainer.tsx:39), the integration test maps wire names from TS keys (`wireNameOf`), and the service consumes TS enum VALUES via the resolver's field-by-field mapping (admin-broadcast.mutation.ts:93).
- Freeze tier: enum NAME frozen in both inventories; input `type: BroadcastAudienceType!` nullability pinned (sdl-static-assertions.test.ts:321); runtime values pinned at the enum tier (broadcast-audience-type.enum.test.ts:53-68). Wire-member set itself unpinned — the LOW finding.

**Check 3 — freeze-list membership: PASS (no missing inventory).**
- `PRE_3_1_MUTATION_FIELDS` includes `adminBroadcastNotification` (schema-surface.test.ts:111); the freeze test is a hard `toEqual` against the whole list, so the entry is load-bearing.
- `FROZEN_MUTATION_FIELDS` includes it (sdl-static-assertions.test.ts:79) + the exact-signature + both-input closure pins (:305-339).
- `KNOWN_LIVE_MUTATION_FIELDS` (warning-surfacing wire inventory, "every deployed Mutation root field is enumerated") includes it with the DEV3-022d rationale (warning-surfacing.test.ts:291-298); test A1 hard-compares the live introspected mutation set to it (:319-330).
- `PRE_3_1_ENUMS` += `BroadcastAudienceType` (schema-surface.test.ts:132); `PRE_3_1_TYPE_NAMES` += `AdminBroadcastNotificationInput`/`BroadcastAudienceInput`/`BroadcastAudienceType` (:141-146); whole-schema named-type delta unchanged (additions array ends at `HealthCheck`, :310-330).
- allowlist-coverage.test.ts walks the LIVE schema (no name list to miss); the mutation carries its `authScopes` block so it sits in the scoped set, and it is correctly ABSENT from `PUBLIC_OPERATIONS` (default-deny posture holds; scope snapshot pinned to EXACTLY `{ $all: { authenticated: true, role: [UserRole.Admin] } }` at admin-broadcast.integration.test.ts:316-328).
- The only other admin mutation-name array (`ADMIN_OPERATIONS`, admin-users.integration.test.ts:256-264) is domain-scoped by its own comment to the seven user-management operations — the broadcast surface's BFLA matrix lives in its own integration suite. No regeneration-facing freeze list is missing the mutation.

**Check 4 — conformance widening: PASS (no latent strictly-number pin anywhere).**
- The widening is EXACTLY `readonly entityId: AuditLogSelectType["entityId"]` (admin-audit.contract.types.ts:33) = `number | null` (nullable `integer("entity_id")`, audit-logs.ts:39; `$inferSelect` via audit-log.types.ts:3). `actorId`/`actionType`/`entityType`/`details`/`ActorContextRef` untouched.
- Repo-wide `entityId` sweep: every other occurrence is either runtime `number` values, already-nullable read/filter surfaces (`audit-trail.types.ts:8,20`; SDL `entityId: Int` schema.graphql:7,15; codegen `number | null` graphql.ts:9,257), or tests asserting the NULL pass-through (`admin-broadcast.service.test.ts:347,904`; `audit-trail.service.test.ts:363`; frontend `AuditTrailRow.tsx:100` null placeholder backed by `AdminUsersLabels.auditTrail.table.noEntityIdValue`, adminUsers/index.ts:530).
- `contracts.static-assertions.test.ts` contains ZERO `entityId` patterns (grep = 0 hits) — no forbidden-pattern suite pins the old shape; no other contract file references the field.
- No orphaned `@ts-expect-error`: the only `entityId: null` mention in the conformance suite is the positive probe (:188); all negative probes use `entityId: 1` (:264,320,331,523).
- Write path coherent end-to-end: `AuditService.createAuditLog` inserts `input.entityId` verbatim (audit.service.ts:83-89) into the nullable column; the broadcast service writes `entityId: null` (admin-broadcast.service.ts:395).

**Check 5 — codegen input drift: PASS.**
- `BroadcastAudienceInput` (graphql.ts:75-80): `country: string | null | undefined`, `planId: number | null | undefined`, `role: UserRole | null | undefined`, `type: BroadcastAudienceType` (required) — EXACT match to the Pothos registration (`required: false` × 3 companions, `required: true` discriminator, admin-broadcast.pothos.ts:42-53) and to the SDL (`String`/`Int`/`UserRole`/`BroadcastAudienceType!`, schema.graphql:198-203; all four field types re-pinned at sdl-static-assertions.test.ts:312-325).
- `AdminBroadcastNotificationInput` (graphql.ts:15-19): `audience` required, `body: string | null | undefined`, `title: string` — matches Pothos (:61-71) and SDL (schema.graphql:28-32; pinned at sdl-static-assertions.test.ts:327-338). Zero extra/missing fields — the closed-input posture survives codegen.

**Adjacent freeze (locale): PASS.**
- `DashboardLabels.broadcasts` present (shared/locale/types/dashboard/index.ts:43) with both implementations `broadcasts: "Broadcasts"` (en:17) / `"الإعلانات"` (ar:17); both leaf consts compile-typed `DashboardLabels` (en:1-3, ar:1-3) so key parity is tsgo-enforced.
- `AdminBroadcastsLabels` mirrors the four cohort kinds exactly (types/adminBroadcasts/index.ts:50-69) and pins the no-preview oracle posture (:70-74) — consistent with the `Int!`-count contract.
- `AdminUsersLabels.auditTrail` block (types/adminUsers/index.ts:483-550) is coherent with the null-entityId read surface (`noEntityIdValue` :530, `entityIdHeader` :520).

---

## Verdict

**PASS.** All five freeze-consistency checks hold: the `adminBroadcastNotification(input: AdminBroadcastNotificationInput!): Int!` signature, the `BroadcastAudienceType`/input surfaces, and the codegen artifacts agree byte-consistently across Pothos registration, committed SDL, codegen types, and every mutation/enum/type freeze list (schema-surface baseline, sdl-static artifact tier, warning-surfacing live inventory — nothing missing, nothing stale). The audit-widening shows no latent strictly-number pin in any contract, static assertion, read surface, or UI. Two new findings, both freeze-depth hardening notes, zero production-code defects: 1 LOW (BroadcastAudienceType wire-member set unpinned at the GraphQL freeze tier — additive one-test fix recommended next round) and 1 INFO (upper-bound symmetry probe residual from R1a, marginal given the schema-derived type). Per the ≥10-iteration protocol (stop = 2 consecutive zero-new-findings), the streak resets at R5; R6 should re-scan and may verify the enum-member pin if adopted.

## Wave-1 fix re-verification

**VERIFIED — compiles-in and pins the widening.**
- The positive probe is present in the POSITIVES block as a `satisfies` (must-compile) assertion: `entityId: null` with `entityType: "notification_broadcast"` (backend/types/contracts/contracts.conformance.test-d.ts:182-190, comment "Positive — AuditWrite: entity-less broadcasts (no single backing row) write a NULL entityId — the contract admits entities without one").
- Type-level proof of compilability without running tsgo: `AuditLogWriteContract.entityId: AuditLogSelectType["entityId"]` (admin-audit.contract.types.ts:33); `AuditLogSelectType = typeof auditLogs.$inferSelect` (audit-log.types.ts:3); the physical column is `integer("entity_id")` with NO `.notNull()` (audit-logs.ts:39) → the inferred select type is exactly `number | null` → `null` is assignable and the `satisfies` compiles, while a revert to `readonly entityId: number` would fail this probe at tsgo time (the round-1 gap is closed: the widening is no longer guarded only transitively by the broadcast service consumer).
- No negative probe was orphaned by the widening (no `@ts-expect-error` anywhere expects `entityId: null` to error), so the suite remains exactly-balanced; the pre-existing `entityId: 1` positive (:174-180) still compiles — both bounds of `number | null` exercised.
- Consumer coherence re-confirmed: `AuditService.createAuditLog` accepts and inserts the null (audit.service.ts:82-90), the broadcast service writes `entityId: null` in the same transaction as the emit (admin-broadcast.service.ts:395), and the read side (SDL `Int`, codegen `number | null`, UI null placeholder) stays nullable-consistent.
