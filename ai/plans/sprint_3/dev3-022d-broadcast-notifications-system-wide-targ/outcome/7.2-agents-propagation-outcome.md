# Task 7.2 — AGENTS.md Propagation + Engine Doc Status Flip — Outcome

**Date:** 2026-09-02 · All four propagation targets updated (rules/references ONLY — no implementation prose duplicated):

1. **`backend/services/AGENTS.md`** — new section "Admin Broadcast Service" appended: one-paragraph composition rule (validate → gate → claim → cohort → emitForUsers → in-tx audit → post-commit publish) + pointer to `docs/notifications/broadcast-notifications.md`.
2. **`docs/notifications/realtime-engine.md`** — §3.2 consumption-table row DEV3-022d marked **SHIPPED** with the canonical-doc link. Engine content rules untouched (the engine is consumed, not edited — its §3.6 idempotency posture and §3.2 receipt composition remain the authority).
3. **`backend/db/repo/AGENTS.md`** — new section "Broadcast Audience Resolution" registered: tx/raw branch parity, exact-match country (never LIKE), DISTINCT + id-ASC on the fanning-out plan join, invariant-pinned governance predicate (suspended INCLUDED — "do not fix").
4. **Root `AGENTS.md`** — "Important References" gains one line for `docs/notifications/broadcast-notifications.md` (placed beside the engine doc reference).

## Verification

- Sub-loop on the touched .md files: structurally excluded (oxlint "No files found to lint" on any .md — the 0.1 §7 tool limitation; consistent with 7.1's outcome).
- No runtime file touched — tsgo/lint/biome deltas remain zero by construction (docs-only change).

## Carry-forward

- The engine doc's deferred ledger (D1-D9) is unaffected; the broadcast feature's own residuals live in the plan's `deferred-items.md` (RV-1..RV-9) and are cross-referenced from the canonical doc where they affect future emitters.
