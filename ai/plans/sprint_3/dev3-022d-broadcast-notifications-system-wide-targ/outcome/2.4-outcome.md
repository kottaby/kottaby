# Task 2.4 — AdminBroadcastService.broadcast (the composition core)

**Plan:** `ai/plans/sprint_3/dev3-022d-broadcast-notifications-system-wide-targ`
**Task:** 2.4 (Phase 2) — `AdminBroadcastService.broadcast(input, actorId, locale, idempotencyKey?, options?, outerTx?) → Promise<number>` per plan §4.1's strict 7-step flow
**Agent:** Implementation Executor (service layer only)
**Executed:** 2026-09-02 on branch `feat/dev3-022d-broadcast-notifications` (checked out from `main` per continuation brief — both refs at b020816, working tree carried over; **no commits made**)
**Result:** **SERVICE GREEN (14/0) · JOURNEY GREEN (11/0 — the 2.0 test-first RED is resolved) · QL exit 0 (both files) · tsgo 0 errors project-wide.**

---

## 1. Files

| File | Op | Note |
|---|---|---|
| `backend/services/notifications/admin-broadcast.service.ts` | existed from earlier work; **finished** | audit-metadata key order aligned to the documented composition (`{ scope, role?, country?, planId?, recipientCount }` — recipientCount LAST); nothing else changed |
| `backend/services/notifications/admin-broadcast.service.test.ts` | existed from earlier work; **finished** | fixed 2 behavior-level test defects (below), 8 lint/type findings, 1 comment-hygiene leak |

No engine file, no journey file, no repo/schema file touched. The journey (`test/workflows/notifications/admin-broadcast.journey.test.ts`) is byte-identical to its 2.0-committed state.

## 2. Seven-step flow — compliance evidence (plan §4.1 + outcome/2.0 §2 binding contract)

| # | Step | Implementation | Proof |
|---|------|----------------|-------|
| 1 | `assertActorAdmin(actorId, locale, outerTx)` pre-tx, zero writes, gate self-logs | first statement of `broadcast`; service never re-logs denials | suite `anonymous caller → UNAUTHORIZED` / `non-admin + missing actor → FORBIDDEN` (zero rows/audit/publish); journey steps 7–8 |
| 2 | Pre-DB validation: title trim/≤255; coherence matrix (`isBroadcastAudienceType`, `toUserRole`, trimmed country ≤100, positive-safe-int planId, per-kind companion exclusivity) | `validateAudienceCoherence` + per-kind validators; `ValidatedAudienceSelector` union (no assertions, no spread) | suite `validation matrix` (13 probes: blank/`>255` title, Role-without-role, Role+country, Role+planId, All+role, All+planId, blank/`>100`/Country+role country, `planId 0/1.5/null`, hostile role `"wizard"` via widened alias, hostile kind string) — all pre-DB, `existsById` spy NEVER called, zero state |
| 3 | Plan existence FIRST (`PlanRepository.existsById(planId, readTx)`) → `NotFoundError("PLAN", t.planCatalog.planNotFound)`; cohort via `BroadcastAudienceRepository.resolveAudienceIds(selector, readTx)`; `readTx = outerTx` when supplied | `resolveBroadcastRecipients` — normalized selector (never the raw input object) | suite `tx propagation` spy assertions: gate/exists/resolve all received the caller's tx; `resolvedSelector !== input.audience`; journey steps 5–6 (`PLAN_NOT_FOUND` + translated copy) |
| 4 | `BROADCAST_AUDIENCE_EMPTY` / `BROADCAST_AUDIENCE_TOO_LARGE` (`BROADCAST_MAX_RECIPIENTS = 5000`), zero writes | `assertCohortSize` before ANY write | suite `empty cohort` + `oversized cohort` (scripted resolution of 5001 ids → localized rejection, zero rows/audit/publish); journey step 6 (empty) |
| 5 | ONE `withTransaction(outerTx, …)`: `NotificationEngine.emitForUsers({userIds, type: SystemBroadcast, title, body, relatedEntityType/Id: null, idempotencyKey}, locale, tx, options)` field-by-field; replay = `idempotencyKey !== undefined && cache !== undefined && receipt.emitClaimKey === undefined` → ZERO audit; fresh → exactly ONE `AuditService.createAuditLog({actorId, Create, "notification_broadcast", entityId: null, details: metadata JSON}, tx)` | inline in `broadcast`; `details = buildBroadcastAuditDetails` — scope + kind companion + recipientCount only, never copy text | suite `tx propagation` (emit input toEqual = exact field map; `emitOptions` is the caller's object; audit contract toEqual incl. `entityId: null` and byte-exact metadata), `replay` (identical count, zero new rows/audit/publish), `forced insert failure` (WHOLE unit rolls back: zero notifications + zero audit + no publish), `cache-claim outage fails OPEN` (insert + audit + publish + exactly ONE engine warn), `zero logs on happy paths` (log spy). Journey steps 1–2 + 9 |
| 6 | Post-commit, fresh-only `publishReceipts([receipt], locale, options)` | after the `withTransaction` block resolves; skipped on replay | suite per-cohort `transportSpy.publishCount === 1` with FULL id list; journey steps 1–5, 9 (envelope id-set == DB rows oracle) |
| 7 | Return `receipt.recipientUserIds.length` | return statement | every happy-path test: returned count == DB rows for the run-unique title (≥ seeded users included); journey step 1 count == DB oracle |

Journey-only checks satisfied: `all` includes every governed cast member + seeded users while G (`is_deleted`) appears in NO cohort/envelope; `country:"EG"` exact-matches only S (seeded `"Egypt"` untouched); `plan:P` excludes the expired-window subscriber (strict `now() < end_date`); step 10 reads inboxes via `NotificationEngine.listMyNotifications` (verbatim copy, `relatedEntityType/Id = null`, `isRead = false`, no cross-actor leakage).

## 3. 2.4.TE — Test engineering evidence

Runner: `bun run test/scripts/run-test.ts` (never raw `bun test`).

| Suite | Result |
|---|---|
| `backend/services/notifications/admin-broadcast.service.test.ts` | **14 pass / 0 fail / 243 expects** |
| `test/workflows/notifications/admin-broadcast.journey.test.ts` | **11 pass / 0 fail / 262 expects — GREEN** (2.0's intended-RED resolved without touching the journey) |

The 14-test service matrix covers the full REQ-071-style spread: happy path per cohort kind (rows + ONE metadata-only audit + ONE full-list envelope + zero logs), anonymous/non-admin denials with zero state, the 13-probe validation matrix, unknown plan, empty cohort, oversized cohort (scripted), replay, cache-outage fail-open (exactly ONE engine warn), forced-insert rollback (zero notifications + zero audit), tx propagation + field-by-field mapping (spy-level), verbatim copy storage (Arabic/RTL/emoji/zero-width title stored UNtrimmed; `<script>`/`DROP TABLE`/`%`/`_` body stored byte-for-byte and inert with user rows intact). Fixtures via `entity-setup` (`createTestUser`/`createTestPlan`) + a `provisionCast` helper inside `runInRollback`; engine boundaries exercised through the real engine with `SpiedFanoutTransport` + a scripted in-memory claim cache (no Redis/network).

**Defects found & fixed while iterating (service-side correctness, not journey-weakening):**
1. Audit `details` JSON key order: service emitted `{scope, recipientCount, planId}`; the documented composition is `{scope, <companion>, recipientCount}`. Service reordered (metadata content unchanged — journey parses JSON and was already green either way; the byte-exact service test now matches the documented shape).
2. No service-logic defects: both initial failures were test-side (a local `const count` shadowing the drizzle `count` import → TDZ; a tx object-identity assertion that contradicted the engine's documented savepoint nesting — the engine savepoints its insert one level deeper inside the service's handed-off unit, so the honest identity assertion is `auditTx === emitTx` with unit atomicity proven behaviorally by the forced-rollback test).

## 4. 2.4.QL / tsgo

| Gate | Result |
|---|---|
| sub-loop `--lifecycle duplicates` on `admin-broadcast.service.ts` | **exit 0** (tsgo/oxlint/biome/lint:type-aware/check:duplicates ✅ — re-run on the final content) |
| sub-loop on `admin-broadcast.service.test.ts` | **exit 0** (after fixing: 4× TS6133 unused (`db`, `NotFoundError`, `NONEXISTENT_PLAN_ID`) + the savepoint-narrowing guard, 6× `no-shadow` (`count` → `broadcastCount` renames incl. the stale `recipientCount: count` metadata refs), 1× `no-unsafe-type-assertion` (hostile-kind string → widened-alias swap), 1× `no-unnecessary-type-assertion` (receipt index), 1× `sonarjs/prefer-specific-assertions` (`toHaveLength`) |
| `bun tsgo` full repo | **0 errors** — the journey's TS2307 is resolved; the concurrent redis-claim-cache error from the 2.1 session is gone (2.3 committed) |

## 5. 2.4.SEC — Security & Tenancy Audit (PASS)

- **BFLA double wall (service half):** `assertActorAdmin` is literally the first statement; re-verification runs against the live `users` row through the shared canonical gate (`admin-gate.helpers.ts`) — no forked copy.
- **BOPLA:** zero spreads of `input`/`audience`/`selector` anywhere (grep: 0 hits) — the engine batch input is an explicit field map; the audit metadata is composed field-by-field from the VALIDATED selector.
- **BOLA:** the only identity input is `actorId`; recipients derive solely from the audience selector; no per-identity acceptor exists.
- **Oracle hygiene:** `PLAN_NOT_FOUND` is thrown only inside `resolveBroadcastRecipients` on this admin surface (grep: single site).
- **Logs:** 3 `logDomainError` diagnostics (rejection helper + plan-miss), each `{code, entity, entityId?, locale}` — never copy text, never recipient lists, never the raw idempotency key (grep-verified); happy paths and replays log nothing (spy-tested). Zero `console.*` (grep: 0).
- Denials and the replay path write ZERO audit rows; the cap and empty guards reject pre-write.

## 6. 2.4.SR / 2.4.IV — Semantic Review & Instruction Verification (PASS)

- **SR:** one atomic write unit per broadcast; publish strictly post-commit and fresh-only; no dead code (every branch is test-pinned: replay, fail-open, rollback, each coherence arm); canonical types only (`BroadcastNotificationSubmitInput`, `BroadcastAudienceSelector`, `DBTransaction`, engine receipt types from `@/backend/types`) — the one local union (`ValidatedAudienceSelector`) is a narrowing aid structurally assignable to the canonical selector, not an entity shape; comment hygiene grep (`REQ-N|DB-N|task IDs|plan paths|§`) over both files → **0 hits** (one "REQ-024-style" comment reworded).
- **IV (backend/AGENTS.md, backend.instructions.md, backend/services/AGENTS.md):** engine import-by-reference honored — the service composes `emitForUsers`/`publishReceipts` and never writes `notifications` rows itself (single-writer rule); audit-emission rule satisfied (exactly ONE in-tx audit row via `AuditService.createAuditLog(contract, tx)`, composition-only, capped metadata, zero audit on denials); i18n via `getServerTranslations(locale)` — no hardcoded user-facing strings; types from `@/backend/types`; no service-layer `.types.ts`; service tests use no real external APIs (spied transport + scripted cache); `backend/services/notifications/AGENTS.md` does not exist (verified before citing).

## 7. Carry-forward for Phase 3 (GraphQL) / Phase 4

1. Resolver maps the GraphQL input field-by-field into `BroadcastNotificationSubmitInput` and calls `AdminBroadcastService.broadcast(input, ctx.user.id ?? 0, ctx.locale, ctx.idempotencyKey, { transport, cache } from the composed fan-out/claim seams)` — `actorId` NEVER from input; `undefined` cache is the documented fail-open default.
2. The mutation returns the service's count as the payload `Int!`; expected rejections already carry `extensions.code` (`UNAUTHORIZED` / `FORBIDDEN` / `VALIDATION_ERROR` with `BROADCAST_*` / `NOT_FOUND` with `PLAN_NOT_FOUND`) for the shared `projectMutationFieldErrors` surface.
3. Journey + service suites are the regression pair for 2.M and 6.1 gates; 2.M should re-run both plus the baseline delta check.
4. `BROADCAST_MAX_RECIPIENTS` is exported from the service module — the GraphQL layer should NOT re-declare the cap.
