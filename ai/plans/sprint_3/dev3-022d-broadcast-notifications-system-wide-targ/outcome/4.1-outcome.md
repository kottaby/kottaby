# Task 4.1 Outcome — Apollo authLink additive context-header merge + frontend mutation document

Task: 4.1 — Apollo authLink additive context-header merge + frontend mutation document (REQ-023, REQ-060, REQ-063, REQ-074)
Agent: Implementation Executor (general-purpose sub-agent) — COMPLETION run (prior run canceled mid-flight)
Branch: feat/dev3-022d-broadcast-notifications (verified before every op); NO commit made.

## Files finalized

| File | Op | Status |
|---|---|---|
| `frontend/graphql/sharedDocuments/notifications/broadcast.documents.ts` | CREATED (prior canceled run) — VERIFIED, REUSED | Contract-exact: exports `adminBroadcastNotificationMutationDocument: TypedDocumentNode<AdminBroadcastNotificationMutation, AdminBroadcastNotificationMutationVariables>` for `mutation AdminBroadcastNotification($input: AdminBroadcastNotificationInput!) { adminBroadcastNotification(input: $input) }`; types imported from `@/frontend/graphql/generated/gql/graphql` |
| `frontend/graphql/sharedDocuments/notifications/broadcast.documents.test.ts` | CREATED (prior canceled run) — VERIFIED, REUSED | 8 pins: named op + single-variable surface (`input` ONLY, zero identity vars), variable threaded (no literals), non-null `AdminBroadcastNotificationInput`, bare `Int!` payload (no sub-selection), no token/idempotency material in document text, barrel identity (`toBe` same instance), TypedDocumentNode codegen binding by assignment |
| `frontend/providers/apollo/utils/link-factories.ts` | EDITED (this run) | `createAuthLink` now ADDITIVELY merges `operation.getContext().headers` via object spread; the three fixed writers (`apollo-require-preflight`, `x-apollo-operation-name`, conditional `authorization`) are applied AFTER the spread so context can never clobber them; absent context headers ⇒ byte-identical output (empty spread, key order preserved). Token/preflight/op-name logic otherwise untouched |
| `frontend/providers/apollo/utils/link-factories.test.ts` | CREATED (prior canceled run) — VERIFIED, REUSED | 4 tests through public `ApolloLink.execute` + capturing terminal: absent context headers ⇒ byte-identical 2-writer (no token) / 3-writer (token) output incl. exact key ORDER; present ⇒ merged (`x-idempotency-key`, `x-trace`); fixed keys are final (spoofed preflight/op-name/authorization never clobber) |
| `frontend/graphql/sharedDocuments/notifications/index.ts` | EDITED (this run) | Barrel: added `export * from "./broadcast.documents";` (broadcast listed before notification.documents; top-level sharedDocuments barrel already re-exports notifications) |

Mirrors of all authored files: `/tmp/t41/`.

## Shared-worktree race (recovery evidence)

Parallel agents flipping main⇄feat with `git checkout -f` wiped the two tracked-file edits (link-factories.ts, notifications/index.ts) mid-session (untracked files + untracked worklog.md survived). Both edits re-applied atomically from context + verified byte-exact against `/tmp/t41/` mirrors before every test/QL run; final state re-verified after the last race window.

## Test evidence (all green, one iteration — zero reruns needed)

- `bun run test/scripts/run-test.ts frontend/graphql/sharedDocuments/notifications/broadcast.documents.test.ts` → **8 pass / 0 fail**, 29 expects
- `bun run test/scripts/run-test.ts frontend/providers/apollo/utils/link-factories.test.ts` → **4 pass / 0 fail**, 10 expects
- Regression neighbors: `frontend/graphql/sharedDocuments/notifications/notification.documents.test.ts` → 12/0; `frontend/providers/apollo/error-link.map.test.ts` → 29/0; `frontend/providers/apollo/apolloCache.test.ts` → 11/0. Total **64 pass / 0 fail**.

## Quality Loop status (FINAL — all four files exit 0)

- `bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates` → **exit 0 for ALL FOUR** (broadcast.documents.ts, link-factories.ts, broadcast.documents.test.ts, link-factories.test.ts): tsgo / oxlint / biome:check / lint:type-aware / check:duplicates all pass per file (logs `/tmp/t41/ql-doc.log`, `/tmp/t41/ql-lf2.log`, `/tmp/t41/ql-doctest.log`, `/tmp/t41/ql-final.log`).
- Mid-session the backend `adminBroadcastNotification` mutation landed on this branch (commit `e0831a2`, parallel agent) and the generated types appeared; `bun codegen` re-run → SUCCESS (`AdminBroadcastNotificationMutation`/`-Variables` now exported from `frontend/graphql/generated/gql/graphql.ts`), making document-tier tsgo green with ZERO changes to the document files (binding is by assignment). An earlier codegen attempt (pre-landing) failed exactly as the contract anticipated; `generated/` stayed git-clean.
- One QL iteration on link-factories.ts: oxlint `no-unsafe-type-assertion` on the context-headers narrowing → replaced with a module-scope type guard `isHeaderRecord` (no assertions added anywhere).
- 4.1.SEC verified by test pin: no authorization/bearer/idempotency/token material in the document text; idempotency key rides headers only, never the input DTO.
- 4.1.IV: `frontend/graphql/AGENTS.md` + `frontend/graphql/sharedDocuments/AGENTS.md` conventions followed (`<entityName>MutationDocument` naming, domain barrel export). Note: plan line says `frontend/providers/apollo/utils.ts`; the real file is `frontend/providers/apollo/utils/link-factories.ts` (existing module, tests + AGENTS citations target it).

## Carry-forward for downstream tasks

- **Document export (4.3 consumer contract):** `import { adminBroadcastNotificationMutationDocument } from "@/frontend/graphql/sharedDocuments/notifications/broadcast.documents";` (deep import) or `@/frontend/graphql/sharedDocuments` (top-level barrel — SAME instance, `toBe`-pinned). Typed as `TypedDocumentNode<AdminBroadcastNotificationMutation, AdminBroadcastNotificationMutationVariables>`; call as `adminBroadcastNotificationMutationDocument({ input })` — variables surface is EXACTLY `{ input: AdminBroadcastNotificationInput }`, zero identity variables.
- **Header key (4.3 transport contract):** `x-idempotency-key` — set via the mutation's Apollo context: `context: { headers: { "x-idempotency-key": composeSessionKey } }`. The authLink additively merges it into outgoing headers and forbids clobbering `apollo-require-preflight`, `x-apollo-operation-name`, `authorization`. Never place the compose-session key in the input DTO.
- **Codegen follow-up:** DONE in this session — backend field + `bun codegen` landed; generated types exported; document-tier QL exit 0. No further action for 4.1.
