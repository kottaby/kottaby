# 6.1 Review Round R8 — Hygiene sweep (comments / dead code / cross-layer)

- **Task:** 6.1-R8 (review iteration 8 of ≥10) — READ-ONLY hygiene sweep. No builds/tests/git state touched; only this findings file written.
- **Surface enumeration:** `git diff 70601c9..HEAD --name-only` yields only the post-baseline tail (19 code files). To cover EVERY plan code file the surface was widened to the plan's true base commit: `git diff bbcb363..HEAD --name-only` (bbcb363 = the DEV3-020 audit commit immediately preceding `b020816 feat(notifications): broadcast foundations`). That yields **63 code files**; minus 18 test/test-d files and 2 generated artifacts (`frontend/graphql/generated/gql/graphql.ts`, `schema.graphql`) → **43 non-test runtime files** swept in full: `app/(dashboard)/admin/broadcasts/page.tsx`, backend (repo/enum/graphql/services/types), frontend (views/providers/sharedDocuments), shared/locale.
- **Prior-round findings treated as known (not re-reported):** R1a entityId probe; R1c useRef-eager-UUID + field-error dead path + stale inline errors; R1d gate blind spot; R2 cohort-drift window; R3 batch amplification; R4 plans-error collapse, snackbar-timer reuse (fixed via `successEpoch`), audience aria gap (fixed via `AUDIENCE_ERROR_ID` + `aria-describedby`); R5 enum member-pin gap. All Wave-1 fix doc-comments re-verified in place (`changeAudienceKind` copy-preservation comment Container:85-87, epoch comment :70-71, skin token comments :12/:24 now accurate).

---

## Checklist verification (evidence-first)

### 1. Plan-artifact refs in NON-test runtime comments — 2 NEW violations (F1, F2)

Full-token sweep (`REQ-\d`, `Task \d`, `Phase \d`, `.ai/plans`, `tasks.md`, `specs.md`, `plan.md`, `DEV3-022d`) over all 43 runtime files, plus an extended sweep (`plan §`, `specs? §`, `§\d`, `DB-\d`) to catch document-section and decision-ID spellings. Total hits: **4 lines**; disposition:

- `backend/graphql/pothos/shared/enum.pothos.ts:112` ("owned by DEV3-020") — **pre-existing** (byte-identical at base `bbcb363`; added by the DEV3-020 plan, not this one). Not flagged.
- `frontend/views/dashboard/nav/navItems.ts:82-83` ("REQ-065", "plan §5.2") — **pre-existing** at base (DEV3-010 nav work). Not flagged.
- `frontend/views/dashboard/nav/navItems.ts:102` ("DEV3-022d — a pure ADD") — **inside the documented exempted canonical-retargets block**, mirroring the sibling DEV3-004/DEV3-016/DEV1-005 entries exactly. Convention-consistent; not flagged.
- `shared/locale/types/adminBroadcasts/index.ts:14,22` — **NEW, this plan's file** → findings F1/F2 below.

Test files (exempt tier): plan test headers carry REQ/ticket refs per the established repo pattern (siblings `backend/services/notifications/notification-engine.emit.test.ts`, `backend/services/admin/user-management.chaos.test.ts`, `backend/graphql/test/plan-catalog.roles.test.ts` carry the identical class); `navItems.test.ts` header ("REQ-064 (DEV3-022d)" at :11) mirrors the sibling-entry pattern named in the documented exemption. No NEW inconsistency in any test header.

### 2. Noisy/trivial comments — CLEAN

Every plan-introduced comment read in context (all 43 runtime files): all are contract-bearing (bounds, ownership, invariants, fix rationale) — none repeats the obvious. Repo-wide marker scan over all 63 plan files: **zero `TODO`/`FIXME`/`HACK`/`XXX`/`eslint-disable`**; the only `@ts-expect-error` cluster is the conformance suite's documented negative-probe idiom (`contracts.conformance.test-d.ts:216-520`, header-explained). The pre-existing generic one-liners in `link-factories.ts` ("Helper to create auth link…", :6/:11/:18/:53) pre-date the plan — the plan's added block (:23-27) is substantive.

### 3. Dead code — 1 new finding (F3), 1 completeness note (F4)

- Every new export cross-checked for importers repo-wide: `BROADCAST_MAX_RECIPIENTS` (service + integration test), all `broadcast-compose.helpers` exports, pothos inputs, enum/guard pairs (`isBroadcastAudienceType`), documents, link factories — all consumed. Exception: F3.
- Unreachable branches: the repository's per-case `== null` guards + `default` throw arms and the claim-cache `error` listener are the DOCUMENTED accepted classes. `admin-broadcast.service.ts:212-217` adds one analogous arm beyond those two named files → F4 (completeness note only). The mutation's `if (!ctx.user)` arm (admin-broadcast.mutation.ts:80-83) is the repo-standard documented TS-narrowing arm (no-non-null-assertion rule), sibling-consistent — accepted class, not flagged.

### 4. Cross-layer imports — CLEAN

Every import line of all 63 files inspected: `shared/locale/**` imports ONLY `@/shared/locale/*`; `frontend/views|providers|graphql` import only `@mui/*`, `react`, `@apollo/*`, `crypto-js`, `@/frontend/*`, `@/shared/*`, and `@/frontend/graphql/generated` (allowed); backend including `backend/graphql/**` imports only backend-internal modules (+ the pre-existing allowed backend→shared `recitation-reading.enum` at enum.pothos.ts:37); `app/(dashboard)/admin/broadcasts/page.tsx` imports `@/backend/enum/users/user-role.enum` + frontend + shared, byte-pattern-identical to the sibling `app/(dashboard)/audit/page.tsx`. **Zero frontend→backend, zero shared→frontend/backend, zero backend/graphql→frontend.**

### 5. Console usage — CLEAN

`rg '\bconsole\.'` over ALL 63 plan files (runtime + tests): zero call sites. The single textual hit is the string literal inside `redis-claim-cache.test.ts:382-384` — the file's own static-scan test asserting the module source contains no `console.`. Logger discipline holds (`logger.logDomainError` only, exactly as documented).

### 6. Naming — CLEAN

`Broadcast*` prefix consistent across every layer: `BroadcastAudienceType` (enum + wire), `BroadcastAudienceSelector`, `BroadcastNotificationSubmitInput`, `BroadcastAudienceRepository`, `AdminBroadcastService`, `BroadcastCompose{Header,Form,Fields,Companions,ConfirmDialog,SuccessSnackbar,Container}`, `broadcast-compose.{helpers,skin}.ts`, `adminBroadcastNotificationMutationDocument`, wire `AdminBroadcastNotificationInput`/`BroadcastAudienceInput`. No abbreviated names introduced; the short identifiers in use (`t`/`tc`/`tu` translation handles, `tx` transaction) follow the repo's established idiom.

---

## Findings

**[LOW] shared/locale/types/adminBroadcasts/index.ts:14 — runtime JSDoc cites the internal plan document by section: "Audience rules mirrored by the key set (plan §5.5):".** This file is a NEW non-test runtime artifact of this plan, and "plan §5.5" points at the plan's `plan.md` §5.5 — an internal planning artifact reference that dies the moment the plan is archived. Repo-wide, no sibling runtime file uses the "plan §" spelling at all (the only other hit, `navItems.ts:83`, is pre-existing DEV3-010 work covered by the navItems convention). Fix: reword to self-contained contract language, e.g. "Audience rules mirrored by the key set:" — the four bullet lines below it already state the rules completely; the citation adds zero information for a runtime reader.

**[LOW] shared/locale/types/adminBroadcasts/index.ts:22 — runtime JSDoc cites plan-spec artifacts: "There is deliberately NO recipient-count preview label (REQ-063/DB-6):".** Same file/header block as F1: `REQ-063` is this plan's spec requirement id (specs.md:112) and `DB-6` its decision-table id (specs.md:192) — internal plan artifacts, non-test runtime file, documented exception covers only the navItems convention. Mitigating context: the sibling `shared/locale/types/plans/index.ts:50` carries a bare "REQ-062" ref from an earlier plan, so a REQ-ref residue pattern exists in this directory — but that precedent does not cover NEW plan files, and the `DB-6` decision-id component has no sibling precedent anywhere. Fix: drop the parenthetical (the sentence is already fully self-explanatory) or restate the rationale inline ("no pre-send count exists by design").

**[LOW] frontend/views/admin/broadcasts/broadcast-compose.helpers.ts:27,78 — `ComposeFieldPath` and `isComposeFieldPath` are exported but imported by NO other module in the repo.** `isComposeFieldPath` is consumed only in-module (passed to `applyProjectedFieldErrors` at :147) and `ComposeFieldPath` only via the in-file `ComposeFieldErrors` alias (:30, which IS legitimately exported). The `export` keyword on both is surplus API surface — knip (whose `ignore` list in `knip.config.ts` has no broadcast entries) would report them as unused exports on the next clean run. Fix: drop the `export` keyword from both (zero call-site changes needed), or — if they are deliberately published as the shared-seam contract for future server `fields[]` emitters — add the one-line comment saying so, mirroring the documented-public-API pattern used in `knip.config.ts` for `whatsapp/cloud-api/index.ts`.

**[INFO] backend/services/notifications/admin-broadcast.service.ts:212-217 — the audience-coherence `switch` `default` arm is runtime-unreachable** (the preceding `isBroadcastAudienceType` guard at :190 admits exactly the four members the switch enumerates, and the enum's runtime values are checked via `Object.values` in the guard itself). Listed for completeness because it sits beyond the two files the round instructions name as accepted; however it is (a) self-documented with the correct rationale ("TS enums are erased at runtime… same posture as the audience repository's unhandled-kind arm") and (b) the exact fail-closed idiom already accepted in `broadcast-audience.repository.ts:155-160/218-220`. Treated as within the accepted class — no action required; recorded only so the sweep's unreachable-branch inventory is exhaustive.

---

## Verdict

Counts: CRITICAL 0 · HIGH 0 · MEDIUM 0 · LOW 3 (F1, F2, F3) · INFO 1 (F4) → **PASS** (no CRITICAL/HIGH/MEDIUM; all three LOWs are comment/export-surface hygiene, none touches runtime behavior, wire contracts, or security posture).

Hygiene posture is otherwise exemplary: zero plan-artifact leakage outside one locale-types header block, zero console/TODO/suppression markers, zero cross-layer violations across 63 files, canonical `Broadcast*` naming throughout, and all new exports consumed except the two over-exports of F3. The comment-tier sweep also re-verified the three Wave-1 doc-comment fixes from R4/R5 are in place and accurate. Per the ≥10-iteration protocol (stop = 2 consecutive zero-new-findings), the zero-new-findings streak remains at 0 going into R9; F1/F2/F3 are single-comment/single-keyword fixes suitable for batching into the next fix wave (or accepting as-is if the synthesizer rules the sibling REQ-ref precedent extends to locale type files).
