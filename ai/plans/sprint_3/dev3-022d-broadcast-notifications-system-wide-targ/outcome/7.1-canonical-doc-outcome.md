# Task 7.1 — Canonical Doc — Outcome

**Created:** `docs/notifications/broadcast-notifications.md` (10 sections + test map, ~150 lines).

## Section map

1. Provenance + composition posture (engine substrate DEV3-010, admin governance substrate)
2. What it is — live-surface map (mutation/page/service/repo/cache/i18n)
3. Cohort taxonomy — frozen four kinds, TS-only enum ruling, per-kind resolution semantics, governance-exclusion ruling (deleted/blocked OUT, suspended deliberately IN + invariant posture), ordering/dedup
4. Authorization — double wall + strict denial ordering with zero-state-on-denial
5. Idempotency — header-only contract, rotation-after-success, RedisClaimCache (SET NX EX, 2s bounds, fail-open), replay semantics (sequential guarantee / parallel §3.6 window / corrupt-receipt fail-open) + the ledgered cohort-drift residual
6. Atomicity — one tx, single-writer engine insert, publish strictly post-commit
7. Recipient cap 5000 fail-closed + no-preview oracle posture
8. Audit contract — one metadata-only row per accept, null entityId, zero-on-denial, trigger interplay
9. Verbatim-copy localization boundary + Arabic CLDR plural carrier
10. What NOT to do + Test map (evidence suites) + import-by-reference to the engine doc

## Style compliance

- Zero task IDs / REQ ids / decision-record ids / plan paths in the doc body (provenance line names the feature ticket; everything else is system description).
- Structure/tone calibrated against `docs/notifications/realtime-engine.md`; engine internals cited by reference (§3.2/§3.6), not duplicated.

## Quality-loop note (tool-reasoned exclusion)

`bun run scripts/health/sub-loop.ts docs/notifications/broadcast-notifications.md --lifecycle duplicates` structurally exits 1 on any `.md` (oxlint stage reports "No files found to lint") — the documented tool limitation from the 0.1 baseline outcome §7. The doc passes the repo's other gates implicitly (markdown-only change; tsgo/lint/biome do not target .md content). Recorded here per the Fix-Or-Report protocol.

## Carry-forward

- The doc consolidates the review waves' semantic findings (cohort-drift residual, governance-gate residual, Arabic plurals) as permanent reference statements — future emitters inherit them by reading §4-§9.
