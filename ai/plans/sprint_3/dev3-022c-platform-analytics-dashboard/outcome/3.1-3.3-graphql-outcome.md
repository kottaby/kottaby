# Tasks 3.1–3.3 — GraphQL resolvers & API surface outcome

## What was done

### Task 3.1 — Pothos objects
- CREATE `backend/graphql/pothos/admin/platform-analytics.pothos.ts` — eleven
  `objectRef<CanonicalType>` implementations (`PlatformAnalytics` root +
  Users/Sessions/Revenue/CurrencyRevenue/Subscriptions/Teachers/Ratings/Health/
  SessionTrendPoint/RevenueTrendPoint), all backed by canonical types from
  `@/backend/types` (REQ-004 — zero local types). `t.exposeInt` counters,
  `t.exposeString` money, `DateTime` scalar for `generatedAt`/`bucketStart`
  (REQ-068 — NO `.toISOString()` anywhere), `exposeFloat(…, { nullable: true })`
  ONLY on the two rating averages (REQ-060). NO `id` in the subtree (D10). Every
  field projects the composed snapshot — zero resolve-time data access.
- UPDATE `backend/graphql/pothos/admin/index.ts` — barrel re-export.

### Task 3.2 — admin query + schema regeneration + D13 reconciliation
- CREATE `backend/graphql/query/admin/platform-analytics.query.ts` — the thin
  hive: `queryField("adminPlatformAnalytics", …)` with the load-bearing
  `$all: { authenticated, role: [UserRole.Admin] }` conjunction, PRE-RESOLVER
  `ctx.user` null check throwing localized `UnauthorizedError` via
  `await ctx.t("errorsTranslations")`, resolver body passing ONLY
  `ctx.user.id` + `ctx.locale` to the service, NO args, NO try/catch,
  description per plan §3.2 verbatim.
- UPDATE `backend/graphql/query/admin/index.ts` — side-effect import.
- Regenerated artifacts: `bun run generate:gqlSchema && bun codegen` — committed
  `frontend/graphql/generated/schema.graphql` + generated modules in the SAME
  change set.
- D13 BASELINE RECONCILIATION (`sdl-static-assertions.test.ts` +
  `schema-surface.test.ts`): the bundled inventories omitted the already-shipped
  DEV3-016 admin-users operations AND plan-catalog mutations — the frozen
  baselines were recomputed verbatim from the live schema: Query root = 15
  fields, Mutation root = 13, enums += `AuditActionType` +
  `AdminUserGovernanceFilter`, type inventory += the 12 Admin* family names; the
  delta pins now list ONLY the DEV3-022c growth (`adminPlatformAnalytics` +
  the 11 `PlatformAnalytics*` types). New per-type SDL pins: zero-arg root,
  no-id subtree, root-section contract, DateTime/String! money typing, nullable
  float pins, no mutation/input/subscription growth. The "NO Subscription root"
  lexical check was narrowed to whole-word root-type regexes (a blanket
  substring ban false-positived on the legitimate `PlatformAnalyticsSubscriptions`
  value object).
- Verified: `public-operations.ts` frozen six untouched (analytics NOT public);
  `handshake-code-surface.test.ts` green untouched; `plan-catalog.schema.test.ts`
  committed-vs-live SDL equality green; NO `app/api/**` change.

### Task 3.3 — wire matrix
- CREATE `backend/graphql/test/platform-analytics.query.test.ts` (10 tests, 168
  assertions) over the REAL composed schema + REAL service/repo stack:
  1. anonymous → UNAUTHORIZED pre-resolver (resolver spy: ZERO calls).
  2. student/teacher/parent → FORBIDDEN pre-resolver (spy: ZERO calls — the
     `$all` conjunction is load-bearing).
  3. ANY argument → validation failure pre-resolver (Unknown argument; spy:
     ZERO calls) + static zero-arg SDL pin.
  4. admin happy path → the full CLOSED shape over the live stack: root
     sections exactly once, every documented leaf, no extras, trend arrays
     (30-bucket skeleton), per-currency row leaves, rating averages present-
     and-nullable, in-process DateTime payload handling, read purity
     (audit/notifications deltas == 0, zero domain logs).
  5. governed (deleted) admin → service-tier FORBIDDEN with the canonical
     translated message + bounded payload, zero partial data, exactly ONE
     bounded domain log; plus the D8 backstop pin (the governed admin PASSES
     the scope engine — the translated message proves resolver → service
     traversal).
- Assertions use runtime type guards (isRecord/isRecordArray) — zero unsafe
  type assertions (oxlint clean).

## Verification

| Gate | Command | Result |
| --- | --- | --- |
| Typecheck | `bun tsgo` | exit 0 |
| Biome | `bun run biome:check` | exit 0 (1089 files, no fixes) |
| Sub-loop ×5 files | `--lifecycle duplicates` | all exit 0 |
| Wire matrix | `run-test.ts backend/graphql/test/platform-analytics.query.test.ts` | **10 pass / 0 fail**, 168 assertions |
| SDL pins | `run-test.ts backend/graphql/test/sdl-static-assertions.test.ts` | 24 pass / 0 fail |
| Schema surface | `run-test.ts backend/graphql/test/schema-surface.test.ts` | 33 pass / 0 fail |
| Handshake (public ops) | `run-test.ts backend/graphql/test/handshake-code-surface.test.ts` | 22 pass / 0 fail |
| Plan-catalog SDL | `run-test.ts backend/graphql/test/plan-catalog.schema.test.ts` | 5 pass / 0 fail |
| Schema purity | `git diff -- backend/db/schema/ backend/db/migration/` | EMPTY |

## Files touched

- NEW: `backend/graphql/pothos/admin/platform-analytics.pothos.ts`,
  `backend/graphql/query/admin/platform-analytics.query.ts`,
  `backend/graphql/test/platform-analytics.query.test.ts`
- MODIFIED: `backend/graphql/pothos/admin/index.ts`,
  `backend/graphql/query/admin/index.ts`,
  `backend/graphql/test/sdl-static-assertions.test.ts` (D13 reconcile + pins),
  `backend/graphql/test/schema-surface.test.ts` (D13 reconcile),
  `frontend/graphql/generated/**` (codegen artifacts)
- tasks.md Phase 3 checkboxes flipped.

## Deviations

- The HTTP-tier wire variant (setupTestServerLifecycle) was skipped per the
  task's "when the port window allows" allowance; the in-process execution runs
  the real scopes/resolver/service stack.
- The blanket `Subscription` substring ban became a whole-word root-type regex —
  intent-preserving, required by the new legitimate type name.
- None otherwise.
