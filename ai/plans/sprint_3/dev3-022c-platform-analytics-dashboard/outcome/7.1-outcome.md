# Task 7.1 Outcome — Canonical Doc `docs/admin/platform-analytics.md`

**Feature:** `dev3-022c-platform-analytics-dashboard`
**Agent:** Knowledge Propagation Subagent (7.1+7.2)
**Date:** 2026-09-05
**Mode:** Documentation creation — ONE new file; zero code files touched; no commit made; tasks.md untouched.

---

## 1. What was created

`docs/admin/platform-analytics.md` — the canonical reference for the `adminPlatformAnalytics` surface, content per the task card EXACTLY. Structure mirrors the sibling admin docs (`docs/admin/user-management.md`, `docs/admin/audit-trail.md`: header block with Domain/Specs/Status → Why → Read-Surface Contract → Rules sections → What NOT to Do → Rollout Summary → Related Documents; ILLUSTRATIVE/NON-AUTHORITATIVE code blocks with canonical path:line citations).

Outline:

1. **Why** — FR-10.6 mandate; the five governing constraints with § pointers.
2. **Read-Surface Contract** — zero-argument `adminPlatformAnalytics: PlatformAnalytics!` closed shape; aggregate anonymity (no `id` in any of the eleven types); `$all` authScopes conjunction (load-bearing, ANY-semantics hazard); thin resolver; NOT public (frozen six intact); `DateTime`-scalar discipline (the only `String` leaves are money/currency); the ONLY nullable fields = the two rating averages; **§2.1 the metric definitions table covering EVERY field** (below).
3. **Temporal Contract** — single snapshot (§3.1), UTC boundary rulings table (§3.2), clock-source exceptions incl. the `getStats` skew waiver (§3.3), statement-level snapshot-consistency ruling + rejected `REPEATABLE READ` (§3.4).
4. **Money, Currencies, and Honesty** — money-as-string + currency containment + honest-EMPTY arrays (§4.1), the verbatim offline-activation honesty note (§4.2), honest-null ratings (§4.3), the one sanctioned display-only `Number(amount)` recharts pivot (§4.4).
5. **Authorization** — the governed-admin service-tier divergence from `assertActorAdmin` WITH rationale.
6. **Read Purity** — the no-write/no-audit rule + the test-locked proof obligation.
7. **Behavioral Contract** — pointer to `test/workflows/admin/platform-analytics.journey.test.ts` (journeys A–D).
8. **What NOT to Do** — the full prohibited list.
9. **Trend Day-Bucket Decoder Contract** — strict ISO rebuild; throws on unparseable.
10. **Rollout Summary** — shipped files + test inventory.
11. **Related Documents** — substrate reuse, error/gateway/SQL/invariant/harness docs, authoritative implementations, journey suite.

### Metric definitions table (§2.1) — coverage proof (every field)

| Section | Fields covered | SQL semantics recorded (incl. enum members feeding each counter) |
|---|---|---|
| Users (11) | `totalCount`, `activeCount` (NULL-safe coalesce governance chain), `suspendedCount`/`blockedCount`/`deletedCount` (MAY overlap), 4 role counters (partition `total` exactly), `newThisWeekCount` (JS-computed 7-day cutoff bound as parameter + skew-waiver §3.3 pointer), `recentlyActive24h` (`last_active_at > now−24h` strict `>`, NULL-safe governance exclusion; B.15 consumer) | Ten counters consumed VERBATIM from `AdminUserRepository.getStats` (never re-declared/forked) |
| Sessions (10) | `total`; `today`/`thisWeek`/`thisMonth` (closed ranges `[boundary, now]`); 5 × `SessionStatus` members (`scheduled/started/completed/cancelled/disputed` — all present even when zero; partition `total`); `awaitingConfirmation` = `status='completed' AND confirmed_by_student_at IS NULL` (lifecycle-derived, the student-confirmation FLIP documented) | One bare aggregate over `session`; empty table → one row of zeros |
| Revenue | `gatewayRevenueByCurrency` row shape (`currency`, all-time `totalAmount`, windowed `last30DaysAmount`, all-time `paidPaymentsCount`); `offlineActivationsCount` (`payment_method IN ('offline_cash','bank_transfer','scholarship')`) | `student_payments WHERE status='paid' GROUP BY currency ORDER BY currency ASC`; `coalesce(sum(amount),0)::text`; offline structural absence + the §4.2 verbatim note |
| Subscriptions (7) | `total`; 5 × `SubscriptionStatus` members (`active/pending/expired/cancelled/suspended`); `activeInWindowNow` (ACTIVE-window predicate verbatim: `coalesce(start_date,now) <= now AND (end_date IS NULL OR now < end_date)` — strict `<`; NULL start = starting now; expired-end-date-with-active-status excluded) | Mirrors the directory's proven shape with `now` bound as a parameter instead of SQL `now()` |
| Teachers (3) | `certifiedCount` (`is_approved=true`), `evaluatorCount` (`is_evaluator=true`), `onlineNowCount` (`is_approved=true AND is_online=true`; ⊆ certified); applicants structurally excluded (B.6/B.7) | One bare aggregate over `teacher` |
| Ratings (4) | `averageSessionRating` (0–5 band, non-null values only, `round(avg::numeric,2)::float8`), `sessionRatingsCount` (non-null count), `averageEvaluationScore` (0–100 band, soft-delete excluded NULL-safely), `evaluationScoresCount`; honest-null when empty | Two single-row reads |
| Health (2) | `pendingDisputes` (`session.status='disputed'`), `pendingWithdrawals` (`type='withdrawal' AND status='pending'`); no other indicator may be silently added | Two counts |
| Trend points | `bucketStart` (true midnight-UTC `Date`), `sessionCount`; revenue (day, currency, decimal-string `amount`) | SPARSE reads; zero-fill/expansion is the service's duty |

### Required content items — all present

- **Single-snapshot/captured-now contract (§3.1):** `generatedAt` = the ONE captured `new Date()` per request; every windowed read receives the same `Date` reference; eleven reads in one `Promise.all` over the same tx; wall clock never re-sampled mid-request.
- **UTC boundary rulings (§3.2):** day / ISO-week Monday / month / 24h / 30-day closed window `[now − 30d, now]` — **including the 31-day-touch vs 30-bucket skeleton note**: the rolling window touches 31 UTC calendar days whenever `now` is not exactly midnight; the boundary-day sliver means a currency's scalar `last30DaysAmount` may legitimately exceed the sum of its 30 displayed trend buckets; documented contract, not a bug; "fixing" it is listed under What NOT to Do.
- **Money-as-string + currency containment + honest-EMPTY (§4.1)** and **the sanctioned display-only recharts pivot (§4.4):** `Number(amount)` for plot geometry ONLY — never for math/aggregation; `formatMoneyAmount` groups digits without constructing a number; the revenue table keeps wire strings verbatim; the exception is the ONE blessed conversion.
- **Offline-activation honesty note (§4.2), verbatim statement:** "`gatewayRevenueByCurrency` excludes offline activations" (offline cash / bank transfer / scholarship bypass the `student_payments` ledger entirely — INV-PAY5); counted as their OWN metric; **mixing the two into one number is prohibited**; anchored to `OFFLINE_ACTIVATION_GATEWAYS` and both executor branches.
- **Honest-null rating averages (§4.3):** `null` iff count = 0; "no ratings yet" ≠ "rated zero"; frontend renders `—`, never a fabricated `0.00`.
- **Read-purity/no-audit rule (§6):** zero writes / audit / notifications on ALL paths; happy path structurally silent; freshness = fresh DB read per request (no cache, no memoized module state); test-locked proof obligation (byte-identity digests + zero audit/notification deltas).
- **Governed-admin service-tier divergence from `assertActorAdmin` WITH rationale (§5):** the documented non-fail-closed GraphQL context window; absent actor → `ForbiddenError` via the shared `assertActorAdminActive` gate (the documented amendment); deterministic deleted → blocked → suspended; rationale = suspended-admin-with-live-token would otherwise read whole-platform aggregates; reuse keeps role-check semantics un-forked; observable deltas sanctioned.
- **`getStats.newThisWeekCutoff` `Date.now()` skew waiver (§3.3):** ms-scale skew vs the captured `now`; reuse-not-rebuild forbids modifying the shared substrate to accept a caller-supplied `now`; forked `getStats` would create a second source of truth.
- **Statement-level snapshot-consistency ruling + rejected `REPEATABLE READ` (§3.4):** per-statement visibility; cross-metric drift accepted and documented (monitoring dashboard ≠ ledger); `REPEATABLE READ` considered and REJECTED (locks nothing relevant, costs a longer-lived snapshot, solves no requirement).
- **"What NOT to do" list (§8):** no caching layer; no new error codes; no per-entity drill-downs/arguments/exports on this surface (no LIKE surfaces → no `escapeLikeWildcards` obligation, re-evaluation owed by any future drill-down ticket); no cross-currency sums; no `id` on value types; no `now()` re-sampling; no `toISOString()`; no LIKE/string-interpolated SQL; no offline/gateway mixing; no money-to-number math (pivot exception); no "fixing" the 31-day-touch divergence.
- **Journey-suite pointer (§7):** `test/workflows/admin/platform-analytics.journey.test.ts` as the authoritative behavioral contract with the A–D summaries.
- **Trend day-bucket decoder contract (§9):** `decodeTrendDayBucket` strict ISO rebuild; input driver-dependent (parsed `Date` client-locally OR raw text); **throws a domain error on unparseable input — never a silent NaN**; raw branch normalizes server-side with `AT TIME ZONE 'UTC'` for cross-TZ branch parity.

---

## 2. Anchoring & spot-checks (7.1.SR)

Every claim is anchored to shipped code via path(:line) citations. Explicit spot-verification of THREE metrics against the repo files (grep-level evidence):

| # | Metric claim in doc | Shipped code evidence | Result |
|---|---|---|---|
| 1 | `sessions.awaitingConfirmation` = `status='completed' AND confirmed_by_student_at IS NULL` | `platform-analytics-query-helpers.ts:274` (tx branch) + `:291` (raw branch `filter (where status = $7 and confirmed_by_student_at is null)`) | ✅ MATCH |
| 2 | `subscriptions.activeInWindowNow` = active AND `coalesce(start_date,$now) <= $now AND (end_date is null or $now < end_date)` (strict `<`) | `platform-analytics.repository.ts:262-266` (tx) + `:278-282` (raw; `end_date is null or $6 < end_date` at `:281`) | ✅ MATCH |
| 3 | `gatewayRevenueByCurrency` = `student_payments WHERE status = $3 GROUP BY currency ORDER BY currency ASC` with `coalesce(sum(amount),0)::text` | `platform-analytics-query-helpers.ts:394,398,400` (`:text` at `:394`, paid filter at `:398`, currency ASC at `:400`; windowed `last30DaysAmount` filter at `:395`) | ✅ MATCH |

Additional verifications (read directly during authoring): `countRecentlyActiveUsers` strict-`>` 24h + NULL-safe governance (`repository.ts:117-143`, `RECENT_ACTIVITY_WINDOW_MS` `:88`); `getTeacherPresenceStats` trio (`:320-351`); `getRatingStats` honest-null + soft-delete exclusion (`:366-414`); `getHealthIndicators` pair (`:424-460`); `AdminUserRepository.getStats` ten counters + `newThisWeekCutoff = new Date(Date.now() - 7d)` at `admin-user.repository.ts:179,178-213`; service pipeline gate-first/single-`now`/`Promise.all`/assembly (`platform-analytics.service.ts:134-179`, skeleton `:58-63`, merge/expand `:71-115`); query `$all` scopes + thin resolver (`platform-analytics.query.ts:48-70`); decoder throw-on-unparseable (`platform-analytics-query-helpers.ts:179-186`); wire surface (eleven types + `adminPlatformAnalytics: PlatformAnalytics!` zero-arg, `frontend/graphql/generated/schema.graphql:502-590,607`); the sanctioned pivot `Number(row.amount)` display-only (`frontend/views/admin/analytics/platform-analytics-display.ts:94-114`).

No prose-only phantoms introduced: every SQL predicate, error code, boundary rule, and prohibition in the doc was read out of the shipped files before being written down.

**Clean-language check:** no `.ai/plans/` paths, no task IDs, no plan-artifact references anywhere in the doc (grep-verified: `\.ai/plans|tasks\.md|RULING|Task [0-9]|plan\.md` → 0 matches). References follow the sibling-doc convention: canonical requirement IDs (FR-10.6, INV-PAY5, B.9/B.15/B.6/B.7), the ticket ID `DEV3-022c` in the Rollout Summary (mirrors `docs/admin/user-management.md`'s DEV3-016 usage), doc-scope rule tags (JR-C-1-style pointers excluded deliberately), and the shipped code paths.

## 3. Quality loop (7.1.QL)

| Gate | Result |
|---|---|
| `bun run scripts/health/sub-loop.ts docs/admin/platform-analytics.md --lifecycle duplicates` | Discovery ran (applicable rule files: root `AGENTS.md`); **tsgo passed** (no project errors); **oxlint short-circuits on markdown** — "No files found to lint" → gate stops at exit 1. **Runner limitation, not a content failure:** confirmed identical behavior on an UNMODIFIED pre-existing markdown doc (`docs/admin/audit-trail.md` — same tsgo-pass → oxlint-zero-files stop), i.e. markdown is not a lintable file type for this repo's runner. |
| Mandated fallback `bunx @biomejs/biome check docs/admin/platform-analytics.md` | `Checked 0 files in 4s. No fixes applied.` — biome.json's file set does not include markdown (report: "These paths were provided but ignored"), so no content diagnostics exist for `.md` in this repo; recorded per the task's fallback instruction. |
| Content gates (manual, since no markdown linter exists in-repo) | Structure mirrors sibling docs; all anchors re-read against shipped code; plan-artifact grep 0; no console/log claims; no code blocks claimed authoritative. |

## 4. Files

| File | Action |
|---|---|
| `docs/admin/platform-analytics.md` | CREATE (~250 lines) |

No other file touched by this task. `docs/notifications/realtime-engine.md`, `shared/AGENTS.md`, `docs/specs/state-machine-invariants.md` untouched (verified empty diffs). No commit made.

*End of Task 7.1 outcome.*
