# Task 2.3 Outcome — Journey C: Freshness Evolution / Anti-Cache Proof (TEST-FIRST, RED)

**Feature:** `dev3-022c-platform-analytics-dashboard`
**Branch:** `feat/dev3-022c-platform-analytics-dashboard` (no commit made — working tree only)
**Date:** 2026-09-04
**Scope:** Journey C block of `test/workflows/admin/platform-analytics.journey.test.ts` (lines ~1032–1125). No production files touched.

---

## 1. Intent

Prove the REQ-074 Journey-C EARS: WHEN two sequential admin reads bracket a new payment THEN the second read exposes exactly the delta — **a cached answer is a correctness bug here** (REQ-021: the database is read on EVERY request, no server cache, no memoized module state).

## 2. Steps (ordered, actor-attributed)

| Step | Actor | Action | Observed |
|---|---|---|---|
| 1 | Admin | `readAnalytics(admin.userId)` (t1) — bracketed by byte-identity snapshots | t1 snapshot: `generatedAt`, session counters, EGP/USD revenue rows, both trends |
| 2 | System | ONE committed `db.transaction`: `createTestStudentPayment(tx, studentActor.userId, subscriptionIds[0], { amount: "123.45", currency: "EGP" })` (today) + `createTestSession(tx, onlineTeacher.userId, studentActor.userId, { status: Completed, startedAt/endedAt/confirmedByTeacherAt = now })` (today, unconfirmed) | +1 payment id / +1 session id tracked for teardown |
| 3 | Admin | `readAnalytics(admin.userId)` (t2) — bracketed by byte-identity snapshots against the post-fixture state | t2 snapshot |

## 3. Asserted freshness oracle

- `generatedAt(t2) > generatedAt(t1)` (strictly — a cached `generatedAt` fails immediately) and `expect(readT2).not.toEqual(readT1)` (the whole-snapshot anti-cache gate).
- Same read day guard: `utcDayStart(t1) === utcDayStart(t2)` (bucket alignment).
- Sessions: exactly `total+1`, `today+1`, `thisWeek+1`, `thisMonth+1`, `completed+1`, `awaitingConfirmation+1`; `scheduled/started/cancelled/disputed` unchanged.
- EGP ascends by the EXACT fixture amount via string-normalized compare — `t2Egp.totalAmount === formatMinorUnits(toMinorUnits(t1Egp.totalAmount) + toMinorUnits("123.45"))` (integer minor-unit `bigint` arithmetic, never float); same for `last30DaysAmount`; `paidPaymentsCount+1`.
- **EGP remains the SAME currency row**: exactly one EGP row on both reads (row ascends within its bucket — no new currency row minted) and the USD row is byte-identical between t1 and t2 (`t2UsdRows toEqual t1UsdRows`).
- Trend buckets: `sessionTrendDaily` **last bucket +1** (30-bucket equality against t1's projection with only the last point incremented); `revenueTrendDaily` equal to t1's projection with ONLY the EGP-today point ascended by the exact amount (every other (day, currency) point untouched).
- Purity oracles across both reads: post-fixture byte-identity windows around BOTH reads + whole-suite audit/notification baselines unchanged.

## 4. RED evidence (verbatim — shared file)

```
test/workflows/admin/platform-analytics.journey.test.ts(104,42): error TS2307: Cannot find module '@/backend/services/admin/platform-analytics.service' or its corresponding type declarations.
```

Runner: `error: Cannot find module '@/backend/services/admin/platform-analytics.service' from '/home/z/my-project/test/workflows/admin/platform-analytics.journey.test.ts'` — `0 pass / 1 fail / 1 error`. Expected RED until Task 2.6.

## 5. Semantic review (2.3.SR)

1. Two INDEPENDENT service invocations — each `readAnalytics()` is a fresh top-level call; no shared module state could leak between them (asserted, not assumed: t2 must DIFFER from t1 in exactly the committed delta — a memoized snapshot would fail `not.toEqual` and/or the `generatedAt` gate).
2. Both reads are still admin-gated (2.3.SEC): the actor gate runs per call through `assertActorAdminActive` semantics — nothing about freshness weakens it (denials for the same actor id are covered in Journey D).
3. Exact-amount arithmetic on decimal strings via integer minor units (`toMinorUnits` regex-parses, no `parseFloat` anywhere); the C payment amount `"123.45"` is chosen distinct from B's `"150.75"` so the delta is unambiguous.
4. The C fixtures commit in their OWN `db.transaction` (separate from journeys A/B/D cast transactions) — commit-or-nothing per journey step; ids tracked in the shared teardown lists.
5. The unconfirmed completed session doubles as a second `awaitingConfirmation` flip probe at freshness time (t2 delta pins the lifecycle-derived counter, not just `completed`).

## 6. Instruction verification (2.3.IV)

Journey rules 1–12 compliant (committed fixtures, no rollback wrapper, tracked ids, real actors, no `console.*`/`any`, `bun:test`, `@/` aliases); tests.instructions relative-timestamp + money-string rules compliant; Biome clean (`No fixes applied`); sub-loop exits 1 at tsgo on the sanctioned RED only — definitive pass deferred to Task 2.6.

## 7. Carry-forward

- Task 2.6 must capture `now = new Date()` ONCE per request and derive `generatedAt` + every window from it — the `generatedAt(t2) > generatedAt(t1)` plus exact-delta assertions are the anti-cache/anti-clock-skew oracle.
- The +1 session/+1 payment ids are tracked and hard-deleted in the shared `afterAll` (payments under the RULING-1 suspension wrapper FIRST).

*End of Task 2.3 outcome.*
