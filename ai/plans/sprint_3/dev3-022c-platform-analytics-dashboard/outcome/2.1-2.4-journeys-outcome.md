# Tasks 2.1–2.4 — Journey suite outcome (REBUILD)

> Rebuild note: the catastrophe wipe destroyed the original test-first RED phase.
> This rebuilt suite was authored AFTER the repository/service landed (`dcb354e` /
> `68db51c`) and is GREEN by construction — the inversion is recorded honestly; no
> RED state is claimed. The 6.4 pentester outcome's "stale TEST-FIRST RED comment"
> INFO finding is thereby resolved: the rebuild carries no stale RED markers.

## What was done

- CREATE `test/workflows/admin/platform-analytics.journey.test.ts` (~915 lines,
  10 tests, 563 assertions) — the four journeys in ONE shared file per plan:

  - **Journey A — cold platform honesty** (3 tests): admin-only cast; every
    journey-owned metric equals DIRECT-DB oracles (users section == reused
    `AdminUserRepository.getStats` + independent 24h-presence oracle; sessions ==
    direct counts; per-currency paid totals == BigInt-cents oracle; teachers ==
    NULL-safe presence oracle; ratings == honest null⟺empty duality with exact
    `round(avg,2)` parity; health == disputed/pending-withdrawal oracles); 30-bucket
    UTC-midnight session-trend skeleton with the last bucket == today's direct
    window count; revenue trend EMPTY-or-full-grid window honesty; cold read silent.
  - **Journey B — full cast observation** (1 test): ACTIVE-window subscription paid
    through the OFFLINE gateway (one fixture moves active + active-in-window +
    offline-activation), EGP + USD paid payments today, cast teacher flipped
    online, five sessions (4 today + backdated cancelled), rated report, scored
    evaluation, pending withdrawal — all committed in ONE transaction, every
    metric moving by EXACT deltas (incl. per-(bucket,currency) trend-grid deltas
    and the +4-today mapping recorded below).
  - **Journey C — freshness evolution / anti-cache proof** (1 test): two reads with
    one committed mutation between them move by exactly +1 in the mutated members
    only; `generatedAt` non-decreasing; the trend re-reflects each read's own
    captured `now`.
  - **Journey D — denial & purity matrix** (5 tests): deterministic chain —
    anonymous/malformed (0/−1/NaN/2.5) → absent id → non-admin (student/teacher/
    parent) → governed precedence deleted → blocked → suspended (suspension probed
    on a non-deleted, non-blocked admin); each denial maps to
    UnauthorizedError/ForbiddenError with the translated substring and exactly ONE
    `logDomainError` { code, entity: "users", entityId, locale } captured via a
    single-call `probeDenial` spy; happy path: consecutive reads byte-identical
    apart from `generatedAt`, whole-suite audit/notifications deltas == 0.

- Journey B session mapping (recorded per task 2.2): `completed confirmed`,
  `completed unconfirmed`, `disputed`, `scheduled` created today (createdAt =
  now−6..9min) → today/week/month each +4; `cancelled` backdated 40 days → total
  +5, today/week/month +0 from it; awaitingConfirmation +1 (only the
  completed-unconfirmed fixture).

## Harness compliance (test/workflows/AGENTS.md)

- Committed fixtures in `beforeAll` (cast + governed admins + second teacher in
  ONE `db.transaction`); NO `runInRollback`; per-run UUID prefix; real role rows
  via `createJourneyFixtures`/factories (never monkey-patched); bun:test only;
  `@/` aliases; tracked hard-delete in `afterAll` via `journeyCleanup` plus
  suite-owned ledger deletes for non-user rows.
- IMMUATBLE-TABLE CLEANUP (discovered this task): `student_payments` and
  `teacher_transaction` carry BEFORE DELETE immutability triggers (migration 3).
  The suite deletes its ledger/payment rows under a local `suspendDeletes`
  wrapper with the same discipline as `withAuditDeleteTriggersSuspended`
  (discover via pg_trigger → disable → delete → restore EXACT prior `tgenabled`
  state; 'D' stays disabled). `teacher_transaction.wallet_id` is RESTRICT, so
  ledger rows precede the wallet/users cascade.
- Logger assertion pattern: `probeDenial` snapshots `spy.mock.calls` BEFORE
  `mockRestore()` (restoring clears history — the initial failure mode).

## Verification (all from the worktree root)

| Gate | Command | Result |
| --- | --- | --- |
| Typecheck | `bun tsgo` | exit 0 |
| Duplicates/health | `sub-loop.ts test/workflows/admin/platform-analytics.journey.test.ts --lifecycle duplicates` | exit 0 |
| Suite | `bun run test/scripts/run-test.ts test/workflows/admin/platform-analytics.journey.test.ts` | **10 pass / 0 fail**, 563 expect() calls |
| Idempotence | suite re-run ×3 | stable (cleanup residue-free) |
| Schema purity | `git diff -- backend/db/schema/ backend/db/migration/` | EMPTY (REQ-043) |

## Files touched

- `test/workflows/admin/platform-analytics.journey.test.ts` (NEW only)

## Deviations

- Test-first ordering inverted by the rebuild (documented in-file); journeys are
  GREEN by construction.
- Journey B provisions its second certified teacher at CAST level (not inside the
  journey transaction) so the users-section counters hold exact baselines — the
  plan's "users == baseline + 0" assertion is thereby strictly true.
- None otherwise.
