# Task 2.1 Outcome — Journey A: Cold Platform Honesty (TEST-FIRST, RED)

**Feature:** `dev3-022c-platform-analytics-dashboard`
**Branch:** `feat/dev3-022c-platform-analytics-dashboard` (no commit made — working tree only)
**Date:** 2026-09-04
**Scope:** Journey A inside `test/workflows/admin/platform-analytics.journey.test.ts` (the file all four journeys A–D share). Zero production files touched; the service import is deliberately RED (Task 2.6 ships `PlatformAnalyticsService`).

---

## 1. Intent

Prove the REQ-074 Journey-A EARS: WHEN an admin reads a platform with no journey fixtures THEN every journey-owned metric differs from its pre-suite baseline by exactly zero AND both trend series are still fully populated (sessions: 30 zero-filled buckets; revenue: skeleton-consistent or honestly empty) AND both rating averages are honest `null` for families with zero rows (never a fabricated 0).

## 2. Files

| File | State | Notes |
|---|---|---|
| `test/workflows/admin/platform-analytics.journey.test.ts` | CREATE (complete, 1230 lines, journeys A–D) | This task owns the Journey A `describe` block (lines ~717–792) plus the shared harness it needs: module constants, `toMinorUnits`/`formatMinorUnits`, UTC helpers, `expectJourneyError`, `readAnalytics`, audit/notification oracles, `captureBaseline` (direct-DB mirror of EVERY metric), trend projectors, `tableDigest` byte-identity machinery, `TrackedFixtures` + tracked-id arrays, suite `afterAll` teardown |
| `test/helpers/db-cleanup.ts` | MODIFIED (RULING 1 deliverable, +107 lines, 0 deletions) | `withImmutabilityTriggersSuspended(tables, fn)` — generalized `withAuditDeleteTriggersSuspended` pattern; consumed by the suite `afterAll` (shared by journeys B–D) |

## 3. Fixture / delta mapping (Journey A)

| Fixture (committed in ONE `db.transaction` in `beforeAll`) | Rows |
|---|---|
| Admin cast | `provisionAdminActor(tx, { tracked })` → real `users` row (role `admin`) + real `admin` role-child row; both registered for tracked teardown; `castUserIds` records the user id |

Baseline: `captureBaseline()` runs AFTER the cast commit and BEFORE any read, mirroring every metric predicate by direct DB counts (`session`, `student_payments`, `subscriptions`, `teacher`, `reports`, `evaluations`, `teacher_transaction`, `users`, `audit_logs`, `notifications`) — **the baseline is whatever the shared DB already holds; asserted, never assumed zero**. Every metric assertion is `read == baseline + 0`.

## 4. Assertions (Journey A)

- `sessions`/`subscriptions`/`teachers`/`users` sections `toEqual` the baseline aggregate rows (exact field-for-field equality — a drift in ANY counter fails).
- `health.pendingDisputes` / `health.pendingWithdrawals` / `revenue.offlineActivationsCount` == baseline.
- `revenue.gatewayRevenueByCurrency` `toEqual` baseline rows (empty array iff no paid payments exist — no phantom currency row).
- `expectTrendSkeleton`: `sessionTrendDaily` has exactly 30 buckets, last bucket == midnight-UTC of the read's day; the full projected array `toEqual expectedSessionTrend(baseline, readDayStartMs, 0)` — 30 zero-filled buckets relative to the read's day.
- `revenueTrendDaily` `toEqual expectedRevenueTrend(baseline, readDayStartMs, ∅)` — honestly EMPTY iff no window currency, else exactly `30 × currencies` skeleton with exact string amounts.
- Honest-null ratings: `averageSessionRating === null` iff `sessionRatingsCount === 0`; same for the evaluation family; non-empty families assert the 2-dp-rounded family mean (`toBeCloseTo(_, 2)`).
- Baseline-day guard: `utcDayStart(baselineA.now)` == read day (no midnight straddle).
- Read purity: all 8 observed tables byte-identical (count + `to_jsonb` content digest, PK-ordered), audit rows == whole-suite baseline, notification rows == baseline.

## 5. RED evidence (verbatim)

`bunx tsgo -b --noEmit 2>&1 | grep "error TS"` — exactly ONE error, the sanctioned RED:

```
test/workflows/admin/platform-analytics.journey.test.ts(104,42): error TS2307: Cannot find module '@/backend/services/admin/platform-analytics.service' or its corresponding type declarations.
```

`bun run test/scripts/run-test.ts test/workflows/admin/platform-analytics.journey.test.ts` (tail):

```
test/workflows/admin/platform-analytics.journey.test.ts:

# Unhandled error between tests
-------------------------------
error: Cannot find module '@/backend/services/admin/platform-analytics.service' from '/home/z/my-project/test/workflows/admin/platform-analytics.journey.test.ts'
-------------------------------

 0 pass
 1 fail
 1 error
Ran 1 test across 1 file. [101.00ms]
```

Expected RED — journeys A–D flip GREEN in Task 2.6.

## 6. Semantic review (2.1.SR)

1. ONE `db.transaction` in Journey A's `beforeAll` (commit-or-nothing — a throwing `beforeAll` leaves nothing behind). NO `runInRollback` anywhere in the file (journey-layer rule 1; the service owns its transactions).
2. Tracked ids for hard delete: admin user id in `castUserIds`, both rows registered in `TrackedFixtures`; `afterAll` hard-deletes + re-probes zero residue.
3. Baseline is captured by direct DB counts mirroring each metric's predicate — never by calling the service under test (the baseline must be independent of the implementation).
4. Real role resolution: admin holds real `users.role='admin'` + real `admin` row via the actor-context factory; no permission stubbing, no monkey-patching.
5. Timestamps are relative to the captured wall clock (`new Date()` at capture/fixture time; buckets derived via UTC helpers) — no absolute dates (REQ-026).

## 7. Instruction verification (2.1.IV)

- `test/workflows/AGENTS.md` rules 1–12: no rollback wrapper (1); committed fixtures + `TrackedFixtures` + zero-residue probes (2); `jrn_pan_<8hex>` prefix (3); honest authorization via actor-context factory (4); analytics has NO external channel — nothing to spy, purity probes are the seam proof (5, recorded expressly); try/catch `expectJourneyError` + translated substrings, never `expect(...).rejects.toThrow()` (6); `bun:test` imports, no `console.*`, no `any` (7); `@/` aliases (8); own fixtures only via entity-setup factories (9); file per workflow under `test/workflows/admin/` (10); ordered steps (11); denial + visibility assertions (12).
- `.agents/instructions/tests.instructions.md`: pre-existing data counted BEFORE operations and asserted as `initialCount + delta`; unique emails via `randomUUID()` inside factories; `bun:test`; no `any`; money as decimal strings. The `runInRollback` default is superseded by the documented `test/workflows/` exception (AGENTS.md rule 1 + `docs/testing/workflow-journey-tests.md`).
- Quality loop (2.1.QL): `bun run scripts/health/sub-loop.ts test/workflows/admin/platform-analytics.journey.test.ts --lifecycle duplicates` → **exits 1 at tsgo** with ONLY the sanctioned missing-service TS2307 (expected RED; sub-loop stops at the first gate). Biome direct check on the file: `Checked 1 file … No fixes applied.` (0 diagnostics). Definitive sub-loop pass is deferred to Task 2.6 when the service import resolves.

## 8. Carry-forward

- Task 2.6 flips Journey A GREEN; the service must return `generatedAt`/sections matching the canonical types (`@/backend/types/admin/platform-analytics.types.ts`) byte-for-field so the `toEqual` section assertions hold.
- The baseline mirror pins the exact metric semantics (e.g. `awaitingConfirmation` = completed AND `confirmed_by_student_at IS NULL`; governance-aware `recentlyActive24h` with NULL-safe false) — repo/service implementations that diverge fail here by design.
- Task 2.6.TE Tier-4 owns the repo-spy "denial pre-DB" proof; the journey layer proves the observable purity (byte-identity + zero audit + zero notifications).

*End of Task 2.1 outcome.*
