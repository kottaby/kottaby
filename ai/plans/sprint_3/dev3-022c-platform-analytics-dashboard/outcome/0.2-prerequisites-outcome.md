# Task 0.2 Outcome — Prerequisite & Reuse Verification (verify-then-claim sweep)

**Feature:** `dev3-022c-platform-analytics-dashboard`
**Branch:** `feat/dev3-022c-platform-analytics-dashboard`
**Date:** 2026-08-31
**Mode:** READ-ONLY verification sweep — zero source files modified.

**Result: 21/21 anchor groups verified OK (0 MISSING). 1 expectation REFUTED (drift, non-blocking): `test/ui/components/admin/` already exists — recorded as ⚠️ in deferred-items (DI-0.2-1), affects Task 4.4 wording only (ADD, not CREATE).**

> Line numbers are ACTUAL current positions; plan-cited ranges that drifted are noted per row. Drift is cosmetic (symbols intact) except where flagged.

---

## Verified-anchor table

| # | Anchor | Verified path:line | Status | Notes |
|---|--------|--------------------|--------|-------|
| 1a | `AdminUserRepository.getStats` | `backend/db/repo/admin/admin-user.repository.ts:178-213` | ✅ OK | Plan cited `:450-485` — drifted. Namespace-exported fn; 10 counters in ONE `count(*) filter (...)` aggregate (totalCount, activeCount, suspendedCount, blockedCount, deletedCount, adminsCount, teachersCount, studentsCount, parentsCount, newThisWeekCount); `tx?: DBTransaction` optional-last; 7-day cutoff computed in JS & bound as parameter (`admin-user.repository.ts:179`), never SQL `now() - interval`. |
| 1b | ACTIVE-window subquery semantics | `backend/db/repo/admin/admin-user-query-helpers.ts:134-143` (`studentHasActiveSubscriptionSubquery`) | ✅ OK | Plan cited `admin-user.repository.ts:337-346` — drifted (helpers file split per repo header `:34-38`). Semantics verbatim: `EXISTS(SELECT 1 FROM subscriptions WHERE user_id = users.id AND status = 'active' AND now() >= coalesce(start_date, now()) AND (end_date IS NULL OR now() < end_date))` — the exact ACTIVE-window predicate the analytics user/subscription queries reuse. Used at `admin-user.repository.ts:124,290`. Complementary NULL-safe ACTIVE governance semantics: `admin-user.repository.ts:183-187` (getStats `activeCount` coalesce chain) and `admin-user-query-helpers.ts:77-81` (`buildFilterChain` Active case). |
| 2a | `assertActorAdmin` precedent | defined `backend/services/admin/admin-gate.helpers.ts:59`; consumed `backend/services/admin/user-management.service.ts:118,157,193,231,271,342,395` | ✅ OK | Plan cited `user-management.service.ts:240-271` — the getDetailById→createUser usage block sits intact at `:231-271`. Gate lives in `admin-gate.helpers.ts` (single source of gate truth; `assertActorAdminActive` reuses it verbatim at `:107-108`). |
| 2b | `withTransaction` import | `backend/services/admin/user-management.service.ts:62` | ✅ OK | Plan cited `:67` — drifted. `import { withTransaction } from "@/backend/lib/db/with-transaction";`; also imported `assertActorAdmin` at `:65`. |
| 3a | `$all` admin scope (query tier) | `backend/graphql/query/admin/admin-users.query.ts:74-79` | ✅ OK | Exact match: `authScopes: { $all: { authenticated: true, role: [UserRole.Admin] } }`; pattern repeated at `:106,128,154`; D10 documented in header `:8-13`. |
| 3b | `$all` admin scope (mutation tier) | `backend/graphql/mutation/admin/admin-users.mutation.ts:64-69` | ✅ OK | Exact match, same `$all` conjunction; repeated at `:102,135`. |
| 4a | `DateTime` scalar registration | `backend/graphql/pothos/shared/scalar.pothos.ts:28` | ✅ OK | `export const DateTimeScalar = gqlSchemaBuilder.addScalarType("DateTime", DateTimeResolver, {})` — registered ONCE here; domain files reference by NAME (`type: "DateTime"`). |
| 4b | builder `Scalars` slot | `backend/graphql/pothos/builder.ts:76-81` | ✅ OK | Plan cited `:76-82` — matches. `DateTime: { Input: Date; Output: Date \| string }` on `gqlSchemaBuilder` `SchemaBuilder<{ Context; Defaults: "v3"; Scalars }>`. |
| 5a | Journey harness `test/workflows/AGENTS.md` | `test/workflows/AGENTS.md` (exists) | ✅ OK | Harness rulebook present. |
| 5b | Helpers barrel `@/test/workflows/helpers` | barrel `test/workflows/helpers/index.ts:24-31`; anchor import `backend/services/notifications/realtime/fanout-transport.test.ts:25` | ✅ OK | Anchor line imports `SpiedFanoutTransport` from `@/test/workflows/helpers/spied-transport` (barrel subpath). The BARE barrel `from "@/test/workflows/helpers"` is imported by 20+ suites (e.g. `test/workflows/admin/admin-user-lifecycle.journey.test.ts:60`, `backend/services/notifications/notification-engine.emit.test.ts:70`). |
| 5c | Actor-context factories | `test/workflows/helpers/actor-context.ts:88` `provisionStudentActor`; `:105` `provisionCertifiedTeacherActor`; `:122` `provisionParentActor`; `:136` `provisionAdminActor` | ✅ OK | All four re-exported via barrel (`index.ts:24` `export * from "./actor-context"`). |
| 6 | `withAuditDeleteTriggersSuspended` + `deleteUsersByIds` | `test/helpers/db-cleanup.ts:83` and `:123` | ✅ OK | Both inside the plan-cited `:83-140` window (`deleteUsersByIds` wraps its delete in `withAuditDeleteTriggersSuspended` at `:126`; `countUsersByIds` follows at `:143`). |
| 7a | `withPageAuth` signature | `frontend/lib/auth/withPageAuth.ts:34-47` (options interface) + function at `:67` | ✅ OK | `WithPageAuthOptions { readonly roles?: readonly UserRole[]; readonly redirectTo?: string }` exactly at the plan-cited `:34-47`; `export async function withPageAuth(options?): Promise<WithPageAuthResult>` at `:67`; redirect discipline: anonymous → `/login?redirect=…` (relative path, PNA-safe per `:73-83`), role mismatch → `roleDashboardPath` (imported `:31`). |
| 7b | `roleDashboardPath` | `frontend/lib/auth/roleDashboardRoute.ts:52-65` | ✅ OK | Exact match — role-switch to `ROLE_DASHBOARD_ROUTE[role]`, unknown/null falls back to student dashboard; bare `/dashboard` never a target. |
| 7c | `getLocaleFromCookie` | `shared/locale/server-cookies.ts:6-13` | ✅ OK | Exact match — reads `NEXT_LOCALE` (`:4`), `isAppLocale`-guarded, falls back to `defaultLocale`. |
| 8 | `NOTIFICATION_COUNT_POLL_INTERVAL_MS` | `frontend/components/ui/NotificationUnreadBadge.tsx:18` | ✅ OK | `const NOTIFICATION_COUNT_POLL_INTERVAL_MS = 120_000;` — polling-cadence precedent (120s, per `frontend/AGENTS.md` cited in file header `:14`); consumed at `:62` (`pollInterval`). |
| 9 | `AdminUserStatsReturnType` | `backend/types/admin/admin-user.types.ts:91-102` | ✅ OK | Exact match — 10 readonly counters; this is the type Task 1.1 intersects with `recentlyActive24h`. |
| 10 | `backend/db/test/entity-setup.ts` helper inventory + parameter convention | file is **exactly 174 lines** (`wc -l` = 174) | ✅ OK | **Factories present (6):** `createTestUser:47`, `createTestStudent:77`, `createTestParent:104`, `createTestApplicant:116`, `createTestAdmin:142`, `createTestPlan:153`. **Parameter convention confirmed:** `(tx, …ids, overrides?)` — `tx` FIRST (`entity-setup.ts:10-11`), optional ids between (`createTestStudent(tx, userId, overrides?)`), `overrides` last spread (`{...overrides}` at `:64,91,129`). **The 8 target factories CONFIRMED ABSENT (verified gap Task 1.3 fills):** `createTestSubscription`, `createTestStudentPayment`, `createTestSession`, `createTestSessionReport`, `createTestEvaluation`, `createTestWallet`, `createTestTeacherTransaction`, `createTestTeacherRow` — none exist in the file (full read). Note: `createTestPlan` exists but is NOT one of the 8 — no subscription/payment/session/report/evaluation/wallet/teacher-transaction factory exists. |
| 11 | `recharts` in dependencies | `package.json:183` `"recharts": "^3.10.1"` | ✅ OK | Inside `"dependencies"` block (`package.json:74`, devDependencies start `:197`) — a true runtime dependency. |
| 12a | Admin nav block | `frontend/views/dashboard/nav/navItems.ts:131-145` | ✅ OK (path drift) | Plan cited `frontend/views/dashboard/navItems.ts:126-135` — actual file lives one segment deeper (`…/dashboard/nav/navItems.ts`). `[UserRole.Admin]: [...]` spans `:131-145` and routes the existing admin pages (`/admin/dashboard`, `/admin/users`, `/admin/plans`, `/admin/broadcasts`, `/disputes`); this is the block Task 4.5 extends with the analytics entry. |
| 12b | Governance fields (`users`) | `backend/db/schema/users/users.ts:30-37` | ✅ OK | Exact match — `isDeleted:30, deletedAt:31, suspended:32, suspendedAt:33, suspendedPeriodDays:34, isBlocked:35, blockedAt:36, lastActiveAt:37`. |
| 12c | `session` columns | `backend/db/schema/classes/session.ts:50-81` | ✅ OK (drift) | Plan cited `:32-56` — that range is now header doc-comment; the `pgTable` def spans `:50-81`. Columns verified: `status:60, sessionType:61, intent:62, fee:63, feeHeld:64, heldBalanceLane:65, startedAt:66, endedAt:67, confirmedByStudentAt:68, confirmedByTeacherAt:69, confirmationDeadline:70, cancelReason:71, disputeReason:72, disputedAt:73, resolutionNote:74, resolvedAt:75` — all analytics-relevant columns present. |
| 12d | `student_payments` | `backend/db/schema/billing/student-payments.ts:23-48` | ✅ OK | Exact match — `amount:33` decimal(10,2), `currency:34` char(3) default EGP, `paymentGateway:35`, `status:36`, amount CHECK `>= 0` at `:44`; append-only (trigger) per header `:16-19`. |
| 12e | `subscriptions` | `backend/db/schema/billing/subscriptions.ts:19-42` | ✅ OK | Exact match — `status:29` (subscriptionStatus, default pending), `startDate:30`, `endDate:31`, offline-payment trio `paymentMethod:32/paymentReference:33/paymentVerifiedAt:34`. |
| 12f | `teacher` | `backend/db/schema/teachers/teacher.ts:19-38` | ✅ OK | Exact match — `isApproved:25, isEvaluator:26, averageRating:27` decimal(3,2), `isOnline:28`; averageRating 0–5 CHECK at `:37`. |
| 12g | `reports` rating CHECK | `backend/db/schema/classes/reports.ts:29` + `:36-41` | ✅ OK | Exact match — `studentRatingByTeacher:29`; `reports_student_rating_by_teacher_check` 0–5 at `:36-41` (Task 1.3 factory must honor it). |
| 12h | `evaluations` | `backend/db/schema/teachers/evaluations.ts:32,34,43` | ✅ OK | Exact match — `score:32` (0–100 CHECK at `:43`), `isDeleted:34`. |
| 12i | `teacher_transaction` | `backend/db/schema/billing/teacher-transaction.ts:26-49` | ✅ OK | Exact match — `walletId:30, sessionId:33, amount:35` decimal(10,2), `type:36`, `status:37`; amount CHECK `>= 0` at `:45`; append-only trigger per header. |
| 12j | pg enum mirrors | `backend/db/schema/enums.ts:9-114` | ✅ OK | Exact match — all 16 pgEnums (`userRole:9 … teacherRequestPreference:114`), incl. `sessionStatus:23, paymentStatus:29, transactionType:31, transactionStatus:33, paymentGateway:35-44, subscriptionStatus:46-52`; TypeScript mirrors live under `backend/enum/` (header `:4-6`). |
| 13a | `frontend/views/admin/analytics/**` ABSENT | — | ✅ OK (absent confirmed) | No `analytics` dir anywhere under `frontend/views/admin/` (which holds only `audit, broadcasts, disputes, index.ts, plans, users`). Genuinely net-new (plan D12 / Task 4.4). |
| 13b | `app/(dashboard)/admin/analytics/` ABSENT | — | ✅ OK (absent confirmed) | `app/(dashboard)/admin/` holds only `broadcasts, dashboard, plans, users` — analytics route dir net-new (Task 4.2 per tasks.md `:307`). |
| 13c | `test/ui/components/admin/**` ABSENT | **REFUTED** — `test/ui/components/admin/` EXISTS | ⚠️ DRIFT | Directory already contains 4 files: `AdminDisputesContainer.suite.tsx`, `AdminDisputesContainer.test.tsx`, `AuditTrailView.test.tsx`, `BroadcastComposeContainer.test.tsx` (DEV3-005 / DEV3-016 / DEV3-022d lineage). Plan D12's "test/ui/components/admin/ is net-new" is factually stale. **Non-blocking:** the analytics test file `PlatformAnalyticsContainer.test.tsx` itself does not exist; Task 4.4.TE ADDS it into the existing directory (with `test/ui/AGENTS.md` present and already governing it). Logged as ⚠️ deferred-item DI-0.2-1 → Task 4.4. |
| 13d | `frontend/views/admin/` EXISTS | present (`audit, broadcasts, disputes, index.ts, plans, users`) | ✅ OK | — |
| 13e | `app/(dashboard)/admin/` EXISTS | present (`broadcasts, dashboard, plans, users`) | ✅ OK | — |
| 13f | `test/workflows/admin/` EXISTS | present — 4 DEV3-016 journey suites: `admin-user-lifecycle.journey.test.ts`, `admin-user-denials.journey.test.ts`, `cold-start-certification.journey.test.ts`, `audit-trail.journey.test.ts` | ✅ OK | `platform-analytics.journey.test.ts` (Task 2.x) joins this directory. |

---

## Instruction-file inventory (citation discipline — Task 0.2 bullet 3)

**`.agents/instructions/` — exactly 3, all present:**

| File | Status |
|---|---|
| `.agents/instructions/frontend.instructions.md` | ✅ PRESENT |
| `.agents/instructions/backend.instructions.md` | ✅ PRESENT |
| `.agents/instructions/tests.instructions.md` | ✅ PRESENT |
| (`.agents/instructions/design.instructions.md`) | ABSENT (not part of the claimed trio) |

**Layer AGENTS.md files — ALL 8 claimed candidates confirmed PRESENT:**

| File | Status |
|---|---|
| `backend/AGENTS.md` | ✅ PRESENT |
| `frontend/AGENTS.md` | ✅ PRESENT |
| `frontend/graphql/AGENTS.md` | ✅ PRESENT |
| `shared/AGENTS.md` | ✅ PRESENT |
| `backend/services/AGENTS.md` | ✅ PRESENT |
| `test/workflows/AGENTS.md` | ✅ PRESENT |
| `test/ui/AGENTS.md` | ✅ PRESENT |
| `app/AGENTS.md` | ✅ PRESENT |

**Absence checks requested by the sweep:**

| File | Status |
|---|---|
| `shared/locale/AGENTS.md` | **PRESENT** (the sweep flagged it "note absence if indeed absent" — it is NOT absent; it exists and governs `shared/locale/server-cookies.ts`) |
| `frontend/views/AGENTS.md` | ✅ CONFIRMED ABSENT |
| `frontend/components/ui/AGENTS.md` | ✅ CONFIRMED ABSENT |

Citation consequence: tasks may cite all 8 layer AGENTS.md files listed above; only `frontend/views/` and `frontend/components/ui/` fall back to the `.agents/instructions/frontend.instructions.md` layer file.

---

## Governing docs (Task 0.2 cross-check) — ALL 8 PRESENT

| Doc | Lines | Status |
|---|---|---|
| `docs/admin/user-management.md` | 214 | ✅ |
| `docs/graphql/error-handling-contract.md` | 181 | ✅ |
| `docs/drizzle/prepared-statements.md` | 309 | ✅ |
| `docs/graphql/dataloader-batching.md` | 299 | ✅ |
| `docs/specs/open-decisions-and-gaps.md` | 302 | ✅ |
| `docs/specs/state-machine-invariants.md` | 298 | ✅ |
| `docs/workflows/05-admin-governance-override.md` | 245 | ✅ |
| `docs/testing/workflow-journey-tests.md` | 124 | ✅ |

---

## Findings summary

1. **0 MISSING anchors** — no ❌ rows required; no downstream task (1.1, 1.3, 1.4, 2.1-2.6, 3.1-3.3, 4.1-4.5) is STOPPED by a missing prerequisite.
2. **1 refuted expectation (⚠️ DI-0.2-1, logged in `deferred-items.md`):** `test/ui/components/admin/` already exists with 4 component suites. Plan D12 / Task 4.4.TE must treat the component test as an **ADD** into the existing directory, not a directory CREATE. `frontend/views/admin/analytics/` and `app/(dashboard)/admin/analytics/` remain genuinely net-new.
3. **Line-number drift (cosmetic, symbols verified):** plan-cited ranges that moved: `getStats` 450-485 → **178-213**; ACTIVE-window subquery 337-346 → **admin-user-query-helpers.ts:134-143**; `withTransaction` import 67 → **62**; `assertActorAdmin` usage 240-271 → **231-271** (defn in `admin-gate.helpers.ts:59`); navItems path → **`frontend/views/dashboard/nav/navItems.ts`** (131-145); session columns 32-56 → **50-81**. All other cited ranges matched exactly.
4. **`entity-setup.ts` gap CONFIRMED exactly as planned:** 174 lines, 6 existing factories, `(tx, …ids, overrides?)` convention, and none of the 8 factories Task 1.3 appends.
5. **`recharts@^3.10.1`** is a runtime `dependencies` entry (`package.json:183`) — no install needed for the chart tier.

**Downstream impact verdict:** Phase 1 (1.1, 1.3, 1.4), Phase 2 (2.1-2.6), Phase 3 (3.1-3.3), Phase 4 (4.1-4.5) — all GO. Only Task 4.4 carries the DI-0.2-1 wording correction (ADD vs CREATE).
