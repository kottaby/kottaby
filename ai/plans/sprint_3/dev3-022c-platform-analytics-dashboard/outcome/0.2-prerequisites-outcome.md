# Task 0.2.OUT — Prerequisite & Reuse Verification (verify-then-claim sweep)

**Plan:** DEV3-022c Platform Analytics Dashboard
**Branch:** `feat/dev3-022c-platform-analytics-dashboard` (base `a259524`)
**Verdict: ALL anchors VERIFIED PRESENT — 0 missing. 6 citations line/path-drifted; actual locations recorded below (§2). No ❌ entries; no downstream task stopped.**

Method: every cited anchor opened via Read/Grep at its cited coordinates; drift recorded against the actual file content. No foreign-layer files were modified.

---

## 1. Verified-Anchor Table (exact-match anchors)

| # | Anchor (tasks.md citation) | Cited path:line | Verdict | Evidence (actual location) |
|---|---|---|---|---|
| 1 | `$all` admin scope pattern (query tier) | `backend/graphql/query/admin/admin-users.query.ts:74-79` | ✅ EXACT | Lines 74–79: `authScopes: { $all: { authenticated: true, role: [UserRole.Admin] } }` on the `adminUsers` field |
| 2 | `$all` admin scope pattern (mutation tier) | `backend/graphql/mutation/admin/admin-users.mutation.ts:64-69` | ✅ EXACT | Lines 64–69: identical `$all` block on `adminCreateUser` |
| 3 | `DateTime` scalar registration | `backend/graphql/pothos/shared/scalar.pothos.ts:28` | ✅ EXACT | Line 28: `export const DateTimeScalar = gqlSchemaBuilder.addScalarType("DateTime", DateTimeResolver, {});` |
| 4 | Builder `Scalars` slot | `backend/graphql/pothos/builder.ts:76-82` | ✅ EXACT | Lines 76–82: `Scalars: { DateTime: { Input: Date; Output: Date \| string } }` inside the `SchemaBuilder<…>` generic |
| 5 | Journey harness `test/workflows/AGENTS.md` | `test/workflows/AGENTS.md` | ✅ EXACT | File present |
| 6 | Helpers barrel `@/test/workflows/helpers` | existence anchor `backend/services/notifications/realtime/fanout-transport.test.ts:25` | ✅ EXACT (see note) | Line 25: `import { SpiedFanoutTransport } from "@/test/workflows/helpers/spied-transport";` — a `@/test/workflows/helpers/*` path import as cited; the barrel itself is `test/workflows/helpers/index.ts` (pure `export *` of actor-context, journey-actor-fixtures, journey-cleanup, journey-fixtures, spied-transport, tracked-fixtures) |
| 7 | `provisionAdminActor` | actor-context factories | ✅ PRESENT | `test/workflows/helpers/actor-context.ts:136` |
| 8 | `provisionStudentActor` | actor-context factories | ✅ PRESENT | `test/workflows/helpers/actor-context.ts:88` |
| 9 | `provisionCertifiedTeacherActor` | actor-context factories | ✅ PRESENT | `test/workflows/helpers/actor-context.ts:105` |
| 10 | `provisionParentActor` | actor-context factories | ✅ PRESENT | `test/workflows/helpers/actor-context.ts:122` |
| 11 | `withAuditDeleteTriggersSuspended` + `deleteUsersByIds` | `test/helpers/db-cleanup.ts:83-140` | ✅ EXACT | `withAuditDeleteTriggersSuspended` :83–109 (pg_trigger discovery → disable → restore-in-finally); `deleteUsersByIds` :123–140 |
| 12 | `roleDashboardPath` | `frontend/lib/auth/roleDashboardRoute.ts:52-65` | ✅ EXACT | Lines 52–65: `export function roleDashboardPath(role: string \| UserRole \| null \| undefined): string` |
| 13 | `getLocaleFromCookie` | `shared/locale/server-cookies.ts:6-13` | ✅ EXACT | Lines 6–13: `export async function getLocaleFromCookie(): Promise<AppLocale>` (NEXT_LOCALE, falls back to `defaultLocale`) |
| 14 | `NOTIFICATION_COUNT_POLL_INTERVAL_MS` (polling cadence precedent) | `frontend/components/ui/NotificationUnreadBadge.tsx:18` | ✅ EXACT | Line 18: `const NOTIFICATION_COUNT_POLL_INTERVAL_MS = 120_000;` |
| 15 | `AdminUserStatsReturnType` | `backend/types/admin/admin-user.types.ts:91-102` | ✅ EXACT | Lines 91–102: interface with the ten readonly counters (`totalCount`, `activeCount`, `suspendedCount`, `blockedCount`, `deletedCount`, `adminsCount`, `teachersCount`, `studentsCount`, `parentsCount`, `newThisWeekCount`) |
| 16 | `recharts` in dependencies | `package.json` | ✅ EXACT | `package.json:181` — `"recharts": "^3.10.1"` inside `dependencies` (block spans :72–194; devDependencies start :195) |
| 17 | Admin nav block | `frontend/views/dashboard/navItems.ts:126-135` | ⚠️ DRIFT (path) | Lines 126–135 are EXACTLY the `[UserRole.Admin]` block (`/audit` entry :133, `/profile` :134) — but at the ACTUAL path **`frontend/views/dashboard/nav/navItems.ts`** (the cited path lacks the `nav/` segment and does not exist). Task 1.4 anchors on the actual path. |
| 18 | Governance fields | `backend/db/schema/users/users.ts:30-37` | ✅ EXACT | `isDeleted`:30, `deletedAt`:31, `suspended`:32, `suspendedAt`:33, `suspendedPeriodDays`:34, `isBlocked`:35, `blockedAt`:36, `lastActiveAt`:37 |
| 19 | `session` columns | `backend/db/schema/classes/session.ts:32-56` | ✅ EXACT | `pgTable("session", …)` :32 → :56 (status/sessionType/intent/fee/feeHeld/startedAt/endedAt/confirmed*/confirmationDeadline/createdAt/updatedAt) |
| 20 | `student_payments` | `backend/db/schema/billing/student-payments.ts:23-48` | ✅ EXACT | `pgTable("student_payments", …)` :23 → :48 (amount decimal(10,2), currency char(3) default EGP, paymentGateway, status; amount ≥ 0 check :44) |
| 21 | `subscriptions` | `backend/db/schema/billing/subscriptions.ts:19-42` | ✅ EXACT | `pgTable("subscriptions", …)` :19 → :42 (status default pending, startDate/endDate, paymentMethod/paymentReference) |
| 22 | `teacher` | `backend/db/schema/teachers/teacher.ts:19-38` | ✅ EXACT | `pgTable("teacher", …)` :19 → :38 (`isApproved`:25, `isEvaluator`:26, `averageRating`:27, `isOnline`:28; 0–5 average-rating check :37) |
| 23 | `reports` rating check | `backend/db/schema/classes/reports.ts:29,36-41` | ✅ EXACT | `studentRatingByTeacher` :29; `reports_student_rating_by_teacher_check` (0–5) :37–40 within the check array :36–41 |
| 24 | `evaluations` | `backend/db/schema/teachers/evaluations.ts:32,34,43` | ✅ EXACT | `score` :32; `isDeleted` :34; `evaluations_score_check` (0–100) :43 |
| 25 | `teacher_transaction` | `backend/db/schema/billing/teacher-transaction.ts:26-49` | ✅ EXACT | `pgTable("teacher_transaction", …)` :26 → :49 (walletId, sessionId nullable, amount decimal(10,2), `transactionType`, `transactionStatus`) |
| 26 | pg enum mirrors | `backend/db/schema/enums.ts:9-114` | ✅ EXACT | First mirror `userRole` :9; last `teacherRequestPreference` ends :114 (includes `sessionStatus`:23, `paymentStatus`:29, `transactionType`:31, `transactionStatus`:33, `paymentGateway`:35, `subscriptionStatus`:46 — the value sets the analytics counters group by) |

## 2. Drifted Citations — Actual Locations Recorded (all targets EXIST; no ❌)

| Anchor (tasks.md citation) | Cited | Actual (verified) | Nature of drift |
|---|---|---|---|
| `AdminUserRepository.getStats` | `backend/db/repo/admin/admin-user.repository.ts:450-485` | **`backend/db/repo/admin/admin-user.repository.ts:178-213`** | Line drift (file is 399 lines total). `getStats(tx?)` returns `AdminUserStatsRow` — ten `count(*) filter (…)` counters over `users`, engine-portable JS-bound cutoff for `newThisWeekCount`. |
| ACTIVE-window subquery semantics | `backend/db/repo/admin/admin-user.repository.ts:337-346` | **`backend/db/repo/admin/admin-user-query-helpers.ts:126-141`** (`studentHasActiveSubscriptionSubquery()`), consumed at `admin-user.repository.ts:45,124,290` | File+line drift. The semantics: `status = 'active' AND now() >= coalesce(start_date, now()) AND (end_date IS NULL OR now() < end_date)` — the ACTIVE-window precedent the plan's reuse rule (protocol rule 9) points at. |
| `assertActorAdmin` precedent | `backend/services/admin/user-management.service.ts:240-271` | **Definition:** `backend/services/admin/admin-gate.helpers.ts:59-90` (anonymous → `UnauthorizedError`:68; missing actor → `ForbiddenError`:78; non-admin → `ForbiddenError`:88). **Import:** `user-management.service.ts:65`. **Call precedent:** `user-management.service.ts:271` (tail of the cited range) | Partial drift. Semantic precedent fully present; Phase 2/3 will import from `admin-gate.helpers`. |
| `withTransaction` import | `backend/services/admin/user-management.service.ts:67` | **`backend/services/admin/user-management.service.ts:62`** (`import { withTransaction } from "@/backend/lib/db/with-transaction";`) | Line drift (minor; :67 is inside the adjacent `user-management.helpers` import block). |
| `withPageAuth` signature | `frontend/lib/auth/withPageAuth.ts:34-47` | **Interface `WithPageAuthOptions`:34-47** (exact); the async fn itself: **`frontend/lib/auth/withPageAuth.ts:67`** (`export async function withPageAuth(options?: WithPageAuthOptions): Promise<WithPageAuthResult>`) | Partial drift — cited range holds the options type; the function signature is at :67. |
| Admin nav block path | `frontend/views/dashboard/navItems.ts:126-135` | **`frontend/views/dashboard/nav/navItems.ts:126-135`** | Path drift (missing `nav/` segment); lines exact (see table row 17). |

## 3. Confirmed Absences (net-new CREATE space)

| Claim | Verdict | Evidence |
|---|---|---|
| `frontend/views/admin/analytics/**` ABSENT | ✅ CONFIRMED | `ls frontend/views/admin/analytics` → "No such file or directory" (`frontend/views/admin/` holds only `index.ts`, `plans/`, `users/`) |
| `test/ui/components/admin/**` ABSENT | ✅ CONFIRMED | `ls test/ui/components/admin` → "No such file or directory" |
| 8 fixture factories absent from `backend/db/test/entity-setup.ts` | ✅ CONFIRMED | Full-file read: existing factories are exactly `createTestUser`, `createTestStudent`, `createTestParent`, `createTestApplicant`, `createTestAdmin`, `createTestPlan`. None of `createTestSubscription` / `createTestStudentPayment` / `createTestSession` / `createTestSessionReport` / `createTestEvaluation` / `createTestWallet` / `createTestTeacherTransaction` / `createTestTeacherRow` exists. File is **174 lines** (`wc -l`) — matches the task's stated size exactly. Parameter convention verified: `(tx, …ids, overrides?)` with `tx` FIRST and overrides last (`Partial<…SelectType> = {}`). |

## 4. Confirmed Existences (container layers)

| Claim | Verdict | Evidence |
|---|---|---|
| `frontend/views/admin/` EXISTS | ✅ | `index.ts`, `plans/`, `users/` |
| `app/(dashboard)/admin/` EXISTS | ✅ | `dashboard/`, `plans/`, `users/` |
| `test/workflows/admin/` EXISTS (holds DEV3-016 journey suites) | ✅ | `admin-user-denials.journey.test.ts`, `admin-user-lifecycle.journey.test.ts` |

## 5. Instructive-File Inventory (citation discipline)

Confirmed PRESENT (only these are cited by this ticket):
- `.agents/instructions/backend.instructions.md`, `.agents/instructions/frontend.instructions.md`, `.agents/instructions/tests.instructions.md` — all three present in `.agents/instructions/`.
- Layer AGENTS.md (8/8 present): `backend/AGENTS.md`, `frontend/AGENTS.md`, `frontend/graphql/AGENTS.md`, `shared/AGENTS.md`, `backend/services/AGENTS.md`, `test/workflows/AGENTS.md`, `test/ui/AGENTS.md`, `app/AGENTS.md`.

Per tasks.md 0.2 the ONLY instruction files are the three `.agents/instructions/*` files plus the layer AGENTS.md files confirmed above — citation discipline for later tasks uses exactly this set.

## 6. Health Verification (protocol rule 2)

`bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates` on this outcome file and on `0-baseline-outcome.md`: `tsgo` stage PASS; `oxlint` stage reports "No files found to lint" (exit 1) — the KNOWN pre-existing ".md sub-loop artifact" for non-code files (referenced at DEV3-020 `outcome/6.1-review-types-outcome.md:7`; harness-owned, not patched). Duplicates stage covered by repo-standard `jscpd` run → 0 clones / 0.00% (608 files, config `.jscpd.json`).

## 7. Outcome of the Sweep

- **All reuse claims in plan/tasks are grounded**: nothing required by Phase 1/2 is missing from the bundled tree; every semantic anchor exists.
- **6 citations drifted** (§2) — actual locations recorded above and to be used by later tasks. Per protocol: foreign layers NOT patched; no ❌ logged (nothing is absent).
- **Downstream impact:** none stopped. Task 1.3 must anchor fixture factories on `backend/db/test/entity-setup.ts` (174 lines, 6 existing factories, `(tx, …ids, overrides?)` convention). Task 1.4's nav edit anchors on `frontend/views/dashboard/nav/navItems.ts:126-135`. Phase 2 repo work reuses `getStats` (`admin-user.repository.ts:178-213`) + `studentHasActiveSubscriptionSubquery` (`admin-user-query-helpers.ts:126-141`); service work reuses `assertActorAdmin` (`admin-gate.helpers.ts:59`) and `withTransaction` (`user-management.service.ts:62`).
- **Requirements coverage:** REQ-002 (reuse-not-rebuild) — verified, all precedents present and will be reused, never reimplemented.

## 8. Deferred Items Encountered

None new. Ledger unchanged (only the four pre-registered `📅 Forward` rows D-1..D-4; zero ❌/⚠️).
