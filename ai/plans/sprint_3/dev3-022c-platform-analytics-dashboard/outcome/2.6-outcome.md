# Task 2.6 Outcome — PlatformAnalyticsService (service + service test + RED→GREEN journey flip)

## Summary

Implemented `PlatformAnalyticsService.getPlatformAnalytics` exactly per plan §4.2 as amended by RULING 2: the shared governance gate `assertActorAdminActive` is the FIRST statement (pre-tx, pre-read; deep import per the cold-start precedent), then ONE `withTransaction` block captures a single `now` and composes ELEVEN repository reads in ONE `Promise.all` over the SAME transaction handle, and the pure module-scope helpers `buildDailySkeleton` / `mergeSessionTrend` / `expandRevenueTrend` assemble the 30-bucket UTC-midnight trend series (sessions zero-filled always; revenue expanded day-major + currency-ascending over the window's currency set with exact `"0"` fills and an honestly EMPTY series when no currency exists). The happy path is structurally silent and write-free. The 4-tier service suite passed on its first run, and the committed journey file flipped RED→GREEN with ZERO journey-assertion changes — all four journeys (A–D) pass, completing the deferred Tasks 2.1–2.4 verification.

## Files created / modified

| File | Action | Notes |
|---|---|---|
| `backend/services/admin/platform-analytics.service.ts` | CREATE | 182 physical lines — namespace `PlatformAnalyticsService`, single public method; 3 pure module-scope helpers + 2 numeric consts are the only top-level state |
| `backend/services/admin/platform-analytics.service.test.ts` | CREATE | 4-tier suite, 16 tests / 434 expect() calls |
| `backend/services/admin/index.ts` | MODIFY | one `export * from "./platform-analytics.service";` line added (existing alphabetical barrel style) |
| `test/workflows/admin/platform-analytics.journey.test.ts` | MODIFY | MINIMAL lint-hygiene only (see Deviations §2) — zero assertion changes |

Files NOT modified: `tasks.md`, `backend/db/repo/**` (incl. the protected `admin-user.repository.ts` — git-diff guard = 0 lines), all schema/migration files (diff = empty), `backend/db/repo/admin/index.ts` (repo barrel left as Task 2.5 shipped it; the service deep-imports `@/backend/db/repo/admin/platform-analytics.repository` — the same module path the test spies on).

## Service pipeline (as built)

1. `await assertActorAdminActive(actorId, locale, outerTx);` — FIRST statement. The service adds NO `actorId ≤ 0` guard, NO absent-row branch, NO translation resolution, NO logging of its own (RULING 2.1–2.3).
2. `return withTransaction(outerTx, async tx => { const now = new Date(); … })` — one `Promise.all` over the same `tx`: `AdminUserRepository.getStats(tx)` (REUSE, verbatim spread) + `countRecentlyActiveUsers(now, tx)`, `getSessionStats(now, tx)`, `getSessionDailyTrend(now, tx)`, `getRevenueStats(now, tx)`, `getRevenueDailyTrend(now, tx)`, `getSubscriptionStats(now, tx)`, `countOfflineActivations(tx)`, `getTeacherPresenceStats(tx)`, `getRatingStats(tx)`, `getHealthIndicators(tx)`. Assemble `{ generatedAt: now, users: { ...getStatsResult, recentlyActive24h }, sessions, revenue: { gatewayRevenueByCurrency, offlineActivationsCount }, subscriptions, teachers, ratings, health, sessionTrendDaily, revenueTrendDaily }`.
3. Trend assembly: `buildDailySkeleton(now)` = 30 consecutive UTC midnights ending at `now`'s day; sparse session rows merge by `bucketStart.getTime()` with `0` fills (rows outside the skeleton are ignored, never fabricated); revenue derives the currency set ONLY from the window's sparse trend rows (an all-time-only currency never fabricates points), expands day-major then currency-ascending (`localeCompare`), passes exact decimal strings through, `"0"`-fills absent pairs, returns `[]` when the set is empty.

## Verification results

| Command | Result |
|---|---|
| `bun run test/scripts/run-test.ts backend/services/admin/platform-analytics.service.test.ts` | **16 pass / 0 fail** (434 expect calls) — first run green; re-confirmed green after lint fixes |
| `bun run test/scripts/run-test.ts test/workflows/admin/platform-analytics.journey.test.ts` | **8 pass / 0 fail** (189 expect calls) — Journey A (cold honesty), B (full-cast deltas), C (freshness anti-cache), D (denial & purity matrix) ALL GREEN — the 2.1–2.4 RED→GREEN flip, zero journey-assertion edits |
| `bun run scripts/health/sub-loop.ts backend/services/admin/platform-analytics.service.ts --lifecycle duplicates` | **exit 0** (after 2 lint iterations: `Array#sort` → `toSorted`, then `localeCompare` comparator per sonarjs) |
| `bun run scripts/health/sub-loop.ts backend/services/admin/platform-analytics.service.test.ts --lifecycle duplicates` | **exit 0** (after 1 lint iteration: `no-await-in-loop` → `Promise.all` fan-out on the two denial sweeps) |
| `bun run scripts/health/sub-loop.ts test/workflows/admin/platform-analytics.journey.test.ts --lifecycle duplicates` | **exit 0** (completes the deferred 2.1–2.4 QL; 4 minimal fixes, see Deviations §2) |
| `bunx tsgo -b --noEmit 2>&1 \| grep -c "error TS"` | **0** — the sanctioned RED import error from Task 2.5's baseline is resolved (journey import now resolves) |
| `git status --porcelain` / `git diff --name-only` | exactly the 4 files above (+ pre-existing `tmp-scratch/`); `git diff -- backend/db/schema/ backend/db/migration/ backend/db/repo/admin/admin-user.repository.ts` = EMPTY |

Service test coverage map (4 tiers): Tier 1 actor matrix through the REAL gate — `actorId=0` → `UnauthorizedError` pre-DB (findById spy ZERO calls); absent id → `ForbiddenError`; student/teacher/parent → `ForbiddenError`; deleted/blocked/suspended admins → matching translated messages; multi-flagged admin → `accountDeleted` (deterministic ladder); live admin → full snapshot shape through BOTH transaction paths (caller `tx` savepoint AND the production top-level `db.transaction` with a committed, afterAll-cleaned admin). Tier 2 — the six windowed repo spies are pinned on the IDENTICAL `now` reference, all eleven reads share ONE tx reference, `generatedAt` is reference-equal to the captured instant, and the users section equals the ten directory counters verbatim plus `recentlyActive24h`. Tier 3 — sparse-full / sparse-empty / multi-currency skeletons (60-point day-major expansion, exact `"0"` fills, all-time-only currency excluded) + 8-table content-digest purity pre/post composite read. Tier 4 — all eleven aggregate-repo spies receive ZERO calls on every denial path; `logDomainError` ZERO on success and exactly ONE per denial (helper-owned).

## Semantic review — item-by-item

1. **No shared mutable module state** — top-level scope = `TREND_BUCKET_COUNT`, `DAY_MS` (immutable numeric consts) + three pure functions; no `let`/`var`, no caches, no registries (grep-verified).
2. **One transaction; every repo call receives the same tx** — a single `withTransaction(outerTx, …)` block; the Tier-2 spy harness proves all eleven reads captured the identical tx reference.
3. **No try/catch swallowing DomainErrors** — zero `try`/`catch` in the service file (grep-verified); DomainErrors propagate from the gate untouched.
4. **Enums as value imports** — the service imports NO enums at all: every enum predicate lives in the repository layer (correct tier); no string literals stand in for enum values anywhere.
5. **No dead branches** — every statement is exercised: both `withTransaction` branches (savepoint + top-level), both empty/non-empty currency arms, the `"0"`-fill path, the out-of-skeleton-row drop path (16-test suite + 8-test journey).
6. **Clean comments — ZERO plan-artifact refs** — grep `REQ-|Task [0-9]|tasks\.md|specs\.md|plan|RULING|Phase|§` over service + test = NO MATCHES; comments state domain contracts only (gate ownership, single-instant discipline, honesty of the EMPTY revenue series).
7. **Log context bounded** — the service logs NOTHING (no logger import); the only domain logs on the surface are the gate's own bounded `{ code, entity: "user", entityId[, locale] }` lines. Metric payloads and SQL text never enter any log.
8. **Zero writes on all paths** — grep for `.insert(` / `.update(` / `.delete(` / audit / notification calls = NO MATCHES; structurally write-free, proven by the digest-purity tests and the journeys' byte-identity + zero-audit/zero-notification oracles.
9. **Scope boundary** — service + service test + services/admin barrel + minimal journey lint fixes; nothing else touched (git-verified). `tasks.md` untouched.
10. **RULING-2 observable deltas recorded (A5 obligation)** — absent-actor (and any other unresolvable/negative/non-integer id) → `ForbiddenError`, NOT `UnauthorizedError`; `actorId = 0` → `UnauthorizedError` pre-DB; two pre-tx actor reads performed by the helper; gate log lines carry `entity: "user"` and governance lines include `locale`; the service resolves NO error-copy keys (the helper owns `getServerTranslations`) — the plan §4.2 step-1/service-translation clause is superseded.

## Deviations

1. **RULING-2 amendments (binding; supersede plan §4.2 step 1 + the task card's translation clause)** — gate consumed via deep import as the first statement; NO bespoke role/governance ladder, NO `actorId ≤ 0` pre-guard, NO `getServerTranslations` import in the service (it would be dead — the helper owns denial copy; the happy path throws nothing). Observable deltas listed in semantic item 10.
2. **Journey file lint fixes (completing the deferred 2.1–2.4 QL; zero behavior change, all four journeys re-verified green)** — (a) two `[...new Set(...)].sort()` → `.toSorted((l, r) => l.localeCompare(r))` (unicorn/no-array-sort + sonarjs/no-alphabetical-sort; identical ordering for currency codes); (b) `type TableDigests = Record<ObservedTable, TableDigest>` → `{ [tableName: string]: TableDigest }` and `Object.fromEntries(entries) as TableDigests` → annotated assignment (typescript-eslint/no-unsafe-type-assertion). No assertion, fixture, or expectation was altered.
3. **Repo barrel NOT updated** — `backend/db/repo/admin/index.ts` does not export the analytics repository (as Task 2.5 shipped it); the service deep-imports the repository module. Keeping the edit set inside this task's named scope; the barrel line can land with Task 3.x if the GraphQL layer wants the short path.
4. **Service file needed no max-lines split** — 182 physical lines (well under the 300 counted cap); helpers stayed in the service file per the plan.

## Carry-forward for Phase 3 (GraphQL layer)

- **Exact service signature** — `PlatformAnalyticsService.getPlatformAnalytics(actorId: number, locale: string, outerTx?: DBTransaction): Promise<PlatformAnalyticsReturnType>`; namespace-exported from `@/backend/services/admin` (barrel) and `@/backend/services/admin/platform-analytics.service` (deep). Resolvers propagate `ctx.locale` and `ctx.user.id`; the service re-gates (defense-in-depth), so resolver-tier anonymous conversion to `UNAUTHORIZED` remains pre-resolver per plan §3.2.
- **Return shape** — root `{ generatedAt: Date, users (11 counters = the ten `AdminUserStatsReturnType` + `recentlyActive24h`), sessions (10), revenue { gatewayRevenueByCurrency: {currency, totalAmount, last30DaysAmount, paidPaymentsCount}[], offlineActivationsCount }, subscriptions (7), teachers (3), ratings (nullable averages + counts), health (2), sessionTrendDaily: {bucketStart: Date, sessionCount}[], revenueTrendDaily: {bucketStart: Date, currency, amount}[] }`. `generatedAt`/`bucketStart` are true `Date` instants (DateTime scalar); money is exact decimal `string` end-to-end; `revenueTrendDaily` is EMPTY when no currency exists in the window.
- **Denial surface** — `actorId=0` → `UnauthorizedError("UNAUTHORIZED")`; absent/non-admin → `ForbiddenError("FORBIDDEN")`; governed → `ForbiddenError` with `accountDeleted`/`accountBlocked`/`accountSuspended` (deterministic deleted → blocked → suspended). Zero new error codes.
- **Misc** — the repo barrel currently lacks the analytics export (deep import path `@/backend/db/repo/admin/platform-analytics.repository` works today); `AdminUserRepository.getStats` computes its own 7-day cutoff internally (not bound to the snapshot `now` — accepted reuse posture, journeys green).

*End of Task 2.6 outcome.*
