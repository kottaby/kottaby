# 7.1 Outcome — Canonical doc `docs/admin/platform-analytics.md`

**Branch:** feat/dev3-022c-platform-analytics-dashboard @ f9b0759 (verified before start; no HEAD flip).
**Scope held:** exactly ONE new file created (`docs/admin/platform-analytics.md`, 166 lines) + this outcome. Zero source edits, zero commits, zero tasks.md changes (`git status` shows only the untracked doc).

## What was written

Canonical operator/maintainer doc for the admin whole-platform analytics dashboard, all 12 required sections: (1) overview — read-only single-snapshot surface, route `app/(dashboard)/admin/analytics/page.tsx` + `withPageAuth` Admin guard, zero-arg `adminPlatformAnalytics` with `$all{authenticated, role:[Admin]}`; (2) metric definitions tables — every exposed field → exact SQL semantics + feeding enum members, each row anchored to `repo.<function>` (getStats reuse, countRecentlyActiveUsers, getSessionStats, getRevenueStats, countOfflineActivations, getSubscriptionStats, getTeacherPresenceStats, getRatingStats, getHealthIndicators, getSessionDailyTrend, getRevenueDailyTrend); (3) single-captured-`now` + one-transaction `Promise.all` of 11 reads (REQ-011/040); (4) UTC boundary rulings — day/ISO-week-Monday/month/24h/30-day zero-filled skeleton + `UTC_TIMESTAMP_DECODER`; (5) money-as-string, GROUP-BY-currency containment, honest-empty array, VERBATIM REQ-015 offline-exclusion note; (6) honest nulls; (7) read purity + Journey D byte-identity pointer; (8) governed-admin service-tier divergence with REQ-032 rationale; (9) REQ-041 statement-level ruling, REPEATABLE READ rejected; (10) "what NOT to do" (10 items incl. no caching/D-1, no new error codes, no drill-downs/D-2, no cross-currency sums, no `id` on value objects, no `now()` re-sampling, no `.toISOString()`, no LIKE surfaces, no public exposure); (11) journey A–D contract pointer + suites list; (12) deferred-items D-1..D-4 pointer.

## Anchoring method — 3 metric spot-verifications against `backend/db/repo/admin/platform-analytics.repository.ts`

1. **`sessions.today`** — doc §2.2 says "count(*) filter (where createdAt >= utcDayStart(now))". Verified: `getSessionStats` builds `todayStart = utcDayStart(now)` (repo L209) and `today: count(*) filter (where session.created_at >= ${todayStart})` (L215); `utcDayStart` = `Date.UTC(y, m, d)` (L100-102). MATCH.
2. **`gatewayRevenueByCurrency[].totalAmount`** — doc §2.3 says per-currency paid-only exact-string sums. Verified: `getRevenueStats` — `.where(eq(studentPayments.status, PaymentStatus.Paid))` (L297), `totalAmount: coalesce(sum(amount),0)::text` (L289), `GROUP BY currency` + ascending order (L298-299), `paidPaymentsCount: count(*)::int` (L294). MATCH.
3. **`subscriptions.activeInWindowNow`** — doc §2.4 says the expiry-oracle window filter. Verified: `getSubscriptionStats` — `count(*) filter (where status = 'active' AND coalesce(start_date, ${now}) <= ${now} AND (end_date IS NULL OR ${now} < end_date))` (L368-371), `SubscriptionStatus.Active` bound member value. MATCH.
   (Extra: `pendingWithdrawals` → `getHealthIndicators` L477-485 `teacher_transaction` `type='withdrawal' AND status='pending'` — MATCH.)

## Quality gate

`bun run scripts/health/sub-loop.ts docs/admin/platform-analytics.md --lifecycle duplicates` → **exit 1** (recorded honestly):
- tsgo stage ✅ ("passed, no errors for docs/admin/platform-analytics.md").
- oxlint stage ❌ exit 1 — `No files found to lint … 0 files with 303 rules`. This is the **known `.md` sub-loop artifact**, NON-APPLICABLE to markdown (oxlint's configured scope contains zero .md files). Proven file-independent: control run of the identical oxlint command on untouched `docs/admin/user-management.md` also exits 1 (same DEV2-004 outcome 7.1 precedent). Downstream biome/lint stages unreachable after the hard stop.
- **duplicates stage: PASSES** — `check:duplicates` skips non-`.ts/.tsx` targets by design (`shouldSkipJscpd`, `scripts/health/sub-loop-checks.ts`); direct `bunx jscpd -c .jscpd.json docs/admin/platform-analytics.md` → `Found 0 clones`, exit 0.

## Next actions

None for 7.1 — doc complete and anchored. 7.2 (AGENTS.md propagation) proceeds per tasks.md.
