# Task 2.5 Outcome — PlatformAnalyticsRepository

## Summary

Implemented the read-only analytics repository `PlatformAnalyticsRepository` per plan §4.1, with the sanctioned dual-branch executor (RULING 3) and the 4-tier test suite at the RULING-4 canonical path. Executed via three agent passes (author → continuation/finalize → max-lines split) after two infra timeout interruptions; all quality gates verified green by the orchestrator.

## Files created/modified

| File | Action | Notes |
|---|---|---|
| `backend/db/repo/admin/platform-analytics.repository.ts` | CREATE | 10 public methods, identical signatures; 463 physical / 267 oxlint-counted lines |
| `backend/db/repo/admin/platform-analytics-query-helpers.ts` | CREATE | 429 physical / 291 counted lines — repo-row interfaces, calendar helpers, shared mappers, 5 extracted impls (bodies byte-identical); follows `admin-user-query-helpers.ts` precedent |
| `backend/db/test/logic/admin/platform-analytics.repository.test.ts` | CREATE | 4-tier suite, 30 tests / 225 expect() calls — RULING-4 path (runner-covered by `test:db`) |

Files NOT modified: `backend/db/repo/admin/admin-user.repository.ts` (reuse-not-rebuild), all schema/migration files (read-only ticket), journey file (owned by 2.1-2.4/2.6).

## Method contracts (carry-forward for Task 2.6)

- `countRecentlyActiveUsers(now, tx?)` — 24h activity, NULL-safe governance exclusion (isDeleted/suspended/isBlocked treated false)
- `getSessionStats(now, tx?)` — 10 counters; today = [utcDayStart(now), now] with 1ms-before-today exclusion; ISO-Monday week; month start; awaitingConfirmation flips on confirmedByStudentAt
- `getSessionDailyTrend(now, tx?)` — sparse (day, count) rows over 30-day window; zero-fill is the SERVICE's duty (plan D6)
- `getRevenueStats(now, tx?)` — paid payments grouped by currency, gateways NOT IN (OfflineCash, BankTransfer, Scholarship); money `coalesce(sum(amount),0)::text`
- `getRevenueDailyTrend(now, tx?)` — sparse (day, currency, amount) rows over window currency set
- `getSubscriptionStats(now, tx?)` — 5 enum status counters + total + activeInWindowNow (ACTIVE-window semantics; expired end_date with status='active' excluded)
- `countOfflineActivations(tx?)` — counts exactly the excluded offline gateways
- `getTeacherPresenceStats(tx?)` — certifiedCount / evaluatorCount / onlineNowCount
- `getRatingStats(tx?)` — reports.studentRatingByTeacher + evaluations.score averages, `round(avg::numeric,2)::float8`, soft-delete excluded, NULL when empty (honest null)
- `getHealthIndicators(tx?)` — pendingDisputes (disputed sessions) + pendingWithdrawals (teacher_transaction withdrawal/pending)

## Ruling compliance

- RULING 3 (executor): trailing `tx?: DBTransaction` on all 10 methods; tx-branch runs Drizzle builder on `tx`; non-tx branch routes raw parameterized `$n` SQL through `queryDb`; global `db` never imported (pinned by Tier-4 static scan).
- RULING 4 (test path): suite at `backend/db/test/logic/admin/` (sibling precedent, runner-covered). Plan's `backend/db/repo/admin/__tests__/` amended accordingly.
- RULING 1: immutability triggers irrelevant here (INSERT-only factories, rollback-based tests).

## Verification results

- `run-test.ts backend/db/test/logic/admin/platform-analytics.repository.test.ts` → **30 pass / 0 fail** (Tier 1 dual-branch coverage; Tier 2 boundary matrix incl. ISO-Monday, first-of-month, 1ms-before-today, awaitingConfirmation flip, activeInWindowNow expiry, never-merged currencies, 24h boundary, 30-day edges; Tier 3 empty-state honesty; Tier 4 static security scans incl. zero `--` in SQL text, zero prepared statements, parameterization-only, enum VALUE imports, 10 dual branches)
- `sub-loop --lifecycle duplicates` on both repo files → **exit 0** (after max-lines split; initial 502>300 oxlint failure resolved via helpers split — zero behavior change)
- `bunx tsgo -b --noEmit` → exactly 1 error (sanctioned journey RED import; owned by Task 2.6)

## Semantic review

Dumb-read only (zero business assembly; trend zero-fill lives in the service per plan D6) ✓ · no cross-layer imports ✓ · enums as VALUE imports ✓ · no dead branches ✓ · no mutable module state ✓ · no read-then-write races (single-pass aggregate reads) ✓ · clean comments, zero plan-artifact references (grep-verified) ✓ · parameterized SQL only ✓ · scope boundary = exactly 3 new files ✓

## Deviations

1. Test path moved to `backend/db/test/logic/admin/` (RULING 4 — plan path invisible to test runners).
2. Repo split into two files (oxlint max-lines 300 cap; admin-user sibling precedent).
