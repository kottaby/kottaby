# Task 4.3.BF-BS Outcome — Agent-Browser Functional Loop, `/admin/analytics`

*Visual QA subagent run. Page + container were shipped and unit-green (see `outcome/4.3-outcome.md` / `outcome/4.4-outcome.md`); this pass supplies the deferred browser-tier evidence (4.3.BF + 4.4.BF) and the visual loop (4.3.BS + 4.4.BS, see `outcome/4.4-visual-outcome.md`).*

## 1. Environment

- Dev server: `bun run dev` (Next 16.3.3 turbopack, `NEXT_DIST_DIR=.next-dev`) — had to be started detached via subshell (`(bun run dev > dev.log 2>&1 &)`); the first `nohup` invocation died with its parent shell. Ready in 525 ms; route compiles instant.
- DB: `postgres://postgres@127.0.0.1:5432/app_db`, schema pushed + standard seed. Credentials verified against the seed source (`backend/db/seeds/users/seed-users.ts` + `backend/db/seeds/lib/seed-config.ts`): every demo user is created with `ADMIN_PASSWORD ?? Seed_Pass1!`; `.env` carries `ADMIN_EMAIL=admin@app.local` / `ADMIN_PASSWORD=adminpassword123`, so **admin@app.local / adminpassword123** works, and the same password covers the demo **student@draftacademy.local** (used for F7). Users table: `admin@app.local (admin)`, `teacher|parent|student@draftacademy.local`.
- Browser: `agent-browser` (chromium), viewport set per scenario; locale driven by the `NEXT_LOCALE` cookie (the app's locale mechanism — `shared/locale/server-cookies.ts`, `LOCALE_COOKIE_NAME = "NEXT_LOCALE"`); auth via the httpOnly `access_token` cookie through the real `/login` form.
- **No git-mutating command was run; no repository file was modified by this task.**

## 2. Functional results (F1–F8)

| # | Scenario | Result | Evidence |
|---|---|---|---|
| F1 | Anonymous `GET /admin/analytics` | **PASS** | After clearing cookies, URL lands on `/login?redirect=%2Fadmin%2Fanalytics`, document title = localized login meta; page body never renders. |
| F2 | Login as admin via `/login` form | **PASS** | Filled Email/Password TextFields (admin@app.local / adminpassword123), submitted; `?redirect=` param honored → landed on `/admin/analytics`, header shows "Super Admin / admin@app.local". |
| F3 | Dashboard renders as admin | **PASS** | All 7 section cards present (Users, Sessions, Revenue, Subscriptions, Teachers, Ratings, Operational Health), both trend charts render (recharts SVG bodies, 300 px plot height), Refresh button present, `Last updated 09/04/2026, 20:34` caption present. |
| F4 | Manual Refresh | **PASS** | Click → `POST /api/graphql` 200 captured in the network log. Caption update leg proven during F5 (below): caption ticked to a later minute after a server round-trip. In-flight busy chip is too short-lived to catch by hand at local latency (response < 400 ms); the chip + stale-snapshot-under-aria-busy behavior remains pinned by the component suite (8 pass / 0 fail). |
| F5 | Polling (120 s cadence) | **PASS** | Observed live: with the network log cleared and no user input, at t≈70 s of observation (≈120 s after the previous request) automatic `POST /api/graphql` requests appeared AND the caption self-updated `20:34 → 20:36` — pollInterval fires and refreshes the staleness caption without user action. |
| F6 | Freshness parity (DB → UI) | **PASS** | `INSERT INTO student_payments … (4, NULL, 499.99, 'EGP', 'paymob', 'paid', now(), now()) RETURNING id=153` (column shape copied from `backend/db/schema/billing/student-payments.ts`). Before: Revenue card = `No revenue recorded yet.` / Offline activations 0. After Refresh: `EGP 499.99 | 499.99 | 1` — the EGP row ascends by exactly the inserted amount (DOM text compared before/after; screenshot `/tmp/ui-shots/functional-F6-after-refresh-desktop-en.png`). |
| F6 cleanup | Fixture removal | **PASS (with note)** | `DELETE FROM student_payments WHERE id=153` succeeded directly — **no immutability trigger is installed on `student_payments` in this dev DB** (`pg_trigger` shows none), so `session_replication_role`/trigger-suspend gymnastics were unnecessary. Post-delete count = 0; a follow-up Refresh restored `No revenue recorded yet.` (also proves the delete path). **Note:** the schema docblock declares the table append-only via `3-immutability-triggers.sql`; local dev DB apparently skips that migration — flagging for the fixtures/ops owner. |
| F7 | Wrong-role bounce | **PASS** (live, not just unit) | Logged in as `student@draftacademy.local` / adminpassword123 (same seed credential), navigated to `/admin/analytics` → server guard bounced to **`/student/dashboard`** (role-specific path, NOT bare `/dashboard`), student chrome rendered. |
| F8 | dev.log runtime errors | **PASS** (with incidental note) | Normal flows produced zero hydration errors, zero failed API calls (all POST /api/graphql 200, all GET 200). The only `UnauthorizedError` domain rejections are the expected anonymous `Me` probes during F1. Two `[browser] Uncaught Error: Not supported on server side` entries at `useDisconnectNotifier → notifications.show` exist — both trace exclusively to an artificial fault-injection probe of this pass (route-aborted refresh, see §4), not to any normal flow; the helper is outside this task's module scope (follow-up watch item, §5). |

## 3. Incidental extra coverage (free, from the F4 fault probe)

While attempting to catch the in-flight refresh chip, the refresh was once route-aborted and later re-probed; this incidentally exercised:

- The app-level connectivity gate: aborted requests flip the app to the full-screen `Server connection lost / RETRY` posture (`AppApolloProviderHelpers.InitializingUI`) — it rendered, and the gate cleared cleanly once the route was removed (recovery: `agent-browser network unroute` + reload; the RETRY click alone kept the gate because the aborting route was still installed — harness mistake, not app behavior).
- The Apollo error-link routing (`error-routing.ts` → `link-factories.ts`) for network-failure kind.

**Probe aftermath checklist:** routes removed (`network unroute`), fixture deleted, dashboard re-verified populated (9 h2 nodes), baseline restored.

## 4. Deviations / notes

1. **Screenshot inspection is DOM-audit-based.** This subagent context cannot render PNG images, so the visual checkpoints were verified via computed-style/geometry `eval` audits (grid column counts, offsets, overflow, touch-target heights, directions, computed palette colors) — arguably stricter for the listed checkpoints — and every capture was still saved to `/tmp/ui-shots/` for human review. See `outcome/4.4-visual-outcome.md`.
2. **F4 busy-chip leg** verified via the pinned component suite instead of a live catch (local latency < chip visibility threshold); F4's network leg and caption-update leg are live-verified.
3. **F6 cleanup** needed no trigger suspension because the immutability trigger set is absent in this dev DB (see table above) — recorded as an environment note, not worked around silently.
4. `SEED_ANCHOR_DATE=2026-06-25` in `.env` vs sandbox clock 2026-09-04: irrelevant to every F-result (all trends zero-filled windows, revenue sums unanchored); the F6 fixture used `now()` per the task.
5. No task-card edits, no commits; all screenshots under `/tmp/ui-shots/` (13 files).
