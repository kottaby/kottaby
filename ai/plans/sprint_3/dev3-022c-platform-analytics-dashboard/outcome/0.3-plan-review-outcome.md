# Task 0.3 Outcome — Plan Review Gate (@plan-review, Phase 1.5)

**Feature:** `dev3-022c-platform-analytics-dashboard`
**Branch:** `feat/dev3-022c-platform-analytics-dashboard` (tree state also carries the landed DEV3-018 cold-start surface as verified working-tree content)
**Date:** 2026-08-31
**Mode:** REVIEW-ONLY — zero source files and zero plan files modified; the only writes are this outcome file and four ❌ rows appended to `deferred-items.md`.
**Method:** `.agents/skills/plan-review/SKILL.md` (read plan → map affected layers → read layer AGENTS.md → check compliance → report), extended with the task card's five review dimensions (completeness, consistency, implementability, testability, risk) and independent spot-verification of every load-bearing anchor cited by specs.md / plan.md / tasks.md.

---

## Verdict

**PASS-WITH-LOGGED-FINDINGS.**

The plan is architecturally sound, internally consistent, and comprehensively traceable (every REQ-001..083 maps to plan decisions D1–D13 and to tagged tasks; journeys A–D map 1:1 to test-first tasks 2.1–2.4; all mandated test tiers have runners in `package.json`). No CRITICAL findings. **Four findings (2 HIGH, 2 MEDIUM) are logged as ❌ blockers in `deferred-items.md`** — each is a bounded decision/amendment that must be resolved by its owning task before that task executes; none invalidates the plan's structure. Findings already resolved by Task 0.2's verification sweep (anchor drift, navItems path, `test/ui/components/admin` existence) are recorded here as RESOLVED and were NOT duplicated as blockers.

---

## Findings Table

| # | Severity | Finding | Affected file(s)/task(s) | Disposition |
|---|---|---|---|---|
| F-1 | **HIGH** | **Journey teardown is impossible as written for `student_payments` and `teacher_transaction`:** both tables are trigger-immutable (UPDATE + DELETE raise exceptions, migration `backend/drizzle/20260904084152_custom_3-immutability-triggers/migration.sql:67-98`), yet Journeys B/C require PAID payment + pending-withdrawal fixtures and task 2.4 / REQ-074 / plan §4.4 mandate tracked hard-deletes of exactly those tables in `afterAll`. The only trigger-suspension helper covers `audit_logs` (`test/helpers/db-cleanup.ts:83-109`); `JOURNEY_TRACKED_TABLE_DELETE_ORDER` (`test/workflows/helpers/journey-fixture-registry.ts:67-76`) cannot track these tables and its guidance says do NOT track `teacher_transaction`. Zero-residue (harness rule 2) and REQ-042 byte-identity would fail on re-runs. | specs REQ-074/REQ-042; plan §4.4; tasks 1.3, 2.2, 2.4 | **MUST-LOG-AS-BLOCKER → DI-0.3-1** (resolve before 1.3/2.2/2.4: record a test-only trigger-suspension approach for the immutability family, then amend 2.4 teardown + REQ-074 wording) |
| F-2 | **HIGH** | **Governance gate already exists — reuse decision owed:** `assertActorAdminActive` (`backend/services/admin/admin-gate.helpers.ts:107-148`) already implements role + governance (deleted→blocked→suspended, existing `accountDeleted`/`accountBlocked`/`accountSuspended` keys, ONE `logDomainError` per denial) — the plan (D8, §4.2) prescribes hand-rolling this pipeline as net-new and REQ-002's reuse list omits the helper. Reuse would change two plan-mandated observables: absent-row → `ForbiddenError` (helper) vs `UnauthorizedError` (plan REQ-031/REQ-054/journey 2.4 — note the plan also claims "parity with `assertActorAdmin`", which the shipped helper contradicts on that branch), and two actor-row reads (helper) vs the plan's single read. Landed DEV3-018 rule: "consume-and-extend additively; never fork the role-check semantics"; helper header: "single source of gate truth". | specs REQ-002/031/032/054; plan D8/§4.2; tasks 0.2, 2.4, 2.6, 7.1 | **MUST-LOG-AS-BLOCKER → DI-0.3-2** (explicit ruling before 2.6/2.4: reuse + amend REQ-031 absent-row semantics & tests, or record why the bespoke pipeline is kept) |
| F-3 | **MEDIUM** | **Repo non-tx executor conflicts with layer AGENTS.md:** plan §4.1 / task 2.5 prescribe `tx ?? db`, but `backend/db/repo/AGENTS.md` marks CRITICAL "non-transactional read methods MUST use `queryDb(tx)` from `@/backend/db`". Tree precedent is mixed (`users/user.repository.ts` uses `queryDb`; `admin-user.repository.ts` and `audit-trail.repository.ts` — the mirrored siblings — use `tx ?? db`). | plan §4.1; tasks 2.5 (2.5.IV), 6.2 | **MUST-LOG-AS-BLOCKER → DI-0.3-3** (one-line ruling before 2.5: adopt `queryDb(tx)` or document the sibling-precedent deviation) |
| F-4 | **MEDIUM** | **Repo test placed outside the DB-test layer:** plan/task 2.5.TE place the suite at `backend/db/repo/admin/__tests__/…`, outside `backend/db/test/**` — the scope of the `test:db` runner (`package.json:21-23`) and the placement rule of `backend/db/test/AGENTS.md`; sibling precedent is `backend/db/test/logic/admin/admin-user.repository.test.ts`. The suite would be silently excluded from `bun run test:db` sweeps. | plan §7 item 2; task 2.5.TE, 5.1 | **MUST-LOG-AS-BLOCKER → DI-0.3-4** (relocate to `backend/db/test/logic/admin/platform-analytics.repository.test.ts` or record justification) |
| F-5 | MEDIUM | **Stale D13/REQ-061/§3.5/§4.2 premises:** the bundled `FROZEN_QUERY_FIELDS`/`FROZEN_MUTATION_FIELDS` (`sdl-static-assertions.test.ts:109,137`) and `schema-surface.test.ts` inventories have ALREADY been reconciled to the shipped admin/audit/cold-start surfaces (in-file "REQ-061 reconcile-then-extend" comments; `adminUsers`/`adminUserStats` at :147-148), and `apolloCache.test.ts` already pins SEVEN policies incl. `NotificationListPage` + `AdminAuditLogPage` (frozen-surface test ~:180-190) — so "the bundled baselines are verified-stale" is no longer true and task 4.2's "five→six…→seventeen" arithmetic is off (7 + 11 = **eighteen**). | plan D13/§3.5/§5.4; specs REQ-061; tasks 3.2, 4.2 | **RESOLVED-BY-PLAN-CLARITY** — the operative mandate ("recompute the exact literals verbatim against the live schema in the same change set", i.e. reconcile-then-extend) is invariant to the stale rationale, is the test files' own documented procedure, and the exact-equality assertions force the correct literals. Caution recorded: expect recompute to be a no-op for existing names; new totals = 18 cache policies / 19 with the merge policy counted. |
| F-6 | LOW | **Anchor/path drift — already dispositioned by Task 0.2:** plan-cited ranges that moved: `getStats` 450-485 → **178-213**; ACTIVE-window subquery `admin-user.repository.ts:337-346` → **`admin-user-query-helpers.ts:134-143`**; `assertActorAdmin` `user-management.service.ts:240-271` → defined **`admin-gate.helpers.ts:59`** (usage `:231-271`); `withTransaction` import :67 → **:62**; `Promise.all` precedent :679-682 → **:123-126**; nav file `frontend/views/dashboard/navItems.ts` → **`frontend/views/dashboard/nav/navItems.ts`** (admin block :131-145); `session` columns :32-56 → **:50-81**; errors-namespace anchor `shared/locale/types/errors/index.ts:21-100` → the 1-line index re-exports `labels.ts` (keys `unauthorized`/`forbidden`/`internalServerError`/`accountDeleted`/`accountBlocked`/`accountSuspended` verified at `labels.ts:22-41`); barrel "currently re-exports only X" claims stale (`services/admin/index.ts` = 5 modules; `pothos/admin/index.ts` + `query/admin/index.ts` also carry audit-trail). | specs REQ-002/051/065; plan §3.2/§4.2/§5.2; tasks 0.2, 1.4, 2.6 | **RESOLVED-BY-PRIOR-TASK-DISPOSITION** — Task 0.2's outcome (`0.2-prerequisites-outcome.md`) independently re-derived and recorded every actual `path:line` (21/21 OK, 0 MISSING) and executors must read it first per the Non-Negotiable Execution Protocol. Independently re-verified during this review — no discrepancy found with 0.2's corrections. |
| F-7 | LOW | `shared/locale/AGENTS.md` EXISTS (33 lines; task 1.4 claims it "does not exist") — it additionally governs `shared/locale/**` alias discipline and confirms the ONE-argument `getTranslations(locale)` / `getServerTranslations(locale)` signatures REQ-003 mandates. | task 1.4.IV | **RESOLVED-BY-PRIOR-TASK-DISPOSITION** — 0.2's instruction-file inventory already records it as PRESENT; 1.4.IV validates against discovered files. |
| F-8 | LOW | Journey A wording: specs §2.9 say "trend arrays SHALL still be fully populated" for BOTH series, while plan §4.2/§4.4 + task 2.1 rule the revenue trend "EMPTY array when no currency exists in the window" / "skeleton-consistent". | specs §2.9 Journey A; plan §4.2/§4.4; task 2.1 | **RESOLVED-BY-PLAN-CLARITY** — plan §4.2 gives the explicit empty-window ruling and tasks 2.1/2.6.TE align with it; the plan is the implementation authority. Note kept here so 2.1's outcome records the deliberate reading. |
| F-9 | LOW | Log-context entity string: plan REQ-036 mandates `entity: "users"` (plural); the shipped gate helpers log `entity: "user"` (singular). | specs REQ-036; plan §4.2; task 2.6 | **RESOLVED-BY-PLAN-CLARITY** — plan is internally consistent (REQ-036 ↔ §4.2 ↔ 2.6); if DI-0.3-2 resolves to reuse `assertActorAdminActive`, adopt its existing `entity: "user"` value and note it in 2.6's outcome. |
| F-10 | INFO | Task 0.1 already recorded that `scripts/lint-service.ts` has NO `--id` flag (task cards 0.1/5.2 say `--id baseline`/`--id final`); 0.1 ran without it. | tasks 0.1, 5.2 | **RESOLVED-BY-PRIOR-TASK-DISPOSITION** — follow 0.1's adaptation at 5.2 (`bun run scripts/lint-service.ts --json`). |
| F-11 | INFO | Repo test conventions: the skill checklist's "no `expect().rejects.toThrow()`" is not restated in task 2.5.TE, but `backend/db/test/AGENTS.md` rule 3 mandates the try/catch helper and 2.5.IV validates against discovered AGENTS.md. | task 2.5.TE | **RESOLVED-BY-PLAN-CLARITY** — covered by the IV gate; no change needed. |

---

## Review dimensions — summary of evidence

- **Completeness:** PASS. REQ-001..083 all trace to plan decisions (D1–D13) and to `_Requirements:`-tagged tasks (0.1→7.3, incl. every .QL/.TE/.SEC/.SR/.IV/.OUT gate). Journeys A–D (specs §2.9) map 1:1 to test-first tasks 2.1–2.4 with per-journey assertion sets (plan §4.4). Every task carries acceptance-oriented subtasks and an outcome-file requirement.
- **Consistency:** PASS with the logged wording/premise findings (F-5, F-8, F-9). Type shapes (§2.2) match the SDL (§3.1) leaf-for-leaf and match `AdminUserStatsReturnType` (verified `backend/types/admin/admin-user.types.ts:91-102` — the ten counters are exact); `PlatformAnalyticsUsers` = 10 + `recentlyActive24h` = eleven ✓. Session/subscription status counters match the real enums (exactly five members each, verified). Document selection (§5.4) covers every SDL leaf. Metric definitions (`awaitingConfirmation`, `activeInWindowNow`, honest-null ratings, money-as-string, currency containment, UTC boundaries) are identical across specs/plan/tasks. The ACTIVE-window predicate the plan reuses verbatim exists at `admin-user-query-helpers.ts:134-143`.
- **Implementability:** PASS with blockers F-1..F-4. Ordering/dependencies are sound (0.x verify → 1.x substrate → 2.1–2.4 RED → 2.5/2.6 GREEN → 2.M gate → 3 wire → 4 UI → 5 sweeps → 6 reviews → 7 knowledge). Verified in tree: all four actor factories, `runInRollback` (`backend/db/test/test-utils.ts:34`), `withAuditDeleteTriggersSuspended`/`deleteUsersByIds` (`test/helpers/db-cleanup.ts:83/:123`), `withPageAuth`/`roleDashboardPath`/`getLocaleFromCookie` anchors (exact), `DateTime` scalar (`scalar.pothos.ts:28` + `builder.ts:76-82` + `codegen.ts:21-30`), frozen-six allowlist (`public-operations.ts:36-46`), `$all` pattern (`admin-users.query.ts:74-79`), `entity-setup.ts` = 174 lines with the 8 factories confirmed absent, `recharts@^3.10.1` in dependencies, `mapGraphQLErrorByCode` (`frontend/providers/apollo/error-link.map`) + `extractErrorCode` (`frontend/lib/graphql-error-utils.ts:17`), parity-suite + namespace patterns (`defineNamespace<Labels>("x.y", t => t.xTranslations)`, function-leaf precedent `welcome: (userName) => string`), `test/ui` component-tier conventions, and `frontend/views/admin/{analytics}` + `app/(dashboard)/admin/{analytics}` genuinely absent (net-new per D12).
- **Testability:** PASS. Acceptance criteria are verifiable oracles (exact deltas, boundary math, byte-identity, denial matrices); mandated layers have runners: `test:db`/`run-test.ts` (repo), `test:services` (service), `test:graphql` (wire), `run-test.ts test/workflows` (journeys), `test:ui:components` (component), plus schema-inventory/documents/cache/nav/parity suites named per file. Coverage mandate (REQ-070) backed by `test:db:coverage`.
- **Risk:** PASS with F-1/F-2 logged. Schema-drift guard (REQ-043) is enforced at 0.1 (verified EMPTY), 1.2, 2.M, 5.2; reuse-not-rebuild is protected by git-diff gates on `admin-user.repository.ts`/`user-management.service.ts` (2.M/5.2) — F-2 extends this protection to the governance gate; security postures (BFLA `$all` conjunction, zero-arg BOLA/BOPLA, log hygiene, oracle discipline, read purity, rate-limit forward item) are all specified with wire-level proofs.

## AGENTS.md layer compliance (skill Step 4)

Read: `backend/AGENTS.md`, `backend/db/repo/AGENTS.md`, `backend/db/test/AGENTS.md`, `backend/services/AGENTS.md`, `backend/graphql/AGENTS.md`, `backend/types/AGENTS.md`, `frontend/AGENTS.md`, `frontend/graphql/AGENTS.md`, `shared/AGENTS.md`, `shared/locale/AGENTS.md`, `app/AGENTS.md`, `test/workflows/AGENTS.md`, `test/ui/AGENTS.md`.

- Type imports ✓ (single canonical types file + barrel; no local types in Pothos/service/repo beyond the sanctioned repo-row allowance).
- Service boundaries ✓ (server page renders a client container; data via Apollo `useQuery`; services callable from SSR).
- MUI v9 ✓ (`sx`-only, theme tokens, `*Outlined` icons, no `useLazyQuery`, no `FormEvent` — matches `frontend/AGENTS.md`).
- i18n ✓ (compile-time namespace registration exactly per `shared/locale/AGENTS.md` + `shared/AGENTS.md`; no `next-intl`/`shared/messages/`/hardcoded text; ONE-arg server translations verified).
- Logging ✓ (`@/backend/lib/logger` / `@/frontend/lib/logger`; no `console.*`; bounded denial context).
- Test conventions ✓ (journeys: committed fixtures + TrackedFixtures-style cleanup, honest authorization, no `runInRollback`; repo tests: `runInRollback` + `tx` + try/catch helper) — EXCEPT the teardown/tracking gap logged as F-1 and the repo-suite placement logged as F-4.
- GraphQL documents ✓ (`{entity}QueryDocument` naming, `TypedDocumentNode`, `@apollo/client/react`; zero-`id` embedded subtree is the documented embedded-type exception with `keyFields: false`, matching the six shipped precedents).

---

## Blockers appended to `deferred-items.md` (explicit statement)

The following ❌ rows were APPENDED (nothing overwritten) to `ai/plans/sprint_3/dev3-022c-platform-analytics-dashboard/deferred-items.md`:

1. **DI-0.3-1** (HIGH, finding F-1) — Journey teardown vs `student_payments`/`teacher_transaction` immutability triggers; resolve before tasks 1.3/2.2/2.4.
2. **DI-0.3-2** (HIGH, finding F-2) — Governance-gate reuse decision (`assertActorAdminActive` exists; REQ-031 absent-row divergence); resolve before tasks 2.6/2.4.
3. **DI-0.3-3** (MEDIUM, finding F-3) — Repo non-tx executor ruling (`tx ?? db` vs `queryDb(tx)`); resolve before task 2.5.
4. **DI-0.3-4** (MEDIUM, finding F-4) — Repo test relocation into `backend/db/test/logic/admin/`; resolve before task 2.5.

Pre-existing ledger rows acknowledged, not modified: DI-0.2-1 (⚠️, ADD-vs-CREATE wording for `test/ui/components/admin/`) and D-1..D-4 (📅 forward-owned). Per task 0.3's rule, these four ❌ rows must be resolved (and their statuses flipped) by their owning tasks BEFORE those tasks start; the final gate (REQ-076) still requires ZERO open ❌/⚠️.

*End of Task 0.3 plan-review outcome.*
