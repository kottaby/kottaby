# 6.1 review-types outcome — DEV3-022c wave 6.1 (rebuilt tree @ 27e259a)

Mode: READ-ONLY independent review. No source edits, no commits, no tasks.md flips.
Branch verified `feat/dev3-022c-platform-analytics-dashboard` @ 27e259a before start, mid-review (x2), and before finish — NO HEAD-flip observed this session.

## Scope reviewed (all files read fully)

1. `backend/types/admin/platform-analytics.types.ts`
2. `shared/locale/types/analytics/index.ts` + `Translations` (`shared/locale/types/message.ts`) + en/ar bundles + `navItems.ts` label wiring
3. `frontend/graphql/generated/gql/graphql.ts` (AdminPlatformAnalytics family) + `frontend/graphql/sharedDocuments/admin/platform-analytics.documents.ts` + consumer
4. `backend/services/admin/platform-analytics.service.ts`, `backend/graphql/query/admin/platform-analytics.query.ts`, `backend/graphql/pothos/admin/platform-analytics.pothos.ts`, `frontend/views/admin/analytics/**` (8 files)
5. REQ-004 / REQ-060 / REQ-066 spot-verification

## Per-checklist verdict

**1. Canonical types — ✓.** All 10 shapes every-member `readonly`, arrays `readonly T[]`; zero `any`/`unknown`/loose unions. Exactly ONE intersection (`PlatformAnalyticsUsersReturnType = AdminUserStatsReturnType & { readonly recentlyActive24h: number }`, types.ts:26-28) — base type verified fully readonly (`admin-user.types.ts:91-102`), so readonly closure holds. Money = exact decimal strings only (`totalAmount`/`last30DaysAmount`/`amount`: `string`); zero `number` money. Date-vs-scalar: `generatedAt`/`bucketStart` are `Date`; no ISO-string members, no `.toISOString()` in the canonical→service→pothos path (only docstring mention, pothos:19).

**2. Locale types + Translations — ✓.** `AnalyticsLabels` = 71 readonly slots, sole function leaf `lastUpdatedLabel(at: string)`; both bundles annotated `: AnalyticsLabels` (`en/analytics/index.ts:4`, `ar/analytics/index.ts:4`) so missing keys fail tsgo, plus 14-test parity belt incl. Arabic-script + registry-wiring pins (`analytics-namespace.parity.test.ts`). Wired as `analyticsTranslations: AnalyticsLabels` (`message.ts:24`); en/ar `messages.ts:25` register both. Nav label type-safe: `DashboardLabels.analytics` (`dashboard/index.ts:43`) → `labelKey: "analytics"` flows through `NavLabelKey` (`keyof DashboardLabels | keyof HandshakeCodeLabels` minus compile-time collision carve-out) resolved via property access `dashboardLabels[item.labelKey]` (navItems.ts:166-174) — no casts, no string-index escape.

**3. Generated alignment + TypedDocumentNode — ✓ (see I-2).** `adminPlatformAnalyticsQueryDocument: TypedDocumentNode<AdminPlatformAnalyticsQuery>` (documents.ts:24), named operation, zero variables (`Exact<{ [key: string]: never }>`, graphql.ts:253), selection = SDL leaf-for-leaf (pinned by documents contract test 3/0). Consumed via `useQuery` in the container; views type the snapshot as `AdminPlatformAnalyticsQuery["adminPlatformAnalytics"]` (index-access alias — MetricGrid:26, TrendCharts:15). Generated shapes match canonical field-for-field across all 11 object types (counters `number`, money `string`, ratings `number | null`); DateTime is `string` in generated per codegen (`codegen.ts:23` `DateTime: "string"`) — the wire transport representation, not drift.

**4. Zero local types — ✓ backend / ✓ with MINOR findings in views.** Service: no type declarations; type-only imports from `@/backend/types` (+ repo sparse-row types for the merge helpers — see I-1). Query: no types, thin hive. Pothos: 11 `objectRef<T>` parameterized by canonical types, type-only import; ratings `exposeFloat(…, { nullable: true })` = REQ-060 honest nullables; `t.expose(…, { type: "DateTime" })` Date passthrough. Views: `Snapshot` aliases = generated index-access (acceptable); props interfaces = component contracts (acceptable). Findings M-1/M-2 below. No `as` casts, no `any`, no bracket-string access anywhere in scope.

**5. Requirements — ✓.** REQ-004 canonical single home held (violations = M-1/M-2 only, both minor). REQ-060 honest ratings: canonical `number | null` → pothos nullable → generated `number | null` → view renders `?? "—"` + `noRatingsYet` (MetricGrid:132-148). REQ-066 locale types complete, property-access-only consumption (`t.usersSection` etc., incl. F-1 aria-label keys).

## Findings

- **MINOR M-1** — `SessionTrendChart.tsx:18-21` + `RevenueTrendChart.tsx:24-28`: local `SessionTrendPoint`/`RevenueTrendPoint` interfaces re-declare the generated leaf shapes with a widened `bucketStart: string | Date` instead of aliasing the generated leaf types. Leaf-by-leaf mapping (TrendCharts:51-59, 73-81) keeps drift caught at the mapping site, but the widening weakens the transport pin and the interfaces are the only in-scope structural re-declarations. Suggest aliasing the generated leaf types (or recording the dual-representation rationale at the interfaces).
- **MINOR M-2** — `AnalyticsHeaderBar.tsx:18-23`: `AnalyticsHeaderBarProps.generatedAt: string | Date | null` — the only production caller passes the generated `string` (Container:59); the `Date` branch + `instanceof` conversion (HeaderBar:59) is dual-representation tolerance with no production producer. Weakens the transport pin; harmless at runtime.
- **INFO I-1** — `backend/db/repo/admin/platform-analytics.repository.ts:121-157`: six repo row interfaces (`SessionTrendRow`, `RevenueStatsRow`, `RevenueTrendRow`, `TeacherPresenceRow`, `RatingStatsRow`, `HealthIndicatorsRow`) structurally mirror canonical section shapes field-for-field (without `readonly`). Outside this wave's named scope (repo layer) and drift is caught at the service composition site (return type `PlatformAnalyticsReturnType`), but worth REQ-004 bookkeeping.
- **INFO I-2** — DateTime transport is `string` in generated types vs `Date` in canonical — inherent codegen posture (codegen.ts:23), pinned by wire matrix; all frontend date handling is presentation-boundary only (`formatApplicantDate`, `tickFormatter`), backend zero `.toISOString()`.

## Gates re-run

- `bun run tsgo`: **exit 0** (run-locked, next-env.d.ts restored). Run once, passed first try.
- Branch pin: `feat/dev3-022c-platform-analytics-dashboard` @ 27e259a — verified at start / mid / finish; **no HEAD-flip this session**.

## Final verdict

**CLEAN** — zero BLOCKING, zero MAJOR; 2 MINOR (M-1, M-2: view-layer transport-pin widening) + 2 INFO recorded. No source changes made by this review; M-1/M-2 are optional hardening for the 7.x doc/PR pass or a follow-up patch, not release blockers.
