# Task 4.5 Outcome — Frontend Integration Sweep (locale / nav / cache / docs graph)

**Feature:** `dev3-022c-platform-analytics-dashboard`
**Date:** 2026-08-31
**Mode:** Sweep-only verification — zero repo files patched (working tree before/after identical except task-outcome files + one git-ignored-pattern login-artifact dir, see §6).

**Result: COMPLETE — all 5 sweep suites green (158 tests, 0 failures), apollo-directory side suites green (33 tests), live agent-browser role-filtration proof captured (admin SHOWS / non-admin ABSENT), `tsgo` "error TS" count = 0.**

---

## 1. Sweep suite results (mandated runner, each via `bun run test/scripts/run-test.ts <path>`)

| # | Suite | Pass | Fail | expect() calls |
|---|---|---|---|---|
| 1 | `shared/locale/analytics-namespace.parity.test.ts` | **85** | **0** | 757 |
| 2 | `frontend/views/dashboard/nav/navItems.test.ts` | **26** | **0** | 144 |
| 3 | `frontend/providers/apollo/apolloCache.test.ts` | **12** | **0** | 35 |
| 4 | `frontend/graphql/sharedDocuments/admin/platform-analytics.documents.test.ts` | **11** | **0** | 140 |
| 5a | `frontend/providers/apollo/error-link.map.test.ts` (dir-suite member) | **29** | **0** | 98 |
| 5b | `frontend/providers/apollo/utils/link-factories.test.ts` (dir-suite member) | **4** | **0** | 10 |
| | **Total** | **167** | **0** | 1184 |

**Directory-suite note:** `frontend/providers/apollo/` has NO index/dir-level test file (verified by directory listing). The directory's complete test inventory is the three files above (3a + 5a + 5b) — all executed, all green. Tasks 4.5's "plus the apollo directory suite if it exists" is satisfied by running the directory's full test inventory.

**Docs-graph suite highlights (suite 4):** closed eleven-node selection, zero `id` selected anywhere in the subtree (aggregate anonymity), admin sub-barrel ↔ top-level barrel re-export the SAME document instance (cache-key safety), TypedDocumentNode binding intact.

**Cache suite highlights (suite 3):** behavioral proofs that HealthCheck and HandshakeCodeLookup writes normalize INLINE (no standalone cache entities; second write replaces the inline value loss-safely).

## 2. Nav role-filtration — live agent-browser evidence (4.5.SEC proof)

Method per `test/ui/AGENTS.md`: `scripts/browser-login.ts` cookie injection (real emails redacted from prompts — credentials read from env/psql, never echoed). Dev server on port 3000 reused; no test server spawned.

### Admin session — Analytics entry PRESENT
- Login: `Super Admin (role: Admin, id: 1)` via `browser-login.ts --inject` (env `ADMIN_EMAIL`/`ADMIN_PASSWORD`).
- `agent-browser open http://localhost:3000/dashboard` → sidebar snapshot shows **exactly one** `link "Analytics"` listitem, positioned between `Broadcasts` and `Disputes` (matches the pinned audit→broadcasts→… order).
- `get attr a[href='/admin/analytics'] href` → `/admin/analytics` ✓
- EN label verified: `Analytics`. Locale toggled to Arabic (snapshot `dir="rtl"`) → same anchor now reads **`التحليلات`** (href unchanged) — both locale renders of `labelKey: "analytics"` proven live through the real runtime resolution path.
- Screenshot (EN state, Analytics visible in sidebar): `/tmp/ui-shots/nav-admin.png`

### Non-admin session — Analytics entry ABSENT
- Login: `Demo Student (role: Student, id: 4)` (`student@draftacademy.local`, password = the shared seeded `ADMIN_PASSWORD` credential per `backend/db/seeds/users/seed-users.ts:54` + `seed-config.ts` — all demo users are seeded with the same credential).
- Student sidebar (AR default): لوحة التحكم، الإشعارات، الجلسات، الاشتراكات، الواجبات، طلبات الربط، الملف الشخصي — **no Analytics entry**.
- DOM assertion: `document.querySelectorAll("a[href='/admin/analytics']").length` → **0**; body text search for `التحليلات` → **ABSENT** (AR locale).
- Re-verified after EN switch — full nav link inventory: `/student/dashboard|Dashboard, /notifications|Notifications, /student/sessions|Sessions, /subscriptions|Subscriptions, /homework|Homework, /student/link-requests|Link Requests, /profile|Profile`.
- Screenshot: `/tmp/ui-shots/nav-nonadmin.png`

### Corroborating suite evidence (fallback tier, per task-card allowance)
`navItems.test.ts` role-filtration pins (all green in suite 2): non-admin Student/Teacher/Parent each do NOT include `/admin/plans`, `/audit`, or `/admin/broadcasts` items (`test.each` blocks at lines 141–148, 167–174, 207–214). The analytics entry lives in the same admin-only block of `getNavItemsForRole` (admin block of `navItems.ts`), and the live session checks above close the loop at the rendered tier.

## 3. Exactly-one-bundle ownership of the `analytics` dashboard label (4.5.SR proof)

**Cited passing assertion** (suite 2, 26/26): `frontend/views/dashboard/nav/navItems.test.ts:54-66` — *describe "nav label ownership — exactly one bundle owns each nav key" / test "every nav label key across all roles is owned by exactly ONE namespace"*. The test builds `ALL_NAV_ITEMS` as the union of `getNavItemsForRole` over all roles (the admin role's union member includes the `/admin/analytics` entry whose `labelKey` is `"analytics"`, per `navItems.ts:143`) and asserts for EVERY used key: `ownedByDashboard !== ownedByHandshake` (dashboard-bundle XOR feature-bundle). Since the suite passes and `analytics` is in the used-key set, the assertion proves exactly-one-bundle ownership for the `analytics` key.

**Runtime belt (direct probe, this sweep):**
```
analytics in dashboardEn: true  => "Analytics"
analytics in dashboardAr: true  => "التحليلات"
analytics in handshakeCodeEn: false
```
Static corroboration: `shared/locale/types/dashboard/index.ts:43` (`readonly analytics: string`, nav-label doc), `en/dashboard/index.ts:17`, `ar/dashboard/index.ts:17`; zero `analytics` matches in any handshakeCode leaf/type file.

## 4. Semantic review (4.5.SR)

1. **Single navigation entry:** exactly one `"/admin/analytics"` route occurrence in `frontend/views/dashboard/nav/navItems.ts` (line 143); live admin sidebar rendered exactly one Analytics listitem; `navItems.test.ts` adjacency/order pins green.
2. **Exactly-one-bundle ownership:** proven above (§3) — dashboard bundle owns `analytics` on both locale maps; no feature-bundle shadowing; `resolveNavItemLabel` resolves from the owning bundle (its own suite block "every nav item across all roles resolves to a non-empty label" green in both locales).
3. No code authored/patched by this sweep → no new i18n surfaces, no style props, no new deps; everything verified is the already-landed Phase-4 surface.

## 5. Security & tenancy audit (4.5.SEC)

- **Admin-only visibility proven at the rendered tier:** Analytics nav entry present ONLY in the Admin session; ABSENT (0 anchors, 0 label occurrences) in the Student session — §2 evidence.
- Suite tier: role-gated nav fixtures (Student/Teacher/Parent subsets) assert non-inclusion of sibling admin-only routes; the analytics entry shares the same admin block.
- Server-tier route guarding (`withPageAuth` role enforcement on `/admin/analytics`) was proven in the Task 4.3/4.4 functional pass (F1/F7: anonymous bounce to login; student session bounces to `/student/dashboard`) — recorded in `outcome/4.3-visual-outcome.md` / `outcome/4.4-visual-outcome.md` and not re-run here.
- Cookie artifacts from this sweep are chmod-600 under git-ignored-style dirs and were never echoed to stdout.

## 6. Quality loop (4.5.QL)

- **Zero repo files patched** during the sweep (`git status --short` before/after identical except task outcome files + one new untracked artifact dir). Therefore **no `sub-loop --lifecycle duplicates` re-runs were required** — the obligation is vacuously satisfied; the previously-landed Phase-4 files remain at their Task 4.3/4.4 sub-loop-clean state.
- New untracked dir `.browser-auth-student/` (student login cookie jar, mode 600) was created by `browser-login.ts --jar-dir .browser-auth-student` to avoid clobbering the admin jar. `.gitignore:64` covers `.browser-auth/` but not this sibling name — **left untouched (out of Phase-4 file scope; no sweep-failure fix rationale)**; flagged for the orchestrator to optionally extend the gitignore pattern or delete the dir.

## 7. Instruction verification (4.5.IV)

- `.agents/instructions/frontend.instructions.md`: sweep used the sanctioned bun command discipline throughout (`bun run test/scripts/run-test.ts`, `bunx tsgo -b --noEmit`, `bun run scripts/browser-login.ts`); no product code authored → MUI/React/Next/Apollo/Zustand/i18n/logging authoring rules not triggered; the live locale-switch check exercised the compile-time i18n system (labels from the dashboard bundle, never hardcoded).
- `test/ui/AGENTS.md` rules honored: cookie-injection login (no typed credentials), DOM/`snapshot`/`eval` verification first, screenshots written to `/tmp/ui-shots/` only, no dev-server kill, no repo-file test edits.

## 8. Verification summary table

| Check | Command | Result |
|---|---|---|
| All sweep suites (6 files) | `bun run test/scripts/run-test.ts …` | **167 pass / 0 fail** (1184 expects) |
| Types | `bunx tsgo -b --noEmit 2>&1 \| grep -c "error TS"` | **0** |
| Nav role-filtration (live) | agent-browser admin + student sessions | **PRESENT (admin) / ABSENT (student)** — screenshots in `/tmp/ui-shots/` |
| Exactly-one-bundle ownership | navItems.test.ts:55-66 + runtime probe | **GREEN** (dashboard ✓ / handshakeCode ✗, both locales) |
| Quality loop | n/a — zero patches | **No sub-loop runs required** |

## 9. Carry-forward notes

- None blocking. Optional (orchestrator): extend `.gitignore` with `.browser-auth-*/` or remove `.browser-auth-student/`.
- Phase 4 frontend sweep gate is fully green; next per plan = Phase 5 backend verification sweep (5.1).
