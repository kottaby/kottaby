# Task 5.3 — end-to-end smoke outcome

Setup: dev server booted with pinned `DATABASE_URL` (sandbox env hazard
documented in 4.1-4.5 outcome), seeded `kottaby_test` DB, admin session via
`scripts/browser-login.ts` (cookie jar). All figures captured 2026-09-02.

## UI ↔ GraphQL ↔ direct-DB parity (required figure set)

| Figure | UI (page scrape, 4.4.BF) | Live GraphQL | Repo direct read | Raw SQL |
| --- | --- | --- | --- | --- |
| users total | 49 | 49 | 49 | 49 (`deleted_at IS NULL`) |
| users active | 34 | 34 | 34 | — |
| sessions.today | 25 | 25 | 25 | 25 (`created_at >= UTC midnight`) |
| revenue EGP totalAmount | 575.00 | "575.00" | "575.00" | — |
| revenue USD totalAmount | 250.00 | "250.00" | "250.00" | — |
| subscriptions.activeInWindowNow | 5 | 5 | 5 | — |
| teachers.onlineNowCount | 5 | 5 | 5 | — |
| health.pendingDisputes | 5 | 5 | 5 | — |
| health.pendingWithdrawals | 5 | 5 | 5 | — |

Money strings arrive verbatim end-to-end (exact-string discipline, REQ-064) —
no float reformatting at any layer.

## Wire surface assertions

- Live introspection: `Query.adminPlatformAnalytics` exists with
  `args: []` — the zero-arg closed surface holds through the real gateway.
- Generated-committed SDL equality: pinned by `plan-catalog.schema.test.ts`
  (committed-vs-live, 5 pass / 0 fail — see 5.1).
- Wrong-role HTTP access: anon → `/login?redirect=…`; teacher →
  `/teacher/dashboard`; parent → `/parent/dashboard`; student →
  `/student/dashboard` (4.3.BF evidence, re-confirmed this phase).
- Read-only posture: the smoke issued zero mutations; the refresh path
  issues exactly one query per activation (4.4.BF network log).

## 5.3.BF agent-browser functional pass

Executed via agent-browser + browser-login loops (server boot per
tool-call, self-contained batches — sandbox process model documented in the
4.1-4.5 outcome addendum). All assertions above verified live; iterations
required: one (session-expiry re-capture during the visual pass, resolved by
fresh login + per-capture title assertion).

Verdict: **5.3 GREEN** — smoke clean on the full surface.
