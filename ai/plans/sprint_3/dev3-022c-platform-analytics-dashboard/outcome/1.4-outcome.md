# Task 1.4 Outcome — i18n Namespace `analytics` (full registration, REQ-066)

**Feature:** `dev3-022c-platform-analytics-dashboard`
**Branch:** `feat/dev3-022c-platform-analytics-dashboard` (subagent switched from `main` — both refs at `dcb08e9`; tree also carries concurrent Task 1.1/1.3 working-tree content, untouched by this task)
**Date:** 2026-08-31
**Mode:** Implementation — the `analytics` namespace registered end-to-end, dashboard nav label wired, parity suite authored. No commits made.

**Result: COMPLETE — 9 tracked files modified + 6 files created, parity suite 85/85 green, navItems suite 26/26 green, quality loop 15/15 exit 0, `bun tsgo` clean (baseline floor held at 0 errors).**

---

## 1. Files touched (exact list — scope boundary verified via `git status --short`)

### Created
| File | Content |
|---|---|
| `shared/locale/types/analytics/index.ts` | `AnalyticsLabels` — 71 keys: `metaTitle`, `metaDescription`, `title`, `subtitle`; 7 section titles (`usersSection`, `sessionsSection`, `revenueSection`, `subscriptionsSection`, `teachersSection`, `ratingsSection`, `healthSection`); every metric label (users ×11 incl. `recentlyActive24hLabel`; sessions ×10 incl. `awaitingConfirmationLabel`; revenue incl. `offlineActivationsLabel`, table headers `currencyHeader`/`totalAmountHeader`/`last30DaysAmountHeader`/`paidPaymentsCountHeader`, `noRevenueYet`; subscriptions ×7 incl. `activeInWindowNowLabel`; teachers ×3; ratings ×4 + `noRatingsYet`; health ×2); trend labels (`sessionTrendTitle`, `revenueTrendTitle`, `sessionsSeriesLabel`, `revenueSeriesLabel`, `dailyLabel`, axis labels `trendDateAxisLabel`/`trendCountAxisLabel`/`trendAmountAxisLabel`); `refreshAction`, `refreshingLabel`, `lastUpdatedLabel: (at: string) => string` (the single function leaf, per the plural/function precedent); `loadErrorTitle`, `loadErrorBody`, `deniedTitle`, `deniedBody`, `retryAction`. All §5.5/REQ-066 minimum-surface keys present verbatim. |
| `shared/locale/en/analytics/index.ts` | `analyticsEn: AnalyticsLabels` — professional English on every leaf. |
| `shared/locale/ar/analytics/index.ts` | `analyticsAr: AnalyticsLabels` — Arabic script on EVERY leaf (incl. the `lastUpdatedLabel` function output; headers like `آخر 30 يومًا`, `التاريخ (UTC)`). |
| `shared/locale/namespaces/analytics/analytics.namespace.ts` | `export const Analytics = defineNamespace<AnalyticsLabels>("analytics.analytics", translations => translations.analyticsTranslations);` — exact mirror of the existing namespace-file style (stable `<ns>.<ns>` id per shared/AGENTS.md Browser-Translation-Cache rule). |
| `shared/locale/namespaces/analytics/index.ts` | 1-line barrel: `export * from "./analytics.namespace";` (pattern identical to the other 15 namespace barrels). |
| `shared/locale/analytics-namespace.parity.test.ts` | Parity suite modeled EXACTLY on `notifications-namespace.parity.test.ts` (see §4). |

### Modified
| File | Change |
|---|---|
| `shared/locale/namespaces/index.ts` | `export * from "./analytics";` inserted (alphabetical, between `adminUsers` and `applicant`). |
| `shared/locale/namespaces/registry.ts` | `Analytics` imported + added to the `namespaces` const (alphabetical). |
| `shared/locale/types/message.ts` | `Translations` gains `analyticsTranslations: AnalyticsLabels` (appendive, end of interface; type import alphabetical). |
| `shared/locale/en/messages.ts` | `analyticsTranslations: analyticsEn` + import. |
| `shared/locale/ar/messages.ts` | `analyticsTranslations: analyticsAr` + import. |
| `shared/locale/types/dashboard/index.ts` | `DashboardLabels` gains `readonly analytics: string` (nav label, documented "admin — the platform analytics dashboard"). |
| `shared/locale/en/dashboard/index.ts` | `analytics: "Analytics"`. |
| `shared/locale/ar/dashboard/index.ts` | `analytics: "التحليلات"`. |
| `frontend/views/dashboard/nav/navItems.ts` | Admin block gains EXACTLY ONE entry `{ route: "/admin/analytics", labelKey: "analytics", Icon: InsightsOutlined }` + `InsightsOutlined` import (see §3 for placement + icon provenance). |

**Scope note:** `git status --short` also shows `backend/db/test/entity-setup.ts` (M), `backend/types/admin/index.ts` (M), `backend/types/admin/platform-analytics.types.ts` (??), `outcome/1.1-outcome.md` (??), `outcome/1.3-outcome.md` (??) — these are **concurrent Task 1.1/1.3 working-tree content, NOT touched by this task** (verified: zero edits made by this agent to any file outside the 15 above).

---

## 2. Key-surface decisions (plan §5.5 ↔ implementation)

- §5.5 mandates the *minimum* surface and names exact keys; it leaves per-counter label naming open. Naming convention adopted: section-prefixed `<section><Counter>Label` (e.g. `usersTotalLabel`, `subscriptionsActiveLabel`) for map-unique keys, with the prompt/REQ-mandated verbatim names kept unprefixed where so mandated: `recentlyActive24hLabel`, `awaitingConfirmationLabel`, `offlineActivationsLabel`, `currencyHeader`, `totalAmountHeader`, `last30DaysAmountHeader`, `paidPaymentsCountHeader`, `noRevenueYet`, `noRatingsYet`, plus the §5.5 trend/refresh/error keys verbatim (`sessionTrendTitle`, `revenueTrendTitle`, `sessionsSeriesLabel`, `dailyLabel`, `refreshAction`, `refreshingLabel`, `lastUpdatedLabel`, `loadErrorTitle`, `loadErrorBody`, `deniedTitle`, `deniedBody`, `retryAction`).
- Additions beyond the verbatim list, derived 1:1 from the plan §2.2 metric tree so the container never lacks a label: users ×10 remaining counters, sessions ×9 remaining counters, subscriptions ×7 (incl. `activeInWindowNowLabel`), teachers ×3, ratings ×4, health ×2, `revenueSeriesLabel` (mirrors `sessionsSeriesLabel`), axis labels `trendDateAxisLabel`/`trendCountAxisLabel`/`trendAmountAxisLabel` (§5.5 "axis labels"). No key maps to more than one counter; no counter lacks a key.
- Metric VALUES are never embedded in copy — labels render counts only (SEC: no user-supplied interpolation anywhere; `lastUpdatedLabel` composes over a pre-formatted timestamp string only).

## 3. Nav registration — placement + icon provenance

- **Entry:** `{ route: "/admin/analytics", labelKey: "analytics", Icon: InsightsOutlined }` — added exactly once (`grep -c '"/admin/analytics"'` = 1), admin block, **directly after the `/admin/broadcasts` entry**, i.e. after `/audit` but NOT immediately after it. Rationale: `navItems.test.ts:178-186` PINS `broadcastIndex === auditIndex + 1`; inserting between `/audit` and `/admin/broadcasts` would break that pinned adjacency. Placement satisfies "after the `/audit` entry" while keeping the pinned test green (it is: 26/26).
- **Icon:** `InsightsOutlined` exists in the installed `@mui/icons-material@^9.4.0` (verified in `node_modules`). The task prompt said "from lucide-react", but the file's binding import style is `@mui/icons-material` (every nav `Icon` is an MUI `SvgIconComponent` — a lucide icon could not type-satisfy `DashboardNavItem.Icon`), and plan §5.6's MUI discipline mandates `*Outlined` MUI names. lucide-react@^1.37.0 is installed but is NOT the nav-file's import source. Deviation from prompt wording recorded here; icon name itself is exactly `InsightsOutlined`. Import placed in the file's biome-organized (local-alias-alphabetical) position; `biome check` on the file: clean, no fixes applied.
- **Interim 404:** `app/(dashboard)/admin/analytics/` does not exist yet (Phase 4 / Task 4.2), and `/admin/analytics` matches no static route or catch-all (`app/(dashboard)/[feature]` is single-segment only), so the nav link 404s until the page ships — acceptable interim state per the task card; a one-line in-file comment records this honestly.

## 4. Parity suite (`shared/locale/analytics-namespace.parity.test.ts`)

Modeled EXACTLY on `notifications-namespace.parity.test.ts`: mandated-key inventory (all 71 slots, `test.each` on both maps) + exhaustive-inventory guard (no silent key minting); key-set identity en↔ar; every value a non-empty string or function (symmetric sweep); Arabic-script assertion on every ar string slot AND on the ar function-slot output with Arabic-flavored args; exact template pin for `lastUpdatedLabel` (`"Last updated 14:32 UTC"` / `"آخر تحديث 14:32 بتوقيت UTC"`); registry + bundle wiring (`namespaces.Analytics` present, id `analytics.analytics`, getter resolves both composed bundle slices, slice-identity with no re-wrapper); function-slot inventory (exactly ONE function leaf on both maps, string/function split stable).

## 5. Verification results

| Check | Command | Result |
|---|---|---|
| Parity suite | `bun run test/scripts/run-test.ts shared/locale/analytics-namespace.parity.test.ts` | **85 pass / 0 fail** (757 expect calls) |
| Nav suite | `bun run test/scripts/run-test.ts frontend/views/dashboard/nav/navItems.test.ts` | **26 pass / 0 fail** (144 expect calls) — ownership-exclusivity ("every nav label key owned by exactly ONE namespace", incl. the new `analytics` key: dashboard ✓ / handshakeCode ✗) and the audit→broadcasts adjacency pin stay green |
| Quality loop | `bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates` over ALL 15 touched files | **15/15 exit 0** (each includes per-file tsgo + oxlint; discovery printed `AGENTS.md` per file) |
| Types | `bun tsgo` | Clean — 0 errors (baseline floor held) |
| Format/lint | `bunx @biomejs/biome check` over the 6 primary new/edited files | Clean, no fixes applied |
| Sibling-parity regression sweep | `bun run test/scripts/run-test.ts shared/locale` (11 files) | 427 pass / **4 fail — ALL 4 PRE-EXISTING, NOT THIS TASK'S**: 3 × `adminUsers-namespace.parity.test.ts` (`adminUsers.directoryChips.childrenCount` is a pluralized function the older test's string/group walker rejects) + 1 × `sessions-namespace.parity.test.ts` (errors-registry placeholder-name walk). Proof of non-attribution: the adminUsers and errors leaf files have ZERO diff vs HEAD (`git diff --stat` over those paths is empty); the failing assertions never read any file this task touched. Out of 1.4 scope — flagged for the orchestrator (likely a DEV3-016-era drift; fixing means editing those two test files, which this task's file list forbids). |

## 6. Instruction verification (1.4.IV)

- **`shared/AGENTS.md` namespace-registration checklist (5 steps):** ① interface in `types/analytics/index.ts` ✓ ② en/ar implementations ✓ ③ export in `types/message.ts` (`Translations`) ✓ ④ "path mapping in `serverLegacy.ts`" — **N/A: `serverLegacy.ts` does not exist** (compile-time migration complete; `server.ts` resolves the whole bundle via `messagesByLocale`, no namespacePaths map — verified by full read) ⑤ "LocaleProvider translations in `app/[locale]/layout.tsx` if layout SSR" — **N/A: `frontend/providers/LocaleProvider.tsx` carries only `locale` in context (full read; no per-namespace registration surface)**. Both N/A items verified-in-tree and recorded rather than silently skipped.
- **`shared/AGENTS.md` "Adding New Namespaces"** (translation-cache rule): `defineNamespace` passed the stable id `"analytics.analytics"` ✓.
- **`shared/AGENTS.md` layer isolation + import convention:** zero imports from `@/frontend|backend|app` in shared files; all `@/`-aliased ✓. **Public import contract** ("add new canonical exports to the barrels when modules land; run `bun tsgo` after barrel changes"): `Analytics` reachable via `@/shared/locale` (namespaces barrel) — `bun tsgo` run after the barrel change ✓.
- **`shared/locale/AGENTS.md`:** `@/shared/locale/...` alias discipline ✓; leaf modules export one typed const, no logic (single sanctioned function leaf) ✓.
- **`.agents/instructions/frontend.instructions.md`:** `*Outlined` icon naming ✓; i18n rules (compile-time system, no hardcoded UI text, `useAppTranslation`/`getTranslations` shapes respected) ✓; bun command discipline ✓.

## 7. Semantic review (1.4.SR) — item by item

1. **No string-literal i18n access invented** — every product file uses typed property access; the only key literals are technical (`labelKey: "analytics"`, registry export names, the parity test's key inventory). The parity test's `Reflect.get` usage mirrors the sanctioned notifications-suite pattern (runtime belt over compile-typed maps).
2. **No `Translation` enum invented** — none; the handle/`defineNamespace` pattern is the only registration mechanism touched.
3. **`useAppTranslation(Analytics)` handle shape confirmed** — `useAppTranslation<TLabels>(handle: NamespaceHandle<TLabels>): TLabels` overload (`shared/locale/client/use-app-translation.ts:9`); `Analytics: NamespaceHandle<AnalyticsLabels>` ⇒ `const t = useAppTranslation(Analytics)` yields the full `AnalyticsLabels` property chain for `PlatformAnalyticsContainer` (Task 4.4). Server side: `getTranslations(locale).analyticsTranslations` (ONE-arg, property chain) per REQ-003.
4. **Nav item added once, no duplicates** — single entry; non-admin roles untouched; admin block order otherwise unchanged.
5. **Clean comments** — the one added nav comment states runtime truth (route 404s until the page ships); no plan/ticket artifacts introduced.
6. **No dead code** — every created symbol is wired (type→leaves→namespace→registry→bundle→test; `analytics` dashboard key→nav item→both locale bundles→nav tests); no unused imports (biome + sub-loop + tsgo all clean).

## 8. Carry-forward for Phase 4 (exact import handles)

- **Namespace handle:** `import { Analytics } from "@/shared/locale/namespaces/analytics";` → `useAppTranslation(Analytics)` returns `AnalyticsLabels`. Also re-exported via the public barrel `@/shared/locale` (registry id: `"analytics.analytics"`).
- **Labels type:** `import type { AnalyticsLabels } from "@/shared/locale/types/analytics";`
- **Server/metadata:** `getTranslations(locale)` from `@/shared/locale/server` → `.analyticsTranslations.metaTitle` / `.metaDescription` (Task 4.2 `generateMetadata`).
- **DashboardLabels nav key:** `analytics` (dashboard-bundle owned; resolves via `resolveNavItemLabel` from `DashboardLabels`; e.g. `Dashboard.getLabels(getTranslations(locale)).analytics`).
- **Nav entry (already landed):** admin block, position directly after `/admin/broadcasts` (which is pinned directly after `/audit`): `{ route: "/admin/analytics", labelKey: "analytics", Icon: InsightsOutlined }` — icon imported from `@mui/icons-material`.
- **Bundle slices:** `enMessages.analyticsTranslations === analyticsEn` (`@/shared/locale/en/analytics`), `arMessages.analyticsTranslations === analyticsAr` (`@/shared/locale/ar/analytics`) — identity pinned by the parity suite.

## 9. Environment + findings notes

- Sandbox fix (no repo change): the mandated runner resolves `~/.bun/bin/bun`, absent in this environment — symlinked `/home/z/.bun/bin/bun → /usr/local/bin/bun` so `run-test.ts`/sub-loop could execute.
- Pre-existing failure observation (§5, out of scope) is the only ⚠️-class note; no deferred-item row created (not introduced by this task, no downstream task owns it here — left for the orchestrator to route).
