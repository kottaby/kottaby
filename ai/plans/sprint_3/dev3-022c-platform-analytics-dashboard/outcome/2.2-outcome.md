# Task 2.2 Outcome — Journey B: Full Cast Observation (TEST-FIRST, RED)

**Feature:** `dev3-022c-platform-analytics-dashboard`
**Branch:** `feat/dev3-022c-platform-analytics-dashboard` (no commit made — working tree only)
**Date:** 2026-09-04
**Scope:** Journey B block of `test/workflows/admin/platform-analytics.journey.test.ts` (lines ~794–1030). No production files touched.

---

## 1. Intent

Prove the REQ-074 Journey-B EARS: WHEN any actor family commits platform activity THEN the admin's NEXT read reflects it exactly (`baseline + exact fixture delta`), currencies never merge, the governed student is excluded from the active counters, and WHEN a non-admin actor's own state changes THEN that actor remains structurally unable to read the aggregate (`ForbiddenError` every time) with zero side effects.

## 2. Fixture list (exact, ONE committing `db.transaction` in `beforeAll`)

| # | Fixture | Factory / actor call | Metric touched |
|---|---|---|---|
| 1 | Student actor | `provisionStudentActor(tx, { tracked })` | `users.studentsCount`, `recentlyActive24h`, `activeCount` |
| 2 | Certified teacher ONLINE | `provisionCertifiedTeacherActor` + `tx.update(teacher).set({ isOnline: true })` | `teachers.certifiedCount`, `onlineNowCount`, `users.teachersCount` |
| 3 | Certified teacher OFFLINE | `provisionCertifiedTeacherActor` (default `isApproved=true, isOnline=false`) | `teachers.certifiedCount` only |
| 4 | Parent actor | `provisionParentActor(tx, { tracked })` | `users.parentsCount` |
| 5 | Governed student | `createTestUser(tx, { role: "student", isDeleted: true, deletedAt: new Date() })` + `createTestStudent` | `deletedCount` +1; excluded from `activeCount`/`recentlyActive24h` (eligibility exclusion, not reader gating) |
| 6 | Plan + ACTIVE-window paid subscription | `createTestPlan` + `createTestSubscription(tx, studentActor.userId, plan.id)` (defaults: `active`, start=call time, `endDate=null`) | `subscriptions.total/active/activeInWindowNow` |
| 7 | One paid EGP payment TODAY | `createTestStudentPayment(tx, studentActor.userId, subscriptionId, { amount: "150.75", currency: "EGP" })` | EGP revenue row |
| 8 | One paid USD payment TODAY | `createTestStudentPayment(…, { amount: "49.99", currency: "USD" })` | USD revenue row (separate row — never merged) |
| 9 | Pending withdrawal | `createTestWallet(tx, onlineTeacher.userId)` + `createTestTeacherTransaction(tx, walletId, null, { type: Withdrawal, status: Pending })` | `health.pendingWithdrawals` |
| 10–14 | Five sessions (see mapping) | `createTestSession(tx, teacherId, studentId, {…})` | sessions section + trend |
| 15 | One report (rated 5) | `createTestSessionReport(tx, completedConfirmed.id)` | `ratings.sessionRatingsCount`, average |
| 16 | One evaluation (score 85) | `createTestEvaluation(tx, offlineTeacher.userId, onlineTeacher.userId, null)` | `ratings.evaluationScoresCount`, average |

## 3. The `sessions.today = +4` mapping (RECORDED — the chosen delta math)

Five committed sessions; **four are "today" (createdAt = fixture instant), the `cancelled` one is backdated `40 days` (constant `CANCELLED_SESSION_DAYS_AGO = 40`), which places it outside today, outside the ISO week, outside the month, AND outside the trailing-30-day trend window** (so it lifts `total` and the `cancelled` status counter but NEVER the window counters nor any trend bucket):

| Session | Status | Lifecycle overrides | Today? | Counter effect |
|---|---|---|---|---|
| completedConfirmed | `completed` | `startedAt/endedAt/confirmedByStudentAt/confirmedByTeacherAt = now` | yes | `today+1`, `completed+1` (confirmed → NOT awaiting) |
| completedUnconfirmed | `completed` | `startedAt/endedAt/confirmedByTeacherAt = now` (student confirmation NULL) | yes | `today+1`, `completed+1`, **`awaitingConfirmation+1`** |
| disputed | `disputed` | `disputeReason`, `disputedAt = now` | yes | `today+1`, `disputed+1`, `pendingDisputes+1` |
| scheduled | `scheduled` | defaults (created now) | yes | `today+1`, `scheduled+1` |
| cancelled | `cancelled` | `cancelReason`, **`createdAt = now − 40d`** | **NO** | `total+1`, `cancelled+1` only |

→ asserted: `sessions.today = baseline + 4`, `thisWeek/thisMonth = +4`, `total = +5`, `sessionTrendDaily` **last bucket +4** (the 40-day-old cancel lands in NO bucket of the 30-day window).

## 4. Asserted deltas (exact, over the baseline captured immediately BEFORE the fixture transaction)

- Sessions: `total+5`, `today+4`, `thisWeek+4`, `thisMonth+4`, `scheduled+1`, `completed+2`, `cancelled+1`, `disputed+1`, `awaitingConfirmation+1`, `started` unchanged.
- Health: `pendingDisputes+1`, `pendingWithdrawals+1`.
- Teachers: `certifiedCount+2`, `evaluatorCount` unchanged, `onlineNowCount+1` (online ⊆ certified).
- Subscriptions: `total+1`, `active+1`, `activeInWindowNow+1`; pending/expired/cancelled/suspended unchanged.
- Ratings: counts `+1/+1`; averages `toBeCloseTo` the 2-dp family means recomputed as `(baselineSum + fixture)/(baselineCount + 1)`.
- Revenue: **TWO separate currency rows** — expected rows = baseline rows with EGP/USD ascended or appended, exact decimal-string sums via integer minor-unit math (`toMinorUnits`/`formatMinorUnits` — never float), `paidPaymentsCount+1` each; plus explicit `filter(currency).toHaveLength(1)` pins for EGP and USD (never merged).
- Trends: `expectedSessionTrend(baseline, readDay, 4)`; `expectedRevenueTrend(baseline, readDay, {EGP: 150.75, USD: 49.99})` — full 30-bucket equality.
- Governed student exclusion: `totalCount+5`, `activeCount+4`, `recentlyActive24h+4`, `deletedCount+1`, role counters `+2/+2/+1`, `newThisWeekCount+5`, `adminsCount` unchanged.

## 5. Denial probes + purity

- AFTER the observation, the SAME student/teacher/parent actors call `PlatformAnalyticsService.getPlatformAnalytics(own userId, own locale)` directly → `ForbiddenError` (instance) with message containing `tErrors.forbidden` — cross-actor visibility: their own state changes buy them nothing (REAL role resolution, never patched).
- Row-count purity at each step: pre/post `snapshotObservedTables()` byte-identity (count + content digest per table — strictly stronger than row counts) + zero audit delta + zero notification delta.

## 6. RED evidence (verbatim — identical for all four journeys, same file)

```
test/workflows/admin/platform-analytics.journey.test.ts(104,42): error TS2307: Cannot find module '@/backend/services/admin/platform-analytics.service' or its corresponding type declarations.
```

Runner: `error: Cannot find module '@/backend/services/admin/platform-analytics.service' from '/home/z/my-project/test/workflows/admin/platform-analytics.journey.test.ts'` — `0 pass / 1 fail / 1 error`. Expected RED until Task 2.6.

## 7. Semantic review (2.2.SR)

1. Fixture deltas derive from committed rows only — every delta above maps 1:1 to a row created in the ONE fixture transaction; no hidden fixtures are shared across journeys without tracked ids (all ids land in the tracked arrays and are deleted in `afterAll`).
2. Denial probes execute REAL role resolution (actors hold real `users.role` + role-child rows); the governed-student fixture proves metric-level eligibility exclusion, not reader gating.
3. Boundary-sensitive deltas covered (2.2.TE): `awaitingConfirmation` flip (confirmed vs unconfirmed twin sessions), currency split (EGP ≠ USD rows), online/offline teacher split, backdated-outside-window cancelled session.
4. Immutability-aware fixtures: the EGP/USD payments and the pending withdrawal ride the RULING-1 teardown (delete FIRST under `withImmutabilityTriggersSuspended`); factories insert-only, so fixtures commit normally.
5. Wallet headroom not needed: the withdrawal fixture asserts only the counter, and the ledger factory deliberately does not move `wallet.balance` (documented in the factory JSDoc).

## 8. Instruction verification (2.2.IV)

Same rule set as 2.1 (journey rules 1–12, tests.instructions pre-existing-count + relative-timestamp + money-string rules) — all compliant; Biome clean; sub-loop exits 1 at tsgo on the sanctioned RED only (definitive pass deferred to Task 2.6).

## 9. Carry-forward

- Task 2.6 must implement `getRevenueStats` grouping (`ORDER BY currency`), the ACTIVE-window predicate, and the governance-aware user counters exactly as mirrored in `captureBaseline` — otherwise these assertions fail by design.
- The EGP/USD payment ids + withdrawal id + all session/report/evaluation/plan/subscription/wallet ids are tracked and hard-deleted in the shared `afterAll` (RULING-1 order).

*End of Task 2.2 outcome.*
