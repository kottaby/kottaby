# 6.3 Review Wave — Frontend (review-frontend) — Outcome

- **Plan:** DEV3-022c — Platform Analytics Dashboard
- **Branch under review:** `feat/dev3-022c-platform-analytics-dashboard` (HEAD at review time: `638b171` "docs(analytics): 5.3 E2E smoke — live populated dashboard verified")
- **Reviewer:** independent frontend review sub-agent (wave 6.3, Task ID `6-c`)
- **Mode:** READ-ONLY — no source files edited, no commits made, no tasks.md checkboxes flipped.
- **Requirements anchored:** REQ-062, REQ-063, REQ-064, REQ-065, REQ-066, REQ-067, REQ-068, REQ-075.

> ⚠️ **ENVIRONMENT INCIDENT (disclosed up front):** mid-review the sandbox workspace was
> replaced (repo swapped to an empty `Initial commit` on `main`; the branch and history
> vanished irrecoverably — `.git/objects` 60K, no packs, no reflog). All findings below
> were gathered **before** the swap, directly on the review branch, file-by-file. Items
> that could not be completed after the swap are listed in **§5 Blocked / not verified**.
> `bun tsgo` could **not** be run (no `package.json`/`tsconfig.json` exists in the
> swapped workspace; `bun` 1.3.14 present, `bun tsgo` → `error: Script not found "tsgo"`).

---

## 1. Verdict

**PASS WITH FINDINGS** — no blocking defect found on the reviewed surface. The shipped
implementation is faithful to REQ-062..068 and REQ-075: the document is a closed,
leaf-for-leaf complete, zero-argument selection; the cache registers exactly the eleven
embedded types; the page is a pure server shell with the exact guard signature; the
container's six-state machine (skeleton / populated / error+retry / denied / refreshing
with stale retention / stale+error) is correct and honestly null; styling is sx-only
with theme-token-only colors; the nav registration is a single ownership-clean entry;
the component tier is label-keyed and locale-parameterized over ar+en.

Four **minor** code findings + two **process observations** are logged (§4). None block
Phase 6.4; findings F-1..F-3 should be picked up before Phase 7.3 final gate. The
`bun tsgo` gate is **environment-blocked** and must be re-run on an intact checkout.

## 2. Scope covered (all read on-branch before the swap)

| # | Artifact | Evidence basis |
|---|----------|----------------|
| 1 | `frontend/graphql/sharedDocuments/admin/platform-analytics.documents.ts` | full read + leaf-for-leaf diff against committed SDL (`frontend/graphql/generated/schema.graphql`) |
| 2 | Barrels: `sharedDocuments/admin/index.ts`, `sharedDocuments/index.ts`, `views/admin/analytics/index.ts`, `views/admin/index.ts` | full reads + consumer grep |
| 3 | `frontend/providers/apollo/apolloCache.ts` | full read; entry inventory counted |
| 4 | `app/(dashboard)/admin/analytics/page.tsx` | full read + `withPageAuth` contract read |
| 5 | `frontend/views/admin/analytics/**` (13 files, 964 lines) | full reads (container, header, metrics grid, revenue card, metric rows, both trend charts, trends grid, states, skeletons, icon chip, display formats) |
| 6 | `frontend/views/dashboard/nav/navItems.ts` | full read of admin block + type surface |
| 7 | `test/ui/components/admin/PlatformAnalyticsContainer.test.tsx` (282 lines) | full read |
| 8 | `shared/locale/types/analytics/index.ts` + `en/analytics` + `ar/analytics` + parity test (presence) | reads / key spot-checks |
| 9 | Backend denial codes (`backend/services/admin/platform-analytics.service.ts:220-248`), `extractErrorCode` | grep confirmation |

## 3. Verified-clean matrix (scope assertions)

| Assertion (scope) | Result | Evidence |
|---|---|---|
| **Single-entry discipline / closed selection (REQ-062)** | ✅ | `adminPlatformAnalyticsQueryDocument: TypedDocumentNode<AdminPlatformAnalyticsQuery>` (codegen type, no inline literals), named operation `AdminPlatformAnalytics`, **zero arguments** (SDL: `adminPlatformAnalytics: PlatformAnalytics!` — no args). Selection is leaf-for-leaf **complete and closed** vs SDL: users 11/11 leaves, sessions 10/10, subscriptions 7/7, teachers 3/3, ratings 4/4 (both honestly-nullable `Float` averages selected), health 2/2, `PlatformAnalyticsCurrencyRevenue` 4/4, both trend points 2/2 & 3/3, `generatedAt`. **No `id` anywhere; no extra fields; no fragments/aliases.** Container imports via the `sharedDocuments` barrel — one consumption path. |
| **Barrel wiring** | ✅ | `admin/index.ts` re-exports `./platform-analytics.documents` alongside admin-users; top barrel `export * from "./admin"` unchanged in shape. View barrel excludes the container deliberately (documented anti-cycle posture, mirrors admin/users). |
| **Eleven `keyFields:false` + 17-entry pin (REQ-062)** | ✅ | `apolloCache.ts` registers exactly: `PlatformAnalytics`, `…CurrencyRevenue`, `…Health`, `…Ratings`, `…Revenue`, `…RevenueTrendPoint`, `…SessionTrendPoint`, `…Sessions`, `…Subscriptions`, `…Teachers`, `…Users` (11, alphabetical, all `keyFields: false`) + 6 pre-existing policies (`AdminDashboardScheduleResult`, `AdminNoteInfo`, `HealthCheck`, `NotificationListPage`, `HandshakeCodeLookup`, `OnlineMeetingInfo`) = **17 typePolicies entries**. Header comment accurately states the inline-under-`Query.adminPlatformAnalytics` posture. |
| **Page guard plumbing (REQ-063)** | ✅ | Server Component (`generateMetadata` present, no `"use client"`), `await withPageAuth({ roles: [UserRole.Admin], redirectTo: "/admin/analytics" })`, zero data fetching in the shell (client owns the query per REQ-062). `withPageAuth` semantics verified: anonymous → `/login?redirect=…`, role-mismatch → `roleDashboardPath(ctx.role)` (bare `/dashboard` never targeted). |
| **Metadata i18n (REQ-063)** | ✅ | `generateMetadata` resolves `getLocaleFromCookie()` → `getTranslations(locale).analyticsTranslations` → `metaTitle`/`metaDescription`; both keys exist in the `AnalyticsLabels` type and in en+ar bundles. |
| **Polling / stale retention (REQ-064)** | ✅ | `useQuery(…, { variables: {}, pollInterval: 120_000, notifyOnNetworkStatusChange: true })`; `POLL_INTERVAL_MS = 120_000` constant; no `useLazyQuery`. Refresh chip = `loading && snapshot !== undefined`; manual refresh + retry both ride `refetch()`; error-after-snapshot renders the inline alert **above** the retained snapshot (honest staleness). `denied = UNAUTHORIZED \|\| FORBIDDEN` matches the backend denial codes actually emitted (service 220–248). |
| **Error / denied / skeleton states (REQ-064/075b)** | ✅ | Six documented states implemented as described: initial skeleton (zero figures, no stamp), denied (refresh hidden, localized notice), load-error (canonical copy + retry CTA, raw server text never rendered), refreshing (stale kept, disabled button + spinner), stale+error, populated. Skeletons mirror the populated 4/2/1 grid + two 220px chart blocks (no layout shift). |
| **MUI v9 sx-only purity / theme tokens (REQ-064/067)** | ✅ | All styling via `sx` (incl. `sx={(theme) => …}` callbacks); recharts `stroke`/`fill`/`contentStyle` resolved from `useTheme()` (`divider`, `text.secondary`, `primary.main`, `secondary.main`, `success.main`, `background.paper`) — **zero hardcoded hex/rgb** in the surface. `*Outlined` icon family throughout. No `FormEvent`. |
| **RTL logical spacing (REQ-064)** | ✅ | Logical properties only: `inlineSize`, `blockSize`, `minBlockSize: 44` / `minInlineSize: 44` on all actionable controls (refresh, retry, icon chip), `borderInlineStart*` accents, `marginBlockStart/End`. No physical `ml/mr/pl/pr` anywhere in the surface. |
| **Honest null / empty states** | ✅ | `formatRatingDisplay(null) → "—"` (never a coerced 0.00); `gatewayRevenueByCurrency: []` renders `labels.noRevenueYet` while the offline-activations honesty counter persists below its divider (offline excluded from gateway rows — REQ-015 posture preserved; **no cross-currency total is ever rendered**). |
| **i18n handle usage (REQ-066)** | ✅ with finding F-1 | Views consume `useAppTranslation(Analytics)` and pass the `AnalyticsLabels` object; every label/CTA/caption is a property access (incl. function-leaf `lastUpdatedLabel(preFormatted)`). Timestamps pre-formatted via `Intl.DateTimeFormat` (no `toISOString()` in views — REQ-068 discipline held; trend axis uses `timeZone: "UTC"`). Exception logged as F-1. |
| **Nav single admin entry (REQ-065)** | ✅ | Exactly one `{ route: "/admin/analytics", labelKey: "analytics", Icon: AnalyticsIcon (InsightsOutlined) }` in the Admin list; no duplicate admin-analytics entry anywhere; `analytics` key present in `DashboardLabels` + en (`"Analytics"`) + ar (`"تحليلات"`). |
| **Test tiers (REQ-075b)** | ✅ with nits | Component suite runs both locales (`["ar","en"]`); all assertions **label-keyed** via `Analytics.getLabels(getTranslations(locale))` and **derived** through the real formatters (`formatCountDisplay`/`formatMoneyDisplay`/`formatRatingDisplay`/`formatInstantDisplay`) — no eyeballed UI strings (only fixture data like `"USD"`). Population gate is a metric label (not the header that exists during loading). Mocks use `variables: {}` (deterministic request identity). Covers loading / populated / error+retry-recovery / FORBIDDEN denial (refresh hidden) / refreshing stale-retention / empty revenue. |

## 4. Findings

| ID | Severity | REQ | Finding | Suggested resolution |
|----|----------|-----|---------|----------------------|
| F-1 | Minor | REQ-066 | `SessionTrendChart.tsx` + `RevenueTrendChart.tsx` hardcode bilingual chart `aria-label`s (`locale === "ar" ? "مخطط …" : "Daily …"`) — these a11y strings bypass the `analytics` namespace (no aria keys exist in `AnalyticsLabels`), contradicting the surface's "every user-facing string via the Analytics handle" posture and the parity-tested translation flow. | Add `sessionTrendAriaLabel` / `revenueTrendAriaLabel` (or `chartAriaLabel(category)`) leaves to `AnalyticsLabels` + en/ar bundles + parity list; consume via property access. |
| F-2 | Minor | REQ-067 | Neither chart's `YAxis` carries an i18n-aware tick formatter — numeric ticks render in recharts' default (ASCII) numerals for both locales, while REQ-067 requires "numeric tick labels formatted via … plain locale numerals". | Add a shared `formatAxisNumber` (locale `Intl.NumberFormat`) as `tickFormatter` on both `YAxis` (and consider `sessionsSeriesLabel`/`amountAxisLabel` which are defined but currently unused by the charts). |
| F-3 | Minor | REQ-064 | `PlatformAnalyticsMetricsGridProps.refreshing` is declared and passed by the container but never destructured/consumed — dead prop API. | Consume it (e.g. dim/skeleton shimmer) or drop it from the interface and the call site. |
| F-4 | Minor (doc accuracy) | REQ-015 (spirit) | `RevenueTrendChart` docstring claims "tooltips keep the raw strings", but only the x-label is formatted; the per-currency tooltip **values** render `Number(point.amount)` — the money-string honesty claim in the comment is not literally true at runtime. | Either pass a `Tooltip` value formatter that re-renders the exact decimal strings, or correct the docstring to "bar heights only; tooltip values are numeric renderings of the exact decimals". |
| O-1 | Observation (process) | REQ-062 | `frontend/graphql/AGENTS.md`'s embedded-type list does **not** yet contain the eleven `PlatformAnalytics*` entries. REQ-062 bundles this with the cache registration, while the plan schedules AGENTS.md propagation in Task 7.2. | Must land no later than Task 7.2 (before the 7.3 gate); no code impact. |
| O-2 | Observation | REQ-064 | Currency-series palette cycles 3 theme colors (`index % 3`): a >3-currency snapshot would repeat colors across Legend entries (currently impossible-ish but unpinned). Also the analytics view barrel is not re-exported from `views/admin/index.ts` and no consumer imports the barrel (all consumers use direct sub-paths; container deliberately by-path) — acceptable, but the barrel's raison d'être is currently test-only. | Optional: assert a max-currency bound at the wire tier or extend the palette; consider exporting the analytics barrel from the admin barrel for symmetry. |
| N-1 | Nit | REQ-075b | In the "empty revenue" test, a first `renderContainer` is immediately `cleanup()`-ed without assertions (dead render). | Remove the dead first render. |
| N-2 | Nit | REQ-064 | A mid-session **denial** with a retained snapshot surfaces the generic load-error copy (not the denied copy) — consistent with the container's documented state table (state 5) and a governance edge the route guard should prevent; noted for awareness only. | Optional: branch on `denied && snapshot !== undefined` for denied-specific stale copy. |

**No ❌ items** — nothing found rises to a plan-blocking defect.

## 5. Blocked / not verified (environment swap)

| Item | Status |
|------|--------|
| `bun tsgo` gate | **NOT RUN** — workspace replaced mid-review; current directory has no `package.json`/`tsconfig.json`. Must be re-run on an intact checkout of `feat/dev3-022c-platform-analytics-dashboard`. (Branch-level confidence: the 5.1/5.2 outcome records the gate green at `9866c53`; only doc commits followed.) |
| `frontend/providers/apollo/apolloCache.test.ts` pinned-inventory contents (REQ-075c) | Not read pre-swap — existence implied by plan 4.2 outcomes; re-verify the 17-entry pin assertion. |
| `frontend/views/dashboard/nav/navItems.test.ts` ownership assertions (REQ-065) | Not read pre-swap; re-verify the analytics key resolves on exactly one bundle. |
| `frontend/graphql/sharedDocuments/admin/platform-analytics.documents.test.ts` contents (REQ-075a) | File confirmed present (10.4 KB) on-branch; contents not audited pre-swap. |
| Recharts dynamic-import precedent comparison (REQ-067 posture) | Not completed; charts are statically imported inside `"use client"` modules (commit `6964d76` recorded this as a deliberate test-harness decision). |

## 6. Next actions

1. Re-run `bun tsgo` + `bun run biome:check` on an intact checkout (this wave's gate, environment-blocked here).
2. Resolve F-1..F-4 (small, contained: 2 locale keys + 2 formatters, 1 prop, 1 docstring/tooltip formatter).
3. Confirm the three REQ-075 tier files listed in §5 during 6.4's sweep (or a follow-up frontend spot-check).
4. Carry O-1 into Task 7.2 (AGENTS.md embedded-type list) so the 7.3 gate sees it green.
