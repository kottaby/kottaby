# Task 4.1 Outcome — Platform Analytics GraphQL document (document + test + barrel)

**Feature:** `dev3-022c-platform-analytics-dashboard`
**Branch note:** repo checked out on `main` at `e419280` (byte-identical to `feat/dev3-022c-platform-analytics-dashboard`; no branch switch performed — git-mutating commands forbidden for this subagent). The orchestrator commits the change set including the regenerated codegen artifact.
**Mode:** one shared document + companion contract test + one barrel line + codegen regeneration; no commit made.

---

## 1. Summary

- Created `frontend/graphql/sharedDocuments/admin/platform-analytics.documents.ts` — `adminPlatformAnalyticsQueryDocument: TypedDocumentNode<AdminPlatformAnalyticsQuery> = gql\`…\`` with the NAMED operation `AdminPlatformAnalytics`, ZERO variables (second `TypedDocumentNode` type parameter omitted — the no-arg convention of `sharedDocuments/AGENTS.md`, matching `myUnreadNotificationCountQueryDocument`), and the FULL selection set EXACTLY per plan §5.4: the `generatedAt` coherence stamp, all seven section aggregates (`users` 11 counters incl. `recentlyActive24h`, `sessions` 10, `revenue` + `gatewayRevenueByCurrency` 4, `subscriptions` 7, `teachers` 3, `ratings` 4, `health` 2), and both 30-day trend series (`sessionTrendDaily` 2, `revenueTrendDaily` 3). NO `id` selections anywhere (aggregate anonymity — none of the eleven `PlatformAnalytics*` types carries one; selecting it would fail validation). Zero fragments, zero arguments (closed input surface). Field order copied verbatim from the plan §5.4 block (contract source order, not alphabetical).
- The `AdminPlatformAnalyticsQuery` operation type did NOT exist in `frontend/graphql/generated/gql/graphql.ts` pre-change (Task 3.2 outcome Deviations §2: the operation/types module only emits types for operations selected by a frontend document — none existed). Ran `bun run generate:gqlSchema && bun codegen` after creating the document: `frontend/graphql/generated/gql/graphql.ts` regenerated (+28 lines — the ONLY regenerated delta; `schema.graphql` rewritten byte-identical, git-diff EMPTY on it), now exporting `AdminPlatformAnalyticsQuery`, `AdminPlatformAnalyticsQueryVariables = Exact<{ [key: string]: never }>`, the compact extracted types `AdminPlatformAnalyticsQuery_adminPlatformAnalytics[_<section>[_…]]`, and the generated `AdminPlatformAnalyticsDocument` node. Codegen validated the document against the committed SDL (`noSilentErrors` — an unknown leaf would have failed generation), which independently proves the leaf-for-leaf match.
- Updated `frontend/graphql/sharedDocuments/admin/index.ts` with one appended `export * from "./platform-analytics.documents";` line (alphabetical position between `audit-trail` and `teacher-certification`; reaches the top-level barrel transitively via the existing `export * from "./admin";`).
- Created `frontend/graphql/sharedDocuments/admin/platform-analytics.documents.test.ts` mirroring the `notification.documents.test.ts` precedent — 11 tests / 140 expect calls, zero server/DB/network: named-operation pin (single named query, `AdminPlatformAnalytics`, zero variables), no-fragments pin (exactly one OperationDefinition), zero-argument pin on the root field (closed input surface), full closed-selection pins (root exact order = stamp + 7 sections + 2 trends; per-node exact leaf arrays for all 11 object nodes; `generatedAt` present as a bare scalar; every contracted scalar leaf bare; object-selection set EXACTLY the 11 documented nodes — no undeclared nesting), NO-`id`-anywhere recursive scan (aggregate anonymity), barrel-identity re-export pins (admin sub-barrel AND top-level barrel both `toBe` the deep import), and the `TypedDocumentNode` compile-proof-by-assignment against the generated `AdminPlatformAnalyticsQuery`.
- Scope hygiene: `frontend/providers/apollo/apolloCache.ts` + `apolloCache.test.ts` show as modified in the shared working tree — that is Task 4.2's parallel work, NOT touched by this task (git-diff verified zero edits from this subagent outside the four files listed in §2).

## 2. Files created / modified / NOT modified

| File | Action | Notes |
|---|---|---|
| `frontend/graphql/sharedDocuments/admin/platform-analytics.documents.ts` | CREATE | one export: `adminPlatformAnalyticsQueryDocument: TypedDocumentNode<AdminPlatformAnalyticsQuery>`; imports ONLY `gql` + `type TypedDocumentNode` from `@apollo/client` and `type AdminPlatformAnalyticsQuery` from the generated `graphql.ts` |
| `frontend/graphql/sharedDocuments/admin/platform-analytics.documents.test.ts` | CREATE | 11 tests / 140 expect calls; AST-inspection only (graphql kind guards, no unsafe assertions) |
| `frontend/graphql/sharedDocuments/admin/index.ts` | MODIFY | +1 line: `export * from "./platform-analytics.documents";` |
| `frontend/graphql/generated/gql/graphql.ts` | REGENERATED (+28 lines) | `AdminPlatformAnalyticsQuery` + `…Variables` + compact extracted types + `AdminPlatformAnalyticsDocument` |

NOT modified (read-only by design): `frontend/graphql/generated/schema.graphql` (regenerated byte-identical — git-diff EMPTY), `frontend/graphql/sharedDocuments/index.ts` (admin barrel already re-exported), `frontend/graphql/sharedDocuments/documents.contract.test.ts` (curated-table suite — not extended: its table never enumerated every domain, and the notification/broadcast/audit-trail/teacher-certification documents did not enroll either; this suite owns the auth/session contract rows), `frontend/providers/apollo/apolloCache.ts` + `apolloCache.test.ts` (Task 4.2's scope — the eleven `keyFields: false` registrations; observed as parallel in-flight edits, untouched here), `frontend/graphql/AGENTS.md` embedded-type list (Task 4.2's bullet), every backend file, `tasks.md`.

## 3. Verification commands + results

| Command | Result |
|---|---|
| `bun run scripts/health/sub-loop.ts frontend/graphql/sharedDocuments/admin/platform-analytics.documents.ts --lifecycle duplicates` | **exit 0** — tsgo ✅, oxlint ✅, biome:check ✅, lint:type-aware ✅, check:duplicates ✅ |
| `bun run scripts/health/sub-loop.ts frontend/graphql/sharedDocuments/admin/platform-analytics.documents.test.ts --lifecycle duplicates` | **exit 0** (first run flagged 3 oxlint warnings — two `no-map-spread` in the AST-path collectors, one `prefer-set-has`; fixed in-file with `[path].concat(…)` + a `Set` membership check, re-run green) |
| `bun run scripts/health/sub-loop.ts frontend/graphql/sharedDocuments/admin/index.ts --lifecycle duplicates` | **exit 0** — all five gates ✅ |
| `bun run test/scripts/run-test.ts frontend/graphql/sharedDocuments/admin/platform-analytics.documents.test.ts` | **11 pass / 0 fail** (140 expect calls) |
| `bunx tsgo -b --noEmit 2>&1 \| grep -c "error TS"` | **0** (full-repo type check) |
| `bun run generate:gqlSchema && bun codegen` | exit 0 both; `gql/graphql.ts` +28 lines (the analytics operation types — first materialization, per the Task 3.2 carry-forward); `schema.graphql` byte-identical |
| `bun run test/scripts/run-test.ts frontend/graphql/sharedDocuments/documents.contract.test.ts` | **20 pass / 0 fail** (regression — untouched) |
| `bun run test/scripts/run-test.ts frontend/graphql/sharedDocuments/notifications/notification.documents.test.ts` | **12 pass / 0 fail** (regression — the mirrored precedent) |
| `bun run test/scripts/run-test.ts frontend/graphql/sharedDocuments/billing/plan-catalog.documents.test.ts` | **5 pass / 0 fail** (regression) |
| `bun run test/scripts/run-test.ts backend/graphql/test/plan-catalog.schema.test.ts` | **5 pass / 0 fail** (committed-vs-live SDL byte-equality still green after regeneration) |
| `git status --porcelain` | M `gql/graphql.ts`, M `admin/index.ts`, ?? the two new files (+ parallel Task 4.2 edits to `apolloCache.ts`/`apolloCache.test.ts` NOT from this task; + pre-existing `tmp-scratch/`) |

## 4. Semantic review — item-by-item

1. **Document ↔ generated SDL leaf-for-leaf** — cross-checked field-by-field against `frontend/graphql/generated/schema.graphql:502-591`: root `PlatformAnalytics` exposes exactly `generatedAt` + `health`/`ratings`/`revenue`/`revenueTrendDaily`/`sessionTrendDaily`/`sessions`/`subscriptions`/`teachers`/`users` — the document selects all ten, nothing else; `PlatformAnalyticsUsers` 11 counters (incl. `recentlyActive24h`), `…Sessions` 10, `…Revenue` + `…CurrencyRevenue` 1+4, `…Subscriptions` 7, `…Teachers` 3, `…Ratings` 4, `…Health` 2, `…SessionTrendPoint` 2, `…RevenueTrendPoint` 3 — all present, zero extras, zero omissions. Independent machine proof: codegen (`noSilentErrors`) validated every leaf against the SDL, and the test's 11-object-node/exact-leaf-array pins re-prove it at the AST tier.
2. **camelCase naming consistent with codegen** — every selected leaf is the SDL's camelCase wire name verbatim; the generated `AdminPlatformAnalyticsQuery_adminPlatformAnalytics_*` extracted types mirror the document shape one-to-one (e.g. `recentlyActive24h: number`, `amount: string`); no renaming, no mapping layer.
3. **Closed document — no fragments beyond the contract** — the document is a single anonymous-free named operation, ZERO `FragmentDefinition`s, ZERO arguments on the root field, ZERO variables (the generated variables type is `Exact<{ [key: string]: never }>`); the test pins each of these structurally, so client steering is structurally impossible (4.1.SEC).
4. **`id` discipline — the sharedDocuments `id`-requirement rule consciously NOT applicable** — the rule targets normalizable entity types so Apollo can auto-update cache entries; every type in this subtree is an aggregate/embedded value object with NO `id` in the SDL (Task 3.1 outcome §4.2: "the Apollo `id`-exposure rule is consciously NOT applicable"). Selecting `id` would be a validation error (the Task 3.3 wire matrix already proves id selections fail validation); the test's recursive NO-`id` scan pins the absence. Cache-normalization opt-out (`keyFields: false`) lands in Task 4.2.
5. **`TypedDocumentNode` typing per convention** — `adminPlatformAnalyticsQueryDocument` (const name per the `{entityName}QueryDocument` table) typed `TypedDocumentNode<AdminPlatformAnalyticsQuery>` with the second parameter omitted (no-arg query rule); compile-proof-by-assignment in the test; imports exactly `import { gql, type TypedDocumentNode } from "@apollo/client"` + `import type { … } from "@/frontend/graphql/generated/gql/graphql"` (never `@apollo/client/core`, never inline type literals, never indexed-access workarounds).
6. **Consumer import conventions honored** — document exported through the admin sub-barrel and the top-level barrel; barrel-identity (`toBe`) pinned for BOTH paths; no consumer/view changed in this task (the container is Task 4.4).
7. **Clean comments — ZERO plan-artifact refs** — strict grep (`REQ-[0-9]|Task [0-9]|tasks\.md|specs\.md|plan\.md|RULING|Phase [0-9]|§`) over all three touched source files = NO MATCHES; headers state domain contracts only (aggregate anonymity, zero-variable closed surface, decimal-string money, useQuery-not-useLazyQuery).
8. **Instruction verification** — `frontend/graphql/AGENTS.md` + `frontend/graphql/sharedDocuments/AGENTS.md` + `frontend/AGENTS.md` + `.agents/instructions/frontend.instructions.md`: codegen types only ✅; naming tables ✅; `@apollo/client` import rule ✅; no `useLazyQuery` anywhere in the documents layer ✅; sub-loop discovery additionally surfaced `.github/instructions/frontend.instructions.md` + `.agents/instructions/tests.instructions.md` (test file) — no rule in them conflicts (no MUI/React surface in a document/test file). The `id`-requirement deviation is documented in item 4 above.
9. **Scope boundary** — four paths (two CREATE, one +1-line barrel edit, one regenerated artifact); git-diff verified; `schema.graphql` diff EMPTY; everything else untouched by this task.

## 5. Deviations / observations

1. **`documents.contract.test.ts` NOT extended** — the curated contract table there predates every post-auth domain (notifications, broadcast, audit-trail, teacher-certification were never enrolled); enrolling the analytics document is optional duplication of this task's dedicated suite. No reconcile was stale to repair (suite green pre- and post-change).
2. **oxlint fixed two test-file patterns on first gate run** — the recursive AST collectors initially used spread-inside-`flatMap` (`no-map-spread`) and the bare-leaf guard used an array `.includes` (`prefer-set-has`); both fixed in-file (`concat` semantics + `Set.has`), no suppressions, second gate run exit 0.
3. **Parallel Task 4.2 edits visible in the shared working tree** — `apolloCache.ts`/`apolloCache.test.ts` modifications present but NOT authored by this subagent; recorded so the orchestrator attributes them correctly and no one "reverts" them as out-of-scope drift.
4. **`generate:gqlSchema` run alongside `bun codegen`** (sharedDocuments/AGENTS.md mandates both) — proved byte-identical for `schema.graphql`, keeping the committed-artifact equality test green and confirming zero backend drift.

## 6. Carry-forward for Task 4.2 / 4.4

- **Exact export name (import from `@/frontend/graphql/sharedDocuments` or the deep path `…/sharedDocuments/admin/platform-analytics.documents`):** `adminPlatformAnalyticsQueryDocument` — typed `TypedDocumentNode<AdminPlatformAnalyticsQuery>`; Task 4.4 consumes it as `useQuery(adminPlatformAnalyticsQueryDocument, { pollInterval: 120_000, notifyOnNetworkStatusChange: true })`.
- **Operation type name:** `AdminPlatformAnalyticsQuery` (generated in `frontend/graphql/generated/gql/graphql.ts`, now committed-shape) — zero variables (`AdminPlatformAnalyticsQueryVariables = Exact<{ [key: string]: never }>` exists but the document convention omits the second type parameter). Extracted section types follow `AdminPlatformAnalyticsQuery_adminPlatformAnalytics[_<section>]` (e.g. `AdminPlatformAnalyticsQuery_adminPlatformAnalytics_revenue_gatewayRevenueByCurrency`) for per-section component props.
- **Operation name (wire):** `AdminPlatformAnalytics` — pinned by the test; Apollo cache writes land under the eleven embedded types with NO `id` (Task 4.2 must register all eleven with `keyFields: false` BEFORE the first live write to avoid "Cache data may be lost" warnings).
- **Data-shape facts for the container:** `generatedAt`/`bucketStart` arrive as strings (DateTime scalar → codegen `string`); money leaves (`totalAmount`, `last30DaysAmount`, `amount`) are exact decimal STRINGS; the only nullable members are `ratings.averageSessionRating` and `ratings.averageEvaluationScore` (`number | null`); both trend fields are arrays (`Array<…>`).

*End of Task 4.1 outcome.*
