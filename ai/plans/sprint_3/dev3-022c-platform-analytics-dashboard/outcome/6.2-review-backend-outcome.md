# 6.2 Review — Backend (independent, READ-ONLY)

Agent: review-backend (subagent) · Branch `feat/dev3-022c-platform-analytics-dashboard` @ `27e259a` (verified before AND after review — never flipped).
Scope: plan §6.2 — repository, service, GraphQL query + Pothos, D13 baselines, backend/journey test honesty. Rebuilt tree (pre-wipe 6.x outcomes reference the LOST tree; this review judges the REBUILT code).

## Scope reviewed
- `backend/db/repo/admin/platform-analytics.repository.ts` (491 L, read fully)
- `backend/services/admin/platform-analytics.service.ts` (329 L, read fully)
- `backend/graphql/query/admin/platform-analytics.query.ts` + `backend/graphql/pothos/admin/platform-analytics.pothos.ts` (read fully)
- `backend/types/admin/platform-analytics.types.ts`, `backend/lib/db/with-transaction.ts`, `backend/enum/{billing,scheduling}/*` (read)
- D13: `backend/graphql/test/sdl-static-assertions.test.ts`, `schema-surface.test.ts` pins vs `frontend/graphql/generated/schema.graphql`
- Tests: repo / service / wire-matrix / journey suites (read + re-run)

## Per-checklist verdicts
1. **Repository** — ✓
   - Set-oriented: every counter is ONE single-row `count(*) filter (where …)::int` aggregate; trends are 2 sparse grouped reads; no N+1 anywhere.
   - Param hygiene: all values (cutoffs, `now`, enum members) bound via drizzle `sql\`\`` parameters; `eq`/`inArray` helpers for enum predicates; ZERO `sql.raw`/concatenated SQL/LIKE surface (grep-clean).
   - tx discipline: every method takes `tx?: DBTransaction` optional-LAST; `(tx ?? db)` branch; non-tx else-branch has dedicated fixture-free coverage (repo test :861).
   - UTC math: `utcDayStart`/`isoWeekStart`/`utcMonthStart` + `UTC_TIMESTAMP_DECODER` on BOTH `date_trunc('day',…)` projections (repo :263, :314); no SQL `now()` — the captured `now` is bound.
   - Enum VALUE imports: plain TS string enums (`SessionStatus.Scheduled` = `"scheduled"` etc.) — member VALUES bound, not reverse-mapped wrappers.
   - NULL-safe governance: `coalesce(users.isDeleted/suspended/isBlocked, false) = false` (:178-180), evaluations soft-delete `coalesce(isDeleted,false) = false` (:457); activeInWindowNow expiry oracle (:369).
2. **Service** — ✓
   - Denial order malformed → absent → non-admin → **deleted → blocked → suspended**, each throwing + exactly ONE `logDomainError` ({code, entity:"users", entityId, locale} only); malformed-id rejected BEFORE any DB access; aggregate reads all post-gate (Tier-4 zero-call spy proof).
   - Single `now` captured once inside the snapshot (service :261), shared by all 6 windowed reads AND `generatedAt` (test asserts `toBe` identity).
   - ONE transaction: `withTransaction(outerTx, …)` (savepoint-on-outer / top-level) with a single `Promise.all` of all 11 reads over the SAME `tx` incl. reused `AdminUserRepository.getStats(tx)` (REQ-002 reuse kept unmodified).
   - Silent happy path: zero logs/writes/audit by construction (pinned by row-delta + zero-log tests); no try/catch swallowing (injected repo error propagates by identity); zero shared mutable module state (pure helpers + consts only).
3. **GraphQL** — ✓ query: `$all { authenticated, role: [UserRole.Admin] }` conjunction, pre-resolver `ctx.user` check (localized `UnauthorizedError` via `ctx.t`), ZERO args, no try/catch, thin resolver passing only `ctx.user.id` + `ctx.locale`. Pothos: exactly 11 objectRefs, no `id` fields, `DateTime` scalar for `generatedAt` + both `bucketStart`s, nullable `Float` ONLY on the two rating averages, all types imported from the canonical types file (no local types), no `toISOString` anywhere (grep-clean across all four files).
4. **D13 baselines** — ✓ spot-checked 3 pins against live SDL: `adminPlatformAnalytics: PlatformAnalytics!` zero args (schema.graphql:459); `PlatformAnalyticsRatings` = `Float`/`Float` nullable + `Int!` counts; `PlatformAnalyticsRevenueTrendPoint` = `DateTime!`/`String!`/`String!`. `FROZEN_QUERY_FIELDS` + schema-surface type-delta include the analytics family (11 types + `DateTime`) — pins match the live schema.
5. **Test honesty** — ✓ repo suite has ZERO spies/mocks (real PG, factory fixtures, delta oracles, BigInt cents math, SQLSTATE 23514 CHECK probes, shifted-`now` reclassification proving no SQL `now()`); service Tier-1 denials hit the REAL stack with real actor rows (spies only count logs — silenced, asserted, restored in `finally`); wire matrix runs the real authScopes engine + real resolver + real service/repo (denial spies assert `toHaveLength(0)` pre-resolver BEFORE restore); journeys use committed real-role actors, no scope stubbing, `probeDenial` snapshots calls BEFORE `mockRestore`; zero `expect(...).rejects.toThrow()`; zero fake-oracle drift (oracles are independent raw-SQL/BigInt, never routed through the repo under test).

## Findings
- **MINOR F-1** — `backend/services/admin/platform-analytics.service.ts:90-92` (`trendBucketKey`) re-projects bucket instants through LOCAL calendar fields. Correct for the live path (node-postgres delivers naive timestamps as LOCAL-parsed `Date`s → repo decoder's `instanceof Date` identity branch → local projection recovers the stored day, valid in ANY server TZ), but for the repo decoder's raw-TEXT fallback (`platform-analytics.repository.ts:79-95` already emits a true UTC-midnight instant) a second local projection under `TZ≠UTC` would shift trend keys back one day. The two compensation strategies only compose under "driver-Date OR TZ=UTC"; docstrings disclose the UTC-runner assumption. Latent defensive-path inconsistency only — no live impact on the pg driver / UTC deployments; suites+parity all green.
- **INFO O-1** — Governance re-check (`UserRepository.findById(actorId, outerTx)`) runs outside the snapshot tx when `outerTx` is undefined: an admin governed between gate and snapshot could see one last read. Inherent TOCTOU for a read model; D8 explicitly accepts the service-tier backstop.
- **INFO O-2** — Journey file uses module-scope `let` suite state (`bundle`, governed-admin ids, baselines) — sanctioned test-scope pattern per `test/workflows/AGENTS.md`, not the REQ-045 production violation.
- **INFO O-3** — Wire Probe 2 hardcodes `wiredContext(1, role)` (user id 1, row may not exist) — harmless: denial is pre-resolver and the resolver-spy `toHaveLength(0)` assertion proves no service call.

## Suite re-run evidence (sanctioned runner, `DATABASE_URL=…kottaby_test`)
| Suite | Result |
|---|---|
| repo `platform-analytics.repository.test.ts` | 23 pass / 0 fail (241 expect) |
| service `platform-analytics.service.test.ts` | 12 pass / 0 fail (266 expect) |
| wire `platform-analytics.query.test.ts` | 10 pass / 0 fail (168 expect) |
| journey `platform-analytics.journey.test.ts` | 10 pass / 0 fail (563 expect) |
| `sdl-static-assertions.test.ts` | 24 pass / 0 fail (105 expect) |
| `schema-surface.test.ts` | 33 pass / 0 fail (189 expect) |

All six match the Phase-5 baseline counts exactly.

## Final verdict
**CLEAN** — no BLOCKING, no MAJOR. One MINOR (F-1, disclosed-limits defensive-path note — recommend a one-line comment alignment or normalizing `trendBucketKey` to the decoder's UTC contract if a text-delivering driver is ever adopted; no resolution required for this wave). REQ-010..REQ-054 (as applicable to this surface), REQ-061, REQ-070..REQ-074 verified: 6.2 OUT.
