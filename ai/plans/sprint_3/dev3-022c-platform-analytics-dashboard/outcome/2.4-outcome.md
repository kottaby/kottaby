# Task 2.4 Outcome — Journey D: Denial & Purity Matrix (TEST-FIRST, RED)

**Feature:** `dev3-022c-platform-analytics-dashboard`
**Branch:** `feat/dev3-022c-platform-analytics-dashboard` (no commit made — working tree only)
**Date:** 2026-09-04
**Scope:** Journey D block of `test/workflows/admin/platform-analytics.journey.test.ts` (lines ~1127–1229) + the shared suite `afterAll` teardown (lines ~647–715). No production files touched.

---

## 1. Intent

Prove the REQ-074 Journey-D EARS + REQ-031/032/054 (as amended by RULING 2) + REQ-042 read purity: every denial tier is rejected with the canonical localized message in deterministic order, every denial and both happy reads leave EVERY observed table byte-identical with zero `audit_logs` and zero notifications, and the suite tears down with zero residue.

## 2. Denial matrix (exact)

Cast: FOUR governed admins provisioned in ONE committed transaction via `provisionAdminActor` (real `admin` rows), then governance flags flipped in the SAME transaction (`isDeleted`+`deletedAt` / `isBlocked`+`blockedAt` / `suspended`+`suspendedAt` / all three) — the live-token-at-service-tier governance scenario (the GraphQL context boundary is not fail-closed for governed users; the service tier must be).

| # | Probe | Expected error | Message oracle |
|---|---|---|---|
| 1 | `actorId = 0` (anonymous sentinel, `ANONYMOUS_ACTOR_ID`) | `UnauthorizedError` (instance) | contains `tErrors.unauthorized` |
| 2 | Absent actor id (`2_000_000_000`, provably absent — direct `users` count == 0 asserted BEFORE the call) | **`ForbiddenError`** per **RULING 2 amendment** (supersedes the task card's original `UnauthorizedError` for absent rows) | contains `tErrors.forbidden` |
| 3 | student / teacher / parent direct service calls (journey-B cast, own ids + locales) | `ForbiddenError` ×3 | contains `tErrors.forbidden` |
| 4a | deleted admin | `ForbiddenError` | contains `tErrors.accountDeleted` |
| 4b | blocked admin | `ForbiddenError` | contains `tErrors.accountBlocked` |
| 4c | suspended admin | `ForbiddenError` | contains `tErrors.accountSuspended` |
| 4d | multi-flagged admin (deleted+blocked+suspended) | `ForbiddenError` | contains `tErrors.accountDeleted` — **deterministic ladder: deleted → blocked → suspended, deleted evaluated first** |

All rejections use the try/catch helper `expectJourneyError` (NEVER `expect(...).rejects.toThrow()`); messages are canonical localized copies from `getServerTranslations("en").errorsTranslations` — no hardcoded English, no raw key echoes.

## 3. Per-denial purity + whole-suite purity

- The whole matrix is bracketed by `snapshotObservedTables()` byte-identity (all EIGHT observed tables: `users`, `session`, `student_payments`, `subscriptions`, `teacher`, `evaluations`, `reports`, `teacher_transaction` — count + `to_jsonb` full-row md5 digest, PK-ordered) + zero audit delta + zero notification delta (audit/notification counts pre-asserted == whole-suite baselines).
- **Pre-DB denials**: the observable purity (byte-identity, zero audit, zero notifications) is what the journey layer can prove without a repo spy; the repo-spy "ZERO aggregate reads on every denial path" proof is Tier 4 of Task 2.6.TE (the repository does not exist during test-first authoring — recorded as the sanctioned division of labor, matching the plan's "(denial is pre-DB where applicable)" qualifier).
- Final whole-suite anchor: tables byte-identical to the post-fixture snapshot (`digestsPostFixture`, captured after ALL fixtures across A–D committed), audit delta == 0 and notification delta == 0 for the ENTIRE suite (baselines captured before ANY provisioning in Journey A's `beforeAll`).

## 4. Teardown (suite `afterAll` — RULING 1 order; NO `runInRollback` anywhere)

1. **FIRST** — tracked `student_payments` ids + `teacher_transaction` ids hard-deleted inside `withImmutabilityTriggersSuspended(["student_payments", "teacher_transaction"], …)` (their UPDATE/DELETE triggers raise otherwise, and the `subscriptions`/`session` ON DELETE SET NULL parents would fire the UPDATE guards if deleted first).
2. FK-safe deletes: `reports` → `evaluations` (evaluator RESTRICT) → `session` (RESTRICT into teacher/students) → `subscriptions` → `wallet` (RESTRICT from transactions — already gone) → `plans`.
3. Actor rows via `deleteUsersByIds` (its audit sweep rides `withAuditDeleteTriggersSuspended` internally), then the `TrackedFixtures` belt-and-braces reverse-order sweep.
4. Mandatory zero-residue probes: `countUsersByIds == 0`; per-table `countIfAny == 0` for every tracked id list; audit rows == baseline; notifications == baseline.

## 5. RED evidence (verbatim — shared file)

```
test/workflows/admin/platform-analytics.journey.test.ts(104,42): error TS2307: Cannot find module '@/backend/services/admin/platform-analytics.service' or its corresponding type declarations.
```

Runner: `error: Cannot find module '@/backend/services/admin/platform-analytics.service' from '/home/z/my-project/test/workflows/admin/platform-analytics.journey.test.ts'` — `0 pass / 1 fail / 1 error`. Expected RED until Task 2.6.

## 6. Semantic review (2.4.SR)

1. Deterministic denial order asserted in the mandated sequence (deleted → blocked → suspended), and the multi-flagged admin proves deleted is evaluated FIRST when flags coincide (REQ-054).
2. No permission monkey-patching anywhere in the file — every denial throws through the real `assertActorAdminActive` gate over real role rows (2.4.SEC: anonymous/absent/non-admin/governed ×3 tiers all covered).
3. Error-oracle discipline: only canonical localized copies asserted; error classes pinned by `instanceof` (`UnauthorizedError` vs `ForbiddenError` — the classes fix `extensions.code` `UNAUTHORIZED`/`FORBIDDEN`).
4. Byte-identity snapshots use content digests (not just counts), so an UPDATE that preserves row counts still fails — the strongest observable read-purity oracle at this layer.
5. Teardown order proven against the schema FK graph (RESTRICT + ON DELETE SET NULL + immutability triggers) — RULING 1 amendment A1 applied verbatim; `JOURNEY_TRACKED_TABLE_DELETE_ORDER` and `journeyCleanup` untouched (registry stays "immutable rows are assert-ZERO, never cleaned" for DEV3-016 journeys).

## 7. Instruction verification (2.4.IV)

Journey rules 1–12 compliant — notably rule 1 (no rollback wrapper), rule 2 (tracked cleanup + mandatory zero-residue probes), rule 6 (try/catch + translated substrings), rule 4 (honest authorization); tests.instructions compliant (`bun:test`, no `any`, no `console.*`, no `oxlint-disable`); `.agents/instructions/backend.instructions.md` not violated (test-only file). Biome: `Checked 1 file … No fixes applied.` (0 diagnostics). Sub-loop exits 1 at tsgo on the sanctioned RED only — **deviation recorded: definitive sub-loop pass (exit 0) is deferred to Task 2.6**, when the missing-service import resolves and all four journeys can actually execute.

## 8. Carry-forward

- Task 2.6 implements the gate via `assertActorAdminActive` FIRST statement; Journey D pins its observable semantics (0 → UNAUTHORIZED; absent → FORBIDDEN; non-admin → FORBIDDEN; governed ladder deleted → blocked → suspended) — the RULING-2 accepted deltas (absent-actor FORBIDDEN, two pre-tx actor reads, gate logs `entity: "user"`) are already the asserted behavior here.
- Task 2.6.TE Tier 4 owns the repo-spy pre-DB proof; REQ-076/2.M re-runs this suite for the gate.

*End of Task 2.4 outcome.*
