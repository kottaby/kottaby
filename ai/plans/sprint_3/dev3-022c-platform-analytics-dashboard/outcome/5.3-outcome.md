# Task 5.3 Outcome — End-to-End Smoke (dev server, full surface)

**Feature:** `dev3-022c-platform-analytics-dashboard`
**Date:** 2026-09-04 (sandbox UTC)
**Mode:** Scripted E2E smoke pass — **zero repo files patched** (`git status --short`: no tracked-file changes before/after; only pre-existing untracked `.browser-auth-student/` from 4.5). No git-mutating command run. tasks.md untouched.

**Result: COMPLETE — 14/14 parity assertions PASS, live introspection `adminPlatformAnalytics` present with ZERO args, student wrong-role bounce verified live, generated↔committed SDL equality green (`git diff --quiet` clean + plan-catalog.schema 5/0), dev.log adds ZERO new error lines.**

---

## 1. Environment (reused per prior evidence)

- Dev server: already up at http://localhost:3000 (`curl` → 200 before starting; never restarted, never killed).
- DB: `$PGBIN/psql -h 127.0.0.1 -U postgres -d app_db` (PGBIN=/tmp/pg-root/usr/lib/postgresql/17/bin, LD_LIBRARY_PATH set) — reachable.
- Login: `scripts/browser-login.ts --inject` cookie injection (the established 4.5 approach). Admin = `Super Admin (role: Admin, id: 1)` via `.env` ADMIN_EMAIL/ADMIN_PASSWORD; student = `Demo Student (role: Student, id: 4)` (`student@draftacademy.local`, shared seeded ADMIN_PASSWORD credential) via `--email-key` override into session `e2e-student` (jar `.browser-auth-student/` — the 4.5 dir).
- Locale: switched through the app's real mechanism (`NEXT_LOCALE` cookie) to EN for the parity read; page fully populated both locales (AR render seen before switch — 9 h2 nodes).
- Predicates read from source before running psql: `platform-analytics.repository.ts` + `platform-analytics-query-helpers.ts` (countRecentlyActiveUsers / getSessionStats / getRevenueStats / getSubscriptionStats / getTeacherPresenceStats / getHealthIndicators) and `admin-user.repository.ts getStats` (users.total/active), enum literals taken from `backend/enum/*` (all lowercase values).

## 2. UI-vs-DB parity table (DOM values read via `agent-browser eval`; DB via psql repo-SQL equivalents)

Seeded baseline: users=4 (all created 2026-09-04 14:14 UTC), session/subscriptions/teacher/teacher_transaction/student_payments tables all EMPTY; no paid payments → per the F6 (4.3) precedent, ONE paid payment fixture was inserted to exercise the revenue row live (§3).

| # | Metric (section) | UI value (DOM) | DB value (psql, repo predicate) | Result |
|---|---|---|---|---|
| 1 | Users — Total users | 4 | `count(*) FROM users` = 4 | **PASS** |
| 2 | Users — Active users | 4 | `count(*) FILTER (coalesce(is_deleted,false)=false AND coalesce(suspended,false)=false AND coalesce(is_blocked,false)=false)` (getStats) = 4 | **PASS** |
| 3 | Users — Recently active (24h) | 4 | `last_active_at > now()-interval '24h' AND coalesce(is_deleted,false)=false AND coalesce(suspended,false)=false AND coalesce(is_blocked,false)=false` (repo raw-branch SQL verbatim) = 4 (margins 4–419 min — not boundary-fragile) | **PASS** |
| 4 | Users — New this week | 4 | `created_at > now()-interval '7 days'` = 4 | **PASS** |
| 5 | Users — Admins / Teachers / Students / Parents | 1 / 1 / 1 / 1 | role counts = 1 / 1 / 1 / 1 | **PASS** |
| 6 | Sessions — Today | 0 | table empty; `count(*) FILTER (created_at >= utc-day-start AND created_at <= now)` = 0 | **PASS** |
| 7 | Sessions — Total (+ all window/status counters) | 0 (×10 rows) | `count(*) FROM "session"` = 0 | **PASS** |
| 8 | Revenue — EGP **totalAmount** (live fixture leg) | `765.43` | `coalesce(sum(amount),0)::text FROM student_payments WHERE status='paid' AND currency='EGP'` = `765.43` | **PASS** |
| 9 | Revenue — EGP paidPaymentsCount / last30Days | 1 / 765.43 | count = 1; 30-day window sum = 765.43 | **PASS** |
| 10 | Subscriptions — Active in window now | 0 | `status='active' AND coalesce(start_date, now) <= now AND (end_date IS NULL OR now < end_date)` = 0 | **PASS** |
| 11 | Teachers — Online now | 0 | `count(*) FILTER (is_approved=true AND is_online=true) FROM teacher` = 0 (teacher table empty — applicants/role-only users absent, per contract) | **PASS** |
| 12 | Operational Health — Pending disputes | 0 | `count(*) FROM "session" WHERE status='disputed'` = 0 | **PASS** |
| 13 | Operational Health — Pending withdrawals | 0 | `count(*) FROM teacher_transaction WHERE type='withdrawal' AND status='pending'` = 0 | **PASS** |
| 14 | Revenue — Offline activations (honesty separation) | 0 | `payment_method IN ('offline_cash','bank_transfer','scholarship')` = 0 — and the live paymob fixture was correctly NOT folded into it (gateway vs offline separation holds) | **PASS** |

**Parity score: 14/14 PASS.** Ratings section also eyeballed: honest `—` averages + "No ratings recorded yet." (consistent with 0 rated rows both families — not counted in the mandated metric set).

## 3. Revenue fixture lifecycle (F6-pattern, with cleanup)

- Trigger state checked FIRST: `pg_trigger` on `student_payments` → **0 non-internal triggers** (dev DB skips `3-immutability-triggers.sql`, same environment note as 4.3 F6) → plain DELETE legal.
- `INSERT INTO student_payments (student_id, subscription_id, amount, currency, payment_gateway, status, created_at, updated_at) VALUES (4, NULL, 765.43, 'EGP', 'paymob', 'paid', now(), now()) RETURNING id=168` — non-offline gateway (`paymob`) per the metric definition; the existing `students` row (id 4) was used, **no students fixture row needed**.
- Click Refresh → DOM read: revenue table row `["EGP","765.43","765.43","1"]` (== psql row exactly); Offline activations stayed 0.
- Cleanup: `DELETE FROM student_payments WHERE id=168` → `DELETE 1`, `payments_after=0`; Refresh → empty state restored (`"No revenue recorded yet.","Offline activations","0"`). **No fixture residue.**

## 4. Wrong-role HTTP access (student → admin route)

- Session `e2e-student` (Student, id 4, cookie-injected) → `agent-browser open http://localhost:3000/admin/analytics` → **URL = `http://localhost:3000/student/dashboard`** — the server-side `withPageAuth` guard bounced to the role-specific path (never bare `/dashboard`), matching 4.3 F7.
- DOM after bounce: h1 `مرحباً، Demo Student` (student chrome), `a[href='/admin/analytics']` count = **0**, body text contains neither "Platform Analytics" nor "تحليلات المنصة".
- dev.log shows the pair `GET /admin/analytics 200` → `GET /student/dashboard 200` (bounce is a server redirect, no error logged).
- Screenshot: `/tmp/ui-shots/e2e-student-bounce.png`.

## 5. Live introspection — `adminPlatformAnalytics` with ZERO args

- Query posted to `http://localhost:3000/api/graphql`:
  `query IntrospectAdminAnalytics { __schema { queryType { fields { name args { name type { kind name ofType { kind name ofType { kind name } } } } } } } }`
- **Anonymous introspection is allowed repo-wide** — HTTP 200 both anonymous and with the admin cookie jar (`curl -b .browser-auth/cookies.txt`, also 200).
- Parsed result: `{ "found": true, "args": [], "argCount": 0, "totalQueryFields": 23 }` → **`adminPlatformAnalytics` exists on the live Query type with an EMPTY argument list.** Re-confirmed 200 on a repeat anonymous post after the pass.

## 6. Generated ↔ committed SDL equality

- `git diff --quiet -- frontend/graphql/generated/schema.graphql` → **clean** (no drift between generated and committed SDL in the working tree).
- `bun run test/scripts/run-test.ts backend/graphql/test/plan-catalog.schema.test.ts` re-run (permitted: nothing touched) → **5 pass / 0 fail** (22 expects), including the pinned assertion *"Committed schema.graphql matches live code-first graphQLSchema exactly"* — live-schema tier corroboration on top of the git-diff tier.

## 7. dev.log status

- Baseline at pass start: 239 lines, 23 error-ish lines — ALL pre-existing and previously attributed (4.3 §4: expected anonymous `Me`/`MyUnreadNotificationCount` UnauthorizedError domain rejections; two `[browser] Not supported on server side` entries from the 4.3 fault-injection probe).
- After the pass: error-ish count **still 23 → ZERO new error/unhandled/exception lines** during login, dashboard load, snapshot fetches, refresh round-trips, introspection, and the student bounce. Every request on the verified surface returned 200/204.
- **One isolated anomaly to flag (not on any verified flow):** a single `POST /api/graphql 500 in 17ms` (dev.log line 243) appears in the early harness-launch window — BEFORE the first `GET /admin/analytics` of the pass (first `open` attempts timed out at the agent-browser MCP layer and were retried/relaunched). It has NO accompanying server-side error/domain log line; every verified flow (admin login ×2, ~300 ms `adminPlatformAnalytics` snapshot POSTs, refresh POSTs, both introspection curls, student login, student bounce) returned 200; probes aimed at reproducing it (anonymous introspection re-run, empty body, malformed JSON, invalid field) deterministically return 200/400/400/400. Unattributable to the analytics surface; recorded for the orchestrator, no action taken.

## 8. Screenshots

| File | Content |
|---|---|
| `/tmp/ui-shots/e2e-admin-dashboard.png` | Populated admin dashboard, EN locale, 7 metric sections + 2 trend charts, revenue row visible during the fixture leg's verification window (1440×900) |
| `/tmp/ui-shots/e2e-student-bounce.png` | Student chrome on `/student/dashboard` after the `/admin/analytics` bounce |

## 9. Instruction verification / hygiene

- CRITICAL GIT RULE honored: no git-mutating commands (status/diff read-only only); **no commit made**; tasks.md NOT edited.
- Cookie artifacts stayed in the git-ignored `.browser-auth/` + pre-existing untracked `.browser-auth-student/` (mode 600, values never echoed to stdout).
- All fixtures deleted; no DB state changes remain (verified counts above).
- Dev server left running (no kill), matching the handoff state.

## 10. Verification summary

| Check | Result |
|---|---|
| UI↔DB parity (mandated metrics + users/newThisWeek/offline separation) | **14/14 PASS** |
| Revenue fixture insert → UI row == psql sum → DELETE → empty state restored | **PASS** (no triggers; no residue) |
| Student wrong-role bounce `/admin/analytics` → `/student/dashboard` | **PASS** (live, role-specific path) |
| Live introspection `adminPlatformAnalytics` zero args | **PASS** (`args: []`, anonymous + cookie 200) |
| Generated ↔ committed SDL equality | **PASS** (`git diff --quiet` clean; plan-catalog.schema 5/0) |
| dev.log new errors during pass | **0** (one isolated pre-window 500 flagged, unattributed, no error log line) |
| Repo files patched | **0** |
