# 6.3 Review-Frontend Outcome — REBUILT tree (independent, READ-ONLY)

- Task: DEV3-022c wave 6.3 frontend review (re-run post repo-wipe; pre-wipe outcome referenced the LOST tree @638b171).
- Branch `feat/dev3-022c-platform-analytics-dashboard` @ `27e259a` verified at start (git branch --show-current + rev-parse) and re-verified at close. No source edits, no commits, no tasks.md changes; only file written is this outcome.
- Method: every named file read fully; violation greps run; F-1..F-4 each verified explicitly; gates re-run via sanctioned runners.

## Scope reviewed (all read in full)

1. `frontend/graphql/sharedDocuments/admin/platform-analytics.documents.ts` + `.test.ts` + barrels (`admin/index.ts`, top-level `index.ts`)
2. `frontend/providers/apollo/apolloCache.ts` + `apolloCache.test.ts`
3. `app/(dashboard)/admin/analytics/page.tsx` (+ `withPageAuth.ts` guard semantics)
4. All 8 files under `frontend/views/admin/analytics/`: PlatformAnalyticsContainer, AnalyticsHeaderBar, AnalyticsStateBanners, AnalyticsMetricGrid, AnalyticsTrendCharts, SectionPrimitives, SessionTrendChart, RevenueTrendChart
5. `frontend/views/dashboard/nav/navItems.ts` + `nav/navItems.test.ts` (nav/ variant; no top-level navItems.ts exists)
6. `test/ui/components/admin/PlatformAnalyticsContainer.test.tsx`

## Per-checklist verdicts

| # | Checklist | Verdict |
|---|-----------|---------|
| 1 | Documents: `gql` + `TypedDocumentNode<AdminPlatformAnalyticsQuery>`, NAMED op `AdminPlatformAnalytics` (documents.ts:24-25), ZERO variables, selection closed leaf-for-leaf vs `schema.graphql:356-445` (all 10 root fields; 11/11 users, 10/10 sessions, 4/4 currency rows, 7/7 subscriptions, 3/3 teachers, 4/4 ratings, 2/2 health, both trend point types), NO `id` anywhere, barrel re-export identity test | ✅ PASS |
| 2 | apolloCache: exactly 11 `PlatformAnalytics*` entries all `keyFields:false` (PlatformAnalytics, Users, Sessions, Revenue, CurrencyRevenue, Subscriptions, Teachers, Ratings, Health, SessionTrendPoint, RevenueTrendPoint — matches the 11 SDL embedded types 1:1) + 6 legacy = 17-entry FROZEN inventory asserted at apolloCache.test.ts:176-197; behavioral inline-write proofs present | ✅ PASS |
| 3 | page.tsx: Server Component, `withPageAuth({roles:[UserRole.Admin], redirectTo:"/admin/analytics"})` with wrong-role fallback riding `roleDashboardPath` (withPageAuth.ts:95), `generateMetadata` via `getLocaleFromCookie()` + `analyticsTranslations.metaTitle/metaDescription`, renders `<PlatformAnalyticsContainer />` only (zero server data fetching) | ✅ PASS |
| 4a | MUI v9 sx-only: zero hex/rgb in the whole directory (grep exit 1); colors all `theme.palette.*` incl. recharts strokes/fills/contentStyle via `useTheme`; `fontWeight` appears only inside sx (SectionPrimitives.tsx:31); 44px targets on Refresh + Retry (`minHeight:44`) | ✅ PASS |
| 4b | Data: `pollInterval: 120_000` + `notifyOnNetworkStatusChange: true` (Container:39-42), no `useLazyQuery`; refetch keeps stale snapshot under `refreshingLabel` chip (NetworkStatus.refetch; Apollo retains data) | ✅ PASS |
| 4c | States: FORBIDDEN (extractErrorCode traverse of `errors[].extensions.code`) → in-container `DeniedNotice`; any other error → `LoadErrorAlert` + Retry; raw server `message` never rendered (REQ-053; pinned by test:194-209) | ✅ PASS |
| 4d | Honest data: rating averages `?? "—"` (never fabricated 0), `noRevenueYet` on empty gateway window, `noRatingsYet`; money is EXACT string display-only — sole `Number(point.amount)` is the chart presentation boundary, documented (RevenueTrendChart.tsx:75-77); no float math anywhere | ✅ PASS |
| 4e | i18n: `useAppTranslation(Analytics)` property access in all 5 consuming files; RTL logical props throughout (`gapInline`, `gapBlock`, `marginBlock*`, `paddingBlock*`); skeletons keyed by internal ids not user copy | ✅ PASS |
| 4f | Post-rebuild revenue patch: per-currency rows render as `${currency} · ${t.totalAmountHeader}` / `${currency} · ${t.last30DaysAmountHeader}` (AnalyticsMetricGrid.tsx:122-123); en "Total amount"/"Last 30 days", ar "المبلغ الإجمالي"/"آخر 30 يوماً" — old parenthetical style gone | ✅ PASS |
| 5 | Nav: exactly ONE `/admin/analytics` entry (nav/navItems.ts:135), Admin role only, `labelKey:"analytics"` ownership-clean (present in dashboard bundle, absent from handshakeCode; compile-time `NavLabelKey` collision carve-out + runtime belt in navItems.test.ts:46-58); repo-wide grep shows the route only in page.tsx + navItems.ts | ✅ PASS |
| 6 | Container test: real Apollo+MockLink, label-keyed assertions via `getTranslations("en").analyticsTranslations`, v4 error shape (`errors[].extensions.code`), 5 tests = loading/populated/error/FORBIDDEN/honest-null+empty-revenue; recharts stubbed per posture | ✅ PASS (see M-2) |

## F-1..F-4 verification (pre-wipe findings mandated into the rebuild)

- **F-1 (bilingual chart aria-labels via namespace): HEALED.** Both charts take `ariaLabel` props; `AnalyticsTrendCharts.tsx:56,79` passes `t.sessionTrendAriaLabel` / `t.revenueTrendAriaLabel`; en leaves at `shared/locale/en/analytics/index.ts:74-75`, ar at `shared/locale/ar/analytics/index.ts:74-75`; figure carries the localized name; XAxis (both) and Revenue YAxis get localized `aria-label`s too.
- **F-2 (locale-aware YAxis tickFormatter): HEALED (with one nuance → M-1).** Both charts format ticks through `Intl.DateTimeFormat` month/day (SessionTrendChart.tsx:31-37, RevenueTrendChart.tsx:46-52) — no hardcoded date strings. Nuance: locale tag is `undefined` (ambient/browser locale), not the app locale.
- **F-3 (no dead `refreshing` prop): HEALED.** `refreshing` exists only on `AnalyticsHeaderBarProps` and is consumed twice (refreshingLabel chip, line 63; refresh-button disable, line 68). MetricGrid/other components take no refreshing prop (grep: 8 hits, all live).
- **F-4 (honest tooltip docstring): HEALED.** RevenueTrendChart docstring:13-16 states the raw decimal string is parsed ONLY at the presentation boundary and never used for math — matches the code (Number() at line 77, chart render only). SessionTrendChart docstring matches (numeric value display-only).

## Findings

- **M-1 MINOR — tick formatter uses ambient locale, not the app locale; local fork instead of the format-date.ts helper.** `SessionTrendChart.tsx:36` / `RevenueTrendChart.tsx:51` call `Intl.DateTimeFormat(undefined, …)`; the docstring's "through the caller's locale" overstates (it is the runtime/browser default). tasks.md:322 named `frontend/lib/i18n/format-date.ts` as the vehicle; that helper's fixed full-timestamp options are indeed unsuitable for axis ticks, but the app-locale resolution discipline (`resolveLocaleTag` + explicit `locale` arg, as `AnalyticsHeaderBar.tsx:59` does via `useAppLocale`) was not carried over. Corroborated by visual QA: ar page rendered en-formatted ticks. Non-blocking; suggest threading `useAppLocale()` into the formatter.
- **M-2 MINOR — container test omits the mandated refreshing state.** REQ-075(b) and tasks.md 4.4.TE list "refreshing state → stale data retained + busy indicator" among the required component-test states; the suite (5 tests) covers loading/populated/error/FORBIDDEN/honest-null+empty but has no refetch-state test, and its docstring silently narrows the contract (test file:10-17). Behavior is implemented and was live-verified in 4.4.BF (refresh fires POST /api/graphql; chip + disabled button). Suggest: second MockLink response + refetch → assert stale figures persist and `t.refreshingLabel` chip renders. Non-blocking (no runtime defect).
- **I-1 INFO — inline `style` prop on `<figure>`** in both charts (layout reset: margin/width/minHeight; non-MUI element with no sx support; zero colors). sx-only discipline read as MUI-component styling.
- **I-2 INFO — SessionTrendChart YAxis has no `aria-label`** while RevenueTrendChart's YAxis has `amountAxisLabel`; the figure-level localized name still names the session chart. Asymmetry only.
- **I-3 INFO — test-comment precision nits:** documents.test.ts:66 comment says fields are "selected EXACTLY once" but the assertion is presence-only (`toContain`); navItems.test.ts pins `/admin/plans` but has no explicit `/admin/analytics` single-entry assertion (route uniqueness verified by this review's grep; ownership belt covers `labelKey`).
- **Carry-over (owned by 6.1, not re-opened):** 6.1 M-2 `AnalyticsHeaderBarProps.generatedAt: string | Date` union incl. the defensive `toISOString()` at AnalyticsHeaderBar.tsx:59 — client-side display formatting only; REQ-068 concerns the GraphQL surface, which 6.2 already verified clean.

## Gate results (re-run this session)

- `bun run test:ui:components` (sanctioned runner): **178 pass / 0 fail** (15 files, 1119 expect calls, 60.5s) — matches the Phase-4/5 baseline.
- `bun run tsgo`: **exit 0**.
- Violation greps: `#[hex]|rgb(` → 0 hits; `fontWeight|textAlign` → only inside sx; `Number(|parseFloat` → only RevenueTrendChart.tsx:77 (documented boundary).

## Final verdict

**CLEAN** — 0 BLOCKING, 0 MAJOR, 2 MINOR (M-1, M-2 — logged as non-blocking follow-ups), 3 INFO + 1 carry-over. REQ-062..REQ-068 and REQ-075 verified on the rebuilt tree; F-1..F-4 all HEALED; the post-rebuild revenue labeled-rows patch is correctly namespace-backed in en+ar.

## ORCHESTRATOR RESOLUTION ADDENDUM (post-wave)

- **M-1 (tick formatter locale) — RESOLVED.** Added `formatChartTick(locale,
  value)` to the shared `frontend/lib/i18n/format-date.ts` helper (the
  plan-mandated REQ-067 helper) using the module's existing app-locale
  fallback rule; both charts now receive `locale` from `useAppLocale()` via
  `AnalyticsTrendCharts` and format XAxis ticks + Tooltip labels through the
  helper. The chart-local `Intl.DateTimeFormat(undefined)` fork is removed.
- **M-2 (refreshing state untested) — RESOLVED.** The container suite gains
  a 6th case: populated → manual Refresh → a 5s-delayed refetch holds
  `NetworkStatus.refetch` in flight → asserts `refreshingLabel` chip,
  STALE snapshot retention (figure 101 still rendered), and the disabled
  refresh control. (A result-less MockLink case errors the refetch instead
  of holding it open — recorded for future suite authors.)
- **INFO items** (figure inline style reset, YAxis aria-label asymmetry,
  test-comment precision, generatedAt union from 6.1-M-2): accepted as
  recorded — the Date branch exists deliberately so tests may pass Date
  fixtures at the presentation boundary; no code change.
- Gates after resolution: component suite **179 pass / 0 fail** (6 container
  cases), tsgo 0, sub-loop exit 0 on all five touched files, lint-service
  success/exitCode 0 (baseline parity held).
