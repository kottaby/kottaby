# Task 1.1 (+1.2) — Canonical types outcome (RECOVERY RECORD)

Lost in the catastrophe wipe; rebuilt and re-committed as `edbd4a8`
(`backend/types/admin/platform-analytics.types.ts` + barrel export — exactly
plan §2.2: intersection type for users over the reused `AdminUserStatsReturnType`,
readonly members, money as `string`, nullable rating averages, `Date` for
DateTime-bound instants, NO id fields anywhere). Task 1.2 verify-only schema-drift
guard folded here as in the original run: `git diff -- backend/db/schema/
backend/db/migration/` EMPTY at commit and re-verified at every later gate.
Runtime type conformance is pinned by the repo suite's
`AdminUserStatsReturnType` superset test (23/23) and the service suite's
sentinel composition test (12/12). Gates at commit: tsgo 0, sub-loop 0.
