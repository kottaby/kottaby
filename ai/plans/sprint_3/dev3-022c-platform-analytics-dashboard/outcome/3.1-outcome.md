# Task 3.1 Outcome — Platform Analytics Pothos Objects (eleven embedded value objects)

**Feature:** `dev3-022c-platform-analytics-dashboard`
**Branch note:** repo checked out on `main` at `e419280`, which is the SAME commit `feat/dev3-022c-platform-analytics-dashboard` points at (identical trees; no branch switch performed — git-mutating commands forbidden for this subagent).
**Mode:** Pothos object-substrate creation + one barrel line; zero query/mutation registration in this task, zero schema/migration edits, no commit made.

---

## 1. Summary

- Created `backend/graphql/pothos/admin/platform-analytics.pothos.ts`: ELEVEN `gqlSchemaBuilder.objectRef<CanonicalType>("<Name>").implement({ fields: t => ({…}) })` calls — the root `PlatformAnalytics` plus the ten section/trend value objects — every ref backed by its canonical type imported from `@/backend/types` (`PlatformAnalyticsReturnType`, `PlatformAnalyticsUsersReturnType`, `PlatformAnalyticsSessionsReturnType`, `PlatformAnalyticsCurrencyRevenueReturnType`, `PlatformAnalyticsRevenueReturnType`, `PlatformAnalyticsSubscriptionsReturnType`, `PlatformAnalyticsTeachersReturnType`, `PlatformAnalyticsRatingsReturnType`, `PlatformAnalyticsHealthReturnType`, `PlatformAnalyticsSessionTrendPointReturnType`, `PlatformAnalyticsRevenueTrendPointReturnType`). GraphQL type names drop the `ReturnType` suffix exactly per the SDL contract. All eleven objects are embedded value objects: NO `id` anywhere, NO local types, NO `toISOString()`, NO resolve-time data access (every field is a pure projection of the service-composed snapshot).
- Field exposure per the SDL contract: `t.exposeInt` for every counter; `t.exposeString` for `currency`/`totalAmount`/`last30DaysAmount`/`amount` (exact decimal-string money, never numeric); `t.expose("generatedAt", { type: "DateTime" })` + `t.expose("bucketStart", { type: "DateTime" })` (the registered scalar by name — zero String timestamps); `t.exposeFloat("averageSessionRating", { nullable: true })` + `t.exposeFloat("averageEvaluationScore", { nullable: true })` — the ONLY nullable fields on the surface (honest-absence averages).
- List fields use the closest in-bundle read-model convention, loadable-free: plain `t.field({ type: [Ref], resolve })` exactly like `AdminUserPage.items`, with the spread copy `[…parent.x]` matching the `AdminUserActivityEntry.changedFields` convention for `readonly T[]` canonical members (`gatewayRevenueByCurrency`, `sessionTrendDaily`, `revenueTrendDaily`); the seven root section fields are single-object `t.field({ type: X, resolve: parent => parent.x })` projections (the `AdminUserDetail` composition pattern).
- Updated `backend/graphql/pothos/admin/index.ts` with `export * from "./platform-analytics.pothos";` (existing star-export style). Registration reaches the schema transitively: the query file (Task 3.2) imports `PlatformAnalyticsPothosObject` from this barrel — same convention as `audit-trail.query.ts` → `pothos/admin`.
- Descriptions: the authoritative SDL contract block prescribes NO type/field descriptions — the verbatim-copy set for the objects is EMPTY; the only prescribed description text is the root query's (Task 3.2). Adding invented descriptions would have drifted the exact-surface contract, so none were added (confirmed in the regenerated artifact: the eleven types carry no description comments).

## 2. Files created / modified / NOT modified

| File | Action | Notes |
|---|---|---|
| `backend/graphql/pothos/admin/platform-analytics.pothos.ts` | CREATE | 11 objectRefs; ~330 lines incl. domain-only JSDoc; imports ONLY `gqlSchemaBuilder` + the 11 canonical types from `@/backend/types` |
| `backend/graphql/pothos/admin/index.ts` | MODIFY | one appended `export * from "./platform-analytics.pothos";` line |

NOT modified (read-only by design): `backend/types/admin/platform-analytics.types.ts` (canonical substrate — consumed via import), `backend/graphql/pothos/shared/scalar.pothos.ts` (`DateTime` already registered; referenced by name only), `backend/graphql/pothos/builder.ts` (Scalars slot already carries `DateTime`), `backend/graphql/pothos/admin/admin-user.pothos.ts` (pattern source only), every query/mutation file (Task 3.2's scope), `backend/db/schema/**`, `backend/db/migration/**`, `tasks.md`.

## 3. Verification commands + results

| Command | Result |
|---|---|
| `bun run scripts/health/sub-loop.ts backend/graphql/pothos/admin/platform-analytics.pothos.ts --lifecycle duplicates` | **exit 0** — tsgo ✅, oxlint ✅, biome:check ✅, lint:type-aware ✅, check:duplicates ✅ |
| `bun run scripts/health/sub-loop.ts backend/graphql/pothos/admin/index.ts --lifecycle duplicates` | **exit 0** — all five gates ✅ |
| `bunx tsgo -b --noEmit 2>&1 \| grep -c "error TS"` | **0** (full-repo type check, shared with Task 3.2) |
| `bun run generate:gqlSchema && bun codegen` (Task 3.2 gate, run after the query landed) | exit 0; artifact carries EXACTLY the eleven object types + root query field, nothing else |
| `git diff backend/graphql/pothos/admin/index.ts` | +1 line (the barrel export) |

SDL-side proof of the object surface (artifact diff verified field-by-field against the contract):

- `PlatformAnalytics`: `generatedAt: DateTime!`, seven section fields (`users`/`sessions`/`revenue`/`subscriptions`/`teachers`/`ratings`/`health`, each `<Type>!`), `sessionTrendDaily: [PlatformAnalyticsSessionTrendPoint!]!`, `revenueTrendDaily: [PlatformAnalyticsRevenueTrendPoint!]!` — 10 fields.
- `PlatformAnalyticsUsers`: 11 counters (ten directory counters + `recentlyActive24h`), all `Int!`.
- `PlatformAnalyticsSessions`: 10 counters, all `Int!`. `PlatformAnalyticsSubscriptions`: 7 counters. `PlatformAnalyticsTeachers`: 3 counters. `PlatformAnalyticsHealth`: 2 counters.
- `PlatformAnalyticsRevenue`: `gatewayRevenueByCurrency: [PlatformAnalyticsCurrencyRevenue!]!` + `offlineActivationsCount: Int!`.
- `PlatformAnalyticsCurrencyRevenue`: 3 × `String!` + `paidPaymentsCount: Int!`. `PlatformAnalyticsRevenueTrendPoint`: `bucketStart: DateTime!` + `currency: String!` + `amount: String!`. `PlatformAnalyticsSessionTrendPoint`: `bucketStart: DateTime!` + `sessionCount: Int!`.
- `PlatformAnalyticsRatings`: `averageSessionRating: Float` + `averageEvaluationScore: Float` (the two nullable members) + two `Int!` counts.

## 4. Semantic review — item-by-item

1. **Single canonical object per shape; no local types** — every ref is `objectRef<{Canonical}ReturnType>`; grep for `^(export )?(type|interface)` over the file = NO MATCHES; all 11 imports resolve from `@/backend/types`.
2. **No `id` exposed anywhere in the subtree** — grep `'"id"'` = NO MATCHES; proven again at the artifact tier by the new SDL assertion suite (Task 3.2): "NO analytics object carries an `id` field" over all eleven types. The Apollo `id`-exposure rule is consciously NOT applicable: these are aggregate/embedded value objects (same posture as `HealthCheck`/`HandshakeCodeLookup`/`NotificationListPage`), and the canonical shapes carry no id to expose.
3. **DateTime discipline** — the three instant fields ride the registered `DateTime` scalar by name; grep `toISOString` over the file = NO MATCHES; no field is typed `String` for a timestamp.
4. **Money as exact decimal strings** — `totalAmount`/`last30DaysAmount`/`amount` are `t.exposeString`; no numeric coercion anywhere in the projection layer (the canonical `string` passes straight through).
5. **Nullable only where honest** — exactly two `nullable: true` on the whole surface (the two rating averages); every other field is v3-default non-nullable; `null` flows only when the rating band is empty.
6. **No resolve-time data access / no N+1** — zero service/repository calls in the file; every resolve is `parent => parent.x` or a spread copy of a `readonly` array (the `readonly`-array convention already shipped by `AdminUserActivityEntry.changedFields`); `t.loadable` is deliberately absent (the parent is one resolved snapshot, not a per-row entity — the dataloader rule targets per-parent-object service calls, which do not exist here).
7. **No shared mutable module state; no dead declarations** — top-level scope is exactly the 11 exported object consts; each is consumed by the schema graph (root → sections → points); no `let`/`var`, no caches.
8. **No enum registration, no input type, no mutation** — the file registers objects only; D7 (no new enum) structurally honored; the enum-set freeze test stays green.
9. **Clean comments — ZERO plan-artifact refs** — strict grep (`REQ-[0-9]|Task [0-9]|tasks\.md|specs\.md|plan\.md|RULING|Phase [0-9]|§`) over the file = NO MATCHES; JSDoc states domain contracts only (aggregate anonymity, single-snapshot coherence, honest-absence averages, midnight-UTC buckets, decimal-string money).
10. **Instruction verification** — `backend/AGENTS.md` + `backend/graphql/AGENTS.md` + `backend/graphql/pothos/AGENTS.md` + `.agents/instructions/backend.instructions.md`: canonical types from `@/backend/types` ✅; no local definitions ✅; no dynamic imports in Pothos files ✅; scalar referenced by name (never re-registered) ✅; no `console.*` ✅; locale propagation N/A at the object tier (fields are pure projections; the resolver propagates `ctx.locale` to the service). Sub-loop discovery's per-file instruction mapping printed no additional binding rule set beyond the ones read.

## 5. Deviations

1. **No type/field descriptions on the eleven objects** — interpretation note: the task phrase "descriptions per the SDL contract verbatim" resolves to the empty set, because the authoritative SDL block contains no descriptions; the only prescribed description in the whole contract is the root query's (shipped verbatim in Task 3.2). The regenerated artifact therefore carries no description comments on the eleven types — an exact match, not an omission.
2. **List resolves spread `readonly` arrays** (`[…parent.gatewayRevenueByCurrency]` etc.) — the canonical members are `readonly T[]` and the in-bundle convention for readonly list members is the spread copy; a direct `parent => parent.x` return was avoided to stay on the shipped pattern. No behavior difference (GraphQL serializes both identically).

## 6. Carry-forward for Task 3.2/3.3

- Object export names (import from `@/backend/graphql/pothos/admin`): `PlatformAnalyticsPothosObject` (root — the query field's type) plus `PlatformAnalyticsUsersPothosObject`, `PlatformAnalyticsSessionsPothosObject`, `PlatformAnalyticsRevenuePothosObject`, `PlatformAnalyticsCurrencyRevenuePothosObject`, `PlatformAnalyticsSubscriptionsPothosObject`, `PlatformAnalyticsTeachersPothosObject`, `PlatformAnalyticsRatingsPothosObject`, `PlatformAnalyticsHealthPothosObject`, `PlatformAnalyticsSessionTrendPointPothosObject`, `PlatformAnalyticsRevenueTrendPointPothosObject`.
- The wire shape Task 3.3 must prove: every top-level section key present exactly once; the two `Float` averages present-and-nullable; `generatedAt`/`bucketStart` parse as DateTime-format strings; trend arrays are arrays; NO `id` selectable anywhere in the subtree (validation must reject it).
- `frontend/graphql/generated/gql/graphql.ts` gains analytics operation types only when a frontend document selects them (Task 4.x) — codegen is already verified green against the regenerated SDL.

*End of Task 3.1 outcome.*
