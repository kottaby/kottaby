# Task 7.2 Outcome — AGENTS.md & Adjacent-Doc Propagation

**Feature:** `dev3-022c-platform-analytics-dashboard`
**Agent:** Knowledge Propagation Subagent (7.1+7.2)
**Date:** 2026-09-05
**Mode:** Propagation updates — FOUR files modified, all additive insertions; three files confirmed untouched; no commit made; tasks.md untouched.

---

## 1. Propagation files (each: change, placement, proof)

### 1.1 `backend/services/AGENTS.md` — analytics read-model rule

- **Change (+1 line, 0 deletions):** one new bullet in the admin-services rule block, placed directly after the sibling "Admin audit trail (read surface)" bullet (the other admin read-surface entry, same bullet format):
  > **Platform analytics (admin read model):** `PlatformAnalyticsService.getPlatformAnalytics` composes fresh per-request aggregate snapshots — the database is read on EVERY request (never cached, never memoized), the happy path writes ZERO rows and emits ZERO audit/notification records, and the governed-reader re-check runs at the service tier via the shared admin gate (`assertActorAdminActive` as the FIRST statement). See `docs/admin/platform-analytics.md` for the metric contracts, money-as-string rules, and read-purity/no-audit rule.
- Covers all four mandated rule elements (fresh per-request aggregates / never cached / never audited / governed-reader re-check at service tier) + the canonical-doc pointer. Truthful against the shipped service (gate at `platform-analytics.service.ts:134-135`; zero-write silence per the file header + digest-purity tests).
- Proof: `git diff --numstat` = `1 0`; added-line plan-artifact grep = 0.

### 1.2 `frontend/graphql/AGENTS.md` — embedded-type list gains the eleven names

- **Change (+21 lines, 0 deletions):** the "Current embedded types" list (§ Embedded type normalization policy) gains ALL ELEVEN new type names in the list's existing format — `` - `TypeName` (fields: …) — no `id`. `` with the root entry carrying the extra semantics note (cached inline under `Query.adminPlatformAnalytics`, replaced wholesale per refetch, pointer to the canonical doc):
  `PlatformAnalytics`, `PlatformAnalyticsUsers` (11 counters incl. `recentlyActive24h`), `PlatformAnalyticsSessions` (10 incl. `awaitingConfirmation`), `PlatformAnalyticsRevenue`, `PlatformAnalyticsCurrencyRevenue`, `PlatformAnalyticsSubscriptions` (7 incl. `activeInWindowNow`), `PlatformAnalyticsTeachers`, `PlatformAnalyticsRatings` (the two nullable averages marked), `PlatformAnalyticsHealth`, `PlatformAnalyticsSessionTrendPoint`, `PlatformAnalyticsRevenueTrendPoint`.
- Field lists verified against the generated SDL (`frontend/graphql/generated/schema.graphql:502-590`) and against the Task 4.2 cache registrations (`keyFields: false` × 11). Closes the routing note left open by the 4.2 outcome ("the AGENTS.md list-side edit is owned by the later documentation task").
- Proof: `git diff --numstat` = `21 0`; every added line is a list item inside the existing list (additive; the closing "If you add a new GraphQL type…" instruction untouched below).

### 1.3 Root `AGENTS.md` — Important References gains the canonical-doc line

- **Change (+1 line, 0 deletions):** one bullet inserted in the Important References section directly after the `docs/admin/audit-trail.md` line (keeps the admin canonical-doc cluster together; same entry style as its siblings):
  > `docs/admin/platform-analytics.md` — Platform analytics canonical reference (DEV3-022c: zero-argument `adminPlatformAnalytics` whole-platform snapshot, single-captured-instant + UTC boundary rulings (… incl. the 31-day-touch vs 30-bucket skeleton note), metric definitions per enum member, money-as-string + currency containment + honest-EMPTY arrays, offline-activation honesty split, honest-null rating averages, governed-admin service-tier denial via `assertActorAdminActive`, statement-level snapshot-consistency ruling, read-purity/no-audit rule, "what NOT to do" list, journey-suite behavioral contract)
- Proof: `git diff --numstat` = `1 0`; added-line plan-artifact grep = 0.

### 1.4 `docs/admin/user-management.md` — the ONE consumer-pointer line (decision recorded)

- **Decision:** the doc was NOT left byte-identical; **EXACTLY ONE** consumer-pointer line was added (the REQ-081-sanctioned option), because the analytics surface consumes the DEV3-016 substrate (`getStats`) and a pointer there prevents future readers from forking the counters. The line lives in §7 Related Documents (the doc's reference list — the least contract-bearing section), immediately after the types-consolidation entry:
  > Platform analytics consumer: the platform-analytics read model reuses `AdminUserRepository.getStats` verbatim for its users section (the ten directory counters, consumed unchanged — see `docs/admin/platform-analytics.md`).
- **No contract restated** (no getStats internals, no directory contract, no audit rules duplicated); the user-management doc's substantive sections are byte-identical.
- Proof: `git diff --numstat` = `1 0`. This also completes the Task 5.2 differential gate note ("`docs/admin/user-management.md` → EMPTY … consumer-pointer decision deferred to Task 7.2") — the diff is now exactly the one sanctioned line.

## 2. Untouched-files confirmation

| File | Required state | Verified |
|---|---|---|
| `docs/notifications/realtime-engine.md` | untouched | ✅ `git diff --stat` over the path = EMPTY; absent from `git status` |
| `shared/AGENTS.md` | untouched | ✅ same |
| `docs/specs/state-machine-invariants.md` | untouched | ✅ same |

`git status --porcelain` for the whole task: `M AGENTS.md`, `M backend/services/AGENTS.md`, `M docs/admin/user-management.md`, `M frontend/graphql/AGENTS.md`, `?? docs/admin/platform-analytics.md` (+ the pre-existing untracked `.browser-auth-student/` dir from Task 4.5 — not this task's).

## 3. Quality loop (7.2.QL)

| File | `sub-loop --lifecycle duplicates` | Fallback `bunx @biomejs/biome check` |
|---|---|---|
| `AGENTS.md` (root) | markdown short-circuit: discovery + tsgo pass, oxlint "No files found to lint" (pre-existing runner behavior for `.md` paths — identical on the unmodified control `docs/admin/audit-trail.md`) | `Checked 0 files … No fixes applied.` (markdown not in biome's file set) — exit recorded, no diagnostics possible |
| `backend/services/AGENTS.md` | same short-circuit | same |
| `frontend/graphql/AGENTS.md` | same short-circuit | same |
| `docs/admin/user-management.md` | same short-circuit | same |
| `docs/admin/platform-analytics.md` (7.1 file) | same short-circuit | same |

The markdown short-circuit is a REPO-RUNNER limitation, not a content failure (verified against an unmodified control doc; biome.json's file set excludes markdown; no markdown linter exists in `package.json`). Per the task's fallback instruction the biome runs were executed and recorded. All `tsgo` runs inside the sub-loops reported no project errors.

**Type check:** `bunx tsgo -b --noEmit 2>&1 | grep -c "error TS"` → **0** (grep exit 1 = zero matches) — clean-zero baseline parity maintained.

## 4. Semantic review (7.2.SR)

1. **Additive & truthful:** all four diffs are pure insertions (`numstat` deletions = 0 in every file); every propagated statement was verified against shipped code or the generated SDL before writing; nothing pre-existing was reworded or removed.
2. **No layer contradicts the canonical doc:** the services rule (fresh/never-cached/never-audited/service-tier governed re-check) restates §3.1/§6/§5 of `docs/admin/platform-analytics.md`; the embedded-type list restates §2 of it (eleven no-`id` types, `keyFields: false` posture); the root reference and the user-management pointer are pure pointers. No AGENTS.md layer asserts anything the canonical doc does not.
3. **Clean language:** added-line grep over all four modified files for `\.ai/plans|tasks\.md|RULING|Task [0-9]|plan\.md` → 0 matches (the root AGENTS.md's pre-existing `ai/plans/dev3-002…` BLT-03 pointers are untouched pre-existing lines, not added by this task).
4. **Scope boundary:** no code files, no schema, no tests, no tasks.md, no deferred-items.md touched; no commit made.

*End of Task 7.2 outcome.*
