# Task 4.2 Outcome — Apollo Cache Registration

**Task ID:** 4.2 (Apollo cache registration — `frontend/providers/apollo/apolloCache.ts`)
**Agent:** Apollo Cache Subagent
**Date:** 2026-09-04
**Branch:** `main` @ `1ca9805` (phase-3b wire-matrix commit; read-only `git` only — no branch moves, no commits made by this subagent)
**Mode:** Registration UPDATE ONLY — exactly two files touched; no commit; `tasks.md` untouched.

---

## 1. Summary

- Registered **ALL ELEVEN** new embedded types in `createApolloCache()` with `keyFields: false`: `PlatformAnalytics`, `PlatformAnalyticsUsers`, `PlatformAnalyticsSessions`, `PlatformAnalyticsRevenue`, `PlatformAnalyticsCurrencyRevenue`, `PlatformAnalyticsSubscriptions`, `PlatformAnalyticsTeachers`, `PlatformAnalyticsRatings`, `PlatformAnalyticsHealth`, `PlatformAnalyticsSessionTrendPoint`, `PlatformAnalyticsRevenueTrendPoint` — each verified against the generated SDL (`frontend/graphql/generated/schema.graphql:502-590`): every one is a scalar-only object with **no `id` field anywhere in the aggregate**, exactly the `frontend/graphql/AGENTS.md` embedded-type policy trigger.
- The registered policy surface is now **EIGHTEEN entries** — the pre-existing `AdminDashboardScheduleResult` `rows: { merge: false }` policy (unchanged) plus **seventeen `keyFields: false`** entries (six pre-existing + eleven new). No new merge policy was added.
- **D13-style reconcile executed against the LIVE tree, with a stale-drift finding:** the card's drift description ("pinned list pins five names / omits `NotificationListPage` while the file registers six") did NOT match the working tree — the pinned inventory already reconciled to the real live SEVEN (`AdminAuditLogPage`, `AdminDashboardScheduleResult`, `AdminNoteInfo`, `HandshakeCodeLookup`, `HealthCheck`, `NotificationListPage`, `OnlineMeetingInfo`), i.e. the card's arithmetic was authored before `AdminAuditLogPage` was registered (card precedent list omits it). Per the D13-style duty (recompute verbatim against the LIVE state, "one honest regeneration, zero carried fiction"), the frozen inventory was recomputed against the live post-change registration set → **eighteen** pinned names, not the card's predicted seventeen (pinning seventeen was impossible without shrinking a live registration or omitting a new type — both forbidden by the extend-never-shrink rule and the "register ALL eleven" mandate).
- Test file updated in the SAME change set: frozen-inventory assertion recomputed to the eighteen live names (sort via `localeCompare`, so the pin is order-independent), header docs updated (suite scope + frozen count), plus ONE new sweep test pinning `keyFields === false` for all eleven new types (the test-side expression of the "no policy beyond `keyFields: false`" review duty). Baseline 11 pass → **12 pass / 0 fail**.
- Gates: sub-loop exit 0 on both files; suite green; full-repo `tsgo -b --noEmit` → **0** `error TS` lines; `git diff --name-only` under `frontend/providers/apollo/` = exactly the two target files.

## 2. Files modified / NOT modified

**Modified (the complete scope):**
- `frontend/providers/apollo/apolloCache.ts` — header JSDoc extended (embedded-types enumeration now includes the eleven-type family + a family paragraph: one root `adminPlatformAnalytics` read model, scalar-only snapshots, read back embedded, replaced wholesale per refetch); eleven `keyFields: false` entries appended as one family block after `OnlineMeetingInfo` with a family-level comment stating the domain contract and the ordering convention.
- `frontend/providers/apollo/apolloCache.test.ts` — header title/scope + frozen-count updated (seven → eighteen); `PLATFORM_ANALYTICS_EMBEDDED_TYPES` const (the eleven names in registration order); new sweep test `all eleven PlatformAnalytics embedded types keep keyFields:false`; frozen-inventory test recomputed to the eighteen verbatim live names.

**Files NOT modified, and why:**
- `frontend/graphql/AGENTS.md` — its "Current embedded types" list gains the eleven names per plan knowledge-propagation, but that edit is owned by a later documentation task (tasks.md assigns "UPDATE `frontend/graphql/AGENTS.md` — embedded-type list gains the eleven new type names" outside this ticket's 2-file scope). Reported here so the orchestrator routes it.
- `frontend/graphql/generated/gql/graphql.ts`, `frontend/graphql/sharedDocuments/admin/index.ts`, `frontend/graphql/sharedDocuments/admin/platform-analytics.documents(.test).ts` — present as in-flight working-tree changes of the CONCURRENT Task 4.1 execution (absent from this task's start-of-work `git status`); never touched by this subagent.
- `tasks.md`, `deferred-items.md` — orchestrator-owned; untouched. No new deferral created (nothing was deferred).

## 3. Verification commands + results

| Command | Result |
|---|---|
| `bun run scripts/health/sub-loop.ts frontend/providers/apollo/apolloCache.ts --lifecycle duplicates` | **exit 0** — tsgo ✅, oxlint ✅, biome:check ✅, lint:type-aware ✅, check:duplicates ✅ ("All checks … passed") |
| `bun run scripts/health/sub-loop.ts frontend/providers/apollo/apolloCache.test.ts --lifecycle duplicates` | **exit 0** — all gates ✅ (check:duplicates skipped as outside jscpd scan scope — pre-existing behavior for this path) |
| `bun run test/scripts/run-test.ts frontend/providers/apollo/apolloCache.test.ts` | **12 pass / 0 fail** (35 expect calls) — baseline before edit: 11 pass / 0 fail (24 expect calls), pinned to the live seven |
| `bunx tsgo -b --noEmit 2>&1 \| grep -c "error TS"` | **0** — full-repo clean (run with the sibling Task 4.1 in-flight files present in the tree) |
| `git diff --name-only -- frontend/providers/apollo/` | exactly `apolloCache.ts` + `apolloCache.test.ts` — 2-file scope honored |
| Baseline SDL verification | `schema.graphql:502-590` — all eleven `type PlatformAnalytics*` objects present, zero `id` fields across the aggregate (embedded-policy precondition confirmed before registration) |

Sub-loop discovery for this path maps: root `AGENTS.md`, `frontend/AGENTS.md`, and the nonexistent `.github/instructions/frontend.instructions.md` (⚠ marker — the operative instruction file is `.agents/instructions/frontend.instructions.md`, read in full). `frontend/graphql/AGENTS.md` is a SIBLING branch of the discovery walk (not an ancestor of `frontend/providers/apollo/`), so it was read manually per the task card's 4.2.IV mandate — it governs, even though the sub-loop does not print it for this path.

## 4. Semantic review — item-by-item (4.2.SR / 4.2.SEC / 4.2.IV)

1. **Only `keyFields: false` entries added; no new merge policies** — all eleven new entries are exactly `{ keyFields: false }`; the file's ONLY merge policy remains the pre-existing `AdminDashboardScheduleResult.rows` (byte-identical, pinned by the sibling-regression test). The new sweep test additionally locks each of the eleven at `keyFields === false`, so a future flip to a keyed policy would red the suite.
2. **Deterministic ordering** — the eleven entries are appended as ONE family block after the last existing entry (`OnlineMeetingInfo`), in the read model's own shape order (root first, then the section objects, then the two trend-point types) — a fixed, repeatable order stated in the family comment; the file's pre-existing convention is arrival-order append (its current order is NOT alphabetical), so appending preserves convention while the documented shape order makes the block internally deterministic. The frozen-inventory pin sorts keys (`localeCompare`) before comparing, so the test is order-independent by construction.
3. **No cross-user cache bleed possible (embedded-only)** — `keyFields: false` means NO normalized entity key can ever exist for any `PlatformAnalytics*` type: values are stored inline under `Query.adminPlatformAnalytics` and replaced wholesale per refetch. There is no per-user argument on the query (single admin-gated aggregate, one snapshot instant `generatedAt`), and with no id-derived cache keys there is no mechanism by which one actor's snapshot could be re-read under another's context. Nothing cacheable-by-id exists on this subtree (4.2.SEC satisfied by construction).
4. **Clean comments** — domain-only: the family comment states the no-`id` precondition, the inline-under-root read path, the wholesale-replace semantics, and the ordering convention; no plan/task/REQ/phase/§ artifacts (grep over both files for `REQ-[0-9]|Task [0-9]|tasks\.md|specs\.md|plan\.md|RULING|Phase [0-9]|dev3-|§|\.ai/` → **zero matches**); no trivial per-field restatements; existing comments untouched.
5. **Scope boundary** — exactly two files (git-verified); no lint suppressions; no `console.*`; no `any`/unsafe casts introduced (test reuses the file's established guard-and-throw `Reflect.get` narrowing helpers — the new sweep adds zero casts).
6. **Reconcile honesty (D13-style)** — the pinned inventory is a verbatim recompute of the LIVE registration set (not the card's stale 17-name prediction); the discrepancy is recorded in §1/§7 rather than silently patched, per "one honest regeneration, zero carried fiction".
7. **Extend, never shrink** — pre-existing seven registrations all keep their exact posture (six `keyFields: false` + the merge policy); the sibling-regression pins and behavioral Health/Handshake halves are untouched and still green.

## 5. Instruction verification (4.2.IV)

| Rule file | Relevant rule | Compliance |
|---|---|---|
| `frontend/graphql/AGENTS.md` (embedded-type policy — read per card mandate) | "Types that lack an `id` field … must opt out of normalization via `keyFields: false` in `frontend/providers/apollo/apolloCache.ts` … If you add a new GraphQL type without an `id` field, add it to `typePolicies` … and list it here." | All eleven types verified `id`-less in the generated SDL, registered with `keyFields: false` ✅; the AGENTS.md list-side edit is owned by the later docs task and reported to the orchestrator (Fix-Or-Report: cross-file change → report, don't edit out of scope) ✅ |
| `frontend/AGENTS.md` | Apollo/GraphQL conventions (shared client instance, no `useLazyQuery`, error-link seam) | Cache-config-only change; no hook/document/error-surface code touched ✅ |
| `.agents/instructions/frontend.instructions.md` | Apollo imports from `@apollo/client` / `@apollo/client/react`; no mapping layers; code style (no nested ternaries); `bun quality-gate` discipline | File already imports `InMemoryCache` from `@apollo/client` (unchanged); no runtime logic added; sub-loop (the file-scoped quality gate) exit 0 on both files ✅ |
| Root `AGENTS.md` (discovered) | project conventions / lint discipline | No `oxlint-disable`, no suppressions; sub-loop lint gates green ✅ |

## 6. Cross-file dependencies & observations

- **Concurrent sibling work:** Task 4.1's document/codegen files appeared in the working tree during this execution (start-of-work `git status` showed only `tmp-scratch/`); they were never touched here. `tsgo` and the suite were re-run AFTER they appeared — still clean, so the two tickets compose without conflict.
- **Registration count ledger:** registered policies 7 → **18** (17 × `keyFields: false` + 1 merge policy). The frozen pin and the suite header now say eighteen; any future embedded type extends to nineteen with the same recompute discipline.
- **Plan §5 knowledge-propagation routing:** `frontend/graphql/AGENTS.md` "Current embedded types" list still lacks the eleven names — owed by the later docs task (explicitly out of this ticket's 2-file scope).

## 7. Carry-forward

- **For Task 4.4: none blocking — embedded types are transparent to the query.** The container/documents task needs no cache-side action: `keyFields: false` changes no read/write API, the `adminPlatformAnalytics` root needs no `id` selection, and the whole snapshot replaces cleanly per refetch. Two confirmations for 4.4's sweep gate: (a) `apolloCache.test.ts` stays green as-is for the 4.4 multi-suite run; (b) the generated `PlatformAnalyticsQuery` types arriving via Task 4.1's codegen are operation-derived and unaffected by these policies.
- **For the orchestrator (routing, not a deferral):** the `frontend/graphql/AGENTS.md` embedded-type list edit (eleven names) belongs to the later documentation task per tasks.md; do not double-assign.

*End of Task 4.2 outcome.*
