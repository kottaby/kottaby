# Task 2.6 — Service test suite outcome (REBUILD)

> Rebuild note: authored after the service commit (`68db51c`) — the test-first
> inversion is recorded honestly. This suite completes task 2.6.TE's final step:
> the journey suite re-ran GREEN immediately after (10/10, see
> `2.1-2.4-journeys-outcome.md`).

## What was done

- CREATE `backend/services/admin/platform-analytics.service.test.ts` (~520 lines,
  12 tests, 266 assertions) — four tiers per plan task 2.6.TE:

  - **Tier 1 — actor matrix** (real DB, `runInRollback`): `actorId` 0/−1/NaN/2.5 →
    `UnauthorizedError(t.unauthorized)`; absent row → UnauthorizedError;
    student/teacher/parent role rows → `ForbiddenError(t.forbidden)`; governed
    admins in the deterministic deleted → blocked → suspended order with the
    MATCHING translated messages (`accountDeleted`/`accountBlocked`/
    `accountSuspended`; suspension probed on a non-deleted, non-blocked admin);
    every denial logs exactly ONE bounded `logDomainError` `{ code, entity:
    "users", entityId, locale }` (code UNAUTHORIZED vs FORBIDDEN pinned).
    All probes pass the rollback `tx` as `outerTx` so the PRE-TX actor gate sees
    the uncommitted actors.
  - **Tier 2 — snapshot composition**: every windowed repo method receives the
    SAME captured `now` instance, which IS `generatedAt` (object identity across
    all 6 windowed calls); distinct per-method sentinels prove the ten
    `getStats` fields flow through VERBATIM plus `recentlyActive24h` and that no
    section cross-wires; an explicit `outerTx` scopes the snapshot — REAL repo
    layer over the caller's savepoint tx sees tx-local (uncommitted) session and
    user rows exactly as a direct tx-side count oracle reports (`withTransaction`
    opens a SAVEPOINT, so the executor handle differs from the raw `outerTx`
    while the DATA visibility contract holds — assertion pins visibility, not
    handle identity).
  - **Tier 3 — trend-assembly chaos** (pure repo spies): sparse-empty → 30
    zero-filled UTC-midnight buckets + EMPTY revenue grid + empty per-currency
    rows; sparse-full → 1:1 bucket mapping without zero-fill distortion;
    multi-currency → 30×N grid with exact `"0"` fills, per-bucket byte-order
    currency (EGP < USD), and a rogue out-of-skeleton bucket ignored in BOTH
    trends.
  - **Tier 4 — pre-DB proof + purity**: on EVERY denial path (anonymous /
    malformed / absent / non-admin / governed) the ten analytics repo methods AND
    `AdminUserRepository.getStats` are called ZERO times; the happy path emits
    ZERO domain logs; a composite read leaves users/session/payments/
    subscriptions/teacher/audit/notifications row counts unchanged; injected repo
    rejections propagate UNMASKED (no try/catch swallowing — the thrown error is
    the SAME object identity).

## Verification

| Gate | Command | Result |
| --- | --- | --- |
| Typecheck | `bun tsgo` | exit 0 |
| Duplicates/health | `sub-loop.ts backend/services/admin/platform-analytics.service.test.ts --lifecycle duplicates` | exit 0 |
| Suite | `bun run test/scripts/run-test.ts backend/services/admin/platform-analytics.service.test.ts` | **12 pass / 0 fail**, 266 expect() calls |
| Repo suite re-run | `…repository.test.ts` | 23 pass / 0 fail |
| Journey suite re-run | `…platform-analytics.journey.test.ts` | 10 pass / 0 fail (2.6.TE GREEN-flip step) |
| Baseline parity | `bun run biome:check` | exit 0 ("Checked 1088 files… Fixed 1 file" — the one fix was this suite's `useIterableCallbackReturn`) |
| Schema/reuse purity | `git diff -- backend/db/schema/ backend/db/migration/ backend/db/repo/admin/admin-user.repository.ts` | EMPTY |

## Files touched

- `backend/services/admin/platform-analytics.service.test.ts` (NEW only)

## Deviations

- Test-first inversion (rebuild) — recorded above.
- The outerTx test pins DATA visibility (tx-local rows readable through the
  composite read) instead of executor-handle identity, because
  `withTransaction(outerTx, …)` legitimately opens a SAVEPOINT child — the
  REQ-040 contract is "every read shares ONE tx", which the real-repo visibility
  proof establishes end-to-end.
- None otherwise.
