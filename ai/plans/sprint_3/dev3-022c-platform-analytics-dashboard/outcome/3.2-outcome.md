# Task 3.2 Outcome — `adminPlatformAnalytics` query + schema regeneration + baseline reconciliation

**Feature:** `dev3-022c-platform-analytics-dashboard`
**Branch note:** repo checked out on `main` at `e419280` — byte-identical to `feat/dev3-022c-platform-analytics-dashboard` (same commit; no branch switch performed — git-mutating commands forbidden for this subagent). The orchestrator commits the change set including the regenerated artifacts.
**Mode:** one query registration + two barrel/test reconciliations + artifact regeneration; no commit made.

---

## 1. Summary

- Created `backend/graphql/query/admin/platform-analytics.query.ts` — `gqlSchemaBuilder.queryField("adminPlatformAnalytics", t => t.field({ … }))` with `type: PlatformAnalyticsPothosObject`, the MANDATORY `authScopes: { $all: { authenticated: true, role: [UserRole.Admin] } }` conjunction (the ANY-semantics plain map would be a security defect), the verbatim closed-contract description, and a resolver that is the thin hive: pre-resolver `ctx.user` null-check belt throwing `UnauthorizedError(await ctx.t("errorsTranslations")).unauthorized` (TypeScript-narrowing only — the `authenticated` scope already denied anonymous callers with the same code), then `PlatformAnalyticsService.getPlatformAnalytics(ctx.user.id, ctx.locale)` and nothing else. NO try/catch, NO args, no third argument, no logic/types/SQL in the resolver.
- Appended the side-effect import to `backend/graphql/query/admin/index.ts` (`import "./platform-analytics.query";` — existing style; NO named exports in query files).
- Reconciled + extended `backend/graphql/test/sdl-static-assertions.test.ts`: `FROZEN_QUERY_FIELDS` gains `adminPlatformAnalytics` AND the full baseline is reconciled to the live artifact (baseline-repair duty D13: the bundled inventories predate the shipped session-lifecycle/wallet surfaces); `FROZEN_MUTATION_FIELDS` reconciled the same way; a new describe pins the root query signature and the per-type SDL field surface of every new type.
- Updated `backend/graphql/test/schema-surface.test.ts`: the query-additions and named-type-additions inventories gain `adminPlatformAnalytics` and the eleven type names (sorted); NO new enum joins; the `PRE_3_1_*` "contains" posture untouched.
- Ran `bun run generate:gqlSchema && bun codegen` — regenerated artifacts left in the working tree for the orchestrator to commit in the same change set. `plan-catalog.schema.test.ts` committed-vs-live SDL equality stays green.

## 2. Files created / modified

| File | Action | Notes |
|---|---|---|
| `backend/graphql/query/admin/platform-analytics.query.ts` | CREATE | one side-effect `queryField` registration; imports: `UserRole`, `PlatformAnalyticsPothosObject` (from the pothos/admin barrel), `gqlSchemaBuilder`, `UnauthorizedError`, `PlatformAnalyticsService` (from `@/backend/services`) |
| `backend/graphql/query/admin/index.ts` | MODIFY | appended `import "./platform-analytics.query";` |
| `backend/graphql/test/sdl-static-assertions.test.ts` | MODIFY | FROZEN reconcile+extend (see §3) + new analytics describe (4 tests) |
| `backend/graphql/test/schema-surface.test.ts` | MODIFY | +`DEV3_022C_QUERY_FIELDS`, +`DEV3_022C_TYPE_NAMES` folded into the two additions assertions |
| `frontend/graphql/generated/schema.graphql` | MODIFY (regenerated) | +96 lines: 11 object types + `adminPlatformAnalytics: PlatformAnalytics!` with the verbatim description; zero other changes |
| `frontend/graphql/generated/gql/graphql.ts` | REGENERATED, byte-identical | codegen ran clean; the operation/types module changes only when a frontend document selects the new fields (Task 4.x) — see Deviations §2 |

NOT modified: `backend/lib/gateway/public-operations.ts` (diff EMPTY — frozen six intact, analytics NOT public), `backend/graphql/test/handshake-code-surface.test.ts` (diff EMPTY, green), `backend/lib/gateway/route-inventory.ts` + its tests (ROUTE_INVENTORY unchanged — no `app/api/**` route added, git status of `backend/lib/gateway/` empty), `app/api/**` (zero changes), `tasks.md`, every service/repo/type file.

## 3. Baseline reconciliation (the mandated duty) — exact deltas

The bundled inventories were stale against the LIVE schema (both root-set tests failed PRE-CHANGE with exactly the deltas below — verified by running the suite before editing). Reconciled verbatim, monotonic, nothing dropped:

- `FROZEN_QUERY_FIELDS` (exact-equality vs the artifact's Query root): **+5 reconcile** — `adminDisputedSessions`, `myStudentSessions`, `myTeacherSessions`, `myWallet`, `sessionById` (shipped session-lifecycle + wallet reads the inventory predated); **+1 extend** — `adminPlatformAnalytics`. 18 → 24 entries. Note: the literal is stored in `localeCompare` order (the assertion sorts only the received side), which places `adminPlatformAnalytics` AFTER `adminPlans` (collation, not byte order).
- `FROZEN_MUTATION_FIELDS`: **+8 reconcile** — `cancelSession`, `completeSession`, `confirmSessionCompletion`, `createSession`, `openSessionDispute`, `requestWithdrawal`, `resolveSessionDispute`, `startSession`. 18 → 26 entries.
- `schema-surface.test.ts`: the live `PRE_3_1_*` arrays were ALREADY reconciled to the shipped admin/session/wallet surface (the suite was green pre-change — its baseline comment records the earlier cold-start/broadcast/audit fold-ins), so no reconcile was needed there; the additions assertions gained only this ticket's names: `DEV3_022C_QUERY_FIELDS = ["adminPlatformAnalytics"]` and `DEV3_022C_TYPE_NAMES` = the eleven `PlatformAnalytics*` object names (sorted). `PRE_3_1_ENUMS` + the enum-set test UNTOUCHED (no new enum). The `PRE_3_1_*` contains-posture loops untouched.
- New artifact-side pins in `sdl-static-assertions.test.ts` (describe "Platform analytics surface (extend) — artifact-side pins"): (1) `adminPlatformAnalytics: PlatformAnalytics!` NON-NULLABLE with ZERO arguments; (2) all eleven objects exist and expose EXACTLY the contracted field surface — a name→SDL-type map per type (including the two nullable `Float` averages and the `DateTime!` instants), checked name-set AND type per field; (3) NO `id` field on any of the eleven types (aggregate anonymity); (4) every instant rides `DateTime` (`generatedAt` + both `bucketStart`s) with a belt-and-braces scan proving the only `String` leaves are `amount`/`currency`/`last30DaysAmount`/`totalAmount` (money + currency codes, zero String timestamps).

## 4. Verification results (all gates)

| Command | Result |
|---|---|
| `bun run test/scripts/run-test.ts backend/graphql/test/sdl-static-assertions.test.ts` | **33 pass / 0 fail** (185 expect calls) — was 27 pass / 2 fail pre-change (the two stale root-set tests, per the mandated reconcile) |
| `bun run test/scripts/run-test.ts backend/graphql/test/schema-surface.test.ts` | **46 pass / 0 fail** (347 expect calls) — green pre- and post-change |
| `bun run test/scripts/run-test.ts backend/graphql/test/handshake-code-surface.test.ts` | **22 pass / 0 fail** (44 expect calls) — untouched file; frozen-six parity intact |
| `bun run test/scripts/run-test.ts backend/graphql/test/plan-catalog.schema.test.ts` | **5 pass / 0 fail** (22 expect calls) — includes the committed-`schema.graphql` ↔ live `printSchema(lexicographicSortSchema(...))` byte-equality test against the regenerated artifact |
| `bunx tsgo -b --noEmit 2>&1 \| grep -c "error TS"` | **0** |
| `bun run test/scripts/run-test.ts backend/services/admin/platform-analytics.service.test.ts` | **16 pass / 0 fail** (434 expect calls) — service untouched, regression sanity |
| `bun run scripts/health/sub-loop.ts backend/graphql/query/admin/platform-analytics.query.ts --lifecycle duplicates` | **exit 0** (all five gates) |
| `bun run scripts/health/sub-loop.ts backend/graphql/query/admin/index.ts --lifecycle duplicates` | **exit 0** |
| `bun run scripts/health/sub-loop.ts backend/graphql/test/sdl-static-assertions.test.ts --lifecycle duplicates` | **exit 0** |
| `bun run scripts/health/sub-loop.ts backend/graphql/test/schema-surface.test.ts --lifecycle duplicates` | **exit 0** |
| `bun run generate:gqlSchema && bun codegen` | exit 0 both; artifacts regenerated (schema.graphql +96 lines; gql/graphql.ts byte-identical) |
| `git status --porcelain` | exactly: M `pothos/admin/index.ts`, M `query/admin/index.ts`, M both test files, M `frontend/graphql/generated/schema.graphql`, ?? the two new source files (+ pre-existing `tmp-scratch/`) — scope boundary honored |

## 5. Semantic review — item-by-item

1. **Resolver is the thin hive** — the resolver body is the null-check belt + ONE service call passing ONLY `(ctx.user.id, ctx.locale)`; zero logic, zero local types, zero SQL, zero repository access; the service re-gates (defense-in-depth) so governed admins are denied at the service tier exactly as the contract prescribes.
2. **`$all` conjunction verified load-bearing** — `authScopes: { $all: { authenticated: true, role: [UserRole.Admin] } }`; the builder's scope map throws `UnauthorizedError` on the `authenticated` miss (401 pre-resolver) and maps role misses to the localized `ForbiddenError` (403 pre-resolver); a plain key-map would have combined with ANY semantics — the `$all` shape is copied from the shipped admin-query precedent verbatim.
3. **Zero-argument surface** — no `args` block at all; any argument in a client document is a `GRAPHQL_VALIDATION_FAILED` pre-resolver (closed input surface; nothing steerable).
4. **Localized pre-resolver error** — `await ctx.t("errorsTranslations")` + `tErrors.unauthorized` (the same bound-locale pattern the shipped `audit-trail`/`parent-link`/notification resolvers use); no hardcoded English denial string, no `getServerTranslations`/`next-intl` import.
5. **NO try/catch** — service `DomainError`s propagate to the single boundary finalizer; zero new error codes (the denial surface is `UNAUTHORIZED`/`FORBIDDEN` only).
6. **Codegen ↔ Pothos consistency** — the regenerated SDL contains exactly the eleven types + the root field with the verbatim description, and the committed-vs-live equality test is green; no input/enum/mutation/no other root field changed in the artifact diff.
7. **Clean comments — ZERO plan-artifact refs in ADDED lines** — strict grep over the two new files = NO MATCHES; the git-diff added-lines scan over both test files = NO MATCHES (the two test files carry pre-existing ticket-style comments from earlier reconciles, untouched).
8. **Scope boundary** — six source/artifact paths + two new files, nothing else (git-verified); `public-operations.ts`, `handshake-code-surface.test.ts`, `route-inventory*`, `app/api/**` all diff-empty; the frozen six re-read and confirmed (`login`, `refreshToken`, `logout`, `registerUser`, `recitationReadings`, `_health`).
9. **Instruction verification** — `backend/graphql/query/AGENTS.md` (side-effect import added to the sub-directory barrel; no direct import of the query file anywhere), `backend/graphql/AGENTS.md` + `.agents/instructions/backend.instructions.md` (locale propagation ✅, DomainError-only throws ✅, services-layer delegation ✅, no dynamic imports ✅), `docs/graphql/error-handling-contract.md` (no masking/finalizer work in the resolver; `UNAUTHORIZED`≠`FORBIDDEN` preserved end-to-end).

## 6. Deviations / observations

1. **`FROZEN_MUTATION_FIELDS` reconcile executed** — the task card named `FROZEN_QUERY_FIELDS` explicitly; the governing reconcile duty (plan §3.5 + the D13 baseline-repair decision) mandates covering BOTH frozen root inventories, and the mutation baseline was stale by the same mechanism (+8 shipped session-lifecycle/wallet mutations). Both arrays now equal the live artifact.
2. **`gql/graphql.ts` byte-identical after codegen** — the generated operation/types module only emits types for operations that exist in frontend documents; no frontend document selects the analytics surface yet (the dashboard views are Task 4.x). The artifact is regenerated and consistent; the analytics operation types will materialize when the first document lands. Codegen itself exits 0.
3. **`schema-surface.test.ts` needed no reconcile** — the plan's §3.5 note ("the additions assertions … also omit the shipped admin surface") described a state that had already been repaired on this branch (the suite was green pre-change with the admin/session/wallet surface enrolled in its baseline). Only this ticket's additive names were folded in; nothing was re-reconciled that was not stale.
4. **`FROZEN_QUERY_FIELDS` literal stored in `localeCompare` order** — the exact-equality assertion sorts only the received side, so the literal must carry the same collation order; `adminPlatformAnalytics` sorts after `adminPlans` under `localeCompare` (not byte order). Recorded so the next extend does not "fix" it back to byte order.
5. **Pre-change baseline evidence preserved** — the two sdl-static root-set tests failed PRE-CHANGE with exactly the reconcile deltas listed in §3 (5 missing queries / 8 missing mutations), proving the reconcile was mandated drift repair, not assertion weakening.

## 7. Carry-forward for Task 3.3 (GraphQL wire matrix)

- **Query name/type (exact):** `adminPlatformAnalytics: PlatformAnalytics!` — ZERO arguments; description pinned verbatim on the artifact (`"""Admin-only whole-platform analytics snapshot (…) Governed admins are denied at the service tier."""`).
- **SDL shape to prove (closed):** root `PlatformAnalytics` with EXACTLY `generatedAt: DateTime!`, `users`, `sessions`, `revenue`, `subscriptions`, `teachers`, `ratings`, `health` (sections `!`), `sessionTrendDaily: [PlatformAnalyticsSessionTrendPoint!]!`, `revenueTrendDaily: [PlatformAnalyticsRevenueTrendPoint!]!`; eleven type names `PlatformAnalytics{,Users,Sessions,Revenue,CurrencyRevenue,Subscriptions,Teachers,Ratings,Health,SessionTrendPoint,RevenueTrendPoint}`; ONLY nullable members anywhere in the subtree: `ratings.averageSessionRating: Float` + `ratings.averageEvaluationScore: Float`; money leaves are `String!` (`totalAmount`, `last30DaysAmount`, `amount`); `bucketStart: DateTime!` on both trend points; NO `id` anywhere (selecting it must fail validation).
- **Denial observables:** anonymous → `UNAUTHORIZED` PRE-resolver (resolver spy never invoked — the `authenticated` scope throws first); student/teacher/parent → `FORBIDDEN` PRE-resolver (role-scope miss, resolver never invoked); ANY argument → `GRAPHQL_VALIDATION_FAILED` pre-resolver; active admin → full closed-shape snapshot (exactly one service call — the resolver passes only `ctx.user.id` + `ctx.locale`); governed admin (deleted/blocked/suspended) → `FORBIDDEN` at the SERVICE tier (resolver DOES run; the localized message comes from `assertActorAdminActive`, deterministic deleted → blocked → suspended). Zero new error codes; scope snapshot readable off `pothosOptions.authScopes` as `{ $all: { authenticated: true, role: [UserRole.Admin] } }` (handshake-code-surface introspection pattern).
- **Public-operations parity:** `adminPlatformAnalytics` is ABSENT from `PUBLIC_OPERATION_NAMES` (frozen six untouched) — the allowlist-agreement posture tests stay green without edits.

*End of Task 3.2 outcome.*
