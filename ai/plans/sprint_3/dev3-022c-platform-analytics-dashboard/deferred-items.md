# Deferred Items Ledger

**Feature:** `dev3-022c-platform-analytics-dashboard`  
**Plan Directory:** `ai/plans/sprint_3/dev3-022c-platform-analytics-dashboard`  
**Created:** `2026-08-31`

---

## Purpose

This ledger tracks all work deferred from one task to another to ensure no deferred item is forgotten. Every deferred item must be explicitly logged here and resolved before the plan is marked complete.

---

## Ledger Table

| ID | Deferred Item | Source Task | Target Task | Status | Verified By | Notes |
|---|---|---|---|---|---|---|
| DI-0.2-1 | Plan D12 / Task 4.4.TE claim correction: `test/ui/components/admin/` ALREADY EXISTS (4 suites: `AdminDisputesContainer.suite.tsx`, `AdminDisputesContainer.test.tsx`, `AuditTrailView.test.tsx`, `BroadcastComposeContainer.test.tsx`) — the Task 4.4 component test `PlatformAnalyticsContainer.test.tsx` is an ADD into the existing directory, NOT a directory CREATE. `frontend/views/admin/analytics/` and `app/(dashboard)/admin/analytics/` remain genuinely net-new. | 0.2 | 4.4 | ⚠️ Partial | Task 0.2 verification sweep (2026-08-31) | Non-blocking — no scaffold work owed; `test/ui/AGENTS.md` already governs the existing dir. RESOLVED by prompt amendment to Task 4.4 (ADD into existing dir, not a directory CREATE) — flip to ✅ when 4.4 completes. |
| D-1 | Server-side metric caching variant (platform analytics aggregate queries) | 0.1 | Future performance ticket | 📅 Forward | plan §7 | Pre-registered forward-owned item; explicitly out of scope for this plan, owned by a later performance ticket |
| D-2 | Drill-down/detail pages + CSV export | 0.1 | Future UX ticket | 📅 Forward | plan §7 | Pre-registered forward-owned item; dashboards ship without drill-down/CSV, owned by a later UX ticket |
| D-3 | Bespoke analytics rate limiter | 0.1 | Rate-limiting hardening stream (REQ-038) | 📅 Forward | plan §7 | Pre-registered forward-owned item; no analytics-specific limiter in this plan, owned by the rate-limiting hardening stream |
| D-4 | Trend covering index | 0.1 | Deferred until production telemetry demands it | 📅 Forward | plan §7 | Pre-registered forward-owned item; no speculative index added (read-only schema ticket REQ-043), revisit only on production telemetry evidence |
| D-5 | Repo-suite fixtures TZ-sensitive on non-UTC hosts (7 fixture-path failures under TZ=America/New_York; trend parity holds; sanctioned test hosts run UTC) — entity-setup timestamp normalization follow-up | 2.M | future test-hardening ticket | 📅 Forward | 2.M midpoint review | Non-blocking for this plan; test matrix runs UTC |
| DI-0.3-1 | **Journey teardown vs payment-immutability triggers (REQ-074 / plan §4.4 / tasks 1.3+2.2+2.4).** `student_payments` AND `teacher_transaction` are trigger-immutable — UPDATE **and** DELETE blocked by `backend/drizzle/20260904084152_custom_3-immutability-triggers/migration.sql` (`prevent_student_payments_update/delete_trigger` :67-73, `prevent_teacher_transaction_update/delete_trigger` :92-98). The plan mandates journey fixtures of PAID `student_payments` rows (Journeys B/C revenue) and a pending-withdrawal `teacher_transaction` row, then "tracked hard-deletes in `afterAll`" of exactly those tables — impossible as written: the only suspension helper covers `audit_logs` (`test/helpers/db-cleanup.ts:83-109`), and `JOURNEY_TRACKED_TABLE_DELETE_ORDER` (`test/workflows/helpers/journey-fixture-registry.ts:67-76`) cannot track these tables (journey-fixtures guidance explicitly says do NOT track `teacher_transaction`). Zero-residue rule 2 + REQ-042 byte-identity would fail on any re-run. RESOLVE BEFORE tasks 1.3/2.2/2.4: pick and record a test-only approach (e.g. extend the trigger-suspension helper to the immutability family, mirroring the audit_logs precedent and `backend/db/test/logic/audit/audit-immutability.test.ts:320`), then amend task 2.4 teardown + REQ-074 wording. | 0.3 | 1.3, 2.2, 2.4 | ✅ Done | ruling outcome 0.3-rulings-outcome.md (2026-08-31) | RULED (RULING 1): test-only helper `withImmutabilityTriggersSuspended(tables, fn)` added to `test/helpers/db-cleanup.ts` (pg_trigger disable/restore, `withAuditDeleteTriggersSuspended` pattern); journey `afterAll` deletes tracked `student_payments`/`teacher_transaction` ids FIRST under it, then FK-safe teardown; `session_replication_role` rejected (privilege/env-dependent, suppresses RI triggers); REQ-074/§4.4/2.4 wording amended; registry untouched. |
| DI-0.3-2 | **Governance gate reuse decision (REQ-031/032, D8, §4.2, tasks 2.6+2.4).** `assertActorAdminActive` (`backend/services/admin/admin-gate.helpers.ts:107-148`) ALREADY implements role gate + governance in deterministic order deleted→blocked→suspended with the existing `accountDeleted`/`accountBlocked`/`accountSuspended` keys and ONE `logDomainError` per denial — the plan treats this as bespoke net-new work ("divergence from role-only `assertActorAdmin`") and REQ-002's reuse inventory omits the helper. Two observable divergences force an explicit ruling BEFORE 2.6/2.4: (a) absent-actor row → helper throws `ForbiddenError`; plan REQ-031/REQ-054/journey 2.4 mandate `UnauthorizedError` (plan also claims "parity with `assertActorAdmin`", which the shipped helper contradicts on this branch); (b) the helper re-reads the actor row for governance (two reads) vs plan's single-read pipeline. Either REUSE `assertActorAdminActive` and amend REQ-031's absent-row semantics + service/journey test expectations (consistent with the landed DEV3-018 first-lander rule "consume-and-extend additively; never fork the role-check semantics" and the helper's "single source of gate truth" header), or record in the canonical doc why the bespoke pipeline is kept despite the sibling precedent. | 0.3 | 2.6, 2.4 | ✅ Done | ruling outcome 0.3-rulings-outcome.md (2026-08-31) | RULED (RULING 2): REUSE `assertActorAdminActive` as the service's FIRST statement (cold-start precedent :169-170); actorId=0 → UnauthorizedError, absent/negative/non-integer → ForbiddenError, governance deleted→blocked→suspended kept with one log per denial; REQ-031/054/072 + §3.3/§4.2 + tasks 2.6/2.4 amended; gate logs `entity: "user"` (helper-owned). |
| DI-0.3-3 | **Repo non-tx executor ruling (plan §4.1, task 2.5).** Plan prescribes executor `tx ?? db` for every `PlatformAnalyticsRepository` method, but `backend/db/repo/AGENTS.md` marks CRITICAL: "All non-transactional read methods in database repositories MUST use `queryDb(tx)` from `@/backend/db`" (Neon HTTP fast path). Tree precedent is MIXED: `backend/db/repo/users/user.repository.ts` uses `queryDb`; `admin-user.repository.ts` and `audit-trail.repository.ts` (the siblings this plan mirrors) use `tx ?? db`. Record the ruling before 2.5 executes (adopt `queryDb(tx)` per layer AGENTS.md, OR follow the admin-repo sibling precedent with a documented deviation in the 2.5 outcome) so 2.5.IV does not bounce at the mid-point/review waves. | 0.3 | 2.5 | ✅ Done | ruling outcome 0.3-rulings-outcome.md (2026-08-31) | RULED (RULING 3): dual-branch executor — `tx` supplied → Drizzle builder on tx, absent → raw parameterized SQL via `queryDb` (Neon HTTP fast path); `tx ?? db` rescinded, no `db` import in the new repo file; repo-AGENTS CRITICAL rule wins over the admin/audit-trail sibling deviation. |
| DI-0.3-4 | **Repo test placement (task 2.5.TE).** Plan places the repo suite at `backend/db/repo/admin/__tests__/platform-analytics.repository.test.ts`, OUTSIDE `backend/db/test/**` — the scope of the layer runner `test:db` (`package.json:21-23` runs `backend/db/test/`) and the placement rule of `backend/db/test/AGENTS.md` ("This directory contains database-layer tests"). Sibling precedent: `backend/db/test/logic/admin/admin-user.repository.test.ts`. Relocate the suite to `backend/db/test/logic/admin/platform-analytics.repository.test.ts` (or record justification) BEFORE 2.5.TE so the suite is included in `bun run test:db` sweeps and differential regression. | 0.3 | 2.5 | ✅ Done | ruling outcome 0.3-rulings-outcome.md (2026-08-31) | RULED (RULING 4): suite path `backend/db/test/logic/admin/platform-analytics.repository.test.ts` — inside the `test:db` runner root (cwd `backend/db/test`) and the `test:db:sequential`/`coverage` scope; sibling of `logic/admin/admin-user.repository.test.ts`; `backend/db/repo/admin/__tests__/…` ruled out (silently excluded from sweeps); tasks 2.5.TE/5.1 + plan §7 paths follow the ruling. |

---

## Status Values

- ✅ **Done** — Item completed and verified
- ⚠️ **Partial** — Partially completed, needs follow-up work
- ❌ **Blocked** — Not resolved, plan cannot complete until addressed
- 🔄 **In Progress** — Currently being worked on
- 📅 **Forward** — Pre-seeded forward item owned by a later ticket; non-blocking for this plan
