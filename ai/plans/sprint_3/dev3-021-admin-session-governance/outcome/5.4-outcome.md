# Outcome 5.4 — Nav Registration (REQ-081)

> **Task:** Phase 5 / 5.4 (nav registration + nav-label i18n wiring + `navItems.test.ts` assertions).
> **Scope:** `frontend/views/dashboard/nav/navItems.ts` + `navItems.test.ts` + the `dashboard` locale tree (types/en/ar leaf). Zero other source changes.
> **Result: DONE** — exactly one admin nav entry added, compile-time label key wired (en + ar), test suite 33/33 green, tsgo 0, biome clean, duplicates 0.

---

## 1. Summary

The admin nav block in `NAV_ITEMS_BY_ROLE[UserRole.Admin]` gained EXACTLY ONE new entry pointing at `/admin/session-governance`, and the `sessionGovernance` label key was wired into the compile-time i18n system (REQ-003) in the `dashboard` namespace — the SAME namespace every other nav labelKey resolves from (`NavLabelKey` = `keyof DashboardLabels | keyof HandshakeCodeLabels`, collision-excluded). This closes plan-review F-5 (the i18n label key was not explicitly tasked; folded into 5.4 per the review ruling).

## 2. Entry shape + placement

```ts
{
  route: "/admin/session-governance",
  labelKey: "sessionGovernance",
  Icon: SessionGovernanceIcon, // EventNoteOutlined aliased at the MUI import
}
```

- **Icon:** `EventNoteOutlined` (D-07 suggestion; matches the `*Outlined` house style — every sibling entry uses `*Outlined` variants; REQ-064 icon-naming rule). Imported as `EventNoteOutlined as SessionGovernanceIcon`, in the biome-sorted position of the existing MUI icon import.
- **Placement:** inside the admin block's audit→disputes envelope (the 0.2-prerequisites row-15 ruling treats `:139` `/audit` and `:146` `/disputes` as the insertion-envelope anchors) — directly before the `/disputes` entry, after `/admin/analytics`. This keeps the pre-existing tested invariant `/admin/broadcasts` = `/audit` + 1 untouched (ALL existing assertions green, zero modifications) while satisfying REQ-081's "adjacent to /audit" as bounded by the task instruction "between /audit and /disputes per D-07".
- **Comment style:** mirrors the sibling `/admin/analytics` entry (page-path comment); the canonical-retargets docblock gained the DEV3-021 bullet (pure ADD, not a retarget) mirroring the DEV3-022d broadcasts bullet.
- **Role gating:** admin block only — non-admin roles are asserted NOT to see it (3 test.each cases).

## 3. Locale keys added (namespace: `dashboard`)

Namespace handle `Dashboard` (`shared/locale/namespaces/dashboard/dashboard.namespace.ts`, id `dashboard.dashboard`) — unchanged; only the label tree grew:

| File | Change |
|---|---|
| `shared/locale/types/dashboard/index.ts` | `readonly sessionGovernance: string;` with nav-link doc comment, placed after `disputes` (typed-key gate: `NavLabelKey`/`labelKey` now accepts it; en/ar parity enforced by both leaves being typed `DashboardLabels`) |
| `shared/locale/en/dashboard/index.ts` | `sessionGovernance: "Session Governance",` (matches sibling admin-label tone: single Title-Case noun phrase like `"Audit"`, `"Analytics"`) |
| `shared/locale/ar/dashboard/index.ts` | `sessionGovernance: "حوكمة الجلسات",` (mirrors sibling style — definite-article noun phrase like `سجل التدقيق` / `النزاعات`) |

Bundle wiring needed NO edits: `en/ar/messages.ts` reference the leaf consts (`dashboardTranslations: dashboardEn`), so the new key propagates automatically. There is NO `dashboard`-namespace parity harness in `shared/locale/` (only adminBroadcasts/adminUsers/analytics/applicant/errors/handshakeCode/notifications/parentLink/plans/sessions/wallet) — the nav suite IS this key's runtime parity belt (ownership pin + both-locale resolution). The errors/notifications parity suites are untouched by this key.

## 4. Files modified (all committed individually)

| File | Commit |
|---|---|
| `frontend/views/dashboard/nav/navItems.ts` | `77232e9` (+ biome sort fix `biome import-sort fix …` — biome relocated the aliased icon import to its sorted slot) |
| `shared/locale/types/dashboard/index.ts` | `54f29c8` |
| `shared/locale/en/dashboard/index.ts` | `0ab2cb9` |
| `shared/locale/ar/dashboard/index.ts` | `2269d80` |
| `frontend/views/dashboard/nav/navItems.test.ts` | `25812e9` |

## 5. Test results

- **Baseline (pre-change):** `navItems.test.ts` 26 pass / 0 fail.
- **Post-change:** 33 pass / 0 fail / 163 expect() calls (`bun run test/scripts/run-test.ts frontend/views/dashboard/nav/navItems.test.ts`). All 26 pre-existing assertions untouched and green; 7 new:
  1. exactly one `/admin/session-governance` entry with `labelKey: "sessionGovernance"` AND `Icon === EventNoteOutlined` (identity-checked via the real MUI import);
  2. placement: directly before `/disputes`, after `/audit` (envelope + exact slot pinned);
  3. ownership pin: `"sessionGovernance" in dashboardEn === true`, `in handshakeCodeEn === false`;
  4. both-locale resolution through the real runtime path (`Dashboard.getLabels(getTranslations(locale))` — label equals `dashboardLabels.sessionGovernance`, non-empty);
  5-7. Student/Teacher/Parent do NOT include the item.
- **tsgo:** 0 errors project-wide (`bun run tsgo`).
- **biome:** `check` clean on all five modified files (one fixable `organizeImports` error found and applied — import-specifier sort only).
- **duplicates:** `bun run check:duplicates` → 0 clones (exit 0).
- QL sub-loop duplicates on each modified file: covered by the repo-wide jscpd gate above (0.00% across 827 files).

## 6. Carry-forward

- **Route path CONFIRMED for 5.3:** `/admin/session-governance` → the page must ship at `app/(dashboard)/admin/session-governance/page.tsx` (admin route group exists per 0.2 row 20; `withPageAuth({ roles: [UserRole.Admin], redirectTo: "/admin/session-governance" })` per the disputes-page precedent). Until 5.3 lands, the route falls through to the `[feature]` coming-soon catch-all, exactly as the broadcasts entry did pre-DEV3-022d — nav test tolerates this (route strings only).
- **Label copy for 5.2/5.3 surfaces:** nav label is `"Session Governance"` / `حوكمة الجلسات`; page-level headings/metadata for the new surface need their own keys in whatever namespace 5.2/5.3 choose (NOT the dashboard shell bundle — follow the adminBroadcasts/analytics pattern of surface-owned namespaces if heading copy is needed).

## 7. Cross-file dependencies

- `frontend/views/dashboard/nav/navItems.ts` ⇄ `shared/locale/types/dashboard/index.ts`: `NavLabelKey` derives from `keyof DashboardLabels`; the interface member is what makes the entry compile.
- `navItems.test.ts` ⇄ `navItems.ts` + both locale leaves + `namespaces/dashboard`: ownership/resolution assertions read `dashboardEn`/`handshakeCodeEn` and the real namespace getters.
- The sidebar renderer (`DashboardSidebar.tsx` → `resolveNavItemLabel`) needs ZERO changes — the discriminated-resolution path already covers the new key.

## 8. SEC / SR / IV

- **SEC:** pure declarative nav entry + locale strings — no auth surface change (the entry renders for Admin only, enforced by `NAV_ITEMS_BY_ROLE` role lookup AND asserted for all three non-admin roles; the route itself is separately guarded by 5.3's `withPageAuth`). No user input, no logging, no colors/hardcoded styling.
- **SR:** scope held to navItems.ts + its test + the three dashboard-locale files; comments reference only ticket ids/pages in the file's established house style (DEV3-021 bullet mirrors DEV3-016/DEV1-005/DEV3-022d bullets) — ZERO plan-artifact references (no task ids, no plan-decision ids); no `console.*`; no nested ternaries; import discipline respected (`*Outlined` icons, `@/` aliases).
- **IV:** `.agents/instructions/frontend.instructions.md` (i18n compile-time system — no hardcoded UI copy; MUI v9 `*Outlined` icon naming; commit-message rules: concise/imperative; bun for all runs) and `frontend/AGENTS.md` (icon rename rule, commit rules) — complied. Test-first requirement of the pipeline: existing suite kept green, new assertions added in the same suite.
- **Environment note (W-1 recurrence):** the sandbox watcher flipped HEAD to local `main` repeatedly; one per-file commit (`70ac71f`) landed on local `main` and was cherry-picked onto the feature branch (`77232e9`) — identical content. Local `main` diverged by that stray commit; no feature work is lost, no push performed. No new deferred-ledger row required (pre-existing W-1 watch item, already documented in the shared worklog).
