# Outcome 4.1 — Admin Session Directory Filter Input (Phase 4 — GraphQL)

> **Task:** 4.1 (Phase 4 — GraphQL) + subtasks 4.1.QL / 4.1.TE / 4.1.SEC / 4.1.SR / 4.1.IV
> **Branch:** `feat/dev3-021-admin-session-governance` (re-asserted before every operation batch; verified after every commit — W-1 flip-war active all session)
> **Scope:** ADD the `AdminSessionListFilterPothosInput` registration to `backend/graphql/pothos/classes/session-filter-input.pothos.ts` + a paired SDL-contract test. Blast radius: the one Pothos file + one new test file + this outcome. The existing participant `SessionListFilterPothosInput` definition is untouched (REQ-061 — mirrored job-by-job, no extension of the participant input).

---

## 1. Summary

Task 4.1 is **DONE**. The admin sessions directory filter input now exists as a NEW, independently registered Pothos input object in the same module as the participant filter input, following that file's established registration/naming/documentation pattern exactly: string-named `gqlSchemaBuilder.inputType(...)` (Input Exception Policy — no local type, structural map of the canonical `@/backend/types` input), side-effect registration on the shared builder, named export for the consuming query module. The input carries the six filter keys ONLY — `page`/`pageSize` stay resolver-level Int args per the SDL in plan §3 (the participant input makes the identical split: its Pothos input has no pagination members; pagination is `t.arg.int` at the query field). Enums come from the shared registry; the date window rides the shared `DateTime` scalar by NAME. A fresh-emission SDL suite pins the exact wire shape, the closed whitelist, the optional-ness, and the participant input's non-extension.

## 2. Exact input name + field SDL shapes

**Pothos ref:** `AdminSessionListFilterPothosInput` (named export, `backend/graphql/pothos/classes/session-filter-input.pothos.ts`)
**SDL type name:** `AdminSessionListFilterInput`

```graphql
input AdminSessionListFilterInput {
  teacherUserId: Int
  studentUserId: Int
  type: SessionType
  status: SessionStatus
  dateFrom: DateTime
  dateTo: DateTime
}
```

- ALL six members optional (`required: false` — no `!` wrappers on the wire); an absent/null member drops out of the repository predicate (filters never error — same discipline as the participant input).
- `teacherUserId`/`studentUserId`: `t.int({ required: false })` — user ids on the session's participant columns, never surrogate keys.
- `type`: `t.field({ type: SessionTypePothosEnum, required: false })` — shared registry enum (`shared/enum.pothos.ts:139`), never re-registered.
- `status`: `t.field({ type: SessionStatusPothosEnum, required: false })` — shared registry enum (`shared/enum.pothos.ts:114`).
- `dateFrom`/`dateTo`: `t.field({ type: "DateTime", required: false })` — the shared `DateTime` scalar referenced BY NAME per `scalar.pothos.ts`/`pothos/AGENTS.md` (the `AdminAuditLogFiltersInput.from/to` precedent, `admin/audit-trail.pothos.ts:85-86`); ISO-8601 on the wire, resolved to `Date` before the service sees them; half-open creation-instant window (`>= dateFrom`, `< dateTo` — repo semantics pinned by 2.1).
- **NO `page`/`pageSize` members** — resolver-level Int args of `adminSessions` per plan §3 SDL (mirrors the participant lists: their input also carries no pagination; `t.arg.int({ required: false, defaultValue: 1|25 })` lives at the query field). Canonical `AdminSessionListFilterInput`'s `page`/`pageSize` members are satisfied by those args (1.1 outcome §9.3 binding).

## 3. Files modified

| File | Change |
|---|---|
| `backend/graphql/pothos/classes/session-filter-input.pothos.ts` | **MODIFIED** — added the `AdminSessionListFilterPothosInput` registration (named export + fields above) + a file-header "Contract (admin)" section; the participant contract section and its input definition are byte-untouched apart from the header title line now naming both registrations (its "plan §3.1" plan-artifact mention was dropped in the rewritten title lines — the participant bullet block is verbatim) |
| `backend/graphql/test/admin-session-filter-input.sdl.test.ts` | **CREATED (4.1.TE)** — 6-test SDL-contract suite (see §5) |
| `ai/plans/sprint_3/dev3-021-admin-session-governance/outcome/4.1-outcome.md` | **CREATED** — this file |

**NOT modified:** `shared/enum.pothos.ts` (both enums already registered), `shared/scalar.pothos.ts` (DateTime already registered), any query/mutation module (consumption is 4.2's), `frontend/graphql/generated/*` (codegen is 4.4's), `deferred-items.md` (no new debt — see §8), `tasks.md`/`worklog.md` (per rules).

## 4. Registration / export pattern (mirrored exactly)

- Same string-named `inputType` call form as the participant input: `gqlSchemaBuilder.inputType("AdminSessionListFilterInput", { fields: t => ({ ... }) })` — `inputRef<BackendType>` is deliberately NOT used (per `backend/graphql/AGENTS.md` WhatsApp pattern note: string-named input avoids null-incompatibility coupling to the backend type).
- No local TS type declared anywhere in the file; the docblock documents the structural map onto the canonical `AdminSessionListFilterInput` from `@/backend/types` (REQ-004 discipline, Input Exception Policy).
- Registered at import time on the shared builder; the named export is the ONLY consumption surface (`import { AdminSessionListFilterPothosInput } from "@/backend/graphql/pothos/classes/session-filter-input.pothos"`), identical to how `session-lifecycle.query.ts:71` consumes the participant input. Deliberately not registered anywhere else (single canonical input definition per SDL name).
- New imports added to the file: `SessionTypePothosEnum` joins the existing `SessionStatusPothosEnum` import from `@/backend/graphql/pothos/shared/enum.pothos` (VALUE import — runtime enum registration). No `@/backend/types` import needed (string-named input; the canonical type is referenced in prose, matching the participant input's exact pattern).

## 5. 4.1.TE — paired test (pattern exists → coverage added)

**Pattern source:** `backend/graphql/test/session-sdl.test.ts` (fresh deterministic `gqlSchemaBuilder.toSchema()` emission + printed-SDL assertions for objects awaiting their resolver-module wiring) and `backend/graphql/test/schema-surface.test.ts` (`GraphQLInputObjectType.getFields()` exact-field-set pinning for filter inputs).

**Suite:** `backend/graphql/test/admin-session-filter-input.sdl.test.ts` — 6 pass / 0 fail / 25 expect():

1. **Clean construction** — `input AdminSessionListFilterInput {` occurs EXACTLY ONCE in the emitted SDL (a duplicate registration would throw "has already been declared" at import time).
2. **Closed whitelist** — the input exposes EXACTLY the six fields `[dateFrom, dateTo, status, studentUserId, teacherUserId, type]` (sorted exact-match).
3. **Field types** — `teacherUserId`/`studentUserId` = `Int`, `type` = `SessionType`, `status` = `SessionStatus`, `dateFrom`/`dateTo` = `DateTime` (exact `toString()` pins).
4. **Optional-ness** — every member's type string is nullable (no `!` — absent members drop out, never error).
5. **SEC whitelist probe** — no `page`/`pageSize`/`limit`/`offset` (pagination stays resolver-level) and no `actorId`/`id`/`sessionId`/`userId` (no caller-identity surface).
6. **Participant non-extension pin (REQ-061)** — `SessionListFilterInput` still exposes EXACTLY `["status"]` as `SessionStatus`; the admin surface is a distinct input object.

Regression: `backend/graphql/test/session-sdl.test.ts` re-run — **11 pass / 0 fail** (session surface construction unaffected; the filter-input module imports the shared enums without re-registering them).

## 6. Verification results (exit codes)

| Gate | Command | Result |
|---|---|---|
| 4.1.QL sub-loop (source file) | `bun run scripts/health/sub-loop.ts backend/graphql/pothos/classes/session-filter-input.pothos.ts --lifecycle duplicates` | **exit 0** — tsgo ✅ oxlint ✅ biome:check ✅ lint:type-aware ✅ duplicates ✅ (run twice: pre- and post-concurrent-churn) |
| 4.1.QL sub-loop (test file) | `bun run scripts/health/sub-loop.ts backend/graphql/test/admin-session-filter-input.sdl.test.ts --lifecycle duplicates` | **exit 0** after one fix round (sonarjs `prefer-regexp-exec`: `String.match` non-global → global-flag probes) and one fix round (fresh emission needs the `DateTime` scalar's side-effect import — see §7) — tsgo ✅ oxlint ✅ biome:check ✅ lint:type-aware ✅ duplicates ✅ (jscpd skip: outside scan scope) |
| 4.1.TE | `bun run test/scripts/run-test.ts backend/graphql/test/admin-session-filter-input.sdl.test.ts` | **exit 0 — 6 pass / 0 fail / 25 expect()** |
| 4.1.TE regression | `bun run test/scripts/run-test.ts backend/graphql/test/session-sdl.test.ts` | **exit 0 — 11 pass / 0 fail** |
| Project typecheck | `bun run tsgo` | **exit 0** (final run; one mid-session transient had 5 errors all inside the concurrent 4.3 agent's in-progress `admin-session-governance.mutation.test.ts` — zero in my files, gone after their next commit) |
| Biome standalone (fallback) | `bunx @biomejs/biome check <both files>` | **exit 0** — Checked 2 files, no fixes applied |

**Environment note (concurrent Phase 4 work in flight):** while this task ran, sibling commits landed on the same branch (`9206e19` mutation input quartet, `e40d708` mutation barrel registration, `bee8b35`/`cbc81ee` wire-tier suite). Two knock-on observations, both OUTSIDE this task's blast radius: (a) `backend/graphql/test/schema-surface.test.ts` currently fails 6 frozen-baseline/codegen-sync pins because the production Mutation root grew before 4.3/4.4 reconcile the baselines and regenerate artifacts — my input is provably NOT in the production schema chain yet (`grep AdminSessionListFilterInput frontend/graphql/generated/schema.graphql` = 0), so none of those failures involve this task's surface; (b) the transient tsgo errors above. No ledger rows added: both are in-flight task states, not debt.

## 7. Cross-file dependencies & carry-forward for 4.2

1. **Import for the query module (4.2):**
   `import { AdminSessionListFilterPothosInput } from "@/backend/graphql/pothos/classes/session-filter-input.pothos";`
   Bind as a REQUIRED arg per plan §3 SDL (`filter: AdminSessionListFilterInput!`): `filter: t.arg({ type: AdminSessionListFilterPothosInput, required: true })` — note the participant lists use `required: false` + `?? {}`; the admin SDL is non-null, so no empty-filter fallback is needed.
2. **Resolver mapping (BOPLA):** copy field-by-field onto the canonical type (`{ teacherUserId: args.filter.teacherUserId, studentUserId: args.filter.studentUserId, type: args.filter.type, status: args.filter.status, dateFrom: args.filter.dateFrom, dateTo: args.filter.dateTo }`) — NO spread; types align 1:1 (Int→number, registry enums→`SessionType`/`SessionStatus`, DateTime→`Date`). `page`/`pageSize` bind as separate `t.arg.int({ required: false, defaultValue: 1 | 25 })` args (SDL defaults + `??` restore convention for explicit nulls, as the participant factory does) and pass to `SessionAdminGovernanceService.listAll(actorId, filter, page, pageSize, locale)` (3.1 outcome §2 — filter is shape-validated there; absent members drop out).
3. **Production registration path:** the input joins the production schema ONLY when 4.2's query module (transitively the barrels → `gqlSchema.ts`) imports it — until then the fresh-emission suite is its build-time proof. 4.4 (`generate:gqlSchema` + `codegen`) then propagates it into the committed SDL artifact + frontend types.
4. **DateTime scalar residency (resolves the 4.1 portion of the deferred-ledger watch item "verify registry vs each new Pothos file's imports during 4.x"):** the scalar is registered ONCE in `shared/scalar.pothos.ts`, wired into the production build via the definitions barrel (`gqlSchema.definitions.ts:15`) — it is NOT bundled with the builder module itself. Domain Pothos files reference it by NAME and need no import (verified: `session-filter-input.pothos.ts` builds green in the production chain and in the sub-loop); ONLY standalone fresh-emission tests must add `import "@/backend/graphql/pothos/shared/scalar.pothos"` explicitly (my suite does, with an explanatory comment). No duplicate registration exists anywhere (would throw at import).

## 8. 4.1.SEC — whitelist surface audit

- The input is a CLOSED six-member whitelist: any extra client field is a GraphQL "unknown field" validation failure BEFORE any resolver runs (pinned by test 2 + 5). GraphQL input objects cannot smuggle unlisted keys.
- Members are: two filter-scoped participant user ids, two closed enum vocabularies, two `DateTime` window bounds. No caller-identity surface (`actorId`/`userId`/`id`/`sessionId` probes fail — test 5): the filter describes session rows, and is addressed BY an admin (the `$all` role scope at 4.2's field), never carrying an admin assertion.
- No pagination members (resolver-level args; clamping/normalization remains the service's, per the 2.1/3.1 contracts). No audit/reason/write-shaped fields — the input is read-path only.
- Downstream backstops: the canonical zod `AdminSessionListFilterInputSchema` (1.1) strip-mode-drops unknown keys and closes the enum/id vocabularies; the repo predicate ANDs only its known members (2.1). No path exists from this input into any write — the directory is read-only (REQ-029).

## 9. 4.1.SR — semantic checklist

- **No dead code:** both registrations in the file are exported and consumed (participant: production query module; admin: 4.2 by design, test today). No unreachable branches; no unused imports (both enums used; `gqlSchemaBuilder` used).
- **Comment hygiene:** ZERO plan-artifact references in all NEW/rewritten comment text (scanned for `REQ-\d|DEV3-\d|Phase \d|Task \d|plan\.md|tasks\.md|specs\.md`): the rewritten file header and the new input docblock describe domain behavior only. The pre-existing one-line participant label `(REQ-020)` was left byte-identical with the rest of the participant block per REQ-061's no-modification discipline.
- **Scope boundary:** exactly the one Pothos file + one test file + this outcome. The participant input's definition, the enum/scalar registries, and every other module are untouched (`git show` diffs confirm: source-file delta is header text + one new import + one new registration block).
- **No behavioral drift:** participant SDL unchanged (regression-pinned by test 6 + session-sdl suite green); no enum/scalar re-registration (single-definition rule).

## 10. 4.1.IV — instructions compliance

- `backend.instructions.md` (Pothos/GraphQL + Type Definition Pattern): no local types in Pothos files ✓ (string-named input + prose mapping, the file's established Input Exception Policy pattern); `nullable` semantics mirror the optional TS members via `required: false` ✓; no console.*, no dynamic imports, no hardcoded error strings (no error path at all) ✓; bun-only commands ✓; GraphQL layer contains zero business logic / zero repo calls ✓.
- `backend/graphql/AGENTS.md`: Input Exception Policy ✓ (input as separate definition); single canonical type discipline ✓; enum CRITICAL rule ✓ (registry import, no re-registration, no literal arrays); WhatsApp-section guidance honored (`inputType(string-named)` over `inputRef`) ✓.
- `backend/graphql/pothos/AGENTS.md`: scalar discipline ✓ (by-NAME reference, no re-registration, builder/registry sides untouched); enum canonical workflow ✓; DataLoader rule N/A (no field resolvers in this module) ✓; no `oxlint-disable` ✓.
- `backend/AGENTS.md`: Pothos layer maps to `backend/types` shapes ✓ (structural map documented; no `.types.ts` created anywhere).
- Sub-loop rule files (`.github/instructions/backend.instructions.md`, `tests.instructions.md`) — read before fixing, all violations fixed at root cause (regex global flags; scalar side-effect import); zero suppressions.

## 11. Deferred rows

**None added.** The DateTime-residency watch item's 4.1 portion is VERIFIED (result in §7.4 — informational, recorded here; the ledger row itself stays for 4.2/4.3/4.4 to consume). The schema-surface frozen-baseline churn belongs to the in-flight 4.3/4.4 waves, not to this task.
