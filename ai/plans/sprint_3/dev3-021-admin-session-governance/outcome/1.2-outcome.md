# Outcome 1.2 — Errors Locale Extension for Admin Session Governance

> **Task:** 1.2 (Phase 1 — Types & i18n Substrate) · **Scope:** `shared/locale` errors tree only (type definitions + en + ar message files) · **Branch:** `feat/dev3-021-admin-session-governance`
> **Inputs honored:** outcome/0.1 + 0.2 + 0.3 read in full before any edit; binding F-3 (cancel eligibility = `scheduled|started`; `disputed` refused) respected in the key mapping; REQ-002/REQ-050/REQ-051 drive the key set; REQ-003 compile-time system enforced by construction.

---

## 1. Summary

Extended the canonical `errors` namespace with the admin session-governance error copy. A full key inventory of the existing session domain was taken first (see §2) — the errors tree ALREADY carries the complete conflict/not-found/certification vocabulary the governance services need (`sessionNotFound`, `sessionInvalidTransition`, `teacherNotCertified`, plus the global `unauthorized`/`forbidden` gate copy). Reusing those keys per the dispatch rule ("do NOT duplicate keys that already exist"), the genuinely missing copy is the admin-reschedule validation pair, which is the ONLY new surface: two new flat keys following the established session-domain naming convention (`session*`-prefixed top-level strings — the convention of `sessionNotFound`/`sessionInvalidTransition`/`sessionIntentCorrupt`; NOT a nested sub-group like `adminUsers`, which has no session-domain precedent).

Both leaf maps are typed `ErrorsLabels` (compile-time belt #1 — `bun tsgo` fails on any en↔ar drift), and the runtime parity harness covers the new keys automatically (belt #2 — top-level key-set equality + zero-dead-key leaf walker). No new keys were minted for semantics an existing key already owns; no `validationFailed`/`rateLimited`-style near-duplicates (shared/AGENTS.md errors-namespace rule).

## 2. Existing session-domain keys inventoried (REUSED — zero additions)

Grep of `shared/locale/{types,en,ar}/errors/` for the session domain — these all exist and are NOT duplicated:

| Key (flat, top-level `ErrorsLabels`) | Existing en copy | Governance error path it serves |
|---|---|---|
| `sessionNotFound` | "The requested session was not found." | Mutation-target guard-miss classification (`NotFoundError("SESSION", …)`); directory detail per REQ-012 returns `null` instead of throwing |
| `sessionInvalidTransition` | "This action is not allowed for the session in its current state." | ALL state-conflict denials: reschedule (not `scheduled\|started`), cancel (not pre-terminal per binding F-3 — includes the `disputed` refusal), reassign (not `scheduled`), join (not `started`) → `ConflictError("SESSION_INVALID_TRANSITION", …)` |
| `teacherNotCertified` | "The selected teacher is not certified for this action." | Reassign certification failure (candidate teacher not approved — FK-violation class) → `ConflictError("TEACHER_NOT_CERTIFIED", …)` |
| `teacherNotFound` | "The selected teacher was not found." | Candidate-teacher lookup miss on reassign |
| `unauthorized` / `forbidden` | global gate copy | BFLA 401/403 split — already wired inside `assertActorAdmin`/`resolveAdminActorRow`; zero new code |
| `validation` | "Invalid input." | Generic pre-DB shape-guard precedent (id-shape, optional cancel-reason length via `normalizeOptionalReasonText`) |
| `sessionIntentCorrupt`, `invalidSessionIntent`, `insufficientBalance`, `idempotencyKeyRequired` | existing | Adjacent lifecycle vocabulary — untouched, still correct |

## 3. Exact key list ADDED (namespace `errors`, flat top-level)

| Key path | en | ar |
|---|---|---|
| `errorsTranslations.sessionRescheduleWindowInvalid` | "The start time must be before the end time." | "يجب أن يكون وقت البدء قبل وقت الانتهاء." |
| `errorsTranslations.sessionRescheduleStartInPast` | "The start time cannot be more than 5 minutes in the past." | "لا يمكن أن يكون وقت البدء في الماضي بأكثر من 5 دقائق." |

- Semantics: the two admin-reschedule pre-write validation rejects (timing pair inverted; start older than the 5-minute grace window). Both are `ValidationError`-class copy — concrete, user-appropriate, and free of internals (the 5-minute grace is a public validation rule, not a constraint leak).
- Arabic follows the established tone of the session-domain entries (`المعلم المحدد غير معتمد حالياً.`, `لا يمكن تنفيذ هذا الإجراء على الجلسة في حالتها الحالية.`) and the Arabic-numeral convention of the file (`5 دقائق`, like `255 حرفاً` / `8 أحرف`).
- Both keys are plain `string` members of `ErrorsLabels` → auto-included in the derived `ErrorMessageKey` union; docblocks quote the en copy + describe the denial class (no code literals pinned in source; code registration is Task 1.3's surface).
- No other namespace/type/barrel file needed changes: `shared/locale/types/message.ts` (`errorsTranslations: ErrorsLabels` handle) and the `Errors` namespace handle are unchanged contracts.

## 4. Files modified / NOT modified

**Modified (exactly 3 — locale tree only, +8 lines, 0 deletions):**
1. `shared/locale/types/errors/labels.ts` — +2 documented readonly keys on `ErrorsLabels`
2. `shared/locale/en/errors/index.ts` — +2 en values
3. `shared/locale/ar/errors/index.ts` — +2 ar values

**NOT modified (deliberately):**
- `backend/types/classes/admin-session-governance.types.ts` — Task 1.1's file (write-conflict avoidance per dispatch); not touched, not created. (Observed concurrently-modified `backend/types/classes/index.ts` in the shared working tree belongs to Task 1.1 — left untouched by this task.)
- `docs/graphql/error-handling-contract.md` + any centralized code list — Task 1.3 owns code registration.
- `shared/locale/errors-namespace.parity.test.ts` — harness is discovery-based; new top-level keys enter its gates automatically (verified by run). No test edits needed.
- Notification copy keys (wave ids en+ar) — Task 3.2 owns them (binding F-4 pin).
- Nav label key (`sessionGovernance`) — F-5 assigns it to 5.4 (or a later 1.x extension); out of this task's errors-tree scope.

## 5. Verification results (exit codes)

| Gate | Command | Result |
|---|---|---|
| 1.2.QL | `bun run scripts/health/sub-loop.ts shared/locale/types/errors/labels.ts --lifecycle duplicates` | **exit 0** (tsgo + oxlint + biome:check + lint:type-aware + jscpd duplicates all green) |
| 1.2.QL | same for `shared/locale/en/errors/index.ts` | **exit 0** |
| 1.2.QL | same for `shared/locale/ar/errors/index.ts` | **exit 0** |
| 1.2.TE | `bun run test/scripts/run-test.ts shared/locale/errors-namespace.parity.test.ts` | **exit 0 — 8 pass / 0 fail / 126 expect()** (incl. "identical sorted key sets across BOTH locale sources" and "every leaf value non-empty" over the new keys) |
| 1.2.TE (type shape) | `bun run tsgo` (project-wide) | **exit 0** — both leaf consts typed `ErrorsLabels` compile with the additions; any en↔ar key drift would fail here (primary parity gate) |
| 1.2.TE (format) | `bunx @biomejs/biome check` on the 3 files | **exit 0 — Checked 3 files, no fixes applied** |

## 6. Carry-forward knowledge for Task 3.1 (service consumption)

- **Handle:** services use the one-arg precedent — `const t = getServerTranslations(locale).errorsTranslations;` (typed `ErrorsLabels`; exactly `admin-gate.helpers.ts:69/135` and the `ReturnType<typeof getServerTranslations>["errorsTranslations"]` parameter style of `session-lifecycle.transitions.ts`). Resolvers that need it directly use `ctx.t("errors")` (bound to `ctx.locale`). NEVER `getBackendTranslations`/`next-intl`/string literals.
- **New keys (reschedule pre-write validation, before any read):**
  - `t.sessionRescheduleWindowInvalid` — throw when `startedAt >= endedAt` (recommended `ValidationError`, code proposal `SESSION_RESCHEDULE_WINDOW_INVALID` — verify/register in 1.3).
  - `t.sessionRescheduleStartInPast` — throw when `startedAt` is older than now−5min (recommended `ValidationError`, code proposal `SESSION_RESCHEDULE_START_IN_PAST` — verify/register in 1.3). The 5-minute constant lives in the guard layer, never in the copy.
- **Reused keys (do NOT re-mint):** state conflicts (reschedule/cancel/reassign/join, incl. the F-3 `disputed`-cancel refusal) → `t.sessionInvalidTransition` with `ConflictError("SESSION_INVALID_TRANSITION", …)`; guard-miss not-found classification → `t.sessionNotFound` with `NotFoundError("SESSION", …)` (directory DETAIL returns `null` instead per REQ-012); certification denial → `t.teacherNotCertified` with `ConflictError("TEACHER_NOT_CERTIFIED", …)`; BFLA → `assertActorAdmin` untouched (emits `t.unauthorized`/`t.forbidden` internally); optional cancel-reason shape → `normalizeOptionalReasonText(value, t)` precedent (throws generic `t.validation`).
- **Frontend note:** `VALIDATION`-class failures render via the existing code→behavior map (toast / form-field pairs per `docs/graphql/error-handling-contract.md`) — no frontend key work required for the new copy.

## 7. Cross-file dependencies (recorded, NOT edited)

1. **Task 1.1 (zod schemas):** plan §2 claims a "`zodToResult`-style per existing precedent" — **no such precedent exists in the tree**: `zod@^4.5.4` is declared in `package.json` but there are ZERO `from "zod"` imports across `backend/`, `shared/`, `frontend/`, `app/`, `scripts/`, `test/`. Task 1.1 establishes the first zod usage (or falls back to the established pure-guard + `ValidationError(t.<key>)` pattern of `session-lifecycle.guards.ts`). Either way: schema-side messages must NOT embed literal English — the reschedule timing rejects must surface `t.sessionRescheduleWindowInvalid` / `t.sessionRescheduleStartInPast` (service layer) or be message-less shape guards whose failure the service re-raises with these keys.
2. **Task 1.3 (codes):** register/verify the two proposed codes above against the error-handling contract registry before 3.1 throws them.
3. **Task 3.1 (services):** consume exactly the key paths in §6; reject keys that would duplicate them (e.g. no `sessionRescheduleConflict` — the state conflict is `sessionInvalidTransition`).

## 8. Semantic review (1.2.SR) — PASS

- Every added key maps to a spec'd error path (reschedule validation); zero dead keys; zero near-duplicates of the 18 canonical transport keys or of each other.
- Comments in modified files: semantic only — ZERO plan-artifact references (no REQ-*/Task/plan.md strings; verified by reading the diff).
- No cross-layer imports introduced (labels.ts imports nothing; en/ar import only `@/shared/locale/types/errors` — pre-existing line).
- Scope boundary honored: `git status` shows exactly the 3 locale-tree files owned by this task; `git diff --stat` = +8/−0 across them.

## 9. Security & confidentiality review (1.2.SEC) — PASS

- No secrets, tokens, PII, SQL, constraint names, or stack details in any new copy (REQ-032). Both messages state the public validation rule and the remediation.
- Arabic values verified Arabic-script (native parity-harness leaf walker + visual check); no English leakage into `ar`, no machine constants (`_health`-class) minted.
- Copy contains no user/row identifiers — interpolation-free static strings.

## 10. Instruction verification (1.2.IV) — PASS

- `.agents/instructions/backend.instructions.md` (read in full): "Never hardcode error strings — in any layer" satisfied by construction (these keys ARE the canonical source the layers consume); `getServerTranslations(locale)` one-arg service precedent + `ctx.t("errors")` resolver precedent honored; DomainError-class throwing guidance reflected in §6 recommendations; no `console.*`, no nested ternaries, `@/` alias discipline intact (no new imports).
- `shared/AGENTS.md` + `shared/locale/AGENTS.md` (read in full): errors namespace is THE canonical transport-message surface — new session-domain copy landed there (not in `sessions`/`dashboard` namespaces); no near-duplicate semantics; leaf modules remain pure string literals, no logic; ICU/typed-function patterns not needed (static strings); `@/shared/locale/...` import rule respected.

## 11. Environment note (carried from 0.1 W-1)

The sandbox watcher again flipped HEAD to `main` several times during this task (uncommitted locale edits are branch-independent and survived each flip). `git checkout feat/dev3-021-admin-session-governance` re-asserted after every flip; branch verified before this outcome write. Nothing was committed (per dispatch).
