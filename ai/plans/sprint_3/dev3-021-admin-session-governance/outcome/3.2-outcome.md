# Outcome 3.2 — Notification Wave Emitters (Phase 3 — Service) — VERIFICATION + F-4 PIN

> **Task:** 3.2 (Phase 3 — Service) + subtasks 3.2.QL / 3.2.TE / 3.2.SEC / 3.2.SR / 3.2.IV
> **Branch:** `feat/dev3-021-admin-session-governance` (re-asserted before every operation batch; verified after every test run)
> **Scope:** verify the three admin-governance notification waves end-to-end and pin the canonical wave-id set (binding plan-review condition F-4). **Verification-only wave: zero source files modified.** All wave machinery, copy keys, and call sites were landed complete by the 3.1 implementation wave and are re-verified here with path:line evidence and fresh test executions.

---

## 1. Summary

Task 3.2 is **DONE — verification-only, no code churn**. The three governance wave ids are registered end-to-end: type-level (`SessionGovernanceWaveKind`, a closed union), service-level dispatch (three caller-facing emitters on the `SessionRequestNotificationService` namespace + the extracted wave machinery module), emit call sites inside all three `SessionAdminGovernanceService` mutation bodies with publish-after-commit, and recipient-locale copy keys registered in the types/en/ar notifications triple and gated by the parity suite. All four test gates re-run green on this wave: parity **104 pass / 0 fail**, notification service regression **20 pass / 0 fail**, governance service suite (exercises every emit call site end-to-end) **37 pass / 0 fail / 2 skip**, project `tsgo` **exit 0**.

**Gaps found: NONE.** Nothing was filled; nothing was renamed.

---

## 2. FINAL wave-id pin (binding plan-review condition F-4 — RESOLVED)

**Canonical wave-id set (pinned): plan §4's ids, which are exactly the ids the implementation uses:**

| Canonical wave id | Specs label (REQ-020/022/024) | Status |
|---|---|---|
| `sessionGovernance.rescheduled` | `session.rescheduled` | ✅ PINNED (implementation + plan agree) |
| `sessionGovernance.cancelled` | `session.cancelled` | ✅ PINNED (implementation + plan agree) |
| `sessionGovernance.teacherReassigned` | `session.teacherReassigned` | ✅ PINNED (implementation + plan agree) |

**Rationale (F-4 resolution):** F-4 offered two acceptable pins — plan §4's `sessionGovernance.*` or "whatever vocabulary the `SessionRequestNotificationService` emit-input convention requires". The landed implementation uses plan §4's ids verbatim as the closed TS union `SessionGovernanceWaveKind` (`backend/types/classes/session-notification.types.ts:21-24`), consistently across type → dispatch → emit call sites → per-recipient claim recipe (`session:<sessionId>:<waveKind>`) → test assertions. Neither specs' `session.*` nor plan's ids are literal `NotificationType` enum values (the engine maps waves onto `NotificationType.SessionCancellation` / `NotificationType.SessionRequest`, see §3), so the specs spelling is a stale conceptual label. Reusing the implementation's ids is the zero-churn, fully-registered choice. **Journeys 6.1–6.3 must assert against the pinned ids above.** The specs' `session.*` labels are recorded as superseded; no spec/plan file was edited (F-4 explicitly resolves without plan-file edits).

**Precision note on 3.1 outcome wording:** the 3.1 outcome §2 says reassign recipients are "[student, teacher] (+ outgoing teacher first for reassign)". The authoritative code order is `[student, outgoingTeacher, incomingTeacher]` (`session-request-notification.service.ts:348-354`). REQ-024 mandates the recipient SET (student + old teacher + new teacher), not an order; the suite pins the triple; no code change required. This note is the correction of record.

---

## 3. Per-wave verification table

Legend — **Type reg:** closed-union registration in `backend/types/classes/session-notification.types.ts` (`SessionGovernanceWaveKind`, lines 21–24). **Dispatch:** caller-facing emitter on the `SessionRequestNotificationService` namespace (`backend/services/classes/session-request-notification.service.ts`) delegating to the wave machinery module (`backend/services/classes/session-request-notification.governance.ts`, "SGM" below). **Emit call site:** the governance mutation tx body (`backend/services/classes/session-admin-governance.helpers.ts`) + publish-after-commit in the hub (`backend/services/classes/session-admin-governance.ts`). **Recipient resolution:** per-recipient persisted-locale copy (`SGM:141` `recipient.locale ?? defaultLocale` → `SGM:142-145` `getServerTranslations(recipientLocale).notificationsTranslations`; engine never translates — `SGM:18-22`).

| Wave id | Type registration | Dispatch (emitter) | Emit call site | Publish-after-commit | Recipients (order) | en key (title/body) | ar key (title/body) | Recipient resolution |
|---|---|---|---|---|---|---|---|---|
| `sessionGovernance.rescheduled` | `session-notification.types.ts:21-24` | `notifySessionGovernanceRescheduled` @ `session-request-notification.service.ts:307-315` (→ `emitGovernanceWave` @ `SGM:203-211`) | `session-admin-governance.helpers.ts:253` (reschedule tx body, inside `withTransaction`) | `session-admin-governance.ts:257` (`reschedule` @ :211) | `[student, teacher]` (service.ts:314) | `eventSessionGovernanceRescheduledTitle`/`…Body` @ `shared/locale/en/notifications/index.ts:66-68` | `shared/locale/ar/notifications/index.ts:67-69` | per-recipient locale (SGM:141-145); claim key `session:<sessionId>:sessionGovernance.rescheduled` @ SGM:154 |
| `sessionGovernance.cancelled` | `session-notification.types.ts:21-24` | `notifySessionGovernanceCancelled` @ `session-request-notification.service.ts:322-330` | `session-admin-governance.helpers.ts:336` (cancel tx body, after refund + claim backfill) | `session-admin-governance.ts:332` (`cancel` @ :300) | `[student, teacher]` (service.ts:329) | `eventSessionGovernanceCancelledTitle`/`…Body` @ `en/notifications/index.ts:69-70` | `ar/notifications/index.ts:70-71` | per-recipient locale (SGM:141-145); claim key `session:<sessionId>:sessionGovernance.cancelled` @ SGM:154 |
| `sessionGovernance.teacherReassigned` | `session-notification.types.ts:21-24` | `notifySessionGovernanceTeacherReassigned` @ `session-request-notification.service.ts:339-355` (outgoing teacher resolved by id via `resolveGovernanceParticipant` @ SGM:100-119) | `session-admin-governance.helpers.ts:413` (reassign tx body — passes `current.teacherId` as the outgoing teacher, captured pre-write) | `session-admin-governance.ts:388` (`reassignTeacher` @ :367) | `[student, outgoingTeacher, incomingTeacher]` (service.ts:348-354; see §2 precision note) | `eventSessionGovernanceTeacherReassignedTitle`/`…Body` @ `en/notifications/index.ts:71-73` | `ar/notifications/index.ts:72-74` | per-recipient locale (SGM:141-145); claim key `session:<sessionId>:sessionGovernance.teacherReassigned` @ SGM:154 |

**Supporting machinery (dispatch layer, one per all three waves):**
- Engine type mapping `toGovernanceNotificationType` @ `SGM:53-57`: `sessionGovernance.cancelled → NotificationType.SessionCancellation`; reschedule/reassign → `NotificationType.SessionRequest` (the session-domain wave carrier every participant flow uses). Exhaustive copy switch `composeGovernanceWaveCopy` @ `SGM:65-91` with a `never` exhaustiveness guard.
- Per-recipient emit planning `planGovernanceWave` @ `SGM:134-160` (sync, copy composed per recipient BEFORE any I/O); head-first sequential receipt walk `emitGovernanceReceiptsFrom` @ `SGM:169-195` (every engine call across an `await`, never inside a loop body — mirrors the refund-walk shape); wave entry `emitGovernanceWave` @ `SGM:203-211`.
- Receipt producer contract: receipts are UNPUBLISHED on the caller-tx path; the hub publishes strictly after its own commit via `NotificationEngine.publishReceipts` (hub :257/:332/:388) — nothing is pushed for a rolled-back emit (`SGM:10-16`).
- Wave-context resolution shared with the participant waves: `resolveWaveContext` @ `session-request-notification.service.ts:69-…` (positive-safe-int pre-check, `SessionRepository.findWaveContextById`, corrupted-intent fail-closed) — the governance emitters reuse the SAME participant mechanism as the six existing participant waves (`teacher_request`, `outcome_*`), satisfying the "mirror the existing wave mechanism" requirement.

---

## 4. Recipient-locale semantics (REQ-003 compliance) — VERIFIED

- Copy resolves PER RECIPIENT: the recipient's persisted `locale` (fallback platform `defaultLocale`) is used both for composing the title/body and as the engine call's locale argument (`SGM:140-145, 181, 190`) — same mechanism as the participant waves; zero hardcoded strings in any emitter (all copy flows through the compile-time `notifications` namespace keys).
- Copy keys are compile-time typed: declared on `NotificationsLabels` (`shared/locale/types/notifications/index.ts:160-176`), implemented in en + ar, gated for en↔ar parity by `shared/locale/notifications-namespace.parity.test.ts:103-108` (required-key list) — a missing/divergent key fails the suite.
- Copy is plain and factual: no participant names, no timing values, no refund promises (docblock contract @ `SGM:59-64`; copy text verified: "Session rescheduled" / "A platform administrator cancelled your session." / "Session teacher changed" + ar mirrors).
- Idempotency claim recipe `session:<sessionId>:<waveKind>` (`SGM:154`) carries domain ids only — no PII; the engine folds the recipient cohort into the hashed claim identity, keeping one wave-level key per-recipient deterministic (`SGM:128-133`).

---

## 5. Files modified

**NONE (product code). This wave is verification-only** — 3.1 landed the full surface (see 3.1 outcome §3: `session-request-notification.governance.ts` +211, service +70, types +13, locale triple +17/+8/+8, parity gates +10). The only file created by THIS wave is this outcome document. No `deferred-items.md` change: no out-of-scope discoveries (zero gaps, zero dead code, zero naming conflicts).

---

## 6. Test / quality results (3.2.QL / 3.2.TE)

| Gate | Command | Result |
|---|---|---|
| 3.2.QL | `bun run tsgo` (project typecheck; no per-file sub-loop needed — zero source files modified by this wave) | **exit 0** |
| 3.2.TE | `bun run test/scripts/run-test.ts shared/locale/notifications-namespace.parity.test.ts` | **exit 0 — 104 pass / 0 fail / 626 expect()** (governance keys in the required-key parity gates, test lines 103-108) |
| 3.2.TE | `bun run test/scripts/run-test.ts backend/services/classes/session-request-notification.service.test.ts` (the 20-pass regression suite) | **exit 0 — 20 pass / 0 fail / 346 expect()** |
| 3.2.TE (bonus evidence) | `bun run test/scripts/run-test.ts backend/services/classes/session-admin-governance.service.test.ts` — exercises all three emit call sites end-to-end (per-recipient-locale waves, per-receipt publish-after-commit fan-out, three-recipient reassign wave, zero publishes on denial paths) | **exit 0 — 37 pass / 0 fail / 2 skip (authored real-Postgres-only chaos races) / 411 expect()** |

Every run: branch re-asserted immediately before and verified after (W-1 flip watcher active throughout; runs referencing a flipped foreign tree would have been discarded — none were).

---

## 7. 3.2.SEC — security & confidentiality — PASS

- No secrets/credentials in any copy string (verified all six en + ar strings); copy contains no PII — no participant names, no timing values (plain factual copy per the SGM:59-64 contract).
- Claim/emit inputs carry domain ids only (`userId`, `sessionId`, wave kind); no free-text of any actor is echoed into notifications.
- Localized copy exclusively via compile-time namespace keys (`getServerTranslations(locale).notificationsTranslations`); no string literals composed in service code; the only literal thrown is the localized `internalServerError` key on the engine-contract-breach fail-closed path (`SGM:183-187`).
- Waves ride the mutation transaction (all-or-nothing with the write that caused them) and publish only after the caller's commit — a rolled-back mutation pushes nothing (pinned by the governance suite's rollback-purity and zero-publishes-on-denial assertions).

## 8. 3.2.SR / 3.2.IV — semantic review & instruction verification — PASS

- **SR:** no dead code — every emitter has exactly one production call site (helpers :253/:336/:413) plus named test coverage; the machinery module's exports (`emitGovernanceWave`, `resolveGovernanceParticipant`) each have in-tree consumers; exhaustive `switch`/`never` guard over the closed wave union; no cross-layer violations (services → repos/types/enum/shared-locale only, no GraphQL imports, no `console.*`, no nested ternaries); comments describe domain behavior only — **zero plan-artifact references** (grep across all touched files: clean). Scope boundary: no file outside the verified wave surface was touched; tasks.md/worklog untouched.
- **IV:** mirrors the established participant-wave registration pattern exactly (same namespace, same `resolveWaveContext` read, same receipt walk shape, same `tx?: DBTransaction` last-param convention, same `NotificationEngineCallOptions` test seam); enums via value imports (`NotificationType.SessionCancellation`); locale accepted as `string` param and propagated per call (backend.instructions.md i18n section); barrel discipline untouched (no new files → no barrel changes needed).

---

## 9. Carry-forward & cross-file dependencies

**For 4.1–4.3 (GraphQL):** resolvers need NO wave code — the service owns emission and publish-after-commit internally; only pass `ctx.locale` through to the service calls (it parameterizes server-side error copy, not wave copy, which is per-recipient). The `options?: NotificationEngineCallOptions` seam on the three mutation signatures exists for resolver-level tests (spied fan-out / in-memory claim cache) and can be threaded from 4.3's Tier-3/Tier-4 tests.

**For 5.x (UI):** the waves surface through the existing notification inbox rendering (engine type `NotificationType.SessionCancellation` for cancel, `SessionRequest` for reschedule/reassign, `relatedEntityType: "session"` + `relatedEntityId` for deep-link). If the UI ever needs the canonical copy keys: `eventSessionGovernanceRescheduledTitle/Body`, `eventSessionGovernanceCancelledTitle/Body`, `eventSessionGovernanceTeacherReassignedTitle/Body` (notifications namespace, en+ar registered). 5.x does not compose these keys itself — they are backend-emitter keys documented here for cross-referencing only.

**For 6.1–6.3 (journeys):** assert against the PINNED ids in §2; claim recipe `session:<sessionId>:<waveKind>`; receipts arrive in recipient order `[student, teacher]` / `[student, outgoingTeacher, incomingTeacher]`; publish fan-out is one transport call PER delivery receipt after the mutation's commit (per-receipt contract pinned by the engine's own tests + the 3.1 suite).

**Dependencies (upstream, verified standing):** `SessionRepository.findWaveContextById` (participant join read), `UserRepository.findById` (outgoing-teacher locale resolution), `NotificationEngine.emitForUser` / `publishReceipts` (single-writer engine seam), `SessionGovernanceWaveKind` type + notifications locale triple. All consumed as-is; none modified by this wave.
