# Outcome 4.4 — Codegen Reconcile (Phase 4 — GraphQL)

> **Task:** 4.4 (Phase 4 — GraphQL) — `bun run generate:gqlSchema && bun codegen` (REQ-060); verify generated TS types include the new ops
> **Branch:** `feat/dev3-021-admin-session-governance` (re-asserted before EVERY command batch; verified after every commit — the W-1 flip-war stayed active all session; one incident escaped a gate and was repaired, see §8)
> **Scope:** regenerate + commit the SDL artifact, run codegen, reconcile the stale `schema-surface.test.ts` frozen baseline, record the 5.1 generated-name contract. NO Pothos source changes were needed — the 4.1/4.2/4.3 registrations proved wire-exact.

---

## 1. Summary

Task 4.4 is **DONE**. `generate:gqlSchema` emitted the refreshed SDL artifact with the FULL admin session-governance surface (2 queries + 1 filter input + 4 mutations + 4 mutation inputs + the widened `Session.needsAttention: Boolean!`) and it is committed (`6fe34b5`). `bun run codegen` ran green (exit 0); its tracked output `frontend/graphql/generated/gql/graphql.ts` regenerated **byte-identically (no diff)** — correct behavior, because that file is document-driven and 5.1 has not authored the admin documents yet (see §5 for the probe that proves the operation types it will emit). The only source-adjacent defect codegen surfaced was the **stale frozen baseline in `backend/graphql/test/schema-surface.test.ts`** (the 5 remaining of the 6 pre-documented failing pins — codegen-sync itself now passes): reconciled to the live built schema per the file's own DEV3-017 precedent, **41 pass / 0 fail**, committed (`c38d9c0`). `bun run tsgo` → **exit 0 (0 errors)**. No naming mismatches vs the 4.2/4.3 carry-forwards exist anywhere on the wire — zero Pothos registration fixes required.

## 2. Codegen commands + results

| Step | Command | Result |
|---|---|---|
| 1. SDL regeneration | `bun run generate:gqlSchema` (→ `scripts/generator/generate-gql-schema.ts`) | **exit 0** — `[generate:gqlSchema] wrote frontend/graphql/generated/schema.graphql (24098 bytes)` (was 21784 stale). Branch verified before AND after the run. |
| 2. Commit artifact | `git add frontend/graphql/generated/schema.graphql && git commit` | `6fe34b5` — `chore(graphql): regenerate SDL artifact with admin session-governance surfaces` (+60 lines; both generated artifacts are **TRACKED** files, not gitignored — `git ls-files` confirms). |
| 3. TS codegen | `bun run codegen` (→ `graphql-codegen --config codegen.ts`) | **exit 0** — Load GraphQL schemas ✔ / Load GraphQL documents ✔ / Generate ✔. `frontend/graphql/generated/gql/graphql.ts` regenerated **byte-identically → no diff → nothing to commit** (document-driven output; `documents` globs = `frontend/graphql/sharedDocuments/**/*.ts` + `frontend/views/**/*.documents.ts`, none reference the admin surface yet; `ignoreNoDocuments: true` keeps CI green until 5.1). |
| 4. Baseline reconcile | `bun run test/scripts/run-test.ts backend/graphql/test/schema-surface.test.ts` | first run **36 pass / 5 fail** (the 5 stale frozen-baseline pins; the 6th documented failure — *codegen-sync byte-identical SDL* — now PASSES thanks to step 2) → reconciled → **41 pass / 0 fail / 263 expect()** |
| 5. Quality sub-loop | `bun run scripts/health/sub-loop.ts backend/graphql/test/schema-surface.test.ts --lifecycle duplicates` | **exit 0** — tsgo ✅ oxlint ✅ biome:check ✅ lint:type-aware ✅ check:duplicates ✅ (jscpd skip: outside scan scope) |
| 6. Project typecheck | `bun run tsgo` | **exit 0 — 0 errors** (branch verified before and after) |

## 3. Grep evidence — new surfaces in the committed SDL artifact

Path: `frontend/graphql/generated/schema.graphql` (committed @ `6fe34b5`). All greps re-run on a verified feature-branch tree:

```text
=== root queries ===
663:  adminSession(id: ID!): Session
668:  adminSessions(filter: AdminSessionListFilterInput!, page: Int = 1, pageSize: Int = 25): SessionPage!
=== root mutations ===
337:  adminCancelSession(input: AdminSessionCancelInput!): Session!
348:  adminJoinSession(input: AdminSessionJoinInput!): Session!
353:  adminReassignTeacher(input: AdminSessionReassignInput!): Session!
358:  adminRescheduleSession(input: AdminSessionRescheduleInput!): Session!
=== input types ===
48:input AdminSessionCancelInput      { reason: String;  sessionId: ID! }
53:input AdminSessionJoinInput        { sessionId: ID! }
57:input AdminSessionListFilterInput  { dateFrom: DateTime; dateTo: DateTime; status: SessionStatus;
                                        studentUserId: Int; teacherUserId: Int; type: SessionType }   # all six optional
66:input AdminSessionReassignInput    { newTeacherUserId: ID!; sessionId: ID! }
71:input AdminSessionRescheduleInput  { endedAt: DateTime!; sessionId: ID!; startedAt: DateTime! }
=== widened canonical Session object ===
777:  needsAttention: Boolean!
```

**11/11 carry-forward surfaces present, byte-exact vs REQ-060 and the 4.1/4.3 SDL pins.** `Session`/`SessionPage` are REUSED (no sibling admin object types), and `SessionPage { items: [Session!]!, page: Int!, pageSize: Int!, totalCount: Int! }` verified.

## 4. Wire-vs-carry-forward audit (one prose discrepancy found — recorded, no source fix)

- All 11 surface names, arg shapes, nullabilities, and SDL defaults match the 4.2 §2/§6 and 4.3 §2/§6 carry-forwards exactly.
- **Discrepancy (prose only, no code defect):** 4.2's outcome §2 transcribed the filter members as `teacherUserId: ID / studentUserId: ID`, but the ACTUAL wire (per 4.1's binding, the canonical zod input, and commit `9366099` "numeric Int filter values on the governance directory wire") is **`Int`** — confirmed in the regenerated SDL and in the generated TS (`studentUserId: number`). 5.1 must send **numeric** filter values. Recorded here so the error never propagates into the Apollo documents.

## 5. Generated TS verification (`frontend/graphql/generated/gql/graphql.ts`)

- The tracked file is **unchanged** by this task's codegen run (byte-identical regeneration — schema input/object types are emitted only when reachable from existing operation documents; no existing document references the admin surface yet). Grep for the new operation names in it: **0 matches** — EXPECTED pre-5.1, not a failure.
- **Operation-emission proof (verification-only probe, NOT a repo artifact):** a throwaway codegen run from `/tmp/dev344-probe/codegen.probe.ts` (same plugins/config as `codegen.ts`, schema pointed at the committed `schema.graphql`, probe documents mirroring the planned 5.1 shapes) generated `/tmp/dev344-probe/graphql.probe.ts` with **exit 0** and emitted EXACTLY:

```text
export type AdminSessionsQuery = { adminSessions: AdminSessionsQuery_adminSessions };
export type AdminSessionsQueryVariables = Exact<{ filter: AdminSessionListFilterInput; page?: Int; pageSize?: Int }>;
export type AdminSessionQuery = { adminSession: AdminSessionQuery_adminSession | null };   // nullable payload — null-not-error posture preserved in TS
export type AdminSessionRescheduleMutation / AdminSessionCancelMutation /
           AdminSessionReassignMutation / AdminSessionJoinMutation (+ …Variables)          // all four
AdminSessionListFilterInput = { dateFrom: string | null | undefined; dateTo: string | null | undefined;
                                status: SessionStatus | null | undefined; studentUserId: number | null | undefined;
                                teacherUserId: number | null | undefined; type: SessionType | null | undefined }
AdminSessionRescheduleInput = { endedAt: string; sessionId: string | number; startedAt: string }  // ID scalar → string | number
…needsAttention: boolean        // emitted in every Session selection typed by the probe
export const AdminSessionsDocument / AdminSessionDocument / AdminSession{Reschedule,Cancel,Reassign,Join}Document  // TypedDocumentNode constants
```

(`DateTime` scalar maps to `string` per `codegen.ts`; `avoidOptionals: true` → `| null | undefined` on nullable members; `enumType: native`.) The repo-side regeneration will produce these names the moment 5.1's documents exist under the codegen `documents` globs.

## 6. Files committed by 4.4

| File | Change | Commit |
|---|---|---|
| `frontend/graphql/generated/schema.graphql` | regenerated SDL artifact (+60 lines: 11 surfaces + `needsAttention` + docblocks) — **TRACKED file, committed** (not gitignored; CI regenerates but the repo keeps it in lockstep, enforced by the suite's codegen-sync pin) | `6fe34b5` |
| `backend/graphql/test/schema-surface.test.ts` | frozen-baseline reconcile (+138/−26; §7) | `c38d9c0` |
| `frontend/graphql/generated/gql/graphql.ts` | regenerated byte-identically — no diff, nothing to commit | — |

## 7. The schema-surface reconcile (the one codegen-surfaced defect)

4.1 §6/4.2 §5/4.3 §6 all pre-assigned the 6 failing `schema-surface.test.ts` pins to 4.4's reconcile. After the SDL regeneration fixed the codegen-sync pin, 5 remained. Diagnosis: the frozen baseline was stale in TWO layers — (a) this ticket's sanctioned additions (never yet pinned), and (b) **four already-merged PRs whose surfaces were never enumerated** by their own tickets (verified pre-existing: all four modules are ancestors of the branch point `ffce457`, i.e. the pins were red before this ticket's first Phase 4 commit). Reconciled per the file's own DEV3-017 precedent (re-anchor to the LIVE built schema via `printSchema(lexicographicSortSchema(graphQLSchema))`, documented constants, "one-time reconciliation, not a silent baseline flip"):

- **DEV3-021 sanctioned additions:** `DEV3_021_QUERY_FIELDS` (`adminSession`, `adminSessions`), `DEV3_021_MUTATION_FIELDS` (governance quartet), `DEV3_021_TYPE_NAMES` (5 inputs). NO new enum (the freeze holds — the surface reuses `SessionStatus`/`SessionType`).
- **Merged-PR re-anchors (`MERGED_PR*` constants, provenance in-file):** PR #49 parent-link (`288997b`) — 2 queries, 3 mutations, enum `LinkStatus`, types `IncomingParentLinkRequest`/`OutgoingParentLinkRequest`; PR #51 cold-start (`1723cfc`) — mutation `adminCertifyTeacherColdStart`; PR #53 broadcast + audit trail (`674ce2e`) — mutation `adminBroadcastNotification`, query `adminAuditLogs`, enum `BroadcastAudienceType`, types `AdminBroadcastNotificationInput`/`BroadcastAudienceInput`/`AdminAuditLogEntry`/`AdminAuditLogFiltersInput`/`AdminAuditLogPage`.
- **Sorted-positions pin re-anchored:** the DEV3-021 quartet lexically interleaves the prior five-field admin-USER block, so the contiguity pin now covers the whole contiguous eleven-field `admin*` mutation block (verified against the live sorted inventory).
- Docblock gained a `Reconciliation note (DEV3-021, 4.4 codegen reconcile)` paragraph naming every addition + the codegen-sync restoration.

**Orchestrator note:** the `MERGED_PR*` re-anchors are descriptive pins over already-merged, already-reviewed surfaces — included because array-equality pins cannot be partially reconciled. Flagged here (and as a 7.3 input) so the owning tickets/orchestrator know their surfaces were re-anchored inside this ticket's diff.

## 8. W-1 flip-war incident log (transparency)

- **Escaped-gate incident:** the `git commit` for the regenerated SDL executed after the watcher flipped HEAD to `main` — commit `e33e6ba` landed on main. **Repair (same session, no forbidden ops):** checked out the feature branch, `git cherry-pick e33e6ba` → `6fe34b5` on `feat/dev3-021-admin-session-governance`, then `git branch -f main f71c8f3` (ref move only — no `checkout main`, no `reset --hard`, no `-f` checkouts) restoring main to its pre-incident tip. Verified: feature tip `6fe34b5`, main tip `f71c8f3`.
- All other batches began with `git checkout feat/dev3-021-admin-session-governance && git branch --show-current` (verified); four diagnostics runs that started on main were VOIDED and re-run on verified trees. Recorded results are exclusively from verified feature-branch batches.

## 9. Carry-forward for 5.1 (exact generated names — probe-proven)

**Generated types (from `@/frontend/graphql/generated/gql/graphql`, materialize on 5.1's first `bun run codegen`):**
- `AdminSessionsQuery` + `AdminSessionsQueryVariables` (+ nested `AdminSessionsQuery_adminSessions`, `AdminSessionsQuery_adminSessions_items`)
- `AdminSessionQuery` + `AdminSessionQueryVariables` — payload `adminSession: … | null` (render absence from `null`, no errors-path)
- `AdminSessionRescheduleMutation(+Variables)`, `AdminSessionCancelMutation(+Variables)`, `AdminSessionReassignMutation(+Variables)`, `AdminSessionJoinMutation(+Variables)` — each payload `{ adminXxx: { id, needsAttention, status, … } }`

**Documents-layer exports (repo convention, `documents.contract.test.ts`: operation name must match the `{entityName}…Document` export, camelCase consts, `id` first on every object selection):**
`adminSessionsQueryDocument` (op `AdminSessions`), `adminSessionQueryDocument` (op `AdminSession`), `adminSessionRescheduleMutationDocument` (op `AdminSessionReschedule`), `adminSessionCancelMutationDocument` (op `AdminSessionCancel`), `adminSessionReassignMutationDocument` (op `AdminSessionReassign`), `adminSessionJoinMutationDocument` (op `AdminSessionJoin`). Files land under `frontend/graphql/sharedDocuments/**` (already a codegen documents glob — 0 config changes needed) and must be re-exported through the `sharedDocuments` barrel (contract test #5).

**Wire rules for the documents:** `filter` REQUIRED (`AdminSessionListFilterInput!`); `page`/`pageSize` optional (SDL defaults 1/25; explicit `null` normalizes at the resolver); filter ids are **`Int` → send numbers** (§4); every `Session` selection MUST include `id` (+ `needsAttention` per 4.2 §3 — `true` possible on directory rows, always `false` on the detail read); mutation payloads return `Session!` (select `id` first for cache normalization); `adminSession` payload nullable → `data.adminSession === null` means "no such row".

**CI note:** `ignoreNoDocuments: true` means a pre-5.1 CI codegen run stays green even with zero new documents; 5.1's documents make the §5 names materialize (then `bun run codegen` MUST be re-run and `graphql.ts` committed with the documents).

## 10. Cross-file dependencies

- `scripts/generator/generate-gql-schema.ts` → `@/backend/graphql/gqlSchema` (production chain incl. the 4.1/4.2/4.3 registrations) → `frontend/graphql/generated/schema.graphql`.
- `codegen.ts` → reads the committed SDL + documents globs → `frontend/graphql/generated/gql/graphql.ts`.
- `backend/graphql/test/schema-surface.test.ts` codegen-sync pin ↔ committed SDL byte-identity (regeneration restored it; it re-fails if the SDL ever goes stale again).
- `frontend/graphql/sharedDocuments/documents.contract.test.ts` + `sharedDocuments/AGENTS.md` → the 5.1 naming/`id` contract summarized in §9.
- Upstream outcomes consumed: 4.1 (filter input + SDL pins), 4.2 (queries + `needsAttention` mechanism + Session SDL recalibration), 4.3 (mutation quartet + inputs + idempotency mechanism) — all verified byte-exact on the wire by this task; no upstream fixes required.
