# Outcome 5.1 — Apollo Documents Module (Phase 5 — Frontend & Navigation)

> **Task:** 5.1 — CREATE the shared Apollo documents for the admin session-governance surface (REQ-063; REQ-010..012/020..027 field needs; plan §5 document list)
> **Branch:** `feat/dev3-021-admin-session-governance` (re-asserted before EVERY command batch; the W-1 flip-war stayed active all session — every recorded result below comes from a verified feature-branch tree; three diagnostics runs that landed on `main` were VOIDED and re-run)
> **Scope:** documents file + admin sub-barrel row + contract-test rows/bindings + regenerated `graphql.ts`. Zero other source changes.

---

## 1. Summary

Task 5.1 is **DONE**. The F-1 HIGH plan-review finding is applied exactly as resolved: the documents live at **`frontend/graphql/sharedDocuments/admin/admin-session-governance.documents.ts`** (domain sub-directory + barrel — `AGENTS.md` wins over REQ-063's literal flat path `sharedDocuments/adminSessions.documents.ts`, which is marked "Never (stale flat path)" in the layer rules). The file hosts **6 operations** (2 queries + 4 mutations) in the exemplar (`scheduling/session-lifecycle.documents.ts` + `session-reads.documents.ts`) style: inline selections, `id` first on every object selection, ONE uniform `Session` field shape across the family, `TypedDocumentNode<Op, OpVariables>` annotations over codegen types only, NO hooks co-located (hooks are consumed in views from `@apollo/client/react`; `useLazyQuery` banned). Registered in the existing `admin/` sub-barrel (`index.ts` — alphabetical position, first row). `bun run codegen` regenerated the tracked `frontend/graphql/generated/gql/graphql.ts` and it **gained exactly the 6 operation types + Variables + 6 TypedDocumentNode constants** predicted by 4.4 §9's probe — all 11 predicted name families materialized byte-consistently. The shared-document contract suite passes **20 pass / 0 fail / 124 expect()** with the 6 new table rows, 6 new compile-time typed bindings, and a new barrel≡deep-import identity assertion. `bun run tsgo` → **exit 0 (0 errors)** project-wide. QL sub-loop (`--lifecycle duplicates`) → **exit 0** on both the documents file and the barrel (tsgo ✅ oxlint ✅ biome:check ✅ lint:type-aware ✅ check:duplicates ✅). One out-of-scope discovery (REQ-010's `durationMinutes` is not on the wire) → deferred ledger row **D-07** + watch bullet, committed.

## 2. Document names + operation/variable shapes (the 5.2 import surface)

File: `frontend/graphql/sharedDocuments/admin/admin-session-governance.documents.ts`

| Export (const) | GraphQL operation | Channel | Variables declared |
|---|---|---|---|
| `adminSessionsQueryDocument` | `query AdminSessions($filter: AdminSessionListFilterInput!, $page: Int, $pageSize: Int)` → `adminSessions { items { …Session }, page, pageSize, totalCount }` | query | `filter` (REQUIRED, non-null), `page?`, `pageSize?` (server defaults 1/25) |
| `adminSessionQueryDocument` | `query AdminSession($id: ID!)` → `adminSession { …Session }` | query | `id: string \| number` |
| `adminSessionRescheduleMutationDocument` | `mutation AdminSessionReschedule($input: AdminSessionRescheduleInput!)` → `adminRescheduleSession { …Session }` | mutation | `input` |
| `adminSessionCancelMutationDocument` | `mutation AdminSessionCancel($input: AdminSessionCancelInput!)` → `adminCancelSession { …Session }` | mutation | `input` |
| `adminSessionReassignMutationDocument` | `mutation AdminSessionReassign($input: AdminSessionReassignInput!)` → `adminReassignTeacher { …Session }` | mutation | `input` |
| `adminSessionJoinMutationDocument` | `mutation AdminSessionJoin($input: AdminSessionJoinInput!)` → `adminJoinSession { …Session }` | mutation | `input` |

Naming follows the binding 4.4 §9 contract and the `{entityName}…Document` ↔ operation-name rule the contract test enforces (op `AdminSessionReassign` ↔ `adminSessionReassignMutationDocument`); note the plan §5's literal `AdminSessionReassignTeacherMutationDocument` spelling is superseded — the wire field is `adminReassignTeacher` and the op name drops the wire prefix per the codegen probe canon. `items`/`totalCount` (not `rows`/`total`) are the real `SessionPage` wire names (4.4 §3 byte-exact SDL; same selection as the participant list documents).

**Uniform `Session` selection (all 6 documents, `id` first, exemplar field order + the governance badge):**
`id, status, intent, sessionType, fee, feeHeld, studentId, teacherId, startedAt, endedAt, confirmationDeadline, confirmedByStudentAt, confirmedByTeacherAt, createdAt, updatedAt, cancelReason, disputeReason, disputedAt, resolutionNote, resolvedAt, needsAttention`
— i.e. the participant family's "ONE field shape" + `needsAttention: Boolean!` (4.2 §3: selected on BOTH reads; `true` possible on directory rows, always `false` on detail/mutations, keeping mutation cache writes badge-correct). The wire field is `sessionType` (REQ-010's "type" maps to it); `durationMinutes` is NOT selectable (see §4/D-07).

**Generated variable shapes (all verified in the regenerated `graphql.ts`):**
- `AdminSessionsQueryVariables = Exact<{ filter: AdminSessionListFilterInput; page: number | null | undefined; pageSize: number | null | undefined }>`
- `AdminSessionListFilterInput = { dateFrom?: string; dateTo?: string; status?: SessionStatus; studentUserId: number | null | undefined; teacherUserId: number | null | undefined; type?: SessionType }` — **filter ids are `Int` → send numbers** (4.4 §4 reconciliation honored)
- `AdminSessionQueryVariables = Exact<{ id: string | number }>`
- `AdminSessionRescheduleInput = { endedAt: string; sessionId: string | number; startedAt: string }`; `AdminSessionCancelInput = { reason?: string; sessionId: string | number }`; `AdminSessionReassignInput = { newTeacherUserId: string | number; sessionId: string | number }`; `AdminSessionJoinInput = { sessionId: string | number }`
- Payloads: `AdminSessionsQuery = { adminSessions: AdminSessionsQuery_adminSessions }`; `AdminSessionQuery = { adminSession: AdminSessionQuery_adminSession | null }` (nullable — `data.adminSession === null` means "no such row", render absence, no errors-path); each mutation = `{ adminXxx: AdminSessionXxxMutation_adminXxx }` with the full uniform `Session` row (incl. `needsAttention: boolean`).

## 3. Files modified (one commit each — per-file immediate commits)

| File | Change | Commit |
|---|---|---|
| `frontend/graphql/sharedDocuments/admin/admin-session-governance.documents.ts` | **CREATE** — 6 documents (271 lines) | `bb1f5c7` |
| `frontend/graphql/generated/gql/graphql.ts` | regenerated by `bun run codegen` — +6 operation types + Variables + 6 `Document` consts (+93 lines) | `7fedb11` |
| `frontend/graphql/sharedDocuments/admin/index.ts` | barrel row `export * from "./admin-session-governance.documents";` (alphabetical, first) | `1d20fd7` |
| `frontend/graphql/sharedDocuments/documents.contract.test.ts` | +6 `DOCUMENT_CONTRACT_TABLE` rows (new DEV3-021 section), +6 typed bindings + expects (compile-time proof), +1 barrel≡deep-import identity assertion (`adminSessionsViaBarrel`) — 90 insertions, biome-clean with no fixes needed | `2971bdd` |
| `ai/plans/sprint_3/dev3-021-admin-session-governance/deferred-items.md` | watch bullet + ledger row D-07 (`durationMinutes` wire gap, §4) | `0d03859` |

Top-level `sharedDocuments/index.ts` untouched — `export * from "./admin"` already existed. No AGENTS.md layout edit owed: `admin/` is an existing sub-directory (the Layout tree's omission of `admin/` is a pre-existing documentation gap from earlier features — flagged for 7.2, not patched here to keep this task's blast radius scoped).

## 4. Out-of-scope discovery → deferred ledger

**REQ-010 lists `durationMinutes` among directory-row fields, but the canonical `Session` GraphQL object does not expose it** (Phase 4 reused `Session`/`SessionPage` without a duration member; the DB/return-type column exists only server-side). The documents therefore select the canonical 21-field `Session` shape and 5.2 renders duration from `startedAt`/`endedAt` (same derivation the service performs). Recorded as ledger row **D-07** + watch bullet (committed `0d03859`); presentation-only gap, no wire change owed by this ticket.

## 5. Codegen results (post-document regeneration)

- `bun run codegen` (schema input = the committed 4.4 `schema.graphql`; documents globs already include `sharedDocuments/**` — zero config changes): **exit 0**, `graphql.ts` +93 lines, committed `7fedb11`.
- Verification greps on the regenerated file — ALL present, names byte-matching 4.4 §9's probe: `AdminSessionsQuery(_adminSessions(_items))`, `AdminSessionQuery(_adminSession | null)`, `AdminSession{Reschedule,Cancel,Reassign,Join}Mutation(_admin*)` + all 6 `…Variables`; TypedDocumentNode consts `AdminSessionsDocument`, `AdminSessionDocument`, `AdminSession{Reschedule,Cancel,Reassign,Join}Document` (codegen emits `{Op}Document` consts; the repo-facing `{entityName}…Document` export names live in the documents layer per convention). `needsAttention: boolean` appears in every emitted `Session` row type. `AdminSessionsQueryVariables` pins `filter` non-null; filter id members typed `number`.
- `bun run generate:gqlSchema` NOT re-run: the committed SDL artifact is already current (4.4), and the schema-side codegen-sync pin stays green because documents don't affect the SDL.

## 6. Contract-test results (5.1.TE)

`bun run test/scripts/run-test.ts frontend/graphql/sharedDocuments/documents.contract.test.ts` on a verified feature tree → **20 pass / 0 fail / 124 expect()** (baseline before this task: 14 tests). New coverage: 6 named-operation/channel/variable rows; `id`-requirement resolution over `adminSessions.items`, `adminSession`, and the four mutation payloads; barrel≡deep-import instance identity for `adminSessionsQueryDocument`; TypedDocumentNode assignment proofs for all 6 documents against the regenerated operation types.

## 7. Pipeline record (QL / SEC / SR / IV)

- **5.1.QL** — sub-loop `--lifecycle duplicates` exit 0 on `admin/admin-session-governance.documents.ts` AND `admin/index.ts` (tsgo, oxlint, biome:check, lint:type-aware, check:duplicates all ✅ — no fallback needed). `bun run tsgo` project-wide exit 0. Biome `check --write` on the contract test: "No fixes applied" (formatting canonical on first write).
- **5.1.SEC** — selections expose only the canonical domain surface (`Session`/`SessionPage` fields; no `passwordHash`, no meeting-provider credentials/tokens, no internal columns — `needsAttention` is presentation-only per its resolver docblock). Documents carry no secrets; the cancel idempotency key travels via the transport header (`X-Idempotency-Key`), never in the document text.
- **5.1.SR** — blast radius = exactly the five files in §3 (verified per-commit `--name-only`); no new sub-directory, no barrel re-ordering beyond the alphabetical insert, no co-located test file (contract rows are the TE vehicle per task scope; the sibling `.documents.test.ts` precedent noted for 5.2's component-test phase); zero plan-artifact references in code comments (comments cite layer AGENTS.md rules and the SDL surface only).
- **5.1.IV** — `frontend.instructions.md` (Apollo & GraphQL block: `gql`/`TypedDocumentNode` from `@apollo/client`, hooks from `@apollo/client/react`, NO `useLazyQuery`, generated-types-only with no mapping/inline literals, codegen both steps) ✅; `frontend/graphql/AGENTS.md` + `frontend/graphql/sharedDocuments/AGENTS.md` (domain sub-directory + barrel, `{entityName}…Document` convention, `id` Field Requirement, no `@apollo/client/core` imports) ✅; `tests.instructions.md` (bun:test, run-test script usage, stale-pin rule — no external pin tests reference the contract table, verified by grep) ✅. REQ-063 satisfied in substance via the F-1-resolved path (deviation recorded per 0.3 §4 condition 1).

## 8. Carry-forward for 5.2 (exact hook/import surface)

```ts
import { useQuery, useMutation } from "@apollo/client/react";           // hooks ONLY
import {
  adminSessionsQueryDocument,      // directory: useQuery(adminSessionsQueryDocument, { variables: { filter, page, pageSize } })
  adminSessionQueryDocument,       // drawer detail: useQuery(adminSessionQueryDocument, { variables: { id } }); handle data.adminSession === null
  adminSessionRescheduleMutationDocument,
  adminSessionCancelMutationDocument, // idempotent: send context.headers["X-Idempotency-Key"] per logical cancel attempt
  adminSessionReassignMutationDocument,
  adminSessionJoinMutationDocument,
} from "@/frontend/graphql/sharedDocuments";                            // barrel (preferred) — deep import path also valid:
// "@/frontend/graphql/sharedDocuments/admin/admin-session-governance.documents"
```
- `filter` values: `studentUserId`/`teacherUserId` are **numbers**; `status`/`type` are native enums (`SessionStatus.Scheduled` …); `dateFrom`/`dateTo` ISO-8601 strings (half-open `>= from`, `< to` UTC).
- Mutations return the full `Session` row — cache converges by `id` (no list refetch needed); render `needsAttention` badge from directory rows only (detail always `false`).
- Duration column: derive from `startedAt`/`endedAt` (D-07).
- NO `useLazyQuery`; error surface via the existing `errorPolicy`/graphql error-surface utilities.

## 9. Cross-file dependencies

- `frontend/graphql/generated/schema.graphql` (4.4 artifact) → `codegen.ts` → `frontend/graphql/generated/gql/graphql.ts` → this documents file's type imports.
- `frontend/graphql/sharedDocuments/admin/index.ts` → top-level `sharedDocuments/index.ts` (`export * from "./admin"`) → the barrel identity the contract test pins.
- `documents.contract.test.ts` rows are structural pins: any future selection/variable drift in these 6 documents fails the suite (update rows in the same change — tests.instructions stale-pin rule).
- Upstream outcomes consumed: 4.4 §9 (names + wire rules — all honored byte-exact), 4.2 §3/§5 (`needsAttention` on both reads; `items`/`totalCount` selection), 4.3 (mutation inputs + idempotency header mechanism), 0.3 F-1 (resolved path).
- Concurrent-agent note: the 5.4 nav agent committed interleaved on the same branch (`f7b5104`, `70ac71f`-side work); all five commits above touch disjoint files — zero cross-agent conflicts.

## 10. Flip-war incident log (transparency)

No commit escaped to `main` this session. The watcher flipped HEAD between command batches ~20× (reflog-verified); mitigations: branch re-assertion before every batch, untracked-file-then-commit ordering, atomic patch+add+commit single commands for tracked files, flip-proof reads via `git show feat/…:<path>`. Three diagnostics runs (one codegen invocation, two type-shape greps) executed against a flipped `main` tree produced no writes and were VOIDED and re-run on verified trees; `bun run codegen` and the commit `7fedb11` were verified to have landed on the feature branch via `git log -- <path>`.
