# Outcome 6.1–6.4 — Cross-Actor Journey Suite (Phase 6 — journeys) — ALL GREEN

> **Tasks:** 6.1 (W-1 cancel-with-refund) · 6.2 (W-2 reassign) · 6.3 (W-3 join) · 6.4 (W-4 role-denial matrix) + subtasks .QL/.TE/.SR/.IV (6.4 also .SEC)
> **Branch:** `feat/dev3-021-admin-session-governance` (re-asserted before EVERY command batch; every commit verified to have landed on feat)
> **Deliverable:** `test/workflows/admin/admin-session-governance.journey.test.ts` — the four W-journeys in ONE file. **Final: 13 pass / 0 fail / 228 expect() calls, TWO consecutive green runs (idempotent-teardown proof, REQ-J6); sub-loop `--lifecycle duplicates` exit 0; project `tsgo` exit 0.**

---

## 1. Summary

Task 6.1–6.4 is **DONE**. The 534-line draft (commit `117cdfe`) was restored onto the feature branch and COMPLETED, not rewritten: the draft's harness (journey prefix, fixture registry, spied-emit seam, read-back oracles, audit-details type-guard parser) and its five W-1 step tests were preserved verbatim except where noted in §5. This wave added the W-2/W-3 real-flow fixtures, the try/catch denial-inspection + byte-identity oracles, and the three remaining journey describe blocks. Final state: all four journeys green through the REAL `SessionAdminGovernanceService` on the REAL test database (pglite, serialized), zero `runInRollback`, zero `.rejects.toThrow()`.

**Environment note (flip-war, recorded for the orchestrator):** mid-task the branch ref was reconciled from the lineage-A tip (`6eb5200`) to `b15f16b` — the flip-war-orphan restoration that carries the draft commit `117cdfe` itself — while HEAD kept alternating with local `main` between batches (one Edit-tool batch and one patch batch were lost to a mid-batch flip and re-applied; no commit ever landed on `main`). All five task commits below were made on feat and verified there immediately after.

| Commit | Content |
|---|---|
| `1b558d8` | harness extensions: W-2/W-3 real-flow booking fixtures + per-session ids + `expectJourneyError`/`expectDenialByteIdentical` oracles |
| `8220a32` | W-2 reassign + W-3 join + W-4 role-denial matrix journeys (+382) |
| `dcf5561` | W-3 read-equivalence compared second-aligned across executors (+28/−2) |
| `286dd9a` | W-4 role vocabulary resolved lazily (describe-body `cast` is not yet bound at registration time) (+4/−4) |
| `a2d9268` | afterAll claim sweep batched via `Promise.all` (sub-loop `no-await-in-loop`) (+5/−2) |
| `133ead4` | formatter pass on the journey file (import order + line wrapping; suite + sub-loop re-verified green) (+13/−5) |

All six commits touch exactly ONE file: `test/workflows/admin/admin-session-governance.journey.test.ts` (final size 1032 lines). Outcome files: this document.

## 2. Harness (layer-contract compliance — REQ-074/075)

- **REAL services → REAL DB, NO `runInRollback`** — every step calls the real service on the production (no-`outerTx`) transaction path; publish-after-commit is observable through the documented `NotificationEngineCallOptions` seam.
- **Fixtures committed in `beforeAll` inside ONE commit-or-nothing `db.transaction`** and hard-deleted in `afterAll`: the `buildSessionJourneyCast` cast (funded primary student `trial: 3` — three trial units fund the three held bookings on the SAME provenance lane, zero-balance second student, certified teacher pair T1/T2, applicant, parent, admin), a REAL unapproved `teacher` row (honest strict-true certification-denial target), per-recipient wave locales (student en, outgoing teacher ar, incoming teacher en), three sessions booked through the REAL `SessionLifecycleService.createSession` (W-1/W-2/W-3, each with its idempotency claim tracked), and the direct-insert lapsed-deadline badge row (the W-3 join-denial and W-4 matrix target — denials write nothing). `afterAll` sweeps the governance audit rows first under `withAuditDeleteTriggersSuspended` (append-only + actor-RESTRICT), then `registry.cleanup()`, then mandatory zero-residue probes (cast users, fixture sessions, spent claim keys).
- **Spied, never sent** — a fresh `SpiedFanoutTransport` + in-memory `MemoryEmitClaimCache` per mutation call (`emitSpyOptions`); the negative W-2 arm additionally asserts `publishCount === 0`.
- **Honest roles only** — every denial resolves through the real `users.role` rows; the anonymous tier is the gate's own sentinel (`actorId = 0`); no monkey-patching.
- **REQ-075** — all denials through `expectJourneyError` (returns the caught `DomainError`) + exact `getServerTranslations("en").errorsTranslations` copy assertions; zero `.rejects.toThrow()`.

## 3. Per-journey results

| Journey | Steps (pass) | Key asserts | Fixtures | Spy points | Result |
|---|---|---|---|---|---|
| **W-1** cancel-with-refund (6.1, draft completed) | 5 | Directory filter `status=scheduled` shows the held row (badge FALSE) and the lapsed-deadline row alone (badge TRUE) · detail read zero-side-effects · cancel: status flip committed, `feeHeld` stays on the returned row (the refund, not the guard, releases), SAME-lane refund exactly +1 trial unit with hifz/tajweed untouched, EXACTLY ONE audit row `{action:"cancel", reason:"Operational override"}` (Override/session/admin), student list shows the cancelled row, admin re-filter sees the row under `status=cancelled`, gone from `scheduled` | real booking `w1` + claim; badge row | `publishCount 2`, `publishedUserIds [student, teacher]`; inbox +1 per recipient; cancel wave copy per RECIPIENT locale (student en, teacher ar), `NotificationType.SessionCancellation` | ✅ 5/5 |
| **W-2** reassign (6.2) | 3 | T1→T2 (both certified): row `teacherId = T2` committed + student-facing `listMyStudentSessions` shows the NEW teacher + outgoing teacher's list lost the row + incoming teacher's list gained it · EXACTLY ONE audit row `{action:"reassign", from:{teacherId:T1}, to:{teacherId:T2}}` · certification denial: unapproved candidate → `ConflictError` code `TEACHER_NOT_CERTIFIED` + localized `t.teacherNotCertified` + row byte-identical + zero audit rows + zero inbox movement + `publishCount 0` | real booking `w2` + claim; real unapproved `teacher` row | `publishCount 3`, `publishedUserIds [student, outgoing, incoming]` (the 3.2 §2 pinned triple); wave copy per recipient locale (en / ar / en), `NotificationType.SessionRequest`; non-participant inboxes stay 0 | ✅ 3/3 |
| **W-3** join (6.3) | 3 | Teacher-actor start precondition (no audit rows yet) · join STARTED: audit delta == EXACTLY 1 `{action:"join_observe"}` (Override/session/admin), returned row participant-equivalent to `getDetail` (second-aligned comparison, §5) and the stored row byte-identical (zero column change), no wave (inbox deltas 0) · join SCHEDULED: localized `ConflictError` `SESSION_INVALID_TRANSITION` + audit delta == 0 + row byte-identical | real booking `w3` (started in-step by its owning teacher); badge row (scheduled target) | none (join emits no wave — asserted by inbox deltas) | ✅ 3/3 |
| **W-4** role-denial matrix (6.4) | 2 | 3 persisted roles {teacher, student, parent} × 6 operations (adminSessions, adminSession, adminRescheduleSession, adminCancelSession, adminReassignTeacher, adminJoinSession): every denial `ForbiddenError`/`FORBIDDEN`/`t.forbidden` **byte-identical to the `resolveSessionDispute` reference denial** captured live per role · anonymous (`actorId 0`) × 6: `UnauthorizedError`/`UNAUTHORIZED`/`t.unauthorized` byte-identical · ZERO writes: both mutation-target rows byte-identical, zero audit rows by any denied actor, zero inbox deltas across the whole cast | badge + `w2` rows as untouched targets; real role rows | none expected — `publishCount` absence proven by inbox deltas; spy not needed on gate-only denials | ✅ 2/2 |

**13/13 tests pass · 0 fail · 0 skip · 228 expect() calls** (run twice consecutively — second green run proves zero residual state per the harness idempotent-teardown rule).

## 4. 7-vs-6 operation reconciliation (specs' actor table vs the implemented surface — documented, pinned in the test file)

The specs' Tier-4/actor-table wording says "all **7** operations (403)" per role. The implemented governance surface is **6** operations (`adminSessions`, `adminSession`, `adminRescheduleSession`, `adminCancelSession`, `adminReassignTeacher`, `adminJoinSession` — REQ-060's two queries + 4.3's four mutations). The specs counted `resolveSessionDispute` — the pre-existing shared admin-gate mutation on the session domain — as the seventh. W-4 therefore runs the 6 implemented operations as matrix rows and uses the 7th (`SessionLifecycleService.resolveSessionDispute`) **as the live byte-identity reference denial** (same `assertActorAdmin`-family gate, same localized copy), exactly as the task brief and the 4.3 outcome prescribe. The role vocabulary is maximal at three persisted non-admin roles (`users.role` = {admin, teacher, student, parent}); the specs' `supervisor` row is not a persistable user role (permission-group concept only — the 3.1 outcome's ledger row), so the 3×6 (+anonymous×6) matrix is exhaustive over real data.

## 5. Fix rounds (all TEST-SIDE; product code untouched)

1. **W-3 read-equivalence precision (`dcf5561`):** `join`'s return (guarded UPDATE `.returning()`) carries millisecond Date remainders while the `queryDb`-path `getDetail` returns whole-second Dates for the SAME stored rows — the executor-level face of the whole-second column convention already documented by the 2.1 outcome's `alignedInstant` aid. Fixed with a `comparableSessionRow` projection (every Date floored to whole seconds) instead of weakening the assertion to field picks. Not an implementation bug: both rows describe identical stored values.
2. **W-4 describe-body eagerness (`286dd9a`):** `bun:test` evaluates describe callbacks at file load, before `beforeAll` binds `cast` — the role vocabulary became a lazy accessor (mirroring the existing `castInboxUserIds` pattern).
3. **Sub-loop lint (`a2d9268`):** the draft's afterAll claim sweep awaited `db.$count` inside a `for` loop (`no-await-in-loop`); batched via `Promise.all` and asserted from the resolved counts. This was the only sub-loop finding; second run exit 0.

## 6. Verification results (exit codes + counts)

| Gate | Command | Result |
|---|---|---|
| 6.x.TE | `bun run test/scripts/run-test.ts test/workflows/admin/admin-session-governance.journey.test.ts` | **exit 0 — 13 pass / 0 fail / 228 expect()** (twice consecutively; teardown idempotent) |
| 6.x.QL | `bun run scripts/health/sub-loop.ts test/workflows/admin/admin-session-governance.journey.test.ts --lifecycle duplicates` | **exit 0** — tsgo ✅ oxlint ✅ biome:check ✅ lint:type-aware ✅ check:duplicates ✅ (one fix round, §5.3) |
| Project typecheck | `bun run tsgo` | **exit 0** |

## 7. SEC / SR / IV

- **SEC (6.4):** the BFLA matrix is maximal over the persistable role vocabulary AND the anonymous sentinel; every denial byte-identical to the shared-gate reference (class + `extensions.code` + localized message); zero reads past the gate (gate-only denials) and zero writes anywhere — target rows byte-identical, zero audit rows by denied actors, zero inbox movement. BOPLA: the matrix passes field-literal inputs only; no spread of any input anywhere in the file.
- **SR:** no dead code (every helper has in-file consumers); assertions scoped to fixture ids / per-actor deltas so pre-existing shared-DB rows can never satisfy or break them; comments describe domain behavior only (zero plan-artifact references); the W-1 draft's sound work preserved — the only draft change beyond fixture plumbing is the §5.3 batching fix; no `console.*`, no `any`, no `expect(...).rejects.toThrow()`, no `runInRollback`.
- **IV:** mirrors `test/workflows/AGENTS.md` + the exemplars (audit-trail/admin-user-denials) — committed cast via `buildSessionJourneyCast`, `SpiedFanoutTransport` + in-memory claim cache through the `NotificationEngineCallOptions` seam, `expectJourneyError` try/catch pattern, `withAuditDeleteTriggersSuspended` teardown, `@/` alias imports, `bun:test` only. Wave-id assertions use the 3.2 §2 pinned ids (`sessionGovernance.*` copy keys); the reassign recipient triple follows the 3.2 §2 precision note `[student, outgoingTeacher, incomingTeacher]`.

## 8. Deferred rows

**None added.** No out-of-scope product work discovered; the executor timestamp-precision nuance is test-observation only and follows the already-documented whole-second convention (2.1 outcome §8.5). `tasks.md`/`worklog.md` intentionally untouched per task rules (6.1–6.4 checkboxes remain for the orchestrator/final gate).
