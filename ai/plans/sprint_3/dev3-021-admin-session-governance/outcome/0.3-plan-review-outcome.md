# 0.3 Plan-Review Outcome — DEV3-021 Admin Session Governance

> **Task:** 0.4 (Phase 0 — Plan-review gate) · **Outcome file id:** `0.3-plan-review-outcome.md` (per tasks.md naming)
> **Reviewer:** plan-review subagent · **Methodology:** `.agents/skills/plan-review/SKILL.md` (read in full) over `specs.md` + `plan.md` + `tasks.md` (+ `deferred-items.md`, `prototype/screens.json`)
> **Date:** Phase 0, post 0.1–0.3 · **Branch:** `feat/dev3-021-admin-session-governance`

---

## Verdict: **PASS-WITH-NOTES**

Phase 1 execution **may begin immediately**. Zero CRITICAL findings. Ten findings recorded (1 HIGH, 4 MEDIUM, 5 LOW); every one has a deterministic, in-flight resolution that does **not** require editing the plan files — the repo's layer AGENTS.md files and the plan's own canonical enumerations supply the ruling. Conditions attached to specific phases are listed in §4 and must be honored at the named tasks; none blocks Phase 1–4 start.

---

## 1. Scope of review performed

- Read in full: `specs.md` (REQ-001..REQ-081), `plan.md` (§1–6, D-01..D-07), `tasks.md` (Phases 0–7), `deferred-items.md`, `prototype/screens.json`.
- Layer AGENTS.md consulted per plan-review skill (affected layers): root `AGENTS.md`, `backend/services/AGENTS.md`, `backend/types/AGENTS.md`, `frontend/graphql/sharedDocuments/AGENTS.md`, `test/workflows/AGENTS.md`.
- Repo spot-checks (existence/anchors only, no deep source reads): all REQ-002 reuse targets, all new-file target directories, all referenced scripts, nav admin block, `SessionStatus` enum, `NotificationType` enum, `listMyStudentSessions` pagination convention, `SessionRequestNotificationService` → `NotificationEngine` wiring.

### Verified anchors (positive findings — no action needed)

| Plan/spec claim | Verified |
|---|---|
| `refundHeldLaneToProvenance` @ `backend/services/classes/session-lifecycle.transitions.ts:207` | ✅ exact line match |
| `AuditService.createAuditLog(input, tx)` @ `backend/services/admin/audit.service.ts:82` | ✅ exact line match |
| `AuditLogWriteContract` @ `backend/types/contracts/admin-audit.contract.types.ts` | ✅ (line 22) |
| `assertActorAdmin` @ `backend/services/admin/admin-gate.helpers.ts` | ✅ (line 114; `assertActorAdminActive` sibling exists) |
| `AuditActionType` enum @ `backend/enum/audit/audit-action-type.enum.ts` | ✅ |
| `resolveSessionDispute` + `authScopes` split @ `backend/graphql/mutation/classes/session-lifecycle.mutation.ts` | ✅ |
| `SessionPothosObject` (:134) / `SessionPagePothosObject` (:206) @ `session.pothos.ts` | ✅ |
| Barrels `backend/graphql/{query,mutation}/classes/index.ts`; `backend/services/classes/index.ts`; `backend/types/classes/index.ts` | ✅ |
| `SessionListFilterPothosInput` ref with SDL name `SessionListFilterInput` @ `session-filter-input.pothos.ts` | ✅ — confirms task 4.1's `AdminSessionListFilterPothosInput` ref + REQ-060 SDL name `AdminSessionListFilterInput` is the established two-name pattern, not a conflict |
| `withTransaction` precedent @ `backend/lib/db/with-transaction.ts`; `SessionLifecycleService` namespace @ `session-lifecycle.service.ts:117` | ✅ |
| `scalar.pothos.ts` (DateTime), `session.repository.ts`, `schema/classes/session.ts`, `navItems.ts` + `navItems.test.ts`, `test/workflows/AGENTS.md` + helpers barrel, `app/(dashboard)/disputes/page.tsx` + `AdminDisputesChrome.tsx`, `docs/{sessions,specs,graphql,workflows,admin}/…`, `deferred-items-template.md` | ✅ all exist |
| Scripts: `tsgo`, `biome:check`, `oxlint`, `scripts/lint-service.ts`, `generate:gqlSchema`, `codegen`, `test/scripts/run-test.ts`, `scripts/health/sub-loop.ts`, `check:duplicates` (jscpd — QL duplicates gate), `test:ui:components` (Happy DOM), `test:ui:e2e` (Playwright) | ✅ all present in `package.json` |
| `SessionRequestNotificationService` delegates to `NotificationEngine.emitForUser` (publish-after-commit) | ✅ — plan §4 reuse complies with the single-writer notification rule in `backend/services/AGENTS.md` |
| D-03 eligibility vs REQ-020 (reschedule `scheduled|started`), REQ-024 (reassign `scheduled`), REQ-026/027 (join `started`) | ✅ consistent |
| Nav admin block (`/audit` line 139 … `/disputes` line 146) vs D-07 anchor "145-146" | ✅ insertion point feasible |
| REQ-074/075 vs `test/workflows/AGENTS.md` (REAL-DB, committed fixtures + tracked cleanup, no `runInRollback`, try/catch helper, no `.rejects.toThrow()`, spied dispatch) | ✅ exact alignment |
| Type naming `{Input,ReturnType}` + `backend/types/` only (REQ-004) vs `backend/types/AGENTS.md` | ✅ compliant |
| Phase 6 test-first ordering | ✅ explained in tasks.md Phase 6 header ("authored BEFORE the service (test-first); may initially fail/pending until Phase 3 lands") — see F-9 note |

---

## 2. REQ coverage completeness — PASS

Every REQ id in specs maps to ≥1 owning task; no task references a nonexistent REQ.

| REQ block | Owning tasks |
|---|---|
| REQ-001 | 0.1 · REQ-002 → 0.3 · REQ-004 → 1.1 |
| REQ-010..012 | 2.1, 3.1 (range REQ-010..029), 4.2 |
| REQ-020..029 | 2.1 (020..025), 3.1 (010..029 incl. 026/027/028/029), 4.3 (020..027), 5.2.2 (badge matrix) |
| REQ-030..033 | 4.2/4.3 (030), 2.1.SEC (031 BOPLA audit), SEC subtasks (032), deferred ledger (033 = ledger D-03) |
| REQ-040..044 | 2.1 (040/041/042), 3.1 (040..044) |
| REQ-050..051 | 1.2 |
| REQ-060..064 | 4.2/4.3/4.4 (060), 4.1 (061), 4.x + ledger watch (062), 5.1 (063), 5.2/5.3 (064) |
| REQ-070..075 | 2.1.TE / 3.1.TE (Tier-1..3), 6.4 (Tier-4), Phase 6 (074), journey AGENTS.md rule 6 (075) |
| REQ-080 / REQ-081 | 7.1 / 5.4 |

Never-cited-by-id REQs (REQ-003, REQ-031, REQ-032, REQ-062, REQ-070..075) are covered in substance via task ranges and QL/TE/SEC/SR/IV subtasks — recorded as F-8 nit, no coverage gap.

---

## 3. Findings

| ID | Severity | Description & evidence | Resolution (owner task) |
|---|---|---|---|
| F-1 | **HIGH** | **REQ-063 / task 5.1 document path violates layer AGENTS.md.** Plan §5, REQ-063 and task 5.1 place the documents file at the TOP LEVEL `frontend/graphql/sharedDocuments/adminSessions.documents.ts`. `frontend/graphql/sharedDocuments/AGENTS.md` (Layout + "Consumer import conventions") mandates domain sub-directories with `index.ts` barrels and explicitly marks flat top-level paths "Never (stale flat path) ← broken after refactor"; every existing `.documents.ts` lives in a sub-directory (`admin/`, `scheduling/`, …). | At **5.1**: create `frontend/graphql/sharedDocuments/admin/admin-session-governance.documents.ts` (admin/ domain matches `admin-users.documents.ts`, `audit-trail.documents.ts` precedent) or `scheduling/`; add the barrel export; run `generate:gqlSchema && bun codegen`. Record the deviation from REQ-063's literal path in `outcome/5.1-*` (AGENTS.md wins over spec wording). **Must be resolved before Phase 5, task 5.1; does not block Phases 1–4.** |
| F-2 | **MEDIUM** | **Operation/function count drift.** REQ-030 says "five admin mutations"; REQ-073, the actor table and journey W-4 say "ALL seven new operations" / "5 roles × 7 operations"; D-02 says "five new service functions" while plan §4 lists **six** functions and tasks 3.1 says "all six functions". Canon per REQ-060 SDL + plan §3: **4 mutations + 2 queries = 6 operations; 6 service functions**. (REQ-040's "four mutating operations" is consistent with 4.) | Implementers treat the SDL enumeration as canon: W-4 / 6.4 = 5 roles × **6** operations; service = **6** functions. Record the reconciliation in `outcome/4.3-*` and `outcome/6.4-*`. Non-blocking (enumerations are unambiguous; only count words are stale). |
| F-3 | **MEDIUM** | **Cancel eligibility wording gap: `disputed`.** REQ-022 gates cancel on "a pre-terminal state", but `SessionStatus` = {scheduled, started, completed, cancelled, **disputed**} (`backend/enum/scheduling/session-status.enum.ts`) — `disputed` IS pre-terminal, while D-03 canonizes cancel = `scheduled|started` only and specs §1 non-goals + REQ-025 exclude disputed rows from this ticket's writes (arbitration boundary, DEV3-022). Task 2.1's guard name `guardCancelPreTerminal` encodes the imprecise term. | At **2.1**: implement `guardCancel` eligibility clause as `WHERE status IN ('scheduled','started')` per D-03; `disputed` → localized conflict (`SESSION_INVALID_TRANSITION`), zero audit rows (JR-C-1 denial rule, `backend/services/AGENTS.md`). Either rename the guard or define in the 2.1 outcome that "PreTerminal ≡ {scheduled, started}" for this ticket. Carry the explicit matrix (incl. refused `disputed`) into the REQ-080 doc (7.1). Non-blocking; pin before 2.1 implementation. |
| F-4 | **MEDIUM** | **Notification wave-id naming divergence.** Specs REQ-020/022/024 name waves `session.rescheduled` / `session.cancelled` / `session.teacherReassigned`; plan §4 names them `sessionGovernance.rescheduled` / `sessionGovernance.cancelled` / `sessionGovernance.teacherReassigned`. Neither spelling is a literal value in `NotificationType` (`backend/enum/notifications/notification-type.enum.ts` uses e.g. `session_cancellation`), so both are conceptual labels — but an implementer hunting for `session.rescheduled` at 3.2 would stall. | At **3.2**: pin the three wave ids to plan §4's `sessionGovernance.*` (or to whatever `NotificationType`/copy-key vocabulary the `SessionRequestNotificationService` emit-input convention requires), register copy keys en+ar, and record the chosen ids in `outcome/3.2-*`. Journeys 6.1–6.3 assert against the pinned ids. Non-blocking with pin-before-3.2 note. |
| F-5 | **LOW** | **Nav label i18n registration not explicitly tasked.** REQ-081's `labelKey: "sessionGovernance"` requires a compile-time i18n label (NavLabelKey union + labels map + en/ar parity), but task 1.2 scopes only the `errors` namespace and 5.4 mentions only `navItems.ts` + `navItems.test.ts`. | Fold the `sessionGovernance` label key (en + ar) into **5.4** (or 1.2). Self-correcting: `navItems.test.ts` TE and `tsgo` fail loudly if the key is missing. |
| F-6 | **LOW** | **Decision-ID namespace collision.** Plan decisions D-01..D-07 vs deferred-ledger D-01..D-05 reuse the same ids with different meanings (plan D-03 = eligibility matrix; ledger D-03 = rate-limit deferral, as in REQ-033; plan D-04 = view structure; ledger D-04 = realtime dashboards). `deferred-items.md` also cites "plan §0/D-07" — plan.md has no §0. | At **0.2** ledger re-init: prefix ledger entries `DEF-01..DEF-05` (or annotate "ledger D-≠ plan D"). Read REQ-033's "deferred item D-03" as ledger-D-03. Contexts are otherwise separable; non-blocking. |
| F-7 | **LOW** | **Dangling cross-references.** Specs §3 says the eligibility matrix lives in "plan §3" (it is plan §1, D-03); specs §1 non-goals cites "per D2" (no such plan decision id — it is the ticket-level dispute-exclusion ruling evidenced by REQ-025). | Read "plan §3" as plan §1/D-03; treat "D2" as the REQ-025 arbitration-boundary ruling. No file edits required; note carried in this outcome. |
| F-8 | **LOW** | **Task-definition nits.** (a) 2.1.TE path `backend/db/test/repo/session-repository.admin.test.ts` skips the domain sub-directory used by existing repo tests (`backend/db/test/repo/classes/session.repository.test.ts`). (b) REQ-070..075 (and REQ-003) ids are never cited verbatim in tasks.md though covered via Tier labels and subtasks. (c) Tasks 1.3 and 2.2 do not enumerate the full QL/TE/SEC/SR/IV subtask chain. | (a) write the test at `backend/db/test/repo/classes/session-repository.admin.test.ts`; (b)/(c) no action — coverage verified in §2; sub-loop and completion definition still apply to 1.3/2.2 per the header protocol. |
| F-9 | **LOW** | **Prototype not referenced by plan/tasks.** `prototype/` holds 11 screens per `screens.json` (directory default/empty/error, detail drawer, reschedule/cancel/reassign dialogs, join observation, mobile directory + reschedule/cancel bottom sheets; worklog's "12 screens" counts screens.json). Tasks 5.2/5.3 never mention consuming the prototypes, and plan §5's mobile pattern ("full-screen Dialog ≤ md; temporary Drawer on mobile") differs in naming from the prototype's "Bottom Sheet" screens. | At **5.2/5.3**: open `prototype/screens.json` + PNGs as the visual canon; pin the mobile dialog pattern (bottom-sheet vs Dialog/Drawer) at 5.2.3 with the prototype as authority and record the choice in the 5.2 outcome. Non-blocking (plan §5 is internally consistent). |
| F-10 | **LOW** | **`frontend/views/AGENTS.md` does not exist**, though root `AGENTS.md`'s layer table and task 7.2 reference it. Task 7.2's "update" is effectively a CREATE (1–2 line rule referencing the 7.1 doc). | At **7.2**: create `frontend/views/AGENTS.md` with the rule (or hang it off the existing `frontend/AGENTS.md`) and note the creation in `outcome/7.2-*`. Non-blocking. |

Non-findings explicitly checked and cleared: plan D-03 vs REQ-020/024/026/027 (consistent); D-04 flat component tree matches the `disputes/` scaffold precedent; D-05 schema-column claims match worklog Phase-0 verification; D-06 no-lane no-op consistent with REQ-022; D-07 nav anchor verified; REQ-011 honest total ↔ repo `listForAdmin {rows,total}`; REQ-012 null-not-error ↔ nullable SDL `adminSession: Session`; REQ-021 validation envelope owned by 1.1 zod schemas + 2.1/3.1 (in cited REQ ranges); REQ-023 idempotency via existing claim plumbing + deferred watch item + 4.3 Tier-3; execution protocol (outcome files, checkboxes, `run-test.ts`, `sub-loop.ts --lifecycle duplicates`, zero-outside-changes rule) complete; 4-tier test framework coherent; journey tests REAL-DB per layer AGENTS.md with test-first ordering acknowledged in the Phase 6 header.

---

## 4. Gate decision & conditions

**PASS-WITH-NOTES — Phase 1 (Types & i18n Substrate) may begin now.**

Binding conditions (resolutions recorded above; no plan-file edit required):

1. **F-1** must be applied at task **5.1** (documents into `sharedDocuments/admin/` + barrel; AGENTS.md wins over REQ-063's literal path). Hard gate before Phase 5.
2. **F-3** must be pinned at task **2.1** (cancel eligible set = `scheduled|started` exactly; `disputed` refused, zero audit rows).
3. **F-4** must be pinned at task **3.2** (canonical wave ids recorded in the 3.2 outcome before journeys 6.1–6.3 assert them).
4. **F-2** must be reflected in outcomes of **4.3** and **6.4** (6 operations, 6 service functions are canon).

No finding is CRITICAL; none is unresolvable; therefore the gate is **not** FAILED. The minor nits (F-5..F-10) are recorded without blocking per the review-gate rule that documentation-level items not affecting implementation correctness may be logged with judgment — each nonetheless carries a named owning task above.

---

## 5. Evidence index (spot-check log)

- Enums: `backend/enum/scheduling/session-status.enum.ts:7-13`; `backend/enum/notifications/notification-type.enum.ts:6-11`; `backend/enum/audit/audit-action-type.enum.ts` (exists).
- Services: `backend/services/classes/` (11 files incl. `session-lifecycle.{service,transitions,guards,governance,booking,confirmation}.ts`, `session-request-notification.service.ts`); `backend/services/admin/` (incl. `admin-gate.helpers.ts`, `audit.service.ts`); `NotificationEngine.emitForUser` delegation in `session-request-notification.service.ts:211/221`.
- GraphQL: `backend/graphql/pothos/classes/{session,session-filter-input,create-session-input}.pothos.ts`; `pothos/shared/scalar.pothos.ts`; `query/classes/index.ts`; `mutation/classes/index.ts`; `session-lifecycle.mutation.ts` (authScopes ×5).
- Types/DB: `backend/types/classes/index.ts`; `backend/types/contracts/admin-audit.contract.types.ts`; `backend/db/repo/classes/session.repository.ts`; `backend/db/schema/classes/session.ts`; `backend/lib/db/with-transaction.ts`.
- Frontend: `frontend/graphql/sharedDocuments/{AGENTS.md,index.ts,admin/,scheduling/,…}`; `frontend/views/admin/disputes/*`; `frontend/views/dashboard/nav/{navItems.ts,navItems.test.ts}` (admin block lines 133–147); `app/(dashboard)/disputes/page.tsx`; `app/(dashboard)/admin/*` pages precedent.
- Tests/tooling: `test/workflows/AGENTS.md`; `test/workflows/helpers/*` (TrackedFixtures, actor-context, session-cast, admin-governance-cast); `test/scripts/run-test.ts`; `scripts/health/sub-loop.ts`; `scripts/lint-service.ts`; `package.json` scripts `tsgo/biome:check/oxlint/generate:gqlSchema/codegen/test:ui:components/test:ui:e2e/check:duplicates`.
- Plan artifacts: `prototype/screens.json` (11 screen entries); `.agents/spec-process-guide/templates/deferred-items-template.md`; `docs/admin/` (5 sibling docs); `docs/{sessions/session-lifecycle.md, specs/state-machine-invariants.md, graphql/error-handling-contract.md, workflows/05-admin-governance-override.md}`.
