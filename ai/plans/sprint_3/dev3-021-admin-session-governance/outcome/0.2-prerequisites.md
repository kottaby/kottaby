# Outcome 0.2 — Prerequisites Verify-then-Claim Sweep (task 0.3)

> **Task:** Phase 0 / 0.3 (file name `0.2-prerequisites.md` per tasks.md line 14).
> **Scope:** RESEARCH ONLY — every reuse target in REQ-002 (specs.md) + plan D-05/D-07 claims verified against the working tree at `feat/dev3-021-admin-session-governance`. Zero source files modified.
> **Result: 20/20 VERIFIED — no ❌ MISSING artifacts, no affected-task deferrals.** Three anchor drifts recorded (rows 14, 15 + document-naming note row 19) — none block any phase.

---

## 1. Verification table

| # | Artifact | Claimed location | Actual path:line | Status |
|---|---|---|---|---|
| 1 | `refundHeldLaneToProvenance` | `backend/services/classes/session-lifecycle.transitions.ts:207` | `backend/services/classes/session-lifecycle.transitions.ts:207` | ✅ exact |
| 2a | Guarded-transition pattern (transitions file) | `session-lifecycle.transitions.ts` | rejection helpers `rejectSessionNotFound`:61 · `rejectStateConflict`:75 · `rejectCertificationConflict`:93 · `rejectTransitionMiss`:139 (kind union incl. `"adminResolve"`) | ✅ |
| 2b | Guarded-transition pattern (guards file) | `session-lifecycle.guards.ts` | pure pre-DB guards: `MAX_IDEMPOTENCY_KEY_LENGTH`:45 · `isPositiveSafeInteger`:97 · `isPositiveSafeSessionId`:111 · `assertPositiveSafeSessionId`:123 · `normalizeRequiredReasonText`:137 · `normalizeOptionalReasonText`:155 · `normalizeAdminListBounds`:206 · `guardStatusFilter`:221 | ✅ |
| 2c | Guarded UPDATE precedent (single-statement state gate) | — | `SessionRepository.cancelSessionOnce` `backend/db/repo/classes/session.repository.ts:222` (`.update(session)` :231-234, `rows[0] ?? null` zero-row miss); `openDisputeOnce` :253; `resolveDisputeCancelOnce` :285 / `resolveDisputeCompleteOnce` :318 (guarded updates :293/:326) | ✅ |
| 3 | `assertActorAdmin` (BFLA gate) | `backend/services/admin/admin-gate.helpers.ts` | `backend/services/admin/admin-gate.helpers.ts:114` (delegates to `resolveAdminActorRow` :64; anonymous→`UnauthorizedError` :77, missing-row→`ForbiddenError` :87, non-admin→`ForbiddenError` :97; strict variant `assertActorAdminActive` :133) | ✅ |
| 4 | `AuditService.createAuditLog(input, tx)` | `backend/services/admin/audit.service.ts:82` | `backend/services/admin/audit.service.ts:82` (`export namespace AuditService` :65) | ✅ exact |
| 5 | `AuditLogWriteContract` | `backend/types/contracts/admin-audit.contract.types.ts` | `backend/types/contracts/admin-audit.contract.types.ts:22` (fields :24-35) | ✅ |
| 6 | `AuditActionType` enum values | `backend/enum/audit/audit-action-type.enum.ts` | `backend/enum/audit/audit-action-type.enum.ts:6-14` — Create/Update/Delete/Override/Adjust/Suspend/Reactivate (all 7 spec values present) | ✅ |
| 7 | `withTransaction` helper | "backend/db or backend/lib" | canonical: `backend/lib/db/with-transaction.ts:27`; imported by `backend/services/classes/session-lifecycle.service.ts:84` | ✅ (⚠️ see note N-1) |
| 8 | `authScopes: { $all: { authenticated: true, role: [UserRole.Admin] } }` in `resolveSessionDispute` | `backend/graphql/mutation/classes/session-lifecycle.mutation.ts` | `backend/graphql/mutation/classes/session-lifecycle.mutation.ts:270-275` (field registered :255; 401/403-split comment :265-269) | ✅ |
| 9 | `SessionPothosObject` / `SessionPagePothosObject` | `backend/graphql/pothos/classes/session.pothos.ts` | `session.pothos.ts:134` / `session.pothos.ts:206` | ✅ |
| 10 | Side-effect registration barrels | `backend/graphql/query/classes/index.ts` + `backend/graphql/mutation/classes/index.ts` | query barrel :12 (`import "./session-lifecycle.query";`) · mutation barrel :14 (`import "./session-lifecycle.mutation";`) | ✅ |
| 11 | Journey harness + helpers barrel | `test/workflows/AGENTS.md` + helpers barrel | `test/workflows/AGENTS.md` (exists) · `test/workflows/helpers/index.ts:22-29` (`export *` barrel); journey precedents `test/workflows/admin/audit-trail.journey.test.ts`, `test/workflows/sessions/session-lifecycle.journey.test.ts` | ✅ |
| 12 | Admin disputes page + view assembly | `app/(dashboard)/disputes/page.tsx` + `frontend/views/admin/disputes/AdminDisputesChrome.tsx` | page: `app/(dashboard)/disputes/page.tsx:37-39` (`withPageAuth({roles:[UserRole.Admin], redirectTo:"/disputes"})` :38) · chrome: `AdminDisputesChrome.tsx:23` | ✅ |
| 13 | `SessionListFilterPothosInput` | `backend/graphql/pothos/classes/session-filter-input.pothos.ts` | `backend/graphql/pothos/classes/session-filter-input.pothos.ts:28` | ✅ |
| 14 | Schema: `startedAt`/`endedAt`; NO `scheduledAt`/`meetingUrl` | plan D-05: `backend/db/schema/classes/session.ts:46-47` | `startedAt` **:66**, `endedAt` **:67**; `heldBalanceLane` :65, `confirmationDeadline` :70, `feeHeld` :64, `status` :60, `sessionType` :61, `createdAt` :76. Grep for `scheduledAt|meetingUrl|scheduled_at|meeting_url` over the file → **0 matches** (absence confirmed). Lines 46-47 are docblock text — claim drift only. | ✅ (anchor drift — actual 66/67) |
| 15 | Nav admin block (`/audit`, `/disputes` placement) | plan D-07: navItems.ts:145-146 | `frontend/views/dashboard/nav/navItems.ts` — `[UserRole.Admin]` block :132-148; `/audit` **:139**; `/admin/broadcasts` :140; `/admin/analytics` :142; `/disputes` **:146**; `/profile` :147 (block tail actually ends :147-149). `navItems.test.ts` exists for task 5.4. | ✅ (anchor drift — `/disputes` :146 matches; block ends :147-149, not 145-146) |
| 16 | `DateTime` scalar registration | `backend/graphql/pothos/shared/scalar.pothos.ts` | `backend/graphql/pothos/shared/scalar.pothos.ts:30` (`gqlSchemaBuilder.addScalarType("DateTime", DateTimeResolver, {})`, resolver import :24; fields reference by NAME `type: "DateTime"` per docblock :8/:29) | ✅ |
| 17 | Session repository | `backend/db/repo/classes/session.repository.ts` | `backend/db/repo/classes/session.repository.ts:86` — `export namespace SessionRepository` (NOT a class); methods are `export async function` members; read machinery delegated to sibling `session.repository.helpers.ts` (imported `* as sessionRepositoryImpl` :56, one-to-one wrappers); module-scope predicate-builder pattern :78-84 | ✅ |
| 18 | Mutation idempotency mechanism + claim helper | `session_request_idempotency` table (DB) | repo: `backend/db/repo/classes/session-request-idempotency.repository.ts:46` (`insertClaim` :63 · `updateClaimSessionId` :86 · `findByKey` :109); service usage: `backend/services/classes/session-lifecycle.booking.ts` — key guard :81-82, replay lookup :168, savepoint-bracketed claim insert :222-237 (duplicate → `replayBooking` :237), backfill :244 | ✅ (⚠️ see note N-2) |
| 19 | `frontend/graphql/sharedDocuments/` + exemplar | directory + 1 exemplar | exists with per-domain subfolders + `index.ts` barrel-of-barrels (`frontend/graphql/sharedDocuments/index.ts:1-8`: admin, auth, billing, notifications, parents, scheduling, students, teachers). Exemplar: `frontend/graphql/sharedDocuments/scheduling/session-lifecycle.documents.ts:147` (`cancelSessionMutationDocument`). Contract suite: `frontend/graphql/sharedDocuments/documents.contract.test.ts:135` (`DOCUMENT_CONTRACT_TABLE`) | ✅ (⚠️ see note N-3) |
| 20 | `app/(dashboard)/admin/` routes | confirm existing admin group | exists: `admin/dashboard`, `admin/users`, `admin/users/[id]`, `admin/plans`, `admin/broadcasts`, `admin/analytics` (page.test.ts siblings for plans/analytics). **No `admin/session-governance` page yet.** NOTE: `/audit` lives OUTSIDE the admin group at `app/(dashboard)/audit/page.tsx`; `/disputes` at `app/(dashboard)/disputes/page.tsx` — task 5.3 anchors into the EXISTING `admin/` group. | ✅ |

### Anchor-drift notes (informational, non-blocking)
- **Row 14:** plan D-05's "46-47" is the schema file's docblock; the real columns sit at **:66-:67**. REQ-004/D-05's substantive claim (columns exist; no scheduled/meeting columns) is CONFIRMED.
- **Row 15:** plan D-07's "admin block ends at 145-146" is stale by 2 lines: `/disputes` is :146, but the block continues with `/profile` :147 and closes :148-:149. Task 5.4 should treat **:139 (`/audit`) and :146 (`/disputes`)** as the insertion-envelope anchors.
- **Row 7 (N-1):** a SECOND, byte-similar `withTransaction` exists in `backend/services/notifications/notification-engine.helpers.ts:18` (notification-engine-local). The canonical import for this feature is **`@/backend/lib/db/with-transaction`** — do not import the notification-local twin.
- **Row 18 (N-2):** the existing claim row is booking-shaped (`insertClaim({ idempotencyKey, userId })`, `sessionId` backfilled later; unique key = `session_request_idempotency_key_unique`). REQ-023's admin-cancel idempotency can reuse these repo methods, but "what the claim points at" (no created session id; cancel mutates an existing row) needs an explicit shape decision in task 3.1 — recorded here so the service subagent decides deliberately instead of inventing silently.
- **Row 19 (N-3):** REQ-063 specifies a FLAT path (`sharedDocuments/adminSessions.documents.ts`) + PascalCase `{X}QueryDocument` names; the tree convention is domain SUBFOLDER + `index.ts` barrel + camelCase export names (`cancelSessionMutationDocument`, operation name PascalCase `CancelSession`), and `documents.contract.test.ts` registers every document in `DOCUMENT_CONTRACT_TABLE` (asserting `id` on object selections). Task 5.1 should reconcile with the orchestrator before creating the file; the contract-test row addition is part of that task's blast radius.

---

## 2. Carry-forward knowledge for future subtasks

### 2.1 Exact export names & signatures (verbatim from source)

- `refundHeldLaneToProvenance(resolved: SessionReturnType, context: "cancelSession" | "resolveSessionDispute" | "sweepExpiredSessions", tx: DBTransaction): Promise<void>` — `session-lifecycle.transitions.ts:207`. Null-lane row = silent no-op (`:212-214`); unreadable lane = fail-closed throw (rolls back caller's tx). **⚠️ The `context` param is a CLOSED string-literal union — a governance cancel cannot pass a new literal like `"adminCancelSession"` without widening the union in this file (the file that owns the primitive). Task 3.1 must either pass an existing literal or make a one-line union extension in `session-lifecycle.transitions.ts` (owner file — not a foreign layer); decide explicitly.**
- `assertActorAdmin(actorId: number, locale: string, outerTx?: DBTransaction): Promise<void>` — `admin-gate.helpers.ts:114`. Anonymous (`actorId === 0`) → `UnauthorizedError(tErrors.unauthorized)`; missing row / non-admin → `ForbiddenError(tErrors.forbidden)`; ZERO audit rows / ZERO writes on denial; locale via `getServerTranslations(locale).errorsTranslations` (one-arg precedent).
- Strict sibling: `assertActorAdminActive(actorId, locale, outerTx?)` — `admin-gate.helpers.ts:133` (adds deleted/blocked/suspended ladder).
- Session-domain equivalent used by `resolveSessionDispute`: `assertAdminGovernanceClean(actorUserId, t, tx?)` — `backend/services/classes/session-lifecycle.governance.ts:63` (role + governance from DB row; called at `session-lifecycle.service.ts:444` BEFORE `withTransaction`). REQ-030's "redundant service-side re-assert" precedent.
- `AuditService.createAuditLog(input: AuditLogWriteContract, tx: DBTransaction): Promise<void>` — `audit.service.ts:82`. Composition-only writer; `details` defensively truncated to `varchar(2000)` before insert; append-only (no update/upsert); insert shares caller's tx fate.
- `interface AuditLogWriteContract { readonly actorId: AuditLogSelectType["actorId"]; readonly actionType: AuditActionType; readonly entityType: string; readonly entityId: AuditLogSelectType["entityId"]; readonly details: string; }` — `admin-audit.contract.types.ts:22` (append-only; `id`/`createdAt` prohibited from input).
- `enum AuditActionType { Create="create", Update="update", Delete="delete", Override="override", Adjust="adjust", Suspend="suspend", Reactivate="reactivate" }` — `audit-action-type.enum.ts:6-14`. Governance actions map to `AuditActionType.Override` per REQ-020/022/026. Read-side coercion helper exists: `toAuditActionType(raw): AuditActionType | null` — `admin-gate.helpers.ts:25`.
- Contract-composer precedent: `buildAuditContract(actorId, actionType, entityId, details: Record<string, unknown>): AuditLogWriteContract` — `backend/services/admin/user-management.helpers.ts:334` (JSON.stringify + truncate; `entityType` constant "user" — governance will need `entityType: "session"`).
- `withTransaction<T>(outerTx: DBTransaction | undefined, fn: (tx: DBTransaction) => Promise<T>): Promise<T>` — `backend/lib/db/with-transaction.ts:27`. `outerTx` provided → SAVEPOINT on outer tx (the `runInRollback` test path); undefined → top-level `db.transaction` (production path). Production service call shape: `withTransaction(outerTx, async tx => { ... })` (`session-lifecycle.service.ts:313/368/446`).
- Guarded-UPDATE contract: repo methods return `rows[0] ?? null`; `null` ⇒ classify via `rejectTransitionMiss(kind, sessionId, actorUserId, tx, t)` — `session-lifecycle.transitions.ts:139` — which throws `SESSION_NOT_FOUND`-class or state-conflict denials. Service composition precedent (`resolveSessionDispute`, `session-lifecycle.service.ts:446-479`): governance re-check → `withTransaction` → optional pre-write probe → guarded UPDATE → null ⇒ `rejectTransitionMiss` → refund primitive on the SAME tx.
- Pagination precedent (REQ-010/011): `listMyStudentSessions(studentId, filter, page, pageSize, tx?): Promise<SessionPageReturnType>` — `session-lifecycle.service.ts:626`; bounds via `normalizePageBounds` (page ≥1, pageSize 1..50), filter via `guardStatusFilter`; honest total = count under the SAME filtered predicate (`session-lifecycle.service.ts:636-645`); read-surface guards `normalizeAdminListBounds` :206 / `guardStatusFilter` :221 in `session-lifecycle.guards.ts`.
- Idempotency primitives (REQ-023): `SessionRequestIdempotencyRepository.insertClaim(insert: SessionRequestIdempotencyInsertType, tx?)` :63 (duplicate key raises PG `23505`, NOT caught — bubbles to service cause-chain), `updateClaimSessionId(claimId, sessionId, tx?)` :86, `findByKey(key, tx?): Promise<SessionRequestIdempotencySelectType | null>` :109. Booking usage: length guard (`MAX_IDEMPOTENCY_KEY_LENGTH = 128`, `session-lifecycle.guards.ts:45`), savepoint-bracketed claim insert `session-lifecycle.booking.ts:222-237`, replay branch `replayBooking` :237.
- Audit-emission rule (governs all new mutations): `backend/services/AGENTS.md:8,10` — every admin mutation that commits appends exactly ONE `audit_logs` row INSIDE the same `withTransaction(outerTx, …)` via `AuditService.createAuditLog(contract, tx)`; denial paths write ZERO audit rows.

### 2.2 Barrel registration pattern (tasks 4.2/4.3)

`backend/graphql/{query,mutation}/classes/index.ts` are SIDE-EFFECT barrels: each line is `import "./<file>";` — the imported file registers root fields on `gqlSchemaBuilder` at import time and exports NOTHING. New files `admin-session-governance.query.ts` / `admin-session-governance.mutation.ts` get ONE side-effect line each; the top-level `backend/graphql/{query,mutation}/index.ts` already imports these domain barrels, and `gqlSchema.ts` imports the top-level barrels exactly once (barrel docblocks :5-11). Resolver field pattern (mutation file): `gqlSchemaBuilder.mutationField("<name>", t => t.field({ type, args, description, authScopes, resolve }))`; admin scope = the :270-275 `$all` conjunction; `ID` args arrive as strings and are `Number()`-parsed at the resolver boundary only (narrowing guard `if (!ctx.user) throw new UnauthorizedError(...)` precedes).

### 2.3 Pothos filter-input pattern (task 4.1)

`SessionListFilterPothosInput` = `gqlSchemaBuilder.inputType("SessionListFilterInput", { fields: t => ({ status: t.field({ type: SessionStatusPothosEnum, required: false }) }) })` — `session-filter-input.pothos.ts:28-33`. Discipline: backed by the canonical `@/backend/types` input (no local type), enum-typed members rejected at GraphQL validation, absent/null members drop out at the service guard (never error), registered at import time and consumed by the query module's transitive registration. `AdminSessionListFilterPothosInput` mirrors this job-by-job (REQ-061) with `SessionType`/`SessionStatus` enums + `DateTime`-typed `dateFrom`/`dateTo` (scalar by NAME, `scalar.pothos.ts:30`).

### 2.4 Idempotency decision (recorded, not implemented)

The existing mechanism is a claim-row table (`session_request_idempotency`: id, idempotency_key unique, user_id, nullable session_id, created_at) claimed BEFORE the write inside the same tx, with the duplicate-key error driving the replay branch. For `adminCancelSession` (REQ-023) the claim cannot backfill a created-session id — task 3.1 must choose the claim payload/shape (e.g. key scoped to actor+sessionId semantics via `insertClaim`'s `userId` field) and reuse `findByKey` for the replay branch. This is the ONLY reuse-target reuse with an open shape decision; everything else composes verbatim.

### 2.5 Frontend anchors (tasks 5.1-5.4)

- Route/page precedent: `app/(dashboard)/disputes/page.tsx:37-39` — `export default async function AdminDisputesPage() { await withPageAuth({ roles: [UserRole.Admin], redirectTo: "/disputes" }); return <AdminDisputesContainer />; }` (+ `generateMetadata`). Task 5.3 mirrors this under `app/(dashboard)/admin/session-governance/page.tsx` (admin group exists, row 20).
- View tree precedent: `frontend/views/admin/disputes/` — Container/Chrome/Body/Row/Pager/NoticeSnackbar/dialog units; `AdminDisputesChrome.tsx:23` props shape `({ title, countLine })`.
- Documents precedent: subfolder + camelCase export + TypedDocumentNode + contract-test row (note N-3). Apollo hooks per REQ-063; document barrels: `frontend/graphql/sharedDocuments/index.ts` (`export *` per domain folder).
- Nav precedent: `frontend/views/dashboard/nav/navItems.ts:132-148` — entries are `{ route, labelKey, Icon: <MUI icon> }`; recent precedent entries `/admin/broadcasts` :140 and `/admin/analytics` :142 (both with explanatory comments); `navItems.test.ts` must be extended (task 5.4).

---

## 3. MISSING ⇒ ❌-defer

**EMPTY — all 20 reuse targets verified.** No downstream task (1.1-7.3) is blocked by a missing artifact. The three non-blocking flags (N-1 canonical `withTransaction` import path, N-2 idempotency claim shape, N-3 documents path/naming convention) are decisions for tasks 3.1/4.x/5.1 respectively, recorded above so their subagents start from ground truth.
