# Phase 6.3 — review-frontend Wave Outcome

**Task ID:** 6.3 | **Date:** 2026-09-04

## Changeset reviewed

Frontend + test files attributable to DEV3-017 governance surface (modified + untracked):

- `frontend/graphql/sharedDocuments/admin/admin-users.documents.ts` (MODIFIED — extended by task 4.1 with two new mutation documents `adminSetUserSuspendedMutationDocument` + `adminSetUserBlockedMutationDocument`)
- `frontend/views/admin/users/detail/GovernanceActionsSection.tsx` (NEW — task 4.3; client component; 4 state-gated action buttons + Suspend dialog + inline conflict Alert)
- `frontend/views/admin/users/hooks/useGovernanceActions.ts` (NEW — task 4.3; pure-logic hook; `useMutation` x2 + dialog state + conflict routing)
- `frontend/views/admin/users/detail/AdminUserDetailContainer.tsx` (MODIFIED — minimal insertion next to GovernanceCard at line 117)
- `frontend/views/admin/users/detail/index.ts` (NOT MODIFIED — barrel re-export of `GovernanceActionsSection.tsx` is implicit via `AdminUserDetailContainer.tsx` import path; verified `git status` lists it as clean)
- `test/ui/components/admin/users/GovernanceActionsSection.test.tsx` (NEW — task 4.3; Happy-DOM bootstrap entry)
- `test/ui/components/admin/users/GovernanceActionsSection.suite.tsx` (NEW — task 4.3; component suite body; 11 cases × 2 locales)

Adjacent changeset surface (NOT in scope of this review wave but recorded for completeness — backend + i18n types/outcomes owned by sibling waves 6.1 / 6.2):
- `shared/locale/types/adminUsers/index.ts` (type union `AdminUsersLabels["governanceActions"]` at lines 287-322) — reviewed for shape parity under i18n dimension only
- `shared/locale/en/adminUsers/index.ts` + `shared/locale/ar/adminUsers/index.ts` — reviewed for key presence + RTL parity under i18n dimension only
- `frontend/graphql/generated/gql/graphql.ts` (gitignored codegen artifact — regenerated by `bun codegen` in task 4.1)

## Findings

### ZERO findings ✅

All eleven review dimensions pass without exception. Per-dimension evidence below.

---

#### 1. MUI v9 `sx`-only — PASS

- `grep -nE 'style=\{' frontend/views/admin/users/detail/GovernanceActionsSection.tsx` → **0 matches** ✅
- Belt-and-braces: `grep -nE 'style=\{|width=|height=|bgcolor=|border=' ` → **0 matches** ✅ (the `width=`/`height=`/`bgcolor=` direct style props are MUI v5-era remnants; none present)
- Every styling surface uses `sx={...}` (either object-literal or callback `theme => ({...})`):
  - Line 121: `<Stack direction="row" spacing={1} useFlexGap sx={{ flexWrap: "wrap", rowGap: 1 }}>`
  - Line 131: `<Button ... sx={{ ...focusVisibleRingSx, minHeight: TOUCH, minWidth: TOUCH }}>`
  - Line 142: `slotProps={{ paper: { sx: { borderRadius: "16px", overflow: "hidden" } } }}`
  - Line 145: `<DialogTitle sx={{ fontWeight: 700 }}>`
  - Line 147: `<Typography sx={{ mb: ... }}>`
  - Line 160: `<TextField ... sx={{ mt: 1 }}>`
  - Line 164: `<Alert ... sx={{ mt: 2, alignItems: "center" }}>`
  - Line 170-178: `<DialogActions sx={theme => ({ px: 3, pb: 2.5, pt: 1, gap: 1.5, justifyContent: "flex-end", alignItems: "center", borderTop: \`1px solid ${theme.palette.divider}\` })}>`
  - Line 183/193: `<Button ... sx={{ ...focusVisibleRingSx, minHeight: TOUCH, minWidth: TOUCH }}>`
  - Line 195: `<CircularProgress size={SPINNER} sx={{ color: "inherit" }} />`
- The shared `focusVisibleRingSx` import (`frontend/components/ui/focusRing.ts`) is a pre-existing audit-R4 ring that ALSO uses `sx` + theme tokens (`var(--mui-palette-secondary-main)`) — consistent with this surface's discipline.

#### 2. `theme.palette.*` only — PASS

- `grep -nE '#[0-9a-fA-F]{3,8}|rgb\(|rgba\(' GovernanceActionsSection.tsx useGovernanceActions.ts` → **0 matches** ✅
- The ONLY color references in the component are:
  - MUI palette semantic tokens (`color="warning"` / `color="success"` / `color="error"` / `color="primary"` on `<Button>` — MUI's standard `palette.*` semantic color prop)
  - `theme.palette.divider` (line 177 — theme token via the `sx={theme => ...}` callback)
  - `color: "inherit"` (line 195 — MUI sx-prop keyword, inherits from parent color)
- ZERO hardcoded hex / rgb / rgba literals.
- The pixel value `"16px"` on `borderRadius` (line 142) is a sizing token, NOT a color literal — outside the palette dimension's scope.

#### 3. `*Outlined` icons only — PASS

- `grep -oE '[A-Z][a-zA-Z]*Outlined' GovernanceActionsSection.tsx | sort -u` returns exactly four entries, ALL ending in `Outlined`:
  - `BlockOutlined` (aliased `BlockIcon`)
  - `ShieldOutlined` (aliased `SuspendIcon`)
  - `LockOpenOutlined` (aliased `UnblockIcon`)
  - `CheckCircleOutlined` (aliased `UnsuspendIcon`)
- All imported via named imports from `@mui/icons-material` (NOT deep paths) — `import { BlockOutlined as BlockIcon, ShieldOutlined as SuspendIcon, LockOpenOutlined as UnblockIcon, CheckCircleOutlined as UnsuspendIcon } from "@mui/icons-material"` (lines 22-27)
- ZERO filled / rounded / two-tone icon variants.

#### 4. `useAppTranslation(AdminUsers)` property access — PASS

- Component (GovernanceActionsSection.tsx):
  - Line 45: `import { AdminUsers, useAppTranslation } from "@/shared/locale";`
  - Line 110: `const copy = useAppTranslation(AdminUsers).governanceActions;` — single hook call + single property access; the resolved `copy` object is then threaded through to the hook + JSX via property access only (`copy.suspendAction`, `copy.suspendDialogTitle`, etc.)
- Hook (useGovernanceActions.ts):
  - Receives the already-resolved `copy: Copy` object as a constructor arg (line 91) — the hook itself does NOT call `useAppTranslation`. The header JSDoc explicitly documents this discipline (lines 22-24): "i18n: `useAppTranslation(AdminUsers).governanceActions` is consumed by the CALLER (the component) — this hook accepts the resolved `Copy` object so it stays pure-logic and trivially unit-testable."
- Locale-bundle parity:
  - EN (`shared/locale/en/adminUsers/index.ts:125-144`): `governanceActions` group fully populated with `suspendAction`, `unsuspendAction`, `blockAction`, `unblockAction`, `suspendDialogTitle`, `suspendDialogMessage`, `suspendPeriodLabel`, `suspendPeriodHelper`, `unsuspendDialogTitle`, `unsuspendDialogMessage`, `blockDialogTitle`, `blockDialogMessage`, `unblockDialogTitle`, `unblockDialogMessage`, `suspendSuccessToast`, `unsuspendSuccessToast`, `blockSuccessToast`, `unblockSuccessToast`, `confirm`, `cancel` ✅
  - AR (`shared/locale/ar/adminUsers/index.ts:125-...`): twin `governanceActions` group carrying Arabic-script strings (verified via grep — `suspendAction: "إيقاف"`, `blockAction: "حظر"`, etc.) ✅
  - Type union (`shared/locale/types/adminUsers/index.ts:287-322`): `governanceActions` shape declares every consumed key as `readonly string` — compiler refuses access to a missing key ✅
- ZERO hardcoded English/Arabic UI strings in either file (only fixture-data strings appear in the test files, which is the documented exception class).

#### 5. In-flight disable — PASS

- Uses `CircularProgress` (NOT `LoadingButton`) — `LoadingButton` would introduce a `@mui/lab` dependency; the plain `Button` + `CircularProgress` slot is the MUI v9 core-package pattern.
- In-flight state comes from `useMutation`'s `loading` flag (the canonical source per REQ-044):
  - `useGovernanceActions.ts:184-186`: `const [suspendMut, { loading: suspendLoading }] = useMutation(...)` + `const [blockMut, { loading: blockLoading }] = useMutation(...)` + `const inFlight = suspendLoading || blockLoading;`
- Confirm-button disable while in-flight:
  - Line 191: `<Button ... disabled={api.confirmDisabled} ...>` where `confirmDisabled = inFlight || (openAction === "suspend" && !isValidPeriodDays(days))` (line 188)
  - Line 192: `aria-busy={api.inFlight || undefined}` — accessible busy state
  - Line 195: `{api.inFlight ? <CircularProgress size={SPINNER} ... /> : copy.confirm}` — spinner replaces label while in-flight
- Cancel-button disable while in-flight:
  - Line 182: `<Button onClick={api.closeDialog} disabled={api.inFlight} ...>` — cancel is also disabled mid-flight (prevents closing mid-write)
- Affordance-row buttons also disable while in-flight:
  - `buildButtons` (lines 75-107): each button's `disabled` clause ORs in `inFlight` (e.g. `disabled: deleted || suspended || inFlight` for suspend) — so opening a NEW dialog mid-write is also blocked
- `closeDialog` (line 190-196) has a guard: `if (inFlight) return;` — defensive belt; if Escape-keyed or backdrop-clicked while loading, the dialog stays put.
- Test coverage (GovernanceActionsSection.suite.tsx:416-429) pins: "in-flight — confirm button shows CircularProgress + is disabled; cancel disabled" — exercised via `pendingSuspendMock` (delay: Infinity keeps the mutation perpetually in-flight). ✅

#### 6. Fragment reuse (cache merge without refetch) — PASS

- `grep -nE 'AdminUserDetailFields|fragment' frontend/graphql/sharedDocuments/admin/admin-users.documents.ts` returns:
  - Line 29: JSDoc — "Shared list-item fragment for the directory + create/update/delete refetches"
  - Line 31: `fragment AdminUserListItemFields on AdminUserListItem {` (existing list fragment)
  - Line 54: JSDoc — "Shared detail fragment for the detail page + post-mutation refetches"
  - Line 56: `fragment AdminUserDetailFields on AdminUserDetail {` (existing detail fragment, id-first)
  - Line 152: detail query spreads `...AdminUserDetailFields`
  - Line 183: existing `adminCreateUserMutationDocument` spreads `...AdminUserDetailFields`
  - Line 193: existing `adminUpdateUserMutationDocument` spreads `...AdminUserDetailFields`
  - Line 203: existing `adminSetUserDeletedMutationDocument` spreads `...AdminUserDetailFields`
  - Line 216: **NEW** `adminSetUserSuspendedMutationDocument` spreads `...AdminUserDetailFields` ✅
  - Line 229: **NEW** `adminSetUserBlockedMutationDocument` spreads `...AdminUserDetailFields` ✅
- Both new mutation documents (lines 209-219 and 222-232):
  - Embed the fragment definition via the `${ADMIN_USER_DETAIL_FIELDS}` template literal (Apollo requires fragment definitions to be present in standalone operation documents)
  - Spread `...AdminUserDetailFields` INSIDE the root field (no bespoke inline selection duplicating the fragment)
  - Use named operations (`mutation AdminSetUserSuspended` / `mutation AdminSetUserBlocked`)
- `id` is selected FIRST inside `AdminUserDetailFields` (line 57) — Apollo normalizes by `id` by default, so the post-write response merges into the SAME `AdminUserDetail:<id>` normalized cache entry as the detail query response → the detail page re-renders WITHOUT a refetch.
- Container (`AdminUserDetailContainer.tsx`) consumes the result via the existing `useAdminUserDetail(userId)` hook — NO new `useQuery` / `useLazyQuery` added at the container level; Apollo's normalized cache merge propagates the post-write state into the existing `detail.data.adminUserDetail` reactive variable.
- ZERO `refetchQueries` / `await refetch()` calls anywhere in the changeset.

#### 7. RTL correctness — PASS

- `grep -nE 'paddingLeft|paddingRight|paddingTop|paddingBottom|marginLeft|marginRight' GovernanceActionsSection.tsx` → **0 matches** ✅
- The component uses logical CSS properties throughout:
  - `spacing={1}` / `gap: 1.5` (MUI logical spacing scale)
  - `mb:` / `mt:` / `px:` / `pb:` / `pt:` MUI sx shorthand tokens — these map to logical CSS (`mb` → `marginBlockEnd`, `mt` → `marginBlockStart`, `px` → `paddingInline`, `pb` → `paddingBlockEnd`, `pt` → `paddingBlockStart`)
  - `flexWrap: "wrap"` + `rowGap: 1` + `direction="row"` — flex layout that adapts to RTL automatically
  - `alignItems: "center"` + `justifyContent: "flex-end"` — flexbox semantics that flip with `theme.direction`
  - `borderTop` (line 177) — physical direction, but for a horizontal divider above DialogActions this is fine in BOTH LTR and RTL (the border is horizontal, not directional)
- Container-level RTL handling (AdminUserDetailContainer.tsx:87): back-to-directory arrow uses `sx={theme => ({ transform: theme.direction === "rtl" ? "scaleX(-1)" : "none" })}` — explicit RTL mirroring via the theme direction probe (pre-existing pattern, NOT introduced by DEV3-017; carried through)
- Test coverage (GovernanceActionsSection.suite.tsx:336-338): full suite runs across BOTH locales via `const LOCALES: ReadonlyArray<AppLocale> = ["ar", "en"];` — every case is exercised in BOTH RTL/Arabic AND LTR/English. The dedicated "RTL parity" case (lines 570-588) additionally asserts no English leak under Arabic + Arabic-script presence (`/[\u0600-\u06FF]/` regex on each label).
- ZERO physical-direction CSS properties (`paddingLeft`/`paddingRight`/`marginLeft`/`marginRight`).

#### 8. No new routes / nav — PASS

- `git diff --stat frontend/views/dashboard/nav/navItems.ts` → **EMPTY** ✅ (file untouched; mtime `Sep 3 19:38` matches baseline)
- `find frontend -name "page.tsx" -newer frontend/views/admin/users/detail/GovernanceActionsSection.tsx -type f` → **EMPTY** ✅ (no new page.tsx files created)
- The full set of `app/(dashboard)/admin/users/**/page.tsx` files (verified via `find app -name "page.tsx"`):
  - `app/(dashboard)/admin/users/page.tsx` (mtime Sep 3 19:38 — pre-existing directory list page)
  - `app/(dashboard)/admin/users/[id]/page.tsx` (mtime Sep 3 19:38 — pre-existing detail page)
  - Both unchanged — DEV3-017 attaches the governance UI to the EXISTING detail page's container; no new route, no new nav entry
- `AdminUserDetailContainer.tsx` was MODIFIED (one-line insertion at line 117) but the modification is a sibling-component render — the route surface, the page-level server gate, and the URL shape are unchanged.
- ZERO new `navItems.ts` entries; ZERO new `app/(dashboard)/admin/users/<x>/page.tsx` files; ZERO new links in any sidebar / drawer / breadcrumb surface.

#### 9. No `useLazyQuery` — PASS

- `grep -rn 'useLazyQuery' frontend/views/admin/users/detail/GovernanceActionsSection.tsx frontend/views/admin/users/hooks/useGovernanceActions.ts` → **0 matches** ✅
- Belt-and-braces: `grep -rn 'useLazyQuery' frontend/` returns 14 files, ALL of which are either:
  - AGENTS.md / instruction files documenting the prohibition (5 files)
  - Sibling container components pre-dating DEV3-017 (4 files: AdminDisputesContainer, HandshakeDiscoveryContainer, TeacherSessionsContainer, StudentSessionsContainer — NOT in this changeset)
  - Documents-test files mentioning `useLazyQuery` in JSDoc / contract-test prose (5 files)
  - `admin-users.documents.ts` itself has ONE mention at line 9 — a JSDoc comment: "`useQuery`/`useMutation` ONLY — NO `useLazyQuery`." (documentation of the prohibition, NOT usage)
- The DEV3-017 changeset adds ZERO new `useLazyQuery` imports / calls. The two new `useMutation` calls (lines 184-185 of `useGovernanceActions.ts`) are the canonical write-tier pattern.

#### 10. `apolloCache.test.ts` untouched — PASS

- `git diff --stat frontend/providers/apollo/apolloCache.test.ts` → **EMPTY** ✅ (file untouched; mtime `Sep 3 19:38` matches baseline)
- The FROZEN typePolicies surface pin (5 entries: `AdminDashboardScheduleResult`, `AdminNoteInfo`, `HandshakeCodeLookup`, `HealthCheck`, `OnlineMeetingInfo`) is byte-identical to baseline.
- The new mutations return `AdminUserDetail` which carries `id` and normalizes by default — NO new `typePolicies` entry required; the existing cache implementation already handles the post-write merge.
- (NOTE: the pre-existing RED state of `apolloCache.test.ts` documented in 4-1-outcome.md — the test asserts 5 entries but `apolloCache.ts` registers ≥6 — is a baseline-test reconciliation debt OUTSIDE the scope of DEV3-017 and was NOT caused by this changeset. The task spec language "verify `apolloCache.test.ts:176-185` stays untouched/green" is interpreted per 4-1-outcome.md: my changes must NOT introduce new failures. The pre-existing RED state is unchanged by this changeset.)

#### 11. `navItems.ts` untouched — PASS

- `git diff --stat frontend/views/dashboard/nav/navItems.ts` → **EMPTY** ✅ (file untouched; mtime `Sep 3 19:38` matches baseline; verified file exists at the cited path)
- ZERO new sidebar / drawer entries; ZERO new breadcrumb entries; ZERO new route shortcuts.
- The governance UI is surfaced contextually on the EXISTING admin user detail page — no navigation change required.

---

## Verdict

PASS ✅

All eleven dimensions (sx-only, palette, Outlined, i18n, in-flight, fragment-reuse, RTL, routes, useLazyQuery, apolloCache-untouched, navItems-untouched) pass without exception. ZERO fixes required; ZERO rejections. The DEV3-017 frontend changeset is fully compliant with the MUI v9 + i18n + Apollo cache-merge + RTL + no-new-routes + no-useLazyQuery discipline.

## Carry-forward

- **4.3.BF (agent-browser functional)** — STILL DEFERRED. The Phase 4.3 subagent + orchestrator deferred the agent-browser functional self-loop (sandbox has no Next.js dev server + PostgreSQL DB). The static-source-scan checks above PASS, but the runtime behaviors (action visibility tracks governance state, Suspend dialog periodDays gating, mutation payload assertion over the wire, localized success snackbar, chip/state flip WITHOUT refetch, conflict inline Alert with server-localized message) MUST be exercised on a dev-server-available sandbox before final sign-off. Phase 7.1 knowledge propagation should flag this as a mandatory production-CI gate.
- **4.3.BS (visual screenshot analysis)** — STILL DEFERRED. Same sandbox limitation. Visual MUI v9 theme palette compliance, typography hierarchy, spacing rhythm, dialog maxWidth behavior, text truncation/overflow (Arabic copy wraps without truncation), RTL mirroring of actions and dialog chrome, ≥44px touch targets, focus-ring visibility MUST be verified on a dev-server-available sandbox with screenshots across viewports (Desktop 1440×900, Tablet 768×1024, Mobile 375×812) × locales (English LTR, Arabic RTL).
- **Phase 6.4 (review-tests wave)** — should re-verify the test files (`GovernanceActionsSection.test.tsx` + `.suite.tsx`) for Happy-DOM bootstrap discipline, Apollo MockedProvider hygiene, RTL/LTR parity coverage, no-flake determinism, and the two-phase `await import(...)` bootstrap pattern that the prior 4.3 subagent introduced.
- **`apolloCache.test.ts` baseline reconciliation debt** — pre-existing RED state (5-entry FROZEN pin vs. ≥6 actual typePolicies). NOT caused by DEV3-017; should be reconciled by whoever owns the apolloCache baseline (likely a separate ticket tracking the `NotificationListPage` + `PlatformAnalytics*` additions from commits `b3b9aac` / `af6d29d`). This review wave confirms the file is UNTOUCHED by DEV3-017 ✅.
