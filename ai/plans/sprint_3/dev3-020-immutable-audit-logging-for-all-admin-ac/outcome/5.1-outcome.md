# Task 5.1 Outcome — Journey Green + Full Differential Suite Run (DEV3-020)

**Plan directory:** `ai/plans/sprint_3/dev3-020-immutable-audit-logging-for-all-admin-ac`
**Task:** 5.1 Journey green + full differential suite run (+ REQ-043 chaos cross-check + coverage-gate statement)
**Branch:** `feat/dev3-020-immutable-audit-logging-for-all-admin-ac` (checked/restored before EVERY command batch — the recurring sandbox branch-flip artifact struck between every batch this pass; no source file was harmed by it)
**Method:** every suite driven through the sanctioned runner `bun run test/scripts/run-test.ts <path>`; every result recorded verbatim; environment blockers diagnosed before being worked around.

---

## 1. Suite-by-suite results table

| # | Suite (path) | Files | Result | expect() | Verdict |
|---|---|---|---|---|---|
| 1 | `test/workflows/admin/audit-trail.journey.test.ts` | 1 | **7 pass / 0 fail** | 126 | **GREEN — exact 2.2-signature match** (see §2) |
| 2 | `backend/db/test/logic/audit` | 2 (`audit-trail.repository.test.ts` + `audit-immutability.test.ts`) | **32 pass / 0 fail** | 368 | GREEN |
| 3 | `backend/services/admin` | 3 (`audit-trail.service.test.ts` 35 + `user-management.service.test.ts` 61 + `user-management.chaos.test.ts` 8) | **104 pass / 0 fail** | 472 | GREEN (new service + DEV3-016 regression lock byte-green) |
| 4 | `backend/graphql/test` | 14 | **236 pass / 4 fail** | 1601 | GREEN for every DEV3-020-owned file; the 4 fails are **pre-existing main debt, NOT this plan** (see §3) |
| 5 | `shared/locale` | 6 | **140 pass / 0 fail** | 1496 | GREEN (parity suites incl. `adminUsers-namespace.parity.test.ts`) |
| 6a | `frontend/graphql/sharedDocuments/admin/audit-trail.documents.test.ts` | 1 | **8 pass / 0 fail** | 32 | GREEN |
| 6b | `frontend/providers/apollo/apolloCache.test.ts` | 1 | **11 pass / 0 fail** | 24 | GREEN (frozen-policy pin now includes the audit-trail entry — 4.2's additive re-pin holds) |
| 7 | `frontend/views/dashboard/nav/navItems.test.ts` | 1 | **20 pass / 0 fail** | 116 | GREEN (zero-change nav retarget pins) |
| 8 | `test/ui/components/admin` | 1 (`AuditTrailView.test.tsx`) | **15 pass / 0 fail** | 95 | GREEN |
| 9 | `backend/graphql/test/handshake-code-surface.test.ts` (Task 5.2 pin, run here for completeness) | 1 | **22 pass / 0 fail** | 44 | GREEN |

Totals: **553 pass / 4 fail / 4372 expect()** — the only 4 failures are the pre-existing `plan-catalog.roles.test.ts` anonymous-gate pins, proven byte-identical on `main` (§3).

## 2. Journey differential statement (vs the recorded 2.2 mutation-sensitivity signature)

Outcome 2.2 recorded the green signature **7 pass / 0 fail / 126 expect() calls** (and its mutation-kill RED signature 5/2/81) as the carry-forward gate for Phase 5. This run reproduced the green signature **byte-exactly: 7 pass / 0 fail / 126 expect() calls (~1.2s)** — all eight journey steps (single-tx cast → produce/observe 1 create row → 4-row newest-first governance lane → 7-value action filter sweep → gapless pagination + half-open windows → triple denial with byte-unchanged oracles → governed history readable → audit-first teardown) green with **zero assertion drift**. The Phase-0–4 surface did not move under the journey.

## 3. Differential findings during the run (none are DEV3-020 regressions)

1. **Wire-matrix block = stale dev server (environment, resolved).** The first `backend/graphql/test` directory run failed the `audit-trail.query.test.ts` wire matrix with 60s `waitForServer` timeouts: a leftover `bun next dev --webpack` process (PID 23173/23197, started ~21:58 — the worklog 4-final browser-loop leftover) held Next.js's single-instance dev-server lock, so the test's own port-3066 server could not boot ("Another next dev server is already running"). **Remediation: killed the stale processes (environment hygiene only, zero source changes); full directory re-run → wire matrix green** (236/4 with only the §3.2 fails). The 4-final dev-server-debt finding stands as pre-existing; CI never runs a server build.
2. **4 fails in `plan-catalog.roles.test.ts` = pre-existing main debt (NOT this plan).** The DEV1-005 role matrix expects anonymous calls to `adminPlans`/`createPlan`/`updatePlan`/`setPlanActiveStatus` to surface `UNAUTHORIZED`; they surface `FORBIDDEN` (4 tests, same 4 on every run). **Proven pre-existing:** the identical file fails identically on `main` (`21 pass / 4 fail`, same four anonymous-gate cases) — the branch adds zero commits touching plan-catalog resolvers, the admin gate, or any auth surface (full `git diff main..HEAD` file list contains only audit-trail + its re-pins). Reported as a finding per the phase rules; not fixed here (root cause is outside this plan's file set — DEV3-016/DEV1-005 gate semantics on main).

## 4. REQ-043 chaos cross-check (masked-INTERNAL mid-read failure — CONFIRMED in both tiers)

REQ-043 (plan.md:333) requires the forced mid-read failure chaos case to surface masked `INTERNAL_SERVER_ERROR` with exactly one correlated log, never a partial page presented as complete. Coverage confirmed by test name in BOTH suites (both green in the runs above):

- **Service tier — `backend/services/admin/audit-trail.service.test.ts`:** describe `AuditTrailService — corrupt stored enum (fail-closed, masked-internal)` → test **"unknown raw actionType → plain Error (never a domain code, never a cast)"** — canned corrupt row at the repository seam, asserts a plain non-domain `Error` carrying the raw stored value (masked at the transport boundary, D6 branch) and exactly one call to each paired read.
- **Wire tier — `backend/graphql/test/audit-trail.query.test.ts`** Tier 5 describe `chaos tier — corrupt stored enum fails closed and masks at the boundary`:
  - **"unknown raw actionType → plain non-domain Error, masked to INTERNAL_SERVER_ERROR with zero leak material"** — through the REAL admin gate, then `expectMaskedInternal(...)`: `extensions.code === "INTERNAL_SERVER_ERROR"`, requestId attached, corrupt value + mapper prose + thrown message all absent from the serialized body, non-empty localized replacement message;
  - **"generic unexpected-internal fault from the snapshot read → masked INTERNAL_SERVER_ERROR with zero leak material"** — the FORCED MID-READ failure: `countEntries` rejects mid-pipeline, and the suite asserts `listItems` leg **never ran** (`listSpy` zero calls — a partial page can never be presented), then the same masked-INTERNAL envelope with zero leak material.
- **Exactly-one-correlated-log assertion — `backend/graphql/test/error-finalizer.test.ts`:** test **"raw Error surfaces as localized INTERNAL_SERVER_ERROR with requestId and NO leak material"** — captures the logger streams around the production `finalizeGraphqlResponseScope` and asserts **exactly ONE `[ERROR]` line** (`errorLines` length 1) carrying the requestId (+ operationName). The wire matrix drives the same finalizer function, so the single correlated log line proven here is the log the masked wire path emits.
- The REQ-071 chaos lineage recorded in outcome 2.4 §2.3 (corrupt-enum case, spy-lifecycle registry) is the same coverage — cross-checked, no gap.

## 5. Coverage-gate statement (REQ-076 — 100% statement/branch on new service/repository code)

Enforced by the suites' assertion matrices (the plan's sanctioned enforcement posture — plan.md:500 pins the "100% statement/branch on new code" service tier to `backend/services/admin/audit-trail.service.test.ts`, no runtime coverage collector exists in the repo's gates), demonstrated green today:

- **Repository** (`audit-trail.repository.ts`): 2.3's matrix — **15 tests / 46 expect()** covering every branch: each of the six filter dimensions dropped in and out, both half-open window boundaries at exact-instant precision (row AT `from` included, row AT `to` excluded), ordering + `id` tiebreak, gapless page tiling / out-of-range offset / empty-set honesty, null pass-through, join projection integrity, the zero-write oracle, and the `actor_id` FK error path (`expectRepoError`). Re-run GREEN today (32/0/368 across the audit db tier incl. the immutability DDL pins).
- **Service** (`audit-trail.service.ts`): 2.4's matrix — **35 tests / 190 expect()** covering every branch: both gate denials (one bounded `logDomainError` each + zero row contact + byte-unchanged write oracles), every pre-DB validation rejection (each malformed id/entityType/enum/window member incl. degenerate `from === to`), pagination clamps (0/1/100/101/fractional/negative) + defaults echo, both transaction shapes (top-level REPEATABLE READ with byte-equal txConfig + executor-identity oracles; nested `outerTx` join with `db.transaction` never called), honest envelope echo, out-of-range page honesty, determinism, and the corrupt-enum masked-internal branch. Re-run GREEN today (104/0/472 across the services tier incl. the DEV3-016 regression lock + chaos tier).
- Static side of the same gate: `bun tsgo` 0 errors (no dead/unused branches survive the compiler), oxlint 0/0 with `--deny-warnings`, `lint:type-aware` sub-loop exit 0 recorded per file in outcomes 2.3/2.4 — the counters re-proven at baseline+0 in the Task 5.2 outcome.

## 6. Carry-forward

- The 4 `plan-catalog.roles.test.ts` anonymous-gate fails (UNAUTHORIZED-vs-FORBIDDEN) belong to the DEV3-016/DEV1-005 owners on `main`; Phase 6 review waves should read them against the closed error-set contract but must not "fix" them inside DEV3-020.
- Phase 6.2 should re-use the exact per-suite numbers in §1 as its differential baseline.
