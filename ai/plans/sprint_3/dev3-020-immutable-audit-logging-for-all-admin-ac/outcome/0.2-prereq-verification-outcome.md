# Task 0.2 — Prerequisite Verification & Prose-Phantom Sweep Outcome (DEV3-020)

**Plan directory:** `ai/plans/sprint_3/dev3-020-immutable-audit-logging-for-all-admin-ac`
**Method:** verify-then-claim — every row below was checked against LIVE code on branch `feat/dev3-020-immutable-audit-logging-for-all-admin-ac` (clean tree). Baseline counts live in `phase0-baseline-outcome.md`.

---

## 1. Verified-fact table (10 items)

| # | Item | Live finding (path + lines) | Verdict |
|---|---|---|---|
| 1 | Audit-immutability trigger (REQ-020 branch) | `backend/db/migration/3-immutability-triggers.sql:29–53` — audit_logs covered: `CREATE OR REPLACE FUNCTION prevent_audit_logs_update()` (:29–34) + `prevent_audit_logs_delete()` (:36–41), `DROP TRIGGER IF EXISTS` + `CREATE TRIGGER prevent_audit_logs_update_trigger` (:43–47) / `..._delete_trigger` (:49–53); same idempotent pattern for `student_payments` (:59–83) and `teacher_transaction` (:89–113). SHIPPED drizzle copy `backend/drizzle/20260825222701_custom_3-immutability-triggers/migration.sql` EXISTS — byte-equivalent DDL modulo drizzle's `--> statement-breakpoint` markers (verified by diff). LIVE on the migrate-provisioned DB: `pg_trigger` probe returns `prevent_audit_logs_update_trigger \| O` and `prevent_audit_logs_delete_trigger \| O` (both enabled). SQLite parity file `3-immutability-triggers-sqlite.sql` also present (not relevant to REQ-020). | **CONFIRMED** |
| 2 | `backend/db/repo/audit/` | ABSENT (`ls` → "No such file or directory"). Siblings: `admin/`, `billing/`, `notifications/`, `parents/`, `students/`, `teachers/`, `users/`, `index.ts`, `AGENTS.md`. Top-level `backend/db/repo/index.ts` has no `./audit` export. | **CONFIRMED ABSENT → CREATE** (Task 2.3) |
| 3a | `frontend/views/admin/` + subdirs | EXISTS: `frontend/views/admin/index.ts` (`export * from "./plans"; export * from "./users";`) + `plans/` (catalog/dialogs/forms/hooks/ui) + `users/`. `frontend/views/admin/audit/` ABSENT. | **CONFIRMED → CREATE audit/** |
| 3b | `app/(dashboard)/audit/page.tsx` | ABSENT. `app/(dashboard)/` live routes: `[feature]/`, `admin/` (dashboard, plans, users/[id]), `dashboard/`, `notifications/`, `parent/`, `profile/`, `student/`, `teacher/`, `layout.tsx`. | **CONFIRMED ABSENT → CREATE** |
| 4 | Audit test under `backend/db/test/logic/audit/` | ABSENT (no `audit/` dir; dirs: admin, auth, billing, notifications, students, teachers, users). Repo-wide grep for `audit` under `backend/db/test` hits only `logic/AGENTS.md` prose — no test file. | **CONFIRMED ABSENT → CREATE** (Task 2.3.TE) |
| 5 | `/audit` routing vs catch-all (REQ-064) | Dynamic segment: `app/(dashboard)/[feature]/page.tsx` — a SINGLE-segment dynamic route (`params: Promise<{ feature: string }>`, renders `ComingSoonView`). Its own doc (:12–14, :16–21) states "More specific routes take precedence over this catch-all" and even lists **Audit** among the links currently served by the catch-all. Precedent static siblings that already beat it: `/notifications`, `/dashboard`, `/profile`, `/admin/*`. A new static `app/(dashboard)/audit/page.tsx` wins by Next.js static-beats-dynamic precedence. | **CONFIRMED** |
| 6 | `backend/services/admin/admin-gate.helpers.ts` | **EXISTS — DRIFT vs plan expectation (expected ABSENT).** The DEV3-022c extraction HAS LANDED: file carries BOTH `toAuditActionType` (:25–44, exported, fail-closed switch over the 7 `AuditActionType` members → null default) and `assertActorAdmin` (:59–90, exported, `actorId===0` → `UnauthorizedError`; missing actor or non-admin → `ForbiddenError`, each with exactly one bounded `logger.logDomainError`, zero writes). Consumers migrated: `user-management.service.ts:65` imports `assertActorAdmin` (7 call sites :118–:395); `user-management.mappers.ts:19,234` imports/calls `toAuditActionType`. **The admin barrel `backend/services/admin/index.ts` does NOT yet export it** (only `./audit.service` + `./user-management.service`, :9–10). | **DRIFTED (exists) → D2 = IMPORT branch** |
| 7 | `shared/locale/adminUsers-namespace.parity.test.ts` | ABSENT. Live inventory of `shared/locale/*-namespace.parity.test.ts` is EXACTLY five: `applicant-`, `errors-`, `handshakeCode-`, `notifications-`, `plans-namespace.parity.test.ts`. (`handshakeCode` parity VERIFIED PRESENT, as plan D13 states.) | **CONFIRMED ABSENT → D13 = CREATE** (model on `handshakeCode-namespace.parity.test.ts`) |
| 8 | apolloCache typePolicies vs frozen assertion | LIVE registered keys in `frontend/providers/apollo/apolloCache.ts:27–59` (6): `AdminDashboardScheduleResult` (rows `merge:false`), `AdminNoteInfo` (`keyFields:false`), `HealthCheck` (`keyFields:false`), `NotificationListPage` (`keyFields:false`, :47–49), `HandshakeCodeLookup` (`keyFields:false`), `OnlineMeetingInfo` (`keyFields:false`). Frozen assertion at `apolloCache.test.ts:176–185` ("policy surface is FROZEN to the five documented entries") lists only 5 — **missing `NotificationListPage`**. Test run: **FAILS** with exactly `+ "NotificationListPage"` (9 pass / 1 fail) — the known drift, live. Reconciliation direction (never subtract): update the frozen assertion to the SIX-entry list. | **CONFIRMED + DRIFT documented** |
| 9 | Schema-surface + SDL frozen pins (Task-3.2 re-pin targets) | Full verbatim arrays in §3 below. Both suites currently RED at baseline purely from stale pins vs landed Sprint-3 surfaces (DEV3-016/DEV1-005/DEV1-013); exact failure diffs in §4. The artifact↔builder byte-identity test (`schema-surface.test.ts` "Codegen sync") PASSES. | **CONFIRMED (stale pins = known baseline reds)** |
| 10 | Bundle anchors | Per-anchor results in §2. | Mixed: 12 CONFIRMED, 4 line-path drifts, 1 phantom ABSENT as predicted |

---

## 2. Item 10 detail — bundle anchors (path :lines, shape)

| Anchor | Live location | Notes / verdict |
|---|---|---|
| `AuditService.createAuditLog` | `backend/services/admin/audit.service.ts:82–90` — `export async function createAuditLog(input: AuditLogWriteContract, tx: DBTransaction): Promise<void>` inserting `{actorId, actionType, entityType, entityId, details: truncateDetailsSafely(input.details)}` | **EXACT** match to anchor :82–90 |
| `assertActorAdmin` | **MOVED** → `backend/services/admin/admin-gate.helpers.ts:59–90` (see item 6). Old anchor `user-management.service.ts:240–271` STALE — service file is now 446 lines and only imports the gate at :65 | **DRIFTED-INTO-HELPERS** (extraction landed) |
| `toAuditActionType` | **MOVED** → `admin-gate.helpers.ts:25–44`; consumed at `user-management.mappers.ts:19` (import) + `:234` (call). Old anchor `user-management.service.ts:130–149` STALE | **DRIFTED-INTO-HELPERS** |
| `getActivity` | `backend/db/repo/admin/admin-user.repository.ts:238–256` (anchor :510–528 STALE — file is 399 lines). Pattern intact: executor `(tx ?? db)` at :243; `.select({id, actionType, actorName: users.fullName, details, createdAt})`; `.innerJoin(users, eq(users.id, auditLogs.actorId))` :252; `.where(and(eq(auditLogs.entityType,"user"), eq(auditLogs.entityId, userId)))` :253; `.orderBy(desc(auditLogs.createdAt), desc(auditLogs.id))` :254; `.limit(limit)` :255 | **CONFIRMED (line drift only)** — this is the Task-2.3 pattern donor |
| `AuditActionTypePothosEnum` | `backend/graphql/pothos/shared/enum.pothos.ts:112–114` — `gqlSchemaBuilder.enumType(AuditActionType, { name: "AuditActionType" })`; already used by `admin/admin-user.pothos.ts:34,160` | **CONFIRMED** (task text omitted the `shared/` segment) |
| `DateTime` scalar | `backend/graphql/pothos/shared/scalar.pothos.ts:28` — `export const DateTimeScalar = gqlSchemaBuilder.addScalarType("DateTime", DateTimeResolver, {})` | **EXACT** match to plan D4 anchor |
| `withAuditDeleteTriggersSuspended` | `test/helpers/db-cleanup.ts:83–109` — discovers non-internal `audit_logs` triggers via `pg_trigger` probe (:84–86), no-op when none (:91–93), disables, runs `fn`, restores exact prior `tgenabled` in `finally` (:100–108) | **EXACT** match to anchor :83–109 |
| `isPgliteProvider` | `test/helpers/skip-when-pglite.ts:48–50` (+ `describeGraphqlSuite` at :56 = `describe.skip` under pglite) | **CONFIRMED** |
| Journey helpers barrel `@/test/workflows/helpers` | `test/workflows/helpers/index.ts:24–29` — `export *` from: `actor-context`, `journey-actor-fixtures`, `journey-cleanup`, `journey-fixtures`, `spied-transport`, `tracked-fixtures`. Key exports: `provisionStudentActor` (`actor-context.ts:88`), `provisionParentActor` (:122), `provisionAdminActor` (:136), `JourneyActor` (:43); `JourneyActorFixture` (`journey-actor-fixtures.ts:79`); `TrackedFixtures` (`tracked-fixtures.ts:173`). `test/workflows/admin/` EXISTS with the 2 DEV3-016 journeys (`admin-user-denials`, `admin-user-lifecycle`) | **CONFIRMED** (matches tasks.md 2.2 scaffolding note incl. L136/L88/L122) |
| Admin nav `/audit` item | **PATH DRIFT:** `frontend/views/dashboard/nav/navItems.ts:133` (not `.../dashboard/navItems.ts`) — `{ route: "/audit", labelKey: "audit", Icon: AuditIcon },` inside `[UserRole.Admin]` (:126–135); icon aliased at :7 `AssessmentOutlined as AuditIcon`. ZERO-CHANGE retarget (D12) stands — the item already points at `/audit` | **CONFIRMED (path drift only)** |
| Seven `adminUsers.activity.action*` labels | `shared/locale/types/adminUsers/index.ts` — `activity` block :422–451; the seven action chip keys at **:438–450**: `actionCreate` (:438), `actionUpdate` (:440), `actionDelete` (:442), `actionReactivate` (:444), `actionOverride` (:446), `actionAdjust` (:448), `actionSuspend` (:450). Non-action siblings: `title` :424, `empty` :426, `entryActionLabel` :428, `viewAll` :430, `viewFullAuditLog` :432, `changedFields` :434, `byActor` :436 | **CONFIRMED** (anchor range :417–451 ≈ live :422–451) |
| `formatApplicantDate` | `frontend/lib/i18n/format-date.ts:56–59` — `export function formatApplicantDate(iso: string, locale: string): string` via `Intl.DateTimeFormat(resolveLocaleTag(locale), APPLICANT_DATE_OPTIONS)` | **EXACT** match |
| `withPageAuth` | `frontend/lib/auth/withPageAuth.ts:67` — `export async function withPageAuth(options?: WithPageAuthOptions): Promise<WithPageAuthResult>`; re-exported by `frontend/lib/auth/index.ts:3`; canonical usage pattern `frontend/views/dashboard/home/RoleDashboardPage.tsx:34` | **CONFIRMED** |
| `roleDashboardPath` | `frontend/lib/auth/roleDashboardRoute.ts:52` — `export function roleDashboardPath(role: string \| UserRole \| null \| undefined): string` | **CONFIRMED** |
| `PermissionDeniedFallback` | `frontend/components/ui/PermissionDeniedFallback.tsx:36` | **CONFIRMED** |
| `RetryableNotice` | `frontend/components/ui/RetryableNotice.tsx:50` — `export function RetryableNotice({ kind, onRetry, retryInFlight = false }: Readonly<RetryableNoticeProps>)` | **CONFIRMED** |
| `AppDataGrid` | **ABSENT** as component. Sole references are prose: root `AGENTS.md:391` + `frontend/COMPONENT_PATTERNS.md`. No definition anywhere under `frontend/` (grep). | **CONFIRMED ABSENT (prose-only phantom — D10 holds; build the page table from raw MUI `Table` primitives)** |

---

## 3. Task-3.2 re-pin targets — LIVE arrays (verbatim)

### `backend/graphql/test/schema-surface.test.ts` (built-schema tier)

`PRE_3_1_QUERY_FIELDS` (:91–99):
```ts
["adminPlans", "me", "myApplicantProfile", "myNotifications", "myUnreadNotificationCount", "planCatalog", "recitationReadings"]
```
Query-additions assertion (:195–200) expects additions ≡ `["_health", "findStudentByHandshakeCode", "myHandshakeCode"]`.

`PRE_3_1_MUTATION_FIELDS` (:101–112), pinned EXACTLY at :260–266:
```ts
["createPlan", "login", "logout", "markAllNotificationsRead", "markNotificationRead", "refreshToken", "registerUser", "setPlanActiveStatus", "updateMyLocale", "updatePlan"]
```

`PRE_3_1_ENUMS` (:114–122), pinned EXACTLY at :268–275:
```ts
["ApplicantStatus", "AppLocale", "Gender", "NotificationType", "RecitationReading", "RegisterPublicRole", "UserRole"]
```

`PRE_3_1_TYPE_NAMES` (:124–146):
```ts
["AppLocale", "ApplicantProfile", "ApplicantStatus", "CreatePlanInput", "Gender", "LoginPayload", "LogoutPayload", "Mutation", "MyNotificationsFilterInput", "Notification", "NotificationListPage", "NotificationType", "Plan", "Query", "RecitationReading", "RefreshTokenPayload", "RegisterPublicRole", "RegisterUserInput", "UpdatePlanInput", "User", "UserRole"]
```
Whole-schema type-name additions assertion (:283–284) expects ≡ `["DateTime", "HandshakeCodeLookup", "HealthCheck"]`.

### `backend/graphql/test/sdl-static-assertions.test.ts` (committed-artifact tier, over `frontend/graphql/generated/schema.graphql`)

`FROZEN_QUERY_FIELDS` (:78–85):
```ts
["_health", "me", "myApplicantProfile", "myNotifications", "myUnreadNotificationCount", "recitationReadings"]
```
`FROZEN_MUTATION_FIELDS` (:67–75):
```ts
["login", "logout", "markAllNotificationsRead", "markNotificationRead", "refreshToken", "registerUser", "updateMyLocale"]
```
Root-set pins asserted at :220–228 (Mutation exact-7; Query exact-6).

---

## 4. Pre-existing test failures at baseline (exact diffs — differential reference for Phases 3–7)

### `sdl-static-assertions.test.ts` — 15 pass / 3 fail
1. Mutation-root pin (:220–223): artifact Mutation root = `FROZEN_MUTATION_FIELDS` + `adminCreateUser`, `adminSetUserDeleted`, `adminUpdateUser` (DEV3-016) + `createPlan`, `setPlanActiveStatus`, `updatePlan` (DEV1-005).
2. Query-root pin (:225–228): artifact Query root = `FROZEN_QUERY_FIELDS` + **8**: `adminPlans`, `adminUserActivity`, `adminUserDetail`, `adminUserStats`, `adminUsers`, `findStudentByHandshakeCode`, `myHandshakeCode`, `planCatalog`.
3. "NO Subscription root" (:324–330): AST check passes; the lexical `sdlText` scan trips on the token "Subscription" inside `hasActiveSubscription` (DEV3-016 `AdminStudentSnapshot`) and "subscription plan" description prose — false-positive class, requires re-pin handling (tighten the lexical scan or re-scope), NOT a real Subscription root.

### `schema-surface.test.ts` — 29 pass / 4 fail
1. Query additions (:196): received `["_health", "adminUserActivity", "adminUserDetail", "adminUsers", "adminUserStats", "findStudentByHandshakeCode", "myHandshakeCode"]` (+4 DEV3-016 fields).
2. Mutation set (:264): received baseline + `adminCreateUser`, `adminSetUserDeleted`, `adminUpdateUser` (+3).
3. Enum set (:274): received baseline + `AdminUserGovernanceFilter`, `AuditActionType` (+2).
4. Whole-schema type delta (:284): received `["DateTime", "HandshakeCodeLookup", "HealthCheck"]` + **13**: `AdminCreateUserInput`, `AdminParentSnapshot`, `AdminStudentSnapshot`, `AdminTeacherSnapshot`, `AdminUpdateUserInput`, `AdminUserActivityEntry`, `AdminUserDetail`, `AdminUserFiltersInput`, `AdminUserGovernanceFilter`, `AdminUserListItem`, `AdminUserPage`, `AdminUserStats`, `AuditActionType`.
(All other 29 tests pass, including artifact↔builder byte-identity.)

### `apolloCache.test.ts` — 9 pass / 1 fail
Frozen policy list (:178–184) received the live six with `+ "NotificationListPage"`.

---

## 5. Branch decisions

| Decision | Live evidence | Branch taken |
|---|---|---|
| **REQ-020 (verify-vs-create trigger)** | Trigger SQL present + shipped + LIVE in DB (item 1) | **VERIFY** — consume the existing trigger by reference; NO new migration file. The drift branch (create a new drizzle custom migration) stays CLOSED unless a later grep contradicts. `bun run db push` remains forbidden for this surface. |
| **D2 (import-vs-extract gate)** | `backend/services/admin/admin-gate.helpers.ts` EXISTS with BOTH functions already extracted verbatim; service + mappers already import them (item 6) | **IMPORT** — Task 2.1 shrinks to: (a) import `assertActorAdmin` (already imported by the consuming service); (b) `toAuditActionType` is ALREADY in the helpers module — do NOT re-extract, do NOT fork; (c) the genuinely outstanding edit is `backend/services/admin/index.ts` gaining `export * from "./admin-gate.helpers";`; (d) re-run DEV3-016 suites as byte-equivalence lock. |
| **D13 (verify-or-create parity suite)** | `adminUsers-namespace.parity.test.ts` ABSENT; inventory exactly applicant/errors/handshakeCode/notifications/plans (item 7) | **CREATE** — modeled on `shared/locale/handshakeCode-namespace.parity.test.ts`, pinning the new `adminUsers.auditTrail` block under both locales + Arabic-script presence. |

---

## 6. Carry-forward knowledge for later tasks

1. **Anchor line numbers moved in two files** (no semantic drift): `getActivity` is at `admin-user.repository.ts:238–256` (donor pattern for Task 2.3: `(tx ?? db)` executor, `actorName: users.fullName` inner-join projection, `desc(createdAt), desc(id)` ordering); the gate + coercion live in `admin-gate.helpers.ts` (:59–90, :25–44). Do NOT chase the stale plan anchors :240–271 / :130–149 / :510–528.
2. **Task 2.1 minimum diff:** barrel export of `admin-gate.helpers` in `backend/services/admin/index.ts` + verification that no private copies remain (verified: none). `user-management.service.ts:16–23` header already documents the extracted gate.
3. **Task 2.3 forbidden-surface reminders verified relevant:** repo layer has NO audit write path (repo index has no audit module); `escapeLikeWildcards` obligation never arises (D5).
4. **Task 2.2:** `test/workflows/admin/` exists with the two DEV3-016 journeys; helpers barrel confirmed (§2). Only the new journey file gets added.
5. **Task 3.2 re-pin mechanics:** absorb the DEV3-016/DEV1-005/DEV1-013 drift ADDITIVELY (the arrays in §3 must grow to include the +4 query / +3 mutation / +2 enum / +13 type entries AND the new `adminAuditLogs` query + `AdminAuditLog*` types), then re-pin. The lexical "Subscription" scan in `sdl-static-assertions.test.ts:329` needs its scope fixed (match on type NAME / definitions, not raw artifact text) — the artifact legitimately contains the token inside `hasActiveSubscription` and prose.
6. **Frontend cache policy (Task 4.x):** the DEV3-020 read surface returns `AdminAuditLogEntry` objects WITH `id` (normalizable entities) inside an `AdminAuditLogPage` wrapper — if the wrapper type must be written to the cache, mirror the `NotificationListPage` posture (`keyFields:false`) and extend the frozen assertion to the then-current full set (never subtract). The current assertion→live delta is exactly `NotificationListPage`.
7. **Nav:** the `/audit` item already exists at `frontend/views/dashboard/nav/navItems.ts:133` (path has an extra `nav/` segment vs plan prose) — ZERO-CHANGE retarget (D12); do not add a duplicate item.
8. **Environment:** `~/.bun/bin/bun` symlink → `/usr/local/bin/bun` required by the mandated test runner (was missing; created). Quality gate (`sub-loop.ts --lifecycle duplicates`) exits 1 on `.md` files via a lint-path artifact — treat tsgo step as the signal for Markdown artifacts.
9. **Ledger:** six pre-registered reference entries seeded (see `phase0-baseline-outcome.md` §3); no ❌ entries — no item in Task 0.2 found a missing REQUIRED artifact (all "expected absent" items were indeed absent → CREATE paths; the single "expected absent but present" item, `admin-gate.helpers.ts`, resolves to the plan's own IMPORT branch, not a blocker).
