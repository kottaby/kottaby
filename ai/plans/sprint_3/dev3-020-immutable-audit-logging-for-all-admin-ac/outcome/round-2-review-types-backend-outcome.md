# Round 2 Review — types + backend (Task ID `6-r2a`)

- **Plan:** DEV3-020 — immutable audit logging for all admin actions (round 2, independent fresh reviewer)
- **Scope:** types (`backend/types/audit/*`), backend (repo `backend/db/repo/audit/*`, `backend/services/admin/audit-trail.service.ts` + barrels, `backend/graphql/pothos/admin/audit-trail.pothos.ts`, `backend/graphql/query/admin/audit-trail.query.ts` + barrels) and their tests (`backend/db/test/logic/audit/*`, `backend/services/admin/audit-trail.service.test.ts`, `backend/graphql/test/audit-trail.query.test.ts`, `test/workflows/admin/audit-trail.journey.test.ts`)
- **Method:** fresh read of every scope file from the branch tree (`git show feat/dev3-020-immutable-audit-logging-for-all-admin-ac:<path>` / `git grep <branch-ref>`) — deliberately branch-ref-based because the shared working tree was being flipped to `main` by a concurrent process during the review (see Environment note). No files modified.
- **Verdict: CLEAN — 10/10 checks PASS, R1 fixes (a) and (b) both held, zero findings (0 CRITICAL / 0 HIGH / 0 MEDIUM / 0 LOW).**

---

## Check-by-check verdict

| # | Check | Verdict | Evidence (file:line, branch tree) |
|---|-------|---------|-----------------------------------|
| 1 | Canonical types single-definition | **PASS** | Repo-wide grep over the branch tree: `AdminAuditTrailFiltersSubmitInput` defined exactly once — `backend/types/audit/audit-trail.types.ts:4`; `AdminAuditLogEntryReturnType` once — `:14`; `AdminAuditLogPageReturnType` once — `:25`; frontend seed `AuditTrailFiltersSeed` once — `frontend/views/admin/audit/audit-trail-filters.ts:38`. Every other occurrence is an import, re-export, call-site, or docblock reference. No cross-layer shape confusion: frontend files (`AuditTrailView.tsx`, `AuditTrailFilterBar.tsx`, `app/(dashboard)/audit/page.tsx`, UI test) touch only the seed type; backend + backend-contract tests (service test, journey test) touch only the canonical. Barrels are pure `export *` re-export chains (`types/audit/index.ts` → `backend/types/index.ts`). |
| 2 | Service pipeline order gate → validate → paginate → single repeatable-read tx → map | **PASS** | `audit-trail.service.ts:264` (`assertActorAdmin`) → `:266` (`normalizeTrailFilters`) → `:267` (`resolvePageBounds`) → `:269` (`readInSnapshot`: count + list paired inside ONE transaction — fresh top-level at `isolationLevel: "repeatable read"` when no caller tx (`:197`), nested join when supplied) → `:278` (`pageRows.map(mapTrailRow)`). Pinned by tests: service test `:572-611` asserts exactly ONE top-level `db.transaction` call with `{ isolationLevel: "repeatable read" }`, the same executor handed to both reads (and not `db`), and the gate probe OUTSIDE the tx; `:612-641` asserts a supplied caller tx is joined with NO fresh top-level transaction. |
| 3 | Closed error set + exactly-one bounded log per denial + silent happy path | **PASS** | Closed set: `UnauthorizedError`/`ForbiddenError` (shared gate, `admin-gate.helpers.ts` — pre-existing, verbatim-extracted), localized `ValidationError` (validation + pagination via `resolvePageBounds`), plain `Error` for a corrupt stored enum (`audit-trail.service.ts:212` — masked to a generic internal error at the transport boundary, never a domain code). Each gate denial logs exactly once via `logDomainError` with ids/codes only (no filter payloads, no `details`, no PII); validation/pagination rejections log nothing (service test `:273-284`); happy path logs nothing. Zero writes on every path incl. denials (service test zero-write oracles `:186-188`, `:214-216`; wire test + journey row-count oracles). |
| 4 | Repo parameterization + zero writes + `(tx ?? db)` + tx-last | **PASS** | `audit-trail.repository.ts`: predicate builder `buildWhere` (`:94`) emits only bound-parameter predicates `eq` / `gte` / `lt` ANDed; absent members drop out; zero string interpolation reaches SQL text — the only raw fragment is the static `sql<number>\`count(*)::int\`` (no interpolation). No `LIKE`/`ilike`/`sql.raw` anywhere in the repo or service (grep: zero hits). Both methods read-only (`select` only; no insert/update/delete). `(tx ?? db)` at `:148` and `:177`; `tx?: DBTransaction` is the optional-LAST parameter of both `listEntries` (`:142`) and `countEntries` (`:176`). Shared `buildWhere` makes list/count drift structurally impossible; deterministic ordering `createdAt DESC, id DESC` (`:162`) with the id tiebreak for same-timestamp batch rows. |
| 5 | Thin resolver | **PASS** | `audit-trail.query.ts`: single side-effect-registered `queryField("adminAuditLogs", …)`, no named exports; `authScopes: { $all: { authenticated: true, role: [UserRole.Admin] } }` (explicit `$all` conjunction, not the ANY-semantics plain map); `ctx.user` used purely for TS narrowing with a translated `UnauthorizedError` belt matching the scope's own throw; NO try/catch, NO business logic; the six filter members are copied FIELD-BY-FIELD into the service whitelist (`?? null` per member) — no `{ ...args.filters }` spread; only `ctx.user.id` (identity) + `ctx.locale` cross the boundary. |
| 6 | Race conditions | **PASS** | No module-level mutable production state in any new backend module — top-level declarations are `const` only (`MAX_ENTITY_TYPE_LENGTH`) or immutable refs; the resolver/pothos files register schema objects at import time (no mutable module state). No read-then-write anywhere: the surface is a pure read; the one cross-read consistency hazard (count/list tearing under a concurrent producer commit) is closed by the single repeatable-read snapshot tx (check 2), and stale-page races resolve to the documented honest-empty-page semantics, never an error. |
| 7 | Dead code (unused exports) | **PASS** | Consumer sweep over the branch tree: canonical types → service, pothos, service test, journey test; `NormalizedAuditTrailFilters` + `AuditTrailEntryRow` → service + repo test; `AuditTrailRepository` (`listEntries`/`countEntries` both live) → service, repo test, wire test; `AuditTrailService` → resolver, service test, wire test, journey test; `AdminAuditLogEntryPothosObject` → consumed in-file as the `items` element type (`audit-trail.pothos.ts:61`) and registered via the query import chain; `AdminAuditLogPagePothosObject` + `AdminAuditLogFiltersInput` → query; module-local `buildWhere` shared by both methods. Every barrel line (`repo/audit/index.ts`, `repo/index.ts`, `types/audit/index.ts`, `pothos/admin/index.ts`, `query/admin/index.ts`, `services/admin/index.ts`) points at a consumed module; wiring chain `query/admin → query/index → gqlSchema` intact. No unused exports found. |
| 8 | Cross-layer imports | **PASS** | Grep over the branch tree: no file under `backend/**` imports `@/frontend/**` or any frontend module (only `AGENTS.md` prose and the pre-existing cross-layer-ban static-assertion test mention the rule). New backend scope imports stay within `backend/**` + `@/shared/locale`. The frontend, in turn, touches only its own seed type + codegen output. |
| 9 | Enum discipline | **PASS** | Value-import exactly where the enum VALUE is used at runtime: service (`Object.values(AuditActionType)` membership re-assertion, `audit-trail.service.ts:46`), resolver (`UserRole.Admin` in the auth scope, `:41`), pothos (`AuditActionTypePothosEnum` shared-registry ref — reused, never re-registered), tests (member values). Type-only where the name appears only in type position: `backend/types/audit/audit-trail.types.ts:1` and repo `:30` both use `import type`. No violations in either direction. |
| 10 | Comments free of plan artifacts | **PASS** | Grep of all 10 scope files for `REQ-[0-9]+`, `Task [0-9]`, `DEV3-020`, `\bD[0-9]+\b`, `§`, `ai/plans`, `plan.md`, `tasks.md`: zero hits in every file. Docblocks describe behavior and invariants only. (The pre-existing `admin-gate.helpers.ts` retains its historical `DEV3-016` provenance note — that file is NOT in this plan's diff, so it is baseline, not a finding.) |

## R1 fix verification

- **(a) Frontend type rename left no stale references — HELD.** Grep for `AdminAuditTrailFiltersSubmitInput` across `frontend/**`, `app/**`, `test/ui/**`: zero occurrences; every frontend consumer uses the renamed `AuditTrailFiltersSeed` (`audit-trail-filters.ts:38` definition; `AuditTrailView.tsx:53,61,79`, `AuditTrailFilterBar.tsx:18,36`, `app/(dashboard)/audit/page.tsx:5,89,100,102`, `test/ui/.../AuditTrailView.test.tsx:79,198`). The canonical name survives only as the backend definition and its backend-side consumers (service, pothos docblock, service test, journey test) — exactly the intended post-fix state: one definition per shape, no stale aliases.
- **(b) Wire smuggle probes assert `/userId/` — HELD.** `backend/graphql/test/audit-trail.query.test.ts:830` (root smuggle `adminAuditLogs(userId: …)`) and `:838` (`filters: { userId: … }`) both assert `expect(errorMessageOf(item)).toMatch(/userId/)`, with the in-test rationale documented (a schema-absent server would produce the same code, so the message text is the discriminator against false-passing under schema drift). The forged-enum and Int-overflow probes complete the tier and the whole probe set is gated by the row-count oracle.

## Test-layer sanity (supporting evidence)

- `audit-trail.repository.test.ts` (631 L): single-dimension filters, half-open window boundaries (from inclusive / to exclusive), combined chain, ordering + tiebreak, gapless page tiling, honest empty/out-of-range semantics, live-join projection, zero-write oracle, and the FK join-integrity precondition.
- `audit-trail.service.test.ts` (685 L): gate denials (one bounded log, zero reads, zero writes), 12-case malformed-filter matrix, smuggled enum re-assertion, validation-silence, pagination boundaries, normalization fallbacks, honest echoes, determinism, the two snapshot-tx regimes, and corrupt-enum fail-closed.
- `audit-trail.query.test.ts` (927 L): real-wire anonymous/role denials with byte-unchanged oracles, admin happy path vs direct-DB oracle, hostile pagination, BOPLA smuggle tier, and the chaos tier (repository-seam corruption → masked INTERNAL_SERVER_ERROR with zero leak material).
- `audit-trail.journey.test.ts` (647 L): produce→observe→filter (all seven action types)→paginate→deny→governance-readability, with audit + notification oracles byte-unchanged across denials.
- `audit-immutability.test.ts` (485 L): static zero-mutation corpus scan with non-vacuity fixtures, live trigger enforcement (UPDATE/DELETE rejected, append open), and the migration DDL pin; teardown allowlist (`db-cleanup.ts`, journey test, `journey-cleanup.ts`) is live-matched.

## Environment note (not a finding)

The shared working tree repeatedly flipped from the feature branch back to `main` during this review (observed on three separate batches). All review content was therefore read via branch-ref `git show` / `git grep` (`feat/dev3-020-immutable-audit-logging-for-all-admin-ac` @ `333e51f`), which is immune to the checkout state; the branch was restored to the feature branch whenever it was found on `main`. `worklog.md` is byte-identical on both branches, so the appended worklog section is branch-safe. No files were modified by this review other than this outcome file and the worklog append.

## Findings

**Zero.** No CRITICAL, HIGH, MEDIUM, or LOW findings in the types + backend scope. The R1 remediations held; the implementation matches the plan's type architecture, pipeline contract, error/logging discipline, repository parameterization rules, resolver thinness, and hygiene constraints exactly.
