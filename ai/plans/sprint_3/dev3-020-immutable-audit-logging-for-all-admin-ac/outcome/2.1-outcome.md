# Task 2.1 Outcome — Shared Admin Gate + Audit Coercion, IMPORT Branch (DEV3-020)

**Plan directory:** `ai/plans/sprint_3/dev3-020-immutable-audit-logging-for-all-admin-ac`
**Task:** 2.1 Shared admin gate + audit coercion extraction (`admin-gate.helpers.ts` — VERIFY-OR-CREATE, D2)
**Branch:** `feat/dev3-020-immutable-audit-logging-for-all-admin-ac`
**D2 branch decision:** **IMPORT** (per Phase 0 verdict, `0.2-prereq-verification-outcome.md` §5/§1-item-6 — DEV3-022c landed the helpers first)

---

## 1. Branch taken: IMPORT — evidence

Phase 0 verdict re-confirmed live at task start against `backend/services/admin/admin-gate.helpers.ts` (90 lines, read in full, UNMODIFIED):

| Symbol | Location in helpers module | Shape |
|---|---|---|
| `toAuditActionType(raw: string): AuditActionType \| null` | **:25–44** (exported) | fail-closed switch over the 7 `AuditActionType` members → `null` default |
| `assertActorAdmin(actorId: number, locale: string, outerTx?: DBTransaction): Promise<void>` | **:59–90** (exported) | actorId===0 → `UnauthorizedError`; missing actor or non-admin → `ForbiddenError`; exactly one bounded `logger.logDomainError` per denial; zero writes |

Per extend-never-fork: the file was **NOT created and NOT modified** (zero-byte diff — `git diff backend/services/admin/admin-gate.helpers.ts` empty). The only genuinely outstanding edit per the plan (and per Phase 0 item 6: "The admin barrel does NOT yet export it") was the barrel export.

### Pre-existing consumers already migrated by DEV3-022c (verified — no private copies remain)

| File | Line | Evidence |
|---|---|---|
| `backend/services/admin/user-management.service.ts` | **:65** | `import { assertActorAdmin } from "@/backend/services/admin/admin-gate.helpers";` — 7 call sites **:118, :157, :193, :231, :271, :342, :395** |
| `backend/services/admin/user-management.service.ts` | **:17–23** | header JSDoc documents the gate lives in `admin-gate.helpers.ts` (`assertActorAdmin`) |
| `backend/services/admin/user-management.mappers.ts` | **:19** | `import { toAuditActionType } from "@/backend/services/admin/admin-gate.helpers";` — call site **:234** |

**Absence-of-private-copies proof (repo-wide, non-test):** `rg "export (async )?function (assertActorAdmin|toAuditActionType)" backend/ shared/ frontend/ scripts/` → exactly 2 hits, both `admin-gate.helpers.ts:25` and `:59`. No `const` aliases either (`rg "function assertActorAdmin|function toAuditActionType|const assertActorAdmin|const toAuditActionType"` same 2 hits). The service file carries ZERO local definitions — nothing to delete (the "DELETE the private copies" half of Task 2.1 was already satisfied by the DEV3-022c extraction; only the barrel export remained).

## 2. The exact barrel diff (sole edit of this task)

`backend/services/admin/index.ts` — one line inserted, alphabetical order preserved:

```diff
  */
+export * from "./admin-gate.helpers";
 export * from "./audit.service";
 export * from "./user-management.service";
```

Style verified against the existing lines: `export * from "./<name>";` — relative `./` path, no `@/`, no `../`, max one `/`, no imports (barrel-convention compliant per `backend.services/AGENTS.md` + backend instructions §Barrel Files Conventions). No name collisions: `assertActorAdmin`/`toAuditActionType` are unique repo-wide, so `export *` needs no aliasing, and the parent barrel `backend/services/index.ts:9` (`export * from "./admin"`) now re-surfaces them at `@/backend/services` automatically. Files in the barrel after edit: **12 lines** (8 header-comment + 3 exports + trailing blank).

## 3. Quality gates (all green)

| Gate | Result |
|---|---|
| `bun run scripts/health/sub-loop.ts backend/services/admin/index.ts --lifecycle duplicates` | **exit 0** (tsgo ✅, oxlint ✅, biome ✅, lint:type-aware ✅, jscpd duplicates ✅) |
| `bun run scripts/health/sub-loop.ts backend/services/admin/admin-gate.helpers.ts --lifecycle duplicates` (2.1.QL, tasks.md) | **exit 0** (same five steps ✅) |
| `bun run scripts/health/sub-loop.ts backend/services/admin/user-management.service.ts --lifecycle duplicates` (2.1.QL, tasks.md) | **exit 0** (same five steps ✅) |
| Clone check (2.1.QL "MUST NOT report a clone of the two functions anywhere else") | single-definition rg sweep (§1) → both symbols exist ONLY in `admin-gate.helpers.ts`; no clones |
| `bun run test/scripts/run-test.ts backend/services/admin/user-management.service.test.ts` | **61 pass / 0 fail**, 236 expect() calls, 3.54s — byte-green, unchanged (file untouched by this task) |
| `bun run test/scripts/run-test.ts backend/services/admin/user-management.chaos.test.ts` | **8 pass / 0 fail**, 46 expect() calls, 1.48s — byte-green, unchanged. NOTE: actual chaos filename is `user-management.chaos.test.ts` (dispatch brief's `user-management.service.chaos.test.ts` is a near-miss; Glob-verified this is the only `*.chaos.test.ts` under `backend/services/admin/`) |
| `bun tsgo` | **exit 0, 0 `error TS` lines** → delta vs baseline (0) = **0** |
| `git status --porcelain` | task delta = exactly `backend/services/admin/index.ts` (M). Pre-existing from earlier phases: `deferred-items.md` M, `tasks.md` M, `backend/types/audit/index.ts` M, `backend/types/audit/audit-trail.types.ts` + `outcome/` untracked |

## 4. Semantic review (2.1.SEC + 2.1.SR + 2.1.IV)

- **Gate semantics UNCHANGED** (read from the unmodified `admin-gate.helpers.ts`): `actorId === ANONYMOUS_ACTOR_ID (0)` → one `logger.logDomainError("…anonymous caller", {code:"UNAUTHORIZED", entity:"user", entityId})` + `UnauthorizedError` (:62–69); actor row missing → one `logDomainError(code:"FORBIDDEN")` + `ForbiddenError` (:71–79); `toUserRole(actor.role) !== UserRole.Admin` → one `logDomainError(code:"FORBIDDEN")` + `ForbiddenError` (:81–89). Exactly **one** bounded `logDomainError` per denial, ids+codes only (no PII).
- **Zero writes:** the gate's only await is `UserRepository.findById(actorId, outerTx)` (:71) — a read. No insert/update/delete anywhere in the module; `toAuditActionType` is pure (:25–44). Denials emit ZERO audit rows (JR-C-1 discipline documented at :50–52).
- **Verbatim extraction diff (2.1.SR):** nothing to diff — the extraction predates this task (DEV3-022c lineage) and the DEV3-016 suites (61+8 tests, byte-green) are the regression lock. No new dependencies; no dead exports (both symbols have live consumers AND the barrel export is the plan-mandated surface for Task 2.4/3.1); no cross-layer imports (helpers import repo/enum/lib/types/shared-locale — all within the service layer's permitted dependency set; barrel adds zero imports).
- **No plan-artifact references:** token sweep over `index.ts` + `admin-gate.helpers.ts` → zero `REQ-NNN`/`DEV3-020`/`Task N`/`D-N` tokens (the helpers header's DEV3-016 mention is a pre-existing cross-ticket reference from the landed extraction, not a DEV3-020 plan artifact; file untouched per IMPORT branch).
- **Instruction verification (2.1.IV):** validated against `.agents/instructions/backend.instructions.md` (barrel conventions: `export *`, `./` paths, no imports, shortest-path rule; i18n via `getServerTranslations`; typed errors; logger discipline) and `backend/services/AGENTS.md` (audit-emission rule — denials emit ZERO audit rows; duplication-elimination via shared helpers; no `.types.ts` in service layer — N/A). No `backend/services/admin/AGENTS.md` exists (checked).

## 5. Deviations / environment notes

- **Chaos test filename:** `user-management.chaos.test.ts`, not `user-management.service.chaos.test.ts` as written in the task text — run what exists (both DEV3-016 suites green).
- **Recurring stray `main` checkout:** HEAD flipped to `main` twice mid-task (once after the barrel edit's `git diff`, once before the final sweep) — same sandbox quirk Phase 1 documented. Both refs sit at the same commit (`a259524`), working tree unaffected; restored to the feature branch each time and verified stable. Agents in later phases: re-check `git branch --show-current` and restore if `main` appears.
- `admin-gate.helpers.ts` was **not** touched (extend-never-fork = import, don't rewrite). Its header comment ("Extracted VERBATIM from user-management.service.ts") remains accurate.

## 6. Carry-forward for Tasks 2.4 / 3.1 (and 2.2 authoring)

- **Canonical import path for the gate pair from Task 2.4's `AuditTrailService` (and any admin-domain consumer): `@/backend/services/admin`** (now the highest barrel carrying both symbols, per the shortest-import-path rule). Direct-file import `@/backend/services/admin/admin-gate.helpers` also works (that's what the pre-existing service/mappers use) but new code should prefer the barrel. Both are additionally re-exported at `@/backend/services` via the parent barrel.
- **Pipeline contract (tasks.md 2.4):** `await assertActorAdmin(actorId, locale, outerTx)` FIRST (pre-DB fail-closed — actorId=0 → `UnauthorizedError` pre-DB; non-admin → `ForbiddenError` pre-DB; each denial = one bounded `logDomainError`, zero writes, zero audit rows), then the service's own pre-DB filter/pagination validation, then the REPEATABLE READ tx containing `countEntries` + `listEntries`, then `toAuditActionType(row.actionType)` at map time — `null` → masked-internal plain `Error` (D6; precedent `user-management.service.ts:787-792` region as pinned by the plan, file now 446 lines).
- **Task 2.2 (journey test authoring):** denials to pin: student/parent actor → `ForbiddenError`; `actorId=0` → `UnauthorizedError`; both with `audit_logs` + `notifications` oracles byte-unchanged. The gate's exact denial shape (§4) is what the journey asserts.
- **`toAuditActionType` return-null path** must stay masked (never leak raw stored strings to GraphQL) — the helpers' fail-closed switch is the single coercion point; do NOT duplicate a coercion switch in the repo layer (D6: coercion is service-owned; repo rows stay raw `string`).
