# Task 3.1 Outcome — Pothos objects/input + `adminAuditLogs` query registration (DEV3-020)

**Plan directory:** `ai/plans/sprint_3/dev3-020-immutable-audit-logging-for-all-admin-ac`
**Task:** 3.1 (+ 3.1.QL / 3.1.TE / 3.1.SEC / 3.1.SR / 3.1.IV)
**Branch:** `feat/dev3-020-immutable-audit-logging-for-all-admin-ac` — HEAD was found flipped to `main` at task start AND after virtually every tool invocation (the recurring sandbox artifact, now much more aggressive: ~20+ restorations this task). Restored each time before every read/gate/run and re-verified at the end. The flip also HIDES the committed `outcome/` directory (it only exists in commit 89afbd6 on the feature branch), which initially looked like missing outcome inputs.
**Spec sources:** plan §3.1 (exact SDL), §3.2 (Pothos registration details), §3.3 (error mapping), §3.4 (permission matrix); tasks.md Phase 3 item 3.1; REQ-030, REQ-032, REQ-050, REQ-051, REQ-060, REQ-061; outcome files 2M (GO verdict), 2.4 (service signature + resolver carry-forward), 0.2 (enum/scalar anchors, surface-suite baseline).
**Files (final):** `backend/graphql/pothos/admin/audit-trail.pothos.ts` (89 lines, NEW), `backend/graphql/query/admin/audit-trail.query.ts` (89 lines, NEW), `backend/graphql/pothos/admin/index.ts` (+1 export line), `backend/graphql/query/admin/index.ts` (+1 side-effect import line).

---

## 1. Inheritance audit — prior interrupted agent's unverified files

The prior run left both new files + both barrels + the two test files + the regenerated SDL UNVERIFIED. This task audited every file in full against plan §3.2 / tasks.md 3.1 / the sibling precedents (`admin-users.query.ts`, `admin-user.pothos.ts`, `notification.mutation.ts:114-117` belt, `shared/enum.pothos.ts` registry, `shared/scalar.pothos.ts` DateTime).

| Check | Verdict |
|---|---|
| `AdminAuditLogEntry` via `objectRef<AdminAuditLogEntryReturnType>`; `t.exposeID("id")` FIRST (Apollo normalization, `id: ID!` per plan §3.1 — intentional upgrade over `AdminUserListItem`'s `exposeInt`, plan-normative) | **COMPLIANT** |
| `actionType: t.expose("actionType", { type: AuditActionTypePothosEnum })` — REUSED from `pothos/shared/enum.pothos.ts:112-114`, never re-registered (gate A2 discipline) | **COMPLIANT** |
| `actorId`/`actorName`/`entityType` exposed; `entityId`/`details` nullable; `createdAt: t.expose("createdAt", { type: "DateTime" })` (D4 — shared scalar, NO `toISOString()` hand-serialization, unlike the pre-DEV3-020 `AdminUserDetail` string fields) | **COMPLIANT** |
| `AdminAuditLogPage` embedded wrapper — `items` (`[AdminAuditLogEntry!]!` via `t.field` + pass-through resolve, byte-matching the `AdminUserPage` items style) + `totalCount`/`page`/`pageSize`, NO `id` | **COMPLIANT** |
| `AdminAuditLogFiltersInput` — closed six-member input, all `required: false`, enum member via reused `AuditActionTypePothosEnum`, `from`/`to` via `t.field({ type: "DateTime", required: false })`; field order matches plan §3.1 exactly (`actorId, actionType, entityType, entityId, from, to`); naming + `inputType(...)` style matches `AdminUserFiltersInput` | **COMPLIANT** |
| NO local type declarations — canonical `AdminAuditLogEntryReturnType`/`AdminAuditLogPageReturnType` imported from `@/backend/types` | **COMPLIANT** |
| Query: ONE `queryField("adminAuditLogs", …)`; `authScopes: { $all: { authenticated: true, role: [UserRole.Admin] } }` (the load-bearing conjunction, `admin-users.query.ts:74-79` precedent) | **COMPLIANT** |
| `ctx.user` belt with `UnauthorizedError((await ctx.t("errorsTranslations")).unauthorized)` (translated pattern per `notification.mutation.ts:114-117` — deliberately NOT the hardcoded English string in `admin-users.query.ts`; resolver-i18n rule) | **COMPLIANT** |
| EXPLICIT field-by-field filters copy into `AdminAuditTrailFiltersSubmitInput` shape — NO `{ ...input }` spread (BOPLA, REQ-032); null-normalized (`?? null` on all six) | **COMPLIANT** |
| `args.page ?? null`, `args.pageSize ?? null`, `ctx.locale`, `ctx.user.id` → `AuditTrailService.listAuditTrail(filters, page, pageSize, locale, actorId)` — outerTx omitted (resolver owns no tx; service opens the repeatable-read snapshot). NO try/catch, NO business logic, NO named exports | **COMPLIANT** |
| Barrels: `pothos/admin/index.ts` +1 `export * from "./audit-trail.pothos";`; `query/admin/index.ts` +1 `import "./audit-trail.query";` — one line each, side-effect barrel discipline | **COMPLIANT** |
| `backend/lib/gateway/public-operations.ts` UNTOUCHED (`git diff` empty) — the frozen six stay frozen | **COMPLIANT** |
| Plan-artifact tokens in the 4 new/edited files: grep for `REQ-`/`DEV3-020`/`Task 3`/`D4`/`plan.md`/`tasks.md`/`specs.md` → **0 hits** | **COMPLIANT** |

**Verdict: ZERO deviations found. Nothing to fix — the interrupted agent's implementation was spec-exact. The only outstanding work was the un-run pipeline (codegen completion, gates, suites, outcomes, checkboxes).**

## 2. Registration wiring (verified live)

- Objects register transitively through the resolver import chain: `gqlSchema.definitions.ts` → `query/` barrel → `query/admin/index.ts` → `audit-trail.query.ts` → `@/backend/graphql/pothos/admin` barrel → `audit-trail.pothos.ts`. ESM module identity guarantees single registration (top-level `pothos/index.ts` intentionally only carries `notifications`; admin objects follow the resolver-transitive convention, like the DEV3-016 admin objects).
- Duplicate-registration smoke: `bun run generate:gqlSchema` completed without duplicate-enum/duplicate-type errors and the schema-surface suite (which BUILDS the real schema) passed — `AuditActionType` remains a single enum registration; the schema's enum set is unchanged.

## 3. Gates

| Gate | Result |
|---|---|
| `bun run scripts/health/sub-loop.ts backend/graphql/pothos/admin/audit-trail.pothos.ts --lifecycle duplicates` | **exit 0** (tsgo ✅ oxlint ✅ biome:check ✅ lint:type-aware ✅ check:duplicates ✅) |
| `… sub-loop.ts backend/graphql/query/admin/audit-trail.query.ts --lifecycle duplicates` | **exit 0** (all five steps ✅) |
| `… sub-loop.ts backend/graphql/pothos/admin/index.ts --lifecycle duplicates` | **exit 0** |
| `… sub-loop.ts backend/graphql/query/admin/index.ts --lifecycle duplicates` | **exit 0** |
| `bun tsgo` | **exit 0, 0 errors project-wide** |
| Semantic review | thin resolver ✓; no local types ✓; enum REUSE not re-registration ✓; no dead code ✓; zero plan-artifact tokens ✓; no try/catch, no business logic ✓; `git status --porcelain` fully accounted (7 entries, all owned by 3.1/3.2) |

## 4. 3.1.SEC / 3.1.SR / 3.1.IV verification notes

- **SEC:** the `$all` conjunction is load-bearing — anonymous → `UNAUTHORIZED`, authenticated non-admin → `FORBIDDEN`, both pre-resolver (builder scope machinery, plan §3.3); the translated belt throw matches the `authenticated` scope's own message so the belt is invisible when the scope did its job. The input type is a closed six-member whitelist — smuggled identity args (`userId` root or in `filters`) die at GraphQL validation before a resolver runs (wire proof lands in 3.3). Scalar discipline: `DateTime` in/out only; no hand-rolled serialization anywhere in the new files.
- **SR:** resolver is a 5-line delegation; filters copy is explicit and null-normalized; no module-level mutable state; no cross-layer shortcuts (query → service only); no manual ReturnType construction.
- **IV:** validated against `backend/graphql/AGENTS.md` (id exposure for Apollo cache, locale propagation + translated direct throws, authScopes contract), `backend/graphql/query/AGENTS.md` (side-effect barrels, delegation, `@/` imports, file org), `backend/graphql/pothos/AGENTS.md` (scalar/enum registration patterns, canonical-type backing), and `docs/graphql/api-gateway-and-routing.md` §8 registration recipe — all five recipe rules satisfied (placement/side-effect barrel; pothos in domain dir backed by `backend/types` with `id` exposed; enum registered exactly once; authScopes on the operation; codegen run in the same change set). `AdminAuditLogPage` is a deliberate embedded value object (NO `id`) mirroring `AdminUserPage`/`NotificationListPage`; the frontend `keyFields:false` cache posture for the wrapper is the Phase-4 carry-forward documented in `0.2-prereq-verification-outcome.md` §6.
- **TE:** deferred to Task 3.3 per plan (wire matrix owns resolver behavior); the builder-level smoke (clean `generate:gqlSchema` + schema-surface build tier) ran green this task.

## 5. Deviations / environment notes

- Branch flips: found on `main` at start and repeatedly between every command; outcome directory transiently "missing" for the same reason. Restored each time; final state re-verified (see worklog entry for this task).
- No gate repairs were needed (contrast Task 2.2/2.4) — zero lint or type findings on the new files at first run.

## 6. Carry-forward

- **For Task 3.3:** the wire matrix imports the built schema (no live server); denial expectations: anonymous → `UNAUTHORIZED`, student/teacher/parent → `FORBIDDEN`, both PRE-resolver; smuggled identity args + invalid `actionType` literal → `GRAPHQL_VALIDATION_FAILED`; hostile `page`/`pageSize` → `VALIDATION` (service pre-DB); corrupt stored enum → masked `INTERNAL_SERVER_ERROR`.
- **For Task 4.x:** `AdminAuditLogEntry` rows are normalizable entities (`id: ID!`); `AdminAuditLogPage` wrapper needs the `NotificationListPage`-style `keyFields:false` posture if written to the cache, with the frozen apolloCache assertion extended additively.
