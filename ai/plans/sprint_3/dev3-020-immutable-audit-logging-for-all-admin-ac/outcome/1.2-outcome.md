# Task 1.2 Outcome — Immutability Trigger Verification (DEV3-020)

**Plan directory:** `ai/plans/sprint_3/dev3-020-immutable-audit-logging-for-all-admin-ac`
**Task:** 1.2 Immutability trigger verification
**Branch:** `feat/dev3-020-immutable-audit-logging-for-all-admin-ac` (restored from an errant `main` checkout at task start; both refs at commit `a259524`, zero content delta)

---

## 1. Branch taken

**VERIFY** — per Phase 0 verdict (`0.2-prereq-verification-outcome.md` §5): the trigger SQL is present, shipped, and LIVE. Consumed by reference.

- **NO new file created** (the drift branch stays closed — Task 0.2's verification stands; nothing in this task contradicted it).
- **NO changes under `backend/db/schema/**`** — `git diff --name-only backend/db/schema/` → empty (REQ-042 zero-drift gate holds).
- **`bun run db push` NOT run** (forbidden channel for this surface).

## 2. Files verified (read in full this task)

| File | Role | Verified content |
|---|---|---|
| `backend/db/migration/3-immutability-triggers.sql` | canonical source SQL | `audit_logs` coverage: `CREATE OR REPLACE FUNCTION prevent_audit_logs_update()` (:29–34) + `prevent_audit_logs_delete()` (:36–41), `DROP TRIGGER IF EXISTS` + `CREATE TRIGGER prevent_audit_logs_update_trigger` BEFORE UPDATE (:43–47) and `prevent_audit_logs_delete_trigger` BEFORE DELETE (:49–53). Same idempotent pattern for `student_payments` (:59–83) and `teacher_transaction` (:89–113). |
| `backend/drizzle/20260825222701_custom_3-immutability-triggers/migration.sql` | shipped drizzle copy | byte-equivalent DDL; deltas vs source are only `--> statement-breakpoint` markers and the `-- Source: 3-immutability-triggers.sql` header (Phase 0 diff; re-read this task) |

**Idempotence confirmation:** every trigger uses `CREATE OR REPLACE FUNCTION` + `DROP TRIGGER IF EXISTS` + `CREATE TRIGGER`; no `CONCURRENTLY` (per `docs/DATABASE_MIGRATIONS.md` — Drizzle's migrator is always transactional). Safe to re-run any number of times.

**Inline-comment check:** no inline `--` comments inside any SQL body — the `$$ … $$` plpgsql function bodies contain zero comments (the parameter-binding hazard class applies to TS `sql` template tags, and would equally break these function bodies; both are clean). File-level `--` header/section comments are outside any statement body.

## 3. Environment applied to (MUST-RECORD)

- **Environment: migrate-provisioned dev DB `app_db`** — userspace PostgreSQL 17.11 cluster (`/home/z/pg`, socket `/home/z/pgsocket`, port 5432), `.env` `DB_PROVIDER=postgres`, `DATABASE_URL=postgresql://postgres@127.0.0.1:5432/app_db`.
- **Applied via `bun run db migrate`** during environment setup (worklog Task `setup`); re-verified live this task via the Drizzle journal:
  `SELECT name, applied_at FROM drizzle.__drizzle_migrations` → `20260825222701_custom_3-immutability-triggers | 2026-09-01 14:17:03.838641+00` (6 journal entries total = all 6 migration folders).

## 4. Live trigger probe (re-run this task)

`SELECT tgname, tgenabled FROM pg_trigger WHERE tgrelid='audit_logs'::regclass AND NOT tgisinternal` on `app_db`:

| tgname | tgenabled |
|---|---|
| **`prevent_audit_logs_delete_trigger`** | `O` (enabled) |
| **`prevent_audit_logs_update_trigger`** | `O` (enabled) |

The two live trigger names on `audit_logs` are exactly **`prevent_audit_logs_update_trigger`** and **`prevent_audit_logs_delete_trigger`**.

**Behavioral probe note (honest scope):** `audit_logs` currently holds **0 rows** in the dev DB (count probed), so a live UPDATE/DELETE attempt cannot exercise the row-level triggers here; direct invocation of the trigger functions is rejected by PostgreSQL ("trigger functions can only be called as triggers"). The **behavioral pin is Task 2.5's trigger-tier test** (`pg_trigger` probe + `expectRepoError` try/catch attempts) per tasks.md 1.2.TE — this task's tier is structural verification (triggers exist, enabled, journal-applied), which PASSES.

## 5. Quality gates

| Gate | Result |
|---|---|
| Sub-loop on a NEW migration file | **N/A** — VERIFY branch creates/modifies no SQL file; nothing to gate. For completeness the gate was run against the verified (unmodified) `backend/db/migration/3-immutability-triggers.sql`: tsgo step ✅, oxlint step `No files found to lint` → exit 1 = the known non-lintable-extension path artifact (same class as `.md` files, Phase 0 baseline §4.4); not a content violation. |
| `bun tsgo` | exit 0, 0 errors (delta 0) — shared with Task 1.1 run |
| `git status --porcelain` | zero delta from this task (no files created/modified — VERIFY = verify-and-record only) |
| Semantic review | idempotent (re-runnable); matches `docs/DATABASE_MIGRATIONS.md` conventions (custom SQL in `backend/db/migration/`, shipped drizzle folder, applied via `bun db migrate`, no `CONCURRENTLY`); zero Drizzle schema drift |
| Security & tenancy | audit surface coverage = UPDATE + DELETE on `audit_logs` only; both trigger functions `RAISE EXCEPTION` unconditionally (no conditional paths, no writes); the same file also covers the pre-existing `student_payments`/`teacher_transaction` ledgers (untouched, not modified by this ticket); no other table touched; INSERT unaffected |

## 6. Carry-forward for later tasks

- Task 2.5(c) pins migration-DDL idempotence by asserting the shipped file contains `CREATE OR REPLACE` + `DROP TRIGGER IF EXISTS` — both confirmed present in `backend/drizzle/20260825222701_custom_3-immutability-triggers/migration.sql`.
- Task 2.5's `withAuditDeleteTriggersSuspended` helper (`test/helpers/db-cleanup.ts:83–109`) discovers these triggers via the same `pg_trigger` probe — trigger names are stable and verified.
- Push-provisioned environments never apply custom SQL triggers (D-TRIGGER-PUSH-GAP, ledger) — ops runbook path remains documented in the canonical doc (REQ-080), owned by a later ticket.
