# Task 4.2 Outcome — Frontend GraphQL documents + Apollo cache policy + contract tests (DEV3-020)

**Plan directory:** `ai/plans/sprint_3/dev3-020-immutable-audit-logging-for-all-admin-ac`
**Task:** 4.2 (+ 4.2.QL / 4.2.TE / 4.2.SR / 4.2.IV)
**Branch:** `feat/dev3-020-immutable-audit-logging-for-all-admin-ac` (recurring flip-to-`main` sandbox artifact struck between tool calls; restored with a check-and-heal guard before every command batch, re-verified at the end)
**Spec sources:** tasks.md Phase 4 item 4.2; plan §5 (document shape + cache registration bullet); REQ-063, REQ-074; `frontend/graphql/AGENTS.md` (TypedDocumentNode pattern, embedded-type policy, no-useLazyQuery); `frontend/graphql/sharedDocuments/AGENTS.md` (naming table, barrel pattern); `.agents/instructions/frontend.instructions.md` (Apollo import pattern); contract precedent `frontend/graphql/sharedDocuments/documents.contract.test.ts` + sibling `notification.documents.test.ts`.
**Files (final):** CREATE `frontend/graphql/sharedDocuments/admin/audit-trail.documents.ts` + colocated `audit-trail.documents.test.ts`; EDIT `frontend/graphql/sharedDocuments/admin/index.ts` (+1 export), `frontend/providers/apollo/apolloCache.ts` (+policy), `frontend/providers/apollo/apolloCache.test.ts` (frozen inventory 5→7 + dedicated keyFields pin), `frontend/graphql/AGENTS.md` (+1 embedded-type row), `frontend/graphql/generated/gql/graphql.ts` (codegen, +23 lines). `frontend/graphql/sharedDocuments/index.ts` verified UNTOUCHED (line 1 already `export * from "./admin"`).

---

## 1. Document shape (`audit-trail.documents.ts`)

Single exported document, byte-faithful to plan §5's wire shape:

```graphql
query AdminAuditLogs($filters: AdminAuditLogFiltersInput, $page: Int, $pageSize: Int) {
  adminAuditLogs(filters: $filters, page: $page, pageSize: $pageSize) {
    items { id actionType actorId actorName entityType entityId details createdAt }
    totalCount
    page
    pageSize
  }
}
```

- **Export:** `adminAuditLogsQueryDocument` — typed `TypedDocumentNode<AdminAuditLogsQuery, AdminAuditLogsQueryVariables>` (both types imported type-only from `@/frontend/graphql/generated/gql/graphql`); `gql` + `type TypedDocumentNode` imported from `"@apollo/client"` (never `@apollo/client/core`).
- **Entry selection:** `id` FIRST, then the seven remaining scalar/enum fields in schema order (`actionType`, `actorId`, `actorName`, `entityType`, `entityId`, `details`, `createdAt`) — matches `AdminAuditLogEntry`'s 8 fields exactly; the row is scalar-only (no nested objects).
- **Wrapper selection:** `items` FIRST, then `totalCount`/`page`/`pageSize` — matches `AdminAuditLogPage` (4 fields); the wrapper selects NO `id` (embedded value type).
- **Header comment** documents the AGENTS.md discipline (id-first, wrapper embedded, `useQuery` from `@apollo/client/react` — NO `useLazyQuery`, typed variables). Zero plan-artifact tokens (grep sweep: 0 hits for REQ-/task-id/plan-path tokens across all new/edited files).
- The document layer itself declares no hooks — consumption is `useQuery(adminAuditLogsQueryDocument, { variables: { filters, page, pageSize } })` downstream (Task 4.4), per the sibling `adminUsersQueryDocument` pattern.

## 2. Barrel wiring

- `frontend/graphql/sharedDocuments/admin/index.ts`: `export * from "./audit-trail.documents";` added below the admin-users line — barrel comment discipline unchanged.
- Top barrel `frontend/graphql/sharedDocuments/index.ts:1` already `export * from "./admin";` — VERIFIED, no change (deep import and top-barrel paths resolve to the identical instance; pinned by the new suite's parity test).

## 3. Apollo cache policy + frozen-inventory reconciliation

- `frontend/providers/apollo/apolloCache.ts`: `typePolicies` gains `AdminAuditLogPage: { keyFields: false }` (grouped with the sibling pagination wrapper `NotificationListPage`, with an explanatory comment: the normalizable entities are the `AdminAuditLogEntry` rows inside `items`, each carries `id`; the wrapper never needs an identity). `AdminAuditLogEntry` itself normalizes by `id` naturally — NO registration. File header comment updated to include the sixth embedded type.
- `frontend/providers/apollo/apolloCache.test.ts` frozen policy-inventory assertion (was ~:176-185) extended ADDITIVELY from the stale five-key literal to the LIVE seven-key set — reconciling the known `NotificationListPage` drift (cache registered six, test pinned five) without subtracting anything:

```ts
test("policy surface is FROZEN to the seven documented entries", () => {
  const cache = createApolloCache();
  expect(Object.keys(typePoliciesOf(cache)).toSorted((a, b) => a.localeCompare(b))).toEqual([
    "AdminAuditLogPage",
    "AdminDashboardScheduleResult",
    "AdminNoteInfo",
    "HandshakeCodeLookup",
    "HealthCheck",
    "NotificationListPage",
    "OnlineMeetingInfo",
  ]);
});
```

- Added a dedicated `typePolicies.AdminAuditLogPage.keyFields === false` config-exposure test (mirrors the HealthCheck/HandshakeCodeLookup siblings); the sibling-regression pins (`AdminNoteInfo`/`OnlineMeetingInfo` keyFields, `AdminDashboardScheduleResult.rows` merge:false) and both behavioral proof blocks pass UNCHANGED; the suite header's "five-entry" wording updated to "seven-entry".

## 4. Contract test pins (`audit-trail.documents.test.ts`)

Colocated pure suite modeled on `documents.contract.test.ts` + `notification.documents.test.ts` (assertion-free graphql kind-guard AST helpers, no unsafe assertions, zero server/DB/network). 8 tests:

1. **Named operation + channel + variables** — single query operation named `AdminAuditLogs`; declared variable set EXACTLY `["filters", "page", "pageSize"]` in source order.
2. **Argument wiring** — every declared variable threaded into the root-field argument (`$filters`/`$page`/`$pageSize` → `filters`/`page`/`pageSize`); no dead variables, no literal arguments.
3. **Declared variable types** — `filters: AdminAuditLogFiltersInput`, `page: Int`, `pageSize: Int` (plain NamedTypes; the set is closed — any extra definition throws).
4. **Entry row** — `adminAuditLogs.items` selects exactly the eight-field scalar row with `id` FIRST.
5. **Scalar-only row** — no entry field carries a nested selection set (a future object addition must bring its own `id` + pin).
6. **Wrapper** — `items` first; full set `{items, page, pageSize, totalCount}`; NO `id` (embedded `keyFields: false` value type).
7. **Barrel parity** — top-barrel re-export `===` deep import (cache-key safety).
8. **Codegen binding** — compile-time proof by assignment: `TypedDocumentNode<AdminAuditLogsQuery, AdminAuditLogsQueryVariables> = adminAuditLogsQueryDocument`.

## 5. Codegen verification

| Artifact | Result |
|---|---|
| `bun run generate:gqlSchema` | exit 0; `schema.graphql` byte-stable at 11832 bytes (the Task 3.1/3.2 regeneration — no drift) |
| `bun codegen` | exit 0; `frontend/graphql/generated/gql/graphql.ts` +23 lines, PURELY ADDITIVE: `AdminAuditLogFiltersInput`, compact extracted types `AdminAuditLogsQuery_adminAuditLogs` / `AdminAuditLogsQuery_adminAuditLogs_items`, `AdminAuditLogsQuery`, `AdminAuditLogsQueryVariables` (`Exact<{ filters: AdminAuditLogFiltersInput \| null \| undefined; page: number \| null \| undefined; pageSize: number \| null \| undefined }>`), and the typed `AdminAuditLogsDocument` TypedDocumentNode — exactly the operation-scoped growth predicted in `3.2-outcome.md` §1 |

## 6. Gates

| Gate | Result |
|---|---|
| `bun run test/scripts/run-test.ts frontend/graphql/sharedDocuments/admin/audit-trail.documents.test.ts` | **8 pass / 0 fail / 32 expect()** |
| `bun run test/scripts/run-test.ts frontend/providers/apollo/apolloCache.test.ts` | **11 pass / 0 fail / 24 expect()** |
| Sibling regression (run after the barrel/policy edits): `documents.contract.test.ts` 13/0/67, `notification.documents.test.ts` 12/0/59 | **all green, unchanged** |
| sub-loop `--lifecycle duplicates` on each new/edited .ts file (5 files: both audit-trail files, admin/index.ts, apolloCache.ts, apolloCache.test.ts) | **exit 0 ×5** |
| `bun tsgo` | **exit 0, 0 errors project-wide** |
| `git status --porcelain` | only Task-4.2-owned entries + concurrently-owned Phase-4 files (Task 4.1 `shared/locale/adminUsers*` + parity suite, Task 4.5 `navItems.test.ts` + its tasks.md checkbox flip + `outcome/4.5-outcome.md`) — zero unexplained entries; `schema.graphql` byte-stable (no SDL drift) |

One gate repair during the quality loop: the sub-loop's type-aware lint flagged `expectedTypes[name] === undefined` (`sonarjs/different-types-comparison` — a `Record<string,string>` index is statically non-undefined) in the variable-types test; fixed file-locally by switching the lookup to a `ReadonlyMap<string, string>` (`.get()` → honestly `string \| undefined`), re-run → exit 0.

## 7. 4.2.SR / 4.2.IV verification notes

- **SR:** zero bespoke error mapping in the documents layer (error seams remain `mapGraphQLErrorByCode` / `PermissionDeniedFallback` / `RetryableNotice` for Task 4.4 to consume); ALL types are codegen-generated (`AdminAuditLogsQuery`/`AdminAuditLogsQueryVariables` imported from the single `graphql.ts`) — no inline type literals, no indexed-access workarounds, no schema-level object types; no mapping layers.
- **IV:** validated against `frontend/graphql/AGENTS.md` (TypedDocumentNode typing pattern ✓, document constant naming `<entityName>QueryDocument` ✓, `id`-first ✓, no `useLazyQuery` ✓, codegen after document changes ✓, embedded-type list updated ✓) and `.agents/instructions/frontend.instructions.md` (`gql`/`TypedDocumentNode` from `"@apollo/client"` ✓, generated-type import path ✓, naming table ✓).

## 8. Carry-forward for Task 4.4 (AuditTrailView)

- **Document export:** `adminAuditLogsQueryDocument` — import via the top barrel (`import { adminAuditLogsQueryDocument } from "@/frontend/graphql/sharedDocuments";`) or deep path `@/frontend/graphql/sharedDocuments/admin/audit-trail.documents` (identical instance).
- **Hook pattern:** `useQuery(adminAuditLogsQueryDocument, { variables: { filters, page, pageSize } })` — hooks from `"@apollo/client/react"`; stateful `useQuery` ONLY (no `useLazyQuery`). Variables are all optional; omit `filters`/`page`/`pageSize` for the unfiltered first page (service defaults apply, echo comes back in `page`/`pageSize`/`totalCount`).
- **Generated type names (from `frontend/graphql/generated/gql/graphql.ts`):** `AdminAuditLogsQuery`, `AdminAuditLogsQueryVariables`, `AdminAuditLogsQuery_adminAuditLogs` (wrapper), `AdminAuditLogsQuery_adminAuditLogs_items` (row), `AdminAuditLogFiltersInput` (six nullable members: `actionType: AuditActionType`, `actorId: Int`, `entityId: Int`, `entityType: String`, `from: DateTime`, `to: DateTime` — all string-typed, `DateTime` maps to `string`), wire enum `AuditActionType` (seven GraphQL wire-name members). Row fields: `id: string`, `actionType: AuditActionType`, `actorId: number`, `actorName: string`, `entityType: string`, `entityId: number | null`, `details: string | null`, `createdAt: string`.
- **Cache:** `AdminAuditLogEntry` rows normalize by `id`; the `AdminAuditLogPage` wrapper is registered `keyFields: false` — filter/pagination rewrites replace the wrapper inline with no cache-data-loss warnings; no `fetchPolicy` work expected at the view layer.

## 9. Deviations / environment notes

- Branch flips (recurring sandbox artifact): HEAD was found on `main` between tool calls; a check-and-heal guard (`git branch --show-current | grep -q feat/dev3-020 || git checkout …`) prefixed every command batch; all edits/tests/gates ran on the feature branch and the final state was re-verified.
- `frontend/graphql/sharedDocuments/AGENTS.md`'s Layout tree does not list the `admin/` sub-directory (pre-existing drift from DEV3-016's document addition, outside this task's file set) — reported for a future hygiene pass, not fixed here.
- Concurrent Phase-4 subagents were active in the same working tree (Task 4.1 locale files, Task 4.5 nav pin); their files were left strictly untouched.
