# Task 4.5 — Navigation Retarget Pinning Outcome

**Task:** 4.5 Navigation retarget pinning (ZERO nav-model change — D12) · **Requirements:** REQ-065
**Branch:** `feat/dev3-020-immutable-audit-logging-for-all-admin-ac` (clean-tree method; branch re-verified/restored before EVERY command batch — the flip-to-main artifact persisted throughout this task)
**Method:** verify-then-pin — nav model audited live, then the existing test file extended minimally. No production file touched.

---

## 1. Zero-change proof (nav model untouched)

| Item | Live finding | Verdict |
|---|---|---|
| Actual navItems path | `frontend/views/dashboard/nav/navItems.ts` (NOT `frontend/views/dashboard/navItems.ts` — the plan's anchor drift, already resolved by Task 0.2) | CONFIRMED |
| Admin `/audit` item | `navItems.ts:133` — `{ route: "/audit", labelKey: "audit", Icon: AuditIcon }` inside `[UserRole.Admin]` (:126–135); icon alias `AssessmentOutlined as AuditIcon` at :7 | CONFIRMED — pre-existing, untouched |
| `git diff` for the nav model | `git status --porcelain frontend/views/dashboard/nav/` shows ONLY the test file; `git diff main..HEAD -- frontend/views/dashboard/nav/` is EMPTY (file identical on both branches) | **ZERO nav-model change holds** |
| Route serving today | `app/(dashboard)/audit/page.tsx` ABSENT at task start → `/audit` still falls through to `app/(dashboard)/[feature]/page.tsx` (`ComingSoonView`); Tasks 4.3/4.4 shipping the page IS the retarget | CONFIRMED (matches plan §5 / Task 0.2 item 5) |
| `audit` label-key ownership | `DashboardLabels` ONLY: `shared/locale/types/dashboard/index.ts:41`, `shared/locale/en/dashboard/index.ts:16` ("Audit"), `shared/locale/ar/dashboard/index.ts:16` ("سجل التدقيق"); ABSENT from `HandshakeCodeLabels` | CONFIRMED — no label move |

## 2. Pins added to `frontend/views/dashboard/nav/navItems.test.ts`

Extended the EXISTING file (+5 tests / +10 expects; nothing duplicated — the ownership-matrix test at :46–59 was left untouched and stays green):

New `describe("Admin audit navigation (REQ-065)")`:

1. **Admin nav CONTAINS `/audit` with `labelKey: "audit"`** — plus an exactly-one guard (`filter(route === "/audit")` → `toHaveLength(1)`) pinning "no duplicate nav item" (4.5.SR) at test level.
2. **`audit` stays owned by `DashboardLabels`** — runtime pin: `"audit" in dashboardEn` → true, `"audit" in handshakeCodeEn` → false (the targeted counterpart of the generic ownership-matrix sweep; pins "no label move").
3–5. **`test.each` Student/Teacher/Parent navs EXCLUDE `/audit`** — mirrors the sibling `/admin/plans` exclusion pin.

Header doc block gained a REQ-065 bullet (production language only; zero plan artifacts — requirement IDs only, consistent with the file's existing convention).

## 3. Gate results

| Gate | Command | Result |
|---|---|---|
| Test suite | `bun run test/scripts/run-test.ts frontend/views/dashboard/nav/navItems.test.ts` | **20 pass / 0 fail / 116 expect()** (baseline 15 / 0 / 106 → +5 / +10) |
| Quality loop | `bun run scripts/health/sub-loop.ts frontend/views/dashboard/nav/navItems.test.ts --lifecycle duplicates` | **exit 0** — tsgo ✅ oxlint ✅ biome ✅ lint:type-aware ✅; check:duplicates skipped (file outside jscpd scan scope → pass) |
| Type check | `bun tsgo` | **exit 0, 0 errors project-wide** |
| Scope | `git status --porcelain` | This task's footprint: `frontend/views/dashboard/nav/navItems.test.ts` + bookkeeping (tasks.md checkbox, this outcome). Concurrent in-flight files from sibling Phase-4 tasks visible and UNTOUCHED (Task 4.1: `shared/locale/{types,en,ar}/adminUsers`; Task 4.2: `frontend/graphql/sharedDocuments/admin/index.ts`, `frontend/graphql/generated/gql/graphql.ts`, `frontend/providers/apollo/apolloCache{,.test}.ts`, `frontend/graphql/AGENTS.md`) |

## 4. Semantic review (4.5.SR) & instruction verification (4.5.IV)

- **No duplicate nav item:** pinned (`toHaveLength(1)` on the `/audit` route in the admin nav).
- **No label move:** pinned (`audit` resolves from the dashboard bundle only); `resolveNavItemLabel` paths untouched.
- **No bottom-nav assumptions:** assertions cover route + labelKey only; rendering stays `DashboardSidebar` (mobile = Drawer) — untouched by design.
- **Instruction files (auto-discovered by the sub-loop):** `.github/instructions/tests.instructions.md`, `.github/instructions/frontend.instructions.md`, root `AGENTS.md`, `frontend/AGENTS.md` — translation discipline held (only technical keys/routes as literals; labels asserted via namespace handles, never hardcoded copy); `bun:test` conventions and import order unchanged.
- **Zero plan artifacts** in the added text (no task IDs, no plan slugs; REQ-065 is a product requirement).

## Stage Summary

- Task 4.5 COMPLETE: nav model byte-unchanged (`navItems.ts` identical on both branches); retarget pins landed in `navItems.test.ts` — admin nav carries exactly one `/audit` entry with `labelKey: "audit"`, student/teacher/parent navs exclude it, `audit` stays owned by `DashboardLabels`. All gates exit 0.
- tasks.md 4.5 **main** checkbox flipped → `[x]` ONLY (4.5.QL/SR/IV sub-checkboxes intentionally left for orchestrator verification, per instruction).
- NEXT: Tasks 4.3/4.4 ship `app/(dashboard)/audit/page.tsx` + `AuditTrailView` — that act completes the retarget by making the pinned route resolve to the real page instead of the `[feature]` catch-all.
