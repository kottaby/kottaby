# Task 7.1 Outcome — Canonical reference doc `docs/admin/audit-trail.md` (CREATE)

**Plan directory:** `ai/plans/sprint_3/dev3-020-immutable-audit-logging-for-all-admin-ac`
**Task:** 7.1 — CREATE `docs/admin/audit-trail.md` (canonical reference) — Task ID 7-a, Phase 7
**Branch:** `feat/dev3-020-immutable-audit-logging-for-all-admin-ac` — the recurring flip-to-`main` sandbox artifact struck repeatedly this session (HEAD found on `main` at effectively every tool-batch boundary); a check-and-heal checkout was prefixed to every batch and all edits verified on the feature branch. All four edited AGENTS.md files are byte-identical between `main` and the feature branch (verified ref-to-ref before editing), so working-tree edits rode the flips safely; `worklog.md` likewise 0-diff.
**Doc path (REQ-080 deliverable):** `docs/admin/audit-trail.md` — CREATED, 11 sections, ~150 lines, production language.

---

## 1. Doc section checklist (all mandatory sections present)

| # | Mandated section | Where | Key content |
|---|---|---|---|
| 1 | **WHY** (FR-10.5 / Workflow 05 §7 / PRODUCTION_READINESS §1.3.1–1.3.5 mapping) | §1 | FR-10.5 (permanent logging: actor/action/entity/timestamp; resolved as `audit_logs` + `audit_action_type`, decision A.5); Workflow 05 §7 (field set + audited-action catalog); PRODUCTION_READINESS §1.3.1–1.3.5 (each launch-gate row mapped: logged / immutable / field-complete / filterable / all 7 action values exercised). Repo's own planning docs referenced by path — sanctioned; zero tokens from THIS plan (no DEV3-020/REQ-NNN/task-ID anywhere in the doc). |
| 2 | **Read-surface contract** | §2 | Wire signature + entry/envelope shapes (illustrative, marked NON-authoritative, canonical paths cited); `id`-first `ID!`, nullable `entityId`/`details` by design, live `actorName` projection (NOT NULL RESTRICT join); `createdAt DESC, id DESC` deterministic tiebreak (same-tx batch rows, insertion-latest, no page dup/drop; offset posture + keyset recorded as future refinement); pagination (page ≥ 1, pageSize 1..100 default 25, pre-DB validation, ONE repeatable-read snapshot tx pairing count+list, out-of-range → empty items + honest totalCount, never clamped); closed six-member ANDed filter whitelist, field-by-field resolver copy (no spread), absent-member fallback to unfiltered, malformed → VALIDATION pre-DB; id range 1..2147483647; entityType trim/non-empty/≤100; **half-open window** `created_at >= from AND created_at < to` (boundary at `from` in, at `to` out, adjacent windows disjoint); **client UTC-day convention** (from = start-day UTC midnight, to = EXCLUSIVE midnight after end day; same-day pair valid = exactly that day; strict `YYYY-MM-DD`, no rollover). |
| 3 | **Two-tier immutability proof** + push-vs-migrate caveat | §3 | Tier 1 application single-writer (static scan: zero Drizzle/raw-SQL mutators, `AuditService` surface locked to `createAuditLog`, bijective teardown allowlist) + DDL pin; Tier 2 DB triggers (`prevent_audit_logs_update_trigger` / `prevent_audit_logs_delete_trigger`, BEFORE, RAISE EXCEPTION 'audit_logs is immutable — …', idempotent SQL, append path open, behavioral tamper-throw proof). **Caveat documented honestly:** triggers ship via custom SQL migration (journal `20260825222701_custom_3-immutability-triggers`); push-provisioned DBs never apply custom SQL → table without trigger tier; rollout rule = provision via migrate path + verify with `pg_trigger` probe. **Verified branch recorded:** migrate-provisioned dev DB, BOTH triggers live and enabled (`tgenabled = 'O'`), tamper attempts observed to throw. Honest residual: raw-DB-credential bypass (environment-tier, not app-fixable). |
| 4 | **Governance-window acknowledgment** | §4 | Zero governance filtering by design; governed targets' history remains visible; window is ACKNOWLEDGED not expanded — no request-time re-check, no fail-closed claim for pre-issued tokens; filters are DATA, never authorization inputs. |
| 5 | **History-survives-governance rule** + invariant references | §5 | INV-U1 / INV-U5 (`docs/specs/state-machine-invariants.md`), Workflow 05 §8 no-hard-deletes; append-only + NOT NULL RESTRICT actor FK → audit history outlives every governance state change. |
| 6 | **Details-hygiene consumption note** | §6 | Read layer = VERBATIM pass-through (never parses/truncates/re-formats; escaped text in `dir="auto"` pre; em-dash null placeholders); hygiene WRITER-enforced (≤2000 chars truncated before insert, names-only, never PII/credentials/passwordHash/email pairs); consumers must not re-sanitize nor query on details. |
| 7 | **Deep-link contract** | §7 | `/audit?entityType=…&entityId=…` + optional `actionType`/`actorId`/`from`/`to`; per-parameter independent validation with silent-drop posture (never reflected, never an error); repeated params → first value; id range 1..2147483647 (wire Int max); enum fail-closed; strict UTC days, inverted pair dropped whole, same-day pair valid; survivors = `initialFilters` seed, view owns later state, view never rewrites URL; `withPageAuth` admin page gate. |
| 8 | **Anti-pattern list** | §8 | NEVER add update/delete/edit surface to `audit_logs`; NEVER LIKE-search `details`; NEVER fork a second audit writer; NEVER filter history by governance; NEVER add a second enum registration; (+ bonus: NEVER read the trail without the `$all` conjunction). |
| 9 | **Architecture map** | §9 | One-line responsibility per module: enum, types, schema, repository, service, gate helpers, writer, pothos, query, documents, apolloCache, route, and all 8 view files — full table, paths authoritative. |
| + | Test locks + Related documents | §10–11 | All 9 suites pinned by path; related-doc list (user-management write half, Workflow 05, FR/specs/readiness, error contract, with-page-auth, DATABASE_MIGRATIONS for the migrate-vs-push caveat). |

## 2. Discipline verification

- **Zero plan-artifact tokens in the doc**: grepped the created file for `DEV3-020`, `REQ-[0-9]`, `Task [0-9]`, `D-TRIGGER-PUSH-GAP`, `tasks.md`, `specs.md`, `plans/sprint`, bare `§` + digit from THIS plan → **0 hits**. Sanctioned repo-artifact references used instead: FR-10.5, Workflow 05 §7/§8, PRODUCTION_READINESS §1.3.x, A.5, INV-U1/U5 — all pre-existing repo docs.
- Doc conventions mirror `docs/admin/user-management.md` (metadata header, canonical-reference framing, ILLUSTRATIVE/NON-authoritative code blocks, authoritative paths cited per section, What-NOT-to-do list, test locks, related documents).

## 3. Gates

| Gate | Expected | Observed |
|---|---|---|
| `bun run scripts/health/sub-loop.ts docs/admin/audit-trail.md --lifecycle duplicates` | Known artifact on `.md`: exit 1 "No files found to lint" → SKIP per dispatch | **Reproduced**: "Finished … on 0 files" → true exit 1 (captured without pipe). Sanctioned replacement gate applied. |
| `bun run lint` (sanctioned replacement) | exit 0 | **exit 0** |
| `bun tsgo` | unchanged, 0 | **exit 0** (no TS changes in this task — doc is `.md` only) |

## 4. Files changed by this task

- `docs/admin/audit-trail.md` — CREATED (the only 7.1 deliverable). No source code changes.

## 5. Verdict

**Task 7.1 COMPLETE.** All 9 mandated sections present and evidence-backed against the R6-verified implementation facts (trigger branch, pipeline, gates, review rounds); the two forward-referencing ledger/attack-review notes (push-vs-migrate caveat, governance-window acknowledgment) are now carried by the canonical doc as required. Gates: lint 0 / tsgo 0 / sub-loop artifact documented. Ready for 7.2 (same Task ID 7-a batch) and the 7.3 final synthesis.
