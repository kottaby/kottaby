# Task 2.5 Outcome — Immutability Proof Triple (static scan + trigger tier + DDL pin)

**Plan directory:** `ai/plans/sprint_3/dev3-020-immutable-audit-logging-for-all-admin-ac`
**Task:** 2.5 Immutability proof triple
**Branch:** `feat/dev3-020-immutable-audit-logging-for-all-admin-ac` (found flipped to `main` twice during the task — the known stray-checkout artifact; restored both times, zero content impact since both refs point at the same commit and all work is uncommitted working-tree state)
**Artifact:** `backend/db/test/logic/audit/audit-immutability.test.ts` (NEW — 17 tests, 316 expect() calls)

---

## 1. TRIGGER-TIER BRANCH EXECUTED (MUST-RECORD): TRIGGERS PRESENT → both mutation attempts THREW

- **Environment = real PostgreSQL, NOT pglite.** `.env.test` defines no `DB_PROVIDER`, `.env` line 1 = `DB_PROVIDER=postgres` → `isPgliteProvider()` returned **false** → `describeTriggerTier = describe` (the tier RAN — it was not skipped).
- **Pre-run catalog probe** (`psql` on `app_db`): `prevent_audit_logs_delete_trigger|O` + `prevent_audit_logs_update_trigger|O` — both installed and enabled.
- **In-test branch gate:** `requireEnforcingTrigger()` probed `pg_trigger` via the `db-cleanup` probe shape (`SELECT tgname, tgenabled FROM pg_trigger WHERE tgrelid = 'audit_logs'::regclass AND NOT tgisinternal`) inside each `runInRollback` tx; both triggers were found present and enabled → the **behavioral branch** executed for both tests:
  - `direct UPDATE on an audit row is rejected by the immutability trigger` — PASS (fixture row INSERTed in-tx, read back, then `tx.update(auditLogs).set(...)` threw; the full error-message chain contains `audit_logs is immutable`).
  - `direct DELETE of an audit row is rejected by the immutability trigger` — PASS (`tx.delete(auditLogs)` threw with the same enforced message).
  - `the append path stays open` — PASS (INSERT + read-back succeeds; over-blocking control proving the BEFORE UPDATE/DELETE triggers never touch INSERTs).
- The push-provisioned structural branch (`expect(scanAllAuditMutationShapes(productionSources)).toEqual([])`) exists but did NOT execute in this environment.

## 2. Static-scan tier results

Corpus (computed with the test's own walk): backend tree walked = **345** `.ts`/`.tsx` files; **production corpus = 278** (67 excluded as in-backend test layers); repo-root `test/` teardown corpus = **55** files.

- **Exclusions (path-exact, never globs):** test-layer directories `backend/db/test`, `backend/graphql/test`, `backend/lib/api/test`, `backend/lib/errors/test`, `backend/db/repo/students/__tests__`, `backend/services/auth/__tests__`, `backend/services/students/__tests__` (all rollback-contained test code per `backend/db/test/AGENTS.md`), plus co-located `*.test.ts(x)` files (handshake-scan precedent). Self-exclusion is asserted (the scan never scans its own file, which contains mutation shapes by design).
- **Anti-blind-spot guards:** production corpus ≥ 250; sentinel files asserted present in the corpus: `audit.service.ts`, `admin-user.repository.ts`, `audit-logs.ts` (schema), `admin-user.pothos.ts` (GraphQL read surface). All backend production roots are walked (no per-directory allowlist needed — the whole tree is scanned).
- **Result: ZERO violations.** Zero Drizzle `update(auditLogs)`/`delete(auditLogs)` callsites and zero raw-SQL `UPDATE audit_logs`/`DELETE FROM audit_logs` statements across all 278 production sources.
- **AuditService module-surface lock:** `Object.keys(AuditService)` = exactly `["createAuditLog"]` — no mutation method beyond the append-only writer.
- **Teardown allowlist (path-exact, bijective):** `test/helpers/db-cleanup.ts` + `test/workflows/helpers/journey-cleanup.ts` — the ONLY `test/`-tree files matching the scanners, and every allowlist entry matches (live-match assertion: no dead entries, no unlisted mutators; `journey-cleanup.ts:78` `db.delete(auditLogs)` and the two raw `DELETE FROM audit_logs` statements in both helpers are the live positive controls). A fourth test proves no allowlisted path leaks into the production corpus.
- **Non-vacuity:** 4 fixture tests prove both scanners fire on crafted mutating shapes and stay silent on the append path, read shapes (`select`/`$count`/`eq`), other-table updates, reads, `ALTER TABLE audit_logs`, and trigger-DDL vocabulary (`BEFORE UPDATE ON audit_logs` does not trip the raw-SQL scanner).

## 3. Migration-DDL pin result

`backend/db/migration/3-immutability-triggers.sql` read from disk (path resolved via `join(process.cwd(), "backend", "db", "migration", ...)` — the same repo-root convention as the `admin-user.repository.test.ts` source-reading tests). Both pin tests PASS:

- `CREATE OR REPLACE FUNCTION prevent_audit_logs_update` + `..._delete` present (idempotent re-runnable function definitions).
- `DROP TRIGGER IF EXISTS prevent_audit_logs_update_trigger ON audit_logs;` + `..._delete_trigger ...;` present, with matching `CREATE TRIGGER` statements for both names — the exact names the `pg_trigger` probe (and `withAuditDeleteTriggersSuspended`) rely on.
- `BEFORE UPDATE ON audit_logs` + `BEFORE DELETE ON audit_logs` present; NO `BEFORE INSERT ON audit_logs` (append path untouched); the enforced message `audit_logs is immutable` originates in the DDL — the same substring the trigger tier asserts on.

## 4. Quality gates

| Gate | Result |
|---|---|
| `bun run test/scripts/run-test.ts backend/db/test/logic/audit/audit-immutability.test.ts` | **17 pass / 0 fail**, 316 expect() calls, ~0.5s |
| `bun run scripts/health/sub-loop.ts backend/db/test/logic/audit/audit-immutability.test.ts --lifecycle duplicates` | **exit 0** — tsgo ✅, oxlint ✅, biome:check ✅, lint:type-aware ✅, check:duplicates ✅ (jscpd scan-scope skip for this path) |
| `bun tsgo` | exit 0, **0 errors** → delta vs baseline = **0** |
| Semantic review | honest environment branching (tier ran, not skipped; no force-skip — `isPgliteProvider()` false on real postgres); assertions via `expectRepoError` try/catch only (no `rejects.toThrow` anywhere); zero `console.*`; allowlist path-exact with live-match bijection; every DB statement inside `runInRollback` (fixture rows never commit) |
| Security & tenancy | scan covers the WHOLE backend tree (sentinel-guarded, no root blind spots); trigger tier proves tamper attempts fail-closed at the DB; test writes are rollback-contained; allowlist additions require an explicit reviewed path edit |

## 5. Notes + carry-forward

- **Drizzle error wrapping:** the PG `RAISE EXCEPTION` message is nested in the driver-error `cause` chain (top-level `message` is Drizzle's "Failed query: …"). The test's `errorMessageChain()` walks the chain — same wrapping-layer rationale as `constraintNameOf` in `test-utils.ts`. Tasks writing future trigger-behavior assertions should assert on the chain, not the top-level message.
- **Concurrent Phase-2 landings observed:** `backend/db/repo/audit/` + `audit-trail.repository.test.ts` + barrel edits landed mid-task from the parallel 2.1/2.3 workstream; no interference (those sources are read-only on `audit_logs` and sit inside the scanned corpus — scan still green on the final run).
- **Allowlist maintenance contract:** any future committed-teardown file that must remove audit rows (e.g. a new journey cleanup) must be added to `TEARDOWN_ALLOWLIST_PATHS` by exact path; the bijection test then requires the file to actually match (and conversely an entry whose file stops mutating fails the test). Rollback-contained DB tests under `backend/db/test/**` never need allowlisting.
- The suite is environment-portable: under pglite the trigger tier skips wholesale; on a push-provisioned postgres DB the structural branch asserts the scan and the outcome of THAT run must record the push-vs-migrate gap (documented ledger entry D-TRIGGER-PUSH-GAP).
