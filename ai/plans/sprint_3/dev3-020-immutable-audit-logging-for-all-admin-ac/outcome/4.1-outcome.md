# Task 4.1 Outcome — `adminUsers.auditTrail` i18n block + namespace parity suite (DEV3-020)

**Plan directory:** `ai/plans/sprint_3/dev3-020-immutable-audit-logging-for-all-admin-ac`
**Task:** 4.1 (+ 4.1.QL / 4.1.TE / 4.1.SR / 4.1.IV)
**Branch:** `feat/dev3-020-immutable-audit-logging-for-all-admin-ac` (recurring flip-to-`main` sandbox artifact struck continuously; restored before every command batch)
**Spec sources:** plan §5.6 (exact block shape) + §5.5 UI-state table (em-dash null placeholder; retry via `common.retry`, NOT a namespace key); tasks.md Phase 4 item 4.1; `0.2-prereq-verification-outcome.md` §1 item 7 + §5 D13 (=CREATE) + §2 (seven `activity.action*` keys at types :438–450); `shared/AGENTS.md` (i18n rules, no-near-duplicates discipline).
**Files (final):** `shared/locale/types/adminUsers/index.ts` (+1 block, 25 leaf keys), `shared/locale/en/adminUsers/index.ts` + `shared/locale/ar/adminUsers/index.ts` (block implemented in both leaves), `shared/locale/adminUsers-namespace.parity.test.ts` (NEW, 43 tests), `shared/locale/handshakeCode-namespace.parity.test.ts` (GATE REPAIR — see §5). NO change to `shared/locale/namespaces/index.ts` (no new namespace registration — D13 holds).

---

## 1. Exact `auditTrail` block added (25 leaf keys, per plan §5.6)

`AdminUsersLabels` gains (appended after `pagination`):

| Sub-block | Keys |
|---|---|
| (root) | `pageTitle`, `pageSubtitle` |
| `filters` | `actorIdLabel`, `entityTypeLabel`, `entityIdLabel`, `actionTypeLabel`, `fromDateLabel`, `toDateLabel`, `applyAction`, `clearAction` |
| `table` | `whenHeader`, `actorHeader`, `actionHeader`, `entityTypeHeader`, `entityIdHeader`, `detailsHeader`, `detailsShowLabel`, `detailsHideLabel`, `noDetailsValue`, `noEntityIdValue`, `allActionsOption` |
| `emptyState` | `title`, `message` |
| `errorState` | `title`, `message` |

Deliberately NOT minted (per plan §5.6 + no-near-duplicates discipline):
- **NO `retry` key** in `errorState` — plan §5.5 routes the query-error retry through `common.retry`; error-copy keys stay out of domain namespaces (REQ-051 discipline untouched; flat `ErrorsLabels` surface unchanged).
- **NO per-action labels** inside `auditTrail` — the action column renders through the REUSED seven `adminUsers.activity.action*` chips (`actionCreate`, `actionUpdate`, `actionDelete`, `actionReactivate`, `actionOverride`, `actionAdjust`, `actionSuspend` — types :438–450). Zero new action vocabulary; the parity suite enforces this pin (see §3).

## 2. Copy (en concise production English · ar proper Arabic script)

| Key path | en | ar |
|---|---|---|
| `pageTitle` | Audit trail | سجل التدقيق |
| `pageSubtitle` | Immutable record of every administrative action across the platform. | سجل غير قابل للتغيير لجميع الإجراءات الإدارية على المنصة. |
| `filters.actorIdLabel` | Actor ID | معرّف المنفّذ |
| `filters.entityTypeLabel` | Entity type | نوع الكيان |
| `filters.entityIdLabel` | Entity ID | معرّف الكيان |
| `filters.actionTypeLabel` | Action type | نوع الإجراء |
| `filters.fromDateLabel` | From date | من تاريخ |
| `filters.toDateLabel` | To date | إلى تاريخ |
| `filters.applyAction` | Apply | تطبيق |
| `filters.clearAction` | Clear | مسح |
| `table.whenHeader` | When | الوقت |
| `table.actorHeader` | Actor | المنفّذ |
| `table.actionHeader` | Action | الإجراء |
| `table.entityTypeHeader` | Entity type | نوع الكيان |
| `table.entityIdHeader` | Entity ID | معرّف الكيان |
| `table.detailsHeader` | Details | التفاصيل |
| `table.detailsShowLabel` | Show details | عرض التفاصيل |
| `table.detailsHideLabel` | Hide details | إخفاء التفاصيل |
| `table.noDetailsValue` | — | — |
| `table.noEntityIdValue` | — | — |
| `table.allActionsOption` | All actions | جميع الإجراءات |
| `emptyState.title` | No audit entries | لا توجد سجلات تدقيق |
| `emptyState.message` | No administrative actions match the current filters. Adjust or clear the filters to see the full trail. | لا توجد إجراءات إدارية مطابقة للمرشحات الحالية. عدّل المرشحات أو امسحها لعرض السجل الكامل. |
| `errorState.title` | Could not load the audit trail | تعذّر تحميل سجل التدقيق |
| `errorState.message` | Something went wrong while fetching the audit trail. Try again. |حدث خطأ أثناء جلب سجل التدقيق. حاول مرة أخرى. |

**ar quality notes:** every prose slot is native Arabic UI copy (no transliterations), matching the established register of the surrounding namespace (`سجل التدقيق` for the audit trail, `المنفّذ` for the acting admin, `نوع الكيان`/`معرّف الكيان` for the entity pair, `تطبيق`/`مسح` for the filter actions — consistent with the existing `filters.clear` register). Diacritics (شدّة) used where they aid readability (`المنفّذ`, `معرّف`, `تعذّر`, `عدّل`). The two null placeholders (`noDetailsValue`, `noEntityIdValue`) are the locale-neutral em-dash `—` in BOTH locales — the plan's §5.5 em-dash placeholder posture, matching the pre-existing `genderOptions.unspecified` slot; they are the ONLY three non-Arabic-script leaves in the entire ar map (verified by a runtime probe, and pinned in the parity suite).

## 3. Parity suite coverage (`shared/locale/adminUsers-namespace.parity.test.ts` — NEW, modeled on `handshakeCode-namespace.parity.test.ts`)

6 describe blocks, 43 tests, 499 expect() calls:
1. **Compile-time parity mirror** — depth-first leaf-path set equality across `adminUsersAr`/`adminUsersEn` (grouped sub-blocks flattened; primary gate remains the shared `AdminUsersLabels` typing on both leaf consts) + every leaf non-empty on BOTH maps (zero dead keys).
2. **auditTrail block pin** — exact 25-leaf-path set under BOTH locales (a key dropped from both maps simultaneously still fails) + `test.each` non-empty resolution per key in both locales + both null placeholders exact-pinned to `—` in both locales.
3. **No English fallthrough** — every ar leaf contains Arabic script (`/[\u0600-\u06FF]/`), with the locale-neutral em-dash as the single tolerated exception.
4. **Single action vocabulary** — the seven `activity.action*` chips resolve non-empty in both locales, are pairwise distinct within each locale, and the `auditTrail` block mints ZERO action-chip keys of its own.
5. **ICU placeholder-name parity** — per auditTrail key, `{name}` placeholder sets agree ar/en (currently zero placeholders; future interpolation inherits the pin).
6. **Registry + bundle wiring** — `namespaces.AdminUsers` present, `AdminUsers.id === "adminUsers.adminUsers"` (`<ns>.<ns>` convention), `AdminUsers.getLabels(enMessages/arMessages)` resolves the composed bundle slices, and `adminUsersTranslations` exists on both message bundles.

Pure unit tier (no server/DB/network); runs via the mandated runner. `namespaces/index.ts` untouched.

## 4. Gates

| Gate | Result |
|---|---|
| `bun run test/scripts/run-test.ts shared/locale` | **140 pass / 0 fail / 1496 expect() across 6 files** — all five pre-existing parity suites + the new adminUsers suite green (after the §5 repair) |
| `bun run test/scripts/run-test.ts shared/locale/adminUsers-namespace.parity.test.ts` (standalone) | **43 pass / 0 fail / 499 expect()** |
| sub-loop `--lifecycle duplicates` per touched file | **exit 0 ×5** — types/adminUsers/index.ts, en/adminUsers/index.ts, ar/adminUsers/index.ts, adminUsers-namespace.parity.test.ts, handshakeCode-namespace.parity.test.ts (tsgo ✅ oxlint ✅ biome:check ✅ lint:type-aware ✅ check:duplicates ✅ on each) |
| `bun tsgo` | **exit 0, 0 `error TS` lines project-wide** |
| `git status --porcelain` (this task's delta) | the 4 task files + the justified handshakeCode repair (§5) + plan bookkeeping (tasks.md 4.1 checkbox, this outcome, worklog). Concurrent Phase-4 artifacts from sibling agents (4.2: frontend/graphql/**, apolloCache*, 4.5: navItems.test.ts + outcomes) observed in the shared tree — NOT touched by this task. |

Semantic review (4.1.SR): zero hardcoded UI strings outside locale leaves; no near-duplicate action labels (enforced by suite); no error keys minted (REQ-051 untouched). Instruction verification (4.1.IV): `shared/AGENTS.md` registration checklist re-checked — existing namespace extension (no step 3/4/5 registration needed), `@/` imports only, view-layer `Pick`/`Omit` composition preserved, no layer violations.

## 5. Gate repair (5th touched file, pre-existing red — proof attached)

`bun run test/scripts/run-test.ts shared/locale` initially reported **2 fails in `handshakeCode-namespace.parity.test.ts`** — PRE-EXISTING at clean HEAD (proven: `git stash push -u` → same 8 pass / 2 fail → `git stash pop`; my 4 files are not imported by that suite, causality structurally impossible). Root cause: the suite's format-copy sweep and placeholder-parity walk iterate the reused `errors` maps' TOP-LEVEL keys as string slots, but `errorsAr`/`errorsEn` carry two grouped sub-blocks (`planCatalog` — added by the plan-catalog CRUD work — and `adminUsers`), so `nonEmptyLabelOf` threw on the object slots (the hardcoded `handshakeCode.` message prefix masked the real map). Repair (minimal, intent-preserving): both sweeps now walk depth-first leaf paths (`leafPathsOf`/`sortedLeafPathsOf`/`leafValueOf` helpers, mirroring `errors-namespace.parity.test.ts`'s nested-block posture); header comment updated accordingly. No assertion weakened — coverage strictly widened (nested error-copy leaves are now inside the code-smuggling and placeholder-parity sweeps).

## 6. Carry-forward for Task 4.4 (AuditTrailView) and 4.x

1. **Label paths (exact):** `adminUsersTranslations.auditTrail.pageTitle|pageSubtitle`; `.filters.actorIdLabel|entityTypeLabel|entityIdLabel|actionTypeLabel|fromDateLabel|toDateLabel|applyAction|clearAction`; `.table.whenHeader|actorHeader|actionHeader|entityTypeHeader|entityIdHeader|detailsHeader|detailsShowLabel|detailsHideLabel|noDetailsValue|noEntityIdValue|allActionsOption`; `.emptyState.title|message`; `.errorState.title|message`.
2. **Action chips:** render the action column through `adminUsersTranslations.activity.action*` keyed by the `AuditActionType` wire value — do NOT read any action label from `auditTrail.*` (there are none, by pin).
3. **Client reads:** `useAppTranslation(AdminUsers)` (handle const), server metadata: `getTranslations(locale).adminUsersTranslations.auditTrail.pageTitle/pageSubtitle` (`AdminUsers.id === "adminUsers.adminUsers"`).
4. **Null rendering:** `noDetailsValue`/`noEntityIdValue` (em-dash) for missing details/entityId; retry button uses `common.retry` (NOT `errorState`); FORBIDDEN → `PermissionDeniedFallback`; rate-limited/unavailable → `RetryableNotice` (per plan §5.5 posture).
5. **gql.ts note:** the concurrent Task-4.2 agent's documents (`adminAuditLogs`) are landing `AdminAuditLog*` TS types as predicted by 3.2 — no interaction with this change set (labels only).
