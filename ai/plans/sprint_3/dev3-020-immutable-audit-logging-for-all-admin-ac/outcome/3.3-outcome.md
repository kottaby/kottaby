# Task 3.3 Outcome — GraphQL wire matrix audit + completion (DEV3-020)

**Plan directory:** `ai/plans/sprint_3/dev3-020-immutable-audit-logging-for-all-admin-ac`
**Task:** 3.3 (+ 3.3.QL / 3.3.TE / 3.3.SEC / 3.3.SR / 3.3.IV)
**Branch:** `feat/dev3-020-immutable-audit-logging-for-all-admin-ac` (recurring flip-to-`main` sandbox artifact — branch verified before EVERY command batch this task; no flip was encountered mid-task, final state re-verified)
**Spec sources:** plan §3.3 (error map), §3.4 (permission matrix), tasks.md Phase 3 item 3.3; REQ-030, REQ-032, REQ-050, REQ-073; outcome files 3.1 (denial expectations carry-forward), 3.2 (surface baselines), 2.4 (service error semantics: pre-DB `VALIDATION`, D6-style plain-Error carrier for corrupt stored enum, honest envelope echo); precedent `backend/graphql/test/notification-integration.matrix.test.ts` (`setupTestServerLifecycle` + `testClient` + raw-HTTP Bearer probes).
**File under audit:** `backend/graphql/test/audit-trail.query.test.ts` — inherited from a prior interrupted-but-completed run at 863 lines, 9 pass / 0 fail / 106 expect (baseline re-confirmed at task start). Final state: **921 lines, 10 pass / 0 fail / 115 expect()**.

---

## 1. Audit table — requirement → test → verdict

| # | Requirement (audit checklist) | Covering test(s) | Verdict |
|---|---|---|---|
| 1 | Anonymous → `UNAUTHORIZED` (pre-resolver) | "anonymous caller answers UNAUTHORIZED through the canonical test client" (canonical-client probe, single-error envelope) + anonymous cell of "anonymous + student/teacher/parent share the denial shape; the table never moves" (raw-wire probe: `data` nulled, `path: ["adminAuditLogs"]`) | **COVERED** |
| 2 | student / teacher / parent → `FORBIDDEN` pre-resolver, no service hit (resolver spy OR `audit_logs` row-count oracle) | "anonymous + student/teacher/parent share the denial shape" — raw-wire FORBIDDEN ×3 with denial shape CONSTANT across roles (same message, same extensions key set — no per-role disclosure) and the `countAllAuditRows()` oracle byte-unchanged across the four-probe denial storm; PLUS "the scope denial precedes any service logic — hostile paging still answers FORBIDDEN" (FORBIDDEN beats the service's pre-DB `VALIDATION` for `pageSize: 0` — ordering proof). Note: the wire server runs as a spawned subprocess, so in-process spies cannot observe it — the row-count oracle (the checklist's sanctioned alternative) is the correct mechanism here | **COVERED** |
| 3 | Admin happy path → closed eight-field projection (`id`, `actionType`, `actorId`, `actorName`, `entityType`, `entityId`, `details`, `createdAt`) + pagination echo (`page`/`pageSize`/`totalCount`) | "the marker page matches the direct-DB oracle on ALL eight fields, in oracle order" (exact key set via sorted-literal assertion, values/ordering/nullability/live `actorName` join vs direct-DB oracle) + "pagination echo — explicit window, defaults, and the out-of-range honest empty page" (explicit page 2/pageSize 1, resolved defaults 1/25, out-of-range page 50 → empty items + unchanged honest count) + "the closed actionType filter narrows exactly to its canonical member" | **COVERED** |
| 4 | Smuggled identity args (`userId` at root; `userId` inside `filters`) → `GRAPHQL_VALIDATION_FAILED` PRE-resolver (BOPLA wire proof) | "smuggled identity fields and a forged enum literal die as GRAPHQL_VALIDATION_FAILED before any resolver runs" — both smuggle shapes probed with `data` key ABSENT from the body (the request never executed) + row-count oracle unchanged; Int-overflow `actorId` beyond the 32-bit wire Int joins the probe set | **COVERED** |
| 5 | Invalid enum literal for `actionType` → `GRAPHQL_VALIDATION_FAILED` | same Tier-4 test, forged literal `NOT_A_REAL_ACTION` → `GRAPHQL_VALIDATION_FAILED`, data absent | **COVERED** |
| 6 | Hostile `pageSize` (0 / 101) → `VALIDATION` with `audit_logs` row-count oracle unchanged | "out-of-range page bounds reject with VALIDATION; the boundary 100 stays accepted" — pageSize 0 / 101 / page 0 → `VALIDATION` (the service's pre-DB localized rejection surfaced at the boundary), boundary pageSize 100 accepted with honest echo, `countAllAuditRows()` byte-unchanged across all probes | **COVERED** |
| 7 | Corrupt-stored-enum + unexpected-internal paths → masked `INTERNAL_SERVER_ERROR` (chaos via service-layer fault injection) with zero leak material | Corrupt-enum path: "unknown raw actionType → plain non-domain Error, masked to INTERNAL_SERVER_ERROR with zero leak material" (repo-seam injection — the sanctioned point, since the pgEnum blocks a corrupt insert and the wire server is a separate process; real admin gate passes, mapper fails CLOSED with plain non-`DomainError` Error carrying the raw value; masked via `finalizeGraphqlResponseScope` with requestId + leak scan). **Unexpected-internal path: was GENUINELY ABSENT → added this task** (see §2) | **COMPLETED** |
| 8 | Closed code set assertions only (`UNAUTHORIZED` / `FORBIDDEN` / `GRAPHQL_VALIDATION_FAILED` / `VALIDATION` / masked `INTERNAL_SERVER_ERROR`) — no ad-hoc codes | grep over the final file: exactly the five sanctioned codes asserted, nothing else | **COVERED** |
| 9 | Comments: zero plan artifacts (no REQ-NNN / Task / DEV3-020 tokens) | grep over the final file (including the new/edited comment blocks): **0 hits** | **COVERED** |

**Inheritance audit verdict:** items 1–6 + 8 + 9 were covered spec-exactly by the inherited file; item 7 was HALF-covered (corrupt-enum yes, unexpected-internal no). No duplicated tests were added — the single genuine gap was closed.

## 2. Additions this task (file grew 863 → 921 lines)

1. **NEW chaos test** — "generic unexpected-internal fault from the snapshot read → masked INTERNAL_SERVER_ERROR with zero leak material": the real admin gate passes, then `AuditTrailRepository.countEntries` is fault-injected at the repository seam to reject with a generic non-domain error ("connection terminated unexpectedly"); asserts the failure is the read-stage carrier (count ran exactly once, the paired `listEntries` leg never ran), the carrier is a plain `Error` and NOT a `DomainError`, and the boundary finalizer masks it into a localized `INTERNAL_SERVER_ERROR` with requestId attached and the internal prose absent from the serialized envelope. Distinct trigger from the corrupt-enum probe (read-stage rejection vs map-stage failure after both reads) — proves the masking contract is not corrupt-enum-specific.
2. **NEW shared helper** `expectMaskedInternal(carrier, leakMaterials)` — feeds one plain non-domain carrier through `finalizeGraphqlResponseScope` on the `adminAuditLogs` path and asserts the masked envelope (code, requestId, per-material leak scan, non-empty localized message). Both chaos tests now route through it (removed the duplicated masking block from the corrupt-enum test — same assertions, single source).
3. **Header comment** extended: the chaos-tier bullet now documents BOTH injected carriers and the unchanged live-HTTP conclusion (no test-only forced-failure fixture exists on the wire schema; the live-HTTP hop of the tier stays owned by the boundary finalizer suite's masking contract).
4. **Gate repairs on inherited latent violations** (the sub-loop had never been run on THIS file before — Tasks 3.1/3.2 gated only the other six files):
   - TS6133: removed the unused `admin` table import (the file's admin fixture provisions via `users`, not the `admin` table — dead import from the interrupted run; also the only project-wide `bun tsgo` error).
   - oxlint `no-unnecessary-type-conversion` + `no-unsafe-enum-comparison`: replaced the `Object.entries/Object.values` enum-scanning twins (`wireNameOf`/`wireNameOfStored`) with a shared module-level `AUDIT_ACTION_WIRE_NAMES` `ReadonlyMap<string, string>` (stored value → wire name) — no enum-to-string comparison, no redundant `String()`, byte-identical outputs.
   - oxlint `no-unsafe-type-assertion`: `caughtServiceError` now narrows via `instanceof Error` guard (throwing a descriptive failure) instead of `as Error`.
   - sonarjs `no-alphabetical-sort`: the projection key-set assertion uses `.toSorted((a, b) => a.localeCompare(b))` (sibling-suite idiom).

## 3. 4-tier mapping (per 3.3.TE)

| Tier | Tests |
|---|---|
| **Tier 1 — happy + denial matrix** | anonymous UNAUTHORIZED (canonical client); anonymous+3-role FORBIDDEN matrix with constant denial shape + row-count oracle; hostile-paging-beats-FORBIDDEN ordering probe; admin closed-projection round-trip vs direct-DB oracle; pagination echo (explicit/defaults/out-of-range); actionType filter narrowing |
| **Tier 2 — boundary pagination** | pageSize 100 accepted with honest echo (inside the Tier-3 test); page 2/pageSize 1 window; resolved defaults 1/25; out-of-range page 50 → empty items + unchanged honest count (never clamped, never an error) |
| **Tier 3 — masked-internal chaos** | corrupt stored enum → plain Error → masked INTERNAL_SERVER_ERROR (zero leak); **NEW** generic unexpected-internal read-stage fault → masked INTERNAL_SERVER_ERROR (zero leak) |
| **Tier 4 — smuggle + bad-enum security probes** | `userId` at root; `userId` inside `filters`; forged `NOT_A_REAL_ACTION` enum literal; Int-overflow `actorId` — all `GRAPHQL_VALIDATION_FAILED` with `data` ABSENT (pre-resolver), row-count oracle unchanged |

## 4. Gates

| Gate | Result |
|---|---|
| `bun run test/scripts/run-test.ts backend/graphql/test/audit-trail.query.test.ts` | **10 pass / 0 fail, 115 expect() calls** (baseline before additions: 9/0/106; the `[ERROR] Unhandled non-domain error masked…` console lines are the finalizer's own expected redacted, requestId-correlated log output from the two chaos probes) |
| `bun run scripts/health/sub-loop.ts backend/graphql/test/audit-trail.query.test.ts --lifecycle duplicates` | **exit 0** (tsgo ✅, oxlint ✅, biome:check ✅, lint:type-aware ✅, check:duplicates ✅ — file outside jscpd scan scope, same as the service suite) |
| `bun tsgo` | **exit 0, 0 errors project-wide** |
| `bun run test/scripts/run-test.ts backend/graphql/test/schema-surface.test.ts` | **33 pass / 0 fail / 172 expect** (green UNCHANGED) |
| `bun run test/scripts/run-test.ts backend/graphql/test/sdl-static-assertions.test.ts` | **18 pass / 0 fail / 60 expect** (green UNCHANGED) |
| `bun run test/scripts/run-test.ts backend/graphql/test/plan-catalog.schema.test.ts` | **5 pass / 0 fail / 22 expect** (green UNCHANGED) |
| `bun run test/scripts/run-test.ts backend/graphql/test/handshake-code-surface.test.ts` | **22 pass / 0 fail / 44 expect** (green UNCHANGED — frozen-six public allowlist untouched) |

## 5. 3.3.SEC / 3.3.SR / 3.3.IV verification notes

- **SEC:** the matrix IS the BFLA/BOPLA wire proof — anonymous UNAUTHORIZED + three-role FORBIDDEN (pre-resolver; the subprocess architecture makes the audit_logs row-count oracle the sanctioned no-hit mechanism, reinforced by the FORBIDDEN-beats-VALIDATION ordering probe), BOPLA smuggle probes die at validation with `data` absent (structurally unreachable even for the legitimate admin), hostile pagination dies pre-DB with the row-count oracle unchanged, and both chaos carriers mask to the closed INTERNAL_SERVER_ERROR with leak scans over the corrupt value / mapper prose / internal prose. The denial-shape constancy across roles proves no per-role disclosure.
- **SR:** closed code set only (five sanctioned codes — grep-verified); shared masking helper removed the one duplicated assertion block; no new local type declarations (runtime-guarded interfaces + a string map only); zero dead code (the unused import removal was IN this file); zero plan-artifact tokens (grep-verified 0 hits on the final file); spy lifecycle discipline preserved (trackedSpies registry + file-level afterEach restore — both new repo spies route through `trackSpy`).
- **IV:** validated against `.github/instructions/tests.instructions.md` + `backend.instructions.md` (as surfaced by the sub-loop), `backend/graphql/AGENTS.md`, root `AGENTS.md`; suite shape conforms to the `notification-integration.matrix.test.ts` precedent (`describeGraphqlSuite` + `setupTestServerLifecycle` + `testClient` for the canonical anonymous probe + raw-HTTP Bearer probes for everything per-request-authenticated); mandated runner used throughout.

## 6. Deviations / environment notes

- The inherited file carried latent tsgo/oxlint/sonarjs violations because the sub-loop gate had never been targeted at it (the interrupted run's suites ran only via the test runner, which does not lint/typecheck). All four repairs are file-local (Fix-Or-Report satisfied: no cross-file change needed).
- The unexpected-internal chaos probe is structurally in-process (service call + `finalizeGraphqlResponseScope`) rather than live-HTTP: the wire server is a spawned subprocess (in-process faults cannot reach it) and the pgEnum structurally blocks persisting a corrupt stored enum, so no test-only forced-failure fixture exists on the wire schema — identical to the corrupt-enum probe's documented conclusion and to the gateway integration suite. The masking contract on the live-HTTP hop remains owned by `error-finalizer.test.ts`.
- Branch flips: none encountered this task (verified before every command batch; final re-check green). Contrast with Tasks 2.4/3.1 where the flip struck continuously.

## 7. Carry-forward

- **For Task 2.2-adjacent journey suites / later phases:** the wire-level denial shapes are now pinned — anonymous → single-item UNAUTHORIZED envelope with `data: null`; roles → FORBIDDEN with byte-constant shape across roles; validation-tier deaths omit `data` entirely. Any new admin read surface can reuse `postDocument`/`postAnonymous`/`expectDenialCode`/`expectMaskedInternal` from this file's helper pattern (extract to a shared helper barrel ONLY if a second consumer materializes — no speculative abstraction).
- **For Task 4.x (frontend):** no new schema/codes beyond 3.2's surface; the `AdminAuditLogPage` wrapper `keyFields:false` cache posture carry-forward from 3.1 stands; the frontend error copy for masked INTERNAL_SERVER_ERROR rides the existing i18n error state labels per plan §5.6.
- **Environment:** the sandbox may flip HEAD to `main` between sessions — re-verify `git branch --show-current` before any command batch; all work in this change set lives on `feat/dev3-020-immutable-audit-logging-for-all-admin-ac` as uncommitted modifications to the 3.1/3.2 change set plus the new outcome files.
