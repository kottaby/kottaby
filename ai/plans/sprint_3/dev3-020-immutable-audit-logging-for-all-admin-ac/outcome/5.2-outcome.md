# Task 5.2 Outcome — Zero-Drift & Frozen-Surface Gates (DEV3-020)

**Plan directory:** `ai/plans/sprint_3/dev3-020-immutable-audit-logging-for-all-admin-ac`
**Task:** 5.2 Zero-drift & frozen-surface gates (schema/migration diffs, public-operations pin, baseline counter deltas, scan guard)
**Branch:** `feat/dev3-020-immutable-audit-logging-for-all-admin-ac` (checked/restored before every command batch; all proofs re-run on the feature branch)
**Companion:** suite results + journey differential + chaos cross-check live in `outcome/5.1-outcome.md`.

---

## 1. Zero-drift proofs (REQ-042)

| Proof | Command | Result | Verdict |
|---|---|---|---|
| Schema drift | `git diff --name-only main -- backend/db/schema/` | **EMPTY (0 files)** | **PASS — zero schema drift** |
| Migration-tree delta | `git diff main..HEAD --stat -- backend/db/migration/ backend/drizzle/` | **EMPTY (0 files)** | **PASS — the branch shipped NO new migration.** Note: the conditional Task-1.2 SQL file was never needed — the immutability trigger SQL (`backend/db/migration/3-immutability-triggers.sql` + drizzle copy `20260825222701_custom_3-immutability-triggers/`) is pre-existing on `main` (Task 1.2 was VERIFY-only, "consumed by reference" per `outcome/1.2-outcome.md`), so an empty migration delta is the CORRECT end state |

## 2. Frozen-surface proofs (REQ-060)

| Proof | Command | Result | Verdict |
|---|---|---|---|
| Public operations pin | `git diff main -- backend/lib/gateway/public-operations.ts` | **EMPTY** | **PASS — gateway surface untouched** |
| Handshake allowlist | `bun run test/scripts/run-test.ts backend/graphql/test/handshake-code-surface.test.ts` | **22 pass / 0 fail / 44 expect()** | **GREEN — allowlist pin holds** |

## 3. Baseline counter deltas (REQ-076) — all +0 vs Phase-0 baseline

Phase-0 baseline (`outcome/phase0-baseline-outcome.md` §1): tsgo 0 errors / oxlint 0+0 / biome clean / lint success.

| Counter | Phase-0 baseline | This run (final) | Delta |
|---|---|---|---|
| `bun tsgo` | exit 0, 0 `error TS` lines | **exit 0, 0 `error TS` lines** (output verified) | **+0** |
| `bun run oxlint` (`--deny-warnings`) | 0 warnings, 0 errors (1050 files) | **"Found 0 warnings and 0 errors." (1074 files, 303 rules)** | **+0** |
| `bun run biome:check` | clean, "No fixes applied" (1074 files) | **clean — "Checked 1098 files in 7s. No fixes applied."** (stable across re-runs; file-count growth = this plan's new files) | **+0** |
| `bun run lint` (lint-service) | success, exit 0 | **exit 0** | **+0** |

File-count notes: oxlint counts 1074 (it excludes additional ignored paths), biome 1098 — both grew only by DEV3-020's added files; zero rule violations were introduced or remain.

### 3.1 Repairs required to restore counter parity (root cause inside this plan — minimal fixes applied)

The first full counter sweep found `bun run oxlint` 1 warning and `bun run lint` exit 1 (6 errors), ALL in three NEW Phase-4 files of this plan that the per-file sub-loops had passed (the sub-loop's oxlint pass resolves the `@/` alias differently from the full type-aware lint-service, which flags the same-module pairs via `import-x/no-duplicates` — a harness-coverage gap recorded for Phase 6):

| File | Finding | Minimal fix |
|---|---|---|
| `frontend/views/admin/audit/AuditTrailFilterFields.tsx:101` | oxlint `typescript(no-unnecessary-type-assertion)` — `(event.target.value \|\| "") as AuditActionType \| ""` is a no-op cast (MUI v9's generic Select already types `e.target.value`, the documented `AdminUserGenderSelect` idiom: "no `as` cast at the caller") | Dropped the no-op assertion (1 line) |
| `frontend/views/admin/audit/AuditTrailFilterBar.tsx` | lint-service `import-x/no-duplicates` ×2 — `ReactNode` (type) + `useState` (value) split from `react` | Merged to `import { type ReactNode, useState } from "react"` (the exact 4.4 `AuditTrailView` precedent / repo convention) |
| `frontend/views/admin/audit/AuditTrailFilterFields.tsx` | lint-service `import-x/no-duplicates` ×2 — `FilterDrafts` (type) + `ACTION_VALUES` (value) from `audit-trail-filters` | Merged to `import { type FilterDrafts, ACTION_VALUES } from …` |
| `frontend/views/admin/audit/audit-trail-filters.ts` | lint-service `import-x/no-duplicates` ×2 — `AdminAuditLogFiltersInput` (type) + `AuditActionType` (value) from the generated graphql module | Merged to one import; the runtime-enum comment retained/adapted |

Post-fix verification: all four counters **baseline+0** (table §3); `bun tsgo` 0; component suite re-run **15 pass / 0 fail / 95 expect()**; documents suite **8 pass / 0 fail / 32 expect()** — behavior byte-equivalent, no test touched. (This is the sanctioned repair class: Phase-0–4 files of this plan, minimal diff.)

## 4. Scan guard over ALL new plan files (REQ-077) — per-check verdicts

Scope: every `*.ts`/`*.tsx` file ADDED by the branch (`git diff main --name-only --diff-filter=A`): **24 files** — `app/(dashboard)/audit/page.tsx`; `backend/db/repo/audit/{audit-trail.repository,index}.ts`; `backend/db/test/logic/audit/{audit-immutability,audit-trail.repository}.test.ts`; `backend/graphql/pothos/admin/audit-trail.pothos.ts`; `backend/graphql/query/admin/audit-trail.query.ts`; `backend/graphql/test/audit-trail.query.test.ts`; `backend/services/admin/{audit-trail.service,audit-trail.service.test}.ts`; `backend/types/audit/audit-trail.types.ts`; `frontend/graphql/sharedDocuments/admin/{audit-trail.documents,audit-trail.documents.test}.ts`; `frontend/views/admin/audit/{AuditTrailView,AuditTrailFilterBar,AuditTrailFilterFields,AuditTrailResults,AuditTrailRow,AuditTrailStates,audit-trail-filters,audit-trail-skin}.ts(x)`; `shared/locale/adminUsers-namespace.parity.test.ts`; `test/ui/components/admin/AuditTrailView.test.tsx`; `test/workflows/admin/audit-trail.journey.test.ts`.

| # | Check | Hits | Verdict |
|---|---|---|---|
| 1 | `console.*` | 0 | **PASS** |
| 2 | `next-intl` import | 0 | **PASS** (translation access is exclusively the shared-locale bundle handles) |
| 3 | LIKE / ILIKE | 0 SQL occurrences | **PASS** — the only case-insensitive match is the English word "like" in a prose comment (`test/ui/components/admin/AuditTrailView.test.tsx:166` — "exactly like the view's builder"); no `LIKE`/`ILIKE` SQL operator anywhere, so the injection surface is absent (D5) |
| 4 | `toISOString()` hand-serialization | 2 production + 3 test/comment hits, all sanctioned | **PASS** — the forbidden pattern is the D4 OUTPUT workaround (`String` + `.toISOString()` surfacing timestamps): `audit-trail.pothos.ts` uses `t.expose("createdAt", { type: "DateTime" })` and the generated SDL carries `createdAt: DateTime!`. The production hits are `audit-trail-filters.ts:218–219` — the WIRE-INPUT encoding of `from`/`to` `DateTime` arguments (`applied.from.toISOString()`), which is the repo's documented DateTime-input idiom (`frontend/graphql/test/AGENTS.md:203` `new Date().toISOString()`) and the half-open-day wire contract from outcome 4.3 §4; the remaining hits are test-side expected-value construction (`audit-trail.query.test.ts:736`, `audit-trail.service.test.ts:359`) and a negation comment (`audit-trail.pothos.ts:21`, "no hand-rolled `toISOString()` presentation layer") |
| 5 | LIKE-escape helper references | 0 | **PASS** — `escapeLikeWildcards` (or any escape helper) never arises; there is no pattern-match surface to escape |

## 5. Deviations / environment notes

- **Branch flips:** HEAD was found on `main` between effectively every command batch this pass (the recurring sandbox artifact at its most aggressive); every batch re-issued `git checkout feat/dev3-020-immutable-audit-logging-for-all-admin-ac` first and re-verified. All proofs above were captured on the feature branch.
- The stale `next dev` server kill (5.1 §3.1) was environment hygiene only — no repo file touched by it.
- This task's total source footprint: the four minimal Phase-4 import/cast repairs (§3.1) — the only deviations from "verification-only", both root-caused to this plan's own new files and both required to restore the REQ-076 baseline+0 gate.

## 6. Carry-forward

- Phase 6 waves can take §1/§2 as the frozen-surface baseline and §3 as the counter baseline; the sub-loop-vs-lint-service resolution gap (§3.1 note) is a harness finding worth a ticket of its own.
