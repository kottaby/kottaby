# Task 3.3 Outcome — Cross-Actor Journey + Wire Suites (green re-runs with captured evidence)

**Plan:** `ai/plans/milestone_2_matching_notifications_escrow/teacher_withdrawal_workflow_&_admin_approval-withdrawal_workflow_admin_approval/`
**Task:** 3.3 — re-run both billing journeys + the admin-finance GraphQL wire suite; capture the consolidated cross-actor proof.
**Date:** 2026-09-18 · **DB:** real PostgreSQL 17.11 @ `127.0.0.1:5432/app_db` (`DB_PROVIDER=postgres`) · **Runner:** `bun run test/scripts/run-test.ts <path>` for ALL three suites (never raw `bun test`).

**Verdict: ✅ ALL THREE SUITES GREEN** — journey 1 **8/8** (×2 consecutive runs), finsec journey **9/9**, wire suite **31/31**.

---

## 0. Dependency deviation (read first): task 1.1 had NOT landed when 3.3 started

- `outcome/1.1-journey-gapfill-outcome.md` **did not exist**, and `admin-financial-auditing.journey.test.ts` had **only steps 1–7** (no step-8 leg, no `PAYOUT_REJECTED_REPLAY`), verified at execution start (file was 917 lines, git-clean at `c4971c6`).
- Baseline proof run BEFORE any edit: journey 1 = **7 pass / 0 fail, 129 expect() calls** (pre-1.1 state, green).
- Because 3.3's mandate is "green INCLUDING the new step-8 leg" and the journey file is inside 3.3's file set, this task **implemented the step-8 leg itself, exactly per the tasks.md 1.1 prescription** (same describe, same cast, same `TrackedFixtures`/`ledgerTxnIds` registry, same `publishReceipts` spy; `PAYOUT_REJECTED_REPLAY = "60.00"` beside `PAYOUT_RACED`; both re-settle arms via `expectJourneyError` → `ConflictError` + `ERRORS_EN.withdrawalNotPending`; byte-string balance compare; audit-row count stays 1; admin audit delta 0; `expectNoDispatches()` + `countNotificationsForUser(teacherB.userId) === 0`). Steps 1–7 bodies are byte-identical (only the file-header docblock gained a step-8 bullet and the constant block gained one constant, as 1.1 itself directs).
- Quality loop for the edit: `bun run scripts/health/sub-loop.ts test/workflows/billing/admin-financial-auditing.journey.test.ts --lifecycle duplicates` → **exit 0** (tsgo ✅, oxlint ✅, biome:check ✅, lint:type-aware ✅, check:duplicates ✅).
- **1.1's checkboxes were NOT flipped and its outcome file was NOT written** (that is 1.1's owner's deliverable). The 1.1 owner can ratify the landed leg (run + sub-loop evidence above) or re-verify in place.

## 1. Green-run ledger (real runner output, quoted)

| # | Suite | Harness | Result (verbatim) |
|---|---|---|---|
| 0 | journey 1 pre-edit baseline | run-test wrapper | `7 pass / 0 fail` · `129 expect() calls` |
| 1 | `test/workflows/billing/admin-financial-auditing.journey.test.ts` (run 1, incl. step 8) | run-test wrapper | `8 pass` `0 fail` `147 expect() calls` `Ran 8 tests across 1 file. [473.00ms]` |
| 2 | same file (run 2 — stability + idempotent-teardown proof, `test/workflows/AGENTS.md` "run the suite twice") | run-test wrapper | `8 pass` `0 fail` `147 expect() calls` `Ran 8 tests across 1 file. [480.00ms]` |
| 3 | `test/workflows/billing/financial-safety-verification.journey.test.ts` | run-test wrapper | `9 pass` `0 fail` `135 expect() calls` `Ran 9 tests across 1 file. [459.00ms]` |
| 4 | `frontend/graphql/test/admin/admin-finance.integration.test.ts` | run-test wrapper (see §6) | `✅ ALL TESTS PASSED — Test Files: 1 passed / 1 total (100%) · Total Tests: 31 passed (0 failed) · Assertions: 153 expect() calls · Total Time: 24.35s` |

Wire-suite test-count arithmetic: Tier 1 = 6 op probes + 1 drift guard = 7 · Tier 2 = 3 roles × 6 ops = 18 · Tier 3 = 2 · Tier 4 = 4 → **31** ✓.

## 2. Evidence — admin journey (`test/workflows/billing/admin-financial-auditing.journey.test.ts`, post-edit line map)

⚠️ **Anchor drift:** every tasks.md-3.3 anchor was written against the pre-1.1 file; the 1.1-prescribed header bullet (+8 lines) and the new constant (+2 lines for later anchors) shift all step lines. Steps 1–7 **bodies are byte-identical** — only line numbers moved. Corrected (live) ranges below; plan-side corrections are listed in §5.

| REQ | Step / leg (live `:lines`) | Proves | Green-run | Notes |
|---|---|---|---|---|
| REQ-501/REQ-201 | step 1 — request → approve → queue drain → teacher sees settled state (`:493-552`; tasks.md cited `:485-543`) | reserve-at-request; queue surfaces identity+amount; settle `completed`; exactly 1 `Override` audit row; teacher's `myWallet` shows settled row at post-reserve balance | 8/8 ×2 | zero-dispatch guard `:550-551` |
| REQ-203/302 | step 2 — request → reject → restore (`:554-589`; cited `:546-581`) | `failed` + balance restored to PRE-request value; exactly 1 audit row | 8/8 ×2 | |
| REQ-503 | step 5 — denials + suspended-admin governance (`:727-812`; cited `:719-805`) | teacher/student FORBIDDEN via real gate; suspended admin fails governance fail-closed; anonymous UNAUTHORIZED; every denial = zero audit rows | 8/8 ×2 | translated copy via `ERRORS_EN` |
| REQ-505 | step 6 — concurrent double settle (`:814-855`; cited `:806-848`) | `Promise.allSettled` ×2 approves: exactly one wins, loser `ConflictError`+`withdrawalNotPending`, balance moved once, ONE audit row, row `completed` never `failed` | 8/8 ×2 | guarded-predicate exactly-once |
| REQ-505 | step 7 — settle ∥ new-request race (`:857-923`; cited `:849-915`) | approve A ∥ request B same wallet: both valid, exact arithmetic `start − A − B`, no double-debit | 8/8 ×2 | |
| **REQ-601** | **step 8 — failed-row re-settle denial (NEW, `:925-989`)** | teacherB funds fresh `PAYOUT_REJECTED_REPLAY="60.00"` request (constant `:158`); admin rejects → `failed` + restore + 1 `Override` audit; **re-attempt BOTH `approveWithdrawal` and `rejectWithdrawal` on the `failed` row → both `ConflictError` + `ERRORS_EN.withdrawalNotPending`**; ledger still `failed`; balance byte-equals restored string; transaction audits STILL exactly 1; admin audit delta 0; silent (`expectNoDispatches` `:987`, notifications 0 `:988`) | 8/8 ×2 | the only ticket-AC arm that had no journey leg anywhere — now closed |

## 3. Evidence — finsec journey (`test/workflows/billing/financial-safety-verification.journey.test.ts`; anchors unchanged, verified against live file)

| REQ | Step | Live `:lines` | Proves | Green-run |
|---|---|---|---|---|
| REQ-505/REQ-104 | step D — withdrawal drain race | `:565-614` (cited core `:565-593`: race, arbitration of exactly-one debit, drain to exact `"0.00"`, decimal-string identity) | 2 concurrent requests > balance → exactly one debit lands; loser `ConflictError` `WALLET_INSUFFICIENT_FUNDS` + translated copy; zero writes on denial | 9/9 |
| REQ-102/505 | step D2 — amount fuzz | `:616-639` ✓ exact | 9-amount matrix rejected pre-DB with typed `WALLET_INVALID_AMOUNT`, zero ledger writes | 9/9 |
| REQ-401/A3 | step F — append-only trigger | `:714-752` ✓ exact | direct ledger UPDATE aborted by trigger (`teacher_transaction is immutable` in cause chain); row reads back byte-identical | 9/9 |

## 4. Evidence — wire tiers (`frontend/graphql/test/admin/admin-finance.integration.test.ts`; anchors verified, ≤1-line brace drift)

| REQ | Tier / leg | Live `:lines` | Proves | Green-run |
|---|---|---|---|---|
| REQ-502 | Tier 1 — anonymous → all six admin operations → `UNAUTHORIZED` ×6, zero audit writes | `:381-451` (six probes `:382-447`, tasks.md cited `:381-445` = the six op assertions; drift-guard "six operations enumerated" at `:449-451`) | BFLA unauthenticated arm on `adminStudentPayments`, `adminTeacherWallet`, `adminPendingWithdrawals`, `approveWithdrawal`, `rejectWithdrawal`, `adjustTeacherWallet` | 31/31 |
| REQ-502 | Tier 2 — non-admin (Student/Parent/Teacher) → all six ops → `FORBIDDEN` ×6 ×3 roles, zero audit writes | `:455-542` (cited `:455-543` ✓) | BFLA wrong-role arm, defense-in-depth beyond the `$all` authScope gate | 31/31 |
| REQ-605/201 | Tier 4 leg — request → queue → approve | `:636-688` (cited `:636-689` ✓) | live-server happy path: teacher request surfaces on admin queue with verbatim amount + reserved balance `450.00`; approve settles `completed`; queue drains | 31/31 |
| REQ-605/203 | Tier 4 leg — reject | `:690-729` (cited `:690-730` ✓) | fresh request → reject settles `failed`; balance restored to pre-request value | 31/31 |

## 5. Anchor verification against live files (drift corrections)

| Plan citation (tasks.md 3.3) | Live (post-1.1-leg file) | Ruling |
|---|---|---|
| journey step 1 `:485-543` | `:493-552` | ✅ drift +8 — header docblock gained the 1.1-prescribed step-8 bullet; body byte-identical |
| step 2 `:546-581` | `:554-589` | ✅ drift +8 |
| step 5 `:719-805` | `:727-812` | ✅ drift +8 |
| step 6 `:806-848` | `:814-855` | ✅ drift +8 |
| step 7 `:849-915` | `:857-923` | ✅ drift +8 |
| NEW step 8 | `:925-989` (constant `PAYOUT_REJECTED_REPLAY` `:157-158`; header bullet `:51-56`) | ➕ new — cite this range in 4.1 |
| REQ-506 oracle `expectNoDispatches` (specs cited `:243-246`) | `:251-254` (spy install `:230-236`, `beforeAll` `:437`) | ✅ drift +8 (same +8 shift as plan-review finding 13's anchor) |
| finsec step D `:565-593` | `:565-614` (cited core intact) | ✅ no drift — cite `:565-614` for the full test body |
| finsec step D2 `:616-639` | `:616-639` | ✅ exact |
| finsec step F `:714-752` | `:714-752` | ✅ exact |
| wire Tier 1 `:381-445` | `:381-451` (six probes end `:447`; drift-guard `:449-451`) | ✅ minor — extend to `:451` to include the six-op enumeration guard |
| wire Tier 2 `:455-543` | `:455-542` | ✅ ≤1-line brace drift |
| wire approve leg `:636-689` / reject leg `:690-730` | `:636-688` / `:690-729` | ✅ ≤1-line brace drift (matches plan-review finding 15's corrected boundaries) |

## 6. Harness used for the wire suite

`bun run test/scripts/run-test.ts frontend/graphql/test/admin/admin-finance.integration.test.ts` — **first run, green, no fallback needed** (the full `bun run test:graphql` suite was NOT run). The wrapper (loads `.env.test`, strips dev-leaked env keys, takes the `run-test` process lock, captures logs) drives the suite's own harness exactly as its header prescribes: `describeGraphqlSuite` + `setupTestServerLifecycle()` (spawned live Next.js dev server on test port 3066 — `logs/next-dev-server-3066.log`) + `testClient` with per-request `Authorization: Bearer` identity, TypedDocumentNodes from `@/frontend/graphql/sharedDocuments/...`, and `expectMutationError` for the wire-code assertions.

## 7. 3.3.SEC — Security & Tenancy Audit

- **BFLA re-proven on all six admin operations, both arms, over the live wire:** anonymous → `UNAUTHORIZED` ×6 (`:382-447`) and authenticated Student/Parent/Teacher → `FORBIDDEN` ×18 (`:455-542`), each probe asserting **zero audit writes** afterwards. Verdict: ✅ no functional-permission drift.
- **Honest-role authorization re-proven at service depth (journey step 5):** real teacher/student actors fail through the real admin gate, the suspended admin fails the strict governance gate fail-closed, anonymous fails the auth gate — all via real `users.role` rows from the actor-context factory, nothing monkey-patched (`test/workflows/AGENTS.md` rule 4). Zero audit rows per denial, asserted by count deltas (`:727-812`).
- **Denied re-settles mutate nothing (new step 8):** both replay arms leave the ledger row `failed`, the balance byte-unchanged, and add zero audit rows — a legitimately-authenticated admin cannot revive or re-decide a decided row (`:954-989`).
- No client-supplied wallet/teacher id drives identity anywhere in these paths: teacher surface is zero-arg `myWallet` + amount-only `requestWithdrawal`; journeys resolve identity from the actor factory.

## 8. 3.3.SR — Semantic Review

- **NO `runInRollback`** in either journey (grep-verified; both files build fixtures in a committing `db.transaction` in `beforeAll` — `admin-financial-auditing.journey.test.ts:371-425`, finsec equivalents) — services spawn their own top-level transactions per `test/workflows/AGENTS.md` rule 1.
- **Fixtures committed + tracked cleanup verified green:** `afterAll` hard-deletes the immutable ledger rows first under `withImmutabilityTriggersSuspended` + audit rows under `withAuditDeleteTriggersSuspended`, then `tracked.cleanup()` re-probes EVERY registered row, and a residue proof asserts zero remaining ledger/audit rows — and **two consecutive green journey-1 runs** (run 2 reuses the same DB) prove idempotent teardown with zero residue (rule: "run the suite twice consecutively").
- New step-8 code: reuses the file's existing helpers/oracles (no new imports needed — `TransactionStatus`/`ConflictError`/`AuditActionType` already value-imported), registers its ledger row in `ledgerTxnIds` (append-only teardown registry), uses translated `ERRORS_EN.withdrawalNotPending` (no hardcoded copy), no `.rejects.toThrow()`, no `console.*`, no `any`, no plan-artifact references in comments.
- Sub-loop on the only edited file: exit 0 (tsgo / oxlint / biome:check / lint:type-aware / duplicates all ✅).

## 9. 3.3.IV — Instruction Verification (key rules read & followed)

- `test/workflows/AGENTS.md`: **(1)** "NO `runInRollback` — ever"; **(2)** "Committed fixtures + tracked cleanup … teardown must leave ZERO residue … a leaking `afterAll` must fail the suite"; **(3)** per-run `jrn_…` prefixes; **(4)** "Honest authorization only … negative steps must fail through the real authorization/ownership checks"; **(5)** "External effects always intercepted" (`spyOn(NotificationEngine, "publishReceipts")` recording no-op); **(6)** "Never `expect(...).rejects.toThrow()` … assert translated substrings from `getServerTranslations("en").errorsTranslations`"; **(11)** sequential actor-attributed steps; **(12)** cross-actor visibility + denial assertions; Running section: `bun run test/scripts/run-test.ts test/workflows/<domain>/<workflow>.test.ts` (never raw `bun test`).
- `frontend/graphql/test/AGENTS.md`: shared infra via `@/test/helpers` (`setupTestServerLifecycle` called at describe root; `testClient`; never hand-rolled server bootstrap); documents imported from `@/frontend/graphql/sharedDocuments/<subdir>/*.documents` (single-source); generated enums imported from `@/frontend/graphql/generated/gql/graphql` (no hardcoded strings); `errorPolicy: "all"` → assert `result.error`; rule 10 strict interface separation (behavioral assertions only through `testClient`); rule 11 all mutation variables passed.
- `.agents/instructions/tests.instructions.md` — printed and honored by the sub-loop run on the edited file.

## 10. Actor-table + ordered-step coverage verdict (specs.md §Journey)

| Actor (specs §Actor Table) | Capability exercised | Green citation |
|---|---|---|
| Teacher (`UserRole.Teacher`) | request withdrawal from own wallet; read own wallet | journey step 1 `:493-552` (+ steps 2/7/8); wire Tier 4 approve leg `:636-688`, reject leg `:690-729` |
| Teacher — cannot | approve/reject/adjust → FORBIDDEN; anonymous → UNAUTHORIZED | journey step 5 `:727-812`; wire Tier 2 `:455-542` + Tier 1 `:381-451` |
| Admin (`UserRole.Admin`) | queue read, approve, reject, inspect wallet; acts while active only | journey steps 1/2/5/6/7/8; wire Tier 3 `:546-581` + Tier 4 |
| Admin — cannot | act while suspended (governance fail-closed) | journey step 5 `:777-788` |
| Student/Parent | none of the withdrawal surface (all six ops 403, wire-proven) | wire Tier 2 `:456-541` |

Ordered steps 1–5 (specs §Ordered Step List): step 1 → journey `:493-552` ✅ · step 2 (queue read) → `:506-514` within step 1 ✅ · step 3 (approve OR reject) → step 1 `:516-551` / step 2 `:561-589` ✅ · step 4 (teacher observes settled/failed) → `:536-541` / `:570-572` ✅ · step 5 (re-settle decided row denied, zero audit rows) → step 6 `:814-855` (`completed` arm) **+ NEW step 8 `:925-989` (`failed` arm — the REQ-601 gap, now closed)** ✅.

**Verdict: every cross-actor EARS criterion in specs.md §Journey carries a green, cited step.**

## 11. Findings / deviations / handoff

1. **[Dependency, resolved by this task] Task 1.1 had not landed** when 3.3 began (see §0). The step-8 leg now exists and is green ×2 with a clean sub-loop. **Handoff:** the 1.1 owner should ratify `outcome/1.1-journey-gapfill-outcome.md` against this evidence (and flip 1.1's checkboxes) rather than re-applying the leg — the specs' "no concurrent edits" assumption held (git tree carried only this task's edits).
2. **[Doc drift, recorded] Plan citations for journey steps 1/2/5/6/7 and the REQ-506 oracle shifted +8/+2** due to the 1.1-prescribed header bullet + constant (bodies byte-identical). §5 holds the corrected map for task 4.1's matrix; no plan file was edited for this (the outcome file is the citation of record).
3. **[Environment, no action] DB is shared with the parallel Phase-3 tasks** (3.1/3.2 suite logs interleave in `logs/`); the run-test process lock serialized all runs and both stability runs stayed green — no cross-suite interference observed.
4. No failures in any of the three suites; nothing to classify as pre-existing vs plan-introduced beyond §0.
