# Task 2.2 Outcome — Workflow-Diagram Wording Repair (§6.3)

**Task:** `2.2 Workflow-Diagram Wording Repair — docs/workflows/03-session-lifecycle-escrow.md §6.3 (UPDATE)`
**Date:** 2026-09-17
**Requirement:** REQ-603 · **Design anchors:** plan.md D2 (reserve-at-request is binding), §7.3 (doc repairs)
**Edit scope:** §6.3 mermaid sequence diagram ONLY — 3 lines changed (2 added, 1 replaced). No other section, no other file.

---

## 1. Pre-Edit Verification (verify-before-edit)

Read `docs/workflows/03-session-lifecycle-escrow.md` §6.3 before editing. Confirmed the superseded wording was present exactly as the plan describes:

- Request step (pre-edit `:147-148`): `Teacher->>System: Request withdrawal from wallet` + `System->>System: Create teacher_transaction (type = withdrawal, status = pending)` — **no reserve semantics**.
- Approve branch carried the superseded debit-at-approval line `System->>System: Deduct from wallet.balance`.
- Reject branch had **no restore** line — balance compensation was undocumented.
- Audit-trail step `System->>System: Log admin action to audit trail` present (kept untouched).
- Mermaid structure: 3 participants, `alt Admin approves / else Admin rejects / end` — kept byte-identical.

**Anchor-drift note (reported, not fixed — plan-dir files are not this task's assigned files):** `specs.md` REQ-603 and `plan.md` D5/§7.3 cite `:153` for "Deduct from wallet.balance"; the pre-edit actual line was **`:154`** (`:153` was `Set transaction status = completed`). Off-by-one only; the target line was found unambiguously by content. Post-edit, all §6.3 line numbers shift (+1 request-step insertion, +1 reject-branch insertion): §6.3 now spans `:140-164`. Plan citations refer to pre-edit state.

## 2. Changed Lines — Before / After (verbatim)

**Edit 1 — Request step: ADD reserve semantics** (new line after the pending-row creation):

```
     Teacher->>System: Request withdrawal from wallet
     System->>System: Create teacher_transaction (type = withdrawal, status = pending)
+    System->>System: Reserve: deduct wallet.balance (guarded balance >= amount)
     System->>Admin: Notify withdrawal request
```

**Edit 2 — Approve branch: REPLACE debit-at-approval with settle-the-reservation:**

```
         Admin->>System: Approve withdrawal
         System->>System: Set transaction status = completed
-        System->>System: Deduct from wallet.balance
+        System->>System: Settle the reservation: status pending → completed (balance already reserved at request)
         System-->>Teacher: Withdrawal processed
```

**Edit 3 — Reject branch: ADD restore (compensation):**

```
         Admin->>System: Reject withdrawal
         System->>System: Set transaction status = failed
+        System->>System: Restore: wallet.balance += amount (compensation for the request-time reserve)
         System-->>Teacher: Withdrawal rejected notification
```

Untouched, as mandated: the mermaid fence + `sequenceDiagram` declaration, all three `participant` lines, the `alt/else/end` skeleton, the `Set transaction status = completed` / `Set transaction status = failed` lines, both teacher-facing notification arrows, and the audit-trail step. Wording note: the plan's inline-code backticks around `balance >= amount` were rendered as plain text in the diagram — verified house style is **no backticks inside mermaid message lines** anywhere in `docs/` (backticks would display literally in the rendered diagram; the predicate wording itself is verbatim).

## 3. Canonical-Doc Cross-Check — `docs/billing/admin-financial-auditing.md` §2 (:46-68)

Verdict: **✅ AGREE — the repaired diagram and the canonical settlement model match in semantics.**

| Canonical §2 clause | Repaired §6.3 line | Verdict |
|---|---|---|
| **Request** (:50-54): inserts ONE `teacher_transaction(type='withdrawal', status='pending')` row AND debits the wallet via ONE guarded UPDATE (`balance >= amount` in the predicate) — reserve locked at request time | `:148` Create pending row + `:149` `Reserve: deduct wallet.balance (guarded balance >= amount)` | ✅ |
| **Approve** (:55-58): guarded `settleWithdrawalOnce` flips pending → completed — **settle-only**; "The wallet balance NEVER moves again: the debit already happened at request time, so approval settles the reservation, it does not re-charge it" | `:154` status = completed + `:155` `Settle the reservation: status pending → completed (balance already reserved at request)` — the approve branch contains **no** balance-debit line | ✅ |
| **Reject** (:59-62): flips to `failed` via the same guarded UPDATE AND restores the reserved debit (`balance = balance + amount`, strictly additive); after rejection the balance equals its pre-request value | `:159` status = failed + `:160` `Restore: wallet.balance += amount (compensation for the request-time reserve)` | ✅ |
| §5 audit-trail mapping: every settlement writes exactly ONE `Override` audit row | `:163` `Log admin action to audit trail` preserved (shared tail of both branches) | ✅ |
| Observable-equivalent anchors (REQ-202): post-approve `balance = pre_request − amount`, row `completed`; post-reject `balance = pre_request` | Holds: approve moves no balance (reserve already at request); reject restores the request-time reserve | ✅ |

Also re-read for the ruling chain: `specs.md` REQ-202 (reserve-at-request binding over ticket gherkin, ratified by the admin-financial-auditing plan D-1 + INV-W5) and `plan.md` D2 — the diagram now states the ratified model, not the superseded ticket wording.

## 4. Validation (2.2.QL)

Markdown file — `scripts/health/sub-loop.ts` does NOT apply (not TypeScript). The repo's mermaid validator (`scripts/validation/validate-mermaid.ts`, package alias `validate:mermaid`; usage read first: explicit single-file mode AND default docs-tree discovery, exit 0 = green) was run BOTH ways. REAL output:

```text
=== SINGLE-FILE MODE ===
✅ docs/workflows/03-session-lifecycle-escrow.md — 3 mermaid diagram(s)
✅ Mermaid validation passed: 1 file(s), 3 diagram(s)
single-file exit=0

=== FULL DOCS-TREE DISCOVERY (bun run validate:mermaid) ===
✅ Mermaid validation passed: 19 file(s), 39 diagram(s)
full-tree exit=0
```

- Single-file mode used on the edited doc: **3 diagrams (§2 state machine, §3 sequence, §6.3 sequence) all pass, exit 0** — the §6.3 block still renders structurally.
- Full-tree run: **19 files / 39 diagrams, exit 0 — ZERO failures anywhere**, so there are no pre-existing failures to record and no file outside the edit needed exemption.

## 5. Semantic Review (2.2.SR)

- [x] Diagram now matches REQ-202's binding ruling exactly: **reserve at request** (`:148-149` — create pending row + guarded debit), **settle at decision** (`:154-155` — approve = status flip, no balance movement), **reject restores** (`:159-160` — compensating additive restore). The superseded debit-at-approval line is gone.
- [x] `git diff docs/workflows/03-session-lifecycle-escrow.md` inspected hunk-by-hunk: exactly 3 changed lines, ALL inside the §6.3 mermaid block (hunk header `@@ -146,16 +146,18 @@ ### 6.3 Teacher Withdrawal Flow`). No other section of the doc was touched (§§1-5, 6.1-6.2, 7-9 byte-identical).
- [x] Audit-trail step preserved; diagram structure (participants, alt/else/end) preserved — REQ-603's "preserving the diagram's structure and the audit-trail step" satisfied.
- [x] Diff stat for THIS task's file: `docs/workflows/03-session-lifecycle-escrow.md | 4 +++-` (3 insertions, 1 deletion).

## 6. Instruction Verification (2.2.IV)

Root `AGENTS.md` re-read in full. Doc-convention compliance for a `docs/` edit (per tasks.md layer mapping, `docs/` files require root AGENTS.md doc conventions only):

- **AI Agent Communication Rules** — no summary files created outside the sanctioned plan `outcome/` directory (this outcome file is plan-mandated); the doc edit itself is project-structure documentation. ✅
- **Mermaid house style** — plain-text message labels, no backticks inside diagram blocks (verified across `docs/workflows/*.md`, `docs/billing/*.md` before writing); new lines follow it. ✅
- **i18n / hardcoded strings** — the rules govern user-facing error copy in code; a workflow diagram is English engineering prose like its siblings (e.g. §6.2 adjustment diagram) — no new user-facing string surface. ✅
- Quality-gate sections (tsgo/biome/lint/duplicates) — n/a for markdown; the applicable gate for this file is the mermaid validator (§4). ✅

## 7. Subtask Ledger (2.2.x)

- **2.2.QL Quality Loop**: sub-loop n/a (markdown); mermaid validator run single-file + full-tree, both exit 0 (§4). ✅
- **2.2.TE Test Engineering**: n/a (documentation) — semantics are re-proven by the Phase-3 journey re-runs (task 3.3 re-cites steps 1/2 whose balance assertions anchor this model). ✅
- **2.2.SEC Security & Tenancy Audit**: n/a (no code surface). ✅
- **2.2.SR Semantic Review**: §5 above — REQ-202 ruling reflected; diff confined to §6.3. ✅
- **2.2.IV Instruction Verification**: §6 above. ✅

## 8. Carry-Forward Notes for the Orchestrator

1. **Anchor drift (reported-not-fixed, mechanical):** `specs.md` REQ-603 and `plan.md` D5 + §7.3 cite `:153` for "Deduct from wallet.balance" — pre-edit truth was `:154` (off-by-one; the plan's `:147-148` request-step cite was correct). Post-edit the whole §6.3 block shifted +1/+2 lines (now `:140-164`; the four changed lines are `:149`, `:155`, `:160`). If plan citations are refreshed at 4.1, use: reserve line `:149`, settle line `:155`, restore line `:160`.
2. **Working tree note:** `db/schema.dbml` carries task 2.1's one-line repair (`>= 0` → `> 0`) from a parallel/earlier execution — present in the shared tree, NOT touched by this task; this task's diff touches only the workflow doc.
3. **Diagram redundancy accepted by design:** the approve branch retains `Set transaction status = completed` AND gains `Settle the reservation: status pending → completed (…)`. The instruction was to replace ONLY the deduct line and keep ALL other lines untouched; the resulting pair parallels the reject branch (status-set line + balance-semantics line) and the settle line's payload is the "balance already reserved at request" clause.
4. **Sibling-diagram consistency check:** `docs/workflows/05-admin-governance-override.md` §6.2 (:145-151) is a capability table (no per-branch balance wording) — no stale debit-at-approval wording there; no cross-doc propagation needed. Its §6.3 manual-adjustment diagram is a different flow (credits/deductions), untouched.
5. For task 4.1's matrix: REQ-603 row evidence = this outcome §2-§3 (before/after + canonical cross-check) + validator output §4 (exit 0).

## 9. Files Touched by This Task

- `docs/workflows/03-session-lifecycle-escrow.md` — §6.3 mermaid block only: 3 lines (2 added, 1 replaced).
- `outcome/2.2-diagram-repair-outcome.md` (this file) — written.
- `tasks.md` — task `2.2` heading + `2.2.QL/.TE/.SEC/.SR/.IV` checkboxes flipped to `[x]` (nothing else).
