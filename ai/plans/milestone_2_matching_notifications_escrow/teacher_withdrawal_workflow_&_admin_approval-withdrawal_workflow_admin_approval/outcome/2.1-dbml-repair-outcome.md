# Task 2.1 Outcome — DBML Check Repair

**Task:** `2.1 DBML Check Repair` — `db/schema.dbml:372` (UPDATE) · Phase 2 (documentation-only; no runtime surface)
**Date:** 2026-09-17
**Scope executed:** exactly one line changed in `db/schema.dbml` (`teacher_transaction.amount` check `>= 0` → `> 0`). No migration, no schema change — DBML is documentation aligning with the already-shipped DB CHECK.
**Pre-execution reads (protocol item 1):** `tasks.md` (task 2.1 scope), `specs.md` (REQ-602 :155), `plan.md` (D5 :75-77), `deferred-items.md` (D6 — INV-W8 wording is NOT this task's), all `outcome/*.md` (0-baseline, plan-review-R1, plan-review-R2), root `AGENTS.md` (doc conventions).
**Tree state at start:** clean (`git status --porcelain` → 0 lines; 0 stashes).

---

## 1. Verify-Before-Edit — both anchors read verbatim (live tree, pre-edit)

**`db/schema.dbml:372` (BEFORE):**

```
  amount      decimal(10,2)       [not null, check: `amount >= 0`]
```

**`backend/db/schema/billing/teacher-transaction.ts:50` (authoritative Drizzle CHECK):**

```typescript
    check("teacher_transaction_amount_check", sql`${t.amount} > 0`),
```

Confirmed drift exactly as REQ-602 states: DBML said `>= 0`; the authoritative Drizzle CHECK says `> 0`.

**Drift origin (historical migrations, read-only check):** the original table migration shipped `>= 0`
(`backend/drizzle/20260904084151_omniscient_karen_page/migration.sql:146` — `CONSTRAINT "teacher_transaction_amount_check" CHECK ("amount" >= 0)`);
a later migration tightened it to `> 0`
(`backend/drizzle/20260914064155_quick_pretty_boy/migration.sql:7` — `ALTER TABLE "teacher_transaction" DROP CONSTRAINT IF EXISTS "teacher_transaction_amount_check", ADD CONSTRAINT "teacher_transaction_amount_check" CHECK ("amount" > 0);`).
The DBML annotation was evidently written against the original bound and never updated when the CHECK was tightened — this repair closes that gap.

---

## 2. The Edit — before/after + diff evidence

**Line 372 AFTER:**

```
  amount      decimal(10,2)       [not null, check: `amount > 0`]
```

**Diff stat (git diff --stat):**

```
 db/schema.dbml | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)
```

**Full diff (exactly 1 line):**

```diff
diff --git a/db/schema.dbml b/db/schema.dbml
index 9682ed9..3346f5c 100755
--- a/db/schema.dbml
+++ b/db/schema.dbml
@@ -369,7 +369,7 @@ Table teacher_transaction {
   wallet_id   integer             [not null]
   session_id  integer
   description varchar(255)
-  amount      decimal(10,2)       [not null, check: `amount >= 0`]
+  amount      decimal(10,2)       [not null, check: `amount > 0`]
   type        transaction_type    [not null]
   status      transaction_status  [not null, default: 'pending']
   created_at  timestamp           [default: `now()`]
```

`git status --porcelain` after the edit: only ` M db/schema.dbml`. Nothing else in the file or tree changed.

---

## 3. Validation (2.1.QL) — sub-loop n/a; no repo DBML gate; inspection + structural + live-DB verification

### 3.1 Sub-loop — does not apply (evidence captured anyway)

Task 2.1.QL states: "`db/schema.dbml` is not TypeScript — sub-loop does not apply." Confirmed empirically:

```
$ bun run scripts/health/sub-loop.ts db/schema.dbml --lifecycle duplicates
ℹ Running tsgo (project-wide, filtering for db/schema.dbml)...
✅ tsgo passed (no errors for db/schema.dbml)
ℹ Running oxlint on db/schema.dbml...
❌ oxlint FAILED — stopping here.
No files found to lint. Please check your paths and ignore patterns.
SUBLOOP_EXIT=1
```

The oxlint "failure" is a **no-input artifact** — oxlint has nothing to lint for a `.dbml` file (tsgo passes trivially: no TypeScript in scope). This is precisely the "sub-loop does not apply" condition the task anticipated; there is no defect in the one-line change to fix.

### 3.2 Repo DBML/mermaid validation gate — investigated, NOT gated in the current repo

Per the task instruction, the repo was checked for a DBML validation entry before falling back to inspection-based verification:

| Check | Result |
|---|---|
| `package.json` scripts | Only `validate:mermaid` exists (`package.json:71` → `bun run scripts/validation/validate-mermaid.ts`); **no `validate:dbml`** (grep `validate` → 1 hit) |
| `bun.lock` / dependencies | grep `-i dbml` over `package.json` + `bun.lock` → **0 hits** (no DBML parser/CLI dependency exists) |
| `.github/workflows/ci.yml` (the only workflow) | Jobs: `quality`, `tests-db`, `tests-services`, `tests-graphql` — **no `dbml-validation` job** |
| `scripts/validation/` | `index.ts`, `validate-mermaid.ts`, `validate-mermaid.test.ts` only — mermaid-only |
| `bun run validate:mermaid` applicability | Script docblock (`scripts/validation/validate-mermaid.ts:19-22`): discovery covers tracked `.mmd` + markdown mermaid fences under `docs/` — **`db/schema.dbml` is not a valid input**; not run (per task: do not run it on unrelated files) |
| M0 pipeline reference (`ai/finished_plans/milestone_0_foundation/cicd-pipeline-with-dbml-mermaid-validati/plan.md:224`) | Historical record only — references `bun validate:dbml`, which the **current repo does not carry** (no script entry, no dependency, no CI job) |

**Conclusion:** no DBML validation gate exists in the current repo, so the task's fallback applies — **inspection-based verification with the diff as evidence** (below), which is in fact stronger than a syntax-only lint because it probes the live database.

### 3.3 Inspection-based verification (diff + structure + scope)

- **Diff scope:** exactly 1 file, 1 insertion, 1 deletion (§2) — the only changed hunk is line 372.
- **Structural sanity:** braces balanced (53 open / 53 close), backtick count even — the file remains well-formed DBML; the new check expression matches the existing house pattern (`check: \`expr\`` backtick style used throughout, e.g. `:289` `session_count > 0`).
- **Scope sweep — all `check:` lines enumerated post-edit:** 17 checks in the file; only `:372` changed. Every other check line is byte-identical to pre-edit (the full diff in §2 is the proof). Notably the other `>= 0` checks (`average_rating` :214, students balances :226-229, `price` :290, wallet `balance`/`total_earning` :303-304, ratings :337/:419/:423, `score` :353) are all consistent with their Drizzle counterparts and were correctly left untouched.

---

## 4. Three-Way Agreement — DBML ↔ Drizzle ↔ live DB (2.1.SR evidence)

Live-database probe (PGlite, the project-configured sandbox provider per `outcome/0-baseline-outcome.md` §3; read-only SELECT):

```sql
SELECT conname, pg_get_constraintdef(oid) FROM pg_constraint
WHERE conname = 'teacher_transaction_amount_check';
```

```json
[ { "conname": "teacher_transaction_amount_check",
    "def": "CHECK ((amount > (0)::numeric))" } ]
```

| Surface | `teacher_transaction.amount` bound |
|---|---|
| DBML (after this repair), `db/schema.dbml:372` | `amount > 0` |
| Drizzle schema, `backend/db/schema/billing/teacher-transaction.ts:50` | `${t.amount} > 0` (`teacher_transaction_amount_check`) |
| Live DB CHECK (PGlite, migrations applied) | `CHECK ((amount > (0)::numeric))` |

**All three agree at `> 0`.** (Before the edit, DBML was the sole dissenting surface.)

---

## 5. Test-Proven Constraint Citations (2.1.TE is n/a — the underlying constraint is already test-locked)

The stricter bound is proven by the shipped suites (all cited per plan; probes live in the wallet repository suite, with the immutability suite locking the tamper surface):

| Probe | Citation | What it proves |
|---|---|---|
| Negative-amount insert rejected | `backend/db/test/repo/billing/wallet.repository.test.ts:501-515` — savepoint-bracketed insert of `amount: "-5.00"` → `expectRepoError`; `constraintNameOf(amountError)` === `"teacher_transaction_amount_check"` + `PG_CHECK_VIOLATION` (assertions at `:513-515`) | `amount < 0` is rejected by the named DB CHECK |
| **Zero-amount movement rejected** | `backend/db/test/repo/billing/wallet.repository.test.ts:725-742` — `debitForArbitrationOnce(... amount: "0.00")` → `constraintNameOf(zeroError)` === `"teacher_transaction_amount_check"` (assertion at `:741`); comment at `:726` states the bound verbatim: "`teacher_transaction_amount_check` (amount > 0 — no no-op movements are ever recorded)" | **The DB CHECK is strictly `> 0`** — a `>= 0` DBML annotation would document a bound the database does not enforce |
| Ledger tamper surface | `backend/db/test/logic/billing/financial-immutability.test.ts` — direct UPDATE rejected (`:363-378`, incl. `amount: "999.99"` relabel attempt), direct DELETE rejected (`:381+`), type-relabel settlement UPDATE raises (`:427+`), compensating-row doctrine (`:457+`, corrective INSERT succeeds while `amount: "0.00"` mutation of the original fails) | The ledger's amount can never be mutated around the CHECK; the check bound is the only write gate |
| Audit trail | `docs/billing/financial-safety-verification.md:39` records the CHECK-probe tier (`wallet_balance_check` / `wallet_total_earning_check` / `teacher_transaction_amount_check` via `expectRepoError` + `constraintNameOf`) | Constraint probes are a documented, shipped verification tier |

(The tasks.md pointer says "financial-immutability.test.ts constraint probes" — the immutability file owns the tamper tiers; the direct `teacher_transaction_amount_check` probes with `constraintNameOf` live in `wallet.repository.test.ts:513/:741`. Both are recorded above so the citation set is complete and precise.)

---

## 6. Security & Tenancy Audit (2.1.SEC)

**n/a** — documentation-only change to a schema-annotation file. No runtime surface, no query, no actor/tenancy path touched. (The DB CHECK itself was already shipped and test-proven; this task changed no constraint.)

---

## 7. Semantic Review (2.1.SR)

- ✅ DBML now matches the Drizzle schema AND the live DB CHECK — all three agree at `> 0` (§4 table).
- ✅ **INV-W8 doc wording untouched (D6 belongs to its owner):** `git diff --name-only -- docs/specs/state-machine-invariants.md` → **empty**; `docs/specs/state-machine-invariants.md:198` still reads `| INV-W8 | teacher_transaction.amount must be >= 0 (check constraint). |` — exactly the pre-edit state. The invariants table remains the invariants-doc owner's edit surface (deferred-items.md D6 ruling respected).
- ✅ No migration authored; no Drizzle source touched; no other DBML line changed (diff = 1 line).
- ✅ Zero plan-artifact references introduced into any code or doc beyond the sanctioned plan-dir outcome/checkbox updates.

## 8. Instruction Verification (2.1.IV)

Root `AGENTS.md` re-read. Compliance check:

- `db/schema.dbml` is a documentation file — per `tasks.md`'s layer mapping, only root `AGENTS.md` applies (no TS layer rules); no AGENTS.md rule governs DBML content beyond general repo discipline.
- "AI Agent Communication Rules" prohibit summary markdown files — this outcome file is the plan-protocol-sanctioned write-back (tasks.md "Non-Negotiable Execution Protocol" item 4), not an unsanctioned summary.
- No lint/comment/cache rules implicated (no code file touched; no caches touched).

---

## 9. Subtask Ledger (2.1.x)

- **2.1.QL Quality Loop:** sub-loop n/a (evidence: §3.1 — tsgo trivial pass, oxlint "No files found to lint" for a non-TS file); repo DBML gate investigated and absent (§3.2); inspection-based verification recorded: 1-file/1-line diff, balanced structure, full check-line scope sweep (§3.3), live-DB three-way agreement (§4). ✅
- **2.1.TE Test Engineering:** n/a (documentation) — underlying constraint already test-proven; citations recorded in §5. ✅
- **2.1.SEC Security & Tenancy Audit:** n/a (§6). ✅
- **2.1.SR Semantic Review:** three-way agreement proven; INV-W8/D6 untouched with byte-level evidence (§7). ✅
- **2.1.IV Instruction Verification:** root AGENTS.md doc conventions re-read (§8). ✅

## 10. Files Touched by This Task

- `db/schema.dbml` — line 372 check repaired (`>= 0` → `> 0`) — the ONLY repo-tree edit.
- `outcome/2.1-dbml-repair-outcome.md` (this file) — written.
- `tasks.md` — task `2.1` heading + `2.1.QL/.TE/.SEC/.SR/.IV` checkboxes flipped to `[x]`.

## 11. Carry-Forward Notes (report-not-fix; no action taken in this task)

1. **Same drift class exists at `db/schema.dbml:401` — OUT OF SCOPE, reported:** `student_payments.amount` reads `[not null, check: \`amount >= 0\`]` in the DBML, while its authoritative Drizzle CHECK is `student_payments_amount_check` with `amount > 0` (`backend/db/schema/billing/student-payments.ts:74`). Task 2.1's scope is exclusively `teacher_transaction.amount` at `:372` (REQ-602; "Nothing else in the file may change"), so this was NOT touched. Recommend the orchestrator queue a one-line sibling repair (same mechanics as this task) for the `student_payments` annotation.
2. **No DBML validation gate in the current repo** (§3.2) — the M0 finished plan references `bun validate:dbml`, but the script, dependency, and CI job do not exist in the current tree (`docs/specs/open-decisions-and-gaps.md:331` also cites the non-existent command). Informational for the orchestrator; re-introducing the gate is outside this verification plan's scope.
3. **D6 (INV-W8 `>= 0` wording) remains open with its named owner** — untouched here, as ruled.
