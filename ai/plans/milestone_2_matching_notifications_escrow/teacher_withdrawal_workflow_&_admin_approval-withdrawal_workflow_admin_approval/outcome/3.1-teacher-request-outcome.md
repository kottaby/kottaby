# Task 3.1 Outcome — Teacher-Side Request Suites (green re-runs with captured evidence)

**Task:** `### - [ ] 3.1 Teacher-Side Request Suites` — `backend/services/billing/wallet.service.test.ts`, `backend/db/test/repo/billing/wallet.repository.test.ts`
**Plan:** `ai/plans/milestone_2_matching_notifications_escrow/teacher_withdrawal_workflow_&_admin_approval-withdrawal_workflow_admin_approval/`
**Date:** 2026-09-18 · **Executor:** task-3.1 subagent · **Kind:** read-only verification (zero source/test edits)

---

## 1. Green-run evidence (the deliverable)

Both suites were executed via the mandated runner (never raw `bun test`), then re-read via `--last`:

```
bun run test/scripts/run-test.ts backend/services/billing/wallet.service.test.ts
bun run test/scripts/run-test.ts backend/db/test/repo/billing/wallet.repository.test.ts
bun run test/scripts/run-test.ts <path> --last --focus "<pattern>"   # result read-back
```

**Suite 1 — `backend/services/billing/wallet.service.test.ts`** (log: `logs/2026-09-18T19-14-52/backend/services/billing/wallet.service.test.ts.log`)

```
(pass) WalletService.requestWithdrawal (runInRollback) > happy path: exactly ONE pending withdrawal row, balance debited, total_earning untouched, ledger refreshed [12.12ms]
(pass) WalletService.requestWithdrawal (runInRollback) > insufficient funds: localized WALLET_INSUFFICIENT_FUNDS conflict and ZERO committed rows (orphan pending row rolls back) [8.73ms]
(pass) WalletService.requestWithdrawal (runInRollback) > exact-balance withdrawal succeeds (the guard is `balance >= amount`) [15.65ms]
(pass) WalletService.requestWithdrawal (runInRollback) > validation matrix: every malformed amount fails pre-DB with WALLET_INVALID_AMOUNT and zero side effects [9.71ms]
 9 pass
 0 fail
 92 expect() calls
Ran 9 tests across 1 file. [321.00ms]
```

`--last` read-back: `Test File: backend/services/billing/wallet.service.test.ts · Exit Code: 0 · Pass: 9 · Fail: 0 · Status: ALL PASSED`.

**Suite 2 — `backend/db/test/repo/billing/wallet.repository.test.ts`** (log: `logs/2026-09-18T19-14-58/backend/db/test/repo/billing/wallet.repository.test.ts.log`)

```
(pass) WalletRepository — transactional paths (runInRollback) > debitForWithdrawalOnce debits the guarded balance, records one pending withdrawal row, and leaves total_earning untouched [7.53ms]
(pass) WalletRepository — transactional paths (runInRollback) > debitForWithdrawalOnce at the exact boundary (balance == amount) succeeds and lands on "0.00" [3.65ms]
(pass) WalletRepository — transactional paths (runInRollback) > insufficient funds: the guarded debit returns null and commits ZERO writes (no orphan ledger row is live) [7.57ms]
(pass) WalletRepository.debitForArbitrationOnce — transactional paths (runInRollback) > an arbitration and a withdrawal racing the same funds guard debit exactly once [20.87ms]
(pass) WalletRepository.debitForArbitrationOnce — transactional paths (runInRollback) > a double arbitration on one wallet debits exactly once — the funds guard serializes the loser [6.69ms]
 24 pass
 0 fail
 115 expect() calls
Ran 24 tests across 1 file. [421.00ms]
```

`--last` read-back: `Test File: backend/db/test/repo/billing/wallet.repository.test.ts · Exit Code: 0 · Pass: 24 · Fail: 0 · Status: ALL PASSED`.

**Combined: 33 pass / 0 fail.** No red suite → no 3.1.SR root-cause investigation was triggered; both suites were green on the first run against the real PostgreSQL 17.11 test DB.

---

## 2. Evidence table (REQ → ticket AC → code → test → green-run → notes)

Every line anchor below was re-read against the live files in THIS run (drift check in §3). "Green" = the suite quote in §1 that contains the named test.

| REQ | Ticket AC | Code `path:line` | Test `file:line` | Green-run result | Notes |
|---|---|---|---|---|---|
| REQ-101 | AC1 — one `withdrawal/pending` row + reserve debit, `total_earning` untouched (INV-W2) | `backend/services/billing/wallet.service.ts:213-218` (`requestWithdrawal`) → `:234-241` (`debitForWithdrawalOnce` call) · `backend/db/repo/billing/wallet.repository.ts:163-172` (`debitForWithdrawalOnce` → `debitWithLedgerRow(insert, TransactionStatus.Pending, …)`) · `backend/db/repo/billing/wallet.repository.shared-writer.ts:74-78` (guarded UPDATE `balance >= amount`) + `:84-94` (ledger INSERT) | `backend/services/billing/wallet.service.test.ts:169-193` (happy path: exactly ONE pending row, balance `100.00→60.00`, `totalEarning` still `100.00`) · `backend/db/test/repo/billing/wallet.repository.test.ts:341-369` (guarded debit, ONE pending row, `total_earning` untouched) | **GREEN** (service 9/9; repo 24/24) | All money oracles are exact decimal-string read-backs (no numeric arithmetic). `sessionId` null + description `"Withdrawal request (pending payout)"` asserted at service `:186-187`. |
| REQ-102 | AC4 — amount grammar/positivity matrix, pre-DB `WALLET_INVALID_AMOUNT`, zero side effects | `backend/services/billing/wallet.service.ts:61` (`WITHDRAWAL_AMOUNT_PATTERN`) · `:75-86` (`assertValidWithdrawalAmount` — trim, grammar, `[1-9]` positivity; throws `ValidationError` at `:83`) · keys: `shared/locale/en/errors/index.ts:93`, ar `:91` | `backend/services/billing/wallet.service.test.ts:251-293` (9-shape matrix: empty/whitespace/`abc`/negative/3-decimals/`.50`/`0`/`0.00`/8-int-digits → each denies `WALLET_INVALID_AMOUNT` + localized message, then zero side effects: 1 wallet row, balance `100.00`, empty ledger) · repo boundary context: `backend/db/test/repo/billing/wallet.repository.test.ts:371-391` (`balance == amount` lands exactly on `"0.00"` — the guard is inclusive, not the source of rejections) | **GREEN** (service 9/9; repo 24/24) | Rejection is pre-DB: the wallet row count and balance are re-read AFTER the whole matrix (`:288-291`). Repo-side inclusivity (`gte`) is the mechanism the service-level exact-balance case rides on. |
| REQ-103 | AC4 — insufficient funds: typed conflict, ZERO committed rows | `backend/services/billing/wallet.service.ts:242-253` (null miss → `ConflictError("WALLET_INSUFFICIENT_FUNDS", t.insufficientBalance)` at `:252`) · `backend/db/repo/billing/wallet.repository.shared-writer.ts:79-83` (guard-miss returns `null` BEFORE any INSERT — no orphan row is ever live) | `backend/services/billing/wallet.service.test.ts:208-235` (zero committed rows: balance still `"10.00"`, ledger `toHaveLength(0)` after the conflict) · `backend/db/test/repo/billing/wallet.repository.test.ts:393-415` (guarded debit returns `null`, wallet + ledger untouched, zero writes) | **GREEN** (service 9/9; repo 24/24) | Service-side orphan-row death is via savepoint rollback; repo-side is debit-first composition (no INSERT on miss). Both layers prove "zero rows" independently. |
| REQ-104 | AC4 ruling — "422" reconciled to the typed `WALLET_INSUFFICIENT_FUNDS` code | `backend/lib/errors/error-code-taxonomy.ts:45-47` (**corrected anchor**; `CONFLICT: 409` `:45`, `VALIDATION: 422` `:47` — see §3) · GraphQL has no per-error HTTP status (`docs/graphql/domain-error-extensions-code.md:9` intro + `:112` testing guidance) · client routes on the CODE: `frontend/views/teacher/wallet/useTeacherWalletWithdraw.ts:86-91` | `backend/services/billing/wallet.service.test.ts:224` via the `expectDomainDenial` oracle (`:55-60`: code equality + localized message equality + code NEVER leaks into the message) — the typed-code contract the wire renders as `errors[].extensions.code` | **GREEN** (service 9/9 — the denial-oracle assertions all passed) | No code change required per the ruling (specs REQ-104). Wire-level rendering of the code re-runs in task 3.3 (GraphQL integration suite). |
| REQ-007 | AC4 — the denial code actually reaches clients over the wire | SDL pins (read-verified this run): `backend/graphql/test/schema-surface.test.ts:250-253` (`WALLET_QUERY_FIELDS = ["myWallet"]` `:251`, `WALLET_MUTATION_FIELDS = ["requestWithdrawal"]` `:253`) · `frontend/graphql/test/warnings/warning-surfacing.test.ts:372` (`requestWithdrawal` in the mutation inventory) · client routing `frontend/views/teacher/wallet/useTeacherWalletWithdraw.ts:86` (`code === "WALLET_INSUFFICIENT_FUNDS"`) | Pointer only (per tasks.md 3.1): the live wire leg is `frontend/graphql/test/admin/admin-finance.integration.test.ts:636-647` — teacher files the payout via `requestWithdrawalMutationDocument` (`:638-642`), pending ledger row captured (`:644-647`) | Pointer (re-run happens in **3.3**) | Tasks.md 3.1 scopes this suite-pair to the service/repo legs; the wire re-run is 3.3's deliverable. SDL/surface anchors were read-verified this run, not re-executed. |
| REQ-501 | AC1/AC4 cross-cutting — BOLA-proof teacher surface | `backend/graphql/query/billing/wallet.query.ts:46-64` (`myWallet` takes NO `args:` block; teacher-only `$all` gate `:52-57`; resolve `:64` → `getMyWallet(ctx.user.id, …)` — identity is ctx-derived) · `backend/graphql/mutation/billing/wallet.mutation.ts:46-50` (`RequestWithdrawalInput` = EXACTLY one field `amount: String!`) · gate `:62-67` | Service suite proves the server-owned-identity flow: `wallet.service.test.ts:169-193` (wallet row resolved server-side from the caller id — no client-supplied wallet id exists anywhere in the input surface; read-back oracles key on the server-created row) | **GREEN** (service 9/9) | Security re-read performed under 3.1.SEC (§5). Wire-level per-role tiers (401/403 ×6) are 3.3's re-run. |
| REQ-505 | AC4/INV-W1 — funds-guard race safety (arbitration ∥ withdrawal) | `backend/db/repo/billing/wallet.repository.shared-writer.ts:74-83` (single guarded UPDATE; miss → `null` before INSERT) | `backend/db/test/repo/billing/wallet.repository.test.ts:819-856` (**arbitration ∥ withdrawal** — `Promise.allSettled` at `:829`; exactly ONE winner `:844-846`; balance drained once to `"0.00"` `:851`; ledger carries only the winner's row `:852-854`) · `:780-817` (**arbitration ∥ arbitration** — same `allSettled` shape at `:790`; one winner, one ledger row) | **GREEN** (repo 24/24) | Actor naming per plan-review R1 finding 11: `:819` is the arbitration-vs-withdrawal race; `:780` races two arbitrations. Both prove "exactly one debit lands" via `Promise.allSettled`. The genuine two-REQUEST drain race is finsec journey step D (`test/workflows/billing/financial-safety-verification.journey.test.ts:565-593`) — re-cited in task 3.3. |

**Wire-level teacher request leg pointer (tasks.md 3.1, last bullet):** `frontend/graphql/test/admin/admin-finance.integration.test.ts:638-647` — verified live: `:636` test start ("admin → teacher payout request surfaces on the pending queue → approve settles it completed"), `:638-642` the `requestWithdrawal` mutation with `input: { amount: PAYOUT_AMOUNT }` under the teacher's bearer context, `:643-647` the updated-wallet assertion + pending-row capture (`:648` tracks the ledger id for teardown). Per tasks.md, this leg's re-run is executed in **task 3.3** — cited here as a pointer only.

---

## 3. Anchor verification & corrections (every tasks.md 3.1 anchor checked against the live files)

| Cited anchor (tasks.md 3.1 / specs) | Live check | Verdict |
|---|---|---|
| `wallet.service.test.ts:169` (AC1 happy path) | `:169` = `test("happy path: exactly ONE pending withdrawal row, balance debited, total_earning untouched, ledger refreshed"…` | ✅ exact |
| `wallet.repository.test.ts:341` (`debitForWithdrawalOnce`) | `:341` = `test("debitForWithdrawalOnce debits the guarded balance, records one pending withdrawal row, …"` | ✅ exact |
| `wallet.service.test.ts:251` (`WALLET_INVALID_AMOUNT` matrix) | `:251` = `test("validation matrix: every malformed amount fails pre-DB with WALLET_INVALID_AMOUNT…"` | ✅ exact |
| `wallet.service.test.ts:237` (exact-balance boundary) | `:237` = `test("exact-balance withdrawal succeeds (the guard is `balance >= amount`)"` | ✅ exact |
| `wallet.service.test.ts:208` (zero committed rows) | `:208` = `test("insufficient funds: localized WALLET_INSUFFICIENT_FUNDS conflict and ZERO committed rows…"` | ✅ exact |
| `wallet.repository.test.ts:393` (guarded debit → null) | `:393` = `test("insufficient funds: the guarded debit returns null and commits ZERO writes…"` | ✅ exact |
| `wallet.repository.test.ts:819` (arbitration ∥ withdrawal) | `:819` = `test("an arbitration and a withdrawal racing the same funds guard debit exactly once"`; `Promise.allSettled` at `:829` | ✅ exact |
| `wallet.repository.test.ts:780` (second funds-guard race) | `:780` = `test("a double arbitration on one wallet debits exactly once…"`; `Promise.allSettled` at `:790` | ✅ exact line; actor note: this one races arbitration ∥ arbitration (`:819` is the arbitration ∥ withdrawal race) — consistent with plan-review R1 finding 11 |
| `frontend/graphql/test/admin/admin-finance.integration.test.ts:638-647` (teacher request leg) | `:638-642` request mutation; `:643-647` pending-row capture (test starts `:636`) | ✅ exact |

**Corrections found during the wider citation sweep (recorded here; cross-file write-back belongs to the orchestrator / 0.2.SR / 4.1):**

1. **`backend/lib/errors/error-code-taxonomy.ts` — CITED `:52-62`, CORRECT `:45-47`.** `specs.md` REQ-104, `plan.md` §8 A8, and `deferred-items.md` D7 cite `:52-62` for `VALIDATION: 422` / `CONFLICT: 409`. The live `ERROR_CODE_HTTP_STATUS` rows are `CONFLICT: 409` at **`:45`** and `VALIDATION: 422` at **`:47`**; `:52-62` is the `LEGACY_ERROR_CODE_ALIASES` comment block. Content-faithful, line-drifted. This outcome uses `:45-47`. (No REQ-104 semantics change — the ruling itself is intact; `VALIDATION→422`/`CONFLICT→409` is exactly what the ruling describes.)
2. **`backend/graphql/test/schema-surface.test.ts` — cited `:250-252`, live span `:250-253`** (query row comment `:250` + array `:251`, mutation row comment `:252` + array `:253`). Off-by-one on the mutation row; cited as `:250-253` above.
3. All other anchors used in the table (service/repo/GraphQL/client-routing/locale keys en `:90,:93` / ar `:88,:91`) verified exact.

---

## 4. 3.1.QL — Quality Loop

Read-only re-runs; no file edited. Sub-loop (`scripts/health/sub-loop.ts`) applies to TS source/test files — none touched, so no sub-loop run is due; the run-test outputs quoted in §1 and the `--last` read-backs are the recorded evidence. Markdown-only artifacts produced: this outcome file + the tasks.md checkbox flips (§7).

## 5. 3.1.SEC — Security & Tenancy Audit (BOLA-proof request surface re-read)

Re-read the cited surface lines while the suites ran green:

- **`myWallet` is zero-arg and ctx-derived** — `backend/graphql/query/billing/wallet.query.ts:46-64`: the field definition carries NO `args:` block (only `type`/`description`/`authScopes`), the `$all` conjunction (`authenticated: true`, `role: [UserRole.Teacher]`) gates it (`:52-57`), and the resolve calls `WalletService.getMyWallet(ctx.user.id, ctx.locale)` at `:64` — the wallet "address" is the verified context identity; a caller cannot name any other wallet.
- **`requestWithdrawal` accepts amount-ONLY input** — `backend/graphql/mutation/billing/wallet.mutation.ts:46-50`: `RequestWithdrawalInput` declares exactly one field, `amount: String!` — no wallet id, no balance, no currency on the wire; teacher-only `$all` gate at `:62-67`.
- **Service honors the same discipline** — `wallet.service.ts:213-218` takes `callerUserId` (threaded from ctx by the resolver) and resolves the wallet row itself via `ensureWalletOnce(callerUserId, tx)` (`:232`); no code path accepts a client-supplied wallet id. `total_earning` is never client-writable (INV-W2 asserted by both suites).
- **Cross-teacher protection at the data tier** — the guarded UPDATE predicates on `wallet.id` resolved from the caller (`shared-writer.ts:74-78`); the repo suite's read-back oracles always key on the server-created wallet row for the fixture teacher.

Verdict: the request surface remains BOLA-proof — zero-arg read, amount-only write, identity server-owned end-to-end. No id-from-client input exists anywhere on this surface.

## 6. 3.1.SR — Semantic Review

- **No failures → no root-cause investigations.** Both suites were green on first run (§1); per the 3.1.SR rule, a red suite would have been a finding — none materialized. No production code was touched.
- **File-edit audit (verified by `git status` at task start AND after execution):** the ONLY writes made by task 3.1 are this outcome file and the tasks.md **3.1** checkbox flips (header + 3.1.QL/.TE/.SEC/.SR/.IV — confirmed line-scoped in the git diff). Concurrent sibling-task artifacts were observed in the shared worktree and are explicitly NOT this task's writes: `M test/workflows/billing/admin-financial-auditing.journey.test.ts` (task 1.1's journey leg), `outcome/3.2-admin-settlement-outcome.md` + tasks.md 3.2 checkbox flips (task 3.2's subagent). Source, tests, docs, schema: untouched by 3.1.
- **Env-config observation (no action):** the suites ran with `DB_PROVIDER=postgres` injected from `.env.test` (19 vars) against the real PostgreSQL 17.11 at `127.0.0.1:5432/app_db`, matching the plan's environment contract.
- **No dead code / no cross-layer imports / no plan-artifact references** were introduced — nothing was written outside the plan directory.

## 7. 3.1.IV — Instruction Verification

Layer AGENTS.md files read for the suites' layers (none edited):

- `AGENTS.md` (root) — §Code Quality Workflow `:173-187` re-read (quality-gate discipline; cache-preservation rules).
- `backend/AGENTS.md` (97 lines) — incl. §6 Test Layer rules (`runInRollback` wrapper + `tx` passed to ALL repo methods, `:44-48`), error-handling taxonomy rule (`:92`), no `oxlint-disable` (`:87`).
- `backend/services/AGENTS.md` (50 lines) — §Testing rules (service tests live next to code, no real external APIs, no integration files under `backend/services/`, `:22-38`), i18n localized-errors rule (`:7`).
- `backend/db/test/AGENTS.md` (127 lines) — all 20 rules re-read; verified against the suites as observed: every case inside `runInRollback` with `tx` threaded (rule 1/16); no `expect(...).rejects.toThrow()` — try/catch helpers (`expectDomainDenial`, typed-stub guards) instead (rule 3); committed fixtures in the standalone-executor block are hard-deleted in `afterAll` in FK order under the sanctioned trigger suspension (rules 9 / `wallet.repository.test.ts:894-923`); `logic/` vs `repo/` split respected (rule 11).
- Absences noted: no `backend/db/AGENTS.md` and no `backend/db/test/repo/AGENTS.md` exist — the deepest applicable layer instruction for the repo suite is `backend/db/test/AGENTS.md`.
- `test/workflows/AGENTS.md` was NOT due here (no workflow file touched; its rules gate tasks 1.1/3.3).

## 8. Requirements coverage for this task

`_Requirements: REQ-101, REQ-102, REQ-103, REQ-104, REQ-007, REQ-501, REQ-505_` — every clause carries a green citation in §2 (REQ-007's wire leg + REQ-505's two-request drain race are pointer rows into task 3.3, exactly as tasks.md 3.1 scopes them).

## 9. Carry-overs for the orchestrator

1. **Taxonomy anchor write-back** (§3 correction 1): `error-code-taxonomy.ts:52-62` → `:45-47` in `specs.md` REQ-104, `plan.md` §8 A8, `deferred-items.md` D7 — one-line citation fixes, left to the owning tasks (0.2.SR ruling / 4.1 matrix) per this task's read-only scope.
2. Schema-surface citation `:250-252` → `:250-253` (§3 correction 2) — cosmetic, fold into 4.1 if the matrix cites it.
3. All sibling outcome files (`0-baseline`, `1.1`, `2.1`, `2.2`, `plan-review-R2`) were not yet present in `outcome/` at this task's start; the pre-execution read covered the one file that existed (`plan-review-R1.md`). During execution, task 3.2's outcome landed concurrently (shared worktree) — no dependency of 3.1 was blocked by any of this; 3.1's inputs (the two suites) are self-contained.
4. **Shared-file concurrency note:** `tasks.md` is being checkbox-flipped by sibling subagents in the same worktree (3.2 observed mid-run). My flips were applied with line-scoped edits touching only the 3.1 block; no sibling content was altered or reverted.
