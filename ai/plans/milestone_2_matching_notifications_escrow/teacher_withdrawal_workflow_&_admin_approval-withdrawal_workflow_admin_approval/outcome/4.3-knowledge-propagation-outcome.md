# Task 4.3 Outcome — Knowledge Propagation (`docs/billing/` addendum + outcome synthesis)

**Task:** `### 4.3 Knowledge Propagation — docs/billing/ addendum + outcome synthesis` (Phase 4 — FINAL task of the plan)
**Plan:** `ai/plans/milestone_2_matching_notifications_escrow/teacher_withdrawal_workflow_&_admin_approval-withdrawal_workflow_admin_approval/`
**Date:** 2026-09-18 · **Branch:** `feat/teacher-withdrawal-workflow-admin-approval`
**Kind:** docs-only propagation + synthesis. Zero code edits, zero test runs (no suite is scoped to 4.3); the only repo-tree edit is the appended addendum block.

---

## 1. Recurring patterns & pitfalls extracted from `outcome/` (each seen 2+ times)

| # | Pattern / pitfall | Where it recurred (outcome citations) | Carry-forward rule |
|---|---|---|---|
| P1 | **PGlite single-connection shim vs real-pool semantics.** The pool shim hands every "connection" the SAME WASM session (`backend/db/pglite-pool.ts:12-16, 277-289`); Drizzle only acquires a dedicated client for a real `Pool` (`isPool` check), so nested top-level transactions (admin list reads: `withTransaction` + `readInSnapshot`) intermittently hit `25001`, and true-concurrency races corrupt outcomes on the shared session. | `outcome/environment-addendum.md` §1 (timeline steps 1–3, root cause) · `outcome/1.1-journey-gapfill-outcome.md` §5 (full mechanism + fix-or-report ruling) · same failures byte-identical at HEAD in the stash attribution run (`outcome/1.1` §4.2) | Test suites written against pool semantics (nested snapshot reads, `Promise.allSettled` races) MUST run against real PostgreSQL — the Phase 3 evidence base is green only under `DB_PROVIDER=postgres` (`outcome/environment-addendum.md` §1 step 6–7; `outcome/3.3-journey-wire-outcome.md` header). Typecheck/lint baselines are provider-independent (`outcome/environment-addendum.md` §3; `outcome/4.2-final-gate-outcome.md` §3). A pglite data-dir reset is NOT a fix — the same-session nested `BEGIN` hazard is structural (`outcome/1.1` §5). |
| P2 | **DBML annotation drift behind Drizzle CHECK changes.** Migrations tightened a CHECK without following the DBML: `teacher_transaction.amount` was repaired `>= 0` → `> 0` (one-line diff), and the SAME drift class was discovered at `student_payments.amount` (`db/schema.dbml:401` vs `backend/db/schema/billing/student-payments.ts:74`) — queued as forward-item D8, not silently absorbed. | `outcome/2.1-dbml-repair-outcome.md` §1–§2 (repair), §4 (three-way agreement), §11.1 (sibling drift report) · `outcome/midpoint-review-R1.md` §3 finding 3 / §4 fix 3 (D8 row added) | When a migration changes a CHECK bound, the DBML annotation must change in the same commit; a live-DB three-way probe (DBML ↔ Drizzle ↔ `pg_constraint`) is the cheap verification (`outcome/2.1` §4). |
| P3 | **Plan-file citation drift.** Function-span anchors drift more than signature-line anchors (20 R1 findings were overwhelmingly span corrections); journey-file anchors shift with ANY insertion (the 1.1 header bullet + constant shifted every step anchor +8/+10), and 3.3's "corrected map" was itself superseded by 1.1's verified map at the mid-point review — re-derivation from the live file at consolidation time is mandatory. | `outcome/plan-review-R1.md` (Lessons: function-span vs anchor citations; §Detailed Findings 1–17) · `outcome/3.3-journey-wire-outcome.md` §5 (+8 map) superseded by `outcome/midpoint-review-R1.md` §3 finding 2 → `outcome/1.1-journey-gapfill-outcome.md` §3 map (verified exact; step 8 = `:935-992`) · `outcome/verification-matrix.md` (anchors re-verified line-by-line at write time) | Cite the signature line + a precise landmark, not the whole body; at consolidation time, re-derive every anchor from the live tree and name WHICH outcome's map is authoritative (3.3 §5 is superseded by 1.1 §3). |
| P4 | **Sandbox daemon force-checking out the default branch.** An external process alternated the working tree between `main` and the feature branch during execution (visible in `git reflog`); every gate had to run behind a re-checkout guard. | `outcome/4.2-final-gate-outcome.md` §9 (workspace note; reflog 20:20–20:25Z) · re-observed during this 4.3 session (reflog flip to `main` mid-session; all work re-done behind `git checkout -f feat/…` + branch-verify guards) | Use atomic checkout→verify→work→commit chains; re-verify the branch immediately before `git add`/`git commit`; keep intermediate artifacts in untracked paths (they survive `-f` checkouts). |
| P5 | **Append-only outcome corrections (audit-trail style).** Superseded narratives are CORRECTED BY APPENDING a dated corrections section, never by rewriting history — 3.3's §0 authorship narrative and +10-stale §5 map were superseded via an appended `## Corrections (mid-point review R1)` block with the original content untouched. | `outcome/3.3-journey-wire-outcome.md` §Corrections (appended; original sections intact) · `outcome/midpoint-review-R1.md` §4 fix 2 (append-only ruling) · `outcome/plan-review-R2.md` §4 ("rewriting them would falsify history" — 0-baseline's drift record left byte-identical) | Corrections append; history stays falsifiable. A superseded map must say in place which map now governs. |
| P6 | **Guarded-predicate UPDATEs (no `SELECT FOR UPDATE`) make races lose by construction — re-proven, not redesigned.** Concurrent double-settle and settle∥new-request journeys green ×2 under a real pool; exactly one winner, one audit row, one balance movement; the pending predicate is the arbiter. The D2/D5 forward contracts were left unchanged. | `outcome/3.3-journey-wire-outcome.md` §1 runs 1–2 + §2 steps 6/7 · `outcome/verification-matrix.md` REQ-505/REQ-702 rows + "Forward-contract rows: D1–D8 = 8, all 🔄 Open" · `outcome/4.2-final-gate-outcome.md` §1 (ledger enforcement grep = 0) | When verification re-proves a design, record the proof and keep the forward contract open with its owner — do not convert a proven ruling into new scope. |
| P7 | **Reported-not-fixed needs an explicit owner + target round, and verification evidence must be real captured output.** Report-only findings were queued with owner/round (D7 anchor → 0.2; student_payments DBML → D8 owner; pglite blocker → orchestrator with options), and every green claim in this plan cites captured runner output, never memory. | `outcome/0-baseline-outcome.md` §4 (D7 queued for 0.2) · `outcome/2.1-dbml-repair-outcome.md` §11.1 (D8 queued with owner) · `outcome/1.1-journey-gapfill-outcome.md` §12.1 (blocker carry-forward with options) · `outcome/plan-review-R2.md` (Lessons: "queued with an explicit owner and target round … otherwise reported-not-fixed items evaporate") · evidence discipline: `outcome/0-baseline` §1, `outcome/1.1` §4, `outcome/verification-matrix.md` §0 | Every "reported not fixed" lands in a ledger row or a named handoff with an owner; every green claim quotes the runner line. |

## 2. Propagation decision — docs-only; instruction files are hand-curated

`tasks.md` task 4.3 prescribes the boundary verbatim:

> "AGENTS.md and `.agents/instructions/*.instructions.md` are hand-curated — NEVER updated from plan outcomes."

**Decision: NO edits to `AGENTS.md` or `.agents/instructions/*.instructions.md`.** Those files encode durable layer conventions and are maintained by their own curation process; plan-run lessons do not overwrite them. Plan knowledge propagates through exactly two sanctioned channels:

1. **The canonical settlement doc** — `docs/billing/admin-financial-auditing.md` gains the verification addendum (§3 below), so future readers of the reference see the re-proven facts with anchors, not plan meta.
2. **The plan's own outcome knowledge base** — this file plus `outcome/verification-matrix.md`, which the orchestrator can mine for future plans.

## 3. The addendum — location and content

**Location:** appended to `docs/billing/admin-financial-auditing.md` as `## Verification addendum (2026-09-18)` — lines **291–303** (title at `:294`, six fact bullets at `:298–303`). The pre-existing §1–§8 are byte-untouched (append-only diff). Every bullet was re-verified against the live tree before writing (journey step-8 leg `:935-992`; `db/schema.dbml:372` = `amount > 0`; §6.3 lines `:149/:155/:160`; `notification-type.enum.ts:10-18`). Bullets state: the failed-row re-settle journey arm (step 8), the three-way DBML agreement at `> 0`, the §6.3 reserve-at-request diagram wording, the re-proven race discipline, the 8-suite/142-test green evidence base under real PostgreSQL 17.11, and the zero-dispatch withdrawal behavior. Internal plan artifacts are referenced only as evidence paths (`outcome/verification-matrix.md`, `outcome/environment-addendum.md`) — no task/REQ identifiers.

## 4. Subtask ledger (4.3.x)

- **4.3.QL Quality Loop:** markdown inspection only — inspection; no repo doc validator for billing docs beyond mermaid (untouched). The repo's only doc validator is `validate:mermaid` (`package.json:71`; investigated in `outcome/2.1-dbml-repair-outcome.md` §3.2 — no `validate:dbml`, no markdown linter for `docs/billing/`), the addendum adds no mermaid fence, and the §6.3 diagram was validated at 2.2 and not touched here — so the mermaid gate has no new input. Inspection evidence: append-only diff confined to the new block; every cited line anchor re-read from the live file (§3). ✅
- **4.3.TE Test Engineering:** n/a per tasks.md (no suite scoped to 4.3; the verification evidence the addendum cites is the already-captured Phase 3/4 output — `outcome/verification-matrix.md` §0). ✅
- **4.3.SEC Security & Tenancy Audit:** n/a — documentation-only; no actor, tenancy, or query surface touched. ✅
- **4.3.SR Semantic Review:** the addendum states facts with citations (file:line anchors + outcome-doc evidence paths); no scope creep into other sections — §1–§8 of the canonical doc are byte-identical, the block is appended at EOF. ✅
- **4.3.IV Instruction Verification:** root `AGENTS.md` "AI Agent Communication Rules" re-read — "**NEVER create summary markdown files** (SUMMARY.md, CHANGES.md, REPORT.md, etc.)" with the exception for project-structure documentation. The addendum IS project-structure documentation for `docs/billing/`; this outcome file is the plan-protocol-sanctioned write-back (same ruling recorded by `outcome/2.1` §8 and `outcome/2.2` §6). No summary files were created anywhere else. ✅

## 5. Task closure — plan execution complete

- **All 10 predecessor tasks (0.1 → 4.2) were executed and gated before this task:** every heading and subtask checkbox in `tasks.md` was already `[x]`, each with its outcome file under `outcome/` (0-baseline, plan-review-R2, 1.1, 2.1, 2.2, 3.1, 3.2, 3.3, verification-matrix, 4.2-final-gate), plus the review gates `outcome/plan-review-R1.md` and `outcome/midpoint-review-R1.md` and the environment record `outcome/environment-addendum.md`.
- **With 4.3, every task block in `tasks.md` is `[x]` (11/11: 0.1, 0.2, 1.1, 2.1, 2.2, 3.1, 3.2, 3.3, 4.1, 4.2, 4.3).**
- **Final gate standing (from `outcome/4.2-final-gate-outcome.md`):** deferred-items enforcement grep = 0 · traceability loop silent · baseline compare delta = 0 (tsgo 0 errors, lint-service PASS) · journey sub-loop exit 0 · 8 suites green this plan-run (142 tests / 0 failures; 150 executions with the stability re-run) under real PostgreSQL 17.11 per `outcome/environment-addendum.md`.
- **Plan verdict: ALL TASKS EXECUTED, GATES GREEN — the verification plan is CLOSED.** Forward items D1–D8 remain 🔄 Open forward-contracts/rulings with named owners (enforced at 4.2); nothing was silently absorbed.

## 6. Files touched by this task

- `docs/billing/admin-financial-auditing.md` — appended `## Verification addendum (2026-09-18)` (lines 291–303; append-only).
- `outcome/4.3-knowledge-propagation-outcome.md` — this file (new).
- `tasks.md` — task `4.3` heading + `4.3.QL/.TE/.SEC/.SR/.IV` checkboxes flipped to `[x]` (nothing else).
- Workspace note: the sandbox daemon flipped the tree to `main` mid-session again (P4); the commit was made behind an atomic checkout-verify-edit-commit chain with the branch re-verified immediately before `git add`.
