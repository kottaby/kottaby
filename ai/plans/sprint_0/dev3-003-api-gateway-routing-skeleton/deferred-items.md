# Deferred Items Ledger

**Feature:** `dev3-003-api-gateway-routing-skeleton`  
**Plan Directory:** `ai/plans/sprint_0/dev3-003-api-gateway-routing-skeleton/`  
**Created:** `2026-08-26` · **Initialized (Task 0.1):** `2026-08-27`

---

## Purpose

This ledger tracks all work deferred from one task to another to ensure no deferred item is forgotten. Every deferred item must be explicitly logged here and resolved before the plan is marked complete.

---

## Ledger Table

| ID | Deferred Item | Source Task | Target Task | Status | Verified By | Notes |
|---|---|---|---|---|---|---|
| BLT-01 | Gateway HTTP-layer per-IP throttling (REQ-035): live route middleware is fail-open in-memory only; durable per-IP limiting out of this ticket's scope | 0.1 | Production-hardening / Sprint-4 ticket | ⚠️ Partial · ✅-targeted | plan-review-R1 §F8 + Task 5.x rate-limit probes · **6.4 pentest wave addendum:** limiter stub proven inert live (XFF burst ×5 → remaining pinned 100); XFF/x-real-ip key spoofable — requires trusted-proxy stripping when the real limiter lands | Preserve fail-open ordering & 429 transport block during Task 3.2; Sprint-4 owns the durable tier. No ❌ for pre-seeds per REQ-083. Backend 6.2 LOW-1 rides here: declared-Content-Length bypass allows unbounded server-side buffering before the drained-length checkpoint — bound the read when hardening. |
| BLT-02 | DB-backed readiness probe (REQ-012 tail): `HealthCheck` payload is DB-free by design; deep readiness against the database is a future surface | 0.1 | Future readiness-probe ticket | ⚠️ Partial · ✅-targeted | plan-review-R1 §(c) adaptations | Sandbox has no Postgres (`DB_PROVIDER=sqlite`); keep Task 3.1 service zero-repo so this stays a clean future extension point. |
| BLT-03 | Optional `healthCheckQueryDocument` frontend document (REQ-062 tail) — optional TypedDocumentNode per frontend/graphql conventions | 0.1 | First consumer (DEV3-001 CI smoke / observability tooling) | ✅ Done · create-deferral DECIDED ✅-targeted by Task 4.1 | plan-review-R1 §F11 + REQ-062 tail · **outcome/4.1-outcome.md** (evidence-based NO verdict, C2-phase4a) | Decision recorded 2026-08-27 (C2-phase4a): document NOT created — ZERO GraphQL consumers exist (`gql/graphql.ts` byte-identical since 3.1 = no health document generated anywhere); sole health consumer on the tree is `frontend/components/ApiStatusIndicator.tsx` which polls REST `/api/health` via plain `fetch`, not Apollo. Codegen NOT rerun; artifacts byte-identical verified via git status. When the first GraphQL consumer lands: `<entityName>QueryDocument`, `TypedDocumentNode<HealthCheckQuery>` single type param, embedded-type `keyFields:false` cache entry MANDATORY (D4 — already shipped in apolloCache.ts), subdir barrel + layout doc note. |
| BLT-04 | `/api/set-locale` envelope adoption — REFERENCE ROW ONLY: envelope already ADOPTED at HEAD (`apiSuccessResponse`/`apiErrorResponse`/`resolveRequestId` consumed; verified during DEV3-002 and re-confirmed by R1 §F6) | 0.1 | — (nothing to adopt) | ✅ Done | outcome/plan-review-R1.md §F6 · DEV3-002 Task 6-x2 regression suite (27/0 green) | Gate correction applied to task-def pre-seed row #6: classification = "envelope (adopted)", not deferred. Registry must carry it as `classification:"envelope"`. |
| BLT-05 | GraphQL query depth/complexity limiting (plan §6.5) — not implemented by this skeleton ticket | 0.1 | Sprint-4 hardening ticket (plan §6.5) | ⚠️ Partial · ✅-targeted | plan §6.5 deferral of record · **6.4 pentest wave addendum (F-01, MEDIUM·pre-existing):** `allowBatchedHttpRequests:true` + no op/alias cap + inert limiter ⇒ one HTTP request carrying an array/aliased batch executes N engine pipelines (live probe: array ×25 + aliases ×40 fully executed) | Must not regress gateway latency posture when Task 3.2 restructuring lands. Cap batch size + op/alias count when Sprint-4 lands (or flip `allowBatchedHttpRequests` if batched HTTP is never a product need). |
| BLT-06 | [HIGH·precondition] Placeholder `Query._health` removal BEFORE new health resolver registration — Pothos throws duplicate-field error if both exist (R1 F1, `builder.ts:137–145`) | GATE-R1 / 0.1 | **Task 3.1 itself** | ✅ Done (discharged by Task 3.1 — placeholder deleted, sanctioned delta realized) | plan-review-R1.md §F1 | Owned BY Task 3.1 (delete placeholder from `queryType` + side-effect-register `query/health.query.ts`); expected codegen delta = `_health` retyped `String!`→`HealthCheck!` + `HealthCheck` type added — record as the ONLY sanctioned schema delta besides the new type (REQ-060 gate reading). |
| BLT-07 | [HIGH] Test-harness liveness probe invalidated by the sanctioned Task 3.1 retyping — `test/helpers/test-lifecycle.ts` `pollOnce` probes `{ _health }`; since `_health: HealthCheck!` that document fails validation (object needs subfields; live probe evidence: HTTP 400 `GRAPHQL_VALIDATION_FAILED`, whereas `{ _health { status } }` → 200), so `waitForServer` can NEVER succeed even on a healthy server ⇒ every `setupTestServerLifecycle` suite (error-contract-matrix, auth, warning-surfacing) is inoperable — compounded by the Next.js 16 dev-server singleton lock ("Another next server is already running", PID of the :3000 process) whenever the interactive dev server lives | C2-phase3b (3.2/3.3 gating) | **Task 5.x harness prep** — **RESOLVED this cycle**: poll doc fixed by owner `0478a6f`; per-dir dev-lock solved via isolated-mirror recipe (rsync → /tmp, real bun install, run inside mirror) — live-wire tiers EXECUTED green (5.1: 14/0, 5.3: 3/0) | ✅ Done (recipe documented in outcome/5.1-outcome.md; CI keeps direct-boot path) | outcome/3.2-outcome.md §env-locked tier · outcome/3.3-outcome.md §DEV2-001 regression table · spawn-log + live :3000 curl evidence · **6.3 wave addendum (R-2):** `frontend/graphql/test/gateway/allowlist-coverage.test.ts` delegated-runner tier also hangs >120s while :3000 lives (UMD/BLT-07 family); 8/0 verbatim-copy evidence stands | Deliberately NOT locally patched by C2-phase3b (test-lifecycle.ts outside its sanctioned file set; REQ-004 extend-in-place discipline for owning streams). One-line class fix when owned: change the poll document to `{ _health { status } }`. Sandbox runner alternative while :3000 must stay up: out-of-band lifecycle start point or dist-dir/port isolation honored by the helper. |
| BLT-08 | [LOW·NEW 6.4] Sanctioned-but-dead media type `application/graphql-response-json` sits in `ALLOWED_CONTENT_TYPES` (transport-guard.ts) yet its requests always land in the raw-Apollo-branch (BLT-09's leak path) — value never realized | C3-6.4 (6.4-pentester-outcome.md F-03) | Gateway hardening ticket (Sprint-4) or DEV3-002-envelope follow-up | ⚠️ Partial · ✅-targeted | outcome/6.4-pentester-outcome.md probe table | Drop from allowlist (→ 415) OR implement graphql-response rendering; either way update transport-guard suite CT matrix in the same change set. |
| BLT-09 | [LOW·PRE-EXISTING 6.4] Dev-only info leak: `POST {}` with `application/json` bypasses the finalizer and returns raw Apollo BadRequestError incl. absolute FS-path `stacktrace[]` and no requestId (prod stacks are off — dev-only exposure) | C3-6.4 (6.4-pentester-outcome.md F-02) | DEV3-002 finalizer-gap register owner (plan closed → carry in gateway hardening ticket) | ⚠️ Partial (dev-only) | outcome/6.4-pentester-outcome.md F-02 probe | Route-side pre-finalizer catch for empty-body/parse-class errors would fold these into the masked envelope; coordinate with error-handling-contract.md exemption rows. |
| BLT-10 | [LOW·NEW 6.4] `GET /api/health` 405 response lacks the `Allow` header (GraphQL route's 405 matrix has it; the REST probe route does not) | C3-6.4 (6.4-pentester-outcome.md F-06) | Gateway hardening ticket (Sprint-4) | ⚠️ Partial · ✅-targeted | outcome/6.4-pentester-outcome.md probe table | One-line `Allow: GET, HEAD, OPTIONS` header on the 405 branch + health-route.probe suite pin. |

### Dropped pre-seeds (gate corrections — renumber-free)

- ~~Row #4 `/api/logs` envelope adoption~~ — **DROPPED**: `/api/logs` does not exist on disk at HEAD (phantom route; R1 §F6). Nothing to adopt.
- ~~Row #5 `/api/cron/ticker`, `/api/cron/execute` envelope adoption~~ — **DROPPED**: cron routes do not exist on disk at HEAD (phantom routes; R1 §F6). Nothing to adopt.
- True route inventory drives registry scope (Tasks 2.2/7.1): exactly `{ app/api/graphql/route.ts (gateway), app/api/set-locale/route.ts (envelope, adopted) }` + NEW health route (envelope).

---

## Status Values

- ✅ **Done** — Item completed and verified
- ⚠️ **Partial** — Partially completed, needs follow-up work
- ❌ **Blocked** — Not resolved, plan cannot complete until addressed
- 🔄 **In Progress** — Currently being worked on
