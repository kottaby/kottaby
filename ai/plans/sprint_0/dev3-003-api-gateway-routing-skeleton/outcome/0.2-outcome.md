# 0.2 Outcome — Prerequisite Substrate Verification (dev3-003)

- **Date:** 2026-08-27 · **Commit:** `12120dd`
- **Method:** Ground truth was authoritatively established by `outcome/plan-review-R1.md` §Ground-Truth Inventory #1–#22 + §Corrections #1–#10 during the Phase-1.5 gate (REQ-082). Per orchestration decision this task **incorporates those rows/corrections by reference** instead of re-deriving them; ONLY the five residual items were cross-checked fresh. Baseline/tooling execution evidence lives in `outcome/phase0-baseline-outcome.md`.

## Fresh residual cross-checks (evidence = path + grep lines, full quotes in phase0 doc §"Task 0.2")
| Check | Verdict | Evidence |
|---|---|---|
| `ctx.authCookieOut` accumulator shape | ✅ FOUND | `backend/graphql/gqlContextFactory.ts:73` (`readonly authCookieOut: AuthCookieOut`), `:156` (`createAuthCookieOut()`), `:198` (returned); route flush via `headers.append("Set-Cookie")`; adjacent `ctx.requestId` pre-landed (:54/:143/:190) |
| `RegisterPublicRole` schema-layer gate on registerUser | ✅ FOUND | `backend/graphql/pothos/auth/register-input.pothos.ts:32` (`role: t.field({ type: RegisterPublicRolePothosEnum, required: true })`); enum values `{student, teacher, parent}` — admin excluded, BFLA REQ-022 (`backend/enum/users/register-public-role.enum.ts:11-15`) |
| Harness exports via `@/test/helpers` re-export index | ✅ FOUND | `test/helpers/index.ts:2` (`testClient`, `extractErrorCode`, `TEST_PORT`) + `:9` (`setupTestServerLifecycle`) |
| `apolloCache.ts` keyFields precedents | ✅ FOUND | `frontend/providers/apollo/apolloCache.ts:28-29` (`AdminNoteInfo { keyFields: false }`) + `:31-32` (`OnlineMeetingInfo { keyFields: false }`) |
| `GRAPHQL_MAX_BODY_BYTES` inline const line | ✅ CONFIRMED | `app/api/graphql/route.ts:29` (`= 2_000_000`), guards `:68` / `:78`; NOT present under plan's name `MAX_GRAPHQL_BODY_BYTES` → naming decision deferred to Task 2.2 per R1 Correction #4 |

## Dispositions recorded by reference from gate R1 (no new verification needed)
- F3: `demoLogin`/`IS_DEMO` **N/A — operation absent at HEAD**; no ❌ row.
- F4/F4a: scope wiring = `builder.ts:67-130` (claimed name `buildAuthScopes` wrong); `permission` scope remains `()=>true` stub — permission-depth enforcement MUST NOT be claimed exercised anywhere.
- #13: no `env-config-keys.ts` exists ⇒ `APP_VERSION` registration NOT mandatory; `resolveAppVersion()` may read `process.env` directly (recorded for Task 2.x).
- Route enumeration @HEAD: exactly `{ app/api/graphql/route.ts, app/api/set-locale/route.ts }` (+ NEW health route later) — phantom plan §3.5 rows dropped from ledger already in Task 0.1.

## SR/OC
- **SR:** No missing/mis-shaped artifact found among dependencies ⇒ zero new ❌ ledger rows required; blockers absent. **Zero substrate edits made** (read-only greps only).
- **OC:** This stub + phase0 doc carry the evidence; boxes flipped immediately after write.

_Requirements satisfied: REQ-004, REQ-019, REQ-035._
