# Task 1.1 Outcome — Create Canonical Gateway Types (`backend/types/gateway/`)

- **Task ID:** C1-phase1 · dev3-003 Phase 1 · REQ-003
- **Date:** 2026-08-27 · **Base:** HEAD `12120dd` + Phase 0 baseline artifacts
- **Pre-reads honored:** tasks.md §Phase 1 (1.1 + sub-gates), plan-review-R1.md Corrections #1–#10 (#3 confirms "no drift found in types subtree"; #10 sprint_0 artifact paths), phase0-baseline-outcome.md (biome-only lint signal, port lock), `backend/types/AGENTS.md`, DEV3-002 `backend/types/errors/api-error.types.ts` precedent (interface shape, docblock density, `{Symbol}ReturnType` naming).
- **Gate finding honored:** `TransportErrorKind` shipped as a TS string union — ZERO enums anywhere (not DB/Drizzle/Pothos) per D3.

---

## Files created / modified

| File | Action | Why |
|---|---|---|
| `backend/types/gateway/health-check.types.ts` | CREATED | REQ-003 canonical health payload contract (no `Schema` suffix — not a DB table). |
| `backend/types/gateway/gateway-context.types.ts` | CREATED | Gateway context documentary metadata + transport guard result contract for Tasks 2.2/3.2/3.3. |
| `backend/types/gateway/index.ts` | CREATED | Domain barrel, `./`-relative `export *` only (AGENTS.md barrel rule). |
| `backend/types/index.ts` | MODIFIED | Added `export * from "./gateway";` keeping alphabetical order (after `./errors`). Docblock inventory sentence updated to record the second non-entity transport-contract domain. |

Files explicitly NOT touched (constraint verification): no `backend/types/gateway/AGENTS.md` exists → noted here per task requirement (none to read); no service/lib/graphql layers; zero cross-layer imports anywhere in the new subtree.

---

## Exported names (CARRY-FORWARD for Phase 2/3 implementers)

Import from **`@/backend/types`** (barrel): `HealthCheckReturnType`, `GatewayRequestMetadata`, `TransportErrorKind`, `TransportGuardResult`.

### `backend/types/gateway/health-check.types.ts`
```ts
export interface HealthCheckReturnType {
  readonly status: "ok";
  readonly service: "kottaby";
  readonly version: string;
  readonly timestamp: string;
}
```
Exactly four keys (REQ-034); embedded value object with NO `id` (pairs with `keyFields:false` at `apolloCache.ts`, D4).

### `backend/types/gateway/gateway-context.types.ts`
```ts
export interface GatewayRequestMetadata {
  readonly requestId: string;
  readonly idempotencyKey: string | null;
}

export type TransportErrorKind =
  | "METHOD_NOT_ALLOWED"
  | "UNSUPPORTED_CONTENT_TYPE"
  | "PAYLOAD_TOO_LARGE"
  | "MALFORMED_JSON";

export type TransportGuardResult =
  | { readonly ok: true; readonly body: unknown }
  | { readonly ok: false; readonly kind: TransportErrorKind };
```

Exact discriminated-union shape pinned for Task 2.2 (`guardTransport` producers) and Task 3.2 (route short-circuit consumers): narrow on `ok`; success arm carries parsed body as `unknown` (forces explicit narrowing); failure arm carries ONLY `kind` (no raw header/body/stack echo — REQ-033/034). Kind→HTTP mapping lives route-side in Task 3.2 (`METHOD_NOT_ALLOWED`→405+`Allow: POST`, `UNSUPPORTED_CONTENT_TYPE`→400, `PAYLOAD_TOO_LARGE`→413, `MALFORMED_JSON`→400). Wire-code reconciliation vs live `GRAPHQL_PARSE_FAILED` remains R1-Correction#4 scope of Task 2.2 — this union is the internal kind vocabulary, not the wire envelope code.

Docblocks carried on type bodies: `GatewayRequestMetadata` = DOCUMENTARY contract only (no runtime construction site may be introduced), headers are NON-AUTHORIZATION by BOLA §6.1 (identity exclusively factory-derived; client-supplied ids are correlation metadata, never an auth signal); `idempotencyKey` null when header absent (never empty-string-coalesced — Task 3.3 Tier-2 test hook).

---

## Constraints audit (1.1.SR/SEC clause-by-clause)

- **ZERO runtime exports:** `rg "export const|export function|export class|export let|export var" backend/types/gateway` → **0 matches**. Full A5 static assertion registers in Task 2.3's suite as planned.
- **Zero imports in the new .types.ts files:** grep → 0 import statements (pure types, no cross-layer or even intra-layer imports).
- **`readonly` everywhere:** all fields across all four exported shapes.
- **No enum anywhere:** both new types use unions/interfaces only; nothing registered into `enum.pothos.ts` / `backend/enum/**` / `schema/enums.ts` (see 1.2 evidence).
- **Barrel purity:** gateway barrel re-exports exactly the two `.types.ts` files — only type symbols exist behind them; root barrel entry uses one-level `"./gateway"` per AGENTS.md.
- **IV:** `backend/types/AGENTS.md` location/naming/barrel rules read and matched; interface-over-type-object style follows the `errors/api-error.types.ts` DEV3-002 precedent.

---

## Verification results (post-edit protocol item 2)

QL strategy note vs baseline oxlint env-limit: Phase 0 recorded a repo-wide `bun oxlint` SIGKILL (tsgolint headless OOM-class, baseline #3b). The instructed fallback (`--lifecycle biome` + direct `bunx jscpd`) was PREPARED but **NOT needed**: file-scoped sub-loop runs oxlint on the single target file and passes in this sandbox (the limitation reproduces only repo-wide / project-wide invocations, as the C1-baseline entry proved). All four touched files therefore ran the FULL primary lifecycle:

| Command | Exit | Result |
|---|---|---|
| `bun run scripts/health/sub-loop.ts backend/types/gateway/health-check.types.ts --lifecycle duplicates` | **0** | tsgo ✅ → oxlint ✅ → biome ✅ → lint:type-aware ✅ → check:duplicates ✅ (jscpd: 0 clones) |
| same … `gateway-context.types.ts` | **0** | all five stages ✅ |
| same … `gateway/index.ts` | **0** | all five stages ✅ |
| same … `backend/types/index.ts` | **0** | all five stages ✅ |
| `bun tsgo` (project-wide gate = 1.1.TE Tier 2) | **0** | `grep -c "error TS"` → **0** — equals Phase 0 baseline + 0 delta |
| `bun biome:check` (lint delta signal per baseline discipline) | **0** | literal "**Checked 453 files in 4s. No fixes applied.**" (450 baseline + 3 new .ts files; 0 diagnostics, 0 tree writes — pre/post `git status --porcelain` clean apart from intended artifacts) |

DB surface: none added by this task → `runInRollback` N/A (recorded); static assertion A5 registration deferred to its owning suite (Task 2.3) per task definition.

---

## Carry-forward knowledge for downstream tasks

1. Import path: `import type { HealthCheckReturnType } from "@/backend/types";` — never deep-path, never local literals (Task 3.1 CRITICAL rule already satisfied structurally).
2. `TransportGuardResult` success arm deliberately typed `unknown` → Task 2.2 guards return the raw parsed body; narrowing belongs to the engine-input step.
3. Do NOT invent wire codes from this union — keep mapping code route-side; live taxonomy codes (`GRAPHQL_PARSE_FAILED` vs `MALFORMED_JSON`) decided once in Task 2.2 per R1 Correction #4 (exactly ONE canonical constant name decision also lands there).
4. `HealthCheckReturnType.status` literal `"ok"` lets Task 5.x integration probes assert equality without magic strings.
