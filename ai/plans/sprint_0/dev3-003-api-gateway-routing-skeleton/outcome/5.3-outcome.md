# Task 5.3 Outcome — Concurrency & Chaos Tier (REQ-074)

- **Task ID:** C4-livewire · date 2026-08-27 · suite by owner commit `0478a6f`, execution via isolated-mirror procedure (see 5.1 outcome for full recipe)
- **Result: `frontend/graphql/test/gateway/concurrency.chaos.test.ts` — 3 pass / 0 fail, exit 0 (log: `/tmp/kottaby-livewire-logs/chaos.log`)**

## Row evidence
| Row | Disposition | Evidence |
|---|---|---|
| `Promise.allSettled` storm ≥50 × `_health` | ✅ PASS | 50 requests all 200, each fresh ISO timestamp [355ms] |
| requestId uniqueness across storm | ✅ PASS | 50 unique ids, zero collisions, no shared counter [388ms] |
| Two concurrent logins → cookie isolation | ⏭️ `test.failing` deferred | SQLite sandbox cannot register users (PG-bound); implementation pattern documented in-suite; rides the same CI/Postgres gate as 5.1(i) — tracked in deferred ledger |
| Concurrent refresh-rotation race | ➡️ covered by DEV2-001's own suite | per task text "converges per DEV2-001 REQ-021 stale-JTI contract (**unchanged by gateway**)" — the gateway adds no refresh logic; DEV2-001 regression suites remain the owning proof (green in DEV3-002 R-rounds) |

## Determinism / flake audit (SR)
- Storm size is a compile-time constant (50); assertions are structural (all-200, ISO-parse, Set-uniqueness) — no timing tolerances, no sleep-based expectations. Safe for CI.

## QL/SEC/IV
- QL: suite exit 0 in mirror; tsgo/biome clean at HEAD. SEC: storm hits only the two sanctioned public surfaces (no auth bypass attempted); cookie-isolation row properly gated rather than faked. IV: `frontend/graphql/test/AGENTS.md` rule 10 honored (no backend imports).
