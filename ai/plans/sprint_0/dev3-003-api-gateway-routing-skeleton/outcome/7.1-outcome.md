# Task 7.1 Outcome — Canonical Doc `docs/graphql/api-gateway-and-routing.md`

- **Task ID:** C3-7.x (builder subagent) · orchestrator-verified 2026-08-27
- **Created:** `docs/graphql/api-gateway-and-routing.md` (193 lines) — extends the `docs/graphql/` family alongside `error-handling-contract.md`.

## Contents (all anchored to code at HEAD — zero invented behavior)
- Seven-step route pipeline w/ ordering rationale + replayable-re-buffer hazard note.
- Transport contract: POST-only · CT allowlist · `MAX_GRAPHQL_BODY_BYTES = 2_000_000` (`transport-guard.ts:54`) · declared-vs-drained 413 · MALFORMED_JSON→GRAPHQL_PARSE_FAILED · full TransportErrorKind→wire table.
- Route inventory table (3 rows from `ROUTE_INVENTORY`) + A4 bidirectional gate + append-same-change-set RULE.
- Public allowlist 6-member table + add-entry rule + REQ-072 coverage gates.
- Two sanctioned probes pin (`/api/health`, `Query._health`) + no-third-surface + version chain + introspection D6.
- Identity-headers policy; CORS/method D6-D8; REQ-018 registration recipe (module placement, side-effect barrels, no-await-import/Pothos/enum/codegen-in-same-commit).
- N/A affirmations (no REST tree, no WebSocket→DEV3-010, escapeLikeWildcards N/A w/ DEV3-008/009 forward contract).
- Test-tier table (incl. `.tsx → bun run test:ui:components` runner wording — discharges 6.3 R-1 cosmetic item) + BLT deferred-row pointer table.

## Verification
- tsgo 0 · biome 477 files/0 diagnostics · static-assertions 17/0 · route-inventory 12/0 · 6 relative links resolve.
- Sub-loop rejects .md inputs (control-proven against shipped `error-handling-contract.md` — identical abort) — recorded honestly; no .ts modified.

## Files NOT modified
Source files, tests, codegen artifacts, db — doc-only change; sanctioned set held (verified via git status).
