# Task 3.4 Outcome — `/api/health` HTTP Probe + Introspection Gate Verification + CORS Posture

- **Task ID:** C2-phase3c · dev3-003 Phase 3 Task 3.4 · REQ-013, REQ-036, REQ-053; D2, D6, D8
- **Date:** 2026-08-27 · Base: working tree at HEAD `8e5ebb8` + uncommitted Phases 0–3.3 (per protocol, nothing is committed)
- **Pre-reads honored:** worklog C1-gate…C2-phase3b; tasks.md §3.4 (all sub-gates) + Non-Negotiable Protocol; plan.md §1.1/D2/D6/D7/D8 + REQ rows; plan-review-R1.md F6/F7/F8 + Corrections #8/#10 (introspection already gated → verify+test-lock; cors posture = document the ambient `.space-z.ai` echo, don't invent); outcome/3.1-outcome.md (payload shape/single producer), outcome/2.1-outcome.md (service location), outcome/2.2-outcome.md constant-unification notes, outcome/3.2-outcome.md (ambient CORS inventory); app/AGENTS.md; DEV3-002 envelope contract (`backend/lib/api`: exact exports `resolveRequestId` :139, `apiSuccessResponse` :164 via barrel `@/backend/lib/api`).

---

## Files changed / NOT changed

| File | Action | Why |
|---|---|---|
| `app/api/health/route.ts` | CREATED | The second sanctioned probe (D2). Single-expression `GET` = `apiSuccessResponse(HealthCheckService.getHealthStatus(), { requestId: resolveRequestId(request.headers) })` — verbatim plan composition using the EXACT helper export names found in `backend/lib/api`. NO auth, NO GraphQL parse, NO DB, NO locale machinery, NO try/catch (producer proven non-throwing by 2.1 suite), NO console.*, plain-fetch `Request` param (zero framework coupling). Only `GET` exported → every other verb rides Next.js' framework 405. Zero CORS vocabulary in the file. |
| `backend/lib/gateway/route-inventory.ts` | MODIFIED (append-row only) | **A4 append rule discharged:** `{ path: "/api/health", classification: "envelope" }` appended as the third row IN THIS CHANGE-SET, with a one-line method note above it (`GET-only LB probe (REQ-013/D2)`). Docblock ground truth refreshed two→three routes + the Task-3.4 carry-forward paragraph marked DISCHARGED. Closed `RouteClassification` vocabulary and entry interface UNTOUCHED ("GET-method note" lives as a row comment/docblock prose — no schema widening). |
| `backend/lib/gateway/route-inventory.test.ts` | MODIFIED (minimal, co-moving lock) | ⚠️ DEVIATION from "sanctioned set", REQUIRED by mandatory gates — see §Deviations below: the file's own Tier-2 test freezes registry ordering as a snapshot (`["/api/graphql","/api/set-locale"]`), so appending the row without updating the lock turns this paired suite red, violating Protocol rule 3 ("every test file must pass before its parent task is checked"). Diff = locked array `+"​/api/health"`, new Tier-2 row-classification assertion for `/api/health`, sanity floor 2→3, one coverage-map bullet. |
| `app/api/graphql/route.ts` | MODIFIED (comment wording ONLY) | Stale introspection docblock said "wording/test-lock owned by Task 3.4; live form preserved until then". This change-set IS that task → comment now records the verified equivalence (`!isProduction` ≡ `NODE_ENV !== "production"` — see §Introspection) + points to the executable source-pin lock. ZERO runtime/config bytes changed (`git diff` shows comment lines only). This was the only wording change genuinely needed. |
| `docs/graphql/error-handling-contract.md` | MODIFIED (CORS section only) | New Rules **§6 "Method & CORS posture — same-origin-first (REQ-016/053 · dev3-003 D6–D8)"** inserted before "What NOT to Do": conservative default (no CORS headers introduced, wildcard ACAO hard-prohibited on credentialed surfaces per DEV2-001 strict-cookie posture), ambient behavior documented AS-IS (gateway's pre-existing `.space-z.ai` origin-ECHO allowlist incl. credentials:true / unknown-origin preflight 403 — never a wildcard, per R1 F8), `/api/health` zero-CORS note, Task-5.1 preflight-probe pointer, forward-contract rule (matrix codified here first). Doc consumed, not forked; no other section touched. |
| `app/api/health/test/health-route.probe.test.ts` | CREATED | TE tiered suite, 15 tests / 49 expects (see §TE). |
| `ai/plans/sprint_0/dev3-003-api-gateway-routing-skeleton/outcome/3.4-outcome.md` | CREATED | This file. |

**NOT touched (constraint verification):** `backend/services/gateway/health-check.service.ts`, `backend/lib/gateway/{index,transport-guard,public-operations,version}.ts`, `backend/lib/api/**`, `backend/graphql/gqlContextFactory.ts`, all pothos/query files, `frontend/**` (incl. generated artifacts — GraphQL schema/codegen unaffected by an HTTP route), db/dbml/enums/locale sources, existing test suites other than the route-inventory co-move above. Dev server :3000 NEVER killed or restarted (read-only curl probes only).

## Inventory-append diff evidence (the A4 fail-closed discharge)

```diff
--- a/backend/lib/gateway/route-inventory.ts
+++ b/backend/lib/gateway/route-inventory.ts
@@ registry body @@
   { path: "/api/graphql", classification: "gateway" },
   { path: "/api/set-locale", classification: "envelope" },
+  // GET-only LB probe (REQ-013/D2) — envelope via DEV3-002 helpers at birth.
+  { path: "/api/health", classification: "envelope" },
 ] as const;
```

- Same change-set created `app/api/health/route.ts` ⇒ static-assertions **A4 bidirectional disk↔ROUTE_INVENTORY walk passes** (`17 pass / 0 fail`, including both directional checks + determinism re-walk) and the independent route-inventory Tier-3 twin passes in the same run.
- Docblock rule preserved: phantom `/api/webhooks|logs|cron/*` stay unregistered (Tier-4 phantom hygiene still green).

## Introspection gate (REQ-036/D6) — equivalence evidence (no config churn)

Plan-review R1 **F7** + Correction #8 anticipated this state; verified live:

- `app/api/graphql/route.ts:20-21`: `const envConfig = getEnvironmentConfig(); const isProduction = envConfig.nodeEnv === "production";`
- `backend/lib/env.ts:69`: `const nodeEnv = process.env.NODE_ENV ?? "development";` — i.e. `envConfig.nodeEnv` IS validated NODE_ENV.
- `route.ts:113`: `introspection: !isProduction` ⇒ logically ≡ `NODE_ENV !== "production"`. It is a NAMED, code-level constant computed at module top and consumed in the Apollo config — written by us in code, NOT an Apollo-server ambient default. Additional prod hardening rides the same flag (`ApolloServerPluginLandingPageDisabled`, `hideSchemaDetailsFromClientErrors: isProduction`, `includeStacktraceInErrorResponses: !isProduction`).
- **Chosen form: equivalence recorded instead of churn** (mandate option A): rewriting to the literal `NODE_ENV !== "production"` would bypass the env-validation layer for zero semantic gain.
- **Test-lock added** (R1 F7 Expected: "add the test-lock/prod-config probe"): `health-route.probe.test.ts` source-pins BOTH halves — `'isProduction = envConfig.nodeEnv === "production"'` present, `/introspection:\s*!isProduction,/` present, `"introspection: true"` ABSENT. Any silent widening now fails the mandated runner.
- Carry-forward: the GET-handler docblock in graphql route.ts also claims "env-registration + canonical-doc wording [for a D7 non-prod interactive-GET opt-in key] are owned by Task 3.4". This Task ID's sanctioned scope did not include new env-config keys and tasks.md 3.4 does not list them → left untouched deliberately; recorded as carry-forward for the Phase-6 docs/canonical-doc task (Task 7.x) before any wording ships.

## CORS posture (REQ-053/D8) — grep evidence

```
$ rg -n "Access-Control-Allow-Origin" app backend shared frontend
→ app/api/graphql/route.ts:261,285      # PRE-EXISTING ambient .space-z.ai origin ECHO
                                        # (value = request origin itself — never "*")
→ docs/graphql/error-handling-contract.md:158 (documentation section added this change-set)
```
- `rg -c 'Access-Control' app/api/health/route.ts` → **0 matches** (also pinned executably in-suite: source contains no `Access-Control` substring at all, and probe responses return `null` for all four `access-control-*` header reads with a hostile foreign `Origin` supplied).
- Wildcard pin: `/Access-Control-Allow-Origin[^\n]*\*/u` over ALL THREE `/api` route sources → no match (in-suite static pin). No new CORS behavior was invented anywhere in this change-set.

## TE — verification results

| Gate | Command | Exit | Result |
|---|---|---|---|
| QL × app/api/health/route.ts | `bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates` | **0** | full lifecycle ✅ |
| QL × health-route.probe.test.ts | same | **0 after fix-loop** | fix loop: oxlint `no-await-in-loop`×2 (loop → `Promise.all`) + `consistent-function-scoping` (`listRoutePaths` hoisted to module scope); then type-aware eslint `sonarjs/publicly-writable-directories` on the negative-control literal (`C:\Windows\Temp` → neutral `C:\Users\bait\leak.txt`). All five lifecycle stages green unpiped. |
| QL × route-inventory.ts / route-inventory.test.ts / graphql route.ts | same | **0 / 0 / 0** | full lifecycle each (graphql route.ts comment-only edit included) |
| TE (new suite) | `bun run test/scripts/run-test.ts app/api/health/test/health-route.probe.test.ts` | **0** | **15 pass / 0 fail**, 49 expects |
| TE (registry twin) | `… backend/lib/gateway/route-inventory.test.ts` | **0** | **12 pass / 0 fail** (locked snapshot moved WITH the append) |
| TE (static gate) | `… backend/lib/gateway/static-assertions.test.ts` | **0** | **17 pass / 0 fail** — A4 bidirectional ✅; A3 now also scans the NEW health route (zero console sites) ✅ |
| Regression sweep (16 suites, exits captured UNPIPED per C2-phase3b hazard note) | runner loop over transport-guard, public-operations, route-inventory, static-assertions, health-check.service, schema-surface, api-response, set-locale-route, request-id, error-finalizer, context-keys, graphql-route.transport, graphql-route.pipeline-order, error-code-taxonomy, error-masking, errors-fields-contract | **all 0** | counts spot-reconfirmed: 44/0 · 26/0 · 12/0 · 17/0 · 11/0 · 12/0 (+15/0 new) — **zero regressions** |
| Project TS | `bun tsgo` → `grep -c "error TS"` | **0** / **0** | zero project-wide errors |
| Project lint | `bun biome:check` | **0** | literal `Checked 472 files in 4s. No fixes applied.` (470 pre-task + exactly the 2 new .ts files) |

### Suite coverage map (tasks.md 3.4.TE tiers)

- **Tier 1** — GET direct-invocation → 200 + `application/json`; envelope key-set exactly `{data, requestId}`; data = EXACTLY the four fields; constants `ok`/`kottaby`/version-from-env-arm; ISO-Z parseable timestamp; inbound `X-Request-Id` echoed VERBATIM (live curl double-proof below); absent header → fresh UUID v4 per call; hostile ids (oversized >128 / comma-joined / control-char) dropped wholesale to UUID fallback; sequential probes share immutable constants w/ fresh timestamps & distinct payload objects (zero module state); data field-equality against the SINGLE producer (D2/D3 identical-by-construction, timestamp adjacency ≤5s).
- **Tier 2** — module namespace exports EXACTLY `["GET"]` (frozen half of the 405 guarantee; no POST/PUT/DELETE/PATCH/OPTIONS escape hatch may appear silently). Next.js semantics documented: with no export for a verb, the framework answers 405 Method Not Allowed, auto-serves HEAD through GET, and synthesizes OPTIONS→204 with `allow: GET, HEAD, OPTIONS` — none of this can be driven headlessly in-suite because of BLT-07 (dev-server singleton owns :3000), so the OTHER half is proven live post-landing (below) and owned long-term by Task 5.1's harness matrix once BLT-07 is fixed.
- **Tier 4** — disclosure regex scan over producer+envelope serialization (negative controls prove POSIX-path AND drive-letter scanners fire; clean envelope matches NEITHER); planted secret-shaped env decoy value+key-name never cross the surface; runtime zero-`Access-Control-*` headers; static wildcard-ACAO pin across all three route sources; REQ-036 introspection code-explicit pins; third-health-surface proof (disk walk == exactly the three inventory routes AND `getHealthStatus(` callers == exactly `app/api/health/route.ts` + `backend/graphql/query/health.query.ts`).

## Live wire evidence (:3000, HMR picked the route up; server untouched)

```
$ curl -s -i http://localhost:3000/api/health
HTTP/1.1 200 OK … content-type: application/json …
{"data":{"status":"ok","service":"kottaby","version":"0.1.0","timestamp":"2026-08-27T04:21:05.921Z"},"requestId":"d45d7fdd-4eee-461d-b9f4-e968efc4c0ac"}

$ curl -s -H "x-request-id: live-evidence-C2-phase3c" http://localhost:3000/api/health
{"data":{"status":"ok","service":"kottaby","version":"0.1.0","timestamp":"2026-08-27T04:21:23.009Z"},"requestId":"live-evidence-C2-phase3c"}

$ curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:3000/api/health   → 405
$ curl -s -o /dev/null -w "%{http_code}" -X PUT  http://localhost:3000/api/health   → 405 (framework default carries NO Allow header)
$ curl -si -X OPTIONS http://localhost:3000/api/health                              → 204 + allow: GET, HEAD, OPTIONS
```

## Sub-gate rollup

- **3.4.QL** ✅ — sub-loop `--lifecycle duplicates` exit 0 on every new/modified .ts (5 files; true unpiped exits; one real fix-loop on the test file, iterated to clean).
- **3.4.TE** ✅ — 15/0 new + 12/0 route-inventory + 17/0 static-assertions + full 16-suite regression sweep all green.
- **3.4.SEC** ✅ — no-auth intentional (LB probe class; allowlist-posture sibling of scopeless `_health`; disclosure bounded by the 2.1 scan + Tier-4 twins here); introspection gate verified code-explicit + newly test-locked; zero wildcard ACAO introduced (grep + executable pins); hostile X-Request-Id suppression proven at route level.
- **3.4.SR** ✅ — single-expression pure composition; envelope helper used (ZERO hand-rolled JSON); no console.*; no `await import(`; third-health-surface provably absent (executable disk+corpus check recorded above, mirrors the plan's grep requirement).
- **3.4.IV** ✅ — app/AGENTS.md route conventions (layer rules respected; backend access via service layer only); DEV3-002 envelope helper contract consumed via the canonical `@/backend/lib/api` barrel exactly as R1 ground-truth row #3 records.
- **3.4.OC** ✅ — this file; main-line checkbox flipped below.

## Deviations & cross-file dependencies discovered

1. **route-inventory.test.ts co-edit** (documented above) — unavoidable consequence of "append row"+"suite must pass"+its frozen ordering snapshot. Kept to four minimal hunks.
2. **Graphql route.ts comment-only touch** — needed to clear a now-false ownership claim inside the file; alternative (leaving stale text) violates zero-dead-knowledge hygiene the reviewer enforces.
3. **OPTIONS on /api/health returns 204+Allow from Next.js machinery, not our code** — any future need for an explicit OPTIONS posture must either be documented as framework behavior or OWNED by an explicit export (none shipped here; D8 forbids adding CORS answers ad hoc).
4. **D7 env-flag ownership mismatch** carried forward (see §Introspection tail) — owner: canonical-doc task / orchestrator call in Phase 6–7.
5. **BLT-07 still open** — headless HTTP 405 tier for this route remains harness-owned (Task 5.1 precondition), supplementing (not replacing) today's module-surface pin + manual curl evidence.

## Carry-forward knowledge for downstream tasks

1. Canonical doc table (REQ-019, Task 7.1) must print THREE rows sourced from ROUTE_INVENTORY — the registry lock now encodes order `[graphql, set-locale, health]`.
2. `frontend/graphql/test/gateway/gateway.integration.test.ts` (5.1) should add an `/api/health` leg (200 envelope + non-GET 405) once setupTestServerLifecycle boots again (BLT-07 poll-document fix first).
3. Next.js dev runtime answers unknown verbs on this route WITHOUT an `Allow` header (unlike our explicit gateway 405s which always carry `Allow: POST`) — if an LB contract ever demands RFC-9110 `Allow` on the probe, that requires exporting explicit method handlers, a deliberate design change NOT taken silently here.
