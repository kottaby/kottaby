# Task 3.1 Outcome — `_health` GraphQL Surface + Schema/Codegen Sync

- **Task ID:** C2-phase3a · dev3-003 Phase 3 · REQ-012, REQ-060, REQ-061; D4 (+ D2/D3 single-producer tie-in)
- **Date:** 2026-08-27 · Base: HEAD `8e5ebb8` (Phases 0–2 + mid-point gate GO)
- **Pre-reads honored:** worklog entries C1-gate/C1-baseline/C1-phase1/C1-phase2/C1-2M/C1; tasks.md §Phase 3 (all 3.1 sub-gates) + Non-Negotiable Execution Protocol; plan.md §3.1/D4 (embedded value object, keyFields:false pairing); plan-review-R1.md Correction #1 (**delete placeholder `_health` FIRST**) and #10 (sprint_0 artifact paths); outcome/2.1-outcome.md (payload shape + service location + delegation contract); backend/graphql/AGENTS.md + pothos/AGENTS.md + query/AGENTS.md (single canonical object rule, side-effect barrel convention, delegate-only resolvers); precedents recitation.query.ts / enum.pothos.ts.

---

## Files changed / NOT changed

| File | Action | Why |
|---|---|---|
| `backend/graphql/pothos/builder.ts` | MODIFIED | **BLT-06 discharge, executed FIRST**: deleted the inline `queryType({ fields: t => ({ _health: t.field({ type: "String", ... }) }) })` placeholder entirely (lines ~132–145 pre-edit). `queryType({})` now declares an EMPTY root, exactly like the pre-existing `mutationType({})`; fields arrive via side-effect imports before `toSchema()`. Post-edit `grep -c "_health" builder.ts` = **0** (exit 1, zero matches incl. comments). |
| `backend/graphql/pothos/shared/health.pothos.ts` | CREATED | `HealthCheckPothosObject = gqlSchemaBuilder.objectRef<HealthCheckReturnType>("HealthCheck").implement({ fields: t => ({ status/service/version/timestamp via t.exposeString }) })`. Canonical type imported from `@/backend/types` barrel — NO local type literal (CRITICAL rule), NO `id` field (D4 embedded value object), enum-free. Docblock density per shared/ precedent (`enum.pothos.ts`). |
| `backend/graphql/query/health.query.ts` | CREATED | Side-effect file (no named exports): `gqlSchemaBuilder.queryField("_health", t => t.field({ type: HealthCheckPothosObject, resolve: () => HealthCheckService.getHealthStatus() }))`. NO `authScopes` key **deliberately** (allowlisted member of `PUBLIC_OPERATIONS`, REQ-017/060), NO ctx reads, NO DB, NO DataLoader, NO `await import(`, NO business logic beyond delegation. |
| `backend/graphql/query/index.ts` | MODIFIED | One side-effect import added in the alphabetical slot: `./auth.query` → `./health.query` → `./recitation.query`. |
| `frontend/graphql/generated/schema.graphql` | REGENERATED | Sole generated-artifact carrier of the delta this phase (see diff evidence below). |
| `backend/graphql/test/schema-surface.test.ts` | CREATED | 3.1.TE Tier-1 schema-build assertion suite (12 tests / 41 expects — see TE section). |
| `ai/plans/sprint_0/dev3-003-api-gateway-routing-skeleton/outcome/3.1-outcome.md` | CREATED | This file. |

**NOT touched (constraint verification):** `app/api/graphql/route.ts` and `app/api/**` byte-frozen (Task 3.2 scope); `backend/graphql/gqlContextFactory.ts` untouched (3.3 owns it); ZERO db/enum files touched (REQ-044 invariant held; `dbml` surface not re-run this task since nothing under its scope changed — no migration/schema/dbml input was modified, verified by git status = sanctioned set only); frontend source untouched except the sanctioned generated artifacts under `frontend/graphql/generated/`; `frontend/providers/apollo/apolloCache.ts` NOT modified (keyFields:false is Task 4.1 scope); no console.*, no new markdown beyond this outcome; dev server :3000 never restarted or probed destructively.

## Verification results (commands + exit codes)

| Gate | Command | Exit | Result |
|---|---|---|---|
| QL × health.pothos.ts | `bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates` | **0** | full five-stage lifecycle ✅ (tsgo→oxlint→biome→lint:type-aware→jscpd 0 clones) |
| QL × health.query.ts | same | **0** | all five stages ✅ |
| QL × builder.ts | same | **0** | all five stages ✅ |
| QL × query/index.ts | same | **0** | all five stages ✅ |
| QL × schema-surface.test.ts | same | **0** | all five stages ✅ after fix loop (see carry-forward §hazards) |
| TE | `bun run test/scripts/run-test.ts backend/graphql/test/schema-surface.test.ts` | **0** | **12 pass / 0 fail**, 41 expect() calls |
| Codegen | `bun run generate:gqlSchema && bun run codegen` | **0/0** | SDL rewritten (3128 bytes); typed-doc-node codegen regenerated cleanly |
| Project TS | `bun tsgo` then `grep -c "error TS"` | **0** / **0** | zero errors project-wide (= Phase-0 baseline + 0) |
| Project lint | `bun biome:check` | **0** | literal `Checked 467 files in 4s. No fixes applied.` (464 pre-3.1 + exactly the 3 new .ts files; 0 diagnostics, no fixes) |
| Post-gate tree | `git status --porcelain` | — | exactly the 6 sanctioned entries above; zero foreign writes by biome --write pass |

## Codegen diff evidence (REQ-060 gate)

`git diff frontend/graphql/generated/schema.graphql` contains ONLY two hunks:

```diff
+type HealthCheck {
+  service: String!
+  status: String!
+  timestamp: String!
+  version: String!
+}
 ...
 type Query {
-  """Health-check query — returns 'ok' when the GraphQL endpoint is alive."""
-  _health: String!
+  _health: HealthCheck!
```

- Retyping `_health: String!` → `_health: HealthCheck!` + one new object type with EXACTLY the four `String!` fields; every pre-existing surface byte-identical (mutation set, enums Gender/RecitationReading/RegisterPublicRole/UserRole, LoginPayload/LogoutPayload/RefreshTokenPayload/User/RegisterUserInput, `me`, `recitationReadings` — diff context confirms no churn elsewhere).
- The dropped description line belonged solely to the deleted placeholder — the new field deliberately ships WITHOUT a description so the diff stays exactly at "retypes + type addition" (sanctioned-delta reading; documentation lives in the module docblocks instead).
- `frontend/graphql/generated/gql/graphql.ts`: regenerated cleanly but **byte-identical to HEAD** — expected and correct: the codegen pipeline emits operation types from documents only (`documents: ["frontend/graphql/sharedDocuments/**/*.ts", …]`, `ignoreNoDocuments: true`) and NO document consumes `HealthCheck` yet. `healthCheckQueryDocument` remains ledger row BLT-03 ("first consumer") with the create-or-defer decision owned by Task 4.1 — its absence is what keeps this artifact stable. Commit-set semantics of REQ-060 are satisfied: the synced `schema.graphql` IS committed alongside the source change.

## Cross-file dependencies discovered

- Ordering invariant made permanent: `gqlSchema.ts` imports the query barrel BEFORE `toSchema()` — with `queryType({})` now empty-at-declaration, Pothos receives its first Query field purely through side-effect registration (this mirrors the already-proven empty `mutationType({})`).
- SEC twin locked as code: `PUBLIC_OPERATION_NAMES/PUBLIC_OPERATIONS` ↔ scopeless `_health` membership asserted inside the new suite (REQ-072 formal introspection coverage test itself remains DEV2-002-D3/Task 5.x scope).
- Resolver execution path proven anonymous-reachable end-to-end via bare `graphql()` on the real schema with `{}` context — same producer consumed later by `/api/health` (Task 3.4, D2/D3 identical-payload invariant).

## Carry-forward knowledge for downstream tasks

1. **Schema-surface freeze suite is reusable:** `schema-surface.test.ts` freezes PRE-3.1 inventories (queries/mutations/enums/named types, spec scalars + introspection excluded). Later gateway tasks must update these frozen tuples ONLY when their plan-sanctioned deltas say so (e.g., none expected for 3.2–3.4).
2. **No-description discipline for new root fields:** if Task 3.4's `/api/health` or future ops follow suit, keep Pothos descriptions out where the plan pins exact schema deltas — otherwise diffs grow beyond the sanctioned envelope.
3. **HAZARD (recurred twice this task):** stale lint-cache artifacts can serve PHANTOM file contents — oxlint reported a pre-edit line (`._health` dot access) with old line numbers AFTER the edit landed on disk (verified by raw reads), and clearing `.eslintcache-type-aware` alone did not purge it mid-run. Mitigations used here, and recommended downstream: (a) keep underscored names OUT of member-access positions entirely (lookup-by-name comparison over `Object.values(...)` passes `no-underscore-dangle` without bracket notation games); (b) when a linter quotes file content that contradicts disk, trust the disk after an explicit byte check and re-run once; caches never self-clear per lint-service docs.
4. **Test-layer typing notes:** the `"graphql"` package's `graphql()` executable takes NO generic (TS2558) — type the result locally (`JSON.stringify(result.data)` regex match used here, which also dodges `no-unsafe-type-assertion`); prefer guard-and-throw narrowing over `as` casts in tests (sonarjs/no-unsafe-type-assertion + prefer-specific-assertions both bite).
5. Script-name correction vs tasks text: generation runs via `bun run codegen` (tasks.md line wrote `bun codegen`; there is no bare-`bun` script mapping — `bun run codegen` is the working form recorded for reproducibility).

## Sub-gate rollup

- **3.1.QL** ✅ — 5/5 touched files through the full `--lifecycle duplicates` lifecycle, exit 0 each.
- **3.1.TE** ✅ — schema-build unit assertions green (12/0): retyped probe, four `String!` fields exactly, behavioral no-id validation failure, zero new mutations/enums/type-delta == {HealthCheck}, allowlist 1:1, anonymous execution, committed-SDL byte-sync proof. Integration-tier wire proof deferred to Task 5.1 per plan.
- **3.1.SEC** ✅ — `_health` scopeless AND tuple-member (grep evidence `public-operations.ts:46`; rationale docblock :27–29); resolver delegate-only; payload audit inherited from 2.1.SEC (exactly 4 machine constants).
- **3.1.SR** ✅ — no logic beyond delegation; zero `await import(`; side-effect-only registration; canonical-type-backed object; grep evidence rows in outcome table above.
- **3.1.IV** ✅ — backend/graphql/AGENTS.md (Type Definition Pattern import-from-`@/backend/types`; side-effect barrels; Delegate-to-services; nullability default) + pothos/AGENTS.md (no local types, no literal enums — file introduces none) + query/AGENTS.md (side-effect-import rules, `@/` aliases, sub-dir layout: root-level `*.query.ts` matches existing flat precedent files auth/recitation).
- **3.1.OC** ✅ — this file; checkbox flipped below.
