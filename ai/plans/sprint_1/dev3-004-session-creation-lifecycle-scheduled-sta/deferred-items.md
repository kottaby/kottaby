# Deferred Items Ledger

**Feature:** `dev3-004-session-creation-lifecycle-scheduled-sta`  
**Plan Directory:** `ai/plans/sprint_1/dev3-004-session-creation-lifecycle-scheduled-sta/`  
**Created:** `2026-08-30`

---

## Purpose

This ledger tracks all work deferred from one task to another to ensure no deferred item is forgotten. Every deferred item must be explicitly logged here and resolved before the plan is marked complete.

---

## Ledger Table

| ID | Deferred Item | Source Task | Target Task | Status | Verified By | Notes |
|---|---|---|---|---|---|---|
| D1 | Session request/lifecycle event notifications (A.4 consumption; REQ-019 zero-notification rule — this ticket writes zero `notifications` rows) | DEV3-004 (this ticket) | DEV3-010 / DEV3-011 | ⏸ Forward | — | Owner-referenced, non-blocking (per tasks.md 0.1 + specs REQ-001) |
| D2 | Dual-confirmation student confirm + 24h auto-cancel sweeper + wallet credit (confirm flips `fee_held=false` + same-lane credit; sweeper reuses this ticket's same-lane refund primitive; claim age not consulted here — plan.md D6) | DEV3-004 (this ticket) | DEV3-012 / DEV3-013 | ⏸ Forward | — | Owner-referenced, non-blocking |
| D3 | `is_online` availability assertion + teacher directory wiring (this ticket asserts certification only — INV-S5) | DEV3-004 (this ticket) | DEV3-008 / DEV2-011 | ⏸ Forward | — | Owner-referenced, non-blocking per specs reconciliation note #3 |
| D4 | Student-facing booking UI over the directory (the client consumer of `createSession`) | DEV3-004 (this ticket) | DEV3-009 | ⏸ Forward | — | Non-blocking; no session-creation route ships in this ticket — tests/journeys exercise the mutation |
| D5 | INV-S6 / INV-S7 / INV-S8 enforcement + the `disputed` transition surface | DEV3-004 (this ticket) | DEV3-005 / DEV2-013 / DEV3-022 | ⏸ Forward | — | Owner-referenced, non-blocking; `disputed` exists in the enum without a transition surface here |
| D6 | Canonical names `SessionLifecycleService` / `SessionRepository` placed in the `classes` domain, staying clear of the future contract-implied `SessionService` (codework happens IN this ticket) | DEV3-004 (this ticket) | DEV3-004 (this ticket) | ✅ Done | — | Renumbered from the template's original "D1" slot to free D1 for the notifications item (tasks.md 0.1 / specs REQ-001 define D1 = notifications; plan.md §Knowledge-Propagation had listed the naming item under "D1"). Resolved in-plan by the naming decision (§1.5) + REQ-004 verify-absence gate — absence verified by task 0.2 (`outcome/0.2-outcome.md` §5: zero code hits). Kept for traceability per the ledger contract |
| D7 | Teacher-with-`students`-row session-creation carve-out (INV-TV6 extension): allow a `role=teacher` caller holding a `students` row to invoke `createSession` as the student side (failed-applicant conversion path) — requires a dedicated custom authScope (e.g. `studentActor` checking the `students` row) + a REQ-032 scope amendment | 0.3-fix ruling (B3, 2026-08-30) — REQ-011 "MAY" carve-out struck; REQ-064 certified-teacher cell amended to unconditional `FORBIDDEN` | Future ticket (owner TBD) | ⏸ Forward | — | Non-blocking; THIS slice keeps teachers unconditionally FORBIDDEN on `createSession` (REQ-032 static `role:[Student]` scope stands — `pothos/builder.ts:127` membership-only scopes cannot express the carve-out); INV-TV6's honest-denial intent for applicant teachers and its student-privileges clause on the normal role-flip conversion path are preserved in-slice. Asserted in tasks 3.3 / 5.2 (Ruling 2026-08-30 cells) |
| D8 | Happy-DOM component test for dialog typing (branch 7 of `StudentSessionsContainer.suite.tsx`) deferred: React 19 + Happy DOM do not deliver dispatched input events into the MUI Dialog portal (controlled textarea `onChange` never fires; native-setter + bubbled input event also unreachable), and the bun process OOM-kills mid-suite (exit 137); compensating control = agent-browser 4.2.BF cancel-flow loop (real Chromium); revisit when Happy DOM upgrades or bun fixes event delegation | 4.2 (4.2-close, 2026-08-30) | Future iteration (env upgrade) — verification NOW via 4.2.BF | ⏸ Forward | — | Non-blocking; `test.skip` keeps the FULL branch-7 body intact in the suite (one-line flip to re-enable); branches 1-6 + 9-11 green in both locales |
| D9 | Happy-DOM component test for the SESSION_NOT_FOUND cache-eviction arm (branch 8 of `StudentSessionsContainer.suite.tsx`) deferred: the dialog's eviction flow (`cache.modify` ROOT_QUERY filter + `evict` + `gc()` broadcast into the active `useQuery` observer) runs away under bun 1.3.14 + React 19 + Happy DOM — bun balloons to ~3.5GB RSS and is OOM-killed (exit 137) deterministically, even with branch 8 run ALONE via `bun test -t` (branches 1-6/9-11 green in isolation; `--smol`, `BUN_JSC_forceRAMSize`, `MIMALLOC_PURGE_DELAY=0` all ineffective); compensating control = agent-browser 4.2.BF which EXPLICITLY asserts cache-driven row removal on `SESSION_NOT_FOUND` in real Chromium; revisit with bun/Apollo/Happy-DOM upgrades | 4.2 (4.2-close, 2026-08-30) | Future iteration (env upgrade) — verification NOW via 4.2.BF | ⏸ Forward | — | Non-blocking; row added by 4.2-close beyond the literal D8 mandate because the ledger contract requires every deferred item to be logged — the mid-suite OOM reported in the 4.2 handoff was diagnosed as THIS branch (not aggregate memory pressure); `test.skip` keeps the full branch-8 body intact (one-line flip to re-enable) |
| D10 | Resolver-side intent overlay (`Object.assign` in `backend/graphql/mutation/classes/session-lifecycle.mutation.ts:108-112`) is a documented typing-honesty residual: the SDL intentionally delivers the full `SessionIntent` union so the service can reject `evaluation` pre-DB (REQ-054); `SessionSubmitInput` stays closed (BOPLA) — revisit if a shared narrow-validation helper lands | 6.1 review wave (review-types F1, LOW) — recorded by 6.1-fix (2026-08-31) | Future ticket (owner TBD) | ⏸ Forward | — | Non-blocking; no security impact (the service re-validates pre-DB before any write). Widening the service boundary param to `SessionIntent` and narrowing internally would let the overlay (and the type lie) be deleted — do that instead of the shared helper if the service boundary is touched first |
| D11 | Inline lint suppression `// oxlint-disable-next-line no-await-in-loop` at `backend/services/classes/session-lifecycle.service.ts:854` (the sweeper's refund loop — the codebase's ONLY disable comment) contradicts backend/services/AGENTS.md ("NEVER use `oxlint-disable` comments") and the docs/quality/linting-rules.md no-await-in-loop recipe, whose sanctioned shape for sequential iterations sharing one transaction is the recursive-helper pattern (NOT `Promise.all` — the per-row fail-closed ordering is deliberate) | R6 review wave (LOW, lint-rule contradiction) — root cause introduced by DEV3-012 (`4fd479b`, post-close-out) | Future backend-services refactor task (owner TBD) | ⏸ Forward | — | Non-blocking; fix = executable micro-refactor following the documented recipe (extract a `processNext(index)`-style sequential helper over the ONE sweep transaction so the `await` leaves the loop body); neutral marker — contributes nothing to the item-status glyph gate |
| D12 | Start-after-deadline escrow gap — the `startSessionOnce` predicate (`backend/db/repo/classes/session.repository.ts:240-251`) does not fuse `confirmation_deadline`, so a deadline-passed scheduled row can be legally started by its owning teacher in the window before the confirmation-deadline cron sweep ticks; that row then permanently escapes the sweep (whose predicate requires the `scheduled` state with the deadline passed) and its escrow hold resolves only via the normal complete→confirm path (or stays held if the student never confirms — see D13) | R7 review wave (LOW, start-after-deadline sweep-bypass window) — predicate shape pre-existing at the DEV3-004 gate (`8779acc`); the bypass interaction arose with the sweeper introduction (DEV3-012, `4fd479b`, post-close-out) | DEV3-012 follow-up task (owner TBD) | ⏸ Forward | — | Non-blocking; no fund-loss path (normal confirm resolution unaffected); fix = fuse the deadline guard into the start predicate (making the sweep authoritative) + add the missing start-vs-expired test arm |
| D13 | Completed-but-unconfirmed release path — rows in the `Completed` state with `fee_held=true` and no student confirmation have no timeout/arbitration release today: the cancel/dispute predicates cover `scheduled`/`started` only, the sweeper covers `scheduled` only, and the admin queue lists `disputed` only, so the hold persists indefinitely until the student confirms | R7 review wave (INFO, escrow-lifecycle design observation, record-only) — introduced by the DEV3-012 dual-confirmation semantics (`4fd479b`, post-close-out) | Escrow-lifecycle design ticket (owner TBD) — extends the DEV3-012 sweep family | ⏸ Forward | — | Non-blocking; record-only (not a theft vector — the student holds their own funds, the teacher cannot self-credit, and the confirm arm is exactly-once); candidate shapes = a completed-row expiry arm or an admin release surface |
| D14 | Arbitration-Complete escrow asymmetry — `resolveSessionDispute(Complete)` (`resolveDisputeCompleteOnce` at `backend/db/repo/classes/session.repository.ts:419-439`, service orchestration `backend/services/classes/session-lifecycle.service.ts:672-701`) flips `fee_held=false` on the started-then-disputed row with NO teacher wallet credit and NO `teacher_transaction` ledger row, while the ordinary complete→confirm path credits that same fee via the wallet-credit slice (`confirmSessionCompletion` → `WalletRepository.creditEarningOnce`) — the student's debited lane unit stays consumed with zero accounting trail. Distinct from D13 (that row is STUCK at `fee_held=true` with no release path; this one lands `fee_held=false` with no credit) | R8 review wave (INFO, escrow-lifecycle design residual, record-only) — the no-credit arm is DEV3-005-era and self-documented as deliberate ("the credit flow is a later ticket's concern") but was never carried into the ledger until R8; post-DEV3-012 the credit mechanism exists for the ordinary path, making the asymmetry concrete | Escrow-lifecycle design ticket (owner TBD) — D13 family, extends the DEV3-012 sweep/credit design | ⏸ Forward | — | Non-blocking; record-only; no theft vector (the teacher cannot self-credit; the decision surface is admin arbitration); candidate shapes = credit the teacher on arbitration-Complete (the confirm-path primitive is available) or explicitly document the platform-retention decision |

**Seeding note (task 0.1, 2026-08-30):** D1–D5 seeded per tasks.md task 0.1 / specs REQ-001 (orchestrator directive) as neutral ⏸ Forward items — each owner-referenced and non-blocking. The pre-populated template's "D1" row (naming decision, resolved in-plan) is preserved as D6 to avoid losing ledger traceability while freeing the D1 ID for the notifications item. Forward items deliberately use the neutral ⏸ marker (never blocked/partial markers) so the REQ-083 gate reads zero on item statuses. Discrepancy recorded in `outcome/0.1-outcome.md` for the 0.3 plan-review gate. (Grep note: residual blocked/partial glyph matches in this file come ONLY from the Status Values legend definitions below — carried over verbatim from the original template — never from item statuses.)

**Ruling addendum (0.3-fix, 2026-08-30):** D7 added per the 0.3 gate's BLOCKING finding B3 (orchestrator ruling: teachers unconditionally FORBIDDEN on `createSession` in this slice; the REQ-011/REQ-064 carve-out wording struck and deferred). D7 uses the neutral ⏸ Forward marker — it joins D6 as a ledger row that contributes nothing to the REQ-083 `grep -c "❌\|⚠️"` gate (which stays 0).

**4.2-close addendum (2026-08-30):** D8 + D9 added per the task 4.2 component-suite closure (test tier `test/ui/components/student/`): both are Happy-DOM/bun RUNNER-TIER limitations, NOT application defects proven anywhere — the deferred flows carry full inline verification duty in the REAL-browser 4.2.BF functional loop (cancel flow typing + submit + snackbar + chip flip; SESSION_NOT_FOUND cache-driven row removal). Both suite branches remain in `StudentSessionsContainer.suite.tsx` under `test.skip(...)` with complete bodies and explanation comments — a one-line flip re-enables each once bun/Happy DOM/Apollo are upgraded. Both rows use the neutral ⏸ Forward marker (REQ-083 gate stays clean).

**6.1-fix addendum (2026-08-31):** D10 added per the 6.1 review-types wave finding F1 (LOW, report-only residual): the mutation resolver's `Object.assign(baseInput, { intent: args.input.intent })` overlay deliberately sets aside `SessionSubmitInput`'s closed `SessionStudentIntentType` typing at ONE boundary so the wire's `evaluation` member can reach the service's pre-DB guard (REQ-054) — lint-clean, BOPLA-safe, zero security impact, and the codebase's sanctioned alternative to an unsafe assertion. Forwarded (⏸) rather than fixed: deleting the overlay requires widening the service boundary param to the full `SessionIntent` union (or landing a shared narrow-validation helper), a signature change owned by a future ticket. Neutral marker — the REQ-083 `grep -c "❌\|⚠️"` item-status gate stays 0.

**R6/F-4 addendum (2026-08-31):** D11 added per the R6 independent re-verification round (LOW, 0 blocking): the sweeper's refund loop at `session-lifecycle.service.ts:854` carries the codebase's only inline lint-suppression comment — introduced by DEV3-012 (`4fd479b`) post-close-out, after this plan's task outcomes had all recorded zero-disable-comment gates. The sequential `await` itself is BY DESIGN (every refund composes on the ONE sweep transaction; interleaving would obscure the fail-closed rollback ordering for zero speedup on a single connection), so the sanctioned fix is the documented recursive-helper recipe — an executable micro-refactor owned by a future backend-services task, never a disable comment and never `Promise.all`. Forwarded (⏸) rather than fixed in the verification session; neutral marker — contributes nothing to the item-status glyph gate.

**R7/F-5 addendum (2026-08-31):** D12 + D13 added per the R7 independent fresh-eyes round (2 new findings, 0 blocking, review at working-tree state): D12 records the start-after-deadline sweep-bypass window (LOW) — the start predicate never consults the confirmation deadline, so a deadline-passed scheduled row can be legally started in the window before the cron sweep ticks and then permanently escapes the sweep (whose predicate requires the scheduled state); the predicate's shape is PRE-EXISTING at the DEV3-004 gate (`8779acc`) while the bypass interaction is new with the sweeper introduction (DEV3-012, `4fd479b`), so the fix — fusing the deadline guard into the start predicate + adding the missing start-vs-expired test arm — is owned by a DEV3-012 follow-up task. D13 records the completed-but-unconfirmed release path (INFO, record-only): a Completed row with `fee_held=true` and no student confirmation has no timeout/arbitration release today, an escrow-lifecycle design observation owned by a future design ticket extending the DEV3-012 sweep family. Both rows use the neutral ⏸ Forward marker — the REQ-083 item-status glyph gate stays clean (count unchanged at 4).

**R8/F-6 addendum (2026-08-31):** D14 added per the R8 independent fresh-eyes round (1 new finding, 0 blocking, review at working-tree state): D14 records the arbitration-Complete escrow asymmetry (INFO, record-only) — the admin arbitration COMPLETE outcome (`resolveSessionDispute(Complete)` → `resolveDisputeCompleteOnce`, repo :419-439 / service :672-701) consumes the escrow hold (flips `fee_held=false`) with no teacher wallet credit and no `teacher_transaction` ledger row, while the ordinary complete→confirm path credits exactly that fee via the DEV3-012 wallet-credit slice, leaving the student's debited lane unit consumed with zero accounting trail on the arbitration path. The no-credit arm is DEV3-005-era and self-documented as deliberate (the repo docblock defers the credit flow to a later ticket), but it was never carried into this ledger, and post-DEV3-012 the credit mechanism now exists for the ordinary path — making the asymmetry concrete rather than hypothetical. DISTINCT from D13 (stuck `fee_held=true` rows there; `fee_held=false` with no credit here); same owner family — escrow-lifecycle design ticket extending the DEV3-012 sweep/credit design (credit the teacher on arbitration-Complete, or document platform retention). Row + addendum use ONLY the neutral ⏸ Forward marker — the REQ-083 item-status glyph gate stays clean (count unchanged at 4).

---

## Status Values

- ✅ **Done** — Item completed and verified
- ⚠️ **Partial** — Partially completed, needs follow-up work
- ❌ **Blocked** — Not resolved, plan cannot complete until addressed
- 🔄 **In Progress** — Currently being worked on
- ⏸ **Forward** — Pre-seeded forward item owned by a later ticket; non-blocking for this plan (neutral marker — deliberately never blocked/partial markers, keeping the REQ-083 item-status gate clean)
