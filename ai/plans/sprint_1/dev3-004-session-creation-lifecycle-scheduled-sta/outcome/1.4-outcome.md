# Task 1.4 Outcome — Shared Platform Fee Constants (REQ-021)

> Task: create `shared/constants/session-fees.constants.ts` — the platform-set interim
> session fees as decimal STRINGS (`SESSION_FEE_HIFZ` / `SESSION_FEE_TAJWEED` = `"25.00"`,
> `SESSION_FEE_CURRENCY` = `"EGP"`, `SESSION_CONFIRMATION_WINDOW_MS` = `86_400_000`) —
> and add the single additive export to the `shared/constants/index.ts` barrel.
> Scope boundary held: 2 mandated files + 1 colocated test file. Nothing else touched.
> This is a retry dispatch: the first 1.4 run stopped at the branch guard (no files were
> created then); this entry covers the executed run.

---

## 1. Files Created / Modified

| File | Action | Content |
|---|---|---|
| `shared/constants/session-fees.constants.ts` | CREATE | Four exported constants, ZERO imports: `SESSION_FEE_HIFZ = "25.00"`, `SESSION_FEE_TAJWEED = "25.00"` (decimal STRINGS per the money discipline — never numbers/floats), `SESSION_FEE_CURRENCY = "EGP"` (platform default currency), `SESSION_CONFIRMATION_WINDOW_MS = 86_400_000` (24h confirmation window for a freshly created session). Header JSDoc describes domain behavior only: platform-set interim pricing per intent, string-carry money discipline end-to-end (decimal column infers string → API → render verbatim, zero arithmetic on fees), currency, confirmation-window semantics, shared-layer isolation. Per-const one-line JSDoc. Mirrors the `free-trial.constants.ts` neighbor's header/JSDoc/export style. |
| `shared/constants/index.ts` | UPDATE (additive) | One line appended: `export * from "./session-fees.constants";` — alphabetical order preserved (`free-trial.constants` < `recitation-reading.enum` < `session-fees.constants`). Barrel still contains ONLY `export *` statements with relative `./` paths. |
| `shared/constants/session-fees.constants.test.ts` | CREATE (sanctioned test file) | Colocated 4-tier bun:test suite (conventions mirrored from `backend/enum/scheduling/held-balance-lane.enum.test.ts` + the shared-layer precedent `shared/locale/errors-namespace.parity.test.ts` incl. source-hygiene pins). 15 tests / 85 expect() calls — all green. |

## 2. Files NOT Modified (and why)

- `shared/constants/free-trial.constants.ts` / `shared/constants/recitation-reading.enum.ts` — read as convention precedents only (header/JSDoc style, barrel ordering); no change needed.
- `backend/**` — no backend layer file consumes the constants yet; their designated consumers are later tasks (2.8 `createSession` fee resolution + deadline computation; 4.x wallet/receipt + UI surface). Pre-wiring imports would create dead imports and violate the zero-dead-code rule; barrel re-export (`@/shared/constants` and deep path) is the sanctioned consumption surface.
- `backend/graphql/**` / SDL — nothing registered: fee constants are server-owned values, never a GraphQL type; the DB `session.fee` column and its schema delta are pre-existing (task 0.2 ground truth) and untouched here.
- Any `.env*` / config — the constants are compile-time literals, deliberately NOT env-driven (platform policy, not deployment config; also keeps the module pure).

## 3. Verification Results

### 3.1 Quality Loop (sub-loop `--lifecycle duplicates`)

| File | Result |
|---|---|
| `shared/constants/session-fees.constants.ts` | ✅ exit 0 after ONE fix cycle: first pass failed oxlint `jsdoc(check-tag-names)` — the header comment's `@/backend, @/frontend` tokens were parsed as JSDoc tags. Fixed in-file by rewording to "imports nothing from the backend, frontend, or app layers" (root-cause fix, no disable comments). Then: tsgo ✅ oxlint ✅ biome:check ✅ lint:type-aware ✅ check:duplicates ✅. |
| `shared/constants/index.ts` | ✅ exit 0 — tsgo ✅ oxlint ✅ biome:check ✅ lint:type-aware ✅ check:duplicates ✅ (first pass). |
| `shared/constants/session-fees.constants.test.ts` | ✅ exit 0 after TWO fix cycles: (1) ESLint `sonarjs/super-linear-regex` on `/^\s*export const\b/m` → replaced with linear `/^export const /m`; (2) a Tier-3 test bug found by the suite's own run (descriptor-map passed where a namespace object was expected) → rewritten to read `descriptors.<NAME>.value` directly. Re-run: tsgo ✅ oxlint ✅ biome:check ✅ lint:type-aware ✅ check:duplicates ✅ (jscpd skips test files — informational). |

Full-repo `bun tsgo` after all edits: exit 0 (baseline delta = 0).

### 3.2 Test Engineering (`bun run test/scripts/run-test.ts shared/constants/session-fees.constants.test.ts`)

**15 pass / 0 fail / 85 expect() calls / exit 0** (~65ms, pure unit tier — no DB, no network, no `.env.test` dependency).

- **Tier 1 — constant shape (4 tests):** each fee constant matches `/^\d+\.\d{2}$/` AND equals its exact mandated value (`"25.00"`); fee constants are strings, never numbers (`Number.isFinite`/`Number.isInteger` = false on the raw value); currency is exactly `"EGP"` (string, 3 uppercase letters); every export is a primitive (`string`|`number` only — no object, no function).
- **Tier 2 — confirmation window (3 tests):** `SESSION_CONFIRMATION_WINDOW_MS === 86_400_000` exactly (both `toBe` and `Object.is`); equals `24 * 60 * 60 * 1000` (24h semantic; arithmetic appears in the ASSERTION only — never on fees); positive `Number.isSafeInteger`.
- **Tier 3 — hostile reads & export surface (7 tests):** module namespace exposes EXACTLY the four constants and nothing else; no numeric-coercion/parse-shaped helper is exported (name-pattern sweep + `typeof !== "function"`); every export survives `Object.getOwnPropertyDescriptors` inspection (own enumerable property, defined value slot, exact mandated value read back through the descriptor); barrel re-export is `Object.is`-identical to the deep import; fee strings survive a JSON round-trip as strings (never collapse to numbers); source-hygiene pins — constants module contains zero `import` statements, zero `Number(`/`parseFloat`/`parseInt(` calls, zero `process.env` reads (purity), and the barrel contains exactly one `session-fees.constants` line in the canonical `export *` form.
- **Tier 4 — N/A by construction:** these constants have NO input surface. The module parses, accepts, or widens nothing — it only exports four immutable primitive literals, and no GraphQL input/resolver surface was added (fee can never be carried by client input through this slice; see §4). Hostile-read coverage for what IS inspectable (descriptors, namespace, barrel, JSON) lives in Tier 3.

### 3.3 Test-convention provenance

Colocated `<file>.test.ts` next to the module (repo convention: `shared/locale/*.test.ts`, `backend/enum/**/*.test.ts`), `bun:test` imports, `@/` deep-import alias, deterministic assertions only, run via the mandated runner `test/scripts/run-test.ts` (not raw `bun test`).

## 4. SEC — Security & Tenancy Audit

- ✅ **Fee is server-owned:** the fee's sole source is this constants module; `createSession` (task 2.8) resolves it per intent server-side. No GraphQL input type, resolver, or schema surface was added in this slice, and `SessionSubmitInput` (task 1.3) structurally omits `fee` — so a client payload structurally cannot carry or influence the fee (BOP layering: wire input → closed whitelist → service resolves constants).
- ✅ **No accidental exposure/coercion:** the module exports only four immutable primitive literals — no coercion/parse helpers, no getters, no mutable containers (Tier 3 pins the exact export surface, so a future "helpful" `sessionFeeToNumber()` export would fail CI here).
- ✅ **String-carry money discipline:** fees are decimal strings end-to-end; the only numeric constant is the confirmation WINDOW (a duration, not money) — no arithmetic on fees exists anywhere in the slice (grep + test pin).
- ✅ **No secrets/oracles/logging:** no `console.*`, no logger, no env reads, no serialization of sensitive data.

## 5. SR — Semantic Review Checklist

- ✅ **Purity:** `session-fees.constants.ts` has ZERO imports (test-enforced pin) and zero `process.env` reads; shared never imports from `@/frontend`/`@/backend`/`@/app` (rule honored; grep-verified — the only `process.env` string in the slice is the test's negative assertion).
- ✅ **Zero dead constants (intended-forward):** all four exports are exercised at runtime by this suite AND have designated forward consumers per the plan: `SESSION_FEE_HIFZ`/`SESSION_FEE_TAJWEED` → task 2.8 `createSession` fee resolution; `SESSION_FEE_CURRENCY` → 4.x wallet/receipt + UI display; `SESSION_CONFIRMATION_WINDOW_MS` → task 2.8 `confirmationDeadline = now + window` and the lifecycle sweeper. They are deliberately NOT imported early by other layers (no speculative imports).
- ✅ **Clean comments (ZERO plan-artifact references):** grep `REQ-|Task [0-9]|Phase [0-9]|DEV3|plan\.md|tasks\.md|specs\.md|B\.[0-9]` over all three files = 0 matches; comments describe domain behavior only (platform-set interim per-intent fees, string-carry discipline, EGP denomination, 24h confirmation window, shared-layer isolation).
- ✅ **No noisy comments / no `console.*`:** header JSDoc density mirrors the `free-trial.constants.ts` neighbor; grep `console\.|logger` = 0.
- ✅ **Scope boundary respected:** `git diff --name-only` = `shared/constants/index.ts` only; untracked new files = the constants module + its test. Exactly the mandated 2 files + sanctioned test file.

## 6. IV — Instruction Verification (`shared/AGENTS.md`, rule by rule)

| Rule | Verdict |
|---|---|
| Layer isolation: NEVER import from `@/frontend/**`, `@/backend/**`, `@/app/**` (ESLint-enforced) | ✅ zero imports at all in the constants file; test imports only `bun:test`, `node:fs`, `node:path`, `@/shared/...` |
| If shared needs a value from another layer, move/duplicate the definition into shared | ✅ the constants ARE the canonical definition (nothing was pulled from other layers) |
| Import convention: `@/` alias, relative `./`/`../` banned (barrel `index.ts` relative re-exports excepted) | ✅ test uses `@/` deep paths; barrel line follows the sanctioned relative `export *` form |
| Prefer deep imports over barrel files | ✅ test deep-imports `@/shared/constants/session-fees.constants`; barrel used only to verify re-export identity |
| File organization: `shared/constants/` = enums + stable domain constants | ✅ correct directory for platform fee constants |
| Shared enum rules (`{domain}.enum.ts`, dot access) | N/A — values not enums; constants file follows the `.constants.ts` naming of `free-trial.constants.ts` |
| i18n/`shared/locale` rules (namespace registration, `Translations` parity) | N/A — no locale files touched (task 1.5's territory) |
| Linting: NEVER `oxlint-disable`; root-cause fixes | ✅ the one oxlint failure was fixed by rewording the JSDoc (no disable comments anywhere) |

Sub-loop rule file additionally surfaced for the test file (`.github/instructions/tests.instructions.md`): honored — `bun:test` utilities, no `console.*`, run via the mandated runner.

## 7. Environment Anomalies (for the orchestrator)

1. **HEAD flip-flop persisted all session:** `git branch --show-current` returned `main` between nearly every command (main now @ `46b0a55`, which carries the 1.1 commit; feature branch @ `d2629ab`, plan-docs only — the two have diverged). The unconditional self-heal checkout from the dispatch protocol was re-run at every step before every write; all implementation files are untracked/modified non-plan files and carried over every flip intact. Final state at write time: on the feature branch. NOTE for the orchestrator: the tasks.md 1.4 checkbox ticks (below) were applied to the worktree copy — if HEAD was on `main` @ `46b0a55` at that moment, the ticks sit on the pre-amendment plan text; reconcile against the feature-branch `tasks.md` at commit time (same situation as the 1.1 entry).
2. `~/.bun/bin/bun` symlink (created by 1.1) still present and functional — no re-provisioning needed.
3. Sub-loop quirk (non-blocking, repo-side): `bun run scripts/health/sub-loop.ts` output prints `main` at the top from the pre-checkout branch echo — cosmetic only.

## 8. Carry-Forward Knowledge (for future subtasks)

- **Exact consumption contract for task 2.8:** `fee` for a Hifz-intent session = `SESSION_FEE_HIFZ`, for Tajweed = `SESSION_FEE_TAJWEED` — both decimal STRINGS; the `session.fee` decimal(10,2) column infers as string, so the value inserts/reads verbatim with zero casts. `confirmationDeadline = now + SESSION_CONFIRMATION_WINDOW_MS` (ms arithmetic on TIMESTAMPS only). `SESSION_FEE_CURRENCY` is for display/receipt surfaces, not for any numeric logic.
- **JSDoc gotcha (shared-layer):** oxlint `jsdoc(check-tag-names)` parses `@/backend`-style tokens in comments as tags and errors. In `shared/**` comments write "backend/frontend/app layers" without the `@/` prefix (the `free-trial.constants.ts` precedent survives because its tokens sit mid-line; new files should avoid `@/…` tokens in comments entirely).
- **Test-infra note:** Bun module-namespace exports read back as writable value-slots (`{value, writable: true, enumerable: true, configurable: false}`) — descriptor tests should assert `enumerable` + value, not immutability of the slot (runtime binding immutability differs per module system and is not a repo guarantee).
- **Barrel order:** `shared/constants/index.ts` is alphabetical (`free-trial` < `recitation-reading` < `session-fees`) — future additions keep sorting by filename.
- **Regex lint trap:** sonarjs rejects `\s*`-prefixed multiline regexes (super-linear backtracking); prefer `/^export const /m`-style linear anchors in test source pins.

## 9. Cross-File Dependencies

- **→ Task 2.8 (service):** `createSession` VALUE-imports the fee constants (deep path `@/shared/constants/session-fees.constants`) to resolve the per-intent fee and compute `confirmationDeadline` from `SESSION_CONFIRMATION_WINDOW_MS`; the closed `SessionSubmitInput` (task 1.3) is what structurally keeps the wire from carrying `fee`.
- **→ Tasks 4.x (wallet/UI/receipts):** `SESSION_FEE_CURRENCY` denominates held-fee display and receipt copy; fee strings render verbatim (no `Number()`/float conversion permitted anywhere downstream).
- **← Barrel:** `shared/constants/index.ts` was the only pre-existing file touched (additive `export *`); verified the top-level barrel contract per `shared/AGENTS.md` (relative `./` sibling re-exports only).
