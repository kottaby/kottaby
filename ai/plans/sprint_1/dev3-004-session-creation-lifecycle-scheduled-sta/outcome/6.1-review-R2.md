# Task 6.1-R2 — Independent Round-2 Review (all four lenses: types + backend + frontend + pentest)

**Date:** 2026-08-31
**Reviewer:** independent round-2 wave agent (no round-1 knowledge except the two outcome files, read for comparison only)
**Baseline:** `b0ca09e` — pre-existing issues are NOT findings. Scope: `git diff b0ca09e --name-only` minus `ai/plans/**`, `*.md`, screenshots, generated codegen artifacts.
**State reviewed:** HEAD `61c39b6` ("fix(session): DEV3-004 — review round-1 fixes") + the single uncommitted worktree hunk `oxlint.config.mts` (assessed in §4).
**Report-only:** zero code changes, zero tree mutations outside this outcome file and the worklog append.

---

## 1. Round-1 fix verification — 3/3 LANDED CORRECTLY

| # | Fix (round-1 finding) | Verdict | Evidence on the current worktree |
|---|---|---|---|
| 1 | **ForbiddenError class** (R1 backend F2) | ✅ LANDED | `session-lifecycle.service.ts:58` imports `ForbiddenError`; `assertActorGovernanceClean` throws `new ForbiddenError(t.forbidden)` at **:555** (R1 cited ~:548 — the +8-line docblock growth accounts for the shift). `backend/lib/errors.ts:51-55` pins `extensions.code = "FORBIDDEN"`; `error-code-taxonomy.ts:44` maps `FORBIDDEN → 403`. Docblock (:539-541) states the class rationale. Lockstep test hardening verified: `session-lifecycle-denials.journey.test.ts:542` now `toBeInstanceOf(ForbiddenError)` (import swapped) — re-run **6 pass / 0 fail / 93 expects**. No other assertion pinned the concrete class (2.8's `expectDomainDenial` is `DomainError`+code+message → untouched, green). |
| 2 | **Exhaustive pothos mappers** (R1 types F3) | ✅ LANDED | `session.pothos.ts:62-126`: all three mappers (`toSessionStatus/Type/Intent`) now take the canonical row-column literal unions as scrutinees (`SessionReturnType["status"]`, `["sessionType"]`, `NonNullable<…["intent"]>`), one case per pgEnum member, **no `default`**, and a trailing fail-closed `const exhaustive: never = …; throw` (registration.service.ts idiom). The compile-error property is REAL: a future pgEnum member without a case fails the `never` assignment at `tsgo` (scrutinee comes from the schema's `$inferSelect` over `pgEnum("session_status", […])`). Resolvers (:147-165) simplified to direct mapper calls; column-`null` intent still resolved by the field before the mapper. R1's `null`-mapper middle layer is gone (no silent path). |
| 3 | **Dialog onClose loading gate** (R1 frontend MINOR) | ✅ LANDED | `CancelSessionConfirmDialog.tsx:216-223`: `handleDialogClose` ignores backdrop/Escape while `loading`; wired at `Dialog onClose` (:228). Prop docblock (:62-67) rewritten — now TRUE. Both action Buttons separately `disabled={loading}` (:269, :276); the container's `closeCancelDialog` remains unconditional but is only invoked through the gate / outcome handlers. |

**No regression from the fix commit** — re-run on this worktree:

| Gate | Result |
|---|---|
| `bun run tsgo` | exit 0, 0 errors |
| `bunx @biomejs/biome check .` | 570 files, 0 diagnostics |
| `session-lifecycle.service.test.ts` | **37 pass / 0 fail / 362 expects** |
| `session-sdl.test.ts` | **10 pass / 0 fail / 45 expects** |
| `schema-surface.test.ts` | **12 pass / 0 fail / 56 expects** (incl. byte-identical codegen-sync pin) |
| extras (fix-commit-touched files) | denials journey **6/0/93** · frontend scheduling queries **14 pass / 0 fail / 53 expects** |

---

## 2. Fresh-sweep verdicts (independent eyes)

### 2.1 types lens — **PASS**

- No new/renamed canonical type drifts from contracts: `SessionReturnType/SelectType/InsertType` derive from `session.$inferSelect`; `SessionStudentIntentType` stays `Equals`-pinned to `SessionRequestContract["intent"]` by the conformance test-d (consumes, never forks, the contract).
- `import type` vs value imports grepped over every new/changed file: **all correct** — type-only where no runtime use (`schema/classes/session.ts` → `$type<>()` only; `session.types.ts`; contracts; `pothos/builder.ts`), VALUE where runtime use exists (pothos mappers/registrations, resolvers, service guard+`Object.values`, repos' `eq(session.status, …)`, frontend `SessionStatus`/`UserRole` from codegen). No site imports a value where only a type is needed (tsgo 0 + biome 0 corroborate).

### 2.2 backend lens — **1 NEW finding (MEDIUM)** (below), everything else PASS

- **Error-code taxonomy exactness (REQ-050) — mapped throw-by-throw:** `ValidationError()` ×3 (:188/:191/:197) + reason guard (:403) = `VALIDATION` 422 pre-DB ✓; `NotFoundError("TEACHER")` :216 = `TEACHER_NOT_FOUND` 404-class ✓; `ConflictError("TEACHER_NOT_CERTIFIED")` :224/:688 = 409 ✓; `ValidationError("INSUFFICIENT_BALANCE")` :243 = 422 custom-code row ✓; `NotFoundError("SESSION")` ×4 (:586/:623/:633/:654) = oracle-safe 404-class ✓; `ConflictError("DUPLICATE_REQUEST")` :592 = 409 replay row ✓; `ConflictError("SESSION_INVALID_TRANSITION")` ×3 (:643/:666/:681) = 409 ✓; `ForbiddenError` :555 = 403 ✓. Every REQ-050 row is constructible exactly as specified.
- **NotFoundError double-suffix rule — PASS:** entity names `"TEACHER"`/`"SESSION"` only; the full codes are never passed to the constructor (grep: zero `NotFoundError("SESSION_NOT_FOUND"…)`-style sites).
- **Claim-table userId scoping on the replay branch — PASS (traced):** `insertClaim` binds `userId: studentId` (ctx, service :254); `session_request_idempotency_key_unique` is on the KEY ALONE (schema :36) so any second insert with the same key lands in the replay branch regardless of caller; `replayBooking` (:573-593) is `Promise<never>` — it **always throws**: `claim.userId !== actorStudentId` → oracle-safe `NotFoundError("SESSION")`; equal → `ConflictError("DUPLICATE_REQUEST")`. The claim's `sessionId` pointer is NEVER read on any path, so a key owned by user A can never surface user B's session (or any session) — the crafted same-key collision yields only (a) A's own 409 or (b) a B-visible 404 indistinguishable from a nonexistent session. The colliding attempt's own debit rolls back with the tx.
- **Refund correctness when `heldBalanceLane` IS NULL — ruling is RIGHT (PASS):** the refund keys on lane presence at cancel time (service :416-424), not on `feeHeld`. The ONLY producer of `feeHeld = true` in the codebase is this service's booking insert (:279), which sets the lane in the SAME statement — so `feeHeld=true ∧ lane=NULL` is structurally unreachable through the app; pre-existing rows (DEV1-004 era) carry the schema default `fee_held=false` and `held_balance_lane=NULL`, and cancelling them refunding nothing is exactly correct (no hold exists). `cancelSessionOnce` clears `feeHeld` in the same guarded statement and terminal states are unreachable in its predicate — a double cancel can never double-refund. Unreadable non-null lane fails closed and rolls the cancellation back.
- **Raw `new Error` sites** (service :421 unreadable-lane; repo "insert returned no rows" guards) — NOT a REQ-052 violation: they mirror pre-existing baseline idioms (`UserRepository.create`/`ApplicantRepository.create`/registration.service exhaustive guards), are unreachable invariants, and REQ-050's last row routes them to the masked `INTERNAL_SERVER_ERROR` 500 boundary. Pass.
- **tx propagation, BOPLA spreads, log hygiene, cross-layer imports** re-spot-checked on the post-fix tree — unchanged green (R1 evidence carries; the fix commit touched none of those surfaces).

### 2.3 frontend lens — **PASS** (2 INFO notes, below)

- **`teacherConfirmedAt` i18n key parity — PASS:** present in all three layers — `shared/locale/types/sessions/index.ts:38`, `en/sessions/index.ts:12`, `ar/sessions/index.ts:12` (`"تأكيد المعلم"`) — namespace-parity test green; `SessionRow.tsx:242-244` renders the cell only when `confirmedByTeacherAt !== null` via the locale formatter.
- **`SessionRow` actions prop — per-action disabling verified (not global):** each `SessionRowAction` carries its own `disabled` (:127, applied at :251 `disabled={action.disabled === true}`); the teacher container feeds `startInFlightId === session.id` / `completeInFlightId === session.id` per descriptor (:242, :250) — siblings and the other action kind stay live; the student path omits the prop entirely (byte-unchanged behavior).
- **Container eviction arms / rapid double-click — graceful (PASS):** for an OWNED row the loser of a start/cancel race can never receive `SESSION_NOT_FOUND` — the fused predicates make the miss classify as `SESSION_INVALID_TRANSITION` (owned + wrong state), surfacing a row-scoped inline alert on the already-flipped row (copy is state-accurate, no crash). The `SESSION_NOT_FOUND` arm itself is idempotent: ROOT_QUERY `modify` filter + `evict` + `gc()` on an absent entity are no-ops, and every state update is functional (`prev => …`). No stale closure found (per-call options capture `sessionId`; callbacks re-memoized on `t`/`te`/`client`).

### 2.4 pentest lens — **PASS** (no new findings beyond the backend MEDIUM)

- **`startSession` SQL predicate bypass — NONE:** the guarded UPDATE is `and(eq(id, $id), eq(teacherId, $ctx.user.id), eq(status, Scheduled))` (repo :225) — the only client arg is `id` (a single bound parameter); owner identity and status vocabulary are ctx/enum-bound. No argument combination can widen the predicate; the certification `EXISTS` in `completeSessionOnce` is likewise fused in-statement.
- **Cancel by a student whose session is completed — `SESSION_INVALID_TRANSITION`, no refund reachable:** terminal statuses are structurally outside `cancelSessionOnce`'s status predicate (repo :298); the zero-row miss classifies through the probe (participant ✓ → state conflict, service :638-643); the refund block is only reachable AFTER a non-null guarded-UPDATE row (:407-424). Double-cancel cannot double-refund.
- **`reason` ≤500 both layers — CONFIRMED:** pre-DB service guard (`trimmed.length > MAX_CANCEL_REASON_LENGTH` → `VALIDATION`, service :401-404) **and** client `maxLength` attr on the textarea (`slotProps.htmlInput.maxLength = 500`, dialog :262) plus a submit-time re-guard (:208-211). `reason` is then discarded — never persisted, never logged.

---

## 3. NEW findings

**Count: 0 BLOCKER · 0 HIGH · 1 MEDIUM · 3 INFO**

1. **[MEDIUM] backend/services/classes/session-lifecycle.service.ts:311-327, :348-364, :392-428 (and :442-455) — REQ-054 positive-safe-integer guard is missing on the target session id for `startSession` / `completeSession` / `cancelSession` / `sessionById`.** `isPositiveSafeInteger` guards only `studentId`/`teacherId` inside `createSession` (:187, :190 — grep-verified call sites). The GraphQL boundary parses shape-only (`Number(args.id)` — mutation.ts:144/:170/:198, query.ts:178), so a malformed `ID` string ("12abc" → `NaN`, "1.5", 1e20) reaches the parameterized SQL as a non-integer → PostgreSQL 22P02 → masked `INTERNAL_SERVER_ERROR` (500). Deviations: (a) REQ-050's "Bad id shape → `VALIDATION` 422, pre-DB" row is not honored on these four entry points (well-formed-but-nonexistent/foreign ids still classify correctly as `SESSION_NOT_FOUND`/`null` — the oracle is intact where it matters); (b) REQ-054's "…ALL before ANY DB read" is unmet — `assertActorGovernanceClean` (or the probe) reads before any shape decision; (c) committed docblocks/outcomes assert the opposite: mutation.ts:16-18 and outcome/3.3:34 claim "the service re-validates it as a positive safe integer"; service :434-435 + outcome/2.8:71 claim garbage ids "degrade to `null` through the parameterized lookup" — both false today (the service test's non-safe-integer matrix covers only createSession, :467-480). **No security impact** (bound parameters, fail-closed masking, no injection, no oracle change). Fix shape: extend the existing pre-DB guard to `sessionId` at the top of the four functions (one `isPositiveSafeInteger` check → `ValidationError(t.validation)`), or guard at the `Number()` parse sites — plus a service-suite cell per path. Owner: DEV3-004 feature maintainer (service boundary already flagged for the D10 widening touch).
   > **RESOLVED IN 6.1-R2fix (2026-08-31):** shared module-private `isPositiveSafeSessionId(id: unknown)` + `assertPositiveSafeSessionId(id, t)` added beside `isPositiveSafeInteger` in `session-lifecycle.service.ts` (the exact guard idiom `createSession` uses for teacherId/studentId) and called at the TOP of `startSession` (:362) / `completeSession` (:408) / `cancelSession` (:462 — first check, before the reason guard): a non-positive-safe-integer target id → `ValidationError(t.validation)` (`VALIDATION`/422) BEFORE any DB work (pre-governance — no probe read is spent). `getSessionById` short-circuits malformed ids to the SAME oracle-safe `null` pre-DB (:520) — docblock rewritten to the now-true contract; mutation docblocks (mutation.ts:16-18) now TRUE as written, query.ts shape-decision comment reworded to name the guard. Test cells: service suite "session-id shape guards" (NaN/1.5/1e20/-1/0 — the runtime shapes of "abc"/"12abc"/"1.5"/"1e20"/"-1"/"0" through the shape-only boundary parse — plus a boundary-skipped non-number "abc" leg via the Object.assign overlay, × start/complete/cancel → `VALIDATION` via `expectDomainDenial`; × getSessionById → `null`; zero-writes oracle) → suite **38 pass / 0 fail / 441 expects**; GraphQL cell `startSession(id: "12abc")` → `expectMutationError(…, "VALIDATION")` → mutations suite **36 pass / 0 fail / 105 expects**. Gates: `bun tsgo` 0 · biome 570 files 0 · queries suite 14/0/53 · student+teacher component suites 46 pass / 12 skip / 0 fail / 472 expects · denials journey 6/0/93 · sub-loop `--lifecycle duplicates` exit 0 on all six touched files.
2. **[INFO] oxlint.config.mts:108-114 (uncommitted worktree hunk) — load-bearing lint exemption left uncommitted.** The `no-underscore-dangle` file-list extension for `TeacherSessionsContainer.tsx` (`item.__ref` bracket-access escape, same protocol-mandated Apollo wire property as the dialog) exists only in the worktree; stashing it makes oxlint report 1 warning on that file, i.e. the lint gate is red without it. Commit-ownership class, identical to R1's F4 process note — orchestrator must own the commit (verified: with the hunk, oxlint = 0/0 on the file).
3. **[INFO] frontend/views/teacher/sessions/TeacherSessionsContainer.tsx:283-284, :242, :250 — single per-kind in-flight slot under concurrent same-kind mutations on different rows.** Sibling rows deliberately stay live, so Start(A) then Start(B) can both be in flight; the one slot (`startInFlightId`) then holds B, re-enabling A's CTA while A's mutation is still pending (the docblock's "each affordance disabled while ITS OWN mutation is in flight" holds only for one in-flight per kind). Consequence is benign: a re-click on A hits the server's guarded predicate and lands in the graceful `SESSION_INVALID_TRANSITION` row-alert arm. Hardening only (e.g. `Record<string, true>` in-flight maps) if the flow ever grows retries.
4. **[INFO] backend/db/schema/classes/session.ts:24-26 + backend/enum/scheduling/held-balance-lane.enum.ts:10-11 — docblock accuracy: no production path ever returns `held_balance_lane` to NULL.** Both docblocks state the lane is NULL "after the hold is released or consumed", but cancellation flips only `fee_held` (repo :293) and KEEPS the lane — by design, since the same-statement `RETURNING` row's lane IS the refund driver (service :416-424) — and completion keeps it too. Behavior is coherent (lane = provenance; `fee_held` = hold marker) and DEV3-012/013's documented consumption ("dual-confirm flips `fee_held=false`; timeout release re-uses the same-lane refund primitive") depends on the lane SURVIVING; the "NULL after release/consumed" sentences are the only inaccuracy. Doc-only touch at next schema-file edit.
   > **RESOLVED IN 6.1-R2fix (2026-08-31):** both docblocks reworded to the true semantics — the lane is the row's PERMANENT refund provenance (NULL only while no fee has ever been held; once placed it is never rewritten or nulled — release/consumption flip `fee_held` only, and every cancellation/timeout refund reads the recorded lane and returns to the same lane that paid).

**Not counted (verified non-findings):** the round-1 F1 `Object.assign` intent overlay — unchanged, correctly forwarded as ledger row **D10** (⏸) by 6.1-fix; round-1 INFO items (page upper bound, teacher-existence oracle, gateway depth limiter) stand with their recorded owners and are not re-raised.

---

## 4. Verdict

- **Fix verification: 3/3 landed correctly**, no regression — tsgo 0, biome 0/570, service suite 37/0/362, SDL 10/0/45, schema-surface 12/0/56 (+ denials journey 6/0/93 and frontend scheduling queries 14/0/53, extra).
- **Fresh sweep:** types PASS · backend PASS except 1 MEDIUM (REQ-054 id-shape guard absent on the four non-create entry points + false docblock claims) · frontend PASS (2 INFO) · pentest PASS.
- **Blocking findings: none** — the MEDIUM is a boundary-validation/taxonomy-exactness gap with zero security impact; it can ride the next service-boundary touch (the D10 widening already targets that signature). All INFO items are doc/commit-ownership notes.
