# Task 0.1 Outcome — Pre-Implementation Baseline Capture

**Task ID:** 0.1
**Agent:** Phase 0.1 Baseline Subagent
**Date:** 2026-08-30
**Branch state at capture:** `main` @ `b0ca09e` (⚠ see Anomaly A-1 — feature branch `feat/dev3-004-session-creation-lifecycle-scheduled-sta` points at the IDENTICAL commit `b0ca09e`, so tree content is byte-identical; baseline validity unaffected. No branch switch was performed, per the "do not switch branches" directive.)

---

## 1. Baseline Counts

### 1.1 `bun tsgo` (TypeScript 7 native, `tsgo -b --noEmit`) — **0 errors** ✅

- Command: `bun tsgo 2>&1 | tee /tmp/baseline-tsgo-full.log | grep -c "error TS" > /tmp/baseline-tsgo.txt`
- Result: `grep -c "error TS"` = **0** (`/tmp/baseline-tsgo.txt`); full log at `/tmp/baseline-tsgo-full.log`.
- Underlying script: `scripts/restore-next-env-dts.ts && bun run scripts/lib/run-locked-cmd.ts tsgo tsgo -b --noEmit` — completed with **exit code 0** (re-verified on a second run: `exit=0`, ~1s duration; tsgo 7.0.0-dev.20260707.2).
- Note: tsgo is the Go-native compiler — full-repo check completes in ~1s; incremental state in untracked `tsconfig.tsbuildinfo` (gitignored, never committed — no tree mutation).
- `restore-next-env-dts.ts` ran clean; no `next-env.d.ts` modification observed.

### 1.2 `bunx @biomejs/biome check .` (WITHOUT `--write`) — **0 warnings** ✅

- Command: `bunx @biomejs/biome check . > /tmp/baseline-biome-full.log 2>&1` (no `--write`; "No fixes applied")
- Result: **exit 0**; `Checked 514 files in 9s. No fixes applied.`; `grep -c "warn"` on the full log = **0** (`/tmp/baseline-biome.txt`).
- Zero diagnostics of any kind (errors or warnings). Tree not mutated.

### 1.3 `bun run scripts/lint-service.ts --json --id baseline` — **script ran; child ESLint OOM-killed; adapted baseline = 0 errors / 0 warnings** ⚠

- The CLI accepted `--json --id baseline` natively (parseArgs supports `-i/--id`, `--json`; no argument adaptation needed for parsing).
- Result of the exact invocation (`/tmp/baseline-lint.json`): `{"success": false, "output": "", "exitCode": 1, metrics: {scope: "full-repo", durationMs: 52822}}` — i.e. **exit 1 with EMPTY ESLint output after 52.8s**.
- **Failure mode (root-caused): OOM kill.** `scripts/lint-service.ts` spawns ESLint with a hard-coded `NODE_OPTIONS="-r ./scripts/ts6-eslint-patch.cjs --max-old-space-size=8192"` and `--concurrency=4`. Kernel dmesg confirms: `oom-kill … task=MainThread … anon-rss:3453588kB, total-vm:8188488kB` — the child JVM… (ESLint/Node) heap grew to ~3.4 GB RSS against a **3.9 GiB sandbox** (2 vCPU, no swap) and was SIGKILLed. `exec()`'s callback then maps the killed signal to `exitCode: 1` with empty captured output. A direct reproduction of the raw eslint command OOM-killed identically (0-byte log). **This is an environment constraint, not a code defect.**
- **Adapted baseline run (documented per task directive "capture the failure and adapt"):** identical ESLint config/invocation, reduced footprint — `NODE_OPTIONS="-r ./scripts/ts6-eslint-patch.cjs --max-old-space-size=2048" bun x eslint --concurrency=1` (full-repo, no `--fix`, no `--write`):
  - **exit 0, empty output (no reported problems)** — `/tmp/eslint-lowmem.log`.
  - JSON-format confirmation run: **494 files linted, errorCount=0, warningCount=0** (`/tmp/eslint-json.log`, summarized via bun: `{"files":494,"errors":0,"warnings":warns=0}`).
- ⚠ **Carry-forward constraint for later gates:** full-repo ESLint in this sandbox MUST run with `--concurrency=1` and `--max-old-space-size≈2048` (or raise sandbox memory). The default `lint-service` full-repo path (concurrency 4, 8 GB heap) OOMs. Post-implementation lint gates must reuse this adaptation.

### 1.4 Git baseline

- `git stash list` → **empty** (`/tmp/baseline-stash.txt`).
- `git diff --name-only` → **EMPTY** (`/tmp/baseline-files.txt`) — no tracked file mutated by any baseline command (tsgo, biome, eslint all ran non-mutating; caches/tsbuildinfo are gitignored).
- `git status --porcelain` (captured pre-seeding) → only `?? ai/plans/sprint_1/dev3-004-…/outcome/` (the outcome dir itself — expected deliverable location, untracked).
- After step 3 (ledger seeding, sanctioned): tracked-file delta = **only `deferred-items.md`** (the plan ledger, seeded per directive — not a source file).

**Baseline conclusion: the tree is at a ZERO-error / ZERO-warning state** for tsgo, biome, and (adapted) ESLint. There are therefore **NO pre-existing issues to ignore during post-implementation review** — the post-implementation gate can require **baseline delta = 0** strictly (any new tsgo error, biome diagnostic, or eslint problem is attributable to this ticket's work). Note the known-lint caveat in §1.3 when re-running the ESLint portion of the gate.

## 2. Command Failures & Failure Modes (summary)

| Command | Status | Failure mode |
|---|---|---|
| `bun tsgo` | ✅ exit 0, 0 errors | none |
| `bunx @biomejs/biome check .` | ✅ exit 0, 0 warnings (514 files) | none |
| `bun run scripts/lint-service.ts --json --id baseline` | ⚠ returned `success:false, exitCode:1, output:""` | Child ESLint **OOM-killed** (kernel dmesg `oom-kill`, ~3.45 GB RSS vs 3.9 GiB sandbox; lint-service hard-codes `--max-old-space-size=8192` + `--concurrency=4`); exec() maps the kill to exitCode 1 with empty output. Adapted run (concurrency 1, 2048 MB) → exit 0, 494 files, 0 errors/0 warnings |
| git baseline trio | ✅ | none; diff empty |

## 3. Deferred-Items Ledger — Seeded ✅

`deferred-items.md` seeded with the five forward items (status ⏸ Forward — neutral; no ❌/⚠️):

- **D1** — session request/lifecycle event notifications → DEV3-010/DEV3-011
- **D2** — dual-confirmation student confirm + 24h auto-cancel sweeper + wallet credit → DEV3-012/DEV3-013
- **D3** — `is_online` availability assertion + directory wiring → DEV3-008/DEV2-011
- **D4** — student-facing booking UI over the directory → DEV3-009
- **D5** — INV-S6/S7/S8 + `disputed` state → DEV3-005/DEV2-013/DEV3-022

## 4. Anomalies & Discrepancies (for the 0.3 plan-review gate)

- **A-1 (branch pointer):** HEAD was on `main` @ `b0ca09e` at capture time, not the feature branch. The feature branch points at the identical commit (`b0ca09e`), so the working tree is identical and the baseline is valid. Per the "do not switch branches" directive, **no switch was performed** — the orchestrator/implementer should `git checkout feat/dev3-004-session-creation-lifecycle-scheduled-sta` before Phase 1 (no tree change will occur).
- **A-2 (D1 ID collision, resolved):** the pre-populated `deferred-items.md` template used "D1" for the **naming decision** (✅ Done, in-plan), while tasks.md task 0.1 / specs REQ-001 define D1 = **notifications → DEV3-010/011** (plan.md §Knowledge-Propagation also lists the naming item under "D1"). Resolution: D1 = notifications (tasks.md/specs/orchestrator directive wins); the naming row is preserved as **D6** (✅ Done, verified by task 0.2 §5 collision check) for ledger-contract traceability. Reconciliation note embedded in the ledger.
- **A-3 (template state):** tasks.md task 0.1 describes `deferred-items.md` as "an empty template" (and specs REQ-001 says "currently EMPTY") — in fact it arrived **pre-populated** with D1(naming)/D2–D5 rows. The D2–D5 rows matched the requested items semantically and were re-expressed verbatim with the ⏸ marker.
- **A-4 (status marker):** the template legend used `📅 Forward`; the directive mandates a neutral marker like `⏸ Forward` (never ❌/⚠️). Legend updated to define `⏸ Forward`; all forward rows use it. Grep nuance for the REQ-083 gate: the literal `grep -c "❌\|⚠️"` on the ledger = **2**, and BOTH matches are the template's own Status-Values legend definitions (carried over verbatim from the original template) — **zero** in item statuses. The original pre-populated template already contained these 2 legend matches; the 0.3 gate should interpret the gate as item-status-based (or amend the legend wording).
- **A-5 (outcome filename):** specs REQ-001 names this artifact `outcome/phase0-baseline-outcome.md`; REQ-083 / orchestrator use `outcome/<task-id>-outcome.md` (precedent: `outcome/0.2-outcome.md`). This file follows the `<task-id>` convention (0.1-outcome.md); the 0.3 gate may add an alias if REQ-001's literal name is enforced.
- **A-6 (sandbox memory):** 3.9 GiB RAM / 2 vCPU / no swap. Any quality gate that spawns memory-heavy children (ESLint default config; possibly type-aware lint `--max-old-space-size` paths) must be footprint-tuned (see §1.3) or it will be OOM-killed and misreport as `exitCode: 1` with empty output.

## 5. Artifacts

- `/tmp/baseline-tsgo.txt` (count 0), `/tmp/baseline-tsgo-full.log`, `/tmp/baseline-tsgo-meta.txt` (pipeline exit/duration)
- `/tmp/baseline-biome.txt` (count 0), `/tmp/baseline-biome-full.log` (exit 0)
- `/tmp/baseline-lint.json` (lint-service result incl. metrics), `/tmp/eslint-lowmem.log` (adapted run), `/tmp/eslint-json.log` (494-file JSON proof)
- `/tmp/baseline-stash.txt`, `/tmp/baseline-files.txt`, `/tmp/baseline-status.txt`
- `deferred-items.md` — ledger seeded (D1–D5 forward, D6 done-for-traceability)
