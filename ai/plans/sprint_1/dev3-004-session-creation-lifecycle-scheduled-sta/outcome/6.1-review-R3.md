# Task 6.1-R3 — Independent Round-3 Confirmation Review (all four lenses)

**Date:** 2026-08-31
**Reviewer:** independent round-3 wave agent (CONFIRMATION round — target: ZERO new findings)
**Baseline:** `b0ca09e` — pre-existing issues are NOT findings. Scope: `git diff b0ca09e --name-only` minus `ai/plans/**`, `*.md`, screenshots, generated codegen artifacts.
**State reviewed:** HEAD `55af121` ("fix(session): DEV3-004 — REQ-054 session-id shape guards on all 4 read/mutation paths + lane provenance docblocks") + the single uncommitted worktree hunk `oxlint.config.mts` (the R2 INFO-2 state, unchanged — see §3).
**Report-only:** zero code changes; tree mutations limited to this outcome file and the worklog append.

---

## 1. R2fix verification — 4/4 LANDED CORRECTLY, no regression

| # | Fix (R2 MEDIUM item 1 + INFO item 4) | Verdict | Evidence on the current worktree |
|---|---|---|---|
| 1 | **`startSession` id-shape guard** | ✅ LANDED | `session-lifecycle.service.ts:362` `assertPositiveSafeSessionId(sessionId, t)` — the FIRST statement after `t` is resolved, BEFORE the governance re-check (:365) and before any repository call (:367). |
| 2 | **`completeSession` id-shape guard** | ✅ LANDED | `:408`, same position — before governance (:411) and the guarded UPDATE (:413). |
| 3 | **`cancelSession` id-shape guard** | ✅ LANDED | `:462` — the FIRST check of the whole flow, before the reason guard (:465) and before `withTransaction` (:469). |
| 4 | **`getSessionById` malformed → `null` pre-DB** | ✅ LANDED | `:520-522` — `if (!isPositiveSafeSessionId(sessionId)) return null;` BEFORE `findById` (:524). No probe read, no logger call anywhere in the function (verified by reading the full body — the only null channels are the guard, the row miss, and the participant predicate). |
| + | **Lane-provenance docblocks (R2 INFO-4)** | ✅ LANDED | `backend/db/schema/classes/session.ts` + `backend/enum/scheduling/held-balance-lane.enum.ts` reworded in the fix commit (git show 55af121: +8/+7 lines, docblock-only) — lane = permanent refund provenance, `fee_held` is the release/consumption marker. |

**Test cells — both suites re-run green on this worktree:**

| Suite | Result | New cells verified |
|---|---|---|
| `session-lifecycle.service.test.ts` | **38 pass / 0 fail / 443 expects** (stable across two runs) | "session-id shape guards" cell (:501-566): NaN/1.5/1e20/-1/0 matrix × start/complete/cancel → `expectDomainDenial(…, "VALIDATION")`; boundary-skipped leg (raw "abc" via the sanctioned `Object.assign` overlay, :521) × all three mutations; read → `null` for all six shapes; zero-writes oracle (no session rows, no claims, lane untouched) closes the cell. |
| `session-lifecycle-mutations.test.ts` | **36 pass / 0 fail / 105 expects** | Section 7 (:587-600): `startSession(id: "12abc")` over the live wire — the ID rides verbatim as the raw string, boundary parses shape-only → NaN → service guard → `expectMutationError(…, "VALIDATION")`. |

Extras re-run as regression insurance: denials journey **6 pass / 0 fail / 93 expects** (unchanged from R2 — the guards shifted nothing in the denial classification). Gates: `bun tsgo` exit 0 · `bunx @biomejs/biome check .` 570 files, 0 diagnostics · oxlint on the three session views **0 warnings / 0 errors** (with the uncommitted hunk in place).

---

## 2. Targeted sweep of the fix itself (new code = new risk)

**(a) Guard order matches the documented order — PASS.** In all three mutations the id-shape check is the first guard executed and precedes the governance probe (`startSession` :362→:365, `completeSession` :408→:411) — a garbage id never spends the governance read. In `cancelSession` the id check precedes the reason check (:462→:465), exactly as the docblock (:459-461) states. The module docblock (:26-30) and each function docblock state the same order; code matches words.

**(b) `assertPositiveSafeSessionId` uses the SAME idiom as `createSession` — PASS, no divergence.** `isPositiveSafeSessionId` (:136-138) is `typeof id === "number" && isPositiveSafeInteger(id)` — it REUSES the exact `isPositiveSafeInteger` helper (:122-124) that `createSession` applies to `studentId`/`teacherId` (:224, :227). The throw is `new ValidationError(t.validation)` (:153) — same class, same localized message key, same pre-DB position as `createSession`'s participant guards. The only delta is the deliberate `unknown` parameter + `typeof` leg (the honest widening for the shape-only `Number(args.id)` boundary parse), documented at :126-135 and exercised by the test's boundary-skipped leg.

**(c) `getSessionById` malformed → `null` pre-DB — PASS (no probe, no log noise).** The guard short-circuits before `findById`; no logger call exists on any path of the function; a malformed id is indistinguishable from a nonexistent/foreign one (oracle-safe `null`), and the test cell asserts exactly that for all six hostile shapes plus the boundary-skipped leg.

**(d) No OLD-behavior tests left behind — PASS.** Grep over the session test files for `22P02` / `masked 500` / `INTERNAL_SERVER_ERROR`: every hit is a COMMENT stating the guard rationale or the NEW expected behavior (service test :508-512, mutations test :16-17, :590); zero assertions pin a SQL error or a masked 500 for a malformed session id. The fix commit's diff shows no test deletion (only additions), so nothing had to be removed — no test ever asserted the 22P02→500 path.

---

## 3. Full-lens micro-sweep (greps over `git diff b0ca09e` files + gates)

| Check | Result | Notes |
|---|---|---|
| `bun run tsgo` | ✅ exit 0, 0 errors | Includes the uncommitted test-tier state. |
| `bunx @biomejs/biome check .` | ✅ 570 files, 0 diagnostics | |
| `console.*` in changed production files | ✅ 0 | Only 2 docblock mentions of "never `console.*`" (registration.service.ts:28/:334 — prose). |
| `as ` casts in src/ (non-test) | ✅ 0 | Test files carry only the sanctioned idioms: `as const` (journey-fixtures.ts:76) and the `as AppLocale[]` locale-array overlays (student/teacher component suites, STUI_LOCALE legs). |
| `...input` / `...insert` / `...props` spreads | ✅ 0 in the feature slice | The single `{ ...input, role: "student" }` (registration.service.ts:209) is PRE-EXISTING at baseline (verified via `git show b0ca09e` — same line), outside the REQ-031 gate scope (`services/classes/` + `mutation/classes/`), and R1 assessed that file as the byte-equivalent `withTransaction` extraction. Not re-raised. |
| `await import(` | ✅ 0 | |
| Plan-artifact references in production comments (`Task N.N`, `Phase N`, `.ai/plans` paths) | ✅ 0 | `REQ-####` / `plan §N.N` docblock citations are the pre-existing house vocabulary (present at R1 baseline, passed by rounds 1-2; the commit-message `Plan:/Phase:/Tasks:` trailers are the sanctioned format). The R2fix added only the REQ-054 citations that document the requirement the new guards implement — no NEW process leakage. |
| TODO / FIXME / XXX / HACK | ✅ 0 | |

---

## 4. Spot-reads (last reviewer before sign-off — one pass each over the 3 most complex functions)

- **`createSession` four-phase (:214-329):** pre-DB guards (id shapes, key length, intent membership) → governance re-check → one captured instant → ONE `withTransaction`: cert lock (`FOR UPDATE`) → trial-first debit ladder → savepoint-bracketed claim insert → session insert → claim backfill. Ordering intact; the replay branch returns `replayBooking(...)` which is `Promise<never>` — the throw unwinds the transaction callback so this attempt's debit rolls back (rollback = the only cleanup). The unique-violation detector traverses causes cycle-safely and never inspects messages. No new observation.
- **`cancelSession` refund arm (:450-491):** id-shape guard FIRST → reason length guard (then discarded) → tx: fused `cancelSessionOnce` → zero-row miss classified by the cold probe only after the miss; on a hit the refund keys on the guarded row's own `heldBalanceLane` (:479), fails closed on an unreadable lane (log `{sessionId}` only + raw Error → rollback leaves row and hold consistent), and increments `cancelled.studentId`'s lane in the SAME tx — the correct recipient (every lane debits the student). Double-cancel cannot double-refund (terminal states structurally outside the guarded predicate). No new observation.
- **`replayBooking` (:650-670):** always throws; a claim owned by another caller → oracle-safe `NotFoundError("SESSION")` logging only the CALLER's own id (the claim owner is never surfaced); a vanished claim (null) → fail-closed `ConflictError("DUPLICATE_REQUEST")`; the idempotency key is never logged (the duplicate log carries `{code, entity}` with no entityId). No new observation.

---

## 5. New findings

**Count: 0 BLOCKER · 0 HIGH · 0 MEDIUM · 0 LOW · 0 INFO — the round's verdict is PASS.**

Observations recorded (NOT findings):

1. **Bookkeeping, PASS-direction:** the 6.1-R2fix worklog/outcome note recorded the service suite at **441** expects; the committed suite measures a stable **443** across repeated runs (the worktree test file is byte-identical to HEAD — verified via `git status`). Two more asserts than recorded; both counts green. Immaterial to any verdict.
2. **Commit-ownership (stands from R2 INFO-2, unchanged):** the load-bearing `oxlint.config.mts` `no-underscore-dangle` extension for `TeacherSessionsContainer.tsx` remains an uncommitted worktree hunk; oxlint is 0/0 WITH it (re-verified this round). The orchestrator owns the commit.
3. **Environment (pre-documented class, not code):** the mutations suite's first run this round hit `EADDRINUSE :::3066` from a stale `next-server` (pid 32326) left by an earlier session; SIGKILL freed the port and the suite ran green (36/0/105). The 6.1-R2fix worklog already records the same artifact class (its stale pid 31084). Suite-infra hygiene, no code implication.

---

## 6. Verdict

- **R2fix verification: 4/4 landed correctly** (3 mutation guards + the read's null channel), lockstep test cells present and green, lane-provenance docblocks reworded, no regression.
- **Targeted fix sweep:** guard order ✅ · idiom parity with `createSession` ✅ · read channel silent-pre-DB ✅ · zero old-behavior test leftovers ✅.
- **Micro-sweep:** tsgo 0 · biome 0 · oxlint 0 · all grep gates clean.
- **NEW findings: 0.** Round 3 CONFIRMS the plan's backend surface as review-clean; the remaining open items are exactly the ledger rows rounds 1-2 already recorded (D10 overlay residual; INFO commit-ownership) — nothing new to add.
