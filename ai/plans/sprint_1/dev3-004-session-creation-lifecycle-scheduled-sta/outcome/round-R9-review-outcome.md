# R9 — Independent Review Round 9 (fresh eyes)

**Plan:** ai/plans/sprint_1/dev3-004-session-creation-lifecycle-scheduled-sta/
**Branch:** feat/dev3-004-session-creation-lifecycle-scheduled-sta @ a14c61c + uncommitted F-1/F-3/F-4 fix sets + deferred ledger D1–D14 (reviewed at WORKING-TREE state — that is what gets committed)
**Round scope:** fresh 4-lens sweep (types / backend / frontend / security) over span `git diff --name-only b0ca09e..07e3488 -- backend/ shared/ frontend/ app/` (82 files, read at working tree) plus the F-1 file set (session.repository.ts, session.repository.test.ts, warning-surfacing.test.ts, TeacherWalletContainer.tsx + suite), hunting ONLY genuinely new material findings.
**Carried filters (RECORDED — none of these count as new):** deferred-items.md D1–D14 (incl. the whole escrow-lifecycle design family — D12/D13/D14 cover the escrow lifecycle; anything in that family = RECORDED), R6/R7/R8 outcomes, R5-FE items (chips-group aria-label, raw intent-enum row title, single Snackbar slot), jscpd clones ×2 (cancelSessionOnce≅openDisputeOnce; TeacherWallet suite locale-loop bodies), pentest INFOs (unbounded page, teacher-existence oracle), let-scan statement-position caveat, AGENTS.md out-of-bullet doc-prose refs (:5/:75), service-test REQ-04x names, PRE-EXISTING plan-prose refs in out-of-mandate span files (pothos headers, SessionRow/dialog/docblock self-refs, documents tests, test-file plan refs), session.ts schema docblock's stale "(held at request, decremented at completion)" parenthetical.

---

## (a) Continuity checks — 3/3 PASS

1. **Plan-artifact refs (7 cleaned files) — PASS.** `DEV3-[0-9]|REQ-[0-9]|R-20[0-9]|plan §|4\.BFBS|cron-r2|D9-bis` at working tree: **0 hits** in all 6 code files (service, mutation, query, StudentSessionsContainer, TeacherSessionsContainer, session.repository). AGENTS.md = 2 hits, both the RECORDED out-of-bullet doc-prose lines (:5 REQ-014/015/016, :75 REQ-10) — unchanged since R7/R8.
2. **Dynamic-import pin + service test count — PASS (read, not run).** Pin present and intact at `session-lifecycle.service.test.ts:1373-1385` (both `\bimport\s*\(` and `\brequire\s*\(` scans asserted `[]`, honest failure message naming the two sanctioned resolutions). File carries exactly **55 `test(` declarations** — the recorded 55/0 green shape.
3. **Deferred grep gate — PASS.** `grep -c "❌\|⚠️" deferred-items.md` = **4** (unchanged; D1–D14 all use the neutral ⏸ marker).

## (b) Fresh sweep findings

**None. Zero new findings this round (0 blocking).** The verdict is a genuine clean: every area probed fresh this round came back correct, or resolved exactly onto an already-RECORDED item. Full evidence trail of what was probed and how:

- **session.repository.ts (658 lines, full re-read):** all seven guarded transitions + the read quartet + the probe re-verified line-by-line. The batch sweeper (`sweepExpiredScheduledOnce`, :534-541) is ONE guarded UPDATE (`status=scheduled ∧ confirmation_deadline < now`) returning the cancelled rows; the service refunds ONLY rows in that RETURNING set, on the same transaction — under PostgreSQL row-lock + READ-COMMITTED re-check, a concurrent sweep's UPDATE re-evaluates the predicate against the committed status and matches zero rows, so the **concurrent-sweeper double-refund scenario is structurally impossible** (new probe this round — clean). `startSessionOnce` still lacks the `confirmation_deadline` term = D12 verbatim; `resolveDisputeCompleteOnce` still consumes the hold with no credit = D14 verbatim; `cancelSessionOnce`≅`openDisputeOnce` twins = the recorded jscpd clone.
- **session-lifecycle.service.ts (1316 lines, full re-read):** creation ordering (cert lock → trial-first guarded debit ladder → savepoint-bracketed claim → field-by-field insert → claim backfill), confirm orchestration (guarded exactly-once UPDATE → null-fee fail-closed check → `ensureWalletOnce` → `creditEarningOnce`, all on ONE tx — a credit failure rolls the confirm stamp back, fail-closed), sweep orchestration (refund per returned row, NULL-lane skip, unreadable-lane refusal rolls the whole sweep back), arbitration Cancel refund vs Complete consume (D14 as recorded), idempotent arms of `confirmSessionCompletion` (teacher caller / re-confirm / arbitration-settled all return the current row untouched, zero financial writes), `rejectTransitionMiss` classification (oracle-safe not-found for foreign callers on every participant kind; admin kind distinguishes state only), `refundHeldLaneToProvenance` (NULL skip + `isHeldBalanceLane` fail-closed + same-lane `incrementLane`), page-bound normalization + `guardStatusFilter` drop-out semantics, admin `limit/offset` clamps + honest page echo + contradictory-filter empty page. All clean.
- **Money discipline:** fees stay verbatim decimal strings end-to-end (`sessionFeeForIntent` → insert → `confirmed.fee` → `creditEarningOnce` amount); zero float arithmetic on money; lane ops are integer ±1 under guarded/atomic statements with DB CHECK backstops. Re-confirmed.
- **errors.ts (full re-read):** the `ValidationError`/`ConflictError` overloads verified against BOTH call shapes the service uses — one-arg `ValidationError(t.validation)` → code VALIDATION, and two-arg `ValidationError("INSUFFICIENT_BALANCE", t.insufficientBalance)` / `ConflictError("TEACHER_NOT_CERTIFIED", …)` → custom code + localized message. No code/message transposition anywhere. (New probe this round — clean.)
- **withTransaction + session-request-idempotency.repository.ts + idempotency schema:** savepoint-vs-top-level scoping correct; key never interpolated/logged/coerced (equality-bound param in both executor branches); claim `session_id` set-null-on-delete keeps the replay surface on DUPLICATE_REQUEST (fail-closed, documented). Clean.
- **student.repository.ts lane primitives:** guarded decrement (`balance > 0` fused into the UPDATE), documented unguarded increment ("no upper bound exists on any lane"); `LANE_BALANCE_COLUMNS` frozen map keyed by the enum — no caller-selectable column. `grantFreeTrialOnce` is the only trial grant (once-per-student marker), so no code path can push a refund above any cap. Clean.
- **GraphQL resolvers (mutation 7 fields / query 4 fields, full re-read):** thin delegation, identity ctx-only, `$all` conjunctions exactly where role gates exist, `authenticated`-only + service-side participant predicate on cancel/dispute/confirm, `??` default restoration for explicit-null args, D10 overlay comment honest at :114-131. Clean.
- **Pothos layer:** `session.pothos.ts` exhaustive no-default enum mappers with `never` guards; `heldBalanceLane` structurally absent from the SDL (grep re-run: 0 hits in schema.graphql; the 1 hit in the pothos file is the deliberate-absence docblock); `CreateSessionInput` = exactly `{teacherId, intent}`; `SessionListFilterInput` = `status` only. Plan-prose refs in the pothos headers = PRE-EXISTING recorded class. Clean.
- **SDL inventory:** Mutation root = exactly **12 fields** (cancelSession, completeSession, confirmSessionCompletion, createSession, login, logout, openSessionDispute, refreshToken, registerUser, requestWithdrawal, resolveSessionDispute, startSession) — byte-match with `KNOWN_LIVE_MUTATION_FIELDS` in warning-surfacing.test.ts. Clean.
- **Frontend (containers, SessionRow, chips, both dialogs, empty state):** count/list honesty holds at the UI contract (one shared predicate builder; both list documents fetch `items` + `totalCount` + `page`/`pageSize` under ONE filter); confirm-completion cache.modify writes stamp+hold onto the normalized entity (CTA/pill gated on `Completed ∧ stamp unset ∧ feeHeld` — arbitration-consumed rows render no affordance); teacher container owns its own SESSION_NOT_FOUND eviction while both dispute dialogs deliberately keep the row (symmetric, docblocked); dismissal-during-pending gate intact; sx-only/token styling, symmetric-zero RTL margins, no `style={{`/hex/`t('…')` literals/window./document./fake data (grep census re-run). R5-FE recorded items unchanged. Clean.
- **F-1 file set vs HEAD (re-verified):** session.repository.ts diff = comment lines only (non-comment changed-line scan: **0**); service.test.ts hunks = pin code only; warning-surfacing.ts = the 12-field inventory; TeacherWalletContainer.tsx = the 1-line indentation fix; suite = prettier rewraps + the assertion-equivalent `toHaveLength(2)` swap. Identical to R7/R8's verification.
- **Defect-marker census (span + F-1 files at working tree):** 0 `as any`/`@ts-ignore`/`@ts-expect-error`/`console.*`/TODO/FIXME/HACK/`useLazyQuery`/`dangerouslySetInnerHTML`/`innerHTML` in non-comment source (only test-pin self-references and docblock prohibitions). Clean.
- **Types:** `SessionReturnType = $inferSelect`, `SessionSubmitInput` closed whitelist (`{teacherId, intent}`), `SessionListFilterInput` optional-status, honest `SessionPageReturnType` echo — single-declaration discipline intact.

**Considered and judged BELOW the materiality bar (recorded here so the next round doesn't re-derive them; NOT findings):** (1) no DB index on the sweeper predicate (`status + confirmation_deadline`) or the admin `status=disputed` filter — scale-dependent performance hardening only, no behavioral effect at current data volume; (2) `held_balance_lane` is varchar with no pgEnum — the TS enum + `isHeldBalanceLane` fail-closed guard is the documented sole runtime authority and the refund path refuses unreadable values; (3) dispute-dialog SESSION_NOT_FOUND rows stay in the list — deliberate, docblocked, role-symmetric.

## (c) RECORDED / PRE-EXISTING re-confirmed (unchanged)

- RECORDED: D10 intent overlay (mutation :114-131, comment honest); D11 `oxlint-disable-next-line no-await-in-loop` still at service.ts:854; D12 start-after-deadline sweep-bypass window (`startSessionOnce` predicate verbatim, no `confirmationDeadline` term, :240-253); D13 completed-unconfirmed stuck holds; D14 arbitration-Complete no-credit asymmetry (`resolveDisputeCompleteOnce` :419-439 + service :672-705, docblock unchanged); R5-FE items (chips docblock "labelled toolbar row" with no aria-label attribute; raw intent-enum row title `intentText`; single Snackbar slot); jscpd clones ×2; pentest INFOs; let-scan caveat; AGENTS.md :5/:75 doc-prose; service.test REQ-04x names + describe titles; PRE-EXISTING pothos/SessionRow/dialog/test-file plan-prose refs; session.ts docblock parenthetical.
- Environment artifacts (MUST stay out of the fix commit): `tsconfig.json` M (provisioning rewrite), untracked `phase0-baseline-sandbox2-outcome.md` + the R6/R7/R8 outcome files (plan docs).

## (d) Verdict

**R9 VERDICT: 0 new findings (0 blocking).** A zero-finding round, earned: all three continuity checks PASS, and the fresh 4-lens sweep — including probes prior rounds had not run (concurrent-sweeper double-refund structural impossibility, error-overload call-shape verification, trial-refund-cap reachability, claim set-null replay semantics, count/list UI-contract honesty) — resolved every candidate to either a clean bill or an exact RECORDED match. Working tree remains ready to commit as the fix set — except `tsconfig.json` (provisioning artifact) and the untracked plan-doc outcome files. The escrow-lifecycle residuals stay owned by the recorded D12/D13/D14 family; nothing new is accumulating in the code-as-it-stands.
