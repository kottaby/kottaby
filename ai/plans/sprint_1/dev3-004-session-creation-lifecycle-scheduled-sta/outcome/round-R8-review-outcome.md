# R8 — Independent Review Round 8 (fresh eyes)

**Plan:** ai/plans/sprint_1/dev3-004-session-creation-lifecycle-scheduled-sta/
**Branch:** feat/dev3-004-session-creation-lifecycle-scheduled-sta @ a14c61c + uncommitted F-1/F-3/F-4 fix sets (reviewed at WORKING-TREE state — that is what gets committed)
**Round scope:** fresh 4-lens sweep (types / backend / frontend / security) over span `git diff --name-only b0ca09e..07e3488 -- backend/ shared/ frontend/ app/` (82 files) plus the F-1 file set (session.repository.ts, session.repository.test.ts, warning-surfacing.test.ts, TeacherWalletContainer.tsx + suite), hunting ONLY genuinely new material findings.
**Carried filters:** D1–D13 (deferred ledger), R5-FE items (chips group aria-label, raw intent-enum row title, single Snackbar slot), jscpd clones (cancelSessionOnce≅openDisputeOnce; TeacherWallet suite), pentest INFOs (unbounded page, teacher-existence oracle), heuristic-pin caveats (let-scan statement position), AGENTS.md doc-prose refs, service-test REQ-04x names = RECORDED, not new.

---

## (a) Continuity checks — 3/3 PASS

1. **Plan-artifact refs (7 cleaned files) — PASS.** `DEV3-[0-9]|REQ-[0-9]|R-20[0-9]|plan §|4\.BFBS|cron-r2|D9-bis` at working tree: **0 hits** in all 6 code files (service, mutation, query, StudentSessionsContainer, TeacherSessionsContainer, session.repository). AGENTS.md = 2 hits, both the RECORDED out-of-bullet doc-prose lines (:5 REQ-014/015/016, :75 REQ-10) — unchanged since R7.
2. **Dynamic-import pin + service test count — PASS (read, not run).** Pin present and intact at `session-lifecycle.service.test.ts:1372-1385` (both `\bimport\s*\(` and `\brequire\s*\(` scans asserted `[]`, honest failure message). File carries exactly **55 top-level `test(` declarations** — the recorded 55/0 green shape.
3. **Deferred grep gate — PASS.** `grep -c "❌\|⚠️" deferred-items.md` = **4** (unchanged; the D12/D13 rows and addendum contributed zero glyph hits).

## (b) Fresh sweep findings

**New (1, 0 blocking):**

- `[NEW][INFO]` backend/services/classes/session-lifecycle.service.ts:672-701 + backend/db/repo/classes/session.repository.ts:419-439 — **arbitration-Complete consumes the escrow hold without any credit anywhere.** `resolveSessionDispute(Complete)` on a started-then-disputed row flips `fee_held=false` via `resolveDisputeCompleteOnce` and writes NO teacher wallet credit and NO `teacher_transaction` ledger row — while the ordinary complete→confirm path credits exactly that fee (`confirmSessionCompletion` → `WalletRepository.creditEarningOnce`). Net effect on that path: the student's debited lane unit stays consumed and the fee value dissipates with zero accounting trail. This is DISTINCT from recorded D13 (which covers rows STUCK at `fee_held=true` with no release path — the arbitration-Complete row has `fee_held=false`, outside D13's scope). Attribution: the no-credit arm is DEV3-005-era (`resolveDisputeCompleteOnce` predates DEV3-012's dual confirmation and documents itself as deliberate — "no credit write is part of this method (the escrow hold is consumed, never reimbursed; the credit flow is a later ticket's concern)"; the mutation description likewise says "Complete consumes the hold"), so behavior matches its documentation everywhere — but the design residual was never carried into the deferred ledger, and post-DEV3-012 the "later ticket" credit mechanism now exists for the ordinary path, making the asymmetry concrete. Record-only for the escrow-lifecycle design ticket (same owner family as D13): either credit the teacher on arbitration-Complete (the confirm-path primitive is available) or explicitly document the platform-retention decision. Non-blocking.

**No other new findings.** Repeatedly probed and CLEAN this round:

- **Money discipline end-to-end:** fees are decimal strings carried verbatim (creation insert → `confirmed.fee` → `creditEarningOnce` binds `${wallet.balance} + ${insert.amount}` SQL-side); zero JS float arithmetic on money anywhere in the slice; lane debit/refund are integer unit ops (`- 1` / `+ 1`) under guarded predicates with DB CHECK backstops.
- **Replay economics:** `replayBooking` THROWS (`Promise<never>`, DUPLICATE_REQUEST / oracle-safe SESSION_NOT_FOUND for cross-user keys) — the throw-and-rollback provably burns no lane unit on replay even though the debit ladder runs before the claim insert; `isClaimKeyUniqueViolation` traverses the Drizzle cause chain for `23505` only (the claim table's sole unique constraint is the key; FK misses are 23503 and surface untouched).
- **Create ordering:** certification lock (`SELECT … FOR UPDATE`) → trial-first debit ladder → savepoint-bracketed claim → field-by-field insert (zero spreads) → claim backfill, one `withTransaction`; `ensureWalletOnce` is `ON CONFLICT DO NOTHING` + authoritative SELECT; the credit's exactly-once guard lives in `confirmStudentCompletionOnce`'s predicate (`feeHeld ∧ stamps`), so confirm×arbitration races classify to the idempotent arm, never double-credit.
- **Clock consistency:** the sweep predicate compares `confirmation_deadline < now` where BOTH sides are app-captured (`now` bound param; deadline written from the same app clock at creation) — no JS/DB clock mixing inside any lifecycle predicate.
- **Pagination honesty:** `normalizePageBounds` (page < 1 → 1; pageSize 1..50 → 25) and `listAdminDisputedSessions` clamps (`Number.isSafeInteger`, offset ≥ 0) close the negative-page/offset hole; the admin filter arg is honored honestly (contradicting status → empty page by definition, no DB round-trip). `totalCount` staleness after a not-found eviction was checked and is NOT material: no container renders `totalCount` (grep-verified).
- **UI gating:** the student confirm CTA / pending pill key off `Completed ∧ confirmedByStudentAt unset ∧ feeHeld` — an arbitration-completed row (`feeHeld=false`) renders no misleading affordance.
- **Cross-layer imports:** zero `@/backend` in frontend session surfaces, zero `@/frontend|@/app|@/backend/graphql` in the service, zero backend imports from `shared/constants`+`shared/locale` (shared-layer isolation holds).
- **Defect markers:** 0 `as any`/`@ts-ignore`/eslint|biome-ignore, 0 `console.*`, 0 TODO/FIXME/HACK, 0 `style={`/hex colors/`t('…')` literals/`useLazyQuery`/raw `window.`/`document.`/fake data in all span UI+backend sources at working tree (docblock mentions of banned patterns are the documented prohibitions themselves).
- **Span-adjacent diffs re-read:** `errors.ts` ConflictError overload (R1 fix), `registration.service.ts` withTransaction dedup, `navItems.ts` route fixes (`/student/sessions`, `/teacher/sessions` matching the page shells), `teacher.repository.ts` certification lock, `session-request-idempotency.repository.ts` (key never interpolated/logged/coerced), `session.pothos.ts` (`heldBalanceLane` deliberately absent from SDL — re-confirmed), `session-fees.constants.ts`, schema `session.ts` — all clean.
- **F-1 wallet files vs HEAD:** container diff still the 1-line indentation fix; suite diff still prettier-style rewraps + the assertion-equivalent `toHaveLength(2)` swap (matches R7's verification exactly).

## (c) RECORDED / PRE-EXISTING re-confirmed (unchanged)

- RECORDED: D10 intent overlay (mutation :127-131, honest comment); D11 `oxlint-disable-next-line no-await-in-loop` still at service.ts:854; D12 start-after-deadline sweep-bypass window (`startSessionOnce` predicate verbatim, no `confirmationDeadline` term); D13 completed-unconfirmed stuck holds; R5-FE items; jscpd clones ×2; pentest INFOs; let-scan caveat; AGENTS.md :5/:75 doc-prose; service.test REQ-04x names + describe titles.
- PRE-EXISTING (present at 8779acc): plan-prose refs in out-of-mandate span files (pothos headers incl. `session-filter-input.pothos.ts` "plan §3.1"/REQ-020, SessionRow "DEV3-012" docblocks, documents/contract tests); the `session.ts` schema docblock's "(held at request, decremented at completion)" parenthetical (never matched the recorded behavior at 8779acc, where `completeSessionOnce` already left `fee_held` untouched); unbounded admin list page (pentest INFO); `timestamp`-without-timezone convention (codebase-wide, not span-introduced).
- Environment artifacts (MUST stay out of the fix commit): `tsconfig.json` M (provisioning rewrite), untracked `phase0-baseline-sandbox2-outcome.md` + the R6/R7/R8 outcome files (plan docs).

## (d) Verdict

**R8 VERDICT: 1 new finding (0 blocking)** — 1 INFO (arbitration-Complete consumes the hold with no credit/ledger trail; deliberate-per-docblock but unledgered design residual; record-only for the escrow-lifecycle design ticket, D13-family). All three continuity checks PASS; the core lifecycle (state machine, guarded transitions, replay economics, money discipline, authorization, cache convergence) re-read line-by-line at resolver+service+repo+SQL depth and holds clean. Working tree remains ready to commit as the fix set — except `tsconfig.json` (provisioning artifact) and untracked plan-doc artifacts.
