# Task 6.1 — Review Wave Outcome: review-types + review-backend (combined, round 1)

**Date:** 2026-08-31
**Reviewer:** 6.1 combined wave agent (review-types + review-backend)
**Baseline:** `b0ca09e` (outcome/0.1: tsgo 0 · biome 0 · eslint 0/0 — pre-existing issues are NOT findings)
**Scope:** `git diff b0ca09e --name-only` limited to backend/ + shared/ + test/ (production files reviewed line-by-line; ai/plans/**, *.md docs, screenshots, generated codegen artifacts excluded). REPORT ONLY — zero code changes, zero tree mutations outside this outcome file and the worklog append.
**Prior review:** outcome/2.M-midpoint-outcome.md (backend, 54 files) — verdict PASS, 1 MINOR doc-only finding (test/workflows/AGENTS.md provenance heading; stands as reported there, not re-counted below).

---

## 1. Re-verified gates on the current worktree

| Gate | Result |
|---|---|
| `bun tsgo` | exit 0, 0 errors (includes the uncommitted test-hardening edits) |
| `bunx @biomejs/biome check .` (no `--write`) | "Checked 570 files in 4s. No fixes applied." — 0 diagnostics |
| Baseline delta | **0 new tsgo/biome problems** attributable to the feature |

---

## 2. Findings (severity-filtered, feature-specific; FIX NOTHING — REPORT ONLY)

| # | Severity | Location | Description |
|---|---|---|---|
| F1 | LOW | backend/graphql/mutation/classes/session-lifecycle.mutation.ts:108-112 | The resolver builds the service input by overlaying a hostile SDL value onto a typed base — `Object.assign(baseInput, { intent: args.input.intent })` where `baseInput: SessionSubmitInput` pins `intent: SessionIntent.Hifz`. At that one point the closed `SessionStudentIntentType` (Hifz \| Tajweed) invariant of the canonical `SessionSubmitInput` is intentionally violated so the out-of-contract `evaluation` member can reach the service's pre-DB guard (REQ-050 VALIDATION path). The overlay is explicitly documented, has no security impact (BOPLA holds — `intent` is a client-owned field by design and the service re-validates pre-DB before any write), and is the codebase's lint-clean alternative to an unsafe assertion. Residual risk is purely static-typing honesty: a reader could mistake the type for a runtime guarantee at this boundary. Forward note: widening the service boundary param to `SessionIntent` and narrowing internally would let the overlay (and the type lie) be deleted; owner = feature maintainer, next touch. |
| F2 | LOW | backend/services/classes/session-lifecycle.service.ts:548 (`assertActorGovernanceClean`) | The governance denial throws `new ConflictError("FORBIDDEN", t.forbidden)` instead of `new ForbiddenError(t.forbidden)`. Wire-identical (`extensions.code` = `FORBIDDEN`, 403 via the taxonomy; `ConflictError`'s overloaded ctor accepts a custom code) and explicitly covered by the REQ-064 B4 cells (mutation grid + service suite `expectDomainDenial(error, "FORBIDDEN", …)`), but the error CLASS misdescribes the semantic — a governance/authorization denial surfaced through a Conflict class. Cosmetic/clarity only; no transport or masking difference (`finalizeGraphqlErrors` passes all DomainErrors through by identity/code). |
| F3 | LOW | backend/graphql/pothos/classes/session.pothos.ts:54-104 | The DB-string → TS-enum mappers (`toSessionStatus` / `toSessionType` / `toSessionIntent`) hardcode the pgEnum literal sets as switch cases inside the Pothos layer. This is a runtime REPRESENTATION mapping (not enum registration — the enum-object Pothos registration rule is honored; no `values: [...]` anywhere), it fails closed (`null` → resolver throw) on unknown values, and the wire vocabulary is pinned by `session-sdl.test.ts` against the canonical enum members. But the house precedent places such mappers beside the enum (cf. `toUserRole` in `backend/enum/users/user-role.enum.ts`, consumed via import), and a future `SessionStatus` member added to the pgEnum would NOT be compile-checked in these switches (silent `default → null` → runtime "Unexpected session status" only). Low-risk consistency/drift observation; a `Record<SessionStatus, string>` reverse-map (exhaustiveness-locked) or relocation beside the enum would close it. |
| F4 | INFO | (worktree state, not a code defect) | 3 files carry uncommitted worktree modifications on top of HEAD: `backend/graphql/test/session-lifecycle-mutations.test.ts` (+29), `frontend/graphql/test/scheduling/session-lifecycle-queries.test.ts` (+2) — the sanctioned 5.3 in-gate eslint `sonarjs/assertions-in-tests` hardening (`expect(result.error).toBeDefined()` guards, documented in the worklog 5.3 entry) — and `oxlint.config.mts` (+4, `no-underscore-dangle` exemption for two frontend session views, frontend-wave scope). All reviewed here are sound; flagging only so commit ownership stays with the orchestrator (this wave commits nothing). |

Count: **0 CRITICAL · 0 HIGH · 0 MEDIUM · 3 LOW · 1 INFO(process)**. No pre-existing-issue findings raised (every checked production file is new in this diff or delta-reviewed against `b0ca09e`).

---

## 3. review-types — per-check verdicts

| Check | Verdict | Evidence |
|---|---|---|
| No local Pothos types | ✅ PASS | `session.pothos.ts` objectRefs are backed EXCLUSIVELY by canonical `SessionReturnType` / `SessionPageReturnType` from `@/backend/types` (imported, not re-declared); zero `export type …Definition` / ad-hoc shapes in any `*.pothos.ts`. `CreateSessionInput` + `SessionListFilterPothosInput` are the sanctioned Input-Exception shapes (structural maps of `SessionSubmitInput` / `SessionListFilterInput`, docblocks state the backing types). `heldBalanceLane` deliberately absent from SDL (documented). |
| No service `.types.ts` | ✅ PASS | `backend/services/classes/` contains only `session-lifecycle.service.ts` (+ test). Zero type-defining files anywhere in the service layer of the diff. `backend/services/shared/withTransaction.ts` is runtime-only. |
| Contract types consumed-not-redefined | ✅ PASS | `session.types.conformance.test-d.ts` CONSUMES the pre-existing `SessionRequestContract` + `SESSION_REQUEST_SESSION_TYPE` from `backend/types/contracts/session-request.contract.types.ts` (byte-identical to b0ca09e — verified via `git log`) and pins `SessionStudentIntentType ≡ SessionRequestContract["intent"]` with an `Equals` probe. No contract type is re-declared or forked anywhere in the slice. |
| Additive-only diffs on existing type files | ✅ PASS | `backend/types/classes/index.ts` +1 export line; `shared/locale/types/errors/index.ts` +7 new readonly error keys; `shared/locale/types/message.ts` +1 namespace member +1 import; `shared/constants/index.ts` +1 export. Zero modifications/deletions of existing type members. New type files: `session-request-idempotency.types.ts`, `shared/locale/types/sessions/index.ts`. |
| No duplicate type names across files | ✅ PASS | Canonical session vocabulary declared exactly once each (`SessionSelectType/InsertType/ReturnType/SubmitInput/ListFilterInput/PageReturnType/StudentIntentType/TransitionProbeRowType` in session.types.ts; claim Select/Insert in session-request-idempotency.types.ts; `HeldBalanceLane` in the enum). No string-union re-declarations of any enum anywhere (the only vocabulary literals are the pothos mappers' runtime switch cases — F3). Pre-existing `SessionRequestContract`/`SessionEventNotification*` untouched. |
| Import-path consistency (`@/` aliases) | ✅ PASS | Cross-layer imports all `@/`-aliased (`@/backend/types`, `@/backend/db/...`, `@/backend/enum/...`, `@/shared/constants/...`); `backend/types/classes/index.ts` + `shared/locale/types/index.ts` barrels use `./` relative `export *` per backend/types/AGENTS.md. Deep type imports (`@/backend/types/classes/session.types`) appear only in the conformance test-d, mirroring the pre-existing contracts-suite pattern. |
| Enum VALUE vs TYPE import correctness (every site) | ✅ PASS | TYPE-only where never used at runtime: `backend/db/schema/classes/session.ts` (`import type { HeldBalanceLane }` — `$type<>()` binding only), `backend/types/classes/session.types.ts` (`import type { SessionIntent, SessionStatus }`). VALUE where runtime usage exists: student.repository (map keys), session.repository (`eq(session.status, …)`), service (guard + payload + `Object.values`), pothos shared/enum registrations, pothos class mappers, resolvers (`UserRole` scope input), tests. No site imports a value where only a type is needed (tsgo 0 + biome 0 confirm no unused-value imports). |

---

## 4. review-backend — per-check verdicts

| Check | Verdict | Evidence |
|---|---|---|
| Four-phase create ordering intact | ✅ PASS | `createSession` inside ONE `withTransaction`: ① cert lock `TeacherRepository.lockForCertificationCheck` (`SELECT … FOR UPDATE`, tx-required) :209 → ② trial-first guarded debit ladder (`decrementLaneIfAvailable` Trial → intent lane, all-miss throws) :231-246 → ③ savepoint-bracketed claim insert (`tx.transaction(claimTx => insertClaim(…, claimTx))`) :250-263 → ④ session insert :271 + claim `session_id` backfill :288. Never reordered; pre-DB validation + governance re-check run before the tx (reads only); replay branch throws (rollback = only cleanup). |
| Guarded single-statement transitions | ✅ PASS | `startSessionOnce` / `completeSessionOnce` / `cancelSessionOnce` are each ONE fused-predicate `UPDATE … WHERE … RETURNING` (row identity + owner/participant + state; completion additionally fuses the `EXISTS` certification re-assertion in the SAME statement; cancellation clears `feeHeld` in the same statement). Debit is ONE `balance > 0` guarded UPDATE. Zero check-then-write windows in any transition. |
| Probe classification-only | ✅ PASS | `findTransitionProbe` is invoked ONLY inside `rejectTransitionMiss`, which runs only AFTER a guarded update matched zero rows; it selects the 4-column classification projection and only logs + throws typed denials. The cancel refund is driven by the guarded UPDATE's `RETURNING` row (`cancelled.heldBalanceLane`), never by the probe. |
| tx propagation audit (EVERY new repo method, every call site) | ✅ PASS | All methods in `SessionRepository` (10), `SessionRequestIdempotencyRepository` (3), `StudentRepository` new methods (2), `TeacherRepository` (1, lock tx-REQUIRED) take `tx?: DBTransaction` as LAST param. Full grep of every call site (repo-wide, incl. tests): service passes `tx`/`claimTx` at all 12 write-path sites (:209, :231, :236, :254, :271, :288, :322, :359, :407, :423, :546, :577, :614); writes execute on `tx ?? db`; non-tx reads use `queryDb` (Neon HTTP pattern per repo AGENTS.md). No missing-tx call site found. |
| Zero cross-layer imports | ✅ PASS | Code grep (`@/frontend`, `@/app`, relative traversals) over shared/ + backend/ changed files: ZERO hits (only `shared/AGENTS.md` negative-pattern docblock mentions). Repos import db/schema/enum/types only; service imports repos/enums/lib/service-shared/types + `shared/` constants & locale (sanctioned direction); `shared/constants/session-fees.constants.ts` imports NOTHING; schema imports sibling schemas + type-only enum. |
| REQ-019 grep gates (notification/audit/wallet/teacher_transaction/reports) | ✅ PASS | `rg -n "notification\|audit-log\|wallet\|teacher_transaction\|reports"` over `backend/services/classes/session-lifecycle.service.ts` + `backend/db/repo/classes/` → 3 hits, ALL JSDoc prose (service:39 "imports nothing from the notification…", repo :11/:34 "reports" as verb). Extended grep across the whole classes slice (graphql mutation/query/pothos, types, schema, enum, shared constants): 0. `test/workflows/helpers/journey-fixtures.ts` imports `auditLogs`/`wallet`/`teacherTransaction`/`notifications` schemas but uses them ONLY as read-only `$count`/SELECT counters to ASSERT zero side effects (REQ-J6 honest-cleanup proof) — test-only, outside the production gate scope, no writes. |
| REQ-031 zero-`...input` grep gate | ✅ PASS | `rg -n "\.\.\.input\|\.\.\.(insert\|props)"` over `backend/services/classes/` + `backend/graphql/mutation/classes/` → **0 matches**. `createSession` maps the insert field-by-field (BOPLA); the F1 `Object.assign` overlay is not a spread and carries only the client-owned `intent`. |
| REQ-036 log hygiene | ✅ PASS | Zero `console.*` in production code (only test assertions asserting absence + docblocks). All logger calls inspected (service :211/:219/:238/:418/:548/:579/:586/:616/:626/:636/:647/:659/:674/:681): contexts are `{code, entity, entityId}` / `{sessionId}` exclusively. The idempotency key is NEVER logged, never trimmed/re-derived; `replayBooking` logs the CALLER's own id (never the claim owner's); no payloads, no other-party identity. Resolvers/repos/schema do zero logging by design. |
| TOCTOU re-scan on mutations | ✅ PASS | Resolvers are thin delegation (no read-then-write at the GraphQL layer). `createSession`: cert value read under `FOR UPDATE` in the SAME tx that writes; debit guarded single-statement; claim uniqueness enforced by DB unique index (23505 → replay-throw); governance re-check is an out-of-tx read = accepted defense-in-depth (login/SSR boundary is authoritative; cancel deliberately exempt per REQ-023). start/complete/cancel: single fused-predicate UPDATEs (row-lock serialized; double cancel can never double-refund — terminal states unreachable in the predicate). Accepted residual: governance state can flip between the pre-tx re-check and the write — documented, defense-in-depth only. |
| Dead code scan | ✅ PASS | Every new export/method is consumed: `SessionRepository` all 10 methods (service/resolvers), idempotency repo all 3, `StudentRepository.decrementLaneIfAvailable`/`incrementLane` (service), `TeacherRepository.lockForCertificationCheck` (service), all 7 service namespace methods (resolvers), all internal helpers, `HeldBalanceLane` members + `isHeldBalanceLane`, `SESSION_FEE_HIFZ/TAJWEED/CONFIRMATION_WINDOW_MS` (service) + `SESSION_FEE_CURRENCY` (frontend `SessionRow`), `withTransaction` (2 services). No unused exports in the new repos/service/resolvers. |
| Race-condition re-scan | ✅ PASS | Module-level mutable state: NONE. `LANE_BALANCE_COLUMNS` is `Object.freeze`d + `Readonly<Record<HeldBalanceLane, AnyPgColumn>>` (missing-member = compile error); `participantDialect` is a stateless `PgDialect`; `SESSION_STARTED_STATUS` etc. are consts; the mutation/query registration factories run once at import. Unguarded read-then-write: none on any write path (the only composition — cancel UPDATE → lane increment — is same-tx, refund driven by the guarded row, exactly-once by state reachability). Journey fixture registry is per-suite instantiated, not module-global. |

**Adjacent diff deltas reviewed (no findings):** `backend/lib/errors.ts` — `ConflictError` overload addition is additive (all prior `(message[, options])` call sites compile unchanged; runtime branch logic covers both forms; `ValidationError` overload it mirrors was pre-existing). `backend/services/auth/registration.service.ts` — mechanical `withTransaction` extraction to `backend/services/shared/withTransaction.ts`, implementation byte-equivalent, `db` import dropped, zero behavior change. `backend/graphql/pothos/shared/enum.pothos.ts` — three enum-object registrations per the CRITICAL pattern (no literal arrays). Barrels (`repo/index`, `mutation/index`, `query/index`, `enum/scheduling/index`, `schema/classes/index`, `services/index`) — additive side-effect/export lines only. `oxlint.config.mts` — frontend-scoped rule exemption (F4).

---

## 5. Verdict

**review-types: PASS** (7/7 checks; 1 LOW static-typing-honesty note F1, 1 LOW consistency note F3).
**review-backend: PASS** (11/11 checks; 1 LOW error-class semantics note F2; process note F4).

Phase 6 review-gate obligations for this wave are satisfied: every check green, no BLOCKER/HIGH/MEDIUM findings, no fix applied in-pass (all findings are report-only candidates with owners), and the baseline delta remains 0 (tsgo/biome re-verified on the current worktree).
