# Task 2.3 Outcome — Journey J2: Hostile & Boundary Legs (`session-lifecycle-denials`)

**Task ID:** 2.3 · **Worktree:** `/home/z/feat-dev3-004` · **Branch:** `feat/dev3-004-session-creation-lifecycle-scheduled-sta` (NO checkout/commit/push)

---

## 0. Summary

`test/workflows/sessions/session-lifecycle-denials.journey.test.ts` (NEW, one file, 6 legs / 6 tests / 93 expect calls) implements the hostile & boundary journey against the REAL `SessionLifecycleService` on the REAL `kottaby_test_db` PostgreSQL instance. The service surface already existed (task 2.8), so the journey was written against the implemented contract and passed **green on its first execution**; two consecutive runs (the REQ-J6/AGENTS-rule-2 idempotent-teardown proof) are both **6 pass / 0 fail / 93 expect()**, with an empirical post-run DB sweep showing ZERO residual rows (`0` `jrn_sessions_*` users, `0` `session` rows, `0` `session_request_idempotency` rows).

Layer discipline held end-to-end: committed fixtures in `beforeAll` (ONE short `db.transaction` via `buildSessionJourneyCast`, real role-child rows only, nothing monkey-patched), tracked hard-deletes in `afterAll` (`registry.cleanup()` plus a defensive residual sweep so a mid-test failure can never strand restrict-FK children), **NO `runInRollback` anywhere**, denials asserted via a try/catch helper on the `DomainError` code contract + exact translated messages (never `rejects.toThrow()`), external-effect absence proven by row-count deltas through the helpers' counters (audit logs, notifications).

**Service bugs found/fixed: NONE.** Every leg passed against the unmodified 2.8 service — zero service code was touched by this task (the only file created is the journey test itself).

## 1. Files Created / Modified

| File | Action | Content |
|---|---|---|
| `test/workflows/sessions/session-lifecycle-denials.journey.test.ts` | CREATE (~560 lines) | Journey J2 — 6 hostile legs, per-run `jrn_sessions_<8hex>` prefix, module-scope oracles (lane balances, session/claim counts, full-row read-backs, `nonexistentSessionId` ghost-id oracle, `denialShape` fingerprint), committed cast + tracked teardown with defensive residual sweep |
| `ai/plans/.../tasks.md` | UPDATE | `- [ ] 2.3` → `- [x] 2.3` |
| `ai/plans/.../outcome/2.3-outcome.md` | CREATE (this file) | close-out record |
| `ai/plans/.../worklog.md` | CREATE+APPEND | worklog entry for 2.3 (the plan had no worklog file; created as the canonical append target) |

**Files NOT modified:** `backend/**` (zero service/repo/schema changes — no bugs found), `test/workflows/helpers/**` (consumed as-is), `test/workflows/AGENTS.md`, `docs/testing/workflow-journey-tests.md`, and the parallel agent's `test/workflows/sessions/session-lifecycle.journey.test.ts` (J1 — untouched per dispatch).

## 2. Leg → Implementation Map (what each test actually proves)

**Cast** (via `buildSessionJourneyCast`, `{ trial: 1, hifz: 1 }` primary override so leg 1's "untouched" claim is non-vacuous): primary student (funded), second student (zero-balance by construction), certified teacher T, second certified teacher, teacher applicant AP (real `applicants` row, NO `teacher` row), parent, admin — 14 real rows, unique per-run labels.

1. **Applicant booking → `TEACHER_NOT_FOUND`, caller untouched (INV-TV1)** — primary student books AP (`intent=Tajweed`, fresh key): `NotFoundError` / code `TEACHER_NOT_FOUND` / exact `t.teacherNotFound`; lane balances byte-identical before→after; `session` count, claim count unchanged (the key was never burned); AP still has NO `teacher` row (nothing mints certification); zero notification rows for the caller. *(REQ-J5, REQ-011, REQ-030, INV-TV1)*
2. **Zero-balance → `INSUFFICIENT_BALANCE` + key-rollback proof** — second student (all lanes 0) books T with `${PREFIX}-key-rollback-proof`: `ValidationError` / code `INSUFFICIENT_BALANCE` / exact `t.insufficientBalance`; ZERO writes across all three tables (session count, claim count, lane snapshot all static). Then the student is funded (`balanceHifz = 1`, a real committed journey step) and **THE SAME key** books successfully: row `scheduled`, `feeHeld=true`, `heldBalanceLane=Hifz` (empty trial skipped → intent lane), fee `SESSION_FEE_HIFZ`, deadline bracketed to `now + SESSION_CONFIRMATION_WINDOW_MS` EXACTLY; claim row backfilled with the session pointer (both ids tracked); lane debited exactly once; owner's list contains the new id. *(REQ-J5, REQ-040, REQ-041, REQ-046, REQ-050-Ruling)*
3. **Foreign ≡ nonexistent oracle pairing (REQ-J4)** — primary student books second teacher (trial lane consumed: trial-first visible in balances). Second student then: `cancelSession(primary.id)` → `SESSION_NOT_FOUND` (`NotFoundError`, exact `t.sessionNotFound`) and `getSessionById(primary.id)` → `null`; a ghost id (`max(session.id) + 1_000_000`, collision-proof under parallel runs) yields the **byte-identical denial fingerprint** (`{name, code, message}` deep-equal) and the same `null`. Owner-visible row re-read byte-identical through the hostile probes (no refund fired); the foreign caller's OWN leg-2 session stays readable and intact. *(REQ-J4, REQ-030, REQ-033)*
4. **Applicant has NO session surface** — AP `startSession(primary.id)` → `SESSION_NOT_FOUND`; ghost id → **identical fingerprint**; `completeSession`/`cancelSession`/`getSessionById` all `SESSION_NOT_FOUND`/null; AP's own `listMyTeacherSessions` honestly EMPTY (`items []`, `totalCount 0`); still no `teacher` row; probed row byte-identical. *(REQ-J4, REQ-030, REQ-032, INV-TV1)*
5. **`intent=evaluation` → pre-DB `VALIDATION`** — hostile intent smuggled onto a valid `SessionSubmitInput` base via the lint-clean `Object.assign` overlay (`SessionIntent.Evaluation` is structurally unreachable from the whitelist): `ValidationError` / code `VALIDATION` / exact `t.invalidSessionIntent`; zero writes in session/claim/lane surfaces (key unburned); NO row exists with `sessionType=teacher_evaluation` NOR `intent=evaluation` scoped to the fixture actors — the evaluation shape is not producible through this surface. *(REQ-J5, REQ-032, REQ-047, REQ-054)*
6. **Admin & Parent: no bypass + zero audit rows (REQ-064)** — clean-state probes: `getSessionById` → null for both; `cancelSession`/`startSession`/`completeSession` on foreign sessions → `SESSION_NOT_FOUND`; owner-scoped lists honestly EMPTY (never others' data); rows byte-identical after the probes. Then the accounts are placed in the REAL governed state (`isBlocked = true` — the same predicate the login boundary denies on): `createSession` (admin AND parent) → `ConflictError` / code `FORBIDDEN` / exact `t.forbidden` with **identical fingerprints** (REQ-033); `startSession`/`completeSession` → `FORBIDDEN`; **`cancelSession` stays EXEMPT** from the governance re-check (the governed non-participant still receives the oracle `SESSION_NOT_FOUND`, never `FORBIDDEN` — the 2.8 bounded-scope ruling made observable); denied bookings produced ZERO claims (governance fires pre-transaction — keys unburned); zero audit-log and zero notification row deltas across every hostile attempt (count-delta oracle, REQ-019). *(REQ-064, REQ-023, REQ-019, REQ-033)*

**afterAll:** defensive sweep tracks ANY residual `session`/`session_request_idempotency` row referencing the fixture actor ids, then `registry.cleanup()` hard-deletes everything in FK-safe order inside one committed transaction.

## 3. QL — Quality Loop (sub-loop `--lifecycle duplicates`, exit 0 MANDATORY)

| File | tsgo | oxlint | biome:check | lint:type-aware | check:duplicates | Result |
|---|---|---|---|---|---|---|
| `test/workflows/sessions/session-lifecycle-denials.journey.test.ts` | ✅ | ✅ | ✅ | ✅ | ✅ (skipped for `.test.ts` per the sub-loop's own jscpd scope rule) | **exit 0** (one fix cycle: `readLaneBalances` nullable-column projection, enum-member (not widened-string) comparisons for `toBe`/`eq`, removed an unused snapshot var) |

## 4. TE — Test Execution (the mandated runner; never raw `bun test`)

| Run | Command | Result |
|---|---|---|
| 1 | `bun run test/scripts/run-test.ts test/workflows/sessions/session-lifecycle-denials.journey.test.ts` | **6 pass / 0 fail / 93 expect()** (~340 ms) |
| 2 (consecutive) | same command | **6 pass / 0 fail / 93 expect()** (~332 ms) — zero residual state (REQ-J6) |
| Whole layer | `bun run test/scripts/run-test.ts test/workflows` | **16 pass / 0 fail / 272 expect() across 2 files** — J2 (6) + the parallel J1 suite (10) co-green on the shared DB |
| Post-run DB sweep | direct `psql` counts | `jrn_sessions_%` users = 0, `session` = 0, `session_request_idempotency` = 0 — teardown totality proven |

## 5. SEC / SR / IV verdicts

- **SEC:** honest authorization only — every denial flows through the real role rows (`users.role` + role-child rows) and the service's real predicates; the applicant's impossibility is by construction (no `teacher` row), never simulated; the governed state for leg 6 is a real `users.isBlocked` write (the exact state production denies at login), not a monkey-patch. Oracle ruling honored (foreign ≡ nonexistent on reads AND mutation denials, byte-identical fingerprints asserted). BOPLA — no spreads; BFLA — admin receives identical oracle denials + governance `FORBIDDEN`, no bypass path exercised or observed.
- **SR:** zero business logic in the journey (fixtures + oracles + assertions only); no `console.*`, no `any`/`as` casts (the hostile intent uses the `Object.assign` overlay precedent), enums as VALUE imports, `@/` aliases throughout, per-run UUID prefix, ordered tests share the committed cast exactly as the J1 design prescribes.
- **IV:** `test/workflows/AGENTS.md` rules 1–10 each hold (no `runInRollback`; committed fixtures + tracked cleanup incl. service-created rows; unique prefix; honest authorization; external-effect absence via count deltas; try/catch denial helper with translated messages; `bun:test` only; `@/` aliases; no seeded rows — everything via `entity-setup`-backed builders; one journey file in the domain subdirectory); `docs/testing/workflow-journey-tests.md` fixture-lifecycle + runner contract followed; specs REQ-J4/J5, REQ-030/032/033/064, REQ-077 covered per the file-header map.

## 6. Environment Notes

- PostgreSQL 17.11 (user-space `/tmp/pgdata`, port 5432) was already running; binaries resolved at `/tmp/pg-deb/pgroot/usr/lib/postgresql/17/bin`.
- Full-repo `bun tsgo`: **0 errors for this task's file** (the only remaining error at close-out time was in the parallel agent's in-flight `session-lifecycle.journey.test.ts:576` — a widened-string `toBe` overload identical to the one fixed here; outside this task's file boundary per dispatch, left to the J1 owner).

## 7. Verdict

**Task 2.3 COMPLETE.** All 6 hostile legs green first-run against the unmodified 2.8 service surface; two consecutive green runs + empirical zero-residue sweep prove idempotent teardown; sub-loop exit 0; whole-layer run co-green with J1; zero service bugs found (zero backend files touched). Scope boundary exact: ONE test file + close-out artifacts; the parallel agent's J1 file was never opened for edit.
