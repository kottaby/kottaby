# 0.3-fix Outcome — Gate Blocker Resolution (B1–B4)

> Task: apply the orchestrator's 2026-08-30 rulings for the four BLOCKING findings of
> `outcome/0.3-outcome.md` (plan-review gate round 1). Scope: ONLY the plan files
> (`specs.md`, `plan.md`, `tasks.md`) + `deferred-items.md`, checkbox sync, this outcome, worklog.
> **No source files touched.** Every amendment carries an inline dated note
> `Ruling (2026-08-30, orchestrator):` where the markdown format allows.
> Advisories A1–A10 were deliberately NOT applied (out of this task's mandate; owner: orchestrator batch).

---

## 1. Amendments by Blocker

### B1 — i18n errors key registry (REQ-051's 7 keys = single source of truth)

| File → Section | Before | After |
|---|---|---|
| `tasks.md` → task 1.5(a) | Six keys listed (`sessionNotFound, sessionInvalidTransition, teacherNotCertified, insufficientBalance, idempotencyKeyRequired, invalidSessionIntent`); `TEACHER_NOT_FOUND` mapped onto the EXISTING `notFound` key (mandated `teacherNotFound` dropped) | EXACTLY the SEVEN REQ-051 keys (`sessionNotFound, sessionInvalidTransition, teacherNotCertified, teacherNotFound, insufficientBalance, idempotencyKeyRequired, invalidSessionIntent`); `TEACHER_NOT_FOUND` maps onto the NEW `teacherNotFound` key, NOT the generic `notFound`; reuse guidance kept (`duplicateRequest`/`validation`/`forbidden`/`unauthorized`/`notFound`/`internalServerError` for their existing generic surfaces) + dated ruling note |
| `plan.md` → §2.7(a) heading | "new `sessions` grouping (REQ-051)" (re-fed the FLAT-ambiguity) | "new FLAT domain-prefixed keys (REQ-051)" |
| `plan.md` → §2.7(a) keys row | `sessionTeacherNotCertified`, `sessionTeacherNotFound`, `sessionInsufficientBalance`, `sessionIdempotencyKeyRequired`, `sessionInvalidIntent` (5 `session*` renames) | REQ-051's exact spellings (`teacherNotCertified`, `teacherNotFound`, `insufficientBalance`, `idempotencyKeyRequired`, `invalidSessionIntent`) + ruling note declaring the 7-key list the single source of truth and the renames DROPPED |
| `plan.md` → §2.7(a) reuse paragraph | plain reuse sentence | kept + clarified: `notFound` stays reused by its existing generic consumers but is no longer the `TEACHER_NOT_FOUND` mapping target; "FLAT domain-prefixed" phrasing resolved (flat camelCase, no forced `session` prefix, no nested grouping) |
| `plan.md` → §4.1 createSession step 1 | `NotFoundError("TEACHER", t.sessionTeacherNotFound)` | `NotFoundError("TEACHER", t.teacherNotFound)` (last live `session*` rename usage) |

### B2 — INSUFFICIENT_BALANCE = `ValidationError` → 422 (REQ-050 spec authority)

| File → Section | Before | After |
|---|---|---|
| `plan.md` → §1.2 diagram (PHASE 2 debit ladder) | `└─ miss → ConflictError(INSUFFICIENT_BALANCE) → ROLLBACK` | `└─ miss → ValidationError(INSUFFICIENT_BALANCE) → ROLLBACK` + ruling note below the diagram (422; `ValidationError` already supports the `(code, message)` ctor at `backend/lib/errors.ts:65-130` → no class extension needed for this code; `ConflictError` extension remains required for `TEACHER_NOT_CERTIFIED`/`SESSION_INVALID_TRANSITION`/`DUPLICATE_REQUEST`) |
| `plan.md` → §4.1 createSession step 2 | "all miss → `INSUFFICIENT_BALANCE`" (class unstated) | "all miss → `ValidationError("INSUFFICIENT_BALANCE", …)` → 422 (Ruling 2026-08-30 — REQ-050; see §3.3)" |
| `plan.md` → §3.3 map row | `ConflictError` via the same overloaded ctor → **409** | `ValidationError("INSUFFICIENT_BALANCE", …)` via the EXISTING overload (no class extension needed) → **422** + ruling note appended to the §3.3 mechanism paragraph (spec authority; the "rows keep codes/statuses unchanged" sentence now carves out the INSUFFICIENT_BALANCE row; explicit "do NOT route this code through the extended ConflictError" instruction) |
| `tasks.md` → 2.8 error-construction prerequisite | blanket "custom 409 codes (`TEACHER_NOT_CERTIFIED`, `SESSION_INVALID_TRANSITION`, …)" ambiguity | scoped: extension required ONLY for `TEACHER_NOT_CERTIFIED`/`SESSION_INVALID_TRANSITION`/`DUPLICATE_REQUEST`; `INSUFFICIENT_BALANCE` = `ValidationError("INSUFFICIENT_BALANCE", …)` → 422 on the existing overload + dated [B2] ruling note |
| `tasks.md` → 2.8 createSession phase (2) | "all-miss → `INSUFFICIENT_BALANCE` (rollback-only cleanup)" | "all-miss → `ValidationError("INSUFFICIENT_BALANCE", …)` → 422 (Ruling 2026-08-30 — REQ-050; rollback-only cleanup)" |
| `tasks.md` → 2.8.TE Tier 4 | "every denial path typed per REQ-050" | + "incl. `INSUFFICIENT_BALANCE` surfaced as `ValidationError`/422 — Ruling 2026-08-30" |

### B3 — createSession × teacher = FORBIDDEN (unconditional; carve-out deferred → D7)

| File → Section | Before | After |
|---|---|---|
| `specs.md` → REQ-011 | "Teacher-role callers (`role=teacher`) MAY create a session as the STUDENT side only if they hold a `students` row (failed-applicant conversion, INV-TV6); otherwise they are FORBIDDEN by the role scope." | Sentence STRUCK (~~…~~) + ruling note: teachers unconditionally FORBIDDEN in this slice; REQ-032 static scope stands (`builder.ts:127` membership-only ground truth); INV-TV6 honest-denial intent preserved (applicants get the honest role-scope FORBIDDEN; student-privileges clause holds on the normal role-flip path); carve-out deferred → **D7** |
| `specs.md` → REQ-064 certified-teacher `createSession` cell | "`FORBIDDEN` (role; unless he holds a students row)" | "`FORBIDDEN` (role — Ruling 2026-08-30: unconditional; students-row carve-out deferred → D7)" |
| `specs.md` → REQ-064 teacher-applicant `createSession` cell | "role-blocked" | "`FORBIDDEN` (role — honest denial, INV-TV6)" + ruling paragraph after the REQ-064 table (both teacher rows unconditional; REQ-074/5.2 assert the cells) |
| `specs.md` → §2.9 Actor Table (Certified Teacher / CANNOT) | "create as target of his own… (student-side only if he holds a `students` row)" | "create — unconditionally `FORBIDDEN` in this slice (Ruling 2026-08-30 → D7)" (consistency touch-up: same carve-out reference, would have re-triggered B3 at gate round 2) |
| `plan.md` → §3.4 matrix + footnote | Certified-teacher cell `FORBIDDEN\*` + `\*` footnote re-stating the students-row MAY carve-out | Cell `FORBIDDEN` (asterisk removed); footnote replaced by combined **Rulings (2026-08-30, orchestrator)** paragraph: (1) createSession × teacher FORBIDDEN unconditional → D7; (2) REQ-023 governance re-check scope (see B4); roleDashboardPath/redirect guidance retained |
| `tasks.md` → 3.3 | static role:[Student] scope, no teacher-cell assertion | new bullet: teacher denial on `createSession` is unconditional FORBIDDEN (static scope stands; carve-out → D7; INV-TV6 honest-denial preserved) + 3.3.TE now names the teacher→FORBIDDEN cells |
| `tasks.md` → 5.2 | "EVERY cell of the REQ-064 matrix" (unpassable under old cell) | + "Ruling 2026-08-30 cells (assert explicitly)" bullet: certified-teacher AND applicant `createSession` → `FORBIDDEN`, unconditional (D7 owns the future carve-out) |
| `deferred-items.md` → ledger | rows D1–D6 | row **D7** added: teacher-with-`students`-row session-creation carve-out (INV-TV6 extension) → future ticket (owner TBD), status ⏸ Forward, source "0.3-fix ruling (B3, 2026-08-30)", notes record the dedicated-custom-authScope + REQ-032-amendment path and in-slice honest-denial preservation; ruling addendum note added (D7 is ⏸ — REQ-083 `grep -c "❌\|⚠️"` gate stays 0) |
| `specs.md` → REQ-032 | — | UNCHANGED (static `$all{authenticated, role:[UserRole.Student]}` scope stands, per ruling) |

### B4 — REQ-023 service-layer governance re-check: SHALL honored, bounded scope

| File → Section | Before | After |
|---|---|---|
| `plan.md` → §6.3 (BFLA) | "as an explicit decision point this plan MAY add a service-layer governance re-check … (never decided)" | **Ruling (2026-08-30, orchestrator):** SHALL HONORED with bounded scope — `SessionLifecycleService` re-checks the ACTING user's governance status (deleted/blocked/suspended per REQ-023's own wording) on `createSession` (student), `startSession` (teacher), `completeSession` (teacher) only; governed → `FORBIDDEN`; **`cancelSession` EXPLICITLY EXEMPT** (governed student may still cancel in-flight — no-punishment clause preserved); still no enforcement claim about the context boundary; INV-U5 sentence retained |
| `plan.md` → §3.4 footnote | same MAY language | replaced by the same decision inside the combined Rulings paragraph (see B3 row above) |
| `tasks.md` → 2.8 (new bullet) | no governance re-check step (uncovered SHALL) | new **Governance re-check** bullet: re-check on `createSession`/`startSession`/`completeSession` (deleted/blocked/suspended → `FORBIDDEN`) — `cancelSession` EXEMPT; ground truth pinned: governance fields on `users` are **`isDeleted` (`is_deleted`), `isBlocked` (`is_blocked`), `suspended` (`suspended`)** (`backend/db/schema/users/users.ts:23-29`), already materialized by `UserRepository.findById` (`backend/db/repo/users/user.repository.ts:74` → `UserSelectType`); login-boundary predicate `isDeleted || isBlocked || suspended` (`auth.service.ts:72-81`) is the pattern to mirror; which read to use = implementer's choice (reads only, no over-specification) |
| `tasks.md` → 2.8.TE | — | governance re-check branches added to Tier-4/coverage scope (governed → FORBIDDEN on create/start/complete; cancel exempt) |
| `tasks.md` → 3.2.SEC | "…asserts login/SSR denial plus the plan's service-layer governance re-check **if plan.md adds one**" | asserts login/SSR denial PLUS the now-DECIDED re-check (bounded scope; cancel EXEMPT); queries carry no re-check; mutation cells implemented in 2.8, asserted in 3.3/5.2 |
| `tasks.md` → 5.2 | — | B4 ruling cells: governed acting user on `createSession`/`startSession`/`completeSession` → `FORBIDDEN`; the SAME governed participant's `cancelSession` (own in-flight) → still succeeds |

---

## 2. Ground Truth Verified for the Amendments (read-only)

- `backend/db/schema/users/users.ts:23-29` — governance columns: `isDeleted` ("is_deleted"), `deletedAt`, `suspended` ("suspended"), `suspendedAt`, `suspendedPeriodDays`, `isBlocked` ("is_blocked"), `blockedAt`. Comment: "Governance fields (is_deleted, suspended, is_blocked) apply to all roles."
- `backend/db/repo/users/user.repository.ts:74-99` — `UserRepository.findById` already selects/materializes all governance fields onto `UserSelectType` (no new repo read is strictly required; choice left to the 2.8 implementer).
- `backend/services/auth/auth.service.ts:72-81` — login-boundary governance predicate: `user.isDeleted || user.isBlocked || user.suspended` → `ForbiddenError` (the exact predicate the service-layer re-check mirrors).
- `docs/specs/state-machine-invariants.md:80` — INV-TV6 canonical wording: "A failed applicant who is converted to a student retains student privileges (can subscribe to plans and attend sessions) during the cooldown period." (basis for the honest-denial/normal-conversion-path preservation wording).
- `backend/graphql/pothos/builder.ts:127` — membership-only `role` scope (from 0.3 gate evidence; basis for "static scope cannot express the carve-out").

## 3. Checkbox Sync (`tasks.md`)

- `- [ ] 0.1` → `- [x] 0.1` (outcome/0.1-outcome.md exists)
- `- [ ] 0.2` → `- [x] 0.2` (outcome/0.2-outcome.md exists)
- `- [ ] 0.3` left UNCHECKED (gate re-run round 2 pending)

## 4. Not Applied / Notes

- Advisories A1–A10: NOT applied (mandate = B1–B4 only); they remain for the orchestrator's batch update.
- All four blockers applied cleanly — no partial/failed amendments. (One tooling hiccup: an early MultiEdit on `plan.md` reported non-atomic behavior and leaked its first edit (the §1.2 diagram line) before failing on a bad anchor; the line was verified applied and re-aligned to the diagram width, and all remaining plan edits were re-applied and verified by grep.)
- REQ-083's ledger gate ("grep -c ❌|⚠️ = 0 EXCEPT pre-seeded D1–D5") remains literally satisfied with D7 present: D6 is ✅ Done and D7 is ⏸ Forward — neither glyph matches the gate grep (same precedent as D6's addition in 0.1).
