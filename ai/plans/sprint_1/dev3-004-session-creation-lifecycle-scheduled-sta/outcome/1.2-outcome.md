# Task 1.2 Outcome — Schema Deltas (push-only): `session.held_balance_lane` + `session_request_idempotency`

> Task: apply REQ-013's EXACTLY-two structural deltas via `db push` ONLY — (a) the nullable
> `held_balance_lane varchar(20)` provenance column on `session` typed through
> `.$type<HeldBalanceLane>()`, and (b) the NEW `session_request_idempotency` claim table —
> plus the `classes` barrel export, then verify the REQ-045 diff discipline and cover both
> deltas with a 4-tier DB test suite inside `runInRollback`.
> Scope boundary held: 2 schema files + 1 barrel + 1 new DB test file (+ the one-line
> `logic/AGENTS.md` layout entry its own rule 4 mandates for a new test sub-directory).
> Nothing else touched; `backend/db/migration/**` diff = 0 files; no `db reset`/`cleanGenerate`.

---

## 1. Files Created / Modified

| File | Action | Content |
|---|---|---|
| `backend/db/schema/classes/session.ts` | UPDATE | Added `heldBalanceLane: varchar("held_balance_lane", { length: 20 }).$type<HeldBalanceLane>()` — nullable (no `.notNull()`, no default), NO CHECK, NO index — placed immediately after `feeHeld` (the escrow pair it records provenance for). Import: `import type { HeldBalanceLane } from "@/backend/enum/scheduling/held-balance-lane.enum";` (TYPE-position-only, deep path — mirrors how schema files import siblings/domains via `@/` aliases; the pgEnum imports stay on `@/backend/db/schema/enums`). Extended the existing table-header JSDoc escrow paragraph by 4 domain lines (which lane funded the hold, when it is NULL, same-lane refund contract). No other change. |
| `backend/db/schema/classes/session-request-idempotency.ts` | CREATE | `export const sessionRequestIdempotency = pgTable("session_request_idempotency", { id, idempotencyKey, userId, sessionId, createdAt }, t => [unique(...), index(...)])`. Columns: `id: integer("id").primaryKey().generatedAlwaysAsIdentity()`; `idempotencyKey: varchar("idempotency_key", { length: 128 }).notNull()`; `userId: integer("user_id").notNull().references(() => users.id, { onDelete: "cascade" })`; `sessionId: integer("session_id").references(() => session.id, { onDelete: "set null" })` (nullable); `createdAt: timestamp("created_at").defaultNow().notNull()`. Table constraints (array-return callback form, mirroring `users`/`session`): `unique("session_request_idempotency_key_unique").on(t.idempotencyKey)` + `index("session_request_idempotency_user_id_idx").on(t.userId)`. Header JSDoc documents the durable-claim semantics, the opaque never-logged key, and both FK behaviors. Zero `--` comments, zero `sql` templates, zero `inArray`. |
| `backend/db/schema/classes/index.ts` | UPDATE (additive) | One line: `export * from "./session-request-idempotency";` after `./session` (alphabetical: `session` < `session-request-idempotency`). Top-level `backend/db/schema/index.ts` NOT touched (already re-exports `./classes`). |
| `backend/db/test/logic/classes/session-schema-deltas.test.ts` | CREATE (sanctioned test file) | 4-tier bun:test DB suite (11 tests / 92 expect()) — see §3.2. |
| `backend/db/test/logic/AGENTS.md` | UPDATE (mandated by its own rule 4) | One layout entry documenting the new `classes/` test sub-directory under Layout. |

## 2. Files NOT Modified (and why)

- `backend/db/schema/enums.ts` — NO pgEnum added (REQ-013a: ApplicantStatus-varchar precedent; the lane vocabulary's sole runtime authority is `backend/enum/scheduling/held-balance-lane.enum.ts` from task 1.1).
- `backend/db/schema/index.ts` (top-level barrel) — `./classes` was already re-exported; no new sub-directory was created under `backend/db/schema/`.
- `backend/db/migration/**` — ZERO files touched (verified `git diff --name-only backend/db/migration/` = empty): push-only discipline, no custom SQL.
- `backend/enum/scheduling/**` — consumed type-only (schema) / value (test); the enum barrel needed no change.
- `backend/graphql/pothos/shared/enum.pothos.ts` — deliberately NOT registered: the lane is server-internal provenance, never in SDL (structurally unreachable from client input); pinned by a Tier-4 test.

## 3. Verification Results

### 3.1 Push (both databases, push-only)

| DB | Command | Result |
|---|---|---|
| dev (`kottaby_db`) | `bun --no-env-file run scripts/dbActions/index.ts --env-file=.env push` | exit 0 — "[✓] Changes applied … ✓ Success" |
| test (`kottaby_test_db`) | `bun --no-env-file run scripts/dbActions/index.ts --env-file=.env.test push` | exit 0 — "[✓] Changes applied … ✓ Success" |

Post-push SQL introspection (psql, BOTH DBs identical):
- `session.held_balance_lane`: `character varying`, max length **20**, `is_nullable = YES`. ✅
- `session_request_idempotency` columns: `id integer NOT NULL` (identity PK), `idempotency_key varchar(128) NOT NULL`, `user_id integer NOT NULL`, `session_id integer NULL`, `created_at timestamp NOT NULL` (default now()). ✅
- Constraints: `session_request_idempotency_pkey` (p), `session_request_idempotency_key_unique` (u), `session_request_idempotency_user_id_users_id_fkey` (f, cascade), `session_request_idempotency_session_id_session_id_fkey` (f, set null). ✅
- Indexes: PK + `session_request_idempotency_key_unique` + `session_request_idempotency_user_id_idx`. ✅
- `session` CHECK-constraint count = **0** (no new CHECK anywhere on the table; pre-existing students CHECKs untouched). ✅

REQ-045 diff discipline: `git diff backend/db/schema/**` = exactly the two artifacts + barrel (verified by reading the diff); `backend/db/migration/**` diff = empty; `db reset`/`db cleanGenerate` never invoked (and remain repo-disabled).

### 3.2 Quality Loop (sub-loop `--lifecycle duplicates`)

| File | Result |
|---|---|
| `backend/db/schema/classes/session.ts` | ✅ exit 0 — tsgo ✅ oxlint ✅ biome:check ✅ lint:type-aware ✅ check:duplicates ✅ (first pass) |
| `backend/db/schema/classes/session-request-idempotency.ts` | ✅ exit 0 — all five checks ✅ (first pass) |
| `backend/db/schema/classes/index.ts` | ✅ exit 0 — all five checks ✅ (first pass) |
| `backend/db/test/logic/classes/session-schema-deltas.test.ts` | ✅ exit 0 after ONE fix cycle: oxlint `no-await-in-loop` ×3 on the lane round-trip `for` loop. Root-cause fix (no disable comments): extracted a file-local `roundTripLane(tx, actors, lane)` helper and call it once per member — sequential statements inside the same rollback transaction, no loop. Re-run: all five checks ✅. |

Full-repo `bun tsgo` after all edits: exit 0, zero errors (baseline delta = 0).

### 3.3 Test Engineering (`bun run test/scripts/run-test.ts backend/db/test/logic/classes/session-schema-deltas.test.ts`)

**11 pass / 0 fail / 92 expect() calls / exit 0** (~240ms). Every test runs inside `runInRollback` with `tx` passed to every Drizzle call and helper; `expect(...).rejects.toThrow()` is never used — error probes go through the repo's `expectRepoError` try/catch helper with PG-code identification via the Drizzle `cause` chain (the `logic/teachers/applicant-lifecycle.test.ts` precedent) and a translated-substring assertion on the DRIVER's message (deepest cause), since the top-level Drizzle message is a generic "Failed query" wrapper.

- **Tier 1 — presence/type round-trip (2 tests):** lane-less session insert reads `heldBalanceLane` back as `null`; each of the three members (`trial`/`hifz`/`tajweed`) round-trips the varchar(20) verbatim, the read-back value re-validates through the app-layer `isHeldBalanceLane` guard, and `$inferSelect` is compile-pinned to yield `HeldBalanceLane | null` via the zero-cast `acceptLane(lane: HeldBalanceLane | null)` sink (tsgo fails if the `$type<>()` binding or the column is lost — verified by tsgo exit 0 with the sink call in place). Claim-table insert → select round-trip of all columns (id, key, userId, sessionId, createdAt as Date) is Tier-3's first test (same discipline, exercised against a session-bound claim).
- **Tier 2 — boundary (3 tests):** `held_balance_lane` accepts a member within varchar(20) capacity; idempotency key of EXACTLY 128 chars accepted and read back verbatim (`toHaveLength(128)`, userId/sessionId/createdAt verified); 129 chars rejected with PG `22001` (`string_data_right_truncation`) + driver message contains "value too long". Error probes are the LAST statement of their own `runInRollback` body (a failed statement aborts the surrounding PostgreSQL transaction).
- **Tier 3 — FK behavior probes + unique chaos (4 tests):** claim insert→select round-trips all columns (session-bound); deleting the owning `users` row CASCADES the claim away (count 1 → 0, by userId and by key); deleting the `session` row sets the claim's `session_id` to NULL while the claim row itself SURVIVES (replay keeps surfacing duplicate semantics instead of silently re-booking); a duplicate `idempotencyKey` violates the unique constraint with PG `23505` (unique-violation — the exact signal task 2.7/2.8's cause-chain traversal will translate into the duplicate-request conflict).
- **Tier 4 — nothing in schema is client-reachable (2 static tests + rationale):** (a) the physical names `session_request_idempotency` and `held_balance_lane` appear in ZERO files under `backend/graphql/**` and `HeldBalanceLane` is unregistered in the Pothos enum registry — the lane is server-internal provenance structurally absent from the SDL, and the claim table has no GraphQL surface; (b) source pins — the lane declaration binds `$type<HeldBalanceLane>()` and carries no `index(`/`check(`, the session table declares no CHECK at all, and neither schema file contains a `sql` template or an inline `--` comment. Rationale (recorded): this slice adds no resolver and no input type; the claim table is written only by future repositories/services; the closed create-input whitelist is specified to omit every server-controlled field including `heldBalanceLane`; key uniqueness is enforced DB-side so no read path can leak another user's claim.
- **Regression:** sibling DB suites re-run green after the push — `logic/teachers/applicant-lifecycle.test.ts` 10 pass / 0 fail (38 expect), `logic/auth/registration.service.test.ts` 18 pass / 0 fail (80 expect).

### 3.4 Test-convention provenance

New test sub-directory `backend/db/test/logic/classes/` (domain-grouped per `backend/db/test/logic/AGENTS.md`, which mirrors the schema/repo/type/enum layouts; its rule 4 required the one-line Layout registration). Canonical patterns mirrored from `logic/teachers/applicant-lifecycle.test.ts`: header coverage map, `runInRollback` + tx-everywhere, `entity-setup.ts` fixtures (`createTestUser`, `createTestStudent`) + a file-local actor helper, `expectRepoError`, file-local `hasPostgresErrorCode` cause-chain walker (extended with a `rootCause` helper for driver-message assertions), independent read-back oracles via direct Drizzle selects on the same tx, unique-value generation via `randomUUID()`.

## 4. SEC — Security & Tenancy Audit

- ✅ **Claim table keyed by key + userId; key opaque ≤128, never logged:** `idempotency_key` is a NOT NULL UNIQUE varchar(128) storing the raw header value; neither schema file nor the test logs it (`grep console.|logger` = 0 across all touched files). Observation for downstream tasks (2.7/2.8): Drizzle's own `DrizzleQueryError` top-level message embeds the SQL params (including the key) — that is framework-wide behavior for every query in this repo and outside this slice's control; the service's duplicate-branch must translate the cause-chain 23505 into the conflict error WITHOUT echoing the key into any log it writes itself.
- ✅ **FK cascade preserves cleanup correctness (INV-U4-adjacent):** `user_id` ON DELETE CASCADE means no orphaned claims ever outlive their owner; `session_id` ON DELETE SET NULL keeps the claim's duplicate-blocking semantics intact after the session row is gone (proven by Tier 3). Both behaviors are DB-enforced, not app-remembered.
- ✅ **No oracle columns:** the delta adds NO sortable/leakable oracle surface — `held_balance_lane` is server-internal provenance absent from SDL (Tier-4 pinned), the claim table is keyed by the caller's own opaque key and reachable only through future participant-scoped repos; no balance values, no other-party identity columns.
- ✅ **Tenancy:** `userId` is NOT NULL and cascade-bound to `users.id` — a claim always belongs to exactly one user; the user-id index (`session_request_idempotency_user_id_idx`) makes per-user claim lookups cheap without exposing cross-user scan paths.

## 5. SR — Semantic Review Checklist

- ✅ **Sole-ground-truth discipline:** the physical names exist ONLY in the Drizzle schema — `git diff backend/db/schema/**` contains exactly the two artifacts + barrel; grep for `held_balance_lane`/`session_request_idempotency` outside `backend/db/schema/classes/` hits only the new test (its Tier-4 negative pins) and plan docs. No duplicate column/table declarations anywhere.
- ✅ **`.$type<>()` flows the enum into inference with zero downstream casts:** `SessionSelectType` (= `typeof session.$inferSelect`, pre-existing in `backend/types/classes/session.types.ts`) now carries `heldBalanceLane: HeldBalanceLane | null` automatically — proven at compile time by the `acceptLane` sink (a `HeldBalanceLane | null`-typed parameter receiving the field directly) and at runtime by guard-validated round-trips; no `as` casts anywhere in the delta or the test (grep-verified).
- ✅ **Enums: TYPE import in schema, VALUE import in runtime expressions:** schema uses `import type { HeldBalanceLane }` (type position only — no runtime dependency from the schema layer on the enum module); the TEST value-imports `{ HeldBalanceLane, isHeldBalanceLane }` for its runtime member expressions/guard calls (grep-verified both directions).
- ✅ **No inline `--` in any `sql` template; no `inArray`:** neither schema file contains any `sql` template or `--` sequence at all (Tier-4 test pins this permanently against both files); no `inArray` anywhere in the delta or the test.
- ✅ **No cross-layer imports / no dead exports / no console.***: schema files import only drizzle-orm + sibling/domain schema tables + the type-only enum; the test imports bun:test, node:crypto/fs/path, drizzle-orm, and `@/backend/*` layers only (grep `@/frontend|@/shared|@/app` = 0). The new table export is the designated consumption surface for tasks 2.6/2.7 (repos) and 1.3 (types) — intended-forward wiring, not dead code.
- ✅ **Scope boundary respected:** `git status` delta = 2 modified schema files + 1 new schema file + 1 new test file (+ the AGENTS.md layout line its rule 4 mandates). Zero changes outside `backend/db/schema/classes/` and `backend/db/test/logic/`.

## 6. IV — Instruction Verification

| Rule (source) | Verdict |
|---|---|
| Drizzle ORM only; `pgTable(...)`; no raw SQL DDL (`backend/db/schema/AGENTS.md`) | ✅ both deltas are pure `pgTable` builder chains; zero `sql` templates (test-pinned) |
| One table per domain file; distinct domains in distinct files | ✅ new table in its own `classes/` file (same scheduling domain as `session`, matching the plan-of-record layout); `session.ts` untouched beyond the column + JSDoc |
| Enum values verified, never guessed (`backend/db/schema/AGENTS.md`) | ✅ no new pgEnum; the varchar vocabulary is the task-1.1 TS enum (values `trial`/`hifz`/`tajweed` consumed via `.$type<>`, guard re-validated in tests) |
| Schema synchronization: `bun run db push` after schema edits; `db reset`/`cleanGenerate` permanently disabled | ✅ pushed to BOTH DBs via `scripts/dbActions/index.ts --env-file=.env|.env.test push` (exit 0 ×2); destructive commands never invoked |
| Structural ground truth: verify the table does not already exist before authoring | ✅ task 0.2 §5 collision check (zero code hits) + `git ls-files` confirm no pre-existing `session-request-idempotency` file/table; column absent from `session.ts` before this task |
| Adding-New-Tables steps 1–4 (`backend/db/schema/AGENTS.md`) | ✅ matching sub-directory `classes/`; `<entity>.ts` created with `export const sessionRequestIdempotency = pgTable(...)`; sub-directory barrel gained the `export *` line; no new sub-directory under `backend/db/schema/` so no top-level barrel change needed |
| Import convention: `@/` aliases for cross-domain refs; siblings in the same sub-directory may use relative | ✅ used the repo's dominant `@/backend/db/schema/<subdir>/<sibling>` form for BOTH the cross-domain (`users`) and same-subdir (`session`) refs — mirrors `billing/student-payments.ts` → `subscriptions` sibling precedent; enum via `@/backend/enum/scheduling/held-balance-lane.enum` deep path (backend enum convention: deep imports preferred) |
| Consumers import via the top-level barrel; deep imports valid | ✅ barrel export added; the test uses deep schema paths (test-layer convention per existing DB suites) |
| Pothos enum registration only when GraphQL-exposed (`backend/db/schema/AGENTS.md` cross-ref) | N/A-by-design — lane never in SDL; claim table never in SDL; Tier-4 test pins both |
| Linting: NEVER `oxlint-disable`; root-cause fixes (`backend/db/schema/AGENTS.md`, DB test AGENTS) | ✅ the one oxlint failure was fixed by removing the loop (helper-per-member), no disable comments |
| `docs/DATABASE_MIGRATIONS.md` push-only rule | ✅ push-only honored on both DBs; NO custom SQL migration authored; `backend/db/migration/` diff = 0 files; `db reset`/`db cleanGenerate` (permanently disabled) never attempted; no `CREATE INDEX CONCURRENTLY` hazard (push applies plain index DDL) |
| DB test rules (`backend/db/test/AGENTS.md` + `logic/AGENTS.md`) | ✅ `runInRollback` everywhere with tx passed to every call; entity-setup fixtures + file-local helpers; `expectRepoError` (never `rejects.toThrow`); translated-substring + PG-code assertions; no `any`, no `console.*`; pre-existing-data-safe counts (targeted where-clauses on in-tx rows); new sub-directory documented in the Layout section per rule 4; bun:test only; run via `test/scripts/run-test.ts` |

## 7. Environment Notes (for the orchestrator)

1. PostgreSQL 17.11 (user-space cluster at /tmp/pgdata) was verified accepting connections before the push (worklog env-setup command reused); no restart was needed this session.
2. `~/.bun/bin/bun` symlink (created by task 1.1) present and functional — `run-test.ts` ran normally.
3. Sub-loop ran clean on all four files after the single test-file fix cycle; no lint-queue server was needed (the sub-loop's in-process path covered tsgo/oxlint/biome/lint:type-aware/check:duplicates).

## 8. Carry-Forward Knowledge (for future subtasks — esp. 2.6/2.7/2.8)

- **Repo patterns used (reuse verbatim in 2.6/2.7 repos and their tests):**
  - Identity PK: `id: integer("id").primaryKey().generatedAlwaysAsIdentity()`.
  - FK: `.references(() => users.id, { onDelete: "cascade" })` / `{ onDelete: "set null" }` / `{ onDelete: "restrict" }` — Drizzle's default FK-constraint naming applies (`<table>_<col>_<reftable>_<refcol>_fkey`).
  - Table constraint block: second `pgTable` argument as an ARRAY callback `t => [unique("name_unique").on(t.col), index("name_idx").on(t.col)]` (repo-wide form; `users.ts` and `session.ts` precedents).
  - Timestamp: `createdAt: timestamp("created_at").defaultNow().notNull()`.
  - Type-only enum binding: `import type { X } from "@/backend/enum/...";` + `.$type<X>()` — the applicant-varchar precedent generalized.
- **`SessionSelectType` (from `@/backend/types/classes/session.types.ts`, pre-existing) now includes `heldBalanceLane: HeldBalanceLane | null`** — task 1.3's `SessionReturnType` re-application and 2.6's repo reads get the enum type for free; NO downstream cast is needed or allowed.
- **Claim-table error surface for 2.7/2.8:** a duplicate `idempotency_key` insert raises PG `23505` wrapped by Drizzle — traverse the `cause` chain (`isUniqueViolation`-style, NEVER reading `.code` off the top-level Drizzle error); the unique-constraint name is `session_request_idempotency_key_unique`. The claim insert must happen in-phase with the session insert (transactional fate-sharing per REQ-040 ordering).
- **PG-code facts proven by tests:** 129-char key → `22001` "value too long"; duplicate key → `23505`; a failed statement aborts the surrounding transaction (structure error probes as the last statement of a `runInRollback` body, or use a nested savepoint).
- **Test infrastructure:** `logic/classes/` is now an established test sub-directory for the session domain — future session-domain DB tests belong there (or `backend/db/test/repo/` once task 2.x scaffolds it); the file-local `setupSessionActors` (student user + students row + teacher user + teacher row via shared-PK insert) is the minimal actor set for creating `session` rows; `hasPostgresErrorCode`/`rootCause` cause-chain helpers are file-local (extract to `test-utils.ts` only if a third suite needs them, per the AGENTS duplication rule).
- **SEC observation:** Drizzle's error wrapper embeds query params (incl. idempotency keys) in its top-level message — downstream services must not forward raw Drizzle messages into their own logs on the duplicate path.

## 9. Cross-File Dependencies

- **← Task 1.1:** `HeldBalanceLane` + `isHeldBalanceLane` consumed from `backend/enum/scheduling/held-balance-lane.enum` (type-only in schema; value in test) — barrel path stable.
- **→ Task 1.3 (canonical types):** `SessionSelectType` auto-gains `heldBalanceLane: HeldBalanceLane | null`; `session-request-idempotency.types.ts` derives Select/Insert from `typeof sessionRequestIdempotency.$infer*` (the table export is the barrel-stable source).
- **→ Tasks 2.6/2.7 (repos):** `sessionRequestIdempotency` is exported through `backend/db/schema/classes/index.ts` (and the top-level schema barrel); `insertClaim`/`findByKey` repo methods consume `SessionRequestIdempotencyInsertType`/`SelectType`; 23505 → cause-chain translation.
- **→ Task 2.8 (service):** REQ-040 transaction order ends with the claim insert + session insert; `cancelSession` reads `row.heldBalanceLane` (guard-validate varchar reads via `isHeldBalanceLane`) to refund the SAME lane; dual-confirm/sweeper consumers (DEV3-012/013 forward-contract) null/populate the column per the same-lane ruling.
- **← Test layer:** `backend/db/test/logic/classes/` registered in `logic/AGENTS.md` Layout; sibling suites re-run green (no schema-push regression).
