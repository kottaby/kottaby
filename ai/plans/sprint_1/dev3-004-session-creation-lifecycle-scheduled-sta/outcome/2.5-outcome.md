# Task 2.5 Outcome — TeacherRepository: Certification Lock (NEW repo)

> Task: CREATE `backend/db/repo/teachers/teacher.repository.ts` with the NEW `TeacherRepository`
> namespace exposing ONE method — `lockForCertificationCheck(teacherId: number, tx: DBTransaction):
> Promise<{ id: number; isApproved: boolean | null } | null>` — the `SELECT id, is_approved FROM
> teacher WHERE id = $1 FOR UPDATE` certification lock via Drizzle `.for("update")` (tx REQUIRED,
> minimal projection, write-path → no prepared statement), plus the additive barrel line in
> `backend/db/repo/teachers/index.ts`. Scope boundary held: 2 repo-layer files + 1 sanctioned DB
> test suite (new `backend/db/test/repo/teachers/` directory). Nothing else touched — the parallel
> 2.4 dispatch's `backend/db/repo/students/**` files were NOT read-modified.

---

## 1. Files Created / Modified

| File | Action | Content |
|---|---|---|
| `backend/db/repo/teachers/teacher.repository.ts` | CREATE | `export namespace TeacherRepository` with exactly ONE exported method, `lockForCertificationCheck(teacherId: number, tx: DBTransaction): Promise<{ id: number; isApproved: boolean \| null } \| null>`. Implementation: `tx.select({ id: teacher.id, isApproved: teacher.isApproved }).from(teacher).where(eq(teacher.id, teacherId)).for("update")` → `rows[0] ?? null`. Imports: `eq` (drizzle-orm), `teacher` (deep schema path `@/backend/db/schema/teachers/teacher`), `DBTransaction` (type-only, `@/backend/types`). Header JSDoc documents the shared-PK/applicant relationship, the same-transaction certification re-assertion purpose, the tx-required/no-prepared-statement/no-i18n/no-log conventions; method JSDoc documents lock-until-commit-or-rollback semantics, the REQUIRED-tx rationale (a lock released at statement end protects nothing), the minimal projection, and the honest `null` for teacherless ids (an applicant's `users.id` never mints certification). ZERO raw SQL, ZERO `--` sequences, ZERO interpolation, ZERO casts, ZERO i18n/logger imports. |
| `backend/db/repo/teachers/index.ts` | UPDATE (additive) | One line appended: `export * from "./teacher.repository";` after the existing `./applicant.repository` line (alphabetical: applicant < teacher). Barrel remains a pure relative `export *` list. Top-level `backend/db/repo/index.ts` NOT touched (it already re-exports `./teachers`). |
| `backend/db/test/repo/teachers/teacher.repository.test.ts` | CREATE (sanctioned test file) | 4-tier bun:test DB suite — **14 tests / 43 expect() / all green** (see §3.2). New directory `backend/db/test/repo/` created per `backend/db/test/AGENTS.md` rule 11 (`repo/` = individual-repository-method tests; `run-test.ts`'s own help examples reference `backend/db/test/repo/*.test.ts`) and `backend/db/test/logic/AGENTS.md`'s "one repo per file" note. |

`git status` delta after the task: `M backend/db/repo/teachers/index.ts` + untracked `backend/db/repo/teachers/teacher.repository.ts` + untracked `backend/db/test/repo/` — plus `M backend/db/repo/students/student.repository.ts` which belongs to the PARALLEL task-2.4 dispatch (present before/throughout this run; never read-modified by this task).

## 2. Files NOT Modified (and why)

- `backend/db/repo/index.ts` (top-level barrel) — already re-exports `./teachers`; no new sub-directory was created under `backend/db/repo/` (Adding-New-Repositories step 4 N/A).
- `backend/db/repo/students/**` — task 2.4's territory (parallel dispatch); not touched.
- `backend/db/schema/teachers/teacher.ts` — read-only ground truth: `isApproved: boolean("is_approved").default(false)` (nullable, no `.notNull()`) is why the projection type is `boolean | null`; the schema is consumed, never amended.
- `backend/types/teachers/teacher.types.ts` — `TeacherSelectType` already exists; the lock's minimal projection is a two-column literal (NOT a `Pick` fork) because the plan mandates the exact inline shape `{ id; isApproved }` and no other consumer needs a named type yet (a named alias would be a speculative/dead export).
- `entity-setup.ts` — still has NO teacher factory (verified); the test uses a file-local `createTestTeacherRow(tx, userId, isApproved)` shared-PK helper mirroring the `createTestParent`/`createTestAdmin`/session-cast `buildTeacherCastMember` pattern instead of expanding entity-setup's file list in this task (same ruling as 2.1; §7 carry-forward).
- `backend/services/**` / `backend/graphql/**` — no consumer wiring: the service (task 2.8) imports the method in its phase-1 lock; pre-wiring would create dead imports.

## 3. Verification Results

### 3.1 Quality Loop (sub-loop `--lifecycle duplicates`)

| File | Result |
|---|---|
| `backend/db/repo/teachers/teacher.repository.ts` | ✅ exit 0 — tsgo ✅ oxlint ✅ biome:check ✅ lint:type-aware ✅ check:duplicates ✅ (FIRST PASS) |
| `backend/db/repo/teachers/index.ts` | ✅ exit 0 — all five checks ✅ (FIRST PASS) |
| `backend/db/test/repo/teachers/teacher.repository.test.ts` | ✅ exit 0 after TWO fix cycles (all root-cause, no disable comments): (1) oxlint `unicorn(consistent-function-scoping)` on the hand-rolled promise-executor gate → replaced with `Promise.withResolvers()` (esnext target; verified supported); (2) lint:type-aware ×3 — `sonarjs/no-alphabetical-sort` (`.toSorted()` now uses `localeCompare`) and `sonarjs/prefer-specific-assertions` ×2 (`.match(...).length` → `.toHaveLength`). |

Full-repo `bun tsgo` after all edits: **exit 0** (baseline delta = 0; note — this project-wide check already includes the parallel 2.4 students WIP, which compiles cleanly at check time).

### 3.2 Test Engineering (`bun run test/scripts/run-test.ts backend/db/test/repo/teachers/teacher.repository.test.ts`)

**14 pass / 0 fail / 43 expect() / exit 0** (~1.2s; DB = `kottaby_test_db` via the runner's `--env-file=.env.test`). Every rollback-isolated test runs inside `runInRollback` with `tx` passed to the repo call, the entity-setup helpers, and the direct Drizzle read-backs; no `rejects.toThrow()` anywhere (the method signals "missing row" by returning `null`, so there are no throwing paths).

- **Tier 1 — branch/statement (3 tests):** approved row → `{id, isApproved: true}` with a key-set pin (`Object.keys` = exactly `["id","isApproved"]` — minimal projection); unapproved row → `isApproved: false`; nonexistent id (max+1,000,000 pattern, mirroring the applicant-lifecycle absent-id helper) → `null`.
- **Tier 2 — boundary / honest failure (3 tests):** teacher-applicant user (`role=teacher` users row + `applicants` row, NO `teacher` row) → `null` (INV-TV1 honest failure — the applicant's `users.id` is not a teachable id); a row inserted with DB-level `isApproved: null` → the flag surfaces verbatim as `null` (the `boolean | null` return boundary is never coerced — null ≢ false); re-locking the same row twice inside ONE transaction is idempotent (`toEqual`) — the lock is tx-scoped, not statement-scoped.
- **Tier 3 — the lock is OBSERVABLE (2 tests, real concurrent tx pairs):**
  - **Commit path (515ms):** a committed teacher row (`isApproved=false`); tx A locks it via the repo and parks on a gate; a `Promise.race` probe shows tx B's `lockForCertificationCheck` still pending after a 500ms window (`"blocked"`) — the second FOR UPDATE read BLOCKS; a contrast plain-MVCC read in tx C completes instantly and still sees `false` (proving the serialization is the FOR UPDATE lock, not ordinary read behavior); A then flips `isApproved=true` and COMMITS; B resolves only afterwards with `{id, isApproved: true}` — the waiter reads as of ITS lock acquisition, i.e. the value the holder committed (INV-S5/TOCTOU-free semantics demonstrated, not assumed).
  - **Rollback path (506ms):** a committed row locked by a `runInRollback` holder that changes nothing; probe proves the waiter blocked; the holder returns → forced ROLLBACK → lock released → the waiter acquires and reads the durable committed value (`isApproved=false`) — an aborted holder leaves no trace and never wedges the row.
  - Fixtures created outside `runInRollback` (the concurrency tier requires committed rows) are registered and hard-deleted in `afterAll` (teacher row, then owning user — cascade direction), per `backend/db/test/AGENTS.md` rule 9. **Empirical zero-leak check:** post-suite SQL sweep over `kottaby_test_db` → `users(test-%@test.local)=0`, `teacher=0`, `applicants=0`.
  - Robustness: both concurrent promises get settled-mapped branches (no unhandled rejections), the probe timer is cleared, and the holder's release gate is opened idempotently in `finally` so a mid-probe assertion failure can never deadlock the suite.
- **Tier 4 — injection surface / static pins (6 tests, file-content scans of the repo source, mirroring the session-schema-deltas static tier):** no `--` sequences, no `${` interpolation, no `sql\`` templates, no `queryDb(` (a locking read must never ride the non-tx fast path); no `.prepare(`/`sql.placeholder` (write-path → no prepared statements); `tx: DBTransaction` REQUIRED — no `tx?:` and no `DBQueryExecutor` branch; no `getServerTranslations`/`logger`/`console.`; exactly one exported function inside the namespace + `.for("update")` present + the `.select(` projection block references exactly the two teacher columns; comments contain zero plan-artifact references (REQ-/DEV3/Phase/Task/plan.md/tasks.md/specs.md regex = no match).
- **Regression:** sibling teachers-domain suite re-run green after the barrel change — `logic/teachers/applicant-lifecycle.test.ts` → 10 pass / 0 fail / 38 expect.

### 3.3 Test-convention provenance

New `backend/db/test/repo/teachers/` sub-directory: per parent `backend/db/test/AGENTS.md` rule 11 ("repo/: tests specifically dedicated to achieving 100% coverage of individual repository methods in isolation") and `backend/db/test/logic/AGENTS.md` ("the pre-existing backend/db/test/repo/ directory holds repository unit tests (one repo per file)" — the directory itself was absent on this branch; recreated, no barrel needed since the runner globs). Patterns mirrored from `logic/teachers/applicant-lifecycle.test.ts`: header coverage map, `runInRollback` + tx-everywhere, entity-setup fixtures + file-local helpers, absent-id derivation via `coalesce(max(id),0)+offset`, sorted key-set pins. No `logic/AGENTS.md` Layout edit required (that rule 4 governs `logic/` sub-directories only; the parent test/AGENTS.md already documents `repo/`'s role, and run-test.ts's help text cites `backend/db/test/repo/` paths verbatim).

## 4. SEC — Security & Tenancy Audit

- ✅ **Parameterized id only:** the id reaches the query exclusively as a Drizzle `eq()` bound parameter (`WHERE id = $1`) — no raw SQL string exists in the file that a value could be interpolated into (Tier-4 pins: zero `${`, zero `sql\``, zero `--`); hostile id shapes have no surface (the query is PK equality; a non-matching id degrades to the honest `null`).
- ✅ **Lock scope = transaction (no cross-tx leakage):** the method takes NO executor other than the caller's `tx` — the REQUIRED (non-optional) parameter plus the `queryDb(`-absence pin make a non-transactional execution structurally impossible, so the FOR UPDATE lock is always bounded by the caller's commit/rollback; Tier 3 proves the boundary behaviorally both ways (waiter blocked until commit; lock released on rollback; plain MVCC readers unaffected — no lock escalates into a read path).
- ✅ **Applicant → null honest failure (INV-TV1):** a `users.id` that only owns an `applicants` row resolves to `null` — the repo neither throws nor mints certification state; the service layer (2.8) translates `null` → the documented not-found denial. The projection exposes ONLY `id` + `isApproved` — no subjects, ratings, online flag, or governance fields leak off the locked row (minimal-projection pin).
- ✅ **No secrets/oracles/logging:** zero `console.*`, zero logger, zero env reads, zero serialized payloads in the repo file or the suite (grep + Tier-4 pin).

## 5. SR — Semantic Review Checklist

- ✅ **Minimal projection:** exactly `{ id, isApproved }` — key-set-pinned at runtime and count-pinned statically; no `SelectType` dump, no speculative fields.
- ✅ **Zero business logic / zero dead methods:** one namespace, one exported function (static count = 1); no permission checks, no error translation, no i18n imports, no logger — the caller decides what `null` / `false` / `null-flag` mean (repo/AGENTS.md separation-of-concerns).
- ✅ **tx REQUIRED (last param):** signature is `(teacherId: number, tx: DBTransaction)` — required by the task mandate and semantically necessary (a FOR UPDATE outside a tx releases at statement end); the JSDoc states the rationale; no optional-`tx` overload exists.
- ✅ **Write-path → NO prepared statement:** no `.prepare(`/`sql.placeholder` (Tier-4 pin); the method always executes inside a caller transaction, so the Neon-HTTP `queryDb` read pattern is intentionally absent (its own JSDoc branch would be dead code here).
- ✅ **Enums:** none involved (`isApproved` is a boolean column; no enum import needed — nothing to VALUE-import).
- ✅ **Clean comments (ZERO plan-artifact references):** grep `REQ-\d|DEV3|Phase \d|Task \d|plan\.md|tasks\.md|specs\.md` over both repo files + test = 0 matches (enforced by the Tier-4 test for the repo file); JSDoc describes domain behavior only (lock semantics, applicant/teacher row relationship, tx-required rationale).
- ✅ **No `console.*` / no spreads / no `any` / no `oxlint-disable`:** grep-verified across all three files; the two lint fix cycles were root-cause fixes (`Promise.withResolvers`, `localeCompare`, `.toHaveLength`) with zero suppression comments.
- ✅ **Scope boundary respected:** diff = exactly the 2 teachers repo files + the new test dir; `backend/db/repo/students/**` and every other path untouched (the one `M students/...` entry in git status is the parallel 2.4 dispatch's file, not this task's).

## 6. IV — Instruction Verification (`backend/db/repo/AGENTS.md`, item by item)

| Rule | Verdict |
|---|---|
| Repos = pure data access; no business logic / permission checks | ✅ single locking read; decisions (null → NotFound, false → Conflict) are the service's job (task 2.8) |
| Registration repos take `tx` — write-path transactionality | ✅ this is a WRITE-PATH locking read: `tx: DBTransaction` REQUIRED (stronger than the optional-`tx` read convention; locking without a tx is meaningless — JSDoc-documented) |
| Neon HTTP Client for bare reads (`queryDb(tx)`) | N/A-by-design — this is NOT a bare read; it is a transaction-bound locking read that must never execute outside the caller's tx, so the `queryDb` fast path is deliberately absent (Tier-4 test pins `queryDb(` = 0 occurrences) |
| Prepared Statements (simple TCP reads MAY prepare) | ✅ NO prepared statement — plan marks the lock as write-path; Tier-4 pins `.prepare(`/`sql.placeholder` = 0 |
| `inArray` + prepared statements PROHIBITED | ✅ no `inArray` anywhere in the file (single-PK equality) |
| Batch lookup methods for DataLoader | N/A — no GraphQL DataLoader consumer for this method (single-id lock; batching a row lock is meaningless) |
| Query guidelines (avoid relational-query API pitfalls) | ✅ plain `.select().from().where().for()` builder — the sanctioned shape |
| Separation of concerns (services own orchestration) | ✅ no orchestration, no error classes, no translations |
| No hardcoded error strings (i18n via `getServerTranslations`) | ✅ the repo returns data/`null` and throws nothing; zero i18n imports (Tier-4 pin) — the translated-message mapping happens in the service layer |
| Type Definition Pattern (types from `backend/types/`) | ✅ parameter/return types from `@/backend/types` (`DBTransaction`); the projection is the plan-mandated inline literal (no new type file, no schema-type re-export) |
| Import convention (consumers via top-level barrel; `@/` aliases in repo files; relative only for same-subdir siblings) | ✅ repo file uses `@/` deep paths; barrel line is relative `./teacher.repository`; the namespace is barrel-reachable as `import { TeacherRepository } from "@/backend/db/repo"` (mirrors the AGENTS.md example verbatim) |
| File organization (`<subdir>/<entity>.repository.ts`, one namespace per file) | ✅ `teachers/teacher.repository.ts`, `export namespace TeacherRepository` |
| Adding New Repositories steps 1–5 | ✅ 1: matching subdir `teachers/` exists; 2: file created with the namespace; 3: subdir barrel gained `export * from "./teacher.repository";`; 4: N/A (no new subdir under repo/); 5: wired types N/A (no new type file needed), GraphQL exposure N/A (never in SDL — server-internal lock) |
| Linting: NEVER `oxlint-disable`; root-cause fixes | ✅ both test fix cycles fixed root causes; zero disable comments anywhere |
| Deep imports valid for avoiding transitive side-effects | ✅ the test imports `TeacherRepository` from the top-level barrel per the canonical example; the suite runs green with the parallel students WIP in-tree |

Sub-loop rule files additionally surfaced (`.github/instructions/backend.instructions.md`, `tests.instructions.md`): honored — no `console.*`, no nested ternaries, `bun:test` only, run via `test/scripts/run-test.ts` (never raw `bun test`), no `as any`.

## 7. Environment Notes (for the orchestrator)

1. PostgreSQL 17.11 (user-space `/tmp/pgdata`, port 5432) was up at dispatch start; the initial readiness probe failed only because `pg_isready` lacked `LD_LIBRARY_PATH` — with the worklog's library path the server was already accepting connections; no restart needed.
2. `~/.bun/bin/bun` symlink intact (no re-provisioning).
3. **Parallel-dispatch coexistence:** task 2.4's `backend/db/repo/students/student.repository.ts` modifications were present (uncommitted) throughout this run; all project-wide tsgo/sub-loop checks and the test runs passed WITH that WIP in-tree — no interference, no re-runs needed. This task made zero writes outside its own 3 files.
4. Post-suite DB sweep: zero residual `test-%@test.local` users, zero `teacher` rows, zero `applicants` rows in `kottaby_test_db` — the concurrency tier's committed fixtures were fully reclaimed by `afterAll`.

## 8. Carry-Forward Knowledge (for task 2.8 + later tasks)

- **Exact consumption contract for 2.8 phase (1):** inside `withTransaction(outerTx)`, call `const lockedTeacher = await TeacherRepository.lockForCertificationCheck(input.teacherId, tx);` — `null` → `NotFoundError("TEACHER", t.teacherNotFound)`; `lockedTeacher.isApproved === false` (and `null` flag, which is falsy-certification) → `ConflictError("TEACHER_NOT_CERTIFIED", …)` via the extended ctor (B2 ruling scope). The returned lock value IS the certification the insert commits against — do not re-read the teacher through another method inside the same tx (the lock is already held; a second identical call is a harmless tx-scoped re-lock, proven idempotent by Tier 2).
- **Barrel reachability:** `TeacherRepository` resolves from `@/backend/db/repo` (top-level barrel) exactly as the AGENTS.md import example shows; deep path `@/backend/db/repo/teachers/teacher.repository` also valid.
- **`isApproved` nullability is real:** the column is `.default(false)` WITHOUT `.notNull()`, so the projection is `boolean | null` — the service's certification assert must treat `null` as NOT certified (only strict `true` passes); a Tier-2 test pins the null-flag passthrough.
- **Concurrency-test recipe (reusable for 2.6/2.8 Tier 3):** committed fixture + gate-driven holder (`Promise.withResolvers`) + `Promise.race` blocked-probe (500ms) + settled-mapped waiter + idempotent release in `finally` + `afterAll` hard-delete sweep; `runInRollback` can itself BE the holder (lock released by the forced rollback). Caveat learned: a FOR UPDATE cannot observe/blocked-wait on an UNCOMMITTED INSERT from another tx (READ COMMITTED snapshots exclude it) — contention tests must lock COMMITTED rows.
- **New test home:** `backend/db/test/repo/` now exists — repository-isolation suites for 2.6/2.7 belong in `backend/db/test/repo/<domain>/` (one repo per file), while cross-repo/service flows stay under `logic/`.
- `entity-setup.ts` still lacks a teacher factory — the 2.1 carry-forward (extract `createTestTeacher`) stands; 2.6/2.8 suites will each need the file-local shared-PK helper until then.

## 9. Cross-File Dependencies

- **← `backend/db/schema/teachers/teacher.ts`** (read-only): table `teacher`, shared PK → `users.id` (cascade), nullable `is_approved` — the projection's `boolean | null` and the applicant→`null` behavior derive from it.
- **← `backend/db/repo/teachers/applicant.repository.ts` / AGENTS.md**: namespace + JSDoc + import conventions mirrored; the barrel line is additive and alphabetical.
- **→ Task 2.8 (`SessionLifecycleService.createSession` phase 1):** the four-phase tx's FIRST statement is this lock; §8 documents the exact branch mapping. Phase-4's fused complete-time certification check (2.6's `completeSessionOnce` EXISTS clause) is a separate mechanism — this lock covers the create path only.
- **→ Test layer:** `backend/db/test/repo/teachers/teacher.repository.test.ts` is the enforcement suite; future edits to the repo method (new columns in the projection, optional-tx drift, prepared statements) fail the Tier-4 static pins; the concurrency recipe is the reusable Tier-3 pattern.
- **↔ Parallel task 2.4:** no shared files; both tasks' project-wide gates (tsgo, sub-loop, test runs) verified green simultaneously.

## 10. Verdict

Task 2.5 **COMPLETE**: NEW `TeacherRepository` namespace (one REQUIRED-tx locking read, minimal projection, honest nulls) + additive barrel line + 14-test 4-tier suite with the lock OBSERVABLY proven under concurrent transactions (blocked-probe → serialized post-commit read → rollback release). QL exit 0 on all three files; TE 14/0 green with zero DB leakage; SEC/SR/IV tables green; `bun tsgo` exit 0; no commits made.
