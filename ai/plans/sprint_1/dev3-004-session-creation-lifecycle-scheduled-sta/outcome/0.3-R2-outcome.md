# 0.3-R2 Outcome — Plan-Review Gate Round 2 (Verification of B1–B4 Fixes)

> Task: verification round after the `0.3-fix` amendments. Method: re-read every amended section
> in the actual files, re-verify the repo ground truth the amendments rely on (read-only), and run a
> grep-level consistency sweep across `specs.md` / `plan.md` / `tasks.md` / `deferred-items.md` for
> regressions or NEW contradictions introduced by the edits. Round-1 advisories A1–A10 were NOT
> re-litigated (non-blocking, deliberately untouched by the fix mandate).
> **No source files modified.** Only this outcome file + worklog written.

---

## 1. Reviewed Material

| Item | State |
|---|---|
| `outcome/0.3-outcome.md` (round 1) | 4 BLOCKING (B1–B4) + 10 advisory; GATE: FAIL |
| `outcome/0.3-fix-outcome.md` (fix report) | claimed all 4 blockers applied |
| `specs.md`, `plan.md`, `tasks.md`, `deferred-items.md` (working tree) | re-read at the amended sections IN FULL |
| `git diff b0ca09e -- <plan dir>` | used as an independent change ledger — confirms the diff contains EXACTLY the B1–B4 amendments + checkbox sync (0.1/0.2 → `[x]`, 0.3 left `[ ]`) + the D7 ledger row; zero unrelated edits; zero source-file edits |
| Repo ground truth re-spot-checked | `backend/db/schema/users/users.ts:23-29` (`isDeleted`/`suspended`/`isBlocked` columns — exactly the field names task 2.8 records), `backend/db/repo/users/user.repository.ts:74-94` (`findById` materializes all three governance fields on `UserSelectType` in both tx and raw-SQL paths), `backend/services/auth/auth.service.ts:72-83` (`assertUserActive`: `isDeleted || isBlocked || suspended` → `ForbiddenError` — the predicate task 2.8 mirrors), `backend/graphql/pothos/builder.ts:127` (`role: (roles) => roles.includes(ctx.role)` — membership-only; the B3 ruling's basis) — ALL CONFIRMED |

- Environment note (same as rounds 0.1/0.3, NOT a plan finding): HEAD is on `main` @ `b0ca09e`;
  `feat/dev3-004-session-creation-lifecycle-scheduled-sta` points at the IDENTICAL commit, so the
  review is unaffected. Orchestrator must still re-checkout the feature branch before Phase 1 edits.
  Working tree delta = the 4 plan files (amendments) + untracked `outcome/` — nothing else.

---

## 2. Per-Blocker Verdicts

### B1 — i18n errors key registry → **PASS**

Verified in the actual files:

- `tasks.md` 1.5(a): lists EXACTLY the SEVEN REQ-051 keys verbatim — `sessionNotFound`,
  `sessionInvalidTransition`, `teacherNotCertified`, `teacherNotFound`, `insufficientBalance`,
  `idempotencyKeyRequired`, `invalidSessionIntent` — with `TEACHER_NOT_FOUND` mapped onto the NEW
  `teacherNotFound` key (explicitly "NOT the existing generic `notFound`"); generic-key reuse
  guidance retained; dated ruling note present.
- `specs.md` REQ-051 (the "EXACTLY" authority, unchanged): the same 7-key list — now matched by
  both consuming docs. Three-way contradiction eliminated (all three docs now name the same
  registry).
- `plan.md` §2.7(a): heading is "new FLAT domain-prefixed keys (REQ-051)" (no `sessions` grouping
  language); keys row uses REQ-051's exact spellings; ruling note declares the 7-key list the
  single source of truth and the five `session*` renames DROPPED; reuse paragraph clarifies
  `notFound` keeps its existing generic consumers but is no longer the `TEACHER_NOT_FOUND` target,
  and resolves the "FLAT domain-prefixed" ambiguity (flat camelCase, no forced `session` prefix,
  no nested grouping) — exactly what round-1 B1 asked for.
- `plan.md` §4.1 createSession step 1: `NotFoundError("TEACHER", t.teacherNotFound)` — the last
  live `session*` rename usage is gone.
- Grep sweep: `sessionTeacherNotCertified|sessionTeacherNotFound|sessionInsufficientBalance|
  sessionIdempotencyKeyRequired|sessionInvalidIntent` occur ONLY inside the ruling note that
  documents their removal (plan §2.7(a)) and in the two outcome history files. Zero live usages.
  No normative text anywhere still maps `TEACHER_NOT_FOUND` → generic `notFound`.

### B2 — INSUFFICIENT_BALANCE class/status → **PASS**

Verified in the actual files:

- `specs.md` REQ-050 row (unchanged, spec authority): `ValidationError` custom code → `INSUFFICIENT_BALANCE` → 422; REQ-012 also says "422-class". REQ-052's `ConflictError` extension mandate is untouched.
- `plan.md` §1.2 diagram PHASE 2 miss row: `ValidationError(INSUFFICIENT_BALANCE) → ROLLBACK`
  (amended from ConflictError) + ruling note below the diagram (422; existing `ValidationError`
  `(code, message)` ctor at `errors.ts:65-130`; ConflictError extension remains required ONLY for
  `TEACHER_NOT_CERTIFIED` / `SESSION_INVALID_TRANSITION` / `DUPLICATE_REQUEST`).
- `plan.md` §3.3 map row: `ValidationError("INSUFFICIENT_BALANCE", …)` via the EXISTING overload → **422**; the mechanism paragraph now carves the INSUFFICIENT_BALANCE row out of the "rows keep codes/statuses unchanged" sentence and carries the full ruling with an explicit "do NOT route this code through the extended ConflictError" instruction. The other three custom-409 rows keep ConflictError/409 — extension scoped to exactly the three 409 codes.
- `plan.md` §4.1 createSession step 2: names the class — `ValidationError("INSUFFICIENT_BALANCE", …)` → 422 with the ruling reference.
- `tasks.md` 2.8: error-construction prerequisite explicitly scoped — extension required ONLY for the three custom 409 codes; `INSUFFICIENT_BALANCE` is NOT one of them → ValidationError/422 with the [B2] ruling note; createSession phase (2) says the same; 2.8.TE Tier 4 adds "incl. `INSUFFICIENT_BALANCE` surfaced as `ValidationError`/422".
- Grep sweep: `ConflictError(INSUFFICIENT_BALANCE)` occurs only in the ruling note ("was amended
  from…") and outcome history. Every other `INSUFFICIENT_BALANCE` occurrence in the four files is
  class-neutral or ValidationError/422-consistent. Zero residual 409 pairing. Plan §1.2/§3.3/§4.1
  and tasks 2.8 are now mutually consistent AND consistent with REQ-050.

### B3 — createSession × teacher role scope → **PASS**

Verified in the actual files:

- `specs.md` REQ-011: the carve-out sentence is struck (~~…~~) with the dated ruling note —
  teachers unconditionally FORBIDDEN in this slice; REQ-032's static `role:[UserRole.Student]`
  scope stands (`builder.ts:127` membership-only ground truth cited); INV-TV6 honest-denial intent
  preserved (applicant AND certified teacher both get the honest role-scope FORBIDDEN; the
  student-privileges clause holds on the normal role-flip path); carve-out deferred → **D7**.
- `specs.md` REQ-064: certified-teacher `createSession` cell → `FORBIDDEN` (role — Ruling
  2026-08-30: unconditional; students-row carve-out deferred → D7); applicant cell → `FORBIDDEN`
  (role — honest denial, INV-TV6); ruling paragraph after the matrix states both teacher rows are
  unconditional and that REQ-074/task 5.2 assert these cells. §2.9 Actor Table "Certified Teacher /
  CANNOT" cell updated to "create — unconditionally `FORBIDDEN` in this slice (Ruling 2026-08-30 →
  D7)" — the residual carve-out reference that would have re-triggered B3 was caught and fixed.
- `specs.md` REQ-032: UNCHANGED (diff shows no edit) — the ruling keeps the static scope and
  defers the carve-out, exactly the round-1 recommended option.
- `plan.md` §3.4: certified-teacher cell is plain `FORBIDDEN` (asterisk removed — no `FORBIDDEN\*`
  anywhere); the old `\*` footnote (which re-stated the MAY carve-out) is REPLACED by the combined
  Rulings paragraph — ruling (1) covers createSession × teacher unconditional FORBIDDEN → D7,
  ruling (2) covers the REQ-023 governance re-check (see B4); roleDashboardPath/redirect guidance
  retained.
- `tasks.md` 3.3: new "Teacher denial on `createSession`" bullet (static scope stands EXACTLY
  `$all{authenticated, role:[Student]}`; certified OR applicant → unconditional FORBIDDEN; → D7);
  3.3.TE now asserts "teacher-role callers (certified + applicant) → `FORBIDDEN` on
  `createSession`"; task 5.2's "Ruling 2026-08-30 cells (assert explicitly)" bullet asserts both
  teacher cells — the previously-unpassable every-cell assertion (REQ-074/5.2) is now passable.
- `deferred-items.md`: row **D7** present (⏸ Forward, owner "Future ticket (owner TBD)", source
  "0.3-fix ruling (B3, 2026-08-30)", notes record the dedicated-custom-authScope + REQ-032
  amendment path as the future resolution and the in-slice honest-denial preservation) + ruling
  addendum note. Round-1 directive "deferred-items.md has D7 forward row" — satisfied.
- Journeys cross-checked for consistency: J1/J2 (tasks 2.2/2.3) have NO teacher-creates leg
  (teachers only start/complete/cancel/observe), so no journey contradicts the unconditional ban;
  J2 leg 6 (admin/parent denials) is unaffected.

### B4 — REQ-023 governance re-check → **PASS**

Verified in the actual files:

- `plan.md` §6.3: the "MAY add … (explicit decision point)" language is GONE; replaced by the
  dated ruling — REQ-023's SHALL is HONORED with a bounded scope: `SessionLifecycleService`
  re-checks the ACTING user's governance status (deleted/blocked/suspended per REQ-023's own
  wording) on `createSession` (student), `startSession` (teacher) and `completeSession` (teacher)
  ONLY; governed → `FORBIDDEN`; **`cancelSession` EXPLICITLY EXEMPT** (governed student may still
  cancel in-flight — REQ-023's no-punishment clause / cancel-must-still-work tension preserved);
  no enforcement claim about the context boundary; INV-U5 sentence retained.
- `plan.md` §3.4 combined Rulings paragraph carries the identical decision (ruling 2) — the two
  plan sections agree.
- `tasks.md` 2.8: new **Governance re-check** bullet implements the SHALL with the exact bounded
  scope + cancel exemption, and pins REAL field names with repo evidence — `isDeleted`
  (`is_deleted`), `isBlocked` (`is_blocked`), `suspended` (`suspended`) at
  `backend/db/schema/users/users.ts:23-29`; `UserRepository.findById`
  (`backend/db/repo/users/user.repository.ts:74`) already materializes all three; the login
  boundary predicate `isDeleted || isBlocked || suspended` (`auth.service.ts:72-81`) is the
  pattern to mirror; read-only latitude granted to the implementer. All three citations re-verified
  against the working tree — accurate.
- `tasks.md` 2.8.TE: governance re-check branches added (governed acting user → FORBIDDEN on
  create/start/complete; cancel exempt).
- `tasks.md` 3.2.SEC: the conditional "if plan.md adds one" wording is GONE; now asserts the
  DECIDED re-check with the same bounded scope + exemption, and correctly scopes query resolvers
  as carrying no re-check (mutation cells implemented in 2.8, asserted in 3.3/5.2).
- `tasks.md` 5.2: B4 ruling cells asserted — governed acting user on create/start/complete →
  `FORBIDDEN`, and the SAME governed participant's `cancelSession` (own in-flight) → still
  succeeds.
- `specs.md` REQ-023 (unchanged SHALL) is now covered end-to-end: plan decides (§3.4 + §6.3),
  task 2.8 implements, 2.8.TE/3.2.SEC/5.2 verify, and specs' own traceability row (REQ-023 →
  "boundary denial + service-layer re-check", §4 line 272) matches. The internal
  cancel-must-still-work tension is resolved by the exemption rather than by amending REQ-023.

**Blocking verdict: 4/4 PASS.**

---

## 3. Regression Sweep (grep-level, all four files)

Swept dimensions: i18n key names · error classes/codes/HTTP statuses · role-matrix cells ·
deferred-item letters D1–D7 · REQ references used in amended text · stale MAY/conditional
wording · REQ-083 ledger gate.

| # | Location | Observation | Severity |
|---|---|---|---|
| S1 | `plan.md` §2.1 governance row (existing-schema table, ~line 146) | Row still says "governance columns are never read here". After B4, `SessionLifecycleService` DOES read `isDeleted`/`isBlocked`/`suspended` (service-layer re-check). The row's other claims (login/SSR fail-closed; context boundary carries no filter) remain true, and the normative texts (§3.4 rulings, §6.3, task 2.8) are consistent — this is a stale descriptive phrase in a baseline-verification table, not a normative contradiction. | Non-blocking (introduced-by-amendment staleness; recommend a one-phrase touch-up in the orchestrator's advisory batch: "…never read here except the REQ-023 service-layer re-check (§6.3 ruling)") |
| S2 | `specs.md` §4 traceability row REQ-002/REQ-051 (~line 260) | Descriptive phrase "new flat `session*` keys in the errors triple" survives — a residual of the dropped `session*` rename vocabulary (only 2 of the 7 keys are session-prefixed). Pre-existing (not introduced by the fix); non-normative — REQ-051's exact list governs, and plan §2.7(a)'s ruling + task 1.5(a) are aligned. No task consumes this row prescriptively. | Non-blocking (batch touch-up: "the seven REQ-051 flat keys") |
| S3 | `tasks.md` 2.M ("no new ❌/⚠️ beyond D1–D5"), 6.1 ("= EXACTLY the pre-seeded D1–D5"), 7.4 ("only D1–D5 remain ⚠️-free forward items") | Ledger enumerations now under-inclusive after D6 (✅, from 0.1) and D7 (⏸, from 0.3-fix) joined the ledger. Mechanically harmless: the REQ-083 gate is the glyph grep `grep -c "❌\|⚠️"` and neither ✅ nor ⏸ matches it — gate stays 0 (same precedent as D6's addition; the ledger's own ruling addendum documents this). The prose just doesn't mention the two extra rows. | Non-blocking (batch touch-up: "beyond D1–D5 + the traceability rows D6/D7") |
| S4 | Deferred-item letters vs plan §1.5 Key-Design-Decisions table | Ledger **D7** (carve-out) shares its letter with plan decision-table **D7** (held_balance_lane varchar). Different namespaces; every normative deferral reference is qualified ("deferred-items **D7**" / "deferred → D7" in deferral context), and the same overlap already existed for D2/D3/D5 pre-fix. | Non-blocking (awareness note only) |
| S5 | `plan.md` §1.2 diagram line (~47) | Amended miss row ends `→ ROLLBACK│` with the box border one column out of alignment vs neighboring lines (the fix outcome disclosed a non-atomic MultiEdit hiccup here and re-aligned; one char of padding remains off). Pure ASCII-art cosmetics; zero semantic content. | Non-blocking (cosmetic) |
| S6 | `deferred-items.md` literal REQ-083 grep | `grep -c "❌\|⚠️"` on the file returns 2 — both matches are the Status-Values LEGEND lines (⚠️ Partial / ❌ Blocked), carried verbatim from the original template since 0.1; item statuses contribute 0 (D1–D5/D7 ⏸, D6 ✅). Documented in the ledger's own grep note + 0.1 outcome; unchanged by the fix. | Non-blocking (pre-existing, documented) |

Sweeps that came back CLEAN:

- Key names: the 7 REQ-051 keys spelled identically in specs/plan/tasks; zero live `session*`-renamed
  key usages (only the ruling note + history); `t.teacherNotFound` in plan §4.1 matches the new key.
- Error classes/codes: `INSUFFICIENT_BALANCE` ↔ ValidationError/422 everywhere; ConflictError/409
  scoped to exactly TEACHER_NOT_CERTIFIED / SESSION_INVALID_TRANSITION / DUPLICATE_REQUEST;
  `NotFoundError("TEACHER"|"SESSION", …)` entity-name discipline intact; no `FORBIDDEN\*` remnants.
- Role matrices: specs REQ-064 ↔ plan §3.4 ↔ §2.9 Actor Table all agree on both teacher
  `createSession` cells = unconditional FORBIDDEN; REQ-032 text untouched; cancelSession remains
  dual-participant everywhere (no scope drift from the B4 exemption).
- Stale wording: "MAY add a service-layer", "explicit decision point", "if plan.md adds one",
  REQ-011 "MAY … carve-out" — all absent from normative text (survive only in outcome history).
- Deferred letters/REQ references in amended text: D7 references consistent across specs (REQ-011,
  REQ-064, §2.9), plan (§3.4), tasks (3.3, 5.2), ledger; REQ-050/REQ-051/REQ-023/REQ-032/REQ-064/
  REQ-074 citations in the ruling notes all point at real, correctly-quoted requirements.
- REQ-083 gate: ledger item statuses clean (see S6); D7 row is ⏸ with owner-reference — non-blocking.
- Scope discipline: `git diff` vs base contains ONLY the plan-directory amendments (4 files) —
  zero source files touched by the fix, matching its mandate.

**New blocking findings: none.**

---

## 4. GATE VERDICT

**GATE: PASS**

- B1 PASS — 7-key REQ-051 registry is now the single source of truth across specs/plan/tasks;
  `teacherNotFound` restored; `session*` renames dropped with no live remnants.
- B2 PASS — `INSUFFICIENT_BALANCE` = `ValidationError` → 422 consistently in plan §1.2/§3.3/§4.1
  and tasks 2.8/2.8.TE; ConflictError extension scoped to the three 409 codes only.
- B3 PASS — REQ-011 carve-out struck with ruling; REQ-064 both teacher cells FORBIDDEN
  (certified + applicant) with post-matrix ruling; §2.9 Actor Table aligned; plan §3.4 footnote
  replaced; tasks 3.3/3.3.TE/5.2 assert the cells; REQ-032 unchanged; ledger D7 forward row present.
- B4 PASS — REQ-023 SHALL honored with bounded scope (create/start/complete; cancelSession
  explicitly exempt) in plan §6.3 + §3.4; implemented in task 2.8 with verified real field names
  (`isDeleted`/`isBlocked`/`suspended`); asserted by 2.8.TE + 3.2.SEC + 5.2.
- Sweep: 6 non-blocking notes (S1–S6 above), no new blockers. S1/S3 are the only notes caused by
  the amendments themselves (stale descriptive phrases); recommend batching them with advisories
  A1–A10 into the orchestrator's plan-update pass. Advisories A1–A10 remain open/unapplied, as
  mandated — they are not gate-blocking.
- Gate preconditions satisfied: round-1 blocking findings resolved in the actual files; gate
  outcome documented; task 0.3 checkbox remains `[ ]` pending the orchestrator's gate-acceptance.
- Next step (orchestrator): accept the gate, batch the advisory + sweep touch-ups (A1–A10, S1–S5)
  into the plan-update commit, re-checkout `feat/dev3-004-session-creation-lifecycle-scheduled-sta`
  (currently HEAD sits on `main` @ b0ca09e — identical content), then dispatch Phase 1.
