# Task 1.5 Outcome — i18n Registrations (errors keys + `sessions` UI namespace)

> Task: (a) add the SEVEN REQ-051 session-lifecycle keys as FLAT camelCase members on the
> existing `ErrorsLabels` interface across the `types`/`en`/`ar` errors triple; (b) register the
> NEW `sessions` UI namespace end-to-end per the `shared/AGENTS.md` Namespace Registration
> checklist, mirroring the `applicant` namespace wiring (the most recent full namespace precedent —
> `ApplicantLabels` → `en`/`ar` leaves → `Translations` entry → bundle composition → `defineNamespace`
> handle → registry exposure). Scope boundary held: ONLY `shared/locale/**` files (+ the sanctioned
> colocated-convention test file). Nothing else touched.

---

## 1. Files Created / Modified

| File | Action | Content |
|---|---|---|
| `shared/locale/types/errors/index.ts` | UPDATE | 7 new FLAT readonly string members appended to `ErrorsLabels` (NO nested group, NO forced prefix): `sessionNotFound`, `sessionInvalidTransition`, `teacherNotCertified`, `teacherNotFound`, `insufficientBalance`, `idempotencyKeyRequired`, `invalidSessionIntent` — each with one-line domain JSDoc. `teacherNotFound` is a DEDICATED key (generic `notFound` keeps its existing consumers; it is NOT the `TEACHER_NOT_FOUND` mapping target). |
| `shared/locale/en/errors/index.ts` | UPDATE | English values for the 7 keys — short, technical, generic-state only. |
| `shared/locale/ar/errors/index.ts` | UPDATE | Arabic values — natural MSA mirroring the neighboring `applicantNotFound`/`trialAlreadyGranted` phrasing family. |
| `shared/locale/types/sessions/index.ts` | CREATE | `SessionsLabels` interface — 30 keys per plan §2.7(b) with per-key domain JSDoc; header documents the `useAppTranslation(Sessions)` / `getTranslations(locale)` consumption and the statusDisputed vocabulary-stability pin. |
| `shared/locale/en/sessions/index.ts` | CREATE | `sessionsEn: SessionsLabels` — 30 English values. |
| `shared/locale/ar/sessions/index.ts` | CREATE | `sessionsAr: SessionsLabels` — 30 Arabic values. |
| `shared/locale/types/message.ts` | UPDATE | `import type { SessionsLabels }` + `sessionsTranslations: SessionsLabels;` appended to the top-level `Translations` interface (the checklist's "MessageSchema" export point; on this branch that surface IS `Translations`). |
| `shared/locale/en/messages.ts` | UPDATE | Import `sessionsEn` + `sessionsTranslations: sessionsEn` on `enMessages`. |
| `shared/locale/ar/messages.ts` | UPDATE | Import `sessionsAr` + `sessionsTranslations: sessionsAr` on `arMessages`. |
| `shared/locale/namespaces/sessions/sessions.namespace.ts` | CREATE | `export const Sessions = defineNamespace<SessionsLabels>("sessions.sessions", translations => translations.sessionsTranslations);` — mirrors the `Applicant`/`Errors` handles (id convention `<ns>.<ns>`, getter resolves the bundle slice). |
| `shared/locale/namespaces/sessions/index.ts` | CREATE | Barrel: `export * from "./sessions.namespace";`. |
| `shared/locale/namespaces/index.ts` | UPDATE | Import + `export * from "./sessions";` (slot between `recitation` and `translation`) + `Sessions` added to the `namespaces` const. |
| `shared/locale/types/index.ts` | UPDATE | Public types barrel: `export * from "./sessions";` appended (alphabetical; per the Public import contract "add new canonical exports there when modules land"). |
| `shared/locale/sessions-namespace.parity.test.ts` | CREATE (sanctioned test, repo convention: `shared/locale/*.test.ts` sibling suites) | 20 tests / 285 expect() — Tier 1 + Tier 3 + registry/wiring/arabic-script belts. |

## 2. Exact key registries as implemented

### 2.1 `errors` namespace — the SEVEN new flat keys (REQ-051 verbatim)

| Key | en | ar |
|---|---|---|
| `sessionNotFound` | "The requested session was not found." | "لم يتم العثور على الجلسة المطلوبة." |
| `sessionInvalidTransition` | "This action is not allowed for the session in its current state." | "لا يمكن تنفيذ هذا الإجراء على الجلسة في حالتها الحالية." |
| `teacherNotCertified` | "The selected teacher is not certified for this action." | "المعلم المحدد غير معتمد حالياً." |
| `teacherNotFound` | "The selected teacher was not found." | "لم يتم العثور على المعلم المحدد." |
| `insufficientBalance` | "Your balance is insufficient for this request." | "رصيدك غير كافٍ لإتمام هذا الطلب." |
| `idempotencyKeyRequired` | "A request idempotency key is required. Please retry the request." | "مفتاح منع تكرار الطلب مفقود. يرجى إعادة المحاولة." |
| `invalidSessionIntent` | "The requested session intent is not valid for this request." | "غرض الجلسة المطلوب غير صالح." |

REUSED existing keys (untouched): `duplicateRequest`, `validation`, `forbidden`, `unauthorized`, `notFound`, `internalServerError` (+ the rest of the existing registry). No near-duplicates minted; `teacherNotFound` ≠ `notFound` copy (test-pinned).

### 2.2 NEW `sessions` namespace — 30 keys (plan §2.7(b), ALL keys it lists)

Page chrome: `studentPageTitle`, `teacherPageTitle`, `statusFilterAll`, column labels `status`, `intent`, `fee`, `deadline`, `createdAt`, empty states `studentEmptyTitle`, `studentEmptyBody`, `teacherEmptyTitle`, `teacherEmptyBody`.
Status chips (one per `SessionStatus` member): `statusScheduled`, `statusStarted`, `statusCompleted`, `statusCancelled`, `statusDisputed` (translated though unreachable today — vocabulary-stability pin).
Action copy: `startSession`, `completeSession`, `cancelSession`, `cancelConfirmTitle`, `cancelConfirmBody`, `cancelReasonLabel`, `cancelReasonPlaceholder`.
Feedback: `sessionStartedNotice`, `sessionCompletedNotice`, `sessionCancelledNotice`, `holdReleasedNotice`, `duplicateBookingInfo` (success-equivalent info tone), `genericError` (generic error-state copy).

## 3. Verification Results

### 3.1 Quality Loop (`bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates`)

| File | Result |
|---|---|
| `shared/locale/types/errors/index.ts` | ✅ exit 0 (first pass) — tsgo ✅ oxlint ✅ biome:check ✅ lint:type-aware ✅ check:duplicates ✅ |
| `shared/locale/en/errors/index.ts` | ✅ exit 0 (first pass) |
| `shared/locale/ar/errors/index.ts` | ✅ exit 0 (first pass) |
| `shared/locale/types/sessions/index.ts` | ✅ exit 0 (first pass) |
| `shared/locale/en/sessions/index.ts` | ✅ exit 0 (first pass) |
| `shared/locale/ar/sessions/index.ts` | ✅ exit 0 (first pass) |
| `shared/locale/types/message.ts` | ✅ exit 0 (first pass) |
| `shared/locale/en/messages.ts` | ✅ exit 0 (first pass) |
| `shared/locale/ar/messages.ts` | ✅ exit 0 (first pass) |
| `shared/locale/namespaces/index.ts` | ✅ exit 0 (first pass) |
| `shared/locale/types/index.ts` | ✅ exit 0 (first pass) |
| `shared/locale/namespaces/sessions/sessions.namespace.ts` | ✅ exit 0 (first pass) |
| `shared/locale/namespaces/sessions/index.ts` | ✅ exit 0 (first pass) |
| `shared/locale/sessions-namespace.parity.test.ts` | ✅ exit 0 after ONE fix cycle: oxlint `typescript(no-unnecessary-type-assertion)` ×2 — the typed `translations.errorsTranslations[key]` reads were already `string`, so the `as string` casts were removed (root-cause fix, no disable comments). Re-run: all checks ✅. |

### 3.2 Compile gate

`bun tsgo` (full repo) → **exit 0** after all edits. `Translations` parity holds: any missing `sessionsTranslations` leaf key or errors key would fail type-check — none.

### 3.3 Test Engineering (`bun run test/scripts/run-test.ts shared/locale/sessions-namespace.parity.test.ts`)

**20 pass / 0 fail / 285 expect() / exit 0** (~72ms, pure unit tier — no DB, no network, no `.env.test`).

- **Tier 1 — parity (both surfaces):**
  - `errors`: each of the 7 mandated keys resolves as a non-empty string in BOTH `errorsAr`/`errorsEn` AND is present on both compile-typed maps (`Object.hasOwn`); `teacherNotFound` copy is pinned DIFFERENT from generic `notFound` in both locales (dedicated-key ruling); ICU placeholder-name sets agree ar/en for EVERY errors key (no locale-local drift).
  - `sessions`: identical sorted key sets ar/en (runtime belt under the compile gate); every one of the 30 mandated registry keys present on BOTH maps; every value on BOTH maps non-empty (zero dead keys, symmetric sweep); ICU placeholder-name sets agree ar/en per key.
- **Registry + bundle wiring:** `namespaces.Sessions` exposed with id `"sessions.sessions"` (`<ns>.<ns>` convention); `Sessions.getLabels(enMessages)`/`(arMessages)` resolve the exact bundle slices; `sessionsTranslations` present on both bundles.
- **Tier 2 — Arabic natural-phrasing review (manual, recorded here):** Arabic copy written in natural MSA consistent with the neighboring entries: lookup-miss keys follow the established "لم يتم العثور على …" family (`applicantNotFound` precedent); chips use idiomatic short forms (مجدولة / جارية / مكتملة / ملغاة / قيد النزاع); dialog/notice copy uses polite phrasing + period terminal punctuation matching the en strings; balance copy mirrors the existing "رصيدك …" voice. Orthographic note: the repo's existing `ar/errors` file itself mixes tanwīn spellings ("جداً" in `rateLimitExceeded` vs "مسبقًا" in `trialAlreadyGranted`); the new copy uses the widely-accepted alef-form ("مسبقاً" in `duplicateBookingInfo`), consistent with that existing coexistence. A runtime belt also asserts sampled `ar` values actually contain Arabic script (guards an English paste into the `ar` leaf).
- **Tier 3 — synchronous `getTranslations(locale)` resolution:** sampled keys (`errors`: `sessionNotFound`, `teacherNotFound`, `insufficientBalance`; `sessions`: `studentPageTitle`, `statusScheduled`, `statusDisputed`, `duplicateBookingInfo`, `genericError`) resolve synchronously in BOTH locales through the pure in-memory `getTranslations` (the `readTranslation`/`translation-cache-store` helper documented in `test/ui/components/translation-preload.ts` does NOT exist on this branch — the sync static-import system needs no preloading; that preload file warms handles eagerly, which the new handle supports identically). Unknown-locale fallback resolves the default bundle without throwing.
- Regression: the sibling suites `shared/locale/errors-namespace.parity.test.ts` (8 pass / 163 expect) and `shared/locale/applicant-namespace.parity.test.ts` (11 pass / 103 expect) re-run green after the `errors` registry change.

### 3.4 Test-convention provenance

Suite colocated at `shared/locale/sessions-namespace.parity.test.ts` mirroring the two existing sibling locale suites (`errors-namespace.parity.test.ts` runtime-parity structure; `applicant-namespace.parity.test.ts` new-errors-keys + registry-wiring structure), `bun:test`, `@/` deep imports, mandated runner.

## 4. SEC — Security & Tenancy Audit

- ✅ **Generic-state copy only:** the 7 error strings and all 30 sessions strings describe STATE, never persons — no other-party identity, no names, no role-identifying phrasing beyond generic teacher/student role nouns that already pervade the registry (e.g. `applicantNotFound`).
- ✅ **No lane values, no governance flags:** grep over all new copy for `trial|hifz|tajweed|isApproved|isBlocked|suspended|isDeleted|balance_*` → zero matches in the NEW keys (the only hits in the touched files are PRE-EXISTING keys `accountSuspended`/`trialAlreadyGranted`, untouched). The `intent` column label renders "Type"/"النوع" — the intent VALUES themselves are never baked into copy.
- ✅ **No secrets/oracles/logging:** zero `console.*`, zero `process.env`, zero serialization surface in every touched file (grep-verified).
- ✅ **No behavioral surface added:** locale files are pure data + one pure handle; no GraphQL/HTTP surface, no tenancy decision, no input path.

## 5. SR — Semantic Review Checklist

- ✅ **Handle-const consumption:** the `Sessions` handle is a `defineNamespace` const with getter `translations.sessionsTranslations` — client components consume `useAppTranslation(Sessions)` with property access; Server Components resolve via `getTranslations(locale)` + property access (the in-repo system is synchronous); NO string-literal namespace introduced anywhere (grep over `shared/locale` non-test files: zero `useAppTranslation("…")`).
- ✅ **No duplicate keys:** compile gate (TS flags duplicate object-literal keys) + parity tests pin the exact registry; the `errors` additions introduce no near-duplicate of any existing key.
- ✅ **Zero dead exports / intended-forward wiring:** every new export is exercised — keys and handle at runtime by the new suite, and the wiring (barrels, bundles, `Translations` entry) is the sanctioned consumption surface for the Phase 2/3 services/resolvers (`ctx.t("errors")` → new codes; `getServerTranslations(locale)` full tree) and Phase 4 UI (`useAppTranslation(Sessions)`).
- ✅ **Clean comments (ZERO plan-artifact references):** grep `REQ-|Task [0-9]|Phase [0-9]|DEV3|plan\.md|tasks\.md|specs\.md|B\.[0-9]` over every created/modified file → 0 matches; all JSDoc describes domain behavior only.
- ✅ **Scope boundary respected:** `git status` delta = 8 modified + 5 untracked paths, ALL under `shared/locale/**` (+ the sanctioned test file). Zero backend/frontend/app/config files touched.

## 6. IV — Instruction Verification (`shared/AGENTS.md` Namespace Registration checklist, item by item)

| Checklist item | Verdict |
|---|---|
| 1. Add interface to `shared/locale/types/<namespace>/index.ts` | ✅ `SessionsLabels` in `shared/locale/types/sessions/index.ts` (canonical label types live in `shared/locale/types/**`; view layer may `Pick`/`Omit` later per the View-layer label types rule — nothing duplicated). |
| 2. Add implementations to `shared/locale/ar/<namespace>/index.ts` and `shared/locale/en/<namespace>/index.ts` | ✅ `sessionsAr` / `sessionsEn`, both typed `SessionsLabels` (compile parity on both leaves + runtime belt suite). |
| 3. Export in `shared/locale/types/message.ts` (add to `MessageSchema`) | ✅ `sessionsTranslations: SessionsLabels;` on the top-level `Translations` interface in `shared/locale/types/message.ts` (on this branch that interface IS the schema surface; both `arMessages`/`enMessages` are typed against it, so a missing leaf key = `tsgo` failure). |
| 4. Add path mapping in `shared/locale/serverLegacy.ts` (`namespacePaths` map) | ➖ **N/A — file does not exist on this branch** (verified: no `shared/locale/serverLegacy.ts`, no `namespacePaths` map anywhere in `shared/locale/`). The live analog of "namespace-path registration" on this branch is the bundle composition (`ar/messages.ts` + `en/messages.ts`) + the `namespaces` registry — both updated. |
| 5. If used in layout SSR, add to `LocaleProvider` translations in `app/[locale]/layout.tsx` | ➖ **N/A — no `app/[locale]/` directory exists on this branch** (verified: `app/` has `(auth)`, `(dashboard)`, `api`, layout.tsx, page.tsx; no LocaleProvider SSR registration site), and this task touches no layout. |
| Public import contract (`shared/locale/index.ts` + `shared/locale/types/index.ts` are the public barrel — add new canonical exports when modules land; `bun tsgo` after barrel changes) | ✅ `shared/locale/types/index.ts` gains `export * from "./sessions";`; the handle is exported through the existing `shared/locale/namespaces/index.ts` barrel (already re-exported by `shared/locale/index.ts` → `./namespaces`); `bun tsgo` re-run after barrel changes → exit 0. (Precedent note: the `applicant` namespace did not add its type module to the types barrel; this registration follows the stricter public-contract bullet instead.) |

## 7. Environment Anomalies (for the orchestrator)

1. **HEAD flip-flop persisted the whole session** — `git branch --show-current` returned `main` between nearly every command; the unconditional self-heal checkout was re-run before every step (edits to `shared/locale/**` carried over every flip because those files are byte-identical on both branch tips; the new `sessions/**` files and the test are untracked and immune). All writes were verified on `feat/dev3-004-session-creation-lifecycle-scheduled-sta` immediately before/after.
2. **tasks.md checkbox ticks** (below) were applied immediately after a re-verified feat checkout on the AMENDED tasks.md (marker-verified: the seven-key 1.5(a) text confirmed present before the edit). Same reconciliation caveat as the 1.1/1.4 entries: if HEAD was forced back to `main` (pre-amendment plan text) at commit time, the orchestrator must carry the seven 1.5 ticks into the feature-branch tasks.md.
3. `outcome/1.1-outcome.md` is NOT present in the worktree or the branch tree (only 0.1/0.2/0.3/0.3-fix/0.3-R2/1.4 are) — presumably lost in an earlier flip; the 1.1 worklog entry carries its full record. Nothing in 1.5 depended on it.

## 8. Carry-Forward Knowledge (for future subtasks)

- **Error-code → key mapping (Phase 2/3 consumers):** `SESSION_NOT_FOUND → sessionNotFound`; `SESSION_INVALID_TRANSITION → sessionInvalidTransition`; `TEACHER_NOT_CERTIFIED → teacherNotCertified`; `TEACHER_NOT_FOUND → teacherNotFound` (DEDICATED — never generic `notFound`); `INSUFFICIENT_BALANCE → insufficientBalance`; missing `X-Idempotency-Key` → `idempotencyKeyRequired`; non-bookable intent → `invalidSessionIntent`; `DUPLICATE_REQUEST` stays on existing `duplicateRequest`; generic `notFound`/`forbidden`/`unauthorized`/`internalServerError` unchanged.
- **Namespace consumption:** `Sessions` handle lives at `@/shared/locale/namespaces/sessions` (also via the namespaces barrel); labels type at `@/shared/locale/types/sessions`; server surfaces resolve `getTranslations(locale).sessionsTranslations` / `getServerTranslations(locale).sessionsTranslations` by property access.
- **statusDisputed is deliberate dead vocabulary** (unreachable chip today) — do not "clean it up"; the parity suite pins its presence.
- **Test-extension pattern:** when Phase 4 renders a new sessions surface, extend the suite with emitter/consumer discovery rather than re-listing keys; the sync `getTranslations` tier needs no preload scaffolding.
- **JSDoc gotcha (carried from 1.4):** avoid `@/…` tokens inside `shared/**` comments (oxlint parses them as tags) — honored in all new files.

## 9. Cross-File Dependencies

- **→ Phase 2 services / Phase 3 resolvers:** new errors keys are the i18n targets for the session-lifecycle reject codes; `ctx.t("errors")` and `getServerTranslations(locale)` resolve them by property access (single-arg full-tree convention). Until those consumers land, the keys are wired but not runtime-exercised outside tests (intended-forward, per plan).
- **→ Phase 4 UI:** `useAppTranslation(Sessions)` + `SessionsLabels` are the sanctioned surface for the student/teacher session pages; `frontend/views/**` may slice via `Pick`/`Omit` from the canonical type (no key duplication).
- **← Precedents mirrored:** `applicant` namespace (types → en/ar → Translations → bundles → handle → registry), `errors` triple key style, sibling parity-test structure.
- **← Compile parity:** `shared/locale/types/message.ts` + both `*/messages.ts` must stay in lockstep with any future `SessionsLabels`/`ErrorsLabels` change (`bun tsgo` is the gate).
