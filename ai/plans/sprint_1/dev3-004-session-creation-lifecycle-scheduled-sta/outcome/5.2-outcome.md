# 5.2 Outcome — GraphQL Integration Matrix, Full REQ-064 Grid (consolidation re-run)

**Date:** 2026-08-31 · **Task ID:** 5.2 · Phase 5 consolidation — the REQ-064 matrix suites already existed and ran green in Phase 3/4; this task re-ran the full GraphQL tier fresh and captured the evidence.

---

## 1. Re-run results (all fresh in this session, post-fix runs)

| Suite | Result | Expects | Exit |
|---|---|---|---|
| `backend/graphql/test/session-lifecycle-mutations.test.ts` (REQ-064 mutation grid) | **35 pass / 0 fail** | 102 expect() | 0 |
| `frontend/graphql/test/scheduling/session-lifecycle-queries.test.ts` (REQ-064 query grid) | **14 pass / 0 fail** | 53 expect() | 0 |
| `backend/graphql/test/session-sdl.test.ts` (SDL snapshot parity) | **10 pass / 0 fail** | 45 expect() | 0 |
| `backend/graphql/test/schema-surface.test.ts` (surface freeze) | **12 pass / 0 fail** | 56 expect() | 0 |
| `backend/lib/gateway/public-operations.test.ts` (backend allowlist) | **26 pass / 0 fail** | 52 expect() | 0 |
| `frontend/graphql/test/gateway/allowlist-coverage.test.ts` (frontend allowlist) | **8 pass / 0 fail** | 42 expect() | 0 |
| **Total** | **105 pass / 0 fail** | 350 | — |

Grid coverage re-proven: anonymous → `UNAUTHORIZED` on all ops; wrong-role (teacher/applicant/parent/admin, incl. **Ruling 2026-08-30 B3**: certified-teacher AND applicant `createSession` → unconditional `FORBIDDEN`); participant legs; oracle shape-constancy pairings (foreign id ≡ nonexistent id); idempotency variance cells (missing header → `VALIDATION`, replay → `DUPLICATE_REQUEST`, spent-by-foreign → oracle collapse); **B4 governance cells** (deleted/blocked/suspended governed actor → `FORBIDDEN` on create/start/complete, cancel-own-in-flight still succeeds per REQ-023); filter coherence; SDL parity; allowlists byte-stable; zero unauthenticated create surface.

## 2. In-gate code fix (ours) — sonarjs/assertions-in-tests (31 sites)

The full-repo ESLint gate (5.3) exposed **31 `sonarjs/assertions-in-tests` errors**: 29 tests in the mutations grid + 2 in the queries grid asserted ONLY through the canonical helper `expectMutationError(...)` (`test/helpers/expect-mutation-error.ts`), whose internal `expect` calls SonarJS cannot see from the test file.

**Fix (code-side, behavior-preserving, guard-preserving):** added the recognized inline assertion `expect(<result>.error).toBeDefined();` immediately before each helper call — the exact convention the queries suite's own passing tests already used. 29 + 2 sites edited; zero logic changed. Sub-loop QL (`--lifecycle duplicates`) exit 0 on both files; both suites re-run green (mutation grid expects 73 → 102, query grid 51 → 53 — +31 = exactly the added assertions).

## 3. 5.2.SR Semantic Review — PASS (embedded)

- **Fixtures via `entity-setup.ts`:** both grids build their cast through `buildSessionJourneyCast` (`test/workflows/helpers/session-cast.ts`) — the sanctioned journey-tier wrapper whose builders sit on the `backend/db/test/entity-setup.ts` factories (`session-cast.ts:4,48`); per-run `journeyPrefix` + fixture registry with `afterAll` hard-delete cleanup.
- **Translation-substring error assertions (never raw keys):** the wire/message layer is owned by `backend/graphql/test/error-contract-matrix.test.ts`, which asserts localized messages via `getTranslations` handles (`tEn.duplicateRequest` :216-217, `tAr.unauthorized` :302, `tEn.validation` :268 — substrings/verbatim, never raw keys). Run for this gate: **35 pass / 1 fail / 222 expects** — see §4: the single failure is a PRE-EXISTING out-of-slice wire-probe drift, unrelated to error-message assertions. The REQ-064 grids themselves assert `extensions.code` exactness per the ticket's ruling.

## 4. Pre-existing out-of-slice finding (REPORTED, not fixed here)

`error-contract-matrix.test.ts:514-521` — test "_health answers with intact data and NO error channel" queries bare `{ _health }`, but the committed SDL types `_health: HealthCheck!` (`frontend/graphql/generated/schema.graphql:100`; retype predates this ticket — the suite and `health.pothos.ts` are **byte-identical to b0ca09e**, `git diff b0ca09e` on them = empty). A bare selection of an object-typed field fails validation → `data` undefined → assertion at `:520` fails. The suite's own helpers already work around the retype (`test/helpers/test-lifecycle.ts:12-14` polls `{ _health { status } }`). **Owner:** DEV3-003 transport suite / Phase-6 review waves; one-line fix candidate: probe `_health { status }` and expect `{ _health: { status: "ok" } }`. Not part of the 5.2 gate list; not touched by this phase.

## 5. Environment fixes during this gate (both env-side, no code change)

1. **Stale interactive `next dev` on :3000** (PID 18077/18089, ~31 min uptime, left from the Phase-4 browser loops) triggered Next's dev-dir lock: test-tier server on :3066 aborted with "Another next dev server is already running" → first matrix run 0/1 (server never started). Fixed with the repo-blessed `bash scripts/stop-next.sh`; re-run green.
2. **Stale test-tier server survivor:** a suite `afterAll` SIGTERM kills the spawned `next dev` wrapper but can orphan the `next-server` child on :3066; the next run's hooks timed out against it (0/1 at 240 s). Fixed by killing the stale PIDs. **Reported helper gap (out-of-slice):** `test/scripts/kill-test-servers.ts` checks only `TEST_SERVER_PORT = 3099` (`test/helpers/port-helpers.ts:3`) while the GraphQL suites' server actually binds **3066** (`test/helpers/graphql-test-helpers.ts` TEST_PORT) — the repo's own kill script never cleans the port the suites use.

## 6. Verdict

**PASS** — full REQ-064 grid + oracle pairings + B3/B4 ruling cells + SDL parity + surface freeze + both allowlists green (105/0); 5.2.SR satisfied; one helper-only-assertion lint debt paid down; two env hazards and one pre-existing out-of-slice suite failure documented with owners. Phase 5.2 closed.
