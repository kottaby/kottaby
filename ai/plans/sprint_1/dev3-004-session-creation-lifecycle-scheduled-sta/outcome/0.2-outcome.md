# 0.2 Outcome — Prerequisite Verification & Dependency Guard (READ-ONLY)

> Task: verify dependency ground truth before any Phase 1+ edit. NO source files patched.
> Branch: `feat/dev3-004-session-creation-lifecycle-scheduled-sta` · Verified against working tree @ origin/main b0ca09e base.
> Verdict legend: ✅ verified · ⚠️ verified-with-nuance · ❌ missing

---

## 1. `session` table shape — ✅ VERIFIED

**File:** `backend/db/schema/classes/session.ts` (table `session`, lines 32–63)

Actual column list (in declaration order):

| Column (TS) | SQL type / builder | Constraints & defaults | Line |
|---|---|---|---|
| `id` | `integer` identity | `primaryKey().generatedAlwaysAsIdentity()` | :35 |
| `teacherId` | `integer("teacher_id")` | **NOT NULL**, FK → `teacher.id`, `onDelete: "restrict"` | :36-38 |
| `studentId` | `integer("student_id")` | **NOT NULL**, FK → `students.id`, `onDelete: "restrict"` | :39-41 |
| `status` | `sessionStatus("status")` (pgEnum) | **NOT NULL**, **default `"scheduled"`** | :42 |
| `sessionType` | `sessionType("session_type")` (pgEnum) | **NOT NULL**, **default `"student_session"`** | :43 |
| `intent` | `sessionIntent("intent")` (pgEnum) | nullable (no notNull, no default) | :44 |
| `fee` | `decimal("fee", { precision: 10, scale: 2 })` | **nullable** (plan calls it "platform-set fee" — nullability is DB-level; contract narrows non-null, see §9) | :45 |
| `feeHeld` | `boolean("fee_held")` | default `false`; **nullable at DB level** (no `.notNull()`) | :46 |
| `startedAt` | `timestamp("started_at")` | nullable | :47 |
| `endedAt` | `timestamp("ended_at")` | nullable | :48 |
| `confirmedByStudentAt` | `timestamp("confirmed_by_student_at")` | nullable | :49 |
| `confirmedByTeacherAt` | `timestamp("confirmed_by_teacher_at")` | nullable | :50 |
| `confirmationDeadline` | `timestamp("confirmation_deadline")` | nullable — **this is the "deadline timestamp"** (24h window from request) | :51 |
| `createdAt` | `timestamp("created_at")` | NOT NULL, `defaultNow()` | :52 |
| `updatedAt` | `timestamp("updated_at")` | NOT NULL, `defaultNow()` + `$onUpdate` | :53-56 |

Indexes (:58-62): `session_teacher_id_idx`, `session_student_id_idx`, `session_teacher_id_student_id_idx`.

⚠️ **Nuances to carry forward (not gaps):**
- `fee` and `feeHeld` are nullable at the DB level (`feeHeld` defaults to `false`); the SessionRequestContract narrows both (`fee` non-null, `feeHeld: true` literal) — the Phase 1/2 code must honor the contract at the service layer.
- "Deadline timestamps" = `confirmationDeadline` (+ `startedAt`/`endedAt` lifecycle stamps). No other deadline column exists.
- NOT NULL FKs confirmed with `restrict` delete on BOTH sides.

## 2. `sessionStatus` / `sessionType` / `sessionIntent` pgEnums — ✅ VERIFIED

**File:** `backend/db/schema/enums.ts` (registry header claims 15 pgEnums total):

- `sessionStatus = pgEnum("session_status", ["scheduled", "started", "completed", "cancelled", "disputed"])` — **:13** → includes **`disputed`** ✅
- `sessionType = pgEnum("session_type", ["student_session", "teacher_evaluation", "re_evaluation"])` — **:15**
- `sessionIntent = pgEnum("session_intent", ["hifz", "tajweed", "evaluation"])` — **:17** → includes **`evaluation`** ✅

Matching TS enums already exist in `backend/enum/scheduling/` (see §10 note): `session-status.enum.ts` (`SessionStatus`, incl. `Disputed` = "disputed", lines 7-13), `session-type.enum.ts` (`SessionType`, lines 6-10), `session-intent.enum.ts` (`SessionIntent`, lines 6-10), barrel `backend/enum/scheduling/index.ts` re-exporting all three.

## 3. `students` balance lanes — ✅ VERIFIED

**File:** `backend/db/schema/students/students.ts` (table `students`, lines 18-47):

- `balanceHifz: integer("balance_hifz").default(0)` — :24 (nullable, default 0)
- `balanceReviews: integer("balance_reviews").default(0)` — :25 (⚠️ FOURTH lane beyond the three named in the task — exists and is CHECK-guarded; relevant for DEV3-004 held-fee lane vocabulary `Trial|Hifz|Tajweed` — reviews lane is NOT part of the trial/hifz/tajweed escrow lanes per plan)
- `balanceTajweed: integer("balance_tajweed").default(0)` — :26 (nullable, default 0)
- `balanceTrial: integer("balance_trial").notNull().default(0)` — :27 (**NOT NULL**, default 0)

CHECK ≥ 0 constraints (:42-45):
- `students_balance_hifz_check` → `${balanceHifz} >= 0`
- `students_balance_reviews_check` → `${balanceReviews} >= 0`
- `students_balance_tajweed_check` → `${balanceTajweed} >= 0`
- `students_balance_trial_check` → `${balanceTrial} >= 0`

## 4. `teacher.isApproved` — ✅ VERIFIED

**File:** `backend/db/schema/teachers/teacher.ts` :25 — `isApproved: boolean("is_approved").default(false)` (nullable at DB level, defaults false). Siblings: `isEvaluator` :26, `isOnline` :28, `requestPreference` :30 (default `"queue"`).

## 5. Collision check — ✅ VERIFIED (no collisions)

- **`SessionRepository`**: grep over repo → matches ONLY in plan docs (`ai/plans/sprint_1/dev3-004-.../tasks.md|plan.md|specs.md|deferred-items.md`). **Zero code hits.** Absent ✅
- **`SessionLifecycleService`**: same result — plan docs only. **Zero code hits.** Absent ✅
- **Auth `SessionService`**: `backend/services/auth/` actual contents = `auth.service.ts`, `registration.service.ts`, `index.ts` (barrel), `__tests__/registration-trial-provisioning.test.ts`. **NO session.service.ts.** The only backend `SessionService` token is the future-contract doc comment at `backend/types/contracts/session-request.contract.types.ts:3` ("Governs `SessionService.createFromRequest`…"). Ground truth confirmed ✅
- **`class_instances` / `ClassSessionService` subsystem**: hits are **docs prose only** — `docs/DATABASE_MIGRATIONS.md`, `docs/SQLITE_LOCAL_DEV.md`, `backend/AGENTS.md:120`, `backend/services/AGENTS.md:106,112,125`, `backend/db/seeds/AGENTS.md:62,145`, `backend/db/schema/AGENTS.md:45`, `backend/db/repo/AGENTS.md:89,91`, `backend/db/test/AGENTS.md:58`, `backend/db/test/logic/AGENTS.md:23`, plus plan-doc/py-tooling (`scripts/run_spec_plan_kimi.py`). **No schema table, no repository, no service** — `backend/db/schema/classes/` = `index.ts, home-work.ts, lessons.ts, progress.ts, recitation.ts, reports.ts, session.ts` (no class-instance table); `backend/db/repo/` = only `admin/ students/ teachers/ users/ parents/` (no classes/session repo). Absent in code ✅

## 6. Pothos enum registration — ✅ VERIFIED; ALL THREE ARE MISSING

**File:** `backend/graphql/pothos/shared/enum.pothos.ts` (72 lines). Registered today: `UserRole` (:31), `Gender` (:36), `RegisterPublicRole` (:45), `RecitationReading` (:56), `ApplicantStatus` (:69).

**Missing (to be registered in Phase 3): ALL of — `SessionStatus`, `SessionIntent`, `SessionType`.** None appears anywhere in the file. Backing TS enums already exist under `backend/enum/scheduling/` (CRITICAL RULE satisfied — no literal arrays needed).

## 7. `DUPLICATE_REQUEST` + error ctor ground truth — ✅ VERIFIED

- **`ErrorCode` union member**: `backend/types/errors/api-error.types.ts:43` — `"DUPLICATE_REQUEST"` inside `export type ErrorCode` (:38-47, 9 canonical codes). ✅
- **HTTP status mapping**: `backend/lib/errors/error-code-taxonomy.ts:46` — `DUPLICATE_REQUEST: 409` in `ERROR_CODE_HTTP_STATUS` (:41-51); canonical self-map :73. Also asserted in `backend/graphql/test/error-contract-matrix.test.ts:476-477` and `backend/lib/errors/test/error-code-taxonomy.test.ts:35,56,224`.
- **`ConflictError` FIXED code**: `backend/lib/errors.ts:158-163` — ctor hard-codes `super("CONFLICT", message, options)`. **Cannot produce a custom 409 code.** ✅
- **`ValidationError` overloaded ctor**: `backend/lib/errors.ts:65-130` — overload set `(message)` / `(code, message, options?)` / `(message, fields)` / `(code, message, options, fields)`; the `(code, message)` form at :79 (impl :99-110) is the ONLY custom-code construction facility among the concrete classes. `DomainError` base (:19-29) takes `(code, message, options)` directly — that is the generic escape hatch, but the plan's ground truth stands: **no concrete 409 class accepts a custom code; `DUPLICATE_REQUEST` therefore requires the additive `ConflictError` extension scheduled in task 2.8** (or direct `new DomainError("DUPLICATE_REQUEST", …)`, precedent `backend/lib/api/test/api-response.test.ts:309-311`). ✅
- Re-export surface: `backend/lib/errors.ts:234,240` re-exports taxonomy + masking so `@/backend/lib/errors` is the single barrel (exact-file beats directory).

## 8. `ctx.idempotencyKey` capture — ✅ VERIFIED

**File:** `backend/graphql/gqlContextFactory.ts`
- `Context.idempotencyKey?: string | null` declared :55-71 (propagation-only, optional-in-type, always materialized at runtime).
- Capture: `createGraphQLContext` :154 → **:168 `const idempotencyKey = request.headers.get("x-idempotency-key");`** (raw passthrough, `null` when absent, no trim/coalesce) → returned in context object :216. Exactly ONE capture site.
- **`docs/IDEMPOTENCY.md` exists** — X-Idempotency-Key header (:11-17), 24h expiry + `409 Conflict`/`DUPLICATE_REQUEST` semantics (:19-29). Reinforced by `backend/graphql/AGENTS.md:10` ("captured exactly once in `createGraphQLContext`… PROPAGATION-ONLY").

## 9. `SessionRequestContract` — ✅ VERIFIED

**File:** `backend/types/contracts/session-request.contract.types.ts` (33 lines; Dev1→Dev3 contract). Structural invariants:

| Invariant | Evidence |
|---|---|
| `feeHeld` **`true` literal** | :27 `readonly feeHeld: true;` ("At request time the fee is ALWAYS held") |
| **non-null `fee`** | :25 `readonly fee: NonNullable<SessionSelectType["fee"]>;` (drizzle decimal `string`, preserved verbatim) |
| **non-null `confirmationDeadline`** | :29 `NonNullable<SessionSelectType["confirmationDeadline"]>` — NOW()+24h computed by producing service |
| **`intent` subset** | :21 `SessionIntent.Hifz \| SessionIntent.Tajweed` (evaluation sessions MUST use `EvaluationSessionContract`) |
| `sessionType` pinned | :15 `SESSION_REQUEST_SESSION_TYPE = SessionType.StudentSession` (VALUE import) + :23 literal-family type |
| **`idempotencyKey` present** | :31 `readonly idempotencyKey: string;` (repeated keys → already-created session per docs/IDEMPOTENCY.md) |
| identity/BOLA | :18-19 readonly `studentId`/`teacherId`; header :8 — studentId must equal ctx-derived student identity (runtime BOLA assert by consumer); :9 — **balance state EXCLUDED** (eligibility is consumer's concern) |

Contract barrel `backend/types/contracts/index.ts` present; sibling contracts `session-completion-escrow.contract.types.ts`, `evaluation-session.contract.types.ts`, `session-notification.contract.types.ts`, `teacher-availability.contract.types.ts` already exist.

## 10. `test/workflows/` + helpers — ✅ VERIFIED with ⚠️ SCAFFOLD GAP (expected, task 2.1)

- `test/workflows/` exists; **sole content = `AGENTS.md`** (57 lines, read in full). Header states: "This directory currently contains only this rules file. The first journey implementation scaffolds `test/workflows/helpers/` … and the first `test/workflows/<domain>/` subdirectory."
- **`test/workflows/helpers/` DOES NOT EXIST** → confirmed scaffold gap, lands in task 2.1 (helpers: cast provisioning + tracked cleanup, pure `export *` barrel). Key rules absorbed: no `runInRollback` in this layer (rule 1), committed fixtures + FK-safe cleanup (rule 2), `jrn_<domain>_<8-hex>` prefixes (rule 3), spy notification dispatch boundary (rule 5), try/catch + translated-substring assertions, never `rejects.toThrow` (rule 6), run via `bun run test/scripts/run-test.ts` (:44-56).

## 11. Ancillary artifacts — ✅ ALL VERIFIED

| Artifact | Status | Evidence |
|---|---|---|
| `docs/DATABASE_MIGRATIONS.md` (push-only rule) | ✅ exists | present at repo root docs/ |
| `docs/graphql/domain-error-extensions-code.md` | ✅ exists | referenced by `backend/lib/errors.ts:9` + `api-error.types.ts:22` |
| `shared/locale/namespaces/define-namespace.ts` | ✅ exists | `export function defineNamespace<TLabels>(` at :8 |
| `frontend/views/dashboard/navItems.ts` | ✅ exists | Student nav: `{ route: "/sessions", labelKey: "sessions", Icon: SessionsIcon }` :54; Teacher nav: same route :61. Shape = `DashboardNavItem { route, labelKey: keyof DashboardLabels, Icon }` (:28-32), role-keyed map `NAV_ITEMS_BY_ROLE` (:51-80). `/sessions` currently has NO real page → resolves to `app/(dashboard)/[feature]/page.tsx` ComingSoon catch-all (per comment :47-49) |
| `frontend/lib/auth/withPageAuth.ts` | ✅ exists | `export async function withPageAuth(options?: WithPageAuthOptions): Promise<WithPageAuthResult>` :67 (+ `WithPageAuthOptions` :34, `WithPageAuthResult` :50) |
| `frontend/lib/auth/roleDashboardRoute.ts` | ✅ exists | `roleDashboardPath` :52, `isDashboardDispatcherRedirect` :81, `resolvePostAuthTarget` :112 |
| `backend/services/auth/registration.service.ts` module-private `withTransaction` | ✅ | `async function withTransaction<T>(outerTx, fn)` :128-136 (NOT exported; savepoint-or-top-level tx helper, doc :121-127) |
| `backend/db/repo/teachers/` | ✅ | **`applicant.repository.ts` + `index.ts`** (barrel) — nothing else. No teacher.repository.ts exists |
| `frontend/graphql/sharedDocuments/` | ✅ | `AGENTS.md`, `auth/` (`auth.documents.ts`, `recitation.documents.ts`, `index.ts`), `teachers/` (`applicant.documents.ts`, `index.ts`), `documents.contract.test.ts`, `index.ts`. **No `scheduling/` yet** |
| `backend/enum/scheduling/` | ✅ exists | `index.ts` barrel (`export *` × 3) + `session-status.enum.ts`, `session-type.enum.ts`, `session-intent.enum.ts`. Task 1.1 will UPDATE this barrel (dir is not a scaffold gap) |
| `shared/constants/index.ts` | ✅ exists | present |
| `app/(dashboard)/` route group | ✅ | subdirs: `[feature]`, `admin`, `dashboard`, `parent`, `profile`, `student`, `teacher` + `layout.tsx`. **withPageAuth usage shape:** pages delegate to the shared wrapper — `app/(dashboard)/student/dashboard/page.tsx:7` and `app/(dashboard)/teacher/dashboard/page.tsx:7` call `createRoleDashboardPage(UserRole.X, "/x/dashboard")` from `frontend/views/dashboard/RoleDashboardPage.tsx`, which internally runs `await withPageAuth({ roles: [role], redirectTo: path })` (RoleDashboardPage.tsx:3,28-29). New `/sessions` pages should follow this same pattern |

---

## MISSING / GAP SECTION

**Required artifacts missing: NONE.** Every item 1-11 artifact verified present (no ❌ entries; nothing blocking Phase 1-3 was found, therefore nothing needs to be written to deferred-items.md by this task).

**Non-blocking observations (informational only):**
1. `test/workflows/helpers/` absent — expected scaffold gap, owned by task 2.1 (per plan; NOT a 0.2 blocker).
2. `frontend/graphql/sharedDocuments/scheduling/` absent — expected; created in Phase 3.
3. `fee`/`feeHeld` nullable at DB level — contract narrows; Phase 1.2/2.x must enforce non-null via the contract, not the schema.
4. `students.balanceReviews` is a 4th CHECK-guarded lane not named in task 0.2's list — recorded for completeness; DEV3-004's held-lane vocabulary remains Trial|Hifz|Tajweed (REQ-013a/REQ-045).
5. `backend/services/auth/` also contains `index.ts` + `__tests__/` beyond the two named services — no naming collision either way.

**Phase 3 registration queue (from §6):** `SessionStatus`, `SessionIntent`, `SessionType` → all three absent from `backend/graphql/pothos/shared/enum.pothos.ts`.
