# 7.1 Outcome — Canonical Doc: `docs/sessions/session-lifecycle.md`

**Task ID:** 7.1 · **Date:** 2026-08-31 · **Branch:** `feat/dev3-004-session-creation-lifecycle-scheduled-sta` (NO checkout/commit/push)
**Deliverable:** CREATE `docs/sessions/session-lifecycle.md` — the canonical knowledge-propagation document per REQ-080/REQ-081. Docs-only task; zero source files touched.

---

## 1. Files Created / Modified

| File | Action | Content |
|---|---|---|
| `docs/sessions/session-lifecycle.md` | CREATE (~200 lines, 12 sections) | The canonical session-lifecycle reference. |
| `ai/plans/.../outcome/7.1-outcome.md` | CREATE | This close-out record. |

## 2. Section map (task-text structure → doc sections, 1:1)

| Task-mandated element | Doc section |
|---|---|
| Why | §1 Why |
| State machine + guarded-transition pattern | §2 (states/mermaid, fused-predicate UPDATE table for `startSessionOnce`/`completeSessionOnce`/`cancelSessionOnce`, cold-probe classification rules, invariant binding table incl. INV-S1..S8 with S6/S7/S8 explicitly DEV3-005-owned, INV-B1/B4/B8, INV-W3/W4, INV-U2/U5 incl. the bounded governance re-check + cancelSession EXEMPT ruling, INV-TV1; decisions A.8/A.10/B.2/B.3/B.4/B.18/C.5 bound in §2.3) |
| Four-phase creation invariant | §3 (Phase 0 pre-DB guards + governance re-check + one-`now` deadline → Phase 1 FOR UPDATE cert lock → Phase 2 debit ladder → Phase 3 savepoint-bracketed claim → Phase 4 field-by-field insert + claim backfill; rollback-is-the-only-cleanup) |
| Hold-as-debit ruling & B.4 reconciliation (supersedes TEAM_ALLOCATION Contract-1 phrasing) | §4 (ruling, `fee_held` marker vs `held_balance_lane` provenance, supersession note, DEV1-004-era NULL-lane rows refund nothing) |
| Trial-first ladder + same-lane refund | §5 (INV-B8 ladder, `INSUFFICIENT_BALANCE`/422 rollback, frozen enum-keyed lane map, reviews-lane exclusion, fail-closed unreadable provenance, double-cancel-cannot-double-refund) |
| Idempotency claim design | §6 (claim table shape, propagation-only key, savepoint bracketing, 23505 cause-chain discipline, **replay-throw ruling**, key released on failure, 24h sweeper deferral) |
| Oracle ruling contrast-with-plans + anti-copy-paste warning | §7 (sessions sensitive ⇒ collapse foreign≡nonexistent on reads AND mutations; plans public ⇒ NOT_FOUND fine; explicit anti-copy-paste blockquote; documented teacher-target exception) |
| `is_online` deferral note (D3) | §8 (certification-only assertion; owners DEV3-008/DEV2-011; directory/booking-UI knock-on D4) |
| Battle-tested gotchas (folded in per directive) | §9 Battle-Tested Gotchas — What NOT to Repeat: (a) replay-throw not replay-return; (b) REQ-054 session-id shape guards (VALIDATION pre-DB / oracle-null read channel); (c) FORBIDDEN via the `ForbiddenError` class; (d) exhaustive Pothos enum mappers — no silent default, compile-guarded drift (never-guard throw); (e) Happy-DOM portal-input limitation (D8/D9) with real-browser verification as the compensating control; (f) `FOR UPDATE` cannot block on uncommitted INSERTs (READ COMMITTED) — concurrency tests must lock committed rows (2.5's committed-fixture/gate-holder/`Promise.race` recipe) |
| Consumer-guidance table | §10 (DEV3-005/006/011/012/013/021 + DEV2-016 — must/must-not rows mirroring the specs' binding traceability note) |
| Rollout summary | §11 (shipped surface + verification posture) |
| Related Documents | §12 (specs, decisions addendum, IDEMPOTENCY, error contract, gateway, cross-stream contracts, free-trial/applicant canonical docs, TEAM_ALLOCATION supersession note, plan of record) |

## 3. Propagation discipline

- **Propagated (permanent/global):** the guarded-transition pattern; four-phase ordering; hold-as-debit + same-lane refund; trial-first ladder; idempotency claim + replay-throw; sessions-vs-plans oracle ruling (both directions + anti-copy-paste); `is_online` deferral; the six battle-tested gotchas; invariant/decision bindings.
- **Excluded (plan-specific, never propagated):** REQ numbering as binding consumer law (cited only as provenance); task IDs/checkbox mechanics; the D7 teacher-carve-out *design* (deferred ruling recorded as a fact — the doc states the in-slice unconditional FORBIDDEN and points to the ledger, it does not prescribe the future authScope implementation); sprint-specific dates/agents; per-test file/line anchors (test-tier details stay in the outcome files).

## 4. Gates

| Gate | Result |
|---|---|
| Biome scope verification on `docs/sessions/session-lifecycle.md` | **N/A confirmed once** — biome reports "Checked 0 files … paths provided but ignored" for `.md` inputs (markdown is outside biome's lint scope; exit 0). Doc-only file, no lint surface. |
| Full-repo `bun tsgo` | **exit 0, 0 errors** (docs-only change; re-verified after all Phase-7 edits). |
| REQ-045 discipline | `backend/db/schema/**` / `backend/db/migration/**` untouched by Phase 7 (verified by git status: only docs/AGENTS.md files modified). |

## 5. Verdict

**Task 7.1 COMPLETE.** Canonical doc created with the full mandated structure, all listed invariants/decisions bound, and the six battle-tested gotchas folded in. Checkbox flipped in `tasks.md`.
