# Task 1.1 Outcome — HeldBalanceLane TS Enum + Type Guard

> Task: create the canonical held-balance-lane provenance vocabulary
> (`backend/enum/scheduling/held-balance-lane.enum.ts`: string enum `Trial`/`Hifz`/`Tajweed`
> + `isHeldBalanceLane` guard) and add it to the `backend/enum/scheduling/index.ts` barrel,
> mirroring the ApplicantStatus varchar-enum precedent (`backend/enum/teachers/applicant-status.enum.ts`).
> Scope boundary held: 2 mandated files + 1 colocated test file. Nothing else in the repo touched.
> Branch note: see "Environment anomalies" — HEAD flip-flops between `main` @ `b0ca09e` and the
> feature branch @ `d2629ab` during the session (orchestrator checkout dance); all work is
> content-based and identical on both trees for `backend/**`.

---

## 1. Files Created / Modified

| File | Action | Content |
|---|---|---|
| `backend/enum/scheduling/held-balance-lane.enum.ts` | CREATE | `export enum HeldBalanceLane { Trial = "trial", Hifz = "hifz", Tajweed = "tajweed" }` + `export function isHeldBalanceLane(value: unknown): value is HeldBalanceLane` — exact structural mirror of the ApplicantStatus precedent: domain JSDoc header (column mapping, canonical values, "NO pgEnum — sole runtime authority" statement, same-lane refund + nullable-column domain contract), then the enum, then a fail-closed guard JSDoc, then the guard body `typeof value === "string" && (Object.values(HeldBalanceLane) as string[]).includes(value)`. ZERO imports. Member string values per plan §2.4 exactly. |
| `backend/enum/scheduling/index.ts` | UPDATE (additive) | Added `export * from "./held-balance-lane.enum";` as the first line (alphabetical ordering preserved: held-balance-lane < session-intent < session-status < session-type). Barrel still contains ONLY `export *` statements with relative `./` paths. Top-level `backend/enum/index.ts` NOT touched (already re-exports `./scheduling`). |
| `backend/enum/scheduling/held-balance-lane.enum.test.ts` | CREATE (sanctioned test file) | Colocated 4-tier bun:test suite, mirroring `applicant-status.enum.test.ts` structure (deterministic LCG fuzz seed 8675309, same tier discipline). 25 tests / 331 expect() calls — all green. |

## 2. Files NOT Modified (and why)

- `backend/enum/index.ts` — top-level barrel already re-exports `./scheduling`; no new sub-directory was created (per plan §2.4 and backend/enum/AGENTS.md "Adding New Enums" step 4).
- `backend/graphql/pothos/shared/enum.pothos.ts` — deliberately NOT registered: the lane is server-internal provenance, never appears in SDL, so NO Pothos registration and NO codegen enum entry (plan §2.4; keeps `heldBalanceLane` structurally unreachable from client input).
- `backend/db/schema/enums.ts` / any schema file — NO pgEnum, NO column, NO CHECK: the schema delta (`session.held_balance_lane varchar(20) NULL`) belongs to task 1.2. REQ-045's diff discipline (`git diff backend/db/schema/** backend/db/migration/**` = empty at this point) is preserved — verified `git diff --name-only` = only the three files above.
- `backend/enum/teachers/applicant-status.enum.*` — read as precedent only; ApplicantStatus lives under `teachers/`, not `scheduling/` (the task text's "in backend/enum/scheduling/" is a minor location inaccuracy; the structure-mirror instruction was followed regardless).

## 3. Verification Results

### 3.1 Quality Loop (sub-loop `--lifecycle duplicates`)

| File | Result |
|---|---|
| `backend/enum/scheduling/held-balance-lane.enum.ts` | ✅ exit 0 — tsgo ✅ oxlint ✅ biome:check ✅ lint:type-aware ✅ check:duplicates ✅ (first pass, no fixes needed) |
| `backend/enum/scheduling/index.ts` | ✅ exit 0 — all five checks ✅ (first pass) |
| `backend/enum/scheduling/held-balance-lane.enum.test.ts` | ✅ exit 0 after ONE fix cycle: first run failed lint:type-aware with 3 × `sonarjs/no-primitive-wrappers` errors on the Tier-4 `new String(...)` boxed-string tests. Fixed within the file only: boxed values are now built with `Object(...)` (identical boxed-String shape, typeof "object", same coercion semantics) with an explanatory comment. check:duplicates notes it skips test files (outside jscpd scan scope) — informational. |

Full-repo `bun tsgo` after all edits: exit 0, zero errors (baseline delta = 0).

### 3.2 Test Engineering (run via `bun run test/scripts/run-test.ts`)

`bun run test/scripts/run-test.ts backend/enum/scheduling/held-balance-lane.enum.test.ts` →
**25 pass / 0 fail / 331 expect() calls / exit 0** (~100ms, pure unit tier — no DB, no schema imports, no `.env.test` dependency).

- **Tier 1 — branch coverage:** frozen value-set assertion (`"trial|hifz|tajweed"`), every member passes (true branch), casing variants rejected (`Trial`/`TRIAL`/`tRial`/`Hifz`/`HIFZ`/`Tajweed`/`TAJWEED`), non-member strings rejected, empty string rejected, non-string short-circuits the typeof check (left-false branch), `unknown`→`HeldBalanceLane` narrowing proven via `expect.unreachable` guard.
- **Tier 2 — boundary:** unicode/Arabic/RTL strings (القرآن، حفظ، تجويد، RTL marks, combining diacritics, emoji, Hebrew), 10k-char long strings (pure, prefixed, suffixed, repeated-member), near-miss spellings (`trials`, `hifzz`, `tajweeed`), whitespace boundaries (leading/trailing/tab/newline/NBSP), embedded control chars (NUL, CR, BOM), primitive non-strings (0, NaN, true, undefined, null).
- **Tier 3 — chaos:** 200 deterministic LCG fuzz strings all rejected without throwing; Symbols (`Symbol.for("trial")`, `Symbol(member)`, `Symbol.iterator`), objects/arrays (`{}`, `{lane: member}`, empty/member arrays, `Date`, `Map`, `Set`, RegExp), LIKE-wildcard/injection shapes (`%`, `_`, quotes, `'; DROP TABLE students; --`), functions masquerading as members — every hostile input returns `false` and never throws.
- **Tier 4 — coercion resistance:** boxed String objects (`Object("trial")` — typeof "object", never coerced true, incl. `Object("Trial")` and `Object("")`), whitespace-padded member strings never accepted, `toString` override returning a member still rejected, throwing `toString`/`valueOf` never triggered, `Object.create(null)` (plain + forged array-like), `__proto__`-bearing payload, numeric coercion traps (0, -0, NaN, Infinity, 1e21, bigint, booleans).

### 3.3 Environment constraint fixed (sandbox, not repo)

`test/scripts/run-test.ts` spawns `/home/z/.bun/bin/bun` (hard-coded, per the repo's `~/.bun/bin/bun` convention); that path did not exist in this sandbox (bun lives at `/usr/local/bin/bun`). Remedy: `mkdir -p /home/z/.bun/bin && ln -sf /usr/local/bin/bun /home/z/.bun/bin/bun` (env fix only — zero repo files touched). Carry-forward: after a sandbox restart this symlink must be recreated or run-test.ts fails with `ENOENT ... posix_spawn '/home/z/.bun/bin/bun'`.

## 4. SEC — Security & Tenancy Audit

- ✅ **Vocabulary is app-layer only:** the enum + guard are the sole runtime authority; NO pgEnum was added, NO DB CHECK added, NO index — the future `held_balance_lane` column (task 1.2) is nullable varchar(20) with app-layer guarding per the ApplicantStatus-varchar precedent. Nothing in this slice touched `backend/db/schema/**` or `backend/db/migration/**` (verified by git).
- ✅ **Never accepted from client input anywhere in this slice:** the slice adds no GraphQL surface, no resolver, no input type; the enum is server-internal provenance and is deliberately unregistered in Pothos (absent from SDL ⇒ structurally unreachable from client payloads). The guard exists so that any future value read from the varchar column or a transport payload is validated fail-closed (exact-member match; case/whitespace/type mismatches → `false`, never coercion, never throw).
- ✅ **No secrets/oracles/logging:** no `console.*`, no logger calls, no value serialization; nothing about the lane value is logged anywhere in the new code.

## 5. SR — Semantic Review Checklist

- ✅ **Enum used via VALUE import only:** the test imports `{ HeldBalanceLane, isHeldBalanceLane }` as a runtime VALUE import from the deep path; no `import type` exists in any of the three files (grep-verified). (Downstream task 1.2 will use `import type { HeldBalanceLane }` in the schema file — type-position-only usage, per plan §2.2 — that is a different file and out of this slice.)
- ✅ **Zero dead exports:** the enum file exports exactly the two mandated symbols (enum + guard), both exercised at runtime by the test; the barrel re-export exposes them for tasks 1.2/2.8 (their designated consumers). No other symbols, no unused imports anywhere.
- ✅ **No cross-layer imports:** the enum file has ZERO imports; the barrel contains only relative `export *` statements; the test imports `bun:test` + the same-layer deep path. No `@/frontend`, `@/shared`, or `@/app` imports (grep-verified).
- ✅ **Clean comments (ZERO plan-artifact references):** grep for `REQ-|Task [0-9]|Phase [0-9]|DEV3|plan\.md|tasks\.md|specs\.md` over all three files = 0 matches; comments describe domain behavior only (column provenance, same-lane refund contract, nullable lifecycle, fail-closed guard semantics). The one Tier-4 comment explains a lint-constraint workaround, not a process artifact.
- ✅ **No noisy comments / no console.*:** JSDoc density mirrors the precedent file exactly; grep for `console.|logger` = 0 matches.
- ✅ **Scope boundary respected:** `git diff --name-only` = `backend/enum/scheduling/index.ts` only; untracked new files = the enum + its test. Exactly the mandated 2 files + sanctioned test file.

## 6. IV — Instruction Verification (backend/enum/AGENTS.md, rule by rule)

| Rule | Verdict |
|---|---|
| Backend-only (no frontend imports of `@/backend/enum`; shared never imports it) | ✅ zero cross-layer imports; nothing shared/frontend-facing was created |
| Sub-directory layout: domain-grouped enums under matching subdir with own barrel | ✅ `scheduling/` (scheduling domain, alongside the three session enums) |
| Each subdir barrel re-exports every `*.enum.ts` in the subdir | ✅ barrel updated; 4/4 files re-exported |
| Top-level barrel re-exports every subdir barrel — unchanged when no new subdir | ✅ verified `./scheduling` present; no edit needed |
| Deep imports preferred over barrel | ✅ test uses deep path `@/backend/enum/scheduling/held-balance-lane.enum` |
| File naming `<entity>.enum.ts` | ✅ `held-balance-lane.enum.ts` |
| One logical domain per file; related enums co-located | ✅ single enum + its guard in one file (guard co-location matches ApplicantStatus precedent) |
| Adding-New-Enums steps 1–3 | ✅ subdir identified; new file created; subdir barrel gained `export * from "./held-balance-lane.enum";` |
| Step 4 (new subdir → top-level barrel) | N/A — no new subdir |
| Step 5 (register in Pothos IF GraphQL-exposed) | N/A-by-design — lane is server-internal provenance, never in SDL (plan §2.4) |
| Linting: no `oxlint-disable` comments; root-cause fixes | ✅ none used; the one lint failure was fixed at root (Object() boxing instead of banned primitive-wrapper constructor) |

Sub-loop rule files additionally honored (`.agents/instructions/backend.instructions.md` + `tests.instructions.md`): barrel conventions (`export *` only, relative `./` paths, no imports in barrels, ≤1 `/` per path, alphabetical order preserved); no `console.*`; no nested ternaries; `bun:test` utilities; deep `@/backend/...` import style; run via `test/scripts/run-test.ts` (not raw `bun test`).

## 7. Environment Anomalies (for the orchestrator)

1. **HEAD flip-flop:** during this session HEAD repeatedly moved between `main` @ `b0ca09e` and `feat/dev3-004-session-creation-lifecycle-scheduled-sta` @ `d2629ab` (reflog shows checkout pairs around subagent activity). At outcome-writing time HEAD was back on `main` @ `b0ca09e`; the worktree `tasks.md` was therefore the pre-amendment version (0.1/0.2 checkboxes unticked) while the feature branch carries the amended/committed version at `d2629ab`. All implementation work is unaffected (the two trees are identical for `backend/**`), but **the tasks.md checkbox sync for 1.1 (below) must be reconciled by the orchestrator against the feature-branch version of tasks.md when it next holds the checkout**, and 0.3 remains `[ ]` (gate passed at R2, checkbox owner: orchestrator).
2. `~/.bun/bin/bun` symlink created (see §3.3) — needed by `test/scripts/run-test.ts` in this sandbox.
3. Prior-session lint constraint (0.1): full-repo ESLint must run with reduced footprint; not triggered here — the sub-loop's in-process lint service ran clean on all three files.

## 8. Carry-Forward Knowledge (for future subtasks)

- **Exact file contract for task 1.2:** the schema column must be `heldBalanceLane: varchar("held_balance_lane", { length: 20 }).$type<HeldBalanceLane>()` — nullable, NO CHECK, NO index — with `import type { HeldBalanceLane } from "@/backend/enum/scheduling/held-balance-lane.enum";` (type-position-only usage; the enum's runtime consumers will VALUE-import it). `$inferSelect` will then yield `HeldBalanceLane | null` with zero casts downstream.
- **Guard semantics (fail-closed, coercion-proof):** `isHeldBalanceLane` returns `true` ONLY for the exact strings `"trial" | "hifz" | "tajweed"`; boxed Strings, whitespace-padded members, casing variants, Symbols, objects with `toString` overrides, and every non-string return `false` without throwing. Repos/services reading the varchar column should route values through this guard before trusting them.
- **`students.balanceReviews` is NOT in this vocabulary** — the reviews lane never funds held session fees; only Trial/Hifz/Tajweed participate in the debit-ladder / same-lane-refund mechanism.
- **Testing precedent for pure enum suites:** colocated `<file>.enum.test.ts` next to the enum (NOT under `test/`), `bun:test` imports, deterministic LCG fuzz (seed 8675309), 4-tier discipline; run with `bun run test/scripts/run-test.ts <path>`; jscpd skips test files in check:duplicates; sonarjs forbids `new String()` even in coercion tests — use `Object(...)` for boxed-string fixtures.
- **ApplicantStatus precedent lives at `backend/enum/teachers/`** — task text said `scheduling/`; future readers looking for the varchar-enum+guard template should use `backend/enum/teachers/applicant-status.enum.ts`.

## 9. Cross-File Dependencies

- **→ Task 1.2 (schema):** imports `HeldBalanceLane` (type-only) from `backend/enum/scheduling/held-balance-lane.enum` and relies on the barrel path being stable. Column length 20 accommodates the longest member (`tajweed`, 7 chars) with headroom.
- **→ Task 1.3 (canonical types):** `SessionSelectType.heldBalanceLane` becomes `HeldBalanceLane | null` via the schema's `$type<>()`; `SessionReturnType` re-applies the enum type per the backend type pattern.
- **→ Task 2.8 (service):** the debit ladder keys `HeldBalanceLane.Trial/Hifz/Tajweed` → `students.balance_*` columns via a frozen lane→column map; `cancelSession` refunds to `row.heldBalanceLane` (guard-validate the varchar read).
- **← Barrel:** `backend/enum/index.ts` needed NO change (pre-existing `./scheduling` re-export) — verified, not assumed.
