# 0.3 Outcome — Phase-1.5 Plan-Review Gate (Round 1)

> Task: `@plan-review` gate round 1 (single-agent round; multi-agent waves come in Phase 6).
> Method: `.agents/skills/plan-review/SKILL.md` workflow applied pragmatically to the five
> gate dimensions in task 0.3 (internal consistency / completeness / contradictions /
> feasibility / task↔REQ coverage), plus repo ground-truth spot-checks against the working tree.
> **No source files were modified.** Read-only review; only this outcome file + worklog written.

---

## 1. Reviewed Files & Versions

| File | Size | State |
|---|---|---|
| `ai/plans/sprint_1/dev3-004-session-creation-lifecycle-scheduled-sta/specs.md` | 285 lines (REQ-001..REQ-083, §2.9 REQ-J1..J6) | read IN FULL |
| `ai/plans/sprint_1/dev3-004-session-creation-lifecycle-scheduled-sta/plan.md` | 731 lines (§1–§6) | read IN FULL |
| `ai/plans/sprint_1/dev3-004-session-creation-lifecycle-scheduled-sta/tasks.md` | 436 lines (Phase 0–7, 27 tasks + 2.M) | read IN FULL |
| `worklog.md` (env-setup entry) | — | read |
| `outcome/0.2-outcome.md`, `outcome/0.1-outcome.md`, `deferred-items.md` | — | read (prior-phase corroboration) |

- Repo state at review time: HEAD `b0ca09e` (origin/main base the feature branch was cut from).
- ⚠️ **Environment observation (not a plan finding):** the working tree is currently checked out on
  `main` (clean vs origin/main except the plan's own `deferred-items.md` seeding + untracked
  `outcome/`), not on `feat/dev3-004-session-creation-lifecycle-scheduled-sta` as the worklog
  states. Content is identical to the branch base (b0ca09e), so this review is unaffected —
  but the branch must be re-checked out before any Phase 1+ file edit. Owner: orchestrator/env-setup.

---

## 2. Ground Truth Verified During Review (repo spot-checks)

These claims in plan/tasks were checked against code and hold — no findings:

- `role` authScope is a **membership-only** check on the single `ctx.role`
  (`backend/graphql/pothos/builder.ts:127` → `roles.includes(ctx.role)`); `$all` conjunction
  documented (builder.ts:29,39). → basis for BLOCKING finding B3.
- Error machinery: `ErrorCode` union = 9 canonical codes (`backend/types/errors/api-error.types.ts:38-47`);
  `DUPLICATE_REQUEST` already mapped 409 (`error-code-taxonomy.ts:46,73`); `ConflictError` ctor
  hard-codes `"CONFLICT"` (`errors.ts:158-163`); `ValidationError` has the only custom-code overload
  (`errors.ts:65-130`); `DomainError` base accepts plain `code: string` (errors.ts:21); masking passes
  DomainError codes through **verbatim** — "custom domain codes remain legal transport values"
  (`error-masking.ts:19,668-669`); `USER_NOT_FOUND`-style entity codes are an established precedent
  (`errors.ts:35`). → REQ-050's custom codes are constructible without union changes; no feasibility gap.
- `DateTime` scalar registered (`backend/graphql/pothos/shared/scalar.pothos.ts:28`, `DateTimeResolver`).
- i18n: `ErrorsLabels` is FLAT (`shared/locale/types/errors/index.ts:8`) with existing
  `duplicateRequest`/`notFound`; `defineNamespace` exists; `getServerTranslations(locale)` is one-arg
  full-tree (`shared/locale/server-graphql.ts:3`); Namespace-Registration checklist lives at
  **`shared/AGENTS.md:236`** (task 1.5(b)'s pointer is correct; `shared/locale/AGENTS.md` is 33 lines).
- Test infra exists: `runInRollback`/`expectRepoError`/`entity-setup.ts` (`backend/db/test/`),
  `test/helpers/test-lifecycle.ts` + `expect-mutation-error.ts` + `graphql-test-helpers.ts`,
  `test/ui/components/translation-preload.ts`, `test/scripts/run-test.ts`.
- Frontend seams exist: `mapGraphQLErrorByCode` (`frontend/providers/apollo/error-link.map.ts`),
  `PermissionDeniedFallback`, `RetryableNotice`, `withPageAuth`, `roleDashboardPath`,
  nav `sessions` entries (`navItems.ts:54,61`).
- Scripts exist: `generate:gqlSchema`, `codegen`, `tsgo`, `biome:check`, `scripts/health/sub-loop.ts`,
  `scripts/lint-service.ts`. `logger.logDomainError` exists (`backend/lib/logger.ts:72`).
- Task 0.2's outcome corroborates all schema/collision/contract claims (session shape, pgEnums incl.
  `disputed`/`evaluation`, lanes + CHECKs, no `SessionRepository`/`SessionLifecycleService`/
  `TeacherRepository` code symbols, all 3 session Pothos enums unregistered, ctx.idempotencyKey
  captured, `SessionRequestContract` invariants).
- Task↔REQ coverage: every REQ-001..083 (minus the B4 nuance) and REQ-J1..J6 maps to ≥1 task;
  journeys J1/J2 → tasks 2.2/2.3 with J1..J6 tags; plan §4.1 service signatures, §4.2 repo methods,
  and §5.4's **seven** GraphQL documents all exist exactly as tasks 2.8/2.6/4.1 cite them;
  test-kind inventory (repo/db · service · journey · graphql-integration · ui-component · chaos ·
  coverage gates) is derivable from the X.Y.QL/TE/SEC/SR/IV pipelines.

---

## 3. Findings

| # | Severity | Location | Description | Recommendation |
|---|---|---|---|---|
| B1 | **BLOCKING** | specs REQ-051 vs plan §2.7(a) vs tasks 1.5(a) | **Three-way contradiction on the i18n errors key registry.** REQ-051 says "EXACTLY": `sessionNotFound, sessionInvalidTransition, teacherNotCertified, teacherNotFound, insufficientBalance, idempotencyKeyRequired, invalidSessionIntent` (7 keys). Plan §2.7(a) renames 5 of them (`sessionTeacherNotCertified`, `sessionTeacherNotFound`, `sessionInsufficientBalance`, `sessionIdempotencyKeyRequired`, `sessionInvalidIntent`). Task 1.5(a) lists only 6 keys and instructs mapping `TEACHER_NOT_FOUND` onto the **existing** `notFound` key — i.e. it drops the mandated `teacherNotFound` key entirely. No single registry can satisfy all three docs; task 1.5 is unverifiable against REQ-051 as written. | Make REQ-051's exact 7-key list the single source of truth: fix task 1.5(a) (add `teacherNotFound`; drop the `notFound` reuse for `TEACHER_NOT_FOUND`) and revert plan §2.7(a)'s `session*` renames — or record an explicit amendment decision to REQ-051 before Phase 1 closes. Resolve the spec's own "FLAT domain-prefixed" phrasing ambiguity in the same pass. |
| B2 | **BLOCKING** | specs REQ-050 vs plan §1.2 (PHASE 2) & §3.3 | **`INSUFFICIENT_BALANCE` error class/status contradiction.** REQ-050's exact map: `ValidationError` custom code → **422**. Plan §1.2 diagram: `ConflictError(INSUFFICIENT_BALANCE) → ROLLBACK`; plan §3.3 row: ConflictError via the new `(code, message)` overload → **409**. Task 2.8 names the code but not the class, so it inherits the ambiguity; whichever class is implemented, one binding doc is violated (observable 409-vs-422 class difference). | Align plan §1.2/§3.3 to REQ-050 (`ValidationError("INSUFFICIENT_BALANCE", …)` via the existing overload → 422) — or amend REQ-050 with a recorded ruling if 409 is preferred. Pick one before task 2.8; record the mechanism in `outcome/2.8-outcome.md` per REQ-052. |
| B3 | **BLOCKING** | specs REQ-032 vs REQ-011/REQ-064 (+ plan §3.4 footnote, tasks 3.3 & 5.2) | **createSession role scope cannot express the INV-TV6 teacher-as-student carve-out.** REQ-032 mandates exactly `{ $all: { authenticated: true, role: [UserRole.Student] } }`; repo ground truth (`pothos/builder.ts:127`) is pure `ctx.role` membership — a `role=teacher` user fails it regardless of holding a `students` row. Yet REQ-011 says teacher-role callers MAY create as the student side when they hold a `students` row, REQ-064's certified-teacher cell says "`FORBIDDEN` (role; unless he holds a `students` row)", and plan §3.4's footnote repeats it ("a teacher without a `students` row fails the role scope" — implying one with a row passes, which the mandated scope cannot do). REQ-074/task 5.2 require asserting EVERY REQ-064 cell → that cell is unpassable under REQ-032's scope. | Record a pre-implementation ruling. Simplest: teachers are unconditionally FORBIDDEN on `createSession` in this ticket — amend the REQ-064 cell + REQ-011 "MAY" wording, note the failed-applicant conversion path in the canonical doc as future scope. Alternative: a dedicated custom authScope (e.g. `studentActor` checking the `students` row) + amend REQ-032. Update tasks 3.3/5.2 cell inventory to match. |
| B4 | **BLOCKING** | specs REQ-023 vs plan §3.4/§6.3 vs tasks 2.8/3.2.SEC | **REQ-023's service-layer governance re-check is a SHALL in specs, a "MAY" in plan, and conditional in tasks — no covering implementation step.** REQ-023: "the plan SHALL add a service-layer governance re-check on session mutations as defense-in-depth". Plan §3.4/§6.3: "this plan MAY add … (explicit decision point)" — never decided. Task 2.8's service description has no such step; task 3.2.SEC verifies it only "if plan.md adds one". Note REQ-023 is also internally tense: a blanket re-check on ALL session mutations would deny a governed student's cancellation, contradicting its "may cancel in-flight sessions (his units return)" clause. 7.1 would end up documenting a defense that doesn't exist. | Decide the scope now and schedule it: e.g. governance re-check on `createSession`/`startSession`/`completeSession` only (exclude `cancelSession` per the no-punishment clause), implemented in task 2.8 via an explicit governed-state read, with 3.2.SEC/5.2 asserting it. Or amend REQ-023 via the decisions addendum (boundary-only acknowledgment) — but it cannot remain "MAY". |
| A1 | Advisory | plan.md §6.7 (Knowledge-Propagation bullet) | Plan misdefines deferred-item **D1** as the naming decision ("codework resolved INSIDE this ticket"); specs REQ-001 + tasks 0.1 define D1 = event notifications → DEV3-010/011. Task 0.1 already reconciled at the ledger level (naming item preserved as D6; discrepancy recorded in `outcome/0.1-outcome.md`). | Correct plan §6.7's D1–D5 bullet to match REQ-001 (notifications = D1) so the plan doc and ledger agree; keep the D6 cross-reference. |
| A2 | Advisory | specs §4 traceability rows; plan §6.6 | Phantom REQ IDs: traceability cites "REQ-050..055" and "REQ-063..066" (REQ-055/REQ-066 are not defined; §2.5 ends at REQ-054, §2.6 at REQ-065); plan §6.6 cites a "REQ-069-style depth review" (REQ-069 does not exist). No task cites the phantom IDs, so no coverage is lost. | Correct the ranges to REQ-050..054 / REQ-060..065 and drop the REQ-069 mention (call it a plain depth review). |
| A3 | Advisory | plan §1.2 diagram vs §4.1 + task 2.8 | Diagram names the service file `backend/services/classes/session-lifecycle.svc`; §4.1 and task 2.8 (and repo convention `*.service.ts`) use `session-lifecycle.service.ts`. | Fix the §1.2 diagram label to `session-lifecycle.service.ts`. |
| A4 | Advisory | plan §1.2 PHASE 3 / §4.1 step 3; task 2.8 | Replay-branch wording ("read + return the ALREADY-CREATED session; success-equivalent") can be misread as a resolver-level success payload, while REQ-050/REQ-043(e)/task 2.2 require the replay to surface `DUPLICATE_REQUEST` (409 conflict; "success-equivalent" is only the client UX mapping per the error-handling contract). | Tighten wording: the replay branch throws `ConflictError("DUPLICATE_REQUEST")` (or DomainError equivalent); the claim→session join informs messaging/cache only, never a success return. |
| A5 | Advisory | specs REQ-065 + plan §2.7(b) vs task 1.5(b) | Specs/plan point the namespace-registration checklist at `shared/locale/AGENTS.md` (33 lines, no checklist); the checklist actually lives at `shared/AGENTS.md:236`. Task 1.5(b) already corrects this. | Update REQ-065/plan §2.7(b) pointer to `shared/AGENTS.md` (Namespace Registration ~:236). |
| A6 | Advisory | specs §2.9 J1 step 3; task 2.2 step 2 | "Trial lane provably consumed before paid lane" is not actually provable inside J1's step set (A books once on trial, B books once on trial — nobody books twice with a paid lane). The real trial-first proof is REQ-072's double-booking, which task 2.8.TE carries. | Reword task 2.2's claim (trial-first observable via both fixtures binding trial) and cross-reference task 2.8.TE's REQ-072 ordering proof as the authoritative trial-before-paid evidence. |
| A7 | Advisory | tasks 2.1, 2.2, 2.3, 5.1 | Subtask-pipeline/booking gaps: 2.1 has QL/SR/IV but no TE/SEC; 2.2/2.3 (test-first producers of files) have no QL subtasks (protocol #2's sub-loop still applies to their created files); 5.1 ("consolidate/rerun" chaos scenarios) names no file path for the consolidated suite. | Add a minimal QL line to 2.2/2.3, TE/SEC lines to 2.1 (or mark them N/A with reason), and give 5.1 an explicit suite path (or state it re-runs 2.8.TE's scenarios). |
| A8 | Advisory | plan §5.2 vs repo (`navItems.ts:54,61`) | Plan says the existing nav icon is `SchoolOutlined`; the repo symbol is `SessionsIcon` (0.2 outcome §11). Instruction "keep the existing icon" is what matters. | Correct the symbol name in plan §5.2/task 4.2 (or just say "existing icon variable unchanged"). |
| A9 | Advisory | tasks 0.2/2.8/3.2/4.2 line-number citations | Several cited line ranges drift slightly vs the working tree (e.g. `errors.ts:159-163` vs actual `:158-163`; `roleDashboardRoute.ts:15-23` vs `roleDashboardPath` at `:52`). Task 0.2 verified the underlying ground truth, so nothing is un-executable. | Treat line numbers as advisory hints; task executors re-locate symbols (task 0.2 evidence already in `outcome/0.2-outcome.md`). |
| A10 | Advisory | specs REQ-001 vs task 0.1 | REQ-001 names the Phase-0 artifact `outcome/phase0-baseline-outcome.md`; task 0.1 writes `outcome/0.1-outcome.md` (prior plans shipped both names). REQ-083's per-task outcome gate is satisfied either way. | Have task 0.1 (or 5.3/7.4) emit/alias `phase0-baseline-outcome.md` to satisfy REQ-001's named artifact verbatim. |

**Blocking: 4 · Advisory: 10.**

---

## 4. GATE VERDICT

**GATE: FAIL** — 4 blocking findings must be resolved before Phase 1+ implementation begins
(task 0.3's own protocol: "resolve every blocking finding before proceeding").

Blocking summary:

1. **B1 — i18n errors key registry (specs REQ-051 vs plan §2.7(a) vs tasks 1.5(a))**: three-way
   contradiction on an "EXACTLY" registry; task 1.5 drops the mandated `teacherNotFound` key.
   → Reconcile to one registry (recommend REQ-051's 7-key list) before task 1.5.
2. **B2 — `INSUFFICIENT_BALANCE` class (specs REQ-050 vs plan §1.2/§3.3)**: spec says
   ValidationError/422; plan says ConflictError/409. → Align (recommend the spec) before task 2.8.
3. **B3 — createSession scope vs INV-TV6 carve-out (REQ-032 vs REQ-011/REQ-064)**: the mandated
   static role scope cannot express "teacher with `students` row may create"; REQ-064's cell and
   task 5.2's every-cell assertion are unpassable as written. → Record the ruling, amend the
   matrix cell or the scope, before tasks 3.3/5.2.
4. **B4 — REQ-023 governance re-check**: spec SHALL, plan MAY, tasks conditional → uncovered
   SHALL; also internally tense with cancel-must-still-work. → Decide scope, schedule in task 2.8,
   or amend REQ-023 via the addendum, before task 2.8/7.1.

Each blocking fix is a plan/tasks/spec-text reconciliation — none requires code exploration beyond
what tasks 0.1/0.2 already produced, and none invalidates the architecture (§1 diagrams, §2 schema
deltas, §3 SDL, §4 services/repos, §5 frontend design, §7 propagation all otherwise verified
consistent with specs and repo ground truth).

**Recommended next step (orchestrator):** apply the four blocking reconciliations as a
plan-update commit (tasks.md/plan.md/specs.md edits only), then re-run this gate (round 2) before
dispatching Phase 1. Advisory items may be batched into the same update.
