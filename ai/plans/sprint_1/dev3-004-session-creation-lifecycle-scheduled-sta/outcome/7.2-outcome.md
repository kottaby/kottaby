# 7.2 Outcome — Decisions Addendum + State-Machine Cross-Reference

**Task ID:** 7.2 · **Date:** 2026-08-31 · **Branch:** `feat/dev3-004-session-creation-lifecycle-scheduled-sta` (NO checkout/commit/push)
**Deliverables:** REQ-081's two doc mutations — (a) the five-item implementation addendum appended to `docs/specs/open-decisions-and-gaps.md`; (b) a single cross-reference line in `docs/specs/state-machine-invariants.md` with ZERO renumbering.

---

## 1. Files Created / Modified

| File | Action | Content |
|---|---|---|
| `docs/specs/open-decisions-and-gaps.md` | APPEND (additive; sections A–D untouched) | New final section `## E. Implementation Addendum — Session Creation & Lifecycle (DEV3-004, 2026-08-31)` with E.1–E.5 in the doc's established decision style (status glyph, Decision, Schema/Forward impact), headed by a pointer to the canonical doc. |
| `docs/specs/state-machine-invariants.md` | ONE cross-reference line | Inserted directly under `## 1. Session Lifecycle` (before §1.1): a blockquote line pointing at `docs/sessions/session-lifecycle.md` and stating the implementation split (INV-S1..S5 shipped by DEV3-004; INV-S6/S7/S8 + `disputed` owned by DEV3-005/DEV2-013/DEV3-022). **No section was renumbered, renamed, or reordered** — §1.1..§10 byte-identical. |
| `ai/plans/.../outcome/7.2-outcome.md` | CREATE | This close-out record. |

## 2. The five mandated addendum items (as written)

| # | Addendum item | Status marker | Key content |
|---|---|---|---|
| E.1 | **Hold-as-debit + same-lane refund ruling** (reconciles B.4) | ✅ RESOLVED (ruling, 2026-08-30) | Hold = guarded debit of one unit at request (trial-first per INV-B8); `session.held_balance_lane` = permanent provenance (placed once, never nulled); cancellation re-increments the SAME lane exactly once; **supersedes `docs/planning/TEAM_ALLOCATION.md` Contract 1's "held, not decremented" phrasing**; wallet-side release/credit lands with DEV3-012/013. |
| E.2 | **Interim constant fees** (refines B.3) | ✅ RESOLVED | `SESSION_FEE_HIFZ`/`SESSION_FEE_TAJWEED` = `"25.00"` decimal strings, EGP, `SESSION_CONFIRMATION_WINDOW_MS = 86_400_000`; string-carry money discipline; forward: plan-linked pricing → **DEV3-013** (do not add per-plan fee inputs to the wire before that ticket). |
| E.3 | **`is_online` assertion deferral** | ⏸ DEFERRED (ledger D3) | Certification-only assertion in Sprint 1 (`is_online` defaults false, no setter surface until DEV2-011 — asserting it would block ALL bookings); owners **DEV3-008 / DEV2-011**; in-session lock (INV-S6/A2) = DEV2-011/012 + DEV3-005. |
| E.4 | **`session_request_idempotency` claim table + 24h-sweeper deferral** | ✅ IMPLEMENTED (sweeper deferred — ledger D2) | Durable DB claim (key unique 128, user FK cascade, session FK set-null), 23505 → replay **throws** `DUPLICATE_REQUEST` (409), replay's own partial writes roll back (no second debit), success-equivalence is the CLIENT's REQ-065 mapping; key released on failed bookings. Sweeper/expiry → **DEV3-012**. |
| E.5 | **Sessions-are-sensitive oracle ruling** (contrast DEV1-005 plans) | ✅ RESOLVED | Sessions: foreign id ≡ nonexistent id on EVERY read AND mutation (identical `null` channel / byte-identical `SESSION_NOT_FOUND`); plans catalog: public data ⇒ distinguishable `NOT_FOUND` acceptable; copy neither ruling into the other domain; documented teacher-target exception on `createSession` (public-directory data, DEV3-008). |

## 3. Gates

| Gate | Result |
|---|---|
| Zero renumbering check (`state-machine-invariants.md`) | ✅ only insertion = the one blockquote line under `## 1.`; all section numbers/headings unchanged (diff verified). |
| Additivity check (`open-decisions-and-gaps.md`) | ✅ sections A–D + Summary byte-untouched; the addendum is a pure append after the Summary's closing line (established `## E` lettering continues the A/B/C/D series without renumbering anything). |
| Biome scope on both `.md` files | **N/A** (verified once this phase — markdown is outside biome's lint scope; `Checked 0 files`, exit 0). |
| Full-repo `bun tsgo` | **exit 0** (docs-only change). |

## 4. Verdict

**Task 7.2 COMPLETE.** All five REQ-081 addendum items recorded with owners/forwards; the state-machine registry carries exactly one cross-reference line and zero renumbering. Checkbox flipped in `tasks.md`.
