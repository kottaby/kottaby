# 7.3 Outcome — AGENTS.md Propagation (rule-only one-liners + pointers)

**Task ID:** 7.3 · **Date:** 2026-08-31 · **Branch:** `feat/dev3-004-session-creation-lifecycle-scheduled-sta` (NO checkout/commit/push)
**Discipline:** rules and pointers ONLY — **never code, never plan-specific constraints** (no REQ/task IDs as instructions, no test mechanics). Each file's existing style was read first and the addition mirrors it minimally.

---

## 1. Files Modified (4)

| File | Placement | Addition (style-mirrored) |
|---|---|---|
| `backend/services/AGENTS.md` | Top bullet list, directly after the sibling canonical-doc pointers (registration / auth / applicant-lifecycle / trial-provisioning) | One bold rule bullet: `SessionLifecycleService` (`backend/services/classes/session-lifecycle.service.ts`) is the **sole owner** of the session state machine; every transition is a guarded single-statement UPDATE with a cold classification probe; "hold" = guarded debit at request (trial-first per INV-B8) + same-lane refund on cancel; idempotent creation via the claim table (**replays THROW `DUPLICATE_REQUEST`** — the client maps the 409 to a success-equivalent notice); **ZERO notification/audit/wallet rows**. Pointer → `docs/sessions/session-lifecycle.md` (+ consumer list). |
| `backend/db/repo/AGENTS.md` | `## Rules` bullet list, after the Directory-Role-Filtering bullet | Three bold rule bullets: **(1) Guarded transition pattern** — ONE `UPDATE … WHERE <identity> AND <owner/participant> AND <required pre-state> RETURNING *`, never check-then-write; zero-row misses classified by a cold probe read that never feeds a write. **(2) `session.held_balance_lane` provenance** — written once at creation, never rewritten/nulled; refunds (and DEV3-012/013 confirm/sweeper flows) read the recorded lane to refund the SAME lane; never "clean it up". **(3) `FOR UPDATE` locking reads (certification-lock precedent)** — REQUIRED `tx` (a lock released at statement end protects nothing), never the `queryDb` non-tx fast path, minimal projection, honest `null`; `FOR UPDATE` cannot block on uncommitted INSERTs under READ COMMITTED → concurrency tests must lock committed rows. Pointer → canonical doc §2. |
| `backend/graphql/AGENTS.md` | Top bullet list, directly after the Gateway-route bullet (its sibling context rule) | One bold rule bullet: **participant-scoped operations** — "authenticated, then the service checks participation" ops declare ONLY `{ authenticated: true }` with the predicate service-side (never widened for admins/other roles); when a role leg exists make the conjunction EXPLICIT with `$all { authenticated: true, role: [UserRole.X] }` (plain key-map = ANY semantics — wrong); sessions are disclosure-sensitive: foreign id ≡ nonexistent id on every read/mutation. Pointer → canonical doc §2/§7. |
| `AGENTS.md` (root) | `## Important References` list, after the sibling `docs/students/free-trial-provisioning.md` entry | One reference line: `docs/sessions/session-lifecycle.md` — Session lifecycle canonical reference (DEV3-004: state machine + guarded-transition pattern, four-phase creation invariant, hold-as-debit + same-lane refund, idempotency claim design, sessions-are-sensitive oracle ruling + anti-copy-paste warning, consumer guidance for DEV3-005/006/011/012/013/021 + DEV2-016). |

## 2. Propagation discipline

- **Propagated (permanent/global rules):** sole-owner service rule; guarded single-statement transition pattern; probe-classification-only; hold-as-debit + same-lane refund; provenance-column permanence; FOR UPDATE tx-required rule + the READ-COMMITTED concurrency-testing caveat; replay-throw; zero-notification/audit/wallet rule; participant-scoped scope-declaration pattern; explicit `$all` conjunction; sessions-are-sensitive oracle posture; canonical-doc pointers.
- **Excluded (plan-specific):** REQ numbers, task IDs, sprint dates, ticket ownership tables, test file paths, and the D1–D10 ledger mechanics — none of these appear as instructions in the AGENTS bullets (the root AGENTS entry names the consumer tickets only because the canonical-reference entries in that list conventionally name their audience).

## 3. Gates

| Gate | Result |
|---|---|
| Additivity | All four diffs are pure bullet/line insertions at the mirrored position — zero existing lines modified or reordered. |
| Biome scope on the AGENTS.md files | **N/A** (verified once this phase — `.md` inputs are ignored by biome: `Checked 0 files`, exit 0). |
| Full-repo `bun tsgo` | **exit 0, 0 errors** (confirmed after the AGENTS edits — they cannot affect compilation, re-verified anyway). |

## 4. Verdict

**Task 7.3 COMPLETE.** Four AGENTS.md files gained rule-only one-liners + canonical-doc pointers in their existing style; zero code and zero plan-specific constraints propagated. Checkbox flipped in `tasks.md`.
