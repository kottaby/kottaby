# Task 4.1 Outcome — Frontend GraphQL Documents (scheduling/session.documents.ts)

> **Task 4.1 — Implement `session.documents.ts` shared documents (Phase 4: Frontend GraphQL Documents, Stores & UI Views).**
> Branch `feat/dev3-004-session-creation-lifecycle-scheduled-sta` · NO checkout/commit/push (orchestrator commits).
> Plan §5.4 documents table · instructions `frontend/graphql/sharedDocuments/AGENTS.md` · REQ-062.

---

## 1. Files

| File | Action | Summary |
|---|---|---|
| `frontend/graphql/sharedDocuments/scheduling/session.documents.ts` | CREATE | The SEVEN TypedDocumentNode documents per plan §5.4, `gql` + `TypedDocumentNode` from `@apollo/client` (never `/core`), all 14 type imports from `@/frontend/graphql/generated/gql/graphql` only. `id` in EVERY `Session` selection (first field, matching the backend's cache-normalization convention). No `useLazyQuery` (banned). |
| `frontend/graphql/sharedDocuments/scheduling/index.ts` | CREATE | Sub-barrel: `export * from "./session.documents";` + docblock naming the seven documents. |
| `frontend/graphql/sharedDocuments/index.ts` | UPDATE | Additive `export * from "./scheduling";` (alphabetical: `auth` < `scheduling` < `teachers`). |
| `frontend/graphql/sharedDocuments/documents.contract.test.ts` | UPDATE | Contract table extended with the seven DEV3-004 rows (operation name / channel / declared variables / object-`id` paths incl. `myStudentSessions.items` & `myTeacherSessions.items`); barrel-parity test now also proves `sessionByIdViaBarrel` ≡ deep import; TypedDocumentNode compile-proof block extended with all seven assignments against the generated operation types. Docblock's channel/id sections updated for the new surface. |
| `frontend/graphql/generated/gql/graphql.ts` | REGENERATED (sanctioned) | `bun run generate:gqlSchema && bun codegen` — +108 lines, strictly the DEV3-004 operation types (7 × `{Op}Query/Mutation` + `{Op}Variables` + extracted compact types + typed-document-node AST constants). |

## 2. Document names & operation shapes

| Document const | Operation | `TypedDocumentNode` typing | Selection shape |
|---|---|---|---|
| `sessionByIdQueryDocument` | `query SessionById($id: ID!)` | `TypedDocumentNode<SessionByIdQuery, SessionByIdQueryVariables>` | `sessionById(id: $id) { id … 15 Session fields }` (nullable — no-oracle `null` for foreign/nonexistent) |
| `myStudentSessionsQueryDocument` | `query MyStudentSessions($filter: SessionListFilterInput, $page: Int, $pageSize: Int)` | `TypedDocumentNode<MyStudentSessionsQuery, MyStudentSessionsQueryVariables>` | `myStudentSessions(…) { items { id … } page pageSize totalCount }` |
| `myTeacherSessionsQueryDocument` | `query MyTeacherSessions($filter, $page, $pageSize)` | `TypedDocumentNode<MyTeacherSessionsQuery, MyTeacherSessionsQueryVariables>` | mirror of `myStudentSessions` |
| `createSessionMutationDocument` | `mutation CreateSession($input: CreateSessionInput!)` | `TypedDocumentNode<CreateSessionMutation, CreateSessionMutationVariables>` | `createSession(input: $input) { id … }` → `Session!` for cache normalization |
| `startSessionMutationDocument` | `mutation StartSession($id: ID!)` | `TypedDocumentNode<StartSessionMutation, StartSessionMutationVariables>` | `startSession(id: $id) { id … }` |
| `completeSessionMutationDocument` | `mutation CompleteSession($id: ID!)` | `TypedDocumentNode<CompleteSessionMutation, CompleteSessionMutationVariables>` | `completeSession(id: $id) { id … }` |
| `cancelSessionMutationDocument` | `mutation CancelSession($id: ID!, $reason: String)` | `TypedDocumentNode<CancelSessionMutation, CancelSessionMutationVariables>` | `cancelSession(id: $id, reason: $reason) { id … }` |

Every `Session` selection carries the full 15-field public SDL shape (`id, status, intent, sessionType, fee, feeHeld, studentId, teacherId, startedAt, endedAt, confirmationDeadline, confirmedByStudentAt, confirmedByTeacherAt, createdAt, updatedAt`) with `id` FIRST — cache normalization by `id` lets 4.2/4.3 converge lists from returned `Session!` payloads with no refetch. Selection sets are fully spelled out (no interpolated selection constants — Apollo `gql` rejects string interpolation; mirrors the auth/teachers precedent of literal SDL).

## 3. Gates (all re-run green first-hand)

| Gate | Result |
|---|---|
| `bun run generate:gqlSchema` | PASS — `schema.graphql` rewritten byte-identical (5545 bytes, same as the 3.4 recorded size; `git status` shows it unmodified) |
| `bun codegen` | PASS — `graphql.ts` +108 lines, ONLY DEV3-004 operation artifacts (7 op types + 7 Variables + extracted compact types + 7 `*Document` AST consts); zero unrelated drift |
| `documents.contract.test.ts` | **18 pass / 0 fail / 110 expect** (was 4 pass / 6-test table; extended rows + barrel parity + compile-proof all green) |
| `bun run tsgo` | 0 errors |
| `bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates` | exit 0 on ALL touched files (session.documents.ts, scheduling/index.ts, sharedDocuments/index.ts, documents.contract.test.ts) |

## 4. Semantic review

- `gql` + `type TypedDocumentNode` from `@apollo/client` only — no `@apollo/client/core` anywhere (grep-verified).
- Types ONLY from `@/frontend/graphql/generated/gql/graphql` — no inline type literals, no mapping layers, no indexed-access workarounds; compact extracted names (`SessionByIdQuery_sessionById`, `MyStudentSessionsQuery_myStudentSessions`) come straight from codegen for downstream consumers.
- `id` in every object-typed selection set (7/7 operations) — pinned structurally by the contract test's `objectSelections` paths.
- No `useLazyQuery` (banned); no hooks at all in this layer — consumers import hooks from `@apollo/client/react` (4.2+).
- Zero dead exports: all 7 constants are re-exported through the sub-barrel + top-level barrel, and the barrel-parity test proves deep-import ≡ barrel instance identity (cache-key safety).
- Barrel changes strictly additive; generated code never hand-edited.

## 5. Notes / deviations

- **Worklog:** commit `3a4b27d` removed `worklog.md` from the repo ("belongs to sandbox coordination ledger"), so no in-repo worklog file exists to append to; this outcome file carries the entry. Orchestrator should mirror it into the external ledger: *Task 4.1 closed — scheduling/session.documents.ts (7 TypedDocumentNode ops), scheduling barrel + top-level barrel, codegen +108 lines, contract test 18/18, tsgo 0, sub-loop exit 0 on all touched files.*

**Task 4.1 CLOSED — all gates green first-hand. Next: 4.2 (Student Sessions page).**
