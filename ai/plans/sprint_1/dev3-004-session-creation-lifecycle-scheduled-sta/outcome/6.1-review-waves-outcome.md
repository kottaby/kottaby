# Task 6.1 — Review Waves Consolidated Outcome (one subsection per wave)

**Date:** 2026-08-31
**Task:** 6.1 [Launch parallel review waves + deferred-items check] — the consolidated task-6.1 deliverable.
**Baseline:** `b0ca09e` (outcome/0.1: tsgo 0 · biome 0 · eslint 0/0 — pre-existing issues are NOT findings).
**Scope convention (all waves):** `git diff b0ca09e --name-only` production files only; `ai/plans/**`, `*.md`, screenshots, and generated codegen artifacts excluded. All waves were REPORT-ONLY at review time; fixes landed exclusively through dedicated fix commits (below), each independently re-verified by a later round.
**Source round files (the consolidated record):** `outcome/6.1-review-types-backend.md` (round-1 waves 1–2) · `outcome/6.1-review-frontend-pentest.md` (round-1 waves 3–4) · `outcome/6.1-review-R2.md` (+fix) · `outcome/6.1-review-R3.md` · `outcome/6.1-review-R4.md`.
**Fix commits:** `61c39b6` (6.1-fix — round-1 fixes) · `55af121` (6.1-R2fix — REQ-054 id-shape guards + lane docblocks) · `8779acc` (lint-config commit — closes the commit-ownership INFO).

**Finding-count summary by wave (round 1):**

| Wave | Checks | Findings | Dispositions |
|---|---|---|---|
| review-types | 7/7 PASS | 2 (2 LOW) | 1 fixed-in-pass (`61c39b6`), 1 recorded-with-owner (ledger D10 ⏸) |
| review-backend | 11/11 PASS | 2 (1 LOW + 1 INFO process) | 1 fixed-in-pass (`61c39b6`), 1 resolved via commit ownership (`8779acc`) |
| review-frontend | 12/12 PASS | 1 (1 MINOR) | fixed-in-pass (`61c39b6`) |
| pentester | 7/7 PASS | 3 INFO (2 hardening + 1 pre-existing infra) | all recorded-with-owner |

Cross-round additions: R2 = 1 MEDIUM + 3 INFO (MEDIUM + 1 INFO fixed in `55af121`, 1 INFO resolved in `8779acc`, 1 INFO recorded-with-owner); R3 = 0; R4 = 0. **No BLOCKER/CRITICAL/HIGH ever; every finding is either fixed-and-re-verified or recorded with an owner. Zero blocking findings looped back to any owning phase.**

---

## Wave 1 — review-types

**Scope:** backend/ + shared/ + test/ delta of `git diff b0ca09e` (types lens: canonical-type discipline). Files read line-by-line: `session.types.ts`, `session-request-idempotency.types.ts`, `session.types.conformance.test-d.ts`, `backend/types/classes/index.ts`, pothos objectRefs/inputs, schema `$type<>` bindings, locale types barrels. Round file: `6.1-review-types-backend.md` §3.

**Checks run (7/7 ✅ PASS):** no local Pothos types (objectRefs backed exclusively by canonical `SessionReturnType`/`SessionPageReturnType`; only the sanctioned Input-Exception shapes) · no service `.types.ts` (zero type-defining files in the service layer) · contract types consumed-not-redefined (`SessionRequestContract` byte-identical to baseline; `SessionStudentIntentType` `Equals`-pinned to the contract by the conformance test-d) · additive-only diffs on existing type files (zero modifications/deletions of existing members) · no duplicate type names across files (canonical vocabulary declared exactly once; no string-union re-declarations of enums) · import-path consistency (`@/` aliases; barrels relative per AGENTS.md) · enum VALUE vs TYPE import correctness at every site (tsgo 0 + biome 0 corroborate).

**Findings (severity + disposition):**

| # | Severity | Location | Finding | Disposition |
|---|---|---|---|---|
| F1 | LOW | backend/graphql/mutation/classes/session-lifecycle.mutation.ts:108-112 | Resolver overlays the hostile SDL value onto a typed base — `Object.assign(baseInput, { intent: args.input.intent })` intentionally violates the closed `SessionStudentIntentType` invariant at ONE boundary so the out-of-contract `evaluation` member can reach the service's pre-DB guard (REQ-050 VALIDATION path). Lint-clean, BOPLA-safe (service re-validates pre-DB; `intent` is client-owned by design), zero security impact — residual risk is static-typing honesty only. | **Recorded-with-owner** → deferred-items ledger row **D10** (⏸ Forward, Future ticket owner TBD, 6.1-fix addendum 2026-08-31). Forward fix: widen the service boundary param to `SessionIntent` and narrow internally (deleting the overlay/type lie). |
| F3 | LOW | backend/graphql/pothos/classes/session.pothos.ts:54-104 | DB-string → TS-enum mappers hardcoded pgEnum literals as switch cases with a silent `default → null` — a future pgEnum member would NOT be compile-checked (consistency/drift risk vs the `toUserRole` house precedent). | **Fixed-in-pass** → 6.1-fix **commit `61c39b6`** (FIX 2): all three mappers retyped to canonical row-column literal-union scrutinees, one case per member, `default` deleted, trailing `const exhaustive: never` fail-closed throw (registration.service idiom) — a future pgEnum member now fails `tsgo` with TS2322. Resolvers simplified to direct mapper calls; error messages preserved. Verified LANDED by R2 (§1 table, item 2). |

Adjacent: R1's INFO F4 (uncommitted worktree files — test-hardening edits + `oxlint.config.mts` hunk; commit-ownership process note) resolved via commit ownership — the lint hunk landed in **commit `8779acc`**, the test hardening in `7a588c8`/`61c39b6`; worktree fully clean at R4.

---

## Wave 2 — review-backend

**Scope:** backend delta of `git diff b0ca09e` (service/repos/resolvers/schema/enum/constants). Round file: `6.1-review-types-backend.md` §4. Gates re-verified on the current worktree at review time: `bun tsgo` 0 · biome 570 files 0 diagnostics · baseline delta 0.

**Checks run (11/11 ✅ PASS):** four-phase create ordering intact (cert lock `FOR UPDATE` → trial-first guarded debit ladder → savepoint-bracketed claim insert → session insert + claim backfill, all inside ONE `withTransaction`; replay branch throws, rollback = only cleanup) · guarded single-statement transitions (start/complete/cancel each ONE fused-predicate `UPDATE … WHERE … RETURNING`; completion fuses the certification `EXISTS`; zero check-then-write windows) · probe classification-only (`findTransitionProbe` invoked only after a zero-row guarded miss; refund driven by the guarded row, never the probe) · tx propagation audit on EVERY new repo method and call site (all 13 write-path sites pass tx/claimTx; writes on `tx ?? db`; non-tx reads via `queryDb`) · zero cross-layer imports · REQ-019 grep gates (notification/audit/wallet/teacher_transaction/reports → only JSDoc prose; journey fixtures use schemas as read-only counters) · REQ-031 zero-`...input` grep gate (0 matches in the slice; insert mapped field-by-field) · REQ-036 log hygiene (zero `console.*`; contexts `{code, entity, entityId}`/`{sessionId}` only; idempotency key never logged) · TOCTOU re-scan on mutations (accepted residual: governance state may flip between the pre-tx defense-in-depth re-check and the write — documented) · dead code scan (every export consumed) · race-condition re-scan (no module-level mutable state; `LANE_BALANCE_COLUMNS` frozen).

**Findings (severity + disposition):**

| # | Severity | Location | Finding | Disposition |
|---|---|---|---|---|
| F2 | LOW | backend/services/classes/session-lifecycle.service.ts:548 (`assertActorGovernanceClean`) | Governance denial threw `new ConflictError("FORBIDDEN", t.forbidden)` — wire-identical (403 via taxonomy) and REQ-064-covered, but the error CLASS misdescribed the semantic (authorization denial surfaced through a Conflict class). Cosmetic/clarity only. | **Fixed-in-pass** → 6.1-fix **commit `61c39b6`** (FIX 1): `ForbiddenError` class added to `backend/lib/errors.ts` (`extensions.code = "FORBIDDEN"` → 403 via taxonomy) and thrown at `assertActorGovernanceClean`; lockstep journey-test hardening (`toBeInstanceOf(ForbiddenError)`). Verified LANDED by R2 (§1 table, item 1 — denials journey 6/0/93 re-run green). |
| F4 | INFO (process) | worktree state | Uncommitted worktree modifications on top of HEAD (sanctioned 5.3 test hardening + the load-bearing `oxlint.config.mts` frontend exemption) — flagged so commit ownership stays with the orchestrator. | **Resolved via commit ownership** — the oxlint hunk landed in **commit `8779acc`** (R4 §4: "R2 INFO-2 RESOLVED"); worktree fully clean at R4. |

Adjacent deltas reviewed (no findings): `errors.ts` `ConflictError` overload (additive), `registration.service.ts` `withTransaction` extraction (byte-equivalent), `enum.pothos.ts` enum-trio registration, barrels, `oxlint.config.mts`.

---

## Wave 3 — review-frontend

**Scope:** frontend/app/shared delta of `git diff b0ca09e` (both `app/(dashboard)` session pages, both containers, `SessionRow`, `SessionStatusFilterChips`, `CancelSessionConfirmDialog`, `navItems.ts`, `session.documents.ts` + contract test, locale sessions namespace types/en/ar, `session-fees.constants.ts`, `oxlint.config.mts` hunk). Round file: `6.1-review-frontend-pentest.md` §2.

**Checks run (12/12 ✅ PASS):** MUI v9 sx-only compliance (`style={` 0 hits; no raw hex/rgb) · palette-token exclusivity (all colors via `theme.palette.*` callbacks; no string palette access, no `contrastText`) · `*Outlined` icons only (zero non-Outlined imports; nav retarget reuses the pre-existing `SchoolOutlined`) · `React.SubmitEvent` never `FormEvent` (the dialog form is the only submit) · `useAppTranslation(Sessions)` handle-const property access (no string-literal namespaces, no `Translation` enum, no call-by-key) · no `useLazyQuery` · codegen-types-only (`TypedDocumentNode<…>` from generated `gql/graphql`; contract test pins barrel ≡ deep-import identity) · nav conventions (`labelKey` + Icon preserved on both retargets) · zero hardcoded strings (Arabic-range grep 0; copy flows from Sessions/Errors/Common) · no new Zustand stores · `PermissionDeniedFallback` for denials (never bare `null`) · no `console.*`. Bonus verifications: `aria-busy`/`aria-pressed`/`aria-invalid` present, `maxLength` + live counter + submit-time re-guard, per-kind AND per-row in-flight disabling, cache convergence normalization/evict-only (no refetch storms).

**Findings (severity + disposition):**

| # | Severity | Location | Finding | Disposition |
|---|---|---|---|---|
| 1 | MINOR | frontend/views/student/sessions/CancelSessionConfirmDialog.tsx:62-63 (docblock) vs :214 (behavior) | Doc/behavior drift on dismissal-during-pending: the `onClose` docblock claimed dismissal is "ignored while pending", but neither dialog nor containers gated it — Escape/backdrop dismissal forwarded unconditionally while a cancel mutation was in flight. Impact benign (outcome arms converge cache/snackbars; dialog re-mounts fresh), but the retry contract was stated and not enforced. | **Fixed-in-pass** → 6.1-fix **commit `61c39b6`** (FIX 3): `handleDialogClose` gates backdrop/Escape dismissal on the `loading` flag, wired at `Dialog onClose`; both action Buttons already `disabled={loading}`; prop docblock rewritten to the now-true contract. Verified LANDED by R2 (§1 table, item 3). |

Recorded-as-accepted elsewhere (not re-raised): the 4.2 §8.2 filter-chips-hidden-on-empty note stands in outcome/4.2; D8/D9 runner-tier skips accepted per the 4.2/4.3 BF/BS verdicts.

---

## Wave 4 — pentester

**Scope:** security lens over the backend GraphQL resolvers/pothos inputs/service/repos + grep sweeps over the whole backend delta (service 719 lines, both repositories, idempotency schema, `withTransaction`, `errors.ts` delta, enum, lane-debit/refund paths). Round file: `6.1-review-frontend-pentest.md` §3.

**Checks run (7/7 ✅ PASS):** BOLA/IDOR oracle-safety (foreign id ≡ nonexistent id on EVERY read/mutation — identical `null` channel and byte-identical `NotFoundError("SESSION")` classification through `rejectTransitionProbe`; suite oracle pairings pin it) · BOPLA closed inputs (`CreateSessionInput` = exactly `{teacherId, intent}`; insert mapped field-by-field with server-pinned `studentId`/`status`/`sessionType`/`fee`/`feeHeld`/lane/deadline; repo-wide spread grep 0; `reason` validated then discarded) · BFLA role scopes exact (`createSession` Student-only with explicit conjunction — no admin bypass; start/complete Teacher + service-side ownership; cancel authenticated-only with every non-participant dying service-side oracle-safe) · idempotency-claim abuse (claim ctx-bound `userId`; cross-user key reuse surfaces oracle-safe 404, never the other user's session; key never logged) · query depth/complexity on the new list resolvers (`pageSize` service-bounded 1..50 pre-DB, closed filter vocabulary, owner-scoped predicates) · injection surface (zero LIKE/concat SQL; parameterized Drizzle + dialect-rendered predicates + `sql.identifier()` from a frozen enum-keyed map) · log-injection (`reason` never enters logs; no client free text in any log context).

**Findings (severity + disposition):**

| # | Severity | Location | Finding | Disposition |
|---|---|---|---|---|
| 1 | INFO | backend/services/classes/session-lifecycle.service.ts:695-700 (`normalizePageBounds`) | Only the lower page edge is normalized and only `pageSize` is hard-capped (1..50); `page` itself is unbounded so the list `OFFSET` is client-controlled. Practical impact small (owner-scoped predicate bounds any deep-offset walk to the caller's OWN rows; Postgres short-circuits past table end). Hardening only: `maxPage` cap or keyset pagination if session volume grows. | **Recorded-with-owner** — Owner: DEV3-004 session-list service maintainer (this slice); deferred hardening, act only if session volume grows. |
| 2 | INFO | backend/services/classes/session-lifecycle.service.ts:209-225 | Teacher-existence oracle on `createSession` (nonexistent teacher → 404; existing-but-uncertified → 409; sessions themselves FULLY oracle-collapsed). Verified deliberate documented design — the oracle is over the TEACHER entity, which is public-directory data (D3 → DEV3-008), pinned by the 5.2 grid. | **Recorded-with-owner** — Owner: feature maintainer; revisit only if the teacher directory's public-data contract changes (D3 → DEV3-008). |
| 3 | INFO (pre-existing infra) | backend/lib/gateway/transport-guard.ts | No GraphQL query depth/complexity limiter at the gateway — absent at baseline `b0ca09e` too (filtered per the baseline rule; NOT a finding against this feature). The new list resolvers add no unbounded read. | **Recorded-with-owner** — flagged for the gateway owner; pre-existing infra, no new exposure added. |

---

## Cross-round iterations (R2 → R2fix → R3 → R4)

Round files: `6.1-review-R2.md`, `6.1-review-R3.md`, `6.1-review-R4.md`.

- **R2 (independent, all four lenses):** round-1 fix verification **3/3 LANDED** (ForbiddenError class, exhaustive pothos mappers, dialog dismissal gate — no regression; service 37/0/362, SDL 10/0/45, schema-surface 12/0/56 re-run green). Fresh sweep found **1 MEDIUM + 3 INFO**: [MEDIUM] REQ-054 positive-safe-integer guard missing on the target session id for `startSession`/`completeSession`/`cancelSession`/`sessionById` (shape-only `Number(args.id)` → malformed IDs reached SQL as 22P02 → masked 500 instead of the mandated `VALIDATION`/422-or-`null`; zero security impact — bound parameters, fail-closed) → **fixed** in 6.1-R2fix **commit `55af121`** (shared `isPositiveSafeSessionId`/`assertPositiveSafeSessionId` at the top of all three mutations + pre-DB `null` short-circuit on `getSessionById`, lockstep test cells: service "session-id shape guards" matrix + live-wire `startSession(id: "12abc")` → `VALIDATION`). [INFO] uncommitted load-bearing oxlint hunk → **resolved** in **commit `8779acc`**. [INFO] lane-provenance docblock inaccuracy → **fixed** in `55af121`. [INFO] single per-kind in-flight slot under concurrent same-kind mutations (benign — server-guarded converge) → **recorded-with-owner** (hardening only if the flow grows retries).
- **R3 (confirmation):** R2fix verification **4/4 LANDED** (guard order matches docblocks; idiom parity with `createSession`; read channel silent pre-DB; zero old-behavior test leftovers) — **0 new findings** (0/0/0/0/0).
- **R4 (final, clean tree at `8779acc`):** gates tsgo 0 · biome 0 (570 files) · ESLint 0/0; skeptical diff pass over `git diff b0ca09e --stat` (159 files, +19 857/−204) structurally clean — **0 new findings** (0/0/0/0/0).

**Iteration summary:** R1 findings (3 LOW + 1 INFO process + 1 MINOR + 3 INFO) → fixed (`61c39b6`) / recorded-with-owner (D10 ⏸; INFO owners) → R2: 1 MEDIUM + 3 INFO → fixed (`55af121`, `8779acc`) / recorded-with-owner → R3: **0 findings** → R4: **0 findings**. **Stop condition SATISFIED — two consecutive zero-finding rounds (R3 + R4).** The review loop is COMPLETE; remaining ledger rows (D8/D9 runner-tier skips, D10 typing-honesty residual) are pre-recorded forward items, not defects.

---

## Deferred-items check

`grep -c "❌\|⚠️"` on `deferred-items.md` = **4 matches, zero of them item statuses** — all four hits are the Status Values **legend definitions** (`⚠️ Partial`, `❌ Blocked`) plus two prose addenda that quote the gate expression itself (0.3-fix ruling addendum, 6.1-fix D10 addendum). REQ-083 item-status gate: **clean (0)**.

Ledger item statuses (10 rows): **D1–D5, D7, D8, D9, D10 = ⏸ Forward** (9 rows — each owner-referenced, non-blocking), **D6 = ✅ Done** (the in-plan-resolved canonical-naming decision, renumbered from the template's original D1 slot at 0.1 seeding and kept for traceability — resolved by the §1.5 naming decision + task-0.2 verify-absence gate, not a review deferral). No ❌/⚠️/🔄 status on any row.

---

## Final battery (R4, HEAD `8779acc`, clean worktree)

Full completion battery via the mandated `bun run test/scripts/run-test.ts <path>` — 19 suites / 20 test files: **370 pass / 0 fail / 12 skip / 2,842 expects**. Suite detail: service 38/0/443 · mutations 36/0/105 · queries 14/0/53 · SDL 10/0/45 · schema-surface 12/0/56 · public-operations 26/0/52 · allowlist-coverage 8/0/42 · session-schema-deltas 11/0/110 · student-lane-debit 16/0/82 · teacher.repository 14/0/43 · session.repository 41/0/223 · idempotency.repository 13/0/77 · held-balance-lane.enum 25/0/331 · session-fees.constants 15/0/85 · sessions-namespace.parity 20/0/289 · session.types.static-assertions 9/0/62 · student components 19/0/4skip/181 · teacher components 27/0/8skip/291 · journeys 16/0/272 (J1 10/179 + J2 denials 6/93). The 12 skips are exactly the pre-recorded D8/D9 runner-tier deferrals (real-Chromium verification duty already discharged in 4.2.BF). Every previously-reported cell reproduces identically across R2fix → R3 → R4.

---

## Verdict

- **review-types: PASS** (7/7) · **review-backend: PASS** (11/11) · **review-frontend: PASS** (12/12) · **pentester: PASS** (7/7).
- All findings across four rounds: 0 CRITICAL · 0 BLOCKER · 0 HIGH · 1 MEDIUM (fixed, `55af121`, re-verified R3) · 3 LOW (2 fixed `61c39b6`, 1 → D10 ⏸) · 1 MINOR (fixed `61c39b6`) · 8 INFO/process (fixed `8779acc`/`55af121`, resolved, or recorded-with-owner).
- Deferred-items check: REQ-083 gate clean — item statuses all neutral (9 × ⏸ Forward, 1 × ✅ Done in-plan), legend glyphs excluded.
- Stop condition satisfied (R3 + R4 both 0). **Task 6.1 review gate: COMPLETE.**
