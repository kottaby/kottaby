# R7 — Independent Review Round 7 (fresh eyes)

**Plan:** ai/plans/sprint_1/dev3-004-session-creation-lifecycle-scheduled-sta/
**Branch:** feat/dev3-004-session-creation-lifecycle-scheduled-sta @ a14c61c + uncommitted F-1/F-3/F-4 working-tree fix sets (reviewed at WORKING-TREE state — that is what gets committed)
**Round scope:** (1) verification of the previously-flagged categories only — plan-artifact refs in the 7 cleaned files, docblock honesty, pin honesty incl. the NEW F-4 dynamic-import pin; (2) fresh 4-lens sweep (types / backend / frontend / security) over span `b0ca09e..07e3488 -- backend/ shared/ frontend/ app/` (82 files) plus the F-1 files, at working-tree state.
**Carried filters:** deferred-items.md rows D1–D11 and round outcomes R5/R6 = RECORDED; anything present at 8779acc in DEV3-004-authored files = PRE-EXISTING.

---

## (a) Fix verification — PASS on all counts

1. **Plan-artifact refs (7 cleaned files) — PASS (with precise residual inventory).** Full R6 regex family (`DEV3-\d|REQ-\d|R-1xx|R-2xx|plan §|4\.BFBS|cron-r2|D9-bis|DEV2-|DEV1-|task N|Phase N|tasks\.md|specs\.md|plan\.md|outcome §|gate AN|.ai/plans`) re-run at working tree: **0 hits in all 5 comment-swept code files** (service, mutation, query, StudentSessionsContainer, TeacherSessionsContainer) — unchanged since R6's check. AGENTS.md: the rewritten session-lifecycle bullet (line 7) = 0 refs; 3 doc-prose hits remain in OTHER bullets (:4 DEV2-002, :5 REQ-014/015/016, :75 REQ-10) — out-of-bullet doc-prose, PRE-EXISTING (R5-TB/F-3 recorded, deliberately untouched). service.test.ts: plan refs remain (REQ-04x test names, DEV3-005/DEV3-012 describe titles, R-107, the D2/DEV3-012 comment at :1674) — verified by hunk-scan that the working diff touches ONLY the Tier-4 pin block (@@ -1309,14 +1309,79 @@), so every one of those refs is byte-identical to HEAD = PRE-EXISTING, out of the comment-sweep mandate per F-3/R6 records.
2. **Docblock honesty — PASS.** Service header "Cross-surface dependency policy" is TRUE against the module's real imports (6 repositories via the `@/backend/db/repo` barrel incl. `WalletRepository`; zero notification/audit/report imports; `withTransaction` the only services import). AGENTS.md bullet diff = exactly 1 line, honest, domain-worded. Mutation header lists exactly the 7 registered mutations. Intent-overlay `Object.assign` comment (mutation :114-126) honest — matches D10's recorded residual.
3. **F-4 dynamic-import pin (service.test.ts:1372-1385) — PASS (honest + fail-closed).** Read in full: two sibling assertions `serviceSource.match(/\bimport\s*\(/g)` and `/\brequire\s*\(/g` each asserted `toEqual([])`. Fail-closed in both directions: the `\b`-anchored patterns fire on ANY `import(`/`import (`/`require(`-style call site anywhere in the file (comments and strings included — over-strict only ever fails the suite, never passes a bypass); live service source = **0 dynamic sites, 13 from-clauses all on the 13-entry allowlist, zero duplicates** (independently re-run against the real file). Failure message honestly names the two sanctioned resolutions (statically pinned import admitted on the allowlist, or consciously extend BOTH pins in one change). The pin closes R6 finding 2 exactly as recorded.
4. **F-1 repo pins (session.repository.ts + its test) — PASS (independently re-counted).** Interpolations **17**, per-expression census exactly = ALLOWED (SESSION_SELECT_COLUMNS×3, rendered.sql×4, params.length+1×2, params.length+2×2, `teacher`, eq(teacher.id,…), eq(teacher.isApproved,true), + the 3 newly admitted schema-column/clock scalars `session.confirmedByStudentAt`/`session.confirmationDeadline`/`now`) — zero caller-input channels. `const executor = tx ?? db;` = **9**; `queryDb<` = **6**; `export async function` = **17**; `tx?: DBTransaction` = **20** (≥17 floor); no required-tx signature, no `.prepare(`, no `sql.placeholder`, no `--`. Let-scan = statement-position over comment-stripped source (R6's INFO caveat stands — RECORDED, record-only).
5. **warning-surfacing 12-field inventory — PASS.** `KNOWN_LIVE_MUTATION_FIELDS` (working tree) = exactly the 12 Mutation root fields of the committed SDL (`frontend/graphql/generated/schema.graphql`); working schema byte-identical to HEAD (no diff); A2 gap-pin untouched. Docblock now says "twelve session/auth/wallet mutations pinned below" — TRUE.
6. **TeacherWalletContainer + suite — PASS.** Container working diff = 1 indentation line (formatting only). Suite diff = prettier-style rewraps + `getAllByText("EGP").length).toBe(2)` → `toHaveLength(2)` (assertion-equivalent). No behavioral change.
7. **Executable-identity of the comment sweep — RE-CONFIRMED.** Independent diff-line classification re-run at working tree: every changed line in service.ts, mutation.ts, query.ts, both session containers, AND session.repository.ts is comment syntax (or blank) — 6/6 comment-only. service.test.ts's single hunk is test-pin code (F-3 rewrite + F-4 addition), repo test's hunks are pin updates, warning-surfacing's is the inventory update — all test-tier, all previously suite-verified green (117/0 db, 55/0 service-direct, 66/0 graphql, 137/0/30skip ui).

## (b) Fresh sweep findings (new eyes, 4 lenses)

**New (2, 0 blocking):**

- `[NEW][LOW]` backend/db/repo/classes/session.repository.ts:240-251 — **start-after-deadline sweep-bypass window.** `startSessionOnce`'s predicate is `id ∧ teacherId ∧ status=scheduled` — it does NOT consult `confirmation_deadline`. A deadline-passed scheduled row can therefore be started by its owning teacher in the window before the cron sweep ticks (`app/api/cron/sweep-sessions`), permanently escaping the sweep (whose predicate requires `status=scheduled ∧ deadline<now`, :534-544), re-freezing the student's escrow hold, and — once the teacher completes — moving the row into the ONE state (`completed`) from which cancel/dispute/sweep are all unreachable (see next finding). The deadline is otherwise consumed by nothing else in the lifecycle (grep-verified: only writer = creation, :387; only consumer = the sweep predicate). No test covers start-vs-expired-deadline (grepped both suites). Attribution: the predicate is verbatim at 8779acc (PRE-EXISTING shape); the BYPASS INTERACTION is new with the DEV3-012 sweeper (`4fd479b`, post-close-out) — R6 verified confirm-vs-sweeper disjointness but not start-vs-sweeper. Fix direction (future-ticket owner): fuse `confirmationDeadline >= now` into the start predicate (making the sweep authoritative), or document the sweep as best-effort; either way add the missing test arm. Non-blocking.
- `[NEW][INFO]` backend/db/repo/classes/session.repository.ts:311/:348 (+ :534) — **no timeout/arbitration release path for a completed-but-unconfirmed row.** `cancelSessionOnce` and `openDisputeOnce` predicate on `scheduled|started` only, the sweep on `scheduled` only, and the admin work queue lists `disputed` only — so a `completed` row with `fee_held=true` persists indefinitely until the student confirms (the only release = the teacher credit). Escrow-design observation, not a theft vector (the student holds their own funds; the teacher cannot self-credit; the confirm arm is exactly-once). Introduced by the DEV3-012 dual-confirmation semantics (post-close-out). Record-only for a future product/backend ticket (e.g. a completed-row expiry arm or an admin release surface).

**Clean sweeps (re-verified fresh):** race check — confirm predicate (completed ∧ feeHeld ∧ stamp-absence) vs sweeper (scheduled ∧ deadline<now) remain disjoint; every transition is one fused guarded UPDATE (no check-then-write window); sweep refunds sequential on ONE transaction, fail-closed on unreadable lanes (re-read `refundHeldLaneToProvenance` → `StudentRepository.incrementLane`, parameterized). Auth scoping re-read at resolver+service depth: `$all` conjunctions on createSession(Student)/start/complete(Teacher)/resolve+adminDisputedSessions(Admin); cancel/dispute authenticated-only with the participant predicate service-side; cron route (out of span, traced for the sweeper path) = timing-safe bearer + fail-closed mode gates + bare 404 + masked errors — hardened. SQL: 0 LIKE/ILIKE, 0 `--`, 0 raw inserts; interpolation census zero-caller-input. Frontend: 0 module-level mutable state, 0 `let/var` in containers, slot book immutable as documented, 0 `style={`/hex/rgb, 0 `t('…')` literals (docblock examples only), 0 `useLazyQuery`, RTL-safe (symmetric-zero margins only), 0 fake data, 0 TODO/FIXME/console./`as any`/@ts-ignore across the span. Dead code: none found. Cross-layer imports: none in span files. Stale docblocks: none found beyond the two NEW observations above.

## (c) RECORDED / PRE-EXISTING re-confirmed (unchanged)

- RECORDED: D10 intent overlay (mutation :131, comment honest); D11 `oxlint-disable-next-line no-await-in-loop` still at service.ts:854 (deferred, not fixed — as F-4 recorded); R6's let-scan statement-position caveat (repo test :1222-1258); D1–D9 ledger rows.
- PRE-EXISTING (present at HEAD/8779acc): AGENTS.md other-bullet doc-prose refs (:4/:5/:75); service.test.ts plan refs (REQ-04x names, DEV3-005/012 describe titles, R-107 :1105, D2 comment :1674); jscpd clone `cancelSessionOnce`≅`openDisputeOnce`; jscpd clone in the TeacherWallet suite; pentest INFOs (unbounded list page, teacher-existence oracle); R5-FE items (chips group aria-label, raw intent-enum row title, single Snackbar slot); CancelSessionConfirmDialog dismissal-drift class (fixed 61c39b6, verified landed).
- Environment artifacts (unchanged, MUST be excluded from the fix commit): `tsconfig.json` M = provisioning rewrite (reformat + `.next-dev/**` includes); untracked `phase0-baseline-sandbox2-outcome.md` (and now this R7 outcome file + the R6 outcome file, plan docs).

## (d) Verdict

**R7 VERDICT: 2 new findings (0 blocking)** — 1 LOW (start-after-deadline sweep-bypass window; interaction-level, predicate itself PRE-EXISTING at 8779acc; future-ticket fix + missing test arm) + 1 INFO (no release path for completed-unconfirmed escrow rows; record-only). All fix-verification categories PASS: plan-artifact refs = 0 in the 5 swept code files (AGENTS.md bullet and service.test residuals are PRE-EXISTING out-of-mandate prose, inventoried above); docblocks honest; the NEW dynamic-import pin is honest and fail-closed; F-1 pins re-verified by independent recount; 12-field SDL inventory exact. Working tree remains ready to commit as the fix set — except `tsconfig.json` (provisioning artifact). R8 (if any) should sweep fresh against the committed fix set.
