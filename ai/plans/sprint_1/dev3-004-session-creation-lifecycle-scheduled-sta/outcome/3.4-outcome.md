# 3.4 Outcome — Registration, Codegen & Structural Gates

> Task: verify the Phase-3 side-effect barrel chains (3.2/3.3 wrote them — verified, NOT redone),
> regenerate the committed SDL artifact via `bun run generate:gqlSchema && bun codegen`, and assert the
> structural gates (allowlist byte-unchanged, A1 dynamic-import scan, A2 literal-enum scan, freeze-pin
> extension + codegen-sync) per gateway Rule 8 (`docs/graphql/api-gateway-and-routing.md` §8).
> _Requirements: REQ-060, REQ-061, REQ-074, REQ-076._
> Branch `feat/dev3-004-session-creation-lifecycle-scheduled-sta` · NO checkout/commit/push (orchestrator commits).

---

## 1. Files

| File | Action | Content |
|---|---|---|
| `backend/graphql/query/index.ts` | VERIFIED (3.2's additive edit) | Side-effect chain complete: `import "./classes";` → `query/classes/index.ts` → `import "./session-lifecycle.query";`. Import order alphabetical (`auth` < `classes` < `health` < `recitation` < `teachers`); docblock already names the classes (session-lifecycle) queries. Strictly additive diff (+1 import line, +2 docblock lines). |
| `backend/graphql/mutation/index.ts` | VERIFIED (3.3's additive edit) | Side-effect chain complete: `import "./classes";` → `mutation/classes/index.ts` → `import "./session-lifecycle.mutation";`. Docblock already names the lifecycle quartet. Strictly additive diff (+1 import line, +2 docblock lines). |
| `frontend/graphql/generated/schema.graphql` | REGENERATED (script, never hand-edited) | `bun run generate:gqlSchema` → 5545 bytes, **+57 lines**, exactly the DEV3-004 surface (§2). `bun codegen` ran clean; `frontend/graphql/generated/gql/graphql.ts` UNCHANGED (codegen output is document-driven — no frontend document consumes the new types until task 4.1, so there is zero gql output drift by design). |
| `backend/graphql/test/schema-surface.test.ts` | UPDATE (freeze pins extended — the ticket's declared RED→GREEN duty) | Added `DEV3_004_QUERY_FIELDS` / `DEV3_004_MUTATION_FIELDS` / `DEV3_004_TYPE_NAMES` pins mirroring the file's existing `PRE_3_1_*` / `DEV3_004_ENUMS` convention; mutation-freeze test re-titled "mutation set grows ONLY by the DEV3-004 lifecycle quartet" (baseline quartet remains pinned; `_health`-never-a-mutation pin kept); whole-schema named-type delta extended with `Session`, `SessionPage`, `CreateSessionInput`, `SessionListFilterInput`; codegen-sync belt-and-braces pins now assert all 7 root operations + 4 new type/input blocks inside the committed artifact. Docblock's surface-freeze section rewritten to describe the full post-3.4 posture. |

**NOT modified** (byte-unchanged, gate-a evidence): `backend/lib/gateway/public-operations.ts`,
`backend/lib/gateway/public-operations.test.ts`, `docs/graphql/api-gateway-and-routing.md`,
`frontend/graphql/test/gateway/allowlist-coverage.test.ts`, `backend/graphql/pothos/shared/enum.pothos.ts`
(and every file under `backend/graphql/query/` + `backend/graphql/mutation/` from 3.2/3.3 — gates b/c ran read-only).

## 2. SDL diff summary — `frontend/graphql/generated/schema.graphql` vs the 3.1 state (+57 lines, 5545 bytes)

**Exactly THIS ticket's surface; nothing else:**

Root operations (7):
| Root | SDL signature |
|---|---|
| Query.`sessionById` | `sessionById(id: ID!): Session` |
| Query.`myStudentSessions` | `myStudentSessions(filter: SessionListFilterInput, page: Int = 1, pageSize: Int = 25): SessionPage!` |
| Query.`myTeacherSessions` | `myTeacherSessions(filter: SessionListFilterInput, page: Int = 1, pageSize: Int = 25): SessionPage!` |
| Mutation.`createSession` | `createSession(input: CreateSessionInput!): Session!` |
| Mutation.`startSession` | `startSession(id: ID!): Session!` |
| Mutation.`completeSession` | `completeSession(id: ID!): Session!` |
| Mutation.`cancelSession` | `cancelSession(id: ID!, reason: String): Session!` |

Types (2): `type Session { … }` — exactly the 15 plan §3.1 fields (`id: ID!` … `updatedAt: DateTime!`;
`heldBalanceLane` structurally absent from the whole artifact) — and `type SessionPage { items: [Session!]!;
page: Int!; pageSize: Int!; totalCount: Int! }`.

Inputs (2, "as named" per plan §3.1): `input CreateSessionInput { intent: SessionIntent!; teacherId: ID! }`
and `input SessionListFilterInput { status: SessionStatus }`.

Enums (3 registrations): `SessionStatus`, `SessionType`, `SessionIntent` are present in the artifact —
they joined at task 3.1 (`shared/enum.pothos.ts` registers each exactly once, enum-object form); the 3.4
diff adds **zero** new enum blocks, and `session-sdl.test.ts` re-proves the exactly-once registration + member parity.

**Drift confirmation:** `git diff --stat` over the whole tree shows ONLY `tasks.md` (checkbox flips),
the two top-level barrels (3.2/3.3's verified additive edits), `schema-surface.test.ts` (pin extension), and
`schema.graphql` (+57). Within `frontend/graphql/generated/` exactly ONE file changed — `schema.graphql`;
`gql/graphql.ts` is untouched (no uncommitted document consumes the new types before 4.1). Zero unrelated drift;
the untracked 3.2/3.3 artifacts are that phase's pending change set, untouched by 3.4.

## 3. Gate results

| Gate | Verdict | Evidence |
|---|---|---|
| a. Allowlist-coverage (public operations byte-unchanged) | **PASS** | `git status --porcelain` over `backend/lib/gateway/`, `docs/graphql/api-gateway-and-routing.md`, `frontend/graphql/test/gateway/allowlist-coverage.test.ts` → EMPTY (byte-unchanged). `PUBLIC_OPERATION_NAMES` remains the frozen 6-member set (`login`, `refreshToken`, `logout`, `registerUser`, `recitationReadings`, `_health`) — none of the 7 new operations is allowlist material: every one carries `authScopes` (explicit `$all{authenticated, role:[…]}` on create/start/complete + the lists; `{authenticated: true}` on `sessionById`/`cancelSession`), per Rule 8.4 "either an `authScopes` set or — only for genuinely anonymous-by-design operations — an allowlist entry". Both coverage directions re-proven against the BUILT schema with the new surface: `frontend/graphql/test/gateway/allowlist-coverage.test.ts` **8 pass / 0 fail / 42 expect** (unscoped-fields ≡ allowlist; default-deny both roots) and `backend/lib/gateway/public-operations.test.ts` **26 pass / 0 fail / 52 expect** (frozen six-member freeze intact). |
| b. Gate A1 — zero `await import(` in resolver trees | **PASS** | `rg -n "await import\(" backend/graphql/query/ backend/graphql/mutation/` → **0 matches** (both trees). Top-level static imports only. |
| c. Gate A2 — zero literal-array enum registrations | **PASS** | `rg -n "enumType\(gqlSchemaBuilder\|values:" backend/graphql/pothos/shared/enum.pothos.ts` → **0 matches** (exit 1). All 8 registrations use the mandated enum-object form `gqlSchemaBuilder.enumType(<TSEnum>, { name })` incl. the DEV3-004 trio; the only `.enumType(` occurrences in `backend/graphql/pothos/` are this shared registry (9 rg hits = 8 registrations + 1 docblock exemplar) — no domain file re-registers any enum. Repo-level static-assertions gate A1–A5: `backend/lib/gateway/static-assertions.test.ts` **17 pass / 0 fail**. |
| d. Freeze pins + codegen-sync | **PASS** | `backend/graphql/test/schema-surface.test.ts` went **8 pass / 4 fail → 12 pass / 0 fail / 56 expect** after the pin extension (query-additions, mutation-quartet, enum-set, named-type-delta, and byte-identical codegen-sync all green; the 4 pre-existing REDs were exactly the declared 3.4 duty). |

## 4. Test / verification evidence (mandated runner, first-hand runs)

| Suite | Result |
|---|---|
| `backend/graphql/test/schema-surface.test.ts` | **12 pass / 0 fail / 56 expect** (was 8/4 RED before the pin extension) |
| `backend/graphql/test/session-sdl.test.ts` | **10 pass / 0 fail / 45 expect** |
| `frontend/graphql/test/gateway/allowlist-coverage.test.ts` | **8 passed / 0 failed / 42 expect** |
| `backend/lib/gateway/public-operations.test.ts` | **26 pass / 0 fail / 52 expect** |
| `backend/lib/gateway/static-assertions.test.ts` (A1–A5) | **17 pass / 0 fail / 20 expect** |
| `bun tsgo` (full repo) | **exit 0** |
| sub-loop `--lifecycle duplicates` on touched files | **exit 0 ×4**: `query/index.ts`, `mutation/index.ts`, `schema-surface.test.ts`, `schema.graphql` |

Out-of-scope note: `backend/graphql/test/session-lifecycle-mutations.test.ts` (3.3's live-boot REQ-064 suite —
spawns a next-server worker) is NOT a 3.4 gate; a courtesy re-run in this sandbox hit the same sandbox infra
wedge 3.3 already documented (worker spin-up → 240 s beforeAll timeout; zombie-held port), while its owner tier
was reported green first-hand at 3.3 close-out (35 pass / 0 fail). Not retried here to avoid polluting the gate
evidence with known-infra noise; 3.3's outcome §5 is the evidence of record.

## 5. Semantic review (3.4.SR) & instruction verification (3.4.IV)

- **Generated code never hand-edited:** `schema.graphql` produced solely by `scripts/generate-gql-schema.ts`
  (`printSchema(lexicographicSortSchema(...))` emission, byte-proven by the codegen-sync test); `gql/graphql.ts`
  solely by graphql-codegen. No manual keystrokes on either.
- **Barrel changes strictly additive:** each top-level barrel diff is +1 side-effect import line (alphabetical
  placement preserved) + docblock mention; no reorder, no deletion, no named exports.
- **Rule 8 conformance (all 5 clauses):** (1) resolver modules live in `query|mutation/classes/` registered via
  side-effect barrels, no dynamic import (gate b); (2) Pothos objects/inputs in `pothos/classes/` backed by
  `backend/types/` return types, `Session` exposes `id` first for cache normalization; (3) enums registered
  EXACTLY ONCE in `pothos/shared/enum.pothos.ts`, enum-object form, no `values:[…]` (gate c); (4) every operation
  carries `authScopes` — NO allowlist entry warranted, allowlist byte-unchanged (gate a); (5) codegen re-run in
  the SAME change set with zero unrelated drift (§2).

## 6. Verdict & carry-forward

Task 3.4 CLOSED — all gates green first-hand; the committed SDL artifact now matches the production builder
byte-for-byte with exactly the seven operations + two object types + two inputs added over the 3.1 state.

Carry-forward:
- **4.1** owns the frontend `sharedDocuments/scheduling/` documents — they will drive the next `bun codegen`
  (types currently unused by documents; `gql/graphql.ts` will grow then — expected, sanctioned drift).
- The freeze pins (`DEV3_004_*` constants) are the extension points any post-DEV3-004 surface change must edit
  by name — the suite fails closed on unnamed additions in either direction.
- Nothing committed/staged by this task (orchestrator owns commits per protocol).
