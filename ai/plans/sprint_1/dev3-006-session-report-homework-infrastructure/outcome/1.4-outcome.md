# Task 1.4 Outcome — i18n keys: errors namespace + notification copy slots (en/ar parity)

> Plan dir: `ai/plans/sprint_1/dev3-006-session-report-homework-infrastructure` · Task 1.4 of `tasks.md` · Requirements: REQ-002, REQ-016, REQ-018, REQ-019
> Branch note: orchestrator brief said `feat/dev3-006-session-report-homework` was checked out; the sandbox worktree is actually on `main` @ `79acb01` (which contains that branch's tip commit `ffce457` plus the phase-0 outcomes commit). Per instructions NO branch switch and NO commit were performed; all edits are left uncommitted in the worktree.

## 1. What was done

Extended the two existing compile-time i18n namespaces (no new namespace — `shared/AGENTS.md` registration checklist items 3–5 already wired and re-verified: `errors.errors` handle at `shared/locale/namespaces/errors/errors.namespace.ts:4`, `notifications.notifications` at `shared/locale/namespaces/notifications/notifications.namespace.ts:4`, `Translations` slices at `shared/locale/types/message.ts:21,31`):

1. **`errors` namespace** — 9 new FLAT top-level domain-prefixed string keys on `ErrorsLabels` + both leaf maps (en/ar).
2. **`notifications` namespace** — 3 new copy slots (1 string title + 2 function-valued bodies) on `NotificationsLabels` + both leaf maps, function-valued placeholder style matching the existing event bodies.
3. **Parity suites updated** so the new keys are mechanically pinned (notifications suite inventory 49→52 slots, function slots 14→16; errors suite gained a domain-key pin block).
4. `Translations` (`shared/locale/types/message.ts`) needed NO edit — it references `ErrorsLabels`/`NotificationsLabels` by type, so the new keys propagate transitively.

Ground-truth re-verified before editing (per 0.2 item 19): `getServerTranslations(locale)` takes exactly ONE argument (`shared/locale/server-graphql.ts:3-5`); leaf maps are typed consts; flat property access is the consumption contract.

## 2. Per-candidate audit verdicts (errors namespace)

Full audit of `ErrorsLabels` (51 pre-existing keys incl. grouped `planCatalog`/`adminUsers`) against each candidate. The only generic transport keys are `validation` ("Invalid input.") — used by guards for id-shape denials (e.g. `assertPositiveSafeSessionId` → `ValidationError(t.validation)`) — and `duplicateRequest` ("This value is already in use."). NO generic required/length/range key exists; domain-specific validation keys are the established precedent (`planTitleRequired`, `planTitleTooLong`, `broadcastTitleInvalid`, `walletInvalidAmount`, `suspensionPeriodInvalid`).

| Candidate | Verdict | Rationale |
|---|---|---|
| `sessionReportAlreadyExists` | **new** | No key expresses one-report-per-session conflict; plan-of-record names this exact key for `ConflictError("SESSION_REPORT_ALREADY_EXISTS", …)`. `duplicateRequest` is a generic envelope string, wrong semantics for a domain conflict. |
| `homeworkAlreadyGraded` | **new** | No key expresses the one-shot grade-window reject. |
| `sessionReportNotesRequired` | **new** | No generic "required" key; `planTitleRequired`/`idempotencyKeyRequired` are other domains (synonym-minting across domains prohibited). |
| `sessionReportNotesTooLong` | **new** | No generic max-length key; `planTitleTooLong` is plan-domain. |
| `sessionRatingRange` | **new** | No 0..5 range key exists. |
| `homeworkGradeRange` | **new** | No 0..100 range key exists. |
| `homeworkAyahRangeInvalid` | **new** | No ayah-range/positive-safe-integer key with user copy exists. |
| `homeworkSurahJuzInvalid` | **new** | No surah/juz vocabulary key exists (the enum guard itself is task 1.3's deliverable). |
| `homeworkAssignmentBlocksRequired` | **new** | `planPatchEmpty`/`userPatchEmpty` are "no fields supplied" semantics in other domains — not reusable. |

**Zero reuses** were possible without minting cross-domain synonyms; generic `validation` remains untouched and continues to serve id-shape denials. No existing key was modified or removed.

## 3. Files changed (8, all under `shared/locale/`)

| File | Anchor | Change |
|---|---|---|
| `shared/locale/types/errors/labels.ts` | :165-182 | 9 new `readonly` string keys on `ErrorsLabels` with doc comments |
| `shared/locale/en/errors/index.ts` | :82-91 | 9 en copy strings |
| `shared/locale/ar/errors/index.ts` | :81-90 | 9 ar copy strings |
| `shared/locale/types/notifications/index.ts` | :160-176 | "Session-report-ready event copy" section: title string + 2 function-valued bodies with privacy doc comments |
| `shared/locale/en/notifications/index.ts` | :66-69 | 3 en copy slots |
| `shared/locale/ar/notifications/index.ts` | :67-70 | 3 ar copy slots |
| `shared/locale/notifications-namespace.parity.test.ts` | :103-105 (MANDATED_KEYS +3), :143-144 (FUNCTION_KEYS +2), :181-182 (sample args), :334-364 (new template/privacy pins) | inventory 49→52 slots; function slots 14→16; count comments updated |
| `shared/locale/errors-namespace.parity.test.ts` | :27-30 (header item 5), :125-166 (`SESSION_REPORT_HOMEWORK_KEYS` block) | new pin block for the 9 domain-service keys (route.ts discovery cannot see service-tier consumers) |

Non-repo environment note: `.env.test` was created (gitignored, per `.env.example` "Test environment" instructions, copied from the repo's PGlite-posture `.env`) because the mandated runner hard-fails without it. It is a local test-env artifact, not a source change.

## 4. Final key list + copy (en/ar)

### errors namespace (FLAT; consumption `t.errorsTranslations.<key>`)

| Key | en | ar |
|---|---|---|
| `sessionReportAlreadyExists` | A session report has already been submitted for this session. | تم تقديم تقرير لهذه الجلسة بالفعل. |
| `homeworkAlreadyGraded` | This homework has already been graded. | تم تصحيح هذا الواجب بالفعل. |
| `sessionReportNotesRequired` | Session report notes are required and cannot be empty. | ملاحظات تقرير الجلسة مطلوبة ولا يمكن تركها فارغة. |
| `sessionReportNotesTooLong` | Session report notes cannot exceed 2000 characters. | يجب ألا تتجاوز ملاحظات تقرير الجلسة 2000 حرف. |
| `sessionRatingRange` | The session rating must be a whole number between 0 and 5. | يجب أن يكون تقييم الجلسة رقماً صحيحاً بين 0 و5. |
| `homeworkGradeRange` | Homework grades must be whole numbers between 0 and 100. | يجب أن تكون درجات الواجب أرقاماً صحيحة بين 0 و100. |
| `homeworkAyahRangeInvalid` | Homework verse numbers must be positive whole numbers, and the starting verse cannot come after the ending verse. | يجب أن تكون أرقام الآيات في الواجب أرقاماً صحيحة موجبة وألا يكون رقم الآية الأولى أكبر من رقم الآية الأخيرة. |
| `homeworkSurahJuzInvalid` | Select a valid surah or juz for the homework assignment. | اختر سورة أو جزءاً صالحاً للواجب. |
| `homeworkAssignmentBlocksRequired` | Provide at least one homework assignment block (new portion or revision). | يجب تحديد جزء الحفظ أو جزء المراجعة على الأقل في الواجب. |

### notifications namespace (placeholders are function-valued)

| Key | Signature | en | ar |
|---|---|---|---|
| `eventSessionReportReadyTitle` | `string` | Session report ready | تقرير الجلسة جاهز |
| `eventSessionReportReadyBody` | `(teacherName: string) => string` | `${teacherName} submitted a report for your session.` | `قدّم ${teacherName} تقرير جلستك.` |
| `eventSessionReportReadyParentBody` | `(studentName: string, teacherName: string) => string` | `${teacherName} submitted a session report for ${studentName}.` | `قدّم ${teacherName} تقرير جلسة ${studentName}.` |

Arabic register matches the file's existing style (verb-first `قدّم X …` mirroring `قبل X طلب جلستك.`; domain terms الجلسة/الواجب/الآيات/سورة/جزء/الحفظ/المراجعة consistent with `intentHifz`="الحفظ", dashboard `homework`="الواجبات").

## 5. Quality loop (P2) — `bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates`

All exit 0. Sample tail line per run (each run prints per-check ✅ for oxlint, biome:check, lint:type-aware, check:duplicates):

```
✅ All checks for lifecycle "duplicates" passed for shared/locale/types/errors/labels.ts            EXIT=0
✅ All checks for lifecycle "duplicates" passed for shared/locale/en/errors/index.ts                EXIT=0
✅ All checks for lifecycle "duplicates" passed for shared/locale/ar/errors/index.ts                EXIT=0
✅ All checks for lifecycle "duplicates" passed for shared/locale/types/notifications/index.ts      EXIT=0
✅ All checks for lifecycle "duplicates" passed for shared/locale/en/notifications/index.ts         EXIT=0
✅ All checks for lifecycle "duplicates" passed for shared/locale/ar/notifications/index.ts         EXIT=0
✅ All checks for lifecycle "duplicates" passed for shared/locale/notifications-namespace.parity.test.ts EXIT=0
✅ All checks for lifecycle "duplicates" passed for shared/locale/errors-namespace.parity.test.ts   EXIT=0
```

## 6. Test engineering (P3) — mandated runner results

| Suite | Result |
|---|---|
| `bun run test/scripts/run-test.ts shared/locale/errors-namespace.parity.test.ts` | **18 pass / 0 fail** (176 expect calls) — includes new `session-report/homework service keys` describe (9 `test.each` rows + exhaustiveness pin) |
| `bun run test/scripts/run-test.ts shared/locale/notifications-namespace.parity.test.ts` | **105 pass / 0 fail** (616 expect calls) — includes new slots in MANDATED_KEYS, 16 function slots, template + privacy pins |
| Regression: `applicant-namespace` / `parentLink-namespace` / `analytics-namespace` / `adminBroadcasts-namespace` | 11 / 58 / 90 / 59 pass, 0 fail each |

Mechanical en/ar key parity: both suites' "identical sorted key sets" tests green; interpolation arity pinned per new slot by (a) the compile-time `NotificationsLabels` signatures (identical param lists in both typed leaf maps — missing/mismatched arity fails `tsgo`), and (b) `FUNCTION_SLOT_SAMPLE_ARGS` callable pins (`eventSessionReportReadyBody` 1 arg, `eventSessionReportReadyParentBody` 2 args, both locales). A new privacy pin asserts the rendered new copy in BOTH locales contains no grade/note/rating wording and no digits.

## 7. Type-check status (tsgo)

- Scoped: every one of the 8 edited files passed the sub-loop's scoped tsgo step (section 5).
- Full `bun run tsgo`: **9 errors — ALL pre-existing to this task and located in sibling-task files that are uncommitted in the shared worktree**: `backend/enum/shared/surah-juz-ref.enum.test.ts` (6× TS2769) and `scripts/verify-unique-constraints.ts` (3× TS2352). Neither file imports anything from `shared/locale` (verified: the test imports only `bun:test` + `@/backend/enum/shared/surah-juz-ref.enum`; the script imports only `@/scripts/dbActions/envFile`), and `git status` confirms they belong to task 1.3's in-flight changeset. **Zero errors are attributable to Task 1.4's diff** (tsgo is project-wide; no error appears in any `shared/locale` file). The 0-error floor will be restored by whichever task owns those files.

## 8. Security & tenancy audit (1.4.SEC)

- Notification bodies interpolate ONLY counterparty full names: static review of the two new placeholder signatures — `(teacherName: string)` and `(studentName: string, teacherName: string)`. No id/grade/note/timestamp parameter exists in any new copy string; suite-level regex pin proves no grade/note/rating wording and no digits in rendered copy (en+ar).
- Error messages are generic and value-free (no user input echoed); `sessionReportAlreadyExists` is actor-scoped conflict copy (the submitting owner), mirroring the existing `trialAlreadyGranted`/`teacherAlreadyCertified` precedent — read-path existence-oracle posture (foreign ≡ nonexistent) is untouched and no new read-side key was introduced.
- No hardcoded strings introduced outside the locale maps (diff confined to the 8 files).

## 9. Semantic review (1.4.SR)

- Flat key naming honored: all new `errors` keys are top-level, domain-prefixed camelCase (no new nested groupings); notification slots top-level `event*`-prefixed (the "exactly SEVEN `type*` labels" pin stays green).
- No duplicated copy across keys: every new string is unique; generic `validation`/`duplicateRequest` untouched; no synonym minted (audit section 2).
- Interfaces compile; `ErrorMessageKey` mapped type remains coherent (all new keys are string-valued).
- Zero plan-artifact references in code comments (regex scan over all 8 files: no `REQ-`, `DEV3`, plan/task-file mentions).

## 10. Instruction verification (1.4.IV) — `shared/AGENTS.md` namespace checklist

| Checklist item | Status |
|---|---|
| 1. Interface in `shared/locale/types/<namespace>/` | N/A-new / ✅ extended in place (`types/errors/labels.ts`, `types/notifications/index.ts`) |
| 2. Implementations in `ar/` + `en/` leaf maps | ✅ both maps, both namespaces |
| 3. Export in `types/message.ts` (`Translations`) | ✅ already exported (`errorsTranslations`:21, `notificationsTranslations`:31) — no edit needed |
| 4. `namespacePaths` / server wiring | ✅ already wired for both namespaces (no new namespace) |
| 5. LocaleProvider (layout SSR) | N/A — no new namespace; client handle unaffected |
| `@/` alias + layer isolation | ✅ no new imports added at all |

## 11. Carry-forward (exact contract for downstream tasks)

- Services (task 2.6-2.8) call `getServerTranslations(locale)` (ONE arg) then read `t.errorsTranslations.sessionReportAlreadyExists`, `t.errorsTranslations.homeworkAlreadyGraded`, and the seven validation keys listed in section 4 — flat property access, no invented groupings.
- Notification seam (`notifySessionReportReady`) composes per recipient: student → `notificationsTranslations.eventSessionReportReadyTitle` + `eventSessionReportReadyBody(teacherFullName)`; linked parent (INV-P1 gate) → same title + `eventSessionReportReadyParentBody(studentFullName, teacherFullName)`; names bidi-isolated by the sender before interpolation (matching the `eventParentLinkExpiringBody` precedent).
- Sibling-task discrepancy: the working tree carries uncommitted task 1.1-1.3 changes and `main` (not the feature branch) is checked out; orchestrator should reconcile branch state before commit.

## 12. Residual risks

- The 9 sibling-task tsgo errors (section 7) must be cleared by task 1.3's owner before the phase gate; they do not mask any Task 1.4 error (none exist in `shared/locale`).
- `.env.test` is gitignored/local; other agents in this sandbox will find it already materialized (PGlite posture, `TEST_CI=false`).
