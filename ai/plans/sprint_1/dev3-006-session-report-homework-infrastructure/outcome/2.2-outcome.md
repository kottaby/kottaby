# Task 2.2 Outcome — ReportRepository

**Plan:** `ai/plans/sprint_1/dev3-006-session-report-homework-infrastructure` · Task 2.2
**Requirements:** REQ-010, REQ-040, REQ-060
**Repo state:** `main`, shared worktree, NO branch switch, NO commit. Scope files: `backend/db/repo/classes/report.repository.ts` (NEW, predecessor-authored, audited + 1 test fix), `backend/db/repo/classes/index.ts` (barrel, +2 lines), `backend/db/test/repo/classes/report.repository.test.ts` (NEW, predecessor-authored; 1 lint fix applied this session). No other file touched by this task.

---

## 1. Audit verdict

**PASS — predecessor's implementation conforms to plan §4.1 and the repo conventions; completion work this session was verification + 2 in-file fixes (both inside task-scope files).**

### What the predecessor did right (verified line-by-line)

- **§4.1 signatures exact** (`report.repository.ts:61,83`):
  - `export async function insertReport(insert: ReportInsertType, tx?: DBTransaction): Promise<ReportSelectType>`
  - `export async function findBySessionId(sessionId: number, tx?: DBQueryExecutor): Promise<ReportSelectType | null>`
- **Namespace convention** (`backend/db/repo/AGENTS.md` §File Organization): one `export namespace ReportRepository` per file; canonical export.
- **tx LAST everywhere; executor discipline per house pattern**: writes on `tx ?? db` (`:62`), tx reads as Drizzle select (`:85`), standalone reads as raw parameterized SQL via `queryDb` (`:88-95`, Neon-HTTP-eligible — the `backend/db/repo/AGENTS.md` mandate for non-transactional reads). Shape-identical to `SessionRepository` (0.2 item 21).
- **Single statements, no business logic**: `INSERT … RETURNING` and one id-equality read; NO validation, NO error-class translation, NO SELECT-then-INSERT guard — the `reports_session_id_unique` 23505 surfaces raw so the service (2.7) owns the `SESSION_REPORT_ALREADY_EXISTS` mapping (doc comment `:55-57`).
- **No `--` sequences in any sql template; no prepared statements; no array-membership operators; no `SELECT *`** — all pinned by the suite's static source pins AND independently re-verified by grep this session.
- **Canonical types only** — `ReportInsertType`/`ReportSelectType` via `@/backend/types` (task 1.3's file, `backend/types/classes/report.types.ts:5`); `DBTransaction`/`DBQueryExecutor` via `@/backend/types` (0.2 item 21 anchors).
- **Test suite honors `backend/db/test/AGENTS.md`**: everything under `runInRollback`; `tx` passed to every repo/entity call; constraint probes via `expectRepoError` inside an explicit `savepoint … rollback to savepoint` bracket (no `rejects.toThrow` deadlock anti-pattern); standalone (`queryDb` + pool-fallback) branches covered by a separate committed-fixture describe with `afterAll` hard-delete in FK order (rule 9); fixtures via `entity-setup.ts` helpers (rule 15); Tier 1-4 map matches tasks.md.

### Findings & fixes applied this session

| # | Finding | Severity | Fix |
|---|---|---|---|
| F1 | `report.repository.test.ts:324` — flatten helper used `signature.replace(/\s+\)/g, ")")`; type-aware lint flags `sonarjs/super-linear-regex` on `\s+\)` → sub-loop `lint:type-aware` FAILED (EXIT=1). | blocker (QL gate) | **Fixed in place:** replaced with `.replace(/ \)/g, ")")` — behavior-identical because the preceding `.replace(/\s+/g, " ")` collapses every whitespace run to one space, so `\s+\)` and ` \)` match exactly the same positions. Sub-loop re-run EXIT=0. |
| F2 | (cross-file note, no action) `bun run tsgo` was transiently red from `test/workflows/classes/session-report-homework.journey.test.ts` (task 2.1, other agent, actively edited during this session). Final state at close of this task: tsgo EXIT=0 project-wide. No fix applied from here per the fix-or-report rule. | report-only | Reported to orchestrator; the sibling agent owns that file. |

## 2. Barrel updates

`backend/db/repo/classes/index.ts` — exactly 2 additive lines, alphabetical:

```diff
+export * from "./home-work.repository";
+export * from "./report.repository";
 export * from "./session.repository";
```

Root `backend/db/repo/index.ts`: **unchanged** — already re-exports `./classes` (`:20`), so consumers get `ReportRepository` from `@/backend/db/repo` with zero edits (plan §4.1 "unchanged if it re-exports the classes barrel" — verified).

## 3. Verification evidence

### 2.2.QL — `bun run scripts/health/sub-loop.ts backend/db/repo/classes/report.repository.ts --lifecycle duplicates` → **EXIT=0**

```
✅ tsgo passed (no errors for backend/db/repo/classes/report.repository.ts)
✅ oxlint passed
✅ biome:check passed
✅ lint:type-aware passed
✅ check:duplicates passed
✅ All checks for lifecycle "duplicates" passed for backend/db/repo/classes/report.repository.ts
```

Test file: same lifecycle → **EXIT=0** (after F1 fix).

**Discovery output (verbatim):**

```
━━━ Applicable Rule Files ━━━

Instruction files (read before fixing):
  ⚠  .github/instructions/backend.instructions.md

AGENTS.md files (read before fixing):
  ✓  AGENTS.md
  ✓  backend/db/repo/AGENTS.md
  ✓  backend/AGENTS.md

Fix-Or-Report Rule:
  • If a rule violation can be fixed within THIS file: fix it.
  • If fixing requires modifying ANOTHER file: do NOT modify that file.
    Report the cross-file dependency to the orchestrator instead.
```

(Known cosmetic quirk per 2.5 outcome: tool prints the legacy `.github/instructions/…` path; validated against the real `.agents/instructions/backend.instructions.md`, read in full this session, plus the two AGENTS.md files.)

### 2.2.TE — mandated runner

```
bun run test/scripts/run-test.ts backend/db/test/repo/classes/report.repository.test.ts
→ 14 pass / 0 fail · 63 expect() calls · EXIT=0
```

Tier coverage (all green):
- **Tier 1:** happy insert + exact RETURNING shape (all 6 `$inferSelect` keys pinned locale-sorted); insert with omitted nullable columns (schema defaults); `findBySessionId` hit + miss(null); full column mapping on read-back.
- **Tier 2:** maximal 2000-char notes stored verbatim; rating 0 and 5 through the raw repo (CHECK backstop holds; service-level range validation is 2.5's `assertRating0To5`).
- **Tier 3 (chaos):** duplicate insert for the same session inside a `savepoint` bracket → `expectRepoError` returns the raw driver error; `hasPostgresErrorCode(err, "23505")` + `constraintNameOf(err) === "reports_session_id_unique"`; error class pinned NOT `ConflictError`/`DomainError` (repo does not translate — the service owns `SESSION_REPORT_ALREADY_EXISTS`); savepoint rollback leaves the winner's row queryable.
- **Tier 4 (security):** hostile notes payload (`Robert'); DROP TABLE reports;--`, quotes, backticks, `%like%`, `_underscore_`, RTL overrides, unicode, newlines/tabs) round-trips byte-identical through insert AND read; static source pins — `session_id = $1` bound parameter present, no `SELECT *`, no `.prepare(`, no `sql.placeholder`, no `inArray`, no `sql.raw`, no `--`, no i18n/logger/console, one namespace, no plan-artifact references (`REQ-\d|DEV3|Phase \d|Task \d|plan\.md|tasks\.md|specs\.md` absent).
- **tx propagation:** insert under `runInRollback` disappears after the block, observed via a fresh `db` executor read (rule 1's observable rollback proof).

**Coverage** (`bun run test:db:coverage` — the wired entry point; `run-test.ts` has no coverage flags, and raw `bun test` is blocked by the runner guard):

```
backend/db/repo/classes/report.repository.ts   |  100.00 |  95.65
```

**100% statements.** Branch residual (95.65%) = the defensive `if (!row) throw` guard in `insertReport` — unreachable by SQL (`INSERT … RETURNING` always returns the inserted row) and kept for the identical house shape `SessionRepository.insertSession` ships. Every reachable branch (tx read / standalone read, hit / miss) is exercised.

### Type check / lint floors

- `bun run tsgo` project-wide: **EXIT=0, 0 errors** at close of this task (the transient journey-test errors belonged to task 2.1's in-flight file, owned by the sibling agent).
- `bun run oxlint` project-wide: 0 warnings; the only 2 errors were in `test/workflows/classes/session-report-homework.journey.test.ts` (task 2.1's file) — zero in this task's scope.

## 4. 2.2.SEC — Security & Tenancy Audit

- **Bound parameters only:** the standalone read rides `$1` (`session_id = $1`); the tx-branch read is Drizzle `eq()` (parameterized); the insert is a typed `values(insert)` builder call. No string-concatenated SQL, no `sql.raw`, no `--`.
- **No LIKE/ILIKE surface:** grep-verified — zero `LIKE`/`ILIKE`/pattern operators in the module; the only predicate is id equality.
- **No cross-tenant predicate — justified:** the read is session-keyed, not caller-keyed. Participant scoping (foreign caller ⇒ `null`, oracle-collapsed per REQ-030) is the READ SERVICE's decision (2.7 `getSessionReport`), recorded in the method doc (`:79-81`). The repo is tenant-agnostic by design; the caller's userId never touches this module.
- **No BOPLA shape:** insert param is typed `ReportInsertType`; the service (2.7) maps `sessionId`/`teacherNotes`/`studentRatingByTeacher` field-by-field from validated state — the client cannot reach `reports.session_id` or any column through an unsanitized bag. There is NO `teacher_id` column to spoof (schema doc `reports.ts:14-16`).
- **No `*` select:** the standalone read lists all six columns explicitly (no schema-eviction surprises; `RETURNING` covers the tx branch).
- **Raw-error surface is disclosure-safe:** constraint failures propagate as driver errors whose message the transport finalizer masks (`maskInternalError` per the error-handling contract); no repo-level string is user-facing.

## 5. 2.2.SR — Semantic Review

- Atomic single statements (1 insert, 1 select); `tx` last-arg convention uniform; zero dead code (the `!row` guard is the house defensive shape, documented); naming matches `SessionRepository` conventions (namespace, `insert*/findBy*` verbs, `executor` local); zero `console.*`/logging/i18n imports; zero TODO/FIXME; zero plan-artifact strings in comments (regex-scanned); barrel `export *` per the barrel rules (relative `./` path, one `/` per path).
- Private `isDBTransaction` type guard (`:44-46`) narrows `DBQueryExecutor` → `DBTransaction` without an unsafe cast (`"select" in tx` — `PgAsyncTransaction` has the builder API, raw `Pool`/`PoolClient` do not).

## 6. 2.2.IV — Instruction Verification

- `.agents/instructions/backend.instructions.md`: repo = data-access only ✅; types from `@/backend/types` ✅; no hardcoded user-facing error strings (the single `throw new Error("ReportRepository.insertReport: …")` is the internal no-row invariant — the exact shape shipped by `SessionRepository.insertSession`, not a user-facing message; repo files are i18n-free by layer convention) ✅; no `console.*` ✅; no nested ternaries ✅.
- `.agents/instructions/tests.instructions.md`: `runInRollback` + `tx` everywhere ✅; `expectRepoError` (never `rejects.toThrow`) ✅; no seed-data reads ✅; mandated runner used ✅; no `any` ✅; **stale-pin rule honored** — pins updated in the same change as the source they pin (this session's regex fix kept the pins green).
- `backend/db/repo/AGENTS.md`: namespace-per-file ✅; sub-directory barrel updated ✅; `queryDb` for non-transactional reads ✅; types from `backend/types/` ✅.
- **Instruction nuance (recorded, mirrors 2.5's practice):** `backend.instructions.md` says simple read-only methods "MUST use" module-level prepared statements, while `backend/db/repo/AGENTS.md` (layer-specific) mandates `queryDb` for non-transactional reads and says module-level prepared statements "MUST be removed if replaced by `queryDb(tx)`". Plan §4.1 makes prepared statements merely *permitted* ("may use … id-equality reads qualify"). Followed the layer AGENTS.md + the shipped sibling shape (`SessionRepository`) — `queryDb`, no prepared statements. The suite pins the no-prepare invariant so the decision is re-visible.

## 7. Deviations

- **DV-1 (none blocking):** none against plan §4.1. Tasks.md's sketch named the test path `__tests__/report.repository.test.ts`; the verified house convention is `backend/db/test/repo/classes/*.test.ts` (where `SessionRepository`'s suite lives; `backend/db/test/AGENTS.md` §Layout) — followed the convention, consistent with how 2.5 recorded the analogous colocated-test deviation.
- **DV-2:** `findBySessionId` uses a runtime `isDBTransaction` guard rather than branching on `tx === undefined` because its `tx` is the wider `DBQueryExecutor` per plan §4.1 — the guard is what makes the widened executor type safe.

## 8. Carry-forward (contract for 2.7)

```ts
import { ReportRepository } from "@/backend/db/repo";
// or deep: "@/backend/db/repo/classes/report.repository"

ReportRepository.insertReport(insert: ReportInsertType, tx?: DBTransaction): Promise<ReportSelectType>
ReportRepository.findBySessionId(sessionId: number, tx?: DBQueryExecutor): Promise<ReportSelectType | null>
```

- `insertReport` does NOT catch 23505 — 2.7 wraps the call and maps `isUniqueViolation(error)` → `ConflictError("SESSION_REPORT_ALREADY_EXISTS", t.sessionReportAlreadyExists)` (0.2 item 7 helper).
- `findBySessionId` returns `null` for "no report"; participant-oracle collapse (foreign ≡ nonexistent) is 2.7's read-path responsibility.
- Persist `ReportInsertType` = `{ sessionId, teacherNotes, studentRatingByTeacher }` only — notes come from `assertTeacherNotes`' TRIMMED return (2.5 carry-forward).
