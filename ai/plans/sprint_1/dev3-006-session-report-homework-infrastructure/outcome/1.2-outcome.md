# Task 1.2 — Schema Amendments (push-only, bounded by 1.1 verdicts)

**Plan:** `ai/plans/sprint_1/dev3-006-session-report-homework-infrastructure`
**Repo state at start of task:** branch **`main`** @ `79acb01` (see ⚠ coordination note C1 below — the task brief expected `feat/dev3-006-session-report-homework` to be checked out; it exists but sits at `ffce457`, one commit behind `main`). All Task 1.2 changes are **uncommitted working-tree edits**; the orchestrator owns branch selection and commits.
**1.1 verdicts applied (bounds):** R1 = **ADD unique** (`reports`), R2 = **no-op** (`home_work` grades already nullable — untouched), R3 = **ADD unique** (`home_work`). R4/R5/R6 = confirmed, no action.
**drizzle-kit / drizzle-orm:** `1.0.0-rc.4` / `1.0.0-rc.4`.

---

## 1. Edits made (files changed)

| # | File | Change |
|---|---|---|
| 1 | `backend/db/schema/classes/reports.ts` | pg-core import gains `unique` (line 2); table-config array gains `unique("reports_session_id_unique").on(t.sessionId),` as first entry (line 36); doc comment lines 10–12 amended to state the now-true one-report-per-session posture (see deviation DV-2) |
| 2 | `backend/db/schema/classes/home-work.ts` | pg-core import gains `unique` (line 2); table-config array gains `unique("home_work_session_id_unique").on(t.sessionId),` as first entry (line 45); doc comment lines 9–10 amended to state one-row-per-session (DV-2) |
| 3 | `backend/db/schema/classes/report-homework-schema.static-assertions.test.ts` | NEW — static schema assertions suite (6 tests, no DB round-trip) |
| 4 | `backend/drizzle/20260907095925_remarkable_professor_monster/` | NEW — generated migration folder (`migration.sql` + `snapshot.json`) |
| 5 | `scripts/verify-unique-constraints.ts` | NEW — DB introspection probe (per task brief: saved under repo-root `scripts/`, not inside `backend/`) |

**NOT touched (verdict-bounded):** `currentGrade`/`revisionGrade` nullability (R2 no-op — no `.notNull()` added or removed); both grade CHECKs + rating CHECK (R5); `backend/db/schema/enums.ts`; the `surahJuzRef` columns; every zero-write table (`session`, `students`, `users`, `wallet`, `teacher_transaction`, `notifications`); no `uniqueIndex(...)` anywhere (house rule: `unique("<table>_<col>_unique").on(t.<col>)` per `recitation.ts:30` template).

### Verbatim diff (schema modules)

```diff
--- a/backend/db/schema/classes/home-work.ts
-import { check, index, integer, pgTable, timestamp } from "drizzle-orm/pg-core";
+import { check, index, integer, pgTable, timestamp, unique } from "drizzle-orm/pg-core";
- * Per-session homework assignment with two parallel tracks:
+ * Per-session homework assignment, one row per session: `session_id` is
+ * NOT NULL and UNIQUE (one-to-one with session). Two parallel tracks:
   t => [
+    unique("home_work_session_id_unique").on(t.sessionId),
     check("home_work_current_grade_check", ...

--- a/backend/db/schema/classes/reports.ts
-import { check, index, integer, pgTable, text, timestamp } from "drizzle-orm/pg-core";
+import { check, index, integer, pgTable, text, timestamp, unique } from "drizzle-orm/pg-core";
- * range). One report row per session is the typical pattern but the schema does
- * NOT mark session_id unique here, so multiple report revisions are
- * structurally allowed (the application layer enforces the one-per-session
- * invariant if desired).
+ * range). One report row per session: `session_id` is NOT NULL and UNIQUE
+ * (one-to-one with session) — a duplicate submission is rejected by the
+ * database itself.
   t => [
+    unique("reports_session_id_unique").on(t.sessionId),
     check(
       "reports_student_rating_by_teacher_check", ...
```

**Placement note:** 1.1's carry-forward said "insert BEFORE the existing index line"; both constraints sit as the FIRST config-array entry, which also precedes the index line and exactly mirrors the named template `recitation.ts:30` (`unique(...)` first, then checks/index). DDL output is order-identical either way (verified below).

---

## 2. Generated migration (name + SQL content)

Command: `bunx drizzle-kit generate --ignore-conflicts --config drizzle.config.ts` → exit 0.
**Folder:** `backend/drizzle/20260907095925_remarkable_professor_monster/`
Pre/post folder-listing diff shows ONLY this one new folder (no `meta/` journal appears — drizzle-kit rc.4 folders + `readMigrationFiles` folder-scan is the journal mechanism here).

Generated `migration.sql` (as emitted — inspected BEFORE apply, per the stop rule):

```sql
ALTER TABLE "home_work" ADD CONSTRAINT "home_work_session_id_unique" UNIQUE("session_id");--> statement-breakpoint
ALTER TABLE "reports" ADD CONSTRAINT "reports_session_id_unique" UNIQUE("session_id");
```

**Stop-rule check: PASSED** — exactly two `ADD CONSTRAINT ... UNIQUE` statements, on exactly `home_work(session_id)` and `reports(session_id)`, nothing else. `snapshot.json` delta likewise contains only the two new `uniques` entities (`:4608` `home_work_session_id_unique`, `:4630` `reports_session_id_unique`).

After apply, the repo's standard machinery (`ensureIdempotentMigrations`, invoked inside `migrate.ts`) rewrote the file in place to the house idempotent shape — this is automated tooling, not hand SQL:

```sql
DO $$ BEGIN
    ALTER TABLE "home_work" ADD CONSTRAINT "home_work_session_id_unique" UNIQUE("session_id");
EXCEPTION WHEN duplicate_object OR duplicate_table THEN NULL;
END $$;--> statement-breakpoint
DO $$ BEGIN
    ALTER TABLE "reports" ADD CONSTRAINT "reports_session_id_unique" UNIQUE("session_id");
EXCEPTION WHEN duplicate_object OR duplicate_table THEN NULL;
END $$;
```

**Apply:** `bun run backend/db/scripts/migrate.ts` → exit 0 — "Applying 1 pending Drizzle migration folder(s)… Applied migration: 20260907095925_remarkable_professor_monster … Migrations completed successfully!". (`bun db push` drizzle-kit-over-TCP is unusable in this sandbox — pre-ruled by `outcome/0-baseline-outcome.md` §Environment Provisioning: pglite provider → generate + programmatic migrate path. No `cleanGenerate`, no `reset`, no hand SQL.)

---

## 3. DB introspection evidence (pg_constraint / information_schema)

`bun run scripts/verify-unique-constraints.ts` → **exit 0**, five expectations PASS:

```
PASS  constraint "home_work_session_id_unique" on home_work: contype='u' (u = UNIQUE), definition='UNIQUE (session_id)'
PASS  constraint "reports_session_id_unique" on reports: contype='u' (u = UNIQUE), definition='UNIQUE (session_id)'
PASS  home_work.current_grade is_nullable=YES
PASS  home_work.revision_grade is_nullable=YES
PASS  reports.teacher_id: absent (as required)
[verify-unique-constraints] all expectations hold
```

Queries: `pg_constraint` ⋈ `pg_class` ⋈ `pg_namespace` with `pg_get_constraintdef` (catalog probe, contype `'u'` = UNIQUE, definition exactly `UNIQUE (session_id)`); `information_schema.columns` for grade nullability (R2 intact at the DB tier) and for `reports.teacher_id` absence (C.4 at the DB tier). All probes are read-only catalog SELECTs.

---

## 4. Static schema assertions test (1.2.TE Tier 1)

**File:** `backend/db/schema/classes/report-homework-schema.static-assertions.test.ts`

**Placement rationale (deviation DV-3):** the task brief's fallback path was `backend/db/schema/classes/__tests__/…`, but a house precedent DOES exist — colocated `*.static-assertions.test.ts` suites (`backend/types/classes/session.types.static-assertions.test.ts`, `backend/types/contracts/contracts.static-assertions.test.ts`, `backend/graphql/test/sdl-static-assertions.test.ts`); no `__tests__/` directory exists anywhere under `backend/`. Following the brief's primary directive ("pinned next to the repo test conventions"), the suite is colocated and named accordingly.

**Style source:** `backend/types/classes/session.types.static-assertions.test.ts` (bun:test, describe/test, no DB). Runtime metadata via Drizzle introspection only — `getTableConfig(table)` (`uniqueConstraints`, `checks`), `getColumns(table)` (keys + `notNull` flags), `getTableName`. NOTE: `getTableColumns` is **deprecated** in drizzle-orm rc.4 (`@deprecated — Use getColumns instead`, `utils.d.ts:50-54`); lint:type-aware rejects it, so the suite uses `getColumns`.

**Assertions (6 tests, 12 expect calls, 0 DB round-trips):**
1. `reports` carries `reports_session_id_unique`, targeting exactly `["session_id"]`.
2. `reports` column keys are exactly `{ createdAt, id, sessionId, studentRatingByTeacher, teacherNotes, updatedAt }` — **no `teacherId` key** (C.4 pin).
3. `reports` CHECK names exactly `["reports_student_rating_by_teacher_check"]` (R5a backstop unchanged).
4. `home_work` carries `home_work_session_id_unique`, targeting exactly `["session_id"]`.
5. `home_work.currentGrade.notNull === false` and `revisionGrade.notNull === false` (R2 pin).
6. `home_work` CHECK names exactly `["home_work_current_grade_check", "home_work_revision_grade_check"]` (R5b unchanged).

**Result (mandated runner):** `bun run test/scripts/run-test.ts backend/db/schema/classes/report-homework-schema.static-assertions.test.ts` → **6 pass / 0 fail / 12 expect() calls, exit 0** (fresh run after final edit; log `logs/2026-09-07T10-05-…/`).

**Tier 2 (boundary — push produced exactly the intended DDL):** satisfied by §2's pre-apply SQL inspection (exactly two UNIQUE ADDs, nothing else) + §3's post-apply catalog probes. No Tier 3/4 applicable to DDL itself (carried by repo/service tests in later phases, per tasks.md 1.2.TE).

---

## 5. Quality Loop (1.2.QL) — `--lifecycle duplicates`, per file

| File | Exit | tsgo | oxlint | biome | lint:type-aware | duplicates |
|---|---|---|---|---|---|---|
| `backend/db/schema/classes/reports.ts` | **0** | ✅ | ✅ | ✅ | ✅ | ✅ |
| `backend/db/schema/classes/home-work.ts` | **0** | ✅ | ✅ | ✅ | ✅ | ✅ |
| `backend/db/schema/classes/report-homework-schema.static-assertions.test.ts` | **0** | ✅ | ✅ | ✅ | ✅ | n/a (outside jscpd scope) |
| `scripts/verify-unique-constraints.ts` (extra, beyond mandated three) | **0** | ✅ | ✅ | ✅ | ✅ | ✅ |

Baseline counts (0.1: 0 errors/warnings on all four tools) **not raised by any Task 1.2 file**. Two intermediate failures were caught and fixed within the loop (both in the NEW files, never in the schema modules):
- oxlint `unicorn/no-array-sort` ×3 in the test → `.toSorted(...)` (also satisfying `sonarjs/no-alphabetical-sort` via `localeCompare` comparator).
- lint:type-aware `sonarjs/deprecation` ×3 (`getTableColumns` deprecated) → `getColumns`; `TS2352` row casts in the verify script → `db.execute<Row>()` generic with `type` literals (implicit index signatures), matching the house pattern `student.repository.ts:380` (`executor.execute<{ id: number }>(sql`…`)`); `no-await-in-loop` ×2 → single `Promise.all` fan-out.

### Discovery output (verbatim, for 1.2.IV)

`bun run scripts/health/sub-loop.ts backend/db/schema/classes/reports.ts --lifecycle duplicates` (identical mapping for `home-work.ts`; the test file additionally prints `.github/instructions/tests.instructions.md`):

```
━━━ Applicable Rule Files ━━━

Instruction files (read before fixing):
  ⚠  .github/instructions/backend.instructions.md

AGENTS.md files (read before fixing):
  ✓  AGENTS.md
  ✓  backend/AGENTS.md

Fix-Or-Report Rule:
  • If a rule violation can be fixed within THIS file: fix it.
  • If fixing requires modifying ANOTHER file: do NOT modify that file.
    Report the cross-file dependency to the orchestrator instead.

Oxlint Fix Patterns (see `docs/quality/linting-rules.md` for full details):
  • no-unsafe-type-assertion / no-await-in-loop / consistent-function-scoping /
    no-object-type-as-default-prop / no-unsafe-enum-comparison / no-shadow /
    consistent-return / no-map-spread / no-underscore-dangle
  • NEVER add oxlint-disable comments — fix the root cause
```

Discovery-path quirk (cosmetic, tooling): `sub-loop-rule-files.ts:34,44` hardcodes the LEGACY `.github/instructions/…` paths, which do not exist on disk (hence `⚠`); the actual files live at `.agents/instructions/backend.instructions.md` / `.agents/instructions/tests.instructions.md` and were validated against those. The layer map (`sub-loop-rule-files.ts:59-75`) has no `backend/db/schema/` entry, so `backend/db/schema/AGENTS.md` is not auto-printed — it EXISTS and was read and applied (Drizzle-only, no raw DDL, no reset/cleanGenerate, no new table/enum, schema-sync via the migrate path).

### Instruction verification (1.2.IV)

- **`.agents/instructions/backend.instructions.md`** — schema layer: Drizzle `pgTable` only ✓; no raw SQL DDL ✓ (generate → migrate; the in-place idempotency rewrite is repo machinery, not hand SQL); no barrel/index change required (no new module) ✓; no `console.*` in backend files ✓; Bun command discipline ✓; `db reset`/`cleanGenerate` not used ✓.
- **`backend/db/schema/AGENTS.md`** (exists; domain owner of both edited files) — "Schema Synchronization" rule followed via the sandbox-ruled migrate path (deviation DV-1, pre-recorded by 0.1 baseline); "Structural Ground Truth" respected (no new table/enum; `enums.ts` untouched) ✓.
- **`.agents/instructions/tests.instructions.md`** (printed for the test file) — suite is static/no-DB, so `runInRollback`/tx rules are vacuously satisfied; no `expect(...).rejects.toThrow()` ✓; no `getServerTranslations` ✓; no `any` ✓; `bun:test` only ✓; no seed-data reads ✓.
- **Root `AGENTS.md`** — test invocation via `bun run test/scripts/run-test.ts <path>` (never raw `bun test`) ✓; db-actions policy ✓.

---

## 6. Semantic Review (1.2.SR)

- **Dead imports:** none — `unique` consumed in both table configs (verified by reading final files + tsgo clean); test imports all used (`getColumns`, `getTableName`, `getTableConfig`, `PgTable`, `homeWork`, `reports`); verify script imports all used.
- **Diff minimal + verdict-scoped:** two files, two import tokens, two constraint lines; R2 nullability byte-identical to `ffce457` (absent from diff); CHECKs byte-identical; no `uniqueIndex` anywhere; no other table touched.
- **Naming:** `reports_session_id_unique` / `home_work_session_id_unique` — exact `recitation_session_id_unique` / `wallet_teacher_id_unique` / `users_email_unique` / `students_handshake_code_unique` convention; constraint names not re-spelled.
- **Commented-out columns / dead code / TODO/FIXME / console in backend code:** none (`rg` sweep for `REQ-\d|DEV3-\d|Task 1|tasks\.md|plan\.md|specs\.md` over all four files → zero matches; code comments are domain-language only).
- **Race conditions:** N/A for declarative DDL; the constraint itself IS the REQ-040 race arbiter (service-side cause-chain mapping lands in Phase 2/3, not here).
- **Scope boundary:** exactly the five files in §1; no checkbox flips in `tasks.md`; no commits.

## 7. Security & Tenancy Audit (1.2.SEC)

- **C.4:** no column added anywhere — teacher identity remains reachable ONLY via `reports.session_id → session.teacher_id`. Pinned three ways: schema diff (no new column), static test ("reports exposes exactly the report columns — no teacherId key"), DB probe (`reports.teacher_id: absent`).
- **Zero-touch tables:** `git diff` for Task 1.2 contains only the two schema modules; the generated DDL names only `reports` and `home_work`; `session`, `students`, `users`, `wallet`, `teacher_transaction`, `notifications` untouched (wallet/escrow constraint surface unaltered).
- **No redundant actor-identity exposure:** the only additions are two single-column UNIQUE constraints on an internal FK; no new writable field, no PII, no GraphQL surface change.

---

## 8. Deviations (all explained, none silent)

- **DV-1 — apply path:** `bun run db push` (tasks.md 1.2 wording) is impossible in this sandbox (drizzle-kit TCP vs pglite provider). Followed the pre-ruled path from `outcome/0-baseline-outcome.md` and 0.2 §24: `drizzle-kit generate --ignore-conflicts` → new folder under `backend/drizzle/` → `bun run backend/db/scripts/migrate.ts`, with pre-apply SQL inspection + post-apply catalog probes standing in for push-output inspection. Migration-journal discipline of `docs/DATABASE_MIGRATIONS.md` fully honored (no `cleanGenerate`, no `reset`, no hand SQL).
- **DV-2 — doc-comment amendments:** `reports.ts` previously stated "the schema does NOT mark session_id unique here, so multiple report revisions are structurally allowed" — factually false after the R1 edit. Amended both headers minimally to state the one-row-per-session truth (comment-only; drizzle-kit does not read comments; DDL verified unaffected).
- **DV-3 — test placement:** colocated `*.static-assertions.test.ts` per existing house convention instead of the brief's `__tests__/` fallback (which would have introduced a novel directory convention). Rationale in §4.
- **DV-4 — verify script kept in-tree:** `scripts/verify-unique-constraints.ts` saved per the task brief's explicit placement instruction (repo-root `scripts/`, not `backend/`), quality-looped, and reusable as a re-verification probe on fresh environments. The orchestrator may keep or drop it at commit time.

## 9. Coordination notes for the orchestrator (blocking decisions, NOT Task 1.2 work)

- **C1 — Branch mismatch:** Task 1.2 (like 1.1) executed on **`main`** @ `79acb01`; the feature branch `feat/dev3-006-session-report-homework` exists but is checked out nowhere and sits one commit behind (`ffce457`, missing the phase-0 outcomes commit). All 1.2 changes are uncommitted working-tree edits and will carry across a checkout. Orchestrator to decide branch/commit strategy.
- **C2 — Sibling-task files in the shared working tree:** `git status` shows Task 1.3/1.4 artifacts NOT owned by 1.2 (modified: `backend/enum/shared/surah-juz-ref.enum.ts`, `backend/types/classes/{index,report.types,session-notification.types}.ts`, `shared/locale/**`; untracked: `backend/types/classes/home-work.types.ts`, `report-home-work.types.{conformance.test-d,static-assertions.test}.ts`, `backend/enum/shared/surah-juz-ref.enum.test.ts`, `outcome/1.4-outcome.md`). A project-wide `bun run tsgo` currently reports **16 errors, ALL in 1.3-scoped in-flight files** (`report-home-work.types.conformance.test-d.ts` ×10, `surah-juz-ref.enum.test.ts` ×6) — **zero errors in any Task 1.2 file** (per-file sub-loop tsgo runs are clean). Baseline-floor attribution for those files belongs to their owning tasks.
- **C3 — Sub-loop discovery paths (cosmetic tooling):** `scripts/health/sub-loop-rule-files.ts` prints non-existent `.github/instructions/…` paths (`⚠`); real files live under `.agents/instructions/`. Fix owned by a tooling task if desired; no impact on rule content.

## 10. Carry-forward knowledge (for Phase 2+)

- **23505 arbiter is live:** `reports.session_id` now physically UNIQUE (`contype='u'`, definition `UNIQUE (session_id)`) — REQ-040's "unique constraint as arbiter, never SELECT-then-INSERT" posture is enforceable immediately; the service must traverse the Drizzle cause chain for `23505` with constraint name `reports_session_id_unique` and map to `SESSION_REPORT_ALREADY_EXISTS` (`isUniqueViolation` precedent).
- **INV-HW1 hardened identically:** `home_work_session_id_unique` enforces one assignment row per session at the DB tier; an assignment-only insert attempt for a session that already has one now fails 23505.
- **Nullability contract (R2):** `current_grade` / `revision_grade` are `is_nullable=YES` in the live DB and pinned nullable by the static suite — REQ-015's grade-routing (assignment ungraded → grade prior row later) has its schema prerequisite in place; nothing was relaxed (1.1 no-op honored).
- **CHECK backstops unchanged:** rating 0–5, grades 0–100 remain the last-resort DB guards behind service validation (REQ-016 ordering).
- **Introspection API note for future suites:** drizzle-orm rc.4 deprecates `getTableColumns` → use `getColumns`; `getTableConfig(t)` exposes `.uniqueConstraints` (`.name`, `.columns`), `.checks` (`.name`), `.columns` (`.notNull`); `.toSorted((a,b)=>a.localeCompare(b))` satisfies both `unicorn/no-array-sort` and `sonarjs/no-alphabetical-sort`.
- **Sandbox DDL recipe (repeatable):** edit schema → `bunx drizzle-kit generate --ignore-conflicts --config drizzle.config.ts` → inspect the new folder's `migration.sql` BEFORE applying → `bun run backend/db/scripts/migrate.ts` → catalog probes (`pg_constraint`/`information_schema`). `ensureIdempotentMigrations` rewrites the SQL in place at apply time (expected; do not "restore" it).

## 11. Residual risks

- The idempotency wrappers make a re-run of this migration a silent no-op (`duplicate_object` swallowed) — standard repo posture; acceptable because the catalog probes pin actual presence.
- `scripts/verify-unique-constraints.ts` hardcodes `.env` + `public` schema + the two constraint names; it is a ops-probe, not a library. If the orchestrator drops the file, §3's output is the durable evidence.
- Sibling-task tsgo errors (C2) mask a project-wide green until 1.3 lands its fixes; Task 1.2's own floors are clean.
