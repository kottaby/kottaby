# Task 3.4 — Codegen, schema-surface freeze & SDL pins

**Plan:** `ai/plans/sprint_1/dev3-006-session-report-homework-infrastructure`
**Repo state:** branch `main` @ `9ea52f9` + the uncommitted 3.1–3.3 working-tree edits; this task's edits are also uncommitted (orchestrator owns the commit). No commits made; `tasks.md` untouched.

## 1. Files (this task)

| File | Status | Contents |
|---|---|---|
| `frontend/graphql/generated/schema.graphql` | M (generated) | **+92 lines — EXACTLY the plan §3.1 additions, nothing else** (diff reviewed line-by-line, §6) |
| `backend/graphql/test/schema-surface.test.ts` | M | Baseline inventory: +`DEV3_006_*` constants (7 type names + 3 root fields), pinned into the 4 freeze tests + the codegen-sync belt-and-braces pins; header JSDoc updated; **one-time reconciliation** of pre-existing unpinned committed surfaces (`RECONCILED_*` constants, §3) |
| `backend/graphql/test/session-sdl.test.ts` | M | New `session-report surface` describe: 9 static SDL pins against the LIVE production schema (§4) |

`frontend/graphql/generated/gql/graphql.ts` is **byte-unchanged** — see §2/§7.

## 2. Codegen run + drift evidence (ZERO)

Ran the exact package.json scripts: `bun run generate:gqlSchema` → `scripts/generator/generate-gql-schema.ts`; `bun codegen` → `graphql-codegen --config codegen.ts`.

- `generate:gqlSchema` wrote `frontend/graphql/generated/schema.graphql` (23 387 bytes); `git diff --stat` = `1 file changed, 92 insertions(+)`.
- `bun codegen` regenerated `frontend/graphql/generated/gql/graphql.ts` **with zero output change**: documents exist only for the session-lifecycle ticket/005 surfaces (`frontend/graphql/sharedDocuments/scheduling/*.documents.ts`, admin/auth/billing/…), none references the new types, and the `typescript-operations` plugin emits only operation-referenced types (`rg "SessionReport|SessionHomeWork|SurahJuzRef|SubmitSessionReportInput" gql/graphql.ts` → no matches).
- **Drift check (deterministic emission):** md5 before re-run = after re-run for both artifacts — `schema.graphql e70167cf4ef365a1e851acfc9172e482`, `gql/graphql.ts 51127211f91fd954a6c868f404544165`; `git diff --stat` after re-run identical (`schema.graphql | 92 +++++`, no other file). Zero churn between consecutive runs; the only diff vs HEAD is the intentional planned +92.

## 3. Baseline inventory update (`schema-surface.test.ts`) — purely additive + one documented reconciliation

- **The sanctioned additions (the task's 7 names + root fields):** `DEV3_006_QUERY_FIELDS = ["sessionHomework","sessionReport"]`, `DEV3_006_MUTATION_FIELDS = ["submitSessionReport"]`, `DEV3_006_ENUMS = ["SurahJuzRef"]`, `DEV3_006_TYPE_NAMES = ["HomeWorkAssignmentInput","HomeWorkBlockInput","HomeWorkGradeInput","SessionHomeWork","SessionReport","SubmitSessionReportInput"]` — wired into the Query-freeze, Mutation-freeze, enum-freeze and whole-schema named-type-delta tests, plus 10 belt-and-braces `committedSdl` pins in the codegen-sync test.
- **Reconciliation (`RECONCILED_*`, same documented one-time pattern as the file's admin-surface note — not a silent baseline flip):** the suite was **already red before this task** — the committed-at-HEAD schema (verified present in `git show HEAD:frontend/graphql/generated/schema.graphql`, e.g. `adminAuditLogs` @597, `respondToParentLinkRequest` @395) carried surfaces never enumerated in the assertion arrays: parent-link (2 reads `myIncoming/OutgoingParentLinkRequests`, 3 mutations `requestParentChildLink`/`respondToParentLinkRequest`/`cancelParentLinkRequest`, `LinkStatus` enum, `Incoming/OutgoingParentLinkRequest` objects), admin audit-trail (`adminAuditLogs` + `AdminAuditLogEntry/FiltersInput/Page`), admin broadcast (`adminBroadcastNotification` + `AdminBroadcastNotificationInput`/`BroadcastAudienceInput`/`BroadcastAudienceType`), and `adminCertifyTeacherColdStart`. Re-anchored via `RECONCILED_ADMIN_*` / `RECONCILED_PARENT_LINK_*` constants so the whole-schema freeze the new additions ride on is honest. **No "-" (missing) entries existed** — drift was purely "unpinned committed surface", never "pinned but absent".
- **tasks.md discrepancy recorded:** tasks.md 3.4 says "the three input types"; plan §3.1 (authoritative) and the live schema have **four** inputs. The freeze semantics (ENTIRE schema) force all four; the parent task brief also says 4.

## 4. Session SDL pins (`session-sdl.test.ts`, new describe vs the LIVE `graphQLSchema`)

All assertions are EXACT shapes (no partial pins where full is possible):

1. `sessionReport` — **nullable** `SessionReport`, exactly one arg `sessionId: ID!`.
2. `sessionHomework` — **nullable** `SessionHomeWork`, exactly one arg `sessionId: ID!`.
3. `submitSessionReport` — **non-null** `SessionReport!`, exactly `(id: ID!, input: SubmitSessionReportInput!)` (2 args).
4. `SessionReport` — EXACT 6-field set (sorted set == `SESSION_REPORT_FIELD_ORDER`) with exact type strings: `id: ID!`, `sessionId: Int!`, `teacherNotes: String!`, `studentRatingByTeacher: Int!`, `createdAt/updatedAt: DateTime!` (registered scalar, never ISO-strings).
5. `SessionHomeWork` — EXACT 12-field set + exact type strings: both tracks nullable `Int`/nullable `SurahJuzRef` enum legs, `DateTime!` stamps.
6. **`id` FIRST** pinned at the Pothos SOURCE level (lexical scan of `report.pothos.ts` + `home-work.pothos.ts`, mirroring the Notification precedent — the built schema is lexicographically sorted so SDL order carries no semantics). Helper hoisted to module scope after an oxlint warning.
7. `SurahJuzRef` — registered exactly once in the emitted SDL + member/value parity vs the canonical TS enum (35 members; wire values are the enum KEYS `SurahAlFatihah…SurahAlMaidah`/`Juz1…Juz30` over the snake_case DB values).
8. The 4 input whitelists — CLOSED, exact member sets + exact type strings (`HomeWorkBlockInput{fromAyah:Int!,toAyah:Int!,surahJuz:SurahJuzRef!}`, `HomeWorkGradeInput{currentGrade:Int!,revisionGrade:Int!}`, `HomeWorkAssignmentInput{jadid,madi}`, `SubmitSessionReportInput{teacherNotes:String!,studentRatingByTeacher:Int!,homework,previousGrades}`).
9. Static SDL slice scan: no server-derivable field (`sessionId`/`teacherId`/`createdAt`/`updatedAt`) leaks into any input block.

## 5. Error-code registry — location recorded

**There is NO per-domain code registry file** (0.2 item 12): the typed registry is the frozen 9-category transport `ErrorCode` union (`backend/types/errors/api-error.types.ts:38-47`) + `ERROR_CODE_HTTP_STATUS`; custom domain codes are legal pass-through values that "ride typed error CLASSES and never resolve through the taxonomy" (`docs/graphql/error-handling-contract.md`, "Custom domain codes"). `docs/graphql/domain-error-extensions-code.md` is the **producer-side conventions doc** (subclass hierarchy + throw patterns) — it is NOT a per-code registry (sibling codes `SESSION_NOT_FOUND`/`SESSION_INVALID_TRANSITION` are not enumerated there either), so per the task's own rule the doc was **not** modified.

The registry that actually exists = the typed throw site. `SESSION_REPORT_ALREADY_EXISTS` is already declared there, in code, exactly like its siblings: `backend/services/classes/session-report.service.ts:246` and `:308` — `throw new ConflictError("SESSION_REPORT_ALREADY_EXISTS", t.sessionReportAlreadyExists)` (committed in `3680545`/`9ea52f9`), with the i18n key present in both locales (`shared/locale/en/errors/index.ts:82`, `shared/locale/ar/errors/index.ts:81`), the labels type (`shared/locale/types/errors/labels.ts:166`) and the parity test (`shared/locale/errors-namespace.parity.test.ts:137`). Verified; **nothing to edit** — the taxonomy union stays frozen and the doc stays append-only/untouched.

## 6. Security audit (3.4.SEC) — line-by-line schema-diff review

`git diff frontend/graphql/generated/schema.graphql` (+92) contains ONLY, in diff order: `input HomeWorkAssignmentInput{jadid,madi}`, `input HomeWorkBlockInput{fromAyah,surahJuz,toAyah}`, `input HomeWorkGradeInput{currentGrade,revisionGrade}`, `Mutation.submitSessionReport(id: ID!, input: SubmitSessionReportInput!): SessionReport!` (+ its description comment), `Query.sessionHomework(sessionId: ID!): SessionHomeWork`, `Query.sessionReport(sessionId: ID!): SessionReport`, `type SessionHomeWork` (12 fields), `type SessionReport` (6 fields), `input SubmitSessionReportInput` (4 members), `enum SurahJuzRef` (35 members). **= the plan §3.1 additions list exactly; zero unintended surface.** All three root fields are authScopes-gated (runtime metadata, absent from SDL by design); the public-operation allowlist stays byte-unchanged; both report objects expose `id` + exactly the plan columns (no `teacher_id` — the table has none); inputs carry no server-derivable field.

## 7. Verification results

| Check | Result |
|---|---|
| `bun run test/scripts/run-test.ts backend/graphql/test/schema-surface.test.ts` | **41 pass / 0 fail** (273 expects) |
| `bun run test/scripts/run-test.ts backend/graphql/test/session-sdl.test.ts` | **20 pass / 0 fail** (181 expects) |
| Codegen drift (re-run `generate:gqlSchema` + `codegen`; md5 + `git diff --stat`) | **ZERO** (§2) |
| `bun run tsgo` (project-wide) | **exit 0**, 0 `error TS` lines |
| `sub-loop.ts <file> --lifecycle duplicates` on BOTH hand-edited test files | **exit 0 on both** (tsgo/oxlint/biome:check/lint:type-aware/jscpd; generated artifacts excluded from duplicate claims per tasks.md, included in compile pass) |

**Semantic review (3.4.SR):** baseline additions purely additive (every change is a `+DEV3_006_*`/`+RECONCILED_*` spread or a new named constant; no existing pin weakened or removed — the reconciliation only ADDS names that the live schema provably carries at HEAD); SDL pins assert EXACT shapes (full field-set equality + exact type strings + exact arg sets + closed input member sets, not mere `toContain` presence).
**Instruction verification (3.4.IV):** `.agents/instructions/backend.instructions.md` §"GraphQL Schema Generation" (run both `generate:gqlSchema` then `codegen` after Pothos changes — done, in that order) and `backend/graphql/AGENTS.md` (enum registered ONCE in `shared/enum.pothos.ts`; regeneration keeps `@/frontend/graphql/generated/gql/graphql` in sync; DomainError conventions doc left intact) — honored.

## 8. Carry-forward (for 4.1)

- **Codegen pipeline:** 4.1 creates `frontend/graphql/sharedDocuments/scheduling/session-report.documents.ts` (glob `./frontend/graphql/sharedDocuments/**/*.ts`) → `bun codegen` then emits the operation types into **`frontend/graphql/generated/gql/graphql.ts`** (import alias `@/frontend/graphql/generated/gql/graphql`). Until documents exist that file stays byte-identical (`ignoreNoDocuments: true`).
- **Config mapping (codegen.ts):** `useTypeImports`, `avoidOptionals`, `enumType: "native"`, `strictScalars`, `DateTime: "string"` — so `createdAt/updatedAt` select to `string`; `SurahJuzRef` codegen values are the enum KEYS (`Juz1…Juz30`, `SurahAlFatihah…SurahAlMaidah`); optional input legs are `T | null` (avoidOptionals keeps explicit `?`-free fields).
- **Expected generated operation names** (from plan §6): `SubmitSessionReportMutation` + `SubmitSessionReportMutationVariables` for `submitSessionReportMutationDocument`; reader documents analogously (`SessionReportQuery`/`SessionHomeworkQuery` per 4.1's chosen operation names). Selection contract already pinned here: `id` FIRST on every object selection, `DateTime` stamps, enum fields select as `SurahJuzRef` members, no field beyond the object contract.
- **Schema freeze:** any 4.1-era schema change must extend the same `DEV3_006_*`/`RECONCILED_*` constants — the freeze is now honest and complete.
- The schema-surface suite's pre-existing red state (§3 reconciliation) is resolved at this task; later tickets must keep it green by pinning their own additions the same way.
