# Task 4.1 — Typed documents module + barrels + document contract tests

**Plan:** `ai/plans/sprint_1/dev3-006-session-report-homework-infrastructure`
**Repo state:** branch `main` on top of the 3.1–3.4 working-tree edits; this task's edits are also uncommitted (orchestrator owns the commit). No commits made; `tasks.md` untouched.

## 1. Files (this task)

| File | Status | Contents |
|---|---|---|
| `frontend/graphql/sharedDocuments/scheduling/session-report.documents.ts` | NEW | The DEV3-006 document trio (§2), sibling-style `TypedDocumentNode` constants, `gql` from `@apollo/client`, types from codegen |
| `frontend/graphql/sharedDocuments/scheduling/session-report.documents.test.ts` | NEW | Document contract suite — 11 tests / 60 expects (§4) |
| `frontend/graphql/sharedDocuments/scheduling/session.documents.ts` | M | Hub re-export + header split-map/operation-count updated (11 → 14 operations; DEV3-006 added to title) |
| `frontend/graphql/generated/gql/graphql.ts` | M (generated) | **+92 lines — exactly the codegen emission for the 3 documents** (6 operation types + compact payload types + `SurahJuzRef` enum + 3 typed-document-node consts); `schema.graphql` byte-unchanged from 3.4 (`e70167cf4ef365a1e851acfc9172e482`) |

**NO component, page, store, hook, nav, or view file touched.** Zero UI code (the plan D11 scope ruling was honored — no agent-browser loop run, deliberately, per tasks.md Phase 4 header).

## 2. Export names (the DEV3-006 document trio)

Defined in `frontend/graphql/sharedDocuments/scheduling/session-report.documents.ts`:

| Export | TypedDocumentNode type | Operation (wire) |
|---|---|---|
| `submitSessionReportMutationDocument` | `TypedDocumentNode<SubmitSessionReportMutation, SubmitSessionReportMutationVariables>` | `mutation SubmitSessionReport($id: ID!, $input: SubmitSessionReportInput!)` |
| `sessionReportQueryDocument` | `TypedDocumentNode<SessionReportQuery, SessionReportQueryVariables>` | `query SessionReport($sessionId: ID!)` |
| `sessionHomeworkQueryDocument` | `TypedDocumentNode<SessionHomeWorkQuery, SessionHomeWorkQueryVariables>` | `query SessionHomeWork($sessionId: ID!)` |

Selection sets (codegen-validated against the live schema; pinned by the contract test as EXACT `toEqual` equality):

- `submitSessionReport` / `sessionReport` → `SessionReport` row: `id, sessionId, teacherNotes, studentRatingByTeacher, createdAt, updatedAt` (6 fields, **`id` FIRST**).
- `sessionHomework` → `SessionHomeWork` row: `id, sessionId, currentFromAyah, currentToAyah, currentGrade, currentSurahJuz, revisionFromAyah, revisionToAyah, revisionGrade, revisionSurahJuz, createdAt, updatedAt` (12 fields, **`id` FIRST**; nullable legs ride as-is — codegen `Int | null` / `SurahJuzRef | null`).

Codegen mapping confirmed in the emitted types (`gql/graphql.ts:770-794`): `DateTime` → `string` (`DateTime: "string"` in `codegen.ts`), `SurahJuzRef` → native TS enum (wire-name members), optional query legs as `T | null` (`avoidOptionals`), roots nullable (`SessionReportQuery_sessionReport | null`) while the mutation payload is non-null — exactly plan §3.1.

## 3. Barrel updates — hub pattern (0.2 item 18 ruling), recorded decision

- **Root `frontend/graphql/sharedDocuments/index.ts`: UNTOUCHED** — verified it already re-exports the scheduling barrel (line 6, `export * from "./scheduling"`), so per tasks.md/plan §5 no change is warranted.
- **`scheduling/index.ts`: UNTOUCHED — deliberate.** The scheduling barrel's verified convention (0.2 outcome item 18: "index.ts:11 re-exports ONLY `./session.documents`; … DEV3-006 either adds a new `session-report.documents.ts` + one line in `session.documents.ts` (+ index untouched) … — **follow the hub pattern**") routes every sibling through the `session.documents.ts` re-export hub. The barrel surface still gains exactly the +3 exports (`submitSessionReportMutationDocument`, `sessionReportQueryDocument`, `sessionHomeworkQueryDocument`) — transitively via `index.ts → session.documents.ts → session-report.documents.ts` — and the hub's own invariant ("deep import path + export surface stay IDENTICAL") is preserved. Plan §5's "+3 exports on scheduling/index.ts" is satisfied on the barrel surface; the hub line is the concrete edit. Contract test pins barrel ≡ hub ≡ deep-import identity on all three exports (test `root barrel + scheduling barrel + hub re-export the SAME document instances`).
- **`session.documents.ts` (hub):** +1 re-export line + header JSDoc updated (title now "lifecycle + dispute + report (DEV3-004 + DEV3-005 + DEV3-012 + DEV3-006)"; operation count 11 → 14; split map gains the `session-report.documents.ts` row).

## 4. Document contract tests (`session-report.documents.test.ts`) — 11 pass / 0 fail

Mirrors the co-located sibling convention (`notification.documents.test.ts` / root `documents.contract.test.ts`): pure bun:test, zero server/DB/network, graphql AST kind-guard narrowing, no unsafe assertions. Suites:

1. **Named operations + channel + variables** (table loop) — `SubmitSessionReport`/`SessionReport`/`SessionHomeWork`, channel parity, variable definitions match the operation signatures exactly (`["id","input"]` mutation / `["sessionId"]` reads).
2. **Argument wiring** — every declared variable is threaded into its root-field argument (no dead variables, no literal arguments bypassing the variable contract).
3. **Variable-surface pin** — the whole variable surface is exactly the plan §3.1 signatures; no identity variables exist (identity is server-side).
4. **Closed selection contract** — every operation selects EXACTLY its one root field (no sibling surface); payloads select the EXACT 6/12-field contract rows with `id` FIRST (no over-fetch ⇒ SEC audit enforced by `toEqual` equality, not `toContain`).
5. **Enum legs are leaves** — `currentSurahJuz`/`revisionSurahJuz` select as `SurahJuzRef` enum members with NO sub-selection.
6. **Nullable-leg order** — current track legs then revision track legs in exact plan §3.1 order, `createdAt`/`updatedAt` last.
7. **Codegen binding** — compile-time proof by `TypedDocumentNode<…>` assignment against the generated operation types (tsgo gate) + `.loc` runtime uses.
8. **Barrel parity** — root barrel, scheduling barrel, hub and the defining module resolve to the SAME document instances (cache-key safety).

## 5. Apollo cache — NO-CHANGE DECISION (anchor)

**`frontend/providers/apollo/apolloCache.ts:37-119` (`createApolloCache()` typePolicies) — verified, ZERO edits.** Both `SessionReport` and `SessionHomeWork` carry `id` (schema `frontend/graphql/generated/schema.graphql:783/751`, selected first in every document) so Apollo normalizes them naturally; this surface has NO embedded/result-envelope types (both root fields return the object directly — no pagination wrapper, no scalar-only snapshot), so no `keyFields: false` registration is warranted and none was added. Matches plan §5 ("no registration change") and 0.2 item 18 ("No report/homework entry needed"). The `typePolicies` block stays exactly as of HEAD + 3.x edits.

## 6. Codegen refresh + drift (ZERO)

- `bun run generate:gqlSchema` → wrote `schema.graphql` (23 387 bytes) — byte-identical to the 3.4 artifact (md5 `e70167cf4ef365a1e851acfc9172e482`, no new schema diff).
- `bun codegen` → `gql/graphql.ts` +92 lines: `SubmitSessionReportMutation(+Variables)`, `SessionReportQuery(+Variables)`, `SessionHomeWorkQuery(+Variables)`, compact payload types, `SurahJuzRef` enum, and the 3 typed-document-node emission consts. **All 6 requested type names verified present.**
- **Drift check:** consecutive `bun codegen` re-runs → md5 `41ad87931099e37ee58f7c311b65ceaa` before = after; `git diff --stat` stable at exactly `gql/graphql.ts | 92 +++++` (the intentional planned refresh) and nothing else. The codegen run is also the schema-validation gate (`noSilentErrors: true`): all selection sets + variable usages validated against the live SDL — the "documents reference only codegen-known fields" gate is therefore both compile-time (tsgo) and codegen-time.

## 7. Verification results

| Check | Result |
|---|---|
| `bun run test/scripts/run-test.ts frontend/graphql/sharedDocuments/scheduling/session-report.documents.test.ts` | **11 pass / 0 fail** (60 expects) |
| `bun run test/scripts/run-test.ts frontend/graphql/sharedDocuments/documents.contract.test.ts` | **20 pass / 0 fail** (root structural lock unaffected by the hub edit) |
| Full sharedDocuments co-located sweep (10 suites incl. admin/billing/notifications/parents) | all green — zero `(fail)` lines |
| Codegen drift (re-run `bun codegen`; md5 + `git diff --stat`) | **ZERO** (§6) |
| `bun run tsgo` (project-wide) | **exit 0** — documents compile against the codegen types |
| `sub-loop.ts <file> --lifecycle duplicates` on documents module + hub + test | **exit 0 on all three** |

## 8. Semantic review (4.1.SR) + Security audit (4.1.SEC)

- **SR — zero UI code:** only documents + hub JSDoc + pure AST test; no component/page/store/hook/nav touched. **Zero hardcoded copy:** documents contain field names only; the test's string literals are contract pins (sibling pattern). **Naming matches siblings:** `gql` + `TypedDocumentNode` import from `@apollo/client` (never `/core`), `<entityName><Query|Mutation>Document` const convention, per-export JSDoc with the operation signature, types imported from `@/frontend/graphql/generated/gql/graphql`. **No dead exports:** the trio is the plan-sanctioned deliverable for DEV2-014 consumption; within this task they are exercised by the contract suite (operation/channel/wiring + barrel-identity pins), and the hub/barrel re-export keeps every consumer path live.
- **SEC — no over-fetch:** selection sets request NO field beyond the server contract (exact-equality pins + codegen schema validation; `sessionId`/`teacherId`-style server-derivable fields appear only where the contract includes them). **No bypass:** the ONLY `gql` template literals in the module are the three `TypedDocumentNode`-typed document definitions (the sanctioned pattern); the test contains no GraphQL strings at all. Mutation input rides the closed `SubmitSessionReportInput` whitelist via `$input` — no literal arguments anywhere.
- **IV (instruction verification):** `.agents/instructions/frontend.instructions.md` §"Apollo & GraphQL" (no `useLazyQuery` — none exists; hooks only in views — none here; `gql`/`TypedDocumentNode` from `@apollo/client`; `id` on all object selections; codegen both steps after document changes — done) + `frontend/graphql/AGENTS.md` (naming table, no inline type literals, barrel pattern, codegen section) + `frontend/graphql/sharedDocuments/AGENTS.md` (new-document checklist steps 1–4: file in matching sub-directory, hub re-export, root barrel verified, codegen run) — all honored. **Sub-loop discovery output:** `sub-loop.ts <file> --lifecycle duplicates` → `✅ All checks for lifecycle "duplicates" passed` for `session-report.documents.ts`, `session.documents.ts`, `session-report.documents.test.ts` (pasted in §7 runner results).

## 9. Carry-forward (exact import paths DEV2-014 will consume)

```ts
// Preferred (root barrel):
import {
  sessionHomeworkQueryDocument,
  sessionReportQueryDocument,
  submitSessionReportMutationDocument,
} from "@/frontend/graphql/sharedDocuments";
// Deep import (also valid):
import { submitSessionReportMutationDocument } from "@/frontend/graphql/sharedDocuments/scheduling/session-report.documents";
// Types for consumers (single codegen file):
import type {
  SessionHomeWorkQuery, SessionHomeWorkQueryVariables,
  SessionReportQuery, SessionReportQueryVariables,
  SubmitSessionReportMutation, SubmitSessionReportMutationVariables,
  SurahJuzRef,
} from "@/frontend/graphql/generated/gql/graphql";
```

- Hook usage in views: `useMutation(submitSessionReportMutationDocument)` / `useQuery(sessionReportQueryDocument)` from `@apollo/client/react` (stateful only; `useLazyQuery` banned). Mutations may pass a `refetchQueries`/`update` arm against `sessionReportQueryDocument`/`sessionHomeworkQueryDocument` — all three documents share the cache-normalized `id`-first rows, so normalized writes converge without refetch storms.
- `SessionHomeWorkQuery` nullable semantics for the UI layer: the ROOT is `SessionHomeWorkQuery_sessionHomework | null` (foreign/nonexistent session ⇒ null, no-oracle) AND the track legs (`currentFromAyah…revisionSurahJuz`) are individually `| null` (ungraded session) — two distinct empty states DEV2-014 must render.
- Backend journey note: submit denies `SESSION_INVALID_TRANSITION` (wrong state), `SESSION_REPORT_ALREADY_EXISTS` (duplicate), `FORBIDDEN` (governed teacher) — error surfacing keys already exist in `shared/locale` (3.2/3.3 work).
