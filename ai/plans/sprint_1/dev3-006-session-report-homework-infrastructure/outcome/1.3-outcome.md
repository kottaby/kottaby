# Task 1.3 — Enum guard + canonical types extension

**Plan:** `ai/plans/sprint_1/dev3-006-session-report-homework-infrastructure`
**Repo state:** branch `main` @ `2464696` (orchestrator has already committed Tasks 1.2 + 1.4 there; the brief expected `feat/dev3-006-session-report-homework` — NO branch switch was performed per instruction). All Task 1.3 changes are **uncommitted working-tree edits**; the orchestrator owns the commit.
**Execution model:** continuation task — a predecessor agent died mid-task. At handoff, ALL planned edits were already present in the working tree (file mtimes 10:12–10:17) and the 16 recorded tsgo errors (6× TS2769 in the enum test, 3× TS2352 in the verify script, 10 in the conformance suite — per the 1.2/1.4 outcome snapshots) were **already resolved in-tree**. This session therefore performed: full audit against plan §2.4, forced-clean tsgo verification, quality loop, test runs, semantic + security review, instruction verification, and this outcome. **Zero code changes were required** — the audit found no gap between the in-flight edits and the task spec.

---

## 1. State found at start

| File | Status at handoff | Audit verdict |
|---|---|---|
| `backend/enum/shared/surah-juz-ref.enum.ts` | M — `isSurahJuzRef` present | ✅ exact `isApplicantStatus` body (`backend/enum/teachers/applicant-status.enum.ts:25-27`); 35 members unchanged |
| `backend/types/classes/report.types.ts` | M — `ReportInsertType` + 4 new input/read types | ✅ matches plan §2.4 verbatim |
| `backend/types/classes/home-work.types.ts` | NEW — Select/Insert/Return | ✅ (0.2 item 2 pre-scoped the CREATE) |
| `backend/types/classes/session-notification.types.ts` | M — 3 new wave types | ✅ semantics per plan §2.4 (shape adaptations below) |
| `backend/types/classes/index.ts` | M — +1 barrel line | ✅ pure `export *` shape |
| `backend/enum/shared/surah-juz-ref.enum.test.ts` | NEW — 21 tests | ✅ Tier 1–4, all green |
| `backend/types/classes/report-home-work.types.conformance.test-d.ts` | NEW — compiler-validated | ✅ house harness identical to `session.types.conformance.test-d.ts` |
| `backend/types/classes/report-home-work.types.static-assertions.test.ts` | NEW — 10 tests | ✅ all green |
| `scripts/verify-unique-constraints.ts` (1.2 artifact) | NEW — TS2352 already fixed | ✅ tsgo-clean, sub-loop exit 0 |

The 1.4 outcome had recorded 9 residual sibling-task tsgo errors; those were fixed by the predecessor after its snapshot. Verification: deleted `tsconfig.tsbuildinfo`, forced a full project re-run (12.6 s) → 0 errors; a deliberate-error probe in `report.types.ts` confirmed tsgo genuinely covers this directory (probe reverted; restore re-run also clean).

## 2. Final export surface (exact names for downstream tasks)

**`backend/enum/shared/surah-juz-ref.enum.ts`**
- `enum SurahJuzRef` (35 members, unchanged: `SurahAlFatihah…SurahAlMaidah`, `Juz1…Juz30`)
- `function isSurahJuzRef(value: unknown): value is SurahJuzRef` — NEW

**`backend/types/classes/report.types.ts`**
- `ReportSelectType` (pre-existing), `ReportInsertType` (NEW — did not exist before; 0.2 item 2)
- `ReportReturnType` (NEW), `HomeWorkGradeFieldsInput` (NEW), `HomeWorkBlockInput` (NEW), `HomeWorkAssignInput` (NEW), `SessionReportSubmitInput` (NEW)

**`backend/types/classes/home-work.types.ts`** (NEW file)
- `HomeWorkSelectType`, `HomeWorkInsertType`, `HomeWorkReturnType`

**`backend/types/classes/session-notification.types.ts`**
- `SessionReportWaveParticipant` (NEW), `SessionReportWaveContext` (NEW), `SessionReportWaveContextRow` (NEW)

**`backend/types/classes/index.ts`** — one additive line (`export * from "./home-work.types";`); everything above flows through `@/backend/types` (root barrel already re-exports `./classes` — no further wiring).

## 3. Audit detail (plan §2.4 conformance)

- **Guard:** `isSurahJuzRef` is the verbatim `isApplicantStatus` pattern — `typeof value === "string"` + `Object.values(SurahJuzRef)`-membership, fail-closed, never throws. NO new enum members (D1 ledger governs widening). `report.types.ts` consumes `SurahJuzRef` in type position only (`import type` — correct per task brief; value-import discipline applies at guard/validation sites, e.g. the enum test).
- **Input types:** `HomeWorkGradeFieldsInput = { currentGrade: number; revisionGrade: number }`, `HomeWorkBlockInput = { fromAyah: number; toAyah: number; surahJuz: SurahJuzRef }`, `HomeWorkAssignInput = { jadid?; madi? }`, `SessionReportSubmitInput { readonly teacherNotes; readonly studentRatingByTeacher; readonly homework?; readonly previousGrades? }` — byte-for-byte the plan §2.4 shapes with doc comments.
- **BOPLA:** `id`, `sessionId`, timestamps, and both grade fields are structurally absent from `SessionReportSubmitInput` (pinned at three levels: conformance negatives, static-assertions test 3's forbidden-field scan, and `keyof` closedness pins).
- **Wave row realism:** `SessionReportWaveContextRow` mirrors the REAL join columns — `users.id/full_name/locale` (`backend/db/schema/users/users.ts:15,29`) plus nullable parent legs via `students.parent_id` (`backend/db/schema/students/students.ts:32`, `set null` FK) — exactly the projection convention of the existing `findWaveContextById` (`session.repository.helpers.ts:265-296`).
- **Hard rules honored:** no service-layer `.types.ts`; no types in Pothos resolvers; no `any` (the only `\bany\b` matches are prose in a doc comment and a test name); no spreads/casts/console in the type modules (static test 9).

## 4. Quality Loop (1.3.QL) — `--lifecycle duplicates`, per file

All **exit 0**: `surah-juz-ref.enum.ts`, `report.types.ts`, `home-work.types.ts`, `session-notification.types.ts`, `index.ts`, `surah-juz-ref.enum.test.ts`, `report-home-work.types.conformance.test-d.ts`, `report-home-work.types.static-assertions.test.ts` (+ `scripts/verify-unique-constraints.ts`). Baseline floors hold: project-wide `bun run tsgo` → **0 errors / 0 warnings**; `bun run oxlint` → **0 warnings / 0 errors** (1399 files, 303 rules).

### Discovery output (verbatim, for 1.3.IV — identical mapping for all `backend/types/classes/*` files; the enum files print `backend/AGENTS.md` + root `AGENTS.md`)

```
━━━ Applicable Rule Files ━━━

Instruction files (read before fixing):
  ⚠  .github/instructions/backend.instructions.md

AGENTS.md files (read before fixing):
  ✓  AGENTS.md
  ✓  backend/types/AGENTS.md
  ✓  backend/AGENTS.md
```

Known cosmetic quirk (pre-recorded by 1.2 §5/C3): the tool prints the legacy `.github/instructions/…` path; the real file is `.agents/instructions/backend.instructions.md` and was validated against it.

## 5. Test Engineering (1.3.TE) — mandated runner results

| Suite | Invocation | Result |
|---|---|---|
| Enum guard (Tier 1–4) | `bun run test/scripts/run-test.ts backend/enum/shared/surah-juz-ref.enum.test.ts` | **21 pass / 0 fail, 316 expect() calls** |
| Static structural assertions | `bun run test/scripts/run-test.ts backend/types/classes/report-home-work.types.static-assertions.test.ts` | **10 pass / 0 fail, 91 expect() calls** |
| Type-level conformance | `bun run tsgo` (project-wide; the compiler is the runner per the `.test-d.ts` house harness) | **0 errors** — every positive `satisfies` compiles AND every `@ts-expect-error` directive is consumed, proving each negative genuinely fails to compile |

Tier-1 coverage required by the task and now verified present: guard `true` for ALL 35 members; `false` for `""`, arbitrary strings (deterministic 200-payload fuzz), numbers, `null`/`undefined`; plus boundary (case/whitespace/near-miss), chaos (unicode/RTL/10k/hostile wildcards), and security tiers (coercion/`__proto__`/symbol rejection without throwing). Assignability smoke for all new input types and both insert round-trips live in the conformance suite.

## 6. Security & Tenancy Audit (1.3.SEC)

- The guard is the BOPLA pre-DB filter for the surah/juz vocabulary: unknown values → `false` (→ `VALIDATION` denial at the service layer in Phase 2), no exceptions, no coercion (Tier 4 proves `toString`/`valueOf` are never invoked).
- All `*Input` types exclude server-derivable fields (`id`, `sessionId`, timestamps, teacher identity); `reports` keeps NO `teacherId` anywhere in its type surface (row types derived from the table, which has no such column — 1.2's C.4 pin carries over).
- No PII added; the wave row only formalizes what the notification seam already reads (names + locales + nullable parent link).

## 7. Semantic Review (1.3.SR)

- No duplicated type shapes: the new names exist in exactly one module each (repo-wide symbol grep); row/read types are re-derived `$inferSelect` projections, never hand-declared (static tests 1–2 forbid re-declaration and `Omit<`).
- Select→Return alias discipline: `ReportReturnType` / `HomeWorkReturnType` are type-identical to their Select types (conformance `Equals` pins both directions, incl. "never forked" negatives).
- Barrel: single additive line, pure relative `export *` shape (static test 8 pins all five lines + the shape regex).
- Zero plan-artifact references in code comments (static test 10 scans all four lib files; manual grep over the test files too).

## 8. Instruction Verification (1.3.IV)

- **`.agents/instructions/backend.instructions.md`** — 6-layer flow respected (Types layer owns everything); canonical `$inferSelect`/`$inferInsert` foundation; input type named per convention; `@/backend/types` consumption path; barrel rules (`export *`, `./` paths, no imports) honored; no console/logging.
- **`backend/types/AGENTS.md`** — `{Entity}SelectType/InsertType/ReturnType` naming ✓; all types in `backend/types/` ✓; barrels relative `export *` ✓; `DBTransaction` untouched ✓. The older "ReturnType = Omit forbidden fields" wording is superseded by the current house style this file's siblings already use (`SessionReturnType = typeof session.$inferSelect` with a "no forbidden fields exist" rationale) — the reports/homework tables have no forbidden columns, so Omit would be dead code; deviation DV-B below.
- **Root + `backend/AGENTS.md`** — mandated runner used for tests; no `bun test`; no db actions.

## 9. Deviations (all explained, none silent)

- **DV-A — ReturnType derivation:** plan §2.4 sketched `ReportReturnType = ReportSelectType`; shipped as `ReportReturnType = typeof reports.$inferSelect` (same for homework). Type-identical (pinned by `Equals` conformance tests) and byte-consistent with the `SessionReturnType` precedent in the same directory.
- **DV-B — participant shape:** plan §2.4 sketched `interface SessionReportWaveParticipant extends SessionWaveParticipantContext {}`; shipped as a type alias `= SessionWaveParticipantContext`. Identical assignability, avoids an empty-interface declaration-merging surface; tasks.md 1.3 explicitly allows adapting to actual file content while keeping semantics; static test 5 and the conformance `Equals` pin freeze the identity.
- **DV-C — scope already widened by 0.2 (not new):** `ReportInsertType` and `home-work.types.ts` did not exist despite plan §2.1's claim; creating them was pre-reconciled by 0.2 item 2 and is exactly what shipped.
- **DV-D — barrel position:** `home-work.types` inserted at the head of the classes barrel (adjacent to its domain sibling `report.types`), preserving the file's pre-existing relative order instead of re-sorting four untouched lines. Repo-wide barrels are alphabetical; the classes barrel pre-dated non-alphabetically (`session.types` before `session-notification.types`); static test 8 pins the pure shape either way.

## 10. Carry-forward knowledge (for Phase 2+)

- Repos (2.2/2.3) import: `ReportInsertType`, `ReportSelectType` (→ `@/backend/types`, via `report.types.ts`), `HomeWorkInsertType`, `HomeWorkSelectType` (`home-work.types.ts`).
- Services (2.6–2.8) import: `SessionReportSubmitInput`, `HomeWorkAssignInput`, `HomeWorkBlockInput`, `HomeWorkGradeFieldsInput` (the input whitelist — field-by-field hand-off at the resolver, NO spread); validators call `isSurahJuzRef(block.surahJuz)` for the enum-leg denial (`homeworkSurahJuzInvalid` key from 1.4).
- Notification seam (D6) consumes `SessionReportWaveContextRow` (repo projection shape) → maps to `SessionReportWaveContext` after validation; `parent: null` ⇒ unlinked (INV-P1) ⇒ emit student+teacher waves only; participants are `{ userId, fullName, locale: AppLocale | null }` — pair with 1.4's copy slots (`eventSessionReportReadyTitle/Body/ParentBody`).
- Read surface: `ReportReturnType` / `HomeWorkReturnType` are the Pothos `objectRef<T>` payloads; grade columns are `number | null` and the surah/juz columns carry the closed pgEnum string union (select ≡ insert, conformance-pinned).
- The `.test-d.ts` harness convention (header, `v()` sink, `Equals<A,B>` helper, satisfies/`@ts-expect-error` duality) is reusable for future type-level suites; the compiler run that validates them is plain project-wide `bun run tsgo`.

## 11. Residual risks

- None blocking. The enum widening to 114 surahs remains governed by ledger D1; the 35-member freeze is pinned by the Tier-1 suite, so widening will fail that test until its member-set pin is consciously updated (intended tripwire).
- Worktree note for the orchestrator: Task 1.3's eight files (+ the 1.2 verify script and the three outcome docs) are uncommitted on `main` @ `2464696`; the feature branch named in the brief is not checked out anywhere.
