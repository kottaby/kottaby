# DEV3-012 — Outcome 3.1 (Frontend confirm affordance)

**Author:** orchestrator, round cron-r6. **Status:** closed.
**Scope:** plan task 3.1 — student confirm affordance on completed rows, teacher confirm-state display, i18n ar/en + parity. Backend slice (0.1/1.1/1.2/2.1/2.2) closed in cron-r5 (505f93e).

## What landed

### Wire surface (documents + codegen)
- `frontend/graphql/sharedDocuments/scheduling/session.documents.ts` — ELEVENTH operation `confirmSessionCompletionMutationDocument` (`mutation ConfirmSessionCompletion($id: ID!)`), full Session payload shape (id-first for cache normalization), typed through the new codegen `ConfirmSessionCompletionMutation`/`…Variables`. Module docblock updated (DEV3-004 + DEV3-005 + DEV3-012, "Eleven operations").
- `bun run codegen` — `frontend/graphql/generated/gql/graphql.ts` grew exactly +10 lines (operation types + `ConfirmSessionCompletionDocument`); codegen-growth-only held.

### SessionRow (shared row, payload-driven)
- `SessionRowAction.id` union widened to `"start" | "complete" | "confirm"`; optional `tooltip` (consequence explainer) and `color` token fields; the tooltip-carrying CTA renders through a focusable span bridge with the SAME testid/button shape as the plain variant (`session-action-<id>-confirm`).
- New meta cell `studentConfirmedAt` — the student-confirmation moment renders whenever the stamp is set (dual-confirmation visibility for BOTH roles; teacher surface gets the DEV3-012 "confirm state display" for free because the row is shared).
- New `AwaitingConfirmationPill` (module-scope component): info-toned outlined Chip (`HourglassTopOutlined`), testid `session-awaiting-confirmation-<id>`, rendered ONLY on the exactly-once pending shape — `Completed ∧ confirmedByStudentAt = null ∧ feeHeld = true` (Record lookup, no enum comparison). An arbitration-settled hold (`feeHeld = false`) is deliberately NOT pending: the idempotent mutation would return the row untouched and the stamp would stay unset — an affordance there would be dishonest.
- Complexity discipline: the meta band (`SessionRowMeta`) and the caller-CTA mapping (`RowActionButtons`) extracted module-scope, keeping `sonarjs/cognitive-complexity` green after the additions.

### StudentSessionsContainer (container-owned mutation, no dialog)
- `RowActionKind` extended with `"confirm"`; the D9-bis per-row slot book disables the confirm CTA while ITS OWN row's mutation is in flight.
- `studentActionsForSession` module-scope matrix: the Confirm descriptor (label `confirmCompletion`, tooltip `confirmCompletionTooltip`, color success) ONLY on the exactly-once pending shape — the same predicate the row's pill keys off.
- `useMutation(confirmSessionCompletionMutationDocument)` with cache NORMALIZE `update` (status/feeHeld/confirmedByStudentAt rewritten onto `Session:<id>`; NO refetch).
- Outcome arms (docblock table added): success → `sessions.sessionConfirmedNotice` success snackbar + slot release; `SESSION_NOT_FOUND` family → `evictSessionFromStudentLists` (ROOT_QUERY list filter + evict + gc, pattern copy of the teacher arm retargeted at `myStudentSessions`) + `errors.sessionNotFound`; `SESSION_INVALID_TRANSITION` → row-scoped inline alert; `FORBIDDEN` → error snackbar; else generic.

### TeacherSessionsContainer
- NO changes required — the teacher confirm-state display rides the shared row (studentConfirmedAt meta cell + awaiting pill). The teacher NEVER gets a confirm CTA (their stamp is written by completeSession).

### i18n (ar/en, key-set parity)
- Five new `Sessions` namespace keys: `confirmCompletion`, `confirmCompletionTooltip`, `awaitingStudentConfirmation`, `sessionConfirmedNotice`, `studentConfirmedAt` (types leaf + en + ar, exact parity — `sessions-namespace.parity.test.ts` 20/0 green).

## Suite coverage (both locales, ar + en loop)

Student suite (`test/ui/components/student/StudentSessionsContainer.suite.tsx`):
- **branch 6e (active)** — confirm affordance matrix over THREE Completed shapes on one page: pending (CTA enabled + pill + no cancel/dispute + no stamp cell), arbitration-settled (NO affordances), stamped (meta cell + formatted stamp, no affordances).
- **branch 6f (active)** — confirm success: success snackbar (`MuiAlert-colorSuccess`), stamp meta appears, pill + CTA leave via cache normalize, completed chip holds, no eviction side effect.
- **branch 6g (active)** — `SESSION_INVALID_TRANSITION`: row-scoped inline alert (`MuiAlert-colorError`), chip/pill unchanged, CTA re-enabled.
- **branch 6h (skip-with-body, D9-class)** — `SESSION_NOT_FOUND` eviction arm: the container-owned ROOT_QUERY cache surgery under an active useQuery observer is the same shape the teacher suite documents as deterministically killed/runaway under Happy DOM (its branch 17). Body INTACT, one-line flip. Compensating control: the production wiring is the proven teacher pattern; the live loop below exercises the success path end-to-end.
- Suite docblock updated (visual-matrix list + the `.skip(` inventory now names all five deliberate deferrals per locale).

Teacher suite (`test/ui/components/teachers/TeacherSessionsContainer.suite.tsx`):
- **branch 23 (active)** — DEV3-012 confirm-state display: awaiting pill on the pending shape, NO confirm/start/complete/cancel CTAs on the teacher surface, studentConfirmedAt meta cell + stamp on the stamped shape.

## Gate table (first-hand)

| Gate | Result |
|---|---|
| tsgo (`bun run tsgo`) | exit 0 |
| biome (full repo, 583 files) | 0 findings |
| oxlint full repo (--deny-warnings, 301 rules) | 0 warnings / 0 errors (incl. one mechanical pre-existing `prefer-set-has` fix in the parity test to keep the gate honest) |
| eslint full repo (`bun run lint`) | exit 0 |
| Student suite | 29 pass / 10 skip / 0 fail / 293 expect (baseline 23/8/0/233 — delta = 6e/6f/6g ×2 locales pass + 6h ×2 skip) |
| Teacher suite | 53 pass / 12 skip / 0 fail / 475 expect (branch 23 ×2 locales) |
| Admin suite (untouched, battery) | 19 pass / 4 skip / 0 fail |
| Sessions-namespace parity | 20 pass / 0 fail (513 expect) |
| Documents contract | green (38 pass combined run) |

Notes:
- oxlint `__ref` exemption block gained `StudentSessionsContainer.tsx` (the eviction arm's bracket-access `__ref` — established 8779acc/ResolveDisputeDialog precedent).
- The suite runs used the EXACT locked recipes (`run-locked-cmd stui-{student,teacher,admin}`, TEST_SERVER_MODE=production, 4 preloads).
