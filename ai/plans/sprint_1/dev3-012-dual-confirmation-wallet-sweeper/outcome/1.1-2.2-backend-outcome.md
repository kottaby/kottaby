# Outcome — Tasks 1.1/1.2/2.1/2.2 (backend slice)

**Agent:** orchestrator (direct implementation after the Task-tool transport failures of the prior round; the 2-b-style delegation path stayed unreliable).

## Delivered

- **WalletRepository** (`backend/db/repo/billing/wallet.repository.ts`, + billing barrels): `ensureWalletOnce` (idempotent ON CONFLICT insert — no approval-time wallet writer exists, verified), `creditEarningOnce` (one `earning` ledger row with the fee VERBATIM + explicit wallet balance/total_earning increment — the schema docblock's trigger does NOT exist in the DB, verified via information_schema, so the increment is explicit and atomic with the ledger row), `findByTeacherId`, `listTransactionsByWalletId`.
- **SessionRepository** additions: `confirmStudentCompletionOnce` (guarded once-UPDATE: id ∧ student caller ∧ completed ∧ teacher stamp ∧ student stamp null ∧ fee_held=true → student stamp + fee_held=false; the exactly-once guard lives in the predicate) and `sweepExpiredScheduledOnce` (ONE batch UPDATE: scheduled ∧ deadline < captured now → cancelled + fee_held=false, RETURNING all).
- **SessionLifecycleService**: `confirmSessionCompletion` (R-201 idempotence arms — student replay/teacher caller/arbitration-settled return the CURRENT row untouched; probe classification keeps the oracle-safe posture; credit slice fail-closed on a null fee) + `sweepExpiredSessions` (sequential same-tx refunds BY DESIGN via the shared `refundHeldLaneToProvenance` — context union extended; fail-closed on unreadable lanes rolls the sweep back).
- **GraphQL**: `confirmSessionCompletion(id): Session!` — authScopes `{ authenticated: true }` (participant predicate service-side, oracle-safe); SDL regenerated (`schema.graphql` line 80); `schema.graphql` codegen-grown only (graphql.ts intentionally unchanged until the frontend documents consume it — task 3.1).
- **SDL pins**: DEV3_012_MUTATION_FIELDS pin + mutation-set equality + committed-SDL fragment + freeze docblock extended.
- **Cron route** `app/api/cron/sweep-sessions` (GET): timing-safe bearer compare (SHA-256 digests + timingSafeEqual), mode gates fail closed to a 404 (`CRON_EXECUTION_MODE=external ∧ CRON_EXTERNAL_ENABLED=true`), missing secret fails closed 401, shared envelope with `requestId`, honest counts only.

## Gates (first-hand)

tsgo 0 · biome 0 (258-file backend/app set) · oxlint 0/0 (the sweep's sequential-await is a documented deliberate ordering; probe-status comparisons follow the string-constant sibling pattern) · full eslint exit 0 · battery 115/0 (service 54 incl. 6 new DEV3-012 tests, mutation suite, schema-surface, session-sdl, error-contract-matrix) · static-assertions 9/0 · allowlist suite green.

## Deviations / notes

- Repo-tier duplication of the once-guards was folded into the service-tier runInRollback tests (same real-DB tier the dispute family used); the repo file gained no dedicated suite.
- A stale source pin (static-assertions #5) still expected the single-line Pick that biome's 100-col formatting wraps — normalized to a whitespace-tolerant structural pin (pre-existing red from the DEV3-005 formatting era, surfaced now).
- `resolveSessionDispute` COMPLETE keeps its no-credit semantics (R-202 unchanged: arbitration consumes the hold; the WALLET credit rides dual confirmation only).
