# DEV3-013 — 4.1 Gates + Live QA + 5.1 Close-out

## Gate results (final tree, all first-hand)

| Gate | Result |
|---|---|
| `bun run tsgo` | 0 errors |
| `bun biome check` (touched file sets, 119 files) | 0 findings |
| `bun run oxlint` (touched sets) | 0 warnings / 0 errors |
| eslint per-file (`lint-service.ts -f`, every touched file) | 0 findings |
| full-repo `bun run lint` | ⚠ runner-quirk: exit 1 with EMPTY output — reproduced on the STASHED CLEAN BASELINE (git stash → same failure → stash pop). Environment-level; per-file lint is the working control |
| services battery (`backend/services/`) | 118 pass / 0 fail (incl. 9 new wallet service tests) |
| graphql battery (`backend/graphql/`) | 137 tests / 0 fail (incl. updated schema-surface pins) |
| locale parity (`shared/locale/`) | 74 pass / 0 fail (incl. 35 new wallet parity tests) |
| documents contract | 18 pass / 0 fail (wallet documents satisfy the id-field contract) |
| component battery (`bun run test:ui:components`) | 137 pass / 30 skip / 0 fail — wallet suite 17 pass / 4 skip × locales (skips = D8 typed-amount family, documented skip-with-body) |

## Live QA (real browser, agent-browser, teacher 6090)

1. Seed: registered fresh teacher → shared-PK `teacher` row approved → wallet seeded 60.00 balance + one completed earning row (the CREDIT slice was live-proven in DEV3-012; this is QA seeding).
2. `/wallet` (ar/RTL): nav entry live; balance cards 60.00 EGP verbatim ×2; ledger with +60.00 earning row (مكتمل chip, Arabic-formatted stamp); CTA live. Screenshot: `/tmp/qa-wallet-after-withdrawal.png`.
3. Dialog: title/body/hint «الرصيد المتاح: 60.00 ج.م»; submit DISABLED while empty (client gate live).
4. Type 25.00 → submit un-gates → submit → dialog closes, balance card flips 60.00 → 35.00 IN PLACE (no refetch), total_earning UNTOUCHED, ledger gains -25.00 معلّق (pending) row. DB proof: ONE `withdrawal` row status=pending amount=25.00, balance=35.00, total_earning=60.00.
5. Over-withdrawal: typed "99999999.00" (8 digits) → submit STAYS disabled (client grammar cap). Filled "50.00" (well-formed, > balance) → submit → SERVER denies: localized error snackbar «رصيدك غير كافٍ لإتمام هذا الطلب.» + dialog STAYS OPEN (screenshot `/tmp/qa-wallet-insufficient.png`) + DB verified ZERO rows committed (balance still 35.00, still ONE withdrawal row).
6. Console/page errors: zero.
7. Role guard: a fresh STUDENT hitting `/wallet` is redirected to their own dashboard (withPageAuth live).

## Close-out

- tasks.md 9/9 [x]. Deferred ledger: F8–F12 forward, no ❌/⚠️.
- Scoped commits pushed on feat/dev3-004-session-creation-lifecycle-scheduled-sta after `pull --rebase`.
- QA credential artifacts deleted post-verification.
