# Task 2.2 PlanRepository Outcome — DEV1-005: Plan Catalog CRUD (Admin Only)

**Date:** 2026-08-27
**Plan Directory:** `ai/plans/sprint_1/dev1-005-plan-catalog-crud-admin-only/`
**Target Ticket:** DEV1-005: Plan Catalog CRUD (Admin Only)

---

## Changes Implemented

1. **`backend/db/repo/billing/plan.repository.ts`**:
   - Implemented `PlanRepository` namespace with methods:
     - `insertPlan(insert: PlanInsertType, tx?: DBTransaction): Promise<PlanSelectType>`
     - `updatePlanFields(id: number, patch: PlanUpdateInput, tx?: DBTransaction): Promise<PlanSelectType | null>`
     - `setActiveStatusOnce(id: number, target: boolean, tx?: DBTransaction): Promise<PlanSelectType | null>` — conditional guarded update
     - `existsById(id: number, tx?: DBTransaction): Promise<boolean>`
     - `findById(id: number, tx?: DBTransaction): Promise<PlanSelectType | null>`
     - `listActive(tx?: DBTransaction): Promise<PlanSelectType[]>` — single canonical active predicate (`WHERE is_active = true ORDER BY created_at ASC`)
     - `listAll(tx?: DBTransaction): Promise<PlanSelectType[]>` — `ORDER BY created_at ASC`
   - Strict `tx` propagation across all methods.
2. **`backend/db/repo/billing/index.ts` & `backend/db/repo/index.ts`**:
   - Created billing repo sub-directory barrel and re-exported from top-level `backend/db/repo/index.ts`.
3. **4-Tier Unit Test Suite `backend/db/test/logic/billing/plan-catalog.repository.test.ts`**:
   - Tier 1: Happy path insertion, field updates, activation status toggling, find by ID.
   - Tier 2: Boundary conditions for nonexistent IDs, active-only vs all filtering, and `createdAt ASC` ordering.
   - Tier 3: Chaos & concurrency double-transition guard returning `null` on idempotent second transition.
   - Tier 4: Security verification asserting DB-level CHECK constraint enforcement via `expectRepoError`.

---

## Verification Summary

- `bun run scripts/health/sub-loop.ts backend/db/repo/billing/plan.repository.ts --lifecycle duplicates` → ✅ Exit 0
- `bun run scripts/health/sub-loop.ts backend/db/test/logic/billing/plan-catalog.repository.test.ts --lifecycle duplicates` → ✅ Exit 0
- `bun run test/scripts/run-test.ts backend/db/test/logic/billing/plan-catalog.repository.test.ts` → ✅ 10 pass, 0 fail (42 assertions)
- `bun run test/scripts/run-test.ts backend/db/test/logic/billing/plan-catalog-schema.test.ts` → ✅ 5 pass, 0 fail (19 assertions)

---

## Verdict

Task 2.2 complete and verified. Ready for Task 2.3 (PlanCatalogService implementation).
