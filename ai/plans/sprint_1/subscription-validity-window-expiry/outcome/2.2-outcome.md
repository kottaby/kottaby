# Task 2.2 — Canonical Types EXTEND Outcome

**Plan:** `ai/plans/sprint_1/subscription-validity-window-expiry/`
**Executed:** 2026-09-12 (sandbox session)
**Task:** 2.2 Canonical types EXTEND — `backend/types/billing/subscription.types.ts`

---

## Summary

Extended the canonical billing subscription types file with EXACTLY two new exported types, per the plan's §3.3 canonical-types table:

| Type | Shape | Contract role |
|---|---|---|
| `ExpiredDueSubscriptionRow` | `{ readonly id: number; readonly userId: number; readonly planId: number }` | Identity projection of a subscription row flipped to the expired status by the batch expiry statement — the guarded UPDATE's RETURNING feed for the sweep's follow-up lane settlement |
| `SubscriptionExpirySweepReturnType` | `{ readonly expired: number; readonly lanesZeroed: number }` | Counts-only result of an expiry sweep run — the service→cron-route envelope contract (no row identifiers cross the wire) |

Style mirrors the file's existing conventions:

- Both are `type` aliases with `readonly` object fields — the same shape style as the file's `PurchaseSubscriptionReturnType` (the closest sibling: a return/projection contract). The `interface` form stays reserved (as in the current file) for the submit-input style.
- Docblocks follow the house multi-line `/** */` pattern (summary line + elaboration paragraph) and are clean production-grade domain text only — zero REQ ids, task ids, phases, or plan paths.
- Appended at the end of the file; additive-only (27 insertions, 0 deletions).

## Barrel Verification (zero barrel change — preferred outcome)

- `backend/types/billing/index.ts:4` already carries `export * from "./subscription.types";` — the new types flow through the existing `export *` mechanism unchanged.
- Top-level `backend/types/index.ts` re-exports `./billing`, so both types are importable as `@/backend/types` per the cross-layer import rule.
- No new barrel created; no barrel file touched. NO service-layer `.types.ts` was created (prohibited).

## Files Modified / Not Modified

**Modified (this task):**
- `backend/types/billing/subscription.types.ts` — +27 lines (the two exported type aliases with docblocks), nothing else.

**Not modified (verified):**
- `backend/types/billing/index.ts` and `backend/types/index.ts` (barrels already correct),
- every repository/service/route file (later phases consume these types),
- no other file of any kind by this task. (Other dirty files in the tree — `backend/db/schema/billing/subscriptions.ts`, `shared/locale/**`, `worklog.md`, plan artifacts — belong to tasks 2.1/2.3 running in parallel and to plan-artifact bookkeeping, not to this task.)

## Verification Results

### 2.2.QL — Quality Loop

`bun run scripts/health/sub-loop.ts backend/types/billing/subscription.types.ts --lifecycle duplicates` → **exit 0**, all stages green in order: tsgo ✓ → oxlint ✓ → biome:check ✓ → lint:type-aware ✓ → check:duplicates ✓. Caches never cleared.

### 2.2.TE — Type-Level Compile Proof

Whole-repo `bun run tsgo` → **0 errors** (exit 0; baseline 0, zero new errors). File-scoped tsgo inside the sub-loop also reported no errors for the file.

### 2.2.SEC — Security Audit

Types-only change, no runtime surface: the edit adds two compile-time-only type aliases — no executable statements, no I/O, no env reads, no data access, and therefore no BOLA/BOPLA/BFLA exposure — and the `readonly` fields structurally prevent downstream mutation of sweep results.

### 2.2.SR — Semantic Review

- **Name uniqueness:** `git grep -l` over all tracked files — `ExpiredDueSubscriptionRow` and `SubscriptionExpirySweepReturnType` appear ONLY in `backend/types/billing/subscription.types.ts` (the canonical definition) and this plan's own artifacts (`plan.md` §3.3/§5.1, `tasks.md` task 2.2), which are the names' origin. No pre-existing or duplicate definition anywhere in the repo.
- **Readonly fields:** all fields of both types are `readonly` (matches the plan shapes exactly).
- **Scope boundary:** `git diff --stat` for this task = 1 file, 27 insertions, 0 deletions. No `interface`/`type` churn beyond the addition; no exports removed or renamed (existing five exports untouched).
- **No plan-meta in code:** docblocks contain domain text only (grep-clean of REQ/task/phase/path identifiers).
- **Deferred items:** none discovered — nothing to add to `deferred-items.md` (ledger D2 remains the single sanctioned ❌, unaffected).

### 2.2.IV — Instruction Verification

Applicable rule files printed and read in full by `sub-loop.ts` before fixing: `AGENTS.md`, `backend/AGENTS.md`, `backend/types/AGENTS.md`, `.agents/instructions/backend.instructions.md`. Compliance highlights:

- `backend/types/AGENTS.md`: all types live in `backend/types/billing/`; naming follows the `{Entity}ReturnType`-family convention (`SubscriptionExpirySweepReturnType`) and a named row-projection noun (`ExpiredDueSubscriptionRow`) as pinned by plan §3.3; no `backend/types/**/index.ts` edit required (existing `export *` barrel untouched, `./`-relative, no explicit per-export re-exports); no service-layer `.types.ts` created.
- `backend/AGENTS.md` + root `AGENTS.md`: single canonical definition per purpose (these are distinct-purpose sweep contracts, not duplications of `SubscriptionSelectType`/`SubscriptionReturnType` entity structure); no local type definitions introduced anywhere else; no `oxlint-disable` comments; no plan-meta in comments.
- `.agents/instructions/backend.instructions.md`: types are importable via `@/backend/types`; no runtime code, enum widening, or cross-layer import added.

## Cross-File Dependencies Discovered (carry-forward)

- **Task 4.1** (`backend/db/repo/billing/subscription.repository.ts`): `expireDueActive(now, tx?): Promise<ExpiredDueSubscriptionRow[]>` — the guarded `UPDATE … WHERE status='active' AND end_date IS NOT NULL AND end_date <= $now RETURNING id, user_id, plan_id` must project exactly this shape.
- **Task 5.1** (`backend/services/billing/subscription-expiry.service.ts` + new barrel): `expireDue(outerTx?): Promise<SubscriptionExpirySweepReturnType>` returning `{ expired: due.length, lanesZeroed }`.
- **Task 6.1** (`app/api/cron/expire-subscriptions/route.ts`): success envelope `apiSuccessResponse({ expired, lanesZeroed }, { requestId })` — import the contract from `@/backend/types` (deep-import convention also satisfied via the billing barrel).
- Types intentionally declared ahead of their consumers (Phase 4–6); whole-repo tsgo confirms they are safe standalone additions (no unused-export failures at type level).
