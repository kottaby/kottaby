# Task 3.1 Outcome — Journey suite (test-first RED): `subscription-expiry.journey.test.ts`

**Plan:** `ai/plans/sprint_1/subscription-validity-window-expiry/`
**Task:** 3.1 — Journey suite `test/workflows/billing/subscription-expiry.journey.test.ts` (CREATE — test-first, expected RED until Phase 5 ships)
**Date:** 2026-09-12
**Status:** ✅ Test-first artifact landed with a verified CLEAN RED (the sanctioned missing-service failure only)

---

## Summary

Created the Tier-4 capstone journey suite encoding plan.md §5.3 (state machine + side-effect matrix) and specs §3 (ordered journey steps) end-to-end, written against the not-yet-existing `SubscriptionExpiryService.expireDue()` exactly per plan.md §5.1 (`expireDue(): Promise<SubscriptionExpirySweepReturnType>` shape — `{ expired, lanesZeroed }` counts-only contract). The suite follows `test/workflows/AGENTS.md` verbatim and mirrors the two layer precedents: `test/workflows/billing/subscription-purchase.journey.test.ts` (billing fixtures, publish-spy seam, `expectSingleDenial` GraphQL harness, immutable-ledger teardown legs) and `test/workflows/sessions/session-lifecycle-denials.journey.test.ts` (real booking service calls, zero-write denial probes).

### Journey steps encoded (1 committing `beforeAll` tx + 7 sequential actor-attributed tests)

| Step | Actor | Encoded contract |
|---|---|---|
| beforeAll | System | Student A, Student B (`provisionStudentActor`), certified teacher (`provisionCertifiedTeacherActor`), plan (hifz lane, 30 interval days, 7 sessions) — ONE committing `db.transaction`, all rows `TrackedFixtures`-tracked |
| 1 | Student A | Real purchase + gateway-confirmation activation path → `status = Active`; **window arithmetic** `endDate − startDate === PLAN_INTERVAL_DAYS * MS_PER_DAY` asserted at raw precision AND restated via `secondPrecisionMs`; hifz lane credited exactly `sessionCount`; ONE persisted notification; receipt published post-commit targeted `[A]` (spy) |
| 2 | Student B | Same real activation path → own active window + credited lane (the isolation probe's non-vacuous baseline); cross-actor inbox/list isolation |
| 3 | System | Direct Drizzle `endDate` backdate (second-precision stamp, expired-row fixture shape per `backend/db/test/entity-setup.ts:208-218` docblock) + trial allowance seeded; row REMAINS `Active` (window-vs-status lag — the sweep closes it); full pre-sweep snapshots captured (A row + lanes, B row + lanes + inbox) |
| 4 | System | `SubscriptionExpiryService.expireDue()` → `{ expired: 1, lanesZeroed: 1 }`; A flips `Expired` with **ORIGINAL startDate/endDate/paymentVerifiedAt untouched**; uncovered hifz lane zeroed; `balance_trial` intact (INV-B3, non-vacuous: 2 → 2); **Student B byte-identical** (full row `toEqual` + lanes + inbox) |
| 5 | Student A | Booking on expired+zeroed lane (trial empty) → denied: service-level `ValidationError` with `code === "SUBSCRIPTION_EXPIRED"` (localized copy, never the raw key) AND the real GraphQL scope gate surfaces **exactly one error with `extensions.code === "SUBSCRIPTION_EXPIRED"`** (`expectSingleDenial` shape); **ZERO writes** (lanes, sessions, claims deltas all zero; neither key burned) |
| 6 | Student A (trial facet) | Same booking with trial credit → SUCCEEDS via trial lane (`heldBalanceLane = Trial`, `feeHeld`), expired hifz lane stays 0 (INV-B3 co-existence) |
| 7 | System | Sweep replay → `{ expired: 0, lanesZeroed: 0 }` honest zero counts; terminal states hold (A expired + original dates + zeroed + spent trial; B still byte-identical) |

---

## File created

- `test/workflows/billing/subscription-expiry.journey.test.ts` (702 lines, the ONLY file created/modified in the repo; no other test touched or weakened; no service created)

---

## RED evidence (exact failure output)

### 1. Journey run (sanctioned runner) — module-resolution failure at load time

Command: `cd /home/z/my-project && timeout 240 bun run test/scripts/run-test.ts test/workflows/billing/subscription-expiry.journey.test.ts`
(ran twice — identical deterministic failure; log: `logs/2026-09-12T14-59-33/test/workflows/billing/subscription-expiry.journey.test.ts.log`)

```
bun test v1.3.14 (0d9b296a)

test/workflows/billing/subscription-expiry.journey.test.ts:

# Unhandled error between tests
-------------------------------
error: Cannot find module '@/backend/services/billing/subscription-expiry.service' from '/home/z/my-project/test/workflows/billing/subscription-expiry.journey.test.ts'
-------------------------------

 0 pass
 1 fail
 1 error
Ran 1 test across 1 file. [66.00ms]
```

Exit code 1. The RED is **exactly** the missing-service import — no syntax errors, no assertion failures, no fixture errors.

### 2. Whole-repo tsgo — exactly ONE error, the sanctioned TS2307

`bunx tsgo -b --noEmit` (project-wide):

```
test/workflows/billing/subscription-expiry.journey.test.ts(100,43): error TS2307: Cannot find module '@/backend/services/billing/subscription-expiry.service' or its corresponding type declarations.
```

**Total: 1 error line repo-wide; 0 errors of any other kind.** (Recorded so Phase 5 can compare: after 5.1 lands, this must drop to 0.)

### 3. Quality loop (`3.1.QL`)

`bun run scripts/health/sub-loop.ts test/workflows/billing/subscription-expiry.journey.test.ts --lifecycle duplicates` → exit 1, **short-circuits at tsgo with exactly the TS2307 error above** (the ONLY allowed failure). Because the loop short-circuits, each downstream stage was proven separately on the file:

| Stage | Result |
|---|---|
| tsgo (project-wide, filtered) | ❌ 1 error — TS2307 missing module **only** (sanctioned) |
| oxlint (file) | ❌ 1 error — same TS2307 **only** (sanctioned) |
| biome (file) | ✅ clean (import organization safe-fix applied; re-check 0 errors) |
| lint / ESLint (file, lint-service) | ❌ 1 error — `import-x/no-unresolved` on the same missing module **only** (sanctioned) |
| check:duplicates (jscpd intra-file) | ✅ 0 clones |

All non-tsgo stages are green; every reported failure across every stage is the same single root cause: the not-yet-existing service module.

---

## ⚠️ Ground-truth finding for Phase 5 (important carry-forward)

The task brief stated Phase 2 already shipped the partial index, canonical types, and the `subscriptionExpired` locale key. **Verified against this tree: they are NOT present.**

- No `subscriptions_active_end_date_idx` in `backend/db/schema/billing/subscriptions.ts`
- No `ExpiredDueSubscriptionRow` / `SubscriptionExpirySweepReturnType` in `backend/types/billing/subscription.types.ts`
- No `subscriptionExpired` key anywhere under `shared/locale/`
- No `outcome/0.1` / `2.1` / `2.2` / `2.3` outcome files; `tasks.md` Phase 0/2 checkboxes unchecked

Handling (to keep the mandated CLEAN RED): the journey avoids depending on any Phase 2 artifact. Concretely:
1. It does NOT import `SubscriptionExpirySweepReturnType` (would be a second TS error); the sweep result is asserted by value shape `toEqual({ expired, lanesZeroed })`, which pins the canonical counts-only contract just as strictly.
2. It does NOT reference `ERRORS_EN.subscriptionExpired` (would be a second TS error); the denial copy is asserted as server-localized (non-empty, never the raw key) plus the domain-code contract at both the service and GraphQL (`extensions.code`) surfaces.
3. Phase 5 checkpoint (5.2's mandatory journey re-run) SHOULD strengthen step 5 with `expect(denial.message).toContain(ERRORS_EN.subscriptionExpired)` / the GraphQL `message` equality once 2.3 lands — recorded as a Phase 5 obligation, not a defect of this suite.

---

## Rules compliance

### Semantic review (3.1.SR) — journey rules (`test/workflows/AGENTS.md`)

| Rule | Honored how |
|---|---|
| 1. NO `runInRollback` — ever | Zero rollback wrappers; all services run their own committed top-level transactions; fixtures committed in `beforeAll` |
| 2. Committed fixtures + tracked cleanup + post-teardown re-probes | ONE committing `db.transaction` in `beforeAll`; every fixture AND every service-created row (subscriptions, payments, claims, junctions, notifications, booked session + booking claim) registered; `afterAll` = immutable-ledger leg under `withImmutabilityTriggersSuspended(["student_payments"])` → junction leg with residue re-probe → `tracked.cleanup()` (reverse-order delete + zero-residue existence re-probes for EVERY registered row) |
| 3. Unique per-run prefix | `journeyPrefix("billing")` → `jrn_billing_<8hex>` on plan title and all 5 idempotency keys |
| 4. Honest authorization only | Real `users` + `students` + `teacher` rows via the actor-context factory; denials flow through the real governance/certification/balance code path and the real GraphQL scope gate (`contextFor` builds the factory-shaped Context — nothing monkey-patched); honest role resolution (`toUserRole(user.role)`) |
| 5. External effects intercepted | `spyOn(NotificationEngine, "publishReceipts").mockImplementation(async () => {})` — the exact seam precedent (`subscription-purchase.journey.test.ts:142`); every expected dispatch asserted WITH its targeted userIds (`publishedRecipientUserIds(i)` → `[A]`, `[B]`); restored in `afterAll` |
| 6. Never `.rejects.toThrow()`; translated substrings | `catchJourneyError` for the service-level denial; domain code asserted on the error contract; GraphQL denial via `expectSingleDenial`. Translated-copy substring assertion for `subscriptionExpired` is DEFERRED to the Phase 5 checkpoint (key absent from tree — see ground-truth finding above; referencing it now would break the mandated clean RED) |
| 7. `bun:test` only; no `console.*`; no `any` | All imports from `bun:test`; zero `console.*`; zero `any`/casts — narrowing via runtime guards (`isRecord`, `sweepBaselines()`) |
| 8. `@/` aliases | All imports aliased; helpers via the `@/test/workflows/helpers` barrel |
| 9. Own fixtures, never seeded rows | `createTestPlan` + actor-context factories only; the seeder catalog is never queried |
| 10. One journey per workflow, domain dir | `test/workflows/billing/subscription-expiry.journey.test.ts` |
| 11. Sequential actor-attributed steps | Steps 1–7 run in declaration order, each attributed (Student A / Student B / System), later steps observe earlier committed state |
| 12. Cross-actor visibility BOTH ways + denial probes | A observes own expired row; B observes zero fan-out (byte-identical row/lanes/inbox); two denial surfaces probed (service + wire); zero-write probes on denial |

### Instruction verification (3.1.IV) — `AGENTS.md` (root) + `.agents/instructions/tests.instructions.md`

| Rule | Honored how |
|---|---|
| Run-test script only for DB/service/journey surfaces | Both runs via `bun run test/scripts/run-test.ts <path>` (raw `bun test` never used) |
| No `runInRollback` in journey layer (documented exception) | Compliant (see above) |
| `bun:test` utilities; never Jest/Vitest | Compliant |
| Never `any`; types from `@/backend/types` | `DBTransaction`, `PaymentWebhookEvent`, `PlanSelectType`, `SubscriptionSelectType`, `UserSelectType` imported from `@/backend/types`; no `any`, no type-erasing casts |
| Unique emails/names via helpers; never seed data | Compliant |
| `getServerTranslations` usage | Used only inside the `contextFor` harness exactly as the shipped billing journey does (module-adjacent precedent); no locale-key references to not-yet-existing keys |
| Sub-loop per-file loop; caches never cleared | Sub-loop run recorded; no cache files touched; biome safe-fix only |
| No plan-meta in code | No REQ ids, task ids, or plan paths in any comment/JSDoc (verified by re-read) |
| Enum value imports at runtime | `SubscriptionStatus`, `SubscriptionCreditLane`, `SessionIntent`, `SessionStatus`, `HeldBalanceLane` imported as VALUES and used as enum members (never string literals) in runtime comparisons |

### Security (3.1.SEC)

- **Cross-student isolation probe is present and load-bearing** (step 4 + step 7): Student B holds a real active in-window subscription + credited hifz lane + persisted notification BEFORE A's sweep; after the sweep B's subscription row is `toEqual`-byte-identical (every column incl. timestamps), B's lanes are byte-identical, B's inbox count unchanged. The sweep's single due row is exactly A's (`expired: 1`, never 2), proving predicates key on row ownership.
- Zero-write denial probes (step 5) prove no cross-actor or double-spend writes on rejection (BOLA surface: booking evaluates only the acting student's own lanes).
- The journey creates no privilege escalation surface: all actors hold only their real role rows; the GraphQL probe rides the production scope gate.

---

## Carry-forward for Phase 5

1. **Expected GREEN in Phase 5**: re-run this suite after 5.1 (`SubscriptionExpiryService`) and 5.2 (booking expiry gate) land; the whole-repo tsgo error count must go 1 → 0. The `3.1.TE` checkbox records the test-first artifact + honest RED evidence; the GREEN checkpoint belongs to 5.2.
2. **Strengthen the copy assertion** once the `subscriptionExpired` locale key lands (Phase 2.3 in plan order): assert the denial message contains/equal the translated copy at both surfaces.
3. **Phase 2 prerequisites verified ABSENT in this tree** (partial index, canonical types, locale key — see ground-truth finding). Whoever executes Phase 2/4/5 must not assume the outcome-file claims of "shipped" until re-verified; this suite's `toEqual({ expired, lanesZeroed })` and `extensions.code` assertions already pin the canonical contracts.
4. **Timing note**: steps 4/7 depend on `expireDue()` capturing `now` AFTER the backdate; no sleeps are used — the sweep's SQL-side `end_date <= now` predicate is deterministic against the one-hour-lagged `endDate`.
5. Nothing else in the repo was touched; no other test weakened; no service file created (per task boundary).
