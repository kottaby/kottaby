# Outcome — Task 4.1: `SubscriptionRepository` extensions (expiry sweep + lane-coverage probe)

**Task:** 4.1 — EXTEND `backend/db/repo/billing/subscription.repository.ts` with `expireDueActive` + `hasUncoveredExpiredLane`; CREATE `backend/db/test/logic/billing/subscription-expiry.repository.test.ts`
**Requirements:** REQ-002, REQ-004, REQ-005, REQ-011, REQ-022, REQ-050, REQ-071
**Date:** 2026-09-12 · **Agent:** Subagent (repo)

---

## Summary

Both sanctioned repo methods landed exactly per plan §5.1, plus the repo-level test suite (5 tests, all green). One environment blocker is open on the `main` checkout (see ⚠️ below): the Phase 2.2 canonical types are missing here because the sandbox restored `main` while the phase work lives on `feat/subscription-validity-window-expiry` (commit `f137a27`, verified via `git show`). Everything except the repo file's tsgo/oxlint stage is verified green in-place.

### `expireDueActive(now: Date, tx?: DBTransaction): Promise<ExpiredDueSubscriptionRow[]>`

- ONE guarded query-builder UPDATE: `SET status = SubscriptionStatus.Expired, updatedAt = sql`now()``
  `WHERE status = Active AND end_date IS NOT NULL AND end_date <= $now` `RETURNING { id, userId, planId }` — the exact §5.1 statement shape.
- Enum members only (`SubscriptionStatus.Active/Expired` — value import; REQ-005); SQL-side `lte` comparison keeps the predicate inside the row lock; no SELECT-then-UPDATE.
- Explicit `updated_at = now()` raw set (bypasses `$onUpdate`, same explicit stamp as `activatePendingOnce` / the credit-lane helper); Drizzle's query-builder RETURNING keeps the projected row type-safe with zero provider-divergent decoding.
- Zero-row result = replay/no-op branch (idempotent convergence under concurrent sweeps).

### `hasUncoveredExpiredLane(studentId: number, lane: SubscriptionCreditLane, tx?: DBTransaction): Promise<boolean>`

- ONE read-only statement — `EXISTS(subscriptions ⨝ plans WHERE user_id = $1 AND balance_lane = $2 AND status = 'expired') AND NOT EXISTS(same join WHERE status = 'pending' OR (status = 'active' AND end_date > now()))` — fused so the answer is one consistent snapshot.
- All values bound as parameters ($n); the lane travels as the `SubscriptionCreditLane` enum member; `now()` evaluated SQL-side. No `--` comments inside the `sql` template.
- Owner-scoped by the `user_id` predicate (one student's expired lanes are invisible to another's probe); no dynamic column/identifier resolution anywhere (BOPLA-clean by construction).
- Transactional by contract (booking-tx probe): no `queryDb` bare-read variant; `tx?` follows the repo's standard `tx ?? db` executor convention.

## Files

| File | Action | Notes |
|---|---|---|
| `backend/db/repo/billing/subscription.repository.ts` | EXTEND | +2 methods (+ their docblocks); imports: `isNotNull, lte, sql`, `plans` schema, `SubscriptionCreditLane`, `ExpiredDueSubscriptionRow` (type import from `@/backend/types`) |
| `backend/db/test/logic/billing/subscription-expiry.repository.test.ts` | CREATE | 5 tests / 26 assertions, `runInRollback` + `tx` on every call |

NO other source files were modified by this task. NOTE: `backend/db/repo/students/student.repository.ts`, `backend/db/repo/students/student.repository.zero-lane.helpers.ts` and `oxlint.config.mts` show concurrent modifications in this worktree — those belong to the parallel Task 4.2 agent, not to 4.1.

## Verification

### Test run (4.1.TE) — `bun run test/scripts/run-test.ts backend/db/test/logic/billing/subscription-expiry.repository.test.ts`

```
(pass) expireDueActive flips ONLY past-window active rows; every other shape is untouched
(pass) re-running the sweep after a flip matches zero rows (replay is a no-op)
(pass) end_date exactly equal to now IS due; one millisecond later is not
(pass) two overlapping sweep calls converge: exactly one claims the row, terminal state identical
(pass) hasUncoveredExpiredLane truth table: expired-only → true; covered/none → false

 5 pass
 0 fail
 26 expect() calls
Ran 5 tests across 1 file. [243.00ms]
```

Coverage delivered (maps 1:1 to the task's test matrix):
- past-window `active` → `expired` with the exact `{ id, userId, planId }` projection (filtered to the fixture's owner — the sweep is cohort-wide by design);
- in-window / `pending` / `cancelled` / `suspended` / null-`end_date` actives all untouched; the flipped row keeps its original window span (status-only mutation) and carries the explicit `updated_at` stamp;
- re-run = zero-row idempotency;
- T2 boundary: `end_date == now` IS due; `end_date == now + 1ms` is not;
- T3 chaos: `Promise.allSettled` of two overlapping sweeps — union of claims is exactly the one due row, terminal state identical;
- `hasUncoveredExpiredLane` truth table: expired-only → true; +covering active → false; +pending cover → false; none → false; lane-scoped miss → false; owner-scoped miss (cross-student isolation) → false; lagging active (past-window, unswept) does NOT cover → true.

Regression: sibling suites stay green unchanged — `subscription.repository.test.ts` 6 pass / 0 fail; `subscription-purchase-idempotency.repository.test.ts` 6 pass / 0 fail.

### Quality loop (4.1.QL) — `bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates`

| File | tsgo | oxlint | biome | lint | duplicates | Exit |
|---|---|---|---|---|---|---|
| `backend/db/test/logic/billing/subscription-expiry.repository.test.ts` | ✓ | ✓ | ✓ | ✓ | ✓ | **0** |
| `backend/db/repo/billing/subscription.repository.ts` | ✗ (1 diagnostic) | ✗ (same 1) | ✓ | ✓ (exit 0) | ✓ (0 clones) | 1 — blocked by ⚠️ below |

The repo file's ONLY failing diagnostics (tsgo and oxlint report the identical single error) is the missing canonical type:

```
backend/db/repo/billing/subscription.repository.ts(28,3): error TS2305:
  Module '"@/backend/types"' has no exported member 'ExpiredDueSubscriptionRow'.
```

⚠️ **Cross-file dependency (report per the Fix-Or-Report rule — not fixed here):** this sandbox restored `main` (`2bdea32`), so the Phase 2.2 artifacts (canonical types), the Phase 2.1 partial index and the Phase 3 journey suite are absent from the checkout. They exist on `feat/subscription-validity-window-expiry` (`f137a27`, `cf89212`) — `git show feat/subscription-validity-window-expiry:backend/types/billing/subscription.types.ts` contains `ExpiredDueSubscriptionRow = { readonly id: number; readonly userId: number; readonly planId: number }` exactly as imported, and `scripts/recover-branch.sh` (pre-staged in the worktree) restores the branch. Because `import type` is erased at runtime, the test suite runs green even on `main`; only the type-checking stages await the type.

**Orchestrator hand-off:** commit/stash these two files BEFORE running `bash scripts/recover-branch.sh` (a `checkout -f`/`reset --hard` discards uncommitted tracked-file changes — the repo-file edit would be lost; the new test file is untracked and survives). A patch backup of the repo-file diff is at `/tmp/4-1-subscription-repository.patch`. After recovery, re-run the repo-file sub-loop: with the type present, the only previously-failing diagnostics vanish (both were exactly TS2305), so exit 0 is the expected result.

### Security & tenancy (4.1.SEC)

- `hasUncoveredExpiredLane` keys every row on `subscriptions.user_id = $studentId` — owner-scoped; the suite proves a foreign student's expired row cannot flip another student's probe (outsider case) and that lanes are isolated from each other (hifz vs tajweed).
- Bound parameters only ($n) — zero string interpolation of caller data; the lane is a `SubscriptionCreditLane` enum member, never a caller string; no dynamic identifier→column resolution exists in either method (BOPLA).
- `expireDueActive` accepts NO caller identities — it flips rows purely by window state (the sweep's contract), returns only `id/userId/planId` (no PII), and is invoked by the bearer-gated cron path (Phase 5/6), not by any user-facing surface.
- No logging in the repo layer; no `console.*`; no `any`; no `oxlint-disable`; no `jscpd:ignore`.

### Semantic review (4.1.SR)

- Race conditions: single guarded UPDATE (predicate = lock, zero-row = replay); the probe fuses EXISTS + anti-EXISTS into one statement (no check-then-read window); per plan §5.2 the probe is read-then-throw with no follow-on write — no TOCTOU introduced.
- No dead branches: every predicate half is exercised by the suite (five status guards, the `IS NOT NULL` guard, the inclusive `<=` boundary, all four truth-table rows + lane/owner scoping + the lagging-active case).
- Enum discipline: `SubscriptionStatus` is a value import compared as members (never string literals) in both the WHERE predicate and the SET; `SubscriptionCreditLane`'s symbol is type-position-only in this file (biome normalized the import to `import type`) while the runtime enum VALUE flows as the bound lane parameter — the enum-member-over-string rule holds at the data level.
- Scope boundary: exactly the two files above; no plan-meta in code comments; no `--` comments inside `sql` templates.

### Instruction verification (4.1.IV)

Rule files read IN FULL and complied with (also the exact set printed by the sub-loop runs):

- `/home/z/my-project/AGENTS.md` — rollback+tx doctrine, no `console.*`, `@/` aliases, barrels untouched, caches never cleared, type-definition pattern.
- `/home/z/my-project/backend/AGENTS.md` — bare-reads rule (probe is tx-bound by contract; no `queryDb` variant per the plan's ruling), types from `@/backend/types`, `DBTransaction`/`DBQueryExecutor` imported from `@/backend/types`.
- `/home/z/my-project/backend/db/repo/AGENTS.md` — guarded-transition writes (single statement + RETURNING, no SELECT-then-UPDATE), namespace-per-file, `@/` cross-layer imports, no business logic/i18n/logging in repos, no `inArray`+prepared statements.
- `/home/z/my-project/backend/db/test/AGENTS.md` — `runInRollback` everywhere, `tx` to every repo/Drizzle call, no `.rejects.toThrow` inside rollback, entity-setup helpers only (no seed queries), bun:test, no `any`, helper-signature verification against `entity-setup.ts` source, shared `now` per test, schema columns verified before fixture overrides.
- `/home/z/my-project/backend/db/test/logic/AGENTS.md` — file placed in `logic/billing/`, aliased helper imports, no barrel.
- `/home/z/my-project/.agents/instructions/backend.instructions.md` — repo-layer rules, enum/predicate discipline, layer separation.
- `/home/z/my-project/.agents/instructions/tests.instructions.md` — run-test.ts runner only, no direct `getServerTranslations` in tests, extracted helpers, process-locked DB runs (observed `[process-lock]` serialization).

## Carry-forward

1. **Blocked-on-environment:** repo-file tsgo/oxlint exit 0 awaits the Phase 2.2 canonical types (`ExpiredDueSubscriptionRow`) — restore `feat/subscription-validity-window-expiry` (commit/stash this task's files first — see hand-off above), then re-run `bun run scripts/health/sub-loop.ts backend/db/repo/billing/subscription.repository.ts --lifecycle duplicates`. No code change is expected to be needed.
2. `tasks.md` — 4.1.TE / 4.1.SEC / 4.1.SR / 4.1.IV flipped with evidence; 4.1 + 4.1.QL intentionally left unchecked until the post-recovery sub-loop exit 0 lands (evidence-or-it-didn't-happen rule).
3. Task 4.2 (running concurrently in this worktree — its three files are visibly in progress) reuses `hasUncoveredExpiredLane`'s coverage semantics for the zeroing guard; the expiry repo suite (`subscription-expiry.repository.test.ts`) is the designated extension point for the 4.2 zeroing tests.
4. Downstream (5.1 service / 5.2 booking gate) consumes exactly these two signatures; no signature drift permitted without re-reading this outcome.
