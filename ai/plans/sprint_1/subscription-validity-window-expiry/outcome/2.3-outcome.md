# Task 2.3 — Locale key `subscriptionExpired` (3 files)

**Plan:** `ai/plans/sprint_1/subscription-validity-window-expiry/`
**Executed:** 2026-09-12 (sandbox session, parallel with 2.1/2.2)
**Requirements:** REQ-002, REQ-003, REQ-032

---

## Summary

Added ONE new flat errors-namespace key `subscriptionExpired` to exactly 3 files per the REQ-032 recipe: the compile-time label type (`ErrorsLabels`), the English leaf, and the Arabic leaf. The key sits directly beside `insufficientBalance` in all three files (the sibling it will be thrown against in the booking debit ladder), mirroring the expired-over-insufficient denial ordering. The `errors` namespace was already wired — NO `message.ts` registration, NO new namespace, NO consumer wiring (the service-tier consumer `t.subscriptionExpired` is task 5.2's scope; the client error arm is deferred per ledger D3).

Final placement (verified against the pre-edit tree; the research-cited anchors `labels.ts:174` / `en:85` / `ar:84` were accurate):

| File | Inserted line | Position |
|---|---|---|
| `shared/locale/types/errors/labels.ts` | `:173-174` | immediately before `insufficientBalance` (`:175-176`) |
| `shared/locale/en/errors/index.ts` | `:85` | immediately before `insufficientBalance` (`:86`) |
| `shared/locale/ar/errors/index.ts` | `:84` | immediately before `insufficientBalance` (`:85`) |

## Exact copy shipped

**Type entry** — `ErrorsLabels` flat `string` slot (auto-joins the `ErrorMessageKey` union), docblock is a self-contained user-facing-condition sentence with no identifiers:

```ts
/** Expiry reject — the caller's subscription validity window has ended, so the booking cannot be funded by it. */
readonly subscriptionExpired: string;
```

**English leaf** (`errorsEn`):

```ts
subscriptionExpired: "Your subscription has expired.",
```

**Arabic leaf** (`errorsAr`) — natural MSA, RTL-safe (pure Arabic grapheme run + Arabic full stop; no Latin/bidi-neutral characters):

```ts
subscriptionExpired: "انتهت صلاحية اشتراكك.",
```

Copy-class conformance: en is the "Your subscription has expired."-class self-contained sentence sanctioned by REQ-032; ar mirrors the established `parentLinkRequestExpired` ("انتهت صلاحية …") house pattern; both are terse single sentences matching the `insufficientBalance` sibling register.

## Files modified (scope boundary)

- `shared/locale/types/errors/labels.ts` — +2 lines (docblock + flat type entry)
- `shared/locale/en/errors/index.ts` — +1 line
- `shared/locale/ar/errors/index.ts` — +1 line

Nothing else touched by this task: no `message.ts`, no `namespacePaths`, no new imports, no barrel changes, no consumer code, no plan-meta in code. `git diff` for this task = exactly these 3 files (the other dirty paths in the shared tree belong to the parallel 2.1/2.2 agents).

## Verification results

### 2.3.QL — per-file quality loop (`bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates`)

| File | Result |
|---|---|
| `shared/locale/types/errors/labels.ts` | ✅ tsgo → oxlint → biome → lint:type-aware → duplicates all passed — **exit 0** |
| `shared/locale/en/errors/index.ts` | ✅ tsgo → oxlint → biome → lint:type-aware → duplicates all passed — **exit 0** |
| `shared/locale/ar/errors/index.ts` | ✅ tsgo → oxlint → biome → lint:type-aware → duplicates all passed — **exit 0** |

Caches never cleared. Note: sub-loop's tsgo stage is project-wide with per-file filtering — it also proves the `ErrorsLabels` typing holds across both locale leaves (compile-time parity belt #1).

### 2.3.TE — parity suite (mandated runner; raw `bun test` NOT used)

`bun run test/scripts/run-test.ts shared/locale/errors-namespace.parity.test.ts` — **exit 0**:

```
(pass) compile-time parity mirror — ar/en key sets agree > identical sorted key sets across BOTH locale sources
(pass) compile-time parity mirror — ar/en key sets agree > every leaf value on BOTH maps is a non-empty localized string (zero dead keys)
... (session-report/homework pins, route-emitter pins, recitation pins — all pass)
(pass) machine-constant exemption — `_health` payload constants stay OUT of locale files > ZERO health-flavored keys exist in either locale's errors map
(pass) touched-surface import hygiene (route.ts) > gateway route references ONLY the compile-time system — zero next-intl/getBackendTranslations/shared/messages

 21 pass
 0 fail
 204 expect() calls
Ran 21 tests across 1 file. [64.00ms]
```

The ar/en key-set gate auto-covered the new key (both maps now expose `subscriptionExpired` with identical key sets, non-empty values); the compile-time gate proves the type entry is implemented by both leaves.

### 2.3.SEC — security audit

Copy-only additive change: three static string/type literals in the locale layer — no user input handling, no query/tenancy surface, no injection or BOLA/BOPLA/BFLA vector, no authz change, no PII in the copy (generic second-person sentence; no identifiers interpolated).

### 2.3.SR — semantic review

- Key name spelled identically in all 3 files — `grep -n subscriptionExpired shared/locale` → exactly 3 hits: `labels.ts:174`, `en/errors/index.ts:85`, `ar/errors/index.ts:84`. ✔
- Type + en + ar parity — compile-time (`ErrorsLabels` const typing on both leaves) + runtime parity gate green. ✔
- Flat entry (not a nested sub-block) — `ErrorMessageKey` derivation admits only `string` slots, so the flat placement is compile-time pinned. ✔
- No hardcoded usage added anywhere — no consumer wiring in this task (5.2 owns `ValidationError("SUBSCRIPTION_EXPIRED", t.subscriptionExpired)`); zero matches outside the 3 files. ✔
- Scope boundary — only the 3 locale files modified (git evidence above). ✔
- Anti-pattern sweep over `shared/locale/` — zero hits for `next-intl`, `getBackendTranslations`, `shared/messages`, `Translation.<Upper>` enum access, or two-arg `getTranslations(...)` (only matches are the parity test's own negative assertions). ✔

### 2.3.IV — instruction verification

Rule files read before editing: root `AGENTS.md`, `shared/AGENTS.md`, `shared/locale/AGENTS.md` (exists — path-alias + leaf-purity rules honored: no new imports, leaf modules remain plain literals). Sub-loop printed "No applicable instruction files found" for all 3 files; applicable AGENTS.md = root `AGENTS.md` (read). tasks.md 2.3.IV's listed files (repo + `shared/AGENTS.md`) both read. ✔

## Carry-forward

- Task 5.2 MUST consume this key as `throw new ValidationError("SUBSCRIPTION_EXPIRED", t.subscriptionExpired)` (plan §2.2/§6) — the denial ordering expired-over-insufficient is reflected by the key sitting before `insufficientBalance` in all 3 files.
- The future booking-UI ticket MUST map `extensions.code === "SUBSCRIPTION_EXPIRED"` → this `subscriptionExpired` key at its landing (ledger D3 obligation, recorded in REQ-061 + 9.1 canonical doc).
- Journey test 3.1 can assert translated substrings ("subscription has expired" / "انتهت صلاحية اشتراكك") against this key via `getServerTranslations`.
- No deferred items created by this task.
