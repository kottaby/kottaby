# Task 8.1 — Final Quality Gate & Deferred-Items Enforcement Outcome

**Plan:** `ai/plans/sprint_1/subscription-validity-window-expiry/`
**Executed:** 2026-09-12
**SKILL.md section:** §Deferred-Items Enforcement — Final Quality Gate (Before Knowledge Propagation)

---

## Baseline Comparison (vs `outcome/0.1-baseline-outcome.md`)

| Check | Baseline (Phase 0) | Final Gate | Verdict |
|---|---|---|---|
| `bun run tsgo` (`tsgo -b --noEmit`) | 0 errors | **0 errors** | ✅ zero new |
| `bunx @biomejs/biome check .` | 0 (1777 files) | **0 (1777 files, exit 0)** | ✅ zero new |
| `bun run lint --json` (lint-service full-repo) | success, exit 0 | **success, exit 0** (`id: final-gate`) | ✅ zero new |
| `bun run db push` (drift check) | applied clean | **"No changes detected"** (re-verified at 6.5) | ✅ schema drift-free |

## Per-File Equivalent Quality Gate (`sub-loop.ts --lifecycle duplicates`)

The full `bun quality-gate` run OOMs in this sandbox (eslint worker SIGABRT at ~4 GB — same as the prior session's documented precedent in `worklog.md`), so the sanctioned per-file equivalent gate was run over **every code + test file in the plan diff**:

- Source files (15/15 pass): schema, types, 3 locale files, 2 repo files + zero-lane helpers, expiry service + billing barrel, booking gate, cron route, route-inventory, activation lock-in test, session-lifecycle.service pin test.
- Test files (7/7 pass): subscription-expiry.repository, student-zero-lane.repository, subscription-expiry.service, session-lifecycle.booking (extension), expire-subscriptions-route, route-inventory.test (extension), subscription-expiry.journey.
- **Total: 22/22 files exit 0.** (First pass hit a mid-run sandbox branch reset — the 5 "failures" were the wiped-tree artifact; the atomic recovery + retry pass was 5/5. Both sub-loop cycles never cleared caches.)
- `oxlint.config.mts` (declared deviation, in oxlint's own ignorePatterns — not sub-loop-lintable; tsgo clean).

## Test-Layer Coverage Gate (SKILL.md §Test-Layer Coverage Gate)

| Layer | Suites | Result |
|---|---|---|
| Repository / DB logic | subscription-expiry.repository (5), student-zero-lane.repository (13), student.repository (23, blocking-ticket) | ✅ green |
| Service unit | subscription-expiry.service (8), activation lock-in (22), session-lifecycle.booking (26), session-lifecycle.service (72) | ✅ green |
| Cross-actor journeys | subscription-expiry.journey (7) + blocking-ticket journeys (purchase 11, denials 6, dual-confirmation 11) | ✅ green |
| GraphQL integration | (no new GraphQL surface in this plan — D4; booking suite covers the denial shape) | n/a by design |
| Route / integration | expire-subscriptions-route (11), sweep-sessions sibling (9), route-inventory (15), gateway static-assertions (17), parity (21) | ✅ green |
| UI components / E2E | not mandated by this plan (REQ-060 no-UI ruling) | n/a by design |

## Deferred-Items Enforcement Gate

`grep -c "❌\|⚠️" deferred-items.md` → raw count 15 (includes the definitional Status-Values legend and Anti-Patterns blocks). **Ledger Table rows only: D1 ✅ · D2 ❌ · D3 ✅ — exactly 1 sanctioned ❌ (D2)**, matching the checkpoint expectation ("exactly 1 at Phase 8; MUST be 0 after 9.1"). D2 closes at 9.1 when the canonical doc records the external-trigger ops handoff.

## Known Non-Blocking Items (documented, filtered per review doctrine)

1. **health-route.probe.test.ts "exactly the three inventory routes" pin** — fails IDENTICALLY on pristine `origin/main` (verified via a clean worktree run during 6.5: 14 pass / 1 fail both trees). Pre-existing staleness (the pin predates even the sweep-sessions cron route). Our plan's route-inventory obligations are covered by `route-inventory.test.ts` (15/0) + gateway static-assertions (17/0). Recommended follow-up for repo owners (outside this plan's scope): update the probe's expected list to the full disk truth.
2. **lane-debit "no inArray" source pin** — fails identically on `origin/main` (verified twice: 4.2 report + 7.1 report; 3/3 deterministic). Pre-existing docblock-text artifact; not this plan's diff.
3. **`backend/graphql/test/session-booking-balance.test.ts`** — planned by the blocking ticket but never shipped there (verified absent on main). Out of this plan's scope; recorded for the sibling ticket's owners.

## IV (8.1.IV)

Instructions re-verified against every touched file via the sub-loop runs above (each run prints the applicable AGENTS.md + `.agents/instructions/*.md` files; all previously read in full per-task and re-checked at 6.5).

## Verdict

**8.1 gate: GREEN.** Zero new errors vs baseline on every axis; 22/22 per-file gate; all mandated test layers green; deferred ledger at the exact sanctioned state. Cleared to proceed to 9.1 (knowledge propagation), after which the deferred grep MUST be re-run to 0.
