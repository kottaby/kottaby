# Task 5.2 — Booking Expiry Gate Outcome

**Plan:** `ai/plans/sprint_1/subscription-validity-window-expiry/`
**Executed:** 2026-09-12
**SKILL.md section:** §Task Execution Protocol (per-task pipeline QL → TE → SEC → SR → IV)

---

## Summary

Inserted the lane-aware subscription-expiry gate into `debitBookingLadder` as a strict failure-branch insertion (plan Decision D3): trial debit → intent-lane debit → **only on double-miss**, the one-shot `hasUncoveredExpiredLane` probe → `SUBSCRIPTION_EXPIRED` denial → else the pre-existing `INSUFFICIENT_BALANCE` denial. Ladder order preserved verbatim; trial exemption is structural (a trial-funded booking returns before the gate is ever reached); zero added reads on the successful-booking hot path.

## Files Modified

| File | Change |
|---|---|
| `backend/services/classes/session-lifecycle.booking.ts` | +51/−4: module-local frozen `HELD_LANE_TO_CREDIT_LANE` (exactly two members Hifz/Tajweed — trial structurally absent, INV-B3), `isSubscriptionFundedHeldLane` narrowing helper, `SubscriptionCreditLane` value import, `SubscriptionRepository` import, gate block inside `if (!intentDebited)` BEFORE the `INSUFFICIENT_BALANCE` log+throw (`logDomainError` + `throw new ValidationError("SUBSCRIPTION_EXPIRED", t.subscriptionExpired)`), docblock updated to describe the new ladder step |
| `backend/services/classes/session-lifecycle.booking.test.ts` | +291/−2: six new DB-backed ladder-denial tests (see TE below) |

## Files NOT Modified (negative evidence — Decision D7 / REQ-061)

- **Zero frontend changes.** Verified: `createSessionMutationDocument` is referenced only by its own document file + `documents.contract.test.ts` — no wired consumer exists; `SUBSCRIPTION_EXPIRED` has zero occurrences in `frontend/`; `mapGraphQLErrorByCode("SUBSCRIPTION_EXPIRED", …)` returns `null` (the client error map has no custom-domain-code row — `mapValidationRow` matches the literal `VALIDATION` only; `normalizeGraphQLErrorCode` folds only `RATE_LIMIT_EXCEEDED`). The denial therefore rides `extensions.code` + the server-localized `ValidationError` message; the snackbar arm stays deferred to the future booking-UI surface (ledger D3 obligation recorded for 9.1).

## Verification Results

### 5.2.QL — quality loop
`bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates` → **exit 0** for both files (tsgo → oxlint → biome → lint:type-aware → check:duplicates). Caches never cleared.

### 5.2.TE — test engineering (booking suite: 26 tests, 0 fail, 2208 assertions)
New DB-backed cases (all `runInRollback` + `tx`, try/catch helpers, seeded via real activation/expiry paths):
1. expired + zeroed lane → `SUBSCRIPTION_EXPIRED` thrown BEFORE any write (balances/sessions unchanged)
2. expired subscription + `balance_trial > 0` → booking SUCCEEDS via the trial lane (INV-B3)
3. in-window subscription on an empty lane → probe answers false → unchanged `INSUFFICIENT_BALANCE`
4. never-subscribed empty lane → `INSUFFICIENT_BALANCE` regression intact
5. expired-over-insufficient precedence on the Tajweed lane → expiry denial wins
6. GraphQL denial shape: single thrown denial carrying `extensions.code === "SUBSCRIPTION_EXPIRED"`

### Journey gate (task 3.1 checkpoint — now GREEN)
`bun run test/scripts/run-test.ts test/workflows/billing/subscription-expiry.journey.test.ts` → **7 tests, 0 fail, 77 assertions** (exit 0). The test-first RED suite from Phase 3 passes against the real DB end-to-end: activate → window pin → backdate → `expireDue()` flip+zero (trial intact) → booking denial `SUBSCRIPTION_EXPIRED` (zero writes) → trial booking success → Student B byte-identical → replay `{ expired: 0, lanesZeroed: 0 }`.

### 5.2.SEC — security
The gate evaluates ONLY the authenticated booking student inside their own transaction (the probe predicates key on `user_id`); no scope-gate changes (BFLA unchanged); no new inputs; lane mapping is a frozen enum-keyed map (caller strings cannot select a probe shape).

### 5.2.SR — semantic review
Insertion-only (no ladder redesign); gate reachable exactly when both debits miss (no dead branch — the probe sits inside the existing double-miss branch); frozen two-member map; enum value imports; scope boundary respected (exactly the 2 files).

### 5.2.IV — instruction verification
Root `AGENTS.md`, `backend/AGENTS.md`, `backend/services/AGENTS.md`, `backend/db/test/AGENTS.md`, `.agents/instructions/backend.instructions.md`, `.agents/instructions/tests.instructions.md` — read in full; compliance recorded above.

## Carry-Forward

- 6.1 (cron route) delegates `SubscriptionExpiryService.expireDue()` — the service is green and its contract is `{ expired, lanesZeroed }` counts-only.
- The journey suite is the regression pin for the whole ticket; re-run it after 6.1 (route registration must not break route-inventory static assertions).
- Sandbox hazard note: the first 5.2 dispatch hit a working-tree wipe (recovered via `/tmp` final copies + `reset --hard origin/feat`); the one-shot recover→test→commit→push script pattern (`scripts/one-shot-5.2.sh`) is the reliable execution unit in this environment.
