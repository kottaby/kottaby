# Task 0.1 — Pre-Implementation Baseline Outcome

**Plan:** `ai/plans/sprint_1/subscription-validity-window-expiry/`
**Executed:** 2026-09-12 (sandbox session)
**SKILL.md section:** §Phase 0 — Pre-Implementation Baseline (MANDATORY)

---

## Baseline Counts (all recorded before ANY implementation edit)

| Check | Command | Result | Baseline file |
|---|---|---|---|
| tsgo typecheck | `bun run tsgo` (`tsgo -b --noEmit`) | **0 errors** | `/tmp/baseline-tsgo.txt` |
| biome | `bunx @biomejs/biome check .` (no-write variant for baseline; the repo script adds `--write --unsafe`) | **0 errors / 0 warnings — 1777 files clean, exit 0** | — |
| lint-service | `bun run scripts/lint-service.ts --json --id baseline` | **success: true, exitCode 0, full-repo scope, 105s** | — |
| git modified-file set | `git diff --name-only` | **empty — clean tree at `origin/main` = `2bdea32`** | `/tmp/baseline-files.txt` |
| git stash list | `git stash list` | **empty** | `/tmp/baseline-stash.txt` |

**Branch:** `feat/subscription-validity-window-expiry` (auto-derived per SKILL.md — `BRANCH` variable not provided; created from `origin/main` @ `2bdea32`).

## Environment Notes (sandbox)

- **DB_MODE=postgres honored.** No Postgres daemon ships with the sandbox; provisioned a user-space PostgreSQL **17.11** via `apt-get download` + `dpkg -x` (binaries under `/tmp/pg/pgroot/usr/lib/postgresql/17/bin`), `initdb -D /tmp/pgdata -U postgres --auth=trust`, `pg_ctl` start on `127.0.0.1:5432` (unix socket `/tmp/pgsocket`), `createdb kottaby_db`.
- **`.env`** written with `DB_PROVIDER=postgres`, `DATABASE_URL=postgresql://postgres@127.0.0.1:5432/kottaby_db`, dev-only encryption keys, `ADMIN_EMAIL/ADMIN_PASSWORD` per session variables, plus the cron gates the plan consumes (`CRON_EXECUTION_MODE=external`, `CRON_EXTERNAL_ENABLED=true` — route tests flip these per-case anyway).
- **`.env.test`** written for the `bun --env-file=.env.test` test entry points (mirrors `.env.test.ci` fixture posture: `TEST_SERVER=1`, `TEST_CI=1`, deterministic 64-hex `DATABASE_ENCRYPTION_KEY`, `AUTH_COOKIE_SECURE=false`), pointed at the same sandbox Postgres.
- **Schema:** `bun run db push` → `✓ Changes applied` (drizzle.config.ts, postgres dialect). **Seed:** `bun run db seed` → all seed steps executed successfully (demo student trial reconcile: 1 student processed).
- **`bun install`**: 1260 packages, no errors.
- Sandbox RAM is limited (~4 GB): prior-session worklog notes full `bun quality-gate` / `lint-service` deep runs can OOM (SIGABRT); the per-file equivalent gate (`scripts/health/sub-loop.ts <file> --lifecycle duplicates`) is the sanctioned per-file verification and is what tasks use. lint-service full-repo run above completed green in 105 s, so the sandbox currently sustains it.

## Pre-existing Issues to Ignore During Post-Implementation Review

- None recorded: tsgo 0, biome 0, lint-service green on a clean tree. Any finding in the review waves on files OUTSIDE this plan's diff is pre-existing by construction (baseline diff is empty) and must be filtered.
- Known stale-but-harmless repo facts (documented in research digests, NOT to be "fixed" here): `package.json:63` `cron:worker` points at non-existent `scripts/cron-worker.ts`; `package.json:27` `test:cron` targets missing `backend/services/cron/test/`, `backend/db/test/repo/cron.repository.test.ts`, `backend/lib/cron-auth.test.ts`; `.env.test.ci` references `scripts/ci/materialize-env-test.ts` which does not exist (that file is why `.env.test` was hand-materialized above).

## Ledger Confirmation

`deferred-items.md` EXISTS (authored at spec time). Current ledger state: **D1 ✅** (O1 zeroing semantic ratified by `outcome/plan-review-R1.md`), **D3 ✅** (booking-UI error arm deferred to the surface that lands it, ratified), **D2 ❌** (external scheduler wiring — the single sanctioned open item; closes at 9.1 via the canonical doc's ops handoff). Expected `grep -c "❌\|⚠️"` over the Ledger Table at Phase 8 = exactly 1 (D2); 0 after 9.1.

## Carry-Forward

- Every implementation task MUST compare against THIS file's counts (tsgo 0 / biome 0 / lint 0).
- Review waves scope = `git diff --name-only` vs the (empty) baseline — i.e. exactly this plan's touched files.
- Test runner discipline: all suites via `bun run test/scripts/run-test.ts <path>`; DB/service layers under `runInRollback` + `tx`.
