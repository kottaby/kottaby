# 6.1 Outcome — Review Wave 6.1 (review-types)

> Plan: DEV2-004 · Task: tasks.md 6.1 (Phase 6 — Review Waves)
> Scope: types/enums/Pothos registration discipline over every ticket file.
> Method: read-only review (NO source modified, nothing committed). Rule set read: `backend/types/AGENTS.md`, `backend/enum/AGENTS.md`, `backend/db/repo/AGENTS.md`, `backend/services/AGENTS.md`, `backend/graphql/AGENTS.md` + `query/AGENTS.md` + `pothos/AGENTS.md`, `.agents/instructions/backend.instructions.md`, `docs/graphql/domain-error-extensions-code.md`, plan-review-R1 DISP-1..7, and ALL outcome files phase0→6.0 (standing interpretations honored as non-findings).

## VERDICT: PASS — 0 CRITICAL, 0 HIGH, 0 MEDIUM, 0 LOW code findings. 1 informational note.

## Per-file results

| File | Checks | Verdict |
|---|---|---|
| `backend/enum/teachers/applicant-status.enum.ts` | Canonical location per enum-layer layout; file imports NOTHING; enum + guard exported as values (no `export type`); member set byte-matches the DBML `applicants.status` note (`pending, in_evaluation, failed, passed`) | PASS |
| `backend/enum/teachers/index.ts` | Barrel: `./` relative, `export *` only, alphabetical; top-level `backend/enum/index.ts:6` re-exports `./teachers` (verified) | PASS |
| `backend/enum/teachers/applicant-status.enum.test.ts` | Pure unit tier, no DB/schema imports, no `.skip`/`.only`, no `console.*`, bun:test only | PASS |
| `backend/types/teachers/applicant.types.ts` | Sole canonical `ApplicantProfileReturnType` definition repo-wide (grep: only JSDoc mentions elsewhere — no duplicate type definitions anywhere, incl. no `{Entity}Definition` clones and no service-layer/local Pothos types); closed all-`readonly` 7-field shape; zero governance/secret/write-typed fields; `import type { ApplicantStatus }` = recorded 1.2 Deviation #1 / CF-4 standing interpretation (zero runtime usages in this file — NOT flagged per outcome/1.2); `@/` aliases throughout; `ApplicantSelectType`/`ApplicantInsertType` untouched | PASS |
| `backend/db/repo/teachers/applicant.repository.ts` | Types angle: canonical `ApplicantSelectType` + `DBQueryExecutor`/`DBTransaction` from `@/backend/types`; enum deliberately NOT imported (no status semantics at repo layer — 2.1.SR clause confirmed) | PASS |
| `backend/services/teachers/{applicant-lifecycle.service.ts,index.ts,+test}` | NO service-layer `.types.ts` anywhere (net-new count = 0); canonical types via `@/backend/types` barrel; namespace-per-domain pattern; barrel `./` relative + `export *`; top-level `backend/services/index.ts` registers `./teachers` (DISP-2) | PASS |
| `backend/graphql/pothos/shared/enum.pothos.ts` | SINGLE registration of `ApplicantStatus` repo-wide (grep-verified: one `gqlSchemaBuilder.enumType(ApplicantStatus, …)`); enum-object form backed by the real TS enum; VALUE import (runtime position — first arg of `enumType`); no literal `values: [...]` arrays in any `*.pothos.ts` | PASS |
| `backend/graphql/pothos/teachers/applicant.pothos.ts` | Single canonical object: `objectRef<ApplicantProfileReturnType>("ApplicantProfile")` — generic IS the canonical type; ZERO local type definitions; registered `ApplicantStatusPothosEnum` imported from the shared registry (no re-registration); exactly the 7 canonical fields (CF-3 surface respected — no extras); `id` exposed (Apollo cache); nullable `String` timestamps per DISP-5; no domain barrel created (matches all sibling pothos dirs — binding 3.2 wiring interpretation) | PASS |
| `backend/graphql/query/teachers/{applicant.query.ts,index.ts}` + `backend/graphql/query/index.ts` | Pothos object imported directly from `@/backend/graphql/pothos/teachers/applicant.pothos` per query/AGENTS.md convention (recorded binding from 3.2); side-effect barrels (`import "./applicant.query";` / `import "./teachers";`); no named exports from the query file | PASS |
| `shared/locale/**` (applicant namespace + errors extension) | `ApplicantLabels` interface + ar/en leaves compile-typed; handle id `"applicant.applicant"` mirrors `<ns>.<ns>` convention; `Translations`/bundle/registry wiring additive; 3 new `errors` keys additive with JSDoc REQ pins; imports all `@/shared/locale/...`; parity suite pins key-set + placeholder-shape mechanically | PASS |
| `frontend/graphql/test/teachers/*`, `frontend/graphql/sharedDocuments/teachers/*` | Document typed `TypedDocumentNode<MyApplicantProfileQuery>`; selection = exactly the 7 fields; DISP-1 top-barrel registration present (`export * from "./teachers"` in `sharedDocuments/index.ts`); integration test consumes the shared document (codegen types — no duplicate wire shapes) | PASS |

## Enum VALUE-vs-TYPE import audit (every import site of `ApplicantStatus` / `UserRole`)

| Site | Form | Runtime anchor | Verdict |
|---|---|---|---|
| `backend/types/teachers/applicant.types.ts:9` | `import type` | none in file | ✅ standing Deviation #1 (CF-4: reviewers must not flag) |
| `backend/services/teachers/applicant-lifecycle.service.ts:41` | VALUE | `ApplicantStatus.Passed` compare + `isApplicantStatus` | ✅ |
| `backend/graphql/pothos/shared/enum.pothos.ts:22` | VALUE | `enumType(ApplicantStatus, …)` | ✅ |
| `backend/graphql/pothos/teachers/applicant.pothos.ts` | VALUE (`ApplicantStatusPothosEnum`) / `import type` (`ApplicantProfileReturnType`) | field type ref / shape contract | ✅ |
| `backend/graphql/query/teachers/applicant.query.ts:46` | VALUE (`UserRole`) | `role: [UserRole.Teacher]` authScopes | ✅ |
| `backend/db/test/logic/auth/registration.service.test.ts:46` | VALUE via `@/backend/enum` barrel | `ApplicantStatus.Pending` compare | ✅ (see Note-1) |
| `backend/db/test/logic/teachers/*.test.ts`, `backend/services/teachers/*.test.ts` | VALUE | runtime compares | ✅ |

Biome `useImportType` normalization did not degrade any runtime position — every value import above has a genuine runtime anchor (the CF-1/CF-2 obligations from outcome/1.2 discharged at 2.2/3.1).

## Findings

| # | Severity | Location | Finding | Repair path |
|---|---|---|---|---|
| Note-1 | LOW / informational | `backend/db/test/logic/auth/registration.service.test.ts:46` | `ApplicantStatus` imported from the `@/backend/enum` barrel; `backend/enum/AGENTS.md` says deep imports are *preferred* (barrel explicitly "available for convenience"). Soft-preference only; deliberate per outcome/5.1 ("REQ-012 discipline"). No rule violation — recorded so future reviewers don't re-litigate. | Optional: switch to `@/backend/enum/teachers/applicant-status.enum` on the next substantive edit of this file. No action required this ticket. |

No CRITICAL/HIGH/MEDIUM findings. No duplicate type definitions, no misplaced `.types.ts`, no mis-registered enum/object, no relative cross-layer imports, no canonical-shape drift.

## Suites re-run (this wave)

| Suite | Result |
|---|---|
| `backend/enum/teachers/applicant-status.enum.test.ts` (run-test.ts) | ✅ 21 pass / 0 fail / 278 expects |
| `bun tsgo` (full repo) | ✅ exit 0 — 0 errors |

(Service + repo lifecycle suites re-run under wave 6.2's record below; full-repo biome was green at 5.5 with no source edits since.)

## Residual risk

- GraphQL enum member names (`Pending|InEvaluation|Failed|Passed`) ride the Pothos TS-member-name convention (3.4 recorded) while the DB varchar stores lowercase values — safe because the service guard (`isApplicantStatus`) is the only bridge and the integration suite pins the wire value `"Pending"` live; any future writer that bypasses the service guard would surface here first.
- `assertCanPurchaseVerification` / `recordReapplication` are exported but have no production consumer until DEV2-005 — recorded forward contract (5.5), not dead code.
