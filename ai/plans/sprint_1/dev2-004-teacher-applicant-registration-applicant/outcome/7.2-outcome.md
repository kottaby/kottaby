# 7.2 Outcome — Backward Cross-Link & AGENTS.md Propagation

> Task: tasks.md 7.2 (Phase 7 — Knowledge Propagation & Documentation)
> Requirements: REQ-081, REQ-082 (+ DI-7 disposition from deferred-items.md)
> Recorded: 2026-08-27
> Do-not-commit honored; Task 7.3 NOT implemented per orchestrator instruction.

## Summary

Propagated one-line rule/reference entries across the knowledge graph — every edit is exactly one line (one file is a 2-line markdown blockquote continuation; one is a Layout tree-row continuation), zero code, zero implementation detail, zero duplicated content. The canonical doc (`docs/teachers/applicant-lifecycle.md`, Task 7.1) is referenced from `docs/auth/user-registration.md`, `backend/services/AGENTS.md`, and root `AGENTS.md`; the sharedDocuments Layout inventory gained the shipped `applicant.documents` entry (4.1/4.3 carry-forward); `backend/enum/AGENTS.md` was verified and deliberately NOT edited (convention conditional resolved false — rationale below).

## Propagation edits (file → one-line summary each)

| File | Δ | One-line summary |
|---|---|---|
| `docs/auth/user-registration.md` | +2 (blockquote continuation) | §1 "Role → Child Table Mapping" — appended one line to the B.6/B.7 applicant blockquote (the file's Applicant/lifecycle anchor, line ~23 per plan-review-R1 finding 18) linking the post-registration lifecycle to `docs/teachers/applicant-lifecycle.md` (REQ-081). |
| `backend/services/AGENTS.md` | +1 | One-line rule at the top bullet list (after Registration/Auth doc references): applicant lifecycle logic lives in `ApplicantLifecycleService` (`backend/services/teachers/`) — cooldown/attempt contracts (REQ-014/015/016) owned there; see `docs/teachers/applicant-lifecycle.md`. |
| `backend/enum/AGENTS.md` | 0 | **SKIP — convention verified, conditional false** (rationale below). |
| `AGENTS.md` (root) | +1 | Important References gained one line: `docs/teachers/applicant-lifecycle.md` — Teacher applicant lifecycle canonical reference (state machine REQ-013, cooldown/attempt contracts, zero-arg query contract, INV-TV1..TV7 + B.6/B.7 anchoring, consumer guidance). |
| `frontend/graphql/sharedDocuments/AGENTS.md` | +2/−1 (Layout tree-row continuation) | teachers/ row in the per-domain Layout inventory gained `applicant.documents (myApplicantProfileQueryDocument)` — discharges the 4.1 outcome carry-forward ("applicant.documents was NOT appended … flagging for the next docs-touching leg") and 4.3 deviation #3 ("teachers-docs list entry still owed at knowledge propagation"). The Layout DOES list per-domain file inventories ⇒ conditional true ⇒ edit made. |
| `docs/specs/state-machine-invariants.md` | 0 | **UNTOUCHED (REQ-081)** — diff-empty proof below. |

### `backend/enum/AGENTS.md` skip rationale (7.2.IV convention verification)

The task mandates: "add the `ApplicantStatus` enum entry IF the file's listing conventions require per-enum entries (verify convention first; otherwise note skip rationale)". Verified against the file as it exists on this tree:

1. The Layout section lists **per-subdirectory domain glosses**, never individual enums (`shared/ cross-cutting reference data …`, `teachers/ teacher-availability enums`, `students/ student-lifecycle enums`) — no enum anywhere in the file is listed by name.
2. "Adding New Enums" is a 5-step procedure (subdir → file → barrel → top barrel → Pothos registration + codegen), all steps of which were already discharged by Task 1.1 (barrel wiring) and Task 3.1 (Pothos registration + codegen) — the file itself requires no edit to reflect the new enum.
3. Adding a one-off `ApplicantStatus` name entry would introduce a listing convention used by no other enum (REQ-082: "entries are rules/references only — no … implementation detail"; the enum's details live in its own file + the canonical doc §1).

**Informational observation (not acted on):** the `teachers/` row gloss reads "teacher-availability enums" while the directory actually holds `teacher-request-preference.enum.ts` + `applicant-status.enum.ts` — a pre-existing gloss-drift shared with other rows' style; repairing it is out of this task's one-line-entry mandate (candidate for a future docs-maintenance pass, same class as DI-4).

## DI-7 disposition (historical-record footnote — location audit)

DI-7 (deferred-items.md): *"outcome/2.2 prose says 'five logDomainError sites'; code has exactly 3 … Documentation-count fix owed during knowledge propagation."* Discharged as prescribed by outcome/6.2 Note-2 ("cite '3 log sites / 5 enumerated rejections (2 of which are silent by design)'") — with the location audit the orchestrator requested:

1. **Where the wrong count actually appears** — repo-wide grep for the prose (`five logDomainError|logDomainError sites|five log`): the ONLY occurrences of the wrong count are inside plan artifacts: `outcome/2.2-outcome.md` (Security & Tenancy Audit bullet: "five enumerated ``logDomainError`` sites"; Deviation #5: "F2/F3 five log sites total"), plus DI-7/outcome-6.5 rows that DESCRIBE the error. The shipped code has exactly 3 sites (verified by direct read of `backend/services/teachers/applicant-lifecycle.service.ts`: guard missing-row, guard cooldown-active, reapplication miss).
2. **`backend/services/AGENTS.md` mentions `logDomainError` counts?** NO — the file (read in full, all 168 lines) contains no `logDomainError` mention at all, and no count of any logging sites; its i18n section concerns `getServerTranslations` wording only. Repo `docs/` knowledge (grep over `docs/**`): `logDomainError` appears in `error-handling-contract.md`, `user-registration.md`, `jwt-authentication-service.md` — none with a site count.
3. **Resolution**: the wrong count exists ONLY in `outcome/2.2` (a plan artifact / historical record). Per orchestrator instruction, **outcome files were NOT edited** (historical records); the correction enters repo knowledge via the canonical doc's §8 Error Contract: "**3** `logger.logDomainError` sites / 5 enumerated rejections, 2 of which are silent by design". DI-7 therefore closes as a **historical-record footnote only** — the ledger row's target ("Task 7.2 propagation leg") is satisfied by the canonical doc + this audit, not by patching 2.2.

## Quality Loop (7.2.QL) — sub-loop per modified file (recorded non-applicability)

`bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates` on every modified file (pattern identical on all five — recorded honestly per the task's "exit code 0 or documented non-applicability"):

| File | tsgo stage | oxlint stage | Overall |
|---|---|---|---|
| `docs/auth/user-registration.md` | ✅ passed ("no errors for docs/auth/user-registration.md") | ❌ exit 1 — `No files found to lint … on 0 files with 301 rules` | **NON-APPLICABLE for `.md`** |
| `backend/services/AGENTS.md` | ✅ passed | ❌ same empty-scope stop | **NON-APPLICABLE for `.md`** |
| `AGENTS.md` (root) | ✅ passed | ❌ same empty-scope stop | **NON-APPLICABLE for `.md`** |
| `frontend/graphql/sharedDocuments/AGENTS.md` | ✅ passed | ❌ same empty-scope stop | **NON-APPLICABLE for `.md`** |
| `backend/enum/AGENTS.md` (control — UNTOUCHED) | ✅ passed | ❌ same empty-scope stop | control proof: failure is file-independent |

**Non-applicability evidence (same as 7.1):** oxlint's configured scope contains zero markdown files, so the stage exits 1 on an empty file set regardless of content — the identical exit occurs on the untouched control file. biome: markdown ignored by biome.json ("No files were processed in the specified paths"). eslint: config scopes `**/*.md` (eslint.config.mjs:56) but the eslint toolchain cannot boot in this sandbox (`typescript-eslint does not support TS 7.0`) — reproduced identically on pre-existing `.md` files, so zero file-level findings are obtainable; same anomaly class as phase0's documented lint-service quirk (re-confirmed: `bun run lint` → exit 1, empty output). Compensating full-repo gates below.

### Full-repo gates

| Gate | Result |
|---|---|
| `bun tsgo` | exit **0**, 0 errors (matches baseline; zero NEW) |
| `bun biome:check` | exit **0** — "Checked 500 files in 9s. No fixes applied." — 0 issues (markdown outside biome scope; count unchanged from 5.5) |

### REQ-081 invariant-file diff check (quoted verbatim)

```
$ git diff -- docs/specs/state-machine-invariants.md
(empty — zero output)

$ git status --porcelain -- docs/specs/
(no lines — nothing under docs/specs/ modified or untracked)

$ git status --porcelain | grep -c "state-machine-invariants"
0   (grep exit 1 = zero matches)
```

## Semantic Review (7.2.SR)

- ✅ **Entries are rules/references only**: every inserted line is a pointer or one-line ownership rule — no code, no signatures beyond names, no behavioral prose (REQ-082).
- ✅ **No duplicated content**: `grep -c "docs/teachers/applicant-lifecycle.md"` = **1** in each of root `AGENTS.md`, `backend/services/AGENTS.md`, `docs/auth/user-registration.md` (single mention per file; the canonical doc holds the substance).
- ✅ **Links resolve**: `docs/teachers/applicant-lifecycle.md` exists; existence-checked all referenced targets (`backend/services/teachers/applicant-lifecycle.service.ts`, `frontend/graphql/sharedDocuments/teachers/applicant.documents.ts`, `docs/specs/state-machine-invariants.md`, `docs/specs/open-decisions-and-gaps.md`, `docs/workflows/01-teacher-verification-workflow.md`, `docs/graphql/domain-error-extensions-code.md`) — all OK. `applicant.documents` entry ↔ actual file `applicant.documents.ts` (const name annotated inline for findability).
- ✅ **Diff minimality**: `git diff --stat` over the five files = `AGENTS.md +1 · backend/services/AGENTS.md +1 · docs/auth/user-registration.md +2 · frontend/graphql/sharedDocuments/AGENTS.md +2/−1 · backend/enum/AGENTS.md (absent — untouched) · docs/specs/state-machine-invariants.md (absent — untouched)`. Nothing else in the tree was touched by this task (pre-existing working-tree deltas belong to Phases 0–6 per prior outcomes).

## Instruction Verification (7.2.IV)

- **Root AGENTS.md**: entry appended to the existing `## Important References` bullet list, matching the section's `- \`path\` — description` format and its canonical-reference ordering (placed after the most recent plan-doc entries).
- **backend/services/AGENTS.md**: entry added as a bold one-line bullet in the file's top rule list, mirroring the neighboring `Registration: see docs/auth/…` / `Auth service: see docs/auth/…` doc-reference pattern.
- **backend/enum/AGENTS.md**: convention verified (see skip rationale) — no edit is the convention-compliant action.
- **docs/auth/user-registration.md**: link added INSIDE the existing B.6/B.7 applicant blockquote (§1) — the exact anchor plan-review-R1 finding 18 identified for the REQ-081 cross-link; blockquote continuation syntax (`>`) preserved.
- **frontend/graphql/sharedDocuments/AGENTS.md**: Layout tree-row continuation, matching the row's own continuation style and the "Document any new sub-directory under Layout" rule's intent (here: the shipped file within a pre-listed domain row).

## Files NOT modified

- `docs/specs/state-machine-invariants.md` — REQ-081 (diff-empty proof above).
- `backend/enum/AGENTS.md` — conditional false (skip rationale above).
- All `outcome/*` files except this one and 7.1's — historical records (DI-7 audit, no in-place patch).
- `tasks.md`, `plan.md`, `deferred-items.md`, all source/test files — doc-propagation task; zero code deltas.
