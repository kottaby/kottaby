# 3.1 Outcome — ApplicantStatus Pothos Enum Registration

> Task: tasks.md 3.1 (Phase 3 — GraphQL Resolvers & API Handlers)
> Requirements: REQ-061, REQ-012
> Recorded: 2026-08-27
> Do-not-commit honored: nothing staged/committed; file delta sits alongside sibling-task changes in the working tree.

## Summary

Registered `ApplicantStatus` EXACTLY ONCE, in `backend/graphql/pothos/shared/enum.pothos.ts`, using the **enum-object form** mandated by the CRITICAL rule in `backend/graphql/pothos/AGENTS.md` / `backend/graphql/AGENTS.md` (plan D6). No `values: [...]` literal registration, no inline enum definition, no second registration site anywhere under `backend/graphql/**`.

## Exact change (verbatim from source)

Import added (VALUE import — runtime use as `gqlSchemaBuilder.enumType`'s first argument; Biome organize-imports sorts it lexicographically first among the `@/backend/enum/*` group):

```typescript
import { ApplicantStatus } from "@/backend/enum/teachers/applicant-status.enum";
```

Registration appended after `RecitationReadingPothosEnum` (the file's established convention: newest registration appends last with its own JSDoc — same pattern DEV1-003 used for `RecitationReading`; the header's "DEV1-002 registered enums" list was not retro-edited, matching precedent):

```typescript
export const ApplicantStatusPothosEnum = gqlSchemaBuilder.enumType(ApplicantStatus, {
  name: "ApplicantStatus",
});
```

Direct module import path (`@/backend/enum/teachers/applicant-status.enum`) chosen over the `@/backend/enum` barrel because that is THE FILE'S existing style (all four prior imports are direct `.enum` module paths) — instruction says "follow the file's existing import style".

## Uniqueness verification (grep evidence)

Grep `ApplicantStatus|values:\s*\[` across `backend/graphql/**`:

- ONE `gqlSchemaBuilder.enumType(ApplicantStatus, …)` call — `shared/enum.pothos.ts` only.
- ZERO Pothos files containing a `values:` literal array; the only `values: [` hits are the PROHIBITED-pattern examples inside `backend/graphql/pothos/AGENTS.md` and `backend/graphql/AGENTS.md` (documentation).
- Pre-change sweep confirmed no pre-existing `ApplicantStatus` token anywhere in `backend/graphql/**` (first registration = no collision).

Name-collision check (3.1.SEC): GraphQL name `"ApplicantStatus"` declared once in the schema builder registry; no other `enumType(...)` uses that name. Runtime re-registration hazard ("X has already been declared") structurally impossible from this change.

## Value set guarantee

TS enum members (single source of truth `backend/enum/teachers/applicant-status.enum.ts`, guarded by its own pinned suite, 21 tests): `Pending="pending"`, `InEvaluation="in_evaluation"`, `Failed="failed"`, `Passed="passed"` → SDL will render `PENDING | IN_EVALUATION | FAILED | PASSED`. The Task 2.M gate already pinned "ApplicantStatus is TS-enum-only, zero schema edits" — untouched here.

## Verification outputs

### Per-file quality loop

`bun run scripts/health/sub-loop.ts backend/graphql/pothos/shared/enum.pothos.ts --lifecycle duplicates` → ✅ **exit 0** on first run (tsgo ✅ / oxlint ✅ / biome:check ✅ / lint:type-aware ✅ / check:duplicates ✅).

Instruction discovery printed `.github/instructions/backend.instructions.md` as applicable-but-absent on disk (same DISP-3 discover-absent pattern as Tasks 1.x/2.x); effective rules read: root `AGENTS.md`, `backend/AGENTS.md`, `backend/graphql/AGENTS.md`, plus content-governing `backend/graphql/pothos/AGENTS.md`.

### Full-repo gates

| Gate | Result |
|---|---|
| `bun tsgo` | exit **0**, 0 errors |
| `bun biome:check` | exit **0** — "Checked 490 files in 4s. No fixes applied." (baseline 489 + new Task-3.2 file) |
| Codegen | **Deliberately NOT run** — orchestrator + tasks.md order schema regeneration at Task 3.4 (after 3.3 resolver lands), superseding the generic post-edit codegen reminder in backend instructions |

## Files modified / NOT modified

| File | Action |
|---|---|
| `backend/graphql/pothos/shared/enum.pothos.ts` | MODIFY (+1 import line, +11-line JSDoc'd registration block). Nothing else touched. |
| `schema.graphql` / generated SDL | NOT touched by hand (regeneration owned by Task 3.4) |
| `backend/db/**`, `db/schema.dbml` | NOT touched (`git diff -- backend/db/schema backend/db/migration db/schema.dbml` EMPTY — REQ-045 intact) |

## Semantic Review (3.1.SR)

✅ Single registration site · ✅ enum-object backed by a real TS enum VALUE import · ✅ no duplicate definitions in `backend/graphql/**` · ✅ no string-literal drift surface · ✅ zero dead code (registration is consumed transitively via the `gqlSchema.ts` side-effect chain that already imports `shared/enum.pothos` once).

## Instruction Verification (3.1.IV)

Validated verbatim against `backend/graphql/pothos/AGENTS.md` CRITICAL RULE workflow steps 1–4 (steps: canonical enum exists ✔; barrel re-export verified in Task 1.2 ✔; registered once HERE ✔; domain-file reference-by-import demonstrated in Task 3.2's `applicant.pothos.ts` ✔; step 5 regen deferred to 3.4 per task ordering).

## Carry-forward

**CF → Task 3.4**: `generate:gqlSchema` must now emit `enum ApplicantStatus { PENDING IN_EVALUATION FAILED PASSED }` exactly; Task 5.3 integration asserts that SDL (3.1.TE is satisfied THERE, not now).
