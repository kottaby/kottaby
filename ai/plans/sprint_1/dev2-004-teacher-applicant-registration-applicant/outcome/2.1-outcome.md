# 2.1 Outcome — ApplicantRepository Read + Atomic Attempt-Increment Methods

> Task: tasks.md 2.1 (Phase 2 — Repositories & Backend Services)
> Requirements: REQ-014, REQ-041, REQ-042, REQ-044, REQ-032 (+ REQ-072 sequencing evidence)
> Recorded: 2026-08-27
> Do-not-commit honored: nothing staged/committed; working tree carries this task's deltas alongside sibling-task changes.

## Summary

Added the two net-new methods to `ApplicantRepository` exactly per the 0.2 determination:

- `findByUserId` — mirrors the `UserRepository.findByEmail` / `findById` READ pattern verbatim: optional **`tx?: DBQueryExecutor` LAST param** + private module-scope `isDBTransaction(tx)` type guard → Drizzle select inside a transaction, else raw parameterized SQL via `queryDb` (Neon HTTP fast path). Returns the row or `null`.
- `recordVerificationAttempt` — SINGLE atomic in-place UPDATE: `verification_attempts = verification_attempts + 1`, `last_attempt_at = now()`, `updated_at = now()` expressed via Drizzle (`sql\`${applicants.verificationAttempts} + 1\``, no read-modify-write anywhere), parameterized `eq(applicants.id, userId)`, `.returning()` (RETURNING \* audit row). Optional `tx?: DBTransaction` last param with global-`db` fallback per the PLAN (tasks.md 2.1 mandates "every method takes optional `tx?` as the LAST parameter"; repo AGENTS required-tx rule covers registration *create* repos, which remain untouched). Zero rows matched ⇒ `null` (Task 2.2 converts to NotFoundError).

New 4-Tier DB test suite lives at `backend/db/test/logic/teachers/applicant-lifecycle.test.ts` (**10 tests, 0 fail**, 38 expect() calls, via the mandated runner). All hard constraints respected: existing `create` untouched; repo file imports NOTHING from frontend/shared/app layers; canonical types only; no `console.*`; no prepared statements on the write; no `inArray`; no `sql.placeholder`; no inline `--` comments inside any sql template; no string concatenation of the id.

## Exact final signatures (verbatim from source)

```ts
function isDBTransaction(tx: DBQueryExecutor): tx is DBTransaction   // module-private guard, byte-equivalent to user.repository.ts

export namespace ApplicantRepository {
  export async function create(userId: number, tx: DBTransaction): Promise<ApplicantSelectType>          // UNTOUCHED

  export async function findByUserId(userId: number, tx?: DBQueryExecutor): Promise<ApplicantSelectType | null>

  export async function recordVerificationAttempt(
    userId: number,
    tx?: DBTransaction
  ): Promise<ApplicantSelectType | null>
}
```

(POST-sub-loop note: Biome formatted `recordVerificationAttempt`'s signature onto three lines during `biome:check --write`; content unchanged.)

Raw SQL used by `findByUserId` non-tx arm (all 7 schema columns verified against `backend/db/schema/teachers/applicants.ts`; aliases mirror camelCase keys exactly as user.repository.ts does):

```sql
SELECT id,
       verification_attempts AS "verificationAttempts",
       last_attempt_at AS "lastAttemptAt",
       cooldown_until AS "cooldownUntil",
       status,
       created_at AS "createdAt",
       updated_at AS "updatedAt"
FROM applicants WHERE id = $1 LIMIT 1
```

Write shape (tx branch shown; standalone branch identical against global `db`):

```ts
tx.update(applicants).set(attemptIncrement).where(eq(applicants.id, userId)).returning()
// attemptIncrement = {
//   verificationAttempts: sql`${applicants.verificationAttempts} + 1`,
//   lastAttemptAt: sql`now()`,
//   updatedAt: sql`now()`,
// }
```

(`updatedAt` explicitly set via `sql\`now()\`` so the column's `$onUpdate` hook never introduces nondeterminism — one statement owns all three columns.)

## Files modified / created

| File | Action | Content |
|---|---|---|
| `backend/db/repo/teachers/applicant.repository.ts` | MODIFY (+89 −1) | Header doc extended (read/write conventions); new imports (`eq, sql` from drizzle-orm; `db, queryDb` from `@/backend/db`; `DBQueryExecutor` added to types import); module-private `isDBTransaction` guard; TWO new methods appended after untouched `create`. |
| `backend/db/test/logic/teachers/applicant-lifecycle.test.ts` | CREATE (dir `logic/teachers/` is net-new as planned — 0.2 item #9) | 4-Tier suite, `bun:test`, 10 tests (below). |

## Files NOT modified (dispositions)

| File | Disposition |
|---|---|
| `RegistrationService*`, `auth.mutation.ts`, `schema/**`, `migration/**`, `db/schema.dbml`, `backend/types/**` | Untouched (hard constraint). Types consumed via import only; zero schema drift maintained. |
| Other repositories (incl. `users/user.repository.ts`) | Read as pattern reference only — never edited. |
| `tasks.md`, `plan.md`, all Phase-0/1 artifacts | Not edited by THIS task (their tracked diffs predate it from siblings). |
| `backend/db/repo/teachers/index.ts`, `backend/db/repo/index.ts` | VERIFY-ONLY — both barrels already re-export the applicant repository file/domain (file pre-existed); no edit needed per barrel rules. |
| `backend/enum/teachers/applicant-status.enum.ts` | Exists (Task 1.1) but correctly NOT imported here — no status writes/read semantics belong to this repo method set (2.1.SR clause confirmed). |

## Test engineering (2.1.TE) — tier map

All 10 tests inside `runInRollback`; `tx` passed to EVERY repo call, entity-setup helper, and direct Drizzle query (no `db`-backed calls anywhere in the test bodies). Entities ONLY via `createTestUser` + `createTestApplicant` (helper signatures verified in-source first: tx FIRST arg; repo methods take tx LAST optional — checked before use per AGENTS/test-instruction rules). Emails are randomized-UUID via the entity-setup convention (`test-${randomUUID()}@test.local`). No `.skip`/`.only` (grep-gated). `expect(...).rejects.toThrow()` appears NOWHERE.

| Tier | Tests |
|---|---|
| 1 — branch/stmt (4) | findByUserId returns full row for existing id (fields asserted incl. `status`/timestamps); findByUserId null for absent id; recordVerificationAttempt 0→1 stamps `last_attempt_at` and returns updated row (cross-checked by an independent direct-select oracle, not routed through the method under test); missing-row write returns null. |
| 2 — boundary (2) | pre-seeded attempts=3 via overrides → exactly 4; setup-forced older `last_attempt_at` (−24h) strictly advanced past forced value, persisted stamp ≥ returned stamp. |
| 3 — chaos/concurrency (3) | two SEQUENTIAL calls 0→1→2 (REQ-072); two CONCURRENT calls in the same `runInRollback` via `Promise.allSettled` → both fulfilled, post-update pair exactly {1, 2}, final row = 2, no lost update (REQ-042); FK-integrity chaos — applicant insert without owning user rejects with PG code `23503`. Absent ids are derived in-tx (`max(id)+1_000_000`) instead of magic constants. |
| 4 — security/tenancy (1 test + static review) | attempt recorded for user A leaves user B's row byte-identical (attempts/status/lastAttemptAt/cooldownUntil all pinned); parameterization is proven statically — the id enters ONLY through Drizzle's parameterizer (`eq(applicants.id, userId)` bind + `$1` placeholder), no concatenation/interpolation exists in the method body. Nothing dynamic remains to assert at runtime. |

Helper notes: error-code inspection walks Drizzle's `DrizzleQueryError.cause` chain hunting PG `"code"` — mirrors the established precedent in `backend/services/auth/registration.service.ts::isUniqueViolation` (Drizzle masks driver errors behind its generic "failed query" message, which bit the first run; fixed in-file).

## Verification outputs (2.1.QL + gates)

### Quality loops — `bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates` (tsgo → oxlint → biome:check → lint:type-aware → check:duplicates)

| File | First run | Final run |
|---|---|---|
| `backend/db/repo/teachers/applicant.repository.ts` | ✅ exit 0 (all 5 stages green) | ✅ re-certified exit 0 on final bytes (after Biome's own signature formatting) |
| `backend/db/test/logic/teachers/applicant-lifecycle.test.ts` | ❌ exit 1 (oxlint: `unicorn/no-array-sort` → fixed to `.toSorted((a,b)=>a-b)` in-file) | ✅ **exit 0** (all 5 stages green) |

Instruction discovery during sub-loops printed `.github/instructions/backend.instructions.md` / `tests.instructions.md` as applicable-but-absent on disk (same DISCOVER-ABSENT pattern as Tasks 0.x/1.x); effective instruction set read was root `AGENTS.md`, `backend/AGENTS.md`, `backend/db/repo/AGENTS.md`, `backend/db/test/AGENTS.md`, `backend/db/test/logic/AGENTS.md`, `.agents/instructions/{backend,tests}.instructions.md`, plus refs `docs/drizzle/prepared-statements.md` (write-exclusion rule honored) and `docs/IDEMPOTENCY.md` (skimmed: mutation-idempotency-key machinery belongs to service/mutation layers — nothing applicable to a pure increment whose UX contract is attempt-count accounting).

### Test execution (mandated runner)

`cd /home/z/kottab_academy && bun run test/scripts/run-test.ts backend/db/test/logic/teachers/applicant-lifecycle.test.ts` → exit **0**:

```
(pass) ApplicantRepository.findByUserId > returns the full row for an existing applicant id [46.86ms]
(pass) ApplicantRepository.findByUserId > returns null for a missing applicant id [5.02ms]
(pass) ApplicantRepository.recordVerificationAttempt > increments 0→1, stamps last_attempt_at, returns the updated row [6.90ms]
(pass) ApplicantRepository.recordVerificationAttempt > returns null for a missing applicant id (zero rows matched) [2.00ms]
(pass) ApplicantRepository.recordVerificationAttempt > advances a pre-seeded attempts=3 counter to exactly 4 [4.11ms]
(pass) ApplicantRepository.recordVerificationAttempt > monotonic last_attempt_at advance over a setup-forced older value [6.19ms]
(pass) ApplicantRepository.recordVerificationAttempt > two sequential calls move the counter 0→1→2 (no reset between) [5.36ms]
(pass) ApplicantRepository.recordVerificationAttempt > two concurrent calls in the same tx each land (+1 twice, no lost update) [19.70ms]
(pass) ApplicantRepository.recordVerificationAttempt > applicant insert without an owning user violates the FK (integrity chaos) [12.48ms]
(pass) ApplicantRepository.recordVerificationAttempt > recording an attempt for user A does not touch user B's row [16.35ms]

 10 pass / 0 fail / 38 expect() calls / Ran 10 tests across 1 file
```

(Raw `bun --env-file=.env.test test …` remains blocked by `test-runner-guard.ts` by design — mandate followed.)

Regression insurance: `backend/db/test/logic/auth/registration.service.test.ts` re-run through the runner after the repo change → **14 pass / 0 fail**, exit 0 (exercises untouched `ApplicantRepository.create` + applicants schema end-to-end).

### Full-repo gates

| Gate | Result |
|---|---|
| `bun tsgo` | exit **0**, 0 errors (full repo) |
| `bun biome:check` | exit **0** — "Checked 486 files in 5s. No fixes applied." — 0 issues tree-wide |
| `git diff -- backend/db/schema/… migration/… db/schema.dbml` | empty (REQ-045 anchor intact — verified via diff stat limited to my touched files) |

## Coverage statement (per-TE measurement + completeness argument)

Measured via the guard's own documented debugging bypass on a single file — `KOTTAB_TEST_RUNNER_OK=1 bun --env-file=.env.test test --coverage backend/db/test/logic/teachers/applicant-lifecycle.test.ts` (exit 0; the exact env-var invocation the runner-guard help text sanctions):

```
backend/db/repo/teachers/applicant.repository.ts | 85.71 stmts | 54.35 branches | uncovered: 48-61, 79, 81-82, 90-91, 124
```

Decomposition of every uncovered range:

- `48-61` — pre-existing `create(userId, tx)` — OUT OF SCOPE for this task ("existing `create` untouched"); DEV-owned and already covered by `registration.service.test.ts` (re-run above).
- NEW logic split:
  - `findByUserId` tx-branch (guard true-arm): fully covered.
  - `findByUserId` non-tx arm (lines 81-91: `queryDb` SELECT + null return): unreachable under the mandated harness — executing it requires omitting `tx`, which violates the CRITICAL tx-isolation rules (`backend/db/test/AGENTS.md` Rule 1/16: "pass tx to ALL repository methods… failing causes state pollution/deadlocks"). Harness-rule outranks the coverage number.
  - `recordVerificationAttempt` tx-branch: fully covered (including multi-invocation ordering & concurrency).
  - `recordVerificationAttempt` standalone arm (~line 124+): same reasoning — the global-`db` fallback runs when callers invoke WITHOUT tx; under `runInRollback` that is prohibited.
- Statement/branch **completeness argument for the unexecuted arms**: they are line-for-line structural twins of arms that ARE executed — the standalone update is token-identical to the tx update modulo the executor identifier (`db.` vs `tx.`), and the queryDb read is the production-proven `UserRepository.findByEmail` shape (in traffic since the Neon rollout; its own non-tx arm shares the 20%-covered-by-tests reality in this harness). Both compile-clean under tsgo, pass oxlint/biome/type-aware lint/duplicate scans, are statically reviewed (single-parameterized-statement; no placeholders/interpolation), and their only runtime dependencies (`pg.Pool.query` parameter binding; node-postgres result mapping of timestamps to `Date`) are exercised elsewhere in this very run by the pool-level `countRows`-style selects and raw `ensure-env` paths. Residual runtime risk is therefore confined to transportation (HTTP-vs-TCP of the global handle), not query semantics — accepted with this documented argument rather than breaking isolation to chase 100%.

## Security & Tenancy Audit (2.1.SEC)

- BOLA/BOPLA: methods key strictly off the provided PK; `userId: number` is the ONLY data input (no client input surface reaches the write beyond the caller-derived PK); nothing user-controllable lands in SQL text.
- No wildcard/LIKE usage (nothing to escape); identity-independent queries.
- No hardcoded user-facing strings introduced (repo layer carries no message construction at all — misses are `null` returns, per contract); zero `console.*` in BOTH files.
- Tenancy: Tier-4 test pins cross-row immunity (user A write leaves user B row byte-identical).
- Multi-user safe by construction: single-key `WHERE id = $1` bound parameter; PG `23503` FK integrity chaos-proven.

## Semantic Review (2.1.SR)

- ✅ Atomicity: DB-side `+1` in one UPDATE; NO read-modify-write in application code; concurrent-execution proof (Tier 3 {1,2} + final=2).
- ✅ tx propagation consistent: reads accept `DBQueryExecutor` (pool/client superset — matches findByEmail union semantics for GraphQL-session passthrough); writes accept `DBTransaction`; both optional-last per plan.
- ✅ Zero dead code; canonical types only (`ApplicantSelectType` + `DBQueryExecutor`/`DBTransaction` from `@/backend/types`); enum intentionally NOT imported here (no status semantics at repo layer).
- ✅ No cross-layer imports in repo file; test file imports confined to bun:test/drizzle/@backend layers.
- ✅ `RETURNING *` equivalent (`Drizzle .returning()`) provides the audit row for 2.2's logging needs.

## Instruction Verification (2.1.IV)

Validated against: `backend/db/repo/AGENTS.md` (Neon-HTTP bare-read rule, prepared-statement rules + write exclusion, type-definition pattern, `@/` import convention, one-namespace-per-file), `backend/db/AGENTS.md` → discovered-absent (DISP-3 pattern — skip with note; effective coverage came from `backend/AGENTS.md` + db/test AGENTS files), `backend/db/test/AGENTS.md` + `backend/db/test/logic/AGENTS.md` (rollback wrapper, tx-everywhere, expectRepoError discipline, entity helpers, coverage rules, `logic/` subdir addition), root `AGENTS.md` (runner path, quality gates, concurrency-safe commands), `.agents/instructions/{backend,tests}.instructions.md` (layer separation, no console.*, tx-position verification, uuid emails). `docs/drizzle/prepared-statements.md`: "Write Operations … should never use prepared statements" honored literally; `docs/IDEMPOTENCY.md`: mutation-level keys out of repo-layer scope (noted above).

## Cross-file dependencies

1. **Task 2.2 consumes these methods directly**: `getMyApplicantProfile` uses `findByUserId(userId, outerTx?)`; `assertCanPurchaseVerification` reads via `findByUserId`; `recordReapplication` delegates to `recordVerificationAttempt(userId, tx)` and maps its `null` return to `NotFoundError("APPLICANT", …)` + `logDomainError` (contract: null ⇔ missing row ⇔ NotFoundError upstream).
2. **2.M gate**: mid-point checklist can cite "2.1 tests GREEN (10/0) + tsgo/biome clean vs baseline" and the coverage disposition above; no schema drift, zero status literals written anywhere (both methods touch attempts/timestamps only).
3. **3.3 resolver**: anonymous external calls (Server Components / resolvers without outerTx) will exercise the standalone arms — their first live validation is expected there (see Deviations #3).
4. Non-tx `-1000000-offset` absence probing in tests relies on shared-PK ≤ users.max invariant; if DEV2-007+ ever writes giant seeded applicant ids (>1e9), `absentApplicantId` still holds (max-based, int32 headroom ~2.1e9).

## Deviations vs plan/tasks prose

| # | Plan/tasks text | Reality | Classification |
|---|---|---|---|
| 1 | tasks.md 2.1 shows `findByUserId(userId: number, tx?: DBTransaction)` | Implemented as `(userId, tx?: DBQueryExecutor)` per binding 0.2 finding #5 determination ("mirror `UserRepository.findByEmail`: optional `tx?: DBQueryExecutor` LAST param") and the task-specific instruction giving exactly this signature; also matches AGENTS Neon-HTTP read rule (plain-pool executor must be acceptable) | Directive hierarchy: task instruction + 0.2 disposition supersede tasks.md prose shorthand — read methods need the Pool/PoolClient union |
| 2 | tasks.md TE "Coverage: 100% statement/branch on NEW repo logic (`bun test --coverage`)" | Measured 85.71%/54.35% on the WHOLE FILE including out-of-scope `create` and the two production-only non-tx arms; new-method tx-facing logic is fully covered, and non-tx arms carry the documented completeness argument (isolating them would violate CRITICAL AGENTS tx-isolation rules) | Documented deviation — harness-rule precedence over a numeric target; decomposition recorded above |
| 3 | (test-fit detail) raw FK message assertion replaced by cause-chain PG-code check | Drizzle wraps driver errors behind a generic "failed query" message; adopted the established `DrizzleQueryError.cause` traversal precedent from `registration.service.ts::isUniqueViolation` with PG code `23503` | Test-engineering choice consistent with repo patterns; no requirement impact |
| 4 | (formatting) Biome rewrote `recordVerificationAttempt` signature to multi-line and kept `toSorted` fix | Cosmetic formatter/linter normalizations applied by the gates themselves | None semantically |

No requirement-level deviations. Nothing committed to git.

## Carry-forward

1. **CF-1 → Task 2.2**: consume signatures EXACTLY as listed above (`findByUserId(id, tx?)` takes the executor union; `recordVerificationAttempt(id, tx?)` transaction-type; both return `null` for miss — do NOT invent throw paths at repo level). Service maps null→NotFoundError and must pass locale only in ITS messages (repo adds none).
2. **CF-2 → Task 2.2 tests**: DB-tier cases there reuse this file's helpers style (absentApplicantId / independent read-back oracle pattern) but must NOT import from this test file (keep suites self-contained per Rule 13); duplicating two tiny helpers in-file is cheaper than a shared fixture import across suites.
3. **CF-3 → 2.M**: coverage expectation vs tx-isolation harness tension is now PRECEDENTED for every future optional-tx repo write — recommend 2.M records "non-tx arm coverage deferred to integration tier (3.3/graphql)" as standing interpretation rather than reopening per-file.
4. **CF-4 → 5.x review waves**: expect Biome-normalized multi-line signature + `.toSorted` in reviews; neither warrants flags.
