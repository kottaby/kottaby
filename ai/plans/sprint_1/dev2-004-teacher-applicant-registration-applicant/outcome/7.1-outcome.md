# 7.1 Outcome — Canonical Doc: `docs/teachers/applicant-lifecycle.md`

> Task: tasks.md 7.1 (Phase 7 — Knowledge Propagation & Documentation)
> Requirements: REQ-013, REQ-080, REQ-081 (implementing the REQ-080/081/082 documentation gates)
> Recorded: 2026-08-27
> Do-not-commit honored: nothing staged/committed; Task 7.3 (synthesis) NOT implemented per orchestrator instruction.

## Summary

Created `docs/teachers/` (net-new docs subdir per domain-mapping conventions, pre-registered by 0.2 Adaptation #5 and plan-review-R1 finding 18) with `docs/teachers/applicant-lifecycle.md` — the single canonical reference for the teacher-applicant lifecycle. The doc contains EXACTLY the seven mandated sections plus the error-contract section, follows the doc style/tone of `docs/auth/user-registration.md` (header metadata block, numbered `##` sections, tables, `>` note blockquotes, closing References), marks both code snippets ILLUSTRATIVE/NON-AUTHORITATIVE, and carries a traceable anchor (REQ or outcome file) for every claim. NO edits to `docs/specs/state-machine-invariants.md` (diff verified empty — §Verification below).

## Files created

| File | Content |
|---|---|
| `docs/teachers/applicant-lifecycle.md` | Canonical reference (9 sections — the 7 mandated + error contract + references) |

### Section list (per tasks.md 7.1's explicit content list)

1. **Applicant State Machine (REQ-013)** — transition table (`pending` start row from DEV1-002 registration; `pending → in_evaluation` = DEV2-005 purchase; `in_evaluation → passed | failed` = DEV2-007 aggregation; `failed → in_evaluation` = DEV2-005 re-purchase after full cooldown via the DEV2-004 guard; `passed → teacher row exists` = DEV2-007 write); Sprint-1 DEV2-005..DEV2-010 binding quoted verbatim from REQ-013; value vocabulary anchored to the DBML note + `ApplicantStatus` TS enum + `isApplicantStatus` fail-closed guard.
2. **Cooldown & Attempt Contracts (REQ-014/015/016)** — authoritative read source `applicants.cooldown_until` ONLY; `cooldownActive ⇔ cooldown_until IS NOT NULL AND cooldown_until > now()` (strict `>`, exactly-now permitted per REQ-072/outcome/2.2); atomic single-statement increment with the Drizzle shape reference to `backend/db/repo/teachers/applicant.repository.ts` (snippet marked NON-AUTHORITATIVE; no read-modify-write; `.returning()` audit row; null-miss contract); two-source split table (login/session gating = `users.suspended` per INV-U2, DEV2-001/002 domain; re-purchase gating = `applicants.cooldown_until` per INV-TV3; DEV2-004 writes neither); duration-agnostic guard note (INV-TV4 30d/90d = DEV2-008 write-time concern, binding anti-re-litigation quote from specs §3); INV-TV6 co-existence note (DEV2-009 students record co-exists; applicants row untouched).
3. **Query Contract & Precedence (REQ-017 / plan D5)** — zero-args `myApplicantProfile`; `authScopes { $all: { authenticated: true, role: [UserRole.Teacher] } }` with the full ANY-vs-`$all` engine rationale from outcome/3.3 (role-only ⇒ anonymous gets 403; plain map ⇒ ANY-semantics grants non-teachers; `$all` ⇒ anonymous 401 / non-teacher 403, pinned by outcome/5.3 + outcome/6.4 P2); `null` = certified/never-applied single answer (REQ-035 + D5 disposition: null chosen over throwing); governed accounts never reach the resolver (DEV2-002 fail-closed context); deeper DomainErrors propagate uncaught.
4. **Advisory-Isolation Note (REQ-043)** — single read + pure compute against one captured `now`; guard is advisory-at-its-isolation-level; racing purchase writes are DEV2-005's transactional responsibility; TOCTOU closes only when DEV2-005 passes its transaction's `tx` (outcome/5.5 forward contract; outcome/6.2 residual risk); REQ-043 anchor quoted.
5. **"Registration already ships in DEV1-002" grounding note (REQ-002/010)** — path verified, never rebuilt; cites the REQ-071 lock suite at `backend/db/test/logic/auth/registration.service.test.ts` → describe `"DEV2-004 registration contract locks"` (Task 5.1; 18/18; outcome/5.1) and REQ-010's "SHALL NOT modify the registration write path" clause.
6. **Consumer guidance table** — DEV2-005 (`assertCanPurchaseVerification` before purchase + `recordReapplication` on successful re-purchase; **both accept optional `tx` and MUST receive the purchase transaction's tx**); DEV2-006/007 (status transition writes + `teacher` row are THEIR tickets' writes); DEV2-008 (cooldown writer; durations INV-TV4; keep the strict-`>` reader honest); DEV2-009 (failed→student conversion co-existence, INV-TV6); DEV2-010 (override surface reads `audit_logs`, A.5; INV-TV5 unaffected); DEV3-019 (direct onboarding = INV-TV1 arm (b) boundary — outside the purchase/evaluation lifecycle).
7. **Invariant Anchoring (REQ-081)** — INV-TV1..TV7 table quoting each invariant's wording as it exists in `docs/specs/state-machine-invariants.md` paired with the per-invariant service note from specs §3; B.6/B.7 quoted from `docs/specs/open-decisions-and-gaps.md` (accurate binding: B.6/B.7 live in open-decisions-and-gaps.md, NOT in the invariant file — the doc states this explicitly); Workflow 01 stage mapping (Registered → Pending_Evaluation → In_Evaluation → Evaluation_Complete → Qualified/Cooldown_* ↔ REQ-013 vocabulary); a vocabulary-reconciliation blockquote (draft-era `students.suspended` representation vs resolved B.6 `applicants.cooldown_until`, reconciled by the REQ-016 two-source split — recorded in-doc, invariant file untouched); **change/addendum section stating explicitly: "No invariant gaps were discovered during DEV2-004; no addendum to `docs/specs/state-machine-invariants.md` is required."**
8. **Error Contract** — `APPLICANT_NOT_FOUND` / `APPLICANT_COOLDOWN_ACTIVE` / `APPLICANT_STATUS_CORRUPT` ↔ `errorsTranslations.applicantNotFound` / `applicantCooldownActive` / `applicantStatusCorrupt` (bijective camelCase mapping), producer forms per `docs/graphql/domain-error-extensions-code.md` (Rule 2/3/4), ar/en copy, `{cooldownUntil}` single-placeholder expansion contract, and the logging discipline with the CORRECT count: **3 `logger.logDomainError` sites / 5 enumerated rejections, 2 silent by design** (per outcome/6.2 Note-2's prescribed citation — this discharges the DI-7 prose correction into repo knowledge; see 7.2 outcome for the DI-7 location audit).
9. **References** — specs, invariant file (read-only), open-decisions, Workflow 01, error-contract doc, registration doc, authoritative implementation paths, test locks, outcome evidence.

## Files NOT modified (dispositions)

| File | Disposition |
|---|---|
| `docs/specs/state-machine-invariants.md` | **UNTOUCHED — REQ-081.** Diff verified empty (see Verification). INV-TV1..TV7 + INV-U2 are quoted, never edited; the vocabulary reconciliation is recorded inside the new doc instead. |
| `docs/specs/open-decisions-and-gaps.md`, `docs/workflows/01-teacher-verification-workflow.md` | Quoted as sources; not edited. |
| `tasks.md`, `plan.md`, all prior outcome files | Historical records; not edited (incl. outcome/2.2's "five logDomainError sites" prose — DI-7 disposition recorded in 7.2-outcome, not patched in place). |
| All code files | Doc-only task — zero source changes (REQ-082 "no code, no implementation detail" honored). |

## Verification outputs

### Quality loop (7.1.QL) — sub-loop on the new doc

`bun run scripts/health/sub-loop.ts docs/teachers/applicant-lifecycle.md --lifecycle duplicates`:

- tsgo stage: ✅ passed ("no errors for docs/teachers/applicant-lifecycle.md").
- oxlint stage: ❌ tool exit 1 with `No files found to lint … Finished in 69ms on 0 files with 301 rules` — **NON-APPLICABLE for `.md`**: oxlint's configured scope contains zero markdown files; the exit-1 is the tool's hard-stop on an empty file set, not a finding. Proven file-independent: the identical failure occurs on untouched `.md` files (e.g. `backend/enum/AGENTS.md` control run, and every file listed in the 7.2 quality table below). Subsequent sub-loop stages (biome/lint/duplicates) are unreachable after the hard stop.
- Direct `bunx @biomejs/biome check docs/teachers/applicant-lifecycle.md`: **NON-APPLICABLE** — biome.json ignores markdown ("These paths were provided but ignored", exit 1, "Checked 0 files").
- Direct `bun x eslint docs/teachers/applicant-lifecycle.md`: config nominally scopes `**/*.md` (eslint.config.mjs:56), but the eslint toolchain cannot boot in this sandbox ("typescript-eslint does not support TS 7.0") — **proven file-independent** by running the same command against pre-existing `docs/auth/user-registration.md` (identical toolchain error; zero file-level findings possible either way). Same anomaly class as phase0's documented lint-service quirk (baseline: `scripts/lint-service.ts` exit 1 with empty output on the pristine tree; re-confirmed live during this task: `bun run lint` → exit 1, no output).
- Compensating full-repo gates (below) green; the doc contains no code fences in executable languages beyond the two marked-illustrative TS snippets, no links to non-existent paths (all 7 target paths existence-checked — see 7.2 verification).

### Full-repo gates

| Gate | Result |
|---|---|
| `bun tsgo` | exit **0**, 0 errors (matches phase-0 baseline; zero NEW issues) |
| `bun biome:check` | exit **0** — "Checked 500 files in 9s. No fixes applied." — 0 issues tree-wide (count unchanged from 5.5: markdown is outside biome's scope) |

### REQ-081 invariant-file diff check (quoted verbatim)

```
$ git diff -- docs/specs/state-machine-invariants.md
(empty — zero output)

$ git status --porcelain -- docs/specs/
(no lines — nothing under docs/specs/ modified or untracked)

$ git status --porcelain | grep -c "state-machine-invariants"
0   (grep exit 1 = zero matches)
```

## Semantic Review (7.1.SR)

- ✅ **Canonical**: the doc introduces no new contracts — every normative statement is a quote or a faithful restatement of a REQ (REQ-002/010/012/013/014/015/016/017/030/031/033/035/041/042/043/050/071/072/073/075/080/081/082), a recorded outcome disposition, or an existing doc (`state-machine-invariants.md`, `open-decisions-and-gaps.md`, `domain-error-extensions-code.md`, Workflow 01).
- ✅ **No code beyond illustrative snippets**: exactly two TS snippets, both labeled `ILLUSTRATIVE — NON-AUTHORITATIVE` with the authoritative file path cited adjacent; no executable claim depends on them.
- ✅ **No contradiction with `docs/specs/state-machine-invariants.md`**: INV-TV wording quoted as-is; the draft-era `students.suspended` cooldown representation vs resolved B.6 `applicants.cooldown_until` tension is reconciled explicitly via the REQ-016 two-source split (the reconciliation REQ-016 itself mandates), recorded in-doc — the invariant file requires zero edits for this doc to hold (diff-empty proof above).
- ✅ **Every claim traceable**: sections carry inline REQ anchors + outcome-file citations (2.1/2.2/3.3/5.1/5.3/5.4/5.5/6.2/6.4); the References section enumerates them.
- ✅ Scope discipline: `docs/teachers/` was pre-registered as net-new (0.2 Adaptation #5; plan-review-R1 finding 18); no other docs directory was touched; REQ-080's location conditional ("`docs/auth/` … or `docs/teachers/` if introduced") resolves to `docs/teachers/` exactly as tasks.md 7.1 mandates.

## Carry-forward

- DEV2-005 implementers: read §4 + §6 BEFORE wiring the purchase flow — the tx-passing obligation is the difference between an advisory-only check and a TOCTOU-closed one.
- DEV2-008 implementers: §2.4 is the binding anti-re-litigation record for duration setting.
- If a downstream ticket discovers an invariant gap, it records the addendum in ITS OWN doc change section (§7.4 rule); `docs/specs/state-machine-invariants.md` numbering stays immutable.
- Outcome/2.2's historical "five logDomainError sites" prose is superseded by this doc's §8 count (3 sites / 5 enumerated rejections / 2 silent by design); location audit in 7.2-outcome.
