# 6.4 Outcome — Review Wave: pentester (plan §6 threat-table replay, wire-level)

> Task: tasks.md 6.4 (Phase 6 — Review Waves)
> Method: PASSIVE wire probes via `curl` against the running dev server `:3000/api/graphql` (server left up; no source modified; nothing committed). Identies minted ONLY through public surfaces (`registerUser` → `login`) with randomized `pentest-*-6.4-<ts>@test.local` emails — the same convention the integration suites use; no DB writes performed directly by this wave.
> Recorded deviations honored as context (NOT findings): `$all` authScopes rationale (outcome/3.3), parse-vs-gql in test files, TEST_SERVER_EXTERNAL guard, DI-1..3, DEV3-002 masking behaviors, gateway-owned depth limits (DEV3-003).

## Threat-table verdicts (plan §6 — every row)

| # | Threat class | Verdict | Wire + static evidence |
|---|---|---|---|
| 1 | **BOLA / IDOR** | ✅ **PASS** | Zero-arg artifact live: forged inline arg → `GRAPHQL_VALIDATION_FAILED`; variable-carrying op → `GRAPHQL_VALIDATION_FAILED` (no arg surface — resolver never reachable with input). Aliased duplicate (teacher token) returns only the CALLER's own id twice. Two distinct teachers each see exactly their own id (669 vs 670). Identity is `ctx.user.id` only (`$all: { authenticated, role:[Teacher] }`). |
| 2 | **BOPLA (mass assignment)** | ✅ **PASS** | `mutation { myApplicantProfile { id } }` → `GRAPHQL_VALIDATION_FAILED` ("Cannot query field … on type Mutation"). Introspection: the ENTIRE mutation surface is `login, logout, refreshToken, registerUser` — zero applicant write fields exist to mass-assign. Closed-shape probe: `email`, `suspended`, `isDeleted` on `ApplicantProfile` → `GRAPHQL_VALIDATION_FAILED` (seven-field closed projection). Static: `git diff HEAD` grep for `...input` spread → **zero**. |
| 3 | **BFLA (function-level authorization)** | ✅ **PASS** | Full role matrix replayed over the wire (below): anonymous → `UNAUTHORIZED`; student → `FORBIDDEN`; parent → `FORBIDDEN`; teacher-applicant → granted (own profile only). Admin structurally excluded from registration: `role:"admin"` → `BAD_USER_INPUT` ("Value \"admin\" does not exist in \"RegisterPublicRole\" enum" — SDL enum = `Parent \| Student \| Teacher`, REQ-032/D8). Supervisor: not a persisted role (`user_role` pgEnum = `admin,teacher,student,parent`, C.2) — no supervisor token is mintable; the role gate covers it structurally. Forged/garbage Bearer → `UNAUTHORIZED` (401 semantics preserved, no 500). |
| 4 | **Error oracles / disclosure** | ✅ **PASS** | Student vs parent deny responses are **byte-identical** in message ("You do not have permission to perform this action."), code (`FORBIDDEN`), and data shape — no oracle distinguishing role/account state; no id/email/role tokens leak into any deny. Anonymous deny is the generic "Authentication required." + `UNAUTHORIZED`. Single-null answer (certified vs never-applied) is the documented REQ-035 collapse. |
| 5 | **SQL / LIKE injection** | ✅ **PASS** | No user-controllable input reaches any query in this surface (the field takes NO arguments — structurally no injection surface). Injection-style probe (`updateApplicantStatus(input:{status:"passed"})`) dies at validation. Repo write path is the parameterized Drizzle increment verified in 2.1 (static, no concatenation, no inline `--`). |
| 6 | **Soft-delete / governance integrity** | ✅ **PASS (static)** | `git diff HEAD` grep for `suspended` additions → only `suspended: false/…: null` fixture initializations inside `backend/db/test/logic/auth/registration.service.test.ts` (5.1 test harness, mirrors the integration-suite fixture style); ZERO production writes/reads of governance fields in the ticket diff. Cooldown math reads `applicants.cooldown_until` exclusively (REQ-015/016, outcome/2.2). |
| 7 | **Timing/oracle on cooldown boundary** | ✅ **PASS (structural)** | The read surface accepts no input and performs pure compute against one captured `now` (outcome/2.2, 100% branch coverage); no write-at-read exists to race. No timing-differentiating response behavior observable at the wire (uniform 7-field payload / uniform denies). |
| 8 | **Token / secret hygiene** | ✅ **PASS** | All error envelopes carry only `{ message, path, locations, extensions:{code,requestId} }` — no token, hash, stack, or SQL fragments. Login responses surface only `accessToken` (production contract). No credential material echoed in any probe response. |

## Probe evidence (curl commands + trimmed responses)

### P1 — BOLA replay (no-arg surface)

```
# P1a forged inline argument
$ curl -s -X POST :3000/api/graphql -d '{"query":"query Forge { myApplicantProfile(userId: 999999) { id } }"}'
{"errors":[{"message":"Unknown argument \"userId\" on field \"Query.myApplicantProfile\".",
 "extensions":{"code":"GRAPHQL_VALIDATION_FAILED","requestId":"28959853-…"}}]}

# P1b variable-carrying operation
$ curl -s -X POST :3000/api/graphql -d '{"query":"query Vars($uid: Int!) { myApplicantProfile(userId: $uid) { id } }","variables":{"uid":424242}}'
{"errors":[{"message":"Unknown argument \"userId\" on field \"Query.myApplicantProfile\".",
 "extensions":{"code":"GRAPHQL_VALIDATION_FAILED","requestId":"40ab0ac8-…"}}]}

# P1c aliased duplicate — anonymous: per-path scope denial, no leak
$ curl -s … -d '{"query":"query AliasDup { a: myApplicantProfile { id } b: myApplicantProfile { id } }"}'
{"errors":[{"message":"Authentication required.","path":["a"],…,"extensions":{"code":"UNAUTHORIZED",…}},
           {"message":"Authentication required.","path":["b"],…,"extensions":{"code":"UNAUTHORIZED",…}}],"data":{"a":null,"b":null}}

# P1c-redo aliased duplicate — teacher A token: only caller's own id, twice
{"data":{"a":{"id":669},"b":{"id":669}}}
```

All three replay classes die at validation or answer with caller-scoped data only — matching outcome/3.3's Tier-4 pins (`GRAPHQL_VALIDATION_FAILED`), now confirmed on the live :3000 server.

### P2 — Authz matrix replay over the wire (registerUser → login → Bearer)

```
# P2a anonymous
{"errors":[{"message":"Authentication required.","path":["myApplicantProfile"],"extensions":{"code":"UNAUTHORIZED",…}}],"data":{"myApplicantProfile":null}}

# P2b admin registration attempt (BFLA structural exclusion)
$ … "role":"admin" …
{"errors":[{"message":"Variable \"$input\" has invalid value at .role: Value \"admin\" does not exist in \"RegisterPublicRole\" enum.",
 "extensions":{"code":"BAD_USER_INPUT",…}}]}
# SDL: enum RegisterPublicRole { Parent Student Teacher }   ← admin/supervisor absent (schema.graphql:107-111)

# P2c student (id 667) and P2d parent (id 668) with Bearer tokens
{"errors":[{"message":"You do not have permission to perform this action.","extensions":{"code":"FORBIDDEN",…}}],"data":{"myApplicantProfile":null}}   × 2

# P2e teacher-applicant A (id 669) — full seven-field payload, OWN id only
{"data":{"myApplicantProfile":{"id":669,"status":"Pending","verificationAttempts":0,"lastAttemptAt":null,
 "cooldownUntil":null,"cooldownActive":false,"canPurchaseVerification":true}}}

# P2f teacher-applicant B (id 670) — own id only
{"data":{"myApplicantProfile":{"id":670,"status":"Pending"}}}

# P2g forged token
$ curl … -H "Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.forged.sig" …
{"errors":[{"message":"Authentication required.","extensions":{"code":"UNAUTHORIZED",…}}]}
```

Registrations performed via the public mutation only: student=667, parent=668, teacherA=669, teacherB=670 (emails `pentest-*-6.4-<ts>@test.local`, consistent with the suites' accumulate-on-test-DB convention).

### P3 — Error-oracle uniformity

```
student message : "You do not have permission to perform this action."
parent  message : "You do not have permission to perform this action."
BYTE-IDENTICAL: True   code equal (FORBIDDEN): True   data shape equal: True
no role/id/email leak in either body: True
```

Uniform deny ⇒ no oracle (REQ-035).

### P4 — Query depth / complexity (20-alias batch a1..a20)

```
$ curl … (teacher A token) -d '{"query":"query Batch20 { a1: myApplicantProfile { id status } … a20: … }"}'
{"data":{"a1":{"id":669,…},"a2":{"id":669,…}, …,"a20":{"id":669,…}}}          ← ACCEPTED (200)
anonymous variant: 20 × {"message":"Authentication required.","extensions":{"code":"UNAUTHORIZED"}}
```

**Observation (INFO — out of ticket scope):** no query depth/complexity limit is enforced at the application layer; a 20-alias batch executes. Impact here is bounded — the field is a zero-arg single-row read and every alias returns the caller's OWN row (no amplification, no 500s, no leaks), but depth limiting belongs to the DEV3-003 gateway and should be re-verified there. Flagged as INFO per instruction, NOT a DEV2-004 finding.

### P5 — BOPLA wire check + P6 — Injection/no-write-path

```
$ curl … (teacher A) -d '{"query":"mutation M { myApplicantProfile { id } }"}'
{"errors":[{"message":"Cannot query field \"myApplicantProfile\" on type \"Mutation\".","extensions":{"code":"GRAPHQL_VALIDATION_FAILED",…}}]}

$ curl … -d '{"query":"mutation X($input: UpdateApplicantStatusInput!) { updateApplicantStatus(input: $input) { id } }","variables":{"input":{"status":"passed"}}}'
{"errors":[{"message":"Unknown type \"UpdateApplicantStatusInput\"…"},{"message":"Cannot query field \"updateApplicantStatus\" on type \"Mutation\".","extensions":{"code":"GRAPHQL_VALIDATION_FAILED",…}}]}

$ introspection __schema.mutationType.fields → ["login","logout","refreshToken","registerUser"]   ← zero applicant write surfaces
```

Status/attempt/cooldown fields are server-computed with no client write path — confirmed live.

### P7 — Closed output shape (read-side BOPLA)

```
$ curl … (teacher A) -d '{"query":"query { myApplicantProfile { id email status suspended isDeleted } }"}'
{"errors":[
 {"message":"Cannot query field \"email\" on type \"ApplicantProfile\".","extensions":{"code":"GRAPHQL_VALIDATION_FAILED",…}},
 {"message":"Cannot query field \"suspended\" …"},{"message":"Cannot query field \"isDeleted\" …"}]}
```

Governance fields are not merely hidden — they do not exist on the GraphQL object at all.

## Tenant-integrity conclusion

Two independently registered teachers (669, 670) each receive only their own profile; the operation has NO parameters, so cross-tenant selection is structurally inexpressible on the wire (P1a/P1c die at validation). Combined with the repo/service-layer single-key `WHERE id = $1` parameterization (2.1 static review + Tier-4 cross-row immunity test) and the P1c aliased replay, tenant isolation holds at every layer probed.

## Informational / filtered items (NOT ticket findings)

1. **DI-1..DI-3** — ledgered resolved-reference entries (pre-existing `create` literal; runner import narrowing; heap cap). Verified present with ✅ status; not re-reported.
2. **Gateway depth/complexity limits** — absent at app layer; DEV3-003 scope (P4 above, INFO).
3. **DEV3-002 masking behaviors** — deny copy reflects the canonical masked surface; no re-verification required beyond P3 (uniformity confirmed).
4. Supervisor replay (named in tasks.md 6.4 scope) — structurally impossible: `supervisor` is not a member of `user_role` (C.2) and no registration path mints it; the `role` scope gate is the covering control (consistent with outcome/5.4).

## Findings

**Zero security findings.** Every plan §6 threat row: PASS (rows 6–7 PASS via static/structural evidence complementing the wire probes). One INFO observation recorded (depth limits → DEV3-003). Nothing to repair; no ledger entries required.

**WAVE 6.4 GATE: PASS.**
