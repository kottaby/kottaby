# 3.4 Outcome — Schema Regeneration & Codegen Commit

> Requirements: REQ-061, REQ-004
> Commands: `bun run generate:gqlSchema` (exit 0) → `bun codegen` (exit 0)

## SDL diff summary (frontend/graphql/generated/schema.graphql — the ONLY generated change so far)

```diff
+type ApplicantProfile {
+  canPurchaseVerification: Boolean!
+  cooldownActive: Boolean!
+  cooldownUntil: String
+  id: Int!
+  lastAttemptAt: String
+  status: ApplicantStatus!
+  verificationAttempts: Int!
+}
+
+enum ApplicantStatus {
+  Failed
+  InEvaluation
+  Passed
+  Pending
+}
 (existing types untouched)
 type Query {
   me: User
+  myApplicantProfile: ApplicantProfile
   ...
```

- **BOLA zero-arg proof artifact**: `myApplicantProfile: ApplicantProfile` — the SDL field carries NO parentheses/arguments (grep-verified in the diff; cited for the REQ-075 suite).
- Nullability matches plan §3.1 exactly (id Int! / status! / attempts Int! / booleans! / timestamps nullable). Timestamps surface as nullable ISO-8601 **String** per DISP-5 (HealthCheck exposeString precedent; canonical TS fields stay `Date | null`).
- Enum member naming: Pothos emits TS enum **member names** (`Pending, InEvaluation, Failed, Passed`) — the project-wide convention (identical to `UserRole { Admin, Teacher, Student, Parent }` in the shipped SDL). Plan §3.1's `PENDING IN_EVALUATION …` illustration is superseded by this convention; behavior pinned by the integration suite.

## Codegen state disposition (mechanical, recorded)

`codegen.ts documents` scans ONLY `frontend/graphql/sharedDocuments/**` + `frontend/views/**/*.documents.ts`. With no document yet referencing `myApplicantProfile`, `bun codegen` emits no new operation types (graphql.ts unchanged; verified `grep -c ApplicantStatus graphql.ts` = 0). Consequently:
- The `ApplicantStatus` codegen enum + `MyApplicantProfileQuery` types land with **Task 4.1's shared document** (the integration test anticipated this in a comment; REQ-061's "commit regenerated artifacts in the same change set" is fulfilled incrementally at 3.4 (SDL) + 4.1 (operation types), with the final state re-verified at 5.5).
- Generated files were NOT hand-edited (REQ-061 rule honored).

## Quality loop

- Sub-loop `--lifecycle duplicates` exit 0 on every file authored in 3.3 (query file, barrels, test file). Generated files excluded per health-tool conventions (generated artifacts are not hand-authored source; exclusion verified by running the loop only on authored files).
- `bun tsgo` → exit 0, 0 errors (full repo). `bun biome:check` → exit 0, 493 files, 0 issues.

## Files modified

- `frontend/graphql/generated/schema.graphql` (regenerated — committed in this change set per REQ-061)
- `outcome/3.4-outcome.md`
