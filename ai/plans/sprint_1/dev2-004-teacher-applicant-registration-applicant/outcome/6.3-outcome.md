# 6.3 Outcome — Review Wave: review-frontend

> Task: tasks.md 6.3 (Phase 6 — Review Waves)
> Scope: `frontend/views/teachers/dashboard/{ApplicantStatusCard.tsx,index.ts}` · `frontend/views/dashboard/{RoleDashboardPage.tsx,DashboardView.tsx}` · `frontend/graphql/sharedDocuments/teachers/*` · `frontend/graphql/sharedDocuments/index.ts` · `frontend/lib/i18n/format-date.ts` · `test/ui/components/teachers/ApplicantStatusCard.test.tsx` · `test/ui/components/translation-preload.ts` · `frontend/graphql/test/teachers/*`
> Method: read-only review (no source modified, nothing committed) — every check verified by grep/read against the working tree plus `git diff HEAD` scoping to separate NEW/modified lines from pre-existing code.
> Rule set applied: `frontend/AGENTS.md`, `frontend/graphql/AGENTS.md`, `.agents/instructions/frontend.instructions.md`, `docs/graphql/domain-error-extensions-code.md`, plus recorded dispositions (DISP-1..7, DISP-4 lens, DISP-5, DISP-6) and outcome/4.1/4.2/4.3 deviations (mocked-provider entrypoint, no-op CTA, branch 7b).

## Verdict table (per 6.3 check)

| # | Check | Verdict | Evidence |
|---|---|---|---|
| 1 | sx-only discipline (no direct style props) | **PASS** (1 LOW note) | Grep `\s(fontWeight\|mb\|mt\|mx\|my\|p\|px\|py\|textAlign\|display\|gap\|alignItems\|justifyContent\|flexWrap\|direction)=` over all scope components → the ONLY hit is `DashboardView.tsx:108 <Stack direction="row" …>` which is **pre-existing** (not in the ticket diff — verified via `git diff HEAD`; only hunks touch the props interface, the signature, and the statusSlot render). All NEW markup styles exclusively through `sx={{…}}` / `sx={theme => ({…}}` (17 `theme.palette` callbacks in the card). |
| 2 | Colors: theme.palette.* only | **PASS** | Grep `#[0-9a-fA-F]{3,8}\|rgba?\(\|hsla?\(` over all new frontend scope files → **zero hits**. No string-based palette access (`color="primary.main"` absent). Material-3 `on<Color>` siblings used (`onPrimaryContainer`, `onWarningContainer`, `onSuccessContainer`, `onInfoContainer`, `status.onPendingContainer`); `boxShadow` mapped to `theme.palette.shadow.card` (AGENTS NO-DELETION rule). |
| 3 | Icons: *Outlined only | **PASS** | Card imports 8/8 `*Outlined` (`CheckCircleOutlined`, `ErrorOutlined`, `RateReviewOutlined`, `LockOutlined`, `HourglassEmptyOutlined`, `HistoryEduOutlined`, `ScheduleOutlined`, `SchoolOutlined`). Zero `*Outline`/`Filled`/`TwoTone`/`Sharp`/`Rounded` variants anywhere in scope. |
| 4 | useAppTranslation handles + property access | **PASS** | `useAppTranslation(Applicant)` + `useAppTranslation(Errors)` (card), `useAppTranslation(Dashboard)` (DashboardView, pre-existing) — handles VALUE-imported from `@/shared/locale` per DISP-4; all label consumption is property access (`t.statusCardTitle`, `te.internalServerError`, …). Grep `t\(['"]` → single hit is the JSDoc prohibition note itself (line 78). Zero string-key calls. |
| 5 | useQuery only (no useLazyQuery) | **PASS** | Grep `useLazyQuery` in `frontend/views/teachers/` → zero; repo-wide source sweep (`frontend/ app/`, non-doc/test) → zero. `useQuery` imported from `@apollo/client/react` (card line 3) — stateful, per AGENTS. |
| 6 | RTL: logical properties | **PASS** | Grep `marginLeft\|marginRight\|paddingLeft\|paddingRight\|left:\|right:\|textAlign.*(left\|right)\|mr:\|ml:` over the card → **zero**. Layout composes via flex/grid + logical `alignItems: "flex-start"` (mirrors under RTL) and `flexWrap`; the top-edge accent uses `borderTopWidth/Style/Color` — direction-agnostic (identical LTR/RTL by design). 4.3 browser self-loop screenshots corroborate RTL mirroring. |
| 7 | Apollo: `id` in selection; generated types consumed | **PASS** | `applicant.documents.ts` selection = exactly the seven public fields **including `id`** (cache normalization, REQ-060); typed `TypedDocumentNode<MyApplicantProfileQuery>` with the variables param omitted (no-arg convention per instructions table). Consumers use codegen names only: `MyApplicantProfileQuery`, `MyApplicantProfileQuery_myApplicantProfile` (extracted-field compact convention), `ApplicantStatus` (runtime value-import for the switch). No ad-hoc/inlined type definitions. SDL artifact verified live: `myApplicantProfile: ApplicantProfile` — zero arguments (schema.graphql:76). |
| 8 | i18n parity consumed; no hardcoded user-facing strings | **PASS** | Zero Arabic codepoints in the card (grep `\p{Arabic}` → none). Quoted-string hits are exclusively technical: `"use client"`, error-code comparisons (`"UNAUTHORIZED"`/`"FORBIDDEN"` machine codes), internal tone unions (`"pending" \| "info" \| …` — never rendered), comments. Test suite asserts ONLY preloaded label objects (`ApplicantNs.getLabels(getTranslations(locale))` / `ErrorsNs…`) — REQ-074 honored; `formatApplicantDate` locale fallback (exact-`"en"` else `"ar"`) byte-mirrors the server `resolveLocaleTag` contract. |
| 9 | Composition: role-agnostic slot, teacher-only card, no duplicated guards | **PASS** | `DashboardView` gains `readonly statusSlot?: ReactNode` — pure slot, zero role logic, renders `{statusSlot ? <Box sx={{ mb: 4 }}>{statusSlot}</Box> : null}` (sx-only). `RoleDashboardPage` decides server-side: `statusSlot={role === UserRole.Teacher ? <ApplicantStatusCard /> : undefined}` — other roles' dashboards byte-identical (verified in diff: no other change). `withPageAuth({ roles: [role] })` remains the ONLY guard (unchanged, single boundary); the card carries NO client-side role/lifecycle derivation (flags come from the payload). |
| 10 | React.SubmitEvent discipline | **PASS (N/A)** | No forms exist in the card; the only handler is `handleReapplyIntent(): void` — a documented intentional no-op (DEV2-005 wiring), touching no event object. |
| 11 | Test-file discipline | **PASS** | No `.skip(`/`.only(` (only a JSDoc mention), no `console.*` in any scope file; 18 cases = 9 branches × 2 locales; Apollo v4 `MockedProvider` from `@apollo/client/testing/react` per recorded deviation #1 of outcome/4.2. |

## Findings

| # | Severity | Location | Finding | Disposition |
|---|---|---|---|---|
| 1 | **LOW** | `frontend/views/teachers/dashboard/ApplicantStatusCard.tsx:126` | `<Typography variant="h5" component="h2" …>` uses the Typography `component` prop, which `frontend/AGENTS.md` MUI-v9 list marks "not a valid direct prop". However: (a) the 6.3 zero-tolerance set (fontWeight/mb/mt/p/textAlign/display) is clean; (b) repo precedent is widespread at HEAD — `DashboardView.tsx:65` (`component="h1"`, pre-existing in this very file family), `ComingSoonView.tsx:94`, `AuthFormShared.tsx:55`, `app/(auth)/register/RegisterForm.tsx:349`, `app/page.tsx:1655`; (c) tsgo/oxlint/biome/eslint gates all pass. Reads as stale rule wording vs actual MUI v9 typing rather than a code defect; the semantic heading (`h2` under the page `h1`) is accessibility-correct. | Rules-file reconciliation candidate for a docs/maintenance pass — no repair in this ticket. |
| 2 | **INFO** (pre-existing, filtered) | `DashboardView.tsx:108` | `<Stack direction="row" …>` — `direction` appears in the instructions' Stack invalid-props list. Line is byte-pre-existing (not in the ticket diff). | Informational only per the pre-existing filter. |

Everything else: PASS. No MEDIUM/HIGH/CRITICAL findings.

## Scope-of-diff verification

`git diff HEAD --stat` over the modified scope files confirms the ticket's deltas are exactly: `sharedDocuments/index.ts` (+1, DISP-1), `DashboardView.tsx` (+16/−1: props interface + slot render + header JSDoc), `RoleDashboardPage.tsx` (+16/−3: value-import of `UserRole` for the runtime comparison, card import, conditional slot, JSDoc), `translation-preload.ts` (+10/−2: Applicant+Errors warming). Net-new: card, card barrel, teachers documents domain, `format-date.ts` + `lib/i18n` barrel, component suite, integration suite. `UserRole` import-kind change (`type` → value) is legitimate: `role === UserRole.Teacher` is a genuine runtime anchor (Biome useImportType leaves it alone).

## Notes on cross-cutting conventions honored

- Error surface: deny-class codes branch via the shared `extractErrorCode` seam (`@/frontend/lib/graphql-error-utils`) on `extensions.code` only (never HTTP status) — per frontend/AGENTS.md Apollo-error-mapping rule; `PermissionDeniedFallback` consumed for the UNAUTHORIZED/FORBIDDEN class (never bare null); non-deny codes surface `errors.internalServerError` via inline Alert.
- Corrupt-status default arm + branch 7b (unrenderable cooldown instant) fail closed with mapped copy — consistent with the DEV3-002 contract; switch-over-same-typed-enum mirrors the `ProfileView.getRoleLabel` precedent (`frontend/views/dashboard/profile/ProfileView.tsx:586`), passing the oxlint `no-unsafe-enum-comparison` gate (the AGENTS Record-table guidance targets cross-type literal comparison).
- Translation preload extension warms `Applicant` + `Errors` for ar+en inside the existing loop (1.3 CF-2 discharged) — no locale files touched, no new keys invented (all consumed keys already existed after 1.3).

**WAVE 6.3 GATE: PASS** — zero blocking findings; one LOW (rules-file tension) + one pre-existing INFO, both carried to 6.5 reconciliation.
