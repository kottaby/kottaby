# 0.2 Outcome — Prerequisite Verification (Reuse, Don't Rebuild)

> Requirements: REQ-002, REQ-083
> Method: read-only Explore agent sweep of every artifact in Task 0.2's checklist. Full report retained in agent transcript; this file records the **fact catalog** downstream tasks build on.

## Verified Artifact Inventory (✅ = exists & matches plan)

| # | Artifact | Verdict | Key facts |
|---|---|---|---|
| 1 | `backend/db/schema/teachers/applicants.ts` | ✅ | `export const applicants = pgTable("applicants", { … })`; `id` integer PK→users.id cascade; `verificationAttempts` int default 0; `lastAttemptAt`/`cooldownUntil` nullable timestamps; `status` varchar(50) default 'pending'; timestamps defaultNow + $onUpdate. Comment: teacher row only after passing (B.7). |
| 2 | `backend/db/schema/enums.ts` | ✅ | `export const userRole = pgEnum("user_role", ["admin","teacher","student","parent"])` — contains parent (C.1). TS mirror: `backend/enum/users/user-role.enum.ts` (`UserRole` + `toUserRole`). |
| 3 | `backend/db/schema/users/users.ts` | ✅ | Governance fields present: `isDeleted/deletedAt/suspended/suspendedAt/suspendedPeriodDays/int/isBlocked/blockedAt/lastActiveAt` (booleans nullable; Pothos resolves `?? false`). |
| 4 | `backend/types/teachers/applicant.types.ts` | ✅ | `export type ApplicantSelectType = typeof applicants.$inferSelect;` + `ApplicantInsertType`; exported via `backend/types/teachers/index.ts` → `backend/types/index.ts` (`@/backend/types`). |
| 5 | `backend/db/repo/teachers/applicant.repository.ts` | ⚠️ | Only `create(userId: number, tx: DBTransaction)` exists (namespace `ApplicantRepository`). **`findByUserId` ABSENT — net-new in 2.1 (as planned).** Read pattern to mirror: `UserRepository.findByEmail` — optional **`tx?: DBQueryExecutor` LAST param** + private `isDBTransaction(tx)` guard → tx branch uses Drizzle, else `queryDb<UserSelectType>(sql, params)` Neon-HTTP fast path. `DBTransaction`/`DBQueryExecutor` from `backend/types/db.types.ts` via `@/backend/types`. |
| 6 | Registration teacher branch | ✅ | `backend/services/auth/registration.service.ts` — `createRoleChild(userId, role, tx)` case "teacher" → `await ApplicantRepository.create(userId, tx); return;` — NO teacher row anywhere. `withTransaction(outerTx)` helper at lines 127–135 (savepoint-aware). `registerUser(input, locale, outerTx?)`. |
| 7 | DEV2-002 authScope engine | ✅ | `backend/graphql/pothos/builder.ts` — `AuthScopes: { authenticated: boolean; role: UserRole[]; permission: string[]; superAdmin: boolean; notImpersonating: boolean }`; `role: (roles) => (ctx.role ? roles.includes(ctx.role) : false)`; `authenticated` throws `UnauthorizedError("Authentication required.")` when `!ctx.user`; `scopeAuthOptions.unauthorizedError` maps scope-fail → `ForbiddenError(getServerTranslations(context.locale).errorsTranslations.forbidden)`. Only existing usage: `me` query `{ authenticated: true }`. **`{ role: [...] }` will be FIRST role-gated field — no copy template; implementer must verify anonymous-vs-wrong-role error split against `backend/graphql/test/error-contract-matrix` behavior.** |
| 8 | `backend/db/test/entity-setup.ts` | ✅ | Helpers (tx FIRST arg per `backend/db/test/AGENTS.md`): `createTestUser(tx, overrides?)`, `createTestStudent(tx, userId, overrides?)`, `createTestParent(tx, userId)`, **`createTestApplicant(tx, userId, overrides?)` — ALREADY EXISTS** (defaults status:"pending", attempts 0, both ts null), `createTestAdmin(tx, userId)`. NO new helper needed. |
| 9 | Registration test module | ✅ | `backend/db/test/logic/auth/registration.service.test.ts` (DEV1-002 module — REQ-071 extension target). `backend/db/test/logic/teachers/` does NOT exist — 2.1 creates it fresh. |
| 10 | Locale system | ⚠️ | 6 namespaces (common, landing, errors, dashboard, auth, recitation); **NO `applicant` namespace yet** (1.3 creates it). Mechanism: Labels interface in `shared/locale/types/<ns>/index.ts` + union `Translations` in `shared/locale/types/message.ts` (keys are `<ns>Translations`) + ar/en leaves `shared/locale/{ar,en}/<ns>/index.ts` composed in `{ar,en}/messages.ts` + `defineNamespace<TLabels>(id, getLabels)` handle in `shared/locale/namespaces/<ns>/<ns>.namespace.ts` + registry `shared/locale/namespaces/index.ts` (`export const namespaces = { Auth, Common, … } as const`). ICU `{var}` interpolation. Imports MUST use `@/shared/locale/...` alias. **Actual server API: `getServerTranslations(locale)` ONE-ARG returns full bundle → access `.errorsTranslations`. `ctx.t` valid keys are `keyof Translations` → `ctx.t("errorsTranslations")` (NOT `ctx.t("errors")`).** |
| 11 | sharedDocuments | ⚠️ | `frontend/graphql/sharedDocuments/`: only `auth/` domain (+ `documents.contract.test.ts`). **`teachers/` domain is net-new (4.1).** Naming convention in code: lowerCamel `meQueryDocument` / `registerUserMutationDocument` (docs claim Pascal — follow CODE, note divergence). Pattern: `TypedDocumentNode<T, V> = gql\`…\`` from `@apollo/client`. |
| 12 | Views / dashboard | ⚠️ | Route: `app/(dashboard)/teacher/dashboard/page.tsx` → `createRoleDashboardPage(UserRole.Teacher, "/teacher/dashboard")` (factory at `frontend/views/dashboard/RoleDashboardPage.tsx`, uses `await withPageAuth({ roles: [role], redirectTo: path })` from `frontend/lib/auth/withPageAuth.ts` — hook-style; app/AGENTS path reference stale, use actual). Views tree has only `dashboard/` (+`profile/`); `frontend/views/teachers/` is net-new (4.2). `PermissionDeniedFallback` at `frontend/components/ui/PermissionDeniedFallback.tsx` (`{ title, description }`, uses `useAppTranslation(Errors)`, LockOutlined, role="alert"). `useAppTranslation` usage: `const t = useAppTranslation(Dashboard)` (handle imported from `@/shared/locale`). Chip sx-callback example: `ProfileView.tsx:184`. **NO locale date formatter exists anywhere (zero Intl/formatDate/date-fns hits; date-fns unused dep) — 4.2 creates a minimal util (see Adaptations).** Test scaffold: `test/ui/components/` has `TestWrapper.tsx` (`renderWithWrapper(ui, { locale })`, no Apollo provider — wrap MockedProvider explicitly in tests), `translation-preload.ts` (mocks next/navigation; warms Landing+Auth only — extend for applicant/dashboard), `happydom-preload.ts`; **`readTranslation(handle, locale)` does NOT exist** — existing tests assert against label objects imported from `@/shared/locale/namespaces/...` (follow that). |
| 13 | GraphQL integration tests | ✅ | `frontend/graphql/test/` exists (auth/gateway/warnings suites). Shared infra via `@/test/helpers`: `setupTestServerLifecycle()` (spawns next dev on TEST_PORT 3066, polls `_health` 60s), `testClient` (ApolloClient, HttpLink `…:{TEST_PORT}/api/graphql`, errorPolicy all, no-cache), `extractErrorCode(err)`, `expectMutationError(err, code?)`. Canonical usage: `frontend/graphql/test/auth/auth.test.ts`. |
| 14 | Required docs | ✅ | All 8 referenced docs exist under `docs/`. |
| 15 | Services structure | ⚠️ | `backend/services/`: `auth/`, `gateway/`, `shared/`, `fx/`(stub), `redis/`(stub) + `index.ts` barrel (`export * from "./auth"; export * from "./shared";`). **`teachers/` domain net-new (2.2) — needs folder + barrel + registration in services index.** Namespace pattern confirmed (`export namespace RegistrationService { … }`). `logger.logDomainError(message: string, ctx?: { code?; entity?; entityId?: string\|number; locale?; … })` at `backend/lib/logger.ts:72` (debug under TEST, warn prod). |
| 16 | Error classes | ✅ | `backend/lib/errors.ts`: `DomainError extends GraphQLError` ctor `(code, message, options?)` sets `extensions.code`; `NotFoundError(entity, message)` → code `${ENTITY}_NOT_FOUND` → `NotFoundError("APPLICANT", …)` = `APPLICANT_NOT_FOUND`; `ValidationError` overloaded `(message)`→VALIDATION and **`(code, message, options?, fields?)` custom-code form** → `new ValidationError("APPLICANT_COOLDOWN_ACTIVE", …)`; `ConflictError` → CONFLICT. Codes SCREAMING_SNAKE_CASE; custom pass-through codes normalize to null in taxonomy (kept unmasked by finalizer). |
| 17 | Query layer | ⚠️ | `backend/graphql/query/` flat: `auth.query.ts`, `health.query.ts`, `recitation.query.ts` + side-effect `index.ts` (`import "./auth.query"; …`). **Per `query/AGENTS.md` "Adding New Queries": create subdir (or flat file) `<entity>.query.ts`, register field on `gqlSchemaBuilder`, add side-effect import to subdir index, add subdir import to top-level index, then generate:gqlSchema + codegen.** Resolver = one-line delegation; NO named exports. `me` query is the shape template (`type: UserPothosObject, nullable: true, authScopes: { authenticated: true }, resolve: (_root, _args, ctx) => …`). |
| 18 | Pothos objects/enums | ✅ | Object ref: `gqlSchemaBuilder.objectRef<T>("Name").implement({ fields: t => ({ id: t.exposeInt("id"), … }) })` (user.pothos.ts). Enum registry `backend/graphql/pothos/shared/enum.pothos.ts` holds UserRolePothosEnum/GenderPothosEnum/RegisterPublicRolePothosEnum/RecitationReadingPothosEnum; enum-object form: `gqlSchemaBuilder.enumType(UserRole, { name: "UserRole" })`. CRITICAL rule: enums backed by real TS enum in `backend/enum/`; literal `values:[...]` arrays PROHIBITED in *.pothos.ts. |
| 19 | Locale date formatter | ❌ MISSING | No formatter exists tree-wide. **Resolution: 4.2 creates a minimal project util (see Adaptations #2).** |
| 20 | DBML status note | ✅ | `db/schema.dbml` applicants.status note verbatim: `'pending, in_evaluation, failed, passed'` — exact canonical value set for the TS enum. |

## Instruction-file inventory (auto-discovery targets)

`.agents/instructions/` contains exactly: `backend.instructions.md` (6-layer schema→types→repo→service→graphql→test; barrel conventions), `frontend.instructions.md` (sx-only MUI v9, Outlined icons, applies to frontend/** + app/**), `tests.instructions.md` (runInRollback; tx to ALL repo calls; `rejects.toThrow()` prohibited). **No `mobile-desktop.instructions.md` exists** (tasks.md 4.2.IV reference adapts to the three above).

## Adaptations forced by 0.2 findings (all within DEV2-004 net-new scope — no DEV1-owned file patched)

1. **Translation API shape**: use `getServerTranslations(locale).errorsTranslations` and `ctx.t("errorsTranslations")` (1-arg bundle + property access). The plan's `ctx.t("errors")` / `getServerTranslations(locale, "errors")` wording predates the actual API; adaptation honors REQ-003/051 intent (compile-time i18n, localized errors) without touching shared/locale internals.
2. **Date formatter**: no util exists (Task 4.2 anticipated "VERIFY util location in 0.2"). 4.2 adds ONE small locale-aware formatter (Intl.DateTimeFormat-based, ar/en mapping, RTL-safe) at the project-standard location chosen by the implementing agent under `shared/` or `frontend/lib/` per AGENTS conventions — no scattered `toLocaleDateString`.
3. **Component test scaffold**: `readTranslation` doesn't exist → tests follow the EXISTING repo pattern (import label objects from `@/shared/locale/namespaces/...`, assert preloaded labels, ZERO hardcoded strings — satisfies REQ-074's intent); `MockedProvider` wrapped explicitly inside `renderWithWrapper`; `translation-preload.ts` extended to warm the applicant/dashboard handles.
4. **First role-gated field**: `myApplicantProfile` is the first `{ role: [...] }` field. 3.3 MUST verify anonymous→UNAUTHORIZED vs non-teacher→FORBIDDEN against the builder (`authenticated` scope throwing UnauthorizedError; `scopeAuthOptions.unauthorizedError` mapping) and the existing error-contract matrix test; include `authenticated: true` alongside `role` if required by the engine to satisfy REQ-031.
5. **Net-new folders** (not missing prerequisites — planned deliverables): `backend/enum/teachers/`, `backend/services/teachers/`, `backend/graphql/query/teachers/`, `backend/graphql/pothos/teachers/`, `frontend/views/teachers/`, `frontend/graphql/sharedDocuments/teachers/`, `backend/db/test/logic/teachers/`, `test/ui/components/teachers/`, `frontend/graphql/test/teachers/`, `docs/teachers/`, locale `applicant` namespace.

## Missing-Artifact Gate (0.2.BLOCK)

**No ❌ blockers.** Every DEV1-001/DEV1-002/DEV2-002-owned prerequisite exists and matches the plan. The four adaptations above are in-ticket scope, executed by this ticket's own tasks.

## Semantic Review (0.2.SR)

✅ Exact file paths, method signatures, and the findByUserId/queryDb determination recorded as facts above.
