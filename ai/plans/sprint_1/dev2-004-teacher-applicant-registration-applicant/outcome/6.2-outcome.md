# 6.2 Outcome — Review Wave 6.2 (review-backend)

> Plan: DEV2-004 · Task: tasks.md 6.2 (Phase 6 — Review Waves)
> Scope: repository / service / resolver / test-discipline / architecture / dead-code over every ticket file.
> Method: read-only review (NO source modified, nothing committed). Rule set identical to wave 6.1 plus `backend/db/test/AGENTS.md` + `logic/AGENTS.md` lens via the outcomes. Standing interpretations from outcomes phase0→6.0 and DISP-1..7 honored as non-findings (repo read `tx?: DBQueryExecutor`; 1.2 Deviation #1 `import type`; APPLICANT_STATUS_CORRUPT disposition; non-tx arm coverage; parse-vs-gql integration documents; `$all` authScopes rationale; 4.2-partial fixes; 5.1 concurrency harness deviation; DI-1..3 ledgered infra items).

## VERDICT: PASS — 0 CRITICAL, 0 HIGH, 0 MEDIUM, 0 LOW code findings. 1 documentation note.

## Per-file results

### `backend/db/repo/teachers/applicant.repository.ts`

| Check | Evidence | Verdict |
|---|---|---|
| Atomicity | `recordVerificationAttempt` = ONE UPDATE: `verificationAttempts: sql\`${applicants.verificationAttempts} + 1\`` (DB-side arithmetic), `lastAttemptAt`/`updatedAt` = `sql\`now()\`` in the same statement, `.returning()` audit row; NO read-modify-write | PASS |
| tx discipline | Reads: `tx` → Drizzle select, else `queryDb` (Neon-HTTP rule per repo AGENTS; byte-mirrors `user.repository.ts` — standing interpretation). Writes: `tx` → tx, else global `db`. No global-db READ mixed with a tx WRITE inside any single operation (each method is exactly one statement) | PASS |
| Prepared statements | ZERO `sql.placeholder` anywhere; writes excluded from preparation per `docs/drizzle/prepared-statements.md`; no `inArray` | PASS |
| Inline `--` in sql templates | None (raw SQL contains only aliased columns + `WHERE id = $1 LIMIT 1`) | PASS |
| BOPLA | Sole input `userId: number` (caller-derived PK); parameterized `eq(applicants.id, userId)`; no client-shaped input, no interpolation of the id | PASS |
| Errors | New methods return `null` on miss (no throw paths added); the plain-`Error` + `'pending'` literal in pre-existing `create` is DEV1-owned/pre-existing (DI-1 ledgered) — exempt per filter | PASS (new code) |

### `backend/services/teachers/applicant-lifecycle.service.ts`

| Check | Evidence | Verdict |
|---|---|---|
| Captured-now | ONE `new Date()` per invocation, taken AFTER the single read; strict `>` boundary (INV-TV3); duration-agnostic (INV-TV4 untouched) | PASS |
| DomainError discipline | `NotFoundError(APPLICANT_ENTITY, …)` → `APPLICANT_NOT_FOUND`; `ValidationError("APPLICANT_STATUS_CORRUPT"|"APPLICANT_COOLDOWN_ACTIVE", …)` custom-code overload per `docs/graphql/domain-error-extensions-code.md` Rule 4; ZERO plain `Error` in new code; SCREAMING_SNAKE_CASE throughout | PASS |
| logDomainError scoping | Exactly 3 call sites — guard missing-row, guard cooldown-active, reapplication miss. Happy paths emit NOTHING; `getMyApplicantProfile` is fully silent INCLUDING the corrupt-status throw (spy-pinned test). Context shape `{ code, entity: "applicants", entityId, locale }` matches `logger.logDomainError(message, ctx)` and is test-asserted via `toMatchObject` | PASS |
| Silent paths / swallowed try/catch | No try/catch in service source; unexpected internals bubble uncaught to the masking boundary | PASS |
| env-config | No `resolveEnvConfig` / env reads at all | PASS (N/A as expected) |
| i18n | `getServerTranslations(locale)` ONE-arg from `@/shared/locale/server-graphql` (DISP-4 lens); zero hardcoded messages; cooldown template expanded via single-token `replace` with parity-pinned placeholder | PASS |
| Cooldown source isolation | Reads `applicants.cooldown_until` ONLY; zero governance-field contact (REQ-015/016) | PASS |

### `backend/graphql/query/teachers/applicant.query.ts`

| Check | Evidence | Verdict |
|---|---|---|
| authScopes | `authScopes: { $all: { authenticated: true, role: [UserRole.Teacher] } }` — standing 3.3 interpretation; 401-vs-403 split pinned live by the 8/8 integration matrix | PASS |
| Zero-arg contract | No args in the field config; SDL artifact `myApplicantProfile: ApplicantProfile` (no parentheses, grep-verified in generated schema); Tier-4 foreign-id probes die `GRAPHQL_VALIDATION_FAILED` | PASS |
| Delegation purity | One-line service delegation; the `if (!ctx.user)` branch is type-narrowing only (repo no-non-null-assertion rule), throws canonical `UnauthorizedError` with builder.ts's own message; no business logic, no repo calls | PASS |

### Tests (discipline sweep across all ticket suites)

| Check | Evidence | Verdict |
|---|---|---|
| runInRollback / tx-everywhere / expectRepoError | `applicant-lifecycle.test.ts` (10), `applicant-lifecycle.service.test.ts` (25), registration contract locks (4): every case inside `runInRollback`; `tx` passed to EVERY repo/service/entity-setup/direct-Drizzle call (tx-FIRST helpers vs tx-LAST repo params verified in source); all rejection assertions via `expectRepoError`/`assertErrorCode` try/catch | PASS |
| No `.rejects.toThrow` | grep: zero code occurrences (doc-comment mentions only) in all new suites | PASS |
| Entity-setup-only / no seeds | `createTestUser` + `createTestApplicant` (overrides for junk statuses/cooldowns); absent ids derived in-tx (`max(id)+1_000_000`), no magic constants, no seed reliance | PASS |
| Pre-existing test integrity | 14 DEV1-002 tests byte-untouched in `registration.service.test.ts`; change strictly additive (new describe + 3 helpers + header JSDoc line) | PASS |
| Harness safety | `sql.raw("savepoint dev2004_race")` / `rollback to savepoint …` are fixed literals (no interpolation); bounded rescue restores the outer tx; 5.1 dual-registerUser concurrency deviation is the recorded standing interpretation | PASS |
| Integration suite (`frontend/graphql/test/teachers/applicant-profile.test.ts`) | Shared `myApplicantProfileQueryDocument` as wire doc (REQ-073); local `parse` only for deliberately-invalid BOLA probes (standing interpretation); per-request bearer identity; direct-DB fixtures follow the documented harness convention (pre-existing `frontend/graphql/test/gateway/allowlist-coverage.test.ts` imports `@/backend` too — established pattern, not a new cross-layer violation); `expectMutationError` code assertions only | PASS |
| Locale parity suite | Pure unit tier; occurrence-count (not deduped) placeholder pins per 2.M finding-1 hardening; registry wiring asserted | PASS |

### Architecture & dead code

| Check | Evidence | Verdict |
|---|---|---|
| No cross-layer imports | grep over `backend/**`: zero imports of `frontend`/`app` (only AGENTS-doc mentions); `shared/locale` consumed ONLY via `@/shared/locale/server-graphql` in the service (per services AGENTS) | PASS |
| Barrels | `./` relative, `export *`, max-one-slash, no imports in barrels: `backend/enum/teachers`, `backend/db/repo/teachers` (+ top re-export), `backend/types/teachers` (+ top re-export), `backend/services/teachers` (+ top re-export, DISP-2), `backend/graphql/query/teachers` (+ top, side-effect), `sharedDocuments/teachers` (+ top, DISP-1) | PASS |
| Dead code | No unused exports/imports/methods: repo — all 3 methods consumed (create by registration; read/increment by lifecycle service); service — all 3 functions + 4 module-private helpers + `APPLICANT_ENTITY` consumed (`assertCanPurchaseVerification`/`recordReapplication` await DEV2-005 — recorded forward contract per 5.5, not debt); tests — all helpers used; gates (tsgo/oxlint/biome/type-aware) green on every touched file per per-task outcomes | PASS |

## Findings

| # | Severity | Location | Finding | Repair path |
|---|---|---|---|---|
| Note-2 | LOW / documentation-only | `outcome/2.2-outcome.md` (Security & Tenancy Audit bullet) | Prose says "five enumerated `logDomainError` sites"; the shipped service contains exactly THREE call sites (guard-missing, guard-cooldown, reapplication-miss) — which is the correct behavior per the refined instruction recorded in 2.2 Deviation #5 (function 1 fully silent). Code + tests agree; the outcome prose miscounts. | No code change. During Phase 7 knowledge propagation, cite "3 log sites / 5 enumerated rejections (2 of which are silent by design)". |

No CRITICAL/HIGH/MEDIUM/LOW code findings. Repository atomicity/tx/parameterization discipline, service error/log/i18n discipline, resolver authScopes/delegation contract, and all test-harness rules verified compliant.

## Suites re-run (this wave, mandated runner)

| Suite | Result |
|---|---|
| `backend/enum/teachers/applicant-status.enum.test.ts` | ✅ 21 pass / 0 fail / 278 expects |
| `backend/db/test/logic/teachers/applicant-lifecycle.test.ts` | ✅ 10 pass / 0 fail / 38 expects |
| `backend/services/teachers/applicant-lifecycle.service.test.ts` | ✅ 25 pass / 0 fail / 155 expects |
| `bun tsgo` (full repo) | ✅ exit 0 — 0 errors |

(The `frontend/graphql/test/teachers` integration suite and UI suites were last certified green at 5.3/5.5 with no changes since; re-running them here would require booting the dev server and is unchanged-code.)

## Residual risk

- **Non-tx repo arms** (`queryDb` read arm, global-`db` write arm) remain unexecuted by the backend harness by design (runInRollback tx-isolation rule outranks coverage — standing 2.1 CF-3 interpretation); their first live exercise is the production resolver path, already validated end-to-end by the 8/8 integration matrix through the query's non-tx read.
- **pg same-client concurrency deprecation** (2.2 CF-E): ≥2 concurrent statements on one Drizzle tx client warn under node-postgres pg@8; becomes an error at pg@9 — owned by the future pooling/connection ticket, not this domain.
- **DEV2-005 coupling**: the advisory guard's TOCTOU is closed only when the purchase flow passes ITS transaction's `tx` (5.5 forward contract) — consumer obligation, re-checkable at DEV2-005 review.
