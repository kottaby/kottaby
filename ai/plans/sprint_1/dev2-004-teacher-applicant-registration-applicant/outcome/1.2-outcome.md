# 1.2 Outcome — ApplicantProfileReturnType Canonical Type

> Requirements: REQ-004, REQ-017, REQ-032 (closed readonly output shape; no write fields; BOPLA)
> Task: tasks.md 1.2 (Phase 1 — Types, Enums & Database Schema)
> Recorded: 2026-08-27

## Summary

Extended the canonical applicant types file with `ApplicantProfileReturnType` — a closed, all-`readonly`, 7-field profile return shape — using the exact member list from the plan snippet. The existing `ApplicantSelectType` / `ApplicantInsertType` are byte-unchanged. The enum→type wiring lands via an import from `@/backend/enum/teachers/applicant-status.enum`; every usage site in this file is at type position only (pure interface, zero runtime logic added). ⚠ The plan's literal "VALUE import" wording could not survive in THIS file — Biome's recommended `lint/style/useImportType` rule flags a value-only-at-type-position import and `biome:check` (`--write --unsafe`) auto-normalizes it to `import type` (empirically proven; recorded as Deviation #1 with full gate evidence). The value-import discipline itself is UNHARMED where it has meaning — runtime positions in consumers (2.2 service guard/cooldown math, 3.2 `gqlSchemaBuilder.enumType`) — and the outcome carries explicit carry-forward obligations for those files. Barrel verified pre-wired, untouched. One working diff follows it.

## Files modified

| File | Action | Content |
|---|---|---|
| `backend/types/teachers/applicant.types.ts` | MODIFY (+33 lines) | NEW `export interface ApplicantProfileReturnType { readonly id: number; readonly status: ApplicantStatus; readonly verificationAttempts: number; readonly lastAttemptAt: Date \| null; readonly cooldownUntil: Date \| null; readonly cooldownActive: boolean; readonly canPurchaseVerification: boolean; }` + JSDoc header (closed-shape / REQ-017/REQ-032 contract, guard-validation note, computed-fields note) + new enum import line (see Deviation #1 for its final `type` form) + SOURCE-ANCHORED NOTE comment documenting the useImportType normalization and downstream value-import obligations. **All 7 members verbatim from tasks.md 1.2 snippet** including inline `//` comments. `ApplicantSelectType`/`ApplicantInsertType` definitions UNCHANGED (verified via git diff — no touched lines on them). |
| `outcome/1.2-outcome.md` | CREATE | This file |

Import style decision: alias convention (`@/backend/...`) follows THIS file's existing module-scope imports and every sibling types file (`registration.types.ts`, `teacher.types.ts`, …); barrels are the only relative-path context per AGENTS. Deep enum path matches Task 1.1 cross-file dependency #1. The VALUE-kind of the import was mandated by plan prose but is mechanically unenforceable here — see Deviation #1; the enforced-in-repo kind for pure type-position enum references inside `backend/types/**` is `import type` (nearest precedent: `registration.types.ts` line 23 `import type { Gender } from "@/backend/enum/users/gender.enum"` — the canonical registration ReturnType file, i.e. this exact pattern). Import ordering passes biome organizeImports (`@/backend/db…` < `@/backend/enum…`).

## Files NOT modified (dispositions)

| File | Disposition |
|---|---|
| `backend/types/teachers/index.ts` | **VERIFY-ONLY — confirmed, zero edits**: line 1 already reads `export * from "./applicant.types";` (0.2 finding #4 held). New type therefore flows through the teachers barrel → `backend/types/index.ts` → consumable as `@/backend/types`. Per AGENTS barrel rules, sub-barrels re-export via `./` relative paths only — nothing to add. |
| `backend/types/index.ts` | Untouched — top-level barrel already re-exports `./teachers`; no new domain registration needed. |
| Any `backend/db/schema/**`, `backend/db/migration/**` | Untouched (REQ-045 anchor intact). No schema work belongs to this task. |
| `tasks.md`, `plan.md`, everything else | Scope freeze honored — Task 1.2 carries exactly ONE source-file change. |

## Verification outputs

### Quality loop (1.2.QL)

`bun run scripts/health/sub-loop.ts backend/types/teachers/applicant.types.ts --lifecycle duplicates`
→ **exit 0**, all five stages green (tsgo → oxlint → biome:check → lint:type-aware → check:duplicates):

```
✅ tsgo passed (no errors for backend/types/teachers/applicant.types.ts)
✅ oxlint passed
✅ biome:check passed
✅ lint:type-aware passed
✅ check:duplicates passed
✅ All checks for lifecycle "duplicates" passed
```

Rule discovery during the run: `.github/instructions/backend.instructions.md` listed with ⚠ marker but DISCOVERED-ABSENT on disk (same DISP-3 resolution as Task 1.1); effective instruction set read was `.agents/instructions/backend.instructions.md` + printed `AGENTS.md`, `backend/AGENTS.md`, `backend/types/AGENTS.md`.

### Type gate

`bun tsgo` → **exit 0, 0 errors, full repo** (matches phase-0 baseline; zero NEW issues).

### Biome sweep

`bun biome:check` → exit 0, "Checked 485 files in 4s. No fixes applied." — **0 issues** tree-wide (477 at baseline; delta = sibling-task net-new files; zero findings attributable to this task).

### Duplicate-definition scan (1.2.SR negative-space check)

`rg "ApplicantProfileReturnType"` across the repo → sole code definition at `backend/types/teachers/applicant.types.ts`; all other hits are plan/spec/tasks prose or the Pothos consumer references planned for 3.2. Zero duplicated type definitions anywhere (REQ-004).

### Value-vs-type import probe (Deviation #1 evidence)

1. Authored with the mandated plain `import { ApplicantStatus } from "@/backend/enum/teachers/applicant-status.enum"`. First sub-loop run reported all stages ✅ exit 0 — but inspection of the post-run git diff showed Biome had silently rewritten line 2 to `import type { … }`: the repo's biome script executes `bunx @biomejs/biome check --write --unsafe .`, so the recommended rule `lint/style/useImportType` applies its safe fix during gating rather than failing.
2. Re-introduced the value form and probed WITHOUT write: `bunx @biomejs/biome check <file>` → warning `lint/style/useImportType` ("All these imports are only used as types", FIXABLE) on exactly that import — non-blocking under plain `check`, deterministically rewritten under the repo's actual `--write` runner on EVERY future protocol-mandated quality loop (permanent churn + mask hazard for downstream editors).
3. oxlint: clean in BOTH forms (301 rules, 0 warnings). tsgo/eslint:type-aware: clean in BOTH forms. Only Biome normalizes.
4. Conclusion recorded as Deviation #1 below.

## Security & Tenancy Audit (1.2.SEC — BOPLA/BOLA lens)

- Closed `readonly` shape: every field is `readonly`; consumers cannot mutate the returned profile object.
- Field-by-field verdict:
  - `id` (= users.id) — self-scope PK only, resolved server-side from `ctx.user.id` in 3.3; never an argument.
  - `status` — typed as `ApplicantStatus` enum; raw varchar row values must pass `isApplicantStatus` (fail-closed) at the service boundary before carrying this type.
  - `verificationAttempts`, `lastAttemptAt`, `cooldownUntil` — server-owned lifecycle data.
  - `cooldownActive`, `canPurchaseVerification` — explicitly mandated SERVER-COMPUTED derivations (plan D3); NOT client-writable inputs, not mirrors of any mutation input name/surface.
- ZERO governance fields (`isDeleted/deletedAt/suspended/blockedAt/…` family absent), ZERO secrets/tokens/internal flags, ZERO write-typed or input-shaped fields.
- Timestamp fields remain `Date | null` at the TS canonical-type tier (integrity of the shared objectRef source per REQ-004/REQ-017/032); their GraphQL ISO-8601-string exposure is DISP-5 territory, recorded in outcome/3.2, out of scope here.
- Note: 2.2 must still compute the derived booleans against a single captured `now()` and validate stored status before constructing this type — the type guarantees shape, not runtime validity.

## Semantic Review (1.2.SR)

- ✅ Single canonical definition in the canonical applicant types file; no local `{Entity}Definition` clones; no service/Pothos-layer type definitions introduced.
- ✅ Naming follows `backend/types/AGENTS.md` `{Entity}ReturnType` pattern ("wholeness": one additive canonical type alongside the SelectType/InsertType foundation, which stay intact).
- ✅ No new `.types.ts` file created anywhere outside `backend/types/` (REQ-004) — net new `.types.ts` count = 0.
- ✅ Imports originate ONLY from backend layers (`db/schema` type import, `enum/teachers` enum import); zero imports from `app/`, `frontend/`, or `shared/`.
- ⚠➡✅ Value-import mandate reconciliation: implemented as Deviation #1 (below). The discipline the requirement protects — the TS enum remaining the single runtime vocabulary authority (REQ-012) with value semantics at RUNTIME positions — is untouched: no runtime code exists in this file at all, so nothing was downgraded; consumers with real runtime usage must (and will) value-import `ApplicantStatus` themselves (CF-1/CF-2 pin this). Accepted precedent forms inside this very layer: `import type { Gender }` at type position (`registration.types.ts`), plain value import only where a runtime anchor exists (`evaluation-session.contract.types.ts` pairs it with `EVALUATION_SESSION_INTENT`); replicating that anchor here would add an invented exported constant to the canonical types file (SR zero-dead-code exposure) purely to appease the linter — rejected.

## Instruction Verification (1.2.IV)

Validated against: root `AGENTS.md` (barrel conventions — shortest-path import of the eventual service type; uniqueness of exports), `backend/AGENTS.md` (6-layer flow; type-definition CRITICAL pattern; `@/backend/types` alias discipline for consumers), `backend/types/AGENTS.md` (location rules, single-canonical-object pattern, naming), `.agents/instructions/backend.instructions.md` (type/layer separation, Pothos compatibility note: nullable TS fields ⇒ `nullable: true` exposure at 3.2). Discovered-absent: `.github/instructions/backend.instructions.md` (noted per DISP-3).

## Test Engineering (1.2.TE)

Compile-time tier only, per task definition: tsgo full-repo green (above) proves the interface compiles and the enum→type wiring resolves. A shape-assertion unit test is NOT required for a pure interface (explicitly waived by tasks.md 1.2.TE); the enum→type wiring is exercised indirectly by Task 2.2 service tests once `getMyApplicantProfile` constructs real instances of this type.

## Deviations vs plan

| # | Plan text | Reality | Classification |
|---|---|---|---|
| 1 | tasks.md 1.2: “`ApplicantStatus` imported as VALUE import from `backend/enum/teachers/applicant-status.enum.ts`” (echoing 1.1's coupling note) | Final file carries `import type { ApplicantStatus } …` + an anchored source NOTE comment. Mechanical cause: the file contains ZERO runtime usages of the symbol, and Biome-recommended `lint/style/useImportType` (executed via the repo's `check --write --unsafe` on every mandated quality loop) rewrites the value form deterministically (evidence above). Repo precedent at identical call-site shape (`registration.types.ts` Gender) already uses `import type`; the alternative (artificial runtime-anchor constant à la `evaluation-session.contract.types.ts`) would inject an unmandated export into canonical types. Executed repo-convention-over-plan-prose, same resolution class as Task 1.1 deviation #3 (“if repo convention differs, follow it”). Semantic intent preserved: REQ-012 authority lives in the enum FILE; member typing flows through; 1.1.SR's “value-import semantics FOR RUNTIME USAGE” invariant remains fully enforceable in Tasks 2.2/3.2 (their own loops will FAIL if a consumer strips its needed value import, since there the import gains runtime anchors). | Documented deviation — unavoidable given the toolchain; zero effect on REQ-004/017/032 contracts; consumers pinned by CF-1/CF-2 |
| 2 | Snippet shows fields with trailing comments and no file header | Added a JSDoc header block documenting the closed-shape/guard/computed contracts above the verbatim field list, plus a source-note comment adjacent to the import explaining Deviation #1 in-code (prevents future value-import churn attempts) | Documentation-only additions; member set + types + modifiers + inline comments verbatim from plan — no semantic deviation |

No requirement-level deviations. Nothing committed to git (working tree holds this task's single source diff alongside sibling-task changes from 1.1/1.3/plan-review, which were left untouched).

## Carry-forward

1. **CF-1 → Task 2.2**: `ApplicantLifecycleService.getMyApplicantProfile(userId, locale, tx?)` MUST construct exactly this shape: fetch via `findByUserId`, null ⇒ return null (certified/never-applicant), else validate `row.status` with `isApplicantStatus` (junk ⇒ fail-closed ValidationError), capture `now` ONCE, derive `cooldownActive = cooldownUntil !== null && cooldownUntil > now` and `canPurchaseVerification = !cooldownActive && status !== ApplicantStatus.Passed`, then shape `ApplicantProfileReturnType`. Return type signature should import from `@/backend/types` (barrel path). **The service MUST use its OWN plain value import of `ApplicantStatus`** (comparison against `ApplicantStatus.Passed` is a genuine runtime usage — Biome will NOT normalize it there); if any tool converts it to `import type`, comparisons break compilation — treat that as a signal, not suppress it.
2. **CF-2 → Task 3.2**: Pothos `objectRef<ApplicantProfileReturnType>("ApplicantProfile")` exposes these exact 7 fields; `status` rides `ApplicantStatusPothosEnum` (`gqlSchemaBuilder.enumType(ApplicantStatus, { name: "ApplicantStatus" })` — real-TS-enum backed, VALUE-imported for the schema-builder call, literal arrays prohibited); `lastAttemptAt`/`cooldownUntil` expose as nullable ISO-8601 strings per DISP-5 (HealthCheck precedent) — nullability in the TS type maps to GraphQL non-null defaults being unsafe for them (`nullable: true` REQUIRED per backend instructions); `cooldownActive`/`canPurchaseVerification` expose non-nullable booleans; `id` always exposed (Apollo cache normalization). Note on this file's `status` comment vs GraphQL surface: the ISO-string DISP-5 lens applies at 3.2 exposure only; the canonical TS field deliberately stays `Date \| null` to preserve objectRef integrity (REQ-004).
3. **CF-3 → Tasks 3.3/4.x**: this type is the entire permitted GraphQL surface — review wave must reject any additional field exposure; admin surfaces do NOT route through this type.
4. **CF-4 → Review waves (5.x)**: when diff-reviewing this file, expect the import to be `import type` — do not flag it against the task's value-import wording without reading this outcome's Deviation #1 (gate-proven mechanical impossibility in a zero-runtime-usage file).
