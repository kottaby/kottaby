# 5.2 Outcome — Lifecycle Guard & Attempt-Counter Contract Tests Consolidation (REQ-072)

> Requirements: REQ-014, REQ-015, REQ-042, REQ-043, REQ-070, REQ-072
> Certified pass over the suites authored in 1.1/1.3/2.1/2.2/5.1 — re-executed live at certification time.

## Certified matrix (all green, live run)

| REQ-072 row | Proven in | Result |
|---|---|---|
| `cooldownUntil IS NULL` ⇒ allowed (no-op, REQ-053 silence) | service suite (2.2) | ✅ |
| Future cooldown ⇒ `APPLICANT_COOLDOWN_ACTIVE` + TRANSLATED message includes formatted expiry | service suite Tier 2 | ✅ |
| Exactly `now` ⇒ ALLOWED (strict `>` boundary) | service suite Tier 2 | ✅ |
| Past cooldown ⇒ allowed | service suite Tier 2 | ✅ |
| Missing applicant row ⇒ `APPLICANT_NOT_FOUND` (guard + reapplication paths) | service + repo suites | ✅ |
| Sequential `recordVerificationAttempt` ×2 ⇒ 0→1→2, `last_attempt_at` monotonic | repo suite (2.1) Tier 2/3 | ✅ |
| Concurrent `Promise.allSettled` ×2 ⇒ final attempts = 2, no lost update (REQ-042) | repo suite Tier 3 | ✅ |
| Corrupt status fails closed (`APPLICANT_STATUS_CORRUPT`) | service suite Tier 3 | ✅ |
| Localized keys resolve in BOTH ar/en | service suite Tier 4 + parity suite | ✅ |
| logDomainError scoping (never on happy paths) | service suite (spy-pinned) | ✅ |
| Tenancy: user A's attempt cannot touch user B's row | repo suite Tier 4 | ✅ |
| Registration contract locks (5.1): defaults, atomicity, race | auth module `DEV2-004 registration contract locks` describe | ✅ |

## Live suite results (this certification run)

```
backend/db/test/logic/teachers/applicant-lifecycle.test.ts   10 pass / 0 fail
backend/services/teachers/applicant-lifecycle.service.test.ts 25 pass / 0 fail
backend/enum/teachers/applicant-status.enum.test.ts           21 pass / 0 fail
shared/locale/applicant-namespace.parity.test.ts              11 pass / 0 fail
backend/db/test/logic/auth/registration.service.test.ts       18 pass / 0 fail
```

## Coverage (REQ-070: 100% statement/branch on NEW logic)

`bun --coverage backend/services/teachers/applicant-lifecycle.service.ts` suite → **service file: 100.00% funcs / 100.00% lines**. Repo new-method tx-facing logic fully covered; non-tx arms = structural twins unexecutable under the mandated `runInRollback` harness (standing interpretation from 2.1 CF-3, accepted at 2.M). No `.rejects.toThrow()` anywhere (grep-verified); no raw-key assertions; no seed-data reliance.

## Semantic Review (5.2.SR)

✅ No test uses `.rejects.toThrow()`; assertions against TRANSLATED substrings only; tenancy exclusivity proven; discipline per tests.instructions.md.
