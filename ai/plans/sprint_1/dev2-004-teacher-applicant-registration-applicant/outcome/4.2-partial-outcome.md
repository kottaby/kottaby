# 4.2 Partial Outcome — ApplicantStatusCard UI Component (+ date util + tests)

> Requirements: REQ-003, REQ-018, REQ-062, REQ-063 (+ REQ-015 display path, REQ-074)
> Dispositions honored: **DISP-4** (`useAppTranslation(Applicant)` handle value-imported from `@/shared/locale` — NO `Translation` enum exists or was created), **DISP-6** (explicit `Passed` branch), **DISP-3** (`frontend/views/AGENTS.md`, `frontend/components/ui/AGENTS.md`, `mobile-desktop.instructions.md`, `.github/instructions/tests.instructions.md` all discovered-absent on disk — skipped with this note; effective rule set applied: root/frontend/app AGENTS + `.agents/instructions/frontend.instructions.md` + `test/ui/AGENTS.md`)
>
> **Scope note:** per orchestrator instruction this file records ONLY Task 4.2's code deliverable — component + date util + translation-preload extension + component suite. The Agent-Browser functional/visual self-loops (4.2.BF/4.2.BS) and dashboard mounting (Task 4.3) are deferred to the orchestrator. Pre-existing working-tree edits to `DashboardView.tsx` / `RoleDashboardPage.tsx` belong to that later leg and were NOT touched here.

## Summary

Implemented `ApplicantStatusCard` as a fully self-contained client component ("use client", zero props): it pulls its own data via stateful `useQuery(myApplicantProfileQueryDocument)` (`@apollo/client/react` — useLazyQuery FORBIDDEN and absent, grep-proven below) and renders every reachable branch of plan §5.5's visual state matrix, including review-mandated extras (explicit `Passed` arm per DISP-6; corrupt-status defensive arm). A minimal frontend date util (`formatApplicantDate`) mirrors the backend lifecycle service's module-private Intl formatter option-for-option so client-side `{cooldownUntil}` expansion stays byte-consistent with server-side cooldown stamps (1.3 CF-1 procedure). The i18n preload warms `Applicant` + `Errors` for ar+en, and a dual-locale (RTL/LTR) Happy-DOM suite pins all nine branches against PRELOADED label objects — zero hardcoded Arabic/English in assertions. Zero new i18n keys were needed (every key consumed already existed after Task 1.3, including `certifiedSurfacesHint`); no locale files touched.

## Files changed

| File | Action | Role |
|---|---|---|
| `frontend/views/teachers/dashboard/ApplicantStatusCard.tsx` | CREATE | The card (branches 1–9 + shell/chip/skeleton primitives, sx-only) |
| `frontend/views/teachers/dashboard/index.ts` | CREATE | Domain barrel (`export * from "./ApplicantStatusCard";`) — root AGENTS barrel convention |
| `frontend/lib/i18n/format-date.ts` | CREATE | `formatApplicantDate(iso, locale)` — JSDoc'd Intl wrapper |
| `frontend/lib/i18n/index.ts` | CREATE | Barrel for the net-new `lib/i18n` domain |
| `test/ui/components/translation-preload.ts` | MODIFY | Warm `Applicant` + `Errors` handles for ar+en inside the file's existing `[arMessages, enMessages]` warming loop (1.3 CF-2 discharged) |
| `test/ui/components/teachers/ApplicantStatusCard.test.tsx` | CREATE | 9 branches × 2 locales = 18 cases |

Touch set respects the task constraint exactly (component, date util, preload, test, this outcome). `frontend/lib/graphql-error-utils.ts` was reused NOT authored (committed at `4394e55`). Nothing committed to git; dev server on :3000 kept running throughout.

**View placement** (recorded per plan-review-R1 finding D1-E): `frontend/views/teachers/dashboard/ApplicantStatusCard.tsx` — the 0.2 Adaptation-#5 registered net-new domain folder, kept as its own domain rather than nesting under `frontend/views/dashboard/**`.

## Branch matrix (rendered ⇄ tested)

| # | Condition | Surface | Test case |
|---|---|---|---|
| 1 | query in flight (`loading`) | Card-shaped `Skeleton` stack (title/badge/body/CTA placeholders) on root `aria-busy="true"` + `data-testid="applicant-status-card-loading"` | both locales ✓ (asserts settled copy never leaks) |
| 2 | error code `UNAUTHORIZED` \| `FORBIDDEN` | Shared `PermissionDeniedFallback` (Errors-namespace default copy) — never bare null | FORBIDDEN `result.errors[].extensions.code` mock → fallback renders, card absent ✓ |
| 3 | any other error code | Inline `Alert severity="error" variant="outlined"` carrying `errors.internalServerError` (existing Errors key — none invented) | INTERNAL_SERVER_ERROR mock → alert text ✓, deny surface absent ✓ |
| 4 | `myApplicantProfile === null` (REQ-035 single answer: never-applied/certified) | Certified summary `t.certifiedSummary` + informational `t.certifiedSurfacesHint`; NO status labels/chip at all | honesty probes: all four status labels absent ✓ |
| 5 | `Pending` | Chip (`statusPending`, hourglass icon, primary-family container) + prompt panel `t.pendingPrompt` (purchase flow = DEV2-005 scope, noted) | chip + prompt present, eligible/cooldown copy absent ✓ |
| 6 | `InEvaluation` | Info chip + attempts counter (`attemptCountLabel` × `verificationAttempts`) + `inEvaluationHint` panel | label, hint, count `2` all asserted ✓ |
| 7 | `Failed` && `cooldownActive` (+ parseable `cooldownUntil`) | Warning chip + expiry line `cooldownExpiryLine` with `{cooldownUntil}` expanded via `formatApplicantDate` + DISABLED re-apply Button (LockOutlined, ≥44px) | expanded line matched against independent Intl clone; disabled attr non-null; `eligibleToReapply` suppressed; raw `{cooldownUntil}` never leaks ✓ |
| 7b | `Failed` && cooldownActive but `cooldownUntil` unrenderable | Fail-closed bare warning status (chip only) — never a fabricated date | covered logically by resolver union (server fails closed; cannot be constructed through the typed GraphQL surface — see deviations) |
| 8 | `Failed` && `canPurchaseVerification` | Success affordance (`eligibleToReapply` + CheckCircleOutlined) + ENABLED contained CTA labeled `reapplyCta` (HistoryEduOutlined, ≥44px) | enabled button (no disabled attr); cooldown-prefix probe absent ✓ |
| 9 | `Passed` (**DISP-6**) | Success chip `statusPassed` (SchoolOutlined) + certified narrative `certifiedSummary` (CTA hidden) | passed chip + summary; re-apply/status-failed/status-pending all absent ✓ |
| — | unknown status value (defensive; server fails closed pre-wire) | Inline alert carrying `errors.applicantStatusCorrupt` — DEV3-002 mapped copy, never crashes | not constructible via typed GraphQL mocks (documented in deviations) |

Enum-safety: statuses resolve through an exhaustive `switch` over the same-typed codegen `ApplicantStatus` members mirroring the established `ProfileView.getRoleLabel` precedent, with a default corruption arm (the oxlint gate passed — no unsafe cross-type comparison). Cooldown sub-branching reads ONLY server-computed flags (`cooldownActive` / `canPurchaseVerification`) — the client never re-derives lifecycle math (REQ-015/016 truthfulness).

## Date util consistency contract

`frontend/lib/i18n/format-date.ts` mirrors the backend service table byte-for-byte:

- locale tag resolution — ONLY exact `"en"` selects English; everything else falls back to `"ar"` (identical to `resolveLocaleTag` in `backend/services/teachers/applicant-lifecycle.service.ts` + `shared/locale/server.ts`);
- options — `{ timeZone: "UTC", year: "numeric", month: "2-digit", day: "2-digit", hour: "2-digit", minute: "2-digit", hour12: false }` ⇒ deterministic across host TZs; Arabic-Indic digits under `ar`, Latin under `en`.

The component expands the single ICU token with plain `.replace("{cooldownUntil}", …)` — safe because the 1.3 parity suites pin EXACTLY ONE placeholder per locale.

## Test output (standalone run, Happy DOM, dev :3000 untouched)

Command (mirrors `test:ui:components`' full preload chain per the sibling-suite requirement):

```
TEST_SERVER_EXTERNAL=1 GRAPHQL_TEST_PORT=3000 KOTTAB_TEST_RUNNER_OK=1 \
bun --env-file=.env.test test test/ui/components/teachers/ApplicantStatusCard.test.tsx --timeout=60000 \
  --preload ./test/ui/test-env.ts --preload ./test/ui/components/happydom-preload.ts \
  --preload ./test/ui/components/translation-preload.ts --preload ./test/ui/components/next-dynamic-mock.ts

→ 18 pass, 0 fail, 76 expect() calls (9 branches × ar-RTL + en-LTR) — 2.60s
```

REQ-074 discipline: every string assertion resolves through `ApplicantNs.getLabels(getTranslations(locale))` / `ErrorsNs.getLabels(...)` — the same objects the preload warms; the only literals are technical fixtures (payload id, ISO instant) plus an ASCII-error-code mock and the independently recomputed timestamp (Task 2.2 service-suite technique). No `.skip(`/`.only(` anywhere.

## Static proofs

- **useLazyQuery absence** — `rg -n "useLazyQuery" frontend/ app/` matches ONLY two AGENTS.md prohibition lines + one contract-test comment header; **zero source references**.
- **Error-code extraction pattern followed** — component calls the established shared helper `extractErrorCode(error)` (`@/frontend/lib/graphql-error-utils`, the extraction seam used by LoginForm/RegisterForm; walks `CombinedGraphQLErrors.errors[0].extensions.code` — same shape the production `frontend/providers/apollo/utils.ts` consumes).
- Cooldown formatter table parity verified by direct read of both files during implementation.

## Quality loop (sub-loop `--lifecycle duplicates`, exit 0 required)

| File | Result |
|---|---|
| `frontend/views/teachers/dashboard/ApplicantStatusCard.tsx` | PASS exit 0 |
| `frontend/views/teachers/dashboard/index.ts` | PASS exit 0 |
| `frontend/lib/i18n/format-date.ts` | PASS exit 0 |
| `frontend/lib/i18n/index.ts` | PASS exit 0 |
| `test/ui/components/translation-preload.ts` | PASS exit 0 |
| `test/ui/components/teachers/ApplicantStatusCard.test.tsx` | PASS exit 0 (after fixes below) |

Full-repo gates: `bun tsgo` → exit **0**, 0 errors · `bun biome:check` → exit **0**, "Checked 500 files… No fixes applied" (matches phase-0 baseline discipline; 495→500 delta is this task's five new frontend/test files).

## Deviations & carry-forward

1. **MockedProvider entrypoint (Apollo v4)** — instruction quoted `@apollo/client/testing`, but on the pinned `@apollo/client@4.2.12` the provider lives in the nested `@apollo/client/testing/react` entrypoint (v4 restructure) and the deprecated top-level wire type was replaced by the non-deprecated `MockLink.MockedResponse`. Imports adjusted accordingly with an in-file comment; also dropped v4-removed `addTypename={false}` prop and the legacy unused `deniedMock` stub (dead code), and corrected a meaningless assertion in branch 8 to a real honesty probe (cooldown-template prefix must be absent). No behavioral change.
2. **Render-option plumbing** — `MockedProvider` wraps INSIDE `renderWithWrapper(..., { locale })` so RTL emotion caches/theme/locale context match production; active route locale comes from the preload's mocked `next/navigation` (Pattern-2 suites, per `test/ui/components` divergence note: upstream's Suspense `readTranslation` cache module doesn't exist on this branch — the sync `getLabels(getTranslations(locale))` procedure stands).
3. **Branches 7b/corrupt-status untestable-by-construction** — the typed GraphQL surface (`ApplicantStatus` codegen enum + service fail-closed guard) makes those payloads impossible to author via a well-typed mock; they remain implemented defensively and are recorded here instead of faked with unsafe casts.
4. **Plan §5.3 pending row vs §5.5/task branch spec** — §5.3's prose gloss ("pending → attempts count") is superseded by the authoritative §5.5 visual-state matrix and tasks.md 4.2 enumeration, which specify chip + awaiting-purchase prompt only; the attempts counter renders in InEvaluation (and would be misleading pre-purchase when attempts can't increase yet). Recorded as the binding reading, consistent with INV-TV1 truthfulness.
5. **Re-apply CTA navigation** — enabled CTA click is an intentional silent no-op (documented in-component): the verification-plan purchase route is DEV2-005 scope. Not dead UI — truthful affordance with localized copy and ≥44px hit area. Carried forward to DEV2-005 wiring; React.SubmitEvent discipline asserted N/A (no form events exist; no handlers evolve beyond the no-op onClick).
6. **Browser self-loops (4.2.BF/4.2.BS)** — DEFERRED to the orchestrator per explicit instruction; screenshot evidence section intentionally omitted from this outcome. Tasks.md checkbox ownership for 4.2.BF/BS remains with that orchestrator pass together with Task 4.3 mounting (`DashboardView.statusSlot` work already staged in-tree belongs to it).
7. **Tree state** — working tree carried prior sibling-task artifacts (4.1 docs/codegen, 2.x backend files, untracked outcome dir) before this task; untouched. This task's own authoring began from a partially-staged earlier attempt of the same files, which was completed/fixed/hardened within this session's quality loop (all six files exited 0 post-fix); nothing committed.
