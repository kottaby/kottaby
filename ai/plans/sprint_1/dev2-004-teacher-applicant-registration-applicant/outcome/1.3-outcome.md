# 1.3 Outcome — i18n Keys: `applicant` Namespace (NEW) + `errors` Extensions

> Requirements: REQ-003, REQ-051, REQ-018, REQ-015, REQ-050 (+ DISP-4 BINDING i18n lens; DISP-6 Passed branch)
> Mechanism verification: 0.2 finding #10 + plan-review-R1 D2-A (defineNamespace-handle procedure — NO `Translation` enum was created; the plan's conditional "register in the Translation enum ONLY IF that enum is the established mechanism" resolves to **enum absent → namespace-handle procedure followed**).

## Summary

Registered the new `applicant` compile-time i18n namespace across all six touchpoints of the actual (code-verified) mechanism, mirroring the existing namespaces file-by-file, and extended the canonical `errors` namespace with three additive transport keys for the DEV2-004 deny surfaces (`APPLICANT_NOT_FOUND`, `APPLICANT_COOLDOWN_ACTIVE`, corrupt-status fail-closed). Both locales are atomically complete with EXACT key parity (compile-typed against one shared interface + runtime parity suites). One new dedicated parity test pins ar/en applicant key identity, the `{cooldownUntil}` placeholder contract, and registry wiring. Zero other namespaces, zero frontend/backend files touched.

**Namespace handle recorded for Phase 4 consumer:**

| Item | Value |
|---|---|
| Export name | `Applicant` |
| Handle id | `"applicant.applicant"` (same `<ns>.<ns>` convention as Errors=`"errors.errors"`) |
| Client usage | `const t = useAppTranslation(Applicant)` — handle VALUE-imported from `@/shared/locale` (DISP-4) |
| Server usage | `getServerTranslations(locale).applicantTranslations.<key>` / resolver `ctx.t("applicantTranslations")` |
| Bundle slot | `translations.applicantTranslations` (composed in both messages bundles) |

## Files created

1. `shared/locale/types/applicant/index.ts` — `ApplicantLabels` interface (12 keys, all `readonly`, JSDoc'd).
2. `shared/locale/en/applicant/index.ts` — `applicantEn` natural English copy.
3. `shared/locale/ar/applicant/index.ts` — `applicantAr` natural RTL-correct Arabic copy.
4. `shared/locale/namespaces/applicant/applicant.namespace.ts` — `export const Applicant = defineNamespace<ApplicantLabels>("applicant.applicant", translations => translations.applicantTranslations);`
5. `shared/locale/namespaces/applicant/index.ts` — per-domain barrel (`export * from "./applicant.namespace";`), mirrors siblings.
6. `shared/locale/applicant-namespace.parity.test.ts` — NEW parity gate (see Verification).

## Files modified

7. `shared/locale/types/message.ts` — `applicantTranslations: ApplicantLabels` added to `Translations`.
8. `shared/locale/en/messages.ts` — imports + composes `applicantEn`.
9. `shared/locale/ar/messages.ts` — imports + composes `applicantAr`.
10. `shared/locale/namespaces/index.ts` — `Applicant` imported, `export * from "./applicant";`, and first entry in the `namespaces` const object (alphabetical position ahead of `Auth`).
11. `shared/locale/types/errors/index.ts` — `ErrorsLabels` gains `applicantNotFound`, `applicantCooldownActive` (ICU `{cooldownUntil}`, JSDoc pins REQ-015/REQ-035), `applicantStatusCorrupt`. Purely additive.
12. `shared/locale/en/errors/index.ts` — English values for the three keys.
13. `shared/locale/ar/errors/index.ts` — Arabic values for the three keys.

## Files NOT modified (and why)

- All six sibling namespaces' types/ar/en/handle files — additive-only discipline.
- No `frontend/**`, `backend/**`, or `app/**` file (the consuming card/resolver/service lands in Phases 3–4). Includes `test/ui/components/translation-preload.ts` (its warming extension belongs to Task 4.3's component-test work) — carry-forward CF-2.
- `tasks.md`, `plan.md` untouched by THIS task (their tracked modifications predate this task from sibling tasks/plan review — see Deviations note).
- `bun.lockb`, package.json — no new dependencies.

## Key inventory (recorded for downstream consumers)

### `applicant` namespace (`useAppTranslation(Applicant)`)

| Key | en | ar |
|---|---|---|
| statusPending | Pending evaluation | بانتظار التقييم |
| statusInEvaluation | In evaluation | قيد التقييم |
| statusFailed | Failed | لم ينجح |
| statusPassed *(DISP-6)* | Passed | ناجح |
| statusCardTitle | Application Status | حالة الطلب |
| pendingPrompt | Your application is registered. Purchase your verification sessions whenever you are ready to begin the evaluation. | تم تسجيل طلبك بنجاح، ويمكنك شراء جلسات التحقق متى شئت لبدء التقييم. |
| attemptCountLabel | Verification attempts | عدد محاولات التحقق |
| cooldownExpiryLine *(ICU {cooldownUntil})* | You can re-apply after {cooldownUntil}. | يمكنك إعادة التقديم بعد {cooldownUntil}. |
| eligibleToReapply | Your waiting period has ended — you are now eligible to re-apply for teacher verification. | انتهت فترة الانتظار، وأنت الآن مؤهل لإعادة التقديم للتحقق كمعلم قرآن. |
| reapplyCta | Re-apply | إعادة التقديم |
| certifiedSummary *(DISP-6 certified branch)* | Your teacher verification is complete. You are certified to teach Qur'an recitation. | اكتمل التحقق بنجاح، وأنت الآن معتمد لتعليم القرآن الكريم. |
| inEvaluationHint | Your evaluation covers five recitation sessions before a decision is made. | يشمل التقييم خمس جلسات للتلاوة قبل إصدار القرار النهائي. |

### New `errors` keys (transport surface)

| Key | GraphQL producer | en | ar |
|---|---|---|---|
| applicantNotFound | `NotFoundError("APPLICANT", …)` → `APPLICANT_NOT_FOUND` (REQ-050) | Teacher application not found. | لم يتم العثور على طلب التقديم كمعلم قرآن. |
| applicantCooldownActive | `ValidationError("APPLICANT_COOLDOWN_ACTIVE", msg)` (REQ-015) | You can re-apply for teacher verification after {cooldownUntil}. | يمكنك إعادة التقديم للتحقق كمعلم قرآن بعد {cooldownUntil}. |
| applicantStatusCorrupt | fail-closed path when a row status is not a known `ApplicantStatus` | Your application status could not be read. Please contact support. | تعذر قراءة حالة طلبك. يرجى التواصل مع فريق الدعم. |

**REQ-035 discipline**: `applicantCooldownActive` interpolates ONLY `{cooldownUntil}` + generic copy — no other user data enters the message; both parity suites enforce the single-placeholder shape mechanically.

## Verification outputs

### 1. Quality loop — sub-loop per touched file (`--lifecycle duplicates`, exit 0 required)

All 13 created/modified source files below ran through
`bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates`; every run printed
"✅ All checks for lifecycle \"duplicates\" passed …" with **exit code 0**
(tsgo → oxlint → biome:check → lint:type-aware → check:duplicates):

| File | Result |
|---|---|
| shared/locale/types/applicant/index.ts | PASS (exit 0) |
| shared/locale/en/applicant/index.ts | PASS (exit 0) |
| shared/locale/ar/applicant/index.ts | PASS (exit 0) |
| shared/locale/types/message.ts | PASS (exit 0) |
| shared/locale/en/messages.ts | PASS (exit 0) |
| shared/locale/ar/messages.ts | PASS (exit 0) |
| shared/locale/namespaces/applicant/applicant.namespace.ts | PASS (exit 0) |
| shared/locale/namespaces/applicant/index.ts | PASS (exit 0) |
| shared/locale/namespaces/index.ts | PASS (exit 0) |
| shared/locale/types/errors/index.ts | PASS (exit 0) |
| shared/locale/en/errors/index.ts | PASS (exit 0) |
| shared/locale/ar/errors/index.ts | PASS (exit 0) |
| shared/locale/applicant-namespace.parity.test.ts | PASS (exit 0) |

- AGENTS/instruction discovery during the runs: `AGENTS.md` (root) + `shared/AGENTS.md` + `shared/locale/AGENTS.md` — read and honored (absolute `@/shared/locale/**` alias everywhere; leaf modules = plain literals only). Sub-loop additionally flagged `.github/instructions/tests.instructions.md` as applicable-to-tests — **discovered-absent on disk**; honored DISP-3 (discover-absent-and-skip-with-note) and read the real `.agents/instructions/tests.instructions.md` instead (bun:test ✓, no `getServerTranslations` call inside tests ✓ — parity suites import pure label maps exactly like the existing errors parity suite, no console.* ✓, no `any` ✓).
- Iteration note: first `tsgo` pass surfaced 3 type errors in the parity test (bun `test.each` overload rejects a readonly tuple). Fixed in-file by spreading (`test.each([...APPLICANT_ERROR_KEYS])`) — cross-file fix unnecessary.

### 2. Parity tests (locale-parity tier, REQ-051)

- **Existing dynamic `errors` gate still green (Finding #8 / review Note confirmed):**
  `bun run test/scripts/run-test.ts shared/locale/errors-namespace.parity.test.ts`
  → `8 pass, 0 fail` (123 expect calls). Its dynamic ar/en key-set equality auto-covers the three NEW errors keys; its route-emitter scan and hygiene pins remain satisfied.
- **NEW dedicated applicant gate (created because no dynamic parity existed for the new namespace):**
  `bun run test/scripts/run-test.ts shared/locale/applicant-namespace.parity.test.ts`
  → `11 pass, 0 fail` (87 expect calls):
  1. identical sorted ar/en key sets for the `applicant` leaves (non-empty values, symmetric sweep);
  2. each of the three new `errors` keys resolves non-empty in BOTH locales;
  3. `{cooldownUntil}` pin — `errors.*.applicantCooldownActive` AND `applicant.*.cooldownExpiryLine` contain EXACTLY ONE ICU placeholder named `cooldownUntil` in BOTH locales, plus a drift guard asserting placeholder-name sets equal across locale pairs;
  4. registry wiring — `namespaces.Applicant` exists, `Applicant.id === "applicant.applicant"`, handle getter returns the composed bundle slice by reference for both locales, `applicantTranslations` present on both bundles.

### 3. Type gate

`bun tsgo` → **exit 0, 0 errors** (matches phase-0 baseline; zero NEW issues).

### 4. Biome sweep (belt over every shared/locale file)

`bun x @biomejs/biome check shared/locale/` → "Checked 53 files … No fixes applied." — 0 issues.

### 5. Grep-gate (Task 1.3.TE negative-space requirement)

`git diff | grep -n "next-intl\|shared/messages\|getBackendTranslations"`
→ **EMPTY** (no matches; grep exit 1 = none found). The legacy mechanisms appear nowhere in the diff.

## Security & Tenancy Audit (1.3.SEC)

- Cooldown reject copy = generic text + single timestamp placeholder only; mechanically pinned by both parity suites (single-placeholder assertion). No other-user data, no internal flags, no oracle phrasing ("does not exist"/existence hints) beyond the canonical self-scope denies already established (for self-profile miss, "not found" is the sanctioned localized deny per REQ-050).
- Labels are static literals in `shared/` — layer purity intact: zero imports from `frontend/`/`backend/`/`app/` anywhere in the touched surface (ESLint-enforced + verified visually in diff).
- Property access only — no string-key invocation exists in the compile-time system surface.

## Semantic Review (1.3.SR)

- ✅ Zero hardcoded user-facing strings introduced outside the two locale systems this task registered; task registers keys only (no consumer code).
- ✅ Symmetric ar/en coverage enforced at TWO tiers: compile-time (both leaves typed `ApplicantLabels`/`ErrorsLabels` → missing key fails tsgo) + runtime (both parity suites). Interpolation placeholder NAME identical across locales (explicit test).
- ✅ Dispersion of DISPs: DISP-4 honored (handle mechanism, no enum); DISP-6 honored (`statusPassed` chip + `certifiedSummary` pair registered so Task 4.2 can build the explicit Passed/certified branch without new keys).
- ✅ Registration procedure mirrored file-by-file from the verified mechanism (0.2 finding #10 / D2-A), including the `<ns>.<ns>` id convention observed in Auth/Dashboard/Recitation handles.

## Instruction Verification (1.3.IV)

Read + validated against: root `AGENTS.md`, `shared/AGENTS.md`, `shared/locale/AGENTS.md`, `.agents/instructions/tests.instructions.md` (+ `frontend.instructions.md` read for parity/consumer conventions; it does not apply to `shared/**` source). Rule files discovered-absent: `.github/instructions/tests.instructions.md` (sub-loop hint path) — noted per DISP-3.

## Deviations & Carry-forward

1. **CF-1 (mechanism note for Phase 2/4 consumers)** — REQ-015/the task wording mandates ICU `{cooldownUntil}` template strings; this repo currently has NO runtime ICU expansion machinery (the live convention interpolates via typed function labels, e.g. `Dashboard.welcome(name)`; the formatter files `shared/locale/format.ts` / `client/format.ts` cited by `shared/locale/AGENTS.md` do not exist on disk). The cooldown strings therefore ship as plain-string ICU templates exactly as instructed, and the Phase 2 service (`assertCanPurchaseVerification`) and Phase 4 card MUST expand `{cooldownUntil}` themselves (e.g. `String.prototype.replace` with the locale-formatted timestamp from the date-formatter util planned under 0.2 Adaptation #2) before surfacing the message. If 2.2 prefers typed-function labels instead, that requires editing these keys + the parity pin together and must be recorded in outcome/2.2 — treated here as a conscious implementation-time choice, not debt.
2. **CF-2** — `test/ui/components/translation-preload.ts` warms Landing+Auth handles only; Task 4.3 must extend it to warm the `Applicant` handle (and `Errors`) before authoring ApplicantStatusCard component tests (0.2 item 12 / Adaptation #3).
3. **CF-3** — `shared/locale/types/index.ts` public types barrel does not export `auth`/`errors`/`recitation` either; consistency kept by NOT adding `applicant` there. Consumers reach labels via the `Applicant` handle from `@/shared/locale` (public barrel) or direct deep imports like the parity suites.
4. Tree-state note: the working tree at execution time already carried sibling-task changes (plan.md/tasks.md edits, `backend/enum/teachers/{index.ts,applicant-status.enum.ts,+test}` from Task 1.x; untracked outcome dir). None were touched by this task; the grep-gate and tsgo gates above span the full working diff regardless. Nothing committed to git.
