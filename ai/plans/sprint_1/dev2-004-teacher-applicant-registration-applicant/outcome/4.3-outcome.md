# 4.3 Outcome — Teacher Dashboard Integration

> Requirements: REQ-062, REQ-063
> Note: the mounting change landed via a subagent that then died on an infra error; fixtures + browser self-loops executed directly by the orchestrator. Dev server stayed up throughout.

## Mount description

- `frontend/views/dashboard/RoleDashboardPage.tsx` — `createRoleDashboardPage` now composes an optional role-specific `statusSlot` into `DashboardView`: teacher → `<ApplicantStatusCard />`, all other roles → `undefined` (their dashboards byte-identical to before). Guards untouched — `withPageAuth({ roles: [role] })` remains the ONLY server boundary.
- `frontend/views/dashboard/DashboardView.tsx` — new optional `readonly statusSlot?: ReactNode` prop rendered between the welcome header and the stat grid (`{statusSlot ? <Box sx={{ mb: 4 }}>{statusSlot}</Box> : null}`) — above the fold per plan §5.2.
- Sub-loop exit 0 on both files; tsgo 0; biome 0.

## Component composition tests

Covered by the 18-test suite from Task 4.2 (branch matrix × locales). RoleMismatch documented as unreachable: the page guard redirects non-teachers before any render (evidence: DEV2-001/002 withPageAuth implementation + live sweep below).

## Fixture users (dev DB, idempotent script /home/z/kottab-runtime/dev2004-fixture.ts)

demo-br-{pending,in-eval,cooldown,reapply,certified}@test.local — password `Password123`; applicants rows shaped per branch (cooldown = failed + cooldown_until now+30d; reapply = failed + expired; certified = teacher row, NO applicants row).

## Browser functional self-loop verdicts (agent-browser, live :3000)

| Check | Verdict |
|---|---|
| pending user → /teacher/dashboard | ✅ card renders pending branch ("بانتظار التقييم" chip + prompt copy) |
| in_eval user | ✅ "قيد التقييم" chip + attempt counter ("عدد محاولات التقييم: 1") + hint |
| failed_active user | ✅ warning chip "لم ينجح" + locale-formatted expiry ("يمكنك إعادة التقديم بعد 2026/09/26، 13:05.") + **DISABLED** CTA w/ lock |
| failed_eligible user | ✅ success affordance + **ENABLED** CTA "إعادة التقديم"; click = intentional no-op (DEV2-005 scope), no crash, no navigation |
| certified user | ✅ green certified summary ONLY — zero applicant copy (REQ-063) |
| Role-negative sweep: student@draftacademy.local → /teacher/dashboard | ✅ existing guard redirects to /student/dashboard; card never reachable for non-teachers (canonical deny UX observed) |
| Network assertion | ✅ exactly ONE `myApplicantProfile` operation per dashboard load (reload delta = 2 requests total: AuthProvider `me` + profile query, no variables possible — SDL zero-arg); **zero refetch growth after 8s** (no loops) |
| Console errors during all loops | ✅ none |

## Visual self-loop screenshot inventory (/home/z/kottab-runtime/dev2-004-shots/)

| Shot | Coverage verified |
|---|---|
| pending-ar-desktop.png | pending branch AR desktop; chip/alert hierarchy above stat grid |
| in_eval-ar-desktop.png | attempts counter + hint AR desktop |
| failed-active-ar-desktop.png | formatted Arabic date + disabled CTA lock state |
| failed-eligible-ar-desktop.png | enabled CTA, success affordance AR |
| certified-ar-desktop.png | certified summary AR (no applicant copy) |
| certified-en-desktop.png | EN LTR full dashboard composition (sidebar/nav unchanged) |
| pending-en-mobile.png | 375×812 EN single-column full-bleed card above fold |
| failed-active-ar2-mobile.png | 375×812 AR RTL mirroring (chip/date/CTA right-aligned, ≥44px CTA, no truncation) |

Visual review findings: theme-palette colors only (verified visually — palette chips/warnings/success map to semantic MUI colors); typography rhythm consistent; RTL flip correct end-to-end; mobile CTA comfortably tappable. No defects found requiring another iteration.

## Files modified

- frontend/views/dashboard/RoleDashboardPage.tsx (+statusSlot composition)
- frontend/views/dashboard/DashboardView.tsx (+optional slot prop)
- outcome/4.3-outcome.md
- (fixtures live OUTSIDE the repo: /home/z/kottab-runtime/dev2004-fixture.ts)

## Deviations / carry-forward

1. Re-apply CTA click intentionally does nothing (purchase route = DEV2-005). Rendered as informational affordance per plan §5.5 fallback note.
2. Demo fixture users remain in the dev DB pending orchestrator cleanup before handover (emails demo-br-*@test.local).
3. sharedDocuments/AGENTS.md teachers-docs list entry still owed at knowledge propagation (7.2 owns doc-graph updates alongside DISP-3 convention checks).
