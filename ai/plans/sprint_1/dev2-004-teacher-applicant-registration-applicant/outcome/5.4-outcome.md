# 5.4 Outcome — Security Tier Probes & Fuzz Lock (REQ-075)

> Requirements: REQ-012, REQ-030, REQ-031, REQ-033, REQ-035, REQ-075
> Probes are spread across the enum suite (1.1), service suite (2.2), integration suite (3.3/5.3), and static SDL/lock evidence — consolidated here against plan §6's threat table.

## Threat-table verdicts

| Plan §6 threat | Probe / evidence | Verdict |
|---|---|---|
| BOLA / IDOR | SDL zero-arg artifact: `myApplicantProfile: ApplicantProfile` (schema.graphql grep — no parentheses); integration Tier-4: inline-literal AND variable-supplied foreign-id args die `GRAPHQL_VALIDATION_FAILED` before any resolver; identity exclusively `ctx.user.id` (signature audit, 2.M) | ✅ PASS |
| BOPLA (mass assignment) | diff grep for `...input` spreads = ZERO; `recordVerificationAttempt` accepts zero client input shapes; `ApplicantProfileReturnType` closed readonly; no mutation exists in this ticket (SDL Query-only delta) | ✅ PASS |
| BFLA (function-level authz) | authScopes `{ $all: { authenticated, role: [Teacher] } }`; live matrix: student/parent/admin all `FORBIDDEN`, anonymous `UNAUTHORIZED` (5.3 certification); supervisor not yet a persisted role (C.2) — covered structurally by the role gate | ✅ PASS |
| No privilege escalation via applicant state | grep diff: ZERO writes to `teacher.*` / `is_approved` / `is_evaluator` / `is_online` / `subjects` / `request_preference`; only `teacher`-row touch is a TEST fixture (5.1/3.3 harness), never production code | ✅ PASS |
| Governance non-interference (REQ-016) | grep diff: ZERO `users.suspended` writes in source (only `suspended: false` fixture initializations in test harnesses); cooldown math reads `applicants.cooldown_until` exclusively | ✅ PASS |
| SQL/LIKE injection | no LIKE/ILIKE anywhere in ticket; repo write is a parameterized Drizzle `sql` increment (static review 2.1.SR); id never concatenated | ✅ PASS |
| Guard fail-closed (corrupt status) | service Tier-3: junk values (`%`, `_`, `\`, `'`, unicode/RTL, overlong) → `APPLICANT_STATUS_CORRUPT` ValidationError, never rendered | ✅ PASS |
| Guard fuzz/prototype pollution (enum tier) | enum suite Tier 2-4: case-mismatch, whitespace, `""`, `0`, null, undefined, 200 seeded fuzz strings, 10k payloads, `% _ \ '` composites, `Object.create(null)`, `__proto__` payloads, Symbol, throwing-toString objects — ALL rejected WITHOUT throwing (21 tests) | ✅ PASS |
| Role escalation via crafted payloads | no write surface exists (SDL mutation-free delta); schema rejects unknown fields (GraphQL spec) — covered by Tier-4 unknown-argument probes | ✅ PASS |
| Cooldown replay bypass | sequential guard-then-record semantics locked in 5.2 matrix (strict `>`, boundary allow, monotonic attempts); racing purchase write documented as DEV2-005's transactional responsibility (advisory guard, REQ-043) | ✅ PASS (documented boundary) |

## Determinism

All fuzz inputs seeded/fixed (200 deterministic strings; no Math.random in assertions); CI-safe.

## Findings

Zero security findings. Every probe maps to a threat row; no repairs needed; no deferred entries.

## Semantic Review (5.4.SR)

✅ Probes deterministic in CI; every threat row has live or static evidence; boundary exclusivity proven (5.2 Tier 4).
