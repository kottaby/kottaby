# 0.1 Outcome — Baseline Recording & Deferred-Items Ledger Initialization

> Requirements: REQ-001, REQ-045
> See `phase0-baseline-outcome.md` for the full verbatim command table.

## Summary

All five baseline commands executed and recorded (tsgo 0, biome 0, direct eslint 0, lint-service anomaly documented, git diffs empty). Ledger verified present with zero debt rows.

## Files created/modified

- CREATE `outcome/phase0-baseline-outcome.md`
- VERIFY-ONLY `deferred-items.md` (already initialized by plan author from the template; zero entries; left byte-identical)

## Files NOT modified

- Everything else — baseline phase touches no source.

## Semantic Review (0.1.SR)

- ✅ Outcome contains all five command outputs verbatim (see phase0 file table).
- ✅ Ledger initialized with zero ❌/⚠️ entries (2 grep hits are the template's static legend lines, documented).

## Carry-forward

- Baseline counts: tsgo=0, biome=0, eslint=0. Target at 5.5: zero NEW issues.
- Exempt set: empty.
- lint-service CLI quirk: use direct eslint for differentials.
