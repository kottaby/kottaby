# 1.1 Outcome — ApplicantStatus Canonical Enum & Type Guard

> Requirements: REQ-012, REQ-004, REQ-075 (guard fails closed), REQ-045 (no schema drift)
> Task: tasks.md 1.1 (Phase 1 — Types, Enums & Database Schema)
> Recorded: 2026-08-27

## Summary

Implemented the `ApplicantStatus` TS enum + `isApplicantStatus` guard exactly per plan snippet and REQ-012 member list (`Pending = "pending"`, `InEvaluation = "in_evaluation"`, `Failed = "failed"`, `Passed = "passed"` — verbatim match with the canonical `applicants.status` value set, 0.2 item 20). Wired the existing `backend/enum/teachers/` domain barrel; verified the top-level barrel already re-exports `./teachers` (VERIFY-ONLY disposition). Added a pure-unit 4-Tier test suite: **21 tests pass / 0 fail** via the repo-approved runner. All four files passed `scripts/health/sub-loop.ts --lifecycle duplicates` with exit 0.

**Environment reality check:** `backend/enum/teachers/` was NOT net-new for this ticket. It already existed on disk with `teacher-request-preference.enum.ts` + `index.ts` (and `backend/enum/index.ts` line 6 already re-exported `./teachers`) — exactly as pre-verified by plan-review-R1.md finding D3-A/§10 ("backend/enum/index.ts already re-exports ./teachers … teachers/ currently holds teacher-request-preference.enum.ts"). The plan's "CREATE teachers/index.ts" wording therefore resolved to MODIFY-existing-file; no deviation in substance. `ApplicantStatus` name-collision check against `TeacherRequestPreference` passes (0.2 #10).

## Files created / modified

| File | Action | Content |
|---|---|---|
| `backend/enum/teachers/applicant-status.enum.ts` | CREATE | `ApplicantStatus` enum (4 members) + `isApplicantStatus(value: unknown): value is ApplicantStatus`; JSDoc header citing the canonical value set + B.6/B.7 contract + REQ-012. **File imports NOTHING** (verified by scan); enum + guard exported as VALUE (no `export type`). Guard body is byte-equivalent to the plan snippet. |
| `backend/enum/teachers/applicant-status.enum.test.ts` | CREATE | 4-Tier pure unit suite (21 tests, bun:test, no DB, no `.skip`/`.only`, no `console.*`) |
| `backend/enum/teachers/index.ts` | MODIFY | Added `export * from "./applicant-status.enum";` at top (alphabetical order preserved before `teacher-request-preference.enum`) |
| `outcome/1.1-outcome.md` | CREATE | This file |

## Files NOT modified (dispositions)

| File | Disposition |
|---|---|
| `backend/enum/index.ts` | **VERIFY-ONLY — confirmed, no edit needed**: line 6 already reads `export * from "./teachers";`. Matches plan 1.1 "edit ONLY if absent"; matches plan-review-R1 §10 verification. Sub-loop on this file exit 0 (unchanged file still gated clean). |
| Any `backend/db/schema/**`, `backend/db/migration/**` | Untouched (REQ-045 anchor intact). No pgEnum added; column remains varchar(50); zero schema drift. |
| Everything else | Scope freeze honored — only the 4 files above carry Task 1.1 changes. |

## Verification outputs

### Quality loop (per-file, `--lifecycle duplicates` = tsgo → oxlint → biome → lint → duplicates)

| File | First run | Final run |
|---|---|---|
| `backend/enum/teachers/applicant-status.enum.ts` | ✅ exit 0 (all 5 stages green) | exit 0 |
| `backend/enum/teachers/index.ts` | ✅ exit 0 | exit 0 |
| `backend/enum/index.ts` | ✅ exit 0 | exit 0 |
| `backend/enum/teachers/applicant-status.enum.test.ts` | ❌ exit 1 (tsgo) → ❌ exit 1 (oxlint ×2) | ✅ **exit 0** (all 5 stages green; `check:duplicates` skipped as outside jscpd scan scope — script-reported "passed") |

Fix loop on the test file (all fixes confined to that file):
1. tsgo TS2769 — `expect(Object.values(ApplicantStatus)).toEqual([...])`: plain string literals are not assignable to the nominal enum type under bun:test's strict overload. Fixed via `(Object.values(ApplicantStatus) as string[]).join("|")` assertion against `"pending|in_evaluation|failed|passed"` (same guard-side cast pattern).
2. oxlint `typescript/no-unsafe-type-assertion` — replaced `unknown as Record<…>` and `JSON.parse(raw) as Record<…>` assertions with `const x: unknown = …` annotated fixtures (no casts at all).
3. oxlint `unicorn/consistent-function-scoping` — moved the Tier-4 function fixture to module scope (`memberReturningFunction()`).

Sub-loop rule-discovery notes: it printed `.github/instructions/backend.instructions.md` / `tests.instructions.md` with a ⚠ marker and root+`backend/AGENTS.md` with ✓. `.github/instructions/` does not exist on this tree (`ls`: "No such file or directory") — those entries resolve to DISCOVER-ABSENT (DISP-3 pattern; effective instruction set read was `.agents/instructions/{backend,tests}.instructions.md`, identical-to-convention content). Read per mandate: `AGENTS.md`, `backend/AGENTS.md` (printed), plus task-mandated `backend/enum/AGENTS.md`.

### Test execution

- Raw `bun --env-file=.env.test test backend/enum/teachers/applicant-status.enum.test.ts` **fails by design** (exit 1): `test/scripts/test-runner-guard.ts` preload blocks direct invocations (prints "DIRECT bun test INVOCATION BLOCKED"). Repo convention followed instead.
- Runner used: `bun run test/scripts/run-test.ts backend/enum/teachers/applicant-status.enum.test.ts`
  - Final result: **21 pass / 0 fail**, 278 expect() calls, exit **0**
  - Log: `logs/2026-08-27T08-08-20/backend/enum/teachers/applicant-status.enum.test.ts.log`
- Env note: `~/.bun/bin/bun` did not exist in this sandbox (bun lives at `/usr/local/bin/bun`); created symlink `/home/z/.bun/bin/bun -> /usr/local/bin/bun` so the hardcoded `BUN_BIN` in `run-test.ts` resolves. Environment setup only — zero repo files touched by this fix (CROSS-FILE note: scripts hardcode `~/.bun/bin/bun` per root AGENTS command table, so other sandboxes will need the same).

### `bun tsgo`

- Per-file: sub-loop's project-wide-filtered tsgo reported **0 errors for each of the 4 Task-1.1 files** on every final sub-loop run.
- Full-repo `bun tsgo` during this task's window exited **non-zero with exactly 3 errors — ALL inside `shared/locale/applicant-namespace.parity.test.ts`**, a file concurrently being authored by the parallel Task 1.3 executor in the same working tree (its own sub-loop runs were visible in the process-lock queue mid-task: e.g., `[process-lock] Lock held by PID 9881 (sub-loop: shared/locale/en/applicant/index.ts)`). Zero errors in any Task-1.1 file. Not fixed here (scope freeze, DIFFERENT task owner); Task 1.3's outcome must record full-repo tsgo green.

## Tier-1..4 coverage statement

Guard logic branches: `typeof value === "string"` (true/false arms) ∧ `Object.values(ApplicantStatus).includes(value)` (true/false arm) — all three reachable branches exercised; 100% branch/statement coverage of `isApplicantStatus`.

- **Tier 1 — branch/stmt (5 tests):** canonical value set frozen via join-string assertion (`pending|in_evaluation|failed|passed` — catches accidental member edits, REQ-012 vocabulary pin); every member passes; string non-member rejected (`"inactive"`, `"teacher"`); non-string short-circuit rejected (`undefined`, `0`); type-narrowing proven through the predicate (`value` becomes `ApplicantStatus.Passed`).
- **Tier 2 — boundary (5 tests):** case mismatches (`"Pending"`, `"PENDING"`, `"In_Evaluation"`, `"in_Evaluation"`, `"FAILED"`, `"Passed"`); whitespace (`"pending "` trailing, `" pending"` leading, tab, newline, both-sides); empty string; near-miss spellings (`"in evaluation"`, `"evaluating"`, `"failedd"`, `"pass"`); primitives (`0`, NaN, true, undefined, null) — ALL rejected. Plan-listed inputs covered one-for-one plus adjacent boundaries.
- **Tier 3 — fuzz/chaos (5 tests):** 200 deterministic LCG pseudo-random strings (seeded, reproducible; alphabet cannot alias a member) — ALL rejected without throwing; 10k-char strings (pure long, member-prefixed, member-suffixed); unicode/Arabic/RTL (`القرآن`, `تجويد`, `الحالة`, RTL-mark-wrapped members U+200F, Hebrew U+05D0.., Arabic-injected, combining-diacritic `"pendi\u0300ng"`, emoji); LIKE wildcards & injection (`%`, `_`, `\`, `'`, `'"; DROP TABLE applicants; --'` composites incl. `"in_evaluation_"` near-member with trailing underscore); control chars (NUL `\x00`, CR, ZWNBSP U+FEFF). Guard fails closed on every input — REQ-075 satisfied at unit tier.
- **Tier 4 — security (6 tests):** object whose `toString()` returns a valid member — rejected WITHOUT coercion; object with **throwing** `toString`/`valueOf` — proves the guard never invokes user-code paths (fail-closed, no throw); `Object.create(null)` bare + array-like forged variant; crafted `__proto__`-bearing payload (computed-key literal, "__proto__" lands as own enumerable key — asserted via `Object.keys(...)`) as unknown input; `Symbol.for("passed")`; arrays and functions. ALL rejected without throwing.

## Cross-file dependencies discovered (report-only, none actioned here)

1. **Task 1.2 coupling**: `ApplicantProfileReturnType.status` will import `ApplicantStatus` from `@/backend/enum/teachers/applicant-status.enum` (deep import per `backend/enum/AGENTS.md` preference) — import target now exists.
2. **Task 3.2 coupling**: Pothos registration must use `gqlSchemaBuilder.enumType(ApplicantStatus, { name: "ApplicantStatus" })` in `backend/graphql/pothos/shared/enum.pothos.ts` (CRITICAL rule: real-TS-enum backed; literal arrays prohibited) + codegen.
3. **Parallel Task 1.3 collision domain**: the locale applicant namespace work touched `shared/locale/**` (incl. its parity test currently failing full-repo tsgo — see above). Enum layer is clean; unrelated to enum values.
4. **Env quirk** (carry-forward for orchestrator): `scripts/lint-service.ts` empty-output exit-1 baseline anomaly reconfirmed benign (sub-loop uses the queue client path and exits 0); `~/.bun/bin/bun` absent in fresh sandboxes breaks `test/scripts/run-test.ts` until symlinked.

## Deviations vs plan

| # | Plan text | Reality | Classification |
|---|---|---|---|
| 1 | "CREATE `backend/enum/teachers/index.ts`" | File already existed (with `teacher-request-preference.enum` export); executed as MODIFY, adding the applicant-status line alphabetically first | Plan wording superseded by plan-review-R1 §10 + AGENTS barrel rules — no substantive deviation |
| 2 | "VERIFY-ONLY `backend/enum/index.ts`" | Verified already re-exports `./teachers` (line 6) — recorded, untouched | Exactly per plan |
| 3 | Raw `bun --env-file=.env.test test <file>` suggested as runner attempt | Blocked by test-runner-guard preload by design; used mandated `bun run test/scripts/run-test.ts` | Follows repo convention (task instructions authorized "if repo convention differs, follow it") |
| 4 | Test-file shape free within 4-Tier description | Two fixtures hoisted to module scope and one cast avoided purely to satisfy oxlint/biome gates | None semantically — extra determinism bonus (LCG-seeded fuzz) documented above |

No requirement-level deviations. Do-not-commit honored (nothing staged/committed by this task).
