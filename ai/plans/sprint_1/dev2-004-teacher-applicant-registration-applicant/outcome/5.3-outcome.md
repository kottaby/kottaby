# 5.3 Outcome — GraphQL Integration Matrix Certification (REQ-073)

> Requirements: REQ-030, REQ-031, REQ-035, REQ-060, REQ-073
> Suite: `frontend/graphql/test/teachers/applicant-profile.test.ts` — now riding the SHARED `myApplicantProfileQueryDocument` TypedDocumentNode (Task 4.1) — the exact document the production card consumes.

## Document swap (this task)

- Removed the local wire-shape interface + type guard + `parse`d main document; imported `myApplicantProfileQueryDocument` from the teachers shared-documents domain.
- Tier-4 BOLA probes keep LOCAL `parse`d documents (deliberately INVALID operations — unknown `userId` argument — unexpressible by the shared document). Apollo's `gql` stays un-imported in this file (`@/backend/db` fixture chain flips bun module conditions → graphql-tag UMD crash; `parse` = same DocumentNode).
- Typed access now direct off the codegen types; the seven-field envelope asserted via `expect.objectContaining` + explicit `__typename` pin (codegen type is strict — no `__typename` field; Apollo merges it at runtime).
- Sub-loop `--lifecycle duplicates` exit 0; regression: component suites unaffected.

## Certified matrix (live run — 8 pass / 0 fail / 29 expects, 4.71s)

| Matrix row | REQ | extensions.code asserted |
|---|---|---|
| anonymous → denied | REQ-031 | `UNAUTHORIZED` (401 semantics — `$all` conjunction proof) |
| student → denied | REQ-031 | `FORBIDDEN` |
| parent → denied (C.1 role-confusion probe) | REQ-031 | `FORBIDDEN` |
| admin (DB fixture + real login) → denied | REQ-031 | `FORBIDDEN` |
| teacher-applicant → full shape, id === users.id, Pending/0/nulls/booleans | REQ-060/017 | — (happy path) |
| certified teacher → `{ myApplicantProfile: null }`, NO error | REQ-017/D5 | — (single null answer) |
| inline-literal foreign-id arg | REQ-030/075 | `GRAPHQL_VALIDATION_FAILED` |
| variable-supplied foreign-id arg | REQ-030/075 | `GRAPHQL_VALIDATION_FAILED` |

Engineered server-computed fixtures (pending / failed-active / failed-expired) are certified at the component tier (4.2 suite: 18 pass) and live-verified per branch in the 4.3 browser self-loop (screenshots in /home/z/kottab-runtime/dev2-004-shots/).

## Error-oracle uniformity (REQ-035)

All denials route through the canonical localized deny (`errorsTranslations.forbidden` via scopeAuthOptions / `UnauthorizedError` pass-through) — no existence disclosure; certified-vs-never-applied collapse to the ONE null.

## Runner note (sandbox infra)

Direct guarded invocation used (`KOTTAB_TEST_RUNNER_OK=1 GRAPHQL_TEST_PORT=3000 TEST_SERVER_EXTERNAL=1 bun --env-file=.env.test test … --timeout=60000`) per outcome/3.3's memory-constrained-box adaptation; CI keeps the standard run-server-tests lifecycle (no env flags set).

## Semantic Review (5.3.SR)

✅ Matrix table above maps every row to its REQ; `CombinedGraphQLErrors`-class helpers assert codes; no stack/PII leakage in error assertions.
