# Requirements & Specification: DEV2-004 — Teacher Applicant Registration & Applicants Table

> **Date**: Sprint 1, Week 3
> **Target Ticket**: DEV2-004 — Teacher Applicant Registration & Applicants Table
> **Plan directory**: `ai/plans/dev2-004-teacher-applicant-registration/`
> **Blocking dependencies**: DEV1-001 (`applicants` table, `user_role` enum), DEV1-002 (public `registerUser` contract — teacher branch ALREADY creates `applicants` row, never `teacher`), DEV2-002 (`role` authScope, canonical 401/403 contract, `requireRoleForPage`).
> **Critical design note**: Per the Existing Codebase State rule and the DEV1-002 canonical reference (`docs/auth/user-registration.md` §1, §5), **the applicant-registration write path is already implemented** (role=teacher → `applicants` row with `status='pending'`, `verification_attempts=0`, `last_attempt_at=NULL`, `cooldown_until=NULL`; NO `teacher` row; `CreateRoleChild` dispatch inside the registration transaction). DEV2-004 SHALL NOT re-plan or rebuild that path. This ticket's net-new scope is: (a) permanent invariant tests locking the registration→applicants contract, (b) the canonical applicant lifecycle vocabulary (`ApplicantStatus` value set over the existing `varchar(50)` column — NO DB enum, NO schema change), (c) the applicant self-service status surface (`myApplicantProfile`), and (d) the cooldown/re-application eligibility contract consumed by DEV2-005 (verification plan purchase). Any schema gap discovered is escalated via `deferred-items.md`, never patched inline.

---

## 1. Executive Summary & Problem Statement

**Feature:** Deliver the complete teacher-applicant lifecycle substrate: verify and permanently lock the DEV1-002 registration contract (teacher → `applicants` row, never `teacher`), define the canonical applicant status state vocabulary (`pending → in_evaluation → passed|failed` with cooldown semantics), expose an authenticated self-service applicant status query, and ship the cooldown-eligibility guard contract that gates verification-plan re-purchase in DEV2-005.

**Problem from user perspective:**

- **Teacher Applicant (Ibrahim):** registers as a teacher, must see a truthful "you are an applicant, pending evaluation" status — never a certified-teacher experience. After a failure, he must know exactly when his cooldown expires and that re-application is then permitted (and that re-applying increments his recorded attempt count).
- **Certified Sheikh / Platform integrity:** no applicant may hold a `teacher` row, appear teachable, or be granted any teaching surface before passing verification (B.7, FR-3.1, INV-TV1).
- **Super Admin:** future override surfaces (DEV2-010) require the applicant record (`status`, `verification_attempts`, `cooldown_until`) to be persisted accurately from day one.
- **Dev 2 (owner) / DEV2-005/006/007 (consumers):** need a single authoritative status enum, cooldown math, and attempt-tracking repo methods — not per-ticket re-derivation.
- **Dev 1 / Dev 3:** registration flows (DEV3-019 direct onboarding) and matching (teachers only, never applicants) depend on this boundary holding.

**Business value:** The applicant lifecycle is the quality-gatekeeping foundation of the platform's core promise (uncompromising teacher quality — `docs/architecture/architecture-overview.md` §1). Shipping the permanent contract tests now prevents silent regressions where a later ticket accidentally creates `teacher` rows at registration (catastrophic for INV-TV1) or lets cooldown-expired applicants be double-tracked. The `myApplicantProfile` surface is also the visible deliverable for the M1 gate ("applicant can register, purchase verification plan, complete evaluation loop — and see where they are in it").

**Actors involved:**

- **Teacher Applicant (caller):** registration (existing public surface), status inspection (new authenticated surface).
- **Super Admin (privileged caller):** reads applicant state laterally (DEV3-016 era); no new admin mutation here.
- **DEV2-005 (verification plan purchase):** consumes the cooldown eligibility guard and attempt-increment repo contract.
- **DEV2-006/007 (evaluation loop/aggregation):** consume status transition semantics (`in_evaluation`, `passed`, `failed` writes) via their own tickets.
- **Matching engine (DEV3-008):** structurally guaranteed applicants never enter the teacher directory (no `teacher` row ⇒ no wallet, no `subjects`, no availability).

**Non-goals (explicitly OUT of scope):**

- No schema changes: `applicants` table, indexes, and the `status varchar(50)` column are DEV1-001-owned. `ApplicantStatus` is a TypeScript enum over the varchar (precedent: DBML itself documents status as a varchar with a noted value set). No `pgEnum` addition, no `db push`.
- No verification plan purchase implementation (DEV2-005 owns purchase; this ticket ships only the cooldown guard + attempt-increment contract it must call).
- No evaluation session booking (DEV2-006), scoring/aggregation into `passed`/`failed` (DEV2-007), failed→student conversion (DEV2-009), or admin override (DEV2-010).
- No changes to the public `registerUser` mutation surface, input whitelist, or `RegisterPublicRole` enum (DEV1-002 baseline is frozen).
- No teacher availability/subjects/is_online flow (DEV2-011+).
- No notifications (DEV3-010 owns dispatch).
- No admin CRUD over `applicants` (DEV3-016 era).

---

## 2. Requirements & Acceptance Criteria (EARS Format)

### 2.1 Baseline & Foundational Preparation (MANDATORY)

- **REQ-001 (Pre-Implementation Baseline & Ledger)**: WHEN implementation begins THEN the executing agent SHALL record baseline error counts (`bun tsgo`, `bun biome:check`, `bun run scripts/lint-service.ts --json --id baseline`) AND initialize `ai/plans/dev2-004-teacher-applicant-registration/deferred-items.md` from `.agents/spec-process-guide/templates/deferred-items-template.md` AND write `outcome/phase0-baseline-outcome.md` capturing the counts and the pre-existing modified-file set (`git diff --name-only`).
- **REQ-002 (Dependency Guard — Reuse, Don't Rebuild)**: WHEN domain work starts THEN the agent SHALL verify and NOT reimplement the existing artifacts: `applicants` schema (`backend/db/schema/teachers/applicants.ts`), `ApplicantRepository` registration create method, `RegistrationService` teacher branch calling it inside the registration transaction, canonical types (`ApplicantSelectType`, `ApplicantInsertType` in `backend/types/teachers/applicant.types.ts`), and the DEV2-002 `role` authScope. IF any required artifact is missing THEN the agent SHALL record a ❌ entry in `deferred-items.md` and block dependent tasks — NEVER patch DEV1-001/DEV1-002-owned files in place as a workaround without an explicit, second coordinated fix line.
- **REQ-003 (Type-Safe i18n & Enum Value Imports Compliance)**:
  - Client components MUST use `useAppTranslation(Translation.<Namespace>)` with the `Translation` enum and property access (`t.propertyName`) — never string-literal namespaces and never `t('key')`.
  - Server components MUST use `await getTranslations(locale)` (single argument) and property access.
  - GraphQL resolvers MUST use `ctx.t("namespace")`; services/repositories MUST use `getServerTranslations(locale, "<namespace>")` from `@/shared/locale/server-graphql`.
  - All enum usages in runtime expressions/casts (`ApplicantStatus`, `UserRole`) MUST be value imports (not `import type`) and enum members, never raw string literals.
  - FORBIDDEN: `next-intl` imports, `getBackendTranslations`, `shared/messages/` references, any hardcoded user-facing string.
- **REQ-004 (Canonical Types Discipline)**: WHEN any code is authored THEN all entity types SHALL come from `backend/types/teachers/applicant.types.ts` (`ApplicantSelectType`, `ApplicantInsertType` — existing) and new service/API contracts SHALL be added to the same canonical file (`ApplicantProfileReturnType`, per candidate naming rules) or an existing adjacent canonical types file. No local type definitions SHALL appear in Pothos resolvers, services, or repository files; no service-layer `.types.ts` files.

### 2.2 Core Feature Logic / Happy Paths

- **REQ-010 (Registration Contract — Locked, Not Rebuilt)**: WHEN the permanent verification suite for this ticket runs THEN it SHALL assert that `registerUser` with `role = teacher` creates EXACTLY ONE `users` row AND exactly one `applicants` row sharing the user's PK, AND creates ZERO rows in `teacher`, within one atomic transaction (per DEV1-002 REQ-010/REQ-013/REQ-030). The implementation SHALL NOT modify the registration write path unless a defect is proven by these very tests.
- **REQ-011 (Applicant Row Defaults)**: WHEN a teacher registration completes THEN the `applicants` row SHALL have `status = 'pending'`, `verification_attempts = ` nullish-zero default `0`, `last_attempt_at = NULL`, `cooldown_until = NULL`, `created_at`/`updated_at` set by database defaults (per B.6 signature and `db/schema.dbml`).
- **REQ-012 (ApplicantStatus Canonical Vocabulary)**: WHEN any layer reasons about applicant status THEN the value SHALL come from a new canonical TypeScript enum `ApplicantStatus` defined in `backend/enum/teachers/applicant-status.enum.ts` with EXACTLY the members `Pending = "pending"`, `InEvaluation = "in_evaluation"`, `Failed = "failed"`, `Passed = "passed"` (matching the DBML note on `applicants.status`), plus a type guard `isApplicantStatus(value: unknown): value is ApplicantStatus`; the enum SHALL be re-exported from `backend/enum/teachers/index.ts` (and via the top-level barrel). NO raw string literals for status SHALL appear anywhere outside this file and its tests. NO `pgEnum`/DB migration: the column remains `varchar(50)` — structurally enforced by the "no schema drift" rule.
- **REQ-013 (Status State Machine Semantics — Contract)**: WHEN applicant status is written by any future flow THEN transitions SHALL follow: `pending → in_evaluation` (verification plan purchase — DEV2-005), `in_evaluation → passed | failed` (aggregation — DEV2-007), `failed → in_evaluation` (re-purchase after cooldown — DEV2-005 with this ticket's guard), `pending|in_evaluation|failed` are terminally readable; `passed → teacher row exists` (DEV2-007 writes, not this ticket). This state vocabulary SHALL be documented in the canonical doc (REQ-080) as binding on the Sprint-1 DEV2-005..DEV2-010 chain.
- **REQ-014 (Attempt Counter Contract)**: WHEN DEV2-005 needs to record a (re-)application THEN the repository SHALL expose `ApplicantRepository.recordVerificationAttempt(userId: number, tx?: DBTransaction)` executing a single atomic `UPDATE applicants SET verification_attempts = verification_attempts + 1, last_attempt_at = now() WHERE id = @id RETURNING *` (in-place increment, parameterized — NO SELECT-then-UPDATE read-modify-write), and the service contract `ApplicantLifecycleService.recordReapplication(userId, locale, tx?)` SHALL wrap it with failure-surfacing (NotFoundError if zero rows returned) and `logger.logDomainError` on domain rejections only.
- **REQ-015 (Cooldown State Contract)**: WHEN any layer evaluates whether an applicant may (re-)purchase a verification plan THEN the determination SHALL come exclusively from `applicants.cooldown_until` (INV-TV3 authoritative source): cooldown is active IF AND ONLY IF `cooldown_until IS NOT NULL AND cooldown_until > now()`; an expired or NULL cooldown permits purchase. The guard `ApplicantLifecycleService.assertCanPurchaseVerification(userId, locale, tx?)` SHALL throw `ValidationError` with a custom code (`APPLICANT_COOLDOWN_ACTIVE`) + localized message interpolating the expiry timestamp when blocked, and SHALL be a no-op otherwise. (Purchase execution itself is DEV2-005's scope; this ticket ships the guard + tests.)
- **REQ-016 (User-Level Suspension Bridge — Documented)**: The ticket-acceptance "failed applicant has cooldown_until set AND users.suspended applies" (INV-U2, Workflow 01) SHALL be documented as a two-source contract: re-purchase gating is ALWAYS read from `applicants.cooldown_until`; login/session-class gating is read from `users.suspended` governance fields (DEV2-001/DEV2-002 domain). DEV2-004 SHALL NOT write either of those user-governance fields (a failed-evaluation writer is DEV2-007/DEV2-008 scope); this ticket locks the read semantics only.
- **REQ-017 (My Applicant Profile Surface)**: WHEN an authenticated caller whose `users.role = teacher` calls the new `myApplicantProfile` query THEN the system SHALL return `ApplicantProfileReturnType` — `{ id, status (ApplicantStatus), verificationAttempts, lastAttemptAt, cooldownUntil, cooldownActive, canPurchaseVerification }` — resolved from the caller's `ctx.user.id` with server-computed booleans (`cooldownActive`, `canPurchaseVerification` evaluated against `now()` server-side). IF the caller's user has a `teacher` row (certified) or a governance-blocked/deleted account THEN the query SHALL resolve according to the documented precedence: governed accounts never reach the resolver (DEV2-002 fail-closed context); a certified teacher returns `null` (no active applicant file) — which the client renders as the "certified" state, never as an applicant state.
- **REQ-018 (Status Display Truthfulness)**: WHEN the frontend renders the applicant status dashboard card THEN a `Pending` applicant SHALL see translated pending copy, `InEvaluation` SHALL see in-evaluation copy with attempt count, `Failed` + active cooldown SHALL see the cooldown expiry date, `Failed` + expired cooldown SHALL see the re-application eligibility affordance, and at NO point SHALL an applicant see teacher-certified-only affordances (INV-TV1 honesty; UI evidence of B.7).

### 2.3 Security, Authorization & Tenancy

- **REQ-030 (BOLA / IDOR — Identity From Context Only)**: WHEN ANY applicant-read or lifecycle method executes THEN the target applicant SHALL be derived exclusively from the server-verified caller identity (`ctx.user.id`); the `myApplicantProfile` query SHALL accept ZERO arguments (no `userId`/`id` input may exist in the operation contract); a parent/student/admin caller attempting to read another user's applicant file has no parameter surface to attempt it.
- **REQ-031 (BFLA — Role Gate)**: WHEN `myApplicantProfile` is invoked THEN the Pothos `authScopes` SHALL be `{ role: [UserRole.Teacher] }` (Runtime RBAC from DEV2-002: unauthenticated → `UNAUTHORIZED` 401 semantics; authenticated non-teacher (`student`/`parent`/`admin`) → `FORBIDDEN` 403 semantics with the canonical localized deny). The query SHALL sit in the authenticated (NOT public) surface; it SHALL NOT grant any write path.
- **REQ-032 (BOPLA — No Write Surface)**: WHEN this ticket ships THEN there SHALL be NO mutation accepting applicant fields (`status`, `verificationAttempts`, `cooldownUntil`) from any client; all status/attempt writes are server-internal (`recordVerificationAttempt`, future aggregation). Closed `readonly` input/output shapes; no `{ ...input }` spread into any Drizzle call anywhere.
- **REQ-033 (No Privilege Escalation via Applicant State)**: WHEN any applicant-state change occurs THEN it SHALL NOT affect `teacher.is_approved`, `is_evaluator`, `is_online`, `subjects`, or `request_preference`; a `teacher` row exists only if DEV2-007/DEV2-008 or DEV3-018 (admin cold-start) creates it. The certification boundary (role≠certification, DEV2-002 REQ-023) is structurally preserved: nothing in this ticket can mint a teacher.
- **REQ-034 (Rate Limiting — Unchanged)**: WHEN the new authenticated query is called THEN no NEW rate-limit surface is added beyond the existing authenticated posture; the public `registerUser` rate-limit pattern (fail-open stub per DEV1-002/DEV1-003) remains entirely untouched.
- **REQ-035 (No Oracles in Deny)**: WHEN access to an applicant file is denied (role mismatch, governed account) THEN the response SHALL be the canonical localized deny — it SHALL NOT disclose whether the target user exists, whether an `applicants` row exists for them, or any owner-specific state (aligned with DEV3-002 error-oracle rules and DEV1-002 no-account-oracle pattern).

### 2.4 Atomicity, Concurrency & Data Integrity

- **REQ-040 (Registration Atomicity Is Inherited, Not Re-Opened)**: WHEN registration runs THEN the user + applicants inserts SHALL stay inside the existing DEV1-002 `withTransaction(outerTx)` (SAVEPOINT-aware) transaction; this ticket SHALL NOT refactor those transaction boundaries — it only locks them with tests proving zero residual rows on forced child-insert failure.
- **REQ-041 (tx Propagation)**: WHEN any repository method participates in this ticket's code THEN every repository/service call inside a transaction SHALL receive the same `tx` (optional `tx?: DBTransaction` as the last parameter per repo conventions); mixing `tx` writes with global-`db` reads inside flows is PROHIBITED.
- **REQ-042 (Attempt-Increment TOCTOU Elimination)**: WHEN `recordVerificationAttempt` executes THEN the increment SHALL be the single statement in REQ-014 (no read-then-write; the `verification_attempts` column value is NEVER first read into memory and incremented in the application); `RETURNING *` provides the row for audit/log use.
- **REQ-043 (Cooldown Evaluation Is Pure Read-Compute)**: WHEN `assertCanPurchaseVerification`/profile shaping executes THEN the logic SHALL be a single read followed by pure computation against a captured `now` — no write, no lock needed; concurrent re-purchase attempts at timestamp boundaries are resolved by DEV2-005's transactional purchase flow (this ticket's guard is advisory-at-its-isolation-level and documented as such in the canonical doc).
- **REQ-044 (No Prepared Statements for Writes / Reads follow repo rules)**: WHEN the writes in REQ-014 and the reads in REQ-017 are authored THEN no `sql.placeholder` prepared statements SHALL be used for the write (writes are out of prepared scope per `docs/drizzle/prepared-statements.md`); the single-PK read path MAY use the existing `queryDb(tx)` Neon-HTTP pattern only if `ApplicantRepository` already presents such a method to extend — otherwise a plain parameterized query is used; NO `inArray` + prepared combination anywhere.
- **REQ-045 (Schema Drift Prohibition)**: WHEN implementation completes THEN `git diff` for `backend/db/schema/**`, `backend/db/migration/**`, and `db/schema.dbml` SHALL be EMPTY for this ticket, and `bun validate:dbml` SHALL pass with zero new drift. (The `ApplicantStatus` TS enum deliberately has no `pgEnum` counterpart; the varchar contract note is already in DBML.)

### 2.5 Validation & Error Contracts

- **REQ-050 (DomainError Discipline)**: WHEN any failure surfaces from this ticket's code THEN it SHALL be a `DomainError` subclass propagated with `extensions.code` per `docs/graphql/domain-error-extensions-code.md`: `NotFoundError` (`APPLICANT_NOT_FOUND`) when no applicant file exists for a certified/governed edge, `ValidationError` with custom code `APPLICANT_COOLDOWN_ACTIVE` when REQ-015 blocks, and the canonical `FORBIDDEN`/`UNAUTHORIZED` codes from the DEV2-002 authScope layer. Plain `new Error(...)` is PROHIBITED.
- **REQ-051 (Localized-Only Messages)**: WHEN any message is produced THEN it SHALL resolve through compile-time i18n. New keys (in the EXISTING `errors` namespace plus a NEW `applicant` namespace where display labels are needed) SHALL be registered in `shared/locale/types/**` + BOTH `ar` and `en` implementations (and namespace registration per `shared/locale/AGENTS.md`/`shared/AGENTS.md` if a new namespace is introduced); hardcoded or `console.*`-logged strings are PROHIBITED.
- **REQ-052 (Expected-Rejection Logging)**: WHEN cooldown blocks or domain rejections fire THEN logging SHALL use `logger.logDomainError` (debug under `TEST_SERVER=1`, warn in production — never `console.*`), carrying structured context (`entity: "applicants"`, the user id, and the relevant code) without PII-heavy payloads.
- **REQ-053 (Silent-Path Prohibition for the Happy Path)**: WHEN a pending applicant with no cooldown views their profile or a valid re-application attempt proceeds THEN no error, warning, or swallowed `try/catch` SHALL be emitted — the flow is a first-class contract, not a suppressed side effect.

### 2.6 GraphQL & Frontend Contracts

- **REQ-060 (Query Contract)**: WHEN the GraphQL schema is built THEN a `myApplicantProfile: ApplicantProfile` query SHALL exist, NO-arguments, `authScopes: { role: [UserRole.Teacher] }` (plus DEV2-002's implicit `authenticated` boundary), nullable at the top level (`ApplicantProfile | null` — null = the caller is a certified teacher or has outgrown the applicant file); the `ApplicantProfile` Pothos object SHALL expose a **non-nullable `id`** (the applicant's shared-PK user id, for Apollo cache normalization) and the server-computed `cooldownActive` / `canPurchaseVerification` boolean fields alongside the persisted scalar columns.
- **REQ-061 (Enum Exposure & Codegen)**: WHEN `ApplicantStatus` is exposed on the object THEN it SHALL be registered ONCE as a Pothos enum in `backend/graphql/pothos/shared/enum.pothos.ts` via the **enum-object form** (`gqlSchemaBuilder.enumType(ApplicantStatus, { name: "ApplicantStatus" })`) from the canonical TS enum — NEVER a `values: [...]` literal registration per `backend/graphql/pothos/AGENTS.md`; afterward the agent SHALL run `bun run generate:gqlSchema && bun codegen` and commit the regenerated `schema.graphql` + `frontend/graphql/generated/**` in the same change set.
- **REQ-062 (Frontend Document & View)**: WHEN frontend code consumes this THEN the document SHALL be `myApplicantProfileQueryDocument` in `frontend/graphql/sharedDocuments/auth/` (or a new `teachers/` domain file per that barrel's conventions), imported via `gql` / `TypedDocumentNode` from `@apollo/client`, hooks from `@apollo/client/react`, `useQuery` ONLY (no `useLazyQuery`), and the selection set SHALL include `id`; the new view SHALL live under the teacher-applicant dashboard surface (a status card: state chip, attempt count, cooldown expiry display, re-application affordance gated on `canPurchaseVerification`) rendered in MUI v9 (styling exclusively via `sx`, colors via `theme.palette.*`, `*Outlined` icons, `React.SubmitEvent` discipline for any form).
- **REQ-063 (Per-Audience Rendering Contract)**: WHEN the dashboard card renders THEN: student / parent / admin / supervisor callers SHALL never reach it (server-side guard + role-gated document); a certified sheikh SHALL see the certified state, NOT an applicant card; an applicant SHALL see the truthful status per REQ-018 with ALL strings from `useAppTranslation(Translation.<Namespace>)` and correct RTL/Arabic rendering discipline.

### 2.7 Test Coverage

- **REQ-070 (DB Test Discipline)**: WHEN database tests run THEN every test SHALL execute inside `runInRollback`, pass `tx` to ALL repository/Drizzle calls (verifying actual param positions first), create entities ONLY via `entity-setup.ts` helpers (never seed data), and use the `expectRepoError` try/catch helper — `expect(...).rejects.toThrow()` inside `runInRollback` is PROHIBITED. 100% statement/branch coverage target on all NEW repository/service logic (`bun test --coverage`).
- **REQ-071 (Registration Contract Lock Tests)**: WHEN `backend/db/test/logic/auth/`` (or the appropriate existing registration test module) is extended THEN it SHALL permanently assert REQ-010/011: teacher registration → one users row + one applicants row (shared PK) + ZERO `teacher` rows (rowcount delta = 0) + exact defaults (`status='pending'`, `verification_attempts=0`, `last_attempt_at NULL`, `cooldown_until NULL`), plus the forced-child-insert-failure rollback proving zero residual rows.
- **REQ-072 (Lifecycle Guard Tests)**: WHEN the cooldown guard is exercised THEN the suite SHALL prove: `cooldownUntil IS NULL` → allowed; `cooldownUntil` in future → blocked with `APPLICANT_COOLDOWN_ACTIVE` + localized message (asserted via `expectRepoError` substring against the TRANSLATED message, never the raw key); `cooldownUntil` exactly `now` → allowed (boundary); `cooldownUntil` in past → allowed; missing applicant row → `APPLICANT_NOT_FOUND`; and `recordVerificationAttempt` is a single atomic increment (two sequential calls → attempts go 0→1→2, `last_attempt_at` monotonically advances), plus concurrent `Promise.allSettled` double-increment converging to attempts = 2 with no lost update.
- **REQ-073 (GraphQL Integration Matrix)**: WHEN GraphQL tests run (`setupTestServerLifecycle` + `testClient`) THEN they SHALL assert via `CombinedGraphQLErrors` / `expectMutationError`-class helpers: anonymous → `UNAUTHORIZED`; student/parent/admin → `FORBIDDEN`; applicant gets shape with all fields + `id`; certified teacher → `null`; and `extensions.code` matches `docs/graphql/domain-error-extensions-code.md` for every denial path.
- **REQ-074 (Component/Critical-View Tests)**: WHEN the status card renders THEN Happy DOM component tests SHALL verify against translation-preloaded labels (via `translation-preload.ts` + `readTranslation(handle, locale)` + `TestWrapper locale`) for every status branch in REQ-018 (pending, in-evaluation, failed-active-cooldown, failed-eligible, certified-null), with ZERO hardcoded Arabic/English strings in assertions.
- **REQ-075 (Security Tier Probes)**: WHEN the security tests run THEN they SHALL prove via monkey/fuzz-tier inputs: role escalation cannot be induced by crafted payloads (REQ-031/033), unknown `ApplicantStatus` values fail the guard closed (`isApplicantStatus` rejects non-member values incl. `%`, `_`, `\`, unicode/RTL strings), and there is NO operation by which a caller can pass an arbitrary foreign `userId` (REQ-030 no-parameter-surface proof).
- **REQ-076 (Deviation-from-Baseline Gate)**: WHEN the ticket completes THEN `outcome/` SHALL document tsgo/biome/lint counts versus the REQ-001 baseline with target zero new errors, and the final deferred-items gate SHALL show `grep -c "❌\|⚠️" deferred-items.md` = 0 (any forward items, e.g. purchase wiring for DEV2-005, MUST be resolvable entries targeted at the owning ticket — not ❌/⚠️ debt).

### 2.8 Documentation & Knowledge Gates

- **REQ-080 (Canonical Doc)**: WHEN implementation completes THEN a canonical reference doc SHALL be created at `docs/auth/applicant-lifecycle.md` (or `docs/teachers/applicant-lifecycle.md` if a `docs/teachers/` subdir is introduced by this ticket — the chosen location per domain-mapping conventions) containing: the applicant state machine (REQ-013), the cooldown/attempt contracts (REQ-014/015/016), the query contract + precedence rules (REQ-017), the "registration already ships in DEV1-002" grounding note, and the consumer guidance table for DEV2-005..DEV2-010 and DEV3-019.
- **REQ-081 (Invariant Anchoring & Invariant Addendum)**: WHEN the doc lands THEN it SHALL bind the ticket to `docs/specs/state-machine-invariants.md` INV-TV1..TV7 and B.6/B.7, and SHALL be referenced from `docs/auth/user-registration.md` (one-line link in its Applicant/lifecycle section). NO edits to the canonical invariant file's numbering are made by this ticket unless a documented gap is discovered (then the addendum is recorded in the doc's change section).
- **REQ-082 (AGENTS.md Propagation)**: WHEN knowledge propagation runs THEN: `backend/services/AGENTS.md` SHALL gain a one-line rule (applicant lifecycle service exists + doc reference); `backend/enum/AGENTS.md` SHALL gain the ApplicantStatus entry if the enum file conventions require a listing; root `AGENTS.md` Important References SHALL gain one line for the new canonical doc. AGENTS.md entries are rules/references only — no code, no implementation detail.
- **REQ-083 (Outcome Protocol)**: WHEN every task executes THEN the executor SHALL read all prior `outcome/` files first, write `outcome/<task-id>-outcome.md` afterward (summary, files changed + files deliberately NOT changed, verification outputs, cross-file dependencies), and update the task checkboxes; Phase 1.5 `@plan-review` SHALL be run and its outcome (`plan-review-R1.md`) SHALL exist before implementation begins.

---

## 3. System Decisions & State Machine Invariants Alignment

**Decision References (`docs/specs/open-decisions-and-gaps.md`)**:

| Decision | Alignment |
|---|---|
| **B.6 (Failed applicants table)** | The `applicants` table is THE lifecycle home; REQ-010/011 lock that registration lands here; REQ-013 defines its `status` vocabulary exactly as the DBML note specifies (`pending`, `in_evaluation`, `failed`, `passed`). |
| **B.7 (Teacher record only after verification)** | Central guarantee re-locked at REQ-010 (zero `teacher` rows at registration, permanently tested) and REQ-033 (no path in this ticket mints a `teacher` row). |
| **A.7 / B.15 (Governance fields on `users`)** | REQ-016 bridges: `users.suspended` remains the login/session-class gate owned by DEV2-001/002; DEV2-004's cooldown-read ALWAYS uses `applicants.cooldown_until` and never writes governance fields. |
| **A.8 / A.10 (session_type / session_intent)** | Forward compatibility noted in doc: DEV2-006's evaluation sessions (`teacher_evaluation` / `evaluation`) do not interact with this ticket's applicant surface; no session types introduced. |
| **B.8/C.2 (Generic `subscriptions.user_id`)** | Verified precondition: the future verification subscription binds to the applicant's `users.id` — DEV2-004's attempt/cooldown records key off the same user id, so no indirection mismatch exists. |
| **C.1 (`parent` role exists)** | REQ-031's role gate proves non-teacher callers (incl. parent) are rejected; no cross-role confusion surface. |
| **`docs/IDEMPOTENCY.md`** | REQ-014's single-statement atomic increment + REQ-010's 23505-email-constraint idempotency inheritance (via DEV1-002) satisfy the replay-safety contract without new keys. |
| **DEV1-002 registration canonical contract (`docs/auth/user-registration.md`)** | REQ-002 dependency guard + REQ-010 test-lock formalize reuse; REQ-081 adds a backward reference link. |
| **DEV2-002 RBAC canonical contract (`docs/auth/jwt-authentication-service.md`)** | REQ-031 composes the `role` scope; REQ-060 inherits 401/403 semantics; REQ-033 honors the "role ≠ certification" boundary. |

**State Machine & Lifecycle Invariants (`docs/specs/state-machine-invariants.md`)**:

| Invariant | How this ticket serves it |
|---|---|
| **INV-TV1** (certification only via evaluation or cold-start) | REQ-010/033 make "no certification shortcut" a permanently tested, structurally-enforced property of the applicant surface. |
| **INV-TV2** (5 distinct evaluators) | Not owned here (DEV2-006/007 aggregation scope); recorded as downstream consumer obligation in REQ-080 doc so the vocabulary (`in_evaluation`) is stable when it arrives. |
| **INV-TV3** (cooldown must fully expire before re-purchase) | REQ-015 ships the authoritative guard contract; REQ-072 tests all four boundary cases (null, future, exact-now, past) + missing-row behavior. |
| **INV-TV4** (30d Tajweed / 90d Hifz minimums) | Durations are SET by DEV2-008's failure writer; DEV2-004's guard is duration-agnostic (pure `cooldown_until` read), which is documented as the deliberate two-source-of-truth split — this doc states it so DEV2-008 does not re-litigate. |
| **INV-TV5** (Admin override supersedes) | Unaffected — no override surface exists in this ticket; admin mutations belong to DEV2-010 with audit_logs. |
| **INV-TV6** (failed applicant keeps student privileges) | REQ-016 + doc: after DEV2-009 conversion, the students record co-exists with `applicants`; DEV2-004 does not disturb the applicants row. |
| **INV-TV7** (`teacher_verification` stores levels) | Verified as DEV1-001 schema presence; untouched by this ticket. |

**Canonical Workflows (`docs/workflows/`)**: Workflow 01 (Teacher Verification) is the owner of every state transition this ticket conditions on; REQ-013's vocabulary mirrors its Registered → Pending_Evaluation → In_Evaluation → Evaluation_Complete → Qualified/Cooldown_*. Workflow 05 (Admin Governance) is NOT modified (no admin override surface). No other workflow is functionally touched.

**Architecture Standards**: Drizzle prepared-statements rule (REQ-044) per `docs/drizzle/prepared-statements.md`; Neon-HTTP read pattern (REQ-044) per repo AGENTS; DataLoader guidance (REQ-060 `id` presence) per `docs/graphql/dataloader-batching.md` — no list-returning loaders are introduced; error-code/i18n discipline (REQ-050..053) per `docs/graphql/domain-error-extensions-code.md` + `shared/locale/AGENTS.md`; quality/loop/sub-loop entitlements per `.agents/skills/spec-implementation/SKILL.md`.

---

## 4. Cross-Layer Traceability Matrix

| Requirement ID | Decision Ref / Invariant | Backend Service | GraphQL Mutation/Query | Frontend View | Test Coverage |
|---|---|---|---|---|---|
| REQ-001 | Spec-driven Phase 0 | — (baseline, outcome files) | — | — | `outcome/phase0-baseline-outcome.md` |
| REQ-002 | Open-decisions DEV1 infra awareness | Verify-only: `RegistrationService`, `ApplicantRepository`, DEV1-001 schema | — | — | Phase-1 guard outcome + plan-review gate |
| REQ-003 | i18n/enum compliance precedents | All touched modules | `myApplicantProfile` resolver | Status card + errors | tsgo gate; i18n locale parity (REQ-075 parity check) |
| REQ-004 | Canonical types discipline | `backend/types/teachers/applicant.types.ts` (+`ApplicantProfileReturnType`) | Pothos objects consume types | Codegen types only | tsgo; plan-review; review-types wave |
| REQ-010 | B.7; DEV1-002 REQ-013; FR-3.1 | Existing `RegistrationService` (verify; not modified) | `registerUser` (existing) | RegisterForm (unchanged) | REQ-071 registration-contract lock test |
| REQ-011 | B.6 | Existing `ApplicantRepository.create` (verify) | — | — | REQ-071 default-values assertions |
| REQ-012 | INV (vocabulary precedent per DBML note) | `backend/enum/teachers/applicant-status.enum.ts` (NEW) | `ApplicantStatusPothosEnum` registration (REQ-061) | Codegen enum consumers | Enum value/guard unit tests |
| REQ-013 | B.6; Workflow 01 | Documented; consumed by `ApplicantLifecycleService` signatures | — | — | Doc presence + transition-table contract recorded |
| REQ-014 | INV-TV3 (attempt accounting); IDEMPOTENCY | `ApplicantRepository.recordVerificationAttempt` + `ApplicantLifecycleService.recordReapplication` (NEW) | — (DEV2-005 consumes) | — | REQ-072 atomicity tests (sequential + concurrent `Promise.allSettled`) |
| REQ-015 | INV-TV3; INV-TV4 (read-source split) | `ApplicantLifecycleService.assertCanPurchaseVerification` (NEW) | — | — | REQ-072 boundary + localized-message substring tests |
| REQ-016 | A.7/B.15/B.15 bridge; INV-U2 | Read-only policy, doc-anchored | — | — | Doc/semantic-review assertion |
| REQ-017 | B.6; REQ-030 BOLA | `ApplicantLifecycleService.getMyApplicantProfile(ctx)` (NEW) | `myApplicantProfile` (NEW) | Applicant status card | REQ-073 integration matrix (role/亲和 states) |
| REQ-018 | INV-TV1; B.7 honesty | — | — | Status card branches | REQ-074 component tests per branch |
| REQ-030..035 | BOLA/BOPLA/BFLA/oracle-hygiene; DEV1-002/003 precedents | Context-derived identity; no write surface | AuthScopes + no-arg contract | No bypass path | REQ-075 fuzz + authorization-probe suite |
| REQ-040..045 | DEV1-002 atomicity; Drizzle rules; no-drift policy | tx propagation + single-statement updates | — | — | REQ-071/072 transaction-leg tests; `git diff` diff-empty assertion on schema dirs |
| REQ-050..053 | `docs/graphql/domain-error-extensions-code.md` | `NotFoundError` / custom-code `ValidationError` / `logger.logDomainError` | `extensions.code` assertions | Translated deny/error rendering | REQ-072/073 code assertions + i18n key-existence tests |
| REQ-060 | DataLoader/Apollo `id` rule; DEV2-002 | Pothos object registration | `myApplicantProfile` (authScopes `{ role: [UserRole.Teacher] }`) | — | REQ-073 schema test + role matrix |
| REQ-061 | Pothos enum registration CRITICAL RULE | `backend/graphql/pothos/shared/enum.pothos.ts` (+enum-object form) | SDL reflects enum; codegen re-run | Codegen enum import | Schema/codegen regen diff committed in same changeset |
| REQ-062..063 | Frontend/MUI v9 + codegen rules | — | Document `myApplicantProfileQueryDocument` | Teacher-applicant dashboard status card | REQ-074 Happy DOM component tests |
| REQ-070..076 | Repository/test layer rules | Tests: `backend/db/test/logic/auth/applicant-lifecycle.test.ts`, service tests | `testClient` GraphQL suite | Component suite | Full suites GREEN + `--coverage` + baseline diff |
| REQ-080..083 | Knowledge propagation protocol | `docs/auth/applicant-lifecycle.md`; AGENTS one-liners; `docs/auth/user-registration.md` cross-link | — | — | Doc exists; AGENTS updates; `grep -c ❌|⚠️` = 0 gate |

---

**End of Specification — DEV2-004.** Governing next step: Phase 1.5 — invoke `@plan-review` on the complete plan (specs.md + plan.md + trackable-tasks.md) before any implementation begins.消费
