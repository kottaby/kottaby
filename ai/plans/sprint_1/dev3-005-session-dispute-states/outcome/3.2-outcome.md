# Outcome — Task 3.2 (admin UI, R-109/R-111)

**Task ID:** 3.2 (implemented by the interrupted 3.1/3.2 run; verified, hardened, and suite-created in this round by Task 2-b + the orchestrator)
**Agent:** admin-disputes-suite subagent (partial — transport failure) · orchestrator (completion + verification)

## Delivered (verified line-by-line + live)

- **Page:** `app/(dashboard)/disputes/page.tsx` — admin-gated route following the `roleDashboardRoute`/`withPageAuth` pattern of `/users`//`/teachers`; nav item added (`frontend/views/dashboard/navItems.ts`).
- **Container:** `AdminDisputesContainer.tsx` — sticky count bar (honest `totalCount`), paged list (R-106 clamps mirrored, `ADMIN_DISPUTES_PAGE_SIZE`), always-on chrome with matrix body (loading skeleton / FORBIDDEN permission fallback / generic error / drained-queue empty state via shared `SessionsEmptyState` icon-circle variant / rows + pager), per-row resolve in-flight slot, ghost-page guard (resolving the LAST row of a trailing page steps back a page), single notice slot.
- **Row:** `AdminDisputeRow.tsx` — intent, verbatim fee, filed reason with expand/collapse clamp, created/disputed stamps, participant ids, resolve CTA (44px target).
- **Dialog:** `ResolveDisputeDialog.tsx` — Cancel/Complete radio (explicit choice required — submit disabled until chosen), optional note (500 cap, raw counter), trimmed-empty note → `null` on the wire, success cache convergence: ROOT_QUERY `adminDisputedSessions` filter (Reference `__ref` + raw-`id` dual branches) + honest totalCount decrement + mutation payload auto-merge; code-mapped arms (NOT_FOUND / INVALID_TRANSITION close with error snackbar; VALIDATION/masked keep the dialog open).

## Hardening applied this round

1. **Idempotence guard** in `removeSessionFromAdminQueue` — when nothing matches, the modifier returns the SAME `existing` reference (a fresh-object return on every re-invocation is the canonical cache-modify re-write footgun).
2. **Parameterless `handleResolved`** (unused param cleared TS6133) — mirrors the documented `handleSessionMissing` pattern.
3. **Unused type import removed** from `ResolveDisputeDialog.tsx` (TS6196).
4. **A11y:** resolve CTA Tooltip title = `t.resolveDisputeTitle` (action name) instead of the dialog body copy.
5. **Enum gate:** radio onChange compares the wire string against `DisputeResolution.Complete.toString()` (string-vs-string; oxlint no-unsafe-enum-comparison clean, whitelist semantics preserved).
6. **oxlint config:** `ResolveDisputeDialog.tsx` joins the file-scoped `__ref` exemption block (same load-bearing Apollo wire property as the two pre-existing members).

## Suite (created this round)

- `test/ui/components/admin/` — entry (two-phase bootstrap, mirroring student/teacher) + suite: **11 branches × 2 locales** — loading skeleton, FORBIDDEN fallback, masked generic error, drained-queue empty state, populated rows (chips/fee/reason/participants/stamps/honest count + resolve CTA pin), reason expand/collapse + per-row slot, arbitration dialog (radios, note counter, no-resolution submit gate, clean dismissal), resolve→Cancel success, `SESSION_INVALID_TRANSITION` error arm, VALIDATION dialog-stays-open arm, ghost-page step-back guard.
- **RUNNER-WEDGE skip-with-body (branches 8 + 11):** the resolve→SUCCESS convergence (MockLink resolution racing dialog unmount + ROOT_QUERY cache-filter write) dead-ends the Happy-DOM runner into a timer- AND microtask-starving allocation loop (OOM kill). Reproduced through BOTH `fireEvent.submit` and the click submitter, with `addTypename` on/off; a prototype-level counter trace showed ZERO cache.modify/broadcast activity before death (the wedge precedes the cache write; not an app-data defect). Bodies INTACT — one-line flip re-enables. **Compensating control:** the 4.1 real-browser loop arbitrates BOTH outcomes live (see 4.1 outcome) — both converged correctly.
- Gate: **23 tests · 19 pass / 4 skip / 0 fail · exit 0** (both locales).
