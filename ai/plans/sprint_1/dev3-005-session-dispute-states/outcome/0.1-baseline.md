# Baseline — DEV3-005 start-of-work gate snapshot

**Task ID:** 0.1 · **Agent:** orchestrator (recorded retroactively from worklog-evidenced numbers; the file was deferred by the interrupted cron rounds and is closed here for ledger completeness)

Captured BEFORE this round's test-completion work, from the most recent green runs recorded in `/home/z/my-project/worklog.md` (cron-r3-fix1 and cron-r3-be-close entries) plus the round's own first-hand runs where noted:

| Gate | Baseline (pre-3.1/3.2-test completion) |
|---|---|
| `bun run tsgo` | exit 0 (cron-r3-be-close, after its error-contract wire-probe fix) — REGRESSED to 2 errors by the interrupted 3.2 agent's leftovers (`AdminDisputesContainer` TS6133 `sessionId`, `ResolveDisputeDialog` TS6196 unused type); cleared in this plan's 3.2 close-out |
| biome | 16 backend porcelain files 0 diagnostics (cron-r3-be-close); frontend 3.1/3.2 diff unverified at baseline |
| oxlint | 0/0 on the 16 backend files (301 rules) |
| Student dir (`test/ui/components/student/`) | 21 pass / 4 skip / 0 fail / 209 expect (cron-r3-fix1) |
| Teacher dir (`test/ui/components/teachers/`) | 49 pass / 8 skip / 0 fail dir-level; entry alone 31 pass / 8 skip / 335 expect (cron-r3-fix1) |
| Admin dir | did NOT exist (created in 3.2 close-out) |
| Locale parity suite | green, pre-DEV3-005 key set (grown + re-verified in 3.1: 20 tests / 493 expect / 0 fail) |
| Backend battery (4-suite: lifecycle-mutations, schema-surface, session-sdl, error-contract-matrix) | 97 pass / 0 fail / 457 expect (cron-r3-be-close; re-verified 61 pass / 0 fail in 4.1 after the suite split counts settled) |
| DEV3-004 full battery | 370 pass / 0 fail (frozen close of the previous plan) |

## Deferred ledger seed

`deferred-items.md` seeded with **F1** (dispute actor-id columns + audit-log entries) and **F2** (post-completion dispute window) — both `⏸ Forward`, per the plan's pre-declaration.
