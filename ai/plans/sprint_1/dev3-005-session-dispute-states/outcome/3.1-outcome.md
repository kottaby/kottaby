# Outcome — Task 3.1 (frontend documents + student/teacher UI, R-109/R-110)

**Task ID:** 3.1 (completed across the cron-r3 interrupted run + 2-a + this orchestrator round)
**Agent:** interrupted 3.1 agent (implementation) · student-dispute-suite subagent (2-a, suite completion) · orchestrator (verification)

## Delivered (verified line-by-line + live)

- **Documents:** TypedDocumentNodes for `openSessionDispute` / `resolveSessionDispute` / `adminDisputedSessions` in `frontend/graphql/sharedDocuments/scheduling/session.documents.ts` (+ barrel); generated `gql/graphql.ts` + `schema.graphql` codegen-grown only.
- **Student UI (R-110):** `SessionRow` dispute CTA (disputable = `scheduled|started` only), DISPUTED chip (warning tokens, Record-lookup, vocabulary-stability pin), per-row in-flight slot book extended with the `dispute` kind (cron-r2 D9-bis mechanism), cancel CTA visible-but-DISABLED on disputed rows (dedicated `session-action-<id>-cancel-disabled` testid + tooltip), cancel-reason meta line on cancelled rows, `SessionDisputeConfirmDialog` (required reason, 500-char raw counter, REQUIRED gate blocks empty submit pre-wire, code-mapped snackbars, cache normalize — no refetch flicker). `StudentSessionsContainer` owns the dialog slot + snackbar chrome.
- **Teacher UI:** byte-identical dispute wiring via the reused dialog; container slot book extended (`start|complete|cancel|dispute`); teacher suite branches 20/21/22 (20 active, 21/22 D8 skip-with-body).
- **i18n (R-109):** sessions-namespace ar/en keys grown (openDispute, dispute dialog family, DISPUTED chip, disputeOpenedNotice, cancelDisabledDisputed, admin family); parity suite grown — **0 drift, 20 tests / 493 expect / 0 fail**.

## Test completion (2-a)

- Student suite dispute coverage landed as branches **6b** (active: dialog open, REQUIRED-reason gate blocks empty submit, clean dismissal, slot release), **6c** (D8 skip-with-body: typed counter → submit → success snackbar → chip flip → cancel CTA disabled), **6d** (D8 skip-with-body: `SESSION_INVALID_TRANSITION` → error snackbar, row stays). Renumbered from the originally drafted 7/8/8b to avoid colliding with the pre-existing hidden skips "branch 7 — cancel flow" / "branch 8 — SESSION_NOT_FOUND".
- Student dir gate: **31 tests · 23 pass / 8 skip / 0 fail · exit 0** (both locales).
- Teacher dir gate: **63 tests · 0 fail · exit 0** after the orchestrator fixed branch 5's action-visibility query (see 4.1 outcome).

## Deviations

- None against R-109/R-110. The 6b/6c/6d numbering (vs the plan's implicit 7/8) preserves the suite's unique-branch-label convention.
