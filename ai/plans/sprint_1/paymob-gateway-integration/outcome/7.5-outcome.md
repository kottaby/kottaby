# Task 7.5 — Funnel component tests (Happy DOM) — Outcome

**Task:** CREATE suites under `test/ui/components/` per `test/ui/AGENTS.md` for the three containers (mocked Apollo).
**Requirements:** REQ-004, REQ-075
**Executed:** 2026-09-12 (completes an interrupted prior pass)

---

## Summary

- The three per-container suites (created by tasks 7.2/7.3/7.4) were extended with the funnel-level coverage each prior task could not assert in isolation, and the new cross-container funnel-journey suite was landed. All four suites pass via the mandated runner across BOTH locales.
- **What the interrupted pass had already landed** (verified in the working tree, then repaired):
  - `PlansCatalogContainer.suite.tsx`: branch 13 (in-flight purchase busy CTA inside the dialog, via a `delay: Infinity` mutation mock) — repaired this pass (see fix cycle 4).
  - `PaymentResultContainer.suite.tsx`: branches 14–16 (cached-truth warm start → success on first paint; stale pending cache revalidating to the active arm; array-valued hint record refines nothing).
  - `MySubscriptionsContainer.suite.tsx`: branches 7–8 (verified-pending row — payment stamp SET — renders NO failed chip, pinning the failed-derivation boundary; cached-truth warm start renders the populated list on first paint).
  - `PlansCheckoutFunnelJourney.{test,suite}.tsx` (new files): the cross-container Apollo-cache interplay suite — journey 1 (the instant-activation mutation payload's normalized `StudentSubscription:901` write read back through the payment-result container's authoritative `mySubscriptions` re-query) and journey 2 (the catalog re-query converging on the same `Plan:<id>` entries across a fresh provider), plus a bootstrap self-check test.
- **Genuinely-missing coverage added this pass** (the 7.2/7.3 contract items no suite pinned):
  - **Dialog-cancel keeps the idempotency key** (7.2.TE bullet): new branch 8b in the catalog suite — open dialog → cancel (zero wire calls) → re-open → submit asserts exactly ONE key minted for the surface lifetime and that the cancel-survivor is the key the mutation carries.
  - **Mock-provider branch refetch** (7.2.TE bullet): new branch 11b in the catalog suite — an operation-name-capturing `ApolloLink` proves the `completed` outcome (`checkoutUrl: null`) fires a SECOND `PlanCatalog` operation after the purchase, and the grid stays settled after the refetch.
- **The interrupted pass's failing journey 1 repaired:** the original asserted a cache-read with `mocks: []`, but the container's `cache-and-network` revalidation then errors into the generic alert — the pending arm only survived on the first paint. The mock now supplies the payload's identical pending row as the revalidation truth (`maxUsageCount: Infinity`), so the journey pins a stable, settled funnel state across cache paint + network revalidation.
- No green arms were duplicated: the additions cover only contracts no existing branch asserted (the per-container arm matrices were left untouched).

## Files created

| File | Content |
|---|---|
| `test/ui/components/student/PlansCheckoutFunnelJourney.test.tsx` | Suite ENTRY — the two-phase Happy-DOM bootstrap (sequential top-level-await preloads → dynamic suite import) + a bootstrap self-check test pinning the preload contract |
| `test/ui/components/student/PlansCheckoutFunnelJourney.suite.tsx` | Suite BODY — the cross-container cache-interplay matrix (2 journeys × 2 locales + bootstrap) |

## Files modified

| File | Change |
|---|---|
| `test/ui/components/student/PlansCatalogContainer.suite.tsx` | Added branch 8b (cancel keeps key) + branch 11b (mock-provider refetch); extracted the shared `planBuyTestId` / `clickBuyWhenLive` / `nonNullKeys` helpers (killed an intra-file jscpd clone between branches 8/8b); removed the interrupted pass's dead funnel-journey exports (`FUNNEL_CATALOG_MOCK`, `FUNNEL_DIALOG_PLAN`, `funnelOpenDialogAndConfirm` — declared "consumed by the journey suite" but never imported) and the then-orphaned `confirmOpenDialogPurchase` helper; scrubbed plan-meta from docblocks |
| `test/ui/components/student/PaymentResultContainer.suite.tsx` | Scrubbed plan-meta from the funnel-coverage docblock (branches 14–16 landed by the interrupted pass, unchanged) |
| `test/ui/components/student/MySubscriptionsContainer.suite.tsx` | Scrubbed plan-meta from the funnel-coverage docblock (branches 7–8 landed by the interrupted pass, unchanged) |

## Verification results

| Check | Result | Evidence |
|---|---|---|
| 7.5.TE — journey suite | **5 pass / 0 fail**, 23 expect | `bun --env-file=.env.test run test/scripts/run-test.ts test/ui/components/student/PlansCheckoutFunnelJourney.test.tsx` |
| 7.5.TE — catalog suite | **29 pass / 0 fail / 1 skip (D8-class redirect)**, 163 expect | `bun --env-file=.env.test run test/scripts/run-test.ts test/ui/components/student/PlansCatalogContainer.test.tsx` |
| 7.5.TE — result suite | **33 pass / 0 fail**, 145 expect | `bun --env-file=.env.test run test/scripts/run-test.ts test/ui/components/student/PaymentResultContainer.test.tsx` |
| 7.5.TE — subscriptions suite | **17 pass / 0 fail**, 67 expect | `bun --env-file=.env.test run test/scripts/run-test.ts test/ui/components/student/MySubscriptionsContainer.test.tsx` |
| 7.5.QL sub-loop (`--lifecycle duplicates`) | **exit 0 on ALL 5 touched files** | tsgo → oxlint → biome → lint:type-aware → check:duplicates each ✅ |
| Plan-meta grep (`REQ-`, `7.2`, `7.3`, `7.4`, `task-owned`, `sprint_1`, `paymob-gateway`, `outcome/`) | zero matches in all 5 files | grep-verified after the scrub |

Env note: this worktree has no `.env` — every run went through `--env-file=.env.test` (the sub-loop's DB-pool import needs `DATABASE_URL`; no DB is ever touched by the lint/test chain).

### Sub-loop fix cycles (honest log)

1. Journey suite — tsgo: the payload literal's `status: "Pending"` string did not satisfy `MySubscriptionsQuery_mySubscriptions`; typed the row and lifted the enum value (`SubscriptionStatus.Pending`).
2. Journey suite — tsgo: the revalidation mock's return type; typed as `MockLink.MockedResponse`.
3. Journey suite — oxlint `no-unsafe-type-assertion` on the row literal; widened via a typed literal instead of `as`.
4. Catalog suite — tsgo: `operation.operationName` is `string | undefined`; coalesced to `""` in the capture link.
5. Catalog suite — jscpd intra-file clone (branches 8/8b shared a 10-line block); extracted `planBuyTestId` / `clickBuyWhenLive` / `nonNullKeys` helpers, re-wrote both arms on them.
6. Catalog suite — dead-code removal: the interrupted pass's funnel exports + orphaned dialog-confirm helper (the journey suite builds its own catalog render; no consumer existed).

## 7.5.SEC (security substance of the covered contracts)

- **Idempotency replay safety (7.2.TE):** branches 8 + 8b pin the client-side contract — one key minted per surface lifetime, carried ONLY as the `x-idempotency-key` context header (never the input DTO), kept across failed submits AND dialog cancels, rotated only on success. The capture link proves query reads never carry the header.
- **Mock-provider refetch (7.2.TE):** branch 11b proves the instant-activation branch re-queries the authoritative catalog rather than trusting the mutation payload for post-purchase availability.
- **Zero-trust result page (7.3.TE):** branches 8/9/16 (pre-existing) + 14–15 (this pass) pin that nothing positive renders without server truth — including under cache warm starts and array-valued (multi-value `searchParams`) hint records.
- **Tenancy (7.4.TE):** the subscriptions branches send zero arguments; the verified-pending arm (branch 7) pins the failed-derivation boundary to the row's own unset verification stamp.

## 7.5.SR (semantic review)

- No dead code: the interrupted pass's never-imported funnel exports and the orphaned `confirmOpenDialogPurchase` helper were removed; every helper in all five files has live consumers.
- No plan-meta in code: grep-pinned zero (REQ ids, task ids, plan paths, outcome paths) across all five files.
- No duplicate green arms: additions cover only unpinned contracts; the per-container matrices are byte-unchanged apart from the docblock scrub.
- Translation discipline: all assertions resolve labels through `Checkout.getLabels(getTranslations(locale))` — zero hardcoded copy; fixture data (ids, timestamps, statuses) is test DATA.
- Test data only in test files: the cache-row fixtures live in the suites; no production component or store was touched.
- The journey suite carries no `useLazyQuery`, no `console.*`, no `oxlint-disable`; `bun:test` + `@testing-library/react` + `@apollo/client/testing/react` only.

## 7.5.IV (instruction verification)

Rule files honored: root `AGENTS.md`, `test/ui/AGENTS.md` (component-test conventions: preloads chain, `renderWithWrapper`, no-real-server, translation rules, no hardcoded strings), `.agents/instructions/tests.instructions.md` + `frontend.instructions.md` (as printed by the sub-loops), plus the layer AGENTS.md files read for the consumed view/document surfaces (`frontend/AGENTS.md`, `frontend/views/AGENTS.md`, `frontend/graphql/AGENTS.md`, `frontend/graphql/sharedDocuments/AGENTS.md`, `shared/AGENTS.md`, `shared/locale/AGENTS.md`). The suite split follows the established two-phase bootstrap `*.test.tsx` + `*.suite.tsx` pattern of the sibling suites; the mandated `run-test.ts` runner was used for every execution.

## Carry-forward knowledge

- **Journey 1's mock requirement:** a `cache-and-network` container under `MockedProvider` MUST have a matching mock even when the cache is pre-populated — with `mocks: []` the revalidation errors into the generic-error branch after the cache paint. Any future cross-container cache-warm arm needs the settled revalidation row in the mock list.
- **jscpd granularity:** the intra-file clone threshold (`minTokens: 75`) trips on ~10 shared test lines; the established fix is extracting per-file test helpers (this pass: `planBuyTestId` / `clickBuyWhenLive` / `nonNullKeys`), never `jscpd:ignore`.
- **The skipped D8-class redirect arm** (catalog branch 10) remains skip-with-body — the funnel's hosted-checkout redirect is compensated by the real-browser QA loop, per the wallet-suite posture.
- **9.1 note:** no new deferred rows; the funnel suites run in ~9 s total wall clock across the four files (the result suite's ~1.6 s/branch cache-and-network waits dominate — expected, not a regression).
