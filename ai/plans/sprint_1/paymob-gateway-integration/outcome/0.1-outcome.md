# Task 0.1 Outcome — Phase 0: Pre-Implementation Baseline

**Task:** Record tsgo/biome/lint baselines; confirm the landed subscription-purchase seams' shapes; re-confirm every `path:line` cited in `specs.md` §1.
**Requirements:** REQ-001
**Executed:** 2026-09-07 (two passes — pass 2 resumed after a sandbox tool-session outage; no source files modified at any point)

---

## Summary

- Baselines captured with **read-only** commands (biome run WITHOUT `--write`): **tsgo 0 errors · biome 0 warnings/0 errors · lint full-repo success (no OOM)**. The working tree was clean (`git diff --name-only` = `worklog.md` only, the session-start worklog entry; stash empty).
- All amendments A1–A6 key off seams that are **confirmed present in the tree exactly as the plan predicts**: the A-set is pending (nothing applied yet), which is the correct pre-implementation state.
- **20 path:line anchor groups** from `specs.md` §1 re-verified: **12 OK · 7 DRIFT (all ≤ +22 lines; content present) · 1 MISSING** — the sibling subscription-purchase plan directory was **archived to `ai/finished_plans/sprint_1/subscription-purchase-payment-gateway/`** (pruned to `plan.md`/`specs.md`/`tasks.md`), so its `prototype/` dir and `deferred-items.md:45` anchor no longer resolve at the cited paths. Non-blocking: this plan's own `prototype/` (15 PNGs + `screens.json`) is the live funnel-state anchor, and the archived plan's `specs.md:76`/`:120` still resolve verbatim at the moved path.
- Two sandbox anomalies handled: HEAD had been reset to `main` (re-checked out to `feat/paymob-gateway-integration` — both at `a63c0a7`, content no-op), and a mid-task tool outage ended pass 1 (baselines survived in `/tmp`; pass 2 completed seams + citations + this file).

## Baseline counts

| Gate | Command | Result | Evidence |
|---|---|---|---|
| tsgo | `bun tsgo` (`tsgo -b --noEmit`, whole repo) | **exit 0 — 0 × `error TS`** | `/tmp/baseline-tsgo.txt` (count), `/tmp/baseline-tsgo-full.txt` (raw) |
| biome | `bunx @biomejs/biome check .` — read-only, NO `--write` (confirmed "No fixes applied") | **exit 0 — 0 warn, 0 error (1775 files)** | `/tmp/baseline-biome.txt` |
| lint | `bun run lint --json --id baseline` | **exit 0 — `success:true`, empty output, scope full-repo, 105.9 s; did NOT OOM this session** | `/tmp/baseline-lint.json` |
| git | `git stash list` / `git diff --name-only` / `git status --short` | **stash empty; diff = `worklog.md` only** (expected session-start worklog entry — not a source mutation) | `/tmp/baseline-stash.txt`, `/tmp/baseline-files.txt` |

Any future nonzero from tsgo/biome/lint is a regression introduced by this plan (subject to the lint-service OOM caveat under "Pre-existing issues").

## Seam verification table (amendments key off these)

| Seam | Confirmed shape / amendment-relevant fact |
|---|---|
| `backend/types/billing/payment-gateway.types.ts` | `PaymentCheckoutInput = {studentId, planId, amount, currency}` — **no `specialReference`/`billing`** (A1 pending) · `PaymentWebhookEvent = {reference, outcome: "confirmed"\|"failed", amount, currency}` — **no `providerTransactionId`** (A2 pending) · `PaymentGatewayPort.parseWebhookEvent(rawBody: string): PaymentWebhookEvent` — current signature takes raw body only and returns **non-null** (A3 replaces with `parseWebhookEvent(input: WebhookParseInput): PaymentWebhookEvent \| null`) · `PaymentCheckoutSession = {provider, providerReference, checkoutUrl: string \| null}` |
| `backend/services/billing/payment-gateway/` | `payment-gateway.factory.ts` — `getPaymentGateway(locale?)` lazy singleton; `GATEWAY_ADAPTERS` registry (own-property-guarded) keyed on provider value; `resetPaymentGateway()` **already calls `resetEnvironmentCache()`** (A5 invalidation parity is free); `ValidationError("PAYMENT_GATEWAY_UNSUPPORTED")` fail-closed. `mock-payment-gateway.adapter.ts` + `webhook-signature.helpers.ts` (+ tests) present. **No `paymob/` subdirectory yet** (Phase 3 creates it) |
| `app/api/payments/webhook/route.ts` | POST-only export · kill switch `isPaymentWebhookEnabled()` → **bare 404** (`ENDPOINT_GONE_STATUS`, `:73`; check `:293-295`) · `MAX_PAYMENT_WEBHOOK_BODY_BYTES = 64_000` (`:76`) enforced 3 ways (Content-Length pre-check, incremental byte-budget read, per-read 30 s + total 60 s deadlines) · mock-branch signature gate: `x-payment-signature` header, HMAC-SHA256 via `verifyWebhookSignature`, masked 401 · masked envelopes: success `{ data: { processed, replayed? }, requestId }`; replay acks 200; quarantined/unknown-ref acks `{processed:false}` 200; oversize/unreadable → masked 400 · activation call (`:344-345`): `getPaymentGateway(ENVELOPE_LOCALE).parseWebhookEvent(rawBody)` → `await SubscriptionActivationService.processWebhookEvent(event, ENVELOPE_LOCALE)` · route test: `app/api/payments/webhook/test/payments-webhook-route.test.ts` |
| `backend/lib/gateway/route-inventory.ts` | Row `{ path: "/api/payments/webhook", classification: "provider-ack-exempt" }` at **`:65` EXACT** · `/api/cron/sweep-sessions` registered at `:61` (`envelope`) — **plan-review F7's stale-inventory/A4 failure is RESOLVED** · `RouteClassification` union (incl. `provider-ack-exempt`) at `:40` (specs cites `:33`) · inventory now lists FIVE routes (graphql, set-locale, health, sweep-sessions, payments/webhook) |
| `backend/db/repo/billing/student-payment.repository.ts` | Namespace `StudentPaymentRepository` with exactly: `insertPayment`, `findBySubscriptionId`, `markPaidOnce`, `markFailedOnce` — **NO `findStalePendingByGateway`** (A4 pending) · conventions for A4 to follow: non-tx reads = raw parameterized SQL via `queryDb` with frozen `PAYMENT_READ_COLUMNS` camelCase-aliased projection (`:51-55`); tx reads = Drizzle select (`isDBTransaction` guard); guarded single-UPDATE decision writes with `status = 'pending'` predicate as the concurrency lock; optional `tx?: DBTransaction` last param |
| `backend/db/schema/billing/student-payments.ts` | Columns: `id` (identity PK), `studentId` (FK restrict), `subscriptionId` (FK set-null, nullable), `amount decimal(10,2)`, `currency char(3) default EGP`, `paymentGateway` pgEnum, `status` pgEnum default pending, `createdAt`, `updatedAt ($onUpdate)` · CHECK `student_payments_amount_check` · indexes on student_id/subscription_id · **NO `providerTransactionId`** (REQ-031/A2 pending) · docblock documents the trigger-amended guarded transition (`4-student-payments-status-transition.sql`) |
| `backend/db/migration/` | `1-extensions`, `2-functions`, `3-immutability-triggers{,-sqlite}`, `4-student-payments-status-transition{,-sqlite}`, `rollback-down` all present · **Drizzle mirror has TWO dirs for migration 4**: `20260907182426_custom_4-…` and `20260908103411_custom_4-…` (the latter is what `bun db migrate` applied). Migration 5 (`5-student-payments-provider-transaction{,-sqlite}.sql`) does NOT exist yet (task 2.2) |
| `backend/services/billing/subscription-activation.service.ts` | `SubscriptionActivationService.processWebhookEvent(event: PaymentWebhookEvent, locale: string, outerTx?: DBTransaction): Promise<{ processed: boolean; replayed?: boolean }>` (`:486-490`) · resolves subscription via `SubscriptionRepository.findByPaymentReference(event.reference, …)` (`:494`) · settlement quarantine compares stored amount/currency (`:519`) · success path: guarded `activatePendingOnce` → `markPaidOnce` → `creditLaneBalance` → in-tx `NotificationEngine.emitForUser` (recipient-locale copy) → `publishReceipts` post-commit (`:418-425`) · notification is **keyless** (`emitForUser` called with no idempotency key; dedupe owned by the guarded activation) — task 4.2 adds `payment:<providerTransactionId>:confirmation` · **failure path TODAY: `failPayment` (`:434-442`) does `markFailedOnce` ONLY — NO notification on failure** ("no credit, no notification", docblock `:44-47`) — **A6 amends this** · `subscription-purchase.service.ts` (+ tests) exists alongside |
| `backend/enum/billing/payment-gateway.enum.ts` | `PaymentGateway.Paymob = "paymob"` at **`:13`**; enum also carries the landed `Mock = "mock"` member (`:19`) — research-00's "no mock member" is stale |
| GraphQL surface | `planCatalog` query field registered at `backend/graphql/query/plan-catalog.query.ts:18` (authenticated scope) · `purchaseSubscription` mutation at `backend/graphql/mutation/subscription-purchase.mutation.ts:51` · `mySubscriptions` query at `backend/graphql/query/subscription.query.ts:42` — all landed; Phase 7 codegen gate is real |
| `frontend/views/dashboard/nav/navItems.ts` | Student nav entry `{ route: "/subscriptions", labelKey: "subscriptions", Icon: SubscriptionsIcon }` at **`:120`** (specs cites `:112` — dead catch-all link still live, task 7.4 takes it over) |
| `frontend/lib/auth/withPageAuth.ts` | `export async function withPageAuth` spans **`:67-105` EXACT** — anonymous → `/login?redirect=<path>`; role mismatch → `roleDashboardPath(role)`; returns `{userId, user, role}` |
| `app/api/cron/sweep-sessions/route.ts` | GET-only export · `bearerSecretMatches` at **`:66-73` EXACT** (SHA-256 digest both sides → `timingSafeEqual`, length-agnostic) · mode gates (`CRON_EXECUTION_MODE=external` AND `CRON_EXTERNAL_ENABLED=true`) → bare 404 · `apiSuccessResponse`/`apiErrorResponse`/`resolveRequestId` from `@/backend/lib/api`, `envelopeLocale = "en"`, `getEnv("CRON_SECRET")` ad-hoc read — the exact template for task 5.2 |
| `backend/lib/env.ts` | `EnvironmentConfig` at `:196` (has `paymentGatewayProvider` default `"mock"`, `paymentWebhookSecret`, `paymentWebhookEnabled` — parsed in `readEnvironment()` at `:252-280`) · `getEnvironmentConfig()` `:287` · `resetEnvironmentCache()` `:301` · escape hatches `getEnv` `:308` / `optionalEnv` `:315` / `requireEnv` `:325` · typed getters `:344-441` incl. `getPaymentGatewayProvider()` `:422` · **NO `paymob`/`ngrok` fields or `PAYMOB_*`/`NGROK_*` parsing anywhere** (A5/task 2.1 pending) |
| `backend/types/billing/index.ts` | Barrel lists all 7 sibling type files — task 2.3 must add `paymob.types.ts` + `callback-channel.types.ts` here |
| Docs | `docs/billing/subscription-purchase.md` EXISTS (canonical contract) · `docs/graphql/error-handling-contract.md:101` holds the webhook exemption row · `docs/testing/workflow-journey-tests.md` EXISTS |

## path:line re-confirmation (specs.md §1 ground-truth table)

**Tally: 20 anchor groups → 12 OK · 7 DRIFT · 1 MISSING** (all drift ≤ +22 lines; content verified at the new location in every DRIFT case)

| # | Citation | Status | Note |
|---|---|---|---|
| 1 | `backend/types/billing/payment-gateway.types.ts:12-17,26-30,41-46,55-58` | **OK** | All four interfaces land exactly on the cited lines |
| 2 | `backend/services/billing/payment-gateway/mock-payment-gateway.adapter.ts` + `payment-gateway.factory.ts`; `backend/lib/env.ts` ~`:229-278`, getters `~:416+` | **OK** | Files exist; payment keys parse `:276-278`; getters `:422/:431/:441` (~+6) |
| 3 | `backend/services/billing/subscription-purchase.service.ts`; `backend/graphql/mutation/subscription-purchase.mutation.ts`; `backend/graphql/query/subscription.query.ts`; `docs/billing/subscription-purchase.md` | **OK** | All four exist (mutation field `:51`, query field `:42`) |
| 4 | `app/api/payments/webhook/route.ts`; `backend/lib/gateway/route-inventory.ts:65` | **OK** | Registry row at `:65` EXACT; exemption row at `docs/graphql/error-handling-contract.md:101` |
| 5 | `backend/db/schema/billing/student-payments.ts:23-48` | **DRIFT** | Table def now `:34-59` (columns `:36-52`); same columns, `+11` |
| 6 | `backend/db/migration/3-immutability-triggers.sql:59-77` | **OK** | `prevent_student_payments_update` `:58-64`, UPDATE trigger `:73-77` |
| 7 | `backend/db/schema/billing/subscriptions.ts:33` | **DRIFT** | `paymentReference: varchar(…, 255)` now at `:42` (`+9`); partial unique index `:53-55` |
| 8 | `backend/enum/billing/payment-gateway.enum.ts:10` | **DRIFT** | `Paymob` now `:13` (`+3`) — enum grew the landed `Mock` member (`:19`) |
| 9 | `backend/db/schema/enums.ts:35-44` | **OK** | `paymentGateway` pgEnum at `:35`; `"paymob"` `:38`; `paymentStatus` `:29` |
| 10 | `backend/enum/billing/payment-status.enum.ts:5-10` | **OK** | Members `pending/paid/failed/refunded`, exactly as cited |
| 11 | `backend/services/notifications/notification-engine.service.ts:42,79,116` | **OK** | `emitForUser` `:42`, `emitForUsers` `:79`, `publishReceipts` `:116` — all exact |
| 12 | `backend/enum/notifications/notification-type.enum.ts:11` | **OK** | `PaymentConfirmation = "payment_confirmation"` at `:11` exact |
| 13 | `backend/lib/gateway/route-inventory.ts:33` | **DRIFT** | `RouteClassification` union (incl. `provider-ack-exempt`) now `:40` (`+7`) |
| 14 | `app/api/cron/sweep-sessions/route.ts:66-73` | **OK** | `bearerSecretMatches` exactly `:66-73` |
| 15 | `backend/lib/env.ts` — interface `:196`, `readEnvironment()` `:233`, getters `:322+`, `resetEnvironmentCache()` `:279`, `getEnv/optionalEnv/requireEnv` `:286/293/303` | **DRIFT** | `:196` exact; others `+19…+22` (`readEnvironment`→252, reset→301, getEnv→308, optionalEnv→315, requireEnv→325, getters→344+) — content verified |
| 16 | `backend/db/schema/users/users.ts:17` | **OK** | `phone: varchar("phone", { length: 20 })` at `:17` exact |
| 17 | `frontend/views/dashboard/nav/navItems.ts:112` | **DRIFT** | `/subscriptions` entry now `:120` (`+8`) |
| 18 | `backend/graphql/query/plan-catalog.query.ts:18` | **OK** | `queryField("planCatalog", …)` at `:18` exact |
| 19 | `.agents/skills/paymob-payments/references/` (119 pages) | **OK** | 119 markdown files on disk; `hmac/hmac-transaction-callback.md` + `hmac/hmac-for-card-tokens.md` + `intention-apis/create-intention.md` all present |
| 20 | `ai/plans/sprint_1/subscription-purchase-payment-gateway/prototype/` (12 screens) | **MISSING** | Sibling plan dir archived to `ai/finished_plans/sprint_1/subscription-purchase-payment-gateway/` — archive holds ONLY `plan.md`/`specs.md`/`tasks.md`; `prototype/` pruned. Live funnel-state anchor is THIS plan's `prototype/` (15 PNGs + `screens.json`, incl. `paymob-unified-checkout-*`, `payment-result-*`, `my-subscriptions-*`, `student-plan-catalog-default-*`) |

**Adjacent doc-info anchors (same integrity class):** `ai/plans/sprint_1/subscription-purchase-payment-gateway/deferred-items.md:45` → **MISSING** (file pruned from the archive) · archived `…/specs.md:76` (REQ-017) and `:120` (REQ-064) → **resolve verbatim at `ai/finished_plans/…`** · `docs/planning/SPRINT_PLAN.md:161` → **OK** (the "integrate real gateway in Sprint 2" risk row).

## Pre-existing issues to ignore during later review

1. **lint-service memory ceiling** — a prior session observed full `quality-gate`/`lint-service` OOM (SIGABRT, 4 GB sandbox). This session's standalone full-repo lint completed clean in 105.9 s; treat any lint crash (vs. findings) as environmental, not a plan regression.
2. **Two drizzle mirror dirs for migration 4** (`20260907182426_custom_4-…` and `20260908103411_custom_4-…`; the latter applied). Pre-existing bookkeeping; task 2.2 should keep migration-5 mirror naming in sequence and not "fix" the duplicate.
3. **specs §1 line drift** — `payment-gateway.enum.ts:10`→`13`; `env.ts` block `+19…+22`; `subscriptions.ts:33`→`42`; `student-payments.ts:23-48`→`34-59`; `route-inventory.ts:33`→`40`; `navItems.ts:112`→`120`. All content-verified; fix opportunistically in a later docs touch.
4. **Archived sibling plan paths** — citations to `ai/plans/sprint_1/subscription-purchase-payment-gateway/…` resolve (if at all) under `ai/finished_plans/sprint_1/…`; `deferred-items.md:45` and `prototype/` are gone from the tree. Do not "restore" them; this plan's own prototype + `docs/billing/subscription-purchase.md` are the live anchors.
5. **plan-review F7 (A4 static-assertion failure) is RESOLVED** — re-verified this session: `backend/lib/gateway/static-assertions.test.ts` → **17 pass / 0 fail** (sweep-sessions + webhook rows registered).
6. **HEAD reset to `main`** happened between sessions (sandbox behavior). `feat/paymob-gateway-integration` and `main` both point at `a63c0a7`; re-checkout is a content no-op but MUST be done before any implementation commit (never commit to `main`).
7. **Prior-session OOM note stands for `bun quality-gate`** only; per-file gates (`sub-loop.ts`, tsgo, biome, targeted `run-test.ts`) all ran clean in-sandbox.

## Carry-forward knowledge

- A1/A2/A3 are still pending in `payment-gateway.types.ts` — the port is exactly the mock-shaped seam the plan promises; the first writer of task 2.3 applies all three atomically (types-only file, barrel `backend/types/billing/index.ts` must gain `paymob.types.ts` + `callback-channel.types.ts` exports).
- `resetPaymentGateway()` already invalidates the env cache, so A5's `getPaymobConfig()` getter needs no new invalidation wiring — just parse into `EnvironmentConfig` in `readEnvironment()` (`:252-280`) and add the getter next to `getPaymentGatewayProvider()` (`:422`).
- The webhook route is the mock branch's contract holder: `MAX_PAYMENT_WEBHOOK_BODY_BYTES = 64_000` (not 65536), masked 400/401 envelopes, `{ processed, replayed? }` ack body — the paymob branch (task 4.1) must preserve these surfaces and add its own 404-per-branch gate (REQ-041 ruling).
- `SubscriptionActivationService.processWebhookEvent(event, locale, outerTx?)` is the ONLY activation entry point (route calls it with `ENVELOPE_LOCALE`); A6's failure-notification change goes in `failPayment` (`:434-442`), which today emits nothing. The success notification is deliberately KEYLESS — adding the `payment:<providerTransactionId>:confirmation` idempotency key (task 4.2) changes that design decision and should be recorded in the A-set/ledger when it lands.
- A4's repo method must follow this repo's frozen conventions: `queryDb` + frozen aliased column projection for non-tx reads, Drizzle select on tx, `payment_gateway`/`status`/`created_at` predicates, `tx?: DBQueryExecutor` read shape (see `findBySubscriptionId`).
- Repo is keyed on `subscriptionId` everywhere; there is NO lookup by `payment_gateway`/`status`/date today — `findStalePendingByGateway` is genuinely new query surface (and the sweep needs the subscriptions join for `merchant_order_id` ⇔ `payment_reference`).
- Schema drift check for task 2.2: `student_payments` has NO `providerTransactionId`; the guarded-transition trigger pair (migration 4) is the layer migration 5 amends; mirror-dir convention is `<timestamp>_custom_<name>`.
- Static assertions (A4 disk↔registry walk) are green: any NEW route file (task 5.2 cron route) MUST add its `ROUTE_INVENTORY` row in the same change set or CI fails.
- `PaymentGateway.Mock` exists as enum member `:19` — the mock branch's dispatch stays after provider ≠ paymob; enum value imports (never string literals) per REQ-005.
- Test-runner discipline verified working in-sandbox: `bun run test/scripts/run-test.ts <file>` is the DB/service lane (used for static-assertions here); lint JSON metrics interpret `fileCount: 0` as "full-repo scope", not "zero files".
- `/tmp` baseline evidence (`baseline-tsgo*`, `baseline-biome`, `baseline-lint.json`, `baseline-stash`, `baseline-files`) is reusable for the task 9.1 final-gate comparison — do not re-run tsgo unnecessarily (10+ min).

## Cross-file dependencies discovered

- `payment-gateway.factory.ts` ↔ `backend/lib/env.ts` (`getPaymentGatewayProvider`, `resetEnvironmentCache`) — A5 extends this exact edge.
- `app/api/payments/webhook/route.ts` ↔ `payment-gateway.factory.ts` (`getPaymentGateway`) ↔ `subscription-activation.service.ts` (`processWebhookEvent`) ↔ `student-payment.repository.ts` (`markPaidOnce`/`markFailedOnce`) ↔ `backend/db/migration/4-…` trigger contract — the paymob branch + A6 + A4 all touch spokes of this wheel.
- `route-inventory.ts:65` ↔ `docs/graphql/error-handling-contract.md:101` ↔ `static-assertions.test.ts` — task 4.1 verifies all three stay consistent when the paymob branch lands.
- `subscriptions.payment_reference` (`:42`) ⇔ Paymob `merchant_order_id` ⇔ `special_reference` — REQ-014/A1's correlation key threads schema ↔ adapter ↔ callback.
- This plan's `prototype/screens.json` (15 screens) supersedes the archived sibling prototype as the funnel-state source for Phases 6–7.

## Files NOT modified (and why)

This task is read-only verification + baseline capture. Zero source files touched; the only working-tree change is this outcome file, the `tasks.md` checkbox, and the `worklog.md` append. Baseline commands used read-only variants (biome without `--write` — "No fixes applied" confirmed in output).
