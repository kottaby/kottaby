# Task 9.2 — Knowledge propagation — Outcome

**Date:** 2026-09-13 · **Scope:** docs-only (the plan's LAST task) · **Verification:** content review per the repo's docs-only precedent (no markdown linter exists in the repo; every claim re-checked line-by-line against the landed code and outcome files).

---

## Summary

Wrote the canonical gateway documentation and closed both escalated doc-hygiene adjudications
(D-611, D-515) plus the knowledge-propagation pointers. The new `docs/billing/paymob-gateway.md`
consolidates everything an operator or future contributor needs: the vendor endpoint surface as
landed, the HMAC verification discipline, the env matrix with fail-closed semantics, the
callback-channel architecture (real/ngrok/simulation factory + `notification_url` gating), the
purchase→webhook→activation flow with replay/idempotency invariants, dashboard setup, the
test-credential runbook, reconciliation sweep semantics, troubleshooting, and the
operator-facing-error i18n posture from the mid-point review.

## Files created

| File | Content |
|---|---|
| `docs/billing/paymob-gateway.md` | The canonical Paymob gateway reference (12 sections, 2 Mermaid diagrams) |

## Files modified

| File | Change |
|---|---|
| `AGENTS.md` (root) | One-line **Important Reference** to `docs/billing/paymob-gateway.md` (below the Essential Commands block — smallest possible diff) |
| `backend/services/AGENTS.md` | One minimal rule line: consult `docs/billing/paymob-gateway.md` before touching `backend/services/billing/payment-gateway/` |
| `shared/AGENTS.md` | D-611 rewrite: Translation System File Structure (legacy `serverLegacy`/`clientLegacy`/`MessageSchema` entries → the live `server.ts`/`server-graphql.ts`/`server-cookies.ts`/`client/`/`namespaces/` layout), Namespace Registration steps 1–6 rewritten to the LIVE compile-time handle architecture (types/<ns> interface → en/ar leaves → namespaces/<ns>/<ns>.namespace.ts handle + registry → `Translations` member in `types/message.ts` → en/ar messages bundle registration → parity test + tsgo; the wallet/plans/checkout namespaces are the live exemplars), and the stale string-call examples fixed to the live idioms (`getTranslations(locale).authTranslations`, `useAppTranslation(Auth)` with the namespace HANDLE, `ctx.t("errorsTranslations")`, `getServerTranslations(locale).errorsTranslations`) |
| `docs/graphql/error-handling-contract.md` | D-515: one-sentence discoverability note after the exemptions inventory naming BOTH cron routes' bare-404 gates (disabled cron mode; unconfigured provider/API key) and stating they are deliberately documented on the routes under their `envelope` classification — no inventory row added (the existing wording does not warrant one) |
| `ai/plans/sprint_1/paymob-gateway-integration/deferred-items.md` | D-611 → ✅ Done (9.2, with the rewrite reference); D-515 → ✅ Done (9.2, with the note reference); A-set section: consumed-status sentence added under the intro (the table has no status column — a Notes-style sentence suffices, no columns invented). D-413 and D-514 stay 📅 Forward (9.1 visibility) — untouched |

## Content verification (all claims traced to landed code / outcome files)

- **Endpoints:** intention (`POST v1/intention/`, Token auth), checkout redirect (`publicKey` +
  `clientSecret` exactly), cron sweep route (`GET /api/cron/reconcile-paymob-payments`, Bearer),
  inquiry pair (`api/auth/tokens` → `transaction_inquiry`, token-in-body) — all match
  `outcome/3.2`, `3.3`, `5.1`, `5.2` and the mirror cheatsheet.
- **HMAC:** the 20/20/8 key lists copied verbatim from `outcome/3.1` (mirror-verified, superseding
  the cheatsheet's "19 keys" approximation); serialization + SHA-256-digest `timingSafeEqual`
  discipline; verify-before-parse; one-builder/no-parallel-signer rule (5.4/5.5).
- **Env matrix:** all 13 keys + defaults + fail-closed semantics from `outcome/2.1`; the NGROK_*
  pair (both required, `ngrok-not-configured` / `ngrok-unreachable` fallbacks, readiness budget =
  `PAYMOB_HTTP_TIMEOUT_MS`) from `outcome/5.5`; cron trio from `outcome/5.2`.
- **Callback channels:** the real/ngrok/simulation resolution matrix, single-lazy-resolution,
  localhost-leak guard, authtoken-via-child-env, public-probe-only, production guards, and
  determinism — all from `outcome/5.4` + `5.5` + the mid-point review's two HIGH fixes.
- **Flow + invariants:** replay/quarantine/once-only-guarded-transition/post-commit-publish/
  never-retry-a-timeout — from `subscription-purchase.md` §1/§5/§7 + `outcome/3.3` + `4.2` + `5.1`.
- **Troubleshooting:** integration-ID/mode-mismatch 404, HMAC-secret-mode mismatch, missing
  dashboard callback URL, `ngrok-not-configured`/`ngrok-unreachable`, stuck pendings → sweep,
  bare-404 gates, replay safety — the REQ-080/REQ-081 surfaces.
- **i18n posture:** user-facing localized vs operator-facing English — the mid-point review's
  LOW-info disposition, with the `planPriceShapeInvalid`/`planPriceOutOfRange` precedent.
- **D-611 rewrite accuracy:** verified against the live tree before writing —
  `shared/locale/namespaces/{wallet,plans,checkout}/*.namespace.ts` (handle shape),
  `namespaces/registry.ts` + `index.ts` (registration), `types/message.ts` (`Translations` member),
  `en/ar/messages.ts` (bundle slices), `client/use-app-translation.ts` (handle overload),
  `server.ts` / `server-graphql.ts` (slice-returning getters), `ctx.t("errorsTranslations")`
  call sites, and the parity-test exemplars. Every grep against the rewritten section for
  `serverLegacy`, `clientLegacy`, `MessageSchema`, `namespacePaths`, `app/[locale]` returns zero
  matches; the legacy `next-intl` "do not reintroduce" guard in the Overview is retained.

## Quality gates (docs-only task)

- No sub-loop runs (docs-only — the sub-loop is code-scoped; repo precedent from 4.1/4.2: every
  docs-only edit in this plan family was verified by content review).
- All doc links resolve in-tree (`docs/IDEMPOTENCY.md`, `docs/graphql/error-handling-contract.md`,
  `docs/billing/subscription-purchase.md`); no `console.*`, no plan-meta in the doc (task/REQ/plan
  paths appear only in the Related-Documents pointer section as file paths, not code comments).
- Ledger gate still green: `awk '/^## Ledger Table/,/^## Status Values/' deferred-items.md |
  grep -c "❌\|⚠️"` → **0** (D-413/D-514 remain 📅 Forward, which does not match the gate).
- No code files touched → no `sub-loop duplicates` requirement applies. No commits made.

## 9.2.SR (semantic review, docs)

- No invented facts: every table cell traces to an outcome file or the mirror; the "19 keys"
  cheatsheet approximation is documented as superseded (mirror text wins, per 3.1).
- No columns invented in the A-set table (no status column exists — the consumed status rides a
  sentence under the section intro, exactly as the task brief allows).
- The shared/AGENTS.md rewrite preserves the section's other subsections (Public import contract,
  View-layer label types, Browser Translation Cache) untouched.
- The root/backend-services AGENTS.md edits are one line each — no rule renumbering, no formatting
  churn.

## 9.2.IV (instruction verification)

Rule files read: root `AGENTS.md`, `backend/AGENTS.md`, `backend/services/AGENTS.md`,
`shared/AGENTS.md`, `shared/locale/AGENTS.md`, `app/AGENTS.md`; docs structure template
(`docs/billing/subscription-purchase.md`); the paymob mirror cheatsheet + `docs/INDEX.md` for
operator/runbook facts. Compliance: no summary markdown created outside the plan's own
`outcome/` convention; no commits; only brief-listed files edited (the A-set Notes sentence and
the D-515 note are explicitly in-scope per the task brief and mid-point adjudication).

## Cross-file dependencies

None. All edits were within this task's brief (docs + the four named AGENTS/contract/ledger
files + the plan's own tasks.md/outcome).
