# Task 1.1 — Substrate Verification Audit Outcome

**Date:** 2026-09-12 · **Task:** Verify the shipped substrate against every ticket AC and record execution evidence
**Requirements:** REQ-003, REQ-008, REQ-009, REQ-010, REQ-011, REQ-012, REQ-013, REQ-014, REQ-015, REQ-016, REQ-019, REQ-020, REQ-022, REQ-023, REQ-027, REQ-029
**Nature:** VERIFY — read-only audit; no source, test, or schema file modified by this task.

Every claim below was re-verified fresh today against the **current working tree** — numbers come from real runs against local Postgres (`127.0.0.1:5432/kottaby`, env `.env.test`), and every cited line range was opened and matched to content.

**Concurrency note (affects the draft):** this tree is shared with agents executing Tasks 2.1/2.2/3.1 in parallel. Their edits landed *during* this audit, so the draft's line ranges and gap status were re-checked post-edit; stale items are flagged under "Corrections" per section. All tests were re-run after the edits settled.

## 1. Test Execution Evidence (all green, fresh runs)

| Suite | Command | Result |
|---|---|---|
| Repo credit lanes | `bun --env-file=.env.test run test/scripts/run-test.ts backend/db/test/repo/students/student.repository.test.ts` | ✅ 23 pass / 0 fail, 100 expect() calls (exit 0) |
| Activation service | `bun --env-file=.env.test run test/scripts/run-test.ts backend/services/billing/subscription-activation.service.test.ts` | ✅ **18** pass / 0 fail, 169 expect() calls (exit 0) — includes the Task 2.1 Tajweed test |
| Session lifecycle | `bun --env-file=.env.test run test/scripts/run-test.ts backend/services/classes/session-lifecycle.service.test.ts` | ✅ 72 pass / 0 fail (exit 0) |
| Purchase journey | `bun --env-file=.env.test run test/scripts/run-test.ts test/workflows/billing/subscription-purchase.journey.test.ts` | ✅ **12** pass / 0 fail, 136 expect() calls (exit 0) — includes the Task 2.2 Tajweed leg |
| Denials journey | `bun --env-file=.env.test run test/scripts/run-test.ts test/workflows/sessions/session-lifecycle-denials.journey.test.ts` | ✅ 6 pass / 0 fail (exit 0) |
| Dual-confirmation journey (AC3 bind) | `bun --env-file=.env.test run test/scripts/run-test.ts test/workflows/sessions/session-dual-confirmation.journey.test.ts` | ✅ 11 pass / 0 fail (exit 0) |

**Total: 142 pass / 0 fail across 6 files.** Logs persisted under `logs/2026-09-12T11-18-47/…` (initial wave) and the subsequent re-run directories.

**Correction vs. draft:** the draft's 17/11 pass counts for the activation suite and purchase journey reflected the pre-closure tree; the current tree (2.1/2.2 landed) is 18/12. Both states were genuinely executed and recorded here.

### Denial focus view — actual output (corrected from draft)

Command: `bun --env-file=.env.test run test/scripts/run-test.ts --last --focus "INSUFFICIENT_BALANCE" backend/services/classes/session-lifecycle.service.test.ts` produces:

```
=== Last Test Result ===
Test File: backend/services/classes/session-lifecycle.service.test.ts
Exit Code: 0
Pass: 72
Fail: 0

Status: ALL PASSED
```

**Correction:** the draft pasted `(pass)` spec lines as if they were the focus-view output. They are not: `applyFocusFilter` (`test/scripts/run-test.ts:232-242`) filters *failed* test names and error details only, so with zero failures the focus view is the summary block above. The denial branches are instead evidenced by the captured run log (`logs/2026-09-12T11-18-47/backend/services/classes/session-lifecycle.service.test.ts.log`):

```
[DOMAIN] Session booking rejected: insufficient balance {"code":"INSUFFICIENT_BALANCE","entity":"session","entityId":5534}
(pass) SessionLifecycleService — transactional flows (runInRollback) > total-miss branch: an empty student is denied INSUFFICIENT_BALANCE with zero rows and the key stays reusable [10.23ms]
[DOMAIN] Session booking rejected: insufficient balance {"code":"INSUFFICIENT_BALANCE","entity":"session","entityId":5641}
(pass) SessionLifecycleService — REQ-043 chaos (production tx path, committed fixtures) > REQ-043(d): two concurrent creations with ONE unit → exactly one session + one INSUFFICIENT_BALANCE, lanes never negative [13.02ms]
```

The chaos case at `session-lifecycle.service.test.ts:2313-2343` exists and passes: two concurrent bookings of one unit settle as exactly one fulfillment + one `ValidationError` with code `INSUFFICIENT_BALANCE`, lanes never negative — the guarded `UPDATE … WHERE balance > 0` row-lock serialization proof.

## 2. AC ↔ Evidence Matrix (`docs/planning/TICKETS.md:508-526` + scenarios :528-535)

| Gherkin AC / Scenario | Verified executing test ref (current tree) |
|---|---|
| AC1: Hifz plan session_count=8 → `balance_hifz += 8`; tajweed/reviews unchanged | Repo: hifz-lane credit + siblings untouched `student.repository.test.ts:352-376`, default-executor path `:402-415`. Service: exact delta = `plan.sessionCount`, one lane, siblings zero `subscription-activation.service.test.ts:523-544`. Journey: step 3 hifz credit `subscription-purchase.journey.test.ts:532-602` (lane-delta asserts `:567-571`). NULL-lane purchase/activation fail-close `subscription-activation.service.test.ts:450-486`. |
| AC2: Tajweed plan session_count=12 → `balance_tajweed += 12` | Repo tier-1 tajweed credit `student.repository.test.ts:378-388`. **Service: `subscription-activation.service.test.ts:586-622` (Task 2.1 — landed in-tree concurrently, green in today's re-run). Journey: step 12 `subscription-purchase.journey.test.ts:870-972` (Task 2.2 — landed concurrently, green; lane-delta assert `:911`, replay-no-double-credit asserted).** Gaps G1/G2 are closed; G3 (GraphQL pin) remains → Task 2.3. |
| AC3: balance_hifz=4, attend Hifz session (dual confirmation) → 3 | Shipped semantics = debit-at-request + hold-as-escrow (ratify D1): hifz-hit branch debits exactly one hifz unit at booking `session-lifecycle.service.test.ts:428-447`; confirmation releases the hold with no second debit `session-lifecycle.confirmation.ts:112-138` + happy-path test `session-lifecycle.service.test.ts:2396`; dual-confirmation journey (executed today, 11 pass) asserts the net −1 escrow end state `session-dual-confirmation.journey.test.ts:363-380,451-489`. Net arithmetic matches the AC's −1. |
| AC4: balance_hifz=0, request Hifz → rejected 422 "Insufficient balance" | Service: total-miss branch (zero writes, key reusable) `session-lifecycle.service.test.ts:470-500`; journey leg 2 `session-lifecycle-denials.journey.test.ts:236-259`; chaos concurrency proof `session-lifecycle.service.test.ts:2313-2343`. **GraphQL-transport pin = open gap G3 → Task 2.3** (`backend/graphql/test/` has zero `INSUFFICIENT_BALANCE` hits — verified by grep today). "422" = VALIDATION family (`error-code-taxonomy.ts:41-51`) — ratify D3. |
| Scenario: Hifz plan credits balance_hifz only | `subscription-activation.service.test.ts:523-544`; `subscription-purchase.journey.test.ts:532-602` |
| Scenario: Tajweed plan credits balance_tajweed only | Repo tier `student.repository.test.ts:378-388` + service `subscription-activation.service.test.ts:586-622` + journey step 12 `:870-972` (all green) |
| Scenario: Review plan credits balance_reviews only | Service: `subscription-activation.service.test.ts:546-573` (hifz/tajweed byte-identical); journey step 11 `subscription-purchase.journey.test.ts:791-868` (lane-delta asserts `:834-837`); repo tier `student.repository.test.ts:390-400`. |
| Scenario: Balance decrement on session completion | Confirmation flow (no second debit) `session-lifecycle.confirmation.ts:112-138`; lifecycle tests (confirm happy path `:2396`; resolve-COMPLETE dispute `:1791`); dual-confirmation journey steps 2–5 (`:363-489`) and sweep/race refund legs (`:536-688`). |
| Scenario: Insufficient balance prevents session request | `session-lifecycle.service.test.ts:470-500` + `:2313-2343`; `session-lifecycle-denials.journey.test.ts:236-259`. |
| Scenario: Balance never negative (CHECK constraint) | Tier 4 CHECK floor: `students_balance_hifz_check` (numeric seed) and `students_balance_reviews_check` (NULL-seeded row), savepoint-rescued, row intact post-rollback `student.repository.test.ts:428-461`. CHECK definitions `backend/db/schema/students/students.ts:42-45` (verified). |

Every gherkin AC and listed test scenario is now bound to at least one executing, currently-green test ref. The only remaining evidence gap is G3 (GraphQL transport pin) → Task 2.3.

**Corrections vs. draft:**
- Replay ranges corrected: Tier-1 duplicate-delivery replay is `subscription-activation.service.test.ts:380-413`; Tier-4 zero-credit replay proof is `:899-931`. The draft's "861-885" was actually the tampered-amount quarantine (`:865-897`).
- Chaos case range corrected to `:2313-2343` (draft said 2314-2334).
- Journey refs re-based to post-2.2 tree: step 3 = `:532-602`, step 11 = `:791-868`, step 12 = `:870-972`.
- Gap status: G1/G2 are closed in the current tree (2.1/2.2 landed concurrently); only G3 remains.

## 3. Tenancy / Security Posture (REQ-029, read-only verification)

- `grep -rn "balanceHifz|balanceTajweed|balanceReviews|balanceTrial" backend/graphql/mutation/` → **zero hits** — no mutation accepts caller-supplied balance values on any of the four lanes.
- Same grep across `backend/graphql/pothos/` excluding the `admin/` directory → **zero hits**. The only exposure is the admin read type `backend/graphql/pothos/admin/admin-students.pothos.ts:49-53` (`balanceHifz/balanceReviews/balanceTajweed/balanceTrial` + `trialGrantedAt` on `AdminStudentItem`) — verified by reading the file.
- Amounts are never caller-controlled: activation credits exactly `plan.sessionCount` inside the activation transaction (`subscription-activation.service.ts:393-400`, verified); booking debits the constant-1 unit through the trial-first ladder (`session-lifecycle.booking.ts`, ladder proven by `session-lifecycle.service.test.ts:394-525`).
- Abuse lanes live and green: NULL-lane quarantine (`subscription-activation.service.test.ts:450-486`), ledger-row-missing (orphan) quarantine (`:830-863`), tampered-amount quarantine (`:865-897`), plus the Tier 3 chaos + Tier 4 abuse describes.

## 4. Locale Parity (REQ-023)

All three locations opened and confirmed:

- Type contract: `shared/locale/types/errors/labels.ts:174` — `readonly insufficientBalance: string;` (annotated as the booking balance reject).
- en: `shared/locale/en/errors/index.ts:85` — `"Your balance is insufficient for this request."`
- ar: `shared/locale/ar/errors/index.ts:84` — `"رصيدك غير كافٍ لإتمام هذا الطلب."`

Parity holds: identical key, identical semantics, both locales. The denial surfaces route through this key (`session-lifecycle.service.test.ts:477`; `session-lifecycle-denials.journey.test.ts:255`).

## 5. Ratify-Pending Divergences (flagged for Task 3.2)

1. **D1 — debit timing (plan.md §1.3, REQ-024):** shipped code debits at request (hold-as-escrow), not at attendance as the AC phrases it; net −1 per attended session identical. Evidence: booking debit `session-lifecycle.service.test.ts:394-447`; hold release without second debit `session-lifecycle.confirmation.ts:112-138`.
2. **D2 — reviews lane credit-only (REQ-025):** `reviews` is a `SubscriptionCreditLane` but not a `HeldBalanceLane` (`backend/enum/scheduling/held-balance-lane.enum.ts:17-31`); no booking flow can spend it today. Credit path proven green (`subscription-activation.service.test.ts:546-573`; journey step 11).
3. **D3 — "422" = VALIDATION family (REQ-020/021):** GraphQL envelope is HTTP 200 with `errors[].extensions.code = INSUFFICIENT_BALANCE`; canonical mapping `VALIDATION → 422` (`backend/lib/errors/error-code-taxonomy.ts:41-51`). Executable transport pin = Task 2.3.
4. **D4 — trial-first eligibility (REQ-026):** `(intent lane > 0) OR (trial > 0)`, trial lane consumed first (ladder `session-lifecycle.service.test.ts:502-525`; denial only when both empty `:470-500`). INV-B4/B8 anchors.

## 6. Files Modified

None by this task (verification-only). Pre-existing working-tree modifications (`subscription-activation.service.test.ts`, `subscription-purchase.journey.test.ts`, `db/schema.dbml`) belong to the concurrently executing Tasks 2.1/2.2/3.1 agents — this audit re-ran the two affected suites against the post-edit tree so §1/§2 reflect current truth.

## 7. Self-Review & Instruction Verification

- Scope boundary: only this outcome file was written; the tasks.md checkbox flip (line 33) is the sole other edit.
- No fabricated numbers: 23/18/72/12/6/11 pass counts (total 142) are from real runs today; every line-range citation was opened and matched to content before writing §2.
- Plan-artifact IDs (REQ-*) appear only in this documentation file — no product code touched.
- Rule files surfaced during verification (`backend/graphql`, `backend/services`, `backend/db/test`, `backend/db/schema`, `test/workflows` AGENTS.md files) govern *changes* to those directories; this task made none. Test-infra facts (`runInRollback`, focus-view semantics of `test/scripts/run-test.ts:232-242`) were read from source, not from the known-stale `backend/db/test/AGENTS.md` (deferred item D2).

## 8. Carry-Forward

- All six suites green on the current tree — including the freshly landed Task 2.1/2.2 Tajweed closures; any future failure in these files belongs to this plan's remaining edits.
- Remaining evidence gap: G3 → Task 2.3 (GraphQL `INSUFFICIENT_BALANCE` transport pin — zero hits in `backend/graphql/test/` re-confirmed today).
- Task 3.2: ratify D1–D4 with the anchors in §5.
- The activation suite's hifz/tajweed equivalence note is retired by the executable test at `:586-622` (Task 2.1's own bookkeeping, noted here for the matrix).
- Test runs require `--env-file=.env.test` (Phase 0). `--focus` in `run-test.ts --last` filters failure detail only — for green suites, grep the captured run log for denial lines instead.
- Quality-gate: `sub-loop --lifecycle duplicates` on this file exits 1 solely because oxlint/biome/lint report "No files found to lint" for a `.md` target (tsgo stage passed; "no applicable instruction files found"). Per the task's own gating rule, nothing-to-lint counts as PASS — recorded here for the audit trail.
