# Task 2.2 — Tajweed Journey Leg Outcome

**Date:** 2026-09-12 · **Requirements:** REQ-018, REQ-031
**Nature:** IMPLEMENT — extend journey J1 with a Tajweed-lane leg patterned after the Hifz leg (`:506-545`) and Reviews leg (`:765-813`).

## What Was Done

`test/workflows/billing/subscription-purchase.journey.test.ts` gains a twelfth step (the only file edited, besides `tasks.md` checkboxes and this outcome).

**Fixture (`beforeAll`, inside the suite's ONE committing transaction):**

- `tajweedPlanRow = await createTestPlan(tx, { title: \`${PREFIX} tajweed plan\`, sessionCount: TAJWEED_PLAN_SESSION_COUNT (5), price: "150.00", currency: "EGP", intervalDays: PLAN_INTERVAL_DAYS, balanceLane: SubscriptionCreditLane.Tajweed })`, registered via `tracked.register(plans, tajweedPlanRow.id)` — same convention as the Hifz fixture plan.
- New per-run key `KEY_TAJWEED` and constant `TAJWEED_PLAN_SESSION_COUNT = 5` (distinct from the Hifz plan's 7).
- Header docblock gains the step-12 entry.

**Step 12 — purchase on the Tajweed-lane fixture plan + confirmed event:**

1. Premise: `tajweedPlanRow.balanceLane === SubscriptionCreditLane.Tajweed` (enum member, not a string literal).
2. `SubscriptionPurchaseService.purchase(studentA, { planId: tajweedPlanRow.id }, KEY_TAJWEED, "en")` → pending pair asserted against real `tajweedPlanRow` money; subscription/payment/claim registered in `tracked` + `ledgerSubscriptionIds`/`ledgerPaymentIds` exactly like steps 6/11.
3. Signed-webhook-path equivalent (`SubscriptionActivationService.processWebhookEvent` with the checkout's provider reference) → `{ processed: true }`.
4. Balance oracle: `balance_tajweed` delta = `TAJWEED_PLAN_SESSION_COUNT` exactly; `hifz`, `reviews`, **and `trial`** byte-identical. (`readBalances`/`StudentBalance` gained the `trial` lane — `balanceTrial` is non-nullable — so the sibling-lane invariance covers all four lanes.)
5. Subscription active, payment paid, pending-set counts grew by exactly one pair/junction/claim.
6. Notification/publish assertions: inbox goes 2 → 3; the new row is a `PaymentConfirmation` for student A composed from `NOTIFS_EN` (no hardcoded copy); `publishSpy` grows 2 → 3 and the last receipt's `recipientUserIds` is exactly `[studentA.userId]`.
7. Foreign invariance (journey rule 12): student B's four lanes unchanged (`toEqual` against a before-snapshot), B's and parent's inboxes stay at 0.
8. Replay: the SAME confirmed event reprocessed → `{ processed: true, replayed: true }`, every lane byte-identical afterwards, payment stays `paid`, inbox stays 3, publish count stays 3 — no second credit, no second fan-out.

## Files Modified

- `test/workflows/billing/subscription-purchase.journey.test.ts` (+133 lines: docblock step-12 entry, `KEY_TAJWEED`, `TAJWEED_PLAN_SESSION_COUNT`, `trial` lane on `readBalances`/`StudentBalance`, `tajweedPlanRow` fixture + registration, step-12 test).
- `ai/plans/sprint_1/segregated_session_balance-crediting/tasks.md` — flipped exactly the five `2.2` checkboxes (2.2, 2.2.QL, 2.2.TE, 2.2.SEC, 2.2.SR+2.2.IV).
- This outcome file (created).

## Files NOT Modified (and why)

- `test/workflows/helpers/*` — the existing `TrackedFixtures` registry already covers the plan/claim/notification/le​dger legs; no new helper behavior needed. No cross-file dependency.
- `backend/**` — the Tajwed credit path was already shipped and proven by Task 2.1; this leg only exercises it end-to-end.
- `backend/db/test/entity-setup.ts` — `createTestPlan(:178)` already accepts a `balanceLane` override via `Partial<PlanSelectType>`; verified signature before use.
- All parallel-agent files visible in `git status` (`subscription-activation.service.test.ts`, `db/schema.dbml`, other tasks.md lines) — other tasks' scope, untouched.

## Verification Results

| Gate | Command | Result |
|---|---|---|
| Sub-loop (full depth) | `bun --env-file=.env.test run scripts/health/sub-loop.ts test/workflows/billing/subscription-purchase.journey.test.ts --lifecycle duplicates` | **exit 0** — tsgo, oxlint, biome:check, lint:type-aware, check:duplicates all pass (duplicates stage outside jscpd scope — skipped by design) |
| Journey run 1 | `bun --env-file=.env.test run test/scripts/run-test.ts test/workflows/billing/subscription-purchase.journey.test.ts` | **12 pass / 0 fail** (136 expects) — prior baseline 11 + new step 12 |
| Journey run 2 (immediate re-run) | same command | **12 pass / 0 fail** — idempotent-teardown proof (zero residue; the mandatory post-teardown existence re-probes in `tracked.cleanup()` + junction sweep would have failed run 2 otherwise) |

**SEC:** the existing denial probes (step 7 parent → FORBIDDEN, step 8 foreign key replay → oracle-safe not-found, step 9 anonymous → UNAUTHORIZED, step 10 uncorrelatable reference → zero mutation) all stay green in both runs; the webhook credit path remains provider-signed at the route boundary (`processWebhookEvent` only receives verified events) and fail-closed on lane mapping — no test-side tampering introduced. Foreign-actor invariance asserted for the new leg (B's lanes byte-identical, foreign inboxes at 0, receipt targets student A only).

**SR checklist:** no `runInRollback`; no `.rejects.toThrow()`; no `console.*`; no `any`; all lane references via `SubscriptionCreditLane` enum members; translated copy via `getServerTranslations` bundles only; per-run `jrn_billing_<8hex>` prefix on the new fixture title and key; no read-then-write races added (all assertions are post-commit reads); every new row registered for FK-safe teardown.

**IV:** rule files (re-)read — `test/workflows/AGENTS.md` (rules 1–12), `.agents/instructions/tests.instructions.md`, root `AGENTS.md`, plus the read-only backend layer files (`backend/AGENTS.md`, `backend/enum/AGENTS.md`, `backend/db/{schema,test}/AGENTS.md`, `backend/services/AGENTS.md`) printed/required by the sub-loop. All honored.

## Cross-File Dependencies

None.

## Carry-Forward

- Journey baseline is now **12 pass / 0 fail** (was 11). Any future regression in this file must be checked against both consecutive runs.
- `readBalances`/`StudentBalance` now expose the `trial` lane — later journey legs (e.g. trial-grant flows) can assert `balanceTrial` directly without re-extending the oracle.
- The Tajweed lane is now proven end-to-end in the journey tier exactly like the Reviews lane: fixture-driven plan → purchase → signed webhook → exact lane credit with all siblings byte-identical → replay with no second credit.
