# Task 4.2 — Knowledge Propagation Closeout Outcome

**Date:** 2026-09-12 · **Status:** PASS · **Requirements:** REQ-004, REQ-005, REQ-006, REQ-007, REQ-034
**Nature:** DOCS closeout — synthesize recurring gotchas from all outcome files, final section for the canonical doc, rule-file integrity confirmation, final checkbox flip. No product code, test, schema, or plan-rule file touched.

## Summary

Read all nine prior outcome files plus `plan-review-R1.md` (its "Lessons for Future Plans" section) and synthesized the recurring gotchas into a consolidated registry (below). The canonical doc `docs/billing/segregated-session-balance.md` (Task 3.2, committed in `7521a88e`) did NOT yet cover the durable transport-level facts, so a concise final section **"## 10. Gotchas & Operational Notes"** was appended with the four genuinely reusable operational notes. Plan-specific one-offs (focus-view semantics of `run-test.ts`, sub-loop `.md`/`.dbml` carve-out, path-drift traps, stale rule-file references) were deliberately left out of the canonical doc — they are process/tooling knowledge, not domain knowledge, and live here.

## Recurring Gotchas (consolidated from all outcome files)

1. **`run-test.ts --focus` is view-only** — it binds to `--last` mode; prescribing it as a run filter silently runs the whole file (plan-review R1 anti-pattern finding, tasks.md 1.1). Additionally `applyFocusFilter` (`test/scripts/run-test.ts:232-242`) filters only FAILURES, so with zero failures the focus view prints just the summary block — denial-branch evidence must come from the captured run log (Task 1.1's "Denial focus view" correction).
2. **oxlint "No files found to lint" for non-JS/TS targets** (`.md`, `.dbml`) → sub-loop exits 1. Every docs-only task in this plan (1.1, 3.1, 3.2) hit it and counted it as PASS per the task contract's carve-out. Future tooling-fix candidate: an extension-aware skip in `scripts/health/sub-loop-checks.ts` (recorded; NOT fixed here — the script is outside this task's scope, and AGENTS.md/instructions are hand-curated).
3. **Plan-dir path drift** — plan text says `ai/plans/sprint_1/Segregated Session Balance-crediting/` (spaced/capitalized); the real dir is `ai/plans/sprint_1/segregated_session_balance-crediting`. tasks.md header/P1/QL commands still carry the spaced name; every executor had to resolve the drift manually.
4. **Migration dir is `backend/drizzle/`** — NOT `backend/db/drizzle/` as some plan text states (Task 3.1's per-line parity audit quoted migration SQL from `backend/drizzle/`).
5. **Stale rule-file references (deferred item D2)** — `backend/db/test/AGENTS.md` + `.agents/instructions/tests.instructions.md` cite helpers (`setupStudent`, `createTestTeacher`) that don't exist; the actual helpers live in `backend/db/test/entity-setup.ts`. Reported to maintainers; rule files untouched.
6. **GraphQL transport gotchas (Task 2.3):** student clients require an `X-Idempotency-Key` header or the pre-DB `idempotencyKeyRequired` guard VALIDATION-denies before the balance ladder; wire `ID!` fields serialize as strings (need `String(...)` comparisons); GraphQL integration suites accumulate committed rows by convention (3 pre-existing session rows from `session-lifecycle-mutations.test.ts` fixtures — not residue; scope count assertions to fixture ids).
7. **Claims in test-file comments must be backed by executable tests** — the activation suite's "hifz/tajweed byte-identical" header note was retired by Task 2.1's executable Tajweed test rather than left as an assumption.
8. **Lint service needs a database** — `scripts/lint-service.ts` opens a DB pool for its serialized queue; on a fresh worktree `.env`/`.env.test` must be materialized from the CI template first (Phase 0 note). All test/lint runs in this plan used `--env-file=.env.test`.
9. **jscpd cross-file scan does not cover `backend/services/billing/` test paths** (reported "outside jscpd scan scope" in Tasks 2.1/2.2/2.3) — sibling-mirroring structure remains the duplicate-resistance mechanism there.
10. **`backend/db/repo/students/__tests__/` sits outside `backend/db/test/`** — runs via global `test/run-test.ts`, not `bun run test:db` (substrate-verification note).

## Canonical Doc — Final Section Appended

`docs/billing/segregated-session-balance.md` gained **"## 10. Gotchas & Operational Notes"** with four durable, transport-level facts (idempotency header requirement; `ID!` string serialization + `extensions.code`; shared-DB row-accumulation convention → fixture-scoped assertions; denial evidence via captured server log). These were the only step-1 gotchas that are genuinely reusable *domain-consumer* knowledge; the doc already covered the domain model, credit/hold paths, guarded-mutation rules, D1–D4, anti-patterns, and the test map (§1–§9).

## AGENTS.md / .agents/instructions/ Untouched — Confirmed

- `git log --oneline -3` → `7521a88e`, `7afd194d`, `a63c0a7e` — no rule-file commits.
- `git status --porcelain` → only ` M ai/plans/sprint_1/segregated_session_balance-crediting/tasks.md` (checkbox flips) and `?? .../outcome/4.1-review-wave-outcome.md` (Task 4.1's file). No `AGENTS.md` or `.agents/` path appears modified.

## Files Modified by This Task

1. `docs/billing/segregated-session-balance.md` — appended final section "## 10. Gotchas & Operational Notes" (4 bullets; nothing else in the file changed).
2. `ai/plans/sprint_1/segregated_session_balance-crediting/tasks.md` — flipped exactly the 4.2 checkbox line (`- [ ] 4.2 Knowledge propagation closeout` → `- [x]`). No other line touched.
3. `ai/plans/sprint_1/segregated_session_balance-crediting/outcome/4.2-knowledge-propagation-outcome.md` — this file (created).

## Verification Results

| Check | Command | Result |
|---|---|---|
| Sub-loop on the canonical doc | `bun --env-file=.env.test run scripts/health/sub-loop.ts docs/billing/segregated-session-balance.md --lifecycle duplicates` | exit 1 solely because oxlint reports "No files found to lint" for a `.md` target; tsgo stage green, no applicable instruction files. Per the plan's documented carve-out (Tasks 1.1/3.1/3.2 precedent), nothing-to-lint counts as PASS — noted here. |
| Rule-file integrity | `git log --oneline -3` + `git status --porcelain` | No `AGENTS.md` / `.agents/` path modified — untouched, confirmed. |
| Scope boundary (SR) | `git status --porcelain` after edits | Exactly the three files above; no product code, test, schema, or rule file touched. |

## Self-Review (SR)

- Scope boundary: only the outcome file + the canonical-doc final section + the single 4.2 checkbox line. Clean language throughout; no fabricated claims — every gotcha above cites the outcome file that surfaced it.
- The canonical-doc section is durable domain-consumer knowledge only; plan-specific one-offs stayed in this outcome file.

## Carry-Forward

- **Plan completion:** all tasks are now `[x]`; the finished plan moves to `ai/finished_plans/sprint_1/` per house convention — **that move is the user's/orchestrator's call, not performed by this task.**
- Sub-loop carve-out (oxlint empty-set exit on non-JS/TS targets) remains a future tooling-fix candidate for the tooling owner (`scripts/health/sub-loop-checks.ts` extension-aware skip).
- Deferred items D2 (stale helper names in rule files) and D5 (§5.3.3 expiry invariant, DEV1-008 scope) remain reported/open as recorded in the ledger.
- Path-drift traps (spaced plan-dir name; `backend/db/drizzle/` vs `backend/drizzle/`) should be corrected in the plan template if plans continue to be generated with spaced names.
