# Task 7.1 + 7.2 — Knowledge Propagation & Documentation Outcome

**Plan:** `ai/plans/sprint_1/session-report-homework-infrastructure`
**Scope:** 7.1 canonical doc `docs/sessions/session-report-homework.md` (created) + 7.2 session-lifecycle amendment + six AGENTS.md propagations. No commits made; `tasks.md` untouched.

---

## 7.1 — Canonical doc created: `docs/sessions/session-report-homework.md`

House style matched to `docs/notifications/session-request-notifications.md` / `docs/sessions/session-lifecycle.md`: Why → Pattern → Rules → What NOT to Do → Consumer Guidance → Rollout Summary → Related Documents. Structure verified: 7 numbered sections, 25 table rows, 0 code fences.

Content coverage (every item from the task brief):

| Brief item | Where it landed |
|---|---|
| Why: post-session academic record, report+homework co-creation | §1 (corrupted-transcript framing for the co-creation mandate) |
| Write gate (INV-S7): teacher role + governance re-check + ownership + `status=completed`; cancelled/disputed denied; oracle collapse for foreign/unknown | §2.1 (`$all` role leg at the wire, `assertTeacher` governance re-check pre-DB, `lockForReportGate` FOR UPDATE classification, `SESSION_NOT_FOUND` / `SESSION_INVALID_TRANSITION` reuse) |
| Atomic co-creation (INV-S8); homework-without-report unreachable; full rollback incl. notifications | §2.2 (+ §2.6 post-commit publish making rollback = zero pushes) |
| One-report-per-session arbiter + `SESSION_REPORT_ALREADY_EXISTS`; constraint names | §2.3 (`reports_session_id_unique` / `home_work_session_id_unique`, 23505 → conflict keyed by constraint name; callers-may-rely note) |
| First-vs-subsequent grading (INV-HW3/HW4); assignment rows born ungraded; `previousGrades` → newest prior row via one-shot guarded UPDATE; genuine-first no-op vs all-graded conflict | §2.4 (`findLatestByStudentId` + `gradeHomeWorkOnce`, honest zero-row-miss classification) |
| Oracle-collapse reads: participant-only; parents/foreigners/admins → null; silent reads | §2.5 |
| Notification choreography: single wave-context read; recipient-locale; student + linked-parent (INV-P1); names-only bodies; idempotency key `session:{id}:report`; publish strictly post-commit; engine claim replay | §2.6 (all six steps, incl. per-recipient claim digest + replay-returns-stored-receipt) |
| Append-only posture; pure-wallet discipline; bounded logging | §2.7 |
| Consumer table ("what each may rely on") | §5 — the submit-UX ticket (typed documents at `frontend/graphql/sharedDocuments/scheduling/session-report.documents.ts`), the Surah/Juz UI ticket (Surah/Juz), the parent-portal tickets (reads stay null), the rating-aggregation ticket (rating rows), the dual-confirmation/escrow tickets (escrow independence), the admin academic-tracking ticket (admin = non-participant) |
| Related Documents | §7 — exactly the four mandated docs (session-lifecycle, session-request-notifications, error-handling-contract, domain-error-extensions-code) |

**Clean-docs rule verified:** `rg "REQ-|ai/plans|tasks\.md|plan\.md"` over the doc body → zero matches (domain language only; invariant names INV-S7/S8/INV-HW3/HW4/INV-P1/C.4/C.5 used as repo domain vocabulary per `docs/specs/state-machine-invariants.md` + `docs/specs/open-decisions-and-gaps.md` — all confirmed present there). Ticket IDs appear only in the consumer/rollout tables, the house pattern of both reference docs.

## 7.2 — Amendments (all minimal + additive; per-file diff summaries)

### 1. `docs/sessions/session-lifecycle.md` (+4/−4, four table cells)

- §2.3 invariant binding: the two `INV-S7`/`INV-S8` rows carried the literal "**Upstream-owned** — no report surface exists in this slice" forward-phrasing → replaced with "**Shipped** — `docs/sessions/session-report-homework.md`" (per-cell, one line each).
- §10 consumer table, the dual-confirmation ticket row: header `(INV-S6/S7/S8 enforcement, …)` → `(INV-S6 enforcement, …)`; "report/homework gating" removed from the Owns list; appended "(INV-S7/S8 enforcement has since shipped — `docs/sessions/session-report-homework.md`.)"
- §10 consumer table, this ticket row: "INV-S7 gating is the dual-confirmation ticket's;" → "**Implementation shipped** — `docs/sessions/session-report-homework.md` (plan of record `ai/plans/sprint_1/session-report-homework-infrastructure/`)." Rest of the row untouched. (Plan-directory citation is the established house pattern of this file — see its closing "Plan of record" line.)

```diff
-| **INV-S7** (report only on `completed`) | **Upstream-owned** — no report surface exists in this slice. |
-| **INV-S8** (homework gated on report) | **Upstream-owned** — same. |
+| **INV-S7** (report only on `completed`) | **Shipped** — `docs/sessions/session-report-homework.md` (write gate + governance re-check + one-report arbiter). |
+| **INV-S8** (homework gated on report) | **Shipped** — same doc (atomic report+homework co-creation). |
-| **the dual-confirmation ticket** (INV-S6/S7/S8 enforcement, …) | … Owns: … report/homework gating, the `disputed` transition … |
+| **the dual-confirmation ticket** (INV-S6 enforcement, …) | … (INV-S7/S8 enforcement has since shipped — `docs/sessions/session-report-homework.md`.) |
-| **this ticket** (reports) | INV-S7 gating is the dual-confirmation ticket's; reports hang off `session_id` … |
+| **this ticket** (reports) | **Implementation shipped** — `docs/sessions/session-report-homework.md` (plan of record `ai/plans/sprint_1/session-report-homework-infrastructure/`). Reports hang off `session_id` … |
```

### 2. `backend/db/repo/AGENTS.md` (+2/−1)

- Layout block `classes/` line corrected to name all four repos: `session, report, home-work, session-request-idempotency repositories` (was stale — named only the old SessionRepository pair).
- One new Rules bullet (file's bold-domain-entry style, placed after the Parent-link requests bullet): `ReportRepository`/`HomeWorkRepository` (inserts + `findBySessionId` + `findLatestByStudentId` + one-shot guarded `gradeHomeWorkOnce` with the no-prior-row-no-op vs already-graded-conflict classification), the two new `SessionRepository` methods (`lockForReportGate` tx-REQUIRED, `findReportWaveContextById`), constraint names `reports_session_id_unique` / `home_work_session_id_unique` + the 23505 → `SESSION_REPORT_ALREADY_EXISTS` mapping; cites `docs/sessions/session-report-homework.md`.

### 3. `backend/services/AGENTS.md` (+1)

- One new bullet directly after the Session-lifecycle entry: `SessionReportService` sole-owner statement (write gate = teacher role + service-tier governance re-check + ownership + `status=completed` under a FOR UPDATE row lock; atomic co-creation; `reports_session_id_unique` → `SESSION_REPORT_ALREADY_EXISTS`; `previousGrades` via one-shot guarded `gradeHomeWorkOnce`; participant-only oracle-collapse reads) + the reaffirmation that emissions ride the caller's tx via `SessionReportNotificationService.notifySessionReportReady`, publish strictly after commit, and the single-writer `NotificationEngine` discipline (never insert `notifications` rows from domain services) applies unchanged.

### 4. `backend/types/AGENTS.md` (+1)

- One new bullet at the top list (Registration-types style): `Report*`/`HomeWork*` Select/Insert/Return types, `SessionReportSubmitInput`, and the homework composites (`HomeWorkAssignInput`/`HomeWorkBlockInput`/`HomeWorkGradeFieldsInput`) in `backend/types/classes/report.types.ts` + `home-work.types.ts`.

### 5. `backend/graphql/AGENTS.md` (+1)

- One new bullet after the "Participant-scoped operations" precedent bullet: `submitSessionReport` mutation (explicit `$all { authenticated, role: [UserRole.Teacher] }` + service-tier governance re-check), `sessionReport`/`sessionHomework` queries (`{ authenticated: true }`, nullable oracle-collapse payloads), `SessionReport`/`SessionHomeWork` objects with `id` for Apollo normalization, and the `SurahJuzRef` enum registration (enum-object form, `shared/enum.pothos.ts`, consumed by homework block inputs).

### 6. `shared/AGENTS.md` (+9)

- New section "Session Report & Homework Locale Keys (existing-namespace additions)" after the Recitation Catalog section, checklist style: states NO new namespace was registered; **`errors`** flat keys (`sessionReportAlreadyExists`, `homeworkAlreadyGraded` + validation set `sessionReportNotesRequired`, `sessionReportNotesTooLong`, `sessionRatingRange`, `homeworkGradeRange`, `homeworkAyahRangeInvalid`, `homeworkSurahJuzInvalid`, `homeworkAssignmentBlocksRequired`); **`notifications`** event copy (`eventSessionReportReadyTitle`, `eventSessionReportReadyBody(teacherName)`, `eventSessionReportReadyParentBody(studentName, teacherName)`) with the names-only privacy constraint. All key names verified verbatim against `shared/locale/types/errors/labels.ts` + `shared/locale/types/notifications/index.ts`.

### 7. Root `AGENTS.md` (+1)

- One line appended to **Important References**, immediately after the session-lifecycle entry, matching the list's dense one-line format: path, "Session report & homework canonical reference", parenthetical of the load-bearing rulings (INV-S7 gate, INV-S8 co-creation, one-report arbiter + `SESSION_REPORT_ALREADY_EXISTS`, INV-HW3/HW4 one-shot grade guard, oracle-collapse reads, publish-after-commit choreography, append-only + pure-wallet posture, consumer guidance).

**AGENTS.md hygiene:** all seven additions cite only the canonical doc (a doc path is the established convention); zero plan-task references, zero plan-task IDs, zero plan paths in any new AGENTS.md line (verified: `rg "ai/plans"` over the four backend/shared AGENTS.md diffs → only the pre-existing BLT-03 lines).

## Verification

| Check | Result |
|---|---|
| `bun run tsgo` | **exit 0** (0 errors; docs-only changeset, confirmed) |
| `bun run oxlint` | **0 warnings / 0 errors** (1419 files, 303 rules) |
| Diff surface | 7 files modified, +19/−5 total; every AGENTS.md edit purely additive except the one stale `classes/` layout-line correction and the four mandated session-lifecycle cells; no code files touched |
| Canonical doc structure | 7 numbered sections in house order, 25 table rows, 0 code fences; heading render verified |
| Clean-docs rule | canonical doc body: zero REQ-xx / plan-artifact references (grep-verified) |
| Pre-existing state | untracked `backend/graphql/test/session-report.wire.test.ts` predates this task (recorded in 5.1 as the CI-tier wire suite) — untouched |

## Verdict

**7.1 + 7.2 complete.** The canonical reference exists with the full ruling set (gate, co-creation, arbiter, grading, oracle reads, notification choreography, append-only/pure-wallet posture, consumer guidance); the lifecycle doc's forward-looking INV-S7/S8 phrasing is honestly retired; all six AGENTS.md files carry permanent, global, battle-tested one-liners pointing at the canonical doc. Nothing committed.
