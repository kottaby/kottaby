# Task 3.2 — Mutation resolver: `submitSessionReport`

**Plan:** `ai/plans/sprint_1/session-report-homework-infrastructure`
**Repo state:** branch `main` @ `9ea52f9` (service work committed by orchestrator); this task's 2 files are uncommitted working-tree edits. No commits made; `tasks.md` untouched.

## 1. Files

| File | Status | Contents |
|---|---|---|
| `backend/graphql/mutation/classes/session-report.mutation.ts` | NEW | side-effect registration of `submitSessionReport(id: ID!, input: SubmitSessionReportInput!): SessionReport!` + the module-private `wireBlockToInput` mapping helper |
| `backend/graphql/mutation/classes/index.ts` | M | +1 side-effect import line per barrel convention (+1 doc-comment line) |

## 2. Shape (verified in the assembled schema)

`submitSessionReport(id: ID!, input: SubmitSessionReportInput!): SessionReport!` — mutation non-null, `id` first among args, input referenced by the string-named Pothos const `SubmitSessionReportPothosInput` (DV-B suffix from 3.1).

## 3. Conventions matched (source of each)

- **`authScopes: { $all: { authenticated: true, role: [UserRole.Teacher] } }`** — exact `$all` conjunction per `session-lifecycle.mutation.ts` precedent + `backend/graphql/AGENTS.md` §participant-scoped operations (plain key-map = ANY semantics — wrong). `UserRole` is a VALUE import.
- **ctx.user narrowing guard** — `if (!ctx.user) throw new UnauthorizedError("Authentication required.")` mirroring builder.ts's own `authenticated` scope verbatim (the repo-wide no-non-null-assertion rule); comment marks it TypeScript-narrowing-only.
- **House id helper** — `requirePositiveIntId` from `@/backend/graphql/shared` (`backend/graphql/shared/resolver-guards.ts`, the admin-resolver guard). Composition: `requirePositiveIntId(Number(args.id), "id")` — Pothos `ID` args arrive typed `string | number` and the admin precedent's Int! args don't transfer, so the `Number()` boundary parse precedes the guard (same shape-only parse the session-lifecycle resolvers use); the service re-asserts the positive-safe-integer shape pre-DB with the localized message.
- **Field-by-field mapping, NO spread** — the client whitelist maps member-by-member into the canonical `SessionReportSubmitInput` (`@/backend/types`): `teacherNotes`, `studentRatingByTeacher`, then `homework { jadid { fromAyah, toAyah, surahJuz }, madi { … } }` and `previousGrades { currentGrade, revisionGrade }` when present (presence ternaries; optional legs drop to `undefined`). A module-private `wireBlockToInput` copies one block (canonical `HomeWorkBlockInput` param type — NO local type declared); it also neutralizes transport-`null` legs to `undefined`, matching the service's fail-closed `isSuppliedBlock` predicate.
- **Thin delegation** — identity `ctx.user.id` only; `ctx.locale` propagated as the 4th arg; zero repository calls; zero business rules; no try/catch — DomainErrors (`SESSION_NOT_FOUND`, `SESSION_INVALID_TRANSITION`, `SESSION_REPORT_ALREADY_EXISTS`, `CONFLICT`, `VALIDATION`, `FORBIDDEN`) propagate to the boundary finalizer untouched.
- **i18n: resolver-local copy is EMPTY** — no `ctx.t` call anywhere in the file (grep-verified); the service owns all messages via `getServerTranslations(ctx.locale)`.
- **Barrel** — side-effect import `import "./session-report.mutation";` (mutation/AGENTS.md convention); file has no named exports.

## 4. Deviations / recorded decisions (none silent)

- **`options` passthrough OMITTED.** Plan allows wiring the trailing `options` "ONLY if the house resolver convention provides it". 0.2 item 22: "NO existing resolver passes engine options (zero `backend/graphql` hits) — the option-typed seam comes from the service signature, not resolver precedent"; `createGraphQLContext` exposes no engine-options surface (`ctx.idempotencyKey` is a different mechanism this flow has no parameter for). `submitSessionReport`'s trailing `outerTx?`/`options?` remain service/test-tier seams — production callers omit both.
- **Mapping helper shape:** the `wireBlockToInput` one-liner exists because (a) `SessionReportSubmitInput` fields are `readonly` (no mutate-then-assign), (b) Pothos optional inputs are `T | null | undefined` on the wire and the canonical optional legs are `T | undefined`, and (c) backend.instructions.md forbids nested ternaries. Presence ternaries are one level each, never nested.

## 5. Verification

| Check | Result |
|---|---|
| `bun run tsgo` (project-wide) | **0 errors** |
| `sub-loop.ts <file> --lifecycle duplicates` on the resolver AND both edited barrels | **exit 0 on all three** (tsgo / oxlint / biome:check / lint:type-aware / jscpd) |
| In-memory schema probe (`graphQLSchema` assembled via `gqlSchema.ts`, no generated-file writes) | `Mutation.submitSessionReport(id: ID!, input: SubmitSessionReportInput!): SessionReport!` — type non-null, args exact; input whitelists closed (`SubmitSessionReportInput: homework,previousGrades,studentRatingByTeacher,teacherNotes`) |
| Anti-pattern grep | no spread / no try|catch / no console / no `as number` / no `ctx.t` in code (comments excepted) |

**TE note (coverage carrier):** `authScopes` is runtime scope-auth metadata and does not appear in SDL — the 401 (anonymous) / 403 (non-teacher) pre-resolver assertions are carried by the Phase 5 wire suite (5.1) via `postDocument`/`loginActor`; this task's layer-level check is the schema probe above (registration + nullability + arg shapes).

## 6. Security audit (3.2.SEC)

- `$all` conjunction present in source (line ~100 region); anonymous → 401 pre-resolver, non-teacher → 403 pre-resolver; service re-asserts role + governance as defense in depth.
- Identity from `ctx.user.id` ONLY; no input-derived actor/tenant selection (the input whitelist contains no identity fields — 3.1 §4).
- Locale from `ctx.locale` only; idempotency/transport surfaces not consulted.

## 7. Carry-forward (for 3.4 / 5.1)

- 3.4 SDL pins: `Mutation.submitSessionReport(id: ID!, input: SubmitSessionReportInput!): SessionReport!` exactly as probed; add `SESSION_REPORT_ALREADY_EXISTS` to the error-code registry if that step lands here.
- 5.1 wire tests: 401 anonymous, 403 student/parent/admin (pre-resolver, no resolver body execution), VALIDATION on `id = 0`/`-5`/non-integer via the boundary guard (English copy — the localized copy path is the service's own re-assert), then the service-level matrix already scripted in the journey test.
