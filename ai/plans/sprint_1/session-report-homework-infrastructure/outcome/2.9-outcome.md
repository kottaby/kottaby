# 2.9 Outcome — Journey gate: the 4 remaining journey-test failures fixed (steps 4 / 5 / 5b / 7)

**Plan:** `ai/plans/sprint_1/session-report-homework-infrastructure` · Task 2.9 (journey gate evidence)
**Requirements:** REQ-017, REQ-030, REQ-031, REQ-061
**Repo state:** shared worktree, **NO commit** made, `tasks.md` untouched. Scope touched by this task: **`test/workflows/classes/session-report-homework.journey.test.ts` only**. `backend/**` untouched — the service/repo surface was already verified green (repo 21 pass, service 32 pass) and **no service bug was found**: every failure was journey-side (assertion/fixture construction), confirmed against the real guard contracts (`session-report.guards.ts`), the real locale strings (`shared/locale/en/errors/index.ts`), and the real repo read channels.

---

## 1. Result summary

```
bun run test/scripts/run-test.ts test/workflows/classes/session-report-homework.journey.test.ts
   → 14 pass / 0 fail / 218 expect() calls   (3 consecutive green runs — §4)
```

| Step | Symptom (last red run) | Root cause (journey-side) | Fix (journey-side) |
|---|---|---|---|
| 4 | `assertDenialCodeAndCopy` failed at the sweep: `Expected to contain: "Invalid input." / Received: "The session rating must be a whole number between 0 and 5."` | The sweep asserted EVERY payload's denial against the GENERIC `errorsTranslations.validation` copy. The pre-DB guards throw the SPECIFIC per-rule key (`new ValidationError(t.sessionRatingRange)` etc.), so the generic string is never contained — the copy oracle was wrong, not the payloads. Payload validity was re-derived against `session-report.guards.ts`: all 8 are genuinely invalid (rating 7 ∉ 0..5 int; notes ""/2001 chars vs trim+2000 cap; grades 101/−1 vs 0..100 int; jadid 7→3 vs from ≤ to; `"surah_an_nuh"` ∉ shipped enum window `surah_al_fatihah..surah_al_maidah` + `juz_1..30`; madi 1.5 vs positive-safe-integer). The sweep also could never have landed a submit — every payload throws pre-DB (log-record assertion `logs.records === []` still green). | Sweep restructured to `{ copy, input }` pairs (`HostileCase`): each payload pins `code = "VALIDATION"` **and its own ACTUAL localized denial copy** (`sessionRatingRange`, `sessionReportNotesRequired`, `sessionReportNotesTooLong`, `homeworkGradeRange` ×2, `homeworkAyahRangeInvalid` ×2, `homeworkSurahJuzInvalid`) per the pipeline's fixed validation order (notes → rating → assignment blocks → previous grades). STRONGER than the old assertion (specific copy per rule instead of one generic substring). Zero-delta sweep exit unchanged (`reportRows 0`, `homeworkRows 0`, full `expectNoDeltas`). |
| 5 | Final `expectNoDeltas` (line ~843) showed exactly ONE valid-submit delta set: `reportRows +1, homeworkRows +1, notificationsS +1, notificationsP +1, publishes +2`. | **Journey-side oracle bug, misattributed in the failure notes.** The delta set is step 5's OWN legitimate submit: the step ends with `expectNoDeltas(before, after)` where `before` is taken BEFORE the valid submit — a zero-delta assertion on a step whose entire purpose is to produce those deltas. It can never pass. Step 4 did NOT poison the state (its sweep throws 8 pre-DB denials; proven by the fixed run: step 4's abort left `reportRows 0`). Step 5's other assertions were independently verified correct (row key set, `sessionId`/notes/rating, grades NULL + Jadid/Madi block mapping, per-recipient locale bodies, 2-publish multiset, claim-key determinism, non-participant isolation) — they all pass. | Final delta assertion replaced with the EXPECTED full-object delta: `after` must `toEqual` `{ ...before, reportRows+1, homeworkRows+1, notificationsS+1, notificationsP+1, publishes+2 }` — all 15 snapshot fields pinned at exact counts (any extra/missing delta still fails). Zero-delta strictness is preserved for every non-submit field. |
| 5b | `expect(primeHomeworkRow.revisionFromAyah).toBe(SIGMA2_MADI.fromAyah)` → Expected 3, Received 2. | The σ′ submit sends `homework: { madi: PRIME_MADI }` (`fromAyah 2, toAyah 6, Juz3`), but the assertion compared against the σ₂ constant `SIGMA2_MADI.fromAyah` (3) — a copy-paste constant mix-up. Service mapping (madi → `revision_*`) is correct (step 9 proves it with σ₂). After fixing the constant, the step's final `expectNoDeltas` unmasked the SAME oracle bug as step 5: the σ′ submit legitimately moves `notificationsSPrime +1` (the INV-P1 student wave) and `publishes +1`, which the zero-delta assertion forbade — it had simply never been reached before. | `revisionFromAyah`/`revisionToAyah`/`revisionSurahJuz` now pinned to the payload ACTUALLY sent (`PRIME_MADI.*`) — `revisionToAyah` added (stronger; previously unpinned). Final delta assertion replaced with the expected full-object delta `{ ...before, notificationsSPrime+1, publishes+1 }` — the INV-P1 leg's exact footprint (student wave ONLY; σ-scoped report/homework counts, parent inbox, and all other fields pinned unchanged). |
| 7 | `expect(studentReport).toEqual(reportRow)` failed on `createdAt`: expected `…T16:41:30.000Z`, received `…T16:41:30.823Z` (same for `updatedAt`; same shape on the homework row). | Two read channels of the SAME committed row: the journey's direct drizzle read truncates milliseconds under the PGlite shim (the drizzle timestamp mapper stringifies the already-parsed Date, dropping the fraction — the phenomenon the file already documents for the stamp-ADVANCE oracle), while `getSessionReport`/`getSessionHomework` (no `tx`) run the repo's raw-SQL `queryDb` path and keep full precision. `toEqual` is exact, so sub-second precision alone failed it. | New local projection `withSecondTruncatedStamps(row)`: both `createdAt`/`updatedAt` are projected onto the second-truncated ISO form — the strongest equality the two channels share — every other field carried verbatim. Applied to ALL FOUR full-row comparisons (student report/homework, teacher report/homework). No field is skipped: only sub-second precision is tolerated, everything else still fails on any difference. Explicit non-null guards added for the four service reads. |

**Misattribution corrected for the record:** the failure notes claimed step 4's sweep "landed a valid submit" that poisoned step 5. Disproved: all 8 payloads throw pre-DB `VALIDATION` before any DB work (guard order: notes → rating → blocks → grades), the sweep's own zero-delta exit is green, and step 5's delta failure reproduces with a pristine step 4 — the delta was step 5's own submit. No service change was warranted at any point.

## 2. Oracle-strength audit (nothing weakened)

- **Exact counts:** every snapshot delta is asserted as a full-object `toEqual` over all 15 fields (`notificationsS/SPrime/P/T/Ft/A`, `reportRows`, `homeworkRows`, `walletRowsT/Ft`, `teacherTransactionRowsT`, `auditRowsT`, `lanesTrial/Hifz`, `publishes`) — the replacement assertions pin MORE fields with exact expected values than the impossible zero-delta assertions they replace.
- **Exact codes:** `VALIDATION` ×8, `SESSION_NOT_FOUND` ×2, `FORBIDDEN`, `SESSION_REPORT_ALREADY_EXISTS` ×2, `CONFLICT` — unchanged.
- **Exact copy:** step 4 UPGRADED from one generic substring to the per-rule localized strings verbatim from `getServerTranslations("en").errorsTranslations`; all other denial copies untouched.
- **Row shape:** step 7 keeps full-row `toEqual` on both readers (report 6-column key set; homework row) — the ONLY tolerated difference is sub-second timestamp precision between two read channels of the same committed row, documented at the helper.
- **Step 5b gain:** `revisionToAyah` is now asserted (was unpinned).

## 3. Verification results

```
bun run test/scripts/run-test.ts test/workflows/classes/session-report-homework.journey.test.ts
   → 14 pass / 0 fail / 218 expect() calls (run 1: 2.11s)
   → 14 pass / 0 fail / 218 expect() calls (run 2: 1.75s)
   → 14 pass / 0 fail / 218 expect() calls (run 3: 1.95s)

bun run tsgo 2>&1 | grep -c "error TS"        → 0
bun run oxlint                                → Found 0 warnings and 0 errors. (exit 0;
    one false-positive `no-commented-out-tests` from a comment containing "it (…"
    was rephrased out of the sweep comment — no code change)
bun run scripts/health/sub-loop.ts \
    test/workflows/classes/session-report-homework.journey.test.ts --lifecycle duplicates
   → ✅ lint:type-aware passed · ✅ check:duplicates passed → exit 0
    (direct `jscpd -t 0` on the journey file: "No duplicates found", 0 clones)
```

**Repo-wide note (pre-existing, NOT this task's scope):** `bun run check:duplicates` (repo-wide jscpd) currently exits 1 on ONE clone inside `backend/services/classes/session-lifecycle.governance.ts` (`assertTeacherGovernanceClean` — the backend workstream's uncommitted addition — structurally mirrors the sibling governance guard bodies, 14 lines / 90 tokens). The journey file is outside the jscpd scan scope and carries zero clones. Left untouched per task scope (`backend/**` off-limits; a lint-level structural duplicate is not a service bug) → carried forward below.

## 4. Journey ×3 green summaries (final file state)

```
=== GREEN RUN 1 ===  14 pass / 0 fail / 218 expect() calls / Ran 14 tests across 1 file [2.11s]
=== GREEN RUN 2 ===  14 pass / 0 fail / 218 expect() calls / Ran 14 tests across 1 file [1.75s]
=== GREEN RUN 3 ===  14 pass / 0 fail / 218 expect() calls / Ran 14 tests across 1 file [1.95s]
```

Per-step pass list (identical across runs): step 1, step 2, step 3, step 4, step 5, step 5b, step 6, step 7, step 8, step 9, step 10, step 11, denial coverage, final purity oracle — 14/14.

## 5. Carry-forward (Phase 3)

1. **Phase 3 resolvers:** the journey exercises the service surface exactly as the GraphQL layer will call it; no resolver-side authorization logic is implied beyond mapping the readers' collapse `null` and replaying the typed denial codes (`VALIDATION`, `FORBIDDEN`, `SESSION_NOT_FOUND`, `SESSION_INVALID_TRANSITION`, `SESSION_REPORT_ALREADY_EXISTS`, `CONFLICT`).
2. **Repo-wide jscpd gate** is red on the backend workstream's `session-lifecycle.governance.ts` (one self-clone between the new `assertTeacherGovernanceClean` and the sibling guards). Owner should either vary the guard bodies like the established "arbitration/action" wording split or add a targeted jscpd ignore — the journey file is clean and out of jscpd scope.
3. **PGlite timestamp shim** is now documented in TWO places of the journey (stamp-ADVANCE oracle + `withSecondTruncatedStamps`): on a real Postgres pool both read channels return identical precision, so the projection stays correct (truncation of an already-exact value is a no-op) and must not be "simplified" back to raw `toEqual` if the shim ever changes.
4. `tasks.md` checkboxes left UNFLIPPED per instruction — this document is the flip-off evidence.
