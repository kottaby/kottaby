# 2.8 Outcome — SessionReportService READ surface verified (`getSessionReport` / `getSessionHomework`)

**Plan:** `ai/plans/sprint_1/session-report-homework-infrastructure` · Task 2.8 (2.8.TE / 2.8.QL / 2.8.SEC / 2.8.SR evidence)
**Requirements:** REQ-017, REQ-030, REQ-031, REQ-061
**Repo state:** shared worktree, **NO commit** made, `tasks.md` untouched. Scope touched by this task: **`backend/services/classes/session-report.service.test.ts` only** (the read-surface describe); the SUT read surface in `session-report.service.ts` was **NOT modified** — zero service-side fixes needed (§4).

---

## 1. Implementation summary (verification posture)

The read surface under verification is exactly the module recorded in `outcome/2.8-impl-outcome.md` (signatures, `DBQueryExecutor` widening, `isDBTransaction` narrowing rationale live THERE):

```ts
export async function getSessionReport(callerUserId: number, sessionId: number, locale: string, tx?: DBQueryExecutor): Promise<ReportReturnType | null>
export async function getSessionHomework(callerUserId: number, sessionId: number, locale: string, tx?: DBQueryExecutor): Promise<HomeWorkReturnType | null>
```

Both readers share ONE private participant predicate (`resolveVisibleSessionForCaller`): the SAME pre-DB id-shape guard as the write surface (`assertPositiveSessionId` with locale-resolved translations), then the UNLOCKED reuse of `SessionRepository.findTransitionProbe` (plain SELECT — reads never take `FOR UPDATE`, no new repo method), collapsing every non-participant shape into the bare `null`:

- probe `null` (unknown id) → `null`;
- caller is neither `probe.teacherId` nor `probe.studentId` (foreign teacher/student, parent, admin, any foreign role) → `null`;
- participant → one plain `ReportRepository.findBySessionId` / `HomeWorkRepository.findBySessionId`.

**Render purity (source-pinned + test-asserted):** ZERO writes on any table, NO governance re-check (rationale recorded in the helper's JSDoc: the gate is participant-limited to the caller's OWN session, and historical rows survive later governance flips per INV-U5 — a blocked/deleted account's past sessions stay readable to the other participant), and the read path logs NOTHING — the module contains zero `logger.*` calls on the read surface, and the suite asserts spy silence on every read path including its own fail-closed `VALIDATION` denial.

## 2. Read-tier results (2.8.TE)

The read surface ships as its own describe in the 2.7 suite (32 pass / 0 fail / 298 `expect()` calls total; the read describe contributes 7 tests, all `runInRollback` with `tx` passed as the reader's executor):

| # | Test | Result |
|---|---|---|
| 1 | **Participant hits** — teacher AND student read BOTH the report and the homework rows in full; the two participants' reads are `toEqual`-identical; the report row carries exactly the six `REPORT_ROW_KEYS` (`createdAt, id, sessionId, studentRatingByTeacher, teacherNotes, updatedAt`); row ids/fields match the fixture inserts. | pass |
| 2 | **Non-participant nulls** — foreign teacher, foreign student, the session's own parent, and a foreign admin all collapse to EXACTLY `null` on BOTH readers (8 reads), while the participant control read still hits. | pass |
| 3 | **Unknown id ⇒ null; foreign-null ≡ unknown-null byte identity** — `toBeNull()` on both shapes plus `toBe` identity between the two nulls and identical `JSON.stringify` (the SAME bare `null`, no shape discrimination). Homework reader: same identity pair. | pass |
| 4 | **Enumeration probe** — foreign/nonexistent id set (`count()+1_000_000`, `2_000_000_000`, `2_147_483_647`) × two caller shapes (the session's teacher targeting absent ids; a parent caller targeting absent ids) × both readers: all 12 normalized observations collapse to ONE value — the string `"null"` — i.e. foreign ids and nonexistent ids are INDISTINGUISHABLE on both readers (oracle-collapse proven from every caller shape). | pass |
| 5 | **Hostile id matrix** — `0 / −1 / 1.5 / NaN / 2**53` on BOTH readers ⇒ pre-DB canonical `VALIDATION` with the localized copy, thrown BEFORE any DB access; **the read path stays silent even on its own denial** (`logs.records === []`). | pass |
| 6 | **Read purity** — zero writes and zero log lines across hit and null paths: participant hits, non-participant nulls, and an unknown-id null all leave `reports`/`home_work` counts unchanged, zero notification rows, and `session.fee_held` bit-identical. | pass |
| 7 | **Honest participant null** — a completed, report-less, homework-less session passes the participant gate but the plain repository read misses: both readers return `null`, byte-identical to each other, zero logs (the "participant null" is observably the same object as the "denied null"). | pass |

**Mid-storm read (Tier 3 harness, shared with 2.7):** `readStormObservations` races a STUDENT read loop against the N=4 double-submit storm; every observation is either `null` or a FULL row (exact `REPORT_ROW_KEYS` shape — never a partial/fabricated shape), and a throw of any kind fails the test. The storm-3 test additionally re-reads both participants + homework on the default pool path after commit (teacher read `toEqual` student read; homework `currentGrade` still `NULL` per INV-HW3). Observed behavior per the transport is documented in 2.7-outcome §3 (DV-T3): under PGlite the reads interleave BETWEEN serialized attempts (structural leading-`null` → trailing full-row pattern); on a real multi-connection pool the same loop rides WAL snapshot isolation with free interleaving — either way the contract "never throws, never partial" is asserted identically.

## 3. 2.8.SEC — Security & Tenancy Audit

- **Oracle collapse, field-by-field:** foreign-vs-unknown `null` collapse asserted on three levels — `toBeNull()` per shape, `toBe` identity between the two nulls (the same bare `null` object), and `JSON.stringify` byte identity (tests 3, 4, 7). The enumeration probe extends collapse across id space (small/absent/int4-max) AND caller space (participant caller + parent caller) for both readers — no observable exists that lets a caller distinguish "exists but not yours" from "does not exist".
- **No existence leak via latency or shape:** both readers take the identical code path for every denied shape (one probe SELECT → `null`); no branch discriminates non-participant flavors (single predicate — 2.8.SR), so timing-bucket differences cannot arise from denial classification.
- **Logger silence on reads (house rule):** `logs.records === []` asserted on (a) the hostile-id matrix where the readers DENY (the only place a read path could conceivably log), (b) hit reads, (c) non-participant nulls, (d) unknown-id nulls, (e) the honest participant null. Source-pinned: zero `logger.*` references on the read surface — a read can never leak which shape matched, not even into logs.
- **Render purity / tenancy:** zero writes (row counts + `fee_held` asserted unchanged across hit/null paths); no governance re-check on reads is a deliberate, header-documented tenancy ruling — reads expose ONLY the caller's own session rows via the participant gate, so a governed flip cannot retroactively break the OTHER participant's historical records (INV-U5).
- **No `FOR UPDATE` on reads:** the gate reuses the existing unlocked `findTransitionProbe` (no new lock, no new repo surface) — readers can never block or be blocked by the write path's gate lock (mid-storm read test is the live proof).

## 4. Verification results (2.8.QL / fixes)

```
bun run test/scripts/run-test.ts backend/services/classes/session-report.service.test.ts
   → 32 pass / 0 fail / 298 expect() calls   (read describe: 7/7 green)
bun run test/scripts/run-test.ts backend/services/classes
   → 217 pass / 3 skip / 0 fail              (3 skips = pre-existing sibling PGlite transport skips)
bun run tsgo | grep "error TS"
   → 1 error total: the journey test's known RED (TS2578 @ line 110) — ZERO in backend/**
bun run scripts/health/sub-loop.ts backend/services/classes/session-report.service.test.ts --lifecycle duplicates
   → exit 0
```

**Fixes applied in this task — ALL test-side; ZERO service-side:**

1. *(from the shared 2.7 loop)* PGlite FIFO serialization for the mid-storm read loop (reads must never execute inside an aborted shared session — `25P02`; see 2.7-outcome §3/§4, DV-T3).
2. Test-side typing: `sortedKeysOf(row: object)` (removes 4 `as unknown as Record<…>` unsafe assertions); explicit non-null guard on the teacher participant read before the shape pin.
3. Lint: `toBeNull()` over `toBe(null)` ×5, `toHaveLength` over `.length` comparison, `localeCompare` sort comparator, `{ cause: error }` on the mid-storm rethrow.

The read-surface SUT (gate predicate, tx narrowing, both readers) passed every tier on first contact — no behavioral change was needed.

## 5. 2.8.SR — Semantic Review (summary)

- ONE shared participant predicate for both readers (no duplicated participant logic); both readers structurally identical after the gate (one repo call each).
- Reuse of the existing unlocked probe — zero new FOR UPDATE, zero new repo methods, zero dead code on the read surface.
- `DBQueryExecutor` narrowing (`isDBTransaction`) keeps a pool executor handed in for the plain SELECT from reaching the transaction-only probe parameter (mirrors the per-repo private guard shape).

## 6. Carry-forward (Phase 3 / 2.9)

1. **Phase 3 resolvers:** wire `getSessionReport`/`getSessionHomework` per caller identity resolved server-side; the GraphQL layer needs NO additional authorization logic for existence/ownership (the reader returns the collapse `null` for every denied shape — map it to the API's canonical empty/not-found response); the only error the read surface throws is the pre-DB `VALIDATION` for malformed ids.
2. **2.9 journey gate:** the read call sites already typecheck (`SessionReportService.getSessionReport(userId, sessionId, locale)` — 4th arg optional); the journey's read steps run against this SUT unchanged.
3. The 2.7-era TS2339s for both readers are gone; after 2.9 removes the journey's unused `@ts-expect-error` (line 110) the project typecheck is fully clean.
4. `tasks.md` 2.8 checkboxes left UNFLIPPED per instruction — this document is the flip-off evidence.
