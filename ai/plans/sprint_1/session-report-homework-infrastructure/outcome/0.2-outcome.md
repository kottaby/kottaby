# Task 0.2 — Prerequisite Verification (Ground-Truth Gate)

**Plan:** `ai/plans/sprint_1/session-report-homework-infrastructure`
**Repo state:** branch `main` @ `ffce457` (same commit the 0.1 baseline recorded as `origin/main`; the session-lifecycle ticket surface is merged into it). Working tree clean except plan files.
**Method:** read-only verification; only exception = one execution of `scripts/health/sub-loop.ts` (discovery run, item 16). No source file modified.

**Verdict summary: 22 × [OK], 2 × [MISS]** (items 2 — canonical types gap; detailed below). Every MISS is an additive-gap, not a blocker: plan §2.4 already scopes task 1.3 as "EXTEND in place"; the gap is that one type file must be CREATED and one type must be ADDED, contradicting plan.md §2.1's "exist with Select/Insert pairs only" claim (reconciled in item 2 notes).

---

## 1. Table modules `reports.ts` / `home-work.ts`

**[OK]**
- `backend/db/schema/classes/reports.ts:21` — `export const reports = pgTable(` — imports `check, index, integer, pgTable, text, timestamp` (line 2) + `session` FK (line 3). NO `unique`/`uniqueIndex` import; NO `teacher_id` (doc comment line 15: "NO `teacher_id` column").
- `backend/db/schema/classes/reports.ts:37-41` — CHECK `reports_student_rating_by_teacher_check` (`>= 0 AND <= 5`) + `index("reports_session_id_idx")`. Doc comment lines 10-13 state session_id is NOT unique today (R1 posture confirmed by import surface + body).
- `backend/db/schema/classes/home-work.ts:22` — `export const homeWork = pgTable(` — imports `check, index, integer, pgTable, timestamp` (line 2) + `surahJuzRef` (line 4). NO `unique` import (R3 confirmed).
- `backend/db/schema/classes/home-work.ts:31,35` — `currentGrade: integer("current_grade")` / `revisionGrade: integer("revision_grade")` — both ALREADY nullable (no `.notNull()`; R2 verdict → **no-op**).
- `backend/db/schema/classes/home-work.ts:44-45` — CHECKs `home_work_current_grade_check` / `home_work_revision_grade_check` (0–100). R5 confirmed both.

**Carry-forward (Phase 1):** only conditional DDL left is ADD `unique("reports_session_id_unique")` / `unique("home_work_session_id_unique")` (tasks 1.1/1.2); grade nullability is already correct — do NOT "relax".

## 2. Canonical types `report.types.ts` / `home-work.types.ts`

**[MISS — partial]**
- `backend/types/classes/report.types.ts:3` — `export type ReportSelectType = typeof reports.$inferSelect;` — this is the file's ONLY export (4-line file).
- **`ReportInsertType` DOES NOT EXIST** anywhere (`rg "ReportInsertType"` → 0 code hits; only plan docs).
- **`backend/types/classes/home-work.types.ts` DOES NOT EXIST.** `rg "HomeWorkSelectType|HomeWorkInsertType"` → 0 code hits.
- `backend/types/classes/index.ts:1-4` — barrel re-exports `report.types`, `session.types`, `session-notification.types`, `session-request-idempotency.types`.

**Reconciliation:** plan.md §2.1 asserts the types "exist with Select/Insert pairs only" — false for the homework pair and the insert types. Task 1.3 scope widens: CREATE `home-work.types.ts` (`HomeWorkSelectType`/`HomeWorkInsertType`/`HomeWorkReturnType`), ADD `ReportInsertType` + `ReportReturnType` to `report.types.ts`, then extend the barrel. No downstream Phase 1 task may cite "existing" homework types.

## 3. `SurahJuzRef` enum

**[OK]**
- `backend/enum/shared/surah-juz-ref.enum.ts:8-44` — `export enum SurahJuzRef` = 5 surah members (`SurahAlFatihah`…`SurahAlMaidah`) + `Juz1`…`Juz30` = **35 members**. No `isSurahJuzRef` guard in the file (enum only).
- Guard pattern source for task 1.3: `backend/enum/teachers/applicant-status.enum.ts:25` — `export function isApplicantStatus(value: unknown): value is ApplicantStatus {`.

## 4. `surahJuzRef` pgEnum registration

**[OK]**
- `backend/db/schema/enums.ts:76` — `export const surahJuzRef = pgEnum("surah_juz_ref", [` — already registered; consumed by `home-work.ts:4`. Task plan "NO change to enums.ts" holds.

## 5. Governance re-check helper

**[OK]**
- `backend/services/classes/session-lifecycle.governance.ts:39` — **exact exported name is `assertActorGovernanceClean`** (plan name CONFIRMED).
- Signature (lines 39-43): `export async function assertActorGovernanceClean(actorUserId: number, t: ReturnType<typeof getServerTranslations>["errorsTranslations"], tx?: DBTransaction): Promise<void>` — throws `ForbiddenError(t.forbidden)` on `!actor || actor.isDeleted || actor.isBlocked || actor.suspended` (lines 45-52), after one `logger.logDomainError` (line 46).
- Sibling: `assertAdminGovernanceClean` (line 63) adds the admin-role leg.
- Import site precedent: `@/backend/services` barrel (`session-lifecycle.mutation.ts` imports `SessionLifecycleService` from it); the governance module itself is imported by the service surface.

## 6. Oracle-collapse posture to mirror

**[OK]**
- Read-side collapse: `backend/services/classes/session-lifecycle.service.ts:583-605` — `getSessionById(callerUserId, sessionId, tx?): Promise<SessionReturnType | null>`; line 593 `if (!isPositiveSafeSessionId(sessionId)) { return null; }` (malformed ≡ nonexistent, pre-DB), lines 601-603 foreign participant → the SAME `null` (doc lines 566-576: "A nonexistent id and a non-participant caller resolve to the identical `null` (oracle-safe…)").
- Write-side denial: `backend/services/classes/session-lifecycle.transitions.ts:61-72` — `rejectSessionNotFound(...)` = ONE `logger.logDomainError` (`code: "SESSION_NOT_FOUND"`, `entity: "session"`) + `throw new NotFoundError("SESSION", t.sessionNotFound)`.
- State conflict: `transitions.ts:75-86` — `rejectStateConflict(...)` → `ConflictError("SESSION_INVALID_TRANSITION", t.sessionInvalidTransition)`.

## 7. `isUniqueViolation` cause-chain helper

**[OK]**
- `backend/services/shared/user-provisioning.helpers.ts:54` — `export function isUniqueViolation(error: unknown): boolean` — cycle-safe `seen`-set `Error.cause` traversal for PG `23505` (message never consulted). Still at its canonical home (not moved).
- Re-exported through the barrel: `import { isUniqueViolation } from "@/backend/services/shared"` (`backend/services/admin/cold-start-certification.service.ts:65`).
- The 23505 class family also has: `translateDbError` (`backend/lib/errors.ts:201`) and the session-local `isClaimKeyUniqueViolation` (`session-lifecycle.guards.ts:173-184`).

## 8. Recipient-locale copy-composition precedent

**[OK]**
- `backend/services/classes/session-request-notification.service.ts` — this ticket's `notifySessionReportReady` seam must mirror this module:
  - Recipient locale resolution: lines 190-198 (`emitWave`) — `const recipientLocale = recipient.locale ?? defaultLocale;` then `getServerTranslations(recipientLocale).notificationsTranslations` → `composeWaveCopy(...)`. `defaultLocale` from `@/shared/locale/AppLocale` (line 47).
  - Copy slots: `composeWaveCopy` (lines 123-169) — flat `tNotifications.eventSessionAcceptedTitle` / `eventSessionAcceptedBody(counterparty.fullName)` function-style interpolation; exhaustive switch + `const exhaustive: never` tail (109-113, 163-167).
  - Engine call: `NotificationEngine.emitForUser(input, recipientLocale, tx, options)` (line 211 in-tx; line 221 tx-less); `NotificationEmitInput` with `idempotencyKey: \`session:${sessionId}:${waveKind}\`` (line 207), `relatedEntityType: "session"` (205).
  - Receipt contract: returns `NotificationDeliveryReceipt` verbatim, NEVER publishes (header lines 12-19).

## 9. Notification engine module paths

**[OK]**
- `backend/services/notifications/notification-engine.service.ts:41` — `export namespace NotificationEngine`:
  - `emitForUser(input: NotificationEmitInput, locale: string, tx?: DBTransaction, options?: NotificationEngineCallOptions): Promise<NotificationReturnType | NotificationDeliveryReceipt>` (line 42-47) — in-tx ⇒ receipt w/o publish; tx-less ⇒ publishes internally.
  - `emitForUsers` (line 79) — batch variant returning `NotificationDeliveryReceipt`.
  - `publishReceipts(receipts: readonly NotificationDeliveryReceipt[], locale: string, options?: NotificationEngineCallOptions): Promise<void>` (line 116-122) — post-commit publish.
- `NotificationEngineCallOptions` — same file, lines 34-37: `interface NotificationEngineCallOptions { readonly transport?: NotificationFanoutTransport; readonly cache?: NotificationIdempotencyClaimCache; }`.
- `NotificationType.SessionCompletion = "session_completion"` — `backend/enum/notifications/notification-type.enum.ts:7`.
- `NotificationDeliveryReceipt` — `backend/types/notifications/notification.types.ts:81` (re-exported via `@/backend/types`).
- No GraphQL resolver passes `NotificationEngineCallOptions` today (options are service/test-tier seams — `rg` over `backend/graphql` → 0 hits); callers are services (`parent-link-request.helpers.ts:36` imports `isUniqueViolation` from the user-provisioning module, not engine options).

## 10. `SessionRepository.findTransitionProbe`

**[OK]**
- Public delegation: `backend/db/repo/classes/session.repository.ts:351-355` — `export async function findTransitionProbe(id: number, tx?: DBTransaction): Promise<SessionTransitionProbeRowType | null>` (delegates to the impl module).
- Implementation: `backend/db/repo/classes/session.repository.helpers.ts:187-206` — projection `id, status, studentId, teacherId, startedAt` (lines 188-194); Drizzle select on tx path, raw parameterized `queryDb` otherwise (raw SQL at 200-204).
- **NO FOR UPDATE** on `session` rows anywhere in the repo (plan D2 claim holds): `rg '\.for\("update"\)|forUpdate' backend` → only `backend/db/repo/teachers/teacher.repository.ts:119` (teacher certification lock) + its pinning test `backend/db/test/repo/teachers/teacher.repository.test.ts:353`. `lockForReportGate` will be the FIRST session-row FOR UPDATE.
- Probe row TYPE: `backend/types/classes/session.types.ts:72` — `export type SessionTransitionProbeRowType = Pick<SessionSelectType, "id" | "status" | "startedAt" | "studentId" | "teacherId">`. Note the plan's `lockForReportGate` sketch (`SELECT id,status,teacherId,studentId`) omits `startedAt` — either reuse the full probe type or define a narrower one consciously.

## 11. Error class paths & constructor signatures

**[OK]** — the hierarchy lives at **`backend/lib/errors.ts`** (NOT `backend/errors/`; there is no such directory):
- `backend/lib/errors.ts:19-29` — `DomainError extends GraphQLError`, ctor `(code: string, message: string, options?: GraphQLErrorOptions)`, sets `extensions.code`.
- `backend/lib/errors.ts:37-41` — `NotFoundError(entity: string, message: string, options?)` — code auto-generated `${entity.toUpperCase()}_NOT_FOUND` ⇒ `new NotFoundError("SESSION", msg)` → `SESSION_NOT_FOUND`. Pass entity name, never full code (doc rule).
- `backend/lib/errors.ts:171-183` — `ConflictError` **two-arg `(code, message)` overload CONFIRMED**: `constructor(message: string, options?)` / `constructor(code: string, message: string, options?)`; default form emits code `"CONFLICT"` (line 180).
- `backend/lib/errors.ts:65-130` — `ValidationError` overloaded ctor: `(message)` → code `"VALIDATION"` (lines 113-123); `(code, message, options?)`; `(message, fields)`; full `(code, message, options, fields)`. Localized messages are passed as the `message` arg (e.g. guards throw `new ValidationError(t.validation)`, `session-lifecycle.guards.ts:128`); exposed `code` = `"VALIDATION"` by default. Optional `fields?: readonly ApiFieldErrorType[]` mirrored into `extensions.fields` (lines 148-156).
- Bonus re-exports on the same barrel (lines 254, 260): `error-code-taxonomy` (`ERROR_CODE_HTTP_STATUS`, `isErrorCode`, `normalizeErrorCode`) and masking helpers (`finalizeGraphqlErrors`, `maskInternalError`).

## 12. Domain error-code taxonomy & registry

**[OK]**
- `docs/graphql/domain-error-extensions-code.md` — exists (238 lines). Producer-side throw conventions + subclass table (line 34-43); header (line 3) defers the code↔HTTP taxonomy to `docs/graphql/error-handling-contract.md`.
- **`SESSION_REPORT_ALREADY_EXISTS` is NOT claimed** — `rg` over docs/, backend/, test/ → zero hits (only the plan's own files).
- **Where codes are "registered":** there is NO per-domain code registry. The typed registry is the 9-category transport union `ErrorCode` (`backend/types/errors/api-error.types.ts:38-47`: BAD_REQUEST, UNAUTHORIZED, FORBIDDEN, CONFLICT, DUPLICATE_REQUEST, VALIDATION, RATE_LIMITED, SERVICE_UNAVAILABLE, INTERNAL_SERVER_ERROR) + `ERROR_CODE_HTTP_STATUS` (`backend/lib/errors/error-code-taxonomy.ts:41-51`, frozen). Custom domain codes are legal pass-through transport values outside the nine categories (`error-handling-contract.md` line 56); they ride typed error CLASSES (`ConflictError(code, msg)` overload) and never resolve through the taxonomy.
- `SESSION_INVALID_TRANSITION` is defined only at its throw sites: `backend/services/classes/session-lifecycle.transitions.ts:85` and `session-lifecycle.confirmation.ts:103` — both `new ConflictError("SESSION_INVALID_TRANSITION", t.sessionInvalidTransition)`. `CONFLICT` is `ConflictError`'s default code (`backend/lib/errors.ts:180`).
- Carry-forward: `SESSION_REPORT_ALREADY_EXISTS` lands as a new `ConflictError("SESSION_REPORT_ALREADY_EXISTS", t.sessionReportAlreadyExists)` throw site + a doc amendment; NO taxonomy-array edit is required or expected.

## 13. `docs/sessions/session-lifecycle.md` §10 consumer table

**[OK]**
- §10 "Consumer Guidance" — `docs/sessions/session-lifecycle.md:136-147`.
- The current INV-S7/S8 rows (§2.3 invariant binding, lines 66-67):
  - line 66: `| **INV-S7** (report only on `completed`) | **Upstream-owned** — no report surface exists in this slice. |`
  - line 67: `| **INV-S8** (homework gated on report) | **Upstream-owned** — same. |`
- The REQ-072 §10 amendment target row — line 141: `| **this ticket** (reports) | INV-S7 gating is the dual-confirmation ticket's; reports hang off `session_id` (C.4 removed the redundant teacher FK). One recitation per session (C.5) — write it via the recitation ticket's surface, never from the lifecycle. |` (this row is what REQ-072 rewrites; the §2.3 rows 66-67 also say "upstream-owned" and need the ownership flip to this ticket).

## 14. `test/workflows/helpers/` + AGENTS.md

**[OK]** — all present (Phase 2 journey task does NOT need to scaffold):
- `test/workflows/AGENTS.md` — exists.
- `test/workflows/helpers/index.ts:22-29` — pure `export *` barrel over: `actor-context`, `journey-actor-fixtures`, `journey-cleanup`, `journey-fixture-registry`, `journey-fixtures`, `session-cast`, `spied-transport`, `tracked-fixtures`.
- **`SpiedFanoutTransport`** — `test/workflows/helpers/spied-transport.ts:49` (class implements `FanoutTransportLike` line 24; records frozen `FanoutPublishCall { userIds, payload }`; inspection members `calls` / `publishCount` / `lastCall`).
- Cast helpers: `session-cast.ts` — `SessionJourneyCast` (line 120), `buildSessionJourneyCast` (280), `journeyPrefix` (314). Others: `actor-context.ts` `provisionStudentActor`/`provisionCertifiedTeacherActor`/`provisionParentActor`/`provisionAdminActor` (88/105/122/136); `journey-fixtures.ts` `createJourneyCast` (251), `linkStudentToParentFixture` (157), `setGovernanceFixture` (179), `catchJourneyError` (226); `tracked-fixtures.ts` `TrackedFixtures` (173); `journey-cleanup.ts` `journeyCleanup` (62); self-test + smoke suites present.

## 15. `backend/db/test/entity-setup.ts` fixture factory

**[OK]** — key exports (all take `tx` first + `overrides: Partial<*SelectType>` last):
- `createTestUser(tx, overrides)` (line 70) — governance flags overrideable (`isDeleted`/`suspended`/`isBlocked`, role; lines 82-87) ⇒ teacher governance state control.
- `createTestStudent(tx, userId, overrides)` (100) — `parentId: null` default, overrideable (line 113) ⇒ parent linking; `createTestParent(tx, userId)` (127).
- `createTestSession(tx, teacherId, studentId, overrides)` (299) — **status control confirmed**: every lifecycle instant override-driven; e.g. `{ status: SessionStatus.Completed, startedAt, endedAt, confirmedByTeacherAt }` (doc lines 290-297).
- `createTestSessionReport(tx, sessionId, overrides)` (340) — already exists (rating default 5, CHECK-bounded).
- `createTestTeacherRow(tx, userId, overrides)` (482) — `isApproved: true` default (491) ⇒ teacher verification-state control.
- Also: `createTestApplicant` (139), `createTestAdmin` (165), `createTestPlan` (176), `createTestSubscription` (218), `createTestStudentPayment` (260), `createTestEvaluation` (372), `createTestWallet` (410), `createTestTeacherTransaction` (446). NOTE: no `createTestHomeWork` exists yet — Phase 2 fixtures will need one (or inline inserts).

## 16. `scripts/health/sub-loop.ts` per-file instruction discovery

**[OK]** — ran ONCE: `bun run scripts/health/sub-loop.ts backend/db/schema/classes/reports.ts --lifecycle biome`. Discovery output (verbatim mapping):

```
ℹ  File: backend/db/schema/classes/reports.ts
ℹ  Lifecycle: biome (depth: 1)

━━━ Applicable Rule Files ━━━

Instruction files (read before fixing):
  ⚠  .github/instructions/backend.instructions.md

AGENTS.md files (read before fixing):
  ✓  AGENTS.md
  ✓  backend/AGENTS.md

Fix-Or-Report Rule:
  • If a rule violation can be fixed within THIS file: fix it.
  • If fixing requires modifying ANOTHER file: do NOT modify that file.
    Report the cross-file dependency to the orchestrator instead.

Oxlint Fix Patterns (see `docs/quality/linting-rules.md` for full details):
  • no-unsafe-type-assertion / no-await-in-loop / consistent-function-scoping /
    no-object-type-as-default-prop / no-unsafe-enum-comparison / no-shadow /
    consistent-return / no-map-spread / no-underscore-dangle
  • NEVER add oxlint-disable comments — fix the root cause
```

Then: tsgo / oxlint / biome:check all passed for this file (`✅ All checks for lifecycle "biome" passed`). Note: `backend/db/repo/AGENTS.md` and `backend/db/test/AGENTS.md` exist in-repo but are NOT printed at depth 1 for a schema file — cite them only when working files under those subtrees.

## 17. GraphQL surface-test files

**[OK]**
- `backend/graphql/test/schema-surface.test.ts` — 1192 lines. Baseline inventory = frozen `PRE_3_1_*` arrays: `PRE_3_1_QUERY_FIELDS` (109), `PRE_3_1_MUTATION_FIELDS` (119), `PRE_3_1_ENUMS` (132), `PRE_3_1_TYPE_NAMES` (214); post-baseline additions pinned by name (header lines 14-35) + codegen-sync proof (committed `frontend/graphql/generated/schema.graphql` byte-identical to `printSchema(lexicographicSortSchema(...))`, lines 53-62). The new additions must extend these inventories.
- Session SDL test file: **`backend/graphql/test/session-sdl.test.ts`** (255 lines) — REQ-060 exact-contract parity; imports the session Pothos module side-effect-style (line 47) and asserts against a fresh `toSchema()` emission; enum single-registration + member parity + exact `Session` field order (`id` FIRST).
- Related static tier: `backend/graphql/test/sdl-static-assertions.test.ts`.

## 18. Frontend scheduling documents barrel + Apollo cache

**[OK]**
- `frontend/graphql/sharedDocuments/scheduling/` contains: `index.ts`, `session.documents.ts`, `session-lifecycle.documents.ts`, `session-disputes.documents.ts`, `session-reads.documents.ts`.
- Barrel structure: `index.ts:11` re-exports ONLY `./session.documents`; `session.documents.ts:34-36` re-exports the three child modules (`session-disputes`, `session-lifecycle`, `session-reads`). So the hub is `session.documents.ts`, not a flat index. this ticket either adds a new `session-report.documents.ts` + one line in `session.documents.ts` (+ index untouched) or extends `index.ts` — follow the hub pattern.
- `frontend/providers/apollo/apolloCache.ts:37-84` — `createApolloCache()` uses `keyFields: false` registrations for embedded value types (`AdminNoteInfo`:48, `HealthCheck`:54, `NotificationListPage`:60, `HandshakeCodeLookup`:66, `AdminAuditLogPage`:72, `OnlineMeetingInfo`:75, PlatformAnalytics family:81+). No report/homework entry needed (both objects carry `id` — normalize naturally), per plan §5.

## 19. Locale machine

**[OK]**
- **Errors namespace:** interface `ErrorsLabels` — `shared/locale/types/errors/labels.ts:21` (flat `readonly` string keys; `forbidden`:23, `validation`:24, `sessionNotFound`:139). Leaf maps: `shared/locale/en/errors/index.ts` + `shared/locale/ar/errors/index.ts` (typed `ErrorsLabels` consts — compile-time parity). Namespace handle: `shared/locale/namespaces/errors/errors.namespace.ts:4` — `defineNamespace<ErrorsLabels>("errors.errors", t => t.errorsTranslations)`.
- **Notifications namespace:** interface `NotificationsLabels` — `shared/locale/types/notifications/index.ts:26-160` (flat; interpolation placeholders are FUNCTION-VALUED members, e.g. `eventSessionAcceptedBody: (teacherName: string) => string` line 113, `eventParentLinkExpiringBody` line 159). Leaf maps: `shared/locale/en/notifications/index.ts:3` (`notificationsEn`) + `shared/locale/ar/notifications/index.ts` (`notificationsAr`). Handle: `shared/locale/namespaces/notifications/notifications.namespace.ts:4` (`"notifications.notifications"` getter).
- **`Translations` interface** — `shared/locale/types/message.ts:18-35`: slices `errorsTranslations: ErrorsLabels` (21) and `notificationsTranslations: NotificationsLabels` (31). This is the typed interface the new keys extend transitively.
- **`getServerTranslations(locale)` takes exactly ONE argument** — `shared/locale/server-graphql.ts:3-5` (`getTranslations(locale)` from `shared/locale/server.ts:15-17`, unknown locale → `defaultLocale`).
- **Parity harness:** `shared/locale/notifications-namespace.parity.test.ts` (MANDATED_KEYS inventory line 53, ar-script sweep, template pins, registry-wiring check) and `shared/locale/errors-namespace.parity.test.ts`; sibling pattern per namespace. New report/homework keys = extend both leaf maps + both interfaces + both parity suites' inventories. Namespace registry: `shared/locale/namespaces/index.ts`.

## 20. the session-lifecycle ticket session-lifecycle surface (merged)

**[OK]** — all on `main` @ `ffce457`:
- `backend/services/classes/session-lifecycle.guards.ts`:
  - `isPositiveSafeInteger(value: number): boolean` (97-99); `isPositiveSafeSessionId(id: unknown): boolean` (111-113); `assertPositiveSafeSessionId(id: unknown, t: errorsTranslations): void` (123-130, throws `ValidationError(t.validation)`).
  - Probe-row widenings: `SESSION_STARTED_STATUS` (62), `SESSION_DISPUTED_STATUS` (69), **`SESSION_COMPLETED_STATUS: string = SessionStatus.Completed` (76)**.
  - `isClaimKeyUniqueViolation(error: unknown): boolean` (173-184).
- `SessionStatus` enum — `backend/enum/scheduling/session-status.enum.ts:7-13`; member for completed = **`SessionStatus.Completed = "completed"`** (line 10); other members Scheduled/Started/Cancelled/Disputed.
- **`withTransaction`** — canonical wrapper `backend/lib/db/with-transaction.ts:27-35`: `export async function withTransaction<T>(outerTx: DBTransaction | undefined, fn: (tx: DBTransaction) => Promise<T>): Promise<T>` — `outerTx ? outerTx.transaction(fn) : db.transaction(fn)` (SAVEPOINT vs top-level). Import path used by services: `import { withTransaction } from "@/backend/lib/db/with-transaction";` (`session-lifecycle.service.ts:84`, `parent-link-request.service.ts:88`). (An identical local copy exists for the engine at `backend/services/notifications/notification-engine.helpers.ts:18` — services use the lib one.)

## 21. Repository conventions

**[OK]**
- Barrel: `backend/db/repo/classes/index.ts:13-14` — `export * from "./session.repository"; export * from "./session-request-idempotency.repository";` (new repos get one line here; top-level `backend/db/repo/index.ts` re-exports this barrel).
- Convention (header `session.repository.ts:17-33`): ONE `namespace` per file (canonical export `SessionRepository`); **every method takes `tx?: DBTransaction` as its LAST parameter**; reads run on tx when supplied else raw parameterized `queryDb`; writes on `tx ?? db`; NO prepared statements for dynamic/transactional paths; no business logic in repo.
- Type paths: `DBTransaction` — `backend/types/db.types.ts:23` (`Parameters<Parameters<typeof db.transaction>[0]>[0]`); `DBQueryExecutor` — `backend/types/db.types.ts:30` (`DBTransaction | Pool | PoolClient`); both consumed via `@/backend/types`.
- Repo tests: `backend/db/test/repo/classes/` (e.g. `session.repository.test.ts`, `session-request-idempotency.repository.test.ts`). Helpers imported EXACTLY as: `import { expectRepoError, runInRollback } from "@/backend/db/test/test-utils";` (`session.repository.test.ts:71`); definitions: `runInRollback` test-utils.ts:34, `expectRepoError` :77, `constraintNameOf` :111. `expect(...).rejects.toThrow()` inside `runInRollback` is forbidden (deadlock; use `expectRepoError` — domain-error doc anti-pattern line 186-201).

## 22. Pothos conventions

**[OK]**
- **Enum registration** — `backend/graphql/pothos/shared/enum.pothos.ts`: pattern is `export const XPothosEnum = gqlSchemaBuilder.enumType(X, { name: "X" })` (e.g. `SessionStatusPothosEnum`:114-116, `ApplicantStatusPothosEnum`:101-103, `NotificationTypePothosEnum`:215-217). Each enum registered ONCE; domain files import the registered Pothos enum; after registration run `bun run generate:gqlSchema` + `bun codegen` (header lines 28-29). `SurahJuzRefPothosEnum` does NOT exist yet (Phase 1 adds it).
- **Scalar** — `backend/graphql/pothos/shared/scalar.pothos.ts:30` — `gqlSchemaBuilder.addScalarType("DateTime", DateTimeResolver, {});` — GraphQL scalar name is **`DateTime`**; fields reference by NAME (`type: "DateTime"`).
- **Object construction** — `backend/graphql/pothos/classes/session.pothos.ts:134` — `export const SessionPothosObject = gqlSchemaBuilder.objectRef<SessionReturnType>("Session").implement({...})` (objectRef+implement, repo-wide pattern — NOT `objectType`). Exhaustive mapper pattern: `toSessionStatus(status): SessionStatus` (lines 62-80) — switch with one case per member, NO default, `const exhaustive: never = status; throw ...` tail. `id` exposure: **`t.exposeID("id")`** (line 138 — not `t.globalID`); timestamps: `t.expose("createdAt", { type: "DateTime" })` (196) / nullable variants (179-183, 192-194).
- **Classes barrel: `backend/graphql/pothos/classes/index.ts` DOES NOT EXIST** (directory holds only `session.pothos.ts`, `session-filter-input.pothos.ts`, `create-session-input.pothos.ts`). Plan's "create-if-absent" is accurate — it must be created for `report.pothos.ts`/`home-work.pothos.ts`.
- **Mutation example** — `backend/graphql/mutation/classes/session-lifecycle.mutation.ts:87-141`: `gqlSchemaBuilder.mutationField("createSession", t => t.field({ type, args, authScopes, resolve }))`; `authScopes: { $all: { authenticated: true, role: [UserRole.Student] } }` (98-103 — plain maps degrade to ANY semantics; `$all` is MANDATORY for role legs); `if (!ctx.user) throw new UnauthorizedError("Authentication required.")` TS-narrowing guard (111-113); thin delegation with `ctx.locale` (138); `UserRole` is a VALUE import (77). File has NO named exports; wired via side-effect barrels (`mutation/classes/index.ts:14` `import "./session-lifecycle.mutation";` → `mutation/index.ts` → `gqlSchema.ts`).
- **Query example** — `backend/graphql/query/classes/session-lifecycle.query.ts`: `gqlSchemaBuilder.queryField("sessionById", t => t.field({...}))`; `{ authenticated: true }` role-agnostic + service-side participant oracle; shared registration factory `registerParticipantSessionsField` (110-114); side-effect barrel `query/classes/index.ts:12`.
- **`ctx.t` usage** — `await ctx.t("errorsTranslations")` (async): `backend/graphql/mutation/admin/admin-governance.mutation.ts:61` — `throw new UnauthorizedError((await ctx.t("errorsTranslations")).unauthorized);`.
- **NotificationEngineCallOptions at resolvers:** NO existing resolver passes engine options (zero `backend/graphql` hits) — this ticket will be the first; the option-typed seam comes from the service signature, not resolver precedent.

## 23. GraphQL wire-test harness

**[OK]**
- Harness entry: `setupTestServerLifecycle()` from `@/test/helpers` (barrel `test/helpers/index.ts:6`; impl `test/helpers/test-lifecycle.ts:41-77`) — spawns `next dev --turbopack --port 3066` unless something already answers, probes `{ _health { status } }`.
- In-process client: `testClient` — `test/helpers/graphql-test-helpers.ts:8-19` (ApolloClient HttpLink → `http://localhost:${TEST_PORT}/api/graphql`, `credentials: "include"`, `errorPolicy: "all"`); `extractErrorCode` (:72); `expectMutationError` — `test/helpers/expect-mutation-error.ts` (via barrel).
- **Auth per role:** real `login` mutation over the wire → Bearer token (`loginActor`, `backend/graphql/test/parent-link.wire.test.ts:466-478`), then raw `fetch` helper `postDocument(query, accessToken, variables, extraHeaders)` sending `authorization: Bearer ${accessToken}` (232-250); anonymous via `postAnonymous` (253-265). Actors registered via public `registerUser`; admin rides seeded credentials.
- **Locale:** `Accept-Language` header passed via `extraHeaders` (ar-denial assertion at wire.test.ts:1250 "ar locale — … (Accept-Language negotiation)"); expected copy resolved through `getServerTranslations("en"/"ar").errorsTranslations` — never hardcoded strings (113-114).
- Mandated runner: `bun run test/scripts/run-test.ts <file>` (wire.test.ts:85).

## 24. Migration discipline

**[OK]**
- Ruling (one line): `docs/DATABASE_MIGRATIONS.md:26` — "`db reset` and `db cleanGenerate` are permanently disabled by repo policy. Use `db push` for schema changes and `db migrate` for migration management." (`cleanGenerate` forbidden: lines 18, 30, 133, 153.)
- **Programmatic migrate path:** `backend/db/scripts/migrate.ts` `runMigrations(closePool)` — reads folder via `migrationsFolderForDialect("postgres")` = `backend/drizzle` (line 29 + `scripts/dbActions/generate.ts:10` `DRIZZLE_DIR = "./backend/drizzle"`), auto-bundles custom SQL, runs `ensureIdempotentMigrations`, per-folder transactions.
- **Generate command:** schema edits require a new migration folder under `backend/drizzle/` produced by `drizzle-kit generate --ignore-conflicts --config drizzle.config.ts` — invoked as `bun db` interactive action "3" "Generate Drizzle Schema" → `generateMigrations()` (`scripts/dbActions/actions.ts:18,63`; `scripts/dbActions/generate.ts:89`). (SQLite twin for reference: `bunx drizzle-kit generate --config=drizzle.config.sqlite.ts`, `db:sqlite:generate`.) `bun db push` = `scripts/dbActions/cli-entry.ts` push action (docs line 147).
- **Unique + nullable DDL through migrate:** YES — Drizzle expresses both (`unique("reports_session_id_unique").on(t.sessionId)`; dropping `.notNull()`) so `drizzle-kit generate` emits a plain schema folder the migrate path applies; no custom SQL, no hand-editing (docs prevention rule 2, line 130). Sandbox deviation already recorded by 0.1 baseline: pglite provider ⇒ apply via `bun run backend/db/scripts/migrate.ts` after generate; verify DDL by DB introspection + repo tests.

---

## Carry-forward compendium (exact names Phase 1-5 must reuse)

| Symbol | Exact anchor |
|---|---|
| Governance helper | `assertActorGovernanceClean(actorUserId, t.errorsTranslations, tx?)` — `backend/services/classes/session-lifecycle.governance.ts:39` |
| Transition probe | `SessionRepository.findTransitionProbe(id, tx?)` — `session.repository.ts:351`; impl `session.repository.helpers.ts:187`; row type `SessionTransitionProbeRowType` = `Pick<SessionSelectType, "id"\|"status"\|"startedAt"\|"studentId"\|"teacherId">` — `backend/types/classes/session.types.ts:72` |
| FOR UPDATE precedent | `teacher.repository.ts:119` only — no session-row lock exists; `lockForReportGate` is new |
| Id-guard family | `assertPositiveSafeSessionId` / `isPositiveSafeSessionId` / `isPositiveSafeInteger` — `session-lifecycle.guards.ts:123/111/97`; `SESSION_COMPLETED_STATUS` :76 |
| Errors | `backend/lib/errors.ts` — `ConflictError(code, message)` :171; `NotFoundError("SESSION", msg)` → `SESSION_NOT_FOUND` :37; `ValidationError` default code `"VALIDATION"` :65 |
| 23505 translation | `isUniqueViolation` — `backend/services/shared/user-provisioning.helpers.ts:54` (via `@/backend/services/shared`) |
| Tx wrapper | `withTransaction(outerTx, fn)` — `backend/lib/db/with-transaction.ts:27` |
| Notification seam | `NotificationEngine.emitForUser` / `publishReceipts` — `notification-engine.service.ts:42/116`; `NotificationEngineCallOptions` :34; `NotificationType.SessionCompletion` — `notification-type.enum.ts:7`; `NotificationDeliveryReceipt` — `backend/types/notifications/notification.types.ts:81`; locale-composition precedent — `session-request-notification.service.ts:190-198` (`recipient.locale ?? defaultLocale`) |
| Locale machine | `getServerTranslations(locale)` ONE arg — `shared/locale/server-graphql.ts:3`; `Translations` — `shared/locale/types/message.ts:18`; `ErrorsLabels` — `types/errors/labels.ts:21`; `NotificationsLabels` — `types/notifications/index.ts:26`; leaf maps `shared/locale/{en,ar}/{errors,notifications}/index.ts`; parity suites `shared/locale/{errors,notifications}-namespace.parity.test.ts` |
| Pothos | enums: `gqlSchemaBuilder.enumType(X, { name })` — `enum.pothos.ts`; scalar name `"DateTime"` — `scalar.pothos.ts:30`; objects: `gqlSchemaBuilder.objectRef<T>("Name").implement(...)` + `t.exposeID("id")` + `toSessionStatus`-style exhaustive mappers — `session.pothos.ts:134/138/62`; mutation/query: side-effect registration, `$all` conjunction mandatory for role legs — `session-lifecycle.mutation.ts:98`, `session-lifecycle.query.ts`; `ctx.t("errorsTranslations")` awaited — `admin-governance.mutation.ts:61`; **`pothos/classes/index.ts` barrel does NOT exist — create it** |
| Wire harness | `setupTestServerLifecycle` + `testClient` + `expectMutationError` — `@/test/helpers`; Bearer via `postDocument` + `loginActor`; `Accept-Language` via extraHeaders — `parent-link.wire.test.ts` |
| Test fixtures | `backend/db/test/entity-setup.ts` — `createTestUser`(governance overrides) :70, `createTestSession`(status overrides) :299, `createTestSessionReport` :340, `createTestTeacherRow`(isApproved) :482; `runInRollback`/`expectRepoError`/`constraintNameOf` — `backend/db/test/test-utils.ts:34/77/111`; journey cast + `SpiedFanoutTransport` — `test/workflows/helpers/` (NO scaffolding needed) |
| SDL gates | `backend/graphql/test/schema-surface.test.ts` (`PRE_3_1_*` arrays :109/:119/:132/:214) + `backend/graphql/test/session-sdl.test.ts` |
| Migrations | generate: `drizzle-kit generate --ignore-conflicts --config drizzle.config.ts` (bun db action 3) → new folder in `backend/drizzle/`; apply: `bun run backend/db/scripts/migrate.ts` (pglite) / `bun db migrate`; `db reset` + `db cleanGenerate` FORBIDDEN |

### Reconciliation notes for the orchestrator (MISS-driven)
1. **Item 2 (canonical types):** plan.md §2.1's claim "`report.types.ts` and `home-work.types.ts` exist with Select/Insert pairs" is only half true. Task 1.3 must CREATE `home-work.types.ts` and ADD `ReportInsertType` in addition to its planned extensions. No task assumption beyond 1.3 is invalidated (repos in §4.1 already type against these names, which 1.3 defines).
2. **Item 24 (sandbox DDL path):** plan/tasks say `bun run db push`; the sandbox pglite provider makes push unusable — 0.1 baseline already rules: generate + programmatic migrate, verified by introspection. Recorded, not re-litigated.
3. **Item 13 (amendment scope):** REQ-072 targets the §10 session-report row AND the §2.3 INV-S7/S8 rows (66-67) which still read "upstream-owned" — both need the ownership flip in the docs task.
