# Task 2.1 Outcome — Session-Report/Homework Journey Test (TEST-FIRST RED)

**Plan:** `ai/plans/sprint_1/session-report-homework-infrastructure` · Task 2.1
**Requirements:** REQ-062, REQ-013, REQ-018, REQ-030, REQ-040, REQ-041, REQ-044, INV-S7, INV-S8, INV-HW3, INV-HW4, INV-P1, INV-S3
**Repo state:** shared worktree on `main`, NO branch switch, NO commit. Scope: exactly ONE file — `test/workflows/classes/session-report-homework.journey.test.ts` (created by the prior in-flight agent, audited + completed + lint-repaired by this session). Zero edits to `backend/**`, zero edits to `tasks.md`. `entity-setup.ts` was NOT touched by this task (its in-flight `createTestHomeWork` addition belongs to the 2.2–2.4 agent and is additive; the journey needs no entity-setup change — see §8).

---

## 1. What was done (audit + completion)

The prior agent died after writing the full journey file. This session audited it end-to-end against tasks.md 2.1 + plan §4.4 + specs §2.9, then fixed three harness defects that would have made the suite fail (or fail to gate) for the WRONG reason, then ran and recorded RED.

**Coverage audit — COMPLETE (no gaps found):** the in-flight file already encoded every mandated element; the audit confirmed each against the living spec and verified every import target actually exists on the current tree (helpers, entity-setup factories, canonical types, enums, locale keys — see §4).

| Mandated element (tasks.md 2.1) | In journey file (final anchors) |
|---|---|
| §4.4 side-effect matrix copied verbatim as header comment | header block, lines 16–33 |
| Step 1 — committed cast + baseline snapshot | `test "step 1 …"` (σ via REAL lifecycle; baseline `sideEffectSnapshot`) |
| Step 2 — foreign teacher → `SESSION_NOT_FOUND` oracle, zero side effects | step 2 (foreign + absent-id oracle-identity pair, 2 domain logs) |
| Step 3 — student S submit → `FORBIDDEN`, zero side effects | step 3 |
| Step 4 — invalid-payload sweep ×8 → `VALIDATION` pre-DB, zero rows | step 4 (rating 7; empty notes; 2001-char notes; grade 101; grade −1; `fromAyah > toAyah`; unknown `SurahJuzRef` via runtime `Object.assign` tamper — no casts; non-integer ayah) |
| Step 5 — valid σ submit: 1 report (exact 6-column shape, C.4), 1 `home_work` grades NULL, exactly 2 localized waves (student en / parent ar), bodies names-only, exactly 2 publishes, deterministic per-recipient claim keys | step 5 |
| Step 5b — S′ unlinked-parent INV-P1 negative leg (student wave ONLY, parent inbox untouched) | step 5b (σ′ booked + submitted at journey time) |
| Step 6 — resubmit σ → `SESSION_REPORT_ALREADY_EXISTS`, counts unchanged, claim-key set unchanged | step 6 |
| Step 7 — student S (+ owner T) read report + homework: full rows | step 7 |
| Step 8 — parent/foreign/admin reads → `null` collapse, byte-identical to absent-session read | step 8 (JSON-normalized identity set) |
| Step 9 — σ₂ completed at journey time; submit with assignment + `previousGrades` → σ graded once (88/74, `updatedAt` advanced), σ₂ assignment NULL-graded, +1 report, cumulative 2 waves each | step 9 |
| Step 10 — re-grade attempt via σ₃ + `previousGrades` → typed `CONFLICT` (`homeworkAlreadyGraded`), σ grades unchanged | step 10 |
| Step 11 — forced mid-tx rollback (homework INSERT fails AFTER report INSERT) → zero rows / zero publishes for the attempt | step 11 (Proxy-wrapped tx fault probe — see §5) |
| Denial: anonymous-equivalent caller (no user row) → typed `FORBIDDEN` | denial test |
| Denial: governed teacher flip → `FORBIDDEN`, restore → later gate reachable (duplicate conflict) | denial test (`setGovernanceFixture` flip/restore, restore proven by the `SESSION_REPORT_ALREADY_EXISTS` conflict) |
| Wallet/escrow/`fee_held`/`teacher_transaction` purity vs baseline (REQ-044) | per-step `sideEffectSnapshot` deltas + `final purity oracle` test (wallets/ledger/audit zero; `fee_held` true on all 4 sessions; lanes consumed only by bookings; publish addressees = wave recipients only) |
| `SpiedFanoutTransport` for ALL fan-out (+ memory claim cache; no Redis/WS) | suite-level `engineOptions` seam, asserted every step |
| NO `runInRollback`; committed fixtures in ONE `beforeAll` transaction; tracked IDs incl. every side-effect row; hard-delete + zero-residue re-probe in `afterAll` | `TrackedFixtures` registry: 14 fixture rows at step 1, + σ′/σ₂/σ₃ sessions + claims + all service-created `reports`/`home_work`/`notifications` rows registered as they land; `tracked.cleanup()` in `afterAll` |

## 2. Fixture / cast decisions

- **Actors:** teacher T + foreign teacher Ft (real `users` rows `role:"teacher"` + real `teacher` rows `isApproved:true` via `createTestTeacherRow`), student S + unlinked variant S′ (`createTestStudent`, `parentId:null` default; S′ never linked), linked parent P (`createTestParent` + explicit `students.parent_id = P.id` update — emulates the link-request mutation per the `journey-fixtures` precedent), admin A (`createTestAdmin`), all through `backend/db/test/entity-setup.ts` factories — REAL permission-group/role-child membership, never monkey-patched resolution.
- **Persisted locales pinned per user** (`users.locale` override): S en, P ar — the recipient-locale composition assertion (step 5/9 copy equality against `NOTIFS_EN`/`NOTIFS_AR`) is the proof; the actor-context factories cannot express per-user locale, so the entity-setup builders are used directly — the sanctioned deviation precedent of the session-request journey.
- **σ reaches `completed` through the REAL lifecycle path** (`SessionLifecycleService.createSession → startSession → completeSession`, book→start→complete), composed INSIDE the `beforeAll` committing transaction via the `outerTx` seam of each service call — no raw status surgery. The booking's idempotency claim row is looked up and tracked. σ′, σ₂, σ₃ are booked at journey time through their own commits (`provisionCompletedSession` in-file helper) per steps 5b/9/10.
- **Lane funding:** S funded `{ balanceTrial: 1, balanceHifz: 2 }` exactly (σ burns the trial lane; σ₂/σ₃ burn the two Hifz units — the final oracle asserts `{ hifz: 0, trial: 0 }`), S′ funded 1 trial unit.
- **Tracked teardown:** every fixture AND every service-created row (3 reports, 3 home_work, 5 notifications, 4 sessions, 4 claims) is registered; `afterAll` hard-deletes in reverse registration order and re-probes zero residue (a leaking `afterAll` fails the suite).

## 3. RED evidence (failure class)

`bun run test/scripts/run-test.ts test/workflows/classes/session-report-homework.journey.test.ts` — run TWICE, identical:

```
[run-test] Running: bun test /home/z/my-project/test/workflows/classes/session-report-homework.journey.test.ts
bun test v1.3.14 (0d9b296a)

test/workflows/classes/session-report-homework.journey.test.ts:

# Unhandled error between tests
-------------------------------
error: Cannot find module '@/backend/services/classes/session-report.service' from '/home/z/my-project/test/workflows/classes/session-report-homework.journey.test.ts'
-------------------------------

 0 pass
 1 fail
 1 error
Ran 1 test across 1 file. [75.00ms]
```

**Failure class = `Cannot find module '@/backend/services/classes/session-report.service'` — the DESIGNED RED (missing service surface), the only acceptable cause.** It is a load-time module-resolution failure before any test body runs, so no fixtures are created and no residue can exist; `runInRollback` is unnecessary and absent. The suite turns green only after 2.7 (write surface) and 2.8 (read surface) land (2.9 = GREEN gate). Exit code 1, reproduced deterministically (2/2).

## 4. Verification (P2 quality loop) — exit 0

### 4.1 `bun run tsgo` — **0 errors** (project-wide)

The `@ts-expect-error` on the service import holds the floor during RED (see §7 for the biome-attribution subtlety that mandates the single-line namespace import). One REAL type error was found and fixed in the harness (see §6, DF-1) — it predates this session (prior agent's bug) and was blocking the tsgo-0 floor.

### 4.2 `bun run scripts/health/sub-loop.ts test/workflows/classes/session-report-homework.journey.test.ts --lifecycle duplicates` — **EXIT=0**

```
✅ tsgo passed (no errors for test/workflows/classes/session-report-homework.journey.test.ts)
✅ oxlint passed
✅ biome:check passed
✅ lint:type-aware passed
ℹ  Skipping check:duplicates for test/workflows/classes/session-report-homework.journey.test.ts (outside jscpd scan scope)...
✅ check:duplicates passed
✅ All checks for lifecycle "duplicates" passed for test/workflows/classes/session-report-homework.journey.test.ts
```

Discovery output (verbatim):

```
ℹ  File: test/workflows/classes/session-report-homework.journey.test.ts
ℹ  Lifecycle: duplicates (depth: 3)

━━━ Applicable Rule Files ━━━

Instruction files (read before fixing):
  ⚠  .github/instructions/tests.instructions.md

AGENTS.md files (read before fixing):
  ✓  AGENTS.md
```

(The tool prints the legacy `.github/instructions/…` path — the real `.agents/instructions/tests.instructions.md` was read in full this session; `test/workflows/AGENTS.md` read in full. Note: sub-loop at depth 1 does NOT print `test/workflows/AGENTS.md`, but it is the layer's governing file and was honored.)

## 5. Step-11 fault-injection mechanism (mandated justification)

tasks.md offers "a raw-tx probe OR a fault-injection option the service exposes — choose and justify; NO production-only backdoor flags". The journey uses a **raw-tx probe**: a recording `Proxy` over a REAL `db.transaction` handle (`faultProbeTx`) that (a) re-wraps savepoint `transaction` callbacks so the probe stays in the service's whole transactional window via the `outerTx` seam, and (b) replaces ONLY the `home_work` INSERT with a thrown raw failure AFTER the `reports` INSERT has executed (both inserts counted). No service seam, no env flag, no validation bypass exists; the injected failure is indistinguishable from a constraint/IO fault, and the service must roll back its unit and publish nothing. The probe lives entirely in the journey file.

## 6. Harness repairs made this session (journey-side only)

| ID | Defect (prior agent) | Symptom | Fix |
|---|---|---|---|
| DF-1 | `readBookingLanes` declared `Promise<{ trial: number; hifz: number }>` while `students.balance_hifz` is a nullable column (`balanceHifz` has no `.notNull()`; `balanceTrial` does) | `tsgo` TS2322 → project floor broken (RED would have been reachable, but the tsgo-0 gate of P2 was violated) | explicit `row === undefined` and `row.hifz === null` guards (NULL lane = fixture-integrity failure) + narrowed `{ hifz: row.hifz, trial: row.trial }` reconstruction |
| DF-2 | publish-envelope order-insensitive assertions used bare `.toSorted()` on `readonly number[]` arrays (2 sites × 2) | oxlint `require-array-sort-compare` ×4 → sub-loop exit 1 | envelopes compared as stringified id arrays with `(a, b) => a.localeCompare(b)` comparators — order-insensitive multiset that still catches duplicate/wrong addressees |
| DF-3 | same normalized strings sorted without comparator + pre-existing `Object.keys(reportRow).toSorted()` | type-aware eslint `sonarjs/no-alphabetical-sort` ×5 | `localeCompare` comparators on all three sites |
| DF-4 | `if (!row || row.hifz === null)` guard | eslint `@typescript-eslint/prefer-optional-chain` | split into two explicit throws (semantics unchanged) |
| DF-5 | the DESIGNED unresolved-service import fails `import-x/no-unresolved` under `lint:type-aware` → sub-loop cannot exit 0 during RED (rule has no repo-level ignore; `eslint.config.mjs` out of scope) | sub-loop exit 1 with 1 error (+1 unused-directive warning after naive fixes) | file-scoped `/* eslint-disable import-x/no-unresolved -- TEST-FIRST RED … */` placed after the header doc, with a removal note; the `@ts-expect-error` stays directly above the import. An `eslint-disable-next-line` cannot share the import line with `@ts-expect-error` (both demand the immediately-preceding line; TS also reports TS2307 on the SPECIFIER line after biome reflows the import to multi-line, which makes a directive placed above a multi-line import "unused" — TS2578+TS2307 observed) |

No assertions were weakened; every repair is mechanical/lint-level or a stricter guard (DF-1 adds a NULL-lane integrity check).

## 7. `@ts-expect-error` / eslint-disable REMOVAL CHECKLIST (for 2.7, and 2.8)

**File:** `test/workflows/classes/session-report-homework.journey.test.ts`

1. **Line 72** — delete the whole block directive:
   `/* eslint-disable import-x/no-unresolved -- TEST-FIRST RED: the session-report service module lands with tasks 2.7/2.8 (removal note above the import). */`
2. **Lines 60–71** — delete the "TEST-FIRST RED suppression scope" rationale block comment.
3. **Line 110** — delete `// @ts-expect-error -- service surface lands with the report/homework write/read implementation`.
4. **Line 111** — the import itself may stay as-is (namespace form is biome-stable):
   `import * as SessionReportService from "@/backend/services/classes/session-report.service";`
   — or be converted to named imports `{ getSessionHomework, getSessionReport, submitSessionReport }` (then call sites drop the `SessionReportService.` prefix, 25 occurrences).
5. **Do this in the SAME change that lands the module.** Flip behavior is the designed forcing function: the moment `backend/services/classes/session-report.service.ts` exists, tsgo reports **TS2578 "Unused '@ts-expect-error' directive"** on line 110 and eslint reports the file-scoped disable as an unused-directive WARNING — both must be gone before the 2.7 sub-loop can pass. (2.8 adds the read functions to the same module, so no additional directives exist for 2.8 — the import covers `getSessionReport`/`getSessionHomework` already.)
6. **Structural constraint to preserve if directives are re-introduced for any future RED import:** the import must remain SINGLE-LINE and ≤120 columns (biome `lineWidth: 120` reformats longer named imports to multi-line, which moves the TS2307 report line off the directive-covered line and breaks suppression — observed empirically). The namespace form `import * as X from "…"` is the proven-stable shape.

## 8. Cross-file dependencies (for the repo/service agents) — READ

- **`backend/db/test/entity-setup.ts` is NOT touched by 2.1.** Its in-flight modification (adding `createTestHomeWork`, lines ~363–399) belongs to the 2.2/2.3 agent and is additive; the journey does not use it (homework rows are created by the SERVICE under test, which is the point of the journey). No coordination conflict; do not regress the additive change.
- **C.4 contract consumed by the journey:** step 5 asserts the report row shape is EXACTLY `["createdAt", "id", "sessionId", "studentRatingByTeacher", "teacherNotes", "updatedAt"]` — `reports` must keep carrying NO teacher column.
- **2.7 service contract pinned by the journey (do not deviate):**
  - Signature: `submitSessionReport(teacherUserId, sessionId, input: SessionReportSubmitInput, locale, outerTx?, options?: NotificationEngineCallOptions)` returning the inserted report row (`ReportReturnType`) — the journey asserts `reportRowBySessionId(σ.id) === submits` (same object shape, no re-read).
  - Read functions: `getSessionReport(callerUserId, sessionId, locale)` / `getSessionHomework(callerUserId, sessionId, locale)` returning the full row or EXACTLY `null` (participant gate: teacher/student only; parent/foreign/admin/absent all collapse; NO throw for non-participants).
  - Denials: foreign/absent target → `NotFoundError("SESSION", …)` with `code === "SESSION_NOT_FOUND"` and byte-identical message for both; student submit + governed/absent actor → `ForbiddenError` `code === "FORBIDDEN"`; invalid payloads → `ValidationError` `code === "VALIDATION"` (pre-DB, zero logs); duplicate → `ConflictError("SESSION_REPORT_ALREADY_EXISTS", t.sessionReportAlreadyExists)`; re-grade → `ConflictError` default code `"CONFLICT"` with `t.homeworkAlreadyGraded`; wrong-state → `SESSION_INVALID_TRANSITION` (not directly asserted at journey level — the σ/σ′/σ₂/σ₃ sessions are all completed — covered by 2.7's unit suite).
  - Logging: exactly ONE `logger.logDomainError` per denial (`SESSION_NOT_FOUND` ×2, `FORBIDDEN`, `SESSION_REPORT_ALREADY_EXISTS`, `CONFLICT` sequences asserted via the `recordDomainLogs` spy); pre-DB validation denials and ALL success paths log NOTHING.
  - Notifications: `NotificationType.SessionCompletion`, `relatedEntityType: "session"`, `relatedEntityId: sessionId`, idempotency keys `session:{sessionId}:report` (per-recipient digest verified via `buildEmitClaimKey`); student copy = `NOTIFS_EN.eventSessionReportReadyTitle` / `eventSessionReportReadyBody(teacher.fullName)`; parent copy = `NOTIFS_AR.eventSessionReportReadyParentBody(student.fullName, teacher.fullName)` (recipient-locale composition); unlinked session ⇒ student wave ONLY; publish strictly post-commit via `publishReceipts` through the injected `options` (transport + cache) — the memory claim cache proves the engine's claim path is exercised.
  - `previousGrades` semantics: with an ungraded prior row → guarded UPDATE grades it exactly once (88/74, `updatedAt` advanced) AND the new session's row stays NULL-graded; with all prior rows graded → `CONFLICT(homeworkAlreadyGraded)` and the whole unit rolls back (σ₃ stays report-less).
  - Publish-count oracles: each accepted submit ⇒ exactly 2 envelopes ([student], [parent]) — unlinked ⇒ exactly 1 ([student]); every denial/rollback ⇒ 0.
- **2.7 implementers must also honor the outerTx seam used by step 11:** `withTransaction(outerTx, fn)` must propagate the CALLER's tx handle (the Proxy probe) into every repo/engine call, or the fault probe cannot intercept and the journey's rollback assertions cannot hold.
- **Wallet purity:** the service must write nothing to `wallet`/`teacher_transaction`/`audit_logs` and must not touch `session.fee_held` or the students' lanes (REQ-044/INV-S3 oracles assert zero deltas + zero rows, counters scoped to fixture ids).

## 9. Deviations (all explained, none silent)

- **DV-R1 — namespace import for the RED service import** (prior agent's file used named imports): forced by the biome×tsc interaction (§7 item 6 — biome reflows >120-char named imports to multi-line, moving the TS2307 report line and turning `@ts-expect-error` unused). Removal checklist (§7) documents the 2.7-side cleanup either way.
- **DV-R2 — file-scoped `eslint-disable import-x/no-unresolved`** instead of a next-line directive: `lint:type-aware` cannot reach exit 0 during RED otherwise (the rule has no repo-level ignore pattern; `eslint.config.mjs` is outside this task's scope). The disable is annotated as RED-phase-only, covers exactly one specifier, and is on the removal checklist. No oxlint-disable comments were added (repo rule respected).
- **DV-R3 — envelope equality via stringified id arrays** instead of raw array `toSorted` equality: mandated by lint (DF-2/DF-3); semantics identical (order-insensitive multiset over single-recipient envelopes), and the surrounding `publishCount` assertions keep the "exactly 2 / exactly 1" cardinality.
- **Scaffolding leg NOT executed** (per tasks.md 2.1 "if missing"): `test/workflows/AGENTS.md` + `helpers/` exist (verified, 0.2 item 14) — nothing scaffolded.
- **Admin-A submit denial not separately asserted** at journey level: the role-gate leg is carried by student S's `FORBIDDEN` step (tasks.md's denial list mandates anonymous-equivalent + governed teacher, both present); admin non-participant read collapse IS asserted (step 8). Recorded for 2.9's reviewer.

## 10. Carry-forward for 2.9 (GREEN gate)

- After 2.7/2.8 land: run `bun run test/scripts/run-test.ts test/workflows/classes/session-report-homework.journey.test.ts` → expect 15 tests green (steps 1–11 incl. 5b + denial test + final purity oracle), then the full layer `bun run test/scripts/run-test.ts test/workflows` ×3 for flake evidence. NEVER weaken journey assertions to make it pass.
- First GREEN run must also re-run the sub-loop on this file — removing the §7 directives is a REQUIRED part of the 2.7 change (tsgo TS2578 will fail the floor otherwise).
- The journey hard-deletes everything it tracked; `audit_logs`/`teacher_transaction` are never cleaned by design (immutable tables) — any nonzero count there fails the suite loudly and means a service bug (write leak), not a teardown bug.
- Re-run determinism: two consecutive RED runs already recorded (§3); the GREEN-gate repeat-run requirement (×3) remains 2.9's.

## 11. Residual risks

- **Load-time RED means zero runtime exercise of the harness** until 2.7 lands: correctness of the fixture/body code currently rests on the static gates (tsgo 0 with full typing of every EXISTING module, sub-loop exit 0) + the helpers' own self-test suite. The first GREEN run may still surface harness-level assumptions (e.g. exact copy equality, `updatedAt` monotonicity) — those are journey assertions to satisfy, not to weaken.
- Concurrent sibling tasks (2.2–2.4) are actively editing `backend/db/repo/**` and `backend/db/test/**` in the shared worktree; transient cross-agent lint/test noise observed during this session was confined to their files (e.g. `report.repository.test.ts` lock contention) and does not affect this task's evidence.
- `check:duplicates` is skipped for this file (outside jscpd scan scope) — same accepted posture as 2.5's test file.
- `tasks.md` checkbox for 2.1 left UNFLIPPED per the no-tasks.md-edit instruction — evidence for the P6 flip-off is this document.
