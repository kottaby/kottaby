# Task 2.6 Outcome — Session-Report Notification Seam (D6)

**Plan:** `ai/plans/sprint_1/session-report-homework-infrastructure` · Task 2.6
**Requirements:** REQ-018, REQ-019, INV-P1, INV-P3
**Repo state:** `main` @ `4fae9fa` (shared worktree, clean), NO branch switch, NO commit (per instruction). Scope: exactly **2 NEW files** — `backend/services/classes/session-report-notification.service.ts` + `backend/services/classes/session-report-notification.test.ts`. `git status --porcelain` = only those two `??` entries; no barrels, locale files, or sibling files touched.

---

## 1. What was done

Created the D6 notification seam mirroring `backend/services/classes/session-request-notification.service.ts` (the recipient-locale composition precedent, 0.2 item 8) structure-for-structure, plus its colocated 4-tier suite mirroring `session-request-notification.service.test.ts`.

**Ground truth verified before writing (0.2/1.3/1.4/2.1–2.5 outcomes re-confirmed against source):**
- `NotificationEngine.emitForUser(input, locale, tx?, options?)` — caller-tx path returns the unpublished `NotificationDeliveryReceipt` (`notification-engine.service.ts:42-67`); `NotificationEngineCallOptions` `:34-37`; `publishReceipts` `:116-122`; all re-exported via the `@/backend/services/notifications` barrel.
- **`NotificationType.SessionCompletion` EXISTS** — `backend/enum/notifications/notification-type.enum.ts:7`, value `"session_completion"` (no adaptation needed; recorded per brief).
- `SessionRepository.findReportWaveContextById(id, tx?)` — `session.repository.ts:416-421` delegating to `session.repository.helpers.ts:315-353`; ONE joined read returning the canonical `SessionReportWaveContextRow` (`backend/types/classes/session-notification.types.ts:40-51`) with `parentUserId/parentFullName/parentLocale` null via the LEFT JOIN on `students.parent_id`.
- Copy slots `eventSessionReportReadyTitle` / `eventSessionReportReadyBody(teacherName)` / `eventSessionReportReadyParentBody(studentName, teacherName)` — `shared/locale/types/notifications/index.ts:160-176` + en/ar leaf maps (1.4).
- Engine claim machinery: `buildEmitClaimKey` digest binds `<sorted recipient ids>:<type>:<raw key>` (`emit-idempotency.ts:67-71`) — the SAME raw key `session:{id}:report` with different recipients yields different claims (exactly the D6/REQ-018 differentiation).
- Journey-of-record pins (2.1 outcome §8 + `session-report-homework.journey.test.ts:822-825,927,1191-1193`): copy equality against `NOTIFS.eventSessionReportReady…(fullName)` RAW names; per-recipient claim keys `buildEmitClaimKey([recipientId], NotificationType.SessionCompletion, \`session:{id}:report\`)`.

**Module behavior (all per D6):**
- Reads the wave context **exactly ONCE** per call (`findReportWaveContextById(sessionId, tx)` — source-pinned to 1 occurrence).
- Maps the raw row onto the guard-validated `SessionReportWaveContext`; the parent leg is present IFF the join produced BOTH `parentUserId` AND `parentFullName` (fail-closed: a half-joined leg cannot prove the stored link and is never patched with a fabricated name).
- Student emission (always): copy in the **student's persisted locale** (`locale ?? defaultLocale`), body = `eventSessionReportReadyBody(teacher.fullName)`.
- Parent emission IFF the stored parent link is present (INV-P1): copy in the **parent's persisted locale**, body = `eventSessionReportReadyParentBody(student.fullName, teacher.fullName)`.
- Both emissions: `type: NotificationType.SessionCompletion` (VALUE import), `relatedEntityType: "session"`, `relatedEntityId: sessionId`, `idempotencyKey: \`session:${sessionId}:report\`` (single composition site, source-pinned).
- Emits via `NotificationEngine.emitForUser` **in the caller's tx** (tx is a REQUIRED parameter — every emission rides it); guards the engine's caller-tx contract (`!("notifications" in result)` → typed `DomainError("INTERNAL_SERVER_ERROR", …)`, sibling idiom).
- Returns `NotificationDeliveryReceipt[]` verbatim, **NEVER publishes**; `options` is passed through untouched.
- Requester `locale` resolves ONLY the typed denial copy (pre-DB id-shape `ValidationError(t.validation)`; missing row `NotFoundError("SESSION", t.sessionNotFound)`).
- **Header comment** documents the no-governance ruling + rationale (emits for recipients, not the actor; the calling flow has already gated the actor), the receipt-producer contract, recipient-locale composition, privacy hygiene, and log silence — so review cannot drift.
- **Logs NOTHING** (no logger import at all — source-pinned).

## 2. Exact export surface (2.7 imports this — final anchors)

Module: `backend/services/classes/session-report-notification.service.ts`

```ts
// :126  namespace (mirror of SessionRequestNotificationService)
export namespace SessionReportNotificationService {
  // :147-152
  export async function notifySessionReportReady(
    sessionId: number,
    locale: string,
    tx: DBTransaction,
    options?: NotificationEngineCallOptions
  ): Promise<NotificationDeliveryReceipt[]>;
}
```

Private (not exported): `reportWaveContextOf` (:72), `emitReportReadyNotification` (:90), `reportReadyEmitInput` (:114), `NotificationsTranslations` handle type. No barrel edit — `backend/services/classes/index.ts` untouched; 2.7 imports the module by path (established sibling pattern, 2.5 §1).

**2.7 consumption sketch:**
```ts
import { SessionReportNotificationService } from "@/backend/services/classes/session-report-notification.service";
// step 2e (inside withTransaction):
const receipts = await SessionReportNotificationService.notifySessionReportReady(sessionId, locale, tx, options);
// step 3 (strictly AFTER own commit):
await NotificationEngine.publishReceipts(receipts, locale, options);
```
Receipt order is `[student, parent?]`; publish the array as-is. The seam's typed denials are unreachable behind 2.7's probe (row lock held) — replay-throw, never replay-return (plan D3 posture).

## 3. Test engineering (2.6.TE)

`backend/services/classes/session-report-notification.test.ts` — colocated, 4 tiers, mirrors the sibling's harness exactly (`MapBackedClaimCache`, `SpiedFanoutTransport` from `@/test/workflows/helpers`, `recordDomainLogs` spy, `expectRepoError`, `runInRollback`, entity-setup fixtures, Tier-3 committed cast with Rule-9 FK-safe `afterAll` hard-delete incl. zero-residue posture).

```
bun run test/scripts/run-test.ts backend/services/classes/session-report-notification.test.ts
→ 17 pass / 0 fail · 124 expect() calls · EXIT=0
```

| Tier | Cases |
|---|---|
| 1 | linked session ⇒ exactly 2 receipts (recipients, `SessionCompletion`, `relatedEntityType/Id`, `isRead`, en/ar copy, `recipientUserIds`, tx visibility, zero publishes, zero logs, per-recipient `buildEmitClaimKey` pins); unlinked ⇒ exactly 1 receipt (parent inbox untouched, 1 claim); missing session ⇒ `SESSION_NOT_FOUND` with **ZERO** domain logs + zero rows; hostile id (0) ⇒ pre-DB `VALIDATION` |
| 2 | requester locale never leaks (ar requester, student copy stays en / parent stays ar); null persisted locales ⇒ default (`ar`) copy for BOTH recipients (covers both `?? defaultLocale` arms); hostile-id matrix (0/−1/1.5/NaN/2⁵³/MIN_SAFE) all pre-DB `VALIDATION`; int4-ceiling valid-shape miss ⇒ `SESSION_NOT_FOUND`; hostile unicode/RTL/emoji names composed verbatim |
| 3 (committed cast) | duplicate invocation in separate committed scopes ⇒ engine claim replay (same row ids per recipient, zero new rows, zero new publishes, 4 claim attempts / 2 distinct digests, post-commit `publishReceipts` proven receipt-compatible); forced mid-tx failure after BOTH emissions ⇒ total rollback + zero publishes (ghost pushes impossible); forced engine failure on the parent emission (first emit delegates to the real engine) ⇒ typed `ConflictError` propagates and the caller-tx unit rolls back BOTH emissions; engine contract breach (bare row) ⇒ typed `INTERNAL_SERVER_ERROR` |
| 4 | privacy regex pin — stored copy has NO digits, NO `grade|rating|note|score` vocabulary, NO session-id content (names only, REQ-019); derived-recipient invariance across two participant pairs (scoped counts 2+2); half-joined parent leg fails closed to unlinked via repo spy (no parent emission, no fabricated name, 1 claim); source pins — no `@/backend/lib/logger`/`console.`/`logDomainError`, no `await NotificationEngine.publishReceipts`, no governance import, `findReportWaveContextById` exactly once, `import { NotificationType }` VALUE import, no raw `"session_completion"` literal, idempotency key at exactly one site |

**Coverage (per-file, wired tooling — `bun run test:services:coverage`, EXIT=0):**
```
backend/services/classes/session-report-notification.service.ts   |  100.00 |  100.00
```
**100% statements AND 100% branches** on the new module (stronger than the 2.5 matrix-only precedent — the whole-services run was green: 770 pass / 29 skip / 0 fail, so zero cross-suite regressions). Branch matrix exercised: id-shape guard T/F; row-null T/F; `parentUserId ∧ parentFullName` all three valuations (linked / unlinked / half-leg); `!("notifications" in result)` both arms; both `locale ?? defaultLocale` arms; parent-gate T/F.

## 4. Quality loop (2.6.QL) — `--lifecycle duplicates`, per file

**Service module — EXIT=0** (tsgo ✅ / oxlint ✅ / biome:check ✅ / lint:type-aware ✅ / check:duplicates ✅):
```
✅ All checks for lifecycle "duplicates" passed for backend/services/classes/session-report-notification.service.ts
```
**Test file — EXIT=0** (duplicates skipped — outside jscpd scan scope, accepted posture per 2.5/2.1):
```
✅ All checks for lifecycle "duplicates" passed for backend/services/classes/session-report-notification.test.ts
```
Discovery output (verbatim, service module; test file adds `.github/instructions/tests.instructions.md`):
```
━━━ Applicable Rule Files ━━━

Instruction files (read before fixing):
  ⚠  .github/instructions/backend.instructions.md

AGENTS.md files (read before fixing):
  ✓  AGENTS.md
  ✓  backend/services/AGENTS.md
  ✓  backend/AGENTS.md
```
(Known cosmetic quirk per 2.5/1.3: tool prints the legacy `.github/instructions/…` path; validated against the real `.agents/instructions/backend.instructions.md` + `.agents/instructions/tests.instructions.md` + `backend/services/AGENTS.md`, all read in full.) Transient loop failures fixed in-file during development: 2 unused imports (service), 1 unused type + `Number()` no-op conversion + `sonarjs/concise-regex` (`[0-9]`→`\d`) (test); a first test run surfaced the `session_teacher_id → teacher.id` FK (teacher role-child row required — fixture now uses `createTestTeacherRow`).

## 5. Floors

- `bun run tsgo` (project-wide): **EXIT=0, 0 errors**.
- `bun run oxlint` (project-wide, 1408 files, 303 rules): **0 warnings / 0 errors**.
- Regression: `bun run test/scripts/run-test.ts backend/services/classes` → **185 pass / 3 skip / 0 fail** (skips pre-existing sibling lifecycle cases).

## 6. 2.6.SEC — Security & Tenancy Audit

- **Recipients derived ONLY from the session's joined identity rows:** the sole caller inputs are `sessionId/locale/tx/options`; recipients come from `findReportWaveContextById` inside the caller's tx (derived-recipient invariance test across two pairs; hostile-id matrix proves no input can aim an emission). No recipient id is ever accepted from caller data.
- **Parent emission gated strictly on the stored link:** the gate is the wave row's `parentUserId ∧ parentFullName` — the LEFT JOIN of `students.parent_id` of the session's OWN student; a foreign/other parent is structurally unreachable (2.4 §3), a half-joined leg fails closed, and an unlinked student yields exactly one receipt (tested both ways).
- **PII minimization:** the module logs NOTHING (source-pinned: no logger import, no `console.`, no `logDomainError`); stored copy interpolates only counterparty full names (regex pin: no digits/grade/note vocabulary/session id — REQ-019); no ids or copy in any log path. (The engine's own fail-open `NOTIFICATION_IDEMPOTENCY_DEGRADED` warn lives in the engine, not here; every happy-path test injects a cache so logs stay silent and the silence is asserted.)
- **No write surface of its own:** the module writes nothing except through the engine's single-writer emit; both rows ride the caller's tx (rollback tests prove zero residue, zero ghost pushes).

## 7. 2.6.SR — Semantic Review

- **Single-read discipline:** exactly ONE `findReportWaveContextById` call per invocation (source pin count = 1).
- **No publishing inside:** no `publishReceipts` call (source pin); receipts returned verbatim.
- **Enum VALUE import / no stringly-typed type:** `import { NotificationType }` + member usage; raw `"session_completion"` literal pinned absent.
- **tx propagated to EVERY engine call** (tx is required; no `db` fallback path exists in the module).
- Zero dead code (coverage 100/100), zero `console.*`, zero TODO/FIXME, zero plan-artifact strings in comments, no nested ternaries, no `any`, BOPLA field-by-field emit input, scope = 2 new files only.

## 8. 2.6.IV — Instruction Verification

- `.agents/instructions/backend.instructions.md`: service-layer business orchestration via repos/engine ✅; types from `@/backend/types` ✅; localized errors only (no hardcoded user-facing strings — denial copy via `getServerTranslations(locale)` ONE arg, `shared/locale/server-graphql.ts`) ✅; no `console.*` ✅; no nested ternary ✅; single-writer notifications rule (`backend/services/AGENTS.md`) honored — writes only via `NotificationEngine.emitForUser`, publish-after-commit left to the caller ✅.
- `.agents/instructions/tests.instructions.md`: mandated runner (never raw `bun test`) ✅; `runInRollback` + `tx` everywhere for transactional cases ✅; `expectRepoError` (never `rejects.toThrow`) ✅; `bun:test` utilities, no `any` ✅; Rule-9 committed-fixture cleanup for the Tier-3 cast (FK-safe order, `afterAll`) ✅; entity-setup helpers verified against source before use (`createTestStudent(tx, userId, { parentId })`, `createTestTeacherRow(tx, userId)`) ✅.
- `backend/services/AGENTS.md`: colocated tests (§Testing — basis of DV-1) ✅; notifications seam rule (§No real external APIs — spied transport + in-memory claim cache, no Redis/WS) ✅; domain-scoped module, no monolith ✅.

## 9. Deviations (all explained, none silent)

- **DV-1 — test path colocated:** `backend/services/classes/session-report-notification.test.ts` instead of tasks.md's `__tests__/session-report-notification.test.ts` sketch; `backend/services/AGENTS.md` §Testing ("Service tests live next to the code they cover") + both sibling suites + 2.5 DV-T ruling; no `__tests__/` dir exists under `backend/services/`. The orchestrator brief itself specified the colocated path.
- **DV-2 — namespace export:** `SessionReportNotificationService.notifySessionReportReady` (namespace) instead of the brief's bare-function notation — byte-for-byte the sibling seam's module shape (`SessionRequestNotificationService`), the file the task mandates mirroring. 2.7 imports the namespace member (or destructures).
- **DV-3 — log-free denials:** the sibling seam logs ONE `logDomainError` on its missing-session denial; THIS module logs nothing on the same denial — the task brief + 2.6.SEC mandate module-wide log silence ("Module logs NOTHING — confirm"); the seam has no actor-facing denial of its own (2.7 owns every actor-facing log line). Tested: rejection paths assert `logs.records` empty.
- **DV-4 — RAW name interpolation (NOT bidi-isolated):** the 1.4 carry-forward suggested `isolateBidi` before interpolation, and the locale type doc comments mention sender-side bidi isolation; the shipped seam interpolates the persisted `fullName` VERBATIM. Rationale: the journey-of-record pins exact copy equality against `NOTIFS.eventSessionReportReadyBody(teacher.fullName)` (journey lines 822-825/927/1191-1193; 2.1 outcome §8 "do not deviate"), and the mirror-source sibling interpolates raw. Journey assertions are never weakened — if bidi isolation is wanted for this copy, it is a locale-doc/journey amendment owned by a later task, recorded here as carry-forward.
- **DV-5 — fail-closed additions beyond the bare D6 sketch:** pre-DB id-shape guard (`isPositiveSafeInt` → `VALIDATION`), missing-row `NotFoundError("SESSION", …)`, and the half-joined-parent-leg fail-closed rule — all mirror the sibling `resolveWaveContext` posture, are header-documented, and are coverage-proven (they are defense-in-depth; unreachable behind 2.7's probe).
- **DV-6 — Tier 3 on committed fixtures:** tasks.md 2.6 marks the suite "(runInRollback)"; the replay-across-committed-scopes and rollback-purity proofs REQUIRE real commit boundaries and cannot be proven under a rollback — the sibling's established convention (runInRollback everywhere + ONE committed `beforeAll` cast hard-deleted in `afterAll`, Rule 9) is applied. runInRollback still wraps every non-Tier-3 case and all transactional assertions.

## 10. Carry-forward (contract for 2.7 / 2.9)

- Import + call shape in §2; thread the resolver's `NotificationEngineCallOptions` (first GraphQL-layer consumer of engine options per 0.2 item 9) into BOTH step 2e and the post-commit `publishReceipts`.
- The seam throws typed errors only (`VALIDATION`, `SESSION_NOT_FOUND`, engine `INTERNAL_SERVER_ERROR` breach, any engine-typed failure propagates unswallowed) — 2.7 lets them propagate (replay-throw); none need translation, and none log from this module.
- Journey step 11's outerTx fault probe keeps working: the seam emits on the tx handed down by `withTransaction(outerTx, …)` — no internal transaction is opened.
- 2.9 GREEN gate: journey copy-equality assertions (§DV-4) hold against this implementation as shipped.

## 11. Residual risks

- The `shared/locale/types/notifications/index.ts` doc comments still describe sender-side `isolateBidi` for these slots while the (journey-pinned) implementation interpolates raw names — cosmetic doc/journey divergence for a locale-owning task to reconcile (DV-4); no behavioral risk.
- No other interaction surfaces: `git status` shows only the two new files; the whole-services coverage run (770 pass / 0 fail) and the classes-suite regression run are green.
- `tasks.md` checkboxes for 2.6 (incl. 2.6.QL/.TE/.SEC/.SR/.IV) left UNFLIPPED per the no-tasks.md-edit instruction — evidence for the P6 flip-off is this document.
