# Task 2.5 Outcome — Session-Report Guards Module (pure validators)

**Plan:** `ai/plans/sprint_1/session-report-homework-infrastructure` · Task 2.5
**Requirements:** REQ-014, REQ-016, REQ-033, REQ-002
**Repo state:** `main`, shared worktree; NO branch switch, NO commit (per instruction). Scope: exactly 2 NEW files — `backend/services/classes/session-report.guards.ts` + `backend/services/classes/session-report.guards.test.ts`. Zero edits to any other file (barrels untouched — `session-lifecycle.guards.ts` is likewise not in the `backend/services/classes/index.ts` barrel; 2.7 imports the module by path, the established sibling pattern).

---

## 1. What was done

Created the pure pre-DB guards module for the report+homework write surface, mirroring `session-lifecycle.guards.ts` structure exactly (module-extraction header doc, `t` handle shape, ValidationError idiom, reuse of the exported id-guard family) plus its colocated 4-tier test suite.

**REUSE over duplication (mandated):**
- `assertPositiveSessionId` **delegates verbatim** to the exported `assertPositiveSafeSessionId` (`session-lifecycle.guards.ts:123`) — the id vocabulary stays in exactly one module. The wrapper adds the plan §4.2-requested `asserts id is number` narrowing predicate.
- `assertHomeWorkBlock` reuses the exported `isPositiveSafeInteger` (`session-lifecycle.guards.ts:97`) for both ayah endpoints.
- Surah/Juz membership integrates the **`isSurahJuzRef` enum guard** (`backend/enum/shared/surah-juz-ref.enum.ts:52`) — VALUE import; never string equality against literals (pinned by a 35/35 all-members-accept test + raw-enum-value-string accept + 12 near-miss rejects).

## 2. Exact export surface (2.7 imports these — verbatim signatures)

Module: `backend/services/classes/session-report.guards.ts` (anchors = final file lines)

```ts
// :73  — narrows `id: unknown` to `number` after the call
export function assertPositiveSessionId(id: unknown, t: ErrorsTranslations): asserts id is number;

// :85  — returns the TRIMMED value; persist exactly this (REQ-033: stored verbatim, length-bounded only)
export function assertTeacherNotes(notes: string, t: ErrorsTranslations): string;

// :101
export function assertRating0To5(rating: number, t: ErrorsTranslations): void;

// :113
export function assertGrade0To100(grade: number, t: ErrorsTranslations): void;

// :129 — ayah span (positive safe ints, from ≤ to) + isSurahJuzRef membership
export function assertHomeWorkBlock(block: HomeWorkBlockInput, t: ErrorsTranslations): void;

// :161 — ≥1 block present; each present block passes assertHomeWorkBlock
export function validateAssignment(input: HomeWorkAssignInput, t: ErrorsTranslations): void;

// :180 — currentGrade then revisionGrade through assertGrade0To100
export function validatePreviousGrades(g: HomeWorkGradeFieldsInput, t: ErrorsTranslations): void;
```

Private (not exported): `MAX_TEACHER_NOTES_LENGTH = 2000` (:51), `type ErrorsTranslations` (:61), `isSuppliedBlock` predicate (:146). **YAGNI:** nothing else is exported — this is exactly the 7-function surface 2.7 consumes.

**Input types consumed (type-only, via `@/backend/types`):** `HomeWorkBlockInput`, `HomeWorkAssignInput`, `HomeWorkGradeFieldsInput` (all shipped by task 1.3, `backend/types/classes/report.types.ts`).

## 3. The translations-handle decision

`t` is the **`errorsTranslations` slice**, NOT the full `Translations` tree and NOT a locale string:

```ts
type ErrorsTranslations = ReturnType<typeof getServerTranslations>["errorsTranslations"];
```

Byte-identical to the handle in `session-lifecycle.guards.ts:125` (`t: ReturnType<typeof getServerTranslations>["errorsTranslations"]`), so 2.7 passes the SAME value to old and new guards. Service-head pattern to mirror (already proven at `session-lifecycle.service.ts:161`): resolve ONCE per flow head — `const t = getServerTranslations(locale).errorsTranslations;` (ONE-arg call, `shared/locale/server-graphql.ts:3`) — then thread `t` down. Guards never call `getServerTranslations` themselves (module has only a `import type` reference; pinned by a source-scan test). Denial keys per guard: id→`validation` (reused generic, per 1.4 §2); notes→`sessionReportNotesRequired`/`sessionReportNotesTooLong`; rating→`sessionRatingRange`; grade→`homeworkGradeRange`; ayah span→`homeworkAyahRangeInvalid`; surah/juz→`homeworkSurahJuzInvalid`; no-block→`homeworkAssignmentBlocksRequired` (all 9-key block verified in `shared/locale/types/errors/labels.ts:165-182` + en/ar leaf maps).

## 4. Test file & convention deviation

**`backend/services/classes/session-report.guards.test.ts` — COLOCATED** (46 tests, 12 078 expect calls), mirroring `session-lifecycle.guards.test.ts` (4-tier describes, `function t()` / `tAr()` slice helpers, try/catch `expectValidationError` helper asserting class + `code === "VALIDATION"` + exact localized message).

- **DV-T (documented deviation):** tasks.md 2.5 sketched `__tests__/session-report.guards.test.ts`; the verified house convention is colocated — `backend/services/AGENTS.md` §Testing: "Service tests live next to the code they cover (`*.test.ts` or `test/` subdirectories)"; both existing service suites (`session-lifecycle.guards.test.ts`, `session-lifecycle.service.test.ts`) are colocated; no `__tests__/` directory exists under `backend/services/` (it exists only under `backend/db/repo/`). Followed the AGENTS.md mandate.
- Tier 1: every accept/reject branch of all 7 guards (incl. all-35 `Object.values(SurahJuzRef)` accept loop and a compile-pin proving the `asserts id is number` narrowing — arithmetic on the declared-unknown binding compiles only after the guard call).
- Tier 2 boundaries: notes 2000 accept / 2001 reject (+padded trim interplay both sides); whitespace-only reject; rating −1/0/5/6/0.5/NaN/±Infinity/MAX_SAFE+1/1e21; grades −1/0/100/101/0.5; `fromAyah = toAyah` accept (also MAX_SAFE_INTEGER span accept); `fromAyah > toAyah` reject; ayah 0/negative/fractional/infinite/unsafe reject; SurahJuzRef member accept / unknown-string reject.
- Tier 3: seeded deterministic fuzz (mulberry32 seed `0x5eed`, 300 iterations × 7 guards = 2 100 cases/run) over closed pools (ids incl. BigInt/Symbol-free shape mix, ratings, grades, ayahs, 12 invalid surah strings + 35 members, notes 0..2100, wire-null assignment legs). Invariants: ONLY `ValidationError` ever, `code` always `"VALIDATION"`, message always ∈ the closed 8-string en copy set; second test pins identical seed ⇒ identical accept/deny trace.
- Tier 4: frozen-payload immutability + structuredClone before/after equality on a rejected payload (zero side effects); statelessness; wire-null fail-closed semantics (null leg ≡ absent → `homeworkAssignmentBlocksRequired`, never TypeError; null+valid leg validates the valid leg); enum string smuggling through `wireBlock`/`wireAssignment` Object.assign helpers (casts live at ONE pinned call site — oxlint `no-unsafe-type-assertion` forbids scattered `as SurahJuzRef`); no-input-echo denial pin; **source pin** — module text contains no `@/backend/db`, `Repository`, `logger`, `console.`, `tx`, and imports `getServerTranslations` as type only (absence of injected deps = the purity proof).
- Localized messages: every guard's denial pinned to the exact **en** copy; the same denials pinned to the **ar** leaf map (exceeds the "at least one case" floor); en≠ar distinctness pin incl. one literal ar copy.

## 5. Verification evidence (quality loop, tests, tsgo)

### 2.5.QL — `bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates`

**Guards module — EXIT=0** (tsgo ✅ / oxlint ✅ / biome:check ✅ / lint:type-aware ✅ / check:duplicates ✅):
```
✅ tsgo passed (no errors for backend/services/classes/session-report.guards.ts)
✅ oxlint passed
✅ biome:check passed
✅ lint:type-aware passed
✅ check:duplicates passed
✅ All checks for lifecycle "duplicates" passed for backend/services/classes/session-report.guards.ts
```
**Test file — EXIT=0** (tsgo ✅ / oxlint ✅ / biome:check ✅ / lint:type-aware ✅; duplicates skipped — outside jscpd scan scope):
```
✅ tsgo passed (no errors for backend/services/classes/session-report.guards.test.ts)
✅ oxlint passed
✅ biome:check passed
✅ lint:type-aware passed
✅ All checks for lifecycle "duplicates" passed for backend/services/classes/session-report.guards.test.ts
```

### Discovery output (verbatim, `backend/services/classes/session-report.guards.ts`)

```
━━━ Applicable Rule Files ━━━

Instruction files (read before fixing):
  ⚠  .github/instructions/backend.instructions.md

AGENTS.md files (read before fixing):
  ✓  AGENTS.md
  ✓  backend/services/AGENTS.md
  ✓  backend/AGENTS.md
```
Test file adds `.github/instructions/tests.instructions.md` to the same mapping. (Known cosmetic quirk per 1.2/1.3 outcomes: the tool prints the legacy `.github/instructions/…` path; validated against the real `.agents/instructions/backend.instructions.md` + `.agents/instructions/tests.instructions.md`, both read in full this session, plus `backend/services/AGENTS.md`.)

### 2.5.TE — mandated runner

```
bun run test/scripts/run-test.ts backend/services/classes/session-report.guards.test.ts
→ 46 pass / 0 fail · 12078 expect() calls · EXIT=0
```

**Branch-coverage matrix** (per-file coverage tooling is NOT wired into `run-test.ts` — its flags are `--last`/`--focus` only — and P3 forbids raw `bun test --coverage`; matrix stated per the brief's alternative):
1. `assertPositiveSessionId` — delegation (0 own branches); accept (1/42/MAX_SAFE) + reject (12 malformed shapes) + narrowing compile-pin.
2. `assertTeacherNotes` — 4/4 arms: empty-after-trim throw; valid continue; >2000 throw; ≤2000 return.
3. `assertRating0To5` — 4/4 arms: `!isSafeInteger` throw (NaN/±Inf/fractions/unsafe); `<0` throw; `>5` throw; accept 0..5.
4. `assertGrade0To100` — same 4/4 arms (0..100 window).
5. `assertHomeWorkBlock` — 5/5: fromAyah invalid; toAyah invalid; from>to; surahJuz non-member; full accept.
6. `isSuppliedBlock` — 3/3: object∧non-null true; null false; non-object false.
7. `validateAssignment` — both-absent throw; jadid-supplied validate (valid+invalid legs); jadid-absent skip; madi-supplied validate (valid+invalid legs); madi-absent skip.
8. `validatePreviousGrades` — current throw; revision throw; both-valid; current-first deterministic order (both invalid).
No other runtime branches exist in the module ⇒ **100% branch coverage by the 46 tests**.

### Type check / lint floors

- `bun run tsgo` (project-wide): **0 errors, EXIT=0** (1.4's 9 residual sibling errors were already cleared by 1.3 — floor restored and held).
- `bun run oxlint` (project-wide, 1405 files): **0 warnings / 0 errors** (baseline 0.1 floor held; one transient sibling-file error observed mid-run came from task 2.2/2.3's in-flight `report.repository.test.ts` being edited concurrently by another agent — clean on re-run, unrelated to this task).

## 6. 2.5.SEC — Security & Tenancy Audit

- **Localized + shape-uniform:** every denial is a `ValidationError` (default `code: "VALIDATION"`, `extensions.code` mirrored) with a fixed flat-namespace message from the 1.4 key set; fuzz tier mechanically proves the thrown message is ALWAYS one of the 8 closed strings — no other class, no ad-hoc copy.
- **No input echo / no disclosure deltas:** messages carry zero payload fragments (pinned: hostile 2001-char `<script>` payload denial message equals the fixed copy exactly; fuzz pins the closed set). Guards read no environment, no DB, no clock; no id/entity identifiers appear in validator copy.
- **No ReDoS surface:** the module contains ZERO regex — the notes bound is `trimmed.length` counting (REQ-033: length-bounded only, stored verbatim); surah/juz is closed-vocabulary membership, not pattern-matching.
- **Fail-closed at the wire:** null/garbage optional legs (`isSuppliedBlock`) and non-number ids (`unknown` param) can only produce the typed `VALIDATION` denial — never a TypeError/500.
- **Purity:** frozen-payload and snapshot tests prove zero mutation; no injected deps exist (source pin); happy paths log nothing and guards log nothing — the ONE `logger.logDomainError` per denial remains 2.7's service-head responsibility per P4.

## 7. 2.5.SR — Semantic Review

- Exports = exactly what 2.7 consumes (7 guards); `MAX_TEACHER_NOTES_LENGTH` private (2.7 has no need — the guard owns the bound); no barrel edits (guards modules are internal by sibling precedent).
- Zero side effects, zero DB access, zero logging, zero `console.*`, zero TODO/FIXME/placeholder comments, zero plan-artifact references (no `REQ-`/`DEV3` strings in code comments — regex-scanned), zero `any`, no nested ternaries (sonarjs-clean after extracting `fuzzLeg`).
- Enum VALUE import (`isSurahJuzRef`) integrated at the guard; test uses `SurahJuzRef` members as values, never raw member literals (raw strings appear only as deliberate non-member fuzz).
- Reuse discipline: `assertPositiveSafeSessionId` + `isPositiveSafeInteger` imported from the lifecycle guards module — id/safe-integer vocabulary exists once repo-wide.
- Scope boundary honored: `git status` shows only the 2 new files from this task (other dirty paths belong to sibling tasks 2.2/2.3 in flight — untouched).

## 8. 2.5.IV — Instruction Verification

- `.agents/instructions/backend.instructions.md`: service-layer module, types from `@/backend/types`, DomainError-subclass errors only, no hardcoded error strings (all copy via the passed translations slice), no `console.*`, no nested ternary, ONE-arg `getServerTranslations` referenced type-level only (runtime resolution is the service head's — 2.7).
- `.agents/instructions/tests.instructions.md`: `bun:test` utilities; mandated runner; no `any`; error assertions on translated messages (never raw keys); no DB/`runInRollback` needed (pure module — zero DB ops by construction). Nuance recorded: the "NEVER call `getServerTranslations` directly in tests" rule sits under *Database* Tests (i18n-loader hang without env wiring); the colocated service-test precedent (`session-lifecycle.guards.test.ts:34-38`) resolves the slice via the mandated runner's `--env-file=.env.test` and runs green here (46/46) — followed exactly.
- `backend/services/AGENTS.md`: colocated service tests (§Testing) — basis of DV-T; no real external APIs; domain-scoped module (no monolith).
- `AGENTS.md` / `backend/AGENTS.md` (auto-discovered): mandated runner, no `bun test`, no db actions.

## 9. Deviations (all explained, none silent)

- **DV-T — test path colocated** (§4 above) instead of tasks.md's `__tests__/` sketch; AGENTS.md mandate + both sibling suites win.
- **DV-N — `assertRating0To5` returns `void`** (plan §4.2 sketch named it `assertRating01To5 …: number`; tasks.md 2.5 — the task of record — and the executed brief specify `assertRating0To5` with no return). 2.7 already holds the value; returning it would be dead surface.
- **DV-B — `assertHomeWorkBlock` name** per tasks.md (plan §4.2 sketch said `assertBlock`).
- **DV-P — wire-null presence semantics:** `validateAssignment` treats a runtime `null` leg as ABSENT via the private `isSuppliedBlock` predicate (beyond the plan sketch). Rationale: (a) Tier 3's "only ValidationError ever" must hold for wire-shaped payloads — a bare `=== undefined` gate would surface a TypeError on a null leg; (b) sonarjs `different-types-comparison` forbids direct `=== null` comparisons against the canonical type, so a `unknown`-parameter predicate is the only lint-clean encoding. Fail-closed, tested at Tier 4.
- **DV-A — `asserts id is number` predicate** added on the delegating wrapper (plan §4.2 requested the clause; the reused lifecycle guard returns plain `void`).

## 10. Carry-forward (contract for 2.7)

- Import block for `session-report.service.ts`:
  ```ts
  import {
    assertGrade0To100, assertHomeWorkBlock, assertPositiveSessionId,
    assertRating0To5, assertTeacherNotes, validateAssignment, validatePreviousGrades,
  } from "@/backend/services/classes/session-report.guards";
  ```
- Flow head: `const t = getServerTranslations(locale).errorsTranslations;` ONCE per flow, thread `t` into every guard (and into the lifecycle governance helper — same slice type).
- `assertTeacherNotes` RETURNS the trimmed value — map that return into `ReportInsertType.teacherNotes`; never persist the raw input.
- `assertPositiveSessionId(sessionId, t)` narrows the mutation-arg `unknown` to `number` for the probe/repo calls (pipeline step 0, before any DB work).
- Submission pipeline step 0 ordering: id → notes → rating → `validatePreviousGrades` (if `previousGrades`) → `validateAssignment` (if `homework`) — every denial throws before the governance probe; zero DB touch.
- `validateAssignment` null-leg = absent: 2.8's resolver may pass Pothos `InputMaybe` values through without pre-normalizing `null → undefined`.
- Denial keys ride the 1.4 map (§3); `ConflictError("SESSION_REPORT_ALREADY_EXISTS", …)` / `homeworkAlreadyGraded` are NOT guards' concern (DB-step denials).

## 11. Residual risks

- **2.M mid-point gate outcome** (`2.M-midpoint-gate.md`) was NOT present in the outcome dir at execution time (only Phase 0/1 outcomes exist); this task was executed on the orchestrator's explicit instruction. Gate checklist items referencing 2.1–2.4 remain the midpoint owner's to reconcile.
- Sibling-task in-flight edits (2.2/2.3 repos + tests) are uncommitted in the shared worktree and were actively edited during this session (transient oxlint observation, §5) — orchestrator owns commit hygiene; no interaction with this task's files.
- No coverage tooling per-file in `run-test.ts` — the 100% branch claim rests on the explicit matrix (§5) plus the sub-loop's tsgo/type-aware gates; if per-file coverage is later mandated, `bun run test:services:coverage` (whole-services scope) is the wired entry point.
- `tasks.md` checkboxes for 2.5 (incl. 2.5.QL/.TE/.SEC/.SR/.IV) left UNFLIPPED per the no-tasks.md-edit instruction — evidence for P6 flip-off is this document.
