# Task 13.1 Outcome — Knowledge Propagation (final task)

**Plan:** `ai/plans/sprint_1/DEV1-006-subscription-purchase-payment-gateway/`
**Task:** 13.1 · **Requirements:** REQ-080, REQ-081, REQ-082
**Inputs:** all 24 outcome files (0.1, 2.1–2.3, 3.1–3.2, 4.1–4.4, 5.1–5.2, 6.1–6.2, midpoint-review-R1, 7.1, 8.1, 9.1–9.3, 10.1–10.2, 11.1, 12.1 §9 round-2), `worklog.md`, SKILL.md "Knowledge Propagation" section.

---

## 1. Patterns Extracted (2+ occurrences across outcomes)

| # | Pattern | Occurrences | Propagation decision |
|---|---|---|---|
| P1 | **PGlite single-instance serialization** — one open per data dir; parallel DB invocations lock/timeout/corrupt (drift re-run hung on 2.1's lock; 9.3's live-server + test process corrupted the shared dir → WAL `PANIC`, filesystem-level rebuild required) | 2.1, 2.2 (lock contention timeout), 9.3 (incident + repair), 7.1 (probe ran on a data-dir copy), 11.1 | **Propagated** — 2-line rule in `.agents/instructions/tests.instructions.md` (serialize DB commands/suites via process-lock runners; never live-server harness + DB suites concurrently). Codebase-wide while PGlite is the dev/test provider. |
| P2 | **Immutable-ledger test residue discipline** — `student_payments` rows can never be deleted normally (BEFORE DELETE trigger; even set-null path is blocked); committed fixtures are permanent residue | 7.1 (Decision 1, probe-proven), 9.3 (cleanup order), 11.1 (suspension leg first + `jrn_billing_<8hex>` prefixes) | **Propagated** — same 2-line tests.instructions block (`withImmutabilityTriggersSuspended([table])` teardown-first + unique per-run fixture prefixes). |
| P3 | **In-process `graphql()` harness requirement** — spawned-server harness yields disjoint PGlite snapshots (wire boundary and test process never see the same rows) | 9.3 (5/9 → 9/9 after rebuild), 11.1 (step 7/9 in-process), 10.1 (lane suite precedent) | **Propagated** — folded into the P1 instruction line (never run the live-server harness concurrently with DB-backed suites). |
| P4 | **ReturnType conversion gotcha** — raw `$inferSelect` rows are NOT assignable to enum-typed `*ReturnType` shapes; services map via total-over-vocabulary mappers (probe-verified TS2322 in 3.1; exercised in 6.1/6.2/7.1/9.1) | 3.1, 6.1, 6.2, 7.1, 9.1 | **Propagated** — 1 line in `.agents/instructions/backend.instructions.md` (Type Definition Pattern section): mappers, never the raw row; enum members flow without casts. |
| P5 | **Custom migration pairing convention** — every `backend/db/migration/*.sql` auto-bundles into its own `drizzle/<ts>_custom_<slug>/` folder; SQLite parity files are NEVER bundled and MUST be added to `EXCLUDED_FILES` in `applyCustomMigrations.ts` or `migrate` fails on PG syntax | 2.2 (rule + one-line entry added), midpoint FIX-2 context, carried into backend.instructions | **Propagated** — 1 line in `.agents/instructions/backend.instructions.md` (Seeds section, next to the push/migrate policy). |
| P6 | **Guarded-transition doctrine: zero-row = replay** — the `WHERE status='pending'` predicate IS the lock; replays/quarantines ack `200 {processed:false}` instead of throwing (settlement integrity beats liveness) | 4.1, 4.2, 6.1, 7.1 (whole decision log), 8.1, 11.1; consistent with account-governance/session-lifecycle precedents | **Propagated** — domain content of the canonical doc (§3/§4/§7) + one clause each in the backend/AGENTS.md and backend/services/AGENTS.md rule lines. |
| P7 | **Fail-closed plan-lane doctrine** — NULL `plans.balance_lane` blocks purchase AND rolls back activation; the lane is never guessed | 2.1 (nullable column), 2.3 (seeds converge non-NULL), 4.4 (no-COALESCE credit), 6.1, 7.1, 10.1 | **Propagated** — canonical doc §1 invariant 6 + AGENTS rule lines + open-decisions addendum E.1. |
| P8 | **Webhook fail-closed security posture** — bare-404 kill switch, byte-exact HMAC over single raw-body read, masked 401 collapses misconfiguration with forgery, 64KB byte cap, no payload/secret/financial logging | 5.1, 5.2 (forged-probe matrix), 8.1 (route contract), 11.1 (step 10) | **Propagated** — canonical doc §5 + backend/AGENTS.md rule line. |

## 2. Patterns Verified but Deliberately NOT Propagated (filter per SKILL.md)

| Pattern | Why not |
|---|---|
| **PGlite cannot open dataDir through a symlink** (worktree `PGLITE_DATA_DIR=/home/z/my-project/db/pglite` env fix, 6.5-fixes/9.3) | Sandbox/worktree-specific, not a codebase rule; the fix lives in the gitignored `.env`/`.env.test` and the 6.5/9.3 outcomes. Adding it to AGENTS would encode a machine path as global policy. |
| **`.env.test` / `.env.test.ci` materialization quirks** (`run-test.ts` hard-fails without `.env.test`; UI suites need `TEST_CI` from `.env.test.ci`; `applyDbEnvOverride` placeholder-URL override) | Environment/runner facts already encoded in the runners themselves + 5.1/10.2/parent-link-request docs; not new permanent conventions. |
| **`Subscription` wire-name reservation** (graphql-js default-root auto-adoption) | Entity-specific GraphQL fact; captured in the canonical doc's overview + 9.1 outcome and enforced by `sdl-static-assertions.test.ts` pins — not a layer-wide AGENTS rule. |
| **Junction insert at service level (no `student_subscriptions` repository)** | Plan-defined design, recorded in 6.1 outcome + midpoint R1-10 acceptance; adding it to AGENTS would be a plan-specific constraint. |
| **Trigger probe methodology** (savepoints created AFTER the insert, literal timestamps for `created_at` freeze, cause-chain walker) | Test-technique detail; lives in the 2.2/4.2 outcome files and is already covered by `runInRollback`/`expectRepoError` conventions in tests.instructions.md. |
| **`*.md` files cannot pass sub-loop's oxlint stage** ("No files found to lint") | Tooling behavior, re-confirmed this task; already documented in 8.1's outcome — prose verification uses `--lifecycle tsgo`. |
| ** Vendored drizzle skills** (`.agents/skills/drizzle/SKILL.md`, `drizzle-migrations/SKILL.md`) | Deliberately NOT edited: they are generic project-agnostic drizzle-kit skills (staleness check / per-dialect catalogs, refreshed by `drizzle-kit skills`) — repo-specific `applyCustomMigrations` rules would be overwritten on refresh and do not belong there; the rule went to the project-owned `.agents/instructions/backend.instructions.md` instead. |

## 3. Files Created / Modified

| File | Change |
|---|---|
| `docs/billing/subscription-purchase.md` | **CREATE** — canonical reference (REQ-080): gateway port + mock provider + swap-in path, purchase contract (pending triple, fixed tx order, idempotency + error channels), idempotency tables & structural guards, webhook security contract (kill switch → byte cap → HMAC → parse → service, log hygiene, deployment checklist), ledger trigger amendment (INV-PAY2 exception with frozen columns + writer rules), guarded activation + lane credit + window semantics, GraphQL surface, operational env keys, consumer guidance (DEV1-007/008/009 + DEV2-005) with binding testing notes, anti-pattern list. Domain language only — grep `REQ-\|tasks\.md\|plan\.md\|specs\.md\|ai/plans\|sprint\|DEV1-006` → zero hits (consuming ticket ids DEV1-007/008/009 + DEV2-005 are the task-mandated consumer guidance, matching sibling canonical docs). |
| `docs/specs/state-machine-invariants.md` | **ADDENDA** (REQ-081): §8 gains an implementation-reference pointer to the canonical doc; INV-PAY2 row amended with the guarded status-transition exception (single permitted UPDATE, six frozen columns, terminal decided payments); new **INV-PAY6** (exactly one `pending → paid` activation ever; zero-row = replay; late-confirmed-after-failed rejected-and-logged) and **INV-PAY7** (amount/currency mismatch or missing ledger row quarantines — zero mutation, bounded correlation-id log, `200 {processed:false}`); §4.1 gains the pending-semantics reconciliation (pending-until-paid is the A.9 enum meaning for A.9-compliant status writers; the date-window reading is phase, not enum state). |
| `docs/specs/open-decisions-and-gaps.md` | **ADDENDA** (REQ-081): new section **E. Resolved During Implementation — Subscription Purchase & Payment Gateway** with E.1 (plan-decision D4: `plans.balance_lane` lane encoding, fail-closed purchase/activation on NULL, admin null-vs-absent update semantics, seeder backfill), E.2 (D5: `mock` as a first-class `payment_gateway` member — audit honesty, never mapped to `other`), E.3 (D8: no student purchase UI — the mutation is the contract; checkout funnel belongs to Sprint-2 real-gateway work). Section-lettered consistently with the existing DEV1-004 addenda section D; summary tables untouched (established convention). |
| `AGENTS.md` (root) | **+1 line** in Important References pointing to `docs/billing/subscription-purchase.md` (one-line digest incl. consumer guidance). |
| `backend/AGENTS.md` | **+2 lines** — new short section "Subscription Purchase & Payments (Billing Domain)": one permanent rule line (idempotency-claimed guarded transitions, append-only ledger with single trigger-guarded exception, kill-switch + HMAC fail-closed webhook, quarantine semantics) + canonical-doc pointer. |
| `backend/services/AGENTS.md` | **+1 rule line** next to the plan-catalog bullet: service ownership (`SubscriptionPurchaseService` / `SubscriptionActivationService`), claim → pending pair → verified webhook → guarded activation (zero-row = replay, exactly-once credit), fail-closed NULL lane, "never write `student_payments` outside the guarded decision writers", ack-not-throw posture, doc pointer. |
| `.agents/instructions/backend.instructions.md` | **+2 lines** (permanent cross-plan rules only): (a) ReturnType conversion rule under Type Definition Pattern; (b) custom-SQL auto-bundler rule + `*-sqlite.sql` → `EXCLUDED_FILES` mandate under Seeds, next to the push/migrate policy. |
| `.agents/instructions/tests.instructions.md` | **+2 lines** — new "PGlite Single-Instance & Immutable-Ledger Cleanup (CRITICAL)" block: single-open serialization / no live-server harness with DB suites; suspension-first teardown + unique fixture prefixes for append-only tables. |
| `ai/plans/.../tasks.md` | 13.1 checkbox → `[x]`. |
| `worklog.md` | Task 13.1 entry appended. |

**Skills updated:** none (see §2 — vendored drizzle skills deliberately untouched; no other skill's domain is affected by a permanent cross-plan pattern).

## 4. Verification

| Check | Result |
|---|---|
| `bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates` on the canonical doc | tsgo ✅; oxlint stage structurally N/A for `.md` ("No files found to lint") → per the 8.1 precedent, prose verified at the deepest supported stage: |
| `bun run scripts/health/sub-loop.ts <file> --lifecycle tsgo` × 8 (canonical doc, both specs addenda, root/backend/services AGENTS.md, both instructions files) | **all EXIT 0** — "tsgo passed (no errors for <file>)" |
| `bun run tsgo` (whole repo) | **EXIT 0, 0 errors** (silent pass — baseline preserved) |
| Mermaid validation of the canonical doc's sequence diagram (`bun run validate:mermaid docs/billing/subscription-purchase.md`) | ✅ 1 file, 1 diagram OK |
| Plan-artifact scan of the canonical doc (`rg "REQ-\|tasks\.md\|plan\.md\|specs\.md\|ai/plans\|sprint\|DEV1-006\|13\.1"`) | CLEAN (domain language only) |
| Scope | `git status` = exactly the 8 knowledge files above + tasks.md checkbox + this outcome + worklog. No TS/TSX sources touched → no test re-runs required; tsgo re-run green. |

## 5. Notes for the Orchestrator

- This closes the final task: tasks.md is now 49 `[x]` / 0 `[ ]` (12.1 main was closed by the orchestrator; 13.1 flipped by this task).
- Pre-existing carries from 12.1 remain unchanged and outside 13.1's scope: QG-4 (full-repo `lint:type-aware` OOM — infra ask: lint-service child heap) and the gate-sanctioned `backend/drizzle/20260907182455_ancient_scarecrow/snapshot.json` biome formatting awaiting commit.
- No commits made (per environment policy — orchestrator commits).
