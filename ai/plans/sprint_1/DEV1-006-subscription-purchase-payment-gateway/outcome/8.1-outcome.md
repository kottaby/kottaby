# Task 8.1 Outcome — Payments webhook route

**Date:** 2026-09-07
**Agent:** Webhook Route Subagent (8.1)
**Requirements:** REQ-002, REQ-004, REQ-020, REQ-021, REQ-022, REQ-043, REQ-044, REQ-045, REQ-072

## Summary

`app/api/payments/webhook/route.ts` — the gateway callback surface as a thin trust-boundary shell in exact cron-route parity (`app/api/cron/sweep-sessions/route.ts`): POST-only (only `POST` is exported; every other verb rides the framework 405); the kill switch answers FIRST — `isPaymentWebhookEnabled()` false ⇒ BARE 404 (`new Response(null)`, no envelope/code/requestId, no existence oracle); body read EXACTLY ONCE via `request.text()` and capped at `MAX_PAYMENT_WEBHOOK_BODY_BYTES = 64_000` UTF-8 **bytes** (`TextEncoder` count — a multibyte body under 64_000 characters is still over-cap); over-cap ⇒ masked 400 envelope (`ValidationError("PAYMENT_WEBHOOK_BODY_TOO_LARGE", generic)` — custom code falls through `statusForTransportCode` to the BAD_REQUEST row; payload never echoed); signature gate fail-closed — `secret === undefined || secret.length === 0 || !verifyWebhookSignature(rawBody, header, secret)` all collapse into ONE masked 401 (`DomainError("UNAUTHORIZED", "Invalid payment webhook signature.")`), so a misconfigured deployment is indistinguishable from a forgery and the gate is never bypassable; parse through `getPaymentGateway("en").parseWebhookEvent(rawBody)` (real mock adapter, deterministic) with the service delegation inside one try/catch — `SubscriptionActivationService.processWebhookEvent(event, "en")` (2-arg production call) ⇒ `apiSuccessResponse({ processed, ...(replayed !== undefined && { replayed }) }, { requestId })`; replays AND verified-but-unknown/quarantined events ack 200 (`{processed:false}` — settlement integrity beats liveness); any throw ⇒ `apiErrorResponse` masked envelope + `resolveRequestId` correlation. Locale "en" envelope throughout (server-to-server). NO session reads, NO route-side logger calls (the only log line is the lib's masked-path `logger.error`, redacted, on true infrastructural failures), zero plan artifacts in comments.

## Files Created / Modified

| File | Change |
|---|---|
| `app/api/payments/webhook/route.ts` | CREATE — the POST handler per plan §3.4 contract table |
| `app/api/payments/webhook/test/payments-webhook-route.test.ts` | CREATE — 8.1.TE + 8.1.SEC route-level suite (27 tests) |
| `backend/lib/gateway/route-inventory.ts` | EXTEND — `{ path: "/api/payments/webhook", classification: "provider-ack-exempt" }` + (cross-file fix, below) `{ path: "/api/cron/sweep-sessions", classification: "envelope" }`; docblock ground truth 3→5 routes; `provider-ack-exempt` vocabulary entry reworded from "future" to the landed webhook-ack semantics |
| `backend/lib/gateway/route-inventory.test.ts` | EXTEND — Tier 2 ground-truth rows for both new entries + frozen-ordering snapshot 3→5 paths |
| `docs/graphql/error-handling-contract.md` | EXTEND — exemptions-inventory row for the payments webhook (mirrors the graphql/set-locale row format: Surface / Exemption / Basis, domain language only) |
| `tasks.md` | 8.1 + sub-checkboxes flipped |

## CROSS-FILE FINDING — the cron route was unregistered; the completeness assertion was already red

The mandated verification ("run the route-inventory test") FAILED AT BASELINE before any of my edits: `app/api/cron/sweep-sessions/route.ts` (shipped by DEV3-012) physically exists but had NO `ROUTE_INVENTORY` row, so both the Tier-3 live-tree completeness test (`route-inventory.test.ts`) and static assertion A4 (`static-assertions.test.ts`) were red on `main`-derived state — exactly the drift the task told me to probe for. Fixed in this change set by registering the cron row as `"envelope"` (its actual posture: bearer-gated REST envelope contract with a route-documented bare-404 kill switch) alongside the payments row. Both suites are green again (14/14 and 17/17). Related gap REPORTED, not fixed (scope): the cron route's bare-404 is not itself rowed in the exemptions inventory, and `docs/graphql/api-gateway-and-routing.md` still prints "exactly THREE physical routes" — both pre-existing staleness, flagged as carry-forward.

## Classification Decision

`"provider-ack-exempt"` EXISTS in the closed four-value vocabulary (`gateway | envelope | provider-ack-exempt | deferred`) and is used as mandated. Note the vocabulary's old docblock described it as future "reply-with-provider-contract" acks; the landed payments webhook replies with the REST envelope on every enabled path, so the route-inventory docblock entry was reworded to the now-accurate semantics (webhook ack surface, enveloped replies, route-documented exemptions registered in the error-handling contract). The only actual envelope EXEMPTION is the disabled kill switch's bare 404 — registered as the exemptions-inventory row.

## Verification Results

### Per-file quality loop — `sub-loop.ts <file> --lifecycle duplicates`

| File | tsgo | oxlint | biome:check | lint:type-aware | check:duplicates |
|---|---|---|---|---|---|
| `app/api/payments/webhook/route.ts` | ✅ | ✅ | ✅ | ✅ | ✅ |
| `app/api/payments/webhook/test/payments-webhook-route.test.ts` | ✅ | ✅ | ✅ | ✅ | ✅ |
| `backend/lib/gateway/route-inventory.ts` | ✅ | ✅ | ✅ | ✅ | ✅ |
| `backend/lib/gateway/route-inventory.test.ts` | ✅ | ✅ | ✅ | ✅ | ✅ |
| `docs/graphql/error-handling-contract.md` | ✅ (tsgo lifecycle) | n/a | n/a | n/a | n/a |

Lint fixes applied during the loop: boolean-compare `!== true` → `!` (oxlint) on the kill switch; test-side `no-await-in-loop` → parallel `Promise.all` forgery burst; `no-unnecessary-type-assertion` dropped; `prefer-specific-assertions` `.toBe(len)` → `.toHaveLength(len)`. **Docs-file limitation (environmental, not content):** the sub-loop structurally cannot pass an `.md` file past oxlint (`No files found to lint` — non-zero exit for any non-TS target), so the docs file was verified at `--lifecycle tsgo` (EXIT 0), the deepest stage the tooling supports for prose.

### Whole-repo checks

- `bun run tsgo` → **EXIT 0, 0 errors**.
- Instruction files printed by sub-loop: `.github/instructions/*` ABSENT from the tree (environmental, consistent with all prior outcomes); AGENTS.md files read: root, `app/AGENTS.md` (route-inventory SAME-CHANGE-SET rule), `backend/AGENTS.md` (envelope-helpers barrel rule).

### Test runs — `bun run test/scripts/run-test.ts <path>`

- **`app/api/payments/webhook/test/payments-webhook-route.test.ts`: 27 pass / 0 fail / 99 expect() calls.**
  - **Tier 1 (kill switch):** disabled (unset) ⇒ bare 404 with EMPTY body even for a perfectly signed delivery, service never invoked; `"false" | "1" | "yes" | "TRUE"` all bare-404; positive pin that the parser TRIMS (`" true"` enables — parser-owned semantics, documented at the route boundary).
  - **Tier 1 (signature gate, fail closed):** missing secret config ⇒ 401 `UNAUTHORIZED` (exact envelope keys `["code","message","requestId"]`); whitespace-only secret ⇒ 401 (trim counts as unset); missing header ⇒ 401; empty header ⇒ 401; forged (attacker-secret) signature ⇒ shared masked message; tampered body (valid sig over different bytes) ⇒ 401. Service mock NEVER invoked on any deny path — the route-level "zero DB writes" guarantee is structural (the service's own transactional zero-mutation proofs live in the 7.1 suite).
  - **Tier 2 (bounded body):** byte-exact boundary — padded event body of EXACTLY 64_000 bytes signed + accepted end-to-end (200 `{processed:true}`); cap+1 ⇒ masked 400 `PAYMENT_WEBHOOK_BODY_TOO_LARGE` with zero payload echo (wire-JSON scanned for the padding token AND a raw-body prefix); multibyte probe — 40_000 two-byte chars (80_000 bytes) ⇒ 400 (bytes, not characters, govern).
  - **Tier 2 (envelope contract):** happy path ⇒ 200, body keys exactly `["data","requestId"]`, data keys exactly `["processed"]`, inbound `X-Request-Id` echoed, service called ONCE with the parsed event + locale `"en"`; replay ⇒ data keys exactly `["processed","replayed"]`; `{processed:false}` ack posture ⇒ 200; malformed JSON (validly signed) ⇒ 400 `PAYMENT_WEBHOOK_MALFORMED` with the localized generic `errorsTranslations.validation` message and zero reference echo; structurally-invalid event (unknown outcome) ⇒ same 400.
  - **Tier 3 / masked-path:** service-thrown non-domain error ⇒ 500 `INTERNAL_SERVER_ERROR`, raw text absent from the wire JSON, EXACTLY ONE correlated `logger.error` carrying the SAME requestId, and the log bag contains no body material.
  - **Tier 4 / 8.1.SEC (spoofing probes):** 12 randomized-signature forgeries in a parallel burst ALL denied (service never invoked) while the correctly-signed twin passes; uppercase-hex re-encoding of a valid signature DENIED (lowercase-hex wire contract); empty-secret-config fail-closed (above).
  - **Static route-source pins:** POST-only export surface (no `GET|PUT|DELETE|PATCH|HEAD|OPTIONS` export); the bare kill-switch 404 is the single numeric status decision (no `status: <digit>` literals — the 404 lives only in the named constant); zero `console.` disclosure sites.
  - Harness: pure-function tier per the cron/set-locale precedent — `NextRequest`s constructed directly, NO server boot; `SubscriptionActivationService` mocked at its module boundary (registered BEFORE the route import, cron-test idiom) so no DB statements run; signatures computed with an INDEPENDENT `createHmac(...).digest("hex")` oracle, never the production verifier; env controlled via `process.env` + `resetEnvironmentCache()` (typed getters read the cached snapshot — unlike the cron route's cache-free `getEnv`) with full restore in `afterEach`.
- **`backend/lib/gateway/route-inventory.test.ts`: 14 pass / 0 fail / 33 expect() calls** (was 11 pass / 1 FAIL at baseline — the cross-file finding above).
- **`backend/lib/gateway/static-assertions.test.ts`: 17 pass / 0 fail** (A4 registry↔disk completeness green again).
- **Regression `app/api/cron/sweep-sessions/test/sweep-sessions-route.test.ts`: 9 pass / 0 fail** (pinned counts unchanged).

### Semantic review (SKILL checklist)

- ✅ Stateless handler — no module-level mutable state, no read-then-write, no Redis, no cache mutations.
- ✅ Env reads only through the 5.1-registered typed getters (`isPaymentWebhookEnabled`, `getPaymentWebhookSecret`) in `@/backend/lib/env`; no new keys; fail-closed defaults preserved (unset ⇒ disabled ⇒ bare 404).
- ✅ No dead branches — every path (404 / 400 oversize / 401 ×4 deny shapes / 400 malformed / 200 ×3 / 500 masked) is test-exercised.
- ✅ NO session reads (grep clean — the only "session" match is the docblock sentence); authorization is the signature alone (REQ-043).
- ✅ Zero raw-body/secret logging: the route declares NO logger call sites (grep clean); the sole masked-path log is the lib's redacted line (asserted to carry requestId only).
- ✅ Single body read (`request.text()` once — 5.2's byte-exactness contract preserved: the signature is computed over the untouched bytes).
- ✅ Clean comments — grep for plan artifacts (`REQ-|Task [0-9]|tasks\.md|plan\.md|specs\.md|DEV1-006|Phase [0-9]|sprint`) over all 4 TS files: clean; docs row uses domain language only.
- ✅ Envelope helpers from the `@/backend/lib/api` barrel (no hand-rolled bodies); statuses derive exclusively through the taxonomy (`404` bare-404 is the documented single-constant exception, cron parity).
- ✅ Scope: `git status` shows exactly the files listed above (+ outcome/worklog bookkeeping); no commits (orchestrator commits).

## Carry-forward (route contract for 9.3 / 11.1)

1. **9.3 (GraphQL contract tests)** may reuse the envelope pins here as the REST-side twin; the webhook never surfaces domain codes to callers except the three intentional ones: `PAYMENT_WEBHOOK_BODY_TOO_LARGE` (400), `PAYMENT_WEBHOOK_MALFORMED` (400, adapter-produced), `UNAUTHORIZED` (401, single masked message for all deny shapes). Everything from the service comes back as 200 `{processed, replayed?}`.
2. **11.1 (journey)**: drive the real surface via `POST /api/payments/webhook` with `createHmac("sha256", secret).update(rawBody).digest("hex")` headers against the committed purchase pairs; the route test's env recipe (set `PAYMENT_WEBHOOK_ENABLED`/`PAYMENT_WEBHOOK_SECRET` then `resetEnvironmentCache()`) is the validated way to arm the kill switch in-process.
3. **Deployment**: unset `PAYMENT_WEBHOOK_ENABLED` ⇒ bare 404 (fail closed); empty/whitespace `PAYMENT_WEBHOOK_SECRET` while enabled ⇒ every callback 401s (fail closed) — ops must configure both together.
4. **OPEN cross-file gaps (not fixed, outside task scope):** (a) `docs/graphql/api-gateway-and-routing.md` route table + "exactly THREE physical routes" prose is stale (also missing the cron row); (b) the cron route's bare-404 exemption has no row in the exemptions inventory despite the "register before shipping" rule; (c) `.github/instructions/*.md` absent from the tree (sub-loop prints them with ⚠).

## Cross-File Dependencies

1. Consumed from 5.1/5.2/7.1 as contracted: `isPaymentWebhookEnabled()`/`getPaymentWebhookSecret()` (`@/backend/lib/env`), `getPaymentGateway(locale).parseWebhookEvent(rawBody)` + `verifyWebhookSignature(rawBody, header, secret)` (`@/backend/services/billing/payment-gateway` barrel), `SubscriptionActivationService.processWebhookEvent(event, "en")` 2-arg production call (service module imported directly — cron-route import precedent; its module specifier is the test mock seam).
2. `ROUTE_INVENTORY` rows and the frozen-ordering snapshot in `route-inventory.test.ts` must move together with any future route file (A4 + Tier 3 both walk the live tree bidirectionally).
3. Test count pinned for regression: **27 tests / 99 expects** in `payments-webhook-route.test.ts`; the service mock, the signature oracle, and the static route-source pins are load-bearing.
