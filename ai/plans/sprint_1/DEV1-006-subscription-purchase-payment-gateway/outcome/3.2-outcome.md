# Task 3.2 Outcome — Enum layer

**Date:** 2026-09-07
**Agent:** Enum Layer Subagent
**Requirements:** REQ-005, REQ-034, REQ-061

## Summary

Created the `SubscriptionCreditLane` TS enum mirroring the `subscription_credit_lane` pgEnum (task 2.1) and added the `Mock = "mock"` member to the `PaymentGateway` TS enum mirroring the extended `payment_gateway` pgEnum. Both enums follow the exact sibling conventions in `backend/enum/billing/` (top-of-file JSDoc naming the mirrored pgEnum as canonical, PascalCase members, lowercase snake_case values in pgEnum order). Barrel extended in ASCII order. No consumers needed exhaustiveness updates.

## Files Created / Modified

| File | Change |
|---|---|
| `backend/enum/billing/subscription-credit-lane.enum.ts` | CREATE — `SubscriptionCreditLane` enum (`Hifz = "hifz"`, `Tajweed = "tajweed"`, `Reviews = "reviews"`) |
| `backend/enum/billing/payment-gateway.enum.ts` | EXTEND — appended `Mock = "mock"` after `Other = "other"` (pgEnum order); JSDoc order note updated + one line on mock-vs-`other` ledger distinguishability |
| `backend/enum/billing/index.ts` | EXTEND — `export * from "./subscription-credit-lane.enum";` (ASCII order: after `./payment-status.enum`, before `./subscription-status.enum`) |

## Files NOT Modified (and why)

- `backend/db/schema/enums.ts` — the pgEnums are task 2.1's output (already in tree); this task only mirrors them. Values verified 1:1: pgEnum `["hifz","tajweed","reviews"]` ↔ TS members; pgEnum `payment_gateway` 9th member `"mock"` ↔ `PaymentGateway.Mock`.
- `backend/types/billing/*` — task 3.1 scope (runs in parallel; my enum names are its inputs).
- Seeds, repos, services, GraphQL — tasks 2.3/4.x/9.1 scope.
- No top-level `backend/enum/index.ts` change needed — it already re-exports `./billing`.

## PaymentGateway Exhaustiveness Sweep

Grepped all `.ts`/`.tsx` usages of `PaymentGateway` (backend/frontend/shared/app/test, excluding node_modules): 5 files total.
- `backend/db/repo/admin/platform-analytics-query-helpers.ts` — `OFFLINE_ACTIVATION_GATEWAYS: readonly PaymentGateway[]` array literal (subset: OfflineCash/BankTransfer/Scholarship). Not exhaustive; unaffected by a new member.
- `backend/db/test/entity-setup.ts`, `backend/db/test/logic/admin/platform-analytics.repository.test.ts`, `test/workflows/admin/platform-analytics.journey.test.ts` — single-member references (`PaymentGateway.Stripe`, `PaymentGateway.OfflineCash`) in `inArray`/insert literals. Unaffected.
- Zero exhaustive `switch` statements, zero `Record<PaymentGateway, …>` maps, zero `satisfies` maps exist anywhere. Confirmed by whole-repo `bun run tsgo` exit 0 after the member addition — the compiler demanded nothing.

## Verification Results

### Per-file quality loop — `sub-loop.ts <file> --lifecycle duplicates` (all EXIT 0, run after each edit)

| File | tsgo | oxlint | biome:check | lint:type-aware | check:duplicates |
|---|---|---|---|---|---|
| `subscription-credit-lane.enum.ts` | ✅ | ✅ | ✅ | ✅ | ✅ |
| `payment-gateway.enum.ts` | ✅ | ✅ | ✅ | ✅ | ✅ |
| `index.ts` (billing barrel) | ✅ | ✅ | ✅ | ✅ | ✅ |

Instruction files printed/read: root `AGENTS.md`, `backend/AGENTS.md`, `.agents/instructions/backend.instructions.md`; layer `backend/enum/AGENTS.md` + `backend/types/AGENTS.md` read manually pre-execution. Barrel conventions verified: pure `export *` with `./` relative paths, no imports, ≤ one `/` per path.

### Whole-repo typecheck

- `bun run tsgo` → **EXIT 0** (silent pass), run once after all edits.

### Semantic review (SKILL checklist)

- ✅ Enum members mirror pgEnum values/order exactly (checked against `backend/db/schema/enums.ts` lines 35–65)
- ✅ No plan-meta in comments (grep for `REQ-|Task 3|tasks\.md|plan\.md|specs\.md|DEV1-006|Phase ` over `backend/enum/billing/`: clean) — comments describe domain behavior only
- ✅ No cross-layer imports (files define/re-export enums only); no dead branches; no runtime enum references needing value imports in the touched files
- ✅ No string literals where enum types expected in files touched
- ✅ Scope: `git diff --name-only` for `backend/enum/` shows exactly the 3 files above; all other working-tree diffs belong to tasks 2.1/2.2 (schema layer) per their outcome files
- ✅ No deferred work introduced → no `deferred-items.md` entry needed
- ✅ No DB commands run (PGlite lock respected); no commits (per environment policy)

## Carry-Forward Knowledge (for tasks 3.1, 2.3, 9.1)

- **Exported symbols** (deep-import preferred per `backend/enum/AGENTS.md`):
  - `SubscriptionCreditLane` — `import { SubscriptionCreditLane } from "@/backend/enum/billing/subscription-credit-lane.enum";` — members `Hifz = "hifz"`, `Tajweed = "tajweed"`, `Reviews = "reviews"` (order matches pgEnum).
  - `PaymentGateway` — `import { PaymentGateway } from "@/backend/enum/billing/payment-gateway.enum";` — now 9 members; new member `Mock = "mock"` (last, mirroring pgEnum append order).
- **TASK 3.1 (billing types):** type `SubscriptionReturnType.paymentMethod` as `PaymentGateway | null`; Pothos `Plan.balanceLane` exposure uses `SubscriptionCreditLane`. Import enums as VALUE imports whenever members appear in runtime expressions (REQ-005); `import type` is fine for type-position-only usage.
- **TASK 2.3 (seeds):** lane member names are `SubscriptionCreditLane.Hifz` / `.Tajweed` / `.Reviews` (values `"hifz"|"tajweed"|"reviews"`).
- **TASK 9.1 (GraphQL):** register `SubscriptionCreditLanePothosEnum` from `SubscriptionCreditLane` in `backend/graphql/pothos/shared/enum.pothos.ts` (value-object form, single registration); the SDL enum is `SubscriptionCreditLane { HIFZ TAJWEED REVIEWS }` — GraphQL name normalization happens at registration, TS values stay canonical. `PaymentGateway` registration must include `Mock` → `MOCK`.
- **Mock gateway note for tasks 5/6:** `PaymentGateway.Mock` is the ledger-facing value for mock-provider payments; never map it to `Other` (audit honesty).

## Cross-File Dependencies Discovered

- `backend/enum/billing/index.ts` ordering is ASCII-sensitive; the new line sits between `payment-status` and `subscription-status` because `subscription-credit-lane.enum` < `subscription-status.enum` at the divergence char (`c` < `s`).
- Task 3.1's type files reference these enums; if dispatched truly in parallel with this task, its first tsgo run must happen after this task lands (both sides green as of this outcome).
