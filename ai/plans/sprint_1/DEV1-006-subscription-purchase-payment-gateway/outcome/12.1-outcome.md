# Task 12.1 Outcome — Final Quality Gate (baseline comparison + deferred enforcement)

**Date:** 2026-09-08
**Agent:** Final Gate Subagent
**Plan:** `ai/plans/sprint_1/DEV1-006-subscription-purchase-payment-gateway/`
**Baseline reference:** `outcome/0.1-outcome.md` (tsgo 0 / biome 0 / lint 0, clean tree at ffce457)
**Gate inputs:** SKILL.md "Test-Layer Coverage Gate" + "Deferred-Items Enforcement" + "Execution Summary Template"; tasks.md 12.1

---

## 1. Baseline Comparison (trio re-run)

| Metric | Baseline (0.1) | Final (12.1) | Delta | Status |
|---|---|---|---|---|
| tsgo — lines matching `error TS` | 0 | **0** (`bun run tsgo` exit 0) | **0** | ✅ PASS |
| biome:check | exit 0, "No fixes applied" (1419 files) | exit 0, "Checked 1455 files … Fixed 1 file" | 0 errors; +1 gate-sanctioned formatting fix | ✅ PASS (see note B-1) |
| lint-service `--json --id final` | `success: true`, output empty, exit 0 | **`success: true`**, output empty, exit 0 | 0 | ✅ PASS |

- tsgo log: `/tmp/gate-121-tsgo.log` (0 `error TS` lines); biome log: `/tmp/gate-121-biome.log`; lint JSON: `/tmp/gate-121-lint.json`.
- **B-1 — biome auto-fix (gate-sanctioned formatting):** `backend/drizzle/20260907182455_ancient_scarecrow/snapshot.json` (working tree, uncommitted): 117 insertions / 388 deletions — purely JSON array-collapsing formatting; JSON-semantic no-op; drift-inert (drizzle compares schema statements, not file bytes). Deterministic: reverting it makes the next `biome:check` re-apply it. **Orchestrator: commit it as a gate-sanctioned format fix.**
- No pre-existing-issue filtering was needed — baseline was fully green (0/0/0), so any trio error would have been attributable to this plan; there are none.

## 2. Deferred-Items Enforcement — ✅ PASS

| Check | Expected | Result |
|---|---|---|
| `grep -c "❌\|⚠️" deferred-items.md` | 0 | **0** ✅ |
| Ledger rows with status `Blocked` or `Partial` | 0 | **0** ✅ |
| Ledger status values actually present | — | `—` (main ledger, empty since authoring) + `📋 Pre-existing` ×2 (PRE-1 plan-catalog.roles UNAUTHORIZED-legs; PRE-2 BroadcastCompose SIGABRT) |

- `grep -cE "Blocked|Partial"` returns 4, but every hit is legend/enforcement **prose** (deferred-items.md lines 26, 27, 57, 61) — zero ledger status rows. The ledger's own exit criterion ("cannot be marked complete if any Blocked or Partial status row remains") is satisfied.
- PRE-2 was respected by this gate: UI components were verified via the **sanctioned scoped run** (10.2 mechanism), never the full `test:ui:components`.

## 3. Test-Layer Coverage Gate — ✅ PASS (22 suites · 433 pass · 0 fail · 2 sanctioned skips · 2689 expect())

All runs serialized (process-lock); DB-backed suites via `bun run test/scripts/run-test.ts` (PGlite).

| Layer | Suite | Pass / Fail / expect() | Status |
|---|---|---|---|
| Repo/DB logic | `backend/db/test/logic/billing/subscription.repository.test.ts` | 6 / 0 / 44 | ✅ |
| Repo/DB logic | `backend/db/test/logic/billing/student-payment.repository.test.ts` | 7 / 0 / 52 | ✅ |
| Repo/DB logic | `backend/db/test/logic/billing/subscription-purchase-idempotency.repository.test.ts` | 6 / 0 / 37 | ✅ |
| Repo/DB logic | `backend/db/test/logic/billing/plan-catalog.repository.test.ts` | 16 / 0 / 88 | ✅ |
| Repo/DB logic | `backend/db/test/logic/billing/plan-catalog.service.test.ts` | 22 / 0 / 72 | ✅ |
| Repo/DB logic | `backend/db/test/repo/students/student.repository.test.ts` | 23 / 0 / 97 | ✅ |
| Service unit | `backend/services/billing/subscription-purchase.service.test.ts` | 16 / 0 / 115 (+1 sanctioned skip) | ✅ |
| Service unit | `backend/services/billing/subscription-activation.service.test.ts` | 12 / 0 / 108 (+1 sanctioned skip) | ✅ |
| Service unit | `backend/services/billing/payment-gateway/payment-gateway.factory.test.ts` | 19 / 0 / 94 | ✅ |
| Service unit | `backend/services/billing/payment-gateway/webhook-signature.helpers.test.ts` | 20 / 0 / 93 | ✅ |
| Cross-actor journeys | `test/workflows/billing/subscription-purchase.journey.test.ts` | 10 / 0 / 89 | ✅ |
| GraphQL integration | `backend/graphql/test/subscription-purchase.schema.test.ts` | 14 / 0 / 86 | ✅ |
| GraphQL integration | `backend/graphql/test/subscription-purchase.roles.test.ts` | 10 / 0 / 32 | ✅ |
| GraphQL integration | `backend/graphql/test/subscription-purchase.replay.test.ts` | 9 / 0 / 42 | ✅ |
| GraphQL integration | `backend/graphql/test/plan-catalog.lane.test.ts` | 6 / 0 / 25 | ✅ |
| Route | `app/api/payments/webhook/test/payments-webhook-route.test.ts` | 27 / 0 / 99 | ✅ |
| UI components (scoped) | `test/ui/components/admin/PlanCatalogContainer.test.tsx` | 7 / 0 / 61 | ✅ |
| Parity | `shared/locale/errors-namespace.parity.test.ts` | 8 / 0 / 124 | ✅ |
| Parity | `shared/locale/notifications-namespace.parity.test.ts` | 102 / 0 / 589 | ✅ |
| Parity | `shared/locale/plans-namespace.parity.test.ts` | 4 / 0 / 266 | ✅ |
| SDL/surface pins | `backend/graphql/test/schema-surface.test.ts` | 42 / 0 / 264 | ✅ |
| SDL/surface pins | `backend/graphql/test/sdl-static-assertions.test.ts` | 33 / 0 / 179 | ✅ |
| SDL/surface pins | `backend/lib/gateway/route-inventory.test.ts` | 14 / 0 / 33 | ✅ |

- **Sanctioned skips (not failures):** the two `testOnRealPostgres`-gated Tier-3 true-concurrency chaos tests (`purchase.service` + `activation.service`) skip on the single-connection PGlite provider per the pre-existing sibling convention (`session-lifecycle.service.test.ts:112`, `teacher.repository.test.ts:197`); counts match the pinned 6.1/7.1 outcomes exactly (16+1 / 12+1). Serialized same-key/same-reference chaos variants DO run and carry the invariants.
- **UI scoped-run mechanism (sanctioned, per 10.2 outcome):** `bun run scripts/lib/run-locked-cmd.ts <id> TEST_SERVER_MODE=production bun --env-file=.env.test.ci test --parallel=1 <file> --preload ./test/ui/test-env.ts --preload ./test/ui/components/happydom-preload.ts --preload ./test/ui/components/translation-preload.ts --preload ./test/ui/components/next-dynamic-mock.ts`. `PlanCatalogContainer.test.tsx` is the only plans component suite (the form assertions live inside it); counts match the 10.2 pinned 7/0/61 exactly.
- Journey re-run at the gate: 10 pass / 0 fail — independently re-proven post-11.1.

## 4. `bun quality-gate` battery — ❌ FAIL (3 attributable findings + 1 environmental OOM)

tasks.md 12.1 requires "re-run `bun quality-gate` clean". The battery (tsgo → oxlint → biome:check → check:unused → lint:type-aware → check:duplicates) **failed at the oxlint stage**; the remaining stages were run standalone to give the full picture. Baseline 0.1 recorded only the trio — but every finding below is in (or triggered by) files this plan created/edited, so all are definitionally plan-attributable. **Reported for orchestrator decision; NOT fixed by this gate.**

| # | Check | Result | Finding (exact) | Attribution / suggested fix (orchestrator) |
|---|---|---|---|---|
| QG-1 | `oxlint --deny-warnings --ignore-path .gitignore` | ❌ exit 1 | **1 warning, 0 errors**: `typescript(no-unsafe-type-assertion)` — `backend/services/billing/payment-gateway/mock-payment-gateway.adapter.ts:90:23`: `const candidate = parsed as Record<string, unknown>;` (reproduced standalone on the single file; the ONLY warning on all 1428 files) | Plan file (5.1). Replace the assertion with a narrowing type-guard or a typed parse per repo idiom. |
| QG-2 | `check:unused` (knip) | ❌ exit 1 | **1 configuration hint** (`--treat-config-hints-as-errors`): `backend/enum/billing/payment-status.enum.ts — knip.config.ts — Remove from ignore` (entry at knip.config.ts:82) | Plan-triggered: enum file + ignore entry both pre-exist at ffce457 (verified via `git show`), but this plan's new imports (`subscription-purchase.service.ts:59`, `subscription-activation.service.ts:54`, `student-payment.repository.ts:29`, `student-payment.types.ts:3`, `enum.pothos.ts:36` registration) made the enum used → the pre-existing ignore became stale. Fix: delete the line-82 entry from knip.config.ts. |
| QG-3 | `check:duplicates` (jscpd -t 0) | ❌ exit 1 | **1 clone** (12 lines / 85 tokens): `backend/services/billing/subscription-purchase.service.ts:109:27–120:2` ↔ `backend/services/classes/session-lifecycle.guards.ts:173:42–184:2`; threshold 0 → any clone fails | Plan file (6.1). Extract the shared scaffold / restructure per `DUPLICATES_REMEDIATION_PROMPT` hard rules (NEVER `jscpd:ignore`; never touch `.jscpd.json`). Note: per-file sub-loop duplicates lifecycle could not see this cross-file clone — whole-repo jscpd is the check that catches it. |
| QG-4 | `lint:type-aware` (full-repo) | ❌ exit 2 — **environmental OOM** | eslint child process died of JS heap OOM (~2.8 GB, SIGABRT) — 2 attempts (default heap; retry with `NODE_OPTIONS=--max-old-space-size=6144` not inherited by the service-spawned child). NOT a code finding. | Environmental/resource. The non-type-aware full-repo lint is clean (§1) and per-file `lint:type-aware` passed in every sub-loop. Needs orchestrator decision: raise the lint-service child heap. |

`tsgo` inside the battery passed (exit 0) before the oxlint failure; `biome:check` re-ran clean (no second fix — idempotent after B-1).

## 5. tasks.md Checkbox Audit

- 40 `[x]` boxes. Sanctioned in-flight unchecked: **12.1** (+ its `12.1.IV`) and **13.1** — per dispatch.
- **Stragglers (unchecked but evidenced — flips justified, left to orchestrator):**

| tasks.md line | Box | Evidence (exists) |
|---|---|---|
| 119 | `6.1.QL / .TE / .SEC / .SR / .IV` (main 6.1 box IS `[x]`) | `outcome/6.1-outcome.md` (16 pass / 0 fail; counts re-verified green at this gate) |
| 124 | `6.2.QL / .TE / .SEC / .SR / .IV` (main 6.2 box IS `[x]`) | `outcome/6.2-outcome.md` |
| 129 | `6.5 Backend review checkpoint` | `outcome/midpoint-review-R1.md` |
| 179 + 182 | `11.1` main + `11.1.QL/.TE/.SEC/.SR/.IV` | `outcome/11.1-outcome.md` + this gate's independent journey re-run (10/0/89) |

- `12.1.IV` (instructions re-verified against every touched file) remains open — it was not part of this gate run's dispatch scope and must be executed by the 12.1 owner before 13.1's knowledge propagation.

## 6. Git Diff Scope Audit — ✅ CLEAN

- `git diff --name-only ffce457 HEAD | wc -l` → **120** (HEAD = dae7e48 on `feat/DEV1-006-subscription-purchase-payment-gateway`; worktree otherwise clean apart from gate artifacts noted in §8).
- Top-level breakdown: `backend/` 71 · `ai/plans/` 25 · `shared/` 10 · `frontend/` 8 · `test/` 3 · `app/` 2 · `docs/` 1.
- **Zero files outside the expected surface** (`backend/`, `app/api/payments/`, `frontend/`, `shared/locale/`, `docs/`, `test/`, `ai/plans/`). Spot-checks all map to plan tasks: `app/api/payments/webhook/route.ts` + test (8.1); `docs/graphql/error-handling-contract.md` (8.1 exemptions register); `shared/locale/{en,ar,types}/{errors,notifications,plans}` + notifications parity suite (6.1/7.1/10.2); `test/ui/components/admin/{BroadcastComposeContainer,PlanCatalogContainer}.test.tsx` (codegen-forced fixture / 10.2); `test/workflows/billing/subscription-purchase.journey.test.ts` (11.1).

## 7. Gate Verdict

### ❌ FAIL (conditional — single blocker: quality-gate battery not clean)

| Gate criterion | Verdict |
|---|---|
| Baseline trio — zero NEW errors vs 0/0/0 | ✅ PASS |
| Deferred-items enforcement (0 ❌/⚠️; no Blocked/Partial rows) | ✅ PASS |
| Test-Layer Coverage Gate (every mandated layer ran green) | ✅ PASS |
| tasks.md 12.1 — "re-run `bun quality-gate` clean" | ❌ **FAIL — QG-1, QG-2, QG-3 (deterministic, plan-attributable) + QG-4 (environmental OOM)** |
| Checkbox audit | ⚠️ 4 evidence-backed stragglers (report-only) |
| Scope audit | ✅ CLEAN |

**Blockers requiring orchestrator dispatch (all effectively small, all outside this gate's fix mandate):**
1. **QG-1** — oxlint unsafe-type-assertion warning in `mock-payment-gateway.adapter.ts:90` (plan's file; sub-loop's file-level oxlint evidently passed at 5.1 while the whole-repo `--deny-warnings` run now fails — re-run `sub-loop.ts` on that file to reproduce before fixing).
2. **QG-2** — stale knip ignore entry for `payment-status.enum.ts` (one-line `knip.config.ts` removal; consumed by this plan's new code).
3. **QG-3** — one 12-line jscpd clone between `subscription-purchase.service.ts` and `session-lifecycle.guards.ts` (extract shared guard scaffold; no jscpd:ignore).
4. **QG-4 (environmental)** — full-repo `lint:type-aware` OOMs; give the lint-service eslint child a larger heap so the battery can even reach a clean state after 1–3.
5. Flip the four evidence-backed checkbox groups (§5) and execute `12.1.IV`.

**Not blockers:** biome's drizzle-snapshot format fix (commit it), the 2 sanctioned `testOnRealPostgres` skips, PRE-1/PRE-2 ledger rows (post-plan maintenance by design).

## 8. Working-Tree State After This Gate (no commits made, per dispatch)

- ` M backend/drizzle/20260907182455_ancient_scarecrow/snapshot.json` — biome gate-sanctioned formatting (B-1).
- ` ?? ai/plans/.../outcome/12.1-outcome.md` — this file; `M worklog.md` — 12.1 entry appended.
- Gate artifacts in `/tmp`: `gate-121-*.{log,json,txt}` (tsgo/biome/lint/journey/gql/parity/pins/ui/knip/jscpd/quality-gate logs, diff file list, pre/post-biome git status).

---

## 9. Round 2 Fixes (Task 12.1-fixes — orchestrator dispatch executed)

**Date:** 2026-09-08 · **Agent:** Fix Subagent (12.1-fixes) · **Scope:** QG-1, QG-2, QG-3, checkbox stragglers (§5), 12.1.IV. QG-4 NOT fixable here (environmental, re-proven below). **No commits.**

### 9.1 Files changed (5 + this outcome/tasks.md/worklog)

| File | Fix | Change |
|---|---|---|
| `backend/services/billing/payment-gateway/mock-payment-gateway.adapter.ts` | QG-1 | The `parsed as Record<string, unknown>` assertion (90:23) replaced with the house unknown-JSON narrowing idiom (`parseStoredEmitReceipt` pattern): four `"x" in parsed` guards throw the same `PAYMENT_WEBHOOK_MALFORMED` `ValidationError`, after which `parsed` is narrowed to a member-carrying record and destructured with **zero type assertions**. Behavior-identical (missing member vs ill-typed member both rejected with the same code + message); route + factory pins re-run green. |
| `knip.config.ts` | QG-2 | Stale `backend/enum/billing/payment-status.enum.ts` ignore entry (was line 82) + its payment-status comment bullet deleted (the plan's imports made the enum genuinely used). |
| `backend/lib/errors.ts` | QG-3 (home) | Added exported `isPgUniqueViolation(error: unknown): boolean` — a one-line delegation to the existing private `hasPgCode(error, "23505")`, whose traversal is byte-identical to both local copies (cycle-safe `seen` set, `Error.cause` walk, PG `code` check only, message never consulted). Named `isPgUniqueViolation` (not `isUniqueViolation`) to avoid ambiguity with the wider SQLite-parity variant in `backend/services/shared/user-provisioning.helpers.ts` — the two differ semantically, so importing that one would have been a behavior change on both sides (forbidden). |
| `backend/services/billing/subscription-purchase.service.ts` | QG-3 | Local 19-line `isUniqueViolation` copy (doc + fn, was 102–120) deleted; now imports `isPgUniqueViolation` from `@/backend/lib/errors`; 2 call sites renamed, logic untouched. |
| `backend/services/classes/session-lifecycle.guards.ts` | QG-3 | Pre-existing `isClaimKeyUniqueViolation` (was 173–184) kept under its public name (consumers `session-lifecycle.booking.ts` + guards test untouched) with a minimal-diff 1-line delegation to the shared helper. No behavior change. |

### 9.2 Verification battery

| Check | Command | Result |
|---|---|---|
| tsgo | `bun run tsgo` | ✅ exit 0, 0 errors (also re-passed inside the battery) |
| QG-1 | `bun run oxlint` (deny-warnings, whole repo) | ✅ **0 warnings, 0 errors** on 1428 files (was 1 warning) |
| QG-2 | `bun run check:unused` (knip, config-hints-as-errors) | ✅ exit 0, no hints (stale-entry hint gone) |
| QG-3 | `bun run check:duplicates` (jscpd -t 0, whole repo) | ✅ **0 clones** (was 1); no new clones introduced by the refactor |
| biome:check | `bun run biome:check` | ✅ exit 0, "No fixes applied" (idempotent; edits format-clean) |
| Suites | purchase service / guards / gateway factory / webhook route / errors contract / taxonomy | ✅ 16/0/115 (+1 sanctioned skip) · 50/0/162 · 19/0/94 · 27/0/99 · 23/0/256 · 15/0/151 — every count matches the §3 pinned numbers |
| Sub-loop ×5 | `sub-loop.ts <file> --lifecycle duplicates` on all 5 changed files | ✅ exit 0 on every file (tsgo → oxlint → biome → per-file lint:type-aware → intra-file jscpd) |
| QG-4 | `bun run quality-gate` full battery + tuned retry (`LINT_MAX_OLD_SPACE_MB=2048 LINT_QUEUE_CONCURRENCY=1`) | ❌ still environmental: battery passes tsgo → oxlint → biome:check → check:unused, then `lint:type-aware` child dies — first SIGKILL (kernel OOM-killer), on retry SIGABRT even with the lowered heap; box has ~4 GB RAM total. Code is NOT implicated: per-file type-aware lint passed on all 5 files in the sub-loops, and the non-type-aware full-repo lint is clean (§1). Unchanged orchestrator ask: raise the lint-service child heap / run on a larger box. |

### 9.3 Checkboxes (dispatch-authorized flips)

Flipped to `[x]` per the orchestrator dispatch (evidence: §5 table — outcome/6.1, 6.2, midpoint-review-R1, 11.1, plus §3's green re-runs and §6's touched-file audit for 12.1.IV):

- Line 119 — `6.1.QL / 6.1.TE / 6.1.SEC / 6.1.SR / 6.1.IV`
- Line 124 — `6.2.QL / 6.2.TE / 6.2.SEC / 6.2.SR / 6.2.IV`
- Line 129 — `6.5` main
- Line 179 + 182 — `11.1` main + `11.1.QL / .TE / .SEC / .SR / .IV`
- Line 189 — `12.1.IV`

Remaining unchecked: **12.1 main** (flips when the orchestrator closes this fix round) and **13.1** (knowledge propagation, next task) — both by design. tasks.md now 47 `[x]` / 2 `[ ]`.

### 9.4 Round 2 Verdict

| Gate criterion | Round 1 | Round 2 |
|---|---|---|
| Baseline trio (tsgo/biome/lint vs 0/0/0) | ✅ PASS | ✅ PASS (re-proven) |
| Deferred-items enforcement | ✅ PASS | ✅ PASS (no re-check needed — untouched) |
| Test-Layer Coverage Gate | ✅ PASS | ✅ PASS (affected suites re-run green, counts pinned) |
| tasks.md 12.1 — quality-gate stages | ❌ FAIL (QG-1/2/3 + QG-4) | ✅ **PASS on every plan-attributable stage** — QG-1/2/3 fixed and verified green standalone AND inside the battery up to the lint stage; **QG-4 remains environmental** (re-proven with a tuned retry; not a code finding) |
| Checkbox audit | ⚠️ 4 stragglers + 12.1.IV open | ✅ CLEAN (all flips applied per dispatch) |
| Scope audit | ✅ CLEAN | ✅ CLEAN (git status = 5 code/config files + tasks.md + this outcome + worklog; the pre-existing B-1 drizzle snapshot formatting from §8 is untouched and still awaits the orchestrator's gate-sanctioned commit) |

**Round 2 verdict: ✅ PASS** for 12.1's plan-attributable criteria. The only residual is QG-4 (full-repo `lint:type-aware` memory ceiling in this sandbox) — environmental, documented above and in §4, requiring an orchestrator infrastructure decision (lint-service child heap), not a code fix. `12.1` main may be closed by the orchestrator once this section is acknowledged; `13.1` proceeds.
