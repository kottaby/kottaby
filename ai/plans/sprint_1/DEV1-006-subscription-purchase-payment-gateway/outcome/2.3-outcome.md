# Task 2.3 Outcome — Seed lane backfill

**Date:** 2026-09-07
**Agent:** Seed Backfill Subagent
**Requirements:** REQ-034, REQ-062

## Summary

Every entry in the plan-catalog seeder now declares its balance lane, and the seed's create-or-update logic flows the lane through BOTH paths: new rows receive the lane at creation, and existing rows are reconciled on every seed pass (`updatePlan` lane-only patch when the stored lane differs from the spec), so rows seeded before lanes were introduced converge without duplicate inserts. Verified end-to-end on the live PGlite DB: seeds ran twice cleanly, the backfill UPDATE path was exercised against a deliberately lane-cleared row and restored the lane, and a read-only probe confirmed all four plans carry the correct lane values.

**Provenance note (honest attribution):** when this task started, the seed-plans.ts lane edits were already present in the shared uncommitted working tree — evidently from a prior interrupted incarnation of this task (no 2.3 outcome file, worklog entry, or checkbox state existed). This incarnation verified the file line-by-line against the task spec (found fully conformant — nothing to change), then executed the complete verification pipeline fresh: quality gate, two seed runs, live backfill-path probe, DB probe, and the paired seed test.

## Lane Mapping (the task's core contract — carry-forward verbatim)

| Plan title (seeded) | `balanceLane` | Rationale |
|---|---|---|
| `Hifz Jadid (Memorization)` | `SubscriptionCreditLane.Hifz` (`"hifz"`) | Memorization-family product |
| `Tajweed & Tilawa` | `SubscriptionCreditLane.Tajweed` (`"tajweed"`) | Recitation-rules product |
| `New Teacher Verification & Evaluation Plan` | `SubscriptionCreditLane.Reviews` (`"reviews"`) | Assessment of existing recitation (Muraja'ah-style review), not new memorization or rules teaching |
| `Legacy Trial Plan` | `SubscriptionCreditLane.Reviews` (`"reviews"`) | Trial samples/evaluates current recitation; no dedicated trial lane exists (`shouldBeActive: false` preserved) |

Mechanics in `backend/db/seeds/billing/seed-plans.ts`:
- `DemoPlanSpec` gained `readonly balanceLane: SubscriptionCreditLane` (required on every spec — no laneless demo plans).
- CREATE path: `PlanCatalogService.createPlan({ ..., balanceLane: planSpec.balanceLane }, ...)`.
- UPDATE path (the backfill): after the look-before-create, `if (plan.balanceLane !== planSpec.balanceLane) plan = await PlanCatalogService.updatePlan(plan.id, { balanceLane: planSpec.balanceLane }, locale, tx)` — re-running seeds sets lanes on existing rows (incl. `null → value` convergence).
- Enum imported as a VALUE import (`import { SubscriptionCreditLane } from "@/backend/enum/billing/subscription-credit-lane.enum"` — runtime use in object literals; no string literals anywhere a lane is expected).

## Files Modified (task scope)

| File | Change |
|---|---|
| `backend/db/seeds/billing/seed-plans.ts` | EXTEND — lane on all four plan specs, create-path flow, update-path reconciliation, value-import of `SubscriptionCreditLane`; header JSDoc updated (also removed the stale plan-artifact references from the old header) |

## Files NOT Modified (and why)

- `backend/types/billing/plan.types.ts` (`PlanSubmitInput.balanceLane?`), `backend/services/billing/plan-catalog.service.ts` (create/update lane support + patch passthrough), `backend/db/repo/billing/plan.repository.ts` (`PLAN_READ_COLUMNS` + `balance_lane AS "balanceLane"`), `backend/db/test/logic/billing/plan-seed.test.ts` (lane assertions) — **task 10.1 scope**, already present in the shared tree; this task consumes them and could not compile the seed file without them. Not touched by me.
- `backend/db/seeds/index.ts`, `backend/db/seeds/billing/index.ts` — no wiring change needed; the plans step already calls `seedOrGetPlans("en")`.
- Schema/migration files — task 2.1's output, consumed as-is (`plans.balance_lane` exists on the DB, nullable, no default).

## Verification Results

### Per-file quality loop — `sub-loop.ts backend/db/seeds/billing/seed-plans.ts --lifecycle duplicates` → **EXIT 0**

| tsgo (project-wide) | oxlint | biome:check | lint:type-aware | check:duplicates |
|---|---|---|---|---|
| ✅ | ✅ | ✅ | ✅ | ✅ |

Instruction files printed & read: root `AGENTS.md`, `backend/AGENTS.md`, `backend/db/seeds/AGENTS.md` (seeder rules: seed-or-get pattern, idempotency, service-only data access, enums from `@/backend/enum/**` allowed for seed constants, types from `@/backend/types`). `.github/instructions/backend.instructions.md` printed with ⚠ but absent from the tree (empty instructions dir) — environmental, consistent with prior tasks' notes.

Whole-repo `bun run tsgo` → **EXIT 0** (silent pass; baseline preserved).

### Idempotency — seed CLI run TWICE

- Run 1: `bun --no-env-file run scripts/dbActions/cli-entry.ts --env-file=.env seed` → **EXIT 0** ("All seed steps executed successfully", no duplicate inserts, no reconcile lines — lanes already converged).
- Run 2 (identical command) → **EXIT 0** (same clean output — idempotent).

### Backfill UPDATE path — exercised live, not assumed

1. Throwaway write probe set `balance_lane = NULL` on `"Tajweed & Tilawa"` (raw SQL, row confirmed NULL).
2. Seed re-run → **EXIT 0** with the reconcile path firing: `[INFO] Plan updated successfully [{"planId":2}]` + `[INFO] Reconciled balance lane of plan "Tajweed & Tilawa" (ID: 2) to "tajweed"`.
3. Read-only probe confirmed the lane restored to `tajweed`.

### DB probe (read-only, throwaway script deleted after each run)

```
plans row count: 4
  [1] "Hifz Jadid (Memorization)" -> balanceLane=hifz (isActive=true)
  [2] "Tajweed & Tilawa" -> balanceLane=tajweed (isActive=true)
  [3] "New Teacher Verification & Evaluation Plan" -> balanceLane=reviews (isActive=true)
  [4] "Legacy Trial Plan" -> balanceLane=reviews (isActive=false)
```

### Paired test (2.3.TE evidence) — `bun run test/scripts/run-test.ts backend/db/test/logic/billing/plan-seed.test.ts`

**1 pass / 0 fail / 17 expect() calls** — covers create of all demo plans, second-pass idempotency (identical IDs), lane assertions `found.balanceLane === demoPlan.balanceLane` for every spec, sessionCount/active/deactivated expectations. (Test file itself is 10.1's; run, not modified.)

## Semantic Review (SKILL checklist)

- ✅ Clean comments — ZERO plan-artifact references (grep `REQ-|Task [0-9]|tasks\.md|plan\.md|specs\.md|DEV1-006|Phase [0-9]|sprint` over the seed file: clean). In-file comments explain domain rationale only (why assessment/trial products draw the review lane).
- ✅ `SubscriptionCreditLane` imported as a **value import** (runtime object-literal use).
- ✅ No string literals where the enum is expected — all four specs use enum members; the reconcile comparison is value-based.
- ✅ Seeder rules respected (seeds/AGENTS.md): `seedOrGet` exported, look-before-create + ConflictError race recovery preserved, service-only data access (no `@/backend/db/**` imports in the seeder), types via `@/backend/types`, enum via `@/backend/enum/**`.
- ✅ No dead branches — both UPDATE branches reachable (deactivate path verified inactive; reconcile path proven live above).
- ✅ No cross-layer imports; no module-level mutable state; no security-relevant surface (2.3.SEC: demo seed data only, writes mediated by `PlanCatalogService`, no PII/authz/logic changes).
- ✅ Scope: my only repo-source file is `seed-plans.ts`; `git status` confirms all other working-tree diffs belong to tasks 2.1/2.2/3.1/3.2/5.1/5.2 (documented in their outcomes) and 10.1's in-flight shared-tree work.
- ✅ No deferred work introduced → no `deferred-items.md` entry needed.
- ✅ No commits (per environment policy); PGlite used only by me during this task's verification window.

## Carry-Forward Knowledge (for 10.1/10.2, 6.1/7.1, tests)

1. **Lane mapping is the fixed demo-catalog contract** (table above). 10.2's admin lane select and any catalog/journey-test expectations (11.1) should use these exact title→lane pairs.
2. **The seed reconcile depends on 10.1's read projection**: `PlanRepository.PLAN_READ_COLUMNS` must keep `balance_lane AS "balanceLane"` — the raw `queryDb` path is how the seeder sees stored lanes; removing it would make every pass re-update (still convergent, but noisy).
3. **`PlanUpdateInput = Partial<PlanSubmitInput>`** accepts a lane-only patch (`{ balanceLane }`), which the seed's reconcile uses; `validateAndExtractPlanPatch` passes `balanceLane` through (10.1's line) — keep that passthrough if refactoring.
4. **All four demo plans are lane-configured**, so 6.1's fail-closed `PLAN_LANE_UNCONFIGURED` guard (NULL lane) is never hit by seeded rows — activation crediting (7.1) can rely on `plan.balanceLane` being non-null for catalog plans.
5. **Probe methodology for this sandbox**: the Write tool cannot create files under `/tmp` (restricted to `/home/z`), and entry scripts outside the project cannot resolve `drizzle-orm` (package resolution walks up from the script location). Working pattern: throwaway script at `/home/z/*.ts` importing project modules via project-relative paths (`./my-project/backend/db/client`), run with cwd = project root (PGlite path `./db/pglite` resolves), delete immediately after. Raw `queryDb` probes avoid importing drizzle-orm in the entry file entirely.

## Cross-File Dependencies Discovered

- `seed-plans.ts` ↔ 10.1's service/type/repo changes are a compile-time pair in the shared tree: the seed file's `balanceLane` on `DemoPlanSpec` requires `PlanSubmitInput.balanceLane?` (10.1) and the reconcile requires `updatePlan` passthrough + read projection. Both sides verified green together (`bun run tsgo` EXIT 0).
- `backend/db/test/logic/billing/plan-seed.test.ts` (10.1's TE) is the regression net for this seeder — its lane assertion would catch any lane mapping drift.
- The seeder intentionally stays service-mediated (seeds/AGENTS.md CRITICAL rule): any future lane logic changes must land in `PlanCatalogService`, never as raw DB writes in the seeder.
