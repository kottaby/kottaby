# Task 2.1 Outcome — Schema deltas + regeneration

**Date:** 2026-09-07
**Agent:** Schema Deltas Subagent
**Requirements:** REQ-003, REQ-005, REQ-030, REQ-033, REQ-034

## Summary

All Phase-2 schema deltas implemented in the Drizzle schema layer and applied to the local PGlite database through the generate → migrate pipeline (repo-policy compliant; `db reset` / `db cleanGenerate` / manual SQL edits NOT used):

1. `enums.ts` — new pgEnum `subscriptionCreditLane` (`hifz` | `tajweed` | `reviews`) + `mock` member appended to the `paymentGateway` pgEnum (now 9 members; registry header count 16 → 17).
2. `plans.ts` — nullable column `balanceLane` (`balance_lane`, type `subscriptionCreditLane`); no default, backward-compatible with existing rows.
3. NEW `subscription-purchase-idempotency.ts` — claim table mirroring `backend/db/schema/classes/session-request-idempotency.ts` exactly (the plan cited a `billing/` path for the mirror source; the actual precedent file lives in `classes/` — conventions followed 1:1: identity PK, varchar(128) unique key, cascade/set-null FKs, `defaultNow` timestamp, `unique()` + `index()` extras-array style, alias imports for siblings, full JSDoc claim-semantics block).
4. `billing/index.ts` — `export * from "./subscription-purchase-idempotency";` (ASCII order: before `./subscriptions`, hyphen < `s` at divergence point).
5. `subscriptions.ts` — partial unique index `subscriptions_payment_reference_unique` on `payment_reference` `WHERE payment_reference IS NOT NULL`, declared via `uniqueIndex(...).on(...).where(sql\`...\`)` following the repo precedent in `parents/parent-link-requests.ts` (UNIQUE INDEX, not a table constraint — PG constraints can't carry WHERE).

## Files Created / Modified (task scope)

| File | Change |
|---|---|
| `backend/db/schema/enums.ts` | EXTEND — `subscriptionCreditLane` pgEnum + `paymentGateway` gains `"mock"` |
| `backend/db/schema/billing/plans.ts` | EXTEND — nullable `balanceLane` column + enum import + JSDoc lane paragraph |
| `backend/db/schema/billing/subscription-purchase-idempotency.ts` | CREATE — claim table `subscriptionPurchaseIdempotency` |
| `backend/db/schema/billing/index.ts` | EXTEND — barrel re-export |
| `backend/db/schema/billing/subscriptions.ts` | EXTEND — partial unique index + `sql`/`uniqueIndex` imports + JSDoc paragraph |
| `backend/drizzle/20260907182455_ancient_scarecrow/` | GENERATED — schema migration (migration.sql + snapshot.json; idempotency pass rewrote migration.sql in place) |

## Files NOT Modified (and why)

- `backend/types/billing/*`, `backend/enum/billing/*`, repos, services, seeds — out of task 2.1 scope (tasks 3.1/3.2/4.x/2.3).
- `backend/db/migration/4-student-payments-status-transition*.sql`, `backend/db/scripts/applyCustomMigrations.ts`, `backend/drizzle/20260907182426_custom_4-student-payments-status-transition/`, `backend/drizzle/.custom-migrations.json`, `backend/drizzle/20260907182426_…` — **owned by the parallel task 2.2 agent**, present in the shared working tree (verified: `applyCustomMigrations.ts` diff is only the sqlite-exclusion entry for 2.2's trigger file; the custom_4 folder was bundled by 2.2's own migrate run at 18:24:26, before my first generate at 18:24:55, which correctly reported "no new or changed custom SQL files detected").
- `ai/plans/.../tasks.md` pre-existing 0.1 checkbox diff — orchestrator/0.1 agent's; I only flipped the 2.1 checkbox lines (protocol step 3).
- No `drizzle meta/_journal` files exist in this repo's pipeline (folder-scan migrator; journal lives inside the PGlite DB) — nothing to update there.

## Verification Results

### Per-file quality loop — `sub-loop.ts <file> --lifecycle duplicates` (all EXIT 0, run after each edit, in order)

| File | tsgo | oxlint | biome:check | lint:type-aware | check:duplicates |
|---|---|---|---|---|---|
| `enums.ts` | ✅ | ✅ | ✅ | ✅ | ✅ |
| `billing/plans.ts` | ✅ | ✅ | ✅ | ✅ | ✅ |
| `billing/subscription-purchase-idempotency.ts` | ✅ | ✅ | ✅ | ✅ | ✅ |
| `billing/index.ts` | ✅ | ✅ | ✅ | ✅ | ✅ |
| `billing/subscriptions.ts` | ✅ | ✅ | ✅ | ✅ | ✅ |

Instruction files printed/read: root `AGENTS.md`, `backend/AGENTS.md` (both printed by sub-loop) + `.agents/instructions/backend.instructions.md` + `backend/db/schema/AGENTS.md` read manually (schema-layer rules: enums registry top-level, barrel conventions, sync policy).

### Generate (migration set)

- `bun --no-env-file run scripts/dbActions/cli-entry.ts --env-file=.env generate` → **EXIT 0**, created `backend/drizzle/20260907182455_ancient_scarecrow/migration.sql` containing exactly the five expected deltas:
  1. `CREATE TYPE "subscription_credit_lane" AS ENUM('hifz','tajweed','reviews')` (DO-block wrapped)
  2. `ALTER TYPE "payment_gateway" ADD VALUE 'mock'` (DO-block wrapped)
  3. `CREATE TABLE IF NOT EXISTS "subscription_purchase_idempotency"` (key UNIQUE inline constraint) + both FKs (cascade / set-null) + `user_id` index
  4. `ALTER TABLE "plans" ADD COLUMN IF NOT EXISTS "balance_lane" "subscription_credit_lane"`
  5. `CREATE UNIQUE INDEX IF NOT EXISTS "subscriptions_payment_reference_unique" ON "subscriptions" ("payment_reference") WHERE "payment_reference" IS NOT NULL`
- Drift check: re-run generate → **"No schema changes, nothing to migrate 😴"**, EXIT 0, plus "All migration files already idempotent — no changes needed".

### Migrate (apply to PGlite)

- `bun --no-env-file run scripts/dbActions/cli-entry.ts --env-file=.env migrate` → **EXIT 0**: "Applying 1 pending Drizzle migration folder(s)... Applied migration: 20260907182455_ancient_scarecrow ... Migrations completed successfully!" Enum pre-commit fired for the `mock` ADD VALUE.
- Post-apply DB verification (direct PGlite catalog queries): both enums present with correct member sets/order; `plans.balance_lane` = `subscription_credit_lane`, `is_nullable = YES`; claim table columns/FK deletion actions (`c` cascade, `n` set null)/unique constraint/indexes all present; `subscriptions_payment_reference_unique` confirmed as `CREATE UNIQUE INDEX … WHERE (payment_reference IS NOT NULL)`.

### Semantic review (SKILL checklist)

- ✅ Schema columns ↔ migration output ↔ live DB all match (queried, not assumed)
- ✅ `subscriptionCreditLane` imported as **value import** in `plans.ts` (used as column builder at runtime)
- ✅ No plan-meta in comments (grep for `REQ-|Task 2|tasks.md|plan.md|specs.md|DEV1-006|Phase 2` over all 5 files: clean); comments describe domain behavior only
- ✅ No cross-layer imports (backend-internal only); no dead branches; no ReturnType construction
- ✅ Scope: `git diff --name-only` shows only my 5 schema files + the shared-tree items listed above as owned by task 2.2 / orchestrator
- ✅ No deferred work introduced → no `deferred-items.md` entry needed
- ⚠ TE note: tasks.md said "TE = schema-shape compile + push verification"; per orchestrator dispatch the apply path is generate+migrate (repo policy prefers migrate; push skips the journal). Schema-shape verification done via tsgo + live catalog queries after migrate.

## Carry-Forward Knowledge (for later tasks in this plan)

- **Exported symbols**: table object `subscriptionPurchaseIdempotency` (file `backend/db/schema/billing/subscription-purchase-idempotency.ts`, also re-exported from `@/backend/db/schema/billing` and the top-level `@/backend/db/schema` barrels); pgEnums `subscriptionCreditLane` ("hifz" | "tajweed" | "reviews") and `paymentGateway` (now includes "mock") from `@/backend/db/schema/enums`.
- **Index/constraint names** (referenced by tests/repos later): `subscriptions_payment_reference_unique` (partial unique index), `subscription_purchase_idempotency_key_unique` (unique constraint + backing index), `subscription_purchase_idempotency_user_id_idx`, FK `subscription_purchase_idempotency_user_id_users_id_fkey` (cascade), FK `subscription_purchase_idempotency_etBNqRc9DVFK_fkey` (set-null — drizzle-kit auto-generated random suffix; do NOT hand-write this name in code, reference columns not constraint names).
- TS enum mirrors (`backend/enum/billing/…`) are task 3.2 — `enums.ts` values are the DB-side source of truth until then.
- Migration folder for this task: `20260907182455_ancient_scarecrow` (schema) — custom trigger SQL (2.2) ships as its own `custom_*` folder per the incremental bundler.
- `plans.balanceLane` `$inferSelect` field name is `balanceLane`; column is `balance_lane`; nullable, no default.

## Cross-File Dependencies Discovered

- **Parallel-agent DB contention**: task 2.2 ran migrate against the same PGlite data dir concurrently; my drift re-run of generate hit PGlite's exclusive data-dir lock and hung until the 10-min tool timeout (no output lost — folder state unaffected). Resolved by re-running once 2.2's process exited. **Future tasks: DB commands (generate/migrate/push/seed) must be serialized across concurrently-dispatched subagents — one `cli-entry.ts` invocation at a time.**
- `applyCustomMigrations.ts` bundler + `.custom-migrations.json` sidecar are shared mutable state across tasks adding custom SQL (2.2 modified both while I ran) — my task touched neither (no custom SQL in scope).
- Drizzle-kit auto-names FKs without explicit `references` config names; the snapshot freezes the random name, so schema edits that regenerate the claim table's FK diff would rename it — avoid dropping/re-adding the claim table FKs.
