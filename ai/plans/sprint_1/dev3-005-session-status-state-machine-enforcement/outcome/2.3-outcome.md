# Task 2.3 — Release Composed into `completeSession` / `cancelSession` / `resolveSessionDispute` Outcome

**Date:** 2026-09-08 · **Branch:** `feat/dev3-005-session-state-machine`

## Shipped

1. **`session-lifecycle.transitions.ts`** — NEW shared release primitive `releaseTeacherInSessionLock(teacherId, tx)`: the release twin of `refundHeldLaneToProvenance` — a post-transition, same-transaction follow-up keyed on the transitioned row's teacher id; a zero-row match is the fail-closed hard stop (throws + rolls the flow back, one `logger.error`, never a silent skip).
2. **`completeSession`** — now composes through `withTransaction(outerTx, …)`; the guarded predicate guarantees the `started` pre-state, so the release fires unconditionally on success (same tx).
3. **`cancelSession`** — the release fires ONLY when `cancelled.startedAt !== null` (the start stamp is written by `startSessionOnce` and never cleared by any writer — verified in 0.2 — so it classifies the pre-state): a pre-start cancel releases nothing because no lock was ever applied.
4. **`resolveSessionDispute`** — same rule for BOTH outcomes: CANCEL from a never-started dispute releases nothing; COMPLETE's guarded predicate already required `startedAt IS NOT NULL`, so its release is unconditional. Refund ordering unchanged (refund → release; one transaction, atomic either way).

Sweeper/confirmation flows untouched by design: `sweepExpiredScheduledOnce` only ever cancels `scheduled` rows (no lock exists), and the dual-confirmation touches `completed` rows only.

## Tests (2.3.TE) — the `INV-S6 in-session lock` describe (all green, runInRollback)

- complete-from-started ⇒ unlock (teacher restored in the same tx);
- cancel-from-started ⇒ unlock; **cancel-from-scheduled ⇒ no lock touch** (teacher fixture forced offline stays offline — asserting the scheduled/started distinction the plan mandates);
- resolve-from-started-dispute (Cancel) ⇒ unlock + lane-intact refund (`feeHeld=false`, hifz re-credited); **resolve-from-scheduled-dispute ⇒ none** (teacher flag unchanged);
- resolve COMPLETE from a started dispute ⇒ unlock (start stamp preserved);
- (2.2 block) forced mid-tx fault ⇒ full rollback incl. the teacher row (the lock write raises; session stays `scheduled`, teacher stays online).

Suite totals after this task: **55 pass / 6 skip / 0 fail** (the 6 skips are the pre-existing + newly gated real-PG chaos tiers).

## QL / SEC / SR / IV

- **QL** exit 0 on `transitions.ts` + `service.ts` + test file; `bun tsgo` clean; `bun oxlint` 0 warnings/0 errors; biome-formatted.
- **SEC**: release keyed off the transitioned row's teacher id (server-derived); no oracle change; denial vocabulary untouched.
- **SR**: the pinned service-unit specifier allowlist and barrel-member union stay green with ZERO pin edits (the release primitive imports nothing new — `TeacherRepository` already rode the barrel import in `transitions.ts`); no inline matrix/lock duplicates; `tx` threaded everywhere.
- **IV**: repo write conventions (required tx, guarded UPDATE, `.returning()`) and services AGENTS (localized denials only on pre-existing paths; the new throw is a fail-closed internal error, not a user-facing denial).
