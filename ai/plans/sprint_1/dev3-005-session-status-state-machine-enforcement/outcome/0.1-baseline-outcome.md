# Task 0.1 — Baseline Outcome

**Date:** 2026-09-08
**Branch:** `feat/dev3-005-session-state-machine` (from `main` @ `ffce457`)
**Environment:** Bun 1.3.14 · Debian 13 · `DB_PROVIDER=pglite` (sandbox path per `backend/db/client.ts`)

## Commands & Exit Codes

| Command | Result | Notes |
|---|---|---|
| `bun tsgo` | exit 0 | zero type errors |
| `bun oxlint` | exit 0 | "Found 0 warnings and 0 errors." — 1393 files, 303 rules |
| `bun biome:check` | exit 0 | "Checked 1419 files in 10s. No fixes applied." |
| `bun run scripts/lint-service.ts --json --id baseline` | exit 0 | `success: true`, `exitCode: 0` |

## Baseline Counts

All four gates are **fully green at baseline**: tsgo = 0 errors, oxlint = 0 warnings/0 errors, biome = clean, ESLint (lint-service) = 0 problems. Any new error introduced by DEV3-005 work must be attributed to this plan and fixed before close (ratchet discipline, REQ-0).

## Ledger Init

`deferred-items.md` already exists in the plan directory (authored at plan time, populated with D1–D4 rows from `.agents/spec-process-guide/templates/deferred-items-template.md` lineage). No re-initialization needed — verified identical between `plans` branch and this feature branch.

## Environment Notes (sandbox)

- No system PostgreSQL available; sandbox uses the repo's supported `DB_PROVIDER=pglite` path (`backend/db/pglite-pool.ts`, data dir `./db/pglite`, gitignored).
- `.env` created locally (gitignored) with fixture-only values mirroring `.env.test.ci` posture (`TEST_SERVER=1`, CI fixture encryption key shape, dev JWT secrets). Never committed.
- `bun run backend/db/scripts/migrate.ts` → all migrations applied successfully on PGlite (4 pending folders, incl. custom `2-functions` + `3-immutability-triggers`); exit 0.
