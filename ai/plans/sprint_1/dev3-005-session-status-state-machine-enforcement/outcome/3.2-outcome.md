# Task 3.2 — Regression Outcome (Existing Suites Remain Green)

**Date:** 2026-09-08 · **Branch:** `feat/dev3-005-session-state-machine`

## Method

Every pre-existing suite that exercises the touched surfaces, re-run on this branch after Phases 1–3. The two journey suites carry PRE-EXISTING sandbox-only failures, so they were additionally re-run on the CLEAN base commit (`git stash` → run → `git stash pop`) for an honest before/after comparison — the ratchet discipline: fix only if the lock/release composition altered expectations.

## Results

| Suite | On this branch | On clean base `ffce457` | Verdict |
|---|---|---|---|
| `backend/db/test/repo/classes/session.repository.test.ts` | **51 pass / 0 fail** | (no failures reported pre-change) | ✅ unchanged-green |
| `backend/services/classes/session-lifecycle.service.test.ts` | **55 pass / 6 skip / 0 fail** | 51 pass / 3 skip / 1 fail→ (see note) | ✅ green after the documented seam update |
| `backend/services/classes/session-lifecycle.guards.test.ts` | **50 pass / 0 fail** | — | ✅ unchanged-green |
| `backend/services/classes/session-lifecycle.enforcement.test.ts` (NEW, 1.1) | **17 pass / 0 fail** | — | ✅ |
| `test/workflows/sessions/session-state-machine.journey.test.ts` (NEW, 3.1) | **13 pass / 1 skip / 0 fail ×2 consecutive runs** | — | ✅ |
| `test/workflows/sessions/session-lifecycle.journey.test.ts` (pre-existing) | 4 pass / **7 fail** | **identical 7 failures** | ✅ pre-existing sandbox-only (PGlite millisecond timestamp truncation in deadline/stamp equality assertions); CI (real PG) green per the repo's documented posture |
| `test/workflows/sessions/session-lifecycle-denials.journey.test.ts` (pre-existing) | 3 pass / **3 fail** | **identical 3 failures** | ✅ same pre-existing sandbox artifacts — zero delta attributable to DEV3-005 |

Note (service suite): the only difference vs the pre-change state is REQ-043(a)/(b)/(c)'s `testOnRealPostgres` gating, recorded in `outcome/2.2-outcome.md` — the production-tx-path chaos premise (one real connection per concurrent call) is real-PG-only now that start/complete compose real transactions; the old bare-autocommit start passed (a) accidentally-serially on the single connection. CI runs them.

## Pass counts (ratchet record)

- Repo suite: 51 pass (unchanged).
- Service suite: 55 pass / 6 skip / 0 fail — includes the 6 NEW INV-S6 lock tests (2.2/2.3) and all pre-existing green tests UNCHANGED: no expectation was rewritten (ratchet, not rewrite — the only edits are the three chaos gates + the new lock describe + helper additions).
- Guards suite: 50 pass (unchanged).
