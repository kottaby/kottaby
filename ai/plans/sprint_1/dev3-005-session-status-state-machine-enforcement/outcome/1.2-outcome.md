# Task 1.2 — i18n Keys Outcome (en/ar parity)

**Date:** 2026-09-08 · **Branch:** `feat/dev3-005-session-state-machine`

## Shipped

Two new `errors`-namespace keys, added to all three layers:

| Key | en | ar |
|---|---|---|
| `homeworkRequiresReport` | "A session report must be submitted before homework can be created." | "يجب إرسال تقرير الجلسة قبل إنشاء الواجب." |
| `teacherInActiveSession` | "This teacher is currently in an active session and is unavailable." | "هذا المعلم في جلسة نشطة حالياً وهو غير متاح." |

1. `shared/locale/types/errors/labels.ts` — typed members (compile-time parity belt: both locale leaves are typed `ErrorsLabels`, so any missing key fails `bun tsgo`).
2. `shared/locale/en/errors/index.ts` + `shared/locale/ar/errors/index.ts` — non-empty bodies, inserted adjacent to the session-lifecycle key block.

## Parity Inventory Verification (verify-first mandate)

- `shared/locale/sessions-namespace.parity.test.ts` pins session-lifecycle keys by REQUIRED-PRESENCE (not exhaustive key-set), so no inventory edit was required — new keys ride the dynamic gates.
- `shared/locale/errors-namespace.parity.test.ts` compares `Object.keys(errorsAr)` vs `Object.keys(errorsEn)` dynamically — new keys auto-participate on both sides.

## Quality Loop / Tests / Security / SR / IV

- **QL** exit 0: sub-loop `--lifecycle duplicates` on `labels.ts`, `en/errors/index.ts`, `ar/errors/index.ts`.
- **TE**: `bun run test/scripts/run-test.ts shared/locale/errors-namespace.parity.test.ts` → **8 pass / 0 fail**; `shared/locale/sessions-namespace.parity.test.ts` → **20 pass / 0 fail**. The enforcement suite (1.1) asserts both keys resolve to the exact non-empty translated bodies on the denial path (en + ar fallback).
- **SEC**: messages carry no identifiers (no session id, no teacher id) — pure action-guidance copy.
- **SR**: keys live only in the three canonical layers; no duplicate literals anywhere (duplicates lifecycle green).
- **IV**: compile-time system only (`ErrorsLabels` typed leaves; no `next-intl`, no two-arg `getTranslations`).
