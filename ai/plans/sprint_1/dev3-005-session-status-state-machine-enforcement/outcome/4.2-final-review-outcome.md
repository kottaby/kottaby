# Task 4.2 — Final Gate & Ledger Close Outcome

**Date:** 2026-09-08 · **Branch:** `feat/dev3-005-session-status-state-machine` (from `main` @ `ffce457`)

## Quality Gate at Close (vs 0.1 baseline — ratchet held at zero)

| Gate | Baseline (0.1) | Final | Verdict |
|---|---|---|---|
| `bun tsgo` | 0 errors | **0 errors** | ✅ unchanged |
| `bun oxlint` | 0 warnings / 0 errors | **0 warnings / 0 errors** | ✅ unchanged |
| `bun biome:check` | clean (1419 files) | **clean (1423 files)** | ✅ unchanged (4 new files folded in) |
| `bun run scripts/lint-service.ts --json --id final` | success/0 | **success / exit 0** | ✅ unchanged |

Ledger audit: unresolved-row scan over `deferred-items.md` → **0** (all four rows are `🔄 Reserved` with owning tickets: D1→DEV3-006, D2→DEV2-011/012, D3→DEV3-013/admin-audit, D4→DEV2-013/DEV3-008). The ledger's own explanatory text was reworded so the glyph scan measures ROWS, not the template's legend (the template quoted its own grep pattern).

## Shipped This Plan (final tally)

**Production code (5 files):**
1. `backend/services/classes/session-lifecycle.enforcement.ts` (NEW) — `SESSION_TRANSITION_MATRIX` (closed 5×5 map: 8 legal edges), `isSessionTransitionAllowed`, INV-S7 gate `assertSessionCompletedForReport`, INV-S8 gate `assertReportSubmittedForHomework`, DEV2-011 seam gate `assertTeacherNotInActiveSession`; localized `ConflictError` denials, tx-propagated, repo-surface reads only.
2. `backend/db/repo/classes/report.repository.ts` (NEW) — minimal `existsReportForSession` (the plan's 0.2-gated carve-out; DEV3-006 extension contract in ledger D1).
3. `backend/db/repo/teachers/teacher.repository.ts` — `setOnline(id, online, tx)` guarded write (INV-S6 primitive).
4. `backend/services/classes/session-lifecycle.service.ts` — INV-S6 lock composed into `startSession` (+ `withTransaction` atomicity for the production path), release composed into `completeSession` / `cancelSession`-from-started / `resolveSessionDispute`-both-outcomes via `startedAt` pre-state classification.
5. `backend/services/classes/session-lifecycle.transitions.ts` — `releaseTeacherInSessionLock` (fail-closed release twin of the refund primitive).

**Barrels + i18n (5 files):** classes repo barrel + services classes barrel (`export *`, `./` paths); `homeworkRequiresReport` + `teacherInActiveSession` in labels/en/ar (compile-time typed parity).

**Tests (3 files):**
- `session-lifecycle.enforcement.test.ts` (NEW): 17 pass — full 5×5 matrix truth table, gate pass/deny tiers incl. unknown-id uniform denials + ar-fallback + actor isolation, concurrent in-tx reads, rollback phantom-pass proof.
- `teacher.repository.test.ts` (EXTENDED): 20 pass / 2 pre-existing real-PG skips — lock/release directions, unknown id, idempotent same-value write, uncertified-row parity, rollback un-applies, deadlock-free same-tx composition, static pins.
- `session-lifecycle.service.test.ts` (EXTENDED): 55 pass / 6 real-PG skips — 6 NEW INV-S6 tests (start⇒locked, fault-injection⇒zero partial persistence, complete/cancel/resolve release matrix incl. scheduled-leg no-op) + REQ-043(a)/(b)/(c) gated to the real-PG posture they premise (documented in 2.2).

**Journey (1 file):** `test/workflows/sessions/session-state-machine.journey.test.ts` (NEW): 13 pass / 1 real-PG race skip — J1 dispute→queue→duplicate-deny→cross-actor-deny→admin-Cancel(refund+unlock)→repeat-deny; J2 lock across both exits; 5-edge illegal-transition sweep with full-row zero-write equality; twice-green (REQ-J6).

**Docs (2 files):** `session-lifecycle.md` §1/§2.3/§10 forwards resolved with landed anchors; `state-machine-invariants.md` §1 implementation-reference updated (INV-S6 enforced, S7/S8 gates live, dispute verified, matrix codified).

## Verified Invariants (final)

- **INV-S1** (completed terminal) + **INV-S2** (cancelled terminal): matrix-pinned (17 denials incl. self-edges) + journey sweep row-equality proofs.
- **INV-S6** (in-session lock): lock rides the start transaction; release rides every `started`-exit transaction; fault-injection proves atomic rollback; journey J2 + admin-resolve restore verified.
- **INV-S7/S8**: gate functions live, unit-covered (pass/deny/unknown/rollback), consumed-by-contract documented for DEV3-006.
- **B.18** (disputed = admin-only exit): exactly-once open, cross-actor teacher-complete denial, admin resolve exactly-once, race-tier exactly-once refund.
- **INV-B8** (lane provenance): J1 refund re-credits the RECORDED lane (+1 trial); hifz untouched.

## Compliance Notes

- All tests via `bun run test/scripts/run-test.ts` (never raw `bun test`); repo/service units under `runInRollback` with `tx` everywhere; journeys committed-fixture + `afterAll` hard-delete; the only cross-connection races are gated to the real-PG posture via the repo's own `testOnRealPostgres` seam (PGlite sandbox limitation documented in `test/helpers/skip-when-pglite.ts`).
- P4 self-review: single-tx atomicity (lock/release/refund all inside the caller's `withTransaction`); zero dead code / `console.*`; no cross-layer imports; enums value-imported everywhere; domain errors localized via `getServerTranslations(locale)` (default-locale fallback through the same system); exactly one `logDomainError` per denial site.
- Sandbox note: no system PostgreSQL in this environment; the repo's supported `DB_PROVIDER=pglite` path was used (`.env`/`.env.test` gitignored, fixture-only values). CI (real PG) additionally exercises the gated cross-connection tiers.
