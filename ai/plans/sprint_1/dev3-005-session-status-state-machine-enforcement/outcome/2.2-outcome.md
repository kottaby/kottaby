# Task 2.2 — Lock Composed into `startSession` Outcome

**Date:** 2026-09-08 · **Branch:** `feat/dev3-005-session-state-machine`

## Shipped

`backend/services/classes/session-lifecycle.service.ts` — `startSession` (previously a single bare guarded call on `tx ?? db`) now composes through `withTransaction(outerTx, …)`:

1. `SessionRepository.startSessionOnce(sessionId, teacherUserId, tx)` — unchanged guarded writer;
2. on success, the SAME transaction: `TeacherRepository.findById(started.teacherId, tx)` — the prior-online capture (teacher lookup keyed on the TRANSITIONED row's id, never caller input; a `null` row is the fail-closed unreachable arm that throws and rolls the flow back);
3. `TeacherRepository.setOnline(started.teacherId, false, tx)` — the INV-S6 lock.

Atomicity: production callers (no `outerTx`) now get transition + lock as ONE committed unit — a started session with an unlocked teacher is structurally impossible; test callers pass `outerTx` and ride a SAVEPOINT, exactly like `createSession`/`cancelSession`. Public signature unchanged (`tx` param renamed to `outerTx` to match the sibling flows' documented contract — no call-site change; the GraphQL mutation passes positionally).

The same-tx `findById` read doubles as the documented priorOnline capture (the D2 seam): with no availability-toggle surface, a deliberate mid-session offline state cannot exist, so the release direction stays a plain restore — recorded in the deferred ledger, not improvised (plan §Concurrency).

## Tests (2.2.TE) — in `session-lifecycle.service.test.ts`, new `INV-S6 in-session lock` describe (runInRollback, PGlite-safe)

- start with an online teacher fixture ⇒ `is_online=false` post-flow (same-tx read-back oracle);
- **rollback-fault injection**: a file-local trigger raises on the teacher UPDATE (the lock write) ⇒ the raw `P0001` surfaces unmasked, the session stays `scheduled` with `startedAt=null`, and the teacher stays online — ZERO partial persistence (both writes aborted together).

## Pin/Seam Updates (same-change rule)

`REQ-043(a)` (concurrent double-start on the PRODUCTION tx path) previously passed accidentally-serially on PGlite's single connection (the old start flow was bare autocommit statements). With real per-call transactions, the premise "each concurrent call opens its own real transaction on its own connection" (this block's own doc) is real-PG-only — the test is now gated `testOnRealPostgres`, matching the block's pre-existing gates (d)/(e) and lines 998/2111/2143 precedent. REQ-043(b)/(c) gated identically (same production-path concurrency premise). CI (real PG) runs them unchanged.

## QL / SEC / SR / IV

- **QL** exit 0 on the service file + test file (`--lifecycle duplicates`); `bun oxlint` 0/0; `bun tsgo` clean; biome-formatted.
- **SEC**: lock write keyed off the server-side transitioned row; no new client surface; denial vocabulary unchanged (zero-row start still classifies through `rejectTransitionMiss` BEFORE any lock write — the probe read happens on the miss path only).
- **SR**: no dead code; composition-only (plan Decision 2 style); the barrel-member union across the pinned service unit is unchanged (`TeacherRepository` already rode it via `transitions.ts`); the specifier allowlist pin stays green with zero allowlist edits (no new import specifiers).
- **IV**: backend.instructions + services AGENTS honored (localized denials, logger-only, tx threading).
