# Task 2.1 — `TeacherRepository.setOnline` Outcome

**Date:** 2026-09-08 · **Branch:** `feat/dev3-005-session-state-machine`

## Shipped

`backend/db/repo/teachers/teacher.repository.ts` — ONE method added, exactly the plan signature:

```ts
export async function setOnline(id: number, online: boolean, tx: DBTransaction): Promise<TeacherSelectType | null>
```

- Guarded single-statement UPDATE (`SET is_online = $2, updated_at = now() WHERE id = $1 RETURNING *` — Drizzle form, `sql\`now()\`` stamp per the sibling writers).
- **Unconditional beyond row identity, deliberately**: going offline is always allowed (the lock); the restore direction's policy gating (never resurrect a deliberately-offline teacher) is the SERVICE composition's duty via the documented availability-toggle seam (deferred ledger D2) — the repo stays policy-free per the repo-layer rules.
- `tx` is REQUIRED (repo write convention — the write always joins the caller's atomic unit; no ambient-`db` fallback branch).
- No certification predicate (an uncertified teacher may still go offline).
- Idempotent under retry: a redundant same-value write returns the row.
- Barrel: `backend/db/repo/teachers/index.ts` already `export *`s the file — zero barrel change.

## Comment-hygiene pin compliance

`teacher.repository.test.ts` pins the repo source against plan-artifact references (`REQ-\d|DEV3|…`). The new docblock cites `docs/specs/state-machine-invariants.md` + `docs/sessions/session-lifecycle.md` instead of ticket ids — pin green without touching the pin (same-change rule honored).

## Tests (2.1.TE) — extended `backend/db/test/repo/teachers/teacher.repository.test.ts`

`bun run test/scripts/run-test.ts backend/db/test/repo/teachers/teacher.repository.test.ts` → **20 pass / 0 fail / 2 skip** (the pre-existing cross-connection lock tier skips on the single-connection PGlite sandbox, unchanged). New cases (all `runInRollback`, `tx` to every call):

- lock direction: online teacher → `isOnline=false`, independent read-back oracle agrees;
- release direction: offline teacher → `isOnline=true`;
- unknown id ⇒ `null`;
- redundant same-value write ⇒ row returned, value unchanged (idempotence);
- certification plays no part: unapproved row locks and releases identically;
- rollback composition: the lock inside a forced-rollback tx un-applies (owning user row gone on a fresh read);
- lock+release composed inside ONE transaction complete deadlock-free (the tx-threading proof — an ambient-`db` mix would hang the single connection);
- static pin: REQUIRED-tx signature, guarded write shape, `.returning()`.

## QL / SEC / SR / IV

- **QL** exit 0: sub-loop `--lifecycle duplicates` on the repo file; `bun oxlint` on repo + test files → 0 warnings/0 errors; `bun tsgo` clean.
- **SEC**: id-scoped on the teacher row pinned by the session inside the caller's tx — no cross-tenant write possible; no client-shaped payload.
- **SR**: single writer stays `SessionRepository`(session)/this method(teacher flag); no dead code; enum-free primitive (a boolean flag write).
- **IV**: repo-layer AGENTS rules honored (namespace export, required-tx write, no i18n/logging imports, no prepared statements).
