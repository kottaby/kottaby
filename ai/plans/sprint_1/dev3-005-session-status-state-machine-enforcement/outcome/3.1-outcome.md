# Task 3.1 — Journey Verification Outcome

**Date:** 2026-09-08 · **Branch:** `feat/dev3-005-session-state-machine`

## Shipped

`test/workflows/sessions/session-state-machine.journey.test.ts` (NEW) — 14 tests: **13 pass / 1 skip (the real-PG race) / 0 fail**, verified by TWO consecutive green runs (REQ-J6 zero-residual proof: the second run rebuilds a fresh cast and observes none of the first run's rows).

Journey discipline honored: NO `runInRollback`; committed cast via `buildSessionJourneyCast` (primary student profiled `{trial: 2, hifz: 4}` for six bookings across the arcs); every fixture AND service-created row (sessions + idempotency claims) registered and hard-deleted FK-safely in `afterAll`; per-run `jrn_session-state-machine_<8hex>` prefix; denials asserted by `DomainError.code` + exact translated message through the try/catch helper.

## Coverage per specs

- **J1 (dispute/arbitration/refund/lock)** — 6 steps: student books (trial-lane hold) + teacher starts (teacher forced online ⇒ locked offline); student disputes (queue visibility: `listAdminDisputedSessions` totalCount +1 with the row present); duplicate dispute DENIED with zero writes (full-row equality); teacher's cross-actor complete on the disputed row DENIED with zero writes; admin resolves CANCEL (terminal, `feeHeld=false`, trial lane re-credited +1 on the RECORDED provenance lane, `is_online` restored); repeat resolve DENIED exactly-once (lane unchanged).
- **J2 (INV-S6 lock, both exits)** — leg 1: online teacher → start ⇒ offline → participant cancel ⇒ restored (+ lane refund); leg 2: start ⇒ offline → teacher complete ⇒ restored.
- **Illegal-transition sweep** — completed→start, cancelled→cancel, cancelled→start, disputed→start, scheduled→complete: every attempt denied `SESSION_INVALID_TRANSITION` and proven non-writing by FULL-row equality (`readFullSessionRow` before/after — every column including stamps identical).
- **Race (3.1.TE `Promise.allSettled` on duplicate resolve)** — two concurrent admin CANCEL resolves: exactly one wins, the loser receives the typed conflict, the refund fires EXACTLY once (lane delta +1), the lock lifts once. Gated `testOnRealPostgres` (the premise is one real connection per concurrent call; skipped on the single-connection PGlite sandbox per the repo's own seam — runs in CI).

## QL / TE / SEC / SR / IV

- **QL** exit 0 (sub-loop `--lifecycle duplicates`); `bun oxlint` 0/0; `bun tsgo` clean; biome-formatted.
- **TE**: Tiers 1–4 covered per specs (happy paths, exact-denial messages, zero-write proofs, the race); 64+ expect() calls across 14 tests.
- **SEC**: non-admin resolve denied upstream (role re-check) is exercised by J1's cross-actor step; participant-only ops deny foreign actors (teacher-complete-denied-on-disputed is the teacher's OWN row in a non-completable state; foreign-row blindness stays covered by the service suite's oracle pins).
- **SR**: zero service-created rows left untracked (step assertions + registry totals); no runInRollback import.
- **IV**: journey layer contract per `test/workflows/AGENTS.md` + the sibling journey's documented patterns.
