# Task 4.1 — Documentation Amendments Outcome

**Date:** 2026-09-08 · **Branch:** `feat/dev3-005-session-state-machine`

## `docs/sessions/session-lifecycle.md`

- **§1 state table** — the `disputed` row no longer claims "no transition surface here — DEV3-022/DEV3-005 own it": the dispute writers are shipped and journey-verified (participants open from a live state exactly once; only admin arbitration exits it).
- **§2.3 invariant binding** — the three forwards resolved:
  - INV-S6 → **Landed**: `startSession` composes `TeacherRepository.setOnline(teacherId, false, tx)` onto the SAME transaction; the release composes through `releaseTeacherInSessionLock` (`session-lifecycle.transitions.ts`) into `completeSession`, `cancelSession`-from-`started`, and `resolveSessionDispute`-from-`started` (`started_at` classifies the pre-state); the deliberate-offline ceiling is the DEV2-011/012 seam (ledger D2) with `assertTeacherNotInActiveSession` as the future toggle's pre-write guard.
  - INV-S7 → **Landed**: `assertSessionCompletedForReport` in `backend/services/classes/session-lifecycle.enforcement.ts`; canonical `SESSION_INVALID_TRANSITION` denial, no existence oracle.
  - INV-S8 → **Landed**: `assertReportSubmittedForHomework` in the same module, backed by the minimal `ReportRepository.existsReportForSession`; denial key `homeworkRequiresReport` (en/ar).
- **§10 consumer table** — the DEV3-005 row no longer forwards "owns: … lock, report/homework gating, the `disputed` transition" (stale): it now cites the landed surfaces (matrix + gates + lock/release primitives) and narrows the remaining DEV3-005 scope to ledger D3 (status-history table). The DEV3-006 row now mandates CONSUMING the landed gates (participation checks stay the consumer's, before the gate) instead of "INV-S7 gating is DEV3-005's".

## `docs/specs/state-machine-invariants.md`

- **§1 implementation-reference block** — a second reference paragraph added: INV-S6 ENFORCED (lock + release composition), INV-S7/S8 gates live (module path + function names + DEV3-006 consumption), the `disputed` surface journey-verified end-to-end (journey file cited), the closed matrix codified (`SESSION_TRANSITION_MATRIX`), and the read-side directory filter explicitly remaining DEV2-013/DEV3-008-owned. The stale "INV-S6/S7/S8 + the `disputed` surface are DEV3-005/DEV2-013/DEV3-022-owned" claim was removed from the DEV3-004 paragraph.

## Root `AGENTS.md` (verify-before-touch mandate)

- Line 461's lifecycle-doc reference describes the doc as "DEV3-004: … consumer guidance for DEV3-005/006/…" — it makes NO INV-S6-pending claim, so per the plan ("change only if stale") it was left untouched.

## QL / SR / IV

- **QL**: the sub-loop's tsgo/oxlint/biome stages are code-only (both linters are configured to ignore `*.md` — verified: "no files to lint"), so the applicable markdown gates were run directly: `jscpd` duplicates scan over both edited docs → **0 exact clones** (exit 0). No code files were touched in this task.
- **SR**: no anchors cite line numbers that drift (path + exported-symbol anchors only); the §2.3/§10 wording mirrors the shipped code (verified by the 0.2 sweep's anchors); no forward references to lands that don't exist.
- **IV**: doc edits carry no new process claims beyond the shipped evidence (outcomes 1.1–3.2).
