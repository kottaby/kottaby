# Task 1.1 — Enforcement Module Outcome

**Date:** 2026-09-08 · **Branch:** `feat/dev3-005-session-state-machine`

## Shipped

1. **`backend/services/classes/session-lifecycle.enforcement.ts`** (NEW) — exactly the plan §Service/Repository Contracts surface:
   - `SESSION_TRANSITION_MATRIX: Readonly<Record<SessionStatus, ReadonlySet<SessionStatus>>>` — the closed matrix: `scheduled→{started,cancelled,disputed}`, `started→{completed,cancelled,disputed}`, `disputed→{completed,cancelled}`, `completed→{}`, `cancelled→{}`. Pure module constant, enum-keyed (value import `SessionStatus`, zero string literals).
   - `isSessionTransitionAllowed(from, to)` — pure O(1) predicate; identical-status no-ops deny.
   - `assertSessionCompletedForReport(sessionId, tx, t?)` — INV-S7; `SessionRepository.findById` on the caller's tx; uniform `ConflictError("SESSION_INVALID_TRANSITION", t.sessionInvalidTransition)` for every non-completed outcome incl. unknown id (no existence oracle).
   - `assertReportSubmittedForHomework(sessionId, tx, t?)` — INV-S8; probes via the new minimal `ReportRepository.existsReportForSession`; denies `ConflictError("HOMEWORK_REQUIRES_REPORT", t.homeworkRequiresReport)`.
   - `assertTeacherNotInActiveSession(teacherId, tx, t?)` — DEV2-011 seam (ledger D2); probes `SessionRepository.listForTeacher(teacherId, {status: Started}, 1, 0, tx)`; denies `ConflictError("TEACHER_IN_ACTIVE_SESSION", t.teacherInActiveSession)`; the message discloses NO session id (oracle ruling).
   - `t` optional: request-locale callers pass their resolved `errorsTranslations`; omission falls back to `getServerTranslations(defaultLocale)` on the denial path only (one localization source, never a hardcoded string). Exactly one `logger.logDomainError` per denial site; happy paths silent.
2. **`backend/db/repo/classes/report.repository.ts`** (NEW) — ONLY `existsReportForSession(sessionId, tx?)` per the plan's 0.2-gated carve-out (no report repo existed); tx branch = Drizzle existence select, standalone = parameterized `queryDb`. DEV3-006 extension contract recorded in ledger D1.
3. **Barrels** — `backend/db/repo/classes/index.ts` + `backend/services/classes/index.ts` re-export via `export *` with `./` paths.

## Quality Loop (P2) — all exit 0

- `bun run scripts/health/sub-loop.ts backend/services/classes/session-lifecycle.enforcement.ts --lifecycle duplicates` ✅
- `bun run scripts/health/sub-loop.ts backend/db/repo/classes/report.repository.ts --lifecycle duplicates` ✅
- `bun tsgo` clean after every edit.

## Tests (1.1.TE) — `bun run test/scripts/run-test.ts backend/services/classes/session-lifecycle.enforcement.test.ts`

**17 pass / 0 fail / 56 expect()** (PGlite sandbox):
- Tier 1: full 5×5 matrix truth table (8 legal edges, 17 denials incl. terminals/self-no-ops); gate pass paths.
- Tier 2: gates deny disputed/cancelled/scheduled/unknown-id uniformly; report-missing vs report-present; seam-gate actor isolation + session-id-blind message; default-locale fallback.
- Tier 3: concurrent in-tx gate reads (`Promise.allSettled`) — PGlite-safe; the true cross-connection race is the journey suite's committed-fixture territory (3.1), matching the sibling suites' `testOnRealPostgres` seam.
- Tier 4: rollback phantom-pass proof (in-tx pass + fresh-tx denial after forced rollback).
- Test-file QL exit 0 after fixes: renamed `allowed_`→`isAllowed` (no-underscore-dangle), unrolled loop-await (no-await-in-loop), instanceof-narrowed denial helper (no-unsafe-type-assertion), `.toSorted((a,b)=>a.localeCompare(b))` (sonarjs/no-alphabetical-sort), explicit `expect(outcome).toBeUndefined()` on pass paths (sonarjs/assertions-in-tests).

## Security (1.1.SEC)

Ids are server-derived only; no client shape accepted; denial vocabulary reuses existing codes (`SESSION_INVALID_TRANSITION`) plus two new scoped codes; no existence oracle widened (uniform denials; participation stays the caller's pre-gate duty — D1 contract).

## Semantic Review (1.1.SR)

Zero dead exports (all four symbols journey/service-consumed); no inline matrix duplicates elsewhere (grep: no other transition-set literals); enums value-imported; no `console.*`; no cross-layer imports; tx propagated everywhere (ambient `db` untouched inside gates).

## Instruction Verification (1.1.IV)

Validated against `.agents/instructions/backend.instructions.md` (repo namespace/barrel/queryDb idioms, no hardcoded strings, logger-only) + `backend/services/AGENTS.md` + `backend/db/repo/AGENTS.md` + auto-discovered AGENTS.md files printed by the sub-loop.
