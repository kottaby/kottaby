# Post-Implementation Review Wave (Task 6.1) — Round 1

**Branch:** `feat/dev3-007-recitation-record-per-session-11` @ `4609e5b` (8 implementation commits over baseline `ffce457`).
**Scope:** `git diff --name-only ffce457 HEAD` — 33-file source/test changeset + generated artifacts + plan artifacts.
**Process per SKILL.md §Post-Implementation Review Wave:** four independent review subagents dispatched in parallel (review-types / review-backend / review-frontend / pentester), findings aggregated, deduplicated, pre-existing filtered.

## Wave verdicts

| Wave | Reviewer | CRITICAL | HIGH | MEDIUM | LOW | INFO | Verdict |
|---|---|---|---|---|---|---|---|
| 1 | review-types | 0 | 0 | 0 | 1 | 0 | Approved (LOW → ledger D6) |
| 2 | review-backend | 0 | 0 | 0 | 0 | 0 | Approved clean |
| 3 | review-frontend | 0 | 0 | 0 | 0 | 2 | Approved (2 INFO documented rulings) |
| 4 | pentester | 0 | 0 | 0 | 2 | 2 | Approved (LOWs → ledger D4/D5; INFOs pre-existing platform) |

**Aggregate: 0 CRITICAL / 0 HIGH / 0 MEDIUM / 3 LOW / 4 INFO — zero blocking findings; no fix phase required.**

## Findings detail + resolutions

1. **[LOW → D6] Types test-helper mirroring** (`recitation.types.test-d.ts`, `recitation.types.static-assertions.test.ts`): `Equals` probe + lib-file reader duplicated from session sibling suites. Sanctioned by the plan's own mirror mandate (task 1.1); consolidation requires refactoring out-of-scope session files → ledgered 📅 Forward.
2. **[LOW → D4] Role-claim staleness window**: `ctx.role` sourced from JWT claim platform-wide; a post-issuance demotion on a still-valid token passes the pre-resolver scope wall; the service re-checks governance (REQ-031) but not role — exactly what the plan pins (REQ-030 scope-layer role gate + REQ-052 matrix with no role-recheck row). Bounded by the DB-row ownership predicate (`session.teacherId === caller`). Platform-level, pre-dates this plan, affects all scoped surfaces → ledgered 📅 Forward (platform auth ticket).
3. **[LOW → D5] Governance denial log lacks `locale` key**: reused `assertActorGovernanceClean` logs `{code, entity, entityId}` (no locale); identical behavior on the session-lifecycle surface. Fixing per-service would fork the helper (violates reuse-verbatim Decision 6) or double-log (violates exactly-one-log REQ-035) → central helper fix ledgered 📅 Forward (platform services ticket).
4. **[INFO, pre-existing] Error-masking finalizer logs full SQL + params** on non-domain masking (`backend/lib/error-masking/...:425`) — zero diff to `backend/lib/**` in this changeset; platform-level note only.
5. **[INFO, pre-existing helper] `isUniqueViolation` carries SQLite message-sniff legs** alongside the mandated 23505 cause-chain; on this PostgreSQL-only surface only the code check is reachable — REQ-041 holds in effect.
6. **[INFO, documented ruling] Top-level `sharedDocuments/index.ts` intentionally untouched** (already re-exports `./scheduling`) — recorded in 4.1 outcome §1/§4.
7. **[INFO, structural] Top-level `DOCUMENT_CONTRACT_TABLE` has no recitation rows** — coverage lives in the colocated `recitation.documents.test.ts` per the established per-domain pattern (parent-link/admin/notifications precedents).

## Pre-existing issues filtered (not counted)

- Admin-governance journey drift (DEV3-017, `b01d21d`) — auth-vs-errors namespace copy; zero recitation involvement; byte-untouched (grep-verified empty in changeset).
- Type-guard idiom duplication (`isDBTransaction`/`isTransactionExecutor`, ~10 repo sites).
- `RECONCILED_*` upstream inventory reconciliation in `schema-surface.test.ts` (baseline was red; additive fix per the file's own DEV3-016 precedent).

## Reviewer-verified control evidence (highlights)

- `$all` scope semantics proven at plugin source (`request-cache.js` defaultStrategy `any`; `$all` forAll) — the ANY-semantics hazard is real and defeated.
- Oracle-collapse is byte-equality over full response bodies (wire) and `denialShape` deep-equality (service) — not eyeballed.
- Race arbiter: no recitation SELECT-before-INSERT anywhere; unique constraint serializes (3 independent real-PG race arms).
- Governance re-check pre-tx with spy-ordering proof (0 repo calls when suspended).
- Log inventory: exactly 5 bounded sites in the service; zero on happy/collapse (spy-counted).
- Independent suite re-runs during review: repo 11, service 22, journey 8, wire 25, schema-surface 41, sdl-static 39 — all green; `bun tsgo` 0 errors.

## Deferred-items check (task 6.1 mandate)

Ledger state: D1–D3 📅 Forward (pre-seeded consumers/future surfaces) · J1 ✅ Done · **D4–D6 added this round** (📅 Forward, platform-scope, non-blocking). No ❌ / ⚠️ rows.

**Wave 1 verdict: PASS — zero blocking findings. Proceeding to knowledge propagation; review iterations continue (rounds R2+).**
