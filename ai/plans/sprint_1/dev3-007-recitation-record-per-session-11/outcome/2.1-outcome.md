# Task 2.1 Outcome — RecitationRepository (closed two-method namespace) + four-tier repo suite

**Plan directory (verbatim):** `ai/plans/sprint_1/dev3-007-recitation-record-per-session-11`
**Task:** 2.1 — CREATE `RecitationRepository` (exactly `insertOnce` + `findBySessionId`), wire the classes repo barrel, ship the REQ-070 four-tier DB suite.
**Branch:** `feat/dev3-007-recitation-record-per-session-11` (sandbox supervision flipped the checkout to `main` between commands repeatedly — re-verified/re-checked out before EVERY command; untracked working-tree files survived every flip).
**No commits made; `tasks.md` / `worklog.md` / `deferred-items.md` untouched** (orchestrator-owned per dispatch constraints — no checkbox was toggled by this subagent).
**Requirements:** REQ-011 (repo primitives), REQ-015 (write-once, no update/delete), REQ-040 (tx propagation), REQ-070 (four-tier suite).

---

## 1. What shipped

### `backend/db/repo/classes/recitation.repository.ts` (CREATE — 112 lines)

`export namespace RecitationRepository` closed at EXACTLY two exported functions (tx LAST on both):

| Method | Signature | Cold path (no tx) |
|---|---|---|
| `insertOnce` | `(insert: RecitationInsertType, tx?: DBTransaction): Promise<RecitationSelectType>` | `tx ?? db` single-statement `INSERT … RETURNING` (mirrors `SessionRepository.insertSession`); `23505` propagates RAW — no translation (mirrors `TeacherRepository.insertColdStartCertified`'s raw-23505 contract, verified anchor d2) |
| `findBySessionId` | `(sessionId: number, tx?: DBQueryExecutor): Promise<RecitationSelectType \| null>` | dual-executor branch (verified anchor d1): Drizzle `tx.select()` when the executor is a transaction (runtime narrowing via the `isDBTransaction` type guard, mirrored from `teacher.repository.ts:52-54`), raw parameterized `queryDb` SQL (`WHERE session_id = $1 LIMIT 1`) otherwise; `null` on miss |

- Canonical types imported from `@/backend/types` (`RecitationInsertType`, `RecitationSelectType`, `DBTransaction`, `DBQueryExecutor`) — ZERO local type re-declaration.
- NO update/replace/delete/list method; single-statement insert (no read-then-write window); parameterized equality only (no LIKE/ILIKE, no caller-supplied column/table interpolation, no inline `--` comments in the SQL string); no `console.*`; no i18n/logging imports; clean domain-only comments (zero plan artifacts, verified by grep).
- NO module-level prepared statement: the non-tx read branch uses the `queryDb` bare-read pool path per `backend/AGENTS.md:24-25` / `backend/db/repo/AGENTS.md` (the verified `TeacherRepository.findById` / `session.repository.helpers.ts:167-178` dual-branch pattern — 0.2 anchor d1 ruling), and writes are excluded from preparation by `docs/drizzle/prepared-statements.md`. Header documents this choice.

### `backend/db/repo/classes/index.ts` (UPDATE — exactly +1 line)

`export * from "./recitation.repository";` prepended (alphabetical: `recitation` < `session*`), mirroring 1.1's barrel-ordering decision. Anchor l (0.2) confirmed this barrel edit belongs to 2.1.

### `backend/db/repo/classes/__tests__/recitation.repository.test.ts` (CREATE — 11 tests)

Four-tier suite per REQ-070 + the dispatch's Tier spec. ALL transactional writes inside `runInRollback` with `tx` passed to EVERY call; error surfaces via `expectRepoError` (zero `rejects.toThrow()`); committed fixtures tracked + hard-deleted.

| Tier | Tests (11 total, 42 expect()) |
|---|---|
| Closure pin | `Object.keys(RecitationRepository)` deep-equals `["findBySessionId", "insertOnce"]` (runtime source-pin; `toSorted(localeCompare)` for lint compliance) |
| 1 — contract | insertOnce returns the created row (id/sessionId/name/description/`Date` stamps) + findBySessionId read-back on `tx`; explicit `null` description round-trips; miss (recorded session, no record) → `null`; guaranteed-absent session id → `null` |
| 2 — executors | BOTH executor branches: insertOnce without tx (cold `db` executor) + findBySessionId without tx (cold `queryDb` branch) against the COMMITTED fixture with immediate hard-delete + zero-residue probe; two more cold-branch misses (record-less committed session; absent id) |
| 3 — chaos | duplicate insert inside a SAVEPOINT surfaces RAW `23505` + `constraintNameOf(err) === "recitation_session_id_unique"` + cause-chain `23505` traversal, original record intact after savepoint rollback; concurrent double-insert race on committed fixtures (`Promise.allSettled`, two real connections): exactly ONE winner row + one loser with the same raw `23505`/constraint evidence, winner hard-deleted immediately (skip-gated via `isPgliteProvider()` — RAN, not skipped, on this real-PostgreSQL env) |
| 4 — isolation | sibling-session isolation: insert for session A, read for session B → `null` + scoped count oracle 0; sanity read-back of A's own record |

- Fixtures mirror the sibling suite (`session.repository.test.ts`): `createTestUser`/`createTestStudent` from `entity-setup.ts` + direct `teacher`/`session` row inserts (no factories exist); committed fixture in `beforeAll` via one `db.transaction`, FK-safe `afterAll` hard-delete (recitation → session → teacher/students → users).
- The duplicate-insert arm wraps the colliding call in a Drizzle SAVEPOINT (`tx.transaction`) exactly like the `parent-link-request.repository.test.ts` precedent, so the outer rollback tx stays usable for the post-conflict probe.
- Post-run residue probe against `kottaby_test`: **0 rows** for every fixture name; `recitation` total = 0; 0 orphan sessions. Zero leakage.

## 2. Files created / modified

| File | Action |
|---|---|
| `backend/db/repo/classes/recitation.repository.ts` | CREATE |
| `backend/db/repo/classes/__tests__/recitation.repository.test.ts` | CREATE |
| `backend/db/repo/classes/index.ts` | MODIFY (+1 line, verified `git diff --stat` = 1 insertion) |

`git status --porcelain` shows EXACTLY these three files — no other file touched.

## 3. Verification evidence (commands + exit codes)

| Gate | Command | Exit code | Result |
|---|---|---|---|
| QL-1 | `bun run scripts/health/sub-loop.ts backend/db/repo/classes/recitation.repository.ts --lifecycle duplicates` | **0** | tsgo/oxlint/biome/lint:type-aware/check:duplicates all pass |
| QL-2 | same for `backend/db/repo/classes/index.ts` | **0** | all pass |
| QL-3 | same for `backend/db/repo/classes/__tests__/recitation.repository.test.ts` | **0** | after two fixes: `sort()`→`toSorted()` (unicorn/no-array-sort) and plain `toSorted()`→`toSorted((a,b)=>a.localeCompare(b))` (sonarjs/no-alphabetical-sort); jscpd skipped (test file outside scan scope) |
| TE-1 | `bun run test/scripts/run-test.ts backend/db/repo/classes/__tests__/recitation.repository.test.ts` | **0** | **11 pass / 0 fail / 42 expect()** — race arm RAN (real PostgreSQL; `pg_isready` exit 0 before the run) |
| TE-2 (regression) | `bun run test/scripts/run-test.ts backend/db/repo/classes/__tests__/session.repository.test.ts` | **0** | 10 pass / 0 fail — sibling suite unaffected by the barrel line |
| Sub-loop auto-discovered rule files | root `AGENTS.md`, `backend/db/repo/AGENTS.md`, `backend/AGENTS.md` (+ `⚠ .github/instructions/backend.instructions.md` — path absent from this checkout; the live `.agents/instructions/backend.instructions.md` + `.agents/instructions/tests.instructions.md` were read in full and applied) | — | complied |

## 4. SEC audit (security & tenancy)

- **Parameterized equality only** — `eq(recitation.sessionId, sessionId)` (Drizzle branch) and a single `$1` bind (cold branch); grep proves no `LIKE`/`ILIKE` anywhere → wildcard-escaping N/A (recorded, as the dispatch requires).
- **No caller-supplied column/table interpolation** — the raw SQL string is a fixed literal; the only dynamic element is the bound `$1` value.
- **Identity never input-bound** — `insertOnce` writes the mapped DTO verbatim with no identity derivation/override; the owning `sessionId` is a service-resolved FK, and the closed namespace exposes no surface to steer columns or tables.

## 5. SR — Semantic Review (every item, explicit)

- [x] Namespace closed at two methods — runtime pin test green (`["findBySessionId","insertOnce"]`); no other exports exist in the namespace.
- [x] `tx` param LAST on both methods — `insertOnce(insert, tx?)`, `findBySessionId(sessionId, tx?)`.
- [x] Canonical types imported, no local re-declaration — all four types from `@/backend/types`.
- [x] No dead branches — `if (tx && isDBTransaction(tx))` both arms exercised by tests; `if (!row)` defensive guard is the verbatim sibling invariant (`SessionRepository.insertSession:103-105`, `TeacherRepository.insertColdStartCertified:148-151`), unreachable by `RETURNING` semantics (same convention, not covered by sibling suites either).
- [x] No cross-layer imports — repo touches only `backend/db`, `backend/types`, `drizzle-orm`; test touches `backend/*` + `@/test/helpers` (test layer).
- [x] No module-level mutable state — repo file has zero module-level `let`/collections (only the type guard + namespace); the test file's single `let committedFixture` follows the established `session.repository.test.ts` fixture pattern.
- [x] No race via read-then-write — `insertOnce` is ONE statement; uniqueness decided by the DB constraint, proven by the Tier 3 race.
- [x] Enum VALUE imports — N/A (no enum consumed at runtime; the repo reads/writes no status column).
- [x] No `console.*` — grep clean.
- [x] Clean comments — grep for `REQ-\d|DEV3-\d|Phase \d|Task \d|plan\.md|tasks\.md|specs\.md` over both files: zero hits; comments are domain behavior only (repo docs references allowed, mirroring `session.repository.ts`).
- [x] Only task files modified — `git status --porcelain` = 2 untracked creates + 1 one-line barrel modify.
- [x] `git diff --name-only` matches the expected task file list.
- [x] No unledgered deferrals — nothing deferred; `deferred-items.md` D1–D3 untouched and not applicable.

## 6. IV — Instruction Verification

- `.agents/instructions/backend.instructions.md`: repo = data-access only, no business logic/permission checks ✓; types from `@/backend/types` ✓; `queryDb` bare-read rule ✓ (see §1); no hardcoded user-facing error strings — the only thrown `Error` is the structural no-row invariant carried verbatim from both sibling repos (not user-facing, service never sees it in the happy path) ✓; no `console.*` ✓.
- `.agents/instructions/tests.instructions.md`: `runInRollback` everywhere a tx can be used ✓; `tx` to EVERY repo/helper/direct call ✓; `expectRepoError`, never `rejects.toThrow()` ✓; `bun:test` imports ✓; no `any` ✓; types from `@/backend/types` ✓; mandated runner `bun run test/scripts/run-test.ts` ✓ (`scripts/run-test/` does not exist in this checkout — confirmed); committed fixtures cleaned in `afterAll` + in-test hard-deletes ✓; no `getServerTranslations` in tests ✓; no `console.*` ✓.
- `backend/db/repo/AGENTS.md` + `backend/AGENTS.md` + `backend/db/test/AGENTS.md`: namespace-per-file ✓; barrel re-export line ✓; tx LAST ✓; entity-setup helpers + no seed data ✓; rule-9 committed-fixture discipline ✓; residue probes ✓.
- `docs/drizzle/prepared-statements.md`: NO inline `--` comments inside any `sql`/SQL string (grep clean) ✓; writes not prepared ✓; `inArray` not used ✓; module-level prepared statement deliberately not introduced because the non-tx read branch runs on the `queryDb` pool path (the doc + `backend/AGENTS.md:25` rule that prepared statements MUST be removed when replaced by `queryDb(tx)`) — the exact pattern of the two verified mirror repos.

## 7. Carry-forward knowledge (for task 2.3 and beyond)

- **Exact import path + signatures for `RecitationRecordService` (2.3):**
  - `import { RecitationRepository } from "@/backend/db/repo";` (top-level barrel — recommended convention) or deep `@/backend/db/repo/classes/recitation.repository`.
  - `await RecitationRepository.insertOnce({ sessionId, name, description }, tx): Promise<RecitationSelectType>` — pass the service-mapped `{ sessionId, name, description }` (never the raw `SessionRecitationSubmitInput`; map it field-by-field first). A duplicate surfaces the RAW pg `23505` on the Drizzle cause chain — catch via `isUniqueViolation(err)` (`@/backend/services/shared`) around the tx block → `ConflictError("RECITATION_ALREADY_EXISTS", t.recitationAlreadyExists)`; everything else rethrow untouched.
  - `await RecitationRepository.findBySessionId(sessionId, tx): Promise<RecitationSelectType | null>` — accepts `DBTransaction` OR `DBQueryExecutor` (narrowed internally); `null` = unrecorded; feed it straight into the read pipeline's null-collapse.
- **`RecitationSelectType`** = `{ id: number; sessionId: number; name: string; description: string | null; createdAt: Date; updatedAt: Date }` — 2.3's `RecitationReturnType` is byte-identical (1.1 derivation), so the returned row can be returned as-is.
- **Test conventions proven on this table** (reuse in 2.3's service suite where applicable): savepoint bracket for in-tx unique-violation probes; `describeOnRealPostgres` + `isPgliteProvider()` gate for cross-connection races; committed-fixture + immediate-hard-delete discipline for any default-executor arm; `constraintNameOf` + a cause-chain `23505` walker are both needed (Drizzle masks the driver error).
- **2.2 (journey test, TEST-FIRST):** `RecitationRepository` is now importable from `@/backend/db/repo`; journey fixtures can create the committed session via `db.transaction` + direct `session` insert (no session factory exists) exactly as `session.repository.test.ts` does.
- **Coverage note:** repo method coverage is 100% of branches except the two structurally-unreachable no-row guards (mirrored verbatim from sibling repos, uncovered there too) — consistent with the sibling convention.

## 8. Cross-file dependencies

- `backend/types/classes/recitation.types.ts` (task 1.1) → `recitation.repository.ts` (imports the canonical types via `@/backend/types`).
- `backend/db/schema/classes/recitation.ts` → repo + test (table + constraint `recitation_session_id_unique` are the structural ground truth; zero schema edits).
- `backend/db/repo/classes/index.ts` → `recitation.repository.ts` (one `export *` line; top-level `backend/db/repo/index.ts` untouched — already re-exports `./classes`).
- Downstream (2.2/2.3/3.1/4.1) depend on the two method names + signatures EXACTLY as shipped; renames cascade into the service, resolver, and journey layers.
