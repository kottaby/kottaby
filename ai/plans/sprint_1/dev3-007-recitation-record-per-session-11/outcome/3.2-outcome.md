# Task 3.2 Outcome — Codegen sync + frozen-inventory extension

**Plan directory (verbatim):** `ai/plans/sprint_1/dev3-007-recitation-record-per-session-11`
**Task:** 3.2 — run `bun run generate:gqlSchema && bun run codegen` in the same commit as 3.1, prove the committed generated SDL matches the built schema byte-for-byte (plus a consecutive-run determinism proof), and ADDITIVELY extend the frozen inventories in `backend/graphql/test/schema-surface.test.ts` + `backend/graphql/test/sdl-static-assertions.test.ts` with the session-recitation surface.
**Branch:** `feat/dev3-007-recitation-record-per-session-11` (HEAD `46e8962`, the 3.1 commit) — re-verified/re-checked-out before EVERY command (the sandbox supervisor resets HEAD to `main` between commands, exactly as recorded in 3.1 §6; all working-tree changes survive the flips because the touched files are either untracked or byte-identical between `main` and the feature branch).
**No commits made; `tasks.md` / `worklog.md` / `deferred-items.md` untouched** (orchestrator-owned per dispatch constraints — no checkbox toggled by this subagent).
**Requirements:** REQ-062 (Pothos object contract), REQ-064 (codegen sync + frozen-inventory extension), REQ-074 (schema/codegen pins + type conformance).

---

## 1. What shipped

### Codegen sync (step A)

- `bun run generate:gqlSchema` → **exit 0**; rewrote `frontend/graphql/generated/schema.graphql` (22113 bytes, up from 21784) with EXACTLY 16 added lines and 0 removals (verbatim `git diff`):
  1. `setSessionRecitation(input: SessionRecitationInput!, sessionId: ID!): SessionRecitation!` (Mutation root, sorted position after `setPlanActiveStatus`'s block);
  2. `sessionRecitation(sessionId: ID!): SessionRecitation` (Query root, NULLABLE, last sorted Query field);
  3. the full `type SessionRecitation { … }` block — `createdAt: DateTime!`, `description: String`, `id: ID!`, `name: String!`, `sessionId: ID!`, `updatedAt: DateTime!` (lexicographic field order);
  4. the full `input SessionRecitationInput { … }` block — `description: String`, `name: String!` (NOTE: the lexicographic print emits `description` BEFORE `name`; plan.md §3.1 listed name-first in its source-order sketch, but the dispatch mandated pinning the EXACT printed text — done verbatim).
- `bun run codegen` → **exit 0** (`graphql-codegen --config codegen.ts`, schema = `./frontend/graphql/generated/schema.graphql`); output `frontend/graphql/generated/gql/graphql.ts` was regenerated and is **byte-identical to the committed version** (sha256 `66cbdd…` before = after): the codegen pipeline only emits operation types for DOCUMENTS that consume the surface, and no recitation document exists yet — task 4.1 adds the two shared documents and its codegen run will then emit the recitation operation types. The schema side of the sync (the REQ-064 byte-for-byte artifact↔builder proof) is fully green: `schema-surface.test.ts`'s "Codegen sync" test — `readFileSync(committed) === printSchema(lexicographicSortSchema(graphQLSchema))` — PASSES on the regenerated artifact.
- **Determinism proof (three consecutive generator runs):** sha256 of `frontend/graphql/generated/schema.graphql` after G1 = G2 = G3 = `209e586b4bbcae246eab8ed90ce1d317c1da9d012e2bd45b6ff147f55052926b` — the second (and third) `generate:gqlSchema` run produced **ZERO diff**; the committed generated SDL matches the built schema byte-for-byte.

### `backend/graphql/test/schema-surface.test.ts` (UPDATE — additive extension)

- NEW inventory constants (one new block after `DEV3_022C_TYPE_NAMES`):
  - `DEV3_007_MUTATION_FIELDS = ["setSessionRecitation"]`, `DEV3_007_QUERY_FIELDS = ["sessionRecitation"]`, `DEV3_007_TYPE_NAMES = ["SessionRecitation", "SessionRecitationInput"]` — the sanctioned session-recitation additions, spread into the three freeze expectation arrays (query additions, mutation set, whole-schema named-type delta).
  - `RECONCILED_*` constants (see §2 — the one-time re-anchor of upstream surfaces that shipped without inventory entries: `RECONCILED_QUERY_FIELDS` = adminAuditLogs + parent-link pair; `RECONCILED_MUTATION_FIELDS` = broadcast + cold-start certification + parent-link trio; `RECONCILED_ENUMS` = `BroadcastAudienceType`, `LinkStatus`; `RECONCILED_TYPE_NAMES` = the audit-log page family + the two broadcast inputs + the parent-link objects/status enum + `BroadcastAudienceType`), spread into the same four expectation arrays.
- "Codegen sync" describe: four belt-and-braces `toContain` pins for the recitation surface (both exact root-field lines + both type-block headers) added after the existing DEV3-016 pins — pure insertion.
- Header docblock: one new bullet paragraph documenting the DEV3-007 pair + the reconciliation — pure insertion before the "Pure unit tier" paragraph.
- **Historical pins:** every pre-existing entry/spread remains byte-identical; the ONLY rewritten hunk is the enum-set expectation line (single-line array → multi-line, forced by biome `lineWidth: 120` — the appended `...RECONCILED_ENUMS` spread pushed the line to 138 chars) with all five historical spreads preserved verbatim in their original order. Net diff: +86/−3, and the −3 lines are exclusively that reformat.

### `backend/graphql/test/sdl-static-assertions.test.ts` (UPDATE — 100% additive, +103/−0)

- `FROZEN_MUTATION_FIELDS` += `"setSessionRecitation"` at its sorted position (`setPlanActiveStatus` < `setSessionRecitation` < `startSession`); `FROZEN_QUERY_FIELDS` += `"sessionRecitation"` at its sorted position (`sessionById` < `sessionRecitation`, the new last Query field). The two root-freeze equality tests stay green with the additions as new members only.
- Header docblock: one new bullet documenting the artifact-side recitation pins — pure insertion.
- NEW closing describe **"Session-recitation surface (DEV3-007) — artifact-side pins"** (6 tests):
  1. `setSessionRecitation(input: SessionRecitationInput!, sessionId: ID!): SessionRecitation!` — exact arg array in sorted print order (`input` < `sessionId`), NonNull payload (the write-once record never returns null);
  2. `sessionRecitation(sessionId: ID!): SessionRecitation` — NULLABLE (the collapse channel), single `sessionId: ID!` arg;
  3. both root-field lines pinned verbatim in the artifact text (lexical tier; the lowercase-s query spelling cannot collide with the mutation's `setSessionRecitation`);
  4. `SessionRecitation` exposes EXACTLY the six canonical fields with exact SDL types — `id: ID!` on the wire (Apollo normalization), every instant on the `DateTime` scalar, and NO `teacherId`/`studentId`/`userId` ownership lane (SEC);
  5. `SessionRecitationInput` is the closed two-member whitelist (`description: String`, `name: String!`) with `id`/`sessionId`/`createdAt`/`updatedAt` structurally absent (BOPLA);
  6. **DateTime-once:** the artifact's complete scalar-definition set (AST `Kind.SCALAR_TYPE_DEFINITION` scan) equals exactly `["DateTime"]`, the lexical `/^scalar /gm` count is exactly 1, and the single declaration is `scalar DateTime` — zero new scalars.

## 2. Deviation / reconciliation disclosure (one, forced by pre-existing upstream drift)

**The frozen inventories in `schema-surface.test.ts` were ALREADY RED at the DEV3-007 baseline** (`ffce457`) — independent of anything DEV3-007 did. The built schema carried four surfaces that upstream tickets (#53 broadcast notifications, #51 cold-start certification, #49 parent-link, the audit-log surface) shipped without extending this file's inventories: mutations `adminBroadcastNotification`, `adminCertifyTeacherColdStart`, `cancelParentLinkRequest`, `requestParentChildLink`, `respondToParentLinkRequest`; queries `adminAuditLogs`, `myIncomingParentLinkRequests`, `myOutgoingParentLinkRequests`; enums `BroadcastAudienceType`, `LinkStatus`; named types `AdminAuditLogEntry`, `AdminAuditLogFiltersInput`, `AdminAuditLogPage`, `AdminBroadcastNotificationInput`, `BroadcastAudienceInput`, `BroadcastAudienceType`, `IncomingParentLinkRequest`, `LinkStatus`, `OutgoingParentLinkRequest`. (`sdl-static-assertions.test.ts`'s own `FROZEN_*` arrays WERE kept current by those tickets — the two files are supposed to mirror one "sanctioned growth history" and `schema-surface.test.ts` had fallen behind; its pre-3.2 run failed 5 tests.) Because task 3.2.TE mandates BOTH suites green, the missing members were registered ADDITIVELY via the new `RECONCILED_*` constants — the exact documented pattern the file itself established for the DEV3-016 reconciliation ("re-anchored to the LIVE built schema … a documented one-time reconciliation, NOT a silent baseline flip"). No historical pin value was removed, renamed, or reordered; all 11 recitation-adjacent additions are new members at their sorted positions. Zero un-ledgered deferrals arise (this is completed in-task work, not deferred work).

## 3. Files modified (complete list — same commit as 3.1's resolvers per REQ-064)

| # | File | Action | Diff |
|---|---|---|---|
| 1 | `frontend/graphql/generated/schema.graphql` | REGENERATED (`generate:gqlSchema`) | +16/−0 (the recitation surface, nothing else) |
| 2 | `backend/graphql/test/schema-surface.test.ts` | UPDATE (additive) | +86/−3 (−3 = the single biome-forced enum-expectation reformat, all historical spreads preserved) |
| 3 | `backend/graphql/test/sdl-static-assertions.test.ts` | UPDATE (additive) | +103/−0 |

### Files NOT modified (and why)

- `frontend/graphql/generated/gql/graphql.ts` — `bun run codegen` ran (exit 0) but is byte-identical: no recitation documents exist until task 4.1; the schema-side sync proof is the schema.graphql byte-equality test (green).
- `backend/lib/gateway/public-operations.ts` — `git diff` EMPTY; the only "recitation" tokens in it are the pre-existing Qira'ah `recitationReadings` allowlist entry (public catalog, different domain, D-0.2-3). Neither new op is public — no allowlist drift.
- All resolver/pothos/service/repo/types files — 3.1/Phase-1/2 output; byte-untouched by 3.2.
- Qira'ah name-siblings (`backend/graphql/query/recitation.query.ts`, `frontend/graphql/sharedDocuments/auth/recitation.documents.ts`) — byte-untouched (D-0.2-3).
- `tasks.md`, `worklog.md`, `deferred-items.md` — orchestrator-owned; nothing new deferred.

## 4. Verification evidence (commands + exit codes)

### QL — `bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates` (per file)

| # | File | Exit code | Notes |
|---|---|---|---|
| QL-1 | `backend/graphql/test/schema-surface.test.ts` | 0 → 0 | first run green; re-run after the `BroadcastAudienceType` fix, still 0. Full chain tsgo → oxlint → biome:check → lint:type-aware → check:duplicates; check:duplicates reported the file outside the jscpd scan scope (skipped by the tool's own scoping) |
| QL-2 | `backend/graphql/test/sdl-static-assertions.test.ts` | 1 → **0** | run 1: `lint:type-aware` `sonarjs/prefer-specific-assertions` on the scalar-count assertion (`expect(x.length).toBe(1)` → `expect(x).toHaveLength(1)` — root-cause fix in-file, no disable comment); run 2 all green |

(The generated artifact `frontend/graphql/generated/**` is excluded from biome's scan scope by `biome.json` (`"!frontend/graphql/generated"`) — followed the tool's own scoping; no sub-loop run applicable to it.)

### Codegen determinism (step A)

| Step | Command | Exit | sha256(`frontend/graphql/generated/schema.graphql`) |
|---|---|---|---|
| G1 | `bun run generate:gqlSchema` | 0 | `209e586b4bbcae246eab8ed90ce1d317c1da9d012e2bd45b6ff147f55052926b` |
| C1 | `bun run codegen` | 0 | (unchanged; `gql/graphql.ts` sha `66cbdd…` unchanged) |
| G2 | `bun run generate:gqlSchema` (2nd run) | 0 | `209e586b…` — **ZERO DIFF vs G1** |
| G3 | `bun run generate:gqlSchema` (3rd run, post-test-edits) | 0 | `209e586b…` — **ZERO DIFF** |

Byte-for-byte committed-SDL ↔ built-schema equality is additionally pinned as a permanent test (schema-surface "Codegen sync" describe) and passes on the regenerated artifact.

### TE — both suites via the mandated runner (`bun run test/scripts/run-test.ts`)

| Suite | Result | Exit |
|---|---|---|
| `backend/graphql/test/schema-surface.test.ts` | **41 pass / 0 fail / 267 expect()** (pre-3.2 state: 5 fail — 3 recitation freezes + enum set + codegen sync; also carried pre-existing upstream drift in 3 of those, §2) | 0 |
| `backend/graphql/test/sdl-static-assertions.test.ts` | **39 pass / 0 fail / 207 expect()** (33 pass pre-3.2; +6 new recitation/scalar tests) | 0 |

### SEC — Security & Tenancy Audit (explicit verdicts)

- **NO public-operations drift:** `git diff backend/lib/gateway/public-operations.ts` = EMPTY (byte-identical). The allowlist remains the closed 7-member set (`login`, `refreshToken`, `logout`, `registerUser`, `recitationReadings`, `_health`); grep over the file finds "recitation" ONLY as the pre-existing public Qira'ah `recitationReadings` catalog entry (module docblock + tuple member). Neither `setSessionRecitation` nor `sessionRecitation` is allowlist material — both are authScopes-gated (`$all { authenticated, role: [Teacher] }` / `{ authenticated: true }`), so no anonymous surface was added.
- **NO update/delete/list recitation fields leaked into the SDL:** case-insensitive scan of the printed artifact for every "recitation" token yields EXACTLY: the four sanctioned surface names (`setSessionRecitation`, `sessionRecitation`, `type SessionRecitation`, `input SessionRecitationInput`) + the pre-existing Qira'ah-domain tokens (`recitationReadings` query line, `enum RecitationReading`, `preferredRecitation: RecitationReading` on `RegisterUserInput` — contract metadata only, C.5). A dedicated regex scan for `(update|delete|remove|edit|patch|list)[a-z]*recitation` / `recitation[a-z]*(update|delete)` → **zero hits** (write-once invariant holds on the wire contract; D1's future audited-correction surface remains forward).
- **BOPLA closed whitelist on the artifact:** `SessionRecitationInput` = exactly `description` + `name`; `id`/`sessionId`/`createdAt`/`updatedAt` structurally absent (asserted in the new suite); the record object exposes no ownership lane (`teacherId`/`studentId`/`userId` absent).
- **No new scalars:** DateTime-once proven (§1 item 6) — the schema's only custom scalar is the pre-existing `DateTime`, and both record timestamps ride it (REQ-062; no `toISOString`-into-String anywhere on the surface).

### SR — Semantic Review (every dispatch item)

- [x] **Additions purely additive** — `git diff` of the two test files: +186 added lines; the ONLY removed lines are the 3 lines of the enum-expectation reformat (biome `lineWidth: 120` forced the multi-line form; all five historical spreads preserved verbatim in original order + one new spread appended; zero pin VALUES altered). No historical pin rewritten.
- [x] **Sorted-order strings verified against the actual printed SDL** — the pinned strings were read from the regenerated artifact (G1 output) and pinned verbatim: `setSessionRecitation(input: SessionRecitationInput!, sessionId: ID!): SessionRecitation!` (sorted `input` < `sessionId`), `sessionRecitation(sessionId: ID!): SessionRecitation`, the `SessionRecitation` block (createdAt/description/id/name/sessionId/updatedAt), the `SessionRecitationInput` block (description-before-name in lexicographic print). The suite-passing codegen byte-equality test guarantees the pinned text IS the `lexicographicSortSchema` print.
- [x] **No dead branches** — additions are data arrays + linear fail-fast assertions; no `if`/`throw` paths added beyond the file's established helpers (which were untouched).
- [x] **No cross-layer imports** — zero new import statements in either file (grep over added lines = 0); `Kind.SCALAR_TYPE_DEFINITION` rides the existing `Kind` import in sdl-static.
- [x] **Clean comments (zero plan artifacts)** — grep over all 186 added lines: zero `REQ-\d` / `Task \d` / `Phase \d` / `.ai/plans` / `tasks.md` / `specs.md` / `plan.md` hits; zero `console.*`; zero `oxlint-disable`/`eslint-disable`/`biome-ignore`. The only ticket tag is `DEV3-007` (12×), mirroring the files' own established per-ticket pin-label convention (`DEV3_004_*`, `DEV3_016_*`, `DEV3_017_*`, `DEV3_022C_*`).
- [x] **Only intended files modified** — `git status --porcelain` = EXACTLY the 3 files in §3 (2 test files + 1 generated artifact). No refactoring of out-of-scope files ("while I'm here" = zero).
- [x] **No unledgered deferrals** — `deferred-items.md` untouched; nothing new deferred (the `gql/graphql.ts` no-op codegen is the contracted pre-4.1 state, not a deferral; D1–D3 remain 📅 Forward, J1 remains ✅ Done).

### IV — Instruction Verification (re-read post-edit)

- **`.agents/instructions/backend.instructions.md`** (re-read in full): §"GraphQL Schema Generation" — "After modifying Pothos schema files, run `bun run generate:gqlSchema`; after modifying documents or schema, also run `bun codegen`; both steps required for consistent frontend-backend type integration" → both executed in this task, in the same change set as 3.1's registrations ✓; no source-layer rule touched by 3.2 (no new backend source files) ✓.
- **`backend/graphql/AGENTS.md`** (re-read in full): SDL-freeze protocol honored — "Run `bun run generate:gqlSchema` then `bun codegen` after registering … so the frontend codegen output stays in sync" ✓; the recitation surface follows the layer's single-canonical-object + id-for-Apollo-normalization + closed-input rules that the artifact-side pins now enforce permanently ✓; no enum/scalar re-registration (DateTime-once pinned) ✓.
- **`.agents/instructions/tests.instructions.md`** (test-layer mapping for `**/*.test.ts`): bun:test only, no `any`, no `console.*`, no disable comments, pins updated in the same change as the schema change (stale-pin rule) ✓; suites run via the mandated `test/scripts/run-test.ts` runner, never raw `bun test` ✓.

## 5. Carry-forward for task 4.1 (shared documents + contract test) and 5.1 (wire suite)

1. **Codegen state:** `frontend/graphql/generated/schema.graphql` now carries the recitation surface and is committed-ready; `frontend/graphql/generated/gql/graphql.ts` is currently UNCHANGED because codegen emits per-document operation types. After 4.1 adds the two shared documents, its `bun run codegen` will emit (expected names, matching the codegen `typed-document-node` + `typescript-operations` naming): `SessionRecitationQuery`/`SessionRecitationQueryDocument` + `SessionRecitationQueryVariables` (`variables: { sessionId: string }` — the ONLY variable; zero identity variables), and `SetSessionRecitationMutation`/`SetSessionRecitationMutationDocument` + `SetSessionRecitationMutationVariables` (`variables: { input: { name: string; description?: string | null }, sessionId: string }` — `SessionRecitationInput` maps to `{ description?: string | null; name: string }` under `avoidOptionals`).
2. **Document contract (REQ-074):** select `id` FIRST on `SessionRecitation` (Apollo normalization — the object carries `id: ID!` per the new artifact pins); the query's variable surface is exactly `sessionId`; the mutation's is exactly `sessionId` + `input` (field-by-field mapping happens resolver-side; documents never carry identity variables).
3. **4.1 wiring:** `scheduling/index.ts` gains `export * from "./recitation.documents";` (0.2 anchor m); document file at `frontend/graphql/sharedDocuments/scheduling/recitation.documents.ts` — do NOT touch the Qira'ah `auth/recitation.documents.ts` sibling (D-0.2-3).
4. **5.1 wire matrix:** the behavioral scope pins (anonymous → `UNAUTHORIZED` on BOTH ops; authenticated non-teacher on the mutation → `FORBIDDEN` pre-resolver) graduate from 3.1's ephemeral check; the artifact-side and built-side shape pins now live permanently in these two suites and must stay green through 5.x.
5. **Reconciliation note for reviewers:** the `RECONCILED_*` inventory block in `schema-surface.test.ts` (§2) is a one-time re-anchor of upstream surfaces — future tickets must keep BOTH mirror files' inventories in lockstep (the "single sanctioned growth history" the two headers promise).

## 6. Cross-file dependencies

- `backend/graphql/gqlSchema` (built schema) → byte-source of `frontend/graphql/generated/schema.graphql` (`scripts/generator/generate-gql-schema.ts`, `lexicographicSortSchema` + `printSchema`); both suites consume either the builder (schema-surface) or the artifact (sdl-static).
- 3.1's committed resolvers/pothos (`recitation.pothos.ts`, `recitation.mutation.ts`, `recitation.query.ts`, both classes barrels) → the ONLY registration path that made the surface appear in the built schema; no source file was touched by 3.2.
- `scripts/generator/generate-gql-schema.ts` + `codegen.ts` → the two codegen scripts (`package.json` `generate:gqlSchema` / `codegen`); `codegen.ts` documents glob `frontend/graphql/sharedDocuments/**/*.ts` for 4.1's documents.
- `backend/lib/gateway/public-operations.ts` → verified byte-identical (SEC); closed allowlist unchanged.
- Downstream: 4.1 (documents + codegen operation types), 5.1 (wire matrix), orchestrator (single commit folding 3.1 resolvers + 3.2 artifacts/test pins per REQ-064).
