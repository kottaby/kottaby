# Task 4.1 Outcome — Shared documents + barrels + contract test (zero UI)

**Plan directory (verbatim):** `ai/plans/sprint_1/dev3-007-recitation-record-per-session-11`
**Task:** 4.1 — create the session-recitation shared GraphQL documents (`sessionRecitationQueryDocument` + `setSessionRecitationMutationDocument` as `TypedDocumentNode`s, `id` selected FIRST), extend the barrel chain, add the naming/typing contract test at the established colocated location, and re-run codegen so the operation types emit.
**Branch:** `feat/dev3-007-recitation-record-per-session-11` — re-verified/re-checked-out before EVERY command (the sandbox supervisor resets HEAD to `main` between commands, per the recorded 3.1 §6 / 3.2 gotcha; all working-tree changes survived the flips).
**No commits made; `tasks.md` / `worklog.md` / `deferred-items.md` untouched** (orchestrator-owned — no checkbox toggled by this subagent).
**Requirements:** REQ-065 (shared documents, no view), REQ-033 (zero identity variables = wire-level BOPLA hygiene), REQ-043 (no error-map/dispatcher row; no idempotency key).

---

## 1. What shipped

### `frontend/graphql/sharedDocuments/scheduling/recitation.documents.ts` (CREATE)

- `sessionRecitationQueryDocument: TypedDocumentNode<SessionRecitationQuery, SessionRecitationQueryVariables>` — `query SessionRecitation($sessionId: ID!) { sessionRecitation(sessionId: $sessionId) { id sessionId name description createdAt updatedAt } }`; payload selections carry `id` FIRST; the nullable payload (`SessionRecitation | null` — the foreign/nonexistent/no-record null collapse) is consumed as-is.
- `setSessionRecitationMutationDocument: TypedDocumentNode<SetSessionRecitationMutation, SetSessionRecitationMutationVariables>` — `mutation SetSessionRecitation($sessionId: ID!, $input: SessionRecitationInput!) { setSessionRecitation(sessionId: $sessionId, input: $input) { id sessionId name description createdAt updatedAt } }`; `id` FIRST; non-null payload.
- ZERO identity variables on both operations (variable surfaces are EXACTLY `{sessionId}` and `{sessionId, input}`) — caller identity is never wire-visible; ownership/governance resolve server-side.
- Declaration style mirrors the scheduling siblings byte-for-byte (`session-reads.documents.ts` / `session-lifecycle.documents.ts`): `import { gql, type TypedDocumentNode } from "@apollo/client"`, generated types imported from `@/frontend/graphql/generated/gql/graphql` (`useTypeImports`), PascalCase operation names, multi-line `TypedDocumentNode<...>` parameter formatting, shared field-shape header docblock. The Qira'ah catalog sibling `auth/recitation.documents.ts` is byte-untouched (D-0.2-3).

### `frontend/graphql/sharedDocuments/scheduling/index.ts` (UPDATE)

- Added `export * from "./recitation.documents";` BEFORE the existing `export * from "./session.documents";` — canonical (alphabetical) order per the layer's own barrels (`admin/`: admin-users < audit-trail < platform-analytics < teacher-certification; `notifications/`: broadcast < notification).
- Docblock extended to describe the session-recitation re-export alongside the lifecycle/dispute ones.

### `frontend/graphql/sharedDocuments/index.ts` (VERIFY — NO change)

- Top-level barrel line 6 already reads `export * from "./scheduling";` (0.2 anchor m), so the AGENTS.md barrel step 3 ("ensure the sub-directory is exported from the top-level `index.ts`") is already satisfied — no edit required. Recorded here as the dispatched verify-and-record ruling; `git status` confirms the file is untouched.

### `frontend/graphql/sharedDocuments/scheduling/recitation.documents.test.ts` (CREATE — contract test)

- Placed at the established sharedDocuments contract-test location pattern: colocated `<module>.documents.test.ts` beside the documents (5 existing precedents: admin-users / audit-trail / platform-analytics / teacher-certification / parent-link / broadcast / notification; the scheduling directory had none yet). Mirrors `parents/parent-link.documents.test.ts` discipline exactly (AST kind-guard narrowing, no unsafe assertions, no server/DB/network).
- Pins: single named operations (`SessionRecitation` / `SetSessionRecitation`) on the right channels; exact variable surfaces `["sessionId"]` / `["sessionId", "input"]`; every declared variable threaded into its root-field argument; belt-and-braces zero-identity assertion (no `user`/`teacher`/`student`/`owner` token in any variable); exact six-field canonical row with `id` FIRST (`selections[0].name.value === "id"` asserted via exact-order equality + index-0 pin); no ownership-lane field selected (BOPLA surface hygiene); `description` (the sole nullable field) selected; barrel ≡ deep-import instance identity; `TypedDocumentNode` compile-time binding against the codegen output; `sessionRecitation` is the ONLY nullable payload (null-collapse compile-time proof by assignment; the mutation's non-null side enforced at the `bun tsgo` gate).

### `frontend/graphql/generated/gql/graphql.ts` (REGENERATED — codegen output, +26/−0)

- `bun run codegen` emitted the recitation operation types exactly as the 3.2 carry-forward predicted: `SessionRecitationQuery` (+ compact `SessionRecitationQuery_sessionRecitation`), `SessionRecitationQueryVariables = Exact<{ sessionId: string | number }>`, `SessionRecitationDocument`; `SetSessionRecitationMutation`, `SetSessionRecitationMutationVariables = Exact<{ sessionId: string | number; input: SessionRecitationInput }>`, `SetSessionRecitationDocument`. Purely additive (+26/−0, verified via `git diff --numstat`); no pre-existing type changed.

### MUST-NOT-TOUCH surfaces — all confirmed untouched

- `frontend/providers/apollo/apolloCache.ts` — `git diff` EMPTY (frozen policy surface; `SessionRecitation` is id-bearing → default normalization, no `keyFields: false` registration).
- Error dispatcher / mutation error maps — no new row (REQ-043); no dispatcher file in the diff.
- `frontend/graphql/generated/schema.graphql` — byte-identical after `generate:gqlSchema` (22113 bytes, not in `git status`); no view/page/component/nav file anywhere in the diff (full `git status --porcelain` = exactly the 4 files above).

## 2. N/A ruling — UI `.BF`/`.BS` agent-browser loops

**Explicitly NOT generated, per plan.md §5.4 scope ruling + tasks.md Phase 4 scope note:** this ticket ships ZERO UI (REQ-065; non-goal 3). There is no page, component, nav entry, or rendered surface — therefore no visual-state matrix, no breakpoints (1440/768/375), no RTL states, and no `.BF`/`.BS` agent-browser loops apply. The only frontend artifacts are the documents/barrels/contract test above (plan.md §5.4 "the ONLY frontend changes" table, honored row-for-row).

## 3. Verification evidence (commands + exit codes)

### QL — `bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates` (per file, never bundled)

| # | File | Exit | Notes |
|---|---|---|---|
| QL-1 | `frontend/graphql/sharedDocuments/scheduling/recitation.documents.ts` | **0** | tsgo → oxlint → biome:check → lint:type-aware → check:duplicates, all green |
| QL-2 | `frontend/graphql/sharedDocuments/scheduling/index.ts` | **0** | same chain, all green |
| QL-3 | `frontend/graphql/sharedDocuments/scheduling/recitation.documents.test.ts` | **0** | same chain, all green; `check:duplicates` reports the file outside the jscpd scan scope (`**/*.test.ts` ignore) — skipped by the tool's own scoping |
| — | `frontend/graphql/sharedDocuments/index.ts` | n/a | file NOT modified (verify-and-record ruling, §1); generated artifacts excluded from biome scope by `biome.json` (`!frontend/graphql/generated`), per the tool's own scoping |

### Codegen (step E) — runs + determinism

| Step | Command | Exit | Result |
|---|---|---|---|
| G1 | `bun run generate:gqlSchema` | 0 | `schema.graphql` rewritten at 22113 bytes, **zero diff vs committed** (not in `git status`) — determinism carries over from 3.2 |
| C1 | `bun run codegen` | 0 | `gql/graphql.ts` +26/−0 — the recitation operation types emitted (§1) |
| C2 | `bun run codegen` (2nd run) | 0 | sha256 of `gql/graphql.ts` AND `schema.graphql` both **OK (zero diff)** — `sha256sum -c` green |

### TE — mandated runner (`bun run test/scripts/run-test.ts`)

| Suite | Result | Exit |
|---|---|---|
| `frontend/graphql/sharedDocuments/scheduling/recitation.documents.test.ts` | **10 pass / 0 fail / 83 expect()** | 0 |
| `frontend/graphql/sharedDocuments/documents.contract.test.ts` (regression) | **20 pass / 0 fail / 124 expect()** | 0 |
| `frontend/graphql/sharedDocuments/parents/parent-link.documents.test.ts` (regression) | **15 pass / 0 fail / 112 expect()** | 0 |
| `bun tsgo` (full repo) | 0 errors | 0 |

### SEC — Security & Tenancy Audit (explicit verdicts)

- **NO identity variables:** grep over `recitation.documents.ts` for `userId|teacherId|studentId|ownerId|actorId` → **zero hits**; the contract test additionally pins the whole declared-variable set to `["input", "sessionId", "sessionId"]` with per-token identity-token denial. The generated `…Variables` types confirm the wire surface: query = `{ sessionId }` only, mutation = `{ sessionId, input }` only (REQ-033 wire hygiene).
- **No selection of fields absent from `SessionRecitation`:** the payload is the exact six-field canonical row equal to the SDL block (`createdAt: DateTime!`, `description: String`, `id: ID!`, `name: String!`, `sessionId: ID!`, `updatedAt: DateTime!` — `schema.graphql:747-754`); exact-order equality in the test closes the selection set, and the codegen typing binds the document to the emitted operation type (a stray field would fail both).
- **apolloCache policy surface untouched:** `git diff frontend/providers/apollo/apolloCache.ts` EMPTY; no `keyFields` registration needed (id-bearing entity).
- **No error-map/dispatcher row:** no dispatcher/error-map file in `git status` (REQ-043 — the `RECITATION_ALREADY_EXISTS` conflict is adopted locally by future consumer forms).
- **No public-surface drift:** neither op appears in `backend/lib/gateway/public-operations.ts` (file untouched; both are authScopes-gated per 3.1).

### SR — Semantic Review (every dispatch item)

- [x] **No duplicate operation names** — `query SessionRecitation` occurs in exactly 1 document file; `mutation SetSessionRecitation` likewise; no other sharedDocuments file declares either name.
- [x] **No unused document exports** — exactly TWO exports (the dispatched pair), both consumed by the contract test, the barrel chain, and future consumers; no extra documents declared.
- [x] **Barrels canonical order** — `scheduling/index.ts`: `./recitation.documents` before `./session.documents` (alphabetical, matching admin/notifications precedent); top-level barrel already re-exported `./scheduling` (no edit).
- [x] **NO view/page/component code created anywhere** — `git status --porcelain` = exactly the 4 files (§1); no `frontend/views/**`, no `app/**`, no component file.
- [x] **Id-first selections** — `id` is the FIRST payload selection on BOTH operations (test-pinned via exact-order equality + `fieldNames(selection)[0] === "id"`).
- [x] **Exact variable surfaces** — `["sessionId"]` / `["sessionId", "input"]`, asserted on declarations AND root-field argument wiring (no dead variables, no literal arguments).
- [x] **Clean comments (zero plan artifacts)** — grep over all three authored files: zero `REQ-\d` / `Task \d` / `Phase \d` / `.ai/plans` / `tasks.md` / `specs.md` / `plan.md` hits; comments describe domain behavior only (ticket tag `DEV3-007` appears once in each header, mirroring the layer's established per-ticket convention in `session.documents.ts` / hub docblocks).
- [x] **No `console.*`** — zero hits (grep).
- [x] **No dead branches** — documents are pure declarations; the test is table-driven linear assertions + fail-fast guards, mirroring the parent-link precedent.
- [x] **No cross-layer imports** — documents import only `@apollo/client` + `@/frontend/graphql/generated/gql/graphql`; the test adds `bun:test`, `graphql` (types) and in-layer imports; no `@/backend`/`@/shared`/`@/app` (grep clean).
- [x] **Only task files modified** — `git status --porcelain` = the 3 authored/updated task files + the codegen artifact `frontend/graphql/generated/gql/graphql.ts` (task E: generated files ride this task's commit). No "while I'm here" refactoring.
- [x] **No unledgered deferrals** — `deferred-items.md` untouched; nothing deferred (D1–D3 remain 📅 Forward; J1 remains ✅ Done).

### IV — Instruction Verification (re-read post-edit)

- **`.agents/instructions/frontend.instructions.md`** (re-read in full): Apollo & GraphQL section — `gql` + `TypedDocumentNode` imported from `@apollo/client` ✓; document naming `{entityName}QueryDocument` / `{entityName}MutationDocument` with the exact `TypedDocumentNode<{Pascal}Query, {Pascal}QueryVariables>` convention ✓; generated types from the single `graphql.ts`, never inline literals, no mapping layers ✓; `id` field on all object selections ✓; codegen both steps run ✓; no `console.*` ✓; no `oxlint-disable` ✓.
- **`frontend/graphql/AGENTS.md`** (re-read in full): TypedDocumentNode Convention table satisfied row-for-row ✓; "Import Pattern" (never `@apollo/client/core`) ✓; `id` Field Requirement ✓; "new document file" barrel steps 1–4 executed (file in matching sub-directory; sub-directory `index.ts` line; top-level already exports the sub-directory; codegen run) ✓; step 5 N/A (no new sub-directory).
- **`frontend/graphql/sharedDocuments/AGENTS.md`** (discovered + re-read): layout/barrel pattern, consumer import conventions, codegen command, `id` requirement — all honored ✓.
- **`.agents/instructions/tests.instructions.md`** (test-file mapping): `bun:test` only, no `any`, no `console.*`, no disable comments, pure suite run via the mandated `test/scripts/run-test.ts` runner ✓.
- **`.jscpd.json` scoping** (auto-discovered by sub-loop): `**/*.test.ts` ignored — the colocated test's helper mirror of the parent-link discipline is outside the duplicates scan, matching every existing documents test.

## 4. Files (complete list)

| # | File | Action | Diff |
|---|---|---|---|
| 1 | `frontend/graphql/sharedDocuments/scheduling/recitation.documents.ts` | CREATE | +78 |
| 2 | `frontend/graphql/sharedDocuments/scheduling/index.ts` | UPDATE | +9/−6 (docblock + export line) |
| 3 | `frontend/graphql/sharedDocuments/scheduling/recitation.documents.test.ts` | CREATE | +261 |
| 4 | `frontend/graphql/generated/gql/graphql.ts` | REGENERATED (`bun run codegen`) | +26/−0 |

Files NOT modified (and why): `frontend/graphql/sharedDocuments/index.ts` (top-level barrel already re-exports `./scheduling` — verify-and-record ruling); `frontend/graphql/generated/schema.graphql` (`generate:gqlSchema` zero-diff); `frontend/providers/apollo/apolloCache.ts` (MUST NOT touch — confirmed empty diff); `frontend/graphql/sharedDocuments/scheduling/session.documents.ts` hub (the dispatch routes the export through the domain barrel, not the Session-shape hub; hub siblings keep their single field-shape contract); Qira'ah sibling `frontend/graphql/sharedDocuments/auth/recitation.documents.ts` (D-0.2-3 byte-untouched); any view/page/nav file (zero UI per non-goal 3).

## 5. Carry-forward for task 5.1 (wire matrix) and later consumers

1. **Consumer imports:** `import { sessionRecitationQueryDocument, setSessionRecitationMutationDocument } from "@/frontend/graphql/sharedDocuments";` (barrel, preferred) or the deep path `@/frontend/graphql/sharedDocuments/scheduling/recitation.documents` — both resolve to the SAME instance (cache-key safety, test-pinned). Hooks come from `@apollo/client/react`; no view exists yet (DEV3-006/DEV2-014/DEV1-016/DEV3-021 own future surfaces).
2. **Wire expectations for 5.1:** the documents' wire shape is now frozen by the contract test — query variable surface `{sessionId}`; mutation `{sessionId, input}`; payload = the six-field row with `id` first; read payload nullable (foreign ≡ nonexistent ≡ no-record → `null`), write payload non-null. Smuggle probes (`userId`/`teacherId` on input/args) are expected to die as `GRAPHQL_VALIDATION_FAILED` pre-resolver; the documents structurally cannot smuggle identity (test-pinned).
3. **Codegen naming:** future consumers use `SessionRecitationQuery`(+`Variables`) / `SetSessionRecitationMutation`(+`Variables`) and the compact extracted type `SessionRecitationQuery_sessionRecitation` (codegen emits `SessionRecitationDocument`/`SetSessionRecitationDocument` in `graphql.ts`; the shared `*QueryDocument`/`*MutationDocument` constants remain the only sanctioned import surface per the documents AGENTS).
4. **Freeze discipline:** any future selection change to these documents must update `recitation.documents.test.ts` rows (exact six-field row, `id` FIRST, variable surface) in the SAME change — stale-pin rule; `apolloCache.ts` stays frozen unless a future id-less type joins the surface.
