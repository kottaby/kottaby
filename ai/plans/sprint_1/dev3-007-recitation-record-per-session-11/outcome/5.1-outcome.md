# Task 5.1 Outcome — Wire GraphQL matrix over the live HTTP stack

**Plan directory (verbatim):** `ai/plans/sprint_1/dev3-007-recitation-record-per-session-11`
**Task:** 5.1 — CREATE `backend/graphql/test/recitation-record.wire.test.ts`: the live-HTTP wire matrix for the two session-recitation root fields, covering the full REQ-072 tier list over the real gateway pipeline (HTTP → gateway → scope-auth → resolver → RecitationRecordService → PostgreSQL → finalizer → back).
**Branch:** `feat/dev3-007-recitation-record-per-session-11` — re-verified via `git branch --show-current` before EVERY command (the sandbox supervisor flips the checkout back to `main` between commands, per the recorded gotcha in 0-baseline §1 / 3.1 §6; the untracked test file and plan artifacts survived every flip).
**No commits made; `tasks.md` / `worklog.md` / `deferred-items.md` untouched** (orchestrator-owned per dispatch constraints — no checkbox toggled).
**Requirements:** REQ-030, REQ-032, REQ-033, REQ-043, REQ-052, REQ-053, REQ-063, REQ-072.
**Execution mode:** completion-and-verification pass — a prior agent authored the 871-line file but timed out before verification. This task audited the file in full, ran every gate, and shipped it with **ZERO fixes required** (audit findings below).

---

## 1. What shipped

`backend/graphql/test/recitation-record.wire.test.ts` (CREATE, 871 lines, **25 tests / 202 expect() calls**) — the consolidated wire-tier suite for `sessionRecitation` (nullable read) + `setSessionRecitation` (non-null write), per the established live-HTTP harness conventions of `backend/graphql/test/session-lifecycle-mutations.test.ts` and `backend/graphql/test/parent-link.wire.test.ts`:

- `setupTestServerLifecycle()` + `TEST_PORT` from `@/test/helpers` (spawns/attaches the dev server on 3066); raw `fetch` for every probe (byte-shape fidelity — same convention the parent-link wire test uses where body equality matters; `testClient` would destroy raw bodies through Apollo normalization).
- Real committed fixtures: `buildSessionJourneyCast` (real `users.role` + role-child rows via `@/test/workflows/helpers`, one committing `db.transaction` in `beforeAll`) + two sessions booked OVER THE WIRE (`createSession` with per-run `x-idempotency-key`s, one started / one left scheduled). Identity rides real `signAccessToken` tokens verified by the live server — nothing monkey-patched.
- Expected denial copy resolved through `getServerTranslations("en"|"ar").errorsTranslations` keys (never hardcoded strings); `x-request-id` correlation honored by the gateway's `resolveRequestId` (same convention as `error-contract-matrix.test.ts:556`).
- Runtime-guarded assertion helpers (no casts): `recordOf`/`soleErrorItemOf`/`errorCodeOf`/`errorMessageOf`/`requestIdOf`/`expectDenialCode` (code + data-channel-by-nullability + correlated requestId + no-stacktrace), `expectCanonicalRecitationRow` (exact six-key row + ISO-8601 instants), `expectWireRowMatchesOracle` (field-by-field wire ≡ service oracle).

### Coverage tier → test mapping (REQ-072 list, 25 tests)

| # | Mandated tier (tasks.md 5.1) | Tests (describe — verbatim prefixes) |
|---|---|---|
| 0 | Public-operations allowlist | `wire matrix — public-operations allowlist hygiene` (1 test): `isPublicOperation` exact-match refusals for both ops + case variants |
| 1 | Anonymous → UNAUTHORIZED on BOTH ops | `wire matrix — anonymous tier (credential-less caller × 2 ops)` (3 tests): mutation `data: null` + query field-nulled + constant-shape pairing across ops; `path` asserted per item |
| 2 | Role matrix on the mutation | `wire matrix — wrong-role tier` (4 tests): student / parent / admin → FORBIDDEN pre-resolver; foreign teacher → SESSION_NOT_FOUND via the service collapse (see audit note A-1) |
| 3 | Write-denial oracle collapse | `wire matrix — write-denial tenancy collapse` (1 test): foreign vs nonexistent SESSION_NOT_FOUND bodies **byte-identical** (`JSON.stringify` equality under one pinned correlation id) |
| 4 | BOPLA smuggle probes | `wire matrix — BOPLA smuggle probes` (1 test × 6 probes): `userId`/`sessionOwnerId`/`teacherId` as root args AND as input fields → GRAPHQL_VALIDATION_FAILED with the `data` key ABSENT (request never executed) |
| 5 | Malformed sessionId wire shapes | `wire matrix — malformed sessionId tier` (2 tests): `"0" / "-1" / "1.5" / "abc" / "12abc"` → VALIDATION pre-DB on the write (never a masked 500); the same corpus collapses to the identical `null` on the read (no error channel) |
| 6 | Read-side null collapse | `wire matrix — nullable collapse tier (read, pre-record)` (3 tests): owner pre-record `null`; foreign teacher + parent + nonexistent id → same `null`; foreign ≡ nonexistent **byte-identical null bodies** (no errors key anywhere — the "no extensions.code" proof by body equality) |
| 7 | Happy-path wire ≡ service oracle | `wire matrix — happy path` (1 test): exact six-key row, digit-string ids, ISO-8601 `DateTime` instants, field-by-field equality against `RecitationRecordService.getSessionRecitation` oracle, exactly one row persisted (count delta) |
| 8 | Repeat-write replay (REQ-043) | `wire matrix — repeat-write replay` (1 test): replay WITHOUT any idempotency header → RECITATION_ALREADY_EXISTS (+ `tEn.recitationAlreadyExists`), stored row byte-identical to the pre-replay snapshot, count stays 1 |
| 9 | State window | `wire matrix — never-happened session` (1 test): scheduled target → RECITATION_SESSION_NOT_WRITEABLE (+ key copy), zero rows written |
| 10 | Participant read-back | `wire matrix — participant read-back (post-record)` (1 test): student participant reads the recorded row — wire ≡ oracle again |
| 11 | Localization proofs | `wire matrix — locale negotiation` (2 tests): en + ar copies for both conflicts via `accept-language`, with explicit en ≠ ar divergence assertions |
| 12 | Boundary masking (REQ-053) | `wire matrix — boundary masking` (3 tests): int4-overflow forcing id (positive-safe per the shape guard, unrepresentable by the column) → masked localized INTERNAL_SERVER_ERROR with the correlated `requestId` echoed, no stacktrace / no driver wording / no payload content, in en AND ar, plus rollback purity (row snapshot untouched) |
| 13 | Credential hygiene (SEC) | `wire matrix — credential hygiene` (1 test): denial + success bodies never echo bearer-token material |

## 2. Audit of the prior agent's file (this task's completion pass)

Full read of all 871 lines + cross-check against tasks.md 5.1, specs REQ-030/032/033/043/052/053/063/072, plan.md §3.5/§3.6, and the two harness precedents. **Verdict: contract-compliant as found — zero defects, zero edits applied.** Findings worth recording:

- **A-1 (spec reconciliation, not a defect):** tasks.md 5.1's coverage bullet compresses the role matrix to "student / parent / foreign teacher → FORBIDDEN pre-resolver". The authoritative requirements say otherwise: REQ-030/REQ-063 deny only **non-teacher** roles pre-resolver, and plan.md §3.6 pins "Teacher, foreign session → SESSION_NOT_FOUND (≡ nonexistent)". A foreign teacher passes the `$all { authenticated, role: [Teacher] }` scope by design and is denied by the service's oracle-safe collapse. The file implements (and its header documents) the specs' behavior, matching the service tier (2.2/2.3) and journey steps 5/6 byte-identity pairings. Kept as-is — "fixing" it to FORBIDDEN would contradict REQ-030/032 and destroy the collapse oracle.
- **A-2:** Message-string assertions appear ONLY as exact equality against `getServerTranslations(...).errorsTranslations` keys (never hardcoded literals) — the sanctioned pattern of the sibling suites; every tier's primary assertion targets `extensions.code` / payload shape / body byte-equality.
- **A-3:** Harness state uses `let` module bindings (cast, tokens, session ids, row snapshot) — the exact fixture-state idiom of `session-lifecycle-mutations.test.ts`/`parent-link.wire.test.ts`; bounded, per-run (`journeyPrefix("recwire")`), no unbounded collections. No module-level mutable Maps/Sets.
- **A-4:** Convention mirrors verified line-by-line: `tokenFor` = `signAccessToken({ userId, role })`; booking variables `{ teacherId, intent: "Hifz" }` + `x-idempotency-key` header; cast member token shape `cast.<member>.userId` / `cast.<member>.user.role`; `wireStatusNameOf` returns the SDL enum name ("Started" — matches the SDL `enum SessionStatus` print and the lifecycle wire expectations); `db.$count` usage matches the service suite.
- **A-5:** `x-request-id` correlation is honored server-side (`resolveRequestId` in `app/api/graphql/route.ts`; precedent `error-contract-matrix.test.ts:556`) — the byte-equality collapse proofs rely on it and passed.
- **A-6:** Clean comments: grep `REQ-\d|DEV3-\d|Phase \d|Task \d|plan\.md|tasks\.md|specs\.md|\.ai/plans|§` over the file → **zero hits**; comments describe domain behavior only. No `console.*`/`logger`, no `any`, no `rejects.toThrow()`, no dynamic imports, no oxlint-disable.

## 3. Verification evidence (commands + exit codes)

**DB posture:** PostgreSQL 17.11 at 127.0.0.1:5432 — `pg_isready` → "accepting connections", exit 0 (binaries via `LD_LIBRARY_PATH=/tmp/pg-root/...`, cluster `/home/z/pgdata`); `.env` + `.env.test` re-verified present before runs.

| Gate | Command | Exit code | Result |
|---|---|---|---|
| QL | `bun run scripts/health/sub-loop.ts backend/graphql/test/recitation-record.wire.test.ts --lifecycle duplicates` | **0** | tsgo (project-wide, file-filtered) → oxlint → biome:check → lint:type-aware all pass; check:duplicates skipped (test file outside jscpd scan scope — tool's own scoping, same as every sibling suite) |
| TE run 1 | `bun run test/scripts/run-test.ts backend/graphql/test/recitation-record.wire.test.ts` | **0** | **25 pass / 0 fail / 202 expect()** — 8.70 s, all tiers green over the live stack |
| TE run 2 (teardown-idempotency proof) | same command, consecutive | **0** | **25 pass / 0 fail / 202 expect()** — 8.10 s |
| Residue probe | read-only `recitation` count `WHERE name LIKE 'Wire record recwire_%'` after both runs | — | **0 rows** — committed-fixture teardown totality (temp probe file deleted after the check; `git status` back to only the one untracked file) |
| Differential spot-check (5.2 pre-confirmation) | `git diff --stat main...HEAD -- backend/lib/gateway/public-operations.ts backend/db/schema/ frontend/providers/apollo/apolloCache.ts` | 0 | **empty output** — zero drift on the frozen surfaces |

Notable run evidence: the boundary-masking tier's server-side log shows the masked `[ERROR] Unhandled non-domain error masked at GraphQL boundary` with the pinned correlation ids (`wire-recitation-mask-en`/`-ar`) — the response-side assertions (masked localized copy, correlated requestId, zero leak) all passed; the [DOMAIN]/[ERROR] lines are the shipped finalizer's own logging, unchanged platform behavior.

## 4. SEC — Security & Tenancy Audit (explicit verdicts)

- **Oracle-collapse byte-equality is IN the suite (asserted, not eyeballed):** write denial `expect(JSON.stringify(foreignBody)).toBe(JSON.stringify(missingBody))` under one correlation id; read null-collapse identical form. Both green.
- **BOPLA smuggle surface closed on the wire:** 6 probes (3 identity tokens × root-arg and input-field positions) all die GRAPHQL_VALIDATION_FAILED pre-execution.
- **Token hygiene:** the dedicated credential-hygiene tier proves no bearer-token echo in any response body; error items carry only `code`/`message`/`requestId`-shaped extensions with no stacktrace.
- **No PII in the suite's own artifacts:** payloads are static test constants (`RECORD_NAME`/`RECORD_DESCRIPTION` per-run prefixed); no logging in the test file; the only server logs exercised are the shipped finalizer's masked entries.
- **public-operations allowlist unchanged:** behavioral exact-match refusals asserted in-suite + `git diff` empty (3.1/3.2 outcomes recorded the same; re-verified this task).

## 5. SR — Semantic Review (every dispatch item)

- [x] **Assertions target `extensions.code`/payload shapes** — every tier's primary assertion is the code, the data-channel shape (`null` / field-null / key-absent / row), or whole-body byte equality; message equalities are locale-key-resolved only (A-2).
- [x] **Fixture cleanup complete** — recitation rows hard-deleted first (`inArray` over tracked session ids), then `registry.cleanup()` FK-safe teardown of sessions + cast in one committed transaction; residue probe 0 rows; second consecutive green run proves idempotency.
- [x] **No dead branches** — every guard is reachable (`if (!oracle) throw` fires on oracle absence; `trackedSessionIds.length > 0` covers the beforeAll-failure path; `tokenProbe.length === 0` guards the hygiene probe), all exercised by the green run.
- [x] **No module-level mutable state** — only bounded fixture `let`s per the sibling idiom (A-3); no Maps/Sets/arrays mutated at module scope.
- [x] **No cross-layer imports** — test layer → `@/backend/*`, `@/shared/locale/*`, `@/test/helpers`, `@/test/workflows/helpers` only; no frontend/app imports.
- [x] **Clean comments** — zero plan-artifact references (A-6); no trivial comments.
- [x] **No `console.*`** — grep zero hits; the suite asserts absence/leak via DB oracles and body probes, not prints.
- [x] **Only the one test file modified** — `git status --porcelain` = `?? backend/graphql/test/recitation-record.wire.test.ts` throughout; temp residue-probe file created and deleted within the task.
- [x] **No unledgered deferrals** — nothing deferred by this task; `deferred-items.md` untouched (D1–D3 remain 📅 Forward, J1 remains ✅ Done).

## 6. IV — Instruction Verification

- **`.agents/instructions/backend.instructions.md`**: test-only file — no production layer touched; expected copy via `getServerTranslations` keys (zero hardcoded user-facing strings); `import type { RecitationReturnType } from "@/backend/types"`; DomainError `extensions.code` contract asserted; no `console.*`; mandated runner used ✓.
- **`.agents/instructions/tests.instructions.md`**: bun:test only; no `any`; no `console.*`; unique per-run labels/keys via `journeyPrefix`; committed (non-rollback) fixtures + `afterAll` hard-deletes + residue probe; mandated runner `bun run test/scripts/run-test.ts` (the instructions' `scripts/run-test/run-test.ts` path does not exist in this checkout — recorded by 1.2/2.1/2.2/2.3 already). Documented layer exception, same as the journeys (2.2 §7): `getServerTranslations` is called directly at module scope — the i18n-loader-hang caution targets the rollback tier; the journey/sibling precedent uses it, the task mandates key-resolved expected copy, and the suite is green twice in <10 s ✓.
- **`backend/graphql/AGENTS.md`**: participant-scoped-operations rule asserted behaviorally (foreign ≡ nonexistent indistinguishable on both ops; `{ authenticated: true }` query scope vs `$all` mutation scope exercised); gateway default-deny public-operations rule asserted; DomainError/masking boundary contract asserted; the Qira'ah `recitationReadings` sibling domain is not conflated (only `sessionRecitation`/`setSessionRecitation` probed) ✓.

## 7. Fixture inventory (per run; every row hard-deleted in `afterAll`)

| Table | Rows | Source |
|---|---|---|
| `users` | 7 | `buildSessionJourneyCast` (owner teacher, second teacher, primary student, second student, applicant, parent, admin) |
| `students` | 2 | cast role-children (primary funded `trial: 1, hifz: 1` — exactly two net bookings) |
| `teacher` | 2 | both certified |
| `applicants` / `parents` / `admin` | 1 / 1 / 1 | cast role-children |
| `session` | 2 | booked OVER THE WIRE (`createSession`, keys `${PREFIX}-key-started` / `${PREFIX}-key-scheduled`); one started, one left scheduled; tracked in the registry |
| `session_request_idempotency` | 2 | the two wire bookings' claims |
| `recitation` | 1 (transient) | happy-path write; hard-deleted first in `afterAll` (then cascades with sessions) |

Teardown order: recitation hard-delete over tracked sessions → `registry.cleanup()` (child-tables-first, one committed tx) → residue probe 0 rows.

## 8. Files created / modified / NOT modified

| File | Action |
|---|---|
| `backend/graphql/test/recitation-record.wire.test.ts` | CREATE (prior agent) — audited + verified, 0 fixes |
| `ai/plans/.../outcome/5.1-outcome.md` | CREATE (this file) |
| everything else | NOT modified — `git status --porcelain` shows only the untracked test file; no source, schema, allowlist, dispatcher, locale, or frontend file touched; `tasks.md`/`worklog.md`/`deferred-items.md` orchestrator-owned |

## 9. Carry-forward for 5.2 (full integration gate)

1. **Wire suite is green and stable** — include `backend/graphql/test/recitation-record.wire.test.ts` in the ordered 5.2 sweep (`repo suite, service suite, journey suite, wire suite, schema-surface, SDL assertions, documents-contract, types static+conformance, i18n parity`); expect 25 pass / 202 expect() per run and ~9 s runtime; needs PostgreSQL up + the live-server harness (port 3066; first run may spawn the dev server — allow the 600 s timeout).
2. **Differential checklist pre-verified here** (5.2 re-runs formally): `public-operations.ts`, `backend/db/schema/**`, `apolloCache.ts` all byte-identical vs `main` in this branch's committed delta.
3. **Frozen companion suites must stay green alongside** — `schema-surface.test.ts` (41 pass) and `sdl-static-assertions.test.ts` (39 pass) pin the surface this matrix exercises; any future surface change must update both mirrors AND this matrix in the same change.
4. **Byte-equality tests are correlation-dependent** — the collapse proofs pin the same `x-request-id` on both paired requests (honored by `resolveRequestId`); do not "simplify" them to independent request ids or the byte-equality guarantee dissolves.
5. **The foreign-teacher cell is SESSION_NOT_FOUND by spec** (audit note A-1) — reviewers reading tasks.md's compressed bullet should re-check REQ-030/063 + plan §3.6 before "correcting" the suite.
6. **tasks.md checkbox 5.1 remains `[ ]`** — orchestrator-owned per this plan's dispatch constraints (all prior tasks followed the same practice; the outcome file is the completion evidence).

## 10. Cross-file dependencies

- `backend/graphql/gqlSchema` + classes mutation/query barrels → the live schema the harness boots (3.1 registration path).
- `@/test/helpers` (`setupTestServerLifecycle`, `TEST_PORT`) + `@/test/workflows/helpers` (`buildSessionJourneyCast`, `createSessionFixtureRegistry`, `journeyPrefix`) — pre-existing harness; reused byte-untouched.
- `RecitationRecordService` (2.3) — the read oracle; `signAccessToken` (auth) — token minting; `isPublicOperation` (gateway) — allowlist probe.
- `shared/locale/{en,ar}/errors` (1.2) — expected denial copy; `app/api/graphql/route.ts` `resolveRequestId` — correlation echo.
- Companion freeze suites: `schema-surface.test.ts`, `sdl-static-assertions.test.ts`; sibling wire harnesses: `session-lifecycle-mutations.test.ts`, `parent-link.wire.test.ts`, `error-contract-matrix.test.ts` (conventions mirrored).
