# Task 0.2 Outcome — Prerequisite & Anchor Verification (verify-then-claim gate)

**Plan directory (verbatim):** `ai/plans/sprint_1/dev3-007-recitation-record-per-session-11`
**Task:** 0.2 — verify every plan anchor against the bundled code, record `path:line` evidence, re-scope deviations NOW (before Phase 2).
**Mode:** READ-ONLY on source (this outcome file is the only file written). `0-baseline-outcome.md` NOT touched (does not exist yet at verification time; only `plan-review-R1.md` was present in `outcome/`).
**DB context:** postgres 127.0.0.1:5432, DBs `kottaby`/`kottaby_test`, `.env`/`.env.test` written, schema pushed (per worklog Task 0).

---

## 1. Verdict Table

| # | Anchor | Verdict | Evidence (`path:line`) |
|---|---|---|---|
| a | `recitation.ts` schema contract | **CONFIRMED** | `backend/db/schema/classes/recitation.ts:19-21` (`sessionId` `.notNull().references(() => session.id, { onDelete: "cascade" })`), `:22` (`name varchar(255).notNull()`), `:23` (`description` text, nullable), `:24-28` (`createdAt` defaultNow / `updatedAt` defaultNow + `.$onUpdate`), `:30` (`unique("recitation_session_id_unique").on(t.sessionId)` + `index("recitation_session_id_idx")`) |
| b | `recitation.types.ts` exists w/ 2 types + pre-wired barrels | **DEVIATION D-0.2-1** — file DOES NOT EXIST | repo-wide grep for `RecitationSelectType|RecitationInsertType` hits ONLY plan artifacts (`ai/plans/.../*.md`); `backend/types/classes/index.ts:1-4` re-exports report/session/session-notification/session-request-idempotency — NO recitation line; `backend/types/index.ts:29` DOES re-export `./classes` (top-level barrel OK, transitively) |
| c1 | `isPositiveSafeSessionId` + `assertPositiveSafeSessionId` | **CONFIRMED** (sig noted) | `backend/services/classes/session-lifecycle.guards.ts:111-113` (`(id: unknown): boolean`), `:123-130` (`(id: unknown, t: errorsTranslations): void` — throws `ValidationError(t.validation)`) |
| c2 | `assertActorGovernanceClean` | **CONFIRMED** (sig noted) | `backend/services/classes/session-lifecycle.governance.ts:39-53` — `(actorUserId: number, t: errorsTranslations, tx?: DBTransaction): Promise<void>`; denials: absent/deleted/blocked/suspended → `ForbiddenError(t.forbidden)` + one `logDomainError`; NOTE: takes the **translations object `t`**, not `locale` |
| c3 | `isUniqueViolation` via `@/backend/services/shared` | **CONFIRMED** | `backend/services/shared/user-provisioning.helpers.ts:54-69` (cause-chain traversal, cycle-safe `seen` set; PG `code === "23505"` + SQLite message fallback); re-exported by `backend/services/shared/index.ts:2` (`export * from "./user-provisioning.helpers"`) → `import { isUniqueViolation } from "@/backend/services/shared"` resolves (tsconfig `@/*` → `./*` at `tsconfig.json:26-27`) |
| c4 | `withTransaction` shared helper | **CONFIRMED** (location corrected) | **Canonical:** `backend/lib/db/with-transaction.ts:27-35` — `withTransaction<T>(outerTx: DBTransaction \| undefined, fn: (tx: DBTransaction) => Promise<T>): Promise<T>`; SAVEPOINT on outerTx, top-level `db.transaction` otherwise. Import: `@/backend/lib/db/with-transaction` (as `session-lifecycle.service.ts:84`). A duplicate exists at `backend/services/notifications/notification-engine.helpers.ts:18-26` — DO NOT import that one (notifications domain; endangers the REQ-018 static pin) |
| c5 | `constraintNameOf` in `backend/db/test/test-utils.ts` | **CONFIRMED** (stale plan line ref) | `backend/db/test/test-utils.ts:111-126` (plan said `:34-49` — that range is `runInRollback:34-59`; `expectRepoError` is `:77-96`). Reads `.constraint` direct or one `.cause` level deep |
| d1 | `SessionRepository.findById` dual-executor mirror | **CONFIRMED** (stale plan line ref) | public delegation `backend/db/repo/classes/session.repository.ts:119-121`; **actual dual-executor branch** `backend/db/repo/classes/session.repository.helpers.ts:167-178` (Drizzle `tx.select()` under tx `:168-171`, `queryDb` parameterized cold path `:172-177`). Plan's `session.repository.ts:24-26` is header comment. Same pattern with `DBQueryExecutor` + `isDBTransaction` narrowing: `backend/db/repo/teachers/teacher.repository.ts:52-75` |
| d2 | `TeacherRepository.insertColdStartCertified` raw-23505 mirror | **CONFIRMED** (stale plan line ref) | `backend/db/repo/teachers/teacher.repository.ts:135-153` (raw-23505-propagates-untranslated contract documented `:130`; plan said `:40-57` — that range is the `isDBTransaction` guard) |
| d3 | `SessionLifecycleService.getSessionById` null-collapse mirror | **CONFIRMED** (stale plan line ref) | `backend/services/classes/session-lifecycle.service.ts:583-605` — malformed id → `null` pre-DB `:593-595` (`isPositiveSafeSessionId`), miss → `null` `:597-600`, non-participant → `null` `:601-603`; never throws. Plan's `:178-194` is the `startSession` docblock |
| d4 | session Pothos `t.expose*` + `DateTime` mirror | **CONFIRMED** (stale plan line ref) | `backend/graphql/pothos/classes/session.pothos.ts` — `SessionPothosObject` `:134-199`; `t.exposeID("id")` FIRST `:138`; `t.exposeID` participant ids `:141-142`; `t.exposeString` `:170,190-191,193`; `t.expose("…", { type: "DateTime" })` `:179-183,192,194`; non-nullable `createdAt`/`updatedAt` DateTime `:196-197`. DateTime scalar registered ONCE: `backend/graphql/pothos/shared/scalar.pothos.ts:30` (`gqlSchemaBuilder.addScalarType("DateTime", DateTimeResolver, {})`) |
| d5 | `sessionById` query scope/collapse mirror | **CONFIRMED** (stale plan line ref) | `backend/graphql/query/classes/session-lifecycle.query.ts:152-188` — `nullable: true` `:159`, `authScopes: { authenticated: true }` `:167-169`, `Number(args.id)` coercion + service delegation `:185`; TS-narrowing `UnauthorizedError` guard pattern `:177-179`. Plan's `:43-60` is header text |
| e | `test/workflows/helpers/`, `test/workflows/AGENTS.md`, `test/workflows/sessions/` | **CONFIRMED** — all exist, NO scaffolding deviation for task 2.2 | `ls` evidence: `test/workflows/helpers/` (12 files incl. pure `export *` barrel `index.ts:22-29`), `test/workflows/AGENTS.md`, `test/workflows/sessions/` (`session-lifecycle.journey.test.ts`, `session-lifecycle-denials.journey.test.ts`) |
| f | Error class constructor shapes | **CONFIRMED** — plan assumption HOLDS; no DomainError fallback needed | All in `backend/lib/errors.ts`: `DomainError(code, message, options?)` `:19-29` (extensions.code = code verbatim); `NotFoundError(entity, message, options?)` `:37-41` → code `${entity.toUpperCase()}_NOT_FOUND` (so `NotFoundError("SESSION", …)` ⇒ `SESSION_NOT_FOUND`); `ForbiddenError(message)` `:51-55` ⇒ `FORBIDDEN`; `ValidationError` overloaded `:65-130` (`(message)` ⇒ VALIDATION; `(message, fields)`; `(code, message)`; `(code, message, options, fields)`; `fields` mirrored into `extensions.fields` `:148-156`); **`ConflictError` overloaded `:171-183`** — `(message)` ⇒ `CONFLICT`, `(code, message, options?)` ⇒ arbitrary custom code — `ConflictError("RECITATION_ALREADY_EXISTS", …)` works as planned (precedents `SESSION_INVALID_TRANSITION`, `DUPLICATE_REQUEST` cited in docblock `:159-170`) |
| g | `getServerTranslations(locale)` + `errorsTranslations` + registration | **CONFIRMED** | `shared/locale/server-graphql.ts:3-5` — `getServerTranslations(locale: string)` ONE arg, full tree; `errorsTranslations` wired in `shared/locale/en/messages.ts:22` + `shared/locale/ar/messages.ts:22`; en tree `shared/locale/en/errors/index.ts:3-82` (flat top-level keys incl. `sessionNotFound:72`; NOTE pre-existing nested groups `planCatalog:22` / `adminUsers:37` are legacy — new keys go FLAT); `ErrorsLabels` interface `shared/locale/types/errors/labels.ts:21+`; registration checklist `shared/AGENTS.md:234-238` + namespace contract `shared/AGENTS.md:262-264`; consumption-site parity scanner `shared/locale/errors-namespace.parity.test.ts:48-55` (parses `errorsTranslations.<key>` usages — new keys must be consumed via property access); `ctx.t` resolver pattern: `await ctx.t("errorsTranslations")` — `backend/graphql/mutation/parents/parent-link.mutation.ts:135`, documented `backend/graphql/AGENTS.md:15` |
| h | `DomainError` + `logger.logDomainError` bounded ctx | **CONFIRMED** | `DomainError` class `backend/lib/errors.ts:19-29`; `logger.logDomainError(message: string, ctx?: DomainErrorContext): void` `backend/lib/logger.ts:92-100`; `DomainErrorContext` `:40-46` = `{ code?, entity?, entityId?: string \| number, locale?, [key: string]: unknown }` — **`locale` is a first-class optional key** (REQ-035's `{ code, entity, entityId, locale }` fits natively; note existing governance helper calls pass `{code, entity, entityId}` only) |
| i | `db push` zero-diff gate | **CONFIRMED** — empty diff (verbatim output §3) | `DATABASE_URL=…kottaby bunx drizzle-kit push --force --config=drizzle.config.ts` → `[i] No changes detected`, exit 0 |
| j | `ApiFieldErrorType` shape | **CONFIRMED** | `backend/types/errors/api-error.types.ts:55-62` — `{ field: string; code: string; message: string }` (all readonly; `field` = RHF-consumable path, `code` = SCREAMING_SNAKE field code, `message` = localized at producer). Reachable via `@/backend/types` (`backend/types/index.ts:32` re-exports `./errors`) |
| k | `projectMutationFieldErrors` | **CONFIRMED** (traceability only) | `frontend/lib/mutationFieldErrors.ts` exists; NO frontend edit in this plan |
| l | repo classes barrel convention | **CONFIRMED** — task 2.1 MUST update it | `backend/db/repo/classes/index.ts:13-14` re-exports each `*.repository.ts` (`export * from "./session.repository"`, `"./session-request-idempotency.repository"`); top-level `backend/db/repo/index.ts:20` re-exports `./classes` (no top-level edit needed) |
| m | sharedDocuments scheduling + top barrels | **CONFIRMED** | `frontend/graphql/sharedDocuments/scheduling/` exists incl. `index.ts`; top-level `frontend/graphql/sharedDocuments/index.ts:6` re-exports `./scheduling`. NOTE: `scheduling/index.ts:11` currently re-exports ONLY `./session.documents`, which is itself a re-export HUB (`session.documents.ts:34-36` aggregates the thematic siblings) — task 4.1 adds one `export * from "./recitation.documents";` line to `scheduling/index.ts` |
| n | graphql layer barrels | **CONFIRMED (mutation/query) + DEVIATION D-0.2-2 (pothos)** | `backend/graphql/mutation/classes/index.ts:14` — side-effect import convention (`import "./x.mutation";`); `backend/graphql/query/classes/index.ts:12` — same. **`backend/graphql/pothos/classes/index.ts` DOES NOT EXIST** (dir holds only `create-session-input.pothos.ts`, `session.pothos.ts`, `session-filter-input.pothos.ts`); pothos domain barrels exist only for `admin/` + `notifications/` (`backend/graphql/pothos/index.ts:15`, `backend/graphql/pothos/admin/index.ts:9-11` whose header states "Domain Pothos files are imported directly by the query/mutation files that consume them") |
| o | No pre-existing recitation surface files | **CONFIRMED (for all planned paths) + DEVIATION D-0.2-3 (name siblings exist)** | glob `**/recitation.{service,repository,mutation,query,pothos,documents}.ts` → hits ONLY `backend/graphql/query/recitation.query.ts` (public Qira'ah catalog `recitationReadings`, registered `backend/graphql/query/index.ts:25`, guardrail "does NOT touch the recitation table" `:13-14`) and `frontend/graphql/sharedDocuments/auth/recitation.documents.ts` (Qira'ah catalog document, guardrail `:15-16`). NONE of the seven planned CREATE paths exists. Also note `backend/services/shared/recitation-catalog.service.ts` (Qira'ah catalog service — different domain, do not confuse) |
| p | Journey helper exports available | **CONFIRMED** — see §4 for the concrete list | `test/workflows/helpers/*`; consumer precedent `backend/graphql/test/session-lifecycle-mutations.test.ts:50-55` imports `buildSessionJourneyCast`, `createSessionFixtureRegistry`, `journeyPrefix`, `type SessionJourneyCast` from `@/test/workflows/helpers` |
| q | Service-test translation assertion pattern | **CONFIRMED** | `backend/services/classes/session-lifecycle.service.test.ts:109` (`import { getServerTranslations } from "@/shared/locale/server-graphql"`); file-local helper `t()` `:127-130` = `getServerTranslations("en").errorsTranslations`; `expectDomainDenial(error, code, message)` `:146-151` asserts `instanceof DomainError` + exact `extensions.code` (via `rejectionCode` `:137-139`) + exact translated message; usage e.g. `:395` `expectDomainDenial(error, "INSUFFICIENT_BALANCE", t().insufficientBalance)` |

---

## 2. Deviations & Re-Scoping Decisions (recorded NOW)

### D-0.2-1 — `backend/types/classes/recitation.types.ts` does not exist (task 1.1 is a CREATE, not an UPDATE; one barrel line needed)
- **What the plan claimed:** the file exists at `recitation.types.ts:1-4` with only `RecitationSelectType` + `RecitationInsertType`, and BOTH barrels already re-export it ("no barrel edit needed") (specs.md:60, plan.md:126-133, tasks.md:39).
- **Reality:** no such file anywhere (grep across repo hits only plan prose). `backend/types/classes/index.ts` has NO recitation line. `backend/types/index.ts:29` re-exports `./classes`, so the top-level barrel is genuinely edit-free.
- **Decision (binding for task 1.1):**
  1. `backend/types/classes/recitation.types.ts` is a **CREATE** carrying the full four-member shape (`RecitationSelectType`, `RecitationInsertType`, `RecitationReturnType`, `interface SessionRecitationSubmitInput`) — the two existing export lines are written as part of the create, byte-identical to the plan's "EXISTS — untouched" bodies (`typeof recitation.$inferSelect` / `$inferInsert`).
  2. `backend/types/classes/index.ts` gains ONE line: `export * from "./recitation.types";` (append per the existing ordering). This is the ONLY barrel edit in Phase 1.
  3. Sibling shape reference for style: `backend/types/classes/report.types.ts:1-3`; mirror + conformance references `backend/types/classes/session.types.static-assertions.test.ts` and `session.types.conformance.test-d.ts` both exist as claimed.
- **Impact:** REQ-003 wording ("EXTENDS") is satisfied semantically (end state identical); the 1.1 static-assertion test should pin the barrel line so the "no barrel edit needed" claim becomes true post-1.1.

### D-0.2-2 — `backend/graphql/pothos/classes/index.ts` does not exist (task 3.1 must NOT "update" it)
- **What the plan claimed:** task 3.1 lists `backend/graphql/pothos/classes/index.ts (UPDATE barrel)`.
- **Reality:** no such file; the classes domain (like `users`/`teachers`/`auth`) registers its Pothos objects **transitively through the resolvers that import them** (`backend/graphql/pothos/index.ts:10-13` documents exactly this). Named re-export barrels exist only where objects ship ahead of resolvers (`admin/`, `notifications/`).
- **Decision (binding for task 3.1):** NO pothos barrel is created or updated. `recitation.mutation.ts` and `recitation.query.ts` import `SessionRecitationPothosObject` / `SessionRecitationInput` directly from `@/backend/graphql/pothos/classes/recitation.pothos` (same as `session-lifecycle.query.ts:70` imports `session.pothos` directly). Registration happens via the mutation/query side-effect barrel chain (`mutation/classes/index.ts` → `mutation/index.ts` → `gqlSchema.ts`).

### D-0.2-3 — Name-sibling `recitation.*` files already exist in adjacent (Qira'ah) domain
- **What the plan claimed (anchor o):** "Confirm no pre-existing recitation.service.ts … anywhere (must be CREATEs)."
- **Reality:** the exact planned paths are all free (verdict CONFIRMED for every CREATE), but two name-siblings exist in the UNRELATED Qira'ah-catalog domain: `backend/graphql/query/recitation.query.ts` (public `recitationReadings` catalog field) and `frontend/graphql/sharedDocuments/auth/recitation.documents.ts` (catalog document). Both carry explicit guardrails that they never touch the `recitation` table. `backend/services/shared/recitation-catalog.service.ts` is the same-domain neighbor.
- **Decision (binding for tasks 2.1–4.1):** the planned creates proceed at their planned paths (`query/classes/recitation.query.ts`, `sharedDocuments/scheduling/recitation.documents.ts`, etc.). Implementers must NOT merge/extend the Qira'ah-catalog files (non-goal 6, plan.md:94; specs non-goal 6). The two files stay byte-untouched; SDL/ surface tests may note the coexistence (`recitationReadings` vs `sessionRecitation`) to prevent reviewer confusion. No re-scoping of any task required.

### Stale plan line references (corrections, no re-scoping)
The plan-review-R1 fix #2 corrected some refs but the following plan/tasks citations remain stale (patterns all exist; use the verified lines above):
- `session.repository.ts:24-26` → actual dual-executor branch at `session.repository.helpers.ts:167-178` (delegation at `session.repository.ts:119-121`).
- `teacher.repository.ts:40-57` → actual `insertColdStartCertified` at `135-153`.
- `session-lifecycle.service.ts:178-194` → actual `getSessionById` collapse at `583-605`.
- `session-lifecycle.query.ts:43-60` → actual `sessionById` registration at `152-188`.
- `session.pothos.ts:53-55` / `:76-79` → actual exposes at `138-142` / DateTime at `179-197`.
- `test-utils.ts:34-49` → `constraintNameOf` at `111-126` (`34-59` is `runInRollback`).
- `guards.ts:111-135` → confirmed correct (`isPositiveSafeSessionId:111-113`, `assertPositiveSafeSessionId:123-130`); `governance.ts:39-62` → `assertActorGovernanceClean:39-53` (admin variant `63-85`).

No further deviations: ConflictError custom codes work as assumed (no DomainError fallback needed); journey scaffolding exists (no duties added to task 2.2).

---

## 3. db push zero-diff evidence (verbatim, REQ-010 pin)

Command (workdir `/home/z/my-project`, no `source .env`):
```
DATABASE_URL=postgresql://postgres@127.0.0.1:5432/kottaby bunx drizzle-kit push --force --config=drizzle.config.ts
```
Output (exit code 0):
```
Reading config file '/home/z/my-project/drizzle.config.ts'
Reading schema files:
/home/z/my-project/backend/db/schema/index.ts

Using 'pg' driver for database querying
[⣷] Pulling schema from database...
[✓] Pulling schema from database...
[i] No changes detected
```
**Ruling:** schema is currently consistent → ANY post-ticket `db push` diff MUST be empty (Phase 2.M and 5.2 gates; REQ-010).

---

## 4. Implementation-critical knowledge for Phase 1–5 tasks

### Signatures (verbatim)
- `assertPositiveSafeSessionId(id: unknown, t: ReturnType<typeof getServerTranslations>["errorsTranslations"]): void` — throws `ValidationError(t.validation)`. `isPositiveSafeSessionId(id: unknown): boolean` (pure, no throw) — the read path's collapse guard.
- `assertActorGovernanceClean(actorUserId: number, t: errorsTranslations, tx?: DBTransaction): Promise<void>` — pass the translations object, NOT locale: `const t = getServerTranslations(locale).errorsTranslations; await assertActorGovernanceClean(teacherUserId, t, outerTx)`.
- `withTransaction<T>(outerTx: DBTransaction | undefined, fn: (tx: DBTransaction) => Promise<T>): Promise<T>` — import from **`@/backend/lib/db/with-transaction`** (NOT from `@/backend/services/shared`, NOT from notifications helpers).
- `isUniqueViolation(error: unknown): boolean` — import from `@/backend/services/shared` (barrel) or deep `user-provisioning.helpers`. Cause-chain only for PG codes; keep the 23505-only discipline in the service (no message-sniffing for the recitation path).
- `constraintNameOf(err: unknown): string` + `runInRollback(fn)` + `expectRepoError(fn)` — `@/backend/db/test/test-utils` (`backend/db/test/test-utils.ts`).
- Error constructors (`@/backend/lib/errors`): `new ConflictError("RECITATION_ALREADY_EXISTS", localized)` ✓; `new ConflictError("RECITATION_SESSION_NOT_WRITEABLE", localized)` ✓; `new NotFoundError("SESSION", localized)` ⇒ code `SESSION_NOT_FOUND` ✓; `new ForbiddenError(localized)`; `new ValidationError(localized)` or `new ValidationError(localized, fields)` where `fields: readonly ApiFieldErrorType[]` = `{field, code, message}` entries (e.g. `{ field: "name", code: "…", message: t().validation }`).
- `logger.logDomainError(msg, { code, entity, entityId, locale })` — all four keys native to `DomainErrorContext` (`entity` is free-form string; use `"recitation"` / `"session"` per REQ-035).
- Repo methods take `tx` LAST: `insertOnce(insert, tx?)`, `findBySessionId(sessionId, tx?)`; the plan's `DBQueryExecutor` param type for `findBySessionId` matches the `TeacherRepository.findById:66-75` precedent (Drizzle branch + `queryDb` cold path + `isDBTransaction` narrowing at `:52-54`).
- `SessionRepository.findById(id, tx?)` returns `SessionSelectType | null` with `teacherId`, `studentId`, `status` present (INV-S4) — participant/status predicates read the row, never args.

### Import paths (verified resolvable, `@/*` → repo root)
- `@/backend/lib/errors` (DomainError family + `translateDbError` + masking helpers)
- `@/backend/lib/logger` → `logger`
- `@/backend/lib/db/with-transaction` → `withTransaction`
- `@/backend/services/shared` → `isUniqueViolation` (⚠ also re-exports `recitation-catalog.service` — Qira'ah domain; import the symbol, not the file name, mentally)
- `@/backend/services/classes/session-lifecycle.guards` / `…governance`
- `@/backend/graphql/pothos/builder` → `gqlSchemaBuilder`; `@/backend/graphql/pothos/shared/scalar.pothos` registers `DateTime` (reference by NAME `type: "DateTime"` only)
- `@/backend/enum/users/user-role.enum` → `UserRole` (Teacher = "teacher", `:5-9`); `SessionStatus` members `Scheduled/Started/Completed/Cancelled/Disputed` (enum VALUE imports for status windows)
- `@/shared/locale/server-graphql` → `getServerTranslations(locale)`

### Test-tier knowledge
- Journey (2.2): helpers all exist — cast building `buildSessionJourneyCast` (`session-cast.ts:280`, returns `SessionJourneyCast` `:120`; options `:131`), per-run prefix `journeyPrefix` (`:314`), registry `createSessionFixtureRegistry` (`journey-fixture-registry.ts:122`, `SessionFixtureRegistry` `:104`) + zero-residue oracles `countNotificationsForUser:198`, `countAuditLogsForActor:203`, `countWalletsForTeacher:208`, `countTeacherTransactionsForTeacher:217` (write-purity oracles for journey steps 1/7), teardown `journeyCleanup(registry)` (`journey-cleanup.ts:62`), actor provisioning `provisionStudentActor/provisionCertifiedTeacherActor/provisionParentActor/provisionAdminActor` (`actor-context.ts:88/105/122/136`), notification spy `SpiedFanoutTransport` (`spied-transport.ts`), governance flip fixture `setGovernanceFixture` (`journey-fixtures.ts:179` — journey step 7), error catcher `catchJourneyError` (`:226`), student-parent link `linkStudentToParentFixture` (`:157`). Layer rules: `test/workflows/AGENTS.md` (no `runInRollback`; committed fixtures in ONE `db.transaction`; `TrackedFixtures` registry; real roles; spied dispatch; no `rejects.toThrow()`; translations via `getServerTranslations("en").errorsTranslations` — rule 6).
- Service test (2.3): mirror `session-lifecycle.service.test.ts` — `t()` helper (`:127-130`) + `expectDomainDenial(error, code, t().<key>)` (`:146-151`); `isPgliteProvider()` skip gate (`:110-112`).
- Wire (5.1): live-HTTP harness precedent `backend/graphql/test/session-lifecycle-mutations.test.ts` (`setupTestServerLifecycle` + `testClient` + `expectMutationError`, `:42-60`).
- SDL pins (3.2): `backend/graphql/test/schema-surface.test.ts` + `sdl-static-assertions.test.ts` both exist.
- i18n parity (1.2): add keys to `shared/locale/types/errors/labels.ts`, `shared/locale/en/errors/index.ts`, `shared/locale/ar/errors/index.ts` (flat, domain-prefixed); `errors-namespace.parity.test.ts` auto-scans consumption sites — the keys must appear as `t.<key>`/`errorsTranslations.<key>` property reads in consuming code or the parity suite's coverage rule fails; keys consumed by the service via `t.recitationAlreadyExists` / `t.recitationSessionNotWriteable`.

### Coordination note
`outcome/0-baseline-outcome.md` was NOT present when this file was written (task 0.1 may be running in parallel). This file intentionally contains no baseline counts and does not touch that file.
