# Task 2.3 Outcome — RecitationRecordService (write pipeline + collapse read) + four-tier service suite + J1 closure

**Plan directory (verbatim):** `ai/plans/sprint_1/dev3-007-recitation-record-per-session-11`
**Task:** 2.3 — CREATE `RecitationRecordService` (REQ-012 pipeline order IS the contract) + the REQ-071 four-tier suite + the services barrel line; run the deferred journey QL (ledger **J1**) and turn the test-first journey suite GREEN.
**Branch:** `feat/dev3-007-recitation-record-per-session-11` (re-verified via `git branch --show-current` before every command; PostgreSQL 17.11 at 127.0.0.1:5432 confirmed accepting connections before all DB runs).
**No commits made; `tasks.md` / `worklog.md` untouched** (orchestrator-owned per dispatch constraints — matching 1.1/1.2/2.1/2.2 practice). The ONLY plan-artifact edit is the authorized **J1 closure** in `deferred-items.md`.
**Requirements:** REQ-012, REQ-013, REQ-014, REQ-015, REQ-016, REQ-017, REQ-018, REQ-031, REQ-032, REQ-034, REQ-035, REQ-040, REQ-041, REQ-042, REQ-050, REQ-051, REQ-071; J1 closure (2.2 carry-forward).

---

## 1. What shipped

### `backend/services/classes/recitation.service.ts` (CREATE by the prior agent — audited, verdict: CONTRACT-COMPLIANT, zero fixes needed)

`export namespace RecitationRecordService`, 279 lines, audited line-by-line against plan.md §4.2 + the REQ set before running anything:

| Contract point (plan §4.2 / REQ) | Verdict | Evidence |
|---|---|---|
| Pipeline order: pre-DB guards → governance pre-tx → ONE `withTransaction` (findById → ownership → status → insert) → 23505-only catch | **COMPLIANT** | `isPositiveSafeSessionId` + `assertPositiveSafeSessionId` + payload normalization BEFORE any DB read; `assertActorGovernanceClean(teacherUserId, t, outerTx)` pre-tx; ONE `withTransaction(outerTx, async tx => …)` whose SAME `tx` reaches `SessionRepository.findById(sessionId, tx)` and `RecitationRepository.insertOnce(…, tx)` |
| Governance helper takes the translations OBJECT (0.2 anchor c2) | **COMPLIANT** | `const t = getServerTranslations(locale).errorsTranslations;` then `assertActorGovernanceClean(teacherUserId, t, outerTx)` |
| `outerTx` FINAL parameter (REQ-017) | **COMPLIANT** | `setSessionRecitation(teacherUserId, sessionId, input, locale, outerTx?)` |
| Foreign ≡ nonexistent byte-identical (REQ-032a) | **COMPLIANT** | `sessionRow?.teacherId !== teacherUserId` collapses miss AND non-owner into the single `NotFoundError("SESSION", t.sessionNotFound)` branch (one code path ⇒ byte-identical by construction) |
| Status window (REQ-012c, B.18) | **COMPLIANT** | `scheduled`/`cancelled` → `ConflictError("RECITATION_SESSION_NOT_WRITEABLE", t.recitationSessionNotWriteable)`; `started`/`completed`/`disputed` admitted — enum VALUE import `SessionStatus`, widened to string consts for the row's raw status-union comparison (the guards-module widening idiom; no string literals) |
| 23505 cause-chain ONLY (REQ-013/041) | **COMPLIANT** | `catch` narrows on `isUniqueViolation(error)` (`@/backend/services/shared`) → `ConflictError("RECITATION_ALREADY_EXISTS", t.recitationAlreadyExists)`; EVERYTHING else `throw error` untouched — no message-sniffing, no blanket masking |
| Read pipeline (REQ-014/032b) | **COMPLIANT** | malformed id → `null` pre-DB; miss OR non-participant (`teacherId===caller \|\| studentId===caller` from the DB ROW only) → same `null`; participant → `RecitationRepository.findBySessionId` row \| `null`; NO read path throws, NOTHING logged |
| Log hygiene (REQ-035) | **COMPLIANT** | exactly ONE `logger.logDomainError` per denial, ctx strictly `{ code, entity, entityId, locale }`; governance denials reuse the helper's own single log (no double-log); happy paths + collapse reads log NOTHING; payload content never logged |
| Purity (REQ-016/018) | **COMPLIANT** | zero imports of `NotificationEngine`/`AuditService` (grep + static pin); insert maps field-by-field `{ sessionId, name, description }` (no input spread, no identity passthrough) |
| `withTransaction` import origin (0.2 anchor c4) | **COMPLIANT** | `@/backend/lib/db/with-transaction` (NOT the notifications-domain duplicate) |
| No service-layer `.types.ts`, no local exported types, no `console.*`, no module-level mutable state, clean comments | **COMPLIANT** | grep-verifed (plan-artifact scan: zero hits) + pinned by the suite's source-pin tier |

### `backend/services/classes/recitation.service.test.ts` (CREATE by the prior agent — audited; FIXED: 4 tsgo errors, 10 oxlint warnings, 2 sonarjs errors, 5 runtime log-oracle bugs)

1050→1129 lines, four tiers per task 2.3.TE — ALL required coverage was already authored by the prior agent (no missing tests vs the dispatch's tier list: Tier 1 full denial matrix; Tier 2 name 0/1/255/256 + whitespace-only + unicode/RTL + description null/empty/whitespace/2000/2001 + `fields[]` projection + sessionId fuzz write/read; Tier 3 governance fuzz (deleted/blocked/suspended/absent, pre-tx proof via repo spies) + status fuzz + concurrent double-write race (`testOnRealPostgres`, real PostgreSQL — RAN, not skipped) + outerTx join arm (every `runInRollback` case passes `tx` as `outerTx` → SAVEPOINT) + wire-style `undefined` top-level committed block + rollback purity (transactional trigger raises P0001 → raw error untouched → zero residual rows → retry green); Tier 4 foreign≡nonexistent `denialShape` pairing + DB-row-only participant predicate + write-purity row-count/row-snapshot oracles over `notifications`/`audit_logs`/`teacher`/`wallet`/`teacher_transaction`/`users`/`students` lanes/`session` + `logDomainError` spy counts (one bounded entry per denial, zero on happy/collapse) + static source pins (no NotificationEngine/AuditService + import allowlist + no dynamic imports/console/process.env + no `.types.ts` + barrel-members pin). Fixes applied (all minimal, in-file):

1. **tsgo ×3 (`.fields` on `Error`)** — `expectRepoError` returns `Error`; added the file-local type-guard reader `validationFields(error)` (mirrors the file's own `rejectionCode` no-cast guard idiom) and switched the three `fields[]` assertions to it; `ApiFieldErrorType` added to the type imports.
2. **tsgo ×1 (`absentSessionId(db)`)** — helper param widened `DBTransaction` → `Pick<DBTransaction, "select">` (the EXACT solution the sibling repo suite `recitation.repository.test.ts:88` shipped), body renamed to `executor`.
3. **oxlint `prefer-set-has`** — `BOUNDED_LOG_CONTEXT_KEYS` → `ReadonlySet<string>` + `.has()` via a new `expectBoundedContextKeys` helper.
4. **oxlint `consistent-function-scoping`** — `installRepositorySpies` moved to file scope (it captures no describe-local state).
5. **oxlint `no-await-in-loop` ×6** — the four fuzz/boundary `for` loops restructured to `Promise.all` batches (mirroring the governance-fuzz pattern already in the file); the status-fuzz batch inserts the admissible rows first, then records/counts per session.
6. **oxlint `no-unsafe-enum-comparison`** — `overrides.status === SessionStatus.Started` → comparison against `STARTED_STATUS: string = SessionStatus.Started` (the service's own widening idiom; enum still the vocabulary source).
7. **oxlint `no-unnecessary-type-conversion`** — `String(args[0])` → `args[0]` (spy arg already `string`).
8. **sonarjs `prefer-regexp-exec` + `super-linear-regex`** — barrel-members scan → `.exec()`; exported-type pin regex `^\s*…/gm` → linear `^[ \t]*export (?:interface|type)\b/m` via `.exec()`.
9. **RUNTIME log-oracle bugs ×5** (would have failed TE even though they type-check): the prior agent asserted `expectSingleBoundedDenialLog(...)`/`expectZeroLogCalls()` over windows containing MULTIPLE denials (sessionId fuzz: 5 denials; payload test: 2; SESSION_NOT_FOUND test: 2; name boundary: 2 rejections then `expectZeroLogCalls()`; description boundary + participant-predicate tests: trailing `expectZeroLogCalls()` after a logged denial). Fixed with the batched form `expectBoundedDenialLogs(code, entity, count)` (one-bounded-log-PER-denial contract, aggregate count) + a `mockClear()` in the name-boundary test so the acceptance probes still prove the happy path logs zero.

### `backend/services/classes/index.ts` (UPDATE by the prior agent — verified: exactly +1 line)

`export * from "./recitation.service";` prepended (alphabetical). This is the line the journey's red error named (2.2 §9 item 1). `git diff` = 1 insertion, nothing else.

### `test/workflows/sessions/recitation-record.journey.test.ts` (created by 2.2 — FIXED: 2 tsgo errors; RED→GREEN proven)

1. `readGovernanceStates` selected nullable `users.isDeleted/isBlocked/suspended` (`boolean | null`) into a `boolean`-typed snapshot interface → coalesced `?? false` with a comment (NULL flag = never governed), keeping the order-stable sort.
2. Step 6 passed the `DenialShape | null` module var straight into `toEqual` → narrowed via a local + `expect(foreignShape).not.toBeNull()` setup-integrity guard before the byte-identity pairing.

## 2. Files modified / created / NOT modified

| File | Action | Notes |
|---|---|---|
| `backend/services/classes/recitation.service.ts` | CREATE (prior agent) — audited, 0 fixes | contract-compliant as found |
| `backend/services/classes/recitation.service.test.ts` | CREATE (prior agent) + 9 fix groups | see §1 |
| `backend/services/classes/index.ts` | MODIFY (prior agent) — verified +1 line | the J1-blocking barrel line |
| `test/workflows/sessions/recitation-record.journey.test.ts` | MODIFY (2 fixes) | J1 closure + red→green |
| `ai/plans/.../deferred-items.md` | MODIFY — J1 row → ✅ Done | the ONLY authorized ledger edit |
| `ai/plans/.../outcome/2.3-outcome.md` | CREATE | this file |
| `backend/db/repo/classes/recitation.repository.ts` + `__tests__/recitation.repository.test.ts` + repo barrel | NOT modified | task 2.1's shipped surface; re-verified green (§4 TE-4), untouched |
| `backend/db/schema/**` | NOT modified | zero schema work (REQ-010) |
| Qira'ah name-siblings (`backend/graphql/query/recitation.query.ts`, `backend/services/shared/recitation-catalog.service.ts`, …) | NOT modified | D-0.2-3 coexistence ruling |
| `test/workflows/admin/account-governance.journey.test.ts` | **NOT modified — PRE-EXISTING RED, out of scope** | see §5 |
| `tasks.md`, `worklog.md` | NOT modified | orchestrator-owned (dispatch constraint; no checkbox toggled) |

## 3. Verification evidence (commands + exit codes + pass counts)

**QL — `bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates`:**

| # | File | Exit code | Notes |
|---|---|---|---|
| QL-1 | `backend/services/classes/recitation.service.ts` | **0** | first run, no fixes needed |
| QL-2 | `backend/services/classes/recitation.service.test.ts` | 1 → 1 → **0** | run 1: 4 tsgo errors (fixed); run 2: 10 oxlint warnings (fixed); run 3: 2 sonarjs errors (fixed); run 4: all green |
| QL-3 | `backend/services/classes/index.ts` | **0** | first run |
| QL-4 | `test/workflows/sessions/recitation-record.journey.test.ts` | 1 → **0** | run 1: 2 tsgo errors (fixed); run 2: all green — **ledger J1 QL discharged** |

**TE — mandated runner `bun run test/scripts/run-test.ts <path>` (raw `bun test` never used):**

| # | Suite | Exit code | Result |
|---|---|---|---|
| TE-1 | `backend/services/classes/recitation.service.test.ts` | **0** | **22 pass / 0 fail / 357 expect()** — all four tiers; race arm RAN on real PostgreSQL; log-spy counts exact |
| TE-2 | `test/workflows/sessions/recitation-record.journey.test.ts` | **0** | **8 pass / 0 fail / 140 expect()** — RED at authoring (2.2 outcome §4: exit 1, `Export named 'RecitationRecordService' not found`) → GREEN after 2.3: the red→green evidence |
| TE-2b | same file, SECOND consecutive run | **0** | 8/0/140 again — idempotent-teardown proof (2.2 §9 duty) |
| TE-3 (regression) | `backend/services/classes/session-lifecycle.service.test.ts` | **0** | 55 pass / 0 fail / 731 expect() — sibling suite unaffected |
| TE-4 (repo, untouched-surface confirm) | `backend/db/repo/classes/__tests__/recitation.repository.test.ts` | **0** | 11 pass / 0 fail / 42 expect() |
| TE-5 (layer-wide, J1 step) | `test/workflows` | 1 | **162 pass / 1 fail** across 17 files — ALL 8 recitation journey steps passed; the single failure is the PRE-EXISTING admin-governance drift of §5 (not a DEV3-007 file) |

**DB posture:** `pg_isready` exit 0 before runs; no restart needed.

## 4. SEC — Security & Tenancy Audit (explicit verdicts)

- **BOLA/IDOR collapse on BOTH surfaces** — write: foreign teacher ≡ nonexistent id, byte-identical `SESSION_NOT_FOUND` (service test `denialShape` pairing + journey steps 5/6 cross-actor pairing); read: foreign ≡ nonexistent ≡ non-participant → the SAME `null` (service fuzz + journey step 4). Existence is never an oracle.
- **BOLA participant predicate from DB row only** — no identity parameter beyond `sessionId`/caller id exists on either signature; proven by the dedicated service test and the journey's parent/foreign probes.
- **BOPLA** — the service consumes the closed `SessionRecitationSubmitInput` whitelist (`name`, `description` only); insert maps field-by-field (no spread, no identity/sessionId passthrough from input); no resolver-independent widening here (resolver field-mapping is 3.1's duty — carry-forward §6).
- **BFLA governance re-check pre-tx** — `assertActorGovernanceClean` (shared helper, no twin) runs BEFORE the transaction opens; service test proves the session lookup + insert are never reached for deleted/blocked/suspended/absent callers.
- **Content-PII never logged** — log ctx = `{code, entity, entityId, locale}` only; tests assert bounded keys AND that the over-ceiling payload never appears in serialized log calls.
- **LIKE/ILIKE N/A** — grep over service + test: zero hits (parameterized equality only via the repo); the deliberate N/A is recorded here per REQ-034.

## 5. Semantic Review (SR) — every dispatch item, explicit

- [x] **Single `withTransaction`, SAME tx everywhere** — one `withTransaction(outerTx, …)` unit; `findById(sessionId, tx)` + `insertOnce(…, tx)` receive that unit's `tx`; no `db`/`tx` mixing inside the flow (rollback-purity + pre-tx spy probes prove it).
- [x] **23505 cause-chain only** — `isUniqueViolation` (cause-chain walker, cycle-safe) is the SOLE catch predicate; no message-sniffing, no Drizzle top-level reads.
- [x] **No blanket try/catch** — the single catch narrows to the unique-violation shape and rethrows everything else untouched (proven by the P0001 rollback-purity probe surfacing RAW).
- [x] **Canonical types** — `RecitationReturnType`, `SessionRecitationSubmitInput`, `ApiFieldErrorType`, `DBTransaction`, `DBQueryExecutor` all from `@/backend/types`; zero local re-declaration.
- [x] **Enum VALUE imports** — `SessionStatus` imported as a value; status comparisons use enum-member-derived widened consts (never bare string literals); the test suite imports the enum the same way.
- [x] **No service-layer `.types.ts`** — none exists (runtime `existsSync` pin + grep).
- [x] **No dead branches** — every branch exercised green: both read-executor arms (tx + cold/undefined), miss/foreign collapse, status denials + admissible set, unique-violation vs raw-failure catch arms, empty/over-ceiling/normal payload arms. The read path's `isTransactionExecutor` guard is an input-shape guard for the signature's legitimate `DBQueryExecutor` breadth (mirrors the file-private repo-layer guards — no shared export exists to reuse; grep-verified).
- [x] **No module-level mutable state** — service holds only immutable consts; test mutable state is the describe-scoped spy/fixture pattern of the sibling suites.
- [x] **No cross-layer imports** — backend + `@/shared/locale` only (pinned by the import-allowlist source pin).
- [x] **Clean comments, zero plan artifacts** — grep `REQ-\d|DEV3-\d|Phase \d|Task \d|plan\.md|tasks\.md|specs\.md` over service + test: zero hits; comments are domain behavior only.
- [x] **No `console.*`** — zero (the single grep hit is the source-pin test's own NAME string).
- [x] **Only intended files modified** — `git status --porcelain` = the four DEV3-007 Phase-2 files (2.1's repo trio + 2.3's service trio/journey) + ledger + outcome files; `git diff --stat` = 2 barrel lines (+1 repo, +1 services).
- [x] **No unledgered deferrals except J1 closure** — J1 closed (§3); no new deferrals created. The §5 pre-existing admin-journey failure is a baseline finding (not a deferral of this task) — documented here for the orchestrator/2.M.

### Pre-existing out-of-scope red (finding, NOT fixed — scope-boundary rule)

`test/workflows` layer-wide run surfaces 1 failure in `test/workflows/admin/account-governance.journey.test.ts` (DEV3-017, commit `b01d21d`): step 9e asserts `tAuth.accountBlocked` ("Your account has been suspended or blocked. …" — `shared/locale/en/auth/index.ts:43`) on `AdminUserManagementService.setUserSuspended`'s denial, but the committed service throws the ERRORS-namespace copy (`accountBlocked` = "This account has been blocked." — `shared/locale/en/errors/index.ts:18`). Standalone, the file also fails step 8 on a `users.updatedAt` byte-identity oracle (timestamp drift), cascading into step 9. Both sides of the drift are COMMITTED and byte-unmodified in this working tree; the failing assertion involves only the admin user-management service + locale maps — no recitation import chain — so it predates and is independent of DEV3-007. Left untouched per "no refactoring of files outside the task scope"; flagged for the orchestrator (2.M's layer gate and Phase 5 will meet it).

## 6. Instruction Verification (IV)

- **`.agents/instructions/backend.instructions.md`** — service orchestrates repos (no business logic in repo) ✓; types from `@/backend/types` via alias ✓; i18n via `getServerTranslations` from `@/shared/locale/server-graphql`, zero hardcoded user-facing strings ✓; `logger`, never `console.*` ✓; highest-barrel imports (`@/backend/db/repo`, `@/backend/lib/errors`, `@/backend/lib/db/with-transaction`) ✓; no nested ternaries, no dynamic imports ✓; DomainError family only ✓.
- **`.agents/instructions/tests.instructions.md`** — `runInRollback` for every transactional case with `tx` handed to the service as `outerTx` ✓; production-path block uses COMMITTED fixtures + tracked FK-safe hard-deletes + residue probe (the documented committed-fixture exception, mirroring the sibling suite's chaos block) ✓; `expectRepoError`, NEVER `rejects.toThrow()` ✓; bun:test only, no `any`, no `console.*` ✓; mandated runner used (`test/scripts/run-test.ts` — the instructions' `scripts/run-test/run-test.ts` path does not exist in this checkout, as 2.1/2.2 already recorded) ✓; source-pin discipline: static pins updated in the same change as the source ✓. Note: `t()` calls `getServerTranslations` directly — the documented journey/sibling-suite layer exception (canonical `session-lifecycle.service.test.ts:109,127-130` does exactly this; suite runs green in <1s, no loader hang).
- **`backend/services/AGENTS.md`** — service-layer `.types.ts` prohibited → none ✓; per-domain service, no cross-domain service calls ✓; NotificationEngine is the sole notifications writer → recitation never imports it ✓; audit-emission rule N/A (zero audit rows is the locked contract, REQ-018) ✓; no real external-API calls in service tests ✓.
- **Guard/governance helper reuse (no twins)** — `assertPositiveSafeSessionId` / `isPositiveSafeSessionId` / `assertActorGovernanceClean` reused via import from `session-lifecycle.guards` / `…governance`; repo-wide grep confirms NO exported `isDBTransaction`/`isTransactionExecutor` exists to reuse (the repo-layer guards are file-private), so the read path's local type guard is not a twin.

## 7. Carry-forward for task 3.1 (resolver authors) and beyond

1. **Import paths (verified resolving):**
   - `import { RecitationRecordService } from "@/backend/services/classes";` (barrel — the journey's red named exactly this) or deep `@/backend/services/classes/recitation.service`.
   - Types from `@/backend/types`: `RecitationReturnType` (Pothos object ref payload), `SessionRecitationSubmitInput` (input ref source), `RecitationSelectType` (≡ ReturnType byte-identical, 1.1 derivation).
2. **Signatures (plan §4.2 verbatim — do not deviate):**
   - `RecitationRecordService.setSessionRecitation(teacherUserId: number, sessionId: number, input: SessionRecitationSubmitInput, locale: string, outerTx?: DBTransaction): Promise<RecitationReturnType>` — resolver wiring per REQ-060: `RecitationRecordService.setSessionRecitation(ctx.user.id, Number(args.sessionId), { name: args.input.name, description: args.input.description ?? null }, ctx.locale)` (field-by-field, absent description → explicit `null`, NEVER the raw input object, NEVER `undefined` description).
   - `RecitationRecordService.getSessionRecitation(callerUserId: number, sessionId: number, tx?: DBQueryExecutor): Promise<RecitationReturnType | null>` — nullable query field (`nullable: true` per Pothos rule); the service never throws on any read shape (malformed id collapses to `null` pre-DB), so the resolver needs no error mapping for the read.
3. **Surfaced codes + i18n keys (byte-exact, already pinned by service suite + journey):**
   - `VALIDATION` (`ValidationError`, `fields[]` = `{field:"name"|"description", code:"NAME_REQUIRED"|"NAME_TOO_LONG"|"DESCRIPTION_TOO_LONG", message:t.validation}`) — malformed `sessionId` OR payload; resolver should pass `ctx.locale` and let the transport project `extensions.fields`.
   - `FORBIDDEN` (`ForbiddenError(t.forbidden)`) — governed caller, pre-tx.
   - `SESSION_NOT_FOUND` (`NotFoundError("SESSION", t.sessionNotFound)`) — miss ≡ foreign ≡ non-participant write.
   - `RECITATION_SESSION_NOT_WRITEABLE` (`ConflictError(code, t.recitationSessionNotWriteable)`) — `scheduled`/`cancelled`.
   - `RECITATION_ALREADY_EXISTS` (`ConflictError(code, t.recitationAlreadyExists)`) — write-once arbiter; replay ≡ typed conflict (REQ-043, no idempotency key, no dispatcher row).
   - Keys live flat in `errorsTranslations` both locales (1.2); service resolves them from `locale` — resolvers need NO extra translation plumbing beyond propagating `ctx.locale`.
4. **Pothos wiring reminders (from 0.2/1.1):** `id` FIRST via `t.exposeID`, `sessionId` via `t.exposeID`, `name` non-null + `description` nullable via `t.exposeString`, timestamps via `t.expose("…", { type: "DateTime" })`; DO NOT create `backend/graphql/pothos/classes/index.ts` (D-0.2-2 — import the pothos module directly from the mutation/query files).
5. **Non-nullable mutation return is safe** — a resolved call ALWAYS returns the created row (every denial throws); `RecitationSelectType` carries `id`, so Apollo normalization applies with zero cache-surface work.
6. **2.M gate notes:** repo + service + journey suites green; `bun run db push` zero-diff expectation unchanged (no schema edits); layer-wide `test/workflows` currently carries the §5 pre-existing admin failure — route it before the 2.M layer-green claim.
7. **D1/D2/D3 unchanged** (forward items owned by future tickets); consumers MUST import-by-reference this service, never touch the table.

## 8. Cross-file dependencies

- `backend/types/classes/recitation.types.ts` (1.1) → service + tests (canonical four-member shape via `@/backend/types`).
- `shared/locale/{types/errors/labels.ts, en/errors/index.ts, ar/errors/index.ts}` (1.2) → service denials (`t.recitationAlreadyExists`, `t.recitationSessionNotWriteable`, plus reused `validation`/`forbidden`/`sessionNotFound`).
- `backend/services/classes/session-lifecycle.guards.ts` / `…governance.ts` → service (shared pre-DB + governance helpers; also used by the sibling lifecycle service).
- `backend/db/repo/classes/recitation.repository.ts` (2.1) → service (raw-23505 `insertOnce`, dual-executor `findBySessionId`); `backend/db/repo/classes/session.repository.ts` → service (ownership/status row source).
- `backend/lib/db/with-transaction.ts` → service (SAVEPOINT composition seam, REQ-017).
- `backend/db/schema/classes/recitation.ts` → everything (structural ground truth: `recitation_session_id_unique` arbiter; zero schema edits).
- `test/workflows/sessions/recitation-record.journey.test.ts` (2.2) ⇄ service barrel (J1 red→green now closed); `test/workflows/helpers/**` reused as-is.
- Downstream: task 3.1 (mutation/query resolvers), 4.1 (shared documents), and the future report/consumer tickets (D2/D3) depend on the two method signatures + five surfaced codes EXACTLY as shipped — renames cascade.
