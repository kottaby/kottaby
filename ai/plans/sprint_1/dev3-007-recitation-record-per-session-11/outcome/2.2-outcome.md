# Task 2.2 Outcome — Recitation cross-actor journey test (TEST-FIRST, red at authoring)

**Plan directory (verbatim):** `ai/plans/sprint_1/dev3-007-recitation-record-per-session-11`
**Task:** 2.2 — write `test/workflows/sessions/recitation-record.journey.test.ts` covering ALL 8 ordered cross-actor steps BEFORE the service surface exists (the import of the not-yet-existing `RecitationRecordService` is the intended red).
**Branch:** `feat/dev3-007-recitation-record-per-session-11` (re-verified before EVERY command — the sandbox supervision flips the checkout to `main` between commands; no commit made, per dispatch constraints).
**No commits made** (orchestrator owns commits; `tasks.md`/`worklog.md` untouched — checkbox toggling delegated to the orchestrator per dispatch constraints, matching the 1.1/1.2 practice).

---

## 1. What shipped

**CREATE** `test/workflows/sessions/recitation-record.journey.test.ts` (661 lines, 8 tests = the 8 ordered steps, 1:1 with the specs' cross-actor scenarios):

- Single file, single `describe`, strictly ordered tests sharing the committed cast and the shared started session.
- Committed fixtures: cast built in `beforeAll` inside ONE committing `db.transaction(...)` via `buildSessionJourneyCast` (real `users` rows + real role-child rows — nothing monkey-patched), then the shared session booked by the primary student (`SessionLifecycleService.createSession`, per-run idempotency key) and started by its owning teacher (`SessionLifecycleService.startSession`) — the honest production path; both created rows tracked immediately.
- Tracked hard-deletes in `afterAll`: defensive totality sweep (any residual `session`/`session_request_idempotency` row referencing fixture actors → tracked) → `registry.cleanup()` (FK-safe order inside one committed transaction) → post-teardown assertion that the recitation rows cascaded away with their sessions (zero residue).
- NO `runInRollback` anywhere; NO identity parameters beyond `sessionId` on any call; every service call receives the acting actor's real `users.id`.
- Notification dispatch boundary spied: bare passthrough `spyOn` over `NotificationEngine.emitForUser` / `emitForUsers` / `publishReceipts` (installed in `beforeAll`, re-armed per step in `beforeEach`, restored in `afterAll`) — zero recorded dispatches asserted on EVERY step, on top of the layer's primary absence instrument (row-count deltas through the registry counters, scoped to fixture ids).
- Denials captured with `catchJourneyError` and asserted via a local `expectDomainDenial` helper (exact `DomainError` class + exact `extensions.code` + exact `getServerTranslations("en").errorsTranslations` message, never `.rejects.toThrow()`).

## 2. Journey step → test name mapping (all 8 ordered steps)

| Step (specs §2.9) | Test name (verbatim) | Core assertions |
|---|---|---|
| 1 | `step 1 — owning teacher writes the record on a started session: exactly ONE row, zero effects anywhere` | Fixture premise (session `Started`, owner/participant ids); write returns the created row (`sessionId`, `name`, `description` verbatim, Date stamps); `count(recitation where session_id) === 1`; sibling purity (session row deep-equal, student lanes deep-equal); zero side-effect deltas + zero dispatches |
| 2 | `step 2 — student participant reads the record: name and description observed verbatim` | `getSessionRecitation(student)` → row; `name`/`description` verbatim; deep-equal to the stored row read straight from the table |
| 3 | `step 3 — owner repeat write: RECITATION_ALREADY_EXISTS; the original row stays byte-identical` | Repeat write with a DIFFERENT payload → `ConflictError` + code `RECITATION_ALREADY_EXISTS` + `t().recitationAlreadyExists`; count still 1; row byte-identical (no upsert) |
| 4 | `step 4 — foreign student read collapses to null: never learns the record exists` | `getSessionRecitation(secondStudent, sharedSession)` → `null`; nonexistent-id pairing → the SAME `null` |
| 5 | `step 5 — foreign teacher write on the SAME session: SESSION_NOT_FOUND, byte-identical to the nonexistent-id denial` | `NotFoundError` + `SESSION_NOT_FOUND` + `t().sessionNotFound`; the nonexistent-id denial captured via `catchJourneyError` and `expect(denialShape(ghost)).toEqual(foreignShape)` (class + code + message deep equality); row untouched |
| 6 | `step 6 — parent: read collapses to null; the write denial comes from the DB-row ownership predicate` | Parent read → `null`; parent write → `SESSION_NOT_FOUND` with `denialShape` deep-equal to step 5's foreign-teacher fingerprint (cross-actor byte-identity). Comment records: the parent's ROLE gate (`FORBIDDEN`) is a pre-resolver scope denial asserted on the wire tier; the service layer has no role gate — the session row's participant ids are its only truth |
| 7 | `step 7 — governed (suspended) teacher write: FORBIDDEN at the service re-check; zero rows` | `setGovernanceFixture(secondTeacher, { suspended: true, … })` (real users-row flip, before the step's snapshot); write → `ForbiddenError` + `FORBIDDEN` + `t().forbidden`; count still 1; row untouched. The same actor's clean-state step-5 denial (not-found) vs governed denial (FORBIDDEN) proves the re-check pre-empts ownership resolution |
| 8 | `step 8 — concurrent double owner-write: exactly one row, one RECITATION_ALREADY_EXISTS loser, both participants read the same record` | Fresh started session (booked + started in-step, rows tracked); `Promise.allSettled` of two identical owner writes → exactly 1 fulfilled / 1 rejected; winner row shape; loser `ConflictError` + `RECITATION_ALREADY_EXISTS` + translated message; `count === 1`; session row deep-equal; teacher AND student `getSessionRecitation` deep-equal to each other and to the winner's id |

## 3. Fixture ids created (tracked inventory)

All ids are per-run (unique emails/labels via `journeyPrefix("recitation")` → `jrn_recitation_<8hex>`), so the inventory is stated by COUNT and table:

| Table | Rows | Source |
|---|---|---|
| `users` | 7 | cast: owner teacher, second teacher (the foreign/governed actor), primary student, second student, teacher applicant, parent, admin |
| `students` | 2 | primary (profile `trial: 1, hifz: 1` — funds BOTH bookings), second (all lanes empty) |
| `teacher` | 2 | both certified (`isApproved = true`) |
| `applicants` | 1 | real applicants row, deliberately NO `teacher` row (rides the cast; unused by the 8 steps) |
| `parents` | 1 | parent role-child row |
| `admin` | 1 | admin role-child row (rides the cast; unused by the 8 steps) |
| `session` | 2 | shared started session (beforeAll) + race session (step 8) — both via the real lifecycle services |
| `session_request_idempotency` | 2 | one claim per booking key |

Tracked total after a full green run: **18 rows** (14 cast + 2 sessions + 2 claims). Teardown deletes every tracked row FK-safely; the `recitation` rows cascade with their sessions and the `afterAll` asserts zero recitation residue over the tracked session ids. Idempotent-teardown proof (two consecutive green runs) is a 2.3 duty once the suite can run.

## 4. Red-then-green protocol

### RED (captured at authoring time — the test-first evidence)

Command (mandated runner):
```
bun run test/scripts/run-test.ts test/workflows/sessions/recitation-record.journey.test.ts
```
Exit code: **1**. Verbatim failure output:
```
[run-test] Running: bun test /home/z/my-project/test/workflows/sessions/recitation-record.journey.test.ts
◇ injected env (25) from .env.test // tip: ⌘ enable debugging { debug: true }
[run-test] Log saved to: /home/z/my-project/logs/2026-09-07T11-42-41/test/workflows/sessions/recitation-record.journey.test.ts.log
bun test v1.3.14 (0d9b296a)

test/workflows/sessions/recitation-record.journey.test.ts:

# Unhandled error between tests
-------------------------------
SyntaxError: Export named 'RecitationRecordService' not found in module '/home/z/my-project/backend/services/classes/index.ts'.
-------------------------------

 0 pass
 1 fail
 1 error
Ran 1 test across 1 file. [173.00ms]
```
This is EXACTLY the intended red: the future-path import `import { RecitationRecordService } from "@/backend/services/classes"` fails module resolution because the service does not exist until task 2.3. (A second capture run confirmed exit 1 with the identical error, 212.00 ms — log under `logs/2026-09-07T11-4*`.)

### GREEN (pending 2.3)

After 2.3 lands the service, the QL + green suite for THIS file run there (see deferred ledger row **J1**): sub-loop on the file → `bun run test/scripts/run-test.ts test/workflows/sessions/recitation-record.journey.test.ts` → green → `bun run test/scripts/run-test.ts test/workflows` → green → two consecutive green runs (teardown totality proof).

## 5. Quality pipeline evidence (adapted, per dispatch)

| Gate | Status | Evidence |
|---|---|---|
| QL | **DEFERRED to 2.3** (ledger row J1) | Reason: the file imports the not-yet-existing `RecitationRecordService`; `sub-loop.ts` runs the project-wide tsgo and would red the whole tree on this intentionally-red file. Running it now would also violate the dispatch ("DO NOT run sub-loop.ts on this file yet"). 2.3 executes it (exit 0 required) together with its own service-file loops. |
| TE | **RED captured** (module resolution failure above, exit 1) | That failure IS the test-first evidence. Green re-run happens in 2.3 (its TE already mandates "re-run journey task suite — turns green"). No project-wide tsgo run (per dispatch). |
| SEC | **PASS (design-level, service absent)** | Every denial path is ASSERTED, not assumed: repeat-write conflict (step 3), foreign-write collapse with a byte-identical nonexistent-id pairing (step 5), parent non-participant denial through the ownership predicate (step 6), governance FORBIDDEN with a clean-vs-governed contrast on the SAME actor (steps 5 vs 7), race loser typed conflict (step 8), non-participant read null + nonexistent pairing (steps 4/6). No identity parameters beyond `sessionId` exist on any call surface exercised — participant/ownership predicates are forced to resolve from DB rows (the write calls pass only actorId + sessionId + content payload; the read calls only callerId + sessionId). Payloads are static test constants; no PII, no smuggled identity fields (structurally impossible on the planned input whitelist — only `name`/`description` exist). |
| SR | **PASS — every checklist item** | See §6. |
| IV | **PASS** | `test/workflows/AGENTS.md` + `.agents/instructions/tests.instructions.md` re-read in full and applied — item-by-item in §7. |

## 6. Semantic Review — every item

- [x] **Fixtures cleaned (tracked afterAll hard-deletes)** — `registry.cleanup()` after a defensive residual sweep; recitation cascade residue asserted post-teardown; cleanup is idempotent.
- [x] **Actors attributed per step** — each test names its actor in the title; every service call receives that actor's real `users.id` (owner teacher / student participant / foreign student / foreign teacher / parent / governed teacher; fixture writes attributed to "System" style setup in `beforeAll`).
- [x] **Sibling-table oracles explicit** — per-step `SideEffectSnapshot` deltas over: `notifications` (7 actors), `audit_logs` (7 actors), `users` governance columns (7 rows, order-stable), `teacher` row count, `wallet` rows + `teacher_transaction` ledger rows (both teachers) — all via the registry counters / parameterized Drizzle counts scoped to fixture ids; plus direct row snapshots for `session` (deep-equal) and `students` lane balances (step 1).
- [x] **Zero-notification assertions on every step** — `expectZeroSideEffectDeltas` ends EVERY test; it asserts the notification counters AND `expectZeroDispatches()` over the re-armed (`beforeEach` `mockClear`) passthrough spies on the engine's dispatch entries.
- [x] **No monkey-patched roles** — cast via `buildSessionJourneyCast` (real users + role-child rows); governance via `setGovernanceFixture` on the real row; denials fail through the real predicate paths.
- [x] **No `runInRollback` around service calls** — zero occurrences (only the header doc states the layer rule); fixtures commit; cleanup commits.
- [x] **Clean comments (zero plan-artifact references)** — grep over the file for `REQ-|DEV3-|Phase \d|Task \d|plan\.md|tasks\.md|specs\.md|§` → NONE; comments describe domain behavior only (write-once, oracle-safe collapse, defense-in-depth ordering, layer contract by file reference to `test/workflows/AGENTS.md`).
- [x] **No `console.*`** — grep → NONE (no logger either; absence is asserted via DB oracles, not printed).
- [x] **No dead branches** — the `if (loserReason instanceof Error)` guard is reachable exactly when the asserted single rejection exists; the `if (trackedSessionIds.length > 0)` teardown guard covers the beforeAll-failure path; no unreachable throws.
- [x] **No cross-layer imports** — imports are bun:test, drizzle-orm, `@/backend/*` (schema/enums/errors/services/types), `@/shared/locale`, `@/test/workflows/helpers` — test layer → backend + helpers only, all via `@/` aliases (no relative parent paths).
- [x] **No unledgered deferrals** — exactly ONE new ledger row (J1, QL deferral) appended to `deferred-items.md`; nothing else deferred.

## 7. Instruction Verification (IV)

**`test/workflows/AGENTS.md`** (rules 1–12): 1 no-runInRollback ✔; 2 committed fixtures in ONE `beforeAll` transaction + every created row tracked (cast + service-created sessions + claims) + hard-delete `afterAll` ✔; 3 per-run `jrn_recitation_<8hex>` prefix on labels/keys/copy ✔; 4 honest authorization (real role rows; governance flip on the real row; no stubs) ✔; 5 external effects intercepted — dispatch boundary spied (passthrough `spyOn` on the engine's entries; documented `mock.module` fallback noted for 2.3 should namespace interception fail empirically) + which-user targeting N/A (zero dispatches is the contract) ✔; 6 never `rejects.toThrow()` — `catchJourneyError` + exact translated messages from `getServerTranslations("en").errorsTranslations` ✔; 7 bun:test imports, no console.*, no `any` ✔; 8 `@/` aliases only ✔; 9 no demo/seeded rows (cast via entity-setup-backed builders) ✔; 10 one journey file per cross-actor workflow under the domain subdir ✔; 11 sequential actor-attributed steps sharing committed state ✔; 12 cross-actor visibility asserted both directions (participants observe the record; every non-participant observes null/typed denial) + denial probes with owner rows byte-identical ✔.

**`.agents/instructions/tests.instructions.md`**: bun:test utilities only ✔; no `any` (specific types + `@/backend/types` imports) ✔; unique per-run emails/keys via the prefix ✔; entity rows read from schema before use ✔; error assertions pin TRANSLATED messages, never raw keys ✔; non-rollback data has `afterAll` cleanup ✔. Two deliberate, documented layer exceptions (both mandated by the journey tier and consistent with the canonical `session-lifecycle*.journey.test.ts` files): services run on the global `db` with their own top-level transactions (NO `runInRollback` — the layer's documented exception), and `getServerTranslations` is called directly (module-scope helper, journey convention — AGENTS.md rule 6 overrides the rollback-tier i18n-loader caution; the canonical journeys do exactly this).

## 8. Files created / modified / NOT modified

| File | Action | Notes |
|---|---|---|
| `test/workflows/sessions/recitation-record.journey.test.ts` | CREATE | the journey (661 lines) |
| `ai/plans/.../deferred-items.md` | MODIFY (+1 row) | ledger row **J1** (QL deferral) — the ONLY permitted edit |
| `test/workflows/helpers/**` | NOT modified | no scaffolding duties (helpers existed per 0.2); `SpiedFanoutTransport` intentionally NOT imported — the recitation service has no injected-transport seam, so absence is proven by row-count deltas + engine-entry spies (importing the unused class would be dead code) |
| `backend/services/classes/index.ts` | NOT modified by this task | a parallel task's in-flight edit is visible in the shared tree (`M backend/db/repo/classes/index.ts` + untracked `recitation.repository.ts` — task 2.1's footprint); byte-untouched here |
| `tasks.md`, `worklog.md` | NOT modified | orchestrator-owned (dispatch constraint) |

## 9. Carry-forward for task 2.3 (what must exist for green)

1. **Module + barrel:** `backend/services/classes/recitation.service.ts` (CREATE) exporting `namespace RecitationRecordService`, PLUS one barrel line `export * from "./recitation.service";` in `backend/services/classes/index.ts` — the journey imports `RecitationRecordService` from `@/backend/services/classes`, and the red error names exactly that barrel.
2. **Signatures the journey compiles/calls against (plan §4.2 verbatim):**
   - `setSessionRecitation(teacherUserId: number, sessionId: number, input: SessionRecitationSubmitInput, locale: string, outerTx?: DBTransaction): Promise<RecitationReturnType>` (journey passes 4 args, never `outerTx`)
   - `getSessionRecitation(callerUserId: number, sessionId: number, tx?: DBQueryExecutor): Promise<RecitationReturnType | null>` (journey passes 2 args)
3. **Error codes surfaced by the journey (byte-exact classes + codes + translated messages):**
   - `ConflictError("RECITATION_ALREADY_EXISTS", t.recitationAlreadyExists)` — repeat write (step 3) and race loser (step 8)
   - `NotFoundError("SESSION", t.sessionNotFound)` ⇒ code `SESSION_NOT_FOUND` — foreign-teacher write (step 5), parent write (step 6); MUST be byte-identical for a nonexistent id (deep-equality pairings pin class + code + message)
   - `ForbiddenError(t.forbidden)` ⇒ `FORBIDDEN` — governed caller (step 7), fired PRE-transaction (pre-empts the ownership resolution)
   - Reads NEVER throw: participant → row | null; malformed/foreign/non-participant → `null` (steps 2/4/6)
4. **Behavior contracts the journey pins:** write-once arbiter via `recitation_session_id_unique` (no upsert — step 3 uses a DIFFERENT payload); unique-constraint race arbitration with exactly one winner (step 8, `Promise.allSettled`); write purity across `session`/`students`/`users`/`teacher`/`wallet`/`teacher_transaction`/`notifications`/`audit_logs` (per-step deltas + row snapshots); ZERO dispatches (engine-entry spies re-armed per step). If bun cannot spy the namespace members at green-run time, switch those three spies to the documented `mock.module` fallback (restore in `afterAll`) — the row-count oracles remain authoritative either way.
5. **2.3 duties for this file:** `bun run scripts/health/sub-loop.ts test/workflows/sessions/recitation-record.journey.test.ts --lifecycle duplicates` (exit 0 — resolves ledger J1) → `bun run test/scripts/run-test.ts test/workflows/sessions/recitation-record.journey.test.ts` (green) → run it TWICE consecutively (idempotent-teardown proof) → `bun run test/scripts/run-test.ts test/workflows` (layer green). Then flip J1 to ✅ Done.
6. **Types already in place (1.1):** `RecitationReturnType`, `RecitationSelectType`, `SessionRecitationSubmitInput` flow from `@/backend/types`; the journey's `description` payloads always carry explicit `null` or `string` (never `undefined`) per the whitelist contract.
7. **Coexistence note (0.2 D-0.2-3):** the Qira'ah-catalog `recitation.*` files stay byte-untouched; this journey touches only the session-linked record surface.

## 10. Cross-file dependencies

- `@/test/workflows/helpers` (barrel): cast builder, registry + counters, prefix, `catchJourneyError`, `setGovernanceFixture` — all pre-existing, reused as-is.
- `@/backend/services/classes` (barrel): `SessionLifecycleService` (fixture premise: booking + start) and the FUTURE `RecitationRecordService` (the red).
- `@/backend/services/notifications` → `NotificationEngine` (spy target only — never called by the surface under test; the zero-call log is the point).
- `@/backend/types` (1.1's canonical shapes) and `@/shared/locale/server-graphql` (1.2's keys via `t().recitationAlreadyExists`).
- Downstream: task 2.3 owns the green; the wire tier (5.1) owns the parent/scope-layer FORBIDDEN assertions noted in step 6's comment.
