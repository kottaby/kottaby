# Task 1.2 Outcome — i18n error keys (flat, both locales)

**Plan directory (verbatim):** `ai/plans/sprint_1/dev3-007-recitation-record-per-session-11`
**Task:** 1.2 — add EXACTLY two flat `errorsTranslations` keys to BOTH locales + type registration + parity coverage (REQ-002, REQ-052).
**Branch:** `feat/dev3-007-recitation-record-per-session-11` (re-verified before every command; sandbox flips to `main` between sessions — see 0-baseline §1).
**No commits made** (orchestrator owns commits/worklog/checkboxes per dispatch constraints).

---

## 1. What Shipped

Two NEW flat, domain-prefixed keys appended to the `errors` namespace triple, in the established append-at-tail order (after `sessionIntentCorrupt`, the previous last key):

| Key (wire code → camelCase) | en | ar |
|---|---|---|
| `recitationAlreadyExists` (`RECITATION_ALREADY_EXISTS`) | `A recitation record already exists for this session.` | `يوجد سجل تلاوة لهذه الجلسة بالفعل.` |
| `recitationSessionNotWriteable` (`RECITATION_SESSION_NOT_WRITEABLE`) | `This session cannot receive a recitation record in its current state.` | `لا يمكن إضافة سجل تلاوة لهذه الجلسة في حالتها الحالية.` |

- Arabic refined from the dispatch draft for grammatical gender agreement (`يوجد` masculine for `سجل`) and tone-match with neighbors (`sessionInvalidTransition` ar: `…في حالتها الحالية.`; `بالفعل` tail placement like `teacherAlreadyCertified`/`userAlreadySuspended`). Indefinite `سجل تلاوة` after the negation (`لا يمكن إضافة سجل تلاوة…`) is the natural Arabic construction.
- Copy is plain sentences: NO interpolation holes (`{…}`), NO server internals, NO entity/PII values, NO key echo.

## 2. Files Modified (exactly 4 — all inside `shared/locale/**`)

1. `shared/locale/types/errors/labels.ts` — two `readonly <key>: string` slots appended to the flat `ErrorsLabels` interface (lines 165-168), with one-line JSDoc in the exact neighbor style (`quoted copy — condition → ConflictError("CODE", …).`); keys follow the header convention "lowercase camelCase of the SCREAMING_SNAKE_CASE codes". NO new interface, NO nested group.
2. `shared/locale/en/errors/index.ts` — the two en sentences appended to `errorsEn` (lines 82-83).
3. `shared/locale/ar/errors/index.ts` — the two ar sentences appended to `errorsAr` (lines 81-82).
4. `shared/locale/errors-namespace.parity.test.ts` — additive `describe("recitation record denial keys — flat domain additions on BOTH locales")` block (lines 143-159) inserted between the route-emitters and machine-constant-exemption describes; imports `ErrorMessageKey` alongside `ErrorsLabels` (line 44). The block pins: (a) both keys are `Object.hasOwn` top-level slots on BOTH maps, (b) values are non-empty strings via the existing `nonEmptyLabelOf` helper (which throws unless the slot is a top-level non-empty string — the flat-shape assertion), (c) compile-time flat membership via `const recitationDenialKeys: ErrorMessageKey[] = […]` — `ErrorMessageKey` admits ONLY `ErrorsLabels` slots typed plain `string`, so nesting either key under a grouped sub-block fails `bun tsgo`.

## 3. Files NOT Modified (and why)

- `shared/locale/types/message.ts` — `errorsTranslations: ErrorsLabels` already registered (`:21`); interface widening propagates automatically (proven by `bun tsgo` = 0 errors).
- `shared/locale/en/messages.ts` / `shared/locale/ar/messages.ts` — already wire `errorsEn`/`errorsAr` into the tree (`:22` each).
- `shared/locale/namespaces/errors/errors.namespace.ts` — client handle `Errors` is `defineNamespace<ErrorsLabels>("errors.errors", …)`, generic over the interface → auto-covers the new keys.
- `shared/locale/namespaces/registry.ts` — `Errors` already registered (`:14,32`).
- `shared/locale/server.ts` / `server-graphql.ts` — `getTranslations(locale)` returns the FULL `Translations` tree (`server.ts:6-16`); there is NO per-namespace `namespacePaths` map in this repo (the AGENTS.md "serverLegacy.ts" reference is stale — that file no longer exists), so checklist item 4 is structurally satisfied with no edit.
- `app/**` layouts — errors keys are consumed by backend services/resolvers (`ctx.t` / `getServerTranslations`), not by layout SSR namespace picks; no LocaleProvider surface to amend.
- `frontend/providers/apollo/error-link.map.ts` — uses `ErrorMessageKey` (`:27,174`) only as a widening-safe union (additive keys cannot break it; whole-suite runs confirm).
- `tasks.md`, `worklog.md`, `deferred-items.md` — untouched per dispatch constraints (no new deferrals arose).

## 4. Verification Evidence (commands + exit codes)

**QL — per-file unified quality gate** (`bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates`):

| File | Exit code |
|---|---|
| `shared/locale/types/errors/labels.ts` | 0 |
| `shared/locale/en/errors/index.ts` | 0 |
| `shared/locale/ar/errors/index.ts` | 0 |
| `shared/locale/errors-namespace.parity.test.ts` | 1 → FIXED (first `test.each` table was `readonly ErrorMessageKey[]`; bun:test's `each` overload inferred the callback param as `unknown` → TS2769/TS2345 at :153-157; widened the annotation to `ErrorMessageKey[]` mirroring the suite's own `emitted: string[]` pattern) → 0 |

**TE — parity suite** (`bun run test/scripts/run-test.ts shared/locale/errors-namespace.parity.test.ts`): exit 0 — **10 pass, 0 fail, 134 expect() calls**, including the two new rows `domain key 'recitationAlreadyExists'` / `domain key 'recitationSessionNotWriteable'` (both pass). Runner path note: the mandated runner is `test/scripts/run-test.ts` (scripts/run-test/run-test.ts does not exist in this checkout; the suite header's own invocation string matches what was run).

**TE — type-safety proof**: `bun tsgo` → **exit 0, 0 `error TS`** (grep -c = 0).

**TE — cross-suite regression sweep** (sibling parity suites import `errorsAr`/`errorsEn`): all exit 0 — plans 4/0, handshakeCode 10/0, wallet 35/0, sessions 20/0, applicant 11/0, notifications 98/0, adminUsers 43/0, adminBroadcasts 59/0, analytics 90/0.

**SEC audit** — both keys are static generic sentences; zero `{placeholder}` holes (ICU-placeholder parity logic in sibling suites is thereby trivially N/A); no server internals (`23505`, table names, driver terms), no entity ids/PII; ar slots carry Arabic script.

**SR — Semantic Review (every item):**
- [x] No `Translation` enum references — grep over the 4 touched files finds only `errorsTranslations` (namespace property), `Translations`/`getServerTranslations` (tree/function names in pre-existing comments); no `Translation.` enum exists in the repo.
- [x] Keys are `ErrorsLabels`-flat with domain prefix — both are top-level `string` slots prefixed `recitation*` (NOT nested groups like legacy `planCatalog`/`adminUsers`); flatness enforced at compile time (`ErrorMessageKey`) AND at runtime (`nonEmptyLabelOf`).
- [x] No duplicated keys anywhere in the locale trees — repo-wide grep (all `*.ts`/`*.tsx`) shows each key exactly once per registration file (labels.ts, en, ar) + the single test pin; zero occurrences in the sibling `recitationTranslations` namespace or anywhere else (REQ-055 near-duplicate rule respected: `duplicateRequest`/`conflict`/`sessionNotFound` etc. reused per REQ-052, not re-minted).
- [x] No read-then-write races / module-level mutable state — N/A: the changes are immutable string literals + a readonly test fixture; no state of any kind introduced.
- [x] No dead branches / dead code — no control flow added to source; the test block's `test.each` runs over a live 2-element array (both rows executed and passing — no rot-green risk; the suite's own compile-time typed fixture would fail tsgo if a key were renamed away).
- [x] No cross-layer imports — labels/en/ar import only `@/shared/locale/types/errors`; the test imports `bun:test`, `node:fs`, `node:path`, and `@/shared/locale/*` only (shared → shared, per `shared/AGENTS.md` isolation rule; alias discipline `@/` respected inside `shared/locale/**` per `shared/locale/AGENTS.md`).
- [x] Clean comments — ZERO plan-artifact references (no `REQ-`, no task/phase IDs, no `ai/plans/…` paths) in any added comment; comments describe domain behavior (`write-once reject`, `writeability reject`) + wire code mapping in the file's established JSDoc style; no trivial comments.
- [x] No `console.*` — none added; the test file had none before.
- [x] Only task-listed files modified — my working-tree delta is exactly the 4 files in §2. (Caveat recorded below: task 1.1's `backend/types/classes/*` files appear in the SHARED working tree from a parallel subagent — never touched by this task.)
- [x] `git diff --name-only` — contains my 4 files (`shared/locale/ar/errors/index.ts`, `shared/locale/en/errors/index.ts`, `shared/locale/errors-namespace.parity.test.ts`, `shared/locale/types/errors/labels.ts`) plus task 1.1's in-flight `backend/types/classes/index.ts`; untracked `backend/types/classes/recitation.types*.ts` belong to 1.1 (not mine, byte-untouched by me).
- [x] No deferred work without ledger entry — nothing deferred; no `deferred-items.md` entry needed.

**IV — Instruction Verification (shared/AGENTS.md namespace-registration checklist, item-by-item):**
1. *Add interface to `shared/locale/types/<namespace>/index.ts`* → ✅ DONE for a key-addition: two slots on the existing `ErrorsLabels` in `types/errors/labels.ts` (re-exported via the `types/errors/index.ts` barrel) — no new interface warranted (namespace already exists).
2. *Add implementations to `ar` + `en` errors/index.ts* → ✅ DONE (both files).
3. *Export in `types/message.ts` (`MessageSchema`)* → ✅ NO EDIT NEEDED — `errorsTranslations: ErrorsLabels` already exported there; verified live by tsgo.
4. *Path mapping in `serverLegacy.ts` (`namespacePaths`)* → ✅ NO EDIT NEEDED — file removed in the compile-time i18n migration; `getTranslations` resolves the whole tree (evidence `server.ts:6-16`).
5. *Layout SSR `LocaleProvider` (if used in layout)* → ✅ NO EDIT NEEDED — keys are server/service-side transport copy; the `Errors` defineNamespace handle (generic over `ErrorsLabels`) is already in the namespace registry.

Additional IV: `shared/AGENTS.md` errors-namespace contract (§ "The `errors` namespace", lines 262-264) — no near-duplicate semantics minted; `shared/locale/AGENTS.md` (leaf modules = plain literals, no logic — satisfied; `@/` alias discipline — satisfied); `tests.instructions.md` (bun:test imports, no `console.*`, no `getServerTranslations` call inside tests — the suite imports leaf maps directly, pure unit tier; mandated runner used); `backend.instructions.md` i18n section — no backend code written by this task; nothing in it mandates locale-leaf changes.

## 5. Carry-Forward Knowledge for Tasks 2.3 / 5.1

- **Resolution form:** `const t = getServerTranslations(locale).errorsTranslations;` then property access `t.recitationAlreadyExists` / `t.recitationSessionNotWriteable` (single-arg `getServerTranslations` from `@/shared/locale/server-graphql`; mirror `session-lifecycle.service.test.ts:127-130`'s `t()` helper in tests). GraphQL resolvers instead use `await ctx.t("errorsTranslations")` — note the argument is the TRANSLATIONS-surface name `errorsTranslations`, not `"errors"`.
- **Consumption-site coupling:** the errors-namespace parity suite's ROUTE-emitter scanner only enumerates keys literally read as `errorsTranslations.<key>` inside `app/api/graphql/route.ts` — service-level keys never appear there, so no suite coupling exists for 2.3 (suite stays green with keys unconsumed). Still, consume via property access (compile-time typing demands it; string-literal access is impossible on the typed tree).
- **Throw wiring (2.3):** `new ConflictError("RECITATION_ALREADY_EXISTS", t.recitationAlreadyExists)` and `new ConflictError("RECITATION_SESSION_NOT_WRITEABLE", t.recitationSessionNotWriteable)` — `ConflictError(code, message)` custom-code overload verified in 0.2 (anchor f). `assertActorGovernanceClean` keeps taking the whole `errorsTranslations` object (not single keys).
- **Wire matrix (5.1):** assert `extensions.code` = `RECITATION_ALREADY_EXISTS` / `RECITATION_SESSION_NOT_WRITEABLE` and message equality against `getServerTranslations("en"|"ar").errorsTranslations.<key>` (byte-identical to the copy table in §1 — locale parity already pinned by the extended suite).
- **Key-set discipline:** keys are CLOSED additions — any future rename must update the typed fixture array `recitationDenialKeys` in the parity test (it is `ErrorMessageKey`-typed, so tsgo catches drift) and the ar/en leaves in the same change.
- **Format stability:** both messages are static strings — 2.3/5.1 must NOT wrap them in template literals or append context; the copy is user-safe generic by design (SEC ruling of this task).

## 6. Cross-File Dependencies

- `shared/locale/types/errors/labels.ts` ⇄ `shared/locale/{en,ar}/errors/index.ts`: compile-time parity (typed consts) + runtime parity (suite). Any one-sided edit fails tsgo FIRST, suite second.
- `shared/locale/errors-namespace.parity.test.ts` depends on: both leaf maps, `ErrorMessageKey`/`ErrorsLabels` types, and its own `nonEmptyLabelOf`/`assertEveryLeafNonEmpty` helpers.
- Downstream consumers (created later): `backend/services/classes/recitation.service.ts` (2.3) consumes both keys; `frontend/providers/apollo/error-link.map.ts` may adopt the codes onto `messageKey` slots in a future ticket (widening-safe today).
- Unrelated coexistence note (from 0.2 D-0.2-3): the `recitationTranslations` namespace (Qira'ah catalog preferences) is a DIFFERENT namespace — do not move these transport-error keys there.
