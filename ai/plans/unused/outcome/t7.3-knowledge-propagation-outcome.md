# T7.3 — Knowledge Propagation Outcome

**Task ID:** T7.3 (final task — knowledge propagation)
**Status:** Complete

## Process (per SKILL.md §Knowledge Propagation)

Read all 20 outcome files in `ai/plans/unused/outcome/`; extracted recurring (2+ occurrence) patterns; filtered for **global battle-tested knowledge** (permanent, codebase-wide rules — NOT plan-specific constraints or one-off findings).

## Extracted Patterns → Propagation

| Pattern (occurrences) | Class | Propagated to |
|---|---|---|
| knip must run under Bun on ~4GB hosts (node crashes: 6.4GB ArrayBuffer) — used in every phase | library/runtime pitfall | docs/quality/unused-code-cleanup.md §1 + AGENTS.md command comment |
| knip blind spots: PothosObject side-effect exports (16×), DB/GraphQL-backed enums (4 files), run-locked-cmd wrapper blindness (3 scripts), generator outputs (3 files) | architectural invariant | docs/quality/unused-code-cleanup.md §2 (catalog table + deletion-safety grep protocol) |
| Dependency removal needs config-string/bin/file-path verification + paired ignoreDependencies changes | cross-cutting safety | docs/quality/unused-code-cleanup.md §3 |
| Memory-constrained host playbook: tsgolint ~2.5GB; lint:type-aware chunked cache-warming + LINT_MAX_OLD_SPACE_MB; explicit process.exit for singleton-importing CLI scripts | tooling pitfall (Bun/Node/PGlite) | docs/quality/unused-code-cleanup.md §4 |
| Second-order orphan convergence (re-run until exit 0) | architectural invariant | docs/quality/unused-code-cleanup.md §5 |
| Deletion waves leave doc-truthfulness residue (11 of 13 review findings) | process knowledge | docs/quality/unused-code-cleanup.md §6 |

## NOT propagated (filtered per SKILL.md rules)

- Sandbox git-daemon branch-switching / tree-guard workaround — environment-local, not repo knowledge
- Plan-specific counts, task IDs, ledger rows — stay in the plan's outcome files
- The 26 pre-existing knip-blind type exports — logged in post-implementation-review.md as future-cleanup candidates (not a rule)

## Artifacts Updated

| Artifact | Change |
|---|---|
| `docs/quality/unused-code-cleanup.md` | **created** — canonical reference (6 sections) |
| `AGENTS.md` (root) | `check:unused` added to Essential Commands (+Bun-invocation note); doc pointer added to Important References |
| Skills / instructions | **not updated** — the knip/memory patterns are operational knowledge (docs domain), not changes to the quality-gate/quality-loop skills' own methodology; the doc is linked from AGENTS.md where both skills direct agents to read |

## Verification

- `bun run check:unused` → exit 0 (doc + AGENTS edits introduce no findings)
- `bunx @biomejs/biome check AGENTS.md` clean (markdown not in biome scope); tsgo unaffected (docs/ + md not compiled)
- AGENTS.md anchors verified (Essential Commands block + Important References list)
