# Knowledge Propagation Outcome (7.x)

**Date**: 2026-09-09 · Executor: Orchestrator

## Learnings extracted (recurring across 2+ outcome files)

1. **Census-before-admin-mutation** (2.1, 2.2, R1-R4): every shipped admin-gated mutation must carry a census row + emit per the §2.4 contract — enforced by the drift lock. → PROPAGATED to `backend/graphql/AGENTS.md` (single canonical CRITICAL RULE section) + `docs/admin/audit-trail.md` §10.5.
2. **Savepoint-interleave hazard** (3.x, 4.x): concurrent service calls sharing one test transaction interleave savepoint windows; the loser's ROLLBACK TO clobbers the winner's statements. Production unaffected (pool txs). → Documented in 3.x/4.x outcomes; journey/test design follows the call-classification-only rule for shared-tx races.
3. **pg-enum widening discipline** (3.x, 4.x, R1, R4): comparisons against raw stored enums widen via explicit map return-type annotations (never narrowing casts — R4's HIGH proved they erase fields; never String() conversions — type-aware lint flags them). → Applied consistently across all three audit test suites.
4. **Seeded-admin requirement for GraphQL-layer tests** (3.x, journey): service gates need real actor rows; fixtures track committed writes and clean audit rows first under trigger suspension. → Documented in 3.x/5.x outcomes.

## Artifacts updated

| Artifact | Action |
|---|---|
| `docs/admin/audit-trail.md` | §10.5 Completeness Verification + §10 test-lock entries (6.1) |
| `docs/admin/user-management.md` | §2.4 contract-parity note (6.2) |
| `backend/graphql/AGENTS.md` | census-before-admin-mutation CRITICAL RULE (6.3) |
| Root AGENTS.md | no change (no new canonical doc; existing entry covers) |
| Skills / instructions | no change (no new tooling-level convention beyond what layer AGENTS.md captures) |

## Deferred-items enforcement (final)

`deferred-items.md` D-001..D-004: all 📅 Forward with named owners; zero ❌/⚠️ row statuses → **plan not blocked** (verified via ledger sweep in 6.3).

## Test-layer coverage gate (final)

| Layer | Command | Result |
|---|---|---|
| Drift/logic (audit) | run-test.ts | 19 pass / 0 fail |
| Service (plan-catalog + session-lifecycle) | run-test.ts | 26/0 + 66/0 |
| Cross-actor journeys (admin) | run-test.ts per journey | completeness 13/0; audit-trail 7/0; user-lifecycle 7/0; user-denials 8/0; cold-start 12/0; platform-analytics 8/0 |
| GraphQL integration | roles/schema/preservation/plan-seed | 21+4 baseline-pre-existing / 5 / 2 / 1 pass |
| UI components / E2E | N/A — plan mandates no frontend changes |

## Quality gates (final, vs Phase-0 baseline)

- tsgo: 0 errors (baseline 0) · biome: clean (baseline clean) · oxlint/sonarjs: clean on all touched files
- Full `bun quality-gate`: lint-service stage OOMs in this 4GB sandbox (node SIGABRT) — environment resource limit, not a code defect; the equivalent per-file gate (`sub-loop.ts --lifecycle duplicates` = tsgo→oxlint→biome→lint:type-aware→duplicates) exits 0 on ALL 14 touched files, which is the same check matrix scoped per file.
