# Outcome 5.x — Completeness Journey (Phase 5)

**Task**: 5.1 + 5.2 · **Date**: 2026-09-09 · **Executor**: Journey Subagent + Orchestrator completion pass

## Summary

`test/workflows/admin/audit-completeness.journey.test.ts` (1150+ lines, 13 tests, 352 assertions) performs EVERY wired census action against the real service stack with committed fixtures and tracked cleanup, then proves the audit trail is complete, correctly shaped, chronologically ordered, observer-readable through every filter axis, and honest about denials.

## Journey architecture

- **Cast**: 2 provisioned admins (producer `adminA` + observer `adminB`) + student/parent/teacher denial cast + anonymous sentinel — all tracked via `TrackedFixtures` and hard-deleted in FK-safe order under `withAuditDeleteTriggersSuspended`.
- **Census-driven execution**: the journey imports `ADMIN_ACTION_CENSUS` and executes all 11 wired rows through their real service entries via a per-field runner map; `adminSetUserDeleted` and `setPlanActiveStatus` each exercise BOTH of their verbs (Delete/Reactivate, Suspend/Reactivate).
- **Adjust fixture lane**: System-actor direct insert with `rawActionType(AuditActionType.Adjust)` at a pinned past timestamp — covering the only producer-less verb, attributed to the observer so the producer's footprint stays exactly the executed census actions.

## Oracle results (all pass)

| Oracle | Proves | Result |
|---|---|---|
| Census/leg bijection | the runner map names exactly the wired census rows | ✅ |
| Whole-table count delta | successful actions == rows minted (no missing, no phantom) | ✅ (per leg + whole journey) |
| 1:1 action↔row mapping | every execution has exactly one row with full shape (actor/verb/entity/details) | ✅ |
| Canonical ordering | `createdAt DESC, id DESC` tiebreak holds on the busiest anchor | ✅ |
| Observer filtered reads | `listAuditTrail` actor/actionType/entityType/entityId/time-window axes each return exactly their subset | ✅ |
| Rerun-repeats | repeated `adminSetUserDeleted` toggles append distinct rows (append-only honesty) | ✅ |
| Denial matrix (student/parent/teacher) | every census action denied per role, message contains translated FORBIDDEN, zero rows minted per actor | ✅ |
| Anonymous denial | missing actor row → `UnauthorizedError` from `assertActorAdmin` surfaces; arbitration governance gate → `ForbiddenError` (per-gate class asserted) | ✅ |
| Mid-flight domain failures | already-active plan / unknown id / governed-admin denials mint ZERO rows | ✅ |
| Enum coverage | all 7 `audit_action_type` values exercised (6 wired + Adjust fixture) | ✅ |
| Zero residue | teardown restores baseline: actor-scoped counts 0, platform counts byte-identical | ✅ |

**Run**: 13 pass / 0 fail (~1.2s) via `bun run test/scripts/run-test.ts test/workflows/admin/audit-completeness.journey.test.ts`.

## Regression (all admin journeys)

audit-trail 7✅ · admin-user-lifecycle 7✅ · admin-user-denials 8✅ · cold-start-certification 12✅ · platform-analytics 8✅ (incl. its zero-audit-residue purity oracle) · account-governance 9-10 pass with 1-2 fails — **pre-existing flake, verified on `origin/main`** (stash/checkout A/B test): step 8's lapsed-suspension fixture-write is time/parallel-sensitive and its step 9 denial battery fails as cascade when step 8 leaves the fixture suspended. File untouched by this plan (DEV3-017's suite); not a feature-specific finding.

## Quality gates

- `sub-loop.ts --lifecycle duplicates` exit 0 (tsgo/oxlint/biome/lint:type-aware/clean, sonarjs included)
- tsgo 0 errors; biome clean
- Orchestrator fixes during bring-up: uppercase currency fixture (validation requires `[A-Z]{3}`), per-gate anonymous denial classes, `sql<string>` widening on the fixture returning row, promise-chained sequential census execution, parallelized observer reads, `toReversed`/`toHaveLength`/`localeCompare`/`Set` lint compliance

## Carry-forward knowledge

1. The anonymous (nonexistent actor id) denial class is gate-specific: `assertActorAdmin` → `UnauthorizedError` (missing row), session-arbitration `assertAdminGovernanceClean` → `ForbiddenError`. Per-gate assertions are the honest contract.
2. Census-driven journeys must execute actions SEQUENTIALLY (promise-chained) so newest-first ordering mirrors execution order; observer reads may run in parallel.
3. `returning()` on audit inserts yields the narrow pg-enum union — widen via `sql<string>` in the returning projection for primitive-to-primitive comparisons.
4. account-governance step 8 is a known pre-existing flake (see Regression) — reviewers must filter it, not chase it.

## Cross-file dependencies

- Consumes the census module (`audit-completeness.catalog.ts`) as its action list — the drift test keeps both bound to the shipped GraphQL surface.
- `AuditTrailService.listAuditTrail` is the only read surface exercised; its filter axes are now journey-proven.
