# Task 2.1 + 2.2 Outcome — Backup Script (`scripts/ops/backup-database.ts`)

**Status:** COMPLETE · **Phase:** 2 · **Executed by:** implementation subagents (2 runs: module authoring + refactor; test-engineering subagent) with orchestrator-verified SEC/SR/IV closure.

## Summary

Implemented the logical backup tool per design §Component-1 as a small module family under `scripts/ops/` following the sweep/remind ops-script conventions (`--env` bootstrap via `applyEnvFile`, `--help`, exit codes 0/1/2, `console.*` operator-English stdout):

- `backup-database.ts` — CLI entry + `runBackup()` orchestrator with dependency-injected collaborators (`BackupRunDeps`, `SpawnRunner`, injectable clock/PID-liveness).
- `backup-cli.ts` — arg parsing (`--env`, `--out-dir`, `--help`; usage + exit-2 on unknown/missing).
- `backup-lock.ts` — run-directory lockfile `.lock-<pid>` with PID-liveness stale reclamation (fail-closed), release on all paths.
- `backup-toolchain.ts` — argv-array spawn runner (`bunSpawnRunner`), explicit child-env allowlist, toolchain probe (client major ≥ server major, scrubbed stderr), `runPgDump` (`-Fc` into staging).
- `backup-artifacts.ts` — manifest builder + `manifestProblems` validation, sha256, journal hash over `backend/drizzle/`, UTC stamping with `-2`/`-3` collision suffixing, 0600 writes, staging→final atomic rename, leftover-staging discovery.
- `_shared.ts` — `redactDsn`, `scrubDsnSecrets`, `REDACTED_DSN` (shared with restore-verify per Task 3.1 contract below).
- Tests: `backup-database.test.ts` (66), `_shared.test.ts` (19).

## Files Created / Modified

| File | Disposition |
|---|---|
| `scripts/ops/backup-database.ts` | NEW |
| `scripts/ops/backup-cli.ts` | NEW |
| `scripts/ops/backup-lock.ts` | NEW |
| `scripts/ops/backup-toolchain.ts` | NEW |
| `scripts/ops/backup-artifacts.ts` | NEW |
| `scripts/ops/_shared.ts` | NEW |
| `scripts/ops/_shared.test.ts` | NEW |
| `scripts/ops/backup-database.test.ts` | NEW |

## Files NOT Modified (and why)

- `package.json` / `.gitignore` — Phase 4 scope (registration + `/backups/`).
- `scripts/dbActions/*` — consumed read-only (`applyEnvFile` reuse), never edited.
- `backend/**`, `app/**`, `frontend/**` — zero application surface by design.

## Verification Results

- **Quality loop:** `sub-loop.ts --lifecycle duplicates` exit 0 on all created files (tsgo → oxlint → biome → lint:type-aware → check:duplicates).
- **Tests:** `bun test scripts/ops/_shared.test.ts scripts/ops/backup-database.test.ts` → **85 pass / 0 fail** (395 assertions, 234 ms). Pure-unit runner (no DB) per plan; used documented `KOTTABY_TEST_RUNNER_OK=1` bypass for the repo's raw-bun-test guard.
- **4-Tier coverage:** T1 branch/statement of all pure helpers (args, URL parsing, lock lifecycle incl. malformed/self-pid, manifest validation, journal-hash determinism, redaction matrix, probe branches, runPgDump branches); T2 zero-byte artifact → exit 1 + `_FAILED`, collision suffixing, manifest round-trip, exit-code trio; T3 exit-137 garbage-stderr chaos with scrubbed tail, `Promise.allSettled` ×4 concurrency → exactly one winner + lock refusals, out-dir-replaced-by-file; T4 credential-leak greps over all captured streams on success AND failure, argv-array spawn-spy with explicit env allowlist, 0600 modes, lock release on `_FAILED`.
- **SEC audit:** argv-array spawns only (no shell strings/execSync); 0600 artifact+manifest; writes confined to out-dir; env allowlist for children; fail-closed PID liveness; redaction on every output path. PASS.
- **SR checklist:** atomic rename exactly once on success; `_FAILED` path releases lock; zero dead branches; no cross-layer imports; no manual `ReturnType`; **no enums introduced** (explicit); plan-artifact grep over all new files → **0 hits**; no trivial comments. PASS.
- **IV:** root AGENTS.md + backend/tests instructions validated; `console.*` sanctioned for `scripts/**` (eslint `no-console: off`); test-runner guard bypass is the documented mechanism.

## Carry-Forward Contract for Task 3.1 (restore-verify)

- **Redaction:** import from `./_shared` — `redactDsn(dsn: string | undefined): string` (emits `dbName@host(redacted-user)` or `REDACTED_DSN`), `scrubDsnSecrets(text: string, dsn?: string): string` for stderr/stdout scrubbing. Do NOT duplicate.
- **Spawn seam pattern:** export a `SpawnRunner` type + default implementation; inject in tests.
- **Exit-code + tag contract:** `[env]`/`[guard]`/`[pg_restore]`/`[verify:<oracle-id>]`; 0/1/2 semantics as implemented in backup.
- **Test conventions:** temp dirs via `mkdtempSync`; fake DSN password `supersecret-pw` for leak-greps; `KOTTABY_TEST_RUNNER_OK=1` only for no-DB files.

## Cross-File Dependencies

- `_shared.ts` created as the shared redaction module — restore-verify consumes it (Task 3.1) and Phase-5 integration asserts against both.
- Manifest `journalHash` derivation (sorted `backend/drizzle/` listing hash) must be reused by restore-verify's migration-state oracle comparison.

## Deviations

- Implementation split into 5 files instead of one monolith (plan §Component-1 scope note allows `scripts/ops/_shared.ts` "only if justified"; the module split stays within the `scripts/ops/` family, each file QL-clean). Recorded as the single structural deviation; no behavioral deviation from the CLI contract.
- Two subagent runs timed out mid-closure; orchestrator completed SEC/SR/IV verification, outcome, and checkbox closure directly (task-state duties). Code and tests were authored and verified by subagents.
