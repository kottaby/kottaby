# Task 7.1 — Knowledge Propagation Outcome (DEV3-024)

**Date:** 2026-09-05 (Phase 7, executed after Phase-8 review wave green: post-implementation review R21 stop, GATE VERDICT: PASSED)
**Content policy:** rules + references ONLY — no code, no prose dumps, no plan-artifact references inside the propagated artifacts.

## Propagation targets

| # | Target artifact | Action | Lines added |
|---|---|---|---|
| 1 | Root `AGENTS.md` → `## Important References` | Added canonical-doc pointer | 1 |
| 2 | `docs/ops/disaster-recovery.md` → `## Conventions` | Added durable-rule bullet (fallback target — see rule-placement decision below) | 1 (2 rendered lines) |

No other AGENTS.md / instructions / SKILL.md files were modified. No new instruction files were created.

## Exact lines added (verbatim)

**1. `AGENTS.md` — end of `## Important References` (after the `docs/sessions/session-lifecycle.md` entry):**

```
- `docs/ops/disaster-recovery.md` — DR runbook (RPO 1h / RTO 4h), backup/restore-verify ops scripts, invariant-oracle verification
```

(Path backtick-wrapped and `—`-separated to match the section's existing line format exactly; placed last, consistent with the section's chronological append convention.)

**2. `docs/ops/disaster-recovery.md` — last bullet of `## Conventions`:**

```
- **Guard-assess the exact target DSN.** Any operator script that targets a database with a mutating tool MUST pass the exact target DSN through `assessDestructiveDbCommandSafety` before spawning — including every URL/conninfo normalization channel: query-string `host`/`hostaddr`/`port`/`service`, raw authority spans, and multi-host lists. The assessed string must be the string the tool dials.
```

## Durable-rule placement decision (recorded choice)

- The implementation DID surface a durable rule: the post-implementation review program (21 rounds, 44 findings) live-proved that a target DSN assessed at face value can differ from the endpoint libpq actually dials through URL/conninfo normalization channels (R2 URI query host, R3 `?`/`#`-in-authority + `service=` + hostaddr, R14 backslash-fold, R16 multi-host comma authority). The rule generalizes beyond this plan: any future operator script that mutates a database must guard-assess the exact target DSN across those channels.
- `.agents/instructions/backend.instructions.md` was checked first: it has **no section on `scripts/`/ops tooling or destructive-command guardrails** (sections: Architecture, Barrel Files, Type Definition, Pothos, i18n, Logging, Repository, Service, Seeds, GraphQL Schema Generation, Database Access Patterns, Database Testing Keys, Bun, Code Style, Cron Service, Meeting Provider Adapters, Linting Rules), and its `applyTo: "backend/**/*.ts"` glob would not even apply it to `scripts/ops/**`.
- Per the task's fallback instruction, the rule was added to the runbook's `## Conventions` section instead (`docs/ops/disaster-recovery.md` — the canonical doc this plan created, listed in `AGENTS.md` Important References). **No new instruction files were created.** Naming the guard export in the rule is a durable API anchor, not a code dump.

## QL behavior (recorded honestly)

| File | tsgo | oxlint | Result |
|---|---|---|---|
| `AGENTS.md` | ✅ passed (0 errors for file) | ❌ exit 1 — `No files found to lint.` (oxlint cannot lint `.md`; 0 files, 303 rules) | Known non-JS behavior; sub-loop halts at oxlint by design. Content verification relied on instead. |
| `docs/ops/disaster-recovery.md` | ✅ passed (0 errors for file) | ❌ exit 1 — `No files found to lint.` (same known non-JS behavior) | Same handling. |

Content verification in lieu of linters: `git diff --stat` confirms **exactly 1 insertion per file** (2 files changed, 2 insertions(+), 0 deletions); no duplicate list entries introduced; markdown list format and em-dash convention match surrounding entries.

## Content-policy compliance check

- ✅ **Rules + references only**: one reference line (`AGENTS.md`), one 2-sentence normative rule bullet (runbook). No code blocks, no prose dumps, no feature-business-logic, no entity schemas, no plan-artifact citations (`REQ-*`, `Task X.Y`, `ai/plans/...` — zero occurrences in the added lines).
- ✅ **DO-add filter satisfied**: the rule is a permanent, codebase-wide cross-cutting safety invariant (destructive-DB-command guardrail), not plan-specific constraint.
- ✅ **TE:** N/A — doc-only propagation, no executable surface (per tasks.md pipeline scoping note for 7.1).
- ✅ **SEC:** no credentials, DSNs, hostnames, or secrets introduced — both added lines are placeholder-free.
- ✅ **SR:** pass — matches content policy; no duplication of existing runbook content (the Conventions bullet condenses what `## Guard & safety` documents in depth for the restore tool into a generalizable rule for future operator scripts).
- ✅ **IV:** sub-loop printed root `AGENTS.md` for both files (no applicable instruction files); conventions validated.
