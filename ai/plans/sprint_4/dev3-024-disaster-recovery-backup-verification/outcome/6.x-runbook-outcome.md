# Task 6.1–6.3 Outcome — Canonical DR Runbook (`docs/ops/disaster-recovery.md`)

**Status:** COMPLETE · **Phase:** 6 · **Executed by:** documentation subagent (single run, orchestrator-verified closure).

## Summary

Wrote the canonical disaster-recovery runbook at `docs/ops/disaster-recovery.md` (kebab-case, `docs/<domain>/` per root docs policy), following the repo canonical-doc template (Summary / Why / The Pattern / Rules-equivalent guard & runbook sections / What NOT to Do / Rollout Summary / Related Documents). The document covers: the two registered ops tools with verbatim invocations; artifact layout and exit codes 0/1/2; error-tag vocabulary; MVCC single-snapshot consistency note; copy-pasteable hourly/daily cron + systemd-timer stanzas; the ratified RPO 1h / RTO 4h definitions with budget arithmetic (≤ 2 h drill total = ≥ 50 % RTO headroom); the triple restore gate (explicit `--target` → `assessDestructiveDbCommandSafety` on the exact spawn string incl. conninfo last-occurrence normalization + host/hostaddr dual assessment → `--yes-i-understand`); the step-timed full-recovery runbook with a blank timing table; disaster playbooks (DB-content loss; full-region loss with manual env re-entry); quarterly cold-drill procedure; conventions (operator-English stdout exemption stated for auditors without REQ citations; shell-only permission model / BFLA by absence; artifact hygiene); What NOT to Do; Rollout Summary with DEV3-026 holding checkbox authority; Related Documents; the honest D-001 Neon PITR appendix; and the deferred-items table (D-001/D-002/D-003).

**No plan-artifact references** in the runbook (no REQ numbers, no task ids, no specs/plan paths as authority) — the single mandated exception is the Related Documents pointer to this plan's outcome directory.

## File Created

| File | Disposition |
|---|---|
| `docs/ops/disaster-recovery.md` | NEW — canonical DR runbook |

Section list: Summary · Why · Definitions · The Pattern (Backup / Restore & verify, incl. scheduling) · Guard & safety · Full-recovery runbook (step-timed) · Disaster playbooks · Drill procedure · Conventions · What NOT to Do · Rollout Summary · Related Documents · Appendix: Neon PITR / managed-backup evidence (D-001) · Deferred items.

## Verbatim-command verification table (6.1.SR)

| Doc string | `package.json` entry | Result |
|---|---|---|
| `bun run ops:db-backup [--env <file>] [--out-dir <dir>]` | `"ops:db-backup": "bun run scripts/ops/backup-database.ts"` | Script-name token matches; `bun run scripts/ops/backup-database.ts` appears verbatim in the doc (2×); flags match the tool's own `--help` usage text (`backup-cli.ts`) |
| `bun run ops:db-restore-verify -- --from <runDir\|artifact> --target <dsn> --yes-i-understand [--env <file>]` | `"ops:db-restore-verify": "bun run scripts/ops/restore-verify.ts"` | Script-name token matches; `bun run scripts/ops/restore-verify.ts` appears verbatim (2×); flags match `restore-cli.ts` usage (`--from`, `--target` REQUIRED no-default, `--yes-i-understand`, optional `--env`) |

Mechanical checks run (feature branch, working tree): `grep -oE "bun run ops:(db-backup|db-restore-verify)"` → 6× + 3× doc tokens, all matching registered names; `grep -cF` for both raw entrypoint strings → 2 + 2 hits; exit-code tables and error tags transcribed from `backup-cli.ts` / `restore-cli.ts` usage text and `backup-database.ts` / `restore-verify.ts` emit sites (`[env]`, `[pg_dump]`, `[backup]`, `[guard]`, `[pg_restore]`, `[verify:<oracle-id>]`).

## Verification Results

- **6.1.SR:** PASS — verbatim table above; every deferred claim cites D-001/D-002/D-003 (all three exist in `deferred-items.md`, grep-confirmed); no orphaned references (Related Documents targets exist on the branch; relative outcome-dir pointer is the single sanctioned plan reference). Plan-artifact grep `grep -cE "REQ-0|Task [0-9]"` → **0 hits**.
- **6.1.QL:** recorded honestly — `bun run scripts/health/sub-loop.ts docs/ops/disaster-recovery.md --lifecycle duplicates` does **not** cleanly no-op on a `.md` target: tsgo passes vacuously ("no errors for docs/ops/disaster-recovery.md"), then the oxlint stage fails with `No files found to lint` → **exit 1**. This is the pre-existing non-JS-target behavior already recorded for `package.json`/`.gitignore` in the 4.1 outcome (untouched files behave identically) — not introduced by this task. SR checks (above) carry the correctness burden for the markdown file, per the task's record-behavior clause.
- **6.1.IV:** PASS — canonical docs live under `docs/<domain>/` (`docs/ops/` created), kebab-case filename `disaster-recovery.md`, structure/tone mirrors `docs/notifications/realtime-engine.md` + `docs/DATABASE_MIGRATIONS.md`.
- **6.1.SEC:** PASS — no secrets/DSNs in the doc: `grep -nE "postgres(ql)?://[^ )\`]*:[^ )\`]*@"` → **0 hits**; the only `postgresql://` occurrence is the bare scheme placeholder describing the required `DATABASE_URL` shape; managed-host patterns are named as guard-refusal signals only, without credentials.
- **6.2 (PITR appendix) / 6.2.SR:** PASS — the appendix documents only what was observed in the sandbox drill: a local PostgreSQL 17 cluster (trust auth, port 5432) as drill source/target; no Neon console available to observe; zero retention numbers claimed, nothing aspirational. Console-side configuration is explicitly deferred to the operator under D-001 (owned by the DEV3-026 launch-checklist gate). The `git check-ignore -v backups/dump.pgc` vs bare-`backups` caveat was empirically verified (bare path exit 1 until the dir exists; file path matches) before being documented.
- **6.2.SEC:** PASS — same credential grep as 6.1.SEC: no `postgres://` / `postgresql://user:pass@host` forms anywhere; placeholders only.
- **6.3:** this outcome file; drill-feedback patch list placeholder below.

**Checkbox note:** all nine Phase 6 rows flipped to `[x]`: 6.1, 6.1.QL, 6.1.SR, **6.1.SEC**, 6.1.IV, 6.2, 6.2.SR, **6.2.SEC**, 6.3 — nothing outside Phase 6 touched. Per the `6.2.SEC` row's inline ruling, **6.2.TE / 6.2.QL / 6.2.IV are N/A (docs-only appendix, covered by the 6.1 cycles)** — recorded here; the appendix contains no console screenshots or embedded evidence beyond quoted commands, and both credential greps above returned 0 hits.

## Deviations

- None structural. The 6.1.QL "→ 0" expectation is recorded as the exit-1 non-JS-target behavior above per the task's record-behavior clause; the SEC rows exist in `tasks.md` and are flipped with PASS results above.

## Drill-feedback patch list (placeholder)

> Patches to this runbook may follow the cold-drill execution feedback (friction log + measured wall-clock). When a drill files its evidence, append applied patches here as dated one-liners:
>
> - _none yet — awaiting the cold-drill evidence and friction log_
