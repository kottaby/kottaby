# Task 4.1 + 4.2 Outcome — Repo Wiring (package.json scripts + .gitignore)

**Status:** COMPLETE · **Phase:** 4 · **Executed by:** repo-wiring subagent (single run).

## Summary

Registered the two disaster-recovery tools as `package.json` scripts and excluded backup artifacts from version control:

- `ops:db-backup` → `bun run scripts/ops/backup-database.ts`
- `ops:db-restore-verify` → `bun run scripts/ops/restore-verify.ts`

Both entries are inserted **immediately after** the `ops:remind-link-requests` line (the 0.2-verified anchor; package.json lines 65-66 post-insert), keeping the `ops:*` block contiguous and matching the sibling invocation shape (`bun run scripts/ops/<entrypoint>.ts`). `.gitignore` gained `/backups/` under the existing "Local Storage & Database Runtime Artifacts" section (directly after `/db/`); the rule was absent before this task (confirmed against the 0.2 baseline and by re-probe).

## Files Changed

| File | Disposition |
|---|---|
| `package.json` | +2 script entries (`ops:db-backup`, `ops:db-restore-verify`) after the existing `ops:*` pair |
| `.gitignore` | +1 rule (`/backups/`) in the Local Storage & Database Runtime Artifacts section |
| `scripts/ops/scripts-registration.test.ts` | NEW — pure manifest-registration tests (no DB, no spawns) |

## Registration Tests (`scripts/ops/scripts-registration.test.ts`)

Pure-unit, colocated, following the sibling ops-test conventions (bun:test, node:fs, repo-root resolution from `import.meta.dir` so it is cwd-robust, documented `KOTTABY_TEST_RUNNER_OK=1` raw-runner bypass):

1. `ops:db-backup` exists with the exact runbook invocation and the referenced entrypoint file exists on disk (`fs.existsSync`).
2. `ops:db-restore-verify` exists with the exact runbook invocation and the referenced entrypoint file exists on disk.
3. Every `ops:*` entry shares the sibling `bun run scripts/ops/` invocation shape (group consistency).
4. Raw-text adjacency/order sanity: `ops:db-backup` is the line directly after `ops:remind-link-requests`, `ops:db-restore-verify` directly after it, block still ends before `ws` (per the R1 fix contract "insert below L64, not at literal 66-67").
5. `.gitignore` trimmed rules contain exactly `/backups/`.

No plan-artifact references in comments; no enums; no secrets; no DB or external processes.

## Verification Results

- **Tests:** `KOTTABY_TEST_RUNNER_OK=1 bun test scripts/ops/scripts-registration.test.ts` -> **5 pass / 0 fail** (14 assertions). Sibling suites re-run green: `bun test scripts/ops/_shared.test.ts scripts/ops/backup-database.test.ts scripts/ops/restore-verify.test.ts` -> **116 pass / 0 fail** (863 assertions, unchanged); all four ops files together -> **121 pass / 0 fail**.
- **Quality loop on the test file:** `bun run scripts/health/sub-loop.ts scripts/ops/scripts-registration.test.ts --lifecycle duplicates` -> **exit 0** after two oxlint-driven fixes (initial draft: unused `name` binding caught by tsgo; `prefer-array-find` -> `find()`; `no-unsafe-type-assertion` -> `isPackageJson` type guard instead of `as PackageJson`). All lifecycle stages green (tsgo, oxlint, biome:check, lint:type-aware, check:duplicates — last reported skipped as outside jscpd scan scope).
- **Sub-loop on JSON/ignore files (recorded behavior per 4.1.QL):** `bun run scripts/health/sub-loop.ts package.json --lifecycle duplicates` and the same for `.gitignore` do **not** cleanly no-op and emit **no** schema-valid JSON report: tsgo passes vacuously, then the oxlint stage fails with `No files found to lint` -> **exit 1**. This is pre-existing tooling behavior for non-JS targets (verified: untouched `tsconfig.json` behaves identically), not introduced by this diff. JSON well-formedness is instead enforced by the registration test itself (it parses `package.json` and type-guards the `scripts` map); `.gitignore` coverage is assertion 5 above.
- **SEC:** diff introduces zero secrets — package.json delta is exactly the two script lines (paths only, no DSNs/credentials); .gitignore delta is one path rule; test file contains no fixtures/secrets. PASS.
- **SR:** entries appended within the contiguous `ops:*` group below the 0.2-verified anchor (the existing group is functional-ordered, not alphabetical, so appending preserves group consistency); no dead code; no unused bindings; no cross-layer imports; no trivial comments. PASS.
- **IV:** root AGENTS.md conventions validated (Bun runtime, colocated pure-unit test style, documented raw-bun-test guard bypass for no-DB files, no `console.*`). PASS.

## Carry-Forwards

- **Exact registered invocations for the runbook docs (Tasks 6.1/6.2):** `bun run ops:db-backup` and `bun run ops:db-restore-verify` (equivalently `bun run scripts/ops/backup-database.ts` / `bun run scripts/ops/restore-verify.ts`); exit codes 0/1/2 semantics unchanged from the tool contracts.
- Phase 5 integration test should invoke the registered `ops:db-backup` / `ops:db-restore-verify` scripts (not raw file paths) so this wiring stays exercised.
- Sub-loop cannot lint non-JS files (oxlint `No files found to lint` -> exit 1); manifest/ignore changes must stay covered by dedicated tests — relevant to any future task told to QL a JSON file.

## Deviations

- None structural. The 4.1.QL "exit 0" expectation on `package.json`/`.gitignore` is recorded as the no-op behavior above (pre-existing exit-1 on non-JS targets) per the task's "record behavior" clause; the new test file itself achieves a true exit 0.
