# Task 5.2 — Cold Drill Evidence (DEV3-024, Phase 5)

## Drill metadata

| Field | Value |
|---|---|
| Date (UTC) | 2026-09-07, 14:21:57Z → 14:25:09Z |
| Operator | Executing agent (Task 5.2 subagent), role-playing a never-seen-it cold operator; procedure source limited to `docs/ops/disaster-recovery.md` read in full immediately before execution |
| Environment | Local PostgreSQL 17.11 cluster, trust auth, `127.0.0.1:5432`; source DB `app_db` (schema pushed + seeded); bun 1.3.14; pg_dump/pg_restore/psql 17.11 (client = server major) |
| Repo state | Branch `feat/dev3-024-disaster-recovery-backup-verification` @ `1561387` (re-pinned before every shell; working tree only, nothing committed) |
| Scratch DB | `kottaby_drill_20260907T142157Z` — created empty via `createdb` (simulates re-provisioned infra); verified `public` table count = 0 before restore |
| Backup out-dir | `/tmp/drill-20260907T142157Z` (runbook-documented `--out-dir` override; drill hygiene — default `<repo>/backups` untouched). Run dir: `20260907T142242Z` |
| Artifacts filed | `5.2-restore-report.json`, `5.2-manifest.json` (copies per the runbook's drill-procedure evidence list); dump itself deleted with the out-dir post-drill |

## Command actually executed (verbatim runbook commands)

```bash
# Step 1 — provision scratch (runbook step 1; concrete command supplied OUTSIDE the doc — see F-1/F-2)
createdb kottaby_drill_20260907T142157Z

# Step 2 — backup (verbatim documented invocation)
bun run ops:db-backup --env .env --out-dir /tmp/drill-20260907T142157Z

# Step 3 — restore & verify (verbatim documented invocation)
bun run ops:db-restore-verify -- --from /tmp/drill-20260907T142157Z/20260907T142242Z \
  --target postgresql://postgres@127.0.0.1:5432/kottaby_drill_20260907T142157Z \
  --yes-i-understand --env .env

# Step 4 — review: read VERDICT line + restore-report.json in the run dir
# Step 5 — sign-off: persist evidence copies + timing table (this file)
```

## Timing table (runbook's step-timed format, filled during the drill)

| Step | Start (UTC) | End (UTC) | Duration |
|---|---|---|---|
| 1 — Provision scratch DB | 14:22:18Z | 14:22:18Z | 0.037 s |
| 2 — Backup (fresh — no recent verified run existed) | 14:22:41Z | 14:22:42Z | 0.262 s |
| 3 — Restore & verify | 14:22:56Z | 14:22:56Z | 0.352 s |
| 4 — Review VERDICT + report | 14:23:21Z | 14:23:21Z | 0.003 s |
| 5 — Sign-off (evidence persisted) | 14:25:09Z | 14:25:09Z | 0.003 s |
| **Sum of scripted steps** | | | **0.653 s** |
| Inter-step cold-operator latency (read/interpret between steps) | | | ~62.1 s |
| Connection-context diagnosis before Step 1 (F-2: 2 failed `createdb` attempts on Unix socket) | 14:21:57Z → 14:22:18Z | | 20.5 s |
| **Total wall-clock (first cold keystroke → evidence persisted)** | 14:21:57.90Z | 14:25:09.90Z | **191.995 s (3 m 12 s)** |
| Total, clean run only (Step 1 start → Step 5 end, re-timed after F-2 fix) | 14:22:18.40Z | 14:25:09.90Z | 171.492 s (2 m 51 s) |

The scripted recovery work is < 1 s end-to-end; the total is dominated by honest cold-operator think/read time between steps plus the F-2 diagnosis. Both totals are reported — the larger one is the RTO-honest number.

## Result — VERDICT + report review

- **Verbatim final line: `VERDICT: PASS`** (exit code 0). Full tool transcript preserved in the drill logs; key lines:
  - `restore-verify: artifact sha256 verified (b388d71e…0281f11)` — recomputed hash matches manifest (hash correspondence confirmed)
  - `restore-verify: structural checks 24/24 ok`
  - `restore-verify: critical tables audit_logs=0 notifications=0 parent_link_requests=0 session=0 session_request_idempotency=0 teacher_transaction=0 users=4 wallet=0` (matches seeded source; `users` non-empty source→non-empty restored)
  - `restore-verify: oracles 7/7 passed` (OR-W1, OR-W2, OR-B1, OR-U1, OR-U2, OR-REQ, OR-MIG)
- `restore-report.json` (copy filed beside this file): `verdict: "PASS"`, `durationMs: 223`, 24 structural entries all `ok: true` (non-critical tables carry `rowCount: -1` = not counted — see F-4), 7 oracles all `passed: true`, target database recorded.
- `manifest.json` (copy filed beside this file): sha256 `b388d71e92595b6448a6c0b5909bf7add7d877cc53ca04f143274d7130281f11`, journalHash `bb5cd7ab239e7f81782f6aab11fa2ee290748ce56d2825a6e85cab828dd50252`, artifactBytes 75890, database `app_db`.

## Friction log (cold-operator findings — per the runbook's friction rule, each is a runbook defect)

| # | Step | What happened | Runbook patch needed | Severity |
|---|---|---|---|---|
| F-1 | 1 — Provision | The runbook gives **no concrete provisioning command and no scratch-DB naming convention** — step 1 says only "Fresh Postgres instance with known credentials". The `kottaby_drill_<ts>` name and `createdb` came from the operator brief, **not from the doc** (verbatim doc check: zero occurrences of `kottaby_drill`/`createdb`/naming guidance). A real cold operator would have to invent a name and command. | Document a concrete example: `createdb kottaby_drill_<UTC-stamp>` + empty-verification one-liner. | **Medium** |
| F-2 | 1 — Provision | The shell this operator used did **not** carry the ambient libpq connection context (`PGHOST` unset in the non-interactive shell) — `createdb`/`psql` defaulted to the Unix socket `/var/run/postgresql` and failed twice (~20.5 s of diagnosis) before re-running with `PGHOST=127.0.0.1` exported. The runbook's backup/restore-verify steps are immune (they take `--env`/`--target` explicitly) but the **provisioning step is the only step exposed to the ambient connection environment and the doc never says so**. Timing note: Step 1's table entry is a fresh re-time after the fix; the diagnosis time is reported separately in the total. | Add a connection-context prerequisite line (set `PGHOST`/credentials or use `-h`) next to the toolchain-lockstep prerequisite. | **Medium** |
| F-3 | 4 — Review | The runbook says to open `restore-report.json` and confirm "structural row counts, the 7/7 oracle results, and the hash correspondence" but **never documents the report's field names** — the drill evidence call for `durationMs`, and the knowledge of `verdict`/`oracles[].passed`/`structural[].rowCount`, came from opening the file, not from the doc. Self-describing, but a worst-day operator should not have to reverse a JSON contract under pressure. | One line documenting the report's key fields (including `durationMs`). | Low |
| F-4 | 4 — Review | In `restore-report.json`, non-critical tables appear as `rowCount: -1, sourceNonEmpty: false` with no explanation anywhere in the runbook — the operator hesitated over whether `-1` meant "unknown/failed". It means "not counted (non-critical set)". Hesitation = friction per the runbook's own rule. | Fold a `-1`-semantics note into the F-3 field-documentation line. | Low |

Not friction (checked and cleared during the cold run): the `--` separator and all flags behaved exactly as documented; the backup output prints its run-directory path (no discovery guessing); the `[env]` out-dir-outside-repo warning self-explains; the guard correctly did not refuse the local non-prod-shaped scratch; `--help` availability is documented; `0600` artifact modes observed in the listing.

## RTO assessment

| Measure | Value |
|---|---|
| Measured total wall-clock (RTO-honest, incl. F-2 diagnosis + operator latency) | **191.995 s ≈ 3.2 min** |
| Budget (RTO 4 h × ≥ 50 % headroom rule → drill must land ≤ 2 h) | 7 200 s |
| Headroom remaining | **≈ 97.3 %** |
| Verdict | **PASS** — comfortably inside budget; the procedure's scripted work (< 1 s) leaves effectively all 2 h of headroom for the real-recovery extras the runbook reserves it for (region re-provisioning, manual secret re-entry, cutover) |

## Runbook patches applied (post-drill, minimal, operator-English)

1. **P1 (F-1)** — Full-recovery runbook step 1: added the concrete provisioning example (`createdb kottaby_drill_<UTC-stamp>`) and an empty-verification one-liner.
2. **P2 (F-2)** — Full-recovery runbook prerequisites: added a connection-context line (`PGHOST`/credentials or `-h`) beside the toolchain-lockstep rule.
3. **P3 (F-3 + F-4)** — Full-recovery runbook step 4: added one line documenting `restore-report.json`'s key fields (`verdict`, `structural[]` with `rowCount: -1` semantics, `oracles[]`, `durationMs`).

All applied in this task; no plan-artifact references introduced (hygiene grep re-run after patching: 0 hits for `REQ-0|Task [0-9]`).

## Cleanup performed

- `dropdb --if-exists` the scratch DB → verified zero `kottaby_drill%` databases remain.
- `rm -rf` the drill out-dir `/tmp/drill-20260907T142157Z` (dump + report source copies removed; filed evidence copies above were taken first).
- Default `<repo>/backups` store never touched (drill used the documented `--out-dir` override).
