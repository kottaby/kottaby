# Task 3.1 + 3.2 Outcome — Restore & Verification Script (`scripts/ops/restore-verify.ts`)

**Status:** COMPLETE · **Phase:** 3 · **Executed by:** implementation subagent (authoring), QL-fix subagent, test-engineering subagent, micro-fix subagent; orchestrator-verified SEC/SR/IV closure.

## Summary

Implemented the guarded restore-and-verify tool per design §Component-2/3 as a module family mirroring the backup pattern:

- `restore-verify.ts` — CLI entry + `runRestoreVerify()` DI orchestrator (SpawnRunner-style injectable spawn, injectable psql runner, clock).
- `restore-cli.ts` — arg parsing: `--from <runDir|artifact>`, `--target <dsn>` (REQUIRED — omission → exit 2, no default), `--yes-i-understand` (non-TTY gate), `--help`; usage/exit-2 semantics identical to backup.
- `restore-guard.ts` — target-DSN assessment via `assessDestructiveDbCommandSafety()`; refusal → `formatDestructiveDbBlockMessage` + `[guard]`, exit 2, ZERO spawns. **TOCTOU-safe: single DSN variable threaded parse → guard → pg_restore argv (verified by read-through).**
- `restore-shared.ts` — artifact/manifest resolution, sha256 re-verify vs manifest, DSN redaction reuse from `_shared.ts`, psql runner factory.
- `restore-structure.ts` — structural checks: expected tables derived from `backend/db/schema/` pgTable exports at runtime (derivation note in code), critical-set row counts (`users`, `wallet`, `teacher_transaction`, `session`, `session_request_idempotency`, `audit_logs`, `notifications`, `parent_link_requests` — corrected names per plan-review gate), source-non-empty → 0-rows = FAIL, `__drizzle_migrations` hash correspondence; `restore-report.json` writer (0600).
- `restore-oracles.ts` — `ORACLES: OracleDefinition[]` PURE-DATA registry: OR-W1 (wallet non-negative), OR-W2 (teacher_transaction→wallet referential), OR-B1 (session hold/balance-lane), OR-U1 (audit_log actor integrity), OR-U2 (soft-deleted history retention), OR-REQ (session_request_idempotency→user), OR-MIG (migration-journal hash). Read-only `psql -Atc` counts; registry-driven loop — new invariants are data appends only. Anchors in domain terms (`INV-W1`, `INV-B1/B8`, `workflow 02`, `migration-journal`).

## Verification Results

- **Quality loop:** sub-loop `--lifecycle duplicates` exit 0 on all 6 implementation files + test file (3 sonarjs regex/sort fixes in `restore-shared.ts`, 3 test-side lint fixes).
- **Tests:** `restore-verify.test.ts` → **31 pass / 0 fail** (468 assertions) — Tier 1 arg-refusal matrix + manifest validation + verdict aggregation + report shape; Tier 2 zero-row boundary + oracle-0-vs-error distinction; Tier 3 truncated dump / manifest key chaos / oracle SQL error / hash-flip; Tier 4 guard refusal matrix (prod-shaped env, neon.tech, Upstash, RDS) with spawn-spy asserting ZERO spawns on refusal, `--target` omission, credential greps, report 0600. Full repo suites: backup 85/0 + restore 31/0.
- **SEC audit:** guard executes before ANY restore spawn (spy-asserted); argv-array spawns only; report 0600; oracle SQL is static strings; scratch-target assumption documented in header. PASS.
- **SR checklist:** registry is pure data (verified); TOCTOU single-DSN threading verified by top-to-bottom read; zero dead branches; comment hygiene — plan-artifact grep → **0 hits** (2 violations found and fixed); no enums introduced (explicit); no cross-layer imports. PASS.
- **IV:** root AGENTS.md + backend instructions validated via sub-loop discovery.

## Carry-Forward

- Integration test (Phase 5.1) must exercise REAL backup → REAL restore-verify chain: `runRestoreVerify` is the orchestrator entry; CLI contract `bun run ops:db-restore-verify -- --from <runDir> --target <dsn> --yes-i-understand` (registration lands in Phase 4).
- Critical-table names are the corrected set (see plan-review-R1.md) — integration fixtures must seed those exact tables.
- Test-side lesson: lazy-compute paths inside test bodies (a describe-time path computation leaked a fixture outside the tmp workspace — fixed; pattern to avoid).

## Deviations

- Module family (6 files) rather than single file — mirrors Phase-2 approved pattern, each file QL-clean.
- Subagent timeouts → orchestrator closed task-state (outcome/checkboxes) directly; all code/tests authored and verified by subagents.
