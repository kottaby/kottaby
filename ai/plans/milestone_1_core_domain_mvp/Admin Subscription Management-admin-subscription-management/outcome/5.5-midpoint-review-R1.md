# 5.5 Mid-Point Review — Round 1 (Backend Architecture Review Checkpoint)

**Task ID:** 5.5 · **Branch:** `feat/admin-subscription-management` · **Scope:** Phases 2–5 files (subscription admin/purchase services, plan-change helpers, proration helpers, GraphQL mutations, billing types, repository)

## Process

Three parallel reviewers ran over the Phase 2–5 file set:

1. **backend-architecture** — service boundaries, transaction/locking discipline, idempotency design, replay semantics.
2. **types+schema** — GraphQL/Pothos type fidelity, SDL enum conventions, docblock accuracy, barrel exports.
3. **security-pentest** — idempotency key-space abuse, input validation (non-finite/overflow), race conditions, cross-tenant leakage.

Findings were aggregated and de-duplicated into **9 items** (1 HIGH, 1 MEDIUM, 7 LOW). All 9 were applied by the fixer pass; this round re-verifies.

## Findings & Resolutions

| # | Sev | Finding | Resolution | State |
|---|-----|---------|------------|-------|
| 1 | HIGH | Idempotency key-space squatting — client-supplied keys could collide across admin/purchase flows | `subscription-admin:` key namespace + purchase-ingestion reservation guard + regression tests | ✅ Fixed |
| 2 | MEDIUM | plan.md SDL enum spelling mismatched generated Pothos key convention | Plan doc corrected to Pothos key convention `Upgrade/Downgrade` | ✅ Fixed |
| 3 | LOW | Plan-change replay not serialized | Serialized plan-change replay via pre-claim lookup | ✅ Fixed |
| 4 | LOW | Proration direction loosely typed | Direction typed as `ProrationDirection` | ✅ Fixed |
| 5 | LOW | Mutation docblock asterisk malformed | Docblock asterisk corrected | ✅ Fixed |
| 6 | LOW | Top mutation barrel header stale | Barrel header corrected | ✅ Fixed |
| 7 | LOW | Extend accepted non-finite day counts | Non-finite days guard added | ✅ Fixed |
| 8 | LOW | Renewal intervalDays unbounded | intervalDays ceiling added | ✅ Fixed |
| 9 | LOW | Lane-settle race on concurrent plan changes | **Lock implemented (not deferred):** serialized plan-change replay via pre-claim lookup; owner row's `FOR UPDATE` lock held inside the plan-change transaction (`subscription-plan-change.helpers.ts` docblocks: lines 17/243/279). Verified by Tier 2/3 replay+chaos tests — concurrent identical changes both fulfill with the FIRST result (one change, one settlement, one audit). No ❌/⚠️ row in deferred-items.md (grep hits are legend text only). | ✅ Fixed in-code |

## Verification (this round)

- **tsgo:** 0 errors (`bun tsgo` clean).
- **Test suites ×2 rounds — both rounds stable, 0 failures:**
  - `backend/services/billing/subscription-admin.service.test.ts` → **68 pass / 0 fail** (550 expect) ×2
  - `backend/services/billing/subscription-purchase.service.test.ts` → **22 pass / 0 fail** (157 expect) ×2
  - `backend/db/test/logic/billing/subscription.repository.test.ts` → **19 pass / 0 fail** (150 expect) ×2
  - Total: **109 tests, 0 failures**, runner-mandated (`test/scripts/run-test.ts`), postgres 127.0.0.1:5432/app_db.
- **F9:** lock implemented (pre-claim lookup + `FOR UPDATE` owner-row lock), deferred-items.md clean.

## Verdict

**PASS — mid-point gate cleared.** All 9 findings resolved and verified; no regressions; F9 resolved in-code with no deferred items outstanding.
