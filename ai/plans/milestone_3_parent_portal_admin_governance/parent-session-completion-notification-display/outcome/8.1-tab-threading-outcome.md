# Task 8 Outcome — Tab session threading (Task ID 8.1)

**Plan directory:** `ai/plans/milestone_3_parent_portal_admin_governance/parent-session-completion-notification-display`
**Task:** 8.1 Thread the session highlight to homework + evaluations tabs (R-G)
**Executed:** 2026-09-18 (clock) on branch `feat/parent-session-completion-notification-display` (verified via `git branch --show-current` before any write; STABLE worktree `/home/z/kw` only — `/home/z/my-project` never touched; no commits made, orchestrator commits).
**Requirements:** REQ-013 (primary — highlight threads to homework + evaluations), REQ-014 (one link, three content surfaces; `notifications` copy byte-frozen honored) · REQ-040/REQ-050 posture preserved (zero new routes/nav; approved runners only).

---

## 1. Summary

The `?session=` deep-link highlight now threads through the detail container's `renderTabContent` to **HomeworkTab** and **EvaluationsTab**, applying the SAME highlight + `scrollIntoView` mechanism as the shipped `ReportRow` — one link (`/parent/children/<studentId>?tab=reports&session=<id>`) surfaces the report row (notes + rating), the homework row (the assignment), and the evaluations row (the rating-centric view of the same report records) with the session's row highlighted and scrolled into view on every content tab. The match rule is single-sourced as `isDeepLinkTargetRow` in the detail container's helpers module (the pure module Task 7's landing already composes URLs through), and both new rows apply the exact ReportRow mechanism: `rowRef` + target decision + `useEffect` `scrollIntoView({ behavior: "smooth", block: "center" })` + `aria-current="true"` + the primary-border treatment. Tab switches preserve the pointer unchanged through the container's pre-existing `buildDetailUrl` handlers (REQ-013 AC3 needed NO code — verified). D4 verified: `ParentHomeworkEntryReturnType` already carries `sessionId` end-to-end, so NO projection/mapper/documents/codegen change was owed and no codegen regen was run. Two new paired suites (12 tests each) pin the four tiers.

## 2. Files modified (8 source files + 2 new test files + plan bookkeeping)

| File | Change |
|---|---|
| `frontend/views/parent/monitoring/ParentChildDetailContainer.tabs.tsx` | `renderTabContent` now passes the `session` highlight pointer to `HomeworkTab` and `EvaluationsTab` exactly as it already did to `ReportsTab` (`session={session}`); docblock extended to name the shared pointer across the three content tabs. Progress/Attendance arms unchanged. |
| `frontend/views/parent/monitoring/HomeworkTab.tsx` | Props gain `readonly session: number | null` (docblocked); `renderHomeworkBody` call gains `props.session` after `locale` (mirroring `ReportsTab`'s body-call shape). Nothing else touched. |
| `frontend/views/parent/monitoring/HomeworkTab.body.tsx` | `renderHomeworkBody` signature gains `session: number | null` (after `locale`, mirroring `renderReportsBody`'s parameter order); every `HomeworkRow` now receives `deepLinkSessionId={session}`. All four render branches otherwise byte-unchanged. |
| `frontend/views/parent/monitoring/HomeworkTab.parts.tsx` | `HomeworkRow` gains `deepLinkSessionId: number | null` and the ReportRow mechanism: `rowRef` (`useRef<HTMLDivElement | null>`), `isDeepLinkTarget = isDeepLinkTargetRow(deepLinkSessionId, row.sessionId)`, the identical `useEffect` smooth center `scrollIntoView` guarded by `rowRef.current !== null`, `ref={rowRef}`, `aria-current={isDeepLinkTarget ? "true" : undefined}`, and the identical conditional border treatment (`borderColor`/`borderWidth`/`borderInlineStartColor` on `primary.main` vs the row's baseline palette). React import gains `useEffect, useRef`. |
| `frontend/views/parent/monitoring/EvaluationsTab.tsx` | Mirror of the HomeworkTab change: props gain `session`, body call threads `props.session`. |
| `frontend/views/parent/monitoring/EvaluationsTab.body.tsx` | Mirror of the HomeworkTab.body change: `session` parameter + `deepLinkSessionId={session}` on every `EvaluationRow`. |
| `frontend/views/parent/monitoring/EvaluationsTab.parts.tsx` | `EvaluationRow` gains the identical mechanism (same imports, same effect, same `aria-current`, same border conditionals). The evaluations row re-reads the SAME report rows (`sessionId` present on the generated type), so no data-shape change was needed. |
| `frontend/views/parent/monitoring/ParentChildDetailContainer.helpers.ts` | **The one-file relocation the quality loop dictated:** new exported pure decision `isDeepLinkTargetRow(deepLinkSessionId: number | null, rowSessionId: number): boolean` (docblocked: the one match rule every content tab's row applies). First placed as an export inside the component-bearing parts files, `lint:type-aware` correctly rejected that (`react-refresh/only-export-components` — Fast Refresh requires component files to export only components), and this helpers module is the container family's sanctioned pure-decision home (Task 7's outcome §3 explicitly named `.helpers.ts` as part of Task 8's lane). `buildDetailUrl` and every other export byte-unchanged. |
| `frontend/views/parent/monitoring/HomeworkTab.sessionHighlight.test.ts` | **NEW** — 12 tests / 56 `expect()` calls (structure in §4.2). Pure unit tier: no DOM, no renderer; the React tree is inspected as plain elements. |
| `frontend/views/parent/monitoring/EvaluationsTab.sessionHighlight.test.ts` | **NEW** — mirror suite for the evaluations tab: 12 tests / 56 `expect()` calls. |
| `tasks.md` | `8.1` + `8.1.QL/.TE/.SEC/.SR/.IV` flipped `[x]` (and the task-start `[-]` marker). |
| `outcome/8.1-tab-threading-outcome.md` | This outcome. |

## 3. Files NOT modified (scope discipline + byte-freeze verification)

- **`ReportsTab.parts.tsx` / `ReportsTab.tsx` / `ReportsTab.body.tsx` — byte-untouched.** The reports highlight (the mechanism's reference implementation) was read, mirrored, and left alone; `git status` shows no reports file in the delta.
- `ParentChildDetailContainer.tsx` / the detail shell `app/(dashboard)/parent/children/[studentId]/page.tsx` — NOT touched: the container's session parse (`sessionArg`), the `handleTabChange`/`handleSwitcherChange` `buildDetailUrl(...)` handlers, and the shell's prop forwarding already preserve `?session=` across tabs (REQ-013 AC3 satisfied by the shipped code — verified, no edit owed).
- `ProgressTab*` / `AttendanceTab*` — NOT touched (R-G scopes the threading to homework + evaluations only).
- **Backend `**` — zero drift.** No service, types, mapper, query, pothos, schema, or test file touched; `git status` shows no backend file. No SDL/codegen regen (D4 verified present — §5); `frontend/graphql/generated/**` zero-diff.
- **`shared/locale/**` — zero drift** (R-A): the `notifications` namespace and every other locale leaf byte-frozen; no new keys (the highlight adds no copy — `aria-current` and the border treatment are non-textual).
- Emitter, engine, WS envelope, drawer/feed components, nav, routes — zero drift (R-A/R-B; no new route, no nav change, no `useLazyQuery`, no new fetch anywhere in the delta).
- `deferred-items.md` — no new rows; D4's ledger flip is the orchestrator's move (this outcome is the verification reference).
- `ParentChildrenRootContainer.*` / `useSessionResolution.ts` — Task 7's files untouched (their suite re-run green as a regression check, §4.3).

## 4. Verification evidence

### 4.1 D4 (8.1.SR input) — `ParentHomeworkEntryReturnType.sessionId` VERIFIED PRESENT (no projection change owed)

Re-verified at implementation time, exactly as research-00 R-G's caveat anticipated:

| Layer | Evidence | Status |
|---|---|---|
| Canonical type | `backend/types/parents/parent-monitoring.types.ts:127-133` — `ParentHomeworkEntryReturnType` carries `readonly sessionId: number` at `:129` | ✅ PRESENT |
| Mapper | `backend/services/parents/parent-monitoring.helpers.ts:291-299` — `mapHomeWorkRowToEntry` emits `sessionId: row.sessionId` at `:294` | ✅ EMITTED |
| Frontend document | `frontend/graphql/sharedDocuments/parents/parent-monitoring.documents.ts:201` — the `parentChildHomework` selection includes `sessionId` | ✅ SELECTED |
| Generated type | `frontend/graphql/generated/gql/graphql.ts:1086` — `ParentChildHomeworkQuery_parentChildHomework_items = { id: string, sessionId: number, ... }` | ✅ TYPED |

**Consequence: zero types/mapper/documents/codegen changes; the conditional SDL/codegen regen obligation (8.1.SR) is NOT triggered** — the projection never lacked the field. The reports-side row type (`ParentChildReportsQuery_parentChildReports_items`, `gql/graphql.ts:1069`) equally carries `sessionId`, so the evaluations highlight needs no backend surface either.

### 4.2 QL (8.1.QL) — per-file sub-loop, `--lifecycle duplicates` (tsgo → oxlint → biome → lint:type-aware → check:duplicates)

Order per task: tabs first, then tab/body/parts, then tests.

| File | Exit code | Notes |
|---|---|---|
| `ParentChildDetailContainer.tabs.tsx` | **0** (first run) | Project-wide tsgo clean (validates all concurrent edits at once). duplicates lane skipped by config — `frontend/views/parent/monitoring/**` is in `.jscpd.json`'s ignore list. |
| `HomeworkTab.tsx` | **0** (first run) | idem |
| `HomeworkTab.body.tsx` | **0** (first run) | idem |
| `HomeworkTab.parts.tsx` | **1 → 0** | One gate finding, fixed by RELOCATION (not suppression): `react-refresh/only-export-components` — the parts file exports components, so the new pure predicate export was rejected. Fix: `isDeepLinkTargetRow` moved to `ParentChildDetailContainer.helpers.ts` (the container family's pure module, explicitly Task 8's lane per Task 7's outcome §3); both parts files import it. This also single-sources the match rule — strictly better for the no-second-implementation requirement. Re-run: exit 0. |
| `EvaluationsTab.tsx` | **0** (first run) | idem |
| `EvaluationsTab.body.tsx` | **0** (first run) | idem |
| `EvaluationsTab.parts.tsx` | **0** (first run) | The predicate was already relocated before this file's first run. |
| `ParentChildDetailContainer.helpers.ts` | **0** (first run) | The relocated helper's file, validated in its own right. |
| `HomeworkTab.sessionHighlight.test.ts` | **1 → 1 → 0** | Round 1 (oxlint): `unicorn/no-array-sort` ×2 → `toSorted()`. Round 2 (lint:type-aware): `sonarjs/different-types-comparison` on an always-false `element === undefined` guard (index access is typed non-optional here) → truthiness guard; `sonarjs/prefer-specific-assertions` ×8 → `expect(x).toHaveLength(n)`; `sonarjs/no-alphabetical-sort` ×2 on comparator-less `toSorted` → `localeCompare` comparator (extracted `sortedKeys` helper). Re-run: exit 0. No `oxlint-disable`, no cache clears, every fix in-file. |
| `EvaluationsTab.sessionHighlight.test.ts` | **1 → 1 → 0** | Same three findings + fixes (mirror file). Re-run: exit 0. |

Sub-loop discovery printed, and ALL listed rule files were read IN FULL: root `AGENTS.md`, `frontend/AGENTS.md`, `frontend/views/AGENTS.md`, `.agents/instructions/frontend.instructions.md`, plus `.agents/instructions/tests.instructions.md` for the test files.

### 4.3 TE (8.1.TE) — approved runner only (never raw `bun test`)

```
bun run test/scripts/run-test.ts frontend/views/parent/monitoring/HomeworkTab.sessionHighlight.test.ts
→ RUNNER EXIT 0 — 12 pass / 0 fail / 56 expect() calls

bun run test/scripts/run-test.ts frontend/views/parent/monitoring/EvaluationsTab.sessionHighlight.test.ts
→ RUNNER EXIT 0 — 12 pass / 0 fail / 56 expect() calls
```

Each suite mirrors the ReportsTab highlight contract through its tab's pure surfaces (the `isDeepLinkTargetRow` decision + the already-pure `renderHomeworkBody`/`renderEvaluationsBody` body renderer, whose returned React tree is inspected as plain elements — no DOM):

- **Tier 1 (highlight branches on the tab):** the match rule's true/false arms (`target ≡ pointer` true; different id false; both directions); body threading with the pointer set — every mounted row carries the pointer and EXACTLY ONE row (the named session's) is the target; the complementary arms with no pointer and with a foreign pointer (below).
- **Tier 2 (session id absent from the page data — no crash, no scroll):** pointer `null` → every row renders with a `null` pointer and the decision is false for every row id (including `0`/`-1`/huge — `null` never matches); rows `undefined` (read still pending) → ONLY the skeleton mounts — zero row elements exist, so there is no scroll surface and nothing to crash; the absent-pointer never-match loop.
- **Tier 3 (rapid tab switches while highlight pending — no stale scroll):** rapid pointer alternation table (`2077 → null → 2077 → other → null → 2077`) proves the decision is recomputed from the CURRENT pointer with no memory; every body re-render carries exactly its own render's pointer (five successive renders incl. `null` and foreign — no stale pointer); a remount without data mounts no rows (the skeleton branch) — the previous render's rows do not leak. The DOM-level `scrollIntoView` invocation itself is not exercisable in this repo's harness (no component/renderer test layer exists — `tests.instructions.md`: "No static/component UI test layer exists — UI verification is E2E-only"); the stale-scroll safety is pinned through the mechanism's decision surface PLUS the byte-identical effect structure (`[isDeepLinkTarget]` deps + `rowRef.current !== null` guard) mirrored from the shipped, journey-exercised ReportRow mechanism.
- **Tier 4 (no data leaks for a session not in the child's own rows):** a pointer naming a session absent from the rows highlights NOTHING (silent — no highlight, no error, no notice); the row prop surface is CLOSED (`{ row, labels, locale, deepLinkSessionId }` exactly — the pointer reaches a row only as the highlight input); rows pass through UNMODIFIED (the mounted row object is the identical fixture reference — the pointer never merges into row data); the decision is content-invariant (graded vs ungraded / rated vs unrated rows with the same `sessionId` decide identically — the rule reads only the id).

Regression spot-checks (untouched suites, re-run as evidence):

```
frontend/views/parent/monitoring/ParentChildrenRootContainer.helpers.test.ts → 19 pass / 0 fail / 93 expect()   (unchanged from its recorded baseline)
test/workflows/parents/parent-session-completion-deep-link.journey.test.ts  → 6 pass / 0 fail / 57 expect()    (journey J1 stays GREEN end-to-end)
```

### 4.4 SEC (8.1.SEC) — security & tenancy audit

- **The highlight only ever matches rows already gated to the linked child.** The pointer is matched client-side against `parentChildHomework` / `parentChildReports` page data — both reads run through the pre-existing `requireLinkedChild`-gated service methods (`listChildHomework`/`listChildReports`, one repeatable-read transaction, constant-denial oracle). The delta adds ZERO fetches (no `useQuery`/`useLazyQuery`/`fetch` anywhere in the diff — grep-verified), ZERO new query fields, and ZERO id widening: the pointer is the container's already-parsed `sessionArg` (`Number` + NaN drop, unchanged) flowing as `number | null`, compared `number === number` against rows the parent is already authorized to read.
- **A foreign/unresolvable pointer reveals nothing.** A session id not in the child's own rows matches no row: no highlight, no scroll, no error, no notice, no network activity — existence non-disclosure by construction (pinned by the Tier 4 tests in BOTH suites).
- **Closed render surface:** the pointer reaches a row ONLY as the `deepLinkSessionId` highlight input (prop-key-set pin); the row object passes through by reference unmodified (identity pin) — the pointer can never inject content into the assignment/notes/score display. No logging added (`console.`/`logger` grep over the delta: zero hits); no session or child data enters any new surface.
- **No new navigation:** the highlight performs no URL writes and mounts no links; R-B holds (zero new routes; the tab switcher's existing `buildDetailUrl` behavior is untouched).
- **Frozen surfaces intact:** `git status` post-work shows the delta confined to the 8 frontend files + 2 test files + plan bookkeeping — `shared/locale/**` (the byte-frozen `notifications` copy), `backend/db/schema/`, generated GraphQL, emitter/WS/nav all zero-diff.

### 4.5 SR (8.1.SR) — semantic review

- **Highlight mechanism identical to ReportsTab — no divergent second implementation.** Line-by-line parity with `ReportRow` (`ReportsTab.parts.tsx:53-84`): same `useRef<HTMLDivElement | null>` anchor, same target decision semantics (`pointer !== null && pointer === row.sessionId`), same `useEffect` with `scrollIntoView({ behavior: "smooth", block: "center" })` keyed on `[isDeepLinkTarget]` and null-guarded, same `aria-current="true"`, same conditional border treatment (`borderColor`/`borderWidth: 2 : 1`/`borderInlineStart: 4`/`borderInlineStartColor` on `primary.main` vs the baseline palette). The ONE structural difference is deliberate and strength-gaining: the match rule is single-sourced in `ParentChildDetailContainer.helpers.ts` and IMPORTED by both new rows, so the two tabs cannot drift; `ReportRow` keeps its textually-identical inline rule and was byte-untouched (outside this task's authorized file set). Recorded for Task 9 as an OPTIONAL zero-risk unification (point `ReportRow` at the helper) requiring orchestrator authorization for `ReportsTab.parts.tsx` — not a defect, semantics already identical.
- **Duplicate-surface honesty:** the directory is excluded from jscpd's scan (`.jscpd.json` lists `frontend/views/parent/monitoring/**`), so the duplicates gate did not scan these files — the single-sourcing above is the substantive anti-duplication measure, verified by review, not by the gate.
- **REQ-014 content promise:** ONE link (`/parent/children/<studentId>?tab=reports&session=<id>`) now highlights the session's row on reports (carrying `teacherNotes` + `studentRatingByTeacher` — pre-existing, Task 7's landing), homework (carrying the assignment — Jadid/Madi track blocks, pre-existing render), and evaluations (the rating-centric view of the same report records). The tab switcher's `buildDetailUrl(props.studentId, value, props.session)` preserves `?session=` across tabs — REQ-013 AC3 verified in the shipped code, unchanged. The `notifications` copy stays byte-frozen (R-A; no `shared/` drift).
- **MUI v9 `sx` only, theme palette only:** every new style value comes from the theme callback (`primary.main`, `border.main`, `divider`); no hardcoded colors, no string palette access, no `contrastText` introduced; `aria-current` is a semantic attribute, not styling.
- **Comments clean:** grep over all 10 touched files for `REQ-`, `R-G`, `R-A`, task ids, plan/spec/outcome file names → ZERO hits; comments state domain behavior only. No dead branches (both suites exercise every arm); no `console.*`; no nested ternaries; deep imports throughout; no `oxlint-disable`/`jscpd:ignore` anywhere.
- **No parallel URL builder minted:** `buildDetailUrl` remains the one URL builder (Task 7's carry-forward honored — this task added none).

### 4.6 IV (8.1.IV) — instruction verification

Read IN FULL before/while editing (plus everything the sub-loops printed): root `AGENTS.md` (418 lines, re-read this session — runner discipline, no cache clears, deep-import default, barrel rules, Fix-Or-Report, no-summary-markdown), `frontend/AGENTS.md` (MUI v9 `sx` rules, React 19 patterns, error-surface seams, NO `oxlint-disable`), `frontend/views/AGENTS.md` (layer pointer), `.agents/instructions/frontend.instructions.md` (Apollo seam — none implicated; no `console.*`; no nested ternaries; i18n rules — no copy touched), `.agents/instructions/tests.instructions.md` (bun:test imports only, approved runner, static-scan-pin staleness — grep-verified NO static source-scan pin tests cover the touched files, so no pin updates were owed). Fix-Or-Report: the one cross-file-shaped finding (the predicate's home file) was resolved INSIDE the authorized container family (`ParentChildDetailContainer.helpers.ts` — Task 8's lane per Task 7's outcome §3); no unassigned file was touched.

## 5. Carry-forward knowledge for Task 9 (post-implementation review wave)

1. **Final test baselines for the review wave (all via `bun run test/scripts/run-test.ts <path>`):**
   - `frontend/views/parent/monitoring/HomeworkTab.sessionHighlight.test.ts` → **12 pass / 0 fail / 56 expect()**
   - `frontend/views/parent/monitoring/EvaluationsTab.sessionHighlight.test.ts` → **12 pass / 0 fail / 56 expect()**
   - `frontend/views/parent/monitoring/ParentChildrenRootContainer.helpers.test.ts` → **19 pass / 0 fail / 93 expect()** (re-verified, unchanged)
   - `test/workflows/parents/parent-session-completion-deep-link.journey.test.ts` (journey J1) → **6 pass / 0 fail / 57 expect()** (re-verified GREEN this task)
   - Earlier lanes to re-assert: documents 18/0, resolver 16/0, service 88/0, wire 38/0, schema-surface 71/0, `test:graphql` 188/0, `parentMonitoring` parity 160/0.
   - The ONE known red outside this plan's delta remains Task 6's reported `frontend/views/dashboard/nav/navItems.test.ts` pre-existing failure (attributed, untouched here).
2. **This task's diff surface (for the review-wave scoping + grep-locks):** exactly `ParentChildDetailContainer.tabs.tsx`, `ParentChildDetailContainer.helpers.ts`, `HomeworkTab.tsx`, `HomeworkTab.body.tsx`, `HomeworkTab.parts.tsx`, `EvaluationsTab.tsx`, `EvaluationsTab.body.tsx`, `EvaluationsTab.parts.tsx`, the two new `*.sessionHighlight.test.ts` files, plus plan bookkeeping (`tasks.md`, this outcome). `git diff backend/db/schema/` EMPTY, zero mutations, `shared/locale/**` zero-diff, `frontend/graphql/generated/**` zero-diff — R-A/R-K hold with no exceptions from this task.
3. **D4 RESOLVED by verification** (§4.1): the projection always carried `sessionId` — orchestrator flips the ledger row to ✅ with this outcome as the reference. No codegen regen was run or owed this task.
4. **REQ-013 AC3 needs no new code:** tab-switch session preservation lives in the UNCHANGED `handleTabChange`/`handleSwitcherChange` (`ParentChildDetailContainer.tsx:52-57`) via `buildDetailUrl` — reviewers should NOT expect threading code there.
5. **Repo convention learned (for any future frontend/views work):** `react-refresh/only-export-components` forbids exporting non-component functions from component-bearing files under `frontend/views/**` — shared pure helpers must live in a pure module (here: the container's `.helpers.ts`; the directory's other sanctioned home is `parentMonitoringDisplay.ts`).
6. **Optional follow-up (needs orchestrator authorization, zero risk):** point `ReportRow` at the shared `isDeepLinkTargetRow` so all three content rows literally share one match-rule call site — semantics are already identical; `ReportsTab.parts.tsx` was out of this task's authorized file set.
7. **Tier-3 harness note:** the repo has no component/renderer test layer (`tests.instructions.md`: UI verification is E2E-only), so the DOM `scrollIntoView` call is pinned structurally (byte-identical effect mirror + decision-surface tests), not by simulation — same posture as every prior pure-unit suite in this plan.

## 6. Protocol confirmations

- **P1 outcome read:** ALL outcome files re-read before work (`0-baseline`, `1.1`, `2.1`, `3.1`, `4.1`, `5.1`, `6.1`, `7.1`, `midpoint-review-R1`, `plan-review-R1`, `research-00..04`) plus `specs.md` (REQ-013/014), `plan.md` (D5, R-G), `tasks.md` §Task 8, `deferred-items.md`.
- **P2 QL:** per-file, strict order (tabs → tab/body/parts → helpers → tests), fix-and-re-run until exit 0 on final bytes (§4.2 — the parts-file relocation and the test rounds are recorded honestly).
- **P3 SR:** §4.5 executed before any checkbox flip; full-diff review against `ReportsTab.parts.tsx:53-84` confirmed mechanism parity.
- **P4/P5:** this outcome + the `tasks.md` flips are the task bookkeeping; no summary markdown created.
- **P6 IV:** §4.6 — all printed instruction files read; approved runners only (`bun run scripts/health/sub-loop.ts`, `bun run test/scripts/run-test.ts`); NEVER raw `bun test`; no caches cleared anywhere; plain `bun` binary (`~/.bun/bin/bun` absent in this sandbox — Task 0 tooling note); no commits (orchestrator commits).
