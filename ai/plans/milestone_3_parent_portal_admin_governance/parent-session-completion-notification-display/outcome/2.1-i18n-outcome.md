# Task 2 Outcome — i18n key triple `sessionTargetUnavailableNotice` (Task ID 2.1)

**Plan directory:** `ai/plans/milestone_3_parent_portal_admin_governance/parent-session-completion-notification-display`
**Task:** 2.1 — add the single `parentMonitoring` key family (R-H / REQ-041)
**Executed:** 2026-09-18 (clock) on branch `feat/parent-session-completion-notification-display` (verified via `git branch --show-current`; started from tip `c4971c6`, same commit as the Task 0 baseline).
**Requirements:** REQ-041 (primary) · resolves deferred item **D3** (parentMonitoring parity-test extension — ledger NOT edited; the orchestrator flips D3 after verifying).

---

## 1. Summary

Added exactly ONE new key family to the `parentMonitoring` namespace — `sessionTargetUnavailableNotice: string` — as the en/ar/types triple in lockstep, and extended the colocated parity belt `shared/locale/parentMonitoring-namespace.parity.test.ts` so the new key is pinned en/ar (Tier 1) with an RTL-correctness + privacy-hygiene pin for the Arabic string (Tier 2). This is the transient localized notice the portal root shows when a session deep link fails to resolve (REQ-012 consumption point; the copy consumer lands in Task 7).

- **English copy** (plan.md §6.5 suggestion, finalized): `"This session's details are no longer available."`
- **Arabic copy** (full parity, RTL-correct): `"تفاصيل هذه الجلسة لم تعد متاحة."`
- Both strings state unavailability ONLY — zero session/child data, no digits, no grade/score/notes/rating surface (the privacy-hygiene posture of the notifications parity belt `notifications-namespace.parity.test.ts:382-400` extended to this key, per plan.md §6.4 Privacy pin).
- No new namespace, no `Translation.` enum (none exists — research-00 §9), no interpolation params (plain `string`, per REQ-041 assumption).

## 2. Files modified (exactly 4 source-tree files)

| File | Change |
|---|---|
| `shared/locale/types/parentMonitoring/index.ts` | Added `readonly sessionTargetUnavailableNotice: string;` to `ParentMonitoringLabels` (portal-root section, after `childrenEmptyCta`) with a JSDoc pinning the privacy posture (no session ids / child names / grades / scores / notes / digits — existence non-disclosure). Header docblock surface 1 extended to mention the unresolvable-deep-link notice. |
| `shared/locale/en/parentMonitoring/index.ts` | Added `sessionTargetUnavailableNotice: "This session's details are no longer available.",` (mirrored position). |
| `shared/locale/ar/parentMonitoring/index.ts` | Added `sessionTargetUnavailableNotice: "تفاصيل هذه الجلسة لم تعد متاحة.",` (mirrored position). |
| `shared/locale/parentMonitoring-namespace.parity.test.ts` | (a) `"sessionTargetUnavailableNotice"` appended to `MANDATED_KEYS` (portal-root group) — mandated-inventory + exhaustive-minting pins now cover it; (b) new describe block `session-target deep-link fallback — sessionTargetUnavailableNotice` with three tests: exact en/ar copy pins (Tier 1), Arabic RTL-correctness pin — Arabic-script present, no Latin letters, no embedded bidi control characters `\u202A-\u202E \u2066-\u2069 \u200E \u200F` (Tier 2), and privacy-hygiene pin — no digits and no `\b(grade|graded|score|notes?|rating)\b` surface in EITHER locale (mirrors the frozen notifications belt's regex verbatim); (c) header docblock gains lock item 8 + mandated-content enumeration mention; (d) two stale slot-count comments corrected to the true total (74/76 → 117) since the inventory grew by one. |

The types→en→ar placement is intentionally identical (end of the portal-root/linked-children block) so the three leaves stay mechanically aligned; `ParentMonitoringLabels` remains the single source of truth and both leaves are compile-typed against it.

## 3. Files NOT modified (byte-freeze + scope discipline)

| Surface | Why untouched |
|---|---|
| `shared/locale/notifications-namespace.parity.test.ts` | BYTE-FROZEN (R-A / task text) — read-only precedent study only. Verified: zero working-tree drift (`git status --porcelain` on the file is empty). |
| `shared/locale/types/notifications/`, `shared/locale/en/notifications/`, `shared/locale/ar/notifications/` | The `notifications` i18n namespace is byte-frozen (R-A); REQ-041.3 requires zero changes. Verified: zero drift. |
| `shared/locale/namespaces/**`, `shared/locale/{en,ar}/messages.ts`, `shared/locale/types/message.ts` | No new namespace — the existing `ParentMonitoring` handle (`registry.ts:24`/`:48`, id `parentMonitoring.parentMonitoring`) already wires the slice; registry/bundle pins in the parity suite stay green. |
| `deferred-items.md` | D3 is flipped by the orchestrator after verification (explicit task instruction). |
| Everything else | No other key, comment, ordering, or formatting change in the namespace — `git diff --stat` shows exactly 1 added line per leaf, +12/-1 in types, +39/-2 in the parity test. |

## 4. Verification results

### 4.1 QL (2.1.QL) — `bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates`

Each stage runs tsgo (project-wide, filtered for the file) → oxlint → biome:check → lint:type-aware → check:duplicates (intra-file jscpd). No caches cleared, no disable comments anywhere.

| File | Exit code | Final gate output |
|---|---|---|
| `shared/locale/types/parentMonitoring/index.ts` | **0** | `✅ All checks for lifecycle "duplicates" passed` |
| `shared/locale/en/parentMonitoring/index.ts` | **0** | `✅ All checks for lifecycle "duplicates" passed` |
| `shared/locale/ar/parentMonitoring/index.ts` | **0** | `✅ All checks for lifecycle "duplicates" passed` |
| `shared/locale/parentMonitoring-namespace.parity.test.ts` | **0** | `✅ All checks for lifecycle "duplicates" passed` |

First-run clean on all four (no fix/re-run loops needed). Raw logs: `/tmp/ql-types.log`, `/tmp/ql-en.log`, `/tmp/ql-ar.log`, `/tmp/ql-parity.log`. The included project-wide tsgo pass across all four runs also discharges REQ-041.4 (the three leaves typecheck against `ParentMonitoringLabels` — enforced at compile time by construction, since both leaf consts are typed `ParentMonitoringLabels`).

### 4.2 TE (2.1.TE) — parity test run (approved runner only; NEVER raw `bun test`)

```
bun run test/scripts/run-test.ts shared/locale/parentMonitoring-namespace.parity.test.ts
→ 160 pass / 0 fail / 1266 expect() calls  (Ran 160 tests across 1 file. [86.00ms])
```

New-key coverage inside the run (all `(pass)`):
- `mandated key 'sessionTargetUnavailableNotice' exists on BOTH maps` (via `test.each(MANDATED_KEYS)`)
- `the mandated inventory is exhaustive (no silent key minting beyond the 117 slots)`
- `session-target deep-link fallback — sessionTargetUnavailableNotice > the notice is a non-empty string slot on BOTH maps`
- `session-target deep-link fallback — … > the Arabic notice is RTL-correct — pure Arabic script, no Latin letters, no bidi controls`
- `session-target deep-link fallback — … > the notice states unavailability only — no digits or grade/score/notes/rating surface in EITHER locale`
- pre-existing sweep `every ar STRING slot contains Arabic script` now also covers the new ar value.

Tier mapping: Tier 1 = en/ar key-set + shape parity (mandated-key pins, exact copy pins, ar/en key-set agreement, string/function split stability); Tier 2 = RTL/Arabic-script correctness + privacy hygiene of the rendered notice.

### 4.3 SEC (2.1.SEC) — copy-only audit

- The notice carries NO session/child data: no ids, no names, no dates, no grades/scores/notes/ratings, no digits — asserted at runtime by the parity pin above (regex `\b(grade|graded|score|notes?|rating)\b/i` negation + `\d` negation in BOTH locales, regex lifted verbatim from the frozen belt `notifications-namespace.parity.test.ts:398-401`) and statically by the JSDoc contract on the type member.
- Consistent with the constant-denial oracle (D4/REQ-020/REQ-021): the copy cannot leak WHICH failure occurred (stale vs foreign vs severed) — one generic unavailability notice for every denial cause.
- Grep over the four touched files for hardcoded id-shaped/identifier strings: none (the only string literals are the two notice copies + their pins).

### 4.4 SR (2.1.SR) — semantic review

- **Shared-layer purity:** grep `@/frontend|@/backend|@/app` over all four touched files → NONE. Leaf imports are exactly `import type { ParentMonitoringLabels } from "@/shared/locale/types/parentMonitoring"` (absolute `@/shared/locale/...` alias per `shared/locale/AGENTS.md`); the parity test imports only `@/shared/locale/...` modules. No relative traversals (grep → NONE). Leaves remain logic-free string literals (shared/locale/AGENTS.md leaf-module rule).
- **Exactly ONE key family added:** the unified diff touches exactly `sessionTargetUnavailableNotice` (+ its pins/comments). The types interface grew by exactly one member (116 → 117); `MANDATED_KEYS` grew 116 → 117; the exhaustive-minting pin proves no other key was minted, and the ar/en key-set equality pin proves both leaves moved in lockstep.
- **Comment hygiene:** no plan-artifact references (REQ-*, task ids, plan/spec/deferred filenames) in any touched source file (grep → NONE). JSDoc/docblock additions describe runtime behavior only.
- **Namespace inventory stability:** tab-label keys still exactly five; attendance-status keys still exactly five; function-valued slots still exactly ten (all pre-existing pins re-verified green in the same run).

### 4.5 IV (2.1.IV) — instruction verification

- `shared/AGENTS.md` — read in full: layer isolation (no `@/frontend|@/backend|@/app` imports — confirmed clean), `@/` alias discipline, i18n message-type rules (no frontend/backend type references in label types — the new member is `string`), compile-time translation-system rules, no new namespace registration needed.
- `shared/locale/AGENTS.md` — read in full: `@/shared/locale/...` absolute alias inside `shared/locale/**` (confirmed), leaf modules = plain string/object literals with no logic (confirmed), ICU `{var}` interpolation not needed (plain string key).
- `.agents/instructions/tests.instructions.md` — read in full: run-test script discipline (approved runner used; raw `bun test` NOT used), no `getServerTranslations` direct calls in tests (parity test reads leaf consts directly), `bun:test` imports only, no `oxlint-disable` comments, static source-scan pin tests updated in the SAME change (the mandated-inventory pins in the parity test were updated in lockstep — a stale pin would assert a false contract). Note: the file's "Always use `~/.bun/bin/bun`" guidance is inapplicable in this sandbox — that path does not exist; plain `bun` (/usr/local/bin/bun, v1.3.14) is the working binary per the Task 0 tooling note and the task's environment facts. No cross-file issues arose → Fix-Or-Report not triggered.

## 5. Carry-forward knowledge for future tasks

1. **Consumer seam (Task 7):** the key renders via `useAppTranslation(ParentMonitoring).sessionTargetUnavailableNotice` in the portal root (`ParentChildrenRootContainer` family) as a transient notice on `parentSessionTarget` FORBIDDEN/network failure, before falling through to first-child auto-select (R-D). It is a plain `string` — no call, no params; Task 7 adds NO new keys.
2. **Copy is immutable now:** the exact en/ar strings are pinned byte-exact in `shared/locale/parentMonitoring-namespace.parity.test.ts` (describe `session-target deep-link fallback`); any copy change must update those pins in the same change (tests.instructions.md pin-freshness rule) and re-run the suite.
3. **Parity-belt extension pattern (precedent for any future `parentMonitoring` key):** add the key to the `ParentMonitoringLabels` interface + both leaves (compile-time parity), append to `MANDATED_KEYS`, keep the exhaustive-minting pin's slot count accurate, and add a focused describe block for content-class pins (exact copy / Arabic purity / privacy hygiene) mirroring the frozen notifications belt's regexes.
4. **Baseline comparison (Task 9):** the only shared/ drift attributable to this plan is the 4-file diff in §2 — expect exactly these files in `git diff --name-only` under `shared/` at review time (plus later tasks' own files). The frozen notifications surfaces must still be zero-diff (they are today).
5. **Slot-count comment quirk resolved:** the parity test's two stale count mentions (74/76) disagreed pre-change; both now read 117 = true `ParentMonitoringLabels` member count. If the namespace grows again, update both.
6. **RTL-correctness test recipe:** for pure-Arabic strings, RTL correctness is enforced as: Arabic-script present (`/[\u0600-\u06FF]/`), no Latin letters (`/[A-Za-z]/` absent), no embedded bidi controls (`\u202A-\u202E \u2066-\u2069 \u200E \u200F` absent), no digits. Bidi isolation (`shared/locale/bidi.ts`) is only needed when interpolating counterparty names into mixed-direction sentences — this key has no interpolation surface.

## 6. Cross-file dependencies

- **Depends on:** none — the key triple is additive to a shipped namespace; no schema/codegen/env impact (R-K zero-diff obligations unaffected: `git diff backend/db/schema/` empty, no emitter/WS/notifications-namespace drift).
- **Blocks/feeds:**
  - Task 7 (portal-root resolution flow) — consumes `sessionTargetUnavailableNotice` for the transient failure notice (REQ-012).
  - D3 (deferred-items ledger, target Task 2) — substantively resolved by §4.2; ledger row flip left to the orchestrator per instruction.
  - Task 9 (post-implementation review) — the §5.4 expectation for the shared/ diff surface.
  - Task 1 journey J1 (test-first) — unaffected; the journey asserts the constant FORBIDDEN copy from `errorsTranslations`, not this portal-UI notice (namespace separation documented in `types/parentMonitoring/index.ts` docblock: "Denial copy for 403 responses stays on the `errors` namespace; this namespace owns portal UI copy only").

## 7. Protocol confirmations

- **Outcome read (P1):** ALL existing outcome files read in full before work started: `plan-review-R1.md`, `research-00-planning-basis.md`, `research-01-emission-backend.md`, `research-02-notification-ui.md`, `research-03-parent-portal.md`, `research-04-i18n-tests-precedents.md`, `0-baseline-outcome.md` — plus `specs.md`, `plan.md`, `tasks.md`, `deferred-items.md` and `shared/AGENTS.md`, `shared/locale/AGENTS.md`, `.agents/instructions/tests.instructions.md`.
- **Branch discipline:** implemented on `feat/parent-session-completion-notification-display`; `main` never touched (Task 0 flagged the same-commit ambiguity; the check-out was confirmed before any write).
- **Byte-freeze honored:** zero drift on `shared/locale/notifications-namespace.parity.test.ts` and all `notifications` locale surfaces (verified via `git status --porcelain` post-change).
- **Working tree after this task:** the four shared/ files above + `tasks.md` (Task 0's pre-existing flips + this task's six `[x]` flips) + this outcome file. Nothing else.
