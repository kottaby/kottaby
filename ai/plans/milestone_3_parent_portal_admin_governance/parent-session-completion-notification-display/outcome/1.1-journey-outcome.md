# Task 1.1 Outcome — Journey J1 (parent session-completion deep link), TEST-FIRST

**Plan directory:** `ai/plans/milestone_3_parent_portal_admin_governance/parent-session-completion-notification-display`
**Task:** 1.1 Author journey J1: parent session-completion deep link (TEST-FIRST)
**Executed:** 2026-09-18 (clock) on branch `feat/parent-session-completion-notification-display` (branch confirmed with `git branch --show-current` before any work; never `main`)
**State:** RED by design — the journey runs and fails at exactly the contract-missing steps; it cannot go green until the backend read surface and its typed declaration land.

---

## 1. Summary

Created the test-first cross-actor journey `test/workflows/parents/parent-session-completion-deep-link.journey.test.ts` encoding the six-step cross-actor scenario of the planning basis (research-00 §7) EXACTLY:

| Step | Actor → action | Assertion encoded |
|---|---|---|
| 1 | Teacher submits σ1's report + homework | Report + `home_work` rows land; the completion wave prepares receipts for the student AND the linked parent; dispatch is SPIED at the engine's injected `options.transport` seam (never real channels): exactly 2 publishes, one envelope per recipient, in the engine's deterministic order (student, then parent); the two per-recipient emit claim keys are the deterministic `session:<id>:report` digests; publish is observed only after the submission's own commit (post-commit seam) |
| 2 | System → parent inbox | Exactly one report-wave row for the linked parent carrying the drawer/feed-resolvable triple: wire type `SessionCompletion`, `relatedEntityType="session"`, `relatedEntityId=<σ1.id>` (non-null, numeric, the SESSION id — no child id anywhere on the row); foreign parent's and unlinked student's inboxes hold zero σ1 rows; reads never publish |
| 3 | Parent resolves the session target | `ParentMonitoringService.getSessionTarget(parentP.id, σ1.id, "en")` returns the closed two-field projection `{ sessionId, studentId }` (exact key set pinned) with `studentId` = the LINKED child; pure read — zero publishes |
| 4 | Parent lands on the deep link | The resolved child's `listChildReports` read contains σ1's report row (`sessionId`, `teacherNotes`, `studentRatingByTeacher`); the canonical landing URL `/parent/children/<studentId>?tab=reports&session=<sessionId>` is constructible from the resolved pair alone (exact string pinned); the same session id also resolves on the homework read (one-link content promise) |
| 5 | Negative — unlinked child | Teacher submits σ2's report: the student ALWAYS receives their row (wave ran), the parent leg fails closed — zero rows for either parent, exactly ONE dispatch addressed to the student only, exactly one fresh claim key (the student's) |
| 6 | Negative — denial probes | Foreign parent × linked-pair session, foreign parent × nonexistent session id (`2_000_000_000`), teacher, and student — all four denied through the REAL gates (`requireActor` role arm, linked-child grant arm, null-session arm) with the constant localized `ForbiddenError` (`errorsTranslations.forbidden` substring, code `FORBIDDEN`); the four fingerprints are byte-identical (existence non-disclosure); exactly one bounded domain log per denial, all `code="FORBIDDEN"`; zero publishes |

Conventions honored (per `test/workflows/AGENTS.md` + `docs/testing/workflow-journey-tests.md`, read FIRST as the task mandates): committed `beforeAll` fixtures in ONE committing transaction (commit-or-nothing); `TrackedFixtures` hard-delete teardown in FK-safe reverse order with mandatory zero-residue re-probes (including the per-run prefix probe); NO `runInRollback` anywhere; honest role auth (real `users` rows + real role-child rows; no monkey-patching); denial assertions via try/catch + `getServerTranslations("en").errorsTranslations` substrings — never `expect(...).rejects.toThrow()`; `bun:test` imports only; `@/` aliases only; per-run `jrn_parents_<uuid8>` prefix; fixtures via `backend/db/test/entity-setup.ts` helpers (never seed rows); sessions reach `completed` through the REAL lifecycle path (book → start → complete).

## 2. The forward-contract bridge (compile-safety against the frozen contract)

`ParentMonitoringService.getSessionTarget` does not exist yet — a direct typed call would fail `tsgo` and break the 1.1.QL gate. The journey therefore consumes the read through a narrow, clearly-commented forward-contract bridge:

- A locally-declared interface `ParentSessionTargetRead` types the member EXACTLY as the frozen contract: `getSessionTarget(parentActorId: number, sessionId: number, locale: string, tx?: DBTransaction): Promise<{ readonly sessionId: number; readonly studentId: number }>`.
- `sessionTargetRead()` resolves the member at call time via `Reflect.get` on the service object (the repo-established `Reflect.get` + `unknown` pattern, used e.g. by the shared locale parity tests) guarded by a runtime type guard — zero unsafe type assertions (oxlint `no-unsafe-type-assertion` stays clean).
- An absent member fails LOUDLY with `journey: the parent session-target read is not declared on the parent-monitoring service` — the precise missing-contract failure, never a silent skip.
- The bridge comment describes the DOMAIN contract (parent session-target resolution returns the linked child's ids for a completion-notification deep link) and contains NO task ids, REQ ids, plan file names, or plan paths.

## 3. Files created / modified

| File | Change |
|---|---|
| `test/workflows/parents/parent-session-completion-deep-link.journey.test.ts` | CREATED — the six-step journey (843 lines incl. docblocks; 6 tests, 25+ expect() calls) |
| `ai/plans/.../tasks.md` | MODIFIED — checkboxes 1.1, 1.1.QL, 1.1.TE, 1.1.SEC, 1.1.SR, 1.1.IV flipped `[ ]` → `[x]` |
| `ai/plans/.../outcome/1.1-journey-outcome.md` | CREATED — this outcome |

## 4. Files NOT modified (and why)

- **Emission substrate** (`backend/services/classes/session-report-notification.service.ts`, `session-report.service.ts`, engine, WS envelope) — byte-frozen (R-A); the journey only OBSERVES it through the injected spy seam + persisted rows.
- **`backend/services/parents/parent-monitoring.service.ts`** — the frozen contract's typed member lands in Task 3; touching it now would break the test-first ordering. (The journey deliberately does NOT modify it.)
- **`backend/db/schema/**`** — zero schema changes (R-K).
- **`shared/locale/**`** — no copy changes; denial copy asserted from the existing `errorsTranslations.forbidden` (no new keys).
- **`test/workflows/helpers/**`** — shared scaffolding consumed as-is (`TrackedFixtures`, `SpiedFanoutTransport`, `catchJourneyError`); no new helpers were needed, so the barrel and self-tests stay untouched.
- **Precedent journeys** (e.g. `parent-monitoring.journey.test.ts`) — read as patterns, never edited.

## 5. Verification results

### 5.1 1.1.QL — Quality Loop (exit code 0)

```
bun run scripts/health/sub-loop.ts test/workflows/parents/parent-session-completion-deep-link.journey.test.ts --lifecycle duplicates
→ SUB-LOOP EXIT = 0

✅ tsgo passed (no errors for test/workflows/parents/parent-session-completion-deep-link.journey.test.ts)
✅ oxlint passed
✅ biome:check passed
✅ lint:type-aware passed
✅ check:duplicates passed   (file is outside the jscpd scan scope — **/*.test.ts ignored by .jscpd.json)
```

Two gate findings were found and fixed in-file during the loop (both root-cause fixes; no `oxlint-disable`, no cache clears):
1. oxlint `no-unsafe-type-assertion` on the bridge's original `as unknown as {...}`/`Record<string, unknown>` downcast — replaced with the `Reflect.get` + `unknown` + type-guard pattern (repo precedent: `shared/locale/analytics-namespace.parity.test.ts:148`).
2. lint `sonarjs/no-alphabetical-sort` on a comparator-less `toSorted()` — replaced with the `localeCompare` comparator form.

`tsgo` is clean PROJECT-WIDE (0 `error TS` lines) — the bridge compiles against the frozen contract as required.

### 5.2 1.1.TE — the journey IS the cross-tier contract

Confirmed the authored journey covers the four tiers as declared in the task:
- **Tier 1 (all six steps):** steps 1–6 as separate sequential actor-attributed tests; 100% of the journey's assertion surface is the six-step contract.
- **Tier 2 (boundaries):** the parent row's `relatedEntityId` is pinned non-null, numeric, and equal to the session id (the deep-linkable boundary of the row; the absent/empty-id → feed-fallback arm is the resolver tier's obligation — Task 6's suite pins `resolveNotificationRoute`'s never-fabricate rule — and is recorded as such); foreign-session boundary and nonexistent-session-id boundary probed in step 6; exact two-field key-set boundary on the resolution projection.
- **Tier 3 (ordering/chaos):** emission-before-read ordering is enforced by the sequential step state machine (step 1 emits → step 2 reads the inbox → step 3/4 resolve → land); the post-commit publish seam is observed (publishes recorded only after the submission's own commit; the substrate's rollback-emit property is engine-tested, out of journey scope). Concurrent-call chaos (`Promise.allSettled` on resolution) is deferred to the service/wire tiers where the gate's rate limiter and snapshot discipline live — noted as carry-forward for Tasks 3/4 so the journey doesn't double-pin mocked behavior.
- **Tier 4 (security/abuse):** foreign-parent + nonexistent-session probes (byte-identical oracle), unlinked-child fail-closed emission, teacher/student wrong-role denial probes, bounded denial logs, zero-publish denials.

### 5.3 Runner output — the expected TEST-FIRST RED state

```
bun run test/scripts/run-test.ts test/workflows/parents/parent-session-completion-deep-link.journey.test.ts
→ RUNNER EXIT = 1 (expected RED)

(pass) Step 1 — teacher submits the report: completion receipts prepared for the student AND the linked parent; dispatch spied, published post-commit [19.37ms]
(pass) Step 2 — parent inbox: the completion row carries the resolvable (wire type, entity pointer, session id) deep-link triple [1.96ms]
(fail) Step 3 — parent resolves the session target: the read returns the closed id pair naming the linked child [0.40ms]
(fail) Step 4 — parent lands on the deep link: the resolved child's reports read contains the session's report row and the pinned landing URL is constructible [0.28ms]
(pass) Step 5 — unlinked child's session: the wave fails closed — the student row exists, NO parent emission [11.97ms]
(fail) Step 6 — denial probes: foreign parent, nonexistent session, teacher, and student all hit the constant localized ForbiddenError [0.49ms]

 3 pass / 3 fail / 25 expect() calls
```

**Failure mode documentation (must be the missing-contract failure):** every failing step fails at the bridge with exactly

```
error: journey: the parent session-target read is not declared on the parent-monitoring service
    at sessionTargetRead (test/workflows/parents/parent-session-completion-deep-link.journey.test.ts:227:15)
```

- Steps 3, 4, 6 fail at `sessionTargetRead()` BEFORE any service call — the frozen contract's typed member is absent, which is precisely the expected red. No assertion inside those steps executed, so the red is attributable solely to the missing contract.
- Steps 1, 2, 5 PASS — they exercise the EXISTING emission substrate end-to-end (real lifecycle sessions, real report submission, spied dispatch, fail-closed parent wave, persisted-row triple) and prove the fixture/teardown/spy infrastructure is sound. No fixture, cleanup, or infrastructure error appears anywhere in the log.
- Only benign log lines accompany the run: two `NOTIFICATION_IDEMPOTENCY_DEGRADED` fail-open warns from the lifecycle's OWN completion-prompt emissions during fixture provisioning (no claim cache injected on that internal path — existing engine behavior, out of the journey's asserted scope).

### 5.4 Idempotent-teardown proof + independent residue probe

- The suite was run TWICE consecutively via the approved runner — identical RED state (3 pass / 3 fail, same failure message) — proving zero residual state between runs (per-run `jrn_parents_<uuid8>` prefixes; `cleanup()` leaves nothing).
- Independent DB probe after both runs (throwaway script, deleted after use): `users(email LIKE 'jrn_parents_%') = 0`, `notifications = 0`, `session = 0`, `reports = 0`, `home_work = 0` — zero residue.
- `afterAll` hard-deletes all tracked rows in try/finally (cleanup runs even when a step assertion fails), then re-probes every tracked id set + the run prefix; untracked service-created rows (the lifecycle's completion-prompt waves) cascade away with the `users` delete (`notifications.userId` FK `ON DELETE CASCADE`), matching the precedent journey's teardown semantics.

### 5.5 1.1.SEC — cross-actor tenancy table encoded verbatim

The planning basis §7 actor table's "Cannot Do" column is asserted through the REAL authorization path in step 6:
- **Teacher cannot resolve parent sessions** → `getSessionTarget(teacherT.id, σ1.id)` → constant localized `ForbiddenError` (role arm).
- **Student cannot resolve session targets** → `getSessionTarget(studentS1.id, σ1.id)` → constant localized `ForbiddenError` (role arm).
- **Parent cannot resolve foreign sessions** → `getSessionTarget(parentPF.id, σ1.id)` AND `getSessionTarget(parentPF.id, ABSENT_SESSION_ID)` → byte-identical constant localized `ForbiddenError` (grant arm / null-session arm) — existence non-disclosure.
- **Parent receives no rows for unlinked children** → step 5's fail-closed emission arm (zero parent rows; single student-only dispatch).
- Identity is never client-supplied: every resolution call passes the actor's own verified user id; the dispatch spy pins exactly which userIds each wave targeted (wrong-recipient targeting would fail steps 1/5).

### 5.6 1.1.SR — semantic review

- Cleanup airtight: tracked hard-deletes in try/finally `afterAll`; zero-residue re-probes mandatory (5.4).
- Dispatch spy never leaks to real channels: the spy is a real injected dependency at the engine's `options.transport` seam (structurally implements the fan-out transport port); nothing else is configured — no Redis/socket side effects are exercised by the asserted wave.
- Module-level mutable state: only the journey-convention suite fixtures (`let cast` + `tracked` registry + `transportSpy` + in-memory claim cache — the helpers' established per-suite pattern); NO other shared mutable state — each step re-resolves the session-target read through the stateless bridge (no cross-test result caching), and all helpers are pure functions over their arguments.
- No dead branches; every helper is called; no `console.*`; no `any`; enum values imported as VALUES (`NotificationType`, `SessionStatus`, `SessionIntent`, `SurahJuzRef`).
- Comments are clean: domain language only — zero task ids, REQ ids, plan file names, or plan paths anywhere in the file (verified by grep).

### 5.7 1.1.IV — instruction verification

Sub-loop discovery for the file printed: `.agents/instructions/tests.instructions.md` + root `AGENTS.md` — both read IN FULL, plus the task-mandated `test/workflows/AGENTS.md` and `docs/testing/workflow-journey-tests.md`. Validation against them:
- `tests.instructions.md`'s `runInRollback`/rollback-transaction rules apply to the `backend/db/test/` layer; `test/workflows/` is the DOCUMENTED exception (`test/workflows/AGENTS.md` hard rule 1 + `docs/testing/workflow-journey-tests.md:55-58`), which this file follows. The `getServerTranslations` direct-call caution (db-test layer hang) is likewise superseded in this layer by the journey conventions + precedent journeys (module-level `ERRORS_EN` const, exactly as `parent-monitoring.journey.test.ts:167`).
- Approved runner only; log capture used; never raw `bun test` as a task runner.
- Fix-Or-Report: no cross-file violation required fixing an unassigned file — both gate findings were fixed in-file. No cross-file instruction conflicts remain.
- Note carried forward (already recorded at Task 0): `~/.bun/bin/bun` does not exist in this sandbox; plain `bun` is the working binary.

## 6. Cross-file dependencies (BLOCKING for later tasks)

1. **Task 3 (backend read surface) MUST tighten the forward-contract bridge into a direct typed call.** Once `ParentMonitoringService.getSessionTarget(parentActorId: number, sessionId: number, locale: string, tx?: DBTransaction): Promise<{ readonly sessionId: number; readonly studentId: number }>` exists (with the frozen flow: input validation → parent role re-check → portal rate limit → one repeatable-read transaction: session read → constant localized `ForbiddenError` on miss → linked-child gate → closed two-field projection), the bridge (`sessionTargetRead`, `isSessionTargetMember`, `SESSION_TARGET_MEMBER_KEY`, and the `SESSION_TARGET_READ_UNAVAILABLE` message) must be REPLACED by direct typed calls in steps 3, 4, and 6, and the local `ParentSessionTargetRead` interface deleted (consume the canonical `@/backend/types` interface). This is the one deliberate test-first seam in the file.
2. **Task 9 (final gate) MUST re-run this journey to GREEN** via `bun run test/scripts/run-test.ts test/workflows/parents/parent-session-completion-deep-link.journey.test.ts` — 6 pass / 0 fail is the plan's completion definition for the journey lane.
3. Task 4 (wire tier) owns the en/ar denial-copy parity and the anonymous-401 tier for the same read — the journey's step 6 pins the EN service-layer oracle only (by design; the wire matrix extends it).
4. The absent/empty-`relatedEntityId` → feed-fallback arm is pinned at the resolver tier (Task 6's suite), not here — the notification row's id is always the non-null session id at the emission substrate (research-01 §2), so a journey-level empty-id row cannot exist without faking substrate data.

## 7. Carry-forward knowledge

- **RED bookkeeping:** the journey's RED state is the deliverable of this task — Tasks 3–6 implement toward it; the failure message to watch disappear is `journey: the parent session-target read is not declared on the parent-monitoring service`.
- **Spy seam:** `SessionReportService.submitSessionReport` accepts `NotificationEngineCallOptions` as its 6th arg — `{ transport, cache }` injection is the sanctioned dispatch-boundary spy seam for any wave-asserting journey (no module mocking needed). The lifecycle's internal completion-prompt wave does NOT take injected options → it emits fail-open with the `NOTIFICATION_IDEMPOTENCY_DEGRADED` warn during provisioning; scope wave-row assertions by the report-ready `title` slot to exclude it (precedent + this file both do).
- **Notification residue:** service-created notification rows cascade-delete with their `users` rows (FK `ON DELETE CASCADE`); registering the asserted wave rows explicitly + the `users`-cascade covers teardown.
- **Wave row identity:** the persisted row exposes `relatedEntityType="session"` + `relatedEntityId=<sessionId>` and NO child id — the deep link's child id can only come from the new gated read (the planning basis's core gap).
- **Gate trivia for future files:** oxlint `no-unsafe-type-assertion` rejects even `as unknown as Record<string, unknown>` downcasts — use the `Reflect.get(host, key)` + `unknown` + type-guard pattern for structural runtime lookups; `toSorted()` without a comparator trips `sonarjs/no-alphabetical-sort` — always pass the `localeCompare` comparator.
- **Env:** `.env.test` carries `TEST_SERVER=1`/`TEST_CI=1` → the portal rate limiter is bypassed in journeys (existing limiter behavior); `bun run test/scripts/run-test.ts` injects `.env.test` automatically.
