# Task 4 Outcome — SDL regen + schema pin + wire matrix (Task ID 4.1)

**Plan directory:** `ai/plans/milestone_3_parent_portal_admin_governance/parent-session-completion-notification-display`
**Task:** 4.1 Codegen + SDL surface pin + wire tests (R-J; REQ-021, REQ-031)
**Executed:** 2026-09-18 (clock) on branch `feat/parent-session-completion-notification-display` (verified via `git branch --show-current` before every generator run, test run, and write — the workspace's external HEAD-flip process was active throughout; the branch was re-asserted before each critical step).
**Requirements:** REQ-021 (wire-tier BOLA/BOPLA/BFLA), REQ-031 (codegen pair, SDL pin) · resolves deferred-item **D2** (schema-surface pin for `parentSessionTarget` — ledger NOT edited per orchestrator instruction; this outcome is the verification reference).

---

## 1. Summary

Ran the official codegen pair over Task 3's Pothos surface and landed the two test-tier extensions:

1. `bun run generate:gqlSchema` regenerated `frontend/graphql/generated/schema.graphql` with EXACTLY the new surface: the `ParentSessionTarget` object type + the `parentSessionTarget(sessionId: Int!): ParentSessionTarget!` root field (zero other churn). `bun codegen` then ran clean with **zero output change** — correct, because no frontend document selects the field until Task 5 (the `typescript-operations`/`typed-document-node` plugins generate per-operation, and the documents glob contains no `parentSessionTarget` selection yet).
2. `backend/graphql/test/schema-surface.test.ts` — the parent-monitoring inventory pins gained `"parentSessionTarget"` (Query-field list) and `"ParentSessionTarget"` (named-type list); a new describe pins the exact deep-link shape (`sessionId: Int!): ParentSessionTarget!`, the `$all` parent scope snapshot, the closed two-field projection, and the no-`id` validation failure); the codegen-sync pin now asserts the committed SDL contains the field + type verbatim. One pre-existing baseline drift was reconciled per the file's own established `RECONCILED_*` pattern: the `purchaseVerificationPlan` mutation (shipped in the schema, never enumerated in the Mutation-root additions pin) — necessary, else the mutation-set pin fails on this branch.
3. `backend/graphql/test/parent-monitoring.wire.test.ts` — the role × operation matrix extended from five to six parent-only ops with the session-target cells: anonymous 401 (single + constant-across-six), wrong-role 18-cell 403 matrix, BFLA byte-identity across foreign/0/−1 session ids (pre-service), forged-role-token probe, parent-without-link 403, foreign-session 403 (byte-identical with nonexistent — the oracle), linked-child success cell (closed two-field pair, `studentId` = the LINKED child), nonexistent ≡ foreign oracle + 5× chaos probes, `sessionId` 0/−1 → VALIDATION, 2^31 → parse-time GRAPHQL_VALIDATION_FAILED, BOPLA smuggle probes ×3, en/ar denial parity, and the closed two-field selection pin replacing the id-first pin for the no-id document. Fixtures gained a second linked child (`studentS2`, owner of the seeded session rows) via the REAL parent-link wire flow, a sanctioned teacher-row anchor, two session rows, and FK-safe teardown (sessions deleted FIRST).

Both suites green, the full `test:graphql` lane green (twice), the rename-drift probe demonstrates every relevant pin catches a field rename, and the probe file was reverted byte-identical.

## 2. Files modified

| File | Change |
|---|---|
| `frontend/graphql/generated/schema.graphql` | **Via the official generator only** (`bun run generate:gqlSchema`): +10 lines — `type ParentSessionTarget { sessionId: Int!; studentId: Int! }` and `parentSessionTarget(sessionId: Int!): ParentSessionTarget!` with the resolver's field description. Lexicographically sorted, deterministic emission (re-run reproduces it byte-identically — the committed-SDL sync pin re-proves this every run). |
| `frontend/graphql/generated/gql/graphql.ts` | **Regenerated by `bun codegen` — zero diff.** No document references the new field yet (Task 5 adds the document; the codegen plugins are operation-driven). The run is recorded as executed-and-stable, per R-J's "run after every schema change". |
| `backend/graphql/test/schema-surface.test.ts` | Pins extended as in §1.2: `PARENT_MONITORING_QUERY_FIELDS` + `PARENT_MONITORING_TYPE_NAMES` gain the wire names; new describe "Parent portal session-target read — exact deep-link shape + `$all` scope pins" (3 tests: exact arg/return/scope snapshot, closed two-field `Int!` projection, no-`id` + `Cannot query field "id"` validation proof); codegen-sync pin gains two `toContain` assertions on the committed SDL; docblock updated (five → six parent-only reads). Plus the documented one-time `RECONCILED_VERIFICATION_PLAN_PURCHASE_MUTATION_FIELDS` re-anchor (pre-existing drift on this branch, NOT caused by this task — see §4.4). |
| `backend/graphql/test/parent-monitoring.wire.test.ts` | Matrix extension as in §1.3: header docblock (five → six ops, new matrix-cell bullets), `SESSION_TARGET_DOCUMENT` (closed two-field selection), `forgedRoleTokenOf` helper (payload-splice forgery, signature untouched), `linkParentToStudentOverWire` helper (extracted from the inline flow, now used twice — second linked child), `studentS2` fixture + second link-request id + `linkedSessionId`/`foreignSessionId` session rows (teacher-anchored via the sanctioned single-shared-PK insert; schema defaults otherwise), teardown deletes sessions BEFORE link requests/users, and 11 new tests across all six matrix tiers (anonymous, wrong-role incl. BFLA byte-identity + forged token, parent-without-link, foreign-child, success, oracle/boundary/chaos, BOPLA, locale parity, selection pin). |

## 3. Files NOT modified (scope discipline)

- `backend/graphql/query/parents/parent-monitoring.query.ts` — used ONLY as the rename-drift probe target and **reverted byte-identical** (`git diff --stat` on it is EMPTY; verified after the probe). Final diff: none.
- `backend/graphql/pothos/**`, `backend/services/**`, `backend/types/**` — Task 3's shipped surface untouched.
- `backend/lib/gateway/public-operations.ts` — untouched; NO allowlist entry needed (authenticated parent-only field, never anonymous; the schema-surface "Public-operation allowlist agreement" pins pass unchanged).
- `backend/db/schema/**` — zero drift (R-K). `backend/graphql/mutation/**` — zero changes (R-K: zero new mutations).
- Emission substrate (`session-report-notification.service.ts`, engine, WS envelope) and `shared/locale/**` — untouched (R-A).
- `deferred-items.md` — NOT edited (orchestrator instruction): **D2 is resolved by this task**; flip to ✅ with this outcome as the reference is the ledger owner's move.

## 4. Verification evidence

### 4.1 Codegen diff summary (4.1.SR input)

- `schema.graphql`: `10 insertions(+), 0 deletions(-)` — one object type block + one Query field block (with description). Nothing else moved.
- `gql/graphql.ts`: regenerated, **0 lines changed** (operation-driven codegen; no consumer document yet).
- Generator determinism: a second `generate:gqlSchema` emission reproduced the same bytes; the suite's "Codegen sync — committed SDL is byte-identical to the built schema" test (fresh deterministic emission vs committed file) passes on every run.

### 4.2 QL (4.1.QL) — `bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates`

| File | Exit code | Notes |
|---|---|---|
| `backend/graphql/test/schema-surface.test.ts` | **0** | tsgo (project-wide, filtered) → oxlint → biome → lint:type-aware all green; duplicates lane SKIPPED BY CONFIG (file outside jscpd scan scope) and reported pass |
| `backend/graphql/test/parent-monitoring.wire.test.ts` | **0** | idem (duplicates lane skipped by config — outside jscpd scan scope) |

Generated output exclusion VERIFIED, not assumed: `eslint.config.mjs` ignores `frontend/graphql/generated/**` (incl. the type-aware stage's `ignores` list) and `biome.json` excludes `!frontend/graphql/generated` — the generated artifacts are lint-exempt by config and were only ever written by the official generators.

### 4.3 Tests (4.1.TE) — approved runners only (never raw `bun test` as a task step)

| Runner | Result |
|---|---|
| `bun run test:graphql` (the plan-mandated lane) | **PASS — 12 files / 188 tests / 1092 expect() calls / 0 failed** (56.3s). Run twice (start and post-probe-revert), both green. Pre-Task-4 derivation: 188 − 11 (wire) − 3 (schema-surface) = 174. |
| `bun run test/scripts/run-test.ts backend/graphql/test/schema-surface.test.ts` | **71 pass / 0 fail / 481 expect()** (was 68 committed → +3) |
| `bun run test/scripts/run-test.ts backend/graphql/test/parent-monitoring.wire.test.ts` | **38 pass / 0 fail / 572 expect()** (was 27 committed → +11) |

**Rename-drift probe (Tier 4):** renamed the query field registration to `parentSessionTargetDriftProbe` in `backend/graphql/query/parents/parent-monitoring.query.ts` → re-ran the schema-surface suite → **67 pass / 4 fail**, with the drift caught by FOUR independent pins:
1. `Query._health — retyped probe surface > root query retains EXACTLY the baseline fields plus the pinned additions` (the Query-root additions pin over `PARENT_MONITORING_QUERY_FIELDS`);
2. `Parent portal session-target read > parentSessionTarget carries the exact deep-link shape…` → `Error: Query must register the parentSessionTarget root field`;
3. the no-`id` probe document failing with `Cannot query field "parentSessionTarget" on type "Query".`;
4. `Codegen sync > frontend/graphql/generated/schema.graphql matches a fresh deterministic emission`.
The rename was then REVERTED — `git diff` on the query file is EMPTY — and the suite returned to **71 pass / 0 fail**. The pin set demonstrably fails CI on any accidental rename/drop (REQ-031.2).

**Environment incident (not a test defect):** the FIRST standalone wire run failed in `beforeAll` (120s hook timeout) because a wedged `next-server` (PID 18810) from an earlier lane teardown held TEST_PORT 3066 accepting TCP but never answering HTTP — the lifecycle helper's live-server path hung against it. Resolution: killed the stale test-server process (the sanctioned `bun run test:kill` targets port 3099 in this environment and did not see 3066, so the stale 3066 listener was killed directly — test-port-only policy respected). After cleanup the wire file passed standalone 38/0 and the full lane passed green twice. Carry-forward: if a standalone server-lane file hangs in `beforeAll`, check `logs/next-dev-server-3066.log` + port 3066 for a wedged leftover before suspecting the suite.

### 4.4 SEC (4.1.SEC) — security & tenancy audit

- **Error envelopes:** `expectDenialCode` asserts, for EVERY denial in the matrix, the `extensions.code` value AND `expect(JSON.stringify(errorItem)).not.toContain("stacktrace")` — no stack leaks on any arm. `extensionKeysOf` pins the extensions key set (`code` + correlated `requestId`); `bodyShapeOf` compares whole bodies (only `requestId` redacted) so any session/child field leak would break the byte-identity assertions.
- **BFLA pre-service:** the wrong-role tier's 18 cells assert the scope-layer denial; the session-target BFLA probe proves the denial is BYTE-IDENTICAL across foreign/`0`/`−1` session ids (the argument never reaches the resolver — a per-cause denial log would otherwise differ in `entityId`); the forged-role probe proves a payload-spliced `role: "Admin"` in a real token is rejected at token integrity as UNAUTHORIZED (never a 403 that would mean the forged claim was read).
- **BOLA oracle:** nonexistent ≡ foreign-session 403 bodies byte-identical; 5× sequential chaos probes identical over time; parent-without-link arm constant.
- **BOPLA:** smuggled `parentId`/`userId`/`studentId` args beside the legit `sessionId` die GRAPHQL_VALIDATION_FAILED pre-resolver (data key absent).
- **Boundary honesty:** `sessionId: 0`/`−1` → VALIDATION (shape, not ownership), matching the service's positive-safe-integer guard; `2^31` dies at parse time (Int32 bound) with the data key ABSENT.
- **Public-operations allowlist:** unchanged and unchanged-by-assertion — the schema-surface allowlist-agreement tests pass and `git diff` on `backend/lib/gateway/public-operations.ts` is empty. The new field is authenticated (`$all { authenticated: true, role: [UserRole.Parent] }`), so default-deny needs no entry.
- **Denial-log discipline (Task 3 carry-forward honored):** the service's null-session log is `{ code, entity: "sessions", entityId, locale }` — reviewers (Task 4.5) should expect BOTH denial entities on this surface: `"sessions"` (the session-read arm) and `"students"` (inside `requireLinkedChild`). The wire tier asserts the RESPONSE side only; no row fields appear anywhere in the wire bodies.

### 4.5 SR (4.1.SR) — semantic review

- Generated diff reviewed: exactly `parentSessionTarget` + `ParentSessionTarget` (§4.1) — no schema churn beyond the sanctioned surface; the five existing portal fields stay pinned byte-identical in the field list.
- One pre-existing baseline drift surfaced and reconciled with the file's own documented `RECONCILED_*` pattern: `purchaseVerificationPlan` (verification-plan purchase mutation, present in the schema at `schema.graphql:689` and in `backend/graphql/mutation/verification-plan-purchase.mutation.ts`, absent from the committed Mutation-root additions pin). This is branch-level drift from an earlier feature, NOT this task's change; without the re-anchor the mutation-set pin cannot pass on this branch. It is annotated as a one-time reconciliation in-file (not a silent baseline flip).
- No dead test code: every new test exercises a matrix cell the plan names; the extracted `linkParentToStudentOverWire` helper REMOVED the inline duplication (the second link reuses it) rather than adding a second copy.
- Fixture hygiene: the honest-empty arms stay honest — the seeded sessions belong to `studentS2` (second linked child) so `studentS`'s sessions page remains empty while the session-target success cell has its row; teardown deletes sessions first (RESTRICT FKs), then link requests, then users (admin row never deleted).

### 4.6 IV (4.1.IV) — instruction verification

Read IN FULL: root `AGENTS.md`, `backend/AGENTS.md`, `backend/graphql/AGENTS.md` (codegen pair rule, `$all` scope composition, canonical object type, allowlist default-deny), `.agents/instructions/backend.instructions.md` (GraphQL Schema Generation section), `.agents/instructions/tests.instructions.md` (GraphQL integration tests, runner discipline, pin-staleness rule). Both test files' sub-loops printed their applicable instruction/AGENTS sets and all were read before any edit was finalized. Fix-Or-Report: no cross-file violation required fixing outside the two assigned test files + generated artifacts.

## 5. Carry-forward knowledge

1. **Frozen wire contract (for Task 5 and beyond):**
   ```graphql
   parentSessionTarget(sessionId: Int!): ParentSessionTarget!
   type ParentSessionTarget { sessionId: Int!, studentId: Int! }   # NO id — keyFields:false
   ```
   Registration: `backend/graphql/query/parents/parent-monitoring.query.ts:251` (`queryField` after `parentChildHomework`, `authScopes: parentOnlyAuthScopes`); objectRef `ParentSessionTargetPothosObject` beside the portal refs in `backend/graphql/pothos/parents/parent-monitoring.pothos.ts`; TS backing type `ParentSessionTargetReturnType`.
2. **Exact generated type names for Task 5's TypedDocumentNode** (naming convention verified against the current `frontend/graphql/generated/gql/graphql.ts` emissions, e.g. `MyLinkedChildrenQuery`): after the sharedDocuments document lands (operation name `ParentSessionTarget`, export `parentSessionTargetQueryDocument: TypedDocumentNode<ParentSessionTargetQuery>`), `bun codegen` emits:
   - `ParentSessionTargetQuery = { parentSessionTarget: ParentSessionTargetQuery_parentSessionTarget }`
   - `ParentSessionTargetQuery_parentSessionTarget = { sessionId: number, studentId: number }` (compact field extraction; `Int` → `number`; no `__typename` noise under the current config)
   - `ParentSessionTargetQueryVariables = Exact<{ sessionId: number }>`
   - the TypedDocumentNode runtime const (named from the OPERATION, e.g. `ParentSessionTargetDocument`) inside `graphql.ts` — the documents file's own export keeps the `{field}QueryDocument` camelCase convention (`myLinkedChildrenQueryDocument` precedent, `parent-monitoring.documents.ts:56`).
   Until then `gql/graphql.ts` shows ZERO diff (proven this task) — Task 5 MUST re-run `bun codegen` and commit.
3. **Apollo cache (Task 5):** append `ParentSessionTarget: { keyFields: false }` beside the existing portal no-id entries (`frontend/providers/apollo/apolloCache.ts:114-118`) — the type carries no `id` and the wire test pins that selecting `id` FAILS validation, so normalization-exemption is mandatory (R-I / R14 precedent).
4. **For Task 4.5 reviewers:** suite baselines to protect — wire 38/0 (572 expect), schema-surface 71/0 (481 expect), lane 188/0 (1092 expect). Both denial-log entities on this surface: `"sessions"` (session-read arm) and `"students"` (linked-child gate). The schema-surface file's `RECONCILED_*` constants are the sanctioned re-anchor pattern — the new `RECONCILED_VERIFICATION_PLAN_PURCHASE_MUTATION_FIELDS` row documents pre-existing branch drift, not plan work.
5. **Test-authoring nuances proven here:** (a) the wire file's `beforeAll` carries its own 120s cap — a wedged warm server on 3066 consumes it entirely; check the server log/port first on hangs; (b) the lifecycle helper leaves a warm server on 3066 after a lane (current post-run probe answers HTTP — alive); standalone runs reuse it when live; (c) `bun run test:kill` targets port 3099 in this sandbox (`GRAPHQL_TEST_PORT` default 3066 is what the wire lane actually uses) — stale 3066 listeners need a direct kill; (d) GraphQL `Int!` rejects `2^31` at document-validation time (data key ABSENT), while 0/−1 are parser-clean and reach the service's VALIDATION arm — keep the two boundary arms distinct in future coverage.
6. **Cross-file dependencies:** Task 5 (documents + cache, §5.2/§5.3), Task 6/7 consume the URL the pair constructs; Task 9 re-runs this lane (188/0 expected to hold, +documents-test delta from Task 5). D2 RESOLVED here (ledger flip pending by ledger owner).

## 6. Protocol confirmations

- **P1 outcome read:** all plan `outcome/` files + `specs.md` + `plan.md` + `tasks.md` + `deferred-items.md` read (Task 3's outcome §6/§7 was the binding carry-forward source for this task's pins and matrix cells).
- **P2 QL:** per-file, both exit 0 (§4.2); generated artifacts excluded from lint by config — verified in the lint configs themselves.
- **P3 SR:** §4.5 checklist executed before any checkbox flip; rename-drift probe reverted and re-proven green.
- **P4/P5:** this outcome file + the tasks.md checkbox flips are the task bookkeeping; no summary files created.
- **P6 IV:** §4.6 — all printed instruction files read; approved runners only throughout (`bun run generate:gqlSchema`, `bun codegen`, `bun run scripts/health/sub-loop.ts`, `bun run test:graphql`, `bun run test/scripts/run-test.ts`); NEVER raw `bun test` as a task step; no caches cleared anywhere.
