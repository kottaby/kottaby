# Task 6 Outcome — Resolver Parent cell + call sites + Parent-cell test coverage (Task ID 6.1)

**Plan directory:** `ai/plans/milestone_3_parent_portal_admin_governance/parent-session-completion-notification-display`
**Task:** 6.1 — 4th parameter, builder cell, both call sites, Parent-cell test coverage (R-E/R-F)
**Executed:** 2026-09-18 (clock) on branch `feat/parent-session-completion-notification-display` (verified via `git branch --show-current` before any write).
**Requirements:** REQ-010 (primary), REQ-015 (regression posture — nothing realtime touched), REQ-060 (resolver-test reconciliation history honored — the out-of-band stale-arg reconciliation was NOT redone; Parent-cell coverage ADDED on the already-green suite).

---

## 1. Summary

The session matrix's SessionCompletion row gained its missing Parent audience cell as a BUILDER: `resolveNotificationRoute(relatedEntityType, notificationType?, role?, relatedEntityId?)` now composes the pure synchronous entry URL `/parent/children?session=<id>` — the portal root's linked-children route carrying the row's session id — applied ONLY when the id resolves to a non-empty string form (wire rows carry `number | null`; the numeric id is stringified; absent/null/empty-string ids fall through to the feed — the matrix NEVER fabricates a route). A new exported constant `PARENT_PORTAL_ROOT_ROUTE = "/parent/children"` single-sources the route root (leaf-module discipline, per the frontend route single-sourcing rule). Both production call sites (feed list + drawer body) thread the row's `relatedEntityId` as the 4th argument; no other call sites exist (grep re-verified — matches research-02 §2). The suite grew 8 → 16 tests, all green, with the 8 baseline tests byte-preserved.

## 2. Files modified (exactly the four authorized files + plan bookkeeping)

| File | Change |
|---|---|
| `frontend/lib/notification-route-resolution.ts` | (a) New exported constant `PARENT_PORTAL_ROOT_ROUTE = "/parent/children"` with leaf-module docblock. (b) New module-private builder `parentSessionCompletionEntry(relatedEntityId: string): string` returning `` `${PARENT_PORTAL_ROOT_ROUTE}?session=${relatedEntityId}` `` — the module's ONLY dynamic string construction. (c) New cell type `SessionRouteCell = string \| ((relatedEntityId: string) => string)`; matrix value type widened through it. (d) Matrix gains `SessionCompletion: { Student, Teacher, Parent: parentSessionCompletionEntry }` — Student/Teacher cells stay the SAME static string constants. (e) Signature gains the optional 4th parameter `relatedEntityId?: string \| number \| null`. (f) New module-private helper `relatedEntityIdStringOf()` — the id's string form: `number` → `String(n)`; non-empty string → itself; `null`/`undefined`/`""` → `null`. (g) The matrix-stage block unwraps the cell: `undefined` → feed; function → applied only for a non-empty string-form id, else feed; string → returned as-is. The role-less type stage (`NOTIFICATION_ROUTE_BY_TYPE` + its branch) and the feed fall-through are BYTE-UNCHANGED (diff-verified). Main JSDoc extended (4-param signature, builder application rule). Zero plan-artifact references in any comment/JSDoc. |
| `frontend/views/notifications/feed/NotificationList.tsx` | Call site only: `deepLinkHref={resolveNotificationRoute(notification.relatedEntityType, notification.type, userRole, notification.relatedEntityId)}` (multi-line form — exceeds the 120-col formatter width). No other line touched. |
| `frontend/components/ui/NotificationDrawerBody.tsx` | Call site only: `href={resolveNotificationRoute(item.relatedEntityType, item.type, userRole, item.relatedEntityId)}` (single-line — fits 120). The `NotificationDrawerList` docblock updated to state the id threading and the parent session-completion entry URL. No component/anatomy/behavior change — navigation stays native `<Link>`. |
| `frontend/lib/notification-route-resolution.test.ts` | Header docblock extended (builder contract). Import gains `PARENT_PORTAL_ROOT_ROUTE`. New describe `PARENT_PORTAL_ROOT_ROUTE constant` (1 test) + new describe `resolveNotificationRoute — parent session-completion deep link` (7 tests, §4.2). The 8 baseline tests are byte-preserved (diff: the only removed line is the old single-line import). |
| `tasks.md` | `6.1` + `6.1.QL/.TE/.SEC/.SR/.IV` flipped to `[x]` (and the task-start `[-]` marker). |
| `outcome/6.1-resolver-outcome.md` | This outcome. |

## 3. Files NOT modified (scope discipline)

- **The out-of-band reconciliation was NOT redone** (R-F/D1): the baseline suite's 8 tests and their assertions are untouched; no re-litigation of the RED→green history recorded at Task 0.
- Emission substrate (engine, WS envelope, services), `backend/**` at large — zero drift (R-A; `git diff --stat` over the tree shows no backend file).
- `shared/locale/**` — untouched (R-A; the notifications namespace stays byte-frozen).
- `frontend/graphql/generated/**`, documents, cache — Task 5's authorized surfaces (present as uncommitted concurrent work in the shared worktree — see §7); NOT touched by this task.
- No new repo files besides the outcome; no `AGENTS.md`/instruction files created or edited (hand-curated surfaces).

## 4. Verification evidence

### 4.1 6.1.QL — per-file sub-loop, `--lifecycle duplicates` (progressive tsgo → oxlint → biome → lint → duplicates)

| File | Exit code | Notes |
|---|---|---|
| `frontend/lib/notification-route-resolution.ts` | **0** (first run) | All five stages green; project-wide tsgo clean (0 `error TS`). |
| `frontend/lib/notification-route-resolution.test.ts` | **1 → 1 → 0** | Two gate findings, both fixed IN-FILE (no `oxlint-disable`, no cache clears, no cross-file fixes): (1) oxlint `unicorn/consistent-function-scoping` on the stateless test's non-capturing `parentEntry`/`feedCase` closures → replaced with direct calls + named result consts; (2) type-aware lint `sonarjs/no-undefined-argument` on the explicit literal `undefined` 4th argument → the nullish boundary now iterates `[null, undefined] as const` (the omitted-argument call IS the undefined arm; the loop pins both explicit nullish forms). Re-run: exit 0. |
| `frontend/views/notifications/feed/NotificationList.tsx` | **0** (first run; re-run on final bytes: **0**) | All five stages green. |
| `frontend/components/ui/NotificationDrawerBody.tsx` | **0** (first run; re-run on final bytes: **0**) | All five stages green. |

Both call-site files were re-run after the final byte state was confirmed (the formatter's canonical forms: the drawer call fits 120 cols single-line; the feed call wraps). Sub-loop discovery printed, and all were read IN FULL: `.agents/instructions/frontend.instructions.md`, `.agents/instructions/tests.instructions.md` (test file), root `AGENTS.md`, `frontend/AGENTS.md`; plus `frontend/views/AGENTS.md` (the views-layer pointer for the feed call site).

### 4.2 6.1.TE — approved runner only (NEVER raw `bun test`)

```
bun run test/scripts/run-test.ts frontend/lib/notification-route-resolution.test.ts

BEFORE (Task 0 baseline, re-confirmed at task start):  8 pass / 0 fail / 25 expect() calls
AFTER  (final bytes):                                 16 pass / 0 fail / 50 expect() calls
```

New coverage (all `(pass)`):

- **Tier 1 (Parent-cell contract):** SessionCompletion + Parent + non-empty id → `/parent/children?session=2077` (string id) AND numeric id `2077` → same URL (wire rows carry `number | null`; the id's string form composes the URL). Student/Teacher cells unchanged with an id present (`/student/sessions`, `/teacher/sessions` — static strings ignore the id). Other types + Parent + id → feed (`SessionCancellation`, `SessionRequest`, `SessionDisputeOpened`, `SessionDisputeResolved` — no Parent cells exist).
- **Tier 2 (id boundaries):** omitted 4th argument (the undefined arm), explicit `null`, explicit `undefined` (via the nullish pair), `""` → ALL feed (never a fabricated URL); `"0"` → `/parent/children?session=0` and `"42"`/`42` → `/parent/children?session=42` — a non-empty id composes the URL even when unresolvable downstream: the portal root's resolution flow owns the failure path; the resolver only refuses when NO id exists at all.
- **Tier 3 (statelessness):** repeated and interleaved resolutions — feed call, entry call, re-repeat, interleaved Student/Teacher/omitted-id calls between repeats — every result byte-identical (pure function of its arguments; the module holds only frozen consts).
- **Tier 4 (fabricated inputs):** fabricated wire type + Parent + id → feed; fabricated entity pointer (`"sessions"`) + Parent + id → feed; fabricated role + real SessionCompletion type → the role-less type stage's established static route (`/student/sessions` — pre-existing contract, pinned; never the builder); fabricated type + fabricated role → feed. No fabricated input yields the entry URL.

Regression spot-check: the consumer suite `frontend/views/dashboard/nav/navItems.test.ts` (imports `STUDENT_LINK_REQUESTS_ROUTE`) — the link-requests describe is green; see §6 for the one PRE-EXISTING unrelated failure reported to Task 9.

### 4.3 6.1.SEC — security & tenancy audit

- **Static route family:** the ONLY dynamic string in the module is the builder — `` `${PARENT_PORTAL_ROOT_ROUTE}?session=${relatedEntityId}` `` (grep-verified: one template interpolation in the file). The route root is the module constant; the sole variable is the row's session id. No title/body/name/foreign data is ever interpolated; no logger, no side effects; the module stays directive-free and framework-free (leaf discipline preserved).
- **Id domain:** the wire shape is `relatedEntityId: number | null` (`graphql.ts:960`) — a GraphQL `Int`, numeric by construction; the `string` union member covers the stringified drawer/item typing accommodation. The composed `href` is always a relative path starting `/parent/children?session=` — no scheme/protocol injection is possible, and React escapes attribute values; a malformed id lands at the portal root, whose resolution flow owns the failure path (constant-denial oracle + transient notice per REQ-020/REQ-012 — no client-side fabrication concern).
- **Tenancy:** rows for foreign tenants were never receivable — the emission substrate is byte-unchanged (zero backend drift in the tree; R-A), and the fail-closed wave + constant-denial probes are pinned by the journey suite (unchanged here). The resolver introduces no fetch, no auth surface, no data read — it composes a string.

### 4.4 6.1.SR — semantic review

- **Role-less stage byte-unchanged:** `NOTIFICATION_ROUTE_BY_TYPE` and the `!isWireRole(role)` branch + final feed return show ZERO diff hunks (verified against the pre-change file); the baseline suite's role-less test passes byte-identically.
- **Static cells unchanged:** Student/Teacher cell VALUES are the same `STUDENT_SESSIONS_ROUTE`/`TEACHER_SESSIONS_ROUTE` constants (the one-line object was re-flowed to multi-line solely to admit the Parent key — values identical).
- **No dead branches:** every resolver arm is exercised — null pointer (baseline test), entity map (baseline), matrix static cells (baseline + new), matrix builder arm with/without usable id (new), matrix miss (baseline Admin + new other-types tests), role-less stage (baseline + new fabricated-role test), final fall-through (new fabricated tests).
- **Suite green, nothing regressed:** 8/8 baseline preserved byte-for-byte + 8 new = 16/0; project-wide tsgo clean on every sub-loop run.
- **Comment hygiene:** grep over all four files for plan artifacts (`REQ-`, `R-E`/`R-F`/`R-D`, task ids, plan file names) → ZERO hits in anything this task added. One PRE-EXISTING docblock mention (`REQ-063b`, `NotificationList.tsx:36`) is baseline content outside this task's authorized call-site line — left untouched, recorded here for Task 9's grep-lock classification.

### 4.5 6.1.IV — instruction verification

All instruction/AGENTS surfaces printed by the sub-loops were read IN FULL: root `AGENTS.md` (419 lines — runner discipline, no-cache rule, deep imports, barrel rules, Fix-Or-Report contract), `frontend/AGENTS.md` (route-string single-sourcing for cross-surface navigation targets — satisfied by `PARENT_PORTAL_ROOT_ROUTE`; enum-safety type-guard pattern preserved; no `oxlint-disable`), `frontend/views/AGENTS.md` (pointer rule — read before the feed call site), `.agents/instructions/frontend.instructions.md` (MUI v9/React 19/Apollo/i18n rules — none implicated beyond the call-argument threading), `.agents/instructions/tests.instructions.md` (bun:test imports, approved runner, no-oxlint-disable, static-scan-pin staleness — grep-verified that NO static source-scan pin tests cover the resolver module, so no pin updates were owed). Fix-Or-Report: no cross-file instruction violation arose; both QL findings were fixed within the assigned file.

## 5. Contract as now shipped (frozen facts for Task 7)

- **Entry URL contract:** Parent SessionCompletion rows link to `/parent/children?session=<id>` — `<id>` is the row's `relatedEntityId` (the SESSION id; the wire `Int`, stringified). Composed by `parentSessionCompletionEntry` from `PARENT_PORTAL_ROOT_ROUTE = "/parent/children"` (both exported from `frontend/lib/notification-route-resolution.ts`).
- **Never-fabricate rule:** the builder fires ONLY for a non-empty string-form id; wire `null`, omitted argument, and `""` fall to `/notifications`. Unresolvable VALUES (`"0"`, non-numeric strings) DO compose the URL by design — the portal root owns the failure path (Task 7's Tier 2: absent/empty/malformed `?session=` → failure-notice path, never a crash).
- **Call sites:** feed `NotificationList.tsx:78` and drawer `NotificationDrawerBody.tsx:121` thread `relatedEntityId`; both navigate via native `<Link>`; the realtime toast path has NO navigation and stays untouched (REQ-015 posture — zero realtime code).

## 6. Cross-file dependencies & reports

1. **Task 7 (blocking consumer):** the root container extracts `?session=` from `searchParams` and resolves via `parentSessionTargetQueryDocument` (Task 5's document + cache entry are already in the tree). The entry URL above is the sole producer of that parameter for notification-originated navigation. Race rules per R-D (session flow owns navigation while `?session=` is pending).
2. **Task 9 (baseline comparison input — REPORT, not fixed):** `frontend/views/dashboard/nav/navItems.test.ts` has ONE failing test — "Admin broadcasts navigation > Admin navigation carries exactly one `/admin/broadcasts` entry … directly after the audit entry" (`expect(broadcastIndex).toBe(auditIndex + 1)` failing on `navItems.ts:162-167`, where an entry sits between `/audit` and `/admin/broadcasts`). Deterministic across repeated runs. **Pre-existing on the branch baseline, NOT a Task 6 regression:** `navItems.ts` and its test are byte-identical to the committed state (absent from `git status`), and the test's inputs (`navItems.ts`, dashboard labels, `UserRole`, `STUDENT_LINK_REQUESTS_ROUTE` — unchanged) are untouched by this task's delta. Same class as Task 4's `purchaseVerificationPlan` pre-existing drift — for Task 9's attribution (fix or formally reconcile).
3. **Concurrent-worktree observation (for the orchestrator):** Task 5's files (`parent-monitoring.documents.ts` + its test, `apolloCache.ts`, regenerated `gql/graphql.ts`) were present as UNCOMMITTED working-tree changes while this task executed, with Task 5's checkbox flips landing in `tasks.md` mid-flight (the same external-process pattern Tasks 3/4 recorded). Zero interference: disjoint file sets; this task touched ONLY its four authorized files + plan bookkeeping.
4. Journey J1 stays green (6/0, unaffected — no journey file touched); `docs/parents/parent-link-request.md:174`'s "never interpolates `relatedEntityId`" note describes the parent-link history and is superseded for the session-completion cell by Task 10's doc update (D6).

## 7. Protocol confirmations

- **P1 outcome read:** ALL outcome files re-read before work (`0-baseline`, `1.1`, `2.1`, `3.1`, `4.1`, `midpoint-review-R1`, `plan-review-R1`, `research-00..04`) plus `specs.md` (REQ-010/015/060), `plan.md` (§4.3, D6, §7), `tasks.md`, `deferred-items.md`.
- **P2 QL:** per-file, strict order, fix-and-re-run until exit 0 on final bytes (§4.1).
- **P3 SR:** §4.4 executed before any checkbox flip; diff-audited (role-less stage byte-unchanged; baseline tests byte-preserved).
- **P4/P5:** this outcome + the `tasks.md` flips are the task bookkeeping; no summary markdown created.
- **P6 IV:** §4.5 — all printed instruction files read; approved runners only throughout; no caches cleared; never raw `bun test`; plain `bun` binary (the `~/.bun/bin/bun` path is absent in this sandbox — Task 0 tooling note).
