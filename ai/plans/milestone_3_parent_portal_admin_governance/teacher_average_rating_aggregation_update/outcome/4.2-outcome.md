# Task 4.2 Outcome — Knowledge Propagation (canonical doc + shipped-status pointer)

**Task:** 4.2 Knowledge Propagation (Phase 4 — docs-only)
**Date:** 2026-09-19
**Branch:** `feat/teacher_average_rating_aggregation_update` — worktree content verified feat-tip-current before writing (`grep -c "aggregateLiveRatings" backend/db/repo/teachers/evaluation.repository.ts` → **2**)
**Files touched:** exactly TWO — `docs/teachers/teacher-average-rating.md` (NEW) + `docs/teachers/student-evaluation-submission.md` (ONE appended bullet) — plus this outcome file. `AGENTS.md` / `.agents/instructions/` untouched (hand-curated rule); `tasks.md` untouched (orchestrator's).

---

## Summary

Both deliverables shipped. The canonical doc for the maintained `teacher.average_rating` now exists at `docs/teachers/teacher-average-rating.md` (9 sections, house canonical-reference style, every code path + line anchor verified against the feat tip at write time). The DEV2-016 submission doc's §3 forward-contract block gained a single appended shipped-status bullet — the original contract text is untouched (history preserved), and the block's line range `:65-74` is unchanged (the pointer sits at `:75`).

## Deliverable 1 — `docs/teachers/teacher-average-rating.md` (NEW)

Section list:

1. **What the Column Is — and Is Not** — cached denormalized 0–5 average; column facts (`teacher.ts:27`, clamp `:37`); single writer (submission tx); explicitly NOT the analytics metric.
2. **Aggregation Trigger — Inside the Submission Transaction** — single-writer discipline statement + a 10-stage pipeline table with verified line anchors: pre-DB guards `:262-278`, ONE transaction `:281-283`, eligibility probe `:145`, participant oracle `:146-157`, completion gate `:166-178`, rating insert `:180-188` (score ×20 at `:185`), **aggregate `:189`**, honest-null skip `:190-195`, **convert `:196`**, **guarded update `:197`**, commit; invariant break `:198-207`; `23505` map `:289-298` outside the aggregation.
3. **Formula & Row-Selection Contract** — `average_rating = ROUND(AVG(score) / 20, 2)` quoting the origin block (`student-evaluation-submission.md:65-68`); the tier split (repo raw 0–100 mean `::float8` + `count(*)::int` at `evaluation.repository.ts:171-193`; service `/SCORE_POINTS_PER_STAR` + `toFixed(2)` at `:83,196` — SQL never rounds); the four-filter family table (`evaluated_id`, `session_id IS NOT NULL` — applicant rows never folded in, `score IS NOT NULL`, NULL-safe soft-delete exclusion) with the clean-row guarantees inherited from the origin doc (`:69-73`).
4. **The Honest-Null Ruling** — zero live ratings ⇒ `NULL`, never fabricated 0; impossible-rating argument (min 1★ = 20 ⇒ 1.00); platform precedent (`platform-analytics.repository.ts:39-41`); renderers ("—" at `adminTeachersDirectory.helpers.ts:78-80`, detail cell `:30-33`, CSV empty cells `:80`); the ticket's literal "default 0" recorded as a deferred ruling — **deferred-items ledger row D1** cited by path; unreachable-by-construction note.
5. **Concurrency Model — Recompute-from-Source** — never incremental merge (lost-update corruption); READ COMMITTED subset-safety + self-healing at the next submission; UNIQUE index arbitrates duplicates BEFORE the aggregate runs; no locks (no `FOR UPDATE`, no advisory), correctness never depends on the read staying true.
6. **Ranking Forward Contract** — FR-8.2 (`docs/specs/functional-requirements.md:238-240`) as the requirement anchor; maintained column = canonical ranking input for the future student-facing search/browse surface; that surface consumes READ-ONLY; single-writer discipline holds; **ledger row D2** cited.
7. **What NOT to Do** — no incremental updates; no separate tx/event/queue/trigger; no SQL-side rounding (no second 20); no second ad-hoc writer (the moderation/soft-delete hook composes `aggregateLiveRatings` + `updateAverageRating` in ITS transaction — **ledger row D3**); no conflation with platform-analytics' live 0–100 AVG over ALL evaluation kinds; never catch/translate the `teacher_average_rating_check` `23514`; no locks on the path.
8. **API Surface & Repository Contracts** — zero GraphQL surface; observable via existing admin reads (`adminTeachers` `:58` → `listDirectory` `:296/:314`; `adminUserDetail` `:97` → `admin-user.repository.ts:300`); both repo method signatures with their contracts (REQUIRED last `tx`, honest-null aggregate, guarded returning UPDATE, raw `23514`).
9. **References** — origin contract, schema, implementation, honest-null precedent, deferred-items ledger (D1/D2/D3), requirement anchors (INV-E4 `:317`, FR-8.2 `:238-240`), test locks, sibling canonical docs (all relative links resolve).

Zero plan-artifact ids in the body (no REQ-/Task-/plan.md§ strings); the only D-ids are deliberate ledger references ("the feature's deferred-items ledger, row D1/D2/D3") per the ledger's public vocabulary.

## Deliverable 2 — `docs/teachers/student-evaluation-submission.md` (pointer)

One bullet appended after the forward-contract block's last bullet (`:74`), matching the block's bullet formatting:

> **Shipped status:** this aggregation contract is now IMPLEMENTED by the submission flow itself — the cached average is recomputed from the live family and written to the teacher row inside the rating's OWN transaction (insert → aggregate → convert → guarded update → commit). Canonical contract: [`docs/teachers/teacher-average-rating.md`](./teacher-average-rating.md). The two repository methods (`EvaluationRepository.aggregateLiveRatings`, `TeacherRepository.updateAverageRating`) compose ONLY inside the submission's transaction — that same-transaction discipline is the single-writer guarantee over the column.

The DEV2-016 contract text is byte-identical to before (diff = +1 line only). Side-effect check: the ledger's D5 row cites this doc's `:74` — that line is still the same bullet (the pointer was appended AFTER it), so the ledger reference stays accurate.

## Link Verification (grep: every linked path exists on disk)

| Check | Result |
|---|---|
| Relative markdown links, new doc (6: submission doc ×2, platform-analytics, applicant-lifecycle, + intra-doc) | ALL RESOLVE (scripted `existsSync` pass) |
| Relative markdown links, submission doc (1: → `./teacher-average-rating.md`) | RESOLVES |
| Backticked repo paths cited as authorities (15 unique in new doc, 19 in submission doc) | ALL EXIST |
| Markdown structure (balanced code fences: 4/0) | PASS |

## Line-Number Currency Check (every cited anchor verified against the feat tip)

Scripted check of **42 line anchors + 7 path existences** — all current, including: service pipeline (`:83, :137, :145, :180, :185, :189, :196, :197, :201, :248, :262, :281, :289`), aggregate repo (`:171/:177/:181/:184/:186/:190`), teacher repo (`:296/:314/:415/:422`), schema (`teacher.ts:27/:37`, `evaluations.ts:56/:57/:60`), admin reads (`admin-teachers.query.ts:58`, `admin-users.query.ts:97`, `admin-user.repository.ts:300`), renderers (`adminTeachersDirectory.helpers.ts:78/:80`, `AdminTeacherDetailCells.tsx:30`, `teachers-directory-csv.ts:80`), specs docs (FR-8.2 `:239`, INV-E4 `:317`), and the origin block (`student-evaluation-submission.md:67/:68/:74/:75`).

**One drift found and fixed during verification:** the platform-analytics honest-null header bullet spans `:39-41`, not the `:40-42` the plan artifacts carry (1-line drift, same class as the plan-review's citation-drift findings). The doc cites `:39-41` (verified `:39` = "Rating averages are `null`…"). No other drift.

## Sub-Loop Results (4.2.QL) — recorded honestly

Mandated command, both files (raw verdicts):

```
bun run scripts/health/sub-loop.ts docs/teachers/teacher-average-rating.md --lifecycle duplicates
→ tsgo ✅ passed (project-wide, no errors for the file)
→ oxlint ❌ "No files found to lint" (exit 1)
SUB-LOOP EXIT: 1

bun run scripts/health/sub-loop.ts docs/teachers/student-evaluation-submission.md --lifecycle duplicates
→ identical: tsgo ✅, oxlint ❌ "No files found to lint", exit 1
```

The oxlint failure is a **tooling artifact, not a content finding**: oxlint has no markdown parser, so a `.md` target yields zero lintable files and oxlint exits non-zero on the empty file set — before any diagnostic could even name the file. The remaining stages are likewise n/a-by-configuration for `docs/**/*.md` (verified in the configs, not assumed):

| Stage | Evidence | Verdict for docs/*.md |
|---|---|---|
| tsgo | ran as part of both sub-loop invocations | ✅ pass (no TS to check) |
| oxlint | `bunx oxlint … docs/…md` → "No files found to lint … Finished … on 0 files", exit 1 | n/a (empty file set — no markdown support) |
| biome:check | `bunx biome check <both docs>` → "Checked 0 files … These paths were provided but ignored" | n/a (markdown not a biome target here) |
| lint:type-aware | `eslint.config.mjs` global ignores include `"docs/**"` (`:46`) | n/a (config-excluded) |
| check:duplicates | `.jscpd.json` `format: ["typescript", "tsx"]` | n/a (.md never scanned) |

Since the automated surface cannot scan markdown, the markdown-surface checks were performed manually (scripted, raw results above): link resolution, code-path existence, fence balance — **all pass**. The task's "record the rest as n/a with evidence" obligation is satisfied by this table.

## Ledger Cross-References (status quo preserved; final closing is 4.3's job)

D1 🔄 Open (ruling recorded) · D2 🔄 Open (forward contract) · D3 🔄 Open (documented seam) · D4 ✅ N/A · D5 🔄 Open (owned elsewhere). Row-level `grep -c "❌\|⚠️"` over `deferred-items.md` matches only the legend/enforcement prose (4 hits, all doc text) — **zero ❌/⚠️ ledger entries**, unchanged by this task. This task only FULFILS the "Verified By" column of D1/D2/D3 (the doc rows they name now exist and say what the rows promise).

## Subtask Checklist Evidence

- **4.2.QL** — sub-loop run on both files with raw output + n/a evidence table above; manual markdown surface checks pass. ✅
- **4.2.TE** — n/a (documentation; the test locks are cited in the doc's References). ✅
- **4.2.SEC** — the doc discloses the contract accurately: zero new denials (the only internal failure path is the pre-existing invariant break), zero new surfaces (§8), single-writer discipline + the `23514` never-caught rule made explicit (§7). ✅
- **4.2.SR** — links resolve (scripted), line refs current at write time (42-anchor scripted check, 1 drift found + fixed), no plan-artifact ids outside deliberate ledger references, DEV2-016 contract text unaltered (append-only diff). ✅
- **4.2.IV** — root `AGENTS.md` doc conventions consumed (canonical-doc house style matched against `student-evaluation-submission.md`, `applicant-lifecycle.md`, `monitoring-portal.md`); sub-loop printed rule set read (no additional AGENTS.md applies under `docs/`). ✅

*(Checkbox 4.2 in `tasks.md` intentionally left to the orchestrator per the execution protocol. Nothing committed.)*
