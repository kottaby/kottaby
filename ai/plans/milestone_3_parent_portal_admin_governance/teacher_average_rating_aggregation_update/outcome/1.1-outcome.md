# Task 1.1 Outcome — Canonical Aggregate Type

**Task:** 1.1 Canonical Aggregate Type (Phase 1 — Data Substrate)
**Date:** 2026-09-19
**Branch:** `feat/teacher_average_rating_aggregation_update`
**Files touched:** exactly one code file — `backend/types/teachers/evaluation.types.ts` (EXTEND, additive) — plus this outcome file.

## Summary of What Was Implemented

Added ONE new export to `backend/types/teachers/evaluation.types.ts`, byte-for-byte the shape specified in `plan.md` §2.3 / `specs.md` REQ-004.1:

```ts
/**
 * Single-row aggregate over one teacher's live student-rating family:
 * `averageScore` is the 0-100-scale mean (or null when no live rating
 * rows exist — never a fabricated value), `ratingCount` the live sample
 * size behind it.
 */
export interface EvaluationRatingAggregateType {
  readonly averageScore: number | null;
  readonly ratingCount: number;
}
```

- **Placement:** inserted directly after `EvaluationReturnType` and before `EvaluationSubmitInput` — following the file's existing organization (base row types → derived read shapes → client input last). Both neighbors keep their own doc-comment blocks and byte content.
- **Doc-comment:** describes domain behavior only (live rating family, 0-100 mean, honest null, sample size). Zero plan-artifact references (no REQ-004 / Task 1.1 / plan.md / specs.md strings anywhere in the file).
- **Diff:** `+11 / −0` (verified via `git diff` — pure insertion, no deletion, no modification of existing lines).

## Files Modified / NOT Modified

| File | Status | Why |
|---|---|---|
| `backend/types/teachers/evaluation.types.ts` | **MODIFIED** (+11 lines) | The only planned change: the one additive export. |
| `backend/types/teachers/teacher.types.ts` | NOT modified | Per plan §2.3 / REQ-004.2: the future `TeacherRepository.updateAverageRating` returns the existing `TeacherSelectType` via `.returning()` — no twin type. |
| `backend/types/teachers/index.ts` | NOT modified | Barrel uses `export *` — picks the interface up automatically (proof below). |
| `backend/types/index.ts` | NOT modified | Root barrel `export * from "./teachers"` (`:37`) propagates the type to `@/backend/types` with no edit. |

## Quality Loop Verification (1.1.QL)

Command: `cd /home/z/my-project && unset DATABASE_URL DB_PROVIDER && bun run scripts/health/sub-loop.ts backend/types/teachers/evaluation.types.ts --lifecycle duplicates`

**Result: exit 0 — all stages passed:**
- ✅ tsgo (project-wide run, filtered to the file): no errors
- ✅ oxlint: passed
- ✅ biome:check: passed (no formatting drift)
- ✅ lint:type-aware: passed
- ✅ check:duplicates (jscpd intra-file): passed

The sub-loop auto-printed and I read the applicable rule set: root `AGENTS.md`, `backend/types/AGENTS.md`, `backend/AGENTS.md`, `.agents/instructions/backend.instructions.md`. No conflicts: the addition is a named-interface projection type inside the existing entity type file (single-canonical-type rule respected — it is a repo projection, not a competing entity shape; precedent: other domain type files define multiple named projection interfaces). No fix iterations were needed (first run passed).

## Barrel Pickup Proof

`rg -n "export" backend/types/teachers/index.ts`:

```
1:export * from "./applicant.types";
2:export * from "./evaluation.types";
3:export * from "./teacher.types";
```

Line 2's `export * from "./evaluation.types"` re-exports the new interface; root `backend/types/index.ts:37` (`export * from "./teachers"`) chains it up to the `@/backend/types` alias. The sub-loop's tsgo stage compiled the file clean with the barrel untouched. The type-level consumer import lands in Task 2.1 (repository signature `aggregateLiveRatings(...): Promise<EvaluationRatingAggregateType>`), which is the real end-to-end pickup exercise; per the task instruction, grep + sub-loop tsgo is the recorded proof here (the `bun -e` dynamic-import route is unreliable for type-only symbols).

## Carry-Forward Knowledge for Task 2.1

1. **Import path:** `import type { EvaluationRatingAggregateType } from "@/backend/types"` — no relative paths, no barrel edits needed (chain: `@/backend/types` → `./teachers` → `./evaluation.types`).
2. **Honest-null contract:** `averageScore: number | null` — `null` means zero live rows; the service (Task 2.3) skips the teacher write on `null`. Never fabricate a value. `ratingCount` is always a plain `int` count (0 when empty ⇒ `{ averageScore: null, ratingCount: 0 }`).
3. **The `::float8` cast is load-bearing** (plan §4.2 / plan-review-R1 HIGH-1): Drizzle `sql` templates bypass column mappers and pg returns bare `numeric` as a JS string — the aggregate select MUST cast `avg(${evaluations.score})::float8` (and `count(*)::int`) so the values actually satisfy this interface's `number | null` / `number` at runtime, not just at compile time. Repo precedent: `platform-analytics.repository.ts:366-403`.
4. **Readonly fields:** both properties are `readonly` — repos must construct fresh objects, never mutate an aggregate in place.
5. **No second aggregate type:** this is the single canonical shape for the projection (REQ-004.1); do not introduce a near-twin in repo/service/test files.

## Cross-File Dependencies Discovered

**None.** The type is self-contained (primitives only, no schema imports). Downstream-only dependency: Task 2.1's `EvaluationRepository.aggregateLiveRatings` returns this type, and Task 2.3's service consumes it — both already sequenced after 1.1 in the dependency graph (`0.1 → 0.2 → 1.1 → 2.1 → …`).

## Subtask Checklist Evidence

- 1.1.QL **Quality Loop**: sub-loop exit 0 on the edited file (all five stages green, output above).
- 1.1.TE **Test Engineering**: n/a — type-level correctness is tsgo-enforced (Tier 1); no runtime tests for a pure type.
- 1.1.SEC **Security & Tenancy Audit**: n/a — pure type, no runtime surface.
- 1.1.SR **Semantic Review**: no duplicate aggregate type exists anywhere (`rg "AggregateType|StatsType|SummaryType" backend/types/**/*.types.ts` → no matches before this change; this interface is the single definition). No new type in a service file. Zero plan-artifact references in code comments.
- 1.1.IV **Instruction Verification**: `backend/types/AGENTS.md`, `backend/AGENTS.md`, `.agents/instructions/backend.instructions.md` read (self + auto-printed by sub-loop).

*(Checkboxes in `tasks.md` are intentionally left to the orchestrator per execution protocol.)*
