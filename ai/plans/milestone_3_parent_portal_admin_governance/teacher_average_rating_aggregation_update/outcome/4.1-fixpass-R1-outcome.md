# Fix Pass Outcome — Post-Implementation Review Wave (3 LOW, comment/annotation-level)

**Date:** 2026-09-19
**Branch:** `feat/teacher_average_rating_aggregation_update`
**Worktree freshness (pre-edit verification, both ≥1 as mandated):** `grep -c "aggregateLiveRatings" backend/db/repo/teachers/evaluation.repository.ts` → **2** · `grep -c "teacher-average-rating" docs/teachers/student-evaluation-submission.md` → **1**
**Files touched:** exactly TWO — `backend/db/repo/teachers/evaluation.repository.ts` (header comment rewording only) + `backend/db/test/repo/teachers/evaluation.repository.test.ts` (type annotation + import extension only). `tasks.md` untouched.
**Behavior change:** ZERO — comment rewording + a type-annotation swap to the already-canonical imported type. No executable statement altered.

---

## Finding 1 — `evaluation.repository.ts` file-header: "dual-branch exception" phrasing

The bullet wrongly said `aggregateLiveRatings` was itself a "dual-branch exception" — in this codebase's vocabulary "dual-branch" means methods that branch between tx/cold executors (cf. `platform-analytics.repository.ts` header), whereas `aggregateLiveRatings` is the OPPOSITE: a REQUIRED-`tx`, single-branch method. Reworded to name it the exception TO the dual-branch read convention, keeping the REQUIRED-`tx` rationale (result feeds the caller's same-transaction write) verbatim.

**Before** (old lines 28–37):

```
 *  - `aggregateLiveRatings` is the one dual-branch exception: a single-row
 *    SQL-template aggregate over a rated subject's LIVE student-rating
 *    family (session-linked, scored, non-deleted rows), keyed by the
 *    evaluated subject id. It takes a REQUIRED `tx` because its result
 *    feeds the caller's same-transaction write of that result — a
 *    standalone branch could read a different snapshot than the write
 *    commits against. The mean is computed in SQL (never by materializing
 *    rows and reducing them client-side) and cast `::float8`, the count
 *    `::int`, so both arrive as JavaScript numbers — a bare `numeric`
 *    mean reaches the driver as a string.
```

**After** (new lines 28–37, reflowed at same width):

```
 *  - `aggregateLiveRatings` is the one exception to the dual-branch read
 *    convention: a single-row SQL-template aggregate over a rated
 *    subject's LIVE student-rating family (session-linked, scored,
 *    non-deleted rows), keyed by the evaluated subject id. It takes a
 *    REQUIRED `tx` because its result feeds the caller's same-transaction
 *    write of that result — a standalone branch could read a different
 *    snapshot than the write commits against. The mean is computed in
 *    SQL (never by materializing rows and reducing them client-side) and
 *    cast `::float8`, the count `::int`, so both arrive as JavaScript
 *    numbers — a bare `numeric` mean reaches the driver as a string.
```

## Finding 2 — `evaluation.repository.ts` file-header: file-wide caller-scoping claim made stale by the feature

The pre-existing claim "The read is caller-scoped: the evaluator id is the ONLY filter — no parameter exists through which a caller could widen the read toward other evaluators or a chosen rated subject" was written before the new aggregate existed; the aggregate IS keyed by the chosen rated subject, so the claim was stale as a file-wide statement. Re-scoped to the method it still describes exactly.

**Before** (old lines 24–27):

```
 *    the Neon-HTTP-eligible pattern) otherwise. The read is caller-scoped:
 *    the evaluator id is the ONLY filter — no parameter exists through
 *    which a caller could widen the read toward other evaluators or a
 *    chosen rated subject.
```

**After** (new lines 24–27, reflowed at same width):

```
 *    the Neon-HTTP-eligible pattern) otherwise. In `listByEvaluator`, the
 *    read is caller-scoped: the evaluator id is the ONLY filter — no
 *    parameter exists through which a caller could widen the read toward
 *    other evaluators or a chosen rated subject.
```

## Finding 3 — `evaluation.repository.test.ts` (~line 671): inline duplicate of the canonical aggregate shape

Replaced the hand-inlined structural type with the canonical imported type. The existing `@/backend/types` import statement was extended IN PLACE (no second import of the same module added); named imports remain alphabetically ordered.

**Before**:

```
import type { DBTransaction, EvaluationSelectType } from "@/backend/types";
```

```
      const aggregates: { averageScore: number | null; ratingCount: number }[] = [];
```

**After** (import at `:92`, annotation at `:671`):

```
import type { DBTransaction, EvaluationRatingAggregateType, EvaluationSelectType } from "@/backend/types";
```

```
      const aggregates: EvaluationRatingAggregateType[] = [];
```

`EvaluationRatingAggregateType` is the same type the repository's `aggregateLiveRatings` already returns, so the annotation now cites the canonical source; the `aggregates.push(outcome.value)` and the `toEqual({ averageScore: 70, ratingCount: 2 })` assertions are untouched and type-check unchanged.

---

## Verification

### Sub-loop exits (mandated `--lifecycle duplicates`, `DATABASE_URL`/`DB_PROVIDER` unset)

| File | Result |
|---|---|
| `backend/db/repo/teachers/evaluation.repository.ts` | tsgo ✅ · oxlint ✅ · biome:check ✅ · lint:type-aware ✅ · check:duplicates ✅ → **exit 0** |
| `backend/db/test/repo/teachers/evaluation.repository.test.ts` | tsgo ✅ · oxlint ✅ · biome:check ✅ · lint:type-aware ✅ · check:duplicates ✅ (outside jscpd scan scope — skip recorded) → **exit 0** |

### Test re-run (zero-behavior-change proof)

```
bun run test/scripts/run-test.ts backend/db/test/repo/teachers/evaluation.repository.test.ts
  28 pass
   0 fail
 101 expect() calls
Ran 28 tests across 1 file.
```

**28/0 — identical to the pre-fix-pass count.** All tiers green (transactional/rollback, aggregateLiveRatings, concurrency, standalone executor paths, source-pin statics).
