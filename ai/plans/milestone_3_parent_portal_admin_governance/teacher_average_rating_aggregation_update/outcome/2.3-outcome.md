# Task 2.3 Outcome — Service: Atomic Aggregation Step (GREEN)

**Task:** 2.3 (Phase 2 — the GREEN half of the 2.2/2.3 red-green pair)
**Date:** 2026-09-19
**Branch:** `feat/teacher_average_rating_aggregation_update` (tip `164019b`). Sandbox note: HEAD sat on `main` for the whole session (the flipper never fired); the feat-committed 1.1/2.1 content was materialized into the worktree content-only (see Environment & Scope Note) — no `checkout`/`commit`/`switch`/any HEAD-changing git command was used.
**Files touched with NEW edits:** exactly TWO code files — `backend/services/teachers/student-evaluation.service.ts` (EXTEND) + `backend/services/teachers/student-evaluation.service.test.ts` (EXTEND) — plus this outcome file.
**Status: PASS** — service suite 25/0, journey **15 pass / 0 fail (GREEN, twice)**; the four RED legs 3a/3b/3c/5a flipped at exactly their `averageRating` assertions.

---

## Summary of What Was Implemented

Inside `submitWithinTransaction`, immediately after `EvaluationRepository.insertOnce`, the pipeline now closes the aggregation step — verbatim to `plan.md` §4.1, adapted to the file's real local names (the insert result IS `row`, the eligibility probe IS `probe`, and the existing happy-path return IS `return toEvaluationReturnType(row);`, so the contract needed zero renaming):

1. `const aggregate = await EvaluationRepository.aggregateLiveRatings(probe.teacherId, tx);` — the rated subject is the probe row's server-read teacher id; `tx` is the submission's own transaction (read-your-write coupling).
2. Honest-null skip: `aggregate.averageScore === null` ⇒ return the rating row without touching the teacher row (unreachable by construction — the just-inserted row is live — but the honest-null contract is defended here; domain-language comment).
3. `const averageRating = (aggregate.averageScore / SCORE_POINTS_PER_STAR).toFixed(2);` — the EXISTING `20` constant (no second constant), service-side rounding, exact 2-decimal string.
4. `const updatedTeacher = await TeacherRepository.updateAverageRating(probe.teacherId, averageRating, tx);` — same transaction.
5. On a `null` update return (no teacher row — an invariant break): one bounded `logDenial("Teacher rating aggregation failed: the rated teacher's profile row is missing", "TEACHER_PROFILE_MISSING", "teacher", probe.teacherId, locale)` entry, then `throw new Error("Student evaluation failed: the rated teacher has no teacher row")` — a PLAIN internal `Error`: not a `DomainError`, no i18n key, no catch/translation anywhere; the transaction rolls back with zero residual rows and the flow's `isUniqueViolation` catch rethrows it untouched.

Supporting amendments (same change):
- The `@/backend/db/repo` barrel import was EXTENDED in place (`import { EvaluationRepository, SessionRepository, TeacherRepository } from "@/backend/db/repo";`) — no second import statement of the module.
- Header doc-comment: the pipeline-order paragraph now names the aggregation step (re-averaging the live family, star-scale conversion, cached-average write, recompute-from-source, untranslated invariant break); the cross-surface purity paragraph now names the rated teacher's row's cached average as the second, SAME-transaction write target while keeping every remaining purity claim (zero notification/audit/wallet/ledger/session-row writes; no notification/audit imports); `logDenial`'s JSDoc now says the `entity` label follows `entityId`'s target table — `"session"` for the session-gated arms, `"teacher"` for the aggregation's missing-profile failure (first non-session use).
- The two function-level JSDocs (`submitWithinTransaction`, `submitTeacherEvaluation`) were extended with the same domain-language sentences so they stay truthful about the pipeline.

Public signatures, guard order, existing denial codes, and the return shape are UNCHANGED (the `EvaluationReturnType` six-key shape is pinned by an existing assertion that stayed green).

## Exact Diff Description

| File | Change |
|---|---|
| `student-evaluation.service.ts` | +47/−13 vs feat tip: barrel import gains `TeacherRepository`; header purity + pipeline-order paragraphs and `logDenial` JSDoc amended; both function JSDocs extended; the 20-line aggregation block inserted after `insertOnce` |
| `student-evaluation.service.test.ts` | +267/−7 vs feat tip: header coverage map gains the cached-average paragraph; `TeacherRepository` + `createTestEvaluation` join existing imports; 5 NEW cached-average tests + 1 NEW committed-fixture concurrency describe block; teacher-row snapshot instrumentation added around every existing denial leg; the barrel-members source pin maintained to the mandated 3-repo set |

## Files Modified / NOT Modified

| File | Status |
|---|---|
| `backend/services/teachers/student-evaluation.service.ts` | **MODIFIED** (the pipeline) |
| `backend/services/teachers/student-evaluation.service.test.ts` | **MODIFIED** (Tier 1–4 legs + pin maintenance) |
| `backend/db/repo/teachers/evaluation.repository.ts`, `backend/db/repo/teachers/teacher.repository.ts`, `backend/types/teachers/evaluation.types.ts` | NOT modified by this task — worktree copies are byte-identical to feat tip `164019b` (2.1/1.1's committed content; `git diff 164019b --stat` over all three is EMPTY). They appear in `git status` only because HEAD currently sits on `main` |
| `backend/db/schema/**`, `backend/drizzle/**`, SDL, frontend | NOT modified (zero-schema / zero-SDL gates hold) |
| `test/workflows/teachers/student-teacher-rating.journey.test.ts` | NOT touched by 2.3 (2.2's staged content; both runs green as-is) |
| `tasks.md` | NOT updated (orchestrator's per protocol) |

## Quality Loop (2.3.QL) — `--lifecycle duplicates`

| File | Result |
|---|---|
| `student-evaluation.service.ts` | ✅ exit 0, 1st run (tsgo · oxlint · biome:check · lint:type-aware · check:duplicates) |
| `student-evaluation.service.test.ts` | ✅ exit 0, 2nd run — 1st run caught two REAL tsgo errors in my new Tier-3 test (a `toSorted` comparator over nullable `score` members; `toContain` fed `string \| null \| undefined`). Fixed root-cause (deterministic `allSettled` order instead of sorting; a runtime string-narrowing guard throwing on read-back integrity failure, the file's established narrowers pattern); re-run all green. `check:duplicates` reports the test file as outside the jscpd scan scope (pass/skip) |

Printed instruction sets read in full: root `AGENTS.md`, `backend/services/AGENTS.md`, `backend/AGENTS.md` (+ `backend/db/repo/AGENTS.md`, `backend/db/test/AGENTS.md`, tests instructions pre-read).

## Test Run Outputs (2.3.TE) — wrapped runner only, never raw `bun test`

**Service suite:** `bun run test/scripts/run-test.ts backend/services/teachers/student-evaluation.service.test.ts` → **25 pass / 0 fail, 420 expect() calls** (pre-existing 19 tests all green unmodified-in-behavior + 6 new).

**Journey:** `bun run test/scripts/run-test.ts test/workflows/teachers/student-teacher-rating.journey.test.ts` → **15 pass / 0 fail, 172 expect() calls** — RUN TWICE, byte-identical verdicts (idempotent teardown + zero residue probe green both times). The four carry-forward legs flipped exactly as predicted by 2.2's outcome:

| Leg | Assertion | Status |
|---|---|---|
| 3a | `teacherRow.averageRating === "4.00"` (family {80}) | ✅ GREEN |
| 3b | `teacherRow.averageRating === "3.00"` ((80+40)/2/20 — recomputed) | ✅ GREEN |
| 3c | admin directory row `averageRating === "3.00"` | ✅ GREEN |
| 5a | `teacherRow.averageRating === "3.67"` ((80+40+100)/3/20 — applicant 90 excluded) | ✅ GREEN |

Step-9 (3 rows) and step-10 (28 tracked) final counts and all denial-purity byte-compares stayed green untouched, exactly as 2.2's must-NOT-change list demanded.

### Tier 1–4 Coverage Table (service tier)

| Tier | Legs |
|---|---|
| 1 — branch/statement | Happy write + read-back over the EXTENDED pipeline (existing tests green); cached average moves on the SAME tx (single 4★ ⇒ stored `"4.00"`, previously `null`); **honest-null skip:** unreachable through the public flow by construction (the just-inserted row is live) — covered by the repo-tier empty-family unit test (`{ averageScore: null, ratingCount: 0 }`, 2.1's suite) + this direct note, the task's sanctioned treatment |
| 2 — boundary/exclusion | Seeded live family {60, 50} + 5★ submission ⇒ exact `"3.50"` (recompute-from-source — an incremental merge cannot produce it); applicant (session-less, score 90) row excluded (would read `"3.83"`); soft-deleted (score 100) row excluded (would read `"4.50"`); all-20 family ⇒ `"1.00"`, all-100 family ⇒ `"5.00"` (seeded families + one same-score submission each) |
| 3 — concurrency (committed fixtures, real-PostgreSQL gated) | Two concurrent same-teacher submissions (2★/4★, different sessions) under `Promise.allSettled`: BOTH commit (2 rows; no unique conflict possible), stored value is one of the exact subset means `{"2.00","3.00","4.00"}` within numeric bounds [2.00, 4.00], CHECK never violated (both fulfilled; a clamp breach would reject); the next sequential submission converges EXACTLY to `"3.67"` ((40+80+100)/3/20 — the self-healing property); the existing concurrent duplicate storm stays green (the 23505 loser fails at the insert — the aggregate never runs, zero aggregation residue) |
| 4 — security/oracle/static | Foreign-student ≡ nonexistent-id byte-identical oracles green; write-purity oracles green over the extended pipeline; denial-log contract green (happy paths zero logs); source pins green with the barrel set maintained to `{EvaluationRepository, SessionRepository, TeacherRepository}` |

## Security & Tenancy Audit (2.3.SEC)

- **BOLA/IDOR:** the aggregated subject is `probe.teacherId` — the session row's server-read value and the SAME id the rating insert targets; the mutation takes no teacher id, so no client byte can steer the aggregate or the UPDATE.
- **BOPLA:** the teacher-row payload is member-by-member `{ averageRating, updatedAt }` in the repo; the string is server-computed (`(mean / SCORE_POINTS_PER_STAR).toFixed(2)`) — no caller object is spread anywhere on the path.
- **Purity oracles:** the existing write-purity test (zero `notifications`/`audit_logs` deltas; `users` + `session` rows byte-identical) runs the EXTENDED pipeline and stays green; every new happy path asserts zero denial logs; the teacher row's ONLY moved columns are the two sanctioned ones (proven at the repo tier's payload-minimality probe and by the journey's field-scoped probe).
- **Zero new denials:** every existing gate, code, and message is byte-identical; the ONLY new failure path is the internal invariant break — `TEACHER_PROFILE_MISSING` exists solely as a bounded `logDomainError` context value (not a client-facing `DomainError` code, no i18n key) followed by the plain internal `Error`. Every existing denial leg is snapshot-proven to write NOTHING (teacher-row byte-compare), and the 23505 loser's rollback still precedes the aggregate.
- **Injection:** inputs are an integer teacher id (parameterized) and a server-formatted decimal string — zero search/interpolation surface.
- **Disclosure:** the mutation's return shape is unchanged (the average is NOT added); the average is observable only through the existing admin reads.

## Semantic Review Checklist (2.3.SR)

- [x] **No module-level state** — the aggregation block is three locals inside `submitWithinTransaction`; no caches, no module mutable state (source pin enforces).
- [x] **No `finally`-side / post-commit aggregation** — the step is inline, between the insert and the return, inside the caller's transaction; any failure rolls back both writes.
- [x] **No second 20 constant** — the only numeric literals are the pre-existing `SCORE_POINTS_PER_STAR = 20` definition + its JSDoc (`grep`-verified).
- [x] **Atomicity / `tx` threading** — `withTransaction(outerTx, …)` → `submitWithinTransaction(tx)` → `aggregateLiveRatings(…, tx)` → `updateAverageRating(…, tx)`; the same executor end-to-end.
- [x] **No error translation** — nothing catches `23514` (or anything else) on the new path; the only new throw is the plain internal `Error` for the missing teacher row (D8: loud, untranslated, never masked; rethrown untouched by the flow's unique-violation filter).
- [x] **Zero plan-artifact references** in code comments/JSDoc — grep sweep over both files: the single match is the PRE-EXISTING source-pin regex that asserts the absence (it tests for the pattern, it doesn't reference one).
- [x] **Existing tests not weakened** — the denial tests gained ONLY snapshot instrumentation (snapshot + `toEqual`); every prior assertion remains; the only amended prior assertion is the barrel-members source pin, which the mandated import extension would otherwise have made assert a false contract (stale-pin maintenance per the tests instructions, same-change rule).
- [x] **Scope boundary** — new edits confined to the two listed files; the three repo/type worktree files are byte-identical to feat tip (content restoration only, see below); no schema/drizzle/SDL/frontend files touched.

## Environment & Scope Note (sandbox branch-flap anomaly, for the orchestrator)

HEAD remained on `main` for the entire session (the documented flipper never fired during it), so the feat-committed 1.1/2.1 content was absent from the worktree and the service step could not typecheck or run. Resolution used NO HEAD-changing command: the three feat-committed files were materialized content-only via `git restore --source=164019b --worktree -- <evaluation.repository.ts> <teacher.repository.ts> <evaluation.types.ts>`, and their worktree content is proven byte-identical to the feat tip (`git diff 164019b --stat` over all three = empty, re-verified after all test runs). They therefore carry ZERO new modifications — any `git status` entry on them is an artifact of HEAD sitting on `main`, and disappears the moment the branch tip is checked out. My two task files were never staged/committed, so branch flips cannot lose them.

## Deviation Report (fix-or-report → REPORTED)

1. **Recompute-test seed arithmetic (instruction-internal inconsistency):** the task bullet says "two rows 60/80 … after a 100-score submission the recomputed mean is exact `"3.50"` ((60+80+100)/3/20)" — but (60+80+100)/3/20 = **4.00**, not 3.50. The exact STRING `"3.50"` is the load-bearing assertion, so the seeded live rows are **60 + 50** with the 100-score submission: (60+50+100)/3 = 70 ⇒ 70/20 = `"3.50"`. Intent preserved (exact recomputed mean over a seeded family + a 100-score submission); no spec/plan wording is affected (REQ-011.2 pins only "exact recomputed mean").
2. **Sub-loop iteration:** the test file needed one fix cycle (two tsgo errors in my new Tier-3 test — nullable-score comparator and optional-string narrowing); both fixed root-cause, no lint suppressions, second run exit 0.

## Carry-Forward for Task 3.1 (SDL + schema zero-surface gates)

- Run `bun run generate:gqlSchema` ⇒ `git diff --stat frontend/graphql/generated/schema.graphql` MUST be empty (the mutation signature, input, and `Evaluation!` return are untouched — nothing on this flow can move the SDL).
- `git status backend/db/schema/ backend/drizzle/` MUST show nothing (no schema/migration/snapshot edit anywhere in this plan).
- Re-run the DEV2-016 wire suite: `bun run test:graphql` (`backend/graphql/test/student-evaluation.wire.test.ts` role/oracle matrix) must stay green UNMODIFIED — the extension added no auth-bypass leg (student-only `$all` gate unchanged).
- Branch caveat: run the gates while the worktree reflects the feat tip (verify with the content markers, e.g. `aggregateLiveRatings` in the evaluation repo), per the flip anomaly above.
- Suites to cite in 4.3's table (all current in this state): evaluation repo 28/0, teacher repo 28/0 (2.1's runs), service 25/0 (this task), journey 15/0 ×2 (this task), wire suite (3.1's re-run).

*(Checkbox in `tasks.md` intentionally left to the orchestrator per the execution protocol. Nothing committed.)*
