# Task 2.2 Outcome — Journey Test Extension (test-first, RED)

**Task:** 2.2 (Phase 2 — journey-first RED leg of the 2.2/2.3 red-green pair)
**Date:** 2026-09-19
**Branch:** `feat/teacher_average_rating_aggregation_update` @ `04de3e3` (types `b753828` + repo methods in history)
**Files touched:** exactly ONE code file — `test/workflows/teachers/student-teacher-rating.journey.test.ts` (EXTEND, no new file, no new cast, no registry changes) — plus this outcome file.
**Status: PASS (RED-as-expected)** — the four new cached-average legs fail at exactly the `averageRating` assertions; every pre-existing step stays green.

---

## Summary of What Was Implemented

The existing 10-step journey was extended with the cross-actor cached-average arc. The new legs are interleaved into the flow (renumber-free: `3a/3b/3c/4a/5a`) rather than appended after step 9, because the exact-average arithmetic pins their positions: the family must evolve `{80}` → `{80,40}` → `{80,40,100}`, and the race leg's existing 5★ winner (score 100) IS the "third rating" — appending after step 9 would make the `"4.00"`/`"3.00"` assertions unachievable (the 100-score row would already be committed). This matches the spec's journey step order (first rating → `"4.00"` → second session 2★ → `"3.00"` → admin check → denied legs → teardown).

New/changed surface, all inside the one file:

1. **Step 3a (NEW, RED)** — after the existing 4★ submission: `TeacherRepository.findById` cold read ⇒ `averageRating === "4.00"` (one live row, 80/20).
2. **Step 3b (NEW, RED)** — books + dual-confirms a SECOND session with the same teacher through the journey's existing `bookTrackedSession`/`driveToDualConfirmation` helpers (new per-run idempotency key `KEY_RECOMPUTED`), rates it 2★ through the real service, tracks the row, ⇒ `averageRating === "3.00"` ((80+40)/2/20 — recomputed, not incremented).
3. **Step 3c (NEW, RED)** — admin-observation leg: `TeacherRepository.listDirectory({}, 10, 0)` (raw repo read, no GraphQL tier) ⇒ the cast teacher's projected row carries `averageRating === "3.00"` (`AdminTeacherDirectoryRow.averageRating: string | null`).
4. **Step 4a (NEW, GREEN)** — commits the applicant-evaluation exclusion bait: `createTestEvaluation(tx, teacher.id, secondTeacher.id, null, { score: 90 })` inside its own committing `db.transaction` (journeys hold no outer rollback), `registry.track`ed. The evaluator is the second cast teacher so neither student's read-back assertion is perturbed.
5. **Step 5 (EXTENDED, GREEN)** — race-purity instrumentation: snapshot before the `Promise.allSettled` pair; after the race, a field-by-field payload-minimality probe (id/isApproved/isEvaluator/isOnline/subjects/requestPreference/createdAt byte-identical) — scoped to exclude `averageRating` + `updatedAt` because the race's WINNER legitimately moves exactly those two columns once the service ships; the exact post-race value is pinned separately by step 5a. Pre-2.3 the probe passes (nothing writes); post-2.3 it still passes (the sanctioned two-column move) — the instrumentation never has to be rewritten.
6. **Step 5a (NEW, RED)** — the race's winning 5★ joins the live family {80, 40} ⇒ `averageRating === "3.67"` ((80+40+100)/3/20, exact string — proves the committed applicant row's 90 was excluded); then re-reads the applicant row (still on record, `sessionId` null, score 90 — excluded, not removed).
7. **Denial-purity snapshots (EXTENDED, GREEN)** — steps 4 (sequential duplicate), 6 (scheduled), 7 (stamp-only), 8 (oracle) each snapshot the teacher row via `TeacherRepository.findById` immediately before the denial and byte-compare with `toEqual` after (the file's existing snapshot style, cf. step 8's evaluation-row compare). Step 7's snapshot sits AFTER the completion handshake because the start flow moves the teacher's availability flag — the DENIAL must leave the row identical, not the handshake.
8. **Step 9 (EXTENDED, GREEN)** — the rater's list is now exactly 3 submitted rows, newest first (raced 100 / recomputed 40 / rated 80); the newest and oldest per-field blocks are byte-identical to before, the middle row's block was added.
9. **Step 10 (EXTENDED, GREEN)** — the teardown-worklist truth: 5 sessions, 5 idempotency claims, 4 evaluations (3 ratings + 1 applicant row), trackedCount 14 + 5 + 5 + 4 = **28**.
10. **Fixture capacity** — the cast's primary student provisions `{ trial: 2, hifz: 3 }` (5 escrow lanes for 5 bookings under the trial-first debit ladder; comment updated).
11. **New helpers** (mirroring the file's existing helper style): `readTeacherRow()` (cold repo read), `requiredTeacherRow()`, `requiredApplicantEvaluation()` narrowers.

Every new row created mid-journey is `registry.track`ed: the 2★ rating row, the applicant evaluation row (the second session + its idempotency claim are tracked inside the existing `bookTrackedSession` helper). NO `runInRollback` anywhere; the only `runInRollback` string in the file is the pre-existing header's "NO runInRollback" layer-contract line.

## Why the RED legs sit where they sit (placement proof)

| Leg | Required family state | Flow position |
|---|---|---|
| `"4.00"` (one live row, 80) | `{80}` — before the race's 5★ exists | after step 3, before step 5 |
| `"3.00"` ((80+40)/2/20) | `{80,40}` | step 3b, before step 5 |
| directory `"3.00"` | `{80,40}` | step 3c, before step 5 |
| `"3.67"` ((80+40+100)/3/20) | `{80,40,100}` | the race winner IS the third 5★ submission; the applicant row (90, `sessionId: null`) commits in step 4a BEFORE the race so the winner's recompute provably skips it |

## RED Analysis

**RED (4 — all at exactly the `averageRating` assertions, all `Expected: "X.XX" / Received: null` — the service pipeline does not yet write the cached average):**

| Step | Assertion | Failure |
|---|---|---|
| 3a | `expect(teacherRow.averageRating).toBe("4.00")` | `Expected: "4.00" Received: null` |
| 3b | `expect(teacherRow.averageRating).toBe("3.00")` (LAST expect in the step — everything before it, including the 2★ row commit + tracking, executed) | `Expected: "3.00" Received: null` |
| 3c | `expect(directoryRow?.averageRating).toBe("3.00")` | `Expected: "3.00" Received: null` |
| 5a | `expect(teacherRow.averageRating).toBe("3.67")` | `Expected: "3.67" Received: null` |

**GREEN (11):** pre-existing steps 1-10 (with the consequential step-9/step-10 truth extensions) AND the new fixture step 4a. The denial-purity snapshots (steps 4/5/6/7/8) pass pre-2.3 — the denial paths write nothing — and are 2.3-stable by construction.

## Files Modified / NOT Modified

| File | Status |
|---|---|
| `test/workflows/teachers/student-teacher-rating.journey.test.ts` | **MODIFIED** (+192/−17) |
| `test/workflows/helpers/**` (incl. `journey-fixture-registry.ts`) | NOT modified (no registry change — `evaluations` + `teacher` already tracked) |
| `backend/services/teachers/student-evaluation.service.ts` | NOT modified (that is 2.3) |
| `backend/db/schema/**`, `backend/drizzle/**`, SDL | NOT modified (zero-schema / zero-SDL gates hold) |

The −17 deleted lines are fully accounted for: the two comment blocks (booking keys, escrow lanes), the lane-profile value `{trial:2,hifz:2}`→`{trial:2,hifz:3}` (fixture capacity for the fifth booking), and the step-9/step-10 count-truth extensions (`toHaveLength(2)`→`3`, `ownRows[1]`→`ownRows[2]`, session/claim/evaluation arrays + `trackedCount 24`→`28`) that the new fixtures make factually necessary. No existing assertion logic was removed or weakened; the raced/rated per-field blocks in step 9 and all denial assertions in steps 4-8 are byte-identical to before.

## Quality Loop (2.2.QL)

`cd /home/z/my-project && unset DATABASE_URL DB_PROVIDER && bun run scripts/health/sub-loop.ts test/workflows/teachers/student-teacher-rating.journey.test.ts --lifecycle duplicates`

**Result: exit 0 — all stages passed (1st run, no fix iterations):**
- ✅ tsgo (project-wide, filtered): no errors for the file
- ✅ oxlint
- ✅ biome:check (import ordering + formatting clean)
- ✅ lint:type-aware
- ✅ check:duplicates (file outside the jscpd scan scope — reported as passing/skipped by the loop)

Printed rule sets consumed: `test/workflows/AGENTS.md`, `AGENTS.md`, `.agents/instructions/tests.instructions.md` (+ `backend/db/repo/AGENTS.md`, `backend/AGENTS.md`, backend/tests instructions pre-read).

## Journey Run Outputs (2.2.TE) — wrapped runner, two consecutive runs

Command: `bun run test/scripts/run-test.ts test/workflows/teachers/student-teacher-rating.journey.test.ts`

**Run 1** (14:36 log) and **Run 2** (idempotency proof) — byte-identical verdicts:

```
(pass) step 1  — fixture cast commits as tracked ids with real role rows
(pass) step 2  — the student books, the teacher completes, the student confirms
(pass) step 3  — the student rates the dual-confirmed session: one row, score = rating × 20
(fail) step 3a — the first rating's commit maintains the rated teacher's cached average   [RED: "4.00" vs null]
(fail) step 3b — a second dual-confirmed session rated 2★ recomputes the average          [RED: "3.00" vs null]
(fail) step 3c — the admin teacher directory observes the maintained average              [RED: "3.00" vs null]
(pass) step 4  — a sequential re-submit is denied as already-submitted and writes nothing (teacher-row byte-compare green)
(pass) step 4a — an applicant evaluation of the same teacher exists outside the rating family
(pass) step 5  — two concurrent submits: exactly ONE row, one loser denied (field-scoped purity probe green)
(fail) step 5a — the race's winning rating joins the family: the recompute excludes the applicant evaluation [RED: "3.67" vs null]
(pass) step 6  — a `scheduled` session is denied as not-completed (teacher-row byte-compare green)
(pass) step 7  — a teacher-stamp-only session is denied the same way (teacher-row byte-compare green)
(pass) step 8  — another student + unknown id: byte-identical not-found denial (teacher-row byte-compare green)
(pass) step 9  — the rater's evaluation list is exactly the submitted rows, newest first (3 rows)
(pass) step 10 — teardown worklist is complete (5 sessions / 5 claims / 4 evaluations / 28 tracked)

 11 pass / 4 fail · 170 expect() calls · ~0.5s
```

**Idempotent-teardown proof:** run 2 reproduced the exact same 11/4 pattern (fresh `jrn_teachers_<8hex>` prefix, no collisions), and a post-run sweep over the shared test database shows ZERO residue on every table the suite touches: `teacher=0, evaluations=0, session=0, session_request_idempotency=0, users=0, students=0, applicants=0, parents=0, admin=0, wallet=0, notifications=0, teacher_transaction=0`. The `afterAll` hard-delete + zero-residue self-check passed in both runs (a leaking teardown fails the suite — it did not).

## Security & Tenancy Audit Notes (2.2.SEC)

- **Cross-actor visibility asserted:** the student's action (rating submission) moves a value a DIFFERENT actor observes (admin directory projection, step 3c); the race leg proves a foreign rater gains nothing (step 8 oracles, `foreignRows` empty) and the rated teacher's row never moves outside the sanctioned two columns (step 5's payload-minimality probe).
- **Denial purity:** five denial probes (sequential duplicate, concurrent duplicate loser, scheduled, stamp-only, foreign/oracle) each prove the teacher row untouched — the denial paths write nothing even after the service gains the aggregation step (the loser's 23505 rollback happens before the aggregate could run).
- **No seed rows, no stubs:** every row is a committed fixture via the entity-setup factory or the real services; negative steps fail through real `DomainError` denials (`expectServiceDenial` — code + exact translated message).
- **No new surface, no new denial codes, no i18n keys, no module-level mutable state beyond the file's existing fixture-capture pattern** (the new `let` variables mirror `sessionRated`/`raceRating`; nothing is cached across suites).

## Semantic Review Checklist (2.2.SR)

- [x] **Fixtures committed + tracked:** the 2★ rating row, the applicant evaluation row, the second session and its idempotency claim (via `bookTrackedSession`) are all `registry.track`ed; `evaluations` + `teacher` were already tracked by the registry (no registry change made).
- [x] **No seed rows:** the applicant row is created via `createTestEvaluation` (entity-setup factory) inside the journey flow, committed in its own short `db.transaction` — commit-or-nothing, tracked, FK-safe cleanup order (evaluations delete before their RESTRICT evaluator's user row).
- [x] **Existing steps not weakened:** diff reviewed hunk-by-hunk — additions + the mandated snapshot instrumentation + the two count-truth extensions (step 9 list length, step 10 worklist) only; the `git diff` above is the evidence.
- [x] **No module-level state introduced** (beyond the file's sanctioned fixture-capture lets); no `runInRollback`; no `expect().rejects`; no `console.*`; no `any`; `@/` imports only.
- [x] **Teacher-row reads through the repo:** `TeacherRepository.findById` (cold, optional-`tx` unused) and `TeacherRepository.listDirectory` — no raw `teacher` table import added to the journey.
- [x] **Scope boundary:** `git status --porcelain` = exactly the one journey file. Branch verified = `feat/teacher_average_rating_aggregation_update` after every write (the sandbox branch-flap anomaly re-occurred repeatedly this session; each flip was re-checked-out, working-tree changes never affected).
- [x] **Comment hygiene:** zero plan-artifact references (`rg "REQ-|Task 2\.|tasks\.md|plan\.md|specs\.md|DEV2-|milestone"` → no matches); domain language only.
- [x] **2.2.IV:** `test/workflows/AGENTS.md` + printed tests instructions read in full before editing.

## Carry-Forward for Task 2.3 (service: atomic aggregation step)

**Exactly these FOUR assertions flip green when the service ships** (each fails today with `Expected: "X.XX" Received: null`):

1. **Step 3a** `expect(teacherRow.averageRating).toBe("4.00")` — flips when `submitWithinTransaction`, after `insertOnce`, aggregates the live family (one row, score 80) and writes `TeacherRepository.updateAverageRating(probe.teacherId, (80/20).toFixed(2), tx)` in the SAME transaction.
2. **Step 3b** `expect(teacherRow.averageRating).toBe("3.00")` — flips when the second submission's recompute over {80, 40} lands ((80+40)/2/20) — the exact string also pins recomputed-from-source over an incrementing merge producing a stale scale.
3. **Step 3c** `expect(directoryRow?.averageRating).toBe("3.00")` — flips with the same write, observed through the admin directory's projected row (different actor's surface, zero code change there).
4. **Step 5a** `expect(teacherRow.averageRating).toBe("3.67")` — flips when the race WINNER's recompute over {80, 40, 100} lands, EXCLUDING the committed applicant row (score 90, `sessionId: null`); simultaneously proves the 23505 loser's rollback left no aggregation residue (a residue or subset-average overwrite would not equal the full-family recompute).

**Must-NOT-change list for 2.3 (already final):**
- The denial-purity instrumentation in steps 4/6/7/8 (full `toEqual` byte-compares) and step 5's field-scoped probe (all columns except `averageRating`/`updatedAt`) are 2.3-stable — do not touch them.
- Step 9 (3 rows) and step 10 (28 tracked) are already at their final values — a 2.3 diff showing edits there means something regressed.
- The applicant row's evaluator is the second cast teacher deliberately — keep it (the students' read-back assertions depend on it).
- Escrow lanes stay `{ trial: 2, hifz: 3 }` — five bookings, trial-first debit ladder.

**Expected post-2.3 journey run:** 15 pass / 0 fail; expect() count rises from 170 to ~172 (step 5a's two trailing applicant-row checks execute once the RED assertion ahead of them passes). Both repo suites and the service suite must stay green in the same state; then run the journey twice again for the teardown proof.

**Environment note for 2.3:** the sandbox branch-flap anomaly is ACTIVE — re-verify `git branch --show-current` after every command chain and re-checkout `feat/teacher_average_rating_aggregation_update` when it lands on `main`; unset `DATABASE_URL DB_PROVIDER` before any bun command.

*(Checkbox in `tasks.md` intentionally left to the orchestrator per the execution protocol. Nothing committed.)*
