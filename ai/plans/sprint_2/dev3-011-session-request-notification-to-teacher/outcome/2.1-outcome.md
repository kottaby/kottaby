# Task 2.1 Outcome — Cross-Actor Journey (TEST-FIRST RED) + Stub Scaffold

- **Task:** `2.1` (Phase 2 — journey written BEFORE the real service; stub scaffold makes tsgo pass so RED is runtime, not compile).
- **Executed:** 2026-09-02. tasks.md checkboxes NOT flipped (orchestrator flips after the wave).

## What Was Built

1. **Stub scaffold** (plan-review R1 amendment A3):
   - `backend/services/classes/session-request-notification.service.ts` — `export namespace SessionRequestNotificationService` with the SIX methods at FINAL signatures `(sessionId: number, locale: string, tx?: DBTransaction, options?: NotificationEngineCallOptions): Promise<NotificationDeliveryReceipt>` (`notifyTeacherOfSessionRequest`, `notifyStudentOfSessionAccepted`, `notifyStudentOfSessionDeclined`, `notifyStudentOfSessionAutoRejected`, `notifyStudentOfSessionQueued`, `notifyStudentOfAlternativesOffered`). Every body delegates to one private `throwWaveStub(emitterName): never` that throws `DomainError("INTERNAL_SERVER_ERROR", "<emitter> test-first stub — implemented in task 2.3")` (the shared helper keeps the six identical throws under the jscpd threshold). Parameters are `_`-prefixed (tsconfig `noUnusedParameters` + lint `argsIgnorePattern: ^_`); 2.3 renames them when it replaces bodies. Imports: `DomainError` (VALUE) from `@/backend/lib/errors`; `type NotificationEngineCallOptions` from `@/backend/services/notifications`; `type DBTransaction, NotificationDeliveryReceipt` from `@/backend/types`.
   - `backend/services/classes/index.ts` — `export * from "./session-request-notification.service";` (new domain barrel).
   - `backend/services/index.ts` — ONE surgical line appended in alphabetical position: `export * from "./classes";` (between `./billing` and `./shared`).

2. **Journey** — `test/workflows/classes/session-request-notifications.journey.test.ts` (one file; new dir `test/workflows/classes/`):
   - Actor cast in ONE committing `beforeAll` `db.transaction` with `jrn_sessreq_<uuid8>` prefixing: Student S (`createTestUser` role=student, `locale:"en"` + `createTestStudent`); Teacher T (`createTestUser` role=teacher, `locale:"ar"` + DIRECT `teacher` insert `isApproved:true`); U/V/W (`createTestUser` role=teacher, `locale:"en"` + DIRECT teacher inserts with `requestPreference` `Reject`/`Queue`/`OfferAlternatives` — VALUE imports of the `TeacherRequestPreference` enum); isolation observers X/Y via the actor-context factory (`provisionStudentActor` / `provisionCertifiedTeacherActor` with `tracked`); five `session` rows via DIRECT inserts (S↔T hifz, S↔U tajweed, S↔V evaluation, S↔W hifz, + a dedicated corrupt-probe row S↔T) with `intent` set via the `SessionIntent` VALUE enum import. 19 rows registered in `TrackedFixtures`.
   - Injection seams: `SpiedFanoutTransport` + suite-local `MapBackedClaimCache` (SET-NX-EX semantics + `claimedKeys` recording, modeled on `ScriptedClaimCache`) injected as `{ transport, cache }` through every emitter's `options` param.
   - 9 `test` blocks mapping 1:1 to specs §2.9 steps 1-9: cast-committed oracle (19 tracked rows, empty inboxes, zero publishes); teacher wave (Arabic title/body recomposed via `getServerTranslations("ar").notificationsTranslations`, carrying S's fullName + `intentHifz`; ONE publish to `[T.id]`); replay under held key (prior receipt id equal, counts unchanged); accept wave (English copy naming T, publish `[S.id]`); decline wave (SECOND distinct append-only row; T's inbox unchanged); three preference waves (correct counterparty names U/V/W, six pinned deterministic claim keys via `buildEmitClaimKey`); isolation invariance (X/Y/U/V/W zero rows, no envelope to a non-participant); denial probes (missing id → `SESSION_NOT_FOUND` + exactly-one domain log via a `spyOn(logger, "logDomainError")` recorder; hostile `0`/`-1`/`NaN` → `VALIDATION` with zero logs; corrupt intent → `SESSION_INTENT_CORRUPT` — all error assertions via `catchJourneyError`, zero rows/publishes deltas asserted); teardown via `tracked.cleanup()` (reverse-registration hard delete + `verifyAllAbsent` re-probe).
   - ZERO `runInRollback`, ZERO `console.*`, ZERO `.rejects.toThrow()`.

## QL Evidence (`sub-loop --lifecycle duplicates`, all exit 0)

| File | tsgo | oxlint | biome | lint:type-aware | duplicates |
|---|---|---|---|---|---|
| `backend/services/classes/session-request-notification.service.ts` | ✅ | ✅ | ✅ | ✅ | ✅ |
| `backend/services/classes/index.ts` | ✅ | ✅ | ✅ | ✅ | ✅ |
| `backend/services/index.ts` | ✅ | ✅ | ✅ | ✅ | ✅ |
| `test/workflows/classes/session-request-notifications.journey.test.ts` | ✅ | ✅ | ✅ | ✅ | ✅ (outside jscpd scope) |

## RED Evidence (mandated runner)

`bun run test/scripts/run-test.ts test/workflows/classes/session-request-notifications.journey.test.ts` → **1 pass / 8 fail, 20 expect() calls, exit 1** (`logs/2026-09-02T00-15-58/...log`).

- **step 1 PASSES** — the cast provisions, all inboxes read zero, spy reads zero: the harness/fixtures are proven healthy.
- **steps 2-6 fail with the exact stub throw**: `DomainError: <emitter> test-first stub — implemented in task 2.3` / `code: "INTERNAL_SERVER_ERROR"`, stack traces pointing at `session-request-notification.service.ts:23` (`throwWaveStub`) and the emitter call sites in the journey. Runtime failures, NOT compile errors.
- **steps 8a/8b fail on error-CLASS assertions**: `expected NotFoundError (got DomainError: ...stub...)` / `expected ValidationError (got DomainError: ...stub...)` — `catchJourneyError` captured the stub throw and the instanceof assertion failed, exactly the intended RED.
- **step 7 fails on the envelope-set assertion** (empty spy vs expected participants) because no wave ran.
- **afterAll teardown ran clean** — no `TrackedFixtures` residue errors in the run, so `verifyAllAbsent` proved zero residue for the 19 fixture rows.
- tsgo was already GREEN (sub-loop above) → the RED is runtime-only, satisfying the A3 design.

## Rulings Recorded (standing conditions)

1. **Journey instruction-conflict resolution (R1 LOW finding / standing condition 5):** `getServerTranslations` IS used in this journey for copy-assertion composition and error-substring assertions. Ruling honored: the more-specific layer wins — `test/workflows/AGENTS.md` (rule 6: assert translated substrings from `getServerTranslations("en").errorsTranslations`) governs journey files and overrides `tests.instructions.md:29` and `backend/db/test/AGENTS.md:130` inside `test/workflows/`.
2. **Fixture ruling (R1 amendment A4) applied:** helpers where they exist (`createTestUser`, `createTestStudent`, `provisionStudentActor`, `provisionCertifiedTeacherActor`); DIRECT committed Drizzle inserts inside the SAME committing `beforeAll` tx for what no helper provisions (all five `session` rows; teacher rows incl. U/V/W `requestPreference` variants; T's `users.locale="ar"` / S's `locale="en"` pinning via `createTestUser` overrides). All rows tracked in `TrackedFixtures`.
3. **Corrupt-intent mechanics ruling (new, recorded here):** `session.intent` is a NATIVE pgEnum (`session_intent`), so a non-member literal cannot be stored at all (Postgres rejects it at write time, 22P02). The only persistable corrupt state is NULL, and R1 amendment A6's null-first check (`row.intent === null || !isSessionIntent(row.intent)`) explicitly routes NULL → `SESSION_INTENT_CORRUPT`. The journey therefore corrupts the probe fixture via a direct committed `UPDATE ... SET intent = null` (documented in the test body itself).
4. **REQ-094 clause (ii) NOT journey-scoped** (R1 amendment A9): cache-absent fail-open warn is deferred to the service tier (task 2.3.TE).

## Carry-Forward for 2.3

- REPLACE the six stub bodies with the planned `emitWave` choreography; the journey will flip green once: rows land with the pinned copy (`title`/`body` recomposed assertions are already exact), publishes target exactly `[recipientId]`, replay returns the prior receipt id, claim keys match the six pinned `session:<id>:<waveKind>` digests, and the denial probes produce `NotFoundError("SESSION", …)` / `ValidationError(t.validation)` / `ValidationError("SESSION_INTENT_CORRUPT", …)` with exactly ONE `logDomainError` per not-found/corrupt rejection.
- The journey's engine options object (`{ transport: spy, cache: claimCache }`) must pass through to `NotificationEngine.emitForUser` unchanged, on BOTH the caller-tx and own-commit branches.
- `test/workflows/classes/` now exists; no helper extension was needed (all fixtures fit the ruling).

## Files Touched

| File | Action |
|---|---|
| `backend/services/classes/session-request-notification.service.ts` | CREATED (86-line stub) |
| `backend/services/classes/index.ts` | CREATED |
| `backend/services/index.ts` | MODIFIED (one `export * from "./classes";` line) |
| `test/workflows/classes/session-request-notifications.journey.test.ts` | CREATED (~595 lines, RED-by-design) |
| `ai/plans/sprint_2/dev3-011-session-request-notification-to-teacher/outcome/2.1-outcome.md` | CREATED (this file) |

Cross-file dependencies: **none.**
