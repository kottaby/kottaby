# Task 6.2 Outcome — Review-Backend Wave (Post-Implementation)

- **Task scope (tasks.md 6.2):** `backend/services/classes/session-request-notification.service.ts`, `backend/db/repo/classes/session.repository.ts`, their tests, the journey. Requirements REQ-010..REQ-016, REQ-040..REQ-044, REQ-050..REQ-053.
- **Executed:** 2026-09-02. READ-ONLY audit; zero source edits. Every prior outcome file (0-baseline → 5.2, plan-review-R1) was read first; R1 amendments A1–A13 and the 2.3 deviations D1/D2 were treated as adjudicated (not re-flagged) and instead audited for faithful implementation.

## Empirical Re-Verification (mandated runner only)

`~/.bun/bin/bun run test/scripts/run-test.ts <path>`:

| Suite | Result |
|---|---|
| `backend/db/repo/classes/__tests__/session.repository.test.ts` | **10 pass / 0 fail, 49 expect()** (~227 ms) |
| `backend/services/classes/session-request-notification.service.test.ts` | **20 pass / 0 fail, 346 expect()** (~261 ms) |
| `test/workflows/classes/session-request-notifications.journey.test.ts` | **9 pass / 0 fail, 95 expect()** (~230 ms) |

## Check-by-Check Audit (all line cites re-read from the shipped code)

### (1) Single-writer — PASS

Grep of the service file shows the ONLY write/emit calls are `NotificationEngine.emitForUser` at `session-request-notification.service.ts:211` (caller-tx) and `:221` (no-tx). No `insert`, no `publishReceipts`, no direct fanout/transport call, no `console.*`. The repo module contains zero write verbs (grep `insert|update|delete` → only the `updatedAt`/`createdAt` column aliases at `session.repository.ts:61`). Engine-side (`notification-engine.service.ts:43-78`) is the sole `NotificationRepository.createReturning` caller in the flow.

### (2) Caller-tx receipt / publish-after-commit choreography — PASS

- Caller-tx branch (`:210-219`): hands `tx` to the engine, returns the receipt, NEVER publishes. The engine's own tx-branch (`notification-engine.service.ts:65-68`) confirms it persists inside the caller tx and returns the receipt without publishing.
- No-tx branch (`:221-225`): the `"notifications" in result` guard sits ONLY here (replay ⇒ prior receipt returned verbatim — journey step 3 pins `replayedRow.id === teacherWaveRowId` with publishCount unchanged at 1, journey `:344-359`; fresh ⇒ bare `NotificationReturnType` wrapped `{ notifications: [result], recipientUserIds: [recipient.userId] }`). `emitClaimKey` is correctly omitted from the wrapped receipt — `notification.types.ts:91-94` documents own-commit receipts keeping the field absent.
- **A7-deviation judgment (D1):** A7 pinned `return result` verbatim on the caller-tx path; plan pseudocode cannot compile because `emitForUser` returns the `NotificationReturnType | NotificationDeliveryReceipt` union (`notification-engine.service.ts:48`) and `as`-casts are prohibited. The shipped `!("notifications" in result)` → typed `DomainError("INTERNAL_SERVER_ERROR", …)` guard at `:212-216` preserves A7's real constraints: receipt returned verbatim, NEVER published on caller-tx, no wrap fallback, no silent masking. It is NOT the A2-style untestable dead branch — Tier 3 pins it via `spyOn(NotificationEngine, "emitForUser").mockResolvedValue(bareRow)` (`session-request-notification.service.test.ts:795-810`) asserting code `INTERNAL_SERVER_ERROR` and exactly-one engine call. Ruling: deviation faithfully implements A7's intent. **No finding.**

### (3) Tx propagation — PASS

One `tx: DBTransaction | undefined` threaded per call: emitter (`:233/243/253/263/273/283` signatures) → `emitWave` (`:187`) → `resolveWaveContext` (`:60`) → `SessionRepository.findWaveContextById(sessionId, tx)` (`:68`) and → `emitForUser(input, recipientLocale, tx, options)` (`:211`; `undefined` + same `options` at `:221`). No mixed executors, no shadowing re-read outside the tx. Repo honors both driver splits (`session.repository.ts:50`, `:92`).

### (4) Error taxonomy + logging contract — PASS

Exactly three domain codes, matching plan §3.3 table row-for-row:
- `VALIDATION` — pre-DB `ValidationError(tErrors.validation)` on hostile id (`:64-66`); ZERO logs pinned (`session-request-notification.service.test.ts:442-463, 494-518`, repo-spy zero-calls at `:840`; journey step 8b).
- `SESSION_NOT_FOUND` — ONE message-first `logDomainError("Session not found for notification wave", { code, entity:"session", entityId, locale })` (`:70-75`) + `NotFoundError("SESSION", tErrors.sessionNotFound)` (`:76`; exactly-one-log + zero-rows/zero-publishes pinned at test `:399-401` and the int4-ceiling boundary test `:486-488`).
- `SESSION_INTENT_CORRUPT` — null-first guard `row.intent === null || !isSessionIntent(row.intent)` (`:79`) per R1-A6 (guard file untouched — `git status` clean on `backend/types/contracts/`); ONE log (`:80-85`) + `ValidationError("SESSION_INTENT_CORRUPT", …)` (`:86`); pinned at test `:429-433`.
- Happy path silent: `logs.records` asserted `[]` on happy paths (e.g. test `:362`) and journey step-1 snooper; only engine-owned `NOTIFICATION_IDEMPOTENCY_DEGRADED` warn exists (A9, Tier 3, test `:739-745` region).
- `errorsTranslations.internalServerError` (used by the breach guard, `:215`) exists (`shared/locale/types/errors/index.ts:30`) — typed lookups only, no hand-rolled strings.

### (5) No module-level mutable state — PASS

Grep for `let ` / `var ` / module-scope `new`/`Map` initializers on the service file → none. Module body is imports, four pure functions, and one `export namespace` (`:228-288`).

### (6) Repo driver-split correctness — PASS

- Non-tx `findWaveContextById` SQL (`session.repository.ts:124-133`) is byte-faithful to plan §4.1: `SELECT s.id AS "sessionId", s.intent AS "intent", su.id/full_name/locale, tu.id/full_name/locale FROM session s JOIN users su ON su.id = s.student_id JOIN users tu ON tu.id = s.teacher_id WHERE s.id = $1 LIMIT 1`. No inline comments, no prepared statements, no `inArray`/`sql.placeholder`, no LIKE/ILIKE (grep-verified).
- Tx branch joins (`:93-108`): module-level `alias(users, "wave_student_user")` / `alias(users, "wave_teacher_user")` (`:34-35`) with the correct FK direction — student alias joined on `session.studentId` (`:105`), teacher alias on `session.teacherId` (`:106`), `eq(session.id, sessionId)` + `limit(1)`.
- Field-by-field mapping on BOTH branches (`:113-122`, `:138-147`) — every one of the 8 `SessionWaveContextRow` fields mapped explicitly; no spreads; `intent` stays `string | null` untrusted.
- `findById` non-tx statement (`:54-63`) lists exactly the 15 session columns with correct snake→camel aliasing, matching the 0.2-verified schema inventory (`backend/db/schema/classes/session.ts:32-63`).

### (7) RULE — the two `throw new Error(...)` exhaustiveness guards (:112, :166) — ACCEPTABLE (rule: NOT a finding)

Both are the house idiom, character-for-character: `default: { // Exhaustiveness guard comment…; const exhaustive: never = <union>; throw new Error(\`Unexpected …: ${String(exhaustive)}\`); }` — the cited precedent `backend/services/auth/registration.service.ts:300-304` reads identically. Both sit behind a statically-provable `never` (the closed `SessionIntent` enum / `SessionRequestWaveKind` union exhaustive `case` sets immediately above), so they are compile-time-anchored never-guards, NOT runtime error paths — they cannot be entered without a type-system breach, and unlike `SESSION_NOT_FOUND`/`SESSION_INTENT_CORRUPT` they are not domain projections (no caller translation surface promised by plan §3.3's four-row code table). The oxlint `consistent-return` constraint (recorded in the 2.3 outcome) is exactly why the idiom exists. **Ruling: acceptable pattern; no fix required.** (2.M recorded this as a non-blocking observation; this wave closes it as adjudicated-accepted.)

### (8) No locking / caching mechanisms — PASS

Grep over `backend/**/classes/**` for `for update|pg_advisory|advisory` (case-insensitive) → zero hits. No Redis `SET NX`, no module caches. The only atomic claim is the engine's own (`emit-idempotency.ts`), consumed untouched per plan §4.3's explicit non-mechanisms.

## Findings

**None.** Zero new defects surfaced; both live adjudications (2.3's D1/D2 deviations, the 2.M exhaustiveness observation) were audit-checked and hold.

VERDICT: CLEAN
