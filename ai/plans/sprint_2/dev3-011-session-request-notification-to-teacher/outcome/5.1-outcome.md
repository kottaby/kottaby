# Task 5.1 Outcome — Full Differential Gate (engine regression + new suites + freeze suites, one pass)

- **Task:** `5.1` (Phase 5 — Integration & Differential Testing). Verification-only: ZERO source/suite edits. tasks.md checkbox intentionally NOT flipped (orchestrator flips).
- **Executed at:** 2026-09-02 ~05:05–05:25 EEST
- **Runner:** every suite via `~/.bun/bin/bun run test/scripts/run-test.ts <path>` — no raw `bun test`, zero `test:ui*`/e2e involvement.
- **Requirements:** REQ-070, REQ-071, REQ-072, REQ-073, REQ-074, REQ-075 (partial)

## 1. Engine Regression — ALL UNEDITED, ALL GREEN (consumption-not-modification proof)

Command pattern (each suite individually): `~/.bun/bin/bun run test/scripts/run-test.ts <path>`

| Suite | Result |
|---|---|
| `backend/services/notifications/notification-engine.emit.test.ts` | ✅ 35 pass / 0 fail (35 ran) |
| `backend/services/notifications/notification-engine.inbox.test.ts` | ✅ 18 pass / 0 fail (18 ran) |
| `backend/services/notifications/notification-engine.chaos.test.ts` | ✅ 7 pass / 0 fail (7 ran) |
| `backend/services/notifications/realtime/fanout-degradation.test.ts` | ✅ 4 pass / 0 fail (4 ran) |
| `backend/services/notifications/realtime/fanout-transport.factory.test.ts` | ✅ 7 pass / 0 fail (7 ran) |
| `backend/services/notifications/realtime/fanout-transport.test.ts` | ✅ 3 pass / 0 fail (3 ran) |
| `backend/services/notifications/realtime/in-process-transport.test.ts` | ✅ 6 pass / 0 fail (6 ran) |
| `backend/services/notifications/realtime/ioredis-fanout-client.test.ts` | ✅ 3 pass / 0 fail (3 ran) |
| `backend/services/notifications/realtime/redis-pubsub-transport.test.ts` | ✅ 13 pass / 0 fail (13 ran) |
| `backend/ws/notification-ws-server.test.ts` | ✅ 25 pass / 0 fail (25 ran) |

**Engine-transport regression total: 10 suites, 121 pass / 0 fail.** Engine, realtime fanout transports, and the WS sidecar are untouched by this ticket (git-clean per 2M §2) and regress-free.

## 2. New Suites — ALL GREEN

| Suite | Result |
|---|---|
| `backend/db/repo/classes/__tests__/session.repository.test.ts` | ✅ 10 pass / 0 fail, 49 expect() |
| `backend/services/classes/session-request-notification.service.test.ts` | ✅ 20 pass / 0 fail, 346 expect() |
| `test/workflows/classes/session-request-notifications.journey.test.ts` | ✅ 9 pass / 0 fail, 95 expect() |
| `shared/locale/notifications-namespace.parity.test.ts` | ✅ 85 pass / 0 fail, 476 expect() |
| `shared/locale/errors-namespace.parity.test.ts` | ✅ 8 pass / 0 fail, 82 expect() |

## 3. Freeze Suites (task-3.1 set, UNEDITED) — pre-existing drift IDENTICAL to 3.1

| Suite | Result | vs 3.1 |
|---|---|---|
| `backend/graphql/test/schema-surface.test.ts` | ⚠️ 29 pass / 4 fail (33 ran) | **IDENTICAL — PASS for this ticket.** Same 4 failing legs as 3.1-outcome §2: `Query._health — retyped probe surface`, `mutation set is EXACTLY the refreshed frozen baseline`, `enum set is pinned`, `whole-schema named-type delta is pinned`. Same counts (29/4/33). The `Codegen sync — committed SDL is byte-identical` leg remains GREEN. Zero NEW failures. |
| `backend/graphql/test/sdl-static-assertions.test.ts` | ⚠️ 15 pass / 3 fail (18 ran) | **IDENTICAL — PASS for this ticket.** Same 3 failing legs: mutation-root diff, query-root diff, and the `NO Subscription root` naive-substring false-positive (`hasActiveSubscription` field inside `AdminStudentSnapshot`). Same counts (15/3/18). Zero NEW failures. |
| `backend/graphql/test/handshake-code-surface.test.ts` | ✅ 22 pass / 0 fail | matches 3.1 |
| `backend/graphql/test/plan-catalog.schema.test.ts` | ✅ 5 pass / 0 fail | matches 3.1 |
| `backend/lib/gateway/public-operations.test.ts` | ✅ 26 pass / 0 fail | matches 3.1 |

The 7 failing legs are the documented pre-existing baseline drift (stale pinned inventories post-DEV1-013 / #28 / #32), NOT ticket-attributable — the failure SETS and COUNTS are byte-identical to `outcome/3.1-outcome.md`. D6 ledger posture stands; suites left 100% UNEDITED.

## 4. Orchard Spot-Guard

| Suite | Result |
|---|---|
| `backend/graphql/test/notification-integration.matrix.test.ts` | ✅ **23 pass / 0 fail, 416 expect()**, ~4 s test time |

Environment resolution required for this suite (all environment-level, ZERO source edits):

1. **Port 3066 squat:** a FOREIGN worktree's dev server (`/home/ahmed/Projects/kottaby_kottaby`) had `next dev -p 3066` LISTENING but unresponsive (`curl` → connection failure), causing the suite's own bootstrap (`setupTestServerLifecycle`) to hit its 120 s `beforeEach` timeout on the first bare run.
2. **Turbopack symlink block:** booting THIS worktree's `bun run dev` fails with `TurbopackInternalError: Symlink [project]/node_modules is invalid, it points out of the filesystem root` — `node_modules` here is a symlink into the sibling worktree (`/home/ahmed/Projects/kottaby_kottaby/node_modules`). The webpack dev server works: `next dev --webpack -p 3099`.
3. **Unseeded test DB:** `kottaby_test` was migrated but unseeded; the suite's seeded-admin login answered UNAUTHORIZED. Ran `bun run db seed` (exactly what CI does before `test:graphql` — `.github/workflows/ci.yml` tests-graphql job) — seed completed, admin `[EMAIL_REDACTED]` row created and wire login verified via curl.

Final working invocation: `GRAPHQL_TEST_PORT=3099 ~/.bun/bin/bun run test/scripts/run-test.ts backend/graphql/test/notification-integration.matrix.test.ts` against the live webpack dev server, per the suite's own header note about pointing at a live server via `GRAPHQL_TEST_PORT`. Dev server killed after the run; suite itself UNEDITED.

## 5. Verdict

**5.1 PASSES.** Engine regression 121/0 (UNEDITED), new suites 132/0, freeze-suite green legs green, the 7 pre-existing drift legs identical to 3.1 (no new failures), orchard matrix 23/0 (UNEDITED). All evidence logs under `logs/2026-09-02T05-1*` / `logs/2026-09-02T05-2*/` captured by the mandated runner.

## 6. Files Touched

| File | Action |
|---|---|
| `ai/plans/sprint_2/dev3-011-session-request-notification-to-teacher/outcome/5.1-outcome.md` | CREATED (this file) |

Environment side effects (non-file): `kottaby_test` seeded per CI convention; temporary dev server on port 3099 started and killed; no repo file outside this outcome was written.
