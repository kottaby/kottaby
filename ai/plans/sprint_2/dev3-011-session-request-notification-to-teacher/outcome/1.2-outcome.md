# Task 1.2 Outcome — i18n Keys (notifications extension + errors flat keys + parity suite, ONE changeset)

- **Task:** `1.2` (Phase 1 — i18n)
- **Completed:** 2026-09-01
- **Standing conditions honored:** plan-review R1 amendment A8 applied verbatim (five stale parity-suite sites updated in the SAME changeset; new Arabic function-slot pins mirror the `:180-186`-style `ARABIC_SCRIPT.test(...)` regex assertions, NOT the `:192-238` exact-string pins).

## What Was Implemented

### notifications namespace — 15 new keys (REQ-013 exact inventory)

`shared/locale/types/notifications/index.ts` — appended to `NotificationsLabels` (after `reconnectedQuietly`), each with a JSDoc note:
- Six TITLE string slots: `eventSessionRequestTitle`, `eventSessionAcceptedTitle`, `eventSessionDeclinedTitle`, `eventSessionAutoRejectedTitle`, `eventSessionQueuedTitle`, `eventSessionAlternativesOfferedTitle`.
- Six BODY function slots: `eventSessionRequestBody: (studentName: string, intentLabel: string) => string`; `eventSessionAcceptedBody`, `eventSessionDeclinedBody`, `eventSessionAutoRejectedBody`, `eventSessionQueuedBody`, `eventSessionAlternativesOfferedBody` — each `(teacherName: string) => string`.
- Three INTENT label string slots: `intentHifz`, `intentTajweed`, `intentEvaluation`.

`shared/locale/en/notifications/index.ts` + `shared/locale/ar/notifications/index.ts` — concrete copy for all 15 keys, matching the existing leaf style:
- en titles/bodies; e.g. `eventSessionRequestTitle: "New session request"`, `eventSessionRequestBody: (studentName, intentLabel) => \`${studentName} requested a ${intentLabel} session with you.\``.
- ar copy mirrors; all ar string slots contain Arabic script; all ar function slots return Arabic-script output (proven by the parity pins). Intent labels: `"الحفظ"` / `"التجويد"` / `"التقييم"` — consistent with existing in-tree wording (`ar/adminUsers` `balanceHifz: "رصيد الحفظ"`, `averageRating: "متوسط التقييم"`; en `Hifz`/`Tajweed` per `en/plans`, `en/landing`, `en/adminUsers`).

### errors namespace — 2 new FLAT keys (REQ-051)

- `shared/locale/types/errors/index.ts` — appended at the end of `ErrorsLabels` (after `trialAlreadyGranted`), flat, mirroring the `notificationNotFound`/`studentHandshakeNotFound` precedent: `sessionNotFound` ("— session lookup miss → NotFoundError(\"SESSION\")") and `sessionIntentCorrupt` ("fail-closed deny when a stored sessions.intent value is not a known SessionIntent member"). No nested grouping invented; `ErrorMessageKey` picks both up automatically.
- `shared/locale/en/errors/index.ts`: `sessionNotFound: "The session was not found."`, `sessionIntentCorrupt: "This session could not be read. Please contact support."` (mirrors `notificationTypeCorrupt` copy shape).
- `shared/locale/ar/errors/index.ts`: `sessionNotFound: "لم يتم العثور على الجلسة."`, `sessionIntentCorrupt: "تعذر قراءة هذه الجلسة. يرجى التواصل مع فريق الدعم."` (mirrors ar `notificationTypeCorrupt`).

### Parity suite extension (same changeset)

`shared/locale/notifications-namespace.parity.test.ts`:
1. `MANDATED_KEYS` 26 → 41 (all 15 new keys appended after `reconnectedQuietly`).
2. `FUNCTION_KEYS` 4 → 10 (the six new body function slots appended).
3. The exactly-seven `type*`-prefix pin stays green untouched (new keys are `event*`/`intent*`-prefixed — verified: the `typeKeysOnMap` assertion still passes).
4. Five stale count sites updated: the "(26 slots)" comment → "(41 slots)"; the `:148` title → "no silent key minting beyond the 41 slots"; the `:180` title → "all ten ar FUNCTION slots return Arabic-script output for Arabic-flavored arguments" (now iterates `FUNCTION_KEYS` with per-locale sample args, keeping the `ARABIC_SCRIPT.test(...)` regex-assertion style); the `:240-249` callable pin → "all ten function slots are callable with non-empty output in BOTH locales" (iterates `FUNCTION_KEYS`); the `:276` describe title → "exactly the ten locale functions, on BOTH maps".
5. New fixture + helper: `FUNCTION_SLOT_SAMPLE_ARGS` (per-key `{ en, ar }` argument tuples; ar args are Arabic-flavored, e.g. `["أمينة", "الحفظ"]`) and `callFunctionSlot(localeMap, key, args, localeName)` — invokes via `Reflect.apply` with runtime `typeof` guards (no type assertion; oxlint `no-unsafe-type-assertion` fix per root-cause rule).
6. The exact-string plural/template pins (`:192-238` region: `unreadCount`, `markAllResult`, `markReadAriaLabel`, `realtimeToast`) were left UNCHANGED.

## Files Touched

1. `shared/locale/types/notifications/index.ts` (15 slots added)
2. `shared/locale/en/notifications/index.ts` (15 keys)
3. `shared/locale/ar/notifications/index.ts` (15 keys)
4. `shared/locale/types/errors/index.ts` (2 flat keys)
5. `shared/locale/en/errors/index.ts` (2 keys)
6. `shared/locale/ar/errors/index.ts` (2 keys)
7. `shared/locale/notifications-namespace.parity.test.ts` (extension + 5 stale sites)
8. `ai/plans/sprint_2/dev3-011-session-request-notification-to-teacher/outcome/1.2-outcome.md` (this file)

## Files NOT Modified (and Why)

- `shared/locale/errors-namespace.parity.test.ts` — auto-discovers keys (sorted-set `toEqual` + recursive non-empty walk); adding flat keys keeps it green without edits (verified — it passes).
- `shared/locale/types/message.ts`, `shared/locale/namespaces/**`, `server.ts`, `serverLegacy.ts`, `app/[locale]/layout.tsx` — NO new namespace was created (extension of two EXISTING namespaces; the forbidden list prohibits a new namespace), so the namespace-registration checklist steps 3–5 are N/A.
- `backend/types/contracts/contract-guards.ts` — never edited (R1 standing rule; not in scope anyway).
- `backend/types/classes/*` (dirty in `git status`) — task 1.1's parallel work by another agent; not touched.

## Verification (exact commands + results)

Per-file quality loop — `~/.bun/bin/bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates`:

| File | Exit |
|---|---|
| `shared/locale/types/notifications/index.ts` | 0 |
| `shared/locale/en/notifications/index.ts` | 0 |
| `shared/locale/ar/notifications/index.ts` | 0 |
| `shared/locale/types/errors/index.ts` | 0 |
| `shared/locale/en/errors/index.ts` | 0 |
| `shared/locale/ar/errors/index.ts` | 0 |
| `shared/locale/notifications-namespace.parity.test.ts` | 0 (first run exit 1 — oxlint `no-unsafe-type-assertion` on the initial `callFunctionSlot` cast; fixed by replacing the assertion with `Reflect.apply` + runtime `typeof` guards, then re-run to 0) |

Tests (mandated runner only):
- `~/.bun/bin/bun run test/scripts/run-test.ts shared/locale/notifications-namespace.parity.test.ts` → **85 pass, 0 fail** (476 expect calls) — run again after the Reflect.apply fix, still 85/0.
- `~/.bun/bin/bun run test/scripts/run-test.ts shared/locale/errors-namespace.parity.test.ts` → **8 pass, 0 fail** (82 expect calls).

SEC (1.2.SEC): copy templates interpolate ONLY `studentName` / `teacherName` / `intentLabel` — grep-verified; no email/phone/id/balance/governance strings anywhere in the new copy.
SR (1.2.SR): key names are EXACTLY the REQ-013 inventory; `ErrorsLabels` additions are FLAT; en/ar trees structurally identical (compile-typed both leaves against the same interfaces + runtime parity belt green).
IV (1.2.IV): read and honored `shared/AGENTS.md` (compile-time i18n system, pluralization/interpolation function pattern, no next-intl/getBackendTranslations/shared/messages), `shared/locale/AGENTS.md` (leaf modules = typed consts, `@/shared/locale/...` imports), `.agents/instructions/backend.instructions.md` (i18n rules), `.agents/instructions/tests.instructions.md` (bun:test, no `any`, run-test script). Forbidden token sweep on the parity suite for `next-intl`/`getBackendTranslations`/`shared/messages`/`26`/`four` → zero matches.

## Cross-File Dependencies

None. All changes were in-scope.

## Carry-Forward for Task 2.3 (Emitter Copy Usage)

Wave → key mapping the service layer must use (via `getServerTranslations(recipientLocale).notificationsTranslations`, per REQ-014):

| Emitter | title | body | body args |
|---|---|---|---|
| `notifyTeacherOfSessionRequest` | `eventSessionRequestTitle` | `eventSessionRequestBody(student.fullName, intentLabel)` | intentLabel = `intentHifz`/`intentTajweed`/`intentEvaluation` chosen by the row's validated `SessionIntent` |
| `notifyStudentOfSessionAccepted` | `eventSessionAcceptedTitle` | `eventSessionAcceptedBody(teacher.fullName)` | teacherName |
| `notifyStudentOfSessionDeclined` | `eventSessionDeclinedTitle` | `eventSessionDeclinedBody(teacher.fullName)` | teacherName |
| `notifyStudentOfSessionAutoRejected` | `eventSessionAutoRejectedTitle` | `eventSessionAutoRejectedBody(teacher.fullName)` | teacherName |
| `notifyStudentOfSessionQueued` | `eventSessionQueuedTitle` | `eventSessionQueuedBody(teacher.fullName)` | teacherName |
| `notifyStudentOfAlternativesOffered` | `eventSessionAlternativesOfferedTitle` | `eventSessionAlternativesOfferedBody(teacher.fullName)` | teacherName |

Intent-label selection: `SessionIntent.Hifz` → `intentHifz`, `SessionIntent.Tajweed` → `intentTajweed`, `SessionIntent.Evaluation` → `intentEvaluation` (runtime enum VALUE import in the service).

Error copy for task 2.3's fail-closed branches (via the same locale bundle, `errorsTranslations`):
- `NotFoundError("SESSION", errorsTranslations.sessionNotFound)` → code `SESSION_NOT_FOUND`.
- `ValidationError("SESSION_INTENT_CORRUPT", errorsTranslations.sessionIntentCorrupt)`.

Notes:
- The `errors`-namespace parity suite stays green structurally — new flat keys need no test edits.
- No genuinely NEW locale-namespace rule surfaced during 1.2; per tasks.md task 7.2's OPTIONAL clause, record "no `shared/AGENTS.md` clarification line needed" unless 7.2 finds otherwise.
