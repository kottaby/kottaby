# Task 7.2 Outcome — AGENTS Propagation (REQ-082)

- **Task:** `7.2` (Phase 7 — Knowledge Propagation). Docs/propagation only: three surgical AGENTS.md edits + two deliberate no-edit decisions. Checkboxes NOT flipped (orchestrator flips after the wave).
- **Executed:** 2026-09-02 after reading all 17 files in `outcome/` (0-baseline → 6.4 + plan-review-R1), per the mandatory first step.
- **Standing conditions honored:** plan-review-R1 amendment A12 / standing condition 4 — the stale `classes/` row in `backend/db/repo/AGENTS.md` was EXTENDED in place (no new row minted).

## Edit 1 — `backend/services/AGENTS.md` (one new rule line)

Inserted ONE bullet immediately after the existing real-time-notifications single-writer rule (keeps the two notification-domain rules adjacent, matching the file's topic-grouped bullet style; there is no separate "canonical reference docs" list in this file — pointers ride inline at the end of each rule line, so the doc link follows that local convention).

Before (`:10`, unchanged):
```
- **Real-time notifications (single writer)**: `NotificationEngine` (`backend/services/notifications/`) is the ONLY writer of `notifications` rows — … See `docs/notifications/realtime-engine.md`.
```

After — new line added at `:11`:
```
- **Session-request notification waves**: session-request/response notification waves live exclusively in `SessionRequestNotificationService` (`backend/services/classes/`) — its emitters never authorize (internal primitives; recipients derive server-side from the session row via the joined wave-context read) and never publish inside a caller transaction (caller-tx calls return a receipt; the CALLER publishes receipts post-commit via `NotificationEngine.publishReceipts`). See `docs/notifications/session-request-notifications.md`.
```

Claims grep-verified against the tree:
- `export namespace SessionRequestNotificationService` — `backend/services/classes/session-request-notification.service.ts:228`
- `NotificationEngine.publishReceipts` — `backend/services/notifications/notification-engine.service.ts:117`
- Joined wave-context read — `SessionRepository.findWaveContextById` at `backend/db/repo/classes/session.repository.ts:88`
- "Never publish inside a caller transaction" matches the shipped code: the module's only engine calls are `emitForUser` (6.2 check #1: no `publishReceipts` call anywhere in the service; caller-tx returns the receipt verbatim per deviation D1).

**Forward-pointer note (resolved):** `docs/notifications/session-request-notifications.md` did not exist when this task began (task 7.1 was still in flight), so the pointers were added as deliberate forward references — the Phase-7 peer tasks ship in the same ticket, so no "absent-doc" caveat was attached per the local convention. **Verified before close: task 7.1 has now landed the doc** (`docs/notifications/session-request-notifications.md` present on disk, untracked-new; its content — six-emitter wave table, recipient-locale composition, caller-tx receipt / publish-after-commit — matches the pointer descriptions verbatim). The cross-reference is satisfied.

## Edit 2 — `backend/db/repo/AGENTS.md` (stale `classes/` layout row EXTENDED, not duplicated)

The layout tree at `:21` listed the OLD billing-era contents of `classes/` (class-category, class-performance, class-subject, group-class, dst-migration, subject-resource) while reality is that none of those files exist there and the directory now contains only `session.repository.ts` + `index.ts` + `__tests__/` (verified: `ls backend/db/repo/classes/` → `index.ts  session.repository.ts  __tests__`). The row was updated in place — no second `classes/` row was minted anywhere (grep-verified: exactly one `classes/` row in the layout block).

Before (`:21`):
```
├── classes/             class-category, class-performance, class-subject, group-class, dst-migration, subject-resource
```

After (`:21`):
```
├── classes/             session.repository.ts (SessionRepository: findById + findWaveContextById joined wave-context read)
```

Verified against the shipped repo: `backend/db/repo/classes/session.repository.ts:37` (`export namespace SessionRepository`), `:49 findById`, `:88 findWaveContextById` — exactly the two methods named.

## Edit 3 — root `AGENTS.md` (Important References)

ONE line appended after the existing `docs/notifications/realtime-engine.md` entry (keeps both notification docs adjacent; matches the list's `path — description` format; no plan identifiers, REQ-numbers, or plan paths in production-facing content):

```diff
 - `docs/notifications/realtime-engine.md` - Real-time notification engine (WebSocket): persist-first/push-second, single-writer emit contract, sidecar topology, fail-open idempotency deviation
+- `docs/notifications/session-request-notifications.md` — Session-request notification waves canonical reference (six emitters in `SessionRequestNotificationService`, joined wave-context repository read, recipient-locale copy composition, closed payload, deterministic idempotency keys, caller-tx receipt / publish-after-commit contract)
```

New line now at `AGENTS.md:419`. The description summarizes scope consistent with the shipped contract (closed payload / `relatedEntityType: "session"` / deterministic `session:<id>:<waveKind>` keys / publish-after-commit per 2.3 outcome; recipient-locale composition per REQ-014/D2).

## Deliberate NON-edits (recorded per task mandate)

1. **`test/workflows/AGENTS.md` — left UNTOUCHED.** The journey did NOT surface a genuinely new harness rule:
   - The A4 fixture ruling (DIRECT committed Drizzle inserts for rows no factory can provision — sessions, teacher `requestPreference` variants) is explicitly **plan-scoped** ("plan D9 citation corrected to 'this plan's own fixture ruling, ratified at Phase 1.5 plan-review R1'"). Generalizing it into the harness AGENTS.md would weaken rule 4 (honest provisioning via the actor-context factories) for all future journeys; the correct stance is helpers-first, direct inserts only under an adjudicated plan ruling.
   - The instruction-conflict resolution (`getServerTranslations` allowed in journeys) is ALREADY a hard rule — rule 6 mandates asserting via `getServerTranslations("en").errorsTranslations`; 2.1's outcome recorded the more-specific-layer-wins adjudication. Nothing new to add.
   - The corrupt-intent NULL fixture is session-domain-specific technique, not a harness rule; `MapBackedClaimCache` mirrors the pre-existing `ScriptedClaimCache` precedent at suite scope. No harness-level generalization warranted.
2. **`shared/AGENTS.md` optional clarification — intentionally SKIPPED.** The 1.2 outcome's forward note states the evidence: "No genuinely NEW locale-namespace rule surfaced during 1.2" (extension of two existing namespaces + parity-suite site updates is already covered by existing locale rules). Per the OPTIONAL clause ("only if trivially safe … else record not-done"), no line was added. Not a deferral to anything — the clause's condition was never met.

## Verification

- `git diff -- AGENTS.md backend/services/AGENTS.md backend/db/repo/AGENTS.md` — exactly three hunks: +1 line (services), 1-line replacement (repo layout row), +1 line (root reference). Zero other churn.
- Grep of every changed line confirms content lands in the intended file/section (services bullet at `:11`; repo row at `:21`; root line at `:419`).
- All code claims in the new lines grep-verified against shipped files (see Edit 1/2 verdicts above).
- Markdown sanity: pure `-` bullets / fenced-tree line edits; no headings, tables, or fence counts altered; renders as before.
- No rules contradicted: services bullet is consistent with the single-writer rule it follows (it narrows "where waves may be composed," not who writes rows); repo layout row matches the real directory; root line matches list format.
- No tests run — docs/propagation only (per task contract).

## Files Touched

| File | Action |
|---|---|
| `backend/services/AGENTS.md` | MODIFIED (+1 rule line with canonical-doc pointer) |
| `backend/db/repo/AGENTS.md` | MODIFIED (stale `classes/` layout row updated in place) |
| `AGENTS.md` (root) | MODIFIED (+1 Important References line) |
| `ai/plans/sprint_2/dev3-011-session-request-notification-to-teacher/outcome/7.2-outcome.md` | CREATED (this file) |

NOT touched: `test/workflows/AGENTS.md`, `shared/AGENTS.md`, `docs/**` (the canonical doc is 7.1's artifact), tasks.md checkboxes, deferred-items ledger, any source/test file.

## Cross-File Dependencies

- **7.1 has landed** `docs/notifications/session-request-notifications.md` — the pointers added here (services AGENTS.md `:11` + root Important References `:419`) are satisfied on disk; no dangling links.
- Nothing else outstanding for orchestrator follow-up from this task.
