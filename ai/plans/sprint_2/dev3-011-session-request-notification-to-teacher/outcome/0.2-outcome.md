# Task 0.2 — Anchor Verification Outcome

Read-only verification of all anchors cited by tasks 0.2. No repo file was modified; this file is the only write. Counts: 20 VERIFIED · 3 DRIFTED · 0 MISSING. Nothing blocks.

| # | Anchor | Status | Evidence (actual location / content) |
|---|--------|--------|--------------------------------------|
| 1a | engine `emitForUser` signature + branch semantics | DRIFTED (lines) | File is 182 lines; `emitForUser(input, locale, tx?, options?)` at `notification-engine.service.ts:43-78` |
| 1b | caller-tx branch: receipt returned, NO publish | VERIFIED | `:65-68` returns `{notifications:[row], recipientUserIds, emitClaimKey}` — no publish call |
| 1c | own-commit: commit + store claim + publish once | VERIFIED | `:70-77` — `withTransaction(undefined, …)` commits, then `publishReceiptsFromIndex([receipt], 0, locale, options)` at `:76`; receipt-storage lives at `notification-engine.publish.ts:54-59` |
| 1d | replay handling (~:302-304/:343-345) | DRIFTED (lines) | Now `notification-engine.service.ts:52-60` (`claimOrPriorReceipt` → `priorReceipt` early return) + `notification-engine.helpers.ts:34-54`; `keyed receipt` = `emitClaimKey` attached at `:67`/`:74` |
| 1e | `publishReceipts` (~:475-481) → `publishReceiptsFromIndex` (~:612-645) | DRIFTED (lines) | `publishReceipts` at `notification-engine.service.ts:117-123`; `publishReceiptsFromIndex` at `notification-engine.publish.ts:43-66`; publish-on-rows at `:61-64` |
| 2 | `IDEMPOTENCY_KEY_MAX_LENGTH = 128` (~:40); exported `isPositiveSafeInt` (~:50-52) | VERIFIED | `emit-validation.ts:40` `const IDEMPOTENCY_KEY_MAX_LENGTH = 128;` — NOTE: NOT exported (module-private). `isPositiveSafeInt` exported at `:50-52`: `typeof value === "number" && Number.isSafeInteger(value) && value > 0` |
| 3a | `SessionSelectType` at `types/classes/session.types.ts:3` | VERIFIED | `export type SessionSelectType = typeof session.$inferSelect;` |
| 3b | `types/classes/index.ts` re-exports six files | VERIFIED | home-work, lesson, progress, recitation, report, `session.types` |
| 4 | `isSessionIntent` guard `contract-guards.ts:76-78` | VERIFIED | `export function isSessionIntent(value: string): value is SessionIntent { return VALID_SESSION_INTENTS.has(value); }` — exact lines 76-78 |
| 5a | `SessionEventNotificationContract` `session-notification.contract.types.ts:42-52` | VERIFIED | Fields: `userId` (BOLA, server-resolved), `type: SessionEventNotificationType`, `title`, `body`, `idempotencyKey?`, `entityRef: SessionEventNotificationEntityRef` (both-or-neither union; `isRead` absent by contract) |
| 5b | `contracts.conformance.test-d.ts` + `contracts.static-assertions.test.ts` exist | VERIFIED | Both present in `backend/types/contracts/` (dir listing) |
| 6a | `SessionIntent` members `session-intent.enum.ts` | VERIFIED | `Hifz = "hifz"`, `Tajweed = "tajweed"`, `Evaluation = "evaluation"` (`:7-9`) |
| 6b | `NotificationType.SessionRequest = "session_request"` `:6` | VERIFIED | `notification-type.enum.ts:6` exactly; also `isNotificationType` value guard at `:21-23` |
| 7a | session table `schema/classes/session.ts:32-63` | VERIFIED | Nullable: `intent` (`sessionIntent("intent")` `:44`, no `.notNull()`), `fee`, `startedAt`, `endedAt`, both confirm timestamps, `confirmationDeadline`; NOT NULL: `id`, `teacherId`, `studentId`, `status` (default "scheduled"), `sessionType` (default "student_session"), `createdAt`, `updatedAt`; `feeHeld` nullable w/ default false |
| 7b | teacher table `schema/teachers/teacher.ts:19-38` | VERIFIED | Shared PK `id` → `users.id` cascade (`:22-24`); `requestPreference` at `:30` = `teacherRequestPreference("request_preference").default("queue")` (nullable enum col, default queue) |
| 7c | students table `schema/students/students.ts:18-47` | VERIFIED | Shared PK; `handshakeCode` varchar(50) notNull unique; `parentId` nullable set-null; four `balance*` columns + `balanceTrial` notNull default 0 + `trialGrantedAt` |
| 7d | users table `schema/users/users.ts:11-45` | VERIFIED | `fullName` varchar(255) notNull (`:15`); `locale: appLocale("locale")` nullable at `:29` (emitter fallback hook) |
| 7e | notifications table `schema/notifications/notifications.ts:27-46` | VERIFIED | No `updatedAt`; `title` varchar(255) notNull; `body` text nullable; `relatedEntityType` varchar(100)/`relatedEntityId` integer both nullable; `userId` cascade; no idempotency column |
| 7f | `schema/enums.ts:56-64` | VERIFIED | `notificationType` pgEnum exactly: session_request, session_completion, session_cancellation, parent_link_request, system_broadcast, payment_confirmation, evaluation_result; see also `:27` sessionIntent, `:114` teacherRequestPreference ["queue","reject","offer_alternatives"], `:21` appLocale ["ar","en"] |
| 8 | tx-vs-queryDb split + `isDBTransaction` (`student.repository.ts:33-36`, `:96-112`) | VERIFIED | `:34-36`: `function isDBTransaction(tx: DBQueryExecutor): tx is DBTransaction { return typeof tx === "object" && "select" in tx; }`; `:96-112` `findHandshakeCodeByStudentId`: tx-branch = Drizzle select on supplied tx; non-tx = `queryDb<...>(\`SELECT ... WHERE id = $1 LIMIT 1\`, [id])` |
| 9 | `findLocalesByIds` (`user.repository.ts:152-155`) | VERIFIED | `(userIds: readonly number[], tx?: DBQueryExecutor): Promise<Map<number, AppLocale \| null>>` — map pre-seeded null per id, empty-input shortcut; tasks note: reference only, joined read supersedes |
| 10 | repo barrel style; `backend/db/repo/classes/` absent | VERIFIED | `repo/index.ts:17-23` plain `export * from "./<subdir>";` (admin, billing, notifications, parents, students, teachers, users — no classes). `backend/db/repo/classes/` does NOT exist |
| 11 | co-located `__tests__` precedent + `runInRollback` | VERIFIED | `repo/students/__tests__/student-grant-free-trial-once.test.ts` exists; uses `runInRollback`, `expectRepoError`, fixtures via `createTestUser`/`createTestStudent` |
| 12 | `NotFoundError` ctor (`errors.ts:37-41`); `ValidationError` overloads (`:78-86`) | VERIFIED | `NotFoundError(entity, message, options?)` → code `` `${entity.toUpperCase()}_NOT_FOUND` ``. Overloads: `(message)` / `(code, message, options?)` / `(message, fields)` / `(code, message, options, fields)` — lines 78-86 exact |
| 13 | `defaultLocale` (`AppLocale.ts:3`); `typeSessionRequest` en/ar `:11` | VERIFIED | `export const defaultLocale: AppLocale = "ar";` exact at `:3`. en `:11`: `typeSessionRequest: "Session Request"`; ar `:11`: `typeSessionRequest: "طلب جلسة"` |
| 14 | flat keys `notificationNotFound`/`studentHandshakeNotFound` (`types/errors/index.ts:93,:97`); ErrorsLabels shape + en/ar mirror | VERIFIED | Keys at `:93` and `:97` exactly. `ErrorsLabels` = flat string keys + TWO grouped sub-blocks (`planCatalog` `:46`, `adminUsers` `:67-87`). Mirrors: `en/errors/index.ts:48,50` and `ar/errors/index.ts:48,50` both present |
| 15a | `NotificationsLabels` shape (string slots vs function slots) | VERIFIED | `types/notifications/index.ts:26-95` — 26 keys; 22 string slots; 4 function slots: `markReadAriaLabel(title)`, `markAllResult(count)`, `unreadCount(count)`, `realtimeToast(typeLabel,title)` |
| 15b | parity test: 26 MANDATED_KEYS, 4 FUNCTION_KEYS, ARABIC_SCRIPT regex, type-prefix pin, pinned-Arabic block | VERIFIED | `notifications-namespace.parity.test.ts:50-77` (26 keys), FUNCTION_KEYS `:99` ("markReadAriaLabel","markAllResult","unreadCount","realtimeToast"), `ARABIC_SCRIPT = /[\u0600-\u06FF]/` at `:102`; type-prefix pin `:158-161`; pinned Arabic blocks `:192-238` |
| 15c | `errors-namespace.parity.test.ts` exists + how it walks keys | VERIFIED | Exists; sorts ar/en key sets and `toEqual`s them (`:107-113`), walks every leaf via recursive `nonEmptyString`/`assertEveryLeafNonEmpty` (`:87-103`), plus route-source scanner `routeEmittedErrorsKeys()` reading `app/api/graphql/route.ts` for `errorsTranslations.<key>` sites (`:52-64`) |
| 16 | `getServerTranslations(locale)` ONE-arg + `.notificationsTranslations`/`.errorsTranslations` | VERIFIED | `server-graphql.ts:3-5`: `export function getServerTranslations(locale: string): ReturnType<typeof getTranslations> { return getTranslations(locale); }` (unknown locale → `defaultLocale` fallback in `server.ts:11-13`). Tree property names exactly `errorsTranslations` and `notificationsTranslations` (`en/messages.ts:17,24`; `ar/messages.ts:17,24`) |
| 17 | `test/workflows/AGENTS.md` EXISTS + mandatory rules | VERIFIED | Exists (99 lines, 12 hard rules). Rules: NO `runInRollback` at this layer; commit fixtures in ONE `db.transaction` in `beforeAll` via `TrackedFixtures` registry; `afterAll` hard-deletes registered rows in reverse registration order then re-probes EVERY row (`verifyAllAbsent` — leakage fails suite); unique prefix `jrn_<domain>_${randomUUID().slice(0,8)}`; honest authorization only (real users + role-child rows via `provisionStudentActor`/`provisionCertifiedTeacherActor`/`provisionParentActor`/`provisionAdminActor`); external effects spied (`SpiedFanoutTransport`); NO `expect(...).rejects.toThrow()` — try/catch + `getServerTranslations("en").errorsTranslations` substrings; sequential actor-attributed steps; cross-actor visibility AND denial probes required |
| 18 | `docs/testing/workflow-journey-tests.md` EXISTS + patterns | VERIFIED | Exists (123 lines). Journey layer between service-unit and GraphQL integration: direct service calls with actor's real `actorUserId`; committed fixtures; this is the documented `runInRollback` exception; UUID prefixes; spy dispatch, assert targeted userIds; NOTE — doc's status line says directory "currently contains only its rules file", which is stale (helpers/ + admin/notifications/parents journeys exist) — AGENTS.md is the current authority |
| 19 | emit test: SpiedFanoutTransport (~:70), in-memory ClaimCache (~:177-215), ghost-push proof (~:780-828), the four type definitions + paths | DRIFTED (in-memory model deviates, rest verified) | `SpiedFanoutTransport` imported at `notification-engine.emit.test.ts:70` from `@/test/workflows/helpers` (defined `test/workflows/helpers/spied-transport.ts:49-94`: `calls`, `publishCount`, `lastCall`, `publishedUserIds`, `clear()`). In-memory model is `ScriptedClaimCache` at `:177-215` implementing `NotificationIdempotencyClaimCache` with `claim/get/store` + outage toggles + `preHeldKeys`/`stored`. Ghost-proof `describe("forced outer-transaction rollback — ghost pushes are impossible (REQ-042)")` at `:780-828` — asserts ZERO rows post-rollback and ZERO publishes via spy. Type defs: `NotificationIdempotencyClaimCache` = `{claim(key, ttlSeconds): Promise<boolean>; store(key, value, ttlSeconds): Promise<void>; get(key): Promise<string\|null>}` exported from `@/backend/services/notifications/emit-idempotency` (`:47-51`). `NotificationEngineCallOptions` = `{ readonly transport?: NotificationFanoutTransport; readonly cache?: NotificationIdempotencyClaimCache }` defined+exported in `notification-engine.service.ts:35-38` (NOT in `@/backend/types`). `NotificationDeliveryReceipt` (`:81-95`) and `NotificationEmitInput` (`:47-55`) in `@/backend/types` (`backend/types/notifications/notification.types.ts`) |
| 20 | entity-setup signatures (users/students/teachers w/ role, locale, isApproved, requestPreference overrides) | DRIFTED (teacher helper name, overrides shape) | Signatures verbatim: `createTestUser(tx: DBTransaction, overrides: Partial<UserSelectType> = {}): Promise<UserSelectType>` — accepts `role`, `locale`, governance flags via overrides. `createTestStudent(tx: DBTransaction, userId: number, overrides: Partial<StudentSelectType> = {}): Promise<StudentSelectType>`. NO teacher helper in `entity-setup.ts` — teacher fixtures via `provisionCertifiedTeacherActor(tx, options)` (`test/workflows/helpers/actor-context.ts:105-110`) which inserts `{ id: user.id, isApproved: true }`; `requestPreference` NOT settable there (defaults queue) — journey needing reject/offer_alternatives must insert directly |
| 21 | `backend/services/index.ts` barrel style; `backend/services/classes/` absent | VERIFIED | `:9-14` plain `export * from "./admin" | "./auth" | "./billing" | "./shared" | "./students" | "./teachers"`. `backend/services/classes/` does NOT exist |
| 22 | `public-operations.ts:36-46` six frozen ops + test file | VERIFIED | `PUBLIC_OPERATION_NAMES` at `:36-46` = exactly `["login", "refreshToken", "logout", "registerUser", "recitationReadings", "_health"] as const`; `backend/lib/gateway/public-operations.test.ts` exists |
| 23 | GraphQL freeze suites exist | VERIFIED | All present in `backend/graphql/test/`: `schema-surface.test.ts`, `sdl-static-assertions.test.ts`, `handshake-code-surface.test.ts`, `plan-catalog.schema.test.ts` (committed-vs-live SDL parity test at `:67-73`), `notification-integration.matrix.test.ts` |

## Verdict: NO blocking gaps.

Drift notes for implementation tasks:
1. `notification-engine.service.ts` is 182 lines — ALL line cites :302-369 / :475-481 / :612-645 are stale; mechanics split across `notification-engine.helpers.ts` (`withTransaction` `:18-26`, `claimOrPriorReceipt` `:34-54`), `notification-engine.publish.ts` (`publishAfterCommit` `:23-40`, `publishReceiptsFromIndex` `:43-66`) and `emit-idempotency.ts` (`NotificationIdempotencyClaimCache` `:47-51`, `attemptEmitClaim` `:84-104`, `storeEmitReceiptQuietly` `:112-122`, `buildEmitClaimKey` `:67-71`, TTL `NOTIFICATION_EMIT_CLAIM_TTL_SECONDS = 86_400` `:30`).
2. `IDEMPOTENCY_KEY_MAX_LENGTH` (128) is MODULE-PRIVATE in `emit-validation.ts` — re-derive or export; do NOT import.
3. Own-commit path returns the bare `row` (`NotificationReturnType`), NOT the receipt (`notification-engine.service.ts:77`) — the plan's `(sessionId, …) => Promise<NotificationDeliveryReceipt>` emitters must build/assemble receipts themselves (store under `emitClaimKey` via `publishReceiptsFromIndex`-consumed receipt shape).
4. No `createTestTeacher` in `entity-setup.ts`; `provisionCertifiedTeacherActor` hardcodes `isApproved: true` and cannot set `requestPreference` — for B.16 reject/offer_alternatives variants, insert the teacher row directly in a committing tx (schema default at `teacher.ts:30` is `"queue"`).
5. repo top-level barrel lacks a `classes` entry AND sub-directory — task 2.2 must create both. Services barrel similarly starts without a `classes` entry.
6. `SESSION_NOT_FOUND` must reuse `NotFoundError("SESSION", …)` (entity-name ctor) and errors-namespace needs NEW `sessionNotFound` + `sessionIntentCorrupt` keys — none exist today (parity test's 26-key MANDATED inventory for notifications must grow with any new notification labels; errors parity auto-discovers route emitters).

Verified row: 20 · Drifted: 3 (1a/1d/1e = one engine-line-drift cluster, 19 in-memory model drift, 20 teacher-fixture drift) · Missing: 0
