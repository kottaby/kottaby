# Task 7.1 Outcome — Canonical Documentation

- **Task:** `7.1` (Phase 7 — Knowledge Propagation). Docs-only task: zero source/test edits; tasks.md checkbox NOT flipped (orchestrator flips).
- **Executed:** 2026-09-02.
- **Requirements:** REQ-080, REQ-081.
- **Inputs read in full before writing:** every file under `outcome/` (0-baseline, 0.2, plan-review-R1, 1.1, 1.2, 2.1, 2.2, 2.3, 2M, 3.1, 4.1, 5.1, 5.2, 6.1, 6.2, 6.3, 6.4), `deferred-items.md`, tasks.md 7.1, specs.md REQ-080/081, `docs/notifications/realtime-engine.md` (§3.2/§3.10/§4/§6), and the shipped module header (`backend/services/classes/session-request-notification.service.ts:1-60`).

## 1. CREATE `docs/notifications/session-request-notifications.md`

Created with exactly the mandated structure, no plan/task/REQ citations anywhere in the body (grep-verified clean, see §4):

1. **Why** — ticket purpose (the session-specific seam over the engine: joined context read, recipient resolution, localized copy), who consumes (session intake + accept/decline flows; future session engine; zero frontend needs).
2. **Pattern** — the six-step shared choreography plus the mandated wave table (emitter → recipient side → title/body copy slots incl. argument shapes), the fixed envelope (`type = session_request`, `relatedEntityType = "session"`, `relatedEntityId = session.id`), and the deterministic key `` `session:${sessionId}:${waveKind}` `` with the six `waveKind` literals enumerated. Intent-label slots (`intentHifz`/`intentTajweed`/`intentEvaluation`) documented.
3. **Rules** — single-writer through `NotificationEngine` only; caller-tx receipt with `emitClaimKey` / publish-after-commit via `NotificationEngine.publishReceipts`; own-commit anatomy (engine commits + stores + publishes once; deterministic replay returns the stored receipt with zero new rows/publishes); recipient-locale composition documented as this module's obligation and the first full in-tree implementation of the engine's localization-at-emitter boundary (realtime-engine §3.3); identity-is-read-never-accepted; and the four Phase-6 forward obligations (all literal):
   - **`SESSION_NOT_FOUND` non-precedented** — stated verbatim as a service-internal signal, not precedential for any future public intake surface (oracle hygiene, REQ-034 pentester forward-check item a).
   - **No governance filtering** — emitters deliberately do not filter governed participants; the ruling belongs to the future intake gate; the engine doc's governance-window posture (§3.10) is unchanged (forward-check item b).
   - **Receipt anatomy** — caller-tx ⇒ receipt with `emitClaimKey`, no publish; own-commit ⇒ engine commits/stores/publishes once, replay returns the stored receipt with zero new rows/publishes.
   - **int4 honesty** — `session.id` is 32-bit; ceiling-sensitive callers must not assume wider ids; boundary probing belongs at the int4 ceiling because binding a 64-bit sentinel raises a Postgres range error instead of producing a miss.
   Both shipped design details described cleanly with zero plan-artifact references: the typed `INTERNAL_SERVER_ERROR` breach guard on the caller-tx engine-return narrowing (impossible branch kept typed instead of an `as` cast, test-pinned via engine stubbing) and the int4-ceiling boundary-probe choice.
4. **What NOT to Do** — all six mandated bullets plus the module-header-derived extras: never emit session notifications outside this module; never widen the realtime payload for CTAs; never pass recipient ids as parameters; never trust caller-supplied participants; never mutate session state; never log raw keys/PII (with the exact bounded log context `{ code, entity: "session", entityId, locale }`).
5. **Rollout Summary** — full file table (19 entries) reconciled against the outcome-file inventories (1.1/1.2/2.1/2.2/2.3) and live `git status --short` (ticket file-set matches exactly; the four modified plan files and `outcome/` are plan-internal and rightly excluded from the production doc's table).
6. **Forward Consumption Contract** — table mirroring the ledger's D1–D5 pointers by owning ticket (DEV3-004/005 intake + accept/decline incl. session-row authorship; DEV2-011 in-session detection; DEV3-008 alternatives computation; session-engine-era queue persistence + preference routing), closing with "this module IS the emitter those engines call".
7. **Related Documents** — engine doc, IDEMPOTENCY doc, state-machine invariants (explicitly: no new invariants minted), services/repo AGENTS.md, `shared/locale/`.

## 2. UPDATE `docs/notifications/realtime-engine.md` §3.2 — surgical one-line

Single-cell extension of the DEV3-011 row in the §3.2 consumption table:

- Before: `| DEV3-011 | `session_request` accept/decline wave | `emitForUser` per recipient |`
- After:  `| DEV3-011 | `session_request` accept/decline wave | `emitForUser` per recipient — SHIPPED: `docs/notifications/session-request-notifications.md` |`

No other line, heading, table, or section of the engine doc was touched (one-replacement Edit; §3.10 D5 governance-window text verified intact post-edit).

## 3. `docs/specs/**` UNTOUCHED — verified

```
$ git status --short -- docs/specs/ docs/notifications/
 M docs/notifications/realtime-engine.md
?? docs/notifications/session-request-notifications.md
```

`docs/specs/` shows ZERO entries — `state-machine-invariants.md` and `open-decisions-and-gaps.md` are byte-untouched, as required (this ticket mints no new invariants).

## 4. Plan-Internal Reference Sweep (production-doc cleanliness)

```
$ grep -nE "REQ-[0-9]|tasks\.md|plan\.md|ai/plans|outcome/|task [0-9]|A\.4\.3" docs/notifications/session-request-notifications.md
(exit 1 — zero matches)
```

The doc contains no REQ numbers, task ids, amendment codes (A.4.3 rendered as a neutral pointer to engine-doc §3.3), or plan paths. Ticket names (DEV3-004/005/008, DEV2-011) appear in §6 only, matching the established production-doc convention (`realtime-engine.md` itself names DEV-tickets throughout, incl. the §3.2 DEV3-011 row).

## 5. Concurrency Note (shared tree)

During this task, the task-7.2 agent's edits landed in the same tree (`AGENTS.md`, `backend/services/AGENTS.md`, `backend/db/repo/AGENTS.md` modified — including the root-AGENTS.md Important References line for the new doc, which now resolves to an existing file after this task's creation). Those files are task 7.2's assignment and were NOT touched by this task. The system-reminder flagged `backend/AGENTS.md` / `backend/services/AGENTS.md` as un-injected instruction files when I read (not edited) the service module; since this task edits nothing under `backend/`, no backend-layer file was modified by me.

## Files Touched

| File | Action |
|---|---|
| `docs/notifications/session-request-notifications.md` | CREATED (canonical doc, ~130 lines, seven sections) |
| `docs/notifications/realtime-engine.md` | MODIFIED (one line — shipped-pointer in §3.2 DEV3-011 consumption-table row) |
| `ai/plans/sprint_2/dev3-011-session-request-notification-to-teacher/outcome/7.1-outcome.md` | CREATED (this file) |

## Decisions & Deferrals

- **Decision:** engine-D2 lineage phrased as "the engine's localization-at-emitter boundary (realtime-engine §3.3)" rather than citing the engine plan's deferred-item id — production docs must not carry plan internals; the §3.3 anchor is a stable in-doc cross-reference.
- **Decision:** rollout table lists only source/locale/test/docs files; plan-internal artifacts (plan.md/specs.md/tasks.md/deferred-items.md/outcome/) excluded as non-production.
- **Deferral:** none owned here. REQ-034's documentation clause (staged at 2M/6.3 as belonging to this doc) is now materialized in §3 — the two mandated sentences and the receipt-anatomy/int4 items are literal. The 6.2/6.3 forward-check ("7.1 must state NOT_FOUND-non-precedential + no governance filtering") is closed by this file.

Cross-file dependencies: **none.**
