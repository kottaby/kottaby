# Task 3.1 Outcome — GraphQL HARD-FREEZE Verification (No Implementation)

- **Task:** `3.1` (Phase 3 — GraphQL Resolvers & API Handlers — HARD-FREEZE VERIFICATION)
- **Executed at:** 2026-09-02 ~03:50–04:05 EEST
- **Mode:** verification-only — ZERO source/suite edits, ZERO re-anchoring. This ticket adds ZERO GraphQL surface; Phase 3 exists only to prove the freeze.
- **Requirements:** REQ-017, REQ-030, REQ-060, REQ-074

## 1. Freeze-Suite Results (via mandated `test/scripts/run-test.ts`)

| Suite | Result | Counts | Log |
|---|---|---|---|
| `backend/graphql/test/schema-surface.test.ts` | ⚠️ 4 pre-existing-drift failures | 29 pass / 4 fail (33 ran) | `logs/2026-09-02T03-51-53/backend/graphql/test/schema-surface.test.ts.log` |
| `backend/graphql/test/sdl-static-assertions.test.ts` | ⚠️ 3 pre-existing-drift failures | 15 pass / 3 fail (18 ran) | `logs/2026-09-02T03-53-04/backend/graphql/test/sdl-static-assertions.test.ts.log` |
| `backend/graphql/test/handshake-code-surface.test.ts` | ✅ GREEN | 22 pass / 0 fail | `logs/2026-09-02T03-53-52/...` |
| `backend/graphql/test/plan-catalog.schema.test.ts` | ✅ GREEN (parity check :67-73 = PASS) | 5 pass / 0 fail | `logs/2026-09-02T03-54-32/...` |
| `backend/lib/gateway/public-operations.test.ts` | ✅ GREEN | 26 pass / 0 fail | `logs/2026-09-02T03-55-22/...` |

Also green and freeze-relevant **inside** the drifted suites: the `Codegen sync — committed SDL is byte-identical to the built schema` assertion (schema-surface), the fingerprint/allowlist/`HandShakeCode` posture legs, and every Notification/Users-locale surface leg. The failing legs concern ONLY stale pinned inventories.

## 2. Drift Classification — PRE-EXISTING BASELINE DRIFT (both suites; D6 contingency engaged)

**Verdict: all 7 failures are pre-existing drift, NOT caused by this ticket.** Evidence chain:

1. **Ticket file-set contains zero schema inputs.** `git status --short` (before and after this task, stable at 19 entries) shows the ticket touched only: plan docs, `backend/db/repo/{index.ts,classes/}`, `backend/services/{index.ts,classes/}`, `backend/types/classes/{index.ts,session-notification.types.ts}`, `shared/locale/{en,ar}/{errors,notifications}/index.ts`, `shared/locale/types/{errors,notifications}/index.ts`, `shared/locale/notifications-namespace.parity.test.ts`, `test/workflows/classes/`. Nothing under `backend/graphql/`, `backend/lib/gateway/`, or `frontend/graphql/generated/`. The schema-building inputs (`backend/graphql/pothos/**`) are untouched.
2. **Baseline evidence** (`outcome/0-baseline-outcome.md`): tree was 100% clean at 2026-09-01T14:43Z with both drift-causing commits ALREADY merged — so these suites failed identically before any ticket work existed.
3. **Failure legs are exactly the post-DEV1-013 surfaces the pins never absorbed:**
   - `schema-surface.test.ts` (last content-touch of `PRE_3_1_*` arrays: initial commit `486547d`; file re-touched with **0 insertions/0 deletions** by `7449297d` feat(admin-users) 2026-08-31 — arrays never refreshed):
     - Query-root diff: `+adminUserActivity, +adminUserDetail, +adminUsers, +adminUserStats`
     - Mutation-root diff: `+adminCreateUser, +adminSetUserDeleted, +adminUpdateUser`
     - Enum diff: `+AdminUserGovernanceFilter, +AuditActionType`
     - Named-type diff: `+13 Admin*` types (`AdminCreateUserInput`, `AdminParentSnapshot`, `AdminStudentSnapshot`, `AdminTeacherSnapshot`, `AdminUpdateUserInput`, `AdminUserActivityEntry`, `AdminUserDetail`, `AdminUserFiltersInput`, `AdminUserGovernanceFilter`, `AdminUserListItem`, `AdminUserPage`, `AdminUserStats`, `AuditActionType`)
   - `sdl-static-assertions.test.ts` (last touched by `b3b9aac` feat(notifications) 2026-08-31 01:31 — its pinned arrays predate BOTH the billing plan-catalog surface (`31f01c1` #28, 2026-08-30 22:40) AND the admin-users surface (`7449297d` #32, 2026-08-31 11:35)):
     - Mutation-root diff: `+createPlan, +setPlanActiveStatus, +updatePlan` plus the three admin mutations (its pin is the older 7-op baseline)
     - Query-root diff: `+adminPlans, +planCatalog, +findStudentByHandshakeCode, +myHandshakeCode` plus the four admin queries (8 additions)
     - `NO Subscription root` leg fails on a **naive substring assertion** (`expect(sdlText).not.toContain("Subscription")`) false-positived by the field `hasActiveSubscription` inside `AdminStudentSnapshot` — not by any actual `type Subscription`.
4. `git log -S "adminUsers"` on both suites → **zero history**: the token has NEVER existed in either suite, i.e. the pins were never re-anchored when #32 landed; re-anchoring is the freeze-suite owner's job (D6 ledger row), explicitly out of scope here.

**Handling per plan-review-R1 D6 + tasks.md 3.1 contingency:** suites left 100% UNEDITED; failures recorded with full diffs above; ✅ resolved-pointer ledger entry D6 (already present in `deferred-items.md:24`, now pointed at this evidentiary outcome).

## 3. Codegen NO-DIFF Proof

```
# BEFORE:  git status --short -- frontend/graphql/generated/ backend/graphql/  → (empty)
bun run generate:gqlSchema && bun codegen
  → [generate:gqlSchema] wrote frontend/graphql/generated/schema.graphql (11287 bytes)
  → graphql-codegen: Generate to ./frontend/graphql/generated/gql/graphql.ts ✔
# AFTER:   git status --short -- frontend/graphql/generated/ backend/graphql/  → (empty)
diff before after → identical; full-repo git status entry count unchanged (19 → 19, 0 graphql/gateway/generated paths)
```

**Verdict: NO-DIFF confirmed.** Committed artifacts equal live emission; zero new churn vs the pre-run state. This also cross-confirms the drift finding: the live schema == committed SDL, so the schema itself is NOT drifting — only the suites' frozen inventories are stale.

## 4. Grep-Proof — Root Mutation/Query Surface

Tokens `sessionRequest`, `requestSession`, `acceptSessionRequest`, `declineSessionRequest` across `backend/graphql/`: **ZERO root-field hits.** The only hits are the pre-existing sanctioned `SessionRequest` value of the `NotificationType` enum (`backend/graphql/pothos/shared/enum.pothos.ts:123-124` comment + enum registration; test fixtures in `notification-*.test.ts`). Broader `session` sweep of `backend/graphql/pothos/**`: only comments, the `AuthSession` service type import (auth lifecycle), and `sessionCount` plan fields — no session-related builder refs. The live SDL (dumped verbatim by the failing sdl-static run) shows Query and Mutation roots with **no session-intent operations**.

## 5. Public-Operations Frozen Six

`backend/lib/gateway/public-operations.ts` — UNTOUCHED by this ticket (`git status --short` / `git diff` on the file: EMPTY). `PUBLIC_OPERATION_NAMES` remains exactly the frozen six:

1. `login`
2. `refreshToken`
3. `logout`
4. `registerUser`
5. `recitationReadings`
6. `_health`

Corroborated by `public-operations.test.ts` (26/26 pass) and `handshake-code-surface.test.ts`'s "allowlist stays EXACTLY the frozen six" leg.

## 6. Verdict

**Freeze posture CONFIRMED with respect to this ticket:** zero GraphQL drift attributable to DEV3-011 — codegen no-diff, zero session root operations, gateway allowlist byte-identical, plan-catalog committed-vs-live parity green, handshake surface green. The 7 RED legs across `schema-surface` / `sdl-static-assertions` are pre-existing stale-inventory drift attributable to merge commits #28 (`31f01c1`) and #32 (`7449297d`), both predating this ticket's clean baseline; left UNEDITED and UN-RE-ANCHORED per the D6 contingency, with evidence recorded here and pointed from deferred-items ledger row D6.

_Checkbox intentionally NOT flipped in tasks.md — flip deferred to the orchestrator's gate protocol._
