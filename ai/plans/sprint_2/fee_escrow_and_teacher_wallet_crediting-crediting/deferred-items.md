# Deferred Items Ledger

**Feature:** Fee Escrow & Teacher Wallet Crediting (Close-the-Loop Verification)
**Plan:** `ai/plans/sprint_2/Fee Escrow & Teacher Wallet Crediting-crediting/`
**Created:** 2026-09-11

---

## Ledger

| ID | Deferred Item | Source Task | Target Task | Status | Verified By | Notes |
|---|---|---|---|---|---|---|
| D1 | Runtime adoption of the escrow idempotency-key contract (`EscrowTriggerContract` / `WalletCreditContract`, `backend/types/contracts/session-completion-escrow.contract.types.ts:14-80`) — exactly-once currently rests on the `fee_held = true` guard predicate alone | Plan design D3 (plan.md) | **Financial Safety Verification ticket** (`docs/planning/TICKETS.md:2985-3026`, Sprint 4) | ❌ Blocked | — | External-ticket ownership: adopting a runtime key now would touch the race-proven money path; the Sprint 4 escrow-integrity ticket is the correct place to decide (its double-spend matrix is the acceptance surface). Exempt from this plan's in-plan ❌ gate per Note rule below. |
| D2 | `WalletInsertType` / `WalletReturnType` / `TeacherTransactionInsertType` / `TeacherTransactionReturnType` — only Select/View types exist (`backend/types/billing/wallet.types.ts:4-16`, `backend/types/billing/teacher-transaction.types.ts:3`) | Code-state verification (REQ-8) | **First consumer ticket** (expected: Teacher Withdrawal Workflow, `docs/planning/TICKETS.md:1799-1845`) | ❌ Blocked | — | No current consumer; adding unused type exports would trip knip `check:unused`. Exempt from this plan's in-plan ❌ gate per Note rule. |
| D3 | Stale frozen-root-inventory SDL pins: `backend/graphql/test/schema-surface.test.ts:747` mutation-set pin lacks `purchaseVerificationPlan`; `backend/graphql/test/sdl-static-assertions.test.ts:354-356` `FROZEN_MUTATION_FIELDS` lacks `adjustTeacherWallet` / `approveWithdrawal` / `rejectWithdrawal` + `purchaseVerificationPlan`; `:359-361` `FROZEN_QUERY_FIELDS` lacks `adminPendingWithdrawals` / `adminStudentPayments` / `adminTeacherWallet`. Causes 3 pre-existing test failures in this branch state (the plan itself modifies zero GraphQL files; the codegen-sync pin passes) | Task 3.1 (suite runs, `outcome/3.2-traceability-outcome.md` §2) | **Repo surface-freeze maintainers / next surface-touching ticket** (NOT this plan — pin refresh: add the 4 mutation + 3 query names, bump the frozen counts) | ❌ Blocked | — | External-ticket ownership: the pin lists were last refreshed by other teams' commits (PR #141 `bde0e02`, PR #161 `ea15ed7`/`569d943`/`e9357c8`), and editing the pin suites is production-test code change outside this verification plan. Every escrow/wallet/SDL-shape pin passes; `git diff --name-only -- backend/graphql shared` = 0 files proves the plan introduced zero SDL delta. Exempt from this plan's in-plan ❌ gate per Note rule. |
| D4 | No global GraphQL query depth/complexity limiter exists (no `DepthLimit`/`MaxDepth`/`queryComplexity`/`CostAnalysis` ValidationRule in the execution layer). Current posture: transport guard `MAX_GRAPHQL_BODY_BYTES = 2_000_000` (`backend/lib/gateway/transport-guard.ts:49`), fail-open rate limit (`app/api/graphql/route.ts:26`), prod introspection off (`app/api/graphql/route.ts:49`), flat-type shape pins | Task 3.QL SEC audit (`outcome/3.2-traceability-outcome.md` §7) | **Financial Safety Verification ticket** (`docs/planning/TICKETS.md:2985-3026`, Sprint 4) | ❌ Blocked | — | External-ticket ownership: adding a depth/complexity rule is a runtime-guard decision over the money surface; it is reported as-is (nothing invented) and left to the escrow-integrity ticket's threat model. Honesty note (R1-security): the route's rate-limit hook is currently a fail-open always-allow stub (`backend/lib/ratelimit.ts:75-87`) — requests are not effectively rate-limited today; the operative guards are the 2MB transport cap and prod introspection off. Exempt from this plan's in-plan ❌ gate per Note rule. |
| D5 | Stale schema docblock: `backend/db/schema/classes/session.ts:33-34` still teaches the superseded "held at request, decremented at completion" escrow semantics — the binding ruling is hold-as-debit (settlement flips `fee_held` and never debits; `docs/sessions/session-lifecycle.md` §4–§5, `docs/billing/escrow-and-wallet-crediting.md` §2) | R1-security review (`worklog.md`) | **Financial Safety Verification ticket** (`docs/planning/TICKETS.md:2985-3026`, Sprint 4) / doc-hygiene sweep | ❌ Blocked | — | External ownership: correcting a comment inside a production schema file is outside this markdown-only fix pass; the binding ruling lives in the canonical docs. Exempt from this plan's in-plan ❌ gate per Note rule. |

## Status Legend

- ✅ Done · ⚠️ Partial · ❌ Blocked · 🔄 In Progress

## Gate Rule (this plan)

`grep -c "❌\|⚠️" deferred-items.md` counts **only rows whose Target Task is inside this plan**. Rows D1–D5 are owned by named external tickets / external maintainers (their ❌ denotes "not done anywhere yet", not "owed by this plan") and are exempt from the gate; each must be re-verified as landed or re-scoped when its target ticket executes. Expected raw count: **7** (the D1–D5 rows, the Status Legend line, and this Gate Rule paragraph's own self-match of the grep pattern) — zero of the seven ❌/⚠️ symbols sit on a row whose Target Task is inside this plan. Recorded in tasks.md Task 4.3; recount 5→6 per the R1-content review (the Gate Rule paragraph self-matches the pattern), 6→7 after the R1-fix D5 addition.
