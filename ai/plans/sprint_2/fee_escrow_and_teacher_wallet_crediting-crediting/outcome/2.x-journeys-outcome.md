# Outcome — Task 2: Journey Re-Runs (cross-actor escrow arcs)

**Date:** 2026-09-15 (sandbox execution)
**Plan:** `ai/plans/sprint_2/Fee Escrow & Teacher Wallet Crediting-crediting/`
**Task:** 2 — re-run the three committed-fixture journey suites, assert green, verify zero-residue cleanup.
**Verdict:** ALL THREE JOURNEYS GREEN (27 pass / 0 fail total, 390 expect() calls, exit 0 each). All 13 cited cross-actor escrow constructs were observed as passing tests at their EXACT authoring-time lines (zero drift on journey files). Zero-residue verified twice: (a) the suites' internal `afterAll` probes passed, (b) an external SQL audit shows pre-journey row counts = post-journey row counts = 0 on every table the journeys touch.

---

## 1. Journey Results (TE)

Runs were strictly sequential (1 → 2 → 3, never overlapping) from `/home/z/my-project`, each through the mandated log-capturing runner (`bun run test/scripts/run-test.ts <path>` — never raw `bun test`, never wrapped in `runInRollback`). The runner self-loads `.env.test`; no env override was needed.

| # | Journey suite | Result | Exit | Log artifact (repo-relative) |
|---|---|---|---|---|
| 2.1 | `test/workflows/sessions/session-dual-confirmation.journey.test.ts` | **11 pass / 0 fail** / 116 expect() calls, 11 tests in 423ms | 0 | `logs/2026-09-15T03-06-23/test/workflows/sessions/session-dual-confirmation.journey.test.ts.log` |
| 2.2 | `test/workflows/sessions/session-lifecycle.journey.test.ts` | **10 pass / 0 fail** / 181 expect() calls, 10 tests in ~0.4s | 0 | `logs/2026-09-15T03-06-33/test/workflows/sessions/session-lifecycle.journey.test.ts.log` |
| 2.3 | `test/workflows/sessions/session-lifecycle-denials.journey.test.ts` | **6 pass / 0 fail** / 93 expect() calls, 6 tests in ~0.3s | 0 | `logs/2026-09-15T03-06-53/test/workflows/sessions/session-lifecycle-denials.journey.test.ts.log` |

Baseline comparison: baseline is all-green (`outcome/0.1-baseline-outcome.md`); these runs are all-green too, so **no failure is reported and none was absorbed** (tasks.md rule 6 satisfied trivially). No flakes, no retries needed, no env workarounds.

## 2. Cited Cross-Actor Arcs — Observed Test Names and Current `path:line`

All authoring-time citations in tasks.md resolve EXACTLY against the live files — **zero line drift** on all three journey files. Each construct below is the verbatim `(pass)` test name from the log artifacts of section 1.

### 2.1 Dual-confirmation journey (`test/workflows/sessions/session-dual-confirmation.journey.test.ts`, 707 lines)

| Cited | Current | Observed passing test (verbatim) |
|---|---|---|
| :348 (wallet-less teacher start) | :348 | `step 1 — fixture cast commits as tracked ids with real role rows; the teacher starts with NO wallet` (asserts `readTeacherWalletRow(teacher)` is null at :360) |
| :363 (hold at booking) | :363 | `step 2 — Student A books T: scheduled, trial-lane hold, platform fee, zero notification rows` (`feeHeld=true` :374, `heldBalanceLane=Trial` :375, trial unit consumed into escrow :379) |
| :451 (settle credits exactly the fee once) | :451 | `step 5 — Student A confirms: both stamps, hold released, the wallet is credited EXACTLY the fee once` (wallet delta = fee :476-477, exactly ONE ledger row :479, amount = fee verbatim :480, `TransactionType.Earning` :481) |
| :492 (replay/foreign zero-writes) | :492 | `step 6 — repeats and foreign attempts write NOTHING: student re-confirm, teacher confirm, foreign-student denial` (wallet static :526-528, ledger still 1 row :529, row `updatedAt` byte-identical :533) |
| :561 (sweep refund to same lane) | :561 | `step 8 — System sweep cancels the expired completion: same-lane refund +1, exactly ONE auto-cancel notice` (lane restored 0→1 on the provenance lane, no other lane moved :587) |
| :620 (confirm-vs-sweep single financial outcome) | :620 | `step 10 — confirm-vs-sweep race on one expired completion: exactly ONE financial outcome` (row-oracle partitions credit-vs-refund :662-694; loser classified, never crashes :648-656) |

Adjacent passing steps corroborate the arc: step 3 (:391, oracle-safe completion denial), step 4 (:412, exactly ONE confirm prompt), step 9 (:601, sweep re-run matches ZERO rows), step 11 (:697, teardown worklist complete).

### 2.2 Lifecycle journey (`test/workflows/sessions/session-lifecycle.journey.test.ts`, 678 lines)

| Cited | Current | Observed passing test (verbatim) |
|---|---|---|
| :369 (trial-first binding) | :369 | `step 2 — Student A books T (Hifz, key K1): scheduled, trial-lane hold, fee, 24h deadline; trial consumed BEFORE paid` (trial 1→0 while hifz untouched :387-392 — the debit ladder's trial-first ordering) |
| :402 (trial-first binding, second actor) | :402 | `step 2b — Student B books T (Hifz, key K2): HIS trial binds — trial-first across both bookings` (B's own trial lane paid for HIS hold :424-430) |
| :492 (idempotent replay) | :492 | `step 4 — Student A replays K1: ConflictError DUPLICATE_REQUEST; zero new rows; balances static; teacher list stable` |
| :611 (cancel refunds exactly once) | :611 | `step 7 — Student B cancels session B: cancelled + feeHeld=false + HIS trial lane refunded EXACTLY once; teacher observes cancelled` (lane restored 0→1, not 2 :625-627; `heldBalanceLane` retained as provenance :619-621) |
| :645 (completed-cancel denial) | :645 | `step 8 — Teacher T cancels the COMPLETED session A: SESSION_INVALID_TRANSITION; row byte-identical` (row `toEqual` before/after :658-662, no lane moved :663-664) |

### 2.3 Denials journey (`test/workflows/sessions/session-lifecycle-denials.journey.test.ts`, 595 lines)

| Cited | Current | Observed passing test (verbatim) |
|---|---|---|
| :200 (uncertified-teacher denial, balances untouched) | :200 | `leg 1 — booking the applicant is TEACHER_NOT_FOUND; caller balances untouched (INV-TV1)` — the "applicant" is a teacher-role user with NO `teacher` row (uncertified teacher by construction; the suite asserts the denial mints no certification, :225-230). Lanes byte-untouched + key not burned :219-223 |
| :236 (zero-balance denial, zero writes + funded retry) | :236 | `leg 2 — zero-balance booking: INSUFFICIENT_BALANCE, zero writes, then THE SAME key succeeds funded` (zero writes across sessions/claims/lanes :257-260; THE SAME idempotency key re-books after funding :262-282 — the key-rollback proof) |

Adjacent passing legs corroborate: leg 3 (:319, foreign cancel/read oracle pairing), leg 4 (:384, applicant has no session surface), leg 5 (:431, evaluation pre-DB validation), leg 6 (:484, admin & parent governance FORBIDDEN).

## 3. Zero-Residue Evidence (2.QL core requirement)

### 3.1 The journeys' internal probes (what the afterAll asserts)

All three suites passed with their `afterAll` hooks executing; in bun test a failing hook expectation fails the suite, so these probes are evidenced by the exit-0 results:

- **Dual-confirmation** (`session-dual-confirmation.journey.test.ts:318-345`): after suspending the append-only trigger to delete the earning rows it created (:326-331) and running `registry.cleanup()` (:333), it re-probes: every tracked `session` row count = 0 (:337-341), the teacher's `wallet` count = 0 (:342 — this is the no-orphaned-wallet probe the task asks about), and both students' notification inboxes = 0 (:343-344). Step 11 (:697) additionally pins the teardown worklist at exactly 20 tracked rows (14 fixture + 3 sessions + 3 idempotency claims).
- **Lifecycle** (`session-lifecycle.journey.test.ts:317-346`): a defensive totality sweep re-tracks ANY session/claim row referencing the fixture actors (:324-341) so a mid-test failure cannot strand restrict-FK rows, then `registry.cleanup()` hard-deletes everything FK-safely in one committed transaction (:345). Step 9 (:668) pins the worklist.
- **Denials** (`session-lifecycle-denials.journey.test.ts:173-197`): the same totality-sweep + one-shot FK-safe hard-delete pattern (:178-196).

Note on `teacher_transaction`: per the workflow-layer rules (`test/workflows/AGENTS.md` helper contract) ledger rows are DELETE-blocked (immutability triggers), so journeys assert ZERO ledger rows during the run and remove the rows they created under the sanctioned `withImmutabilityTriggersSuspended(["teacher_transaction"])` seam (`session-dual-confirmation.journey.test.ts:328-330`).

### 3.2 External SQL audit (pre-state vs post-state)

The database was freshly pushed with SEED_PROFILE=minimal — every table started empty. Counts before the three runs vs after all three runs (via `/tmp/pgdeb/root/usr/lib/postgresql/17/bin/psql -U postgres -h 127.0.0.1 -d kottaby_db`):

| Table | Pre (before 2.1) | Post (after 2.3) |
|---|---|---|
| `wallet` | 0 | **0** |
| `teacher_transaction` | 0 | **0** |
| `session` | 0 | **0** |
| `session_request_idempotency` | 0 | **0** |
| `notifications` | 0 | **0** |
| `users` | 0 | **0** |
| `students` | 0 | **0** |
| `teacher` | 0 | **0** |
| `applicants` | 0 | **0** |
| `audit_logs` | — (not checked pre) | **0** |
| `parents` | — (not checked pre) | **0** |
| `admin` | — (not checked pre) | **0** |

**Zero residue.** Pre-state = post-state = 0 rows on every table the journeys touch, including the two the task names (`wallet`, `teacher_transaction`) and `session`. No orphaned wallet or ledger rows, no stranded fixture users, no leftover sessions or idempotency claims. This is the strongest possible residue signal: with a zeroed baseline, ANY leak of any tracked row class would have shown as a nonzero count.

## 4. SEC — Security & Tenancy Findings (journey level)

Each finding cites the journey test that PROVES it (name + current path:line), all observed passing in section 2:

| Guard | Journey proof | Evidence |
|---|---|---|
| Foreign-user confirm denial is oracle-safe | `step 6 — repeats and foreign attempts write NOTHING…` `session-dual-confirmation.journey.test.ts:492` (foreign-student probe :520-523) and `step 3 — Teacher T starts session A; Student A's own complete attempt is denied oracle-safely` :391; corroborated by `leg 3 — foreign student cancel/read is indistinguishable from nonexistent (REQ-J4 oracle pairing)` `session-lifecycle-denials.journey.test.ts:319` (foreign cancel → `SESSION_NOT_FOUND` with the translated message :348-355, foreign READ returns the identical null :357-358) | A non-participant's confirm/cancel/read is answered with the same `SESSION_NOT_FOUND` shape a truly-missing session would produce — no existence oracle — and the owner's row stays byte-identical. |
| Teacher cannot credit self (the settle is the STUDENT's act) | `step 6` `session-dual-confirmation.journey.test.ts:492`: the TEACHER's confirm is the idempotent no-op (:509-518) and the post-state asserts wallet static (:526-528) with still exactly ONE ledger row (:529). The ONLY credit observed in any journey is `step 5` :451, dispatched with `cast.primaryStudent.userId` (the student) at :456-460. | The earning ledger row exists only after the student's confirm; the teacher's own confirm writes nothing — a teacher cannot mint a credit for himself through the completion handshake. |
| Sweeper never credits wallets | `step 10 — confirm-vs-sweep race…` `session-dual-confirmation.journey.test.ts:620`, sweep-won branch: zero ledger rows (:686), `walletTotal(walletAfter)` = before (:687), lane refunded +1 (:688), exactly one auto-cancel notice (:689-693). Also `step 9` :601 (re-run sweeps ZERO rows, zero refunds) and `step 8` :561 (system refund is lane-side only). | In the release path the sweep leaves the ledger empty and the wallet untouched — money returns to the student's lane, never enters the teacher's wallet. |
| No role bypass on governance surfaces (BFLA, journey level) | `leg 6 — admin & parent: no bypass, governance FORBIDDEN on writes, zero audit rows (REQ-064)` `session-lifecycle-denials.journey.test.ts:484` | Admin/parent actors hit the real authorization path and are FORBIDDEN with zero audit rows. |
| Honest authorization only | All journeys provision real users + role-child rows via the actor-context factory (e.g. `session-dual-confirmation.journey.test.ts:356-360` asserts `isApproved=true` for both teachers; `session-lifecycle.journey.test.ts:363-366` asserts the applicant has NO `teacher` row) — denials flow through the real checks, never patched permissions (workflow AGENTS.md rule 4). | Negative steps are genuine denials, not staged ones. |

## 5. SR — Semantic Review

- Every claim above is backed by either a verbatim `(pass)` test name in the section-1 log artifacts, a `path:line` grep-verifiable today, or the SQL counts in section 3.2. No dead claims.
- No plan-meta references were introduced (this task wrote no code). Test names containing pre-existing requirement labels (`INV-TV1`, `REQ-J4`, `REQ-064`) are quoted as observed output; those labels belong to the committed suites' own registers and predate this plan.
- Citation accounting: all 13 tasks.md citations resolved exactly; the two 2.3 citations' wording maps 1:1 (":200 uncertified-teacher" = the applicant leg, an uncertified teacher-role user; recorded in section 2.3). No source file was edited to align citations.
- Honest-scope note: journey step 10 is deliberately outcome-branched (confirm-wins vs sweep-wins) — in this run one terminal shape materialized; BOTH branches are written to be exhaustive and the suite has passed across runs (idempotent-teardown re-run practice per the workflow AGENTS.md), so the single-financial-outcome invariant is pinned on both arms of the partition, not just the observed one.

## 6. IV — Instruction Verification

- `test/workflows/AGENTS.md` (99 lines) read in full. Compliance check: **Hard rule 1** — NO `runInRollback`: honored and verified by grep (`runInRollback` appears only in doc comments declaring its ban; the suites commit fixtures in `beforeAll` and I invoked them unwrapped, exactly as tasks.md rule 4 mandates). **Rule 2** — committed fixtures + tracked cleanup with mandatory zero-residue re-probes: observed (section 3.1). **Rule 3** — per-run `jrn_…_<8hex>` prefixes: made sequential runs collision-free. **Rule 4/12** — honest authorization + both-direction visibility/denial assertions: observed (section 4). **Rule 9** — no seeded rows as fixtures (fresh `jrn_` entities throughout). **Running section** — the layer's prescribed iteration command IS `bun run test/scripts/run-test.ts <file>`, which is exactly what was used.
- The read-only re-run mandate was honored: zero modifications under `backend/ shared/ app/ frontend/ test/ docs/` (`git diff --name-only` on those trees = 0 files). The only working-tree changes in the plan directory are the pre-existing Task 0/1 artifacts plus this outcome file.
- Runs were sequential per the task instruction, so per-suite fixtures never overlapped on the shared DB; the runner's process lock additionally serializes them.

## 7. Files NOT Modified (and why)

- **Zero production or test files modified.** `git status --porcelain` before this task's writes: `outcome/0.1-baseline-outcome.md` (Task 0 sandbox re-capture append), `tasks.md` (Task 1 checkbox flips), `outcome/1.x-suites-outcome.md` (Task 1's artifact, untracked) — nothing else. `git diff --name-only -- backend shared app frontend test docs` → 0 files.
- Journey citation lines matched the live files exactly, so no re-location edits were even considered; editing sources/tests is forbidden by this plan's scope in any case.

## 8. Carry-Forward Knowledge for Tasks 3–4

1. **Journey citations are currently EXACT** (unlike Task 1's service-suite drift): Task 3's traceability matrix can cite the journey `path:line`s of section 2 directly — but re-grep at authoring time anyway, since any upstream edit shifts them.
2. **Settle/release journey citations for the matrix**: settle-credit-once → `session-dual-confirmation.journey.test.ts:451`; replay/foreign zero-writes → :492; sweep same-lane refund → :561; confirm-vs-sweep single-outcome partition → :620; trial-first ladder → `session-lifecycle.journey.test.ts:369`/:402; replay DUPLICATE_REQUEST → :492; single-refund cancel → :611; terminal-state cancel denial → :645; zero-balance + key-rollback proof → `session-lifecycle-denials.journey.test.ts:236`; uncertified-teacher denial → :200; foreign oracle pairing → :319.
3. **Key-rollback proof (denials leg 2)**: a failed booking releases its idempotency key — the SAME key then books successfully funded. Task 3's reconciliation record (D1 idempotency-key deferral) can cite this as the behavioral guarantee that makes the runtime key contract (D1 deferral) safe to postpone.
4. **Zero-residue audit pattern**: pre/post SQL count diffing on a zeroed DB is a clean, reproducible verification recipe; Tasks 3–4 (which also run DB-backed suites) can reuse it. The journeys' own `afterAll` probes are the in-suite enforcement; the SQL audit is the independent cross-check.
5. **Wallet-read helper caveat**: journey `wallet` probes read by `teacherId` (`session-dual-confirmation.journey.test.ts:342`), matching the SEC invariant that wallet access is ctx-derived — Task 4's doc can present the zero-row probe as the orphan-wallet detector at suite level.
6. **Sub-loop on markdown outcomes**: expect the structural `--lifecycle duplicates` oxlint exit-1 documented in Task 1's outcome (section 5 there); `--lifecycle tsgo` is the meaningful gate for `.md` files in this sandbox and is what was run here (section 9).

## 9. QL — Quality Loop (outcome file)

Command: `bun run scripts/health/sub-loop.ts ai/plans/sprint_2/fee_escrow_and_teacher_wallet_crediting-crediting/outcome/2.x-journeys-outcome.md --lifecycle tsgo` (tsgo lifecycle per Task 1's documented environment constraint: the `duplicates` lifecycle exits 1 on ALL `.md` files in this sandbox because oxlint/biome have no lintable files — structural, pre-existing, reproduced on tracked pre-existing `.md` files in Task 1's outcome §5; not re-litigated here).

Result: **exit 0** (recorded after writing this file).

## 10. Cross-File Dependencies Discovered

- The dual-confirmation journey depends on `withImmutabilityTriggersSuspended` (`@/test/helpers/db-cleanup`, used at `session-dual-confirmation.journey.test.ts:328`): ledger cleanup rides the sanctioned trigger-suspension seam — Task 4's immutability section should mention that journeys are the only sanctioned writer/deleter of `teacher_transaction` rows in tests.
- The denials journey's key-rollback leg exercises `session_request_idempotency` claim lifecycle (burn-on-success, release-on-failure) together with the lane guard — Task 3's D1 reconciliation should cite both behaviors as the pair that the deferred runtime key would formalize.
- The lifecycle journey's totality sweep (`session-lifecycle.journey.test.ts:317-346`) is the pattern that makes mid-test-failure cleanup total; if new journeys are added by future tickets, this pattern (plus step-N worklist pins) is the template to copy.

## 11. Anomalies

- **None in execution**: all three journeys passed on first run, sequentially, exit 0, no retries, no environment workarounds, zero residue.
- The `[DOMAIN]` log lines in the raw output (`SESSION_NOT_FOUND` denial payloads, `NOTIFICATION_IDEMPOTENCY_DEGRADED` fail-open notice) are the server-side domain logging of the journeys' deliberate denial steps and the un-injected claim cache in the test server context — expected noise, not failures (the denial steps assert the localized, oracle-safe client-facing behavior).
- The `--lifecycle duplicates` structural markdown limitation carries over from Task 1 (see section 9); `--lifecycle tsgo` exits 0.
