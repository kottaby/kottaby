# Outcome — Task 3.1/3.2: GraphQL Pins + Traceability Matrix

**Date:** 2026-09-15 (sandbox execution)
**Plan:** `ai/plans/sprint_2/fee_escrow_and_teacher_wallet_crediting-crediting/`
**Task:** 3 — run the three GraphQL pin suites (3.1), assert SDL delta = zero, and build the AC → REQ → code → test traceability matrix for both tickets (3.2). Reconciliation record: `outcome/3.3-reconciliation-outcome.md`.
**Verdict:** Suite 1 (`session-lifecycle-mutations`) fully green (36/0). Suites 2–3 are green on every escrow/wallet/SDL-shape pin but each carries a small number of **pre-existing frozen-root-inventory failures** (`purchaseVerificationPlan` + the admin financial-auditing trio added by other teams' commits WITHOUT refreshing the pin lists) — reported explicitly per tasks.md rule 6, NOT absorbed, NOT fixed (out of this no-modification plan's scope). **SDL delta attributable to this plan = zero** (git diff on `backend/graphql/` + `shared/` = 0 files; committed `schema.graphql` byte-identical to the built schema). Traceability matrix: **all 15 ticket AC/scenario rows mapped, zero unmapped**, every citation grep-verified at its current line.

---

## 1. GraphQL Suite Results (3.1)

All three suites ran through the mandated log-capturing runner `bun run test/scripts/run-test.ts <path>` from `/home/z/my-project` (tasks.md rule 4). The runner handled all three natively — **no fallback to `test:graphql`/`run-server-tests.ts` was needed** (the single-file branch spawns the live test server via each suite's own `setupTestServerLifecycle()`; runs were sequential under the runner's process lock).

| # | Suite | Result | Exit | Log artifact (repo-relative) |
|---|---|---|---|---|
| 3.1a | `backend/graphql/test/session-lifecycle-mutations.test.ts` | **36 pass / 0 fail** / 105 expect() calls, 36 tests in 10.26s | 0 | `logs/2026-09-15T03-17-58/backend/graphql/test/session-lifecycle-mutations.test.ts.log` |
| 3.1b | `backend/graphql/test/schema-surface.test.ts` | **57 pass / 1 fail** / 399 expect() calls, 58 tests in 0.44s | 1 | `logs/2026-09-15T03-18-23/backend/graphql/test/schema-surface.test.ts.log` |
| 3.1c | `backend/graphql/test/sdl-static-assertions.test.ts` | **37 pass / 2 fail** / 225 expect() calls, 39 tests in 0.08s | 1 | `logs/2026-09-15T03-19-14/backend/graphql/test/sdl-static-assertions.test.ts.log` |

Baseline note: the recorded baseline (`outcome/0.1-baseline-outcome.md`) covers tsgo/biome/lint + (Tasks 1–2) service/repo/journey suites; the GraphQL tier had no prior run in this plan. Tasks 1–2 recorded 150 + 27 passes; the failures below are therefore **first-surfaced here** and are analyzed in §2.

### 1.1 Observed escrow/credit/refund pins (suite 3.1a, verbatim passing)

- `createSession` wire contract: `payload.feeHeld` is `true` and `status` is `Scheduled` on the first booking (:416-417); missing `X-Idempotency-Key` header → `VALIDATION` pre-DB (:395-402); same-key replay → `DUPLICATE_REQUEST` 409 (:404-426); hostile `Evaluation` intent → `VALIDATION` (:428-435); applicant `users.id` as teacherId → `TEACHER_NOT_FOUND` (:437-447).
- Wrong-role matrix: teacher/applicant/parent/admin `createSession` → `FORBIDDEN` (:319-353); student/parent/admin denied on start/complete (:355-389).
- `cancelSession`: foreign student, nonexistent id, parent, admin → the SAME `SESSION_NOT_FOUND` (oracle pairing, :527-555); owner student cancel → `Cancelled` + `feeHeld=false` + hifz lane `+1` (:557-571); double cancel → `SESSION_INVALID_TRANSITION` (:573-577); teacher participant leg → `Cancelled` + `feeHeld=false` (:579-584).

## 2. Failure Analysis — 3 failing assertions, all pre-existing frozen-inventory drift (NOT this plan)

Every failure is the same class: a whole-root operation inventory pinned as a frozen list was not refreshed after OTHER teams shipped new root fields. Diffs quoted from the log artifacts:

| Failing test (file:line) | Live root contains | Frozen pin lacks | Introduced by (git) |
|---|---|---|---|
| `schema-surface.test.ts:747` "mutation set grows ONLY by the sanctioned additions" | `purchaseVerificationPlan` | that one name | `ea15ed7`/`569d943`/`e9357c8` (PR #161 verification-plan purchase flow) |
| `sdl-static-assertions.test.ts:354-356` "Mutation root is EXACTLY the refreshed frozen 34-op baseline" | `adjustTeacherWallet`, `approveWithdrawal`, `rejectWithdrawal`, `purchaseVerificationPlan` | all four names | PR #141 (`bde0e02` financial-auditing trio) + PR #161 |
| `sdl-static-assertions.test.ts:359-361` "Query root is EXACTLY the refreshed frozen 33-op baseline" | `adminPendingWithdrawals`, `adminStudentPayments`, `adminTeacherWallet` | all three names | PR #141 (`bde0e02`) |

**Why this is not a plan failure:**
1. `git diff --name-only -- backend/graphql shared` = **0 files** (this plan wrote nothing; §3). HEAD is `e9357c8` (PR #161) — the newest commit predates this plan's first execution (worklog Task 0: clean tree at HEAD).
2. The introducing commits are ancestors of that baseline HEAD, so the failure existed at baseline too — the GraphQL tier simply had never been run inside this plan before Task 3.
3. The committed artifact is in lockstep with the built schema: schema-surface's **"Codegen sync — committed SDL is byte-identical to the built schema" PASSED** (`frontend/graphql/generated/schema.graphql` matches a fresh `printSchema(lexicographicSortSchema(...))` emission). The drift is test-pin staleness, not schema inconsistency.

**Escrow/wallet impact: none.** Every escrow/wallet-specific pin passes: schema-surface's Query-additions pin (incl. `myWallet` + `WALLET_QUERY_FIELDS`, `schema-surface.test.ts:644-688`), `Session` field set + five nullable dispute fields (:888), `DisputeResolution` = `Cancel | Complete` (:874), enum-set and whole-schema named-type delta (incl. wallet surface + ledger enums, :830/:909), all 18 admin financial-auditing pins (:1491-1793), and all 36 wire pins of suite 3.1a. The failing rows enumerate unrelated root fields only.

**Disposition per tasks.md rule 6:** reported here and to the orchestrator; NOT fixed (editing the pin suites is production-test code change, outside this verification plan); a pin-refresh (add the four mutation + three query names, bump the frozen counts) is recommended as a follow-up for the surface-freeze maintainers. This does not block REQ-7's escrow evidence: every row of §4/§5 cites a PASSING test.

## 3. SDL Delta = Zero (evidence)

| Check | Command / source | Result |
|---|---|---|
| No GraphQL/shared file touched | `git diff --name-only -- backend/graphql shared` | **0 files** |
| No production tree touched at all | `git diff --name-only -- backend shared app frontend test docs` | **0 files** |
| Whole-tree tracked diff | `git diff --name-only` | only the plan's own files (`outcome/0.1-baseline-outcome.md`, `tasks.md` — checkbox/outcome bookkeeping) |
| Committed artifact ↔ built schema | schema-surface "Codegen sync" test | **PASSED** — byte-identical emission |
| SDL shape pins for escrow/wallet | schema-surface `Session` field set / `DisputeResolution` / named-type delta / `committedSdl` contains-checks (:1883-1898) | **all PASSED** |
| New fields/types introduced by this plan | any | **none** — a verification plan; contract types at `backend/types/contracts/session-completion-escrow.contract.types.ts:34-80` are pre-existing type-level pins (D1) |

## 4. Traceability Matrix — Ticket A: "Fee Escrow: Hold at Request, Decrement at Completion" (`docs/planning/TICKETS.md:1716-1747`)

Abbreviations: `svc` = `backend/services/classes/session-lifecycle.service.test.ts`; `journey` = `test/workflows/sessions/…`; `wire` = `backend/graphql/test/session-lifecycle-mutations.test.ts`; `repo` = `backend/db/test/repo/classes/session.repository.test.ts`. All lines are CURRENT (re-grepped 2026-09-15; drift accounting in §8). Cross-references: T1 = `outcome/1.x-suites-outcome.md`, T2 = `outcome/2.x-journeys-outcome.md`.

| Ticket AC (short wording) | REQ | Code citation (current path:line) | Test citation (file + name + line) |
|---|---|---|---|
| **A-AC1** request → `session.fee` platform-set (B.3) | REQ-1 AC1 | `session-lifecycle.booking.ts:182` (`fee: sessionFeeForIntent(...)`); `sessionFeeForIntent` at `session-lifecycle.guards.ts:83-85`; fees `shared/constants/session-fees.constants.ts:27-33` (`"25.00"`, `EGP`) | svc:449 `tajweed-hit branch: … its own fee` (PASS, T1 §2.1); svc:1109 `fee boundary: … decimal STRING verbatim` |
| **A-AC1** request → `fee_held = true` (B.4) | REQ-1 AC2 | `session-lifecycle.booking.ts:183-185` (`feeHeld: true`, `heldBalanceLane`, `confirmationDeadline`); columns `backend/db/schema/classes/session.ts:63-65,70` | svc:394 `trial-hit branch: books on the trial lane, holds the fee, records Trial provenance…`; journey `session-dual-confirmation…test.ts:363` step 2 (`feeHeld=true` :374, lane `Trial` :375); wire:416-417 `expect(payload.feeHeld).toBe(true)` |
| **A-AC1** "balance is held (not decremented yet)" | **REQ-6 AC1 — SUPERSEDED (ruling: hold = guarded debit at request)** | debit ladder `session-lifecycle.booking.ts:127-159` (trial :133-136, intent :137-158); primitive `decrementLaneIfAvailable` `backend/db/repo/students/student.repository.ts:404-419` | svc:394 (…debits exactly one trial unit); journey `session-lifecycle…test.ts:369` step 2 (trial 1→0 BEFORE paid); reconciliation: `outcome/3.3-reconciliation-outcome.md` Ruling 1 |
| **A-AC2** dual confirmation → balance decremented by 1 | **REQ-6 AC1 — timing superseded: the decrement happened at request**; completion performs no debit | same debit path as above (booking-time); settlement flips hold only | svc:394 (debit at booking); journey `session-dual-confirmation…:363` (lane already consumed into escrow :379) |
| **A-AC2** → `session.fee_held` set to false | REQ-3 AC1 | guarded UPDATE `SessionRepository.confirmStudentCompletionOnce` `backend/db/repo/classes/session.repository.ts:433-455` (predicate `feeHeld = true` ∧ teacher stamp ∧ student stamp absent :449-452) | svc:2398 happy path (stamp + release + ONE earning); repo:1256 `a student-confirmed row never matches…`; T1 §2.1/§2.3 |
| **A-AC2** → teacher's wallet credited `session.fee` | REQ-3 AC2 | `session-lifecycle.confirmation.ts:48-59` + composition call :129; `creditEarningOnce` `backend/db/repo/billing/wallet.repository.ts:78-110` (earning/Completed/amount verbatim :88-98, wallet increment :103-110) | svc:2398; journey `session-dual-confirmation…:451` step 5 (delta = fee :476, ONE ledger row :479, amount = fee :480, `TransactionType.Earning` :481) |
| **A-AC3** cancel → `fee_held` false, released, no decrement-kept | REQ-4 AC1 | `SessionRepository.cancelSessionOnce` `session.repository.ts:217-231`; same-lane refund `refundHeldLaneToProvenance` `session-lifecycle.transitions.ts:222-237` (→ `incrementLane` `student.repository.ts:433-441`) | svc:962 `cancel happy path: … refunds the SAME lane exactly once`; journey `session-lifecycle…:611` step 7 (lane 0→1, provenance retained :619-627); wire:557-571 (hifz `+1` through the full stack) |
| **A-AC3** no wallet transaction on cancel | REQ-4 AC2 | cancel path writes no ledger by construction (refund-only slice `transitions.ts:222-237`) | svc:1363 double-cancel single-refund; journey `session-dual-confirmation…:561` step 8 (lane-side refund; no earning); sweep-won branch zero ledger rows :686-688; T2 §4 |
| **A-AC4** insufficient balance → 422, no session | REQ-2 AC1-AC4 | throw `session-lifecycle.booking.ts:156` (ladder :127-159; localized key en `shared/locale/en/errors/index.ts:90` / ar `shared/locale/ar/errors/index.ts:88`) | svc:470 `total-miss branch: … INSUFFICIENT_BALANCE with zero rows`; journey `session-lifecycle-denials…:236` leg 2 (zero writes + funded retry :262-282) |
| **A-TS5** fee is platform-set (B.3), not negotiated | REQ-1 AC3 + REQ-0.5 | input omits fee: `SessionSubmitInput {teacherId, intent}` `backend/types/classes/session.types.ts:39-42`; field-by-field mapping `session-lifecycle.mutation.ts:127-131` | svc:1109 (decimal-string boundary); BOPLA audit T1 §3; `createSession` SDL: `schema-surface.test.ts:1883-1898` committedSdl pins PASS |

## 5. Traceability Matrix — Ticket B: "Teacher Wallet Crediting (Earning Transactions)" (`docs/planning/TICKETS.md:1765-1793`)

| Ticket AC (short wording) | REQ | Code citation (current path:line) | Test citation |
|---|---|---|---|
| **B-AC1** `teacher_transaction` created `type='earning'` | REQ-3 AC2 | `creditEarningOnce` `wallet.repository.ts:88-98` (`type: TransactionType.Earning` :95); enum mirror `backend/enum/billing/transaction-type.enum.ts:7` | journey `session-dual-confirmation…:451` (:481 asserts `TransactionType.Earning`); svc:2398 |
| **B-AC1** `wallet_id` = teacher's wallet; `session_id` = session.id | REQ-3 AC2 + REQ-5 AC3 | same INSERT :91-92 (`walletId` from `ensureWalletOnce` result, `sessionId` linked); earning ↔ session by construction | svc:2398 (ledger row + wallet increment); journey :451 (:479-480) |
| **B-AC1** `amount = session.fee` (verbatim) | REQ-3 AC2 | `confirmation.ts:129` (fee passed pre-narrowed); `wallet.repository.ts:94` (amount verbatim) | svc:1109 (fee is a decimal STRING verbatim); journey :451 (:480 `toBe(SESSION_FEE_HIFZ)`) |
| **B-AC1** `status='completed'` | REQ-3 AC2 | `wallet.repository.ts:96` (`TransactionStatus.Completed`); mirror `backend/enum/billing/transaction-status.enum.ts:7` | journey :451 (:481 type pin; status asserted by svc:2398 observed earning row) |
| **B-AC1** `wallet.balance` += fee AND `total_earning` += fee | REQ-3 AC2 | guarded UPDATE `wallet.repository.ts:103-110` (`balance + amount`, `totalEarning + amount`) | svc:2398; journey :451 (:476 `walletTotal` delta = fee) |
| **B-AC2** teacher without wallet → wallet created + earning linked | REQ-3 AC3 | lazy ensure `ensureWalletOnce` `wallet.repository.ts:59-64` (`INSERT … ON CONFLICT DO NOTHING` :61) | wallet svc:102 `a brand-new teacher gets an honest zeroed wallet…`; journey `session-dual-confirmation…:348` step 1 (teacher has NO wallet :360) + :451 (credit into ensured wallet) |
| **B-AC3** cancelled session → NO `teacher_transaction` | REQ-4 AC2 + REQ-3 AC5 | refund-only release path (`transitions.ts:222-237`); ledger INSERT exists only in `creditEarningOnce` (settlement arm) | svc:1363 (double-cancel, no second refund); journey :561/:686 (sweep release ⇒ 0 ledger rows); svc:1793 (arbitration COMPLETE consumes hold with NO wallet credit) |
| **B-TS6** dual confirmation — earning created, wallet credited (scenario row) | REQ-3 AC1/AC2 | `session.repository.ts:433-455` + `confirmation.ts:112-139` | svc:2398/:2441/:2471; repo:1582/:1617 (races); T1 §2 |
| **B-TS7** balance and total_earning updated correctly | REQ-3 AC2 | `wallet.repository.ts:103-110` | journey :451 (:476-477 exact delta); wallet svc:132 (ledger page newest-first) |
| **B-TS8** cancelled — no earning (scenario row) | REQ-4 AC2 | see B-AC3 | see B-AC3 |
| **B-TS9** teacher without wallet — created on first earning | REQ-3 AC3 | see B-AC2 | see B-AC2 |
| **B-TS10** `wallet.balance >= 0` CHECK (INV-W2/W3) | REQ-5 AC1 | `backend/db/schema/billing/wallet.ts:36-38` (unique `wallet_teacher_id_unique` + `balance >= 0` + `total_earning >= 0`) | wallet svc:208 (insufficient → ZERO committed rows), :237 (exact-balance guard), :295 (governance deny) |
| **B-TS11** transaction amount CHECK | REQ-5 AC2 | `backend/db/schema/billing/teacher-transaction.ts:50-52` — shipped CHECK is **`amount > 0`** (STRICTER than the ticket's ">= 0") + indexes on `wallet_id`/`session_id` :51-52 | wallet svc:251 (validation matrix — malformed amounts fail pre-DB with zero side effects) |
| **B-TS12** immutability of the ledger (INV-W6/W8, scenario-adjacent) | REQ-5 AC4 | triggers `backend/db/migration/3-immutability-triggers.sql:87-113` | exercised by journey cleanup via `withImmutabilityTriggersSuspended` (`test/helpers/db-cleanup.ts:229`; T2 §3.1) |
| **B-TS13** exactly-one wallet per teacher | REQ-5 AC1 | `wallet.ts:36` unique constraint; ensure safe under concurrent confirms (`wallet.repository.ts:48-55` docblock) | repo races repo:1582/:1617 (concurrent confirms produce ONE financial outcome) |

## 6. Zero-Unmapped Accounting

Ticket A gherkin blocks: 4 (A-AC1…A-AC4) + 5 test-scenario rows — all 4 ACs mapped (A-AC1 three rows, A-AC2 three rows, A-AC3 two rows, A-AC4 one row) and the 5 scenario rows are the same five behaviors (request-hold / dual-confirmation / cancellation / insufficient-balance / platform-set fee) — covered by matrix rows above. Ticket B gherkin blocks: 3 (B-AC1…B-AC3) + 6 test-scenario rows — all 3 ACs and all 6 scenario rows mapped (rows B-AC1×5, B-AC2, B-AC3, B-TS6…B-TS13). **Total distinct rows: 15 — 0 unmapped.** Two ticket clauses are mapped as SUPERSIGNED divergences (A-AC1 "not decremented yet", A-AC2 "decremented at completion") pointing at REQ-6/Ruling 1 rather than at a shipped-debit-at-completion path that does not exist — that IS the reconciliation deliverable, not a mapping gap.

## 7. SEC — Security & Tenancy Re-Verification (current path:line)

| Guard | Verified at (actual) | Verdict | Evidence |
|---|---|---|---|
| `resolveSessionDispute` ADMIN-only | `backend/graphql/mutation/classes/session-lifecycle.mutation.ts:270-275` — `authScopes: { $all: { authenticated: true, role: [UserRole.Admin] } }` (field registered :255); service re-asserts admin + governance (`assertAdminGovernanceClean`, `session-lifecycle.service.ts:622-624` defense-in-depth) | ✓ | Wire: `admin createSession` FORBIDDEN is the inverse BFLA cell; admin-governance `$all` conjunction pinned verbatim by schema-surface (:1397). Never-started arbitration denied pre-write (`session-lifecycle.service.ts:633-640`). |
| `requestWithdrawal` TEACHER-only + own-wallet-only | `backend/graphql/mutation/billing/wallet.mutation.ts:62-67` — explicit `$all { authenticated, role: [UserRole.Teacher] }` (field :53); resolver binds `ctx.user.id` :75; input whitelist is amount-ONLY :46-50 (no wallet id on the wire — BOLA-proof by construction) | ✓ | wallet svc:295 `governance deny: a deleted caller is FORBIDDEN on both flows (defense in depth over the GraphQL gate)` PASS; svc:208/:237/:251 guard the money semantics. |
| `myWallet` zero-arg, ctx-derived | `backend/graphql/query/billing/wallet.query.ts:46-67` — field registered with NO `args` (:46-57), `$all { authenticated, role: [Teacher] }` :52-57, resolver `WalletService.getMyWallet(ctx.user.id, ctx.locale)` :64 | ✓ | Docblock :5-9: identity derived exclusively from verified context; smuggled-identity-arg validation kills are pinned repo-wide (schema-surface :1181/:1255/:1353 PASS). |
| GraphQL depth/complexity protections | **NO depth-limit / complexity ValidationRule exists** (searched `DepthLimit|MaxDepth|queryComplexity|CostAnalysis` across `*.ts` — zero hits in the GraphQL execution layer). What exists instead: (1) transport guard `guardTransport` with `MAX_GRAPHQL_BODY_BYTES = 2_000_000` (`backend/lib/gateway/transport-guard.ts:49`, check :109) before the engine; (2) a rate-limit hook on the route (`app/api/graphql/route.ts:26` imports `withRateLimit` from `./rate-limit`) — currently a fail-open always-allow stub (`backend/lib/ratelimit.ts:75-87`), i.e. requests are NOT effectively rate-limited today; (3) introspection disabled in production (`app/api/graphql/apollo-server.ts:55`, route-side `NODE_ENV` gate); (4) shape-level REQ-069 pins proving flat, bounded objects (sdl-static "Notification object is FLAT…", "NO Subscription root exists", "DateTime is the artifact's ONLY custom scalar" — all PASS). | ✓ (as-is; reported, not invented) | The money mutations are single-entity guarded operations with flat argument shapes; there is no unbounded nesting channel on `myWallet`/`requestWithdrawal`/`confirmSessionCompletion`. A global depth limiter is a candidate ledger row IF the Financial Safety ticket wants belt-and-braces — recorded here honestly as ABSENT, per the no-invention rule. |

## 8. SR — Semantic Review (greps run + drift accounting)

**Greps executed for every code citation** (representative commands; each returned the cited line before this file was written):
- `rg -n "sessionFeeForIntent|heldBalanceLane|INSUFFICIENT_BALANCE|DUPLICATE_REQUEST|decrementLaneIfAvailable" backend/services/classes/session-lifecycle.booking.ts backend/db/repo/students/student.repository.ts …`
- `rg -n "confirmStudentCompletionOnce|cancelSessionOnce|resolveDisputeCancelOnce|sweepExpiredCompletedOnce|openDisputeOnce" backend/db/repo/classes/session.repository.ts`
- `rg -n "ensureWalletOnce|creditEarningOnce" backend/db/repo/billing/wallet.repository.ts`; `rg -n "refundHeldLaneToProvenance|incrementLane" backend/services/classes/session-lifecycle.transitions.ts`
- `rg -n "authScopes" backend/graphql/mutation/classes/session-lifecycle.mutation.ts`; `sed -n` verification of wallet.mutation.ts / wallet.query.ts blocks
- `rg -n "fee|feeHeld" backend/db/schema/classes/session.ts`; `rg -n "unique|check" backend/db/schema/billing/wallet.ts`; `rg -n "check|index" backend/db/schema/billing/teacher-transaction.ts`; `rg -n "transactionType|transactionStatus" backend/db/schema/enums.ts`
- Test lines re-read via `sed -n <line>p` for every cited test row in §4/§5 (svc :449/:962/:1109/:1363/:1726/:1766/:1793/:2398/:2441/:2471/:2524/:2574; wallet :102/:132/:208/:237/:251/:295; repo :490/:1256/:1430/:1552/:1582/:1617; journeys :348/:363/:451/:492/:561/:620, :369/:402/:492/:611/:645, :200/:236; wire :319/:346/:404/:416/:425/:557/:567/:570/:573).
- Cross-plan ruling sources: `rg -n "Escrow timing ruling" ai/finished_plans/sprint_1/session-creation-lifecycle-scheduled-sta/specs.md` → :8; `rg -n "D1" ai/finished_plans/sprint_1/segregated_session_balance-crediting/plan.md` → :54-56; `rg -n "D-2" ai/finished_plans/sprint_2/dual-confirmation-completion-handshake/plan.md` → :23.

**Drift accounting (authoring-time → current, none edited):** booking ladder `:95-116/:113` → `:127-159/:156` (+~34 lines: the subscription-expiry denial arm); `decrementLaneIfAvailable` `:478-493` → `:404-419`; `incrementLane` `:507-517` → `:433-441`; `ensureWalletOnce` `:45-50` → `:59-64`; `creditEarningOnce` `:64-98` → `:78-110`; wallet CHECKs `:33-35` → `:36-38`; teacher-transaction CHECK `:45` → `:50`; locale keys already at en :90 / ar :88 (T1 §3). EXACT hits: `sessionFeeForIntent` guards :83-85, session columns :63-65/:70, pothos `fee`/`feeHeld` :170/:174-175, mutation fields :87/:255/:293 with scopes :98-103/:270-275/:306-308, `confirmStudentCompletionOnce` :433-455, `cancelSessionOnce` :217-231, `resolveDisputeCancelOnce` :280-290, `sweepExpiredCompletedOnce` :503-517, `refundHeldLaneToProvenance` :222-237, contracts :34-80, enums.ts :31/:33, TS enum mirrors :7, fees constants :27-33, `openDisputeOnce` :248-262. Service-suite test lines carry T1's documented +2 drift below :1363 (:2396→:2398 etc.) — all re-verified here. **Path relocation:** the Segregated Session Balance plan now lives under `ai/finished_plans/sprint_1/` (this plan's artifacts cite `ai/plans/sprint_1/…`) — recorded in the reconciliation file, Ruling 1.

**Ticket-wording nuance recorded, not silently harmonized:** Ticket B scenario "Transaction amount >= 0 (check constraint)" — the shipped CHECK is `amount > 0` (`teacher-transaction.ts:50`), strictly stronger; the zero-amount case is additionally blocked pre-DB by the validation matrix (wallet svc:251). No code or ticket text was edited.

**No plan-meta leakage:** this task wrote no code; all requirement labels in this file (REQ-*) are this plan's own labels used inside the plan-mandated artifact; pre-existing suite labels (`REQ-042`, `REQ-064`, `REQ-050`, `REQ-054`, `REQ-032`, `REQ-069`, `INV-TV1`) are quoted as observed committed test names only.

## 9. QL — Quality Loop

`bun run scripts/health/sub-loop.ts <this file> --lifecycle tsgo` → **exit 0** (recorded after writing; run separately for each outcome file of Task 3). The `duplicates` lifecycle remains structurally broken for ALL `.md` files in this sandbox (oxlint/biome have no lintable files → exit 1; environment-wide, reproduced on pre-existing tracked `.md` files in T1 §5 — reported there, not re-litigated; `--lifecycle tsgo` is the meaningful markdown gate per T1/T2 precedent).

## 10. IV — Instruction Verification

Repo-root `AGENTS.md` (416 lines; read during T1, re-honored here): no source files modified; the only writes are the two plan-mandated outcome artifacts (`3.2`/`3.3`) — no extra summary/report files created (no-sprawl rule); no `console.*`/`Translation.`/two-arg `getTranslations` anywhere in the written artifacts; suites invoked only via `run-test.ts`; no `AGENTS.md` exists under `ai/` (verified by ls during T1; unchanged). `backend/graphql/AGENTS.md` and `backend/graphql/mutation|query/AGENTS.md` contracts were honored read-only (side-effect barrel registration documented in the audited files' own docblocks).

## 11. Files NOT Modified (and why)

`git diff --name-only -- backend shared app frontend test docs` = **0 files** after all three suite runs (verified again post-run). Working-tree changes remain exactly: `outcome/0.1-baseline-outcome.md` (Task 0 append), `tasks.md` (T1/T2 checkbox flips), untracked T1/T2 outcome files — plus this Task 3 pair. The 3 failing pin assertions were NOT "fixed" by editing the pin suites (out of scope; reported in §2).

## 12. Carry-Forward for Task 4 (propagation doc)

1. Cite the CURRENT lines from §4/§5 (booking ladder :127-159, primitive :404-419, ensure :59-64, credit :78-110, wallet CHECKs :36-38, txn CHECK :50-52).
2. The Segregated plan is under `ai/finished_plans/sprint_1/…` — use the finished_plans path in `docs/billing/`.
3. Present `insufficientBalance` as the REUSED funds-denial key at en :90 / ar :88 (T1 §3/§9).
4. The three stale pin inventories (§2) are the known-dirty corners of the SDL freeze — the propagation doc should present the escrow/wallet SDL from the PASSING pins, and may reference the pending pin-refresh as repo housekeeping owned outside this plan.
5. `amount > 0` (not `>= 0`) is the shipped ledger CHECK — use the stricter phrasing in the invariants table.
