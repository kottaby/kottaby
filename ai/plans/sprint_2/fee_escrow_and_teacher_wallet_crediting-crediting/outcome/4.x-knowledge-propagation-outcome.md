# Outcome — Task 4: Knowledge Propagation & Closure

**Date:** 2026-09-15 (sandbox execution)
**Plan:** `ai/plans/sprint_2/fee_escrow_and_teacher_wallet_crediting-crediting/`
**Task:** 4 — synthesize the escrow/ledger invariants from all prior outcomes, write the canonical domain doc `docs/billing/escrow-and-wallet-crediting.md`, add ledger rows D3/D4, run the deferred-items gate, and close the plan.
**Verdict:** Doc created (10 sections, 8 invariant rows mapped, ~60 grep-verified citations); D3/D4 ledgered; deferred gate PASSES (zero in-plan ❌/⚠️ targets — raw count accounting deviates from the plan's "expected 3" wording and is documented explicitly in §3); all three touched files pass the sub-loop `--lifecycle tsgo` (exit 0 each).

---

## 1. §4.1 — Invariant Synthesis from ALL Outcome Files

Inputs read in full before execution: `worklog.md` (Tasks 0-env/1/2/3), all four plan files, and all six outcome files (`0.1-baseline`, `plan-review-R1`, `1.x-suites`, `2.x-journeys`, `3.2-traceability`, `3.3-reconciliation`). The synthesized invariant set (what the canonical doc presents as permanent, CI-proven contracts):

| # | Invariant / property | Evidence consolidated |
|---|---|---|
| 1 | Hold-as-debit at request; settlement flips the hold and never debits | 3.3 Ruling 1 (ruling sources + code + tests); T1 svc:394/:470; T2 journeys :369/:402 (trial-first), :363/:451 (timing) |
| 2 | Exactly-one earning credit per hold (ledger row + wallet increment, one transaction) | 3.2 §4/§5 rows (settle predicate `session.repository.ts:448`, `creditEarningOnce` :78-110); svc:2398; journey :451 |
| 3 | Single-winner release: cancel/sweep/arbitration all funnel one refund slice, same lane, no ledger row | 3.2 A-AC3 rows (`cancelSessionOnce` :217-231, `refundHeldLaneToProvenance` :222-237); svc:962/:1363/:1726/:1766/:1793; repo :1430/:1552; journey :611/:561/:686-688 |
| 4 | Confirm-vs-sweep produces exactly ONE financial outcome (either direction) | repo :1582/:1617; journey :620; chaos + D4/D3 anchors 3.3 Ruling 1 |
| 5 | Zero-balance denial is atomic (zero writes, key reusable after funding) | svc:470; journey denials :236; localized `insufficientBalance` en :90 / ar :88 (T1 §3) |
| 6 | Ledger immutability at the DB layer; journeys are the only sanctioned test writer/deleter via the trigger-suspension seam | 3.2 B-TS12 (triggers `3-immutability-triggers.sql:87-113`); T2 §3.1 (`withImmutabilityTriggersSuspended`, `test/helpers/db-cleanup.ts:229`) |
| 7 | Teacher cannot self-credit; sweeper never credits; governance surfaces admin-only | T2 §4 (teacher no-op :509-518, sweep-won :686-688, governance FORBIDDEN denials :484); 3.2 §7 (ADMIN `$all` :270-275, service re-check :624/:633-640) |
| 8 | Wire-surface shape: `myWallet` ctx-derived zero-arg; `requestWithdrawal` amount-only; `Session.fee`/`feeHeld` pinned SDL; codegen-sync lockstep | 3.2 §7 rows 2-3; §3 SDL-delta-zero table (git diff on `backend/graphql shared` = 0 files) |

Green counts carried into the doc's Rollout Summary: service 72/0, wallet 9/0, repo 69/0 (T1 §1); journeys 11/0, 10/0, 6/0 with zero-residue pre/post SQL audits (T2 §1/§3.2); GraphQL wire pins 36/0 (3.2 §1). The 3 pre-existing frozen-inventory pin failures (3.2 §2) are presented in the doc as a known-dirty housekeeping corner (§7), never as escrow evidence — every claim in the doc cites a PASSING test.

## 2. §4.2 — The Canonical Doc

**Created:** `docs/billing/escrow-and-wallet-crediting.md` (new file; no prior doc existed for this domain). Structure mirrors the `docs/billing/` siblings (`financial-safety-verification.md`, `segregated-session-balance.md`): header block (Domain / Status / Source-of-truth-for + consumer MUST-read note) then:

| § | Section | Content mapping to the task |
|---|---|---|
| 1 | Why | failure-mode rationale (double-booking, double-release, double-credit, re-timing the debit) |
| 2 | Hold-As-Debit Semantics (the binding ruling) | the binding ruling with 6 numbered semantics + current citations; superseded ticket wording pointed at `docs/sessions/session-lifecycle.md` §4–§5 and `docs/billing/segregated-session-balance.md` §6 D1 |
| 3 | Settle / Release / Race Matrix | 3.1 state machine (escrow view, 10 rows with guards), 3.2 side-effect matrix, 3.3 race matrix (9 races, each with arbiter + passing proof) |
| 4 | Invariant Map | **8 rows: INV-W1, INV-W2, INV-W3, INV-W4, INV-W7, INV-W8, INV-B4, INV-S3** — each → schema constraint + code enforcement + test anchors at current lines; INV-W5/INV-W6 marked as adjacent, owned by `docs/billing/admin-financial-auditing.md`; INV-W8 recorded with the shipped STRICTER `amount > 0` CHECK |
| 5 | Rules for Extending the Money Path | 8 rules (extend primitives, predicate-is-lock, shared transaction, decimal-string discipline, release writes no ledger, reused denial keys, ctx-derived wallet identity, pinned SDL surface) |
| 6 | Anti-Patterns (What NOT to do) | 8 never-rules |
| 7 | GraphQL Surface & Security Posture | role gates, oracle-safe denials, server-owned pricing, **depth/complexity posture reported as-shipped (NO depth limiter; transport cap + fail-open always-allow rate-limit stub + prod introspection off)**, stale frozen-inventory pins noted with external ownership |
| 8 | Deferred Items & Boundary Hand-offs | D1/D2 pointers (+ D3/D4 as related-surface follow-ups) and the three dependent-ticket hand-offs: Withdrawal Workflow, Admin Re-Evaluation Deduction, Financial Safety Verification |
| 9 | Rollout Summary | nothing to deploy; tier → command → suites table |
| 10 | Related Documents | 9 real docs paths (sessions, billing ×4, specs, workflows, students, graphql, planning/TICKETS) |

**Clean-doc compliance:** zero plan-meta references (grep: no `REQ-`, no `Task N`, no "this plan", no `.ai/plans`, no `tasks.md`, no plan `specs.md`; ruling provenance is bound to the canonical docs, not to plan artifacts); no secrets/credentials; no invented security claims (the ABSENT depth limiter is stated as absent with the actual posture). **Citation discipline:** every path:line in the doc was grep/sed-verified at authoring time (verification log in §5); the line-drift corrections from T1 §2/§3 and 3.2 §8/§12 are applied (booking ladder :127-159 with the throw at :156, primitive :404-419, ensure :59-64, credit :78-110, wallet CHECKs :36-38, txn CHECK :50, locale en :90/ar :88, service-suite +2 drift :2398/:2441/:2471/:2524/:2574, arbitration arms :1726/:1766, Segregated-plan rulings cited via their canonical docs instead of the relocated plan path).

## 3. §4.3 — Deferred-Items Gate

**Ledger changes:** rows **D3** (stale frozen-root-inventory SDL pins — `schema-surface.test.ts:747`, `sdl-static-assertions.test.ts:354-356`, `:359-361`; external owner: surface-freeze maintainers) and **D4** (no global depth/complexity limiter — external owner: Financial Safety Verification, `docs/planning/TICKETS.md:2985-3026`) added in the same table format as D1/D2, both ❌ Blocked with external-target notes mirroring the Gate Rule wording; the Gate Rule paragraph updated from "Rows D1/D2" to "Rows D1–D4".

**Gate command + raw output (run from repo root):**

```
$ grep -c "❌\|⚠️" ai/plans/sprint_2/fee_escrow_and_teacher_wallet_crediting-crediting/deferred-items.md
6
```

**Line-by-line accounting of the 6 matching lines** (each re-checked by `grep -n`):

| Line | Content | Target Task in-plan? |
|---|---|---|
| 13 | D1 row — runtime escrow idempotency-key contracts | **No** — Financial Safety Verification ticket (`docs/planning/TICKETS.md:2985-3026`) |
| 14 | D2 row — wallet/ledger Insert/Return type exports | **No** — first consumer ticket (expected: Withdrawal Workflow, `:1799-1845`) |
| 15 | D3 row — stale frozen-root-inventory SDL pins | **No** — repo surface-freeze maintainers / next surface-touching ticket |
| 16 | D4 row — no depth/complexity limiter | **No** — Financial Safety Verification ticket (`:2985-3026`) |
| 20 | Status Legend line (⚠️/❌ vocabulary definition) | not a row |
| 24 | Gate Rule paragraph (quotes the grep pattern `❌\|⚠️` itself and the phrase "their ❌ denotes") | not a row |

**In-plan ❌/⚠️ target count = 0 → the GATE PASSES** per the ledger's Gate Rule (only rows whose Target Task is inside this plan count).

*R1-fix update:* after ledger row **D5** was added (stale schema-docblock finding, R1-security), the raw count is **7** — the D1–D5 rows at `:13`–`:17`, the Status Legend line at `:21`, and the Gate Rule paragraph at `:25` (each re-checked by `grep -n`); the in-plan target count remains **0** and the gate substance is unchanged. See `worklog.md` task R1-fix.

**DEVIATION FROM THE "EXPECTED RAW OUTPUT 3" WORDING — recorded explicitly, not massaged:**

1. The plan's expected value of 3 (D1, D2, Status Legend) was already an undercount **before** Task 4: running the identical grep against the committed pre-Task-4 ledger (`git show HEAD:…deferred-items.md | grep -c "❌\|⚠️"`) returns **4**, because the Gate Rule paragraph itself matches (it embeds the literal pattern `❌\|⚠️` when quoting the command, and uses the ❌ glyph in prose). `outcome/plan-review-R1.md` finding #6 recorded the same undercounted "3".
2. After adding D3/D4, the raw count is therefore **6** (4 deferral rows + Status Legend + Gate Rule line), not the orchestrator-authorized estimate of 5 (which assumed the same undercount basis as the plan's "3"). An alternative was considered — rewording the Gate Rule line to avoid the literal glyphs, which would have yielded exactly 5 — but that would be adjusting the evidence to fit the estimate, so the line was left self-referential and the actual count recorded here.
3. The substantive gate criterion is unaffected by either deviation: **zero of the six matching lines is a ledger row whose Target Task is inside this plan.** All four ❌ rows are external-target (exempt per the Gate Rule), and the two non-row matches are the legend vocabulary and the gate definition itself.

## 4. SEC / SR / IV

- **SEC:** the doc contains no secrets, no env values, no credentials (grep-verified). Security claims are the verified posture only: BOLA oracle-safe denial, BOPLA fee-omitting input, BFLA role scopes, DB-layer immutability — and the depth/complexity posture is stated as **no global limiter exists** with the three real compensating guards, never claiming a limiter. The arbiter discipline (guarded predicate, classification-only probes) is quoted with citations rather than asserted.
- **SR:** post-write re-grep of the doc's load-bearing citations all resolve (spot-check set: booking :127-159/:156/:182-185, primitive :404-419/:415/:433-441, settle :433-455/:447-452/:448, confirmation :48-59/:66-76/:86-104/:112-139/:129/:135-138, refund :222-237/:236, wallet repo :59-64/:78-110/:94-96/:103-110, wallet schema :36-38, txn schema :38/:50-52, session schema :15-20/:32-39/:63-65/:70, locale en :90/ar :88/labels :186, fees :27-33, mutation :87/:98-103/:127-131/:135-139/:255/:270-275/:293/:306-308, wallet query/mutation :46-57/:64/:46-50/:53/:62-67/:75, pothos :170/:174-175/:91/:126/:145-148, transport :49/:108-113, route :26/:49, service :624/:633-640/:732, contracts :34/:46/:73, db-cleanup :229; test anchors: svc :394/:470/:477/:1109/:1363/:1726/:1766/:1793/:2398/:2441/:2471/:2524/:2574 + chaos :2315/:2347, wallet :102/:132/:208/:224/:237/:251/:295, repo :490/:1256/:1430/:1552/:1582/:1617, journeys DC :348/:363/:451/:492/:561/:620/:686-688 + LC :369/:402/:492/:611/:645 + DEN :200/:236/:319, wire :395/:404/:416/:557/:573, surface pins :644/:747/:1883/:354-356/:359-361). Every cited test is a suite that ran green in Tasks 1–3. No source or test file was edited to align citations.
- **IV:** repo-root `AGENTS.md` (416 lines, read in Tasks 1/3 and re-honored): the doc is a canonical project-structure documentation file under `docs/billing/` (the documented exception to the no-summary-file rule — same class as the sibling domain docs), no other files were created, no `console.*`/`Translation.` concerns apply (markdown only), and no production code was touched. `docs/README.md` surveyed for conventions; the doc follows the established `docs/billing/*` heading/numbering style (Why → model → matrix → invariants → rules → anti-patterns → hand-offs → rollout → related).

## 5. §4.QL — Quality Loop

`bun run scripts/health/sub-loop.ts <file> --lifecycle tsgo` run on all three touched files (the `duplicates` lifecycle remains structurally unreachable for `.md` files in this sandbox — oxlint/biome have no lintable files and exit 1; environment-wide, reproduced on pre-existing tracked `.md` files in T1 §5; not re-litigated):

| File | Result |
|---|---|
| `docs/billing/escrow-and-wallet-crediting.md` | exit 0 |
| `ai/plans/sprint_2/fee_escrow_and_teacher_wallet_crediting-crediting/deferred-items.md` | exit 0 |
| `ai/plans/sprint_2/fee_escrow_and_teacher_wallet_crediting-crediting/outcome/4.x-knowledge-propagation-outcome.md` | exit 0 |

## 6. Files Modified / NOT Modified

- **Created:** `docs/billing/escrow-and-wallet-crediting.md`; `outcome/4.x-knowledge-propagation-outcome.md`.
- **Edited (plan bookkeeping only):** `deferred-items.md` (D3/D4 rows + Gate Rule wording), `tasks.md` (task 4 checkboxes).
- **Zero production files touched:** `git diff --name-only -- backend shared app frontend test docs` = 0 files (the new doc lives under `docs/billing/` but is untracked/new, not a tracked modification).

## 7. Anomalies

1. **Deferred-gate raw count = 6, not the plan's stated "expected 3"** (and not the authorized estimate of 5): full reasoning and line accounting in §3 — the Gate Rule paragraph self-match was missed in the plan's authoring-time count (committed HEAD already yields 4). Gate substance unaffected: zero in-plan targets → PASS.
2. The three pre-existing frozen-inventory pin failures (D3) remain unfixed by design (external owner); they are referenced in the doc only as a housekeeping corner, never as escrow evidence.
3. `duplicates` sub-loop lifecycle remains structurally broken for `.md` files (T1 §5 cross-file dependency stands); `--lifecycle tsgo` used per precedent, exit 0 on all three files.
4. No other anomalies: no suite runs were required by Task 4 (all tiers already green from Tasks 1–3; no re-runs needed and none fabricated).
