# Outcome — Task 1: Service & Repository Suite Re-Runs (escrow/credit/refund proof)

**Date:** 2026-09-15 (sandbox execution)
**Plan:** `ai/plans/sprint_2/Fee Escrow & Teacher Wallet Crediting-crediting/`
**Task:** 1 — re-run the three DB-backed suites, assert green, audit the cited guards.
**Verdict:** ALL THREE SUITES GREEN (150 pass / 0 fail total). Every cited escrow/credit/refund construct was observed passing. SEC audit: all four guard classes verified present; two localized-key citations and five service-suite citations have drifted line numbers (constructs located by grep at their current lines — no source file was touched).

---

## 1. Suite Results

All runs used the mandated log-capturing runner (`bun run test/scripts/run-test.ts <path>` — never raw `bun test`). The runner self-loads `.env.test` and pins `DATABASE_URL`; no env override was needed.

| # | Suite | Result | Exit | Log artifact (repo-relative) |
|---|---|---|---|---|
| 1.1 | `backend/services/classes/session-lifecycle.service.test.ts` | **72 pass / 0 fail** / 860 expect() calls, 72 tests in 1.68s | 0 | `logs/2026-09-15T02-52-31/backend/services/classes/session-lifecycle.service.test.ts.log` |
| 1.2 | `backend/services/billing/wallet.service.test.ts` | **9 pass / 0 fail** / 92 expect() calls, 9 tests in 368ms | 0 | `logs/2026-09-15T02-52-57/backend/services/billing/wallet.service.test.ts.log` |
| 1.3 | `backend/db/test/repo/classes/session.repository.test.ts` | **69 pass / 0 fail** / 450 expect() calls, 69 tests in 808ms | 0 | `logs/2026-09-15T02-53-05/backend/db/test/repo/classes/session.repository.test.ts.log` |

Baseline comparison: baseline is all-green (0 errors / 0 warnings / lint success per `outcome/0.1-baseline-outcome.md`); these runs are all-green too, so **no failure is reported and none was absorbed** (tasks.md rule 6 satisfied trivially).

## 2. Cited Escrow Cases — Observed Test Names (suite output) and Current `path:line`

Authoring-time citations were checked against the live files. Where a line number drifted, the construct was located by grep at its CURRENT line; the drifted value is recorded alongside. No file was edited to "fix" a citation.

### 2.1 Service suite (`backend/services/classes/session-lifecycle.service.test.ts`, 3248 lines)

| Cited | Current | Drift | Observed passing test (verbatim from the `(pass)` log lines) |
|---|---|---|---|
| :394 (hold-at-booking) | :394 | exact | `trial-hit branch: books on the trial lane, holds the fee, records Trial provenance, debits exactly one trial unit` |
| :470 (insufficient-balance rollback) | :470 | exact | `total-miss branch: an empty student is denied INSUFFICIENT_BALANCE with zero rows and the key stays reusable` |
| :1109 (fee boundary) | :1109 | exact | `fee boundary: both intents carry the platform fee as a decimal STRING verbatim (never a number)` |
| :1363 (double-cancel single refund) | :1363 | exact | `REQ-042: double-cancel refunds EXACTLY once — the second cancel is a SESSION_INVALID_TRANSITION with the balance unchanged` |
| :2396 (settle-credits-once) | :2398 | +2 | `confirmSessionCompletion happy path: student stamp + hold release + EXACTLY ONE earning credit (ledger row + wallet increment) in one flow` |
| :2439 (replay zero-writes, student) | :2441 | +2 | `confirmSessionCompletion is IDEMPOTENT: a replayed confirm returns the current row with ZERO new financial writes` |
| :2469 (replay zero-writes, teacher no-op) | :2471 | +2 | `confirmSessionCompletion by the TEACHER is an idempotent no-op: the row returns untouched, zero financial writes` |
| :2522 (sweep refund) | :2524 | +2 | `sweepExpiredSessions: expired scheduled rows cancel + same-lane refund; live rows untouched; second sweep is a zero-row no-op` |
| :2572 (sweep refund, legacy no-hold) | :2574 | +2 | `sweepExpiredSessions: a no-hold legacy row (NULL lane) cancels with NO refund counted` |
| :1791 (arbitration refunds) | :1726 / :1766 (refund arms) | drifted | `resolveSessionDispute CANCEL outcome: the SAME same-lane refund primitive fires inside the arbitration transaction — refund and status flip commit atomically` (:1726) and `resolveSessionDispute CANCEL on an unheld disputed row refunds nothing; a whitespace-only note persists NULL` (:1766) |

Note on :1791: in the current file that line is the closing brace of the :1766 test; the arbitration-refund constructs themselves are the two tests at :1726 and :1766 (both observed passing). The adjacent arbitration test at :1793 (`resolveSessionDispute COMPLETE outcome: consumes the hold with NO wallet credit, writes the end stamp, preserves the start stamp`) also passed and is the hold-consumption arm of arbitration.

Drift pattern: a single +2 insertion somewhere between :1363 and :2396 shifts every citation below that point; the four exact hits (:394/:470/:1109/:1363) bound it. The last commit touching this file is `e0b1184` (`feat(billing): expire overdue subscription windows, deny stale bookings (#156)`).

### 2.2 Wallet suite (`backend/services/billing/wallet.service.test.ts`)

All six citations resolved EXACTLY — no drift:

| Cited | Current | Observed passing test |
|---|---|---|
| :102 (lazy-ensure) | :102 | `a brand-new teacher gets an honest zeroed wallet with an empty ledger (lazy ensure)` |
| :132 (ledger ordering) | :132 | `the ledger page returns the newest-first order over a seeded history` |
| :208 (insufficient-funds zero-commit) | :208 | `insufficient funds: localized WALLET_INSUFFICIENT_FUNDS conflict and ZERO committed rows (orphan pending row rolls back)` |
| :237 (exact-balance guard) | :237 | `exact-balance withdrawal succeeds (the guard is \`balance >= amount\`)` |
| :251 (validation matrix) | :251 | `validation matrix: every malformed amount fails pre-DB with WALLET_INVALID_AMOUNT and zero side effects` |
| :295 (governance deny) | :295 | `governance deny: a deleted caller is FORBIDDEN on both flows (defense in depth over the GraphQL gate)` |

### 2.3 Repository suite (`backend/db/test/repo/classes/session.repository.test.ts`)

All six citations resolved EXACTLY — no drift:

| Cited | Current | Observed passing test |
|---|---|---|
| :490 (cancel-clears-hold) | :490 | `cancelSessionOnce by the student cancels a scheduled row, clearing the hold marker but keeping the provenance lane` |
| :1430 (double-cancel single winner) | :1430 | `double-cancel under Promise.allSettled produces exactly one winner — the hold can never be double-released` |
| :1582 (confirm-vs-sweep race) | :1582 | `confirm-then-sweep race: the confirmation consumes the escrow and the sweep matches zero rows` |
| :1617 (confirm-vs-sweep race, mirror) | :1617 | `sweep-then-confirm race: the sweep releases the escrow and the confirmation matches zero rows` |
| :1552 (sweep idempotence) | :1552 | `double sweep under Promise.allSettled cancels each overdue row exactly once — the re-run matches zero rows` |
| :1256 (escrow-consumed-by-earning) | :1256 | `a student-confirmed row never matches — its escrow was consumed by earning, not held for refund` |

## 3. SEC — Security & Tenancy Audit (guards at their `path:line`)

| Guard | Cited | Verified at (actual) | Verdict | Evidence |
|---|---|---|---|---|
| BOLA oracle-safe denial | `session-lifecycle.confirmation.ts:66-76` | `backend/services/classes/session-lifecycle.confirmation.ts:66-76` — exact | ✓ | `rejectUnknownCaller` (:66-76) throws `NotFoundError("SESSION", t.sessionNotFound)` for BOTH the missing-row and the foreign-row cases; the classifier at :133-138 sends `probe === null \|\| (probe.studentId !== callerUserId && probe.teacherId !== callerUserId)` to the same function — missing vs foreign is indistinguishable to the caller. The `sessionId` appears only in the server-side `logger.logDomainError` payload, never in the thrown message. Asserted in-suite by the :2497 service test (`foreign caller → oracle-safe SESSION_NOT_FOUND`, passed) and the repo races at :1582/:1617. |
| BOPLA — booking input omits fee | `backend/types/classes/session.types.ts` | `backend/types/classes/session.types.ts:29-42` | ✓ | `SessionSubmitInput` (:39-42) is exactly `{ teacherId, intent }`; the docblock (:30-37) names the platform fee among the server-controlled columns that are "structurally absent by construction". The mutation maps field-by-field (`session-lifecycle.mutation.ts:127-131`), so a client payload cannot carry, spoof, or influence the fee. |
| Role scope — `createSession` STUDENT-only | `session-lifecycle.mutation.ts:87` | `backend/graphql/mutation/classes/session-lifecycle.mutation.ts:98-103` (field registered at :87) | ✓ | `authScopes: { $all: { authenticated: true, role: [UserRole.Student] } }` — explicit `$all` conjunction; teacher-role callers fail the `role` leg (unconditional localized FORBIDDEN per the file's :48-60 contract note). |
| Role scope — `confirmSessionCompletion` participant | same file | `backend/graphql/mutation/classes/session-lifecycle.mutation.ts:306-308` (field registered at :293) | ✓ | `authScopes: { authenticated: true }` only; the participant predicate lives service-side (both participants may act; every other authenticated role gets the oracle-safe `SESSION_NOT_FOUND` per the :301-305 comment). Service behavior proven by the passing teacher-no-op and foreign-caller tests above. |
| Role scope — `resolveSessionDispute` ADMIN | same file | `backend/graphql/mutation/classes/session-lifecycle.mutation.ts:270-275` (field registered at :255) | ✓ | `authScopes: { $all: { authenticated: true, role: [UserRole.Admin] } }`; the service additionally re-asserts admin + governance (defense in depth, :268-269 comment). |
| Localized key `insufficientBalance` (en) | `shared/locale/en/errors/index.ts:85` | `shared/locale/en/errors/index.ts:90` | ✓ key present; line drifted +5 | Current :90 is `insufficientBalance: "Your balance is insufficient for this request."`; :85 today is `teacherNotCertified`. Key is typed in `shared/locale/types/errors/labels.ts:186` and pinned by the locale parity tests (`shared/locale/wallet-namespace.parity.test.ts:50`, `shared/locale/sessions-namespace.parity.test.ts:137`, `shared/locale/adminFinance-namespace.parity.test.ts:58`). |
| Localized key `insufficientBalance` (ar) | `shared/locale/ar/errors/index.ts:84` | `shared/locale/ar/errors/index.ts:88` | ✓ key present; line drifted +4 | Current :88 is `insufficientBalance: "رصيدك غير كافٍ لإتمام هذا الطلب."`; :84 today is `homeworkRequiresReport`. |

Consumption-side cross-check (grep-verifiable): the wallet-suite denial at `wallet.service.test.ts:224` and the booking denial at `session-lifecycle.service.test.ts:477` both assert `t().insufficientBalance` through `expectDomainDenial`, so the localized key is exercised by the passing suites, not merely declared.

## 4. SR — Semantic Review

- All citations in this outcome resolve to grep-able constructs (section 2/3 tables carry both the cited and the current line).
- No plan-meta references were introduced anywhere (no code was written by this task). One pre-existing test name contains a requirement label (`REQ-042` at `session-lifecycle.service.test.ts:1363`) — that label predates this plan (committed state, last touched in `e0b1184`) and refers to the Sprint 1 lifecycle requirement register; it is quoted here only as observed test output, not adopted by this plan.
- No dead claims: every ✓ above is backed by either a suite `(pass)` line in the log artifacts of section 1 or a file:line that a grep can re-verify today.

## 5. QL — Quality Loop (outcome file)

Command per tasks.md: `bun run scripts/health/sub-loop.ts ai/plans/sprint_2/fee_escrow_and_teacher_wallet_crediting-crediting/outcome/1.x-suites-outcome.md --lifecycle duplicates`.

| Stage | Result |
|---|---|
| tsgo | ✓ passed (exit 0) — project-wide check, no errors for this file |
| oxlint | ✗ structural failure (see below) — NOT a content violation |

**Anomaly (environment-wide, pre-existing):** the `duplicates` lifecycle cannot exit 0 for ANY markdown file in this sandbox. `checkOxlint` runs `bunx oxlint --deny-warnings --ignore-path .gitignore <file>` (scripts/health/sub-loop-checks.ts:127); oxlint 1.82.0 filters out non-lintable extensions and exits 1 with `No files found to lint` on a `.md` target. The subsequent biome stage fails identically (`No files were processed in the specified paths`, exit 1 — biome.json does not process markdown). Reproduced on a PRE-EXISTING tracked plan file (`ai/plans/sprint_2/fee_escrow_and_teacher_wallet_crediting-crediting/outcome/0.1-baseline-outcome.md`: oxlint exit 1, biome exit 1) and on a finished plan's `tasks.md` (sub-loop `--lifecycle duplicates` exit 1 at oxlint) — i.e., the failure is tool/config behavior, independent of this file's content. The checks that do apply to markdown content were run directly: tsgo ✓, and both linters confirm 0 markdown files are even candidates (nothing to violate).

Fixing this requires editing `scripts/health/sub-loop-checks.ts` (gate the extension-blind linters for non-lintable targets). That is production tooling — out of this verification plan's no-modification scope, so per the repo's Fix-Or-Report rule it is reported, not patched:

```
CROSS-FILE DEPENDENCY:
  Target file: ai/plans/sprint_2/fee_escrow_and_teacher_wallet_crediting-crediting/outcome/1.x-suites-outcome.md
  Blocked by: scripts/health/sub-loop-checks.ts (checkOxlint/checkBiome pass non-lintable targets to oxlint/biome, which exit 1 on "no files")
  Rule violated: tasks.md subtask pipeline "loop must exit 0 for any file the plan touches" (markdown case unreachable in this environment)
  Required fix: skip oxlint/biome (or make "0 lintable files" a pass) when the target extension is not lintable — tooling-maintainer decision, not this plan's
```

Recorded exit codes: `--lifecycle duplicates` → 1 (stopped at oxlint, structural); `--lifecycle tsgo` → 0. No source or tooling file was modified to work around this.

## 6. IV — Instruction Verification

- Repo-root `AGENTS.md` (416 lines) read. Relevant rules and how they were honored: DB-backed suites MUST run through `test/scripts/run-test.ts` (honored — section 1 commands); never raw `bun test`; "NEVER create summary markdown files" — this outcome file is not a free-floating summary: it is the plan-mandated artifact of tasks.md protocol step 5 (`outcome/<task-id>-outcome.md`), the documented exception path for project-structure documentation; no other report/summary file was created.
- No `AGENTS.md` exists under `ai/` or `ai/plans/` (verified by `ls`); the root file governs markdown plan files, as tasks.md states.
- `runInRollback`/deadlock rules and `console.*` rules: not applicable — zero source files were read into modification, only read.

## 7. Files NOT Modified (and why)

- **Zero production files modified.** `git status --porcelain` after all runs: the only entry is `ai/plans/sprint_2/fee_escrow_and_teacher_wallet_crediting-crediting/outcome/0.1-baseline-outcome.md` (modified) — the Task 0 sandbox re-capture append, pre-existing before this task. `git diff --name-only -- backend shared app frontend test docs` → 0 files. The 2 untracked `.deb` files recorded in the worklog are no longer present in status.
- Citation line drift was handled by locating constructs at their current lines (sections 2/3). Editing source or tests to "re-align" citations is forbidden by the task scope and was not done.

## 8. Carry-Forward Knowledge for Tasks 2–4

1. **Line-drift accounting**: the service suite has a uniform +2 shift below :1363; Tasks 2–4 cite more service-suite lines (e.g. Task 3's traceability matrix cites `wallet.service.test.ts` and repo lines) — they should re-grep every citation the same way and record current lines; the wallet and repo suites' citations are currently exact.
2. **Locale citations must be re-grepped**: `insufficientBalance` sits at en :90 / ar :88 (not :85/:84 as the plan artifacts say). Task 3's traceability matrix and Task 4's propagation doc should cite the current lines. The key is additionally guarded by three parity tests, so any doc claim about it can also cite those.
3. **Journey expectations**: the dual-confirmation service tests (happy path :2398, replay :2441, teacher no-op :2471) and repo races (:1582/:1617) already prove exactly-once credit and single-refund semantics at service/repo tiers; the journey tier (Task 2) is expected to observe the same outcomes with committed fixtures — its `afterAll` zero-residue probes are the new surface to watch.
4. **Runner behavior**: all three suites complete quickly under the run-test lock (1.68s / 0.37s / 0.81s of test time); the lock serializes suite runs, so Tasks 2–4 should keep runs sequential.
5. **`:1791` citation caution**: any downstream artifact still citing `session-lifecycle.service.test.ts:1791` for "arbitration refunds" should be re-pointed to :1726 (refund arm) / :1766 (no-refund arm); :1793 is the hold-consumption arm.
6. **Sub-loop on markdown outcomes**: Tasks 2–4 will hit the same structural `--lifecycle duplicates` failure on their `.md` outcome files (section 5). Budget for it: run `--lifecycle tsgo` for the meaningful markdown signal and record the oxlint/biome structural exit-1 with the reproduction evidence, exactly as done here.

## 9. Cross-File Dependencies Discovered

- `session-lifecycle.confirmation.ts` denial path depends on `SessionRepository.findTransitionProbe` (probe is classification-only, :133-138) — Task 3's BOLA matrix row should cite both files together.
- The `insufficientBalance` copy is shared by three domains (booking denial, withdrawal denial, and the adminFinance namespace reuse test) — docs written in Task 4 must present it as a REUSED funds-denial key, consistent with `shared/locale/adminFinance-namespace.parity.test.ts`.
- The mutation file's `$all` scope-conjunction pattern is load-bearing for the 401/403 split (:48-60 of the mutation file); Task 3's SDL/permissions pins should assert scope maps, not just field existence.

## 10. Anomalies

- Line-number drift on 5 service-suite citations (+2 each) and the 2 locale-key citations (+5 en / +4 ar), plus the ambiguous `:1791` arbitration citation (section 2.1 note). All constructs were located and observed passing; recorded per the task instruction ("record the ACTUAL current path:line"), no silent rewrites.
- Sub-loop `--lifecycle duplicates` on this outcome file exits 1 at oxlint — a structural, environment-wide markdown case documented in section 5 (reproduced on pre-existing tracked `.md` files; `--lifecycle tsgo` passes with exit 0). Reported per the Fix-Or-Report rule; not patched (tooling out of scope).
- No suite failures, no flakes, no env workarounds needed (`bun run test/scripts/run-test.ts` sufficed; `--env-file` override was never required).
