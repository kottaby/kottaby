# Task 0.2 Outcome — Prerequisite & Dependency Verification (Dependency Guard)

**Task ID:** 0.2 — Verify pre-existing foundations (READ-ONLY audit)
**Plan:** DEV3-010 — Real-Time Notification Engine (WebSocket)
**Plan directory (ACTUAL):** `ai/plans/sprint_2/dev3-010-real-time-notification-engine-websocket/`
**Date:** 2026-08-29
**Agent:** Task-0.2-Executor (dependency audit)
**Read-only guarantee:** zero source files modified. Only this outcome file + worklog append were written. `deferred-items.md` NOT touched (orchestrator owns it).

---

## 1. Checklist Verdicts

### ✅ Item 1 — `notifications` table schema + indexes

`backend/db/schema/notifications/notifications.ts` holds **exactly** the A.4 column set — no more, no less:

| Column | Evidence (file:line) |
|---|---|
| `id` | `notifications.ts:30` — `integer("id").primaryKey().generatedAlwaysAsIdentity()` |
| `user_id` | `notifications.ts:31-33` — FK → `users.id`, `onDelete: "cascade"` |
| `type` | `notifications.ts:34` — `notificationType("type").notNull()` |
| `title` | `notifications.ts:35` — `varchar(255).notNull()` |
| `body` | `notifications.ts:36` — `text` (nullable) |
| `is_read` | `notifications.ts:37` — `boolean`, default `false` (nullable) |
| `related_entity_type` | `notifications.ts:38` — `varchar(100)` (nullable) |
| `related_entity_id` | `notifications.ts:39` — `integer` (nullable) |
| `created_at` | `notifications.ts:40` — `timestamp.defaultNow().notNull()` |

Indexes (`notifications.ts:42-45`):
- `notifications_user_id_idx` on `(user_id)` — line 43
- `notifications_user_id_is_read_idx` on `(user_id, is_read)` — line 44

No `updated_at` column (write-once + in-place `is_read` flip per docblock `notifications.ts:19-20`). Barrel re-export confirmed: `backend/db/schema/notifications/index.ts:1` (`export * from "./notifications"`).

### ✅ Item 2 — `notificationType` pgEnum ↔ TS mirror byte-parity (7 values, same order)

- **pgEnum:** `backend/db/schema/enums.ts:46-54` — `pgEnum("notification_type", [...])` with EXACTLY: `session_request`, `session_completion`, `session_cancellation`, `parent_link_request`, `system_broadcast`, `payment_confirmation`, `evaluation_result`.
- **TS mirror:** `backend/enum/notifications/notification-type.enum.ts:5-13` — `enum NotificationType` with EXACTLY the same 7 string values in the same order (SessionRequest…EvaluationResult).
- Byte-identical (7/7, order-sensitive). The 1.2 parity static test can pin these as-is.

### ❌ Item 3 — `backend/db/repo/notifications/` repository surface: **DIRECTORY DOES NOT EXIST** (see Blocker B1)

- `backend/db/repo/` contains ONLY: `AGENTS.md`, `index.ts`, `admin/`, `parents/`, `students/`, `teachers/`, `users/` (LS evidence).
- **NO `notification.repository.ts` anywhere in the repo** — repo-wide glob `**/*notification*` under `backend/` matches only 4 files: `db/schema/notifications/notifications.ts`, `enum/notifications/notification-type.enum.ts`, `types/notifications/notification.types.ts`, `types/contracts/session-notification.contract.types.ts`.
- Repo-wide grep for `NotificationRepository`: ZERO code matches (only docs/aspirational AGENTS.md mentions + this plan's own specs/tasks).
- The ONLY import of `schema/notifications` in the entire backend is `backend/types/notifications/notification.types.ts:1` (the `NotificationSelectType`/`NotificationInsertType` aliases). Nothing reads/writes the table today.
- **Plan premise mismatch:** `plan.md:349` says "EXISTING namespace, additive only"; `specs.md:56` REQ-004(c) says "the pre-existing `NotificationRepository` namespace — VERIFY its methods and EXTEND additively". That premise is **false in this tree** (see §6 Blockers and §2 catalogue).

### ✅ Item 4 — DEV2-001 `verifyAccessToken`

- **Path:** `backend/lib/auth/jwt.ts`
- **Signature (jwt.ts:143):** `export async function verifyAccessToken(token: string): Promise<AccessTokenPayload | null>` — returns `null` on ANY failure, never throws (docblock :138-142).
- **Payload (jwt.ts:104-108):** `interface AccessTokenPayload { readonly userId: number; readonly role: string }`.
- Consumers: `backend/graphql/gqlContextFactory.ts:29` (import), `:191` (call); `backend/lib/auth/server-auth.ts:36`, `:82`.

### ✅ Item 5 — DEV2-002 `authenticated` authScope

- **Defined at:** `backend/graphql/pothos/builder.ts:108-128` (`authScopes: ctx => ({ ... })` initializer on `gqlSchemaBuilder`).
- `authenticated` implementation (builder.ts:113-118): throws `UnauthorizedError("Authentication required.")` when `!ctx.user`, else `true` → 401/`UNAUTHORIZED` semantics; scope misses mapped at `scopeAuthOptions.unauthorizedError` (builder.ts:97-107) so `role`/`permission`/`superAdmin` misses become localized `ForbiddenError` (403). Scope keys enumerated in `AuthScopes` type param (builder.ts:69-80): `authenticated`, `role`, `permission`, `superAdmin`, `notImpersonating`.
- **Usage example (auth.query.ts:41-43):**
  ```ts
  gqlSchemaBuilder.queryField("me", t =>
    t.field({
      type: UserPothosObject,
      nullable: true,
      authScopes: { authenticated: true },
      resolve: async (_root, _args, ctx) => ctx.user,
    })
  );
  ```

### ✅ Item 6 — DEV2-003 `SessionEventNotificationContract` / `SessionEventNotificationType`

- **Path:** `backend/types/contracts/session-notification.contract.types.ts` — exists, re-exported by `backend/types/contracts/index.ts:6`.
- Exports:
  - `SessionEventNotificationType` (:22-25) — union of `NotificationType.SessionRequest | SessionCompletion | SessionCancellation`.
  - `SessionEventNotificationEntityRef` (:32-40) — both-or-neither polymorphic pointer union (`{relatedEntityType, relatedEntityId}` | both-undefined).
  - `SessionEventNotificationContract` (:42-52) — `readonly userId`, `type`, `title`, `body`, `idempotencyKey?` (≤ per docs/IDEMPOTENCY.md), `entityRef`. BOLA note: `userId` recipient-resolved server-side (:43).
- Conformance/静态 tests already exist: `contracts.conformance.test-d.ts:32`, `contracts.static-assertions.test.ts:37`.

### ✅ Item 7 — DEV3-002 masking boundary

- **Path:** `backend/lib/errors/error-masking.ts` (886 lines) — GraphQL boundary finalizer module. Exports (per docblock :10-22): `isDomainError`, `maskInternalError`, `redactLogContext`, `finalizeGraphqlErrors`, plus `attachRawErrorHop`/`RAW_ERROR_HOP` envelope-hop key.
- Classification: DomainError ⇒ pass-through verbatim (code + localized message preserved); everything else ⇒ masked INTERNAL_SERVER_ERROR + exactly one correlated `logger.error`. Consumed by `backend/graphql/graphqlErrorsFinalizer.ts`. Tests: `backend/lib/errors/test/error-masking.test.ts`, `security-abuse.contract.test.ts`, `concurrency-chaos.contract.test.ts`.

### ✅ Item 8 — DEV3-003 gateway posture

- **Path:** `backend/lib/gateway/` EXISTS with: `public-operations.ts`, `transport-guard.ts`, `route-inventory.ts`, `version.ts`, `index.ts` (barrel) + co-located tests.
- `public-operations.ts:36-46` — closed frozen allowlist `PUBLIC_OPERATION_NAMES = ["login", "refreshToken", "logout", "registerUser", "recitationReadings", "_health"] as const`; exact-match semantics; `me` deliberately ABSENT (gated `authenticated`). Any new scopeless operation MUST be registered here with a security rationale — **directly relevant to the notifications GraphQL surface (Phase 3)**: `myNotifications`-class fields must NOT land in this allowlist.

### ✅ Item 9 — `backend/db/test/entity-setup.ts` user-fixture helpers

Exists. Exported factory helpers (all take `tx: DBTransaction` FIRST):

| Helper | Signature | Line |
|---|---|---|
| `createTestUser` | `(tx, overrides?: Partial<UserSelectType>) → Promise<UserSelectType>` — random unique email; `role` overridable (`{ role: "admin" }` pattern in docblock :43) | :45 |
| `createTestStudent` | `(tx, userId, overrides?) → Promise<StudentSelectType>` — unique `handshakeCode` | :75 |
| `createTestParent` | `(tx, userId) → Promise<ParentSelectType>` | :102 |
| `createTestApplicant` | `(tx, userId, overrides?) → Promise<ApplicantSelectType>` — `status='pending'` default | :114 |
| `createTestAdmin` | `(tx, userId) → Promise<AdminSelectType>` | :140 |

Note for Task 2.1 actor-context factory: there is **no `createTestTeacher`** (no `teachers` table in this trimmed tree — teacher actor = `createTestUser(tx, { role: "teacher" })` + `createTestApplicant`); "certified teacher" casting must be derived from user role + applicant overrides.

### ✅ Item 10 — Spec docs (0.2.IV evidence)

- `docs/specs/open-decisions-and-gaps.md` — **A.4** at :29-33: "✅ RESOLVED — Create a `notifications` table … `id`, `user_id` (FK), `type` (notification_type enum), `title`, `body`, `is_read`, `related_entity_type`, `related_entity_id`, `created_at`. New `notification_type` enum created." Matches the audited schema exactly. (Also :234 schema-changes summary row.)
- `docs/specs/state-machine-invariants.md` — **INV-P2** :218 ("parent access is strictly read-only") and **INV-P3** :219 ("A parent receives real-time notification when a linked child's session completes") — both present; INV-P3 is the real-time driver invariant for this ticket.
- `docs/DATABASE_MIGRATIONS.md` — exists (154 lines); migration pipeline = `extensions` → schema (drizzle-kit generate) → `combined_custom_logic`; `db reset`/`cleanGenerate` permanently disabled by policy (:18, :26); `db push` for schema changes. Consistent with Phase 1 invariant (zero `git diff` on schema/migration).

### ✅ Item 11 — `docs/IDEMPOTENCY.md`

Exists (`docs/IDEMPOTENCY.md`). Contract: `X-Idempotency-Key` header, UUID-v4, 24h expiry window, `409 Conflict`/`DUPLICATE_REQUEST` on replay; mandated key set = Student/Invoice/Class-Instance/Payment creations. **Confirms plan D5's ruling**: notification emission is OUTSIDE the doc's mandated key set, so the plan's fail-OPEN deviation (D5, `plan.md:97`) is a documented divergence, not a violation. Referenced by `session-notification.contract.types.ts:48`.

### ⚠️ Item 12 — Cache adapter seam + env-config registry: **NO cache adapter code exists; NO `env-config-keys.ts` exists** (advisories A1/A2 — see §6)

**Cache/Redis seam (A1):**
- `backend/services/redis/` contains **ONLY `README.md`** — the documented `IRedisService` interface (`get/set/incr/del/expire/ttl/pipeline/ping/disconnect`, `set(key, value, { ex })` style) and `RedisServiceFactory` are **documentation-only; zero implementation files** (`IRedisService.ts`, `LocalRedisAdapter.ts`, `UpstashRedisAdapter.ts`, `RedisServiceFactory.ts`, `index.ts` are all absent).
- `backend/lib/ratelimit.ts` — `checkRateLimit(_identifier, limiter)` (:83) is a **stub** (placeholder params, no Redis call).
- **No SET NX EX capability exists anywhere in backend code today.**
- Dependencies ARE installed: `ioredis@^6.0.0` + `@upstash/redis@^1.38.2` (`package.json` dependencies). `.env` has `CACHE_PROVIDER=postgres`, `REDIS_PROVIDER=local` (read only by `backend/lib/env.ts`; nothing else consumes them).
- **Consequence for this plan:** plan.md:341 (D5/REQ-016/REQ-078) already rules the cache adapter is **INJECTED at module seam and mocked in service tests** — so there is no seam to reuse; Task 2.6 must **define its own injected SET-NX-EX port** (stateless pure claim helper `notif:emit:<sha256(userId:type:key)>`, 24h TTL, fail-open). NOT a blocker — the plan never assumed an existing adapter — but the engine's Redis claim adapter must be built fresh (env-gated suite per Task 2.5 conventions).

**Env-config registry (A2):**
- **No `env-config-keys.ts` (or similar registry) exists anywhere** — confirmed by repo-wide grep; also confirmed by dev3-003 precedent: `ai/finished_plans/sprint_0/dev3-003-api-gateway-routing-skeleton/outcome/0.2-outcome.md:18` — "no `env-config-keys.ts` exists ⇒ `APP_VERSION` registration NOT mandatory; `resolveAppVersion()` may read `process.env` directly".
- The **effective env-config module is `backend/lib/env.ts`**: `getEnvironmentConfig()` :85 (cached snapshot), `EnvironmentConfig` interface :49-62 (`cacheProvider` :57), `resetEnvironmentCache()` :99 (the `reset*` invalidation path), `getEnv` :106, `optionalEnv` :113, `requireEnv` :123, `resolveEnvConfig(key)` :136.
- **Consequence for Task 1.5:** the wording "register in the env-config keys registry" / "registry-inclusion test" presumes a registry file that does not exist. Task 1.5 must either (a) follow the dev3-003 precedent (typed getters reading via `resolveEnvConfig`/`getEnv` from `backend/lib/env.ts`, defaults + `resetEnvironmentCache()`-covered invalidation, keys documented in `.env.example`), or (b) have the orchestrator create a registry module first. **Orchestrator decision required** (recorded as advisory, not blocker — precedent exists).

### ✅ Item 13 — `backend/lib/logger.ts` with `logDomainError`

- **Path:** `backend/lib/logger.ts` (84 lines).
- `export const logger` (:44-81) — `info/warn/error/debug` + **`logDomainError(message: string, ctx?: DomainErrorContext): void`** (:72) — `debug` tier under `TEST_SERVER=1`/`NODE_ENV=test`, `warn` in production; `[DOMAIN]` prefix + JSON context.
- `export interface DomainErrorContext` (:20-26): `{ code?, entity?, entityId?, locale?, [key: string]: unknown }`.
- `export type Logger = typeof logger` (:83). Exactly the API the engine logging discipline (plan.md:347) targets.

### ✅ Item 14 — Test helpers inventory

| Helper | Exact location | Signature essence |
|---|---|---|
| `runInRollback<T>` | `backend/db/test/test-utils.ts:34` | `(fn: (tx: DBTransaction) => Promise<T>) → Promise<T \| undefined>` — top-level tx forced to ROLLBACK via sentinel; DB suites run via `bun run test/scripts/run-test.ts <path>` |
| `expectRepoError` | `backend/db/test/test-utils.ts:77` | `(fn: () => Promise<unknown>) → Promise<Error>` — try/catch replacement for `rejects.toThrow` (which deadlocks inside `runInRollback`) |
| `setupTestServerLifecycle` | `test/helpers/test-lifecycle.ts:41` | `(): void` — `beforeAll` boots `next dev --turbopack` on `TEST_PORT` (default 3066, `GRAPHQL_TEST_PORT` overridable), polls `{ _health { status } }`; `afterAll` kills |
| `testClient` | `test/helpers/graphql-test-helpers.ts:8` | preconfigured `ApolloClient` (HttpLink → `http://localhost:${TEST_PORT}/api/graphql`, `credentials: "include"`, `errorPolicy: "all"`, `fetchPolicy: "no-cache"`) |
| `expectMutationError` | `test/helpers/expect-mutation-error.ts:33` | `(error: unknown, expectedCode?: string) → CombinedGraphQLErrors` — asserts Apollo v4 container + first-error code via `extractErrorCode` |
| `extractErrorCode` / `TEST_PORT` | `test/helpers/graphql-test-helpers.ts` (exported :6-8; barrel re-export) | error-code resolution chain (v4 `errors[]` → v3 `graphQLErrors` → network fallback) |
| Barrel | `test/helpers/index.ts:1-9` | `import { expectMutationError, testClient, setupTestServerLifecycle, … } from "@/test/helpers"` |

### ✅ Item 15 — `frontend/graphql/sharedDocuments/` conventions

- **Directory exists** with sub-dirs `auth/` (`auth.documents.ts`, `recitation.documents.ts`) + `teachers/` (`applicant.documents.ts`), each with its own `index.ts` barrel; top-level barrel `frontend/graphql/sharedDocuments/index.ts` re-exports `./auth` + `./teachers` (the fuller layout in its `AGENTS.md` is aspirational — `notifications/` subdir does NOT exist yet and will be created by this plan's Phase 4).
- **Example structure** (`auth/auth.documents.ts:1-30`): `import { gql, type TypedDocumentNode } from "@apollo/client"` (NEVER `@apollo/client/core`); types from `@/frontend/graphql/generated/gql/graphql`; const naming `<camelCase>QueryDocument`/`MutationDocument`; selection sets MUST include `id` on every named object type.
- **Adding a new domain (the Phase-4 procedure, per AGENTS.md:68-74):** create `<domain>.documents.ts` in matching subdir → add `export * from "./<domain>.documents"` to subdir `index.ts` → ensure subdir exported from top-level `index.ts` → run `bun run generate:gqlSchema && bun codegen`.

### ✅ Item 16 — App-bar / nav config (frontend)

- **Nav config:** `frontend/views/dashboard/navItems.ts` — `DashboardNavItem { route, labelKey, Icon }` (:28-32); private `NAV_ITEMS_BY_ROLE: Record<UserRole, readonly DashboardNavItem[]>` (:51-80; Student/Teacher/Parent/Admin sets); `getNavItemsForRole(role)` (:88); `resolveNavItemLabel` (:104); re-exports `NotificationsIcon` (:110). **Per-role entries are added by editing `NAV_ITEMS_BY_ROLE`** (plan.md:475 sidebar entry goes here, positioned after each role's dashboard entry).
- **App bar:** `frontend/views/dashboard/DashboardAppBar.tsx` — MUI `AppBar`/`Toolbar` client component (`"use client"` :1), right-side cluster = locale switcher + theme toggle + user avatar + sign-out (:33-35). **The notification bell + `Badge` binds into this right-side cluster** (Task 4.3 target). Layout wrapper: `frontend/views/dashboard/DashboardLayout.tsx` (AppBar + Sidebar + main).

### ⚠️ Item 17 — `app/(dashboard)/` route group exists; **`withPageAuth` does NOT exist in this tree** (advisory A3)

- Route group EXISTS: `app/(dashboard)/` with `layout.tsx`, `dashboard/page.tsx`, `profile/page.tsx`, `student|teacher|parent|admin/dashboard/page.tsx`, and catch-all `[feature]/page.tsx` (→ `ComingSoonView`) — so `app/(dashboard)/notifications/page.tsx` (Task 4.1) slots in cleanly and real routes take precedence over the catch-all.
- **`withPageAuth` is ABSENT:** `backend/lib/auth/` contains only `password.ts`, `jwt.ts`, `server-auth.ts`, `cookies.ts`. `docs/app/with-page-auth.md` documents the *upstream* pattern (claims 59 consumer pages — this trimmed tree has ~8 dashboard pages). No consumer imports it.
- **Actual auth-guard convention in this tree:** the `(dashboard)` group layout delegates to `DashboardLayout` (`app/(dashboard)/layout.tsx:16-18`), whose **client-side guard** (`frontend/views/dashboard/DashboardLayout.tsx:29-35, 66-70`) resolves session via `useAuth()` and redirects anonymous users to `/login?redirect=<currentPath>` (via `buildLoginHref` same-origin validation). Per-role pages are thin: e.g. `app/(dashboard)/student/dashboard/page.tsx` = `createRoleDashboardPage(UserRole.Student, "/student/dashboard")`.
- **Consequence for Task 4.1** (tasks.md:397 "withPageAuth-class authenticated-all-roles guard"): a server-component `notifications/page.tsx` automatically inherits the layout-level client guard — the anonymous→`/login` redirect semantics are satisfied by the existing `DashboardLayout` WITHOUT any per-page wrapper. Task 4.1 must NOT attempt to import `withPageAuth`. Orchestrator to re-word ("withPageAuth-class" → "relies on `(dashboard)` layout guard") or accept as-is since intent is met.

### ✅ Item 18 — Health/test scripts

- `scripts/health/sub-loop.ts` — EXISTS (plus `sub-loop-uncommitted.ts`, `shared/sub-loop-types.ts`).
- `test/scripts/run-test.ts` — EXISTS (alongside `run-db-tests-parallel.ts`, `run-services-tests-parallel.ts`, `run-server-tests.ts`, `run-integration-tests-parallel.ts`, runner guard). DB-bound suites: `bun run test/scripts/run-test.ts <test-path>` per the execution protocol.

---

## 2. Repository Method Catalogue (input to Task 2.4)

**Current `NotificationRepository` surface: EMPTY — the namespace does not exist.**

- No `backend/db/repo/notifications/` directory; no `notification.repository.ts`; zero exported methods; zero consumers. The `backend/db/repo/AGENTS.md:26` layout entry ("notifications/ notification, notification-preferences") is aspirational, not actual.
- Nothing to re-implement, nothing to reuse — **Task 2.4's seven methods are ALL net-new** (plan.md §4.2 table :353-361: `createReturning`, `createManyReturning`, `countUnread`, `countForUser`, `listForUser`, `markReadOnce`, `markAllReadForUser`).
- **Creation procedure** (per `backend/db/repo/AGENTS.md:59-64` "Adding New Repositories"):
  1. Create `backend/db/repo/notifications/notification.repository.ts` exposing `export namespace NotificationRepository { … }`.
  2. Create `backend/db/repo/notifications/index.ts` with `export * from "./notification.repository";`.
  3. Add `export * from "./notifications";` to the top-level `backend/db/repo/index.ts` (currently only `./admin`, `./parents`, `./students`, `./teachers`, `./users` — index.ts:17-21).
- **Repo conventions to honor** (AGENTS.md :34-51): namespace pattern (see `UserRepository` at `backend/db/repo/users/user.repository.ts:32`); non-tx reads via `queryDb(tx)`; writes take `tx: DBTransaction` as LAST param; no business logic/permission checks; no hardcoded error strings (`getServerTranslations(locale, …)` from `@/shared/locale/server-graphql`); types from `backend/types/notifications/notification.types.ts` (`NotificationSelectType`/`NotificationInsertType` — already exist :3-4); no `inArray`+prepared-statement combo; import via top-level barrel `@/backend/db/repo`.
- **Impacted plan wording** (for orchestrator): plan.md:349 "(EXISTING namespace, additive only)", specs.md:56 REQ-004(c) "pre-existing NotificationRepository namespace", tasks.md:195 "Extend … additive methods only" — all must be read as **"CREATE the namespace; all seven methods are new"** (see Blocker B1).

---

## 3. Test-Helper Inventory (exact import paths)

```ts
// DB-tier (repo/service suites) — must run via `bun run test/scripts/run-test.ts <path>`
import { runInRollback, expectRepoError } from "@/backend/db/test/test-utils";
import { createTestUser, createTestStudent, createTestParent, createTestApplicant, createTestAdmin } from "@/backend/db/test/entity-setup";
import type { DBTransaction } from "@/backend/types"; // (re-exported also from test-utils)

// GraphQL integration tier
import { setupTestServerLifecycle, testClient, expectMutationError, extractErrorCode, TEST_PORT } from "@/test/helpers";
//   setupTestServerLifecycle → "@/test/helpers/test-lifecycle"      (:41)
//   testClient / extractErrorCode / TEST_PORT → "@/test/helpers/graphql-test-helpers" (:6-8)
//   expectMutationError → "@/test/helpers/expect-mutation-error"    (:33)

// Journey tier (Task 2.1 — layer `test/workflows/` does NOT exist yet; this plan creates it)
//   NEVER runInRollback; committed fixtures in beforeAll + tracked hard-delete afterAll
```

Domain-error classes for assertions: `backend/lib/errors.ts` — `DomainError` :19, `NotFoundError` :37, `UnauthorizedError` :44, `ForbiddenError` :51, `ValidationError` :65, `ConflictError` :159, `RateLimitExceededError` :166, `translateDbError` :181.

---

## 4. Conventions Quick-Reference (for later tasks)

**Auth scope syntax** (Pothos scope-auth; scope keys typed at `builder.ts:69-80`):
```ts
gqlSchemaBuilder.queryField("myNotifications", t => t.field({
  type: …, authScopes: { authenticated: true },   // 401/UNAUTHORIZED when anonymous
  resolve: (_root, _args, ctx) => …,
}));
// role/permission/superAdmin variants: authScopes: { role: [UserRole.Teacher] } etc. (403/FORBIDDEN)
// Public operations: register in backend/lib/gateway/public-operations.ts PUBLIC_OPERATION_NAMES (exact-match) — NOT expected for notifications.
```

**Cache adapter API — none exists** (A1). Engine (Task 2.6) defines its own injected port with SET-NX-EX semantics: `claim(key, ttlSeconds) → Promise<boolean-ish receipt>`; key scheme `notif:emit:<sha256(userId:type:key)>`, 24h TTL, fail-open on outage. Redis client deps available: `ioredis@^6.0.0` (local), `@upstash/redis` (serverless). Fan-out transport port `NotificationFanoutTransport { publishFanout(userIds, payload) }` is created by Task 2.5 (in-process adapter is the only one legal in tests).

**Logger API** — `import { logger } from "@/backend/lib/logger"`:
```ts
logger.logDomainError("…", { code: "NOTIFICATION_NOT_FOUND", entity: "notifications", entityId: id }); // debug@test / warn@prod
logger.info/warn/error(message, ...args); // NO console.* anywhere
```

**Env-config pattern** (no registry file — A2): read via `resolveEnvConfig(key)` / `getEnv` / `optionalEnv(key, default)` from `backend/lib/env.ts`; cached snapshot via `getEnvironmentConfig()`; invalidate with `resetEnvironmentCache()` (:99) — the `reset*` path Task 1.5 must cover. Precedent: dev3-003 0.2 outcome (registration NOT mandatory without a registry file; document keys in `.env.example`).

**Frontend documents** (Phase 4): new `frontend/graphql/sharedDocuments/notifications/notifications.documents.ts` + subdir barrel + top-level barrel entry; then `bun run generate:gqlSchema && bun codegen`; `id` in every selection set; `TypedDocumentNode<NotificationsFeedQuery, …>` from `@/frontend/graphql/generated/gql/graphql`.

**Frontend page/nav** (Phase 4): `app/(dashboard)/notifications/page.tsx` — server component, layout-level client auth guard (NO `withPageAuth`); bell + `Badge` → `frontend/views/dashboard/DashboardAppBar.tsx` right cluster; sidebar entry → `NAV_ITEMS_BY_ROLE` in `frontend/views/dashboard/navItems.ts` (after each role's dashboard entry, `NotificationsOutlined` icon already imported there :8, :110).

---

## 5. Baseline delta

Read-only audit — no source files touched. `git status` shows only the pre-existing uncommitted plan artifacts (deferred-items.md, tasks.md from Task 0.1) + this outcome file. Quality gates unaffected (no code changed).

**Environment observation for the orchestrator (non-blocking):** HEAD is currently on `main`; the feature branch `feat/dev3-010-real-time-notification-engine-websocket` exists but is NOT checked out — the 0.1 artifacts are uncommitted on `main`'s working tree. Reconcile before Phase 1 (checkout the feature branch keeping the working tree, or commit accordingly).

---

## 6. Overall Verdict: **BLOCKED** — 1 hard blocker, 3 advisories

### ❌ BLOCKER B1 — Pre-existing `backend/db/repo/notifications/` repository does NOT exist

- **Evidence:** §1 Item 3 / §2 above (LS of `backend/db/repo/`, repo-wide globs/greps).
- **Plan claims it exists:** plan.md:349 ("EXISTING namespace, additive only"), specs.md:56 REQ-004(c) ("pre-existing NotificationRepository namespace … EXTEND additively"), tasks.md:195 ("Extend … additive methods only").
- **Practical severity: LOW.** No code depends on the namespace; nothing must be reused or preserved; Task 2.4's own method list already enumerates all seven methods as additions; creation procedure is fully specified by `backend/db/repo/AGENTS.md:59-64`. The corrective action is a **wording/re-scope of Task 2.4** ("extend existing" → "create per AGENTS.md Adding New Repositories") — a plan-owned decision, NOT a DEV1-001-owned structure patch (schema/enums are untouched and green).
- **Per audit protocol:** recorded here for the orchestrator to add to `deferred-items.md` and gate dependent tasks (2.4 wording consumers: plan.md §4.2, specs REQ-004(c), tasks 2.4) until re-scoped. Dependency-guard purpose (REQ-004) is otherwise fully satisfied: all DEV1-001/DEV2-001/DEV2-002/DEV2-003/DEV3-002/DEV3-003 artifacts verified present.

### ⚠️ Advisory A1 — No cache adapter code exists (README-only `backend/services/redis/`; stub `ratelimit.ts`)

Not blocking: plan already injects the adapter at a module seam (plan.md:341, Task 2.6 "cache adapter INJECTED"). Task 2.6 creates the SET-NX-EX port itself; `ioredis`/`@upstash/redis` deps installed. Recorded so 2.5/2.6 do not hunt for a nonexistent seam.

### ⚠️ Advisory A2 — No `env-config-keys.ts` registry file exists

Task 1.5's "register in the env-config registry / registry-inclusion test" wording presumes a registry that does not exist. Effective seam = `backend/lib/env.ts` (`resolveEnvConfig`:136, `resetEnvironmentCache`:99). Precedent: dev3-003 0.2 outcome (registration NOT mandatory; read via env.ts helpers). Orchestrator to pick: follow precedent (typed getters + reset-path invalidation + `.env.example` documentation) or create a registry module in 1.5.

### ⚠️ Advisory A3 — `withPageAuth` does not exist in this tree

Task 4.1 / specs REQ-063 / plan.md:467,506 reference a `withPageAuth`-class server guard; actual convention = client-side `(dashboard)` layout guard (`DashboardLayout.tsx:66-70`). Intent (anonymous → `/login` redirect, all-roles posture) is satisfied by the existing layout — re-word 4.1 or proceed with layout-guard reliance. Documented in `docs/app/with-page-auth.md` (upstream pattern; `backend/lib/auth/withPageAuth.ts` absent here).

### Verdict summary table

| # | Item | Verdict |
|---|---|---|
| 1 | notifications.ts schema + indexes + barrel | ✅ |
| 2 | pgEnum + TS mirror 7-value parity | ✅ |
| 3 | repo/notifications surface | ❌ BLOCKER B1 (does not exist; create per AGENTS.md) |
| 4 | DEV2-001 verifyAccessToken | ✅ |
| 5 | DEV2-002 authenticated authScope | ✅ |
| 6 | DEV2-003 session-notification contract | ✅ |
| 7 | DEV3-002 masking boundary | ✅ |
| 8 | DEV3-003 gateway posture | ✅ |
| 9 | entity-setup.ts fixtures | ✅ (no teacher creator — role override + applicant) |
| 10 | A.4 / INV-P2/P3 / DATABASE_MIGRATIONS.md | ✅ |
| 11 | docs/IDEMPOTENCY.md | ✅ |
| 12 | cache adapter / env-config registry | ⚠️ A1 + A2 (none exist; plan creates its own seams) |
| 13 | logger + logDomainError | ✅ |
| 14 | test helpers (5/5) | ✅ |
| 15 | sharedDocuments conventions | ✅ |
| 16 | nav/app-bar config | ✅ |
| 17 | (dashboard) route group | ✅ exists / ⚠️ A3 withPageAuth absent |
| 18 | sub-loop.ts + run-test.ts | ✅ |

**BLOCKED pending orchestrator action on B1** (deferred-items.md entry + Task 2.4 re-scope). A1–A3 are informational and do not gate implementation.
