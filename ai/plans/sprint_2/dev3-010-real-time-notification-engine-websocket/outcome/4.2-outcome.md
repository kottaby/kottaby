# Task 4.2 Outcome — `useNotificationRealtime` Client Hook (+ BF/BS Browser Verification)

**Task (4.2a):** Implement `frontend/hooks/use-notification-realtime.ts` — ONE cookie-handshake WebSocket per mounted authenticated shell, 2.8 close-code contract (abort 4401/4009 / backoff 1s→30s ±20% jitter otherwise), REQ-025 catch-up refetch on RE-connect, Apollo cache merge (badge bump + list prepend, dedupe by id, zero refetch spam), localized arrival toasts through the GraphQLErrorSurfaceHost snackbar conventions, silent degradation to the polling floor — REQ-021/025/063(d)/064/067. Committed in `765fbdb` (part 1).

**Task (4.2b — this document):** Agent-browser FUNCTIONAL self-loop (BF) + VISUAL/STYLING self-loop (BS) over the live topology (dev server + `bun run ws` sidecar + Redis), the BS defect fix, regression gates, checkbox flips, and evidence consolidation.

**Verdict:** GREEN — all five BF flows verified live and green (ONE-connection, emit→toast, cache merge without refetch, sidecar-kill silent degradation, restart reconnect + catch-up recovery of an outage-missed notification); the 6-combo BS matrix passed with ONE defect found and fixed (BS-D1: realtime/error toast anchor occlusion → sx-only anchor offset, re-verified); zero regression anywhere (tsgo 0 errors, biome 564 files exit 0, component tier 55/0, DB 56/0/222, notifications 83/0/684, dev :3000 healthy).

---

## 1. What Was Built (4.2a — implementation summary)

- **`frontend/hooks/use-notification-realtime.ts` (NEW)** — the realtime hook:
  - ONE `WebSocket` per mounted authenticated shell; handshake rides the httpOnly `access_token` cookie only (zero token handling in JS — no query params, no storage).
  - WS URL resolution: `NEXT_PUBLIC_NOTIFICATION_WS_URL` (Next-inlined, `.env.example`-documented production `wss://` override; D3 owns provisioning) wins when set; otherwise derived `<ws|wss>://<window.location.hostname>:3001` (same host as the app origin so the cookie rides; ports never participate in cookie scoping); SSR-guard fallback `ws://127.0.0.1:3001`.
  - Close-code contract (2.8 BINDING vocabulary): `4401`/`4009` ABORT retrying; `4429`/`1013`/`1001`/abnormal back off `1s→2s→4s…cap 30s` with ±20% jitter (`getNotificationReconnectDelay` exported pure function); unmount → deterministic `close(1000)` + full listener detach; remount constructs its own socket (retired socket provably mute).
  - Message handler: JSON parse → REQ-021 envelope shape guard (`v:1`/`kind:"notification"`/typed `data`) → bounded 200-id recent-set dedupe → backend-snake_case `type` reconciled through `PAYLOAD_TYPE_TO_CACHE_TYPE` to the codegen `NotificationType` (wire names) → `id` coerced to STRING → `Notification:{id}` normalized row write + prepend to every matching page-1 `myNotifications` variant (read-only and type-filtered variants excluded by `notificationMatchesListVariant`) → held-id arrivals are complete no-ops (no count bump, no toast) → `myUnreadNotificationCount` cache modify +1 → localized toast via `PAYLOAD_TYPE_TO_LABEL` enum-handle accessors + `labelsRef` (locale changes mid-connection honored).
  - Catch-up (REQ-025): on RE-connect only (first connect never refetches) — `client.query` network-only × (`myNotifications` page 1 + `myUnreadNotificationCount`).
  - Silent degradation (REQ-064): no toasts/banners on outage; at most one `logger.warn` per disconnected episode (+ one per policy abort).
  - Local refs/state ONLY; socket never enters stores/persist; connection state never escapes the hook (only the transient toast queue does).
- **`frontend/components/ui/NotificationRealtimeToastHost.tsx` (NEW)** — shell-level toast surface: fixed bottom-center flex column (static-position Snackbars inside — concurrent toasts stack in flow, never on top of each other), filled `Alert severity="info"` with `NotificationsOutlined` icon + translated-close `IconButton`, theme-callback `sx`, `prefers-reduced-motion`-gated entrance animation. Mounted EXACTLY ONCE in `DashboardLayout`'s authenticated branch (`app/(dashboard)/layout.tsx` tree) ⇒ one socket per tab (REQ-067); sign-out unmounts it.
- **`app/(dashboard)/layout.tsx` tree mount** — `NotificationRealtimeToastHost` after the auth early-return in `frontend/views/dashboard/DashboardLayout.tsx`.
- **`test/ui/components/notification-realtime.test.tsx` (NEW)** — 18 tests / 3 describes on a recording `FakeWebSocket` global double + real `ApolloClient` on `MockLink` + production `createApolloCache()` + `renderWithWrapper` (host consumes the hook): arrival→merge (zero refetch, zero sent frames), duplicate-id replay no-op, cache-held no-op, read-only + type-filtered variant gating, malformed/foreign/unknown-type drops, ar RTL toast, translated dismiss, 4-deep toast cap, reconnect-1001 catch-up convergence, 4401+4009 abort, unmount ⇒ one `close(1000)` + listeners detached, remount ⇒ fresh socket + retired socket mute, env URL override, pure backoff curve/jitter/clamp pins.
- **4.2b (this session) modification:** `NotificationRealtimeToastHost.tsx` — BS-D1 fix (§4): the stack's bottom anchor raised from `{xs:16, sm:24}` to `{xs:96, sm:104}` (sx-token-only change; hook untouched) so realtime arrivals never occlude concurrent `GraphQLErrorSurfaceHost` error toasts (both stacks are bottom-center fixed columns at `zIndex.snackbar`).

## 2. BINDING for 4.3 / 4.4 — browser-loop runbook + consumption contract

**⚠ RUNBOOK CORRECTION (supersedes the 4.2a worklog note — empirically re-derived this session):**
- **Browse at `http://localhost:3000`** (NOT `http://127.0.0.1:3000`). Next 16's dev-origin check (`allowedDevOrigins: ["*.space-z.ai", "*.space-zai"]` in `next.config.ts`) returns **403 for Turbopack's `crossorigin`-attributed client chunks** when the browser sends `Origin: http://127.0.0.1:3000` (curl-proven: no-Origin → 200, `Origin: …127.0.0.1:3000` → 403, `Origin: …localhost:3000` → 200, `Origin: https://x.space-z.ai` → 200). At `127.0.0.1` React hydration NEVER completes (login form submits as a native GET — no React handlers); at `localhost` everything hydrates.
- **Run the sidecar as `WS_HOST=localhost bun run ws`** (Bun binds `[::1]:3001`; Chrome reaches `ws://localhost:3001`; the `localhost` cookie host matches the app origin; the sidecar's DEFAULT origin allowlist already includes `http://localhost:3000` — no env change needed). With the default `WS_HOST=127.0.0.1` the browser's `ws://localhost:3001` may fail (::1) → silent 4401/abnormal posture. **NEVER mix hosts.**
- Emit trigger was `bun run scripts/tmp-bf-emit.ts ["title"] [body]` — ephemeral harness, **deleted after the BF loop** (not committed). 4.3's page work needs no harness (the feed is query-driven); 4.4's badge loop should re-create an equivalent throwaway emitter if it wants live pushes.
- Apollo cache inspection: `window.__APOLLO_CLIENT__` (Apollo Client devtools hook) is available in dev — `__APOLLO_CLIENT__.cache.extract().ROOT_QUERY` reads `myUnreadNotificationCount` and `myNotifications({"filter":null})`; console-run queries through `__APOLLO_CLIENT__.query` seed the cache fields the realtime merge bumps.

**Consumption contract (unchanged from 4.1/4.2a):** documents from `@/frontend/graphql/sharedDocuments` only; codegen `NotificationType` only (wire names); string ids; count query with NO variables; marking read is NOT a realtime event; hooks from `@apollo/client/react`, `useQuery` stateful only. **4.3/4.4 do NOT re-mount `useNotificationRealtime`** — the `DashboardLayout`-mounted host already owns the tab's socket; the feed page and badge consume the CACHE the hook maintains.

**Anchor contract (post BS-D1):** the realtime toast stack now anchors at `bottom: {xs:96, sm:104}` (one toast height above the error host's `{xs:16, sm:24}`). 4.4's badge/toast surfaces must NOT lower it back; any new shell-level snackbar surface must place itself clear of BOTH stacks.

## 3. BF — Agent-Browser Functional Self-Loop (4.2.BF)

**Topology:** dev server `:3000` (untouched) + `WS_HOST=localhost bun run ws` sidecar `:3001` (Redis transport, Redis `:6379` live) + agent-browser (named session, headless Chromium) at `http://localhost:3000`, admin login `admin@app.local` / `adminpassword123`, landing `/admin/dashboard`.

**Instrumentation (honest method):** a `document_start` init script (`agent-browser open --init-script`) replaces `window.WebSocket` with a counting subclass — records every construction URL, open/close counts, live-instance count, close codes. Cache reads via `window.__APOLLO_CLIENT__` (dev devtools hook); refetch-freedom via agent-browser's network request log (cleared before each probe); sidecar registry truth via `/tmp/ws-sidecar.log`.

| # | Flow | Verdict | Evidence |
|---|---|---|---|
| 1 | **Exactly ONE WS connection per authenticated tab** | ✅ GREEN | Probe: 2 notification-socket constructions in dev = React **StrictMode** double-mount (first socket deterministically torn down: listeners detached + close) + 1 HMR socket (dev-only); settles to **exactly 1 live notification socket / 1 ESTAB TCP pair** (Chrome↔sidecar `::1:3001`); sidecar registry: 1 registered connection for `userId:1` (admin), cookie handshake (URL `ws://localhost:3001`, no query params, no token in JS) |
| 2 | **Emit → toast appears once** | ✅ GREEN | `bun run scripts/tmp-bf-emit.ts "BF toast check" …` → exactly 1 `.MuiAlert-root` with `"New notification — System Announcement: BF toast check"` (EN) / `"إشعار جديد — إعلان النظام: …"` (AR) — the `realtimeToast(typeLabel, title)` template, type label from the enum-handle accessor |
| 3 | **Cache merge WITHOUT page refetch** | ✅ GREEN | Baseline seeded via console-run queries (count 20 / list head `3954`); after emit: `myUnreadNotificationCount` 20→21→22→23 across three emits, `myNotifications({"filter":null})` head prepended `Notification:4369` (the emitted id); network log cleared pre-emit captured **ZERO `/api/graphql` requests** during the arrival — no refetch spam |
| 4 | **Sidecar kill → silent degradation** | ✅ GREEN | `pkill` sidecar → client observed close `1001` (the 2.8 graceful shutdown close-all code); hook backed off and retried (4 reconnect attempts in ~10s — constructions 3→7, codes 1006×3 on refused connects); **ZERO error toasts, zero page errors, page fully interactive**; the REQ-064 `logger.warn` fires into the frontend logger whose browser sink is a documented no-op (`frontend/lib/logger.ts` — `process.stderr` undefined in browser) ⇒ zero console noise, compliant |
| 5 | **Restart → reconnect + catch-up refetch** | ✅ GREEN | Emitted id `4372` DURING the outage (persisted, not pushed); sidecar restarted → hook reconnected (1 new construction, 1 new open, 1 live socket) → **exactly 2 catch-up `/api/graphql` POSTs** (`myNotifications` page-1 + `myUnreadNotificationCount`, network-only) → cache count 23→**24** (server truth incl. the missed row), list head = `Notification:4372` — **the outage-missed notification was recovered at the page-1 head** (REQ-025 divergence-bounding proven end-to-end); no toast for the recovered row (catch-up is not a push — correct) |

**BF iteration log (honest):** ONE blocking environment iteration, ZERO functional defects in the hook/host:
- **Iteration 1 (environment, failed → resolved):** the 4.2a runbook's `http://127.0.0.1:3000` produced a hydration-dead page (Next 16 dev-origin 403s on crossorigin chunks — §2). Diagnosed by curl Origin matrix + missing React fiber on the login form + non-toggling client controls. Resolution: `http://localhost:3000` + `WS_HOST=localhost` sidecar. All five flows then passed first-try with the shipped 4.2a code (no hook patches needed).
- Overlap-defect remediation is logged under BS (§4), not BF.

## 4. BS — Agent-Browser Visual & Styling Self-Loop (4.2.BS)

**Method (honest):** for each of the 6 combos — set viewport + `NEXT_LOCALE` cookie → reload the authenticated dashboard → trigger a live emit → **programmatic DOM geometry audit** (`getBoundingClientRect` + `getComputedStyle` + `textContent` on the live toast, ~1s post-arrival) → screenshot ~1s later (inside the 6s auto-hide window). Geometry audit is the primary inspection (bounding boxes are exact); the PNGs are the committed evidence (`outcome/screenshots-4.2/`).

**Matrix (post-fix, final):**

| Combo | Viewport | Locale | centerOffset | bottomGap | within viewport | Direction / mirroring | Verdict |
|---|---|---|---|---|---|---|---|
| 1 | 1440×900 | en | **0 px** | 104 | ✅ | LTR — icon inline-start, close inline-end, `textAlign:start` | ✅ GREEN |
| 2 | 768×1024 | en | **0 px** | 104 | ✅ | LTR — same | ✅ GREEN |
| 3 | 375×812 | en | **0 px** | 96 | ✅ | LTR — same; width 343 = `calc(100vw − 32px)` | ✅ GREEN |
| 4 | 1440×900 | ar | **0 px** | 104 | ✅ | RTL — `dir=rtl`, icon inline-END, close inline-START (mirrored), Arabic type label `إعلان النظام` + Arabic template | ✅ GREEN |
| 5 | 768×1024 | ar | **0 px** | 104 | ✅ | RTL — mirrored | ✅ GREEN |
| 6 | 375×812 | ar | **0 px** | 96 | ✅ | RTL — mirrored; width 343 cap respected | ✅ GREEN |

- **Positioning:** perfectly centered (offset 0) at every combo; bottom-anchored (96 mobile / 104 tablet+desktop — raised anchor, see BS-D1); no clipping, no off-viewport rendering.
- **Palette compliance:** toast background `rgb(37,99,235)` = `theme.palette.info.main` via the MUI filled `Alert severity="info"` slot; text `theme.palette.common.white`; radius/shadow/zIndex all theme tokens; source-level: zero hex/rgb/color-name literals (4.2a grep, re-confirmed — the only color-bearing string is the `color-mix` overlay built FROM `theme.palette.common.white`).
- **RTL/LTR:** logical properties (`insetInlineStart/End`) mirror correctly; icon↔close swap verified by bounding-box side analysis in both directions; `document.documentElement.dir` = `rtl` for ar.
- **Arabic pluralization of unread count:** NOT visible yet — the badge UI is the 4.4 deliverable (§7 carry-forward); the pluralization function itself is compile- and test-pinned (`shared/locale/notifications-namespace.parity.test.ts` — 0/1/2/3–10/11+ branch pins).

**BS defect log:**

- **BS-D1 (FOUND + FIXED + RE-VERIFIED):** the realtime toast stack and the `GraphQLErrorSurfaceHost` error-toast stack share an identical fixed bottom-center anchor (`bottom:{xs:16,sm:24}`) and `zIndex.snackbar` — a concurrent error toast + realtime arrival render **exactly on top of each other** (live probe: error toast y=826/h=51, realtime toast y=826/h=50 — 100% occlusion; evidence `overlap-defect-desktop-ar.png`). **Fix (sx-token only, hook untouched):** realtime stack anchor raised to `bottom:{xs:96,sm:104}` (one toast height ≈72px + gap above the error host's anchor) with an explanatory comment + updated component docblock. **Post-fix probe:** realtime y=746, error y=826 → **30 px separation, both fully visible** (evidence `overlap-fixed-desktop-ar.png`). Re-audited the full 6-combo matrix on the fixed anchor (matrix above). Unit suite unaffected (55/0 — the suite pins no anchor values).
- **BS-D2..n:** none — no clipping, no centering drift, no RTL defects, no palette violations detected across all six combos and the two overlap probes.
- **Stale-artifact cleanup:** six pre-existing PNGs in `screenshots-4.2/` left by the interrupted prior 4.2b run (captured 13:20–13:24 against the hydration-broken page, no toasts) were deleted; the committed evidence set is the 8 files from this session only.

**Screenshots committed (`outcome/screenshots-4.2/`):** `toast-desktop-en.png`, `toast-tablet-en.png`, `toast-mobile-en.png`, `toast-desktop-ar.png`, `toast-tablet-ar.png`, `toast-mobile-ar.png`, `overlap-defect-desktop-ar.png` (pre-fix occlusion), `overlap-fixed-desktop-ar.png` (post-fix coexistence).

## 5. Verification Commands & Exit Codes (4.2b gates)

| # | Command | Result | Exit |
|---|---|---|---|
| 1 | `bun run scripts/health/sub-loop.ts frontend/components/ui/NotificationRealtimeToastHost.tsx --lifecycle duplicates` (re-run after the BS-D1 sx edit) | ✅ all five checks passed | **0** |
| 2 | `bun tsgo` (full project) | ✅ EXIT 0 — ZERO errors | **0** |
| 3 | `bun biome:check` | ✅ Checked 564 files, no fixes applied (baseline 564 held — screenshots are not lint targets) | **0** |
| 4 | `bun run test:ui:components` | ✅ **55 pass / 0 fail** across 6 files (37 pre-existing + 18 hook tests) — post-sx-edit | **0** |
| 5 | `bun run test/scripts/run-test.ts backend/db/test` | ✅ **56 pass / 0 fail / 222 expects** (baseline holds) | **0** |
| 6 | `bun run test/scripts/run-test.ts backend/services/notifications` | ✅ **83 pass / 0 fail / 684 expects** (baseline holds) | **0** |
| 7 | `curl -s http://localhost:3000/api/health` | ✅ HTTP 200 (`status: ok`) — dev server untouched throughout (no restart, no kill) | n/a |
| 8 | Sidecar lifecycle | ✅ started (`WS_HOST=localhost`, PID recorded), verified listening `[::1]:3001` + Redis transport; KILLED at session end (`pkill -f start-notification-ws`) | n/a |
| 9 | `git status` change-set audit | ✅ exactly: modified `NotificationRealtimeToastHost.tsx` (+anchor fix), new `outcome/screenshots-4.2/` (8 PNGs), new `outcome/4.2-outcome.md`, `tasks.md` checkbox flips, `worklog.md` append; `scripts/tmp-bf-emit.ts` DELETED before commit | n/a |

**4.2a gates (from the worklog, commit `765fbdb`):** unit suite 18/0 ×3 deterministic (expect-count variance 78/81/83 — conditional `waitFor` polling); component tier 55/0 ×2; tsgo EXIT 0; biome 564 files exit 0; DB 56/0/222; notifications 83/0/684; health 200. **Consolidated test matrix for 4.2:** unit tier 18/0 (hook) + component tier 55/0 (incl. the 18) + BF live evidence (§3) + BS evidence (§4).

## 6. SR — Semantic Review (~20-item checklist, 4.2a groundwork + 4.2b visual findings)

1. ✅ Zero `console.*` in the hook + host (grep).
2. ✅ Zero hex/rgb/color-name literals in source (grep); live-computed toast colors resolve to `theme.palette.info.main` / `common.white` via the MUI severity slot.
3. ✅ All `sx` via theme callbacks (`theme => ({…})`); spacing/radius/shadow/zIndex/animation tokens all theme-sourced.
4. ✅ Icons `*Outlined` exclusively: `NotificationsOutlined` (toast) + `Close` (dismiss, exact GraphQLErrorSurfaceHost convention).
5. ✅ Translation access is enum-handle property access only: `useAppTranslation(Notifications)` + `PAYLOAD_TYPE_TO_LABEL` accessors; `useAppTranslation(Common)` for the close aria-label; never call-by-key.
6. ✅ Non-serializable socket confined to the hook's mount-scoped effect closure — zero store/persist references (grep).
7. ✅ Connection state never escapes the hook (only the transient toast queue renders).
8. ✅ Documents imported from `@/frontend/graphql/sharedDocuments` barrel only.
9. ✅ Codegen `NotificationType` (wire names) is the only enum touched; backend snake_case payload types reconciled via `PAYLOAD_TYPE_TO_CACHE_TYPE` (no unsafe enum comparison).
10. ✅ `id` coerced to STRING before any cache write; `Notification:{id}` normalization test-pinned.
11. ✅ Count query consumed with NO variables object.
12. ✅ Catch-up fires on RE-connect only — live-verified: zero catch-up POSTs after the initial login connect; exactly 2 after the post-outage reconnect.
13. ✅ Close-code contract exact — 1001/abnormal backoff path exercised live (kill + refused reconnects); 4401/4009 abort + curve/jitter unit-pinned.
14. ✅ Client sends NOTHING — no `ws.send` anywhere in the hook (grep) + "zero sent frames" unit-pinned on the recording FakeWebSocket.
15. ✅ Bounded 200-id recent-set dedupe + cache-held complete no-op (unit-pinned).
16. ✅ Cookie handshake only — live probe: socket URL `ws://localhost:3001`, no query string, no token anywhere in JS.
17. ✅ ONE socket per tab (REQ-067) — live-verified at the TCP level (§3 flow 1).
18. ✅ Unmount ⇒ deterministic `close(1000)` + all four listeners detached (unit-pinned); dev StrictMode teardown observed live (first socket closed, exactly one survivor).
19. ✅ Silent degradation (REQ-064) — live-verified zero toasts/errors during outage; warn path documented no-op in browser.
20. ✅ Toast queue cap 3 with oldest-first eviction (unit-pinned); live stacking observed (two concurrent toasts stack in flow — mobile-AR probe).
21. ✅ MUI v9 discipline: `sx` only, logical inset properties mirror under RTL (live-verified bounding boxes), translated aria-labels, `prefers-reduced-motion`-gated animation.
22. ✅ The 4.2b sx edit touched ONLY the host's anchor tokens + comments — hook logic byte-untouched (diff audit).

## 7. IV — Instruction Verification

| Instruction | Verdict | Evidence |
|---|---|---|
| tasks.md 4.2 bullet 1 — hook with ONE WS per mounted shell, cookie handshake, NO token in JS | ✅ | §1; live §3 flow 1 |
| tasks.md 4.2 bullet 2 — backoff 1s→30s + jitter; ABORT on 4401/4009; deterministic `close(1000)` on unmount; NO toasts on clean close | ✅ | unit-pinned (curve/abort/unmount tests); live backoff observed §3 flow 4; no toast fired on the 1001 close |
| tasks.md 4.2 bullet 3 — shape-guard → dedupe by `data.id` → cache merge (badge + list, no refetch spam) → localized toast via `Translation.Notifications` | ✅ | §3 flows 2–3; enum-handle accessors §6 #5 |
| tasks.md 4.2 bullet 4 — post-reconnect CATCH-UP refetch (REQ-025); silent degradation w/ 120s polling floor (REQ-064) | ✅ | §3 flows 4–5 |
| tasks.md 4.2 bullet 5 — local React state/refs only; connection state never escapes | ✅ | §6 #6–#7 |
| tasks.md 4.2.QL — sub-loop `--lifecycle duplicates` | ✅ | 4.2a: 5 files exit 0 (zero fix iterations); 4.2b re-run on the edited host: exit 0 |
| tasks.md 4.2.TE — mocked-WebSocket unit suite (merge/dedupe/refetch/4401/unmount/remount) | ✅ | 18/0 ×3 deterministic (4.2a); tier 55/0 re-run post-edit |
| tasks.md 4.2.BF — the five live flows + iterative self-loop | ✅ | §3 — all green; 1 environment iteration, 0 functional defects |
| tasks.md 4.2.BS — 6-combo capture, positioning/palette/RTL/pluralization inspection, iterative patch loop | ✅ | §4 — matrix green; BS-D1 found→fixed→re-verified (sx-only patch + re-capture) |
| tasks.md 4.2.SR — no console.*, no hardcoded strings/colors, enum-handle access, `*Outlined` icons, socket out of stores | ✅ | §6 (22 items) |
| tasks.md 4.2.IV — validate against `frontend/AGENTS.md`, REQ-025/064/067 | ✅ | §7 rows + §6; frontend AGENTS rules honored (logger not console, sx/theme-palette, Outlined icons, hooks from `@apollo/client/react`) |
| REQ-025 — reconnect catch-up bounds divergence | ✅ | live §3 flow 5 (outage-missed row recovered at page-1 head) |
| REQ-064 — silent degradation, no alarm UI | ✅ | live §3 flow 4 |
| REQ-067 — at most one connection per tab | ✅ | live §3 flow 1 (TCP-level) |
| Deferred-items ledger | ✅ untouched | no new ruling needed |

## 8. Deviations

- **D-1 (runbook host correction — documented in §2):** 4.2a's BINDING said browse at `http://127.0.0.1:3000`; that origin is blocked by Next 16's dev-origin check for crossorigin chunks in this sandbox (curl-proven 403 matrix), producing a hydration-dead page. The BF loop therefore ran at `http://localhost:3000` with `WS_HOST=localhost` on the sidecar. This is an environment-provisioning fact, not a code change; recorded so 4.3/4.4 loops don't repeat the 90-minute diagnosis.
- **D-2 (BS inspection method):** screenshots were validated via a programmatic DOM geometry audit (bounding boxes + computed styles) rather than pixel-eyeballing — the executing agent has no image-vision channel; the audit is strictly more precise for position/palette/direction claims and the PNGs remain committed as evidence.
- **D-3 (BS-D1 anchor offset):** the plan's §5.5 wording ("toast renders anchored bottom-center through existing snackbar-host conventions") was refined by the 4.2a carry-forward's sanctioned mitigation: the realtime stack keeps the bottom-center anchor but raised 80px above the error host's to prevent cross-host occlusion (live-proven defect). sx-token-only change; hook untouched.

## 9. Carry-Forward Knowledge

- **For 4.3 (feed page):** consume the cache the hook maintains; do NOT re-mount the hook. The `myNotifications({"filter":null})` variant already receives live prepends; filtered variants (unread-only/type-filtered) are maintained per the variant-matching rules — the feed page's own queries create their own variants, which the realtime merge will serve correctly (page-1 windows only; deeper offsets converge via their own refetch).
- **For 4.4 (badge):** the app-bar badge reads `myUnreadNotificationCount` via `useQuery` — the hook bumps the cached field on every fresh arrival, so the badge updates without refetch **once a count query has populated the field**; if no count query has run yet, the first read fetches fresh (documented hook behavior). Badge aria uses the namespace `unreadCount(count)` pluralization function (AR boundaries 0/1/2/3–10/11+ are parity-test-pinned). Do NOT lower the realtime toast anchor (§2 anchor contract).
- **Known residual (accepted):** with ≥2 stacked GraphQLErrorSurfaceHost toasts AND a realtime arrival simultaneously, the raised realtime anchor can still partially collide with the taller error stack — static sx cannot track dynamic stack heights; the common single-error case is fully solved. Revisit only if 4.4 introduces a unified toast host.
- **Dev-mode note:** the probe counts 2 live WebSockets per tab in dev (1 HMR + 1 notification); production builds have no HMR socket. React StrictMode double-mounts the hook in dev (first socket torn down deterministically) — by design, unit-pinned.
- **Counters (for the next gate):** biome 564 (unchanged); component tier 55/6 files (unchanged); all other baselines unchanged (DB 56/0/222; notifications 83/0/684; documents 12/0/59; WS 23/0/77; workflows 33/0/238); plan-owned totals 301 tests.
- **Env facts:** `WS_ALLOWED_ORIGINS` and `NEXT_PUBLIC_NOTIFICATION_WS_URL` remain UNSET in `.env` (defaults in effect — correct for this topology); Redis `:6379` live (user-space build at `/tmp/redis-root`, restart command in the 0-redis-provisioning worklog entry); sidecar default `WS_HOST` is `127.0.0.1` — **use `WS_HOST=localhost` for browser loops** (§2).
