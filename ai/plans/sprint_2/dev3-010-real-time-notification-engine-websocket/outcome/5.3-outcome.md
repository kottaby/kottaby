# Task 5.3 Outcome — Chaos / Concurrency Suite

## 1. Summary

The chaos tier is live across both server and client: 25-way concurrent mark storms, parallel emit batches with ordering invariants, hostile-input fuzz (ids/types/pagination), and unicode/RTL/control-char literal-text storage + safe rendering, plus reconnect-flicker storms proving exactly-one live connection and zero duplicated toasts. **Zero production bugs found** — the engine, transports, and realtime hook held every invariant under chaos.

## 2. Files

- **NEW** `backend/services/notifications/notification-engine.chaos.test.ts` (737 lines, 7 tests / 494 expects, DB tier, `runInRollback`, `chaos-` fixture prefix)
- **MODIFIED** `test/ui/components/notification-realtime.test.tsx` (+151 lines: `createFlickerClient` helper + chaos describe; existing 18 tests untouched)

## 3. Coverage Map (chaos cells → tests)

| Cell | Test | Result |
|---|---|---|
| 25-way concurrent mark-one/mark-all storms (REQ-044) | server #1 — all-fulfilled wave (12 mark-one + 13 sweeps) + 3 mid-storm observer reads → coherent pages at every serialization point, final all-read/12 rows/badge 0/titles byte-preserved | PASS ×2 |
| Parallel emit batches → full row-set | server #2 — 4 batches + 3 singles concurrent on one tx → 7/4/4 per user, zero leakage, per-batch `createdAt` byte-equality (REQ-047), zero publishes (REQ-042) | PASS ×2 |
| Ordering invariants | server #3 — 5 concurrent batches → newest-first byte-match oracle, pairwise `createdAt DESC` + `id DESC` tiebreak, cross-recipient batch-equal timestamps | PASS ×2 |
| Fuzz: hostile ids | server #4 — NaN/0/-1/-0/1.5/2^53/"7"/""/±Infinity vs markRead/list/count/markAllRead + caller ids → translated `VALIDATION`, zero residue | PASS ×2 |
| Fuzz: hostile types | server #5 — 9 wrong-enum shapes (case variants, SQL-injection string, numbers, booleans, arrays, objects) → `VALIDATION`, zero rows/publishes | PASS ×2 |
| Fuzz: hostile pagination | server #6 — limits 51/0/-1/NaN/1.5/2^53/"50"/∞, offsets −5/−1/2.5/NaN/"0"/∞/2^53 → `VALIDATION` + positive control (limit 50 anchor) | PASS ×2 |
| Unicode/RTL/control-char literal-text storage | server #7 — RTL Arabic, bidi overrides/marks, zero-width, control chars, `<script>`/SQL-shaped copy, astral emoji, exact-255 title → byte-exact round-trip via list + direct-select oracle | PASS ×2 |
| Reconnect flicker storm | client #8 — close(1001)↔open ×4 + 3-frame replay burst (13 deliveries of one id) → exactly-one live connection (1 OPEN, 4 CLOSED, 5 built), zero duplicated toasts, cache converges | PASS ×2 |
| Hostile-text safe rendering | client #9 — bidi/RTL/zero-width/control-char/astral/injection titles → literal-text toast (byte-exact), zero `<script>` materialized, cache merge + badge bumps | PASS ×2 |

## 4. Test Counts (determinism, canonical runner)

- Server chaos suite: **RUN 1 = RUN 2 = 7 pass / 0 fail / 494 expects**
- Component tier (`bun run test:ui:components`): **RUN 1 = RUN 2 = 100 pass / 0 fail** (98 baseline + 2 new)
- Full notifications dir: **94/0/1196 across 9 files** (all green incl. new)
- DB suite: **56/0/222** — exact baseline, zero drift

## 5. Quality Loop

- Server file: 3 iterations (TS2769 narrowing → sonarjs alphabetical-sort/trivial-assertions → exit 0)
- Client file: 2 iterations (no-await-in-loop → index-recursive restructure → exit 0)

## 6. Gates

- `bun tsgo`: **exit 0, 0 errors**
- `bun biome:check`: **exit 0** (579 files = 578 + 1 new)
- Production bugs: **none** — two design observations documented in test comments: (a) Postgres text cannot store U+0000 (fixtures exclude NUL by design — storage impossibility, not an engine defect); (b) concurrent caller-tx emits interleave same-named savepoints — verified safe (happy path only RELEASEs; PG duplicate-savepoint semantics release most-recent; work never destroyed).

## 7. BINDING Notes for 5.4

- Coverage targets: the chaos suite adds 7 server tests + 2 client tests to the coverage evidence set.
- `bun test --coverage` evidence collection (5.4) must include: engine service, emit-validation, emit-idempotency, transports (incl. fanout-degradation), WS server, realtime hook, feed views, badge.
- Counters after this task: biome 579 files; notifications dir 94 tests / 9 files; component tier 100 tests / 9 files.

## 8. Carry-Forwards / Deviations

- **D-1 (session structure)**: executor session (suites + gates) + orchestrator-completed bookkeeping (same surgical-dispatch pattern as 5.2 — environment lesson recorded in worklog 5.2 entry).
- Client flicker test wall-clock ~4.4s inside the 6s toast window — within budget, no flakes observed across double-runs.
