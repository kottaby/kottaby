# Task 3.1 Outcome — Enum Registration + Pothos Objects

**Task:** Register `NotificationType` enum + `Notification` / `NotificationListPage` Pothos objects in `backend/graphql/pothos/` — REQ-060, REQ-061, REQ-014.
**Verdict:** GREEN — objects live, enum registered once, schema-surface suite 16/0/82 ×2 deterministic, zero baseline regression anywhere (tsgo 0 errors, biome 549 files exit 0, all layer suites at baseline counts, dev :3000 healthy, live introspection shows the exact surface).

## 1. What Was Built

- **`NotificationTypePothosEnum`** registered ONCE in `backend/graphql/pothos/shared/enum.pothos.ts` via the mandated enum-object form `gqlSchemaBuilder.enumType(NotificationType, { name: "NotificationType" })` — value-imported from the canonical TS enum (`backend/enum/notifications/notification-type.enum.ts`, the 1.2 byte-parity mirror of the `notification_type` pgEnum). No literal arrays, no re-registration anywhere (grep evidence §7).
- **`NotificationPothosObject`** — the single canonical `Notification` object (`backend/graphql/pothos/notifications/notification.pothos.ts`): `objectRef<NotificationReturnType>("Notification").implement({...})`, backed exclusively by the 1.1 canonical type from `@/backend/types` (NO local type defs). `id` exposed FIRST as `ID!` over the integer pk; exactly the eight REQ-060 inbox fields; `type` maps the pgEnum string union onto the registered enum through the fail-closed `isNotificationType` guard (`User.role`→`toUserRole` mapping precedent — an unexpected value surfaces as a resolver error, never a cast); `isRead` resolves `null → false` over the nullable DB column (`User.isDeleted` governance-field precedent) to present `Boolean!`; `createdAt` serialized as non-nullable ISO-8601 UTC `String!` via the repo's established no-DateTime-scalar convention (`ApplicantProfile` / `HealthCheck` precedents — deviation D-3).
- **`NotificationListPagePothosObject`** — the pagination wrapper (`backend/graphql/pothos/notifications/notification-list-page.pothos.ts`), sanctioned by the Single Canonical Object Type Pattern's wrapper exception ("Wrapper types for collections or complex responses"): `objectRef<NotificationListPageReturnType>("NotificationListPage")` exposing exactly `items: [Notification!]!` (through the single canonical object), `totalCount: Int!`, `hasMore: Boolean!`. NO `id` BY DESIGN — embedded value type; the normalizable entities are the `Notification` rows inside `items`.
- **Barrel wiring (gateway Rule 8 recipe)**: NEW top-level `backend/graphql/pothos/index.ts` domain barrel (`export * from "./notifications"` — root-AGENTS barrel rules: `./` path, one `/`, `export *`) + NEW `backend/graphql/pothos/notifications/index.ts` leaf barrel; `gqlSchema.ts` side-effect-imports the top-level barrel as wiring step 2 so the objects register on `gqlSchemaBuilder` BEFORE `toSchema()` — notifications objects land ahead of their resolvers (3.2/3.3), unlike `users`/`teachers`/`auth` whose objects register transitively through resolver imports (that convention is untouched and documented in the barrel's JSDoc).
- **SDL artifact**: `frontend/graphql/generated/schema.graphql` regenerated via `bun run generate:gqlSchema` — diff contains ONLY the three notification additions (`type Notification`, `type NotificationListPage`, `enum NotificationType`); byte-identity to the built schema proven by the suite's Codegen-sync assertion (D-5 records the deliberate codegen-types deferral to 3.4).
- **Apollo embedded-type policy compliance**: `NotificationListPage` (no `id`) added to `typePolicies` with `keyFields: false` in `frontend/providers/apollo/apolloCache.ts` + listed in `frontend/graphql/AGENTS.md`'s "Current embedded types" — the policy is triggered the moment an id-less type is added (`HealthCheck` day-one precedent; deviation D-6).
- **TE**: schema-build smoke assertions live in the EXISTING `backend/graphql/test/schema-surface.test.ts` (the repo's schema-build test — placement rule honored) as a new `describe("Notification surface — enum + canonical objects")` block: enum carries EXACTLY the 7 canonical values (keys-on-wire + snake_case runtime values + single-source agreement with the canonical TS enum itself), `Notification` exposes `id` + EXACTLY the eight inbox fields with exact GraphQL types and structurally NO `userId`, `id`-FIRST pinned at the SOURCE level (lexical scan — the built schema is lexicographically sorted so field order carries no semantics), `NotificationListPage` exposes exactly items/totalCount/hasMore with `[Notification!]!`.

## 2. BINDING — Exact Public Signatures (for Tasks 3.2 / 3.3 / 3.4 / 4.x)

```typescript
// Enum — import from the registry (NEVER re-register):
import { NotificationTypePothosEnum } from "@/backend/graphql/pothos/shared/enum.pothos";
// Use for: MyNotificationsFilterInput.type field + markAllNotificationsRead(type:) arg.
// Wire names are the TS enum KEYS (SessionRequest, …); runtime values are the
// canonical snake_case strings — Pothos maps automatically, so resolver args
// arrive as the NotificationType TS enum with zero manual conversion.

// Canonical objects — import from the LEAF modules (domain convention):
import { NotificationPothosObject } from "@/backend/graphql/pothos/notifications/notification.pothos";
import { NotificationListPagePothosObject } from "@/backend/graphql/pothos/notifications/notification-list-page.pothos";
// Notification: backed by NotificationReturnType ($inferSelect row). Resolver
//   returns the row AS-IS — the object handles presentation (id → ID! string,
//   type → guard-validated enum, isRead → null-coalesced false, createdAt → ISO string).
// NotificationListPage: backed by NotificationListPageReturnType { items, totalCount,
//   hasMore } — exactly what NotificationEngine.listMyNotifications returns (2.7 BINDING).

// Registration path (already wired — resolvers need NO barrel edits):
// gqlSchema.ts → import "@/backend/graphql/pothos" → ./notifications → both objects.
// 3.2/3.3 resolver files will ALSO transitively register via their own imports
// (ESM module identity — registering once, whichever path evaluates first).
```

- **3.2 (queries)**: `myNotifications` returns `NotificationListPagePothosObject`; `myUnreadNotificationCount` returns `Int!` (no object). Build `MyNotificationsFilterInput` as a Pothos input (input types remain allowed as separate definitions per the exception policy) with `type: NotificationTypePothosEnum` (nullable), `isRead: Boolean` (nullable), `limit`/`offset` nullable `Int` — forward as-is per the 2.7 runtime-defaults BINDING.
- **3.3 (mutations)**: `markNotificationRead` returns `NotificationPothosObject`; `markAllNotificationsRead(type:)` returns `Int!`.
- **3.4 (schema/codegen)**: run `bun run generate:gqlSchema && bun codegen`; commit ALL artifacts in the same change set. The SDL will gain ONLY the four root operations (the three type additions are already committed by 3.1). **MANDATORY refresh:** the schema-surface suite's frozen inventory MUST be refreshed for the four sanctioned operations — `PRE_3_1_QUERY_FIELDS` += `myNotifications`, `myUnreadNotificationCount`; `PRE_3_1_MUTATION_FIELDS` += `markAllNotificationsRead`, `markNotificationRead` (the "ZERO new mutations" freeze currently pins the 4 legacy mutations; the file header documents the sanctioned-refresh protocol introduced by this task). Also extend the static CUD-absence assertion (REQ-032) per tasks.md 3.4.
- **4.x (frontend)**: `NotificationListPage` already ships `keyFields: false` in `apolloCache.ts` — no extra cache config needed for the wrapper. When the feed documents land, evaluate `items: { merge: false }` replacement semantics per the `AdminDashboardScheduleResult.rows` precedent IF filter/pagination refetches emit cache-data-loss warnings. `Notification.id` is `ID!` (stringified integer) — Apollo normalization works out of the box; `id` in EVERY selection per REQ-062.

## 3. Files Touched

| File | Change |
|---|---|
| `backend/graphql/pothos/shared/enum.pothos.ts` | **MODIFIED (additive)** — `NotificationTypePothosEnum` registered via enum-object form; `NotificationType` value-import added (sorted first); registry JSDoc list extended. All pre-existing enum lines byte-unchanged |
| `backend/graphql/pothos/notifications/notification.pothos.ts` | **NEW** — single canonical `NotificationPothosObject` (73 lines) |
| `backend/graphql/pothos/notifications/notification-list-page.pothos.ts` | **NEW** — `NotificationListPagePothosObject` wrapper (32 lines) |
| `backend/graphql/pothos/notifications/index.ts` | **NEW** — domain leaf barrel (`export * from "./notification.pothos"` + `./notification-list-page.pothos`) |
| `backend/graphql/pothos/index.ts` | **NEW** — top-level Pothos object barrel (`export * from "./notifications"`; documents the ahead-of-resolvers registration rationale) |
| `backend/graphql/gqlSchema.ts` | **MODIFIED** — wiring step 2 added (`import "@/backend/graphql/pothos";`) + wiring-order JSDoc renumbered/extended (5 steps). No other line touched |
| `backend/graphql/test/schema-surface.test.ts` | **MODIFIED** — new `describe("Notification surface")` (4 tests); frozen inventory REFRESHED for the pre-existing applicant additions + the sanctioned notification additions (baseline-repair, D-4); header docs extended; `NotificationType` value-import for single-source pins |
| `frontend/graphql/generated/schema.graphql` | **MODIFIED (generated)** — ONLY `type Notification`, `type NotificationListPage`, `enum NotificationType` added (deterministic lexicographic emission) |
| `frontend/providers/apollo/apolloCache.ts` | **MODIFIED (additive)** — `NotificationListPage: { keyFields: false }` typePolicy + JSDoc (embedded-type policy, D-6) |
| `frontend/graphql/AGENTS.md` | **MODIFIED (additive)** — `NotificationListPage` listed under "Current embedded types" |
| `ai/plans/sprint_2/.../outcome/3.1-outcome.md` | This document |
| `ai/plans/sprint_2/.../tasks.md` | 3.1 block checkboxes flipped (6 boxes — only the 3.1 lines) |
| `/home/z/my-project/worklog.md` | Appended Task 3.1 entry |
| `backend/db/schema/**`, `backend/db/migration/**`, `backend/lib/gateway/public-operations.ts`, `backend/lib/env.ts`, `.env*`, `shared/**`, `backend/types/**`, `backend/enum/**`, `backend/services/**`, `test/workflows/**` | **ZERO drift** — `git status` clean on all protected paths; public-operation allowlist byte-unchanged (default-deny preserved); deferred-items ledger NOT touched (no new ruling needed) |

## 4. Verification Commands & Exit Codes

| # | Command | Result | Exit |
|---|---|---|---|
| 1 | `bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates` × 7 source files + apolloCache.ts | ✅ all five checks (tsgo → oxlint → biome:check → lint:type-aware → check:duplicates) passed for EVERY file — zero fix iterations this session | **0** |
| 2 | `bun run test/scripts/run-test.ts backend/graphql/test/schema-surface.test.ts` — RUN 1 / RUN 2 (+ RUN 3 after the frontend policy edits) | ✅ **16 pass / 0 fail / 82 expects — identical all three runs** (determinism proof) | **0** |
| 3 | **BASELINE PROBE (same suite at HEAD, tracked files reverted)** | ⚠️ **9 pass / 3 fail** — the suite was RED at the 2.M baseline: 3 stale-inventory failures (`myApplicantProfile` query field, `ApplicantStatus` enum, `ApplicantProfile`+`ApplicantStatus` type-delta) — the applicant plan never refreshed the frozen inventory (D-4). 3.1 repairs the inventory AND adds the notification block → 16/0 | n/a |
| 4 | `bun run test/scripts/run-test.ts backend/graphql/test/error-finalizer.test.ts` | ✅ 14 pass / 0 fail / 47 expects | **0** |
| 5 | `bun run test/scripts/run-test.ts backend/graphql/test/context-keys.test.ts` | ✅ 14 pass / 0 fail / 61 expects | **0** |
| 6 | `bun run test/scripts/run-test.ts backend/graphql/test/request-id.test.ts` | ✅ 12 pass / 0 fail / 33 expects | **0** |
| 7 | `bun run test/scripts/run-test.ts backend/graphql/test/error-contract-matrix.test.ts` | ⚠️ **0 pass / 1 fail — PRE-EXISTING environment block, not a regression**: fails in `beforeAll` at server boot ("Another next dev server is already running", PID of the mandatory dev server on :3000) — `setupTestServerLifecycle` cannot spawn its TEST_PORT:3066 server because Next's dev-dir lock is held. Verified IDENTICAL at true HEAD via a full-tree stash probe (same boot-stage failure, zero GraphQL assertions reached). No notification change can influence a Next dev-lock boot failure | 1 (env) |
| 8 | `bun run test/scripts/run-test.ts backend/db/test` | ✅ **56 pass / 0 fail / 222 expects** (baseline holds) | **0** |
| 9 | `bun run test/scripts/run-test.ts backend/services/notifications` | ✅ **83 pass / 0 fail / 684 expects across 7 files** (baseline holds) | **0** |
| 10 | `bun run test/scripts/run-test.ts backend/ws/notification-ws-server.test.ts` | ✅ 23 pass / 0 fail / 77 expects (baseline holds) | **0** |
| 11 | `bun run test/scripts/run-test.ts backend/enum/notifications/notification-type.enum.test.ts` | ✅ 25 pass / 0 fail / 293 expects (enum byte-parity holds through registration) | **0** |
| 12 | `KOTTABY_TEST_RUNNER_OK=1 bun test test/workflows` | ✅ 33 pass / 0 fail / 238 expects across 3 files (baseline holds; 2.8 D-6 runner-guard honored) | **0** |
| 13 | `bun tsgo` (full project) | ✅ **EXIT 0 — ZERO errors** | **0** |
| 14 | `bun biome:check` (full repo) | ✅ **Checked 549 files, no fixes applied** (545 baseline + 4 new .ts files: the two pothos objects + two barrels — arithmetic exact) | **0** |
| 15 | `curl -s http://localhost:3000/api/health` | ✅ HTTP 200, `status: ok` — dev server untouched throughout | n/a |
| 16 | `curl -s -X POST http://localhost:3000/api/graphql -d '{"query":"query { __schema { queryType { name } } }"}'` | ✅ 200 + `{"data":{"__schema":{"queryType":{"name":"Query"}}}}` — live schema builds and serves | n/a |
| 17 | Live introspection: `__type(name:"Notification")` / `__type(name:"NotificationType")` | ✅ Notification = exactly 8 fields (no `userId`); NotificationType = exactly 7 values (EvaluationResult, ParentLinkRequest, PaymentConfirmation, SessionCancellation, SessionCompletion, SessionRequest, SystemBroadcast) | n/a |
| 18 | `git status` on protected paths (schema/migration/env/docs/shared/types/enum/services/workflows/public-operations.ts) | ✅ **ZERO drift** | n/a |

## 5. TE — Test Engineering (3.1.TE)

- **Placement**: the schema-build smoke assertions extend the EXISTING schema-build suite `backend/graphql/test/schema-surface.test.ts` (the repo's only schema-structure test — located by searching `backend/graphql` for schema assertions). New `describe("Notification surface — enum + canonical objects")` with 4 tests:
  1. **Enum**: `NotificationType` registered as `GraphQLEnumType`; `getValues()` length EXACTLY 7; names = the TS enum KEYS; runtime values = the canonical snake_case strings (both compared as sorted sets — the built schema is lexicographically sorted); single-source agreement pinned directly against `Object.keys(NotificationType)` / `Object.values(NotificationType)` (value import of the canonical enum — the test FAILS if the registry ever drifts from `backend/enum/`).
  2. **Notification object**: exactly the 8 canonical field names; exact GraphQL types (`ID!`, `NotificationType!`, `String!`, `String`, `Boolean!`, `String`, `Int`, `String!`); `Object.hasOwn(fields, "userId") === false` (SEC pin).
  3. **`id`-FIRST**: pinned at the SOURCE level — lexical scan of `notification.pothos.ts`'s `fields: t => ({` block asserting `id`'s position precedes every other field (the built schema sorts lexicographically, so wire order carries no semantics; the convention is a source-level discipline, exactly like the gateway static gates).
  4. **Wrapper**: `NotificationListPage` exposes exactly `hasMore`/`items`/`totalCount` with `items: [Notification!]!`, `totalCount: Int!`, `hasMore: Boolean!`; NO `id` (embedded value object).
- **Schema-build proof (three independent tiers)**: (a) the suite imports `graphQLSchema` from `@/backend/graphql/gqlSchema` and walks the BUILT schema (a Pothos double-registration would throw "already been declared" at import); (b) the pre-existing Codegen-sync test proves the committed SDL is byte-identical to a fresh `printSchema(lexicographicSortSchema(...))` emission; (c) the LIVE dev server answered introspection POSTs with the exact surface (commands 16–17).
- **Determinism**: RUN 1 = RUN 2 = RUN 3 = 16/0/82 via the canonical runner.

## 6. SEC — Security & Tenancy Audit (3.1.SEC)

- **REQ-060 field surface, exactly**: the object exposes ONLY `id`, `type`, `title`, `body`, `isRead`, `relatedEntityType`, `relatedEntityId`, `createdAt` — pinned by test (exact field-name set + exact types + `userId` structural absence) AND by live introspection. The recipient `userId` is NEVER GraphQL data: every consuming operation is self-scoped (`ctx.user.id` per REQ-032/§3.4 permission matrix), so the recipient is implied by the caller. Matches the 1.1 `RealtimeNotificationPayload.data` allowlist posture (same 7 data fields + `id`).
- **No write surface**: zero notification-CUD fields/objects/inputs introduced (BFLA containment — the emit inputs `NotificationEmitInput`/`NotificationEmitBatchInput` remain server-internal with NO Pothos binding; grep confirms zero references outside definitions + conformance suite). The full REQ-032 static assertion lands at 3.4 as planned.
- **Public-operation allowlist byte-unchanged**: `backend/lib/gateway/public-operations.ts` untouched (git-status clean) — default-deny preserved; the new types carry NO operations yet, and 3.2/3.3 will attach `{ authenticated: true }` scopes.
- **Enum fail-closed mapping**: `type` resolution runs the `isNotificationType` guard — an unexpected row value surfaces as a resolver error (masked to INTERNAL_SERVER_ERROR by the boundary finalizer), never an unsafe cast or a silent passthrough. REQ-014's GraphQL-enum-layer gate is now live end-to-end: wire value → Pothos enum → guard → pgEnum.
- **No PII/secret exposure**: `NotificationReturnType` carries no secret fields; the object's 8 fields are the complete discloseable surface (1.1's "no forbidden fields" audit covers the underlying row shape).

## 7. SR — Semantic Review (3.1.SR) — 20-item checklist

1. ✅ Single canonical object per entity: exactly ONE `Notification` objectRef in the codebase (grep `objectRef<` — 6 total, one per entity incl. Notification; the wrapper is the sanctioned pagination exception, NOT a second notification-shaped entity object).
2. ✅ Object backed by `backend/types` canonical type (`NotificationReturnType`) imported from `@/backend/types` — NOT a local type def (grep: `pothos/notifications/**` exports exactly 2 consts, zero `type`/`interface`/`enum` declarations).
3. ✅ Wrapper backed by `NotificationListPageReturnType` (same rule).
4. ✅ Enum registered ONCE, enum-object form, from the canonical TS enum (grep `enumType(`: exactly 6 registrations, all in `shared/enum.pothos.ts`, all object-form; zero `values: [...]` literals in any `*.pothos.ts`).
5. ✅ No TS enum defined inline in any Pothos file; no string-union re-declaration of the 7 values (the only literal lists are the TEST's parity fixtures, which pin against `Object.keys/values` of the canonical enum itself — same precedent as 1.2).
6. ✅ `id` exposed FIRST (source-level pin, test 3) for Apollo cache normalization; wrapper is an id-less embedded value type by documented design.
7. ✅ Nullability: v3 defaults (non-null unless explicit `nullable: true`) — `body`/`relatedEntityType`/`relatedEntityId` nullable per REQ-060; everything else non-null; TS types align (`body: string | null` etc.).
8. ✅ Timestamp convention: `createdAt` → ISO-8601 UTC `String!` via `toISOString()` (NO DateTime scalar exists in this builder — grep + `ApplicantProfile`/`HealthCheck` precedents; deviation D-3).
9. ✅ Nullable-boolean governance precedent: `isRead` `null → false` at the presentation layer (`User.isDeleted` precedent), keeping the REQ-060 `Boolean!` contract.
10. ✅ Enum field resolution: guard-validated mapping (fail-closed throw), mirroring `User.role`→`toUserRole`; NOT a bare `as` cast.
11. ✅ Top-level static imports ONLY — zero `import(` anywhere under `backend/graphql/` (grep clean; gate A1 posture maintained for the resolver trees 3.2/3.3 will add).
12. ✅ Import discipline: `NotificationType` VALUE-imported at both runtime-consumption sites (enum registry + the guard call inside the object; test file value-imports for the parity pins); type-only imports use `import type`.
13. ✅ Barrel rules: `export * from "./…"` only, relative `./` paths, one `/` max per path, no `../`, no named re-exports (root AGENTS.md barrel rules — both new barrels comply).
14. ✅ JSDoc style: domain-behavior docs only (no REQ/task/phase references) matching `applicant.pothos.ts`/`user.pothos.ts` conventions; file headers explain the pattern + precedents.
15. ✅ Field exposure order matches REQ-060 SDL order (id, type, title, body, isRead, relatedEntityType, relatedEntityId, createdAt).
16. ✅ Near-duplicate sweep: `Notification`-shaped GraphQL surface exists ONLY in the canonical object + the generated SDL + the test pins; no competing object/mirror type anywhere (grep).
17. ✅ Registration order safety: `gqlSchema.ts` wiring (builder → pothos barrel → enum registry → query → mutation → toSchema) is acyclic; the notifications modules value-import `NotificationTypePothosEnum` from the registry, so whichever path evaluates first registers the enum exactly once (ESM module identity; proven by the clean schema build in three tiers).
18. ✅ No business logic in Pothos files: the only computations are presentation-layer maps (ISO serialization, null coalescing, guard validation) — delegation stays with services (2.7 BINDING: resolvers thin, NO DataLoader — REQ-017 flat single-table reads).
19. ✅ Generated SDL diff contains ONLY notification additions (verified: +`type Notification`, +`type NotificationListPage`, +`enum NotificationType`; zero unrelated drift).
20. ✅ Frontend embedded-type policy honored at type-addition time (`keyFields: false` + AGENTS.md list entry — HealthCheck day-one precedent).

## 8. IV — Instruction Verification (3.1.IV)

| Instruction | Verdict | Evidence |
|---|---|---|
| `backend/graphql/AGENTS.md` — enum registration CRITICAL rule (object form, ONCE, `shared/enum.pothos.ts`, no literals, no inline enums, no re-declaration) | ✅ | §7 items 4–5; grep `enumType(` = 6 object-form registrations all in the registry; schema builds clean (double-registration would throw) |
| `backend/graphql/AGENTS.md` — single canonical object CRITICAL rule (`objectRef<{Entity}ReturnType>`, types from `@/backend/types`, no local type defs, no second entity object) | ✅ | §7 items 1–3; wrapper explicitly sanctioned by the pattern's own Exception Policy ("Wrapper types for collections or complex responses") |
| `backend/graphql/AGENTS.md` — nullability contract (v3 defaults, explicit opt-in) | ✅ | §7 item 7 |
| `backend/graphql/AGENTS.md` — `id` exposed for Apollo cache normalization | ✅ | Entity exposes `ID!` first; id-less wrapper handled under the documented embedded-value-type policy (gateway Rule 8 clause 2 + `frontend/graphql/AGENTS.md`) |
| `backend/graphql/pothos/AGENTS.md` — same rules restated for `pothos/**` | ✅ | Same evidence; plus no `oxlint-disable` comments anywhere in the new files |
| Gateway Rule 8.1 (resolver placement + side-effect barrels, no dynamic imports) | ✅ (N/A-for-resolvers) | No resolvers in 3.1; the object-registration barrel wiring uses the same side-effect discipline; zero `import(` under `backend/graphql/` |
| Gateway Rule 8.2 (objects in `pothos/<domain>/`, one canonical per entity, `id` or documented embedded value type) | ✅ | Files at `backend/graphql/pothos/notifications/`; `keyFields:false` shipped per the clause's own cross-reference to `frontend/graphql/AGENTS.md` |
| Gateway Rule 8.3 (enums backed by real TS enums, registered once, object form) | ✅ | §7 item 4 |
| Gateway Rule 8.4 (authorization on every operation / allowlist rationale) | ✅ (N/A-yet) | No operations introduced; allowlist byte-unchanged; scopes land at 3.2/3.3 |
| Gateway Rule 8.5 (codegen in the same commit) | ✅ partial-by-design | SDL committed in this change set (required for the Codegen-sync assertion); `bun codegen` types deliberately deferred to 3.4 — tasks.md 3.4 owns "run generate + codegen; commit ALL generated artifacts in the same change set" AFTER the 3.2/3.3 resolvers (deviation D-5) |
| tasks.md 3.1 verbatim bullets | ✅ all five | §1 maps bullet-to-artifact one-to-one |
| REQ-060 exact SDL contract | ✅ with documented D-2/D-3 reconciliations | Enum wire names follow the mandated enum-object form (keys on wire); `createdAt` follows the repo's no-DateTime-scalar convention — both deviations documented below |

## 9. Deviations

- **D-1 (resumed session, 2.8 precedent):** Session resumed with this task's artifacts already on disk UNCOMMITTED from an interrupted prior run (6 modified files + `pothos/index.ts` + `pothos/notifications/`). Every artifact was re-audited line-by-line against tasks.md §3.1, plan §3.1/§3.2, both GraphQL AGENTS files, the gateway Rule 8 recipe, and all forwarded BINDINGs (1.1 types, 1.2 enum, 2.7 signatures) rather than re-authored. Two gaps were found and closed by this session: the Apollo `keyFields: false` policy entry (D-6) and the full verification/quality/documentation cycle.
- **D-2 (enum wire names — keys, not snake_case):** plan §3.1's SDL block sketches `enum NotificationType { session_request … }`, but the SAME plan (§2.3) and tasks.md §3.1 mandate the enum-object form `gqlSchemaBuilder.enumType(NotificationType, { name: "NotificationType" })`, which structurally maps TS enum KEYS to GraphQL value names (`SessionRequest`) with the snake_case strings as runtime values — identical to every existing enum in the registry (`ApplicantStatus`, `UserRole`, `Gender`). The AGENTS CRITICAL rule prohibits the literal-array form that snake_case wire names would require. Shipped keys-on-wire; the test pins names, runtime values, AND single-source agreement with the canonical TS enum.
- **D-3 (`createdAt: String!`, not `DateTime!`):** plan SDL says `createdAt: DateTime!`, but this builder/registry has NO DateTime scalar (grep evidence; `applicant.pothos.ts` documents the same finding for its timestamps). Rode the established ISO-8601 UTC string convention (`HealthCheck.timestamp`, `ApplicantProfile.lastAttemptAt/cooldownUntil`); the codegen config already maps any future `DateTime` scalar to `string`, so wire semantics are equivalent.
- **D-4 (baseline test repair):** `schema-surface.test.ts` was RED at the 2.M baseline — 3 stale-inventory failures because the applicant plan (pre-DEV3-010 repo history) added `myApplicantProfile`/`ApplicantProfile`/`ApplicantStatus` without refreshing the frozen inventory (empirically proven: 9 pass / 3 fail at HEAD; the 2.M gate never ran this suite — it only verified `backend/graphql` zero-drift). 3.1 refreshed the inventory for BOTH the pre-existing applicant additions AND the sanctioned notification additions (header documents the refresh protocol), then added the notification describe block: 12 tests → 16 tests, 9/3 → 16/0.
- **D-5 (SDL now, codegen types at 3.4):** `frontend/graphql/generated/schema.graphql` regenerated and committed in this change set — REQUIRED for the suite's Codegen-sync byte-identity assertion to stay green. `bun codegen` (→ `frontend/graphql/generated/gql/graphql.ts`) deliberately NOT run: tasks.md 3.4 explicitly owns "Run `bun run generate:gqlSchema && bun codegen`; commit ALL generated artifacts in the same change set" AFTER the 3.2/3.3 resolvers complete the SDL; no frontend code consumes notification types before Phase 4 (tsgo 0 errors confirms). REQ-061's "AFTER all changes" wording scopes the full artifact sync to 3.4.
- **D-6 (scope addition — Apollo embedded-type policy):** beyond the literal 3.1 bullets, `frontend/graphql/AGENTS.md` mandates `keyFields: false` in `apolloCache.ts` for every newly added id-less GraphQL type AT ADDITION TIME (HealthCheck "normalize-safe from day one" precedent). Added `NotificationListPage: { keyFields: false }` + the AGENTS.md list entry — 4 lines of config, prevents Phase-4 cache-data-loss warnings, sub-loop green.
- **D-7 (`throw new Error` in the enum field resolver):** the object's `type` mapping throws a plain `Error` on an unexpected row value — this matches the `User.role`/`toGender` mapping precedents exactly (a presentation-layer integrity failure is never a client-facing domain condition; the boundary finalizer masks it to `INTERNAL_SERVER_ERROR`). The DomainError-subclass rule governs resolver-level domain conditions (3.2/3.3), not these mapping guards.

## 10. Carry-Forward Knowledge

- **For 3.2/3.3**: exact imports + consumption rules in §2. The four operations will need the schema-surface frozen-inventory refresh (see §2's 3.4 note — do it when the ops land, not before, or the freeze assertions will rightly fail).
- **For 3.4**: regenerate + codegen + commit artifacts; extend the static assertions (CUD-absence REQ-032, four-ops presence); verify allowlist still byte-unchanged. The SDL type additions are already committed — expect the 3.4 SDL diff to contain ONLY the four root-operation lines.
- **ENVIRONMENT — `error-contract-matrix.test.ts` (and any `setupTestServerLifecycle` suite) cannot run in this sandbox**: Next's dev-dir lock is held by the mandatory dev server on :3000, so the TEST_PORT:3066 boot is refused before any assertion runs. Verified identical at true HEAD (full-tree stash probe). 3.2/3.3's integration-tier TE (tasks.md names `setupTestServerLifecycle` + `testClient`) WILL hit the same wall — document identically (boot-stage failure, zero GraphQL assertions reached) and rely on the unit-tier schema/resolver-shape assertions + live dev-server introspection probes for schema proof.
- **ENVIRONMENT — never use `git stash` in this sandbox**: the watchdog's branch flips race the stash's tree-reset (observed: stash created, untracked files captured + removed from disk, tracked modifications reappeared, stash exit non-zero). Recovery recipe that worked: verify `git stash show -p` hash == `git diff` hash → `git checkout stash@{0}^3 -- <untracked paths>` → `git stash drop`. For baseline probes prefer the safe pattern used earlier in this session: `/tmp` copies + `git checkout -- <specific tracked files>` + restore (never full-tree reverts).
- **Counter updates for the next gate**: biome 545 → **549** (+4 files); schema-surface suite 12 → **16 tests** (16/0/82); plan-owned totals grow by the 4 new tests / 43 new expects (82 − 39).
- **Biome note**: `bun biome:check` runs with `--write --unsafe` (repo script) — it reported "No fixes applied" on every run, confirming the new files are format/lint-clean as authored.
