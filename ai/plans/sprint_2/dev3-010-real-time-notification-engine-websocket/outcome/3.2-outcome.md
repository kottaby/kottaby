# Task 3.2 Outcome — Query Resolvers

**Task:** Implement `myNotifications` + `myUnreadNotificationCount` query resolvers (thin, self-scoped, authenticated-only) + `MyNotificationsFilterInput` — REQ-017, REQ-018, REQ-032, REQ-060, REQ-061.
**Verdict:** GREEN — both inbox queries live on the built + served schema with exactly `{ authenticated: true }` scopes; integration tier **executed for real** against the live dev server (12/0/135 ×2 deterministic, zero DB residue); schema-surface 22/0/121 ×2; zero baseline regression anywhere (tsgo 0 errors, biome 553 files exit 0, all layer suites at baseline counts, allowlist byte-unchanged, dev :3000 healthy). Session RESUMED from an interrupted prior run with all artifacts on disk uncommitted — every artifact was re-audited line-by-line against tasks.md §3.2, plan §3.1/§3.2, both GraphQL AGENTS files, gateway Rules 4/8, and the 2.7/3.1 BINDINGs, and EVERY verification row below was re-run fresh in the resuming session (all counts reproduced identically; see D-8).

## 1. What Was Built

- **`backend/graphql/query/notifications/notification.query.ts`** (NEW) — the two root query fields, registered at import time via `gqlSchemaBuilder.queryField(...)` (side-effect only, NO named exports per `backend/graphql/query/AGENTS.md`):
  - `myNotifications(filter: MyNotificationsFilterInput): NotificationListPage!` — `authScopes: { authenticated: true }`; resolver delegates to `NotificationEngine.listMyNotifications(ctx.user.id, filter, ctx.locale)`. The nullable filter fields are forwarded with the engine's documented page-window defaults filled in (`limit` ← `NOTIFICATION_INBOX_DEFAULT_PAGE_LIMIT` (20) when absent, `offset` ← 0) — behaviorally identical to forwarding the nullable Ints as-is (the 2.7 BINDING sanctioned both paths) and cast-free under the repo's `no-unsafe-type-assertion` rule.
  - `myUnreadNotificationCount: Int!` — same scope; delegates to `NotificationEngine.getMyUnreadCount(ctx.user.id, ctx.locale)`.
  - Both bodies carry the established `if (!ctx.user) throw new UnauthorizedError("Authentication required.")` narrowing guard (the `myApplicantProfile` precedent — TypeScript narrowing only; the scope step guarantees it is unreachable).
- **`backend/graphql/pothos/notifications/notification-filter-input.pothos.ts`** (NEW) — `MyNotificationsFilterInput` Pothos input (string-named `inputType` form, the `RegisterUserInput` precedent): `type` via the registered `NotificationTypePothosEnum`, `isRead: Boolean`, `limit`/`offset: Int` — ALL nullable; ZERO identity fields (BOPLA-safe by construction).
- **Domain wiring** — `backend/graphql/query/notifications/index.ts` (NEW side-effect barrel) → `backend/graphql/query/index.ts` (MODIFIED, +`import "./notifications";`) → `gqlSchema.ts` step 4; `backend/graphql/pothos/notifications/index.ts` (MODIFIED, +input re-export) so the input registers ahead of the resolver through BOTH paths (ESM single evaluation).
- **SDL regenerated + committed** (`frontend/graphql/generated/schema.graphql`): diff contains EXACTLY `input MyNotificationsFilterInput` + the two Query root operations. `bun codegen` deliberately deferred to 3.4 (the 3.1 D-5 precedent — REQ-061's "AFTER all changes" wording scopes the full artifact sync to 3.4).
- **Tests** — schema-surface suite extended + inventories refreshed (16→22 tests); NEW wire-tier integration suite `backend/graphql/test/notification-query.test.ts` (12 tests / 135 expects / 5 describes).

## 2. BINDING — for Tasks 3.3 / 3.4 / 4.x

```typescript
// Query resolver consumption pattern (LIVE, this task):
import { MyNotificationsFilterInput } from "@/backend/graphql/pothos/notifications/notification-filter-input.pothos";
import { NotificationListPagePothosObject } from "@/backend/graphql/pothos/notifications/notification-list-page.pothos";
import {
  NOTIFICATION_INBOX_DEFAULT_PAGE_LIMIT,   // 20 — single source for the default window
  NotificationEngine,
} from "@/backend/services/notifications/notification-engine.service";

// 3.3 (mutations): mirror this file exactly —
//   markNotificationRead(id: ID!) → NotificationPothosObject (import from the LEAF module)
//   markAllNotificationsRead(type: NotificationType) → Int!
//   authScopes EXACTLY { authenticated: true }; ctx.user.id + ctx.locale only;
//   keep the UnauthorizedError narrowing guard; parse `id` with a positive-safe-int
//   guard (NO `as number`); mutation input/args defined in the pothos tree (D-1 below).
```

- **3.3 (mutations)**: the `UnauthorizedError` narrowing-guard pattern and the leaf-module import discipline are now the established convention in this tree. When 3.3 lands, `PRE_3_1_MUTATION_FIELDS` in `backend/graphql/test/schema-surface.test.ts` MUST be refreshed (`markNotificationRead`, `markAllNotificationsRead` added to the NEW frozen baseline) — the "ZERO new mutations" freeze will rightly fail otherwise (mirror of this task's query-fields refresh). Regenerate the SDL when 3.3 lands (Codegen-sync byte-identity assertion).
- **3.4 (schema/codegen)**: run `bun run generate:gqlSchema && bun codegen` and commit ALL artifacts — the SDL diff at 3.4 should contain ONLY the two mutation root operations (types + query ops are already committed). Extend the static assertions: CUD-absence (REQ-032), four-ops presence. Verify the allowlist is STILL byte-unchanged.
- **4.x (frontend)**: the wire surface is exactly REQ-060 (with the 3.1 reconciliations: enum keys on the wire, `createdAt: String!` ISO-8601). `myNotifications` selections should include `id` (stringified integer) in EVERY `Notification` selection; `filter.type` values on the wire are the enum KEYS (`SystemBroadcast`, …).
- **Integration-suite run pattern (this sandbox)**: `GRAPHQL_TEST_PORT=3000 ADMIN_EMAIL=admin@app.local ADMIN_PASSWORD=adminpassword123 bun run test/scripts/run-test.ts backend/graphql/test/notification-query.test.ts` — see D-3/D-6. 3.3's integration tier should extend THIS suite (or add a sibling) reusing `postQuery`/oracle helpers.

## 3. Files Touched

| File | Change |
|---|---|
| `backend/graphql/query/notifications/notification.query.ts` | **NEW** — `myNotifications` + `myUnreadNotificationCount` root fields (thin delegation, `{ authenticated: true }` scopes) |
| `backend/graphql/query/notifications/index.ts` | **NEW** — domain query side-effect barrel |
| `backend/graphql/query/index.ts` | **MODIFIED** — +`import "./notifications";` + JSDoc mention (only addition) |
| `backend/graphql/pothos/notifications/notification-filter-input.pothos.ts` | **NEW** — `MyNotificationsFilterInput` (4 nullable filter fields, zero identity fields) |
| `backend/graphql/pothos/notifications/index.ts` | **MODIFIED** — +`export * from "./notification-filter-input.pothos";` + JSDoc sentence |
| `backend/graphql/test/schema-surface.test.ts` | **MODIFIED** — frozen inventories REFRESHED (`PRE_3_1_QUERY_FIELDS` += `myNotifications`, `myUnreadNotificationCount`; `PRE_3_1_TYPE_NAMES` += `MyNotificationsFilterInput`); NEW `describe("Notification query surface — self-scoped inbox reads")` (6 tests: field shapes/args, exact scope snapshot, input field set + identity absence, anonymous in-process UNAUTHORIZED ×2 ops, smuggled-identity validation deaths ×3 probes); header docs extended |
| `backend/graphql/test/notification-query.test.ts` | **NEW** — wire-tier integration suite, 12 tests / 135 expects (§5) |
| `frontend/graphql/generated/schema.graphql` | **MODIFIED (generated)** — ONLY `input MyNotificationsFilterInput` + the two Query operations added |
| `ai/plans/sprint_2/.../outcome/3.2-outcome.md` | This document |
| `ai/plans/sprint_2/.../tasks.md` | 3.2 block checkboxes flipped (6 boxes — only the 3.2 lines) |
| `/home/z/my-project/worklog.md` | Appended Task 3.2 entry |
| `backend/db/schema/**`, `backend/db/migration/**`, `backend/lib/gateway/public-operations.ts`, `backend/lib/env.ts`, `.env*`, `shared/**`, `backend/types/**`, `backend/enum/**`, `backend/services/**`, `test/workflows/**`, `docs/**`, `app/**` | **ZERO drift** — `git status` clean on all protected paths; the public-operation allowlist is byte-identical (default-deny preserved); deferred-items ledger NOT touched (no new ruling needed — D-1 below follows existing repo rules, no plan amendment) |

## 4. Verification Commands & Exit Codes

| # | Command | Result | Exit |
|---|---|---|---|
| 1 | `bun run scripts/health/sub-loop.ts backend/graphql/pothos/notifications/notification-filter-input.pothos.ts --lifecycle duplicates` | ✅ all five checks passed — ZERO fix iterations (first try) | **0** |
| 2 | `bun run scripts/health/sub-loop.ts backend/graphql/query/notifications/notification.query.ts --lifecycle duplicates` | ✅ all five passed — ZERO fix iterations (first try) | **0** |
| 3 | `bun run scripts/health/sub-loop.ts backend/graphql/query/notifications/index.ts` / `backend/graphql/query/index.ts` / `backend/graphql/pothos/notifications/index.ts` | ✅ all five passed for EACH barrel — ZERO fix iterations | **0** |
| 4 | `bun run scripts/health/sub-loop.ts backend/graphql/test/schema-surface.test.ts --lifecycle duplicates` | ✅ all five passed — after 3 honest fix iterations (TS18048 ×2 rounds → guarded extraction; TS18049 hoisted-closure narrowing → captured `rootFields` after the guard; oxlint `no-await-in-loop` → `Promise.all`) | **0** |
| 5 | `bun run scripts/health/sub-loop.ts backend/graphql/test/notification-query.test.ts --lifecycle duplicates` | ✅ all five passed — after 4 honest fix iterations (TS2551 `MutateResult.errors`→`.error`; `no-await-in-loop`→`Promise.all`; sonarjs `no-hardcoded-passwords`→credential naming; `no-identical-functions`→module-scope hoist) | **0** |
| 6 | `bun run test/scripts/run-test.ts backend/graphql/test/schema-surface.test.ts` — RUN 1 / RUN 2 | ✅ **22 pass / 0 fail / 121 expects — identical both runs** (was 16/0/82; +6 tests / +39 expects) | **0** |
| 7 | `bun run test/scripts/run-test.ts backend/graphql/test/notification-query.test.ts` (canonical, port 3066) | ⚠️ **0 pass / 1 fail — PRE-EXISTING environment block, not a regression**: fails in `beforeAll` at server boot — "⨯ Another next dev server is already running" (the mandatory :3000 dev server holds Next's dev-dir lock; IDENTICAL to the 3.1 finding for `error-contract-matrix.test.ts`, verified at true HEAD there). Zero GraphQL assertions reached | 1 (env) |
| 8 | `GRAPHQL_TEST_PORT=3000 ADMIN_EMAIL=admin@app.local ADMIN_PASSWORD=adminpassword123 bun run test/scripts/run-test.ts backend/graphql/test/notification-query.test.ts` — RUN 1 / RUN 2 | ✅ **12 pass / 0 fail / 135 expects — identical both runs** (determinism proof). The lifecycle helper's liveness probe succeeds against the LIVE dev server → no second server spawned, nothing killed | **0** |
| 9 | DB residue probe after each integration run (psql) | ✅ `notifications = 0`, `wire-%@test.local` users = 0, orphan students/applicants/parents = 0 — the `afterAll` teardown is complete and the seeded admin's user row is untouched | n/a |
| 10 | `bun run test/scripts/run-test.ts backend/graphql/test/error-finalizer.test.ts` / `context-keys.test.ts` / `request-id.test.ts` | ✅ 14/0/47 · 14/0/61 · 12/0/33 (baselines hold) | **0** |
| 11 | `KOTTABY_TEST_RUNNER_OK=1 bun --env-file=.env.test test frontend/graphql/test/gateway/allowlist-coverage.test.ts` | ✅ **8 pass / 0 fail / 42 expects** — default-deny holds: the two new fields sit in the `scoped` set; the unscoped set remains exactly `["_health", "recitationReadings"]` ≡ allowlist | **0** |
| 12 | `bun run test/scripts/run-test.ts backend/lib/gateway/static-assertions.test.ts` / `public-operations.test.ts` | ✅ 17/0 · 26/0 (gate A1 dynamic-import scan + registry closure hold) | **0** |
| 13 | `bun run test/scripts/run-test.ts backend/db/test` | ✅ **56 pass / 0 fail / 222 expects** (baseline holds) | **0** |
| 14 | `bun run test/scripts/run-test.ts backend/services/notifications` | ✅ **83 pass / 0 fail / 684 expects across 7 files** (baseline holds) | **0** |
| 15 | `bun run test/scripts/run-test.ts backend/ws/notification-ws-server.test.ts` / `backend/enum/notifications/notification-type.enum.test.ts` | ✅ 23/0/77 · 25/0/293 (baselines hold) | **0** |
| 16 | `KOTTABY_TEST_RUNNER_OK=1 bun test test/workflows` | ✅ 33 pass / 0 fail / 238 expects (baseline holds; 2.8 D-6 runner-guard honored) | **0** |
| 17 | `bun tsgo` (full project) | ✅ **EXIT 0 — ZERO errors** | **0** |
| 18 | `bun biome:check` (full repo) | ✅ **Checked 553 files, no fixes applied** (549 baseline + 4 new .ts files: input + resolver + 2 barrels… + the integration test = exactly 4) | **0** |
| 19 | `curl -s http://localhost:3000/api/health` | ✅ HTTP 200 — dev server untouched throughout | n/a |
| 20 | Live cookie-path demo: `login` mutation (`-c` cookie jar) → `myNotifications` + `myUnreadNotificationCount` with the `access_token` cookie | ✅ login returns user `{id:1, role:"Admin"}` + sets 1 `access_token` cookie; both queries answer `{"items":[],"totalCount":0,"hasMore":false}` / `0` (clean inbox — residue-free) | n/a |
| 21 | Live anonymous probes (curl, no credentials) | ✅ BOTH ops → `extensions.code = "UNAUTHORIZED"`, correct `path`, `requestId` attached | n/a |
| 22 | `git diff backend/lib/gateway/public-operations.ts` | ✅ EMPTY — allowlist byte-identical | n/a |

## 5. TE — Test Engineering (3.2.TE)

**Verification path used: BOTH.** The integration tier was ATTEMPTED canonically (row 7 — env-blocked exactly as 3.1 documented) and then **executed for real** against the live dev server via `GRAPHQL_TEST_PORT=3000` (row 8) — the suite code itself is the canonical `setupTestServerLifecycle` + `testClient` shape and boots its own 3066 server in CI; only the PORT env points it at the already-running dev server in this sandbox (the helper's liveness probe short-circuits the spawn — nothing is spawned OR killed). The unit/schema tier (schema-surface) and live curl probes (rows 20–21) provide the independent corroborating tiers.

**`backend/graphql/test/notification-query.test.ts` — 12 tests / 135 expects / 5 describes:**
- **Anonymous tier (2)** — `testClient` (no credentials) → `expectMutationError(result.error, "UNAUTHORIZED")` on BOTH ops.
- **Per-role self-scoped reads (4)** — 4 REAL actors: student/teacher(applicant)/parent registered over the wire through the PUBLIC `registerUser` mutation (real bcrypt path), admin = the seeded admin via `login`. Assertions: every actor's wire page is EXACTLY its own direct-DB oracle (items in `createdAt DESC, id DESC` order, stringified ids, `totalCount`, `hasMore`); NO actor's page EVER contains another actor's fixture titles (cross-inbox isolation); `myUnreadNotificationCount` ≡ direct-DB unread oracle for all 4 roles; `id` arrives as a STRING matching `/^\d+$/` that round-trips against the DB row ids.
- **Filter-over-wire coherence (4)** — `type` filter (page AND total narrowed), `type`+`isRead` conjunction, `isRead`-only, and the `limit`/`offset` window slide (page 1 vs page 2 id-disjoint, `hasMore` math exact, tail window `hasMore:false`). Enum filters travel as GraphQL NAMES (`SystemBroadcast`) — mapped by `wireNameOf` — while the oracle compares runtime values.
- **BOPLA wire probe (2)** — inline-document smuggles (`filter: { userId: 12345 }` and root `myNotifications(userId: 12345)`) die as `GRAPHQL_VALIDATION_FAILED` BEFORE any resolver runs.

**`backend/graphql/test/schema-surface.test.ts` — refreshed + extended (16→22 tests):** frozen inventories now pin the NEW baseline (`myNotifications`/`myUnreadNotificationCount` in `PRE_3_1_QUERY_FIELDS`; `MyNotificationsFilterInput` in `PRE_3_1_TYPE_NAMES` — the whole-schema type-delta assertion still yields exactly `["HealthCheck"]`); the new describe block pins: `myNotifications` → `NotificationListPage!` with ONE optional `filter` arg typed `MyNotificationsFilterInput`; `myUnreadNotificationCount` → `Int!` with ZERO args; BOTH authScopes snapshots are EXACTLY `{ authenticated: true }` (keys pinned, role/permission/superAdmin structurally absent); the input exposes EXACTLY `type/isRead/limit/offset`, all nullable, with `userId`/`user`/`id`/`recipientId` structurally absent; anonymous in-process execution (real `graphql()` against the built schema, empty context) → `UNAUTHORIZED` on each op with correct `path`; and the three smuggled-identity validation deaths (`Unknown argument "userId"` / `unknown field "userId"`).

**Determinism:** integration 12/0/135 RUN 1 = RUN 2; schema-surface 22/0/121 RUN 1 = RUN 2. Fixture teardown verified by post-run psql residue probes (row 9).

## 6. SEC — Security & Tenancy Audit (3.2.SEC)

- **Zero identity args accepted**: there is NO identity field anywhere on the surface — root args are exactly `filter` (list) / none (count); the input's field set is exactly `type/isRead/limit/offset`. Pinned THREE ways: structurally (schema-surface field-set assertions incl. `userId`/`user`/`id`/`recipientId` absence), at validation (unknown-argument/unknown-field documents rejected pre-resolver — unit tier AND wire tier), and behaviorally (every actor reads only its own oracle). BOLA probes die as GraphQL validation failures before a resolver ever runs.
- **Scope exactly `{ authenticated: true }`** on BOTH operations — pinned against the Pothos extension snapshot (`extensions.pothosOptions.authScopes`): keys exactly `["authenticated"]`, value `true`; `role`/`permission`/`superAdmin`/`notImpersonating` structurally absent (no role material — every authenticated role owns an inbox, per REQ-032's "no role scope" clause).
- **BFLA**: ZERO emit references anywhere under `backend/graphql/**` (grep: `emitForUser|emitForUsers|emitBatch|publishReceipts|publishFanout` → no matches). The bulk fan-out primitive remains unreachable from any resolver; `NotificationEngine` is referenced ONLY by `notification.query.ts` (+ its test's JSDoc mention).
- **Default-deny preserved**: `backend/lib/gateway/public-operations.ts` byte-identical (empty `git diff`); the allowlist-coverage gate (8/0/42) confirms unscoped-fields ≡ allowlist with the two new fields correctly in the scoped set.
- **Anonymous rejection**: UNAUTHORIZED with correct path + requestId — proven at 3 tiers (in-process `graphql()` execution, wire `testClient`, wire curl).
- **No PII/secret exposure**: the query returns only the REQ-060 field surface; the recipient `userId` is never GraphQL data (implied by the self-scoped caller).

## 7. SR — Semantic Review (3.2.SR) — 20-item checklist

1. ✅ Thin resolvers: both bodies are single delegation calls (+ the established TS-narrowing guard); zero business logic.
2. ✅ Zero repository imports in resolver files (grep `backend/db/repo|Repository` under `backend/graphql/query/` → no matches).
3. ✅ No DataLoader (REQ-017 documents flat single-table reads; 2.7 BINDING — no per-parent N+1 exists).
4. ✅ Resolvers validation-free: no bounds checks inline — `limit ∈ 1..50`, `offset ≥ 0`, enum/boolean guards all live in the engine's service boundary (REQ-054); the resolver only fills the documented defaults.
5. ✅ `ctx.locale` propagated on EVERY engine call (list + count).
6. ✅ No localized copy needed in the resolvers: the only thrown error is the unreachable narrowing guard mirroring `builder.ts`'s `authenticated` scope message verbatim (the `myApplicantProfile` precedent); the engine owns all localized errors (`getServerTranslations(locale)` at the service seam).
7. ✅ Identity = `ctx.user.id` exclusively; no arg, header, or input field contributes identity.
8. ✅ `authScopes` exactly `{ authenticated: true }` (test-pinned; see §6).
9. ✅ Enum consumed via the registry import (`NotificationTypePothosEnum` from `shared/enum.pothos`) — no re-registration (the schema builds; a double registration would throw "already been declared").
10. ✅ Input type authored in the string-named `inputType` form (the AGENTS-documented form), all-nullable fields, BOPLA whitelist — no identity field can be smuggled.
11. ✅ Default window filling uses the engine's exported `NOTIFICATION_INBOX_DEFAULT_PAGE_LIMIT` — the single source of truth for the default page size (2.7 BINDING), not a local literal.
12. ✅ Top-level static imports ONLY — zero `await import(` anywhere under `backend/graphql/**` (grep; gate A1 green).
13. ✅ Side-effect barrel registration per `query/AGENTS.md`: `query/notifications/index.ts` → `query/index.ts` → `gqlSchema.ts`; the `.query.ts` file has NO named exports and is never imported outside its subtree.
14. ✅ JSDoc style: domain-behavior docs only (no REQ/task/phase references), matching `applicant.query.ts`/`auth.query.ts` conventions; the input file's docs mirror `register-input.pothos.ts`.
15. ✅ Nullability per REQ-060 SDL: `NotificationListPage!` / `Int!` non-null returns; `filter` arg optional + nullable; all input fields nullable; TS types align.
16. ✅ `id` present in every test selection and exposed FIRST by the canonical object (3.1 pin); wire ids normalize as strings.
17. ✅ Zero `console.*` in the new production files.
18. ✅ Zero `as` casts in resolver code — only `?? null` coalescing when building the engine filter object.
19. ✅ Field descriptions present on both root fields (the `me`/`login` description precedent).
20. ✅ Near-duplicate sweep: exactly ONE registration site for each op; the schema-surface frozen inventory pins the complete root set (no stray notification-shaped query anywhere).

## 8. IV — Instruction Verification (3.2.IV)

| Instruction | Verdict | Evidence |
|---|---|---|
| `backend/graphql/query/AGENTS.md` — side-effect imports only, no named exports, sub-directory barrel wiring, "Do not place Pothos object/enum/input type definitions here" | ✅ | `.query.ts` registers via `queryField` with no exports; input type authored in the pothos tree (D-1); wired `notifications/index.ts` → `query/index.ts` |
| `backend/graphql/AGENTS.md` — resolvers delegate to services (never repositories), locale propagation, `ctx.t` discipline | ✅ | §7 items 1–6; delegation is exclusively `NotificationEngine` |
| `backend/graphql/AGENTS.md` — input types allowed as separate definitions (exception policy) | ✅ | `MyNotificationsFilterInput` in `pothos/notifications/`, the `RegisterUserInput` placement precedent |
| Gateway Rule 8.1 (resolver placement + side-effect barrels, never dynamic-import resolvers) | ✅ | `backend/graphql/query/notifications/notification.query.ts`; zero `await import(` under `backend/graphql/**` (grep + gate A1 17/0) |
| Gateway Rule 8.4 (authorization declared on EVERY operation; allowlist only for anonymous-by-design) | ✅ | Both fields carry `authScopes: { authenticated: true }`; no allowlist entry added (byte-identical file); allowlist-coverage gate green both directions |
| Gateway Rule 4 (default-deny public-operation allowlist) | ✅ | `git diff public-operations.ts` EMPTY; `public-operations.test.ts` 26/0; the new fields are scoped, hence not in the unscoped audit set |
| tasks.md 3.2 verbatim bullets (authScopes on BOTH; thin delegation signatures; input ref via registered enum; barrel side-effect registration; NO await import; NO business-logic/repo imports; allowlist unchanged) | ✅ all | §1 maps bullet-to-artifact one-to-one; §6–§7 hold the evidence |
| REQ-060 exact SDL contract (query half) | ✅ | SDL diff adds exactly `myNotifications(filter: MyNotificationsFilterInput): NotificationListPage!` + `myUnreadNotificationCount: Int!` + the input type with the 4 nullable fields; schema-surface pins the shapes |

## 9. Deviations

- **D-1 (input-type placement — follows repo rules over literal task wording):** tasks.md §3.2 sketches `MyNotificationsFilterInput` alongside the query file, but `backend/graphql/query/AGENTS.md` is explicit: "Do not place Pothos object/enum/input type definitions here — those live in `backend/graphql/pothos/<subdir>/`" (and the `RegisterUserInput` precedent lives at `pothos/auth/register-input.pothos.ts`). The input therefore lives at `backend/graphql/pothos/notifications/notification-filter-input.pothos.ts`, imported into the query file — exactly the 3.1 BINDING's "Build `MyNotificationsFilterInput` as a Pothos input" with the repo's placement discipline.
- **D-2 (default-filling, not a widening cast):** the 2.7 BINDING sanctioned either forwarding the nullable Ints with a widening cast or filling the documented defaults at the resolver. Shipped default-filling (`limit` ← exported `NOTIFICATION_INBOX_DEFAULT_PAGE_LIMIT`, `offset` ← 0) — cast-free under `no-unsafe-type-assertion`, behaviorally identical (the engine re-applies the same defaults and owns all bounds validation).
- **D-3 (integration tier executed via `GRAPHQL_TEST_PORT=3000`):** the canonical 3066 boot is env-locked in this sandbox (row 7 — "Another next dev server is already running", identical to the 3.1 finding at true HEAD). The suite was run UNCHANGED with `GRAPHQL_TEST_PORT=3000`, which makes `setupTestServerLifecycle`'s liveness probe succeed against the LIVE dev server and skip the spawn entirely (nothing spawned, nothing killed — the :3000 server was never touched by the suite). The suite file stays CI-canonical (boots its own 3066 server where no dev server holds the lock). This is the honest adaptation the 3.1 carry-forward prescribed, and it delivered REAL end-to-end coverage (registration → login → queries → DB oracle equivalence) rather than a fallback-only proof.
- **D-4 (dev-server HMR stale-state incident — environment knowledge):** creating the query tree triggered a turbopack PARTIAL re-evaluation (new registration modules against the RETAINED `builder.ts` instance) → `PothosSchemaError: Duplicate typename: UserRole` and HTTP 500s on /api/graphql. Healed WITHOUT restarting the dev server by content-touching `builder.ts` (invalidates the whole registration graph → fresh builder + consistent re-registration); `builder.ts` was then reverted byte-identically and the server re-settled healthy (verified by the live probes). Recorded as carry-forward knowledge for 3.3: after creating the mutation resolvers, probe `/api/graphql`; on duplicate-typename 500s, content-touch `builder.ts` and revert.
- **D-5 (authenticated wire calls via raw fetch + Bearer header):** the shared `testClient`'s fixed `HttpLink` cannot attach per-request auth headers, so authenticated reads use a raw `fetch` with `Authorization: Bearer <accessToken>` — the documented production client path read by `createGraphQLContext` (header preferred over cookie). `testClient` remains the vehicle for the anonymous probes and the register/login mutations. The cookie path was additionally proven live via curl (row 20).
- **D-6 (admin fixture credentials):** the suite reads `ADMIN_EMAIL`/`ADMIN_PASSWORD` with the SEED's own fallback chain (`admin@draftacademy.local` / `Seed_Pass1!`); in this sandbox the run needs the env prefix (row 8) because `--env-file=.env.test` carries no ADMIN_* values. No credentials are hardcoded beyond the seed's own documented fallback.
- **D-7 (SDL now, codegen at 3.4):** the SDL was regenerated and committed (required for the Codegen-sync byte-identity assertion); `bun codegen` deliberately deferred to 3.4 per the 3.1 D-5 precedent (tasks.md 3.4 owns the full artifact sync after 3.3 completes the SDL).
- **D-8 (resumed session, 2.8/3.1 precedent):** this session resumed with the task's complete artifact set on disk UNCOMMITTED from an interrupted prior run (resolver + input + barrels + both test files + SDL + tasks.md flips + this outcome document). Rather than re-authoring, every artifact was re-audited line-by-line against tasks.md §3.2 verbatim, plan §3.2/§3.4 (resolver rules, permission matrix), `backend/graphql/AGENTS.md` + `backend/graphql/query/AGENTS.md` + `backend/graphql/pothos/AGENTS.md`, gateway Rules 4/8.1/8.4, the 2.7 engine BINDING (signatures, default-filling, no-DataLoader), and the 3.1 object/enum BINDING (leaf-module imports, registry enum). ZERO gaps were found in the re-audit; the FULL verification cycle was then re-run fresh this session: sub-loop ×7 files exit 0 (zero additional fix iterations), schema-surface 22/0/121 ×2, canonical 3066 integration attempt env-blocked at boot (identical to the prior session's finding), live-:3000 integration 12/0/135 ×2 + psql residue probe clean, tsgo 0 errors, biome 553 files, DB 56/0/222, notifications 83/0/684, WS 23/0/77, enum parity 25/0/293, workflows 33/0/238, allowlist-coverage 8/0/42, static-assertions 17/0, public-operations 26/0, error-finalizer/context-keys/request-id at baseline, live curl probes (health 200, anonymous UNAUTHORIZED ×2, login-cookie-path queries). The only artifacts this session authored: this D-8 note + the worklog entry.

## 10. Carry-Forward Knowledge

- **For 3.3**: exact consumption pattern + refresh obligations in §2 (mutation fields into `PRE_3_1_MUTATION_FIELDS`; SDL regen; input-placement ruling D-1 applies to mutation args/inputs; the `id` positive-safe-int guard with NO `as number`).
- **For 3.4**: expect the SDL diff to contain ONLY the two mutation root operations; biome counter moves 553 → 555; add the REQ-032 CUD-absence static assertion; re-verify the allowlist byte-state.
- **ENVIRONMENT — dev-server HMR duplicate-typename**: see D-4. Any task that adds/edits Pothos registration modules while the :3000 dev server runs may hit `Duplicate typename` 500s; the no-restart heal is a `builder.ts` content-touch (+ revert). Probe `/api/graphql` health after every schema-file edit.
- **ENVIRONMENT — `run-server-tests.ts` ALWAYS spawns AND kills a server on its port**: NEVER route it at port 3000 (`killListenersOnPort(3000)` would kill the mandatory dev server). `frontend/graphql/test/**` paths route through it from `run-test.ts`, so suites there are env-locked in this sandbox even when the suite itself is unit-tier — run such suites directly via `KOTTABY_TEST_RUNNER_OK=1 bun --env-file=.env.test test <file>` (the allowlist-coverage precedent, row 11).
- **ENVIRONMENT — canonical 3066 integration boot**: reproduced the 3.1 finding for THIS suite too (row 7) — document identically in 3.3/3.4 and reuse the `GRAPHQL_TEST_PORT=3000` pattern.
- **Counter updates for the next gate**: biome 549 → **553** (+4 files); schema-surface 16 → **22 tests** (22/0/121); NEW integration suite `backend/graphql/test/notification-query.test.ts` **12/0/135**; plan-owned totals grow by 18 tests / 174 expects (220/1,514 → 238/1,688).
- **Apollo v4 note**: `testClient.mutate(...)` results carry `.error` (CombinedGraphQLErrors), NOT `.errors` — use `extractErrorCode(result.error)` for diagnostics (cost one QL iteration).
