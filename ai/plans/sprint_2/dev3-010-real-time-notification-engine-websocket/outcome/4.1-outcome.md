# Task 4.1 Outcome — Notification Frontend GraphQL Documents

**Task:** Author `frontend/graphql/sharedDocuments/notifications/notification.documents.ts` (+ sub-barrel + top-level barrel export) with the four inbox documents, run `bun run generate:gqlSchema && bun codegen` in the SAME change set, and add document-shape snapshot assertions — REQ-062. **Phase 4 opener + the codegen sync point.**

**Verdict:** GREEN — the four `TypedDocumentNode` documents (`myNotificationsQueryDocument`, `myUnreadNotificationCountQueryDocument`, `markNotificationReadMutationDocument`, `markAllNotificationsReadMutationDocument`) are live through both the deep import and the top-level barrel; `bun run generate:gqlSchema` regenerates the SDL **BYTE-IDENTICALLY** (0-byte diff — the 3.4 prediction held) and `bun codegen` grows `graphql.ts` by **exactly +53 lines of notification additions** (the `NotificationType` enum with wire-name values, `MyNotificationsFilterInput`, the four operation type families with their compact extracted types, and the four generated `DocumentNode` constants — precisely the 3.4 §2 BINDING export list); a NEW co-located structural suite (`notification.documents.test.ts`, 12/0/59 ×2 deterministic) locks operation names/channels/variables, `id`-in-every-`Notification`-selection, the self-scoped variable surface, barrel identity and the codegen type bindings; zero baseline regression anywhere (tsgo 0 errors, biome 560 files exit 0, all layer suites at baseline counts, documents-contract 11/0/53 untouched-and-green, dev :3000 healthy).

## 1. What Was Built

- **`frontend/graphql/sharedDocuments/notifications/notification.documents.ts` (NEW)** — the four documents, each `gql` + `TypedDocumentNode` imported from `@apollo/client` (never `/core`), each typed by codegen operation types from `@/frontend/graphql/generated/gql/graphql` ONLY:
  - `myNotificationsQueryDocument: TypedDocumentNode<MyNotificationsQuery, MyNotificationsQueryVariables>` — `query MyNotifications($filter: MyNotificationsFilterInput)`; the `NotificationListPage` wrapper selects `items` (full eight-field `Notification` row, `id` FIRST) + `totalCount` + `hasMore` (no `id` on the wrapper — embedded value type).
  - `myUnreadNotificationCountQueryDocument: TypedDocumentNode<MyUnreadNotificationCountQuery>` — no-arg form (second type parameter omitted per the sharedDocuments convention table); bare `Int` leaf, zero sub-selection.
  - `markNotificationReadMutationDocument: TypedDocumentNode<MarkNotificationReadMutation, MarkNotificationReadMutationVariables>` — `mutation MarkNotificationRead($id: ID!)`; returns the full eight-field `Notification` row with `id` FIRST so Apollo writes the flipped row back into the same normalized cache entry the feed query produced.
  - `markAllNotificationsReadMutationDocument: TypedDocumentNode<MarkAllNotificationsReadMutation, MarkAllNotificationsReadMutationVariables>` — `mutation MarkAllNotificationsRead($type: NotificationType)`; bare `Int` leaf (affected-row count — consumer refetches/evicts lists, nothing to normalize).
  - JSDoc per constant following the `auth.documents.ts` / `applicant.documents.ts` conventions (wire semantics, self-scoping, cache-normalization notes, conventions citations).
- **`frontend/graphql/sharedDocuments/notifications/index.ts` (NEW)** — sub-barrel: `export * from "./notification.documents";`.
- **`frontend/graphql/sharedDocuments/index.ts` (MODIFIED, +1 line)** — added `export * from "./notifications";` (alphabetical: auth → notifications → teachers). plan.md §5.4 claimed the top-level barrel "already re-exports `notifications/`" — that was FALSE at execution time (the barrel only had auth + teachers); the line was added per the dev2-004 DISP-1 precedent (D-1 below).
- **`bun run generate:gqlSchema && bun codegen` (SAME change set, gateway Rule 8.5 / REQ-061):**
  - SDL: wrote 4914 bytes → `git diff frontend/graphql/generated/schema.graphql` = **0 bytes** (byte-identical regeneration, re-verified idempotent on a second pass — no schema source was touched, exactly as planned).
  - `graphql.ts`: +53 insertions / 0 deletions — audited line-by-line in §3; contains ONLY the notification additions (the 3.4 §2 BINDING predicted every one of them).
- **`frontend/graphql/sharedDocuments/notifications/notification.documents.test.ts` (NEW)** — the document-shape snapshot assertion suite (4.1.TE), 12 tests / 59 expects / 3 describes, mirroring the `documents.contract.test.ts` discipline for the notifications domain:
  - **named operations + channel + variables (6)** — each document is a single named operation with the right channel and the exact sanctioned variable set (`["filter"]`, `[]`, `["id"]`, `["type"]`); every declared variable is actually threaded into its root-field argument (no dead variables, no literal arguments); the total variable surface across all four documents is EXACTLY `filter`/`id`/`type` sorted — zero identity arguments (SEC pin; belt-and-braces `not.toContain("user")` on each name).
  - **id + selection shapes (3)** — every `Notification`-typed selection (`myNotifications.items`, the `markNotificationRead` payload) selects EXACTLY the eight REQ-060 fields with `id` FIRST; the `NotificationListPage` wrapper selects exactly `items`/`totalCount`/`hasMore` and no `id` (embedded value type, `keyFields: false`); the two scalar payloads (`myUnreadNotificationCount`, `markAllNotificationsRead`) select no sub-selection (bare `Int` reads).
  - **codegen binding + barrel parity (3)** — the top-level barrel re-exports the SAME document instances as the deep imports (cache-key safety, `toBe` identity ×4); the constants remain `TypedDocumentNode`-typed against the generated operation types (compile-time proof by assignment — tsgo fails on drift); the generated `NotificationType` enum members are the GraphQL WIRE names (Record-widened `toEqual` over all seven members — the codegen-enum-vs-backend-enum trap pinned as a permanent test).

## 2. BINDING for 4.2 / 4.3 / 4.4 — exact consumption contract

**Document constants** (import from the barrel `@/frontend/graphql/sharedDocuments` — preferred — or deep `…/sharedDocuments/notifications/notification.documents`):
`myNotificationsQueryDocument`, `myUnreadNotificationCountQueryDocument`, `markNotificationReadMutationDocument`, `markAllNotificationsReadMutationDocument`.

**Codegen type exports now live in `@/frontend/graphql/generated/gql/graphql`** (exact generated shapes):
- `enum NotificationType { EvaluationResult, ParentLinkRequest, PaymentConfirmation, SessionCancellation, SessionCompletion, SessionRequest, SystemBroadcast }` — native enum, **values = the GraphQL wire names**. Frontend filter/variable code MUST use THIS enum; NEVER import the backend canonical enum (its runtime values are snake_case, server-side only). Test-pinned.
- `MyNotificationsFilterInput = { isRead: boolean | null | undefined; limit: number | null | undefined; offset: number | null | undefined; type: NotificationType | null | undefined }`.
- `MyNotificationsQuery` / `MyNotificationsQuery_myNotifications` (`{ items, totalCount, hasMore }`) / `MyNotificationsQuery_myNotifications_items` (`{ id: string, type: NotificationType, title: string, body: string | null, isRead: boolean, relatedEntityType: string | null, relatedEntityId: number | null, createdAt: string }`) / `MyNotificationsQueryVariables = Exact<{ filter: MyNotificationsFilterInput | null | undefined }>` — `filter` is nullable-but-present (pass an object or `null`; omit via `undefined`).
- `MyUnreadNotificationCountQuery = { myUnreadNotificationCount: number }` / `…QueryVariables = Exact<{ [key: string]: never }>` — call `useQuery(myUnreadNotificationCountQueryDocument)` with NO variables object.
- `MarkNotificationReadMutation` / `MarkNotificationReadMutation_markNotificationRead` (same eight-field row shape as the list item) / `…MutationVariables = Exact<{ id: string | number }>` — **PASS `id` AS A STRING** (repo wire convention; the `string | number` union is the codegen `ID`-scalar default, not an invitation).
- `MarkAllNotificationsReadMutation = { markAllNotificationsRead: number }` / `…MutationVariables = Exact<{ type: NotificationType | null | undefined }>`.
- Generated document twins also exist (`MyNotificationsDocument`, `MyUnreadNotificationCountDocument`, `MarkNotificationReadDocument`, `MarkAllNotificationsReadDocument`) — repo convention consumes the camelCase `sharedDocuments` constants, not the generated twins (same as `MeDocument` vs `meQueryDocument`).

**Consumption rules (unchanged from the 3.4 BINDING, now test-backed):** string ids; `id` in EVERY `Notification` selection is enforced at the document tier; mark-all returns a bare `Int` (client refetches/evicts lists — nothing to write back); `NotificationListPage` is `keyFields: false` in `apolloCache.ts` (registered by 3.1); marking read is NOT a realtime event (2.7 BINDING); hooks from `@apollo/client/react` ONLY, `useQuery` stateful ONLY (`useLazyQuery` banned); NO mapping layers, NO indexed-access workarounds, NO schema-level object types — codegen names directly.

**4.2 catch-up posture:** the reconnect refetch uses `myNotificationsQueryDocument` (page 1) + `myUnreadNotificationCountQueryDocument` via the Apollo client instance (`client.refetchQueries` / `client.query`) — both documents are ready now.

## 3. Generated-Artifact Diff Audit (4.1.SR centerpiece)

| Artifact | Diff | Audit |
|---|---|---|
| `frontend/graphql/generated/schema.graphql` | **0 bytes** | Byte-identical regeneration (4914 bytes, twice — idempotent). No schema source touched; the Phase-3 SDL is FINAL and its artifact↔builder lockstep holds through the Phase-4 opener. |
| `frontend/graphql/generated/gql/graphql.ts` | **+53 / −0** | Line-by-line audited — EXACTLY the 3.4 §2 BINDING list, nothing else: `MyNotificationsFilterInput` type; `NotificationType` native enum (7 wire-name members); `MyNotificationsQuery_myNotifications_items` + `_myNotifications` + `MyNotificationsQuery` + `MyNotificationsQueryVariables`; `MyUnreadNotificationCountQuery` + Variables; `MarkNotificationReadMutation_markNotificationRead` + Mutation + Variables; `MarkAllNotificationsReadMutation` + Variables; the four generated `DocumentNode` constants. ZERO pre-existing lines modified or removed — all existing exports byte-stable. The change set's only other tracked delta is the +1 barrel line. |

Both artifacts re-generated a SECOND time at the end of the session: diffs unchanged (idempotency proof — the committed content is the deterministic generator output).

## 4. Verification Commands & Exit Codes

| # | Command | Result | Exit |
|---|---|---|---|
| 1 | `bun run generate:gqlSchema` | ✅ wrote `schema.graphql` (4914 bytes) — `git diff` = **0 bytes** (twice) | **0** |
| 2 | `bun codegen` | ✅ `graphql.ts` +53/−0 — audited (§3); re-run idempotent | **0** |
| 3 | `bun run scripts/health/sub-loop.ts frontend/graphql/sharedDocuments/notifications/notification.documents.ts --lifecycle duplicates` | ✅ all five checks passed — **zero fix iterations** | **0** |
| 4 | `bun run scripts/health/sub-loop.ts frontend/graphql/sharedDocuments/notifications/index.ts --lifecycle duplicates` | ✅ all five checks passed — zero fix iterations | **0** |
| 5 | `bun run scripts/health/sub-loop.ts frontend/graphql/sharedDocuments/index.ts --lifecycle duplicates` | ✅ all five checks passed — zero fix iterations | **0** |
| 6 | `bun run scripts/health/sub-loop.ts frontend/graphql/sharedDocuments/notifications/notification.documents.test.ts --lifecycle duplicates` | ✅ all five checks passed — after **3 honest fix iterations** (§5 QL log) | **0** |
| 7 | `bun run test/scripts/run-test.ts frontend/graphql/sharedDocuments/notifications/notification.documents.test.ts` — RUN 1 / RUN 2 | ✅ **12 pass / 0 fail / 59 expects — identical both runs** (determinism proof) | **0** |
| 8 | `bun run test/scripts/run-test.ts frontend/graphql/sharedDocuments/documents.contract.test.ts` | ✅ **11 pass / 0 fail / 53 expects** — suite UNTOUCHED (byte-identical file), stays green at the exact baseline | **0** |
| 9 | `bun run test/scripts/run-test.ts backend/graphql/test/sdl-static-assertions.test.ts` / `schema-surface.test.ts` | ✅ 15/0/55 · 27/0/147 (baselines hold — SDL artifact + built schema both still pinned) | **0** |
| 10 | `bun run test/scripts/run-test.ts backend/db/test` | ✅ **56 pass / 0 fail / 222 expects** (baseline holds) | **0** |
| 11 | `bun run test/scripts/run-test.ts backend/services/notifications` | ✅ **83 pass / 0 fail / 684 expects** (baseline holds) | **0** |
| 12 | `bun tsgo` (full project — the 4.1.TE codegen type-binding compile gate) | ✅ **EXIT 0 — ZERO errors** (the new documents + test compile against the freshly generated types; every `TypedDocumentNode<…>` binding resolves) | **0** |
| 13 | `bun biome:check` (full repo) | ✅ **Checked 560 files, no fixes applied** (557 + 3 new: documents, sub-barrel, test — see D-3) | **0** |
| 14 | `curl -s http://localhost:3000/api/health` | ✅ HTTP 200 (`status: ok` + requestId) throughout — dev server untouched, zero schema source edits, zero HMR risk this session | n/a |
| 15 | SEC greps (§6) | ✅ identity-arg grep ZERO in the documents file; `/core` import grep ZERO; `useLazyQuery` ZERO in code | n/a |
| 16 | `git status` change-set audit | ✅ exactly: modified `graphql.ts` (generated), modified `sharedDocuments/index.ts` (+1 line), new `notifications/` directory (documents + sub-barrel + test) — nothing else; both generated artifacts commit in the SAME change set (Rule 8.5) | n/a |

## 5. QL — Quality Loop Log (4.1.QL) + TE Detail

**Sub-loop `--lifecycle duplicates` per touched file (tsgo → oxlint → biome:check → lint:type-aware → check:duplicates):**

| File | Iterations | Notes |
|---|---|---|
| `notifications/notification.documents.ts` | 0 | all five checks green first pass |
| `notifications/index.ts` | 0 | green first pass |
| `sharedDocuments/index.ts` | 0 | green first pass |
| `notifications/notification.documents.test.ts` | **3** | (1) runtime: graphql-js types `FieldNode.arguments` as optional — the no-arg `myUnreadNotificationCount` root field had `arguments: undefined` → `(field.arguments ?? [])` guard (mirrors the sibling suite's optional-`variableDefinitions` pattern); (2) tsgo: `selectionPath` needed first-segment unrolling for `FieldNode` return narrowing (TS2322), and `expect(<enum member>).toBe("string")` hit bun:test overload inference (TS2769); (3) oxlint: `no-unnecessary-type-conversion` rejected `String(enumMember)` → Record-widened `toEqual` over the whole enum map, and `import-x/no-duplicates` rejected the value+type double import → single mixed import with inline `type` modifiers (repo precedent: `backend/ws/notification-ws-server.ts`). |

**TE (4.1.TE):**
- **Codegen type-binding compile gate:** full-project `bun tsgo` EXIT 0 — the four `TypedDocumentNode<Query|Mutation, Variables>` annotations bind against the generated types (this is also compile-pinned inside the suite by the assignment-proof test).
- **Document-shape snapshot assertions:** the new 12-test suite (§1) — operation name/type + variables per document, `id` (first) + the exact eight-field set in EVERY `Notification` selection, wrapper/scalar-leaf shapes, barrel identity, codegen enum wire names.
- **Determinism:** RUN 1 = RUN 2 = 12/0/59 (row 7).

## 6. SEC — Security & Tenancy Audit (4.1.SEC)

- **Documents request ONLY self-scoped operations:** the total declared variable surface across the four documents is EXACTLY `filter` / `id` / `type` — test-pinned by set equality, plus the belt-and-braces `not.toContain("user")` sweep. There is NO identity variable anywhere: no `userId`, no recipient argument, no session argument — identity is derived server-side from the authenticated caller (the 3.2/3.3 `authScopes: { authenticated: true }` resolvers). Grep corroboration: `userId|user_id|recipientId|accountId|callerId` over `sharedDocuments/notifications/` → ZERO matches in code (the single hit is the test docblock describing this very audit).
- **No argument literal injection:** every root-field argument in every document is a VARIABLE (test-pinned by the argument-wiring assertion) — no inline literal values that could smuggle parameters past the variable contract.
- **BOPLA read-side hygiene:** the `Notification` selections carry exactly the eight public REQ-060 fields — `userId` is structurally absent from the object (pinned on the schema side by sdl-static-assertions/schema-surface, re-run green).
- **No scope widening possible from the client:** the documents only reference the four authenticated-only operations; the anonymous allowlist (`public-operations.ts`) is untouched this change set (`git status` shows no gateway files).
- **Enum safety:** the codegen `NotificationType` (wire names) is the ONLY enum the frontend touches — the suite pins its members, so a future accidental import of the backend snake_case enum produces a visible mismatch at this tier.

## 7. SR — Semantic Review (4.1.SR) — ~20-item checklist

1. ✅ `gql` + `TypedDocumentNode` imported from `@apollo/client` — NEVER `/core` (grep: zero).
2. ✅ Codegen types imported from `@/frontend/graphql/generated/gql/graphql` ONLY.
3. ✅ No inline type literals — all four constants annotated by generated operation types.
4. ✅ No mapping layers / conversion functions anywhere in the change set.
5. ✅ No indexed-access workarounds (no `NonNullable<X["y"]>`) — extracted compact types (`MyNotificationsQuery_myNotifications_items`, `MarkNotificationReadMutation_markNotificationRead`) are the sanctioned nested-field types.
6. ✅ No schema-level object types imported (no bare `Notification` / `NotificationListPage` type imports — only operation-derived families).
7. ✅ Document const naming `{entityName}QueryDocument` / `{entityName}MutationDocument` exactly per the convention table.
8. ✅ Operation names PascalCase matching the codegen type families (`MyNotifications` → `MyNotificationsQuery`).
9. ✅ No-arg query omits the second type parameter (`TypedDocumentNode<MyUnreadNotificationCountQuery>`).
10. ✅ `id` in EVERY `Notification` selection, FIRST — test-pinned for both `myNotifications.items` and the `markNotificationRead` payload.
11. ✅ Full-row selections (all eight REQ-060 fields) so cache entries stay complete across the feed query and the mark-one mutation (same shape ⇒ clean merge).
12. ✅ No fragments — inline selections; the codegen emitted exactly the compact extracted types the 3.4 BINDING predicted.
13. ✅ Zero dead variables — every declared variable is threaded into its root-field argument (test-pinned).
14. ✅ Barrel structure per convention: sub-barrel `notifications/index.ts`, top-level re-export added, alphabetical order preserved (auth → notifications → teachers).
15. ✅ Barrel ≡ deep-import identity for all four constants (test-pinned `toBe` — cache-key safety).
16. ✅ Zero `console.*` in the change set.
17. ✅ Domain-only JSDoc with conventions citations (no task/plan references beyond contract citations used by sibling suites).
18. ✅ **Zero dead documents** — every document has a mandated consumer: `myNotificationsQueryDocument` → 4.3 feed `useQuery` + 4.2 reconnect catch-up refetch; `myUnreadNotificationCountQueryDocument` → 4.3 badge read + 4.2 catch-up; `markNotificationReadMutationDocument` → 4.3 per-row mark-read; `markAllNotificationsReadMutationDocument` → 4.3 MarkAllButton.
19. ✅ **Hooks discipline preserved (documented note):** the documents layer imports NO hooks (grep `@apollo/client/react` in `notification.documents.ts` → zero); consumers (4.2/4.3/4.4) MUST import hooks from `@apollo/client/react` and use stateful `useQuery` ONLY — `useLazyQuery` remains banned repo-wide (grep: zero code matches).
20. ✅ Generated artifacts regenerated AND committed in the SAME change set (gateway Rule 8.5 / REQ-061) — row 16.
21. ✅ The test suite follows the sibling `documents.contract.test.ts` conventions: table-driven contract rows, AST kind-guard narrowing (zero `as` casts), fail-fast throws over vacuous passes, deterministic `toSorted`+`localeCompare` ordering.

## 8. IV — Instruction Verification (4.1.IV)

| Instruction | Verdict | Evidence |
|---|---|---|
| tasks.md 4.1 bullet 1 — create `notification.documents.ts` with the four named constants as `gql` `TypedDocumentNode`s from `@apollo/client` (never `/core`); `id` in EVERY `Notification` selection; codegen types from `graphql` only | ✅ | §1; SR #1–#11; suite describe 2 |
| tasks.md 4.1 bullet 2 — create sub-barrel `index.ts`; verify top-level barrel re-export path; NO inline literals, NO mapping layers, NO indexed-access workarounds | ✅ | §1 (sub-barrel + the added top-level line — D-1 documents the plan-vs-actual reconciliation); SR #3–#6, #14–#15 |
| tasks.md 4.1 bullet 3 — applicable instructions: `frontend/graphql/AGENTS.md` / shared-documents conventions, REQ-062 | ✅ | §8 rows below + §7 checklist |
| REQ-062 — `gql` + `TypedDocumentNode` from `@apollo/client` with `id` in every object selection; hooks from `@apollo/client/react` only and `useQuery` stateful only (NO `useLazyQuery`); consumers use codegen types directly (no inline literals / mapping layers / indexed-access workarounds) | ✅ | Documents satisfy every clause (SR #1–#6, #9–#10); the hooks clauses are consumer-layer obligations — preserved by leaving the documents layer hook-free and documented for 4.2/4.3/4.4 (SR #19); codegen types consumed directly throughout |
| `frontend/graphql/AGENTS.md` — domain file `<domain>.documents.ts` in `sharedDocuments/`; `TypedDocumentNode<{EntityName}Query, {EntityName}QueryVariables>`; `id` in all query selections; regenerate after document changes; documents-contract tier | ✅ | File placement/naming exact; typing pattern exact (no-arg omission per convention); regeneration ran (rows 1–2); contract tier extended via the co-located domain suite (D-2) |
| `frontend/graphql/sharedDocuments/AGENTS.md` — barrel pattern (sub-dir `index.ts` + top-level re-export); import pattern (`@apollo/client`, never `/core`); TypedDocumentNode convention table; codegen after document edits; `id` Field Requirement; no mapping/indexed-access/schema types; no `useLazyQuery`; embedded-types list (`NotificationListPage` `keyFields:false` already registered by 3.1) | ✅ | All honored (§7); the Layout tree already lists the `notifications/` sub-directory so the "document new sub-directory" rule is satisfied without an AGENTS.md edit — the per-file domain-list entry (`notification`) is deferred to a future docs-propagation leg per the dev2-004 7.2 precedent (carry-forward) |
| Gateway Rule 8.5 / REQ-061 — codegen in the same commit | ✅ | Rows 1–2 + row 16 (single change set contains documents + both generated artifacts) |
| Deferred-items ledger | ✅ untouched | No new ruling needed (D1–D4/A1–A5 do not intersect a frontend-documents task; the barrel-line reconciliation follows the standing dev2-004 DISP-1 precedent) |

## 9. Deviations

- **D-1 (plan.md's barrel claim was stale — line added):** plan.md §5.4 asserted "top-level barrel already re-exports `notifications/`"; the actual `sharedDocuments/index.ts` contained only `auth` + `teachers`. Added `export * from "./notifications";` — the exact dev2-004 DISP-1 reconciliation (their plan made the same stale claim for `teachers/`). tasks.md's own wording ("verify top-level barrel re-export path") anticipated verification, and verification required the edit.
- **D-2 (new co-located suite instead of extending `documents.contract.test.ts`):** tasks.md 4.1.TE says "document-shape snapshot assertions" without naming a file; my instruction cited the documents-contract suite (11/0/53) as the conventions/placement model. Implemented as a NEW co-located domain suite (`notifications/notification.documents.test.ts`) following the 3.4 D-2 precedent (new assertion suite, existing green suite left UNTOUCHED): the sibling suite stays byte-identical at exactly 11/0/53 (strongest "stays green" evidence), failure locality is clean, and the notification domain gets its own structural lock. The dev2-004 4.1 precedent also left the contract suite untouched when authoring documents.
- **D-3 (biome counter 557 → 560, not the predicted 559):** the 3.4 BINDING predicted +2 (documents + sub-barrel); 4.1.TE's mandated test file is the third new file (+1). The task brief's "~559" wording absorbs this; recorded so the next counter prediction starts from 560.
- **D-4 (codegen `ID`-scalar typing note, not a change):** `MarkNotificationReadMutationVariables.id` is typed `string | number` (the codegen default for the `ID` scalar). The repo wire convention remains STRING ids (3.3/3.4 BINDING); consumers pass strings — the union is the generator's default, not an invitation to send numbers. Documented in §2 rather than worked around (a scalars-map override for `ID` would be a repo-wide codegen config change, out of scope).

## 10. Carry-Forward Knowledge

- **For 4.2/4.3/4.4:** the §2 BINDING is the consumption contract — exact document constant names, exact generated type names/shapes (incl. the nullable `filter`, the no-arg count query, `id: string | number` → pass STRINGS, `type` nullable), the codegen-`NotificationType`-only rule, and the hooks discipline note.
- **For 4.3:** `useQuery(myNotificationsQueryDocument, { variables: { filter } })` — build `filter` as `MyNotificationsFilterInput` from local chip/pagination state; the wrapper is `keyFields:false` so Apollo stores it per-query — refetch/evict after mark-all (bare `Int` return); per-row mark-one updates the normalized `Notification:{id}` entry automatically when the mutation selection includes `id` (it does — test-pinned).
- **Docs propagation (non-blocking):** the `sharedDocuments/AGENTS.md` Layout row for `notifications/` pre-exists but its domain list (`notification-alerts, notification-preferences`) does not yet name `notification` (this inbox feed) — a future docs-propagation leg may append it (dev2-004 7.2 precedent for `applicant.documents`).
- **Counter updates for the next gate:** biome 557 → **560** (+3); NEW suite `frontend/graphql/sharedDocuments/notifications/notification.documents.test.ts` **12/0/59**; documents-contract stays **11/0/53** (untouched); all other baselines unchanged (DB 56/0/222; notifications services 83/0/684; sdl-static-assertions 15/0/55; schema-surface 27/0/147; query 12/0/135; mutation 13/0/99; WS 23/0/77; enum 25/0/293; workflows 33/0/238; allowlist-coverage 8/0/42; static-assertions 17/0/20; public-operations 26/0/52); plan-owned totals 271/1,868 → **283 tests / 1,927 expects** (+12/+59).
- **Test-runner note:** `run-test.ts` routes `frontend/graphql/test/**` paths to the server-boot tier (which kills its port) — co-located sharedDocuments suites run through the direct bun-test tier with the bunfig preloads (test-runner-guard + graphql-interop), which is why the new suite executes cleanly via `bun run test/scripts/run-test.ts frontend/graphql/sharedDocuments/…` (dev2-004 precedent).
