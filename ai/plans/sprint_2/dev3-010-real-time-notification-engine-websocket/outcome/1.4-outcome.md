# Task 1.4 Outcome — NEW `notifications` UI Namespace (Full 5-Step Registration)

**Task ID:** 1.4 — Register the `notifications` UI namespace per `shared/locale/AGENTS.md`
**Plan:** DEV3-010 — Real-Time Notification Engine (WebSocket)
**Plan directory (ACTUAL):** `ai/plans/sprint_2/dev3-010-real-time-notification-engine-websocket/`
**Date:** 2026-08-29
**Agent:** Task-1.4-Executor (i18n notifications namespace)
**Requirements:** REQ-052, REQ-002

---

## Summary

Registered the new `notifications` UI namespace through the full 5-step procedure (types interface → `en` + `ar` leaves → `Translations`/MessageSchema entry + message-bundle composition → namespace handle via `defineNamespace` → registry registration), carrying the complete notification-feed surface: **25 keys** — feed title, empty state (title+body), load-error state (title+body), filter labels (`all`/`unread`), display labels for **all 7 `NotificationType` values**, mark-read/mark-all labels + confirm copy, per-row mark-read aria template, pluralized mark-all result, badge aria label, **pluralized `unreadCount: (count: number) => string` function** (Arabic zero/singular/dual/3–10/11+ branches), realtime toast template, and quiet reconnect affordance copy (reconnecting + silently-recovered). **6 new files + 4 modified files, all inside `shared/locale/**`** — zero frontend/backend/app files touched (consumers are Phase 4's Tasks 4.3/4.4). All gates green: sub-loop ×10 exit 0, `bun tsgo` exit 0 (parity compile gate), new parity suite **63 pass / 0 fail / 310 expects — deterministic across 2 canonical-runner runs**, sibling locale suites (applicant 11/0, errors 8/0) re-run green, full-repo biome 512 files clean.

## The namespace handle (BINDING for downstream consumers — Tasks 4.3/4.4)

Per plan-review-R1 **A4** + deferred-items disposition A4: there is NO `Translation` enum — the real convention is the exported **namespace handle const**. The handle:

```ts
// exported from shared/locale/namespaces/notifications/notifications.namespace.ts
// (re-exported through @/shared/locale and @/shared/locale/namespaces barrels)
export const Notifications = defineNamespace<NotificationsLabels>(
  "notifications.notifications",
  translations => translations.notificationsTranslations
);
```

**Consumer paths (property access only — never `t('…')`):**

```ts
// client components:
import { Notifications, useAppTranslation } from "@/shared/locale";
const t = useAppTranslation(Notifications);
t.title; t.unreadCount(3); t.realtimeToast(t.typeSessionRequest, row.title);

// server page shell:
import { getTranslations } from "@/shared/locale/server";
const { notificationsTranslations: t } = getTranslations(locale); // t.title …
```

The `useAppTranslation(Notifications)` consumption path was **type-probe-verified**: a scratch consumer touching every key (incl. all 4 function slots) compiled clean under `bun tsgo` (exit 0, 0 errors), then the probe was deleted.

## Files touched (10 code files — the mandated registration set, additive only)

| # | File | Change |
|---|---|---|
| 1 | `shared/locale/types/notifications/index.ts` | **NEW** — `NotificationsLabels` interface, 25 keys, per-key domain JSDoc (no REQ/task/plan refs) |
| 2 | `shared/locale/en/notifications/index.ts` | **NEW** — `notificationsEn: NotificationsLabels` (full English copy) |
| 3 | `shared/locale/ar/notifications/index.ts` | **NEW** — `notificationsAr: NotificationsLabels` (full natural Arabic copy — zero English fallthrough) |
| 4 | `shared/locale/types/message.ts` | **MODIFIED** (+2) — `import type { NotificationsLabels }` + `notificationsTranslations: NotificationsLabels` on `Translations` (the MessageSchema) |
| 5 | `shared/locale/en/messages.ts` | **MODIFIED** (+2) — `notificationsTranslations: notificationsEn` composition |
| 6 | `shared/locale/ar/messages.ts` | **MODIFIED** (+2) — `notificationsTranslations: notificationsAr` composition |
| 7 | `shared/locale/namespaces/notifications/notifications.namespace.ts` | **NEW** — `Notifications = defineNamespace<NotificationsLabels>("notifications.notifications", …)` |
| 8 | `shared/locale/namespaces/notifications/index.ts` | **NEW** — barrel (`export * from "./notifications.namespace"`) |
| 9 | `shared/locale/namespaces/index.ts` | **MODIFIED** (+3) — import + `export * from "./notifications"` + `Notifications` entry in the `namespaces` const (alphabetical: between `Landing` and `Recitation`) |
| 10 | `shared/locale/notifications-namespace.parity.test.ts` | **NEW** — parity + pluralization + registry suite (63 tests / 310 expects) |

Docs: this file, `tasks.md` (1.4 checkbox flips only), `worklog.md` (Task 1.4 entry).
**Zero drift:** `git diff --name-only -- backend/db/schema backend/db/migration` → EMPTY (REQ-048 invariant holds). Tree note: `backend/lib/env.ts` appeared modified mid-task — that is the **parallel Task 1.5 executor's** file, not touched here.

## The 5-step procedure — executed verbatim (1.4.IV)

Procedure source: `shared/locale/AGENTS.md` (read fully — it documents the compile-time system, path-alias discipline, leaf purity) + `shared/AGENTS.md` §"Namespace Registration" (the 5-step list) + §"Pluralization Pattern". Codebase ground truth was verified against the most recent complete registration precedent (`applicant`, dev2-004 — same file set, same wiring) before each step (1.3's lesson: plan/doc claims verified against code).

| Step | Doc instruction | Executed as (codebase ground truth) |
|---|---|---|
| 1 | Add interface to `shared/locale/types/<namespace>/index.ts` | ✅ `NotificationsLabels` in `shared/locale/types/notifications/index.ts`. NOTE: NOT re-exported from `types/index.ts` — the barrel holds only `common`/`dashboard`/`landing`/`message`; `auth`/`errors`/`recitation`/`applicant` types are NOT in it either (verified) — deep import `@/shared/locale/types/notifications` is the established consumer path. |
| 2 | Add implementations to `shared/locale/en/<ns>/index.ts` + `shared/locale/ar/<ns>/index.ts` | ✅ `notificationsEn` + `notificationsAr`, both typed `NotificationsLabels` (this typing IS the compile-time parity gate — a missing key in either locale fails `bun tsgo`). |
| 3 | Export in `shared/locale/types/message.ts` (add to MessageSchema) | ✅ `notificationsTranslations: NotificationsLabels` on `Translations` + composition in `en/messages.ts` + `ar/messages.ts` (the real "MessageSchema entry" mechanism — both leaf consts flow into the composed bundle). |
| 4 | Add path mapping in `shared/locale/serverLegacy.ts` (`namespacePaths` map) | ⚠️ **Stale doc → resolved as n/a with the real mechanism**: no `serverLegacy.ts` and no `namespacePaths` map exist anywhere (grep-verified; same drift class as plan-review A5's `scripts/run-test/`). `server.ts`'s `getTranslations(locale)` returns the FULL composed `Translations` bundle, so step 3's composition makes the namespace resolvable server-side automatically — no per-namespace path mapping exists to edit. The REAL "namespace-path registration" in the codebase is the handle (step 5). |
| 5 | If used in layout SSR, add to `LocaleProvider` translations in `app/[locale]/layout.tsx` | ⚠️ **n/a**: `frontend/providers/LocaleProvider.tsx` takes only a `locale` prop (no translations list — stale doc); the notifications namespace is NOT used by the root layout. Executed instead per the AGENTS.md "Adding New Namespaces" (browser translation cache) rule + A4 disposition: `defineNamespace` with the stable `<ns>.<ns>` id in `namespaces/notifications/notifications.namespace.ts` + registration in `namespaces/index.ts` (`export * from "./notifications"` + `Notifications` in the `namespaces` const). |

`defineNamespace("notifications", …)` from plan §2.4(b) shipped as the id-registered form **`defineNamespace("notifications.notifications", …)`** — the `<ns>.<ns>` stable-id convention every existing namespace uses (`dashboard.dashboard`, `errors.errors`, `applicant.applicant`; pinned by the applicant parity suite and now this suite). The plan's single-token form is shorthand, not the convention.

## Content coverage (REQ-052, plan §2.4(b), tasks.md 1.4 Step 1)

| Mandated content | Keys |
|---|---|
| feed title | `title` |
| empty state | `emptyTitle`, `emptyBody` |
| error state | `loadErrorTitle`, `loadErrorBody` |
| filter labels | `filterAll`, `filterUnread` |
| type labels ×7 | `typeSessionRequest` (`session_request`), `typeSessionCompletion`, `typeSessionCancellation`, `typeParentLinkRequest`, `typeSystemBroadcast`, `typePaymentConfirmation`, `typeEvaluationResult` |
| mark-read / mark-all labels + confirm copy | `markRead`, `markAllRead`, `markAllConfirmTitle`, `markAllConfirmBody` |
| badge aria label | `badgeAriaLabel` (bell button accessible name: "Open notifications" / "فتح الإشعارات" — action-oriented so it is not a word-for-word near-duplicate of `dashboard.notifications`) |
| pluralized unread-count function | `unreadCount: (count: number) => string` |
| realtime toast template | `realtimeToast: (typeLabel, notificationTitle) => string` |
| quiet reconnect affordance copy | `reconnecting`, `reconnectedQuietly` (REQ-052 "WS state copy — reconnecting/failed silently-recovered") |

**Two plan-§5.4/§5.5-driven additions** beyond the tasks.md key list, so Phase 4 never has to reopen this namespace (documented, not silent scope creep): `markReadAriaLabel: (notificationTitle: string) => string` (plan §5.4 accessibility: "per-row mark-read buttons carry translated aria-label with row title context") and `markAllResult: (count: number) => string` (plan §5.5 visual matrix: "Mark-all success → affected count surfaced via localized snackbar" — second pluralization function).

### Pluralization precedent followed exactly

Repo search for function-valued keys found NO existing `(count: number) => string` in the locale tree (only `(userName|feature|title: string) => string` in dashboard/common) — the binding plural-function precedent is `shared/AGENTS.md` §"Pluralization Pattern" (native TS functions, Arabic zero/singular/dual/3–10-few/11+-counted branches). Both count functions follow it branch-for-branch:

- **en** `unreadCount`: 0 → "No unread notifications" · 1 → "1 unread notification" · n → `${n} unread notifications`
- **ar** `unreadCount`: 0 → "لا توجد إشعارات غير مقروءة" · 1 → "إشعار واحد غير مقروء" · 2 → "إشعاران غير مقروءان" · 3–10 → `${n} إشعارات غير مقروءة` · 11+ → `${n} إشعاراً غير مقروءاً`
- `markAllResult` mirrors the same branch structure ("تم تحديد … كمقروء" templates).

### Near-duplicate search (mandated pre-check)

`grep` for "all caught up|Mark all as read|unreadCount|Reconnecting" repo-wide → hits ONLY inside the new `shared/locale/{types,en,ar}/notifications/` files + the new parity test. Pre-existing notification-flavored keys audited and left untouched:
- `dashboard.notifications` ("Notifications"/"الإشعارات") — sidebar nav item + stat-card label for the DASHBOARD surface (1.3 already ruled: "UI-facing feed copy stays in Task 1.4's separate `notifications` namespace"). The new `notifications.title` is the FEED PAGE title mandated by REQ-052 — same word, different owning surface (same accepted pattern as `dashboard.title` vs `common.title`).
- `errors.notificationNotFound` — transport error copy, errors side owns it (Task 1.3, untouched).
- `common.connectionRestored`/`serverConnectionLost` — the Apollo HTTP-provider connectivity surface; the new `reconnecting`/`reconnectedQuietly` are the WS-sidecar quiet-reconnect affordances mandated by REQ-052 (distinct copy: "Realtime notifications restored" / "تم استعادة الإشعارات الفورية").

## Verification outputs

### 1. Quality Loop (1.4.QL) — `bun run scripts/health/sub-loop.ts <file> --lifecycle duplicates` (exit 0 each, true exit codes captured)

| File | Result |
|---|---|
| `shared/locale/types/notifications/index.ts` | ✅ exit 0 (tsgo → oxlint → biome:check → lint:type-aware → check:duplicates) |
| `shared/locale/en/notifications/index.ts` | ✅ exit 0 |
| `shared/locale/ar/notifications/index.ts` | ✅ exit 0 |
| `shared/locale/namespaces/notifications/notifications.namespace.ts` | ✅ exit 0 |
| `shared/locale/namespaces/notifications/index.ts` | ✅ exit 0 |
| `shared/locale/types/message.ts` | ✅ exit 0 |
| `shared/locale/en/messages.ts` | ✅ exit 0 |
| `shared/locale/ar/messages.ts` | ✅ exit 0 |
| `shared/locale/namespaces/index.ts` | ✅ exit 0 |
| `shared/locale/notifications-namespace.parity.test.ts` | ✅ exit 0 (check:duplicates = the sub-loop's sanctioned `*.test.ts` scope-skip, per 1.2 precedent) |

**QL iteration record (honesty note):** the FIRST run on the parity test failed oxlint `typescript(no-unsafe-type-assertion)` on a `functionSlotOf` helper's `as (...args: never[]) => string` narrowing cast. Fixed by removing the helper (the function-slot tests now assert `typeof Reflect.get(...)` directly — zero casts anywhere in the suite); re-run green, and the determinism runs below were executed on the final version.

### 2. MessageSchema parity compile gate (1.4.TE) — `bun tsgo`

**Exit 0, zero `error TS` lines** (full-project build). Both leaf consts are typed `NotificationsLabels`, so any missing key in either locale fails compilation — parity proven at compile time. Run a second time WITH the `useAppTranslation(Notifications)` scratch consumer (touching all 25 keys incl. the 4 function slots): also exit 0, 0 errors → handle consumption typechecks; probe then deleted (tsgo re-run after deletion: exit 0).

### 3. Runtime parity suite — `shared/locale/notifications-namespace.parity.test.ts` via the canonical runner (twice for determinism)

| Run | Result | Exit |
|---|---|---|
| RUN 1 `bun run test/scripts/run-test.ts shared/locale/notifications-namespace.parity.test.ts` | **63 pass, 0 fail**, 310 expect() calls, 1 file | **0** |
| RUN 2 (identical command) | **63 pass, 0 fail**, 310 expect() calls — byte-identical counts | **0** |

Suite structure (mirrors `applicant-namespace.parity.test.ts`, extended for function-valued slots):

| describe | Coverage |
|---|---|
| compile-time parity mirror | identical sorted key sets en↔ar; every value is a non-empty string OR a function (symmetric sweeps both directions); every one of the 25 mandated keys exists on BOTH maps (`test.each`); the mandated inventory is exhaustive (no silent key minting) |
| type-label coverage | exactly SEVEN `type*` keys (asserted against the pinned 7-key list = one per `NotificationType` value, mapping documented in-file); each non-empty in BOTH locales |
| no English fallthrough | every ar STRING slot matches `/[\u0600-\u06FF]/` (Arabic script present); all 4 ar FUNCTION slots return Arabic-script output for Arabic-flavored args |
| pluralization pin — `unreadCount` | exact outputs at 0/1/2/3/10/11/42 in BOTH locales (Arabic zero/singular/dual/few/counted boundaries + English boundaries) + digit-substring interpolation checks |
| pluralization pin — `markAllResult` | exact outputs at 0/1/2/3/11 in BOTH locales |
| template pins | `markReadAriaLabel` + `realtimeToast` exact outputs embedding their arguments in BOTH locales; all 4 function slots callable with non-empty output in BOTH locales |
| registry + bundle wiring | `namespaces.Notifications` exists; `Notifications.id === "notifications.notifications"`; handle getter returns the composed bundle slice by reference (both locales); `notificationsTranslations` on BOTH bundles; bundle slice IS the leaf map (no re-wrapper) |
| function-slot inventory | exactly the 4 function keys are function-valued on BOTH maps; no other slot is function-valued (string/function split stable) |

**Sibling-suite regression proof (same runner):** `applicant-namespace.parity.test.ts` → 11 pass / 0 fail / 96 expects, exit 0; `errors-namespace.parity.test.ts` → 8 pass / 0 fail / 128 expects, exit 0. Both untouched by this task and still green with the new namespace composed into the bundles.

### 4. Full-repo regression probes

- `bun biome:check` → **Checked 512 files**, no fixes applied, exit 0 (baseline 504 + 1.1's `.test-d.ts` + 1.2's `.enum.test.ts` + this task's 6 new files = 512 — accounts exactly).
- Runtime probe via the public barrel `@/shared/locale`: `Notifications.id === "notifications.notifications"`; en/ar bundle slices resolve; `unreadCount(5)`/`unreadCount(11)` branch correctly in both locales; toasts compose; **key sets en↔ar identical, 25 keys**.

### 5. Grep gate — zero hardcoded user-facing strings (1.4.SR)

`git status --porcelain` (task's own footprint): ONLY the 10 `shared/locale/**` files above (+ docs). No consumer file, no other namespace, no hardcoded fallback copy added anywhere. Repo-wide grep for the new copy finds it ONLY in the two leaves + the parity test. No `next-intl`, no `getBackendTranslations`, no `shared/messages` references (compile-time system only). Arabic copy present for every key (compile parity + the suite's Arabic-script sweep + non-empty sweeps).

## Semantic Review (1.4.SR)

- ✅ **Zero hardcoded user-facing strings** — all copy lives in the two locale leaves; this task adds no UI consumers (Phase 4 owns them).
- ✅ **Enum-handle consumption verified** — the `useAppTranslation(Notifications)` path type-probe-compiled clean (all 25 keys, all 4 functions); consumers use the handle + property access, never string namespaces (A4 disposition honored — NO `Translation` enum created).
- ✅ **Arabic copy present for every key** — 25/25 with Arabic script, no English fallthrough (suite-enforced, not just reviewed).
- ✅ **Import purity** — every file inside `shared/locale/**` imports only via `@/shared/locale/...` aliases (path-alias discipline); zero imports from frontend/backend/app; the parity test imports only shared locale modules + `bun:test` (no backend enum import — the 7-type mapping is a documented test fixture, mirroring 1.2's `CANONICAL_VALUES` approach).
- ✅ **Leaf purity** — leaves hold only string literals + the four sanctioned locale functions (pluralization/interpolation per the AGENTS.md patterns; "no logic" rule interpreted exactly as the dashboard precedent's `welcome`/`comingSoonBody` function slots — pure template functions, no branching beyond locale plural rules).
- ✅ **No near-duplicate keys** — mandated grep recorded above; dashboard/errors/common pre-existing keys reused untouched, each owning its own surface.
- ✅ **Additive-only atomicity** — 4 modified files took only import lines + one property line each; zero deletions anywhere.

## Deviations & Carry-forward

1. **D-1 — plural-function precedent is doc-defined, not code-defined:** no `(count: number) => string` existed in the locale tree; `shared/AGENTS.md` §"Pluralization Pattern" is the binding precedent and was followed branch-for-branch (its doc examples use untyped params `(count) =>`; the codebase leaf style uses explicit annotations `(count: number) =>` — the codebase style won, matching `dashboardEn`).
2. **D-2 — stale procedure steps 4/5** (`serverLegacy.ts`/`namespacePaths`, layout `LocaleProvider` translations): documented in the IV table; the real mechanisms (message-bundle composition + `<ns>.<ns>` id'd `defineNamespace` handle + `namespaces` registry) executed instead. Same drift class as plan-review A4/A5 — recorded so the next namespace registration doesn't re-derive it.
3. **D-3 — `defineNamespace("notifications", …)` plan shorthand vs `"notifications.notifications"` convention:** codebase convention won (pinned by this suite + the applicant suite).
4. **D-4 — two keys beyond the tasks.md list** (`markReadAriaLabel`, `markAllResult`): plan §5.4/§5.5-mandated consumer copy, added now so Tasks 4.3/4.4 never reopen the namespace; documented above.
5. **Carry-forward for Tasks 4.3/4.4:** consume `Notifications` from `@/shared/locale` (already re-exported through the public barrel — verified at runtime); badge aria uses `t.unreadCount(count)`; per-row mark-read buttons use `t.markReadAriaLabel(row.title)`; mark-all success snackbar uses `t.markAllResult(affectedCount)`; the retry button next to `loadErrorBody` should reuse the existing `common.retry` key (deliberately NOT duplicated here).
6. No deferred items touched (ledger is orchestrator-owned; D1–D4 do not intersect this task).

**Baseline delta: ZERO new errors/warnings across all probes (sub-loop ×10 green, tsgo 0 errors ×2, biome 512 files clean, new suite 63/0 twice, sibling suites green). Schema/migration drift: none.**
