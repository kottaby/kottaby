# Task 4.3 Outcome — `/notifications` Feed Page (+ BF/BS Browser Verification)

**Task (4.3a):** Implement `app/(dashboard)/notifications/page.tsx` (Server Component, auth-only `withPageAuth` guard, `generateMetadata` via the notifications title template) delegating to the client view tree `frontend/views/notifications/**` — `NotificationsFeedContainer` (list + count queries, filter/pagination local state, mark-one write-back, type-aware mark-all with pluralized affected-count snackbar), `NotificationFilterChips` (read toggle + 7 single-select type chips, mobile collapsed rail), `NotificationList` / `NotificationSkeletonList` / `NotificationEmptyState`, `NotificationRow` (TEXT-node-only content, unread tint/dot/bold posture, RTL-logical order), `MarkAllButton` (confirm dialog), `NotificationFeedError` (`extensions.code` branching), `notification-type-presentation` (compiler-exhaustive per-type label/icon tables) — plus the component suite `notifications-feed.test.tsx` (24 tests × both locales) and `notifications-static-scan.test.ts` (REQ-028 `dangerouslySetInnerHTML` scan), +3 common-locale keys. Committed in `7ea4355` (part 1).

**Task (4.3b — this document):** Agent-browser FUNCTIONAL self-loop (BF) + VISUAL/STYLING self-loop (BS) over the live topology (dev `:3000` + `WS_HOST=localhost bun run ws` sidecar `:3001` + Redis), ONE BF defect found and fixed (mark-all stale-cache hygiene), regression gates, checkbox flips, and evidence consolidation.

**⚠ HONEST SESSION NOTE:** the 4.3b loop was executed across **TWO agent sessions** — the first ran the BS matrix + found/fixed the defect (13 screenshots + the +63-line container change left on disk uncommitted) but hit its turn limit before bookkeeping; the RESUMED session (this document) re-verified every key BF flow live against the fixed code, re-ran all gates, and consolidated the evidence from both sessions.

**Verdict:** GREEN — all BF flows verified live (anonymous redirect, login + populated feed, realtime arrival WITHOUT refetch, mark-one restyle + count decrement, filter chips over the wire, mark-all dialog → affected-count snackbar, pagination round-trip, error drill + retry recovery); the ONE found defect (stale-unread view after mark-all) is FIXED and re-verified live; the 13-PNG BS matrix is green; zero regression anywhere (tsgo 0 errors, biome 574 files exit 0, component tier 81/0, DB 56/0/222, notifications 83/0/684, dev `:3000` healthy).

---

## 1. What Was Built (4.3a — implementation summary)

- **`app/(dashboard)/notifications/page.tsx` (NEW)** — Server Component shell (REQ-063a): auth-ONLY `withPageAuth({ redirectTo: "/notifications" })` guard (every role has an inbox — REQ-065; no role restriction), `generateMetadata` through `notificationsTranslations.documentTitleTemplate`, delegation to the client container. Anonymous `GET /notifications` streams 200 + embedded `NEXT_REDIRECT;replace;/login?redirect=%2Fnotifications;307` (byte-identical posture to every existing guarded page — asserted in the BF loop via the final browser URL).
- **`frontend/views/notifications/NotificationsFeedContainer.tsx` (NEW)** — the inbox surface (REQ-063b): `useAppTranslation(Notifications)` + `(Common)`; `useQuery(myNotificationsQueryDocument, { variables: { filter } })` + `useQuery(myUnreadNotificationCountQueryDocument, { pollInterval: 120_000 })` (count-only polling = the silent-degradation floor); filter (read-state + type) and pagination in LOCAL React state with a memoized filter object (stable variables identity); mark-one = mutation write-back through the mutation selection (row restyles with ZERO refetch) + `ROOT_QUERY.myUnreadNotificationCount` cache modify −1 (the same modifier the realtime hook bumps); mark-all = bare-`Int` mutation + `refetchQueries [visible list variant + count]` `awaitRefetchQueries:true` + localized pluralized affected-count snackbar anchored at `bottom {xs:188, sm:196}` (clear of BOTH existing toast stacks — error 16/24, realtime 96/104); retry via `listQuery.refetch()`; skeleton → error → empty → list+pager early-return branches.
  - **4.3b defect fix (this task, §3 flow 7 + §4 defect log):** +63 lines — `dropStaleInboxWindows(cache, activeFilter)` runs after a successful mark-all sweep and DROPS every OTHER cached `myNotifications` window (all other filter combos/pages, the realtime hook's `filter:null` page) by returning `undefined` from a `ROOT_QUERY.myNotifications` modifier; the ACTIVE window is spared (it was just refetched by `refetchQueries`). Rationale: the mark-all mutation returns a bare `Int`, so nothing restyles the normalized rows of the sweep — without the drop, a later switch back to a stale window re-renders already-swept rows as unread, contradicting the refetched unread-count header. Helpers `isRecord` + `isInboxWindowForFilter` parse the store-field-name argument segment defensively (unparseable/foreign names never match).
- **`NotificationFilterChips.tsx` (NEW)** — read toggle (All/Unread) + one chip per `NotificationType` (single-select, `aria-pressed`), enum-keyed `Record` accessors, `FilterListOutlined` collapsed affordance on xs (`aria-expanded`), chip `minHeight 36` for Arabic glyphs.
- **`NotificationList.tsx` (NEW)** — semantic `ul` (`aria-label` = title, `aria-busy`) + **`NotificationSkeletonList`** (`component="output"`, `aria-busy`, 5 stable module-level keys, geometry mirrors final rows) + **`NotificationEmptyState`** (centered bell-in-tinted-circle, no actions).
- **`NotificationRow.tsx` (NEW)** — icon→content→action DOM order mirrors under RTL; unread posture = `theme.palette.action.selected` tint + 8px primary dot (`aria-label=filterUnread`) + bold (700) title; read rows un-tinted, no dot, no action; content as TEXT nodes through `Typography` ONLY (REQ-028); type chip + locale-aware `<time>` stamp (`formatApplicantDate`); sm+ inline secondary button / xs ≥44px icon button, both carrying the translated `markReadAriaLabel(title)`.
- **`MarkAllButton.tsx` (NEW)** — translated confirm dialog (`markAllConfirmTitle`/`markAllConfirmBody`, cancel affordance); sm+ inline, xs full-width; confirm reuses the `markAllRead` wording.
- **`NotificationFeedError.tsx` (NEW)** — branches on `extractErrorCode` ONLY: `UNAUTHORIZED`/`FORBIDDEN` → `PermissionDeniedFallback`; `RATE_LIMITED`/`SERVICE_UNAVAILABLE` → `RetryableNotice`; else localized `loadErrorTitle`/`loadErrorBody` Alert + retry.
- **`notification-type-presentation.ts` (NEW)** — `Record<NotificationType, …>` label-accessor + icon + chip-order tables, compiler-enforced exhaustive over all 7 types.
- **`index.ts` (NEW)** — barrel.
- **Locale:** +3 common-namespace keys (`previousPage`/`nextPage` for the pager, `filters` for the mobile rail) — types interface + en + ar (compile-typed parity).
- **Tests:** `test/ui/components/notifications-feed.test.tsx` — 24 tests (12 × both locales) on `MockedProvider` + shared `TestWrapper` locale + translation-driven matchers ONLY (populated feed/aria/pluralized summary/locale-aware stamps/RTL DOM-order, empty state, unread + type filter chips over the WIRE, mark-one restyle + count decrement without refetch, mark-all confirm → sweep → snackbar + converged rows, skeletons, FORBIDDEN/RATE_LIMITED/generic error surfaces, retry recovery, offset pagination + filter-change reset, realtime cache-merge consumption path on a real `ApolloClient`/`MockLink`/production cache); `notifications-static-scan.test.tsx` — REQ-028 static scan over `frontend/views/notifications/**` (fails on any `dangerouslySetInnerHTML`).

## 2. BINDING for 4.4 — browser-loop runbook + consumption contract

- **Browse at `http://localhost:3000`** + **`WS_HOST=localhost bun run ws`** sidecar (4.2b D-1 unchanged: `127.0.0.1` origin is hydration-dead under Next 16 dev-origin check). Redis `:6379` must be live (cross-process fan-out). Login fixtures: `admin@app.local`/`adminpassword123` (userId 1) or `student@draftacademy.local` (userId 4 — the seeded student; tasks.md's "student fixture").
- **Cache inspection seam:** `window.__APOLLO_CLIENT__.cache.extract().ROOT_QUERY` — `myUnreadNotificationCount` and every `myNotifications({"filter":{…}})` variant are directly readable; network refetch-freedom via `agent-browser network requests --clear` then `--filter graphql`.
- **Live-push harness pattern:** `scripts/tmp-bf-emit.ts <userId> <type> <title> [body]` (ephemeral, recreated per browser-loop task, NEVER committed, deleted at loop end — this session's copy is deleted; 4.4 re-creates an equivalent emitter if it wants live pushes).
- **Snackbar anchor contract (unchanged):** error host 16/24, realtime toasts 96/104, mark-all success 188/196 — 4.4's badge surfaces add NOTHING to these stacks.
- **Consumption contract (4.1/4.2 BINDING, held):** documents from `@/frontend/graphql/sharedDocuments` only; codegen `NotificationType` (wire names); string ids; count query NO variables; hooks from `@apollo/client/react` (`useQuery` stateful only, zero `useLazyQuery`); the feed does NOT mount `useNotificationRealtime` (DashboardLayout owns the tab's socket — REQ-067); badge reads the count cache the hook + feed co-maintain.
- **NEW (4.3b defect-fix behavior 4.4 must know):** a mark-all sweep now DROPS every non-active `myNotifications` window from the cache — any surface observing a dropped window (nav away + SPA revisit, filter switch-back) refetches fresh instead of serving a pre-sweep snapshot. The count field is always refetched by the sweep, so a 4.4 badge converges immediately after a sweep.

## 3. BF — Agent-Browser Functional Self-Loop (4.3.BF)

**Topology:** dev server `:3000` (never restarted) + `WS_HOST=localhost` sidecar `:3001` (Redis transport) + agent-browser (named session, headless Chromium). Session A (interrupted): student fixture per tasks.md; sidecar registry log shows its browsing (29 registered connections, userId 4) + its emits persisted in the DB (`BF flow 3 live arrival`, `BF live arrival 4.3b` + retry, `BS fixture: …` ×3 typed rows for matrix variety). Session B (resumed): admin fixture; every flow below re-verified live on the FIXED code with fresh evidence.

| # | Flow | Verdict | Evidence (resumed session, unless noted) |
|---|---|---|---|
| 1 | **Anonymous `GET /notifications` → login redirect** | ✅ GREEN | Final browser URL `http://localhost:3000/login?redirect=%2Fnotifications` (asserted TWICE — initial anonymous open + post-browser-restart open; server streams 200 + embedded NEXT_REDIRECT, the standard guarded-page posture) |
| 2 | **Login → feed renders rows** | ✅ GREEN | admin@app.local login → lands on `/notifications`; 20 rows render; header `Notifications / 42 unread notifications` (pluralized); cache `myUnreadNotificationCount` = 42; one list variant `myNotifications({"filter":{"isRead":null,"limit":20,"offset":0,"type":null}})` |
| 3 | **Realtime arrival WITHOUT refetch** | ✅ GREEN | `bun run scripts/tmp-bf-emit.ts 1 system_broadcast …` (emitted id **5025**) with the network log CLEARED pre-emit → row `BF43b live arrival check` prepended at feed head; cache count 42→43; cached list head `Notification:5025`; second emit → toast `New notification — System Announcement: BF43b toast probe`; **ZERO `/api/graphql` requests during both arrivals** (network log: "No requests captured") — the DashboardLayout socket's cache merge served the feed |
| 4 | **Mark-one transitions row + count decrements** | ✅ GREEN | Click `Mark as read: BF43b toast probe` → row bgcolor `rgba(255,255,255,0.16)` (action.selected) → `rgba(0,0,0,0)`, unread dot gone, mark-read affordance gone (read rows render no action); cache count 44→43; **exactly ONE graphql POST** (the mutation — no query refetch) |
| 5 | **Filter chips over the wire** | ✅ GREEN | Unread chip → re-query POST body `operationName:"MyNotifications"`, `variables:{"filter":{"isRead":false,"type":null,"limit":20,"offset":0}}`; all 20 rendered rows unread-dotted (the read row excluded); + System Announcement type chip → `{"filter":{"isRead":false,"type":"SystemBroadcast","limit":20,"offset":0}}`; all rows System Announcement; `aria-pressed` toggles correct (All=false, Unread=true, type=true) |
| 6 | **Mark-all: confirm dialog → affected-count snackbar** | ✅ GREEN | MARK ALL AS READ → dialog (`Mark all as read?` / `All of your unread notifications will be marked as read.` / CANCEL + MARK ALL AS READ) → confirm → snackbar **`43 notifications marked as read`** (pluralized affected count = all unread SystemBroadcast for admin); count 43→**0**; header `No unread notifications`; 3 POSTs (mutation + refetchQueries [active list variant + count]) |
| 7 | **Defect-fix re-verification (stale-unread view)** | ✅ GREEN | Post-sweep cache held ONLY the active variant (the 2 other cached windows DROPPED — pre-sweep cache listed 3 variants); switching back to previously-cached windows (All+SystemBroadcast, then pure All) each fired a FRESH `MyNotifications` POST and rendered **0 unread dots** across all rows — consistent with the `No unread notifications` header. WITHOUT the fix these windows would have served pre-sweep snapshots rendering swept rows as unread (the defect captured in `defect-stale-unread-view-desktop-en.png`, session A) |
| 8 | **Error drill → retry recovers** | ✅ GREEN | Routed `/api/graphql` to a mocked GraphQL-errors response, opened an uncached window (Payment Confirmation filter) → feed error surface `Couldn't load notifications / Something went wrong while loading your notifications. Please try again.` + RETRY; unroute + RETRY → error surface gone, query recovered (empty state for that filter — correct: admin has no Payment Confirmation rows). Along the way the app-level connectivity gate (`Server connection lost / RETRY`) was observed when ALL graphql was hard-aborted — that is the app-wide offline surface (existing `AppApolloProvider` behavior), NOT the feed error branch; the feed-level drill used the non-aborting errors-response route |
| 9 | **Pagination round-trip** | ✅ GREEN | 44 notifications → pager `1 / 3` (PREVIOUS disabled) → NEXT → `2 / 3`, 20 fresh rows (first row = row 21, `BF missed while down`), a new `offset:20` cache variant; PREVIOUS → back to `1 / 3` with the cached page-1 head instantly |

**BF iteration log (honest):** ONE functional defect found across both sessions — **BF-D1, the mark-all stale-cache hygiene gap** (flow 7): found by session A (stale-unread screenshot 16:13:32), fixed with the +63-line `dropStaleInboxWindows` container change (16:13:49), re-verified by session A (fixed screenshot 16:15:43) AND re-verified live end-to-end by the resumed session (flow 7 above: variant-count diff + fresh-POST assertions + zero unread dots on switch-back). Zero other defects: every other flow passed first-try on the shipped 4.3a code.

## 4. BS — Agent-Browser Visual & Styling Self-Loop (4.3.BS)

**Method (honest, 4.2b D-2 precedent):** screenshots at 1440×900 / 768×1024 / 375×812 × en/ar (+ mobile filters-expanded variants + state captures), validated by session A through DOM geometry/computed-style auditing (the executing agents have no image-vision channel); PNGs committed as evidence in `outcome/screenshots-4.3/`. The full matrix was captured on the FIXED code (all timestamps 16:19–16:24, after the 16:13:49 fix); the one pre-fix capture is the defect evidence.

**Matrix (13 PNGs):**

| # | File | Combo / purpose | Verdict |
|---|---|---|---|
| 1 | `feed-desktop-en.png` | 1440×900 en — populated feed, inline filter chips row, pager | ✅ |
| 2 | `feed-desktop-ar.png` | 1440×900 ar — RTL mirroring (icon→content→action order, logical properties) | ✅ |
| 3 | `feed-tablet-en.png` | 768×1024 en — wrapped chips at the tablet breakpoint | ✅ |
| 4 | `feed-tablet-ar.png` | 768×1024 ar — wrapped chips RTL | ✅ |
| 5 | `feed-mobile-en.png` | 375×812 en — collapsed `FilterListOutlined` filter rail | ✅ |
| 6 | `feed-mobile-ar.png` | 375×812 ar — collapsed rail RTL | ✅ |
| 7 | `feed-mobile-en-filters-expanded.png` | 375×812 en — expanded mobile filter rail (`aria-expanded` affordance) | ✅ |
| 8 | `feed-mobile-ar-filters-expanded.png` | 375×812 ar — expanded rail RTL | ✅ |
| 9 | `empty-state-desktop-en.png` | Empty state — centered bell-in-tinted-circle, no actions | ✅ |
| 10 | `error-state-desktop-en.png` | Error surface — localized Alert + retry affordance | ✅ |
| 11 | `skeleton-desktop-en.png` | Loading skeletons — geometry mirrors final rows, `aria-busy` | ✅ |
| 12 | `defect-stale-unread-view-desktop-en.png` | **DEFECT pre-fix** — already-swept rows rendering unread (stale cache window) | evidence |
| 13 | `defect-fixed-unread-view-desktop-en.png` | **DEFECT post-fix** — same view: swept rows render read (fresh window) | ✅ |

- **Palette compliance:** unread tint = `theme.palette.action.selected` (live-computed `rgba(255,255,255,0.16)` dark mode), unread dot = `theme.palette.primary.main`, type-icon medallion = primaryContainer/onPrimaryContainer, all text via `text.primary`/`text.secondary`; source-level ZERO hex/rgb/color-name literals across `frontend/views/notifications/**` (grep, re-confirmed this session).
- **RTL/LTR:** plain flex rows + logical properties mirror under `dir=rtl`; chips wrap at 768; mobile rail collapses at 375; Arabic chip `minHeight 36` prevents glyph clipping (4.3a design, matrix-confirmed).
- **Typography hierarchy:** h5/700 title, subtitle1 h2 rows (700 unread / 500 read), body2 body copy, caption stamps — all `Typography` TEXT nodes (REQ-028).

**BS defect log:**

- **BS-D1 (FOUND + FIXED + RE-VERIFIED, session A; = BF-D1 above):** after a mark-all sweep, every OTHER cached `myNotifications` window kept its pre-sweep snapshot — switching back to such a window re-rendered already-swept rows with the unread tint/dot/bold posture, contradicting the refetched `No unread notifications` header (evidence: `defect-stale-unread-view-desktop-en.png`). **Fix:** `dropStaleInboxWindows` (§1) — sx-free pure cache logic, +63 lines in `NotificationsFeedContainer.tsx` only, zero other files touched. **Post-fix:** `defect-fixed-unread-view-desktop-en.png` + the resumed session's live re-verification (§3 flow 7: variant-drop observable in `cache.extract()`, fresh POST per switch-back, 0 unread dots). Component tier unaffected (81/0 re-run).
- **BS-D2..n:** none — no clipping, no centering drift, no RTL defects, no palette violations detected across the matrix.

## 5. Verification Commands & Exit Codes (4.3b gates)

| # | Command | Result | Exit |
|---|---|---|---|
| 1 | `bun run scripts/health/sub-loop.ts frontend/views/notifications/NotificationsFeedContainer.tsx --lifecycle duplicates` (re-run AFTER the defect-fix edit) | ✅ all checks passed (tsgo / oxlint / biome:check / lint:type-aware / check:duplicates) | **0** |
| 2 | `bun tsgo` (full project) | ✅ EXIT 0 — ZERO errors | **0** |
| 3 | `bun biome:check` | ✅ Checked **575** files no-fixes during the loop (574 committed baseline + the on-disk ephemeral `scripts/tmp-bf-emit.ts`; 574 restored after the harness deletion — screenshots are not lint targets) | **0** |
| 4 | `bun run test:ui:components` | ✅ **81 pass / 0 fail** across 8 files (420 expects) — WITH the defect-fix container change | **0** |
| 5 | `bun run test/scripts/run-test.ts backend/db/test` | ✅ **56 pass / 0 fail / 222 expects** (baseline holds) | **0** |
| 6 | `bun run test/scripts/run-test.ts backend/services/notifications` | ✅ **83 pass / 0 fail / 684 expects** (baseline holds) | **0** |
| 7 | `curl -s http://localhost:3000/api/health` | ✅ HTTP 200 (`status: ok`) — dev server untouched throughout (no restart, no kill) | n/a |
| 8 | Sidecar lifecycle | ✅ `WS_HOST=localhost bun run ws` running throughout (registry log `/tmp/ws-sidecar.log`); KILLED at session end (`pkill -f start-notification-ws`) | n/a |
| 9 | `git status` change-set audit | ✅ exactly: modified `NotificationsFeedContainer.tsx` (+63 defect fix), new `outcome/screenshots-4.3/` (13 PNGs), new `outcome/4.3-outcome.md`, `tasks.md` 4.3 flips, `worklog.md` append; `scripts/tmp-bf-emit.ts` DELETED before commit | n/a |

**4.3a gates (from the worklog, commit `7ea4355`):** component suite 24/0 both locales; full tier 81/0 ×3 deterministic (expect-count variance 422/420 — conditional `waitFor` polling, the 2.2/2.3/4.2a precedent); tsgo EXIT 0; biome 574 files exit 0; DB 56/0/222; notifications 83/0/684; documents 12/0/59; notifications-namespace parity 63/0/310; health 200; curl smoke of the anonymous redirect posture. **Consolidated test matrix for 4.3:** component tier 81/0 (incl. the 24 feed + 2 static-scan) + BF live evidence (§3) + BS evidence (§4).

## 6. SR — Semantic Review (~20-item checklist, 4.3a groundwork + 4.3b re-confirmation)

1. ✅ Zero `console.*` in `frontend/views/notifications/**` + the page shell (grep — re-confirmed this session; logger only).
2. ✅ Zero hex/rgb/color-name literals in source (grep — re-confirmed).
3. ✅ Zero `dangerouslySetInnerHTML` (grep + `notifications-static-scan.test.tsx` test-pinned, recursive incl. comments).
4. ✅ All styling `sx`-only with theme callbacks (`theme => ({…})`); spacing/radius/shadow/zIndex theme tokens.
5. ✅ Icons `*Outlined` exclusively across the tree (CampaignOutlined, CancelOutlined, CheckCircleOutlined, DoneAllOutlined, DoneOutlined, FamilyRestroomOutlined, FilterListOutlined, GradeOutlined, NotificationsOutlined, PaymentsOutlined, RefreshOutlined, ScheduleOutlined) + `Close` for the snackbar dismiss (the exact GraphQLErrorSurfaceHost convention).
6. ✅ Translation access = enum-handle property access only: `useAppTranslation(Notifications)`/`(Common)` + `NOTIFICATION_TYPE_LABEL_ACCESSORS` — never call-by-key (compiler-typed labels interfaces).
7. ✅ Content rendered as TEXT nodes through `Typography` only (REQ-028) — row title/body, chip labels, stamps.
8. ✅ Unread posture via theme tokens (`action.selected` tint + primary dot + weight 700), not ad-hoc colors.
9. ✅ Documents imported from `@/frontend/graphql/sharedDocuments` barrel ONLY.
10. ✅ Codegen `NotificationType` (wire names) only; enum-keyed `Record` tables compiler-enforced exhaustive over all 7 types.
11. ✅ Hooks from `@apollo/client/react` (`useQuery` stateful, `useMutation`, `useApolloClient`); ZERO `useLazyQuery`; `ApolloCache`/`ModifierDetails` are type-only imports.
12. ✅ Count query consumed with NO variables object (polling 120s — count-only posture).
13. ✅ Feed does NOT mount `useNotificationRealtime` — consumes the hook-maintained cache (REQ-067 single-socket ownership preserved; live-verified: arrivals land with zero refetch).
14. ✅ Filter/pagination state in local React state — no store, no URL coupling; memoized filter object (stable variables identity).
15. ✅ Mark-one is NOT a realtime event — mutation write-back + cached count decrement, live-verified exactly one POST.
16. ✅ Mark-all snackbar anchored 188/196 — clear of the error (16/24) and realtime (96/104) stacks; `pointerEvents` gate on the fixed wrapper.
17. ✅ Accessibility: `aria-busy` list region, translated per-row action aria-labels (`markReadAriaLabel(title)`), chip `aria-pressed`, mobile rail `aria-expanded`, semantic `ul`/`li`, `component="output"` skeletons, `PermissionDeniedFallback`-family error surface, ≥44px touch targets.
18. ✅ Error branching on `extractErrorCode` ONLY (never HTTP status) — UNAUTHORIZED/FORBIDDEN / RATE_LIMITED/SERVICE_UNAVAILABLE / generic classes.
19. ✅ Server shell: auth-only guard (all roles — REQ-065), `generateMetadata` via the title template, delegation to client container (no data fetching in the Server Component).
20. ✅ The 4.3b defect fix touched ONLY `NotificationsFeedContainer.tsx` (+63 lines, pure cache hygiene + docblocks) — no other file changed (diff audit); no visual/styling change.
21. ✅ RTL logical properties throughout (`insetInlineStart/End` on the snackbar wrapper; plain flex rows mirror natively).
22. ✅ `logger.debug` (not console) on mutation/retry rejections — observability without local banners; the global error surface owns the UX.

## 7. IV — Instruction Verification

| Instruction | Verdict | Evidence |
|---|---|---|
| tasks.md 4.3 bullet 1 — server shell: `withPageAuth`-class authenticated-all-roles guard, anonymous → `/login`; `getTranslations`; delegates to client container | ✅ | §1; live §3 flow 1 (final URL `/login?redirect=%2Fnotifications`) |
| tasks.md 4.3 bullet 2 — container + chips/list/row/mark-all/empty/skeleton/error; `useAppTranslation(Notifications)`; list + count queries (polling posture); filter + pagination local state; per-row mark-read + mark-all with affected-count snackbar | ✅ | §1; live §3 flows 2–9 |
| tasks.md 4.3 bullet 3 — MUI v9 discipline: sx-only, theme-palette callbacks, `*Outlined`, logical RTL properties, unread rows via `action.selected` tokens | ✅ | §6 items 2–8, 21; BS matrix §4 |
| tasks.md 4.3 bullet 4 — TEXT nodes via `Typography`; `dangerouslySetInnerHTML` PROHIBITED + static scan | ✅ | §6 items 3, 7; `notifications-static-scan.test.tsx` |
| tasks.md 4.3 bullet 5 — accessibility: `aria-busy`, translated per-row action aria-labels, PermissionDeniedFallback-family error surface | ✅ | §6 item 17; live error drill §3 flow 8 |
| Applicable instructions: `frontend/AGENTS.md`, `frontend/views/AGENTS.md`, `frontend/components/ui/AGENTS.md` (+ auto-discovered `frontend.instructions.md`, `mobile-desktop.instructions.md`) | ✅ | §6 (logger-not-console, sx/theme-palette, Outlined icons, hooks from `@apollo/client/react`, 120s count-poll convention, view layers never import backend modules — page-size constant kept local) |
| tasks.md 4.3.QL — sub-loop `--lifecycle duplicates` each file | ✅ | 4.3a: 14 files exit 0 (3 fix iterations logged); 4.3b re-run on the EDITED container: exit 0 (§5 #1) |
| tasks.md 4.3.TE — MockedProvider suite, translation-driven matchers only, both locales, static scan green | ✅ | 24 + 2 tests; tier 81/0 ×3 (4.3a) + 81/0 re-run post-fix (4.3b) |
| tasks.md 4.3.BF — anonymous redirect, student-fixture login, empty→emit→row, mark-one, badge decrement, filter chips over the wire, mark-all snackbar, error drill, iterative self-loop | ✅ | §3 flows 1–9 (badge = count cache field — decremented live; the badge UI itself is 4.4 scope); 1 defect found→fixed→re-verified |
| tasks.md 4.3.BS — 6-combo matrix + collapsed mobile affordance, RTL mirroring, unread tokens, skeleton geometry, Arabic chip heights, centered empty/error states; iterative patch loop | ✅ | §4 matrix (13 PNGs); BS-D1 found→fixed→re-captured |
| tasks.md 4.3.SR — zero direct style props / hardcoded strings / colors; enum-handle access; `*Outlined`; text-only rendering | ✅ | §6 (22 items) |
| tasks.md 4.3.IV — validate against the instruction files | ✅ | this table + §6 |
| REQ-028 — untrusted emitter copy rendered as text only | ✅ | §6 items 3, 7; static scan test-pinned |
| REQ-063 (b) — inbox surface with filters/pagination/mark-flows | ✅ | §1 + live §3 |
| REQ-065 — every role has an inbox (auth-only guard) | ✅ | §1; verified as admin (session B) + student (session A) |
| REQ-066 — mark-read/mark-all converge the unread count | ✅ | live §3 flows 4 + 6 (44→43→0) |
| REQ-068 — error surfaces keyed on `extensions.code` | ✅ | §6 item 18; live generic-error drill |
| Deferred-items ledger | ✅ untouched | no new ruling needed |

## 8. Deviations

- **D-1 (two-session loop — honest):** the 4.3b BF/BS loop ran across an interrupted agent session and this resumed session. Session A captured the 13 screenshots, found + implemented the defect fix, and left its artifacts uncommitted; the resumed session inspected the disk state (diff + screenshot timeline + sidecar/DB evidence), RE-VERIFIED every key BF flow live against the fixed code with fresh evidence (§3), re-ran all gates (§5), and did the bookkeeping. No 4.3a-authored code was re-authored; only the session-A defect fix is new code, audited line-by-line against all BINDINGs before acceptance.
- **D-2 (BS inspection method):** screenshots validated via programmatic DOM geometry/computed-style auditing, not pixel-eyeballing — the executing agents have no image-vision channel (4.2b D-2 precedent); the audit is strictly more precise for position/palette/direction claims and the PNGs remain committed as evidence.
- **D-3 (defect-fix scope):** the plan's §5.4 wording ("mark-all … refetch the visible page") was implemented plus the cache-hygiene sweep (`dropStaleInboxWindows`) — a live-evidence-motivated refinement that makes the refetch contract complete for ALL windows, not just the visible one. Pure cache logic; sx/styling untouched; component tier green.
- **D-4 (fixtures):** tasks.md names the student fixture for the BF login; session A used it, the resumed session re-verified on the admin fixture (both are all-roles inboxes under the auth-only guard — REQ-065 coverage is not diminished; the resumed session's flows required an inbox with >1 page of rows, which the admin fixture provides).

## 9. Carry-Forward Knowledge

- **For 4.4 (badge + nav):** the app-bar badge reads `myUnreadNotificationCount` via `useQuery` — the cache field is maintained by the realtime hook (+1 per arrival / catch-up), the feed's mark-one (−1), and the mark-all sweep (refetch). After a sweep the count field is network-fresh, and every non-active list window is DROPPED — a badge + any feed re-entry converge without stale snapshots. Badge aria reuses the `unreadCount(count)` namespace pluralization function (AR boundaries 0/1/2/3–10/11+ parity-test-pinned); `markAllResult` pluralization is feed-only. Do NOT lower the three snackbar anchors (§2). Nav entry: "Notifications" under the existing general nav group per role; feed reachable today only by direct URL.
- **BF-loop facts for 4.4:** the `agent-browser network route … --body '{"errors":[…]}'` pattern forces FEED-level GraphQL errors while keeping the app-level connectivity gate green (hard `--abort` of all graphql trips the app-wide offline surface instead — a different, existing mechanism); browser `close` discards the httpOnly cookie (re-login per browser restart); MUI chips are `[role="button"]` divs in the DOM (not `<button>` elements) — target them via role/text, not `button` selectors.
- **Counters (for the next gate):** biome 574 committed (unchanged by 4.3b — the ephemeral harness was deleted); component tier 81/8 files (unchanged); DB 56/0/222; notifications 83/0/684; documents 12/0/59; plan-owned totals 327 tests (81 component + 246 backend-tier plan-owned).
- **Env facts (unchanged):** `WS_ALLOWED_ORIGINS` and `NEXT_PUBLIC_NOTIFICATION_WS_URL` UNSET (defaults correct); Redis `:6379` live; sidecar default `WS_HOST=127.0.0.1` — browser loops MUST use `WS_HOST=localhost`.
