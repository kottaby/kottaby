# Task 3.3 Outcome — Mutation Resolvers

**Task:** Implement `markNotificationRead` + `markAllNotificationsRead` mutation resolvers (thin, self-scoped, authenticated-only) — REQ-019, REQ-020, REQ-030, REQ-031, REQ-032, REQ-060, REQ-061.
**Verdict:** GREEN — both inbox mutations live on the built + served schema with exactly `{ authenticated: true }` scopes; the integration tier **executed for real** against the live dev server (13/0/99 ×2 deterministic, zero DB residue); schema-surface refreshed to the NEW frozen mutation baseline (27/0/147 ×2); zero baseline regression anywhere (tsgo 0 errors, biome 556 files exit 0, all layer suites at baseline counts, allowlist byte-unchanged, dev :3000 healthy throughout).

## 1. What Was Built

- **`backend/graphql/mutation/notifications/notification.mutation.ts`** (NEW) — the two root mutation fields, registered at import time via `gqlSchemaBuilder.mutationField(...)` (side-effect only, NO named exports per `backend/graphql/mutation/AGENTS.md`):
  - `markNotificationRead(id: ID!): Notification!` — `authScopes: { authenticated: true }`; resolver delegates to `NotificationEngine.markRead(ctx.user.id, parseNotificationIdArg(args.id), ctx.locale)`. **ID-channel discipline**: Pothos types the ID arg as `string | number`; `parseNotificationIdArg` routes numbers straight through the shared `isPositiveSafeInt` guard (`backend/services/notifications/emit-validation`) and strings through a bounded canonical wire-form check (`/^[1-9]\d*$/`) + `Number.parseInt` + `isPositiveSafeInt`. Every malformed shape ("12abc", "1.5", "1e3", non-ASCII digits, out-of-range magnitudes) parses to NaN — the value the ENGINE's own pre-DB positive-safe-int gate rejects with the localized ValidationError — so a malformed wire form can never silently address a different row, the single error-construction site stays in the service boundary (plan §3.2: "input hardening at the service boundary"), and there is NO `as number` narrowing anywhere. Both bodies carry the established `if (!ctx.user) throw new UnauthorizedError("Authentication required.")` narrowing guard (the `myApplicantProfile`/3.2 precedent — unreachable TypeScript narrowing only).
  - `markAllNotificationsRead(type: NotificationType): Int!` — same scope; delegates to `NotificationEngine.markAllRead(ctx.user.id, args.type ?? null, ctx.locale)`; the optional enum filter forwards as-is (the engine's enum guard is the defense-in-depth re-validation against non-schema transports).
- **Domain wiring** — `backend/graphql/mutation/notifications/index.ts` (NEW side-effect barrel) → `backend/graphql/mutation/index.ts` (MODIFIED, +`import "./notifications";` + JSDoc mention) → `gqlSchema.ts` step 4. No Pothos object/enum/input TYPE definitions in the mutation tree (both ops take only inline scalar/enum args — the `auth.mutation.ts` precedent; the 3.2 D-1 pothos-tree placement ruling governs input TYPES, and 3.3 introduces none).
- **SDL regenerated + committed** (`frontend/graphql/generated/schema.graphql`): diff contains EXACTLY the two Mutation root operations (`markAllNotificationsRead(type: NotificationType): Int!` + `markNotificationRead(id: ID!): Notification!` with their descriptions). `bun codegen` deliberately deferred to 3.4 (the 3.1 D-5 / 3.2 D-7 precedent).
- **Tests** — schema-surface suite refreshed + extended (22→27 tests; `PRE_3_1_MUTATION_FIELDS` is now the NEW frozen baseline of 6 ops); NEW wire-tier integration suite `backend/graphql/test/notification-mutation.test.ts` (13 tests / 99 expects / 5 describes).

## 2. BINDING — for Tasks 3.4 / 4.x

```typescript
// Mutation resolver consumption pattern (LIVE, this task):
import { NotificationPothosObject } from "@/backend/graphql/pothos/notifications/notification.pothos";
import { NotificationTypePothosEnum } from "@/backend/graphql/pothos/shared/enum.pothos";
import { gqlSchemaBuilder } from "@/backend/graphql/pothos/builder";
import { UnauthorizedError } from "@/backend/lib/errors";
import { isPositiveSafeInt } from "@/backend/services/notifications/emit-validation";
import { NotificationEngine } from "@/backend/services/notifications/notification-engine.service";

// 4.x (frontend): mutation documents —
//   mutation MarkNotificationRead($id: ID!) { markNotificationRead(id: $id) { id … } }
//   mutation MarkAllNotificationsRead($type: NotificationType) { markAllNotificationsRead(type: $type) }
//   ids travel as STRINGS on the wire (GraphQL ID); `type` filter values are the
//   enum KEYS (SystemBroadcast, …); mark-one returns the full Notification row
//   (isRead ALWAYS true after a successful call); mark-all returns Int (0 on
//   empty/repeat sweeps). Apollo v4 mutate results carry `.error` not `.errors`.
```

- **3.4 (schema/codegen)**: the SDL is ALREADY regenerated + committed by THIS task — running `bun run generate:gqlSchema` at 3.4 must produce an EMPTY SDL diff (byte-stability proof); `bun codegen` is the only artifact sync outstanding. Add the REQ-032 CUD-absence static assertion + the four-ops presence assertion — the refreshed `PRE_3_1_MUTATION_FIELDS` (now 6 ops) is the frozen baseline to pin against. Re-verify the allowlist byte-state. Biome counter: 556 now; 3.4's new static-assertion file ⇒ expect 557.
- **3.4 (CUD-absence evidence, pre-wired here)**: grep of the generated SDL for `createNotification|updateNotification|deleteNotification` → ZERO matches (recorded this task; 3.4 formalizes it as a test).
- **4.x (frontend)**: the wire surface is exactly REQ-060 §mutations. `markNotificationRead` selections MUST include `id` (Apollo cache update of the flipped row); `markAllNotificationsRead` returns a plain Int — the client refetches/evicts lists itself (marking read is NOT a realtime event — 2.7 BINDING).
- **Integration-suite run pattern (this sandbox)**: `GRAPHQL_TEST_PORT=3000 ADMIN_EMAIL=admin@app.local ADMIN_PASSWORD=adminpassword123 bun run test/scripts/run-test.ts backend/graphql/test/notification-mutation.test.ts` — the 3.2 D-3/D-6 pattern, reused verbatim (live-server liveness probe → no spawn, nothing killed).
- **HMR heal knowledge (refined)**: this session hit the LATE-onset variant — `PothosSchemaError: Duplicate field markNotificationRead on Mutation` (not the 3.2 duplicate-TYPENAME form) surfaced only after SUBSEQUENT file edits, following an initially-clean re-registration. Heal that worked: content-touch `builder.ts` with a COMMENT line (a bare trailing newline was insufficient), dwell ~12s, probe `POST /api/graphql {"query":"{ _health { status } }"}` until HTTP 200, then `git checkout -- builder.ts` and re-probe. No dev-server restart; probe after EVERY schema-file edit batch, not only after file creation.

## 3. Files Touched

| File | Change |
|---|---|
| `backend/graphql/mutation/notifications/notification.mutation.ts` | **NEW** — `markNotificationRead` + `markAllNotificationsRead` root fields (thin delegation, `{ authenticated: true }` scopes, ID-channel guard) |
| `backend/graphql/mutation/notifications/index.ts` | **NEW** — domain mutation side-effect barrel |
| `backend/graphql/mutation/index.ts` | **MODIFIED** — +`import "./notifications";` + JSDoc mention (only additions) |
| `backend/graphql/test/schema-surface.test.ts` | **MODIFIED** — frozen inventory REFRESHED (`PRE_3_1_MUTATION_FIELDS` += `markNotificationRead`, `markAllNotificationsRead` — the NEW frozen baseline); NEW `describe("Notification mutation surface — self-scoped read latch")` (5 tests: arg shapes/types, exact scope snapshot, anonymous in-process UNAUTHORIZED ×2 ops, smuggled-identity validation deaths ×2 probes); header docs extended; freeze-test renamed to "mutation set is EXACTLY the refreshed frozen baseline" |
| `backend/graphql/test/notification-mutation.test.ts` | **NEW** — wire-tier integration suite, 13 tests / 99 expects (§5) |
| `frontend/graphql/generated/schema.graphql` | **MODIFIED (generated)** — ONLY the two Mutation root operations added |
| `ai/plans/sprint_2/.../outcome/3.3-outcome.md` | This document |
| `ai/plans/sprint_2/.../tasks.md` | 3.3 block checkboxes flipped (6 boxes — only the 3.3 lines) |
| `/home/z/my-project/worklog.md` | Appended Task 3.3 entry |
| `backend/db/schema/**`, `backend/db/migration/**`, `backend/lib/gateway/public-operations.ts`, `backend/lib/env.ts`, `.env*`, `shared/**`, `backend/types/**`, `backend/enum/**`, `backend/services/**`, `test/workflows/**`, `test/helpers/**`, `docs/**`, `app/**`, `backend/graphql/pothos/**` | **ZERO drift** — `git diff` empty on all protected paths; the public-operation allowlist is byte-identical (default-deny preserved); deferred-items ledger NOT touched (no new ruling needed) |

## 4. Verification Commands & Exit Codes

| # | Command | Result | Exit |
|---|---|---|---|
| 1 | `bun run scripts/health/sub-loop.ts backend/graphql/mutation/notifications/notification.mutation.ts --lifecycle duplicates` | ✅ all five checks passed — after 1 honest fix iteration (sonarjs/concise-regex: `[0-9]` → `\d` in the canonical-id regex) | **0** |
| 2 | `bun run scripts/health/sub-loop.ts backend/graphql/mutation/notifications/index.ts` / `backend/graphql/mutation/index.ts` | ✅ all five passed for EACH barrel — ZERO fix iterations (first try) | **0** |
| 3 | `bun run scripts/health/sub-loop.ts backend/graphql/test/schema-surface.test.ts --lifecycle duplicates` | ✅ all five passed — ZERO fix iterations (first try) | **0** |
| 4 | `bun run scripts/health/sub-loop.ts backend/graphql/test/notification-mutation.test.ts --lifecycle duplicates` | ✅ all five passed — after 3 honest fix iterations (oxlint 3× enum-vs-union comparisons → enum-guarded `studentRowBySpec` helper; 1× `no-unnecessary-type-conversion` `String()` removal; 4× `sonarjs/no-alphabetical-sort` → `localeCompare` comparators) | **0** |
| 5 | `bun run test/scripts/run-test.ts backend/graphql/test/schema-surface.test.ts` — RUN 1 / RUN 2 | ✅ **27 pass / 0 fail / 147 expects — identical both runs** (was 22/0/121; +5 tests / +26 expects) | **0** |
| 6 | `bun run test/scripts/run-test.ts backend/graphql/test/notification-mutation.test.ts` (canonical, port 3066) | ⚠️ **0 pass / 2 fail — PRE-EXISTING environment block, not a regression**: fails in `beforeAll` at server boot — "⨯ Another next dev server is already running" (the mandatory :3000 dev server holds Next's dev-dir lock; IDENTICAL to the 3.1/3.2 findings). Zero GraphQL assertions reached | 1 (env) |
| 7 | `GRAPHQL_TEST_PORT=3000 ADMIN_EMAIL=admin@app.local ADMIN_PASSWORD=adminpassword123 bun run test/scripts/run-test.ts backend/graphql/test/notification-mutation.test.ts` — RUN 1 / RUN 2 | ✅ **13 pass / 0 fail / 99 expects — identical both runs** (determinism proof); the lifecycle helper's liveness probe succeeds against the LIVE dev server → no second server spawned, nothing killed | **0** |
| 8 | DB residue probe after EACH integration run (psql) | ✅ `notifications = 0`, `wire-%@test.local` users = 0, total users = 4 (seeded set — admin untouched) | n/a |
| 9 | `GRAPHQL_TEST_PORT=3000 … run-test.ts backend/graphql/test/notification-query.test.ts` (3.2's suite) | ✅ **12 pass / 0 fail / 135 expects** — stays green with the mutations live | **0** |
| 10 | `bun run test/scripts/run-test.ts backend/graphql/test/error-finalizer.test.ts` / `context-keys.test.ts` / `request-id.test.ts` | ✅ 14/0 · 14/0 · 12/0 (baselines hold) | **0** |
| 11 | `KOTTABY_TEST_RUNNER_OK=1 bun --env-file=.env.test test frontend/graphql/test/gateway/allowlist-coverage.test.ts` | ✅ **8 pass / 0 fail / 42 expects** — the two mutations sit in the `scoped` set; the unscoped set remains exactly `["_health", "recitationReadings"]` ≡ allowlist (default-deny holds) | **0** |
| 12 | `bun run test/scripts/run-test.ts backend/lib/gateway/static-assertions.test.ts` / `public-operations.test.ts` | ✅ 17/0 · 26/0 (gate A1 dynamic-import scan + registry closure hold) | **0** |
| 13 | `bun run test/scripts/run-test.ts backend/db/test` | ✅ **56 pass / 0 fail / 222 expects** (baseline holds) | **0** |
| 14 | `bun run test/scripts/run-test.ts backend/services/notifications` | ✅ **83 pass / 0 fail / 684 expects across 7 files** (baseline holds) | **0** |
| 15 | `bun run test/scripts/run-test.ts backend/ws/notification-ws-server.test.ts` / `backend/enum/notifications/notification-type.enum.test.ts` | ✅ 23/0 · 25/0 (baselines hold) | **0** |
| 16 | `KOTTABY_TEST_RUNNER_OK=1 bun --env-file=.env.test test test/workflows` | ✅ 33 pass / 0 fail / 238 expects (baseline holds) | **0** |
| 17 | `bun tsgo` (full project) | ✅ **EXIT 0 — ZERO errors** | **0** |
| 18 | `bun biome:check` (full repo) | ✅ **Checked 556 files, no fixes applied** (553 baseline + 3 new .ts files: resolver + domain barrel + integration suite) | **0** |
| 19 | `curl -s http://localhost:3000/api/health` | ✅ HTTP 200 — dev server untouched throughout (healed in place after the HMR incident, D-4) | n/a |
| 20 | Live introspection probe (`__type(name: "Mutation")`) | ✅ Mutation fields = EXACTLY `login, logout, markAllNotificationsRead, markNotificationRead, refreshToken, registerUser` | n/a |
| 21 | Live anonymous probes (curl, no credentials) | ✅ BOTH ops → `extensions.code = "UNAUTHORIZED"`, message "Authentication required.", correct `path`, `requestId` attached, `data: null` | n/a |
| 22 | Live cookie-path demo: `login` (`-c` cookie jar) → `markAllNotificationsRead` → `markNotificationRead(id: "2000000000")` | ✅ login returns user `{id:1, role:"Admin"}` + sets the `access_token` cookie; mark-all answers `0` over the cookie path (clean inbox — empty set); nonexistent mark-one → `NOTIFICATION_NOT_FOUND` with the localized message "The notification was not found." + requestId | n/a |
| 23 | `git diff backend/lib/gateway/public-operations.ts` | ✅ EMPTY — allowlist byte-identical | n/a |
| 24 | CUD-absence grep on the generated SDL (`createNotification|updateNotification|deleteNotification`) | ✅ ZERO matches (REQ-032 pre-wired; 3.4 formalizes as an assertion) | n/a |

## 5. TE — Test Engineering (3.3.TE)

**Verification path used: BOTH.** The integration tier was ATTEMPTED canonically (row 6 — env-blocked exactly as 3.1/3.2 documented) and then **executed for real** against the live dev server via `GRAPHQL_TEST_PORT=3000` (row 7) — the suite code itself is the canonical `setupTestServerLifecycle` + `testClient` shape and boots its own 3066 server in CI; only the PORT env points it at the already-running dev server in this sandbox (the helper's liveness probe short-circuits the spawn — nothing is spawned OR killed). The unit/schema tier (schema-surface) and live curl probes (rows 20–22) provide the independent corroborating tiers.

**`backend/graphql/test/notification-mutation.test.ts` — 13 tests / 99 expects / 5 describes:**
- **Anonymous tier (3)** — `testClient.mutate` (no credentials) → `expectMutationError(result.error, "UNAUTHORIZED")` on BOTH ops; PLUS a raw-wire constancy test: both ops' anonymous denial items share the SAME code, message, single-item envelope, null data, and extensions key set (each with its own correct `path`).
- **mark-one latch (5)** — happy path: student marks own unread SystemBroadcast row → wire row ≡ post-update direct-DB row on ALL 8 fields (stringified id round-trip `/^\d+$/`, enum NAME on the wire, ISO createdAt) with `is_read` the only flipped column; idempotent double-mark (row returns unchanged, DB byte-identical across both calls); FOREIGN id (parent outsider probing the student's row — the J1-step-8 posture) → `NOTIFICATION_NOT_FOUND`, victim row byte-identical; NONEXISTENT id indistinguishable from foreign (code/message/path/envelope/extensions-keys identical — the REQ-039 oracle-safe constancy pin); 7 invalid-format ids ("not-a-number", "0", "-7", "1.5", "1e3", "12abc", "99999999999999999999") → `VALIDATION` with zero fixture movement (oracle unread count unchanged).
- **mark-all sweeps (4)** — type-filtered sweep (SessionRequest): wire count ≡ direct-DB unread-of-type oracle, only that kind flips, other kinds + the outsider's rows untouched; unfiltered sweep (parent): count ≡ oracle, all rows read, student's rows untouched; empty sets (repeat sweep → 0; type-with-no-rows → 0); final sweep drains the student's inbox, the latch is visible to the QUERY surface (`myUnreadNotificationCount` → 0 over the same wire), and the end-state closure proves: exactly the 7 seeded rows persist, every row read, seeded-read rows byte-identical to their snapshots, and every flipped row differs from its seed by AT MOST the `is_read` latch (rewinding it reproduces the seed byte-for-byte).
- **BOPLA wire probe (1)** — inline-document smuggles (`markNotificationRead(userId: 12345, id: "1")` and `markAllNotificationsRead(userId: 12345)`) die as `GRAPHQL_VALIDATION_FAILED` before any resolver runs.

**`backend/graphql/test/schema-surface.test.ts` — refreshed + extended (22→27 tests):** `PRE_3_1_MUTATION_FIELDS` is now the NEW frozen baseline (6 ops, sorted-pin); the new describe block pins: `markNotificationRead` → `Notification!` with EXACTLY ONE arg `id` typed `ID!` (required + non-null); `markAllNotificationsRead` → `Int!` with EXACTLY ONE arg `type` typed `NotificationType` (optional + nullable); BOTH authScopes snapshots are EXACTLY `{ authenticated: true }` (keys pinned, role/permission/superAdmin structurally absent); anonymous in-process execution (real `graphql()` against the built schema, empty context) → `UNAUTHORIZED` on each op with correct `path`; and the two smuggled-identity validation deaths (`Unknown argument "userId"` on each root op).

**Determinism:** integration 13/0/99 RUN 1 = RUN 2; schema-surface 27/0/147 RUN 1 = RUN 2. Fixture teardown verified by post-run psql residue probes (row 8).

## 6. SEC — Security & Tenancy Audit (3.3.SEC)

- **Zero identity args accepted**: root args are exactly `id` (mark-one) / `type` (mark-all). Pinned THREE ways: structurally (schema-surface arg-set assertions), at validation (unknown-argument documents rejected pre-resolver — unit tier AND wire tier), and behaviorally (the parent outsider can never reach the student's rows — foreign id → `NOTIFICATION_NOT_FOUND`, victim byte-identical; the student's sweeps never move the parent's rows).
- **Oracle-safe denial constancy (REQ-039)**: the foreign and nonexistent branches answer with the IDENTICAL error item — code `NOTIFICATION_NOT_FOUND`, message `toBe`-identical, path, single-error envelope, null data, same extensions key set (pinned in the wire suite); the anonymous class is likewise constant across both ops (same code + message + envelope, per-op paths). Response shapes are constant across every denial branch.
- **BOPLA (REQ-031)**: no input object exists for either op; smuggled fields (`userId`) die as `GRAPHQL_VALIDATION_FAILED` before any resolver runs (wire probe + schema-surface validation probe). The mark-one write path is exactly the repo's `.set({ isRead: true })` literal (REQ-029 latch — unchanged from 2.7).
- **Scope exactly `{ authenticated: true }`** on BOTH operations — pinned against the Pothos extension snapshot (`extensions.pothosOptions.authScopes`): keys exactly `["authenticated"]`, value `true`; `role`/`permission`/`superAdmin` structurally absent (REQ-032's "no role scope" clause).
- **BFLA (REQ-032)**: ZERO emit references anywhere under `backend/graphql/**` (grep: `emitForUser|emitForUsers|emitBatch|publishReceipts|publishFanout` → no matches). ZERO create/update/delete notification operations in the generated SDL (grep → no matches — the CUD-absence verdict, pre-wired for 3.4's formal assertion). `NotificationEngine` is referenced under `backend/graphql/**` ONLY by the two notification resolver files (+ JSDoc mentions in their test files).
- **Default-deny preserved**: `backend/lib/gateway/public-operations.ts` byte-identical (empty `git diff`); the allowlist-coverage gate (8/0/42) confirms the unscoped-fields set ≡ allowlist with both mutations correctly in the scoped set.
- **Anonymous rejection**: UNAUTHORIZED with correct path + requestId — proven at 3 tiers (in-process `graphql()` execution, wire `testClient`, wire curl).
- **No PII/secret exposure**: mark-one returns only the REQ-060 field surface (the recipient `userId` is never GraphQL data — implied by the self-scoped caller); mark-all returns a bare Int.

## 7. SR — Semantic Review (3.3.SR) — 20-item checklist

1. ✅ Thin resolvers: both bodies are single delegation calls (+ the established TS-narrowing guard + the sanctioned bounded ID-channel guard); zero business logic.
2. ✅ Zero repository imports in resolver files (grep `backend/db/repo|Repository` under `backend/graphql/mutation/` → no matches).
3. ✅ No DataLoader (mutations are single-row/set-based writes; nothing to batch).
4. ✅ Input hardening lives at the SERVICE boundary (plan §3.2 / REQ-054): the engine's `isPositiveSafeInt` + enum guards run pre-DB on every call; the resolver adds only the bounded canonical wire-form guard (REQ-060's "validation-free beyond bounded guards").
5. ✅ `ctx.locale` propagated on EVERY engine call (mark-one + mark-all).
6. ✅ No localized copy in the resolvers: the only resolver-thrown error is the unreachable narrowing guard mirroring `builder.ts`'s `authenticated` scope message verbatim (the 3.2 precedent); the engine owns all reachable localized errors (`getServerTranslations(locale)` at the service seam). No resolver in this tree throws a reachable localized error — that is the established convention, and it keeps the ValidationError's construction single-sited.
7. ✅ Identity = `ctx.user.id` exclusively; no arg, header, or input field contributes identity.
8. ✅ `authScopes` exactly `{ authenticated: true }` (test-pinned; see §6).
9. ✅ Enum consumed via the registry import (`NotificationTypePothosEnum` from `shared/enum.pothos`) — no re-registration.
10. ✅ No input types needed: both ops take inline scalar/enum args (the `auth.mutation.ts` precedent); no Pothos type definitions in the mutation tree per `mutation/AGENTS.md`.
11. ✅ The ID-channel guard reuses the engine's OWN exported `isPositiveSafeInt` (leaf-module import `@/backend/services/notifications/emit-validation`, exactly how the engine imports it) — no local re-implementation, no duplicated bound constants.
12. ✅ Top-level static imports ONLY — zero `await import(` anywhere under `backend/graphql/**` (grep; gate A1 green).
13. ✅ Side-effect barrel registration per `mutation/AGENTS.md`: `mutation/notifications/index.ts` → `mutation/index.ts` → `gqlSchema.ts`; the `.mutation.ts` file has NO named exports and is never imported outside its subtree.
14. ✅ JSDoc style: domain-behavior docs only (no REQ/task/phase references), mirroring `notification.query.ts`/`auth.mutation.ts` conventions.
15. ✅ Nullability per REQ-060 SDL: `Notification!` / `Int!` non-null returns; `id: ID!` required + non-null; `type: NotificationType` optional + nullable; TS types align.
16. ✅ `id` present in every test selection; the canonical object exposes `id` FIRST (3.1 pin); wire ids normalize as strings and round-trip against DB ids.
17. ✅ Zero `console.*` in the new production files.
18. ✅ Zero `as` casts in resolver code — the ID parse is guard-driven (`typeof` + regex + `isPositiveSafeInt`), and the enum filter uses `?? null` coalescing only.
19. ✅ Field descriptions present on both root fields (the `login`/`myNotifications` description precedent).
20. ✅ Near-duplicate sweep: exactly ONE registration site for each op (mutationField called once per op, in one file); the schema-surface frozen inventory pins the complete root set (no stray notification-shaped mutation anywhere — `jscpd` duplicates check green on every touched file).

## 8. IV — Instruction Verification (3.3.IV)

| Instruction | Verdict | Evidence |
|---|---|---|
| tasks.md 3.3 verbatim bullets (authScopes `{ authenticated: true }`; `markNotificationRead(id: ID!)` → `markRead(ctx.user.id, id, ctx.locale)`; `markAllNotificationsRead(type: NotificationType)` → `markAllRead(ctx.user.id, type, ctx.locale)` returning Int; ID-channel positive-safe-int guard with no `as number`; domain mutation barrel registration; REQ-019/020/030/031/032/060/061) | ✅ all | §1 maps bullet-to-artifact one-to-one; §5–§7 hold the evidence |
| `backend/graphql/mutation/AGENTS.md` — side-effect imports only, no named exports, sub-directory barrel wiring, resolver delegation to services, "Do not place Pothos object/enum/input type definitions here" | ✅ | `.mutation.ts` registers via `mutationField` with no exports; wired `notifications/index.ts` → `mutation/index.ts`; delegation is exclusively `NotificationEngine`; zero type definitions in the mutation tree |
| `backend/graphql/AGENTS.md` — resolvers delegate to services (never repositories), locale propagation, DomainError subclasses only | ✅ | §7 items 1–6; the only thrown error is `UnauthorizedError` (a DomainError subclass); reachable errors originate in the engine |
| `docs/graphql/domain-error-extensions-code.md` — NotFoundError entity-name convention ("NOTIFICATION", never the full code — double-suffix rule); Pothos authScopes 401-vs-403; `expectMutationError`/`CombinedGraphQLErrors` testing convention | ✅ | The engine throws `NotFoundError(NOTIFICATION_ENTITY, …)` (2.7, unchanged); `authenticated` miss → UNAUTHORIZED (401) verified at 3 tiers; both anonymous-tier tests use `expectMutationError(result.error, "UNAUTHORIZED")` |
| Gateway Rule 8.1 (resolver placement + side-effect barrels, never dynamic-import resolvers) | ✅ | `backend/graphql/mutation/notifications/notification.mutation.ts`; zero `await import(` under `backend/graphql/**` (grep + gate A1 17/0) |
| Gateway Rule 8.4 (authorization declared on EVERY operation; allowlist only for anonymous-by-design) | ✅ | Both fields carry `authScopes: { authenticated: true }`; no allowlist entry added (byte-identical file); allowlist-coverage gate green both directions |
| Gateway Rule 4 (default-deny public-operation allowlist) | ✅ | `git diff public-operations.ts` EMPTY; `public-operations.test.ts` 26/0; both mutations are scoped, hence not in the unscoped audit set |
| REQ-060 exact SDL contract (mutation half) | ✅ | SDL diff adds exactly `markNotificationRead(id: ID!): Notification!` + `markAllNotificationsRead(type: NotificationType): Int!`; schema-surface pins the shapes |
| REQ-061 (placement + NO await import; codegen discipline) | ✅ | Placement per convention; codegen sync scoped to 3.4 per the 3.1/3.2 precedent (SDL committed NOW for the Codegen-sync byte-identity assertion — green, row 5) |

## 9. Deviations

- **D-1 (no input types — inline args per repo rules):** tasks.md §3.3 carries no input-object requirement, and the 3.2 D-1 pothos-tree placement ruling governs input TYPES; 3.3's two ops take only a scalar (`id: ID!`) and an enum (`type: NotificationType`), defined inline via `t.arg(...)` — exactly the `auth.mutation.ts` precedent. Nothing was placed in the pothos tree because no type needed defining.
- **D-2 (ID-channel guard routes malformed forms to the engine's gate):** rather than throwing a resolver-local localized ValidationError (which would need `ctx.t` union-narrowing gymnastics and would create a SECOND construction site for the same error), the resolver's `parseNotificationIdArg` applies the canonical wire-form check + `isPositiveSafeInt` and yields NaN for every malformed shape — the value the engine's own pre-DB gate rejects with the localized ValidationError. This follows plan §3.2 verbatim ("Input hardening at the service boundary … `id` positive safe-int via type guard (no `as number`)") and the 2.7 BINDING; the positive-safe-int guard is applied to the parsed id on every call, there are zero casts, and no resolver in this codebase throws a reachable localized error (verified by grep — only the unreachable `UnauthorizedError` narrowing guards exist).
- **D-3 (integration tier executed via `GRAPHQL_TEST_PORT=3000`):** the canonical 3066 boot is env-locked in this sandbox (row 6 — "Another next dev server is already running", identical to the 3.1/3.2 findings). The suite was run UNCHANGED with `GRAPHQL_TEST_PORT=3000`, which makes `setupTestServerLifecycle`'s liveness probe succeed against the LIVE dev server and skip the spawn entirely. The suite file stays CI-canonical. Real end-to-end coverage delivered (registration → login → mutations → DB oracle equivalence → residue-free teardown).
- **D-4 (late-onset HMR duplicate-FIELD incident — environment knowledge):** after an initially-clean HMR re-registration (live introspection showed all 6 mutation fields, anonymous probes answered UNAUTHORIZED correctly), a LATER turbopack partial re-evaluation — triggered by subsequent test-file edits during the quality loop — produced `PothosSchemaError: Duplicate field markNotificationRead on Mutation` and HTTP 500s on `/api/graphql` (the 3.2 D-4 duplicate-TYPENAME variant, field-flavored). Healed WITHOUT restarting the dev server: content-touch `builder.ts` with a comment line (a bare trailing newline was NOT enough to trigger re-evaluation), dwell ~12s, probe until HTTP 200, then `git checkout -- builder.ts` (byte-identical revert) and re-probe (200 + correct UNAUTHORIZED). Recorded as refined carry-forward knowledge in §2.
- **D-5 (SDL now, codegen at 3.4):** the SDL was regenerated and committed (required for the Codegen-sync byte-identity assertion, green row 5); `bun codegen` deliberately deferred to 3.4 per the 3.1 D-5 / 3.2 D-7 precedent (tasks.md 3.4 owns the full artifact sync).
- **D-6 (biome counter 553 → 556, not the predicted 555):** the 3.2 §10 estimate ("553 → 555") under-counted by omitting the new integration suite file. Actual new files: resolver + domain barrel + `notification-mutation.test.ts` = +3 → 556, all clean.
- **D-7 (anonymous-constancy assertion via raw fetch):** the anonymous-tier UNAUTHORIZED tests use the canonical `expectMutationError` helper; the additional constancy assertions (same message/envelope across ops and across foreign/nonexistent branches) ride raw `fetch` responses, because Apollo v4's `CombinedGraphQLErrors.message` is a joined-message convenience whose formatting is not part of the stable contract. The raw wire is the authoritative shape.

## 10. Carry-Forward Knowledge

- **For 3.4**: expect an EMPTY SDL diff when re-running `generate:gqlSchema` (the SDL is final as of this commit); `bun codegen` is the outstanding artifact sync; add the REQ-032 CUD-absence + four-ops-present static assertions against the refreshed `PRE_3_1_MUTATION_FIELDS` (6 ops); biome counter 556 → 557 with the new assertion file; re-verify the allowlist byte-state; the CUD-absence grep evidence is pre-wired (§6, row 24).
- **For 4.x**: consumption pattern + wire shapes in §2 (`id` in every mark-one selection for Apollo cache updates; mark-all returns a bare Int — refetch/evict lists client-side; marking read is NOT a realtime event per the 2.7 BINDING).
- **ENVIRONMENT — late-onset HMR duplicate-field**: see D-4/§2. Probe `POST /api/graphql {"query":"{ _health { status } }"}` (expect HTTP 200) after EVERY schema-file edit batch — the duplicate-field 500 can surface minutes after the file that caused it, following unrelated edits.
- **ENVIRONMENT — canonical 3066 integration boot**: reproduced the 3.1/3.2 finding for THIS suite too (row 6) — document identically in 3.4 and reuse the `GRAPHQL_TEST_PORT=3000` pattern.
- **ENVIRONMENT — `run-server-tests.ts` ALWAYS spawns AND kills a server on its port**: NEVER route it at port 3000. `frontend/graphql/test/**` paths route through it from `run-test.ts` — run such suites directly via `KOTTABY_TEST_RUNNER_OK=1 bun --env-file=.env.test test <file>` (the allowlist-coverage precedent).
- **Apollo v4 note (inherited)**: `testClient.mutate(...)` results carry `.error` (CombinedGraphQLErrors), NOT `.errors` — use `extractErrorCode`/`expectMutationError`.
- **Counter updates for the next gate**: biome 553 → **556** (+3 files); schema-surface 22 → **27 tests** (27/0/147); NEW integration suite `backend/graphql/test/notification-mutation.test.ts` **13/0/99**; plan-owned totals grow by 18 tests / 125 expects (238/1,688 → 256/1,813).
