# Task 3.4 Outcome — Schema Generation, Codegen & Structural Assertions

**Task:** Run schema/codegen + commit generated artifacts + structural schema assertions — REQ-032, REQ-060, REQ-061, REQ-069. **Phase 3 closer.**
**Verdict:** GREEN — `bun run generate:gqlSchema` reproduces the committed SDL BYTE-IDENTICALLY (0-byte diff — the 3.1–3.3 hand-sync chain closes as a byte-stability proof); `bun codegen` reproduces the committed `graphql.ts` byte-identically (0-byte diff — the honest no-op explained in D-1: codegen is document-driven and notification documents are 4.1's deliverable); the REQ-032 BFLA structural verdict is now a PERMANENT artifact-side test suite (`backend/graphql/test/sdl-static-assertions.test.ts`, 15/0/55 ×2 deterministic); zero baseline regression anywhere (tsgo 0 errors, biome 557 files exit 0, all layer suites at baseline counts, allowlist byte-unchanged, dev :3000 healthy + live introspection 200 throughout).

## 1. What Was Built

- **`bun run generate:gqlSchema` — EMPTY diff proof.** The generator (`scripts/generate-gql-schema.ts`, `printSchema(lexicographicSortSchema(graphQLSchema))` → `frontend/graphql/generated/schema.graphql`) ran at the 3.3-committed tree and rewrote the artifact with ZERO byte drift (`git diff frontend/graphql/generated/schema.graphql` = 0 bytes). This is the exact closing condition the 3.3 BINDING predicted ("SDL is FINAL as of this commit — generate:gqlSchema must yield an EMPTY diff"): the hand-synced emissions of 3.1/3.2/3.3 were already the deterministic output of the current Pothos tree, and the artifact↔builder lockstep is now proven in BOTH directions (schema-surface's in-memory byte-identity test + this run's on-disk regeneration).
- **`bun codegen` — EMPTY diff, honestly reconciled (D-1).** `graphql-codegen --config codegen.ts` regenerates `frontend/graphql/generated/gql/graphql.ts` from `documents: ["./frontend/graphql/sharedDocuments/**/*.ts", "./frontend/views/**/*.documents.ts"]` — the plugin set is `typescript-operations` + `typed-document-node` ONLY (operation-derived types; NO schema-level object types per `frontend/graphql/AGENTS.md`). Zero notification documents exist yet (they are task 4.1's deliverable), so the notification schema additions produce NO codegen delta at 3.4: the run is the byte-stability/no-drift proof, not a growth step. The notification operation types materialize in `graphql.ts` the moment 4.1 lands its documents and re-runs codegen (the exact expected exports are documented in §2 so 4.1 can assert them).
- **`backend/graphql/test/sdl-static-assertions.test.ts` (NEW)** — the static schema assertion suite required by tasks.md §3.4: reads the COMMITTED SDL artifact from disk, parses it with `parse()`, and walks the AST (pure static tier — NO schema build, NO DB, NO server boot, NO disk writes; deliberately distinct from `schema-surface.test.ts`, which owns the built-schema tier and stays untouched at 27/0/147):
  - **BFLA verdict (REQ-032)** — ZERO `createNotification`/`updateNotification`/`deleteNotification` field definitions on ANY object or input type in the artifact; the three forbidden tokens appear NOWHERE in the artifact text (lexical belt-and-braces); the Mutation root is EXACTLY the refreshed frozen 6-op baseline (`PRE_3_1_MUTATION_FIELDS` mirror: auth quartet + the sanctioned read-latch pair); the Query root is EXACTLY the frozen baseline + the `_health` probe.
  - **REQ-060 four-ops contract** — `myNotifications(filter: MyNotificationsFilterInput): NotificationListPage!`, `myUnreadNotificationCount: Int!` (zero args), `markNotificationRead(id: ID!): Notification!`, `markAllNotificationsRead(type: NotificationType): Int!` pinned with EXACT argument names/types + return types (AST-rendered SDL notation); `MyNotificationsFilterInput` carries exactly the four nullable filter fields.
  - **Apollo normalization** — `Notification` carries `id: ID!` among EXACTLY the eight REQ-060 inbox fields.
  - **Depth/complexity posture (REQ-069)** — `Notification` is FLAT (every field references a spec scalar or the `NotificationType` enum — zero object-typed/list-typed fields); NO self-referential or recursive shape (no field references `Notification`/`NotificationListPage`); the ONLY object-typed field in the notification family is the page wrapper's bounded `items: [Notification!]!`; `NotificationListPage` exposes exactly `items`/`totalCount`/`hasMore` with NO `id` (embedded wrapper — the normalizable entities are the rows); and NO `Subscription` root exists anywhere in the artifact (realtime delivery is the WS sidecar's contract, never a GraphQL subscription — plan D8).
- **Public-operation allowlist re-verification** — `git diff backend/lib/gateway/public-operations.ts` EMPTY (byte-identical; default-deny preserved), corroborated by the allowlist-coverage gate (8/0/42 — unscoped set ≡ allowlist).

## 2. BINDING — for Phase 4 (frontend consumption of the codegen output)

```typescript
// EXACT export surface of frontend/graphql/generated/gql/graphql.ts AS OF THIS
// COMMIT (byte-stable; codegen is document-driven so it is unchanged since
// DEV2-004 — notification types appear ONLY after 4.1 lands its documents):
//
//   enums:    ApplicantStatus, Gender, RecitationReading, RegisterPublicRole, UserRole
//   types:    RegisterUserInput; RegisterUserMutation(_registerUser)(Variables);
//             MeQuery(_me)(Variables); LoginMutation(_login, _login_user)(Variables);
//             RefreshTokenMutation(_refreshToken)(Variables); LogoutMutation(_logout)(Variables);
//             RecitationReadingsQuery(Variables); MyApplicantProfileQuery(_myApplicantProfile)(Variables)
//   documents: RegisterUserDocument, MeDocument, LoginDocument, RefreshTokenDocument,
//             LogoutDocument, RecitationReadingsDocument, MyApplicantProfileDocument
//
// 4.1 authors frontend/graphql/sharedDocuments/notifications/notification.documents.ts
// with: myNotificationsQueryDocument, myUnreadNotificationCountQueryDocument,
//       markNotificationReadMutationDocument, markAllNotificationsReadMutationDocument
//   gql + TypedDocumentNode from "@apollo/client" (never /core); `id` in EVERY
//   Notification selection; THEN runs `bun run generate:gqlSchema && bun codegen`
//   in the SAME change set (gateway Rule 8.5), after which graphql.ts gains:
//
//   export enum NotificationType { EvaluationResult, ParentLinkRequest, PaymentConfirmation,
//                                  SessionCancellation, SessionCompletion, SessionRequest,
//                                  SystemBroadcast }        // native enum, keys = wire names
//   MyNotificationsQuery(_myNotifications, _myNotifications_items)(Variables)
//   MyUnreadNotificationCountQuery(Variables)
//   MarkNotificationReadMutation(_markNotificationRead)(Variables)
//   MarkAllNotificationsReadMutation(Variables) + the four DocumentNode constants
```

- **Codegen enum ≠ backend enum (critical 4.1 nuance):** the codegen-generated `NotificationType` maps PascalCase keys to the GRAPHQL NAMES on the wire (`SessionRequest → 'SessionRequest'`) because codegen sees only the SDL (which carries the names — the snake_case values are runtime-only). The backend canonical TS enum maps the SAME keys to snake_case runtime values (`SessionRequest → 'session_request'`). Frontend filter/variable code MUST use the codegen enum (values = wire names); never import the backend enum into frontend code.
- **Wire shapes for 4.x** (unchanged from the 3.3 BINDING): ids travel as STRINGS (`ID`); `markNotificationRead` selections MUST include `id` (Apollo cache update of the flipped row); `markAllNotificationsRead` returns a bare `Int` (client refetches/evicts lists); `myNotifications` returns the `NotificationListPage` wrapper (`NotificationListPage` is `keyFields: false` in `apolloCache.ts` — already registered by 3.1); marking read is NOT a realtime event (2.7 BINDING).
- **Counter expectations for 4.1:** biome 557 → 559 (+2: `notification.documents.ts` + sub-barrel `index.ts`); the SDL should AGAIN regenerate byte-identically; graphql.ts grows by exactly the notification operation types above.
- **Integration-suite run pattern (this sandbox, unchanged):** `GRAPHQL_TEST_PORT=3000 ADMIN_EMAIL=admin@app.local ADMIN_PASSWORD=adminpassword123 bun run test/scripts/run-test.ts <suite>` — the canonical 3066 boot stays env-locked while the :3000 dev server runs (documented identically in 3.1/3.2/3.3 and reproduced here, D-3).

## 3. Files Touched

| File | Change |
|---|---|
| `backend/graphql/test/sdl-static-assertions.test.ts` | **NEW** — the static schema assertion suite (15 tests / 55 expects / 3 describes): BFLA CUD-absence verdict + root-set freeze + REQ-060 four-ops exact signatures + `id: ID!` pin + REQ-069 depth/complexity posture, all over the COMMITTED SDL artifact parsed as an AST |
| `frontend/graphql/generated/schema.graphql` | **VERIFIED ZERO-DRIFT (generated)** — `bun run generate:gqlSchema` re-run produced a 0-byte diff; the artifact committed by 3.1–3.3 IS the current deterministic emission (no new commit content) |
| `frontend/graphql/generated/gql/graphql.ts` | **VERIFIED ZERO-DRIFT (generated)** — `bun codegen` re-run produced a 0-byte diff (document-driven pipeline; notification types arrive with 4.1's documents — D-1) |
| `ai/plans/sprint_2/.../outcome/3.4-outcome.md` | This document |
| `ai/plans/sprint_2/.../tasks.md` | 3.4 block checkboxes flipped (6 boxes — only the 3.4 lines) |
| `/home/z/my-project/worklog.md` | Appended Task 3.4 entry |
| `backend/graphql/**` (sources), `backend/db/schema/**`, `backend/db/migration/**`, `backend/lib/gateway/public-operations.ts`, `.env*`, `shared/**`, `backend/types/**`, `backend/enum/**`, `backend/services/**`, `test/**`, `docs/**`, `app/**`, `codegen.ts` | **ZERO drift** — `git status` shows exactly ONE new untracked file (the assertion suite); the allowlist is byte-identical (default-deny preserved); deferred-items ledger NOT touched (no new ruling needed) |

## 4. Verification Commands & Exit Codes

| # | Command | Result | Exit |
|---|---|---|---|
| 1 | `bun run generate:gqlSchema` | ✅ wrote `frontend/graphql/generated/schema.graphql` (4914 bytes) — `git diff` on the artifact = **0 bytes** (byte-stability proof vs the 3.1–3.3 hand-synced SDL) | **0** |
| 2 | `bun codegen` | ✅ Generate outputs → `frontend/graphql/generated/gql/graphql.ts` — `git diff` = **0 bytes** (document-driven no-op at 3.4; D-1) | **0** |
| 3 | `bun run scripts/health/sub-loop.ts backend/graphql/test/sdl-static-assertions.test.ts --lifecycle duplicates` | ✅ all five checks passed — after 1 honest fix iteration (4× `sonarjs/prefer-specific-assertions` → dedicated `not.toContain`/`not.toBe` matchers) | **0** |
| 4 | `bun run test/scripts/run-test.ts backend/graphql/test/sdl-static-assertions.test.ts` — RUN 1 / RUN 2 | ✅ **15 pass / 0 fail / 55 expects — identical both runs** (determinism proof) | **0** |
| 5 | `bun run test/scripts/run-test.ts backend/graphql/test/schema-surface.test.ts` — RUN 1 / RUN 2 | ✅ **27 pass / 0 fail / 147 expects — identical both runs** (existing suite UNTOUCHED, stays green) | **0** |
| 6 | `bun run test/scripts/run-test.ts backend/graphql/test/notification-query.test.ts` (canonical, port 3066) | ⚠️ **0 pass / 1 fail — PRE-EXISTING environment block, not a regression**: fails at `waitForServer` ("Server on port 3066 did not start within the allotted time") because the mandatory :3000 dev server holds Next's dev-dir lock — IDENTICAL root cause to the 3.1/3.2/3.3 findings (this run surfaced as a 60 s boot timeout rather than a fast spawn refusal; same env-block semantics). Zero GraphQL assertions reached; dev server stayed HTTP 200 | 1 (env) |
| 7 | `GRAPHQL_TEST_PORT=3000 ADMIN_EMAIL=admin@app.local ADMIN_PASSWORD=adminpassword123 bun run test/scripts/run-test.ts backend/graphql/test/notification-query.test.ts` — RUN 1 / RUN 2 | ✅ **12 pass / 0 fail / 135 expects — identical both runs** (liveness probe hits the LIVE dev server → no spawn, nothing killed) | **0** |
| 8 | `GRAPHQL_TEST_PORT=3000 … run-test.ts backend/graphql/test/notification-mutation.test.ts` — RUN 1 / RUN 2 | ✅ **13 pass / 0 fail / 99 expects — identical both runs** | **0** |
| 9 | Post-run psql residue probe | ✅ `notifications = 0`, `wire-%@test.local` users = 0, total users = 4 (seeded set — admin untouched) | n/a |
| 10 | `bun run test/scripts/run-test.ts backend/db/test` | ✅ **56 pass / 0 fail / 222 expects** (baseline holds) | **0** |
| 11 | `bun run test/scripts/run-test.ts backend/services/notifications` | ✅ **83 pass / 0 fail / 684 expects across 7 files** (baseline holds) | **0** |
| 12 | `bun run test/scripts/run-test.ts backend/graphql/test/error-finalizer.test.ts` / `context-keys.test.ts` / `request-id.test.ts` | ✅ 14/0 · 14/0 · 12/0 (baselines hold) | **0** |
| 13 | `bun run test/scripts/run-test.ts backend/lib/gateway/static-assertions.test.ts` / `public-operations.test.ts` | ✅ 17/0 · 26/0 (gates A1–A5 + registry closure hold) | **0** |
| 14 | `KOTTABY_TEST_RUNNER_OK=1 bun --env-file=.env.test test frontend/graphql/test/gateway/allowlist-coverage.test.ts` | ✅ **8 pass / 0 fail / 42 expects** — unscoped-fields set ≡ allowlist (default-deny holds in both drift directions) | **0** |
| 15 | `KOTTABY_TEST_RUNNER_OK=1 bun --env-file=.env.test test frontend/graphql/sharedDocuments/documents.contract.test.ts` | ✅ **11 pass / 0 fail / 53 expects** — documents ↔ generated `…Variables` contracts still bind (codegen artifact tier corroborated) | **0** |
| 16 | `bun run test/scripts/run-test.ts backend/ws/notification-ws-server.test.ts` / `backend/enum/notifications/notification-type.enum.test.ts` | ✅ 23/0/77 · 25/0/293 (baselines hold) | **0** |
| 17 | `KOTTABY_TEST_RUNNER_OK=1 bun --env-file=.env.test test test/workflows` | ✅ 33 pass / 0 fail / 238 expects (baseline holds) | **0** |
| 18 | `bun tsgo` (full project) | ✅ **EXIT 0 — ZERO errors** (the new suite + untouched generated artifacts introduce no type errors) | **0** |
| 19 | `bun biome:check` (full repo) | ✅ **Checked 557 files, no fixes applied** (556 baseline + 1 new assertion file — exactly the 3.3 prediction) | **0** |
| 20 | `curl -s http://localhost:3000/api/health` + `POST /api/graphql {"query":"{ _health { status } }"}` | ✅ HTTP 200 + 200 throughout (before, during, after every batch — no HMR incidents this session; zero schema SOURCE files were edited) | n/a |
| 21 | Live introspection (`__type(name: "Mutation")` / `"Query"`) | ✅ Mutation fields = EXACTLY `login, logout, markAllNotificationsRead, markNotificationRead, refreshToken, registerUser`; Query fields = EXACTLY `_health, me, myApplicantProfile, myNotifications, myUnreadNotificationCount, recitationReadings` — both ≡ the frozen baselines the new suite pins on the artifact | n/a |
| 22 | `git diff backend/lib/gateway/public-operations.ts` | ✅ EMPTY — allowlist byte-identical (default-deny preserved) | n/a |
| 23 | CUD-absence grep on the generated SDL (`createNotification\|updateNotification\|deleteNotification`) | ✅ ZERO matches (now ALSO a permanent test — suite test #1/#2) | n/a |
| 24 | BFLA emit-grep under `backend/graphql/**` (`emitForUser\|emitForUsers\|emitBatch\|publishReceipts\|publishFanout`) | ✅ ZERO matches (production AND test files) — the bulk fan-out primitive remains unreachable from any resolver | n/a |

## 5. TE — Test Engineering (3.4.TE)

**Verification path: static/artifact tier (this task's deliverable) + full regression re-run of the plan-owned suites.** The canonical 3066 integration boot was attempted and env-blocked exactly as in 3.1/3.2/3.3 (row 6 — documented identically per the carry-forward obligation); the live-tier pattern delivered the real wire-tier coverage (rows 7–8).

**`backend/graphql/test/sdl-static-assertions.test.ts` — 15 tests / 55 expects / 3 describes:**
- **"BFLA structural verdict — zero notification CUD surface (REQ-032)" (4)** — the forbidden CUD field names are absent from EVERY object AND input type in the artifact (AST scan); the forbidden tokens appear nowhere in the artifact TEXT; the Mutation root equals EXACTLY the refreshed frozen 6-op baseline; the Query root equals EXACTLY the frozen baseline + `_health`. An attempted CUD registration in any future change set fails BOTH the root-set freeze (set inequality) and, for sneaky placements, the all-types field scan.
- **"REQ-060 four-ops contract — exact SDL signatures on the artifact" (5)** — each of the four inbox operations is pinned with its EXACT rendered signature (`myNotifications(filter: MyNotificationsFilterInput): NotificationListPage!` etc.), and `MyNotificationsFilterInput` exposes exactly `type: NotificationType` / `isRead: Boolean` / `limit: Int` / `offset: Int`, all nullable.
- **"Notification object — `id` + REQ-069 depth/complexity posture" (6)** — `id: ID!` among exactly the eight REQ-060 fields; FLATNESS (every field's named type ∈ {Boolean, ID, Int, NotificationType, String} — zero object/list-typed fields); no recursion (no field references `Notification`/`NotificationListPage`); the notification family's ONLY object-typed field is `NotificationListPage.items: [Notification!]!`; the wrapper contract (`items`/`totalCount`/`hasMore`, no `id`); NO `Subscription` root exists in the artifact (realtime = WS sidecar).

**Depth/complexity posture (documented per tasks.md 3.4.TE):** the surface is a FLAT object — all eight `Notification` fields are scalars or the `NotificationType` enum, so a selection can never traverse beyond one hop; the ONLY list in the family is the page wrapper's `items: [Notification!]!`, and its page window is bounded at the SERVICE boundary (limit 1..50, default 20; offset default 0 — the 2.7 engine gates) with `totalCount`/`hasMore` closing the page math (REQ-026 coherence); no self-referential or recursive shape exists (test-pinned); N+1 is structurally impossible — there are no object-typed fields to batch, hence no DataLoader by design (plan D8); realtime bulk delivery is the sidecar's `RealtimeNotificationPayload` (bounded `Pick` of the same 7 data fields + `id`), never a GraphQL subscription (test-pinned absent).

**Determinism:** new suite 15/0/55 RUN 1 = RUN 2; schema-surface 27/0/147 RUN 1 = RUN 2; integration suites 12/0/135 and 13/0/99 RUN 1 = RUN 2 each; teardown verified residue-free (row 9).

## 6. SEC — Security & Tenancy Audit (3.4.SEC)

- **BFLA structural verdict (REQ-032) — RECORDED AS A PERMANENT TEST:** the generated SDL contains ZERO `createNotification`/`updateNotification`/`deleteNotification` operations, proven at THREE layers — (1) AST field-name scan across every object AND input type in the artifact; (2) lexical token scan of the whole artifact text; (3) the Mutation root-set freeze (exactly the 6-op frozen baseline — no notification-shaped mutation beyond the read-latch pair can appear without failing set equality). Corroborated by direct grep (row 23: zero matches) and live introspection (row 21: Mutation = exactly the 6 sanctioned ops on the SERVED schema).
- **Bulk fan-out primitive unreachable from resolvers:** grep of `emitForUser|emitForUsers|emitBatch|publishReceipts|publishFanout` under `backend/graphql/**` → ZERO matches, production AND test files (row 24). `NotificationEngine` is referenced under `backend/graphql/**` only by the two notification resolver files (3.3 finding, unchanged).
- **Default-deny posture intact:** `backend/lib/gateway/public-operations.ts` is BYTE-IDENTICAL (`git diff` empty, row 22) — no notification operation was added to the anonymous allowlist (all four carry `authScopes: { authenticated: true }`); allowlist-coverage 8/0/42 proves the unscoped-fields set ≡ allowlist in both drift directions; public-operations 26/0 + static-assertions 17/0 gates green.
- **No PII surface growth:** the regenerated artifacts expose nothing new beyond the already-reviewed notification surface; `userId` remains structurally absent from the `Notification` object (schema-surface pin, 27/0/147 suite green).
- **Anonymous rejection still live:** the served schema answered the live probes through the whole session (dev :3000 HTTP 200 + `_health` 200; the 3.2/3.3 UNAUTHORIZED wire proofs remain in the green integration suites re-run here).

## 7. SR — Semantic Review (3.4.SR) — generated-diff audit + source checklist

**Generated diff audit (the SR centerpiece — "generated diff contains ONLY notification additions / no unrelated schema drift"):**
- `bun run generate:gqlSchema` → **0-byte diff** on `frontend/graphql/generated/schema.graphql`: the committed artifact is byte-identical to a fresh deterministic emission of the current Pothos tree. The full notification delta (enum + `Notification` + `NotificationListPage` + input + 2 queries + 2 mutations) entered the artifact in the 3.1/3.2/3.3 change sets and NOTHING else moved since — zero unrelated schema drift is proven by byte-equality, the strongest possible audit.
- `bun codegen` → **0-byte diff** on `frontend/graphql/generated/gql/graphql.ts`: the document-derived type set is unchanged (documents unchanged; the SDL's notification additions cannot leak into an operation-derived output until documents reference them). The documents↔codegen contract tier stays green (row 15, 11/0/53).
- The 3.4 change set's net source delta = exactly ONE new test file (assertions only — no production code, no schema source, no documents, no config touched; `git status` corroborates).

**Source checklist (the new suite):**
1. ✅ Pure static tier: `readFileSync` + `parse` + AST walk — zero schema build, zero DB, zero server boot, zero disk writes (mirrors the gateway static-assertions discipline).
2. ✅ Frozen baselines mirror `schema-surface.test.ts`'s refreshed `PRE_3_1_*` inventories verbatim (documented cross-reference in both docblocks' spirit; the artifact tier and built-schema tier pin the SAME sets).
3. ✅ Zero `as` casts — AST narrowing via type predicates + fail-fast guards (`objectTypeDefinition`/`inputObjectTypeDefinition`/`fieldSurface` throw with precise messages).
4. ✅ Deterministic ordering everywhere (`toSorted` + `localeCompare` comparators; frozen arrays pre-sorted).
5. ✅ Zero `console.*`; domain-only docblock (no REQ/task references beyond the contract citations used by sibling suites).
6. ✅ Import order per repo convention (`bun:test` → `node:` → `graphql`); inline `type` modifiers for AST types.
7. ✅ No duplicate logic with `schema-surface.test.ts`: distinct helpers (`renderType`/`namedTypeName`/`fieldSurfaces` over the AST vs the built-schema walk), distinct tier (artifact vs builder) — jscpd duplicates check green.
8. ✅ The suite fails LOUD on drift: every lookup throws (fail-fast) rather than silently passing on absence — a missing type/field can never produce a vacuous pass.
9. ✅ Assertions are pinned to the REQ-060 SDL as-reconciled (`createdAt: String!` — the 3.1 D-3 no-DateTime-scalar ruling; enum names as wire values — the 3.1 D-2 enum-object ruling).
10. ✅ Belt-and-braces by design: structural (AST) + lexical (text) + set-equality (root freeze) redundancies so no single detector's blind spot can mask a BFLA regression.

## 8. IV — Instruction Verification (3.4.IV)

| Instruction | Verdict | Evidence |
|---|---|---|
| tasks.md 3.4 verbatim bullets (run `bun run generate:gqlSchema && bun codegen`; commit ALL generated artifacts in the same change set; static assertion test with ZERO CUD ops + `Notification` carries `id` + all four ops per REQ-060 SDL; allowlist byte-unchanged with git-diff evidence; REQ-032/060/061/069) | ✅ all | §1 maps bullet-to-artifact; rows 1–2 (generators, empty diffs — artifacts already committed in the 3.1–3.3 change sets and re-verified byte-stable HERE); §4 rows 22–23 + §6 (allowlist + CUD evidence); §5 (four-ops + id + depth posture) |
| Gateway Rule 8.5 — "Codegen in the same commit: run `bun run generate:gqlSchema && bun codegen` and commit generated artifacts in the SAME change set" | ✅ | Both generators ran at 3.4 and produced 0-byte diffs — the artifacts committed by 3.1 (SDL), 3.2 (SDL), 3.3 (SDL) ARE the current outputs, so the notification registrations and their artifacts are verifiably co-committed; the 3.4 change set adds the permanent structural gate that keeps them locked (any future drift fails `sdl-static-assertions` + `schema-surface`'s byte-identity test) |
| REQ-061 (codegen discipline: `bun run generate:gqlSchema && bun codegen` after all changes; artifacts committed in the same change set) | ✅ | Rows 1–2; §3 zero-drift table; the deferred-to-3.4 codegen sync obligation from 3.1 D-5 / 3.2 D-7 / 3.3 D-5 is now DISCHARGED |
| REQ-032 (BFLA: zero create surface; emit service-internal; allowlist unchanged) | ✅ | §6 — three-layer CUD-absence proof now test-permanent; emit-grep zero; allowlist byte-identical |
| REQ-060 (exact SDL contract incl. `id` first-field convention) | ✅ | §5 four-ops signatures + `id: ID!` pin (id-FIRST source-order pin remains in schema-surface, untouched and green); `createdAt: String!` per the documented 3.1 D-3 reconciliation |
| REQ-069 (depth/complexity: flat scalars/enum on one object, bounded list, no recursive shape, no N+1) | ✅ | §5 depth/complexity paragraph — every clause is now a named test in the new suite |
| `frontend/graphql/AGENTS.md` — codegen artifacts in `frontend/graphql/generated/gql/`; regenerate after schema/document changes; operation-derived types only | ✅ | The regenerated `graphql.ts` contains ONLY operation-derived types (no schema-level object types leaked from the notification additions); documents-contract tier green (row 15) |
| `backend/graphql/AGENTS.md` — run `bun run generate:gqlSchema` then `bun codegen` after registering enums so the codegen output stays in sync | ✅ | Both ran; byte-stable outputs; §2 documents the exact sync point where notification types enter (4.1 documents + codegen) |
| Mandated runner discipline (`bun run test/scripts/run-test.ts <path>`; frontend gateway tier via `KOTTABY_TEST_RUNNER_OK=1 bun --env-file=.env.test test`) | ✅ | Rows 4–17 use exactly the sanctioned runners (the 2.8 D-6 / 3.2 carry-forward patterns) |

## 9. Deviations

- **D-1 (codegen produces an EMPTY diff at 3.4 — expected, not a failure):** `bun codegen` derives types from DOCUMENTS (`sharedDocuments/**/*.ts` + `views/**/*.documents.ts`) with the `typescript-operations` + `typed-document-node` plugin pair; notification documents are task 4.1's deliverable, so the notification schema additions cannot surface in `graphql.ts` yet. The 3.4 run is therefore the byte-stability/no-drift proof for the artifact pair — the honest reconciliation of "`bun codegen` is the outstanding sync": the sync is a verified no-op, and the exact exports 4.1 will unlock are documented in §2. This preserves the "artifacts committed in the same change set" contract in its strongest form: the committed artifacts are byte-identical to fresh generator output at the Phase-3 boundary.
- **D-2 (the "static schema assertion test" is a 15-test suite, not one test):** tasks.md asks for a static assertion test covering three clauses (CUD-absence, `id`, four-ops). Implemented as a dedicated artifact-side suite (`sdl-static-assertions.test.ts`) with three describes — reviewability and failure locality — while leaving `schema-surface.test.ts` UNTOUCHED (27/0/147 stable, per the "stays green" wording and the biome 556→557 single-file prediction). The REQ-069 depth/complexity posture clauses ride the third describe (tasks.md 3.4.TE requires documenting them; pinning them as tests is strictly stronger).
- **D-3 (canonical 3066 integration boot env-block — timeout flavor):** the canonical runner attempt failed at `waitForServer` after the 60 s boot window ("Server on port 3066 did not start") rather than 3.1–3.3's fast spawn-refusal wording — same root cause (the :3000 dev server holds Next's dev-dir lock; a second dev server cannot boot), zero GraphQL assertions reached, dev server stayed HTTP 200 throughout. Documented identically per the carry-forward obligation; the live-tier `GRAPHQL_TEST_PORT=3000` pattern delivered the real coverage (rows 7–8).
- **D-4 (biome counter 556 → 557):** exactly the 3.3 prediction (+1 = the new assertion file); no other file entered the biome scope.
- **D-5 (no HMR incident this session — and why):** the 3.3 carry-forward mandates probing `/api/graphql` after every schema-file edit batch; 3.4 edited ZERO schema source files (the generators write artifacts the dev server does not own, and the new suite is a pure test file). Probes before/during/after every batch returned HTTP 200 — the mandate was honored with zero events.

## 10. Carry-Forward Knowledge

- **For 4.1 (documents + codegen):** the exact current export surface of `graphql.ts` AND the exact exports 4.1's codegen run will add are in §2 — including the critical enum nuance (codegen `NotificationType` keys map to the GraphQL NAMES on the wire; the backend canonical enum maps the same keys to snake_case runtime values — frontend uses the CODEGEN enum). Run `bun run generate:gqlSchema && bun codegen` in the SAME change set (Rule 8.5); expect biome 557 → 559 (+2 files: documents + sub-barrel); expect the SDL to regenerate byte-identically again; `bun tsgo` is the 4.1.TE compile gate.
- **For 4.1 (type-consumption rules, from `frontend/graphql/AGENTS.md`):** `TypedDocumentNode<{EntityName}Query, {EntityName}QueryVariables>`; extracted nested types via the `{OperationName}_{field}` compact naming (e.g. `MyNotificationsQuery_myNotifications_items`); NO indexed-access workarounds, NO mapping layers, NO schema-level object types; `id` in EVERY `Notification` selection; hooks from `@apollo/client/react`, `useQuery` stateful only.
- **For 4.2/4.3:** wire shapes in §2 (string ids; mark-all returns bare Int; wrapper is `keyFields:false`); `NotificationListPage` embedded-types entry already present in `frontend/graphql/AGENTS.md` (3.1).
- **ENVIRONMENT — canonical 3066 integration boot:** reproduced for the final Phase-3 run (D-3); Phase-4 executors document identically and use `GRAPHQL_TEST_PORT=3000 ADMIN_EMAIL=admin@app.local ADMIN_PASSWORD=adminpassword123` (liveness probe hits the LIVE dev server — no spawn, nothing killed; `run-server-tests.ts` NEVER at port 3000).
- **Counter updates for the next gate:** biome 556 → **557** (+1 file); NEW suite `backend/graphql/test/sdl-static-assertions.test.ts` **15/0/55**; schema-surface stays **27/0/147** (untouched); all other baselines unchanged (DB 56/0/222; notifications 83/0/684; query 12/0/135; mutation 13/0/99; WS 23/0/77; enum 25/0/293; workflows 33/0/238; allowlist-coverage 8/0/42; static-assertions 17/0/20; public-operations 26/0/52; documents-contract 11/0/53); plan-owned totals 256/1,813 → **271 tests / 1,868 expects** (+15/+55).
- **Phase 3 is COMPLETE** (3.1–3.4 all flipped, zero-regression verified at every step); Phase 4 may begin with the §2 BINDING as its consumption contract.
