# Task 5.2 Outcome — WS Sidecar Suite Consolidation + Transport-Degradation Path

## 1. Summary

WS sidecar suite verified as runner-registered and deterministic; the Redis-outage degradation contract is now test-pinned at the transport tier. REQ-011's engine-side swallow (`NOTIFICATION_DELIVERY_DEGRADED`) remains pinned by the 2.6 emit suite (deliberately not duplicated); this task adds the missing pieces: publisher rejection during outage, subscriber survival + post-recovery resume, and zero-delivery during outage.

## 2. Runner-Registration Verification (2.8.TE consolidation)

`bun run test/scripts/run-test.ts backend/ws/notification-ws-server.test.ts` (canonical runner):

- RUN 1: **23 pass / 0 fail / 77 expect() calls** (6.22s) — orchestrator-verified
- RUN 2: identical counts (agent-verified during the task window)
- Suite lives at its committed location `backend/ws/notification-ws-server.test.ts`, discovered by the canonical runner glob — REQ-079 registration satisfied.

## 3. Degradation Test — `backend/services/notifications/realtime/fanout-degradation.test.ts`

- 4 tests / 19 expect() calls — **RUN 1 = RUN 2 = 4/0/19** (deterministic; 77ms each)
- Coverage:
  - **Outage publisher**: `OutageProneFanoutClient` fake in failed state → `publishFanout` rejects with the client's own error unwrapped; ZERO transport-level `logDomainError` calls (no swallow — the engine owns swallowing per REQ-011); zero wire messages; zero subscriber deliveries.
  - **Subscriber resume**: baseline round-trip → outage mid-subscription (publish rejection captured, no exception escapes, listener stays registered — asserted via `isSubscribed()` probe) → recovery → publish round-trips to the SAME subscription (no re-subscription needed).
  - **Row-persistence composition**: file-level docblock documents the engine own-commit path (insert → commit → publish) + cites the emit suite's REQ-011 swallow pin; plus one transport-level invariant sketch (zero pushes/deliveries during outage).
- Live Redis (:6379) untouched — outage simulated exclusively via the fake client; port 6399 referenced only as simulated cause text.

## 4. Quality Loop

`bun run scripts/health/sub-loop.ts backend/services/notifications/realtime/fanout-degradation.test.ts --lifecycle duplicates` → **exit 0, iteration 1, zero fix iterations** (all five sub-checks green: tsgo filtered, oxlint, biome, lint:type-aware, check:duplicates).

## 5. Gates

- `bun tsgo` (project-wide): **exit 0, 0 errors**
- Transport bugs found: **none** — `RedisPubSubTransport` held its documented degradation contract exactly.
- "Structured warn logged" clause (tasks.md §5.2) maps to the ENGINE-side `NOTIFICATION_DELIVERY_DEGRADED` log — covered by the pinned emit suite (`notification-engine.emit.test.ts`, 2.6 Tier 3), not duplicated here per the no-duplication constraint.

## 6. BINDING Notes for 5.3 / 5.4

- Degradation seam locations: publisher rejection = `RedisFanoutClient.publish` propagation; subscriber survival = subscription registry on the transport instance; engine swallow = `NotificationEngine` post-commit catch (2.6).
- 5.3 chaos tier should mint its OWN fixture marker prefix (do not reuse `matrix-`); concurrency storms may reuse the 5.1 helper patterns (`registerActor`/`loginActor`/`seedRows`).
- 5.4 coverage: the new degradation file is runner-discoverable at `backend/services/notifications/realtime/fanout-degradation.test.ts`.

## 7. Carry-Forwards / Deviations

- **D-1 (session structure)**: this task was executed across a focused executor session (test authoring + verification) with orchestrator-completed bookkeeping after three turn-exhausted dispatch attempts (environment learning: surgical dispatches with inlined facts are required for this sandbox's agent budget).
- **D-2 (outage simulation)**: no real Redis outage was induced (live user-space Redis must stay up for the dev topology); the fake-client outage harness provides equivalent contract coverage. A real-outage drill remains available manually via the `0-redis-provisioning` worklog entry's stop/restart command.
