# Session Fee Escrow & Teacher Wallet Crediting — Canonical Reference

**Domain:** Billing & Wallets (session-fee escrow + teacher earning ledger)
**Status:** Implemented and verified (hold-as-debit at booking, single-winner settle/release, exactly-once earning credit)
**Source of truth for:** the escrow pairing between the student's held lane and the teacher's wallet, the settle/release/race matrix over `sessions.fee_held`, the INV-W*/INV-B4/INV-S3 invariant map with schema + code + test anchors, the deferrals that shape the money path, and the boundary hand-offs every dependent billing ticket must honor.

This document consolidates the money-side of the session lifecycle. Downstream consumers (withdrawals, admin adjustments, re-evaluation deductions, financial verification) MUST read it before touching `sessions.fee_held`, `sessions.held_balance_lane`, `wallet.*`, or `teacher_transaction.*`. The hold is written exactly once per booking, consumed exactly once per settlement, and refunded at most once per release — the guarded primitives live in `StudentRepository`, `SessionRepository`, and `WalletRepository` and are composed by `SessionLifecycleService`; consumers extend them, never re-implement them.

---

## 1. Why

A booked session is a two-sided money contract: the student funds a hold from an allowance lane at request time, and the teacher receives the platform fee only when the lesson is actually attended and confirmed. The domain has to be race-proof (two concurrent bookings on a one-unit lane must settle as exactly one session and one debit; a confirm racing a timeout sweep must produce exactly ONE financial outcome), exactly-once (one hold ⇒ at most one earning ledger row and one wallet increment), and fail-closed (a zero-balance denial commits zero rows; an unreadable provenance lane refuses to refund rather than guessing). Each rule below exists because a specific failure mode was proven or ruled out by the executing suites — the worst of them is *re-timing the debit*: moving the decrement to completion re-opens the double-booking race the escrow exists to kill. Read §2 and §5 before adding any writer over these columns, and §6 before touching any test that asserts them.

## 2. Hold-As-Debit Semantics (the binding ruling)

**"Hold" = a guarded debit of one unit lane at request time** — not a balance freeze with a later decrement. The ticket prose once described a balance "held (not decremented yet)" at request and "decremented by 1" at dual confirmation; that wording is superseded (canonical rulings: `docs/sessions/session-lifecycle.md` §4–§5; `docs/billing/segregated-session-balance.md` §6, decision D1). The shipped and re-verified semantics:

1. **Debit ladder at booking (trial-first):** the creation transaction attempts a guarded `UPDATE students SET balance_trial = balance_trial - 1 … WHERE balance_trial > 0 RETURNING id` and, on a miss, the same guarded statement on the intent lane (`balance_hifz` for Hifz, `balance_tajweed` for Tajweed) — `debitBookingLadder` (`backend/services/classes/session-lifecycle.booking.ts:127-159`; trial leg :133-136, intent leg :137-158). An expired-uncovered lane is denied `SUBSCRIPTION_EXPIRED` before the balance denial (:140-149). Both legs missing → `ValidationError("INSUFFICIENT_BALANCE", t.insufficientBalance)` (:156) and the whole transaction rolls back with zero writes.
2. **The guarded primitive IS the lock:** `StudentRepository.decrementLaneIfAvailable` fuses the guard into the statement — `WHERE id = ? AND <lane> > 0 RETURNING id` (`backend/db/repo/students/student.repository.ts:404-419`, guard predicate :415). Zero TOCTOU window; a concurrent one-unit race settles as exactly one fulfillment and one localized denial.
3. **Hold + provenance on the session row:** the insert carries the platform fee, `feeHeld: true`, the winning lane, and the 24h confirmation deadline (`session-lifecycle.booking.ts:182-185`; columns `backend/db/schema/classes/session.ts:63-65,70`). `held_balance_lane` is the row's PERMANENT refund provenance — NULL only while no fee has ever been held, never rewritten or nulled afterwards (schema docblock `backend/db/schema/classes/session.ts:32-39`). The fee itself is platform-owned and never client input: `sessionFeeForIntent` (`backend/services/classes/session-lifecycle.guards.ts:83-85`) reads the constants `SESSION_FEE_HIFZ` / `SESSION_FEE_TAJWEED` = `"25.00"`, `SESSION_FEE_CURRENCY = "EGP"` (`shared/constants/session-fees.constants.ts:27-33`).
4. **Settlement is a flip, not a debit:** dual confirmation runs ONE guarded UPDATE that stamps the student confirmation AND sets `fee_held = false` — `SessionRepository.confirmStudentCompletionOnce` (`backend/db/repo/classes/session.repository.ts:433-455`; predicate `fee_held = true` ∧ status `completed` ∧ teacher stamp present ∧ student stamp absent, :447-452). The teacher's wallet credit composes inside the SAME transaction (`confirmCompletionInTx`, `backend/services/classes/session-lifecycle.confirmation.ts:112-139`; credit call :129). Net effect per attended session: exactly −1 on the funding lane — never −2.
5. **Release re-increments the SAME lane:** cancellation, timeout sweep, and arbitration-cancel all funnel through `refundHeldLaneToProvenance` (`backend/services/classes/session-lifecycle.transitions.ts:222-237`) → `StudentRepository.incrementLane` (`backend/db/repo/students/student.repository.ts:433-441`) — always to the recorded `held_balance_lane`, only when it is non-null (:227-229), and fail-closed on an unreadable value (:230-235, the refusal rolls the whole cancellation back). A NULL-lane legacy row refunds nothing, which is correct: no hold exists.
6. **Two units, one reconciliation point.** The student side moves integer lane units (±1, never currency); the teacher side moves EGP decimal strings (`wallet.balance`/`wallet.total_earning`, `backend/db/schema/billing/wallet.ts:27-28`). The two sides are reconciled ONLY by the settlement event: one consumed hold ⇒ exactly one `earning` ledger row whose `amount` is the session's fee string taken VERBATIM — never re-parsed or re-rounded (`WalletRepository.creditEarningOnce`, `backend/db/repo/billing/wallet.repository.ts:78-110`; verbatim rule in the :70-74 docblock, amount inserted :94). Release paths touch only lanes: zero ledger rows, wallet untouched (§3.3).

## 3. Settle / Release / Race Matrix

### 3.1 State machine (escrow view)

The session state machine itself is canonical in `docs/sessions/session-lifecycle.md` §2 and `backend/db/schema/classes/session.ts:15-20` (`scheduled → started → completed | cancelled`; `disputed` reachable from the two live states only). The escrow overlays it like this:

| Current state | Trigger (actor) | Next state | Escrow effect | Guard / citation |
|---|---|---|---|---|
| — (no row) | Student `createSession` | `scheduled`, hold OPEN | lane −1, `fee_held = true`, lane + deadline stamped | STUDENT-only `$all` scope (`backend/graphql/mutation/classes/session-lifecycle.mutation.ts:87,98-103`); ladder §2.1–§2.2; idempotency claim (:135-139 header propagation; claim insert `session-lifecycle.booking.ts:271`) |
| `scheduled`, held | Teacher `startSession` | `started`, held | none (hold frozen) | participant teacher; guarded transition |
| `started`, held | Teacher `completeSession` | `completed`, held + teacher stamp | none (hold frozen; the stamp is a settle precondition) | participant teacher; `confirmedByTeacherAt` is part of the settle predicate :447-452 |
| `completed`, held | Student `confirmSessionCompletion` | `completed`, hold CONSUMED | `fee_held = false` + ONE `earning` row + wallet increment, one transaction | guarded UPDATE `backend/db/repo/classes/session.repository.ts:433-455`; credit `session-lifecycle.confirmation.ts:48-59,129`; composition `session-lifecycle.service.ts:732` |
| held, any live state | Participant `cancelSession` | `cancelled`, hold RELEASED | `fee_held = false`, lane +1 (provenance), NO ledger row | `SessionRepository.cancelSessionOnce` (`session.repository.ts:217-231`) + refund slice (`transitions.ts:222-237`) |
| `completed`, held, deadline lapsed | System sweeper | `cancelled`, hold RELEASED | same-lane refund, NO ledger row | `sweepExpiredCompletedOnce` (`session.repository.ts:503-517`; scheduled-leg twin :473) |
| `completed`, held | Student re-confirm / teacher confirm | unchanged | zero writes (replay arms) | `resolveSettledOrConflict` (`session-lifecycle.confirmation.ts:86-104`) |
| `scheduled`/`started`, held | Participant `openSessionDispute` | `disputed`, hold FROZEN | no money movement until arbitration | `openDisputeOnce` (`session.repository.ts:248-262`) — `completed` rows are structurally unreachable |
| `disputed`, held | Admin arbitration → CANCEL | `cancelled`, hold RELEASED | same-lane refund | `resolveDisputeCancelOnce` (`session.repository.ts:280-290`); ADMIN-only gate (`session-lifecycle.mutation.ts:255,270-275`) with service re-assertion (`session-lifecycle.service.ts:624`) |
| `disputed`, held | Admin arbitration → COMPLETE | `completed`, hold CONSUMED | hold consumed with NO wallet credit (the settlement window is over; never-started disputes denied pre-write, `session-lifecycle.service.ts:633-640`) | arbitration-COMPLETE classification; hold-consumption test `backend/services/classes/session-lifecycle.service.test.ts:1793` |

### 3.2 Side effects per transition

| Transition | Ledger / wallet writes | Notifications | Exactly-once arbiter |
|---|---|---|---|
| Book | `session` insert; lane −1; idempotency claim | teacher request notice | header-carried `X-Idempotency-Key` → `ctx.idempotencyKey` (`session-lifecycle.mutation.ts:135-139`); missing key is a pre-DB `VALIDATION` (`backend/graphql/test/session-lifecycle-mutations.test.ts:395`); replay THROWS `DUPLICATE_REQUEST` with zero rows (`session-lifecycle.booking.ts:211-224`, wire :404) |
| Settle | `teacher_transaction(earning, completed)` INSERT + guarded wallet increment (both columns) | earning surfaces via `myWallet` | the `fee_held = true` statement predicate (`session.repository.ts:448`) — a second confirm matches zero rows |
| Cancel / sweep / arbitration-cancel | lane +1 only; NO ledger row, NO wallet write | per the notification plans | guarded single-winner transition (status guard consumes the hold) |
| Replay of any terminal op | none | none | replay arms return the current row (`session-lifecycle.confirmation.ts:86-104`) |

The two sides use different units BY DESIGN (§2.6) and are never arithmetically compared outside settlement. The end-to-end money audit — does every earning trace to a consumed hold, and every refund to a released hold — is the Financial Safety Verification ticket's acceptance surface (`docs/planning/TICKETS.md:2985-3026`, §8).

### 3.3 Race matrix — the arbiter and its proof

Every race below is arbitrated by a guarded single statement, never by a probe read. Probe reads (`findTransitionProbe`) are classification-only and never feed a write decision (`session-lifecycle.confirmation.ts:135-138`).

| Race | Actors | Arbiter | Outcome | Proof (all passing) |
|---|---|---|---|---|
| Double-booking, one unit left | 2 concurrent `createSession` | guarded `WHERE lane > 0` debit | exactly one session + one lane debit; loser `INSUFFICIENT_BALANCE`, lanes never negative | real-PG chaos block `backend/services/classes/session-lifecycle.service.test.ts:2315` |
| Same-key request replay | N concurrent retries | claim-table unique key + throw-on-replay | exactly one session, one net debit, `DUPLICATE_REQUEST` on the losers | chaos `session-lifecycle.service.test.ts:2347`; journey `test/workflows/sessions/session-lifecycle.journey.test.ts:492` |
| Confirm vs. confirmation sweep | student confirm ∥ sweeper | mutually exclusive guarded predicates on `fee_held` | exactly ONE financial outcome: credit-once XOR refund-once; loser honestly classified | `backend/db/test/repo/classes/session.repository.test.ts:1582` (confirm-then-sweep) and :1617 (mirror); journey `test/workflows/sessions/session-dual-confirmation.journey.test.ts:620` |
| Re-confirm / teacher confirm | participant retries | replay arms on the settled shape | zero financial writes; the TEACHER's confirm can never mint a credit (student-side settle only) | `session-lifecycle.service.test.ts:2441` (student) and :2471 (teacher no-op); journey `session-dual-confirmation.journey.test.ts:492` |
| Double cancel | 2 cancels | status-guarded transition | exactly one refund; second cancel `SESSION_INVALID_TRANSITION` | repo `session.repository.test.ts:1430`; service `session-lifecycle.service.test.ts:1363`; wire `session-lifecycle-mutations.test.ts:573` |
| Double sweep | 2 sweeper passes | guarded overdue predicate | each overdue row cancelled exactly once; re-run matches zero rows | repo `session.repository.test.ts:1552` |
| Escrow already consumed by earning | sweeper over a confirmed row | settle predicate excludes student-stamped rows | sweep matches zero rows — the hold was consumed by earning, not held for refund | repo `session.repository.test.ts:1256` |
| Failed booking, key reuse | zero-balance retry after funding | claim released on rollback | zero writes on denial; THE SAME key then books funded (key-rollback) | journey `test/workflows/sessions/session-lifecycle-denials.journey.test.ts:236` |
| Sweeper vs. wallet | sweep-won branch | refund-only slice | zero ledger rows, wallet static, lane +1 | journey `session-dual-confirmation.journey.test.ts:561` (step 8) and the sweep-won partition asserts (:686-688) |

## 4. Invariant Map

Canonical registry: `docs/specs/state-machine-invariants.md` (§4.2 for the balance lanes, §5.3 for the wallet). Each row maps the invariant to its schema constraint, its code enforcement, and the suites that prove it.

| Invariant | Statement | Schema constraint | Code enforcement | Test anchors (current lines) |
|---|---|---|---|---|
| **INV-W1** | `wallet.balance` ≥ 0 | `wallet_balance_check` (`backend/db/schema/billing/wallet.ts:37`) | debits carry `balance >= amount` in the UPDATE predicate; the earning increment is strictly additive (§2.6); the CHECK is the concurrent-overdraw backstop | `backend/services/billing/wallet.service.test.ts:208` (insufficient funds → ZERO committed rows), :237 (exact-balance guard is `balance >= amount`) |
| **INV-W2** | `wallet.total_earning` ≥ 0 | `wallet_total_earning_check` (`backend/db/schema/billing/wallet.ts:38`) | only settlement increments it, additively; withdrawals and admin debits never touch it | `wallet.service.test.ts:132` (ledger page) alongside :208/:237; earning-only increments `backend/db/repo/billing/wallet.repository.ts:103-110` |
| **INV-W3** | exactly one wallet per teacher | `wallet_teacher_id_unique` (`backend/db/schema/billing/wallet.ts:36`) | lazy `ensureWalletOnce` — `INSERT … ON CONFLICT DO NOTHING` then select (`wallet.repository.ts:59-64`) | `wallet.service.test.ts:102` (brand-new teacher gets an honest zeroed wallet); race safety via repo :1582/:1617 |
| **INV-W4** | an `earning` transaction is created only upon dual confirmation | ledger row born `completed` (`backend/db/schema/billing/teacher-transaction.ts:42` default is `pending` — the earning INSERT overrides with `TransactionStatus.Completed`) | earning INSERT exists ONLY in `creditEarningOnce` (`wallet.repository.ts:78-110`, type :95, status :96), called only from the settlement slice (`session-lifecycle.confirmation.ts:48-59,129`) | `session-lifecycle.service.test.ts:2398` (stamp + release + EXACTLY ONE earning); journey `session-dual-confirmation.journey.test.ts:451` (one ledger row, amount = fee, `TransactionType.Earning`) |
| **INV-W7** | an `earning` row is linked to a `session_id`; withdrawal/bonus rows may be null | nullable FK `teacher_transaction.session_id` (`backend/db/schema/billing/teacher-transaction.ts:38`, `ON DELETE SET NULL`) | the earning INSERT always carries `sessionId` (`wallet.repository.ts:92`); non-earning writers (withdrawals, admin adjustments — `docs/billing/admin-financial-auditing.md` §4) leave it null | journey `session-dual-confirmation.journey.test.ts:451` (earning ↔ session identity); refund paths write no row at all (§3.2) |
| **INV-W8** | `teacher_transaction.amount` ≥ 0 | shipped CHECK is **`amount > 0`** (`backend/db/schema/billing/teacher-transaction.ts:50`) — STRICTER than the registry's "≥ 0" wording; the zero-amount case is additionally blocked pre-DB | the service validation matrix rejects malformed/non-positive amounts before any DB work | `wallet.service.test.ts:251` (validation matrix, zero side effects); :208 (denial rolls the orphan ledger row back) |
| **INV-B4** | a student cannot request a session when the relevant balance is 0 — extended: `(intent lane > 0) OR (balance_trial > 0)`, trial consumed first | lane CHECK floors (`docs/billing/segregated-session-balance.md` §2.1) | the trial-first ladder §2.1–§2.2; both-miss → localized `insufficientBalance` (en `shared/locale/en/errors/index.ts:90`, ar `shared/locale/ar/errors/index.ts:88`, typed `shared/locale/types/errors/labels.ts:186` — one REUSED funds-denial key across booking and withdrawal) | `session-lifecycle.service.test.ts:470` (total-miss: zero rows, key reusable), :394 (trial-hit debits exactly one unit); journey `session-lifecycle-denials.journey.test.ts:236` (zero writes + funded retry), `session-lifecycle.journey.test.ts:369`/:402 (trial-first across both actors) |
| **INV-S3** | a `teacher_transaction` (type = earning) can only be created when a session is `completed` AND the student confirms | same settle predicate, session-side vantage | `confirmStudentCompletionOnce` requires status `completed` ∧ teacher stamp ∧ student stamp absent ∧ `fee_held = true` (`session.repository.ts:447-452`); the credit runs only on that UPDATE's success (`session-lifecycle.confirmation.ts:112-139`) | repo `session.repository.test.ts:1582`/:1617 (the predicate loses to no one); :1256 (a confirmed row's escrow is consumed by earning); service :2398 |

Two adjacencies worth stating because they bound the map: **INV-W5** (withdrawal lifecycle — reserve at request, settle at decision) and **INV-W6** (ledger immutability via DB triggers) are owned canonically by `docs/billing/admin-financial-auditing.md` §2–§3; this domain's contribution is that release paths never write the ledger at all (§3.2), so the only ledger rows a session can produce are settlement earnings.

## 5. Rules for Extending the Money Path

1. **Extend the guarded primitives; never re-implement them.** The only writers over a lane balance are `decrementLaneIfAvailable` / `incrementLane`; the only earning writer is `creditEarningOnce`; the only settle is `confirmStudentCompletionOnce`; the only release-refund is `refundHeldLaneToProvenance`. New flows compose these inside their own transaction.
2. **The guarded predicate is the lock.** Never branch a money write off a pre-read state. Probe reads classify errors only (`session-lifecycle.confirmation.ts:135-138`); the UPDATE's WHERE clause re-asserts the state it is about to consume.
3. **Share the caller's transaction.** Settlement composes stamp + credit in ONE transaction (`session-lifecycle.service.ts:732` → `confirmCompletionInTx`); there is no post-commit hook and no queue. A rolled-back flow leaves zero rows everywhere.
4. **Money is a decimal string, verbatim.** The fee is inserted, stored, credited, and surfaced without ever being parsed, rounded, or re-derived (`wallet.repository.ts:70-74` docblock, amount :94; string-boundary test `session-lifecycle.service.test.ts:1109`).
5. **Release writes no ledger row.** If a new release-shaped transition appears (cancel variant, sweep variant, arbitration variant), it must route through `refundHeldLaneToProvenance` and must NOT insert a `teacher_transaction`.
6. **Reuse the localized denial keys.** `insufficientBalance` is the shared funds-denial key (booking and withdrawal both assert it through `expectDomainDenial` — `wallet.service.test.ts:224`, `session-lifecycle.service.test.ts:477`); do not mint per-domain copies of a funds denial.
7. **Wallet identity is ctx-derived.** `myWallet` takes zero arguments and resolves `ctx.user.id` (`backend/graphql/query/billing/wallet.query.ts:46-57,64`); `requestWithdrawal` binds the caller id and accepts an amount-ONLY input (`backend/graphql/mutation/billing/wallet.mutation.ts:46-50,75`). No mutation on the teacher self-service surface (`myWallet` / `requestWithdrawal`) accepts a wallet id or a balance value on the wire; administrative adjustment is the explicit ADMIN-governed exception (`adjustTeacherWallet` input `{teacherId, amount, direction, reason}`, `backend/graphql/pothos/admin/admin-finance.pothos.ts:170-177`) — not a self-service input.
8. **New SDL grows only through the pinned surface flow.** `Session.fee` (nullable string) and `Session.feeHeld` (non-null boolean) are exposed at `backend/graphql/pothos/classes/session.pothos.ts:170,174-175`; the wallet/ledger objects live at `backend/graphql/pothos/billing/wallet.pothos.ts:91,126` with the service-capped newest-first ledger page (:145-148). The committed SDL artifact is kept byte-identical to the built schema by the codegen-sync pin.

## 6. Anti-Patterns (What NOT to do)

- **Never re-time the debit.** A "decrement at completion" refactor re-opens the double-booking race (§2, ruling sources). Release flips `fee_held` and refunds the lane; it never debits anything.
- **Never credit a wallet outside `creditEarningOnce`, or outside the confirmation transaction.** Two writes for one settlement (a ledger row without the increment, or the reverse) is the classic split-brain; both writes share one transaction and one guarded statement each.
- **Never let the sweeper or a refund path write a ledger row.** Release economics are lane-side only (§3.2); a release-time earning row would double-pay.
- **Never refund to a guessed lane, and never rewrite/null `held_balance_lane` after it is placed** — it is permanent provenance (`backend/db/schema/classes/session.ts:32-39`); an unreadable value must fail closed, not fall back.
- **Never parse money into floats or re-derive an amount.** Decimal strings end-to-end (§5.4).
- **Never accept a balance, lane name, or fee on the wire.** `SessionSubmitInput` is exactly `{ teacherId, intent }` (`backend/types/classes/session.types.ts:39-42`) and the mutation maps it field-by-field (`session-lifecycle.mutation.ts:127-131`) — mass-assignment of price is structurally impossible.
- **Never hand-edit ledger rows in tests.** `teacher_transaction` UPDATE/DELETE raise at the DB layer (`backend/db/migration/3-immutability-triggers.sql:87-113`); the ONLY sanctioned test cleanup rides `withImmutabilityTriggersSuspended(["teacher_transaction"])` (`test/helpers/db-cleanup.ts:229`).
- **Never replace the guarded predicate with an event/bookkeeping table.** Exactly-once rests on the `fee_held = true` predicate plus the claim table for requests (§3.3); a parallel mechanism would create a second arbiter to keep in sync (see D1, §8).

## 7. GraphQL Surface & Security Posture

- **Role gates (BFLA):** `createSession` is STUDENT-only via an explicit `$all` conjunction (`session-lifecycle.mutation.ts:98-103`); `confirmSessionCompletion` is authenticated with the participant predicate living service-side (`:306-308`); `resolveSessionDispute` is ADMIN-only (`:270-275`) with a service-side governance re-assertion (`session-lifecycle.service.ts:624`); `myWallet` / `requestWithdrawal` are TEACHER-only (`wallet.query.ts:52-57`; `wallet.mutation.ts:62-67`).
- **Oracle-safe denials (BOLA):** for every verb a missing row and a foreign row are indistinguishable to the caller, arm by arm: the confirm arm throws the localized `SESSION_NOT_FOUND` via `rejectUnknownCaller` (`session-lifecycle.confirmation.ts:66-76`) fed by the classifier (:135-138); the cancel/dispute arms route zero-row and foreign-caller misses through `rejectTransitionMiss` → `rejectSessionNotFound` (`session-lifecycle.transitions.ts:76-87,161-174`); the read arm null-folds both cases to the identical `null` (`getSessionById`, `session-lifecycle.queries.ts:41-58`). Proven end-to-end by the foreign-caller journey legs (`session-dual-confirmation.journey.test.ts:492`; `session-lifecycle-denials.journey.test.ts:319`).
- **Server-owned pricing (BOPLA):** §6, rows 6–7.
- **Query-depth / complexity posture (as shipped):** there is **no global GraphQL depth- or complexity-limiting ValidationRule**. The real compensating guards are the transport body cap `MAX_GRAPHQL_BODY_BYTES = 2_000_000` before the engine (`backend/lib/gateway/transport-guard.ts:49`, enforcement `assertWithinBodyLimit` :108-113) and introspection disabled in production (`app/api/graphql/route.ts:49`). A rate-limit mechanism also exists on the route (`app/api/graphql/route.ts:26`; posture documented at `app/api/graphql/rate-limit.ts:16`), but it is currently a **fail-open stub that always allows** (`backend/lib/ratelimit.ts:75-87`) — requests are **not** effectively rate-limited today, so it must not be counted as an active guard. Shape-level SDL pins prove flat bounded objects. The money mutations are single-entity guarded operations with flat argument shapes, so there is no unbounded nesting channel over the escrow surface — but any belt-and-braces depth limiter (and a real rate limiter) is a decision for the Financial Safety Verification ticket (§8, D4), not something to assume exists.
- **Known-dirty corner of the SDL freeze (as shipped):** the frozen whole-root operation inventories in `backend/graphql/test/schema-surface.test.ts:747` (mutation-set pin) and `backend/graphql/test/sdl-static-assertions.test.ts:354-356` (frozen mutation list) and :359-361 (frozen query list) lag behind root fields shipped by OTHER surfaces (`purchaseVerificationPlan`; `adjustTeacherWallet` / `approveWithdrawal` / `rejectWithdrawal`; the `admin*` finance queries) — refreshing those pins is surface-freeze housekeeping owned outside this domain. Every escrow/wallet-specific pin passes, including the `myWallet`/`WALLET_QUERY_FIELDS` additions pin (`schema-surface.test.ts:644-688`) and the committed-SDL contains-pins (:1883-1898), and the codegen-sync check proves the committed artifact matches the built schema.

## 8. Deferred Items & Boundary Hand-offs

Two deferrals are ledgered for this domain — D1 and D2 below, with the related-surface follow-ups D3/D4 summarized at the end of this section; the owning tickets are anchored in `docs/planning/TICKETS.md` — and neither may be resolved casually on this surface:

- **D1 — runtime escrow idempotency-key contracts stay type-level.** `EscrowTriggerContract` / `WalletCreditContract` / `EscrowReleaseContract` each declare an `idempotencyKey` (`backend/types/contracts/session-completion-escrow.contract.types.ts:34-80`; declarations at :34, :46, :73 with the key at :38, :55, :79), but the runtime credit path consumes none of them — exactly-once rests on the settle predicate (:448) and is race-proven both directions (§3.3). Behavioral substitutes already pinned: replay arms + claim-table rollback (§3.3, rows 2/4/8). Adopting a runtime key belongs to the **Financial Safety Verification ticket** (`docs/planning/TICKETS.md:2985-3026`).
- **D2 — no `WalletInsertType` / `WalletReturnType` / `TeacherTransactionInsertType` / `TeacherTransactionReturnType` exports exist** (only Select/View shapes: `backend/types/billing/wallet.types.ts:4-16`, `backend/types/billing/teacher-transaction.types.ts:3`). Adding unused type exports now would trip the unused-code gate; the first consumer ticket adds them (expected: the **Teacher Withdrawal Workflow**).

Related-surface follow-ups recorded alongside (same ledger, external owners): **D3** — the stale frozen-root-inventory SDL pins of §7 (surface-freeze maintainers); **D4** — the absent global depth/complexity limiter of §7 (Financial Safety Verification).

**Boundary hand-offs for dependent tickets:**

1. **Teacher Withdrawal Workflow** (`docs/planning/TICKETS.md:1799-1845`) — consumes `requestWithdrawal` + `myWallet` exactly as verified here and inherits INV-W1's debit discipline (`balance >= amount` in the predicate, `wallet.service.test.ts:237`). The reserve-at-request / settle-on-decision settlement model, the pending-withdrawal predicate, and the immutability amendment are canonical in `docs/billing/admin-financial-auditing.md` §2–§3 — a withdrawal re-uses that model; it must NOT invent a second settlement path over the earning side.
2. **Admin-Ordered Re-Evaluation (Teacher Wallet Deduction)** (`docs/planning/TICKETS.md:2143`) — deducts the re-evaluation fee from `wallet.balance` as a withdrawal-type ledger row. It must ride the same guarded debit + validation matrix (denial = localized `insufficientBalance`, `WALLET_INSUFFICIENT_FUNDS` posture, zero committed rows — `wallet.service.test.ts:208`), write exactly one `audit_logs` row in the same transaction, and never touch `total_earning` (a deduction spends the balance; it does not rewrite lifetime earnings — the bonus/debit vocabulary in `docs/billing/admin-financial-auditing.md` §4).
3. **Financial Safety Verification** (`docs/planning/TICKETS.md:2985-3026`) — the adversarial layer over this substrate: double-spend races, escrow-integrity partitions, ledger immutability attacks, and the wallet-identity recompute (`balance = Σcompleted earnings − Σcompleted withdrawals`). Its probes (`docs/billing/financial-safety-verification.md` §2) already cover the double-spend and replay chaos anchored in §3.3; it also owns the D1 runtime-key decision and the D4 depth-limit decision. If a probe fails, the finding is a defect escalated and fixed minimally — the invariants in §4 are permanent CI-enforced contracts, not prose.

## 9. Rollout Summary

Nothing to deploy: the substrate and every rule above are already shipped, and the executable evidence is the standing suite tiers (all green at verification time: service 72 pass, wallet 9, repository 69; journeys 11 + 10 + 6; GraphQL wire pins 36). Future tickets touch behavior only through the rules in §5 and the hand-offs in §8, and re-run the tiers they touch:

| Tier | Command | Suites |
|---|---|---|
| Service (rollback tx) | `bun run test/scripts/run-test.ts <path>` | `backend/services/classes/session-lifecycle.service.test.ts`, `backend/services/billing/wallet.service.test.ts` |
| Repository (rollback tx) | same | `backend/db/test/repo/classes/session.repository.test.ts` (+ the wallet repository CHECK-probe suite) |
| Journey (committed fixtures) | same | `test/workflows/sessions/session-dual-confirmation.journey.test.ts`, `session-lifecycle.journey.test.ts`, `session-lifecycle-denials.journey.test.ts` |
| GraphQL / SDL pins | `bun run test:graphql` (or the runner per file) | `backend/graphql/test/session-lifecycle-mutations.test.ts`, `schema-surface.test.ts`, `sdl-static-assertions.test.ts` |

## 10. Related Documents

- `docs/sessions/session-lifecycle.md` — the session state machine and the hold-as-debit ruling + trial-first ladder (§4–§5) that this document's escrow timing binds to.
- `docs/billing/segregated-session-balance.md` — the lane model, the credit-vs-hold vocabulary split, and the ratified D1–D4 decisions behind the student side.
- `docs/billing/admin-financial-auditing.md` — withdrawal settlement (INV-W5), ledger immutability amendment (INV-W6), and the admin adjustment vocabulary consumed by the Re-Evaluation Deduction hand-off.
- `docs/billing/financial-safety-verification.md` — the adversarial probe inventory over these invariants (Financial Safety Verification hand-off).
- `docs/specs/state-machine-invariants.md` — the canonical invariant registry (§4.2 balance lanes, §5.3 wallet) mapped in §4.
- `docs/workflows/03-session-lifecycle-escrow.md` — the workflow-level escrow flow (first vs. subsequent sessions, dual confirmation).
- `docs/students/free-trial-provisioning.md` — the `balance_trial` grant-once pattern behind the ladder's trial-first leg.
- `docs/graphql/domain-error-extensions-code.md` — the `extensions.code` wire shape behind the denial matrix (`INSUFFICIENT_BALANCE`, `SESSION_NOT_FOUND`, `SESSION_INVALID_TRANSITION`, `DUPLICATE_REQUEST`).
- `docs/planning/TICKETS.md` — the source tickets: Fee Escrow (:1704), Teacher Wallet Crediting (:1753), Withdrawal Workflow (:1799), Re-Evaluation Deduction (:2143), Financial Safety Verification (:2985).
