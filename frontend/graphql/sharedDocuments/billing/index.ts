/**
 * Billing-domain shared GraphQL documents barrel.
 *
 * Re-exports the teacher wallet documents (DEV3-013): the self-wallet
 * read (`myWallet`) and the payout write (`requestWithdrawal`).
 */
export * from "./wallet.documents";
