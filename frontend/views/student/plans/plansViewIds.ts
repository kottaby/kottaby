/**
 * plansViewIds — testid anchors for the student plan-catalog view (the
 * per-view testid module convention; the suites pin these).
 */

/** Per-card testid prefix (`-<planId>` suffix per card). */
export const PLAN_CARD_TEST_ID_PREFIX = "student-plan-card";

/** Per-card Buy CTA testid suffix. */
export const PLAN_CARD_BUY_SUFFIX = "-buy";

/** Confirm-dialog testid anchor. */
export const PURCHASE_CONFIRM_DIALOG_TEST_ID = "purchase-confirm-dialog";

/** Catalog container testid anchor. */
export const PLANS_CATALOG_TEST_ID = "student-plans-catalog";

/** Empty-state testid anchor. */
export const PLANS_EMPTY_TEST_ID = "student-plans-empty";

/** Error-alert testid anchor. */
export const PLANS_ERROR_TEST_ID = "student-plans-error";

/** Loading-skeleton testid anchor. */
export const PLANS_SKELETON_TEST_ID = "student-plans-skeleton";
