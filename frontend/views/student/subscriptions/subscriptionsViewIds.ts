/**
 * subscriptionsViewIds — testid anchors for the my-subscriptions view (the
 * per-view testid module convention; the suites pin these).
 */

/** Container testid anchor. */
export const SUBSCRIPTIONS_VIEW_TEST_ID = "subscriptions-view";

/** Loading-skeleton testid anchor. */
export const SUBSCRIPTIONS_SKELETON_TEST_ID = "subscriptions-skeleton";

/** Query-error alert testid anchor. */
export const SUBSCRIPTIONS_ERROR_TEST_ID = "subscriptions-error";

/** Empty-state testid anchor. */
export const SUBSCRIPTIONS_EMPTY_TEST_ID = "subscriptions-empty";

/** Per-row testid prefix (`-<id>` suffix per row). */
export const SUBSCRIPTION_ROW_TEST_ID_PREFIX = "subscription-row";

/** Per-row status chip testid suffix. */
export const SUBSCRIPTION_ROW_STATUS_SUFFIX = "-status";

/** Per-row failed-guidance testid suffix. */
export const SUBSCRIPTION_ROW_GUIDANCE_SUFFIX = "-guidance";
