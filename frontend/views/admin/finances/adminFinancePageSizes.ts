/** Page sizes of the admin finance surfaces (the directory default windows). */

/** Payments page size — the directory surfaces' default window. */
export const PAYMENTS_PAGE_SIZE = 10;
/** Wallet-transaction page size. */
export const WALLET_PAGE_SIZE = 10;
/** Withdrawal-queue page size. */
export const WITHDRAWALS_PAGE_SIZE = 25;
