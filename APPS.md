# APPS.md — Application Registry

> Auto-generated by `stakpak init` on 2026-06-22. Verified by user.
> Last updated: 2026-06-22
>
> This is a living document. The agent updates it as apps change.
>
> **Markers:** `[unconfirmed]` = needs investigation · `[issue]` = known issue or stale info · `[unreachable]` = previously found but no longer reachable · `[removed]` = no longer detected

---

## Applications

### kottaby (Kottaby LMS)

- **Description**: Learning Management System for managing classes, students, teachers, billing, scheduling, and communications. Arabic/English bilingual (RTL support).
- **Type**: web-server (full-stack monolith — Next.js serves both frontend UI and embedded GraphQL API)
- **Language/Framework**: TypeScript / Next.js 16.2.9 + React 19 + Apollo Server 5 + Pothos GraphQL + Drizzle ORM
- **Source**: `C:\Users\PC\Projects\kottaby` — `git@github.com:ahmedhosnypro/kottaby.git` (branch: `master`)
- **Entry Point**: `next start` → HTTP server on `:3000` — GraphQL at `/api/graphql` (route handler at `app/api/graphql/route.ts`)
- **Build**: `bun run build` → `next build` → `.next/` output. No production Dockerfile exists.
- **Health Check**: `HEAD /api/graphql` → 204 No Content (lightweight liveness probe)
- **Package Manager**: Bun

#### Dependencies

| Type | Name | Provider | Connection | Notes |
|------|------|----------|------------|-------|
| Database | kottaby (postgres) | Local PostgreSQL (port 5432) | `DATABASE_URL` env var → `postgresql://postgres:***@localhost:5432/kottaby` | Drizzle ORM, 20 schema modules |
| Cache / Rate-Limit | Redis | Local Redis (port 6379) | `REDIS_URL` env var → `redis://localhost:6379` | Docker Compose at `.container/redis/`. Alternative: Upstash Redis (serverless) |
| Cache (alt) | PostgreSQL UNLOGGED tables | Same PG as above | `CACHE_PROVIDER=postgres` | Fallback when Redis unavailable |
| File Storage | local disk | `./storage/public/` | `STORAGE_PROVIDER=local` | Alternative: GCP Cloud Storage, Vercel Blob. S3/R2 stubs exist but not implemented |

#### Environment Variables

| Variable | Required | Source | Description |
|----------|----------|--------|-------------|
| `DATABASE_URL` | yes | `.env` | PostgreSQL connection string |
| `DATABASE_ENCRYPTION_KEY` | yes | `.env` | AES-256-GCM column-level encryption key |
| `JWT_SECRET` | yes | `.env` | JWT signing secret (≥32 chars in production) |
| `GRAPHQL_ENCRYPTION_KEY` | yes | `.env` | GraphQL binary serialization key |
| `DB_PROVIDER` | no | `.env` | `postgres` (default) or `neon` (serverless PG) |
| `REDIS_PROVIDER` | no | `.env` | `local` (default) or `upstash` |
| `REDIS_URL` | no | `.env` | Redis connection URL (default: `redis://localhost:6379`) |
| `CACHE_PROVIDER` | no | `.env` | `redis` (default) or `postgres` |
| `STORAGE_PROVIDER` | no | `.env` | `local` / `gcp` / `vercel` / `s3` / `r2` |
| `LOCAL_STORAGE_PATH` | no | `.env` | Local storage directory (default: `./storage/`) |
| `NEXT_PUBLIC_BASE_URL` | no | `.env` | Public base URL for signed URLs (default: `http://localhost:3000`) |
| `ALLOWED_ORIGIN` | no | `.env` | CORS allowed origin |
| `NODE_ENV` | no | `.env` | `development` / `production` / `test` |
| `CRON_SECRET` | no | `.env` | Auth secret for cron endpoints (`/api/storage/cleanup`, `/api/fx/refresh`) |
| `FX_PROVIDER` | no | `.env` | FX rate provider: `openexchangerates` (USD base) or `fixer` (EUR base, normalised to USD hub) |
| `OPENEXCHANGERATES_APP_ID` | conditional | `.env` | Required when `FX_PROVIDER=openexchangerates` |
| `FIXER_API_KEY` | conditional | `.env` | Required when `FX_PROVIDER=fixer` |
| `ADMIN_EMAIL` | no | `.env` | Initial admin account email |
| `ADMIN_PASSWORD` | no | `.env` | Initial admin account password |
| `GCP_PROJECT_ID` | conditional | `.env` | Required when `STORAGE_PROVIDER=gcp` |
| `GCP_BUCKET_NAME` | conditional | `.env` | Required when `STORAGE_PROVIDER=gcp` |
| `GCP_SECRET_ID` / `GCP_SECRET_VERSION` | conditional | `.env` | GCP Secret Manager for storage credentials |
| `UPSTASH_REDIS_REST_URL` / `UPSTASH_REDIS_REST_TOKEN` | conditional | `.env` | Required when `REDIS_PROVIDER=upstash` |
| `BLOB_READ_WRITE_TOKEN` | conditional | `.env` | Required when `STORAGE_PROVIDER=vercel` |
| `GOOGLE_FONTS_API_KEY` | no | `.env` | Optional Google Fonts API access |
| `NEXT_PUBLIC_IS_DEMO` | no | `.env` | Demo site mode flag |

#### Runtime

| Environment | Runtime | Location | Replicas | Endpoint |
|-------------|---------|----------|----------|----------|
| dev | Local (Bun + Next.js dev server) | Windows machine (this host) | 1 | `http://localhost:3000` |
| prod | **Not deployed** | — | — | — |

#### Delivery Pipeline

- **CI**: [issue] No CI/CD workflows defined. `.github/` contains only `instructions/` (Copilot instructions). No GitHub Actions, no deployment pipeline.
- **Build**: `bun run build` → `next build`
- **Deploy**: Vercel-ready config at `vercel.json` defines a single cron: `GET /api/fx/refresh` daily at `0 6 * * *` UTC (FX rate refresh). No other deployment automation yet.
- **Cron Jobs**: `vercel.json` schedules `/api/fx/refresh` daily at 06:00 UTC. Auth: `CRON_SECRET` (Vercel auto-injects `Authorization: Bearer ${CRON_SECRET}`). `/api/storage/cleanup` still lacks a scheduler.
- **Rollback**: N/A — no production deployment

#### Observability

- **Metrics**: None configured — no Prometheus, no APM
- **Logs**: Custom logger (`backend/lib/logger`) → stdout (console). No external log aggregation.
- **Errors**: None configured — no Sentry, no error tracking service
- **Alerts**: None
- **Rate Limiting**: Built-in — 100 req/min GraphQL, 10 per 15min auth endpoints. Fails open on cache errors.

#### Known Issues & Notes

- [issue] **No production deployment** — app runs only locally. No Vercel config, no Dockerfile for production, no CI/CD pipeline.
- [issue] **Dead Postgres compose** — `.container/postgres/docker-compose.yml` is fully commented out. Postgres runs natively (not containerized). New devs must install PG manually.
- [issue] **Stale config paths** — `graphql.config.yml` and `apollo.config.json` reference `client/graphql/generated/schema.graphql` but actual path is `frontend/graphql/generated/schema.graphql`.
- [issue] **No health monitoring in production** — no Sentry, no APM, no alerting. When deployed, there's no way to know it's broken.
- [issue] **No secrets management** — `.env` file only. No vault, no parameter store. Secrets are local-only with no rotation mechanism.
- [issue] **Signed URL cleanup cron has no external scheduler** — `/api/storage/cleanup` gated by `CRON_SECRET` exists but no scheduler calls it in production. FX rate refresh cron (`/api/fx/refresh`, daily 06:00 UTC) IS scheduled via `vercel.json` since the FX ingestion plan landed; storage cleanup remains unschedulable on Vercel cron.
- [inferred] **Serverless-ready architecture** — env detection for Vercel/Netlify/Lambda/K8s/Cloudflare/Railway suggests intent for multi-platform deployment, but none activated.

---

## Backing Services

| Name | Type | Provider | Region | Used By | Managed By |
|------|------|----------|--------|---------|------------|
| PostgreSQL (local) | PostgreSQL | Native install on Windows | localhost:5432 | kottaby | Manual / not containerized |
| Redis (local) | Redis | Docker Compose | localhost:6379 | kottaby (cache + rate limiting) | `.container/redis/docker-compose.yml` |

## Traffic & Routing

- **Current**: Direct access at `http://localhost:3000` — no reverse proxy, no DNS, no TLS
- **Production**: N/A — not deployed

## Cloud Accounts

*None detected. No AWS profiles, GCP projects, Azure subscriptions, or Kubernetes contexts configured on this machine.*

## Kubernetes Clusters

*None detected.*

## Infrastructure as Code

- **Tool**: None — no Terraform, Pulumi, or CloudFormation
- **Container orchestration**: Docker Compose (Redis only) at `.container/redis/docker-compose.yml`
- **Dev environment**: `.devcontainer/Dockerfile` (Debian-based dev container with Node.js, zsh, PG client)

## Secrets Management

- **Tool**: `.env` files (not committed to git)
- **Pattern**: `.env` → `process.env` → Drizzle / Apollo / storage adapters
- **Files**: `.env` (active), `.env.example` (template), `.env.redis` (Redis-specific variant), `.env.test` (test overrides)

## Orphan Cloud Resources

*None — no cloud accounts configured.*

## Undeployed Repos (no matching cloud resource)

| Repo | Language | Notes |
|------|----------|-------|
| `C:\Users\PC\Projects\cgvs` | TypeScript/Next.js 16 | Same stack as kottaby — appears to be sibling/fork project. Out of scope. |
| `C:\Users\PC\Projects\ares` | TypeScript/Next.js + .NET | Car rental platform. Out of scope. |
| `C:\Users\PC\Projects\9router` | TypeScript/Next.js + Express | Network router dashboard with Dockerfile. Out of scope. |
| `C:\Users\PC\SIde_Projects\bookcars` | Node.js monorepo | Car rental booking (fork). Has Docker Compose. Out of scope. |
| `C:\Users\PC\SIde_Projects\mo3lmti` | TypeScript/Next.js 16 | LMS variant. Out of scope. |
| `C:\Users\PC\SIde_Projects\livelms\livelms` | PHP/Laravel | Laravel LMS. Out of scope. |
| `C:\Users\PC\SIde_Projects\doqment` | TypeScript/Node.js | Document management tool. Out of scope. |
| `C:\Users\PC\IdeaProjects\taskly_base` | Dart/Flutter | Task management app (base). Out of scope. |
| `C:\Users\PC\IdeaProjects\taskly_rest` | Dart/Flutter | Task management app (REST variant). Out of scope. |
| `C:\Users\PC\IdeaProjects\taskly_sqlite` | Dart/Flutter | Task management app (SQLite variant). Out of scope. |
| `C:\Users\PC\IdeaProjects\sce_flutter_lab` | Dart/Flutter | Flutter experiment. Out of scope. |

## Orphan Backing Services

| Resource | Type | Location | Notes |
|----------|------|----------|-------|
| PostgreSQL on :5432 | PostgreSQL | localhost | Running natively (not in Docker). Postgres docker-compose is commented out. No backup strategy detected. |

---

*Last refreshed by Stakpak on 2026-06-22*
